# Daegeum reference contour evidence — 2026-09-25

## Result

The read-only scan of the 192-source feature bundle produced one committed B2
reference export with the strict status:

```text
reference_shape_unreviewed
```

Artifact:

```text
tools/daegeum-vibrato-reference/reference_shape_unreviewed.ngc-20260925.json
```

Artifact SHA-256:

```text
e584ff8c9142ed51223681d30ba5aa3236f29a7c808bcb5b40b208cb0f886bd2
```

Reproduction command:

```sh
/tmp/durango-bgm-rnd-venv/bin/python \
  tools/daegeum-vibrato-reference/reference_yoseong.py \
  --bundle _bgm_rnd/daegeum-transition-bank-ngc-20260924-205600 \
  --output /tmp/reference_shape_unreviewed.ngc-20260925.json \
  --top 1
```

A clean rerun was byte-identical to the committed artifact (`cmp` exit 0,
same SHA-256 above).

## Scan evidence

- catalog sources loaded: 192
- feature tracks loaded: 192
- contiguous voiced runs: 2,033
- internal windows considered: 38,824
- windows passing every signal gate before overlap suppression: 3,720
- selected automatic candidate: `ref_56a286f5764ffe3d`
- source ID: `src_bce31fef7cfe06b2`
- catalog-relative source: `extend-001537.wav`
- catalog-declared source SHA-256:
  `bce31fef7cfe06b2e7559c308516ea803fececb16037810bdd23e6741ed360ea`
- feature index range `[966, 1116)`; approximate feature-window edges
  `9.680–11.180 s`
- feature NPZ SHA-256:
  `938b5fd4e1d75a64ca13ad76fd90250caa88cc2734c7b8fdf9c374c75918d453`
- automatic F0-proxy rate: `4.623495 Hz`
- automatic robust half-depth: `28.606003 cents`
- autocorrelation periodicity: `0.985732`
- F0-proxy band-power ratio: `0.996396`
- median voicing-confidence proxy: `0.987345`
- RMS proxy median / IQR: `-28.629383 dBFS / 2.143778 dB`

The exported 65-point shape contains normalized time, pitch-cent residual and
RMS relative to the candidate median. It stores no absolute path or raw audio.

## Interpretation gate

These measurements support only the existence of a strong, continuous,
periodic F0-proxy window. They do **not** establish any of the following:

- that the motion is human-confirmed 요성/농현;
- that it has a 경기민요, 아리랑, or any other approved style label;
- that its phrase role, breath, articulation, fingering, or attack/release is
  known;
- that the surrounding source phrase fits the target score;
- that source rights permit training, distribution, or game use.

The source catalog says `unverified_local_rnd_only`. The extractor did not open
or rehash the raw WAV in this run; the source SHA is explicitly identified in
JSON as catalog-declared. The actual feature metadata and NPZ were hashed in
the run.

Accordingly, the contour may feed an **offline B2 audition only** while keeping
`reference_shape_unreviewed`. It is not a learned style model, training item,
or game asset.

## Verification

```text
test_f0_and_midi_proxy_identity_mismatch_is_rejected ... ok
test_metadata_identity_mismatch_and_source_bundle_write_fail_closed ... ok
test_non_periodic_straight_tone_is_blocked_without_shape ... ok
test_note_step_pattern_is_not_accepted_as_sustained_note_proxy ... ok
test_output_is_non_overwriting_and_window_parser_is_strict ... ok
test_periodic_internal_window_exports_unreviewed_reference_only ... ok

Ran 6 tests in 0.212s
OK
```

The tests cover a valid synthetic 4 Hz continuous curve, a straight-tone
blocked result, rejection of periodic discrete note steps, F0/MIDI identity
mismatch, source metadata mismatch, attempts to write into the source bundle,
output overwrite refusal, and absence of absolute paths in the result.
