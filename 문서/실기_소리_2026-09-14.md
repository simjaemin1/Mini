# (옮김) 실기 — 소리 한 바퀴 (재민 귀)

**정본은 `public/문서/실기_소리_2026-09-14.md` 로 옮겼다.** 내용은 한 글자도 안 바뀌었다(`git mv`).

**왜 옮겼나 [T323 2026-09-19 · 세션9]** — 소리판(`public/sfx-board.html`)이 이 표를 **읽어서**
키 행에 "어디서 나야 하나"를 같이 보여야 하는데, `Dockerfile.central` 이 배포 이미지로 담는 것은
**`server` · `public` · `sim` 셋뿐**이다. `문서/` 는 애초에 컨테이너에 안 올라간다(`.dockerignore` 와도 무관 —
`COPY` 목록에 없다). 그래서 이 자리에 두면 **배포된 호스트에서 404** 이고, 페이지의 그 칸이 영영 빈다.

사본을 만들지 않았다(`public/` 과 `문서/` 에 같은 표가 둘 있으면 반드시 갈린다). **자리를 옮기고
여기엔 이 쪽지만 남긴다** — 이 경로를 가리키는 옛 카드·보고(T291·T303·T320·T323 …)가 길을 잃지 않게.
