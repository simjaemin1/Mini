# 회부 — 실서버에서 **인구 0 마을 곳간이 음수로 내려간다**

> 2026-08-03e 배치 12. **구현 안 함 · 회부.** 배치의 유일한 미해결 항목.
> 실클라 E2E(`scripts/e2e-village.js`)가 잡았고, 원인을 **econ 층 밖**까지 좁혔다.

---

## 1. 무엇을 봤나 — 실측

플레이어가 마을을 세우고(인구 0) 곳간에 베리를 계속 부으면서 관측한 값
(`VILLAGE_DAY_MS=500` · 60일마다 표본 · 투입 베리 200 ≈ 식량 환산 80):

```
[대기] day  80 · 식량 -69.3/15 · 인구 0
[대기] day 141 · 식량  12.6/15 · 인구 0
[대기] day 202 · 식량  83.2/15 · 인구 0
[대기] day 265 · 식량  12.5/15 · 인구 0
[대기] day 328 · 식량 -45.6/15 · 인구 0
[대기] day 390 · 식량  12.5/15 · 인구 0
[대기] day 452 · 식량 -46.0/15 · 인구 0
```

`식량` = `totalFoodEquivalent(v)`(엔진 정본) · `15` = 회복 문턱(`recoveryFoodThreshold`).

**두 가지가 이상하다:**
1. 투입량과 거의 같은 크기(≈70~80)가 **주기적으로 빠진다.**
2. **음수가 된다.** 곳간에 없는 양이 빠진다는 뜻이다 — 부패도 소비도 0 아래로는 못 간다.

**결과**: 회복 창(`day % 50 === 0 && day >= 100`)마다 문턱 아래에 걸려 **첫 주민이 영영 안 온다.**
재민 확정 (마)가 엔진에서는 성립하는데(하네스 42/0) **실서버 화면에서는 성립하지 않는다.**

## 2. 어디까지 좁혔나

| 후보 | 판정 | 근거 |
|---|---|---|
| **econ 교역** | **결백** | 이번 배치에서 인구 0 마을을 발주 목적지에서 제외했다. 하네스 ②-d: 부자 마을 둘 사이에 빈 터를 두고 200일 — 캐러밴 **147회**가 오가는데 빈 터 곳간 **변동 0 · 음수 0** |
| **econ 그 밖 전체** | **결백** | 같은 하네스가 `tickWorldV2` 200일 전체를 돌린다(부패·자급·이벤트·회복 포함). 변동 0 |
| **전쟁 층** | **아님(이 판에서)** | 그 E2E 실행 중 `⚔️ 전쟁` 로그가 **한 줄도** 안 찍혔다 |
| **서버 층** (`villages.js` 생활층 · 곳간 물리화 `guild-treasury` · 저장/복원) | **남았다** | 위 셋을 뺀 나머지. ★**어느 것인지는 아직 안 쟀다 — 추측으로 적지 않는다** |

## 3. 다음에 무엇을 재야 하나 (계측 계획)

1. **쓰기 지점 특정** — 배치 5 의 석재 원장 선례(Proxy 로 `file:line` 귀속)를 곳간에 건다.
   대상은 플레이어 마을 하나뿐이라 배치 9 때처럼 느려지지 않는다(그때는 전 마을 다중 재화였다).
2. **의심 순서** — `_lifeDaily`(생활층 일일 훅) → 곳간 물리화(`guild-treasury.syncGranary` ·
   `_granStock`) → `serializeEcon`/`restoreEcon` 왕복. 인구 0·곳간 없음·영토 49셀이라는
   **NPC 마을에는 없던 조합**이 이 셋 중 하나의 전제를 깨고 있을 가능성이 높다.
3. **판정 기준** — 인구 0 마을의 곳간은 **플레이어가 넣은 것 외에는 변하지 않아야 한다**
   (부패는 예외 — 다만 0 아래로는 못 간다). 실서버에서 200 게임일 관측해 그걸 확인한다.

## 4. 왜 이번 배치에서 안 고쳤나

원인을 아직 **재지 못했다.** 이 프로젝트가 배치 7 에서 정확히 그 자리에서 오진을 냈다 —
전수라고 믿은 스캔으로 "지도에 철 0개"를 보고했다가 뒤집혔다. 그 뒤로 규칙이 생겼다:
**계측기도 사본 금지 대상이고, 실측 없는 보고는 금지다.**
그래서 여기서는 "전쟁일 것이다 / 생활층일 것이다"를 적지 않고 **좁힌 범위만** 남긴다.

⚠**엔진 층은 이미 실측으로 안전하다** — 세 궤적(빈 곳간 영영 0 · 식량 15 → 첫 주민 → 성장 ·
식량 끊김 → 아사 → 소멸(P))이 `test-village-found.js` 42/0 으로 확인됐고, NPC 세계는
실지도 3시드 800일 **바이트 완전 동일**이다. 막힌 것은 **서버 층의 곳간 한 곳**이다.
