# 회부 — 쓰러짐·구조·사망 다음 층 (T43 · 2026-09-02)

> §12 의 쓰러짐 사슬을 실장하면서 **드러났지만 안 고친 것**들.
> 구현 금지 — 재민 판단이 있어야 여는 문이다. 실장 내용은 `인계/B-신체서버.md` 3-아 · `인계/Z-존서버.md` T43.

---

## A. 지시서가 회부로 못 박은 것

### A-1 쓰러짐·구조·업기·사망 **T43 후속**
이 카드가 세운 것은 사슬 **하나**다 — `HP 0 → 구조 창 → (마을 이송 | 야생 사망) → 깨어남`.
그 사슬 위에 얹힐 것들은 아직 없다:

* **구조 동사가 하나뿐이다.** §12 는 *"먹여주기 · 물 먹이기 · 불가로 옮기기 · 붙들기"* 넷을 들었는데
  이 카드는 **업기=붙들기 하나**로 합쳤다. 나머지 셋은 **새 클라 동사**가 필요하다(B-1 · 아래).
* **쓰러진 사람이 할 수 있는 게 없다.** 지금은 기다리거나 포기하거나 둘뿐이다. §12 에 "스스로
  기어간다"는 없지만, 있으면 구조를 기다리는 3분이 다른 시간이 된다. **설계 판단.**
* **여럿이 함께 구하면 빨라지는가.** 지금은 한 사람만 업을 수 있다(`_carriedBy` 단일).
  둘이 붙들면 절반이 되는 세계인지 아닌지는 §12 에 없다.

### A-2 후유증 다음 칸
§12 는 *"며칠 게임일 동안 스태미나 상한 감소"* 하나만 말했고 그것만 실장했다(`AFTERMATH_STAM_CAP` 0.55 ·
`AFTERMATH_DAYS` 3 · 연속 회복). 열려 있는 칸:
* **화면이 후유증을 말하지 않는다.** 무들에도 없고 HUD 에도 없다 — 스태미나가 덜 차는 것만 보인다.
  "왜 숨이 덜 붙지"를 플레이어가 읽을 방법이 지금은 없다. (새 패널 0 규약 안에서 풀려면 **무들 한 칸**이다.)
* **죽음이 겹치면?** 후유증 중에 또 죽으면 `deadDay` 가 덮여 **다시 3일**이 된다(누적 아님).
  누적시키면 죽음의 나선이 열린다(§7 이 명시적으로 끊어 둔 것) — 그래서 안 겹치게 했다. **판단 확인 필요.**

### A-3 극단 **도달 전** 단계 재튜닝 — 여전히 실기 후
T44 회부 A-3 그대로다. 이 카드도 디버프 표를 한 자도 안 건드렸다.

---

## B. 이 카드가 새로 만든 판단거리

### B-1 ★★"먹여주기 · 물 먹이기" 에는 **클라 동사가 없다**
§12 의 구조 행동 넷 중 셋은 *"남에게 무엇을 한다"* 인데, 이 세계의 먹기(`eat`)·마시기는
**자기 자신에게만** 걸린다(대상 인자가 없다). 넣으려면:
```
  ① 서버 — `eat`/`drink` 에 대상 pid 를 받는 갈래(사거리·쓰러짐 게이트는 tryRescue 와 같은 것)
  ② 클라 — 인벤에서 "먹여주기" 를 고르는 동사 하나(새 패널이 아니라 기존 우클릭 메뉴 자리)
```
⇒ **클라 접점이 2줄을 넘는다.** 이 카드의 예산(클라 2줄) 밖이라 안 했다. **H/N 영역과 같이 정할 일.**
★그리고 이게 없어도 §12 의 요점은 산다 — 극단 축이 남은 채 일으키면 T44 식이 **다시 눕힌다**
(`test-downed ④` 가 못 박는다). 먹여주기는 "일으킨 뒤 다시 안 눕게 하는" 편의지 사슬의 부품이 아니다.

### B-2 ★★**입은 것은 안 떨어진다** — 바닥템에 장비 인스턴스를 실을 자리가 없다
`_deathDrop` 은 인벤(수량·로트·kg 원장)과 **도구 개체**(내구까지)를 떨어뜨린다. 하지만
**착용 장비(`equipment`/`equipSlots`)는 그대로 몸에 남긴다.** 이유는 규약이 아니라 **자료 구조**다:
```
  바닥템(groundItem) 이 실을 수 있는 것 = { item, n, kg, led, lots, tool:{type,d,max} }
  장비 인스턴스        = { id, type, mat, q, attrs{...}, d, max, slot }   ← 속성·품질이 통째로 더 있다
```
`tool` 칸에 억지로 밀어 넣으면 **주웠을 때 다른 물건이 된다**(품질·속성이 뭉개진 기본품).
⇒ 옳은 순서는 **바닥템이 장비 인스턴스를 그대로 싣게 만드는 것**이고, 그건 I(인벤)·F(제작) 영역의
한 배치다. 그 전까지 "몸에 걸친 것은 짐이 아니다"가 이 카드의 **명시적 규약**이다.
(듀랑고도 착용품은 안 떨궜다 — 형태로도 어긋나지 않는다. 다만 그건 결과지 근거가 아니다.)

### B-3 ★★`vitals:true` 의 **부수 효과** — 모든 재접속의 공짜 풀피가 사라졌다
죽음 설계 부록 B-1 이 "별도 판단거리"라고 회부한 그것이다. §12 의 *"로그아웃 ≠ 부활"* 을 실장하려면
`isDown` 만 살릴 수는 없다(HP 0 인데 안 쓰러진 몸 = §0-ⓐ-4 의 그 구멍) ⇒ **HP 도 같이 살아난다**.
```
  종전: HP 30 으로 나가면 → 다시 들어올 때 HP 100          (재접속이 회복 수단이었다)
  지금: HP 30 으로 나가면 → 다시 들어와도 HP 30            (되돌리려면 `vitals` 한 글자)
```
⚠**이건 downed 와 무관한 모든 플레이어에게 걸린다.** 전투에서 다치고 잔 사람이 아침에 다친 채
깨어난다. 그게 옳은 방향이라고 §12 가 말하지만, **HP 회복 수단이 자연 회복 하나로 줄었다**는
사실은 별개다(그리고 그 자연 회복은 아래 B-4 대로 **화면에 안 실린다**).
`test-handoff-body ④` 를 뒤집어 이 새 사실을 못 박아 뒀다.

### B-4 ⚠**HP 자연 회복이 화면에 안 실린다** (T44 회부 B-4 가 그대로 살아 있다 · 이제 더 아프다)
T44 때는 "굶다가 먹으면 화면이 낡는다"였다. 이 카드로 **재접속에도 낡는다** — 다친 채 들어오면
welcome 의 `self.hp` 로 한 번 맞고, 그 뒤 회복은 화면에 안 온다. **N 영역/클라 소관.** 값은 싸다.

### B-5 구조 창 3분의 **실효 구조율**은 여전히 미측정이다
죽음 설계 §0-ⓒ 가 지적한 그대로다. 10초를 3분으로 늘린 근거는 *"소리를 듣고 달려오는 사람"*
(야생 800px ≈ 40초)이지 **실측**이 아니다. 사람이 둘 이상 붙는 라이브가 오기 전엔 못 잰다.
★그때 볼 것은 창의 길이가 아니라 **"쓰러진 걸 남이 아는가"** 다 — 지금 쓰러짐은 그 사람 화면에만
뜨고, 멀리 있는 사람에게는 아무 신호도 안 간다(§12 에 외침·표지가 없다). **설계 판단.**

### B-6 마을 안 **불사**의 경계는 `shelterAt > 0` 이다
추위가 쓰는 완충 정본을 그대로 썼다(사본 금지). 그 술어는 마을 중심에서 멀어질수록 **0 으로 연속
감소**하므로, 경계는 "완충이 0 보다 큰 곳" = 마을 영향권의 **가장 바깥**이다. 즉 **넉넉한 쪽**이다.
좁히려면(예: `> 0.3`) 그건 새 수이고 §12 에 없다 — 안 지어냈다. **판단 확인 필요.**

### B-7 깨어나는 자리가 **사유지 우선**이라 마을이 뒤로 밀린다
사다리는 `사유지 → 길드 → 귀향점 → 가장 가까운 마을 도착 지점`이다(종전 `listRespawnOptions` 순서
그대로 + 4번을 새로 채웠다). 야생 저 끝에 사유지 하나를 세워 두면 **거기서 깨어난다** — 거리와
무관하게. §12 는 *"개인 클레임 → 없으면 가장 가까운 마을"* 이라 이 순서가 맞지만, "가장 가까운"이
사유지에는 안 걸린다는 뜻인지는 안 적혀 있다. **판단 확인 필요.**

---

## C. 이 카드가 **안 건드린** 계약 (오해 방지)

* **econ** — 무수정. 신체·쓰러짐은 플레이어 전용 층이라 3시드 800일 기준선도 비트 동일이다.
* **디버프 표·극단 r 표** — T44 가 세운 값 그대로. 이 카드는 HP 0 **이후**만 더했다.
* **낙하·전투 피해** — `damagePlayer` 는 한 글자도 안 바뀌었다. 바뀐 건 그 **뒤**(쓰러짐이 어떻게 끝나나)뿐이다.
* **클레임·건물** — 사망이 땅을 안 건드린다(제약 C5).
* **지식** — `craftSkill`·`craftLog`·`fishStats`·`oreLedger` 는 사망에서 **한 자도 안 잃는다**(§6 · C6).
