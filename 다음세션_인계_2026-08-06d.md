# durango-mini 인계 배치 21 (2026-08-06d) — 물 셰이더 비용 · 안개 경계 · 자연물 재렌더

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` 와
> `보고_배치20_산과_타일상태.md` 를 읽고** 아래를 순서대로 진행.
> 질문 금지(설계 취향 갈림만 시안 2~3안 첨부 후 네 추천으로 진행), **실측 없는 보고 금지**.
> 기준: `594016a`(배치 20 B 커밋 3개 포함) 이후 — `git log` 로 확인. 없으면 재민에게
> `bash ~/Mini/푸시_번들.command ~/Mini/미푸시_배치20B.bundle` 실행을 요청하고 기다려라.
>
> 푸시: `git push` 시도 → 403(git proxy)이면 **우회 금지**,
> `git bundle create 미푸시_배치21.bundle origin/main..HEAD` → 맥 `~/Mini/` 전달 →
> 재민이 `푸시_번들.command` 실행. **커밋 즉시 번들 백업** — 컨테이너 롤백이 잦다.
> ★번들 파일명은 **세션별로 분리**하라(다른 세션이 덮어써 커밋이 소실된 사고가 실존한다).
> 배포는 재민만. SSH·fly 배포 명령·force push 전부 금지.

## 0. 직전 배치가 남긴 상태 (다 실측된 것)

- 물가 렉의 **CPU 쪽은 잡혔다**: 흐름 텍스처 굽기 388ms → 25ms(걷는 중), 1355ms → 12ms(새 땅).
  `e2e-waterperf` 15/0. 그림은 안 바뀐다(물 21.46% → 21.45%).
- 타일 상태계 5축 실장·검증 완료. `e2e-tilestate` 26/0.
- 회귀 전수 통과: terrain 27 · rooms 55 · cutaway 15 · metallurgy 20 · village 32 · guest 40 ·
  wiring 0 · econ-regress 2,616·651·13,473(비트 동일) · econ-lab-real 6,280·0/51.
- 검은 지그재그 띠 = **기존 안개**로 확정(색이 정확히 rgb(0,0,0)·기울기 ±2px/행·안개 코드 diff 0줄).

## A. 물 프래그먼트 셰이더 비용 — **실측이 먼저다**

`e2e-terrain` 성능 항목에서 물 레이어가 여전히 **141.5 ms/f** 로 찍힌다. 단 이건
헤드리스 SwiftShader(소프트웨어 GL) 값이라 **실 GPU 수치가 아니다**. 초원에서는 0.02ms 라
scissor 는 제대로 듣고 있다.

1. **재민 기기에서 아직 렉이 남았는지부터 물어라**(이건 확인 요청이지 설계 질문이 아니다).
   남았다면 그건 셰이더 쪽이고, 안 남았다면 §A 는 **회부하고 손대지 마라.**
2. 손댈 경우 후보(전부 A/B 손잡이 필수): 셰이더를 **절반 해상도**로 그린 뒤 업스케일 ·
   잔물결 옥타브 축소 · 포말 패스 분리. **그림이 바뀌면 그건 최적화가 아니라 디자인 변경이다** —
   `e2e-waterperf` 의 ⓓ(물 픽셀 비율·평균 |Δ|) 판정을 그대로 통과해야 한다.
3. 하네스는 이미 있다: `scripts/probe-wfcost.js`(비용 분해) · `scripts/e2e-waterperf.js`.

## B. 검은 지그재그 띠 — 안개 경계 처리 [재민 확정 필요]

원인은 확정됐다. **처리 방식만 취향 갈림**이라 시안 2안을 만들어 재민에게 붙여라:

- **ⓐ 계단 지우기** — seen 다이아를 그린 마스크에 블러 한 번. 비용 낮고 변화가 작다.
- **ⓑ 미탐색을 옅은 안개색으로** — 지금은 순수 검정이라 "렌더 구멍"으로 읽힌다.
  지형 실루엣이 어렴풋이 비치면 "안 가 본 땅"으로 읽힌다.

⚠안개 정본(`public/client.js` 시야 레이캐스트·seen 마킹·마스크 합성 323줄)은
배치 19 이전과 diff 0 줄이다 — **건드리는 순간 그 무결성이 깨진다.** 손잡이 필수
(`__terrain19` 에 `fogSoft` 류를 새로 파고, 끄면 지금과 **픽셀 동일**이어야 한다).

## C. 배치 19 C 이월분 — 물가 술 자연화 · 자연물 전면 재렌더

배치 19 에서 미뤄 둔 것이 그대로 남아 있다. 시안 정본은 `scripts/mock-terrain-v4.js`.

1. **물가 술** — 지금은 물가 풀이 딱딱하게 끊긴다. 물가 셀에서 술이 물 쪽으로 눕는 문법.
   ★단 **물가는 각진 블록**이라는 재민 확정을 깨면 안 된다(곡선 스무딩 금지 —
   물길 파기와 문법을 통일하려고 일부러 버린 것이다. `설계_물길파기_관개수로.md` §6).
2. **자연물 12종 재렌더** — 나무·덤불·약초. Blender 캐논 씬 그대로
   (`PPU=45.2548` · `ZSQ=0.8165` · 태양 52°/−35° 3.6 · 렌더 후 좌우 반전 · 앵커 JSON).
   ⚠ColorRamp 는 위치 1.0 에 흰색 요소를 갖고 태어나고 `elements.new()` 뒤 인덱스가 재정렬된다 —
   **색으로 찾아** 교체하라(봉두 255 클리핑 진범, 2회 재발).
3. **들꽃 산포** — 타일 상태계 §3-2 의 "자리별 고유 문턱" 문법을 그대로 쓴다
   (`public/soil-base.js` 의 `hash`). **`Math.random()` 금지** — 프레임마다 흔들린다.
   비옥도 축과 엮어라: 비옥한 뙈기에 들꽃이 더 돋는 게 자연스럽다(축이 이미 있다).

## D. `scripts/e2e-mountain.js` — 배치 20 §C 미완분

배치 20 A(장벽 세그먼트)는 실장됐는데 전용 하네스가 없다. 자명 통과 금지 규약대로:

- ⓐ 세그먼트가 **실제 바위 셀 위에만** — 셀이 0 인 고개 상자에 산 픽셀 0(반례)
- ⓑ **폭 정합** — 넓은 밴드 상자 vs 좁은 밴드 상자의 산 픽셀 폭 비율
- ⓒ **파괴 후 재계산** — 셀 제거 → 그 상자 산 픽셀 소멸 + 캡 등장
  (⚠서버에 산 부수기 메커니즘이 **없다** — `Soil.markMountainRuin` 규격만 있다.
   렌더 재계산만 검증하고 메커니즘은 회부한다)
- ⓓ 결정론(`__mtDbg` · 시계 정오 앵커)
- 상자 잡기는 `e2e-tilestate` 의 `__cellScreen`/속살 상자 수법을 그대로 베껴라 —
  거기서 함정 3개를 이미 밟아 두었다(지붕 위 상자 · 바깥 사각형 귀퉁이 · 카메라 미끄러짐).

## E. 검증 · 보고 (전부 필수)

- 신설 하네스 + 회귀 전수: terrain 27 · rooms 55 · cutaway 15 · metallurgy 20 · village 32 ·
  guest 40 · waterperf 15 · tilestate 26 · wiring 0 ·
  `econ-regress` **2,616 · 651 · 13,473**(비트 동일) · `econ-lab-real 800` **6,280 · 0/51**.
- ⚠E2E 는 **동시 실행 금지**(포트 3010/3020 공유 → EADDRINUSE 거짓 실패).
  `bash scripts/run-regressions.sh` 가 순차로 돈다. ★단 게스트 하네스가 제 central 을 띄워
  러너의 포트 정리와 겹친 사고가 1회 있었다(37/3 → 단독 40/0). **러너부터 고치고 시작해라.**
- 보고 `보고_배치21_*.md`(머리에 재민용 `bash scripts/redeploy-hanbando.sh --central` 한 블록) +
  실화면 `증거_배치21/` + `다음세션_인계.md` 갱신 + 커밋·번들.

## 불변 규칙 (전 배치 공통)

- **지형 데이터 무접촉**: `hanbando-terrain.json` · `server/terrain.js` 술어 · 콜라이더 ·
  자원 스폰 · econ 은 **한 바이트도** 바뀌면 안 된다.
  ★`isWaterCellLocal` 이 느리다고 고치지 마라 — 배치 20 B 는 렌더 쪽에서만 풀었다.
- **`sim/fertility.js` 무접촉** — econ 마을 선별 정적 정본. 이름이 비슷해도
  `server/soil.js`(렌더 상태)와 남남이다.
- 라이브 반영은 `bash scripts/redeploy-hanbando.sh --central`(public/ 변경이면 `--central` 필수).
- 픽셀 하네스는 **시계 정오 앵커**(WRAP_DAY_MS). 렌더 산포에 **`Math.random()` 금지**(셀 해시).
- 커밋은 작업 단위마다. **단계 경계에서 끊고 이월** — 반쪽 구현으로 다음 단계를 넘보지 마라.
- 하네스가 틀렸을 땐 **계측기를 고쳐라. 판정을 완화하지 마라.** 이 프로젝트에서 하네스가
  틀린 게 지금까지 **10건**이고 전부 그렇게 해결했다(배치 20 B 에서만 3건 + 판별력 0 지표 1건).

## 함정 기록 (전부 실제로 밟았다)

- `pkill -f "serve[r]/zone.js"` 는 **자기 셸을 죽인다**(명령줄에 그 문자열이 있다).
  포트 점유 PID 로만 정리해라 — `scripts/dev-boot.sh` 가 그렇게 한다.
- `zone.js` 는 위쪽에서 아래쪽 `const` 를 못 본다(TDZ). `Soil.init` 을 `minedCells` **뒤**에
  둔 이유다. 위로 옮기면 부팅이 죽는다.
- 공간 색인은 구간을 **AABB 로 등록**하고 **동률을 번호로 깨야** 전수 순회와 답이 같다.
  성기게 표본하면 대각으로 스치는 칸을 빠뜨린다(5,000표본 중 33점).
- 흐름 텍스처 예산은 **개수가 아니라 시간**, 순서는 **가운데부터**, 반경은 **40셀**.
  바깥 링까지 채우려 들면 미결이 안 끝나 매 프레임 다시 굽는다(92프레임 미수렴).
- 픽셀 상자는 **뙈기의 속살**만 재라. 바깥 사각형은 네 귀퉁이에 주변 지면이 섞인다.
- 걷기 직후엔 카메라가 **보간으로 미끄러진다**. 두 프레임이 같아질 때까지 기다린 뒤 재라.
- 스폰은 NPC 마을 한복판이다(영토 3,450셀). 지면을 재려면 **걸어 나가야** 한다.
- 컨테이너 롤백 잦음 — 커밋 즉시 번들 백업, 파일명은 세션별 분리.

## 회부 (구현 금지)

- 산 부수기 게임 메커니즘(규격은 `Soil.markMountainRuin` 으로 서 있다) ·
  NPC 가 파괴 지형을 경로에 반영하는 문제 · 다른 바이옴 8종의 램프 아트 ·
  번영도(마을 econ) 별도 시각 축 신설 여부 · 관개수로(`설계_물길파기_관개수로.md` §4·§5 재민 결정 대기) ·
  숲산 톤 심화.
