# durango-mini 인계 배치 20-B 속행 (2026-08-06c) — 타일 상태계: 5축 레이어 합성 + 검증 [A 완료 후 잔여분]

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` · `다음세션_인계_2026-08-06a.md`
> (배치 20 원 인계 — §B·§C 가 이 배치의 명세다) · `08419f5` 커밋 메시지**를 읽고 진행.
> 기준: `08419f5`(배치 20 A — 산 세그먼트 실장 완료) 이후. 질문 금지, 실측 없는 보고 금지.
>
> 푸시: `git push` 시도 → 403 이면 번들(`미푸시_배치20B.bundle` — ★파일명 고정, 다른 세션 번들
> 덮지 마라) → 재민이 `bash ~/Mini/푸시_번들.command ~/Mini/미푸시_배치20B.bundle`.
> 커밋 즉시 번들 백업(컨테이너 롤백 하루 3회 실측).
> ⚠배치 21(자연물)이 병행될 수 있다 — **에셋 폴더(assets/trees·nature)와 소품 산포 코드는
> 손대지 마라.** 이 배치는 타일 '지면' 렌더와 서버 토양치만 만진다.

## A 에서 확정된 것 (반복하지 마라)

- 산 세그먼트는 실장 완료(45종 · 셀 실측 배치 · z 정렬 · mtOff 손잡이 · 능선별 캐시).
- **산 부수기 메커니즘은 서버에 없다** — A 가 전수 실측(wasMountain·breakRock 등 0건,
  isRockCellLocal 은 읽기 전용). `_mtDestroyed` 는 이벤트 자리만 있다.
  ⇒ 이 배치의 §C(파괴 연동)는 **이벤트 규격 정의 + 클라 반응(세그먼트 무효화·지질 플래그)까지만**.
  실제 부수기 게임플레이는 회부 유지(재민 결정 대기).

## B. 타일 상태계 — 원 인계 §B 그대로 (요약)

축 5개(합성 순서): ①기반(바이옴×지질 — `wasMountain` 플래그 신설·DB 영속) ②채굴=번영도(**기존
`mined_cells.prosperity`** 0..1000 lazy 회복 — 읽기 전용) ③비옥도(동적 토양치 **신설** — 초기값
정적 파생·파기↓·리젠↑·산 파괴 셀 800 시작) ④경작(기존 farmland) ⑤답압(기존 §16 길 마모값).

- **연속 수학**(재민 확정 "경계 없이"): 텍스처 층 알파 = smoothstep(값+셀노이즈) — 뙈기 전이.
  소품(자갈·이끼·풀포기) = 자리별 고유 문턱 ±40~90 페이드. 시안 정본:
  `scripts/mock-fertility-gradient.js` · `mock-tile-axes.js`.
- **산터 램프는 별도 축**: 기본 암반, 비옥도는 이끼(450~)→틈새 풀(800~)만. 풀밭 금지.
- 바이옴×지질 테이블 구조 — **한반도 forest 두 축만 실장**, 나머지 8종은 자리만.
- 렌더는 배치 19 의 청크 베이크(`_bakeGroundTile`) 확장 — 상태값 바뀐 청크만 재베이크.
  바위 분기는 A 커밋이 "주석 박힌 블록 한 곳"으로 모아 뒀다.
- 서버: 토양치 테이블 신설(mined_cells 의 lazy 패턴 복제 — cell_key·soil·last_t), 갱신 브로드캐스트
  (청크 묶음). ★`sim/fertility.js`(econ 정적 정본) 무접촉 — 기준선 비트 동일 필수.

## C. 검증 — 원 인계 §C 그대로 (요약)

- 신설 `e2e-mountain.js`(세그먼트=셀 정합·폭 비율·파괴 이벤트 반응·결정론) +
  `e2e-tilestate.js`(청크 국소 재베이크·연속성(500 vs 520)·답압/경작 각 1판정·산터 풀밭 금지 상한).
- 기존 회귀: e2e-terrain 27 · rooms 55 · cutaway 15 · metallurgy 20 · village 32 · guest 40 +
  `econ-regress.js` 비트 동일 + `econ-lab-real.js 800`(6,280·0/51) + wiring-check 0.
  ★E2E 동시 실행 금지(3010/3020 — 순차만). e2e-village 는 폴링 5초 판이 정본.
- 보고 `보고_배치20_산과_타일상태.md`(머리에 `bash scripts/redeploy-hanbando.sh --central`) +
  실화면 `증거_배치20/` 추가(램프 4축·재베이크 국소성·산터 vs 일반) + 인계 갱신 + 커밋·번들.

## ★추가 조사 1건 — 검은 지그재그 띠 (검증 세션 미해결 이월)

배치 19·20 증거와 검증 세션 프로브 모두에서, 능선가·강가 화면에 **셀 계단 모양의 불투명 검은 띠**가
보인다(예: `증거_배치20/산_능선가.png` 중앙). 좁혀진 것: 물 셀 아님(터레인 데이터 실측) ·
길 아님(길은 rgba 갈색 틴트). 남은 용의자: 안개/시야 마스크 · 영토 렌더 · 지면 베이크의 빈 틈.
**특정하고, 결함이면 수리하라.** 판별 지름길: `__terrain19.legacy=true` 로 사라지는지(=19/20 결함)
남는지(=기존 시스템) — 재민의 라이브 확인 결과가 대화에 있을 수 있으니 먼저 물어봐라. 예외적으로
이 항목만 질문 허용.

## 환경 (컨테이너 롤백 후 재설치)

`apt-get update && apt-get install -y blender`(4.0.2) · `npm install pngjs playwright@1.56.1 --no-save`
(★playwright 1.56.1 핀 — `/opt/pw-browsers` 엔 chromium-1194 만 있어 1.62 는 실행 파일을 못 찾는다).
pkill 자기살해 함정(`[x]` 브래킷) · ColorRamp 흰 요소 사냥 — `다음세션_인계_2026-08-06a.md` 함정 절 참조.
