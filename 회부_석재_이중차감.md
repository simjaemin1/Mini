# 회부 — 주거 유지 석재가 **두 번** 청구된다

> 2026-08-02d 배치 5 ②. 원장으로 규명 완료. **수리 손잡이는 만들었고 켜지 않았다**(기본 = 현행 유지) —
> 고치는 대가가 인구 기준선을 깎아 **경제 설계 판단**이 되기 때문이다.

---

## 1. 어떻게 잡았나 — 원장(ledger), 잔차 0

랩에 `LAB_LEDGER=stone` 을 넣으면 `v.storage` 를 Proxy 로 감싸 **석재의 모든 쓰기**를
호출 지점(`file:line`)별로 적는다.

손으로 계수기를 박지 않은 이유가 있다 — 석재 변경 지점이 v1·v2 합쳐 15곳이 넘는다.
손으로 세면 **빠뜨린 곳이 곧 잔차**가 되고, 원장이 "설명 못 한 몫이 있다"고 말할 때
그게 진짜 누수인지 내 누락인지 구분할 수 없다. Proxy 는 정의상 전부 잡으므로
**잔차가 구조적으로 0**이고, 남는 건 "어느 줄이 얼마를 썼나"뿐이다.

실측(음성 대조 · 시드42 · 800일 · 19마을 세계 합계):

| 항목 | 호출 지점 | 석재 |
|---|---|---|
| 생산(채집 돌) | `economy-sim.js:1681` | **+59,138** |
| 귀환화물 입고 | `v2:1063` | +11,472 |
| 수입 도착 | `v2:956` | +7,707 |
| **v2 생계소비** | `v2:496` | **−21,173** |
| 귀환화물 매입(도착지 출고) | `v2:1015` | −11,472 |
| **v1 건축유지** | `economy-sim.js:2240` | **−11,081** |
| 수출 발송 | `v2:704` | −8,093 |
| 감가(부패) | `v2:1154` | −4,762 |
| 석공 도구 | `economy-sim.js:1745` | −4,705 |
| 주거 신축 | `economy-sim.js:2282` | −4,556 |
| 석공 마제석검(여유손) | `economy-sim.js:1761` | −3,276 |
| (그 외 8줄) | | −1,131 |
| **잔차** | | **0.0955** (흐름 59,138의 0.00016% = 부동소수 반올림) |

## 2. 애초 의심(광산2 급감)은 **정상 교역**이었다

광산2 개별 원장에서 최대 지출은 `귀환화물 매입` **−1,467** 이다 —
이웃 캐러밴이 대금으로 광산2의 돌을 실어 간다. `land.stone 1.87` 인 **돌 산지의 정상 수출**이고,
캐러밴 단위가 커서 창(20일) 단위로 보면 뭉텅이로 빠져 급감처럼 보인 것이다. **버그 아님 — 종결.**

## 3. 대신 원장이 잡은 것 — **같은 물리를 두 곳이 각자 적는다**

```
  v1  STONE_MAINT_PC        = 0.02 /인/일
      주석: "건축 유지 — 담장·구들·바닥·숫돌이 닳는다 — 인구 비례 석재 실소비"   (2026-07-12 추가)
  v2  SUBSISTENCE_PER_NPC.stone = 0.04 /인/일
      주석: "주거 수요: 집(한옥)의 **건축·보수**에 목재·석재 소비. 인구↑(새 집)·유지보수로 지속 수요"
```

둘 다 **인구 비례 주거 유지**를 말한다. 게다가 '건축' 몫은 v1 주거 증축(`HOUSE_STONE`, `:2282`)이
이미 실물로 차감한다. 합치면 인구 비례 '유지' 명목으로만
**32,254 = 생산(59,138)의 55%** 가 나간다.

v1 쪽이 나중(2026-07-12)에 들어왔고, v2 쪽은 **가격·부패 목표(`subs×30`)의 입력이기도 해서**
건드리면 석재 시장 전체가 흔들린다. ⇒ 수리 지점은 v1의 `STONE_MAINT_PC` 다.

## 4. 고쳤을 때의 대가 — 실측

| 설정 | 인구 | 소멸/19 | 좀비 | 도구 | 시드별 인구 |
|---|---|---|---|---|---|
| 현행(채택 기본값) | **3,415** | 0 | 0 | 4,896 | 3,407 / 3,364 / 3,473 |
| `STONE_MAINT=0` 단독 | 3,357 (−1.7%) | 0 | 0 | 5,051 | 3,572 / 3,165 / 3,333 |
| `STONE_MAINT=0` + 이번 배치 채택분 | 3,274 (−4.1%) | 0 | 0 | 4,799 | 3,450 / 3,284 / **3,089** |

소멸 0 · 좀비 0 은 유지된다. 하지만 **인구 기준선(~3,415)을 깎는다**(시드42 는 −11%).

## 5. 왜 회부인가

석재 수요는 단순한 소모가 아니라 **광산촌의 소득 축**이다 — v2 주석이 그 의도를 명시한다:

> 효과: ① 숲 마을 → 나무꾼 수요 ② **석재 시장 형성 → 산골 마을이 석재 수출로 식량 구입(채광 자립)**

중복분을 걷으면 석재 수요가 1/3 줄고, 그만큼 **돌 산지 마을의 수출 소득이 준다.**
"이중 차감이니 고친다"는 회계적으로 옳지만, 그 결과가 광산촌 자립 설계를 약화시키는지는
**경제 설계 판단**이라 규칙 7에 따라 회부한다.

## 6. 결정이 필요한 것

- [ ] **(가) 그대로 둔다** — 이중이지만 지금 세계가 그 위에 균형 잡혀 있다. 두 값의 합(0.06)을
      "주거 유지의 총량"으로 재해석하고, 두 주석을 그렇게 고쳐 적는다(가장 싸다).
- [ ] **(나) v1 을 끈다**(`STONE_MAINT=0`) — 회계적으로 정확. 대가는 §4 (인구 −1.7~4.1%).
- [ ] **(다) v1 을 끄고 v2 를 올린다**(예: 0.04 → 0.06) — 총량 보존 + 단일 출처.
      다만 v2 값은 **가격·부패 목표**에도 들어가므로 석재 시장가가 함께 움직인다. 별도 A/B 필요.

내 의견: **(가) 또는 (다)**. (나)는 회계만 맞추고 세계는 나빠진다.
(다)가 구조적으로 가장 옳지만 가격 축까지 건드리므로 3시드 A/B 를 한 번 더 해야 한다 —
이번 배치에서 하지 않은 이유는 그것이 "수리"가 아니라 **석재 시장 재조정**이기 때문이다.
