# 회부 — 무게 모델 다음 층 (2026-08-27 · 좀보이드식 per-item weight 뒤)

> 이번 배치가 심은 것: 카탈로그 kg 정본(242종) · 소지 무게 합산 · 용량/과적 연속 곡선 ·
> 인벤 3층(개체·로트·무기한 벌크) · 곡물 품목화 · 넘침 딱지 · T/Y 제거.
> 아래는 재민이 **구현 금지**로 못 박은 것 + 만들며 **실측으로** 드러난 판단거리다.

---

## A. 재민이 명시적으로 회부한 것 (구현 금지)

### M. NPC 캐러밴 무게 모델 — ★랩 A/B 선행
`물품_무게_고증표_kg.md` §10 의 2~4단계다: `CARGO_PER_TRIP=100` flat → **무게 예산**(≈90kg/짐꾼),
운송비 `∝ weight × dist`, 그 뒤 작물 밸런스 재조정과 L(임금) 재시도.
★**이번 배치는 여기 한 줄도 안 건드렸다.** 무게는 **플레이어 층 전용**이고 econ 교역 경로는 무수정이다.
★지금 상태의 값: `specialty.js` 의 `weight` 는 고증표 값으로 **갱신됐지만 econ 에서 여전히 아무도 안 읽는다**
(전수 grep 으로 확인 — 소비자 0곳). 즉 M 은 **표는 준비됐고 배선만 남은** 상태다.
⇒ 켜는 순간 벌크가 국지화되고 교역량·부양력이 바뀐다. **3시드 800일 A/B 필수**(기준선 게시 1,561/1,487/1,535).

### 지게(A-frame carrier) — 용량 확장구
청동기 고증에 맞는 확장구. `tool` 슬롯이 이미 있으니(`equipSlots.tool`) 붙일 자리는 있다.
★추천 설계: `Carry.capKg(p)` 가 지금은 상수 한 줄인데, **그 함수 하나만** 장비를 보게 하면 된다
(`carry.js` 의 `function capKg(p) { return CFG.CAP_KG; }` — 여기가 유일한 접점이다).
· 제작 수요: 나무 + 섬유(끈) → 제작 숙련 `toolmaking` 에 얹힌다.
· 경제 연결: 지게가 있으면 **곡물 배달 한 짐(28kg)이 한 번에 간다** — 아래 B-4 표 참조.
  즉 지게는 "게시판 납품 왕복 횟수"를 직접 줄이는 물건이라, 만들 이유가 수치로 선다.
· 맞바꿈: 지게를 지면 손이 묶인다(무기·도구 동시 사용 제한)를 걸면 판단이 생긴다.

### 부패 곡선 · 상함 판정 · 보존 가공(말리기·훈제·절임)
★**자리는 이미 파 뒀다** — `server/lots.js` 가 `(품목, 취득일, 수량)` 을 적고 있고,
신선도는 **조회 시 (오늘−취득일)** 로 유도한다(타이머 없음 · 틱 비용 0 · 광맥 번영도와 같은 문법).
⇒ 곡선은 **조회 함수 하나**로 얹힌다. 품목별 곡선이 필요하다:
· 건조 곡물 느림(그래서 곡물이 교역재 우량) · 생선·고기 빠름 · 조리식 중간.
· 겨울나기 캐논과 직결 — 가을에 말려 둔 것이 겨울을 넘긴다.
★판단거리: 상하면 **버려지는가**(수량 소멸) **값이 떨어지는가**(거래소 할인) **먹으면 탈나는가**(HP).
셋은 서로 다른 게임이다. 지금 구조는 셋 다 받을 수 있다.

### P. 플레이어 간 거래(제안·수락) — T/Y 를 대신할 진짜 물건
T/Y 는 이번에 지웠다(아래 C-1). 분업 경제의 핵심이라 언젠가 반드시 필요하다. 설계 골자:
1. **양방향 제안** — A가 `{줄 것, 받을 것}` 을 제안 → B에게 창이 뜬다 → B가 수락/거절/역제안.
2. **양쪽 다 확인 뒤에만** 물건이 움직인다(원자적 — 거래소 `exchange` 와 같은 순서: 잡고 → 옮기고 → 실패 시 복원).
3. **값은 강제하지 않는다** — 플레이어끼리는 마을 시세와 무관하게 정할 수 있어야 한다.
   대신 창에 **그 마을 시세 환산**을 나란히 띄운다(정보는 주되 판단은 사람이).
4. 거리 게이트(현행 80px)는 유지 — 얼굴을 보고 하는 거래다.
5. ★개체 무게가 이미 있으므로 **"2kg 물고기"를 지목해서** 건넬 수 있어야 한다(수량형 UI 로 퇴보 금지).

### 그 밖(이전 배치에서 이월)
곳간 인출 · 마을 소속/멤버십 · 아이템 상세 표(§8.1 전체 — 이번엔 **kg 한 줄만**) ·
MSY 상한 A/B(`회부_MSY상한_사장.md`) · 낚시·신체·거래소 손잡이 튜닝(재민 실기 대기).

---

## B. 실측으로 드러난 판단거리 (재민 확인 필요)

### B-1 ★★시작 지급이 **기본 용량을 넘는다** — 새 플레이어가 태어나자마자 과적이다
실측(`e2e-weight` 가 판정으로 박아 뒀다):
```
  시작 지급: 톱·망치·도끼(3.6kg) + 판자 10장(30.0kg) = 33.6kg
  기본 용량: CARRY_CAP_KG = 25kg  →  134% · 이속 ×0.87
```
새 플레이어의 **첫 경험이 "이유 모를 느림"**이 된다. 과적은 선택이어야 하는데 이건 선택이 아니다.
★판단거리 셋 — ⓐ 용량을 올린다(예: 40kg — 그러면 곡물 40단위 한 짐이 한 번에 간다)
ⓑ 시작 판자를 줄인다(10 → 4장 = 12kg) ⓒ 지게를 시작 지급에 넣는다(위 A항 — 고증·경제 둘 다 맞다).
※**손대지 않았다.** `CARRY_CAP_KG=25` 는 지시서가 "시작점"으로 준 값이고, 셋 중 무엇을 고르든
플레이 감각의 문제라 재민 실기 판정이 맞다. 지금은 **사실만 화면과 하네스에 박아 뒀다.**

### B-2 낚시 한나절이면 **못 걷는다**
`weight-metrics` 실측: 30분 낚시(사이클 ~9초 → ~200마리 · 중앙값 0.58kg) = **115kg = 용량의 462%**.
바닥(×0.4)에 처박힌다. ★판단거리: 이게 **의도한 압력**인가(→ 지게·바구니·훈제 수요가 생긴다)
아니면 어획 산출이 과한가(→ 낚시 손잡이). 둘 다 될 수 있고, **낚시 손잡이는 재민 실기 대기 항목**이라 안 건드렸다.
※이 배치가 낚시에 **끼친 실제 변화 하나**: 종전엔 2.4kg 물고기를 0.8kg 짜리 **세 마리로 뭉갰는데**,
이제 **한 마리 2.4kg** 이다(개수는 줄고 무게는 같다). 거래 가치는 무게 비례라 손해가 아니다(하네스 ⑧).

### B-3 ★고증표에 **없어서 내가 유도한 값** — 확인 바람
표(§1~§9)가 안 덮는 플레이어 전용 아이템이 있었다. 지어내지 않고 **표의 앵커에서 유도**하고
근거를 `server/weights.js` 에 한 줄씩 남겼다. 그래도 재민 확인 대상이다:
```
  풀 fiber        0.30   삼 섬유 다발(0.40)보다 성기고 가볍다
  이엉 thatch     1.00   풀 4단(1.2kg)을 엮어 다진 것
  기둥 pillar     6.00   통나무 3을 깎아 낸 굴립주 — 소나무 통나무 8.00 의 다듬은 몫
  서까래 rafter   1.50   통나무 1 → 2개(레시피) · wood 3.00 ÷ 2
  씨앗 seed_berry 0.02   표 최경량군(사프란 0.01 · 누에 0.01) 급
  물병 water_bottle 1.00 박 + 물 — 표 milk 1.03 과 같은 물 한 되
  건축 아이템     레시피 재료 무게를 그대로 물려받음(벽=판자2=6.00 …)
```
그리고 표가 안 다룬 specialty 키가 **하나** 있었다: `iron_relic`(철제 위세품, gem) — **손대지 않고 2.0 유지**.

### B-4 용량 25kg 에서 "한 짐"이 뜻하는 것 (실측표)
```
  곡물 배달 40단위    28kg  112%  ×0.96
  광석 캐기 8덩이     40kg  160%  ×0.76
  판자 10장           30kg  120%  ×0.93
  참나무 통나무 4     36kg  144%  ×0.83
  도구 3종           3.6kg   14%  ×1.00
```
⇒ **거의 모든 "한 짐"이 용량을 조금씩 넘는다.** 이게 의도라면(왕복을 강제) 좋은 값이고,
아니라면 B-1 의 ⓐ와 같이 올려야 한다. 통나무는 통째로 못 나르는 게 고증에 맞다(가공해서 옮긴다).

### B-5 로트 상한에서 **한 번은 나이를 뭉갠다**
"로트 병합 금지"와 "품목당 로트 수 상한"은 상한에 닿는 순간 서로 부딪친다.
⇒ 지금은 **가장 오래된 둘을 뭉치고 더 오래된 날짜**를 준다. 근거: 어느 쪽이든 근사지만
이 방향은 "먹을 순서"를 안 어기고 **음식이 실제보다 싱싱해 보이는 일이 절대 없다**(안전한 쪽으로만 틀린다).
★판단거리: 부패 곡선이 들어오면 이 근사가 실제로 보이게 된다. 그때 상한(기본 12)을 다시 볼 것.

### B-6 인벤 정수와 소수 몫
부분 소비(한 입 0.25단위)를 받으면서 **인벤 정수 = `floor(Σ로트)`** 규약을 세웠다
(`oreCarry`(선광 소수분 이월)와 같은 문법). 그래서 "0.75 남은 곡식은 아직 못 판다".
무게에는 그 0.75 도 **정확히** 반영된다. ★판단거리: 화면에 "곡식 3 (+0.75)"를 보여 줄 것인가.
지금은 인벤 정수만 보인다.

### B-8 ★★곡물 품목화가 **게시판을 크게 활성화**했다 — 의도한 결과지만 크기를 보라
3시드 800일 실측. econ 지표(인구·소멸·무기Q)는 **비트 동일**인데 게시판만 달라졌다:
```
  게시        1,561/1,487/1,535 → **1,647/1,583/1,679**
  못갚아미게시 46,303/54,953/44,875 → **11,194/10,787/10,489**  (−76%)
  요청 축소      367/336/373 → **13/15/13**
```
★**단일 손잡이 A/B 로 원인을 갈랐다**: `food: 'food'` 한 줄만 빼고 같은 시드를 돌리면
`게시 1,561 · 못갚아미게시 46,303` — **기록값과 정확히 일치**. 즉 전부 이 한 줄 때문이고,
`specialty.weight` 89건 교체는 econ 에 **한 톨도 안 스몄다**.
왜 이렇게 큰가: 곡식은 마을에서 **가장 흔한 재화**라, "갚을 물건이 없어 못 걸던" 의뢰가 대부분 걸리게 된다.
★판단거리: 의뢰가 훨씬 자주 걸리고 덜 잘린다 — **플레이어 체감이 크게 달라진다.**
ⓐ 그대로 둔다(곡물이 화폐 노릇을 하는 게 청동기답다) ⓑ 곡물을 보상에서만 빼고 의뢰 대상으로는 둔다
ⓒ 사건 문턱(±70%·H1.60)을 다시 잰다. **손대지 않았다** — 사건 밀도는 재민 확정 손잡이다.

### B-7 개체 무게 환산은 **게시판에도** 걸었다
2kg 물고기 한 마리 = 표준 0.9kg 짜리 2.22단위. 이 환산을 거래소만 태우면
"같은 물고기가 어디서 파느냐에 따라 다른 값"이 되어 그게 곧 보이지 않는 손이라,
**실물 이동의 정본 하나**(`playerVillageDeposit`)에 얹어 게시판 납품도 같은 환산을 쓴다.
★부수 효과: 큰 물고기로 의뢰를 채우면 **더 적은 마리 수로** 채워진다. 의도한 결과지만 재민 확인 대상.

---

## C. 이번에 **지운 것**

### C-1 T/Y 플레이어끼리 1:1 물물(`trade_offer`) — 제거
`t`/`y` 가 80px 안 최근접 플레이어와 **나무 1 ↔ 돌 1** 을 맞바꿨다. 지운 이유 셋:
① **상대 동의 절차가 없었다** ② 같은 마을 거래소 값이 나무 1.75 · 돌 0.1677(곡식 환산)로 **10배 차이**라
경제를 통째로 우회했다 ③ UI 도 수량 선택도 없는 프로토타입 화석이었다.
서버 핸들러·함수·클라 키·액션 버튼·`index.html` 버튼·README 조작 설명까지 전부 걷어냈고,
`test-weight ⑥` 이 **소스에 남아 있지 않은지**까지 확인한다. 대신 위 **P항**을 설계해 두었다.

---

## D. 이번에 **일부러 안 한 것** (규약)

* **econ 무수정** — `sim/economy-sim*.js` 한 줄도 안 건드렸다. `specialty.js` 의 weight 만 표 값으로 갱신했고
  (econ 소비자 0곳 — 전수 grep 확인) 번들은 규약대로 재생성했다(`--check` 8/8 · 3시드 기준선 불변).
* **플레이어용 별도 무게 테이블 금지** — specialty 가 가진 값은 `weights.js` 가 **읽기만** 한다.
  `test-weight ①` 이 "소스에 옮겨 적은 줄이 없는지"를 구조로 확인한다(실제로 `plank` 한 줄이 걸려서 지웠다).
* **하드 컷 금지** — 용량을 넘겨도 줍고 사고 나를 수 있다. 화면은 사실만 말하고 결정은 사람이 한다.
* **넘침 판정에 상수 복사 금지** — `PRICE_ADJ_MIN` 을 옮겨 적는 대신 **정본에게 물어본다**
  (재고를 잠깐 늘려 보고 값이 안 움직이면 바닥 · `planSliced` 와 같은 문법 · `finally` 복원).
