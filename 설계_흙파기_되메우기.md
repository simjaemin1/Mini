# 설계 메모 — 흙 파기 · 되메우기 [회부 · 구현 금지]

재민 발안(2026-08-07). **결정표(§6)가 채워지기 전엔 구현하지 않는다.**
이 문서는 설계 판단과 **실측**만 적는다.

> 재민: *"채굴은 광맥 구역이 아니라 아무데서나 할 수 있어… 플레이어는 어디가 광맥 구역인지
> 모르고 다 조금씩 파보게 될 거야. 처음 채굴할 때에는 **흙 파내기**라는 작업을 먼저 하고,
> 흙을 얻은 뒤에 채굴을 할 수 있는 거야. 그리고 채굴한 곳은 **흙으로 다시 메우기**를 하고
> 나서야 농사든 건축이든 가능. 이때 **채굴 비옥도는 유지**."*

---

## 1. 왜 이 설계가 맞는가 — 실측 셋

**① 채굴은 정말 아무 데서나 된다.** `mineOreCell` 은 물·통행불가만 막고 광맥 여부를 안 본다.
모든 뭍 셀이 채굴 재고(`minedCells`, 안 파면 만땅 `ORE_K=1000`)를 암묵적으로 갖는다.
⇒ 플레이어는 지도를 곰보로 만들며 배운다. 이건 버그가 아니라 의도된 탐색이다.

**② 그런데 97.2%는 헛탕이다.** 뭍 161,674셀 표본:

| 광석 확률 | 셀 | 비율 |
|---|---|---|
| **0** | 157,175 | **97.2%** ← 파도 흙·맥석만 |
| 0 < p < 5% | 3,212 | 2.0% |
| 5% ≤ p < 25% | 1,087 | 0.7% |
| p ≥ 25% (노다지) | 200 | 0.12% |

평균 0.15%. **시굴의 압도적 다수가 마른 구덩이다.**

**③ 그 헛탕의 산출물이 갈 곳이 이미 있다.** 자원 표에 `clay(진흙)` · `clay_brick(진흙 벽돌)` ·
`pottery(도자기)` 가 이미 존재한다.

⇒ **이 설계의 값은 여기서 나온다: 실패 모드가 자원이 된다.**
시굴은 "광석을 노리고 파는데 흙은 확실히 나온다"가 된다. 낭비 노동이 아니라 흙 공급선이다.

---

## 2. 왜 **채굴 비옥도를 유지**해야 하는가 (재민 지시의 핵심)

되메우기가 **광석 재고를 올리면 무한 광석**이 된다:
흙을 부어 재고를 채우고 → 다시 캐고 → 광석이 나온다. 보존이 깨진다.

재민이 "채굴 비옥도는 유지"라고 못박은 것이 이걸 정확히 막는다.
⇒ **구덩이 깊이는 광석 재고와 별개의 수**여야 한다.

| | 채굴 재고(기존 `minedCells.prosperity`) | 구덩이 깊이(**신설**) |
|---|---|---|
| 뜻 | 이 자리에 광석이 얼마나 남았나 | 이 자리가 얼마나 파여 있나 |
| 올리는 것 | 시간(`oreRegen`) — 광맥이 회복한다 | **되메우기(흙 소비)** |
| 내리는 것 | 채굴 | 흙 파내기 · 채굴 |
| 막는 것 | (없음) | **농사 · 건축** |

★한 사건이 두 축을 밀지 않는다 — 배치 20 F 에서 채굴이 비옥도를 깎던 것을 지운 것과 같은 원칙.

---

## 3. 저장 — **새 테이블 없음**

배치 20 B 의 `soil_cells` 에 컬럼 하나만 붙인다:

```
soil_cells(zone, cell_key, v, d, geo, pit)
                                    ^^^ 신설 — 구덩이 깊이(0 = 평평)
```

방송(`tile_state`)·희소 저장·게으른 리젠·타일 재베이크가 **이미 다 붙어 있다.**
`pit > 0` 인 셀만 행을 갖고, 되메우면 행이 사라진다(기존 프룬 규약 그대로).

⚠단 **`pit` 은 리젠으로 안 줄어든다** — 구덩이는 저절로 안 메워진다. 그게 이 설계의 요점이다.
(광석 재고는 리젠한다. 두 수의 성질이 다르다는 게 §2 의 표다.)

---

## 4. 렌더 — 이미 서 있다, 원천만 바꾼다

지금 채굴 축 그림(파낸 흙·자갈·그늘)은 `ore` 거울에서 나온다. 그걸 **`pit` 에서** 나오게 바꾼다.

- 재민 발안의 *"한 번이라도 채굴한 땅은 채굴 디자인"* → **영구 플래그가 아니라 `pit` 값으로.**
  되메우면 `pit=0` 이 되고 그림도 돌아온다. `wasMountain`(지질 — 진짜 안 돌아온다)과 구분이 선다.
- **연속 유지**(재민 규칙 "경계마다 딱딱 나누지 말고"): 그림은 `pit/PIT_MAX` 에 비례.
- ★**게임 규칙은 날카로워도 되지만 눈에 보여야 한다**: "농사 금지" 문턱을
  **구덩이가 눈에 보이기 시작하는 지점**에 맞춘다. 그래야 "파인 데는 못 짓는다"가 그림으로 읽힌다.
  (문턱만 날카롭고 그림이 멀쩡하면 플레이어가 거부 사유를 읽을 수 없다.)

렌더 쪽 신규 작업은 **사실상 0**이다 — 축·베이크·하네스(`e2e-tilestate`)가 이미 검증돼 있다.

---

## 5. 위험 — 순서대로

### 5-1. NPC 광부 — **초안의 프레이밍이 틀렸다** (재민 지적 → 실측)

초안은 "NPC 가 세상을 곰보로 만들고 econ 기준선이 이동한다"고 적었다. **틀렸다.**
재민 지적대로 **NPC 는 광맥을 인지한다** — `_oreCellsOf(vil)` 이 `ta.isOre(cx,cy)` 로 거르고
품위 순으로 정렬한다(`out.sort((a,b) => b.p - a.p)` — "광부는 좋은 노두를 먼저 판다").
⇒ NPC 는 **마른 구덩이를 안 만든다.** 시굴로 지도를 곰보로 만드는 건 플레이어뿐이다.

그래도 1차엔 NPC 채굴이 구덩이를 **안 만드는** 쪽을 권한다. 이유가 바뀌었다:
**NPC 채굴은 애초에 추상화된 계다.** 타수도 인벤도 없고 재고를 바로 깎는다
(`want = PER * inTeam`, `_oreMineDaily`). 여기에 흙·구덩이를 붙이면
**시뮬레이션이 없는 것을 시뮬레이션하는 셈**이다. 편법이 아니라 추상화 수준을 맞추는 것이다.

⚠단 그러면 마을 광맥 셀엔 구덩이가 안 생기므로, **플레이어가 남의 마을 광맥에 구덩이를
파 놓는 그리핑**은 여전히 규칙이 필요하다(§5-5).

### ⚠5-2. 흙 더미 — **바닥 아이템으로 두면 덫이 된다** (실측)

지게 상한 `CARRY_MAX_KG`(28kg)이라 파낸 흙을 인벤에 강제로 넣으면 한 구덩이 파고 못 움직인다.
재민 확정: *"흙이 인벤으로 안 들어오고 흙 더미로 쌓이는 거 좋아."*

그런데 기존 바닥 아이템은 **10분이면 사라진다**:

```js
const GROUND_ITEM_LIFETIME_MS = 10 * 60 * 1000; // 10분 자동 소멸
```

흙 더미를 일반 바닥 아이템으로 만들면 — 구덩이 파고 10분 자리를 비우면 흙이 증발하고
**그 구덩이는 영영 못 메운다.** 메우려면 딴 데서 파야 하고 그럼 구덩이가 하나 더 생긴다. 덫이다.

⇒ **흙 더미는 아이템이 아니라 구덩이의 소유물이다.**
셀이 제 깊이(`pit`)를 들고, 옆의 더미는 **그 깊이가 눈에 보이는 것**이다.
아이템이 아니니 안 사라지고, 판 만큼과 메울 만큼이 자동으로 맞는다 — **보존이 공짜로 지켜진다.**

| 행동 | 셀 깊이 | 더미 | 인벤 |
|---|---|---|---|
| 판다 | +1 | 자란다 | — |
| 되메운다 | −1 | 준다 | — (노동이지 짐이 아니다) |
| **흙을 가져간다** | 그대로 | 준다 | `clay` +1 ← 이때 비로소 아이템 |

★마지막 줄이 이 설계의 선택지다: 흙을 자원으로 빼 쓰면 **구덩이가 빚을 진다** —
딴 데서 흙을 가져와야 메워진다. 진흙벽돌 만들려고 퍼가면 그만큼 구덩이가 남는 게 정직하다.

### ⚠5-3. 첫 파기 단계가 주 루프에 붙는 세금

새 셀마다 붙는 비용이다. 플레이어가 시굴을 계속 할 거라(§1-①) 이게 길면 채굴 자체가 느려진다.
⇒ **짧게.** "땅을 뜬다"로 읽힐 만큼만.

### ⚠5-4. 지금 안내 문구가 정반대를 가르친다

```
⛏ 이 자리는 다 팠다 — 옆으로 옮기자
```

구덩이를 남기고 옆으로 가라고 **명시적으로 시킨다.** `메우고 가자` 로 바꿔야 한다.

### ⚠5-5. 마을 영토 안 구덩이

플레이어가 NPC 마을 성장 예정지를 구덩이로 막을 수 있다(§5-1 대로 NPC 는 못 메운다).
규칙 필요 — 영토 안엔 못 파거나 · 마을 계는 `pit` 을 무시하거나 · 마을이 시간이 지나면 메우거나.

---

## 6. ★결정표 — 재민이 채워야 착수한다

| # | 결정 | 후보 | 파장 |
|---|---|---|---|
| 1 | 첫 파기 타수 | 몇 타에 흙 1? | 주 루프 속도(§5-3) |
| 2 | 더미에서 **퍼갈 때** `clay` 1개 무게 | kg | 되메우기 자체는 무게 무관(§5-2) |
| 3 | 구덩이 깊이 상한 `PIT_MAX` | 채굴 1회당 +얼마 | 되메우기 비용 |
| 4 | 되메우기 비용 | 흙 몇 개 = 깊이 1 | **판 만큼 = 메우는 만큼** 이면 보존이 깔끔하다 |
| 5 | 금지 범위 | 농사만 / 농사+건축 | 재민은 "농사든 건축이든" — 확정이면 §5-5 규칙 필요 |
| 6 | NPC 채굴이 구덩이를 만드나 | **아니다**(추천 — 추상화 수준) / 만든다 | §5-1 |
| 7 | 마을 영토 규칙 | §5-5 셋 중 | 그리핑 |
| 8 | 흙의 다른 쓰임 | 진흙벽돌·도자기와 연결? | 이미 자원 표에 있다(§1-③) |

★**4번을 "판 만큼 = 메우는 만큼"으로 두면** 흙이 완전 보존되어 모델이 제일 깨끗하다.
남는 흙은 시굴을 많이 한 만큼 쌓이고, 그게 곧 진흙 공급이 된다.

---

## 7. 이건 렌더 배치가 아니다

채굴 루프 · 인벤 · 새 작업 2종 · 농사/건축 배치 판정을 건드린다.
배치 19~21 의 지형 렌더와 성격이 다르다 — **게임 메커니즘 배치**로 따로 세워야 한다.
렌더 쪽은 §4 대로 원천만 갈아 끼우면 되므로 이 배치의 무게는 전부 게임 로직 쪽에 있다.
