# 회부 — 노(爐) 조업에 시간을 붙일 것인가

> 2026-08-02d 배치 5 ④. **구현 안 함 · 회부.** 배치 지시가 "계약이 안 맞으면 이월하고 회부(반쪽 구현 금지)"라
> 못박아 뒀고, 아래 §2 가 **계약이 안 맞는다**는 실측이다.

---

## 1. 무엇을 묻는가

지금 노 조업은 **클릭 1회 = 즉시 제련 1회**다(`tryFurnaceSmelt` — 정광 1 + 숯 n 을 먹고 그 자리에서 철이 나온다).
배치 지시는 "움집·노 **건설**과 같은 진척도 계약(자원 투입 → 시간 경과)으로 맞추는 게 자연스러운지"
`설계_플레이어_NPC_집건설_해석안.md` 의 진척 공식으로 검토하라는 것이었다.

## 2. 검토 결과 — **그 공식은 옮겨올 수 없다**

### (가) 인용된 진척 공식은 **NPC 크루 노동 모델**이지 플레이어 계약이 아니다

```
  h.built += max(h._crewD / L_BUILDSEC, 기본 25%)     // L_BUILDSEC=4600 인·초/층
```
`_crewD` 는 **NPC 가 현장에 머문 초**다. 플레이어에겐 '현장 체류 초' 라는 개념 자체가 없다
(플레이어는 클릭하고 걸어간다). 즉 이 공식의 입력 변수가 플레이어 축엔 존재하지 않는다.
옮기려면 "플레이어가 노 앞에 서 있는 시간"이라는 **새 상태를 만들어야** 하고, 그건 이식이 아니라 신설이다.

### (나) 정작 노 **건설**에도 시간이 없다 — 붙이면 같은 건물 안에서 계약이 어긋난다

실측(`server/zone.js` `_siteStart`/`_siteAdvance`): 노·숯가마 건설은 **자재 + 클릭**으로 단계가
**즉시** 오른다. 시간 항이 0이다. 여기서 조업에만 시간을 붙이면
"짓는 데는 1초, 굽는 데는 2분" 이 된다 — 한 건물 안에서 두 계약이 충돌한다.
자연스럽게 하려면 **건설도 같이** 시간 계약으로 바꿔야 하고, 그건 이 배치의 범위를 넘는다.

### (다) 플레이어 축에 시간 계약이 **아예 없는 건 아니다** — 농지가 유일한 선례

`tryHarvest`: `farmland` 은 `data.readyAt` 타이머를 갖고, 덜 자랐으면 "아직 자라는 중 (N초 남음)".
**노 조업의 자연스러운 짝은 건축 진척이 아니라 이 농지 타이머다.** 그런데 농지 선례를 그대로 쓰면
설계 판단이 줄줄이 따라온다(§3).

## 3. 결정이 필요한 것 (전부 설계 판단 — 규칙 7)

- [ ] **① 시간을 붙일 것인가** — 붙인다면 조업 1회에 몇 분인가(게임 시간? 실시간?)
- [ ] **② 기다리는 동안 플레이어는 무엇을 하는가** — 자리를 떠도 되나(농지형), 곁을 지켜야 하나(체류형)?
      떠나도 되면 노는 사실상 '느린 자판기'가 되고, 지켜야 하면 **대기 플레이**가 생긴다. 이게 본질적 선택이다.
- [ ] **③ 산출을 언제 받나** — 즉시 인벤(농지처럼 '수확' 상호작용?) 아니면 노 안에 쌓였다가 꺼내나(새 UI)
- [ ] **④ 서버 재시작·접속 종료를 건너뛰나** — `readyAt` 은 벽시계라 자동으로 건너뛴다. 노도 그래야 하나?
      (그러면 "불 붙여 놓고 로그아웃"이 최적 전략이 된다 — 그게 의도인가)
- [ ] **⑤ 건설도 같이 시간 계약으로 바꿀 것인가**(§2 (나)) — 안 바꾸면 한 건물 안에서 계약이 어긋난 채 남는다

## 4. 내 의견

**이월을 권한다.** 지금 붙이면 위 다섯 중 넷을 내가 임의로 정하는 셈이고, ②·④는
"노를 켜 놓고 로그아웃한다"는 **플레이 패턴 자체를 만드는** 결정이라 재민 판단이어야 한다.

붙일 값이 실제로 생기는 시점은 분명하다 — **노가 여러 대 돌아갈 때**다.
지금은 한 사람이 노 한 대 앞에서 정광을 하나씩 넣는다. 시간이 붙어도 "클릭하고 기다리기"만 늘 뿐,
바뀌는 결정이 없다. 반면 길드가 노를 여러 대 세우고 숯을 나눠 대기 시작하면
"어느 노에 언제 불을 넣나"가 진짜 선택이 되고, 그때 시간은 **자원 배분 축**이 된다.

## 5. 같은 배치에서 **한 것**

- **숯가마 가득 채우기**(구현 완료) — 통나무 3씩 반복 클릭의 지루함만 없앴다.
  수지는 1회 조업 ×n 로 **완전히 같다**(배치라고 수율이 좋아지지 않는다 — 클릭 수가 물리를 바꾸면 안 된다).
  상한 `KILN_BATCH_MAX=20` 은 **가마 용량이 아니라 UI 폭주 방지**다. 용량 개념을 넣는 건 별도 회부 대상.
  `test-furnace.js` 에 배치 수지·부분 조업 거부·외상 없음 3건을 못박았다(59/0).
