# 본체 이식 계획 — 랩(전쟁실험실) ↔ 서버(server/) 엄밀 차이 감사 + 웨이브 로드맵 (2026-07-12)

> 자족 문서: 새 세션이 이 파일만 읽고 이어받을 수 있게 파일 경로·심볼·계약을 명시한다.
> 캐논: `/Users/simjaemin1/Mini/설계_실축화_1셀1m.md` §11~19 · 검증 관례: `durango-mini/CHECKLIST.md`(프로브 45초·보호 3블록·훅 미설치=무해).
> **1~4파 전부 이식 완료(2026-07-12)** — 1파 §4 · 2파 §6 · 3파 §7 · 4파 §8 구현 기록. §3 로드맵은 항목별 완료 표기.
> ★전 웨이브 미커밋(git 커밋 금지 계약) — 커밋 전 §5-6(git 인덱스 이상) 필독. 게이트: WAR_OPS/WAR_CAPTIVES/ENABLE_ROADS/VILLAGE_LON/BANDIT_BODIES(전부 기본 켜짐·0=봉인, OFF 무해 증명 §6.5e·§7.3e·§8.3).

---

## 1. econ 공유 실태 (확정 — "이미 서버에 도달한 것")

서버는 랩과 **같은 엔진 파일을 require**한다(사본 아님):

- `server/villages.js:1091` → `require('../sim/economy-sim')` (v1, 196KB)
- `server/villages.js:1092` → `require('../sim/economy-sim-v2')` (v2)
- `server/villages.js:1093` → `require('../sim/war-core')` (P1 전쟁 econ 층)
- `server/villages.js:1175` → `require('../sim/battle-core')` (보호 블록 — 바이트 계약)

랩 HTML은 같은 sim 파일을 `sim/build-econ-bundle.js → economy-engine.browser.js → inline-engine.js`로 인라인(3사본 규칙, CHECKLIST 7행). 즉 **sim/economy-sim(-v2).js에 들어간 것은 전부 서버에 자동 도달**하며, 호스트 훅이 미설치면 무해(A/B 바이트 동일 — 랩에서 증명된 계약)다.

### 서버에 이미 도달해 있는 최신 econ 기능(훅 인벤토리 — 파일:행)

| 기능 | 위치 | 서버 상태 |
|---|---|---|
| 영토 1셀 단위 구매(일 4셀·연속 체증, 36f7860) | economy-sim.js (v1 내부) | **도달·작동 중**(훅 아님 — 무조건 경로) |
| `_siegeBlock` 발/착/재라우팅 캐러밴 차단 | economy-sim-v2.js:361·422·607 | 도달. war-core가 march 창에 설치(§2 참고) |
| `_siegeOutMul` 야외 직업 생산 감산 | economy-sim.js:1130~1141 | 도달. war-core 설치 |
| `banditRouteRisk` 길목 위험 주입(파견 기대손실 435·outbound 564·귀환 735) | economy-sim-v2.js | 도달. **1파에서 배선 완료(bandits.js)** |
| `onBanditLoot` 장물 콜백 | economy-sim-v2.js:581·746 | 도달. **1파에서 배선 완료** |
| `_banditRisk` EMA→호위 요청 가중 ×(1+2×risk) | economy-sim-v2.js:499~502 | 도달. 1파가 EMA를 공급(추가로 war-core:500 목책 증축도 이 값을 읽음 — 사슬 자동 연결) |
| `_grudgeBlock` 원한 교역 대칭 차단 | economy-sim-v2.js:423·608 | 도달. war-core:366~368이 발행(작동 중) |
| `captive` 노동 ×0.6·직업 게이트 | economy-sim.js:1133~1141·2000·2039 | 도달·**미설치=무해**(포로 intake가 없어 captive가 안 생김 — 3파) |
| `_tradingN` 여가 기회비용·여가→행복 | economy-sim-v2.js:367 + v1 §12 항 | 도달·작동 중 |
| `lastStats`(happiness/health) | economy-sim.js:1589 | 도달 — 1파 도적 절망 이탈이 사용 |

결론: **"econ 주사위는 이미 서버 도달, 훅 배선만"이라는 전제 그대로 확인됨.** 서버 이식의 실작업은 (a) 호스트 훅 설치 모듈, (b) 랩 호스트 층(지형·실체) 어댑터 두 가지뿐이다.

---

## 2. 기능 매트릭스 — 캐논 §11~19 × 서버 상태

분류: ①=공유 econ으로 이미 서버 도달 · ②=서버 어댑터 필요(접점 명세) · ③=랩 전용 시각/관찰 물리(이식 불요) · ④=설계 결정 필요

| 캐논 | 기능 | 분류 | 비고(접점) |
|---|---|---|---|
| §11 | 도적 econ 주사위·호위 학습 | ① | v2 도달 — 배선만 필요했음 |
| §11 | 도적 3원천·소굴·유지비·토벌(경제·수명주기) | ② → **1파 완료** | `server/bandits.js` 신규(§4) |
| §11 | 도적 실체(소굴 배회 NPC·캐러밴 요격 연출·고립 습격, 랩 updateBandits 6674~) | ② 2파 | 경제 효과는 econ 주사위가 전부 — 연출 생략해도 수치 등가. isNpc/dormant 재사용 |
| §12 | 노동·여가(여가→행복·_tradingN·건축 징발 실효화) | ① | 전부 econ 내부. 반일 근무 연출은 ③ |
| §13 | 전쟁 econ(명분 4종·EU 개전·나선 방지 4중·동원·결과 4종·목책·트라우마) | ① 사실상 | `sim/war-core.js`(P1)가 이미 서버 배선(villages.js:1159 createWar + onGameTick:1585 daily). 상수는 랩 5961~6034 verbatim |
| §13 | 청동 희소성(주석 비축·무기 품질·자가도구) | ① | v1 내부 |
| §13 | 소멸 마을→도적 파이프라인 | ✅ 1파 | bandits.js 해체 검출이 전쟁 초토화도 흡수(별도 이벤트 불요 — §4.2) |
| §14 | battle-core(병종 8·개별 사기·궤주·지휘 대형) | ① | `sim/battle-core.js` 공유·무수정 계약 |
| §14 | LOD 실체 전투·행군·귀환(P2/P3) | 서버 기구현 | `server/war-live.js` + villages.js:1254~1551(관측자 근접 게이트 — 서버 고유 설계, 랩과 등가 아님) |
| §14 | 접근→충돌 전투(경보 180·포진·50m 개전)·행군 실측 속도 | ② 2파 | war-live/P3에 부분 반영(WAR_ALERT_R·WAR_ENGAGE_R 있음) — 랩 v3와 드리프트 감사 필요 |
| §15 | 작전층: march→camp→{assault\|siege\|withdraw} 상태기계·군량 적재/소모·방어 3택·무혈 항복·sortie | ② **2파 본대** | war-core는 명시 제외(헤더 "P1 제외: 공성 결단·siege 상태머신"). 현 서버는 'march 창 = 봉쇄' 단순화(war-core:477~480) — 상태기계로 대체해야 함 |
| §15 | 긴급 소집(scramble)·비전투원 _evac·무저항 함락(walkover)·합성 유닛 전량 금지 | ② 2파 | 실체 층 — villages.js P3(_warDraftPids·_warEnsureDefense)에 얹기. 현 서버 P3는 '주민 0=표준배치 폴백 or headless'로 이미 유령 최소화 방향이나 랩 최종 규칙(스폰 0 예외 목록)과 재동기 필요 |
| §17 | 전쟁 정의 J·승리 권리 스케일 | ① | war-core:303(warJustice)·465~470(J 약탈·불의전 피로) — 이미 서버 작동 |
| §17 | 제3자 평판 전파·_grudgeBlock 제재 | ① | war-core:355(전파)·366~368(발행) |
| §17 | 무기 드랍·궤주 투기(원장) | ① | war-core:307~323 |
| §17 | 본체 벌점(카르마)→J 연동(개인→길드 집계·세탁 방지) | ④ 3파 | 캐논 §17 인계 — central 벌점 레이어 설계 필요(warJustice TODO 자리 예약됨) |
| §18 | 포로 econ 훅(노동 ×0.6·직업 게이트) | ① | 도달·미설치=무해 |
| §18 | 포로 intake(사상 35%·J 상한·호송력)·호송 동반·출구 3종(탈출/동화/몸값)·EU 포로 가치 | ② **3파 본대** | war-core `WAR_CAP_EU = 0` 명시(포로 미포함). intake는 warResolveBattle 되먹임 + P3 귀환 그룹에 동반. 몸값은 마을 단위 |
| §16 | 답압 길(희소 Map·게으른 감쇠·T1/T2·속도·A* 할인·부지 제외) | ② 4파 | 로직은 순수(이식 쉬움). 접점: 스탬프 원천=서버 개체 이동(movePlayerStep/npcStep/캐러밴 tickCaravanBodies/행군 P3 — 서버엔 sepAgents가 없어 별도 순회 or 이동 루프 편승) · A* 할인=villages.js computeRoutePts/거리행렬 · 부지 제외=village-layout · **DB 영속**(캐논 §16 한계 명시 — zone-local-db roads 테이블) · 클라 오버레이 틴트 |
| §19 | 움집 단층 8명(L_MAXFL=1·FLOORCAP=8) | ② 4파 | **서버 쌍둥이 드리프트 확인됨**: `server/village-layout.js:109` `MAX_FLOORS = 4` (랩은 1). 기존 DB 마을 floors 마이그레이션은 ④(재시딩 vs 층 무시) |
| §19 | 경도 로컬 시각(_lonOff — 인간 일과만 로컬) | ②/④ 4파 | 서버 NPC 일과 모델이 얕음(npcHome/Work 배회) — 적용 지점 재설계 필요. econ 일 경계는 전역 유지(블록 계약) |
| §19 | 절대 타임스탬프 시계(게임시각=공식·상태=틱·캐치업) | ④ | 본체 설계 — zone-config WORLD(worldEpoch·dayLengthMs) 재정의 + econ 캐치업. 별도 웨이브(전 존 영향) |
| §19 | 논밭 입지(물가 논·내륙 밭·부지 회피) | 확인 필요 | village-layout 서버 사본에 논/밭 로직 존재(98~120행) — 랩 최신(물가 회피 부지·집터 잠식 비용)과 diff를 4파에서 움집과 함께 재동기 |
| §15후속 | 통합 콜라이더 sepAgents(맵 물리) | ③/④ | 랩 관찰(slow) 전용 물리 — 서버는 자체 30Hz 물리+벽 충돌 보유. 도입하면 500NPC 존 비용 실측 필요. **보류 권장**(캐논도 "관찰 전용 물리라 회귀 무영향" 명시) |
| — | 영토 1셀 크립의 공간 반영 | ③(기지 근사) | econ size는 매일 자람(①)이나 서버 공간 영토(hall data.bnd)는 시딩 스냅샷 고정 — 시각 부채로 기록(Stage 5 후보) |

---

## 3. 웨이브 로드맵 (2~4파)

### 2파 — 전쟁 작전층 + battle-core 재동기 + 도적 실체 ✅ **[완료 2026-07-12 — §6 구현 기록]**
1. **드리프트 감사 선행**: `sim/war-core.js`·`server/war-live.js`는 P1/P2 추출 시점 스냅샷 — 랩 전쟁 모듈 v3(전쟁실험실.html 5956~)가 그 후 진화(작전층·긴급소집·스폰 박멸·종전 재설계). 상수 블록(5961~6034)과 결단 로직을 랩 기준으로 diff → war-core 갱신분 목록화.
2. **작전층 상태기계**(§15): war-core daily에 camp/siege/assault/withdraw + 군량(동원 시 행군×2+공성팩 12일 선차감·camp 일 1일분·철수 환급) + NPC 결단 문턱(0.38 도박/0.58 돌격/군량>곳간 추정 포위/무혈 항복 곳간<5일&&승산<0.25) + 방어 3택(응전 0.52/농번기 0.40·버티기·해산, 히스테리시스 2일) + sortie. `_siegeBlock` 설치를 'march 창'에서 'siege 상태'로 옮김(현 단순화 제거).
3. **실체 층**: villages.js P3에 scramble(마을권 진입 시 실주민 긴급 징발)·_evac·walkover(주민 0 = 무저항 함락 — 표준배치 폴백 제거) — 랩 "사람 유닛 잔여 생성 경로 0" 예외 목록(도적 재결성·야생 LOD·headless) 계약 이식.
4. **도적 실체(1파 잔여)**: 소굴 주변 배회 NPC(dormant 패턴·소굴 마커 주변 2~3명)·캐러밴 요격 연출(경제 효과는 econ 주사위 — 이중계상 금지)·고립 NPC 습격(비치사 강탈)·토벌 전사 실파견(추상 판정 유지 + 실체 왕복). wildlife와 무접점 유지(캐논).
- 위험: war-core는 서버에서 이미 '작동 중'(선포·전투 발생) — 상태기계 교체는 회귀 면적이 큼. 훅 유무 A/B(전쟁 봉인 플래그)로 econ 궤적 보존 증명 필요.

### 3파 — 포로·J 벌점·평판 ✅ **[완료 2026-07-12 — §7 구현 기록]**
1. 포로 intake: war-core warResolveBattle 되먹임에 사상 35%→captive(대상: 추상 전투·라이브 되먹임·walkover — 항복 협상 제외), 상한 round(J×후보)&&승자 병력×0.5.
2. 엔티티 연속: econ npc splice→push(같은 객체·skills 보존) + P3 귀환 그룹 동반(호송 도보) + 화면 렌더(회색 테두리 — client.js others 렌더에 플래그).
3. 출구 3종 데일리(탈출 p=0.002+0.004×(1−d/500)·동화 3년·몸값 여유>60일&&원한<0.4) + EU 포로 가치(WAR_CAP_EU 0→8, WAR_CAP_FRAC 0.35 기존 자리).
4. 본체 벌점→J: central 벌점 레이어(개인→길드 집계) 설계 후 warJustice 가감 연결(하한 유지).
- 위험: captive 훅은 이미 v1에 있으니 econ 측 무위험. intake의 st.cas 회계(사망만 계수) 정합 주의.

### 4파 — 답압 길·움집·경도·(콜라이더 결정) ✅ **[완료 2026-07-12 — §8 구현 기록. 콜라이더 sepAgents=보류 확정(§2 표 권고 채택), 절대 시계=설계 주석·상수만(§8.5)]**
1. 답압 길: `server/roads.js` 신규(희소 Map+0.995^Δ 게으른 감쇠·T1=8/T2=28/MAX=120) — 스탬프 원천은 서버 이동 지점(NPC·캐러밴·행군) 편승, zone-local-db `roads` 테이블(밟힌 셀만·일 1회 dirty 플러시), villages 거리행렬/A*에 0.93/0.87 할인, 클라 틴트 오버레이.
2. 움집: village-layout.js MAX_FLOORS 4→1·수용 8 + 랩 VL(물가 회피 부지·잠식 비용) 재동기 diff. 기존 DB 마을: floors>1 행은 렌더만 1층 취급(마이그레이션 최소) or 존 리셋 시 자연 해소 — 결정 필요.
3. 경도 시각: 마을 `_lonOff=(ccx/cellsW)×0.045` — 서버 소비처가 생기는 2파(scramble 기상) 이후에 의미. 클라 밤 틴트 터미네이터는 독립 구현 가능.
4. 콜라이더 sepAgents: 도입 여부 결정(보류 권장 — §2 표).
5. 절대 시계: 별도 설계 세션(전 존·central 영향 — 캐논 §19 공식 채택 + econ 캐치업).

---

## 4. 1파 구현 기록 — 도적 시스템 서버 이식 (2026-07-12 완료)

### 4.1 파일·심볼
- **신규 `server/bandits.js`** (~400행): 캐논 §11 경제·수명주기 층 전체. 계수 `BDT_*`는 전쟁실험실.html 5809~5823 verbatim(보정 금지). 주요 함수: `denScan`(오지 스코어 소굴 배치)·`campSpot`(해체 은거지=길목)·`formGang`(econ npcs splice — 부활·유령 금지)·`daily`(위기 추적→①해체 전환/②절망 이탈·③소굴 재결성·유지비·토벌 판정·pairSync)·`installHooks`(world.banditRouteRisk/onBanditLoot)·`save/restore`(bandit_state)·`clientCamps`(마커 1종).
- **`server/villages.js`**: 추가 전용 2건 — init에 `state.villageSeed` 캡처(1줄), `banditHost()` 접점 export(마을 목록·econ world·지형 어댑터 ta·getRoute·broadcast·seed·셀 치수). 행동 무변경.
- **`server/zone-local-db.js`**: `bandit_state` 테이블(zone PK·data JSON·day — 존당 1행) + `getBanditState/upsertBanditState`.
- **`server/zone.js`**: 4건 최소 배선 — require(13행 부근), `Bandits.init()`(SimVillages.init 직후), welcome ×2에 `banditCamps`, gameLoop에 `Bandits.onGameTick(now)`(SimVillages.onGameTick 바로 옆).
- **`public/client.js`**: welcome/`bandit_camps` 수신 + `banditcamp` 렌더러 1종(검은 막사+🏴, 점유 단은 '도적 N명' 라벨, 빈 소굴 흐림).

### 4.2 설계 결정(랩과의 차이 — 전부 주석화)
- **소멸 이벤트는 존재하지 않는다**: 랩도 bandit 모듈의 일일 스캔이 해체(순감 5일+식량<3일+잔존≤min(20,피크×0.55))를 판정한다. 서버도 동일 — villages.js에 훅 지점을 만들지 않고 `daily()`가 econ 인구 미끄럼을 검출(전쟁 초토화 소멸도 자동 흡수).
- **추적 카운터(_bdtCri/_bdtFall/_bdtPeak/_banditized/_bdtExoAt/_bdtSupCd)는 vil.econ에 부착** — 랩은 랩 래퍼에 두지만 서버 래퍼는 비영속이라 econ이 올바른 자리(serializeEcon 자동 영속, plain number라 JSON 안전). `_banditRisk`는 원래 econ 소재.
- **교역로 거리 = 코스그리드(4셀) L1 거리장**(2패스 변환) — 랩의 '표본점 맨해튼 최솟값'과 동형·O(1) 조회. 유한쌍 판정은 `world._distMatrix`(도달불능쌍은 교역이 없으니 길목도 없음 — 정직). 다리 가중(BDT_BR_R)은 본체에 다리가 없어 미적용(자리 주석).
- **숲 판정 = `ta.forestMult(x,y)>1.2`** (villages extractLandParamsApprox와 동일 기준). wildlife의 _forM은 활성 청크 한정이라 전역 스캔에 부적합.
- **결정론**: 소굴 배치·재결성 시점/규모는 villageSeed 해시 RNG(랩 bdtDenRng 동형 — 시드만의 함수, 재부팅 불변). 단원 선발·토벌 주사위는 랩과 같이 Math.random(econ srand 스트림 불간섭).
- **onBanditLoot 로그는 대기열**: villages econ 틱이 console.log를 침묵시키는 창 안에서 불리므로 `queueLog`→daily 진입 시 방출(통계·링 버퍼는 즉시).
- 토벌 = 추상 판정(전사+사냥꾼 counts·무기 재고 — 랩 동형 계수). 실파견·배회·습격 연출은 2파.

### 4.3 게이트·가드
- `ENABLE_BANDITS=0` → init/onGameTick 즉시 return(완전 no-op, ENABLE_VILLAGES 관례). `ENABLE_VILLAGES=0`이면 banditHost()가 null → 자동 휴면.
- 테스트 훅(운영 무설정): `BANDIT_MINDAY`(정착 보호기 오버라이드), `BANDIT_FIXTURE=dissolve`(첫 데일리에 3~20명 최빈곤 마을 곳간 소거+순감 강제 — /tmp DB 전용).

### 4.4 검증(전부 /tmp DB 사본 — 실DB 무접촉·git 커밋 없음)
```
cp world-hanbando.db /tmp/bandit-smoke/runX.db
ZONE_ID=hanbando DB_PATH=/tmp/bandit-smoke/runX.db PORT=39xx VILLAGE_DAY_MS=400~1500 \
  [BANDIT_MINDAY=1 BANDIT_FIXTURE=dissolve] [ENABLE_BANDITS=0] timeout 38 node server/zone.js
```
- (a) `node --check` 5파일 전부 통과(bandits/villages/zone-local-db/zone/client).
- (b) 스모크: 소굴 3곳 배치 로그(오지점수 840·마을거리 529~714·전부 숲·2.4s 1회) · D1 재결성 카운트다운(D46/D46/D50 — 시드 결정론, 3개 런 동일) · 픽스처 해체→"광산1 해체(순감 5일·잔존 8명) → 도적단#1 3명 결성(은거지 2054,2079 — 길목쌍 1)" · **econ raid 실발화 4건**("D5 약탈 광산1→임업3 meat 16.5 → 단#1 장물 +8.3" 등, _banditRisk 학습 포함) · D31 기근 해산(약탈 수입 부족 → 굶주림 이탈 → 3명 미만 해산 — 자기조절) · D46/D50 소굴 3곳 재결성 실발화(4/5/6명) · 재부팅 복원("복원: 소굴 3곳(생존 3)·도적단 3(총원 15)·저장 day 52" → day 53 연속).
- (c) `ENABLE_BANDITS=0`: 공지 1줄 외 도적 로그 0·bandit_state 0행·**마을 인구 궤적(D1~6: 152,152,152,158,165,168)이 도적 ON 런과 동일** — '도적 0이면 완전 무해' 계약의 부수 증명(banditRouteRisk는 단 0이면 0 반환·RNG 무접촉).
- (d) villages 회귀: 19마을 시딩·econ 일틱(인구 152→246 성장)·캐러밴 실체 정상·BFS 행렬 로그 기존과 동일.

### 4.5 관찰·인계(1파 잔여)
- 소굴 3곳이 지도 북부 오지에 몰림(마을 분포상 오지 스코어 극대점) — 캐논상 위치가 곧 밸런스(보정 금지), 관찰 항목.
- BDT_MIN_DAY=365 → 신규 존은 실서버 ~2.5일(실시간, 10분/게임일) 후 원천①② 활성. 소굴③은 즉시(30~60일 카운트다운) — 의도.
- econ 훅은 world 함수라 비영속 — 부팅마다 bandits.init이 재설치(재부팅 복원 경로 포함, 검증됨).
- 도적 실체(배회·요격 연출·고립 습격·토벌 실파견)·wildlife 조우 규칙 = 2파.

---

## 6. 2파 구현 기록 — 전쟁 작전층 + 도적 실체 최소 (2026-07-12 완료)

### 6.1 war-core 재동기 감사 결론
- 상수 블록(랩 5961~6034) = war-core 기존 상수와 **일치**(드리프트 없음 — P1 추출이 정확했음). 랩 이후 진화분은
  ①작전층(7907~8140) ②포로(6206~6308 — 3파) ③방화(warResolveBattle 내 builtFloors--) ④warStats op/포로 카운터.
- **의도적 비이식(기록)**: 방화 — 랩은 관찰층 houses[].builtFloors(→_mapBeds)가 경제 의미를 가짐. 서버 실체 집은
  buildings 행(층별 wall/floor)이고 econ과 결합돼 있지 않아 지금 태우면 순수 시각 파괴 + 재실물화 비용만 듦 → 엔티티 부채로 보류(walkover 주석).
- 저장 소재 차이 유지(설계): 랩 vil.Map(_grudge 등) ↔ 서버 econ plain object(serializeEcon 영속) — P1 결정 그대로.

### 6.2 작전층(§15) — sim/war-core.js에 일 단위 상태기계로 이식
- **랩 블록B(프레임 틱) → daily(일 단위) 어댑터**: eta 도달=포위 결정 링 도착(추상층은 공간이 없어 WAR_SIEGE_R 불요).
  march→camp(도착·결단)→{assault|siege|withdraw}, 결단·항복·태세 전부 랩 수식/문턱 verbatim(rng 시드도 랩 동형 xorshift).
- 신규 심볼: WAR_SIEGE_PACK/PACK_CRIT/ASSAULT_ODDS/GAMBLE_ODDS/HOLD_GAMBLE/SORTIE_ODDS/DEF_RESPOND_ODDS/SURR_FOODD/
  SURR_ODDS/DEF_HYST + _opDefEff/_opAtkOdds/_opDefOdds/_opDefEstPack(pure) + createWar 내 _opSetSiege/_opPackLoad/
  _opPackRefund/_opDoSurrender(공납 음수 하한 0)/_warWalkoverOutcome/_opTransNPC/_opNpcDecide/_opCheckSurrender(절박 완화 ×2.2)/
  _opDefStance/_opDefenseDaily(sortie 포함)/_opResolveEngage. warStats에 siege/assault/withdraw/surrender/sortie/walkover.
- **자동 개전 폐지**: daily의 'eta 즉시 warResolveBattle' 제거(ops 모드) — 전투는 assault/sortie 결단에서만(_opResolveEngage).
  무저항 함락 = 방어 npcs<2 → _warWalkoverOutcome(J 상한 약탈·위신재·조공·평판 — 유령 유닛 0).
- **봉쇄 훅 이동**: _recomputeSiege가 'march 창'→'op===siege'만 설치(즉시 설치는 _opSetSiege, 일일 조정자+잔존 안전망 겸).
- **공성팩**: warMobilize에서 적재(_opPackLoad — 랩 _opInitWar의 동원 시점 전진·있는 만큼), camp/siege 일 1일분 소모, 철수·항복 환급.
- **게이트**: `WAR_OPS=0` → 전부 봉인, P1 폴백(eta 즉시 headless + march 창 봉쇄). 테스트 훅 `WAR_MINDAY`(BANDIT_MINDAY 관례).
- **실체 훅**: createWar opts.onEngage(w,day,why) — villages.js가 관측자 근접 시 LiveBattle 승격(true=phase 'battle').
  구 warLodResolveSweep(eta 즉시 승격)는 ops 모드에서 호출 중단(OFF 폴백만 유지 — villages.js onGameTick).

### 6.3 villages.js P3 정합(실체 층)
- `_warTryEngage`: ops 모드에선 w.op!=='assault'면 접근만으로 개전 금지(force=결단 경유만).
- `_warEnsureDefense`: 방어 3택 게이팅 — respond=기존 경보(180셀) 사전 포진 / hold=사전 소집 억제, assault 결단 적군이
  마을권(max(영토반경+40, 경보×0.5)) 진입 시 **긴급 소집(scramble)** — 집결지 회관 앞 12셀(WAR_SCRAM_STANDOFF),
  스냅 금지(자택·작업지에서 실이동 — 오합지졸 개전 창발), g._scram 표식.
- **_evac 최소판**: 전투·scramble 중 방어 마을 비징발 주민 p.simEvacUntil(5초 소프트 TTL, 집합 대사) →
  zone.js npcStep 1게이트(자택 고정·fight 예외). 종결 시 TTL 자동 해제.
- 전투 없는 종결(철수·항복·함락)의 실체 몸: 병사 그 자리 해제 → 일상 AI 도보 귀가(랩 _retFromMarchGroup 대형 귀환은 실체 부채로 기록).
- 테스트 훅(운영 무설정): `WAR_FIXTURE=siege|siegehold|assault|auto`(실경로 선포 강제) + `VILLAGE_WAR_LOG=1`(링 버퍼 드레인+봉쇄 관측 1줄).

### 6.4 도적 실체 최소(1파 잔여 중)
- 소굴 배회 NPC: bandits.js syncBodies — 단당 min(3,n)명 기존 spawnNpc/npcStep 배회 AI 재사용(홈=은거지·워크=둘레 도넛),
  simJob='bandit'(클라 🏴 이모지), 비영속(부팅 재실체화), `BANDIT_BODIES=0` 게이트. banditHost에 spawnNpc/players/npcs 3필드 추가.
- **보류(계획서 판단 — 수치 등가)**: 캐러밴 요격 연출·고립 NPC 습격·토벌 실파견 — 경제 효과는 econ 주사위가 전부(§11 캐논). 후속 웨이브 후보.

### 6.5 검증(전부 /tmp DB 사본 — 실DB 무접촉·git 커밋 없음·45초 회수)
```
cp world-hanbando.db /tmp/wave2/X.db
ZONE_ID=hanbando DB_PATH=/tmp/wave2/X.db PORT=397x VILLAGE_DAY_MS=400~800 WAR_MINDAY=1 \
  WAR_FIXTURE=siege|assault|siegehold [WAR_OPS=0] VILLAGE_WAR_LOG=1 timeout 20~42 node server/zone.js
```
- (a) node --check: war-core/villages/bandits/zone/client 전부 통과. _p3-war-probe **25/25**(프로브는 P1 의미론 하네스 —
  상단 WAR_OPS=0 기본 명시·주석 계약. 작전층 발화는 아래 존 스모크가 검증).
- (b) siege: 선포(공성팩 12.0일분)→D2 도착·주둔→방어 '버티기'→**포위 개시(방어 econ _siegeBlock=ON·야외×0.15 실측)**→
  D3 **절박 항복**(곳간 1.6→0.8일·문턱 완화 경로)→공납·조공 3년·포위 해제(훅 삭제 확인)→활성 0. 항복 후 곳간 0.8→21일 회복 관측.
- (c) assault: D2 돌격 결단→headless 전투(전술 공2→1 방4→4·147틱)→방어승·사상 공1·노획 1.1.
- (d) siegehold(철수): D2 포위→D12 팩<3 '군량 소진' 철수·잔량 환급.
- (e) **OFF 게이트 무해+등가**: WAR_OPS=0 = 작전층 로그 0·eta 즉시 전투(P1 원형). 동일 픽스처에서 전투 결과 문자열·
  인구 궤적(D1~6: 152,151,151,156,164,167) ON/OFF **바이트 동일** — 돌격 경로=P1 등가 + 게이트 무해 동시 증명.
- (f) 재부팅 왕복: siege.db 재기동 — econ day 51 연속·도적 3단 복원·**항복 조공 econ 영속 확인**(농촌17 _warTribOut
  {payee 농촌1, until 1083, next 93} + 원한 0.106 감쇠 중 + 양측 _warFatigue — DB 직접 조회).
- (g) 도적 회귀: 1파 결정론 유지(소굴 D46/D46/D50 재결성 — 시드 동일) + 배회 실체 9명(3단×3) 스폰·econ 무접촉.
- (h) villages 회귀: 19마을 시딩·econ 성장(152→360/D51)·캐러밴 실체 정상·BFS 행렬 로그 기존과 동일(4523ms·×1.54).

### 6.6 위험·인계(2파 잔여)
- **관측자(실플레이어) 경로 미발화**: onEngage 실체 승격·scramble 실발화·evac 게이트는 관측자 필요(헤드리스 스모크 한계).
  코드 경로는 P3 프로브(P1 모드)+힘 교전이 커버 — 실클라 접속 관찰 세션 1회 권장.
- walkover 실발화 없음(방어 주민<2 자연 발생 희귀) — 코드 경로 검토만. 3파 포로 스모크에서 재시도 후보.
- 랩 대비 남은 차이: 방화(§6.1)·전투 없는 종결의 대형 귀환 행군(§6.3)·sortie의 실체 전진 연출(추상 즉시 판정으로 대체).

## 7. 3파 구현 기록 — 포로 §18 + J·평판 점검 + 벌점 자리 (2026-07-12 완료)

### 7.1 war-core.js — 포로 econ 층(랩 6206~6308 어댑터·계수 verbatim)
- 상수 WAR_CAP_ESCORT/ASSIM/ESC0·1·D/RANSOM_* + **WAR_CAP_EU 0→8 활성**(개전 EU 기대 포로 가치 — 사람=재화).
- `warCapture`(1/2: 후보 round(사상×0.35)→상한 round(J×후보)&&승자 생존×0.5→패자 econ splice·captive 부착·무기직 박탈·초과 방면)
  · `warCaptiveIntake`(2/2: **warKill 이후** 승자 push — 림보 격리·랩 수리 계승) · `_capComplete`(귀향 splice 복귀) — 전부 pure.
- `warCaptiveDaily`(createWar 내·daily 2.5절): ⓪이송 큐(world._capTransit — eta 도착=_capComplete) ⓪′**재부팅 유실 재큐**
  (esc 마킹 잔존 시 eta=+1 재등록 — 랩엔 없는 서버 보강: TQ 비영속 정직 복구) ①동화 3년 ②탈출 p=0.002+0.004×(1−d/500)
  ③몸값(30일·p0.15·40/명·고향>60일치·원한<0.4). 결정론(makeRng — 랩 _warCapRng 동형 시드).
- warResolveBattle 재동기: J 선산정→warCapture(패자 로스터 우선)→warKill은 기절(후보)분 제외(st.cas=사망만 계수)→intake→
  베테랑 성장(포로는 패자 econ에 없어 자연 제외)→`w._capResolve` 스태시(+아웃컴·포로 로그). 항복·무저항 함락=사상 0 → 자연 무포로.
- warStats: captured/capFreed/escaped/assimilated/ransomed. 게이트 `WAR_CAPTIVES=0`(CAP_EU도 0 — 완전 봉인).
- **벌점 연동 자리(§17 인계)**: warJustice 상단 TODO 주석 — central 벌점 원장(개인→길드 karma)→J′=max(J_floor, J−k×karma),
  인터페이스 예약 `createWar(opts.karmaOf?(vilName))` 미주입·미호출(무해). 구현은 central 설계 세션 몫.

### 7.2 호스트(실체·클라) 최소
- villages.js `_warOnResolved`: 패자측 '사상 미러' pid 중 포로 수만큼 despawn 대신 **승자 마을 이관**(npcPids·simVillageId —
  econ 이전과 방향 일치)+`simCaptive` → 공격승이면 귀환 그룹 동반 도보(호송), 방어승이면 그 자리 억류 해제. 표본 근사(정직):
  pid는 econ npc와 1:1 아님 — 수만 일치, 이후 출구는 syncVillagePop 재수렴이 흡수.
- zone.js makeEntry `cap` 동적 1비트 → client.js others 병합 + drawPlayerIso **발치 회색 링**(§18 '회색 테두리' 최소).

### 7.3 검증(/tmp DB·45초 회수·git 무접촉)
- (a) node --check 4파일(war-core/villages/zone/client) + _p3-war-probe 25/25 유지.
- (b) 존 스모크(WAR_FIXTURE=assault·D50 본격전 6vs7): **"포로 1명 임업3→광산1 [후보1·J0.60·호송상한3]"** 실발화 —
  이 전투는 방어 **sortie 결단 개전**(응전 승산 68%≥0.60 — 자동 개전 폐지 하에서 방어가 전투를 연 자연 사례 부수 검증).
  DB 확인: 광산1 econ npc n294 captive{home:임업3, since:51} + **무기직 박탈**(→forager — 광산 땅 opts 순서 정합).
- (c) 출구 하네스(결정론·실코드 createWar): 동화 D1(3년 경과 픽스처)·탈출 D26→이송 도착 D27(econ splice 복귀·counts 정합)·
  몸값 D150(식량 40 이전)·재부팅 유실 재큐 1건 — 인구/카운트 대차 일치(A 12·B 8).
- (d) J·평판 점검(하네스): prestige(J 0.40) 결판 → **'불의 침공 소문' 발화·st.unjust=1·관전촌 원한 +0.033**(=(0.5−J)×0.35×prox) +
  **포로 상한의 J 감산 실증**(후보 1 → round(0.4)=0 → 전원 방면). grudge 0.5 주입→grudgeBlockSweep→**econ._grudgeBlock={침략촌:1} 발행**.
- (e) OFF 게이트: WAR_CAPTIVES=0 → CAP_EU=0·warCapture no-op(계약대로 2파 궤적 복귀).
- (f) 재부팅 왕복: cap3.db 재기동(econ day 77) — captive 필드 직렬화 왕복 생존(n294 유지).

### 7.4 위험·인계
- WAR_CAP_EU=8의 선포율 영향은 랩 A/B(선포 3→1)만 근거 — 서버 자연 전쟁(365일+) 장기 관찰 항목.
- 호송 표본의 시각 잔재: simCaptive pid가 econ 출구(탈출·동화)와 비동기 — pop 재수렴이 정리하나 회색 링이 수일 남을 수 있음(관찰).
- 몸값·탈출의 화면 '도보 귀향' 실체는 추상 이송(TQ) — P3 호송 연출은 승자행만 구현(귀향행은 부채로 기록).

## 8. 4파 구현 기록 — 움집·답압 길·물가 회피·영토 크립·경도 (2026-07-12 완료)

### 8.1 움집(§19) — server/village-layout.js 랩 쌍둥이 동기
- `MAX_FLOORS 4→1`(다층 폐지·densify 봉인 — 랩 L_MAXFL=1/VL 4784행 쌍둥이), `HOUSE_CAP_PER_FLOOR 6→8`(랩 L_FLOORCAP=8).
- **마이그레이션 결정 확정: '기존 다층 유지·신축만 단층'** — materializeVillageStructures가 DB floors를 그대로 실물화(클램프 없음)
  하므로 코드 0줄로 성립(인구 결박 없음). 검증: floors=3 픽스처 주입 부팅 → 실물화 행 6061→6151(+2층분) 정상.
- 신규 시딩 회귀: 19마을 시딩 로그 **바이트 동일**(집 2·논밭·land 파라미터 — 초기 pop 8은 CAP 6→8과 무관: houses<2 하한이 지배)
  + 신축 집 floors 전부 1(DB 확인) + 인구 궤적 D1~6 = 152,152,152,158,165,168(1파 기록과 동일 — econ 무영향).

### 8.2 물가 회피 부지(§19 후속) — shore 레이아웃 K/d²
- 랩 4807~4808 이식: 부지 가장자리~물 최소 체비셰프 거리(wDist ±10·d=cheb−HOUSE_HALF) → `W_PEN_K/d²`(K=2000 verbatim)
  연속 페널티를 shore 후보 점수에 가산 — 강안 늘어서기 유지·최전선 급격 비싸짐. 하드 한계(+2셀 링)는 기존
  farFromWater(±4 = 5×5 발자국 ±2 + 2링)가 이미 소유(캐논 '+2링' 기충족 확인). cluster/dispersed는 랩도 링+약페널티만 — 동형 유지.
- '서버 addHouseSite 상당 지점' 결론: 서버는 성장형 집터 추가가 없음(시딩 1회) — 적용 지점은 generate() 후보 점수(위).
  성장층(addHouseSite) 도입 시 랩 5357~5388(2패스 잠식 비용·답압 길 부지 제외 포함) 이식 — 인계.

### 8.3 답압 길(§16) — server/roads.js 신규 + DB + 클라
- **server/roads.js**: 희소 Map(밟힌 셀만·정수키) + 게으른 감쇠(접근 시 0.995^Δ일 — 일괄 패스 0) + T1=8/T2=28/MAX=120 +
  배속 [1,1.10,1.15]·A* 할인 [1,0.93,0.87] 전부 랩 verbatim. RNG 무소비. `ENABLE_ROADS=0` 완전 no-op.
- **스탬프 원천(이동 편승 — 서버엔 sepAgents 없음)**: ①zone.js movePlayerStep(NPC·플레이어 — 셀 변경 시만, 개체 캐시 _rdK)
  ②캐러밴 몸(tickCaravanBodies) ③전쟁 행군·귀환 지휘관 선(대형 전체 통행의 근사 — 병사 개별은 movePlayerStep 비경유라 생략 기록).
  ★서버 고유 정합(정직): dormant NPC는 실보행이 없어 답압 0 — 랩 '빨리감기=답압 0' 계약과 동형(관측자 있는 곳＋캐러밴 간선이 길이 됨).
- **효과 배선**: NPC 보행 ×1.10/1.15(movePlayerStep — ★플레이어 배속 제외: 클라 이동 예측이 길을 몰라 러버밴딩. 랩과 의도적 차이 주석)
  · 교역/행군 경로 A* 할인 = computeRoutePts ×10 정수 스케일(100/140→93/130·87/122) + coarse(4셀 그리드 최대 등급) 조회
  — 거리행렬(econ 운송비)·bfs 무접촉(랩 5007 계약: 경로 모양만·h 소폭 비허용 허용). 반영 시점 = 경로 캐시 재계산 때(기존 수명 정책).
- **DB 영속**: zone-local-db `roads`(zone,cell_key PK — v·d), 게임일 1회 dirty 배치 플러시(트랜잭션)·소멸 셀 행 삭제.
- **클라 최소**: welcome `roads` flat [cx,cy,lv] + 게임일 1회 `road_cells` 변경분(등급 전이만) → 바닥 다이아몬드 틴트
  (lv1 옅은 흙·lv2 짙음, z=-950 지면 오버레이 — 베이크 무접촉).
- 검증: 단위(40스탬프→lv2·coarse 0.87 / 10→lv1·0.93 / 배속 캐시 1.15 / 139일 감쇠 40→19.9→lv1 / 1200일→소거·행 삭제 /
  플러시 2행·diff 2셀) + 존 스모크(45게임일 4,736셀 축적·등급 11셀 발생·road_cells 방송·재부팅 4,736셀 복원) +
  ENABLE_ROADS=0(행 0·인구 궤적 동일 — 무해). ※배속 아티팩트 기록: VILLAGE_DAY_MS=500 테스트에선 감쇠도 1,200배 가속이라
  등급이 일시적(랩 '고배속=점선 답압' 한계 동형) — 운영 10분일에선 캐러밴 간선 등급 누적 정상(수식 단위검증 완료).

### 8.4 영토 셀 크립(§2 매트릭스 잔항) — 확인·정합
- 확인: 서버 공간 영토는 시딩 스냅샷(hall data.bnd — 불변), econ land.size는 매일 1셀 단위 성장(36f7860) — 괴리 확정.
- 정합(최소·정직): clientPayload/`sim_village_day`에 `tr`(econ 영토 등가 반경 px — √(size×25/π)×32) 일일 동기 →
  클라 simvil 렌더가 **호박색 점선 링**으로 '경제 영토의 현재 크기'를 표시(bnd 초록 실선=시딩 실물, 점선=크립).
  실물 경계 성장(bnd 재계산·프론티어 크립)은 Stage 5 후보로 존치(§2 표 그대로 — 시각 부채 해소는 링으로 완화).

### 8.5 경도 시각·절대 시계(§19)
- 서버: 마을 `_lonOff=(ccx/셀폭)×0.045`(랩 L_LON_SPREAD verbatim — 실측 스프레드 2~63분/일 로그), spawnOneNpc가
  p.simLonOff 부여 → zone.js npcStep **야간 귀가 게이트**(fv=(전역 phase+_lonOff)%1 > dayPhaseRatio → 자택 대기) =
  서버 첫 스케줄 소비처(동쪽 마을이 먼저 잔다). 야생·도적·econ 일 경계는 전역 유지(랩 5351 블록 계약). `VILLAGE_LON=0` 게이트.
- 클라 표시 공식: `_lonView=(카메라x−존x0)/존폭×0.045`를 worldPhase()에 가산 — 시계 HUD·밤 오버레이·(밤) 라벨이 로컬 태양시.
  (픽셀 단위 터미네이터 그라디언트는 후속 — 뷰 중심 근사로 이동 시 자연 전이, 주석 기록.)
- 절대 시계: **zone-config.js ABS_CLOCK_DESIGN**(GAME_TIME_SCALE=60·REAL_EPOCH_UTC·ADOPTED:false) + 설계 주석 —
  캐논 §19 공식·시각/상태 분리·캐치업 필요·채택 시 이행 경로 명시. 동작 무변경(어떤 코드도 미참조 — 별도 웨이브 §3-4파 5항 유지).

### 8.6 검증 요약·위험
- node --check 8파일(villages/zone/roads/zone-local-db/zone-config/village-layout/client/war-core) 전부 통과.
- 잔여 위험: ①클라 답압 렌더·크립 링·경도 틴트는 헤드리스 스모크 한계(브라우저 관찰 세션 권장 — welcome/broadcast 페이로드는 로그로 확인)
  ②roads Map 장기 성장(밟힌 셀 무한? — v<1 자동 소거가 상한. 운영 관찰: 일 플러시 행수) ③A* 할인은 경로 캐시 재계산 시점에만 반영(계약).

## 5. 위험 대장(전 웨이브 공통)
1. **war-core/war-live 포크 드리프트**: 랩 war v3가 P1/P2 추출 후 진화 — 2파는 "이식"이 아니라 "재동기+확장". 감사 없이 얹으면 이중 규칙(예: march 봉쇄 vs siege 봉쇄) 충돌.
2. **serializeEcon 키 오염**: 훅·레이어가 econ에 객체 참조를 두면 직렬화 순환(안전망은 키 드랍+경고). 신규 키는 plain number/string만(1파 준수).
3. **부팅 비용 누적**: BFS 행렬 4.6s + 도적 소굴 스캔 2.4s(각 1회). 웨이브마다 부팅 스캔 추가 시 존 기동 시간 감시(fly 헬스체크 한도).
4. **랩=작업 정본 원칙**: 서버 어댑터는 랩 계수 verbatim(보정 금지) — 서버 지형·마을 수 차이가 밸런스를 바꾸면 계수가 아니라 캐논을 갱신.
5. 실DB(world-*.db, central.db) 쓰기 금지 — 테스트는 /tmp 사본, 45초 프로세스 회수(CHECKLIST 검증 관례).
6. **★git 인덱스 이상 관찰(2026-07-12 2~4파 세션 — 수정하지 않고 기록만)**: 인덱스에 다수 파일이 삭제로 스테이징돼
   있음(`git status`에 `D server/villages.js`·`D sim/battle-core.js` 등 — 이전 세션 잔재로 추정). **워크트리가 진실**
   (battle-core·economy-sim v1/v2·browser 번들은 HEAD와 바이트 동일 해시 확인, wildlife.js는 세션 이전 mtime의
   선재 미커밋 수정 +16행 — P3 warThreats 통합분으로 추정). 2~4파 세션은 git 무접촉(커밋 금지 준수) — 다음 커밋
   세션이 인덱스를 정리(`git add -A` 재스테이징 등)한 뒤 커밋할 것. 이 상태로 `git diff`(인덱스 기준)를 읽으면
   econ 파일이 수정된 것처럼 보이니 반드시 `git diff HEAD`로 감사할 것.

---

## 9. 전면 재감사 + de-abstract 이식 (2026-07-14)

### 9.1 시스템별 랩↔서버 재-diff (5개 병렬 감사)
**결론: 값/상수 드리프트 0.** 도적(BDT_*)·전쟁(WAR_*)·답압길(L_ROAD_*)·야생(MOB_*)·경도(0.045) 전 상수 verbatim 일치, 수명주기·작전 상태기계·전투 로직 코어 일치. 잔여 차이는 전부 (a)문서화된 LOD 추상 (b)2파 실체 유예 (c)실측 스케일 계약(집 5×5 vs 12×12)뿐 — 값 충돌 없음.
- **야생 군대인지: 오탐 정정** — 서버는 `wildlife.js:574 H.warThreats()` 전용 채널로 이미 처리(랩은 agrid 병합, 서버는 별 채널·이중등록 방지). §5.6의 'wildlife +16행 warThreats'가 그것. **포팅 불요.**
- **compactW 0(서버) vs 0.8(랩)**: 서버 `village-layout.js:44`에 "landValue 목초 포함→리본 안 됨" 명시 이유 — 의도적, 유지.
- **방화(WAR_J_BURN 3.5)**: 서버 집=econ 비결합(엔티티 부채 §6.1) → 미배선 死상수. war-core.js:49에 주석화(삭제 대신 랩 계약 보존).

### 9.2 de-abstract 이식 (이번 세션 delta — "주사위 제거" 지시)
econ 층 캐러밴 약탈(economy-sim-v2)·랩 토벌을 per-entity 라운드 스크럼으로. econ은 공유라 자동 서버 도달, 서버 배선 3건 `server/bandits.js`(랩 banditDaily 동형·node --check OK): ①`world.banditGang` 훅(installHooks — 캐러밴 de-abstract 발화 필수) ②토벌 per-entity 스크럼 ③파견 튜닝(force<갱 보류·fN=min(force,ceil(갱×1.5))). 상세 메모리 [[durango-bandit-raid-phase2]].

### 9.3 마을실험실 VL 스테일 동기(canon)
`마을실험실.html:5212` `L_FLOORCAP 4→8·L_MAXFL 4→1`(움집 단층 — 전쟁실험실+서버와 일치, 종전 4층×4 스테일). 축소검증(seed42@1200): 무크래시·bad0·마을8/8·인구1000·집164(용량 반감분 보상). **잔여**: W_PEN_K 물가회피·roadLevel≥1 농지/집터 제외 로직은 미이식(전쟁실험실→마을 포팅 필요·회귀 정본이라 신중).

### 9.4 잔여·미커밋
- **@1500 26/26 재베이스라인 필수(로컬)**: 이번 세션 전부(움집·토벌/캐러밴 de-abstract) econ 궤적 이동 — 축소검증만 됨. **서버 존 스모크(DB 사본)도 로컬.**
- 미커밋(git 커밋 금지 계약 유지). 문서 위생 잔여: war-core:35/81 stale line-ref·WAR_MARCH 파생 주석·경도 0.045 명명상수화(서버 villages.js:1180) — 값 무관.
