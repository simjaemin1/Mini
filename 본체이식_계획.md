# 본체 이식 계획 — 랩(전쟁실험실) ↔ 서버(server/) 엄밀 차이 감사 + 웨이브 로드맵 (2026-07-12)

> 자족 문서: 새 세션이 이 파일만 읽고 이어받을 수 있게 파일 경로·심볼·계약을 명시한다.
> 캐논: `/Users/simjaemin1/Mini/설계_실축화_1셀1m.md` §11~19 · 검증 관례: `durango-mini/CHECKLIST.md`(프로브 45초·보호 3블록·훅 미설치=무해).
> **1파(도적 §11)는 이 세션에서 이식 완료** — §4에 구현 기록. 2~4파는 §3 로드맵.

---

## 1. econ 공유 실태 (확정 — "이미 서버에 도달한 것")

서버는 랩과 **같은 엔진 파일을 require**한다(사본 아님):

- `server/villages.js:1091` → `require('../sim/economy-sim')` (v1, 196KB)
- `server/villages.js:1092` → `require('../sim/economy-sim-v2')` (v2)
- `server/villages.js:1093` → `require('../sim/war-core')` (P1 전쟁 econ 층)
- `server/villages.js:1175` → `require('../sim/battle-core')` (보호 블록 — 바이트 계약)

랩 HTML은 같은 sim 파일을 `sim/build-econ-bundle.js → economy-engine.browser.js → inline-engine.js`로 인라인(3사본 규칙, CHECKLIST 7행). 즉 **sim/economy-sim(-v2).js에 들어간 것은 전부 서버에 자동 도달**하며, 호스트 훅이 미설치면 무해(A/B 바이트 동일 — 랩에서 증명된 계약)다.

### 서버에 이미 도달해 있는 최신 econ 기능(훅 인벤토리 — 파일:행)

| 기능 | 위치 | 서버 상태 |
|---|---|---|
| 영토 1셀 단위 구매(일 4셀·연속 체증, 36f7860) | economy-sim.js (v1 내부) | **도달·작동 중**(훅 아님 — 무조건 경로) |
| `_siegeBlock` 발/착/재라우팅 캐러밴 차단 | economy-sim-v2.js:361·422·607 | 도달. war-core가 march 창에 설치(§2 참고) |
| `_siegeOutMul` 야외 직업 생산 감산 | economy-sim.js:1130~1141 | 도달. war-core 설치 |
| `banditRouteRisk` 길목 위험 주입(파견 기대손실 435·outbound 564·귀환 735) | economy-sim-v2.js | 도달. **1파에서 배선 완료(bandits.js)** |
| `onBanditLoot` 장물 콜백 | economy-sim-v2.js:581·746 | 도달. **1파에서 배선 완료** |
| `_banditRisk` EMA→호위 요청 가중 ×(1+2×risk) | economy-sim-v2.js:499~502 | 도달. 1파가 EMA를 공급(추가로 war-core:500 목책 증축도 이 값을 읽음 — 사슬 자동 연결) |
| `_grudgeBlock` 원한 교역 대칭 차단 | economy-sim-v2.js:423·608 | 도달. war-core:366~368이 발행(작동 중) |
| `captive` 노동 ×0.6·직업 게이트 | economy-sim.js:1133~1141·2000·2039 | 도달·**미설치=무해**(포로 intake가 없어 captive가 안 생김 — 3파) |
| `_tradingN` 여가 기회비용·여가→행복 | economy-sim-v2.js:367 + v1 §12 항 | 도달·작동 중 |
| `lastStats`(happiness/health) | economy-sim.js:1589 | 도달 — 1파 도적 절망 이탈이 사용 |

결론: **"econ 주사위는 이미 서버 도달, 훅 배선만"이라는 전제 그대로 확인됨.** 서버 이식의 실작업은 (a) 호스트 훅 설치 모듈, (b) 랩 호스트 층(지형·실체) 어댑터 두 가지뿐이다.

---

## 2. 기능 매트릭스 — 캐논 §11~19 × 서버 상태

분류: ①=공유 econ으로 이미 서버 도달 · ②=서버 어댑터 필요(접점 명세) · ③=랩 전용 시각/관찰 물리(이식 불요) · ④=설계 결정 필요

| 캐논 | 기능 | 분류 | 비고(접점) |
|---|---|---|---|
| §11 | 도적 econ 주사위·호위 학습 | ① | v2 도달 — 배선만 필요했음 |
| §11 | 도적 3원천·소굴·유지비·토벌(경제·수명주기) | ② → **1파 완료** | `server/bandits.js` 신규(§4) |
| §11 | 도적 실체(소굴 배회 NPC·캐러밴 요격 연출·고립 습격, 랩 updateBandits 6674~) | ② 2파 | 경제 효과는 econ 주사위가 전부 — 연출 생략해도 수치 등가. isNpc/dormant 재사용 |
| §12 | 노동·여가(여가→행복·_tradingN·건축 징발 실효화) | ① | 전부 econ 내부. 반일 근무 연출은 ③ |
| §13 | 전쟁 econ(명분 4종·EU 개전·나선 방지 4중·동원·결과 4종·목책·트라우마) | ① 사실상 | `sim/war-core.js`(P1)가 이미 서버 배선(villages.js:1159 createWar + onGameTick:1585 daily). 상수는 랩 5961~6034 verbatim |
| §13 | 청동 희소성(주석 비축·무기 품질·자가도구) | ① | v1 내부 |
| §13 | 소멸 마을→도적 파이프라인 | ✅ 1파 | bandits.js 해체 검출이 전쟁 초토화도 흡수(별도 이벤트 불요 — §4.2) |
| §14 | battle-core(병종 8·개별 사기·궤주·지휘 대형) | ① | `sim/battle-core.js` 공유·무수정 계약 |
| §14 | LOD 실체 전투·행군·귀환(P2/P3) | 서버 기구현 | `server/war-live.js` + villages.js:1254~1551(관측자 근접 게이트 — 서버 고유 설계, 랩과 등가 아님) |
| §14 | 접근→충돌 전투(경보 180·포진·50m 개전)·행군 실측 속도 | ② 2파 | war-live/P3에 부분 반영(WAR_ALERT_R·WAR_ENGAGE_R 있음) — 랩 v3와 드리프트 감사 필요 |
| §15 | 작전층: march→camp→{assault\|siege\|withdraw} 상태기계·군량 적재/소모·방어 3택·무혈 항복·sortie | ② **2파 본대** | war-core는 명시 제외(헤더 "P1 제외: 공성 결단·siege 상태머신"). 현 서버는 'march 창 = 봉쇄' 단순화(war-core:477~480) — 상태기계로 대체해야 함 |
| §15 | 긴급 소집(scramble)·비전투원 _evac·무저항 함락(walkover)·합성 유닛 전량 금지 | ② 2파 | 실체 층 — villages.js P3(_warDraftPids·_warEnsureDefense)에 얹기. 현 서버 P3는 '주민 0=표준배치 폴백 or headless'로 이미 유령 최소화 방향이나 랩 최종 규칙(스폰 0 예외 목록)과 재동기 필요 |
| §17 | 전쟁 정의 J·승리 권리 스케일 | ① | war-core:303(warJustice)·465~470(J 약탈·불의전 피로) — 이미 서버 작동 |
| §17 | 제3자 평판 전파·_grudgeBlock 제재 | ① | war-core:355(전파)·366~368(발행) |
| §17 | 무기 드랍·궤주 투기(원장) | ① | war-core:307~323 |
| §17 | 본체 벌점(카르마)→J 연동(개인→길드 집계·세탁 방지) | ④ 3파 | 캐논 §17 인계 — central 벌점 레이어 설계 필요(warJustice TODO 자리 예약됨) |
| §18 | 포로 econ 훅(노동 ×0.6·직업 게이트) | ① | 도달·미설치=무해 |
| §18 | 포로 intake(사상 35%·J 상한·호송력)·호송 동반·출구 3종(탈출/동화/몸값)·EU 포로 가치 | ② **3파 본대** | war-core `WAR_CAP_EU = 0` 명시(포로 미포함). intake는 warResolveBattle 되먹임 + P3 귀환 그룹에 동반. 몸값은 마을 단위 |
| §16 | 답압 길(희소 Map·게으른 감쇠·T1/T2·속도·A* 할인·부지 제외) | ② 4파 | 로직은 순수(이식 쉬움). 접점: 스탬프 원천=서버 개체 이동(movePlayerStep/npcStep/캐러밴 tickCaravanBodies/행군 P3 — 서버엔 sepAgents가 없어 별도 순회 or 이동 루프 편승) · A* 할인=villages.js computeRoutePts/거리행렬 · 부지 제외=village-layout · **DB 영속**(캐논 §16 한계 명시 — zone-local-db roads 테이블) · 클라 오버레이 틴트 |
| §19 | 움집 단층 8명(L_MAXFL=1·FLOORCAP=8) | ② 4파 | **서버 쌍둥이 드리프트 확인됨**: `server/village-layout.js:109` `MAX_FLOORS = 4` (랩은 1). 기존 DB 마을 floors 마이그레이션은 ④(재시딩 vs 층 무시) |
| §19 | 경도 로컬 시각(_lonOff — 인간 일과만 로컬) | ②/④ 4파 | 서버 NPC 일과 모델이 얕음(npcHome/Work 배회) — 적용 지점 재설계 필요. econ 일 경계는 전역 유지(블록 계약) |
| §19 | 절대 타임스탬프 시계(게임시각=공식·상태=틱·캐치업) | ④ | 본체 설계 — zone-config WORLD(worldEpoch·dayLengthMs) 재정의 + econ 캐치업. 별도 웨이브(전 존 영향) |
| §19 | 논밭 입지(물가 논·내륙 밭·부지 회피) | 확인 필요 | village-layout 서버 사본에 논/밭 로직 존재(98~120행) — 랩 최신(물가 회피 부지·집터 잠식 비용)과 diff를 4파에서 움집과 함께 재동기 |
| §15후속 | 통합 콜라이더 sepAgents(맵 물리) | ③/④ | 랩 관찰(slow) 전용 물리 — 서버는 자체 30Hz 물리+벽 충돌 보유. 도입하면 500NPC 존 비용 실측 필요. **보류 권장**(캐논도 "관찰 전용 물리라 회귀 무영향" 명시) |
| — | 영토 1셀 크립의 공간 반영 | ③(기지 근사) | econ size는 매일 자람(①)이나 서버 공간 영토(hall data.bnd)는 시딩 스냅샷 고정 — 시각 부채로 기록(Stage 5 후보) |

---

## 3. 웨이브 로드맵 (2~4파)

### 2파 — 전쟁 작전층 + battle-core 재동기 + 도적 실체
1. **드리프트 감사 선행**: `sim/war-core.js`·`server/war-live.js`는 P1/P2 추출 시점 스냅샷 — 랩 전쟁 모듈 v3(전쟁실험실.html 5956~)가 그 후 진화(작전층·긴급소집·스폰 박멸·종전 재설계). 상수 블록(5961~6034)과 결단 로직을 랩 기준으로 diff → war-core 갱신분 목록화.
2. **작전층 상태기계**(§15): war-core daily에 camp/siege/assault/withdraw + 군량(동원 시 행군×2+공성팩 12일 선차감·camp 일 1일분·철수 환급) + NPC 결단 문턱(0.38 도박/0.58 돌격/군량>곳간 추정 포위/무혈 항복 곳간<5일&&승산<0.25) + 방어 3택(응전 0.52/농번기 0.40·버티기·해산, 히스테리시스 2일) + sortie. `_siegeBlock` 설치를 'march 창'에서 'siege 상태'로 옮김(현 단순화 제거).
3. **실체 층**: villages.js P3에 scramble(마을권 진입 시 실주민 긴급 징발)·_evac·walkover(주민 0 = 무저항 함락 — 표준배치 폴백 제거) — 랩 "사람 유닛 잔여 생성 경로 0" 예외 목록(도적 재결성·야생 LOD·headless) 계약 이식.
4. **도적 실체(1파 잔여)**: 소굴 주변 배회 NPC(dormant 패턴·소굴 마커 주변 2~3명)·캐러밴 요격 연출(경제 효과는 econ 주사위 — 이중계상 금지)·고립 NPC 습격(비치사 강탈)·토벌 전사 실파견(추상 판정 유지 + 실체 왕복). wildlife와 무접점 유지(캐논).
- 위험: war-core는 서버에서 이미 '작동 중'(선포·전투 발생) — 상태기계 교체는 회귀 면적이 큼. 훅 유무 A/B(전쟁 봉인 플래그)로 econ 궤적 보존 증명 필요.

### 3파 — 포로·J 벌점·평판
1. 포로 intake: war-core warResolveBattle 되먹임에 사상 35%→captive(대상: 추상 전투·라이브 되먹임·walkover — 항복 협상 제외), 상한 round(J×후보)&&승자 병력×0.5.
2. 엔티티 연속: econ npc splice→push(같은 객체·skills 보존) + P3 귀환 그룹 동반(호송 도보) + 화면 렌더(회색 테두리 — client.js others 렌더에 플래그).
3. 출구 3종 데일리(탈출 p=0.002+0.004×(1−d/500)·동화 3년·몸값 여유>60일&&원한<0.4) + EU 포로 가치(WAR_CAP_EU 0→8, WAR_CAP_FRAC 0.35 기존 자리).
4. 본체 벌점→J: central 벌점 레이어(개인→길드 집계) 설계 후 warJustice 가감 연결(하한 유지).
- 위험: captive 훅은 이미 v1에 있으니 econ 측 무위험. intake의 st.cas 회계(사망만 계수) 정합 주의.

### 4파 — 답압 길·움집·경도·(콜라이더 결정)
1. 답압 길: `server/roads.js` 신규(희소 Map+0.995^Δ 게으른 감쇠·T1=8/T2=28/MAX=120) — 스탬프 원천은 서버 이동 지점(NPC·캐러밴·행군) 편승, zone-local-db `roads` 테이블(밟힌 셀만·일 1회 dirty 플러시), villages 거리행렬/A*에 0.93/0.87 할인, 클라 틴트 오버레이.
2. 움집: village-layout.js MAX_FLOORS 4→1·수용 8 + 랩 VL(물가 회피 부지·잠식 비용) 재동기 diff. 기존 DB 마을: floors>1 행은 렌더만 1층 취급(마이그레이션 최소) or 존 리셋 시 자연 해소 — 결정 필요.
3. 경도 시각: 마을 `_lonOff=(ccx/cellsW)×0.045` — 서버 소비처가 생기는 2파(scramble 기상) 이후에 의미. 클라 밤 틴트 터미네이터는 독립 구현 가능.
4. 콜라이더 sepAgents: 도입 여부 결정(보류 권장 — §2 표).
5. 절대 시계: 별도 설계 세션(전 존·central 영향 — 캐논 §19 공식 채택 + econ 캐치업).

---

## 4. 1파 구현 기록 — 도적 시스템 서버 이식 (2026-07-12 완료)

### 4.1 파일·심볼
- **신규 `server/bandits.js`** (~400행): 캐논 §11 경제·수명주기 층 전체. 계수 `BDT_*`는 전쟁실험실.html 5809~5823 verbatim(보정 금지). 주요 함수: `denScan`(오지 스코어 소굴 배치)·`campSpot`(해체 은거지=길목)·`formGang`(econ npcs splice — 부활·유령 금지)·`daily`(위기 추적→①해체 전환/②절망 이탈·③소굴 재결성·유지비·토벌 판정·pairSync)·`installHooks`(world.banditRouteRisk/onBanditLoot)·`save/restore`(bandit_state)·`clientCamps`(마커 1종).
- **`server/villages.js`**: 추가 전용 2건 — init에 `state.villageSeed` 캡처(1줄), `banditHost()` 접점 export(마을 목록·econ world·지형 어댑터 ta·getRoute·broadcast·seed·셀 치수). 행동 무변경.
- **`server/zone-local-db.js`**: `bandit_state` 테이블(zone PK·data JSON·day — 존당 1행) + `getBanditState/upsertBanditState`.
- **`server/zone.js`**: 4건 최소 배선 — require(13행 부근), `Bandits.init()`(SimVillages.init 직후), welcome ×2에 `banditCamps`, gameLoop에 `Bandits.onGameTick(now)`(SimVillages.onGameTick 바로 옆).
- **`public/client.js`**: welcome/`bandit_camps` 수신 + `banditcamp` 렌더러 1종(검은 막사+🏴, 점유 단은 '도적 N명' 라벨, 빈 소굴 흐림).

### 4.2 설계 결정(랩과의 차이 — 전부 주석화)
- **소멸 이벤트는 존재하지 않는다**: 랩도 bandit 모듈의 일일 스캔이 해체(순감 5일+식량<3일+잔존≤min(20,피크×0.55))를 판정한다. 서버도 동일 — villages.js에 훅 지점을 만들지 않고 `daily()`가 econ 인구 미끄럼을 검출(전쟁 초토화 소멸도 자동 흡수).
- **추적 카운터(_bdtCri/_bdtFall/_bdtPeak/_banditized/_bdtExoAt/_bdtSupCd)는 vil.econ에 부착** — 랩은 랩 래퍼에 두지만 서버 래퍼는 비영속이라 econ이 올바른 자리(serializeEcon 자동 영속, plain number라 JSON 안전). `_banditRisk`는 원래 econ 소재.
- **교역로 거리 = 코스그리드(4셀) L1 거리장**(2패스 변환) — 랩의 '표본점 맨해튼 최솟값'과 동형·O(1) 조회. 유한쌍 판정은 `world._distMatrix`(도달불능쌍은 교역이 없으니 길목도 없음 — 정직). 다리 가중(BDT_BR_R)은 본체에 다리가 없어 미적용(자리 주석).
- **숲 판정 = `ta.forestMult(x,y)>1.2`** (villages extractLandParamsApprox와 동일 기준). wildlife의 _forM은 활성 청크 한정이라 전역 스캔에 부적합.
- **결정론**: 소굴 배치·재결성 시점/규모는 villageSeed 해시 RNG(랩 bdtDenRng 동형 — 시드만의 함수, 재부팅 불변). 단원 선발·토벌 주사위는 랩과 같이 Math.random(econ srand 스트림 불간섭).
- **onBanditLoot 로그는 대기열**: villages econ 틱이 console.log를 침묵시키는 창 안에서 불리므로 `queueLog`→daily 진입 시 방출(통계·링 버퍼는 즉시).
- 토벌 = 추상 판정(전사+사냥꾼 counts·무기 재고 — 랩 동형 계수). 실파견·배회·습격 연출은 2파.

### 4.3 게이트·가드
- `ENABLE_BANDITS=0` → init/onGameTick 즉시 return(완전 no-op, ENABLE_VILLAGES 관례). `ENABLE_VILLAGES=0`이면 banditHost()가 null → 자동 휴면.
- 테스트 훅(운영 무설정): `BANDIT_MINDAY`(정착 보호기 오버라이드), `BANDIT_FIXTURE=dissolve`(첫 데일리에 3~20명 최빈곤 마을 곳간 소거+순감 강제 — /tmp DB 전용).

### 4.4 검증(전부 /tmp DB 사본 — 실DB 무접촉·git 커밋 없음)
```
cp world-hanbando.db /tmp/bandit-smoke/runX.db
ZONE_ID=hanbando DB_PATH=/tmp/bandit-smoke/runX.db PORT=39xx VILLAGE_DAY_MS=400~1500 \
  [BANDIT_MINDAY=1 BANDIT_FIXTURE=dissolve] [ENABLE_BANDITS=0] timeout 38 node server/zone.js
```
- (a) `node --check` 5파일 전부 통과(bandits/villages/zone-local-db/zone/client).
- (b) 스모크: 소굴 3곳 배치 로그(오지점수 840·마을거리 529~714·전부 숲·2.4s 1회) · D1 재결성 카운트다운(D46/D46/D50 — 시드 결정론, 3개 런 동일) · 픽스처 해체→"광산1 해체(순감 5일·잔존 8명) → 도적단#1 3명 결성(은거지 2054,2079 — 길목쌍 1)" · **econ raid 실발화 4건**("D5 약탈 광산1→임업3 meat 16.5 → 단#1 장물 +8.3" 등, _banditRisk 학습 포함) · D31 기근 해산(약탈 수입 부족 → 굶주림 이탈 → 3명 미만 해산 — 자기조절) · D46/D50 소굴 3곳 재결성 실발화(4/5/6명) · 재부팅 복원("복원: 소굴 3곳(생존 3)·도적단 3(총원 15)·저장 day 52" → day 53 연속).
- (c) `ENABLE_BANDITS=0`: 공지 1줄 외 도적 로그 0·bandit_state 0행·**마을 인구 궤적(D1~6: 152,152,152,158,165,168)이 도적 ON 런과 동일** — '도적 0이면 완전 무해' 계약의 부수 증명(banditRouteRisk는 단 0이면 0 반환·RNG 무접촉).
- (d) villages 회귀: 19마을 시딩·econ 일틱(인구 152→246 성장)·캐러밴 실체 정상·BFS 행렬 로그 기존과 동일.

### 4.5 관찰·인계(1파 잔여)
- 소굴 3곳이 지도 북부 오지에 몰림(마을 분포상 오지 스코어 극대점) — 캐논상 위치가 곧 밸런스(보정 금지), 관찰 항목.
- BDT_MIN_DAY=365 → 신규 존은 실서버 ~2.5일(실시간, 10분/게임일) 후 원천①② 활성. 소굴③은 즉시(30~60일 카운트다운) — 의도.
- econ 훅은 world 함수라 비영속 — 부팅마다 bandits.init이 재설치(재부팅 복원 경로 포함, 검증됨).
- 도적 실체(배회·요격 연출·고립 습격·토벌 실파견)·wildlife 조우 규칙 = 2파.

---

## 5. 위험 대장(전 웨이브 공통)
1. **war-core/war-live 포크 드리프트**: 랩 war v3가 P1/P2 추출 후 진화 — 2파는 "이식"이 아니라 "재동기+확장". 감사 없이 얹으면 이중 규칙(예: march 봉쇄 vs siege 봉쇄) 충돌.
2. **serializeEcon 키 오염**: 훅·레이어가 econ에 객체 참조를 두면 직렬화 순환(안전망은 키 드랍+경고). 신규 키는 plain number/string만(1파 준수).
3. **부팅 비용 누적**: BFS 행렬 4.6s + 도적 소굴 스캔 2.4s(각 1회). 웨이브마다 부팅 스캔 추가 시 존 기동 시간 감시(fly 헬스체크 한도).
4. **랩=작업 정본 원칙**: 서버 어댑터는 랩 계수 verbatim(보정 금지) — 서버 지형·마을 수 차이가 밸런스를 바꾸면 계수가 아니라 캐논을 갱신.
5. 실DB(world-*.db, central.db) 쓰기 금지 — 테스트는 /tmp 사본, 45초 프로세스 회수(CHECKLIST 검증 관례).
