# 구현 지시 — T403: 무기는 실체다 — X-1 회부 ⑤(T284 · 09-19 부터 열림): "창·장창·도끼·화살 재고 없이 생김 + 소집 반출/복귀 반납 없음 · 없는 무기는 없다(민병 강등)" — **표 먼저, 코드는 표가 허락한 만큼**: 지금 군대의 무기는 어디서 오나(`war-core` 무기 숙련 `_ws*` · 3시드 열 `무기Q` · econ 대장간이 만드는 것 · 곳간 품목 칸) · 세 개 축이 서로를 보나 · 어부 문법으로 "소집 = 곳간에서 무기를 **손에 든다** · 복귀 = 내려놓는다 · 없으면 맨손 민병" 설계 표 · 1층 구현은 **반출/반납 장부만**(몸 무접촉 · 손잡이 끔 · 3시드 동일 · `무기Q` 열이 소집에 흔들리는지 표) (세션10/WAR · T388 뒤 · 2026-09-26)

아래 전문을 **세션10** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T403.md` · 레포 `지시/지시_T403.md`)

---

T388(`batch/war-ration-design-0925` → main · 군량 일곱 단계 전부 장부(약탈까지 여덟 · 행위 0) · **병사는 팩과 마을 식사를 이중으로 먹는다**(1인 하루 2단위 · 실측 ○) · 사기 항은 단위 결함으로 켜도 굶지 않는다 · 세 길 표(ⓐ 짐이 먹는다 — 이중 계산·사기 결함이 같이 닫힌다 · ⓑ 길에서 채집 · ⓒ 약탈은 별도 카드) · 뒤집히는 절 3/97 · 3시드엔 전쟁이 없어 자가 못 된다(자는 한 원정 등가 표) · 재민 판정 칸 다섯) **통과** — "군량은 일곱 단계 전부 장부이고 병사는 이중으로 먹는다"가 이 카드의 문장이다(족보 320). #68(세 길)은 재민 몫 — 그 판정 뒤 구현 카드(ⓐ)가 온다. 그 사이 WAR 의 다른 추상 하나: **무기**.

X-1(T284) 회부 ⑤ 가 09-19 부터 열려 있다: *"창·장창·도끼·화살 재고 없이 생김 + 소집 반출/복귀 반납 없음 · 없는 무기는 없다(민병 강등) · 품목 칸(창·도끼·화살)이 곳간에 없으면 그것부터."* 지금 세계엔 무기의 축이 **셋**이다 — ⓐ `war-core` 의 무기 숙련 `_wsSword/_wsSpear/_wsSling`(직업별 초기값 표 · 훈련 · 몸에 붙는 수) ⓑ 3시드 열 `무기Q`(econ 대장간이 만드는 무기의 질 · `t17-metrics ⓐ`) ⓒ 곳간 품목(창·도끼·화살이 품목으로 있나 — `weights.js`·`itemKo` 에 있나) — **셋이 서로를 보는지**가 첫 물음이다. "생산은 행위"의 WAR 판: 대장간이 만든 창이 곳간에 놓이고 소집 때 병사가 그걸 **손에 든다**.

**카드 T403 — 넷 · 표 먼저. PM 판정**(재민 거부권): ① **세 축 표** — 축마다: 정본 자리(파일:줄) · 무엇을 세나 · 누가 늘리고 누가 줄이나 · **다른 축을 읽나**(`_ws*` 가 `무기Q` 를 보나 · `무기Q` 가 곳간 품목에서 오나 · 전투 계산(`battle-core`)이 무엇을 읽나) · 품목 칸: 창·장창·도끼·활·화살·검·물매 중 곳간 품목으로 **있는 것/없는 것**(`weights.js` kg · `itemKo` 이름 · 대장간 제작 표) — 없는 것은 **빈칸 목록**(값은 안 짓는다 · T372 문법). ② **설계 표 — 어부 문법** — 소집: 병사 몸이 곳간에서 무기 품목을 **꺼내 손에**(`inventory` · 있는 것만 · 없으면 맨손 = 민병 · 강등 규칙은 `battle-core` 가 이미 갖는 수에서 — 새 수 0) · 전투: `battle-core` 가 몸의 손을 읽는다(숙련 `_ws*` 는 그대로 몸의 것) · 복귀: 내려놓는다(`_warFoodGive` 문법 · T388 ⓐ 와 같은 문) · 궤주: 무기 드랍(`war-core:53` 이미 있는 "무기 드랍·궤주 투기" — 그게 실체가 되는 자리) · 화살: 쏘면 준다(있는 `arrow_spawn` 사건과 한 몸) · 필요한 것 표(새 코드 자리 · 3시드 등가 방법 — T388 처럼 "한 원정 등가 표"). ③ **1층 구현 — 반출/반납 장부만** — 소집 순간 곳간 무기 품목 −n(있는 만큼 · 부족분은 민병 수로 표) · 복귀 순간 +n(전사자 몫은 드랍 — 수량은 궤주 규칙 그 수) · 몸 무접촉(손에 드는 것은 다음 카드) · 손잡이 `T403_ARMS_LEDGER` 기본 끔 · **끔 3시드 동일** · 켬 3시드 표(무기Q·인구·소멸 — 전쟁이 없는 t17 세계면 자명 동일 → 그 사실을 적고 `test-war-world` 한 원정 표로) · `test-war-world` 97 + 절(소집 −n · 복귀 +n · 없으면 민병). ④ 한 줄 답 + 재민 판정 칸(민병 강등 규칙 값 · 어느 품목부터). 사흘. 질문 금지 — 답은 표.

★검증 규약: **3시드 800일 열 열 넷째 판 동일**(끔 · `sim/` 접촉) · `test-war-world` 97 + 절 · `test-body`(장비 접점) · `test-item-weight`·`test-itemlabel`(품목 칸 건드리면) · `build-econ-bundle --check` 8 · `inline-engine --check` · `lab-wiring-check` 실패 0 · `test-harness-lint` 61 · 접점 심볼(`_wsSword|_wsSpear|_wsSling|WAR_WS_INIT|weapQ|무기Q|weights.js|itemKo|battle-core|_opPackLoad|_warFoodGive|inventory|T403_ARMS_LEDGER|arrow_spawn|test-war-world`).

## 0. §0 실측 (넷)
ⓐ 세 축 표 + 품목 있음/없음. ⓑ 설계 표(소집·전투·복귀·궤주·화살). ⓒ 장부 1층 diff + 끔 3시드 + 켬 표 + 절. ⓓ 한 줄 답 + 판정 칸.

## 1. 절대 규칙
새 수 0(강등·수량은 있는 규칙에서) · 값 안 짓는다(빈칸 목록) · 몸 무접촉(1층은 장부) · 손잡이 하나(끔) · `Math.random` 0 · **T388 구현(#68 뒤)·T400(세션4 곳간 자재)·T368(세션1) 무접촉** — 곳간 인출 문은 있는 것을 부른다 · ★푸시 2판(자기 기기 · 가지 `batch/arms-ledger-0926` · main 은 PM) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 세 축 ② 설계 ③ 장부 1층 ④ 한 줄 답.

## 3. 검증
3시드 · 하네스 여덟 · 접점 목록.

## 4. 회부만
#68 세 길(재민) · 민병 강등 값(재민 · ④) · 없는 품목 값(재민 · 빈칸) · 손에 드는 2층(PM · ② 표 뒤) · ⑥ 선포 무관 동원(PM) · p95·w·대형 속도(재민 · 그대로).

## 5. 완료
커밋 [승인 대기 · T403] · 가지 푸시 · `보고/T403_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T403]` · 베이스 해시**) · `인계/X-전쟁.md` X-7 · 채팅 "가지 올렸다 <해시> — 세 축 서로 보나 ○/✗ · 품목 있음 n/없음 n · 장부 1층 끔 3시드 동일 · 소집 −n/복귀 +n/민병 n". 실기 0.
