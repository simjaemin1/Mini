# 구현 지시 — T319: 배포 뒤 확인을 닫는다 — 실호스트 자리 하나(레포 밖 `~/Mini/실서버.txt` · 스크립트가 읽는다) · `?as=` 문 닫기(★PM 결정 · 개발 손잡이 `DEV_AS=1` 뒤로만) · `/health` 에 칸 둘(마을 수 · 존 틱 p50 — 정본 값 그대로) · `deploy-check.sh` 어긋남 13 의 루프백 갈래 정리 (세션7 · T310 뒤 · 2026-09-19)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T319.md` · 레포 `지시/지시_T319.md`)

---

T310(main `04f2833c` · 카드 전제 셋을 뒤집은 것 — `DEPLOY.md`·`Caddyfile` 은 견본 · #9 는 T245 가 닫은 문이 아니다(`공개·회부9`) · `/perf` 는 안 문 · **15-벽 "남은 열은 동사가 없다" 가 틀렸다(일곱은 이미 온다 · 표 일곱·이름 하나·동사 둘)** · `deploy-check.sh` 31 라우트) **통과** — 문 넷 확인하러 갔다가 벽의 셈을 고친 게 답이다(족보 246). #33·`nippon` 은 그대로.

**카드 T319 — 넷. PM 판정**(재민 거부권): ① **실호스트 자리** — 레포엔 안 둔다(공개 레포). `~/Mini/실서버.txt`(맥 · 한 줄 `https://…`)를 `deploy-check.sh` 가 `DEPLOY_HOST` env 또는 그 파일에서 읽는다 · 없으면 "실호스트 없음 — 로컬 판" 한 줄로 선다 · 재민이 파일을 놓으면 그대로 실서버 확인이 된다(재민 한 줄 = 파일 하나). ② **`?as=` 닫기** — `zone GET ^/startinfo` 의 `?as=` 를 **`DEV_AS=1` 일 때만** 받는다(기본 닫힘 · 실서버는 env 없음) · `test-doors` 기대표 그 줄 `공개·회부9` → `닫힘(DEV_AS)` · 62 라우트 그대로 · 두 팔(env 유/무). ③ **`/health` 칸 둘** — `villages`(살아 있는 마을 수) · `tickP50Ms`(존 틱 p50 · `/perf` 가 이미 계산하는 그 값 — **같은 함수** · 사본 0) · 키·토큰·좌표 0(T225 규약) · `test-doors` 응답 모양 절. ④ **`deploy-check.sh`** — 어긋남 13 = 루프백이라 열린 안 문이면 스크립트가 **호스트가 루프백인지**를 읽어 그 13 을 "안 문(로컬이라 열림)" 으로 표에 갈라 적는다(거짓 어긋남 0). ⑤ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: `test-doors` 19 + 새 절 셋 · `test-events` 그대로 · `/check_username` 무접촉(#48 재민) · 실서버 쓰기 0 · 3시드 면제(`zone.js` 라우트만) · 접점 심볼(`startinfo|?as=|DEV_AS|/health|/perf|tickP50|deploy-check.sh|DEPLOY_HOST|test-doors`).

## 1. 절대 규칙
새 수 0 · 제품 접촉은 `zone.js` 라우트 두 자리(`startinfo`·`health`) · **`sendInventory`/`doEat`(T305)·어부(T316)·전쟁(T295) 무접촉** · 배포 재민만 · ★푸시 2판(가지 `batch/deploy-close-0919` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
`/check_username`(#48 재민) · #33 · `nippon` 시딩(Z · 다음 카드) · rumor 경주(T304 · Z · 다음 카드).

## 5. 완료
커밋 [승인 대기 · T319] · 가지 푸시 · `보고/T319_<날짜>.md`(**머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> — `?as=` 닫힘 · health 칸 둘 · 실호스트 파일 자리". 실기: 재민이 `~/Mini/실서버.txt` 한 줄 놓고 `bash scripts/deploy-check.sh`.
