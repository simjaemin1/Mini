# 구현 지시 — T406: 능선 선분 색인을 켠다 — T392 가 찾았다: `server/terrain-segindex.js`(08-31 · `42764e2d`) 가 **이미 있고 꺼져 있다**(`TERRAIN_SEG_INDEX=1` · 배포도 꺼짐) · 켜면 숲 청크 6.75 → 0.71ms(×9.5) · 바위 술어 42.7 → 0.36µs(×120) · 청크 산출 해시 같다 ⇒ ★PM 결정(위임): **기본 켬** — 게이트는 **존 전체 청크 산출 해시**(8,763청크 · 켬/끔 비트 동일 · 한반도 + 부팅한 다른 존 하나) · 부팅 시간·틱 p50 전/후 · 되돌림 `=0` · 그리고 WebGL 물·산을 2D 에 붙이는 `drawImage` 되읽기(330ms 의 주인 · 헤드리스)는 **캔버스 겹치기**로 되읽기 0 이 되는지 **표만**(#73 재민 맥 값 뒤 구현) (세션5 · T392 뒤 · 2026-09-26)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T406.md` · 레포 `지시/지시_T406.md`)

---

T392(`batch/cull-radius-0925` → main · `VIEW_RADIUS = TILE_RENDER_RADIUS` 별칭(새 수 0) · 띠 끔(원경 층은 큰지도 전용) · 임업3 개체 나무 2 → 666 · render 12.0 → 15.9ms · **T384 두 팔 표는 도착 미확인 판이라 다시 서지 않았다**(정직하게 적음 — 컬링은 싸지 않고 비슷하다 · 근거는 "개체라 클릭된다 · 층 하나") · 330ms 의 주인 = WebGL 캔버스를 2D 에 붙이는 `drawImage` 되읽기(`_drawWaterLayer` · `_mt3GlDraw` · 헤드리스 "GPU stall due to ReadPixels") · **능선 선분 색인은 이미 있다** · `test-far-trees` ⓑ12 장부 틈 막기 19/0) **통과** — "능선 색인은 이미 있다 — 꺼져 있을 뿐"이 이 카드의 문장이다(족보 323). ★PM: T384 의 두 팔 수가 틀렸던 것을 T392 가 스스로 뒤집었다 — 컬링 팔 결정(★PM #67③)은 근거가 "더 싸다"에서 "개체라 클릭되고 층이 하나"로 바뀐 채 **유지**한다(render 15.9ms 는 예산 안). 재민 실기 ②(멀리 나무)는 **배포 뒤** 닫힌다.

**카드 T406 — 넷. PM 판정**(재민 거부권): ① **`TERRAIN_SEG_INDEX` 기본 켬** — `terrain-segindex.js` 의 기본을 켬으로(되돌림 `=0`) · 게이트: **존 전체 청크 산출 해시** — 한반도 8,763청크 전부 `generateChunkResources` 산출(나무·군락·덤불 자리) 켬/끔 비트 동일 + `europa`(#65 1위 · T373 이 부팅한 그 존) 한 번 더 · 술어 답 1,000점 비트 동일(T383 자 문법 · `isRockCellLocal`·`isWaterCellLocal` · 능선 옆 표본 300 포함) · 부팅 시간 전/후(색인 만드는 비용 — 08-31 은 "부팅 52 → 12초" 라 했다 · 지금 수) · 틱 p50 전/후(낮 창 · T356 자) · T380 청크당 17.8ms → n · 색인 메모리(MB). ② **되읽기 표** — `_drawWaterLayer`·`_mt3GlDraw` 가 WebGL 캔버스를 `drawImage` 로 2D 캔버스에 붙이는 자리 둘: **캔버스 겹치기**(WebGL 캔버스를 DOM 에서 2D 캔버스 위/아래에 겹쳐 두고 되읽기 0 — 안개 마스크가 GL 층 **위**에 서야 하는 규격(지면 데코는 마스크 앞 · 엔티티 시야 별개)과 맞나 · 층 순서 표) · GPU 크롬에선 되읽기가 얼마인지(#73 재민 값 · 이 카드는 헤드리스 값만) · **구현 안 한다** — 표와 층 순서 그림 하나. ③ **T384 정정 기록** — `보고/T384_2026-09-25.md` 머리에 "★T392 정정: 두 팔 표는 도착 미확인 판 — 컬링 13.9 · 띠 12.0(임업3)" **세 줄 추가**(원문 무변 · 아래 붙임) — 틀린 수가 기록에 그대로 남으면 다음 카드가 또 읽는다. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(해시 게이트가 그것이다 · 다르면 켜지 않는다) · `sim/` 무접촉 · `test-terraincache` 26 · `test-resource-index` 20 · `test-trees` 91 · `test-far-trees` 19 · `test-move-soa` 129 · `e2e-move` 37 · `test-client-globals` 26 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`TERRAIN_SEG_INDEX|terrain-segindex|isRockCellLocal|isWaterCellLocal|generateChunkResources|_drawWaterLayer|_mt3GlDraw|drawImage|t392-ridge-index|t384-index-cost|europa`).

## 0. §0 실측 (넷)
ⓐ 켬 게이트(8,763 + europa 해시 · 1,000점 · 부팅 · 틱 · 17.8 → n · MB). ⓑ 되읽기 표 + 층 순서 그림. ⓒ T384 정정 세 줄. ⓓ 한 줄 답.

## 1. 절대 규칙
해시 다르면 켜지 않는다 · 새 수 0 · 되읽기 구현 0(표만) · `server/` 는 `terrain-segindex.js` 기본값 한 줄만 · **T398(세션2)·T399(세션3)·T400(세션4)·T385(세션7) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/seg-index-on-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 켬 ② 되읽기 표 ③ T384 정정 ④ 한 줄 답.

## 3. 검증
해시 게이트 · 하네스 아홉 · 접점 목록.

## 4. 회부만
#73 맥 GPU 값(재민) · 캔버스 겹치기 구현(PM · #73 뒤) · #67 ①②④(재민 · 실기) · `europa` 시딩(#65 · PM).

## 5. 완료
커밋 [승인 대기 · T406] · 가지 푸시 · `보고/T406_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T406]` · 베이스 해시**) · `인계/R1-지형렌더.md`·`W-월드질의.md` 갱신 · 채팅 "가지 올렸다 <해시> — 색인 켬 · 해시 8,763/8,763 + europa · 부팅 …→… s · 청크 17.8 → … ms · 되읽기 표". 실기: 재민 배포 뒤 멀리 나무 + 부팅 시간.
