# 구현 지시 — T433: `T421_SPATIAL_INC` 기본 켬(★PM · 조회 210만 비트 동일이 게이트 · 되돌림 `=0`) + **자의 규약 한 장** — T410·T421·T368 이 각자 적은 계측 규약(부팅 뒤 첫 하루 경계 뒤 조각 · RSS 는 GC 의 값 · **시계 둘**: T326 4초 시계론 몸이 아무것도 못 거둔다 · 같은 창 0.12~0.27 · 같은 씨 · 빼기 0)을 `인계/Z-존서버.md` 한 절로 모으고 틱 자 넷(`t356-tick-anatomy`·`t371-tick-rest`·`t410-zone-awake`·`t376`)이 **같은 규약을 쓰는지 표** · 그리고 #54 무제한 p95 를 켬 위에서 한 번 더(T421 ② 24.2ms 가 T385·T394·T421 겹친 값이라 — 켬 팔 하나 더) (세션7 · T421 뒤 · 2026-09-27)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T433.md` · 레포 `지시/지시_T433.md`)

---

T421(`batch/spatial-inc-0926` → main · 나무를 갈아타지 않고 **같을 때 안 세운다** — `spatial` 0.236 → 0.138µs(−42%) · 그 밖 0.81 → 0.68 · 조회 2,100,000 비트 동일 · 미끼 셋 · `T385_ONE_SWEEP` 기본 켬 · 몹 한 바퀴 0.046 → 0.031 · #54 무제한 몸 2,656 · 낮 p50 16.5ms(49%) · p95 24.2(73%) · drop 0 · `QuadtreeInc` 추가(원판 무변) · `test-move-soa` 158) **통과** — "같은 몸을 다른 순서로 내는 나무는 갈아탈 수 없다 — 그래서 세우지 않는 쪽을 골랐다"가 이 카드의 문장이다(족보 349). ★PM: `T421_SPATIAL_INC` 는 **켠다**(조회 비트 동일 = 세계 무변 · 되돌림 `=0`) · #54 는 재민(값) — 켬 팔의 p95 한 줄이 그 입력.

**카드 T433 — 넷. ★PM 결정(위임 · 재민 거부권)**: ① **켬** — `T421_SPATIAL_INC` 기본 켬(되돌림 `=0` = 종전 비트 동일 · `test-move-soa` ⑭ 가 켬/끔 나란히 문다 그대로) · 켬 뒤 낮 창 사람당 µs(T356 자 · 같은 창 0.12~0.27 · 같은 씨) · `T385_ONE_SWEEP` 켬과 합친 그 밖 값 한 줄 · 26존 동시 판(T410 자)에서 합 CPU 한 줄(45.2 → n). ② **자의 규약 한 장** — `인계/Z-존서버.md` 에 절 하나 "틱 자의 규약": ⓐ 조각은 부팅 뒤 **첫 하루 경계 뒤**부터(T410 §2-⓪) ⓑ RSS 는 GC 뒤 값(`--expose-gc` 있는 문법 · 없으면 "GC 의 값" 표시) ⓒ **시계 둘** — T326 4초 시계는 행위 몸을 못 잰다(T368 §4-ⓒ · T410 §2 인구 −9.8% 는 자의 값) · 행위는 실제 날 길이 자(`t368-farm-day` · `t376`)로만 ⓓ 같은 창·같은 씨·빼기 0 ⓔ 사람당 µs 의 분모(몸 수 · 어느 명부) — 그리고 **표**: 틱 자 넷(`t356-tick-anatomy`·`t371-tick-rest`·`t410-zone-awake`·`t376-unlimited`(이름 확인)) × 규약 다섯 = 지키나/안 지키나 · 안 지키는 칸은 **자 한 줄 고침**(값 무변 · 되돌림 0) 또는 "왜 그 자는 그래도 되나" 한 줄. ③ **#54 켬 팔** — T376 자 그대로(무제한 · 실제 날 길이) · T385·T394·T421 전부 켬 · 몸 · 낮 p50·p95 · 32 조각 예산 · drop · lag · 소멸 — T421 ② 표에 열 하나(24.2 → n) · **어느 카드가 얼마를 줄였나**는 못 가른다고 적는다(같은 자로 한 손잡이씩 끄는 팔 셋이면 가른다 — 하면 표 · 안 하면 한 줄). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 켬 = 조회 비트 동일(`test-move-soa` ⑭ 2,100,000) · 되돌림 `=0` 비트 동일 · 새 수 0 · **T432(세션1 · `zone.js` 청크 문 — 같은 파일 · 자리 다름 · 먼저 착지한 쪽 위에서 리베이스)** · **T426(세션2)·T427(세션3)·T428(세션5) 무접촉** · `sim/` 무접촉(3시드 면제 근거 한 줄) · `test-move-soa` 158 · `test-war-world` 116 · `e2e-move` 37 · `test-nippon-boot` 42 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`T421_SPATIAL_INC|T385_ONE_SWEEP|_rebuildSpatialInc|QuadtreeInc|rebuildSpatialIndex|t356-tick-anatomy|t371-tick-rest|t410-zone-awake|t376|expose-gc|zoneGameDay`).

## 0. §0 실측 (넷)
ⓐ 켬 diff + 낮 µs + 26존 합. ⓑ 규약 한 장 + 자 넷 × 다섯 표. ⓒ #54 켬 팔 열. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 켬 하나(되돌림) · 자 고침은 값 무변 · `zone.js` 는 기본값 한 줄 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/spatial-on-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 켬 ② 규약 한 장 ③ #54 팔 ④ 한 줄 답.

## 3. 검증
비트 동일 · 하네스 여섯 · 접점 목록.

## 4. 회부만
#54(재민 · ③ 열이 입력) · #53 C 층 접속(재민 · WASM 권고 그대로) · 26존 배치(재민 · 플랫폼).

## 5. 완료
커밋 [승인 대기 · T433] · 가지 푸시 · `보고/T433_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/Z-존서버.md` 규약 절 · 채팅 "가지 올렸다 <해시> — spatial 켬 · 낮 … µs · 26존 합 …% · 규약 표 n/20 · #54 켬 팔 p95 … ms". 실기 0.
