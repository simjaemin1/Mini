# 구현 지시 — T392: 컬링 팔로 간다 — T384 두 팔 표가 답을 냈다(숲: 띠 9.1ms · 컬링 5.1ms · 그리고 컬링 팔은 클릭·채집이 된다) ⇒ ★PM 결정(위임 · #67③): `VIEW_RADIUS` 650 → **`TILE_RENDER_RADIUS` 와 같은 상수 하나**(1,500 · 새 수 0 — 지면이 닿는 그 반경) · 띠 ⓐ 는 **끈다**(원경 층은 큰지도 전용으로 남긴다 · 손잡이 그대로) · 안개 밑 나무 안 그리기(T384 §5-2)는 컬링 팔에 이미 있다 · 게이트 = 화소(안개 밖 나무 수 = 그린 수) + 프레임 ms 전/후 · **능선 선분 색인 설계 표**(17.8ms 의 88% 바위 술어 — 답을 안 바꾸는 길 · 구현은 다음) (세션5 · T384 뒤 · 2026-09-25)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T392.md` · 레포 `지시/지시_T392.md`)

---

T384(`batch/far-band-0925` → main · 띠를 서버 없이 기본으로 켰다(숲 1,437그루 모아 235 그림) — 그런데 render 3.3 → 9.1ms(층의 ms 는 `drawImage` 뒤 래스터를 덜 센다 · 진짜 값은 render 차) · **컬링 팔(`VIEW_RADIUS` 1,500)이 5.1ms 로 더 싸다**(안개 게이트를 지나 본 셀의 나무만 15~103그루) · 17.8ms 의 **88% 는 `isRockCellLocal` 하나** · 비트 상자로 바꾸면 답이 0.1~0.5% 갈린다(술어는 지터된 점 · 상자는 셀 중심) · 마을·빈 초지 세 팔 모두 330~376ms/f(띠와 무관 · 회부) · `test-far-trees` 17/0 + ⓐ 절) **통과** — "띠는 켜졌는데 더 싼 길이 옆에 있었다"가 이 카드의 문장이다(족보 311). ★PM 결정(위임 · 재민 거부권 #67③): **컬링 팔**. 근거 셋 — 더 싸다(5.1 대 9.1) · 개체라 클릭·채집이 된다(원경 층은 그림뿐) · 층 하나가 준다(정본 하나). 띠 ⓐ 는 켠 지 하루 만에 끈다 — 잰 수가 그렇게 말했다.

★PM 착지 뒤 빨강 하나 — **`test-far-trees` 17/2**(PM 컨테이너 · main 에 T378 과 T384 가 같이 선 뒤): ⓑ12 "세계 무변 — 개체 5,222 → 5,201" 과 ⓑ10 "그린 56 < 100". 개체가 준 것은 원경이 아니라 **T378 개간이 시험 중에 관측 마을 영토 안 나무를 베기 때문**이고(관측자 무관 · 게임일이 지나며 `_terrGrow` 가 벤다), 그린 수가 56 인 것은 같은 이유(영토 안 나무 0). 제품 결함이 아니라 **자가 세계를 정지로 가정**한 것 — 이 카드 ⓪.

**카드 T392 — 다섯. PM 판정**(재민 거부권 #67③): ⓪ **`test-far-trees` 자를 세계 변화와 가른다** — ⓑ12 는 "원경 요청이 개체를 바꾸지 않는다"를 재는 절이니 **같은 순간** 두 번 세거나(요청 전/후 같은 프레임) 개간·성장 시계를 시험에서 멈춘 팔(`_e2eClock` 정지 · 이미 있는 손잡이)에서 잰다 · ⓑ10 의 100 은 관측 자리가 영토 안이라 나온 수 — 자리를 **영토 밖 숲**(T378 자 `t378-forest-in-village` 가 아는 영토 셀 밖)으로 옮기거나 문턱을 그 자리 나무 수에서 유도(새 수 0) · 17/0 회복 · 자명 통과 반례(개체를 실제로 하나 지우면 ⓑ12 가 빨개진다). ① **컬링 팔** — `34-m-renderloop.js:663` 의 `VIEW_RADIUS` 를 **`TILE_RENDER_RADIUS` 그 상수**로(둘이 같은 뜻 — 개체가 그리는 상자 = 지면이 닿는 상자 · 이름 하나 남기고 하나는 별칭 · 새 수 0) · 개체 컬링이 나무 말고 무엇을 더 그리게 되나(사람·짐승·건물 — 반경이 넓어지면 **전부** 든다 · 종류별 그린 수 전/후 표 · 사람·짐승은 시야 규칙(안개 별개 · 살아 움직이는 것에만)을 그대로 타는지) · 띠 ⓐ 는 `_farDraw` 에서 **끈다**(원경 ⓑ 큰지도 경로는 그대로 · `test-far-trees` ⓐ 절을 "띠 0 · 컬링 n" 으로 뒤집는다) · 게이트: T384 두 팔 자(`t384-two-arms.js`) 그대로 숲·마을·초지 render ms 전/후 + 안개 밖 나무 수 = 그린 수(화소). ② **마을·초지 330~376ms/f** — T384 §5-3 "가르지 않았다" → 가른다: 그 상자에서 render 프로파일(층별 ms · `_natMs`·자연물·지면 타일·건물·사람 — 이미 있는 자들) · 헤드리스 소프트웨어 렌더의 몫인지(같은 자를 GPU 있는 맥 브라우저에서 한 번 — 세션 자기 맥 · 수만) · 주인 이름 한 줄 · **고치지 않는다**(회부 → 세션4 T395 가 클라 프레임 전체를 맡는다 · 이 카드는 이 상자의 표만). ③ **능선 선분 색인 설계 표** — `isRockCellLocal` 이 능선 11 · 선분 2,129 에 대해 무엇을 계산하나(지금 식 그대로 · 호출당 µs) · 선분을 청크 격자에 색인하면 청크당 후보 선분 몇 개(분포) · 같은 답(비트) 조건 · 예상 배율은 **잰 것만**(작은 원형 계측기 `scripts/t392-ridge-index.js` 러너 밖 · 제품 무접촉) · `europa`(#65) 앞의 입력. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변 · `server/` 무접촉 · 새 수 0(1,500 은 `TILE_RENDER_RADIUS` 의 수) · `test-far-trees` 17 + 뒤집은 ⓐ 절 · `test-client-globals` 26 · `e2e-move` 37 · `e2e-fogray` 18 · `test-harness-lint` 61 · 접점 심볼(`VIEW_RADIUS|TILE_RENDER_RADIUS|_farDraw|_farOn|_natMs|drawTreeIso|isRockCellLocal|ridges|t384-two-arms|t392-ridge-index|test-far-trees`).

## 0. §0 실측 (넷)
ⓐ 컬링 diff + 종류별 그린 수 전/후 + render ms 세 자리. ⓑ 330ms 프로파일 표(주인 이름). ⓒ 능선 선분 색인 표. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 세계 무변 · 원경 ⓑ·손잡이 무변 · 안개 규격 무변 · **T395(세션4 클라 프레임)·T387 후속(세션9 오디오) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/cull-radius-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (다섯)
⓪ 자 가르기 ① 컬링 팔 ② 330ms 표 ③ 능선 색인 표 ④ 한 줄 답.

## 3. 검증
화소 게이트 · ms 전/후 · 하네스 다섯 · 접점 목록.

## 4. 회부만
#67 ①②④(재민 · 실기) · 능선 선분 색인 구현(PM · ③ 표 뒤) · 330ms 고치기(세션4 T395) · Safari 실기(재민 · 배포 뒤 멀리 나무).

## 5. 완료
커밋 [승인 대기 · T392] · 가지 푸시 · `보고/T392_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 render 전/후 셋 + 330ms 주인**) · `인계/R1-지형렌더.md` 갱신 · 채팅 "가지 올렸다 <해시> — 컬링 1,500 · 숲 … → … ms · 330ms 주인 = … · 능선 색인 청크당 선분 …". 실기: 재민 배포 뒤 멀리 나무 + 클릭.
