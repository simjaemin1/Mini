# 구현 지시 — T368: 농부 행위 완성 — 남은 추상 둘을 없앤다: ① 수확이 `T100_K` "식량등가" 상수로 들어간다(어떤 작물이었는지가 안 온다 — 0-b 캐논 "뭉뚱그린 품목 없음" 위반) → **그 밭의 작물 품목이 작물 표의 수확량으로** 곳간에(새 수 0 · `crops` 정본에서) ② 관측자 없는 마을은 `_lifeHeadlessDay` 일괄 → **농부도 관측자 무관 실걸음**(T347 채집 문법 그대로: 밭 셀로 걸어가 갈고·심고·거두는 순간 손에 → 곳간) · 손잡이 `T368_FARM_ACT` 기본 끔 · 끔 = 다섯째 판 비트 동일 · 켬 판 30일 등가(T297 자) (세션1 · T359 뒤 · 재민 09-23 "농부가 직접 밭 갈고 수확하는 순간 플러스" · 2026-09-23)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T368.md` · 레포 `지시/지시_T368.md`) **⚠T359 착지 뒤 · T367(다섯째 판) 뒤에 붙인다.**

---

재민 09-23: **"농부가 직접 밭 갈고 수확하는 순간 해당 작물이 플러스되기로 했잖아. 우리 모든 추상을 없애기로 했잖아."** 해본 적 있다 — T100 4판(09-05)이 관측 마을에서 그렇게 한다(`villages.js:4880` 수확 순간 `harvestToGranary`). 남은 추상이 둘이다: 수확이 **품목 없는 식량등가**(`T100_K` · `harvestToGranary(v, n, mul)` 에 작물이 안 온다 — 코드 제 주석이 그렇게 적어 뒀다 `:1010`)이고, **관측자 없는 마을은 몸 없이 일괄**(`_lifeHeadlessDay` ① 절)이다. 순서표(설계_생산_실체 ⓖ)는 농부를 맨 뒤에 뒀지만 농부가 이미 반은 행위라 **당긴다**(재민 09-23).

**카드 T368 — 넷 · 순서가 카드다. PM 판정**(재민 거부권): ① **품목** — 수확 갈래가 `harvestToGranary` 에 **작물 이름**을 넘기고(`vil._crop` 이 그 밭의 작물을 안다 · 호출부 한 줄) econ 은 `T100_K` 대신 **작물 표의 수확량**(`crops.js` 정본 · 밭 한 칸 한 수확 = 그 작물 n단 · kg 은 `weights.js`)을 그 품목으로 넣는다 · 볏짚·세금·`T227_EVEN` 고르기는 그대로 그 안에서 · 총량 항등(품목 합 = 종전 식량등가 합 — 첫날 등가 원칙 ⓓ · 어긋나면 그 차가 발견 · 표) · `T100_K` 는 지우지 않고 대조 자로만. ② **관측자 무관 실걸음** — T347 채집 문법 그대로: 농부는 자기 밭 셀(사유지 `claims.js`)로 걸어가 작물 상태기(`cropAfterHarvest` · 개간·파종·김매기·물대기·수확 — 이미 있는 그 상태기 · 사본 0)의 다음 일을 **몸으로** 하고, 수확은 손에(`inventory[작물]`) → 귀환 곳간(반입 ⓐ ②) · 관측자 없는 마을에서 `_lifeHeadlessDay` ① 절은 **켬이면 안 돈다**(그 마을 밭은 몸이 한다) · 손잡이 `T368_FARM_ACT` 기본 끔 · 걸음 예산은 T356 해부 뒤 값으로 재고 표에(사람당 µs · 농부 수 × 왕복). ③ **켬 판** — 관찰 세계 30일 3마을 3시드(T297 `t297-observe` 자) 등가: 밭 등가가 소수 다섯 자리였다(T297) — 켬 팔이 그 등가를 유지하나 · 품목별 곳간 · 실서버 51마을 200일 켬/끔(T326 자) 인구·곳간·사람당 µs · 3시드 800일은 랩이 몸을 안 타므로 면제 한 줄(`sim/` 을 만지므로 **끔 = 다섯째 판 비트 동일**은 면제 없음). ④ 한 줄 답 + 설계 §0 농부 행 "옮겼다". 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 = 다섯째 판 3시드 열 열 비트 동일 · `test-econ-fieldyield` 197 · `test-farm-life` 33 · `test-forage-act` 86 · `test-fish-act` 73 · `test-wood-act` 87 · `e2e-village` · `lab-wiring-check` 실패 0 · 새 절: ⓐ 품목 합 항등 ⓑ 켬이면 headless ① 절 호출 0 ⓒ 끔 비트 동일 · 접점 심볼(`harvestToGranary|T100_K|_lifeHeadlessDay|cropAfterHarvest|_crop|claims.js|crops.js|weights.js|T227_EVEN|T368_FARM_ACT|actToGranary|_lifeAct|t297-observe`).

## 1. 절대 규칙
새 수 0(작물 표·무게 표 정본만) · 손잡이 기본 끔 · 켜지 않는다 · 뭉뚱그린 품목 0 · **T356(세션3 `zone.js` 걸음)·T361(세션4 집 표)·T364(세션10) 무접촉 — 리베이스 때 둘 다 살린다** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/farm-act-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · ③ 표) · 개간·파종을 어느 직업이(농부 그대로 · 표) · 광부(#43·#47) · 사냥(그 다음).

## 5. 완료
커밋 [승인 대기 · T368] · 가지 푸시 · `보고/T368_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · `설계/설계_생산_실체.md` §0 농부 행 · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 품목 항등 … · headless 0 · 30일 등가 … · µs …". 실기 0.
