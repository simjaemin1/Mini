# [GPT] 지시 — T390: T382 의 빨강 12 중 **환경 6** 을 자기 클론에서 닫고 다시 돈다(`playwright` 미설치 4 · `test-nightly` 낡은 pid · `test-redeploy` VPS 스크립트) · `test-harness-lint` **60/1 의 그 하나**가 무엇인지(PM 컨테이너는 61/0 — 차이는 환경이다 · 어느 절 · 왜) · `test-body`·`test-hunt-vis`·`test-itemlabel` 셋은 PM 재실행에서 초록이었다 — GPT 쪽에서 **다시 돌려 흔들림인지 재현인지** · 결과는 `보고/T390_<날짜>.md` 하나 · 판정 0 · 코드 0 · 가지 `codex/repro-0925` (세션13/GPT · T382 뒤 · 2026-09-25)

아래 전문을 **GPT(Codex · 세션13)** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T390.md` · 레포 `지시/지시_T390.md`) **`[GPT]` — 판정 0 · 코드 0 · 표만.**

---

**★자리부터.** `~/Mini` 안에서 작업하지 마라. T382 가 만든 클론 `~/codex/durango-mini` 그대로:

```
cd ~/codex/durango-mini && git fetch origin && git checkout -b codex/repro-0925 origin/main
```

(`origin/main` 은 `462b4acc` 이상이어야 한다 — `git log -1 --oneline origin/main` 을 보고 머리에 적는다.)

T382(`codex/repro-0923` → main · 별도 클론에서 3시드 800일 **30칸 전부 일치** · 하네스 111종 단독 99/12 · 별도 클론 규약을 지켰다 — 첫 번째로) **통과**(족보 304). 빨강 12 를 PM 이 갈랐다: **환경 6**(`test-lab-alloc`·`test-lab-market`·`test-lab-mining`·`test-lab-psite` = `Cannot find module 'playwright'` · `test-nightly` = "이미 돌고 있다(pid 49771)" 낡은 pid · `test-redeploy` = `redeploy-hanbando.sh` 실행 실패) · **PM 컨테이너 초록 3**(`test-body` 306/0 · `test-hunt-vis` 32/0 · `test-itemlabel` 58/0 — GPT 쪽 흔들림 가능) · **진짜 빨강 2**(`test-terraincache` 18/4 · `test-site-memo` — 야간 러너가 이미 6밤·3밤 세던 것 · **세션4 T383 이 귀속 중이라 GPT 는 건드리지 않는다**) · `test-harness-lint` 60/1 은 로그 마지막 30줄에 실패 줄이 안 실려 **무엇이 빨간지 모른다**.

**카드 T390 — 넷 · 표만 · 코드 0.** ① **환경 6 닫기** — 클론 안에서만: `npx playwright install chromium`(또는 `npm i -D playwright` — 무엇을 어떻게 깔았는지 한 줄 · `package.json` 은 **안 바꾼다**) → `test-lab-*` 넷 재실행 · `test-nightly` 는 낡은 `.pid` 가 어디 있는지(경로) · 지우고 재실행(그 pid 가 정말 죽은 프로세스인지 `ps` 한 줄) · `test-redeploy` 는 VPS 없이 도는 하네스인지(스크립트가 무엇을 요구하나 · 못 돌면 "환경 · 이유" 한 줄로 끝 · 억지로 안 돌린다) → 표: 하네스 · 전 · 후 · 한 것. ② **lint 60/1** — 전체 출력을 파일로 받아(`> /tmp/lint.txt 2>&1`) `✗` 줄을 찾아 **절 번호 · 문장 · 값** 그대로 인용 · PM 컨테이너(61/0)와 다른 이유가 환경(경로 · 노드 버전 · 파일 유무)인지 표. ③ **흔들림 셋** — `test-body`·`test-hunt-vis`·`test-itemlabel` 을 **각각 3판** 단독 재실행(판마다 PASS/FAIL 수) · 3판 중 빨강이 있으면 `✗` 줄 그대로 인용 · 없으면 "3/3 초록 — 첫 판 흔들림" 한 줄. ④ **한 줄 답** — 111종 최종 초록 n · 환경 n(닫은 n · 못 닫은 n) · 진짜 빨강 n(T383 몫). 하루. 질문 금지 — 답은 표.

★검증 규약: **코드 0 · 제품 파일 수정 0 · `package.json`·`package-lock.json` 무변**(설치는 클론 로컬 · `git status` 가 보고서 하나만 보여야 한다 — 그 출력을 §0 에) · 보고서 한 장 `보고/T390_<날짜>.md` · 가지 `codex/repro-0925` 만 푸시(`git push origin codex/repro-0925`) · **main 푸시 금지** · **`~/Mini` 무접촉** · **`test-terraincache`·`test-site-memo` 무접촉**(세션4).

## 0. §0 실측 (넷)
ⓐ 환경 6 전/후 표. ⓑ lint `✗` 절 인용 + 환경 차이. ⓒ 흔들림 셋 × 3판. ⓓ 한 줄 답 + `git status` 출력.

## 1. 절대 규칙
코드 0 · 판정 0 · 별도 클론 · `codex/*` 가지만 · `~/Mini` 금지 · force 0.

## 5. 완료
커밋 `[승인 대기 · T390]` · `git push origin codex/repro-0925` · 채팅 "가지 올렸다 <해시> — 최종 초록 n/111 · 환경 닫음 n/6 · lint 빨강 = ⓧ절 · 흔들림 n/3". 실기 0.
