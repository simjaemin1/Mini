# 구현 지시 — T385: 순회 일곱을 하나로 — T375 가 음수로 증명한 T371 회부 1-ⓑ(순회 한 바퀴 ≈ 0.21~0.25µs/사람 · 주민 전수를 도는 순회 일곱 `spatial`·`stairs`·`fall`·`hpRegen`·`gauge`·`inputTO`·`gaugeNet`) · **의존 표가 본체**(단계 k 가 다른 몸의 단계 j 결과를 읽으면 합칠 수 없다 — 순서가 세계다) · 합칠 수 있는 것만 한 바퀴 · 게이트 = T375 ⑨ 그대로(켬/끔 4,200틱 비트 동일) · `stairs` 주민 루프 문자열 키 → 정수 키 · ⓪ `test-move-soa ⑤` 씨 박기 · T375 손잡이는 "켜면 안 되는 손잡이" 표로 (세션7 · T375 뒤 · 2026-09-25)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T385.md` · 레포 `지시/지시_T385.md`)

---

T375(`batch/active-flag-0923` → main · 문지기를 불리언 필드로 고쳤더니 술어 호출 5,851 → 3,751회(64%)인데 그 밖이 1.15 → 1.46µs(+27%) — 새로 놓은 순회 둘(`t375R1`·`t375R2` = +0.462)이 넷이 던 것(−0.200)보다 더 든다 · **★켜지 마라** 를 스스로 머리에 박음 · 게이트 ⑨ 켬/끔 비트 동일 · `stairs` 는 주민 반쪽에 문지기가 없고 `stairCellCache` 문자열 키를 전수가 만든다 · `test-move-soa` 88/0) **통과** — "문지기를 고쳤더니 값이 반대로 났다 — 음수의 정체가 다음 카드의 값이다"가 이 카드의 문장이다(족보 299). ★PM 판정 T375 §4-2: **손잡이 `T375_ACTIVE_FLAG` 는 남긴다**(게이트 ⑨ 가 제품 글자를 뜬다) — 단 `인계/Z-존서버.md` 에 **"켜면 안 되는 손잡이" 표**를 만들고 첫 행으로 적는다(이 카드가 순회를 합치면서 그 필드를 **흡수**하거나 지운다 — 그때 표에서 뺀다).

**카드 T385 — 넷 · 의존 표가 본체. PM 판정**(재민 거부권): ⓪ **자의 결함 먼저** — `test-move-soa ⑤`(분포 무변)의 `mrun()` 이 `Math.random()` 이라 판마다 흔들린다(T375 §3 · 4판 중 1판 T252 자가 우연히 켜짐) → 씨 박은 대조군(`seed-rand.js` 흐름)으로 · 그 절이 옛 결함 모양(씨 없는 미끼)을 문다는 반례 한 줄. ① **의존 표** — 주민 전수를 도는 순회 일곱(`spatial`·`stairs`·`fall`·`hpRegen`·`gauge`·`inputTO`·`gaugeNet` · `zone.js` 틱 본체 `:11255` 근처 · T371 이름 그대로)마다: 읽는 것(자기 몸 필드 · 다른 몸 필드 · 격자/색인 · 시계) · 쓰는 것 · **다른 몸의 같은 틱 결과를 읽나**(예: `spatial` 이 재생성한 격자를 `fall` 이 같은 틱에 묻나 · `hpRegen` 이 `gauge` 를 읽나) — 읽으면 그 둘은 **순서를 지켜야** 하고 한 바퀴에 못 넣는다 · 표 한 장(7×7 "k 가 j 를 본다" 칸) · 여기서 **합칠 수 있는 집합**을 유도한다(값이 아니라 표에서 나온다). ② **한 바퀴** — ①이 허락한 것만 `players.values()` **한 번**에(몸마다 단계 차례 = 지금 순회 순서 그대로 · 못 합친 것은 제자리) · 문지기는 T375 의 `_active` 필드를 **한 바퀴 머리에서 한 번** 계산(그러면 `t375R1`·`t375R2` 새 순회가 사라진다 = T375 손잡이 흡수) · 손잡이 `T385_ONE_SWEEP` 기본 끔 · **게이트: T375 ⑨ 그대로**(켬/끔 4,200틱 좌표·HP·게이지 비트 동일 · 순서를 바꿨는데 같으면 ①이 맞은 것이고 다르면 ①이 틀린 것 — 다르면 **합치지 말고 표에 적는다**). ③ **`stairs` 주민 루프** — `findStairBuildingForCell` 의 `` `${cx}_${cy}` `` 문자열 키를 정수 키(T333 문법 · `cx*W+cy` 류 — 이미 있는 정수 키 자가 있으면 그것)로 · 세계에 계단이 거의 없으니 **계단 있는 셀 집합이 비면 루프를 안 돈다** 가 먼저(있음/없음 한 비트) · `stairCellCache` 접점만. ④ **값** — T375 자 그대로(낮 창 0.18–0.27 · 같은 씨 · 열아홉 갈래 전/후 표) · 그 밖 1.46(T375 켬)·1.15(끔) 대 이 카드 켬 · T375 §4-1 외삽 "1.0~1.2µs/사람" 에 닿았나(닿지 않은 만큼 = 표에 · 지어내지 않는다) · 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 = 실서버 비트 동일 · `sim/` 무접촉(만지면 3시드) · 새 수 0 · `test-move-soa` 88 + ⓪ 씨 절 + ⑨ 게이트(이 카드 손잡이로) · `test-fish-act` 73 · `test-wood-act` 87 · `test-forage-act` 86 · `test-war-world` 97 · `test-harness-lint` 61 · `e2e-move` 37 · `lab-wiring-check` 실패 0 · 접점 심볼(`_active|_activeGen|T375_ACTIVE_FLAG|T385_ONE_SWEEP|rebuildSpatialIndex|stepStairFor|findStairBuildingForCell|stairCellCache|hpRegen|gauge|inputTO|gaugeNet|isPositionActive|t371-tick-rest|test-move-soa|seed-rand`).

## 0. §0 실측 (넷)
ⓐ 의존 표 7×7 + 합칠 수 있는 집합. ⓑ 게이트 ⑨ 켬/끔 비트 동일 + ⓪ 씨 절. ⓒ `stairs` 정수 키 diff + 비면 안 돈다 절. ⓓ 열아홉 갈래 전/후 + 한 줄 답.

## 1. 절대 규칙
순서가 세계다 — 의존 표 없이 합치지 않는다 · 게이트 다르면 합치지 않는다 · 새 수 0 · 손잡이 하나(`T385_ONE_SWEEP` 끔) · `rebuildSpatialIndex` 증분(T375 §4-4)은 **회부 그대로**(이 카드 밖) · **T381(세션3 `pfFindPath`)·T378(세션2)·T383(세션4 `isWaterTileLocal`)·T374(세션1) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/one-sweep-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 긴 창은 `setsid nohup … & disown`)**.

## 2. 작업 (다섯)
⓪ 씨 절 ① 의존 표 ② 한 바퀴 ③ `stairs` 정수 키 ④ 값 + 한 줄 답.

## 3. 검증
끔 비트 동일 · 게이트 · 하네스 아홉 · 접점 목록.

## 4. 회부만
켜기(재민 · 값 표 뒤) · `rebuildSpatialIndex` 증분(PM) · 몹 순회(`npcs`)에 같은 문법(PM · 이 카드는 주민만) · C 층 접속(#53).

## 5. 완료
커밋 [승인 대기 · T385] · 가지 푸시 · `보고/T385_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 의존 표 요약 + 그 밖 전/후**) · `인계/Z-존서버.md` 갱신(**"켜면 안 되는 손잡이" 표** 신설 · `T375_ACTIVE_FLAG` 첫 행) · 채팅 "가지 올렸다 <해시> — 합친 순회 n/7 · 게이트 n/n · 그 밖 1.15 → … µs · stairs 정수 키". 실기 0.
