# 구현 지시 — T370: A\* 가 틱마다 899명(61%)에게 다시 묻는다 — `needPath` 네 조건(경로 없음 · `pathIndex ≥ length` · 목표 키 바뀜 · 5초 경과) 중 **어느 것이 낮에 무는지 히스토그램**으로 가른 뒤 그 조건을 고친다(새 수 0 · 5000ms 는 있는 수) · 손잡이 `T370_PATH_REUSE` 기본 끔 · 켬 판: A\* 호출/틱 899 → ? · 사람당 µs(T356 자 · 같은 창) · 도착 실패·벽 비비기 수(경로를 덜 물으면 못 가는 사람이 생기나 — 반례) · 실서버 200일 켬/끔 인구·곳간(T326 자) (세션3 · T356 뒤 · 2026-09-23)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T370.md` · 레포 `지시/지시_T370.md`)

---

T356(`batch/tick-anatomy-0923` → main · **해부 표: 낮 A\* 19.3% · 이동 문 19.0% · 따라가기 14.1% · 순회 껍데기 8.6% · 생활층 6.1% · 방송 2.1% · 결정 1.5% · 그 밖 25.7% = 합 8.22µs(빼기 0 · 자가 자를 잰다 · `hrtime.bigint` 할당 함정을 스스로 잡음)** · 밤은 이동 문 28.3% · SoA 1층 첫 켜 = 지형 셀당 비트(`T356_SOA` 기본 끔 · `isWaterTileLocal` 걸음당 6.02 → 0.0155회 · 게이트 408명×4,200틱 비트 동일 · `test-move-soa` 59/0) · 못 잰 둘을 못 쟀다고 적음(손잡이 틱 값 ±15% 창 · 작은 술어 감싸면 인라인이 풀린다)) **통과** — "끄지 말고 한 판 안에서 시계를 대라 · 자가 자를 잰다"가 이 카드의 문장이다(족보 287). ★T356 회부 1 이 이 카드다: **틱마다 899명이 경로를 다시 묻는다 — 2초 쿨다운이 낮에 안 문다.**

코드를 보면(`zone.js:2745~2755`) 재계산 조건은 넷이다: `!npc.path` · `pathIndex >= path.length` · `_pathFor !== targetKey` · `now − _pathAt > 5000`. 결정은 0.5~1.5초에 한 번이라 목표 키가 틱마다 바뀔 리 없고, 5초 만료도 틱마다 899명일 수 없다 ⇒ 남는 건 **`pathIndex ≥ length`**(경로 끝에 닿았는데 `arrived` 가 아니면 다음 틱에 또 묻는다 — 끝 마디에서 막힌 사람이 매 틱 A\* 를 돈다) 아니면 `!npc.path`(경로를 못 찾아 beeline 을 넣었는데 …). **추측이다 — 세어라.**

**카드 T370 — 셋 · 순서가 카드다. PM 판정**(재민 거부권): ① **히스토그램** — 낮 창(T356 자 · 같은 조각)에서 `needPath` 가 참이 된 **이유별** 호출 수/틱(넷 + "경로 못 찾음 beeline") · 같은 주민이 연속 몇 틱 묻는가(1·2~10·>10 분포) · 그때 그 주민의 상태(`behavior` · 목표까지 거리 · 끝 마디 막힘 여부) — 계측기 `scripts/t370-path-why.js`(러너 밖 · `t356-tick-anatomy` 자 재사용). ② **고침** — ①이 가리킨 조건 하나: 경로 끝에서 막혔으면 **있는 5초 만료가 올 때까지 다시 묻지 않는다**(또는 ①이 다른 조건을 가리키면 그것 — 원칙: 같은 목표·같은 자리에서 A\* 를 두 번 돌리지 않는다 · 새 수 0 · 5000ms 그대로) · 손잡이 `T370_PATH_REUSE` 기본 끔 · **끔 = 비트 동일**(게이트 T350 자 4,200틱) · 켬은 좌표가 달라진다(경로를 덜 묻는다) — 그래서 **반례 표**가 게이트다: 켬 판에서 목표 도착률·도착 시간 중앙값·벽 비비기(`_stuckPos`) 수·`followNpcPath` 실패 수가 끔 대비 어떻게(같은 씨 · 같은 창 · T252 자) — 나빠지면 그 조건이 아니다. ③ **값** — 같은 창 켬/끔: A\* 호출/틱 899 → ? · A\* 갈래 µs · 사람당 합 8.22 → ? · 존 틱 p50 · 실서버 200일 켬/끔 인구·곳간·소멸(T326 자) · **켜지 않는다**. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: `sim/` 무접촉(3시드 면제) · 끔 비트 동일(`test-move-soa` 59 + 절) · `test-fish-act` 73 · `test-wood-act` 87 · `test-forage-act` 86 · `test-war-world` 56 · `e2e-move` 37 · `lab-wiring-check` 실패 0 · 접점 심볼(`computeNpcPath|needPath|_pathFor|_pathAt|pathIndex|followNpcPath|_stuckPos|_searchStep|gGet|T370_PATH_REUSE|t370-path-why|t356-tick-anatomy`).

## 1. 절대 규칙
새 수 0(5000 은 있는 수) · 손잡이 기본 끔 · 켜지 않는다 · A\* 알고리즘(`path-core.js`) 무접촉 — 부르는 조건만 · **T371(세션7 '그 밖' 시계 — `zone.js` 시계 자리만 · 리베이스 때 둘 다) · T359(세션1) · T364(세션10) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/path-reuse-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · ③ 표) · SoA 순서 2·3(나무 CSR · 벽 1바이트 · 다음 카드) · C 커널 접속(#53 WASM · 이동 문 19% 위에서) · `NPC_CAP_PER_VILLAGE = 40`(#54 · 재민).

## 5. 완료
커밋 [승인 대기 · T370] · 가지 푸시 · `보고/T370_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 히스토그램 한 줄 + 켬/끔 표**) · `인계/M-이동입력.md` 갱신 · 채팅 "가지 올렸다 <해시> — 이유 … n/틱 · 899 → … · 사람당 … · 도착률 …". 실기 0.
