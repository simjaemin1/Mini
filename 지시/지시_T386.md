# 구현 지시 — T386: GPT BGM R&D(`codex/bgm-rnd-01` 86커밋 → main 스쿼시 `462b4acc`) 의 **검수** — 제품 `bgm.js` +433 의 opt-in 경계 감사(기본 `legacy` 비트 동일을 **13곡 해시**로 실증) · `설계/BGM_RND_*` 11장 → 정본 후보/버림 표(한 장 한 줄 · 판정 0) · 새 수 표(`ARI_VILLAGE_DAY_BEAT 0.72`·`attack 0.085`·`vibRamp 0.35`… 어느 것이 유도고 어느 것이 지어낸 수인가) · `sfx-board.html` 문법으로 **브금판** 한 장(`variant:'legacy'|'phrase'` A/B — 재민 귀 #66 을 브라우저로) · 공개 DDSP-Gugak 대금 체크포인트 **라이선스 표**(저장소 라이선스 파일 그대로 인용 · 판정 0) (세션8/ART · T372 뒤 · 2026-09-25)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T386.md` · 레포 `지시/지시_T386.md`)

---

T372(`batch/grove-kinds-0923` → main · 군락 종 넷 정의 · 빈칸 0 · 새 수 0 · 스프라이트 넷 · `janggu_gung: '합성'` 은 T353 추론이 안 서서 안 고침) **통과** — "자기가 쓴 전제를 다음 카드에서 스스로 뒤집는다"(족보 297). 그 사이 **세션13(GPT)** 이 카드 없이 열흘치 BGM R&D 를 벌였다: 86커밋 · 175파일 · +70,032줄 · `public/assets/audio/bgm/bgm.js` **+433**(`planPerformancePhrase(events, opt)` · `variant:'legacy'|'phrase'` · 기본 `legacy`) · `tools/` 24 폴더(`tools/ddsp-gugak-public-runtime/` 3,142줄 대금 렌더러 · `tools/score-expression/plans/*.json`) · `설계/BGM_RND_*` **11장** · `보고/BGM_RND_*` 4장 · `scripts/test-bgm-phrase.js` 16/0 · `scripts/e2e-bgm-phrase.js` · `render_jeongak.py`·`render_score.py`·`sampler.py`·`score_village_day.py` 수정 · 청취본 WAV 5 는 git 밖(`/Users/simjaemin1/.codex/worktrees/durango-mini-bgm-rnd-01/_bgm_rnd/full-ari-reference-fade-audition-pack-r1-20260925-193000/`). PM 이 게이트 초록(test-audio 125 · client-globals 26 · lint 61 · assets-audit PASS)을 보고 **스쿼시 하나로 착지**했고 `보고/BGM_RND_착지메모_2026-09-25.md` 에 "R&D 미확정 · 정본 아님" 을 못 박았다(족보 298). **정본은 여전히 T353 의 것**(3층 오프라인 · `compose.py`/`motif.py sigim()`/`arirang.py` → `gugak.py` → 13 ogg → `bgm.js scenePick`)이고, 이 카드는 GPT 가 놓고 간 것을 **세션8 의 눈으로** 잰다 — 채택이 아니라 검수다.

**카드 T386 — 넷 · 판정 0 · 표만. PM 판정**(재민 거부권): ① **opt-in 경계 감사** — `bgm.js` +433 이 `variant:'phrase'` 밖에서 한 글자도 안 움직이는지: ⓐ 정적(`performancePhrases.villageDayAriDaegeum` 이 `false` 일 때 새 함수가 불리는 자리 0 — 호출 그래프 표) ⓑ 동적(**13곡 렌더 해시** — R&D 착지 전 `450c6e44` 와 착지 후 `462b4acc` 에서 같은 씨로 `scenePick` 13곡을 렌더해 바이트 동일 · 정본 파이프라인 13 ogg 는 파일이라 그 해시도 표) ⓒ `test-bgm-phrase` 16 이 무엇을 재는지 한 줄씩(자명 통과 절 표시). ② **새 수 표** — `bgm.js` 안 상수 전부(`ARI_VILLAGE_DAY_BEAT = 0.72` · `attack 0.085` · `release 0.20` · `vibRamp 0.35` · `vibRate 4.6` · `vibCents 34` · `scoop 45` · `harm 9` · `bright 1.05` · `breath 0.085` …)에 대해 **출처 칸**: T353 이전부터 있던 수(정본 · 그 카드) / GPT 가 유도한 수(어느 설계 문서 어느 줄 · 유도 근거가 서나) / 지어낸 수(근거 없음) — 셋 중 하나 · **값은 안 바꾼다**. ③ **설계 11장 + 보고 4장 표** — 장마다 한 줄: 무엇을 주장하나 · 정본(T353 `설계/` BGM 문서)과 **충돌**하나 · 정본 후보/참고/버림 셋 중 하나 **권고**(판정은 재민·PM) · `tools/` 24 폴더도 같은 표(쓰는 것/안 쓰는 것/못 돌린 것 — `ddsp-gugak-public-runtime` 은 **체크포인트 라이선스** 칸: 저장소 이름 · 라이선스 파일 원문 한 줄 인용 · 상업 사용 가부는 **판정 안 한다**). ④ **브금판** — `public/sfx-board.html`(T323) 문법 그대로 `public/bgm-board.html` 한 장: 13곡 행 + `village_day_ari` 는 `legacy`/`phrase` 두 버튼 나란히(같은 씨) · ○/✗ 메모 칸(재민 귀 · #66 의 브라우저 판) · 새 오디오 파일 0(있는 정본 13 ogg + `bgm.js` 실시간 렌더만) · `e2e-audio-probe` 호출 수 무변. ⑤ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: **`bgm.js` 무변**(감사만 · diff 0) · `tools/`·`설계/` 무변(표만) · `public/assets/sfx/**` 무접촉(세션9 T387) · 새 파일 = `bgm-board.html` 하나 + 계측기(`scripts/t386-bgm-hash.js` 러너 밖) · `test-audio` 125 · `test-bgm-phrase` 16 · `test-client-globals` 26 · `test-assets-audit` PASS · `e2e-audio-probe` 27 · `test-harness-lint` 61 · 접점 심볼(`planPerformancePhrase|performancePhrases|variant|scenePick|ARI_VILLAGE_DAY_BEAT|test-bgm-phrase|e2e-bgm-phrase|bgm-board|sfx-board|ddsp-gugak-public-runtime|BGM_RND_|BGM_RND_착지메모`).

## 0. §0 실측 (다섯)
ⓐ 경계 감사(호출 그래프 · 13곡 해시 전/후 · 16절 표). ⓑ 새 수 표(출처 셋). ⓒ 설계 11 + 보고 4 + tools 24 표(권고 칸). ⓓ 브금판 한 장(행 수 = 13 + A/B 2). ⓔ 한 줄 답.

## 1. 절대 규칙
판정 0 · `bgm.js` 무변 · 값 무변 · 새 소리 0 · 청취본 5 WAV 는 **레포 밖 그대로**(옮기지 않는다 · worktree `_bgm_rnd` 삭제 금지) · `sfx/` 무접촉 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/bgm-rnd-review-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (다섯)
① 경계 감사 ② 새 수 표 ③ 문서·tools 표 ④ 브금판 ⑤ 한 줄 답.

## 3. 검증
diff 0(bgm.js) · 하네스 여섯 · 접점 목록.

## 4. 회부만
#66 청취본 5 듣기(재민 · 파일) · 설계 11장 정본/버림 판정(재민·PM · ③ 표 뒤) · 체크포인트 라이선스 판정(재민) · #64 셋(샘플 뱅크 · AI Hub · 4060) · GPT 다음 BGM 카드(PM · ③ 표 뒤에만).

## 5. 완료
커밋 [승인 대기 · T386] · 가지 푸시 · `보고/T386_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 경계 감사 결론 + 새 수 셋 중 지어낸 수 n**) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 13곡 해시 동일 n/13 · 새 수 n(정본 n · 유도 n · 지어냄 n) · 설계 11장 후보/참고/버림 n/n/n · 브금판 행 n". 실기: 재민이 배포 뒤 브금판 A/B.
