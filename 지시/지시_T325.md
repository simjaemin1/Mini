# 구현 지시 — T325: 나무꾼을 옮긴다 — T312 문법 그대로(현장 = `resourceAt` 나무 · 베는 순간 손에 · 귀환하면 곳간 · 죽으면 낙하 · `addProduce('wood')` 은 켠 마을에서 0 · 첫날 합 = 수식 · 손잡이 `T325_WOOD_ACT` 끔) · 나무는 유한하다(베면 없어지고 T146 개체 장부 문법으로 자란다 · 새 수 0) (세션1 · T311 뒤 · 2026-09-19)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T325.md` · 레포 `지시/지시_T325.md`)

---

T311(`batch/lab-fix-0919`·`batch/field-even-0919` → main · **고기는 안 샜다 — 두 팔을 가른 것은 집이다**(`_mapBeds`→`_hcap`→인구→고기 · 관찰 팔은 집을 0.25 바닥으로 짓는다) · 배분은 같은 함수 · field-even 13커밋 넷째 판 위 · 끔 동일 · 4팔 못 가름) **통과** — "누수"라는 카드의 말을 호출 수로 뒤집은 것이 답이다(족보 251). 회부 ②(집도 행위인가)는 재민 칸(#51) · `satMul` 칸은 ⓑ 그대로.

**카드 T325 — 어부 문법을 나무꾼에. PM 판정**(재민 거부권): ① **현장** — `resourceAt`(T301·T317 · 관측자 무관)로 마을 반경 16셀 나무 셀(T309 ⓒ 표가 51마을 나무 셀 수를 냈다) · 나무꾼은 가장 가까운 나무로 걷는다(T316 이동 문 · 같은 술어 · **길 캐시는 T324(세션3)가 정본 — 여기서 안 만든다**). ② **행위** — 벨 때마다 `inventory.wood`(kg = `weights.js` 정본 · 새 수 0) · 나무는 **줄어든다**(그 개체 제거 · `resource_update` 방송은 관측자 있을 때만 — T324 ⓐ 규약) · 재생은 **T146 개체 장부 문법**(로지스틱 · 사본 0 · 값은 지금 수식 `land.forest × … × sk` 하루 합에서 유도 — 셀당 하루 재생 = 수식 ÷ 나무 셀 수 · `설계_민물고기 §2` 와 같은 식) · 상한 또는 낮 끝에 곳간 `woodToGranary`(`fishToGranary` 와 같은 꼴 — 셋째면 함수 하나로 합쳐 `actToGranary(v, item, units)` · 사본 0) · 켠 마을에서 `addProduce('wood')` 호출 0. ③ **첫날 등가** — 3시드 51마을 첫날 목재 합 = 수식(폭 = T297 하루 잡음) · 나무 없는 마을(T309 ⓒ) 수 · 관측자 없는 마을도 같은 길(T316 문 둘). ④ 손잡이 `T325_WOOD_ACT` 기본 끔 = 3시드 비트 동일. ⑤ 세계 위 부하는 **T324 표에 얹는다**(같은 자 · 나무꾼 팔 하나 추가 · 세션3 와 리베이스). ⑥ 한 줄 답. 사흘. 질문 금지.

★검증 규약: 끔 3시드 동일 · 게이트 셋 · `test-fish-act` 그대로 + `test-wood-act`(ⓐ 관측자 켬/끔 같은 씨 = 같은 벌목 ⓑ 죽은 나무꾼 손의 통나무 낙하 = 플레이어 함수 ⓒ `addProduce('wood')` 0 ⓓ 곳간 = 손 합 ⓔ 셀 예산 소진 뒤 0 · 재생 다음 날 ⓕ 나무 개체 수 감소 = 벤 수) · `test-resource-index` 20 · `lab-modules --diff` · 접점 심볼(`resourceAt|inventory|weights.js|woodIncomeFn|addProduce|JOB_OUT.lumberjack|_wcut|T146|actToGranary|T325_WOOD_ACT`).

## 1. 절대 규칙
새 수 0 · 사본 0 · 배수·스위치 0 · 손잡이 끔 · **T324(세션3 이동 문·캐시)·T315/T326(세션4)·T295(세션10)** 무접촉 · ★푸시 2판(가지 `batch/wood-act-0919` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
켜기(재민 · 어부와 같이) · 집도 행위인가(#51 재민) · 광부(#43·#47).

## 5. 완료
커밋 [승인 대기 · T325] · `보고/T325_<날짜>.md`(**머리 15줄**) · `설계_생산_실체.md` §0 나무꾼 행 · 채팅 "가지 올렸다 <해시> — 등가 … · 끔 동일 · 하네스 …/n".
