# 구현 지시 — T431: 헤드룸 실측 둘째 판 — T417 이 정직하게 적었다: 30분 한 판이 잰 것은 "혼자 나무 베는 사람 + 날씨"다(헤드리스엔 캐릭터 시트가 안 떠 **발자국 0** · 스폰 마을 곁 30분에 **주민 사건 0**) ⇒ 손 조합(`handCombo` 0.7967 · 숲 짐승 한 판 **1.0407 = 문턱 밖**)이 실제로 오는지 아직 못 봤다 ⇒ ① 발자국을 세운다(캐릭터 시트를 헤드리스에서 띄우는 있는 문법 — T395 프레임 자·`e2e-move` 가 쓰는 그 자리 · 없으면 시트 없이 `step` 을 부르는 자리를 표로) ② **주민 사건이 오는 자리**에서 한 판(마을 안 · 벌목 마을 · 전쟁 자 `test-war-world` 의 교전 자리 · 짐승 근처 — 네 판 30분 · `sfx-cooccur.js` 그대로) ③ 반경 0 결함 **전수**(T417 이 셋을 잡았다 — 자리로 부르는 표의 키 63 전부 · `test-audio ⑱a` 가 이미 29를 막는다 · 나머지 34 는 왜 자리 없이 부르나 표) ④ 한 줄 답 + `worstCombo` 갱신(실측이 손 조합을 넘으면 그 묶음이 정본 · 문턱 0.8 은 무변) (세션9 · T417 뒤 · 2026-09-26)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T431.md` · 레포 `지시/지시_T431.md`)

---

T417(`batch/sfx-animals-0926` → main · 동물 40 중 7 이 운다(새 6 · 곰·꿩·메추라기 · 무스·여우 = PD/CC0/CC-BY 없음 → 녹음 회부) · 뜬 존에서 실제로 나는 종만(나머지 25종은 그 바이옴 존이 뜰 때) · 짐승 맞음/죽음/길들임 이웃 키 · **반경 0 결함 셋**(`axe`·`harvest`·`downed` — 주민 벌목이 존 어디서든 귓가에서 최대 볼륨 · 320·320·384) · `sfx-cooccur.js` 30분 실측 최악 `axe`+`wind` 0.4213 · `craft_done` 서버 한 줄 · `test-audio` 180 · `e2e-audio-probe` 76 · `e2e-pid-collide` 13) **통과** — "반경 0 결함 셋 — 주민 벌목이 존 어디서든 귓가에서 최대 볼륨이었다"가 이 카드의 문장이다(족보 344). ★PM: T417 §8 이 스스로 적은 대로 실측이 얇다 — 얇은 실측으로 `worstCombo` 를 정본 삼으면 손 조합(1.0407 · 문턱 밖)이 실제로 오는 날 헤드룸이 깨진다. **둘째 판**이 이 카드다 · 문턱은 안 올린다(T412 ⑧c).

**카드 T431 — 넷. PM 판정**(재민 거부권): ① **발자국** — 헤드리스에서 캐릭터 시트가 안 떠 `step` 이 0 이었다(T417 §8) · 시트를 띄우는 있는 문법(T395 프레임 자 · `e2e-move` 37 · `e2e-aim` 15 가 쓰는 헤드리스 기동 — 어느 것이 시트를 띄우나 표) 으로 `sfx-cooccur.js` 를 다시 세운다 · 안 뜨면 **왜**(WebGL/이미지 디코드/폰트 — 한 줄) 와 시트 없이 `step` 을 부르는 자리(있는 것 · 새 코드 0 — 없으면 "발자국은 헤드리스에서 못 잰다" 한 줄 + 재민 실기 회부). ② **네 판 30분**(`sfx-cooccur.js` 그대로 · 창 200ms · `__sfx.tap` 읽기 전용): ⓐ 마을 안 정지(주민 사건 — 벌목·채집·제작·대화 — 마을 시각이 낮이고 주민이 도는 창 · T410 뒤면 관측자 0 도 돈다) ⓑ 벌목 마을 곁 이동(`axe`·`harvest`·`tree_fall`·발자국 겹침) ⓒ 교전 자리(`test-war-world` 가 세우는 그 세계 · 화살·타격·`downed`·`hit_body` 겹침 — 자를 러너 밖에서 같은 픽스처로) ⓓ 짐승 곁(늑대·곰 자리 · `e2e-audio-probe` 의 그 픽스처) · 판마다 창 수 · 서로 다른 묶음 수 · 최악 묶음 · 값 · 손 조합(`handCombo` 0.7967 · 짐승 1.0407)과 나란히 · **문턱 0.8 넘는 창이 하나라도 있으면 그 묶음과 시각**. ③ **반경 0 전수** — 소리판 키 63 × (자리로 부르나(pos 있음) · 반경 · 부르는 줄 · 자기 소리인가) — `test-audio ⑱a` 가 막는 29 밖의 34 가 자기 소리(자리 없이)인지 결함인지 표 · 결함이면 T417 문법(이웃 키 반경 · 새 수 0)으로 · 자기 소리면 "왜 자리 없음이 맞나" 한 줄 · `⑱a` 표를 63 으로 늘린다(자기 소리는 명시 목록). ④ 한 줄 답 + `_실측.worstCombo` 갱신(넷 판 최악 · 손 조합을 넘는 실측이 오면 그것이 정본 · 안 오면 T417 값 그대로 + "네 판에서도 안 왔다" 한 줄) · 문턱 무변. 사흘. 질문 금지 — 답은 표.

★검증 규약: 새 소리 0 · 문턱 무변(0.8) · `bgm/`·`tools/` 무접촉(세션8 T430) · 서버 무접촉(반경은 클라 표) · 기존 63키 비트 동일(반경 고침 외) · `test-audio` 180 + 절 · `e2e-audio-probe` 76 · `e2e-aim` 15 · `e2e-pid-collide` 13 · `test-war-world` 116(픽스처만 읽는다 · 무변) · `test-client-globals` 26 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`sfx-cooccur|worstCombo|handCombo|__sfx.tap|resourceHit|damagePlayer|hpWhy|test-audio|e2e-audio-probe|e2e-move|e2e-aim|test-war-world|sfx-board|48-a-audio|step`).

## 0. §0 실측 (넷)
ⓐ 발자국 표(시트 뜸/안 뜸 · 왜 · `step` 창 n). ⓑ 네 판 표(창·묶음·최악·값·손 조합 대조·0.8 넘는 창). ⓒ 반경 전수 63(막는 29 + 34 갈래). ⓓ 한 줄 답 + `worstCombo`.

## 1. 절대 규칙
새 소리 0 · 문턱 무변 · 자 그대로(`sfx-cooccur.js` 판만 넷) · 서버 무접촉 · **T430(세션8)·T426(세션2)·T427(세션3)·T410(세션1) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-cooccur2-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 30분 네 판은 `setsid nohup … & disown`)**.

## 2. 작업 (넷)
① 발자국 ② 네 판 ③ 반경 전수 ④ 한 줄 답.

## 3. 검증
비트 동일 · 하네스 여덟 · 접점 목록.

## 4. 회부만
#60(재민 · 소리판 둘째 귀) · 무스·여우 녹음(재민 · `_sfx_in/`) · 문턱 값(재민 · 0.8 넘는 창이 실제로 오면 — 값이 아니라 어느 묶음을 줄이나) · 발자국 헤드리스 불가면 재민 실기(맥 · #73 옆).

## 5. 완료
커밋 [승인 대기 · T431] · 가지 푸시 · `보고/T431_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T431]` · 베이스 해시**) · `인계/S-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 발자국 ○/✗ · 네 판 최악 … (…) · 0.8 넘는 창 n · 반경 전수 63(결함 n 고침) · worstCombo …". 실기: 재민 #60.
