# 구현 지시 — T355: T349 의 남은 구멍 둘 — 존 게이트 `waitHttp(/health)` 를 정본으로(하네스마다 포트·래퍼가 갈리니 **표를 먼저** · 기계 이관 가능 자리만) · 한 번 읽고 마는 `fetch` ~56자리(`jget` 류)에 마감(`AbortSignal.timeout` · 값은 T349 의 5000 그대로 · 새 수 0) · 오늘 밤 묶음 판에서 2,400초가 사라졌는지 **전/후 한 줄**(야간 보고 두 밤 대조) · `e2e-preserve` 경계 어긋남(`shelf+2` = `wilt`) 은 표만(제품 판단 · 주인 세션 회부) (세션2 · T349 뒤 · 2026-09-22)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T355.md` · 레포 `지시/지시_T355.md`)

---

T349(`batch/boot-witness-0921` → main ff `f0248d30`~`b94ccc36` · **2,400초의 자리는 합이 아니라 한 곳 — Node `fetch` 에 기본 마감이 없다** · 기다림 루프 79+1 자리에 `AbortSignal.timeout(5000)` · 기동 증인 48자리 이관(35 가 아니었다 — T344 표가 잘못 묶었다고 스스로 적음) · `test-war-world` 입 열기 · 린트 ⑩ 판정화 + ⑪ 신설 51/0 · 전수 단독 80종 · 제품 0 · 자기가 깬 `e2e-bigmap-live` 82→44px 를 자기가 잡음) **통과** — "돌아오지도 던지지도 않는다"를 답 없는 TCP 서버로 **직접 재현**한 것이 이 카드의 문장이다(족보 280). ★PM: T349 가 main 을 직접 ff 한 것은 공통 §0 정본(세션이 main 까지 민다 · ff 조건)대로다.

**카드 T355 — 넷. PM 판정**(재민 거부권): ① **존 게이트** — `waitHttp(…/health…)` 류를 전수 표(하네스 · 포트 · 래퍼 이름 · 기동 증인 정본으로 갈 수 있나 O/X · X 면 왜) · **O 만 이관**(`fixture-boot.js waitUp` 문법 그대로 · 띄운 그 틱에 듣기 — T349 ⑧ 함정) · X 는 표에 남기고 손대지 않는다 · 린트 ⑩ 판정이 존 갈래도 세게(정본 n · 포트 0 이 목표가 아니라 **X 표와 수가 맞는 것**이 게이트). ② **단발 `fetch` ~56자리** — `jget` 류 전수 표(파일:줄) · 전부 `AbortSignal.timeout(5000)`(값은 T349 가 이미 쓴 그 수 · 새 수 0 · 던지면 지금의 `null` 경로로 — 판정 무변) · 린트 ⑪ 이 단발 자리도 세게(자명 통과 금지: 마감 없는 미끼 하나가 빨개진다). ③ **두 밤 대조** — 09-22 밤(묶음 B · v3 · T349 착지 뒤 첫 밤)의 `야간러너_2026-09-22.md` 와 09-21 밤을 맞대 **e2e 총 시간 · 2,400초 자리 유무 · `e2e-events`·`e2e-winter` 개별 시간** 한 표 · `nightly-report.js` 연속 빨강 칸(T314 ②)이 그 둘을 어떻게 세는지 한 줄 · **판정은 안 한다**(사실만) · `e2e-nature 심기①`(후보 `efd44387` T341)이 밤에 어떻게 나왔는지 한 줄 → 주인 세션1 회부. ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: **제품 0**(`scripts/` · `fixture-boot.js` 정본 · 린트만) · 전수 단독 판은 **바뀐 하네스만**(80종 재실행 안 한다 — T349 가 했다) · `test-harness-lint` 51 + 존·단발 절 · `run-regress.sh --selftest` · 접점 심볼(`waitHttp|waitUp|fixture-boot|AbortSignal.timeout|jget|test-harness-lint|⑩|⑪|nightly-report|야간러너`).

## 0. §0 실측 (넷)
ⓐ 존 게이트 표(O/X · 이관 n). ⓑ 단발 fetch 표(n 자리 · 린트 절). ⓒ 두 밤 대조 표. ⓓ 한 줄 답.

## 1. 절대 규칙
제품 0 · 새 수 0(5000 은 T349 의 수) · 러너 전수 안 돌린다 · 망고링고 예약 무접촉 · `e2e-preserve` 경계(`shelf+2`=`wilt`)는 **표만**(제품 판단 · 주인 세션 회부 · PM 배분) · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/fetch-deadline-0922` → main ff(ff 조건 그대로) · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 존 게이트 ② 단발 fetch ③ 두 밤 대조 ④ 한 줄 답.

## 3. 검증
린트 · selftest · 바뀐 하네스 단독 · 접점 목록.

## 4. 회부만
`e2e-preserve` 경계(주인 세션 · PM) · `e2e-nature 심기①`(세션1) · 2인자 `ok` 하네스 표시(세는 자 없음 · 그대로) · `prof-mt3merge` 화소 바닥(러너 밖).

## 5. 완료
커밋 [승인 대기 · T355] · 푸시 · `보고/T355_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · 채팅 "올렸다 <해시> — 존 이관 n/n · 단발 n 자리 · 두 밤 e2e … → …". 실기 0.
