# 구현 지시 — T373: Z 정리 셋 + 다음 존 현황 표 — ① 계획기 집계 거짓(물 위 후보 넷을 "대양 분리 4섬"으로 센다 · 플러드 0셀은 섬이 아니다 — 보고 수 정직하게) ② 낡은 주석 둘(`zone-config.js` "광산2 = 배가 답" · 어촌6) ③ `MAX_SPAN = 200` — 유도 근거가 있나(없으면 "상수 · 근거 없음" 표 + 다음 존에서 걸리면 회부) · ④ **다음 존 현황 표** — 존 11개(한반도 뺀 10) 각각 정본에 지형(강·호수·능선·고개·숲)·광맥·마을 칸이 얼마나 찼나 · T348 순서(지형 → 광맥 → 마을) 기준 "다음 존 후보 순위" **표만**(판정은 재민) (세션5 · T360 뒤 · 2026-09-23)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T373.md` · 레포 `지시/지시_T373.md`)

---

T360(`batch/nippon-border-0923` → main · **"대양"은 바다가 아니라 탐색 모양이었다** — 물만 60칸(세 토막)·바위만 40칸 · 200셀 훨씬 안 · 진범은 착지 조건 `label === 1`(사람 안 사는 디딤돌에 못 내린다) · 조건 하나 빼고 같은 계획기 → 다리 셋 76셀 · 도달 12쌍 → 0 · 21/21 · 고개 0 · 배 0 · 한반도 광산2 는 본토 안(최단 폭 0) · `test-nippon-boot` 39/0) **통과** — 재민의 되물음("닛폰은 대륙으로 이어져 있을 텐데?")이 맞았고, "자가 바다를 재는 게 아니라 광선을 쏘고 있었다"가 이 카드의 문장이다(족보 291). #56 닫힘(항해 층 "아직" 유지 · 필요한 자리 0).

**카드 T373 — 넷 · 표만 + 주석. PM 판정**(재민 거부권): ① `plan-bridges-v2`·`t360-border` 집계에서 **플러드 0셀 후보를 섬에서 뺀다**(보고 수 정직 · 세계 무변 · 하네스 한 줄: 섬 수 = 플러드 > 0 인 덩어리 수). ② 주석 둘 정정(`zone-config.js` · 값 무변 · T360 §0-ⓒ 근거 줄). ③ `MAX_SPAN` — 코드 이력(`git log -S`)에서 200 이 어디서 왔나 · 유도 근거가 있으면 그 줄 · 없으면 "상수 · 근거 없음" 한 줄 + **다음 존에서 물리면 그때 근거를 세운다** 회부(지금 값 무변). ④ **다음 존 현황 표** — `hanbando-terrain.json` 존 10개(`jungwon_n`·`bering`·`sibara`·`jungwon_s`·`centaria`·`hindgang`·`europa`·`nordan`·`sahar` + 한반도·닛폰 대조) 각각: rivers·lakes·ridges·passes·forests·ores·villages 칸 수 · 지형 생성이 도는지(`test-nippon-boot` 문법으로 부팅 한 번 · 마을 후보 수 · 도달 쌍) · T348 순서 기준 **"다음에 갈 존" 후보 순위와 이유**(판정 0 · 표만 · 재민이 고른다). 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(정본 JSON diff 0 · `zone-config` 주석만) · `test-nippon-boot` 39 + 섬 수 절 · 3시드 면제 · 접점 심볼(`plan-bridges-v2|MAX_SPAN|t360-border|zone-config|광산2|어촌6|hanbando-terrain.json|plan-zone-villages`).

## 1. 절대 규칙
표만 · 새 수 0 · 배 0 · 고개 0(재민 판정 전) · **T359(세션1)·T372(세션8 `chunk.js` 정의) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/zone-next-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
다음 존(재민 · ④ 표) · 고개(재민 · T360 §0-ⓑ) · #31 계획기·랩이 서버와 다른 물을 본다(PM · 25벌) · #47.

## 5. 완료
커밋 [승인 대기 · T373] · 가지 푸시 · `보고/T373_<날짜>.md`(**머리 15줄에 다음 존 순위 표**) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — 섬 수 정직 · 주석 2 · MAX_SPAN … · 다음 존 후보 1위 …". 실기 0.
