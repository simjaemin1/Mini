# 구현 지시 — T440: 고리 비용 ⓑ 청크 한 판(★PM · 3,057 → 320ms · 의미 변화 0) + **런타임 옛 영토도 다시 훑는다**(개간이 옛 영토를 도는 자리가 부팅뿐이라 부팅 사이 다시 선 그루는 다음 부팅까지 선다 — 주기는 새 수가 아니라 **영토가 자란 날**(T378 §0-ⓕ ⓒ · 그 마을 영토 전체 3,900~5,600칸 · 그날 시계) · 손잡이 0 · 끔 개념 없음 = T426 문법 그대로) · 돌연변이 `INSERT OR IGNORE` 빨강 그대로 (세션2 · T426 뒤 · 2026-09-27)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T440.md` · 레포 `지시/지시_T440.md`)

---

T426(`batch/recut-0926` → main · 개간이 다시 벤다 **참**(묘목까지) · 40일 판 재부팅 0 → 14/4,586/9,131 · 장부 = 마지막 벤 날(`ON CONFLICT DO UPDATE`) · 다시 띄우면 0 · 관측자 0 = 1(6,894행 갈림 → 같음) · 영토 밖 미끼 400 무변 · `test-recut` 12 · 고리 비용 표 ⓐ 198 ⓑ 320(셀 답 50/50 같다)) **통과** — "부팅이 그날 시계로 한 번 더 훑는다 — 그리고 부팅 사이는 아직 선다"가 이 카드의 문장이다(족보 357). ★PM: 고리 비용은 **ⓑ 청크 한 판**(ⓐ 는 "이 세계 DB 나무 0" 이라 지금은 같아도 존 쪽 문이 아니라 의미가 세계에 묶인다 · ⓑ 는 구조적으로 0) · 런타임 주기는 **영토가 자란 날**(있는 사건 · 새 수 0).

**카드 T440 — 넷. ★PM 결정(위임 · 재민 거부권)**: ① **ⓑ 청크 한 판** — `t426-ring-cost.js` ⓑ 그대로 제품에(`zone.js` T295 전쟁 나무 캐시 꼴 · 청크 991판 · 표 조회 22ms) · 셀 답 비트 동일(50마을 × 고리 셀 전수 — 자 하나 · 미끼: 캐시를 안 비우는 판은 빨갛다 — 청크가 바뀌는 사건(벰·심음·재생 단계) 마다 그 청크 판만 새로) · 하루 50마을 3,057 → n ms(p50·최대 어촌2 136 → n) · 6초 판 루프 p95 1,028 → n(T398 §1-4 값). ② **런타임 다시 훑기** — 영토가 자란 날(`_t378` 영토 편입 사건 · 그 마을) 그 마을 영토 전체를 **T426 부팅 갈래 그 함수**로 한 번 더(사본 0 · 같은 문) · 40일 판에서 부팅 없이 619 → 1,461일 사이 다시 선 그루가 영토가 자란 날 베이나(수 · 장부 날) · 영토가 안 자란 마을은 무변(대조) · 관측자 0 = 1. ③ **게이트** — `test-recut` 12 + 절(런타임 훑기 · 미끼: 영토 안 자란 날엔 안 벤다) · 재부팅 게이트(T398 자 `--gate`) 세 검문 그대로 · 3시드 면제 근거(존 런타임 · econ 무접촉 — `t176-ab` 1020 한 판은 돌린다 · 넷째 판-b 와 같은가) · 돌연변이 둘(`INSERT OR IGNORE` · 캐시 안 비움) 빨강. ④ 한 줄 답 + 회부(런타임 옛 영토 재생 = 닫힘 ○/✗ · 관측 마을 주민이 딴 묘목 4 의 자리). 사흘. 질문 금지 — 답은 표.

★검증 규약: 새 수 0(주기 = 사건) · 손잡이 0(T426 문법) · 셀 답 비트 동일 · **T432(세션1 `zone.js` 청크 문)·T435 착지본(`zone.js` 곳간 줄) 위에서 · T439(세션5 클라) 무접촉** · `test-recut` 12 + n · `test-wood-act` 106 · `test-trees` 91 · `test-regrow` 100 · `test-ghost-tree` 10 · `test-far-trees` 19 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · `t176-ab` 1020 · 접점 심볼(`_t325Scan|_T378_TREE|harvestedSeeds|_markHarvested|clearTreesInCells|regrowStageOf|t426-ring-cost|t398-regrow-recut|ON CONFLICT|_t295|ringCache|territoryGrew`).

## 0. §0 실측 (넷)
ⓐ ⓑ diff + 비트 동일 + ms 표. ⓑ 런타임 훑기 40일 판. ⓒ 게이트 + 돌연변이 둘. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 손잡이 0 · 같은 문 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/ring-chunk-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 40일 판은 `setsid nohup … & disown`)**.

## 2. 작업 (넷)
① ⓑ ② 런타임 훑기 ③ 게이트 ④ 한 줄 답.

## 3. 검증
비트 동일 · 하네스 여덟 · 접점 목록.

## 4. 회부만
#71 숲 값(재민) · 재민 실기 ①(배포 뒤 마을 안 숲).

## 5. 완료
커밋 [승인 대기 · T440] · 가지 푸시 · `보고/T440_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/C-작물자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 고리 3,057 → … ms · 셀 답 동일 · 런타임 훑기 n그루 · test-recut …/0". 실기 0.
