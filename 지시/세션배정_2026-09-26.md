# 세션 배정 — 2026-09-26 (PM 정본 · 카드 T404 표 그대로 · main `259dadb6` 기준 · 09-26 낮)

각 세션 채팅에 **아래 한 줄**만 붙여넣으면 된다. 카드 전문은 `~/Mini/지시_T◯◯◯.md` 와 레포 `지시/지시_T◯◯◯.md`(정본 · 세션이 `git pull --ff-only origin main` 뒤 읽는다). 요약은 안내일 뿐, 정본은 각 카드 원문이다.

| 세션 | 카드 | 상태 | 붙여넣을 한 줄(카드 H1 요약) |
|---|---|---|---|
| 1 | T368(+추신) | 진행 | 농부 행위 완성 — 수확 품목이 곳간에 그대로(새 수 0) · 관측자 무관 실걸음 · 손잡이 `T368_FARM_ACT` 기본 끔. |
| 2 | **T398**(새) | — | 나무꾼의 숲 = 영토 밖 고리 — `_t325Scan` 네모를 영토 셀 밖 고리로(새 수 0) · 영토 안 재생은 막지 않는다(개간이 다시 벤다) · 40일 판 실측. |
| 3 | **T399**(새) | — | 1500×뭍 — 같은 뭍 덩어리인데 1500칸 안에 못 닿는 목표가 A* 시간의 82~88% · `searchRadiusCells 64`와 `maxCells 1500`을 하나에서 유도(새 수 0). |
| 4 | **T400**(새) | — | 집도 행위다 1층 구현 — `HUT_STAGES` 정본 자재(통나무22·풀38) · 여유 크루가 자재를 손에 들고 걸어가 놓는 순간 단계 상승 · 손잡이 `T400_BUILD_ACT` 기본 끔. |
| 5 | T392 | 진행(컬링 팔) | `VIEW_RADIUS` 650 → `TILE_RENDER_RADIUS`(1,500)와 같은 상수로 · 띠는 끈다 · 능선 선분 색인 설계 표. |
| 6 | T396 | 진행(기준선 자) | 기준선 자 하나 — `t17-metrics`(econ 단독)와 `t176-ab`(생활층 붙임)가 넷째 판에서 같은 수를 내는지(반올림 차를 0으로) · 다섯째 판 3시드 표 · 판정은 재민(#69). |
| 7 | T385 | 진행(순회 하나) | 순회 일곱을 하나로 — 의존 표가 본체 · 합칠 수 있는 것만 한 바퀴 · 게이트=T375 ⑨ 그대로. |
| 8 | **T401**(새) | — | 11곡의 정본을 되찾는다 — 배포 13곡 중 11곡이 07-30판인데 그 코드가 레포에 없다 · 07-31 코드로 재현 대조 · 값 0·배포 무변. |
| 9 | **T402**(새) | — | "맞음"의 낱말은 이미 있다 — `damagePlayer` 의 `source` 를 `setHp` 로 넘길 때 뭉개지지 않게 `why` 로 그대로 나른다(새 낱말 0) · 남이 쓰러지는 소리 자리 확보. |
| 10 | **T403**(새) | — | 무기는 실체다 — 표 먼저(코드는 표가 허락한 만큼) · 소집=곳간에서 무기를 손에 든다·복귀=내려놓는다·없으면 맨손 민병 · 1층은 반출/반납 장부만(몸 무접촉). |
| 11 | **T404**(이 카드) | — | 보고 머리 규약을 템플릿 한 장으로 — `지시/_보고머리_템플릿.md`(네 칸 빈칸꼴+합격/불합격 예시) · 공통.md 포인터 한 줄 · 배정표 09-26. |
| 12 | **T405**(새) | — | 송국리형 움집 고증 정량 둘째 판 — GPT 첫 판(T377)은 정량 수 0 · 열리는 출처에서 지름·깊이·기둥 수 등 정량표 · `HUT_STAGES` 값과 대조(표만). |
| 13(GPT) | T390 | 진행 | T382 빨강 12 중 환경 6 을 자기 클론에서 닫고 다시 돈다 · `test-harness-lint` 60/1 의 그 하나 정체 · 가지 `codex/repro-0925`. |

* 이 표는 카드 T404 본문에 PM 이 이미 적어 준 09-26(낮) 배정을 그대로 옮긴 것이다 — `~/Mini/태스크보드_v1.md`(정본 §5)는 이 기기에도 없다(T313·T366·T389 때와 같은 결손).
* "붙여넣을 한 줄"은 각 카드 첫 줄(H1)을 세션11 이 요약한 것이다 — 원문은 반드시 `지시/지시_T◯◯◯.md` 전문으로 확인한다(요약은 안내일 뿐 정본이 아니다).
* T389(`batch/doc-assign-0925` → main · 배정표 09-25 · 보고 머리 점검 셋째 T358~T382 24장 완전합격 1(T364)·위반 23/24 · `nightly-known` 무변) **통과** — 카드 T404 본문에 PM 이 이미 적었다.
