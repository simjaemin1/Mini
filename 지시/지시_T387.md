# 구현 지시 — T387: 사람/전투 갈래 11자리 확보 — T365 갈래 표에서 **지금 소리가 하나도 없는 유일한 갈래**(`arrow_spawn`·`arrow_removed`·`hp_changed`·`player_attacked`·`player_down_state`·`player_downed`·`player_respawn`·`pvp_state`·`war_command_ack`·`self_stat`·`death`) · T354 §4 회부 "전투 소리(T329 세션10 뒤)" 가 T364 착지로 풀렸다 · 자리마다 **소리가 붙을 자리인가**(붙이면 안 되는 자리도 표 — 상태 동기화 메시지는 소리가 아니다) → 붙을 자리만 확보 규약 T354 그대로 · 키마다 후보 1~2 소리판에 · ⓪ `e2e-aim ⓔ` 격리(ⓓ 가 연 메뉴가 ⓔ 의 `pointerdown` 을 먹는다 — 컨테이너 12/1) (세션9/SOUND · T379 뒤 · sfx 위임 계속 · 2026-09-25)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T387.md` · 레포 `지시/지시_T387.md`)

---

T379(`batch/aim-capture-0923` → main · 재민 실기 ④ "우클릭 드래그가 패널·창 밖에서 풀린다" = `mouseleave → _aimOff` 한 줄 · 포인터 캡처가 둘(mouseleave · 패널이 가져가는 `pointerup`)을 한꺼번에 풀었다 · 첫 판이 `pointerdown` `preventDefault` 로 호환 사건을 지워 **동사 메뉴를 죽였고 자가 못 잡았다** → ⓓ-2 사건 로그 단언 · `e2e-aim` 신설 13/0(맥) · Safari 는 못 돌림) **통과** — "조준만 보는 자는 조준 밖의 결함을 못 본다"가 이 카드의 문장이다(족보 301). ★PM 컨테이너 재실행 **12/1** — ⓔ(홀드 600ms) 의 사건 로그가 `pointerup(2) mouseup(2)` 뿐이다: `pointerdown` 이 캔버스에 안 왔다. 바로 앞 ⓓ(짧은 우클릭)가 **동사 메뉴를 열어 둔 채**고 `reset()` 이 그 메뉴를 안 닫아 같은 자리의 다음 `pointerdown` 을 메뉴가 먹는다 — 제품 결함이 아니라 **자의 격리 결함**(맥에선 메뉴가 안 떴거나 다른 타이밍). 이 카드 ⓪.

T365(세션12 · Sonnet)가 빠진 소리 138 을 갈래 여섯으로 묶었고 우선순위는 재민 #60 몫이다. 그런데 **사람/전투 11 은 판정이 필요 없는 갈래**다 — 이웃 키가 하나도 없고(`문서/빠진소리_갈래_2026-09-23.md` "사람/전투 — 이웃: 없음"), T364 로 옆 마을이 군대를 보게 됐는데 화면은 여전히 무음이다.

**카드 T387 — 넷. PM 판정**(재민 거부권): ⓪ **`e2e-aim` 격리** — `reset()` 이 열린 동사 메뉴를 닫는다(`46-h-verbs` 가 여는 그 DOM 을 `Escape` 또는 캔버스 밖 좌클릭으로 · 제품 무접촉) · ⓔ 앞에 "메뉴 없음" 전제 단언 한 줄 · 컨테이너에서 13/0 이 되는지는 PM 이 확인(세션은 맥에서 13/0 유지). ① **자리 표 11** — 자리마다: 메시지가 무엇을 말하나(코드 그대로 · `zone.js`/`30-n-net.js` 줄) · **소리가 붙을 자리인가**(ⓐ 사건 = 붙는다: `arrow_spawn` 쏨 · `player_attacked` 맞음 · `player_downed` 쓰러짐 · `player_respawn` 깨어남 · `death` 짐 낙하 ⓑ 상태 동기화 = 안 붙는다: `hp_changed`(HP 눈금 · 매 틱) · `pvp_state`·`player_down_state`(상태 배지) · `war_command_ack`(UI 응답 — UI/알림 갈래로) · `arrow_removed`(화살 사라짐 — 꽂힘 소리는 `spawn` 쪽 자리) ⓒ 애매: `self_stat`(허기 눈금 진입 155초 · 첫 10분 자리 — 소리인가 HUD 인가 **표만** · 재민)) · 이 표가 ②의 입력이고 ⓑ는 확보 안 한다. ② **확보** — ①ⓐ 자리만: 키 이름 후보(`arrow_shoot`·`arrow_hit`·`hit_body`·`downed`·`wake`·`drop_all` 같은 꼴 — 이름은 자리 표가 정한다) · 확보 규약 T354·T358 그대로(파일 페이지 라이선스 · CC0/CC-BY(표기) · BBC·Macaulay ✗ · Sonniss 팩 EULA 표 · 원본 `_sfx_in/` · ogg+m4a · `sources`·CREDITS §2-b · 헤드룸 `_실측` · 이음새 자는 루프만) · **키마다 후보 1~2 를 소리판에 나란히**(1번만 배선 · 재민 첫 귀 판정 전) · 못 찾으면 "없음 · 녹음 회부" 행(억지로 안 넣는다) · 훅은 `48-a-audio.js` 만(서버 무접촉 · 메시지 이름은 이미 있다) · 변주는 `files:[…]` 씨 해시(T354 ②). ③ **한 줄 답**. 이틀. 질문 금지 — 답은 표.

★검증 규약: 출처 없는 소리 0 · `bgm/` 무접촉(세션8 T386) · 서버 무접촉 · `30-n-net.js` 무접촉(T379 로 끝 · 이 카드는 `48-a-audio.js` + 하네스만) · 기존 키 비트 동일 · `test-audio` 125 + 새 키 절 · `test-assets-audit` PASS · `e2e-audio-probe` 27 + 전투 호출 n · `e2e-aim` 13 · `test-client-globals` 26 · `test-harness-lint` 61 · 소리판 행 수 = 키·파일 수 · 접점 심볼(`manifest.json|sources|files|arrow_spawn|player_attacked|player_downed|player_respawn|death|self_stat|48-a-audio|sfx-board|CREDITS|_sfx_in|e2e-aim|reset|46-h-verbs`).

## 0. §0 실측 (넷)
ⓐ `e2e-aim` reset diff + 13/0. ⓑ 자리 표 11(ⓐ/ⓑ/ⓒ). ⓒ 확보 표(키 · 파일 · 출처 · 라이선스 · 구간 · 헤드룸 · 후보 수 · 없음 행). ⓓ 한 줄 답.

## 1. 절대 규칙
출처 없는 소리 0 · 아무 소리나 안 넣는다 · 상태 동기화 메시지에 소리 안 붙인다 · `bgm/`·서버·`30-n-net.js` 무접촉 · `Math.random` 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-combat-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
⓪ e2e-aim 격리 ① 자리 표 ② 확보 ③ 한 줄 답.

## 3. 검증
하네스 일곱 · 비트 동일 · 소리판 행 수 · 접점 목록.

## 4. 회부만
#60 둘째 귀 판정(재민 · 배포 뒤 소리판 — T354·T358·이 카드 것 한 번에) · 나머지 갈래 다섯 우선순위(재민 #60) · `self_stat` 소리/HUD(재민) · Safari 우클릭 실기(재민) · 녹음 회부 행(재민 폰).

## 5. 완료
커밋 [승인 대기 · T387] · 가지 푸시 · `보고/T387_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 붙는 자리 n/11 · 확보 n · 없음 n**) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 붙는 자리 n/11 · 확보 n(후보 n) · 없음·녹음회부 n · e2e-aim 13/0". 실기: 재민이 배포 뒤 소리판.
