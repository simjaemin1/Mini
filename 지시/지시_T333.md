# 구현 지시 — T333: 사람당 6.3~16µs → 3.3µs 로 — 남은 셋(캐시 못 덮은 지형 17.7% = 부팅 때 전 셀 미리 굽기 · 지형 술어 문자열 키 → 정수 키/타입드 배열 · 틱 본체 11%) · 인원 곡선 세 점(1,476 · 800일 세계 8,318 · 그 세계 ×1.2) · 국면(낮/밤)별 (세션3 · T324 뒤 · 2026-09-20)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T333.md` · 레포 `지시/지시_T333.md`)

---

T324(`batch/walk-cost-0919` → main · **250µs 의 주인은 강이었다** — 틱 89%가 지형 판정(`_pointToSegmentDist` 40% · `_isPointInRiver` 36% · 강 55개 4,241점 선형 주사) · 탈출 루프 0(재기 전에 안 고친 것) · 고침 = 08-31 에 만들어 꺼 둔 `terrain-tilecache` 켜기 → **p50 261 → 8.0ms(32.5배) · lag 40.75 → 0%** · 사람당 6.3~16µs · 커서 이어 돌기) **통과** — 후보 셋을 다 재서 셋 다 아님을 적고 프로파일이 가리킨 곳을 고친 것, 그리고 "이미 있던 것을 켰다" 를 그대로 적은 것(족보 258). #53 은 이제 "10,000명 × 3.3µs" 하나다.

**카드 T333 — 셋. PM 판정**(재민 거부권): ① **캐시 전 셀 미리 굽기** — 8.5MB/존 · `(tx,ty)` 순수 함수라 부팅 때(또는 청크 켤 때 그 청크 상자) **전부** 계산해 두면 "처음 밟는 셀" 17.7% 가 0 · 부팅 시간 +몇 초인지 표(같은 값 — `test-terrain-memo` 전수 대조 그대로). ② **문자열 키 → 정수 키** — `WATER_TILES`·`DITCH_CELLS`·같은 꼴 전수(`grep "\.has(\`"` · `+ '_' +`) → `tx * W + ty` 정수 또는 `Uint8Array` · 같은 답(하네스 전수 대조 · 세계 무변) · 걸음당 µs 전/후. ③ **인원 곡선** — `t324-warm.js` 틀로 세 점: 1,476(45일) · **8,318(800일 세계 — `LAB_SEEDCACHE`/DB 로 800일 판을 존에 놓을 수 있으면 · 없으면 `t17-metrics` 800일 econ 상태를 시딩 인구로 넣는 길을 표에)** · 그 ×1.2(≈10,000) · 낮/밤 국면 · 사람당 µs · 상수 항/인원 항 분리 · **10,000 × 30Hz 에 드나 — 안 들면 배수**. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(지형 술어 같은 답 전수 · `test-terrain-memo` + 정수 키 절 · 3시드 면제 — `sim/` 무접촉이어야 한다) · `test-fish-act` 56 · `test-wood-act` 56 · `test-war-world` 54 · `e2e-fogray`(관측자 화면 무변) · 접점 심볼(`terrain-tilecache|TERRAIN_TILE_CACHE|WATER_TILES|DITCH_CELLS|isRockCellLocal|isWaterCellLocal|walkPerf|t324-warm|LAB_SEEDCACHE`).

## 1. 절대 규칙
새 수 0 · LOD 0 · 낮은 Hz 0 · 존 쪼개기 0 · 손잡이 새로 0 · **T334(세션1 나무꾼 분모)·T329(세션10)** 무접촉 · ★푸시 2판(가지 `batch/walk-cost2-0920` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
#53(재민 · 이 곡선 뒤) · 워커 스레드(사람당 µs 가 바닥에 닿은 뒤 · PM 카드) · 낚시 창(T316 추신 · 이 뒤).

## 5. 완료
커밋 [승인 대기 · T333] · `보고/T333_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 사람당 …µs(낮/밤) · 10,000 = …ms/틱 · 배수 …".
