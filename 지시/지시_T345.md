# 구현 지시 — T345: 주민 이동 루프 데이터 지향 재작성 1층(★PM 결정 · 재민 "바닥까지") — 주민 위치·속도·목표·상태를 타입드 어레이(SoA)로 · 지형·벽·나무 충돌을 정수 격자 하나로 · 쿼드트리 갱신은 셀 경계 넘을 때만 · 걸음당 할당 0 · 게이트 = 옛 루프와 좌표 바이트 동일(같은 씨 · 주민 전수 · 틱마다) · 목표 사람당 0.3~0.5µs (세션3 · T340 뒤 · 2026-09-21)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T345.md` · 레포 `지시/지시_T345.md`)

---

T340(`batch/worker-audit-0921` → main · 경계 32자리 · **2워커 1.02배 · 4워커 0.79배**(암달 65%) · 걸음 하나 정적 계수: `movePlayerStep` 호출 43 · 객체 리터럴 33 · `npcStep` 53 · `Math.random` 15 — 꼭 해야 할 일은 지형 5.3·벽 5.0·좌표 갱신뿐 = 200~400ns 와 맞다 · 지금은 12~24배 · `test-route-persist` 조건 대기 · 어부 = 플레이어 낚시 대본 그대로) **통과**(족보 271). ★PM 결정(위임 · 거부권): **워커가 아니라 재작성.** 1층(JS 데이터 지향)부터 · 2층(WASM 커널)은 1층 표 뒤.

**카드 T345 — 1층.** ① **SoA** — 주민 `x·y·vx·vy·목표x·목표y·상태·마을·직업` 을 `Float64Array`/`Int32Array` 열로(주민 = 정수 색인 · 옛 객체는 그 색인을 가리키는 얇은 껍데기 — 다른 층(생활층·전쟁·방송)이 읽는 필드는 게터로 같은 값 · **사본 0**: 정본은 배열 하나) · ② **정수 격자 충돌** — T333 비트맵(지형) + 벽·건물·나무를 **같은 격자**에(셀당 비트 · 무효화 = 건물·나무 변경 때 그 셀만) · 걸음 = 격자 질의 ≤ 6회 + 좌표 갱신 · 계단·다리·물은 격자 비트로 · ③ **쿼드트리** — 셀 경계를 넘을 때만 갱신(같은 셀이면 0) · ④ **걸음당 할당 0 · 함수 겹 ≤ 3 · `Math.random` 0**(씨 해시로) · ⑤ **플레이어도 같은 루프**(`movePlayerStep` 이 두 꼴을 받거나 둘 다 옮긴다 — 사본이 남으면 안 된다 · 표) · ⑥ **게이트 = 세계 무변**: 옛 루프와 새 루프를 같은 씨·같은 입력으로 51마을 7게임일 돌려 **주민 전수 좌표 바이트 동일(틱마다)** · 낚시·벌목·전투 하네스 전부 그대로(`test-fish-act` 73 · `test-wood-act` 87 · `test-war-world` 54 · `e2e-fogray` 0px · `e2e-move`) · ⑦ **자** — 사람당 µs(낮/밤 · T333 문법) · 상수 항 · 10,000 외삽 · 프로파일 상위 10. ⑧ 한 줄 답. 나흘. 질문 금지 — 답은 표.

★검증 규약: 좌표 바이트 동일(새 하네스 `test-move-soa` · 자명 통과 금지: 격자 한 비트를 뒤집으면 달라진다) · 끔/켬 손잡이 **없음**(재작성은 스위치가 아니다 — 옛 루프는 하네스 대조용으로 한 판 남겼다가 착지 뒤 지운다 · 표) · 3시드 면제(`sim/` 무접촉이어야 한다 — 만지면 면제 없음) · `test-terrain-memo` 36 · `test-client-globals` · 접점 심볼(`movePlayerStep|npcStep|decideNpcBehavior|quadtree|_warTerrMemo|terrain-tilecache|isTerrainBlockedLocal|clWallCellMap|resourceAt`).

## 1. 절대 규칙
새 수 0 · 사본 0(정본 = 배열) · 배수·스위치 0 · LOD 0 · 낮은 Hz 0 · 워커 0 · **T346(세션4 온보딩)·T347(세션1 채집 — 걸음은 이 루프를 부른다 · 리베이스 때 둘 다 살린다)·T329(세션10 · 병사도 같은 루프)** 접점 표 · ★푸시 2판(가지 `batch/move-soa-0921` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
2층 WASM(1층 표 뒤 · 재민) · `NPC_CAP_PER_VILLAGE = 40`(#54 · 몸 수 상한 — 이 루프가 서면 푼다) · #53.

## 5. 완료
커밋 [승인 대기 · T345] · `보고/T345_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 좌표 동일 … · 사람당 …µs · 10,000 = …ms".
