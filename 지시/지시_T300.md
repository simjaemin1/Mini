# 구현 지시 — T300: 주거 처방 — 건축 캡 손잡이(`T300_BUILD_CAP` · 기본 끔) · 값 = 마을별 최대(끔 팔 실측 · T264/T275) · 정본 자리 하나 · 3시드 켬/끔 표 · 짚 이월과 나란히 (세션1 · T285 뒤 · 2026-09-18)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T300.md` · 레포 `지시/지시_T300.md`)

---

T285(`batch/field-even-0913` 0f8a1b29 · 코드 0 · 09-18 지연 처리 — PM 이 번들을 못 본 것) 통과 — picker 셋 다 `rational` · `pickDeficitJob` 호출 0/0/0(함수 전체가 죽은 다리) · `hasSlot` 위기 막힘 16~23% 의 이유는 상한 0 이 아니라 숲만큼의 자리가 꽉 차서 · 막힌 마을 3시드 어디서도 안 죽음 · **정본 주석 둘(`:3055`·`:1078`)이 죽은 규칙을 근거로 댄다** — 전부 인정. 족보 (230): "주석이 근거로 대는 규칙이 도는지 호출 수로 센다". **재민 #28 위임 → PM: 꼴 = 캡 · 값 = 마을별 최대.**

**카드 T300.** **PM 판정**(재민 거부권): ① T264 의 마을별 최대 캡(끔 팔 실측 최대 — 값은 표에서 유도 · 새 수 0)을 **정본 자리 하나**(`built` 셋째 항이 사는 곳)에 손잡이 `T300_BUILD_CAP`(`_allocKnob` 문법 · **기본 끔** · 끔 = 넷째 판 비트 동일)으로 심는다 · 값은 마을별(중앙 하나가 아니다 — T275 가 갈랐다). ② 켬 3시드 800일(넷째 판 위) 여덟 수 + `vpop` + 지켜본 여섯(농촌10·19 등)의 소멸·인구 → T252 자 · **짚 이월(`T240_STRAW_CARRY`) 켬/끔 × 캡 켬/끔 4팔**(T275 표를 넷째 판 위에서). ③ 죽은 주석 둘 고침(`:3055`·`:1078` — 도는 규칙으로 문장을 바꾼다 · 코드 0) · `pickDeficitJob` 은 **지우지 않는다**(회부 — 살릴지 지울지 재민). ④ 답 한 줄. 하루. 질문 금지.

★검증 규약: 끔 비트 동일 · 켬 3시드 · 게이트 셋 · [H] 등록 · `test-farm-life` · **T298(세션4)·T299(세션6) 같은 자 — 리베이스 순서 PM** · 접점 심볼(`T300_BUILD_CAP|T240_STRAW_CARRY|built|hasSlot|pickDeficitJob|_allocKnob|t17-metrics`).

## 0. §0 실측 (넷)
ⓐ 손잡이 자리 + 값 표(마을별) + 끔 비트 동일. ⓑ 4팔 3시드 표. ⓒ 주석 둘 diff. ⓓ 한 줄 답.

## 1. 절대 규칙
기본 끔 · 켜지 않는다(표 뒤 PM) · 새 수 0(값은 실측 유도 · 유도 목록 회부) · 배포 재민만 · ★푸시 2판(맥 VM · 가지 `batch/build-cap-0918` **가지만**) · force 0.

## 2. 작업 (넷)
① 손잡이 ② 4팔 ③ 주석 ④ 한 줄 답.

## 3. 검증
비트 동일 · 3시드 · 접점.

## 4. 회부만
`pickDeficitJob` 처분(재민) · 짚 이월 켬(표 뒤) · #11 T100 켜기(넷째 판에 이미 반영됐는지 확인 — T193/T100 손잡이 상태 표 한 줄).

## 5. 완료
커밋 [승인 대기 · T300] · 가지 푸시 · `보고/T300_<날짜>.md`(머리 15줄) · 채팅 한 줄. 실기 0.
