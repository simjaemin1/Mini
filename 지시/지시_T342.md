# 구현 지시 — T342: 간격 15 뒤 빨강 셋을 닫는다 — ① 거부 캐시가 영토 성장을 못 본다(`_terrGrow` 가 `lifeSiteDirty` 만 세우고 `lifeSiteReset` 을 안 한다 · T335) ② `test-shelter ②` 42/50 — 쉼터가 기존 집과 겹친다(야간 09-21 · 단독 빨강 · 간격 15 뒤 처음) ③ `test-site-memo` 두 밤 빨강(①·② 절) · 그리고 #54 — 존 세계 300일 포화 1,718 vs 실서버 판 200일 2,462 대조 (세션4 · T335 뒤 · 2026-09-21)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T342.md` · 레포 `지시/지시_T342.md`)

---

T335(`batch/house-gap-audit-0920` → main · **15 가 17 을 모든 열에서 이긴다** · 정지 마을의 다음 한 채를 막는 것은 땅도 방아쇠도 아니라 **거부 캐시** · "env 문이 없다" 정정) **통과**(족보 266). 이제 셋이 빨갛다.

**카드 T342 — 넷.** ① **거부 캐시** — `_terrGrow` 가 영토를 넓힐 때 `_siteRejS`/`_siteRejL` 을 `lifeSiteReset` 으로 버린다(그 함수 그대로 · 사본 0) · 정지 3곳(광산5·어촌2·임업6)이 다음 한 채를 놓는지 실서버 판 · ② **`test-shelter ②`** — 쉼터 자리가 간격 15 뒤 기존 집과 겹친다(42/50) — 쉼터 배치가 옛 18 을 따로 갖고 있나(사본!) 아니면 발자국 계산이 다른가 · 정본 `HOUSE_GAP` 하나를 부르게 · 50/50 · ③ **`test-site-memo`** — ①절 "하루 5회 이상 훑는다 26회/9일" · ②절 단조성 위반 8 — 간격 15 로 집터 훑기가 늘었나 · 하네스 기대가 18 을 전제했나 · 어느 쪽인지 표 · 고칠 곳 하나 · ④ **#54 대조** — T333 이 "800일 존 세계 인구 1,718 · 300일 포화(주거 게이트)" 를 냈는데 T326 실서버 200일은 2,462 였다 — 같은 세계인가(시계 `VILLAGE_DAY_MS` · 시딩 · 손잡이) · 어느 수가 실서버 값인지 한 표 · 포화의 주인이 주거 게이트면 그 게이트가 무엇을 기다리나. ⑤ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: `test-shelter` 50/50 · `test-site-memo` 초록(씨앗 DB 첫 실행 수 분) · `test-psite-server` · `test-terr-persist` · 3시드 면제(`sim/` 무접촉이어야 한다) · 접점 심볼(`_terrGrow|lifeSiteDirty|lifeSiteReset|_siteRejS|_siteRejL|HOUSE_GAP|shelter|test-site-memo|VILLAGE_DAY_MS|_hcap`).

## 1. 절대 규칙
새 수 0 · 사본 0(쉼터가 제 18 을 갖고 있으면 지운다) · **T340(세션3)·T341(세션1)·T329** 무접촉 · ★푸시 2판(가지 `batch/site-cache-0921` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T342] · `보고/T342_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 캐시 … · shelter 50/50 · site-memo … · #54 …".
