# 구현 지시 — T327: 소문 승계의 경주(`e2e-rumor ⑦d2` · T304 이 잡은 잠든 제품 결함 · Z · T7) — 승계가 순서에 기대는 자리 하나를 순서 무관으로 · 하네스가 순서를 뒤집어도 같은 답 (세션5 · T317 뒤 · 2026-09-19)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T327.md` · 레포 `지시/지시_T327.md`)

---

T317(`batch/chunk-overflow-0919` → main · 넘친 나무가 세계의 것이 됐다 — 이웃만 켠 판 21/21 · 낳은 청크 꺼도 남는다 · 둘 다 꺼지면 0 · 순서 바꿔도 같은 세계 · 생성물 바이트 동일 · 중복 0 · 숲 청크 +0.93ms(처음 21.9 → 상자 거르개)) **통과** — "누가 놓고 누가 지우느냐만 바꿨다" 가 답이고, 21.9→0.93 을 스스로 잡은 것이 값이다(족보 253).

**카드 T327 — 하나.** T304 §0-ⓑ-3 이 기전과 자리를 적어 뒀다: `e2e-rumor ⑦d2` — 소문 승계(rumor succession)가 **처리 순서에 기댄다**(느린 밤에만 빨강 = 경주). ① 그 자리(`server/rumor*`·`zone.js` 소문 절 — T304 가 적은 줄)에서 순서를 정하는 정본(시각? id? 도착 순?)을 표로 · 순서 무관 규칙 하나로(같은 입력 집합 → 같은 답 · 정렬 키 = 이미 있는 값 · 새 수 0) · ② 하네스 `e2e-rumor ⑦d2` 가 **순서를 뒤집어 두 번** 돌려 같은 답(반례: 고치기 전엔 다르다) · 야간 09-18 빨강 재현 → 초록 · `nightly-known` 의 rumor 한 줄 **뺀다**(T304 가 넣은 것). ③ 한 줄 답. 하루. 질문 금지.

★검증 규약: `e2e-rumor` 전부 + ⑦d2 두 순서 · `test-events`·`test-war-world` 무변 · 3시드 면제(`sim/` 무접촉) · 접점 심볼(`rumor|succession|⑦d2|nightly-known`).

## 1. 절대 규칙
제품 접촉 = 소문 절 한 자리 · 새 수 0 · **T324(세션3 이동 문)·T326(세션4)·T295** 무접촉 · ★푸시 2판(가지 `batch/rumor-race-0919` → main ff(하네스 초록 · 3시드 면제) · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
청크 경계 감사거리(T317 ①) · 광맥 없는 43마을(#47).

## 5. 완료
커밋 [승인 대기 · T327] · `보고/T327_<날짜>.md`(**머리 15줄**) · 채팅 "main 올렸다 <해시> — 순서 무관 · 두 순서 동일 · known −1".
