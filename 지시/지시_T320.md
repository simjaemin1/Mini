# 구현 지시 — T320: `char/` 시트 193장도 화소 자로(파일 sha256 → 정규화 화소 해시 · 격자 시트는 투명 비중이 다르다 — 같은 함수 · 재잠금 · 반례 셋 재현) · `harvest` 키 무음 확인 절 (세션8 · T308 뒤 · 2026-09-19)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T320.md` · 레포 `지시/지시_T320.md`)

---

T308(main · 자 한 자리 · 재잠금 300 · 파일 0바이트 · 반례 3/3 · 카드의 "557" 을 틀린 수로 적은 것) **통과**(족보 242). 남은 것은 회부 ③ 하나.

**카드 T320 — 둘. PM 판정**: ① **`char/` 193장** — 파일 sha256 을 **T308 의 정규화 화소 해시 같은 함수**로(사본 0 · 시트라 투명 비중이 크다 — 그래서 더 그 자가 맞다) · 재잠금 193 · 파일 0바이트(sha1 전수) · T308 반례 셋(effort 0/4/6 한 해시 · 보이는 화소 하나 = 다른 해시 · 투명 아래 = 같은 해시)을 **시트 표본 넷**으로 재현 · `asset-lock --check` · `test-charsprite`·`test-icons` 그대로. ② **`harvest` 무음 확인 절** — T305 가 배선한 `inventoryWhere.harvest` 키에 파일이 없어 무음이다 · `test-assets-audit` 에 "배선은 있는데 파일이 없는 키" 표 한 절(고아의 반대 · 지금 몇 개인지 표 · 만들지 않는다 — 녹음은 재민 #34). ③ 한 줄 답. 하루. 질문 금지.

★검증 규약: 자산 바이트 0 · `asset-lock --check` · `test-assets-audit` · `test-charsprite` · `test-audio` 그대로 · 접점 심볼(`asset-lock|pixelHash|char/|icons.lock.json|inventoryWhere|harvest`).

## 1. 절대 규칙
자산 바이트 0 · 지우기 0 · `public/client`·`server` 무접촉 · 세션9(`manifest.json`) 무접촉 · ★푸시 2판(가지 `batch/char-hash-0919` → main ff · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T320] · `보고/T320_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 193 재잠금 · 0바이트 · 반례 3/3 · 무음 키 n".
