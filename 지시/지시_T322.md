# 구현 지시 — T322: 벽시계 단정 4자리를 고친다(`e2e-inv:331` · `test-craft:196` · 예산형 `e2e-conn ②` 60×400ms · `test-resource-index:166` 은 단가라 남긴다 — 표에) · `e2e-fogray` 낡은 판 읽기(rAF 둘 → 프레임 카운터 대기) · `e2e-trade` 워프 예산 25회 소진의 뿌리 · `test-harness-lint ⑧a` 를 이름 그물이 아니라 선언 추적으로 (세션2 · T314 뒤 · 2026-09-19)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T322.md` · 레포 `지시/지시_T322.md`)

---

T314(`batch/night-split-0919` → main · 09-19 판은 기계를 안 돌렸다(v3 가 오늘 밤부터) · 세 밤 50/50 · 거짓 빨강 둘뿐(`mtfuzz`·`conn`) · `fogray`·`trade` 는 부하 아님 · ⑧a 는 이름 그물 · 155개 중 4건 · **conn 판1 오염을 고백한 것**) **통과** — "하네스를 재는 동안 러너를 부르면 안 된다" 가 족보(247)다. main ff 는 PM 이 했다.

**카드 T322 — 넷. PM 판정**: ① **벽시계 단정 셋 고치기** — `e2e-inv:331`·`test-craft:196` 은 총 시간 vs 상수 → 수만 적는다(T304 문법 · 단가가 있으면 단가로) · `e2e-conn ②` 예산형(60×400ms)은 **조건 대기**(상태가 오면 끝 · 상한은 표에만) · `test-resource-index:166` `per<2000` 은 **단가라 남긴다**(PM) — 표에 그 이유. ② **`e2e-fogray`** — PM 자(T294)가 rAF 두 번만 기다려 낡은 판을 읽는다(R1 A rsi = B rsi) → `__fogSegDbg` 에 **프레임 카운터** 한 칸(`34-m-renderloop.js` 한 줄 · 디버그 객체만) 을 두고 카운터가 **바뀔 때까지** 기다린다 · 반례: 통 끔/켬 rsi 가 실제로 다르다(≥3배) 두 판. ③ **`e2e-trade` 워프 예산** — 25회 소진 · minD 24,749 · 보정 1,817 vs 205 가 09-18 과 같은 수 = 결정적 → 하네스 자리 문제인지(워프 목표가 못 닿는 셀) 제품인지 **표로 가른다** · 하네스면 고친다 · 제품이면 회부. ④ **⑧a** — 이름 일곱 그물 → 시간 값의 **선언을 따라가** 단정에 든 것만(T314 ③의 손셈을 린트가 하게) · 155 전수에서 4건 그대로 잡히고 단가·예산형은 표로 분리(예산형은 "경고"). ⑤ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 제품 0(`34-m-renderloop.js` 디버그 칸 한 줄만 · 픽셀 무변 — `e2e-fogray` 0px 자 그대로) · 고친 하네스 각 단독 두 판 · `test-harness-lint` 42→? (새 절) · `run-regress.sh --selftest` · 접점 심볼(`e2e-inv|test-craft|e2e-conn|e2e-fogray|__fogSegDbg|e2e-trade|warp|⑧a|test-harness-lint`).

## 1. 절대 규칙
제품 0(디버그 칸 한 줄 예외) · 러너 전수 안 돌린다 · **하네스를 재는 동안 러너를 부르지 않는다**(족보 247) · ★푸시 2판(가지 `batch/harness-clock-0919` → main ff · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
`e2e-trade` 제품 결함이면 카드(PM) · rumor 경주(Z · 세션7 다음) · 굶긴 상자 장착 층(R2 · PM).

## 5. 완료
커밋 [승인 대기 · T322] · `보고/T322_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 단정 3 고침 · fogray 카운터 · trade … · ⑧a n건".
