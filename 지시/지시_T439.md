# 구현 지시 — T439: 존 경계를 되돌아올 때 클라가 스스로 소켓을 끊는다 — T428 회부 ①: pong 파수꾼(`30-n-net.js:270~283`)이 **주 소켓의 `c.lastPongAt`** 만 보는데 ping 은 주 소켓에만 가므로, 이웃 소켓이 승격되는 순간(`:1523~1527`) 그 소켓의 `lastPongAt` 은 옛 값(37.2초 전)이라 0.6초 뒤 좀비로 판정 → 강제 close → 새 손님으로 광장 재탄생 · 존이 여럿 뜨면 **모든 왕복자**가 밟는다 ⇒ 띄우기 전에 고친다: 승격 가지에서 `existingTarget.lastPongAt = 0`(첫 넘기와 같은 상태 · **한 줄 · 새 수 0**) 또는 승격 시 ping 한 번(표에서 고른다) · `e2e-zone-cross` 28/4 → 32/0 · 되돌기 열 번 왕복 · 재탄생 0 (세션5 · T428 뒤 · 2026-09-27)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T439.md` · 레포 `지시/지시_T439.md`)

---

T428(`batch/three-zones-0926` → main · 존 셋 한 호스트 119s · RSS 943MB · 오류 0 · `PEEK_THRESHOLD = NAT_VIEW_PAD` 한 줄로 경계 안쪽 이웃 존 나무 0/155 → 154/155 · 경계 걸어서 넘음 · **되돌아올 때 재탄생**(pong 파수꾼) · `e2e-zone-cross` 신설 28/4 · ⓪ `e2e-nature` 픽스처 존 안 145/0) **통과** — "존이 여럿 뜨는 순간 모든 왕복자가 밟는다 — 띄우기 전에 고친다"가 이 카드의 문장이다(족보 356). ★PM: T428 이 표만 낸 고칠 자리를 **고친다**(클라 한 줄 · 서버 0).

**카드 T439 — 넷. PM 판정**(재민 거부권): ① **고침** — 후보 둘 표: ⓐ 승격 가지 `existingTarget.lastPongAt = 0`(첫 ping 부터 다시 잰다) ⓑ 승격 순간 그 소켓에 ping 하나 보내고 pong 을 받은 뒤 승격(한 왕복 늦음 · ms) — 둘 다 새 수 0 · **ⓐ 로 간다**(한 줄 · 파수꾼 규약 그대로 "첫 넘기와 같은 상태") · 표에서 뒤집히면 근거 · 파수꾼 문(`:270~283`)은 무변. ② **왕복 자** — `e2e-zone-cross` 에 절: 한반도 ↔ 닛폰 · 한반도 ↔ 중원북 각 **열 번 왕복**(경계 양쪽 1,200px 안에서) · 재탄생 0(플레이어 id 동일 · `welcome` 다시 안 옴 · 광장 좌표 아님) · 승격마다 첫 pong 까지 ms · 미끼(고침 줄을 지운 판은 4 빨강 그대로). ③ **셋 동시 기동 재실측**(T428 자 `t428-three-boot.js` 그대로 · 고침 뒤 값 무변 확인 한 줄) + 존 둘만(한반도+중원북 · 닛폰 없이 — 재민 배포가 둘부터 갈 수 있게) 부팅·RSS 한 줄. ④ 한 줄 답 + 재민 배포 절차 한 줄(`redeploy … --all` 이 셋을 띄우나 · T428 §에서 옮김). 이틀. 질문 금지 — 답은 표.

★검증 규약: 서버 0 · 클라 한 줄(승격 가지) · 새 수 0 · 세계 무변 · **T426(세션2)·T432(세션1)·T436(세션6) 무접촉** · `e2e-zone-cross` 32/0 · `e2e-move` 37 · `e2e-pid-collide` 13 · `test-nippon-boot` 42 · `test-jungwon-boot` 38 · `test-client-globals` 26 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`lastPongAt|30-n-net|existingTarget|PEEK_THRESHOLD|NAT_VIEW_PAD|e2e-zone-cross|t428-three-boot|welcome|recover`).

## 0. §0 실측 (넷)
ⓐ 고침 후보 표 + diff 한 줄. ⓑ 왕복 20 · 재탄생 0 · ms · 미끼. ⓒ 셋·둘 기동 값. ⓓ 한 줄 답 + 배포 절차.

## 1. 절대 규칙
클라 한 줄 · 서버 0 · 새 수 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/zone-return-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 고침 ② 왕복 자 ③ 기동 재실측 ④ 한 줄 답.

## 3. 검증
하네스 일곱 · 미끼 · 접점 목록.

## 4. 회부만
#76(재민 · 이 카드 뒤 배포 `--all`) · 닛폰 수동 지형(재민) · 재시딩(T436 · 재민 값 #78).

## 5. 완료
커밋 [승인 대기 · T439] · 가지 푸시 · `보고/T439_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/N-네트워크.md`·`Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — 승격 한 줄 · 왕복 20 재탄생 0 · 첫 pong … ms · e2e-zone-cross 32/0". 실기: 재민 배포 뒤 경계 왕복.
