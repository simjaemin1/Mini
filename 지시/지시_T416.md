# 구현 지시 — T416: 가드 하나로 — #63 PM 권고: `T263_FOOD_CONS` 를 켜기 **전에** 두 기계를 하나로. T362 실측: `consumeFood` 에 `_cons` 한 줄을 넣으면 **가격 쪽은** `subs` 가드(`SUBSISTENCE_PER_NPC[r] ? 0 : EMA × 30` · v2 `:406`·`:514`)가 12종의 flowT 를 0 으로 막아 5종만 통과하는데 **게시판엔 그 가드가 없어** 식량 의뢰가 0 → 14종 12,000여 건(`rice` 2,544 · `wheat` 2,372 · flowT 0 인 채) · EMA 가 두 기계로 갈라져 들어간다(정본 하나 위반) ⇒ **어느 쪽이 정본인지 표**(가격의 가드가 옳나 — 생존 품목은 시장이 아니라 곳간이 댄다 · 게시판이 옳나 — 의뢰는 부족을 말한다) → **한 자리**(둘 다 같은 술어를 부른다 · 사본 0) → 그 뒤 켬 3시드 표(켜지 않는다 · 켜기는 #63 뒤 재민) (세션6 · T407 뒤 · 2026-09-26)

아래 전문을 **세션6** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T416.md` · 레포 `지시/지시_T416.md`)

---

T407(`batch/nippon-balance-0926` → main · 닛폰 800일 3시드 **소멸 0/7**(인구 934/1,097/1,086 · 마을당 한반도의 94%) · **마을이 16이 아니라 7곳**(품질 게이트 · 물 위 2곳 탈락 — 자는 해안선 띠 `T17_COAST` 를 봐야 서버와 같은 7곳) · 7곳 중 6곳 돌 바닥 · 6곳 숲에서 한반도 3위보다 멀다 · 숲 면적 32% · 자잘 광맥·골짜기·군락 0 · 이름 있는 강·능선·숲 0 · 그림 `산그림/T407_닛폰_소외.png` · 한반도 `t17` 비트 동일) **통과** — "닛폰은 살지만 일곱 곳뿐이고 숲이 셋 중 하나다"가 이 카드의 문장이다(족보 332). 닛폰 지형은 재민 손(그림 뒤). 세션6 은 제 것(T362 회부 #63)으로 돌아온다.

**카드 T416 — 넷. PM 판정**(재민 거부권): ① **두 기계 표** — 가격(`economy-sim` v2 `:406`·`:514` `flowT` · `subs` 가드)과 게시판(`village_board` 의뢰 생성 · 부족 판정 · 가드 없음)이 **같은 EMA(`_consEMA`)를 어떻게 읽나** 줄 번호로 · 생존 품목 12종이 각 기계에서 어떻게 갈리나(가격: 0 · 게시판: 의뢰) · 왜 가드가 한쪽에만 있나(`git log -L` — 어느 카드가 넣었나 · 그때의 이유 한 줄 인용). ② **정본 하나** — 술어 이름 하나(예: `_isSubsistenceFlow(r)` — 있는 `SUBSISTENCE_PER_NPC` 표를 읽는 그 판정)를 두 기계가 **같이 부른다** · 어느 값이 정본인가는 **표에서 유도**: ⓐ 가드가 옳다(생존 품목은 곳간이 대고 시장·게시판은 안 다룬다 → 게시판도 0) ⓑ 게시판이 옳다(부족은 의뢰로 나온다 → 가격도 flowT 를 본다) — 둘의 3시드 켬 팔(`T263_FOOD_CONS=1`)을 **둘 다** 돌려 게시·㉮㉯ 밀도·소멸·인구 표(T362 표 옆에) · **PM 권고 ⓐ**(캐논 "뭉뚱그린 품목 없음"은 품목을 말하지 시장을 말하지 않는다 · 게시 +216% 는 밀도 캐논 띠를 벗어난다) · 판정은 재민 #63 · 코드는 **술어 하나 + 두 호출**(값 무변 · 켜지 않는다 · 끔 = 넷째 판 비트 동일). ③ **켬 3시드 표** — ⓐ·ⓑ 두 팔 × 3시드 800일(`t17-metrics` · 열 열) — 새 기준선 아님 · 표만. ④ 한 줄 답 + #63 한 글자 칸. 이틀. 질문 금지 — 답은 표.

★검증 규약: 끔 = **3시드 800일 열 열 넷째 판 비트 동일** · `build-econ-bundle --check` 8 · `inline-engine --check` · `test-econ-fieldyield` 202 · `test-farm-act` 59 · `test-market`·`test-board`(있는 것) · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`consumeFood|_cons|_consEMA|T263_FOOD_CONS|SUBSISTENCE_PER_NPC|flowT|subs|village_board|postRequest|t17-metrics`).

## 0. §0 실측 (넷)
ⓐ 두 기계 표(줄 · 12종 갈림 · 가드 유래). ⓑ 술어 하나 diff + 끔 3시드 동일. ⓒ ⓐ·ⓑ 켬 3시드 표. ⓓ 한 줄 답 + #63 칸.

## 1. 절대 규칙
새 수 0 · 켜지 않는다 · 사본 0(술어 하나) · **T368 후속(세션1 `villages.js`)·T400(세션4)·T385(세션7) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/subs-guard-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 3시드 아홉 판은 `setsid nohup … & disown`)**.

## 4. 회부만
#63 켜기(재민 · ③ 표 뒤) · #69(재민) · 닛폰 지형(재민 · T407 그림) · 닛폰 재시딩(PM · 지형 뒤).

## 5. 완료
커밋 [승인 대기 · T416] · 가지 푸시 · `보고/T416_<날짜>.md`(템플릿 네 칸) · `인계/E-사건장부.md`·`T-거래.md` 갱신 · 채팅 "가지 올렸다 <해시> — 가드 유래 T… · 술어 하나 · 끔 3시드 동일 · ⓐ 게시 … ㉯ … / ⓑ 게시 … ㉯ …". 실기 0.
