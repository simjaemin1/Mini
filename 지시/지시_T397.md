# 구현 지시 — T397: T387 이 남긴 제 것 넷 + "맞음" 낱말 한 칸 — ① pid 충돌 **실측**(`30-n-net.js` `hp_changed`·`player_respawn`·`player_down_state` 가 `msg.pid === myPid` 를 연결 역할 없이 본다 — 이웃 존 관전 연결의 p1 이 나로 읽히나 · 재현 하네스 · 재현되면 주 연결 가름 한 줄) ② `hp_changed` 에 `why` 한 칸(서버 **두 줄** · `setHp(p, v, why)` 가 이미 받는 낱말 · T305 `where` 꼴) → 층은 표 한 줄로 "맞음" 을 운다 ③ `test-audio ⑧g` 항진(`!(x*0.9 > x)`) 진짜 반례로 ④ 빠진 소리 전수 main 28 vs T354 문서 30 어긋남 원인 ⑤ 이웃 키 있는 갈래 둘 — 걷기/표면(밭 `farmland` · 실내 `floor` · 마당 `vtile` = 지면 3종 옆에 셋) · 환경(`thunder` · `snow` · `water_pool`) 확보(판정 불필요 — 이미 있는 키의 이웃) (세션9/SOUND · T387 뒤 · 2026-09-25)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T397.md` · 레포 `지시/지시_T397.md`)

---

T387(`batch/sfx-combat-0925` → main · 붙는 자리 5/11 · 확보 4키(`arrow_shoot`·`swing`×3·`downed`·`wake_up`) · **"맞음"은 11자리 어디에도 사건으로 없다**(`player_attacked` = 때리는 쪽 · `hp_changed` 에 `why` 없음 — 짐작으로 안 걸었다) · 재접속 `player_downed` 거름 · pid 는 존마다 p1 부터 · 헤드룸 `worstPeak` 가 T354 부터 리미터 낀 거짓 수였다고 스스로 정정(0.7957) · `e2e-aim` 15/0(PM 의 12/1 을 먼저 재현) · `files` m4a 짝 수리 · `test-audio` 145 · `e2e-audio-probe` 44) **통과** — "맞음은 11자리 어디에도 사건으로 없다"가 이 카드의 문장이다(족보 313). ★PM: 회부 1(`why` 한 칸)은 **서버 두 줄에 한해 세션9 에 위임**한다 — `zone.js` 의 `hp_changed` 방송 자리에서 `setHp` 가 이미 받는 `why` 를 전문에 싣는 것뿐(새 낱말 0 · 있는 낱말을 나른다) · 그 밖 서버 무접촉 그대로.

**카드 T397 — 다섯. PM 판정**(재민 거부권): ① **pid 충돌 실측** — T387 §4-8 은 코드로만 봤다: 하네스(`e2e-audio-probe` 문법)로 주 연결 + 이웃 존 관전 연결을 같이 열고, 이웃 존의 p1 이 쓰러지거나 HP 가 바뀔 때 **내 층이 우나**(내 몸 소리) · 재현 ○ 이면 `30-n-net.js` 세 자리(1326·1333·1371)가 **연결 역할**(주/관전 · 이미 있는 그 구분 — T387 이 오디오 층에서 쓴 것)을 보게 한 줄씩 · 재현 ✗ 면 왜(관전 연결이 그 메시지를 안 받나) 한 줄 · `30-n-net.js` 는 그 세 줄만(T379 포인터 캡처 무접촉). ② **`why` 한 칸** — `zone.js` `hp_changed` 방송에 `why`(`setHp(p, v, why)` 의 그 인자 · 없으면 빈 칸 · 두 줄) · 층 표 한 줄: `why` 가 다침 낱말이면 `hit_body`(확보 1 · 후보 2 · T354 규약) · 회복·먹기는 안 운다 · 자명 통과 금지: `why` 없는 옛 전문 미끼는 안 운다 · `e2e-audio-probe` 에 다침 호출 1 · 서버 낱말이 무엇인지는 코드가 이미 갖고 있다(`setHp` 호출부 전수 표 — 짓지 않는다). ③ **⑧g 항진** — `!(x*0.9 > x)` 를 실제 헤드룸 값(`_실측` 표의 그 수)으로 · 미끼(문턱 넘는 조합 하나)가 빨개진다. ④ **28 vs 30** — `scripts/sfx-census.js` 를 T354 시점(`batch/sfx-ear1-0922` 착지 해시)과 지금에 돌려 어느 두 자리가 사라졌나 · 자의 변화인지 자리의 변화인지 한 줄 · 문서 갱신. ⑤ **이웃 키 갈래 둘** — 걷기/표면: 지면 3종(돌·풀·흙) 옆에 **밭·실내 바닥·마당 타일** 셋(`farmland`·`floor`·`vtile` 이 표면 타일 동기 메시지다 — 지면 키를 고르는 그 훅(`step_*` 표)에 셋을 잇는다 · 확보 규약 T354 · 후보 1~2) · 환경: `thunder`·`snow` 는 `_sfxWx` 칸이 이미 있다(층이 읽기만 하면 된다 · `rain`/`rain_light` 문법 · 눈은 루프 · 천둥은 단발 · 이음새 자) · `water_pool`(고인 물 — `amb:water` 시냇물과 다른 소리 · 후보만). 이틀. 질문 금지 — 답은 표.

★검증 규약: 출처 없는 소리 0 · `bgm/` 무접촉(세션8 T391) · 서버 = `zone.js` `hp_changed` **두 줄만** · `30-n-net.js` = ① 세 줄만 · 기존 26키 비트 동일 · `test-audio` 145 + 새 절 · `test-assets-audit` PASS · `e2e-audio-probe` 44 + n · `e2e-aim` 15 · `test-client-globals` 26 · `test-harness-lint` 61 · 소리판 행 수 = 키·파일 수 · 접점 심볼(`hp_changed|setHp|why|player_respawn|player_down_state|myPid|30-n-net|48-a-audio|step_|farmland|floor|vtile|_sfxWx|thunder|snow|water_pool|sfx-census|test-audio ⑧g|sfx-board|CREDITS|_sfx_in`).

## 0. §0 실측 (다섯)
ⓐ pid 충돌 재현 ○/✗ + 세 줄. ⓑ `why` 두 줄 + `setHp` 호출부 표 + `hit_body` 확보. ⓒ ⑧g 반례. ⓓ 28 vs 30. ⓔ 갈래 둘 확보 표.

## 1. 절대 규칙
출처 없는 소리 0 · 서버 두 줄 · 새 낱말 0 · `Math.random` 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-hit-surface-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (다섯)
① pid ② `why` ③ ⑧g ④ 28/30 ⑤ 갈래 둘.

## 3. 검증
하네스 일곱 · 비트 동일 · 소리판 행 수 · 접점 목록.

## 4. 회부만
#60 둘째 귀 판정(재민 · 배포 뒤 · T354·T358·T387·이 카드 한 번에) · 남이 쓰러지는 소리(재민 · 서버 낱말) · 군대 전투 소리 `war_*`(재민 #60 우선순위) · `self_stat`(재민) · 녹음 회부(재민 폰 · 깨어나는 숨) · CC-BY 표시 둘(재민 · `/크레딧` #14ⓒ) · 어부 다섯 + 마을 배경 +3.88 dBFS(PM · T321).

## 5. 완료
커밋 [승인 대기 · T397] · 가지 푸시 · `보고/T397_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 pid ○/✗ · why 두 줄 · 확보 n**) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — pid 충돌 ○/✗ · why 두 줄 · hit_body 확보 · ⑧g 반례 · 28/30 = … · 표면 3 · 날씨 n". 실기: 재민 배포 뒤 소리판.
