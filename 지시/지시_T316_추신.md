# T316 추신 (세션3 · PM 2026-09-19 저녁) — 어부의 한 시도는 한 틱이 아니다

T321(세션9 · main)이 잡았다: NPC 어부의 한 시도는 **한 틱**에 종 추첨·셀 예산·장부가 다 끝난다 — 던짐→기다림→입질→걸림의 **순간이 없다**. 플레이어 낚시에는 그 창이 있다(`biteAt`·`windowMs` · `fish_state` 'wait'→'bite'). 캐논 "생산은 행위다" 에서 한 틱 결산은 추상이다.

**★PM 결정(위임): T316 에 한 항 더 — NPC 어부의 시도에 플레이어와 같은 창을 준다.** `biteAt`·`windowMs` 를 **같은 함수**로(사본 0 · 새 수 0 — 플레이어 값 그대로) · 한 시도 = 던짐 → 창 → 입질/안 물림 → 걸림/놓침 · 그러면 T321 이 결말에 건 소리 셋이 **순서**로 난다(세션9 `test-audio ⑫d` 전제가 바뀐다 — 그 절은 세션9 가 뒤에 고친다 · 무접촉). 첫날 등가(ⓓ①)는 **셀 예산**이 지키고, 창은 하루 시도 수를 정한다 — 그 둘이 어긋나면(창 때문에 예산을 다 못 잡는 마을) 표에 마을 수 · 새 수는 없다.

접점 심볼 추가: `biteAt|windowMs|fish_state|_lifeAct('드리움'|'놓침'|'낚음')`. `villages.js` 어부 자리는 T321 이 낱말 둘을 세웠다 — 리베이스 때 둘 다 살린다.
