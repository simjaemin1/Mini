# 구현 지시 — T375: 몸이 없는 전수 순회 넷의 문지기 — `stairs`·`spatial`·`fall`·`hpRegen` 이 주민 전수를 매 틱 훑으며 문자열 키 Map(`isPositionActive`·`stairCellCache`)을 묻는다(T371: 그 밖의 69% · 0.717µs/사람) ↔ `p.isNpc` 불리언 한 줄로 빠지는 순회 셋은 5.8배 싸다 · **문지기를 불리언 필드 하나로**(틱 초에 `isPositionActive` 결과를 한 번 계산해 `npc._active` 에 — T356 SoA 계약의 `active` 열 · 문자열 Set 에서 유도 · 사본 0 · 정본은 `activeChunkKeys` 그대로) · 순회 넷이 그 필드를 읽는다 · 손잡이 `T375_ACTIVE_FLAG` 기본 끔 · 끔 비트 동일 · 켬 게이트 4,200틱 좌표 비트 동일(T350 자 — 판정이 안 바뀌어야 한다) · 값: 그 밖 1.03 → ? (T371 자 · 같은 창) (세션7 · T371 뒤 · 2026-09-23)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T375.md` · 레포 `지시/지시_T375.md`)

---

T371(`batch/tick-rest-0923` → main · **'그 밖'에 이름 열일곱 — 사슬이라 틈 0 · 못 가른 것 0.001µs(0.1%)** · 가장 큰 것은 함수가 아니라 꼴: 몸이 안 도는 전수 순회 넷(`stairs` 0.238 · `spatial` 0.206 · `fall` 0.157 · `hpRegen` 0.115 = 0.717µs · 69.4%) — 같은 1,651명을 도는데 `p.isNpc` 한 줄로 빠지는 셋은 0.031 · 차이는 일이 아니라 **문지기가 무엇을 읽는가**(문자열 키 Map 대 불리언) · 이 판의 그 밖은 2.11 이 아니라 1.03(세계가 다르다 · 판을 넘어 견줄 수는 비율뿐) · T356 표의 행 합 −0.30 을 적어만 둠 · 제품 0줄) **통과** — "빠뜨린 갈래가 원리상 없게 사슬로 갈라라"가 이 카드의 문장이다(족보 295). T371 회부 1-ⓐ 가 이 카드다. 세션3 T370 이 `zone.js` 경로 조건을 만지는 중 — 이 카드는 **틱 머리 + 순회 넷의 문지기 줄**만.

**카드 T375 — 셋. PM 판정**(재민 거부권): ① **필드 하나** — 틱 머리(`head` 갈래 · `updateActiveChunks()` 직후)에서 주민·몹마다 `isPositionActive(x,y)` 를 **한 번** 계산해 `_active`(불리언)에 적는다 — 정본은 `activeChunkKeys`(문자열 Set) 그대로 · `_active` 는 **유도**(T333 이 `WATER_TILES` 에서 비트를 유도한 그 규율 · 사본이 아니라 그 틱의 캐시) · 그 틱 안에서 청크 집합이 안 바뀌므로 값이 같다(바뀌는 자리가 있으면 표에 · 그 자리 뒤에 다시 계산). ② **순회 넷이 그 필드를 읽는다** — `stepStairFor`·`rebuildSpatialIndex`(활성 청크 건물 부분은 그대로)·`processFalling`·HP 회복 순회의 **문지기 줄만** `_active` 로 · 판정·순서 무변(같은 사람이 같은 순서로 같은 일을 한다 — 게이트가 그걸 잰다) · `stairCellCache` 문자열 키는 이 카드에서 안 만진다(회부 1-ⓐ 둘째 · 표만) · 순회 넷을 한 바퀴로 합치기(1-ⓑ)도 **안 한다**(순서가 바뀌면 세계가 바뀔 수 있다 — 표만). 손잡이 `T375_ACTIVE_FLAG` 기본 끔. ③ **값** — T371 자(`t371-tick-rest` · 같은 창 · 같은 씨) 켬/끔: 넷 갈래 µs 전/후 · 그 밖 합 · 사람당 합 · 존 틱 p50 · **게이트: 켬/끔 4,200틱 좌표·HP·낙하 비트 동일**(T350 문법 · `test-move-soa` 절) · 켜지 않는다. ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: `sim/` 무접촉(3시드 면제) · 끔 비트 동일 · 켬 게이트 비트 동일(문지기가 값을 안 바꾼다는 것이 이 카드의 전부) · `test-move-soa` 59 + 절 · `test-war-world` 85 · `test-terrain-memo` 38 · `e2e-move` 37 · `lab-wiring-check` 실패 0 · 접점 심볼(`isPositionActive|activeChunkKeys|updateActiveChunks|stepStairFor|stairCellCache|rebuildSpatialIndex|roomsFlush|processFalling|_active|T375_ACTIVE_FLAG|t371-tick-rest`).

## 1. 절대 규칙
새 수 0 · 사본 0(정본은 `activeChunkKeys` · 필드는 그 틱의 유도) · 판정·순서 무변 · 손잡이 기본 끔 · **T370(세션3 `zone.js` 경로 조건 — 자리 다름 · 리베이스 때 둘 다)·T374(세션1)·T364(세션10) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/active-flag-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
`stairCellCache` 문자열 키(표 · PM) · 순회 넷 한 바퀴(순서 · PM) · `rebuildSpatialIndex` 통째 재생성 → 증분(PM · 다음) · T356 표 −0.30(기록).

## 5. 완료
커밋 [승인 대기 · T375] · 가지 푸시 · `보고/T375_<날짜>.md`(**머리 15줄에 넷 전/후 표**) · 채팅 "가지 올렸다 <해시> — 넷 0.717 → … µs · 게이트 동일 · 그 밖 1.03 → …". 실기 0.
