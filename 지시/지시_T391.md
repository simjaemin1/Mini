# 구현 지시 — T391: T386 이 잡은 "opt-in 밖에서 움직인 것" 셋을 되돌린다 — ① 정본 Python 굽기 기본값(붙은 음 자동 이음 20ms → 무표기면 재발음 · `render_jeongak.py`·`render_score.py`)을 **T353 정본 기본값으로**(재굽기 해시로 실증) ② `public/assets/audio/bgm/__pycache__/*.pyc` 4장 배포 폴더에서 제거 + `.gitignore` 한 줄 ③ **뱅크가 있다**(맥 `bgm/bk_*` 12묶음 — #64 ① 닫힘) → T353 이 뱅크 없어 못 한 셋(13곡 해시 재현 · `village_day_ari` A/B · `janggu_gung` 칸 실측)을 **이제 한다** (세션8/ART · T386 뒤 · 2026-09-25)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T391.md` · 레포 `지시/지시_T391.md`)

---

T386(`batch/bgm-rnd-review-0925` → main · 기본 경로 Web Audio 명령 12/12 같다(무음 버스 하나 말고) · 바이트 동일은 Chromium OAC 떨림(같은 판 두 번 −91.2dB)이라 못 잰다고 **정직하게** 적음 · 새 수 46 = 정본 30 · 유도 6 · 지어냄 10(전부 opt-in 뒤) · 설계 11장 후보/참고/버림 3/6/2 · 브금판 15행 · **⚠opt-in 밖에서 움직인 것 셋** · 뱅크는 있다) **통과** — "한 글자도 안 움직였다와 들리는 것이 안 바뀌었다는 다르다"가 이 카드의 문장이다(족보 312). ★PM: T386 은 판정 0 카드라 안 뺐고, 이 카드가 뺀다. GPT 의 Python 기본값 변경은 **카드 없는 정본 변경**이라 되돌리는 것이 규약이다(정본 하나 · 새 규칙은 카드로) — 그 규칙("무표기면 재발음")이 옳은지는 **따로** 재민 판정(#70 · T386 §회부 3).

**카드 T391 — 넷. PM 판정**(재민 거부권): ① **Python 기본값 되돌림** — `render_jeongak.py`·`render_score.py`(그리고 GPT 가 만진 `sampler.py`·`score_village_day.py` 도 표) 의 기본값을 착지 전 `450c6e44` 것으로 · GPT 규칙은 **옵션 플래그**(기본 끔)로 남긴다(지우지 않는다 — #70 이 켜면 한 글자) · 실증: 뱅크(③)로 `village_day_jeongak` 을 되돌린 기본으로 다시 구워 **배포 13곡 `.ogg` 해시와 같은지**(같으면 정본 복원 · 다르면 그 차가 발견 — 굽기가 결정적인지부터 표) · 되돌리기 전(GPT 기본)으로 구운 것과의 차 RMS 한 줄. ② **`.pyc` 4장** — `git rm --cached` + `.gitignore` 에 `__pycache__/` 한 줄 · `test-assets-audit` 가 `.pyc` 를 문다(자명 통과 금지: 미끼 `.pyc` 하나 두면 빨개진다). ③ **뱅크 셋** — T386 이 찾은 `bk_*` 12묶음(맥 위치 그대로 · 레포 밖 `~/Mini/_bgm_bank/` 로 **격리 복원** · 원본 무변)으로 T353 §회부의 셋: ⓐ 13곡 재굽기 해시 = 배포 해시 n/13 ⓑ `village_day_ari` A/B(`sigim()` 경로 켬/끔 — T353 이 "13곡 경로에 없다" 고 한 그 자리 · 해시 다르면 소리판 A/B 행) ⓒ `janggu_gung: '합성'` 칸 실측(뱅크에 장구 궁편 실음원이 있나 · 있으면 표만 · 값 안 고침 — T372 가 안 고친 그 자리). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: `bgm.js` 무변(T386 감사 결과 유지) · `variant:'phrase'` opt-in 무변 · 새 소리 0(재굽기는 정본 재현이지 새 소리가 아니다 — 해시가 다르면 **배포하지 않고** 표만) · `sfx/**` 무접촉(세션9) · `test-audio` 145 · `test-bgm-phrase` 16 · `test-assets-audit` PASS + `.pyc` 절 · `e2e-audio-probe` 44 · `test-client-globals` 26 · `test-harness-lint` 61 · 접점 심볼(`render_jeongak|render_score|sampler.py|score_village_day|__pycache__|.gitignore|test-assets-audit|bk_|_bgm_bank|sigim|village_day_ari|village_day_jeongak|janggu_gung|t386-bgm-hash`).

## 0. §0 실측 (넷)
ⓐ 기본값 되돌림 diff + 재굽기 해시 대조. ⓑ `.pyc` 제거 + 절. ⓒ 뱅크 셋 표(13곡 해시 n/13 · A/B · 장구). ⓓ 한 줄 답.

## 1. 절대 규칙
정본 하나(GPT 규칙은 옵션으로) · 값 무변 · 새 소리 0 · 뱅크 원본 무변(격리 복원) · `bgm.js` 무변 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/bgm-restore-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 기본값 ② `.pyc` ③ 뱅크 셋 ④ 한 줄 답.

## 3. 검증
해시 표 · 하네스 여섯 · 접점 목록.

## 4. 회부만
#70 무표기 재발음 규칙(재민) · #66 청취(재민) · 설계 11장 판정(재민·PM) · 체크포인트 라이선스(재민) · `e2e-bgm-phrase` ⑦⑧ 허용치(GPT 다음 카드) · AI Hub·4060(#64 ②③ 재민).

## 5. 완료
커밋 [승인 대기 · T391] · 가지 푸시 · `보고/T391_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 재굽기 해시 n/13**) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 기본값 복원 · 재굽기 해시 n/13 · .pyc 0 · A/B 행 n · 장구 ○/✗". 실기: 재민이 배포 뒤 브금판.
