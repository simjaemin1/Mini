# 구현 지시 — T395: 클라 프레임의 주인 — T384 가 마을·빈 초지에서 **세 팔 모두 330~376ms/f**(띠와 무관 · 순서 무관) 를 보고 "가르지 않았다"고 넘겼다 · 헤드리스 소프트웨어 렌더의 몫인지 제품의 몫인지부터(같은 자를 GPU 있는 맥 브라우저에서 — 재민이 실기에서 "느리다"고 한 적은 없다) · 층별 ms 표(지면 타일 · 자연물 · 개체 · 건물 · 안개 마스크 · HUD · 큰지도) · **주인 이름 하나** · 고침은 그 층 하나(손잡이 0 · 화소 동일 게이트) (세션4 · T383 뒤 · 2026-09-25)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T395.md` · 레포 `지시/지시_T395.md`)

---

T383(`batch/night-reds-0925` → main · 밤 빨강 둘 = 둘 다 자 · `test-terraincache` 26/0(T345 가 옮긴 한 칸을 자가 못 따라감 · 동작 게이트 1,000점 비트 동일 + 미끼) · `test-site-memo` 22/0(T298 이 헛수고를 세계에서 없앴다 — 씨앗에 제품 지문) · `nightly-known` 올릴 것 0 · 제품 0줄) **통과** — "밤 빨강 둘은 둘 다 자였다"가 이 카드의 문장이다(족보 306). ★PM: 세션3(T381)도 같은 `test-terraincache` 를 따로 고쳤고 결론이 같았다(귀속 T345) — 착지는 T383 판. 두 세션이 같은 자를 고친 것은 PM 의 카드 겹침이다(내 것).

이번 카드는 세션4 의 클라 구조 영역(T51·T53·T66)이다. T384 §5-3: 마을·빈 초지 상자에서 **render 330~376ms/f** — 숲 한복판(3.3~9.1ms)보다 **100배** 느리다. 띠·컬링 세 팔이 전부 같고 순서를 뒤집어도 같다 ⇒ 나무가 아니다. 헤드리스 Chromium 소프트웨어 렌더(컨테이너)의 값일 수 있고 제품 층 하나가 그 상자에서만 무거울 수도 있다 — **어느 쪽인지가 먼저**다.

**카드 T395 — 넷 · 순서가 카드다. PM 판정**(재민 거부권): ① **환경인가 제품인가** — T384 자(`scripts/t384-two-arms.js`)의 마을 상자를 그대로: ⓐ 컨테이너 헤드리스(소프트웨어) ⓑ 세션 자기 맥 Chromium(GPU · 헤드리스 아님 · 같은 URL) ⓒ 가능하면 맥 Safari(재민 브라우저 · 수만) — 세 상자 render ms 표 · ⓑ 가 16.7ms 안이면 "환경" 이고 ②는 표만 · ⓑ 도 무거우면 제품. ② **층별 ms** — `34-m-renderloop.js` 의 그리기 순서 그대로(지면 타일 · 자연물 `_natDraw` · 개체 · 건물 · 벽 · 안개 3단계 마스크 · 날씨 `37-r1-weather` · HUD · 큰지도 · 이름표) 층마다 `performance.now()` 차 — **이미 있는 자(`_natMs`·`_farMs` 문법)를 층마다** · 마을·초지·숲 셋 × ⓐⓑ · 표 한 장 · 합이 프레임에 닿는지(못 가른 나머지 = 한 줄) · 주인 이름 하나. ③ **고침** — ②의 주인 층 **하나만** · 화소 동일 게이트(`@pixel` 자 — `e2e-fogray` 18 · `test-far-trees` 문법 · 고친 뒤 같은 씨 같은 자리 화소 diff 0) · 손잡이 0 · 값 전/후 ⓐⓑ · 주인이 안개 마스크면 **규격 무변**(3단계 · 지면 데코는 마스크 앞 — 계약 그대로 · 계산 방법만) · 주인이 환경이면 ③은 "없음" 한 줄. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 화소 동일(`@pixel`) · 행동 변경 0 · 서버 무접촉 · `30-n-net.js`(T379)·`38-r1-fartree.js`·`VIEW_RADIUS`(세션5 T392) 무접촉 · `test-client-globals` 26 · `e2e-move` 37 · `e2e-fogray` 18 · `e2e-weather` 31 · `test-far-trees` 17 · `test-harness-lint` 61 · 접점 심볼(`34-m-renderloop|_natMs|_farMs|_natDraw|drawTreeIso|37-r1-weather|80-bigmap|t384-two-arms|e2e-fogray|performance.now`).

## 0. §0 실측 (넷)
ⓐ 세 상자 render ms(환경/제품). ⓑ 층별 ms 표(셋 × 둘 · 주인). ⓒ 고침 diff + 화소 diff 0 + 전/후. ⓓ 한 줄 답.

## 1. 절대 규칙
층 하나만 · 화소 동일 · 손잡이 0 · 새 수 0 · **T392(세션5)·T387 후속(세션9 오디오) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/frame-owner-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 환경/제품 ② 층별 ms ③ 고침 ④ 한 줄 답.

## 3. 검증
화소 diff 0 · 하네스 여섯 · 접점 목록.

## 4. 회부만
Safari 실기(재민) · #54(재민 · T376) · 다음 층(PM · ② 표 뒤).

## 5. 완료
커밋 [승인 대기 · T395] · 가지 푸시 · `보고/T395_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 환경/제품 + 주인 이름 + 전/후**) · `인계/R2-개체렌더.md`(또는 `R1-지형렌더.md`) 갱신 · 채팅 "가지 올렸다 <해시> — 330ms = 환경/제품 · 주인 … · 마을 … → … ms(맥) · 화소 diff 0". 실기: 재민 배포 뒤 마을에서 부드러운지.
