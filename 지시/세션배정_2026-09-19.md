# 세션 배정 — 2026-09-19 (PM · main `88ba70ad` · **낮 갱신 T313**: 세션1·2·3·5·7·8·11)

각 세션 채팅에 **아래 한 줄**만 붙여넣으면 된다. 카드 전문은 `~/Mini/지시_T◯◯◯.md` 와 레포 `지시/지시_T◯◯◯.md`(정본 · 세션이 `git pull --ff-only origin main` 뒤 읽는다).

| 세션 | 모델 | 카드 | 붙여넣을 한 줄 |
|---|---|---|---|
| 세션1 | Opus | **T311** | `git pull --ff-only origin main` 뒤 `지시/지시_T311.md` — T297 자 결함 둘(고기 3분의 1 누수 `huntIncomeReal`↔`_t172mul` · 관찰 모드 채집꾼·어부 안 뽑힘) 표로 갈라 자 결함이면 고친다 · `batch/field-even-0913` 넷째 판 리베이스 + 끔 비트 동일 + 캡×짚 4팔. |
| 세션2 | Opus | **T314** | `git pull --ff-only origin main` 뒤 `지시/지시_T314.md` — 야간 창 e2e 46종 미측정 원인(`nightly-split.sh` A/B/C 09-19 왜 안 갈랐나) · e2e 48종 세 밤 전부 재는 판 · 부하 거짓 빨강 넷(conn·fogray·mtfuzz·trade) 두 밤 대조 · 벽시계 단정 전수 감사. |
| 세션3 | Opus | **T312** | `git pull --ff-only origin main` 뒤 `지시/지시_T312.md` — 어부를 NPC 인벤토리로 옮긴다(플레이어 꼴 · 사본 0) · 강가 셀 낚기→손에 들고 곳간 · `addProduce('fish')` 그 마을에서 제거 · 손잡이 `T312_FISH_ACT` 기본 끔. |
| 세션4 | Opus | T298 | `git pull --ff-only origin main` 뒤 `지시/지시_T298.md` — 영토+방아쇠 기본 켬 · 되돌림 비트 동일 · 다섯째 판 3시드 · `_mapBeds` 표. |
| 세션5 | Opus | **T309** | `git pull --ff-only origin main` 뒤 `지시/지시_T309.md` — 청크 경계로 가르는 코드 전수 감사(코드 0) · `treeBlockerAt` 메모 자리 표(T295/세션10 이 붙일 세 줄 + 메모) · 관측자 없는 나무꾼·광부 현장 후보 표(`resourceAt` 반경16 · 51마을 전수 · 코드 0). |
| 세션6 | Opus | T299 | `git pull --ff-only origin main` 뒤 `지시/지시_T299.md` — `twogate` 기본 켬(T255 ⓐ) · 관문 0 · 여섯째 판 3시드 · **T298 착지 뒤 리베이스**. |
| 세션7 | Opus | **T310** | `git pull --ff-only origin main` 뒤 `지시/지시_T310.md` — 배포 뒤 확인 표 #27(`/check_username` #2 · `?as=` #9 · 문 62 `test-doors` 실서버 팔 · `/perf` 한 줄) · `인계/E-사건장부.md` 15벽→10 정정 · 남은 열 열개 '없는 동사' 자리 표(코드 0). |
| 세션8 | Opus | **T308** | `git pull --ff-only origin main` 뒤 `지시/지시_T308.md` — 화소 해시 자 정규화(완전투명 아래 RGB 0 고정) · 전수 재잠금 557 · 반례 하네스(무손실 재압축 effort 0/4/6 동일 해시 · 보이는 화소差 다른 해시). |
| 세션9 | Opus | **T305(새)** | `git pull --ff-only origin main` 뒤 `지시/지시_T305.md` — `bgm.js` 곡선 정정(+3.15 dB)·볼륨 유도 · 서버 동사 두 자리(`sendInventory` `where` · `doEat`) · `__sfx.probe()` 상시 절 · 정악 자리 표. |
| 세션10 | Opus · 다른 기기·계정 | T295 | `git pull --ff-only origin main` 뒤 `지시/지시_T295.md` — 동원의 대가 · pid↔econ 결속 · 군량 규모 비례·환급 한 곳 · 품목 = 섭식 정본 함수 · 부하 두 팔. (푸시 403 → 번들을 채팅에) |
| 세션11 | **Sonnet** | **T313** | `git pull --ff-only origin main` 뒤 `지시/지시_T313.md` — [Sonnet] `test-assets-audit` measured 열 읽기(자리 없으면 표만) · `render-meta.json` 합성기 열 `_synth` 개명 · 공통§6 보고머리15줄 규약 한 줄 · 배정표 갱신 · 세션10 전달문(`지시_T295_추신.md`) · `_MANIFEST.txt` 정리. 판정 0 · 코드 0. |
| 세션12 | **Sonnet** · 새 | **T307(새)** | `git pull --ff-only origin main` 뒤 `지시/지시_T307.md` — [Sonnet] main 에서 넷째 판 3시드 재현(시드 42) · 15시드 판 한 벌 · `war-world-perf` W0/W6 두 판. 표만 · 판정 PM. |

* 세션11·12 첫 부트스트랩: 공통 §0 은 `sed -n 1,130p 인계/공통.md` · 족보는 `tail -n 12 인계/족보.md` — 전문 읽기 금지(토큰 규약).
* 순서 의존: T299(세션6) 는 T298(세션4) 착지 뒤 · T305(세션9) 서버 두 자리는 T302(세션7)·T298(세션4) 의 `zone.js` 접점과 리베이스 때 둘 다 살린다.
* 세션13(하네스·야간·CI · Sonnet)은 아직 안 만든다 — T304(세션2)·T293 이 그 자리를 덮고 있다.
* **[T313 낮 갱신 · 2026-09-19]** 세션4·6·9·10·12·13 은 그대로 뒀다 — 정본 보드 `~/Mini/태스크보드_v1.md` §5 `grep "진행("` 로 대조하려 했으나 **이 기기엔 그 파일이 없다**(디바이스 파일 · 이 세션은 다른 기기·계정). PM 이 그 보드로 직접 대조해 주길 회부한다 — 이 표는 이전 세션배정 값을 그대로 옮긴 것뿐이다.
* **[참고]** main 은 이 갱신 시점에 이미 `5e678633`(T323 착지 · "밤" 배치까지 진행)까지 나가 있었다 — 이 표는 카드 T313 이 지정한 **낮 구간(세션1·2·3·5·7·8·11)만** 갱신했다(밤 구간 배정은 이 카드 범위 밖).
