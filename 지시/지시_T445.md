# 구현 지시 — T445: 교전이 무음이다 + 같은 창 같은 키 겹침 — T431 §6: 픽스처 전쟁이 교전·궤주까지 갔는데 병사 몸싸움은 `war_battle`(2Hz 집계 · 무게중심 좌표)로만 와서 소리 층에 안 닿는다(T387 회부 `war_*` 그대로) · §1: 늑대 둘이 한 창(200ms)에 물어 `hit_body`×2 = 0.8917(문턱 밖 · 리미터 0.7634 · 클리핑 0 · 11창) ⇒ ① **같은 창 같은 키는 한 번**(T412 "두 번 울지 않는다" 문법 · 같은 자리 반경 안 · 새 수 0 — 두 늑대는 두 사건이니 **가리지 않고 겹침으로 두는가**가 물음: 표만 · ★PM: 사건 둘 = 소리 둘이 캐논 · 리미터가 잡는다 · 그대로) ② **교전 소리 자리 표** — 서버가 병사 몸싸움에서 내는 메시지 전수(`war_battle` 집계뿐인가 · 몸 단위 타격은 `damagePlayer` 를 타나 · NPC 병사끼리는?) · 소리를 붙이려면 어느 **한 메시지**가 필요한가(새 네트워크 필드 0 규약 — 있는 `hit_body`·`arrow` 사건을 병사 몸이 이미 내나) · 세션10 회부 한 줄 ③ `CHAR_SPRITE=on` 규약(자 머리 + `인계/SOUND`) ④ 한 줄 답 (세션9 · T431 뒤 · 2026-09-27)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T445.md` · 레포 `지시/지시_T445.md`)

---

T431(`batch/sfx-cooccur2-0926` → main · 발자국 ○(`CHAR_SPRITE` env 였다 · 헤드리스 탓 아님) · 판 다섯 · **짐승 판 0.8917 · 11창**(`hit_body`×2 + 비 + 발 + 바람 + 늑대) · 리미터 0.7634 · 클리핑 0 · `worstCombo` 갱신 · 문턱 0.8 무변 · 반경 전수 63 · `test-audio ⑧c` 두 갈래 · **주민 소리 0**(팔 넷 켜도 어촌 곁 30분 0 · 나무꾼 0명 — T432 청크 문) · **교전 무음**) **통과** — "문턱은 짐승 판에서 넘었고 리미터가 잡았다 — 그리고 주민과 교전은 아직 소리가 없다"가 이 카드의 문장이다(족보 362). ★PM: 두 늑대 = 두 사건 = 소리 둘(가리지 않는다 · 리미터가 그 자리) · 주민 소리는 T432(세션1 청크 문) 뒤 재실측 · 교전은 **자리 표**부터.

**카드 T445 — 넷. PM 판정**(재민 거부권): ① **겹침 표** — 같은 창(200ms) 같은 키가 두 사건(다른 몸)인 창 수 · 같은 사건이 두 번 부른 창 수(결함 — T412 "수신 머리의 직전 값" 문법으로 가른다) · 결함이면 고침(새 수 0) · 사건 둘이면 그대로 + 리미터가 무는 창 수(11) · 재민 귀 실기 한 줄(#60). ② **교전 소리 자리 표** — 서버 `war-core`·`zone.js` 가 병사 몸싸움에서 내는 메시지 전수(이름 · 빈도 · 좌표 있나) · 병사 몸의 타격이 `damagePlayer(…, source)` 를 타나(플레이어 병사 · NPC 병사 · NPC 끼리) · `hit_body`·`arrow`·`downed` 가 병사 몸에서 **이미** 오는지(오면 소리 자리는 있다 — 왜 T431 교전 판에서 0 이었나) · 없으면 필요한 한 메시지(있는 사건 하나 · 새 필드 0)를 표로 · 세션10 회부 한 줄(구현은 세션10). ③ **규약** — `sfx-cooccur.js` 머리에 `CHAR_SPRITE=on` 필수 · `인계/SOUND-소리.md` 한 줄 · e2e-audio-probe 에 짐승 판 절(늑대 둘 창 · 리미터 클리핑 0 · 미끼: 리미터 끄면 클리핑 > 0). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 새 소리 0 · 문턱 무변 · 서버 무접촉(표만 · 구현은 세션10) · `bgm/`·`tools/` 무접촉 · **T441(세션10 `war-core`) 무접촉** · `test-audio` 180+ · `e2e-audio-probe` 76 + 절 · `e2e-aim` 15 · `e2e-pid-collide` 13 · `test-war-world` 116(읽기만) · `test-client-globals` 26 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`sfx-cooccur|worstCombo|overKnee|kneeHolds|hit_body|war_battle|damagePlayer|hpWhy|CHAR_SPRITE|drawCharSprite|__sfx.step|limiter|48-a-audio`).

## 0. §0 실측 (넷)
ⓐ 겹침 표(사건 둘/결함). ⓑ 교전 메시지 전수 + 한 메시지. ⓒ 규약 줄 + 절. ⓓ 한 줄 답.

## 1. 절대 규칙
새 소리 0 · 서버 0 · 문턱 무변 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-war-slots-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 겹침 ② 교전 자리 ③ 규약 ④ 한 줄 답.

## 3. 검증
하네스 일곱 · 미끼 · 접점 목록.

## 4. 회부만
교전 한 메시지(세션10 · PM 카드) · 주민 소리 재실측(PM · T432 뒤) · #60(재민 · 늑대 떼 귀).

## 5. 완료
커밋 [승인 대기 · T445] · 가지 푸시 · `보고/T445_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 겹침 창 n(결함 n) · 교전 메시지 n종(몸 타격 ○/✗) · 필요한 한 메시지 = … · CHAR_SPRITE 규약". 실기: 재민 #60.
