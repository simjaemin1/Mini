# T410 추신 — 세션1 · idle 문을 열 때 같이 봐야 할 것 하나 (2026-09-26 · PM)

`~/Mini/지시_T410.md` 본문은 그대로다. 아래 한 절만 ① 문에 더한다.

---

**T398(세션2) §1-5 가 잡았다**: 사람·관측자가 0 이 되면 존 틱이 `updateActiveChunks` **앞에서** 돌아가므로, 마지막 사람이 나간 자리의 청크가 **켜진 채로 · 켤 때의 단계로** 남는다(T398 둘째 판이 그걸로 "다시 띄운 뒤 묘목 452 → 성목"을 헛셌다 — 재부팅 효과가 아니라 idle 스킵이었다). 제품에서 무엇이 틀어지는지는 T398 이 안 쟀다.

T410 ① 에 더할 것(표 한 칸 · 새 수 0):
- 문을 열면(`ZONE_IDLE_SKIP=0`) 이 병은 저절로 없어지나 — `updateActiveChunks` 가 관측자 0 에서 **매 틱** 돌아 마지막 사람 자리의 청크를 끄나(끄면 T316 `_t316WalkAlways` 문 둘이 마을 청크를 다시 켜나 — "마을 청크는 몸이 있으면 켜진다"가 정본 · 표).
- 종전 비트(`=1`)에서는 그 청크가 켜진 채 남는다는 것을 **자로 한 번 밟아** 적는다(T398 자 `t398-regrow-recut.js` 둘째 판 문법 그대로 · 관측자 뺀 뒤 그 마을 청크 활성 여부 · 개체 단계 시계) — 되돌림 값에 병이 남아 있음을 표에 남긴다(고치지 않는다 · 되돌림은 종전 비트 동일이 정본).
- 열었을 때 "켜진 채 남은 청크" 수 = 0 인지(존 하나 · 사람 나간 뒤 60틱).

회부 칸에 한 줄: **T398 §1-5 idle 스킵 청크 잔류 → T410 ① 이 닫음 ○/✗**.

나머지(②③④ · 검증 규약 · 가지 `batch/zone-awake-0926` · T385 뒤 리베이스 — T385 는 main 에 있다(`16f8ccf0` · 착지 `7b7d4b90`) · T421(세션7)이 그 위에서 켠다)는 본문 그대로.
