# 구현 지시 — T432: 마을 청크는 몸이 있으면 켜진다 — T410 이 idle 문을 열었더니 **기본 손잡이에선 주민이 한 걸음도 안 걷는다**(걸음 0 → 0): 막는 것은 이제 **청크 문 둘**(`updateActiveChunks` 가 사람·관측자 둘레 1,200px 만 켜고 · 주민 결정·이동 문은 활성 청크 안의 몸만) ⇒ ★PM 결정(위임 · 캐논 ⓑ① "관측자 없어도 실걸음"의 나머지 반): **주민 자리도 청크를 켠다** — 손잡이 `T432_BODY_CHUNKS`(기본 **끔** · 값이 카드다: 빈 한반도 51마을 청크가 늘 살면 시드 자원 개체·몹·야생이 마을 둘레에서 늘 산다 — 그 값(틱 p50·p95 · 사람당 µs · 활성 청크 수 · 메모리)이 캐논 3.3µs 안이면 켬을 PM 이 닫고 · 넘으면 재민) · 그리고 **T368 B 코드 정리**(문이 열린 세계에선 "몸 XOR 일괄"이 필요 없다 — 되돌림 `ZONE_IDLE_SKIP=1` 에서만 XOR) (세션1 · T410 뒤 · 2026-09-26)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T432.md` · 레포 `지시/지시_T432.md`)

---

T410(`batch/zone-awake-0926` → main · idle 문 열림(`ZONE_IDLE_SKIP` 기본 끔 · `=1` 종전 비트 동일) · 판정 하나 `zoneAwake` 를 틱과 생활층이 같이 봄 · 빈 한반도 CPU 0.64 → 5.94% · p50 1.24ms · 사람당 0.93µs(캐논 28%) · 26존 동시 합 11.6 → 45.2% · 깨어 있는 존 p95 +0.72ms · 행위 넷 켬 걸음 0 → 241만/분 · 일괄 0 · 곳간 곡선 관측자 0 ↔ 1 같음 · **행위 넷 켬 = 4.05µs(캐논 123%)** · `test-farm-act` 63 · `test-nippon-boot` 42) **통과** — "문을 열어도 기본 손잡이의 주민은 청크 문 둘에 선다"가 이 카드의 문장이다(족보 348). ★PM: 청크 문은 캐논의 자리라 PM 이 판정한다(주민 자리도 켠다) · **행위 넷의 4.05µs 는 값**이라 재민(#79) — 이 카드는 그 값의 **분해**(청크 문 값 대 행위 값)를 낸다.

**카드 T432 — 넷. ★PM 결정(위임 · 재민 거부권)**: ① **청크 문** — `updateActiveChunks` 가 사람·관측자 둘레에 **더해** 주민 몸이 선 청크(T410 청크 표의 "주민 자리")를 켠다 · 손잡이 `T432_BODY_CHUNKS`(기본 끔 · 켬은 이 카드 값 뒤 PM) · 켤 때 무엇이 사나 표(시드 자원 개체 · 몹 AI · 야생 · 공간 인덱스 · 낙하 · HP 회복 · `sepNpcs` — T410 §0-ⓑ 표에 열 하나 더) · **몸이 없는 청크는 그대로 끈다**(마을 사이 빈 땅 · 게이트: 활성 청크 수 = 몸이 선 청크 수 + 둘레) · 청크가 켜질 때 시드 자원이 despawn/spawn 을 되풀이하지 않나(T309/T317 규약 · 몸이 움직여 청크가 바뀌면 지난 청크는 언제 꺼지나 — 히스테리시스는 있는 문법으로 · 새 수 0). ② **값 분해** — 빈 한반도(1,329몸 · 관측자 0) 네 팔: ⓐ 종전(T410 · 청크 ∅) ⓑ 청크 켬 · 행위 끔 ⓒ 청크 끔 · 행위 넷 켬(T410 4.05) ⓓ 청크 켬 · 행위 넷 켬 — 틱 p50·p95(낮·밤) · 사람당 µs · CPU% · 활성 청크 수 · 시드 개체 수 · 몹 수 · RSS(자의 규약: 부팅 뒤 첫 하루 경계 뒤 · GC 뒤 — T410 §2-⓪) · **4.05 의 주인**이 걷기인지 행위 본체인지 청크인지 한 줄 · 캐논 3.3 에 닿는 팔이 어느 것인지. ③ **T368 B 정리** — `villages.js` 헤드리스 농부 절의 "몸 XOR 일괄" 갈래: 문이 열린 세계(`zoneAwake` 늘 참)에선 몸 하나 · 되돌림 `=1` 에서만 XOR — 코드로 **한 갈래**(`zoneAwake()` 를 묻는 그 자리 하나 · 사본 0 · 새 술어 0) · 되돌림 비트 동일 · `test-farm-act` ⑤ 그대로 통과 · 26존 동시 판 재실측 한 줄(청크 켬 팔 · T410 자 그대로). ④ 한 줄 답 + 켬 권고(PM 이 닫을 근거: 청크 켬 · 행위 끔 팔이 캐논 안이면 켬). 사흘. 질문 금지 — 답은 표.

★검증 규약: 새 수 0 · 손잡이 하나(되돌림) · 끔 = T410 착지본 비트 동일(`test-move-soa` 158 · 청크 집합 해시) · **T421(세션7 · `zone.js` spatial · 착지됨 — 그 위에서)** · **T426(세션2)·T427(세션3)·T428(세션5) 무접촉** · 3시드 면제 근거 한 줄(존 틱 · econ 무접촉 — `villages.js` 를 만지면 3시드 돌린다) · `test-farm-act` 63 · `test-fish-act` 73 · `test-wood-act` 106 · `test-forage-act` 137 · `test-build-act` 51 · `e2e-village` · `e2e-move` 37 · `test-nippon-boot` 42 · `test-jungwon-boot` 38 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`updateActiveChunks|CHUNK_ACTIVE_RADIUS|activeChunkKeys|isPositionActive|_t316WalkAlways|zoneAwake|ZONE_IDLE_SKIP|T432_BODY_CHUNKS|_t368ZoneAwake|t410-zone-awake|t356-tick-anatomy|canadiaVillage`).

## 0. §0 실측 (넷)
ⓐ 청크 문 diff + 무엇이 사나 표 + 히스테리시스. ⓑ 값 분해 네 팔 + 4.05 의 주인. ⓒ T368 B 한 갈래 + 26존 재실측. ⓓ 한 줄 답 + 켬 권고.

## 1. 절대 규칙
새 수 0 · 손잡이 하나 · 기본 끔(켬은 PM) · `zone.js` 청크 문 자리 + `villages.js` B 갈래만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/body-chunks-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 긴 창은 `setsid nohup … & disown`)**.

## 2. 작업 (넷)
① 청크 문 ② 값 분해 ③ B 정리 ④ 한 줄 답.

## 3. 검증
비트 동일 · 하네스 열 · 접점 목록.

## 4. 회부만
#79 행위 넷 값(재민 · 4.05 > 3.3 — 이 카드 ②가 분해) · 26존 배치 꼬리(재민 · 플랫폼) · canadia 레거시 걷기(PM · 청크 문 우회 갈래 — 이 카드 뒤 지우기 카드) · #69·#75(재민).

## 5. 완료
커밋 [승인 대기 · T432] · 가지 푸시 · `보고/T432_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — 청크 켬 · 행위 끔 사람당 … µs · 넷 켬 … µs(주인 = …) · 활성 청크 n · B 한 갈래 · 되돌림 동일 ○". 실기 0.
