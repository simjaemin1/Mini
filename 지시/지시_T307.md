# [Sonnet] 지시 — T307: 계측 러너 첫 카드 — main `88ba70ad` 에서 넷째 판 기준선 3시드 재현(시드 42 포함) · 15시드 판(`t17-seeds`) 한 벌 · `war-world-perf` W0/W6 T295 전 기준 표 — 표만 · 판정 PM (세션12 · 새 세션 · 2026-09-19)

아래 전문을 **세션12** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T307.md` · 레포 `지시/지시_T307.md`) **이 카드는 `[Sonnet]` 카드다 — 판정 0 · 새 코드 0 · 정본 무접촉 · 답은 표와 파일.**

---

**먼저 읽는다(부분만 · 전문 금지):** `인계/공통.md` §0(`sed -n 1,130p`) · §2 토큰 규약 · 기준선 절(`grep -n "넷째 판" 인계/공통.md` 그 줄 ±10) · `인계/족보.md` `tail -n 12`. 너는 **세션12 = 계측 러너** 자리다(공통 §0 모델 규약): 정해진 계측기를 정해진 시드·팔로 돌려 **표를 낸다**. 해석·판정은 PM 이 한다 — "가름/못 가름" 문장은 `t17-seeds` 가 찍는 것만 옮기고 네가 짓지 않는다. 질문 금지 — 답은 표. **★토큰 규약**: 판마다 출력을 파일로(`> /tmp/t307/<이름>.log 2>&1`) · `nohup … &` 로 띄우고 **5분 이상** 자고 `tail -n 3` 만 본다(30초마다 보지 않는다 · 판당 ~70초 · 15시드 30판 ≈ 35분).

**카드 T307 — 셋.** ① **넷째 판 기준선 재현** — `git rev-parse origin/main` 을 적고(카드 시점 `88ba70ad` · 달라졌으면 그 해시) 3시드(1020·7·42) 800일 51마을 `node scripts/t17-metrics.js 800 <시드>`(`LAB_SEEDCACHE`·`T17_JSON` 문법은 공통 기준선 절 그대로) → 인구·소멸 여덟 수를 공통 §2 기준선 표(넷째 판 8,318 / 7,935 / 7,853 · 소멸 0·0·0)와 **나란히** · 같음/다름만(다르면 어느 수가 얼마나 · JSON 을 `보고/` 옆 `계측/T307_seed<n>.json` 에 남긴다 · `cmp` 결과). PM 은 09-18 에 시드 1020·7 만 재실측했다 — **시드 42 가 이 카드의 첫 답**이다. ② **15시드 판 한 벌** — `node scripts/t17-seeds.js`(T277 러너 · 옵션은 파일 머리 주석대로 · 기본 팔) 를 main 에서 한 번 돌려 짝 Δ 표와 러너가 찍은 규약 문장을 **그대로** 옮긴다(T298 다섯째 판 · T299 여섯째 판이 올 때 대조할 "지금 main" 표) · 벽시계. ③ **`war-world-perf` W0/W6** — `ARMS=W0,W6 node scripts/war-world-perf.js`(기본 WARM_DAYS·DAY_MS·WINDOW_S 그대로 · 시드 1020) 존 틱 p50/p95 표 → T284 보고 표(`grep -n "p50" 보고/T284_2026-09-16.md`)와 나란히 · **T295(세션10) 착지 전 기준**임을 머리에 · 두 번 돌려 두 판의 p50 차이(기계 잡음 바닥 · 족보 80). ④ 한 줄 답("3시드 42 = … · 15시드 러너 문장 … · W0/W6 p50 …/… ms · 잡음 …").

★검증 규약: **코드 0**(`계측/*.json` + `보고/` 한 파일만 · `git diff --stat origin/main`) · 계측기는 러너에 안 넣는다(`@regress` 표 없음 그대로) · 컨테이너에서 돌린다(맥 VM 아님 · 2코어 — **동시에 하나만** · `ps` 로 확인 · `pkill -f` 금지 `kill <pid>`) · 시드 목록·해시·벽시계를 표 머리에.

## 0. §0 실측 (넷)
ⓐ 3시드 여덟 수 ↔ 넷째 판 표. ⓑ 15시드 짝 Δ 표 + 러너 문장. ⓒ W0/W6 p50/p95 두 판 ↔ T284. ⓓ 한 줄 답.

## 1. 절대 규칙
판정 0 · 새 코드 0 · `sim/`·`server/`·`lab/`·`scripts/` 무접촉(env·인자만) · 통로 클론 무접촉 · 배포 0 · ★푸시 2판(맥 VM `device_bash` · `$HOME/pushrepo` · 가지 `batch/measure-0919` **가지만** · main 은 PM · 403 이면 `~/Mini/_inbox/` 번들) · force 0 · PAT 출력 금지(`M` 마스크) · 동시 실행 하나.

## 2. 작업 (넷)
① 3시드 ② 15시드 ③ W0/W6 두 판 ④ 한 줄 답.

## 3. 검증
diff 파일만 · 해시·시드·벽시계 표 · `cmp` 결과.

## 4. 회부만
시드 42 가 다르면(PM · 어느 카드 접점인지는 PM 이 본다) · 15시드 러너가 못 돌면 그 오류 전문 `tail -n 20` 만.

## 5. 완료
커밋 [승인 대기 · T307] · 가지 푸시 · `보고/T307_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 한 줄 답**) · 채팅 "가지 올렸다 <해시> — 42 = … · 15시드 … · W0/W6 …". 실기 0.
