# 구현 지시 — T352: 걸음 루프 C 커널 — 주민 SoA 배열 계약(초안 · 세션3 SoA 카드가 그대로 받는다) + **같은 걸음 문**을 WASM(clang wasm32) · N-API 두 판으로 구워 **같은 스냅샷 1,000틱 좌표 바이트 동일** 게이트 아래 사람당 µs 실측(JS 현 루프 · JS SoA 초안 · WASM · N-API 네 팔 · 1,464명 · 10,000명) · 제품 무접촉 · 계측기만 — #53(2층)의 입력 (세션7 · T350 과 병렬 · 2026-09-22)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T352.md` · 레포 `지시/지시_T352.md`)

---

T345(세션3 · main `3d8a8cdb` · 걸음당 쓰레기 27개 중 넷 제거 · GC −27% · 사람당 5.48µs · 10,000명 46.4ms = 예산 1.39배) 의 답은 **"10배는 할당에 없었다 — 주인은 걸음당 호출 수(지형 6.43 + 벽 5.39 = 11.8회)와 주민 객체 1,464개의 포인터 순회"** 다. 재민 09-22: **"난 예산 턱걸이가 아니라 정말 바닥까지 찍어보고 싶다"** · "C 안 쓰고?" → PM: 순서다 — T350(주사위 0 · 게이트) → SoA(세션3 · JS · 나흘) → **그 배열 위에 걸음 본체만 C**. 이 카드는 그 셋째 층을 **미리 재서** SoA 가 끝나는 날 C 층이 바로 붙게 한다. 재민 확정: "그러자".

**카드 T352 — 넷. PM 판정**(재민 거부권): ① **SoA 배열 계약 초안** — `npcStep`(`zone.js:2576`) · `movePlayerStep`(`:11397`) · `isBlockedByWall`(`:11035`) · `isTerrainBlockedLocal`(`:827`) · `treeBlockerAt`(`:11000`) · `isPositionActive`(`:422`) 가 **걸음 안에서 실제로 읽고 쓰는 `npc` 필드 전부**를 정적으로 뽑아(읽기/쓰기/형 · 자리 줄 번호) 열 목록으로 — `x y targetX targetY speed floor behavior villageId chunkActive …` 각 열의 typed array 종(`Float64Array`/`Float32Array`/`Int32Array`/`Uint8Array` · 정밀도 근거: 좌표는 JS `number` 와 **바이트 동일**이어야 하니 `Float64Array` 가 기본 · 줄이는 건 근거 있을 때만) · 주민 인덱스 = `pid` 가 아니라 **조밀 슬롯**(생사 교체 규칙 한 줄) · `설계/설계_주민_SoA_계약.md` **초안**(정본 확정은 세션3 SoA 카드 · 이 카드는 초안이라고 머리에 적는다). 지형·벽·나무 쪽 입력도 계약에: 지형 = T333 이 미리 구운 **셀당 비트**(`terrain-tilecache.js` `Uint8Array` 메모 · 그대로) · 벽 = 건물 격자 · 나무 = 셀 경계 넘을 때만 묻는 규칙(T345 §4). ② **커널** — 걸음 문 하나(목표를 향한 한 걸음 · 지형 비트 · 벽 격자 · 나무 · 청크 활성 — `decideNpcBehavior` 는 **안 옮긴다**: 결정은 JS 에 남고 커널은 "결정된 목표로 한 걸음" 뿐)를 **C 한 파일** `bench/walk-kernel/walk.c` 로 · 산술은 JS 와 **같은 식·같은 순서**(`Math.floor` = `floor` · `Math.hypot` 자리는 `hypot` — 마지막 비트가 다르면 표에 적고 JS 쪽 식을 따른다) · 두 판으로 굽는다: **WASM**(`clang --target=wasm32 -O3 -nostdlib` · 컨테이너에 `/usr/bin/clang` 있다 · 메모리는 JS 가 넘긴 typed array 를 **공유**(복사 0 · `WebAssembly.Memory` 위에 열을 놓고 JS 도 그 뷰를 쓴다)) · **N-API**(`node-gyp` · `napi_create_external_arraybuffer` 로 같은 배열 공유 · 툴체인이 없으면 **없다고 표에**(맥 VM 과 컨테이너 둘 다 `which` 결과) 하고 WASM 한 판만 · 억지로 안 깐다). 빌드 스크립트 `bench/walk-kernel/build.sh` 하나 · **산출물(`.wasm`·`.node`) 커밋 0**. ③ **실측 네 팔 · 게이트 아래** — 입력 = 실제 부팅에서 얼린 **주민 스냅샷**(1,464명 · `fixture-boot.js waitUp` T344 문법 · 좌표·목표·행동 그대로 · `bench/walk-kernel/fixtures/`) + 지형 비트 + 벽 격자 + 나무 위치 · 결정(`decideNpcBehavior`)은 **네 팔 모두 같은 녹음된 목표열**을 쓴다(주사위를 피하는 법 — T350 전이라도 걸음 문만은 결정적이다) · 팔 넷 = ⓐ JS 현 루프(main 그대로 · `npcStep` 호출) ⓑ JS SoA 초안(계측기 안에서만 · 제품 아님 · ① 계약대로 열 순회) ⓒ WASM ⓓ N-API · **게이트: 1,000틱 뒤 좌표 전원 바이트 동일**(ⓐ 대 ⓑⓒⓓ · `Float64Array` 비교 · 다르면 그 팔은 표에서 **빨강** 이고 값을 안 낸다) · 자 = 사람당 µs(T333 문법 `p50 × 1000 ÷ 걸음`) · N = 1,464 와 **10,000**(스냅샷을 격자 복제 — 같은 마을 좌표를 옮겨 붙인다 · 복제 규칙 한 줄) · 각 팔 5판 · GC 열 · **30Hz 예산 33.3ms 대비 배** · T345 넷째 표(5.48µs · 1.39배)와 같은 줄에. ④ **VPS 요건 표** — 판 둘이 실서버에서 돌려면 무엇이 필요한가(WASM: 런타임 0 · node 22 내장 / N-API: 빌드 툴체인 · 바이너리 호환 · 배포 스크립트 한 줄) · **판정은 재민(#53)** · 세션은 요건만. ⑤ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: **제품 무접촉**(`server/`·`sim/`·`public/` diff 0 — `git diff --stat origin/main -- server sim public` 빈 출력을 보고에) · 새 파일은 `bench/walk-kernel/**` · `scripts/t352-walk-bench.js`(러너 밖 · `@regress` 없음) · `설계/설계_주민_SoA_계약.md` 뿐 · 3시드 면제(`sim/` 무접촉) · 러너 하네스 전부 무변 · `test-harness-lint` 47 그대로 · 접점 심볼(`npcStep|movePlayerStep|isBlockedByWall|isTerrainBlockedLocal|treeBlockerAt|isPositionActive|terrain-tilecache|Float64Array|WebAssembly|napi|walk.c|t352-walk-bench`).

## 0. §0 실측 (다섯)
ⓐ 계약 초안(열 표 · 읽기/쓰기 자리). ⓑ 커널 두 판 빌드 표(툴체인 유무 · 크기). ⓒ 네 팔 게이트 + 사람당 µs 표(1,464 · 10,000 · 예산 배). ⓓ VPS 요건 표. ⓔ 한 줄 답.

## 1. 절대 규칙
제품 무접촉 · 새 수 0 · 배포 0 · 산출물 커밋 0 · 결정(`decideNpcBehavior`)은 안 옮긴다 · **세션3 T350(`zone.js` 주사위)·SoA 카드와 접점: 계약은 초안 — 확정은 세션3 · `zone.js` 는 세션3 것** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/walk-kernel-0922` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · `[작업 중]` 커밋으로 채팅 보고 금지 · 긴 실측은 `setsid nohup … & disown` 으로 걸고 그동안 다음 팔을 쓴다)**.

## 2. 작업 (다섯)
① 계약 초안 ② 커널 두 판 ③ 네 팔 실측 ④ VPS 요건 ⑤ 한 줄 답.

## 3. 검증
제품 diff 0 · 게이트 바이트 동일 · 접점 목록.

## 4. 회부만
WASM vs N-API(#53 재민) · 계약 확정(세션3 SoA 카드) · 셀당 비트로 판정 ≤6회 설계(SoA 카드) · `NPC_CAP_PER_VILLAGE = 40`(#54).

## 5. 완료
커밋 [승인 대기 · T352] · 가지 푸시 · `보고/T352_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 결론 + 네 팔 한 줄 표**) · 채팅 "가지 올렸다 <해시> — 게이트 n/4 동일 · 사람당 ⓐ … ⓑ … ⓒ … ⓓ … µs · 10,000명 … ms". 실기 0.
