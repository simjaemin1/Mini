# 구현 지시 — T419: 석재는 어디서 실물로 쓰이나 — T400 이 집 자재를 표 하나로 세우니 econ 의 집 석재(`HOUSE_STONE 2.5/인`)가 켬에서 0 이 된다 → 광산 수요의 기둥 하나가 사라진다(T361 §1 회부 · T400 회부 "석재 실물 수요(PM)") · 그런데 세계엔 돌을 쓰는 실물이 이미 있다: 화덕·노·가마(`furnace`·`kiln`·`charcoal_kiln` 레시피) · 석기·숫돌(`tools` · `stone`) · 물매탄(T403 `stone`) · 석축/벽(`wall`) · 사유지 표지? — **전수 표**(서버 레시피가 돌을 얼마나 먹나 · econ 이 그 수요를 아나 · 둘이 같은 표인가) → **정본 하나**(T400 문법 · `hut-stages.js` 처럼 레시피 표를 econ 이 유도) · 켬 3시드 광산 열(광산 마을 인구 · 석재 부족/과잉 · 도구Q)이 집 석재 0 을 메우나 표 · 손잡이 `T419_STONE_REAL` 끔 · 끔 비트 동일 (세션4 · T400 뒤 · 2026-09-26)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T419.md` · 레포 `지시/지시_T419.md`)

---

T400(`batch/build-act-0926` → main · `hut-stages.js` 하나를 zone·쉼터·econ·랩이 읽는다(표 한 줄을 고치면 따라온다 — 하네스가 고쳐 봄) · 켜면 크루 둘이 3짐 날라 놓는 순간 단계 오름 · 30일 집 261 ↔ 257(수요) · 걸음 낮의 7.4% · 관측자 게이트 비트 동일 · 끔 3시드 동일 · `test-build-act` 51/51 · ⚠압축 날 4초는 켬 팔을 못 잰다) **통과** — "집 자재는 표 하나가 됐고 압축 날은 켬 팔을 못 잰다"가 이 카드의 문장이다(족보 335). #61·#62 는 재민 거부권 그대로. 켬 3시드 광산 열은 "전부 못 가름" — 석재 수요가 어디로 갔는지 아직 아무도 모른다.

**카드 T419 — 넷. PM 판정**(재민 거부권): ① **돌 쓰는 실물 전수** — 서버 레시피·건물 표에서 `stone`(및 `ore`·`meteorite` 원석)을 재료로 받는 것 전부(파일:줄 · 개수 · 단계) — 화덕/노/가마/숯가마 · 석기(도끼·괭이·칼·숫돌 · `tools`) · 물매탄 · 벽·석축 · 표지 · 그 밖 · econ 쪽 석재 수요 자리 전부(`HOUSE_STONE` · 도구 제작 · 대장간 · `stoneNet` T?) · **둘이 같은 물건을 말하나**(집처럼 어긋난 자리 표). ② **정본 하나** — 어긋난 자리마다 T400 문법: 서버 레시피 표가 정본 · econ 이 그 표에서 단가를 **유도**(사본 0 · `hut-stages.js` 옆에 `stone-uses.js` 같은 표 하나 — 이름은 자리가 정한다) · `HOUSE_STONE` 은 대조 자로만 · 손잡이 `T419_STONE_REAL` 기본 끔 · 끔 = 넷째 판-b 3시드 비트 동일(T420 이 낸 수 · 그 전엔 넷째 판). ③ **켬 3시드 광산 열** — T400 켬(집 석재 0) 팔 대 T400+T419 켬 팔: 광산 마을 인구·석재 부족/과잉 사건·도구Q·`STONE_NET` 발동 — 집이 놓친 석재 수요를 실물이 메우나(못 가름이면 못 가름 · 표) · 바닥 마을 36 곳의 돌 최저 곡선. ④ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 3시드 비트 동일 · `build-econ-bundle --check` 8 · `inline-engine --check` · `test-build-act` 51 · `test-econ-fieldyield` 202 · `test-lab-stone` 152(T195 절) · `test-lab-mining` · `test-itemlabel` 58 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`hut-stages|HOUSE_STONE|stone|ore|furnace|kiln|charcoal_kiln|tools|wall|STONE_NET|stoneNet|T419_STONE_REAL|hutEconMaterials|test-build-act|test-lab-stone`).

## 1. 절대 규칙
새 수 0(레시피 표의 그 수) · 사본 0 · 손잡이 하나(끔) · **T410(세션1 zone idle)·T398(세션2)·T421(세션7) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/stone-real-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#61·#62(재민) · 켜기(재민 · #69 와 같이) · 의뢰 집·플레이어 집 같은 표 실기(재민) · 압축 날 규약(PM).

## 5. 완료
커밋 [승인 대기 · T419] · 가지 푸시 · `보고/T419_<날짜>.md`(템플릿 네 칸) · `인계/F-제작건축.md` 갱신 · 채팅 "가지 올렸다 <해시> — 돌 쓰는 실물 n자리 · 어긋난 자리 n · 표 하나 · 끔 동일 · 켬 광산 열 …". 실기 0.
