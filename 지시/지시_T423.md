# 구현 지시 — T423: 짐이 먹는다 — 군량 = 행위 ⓐ 구현(★PM 결정 · #68: 세 길은 값이 아니라 캐논의 자리 — "생산은 행위"면 소비도 행위 · ⓐ 가 바닥이고 이중 계산·사기 단위 결함을 같이 닫는다 · 재민 거부권 그대로) · T388 §1-ⓐ 표 그대로: 싣기 무변(`_opPackLoad`) → 병사 몸 `inventory` 로 나눠 싣고 → 행군 중 하루 경계에 `doEat`(있는 그 함수 · B 층 허기) → `_packRem` 은 짐의 거울 → 사기 항 = 짐 잔량/필요량(새 수 0) → 귀환 몸이 `_warFoodGive` 로 내려놓는다 · 병사는 마을 `consumeFood` 에서 **빠진다**(이중 계산 닫힘 · T295 "징발자는 생산에서 빠진다"의 소비판) · 손잡이 `T423_RATION_ACT` 끔 · 자 = "한 원정 등가 표"(T388 §2-ⓒ) (세션10/WAR · T413 뒤 · 2026-09-26)

아래 전문을 **세션10** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T423.md` · 레포 `지시/지시_T423.md`)

---

T413(`batch/war-flaky-0926` → main · ⓛ = 자 — 하네스에 시계가 둘(가상 날 · 벽시계 스폰 씨) · 넷에 하나는 전멸 판인데 몸이 치워진 뒤 끝을 못 읽었다 · 10판 116/0 · ⑥ 선포 = 로그 한 줄 → NPC 전쟁은 전부 기습 · 사자가 걸어가야 선포다) **통과** — "하네스에 시계가 둘이었다"가 이 카드의 문장이다(족보 337). 기습 규칙은 재민. **#68 은 PM 이 닫는다** — 재민 09-26 "무조건 내 판정이 필요한 것 제외하고 네 판단": 세 길은 값이 아니라 캐논의 자리다. ⓐ 짐이 먹는다.

**카드 T423 — 다섯 · 어부 문법 여섯째 적용. PM 판정**(재민 거부권): ① **싣기 → 짐** — `_opPackLoad` 가 곳간에서 뺀 팩(품목별 · `consumeFood` 가 고른 그 품목)을 병사 몸(pid 있음)의 `inventory` 에 **나눠 싣는다**(kg · 몸당 짐 상한은 있는 수 25kg · 넘치는 몫은 표 — 그게 발견) · 몸 없는 전쟁(pid 0 · T? "군대 몸이 없는 전쟁")은 종전 장부 그대로(되돌림 문). ② **먹는다** — 행군 중 하루 경계마다 병사 몸이 제 짐에서 `doEat`(B 층 · 있는 함수 · 허기 규칙 그대로) · 짐이 비면 허기가 오른다(그게 세계다 · 사기는 그 뒤) · `_packRem` = 병사 짐 합(거울 · 사기 항 `ration = 짐 잔량 / (병력 × 하루치)` · 새 수 0 · T295 단위 결함 닫힘). ③ **빠진다·내려놓는다** — 원정 중 병사는 마을 `consumeFood` 명부에서 빠진다(이중 계산 0 · T388 §0-ⓑ 실측 1인 하루 2단위 → 1) · 귀환 순간 남은 짐을 `_warFoodGive` 문(있는 것)으로 곳간에 · 전사자의 짐은 궤주 규칙 그대로(드랍 · T403 무기와 같은 문) · 궤주 시 팩 소멸은 이제 "몸이 죽어 짐이 땅에" 로 실체화(T403 드랍 자리 · 값 무변). ④ **자·게이트** — 손잡이 `T423_RATION_ACT` 기본 끔 · 끔 = `test-war-world` 116 + 3시드 자명 동일(t17 전쟁 없음 · 한 줄) · 켬 = **한 원정 등가 표**(T388 §2-ⓒ · 같은 원정 끔/켬: 곳간에서 나간 양 = 몸이 먹은 양 + 내려놓은 양 + 드랍 · 항등 · 병사 허기 곡선 · 사기 곡선 · 결판 무변인가) · `test-war-world` 새 절(싣기 = 짐 합 · 하루 경계 먹음 · 이중 0 · 항등 · 되돌림) · 실서버 한 원정(T364 자 · 옆 마을이 본다 그 판)에서 병사 짐이 줄어드는 로그. ⑤ 한 줄 답. 나흘. 질문 금지 — 답은 표.

★검증 규약: 끔 비트 동일(`test-war-world` 116 · 3시드 자명 동일 한 줄) · `sim/war-core.js`·`battle-core.js`·`villages.js` 전쟁 절만 · `inline-engine --check` · `test-body`(허기 접점) · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`_opPackLoad|_packRem|ration|consumeFood|doEat|inventory|_warFoodGive|_warEngage|M_BREAK|T423_RATION_ACT|test-war-world|t364`).

## 1. 절대 규칙
새 수 0(짐 상한·하루치는 있는 수) · 손잡이 하나(끔) · 사본 0(있는 문 넷) · `Math.random` 0 · **T403 2층(무기 손에)·T400(세션4)·T410(세션1) 무접촉** · ★푸시 2판(자기 기기 · 가지 `batch/ration-act-0926` · main 은 PM) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · ④ 표) · ⓑ 길에서 채집 · ⓒ 약탈(PM · ⓐ 등가 뒤 별도 카드) · 기습 규칙(재민) · 민병 값(재민) · 무기 손에 2층(PM).

## 5. 완료
커밋 [승인 대기 · T423] · 가지 푸시 · `보고/T423_<날짜>.md`(템플릿 네 칸) · `인계/X-전쟁.md` X-9 · 채팅 "가지 올렸다 <해시> — 짐 싣기 n kg/몸 · 먹음 하루 n · 이중 0 · 항등 ○ · 사기 곡선 … · 결판 무변 ○/✗". 실기 0.
