# 구현 지시 — T358: T354 가 못 한 넷 — `deer_call`·`wind` 재확보(출처 폭 넓히기 · 후보 2~3씩 소리판에) · `bronze_hit`→`rock_hit` 가르기(돌 때리는 소리 — 없으면 **직접 녹음 회부** 표) · `axe`·`tiger_growl` 변주 **파일** 붙이기 · **이음새 자를 하네스로**(루프 파일 전부 끝↔머리 비 < 0 dB 게이트 · `wind.ogg` +25.2 dB 가 그 자로 빨개져야 한다) · `'wait'` 낱말 서버 한 줄(어부 던지는 순간 `_lifeAct` — `cast` 제자리) (세션9/SOUND · sfx 위임 계속 · 2026-09-23)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T358.md` · 레포 `지시/지시_T358.md`)

---

T354(`batch/sfx-ear1-0922` → main · 재확보 3(`water` 시냇물 CC0 · `wolf_howl` USFWS PD · `eat` 진짜 씹는 소리) · 새 5(`rain_light` NASA · `harvest` · `drop`(#50 닫힘) · `boar_grunt` ⚠합성 팩 · `step_stone`) · 지면 셋을 재민 귀대로 재배치(`ground` 표) · 변주 층 한 줄(`files:[…]` · 씨 = 자리 또는 울린 횟수 · 주사위 0) · 빠진 소리 전수 **자리 168 · 있음 30 · 없음 138**(`문서/빠진소리_전수_2026-09-23.md` · 자 `scripts/sfx-census.js`) · main 의 `test-audio ⑫` 빨강이 자의 결함(3200자 창)이었음을 잡음 · **이음새 자가 옛 `wind.ogg` +25.2 dB 를 물었다 — 10초마다 딸깍** · `test-audio` 119/0) **통과** — "부재를 이름으로 재지 않는다"(⑫d 가 철자로 창 없음을 재다가 T340 뒤 계속 초록)와 "재민이 '공기 소리'라 한 바람의 정체가 이음새였다"가 이 카드의 문장이다(족보 284). ★못 한 것을 못 했다고 적은 것이 맞다 — 그게 이 카드다.

**카드 T358 — 다섯. PM 판정**(재민 거부권): ① **`deer_call`·`wind` 재확보** — 출처 폭을 넓힌다: Freesound CC0 필터 · Wikimedia Commons(Category:Wind sounds · Category:Cervidae sounds) · xeno-canto 는 새라 아니고 **Macaulay Library 는 라이선스가 안 맞으니 ✗** · USFWS·NPS·USGS 등 미국 정부 PD 녹음(늑대가 거기서 나왔다) · **Sonniss GDC 번들**(상업 가능 · 팩 EULA 표에) · 사슴은 "울부짖음"이 아니라 **풀 밟는 기척·콧김·낮은 부름**(재민 메모) · 바람은 **나뭇잎·풀 스치는** 것(공기 소리 ✗) · 키마다 **후보 2~3개**를 소리판에 나란히(`_후보` 행 · 1번 후보만 배선) · 이음새 자 통과한 것만. ② **`rock_hit`** — 돌·광석·운석 때리기(`resourceHit` 의 `rock`·`ore`·`meteorite`)를 `bronze_hit` 에서 가른다 · 돌 위에 돌/청동 부딪는 PD/CC0 녹음 · **못 찾으면 "직접 녹음 회부" 한 줄**(재민이 폰으로 돌 두 개 부딪혀 녹음 — 무엇을 어떻게 녹음하면 되는지 3줄 규격: 거리·길이·포맷 — 그 파일은 `_sfx_in/` 규약대로) — 억지로 아무 소리나 넣지 않는다. ③ **변주 파일** — `axe` 2~3 · `tiger_growl` 2~3 · 이미 배선된 `files:[…]` 에 붙인다 · 같은 출처 팩 안의 이웃(케니 `chop`·`chop2`… 같은 것)부터 · 소리판 파일마다 행. ④ **이음새 자 → 하네스** — T354 가 만든 자(끝↔머리 차 ÷ 이웃 표본 차 평균)를 `test-audio` 절로: **루프 키 전부**(`ambient`·`buildings` 루프 · `bgm` 은 제외 — 곡은 루프가 아니다) 비 **< 0 dB** 게이트 · 자명 통과 금지(문턱을 +30 으로 비틀면 `wind.ogg` 가 통과해 버린다 — 그 반례) · 지금 `wind.ogg` 가 **빨갛게** 나와야 하고 ①이 그걸 초록으로 만든다(순서: ④ 먼저 빨강을 보고 → ① 로 끈다 · 보고에 둘 다). ⑤ **`'wait'` 낱말 서버 한 줄** — T340 어부 대본의 던지는 순간에 `_lifeAct` 낱말이 없어 `cast` 가 울 자리가 비어 있다(T354 회부 ①) · `villages.js` 어부 절(T312/T340 · 세션3 것)에 **한 줄**(`'드리움'` 낱말을 `wait` 진입에) · `npcAct` 표는 이미 `드리움 → cast` 다 · 세션3 T356 이 `zone.js` 걸음을 만지는 중이라 **`villages.js` 어부 절 그 줄만** · `test-fish-act` 73 그대로 + `e2e-audio-probe` 에 던짐 호출 1. ⑥ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 확보 규약 T354 그대로(파일 페이지 라이선스 · BBC ✗ · Macaulay ✗ · 원본 `_sfx_in/` · ogg+m4a · `sources`·CREDITS §2-b · 헤드룸 `_실측` · 출처 없는 소리 0) · `test-audio` 119 + 이음새 절 · `test-assets-audit`(⚠컨테이너 `@cwasm/webp` 는 손으로 깔아 돌린다 — T354 ⑤ · 그 자체는 PM 회부) · `e2e-audio-probe` 26+1 · `test-client-globals` 26 · `test-fish-act` 73 · `test-harness-lint` 57 · `bgm/` 무접촉(세션8 T353) · 접점 심볼(`manifest.json|files|resourceHit|rock_hit|ambient|wind|deer_call|sfx-board|test-audio|이음새|_lifeAct|드리움|wait|npcAct|e2e-audio-probe|_sfx_in`).

## 0. §0 실측 (다섯)
ⓐ 재확보 둘 + 후보 표(출처·라이선스·이음새). ⓑ `rock_hit` 또는 녹음 규격 3줄. ⓒ 변주 파일 표. ⓓ 이음새 하네스 — 빨강(전) → 초록(후). ⓔ `'wait'` 한 줄 diff + 프로브 호출 수. ⓕ 한 줄 답.

## 1. 절대 규칙
출처 없는 소리 0 · 아무 소리나 안 넣는다(없으면 없다고 · 녹음 회부) · `bgm/` 무접촉 · `zone.js` 무접촉(세션3 T356) · `villages.js` 는 어부 절 한 줄만 · `Math.random` 0 · 기존 키 비트 동일 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-ear2-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (여섯)
④ 이음새 하네스(빨강 먼저) → ① 재확보 둘 → ② `rock_hit` → ③ 변주 → ⑤ `'wait'` → ⑥ 한 줄 답.

## 3. 검증
하네스 여섯 · 비트 동일 · 접점 목록.

## 4. 회부만
`rainSplit` 문턱(재민 · T354 회부 ③ · 세계가 `precip` 을 보내는 날 T93) · 빠진 소리 138 우선순위(재민 · `문서/빠진소리_전수_2026-09-23.md`) · `boar_grunt` 진짜 녹음(못 찾음 · 녹음 회부와 같은 표) · `@cwasm/webp` 설치(PM · 야간 러너) · 둘째 귀 판정(재민 · 배포 뒤 소리판).

## 5. 완료
커밋 [승인 대기 · T358] · 가지 푸시 · `보고/T358_<날짜>.md`(ⓐ~ⓕ · **머리 15줄에 결론**) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 재확보 n/2 · rock_hit ○/녹음회부 · 변주 n · 이음새 절 빨강 n→0 · wait 한 줄". 실기: 재민이 배포 뒤 소리판 둘째 귀 판정.
