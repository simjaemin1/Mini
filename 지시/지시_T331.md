# 구현 지시 — T331: `e2e-trade` 의 뿌리 — 재접속 쏠림(한 판에 pid 가 2~10회 바뀐다 · T322 ③) — 어디서 왜 끊기고 왜 새 pid 를 받나 · 게스트 정체성 정본(`test-guest-identity`)과 같은 규칙으로 같은 pid 를 돌려주게 · 하네스가 끊김을 일부러 넣어도 pid 하나 (세션2 · T322 뒤 · 2026-09-19)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T331.md` · 레포 `지시/지시_T331.md`)

---

T322(main · 단정 셋 고침 · `e2e-fogray` 프레임 카운터 · **`e2e-trade` = 뿌리는 제품(재접속 쏠림 · pid 2~10회)** · ⑧a 선언 추적 3/3 · 예산형 92자리 경고) **통과** — "옛 자에 새 자를 대 본 것" 이 증거였다(족보 256). 그 뿌리를 이 카드가 판다.

**카드 T331 — 둘.** ① **진단** — 한 판(`e2e-trade` 문법)에서 재접속이 **언제**(틱 · 무슨 메시지 뒤) **왜**(서버가 끊나 · 클라가 끊나 · ping 타임아웃 · 워프 뒤 존 핸드오프) 나는지 호출 수·시각 표 · 새 pid 를 주는 자리(`central`·`zone` 입장 · 게스트 정체성 `test-guest-identity` 가 지키는 규칙 116/0 — 왜 여기선 안 지켜지나) · ② **수리** — 같은 브라우저·같은 토큰의 재접속은 **같은 pid**(정체성 정본 그 함수 · 사본 0 · 새 수 0) · 끊는 쪽의 결함이면 그 자리 하나 · 하네스: `e2e-trade` 초록 두 판 + 절 하나(끊김을 일부러 3회 넣어도 pid 1 · 워프 예산 소진 0) · `test-guest-identity` 116 · `e2e-conn`·`e2e-guest-reconnect` 그대로. ③ 한 줄 답. 하루. 질문 금지.

★검증 규약: 제품 접촉 = 재접속/pid 발급 자리 하나(`zone.js`/`central.js`/`30-n-net.js` 중 진단이 가리키는 곳) · 3시드 면제 · `test-doors` 39 · 접점 심볼(`pid|reconnect|guest|token|warp|handoff|e2e-trade|test-guest-identity|e2e-guest-reconnect`).

## 1. 절대 규칙
새 수 0 · **T319(`startinfo`·`/health`)·T324(세션3)·T295** 무접촉 · 하네스를 재는 동안 러너를 부르지 않는다(족보 247) · ★푸시 2판(가지 `batch/reconnect-pid-0919` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T331] · `보고/T331_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 끊김의 자리 … · pid 1 · trade 초록 2/2".
