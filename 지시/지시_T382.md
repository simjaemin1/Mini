# [GPT] 지시 — T382: 독립 재현 — **별도 폴더에 새로 클론**한 durango-mini(main)에서 ① 3시드 800일 열 열(`t17-metrics`)을 돌려 `인계/공통.md` §3 넷째 판 표와 대조(10열 × 3시드 · 일치/불일치) ② 단위 하네스 `scripts/test-*.js` 전수를 **한 개씩 단독**으로 돌려 결과 표(파일 · PASS/FAIL 수 · rc · 초) ③ `test-harness-lint` 결과 · 판정 0 · 코드 0 · 결과는 `보고/T382_<날짜>.md` 하나 · 가지 `codex/repro-0923` 만 푸시(main 금지) (세션13/GPT · 2026-09-23)

아래 전문을 **GPT(Codex · 세션13)** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T382.md` · 레포 `지시/지시_T382.md`) **`[GPT]` — 판정 0 · 코드 0 · 표만.**

---

**★자리부터.** `~/Mini` 안에서 작업하지 마라(그건 다른 레포다 — 첫 판에 무관한 히스토리가 원격에 밀렸다). 반드시:

```
mkdir -p ~/codex && cd ~/codex && git clone https://github.com/simjaemin1/Mini durango-mini && cd durango-mini
git checkout -b codex/repro-0923 origin/main && npm install
```

레포 규약은 `인계/공통.md` §1(절대 규칙)·§2(검증)·§3(기준선)·§6(완료)을 읽어라. 이 카드는 **아무것도 고치지 않는다** — 다른 회사 모델이 같은 수를 내는지가 이 카드의 값이다.

**카드 T382 — 셋.** ① **3시드 열 열** — `node scripts/t17-metrics.js 800 1020`, `… 800 7`, `… 800 42`(각 수 분 · 순서대로 · 출력의 "여덟 수" 줄과 "마을당 n일/건" 줄) → `인계/공통.md` §3 넷째 판 표(인구·소멸·무기Q·확장셀·게시·도구Q·보존식·생곡 + 밀도 둘)와 **열마다** 일치/불일치 표(값 그대로 · 차가 있으면 그 수). ② **단위 하네스 전수** — `ls scripts/test-*.js` 전부를 **한 개씩** `timeout 600 node scripts/<f>` 로(병렬 금지 · 포트 공유) · 표: 파일 · 마지막 결과 줄(`PASS n / FAIL n` 또는 `통과 n / 실패 n`) · rc · 초 · 600초 넘긴 것은 "시간 초과"(`test-terr-persist`·`test-route-persist`·`test-site-memo` 는 컨테이너에서 10분을 넘긴 전력이 있다 — 그렇게만 적는다) · 빨강이 있으면 **마지막 30줄만** 인용(고치지 않는다). ③ `node scripts/test-harness-lint.js` 결과 줄 + `node sim/build-econ-bundle.js --check` · `node sim/inline-engine.js --check` · `node scripts/lab-wiring-check.js` 세 줄. ④ 한 줄 답: "넷째 판 30칸 중 일치 n · 하네스 n종 중 초록 n · 빨강 목록". 반나절.

★규약: 코드 0 · 레포 파일 수정 0(보고 한 장만 추가) · 새 수 0 · `보고/T382_<날짜>.md` 머리 15줄에 ④ · 커밋 메시지 `[승인 대기 · T382]` · **푸시는 `codex/repro-0923` 가지만**(`git push -u origin codex/repro-0923` · main 은 절대 안 민다) · force 0 · 질문 금지 — 답은 표.
