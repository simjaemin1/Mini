# 구현 지시 — T417: 동물 갈래 40 — 이웃 키 넷(`bird`·`deer_call`·`wolf_howl`·`boar_grunt`·`tiger_growl`)이 있어 판정 불필요 · 자리 표 ⓐⓑⓒ(T387·T412 문법 — 사건 / 상태 동기화 / 애매) → 붙는 자리만 확보(T354 규약 · 후보 1~2 · 없으면 녹음 회부) · **헤드룸 "조합"의 뜻 하나로**(T412 ⑧c 여유 0.03dB — 문턱을 올리지 않는다 · ★PM: 최악 조합은 짓는 게 아니라 **실측 표본에서 뽑는다** — e2e 30분 판(`e2e-audio-probe` 전 대본)에서 같은 200ms 창에 실제로 겹친 키 묶음 상위 n · 새 수 0) · **제작·보존 완료 낱말 서버 한 줄 위임**(T412 회부 1 · `sendInventory(player, 'craft')` T305 문법 · 완료와 취소를 가른다) (세션9/SOUND · T412 뒤 · 2026-09-26)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T417.md` · 레포 `지시/지시_T417.md`)

---

T412(`batch/sfx-work-0926` → main · 도구/작업 57 중 붙는 자리 20(새 14) · 9키 배선 · 후보 8 · 없음 1(`dig`) · 수신 머리의 직전 값으로 사건을 가른다(서버 0) · 두 번 울지 않는다 · 갈래 표의 "서버 메시지" 여섯은 메시지가 아니었다(장비·건물 낱말 착시) · 제작 완료 소리는 없다(완료와 취소가 같은 꼴) · 헤드룸 최악 조합 갱신 0.7967(여유 0.03dB) · `test-audio` 170 · 전수 있음 40 → 51) **통과** — "수신 머리의 직전 값으로 사건을 가른다"가 이 카드의 문장이다(족보 333). ★PM 판정 둘: ⓐ 헤드룸 조합은 **실측**에서 — T412 회부 4 "조합의 뜻을 PM 과 다시 정한다" → 정한다: 문턱 0.8 은 그대로 · "최악 조합"은 손으로 겹친 표가 아니라 e2e 전 대본 30분에서 같은 200ms 창에 **실제로 겹친** 키 묶음(상위 n · n 은 있는 수 — 창 수에서) · 그 표본에서 없이 ≤ 0.8 · 손으로 겹친 표는 대조 자로만. ⓑ 제작·보존 완료 낱말 — **서버 한 줄 위임**(`sendInventory(player, 'craft')` 꼴 · 완료 자리 하나 · 취소 자리는 무변 · 새 낱말 0 — `where` 표에 이미 있는 꼴).

**카드 T417 — 넷. PM 판정**(재민 거부권): ① **동물 자리 표 40** — `문서/빠진소리_갈래_2026-09-23.md` 동물 갈래 그대로 · 자리마다 코드 줄 · ⓐ 사건(짐승이 운다 · 공격 · 맞음 · 죽음 · 도망 · 새 날아오름) ⓑ 상태 동기화(`mob_spawn`·위치 갱신·`mobs` 명부) ⓒ 애매(표만) · 이웃 키 이름(어느 있는 키 옆 — 종별 변주 `files:[…]` 로 갈 수 있는 것은 그 표시) · **T412 ⑧의 착시 검사**(메시지가 아닌 낱말 먼저 거른다). ② **확보** — ⓐ 자리만 · 종은 `animals.js` 전부(있는 종만 · 새 종 0) · 확보 규약 T354·T358 · 후보 1~2 · 없음 → 녹음 회부 · 훅은 `48-a-audio.js` · 거리 감쇠 있는 규칙 · 두 번 울지 않는다(T412 문법 — `hp_changed why:mob` 은 사람 쪽 `hit_body` 가 이미 운다 · 짐승이 맞는 소리는 별개 자리). ③ **헤드룸 실측 조합** — e2e 전 대본을 한 번 돌려 200ms 창마다 겹친 키 묶음을 기록(자 하나 `scripts/sfx-cooccur.js` · 러너 밖) · 상위 n 묶음 표 · 없이 peak · 0.8 안인가 · `_실측.worstCombo` 를 그 묶음으로(손으로 겹친 표는 `_실측.handCombo` 대조 자로) · ⑧c 는 실측 묶음을 문다 · 자명 통과 반례(묶음을 비우면 빨개진다). ④ **제작·보존 완료 한 줄** — `zone.js` 제작 줄 완료 자리에 `sendInventory(player, 'craft')`(있는 함수 · 있는 낱말꼴) · 취소는 무변 · 층 표 `where:craft → craft_done`(T412 이 확보한 그 키) · 보존 완료도 같은 자리면 같은 한 줄(다른 자리면 표만) · e2e 완료 1 · 취소 0. 사흘. 질문 금지 — 답은 표.

★검증 규약: 출처 없는 소리 0 · 서버 = 완료 한 줄(+보존 한 줄이면 둘) · `30-n-net.js` 무접촉 · `bgm/` 무접촉(세션8) · 기존 58키 비트 동일 · `test-audio` 170 + 절 · `test-assets-audit` PASS · `e2e-audio-probe` 71 + n · `e2e-aim` 15 · `e2e-pid-collide` 13 · `test-client-globals` 26 · `test-harness-lint` 61 · 소리판 행 수 = 키·파일 수 · 접점 심볼(`animals.js|mobs|mob_spawn|wildlife|48-a-audio|sendInventory|craft_queue|craft_done|worstCombo|sfx-cooccur|sfx-board|CREDITS|_sfx_in|sfx-census`).

## 0. §0 실측 (넷)
ⓐ 자리 표 40(ⓐ/ⓑ/ⓒ · 착시 걸러냄). ⓑ 확보 표. ⓒ 실측 조합 표 + peak + 반례. ⓓ 완료 한 줄 diff + e2e.

## 1. 절대 규칙
출처 없는 소리 0 · 새 종 0 · 서버 한두 줄 · 문턱 0.8 무변 · `Math.random` 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-animals-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#60(재민 · 배포 뒤 소리판 한 번에 — 이제 새 행 17+n) · 주민 수확 소리(재민 · 서버 한 칸) · UI/알림 21(재민 #60) · 녹음 회부 행(재민 폰 · `dig` 등) · CC-BY 표시(#14ⓒ) · 천둥 사건(PM).

## 5. 완료
커밋 [승인 대기 · T417] · 가지 푸시 · `보고/T417_<날짜>.md`(템플릿 네 칸) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 동물 붙는 자리 n/40 · 확보 n · 실측 조합 peak … · craft_done 운다 1". 실기: 재민 배포 뒤 소리판.
