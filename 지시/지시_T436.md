# 구현 지시 — T436: 시딩 게이트가 교역을 본다(★PM · #78 ⓐ) — T429: `pickSeedVillages` 의 식량 하한(`landScore` · 비옥×1.5 + 물×1.2 + 사냥×0.7 ≥ 2.0)은 **후보 칸 한 점의 자급**만 보고 교역을 한 글자도 안 본다 · 한반도(`seedAllVillages`)는 이 문을 안 타지만 탔다면 9곳 탈락(광산 셋 포함)이고 그 9곳은 800일 3시드에서 **모두 살며 먹은 것의 중앙 76.5% 를 교역으로** 먹는다 · 중원북 광산 0/11 · 닛폰 0/7 이 그 문의 값이다 ⇒ 게이트에 **교역 잠재**를 넣는다(닿는 마을의 잉여 · 있는 캐러밴 자 · 새 수 0 — 문턱은 한반도 탈락 9곳이 통과하고 통과 42곳이 그대로인 자리에서 **유도**) · 손잡이 `T436_GATE_TRADE`(기본 **끔** · 한반도 비트 동일 = `seedAllVillages` 라 어차피 안 탄다 — 게이트: 세 존 시딩 JSON 끔 동일) · 켬 = 중원북·닛폰 **재시딩 표**(마을 n · 광산 n · 3시드 800일 · 소멸) · 켜기(존 재시딩 = 그 존 DB 새로)는 재민 배포와 같이 (세션6 · T429 뒤 · 2026-09-27)

아래 전문을 **세션6** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T436.md` · 레포 `지시/지시_T436.md`)

---

T429(`batch/mine-food-0926` → main · 게이트 = 후보 칸 자급 잠재만 · 한반도 탈락 9곳(광산1·3·7 포함)이 3시드 소멸 0 · 교역 중앙 76.5%(최소 40%) · 통과 42곳 중앙 9.2% · 표 기계 `t429-gate-food.js` 정본 선별 51/51 · 16/16 · 25/25 일치 · 계측기 `t429-mine-food.js` 자기검사 `t17` 동일) **통과** — "게이트가 세계가 쓰는 길(교역)을 못 본다"가 이 카드의 문장이다(족보 353). ★PM(#78 · 게이트는 캐논 구조라 PM): **ⓐ** — 광산만 면제(ⓑ)는 탈락 9 중 6 이 임업·농촌이라 병을 절반만 고친다 · ⓒ 그대로는 중원북 광산 0 을 그대로 둔다 · 반대면 재민 한 글자.

**카드 T436 — 넷. ★PM 결정(위임 · 재민 거부권)**: ① **교역 잠재 항** — `pickSeedVillages` 의 `landScore` 에 항 하나: 후보 칸에서 **캐러밴 자(있는 `path-core` 걸음 · 다리 통행 · T360 도달 표 문법)로 닿는 이웃 후보/마을의 식량 잉여**(자급 잠재 − 자기 하한 · 거리 감쇠는 **있는 캐러밴 시계**(econ 좌표 셀×2.5 · 소문 자 T7 의 그 시계)에서 유도 · 새 수 0) · 문턱 2.0 무변 · 계수는 **한반도 탈락 9곳 전부 통과 · 통과 42곳 그대로**가 되는 자리에서 유도(한 값 · 그 유도식 표 · 두 자리에 겹치는 값이 없으면 "없다"고 적고 가장 가까운 값과 그때의 9/42 표) · 손잡이 `T436_GATE_TRADE`(기본 끔 · 끔 = `t429-gate-food` 51/51 · 16/16 · 25/25 그대로). ② **재시딩 표(켬 · 자만 · DB 무접촉)** — 중원북 후보 25 · 닛폰 후보 21 · (한반도 51 은 `seedAllVillages` 라 무변 확인만): 켬에서 통과 n · 광산 n · 이름 · 자리 — 그리고 켬 세계로 **3시드 800일**(`T17_ZONE` 자 · 중원북·닛폰 각 3) · 소멸 · 광산 마을 인구 · 교역 비율(T429 자로) · 끔 판(T409 1,736/1,599/1,792 · T407 닛폰 판)과 나란히. ③ **게이트** — 끔 = 세 존 시딩 JSON 비트 동일(`fixture-boot` 시딩 산출 해시 셋) · 한반도 3시드 면제 근거 한 줄(`seedAllVillages` · econ 무접촉 — `villages.js` 를 만지니 **돌린다** · 여덟 수 동일) · 켬에서 한반도가 문을 탄다면(재민이 언젠가 `seedAllVillages` 를 끄면) 51/51 통과인지 한 줄. ④ 한 줄 답 + 켜기 절차(존 재시딩 = 그 존 `world-<zone>.db` 새로 · 한반도 무변 · 재민 배포 `--all` 에서 어느 존이 새로 뜨나 · T428 과 맞물림). 사흘. 질문 금지 — 답은 표.

★검증 규약: 새 수 0(계수는 유도 · 문턱 무변) · 손잡이 하나 · 끔 비트 동일(시딩 JSON 셋 · `t429-gate-food` 세 존) · 한반도 3시드 여덟 수 동일 · **T435(세션4 `zone.js` 곳간)·T432(세션1 `zone.js` 청크)·T428(세션5 존 셋 — 재시딩은 그 카드 뒤 재민 배포) 무접촉** · `test-nippon-boot` 42 · `test-jungwon-boot` 38 · `test-econ-fieldyield` 202 · `test-farm-life` 33 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`pickSeedVillages|landScore|FOOD_FLOOR|seedAllVillages|T436_GATE_TRADE|t429-gate-food|t429-mine-food|plan-bridges-v2|audit-reachability|path-core|T17_ZONE|fixture-boot|caravan`).

## 0. §0 실측 (넷)
ⓐ 항 diff + 계수 유도 표(9/42). ⓑ 재시딩 표 + 두 존 3시드 켬/끔. ⓒ 게이트(JSON 셋 · 한반도 여덟 수). ⓓ 한 줄 답 + 켜기 절차.

## 1. 절대 규칙
새 수 0 · 손잡이 하나 · 기본 끔 · DB 무접촉(재시딩은 표만) · `villages.js` 게이트 자리만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/gate-trade-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 3시드 아홉 판은 `setsid nohup … & disown`)**.

## 2. 작업 (넷)
① 항 ② 재시딩 표 ③ 게이트 ④ 한 줄 답.

## 3. 검증
비트 동일 · 3시드 · 하네스 여섯 · 접점 목록.

## 4. 회부만
켜기 = 존 재시딩(재민 · 배포 `--all` · T428 뒤) · #76(재민) · 닛폰 수동 지형(재민 · T407 그림 — 재시딩 전에 넣으면 한 번에) · #69·#75(재민).

## 5. 완료
커밋 [승인 대기 · T436] · 가지 푸시 · `보고/T436_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/V-마을.md`·`Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — 교역 항 계수 … (9/9 · 42/42) · 켬 중원북 마을 n · 광산 n · 3시드 … 소멸 n · 닛폰 … · 끔 동일 ○ · 한반도 여덟 수 동일 ○". 실기: 재민 재시딩 배포 뒤.
