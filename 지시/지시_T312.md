# 구현 지시 — T312: 어부를 옮긴다 — NPC 인벤토리(플레이어 꼴 · 사본 0) · 어부는 강가 셀에 가서 낚고 손에 들고 돌아와 곳간에 넣는다(관측자 있든 없든 같은 길) · `addProduce('fish')` 는 그 마을에서 지워진다 · 첫날 합 = 지금 수식(유도 · 새 수 0) · 손잡이 `T312_FISH_ACT` 기본 끔 · 세계 위 부하 (세션3 · T297 뒤 · 2026-09-19)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T312.md` · 레포 `지시/지시_T312.md`)

---

T297(`batch/produce-act-0918` → main · 관찰 모드 헤드리스 결정론 · **밭은 이미 등가(소수 다섯 자리)** · 고기·채집·생선은 30일이면 다른 세계 → **등가는 하루로 건다** · 어부 하루 8.3마리 = 왕복 한 번(짐 25kg = 27.8마리) **가능** · 광부 750g = 62g×12회 **사람의 하루가 아니다** · 51마을 실걸음 외삽 하루 11.7분 · 새 계측기 둘) **통과** — 이 표가 없었으면 어부를 옮기고 나서야 알았을 것들이다. 족보 (236)(237). ⚠"고증 못 댔다" 를 못 댔다고 적은 것도 인정. 광부 수식 판정은 재민 칸(#43) · 고기 누수와 관찰 모드 채집꾼·어부 미선발은 **T311(세션1)**.

**캐논(`설계/설계_생산_실체.md` §1·§2 — 재민 09-18/19 확정)**: 생산은 행위다 · 낚는 순간 **손에**, 귀환하면 **곳간에**, 도중에 죽으면 그 자리에 **낙하** · 관측자 없어도 실걸음 · **ⓘ NPC 인벤토리 = 플레이어와 같은 꼴**(`inventory` 품목별 수 · `carry.js` 무게 · 같은 낙하 함수 · **새 체계 0**) · `npc._carry`(품목 없는 짐 칸 3)는 이 카드에서 **흡수**된다 · 어종은 `설계_민물고기.md` 10종 · 재생은 강가 셀 예산(하루 재생 · 이월 없음 · 값은 지금 수식에서 유도).

**카드 T312 — 첫 이식 · PM 판정**(재민 거부권): ① **NPC 인벤토리** — `econ.npcs[i]`/걷는 npc 에 `inventory`(플레이어와 같은 모양 · 같은 `carry.js` 무게 상한 함수 · 사본 0 — 어느 함수를 부르는지 표) · `_carry` 의 세 사용처(수확 `:4439` · 곳간 `:5291` · 지게 `:5792`)를 `inventory.grain` 등 **품목**으로 바꾼다(행동 무변 — 수확 3칸 = 곡물 n 의 kg 상한 · 지게 표시는 무게>0) · 죽음 경로(econ 사망 정본)에서 **낙하 = 플레이어 낙하 함수 그 자리**. ② **어부 행위** — `_jobSites.fisher`(강가 셀)로 걷는다 · 셀에 닿으면 **던짐→입질→걸림**(소리 층이 이미 셋을 가졌다 T272 · 훅 한 줄씩 · 로직 0) · 걸리면 그 셀 예산에서 **종 하나**를 뽑아 `inventory[종]++`(종은 그 마을의 물 종류 가중 표 한 줄 · `설계_민물고기.md` §1 열 "물" · 계절 0/1) · 무게 상한 또는 낮 끝에 곳간으로 돌아와 `_granStockAdd` **그 함수**로 넣는다(회계는 econ 곳간 정본 — `harvestToGranary` 가 T179 에서 낸 문법 그대로 · **`addProduce('fish')` 는 이 마을에서 호출 0**(공존 = 이중 생산 · T135 `repl` 문법) · 관측자 없는 마을도 **같은 함수·같은 걸음**(헤드리스 결산 `_lifeHeadlessDay` 의 어부 칸을 지운다 — 몸이 걷는다). ③ **첫날 등가(ⓓ①)** — 셀당 하루 예산 = 지금 수식(`land.water × 1.2 × sk` 합) ÷ 그 마을 강가 셀 수(`설계_민물고기.md` §2) · 종 가중은 물 종류 표 · **3시드 마을 51 첫날 어획 합 = 수식과 같다**(자 · 폭은 T297 ⓐ 하루 잡음) · 그 뒤로는 실체가 내는 만큼 — **`land.water < 0.833` 인 16마을**은 어부가 제 입을 못 채운다 → 아무것도 안 한다(그림자가격이 직업을 옮긴다 — 그게 세계다 · 표에 그 16마을 30일 뒤 어부 수). ④ **손잡이 `T312_FISH_ACT` 기본 끔** — 끔 = 3시드 800일 여덟 수 **넷째 판 비트 동일** · 켬 = 위 전부 · 값은 없다(켜기는 재민). ⑤ **부하 — 세계 위**(T284 자): 실지도 51마을 · 관측자 하나 · 손잡이 끔/켬 존 틱 p50/p95 · 걷는 어부 수 · 틱 빚 · **관측자 없는 마을이 걷는 것의 값** — T297 외삽 11.7분/일 은 브라우저 랩이었다 · 서버 수가 이 카드의 답이다. ⑥ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 3시드 비트 동일 · `build-econ-bundle --check` 8/8 · `inline-engine --check` · `lab-wiring-check` · 켬 첫날 등가 자(3시드 · 마을 51) · `test-farm-life`·`test-psite-server`·`test-hunt-vis`·`test-resource-index` 그대로 · 새 하네스 `test-fish-act`: ⓐ 관측자 켬/끔 같은 씨 = 같은 어획(결정성 · T284 ⓓ 문법) ⓑ 죽은 어부의 손에 든 생선이 그 자리에 있다(낙하 · 플레이어 함수 심볼 하나 · 정적) ⓒ 켠 마을에서 `addProduce('fish')` 호출 0(계수) ⓓ 곳간 회계 = 손에서 넣은 합(장부 이중 0) ⓔ 하루 예산 소진 뒤 그 셀 어획 0 · 이월 0 · `test-harness-lint`(벽시계 단정 0) · `lab-modules --diff origin/main` 교집합 · 접점 심볼(`inventory|carry.js|_carry|_jobSites|harvestToGranary|_granStockAdd|addProduce|JOB_OUT|_lifeHeadlessDay|land.water|T312_FISH_ACT|resourceAt|isWaterTileLocal|_flowAtCell`).

## 0. §0 실측 (여섯)
ⓐ 인벤토리 표(함수 셋 · 사본 0 · `_carry` 세 자리). ⓑ 어부 행위 표(걸음 · 예산 · 종 · 반입). ⓒ 첫날 등가 3시드 + 16마을 30일. ⓓ 끔 비트 동일. ⓔ 세계 위 부하. ⓕ 한 줄 답.

## 1. 절대 규칙
새 수 0(예산·종 가중·계절 전부 유도 · 유도 목록 회부) · 사본 0(인벤토리·무게·낙하·곳간 함수) · 배수·상태 스위치 0(공통 §1) · 손잡이 기본 끔 · 켜지 않는다 · **`villages.js` 전쟁 절(T295 세션10)·`chunk.js`(T301)·`economy-sim.js` 캡 자리(T300 `:3087`)·`zone.js` `sendInventory`/`doEat`(T305) 무접촉 — 리베이스 때 둘 다 살린다** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/fish-act-0919` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0.

## 2. 작업 (여섯)
① 인벤토리 ② 어부 행위 ③ 첫날 등가 ④ 손잡이 끔 ⑤ 부하 ⑥ 한 줄 답.

## 3. 검증
3시드 · 게이트 셋 · `test-fish-act` ⓐ~ⓔ · 접점 목록.

## 4. 회부만
켜기·기준선 다섯째 판(재민) · 16마을 처리 관찰(PM) · 나무꾼(다음 카드 · 같은 문법) · 광부 수식(#43 재민) · 어부 소리 파일(세션9) · 군량을 같은 문법으로(전쟁 회부 · T295 뒤).

## 5. 완료
커밋 [승인 대기 · T312] · 가지 푸시 · `보고/T312_<날짜>.md`(ⓐ~ⓕ · **머리 15줄에 결론**) · `설계_생산_실체.md` §0 표 갱신(어부 행 · 문서) · 채팅 "가지 올렸다 <해시> — 인벤토리 … · 등가 … · 끔 동일 · p50 …/… · 하네스 …/n". 실기 0.
