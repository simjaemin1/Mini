# 구현 지시 — T314: 야간 창이 e2e 46종을 못 잰다 — 묶음 기계(T293 `nightly-split.sh` A/B/C)가 09-19 판에서 왜 안 갈랐나 · e2e 48종을 세 밤에 나눠 **전부** 재는 판으로 · 부하 거짓 빨강 넷(`conn`·`fogray`·`mtfuzz`·`trade`) 두 밤 대조 · 벽시계 단정 전수 감사(족보 ⑩) (세션2 · T304 뒤 · 2026-09-19)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T314.md` · 레포 `지시/지시_T314.md`)

---

T304(main `788f79fa` · charsprite 대조군 = 시간을 재던 자 결함 → 전제 검사·유보 · rumor ⑦d2 = 잠든 제품 결함(경주 · Z · T7) · `nightly-known` rumor 한 줄 · `test-harness-lint ⑧a` 가 T301 하네스 벽시계 단정을 잡은 것 → PM 이 그 줄을 뺐다 `05b7ae06`) **통과** — "대조군이 잰 것은 착장이 아니라 시간이었다" 가 이 카드의 문장이다(족보 239). rumor 경주는 세션7 T310 뒤 카드(Z).

**카드 T314 — 넷. PM 판정**(재민 거부권): ① **야간 09-19 판이 46종을 못 쟀다** — 보고는 "창 처리를 단위 먼저로 바꿨다 · e2e 48종만 3시간 24분" 인데, T293 이 만든 **묶음 기계(A/B/C 날짜 주기 · `nightly-split.sh`)** 는 e2e 를 세 밤에 나누라고 있는 것이다. 09-19 판(망고링고 옛 프롬프트 · 손 청크)이 기계를 안 썼는지, 기계가 돌았는데 묶음이 안 갈렸는지 **로그·`regress.log.state`·보고 머리**로 가른다(사실만) · 오늘 밤(09-20 04:00 · v3 프롬프트 · 기계 자동)이 묶음 B 로 도는지 **미리 검증**: `NIGHT_DATE=2026-09-20 bash scripts/nightly-split.sh --dry` 같은 마른 판(없으면 만든다 · 계측기) 으로 그 밤의 목록을 찍어 **48종이 세 밤 안에 전부 한 번씩** 드는지 표(빠지는 종 0 · `e2e-thirst` 혼자 한 청크 그대로). ② **부하 거짓 빨강 넷** — `conn`·`fogray`·`mtfuzz`·`trade` 가 09-18 빨강(러너 연속) · 09-19 미측정. 단독으로 각 두 번(한가한 상자) → 초록이면 "부하 거짓 빨강 후보" 표 · **두 밤째 빨강이면 `nightly-known` 에 올린다** 는 규약(T304 ③)을 기계가 세게: `nightly-report.js` 가 **직전 밤 보고를 읽어 연속 빨강 수**를 한 칸 더 낸다(문서 하나 읽기 · 판정은 안 한다 · "연속 n밤" 숫자만). ③ **벽시계 단정 전수 감사** — `test-harness-lint ⑧a` 를 `scripts/test-*.js`·`e2e-*.js` 전수에 돌려 "잰 시간 vs 상수" 단정을 표(파일:줄 · 상수) · 러너에서만 빨개질 자리 · **고치지 않는다**(각 하네스 주인 세션에 한 줄씩 · 표만 · PM 이 나눈다) · 단 ⑧a 자체가 `per`(단가) 와 총 시간을 가르는지 확인(T301 의 `per < 2000` 은 단가라 자로 남겼다 — 린트가 그걸 빨갛게 하면 린트가 틀린 것). ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 제품 0(`scripts/nightly-*`·`test-harness-lint`·`nightly-known.txt` 만) · `test-nightly` 초록 + 마른 판 절 하나 · `test-harness-lint` 42 그대로(+ 전수 표는 계측 출력) · `run-regress.sh --selftest` · 접점 심볼(`nightly-run.sh|nightly-split.sh|nightly-report.js|nightly-known.txt|regress.log.state|test-harness-lint|⑧a`).

## 0. §0 실측 (넷)
ⓐ 09-19 판 기계 사용 여부 + 오늘 밤 목록 마른 판(48종 세 밤 배치 표). ⓑ 넷 단독 두 판 + 연속 빨강 칸. ⓒ 벽시계 단정 전수 표. ⓓ 한 줄 답.

## 1. 절대 규칙
제품 0 · 새 수 0 · 러너 전수 안 돌린다(단독·마른 판만) · 망고링고 예약 작업 무접촉(프롬프트는 재민이 v3 로 바꿨다 · 기계가 맞으면 그대로 돈다) · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/night-split-0919` → main ff · 403 이면 `~/Mini/_inbox/`) · force 0.

## 2. 작업 (넷)
① 묶음 기계 ② 거짓 빨강 넷 ③ 벽시계 감사 ④ 한 줄 답.

## 3. 검증
`test-nightly` · 린트 · selftest · 접점 목록.

## 4. 회부만
벽시계 단정 고치기(주인 세션 · PM 배분) · rumor 경주(Z · 세션7 T310 뒤) · 굶긴 상자 장착 층(R2 · PM).

## 5. 완료
커밋 [승인 대기 · T314] · 가지 푸시 · `보고/T314_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> — 09-19 기계 … · 세 밤 48/48 · 넷 … · 단정 n자리". 실기 0.
