# 구현 지시 — T303: 소리 자산 정리 — `wolf_growl` → `wolf_howl` 개명 · `harvest`·`boar_grunt`·`hare` 비움 확정 · `ui_click` 없음 · 잠금·매니페스트·CREDITS 삼자 대조 유지 + `.dockerignore` 세 줄(#30 ⓒ) + `@cwasm/webp` 잠금 자(#32 ⓐ) (세션8/ART · T291 뒤 · 2026-09-18)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T303.md` · 레포 `지시/지시_T303.md`)

---

T291(main `b905c1f7` · 삼자 대조 하네스 — 잠기지도 크레딧에 적히지도 않은 소리가 배포되고 있었다) 통과 — 09-14 판정 그대로 · 재민 배포됨(09-18). **#34 결정(재민 위임 → PM)**: `wolf_growl` 은 파일이 울부짖음이니 **`wolf_howl` 로 개명** · `harvest`·`boar_grunt`·`hare` 는 **비움 확정**(출처 없는 소리 0 · 재민 녹음이 오면 그때) · `ui_click` 은 **없음**(UI 는 세계의 소리가 아니다 · 딱따구리 대안 폐기). **#30 ⓒ · #32 ⓐ.**

**카드 T303.** **PM 판정**(재민 거부권): ① 개명 — 파일·매니페스트 키·`CREDITS.md`·잠금표·세션9 표(`mobs` 표의 키 한 칸 — `48-a-audio.js` 는 **키 한 칸만** · 세션9 영역이라 그 한 칸 외 무접촉) · `test-audio`·삼자 대조 하네스가 새 이름으로 서고 옛 이름엔 문다(자명 통과 금지). ② 비움 셋 — 매니페스트 `missing` 로 확정(`pending` 아님 · T261 계약) · 대기 칸 0. ③ `.dockerignore` 세 줄(`public/assets/audio/bgm/*.py` · `*.zip` · `samples/`) — 지우지 않는다 · 배포물 크기 전/후 표(`docker build` 없이 `tar` 로 셈). ④ `devDependencies` `@cwasm/webp` 한 줄 + `asset-lock.js` webp 도 화소 해시(T260 후보 표 그대로 · 88장 sharp 대조 88/88) · `npm ci` 시간 전/후. ⑤ 답 한 줄. 하루. 질문 금지.

★검증 규약: 그림 무변(452 잠금) · 소리 파일 바이트 무변(개명은 이름만 · sha1 같음) · `asset-lock --check` 0 · `test-icons` · `test-assets-audit` · `test-audio` · 삼자 대조 · `test-harness-lint` · 3시드 면제 · **T292b(세션9 main) 리베이스** · 접점 심볼(`wolf_growl|wolf_howl|manifest.json|CREDITS.md|asset-lock|.dockerignore|@cwasm/webp|package.json|test-audio`).

## 0. §0 실측 (넷)
ⓐ 개명 diff + 하네스 두 팔. ⓑ 비움 셋 매니페스트. ⓒ dockerignore 크기 전/후. ⓓ webp 잠금 88/88 + `npm ci` 시간. + 한 줄 답.

## 1. 절대 규칙
소리 파일 바이트 0 변경 · 출처 없는 파일 0 · 지우기 0 · `package.json` 은 그 한 줄만 · 배포 재민만 · ★푸시 2판(맥 VM · 자산은 맥 트리 · 가지 `batch/sfx-tidy-0918` → ff → main) · force 0.

## 2. 작업 (넷+1)
① 개명 ② 비움 ③ dockerignore ④ webp ⑤ 한 줄 답.

## 3. 검증
잠금 · 하네스 다섯 · 접점.

## 4. 회부만
재민 녹음(harvest·멧돼지·토끼) · #29 6장(재민) · 다음 묶음(재민).

## 5. 완료
커밋 [재민 위임 2026-09-18 · T303] · main 푸시(맥 VM) · `보고/T303_<날짜>.md`(머리 15줄) · 채팅 한 줄. 실기(귀)는 재민.
