# 구현 지시 — T421: `rebuildSpatialIndex` 증분 + #54 p95 재실측 — T385 뒤 그 밖에서 두 번째로 큰 것이 `spatial` 0.206(격자 셋 통째 재생성 + 몹 449 삽입 · T371 회부 · T375 §4-4 · T385 §4-4 세 번 미룸) → 증분(움직인 몸만 셀을 옮긴다 · 안 움직인 몸은 제자리 · 몹 같은 문법) · 게이트 = 격자 조회 결과 비트 동일(T385 ⑨ 자 · 4,200틱 · 200만 조회) · 손잡이 끔 · 그리고 **#54 조건이 찼다**(T385 순회 · T394 A* 착지) → 몸 상한 40 → 무제한 켬 판 낮 p50/p95(T376 자 그대로 · 실제 날 길이 · 8/8) — PM 권고 "무제한" 의 마지막 입력 (세션7 · T385 뒤 · 2026-09-26)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T421.md` · 레포 `지시/지시_T421.md`)

---

T385(`batch/one-sweep-0925` → main · 의존 표 7×7 전부 "안 읽는다" — 막는 것은 자리(결정 문·이동 문 앞/뒤) · 앞 둘·뒤 다섯 = 7/7 합침 · 그 밖 1.00 → 0.72µs(−28%) · 게이트 비트 동일 · 외삽 1.0~1.2 엔 못 닿음(0.28 · T375 계수는 술어 든 바퀴였다) · `test-move-soa` 146/0) **통과** — "외삽은 잰 수가 아니었다"가 이 카드의 문장이다(족보 334). 켜기는 값이 섰으니 ★PM 위임: **`T385_ONE_SWEEP` 기본 켬**을 이 카드 ⓪ 에(게이트 그대로 · 되돌림 `=0`).

**카드 T421 — 넷. PM 판정**(재민 거부권): ⓪ **T385 켬** — 기본 켬 · 되돌림 `=0` · ⑨ 게이트 재확인 · 낮 창 값 한 줄. ① **`rebuildSpatialIndex` 증분** — 지금: 틱마다 격자 셋(주민·몹·?)을 비우고 전수 삽입(T371 · 몹 449 포함) → 증분: 몸이 셀을 넘을 때만 옮긴다(이동 문이 x·y 를 쓰는 그 자리 뒤 한 줄 · 셀 키는 정수 T333 문법) · 안 움직인 몸 0 비용 · 죽음/스폰/청크 끔은 지금 문 그대로 · 몹 같은 문법 · 손잡이 `T421_SPATIAL_INC` 기본 끔 · **게이트: 격자 조회 결과 비트 동일**(`nearest-search` 전부 · T385 ⑨ 자 · 4,200틱 · 정수 키 조회 200만 · 미끼: 한 몸의 셀을 안 옮기면 빨개진다) · 값: `spatial` 0.206 → n · 그 밖 0.72 → n · 존 틱 p50. ② **#54 p95** — T376 자(`t376-body-cap.js` · 실지도 51마을 · 200일 · 실제 날 길이) 무제한 팔을 **T385 켬 + T394 켬 + 이 카드 켬** 트리에서: 낮 p50·p95 · 예산 대비 · drop · lag · 소멸 · 몸 수 → T376 표(23.9ms · p95 31.3 = 94%)와 나란히 · 8/8 · 판정 0(재민 #54). ③ **몹 순회** — T385 가 주민만 했다(§4 회부) → 몹 루프(`npcs`) 같은 문법으로 한 바퀴 · 게이트 같은 자 · 값. ④ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 = 비트 동일 · `sim/` 무접촉 · `test-move-soa` 146 + 절 · `test-fish-act` 73 · `test-wood-act` 90 · `test-forage-act` 137 · `test-war-world` 116 · `e2e-move` 37 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`rebuildSpatialIndex|_spatial|nearest|T385_ONE_SWEEP|T421_SPATIAL_INC|npcs|t376-body-cap|VILLAGE_NPC_CAP|t371-tick-rest|test-move-soa`).

## 1. 절대 규칙
새 수 0 · 손잡이 하나(끔) · 게이트 다르면 켜지 않는다 · **T410(세션1 idle 문 · `zone.js` 틱 머리)·T399(세션3 A*) 무접촉** — `zone.js` 는 공간 색인·순회 자리만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/spatial-inc-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#54(재민 · ② 표) · #53 C 층(PM · 다음) · `stairCellCache`(T385 ③ 뒤 남은 것 있으면).

## 5. 완료
커밋 [승인 대기 · T421] · 가지 푸시 · `보고/T421_<날짜>.md`(템플릿 네 칸) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — ONE_SWEEP 켬 · spatial 0.206 → … · 그 밖 0.72 → … · 무제한 p95 …ms(…%) · 몹 한 바퀴". 실기 0.
