# 구현 지시 — T381: `pfFindPath` 한 번이 1.55ms 다(T370: A\* 갈래의 90.9% · 틱마다 1.2명) — 왜 1.55ms 인가를 갈라 잰다(탐색 셀 수 분포 · `maxCells 1500`·`searchRadiusCells 64` 에 닿는 비율 · 실패(null)하는 호출의 몫과 그 주민의 목표가 **물 건너**인가 — 재민 실기 "강에 대고 몸 비빈다 · 집으로 못 간다" 의 자리 · T370 추신) · 새 수 0 · 고칠 자리는 표가 정한다(후보: 실패 뒤 beeline 대신 서 있기 · 같은 (출발 셀·목표 셀) 쌍의 경로 재사용 · 도달불능 목표는 결정에서 거른다 — 전부 손잡이 뒤 · 기본 끔) (세션3 · T370 뒤 · 2026-09-23)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T381.md` · 레포 `지시/지시_T381.md`)

---

T370(`batch/path-reuse-0923` → main · 899 의 98.5% = "경로 끝" — 도착한 배회 주민이 다음 결정까지 매 틱 되묻는다 · 고치면 734 → 20.7/틱(`T370_PATH_REUSE` 기본 끔) · **★그런데 그 되묻기는 갈래의 6.6% 뿐 — 90.9% 는 틱마다 1.2명이 부르는 `pfFindPath` 한 번 1.55ms** · 반례: 못 가는 사람 안 생김 · 200일 −4.3% 는 판 편차와 못 가름(T326 끈 팔 둘이 9.4% 벌어진다) · `test-move-soa` 71/0) **통과** — "세지 않았으면 엉뚱한 자리를 팠다"가 이 카드의 문장이다(족보 296). T370 회부 1 이 이 카드다. **T370 추신**(재민 실기 09-23 "NPC 들이 강에 대고 몸 비비느라 집으로 못 간다" · `computeNpcPath` 가 `null` 이면 beeline)은 T370 보고에 안 실렸다 — 이 카드 ①에 넣는다.

**카드 T381 — 넷 · 순서가 카드다. PM 판정**(재민 거부권): ① **1.55ms 의 해부** — 낮 창(T370 자)에서 `pfFindPath` 호출마다: 탐색한 셀 수 · `maxCells` 1500 에 닿았나 · `searchRadiusCells` 64 에 닿았나 · 결과(경로/`null`) · 경로 길이 · 출발→목표 직선 거리 · **목표가 출발과 같은 뭍 덩어리인가**(T360 `t360-border` 실셀 BFS 자 그대로 — 물·바위로 갈린 목표 = "강 건너") · 주민 행동(`behavior`) · 마을 — 히스토그램: 셀 수 분포 · 닿은 비율 · null 비율 · **강 건너 목표 비율과 그 주민이 어디로 가려 했나(집? 현장?)** · 그 주민이 다음 5초 동안 물가에 서 있나(`_stuckPos` · "몸 비비기" 재현 수). ② **고침 — 표가 가리키는 자리 하나만**(새 수 0 · 손잡이 `T381_PATH_*` 기본 끔): ⓐ `null` 이면 beeline 을 넣지 않는다(쿨다운이면 있던 경로 유지 · 실패면 그 틱은 선다 · 목표 무변) ⓑ 같은 (출발 셀, 목표 셀) 쌍이 2초 안에 다시 오면 **그 결과를 재사용**(실패 포함 · 셀 단위 캐시 · 크기는 주민 수 · 새 수 0) ⓒ 강 건너 목표가 **결정**에서 나오면(집·현장이 물 건너 영토) 그건 이 카드 밖 — 표로 회부(V/Z · 영토가 강을 건넜다는 뜻). ③ **값** — 같은 창 켬/끔: `pfFindPath` 호출/틱 · 평균 ms · 갈래 µs/사람 · null 비율 · 물가 정지 주민 수 · 반례(도착률·도착 시간 중앙값 — T370 자) · 게이트: 끔 비트 동일(T350 4,200틱). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: `sim/` 무접촉 · `path-core.js` 무접촉(부르는 쪽만) · 끔 비트 동일 · `test-move-soa` 71 + 절 · `test-war-world` 85 · `e2e-move` 37 · `lab-wiring-check` 실패 0 · 접점 심볼(`pfFindPath|computeNpcPath|maxCells|searchRadiusCells|_lastAStarAt|_stuckPos|unstuckNpc|t360-border|t370-path-why|T381_PATH`).

## 1. 절대 규칙
새 수 0(1500·64·2000·5000 은 있는 수 · 안 바꾼다 — 바꾸려면 표에서 유도) · 손잡이 기본 끔 · 켜지 않는다 · A\* 알고리즘 무접촉 · **T375(세션7 틱 머리 `_active`)·T378(세션2 `clearTreesInCells`)·T364(세션10) 무접촉 — 리베이스 때 둘 다** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/path-cost-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · T370 회부 2 와 같이 — 같은 씨 두 판) · `wander` 도착 행동 없음(PM 설계 · T370 회부 3) · 강 건너 영토(V/Z · ①ⓒ 표) · SoA 순서 2·3 · C(#53) · #54.

## 5. 완료
커밋 [승인 대기 · T381] · 가지 푸시 · `보고/T381_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 해부 표 + 켬/끔**) · `인계/M-이동입력.md` 갱신 · 채팅 "가지 올렸다 <해시> — 1.55ms 의 주인 … · null … % · 강 건너 … % · 물가 정지 … → …". 실기: 재민이 강가 마을 한 바퀴.
