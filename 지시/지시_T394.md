# 구현 지시 — T394: 몸 비비기의 뿌리 둘 — ⓐ 주민이 태어날 때 받는 일터 도넛(`villages.js` 회관 + 180~320px)이 **지형을 안 본다**(주민 159/1,476 = 10.8% 가 물·바위 위 일터) ⓑ 생활 층이 결정을 넘기면 `decideNpcBehavior` ⑤ 가 그 일터 ±80 을 목표로 준다(막힌 목표의 89%) → 두 자리에 **같은 술어 하나**(A* 가 간선에 대는 그 함수 `isTerrainBlockedLocal` — 사본 0) · 그 뒤 **`T381_PATH_DEAD` 기본 켬**(★PM 결정 위임 · 3시드 무관 · 같은 씨 두 판 게이트) · `localPath` 끝점 막힘(T381 회부 2 · `path-core.js` 정본 · `routePath` 첫 줄과 같게) · 물가 정지 ~29명 전/후 (세션3 · T381 뒤 · 2026-09-25)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T394.md` · 레포 `지시/지시_T394.md`)

---

T381(`batch/path-cost-0923` → main · 1.55ms 의 주인 = "`maxCells` 1500 을 다 태우고 `null`"(호출 37% · 시간 93% · 한 번 6.42ms) · 그 절반은 목표가 애초에 못 가는 칸(물 48.4% · 강 건너 0) · `T381_PATH_DEAD` 끔으로 A* −48~56% · 틱 p50 −1.3~1.8ms(8/8 × 세 판) · `localPath` 끝점 비대칭 · **몸 비비기의 뿌리 = 물 위 일터**(주민 10.8%) · `test-move-soa ⑤` 잡음 바닥 자 수리 · `test-terraincache` 도 같이 고쳤는데 착지 때 PM 이 T383(세션4 · 그 카드의 주인) 판을 남기고 이 가지의 같은 자리 수리는 버렸다 — 결론은 둘이 같았다(귀속 T345)) **통과** — "1.55ms 의 주인은 1500칸을 다 태우고 null 이다"가 이 카드의 문장이다(족보 307). ★PM: T381 회부 4(켜기)는 **위임 판정으로 켠다** — 손잡이가 막는 것은 "못 갈 칸에 A* 를 안 부른다" 뿐이고 열린 목표 도착이 무변임을 세 판이 봤다. 단 **뿌리(회부 1)를 먼저 고치고** 켠다 — 뿌리가 남으면 몸짓만 바뀐다(T381 §4-5 가 그렇게 적었다).

**카드 T394 — 다섯 · 순서가 카드다. PM 판정**(재민 거부권): ① **일터 도넛에 지형** — `villages.js` 주민 태어나기의 `npcWorkX/Y`(회관 둘레 180~320px 도넛)에서 후보 점을 **A* 가 간선에 대는 그 술어**(`isTerrainBlockedLocal` · 존이 `deps` 로 주입하는 그것 · 사본 0 · 새 술어 0)로 거른다 · 막히면 같은 도넛에서 **씨 흐름**으로 다시 뽑는다(`Math.random` 0 · T350 씨 해시 · 시도 상한은 있는 수(`FOLLOW_CAP` 류) 아니면 도넛 둘레 셀 수에서 유도) · 전부 막히면 회관 문간(집 정본 `_actEntitiesAtCell` 이 쓰는 그 셀) · 표: 51마을 물·바위 일터 159 → n(0 이어야 한다 · 아니면 왜) · **몸(pid)·econ 무변**(일터는 몸의 좌표뿐 · 3시드 면제 근거를 한 줄로 — `villages.js` 를 만졌으니 **3시드 열 열 넷째 판 동일**을 그래도 낸다). ② **⑤ 배회의 목표** — `decideNpcBehavior` ⑤ 가 `일터 ± 80` 을 줄 때 같은 술어로 막힌 칸이면 **가까운 열린 칸**(T381 §1 "찾음 × 물 511" 이 끝나는 자리 — `findPath` 가 이미 아는 그 칸 · 새 탐색 0)으로 · 표: 막힌 목표 89% → n. ③ **`localPath` 끝점** — `path-core.js` 의 `localPath` 첫 줄을 `routePath` 첫 줄과 같게(끝점 막힘이면 `null` · 끝점 순서 비대칭 0) · 40쌍 비트 동일(T89 자) + 비대칭 미끼 · 랩 인라인 재생성(`inline-path.js --check`). ④ **`T381_PATH_DEAD` 기본 켬** — ①②③ 뒤에 · 게이트 = T381 §3 그 자(같은 씨 두 판 · 낮 창 · 8/8) · 열린 목표 도착 무변 · **물가 정지 ~29명 → n**(그게 재민 실기 ③의 수) · 끔 되돌림 = 이전 비트 동일. ⑤ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: **3시드 800일 열 열 넷째 판 동일**(`villages.js`·`path-core.js` 접촉) · `sim/path-core.js` 40쌍 비트 동일 · `inline-path`·`inline-engine --check` · `test-move-soa` 107 + 뿌리 절 · `test-fish-act` 73 · `test-wood-act` 90 · `test-forage-act` 137 · `test-war-world` 97 · `e2e-move` 37 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`npcWorkX|npcWorkY|isTerrainBlockedLocal|decideNpcBehavior|computeNpcPath|pfFindPath|localPath|routePath|path-core|T381_PATH_DEAD|_actEntitiesAtCell|seed-rand|t381-path-cost`).

## 0. §0 실측 (다섯)
ⓐ 물·바위 일터 159 → n + 3시드. ⓑ 막힌 목표 89% → n. ⓒ `localPath` diff + 40쌍 + 미끼. ⓓ 켬 게이트 + 물가 정지 29 → n. ⓔ 한 줄 답.

## 1. 절대 규칙
술어 사본 0(A* 의 그 함수) · 새 수 0 · `Math.random` 0 · beeline 안 넣는다(T381 §4-5) · **T385(세션7 순회 · `zone.js` 틱 본체)·T368(세션1 농부 · `villages.js` 밭 절)·T393(세션2 자원 절) 무접촉** — `villages.js` 는 태어나기 절만 · `zone.js` 는 ⑤·A* 자리만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/worksite-terrain-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 3시드는 `setsid nohup … & disown`)**.

## 2. 작업 (다섯)
① 도넛 ② ⑤ 목표 ③ `localPath` ④ 켬 ⑤ 한 줄 답.

## 3. 검증
3시드 · 40쌍 · 게이트 · 하네스 아홉 · 접점 목록.

## 4. 회부만
1500 × 뭍(같은 덩어리인데 못 닿는 목표 · T381 회부 3 · 다음 카드) · 200일 자 결정성(시계 손잡이 · PM · T350 §3-ⓒ) · C 층 접속(#53).

## 5. 완료
커밋 [승인 대기 · T394] · 가지 푸시 · `보고/T394_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 159 → n · 물가 정지 29 → n · 켬**) · `인계/M-이동입력.md` 갱신 · 채팅 "가지 올렸다 <해시> — 물 위 일터 159 → n · 막힌 목표 89% → n% · localPath 비대칭 0 · PATH_DEAD 켬 · 물가 정지 29 → n · 3시드 동일". 실기: 재민 배포 뒤 NPC 가 집에 돌아가는지.
