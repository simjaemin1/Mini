# 구현 지시 — T384: 띠는 클라 혼자 켠다 — T380 이 밝힌 대로 650~1,500px 띠의 나무는 **자료가 있는데 그리기가 버린 것**이라 서버 답(`_farOn`) 없이도 그릴 수 있다 · ⓐ 띠 = 기본 켬(세계 무변 · 방송 0 · 화소만) · ⓑ 원경 방송은 손잡이 그대로 · 회부 5(개체 컬링을 넓히는 길) 두 팔 프레임 ms 표 · **색인 문 17.8ms 해부 표**(술어 넷 중 누가 얼마) (세션5 · T380 뒤 · 2026-09-25)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T384.md` · 레포 `지시/지시_T384.md`)

---

T380(`batch/far-trees-0923` → main · 카드의 전제를 반으로 갈랐다: 활성화는 px 가 아니라 청크 색인(`ceil(1200/1024)=2` ⇒ 5×5 · 최소 2,048px)이라 **화면 안 나무는 전부 이미 클라에 있고** 비는 띠 650~1,500 은 `34-m-renderloop.js:663` 의 `VIEW_RADIUS=650` 이 **버리는 것** · 방송(`far_trees`)이 정말 쓰이는 곳은 큰지도뿐 · 겹침 0(낳은/든 청크 규칙) · 청크당 17.8ms 는 색인 문(T301)의 값 · `test-far-trees` 17/0 · 손잡이 끔 비트 동일) **통과** — "자료가 없다와 그리기가 버린다는 다른 병이다"가 이 카드의 문장이다(족보 302). ★PM 이 착지 때 본 것: `_farDraw` 는 `if (!_farOn) return` 이 ⓐ 띠 **앞에** 있다 — 서버가 `T380_FAR_TREES=1` 로 답한 적이 없으면 **띠도 한 그루 안 그린다**. 즉 재민 실기 ②("멀리서 나무가 안 보인다")는 아직 **안 닫혔다**: 띠는 서버가 몰라도 되는 일인데 서버 손잡이 뒤에 서 있다.

**카드 T384 — 셋. PM 판정**(재민 거부권): ① **띠 ⓐ 는 클라 혼자 켠다** — `38-r1-fartree.js _farDraw` 에서 `_farOn` 검사를 ⓑ(원경 `_farStore`) **앞으로만** 옮긴다: ⓐ(이미 가진 `conns[].resources` 의 나무 · `viewR` 밖 `tileR` 안)는 **항상** 그린다 · 서버 무접촉(요청 `_farRequest` 도 그대로 — 답이 없으면 ⓑ 가 비어 있을 뿐) · 세계 무변 · 개체 0 · 겹침 규칙(개체 상자 안은 건너뜀) 그대로 · **기본 켬**(화소가 바뀌는 것이 곧 이 카드다 — "끔 비트 동일" 절은 ⓑ 에만 남기고, ⓐ 는 "띠 안 나무 수 = 그린 수" 절로 바꾼다 · `__farOff(1)` 대조군은 ⓐ도 끈다 · 하네스 ⓑ9 갱신) · 값: 띠 1,437그루 · 1.239ms/f(T380 ⓑ13) 가 프레임 예산 어디쯤인지(`_natMs` 자연물 층과 같은 자로 나란히) · `_farCapV=4000` 상한에 띠가 닿는 프레임이 있나. ② **회부 5 두 팔 표** — 띠를 원경 층으로 메우는 길(①) 대 `VIEW_RADIUS` 를 1,500 으로 넓혀 **개체 컬링**으로 메우는 길(개체가 클릭·채집이 된다 · 그 비용을 개체 경로가 낸다): **같은 자리·같은 프레임 수**로 렌더 ms 두 팔(숲 한복판 · 마을 · 빈 초지 셋) · 값은 안 고른다(재민 #67) · 컬링 팔은 계측 팔에서만(제품 `VIEW_RADIUS` 무변). ③ **색인 문 17.8ms 해부 표** — T380 §4-2: 숲 청크 하나가 격자점 ~340개마다 `getForestMultiplier`·`isWaterCellLocal`·`isRockCellLocal`·`speciesAt` 넷을 묻는다 → 청크 하나(같은 청크 · 같은 씨)에서 **술어별 호출 수 × 회당 µs** 표 · 넷의 합이 17.8 에 닿는지(나머지 = "못 가른 것" 한 줄) · 어느 술어가 지형 비트(T333 상자)로 대체 가능한지 **표만**(고치지 않는다 — 다음 존 카드와 같은 값에 물린다 · #65 `europa` 앞의 입력). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(정본 JSON diff 0 · 개체 0 · `resources` 무접촉) · `server/` 무접촉(③ 계측기는 `scripts/t384-index-cost.js` 러너 밖 · 술어는 읽기만) · `30-n-net.js` 무접촉(T379 세션9) · `test-far-trees` 17 + ⓐ 절(띠 나무 수 = 그린 수 · 대조군 0) · `test-client-globals` 26(최상위 실행문 무변 · T0-b) · `e2e-move` 37 · `test-harness-lint` 61 · 접점 심볼(`_farDraw|_farOn|_farStore|_farCapV|__farOff|VIEW_RADIUS|TILE_RENDER_RADIUS|_natMs|getForestMultiplier|isWaterCellLocal|isRockCellLocal|speciesAt|generateChunkResources|overflowInto|t384-index-cost`).

## 0. §0 실측 (넷)
ⓐ 띠 켬 diff + 프레임 ms 표(띠 대 자연물 층). ⓑ 두 팔 표(셋 자리 × 렌더 ms). ⓒ 색인 문 해부 표(술어 넷 · 합 대 17.8). ⓓ 한 줄 답.

## 1. 절대 규칙
세계 무변 · 새 수 0 · `VIEW_RADIUS`·`T380_FAR_MAX`·`__farScale` 값 무변(재민 #67) · ⓑ 원경 손잡이 무변(끔) · **T378(세션2 `clearTreesInCells`)·T381(세션3)·T387(세션9 클라 오디오) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/far-band-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 띠 켬 ② 두 팔 표 ③ 색인 문 해부 ④ 한 줄 답.

## 3. 검증
diff 0(세계) · 하네스 다섯 · 접점 목록.

## 4. 회부만
#67 값 넷(재민 · `T380_FAR_MAX` 8192 · `__farScale` · `VIEW_RADIUS` 650 · 활성 반경 1,200) · 원경 방송 켜기(재민 · 17.8ms 내린 뒤) · 색인 문 고치기(PM · ③ 표 뒤 · `europa` 앞) · Safari 실기(재민).

## 5. 완료
커밋 [승인 대기 · T384] · 가지 푸시 · `보고/T384_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 띠 ms 와 17.8 해부 한 줄**) · `인계/R1-지형렌더.md` 갱신 · 채팅 "가지 올렸다 <해시> — 띠 켬 n그루 …ms/f · 두 팔 …/… ms · 색인 문 술어 넷 …/17.8". 실기: 재민이 배포 뒤 멀리 나무가 보이는지.
