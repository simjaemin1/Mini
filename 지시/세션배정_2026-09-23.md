# 세션 배정 — 2026-09-23 (PM 정본 · 카드 T366 표 그대로 · main `dfe59628`)

각 세션 채팅에 **아래 한 줄**만 붙여넣으면 된다. 카드 전문은 `~/Mini/지시_T◯◯◯.md` 와 레포 `지시/지시_T◯◯◯.md`(정본 · 세션이 `git pull --ff-only origin main` 뒤 읽는다).

| 세션 | 모델 | 카드 | 붙여넣을 한 줄 |
|---|---|---|---|
| 1 | Opus | **T359** | `git pull --ff-only origin main` 뒤 `지시/지시_T359.md` — 군락은 마을 계획이 아니라 지형의 것: 덤불·풀 군락을 나무처럼 `generateChunkResources` 가 지형(숲 덮임·물가)에서 낳게 · 밀도는 T357 필요 K 를 생활권 면적으로 유도(새 수 0) · 손잡이 `T359_GROVE_TERRAIN` 기본 끔 · 켬 판 개체 수·채집 30일 등가·사람당 µs. |
| 2 | Opus | **T355** | `git pull --ff-only origin main` 뒤 `지시/지시_T355.md` — T349 남은 구멍 둘: 존 게이트 `waitHttp(/health)` 정본화(표 먼저) · 단발 `fetch` ~56자리에 마감(`AbortSignal.timeout` 5000 · 새 수 0) · 오늘 밤 묶음 판 전/후 한 줄 대조 · `e2e-preserve` 경계 어긋남은 표만. |
| 3 | Opus | **T356** | `git pull --ff-only origin main` 뒤 `지시/지시_T356.md` — 사람당 5.48µs 해부: 갈래 여섯(결정·걸음·A*·틱 본체 순회·델타 직렬화/방송·생활층) 사람당 µs 를 각각 잰다 · SoA 1층(T352 계약 초안 확정 · 걸음 갈래만 열 순회 · 셀당 비트 · 바이트 동일 게이트 4,200틱) · 새 수 0. |
| 4 | Opus | **T361** | `git pull --ff-only origin main` 뒤 `지시/지시_T361.md` — 집도 행위다: 설계 표 먼저(구현은 T356 뒤) · 자재(목재·석재·짚) 단 수 유도 · 곳간↔터 왕복 걸음 문법 · 완공 게이트(자재 완착=완공) · 지금 두 팔과 대조표 · 계측기 하나 · 제품 무접촉. |
| 5 | Opus | **T360** | `git pull --ff-only origin main` 뒤 `지시/지시_T360.md` — 닛폰 "대양 분리"의 정체: 경계가 물인가 바위인가(실셀 BFS 로 지도에 찍는다) · 바위(산맥)면 고개(`passes` 정본)로 · 물이면 최단 도하 폭과 "200셀" 규칙 출처 · 한반도 광산2 도 같은 자로 · 세계 무변 표만. |
| 6 | Opus | **T362** | `git pull --ff-only origin main` 뒤 `지시/지시_T362.md` — `consumeFood` 에 `_cons` 한 줄: 식량 소비가 `_consEMA` 에 잡히게(T337: 0/51 은 구조 결함) · 끔=넷째 판 비트 동일 · 켬 팔 3시드 800일 열 열 + 게시 열(식량 의뢰 수) · 켜지 않는다. |
| 7 | Opus | **T363** | `git pull --ff-only origin main` 뒤 `지시/지시_T363.md` — `/check_username` 닫기: 이름 존재 노출 엔드포인트를 지우고 가입 응답에서만 "이미 있는 이름" 안내(#48 PM 결정) · `central-client.js:71` 호출자 표 · `test-doors` 문 하나 줄기 · T356 대기 중 작은 카드. |
| 8 | Opus | **T353** | `git pull --ff-only origin main` 뒤 `지시/지시_T353.md` — BGM 셋째 층: 음 단위에서 프레이즈 곡선으로(`sigim()` 센트 값을 f0·음량 곡선으로) · "곡선→소리" 어댑터 하나(A=지금 `gugak.py` · B=DDSP) · `tools/ddsp/` 학습 스크립트 · 데이터 표(보유 vs 필요) · A/B 파일 — 배포 자산 13곡 무변. |
| 9 | Opus | **T358** | `git pull --ff-only origin main` 뒤 `지시/지시_T358.md` — T354 가 못 한 넷: `deer_call`·`wind` 재확보 · `bronze_hit`→`rock_hit` 가르기(직접 녹음 회부 표) · `axe`·`tiger_growl` 변주 파일 붙이기 · 이음새 자를 하네스로(루프 끝↔머리 비 < 0dB 게이트) · `'wait'` 낱말 서버 한 줄(`cast` 제자리) — sfx 위임 계속. |
| 10 | Opus | **T364** | `git pull --ff-only origin main` 뒤 `지시/지시_T364.md` — 지나가는 군대를 옆 마을이 본다: T_마을 = 경보 거리 안 모든 군대의 max(목표 여부 무관 · T329 회부②) · 행군로 A\* 가 숲을 안 본다(T329 회부③) · w 셋은 고름 유지 · 3시드 비트 동일. |
| 11 | **Sonnet** | **T366(이것)** | `git pull --ff-only origin main` 뒤 `지시/지시_T366.md` — [Sonnet] 배정표·README 09-23 갱신 · 인계/README.md 영역 표에 sfx 위임 한 줄 + 머리에 공통§6 턴 규약 한 줄 · 보고 머리 15줄 점검 둘째(T347~T357 열한 장 · 위반 표만). 판정 0 · 코드 0. |
| 12 | **Sonnet** | **T365** | `git pull --ff-only origin main` 뒤 `지시/지시_T365.md` — [Sonnet] 빠진 소리 138자리를 사건 종류별로 묶어 우선순위 초안 표(판정 0) — `문서/빠진소리_전수_2026-09-23.md`(자리 168 · 있음 30 · 없음 138) 읽어 갈래 여섯(걷기 표면·도구/작업·동물·사람/전투·환경/날씨·UI/알림)으로 · "첫 10분에 듣는가" 열 · 실기표 키 없음 6항(T332)과 겹치는 자리 표시. |

* 이 표는 카드 T366 본문에 PM 이 이미 적어 준 09-23 배정을 그대로 옮긴 것이다 — `~/Mini/태스크보드_v1.md`(정본 §5 `grep "진행("`)는 이 기기에도 없다(T313 낮 갱신 때와 같은 결손 · 카드 자신이 이를 알고 표를 본문에 박아 줬다).
* 세션1~10 의 "붙여넣을 한 줄"은 각 카드 첫 줄(H1)을 세션11 이 요약한 것이다 — 원문은 반드시 `지시/지시_T◯◯◯.md` 전문으로 확인한다(요약은 안내일 뿐 정본이 아니다).
* T332(실기표 키 24/30 · 총 30항) **통과** — 카드 T366 본문에 PM 이 이미 적었다.
