# 구현 지시 — T430: BGM 정본 파이프라인 한 장(README) — T422 뒤 `tools/` 남는 10 의 자리(정본 옆 2 · 아직 모름 4 · 참고 4)가 표로만 있고 **"13곡은 어디서 어떻게 구워지나"가 한 페이지에 없다**(T353 → T391 복원 → T401 11곡 정본 없음 → T411 문턱 → T422 보관소 — 다섯 보고를 이어 읽어야 안다) ⇒ ① `bgm/README-BGM.md` 를 **정본 한 장**으로(입력 = 어느 폴더 · 굽는 명령 하나 · 출력 = 배포 13곡 · 바이트 재현 2/13 과 11곡의 사정 · 재굽기 금지 조건 · 보관소는 배포 밖) ② **`daegeum-transitions` 권리 표**(T422 "아직 모름 4" 중 재민 판정이 필요한 것 — 출처 · 라이선스 · 게임 안 표기 · 설계 11장 어느 후보인지) ③ **브금판 세 판을 한 페이지에**(#66 프레이즈 A/B · #70 무표기 재발음 A/B · #72 11곡 되찾음/다시 굽기/그대로 — `/bgm-board.html` 에 이미 있는 행을 **재민이 한 자리에서 한 글자씩** 답하게 · 새 행 0) ④ 한 줄 답 (세션8/ART · T422 뒤 · 2026-09-26)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T430.md` · 레포 `지시/지시_T430.md`)

---

T422(`batch/tools-archive-0926` → main · 14 폴더 `git mv` · 56 이름바꿈 · 경로 11줄(로직 0) · 24 폴더 자기 시험 230 중 229 그대로 · 남는 10 = 정본 옆 2(`score-expression` · `bgm-samples` — 맥에서 돈다 · 백업 4묶음 해시 일치) · 아직 모름 4(`daegeum-vibrato-reference` #66 · `ddsp-gugak-public-runtime` 라이선스 · `expressive-synthesis` #64 ②③ · `daegeum-transitions` 권리·설계) · 참고 4 · `.gitattributes` export-ignore · `test-assets-audit ⑩`) **통과** — "`git mv` 는 로직을 안 바꾼다 — 경로 11줄이 전부다"가 이 카드의 문장이다(족보 346). ★PM: T422 ② 정본 자리 표는 판정 입력으로 받는다 — 판정(설계 11장 후보 3/참고/버림)은 재민·PM 몫이고 이 카드는 **그 판정을 한 자리에서 하게 만드는 한 장**이다.

**카드 T430 — 넷 · 문서와 판 · 코드 0. PM 판정**(재민 거부권): ① **`bgm/README-BGM.md` 정본 한 장** — 지금 파일은 T391·T411·T422 가 한 줄씩 덧댄 것이다 · 다시 쓴다(원문은 아래 `## 옛 문서` 로 남긴다 · 지우지 않는다): 입력(어느 폴더 · `bgm-samples` 121파일 · `score-expression`) → 굽는 명령 **하나**(T353 파이프라인 · Python · 기본값 = T391 복원값 · GPT 규칙은 옵션 손잡이 이름) → 출력(배포 13곡 파일명 · 해시 표 · `bgm.js` 가 읽는 자리) → **바이트 재현 2/13** 과 11곡의 사정(T401: 07-31 코드로 같은 곡 0/11 · 반주 9/11 · 선율 0 — "지금 배포 11곡은 정본 코드가 없는 굽기 결과물이다"를 한 문장으로) → **재굽기 금지 조건**(재민 #72 답 전엔 배포 13곡 무변 · 문턱 = T411 같은 판 두 번의 차) → 보관소(`_rnd_archive` 는 배포 밖 · `git archive` 0) → `janggu_gung` 은 샘플(T411 정정) → 시험 넷 이름(`test-bgm-phrase` 16 · `e2e-bgm-phrase` 12 · `test-daegeum-transition-bank` 8 · `test-assets-audit`). 한 장 = 화면 둘 안. ② **`daegeum-transitions` 권리 표** — 폴더 안 파일마다: 출처(녹음/합성/GPT 생성 — `git log --follow` 첫 커밋) · 라이선스(파일 머리·README·없음) · 게임 안에서 쓰이나(`bgm.js`·`sfx/**` 참조 grep — 0 이면 "미반입") · 설계 11장 어느 후보(02·04·08)가 그것을 말하나 · **재민 판정 칸 하나**(들인다/참고/버린다 — 판정 0 · 칸만). `ddsp-gugak-public-runtime` 라이선스도 같은 표에 한 행(라이선스 파일 그대로 인용 · 15단어 안). ③ **브금판 한 페이지** — `/bgm-board.html` 에 #66(프레이즈 A/B) · #70(무표기 재발음 A/B) · #72(11곡 × 되찾음/다시 굽기/그대로) 행이 **이미 있다**(T401·T411) — 세 판이 화면 어디에 흩어져 있는지 표 · 한 자리(탭 하나 · 있는 문법)에서 재민이 위에서 아래로 한 글자씩 답하게 **모으기만**(새 행 0 · 새 소리 0 · 재생 버튼은 있는 것) · 답이 저장되는 자리(있는 `localStorage`/파일 문법 그대로) · 헤드리스 스크린샷 한 장 `~/Mini/산그림/T430_브금판.png`. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 배포 13곡 바이트 무변(해시 표 전/후) · `bgm.js` 무접촉 · `sfx/**` 무접촉(세션9) · 새 소리 0 · 새 행 0 · `tools/` 무접촉(T422 뒤 자리 그대로) · `test-bgm-phrase` 16 · `e2e-bgm-phrase` 12 · `test-daegeum-transition-bank` 8 · `test-assets-audit` PASS · `test-client-globals` 26 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`README-BGM|bgm-board|bgm.js|bgm-samples|score-expression|daegeum-transitions|ddsp-gugak-public-runtime|_rnd_archive|phrase.py|janggu_gung|test-daegeum-transition-bank|test-assets-audit`).

## 0. §0 실측 (넷)
ⓐ README 한 장(옛 문서 아래 보존) + 13곡 해시 표 전/후 동일. ⓑ 권리 표(파일마다 · 판정 칸 빈 채). ⓒ 브금판 한 자리 + 스크린샷. ⓓ 한 줄 답.

## 1. 절대 규칙
코드 0(브금판 HTML 의 자리 옮김만 · 새 행 0) · 배포 13곡 무변 · 새 수 0 · 새 소리 0 · 판정 0(칸만) · **T431(세션9 `sfx/**`)·T426(세션2)·T427(세션3) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/bgm-readme-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① README ② 권리 표 ③ 브금판 한 자리 ④ 한 줄 답.

## 3. 검증
해시 전/후 · 하네스 여섯 · 접점 목록.

## 4. 회부만
#66·#70·#72(재민 · 브금판 — 이 카드 ③ 이 그 자리) · `daegeum-transitions` 판정(재민 · ② 표) · 설계 11장 후보 3 판정(재민·PM · T422 ② + 이 카드 ②) · #64 ②③(재민 · 네 손).

## 5. 완료
커밋 [승인 대기 · T430] · 가지 푸시 · `보고/T430_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T430]` · 베이스 해시**) · `인계/ART-*.md` 갱신 · 채팅 "가지 올렸다 <해시> — README 한 장 · 13곡 해시 동일 13/13 · 권리 표 n행(판정 칸 빈 채) · 브금판 한 자리(#66 n · #70 n · #72 11) · 그림". 실기: 재민 배포 뒤 `/bgm-board.html` 한 자리에서 세 판.
