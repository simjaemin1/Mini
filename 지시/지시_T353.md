# 구현 지시 — T353: BGM 셋째 층 — 표현을 음 단위에서 **프레이즈 곡선**으로(`sigim()` 이 내는 센트 값을 프레이즈 하나의 f0·음량 곡선으로 잇는다 · 손잡이 기본 끔 = 13곡 float 배열 바이트 동일) · 지속음 악기 렌더에 "곡선 → 소리" 어댑터 하나(백엔드 A = 지금 `gugak.py` · 백엔드 B = DDSP 모델) · `tools/ddsp/` 학습 스크립트(재민 4060·WSL2 한 줄 · 컨테이너는 30초 CPU 스모크만) · 데이터 표(보유 vs 필요 · 출처·라이선스) · `village_day_ari` 옛 판/곡선 판 A/B 파일 — 배포 자산 13곡 무변 (세션8/ART · 재민 09-22 · 2026-09-22)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T353.md` · 레포 `지시/지시_T353.md`)

---

재민 09-22: **"지금은 국립국악원에서 받아온 음원을 잘라내서 붙여넣는 방식인데, 엄청나게 어색하다 … 각 악기 소리의 맵시를 학습하고, 비브라토의 맵시도 학습해서 평음→비브라토 또는 그 반대를 자연스럽게 연결하면서 작곡되도록"** · "완전 무에서 유를 만들고 싶지는 않고, 아리랑 등 국악 멜로디를 일부 참고" · "절차형 브금 … 융합 가능해?" → PM: 융합이 아니라 **층 갈아끼우기**다. 지금 `public/assets/audio/bgm/` 은 이미 세 층이다 — 작곡(`compose.py`·`motif.py` 변형기 · `arirang.py` 본조 아리랑 16마디) → 표현(`motif.sigim()` 이 (농현 센트, 꺾기 센트, 주법 이름) 을 낸다 · 규칙 ㉠~㉥) → 합성(`gugak.py` numpy 합성 + 가야금 국악원 조각 262 이어붙이기) → 13곡 오프라인 → `bgm.js` 표. **작곡 층은 그대로 둔다**(참고 선율 + 변형 · 결정적). 어색함의 출처는 둘 — 지속음 악기가 합성음인 것 · **음마다 따로 렌더해서 곡선이 음 경계에서 끊기는 것**(`daegeum(freq,dur)` 한 음 한 호출 · 농현은 고정 4.4Hz 사인). 이 카드는 **표현 층을 프레이즈 단위로** 잇고 합성 층에 DDSP 를 꽂을 자리를 만든다. 학습은 재민 데스크톱(RTX 4060 · WSL2)에서 한 줄 — 세션은 굽지 않는다(컨테이너 GPU 0 · 학습 값을 지어내지 않는다).

**카드 T353 — 다섯. PM 판정**(재민 거부권): ① **프레이즈 곡선** — 작곡 층이 내는 음표열을 프레이즈(악구 · `role == "cadence"` 까지)로 묶어 **f0 곡선(센트 · 프레이즈 길이 샘플) + 음량 곡선 하나**를 만든다: 음 안은 `sigim()` 이 이미 내는 농현 깊이·꺾기 값 그대로(**새 수 0** — 26·10·18·20 등 규칙 표의 값이 곡선 진폭 · 속도 4.4Hz 는 지금 상수 그대로 두고 회부) · 농현은 램프 붙은 사인(지금 `gayageum()` 의 `ramp` 꼴을 프레이즈로 올린다) · 음과 음 사이는 **끊지 않고 글라이드**(길이는 소박의 비율 — 값은 `gugak.py` 가 이미 쓰는 어택/릴리즈 초에서 유도 · 유도 표) · 추성/퇴성/전성은 곡선의 봉우리·경사 · 짧은 음(㉥) 은 곡선 평평. 손잡이 **`BGM_CURVES` 기본 끔**(환경변수 · 끔 = 지금 경로 그대로 · **13곡 `compose` float 배열 해시 동일**이 게이트 — `render-meta.json` 이 재는 그 배열 · 한 곡이라도 다르면 빨강). ② **"곡선 → 소리" 어댑터 하나** — 지속음 악기(`daegeum`·`danso`·`piri`)만: `render_curve(inst, f0_cents[], loud[], sr) → float32[]` 인터페이스 하나 · 백엔드 A = 지금 `gugak.py` 합성기가 곡선을 받게(가변 리샘플 `cents_fn` 이 이미 있다 — 그 자리) · 백엔드 B = DDSP 모델 파일이 `tools/ddsp/models/<inst>/` 에 **있으면** 그것(없으면 A · 조용히 A 로 떨어지지 않는다 — 어느 백엔드가 울었는지 렌더 로그 한 줄). 가야금·타악은 무접촉(뜯는 악기·타악은 샘플이 맞다 — 회부). ③ **`tools/ddsp/`** — `prep.py`(wav 폴더 → 단선율 검사(다성 구간은 버리고 표에) · 무음 자르기 · 16kHz · f0(CREPE 또는 pyin — 하나 골라 근거 한 줄) · 라우드니스 · 조각 4초) · `train.sh <inst> <wav폴더>`(venv 생성 · 의존성 **핀** · GPU 자동 감지 · **GPU 없으면 30초 스모크 모드**(전 과정이 돈다는 것만 · 모델 값 0) · Magenta DDSP(TF) 또는 PyTorch 재구현 중 **하나** — 설치 가능성·유지 상태로 고른 근거 표) · `export.py`(학습 산출 → ② 가 읽는 꼴 · 크기) · `README.md` 한 장: 재민 순서 **넷** — `wsl --install -d Ubuntu` · NVIDIA 윈도우 드라이버 최신(WSL CUDA 는 그 안에 있다 · 우분투 안에 따로 안 깐다) · 우분투에서 `nvidia-smi` 가 4060 을 보인다 · `bash tools/ddsp/train.sh daegeum <폴더>` — 예상 시간은 **적지 않는다**(안 재봤다). 컨테이너에서 스모크 한 판 초록을 보고에. 산출물(모델·venv) **커밋 0**(`.gitignore`). ④ **데이터 표** — 보유: 정악대금 국악원 「단음 다운로드」 5파일 → 조각 113(초 수 · 단음 = 음색은 되고 **농현 맵시는 없다**) · 산조가야금 21파일 · 그 밖 `_sfx_in/` 에 있는 것 전부(레포 밖 · 세션8이 안다) / 필요: DDSP 는 **연속 프레이즈 10~20분 단선율**(반주 0) / 후보 출처: AI Hub 「국악 악보 및 음원 데이터」(71470 · 대금·피리·해금 클립 · MIDI+WAV 쌍 · 시김새 라벨 · 국내 승인 · 상업 가능 — **다운로드는 재민**(승인 계정)) · 국립국악원 아카이브 독주 음원(라이선스 칸을 비워 두고 재민이 확인) · 표에 "재민이 받을 것" 열. **음원 원본은 레포 밖**(`_sfx_in/` 규약) · `CREDITS.md` 무접촉(새 음원이 이 카드에 들어오지 않는다). ⑤ **A/B 파일** — `village_day_ari` 를 ⓐ 지금 경로 ⓑ `BGM_CURVES=1` + 백엔드 A ⓒ (모델이 있을 때만) 백엔드 B 로 굽고 `~/Mini/_bgm_ab/T353_<ⓐⓑⓒ>.wav` 에(레포 밖 · **배포 13곡 .ogg/.m4a 무변** · `render-meta.json` 무접촉) · ffmpeg 실측 표(초·RMS·피크 — T306 자) · 판정은 **재민 귀**(소리판 아님 · 파일 셋). ⑥ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 = 13곡 float 배열 해시 동일(새 절 `test-audio` 하나 · 정적 아님 · `compose` 를 실제로 돌려 잰다 — 러너 시간이 무거우면 `village_day_ari` 한 곡만 러너에 · 13곡은 보고에) · `test-audio` 105 · `test-assets-audit` 초록 · `test-harness-lint` 47 · 제품(`public/client/**`·`bgm.js`·`bgm-loops.js`) diff 0 · 배포 자산 diff 0(`git diff --stat origin/main -- 'public/assets/audio/bgm/*.ogg' '*.m4a' render-meta.json` 빈 출력) · 접점 심볼(`sigim|SIGIMSAE|cents_fn|daegeum|danso|piri|render_curve|BGM_CURVES|tools/ddsp|prep.py|train.sh|export.py|village_day_ari|_bgm_ab`).

## 0. §0 실측 (여섯)
ⓐ 프레이즈 곡선 유도 표 + 끔 해시 동일 13/13. ⓑ 어댑터 인터페이스 + 백엔드 로그. ⓒ `tools/ddsp/` 스모크 초록 + 선택 근거. ⓓ 데이터 표(보유/필요/출처/라이선스/재민이 받을 것). ⓔ A/B 파일 셋 + ffmpeg 표. ⓕ 한 줄 답.

## 1. 절대 규칙
새 수 0(규칙 표 값 · 유도만 · 4.4Hz 회부) · 작곡 층 무접촉(`compose.py` 선율·`arirang.py`·장단 표) · 가야금·타악 무접촉 · 배포 자산 무변 · 음원 원본 레포 밖 · 산출물 커밋 0 · 학습은 재민(세션은 스모크만 · 값 지어내지 않는다) · 배포 0 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/bgm-curves-0922` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · `[작업 중]` 커밋으로 채팅 보고 금지 · 렌더가 길면 `setsid nohup … & disown`)**.

## 2. 작업 (여섯)
① 프레이즈 곡선 ② 어댑터 ③ `tools/ddsp/` ④ 데이터 표 ⑤ A/B ⑥ 한 줄 답.

## 3. 검증
끔 해시 · 하네스 셋 · 자산 diff 0 · 접점 목록.

## 4. 회부만
농현 속도 4.4Hz 상수(유도 또는 학습 · 재민) · 가야금 조각 이음(꼬리 피치벤드 · 뒤 카드) · 타악 합성 → 샘플(세션8 음원 확보 카드) · AI Hub 승인·다운로드(재민) · 국악원 아카이브 라이선스(재민) · 스템 렌더 + `bgm.js` 수직 레이어(세션9 · 뒤) · SejongMusic 정악판 성부(Z) · WSL2 Claude Code 세션13(반복 학습이 되면 · 재민).

## 5. 완료
커밋 [승인 대기 · T353] · 가지 푸시 · `보고/T353_<날짜>.md`(ⓐ~ⓕ · **머리 15줄에 결론**) · `인계/ART` 영역 파일 갱신 · 채팅 "가지 올렸다 <해시> — 끔 13/13 동일 · 어댑터 A(B 없음) · 스모크 초록 · 데이터 표 n행 · A/B 파일 ⓐⓑ". 실기: 재민이 `~/Mini/_bgm_ab/` 셋을 듣고 → 4060 학습 한 줄.
