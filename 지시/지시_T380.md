# 구현 지시 — T380: 멀리서 나무가 안 보인다(재민 실기 09-23) — 나무는 **활성 청크**(관측자 1,200px `CHUNK_ACTIVE_RADIUS`)에만 개체로 서고 그 밖은 없다 · 원경 나무 층: 서버가 **색인**(`resourcesAtCell` · T301 · 관측자 무관 · 활성화 0)으로 먼 청크의 나무 자리를 읽기 전용으로 내주고(`far_trees` 한 방송 · 청크 단위 캐시) 클라가 시야 밖~큰지도 배율까지 정적 층으로 그린다(개체 아님 · 상호작용 0 · 활성 청크에 들어오면 개체가 대신한다 — 겹침 0) · 세계 무변 · 손잡이 `T380_FAR_TREES` 기본 끔 (세션5 · T373 뒤 · R1/Z · 2026-09-23)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T380.md` · 레포 `지시/지시_T380.md`) **⚠T373 뒤에 붙인다.**

---

재민 실기 09-23: **"멀리서 나무가 안 보이는 이유는 뭐야?"** 답: 나무는 개체고 개체는 활성 청크에만 선다 — `zone.js:224 CHUNK_ACTIVE_RADIUS = 1200`(시야 650 + AOI 800 + 마진). 그 밖 청크는 꺼져 있어 나무가 **없다**(안 그린 게 아니라 없다). 줌 아웃·큰지도에서 숲이 비는 게 그 때문이다. 지형(`forests` 타원)은 정본에 있으니 원경은 **정본에서 유도**해 그릴 수 있다 — T301 색인이 관측자 무관으로 "그 셀에 무엇이 서 있나"를 답하고, T317 이 청크 밖 넘침까지 같은 답을 내게 했다. 개체를 세우지 않고 **자리만** 읽는다.

**카드 T380 — 넷. PM 판정**(재민 거부권): ① **서버** — 관측자 시야 밖 청크(활성 반경 밖 · 줌·큰지도 배율이 요구하는 반경까지 — 클라가 요청 반경을 보낸다 · 상한은 존 크기)의 나무 자리를 색인으로 읽어 `far_trees` 방송(청크 단위 · `{cx, cy, pts:[x,y,kind…]}` · 청크당 한 번 캐시 · 수확 장부가 바뀐 청크만 다시) · 활성화 0 · 개체 0 · `resources` 무접촉 · 부하 표(청크당 µs · 캐시 적중률). ② **클라** — 원경 층 하나(`32-m-render` 옆 새 파일 · 활성 청크 안은 그리지 않는다 — 개체가 대신 · 경계에서 겹침 0 · 안개 3단계 규격(fog-render-order) 그대로 · 큰지도(`80-bigmap`)도 같은 데이터로 숲 점) · 스프라이트는 지금 나무 스프라이트의 작은 판(새 그림 0 · 화소 자 T308). ③ **게이트** — 손잡이 기본 끔 = 방송 0 · 클라 비트 동일 · 켬: 활성 청크 안 나무 수 = 개체 수(겹침 0 · 하네스) · 원경 자리 = 색인 답(청크 셋 대조) · `e2e-fogray` 12 · `e2e-bigmap-live` · `test-client-globals` 26 · 부하 표. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(개체 0 · 정본 0) · `sim/` 무접촉 · 손잡이 끔 비트 동일 · 접점 심볼(`CHUNK_ACTIVE_RADIUS|resourcesAtCell|overflowInto|far_trees|T380_FAR_TREES|32-m-render|80-bigmap|fog|e2e-fogray|e2e-bigmap-live`).

## 1. 절대 규칙
개체 0(자리만) · 새 그림 0 · 손잡이 기본 끔 · 켜지 않는다 · **T378(세션2 `clearTreesInCells`)·T370(세션3)·T379(세션9 `30-n-net.js`) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/far-trees-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · 실기) · 원경 반경 상한(줌 배율 · 재민) · 활성 반경 1,200 자체(값 판정 · 재민).

## 5. 완료
커밋 [승인 대기 · T380] · 가지 푸시 · `보고/T380_<날짜>.md`(**머리 15줄**) · `인계/R1-지형렌더.md` 갱신 · 채팅 "가지 올렸다 <해시> — 원경 청크 n · 겹침 0 · 청크당 … µs". 실기: 재민이 줌 아웃·큰지도.
