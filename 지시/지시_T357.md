# 구현 지시 — T357: 군락 밀도 유도 — 세계가 비어 있다(T347: 군락 개체 마을당 약 5 · 걷는 몫 21~25% 을 실체가 2% 만 댄다)를 **수에서 유도한 필요 밀도**로 바꾼다 — 마을마다 채집 수식이 30일에 걷는 단 수 ÷ 개체 산출(w̄) ÷ 재생(r = 4h/K 의 K) = 필요 개체 K · 종류별(덤불·풀 + 실체 없는 넷: 버섯·나물·벌집·야생포도) · `plan-village-forage.js --dry` 로 자리 후보 표 · **세계 무변 · 표만** — #58(재민 결정: 세계를 수식에 맞추나, 수식을 세계에 맞추나)의 입력 (세션1 · T347 뒤 · 2026-09-23)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T357.md` · 레포 `지시/지시_T357.md`)

---

T347(`batch/forage-act-0921` → main · 채집 행위 층 — 나무꾼 문법 세 번째 적용(`actToGranary`·`_actEntitiesAtCell`·`_actTakeAtCell`·`_actRegrowR` 한 몸통) · 손잡이 `T347_FORAGE_ACT` 기본 끔 · 끔 3시드 넷째 판 동일(PM 재실측 3/3 열 열 전부) · **실체와 수식이 다른 품목을 말한다: 교집합 `twig`·`herb` 21~25%** · **등가가 안 선다 — 30일 비 1.2/1.9/2.4%: 세계에 군락이 마을당 약 5개** · 덤불 씨앗 주사위 → 자리 해시 · `test-forage-act` 86/0 · `e2e-forage-village` 12/0) **통과** — "문법은 섰고 세계가 비어 있다"가 이 카드의 문장이다(족보 282). ★PM: T347 이 `[작업 중]` 커밋에서 턴을 끝낸 사고가 공통 §6 규약이 됐다 — 이번 카드부터 그 규약 아래다.

T347 §7-(가)가 옳게 키웠다: 문제는 품목이 아니라 **밀도**다. 군락은 플레이어의 첫 15초를 위해 `plan-village-forage.js` 가 마을마다 딱 그만큼 심은 것(세계 53군락 · 개체 109 · `herb` 군락 0)이지 채집꾼 수백 명의 밑변이 아니다. 그걸 늘리는 것은 세계에 실체를 놓는 일이라 **재민 결정(#58)** 인데, 결정하려면 **얼마나** 가 있어야 한다. 이 카드는 그 수를 **유도**한다 — 짓지 않는다.

**카드 T357 — 넷 · 표만 · 세계 무변. PM 판정**(재민 거부권): ① **필요 밀도 유도** — 마을마다: 채집 수식이 30일에 걷는 단 수 D(T347 §3 의 그 수 · 1,360~1,915단 · 마을별) → 개체 하나의 하루 산출 w̄(T347 1.30 · 종류별로 갈라) → 지속 가능 채취 = 로지스틱 MSY h = rK/4 ⇒ **K = 4D/(30·r)** 꼴로 필요 개체 수 K 를 **T341 의 그 식**(`_actRegrowR` · r 는 나무가 쓴 그 값 — 종류별 r 이 없으면 나무 값 그대로 쓰고 "빌린 값" 이라 표에) 에서 뺀다 · 걷는 몫만(21~25%) 판과 (나) `berry→fruit` 동의어까지 판(45~49%) **둘 다** · 마을 51 × 두 판 표 · 합계와 지금 235 대비 배율. ② **종류 표** — 실체 없는 넷(`mushroom`·`vegetable`·`honey`·`grape`)에 군락 종류 이름 후보(버섯밭·나물·벌집·야생포도 — `chunk.js groves kind` 가 받는 꼴) · 각각 어느 지형(숲·물가·초지 — `decideVillageType` 이 읽는 그 판정)에 서는지 · 필요한 것(개체 · 스프라이트 · 전리품 표 · `weights.js` kg — `walnut` null 처럼 빈칸 목록) · **값은 안 짓는다**. ③ **자리 후보** — `plan-village-forage.js --dry`(없으면 `--dry` 를 만든다 · 세계 무변)로 마을마다 ①의 K 를 놓을 수 있는 셀 수(지형·간격 규칙 그대로)를 세어 **놓을 수 있나** 표(K 대 가용 셀 · 모자라는 마을) · 나무(37/마을)와 겹치는 셀은 제외. ④ 한 줄 답 + #58 에 낼 **두 길의 수**: ⓐ 세계를 수식에 맞춘다(K 개체를 심는다 — 총 개체·마을당·놓을 수 없는 마을 n) ⓑ 수식을 세계에 맞춘다(채집 믹스의 걷는 몫을 지금 235 개체가 대는 만큼으로 — 몇 %가 되나) · 판정은 재민. 이틀. 질문 금지 — 답은 표.

★검증 규약: **세계 무변**(`chunk.js`·`plan-village-forage.js` 산출·DB·정본 JSON diff 0 — `--dry` 만) · `sim/` 무접촉 · 새 코드 = `--dry` 갈래 + 계측기 `scripts/t357-grove-need.js`(러너 밖) · `test-forage-act` 86 · `test-trees` 91 · `lab-wiring-check` 실패 0 · 접점 심볼(`_actRegrowR|_forageOutLast|plan-village-forage|groves|kind|decideVillageType|weights.js|kgOf|t357-grove-need|forageActItems`).

## 0. §0 실측 (넷)
ⓐ 필요 밀도 표(51마을 × 두 판 · 합 · 배율). ⓑ 종류 표(넷 · 지형 · 빈칸 목록). ⓒ 자리 후보 표(K 대 가용 셀). ⓓ 한 줄 답 + #58 두 길의 수.

## 1. 절대 규칙
표만 · 세계 무변 · 새 수 0(유도만 · 빌린 값은 표에) · 손잡이 켜지 않는다 · **T356(세션3 `zone.js` 걸음)·T354(세션9)·T329(세션10) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/grove-need-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 필요 밀도 ② 종류 표 ③ 자리 후보 ④ 한 줄 답 + #58.

## 3. 검증
diff 0 · 하네스 셋 · 접점 목록.

## 4. 회부만
#58(재민) · (나) 동의어(PM · #58 뒤) · (다) `kgOf('walnut')`(표 주인 · PM 배분) · (라) NPC 씨앗 품목(밭 카드 · PM) · 스프라이트(세션8 ART · #58 뒤).

## 5. 완료
커밋 [승인 대기 · T357] · 가지 푸시 · `보고/T357_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 두 길의 수**) · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 필요 K 합 … (지금 235 의 …배) · 놓을 수 없는 마을 n · ⓑ 면 걷는 몫 …%". 실기 0.
