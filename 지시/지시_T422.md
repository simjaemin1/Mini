# 구현 지시 — T422: tools 버림 14 → `tools/_rnd_archive/` 이동 실행(T411 ③ 표 그대로 · ★PM 승인) — 옮긴 쪽 6폴더 시험이 옆 폴더 경로로 깨지는 것은 **한 줄씩** 고친다(경로만 · 로직 0) · 남는 10 폴더 시험 무변 · `test-daegeum-transition-bank.py` 8/0 · 제품 흔적(`ph…`) 표대로 · `.gitattributes`/`README-BGM.md` 한 줄 · 그리고 남는 10 중 "돈다 5" 의 **정본 자리 표**(어느 것이 T353 파이프라인의 정본 옆에 서고 어느 것이 참고인가 — 설계 11장 후보/참고/버림 3/6/2 판정 입력 · 판정은 재민·PM) (세션8/ART · T411 뒤 · 2026-09-26)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T422.md` · 레포 `지시/지시_T422.md`)

---

T411(`batch/bgm-tidy-0926` → main · ⑦⑧ 12/0(20/20 · 문턱 = 같은 판 두 번의 차 · 떨림은 셋 이상 더하는 순서) · `janggu_gung` 사실 정정(2곡 샘플 · 11곡 미상) · tools 24 = 돈다 5 · 안 돈다 n · 버림 14 표 · 스크래치 clone 에서 옮겨 봄 — 남는 10 무변 · 옮긴 6 경로 한 줄씩) **통과** — "문턱은 바닥 그 자체다"가 이 카드의 문장이다(족보 338). ★PM: 이동은 PM 몫이라 했으나 세션8 이 스크래치에서 이미 해 봤고 경로 한 줄씩이 필요하다 — **세션8 이 한다**(PM 은 착지 때 diff 로 본다 · `git mv` 만 · 내용 무변).

**카드 T422 — 셋. PM 판정**(재민 거부권): ① **이동** — T411 §③-3 표의 14 폴더를 `git mv` 로 `tools/_rnd_archive/<이름>/`(내용 무변 · 히스토리 유지) · 옮긴 6폴더 시험의 경로 **한 줄씩**(로직 0 · diff 는 경로 문자열만) · 남는 10 폴더 시험 전부 재실행 무변 · `tools/_rnd_archive/README.md` 한 장(왜 옮겼나 · T386·T411 표 인용 · "R&D 미확정 · 정본 아님" · 되살리려면 `git mv` 역) · 제품 흔적(`ph…` — T411 이 적은 그것)은 표대로 처리(제품 무변). ② **정본 자리 표** — 남는 10 × (T353 파이프라인 정본 옆에 서나 / 참고 / 아직 모름) · "돈다 5" 는 실행 결과 한 줄 · `설계/BGM_RND_*` 11장 후보 3 · 참고 6 · 버림 2(T386 표)와 **맞물림**(어느 문서가 어느 폴더를 말하나) · 판정 0. ③ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: `git mv` 만(`git log --follow` 가 이어진다) · 로직 diff 0(경로 문자열만 · 줄 수 보고에) · 배포 13곡·`bgm.js`·`sfx/**` 무변 · `test-audio` 170 · `test-bgm-phrase` 16 · `e2e-bgm-phrase` 12 · `test-assets-audit` PASS(+ `_rnd_archive` 가 배포 폴더 밖인 절) · `test-render-score-articulation` 14 · `test-daegeum-transition-bank` 8 · `test-harness-lint` 61 · 접점 심볼(`tools/|_rnd_archive|README-BGM|BGM_RND_|test-daegeum-transition-bank|t411`).

## 1. 절대 규칙
내용 무변 · 로직 0 · 새 소리 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/tools-archive-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
설계 11장 판정(재민·PM · ② 표) · #66·#70·#72(재민 · 브금판) · AI Hub·4060(#64 ②③) · GPT 다음 BGM 카드(PM · ② 뒤).

## 5. 완료
커밋 [승인 대기 · T422] · 가지 푸시 · `보고/T422_<날짜>.md`(템플릿 네 칸) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 옮김 14 · 경로 n줄 · 남는 10 무변 · 정본 옆 n · 참고 n". 실기 0.
