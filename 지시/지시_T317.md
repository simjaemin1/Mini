# 구현 지시 — T317: 넘친 나무를 세계의 것으로 — 청크가 자기 밖에 낳은 개체(7.49% · 전부 나무)를 이웃이 켜질 때도 있게(㉢ · `resourceAt` 문법) · 제거는 낳은 청크 기준(`seedKey`) · 3시드 · `test-resource-index` 절 셋 (세션5 · T309 뒤 · 2026-09-19)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T317.md` · 레포 `지시/지시_T317.md`)

---

T309(main `608f5a93` · 경계 갈래 8자리 중 결함 2 = 같은 한 가지(바구니 기준) · 넘침 933/12,461 = 7.49% 전부 나무 · 이웃은 안 낳는다 162/162 · 메모 자리 하나 `_warTerrMemo` · 현장: 나무는 거리 문제 · **광맥 없는 마을 43/51**) **통과** — "관측자 의존이 남아 있다 — T284 ①이 고치려던 바로 그것" 이 답이다(족보 244). 광맥 없는 43마을은 재민 칸(#47) · 메모 한 칸은 T295(세션10)가 붙인다.

**카드 T317 — ㉢ 완전 해소. PM 판정**(재민 거부권): 세계는 관측자와 무관하다(캐논). ① **켤 때 이웃까지** — 청크 C 가 켜질 때 C 안의 셀에 **이웃 8청크가 낳는 개체**를 `resourceAt`(T301 `onlyCell` 문법 · 청크 생성 함수 그 자체)로 물어 같이 놓는다 · 씨·순번 무변(세계 무변 — 300청크 생성물 바이트 동일이 자) · 중복 0(같은 개체가 두 바구니에 안 든다 — 낳은 청크 `seedKey` 가 주인). ② **제거는 낳은 청크 기준** — `zone.js:276` 바구니 기준 제거를 `seedKey` 기준으로(㉡) · 낳은 청크가 꺼져도 **이웃이 켜져 있으면 남는다** · 둘 다 꺼지면 사라진다. ③ **자** — `test-resource-index` 절 셋: ⓕ 이웃만 켠 판에서 넘친 나무 162/162 **있다** ⓖ 낳은 청크 끔·이웃 켬 → 남는다 · 둘 끔 → 0 ⓗ 중복 0 · 300청크 바이트 동일 · 3시드 800일 넷째 판 동일(`chunk.js` 는 랩이 적재한다 — T301 이 잡았다) · 부하: 청크 켜기 한 번당 +µs(이웃 8 × 경계 띠 셀 수 × 15µs 자리 셈 → 실측). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 3시드 비트 동일 · 게이트 셋 · `test-resource-index` 12 + ⓕⓖⓗ · `test-war-world` 30/0 · `test-hunt-vis` · 접점 심볼(`generateChunkResources|onlyCell|resourceAt|seedKey|chunkKey|activeChunkKeys|zone.js:276|chunk.js:56|_warTerrMemo`).

## 1. 절대 규칙
새 수 0 · 사본 0 · 손잡이 없음(결함 수리 · 세계 무변이 자) · **`villages.js` 전쟁 절(T295)·어부 자리(T316 세션3) 무접촉** · ★푸시 2판(가지 `batch/chunk-overflow-0919` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
광맥 없는 43마을(#47 재민) · 전쟁 세 줄+메모(T295) · 발자국 통짜(설계 §6).

## 5. 완료
커밋 [승인 대기 · T317] · 가지 푸시 · `보고/T317_<날짜>.md`(**머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> — 이웃 162/162 · 바이트 동일 · 3시드 동일 · +…µs". 실기 0.
