# 구현 지시 — T396: 기준선 자 하나 — T367 이 밝힌 대로 `t17-metrics`(econ 단독 · `Villages.init()` 0)는 4판(밭)을 못 재고 `t176-ab`(생활층 붙임)는 잰다 · 두 자가 넷째 판에서 **열 열 같은 수**를 내는지(T367 은 "반올림 자리만 다르다" 고 했다 — 그 차를 0 으로) · `t176-ab` 가 `t17-metrics` 와 **같은 열 출력·같은 인자꼴**(`800 <seed>`)이 되게 · 다섯째 판 3시드 열 열을 **그 자로** 표에(T367 표 재현) · 정본 `t17-metrics` 무변 · 러너 밖 · **판정은 재민(#69)** — 이 카드는 재민이 판정할 수 있게 자를 세운다 (세션6 · T367 보류 · 2026-09-25)

아래 전문을 **세션6** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T396.md` · 레포 `지시/지시_T396.md`)

---

T367(`batch/baseline5-0923` · **착지 보류**) — 손잡이 셋을 켰고 되돌림 셋은 넷째 판 바이트 동일 · 그런데 **카드의 자로는 이 판을 못 잰다**: 4판의 곳간 입구 `harvestToGranary` 는 생활층 `_lifeDoTask0` 한 곳이고 `t17-metrics` 는 밭 상태기를 안 돈다(호출 0회) ⇒ 거짓 기근(인구 −57% · 소멸 11~12/51) · 맞는 자 `t176-ab` 로는 인구 −11.7% · 소멸 1·0·1 · #12 단독은 세계를 조금 키운다(소멸 0·0·0) ⇒ 다섯째 판의 대가는 전부 #11(밭 4판)에서 온다 · 텃밭 하한이 밭 유입의 10.6~11.1%. 카드가 준 자의 함정을 **자기가 찾아 맞는 자로 다시 잰 것**이 이 카드다(족보 310). ★PM 이 착지를 보류한 이유 둘: ⓐ 기준선을 바꾸는 것은 **재민 승인**(T17 규약)이고 소멸 1·0·1 이 그 판정의 입력이다 ⓑ 3시드 게이트의 자가 다섯째 판을 못 잰다면 **자부터** 하나여야 한다 — 착지하면 다음 카드부터 모든 세션이 거짓 기근을 게이트로 본다. 이 카드가 ⓑ를 세우고 재민이 ⓐ를 판정한다(#69).

**카드 T396 — 넷 · 자만 · 제품 0 · 정본 무변. PM 판정**(재민 거부권): ① **두 자 대조** — 넷째 판(main 그대로 · 손잡이 전부 끔) 3시드 800일: `t17-metrics` 열 열(정본 · 공통 §3 표) 대 `t176-ab`(생활층 붙임 · `ENABLE_VILLAGES=1` 팔) 열 열 · **열마다 차**(T367 "반올림 자리만" 을 수로 · 차 0 이어야 한다 · 아니면 어느 열이 왜 — 생활층이 끄면 무해하다는 `t176-ab` 머리의 §0-ⓐ 검사 그대로) · 걸린 시간 둘. ② **같은 꼴** — `t176-ab` 에 `t17-metrics` 와 **같은 열 출력**(ⓚ "여덟 수" 한 줄 · ㉮㉯ 밀도 · 소멸 · 마을 표 — 같은 문자열 꼴이라 `diff` 로 대조된다) 과 **같은 인자꼴**(`node scripts/t176-ab.js 800 <seed>` · env 손잡이 그대로) · 새 계산 0(있는 자 둘을 붙인 것 그대로 · 출력 꼴만) · 정본 `t17-metrics` 무변. ③ **다섯째 판 열 열** — `batch/baseline5-0923` 을 **작업트리에서만** 얹어(main 무변 · 착지 아님) ②의 자로 3시드 800일 · T367 표 재현(인구 7,011/6,768/7,482 · 소멸 1·0·1 …) · 같으면 같다 · 다르면 그 차 · **#12 단독 팔**(`T195_TOOL_WEAR` 만 0.9) 도 같은 자로 한 줄(T367 은 `t17` 로 쟀다) · 소멸 둘(농촌9 · 농촌13)의 **왜**(T367 §에 있으면 인용 · 없으면 그 마을 30일 표 — 물 0.08 이 뿌리인지). ④ **한 줄 답 + #69 표** — 재민이 고를 것: ⓐ 다섯째 판 켠다/안 켠다(소멸 1·0·1 · 인구 −11.7%) ⓑ 기준선 자를 `t176-ab`(생활층 포함)로 바꾼다/안 바꾼다 · PM 권고는 **ⓑ 바꾼다**(캐논이 "생산은 행위"라면 기준선 자도 행위를 돌아야 한다 — 그 뒤 ⓐ) · 값 판정은 재민. 이틀. 질문 금지 — 답은 표.

★검증 규약: 제품 0 · `t17-metrics` 무변 · `t176-ab` 는 출력 꼴·인자꼴만(계산 무변 · diff 를 보고에) · 러너 밖(`@regress` 0) · main 무변(③은 작업트리 얹기 · 커밋 0) · 3시드 × (넷째 판 두 자 + 다섯째 판 + #12 단독) = 실행 12 판(`setsid nohup … & disown` · 캐시 `LAB_SEEDCACHE` 재사용) · `test-harness-lint` 61 · 접점 심볼(`t17-metrics|t176-ab|t165-ab|farm-metrics|Villages.init|ENABLE_VILLAGES|harvestToGranary|_lifeDoTask0|T100_FIELD_YIELD|T193_LEDGER|T195_TOOL_WEAR|LAB_SEEDCACHE|공통.md §3`).

## 0. §0 실측 (넷)
ⓐ 두 자 열 열 차(3시드 · 0 이어야). ⓑ 출력 꼴 diff. ⓒ 다섯째 판 열 열 + #12 단독 + 소멸 둘의 왜. ⓓ 한 줄 답 + #69 표.

## 1. 절대 규칙
제품 0 · 정본 자 무변 · main 무변 · 새 수 0 · 판정 0(재민 #69) · **T368(세션1 밭 절)·T394(세션3)·T385(세션7) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/baseline-ruler-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 두 자 대조 ② 같은 꼴 ③ 다섯째 판 열 열 ④ 한 줄 답 + #69.

## 3. 검증
차 0 · diff · 12 판 · 접점 목록.

## 4. 회부만
#69 둘(재민) · T367 착지(PM · #69 뒤 · 리베이스는 그때 세션6) · 텃밭 하한 `T100_GARDEN`(재민 · 4판의 남은 추상) · `t17-metrics` 자체에 생활층을 넣을지(PM · ② 뒤).

## 5. 완료
커밋 [승인 대기 · T396] · 가지 푸시 · `보고/T396_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 두 자 차 + 다섯째 판 한 줄 + #69 표**) · `인계/공통.md` §3 에 "자 둘" 한 줄(정본 표는 무변) · 채팅 "가지 올렸다 <해시> — 두 자 차 열 열 0/…  · 다섯째 판 재현 ○/✗ · 소멸 둘 왜 … · #69 표". 실기 0.
