# 구현 지시 — T340: 워커 분할 진단(코드 0) — 존 틱을 마을 묶음별 워커로 나눌 때의 경계 전수(누가 무엇을 읽고 쓰나 · 마을 간 접점 = 캐러밴·행군·플레이어·방송·econ 하루 틱) · 2워커 예상 배수 · `test-route-persist` 빨강 귀속(09-20 야간 · 미리 굽기 61초와 선계산의 경주인가) · NPC 어부 낚시 창(T316 추신 · 같은 함수) (세션3 · T333 뒤 · 2026-09-21)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T340.md` · 레포 `지시/지시_T340.md`)

---

T333(`batch/walk-cost2-0920` → main · 지형 17.9 → 0.1% · 문자열 키 → 비트 색인(초당 30만 쓰레기 0) · 어댑터가 메모를 지나치던 한 줄 · **사람당 4.79µs(낮 5.24 · 밤 3.78) · 10,000명 = 1.5배** · 남은 주인은 열 자리로 흩어졌다 · **800일 존 세계는 1,718명에서 포화**(주거 게이트 · 8,318 은 랩 수)) **통과** — "다음 배수는 한 곳을 고쳐서 못 얻는다" 를 프로파일로 보인 것(족보 264). 1,718 은 #54(재민·세션4 대조). 사람당 값은 바닥이다 — 그 다음은 코어다.

**카드 T340 — 셋 · 진단은 코드 0.** ① **워커 분할 경계 전수** — 존 틱 한 번의 쓰기 집합을 마을 단위로 가른다: 마을 안에서 닫히는 것(주민 걸음·생활층·곳간) / 마을을 넘는 것(캐러밴 페이싱 · 행군·전투 `simWar` · 플레이어 · 방송 `others` · econ 하루 틱 `tickWorldV2` · 쿼드트리 · 청크 활성화) — 자리마다 "워커 안 / 주 스레드 / 경계 메시지" 셋 중 하나 + 근거 · **결정론**(같은 씨 같은 결과 — 워커 간 순서가 결과에 드는 자리 전수) · 2워커·4워커 예상 배수(T333 프로파일 몫으로 셈 · 측정 아님이라 적는다) · 첫 카드 크기. **구현은 안 한다**(#53 재민 뒤). ② **`test-route-persist` 빨강**(09-20 야간 · 단독도 빨강 · ⑥ 선계산 562/579 남음) — 미리 굽기 61초가 선계산 창을 먹는 경주인지(`git log -3 -- scripts/test-route-persist.js` · 부팅 순서) · 하네스면 조건 대기(T322 문법) · 제품이면 표. ③ **낚시 창** — T316 추신: NPC 어부 시도에 플레이어 `biteAt`·`windowMs` **같은 함수**(사본 0 · 새 수 0) · 한 시도 = 던짐 → 창 → 입질/안 물림 → 걸림/놓침 · `test-fish-act` 절 하나(창이 실제로 시간을 먹는다 · 첫날 등가는 셀 예산이 지킨다 — 창 때문에 예산을 못 다 잡는 마을 수 표) · 세션9 `test-audio ⑫d` 전제가 바뀌므로 **그 절은 무접촉 · 회부**. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: ①③ 코드 = `villages.js` 어부 시도 자리 하나 + 하네스 · 끔 3시드 면제(`sim/` 무접촉이어야 한다) · `test-fish-act` 56 + 절 · `test-route-persist` 초록 · `test-terrain-memo` 36 · 접점 심볼(`biteAt|windowMs|fish_state|simCaravan|simWar|others|tickWorldV2|quadtree|activeChunkKeys|prebake|test-route-persist`).

## 1. 절대 규칙
워커 구현 0 · 새 수 0 · **T341(세션1)·T342(세션4)·T329(세션10)** 무접촉 · ★푸시 2판(가지 `batch/worker-audit-0921` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T340] · `보고/T340_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 경계 n자리 · 2워커 예상 … · route-persist … · 낚시 창 …".
