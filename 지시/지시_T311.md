# 구현 지시 — T311: T297 이 드러낸 자 결함 둘 — 고기 3분의 1 누수(`huntIncomeReal` ↔ `_t172mul`) · 관찰 모드에서 채집꾼·어부가 안 뽑힌다(`mining` 마을 관찰 0 ↔ 빨리감기 11~13) — 어느 쪽이 정본인지 표로 갈라 자 결함이면 고친다 · `batch/field-even-0913`(T227·T240 12커밋) 넷째 판 리베이스 + 끔 비트 동일 + 캡×짚 4팔 (세션1 · T300 뒤 · 2026-09-19)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T311.md` · 레포 `지시/지시_T311.md`)

---

T300(`batch/build-cap-0918` → main · 캡 꼴 정본 자리 하나 `:3087` · 끔 = 넷째 판 여덟 수 동일 · **"값 = 자기 세계의 실측 최대는 정의상 안 문다 — 51/51 닿고 0 넘음"** · 교차 표 21~33 마을에서 물고 T252 못 가름 · 소멸 0) **통과** — 카드가 시킨 값이 뜻이 없다는 것을 표로 증명한 것이 답이다(족보 238). ★PM 결정(위임): **#28 닫힘 — 캡은 꼴만 남기고 값은 없다.** 기준 세계를 말해야 뜻이 생기는 상한은 처방이 아니라 표다 · 켜지 않는다 · `pickDeficitJob` 처분은 재민. ⚠4팔은 `T240_STRAW_CARRY` 가 main 에 없어 못 돌렸다 — 이 카드가 그 순서를 푼다.

**카드 T311 — 셋. PM 판정**(재민 거부권): ① **고기 누수** — T297: 사냥꾼 수 같고 잡은 마릿수 같은데 곳간 고기가 **−31~−40%**. `huntIncomeReal`(랩 `:9741`) → econ 이 다음 날 읽는 자리 → `_t172mul` 사이를 **호출 수·값**으로 짚는다(족보 223·230) · 누수가 랩 자(관찰 모드의 회계)면 고친다(제품 `sim/` 무접촉 · 랩 HTML 의 관찰 회계 줄만) · 제품(`economy-sim.js` 의 고기 배율)이면 **표만**(회부). ② **관찰 모드 미선발** — `mining` 마을에서 채집꾼·어부가 관찰 0 ↔ 빨리감기 11~13 · 인구 42 에서 안 는다. 관찰 모드 직업 배분이 어느 함수를 부르는지(빨리감기와 **같은 함수인가** — 다르면 그게 결함) · 자 결함이면 고친다(랩만) · 같은 함수인데 입력이 다르면 그 입력을 표에. 고친 뒤 T297 `t297-observe.js` 3마을 3시드 30일을 다시 돌려 ⓐ 표를 갱신(밭 등가가 그대로 서야 한다 — 반례). ③ **`batch/field-even-0913` 리베이스** — T227(ⓑ 기각)·T240(이월도 처방 아님) 12커밋을 넷째 판 main 위로(충돌은 `economy-sim.js`·브라우저 번들 둘 — 번들은 `build-econ-bundle` 로 다시 굽는다 · 손으로 안 고친다) · 손잡이 `T227_EVEN`·`T240_STRAW_CARRY` **끔 = 넷째 판 비트 동일** · 그 위에서 **캡×짚 4팔 3시드**(T300 ② 그대로 · `t300-cap.js` 문법 · T252 자) · 표만 · 켜지 않는다. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 제품 `sim/`·`server/` 무접촉(①②는 랩 HTML 관찰 회계·배분 줄만 · ③은 리베이스 뿐 — 새 코드 0) · 3시드 800일 여덟 수 넷째 판 비트 동일(③ 끔 · `build-econ-bundle --check` 8/8 · `inline-engine --check`) · `test-econ-fieldyield` 163 · `test-lab-alloc` · `t297-observe` 재실행 3시드 · 접점 심볼(`huntIncomeReal|_t172mul|_hkill|lifeDayAll|doBulk|pickJob|allocJobs|L_ALLOC_REAL|T227_EVEN|T240_STRAW_CARRY|T300_BUILD_CAP|buildCapOf`).

## 0. §0 실측 (넷)
ⓐ 고기 누수 호출·값 표 + 정본 판정. ⓑ 미선발 함수 표 + 고친 뒤 3시드 30일 재표. ⓒ 리베이스 · 끔 동일 · 4팔 3시드 표. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 사본 0 · 손잡이 기본 끔 · 켜지 않는다 · **T312(세션3 · 어부 이식 · `villages.js`·`economy-sim.js` 어부 자리)·T298(세션4)·T299(세션6)·T295(세션10) 무접촉 — 리베이스 때 둘 다 살린다** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/lab-fix-0919`(①②) · `batch/field-even-0919`(③ 리베이스본 · 옛 가지는 그대로 둔다) · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0.

## 2. 작업 (넷)
① 고기 누수 ② 미선발 ③ 리베이스+4팔 ④ 한 줄 답.

## 3. 검증
3시드 · 게이트 · 하네스 셋 · 접점 목록.

## 4. 회부만
제품 쪽 누수면 카드(PM) · 4팔 판정(PM · T252 자) · `pickDeficitJob` 처분(재민) · T100 켜기(#11 재민).

## 5. 완료
커밋 [승인 대기 · T311] · 가지 둘 푸시 · `보고/T311_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> <해시> — 누수 … · 미선발 … · 끔 동일 · 4팔 …". 실기 0.
