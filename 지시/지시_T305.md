# 구현 지시 — T305: BGM 곡선도 +3.15 dB 증폭기다 — `bgm.js` 곡선 정정(기울기 1)·기본 볼륨 대조 실측 · 서버가 동사를 말한다(`sendInventory` `where` 한 칸 · `doEat` 결과 한 낱말 — 서버 두 자리만 허용) · `__sfx.probe()` 를 `test-audio` 상시 절로 · 정악판 곡 자리 표 (세션9/SOUND · T292 뒤 · 2026-09-19)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T305.md` · 레포 `지시/지시_T305.md`)

---

T292(main `425401b` · 리미터 실측 — "리미터는 필요 없었고 달아 둔 것은 +3.15 dB 증폭기였다" · 오프라인 전 표본 · 곡선 기울기 1 · 버스 값 그대로) + T292-b(카드 없이 — 마을 1.2초 씹는 소리 = `gauges` 틱의 `carry` 칸 · 서버 무접촉 · 틀린 진단 둘을 적은 것) **통과**. 족보 (226) 발신 훅은 거짓 소리 — 그 카드가 낸 말이다. T303(세션8)이 `ui_click` 없음 · `wolf_howl` 개명 · 비움 셋을 확정한다 — 이 카드와 접점 0(자산은 세션8 · 키·훅은 세션9).

**카드 T305 — T292 회부 ②·⑤·⑥ + T292-b 남은 구멍. PM 판정**(재민 거부권): ① **`bgm.js` 곡선** — T292 가 잡은 같은 곡선(원점 기울기 1.4364)이 `public/assets/audio/bgm/bgm.js` 에도 있다 · **이번 한 번 재생기 수정을 허용한다**(곡선 그 자리 하나 · 문턱 아래 기울기 정확히 1 · 위만 1.0 점근 — `48-a-audio.js` 와 **같은 함수를 부른다**(사본 0 · 어느 쪽이 정본인지 표) · 음악이 3 dB 조용해지므로 **기본 볼륨 0.45 를 같이 본다 — 새 수 0**: 고친 뒤 같은 곡 같은 장면의 RMS 가 고치기 전과 같아지는 볼륨을 오프라인 렌더로 **유도**해 표에 적고, 그 값이 0.45 와 얼마나 다른지 적는다 · 값을 바꾸는 것은 재민(회부) · 곡선만 고치고 볼륨은 그대로 둔 판을 기본으로 착지. ② **서버가 동사를 말한다 — 서버 두 자리만 허용**(SOUND 규약 "서버 무접촉" 의 예외 · 이 카드 한 번): `server/zone.js` `sendInventory` 에 `where` 한 칸(밭 수확 = `'harvest'` — T292 ⓑ 그대로 · 새 타입 0 · 낱말 하나) · `doEat` 결과에 동사 한 낱말(먹었다/배가 꽉 찼다 — 허기 `HUNGER_MAX` 에서도 씹는 소리가 나게 · T292-b 남은 구멍) · **그 두 자리 밖은 한 글자도 안 만진다**(diff 로 증명 · `gauges` 틱 무접촉) · 클라는 `carry` 존재가 아니라 **동사로** 가른다(T283 갈래 지움). ③ **`__sfx.probe()` 를 `test-audio` 상시 절로** — 18키 전부 + T292 최악 조합 → 피크·클리핑 0 표를 매 판 낸다(오프라인 렌더 · 기계 의존 ms 0 · 족보 80) · 야간 묶음 등록(`@nightly` 표). ④ **정악판 한 곡의 자리 — 표만(코드 0)**: `scenePick` 넷(장면 축)과 편성 축이 다르다는 T292 ⓔ-10 을 받아 후보 둘(다섯째 장면 칸 / `mood` 축)의 **손잡이 수·접점 파일·곡 전환 하네스가 어떻게 달라지는지** 표 한 장 → 판정 PM(다음 카드). ⑤ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: **곡선 하네스**(고치기 전/후 같은 입력 → 문턱 아래 출력 = 입력 비트 동일 · 위는 단조·≤1.0 · `48-a-audio` 와 같은 함수 심볼 하나 · 정적) · `test-audio` 기존 절 초록 + probe 절 · **서버 diff 두 자리**(`git diff origin/main -- server/zone.js | grep '^[+-]' | grep -v '^[+-][+-]'` 줄 수를 표에 · 그 밖 0) · `e2e-weather` 31/0 그대로 · `test-client-globals`(새 전역 0) · 접점 심볼(`bus.limiter.knee|_curve|bgm.js|scenePick|mood|sendInventory|where|doEat|HUNGER_MAX|gauges|carry|__sfx.probe|test-audio`) · `lab-modules --diff origin/main` 교집합 ∅(econ 무접촉).

## 0. §0 실측 (다섯)
ⓐ 곡선 전/후 표 + 볼륨 유도값(0.45 대비). ⓑ 서버 두 자리 diff 줄 수 + 동사 갈래 표(수확·먹기·배 꽉 참). ⓒ probe 18키 + 최악 조합 표(피크 dBFS · 클리핑). ⓓ 정악 자리 후보 둘 표. ⓔ 한 줄 답.

## 1. 절대 규칙
새 수 0(볼륨은 유도만 · 값은 재민) · 사본 0(곡선 함수 하나) · 서버는 **두 자리만**(그 밖 무접촉 · econ 무접촉 · `T302`(세션7 `fromEcon`)·`T298`(세션4)·`T301`(세션5) 의 `zone.js`·`villages.js` 접점 무접촉 — 리베이스 때 둘 다 살린다) · 자산 파일 무접촉(세션8 T303 이 만진다) · 배포 재민만 · ★푸시 2판(맥 VM `device_bash` · `$HOME/pushrepo` · 가지 `batch/sound-verb-0919` → main ff · `~/mnt/Mini/durango-mini` 무접촉 · 403 이면 `~/Mini/_inbox/` 번들) · force 0 · `.lock` 0.

## 2. 작업 (다섯)
① 곡선+볼륨 유도 ② 서버 동사 두 자리 + 클라 갈래 ③ probe 상시 절 ④ 정악 자리 표 ⑤ 한 줄 답.

## 3. 검증
곡선 하네스 · `test-audio` · 서버 diff 두 자리 · `e2e-weather` · `test-client-globals` · 접점 목록.

## 4. 회부만
기본 볼륨 값(재민 · ⓐ 표 뒤) · 정악 자리 판정(PM) · `render-meta.json` 빈 세 칸(**세션11 T306** 이 한 자로 다시 잰다 — 무접촉) · 어부 행위 소리(던짐/입질/걸림 — 생산 캐논 어부 카드 뒤 · 훅 자리만 미리 표에).

## 5. 완료
커밋 [승인 대기 · T305] · 가지 푸시(맥 VM) · `보고/T305_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 결론**) · `인계/SOUND-소리.md` 갱신(서버 예외 두 자리를 적는다) · 채팅 "가지 올렸다 <해시> — 곡선 … · 볼륨 유도 … · 서버 diff n줄 · probe …/18 · 정악 표". 실기 0(귀 판정은 재민 · T291 표에 행 추가).
