# 구현 지시 — T399: 1500 × 뭍 — T381 회부 3 · 뿌리를 뽑은 뒤(T394) 남은 A* 시간의 주인: **같은 뭍 덩어리인데 1500칸 안에 못 닿는 목표**(켬 판 A* 시간의 82~88%) · 그리고 T394 가 남긴 548 여행의 94% = 튕겨냄 `unstuckNpc` 무작위 도약 · 해부 표(못 닿는 목표의 거리 분포 · `searchRadiusCells 64` 와 `maxCells 1500` 의 관계 — 64셀 반경 원의 넓이 ≈ 12,868셀 > 1,500 · 상한 둘이 서로를 모른다) · 고침 하나(손잡이 기본 끔 · 새 수 0 — 상한 둘을 **하나에서 유도**) · 켬/끔 게이트 T381 자 그대로 (세션3 · T394 뒤 · 2026-09-26)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T399.md` · 레포 `지시/지시_T399.md`)

---

T394(`batch/worksite-terrain-0925` → main · 물 위 일터 162 → 0 · 막힌 목표 여행 10.0 → 0.7% · 물가 정지 36.0 → 19.9명(8/8) · 존 틱 p50 −2.12ms · `localPath` 비대칭 0 · `T394_WORK_TERRAIN`·`T381_PATH_DEAD` 기본 켬 — 켬 = 끔(③이 값을 먼저 가져갔다 · 이중 자물쇠) · 3시드 30칸 동일 · `test-move-soa` 129/0 · `_path-core-test` 42/42) **통과** — "뿌리를 뽑으면 손잡이는 이중 자물쇠가 된다"가 이 카드의 문장이다(족보 315). ★PM: 재민 실기 ③(강 비비기)은 **배포 뒤** 닫힌다(물가 정지 19.9명은 남아 있다 — 그 19.9 의 정체가 이 카드 ②).

T381 §3-ⓐ 가 켬 판에서 남긴 수: A* 시간의 **82~88% 가 "1500 × 뭍"** — 목표가 같은 뭍 덩어리에 있는데 `maxCells 1500` 을 다 태우고 `null`. 그런데 `searchRadiusCells 64` 는 반경 64셀(넓이 ≈ 12,868셀)을 허락한다 — **두 상한이 서로를 모른다**: 반경이 허락한 목표를 칸 수가 못 닿게 한다. 그리고 T394 §머리 "남은 548 의 94% 는 튕겨냄 무작위 도약" — `unstuckNpc` 가 막힌 칸으로 도약을 준다.

**카드 T399 — 넷. PM 판정**(재민 거부권): ① **해부 표** — T381 자(`t381-path-cost.js` · 낮 창 7,200틱 · T394 켬 판)에서 "1500 × 뭍" 호출마다: 출발↔목표 직선 거리(셀) · 그때 꺼낸 칸 1,500 이 덮은 면적 · 목표까지 실제 필요한 칸 수(같은 호출을 `maxCells` 를 풀어 다시 돌린 팔 — 계측 팔에서만) · 분포(닿는 데 1,500~3,000 · 3,000~ · 영영 못 닿음(뭍은 같은데 벽·나무로 막힘)) · 어느 결정 갈래가 그 먼 목표를 줬나(⑤ 배회 · 일과 · 따라가기 · 캐러밴 — 갈래별 몫) · **주인 이름 하나**. ② **튕겨냄 도약** — `unstuckNpc` 가 주는 도약 목표를 같은 술어(T394 ③ 되짚기 함수 · 사본 0)로 열린 칸에 · 도약 후보가 막힌 비율 전/후 · 물가 정지 19.9 → n(같은 자 · 8/8). ③ **상한 하나** — `searchRadiusCells` 와 `maxCells` 를 **하나에서 유도**(둘 중 무엇이 정본인가: 반경이 정본이면 `maxCells = 반경 원의 셀 수`의 몫 — 새 수 0 · 지금 1,500 은 어디서 왔나 표) 또는 목표 거리로 `maxCells` 를 정하는 문(직선 거리 × 있는 계수 — 계수는 정본에서) · 손잡이 `T399_CELL_CAP` 기본 끔 · 게이트 = T381 §3 자(켬/끔 같은 씨 · 8/8) · **값: 1500 × 뭍 82~88% → n% · A* ms/틱 · 존 틱 p50** · 반례: 열린 목표 도착률·도착 시간 무변 이상. ④ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: `sim/path-core.js`·`server/pathfind.js` 만지면 40쌍 비트 동일 + `inline-path --check` · `villages.js` 무접촉(T398 세션2 · T368 세션1) · 3시드 면제 근거 한 줄(`sim/` 만지면 면제 없음) · `test-move-soa` 129 + 절 · `_path-core-test` 42 · `test-hall-door-path` · `e2e-move` 37 · `test-war-world` 97(흔들림이면 세 판) · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`pfFindPath|maxCells|searchRadiusCells|maxPops|unstuckNpc|decideNpcBehavior|computeNpcPath|T381_PATH_DEAD|T394_WORK_TERRAIN|T399_CELL_CAP|t381-path-cost|findPath|localPath`).

## 0. §0 실측 (넷)
ⓐ 1500 × 뭍 해부 표(거리 분포 · 필요 칸 · 갈래별 · 주인). ⓑ 도약 전/후 + 물가 정지 19.9 → n. ⓒ 상한 하나 diff + 켬/끔 게이트 + 값. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0(상한은 유도) · 손잡이 하나(끔) · beeline 안 넣는다 · `Math.random` 0 · **T385(세션7 틱 본체)·T398(세션2)·T400(세션4)·T368(세션1) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/cell-cap-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 해부 ② 도약 ③ 상한 하나 ④ 한 줄 답.

## 3. 검증
게이트 · 40쌍 · 하네스 여덟 · 접점 목록.

## 4. 회부만
켜기(재민 · ③ 값 뒤 · T394 문법 — 뿌리 뒤 켬) · 200일 자 결정성(PM) · C 층 접속(#53) · `test-war-world` 흔들림 자(PM · 성능 단언 → 비율 자).

## 5. 완료
커밋 [승인 대기 · T399] · 가지 푸시 · `보고/T399_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T399]` · 베이스 해시**) · `인계/M-이동입력.md` 갱신 · 채팅 "가지 올렸다 <해시> — 1500×뭍 주인 … · 도약 막힘 …→… · 물가 정지 19.9 → n · 상한 하나 켬 A* …→… ms". 실기 0.
