# 구현 지시 — T301: 관측자 무관 자원 색인 — 나무·바위·광맥을 청크가 안 켜져도 시드 함수로 셀 질의(`resourceAt(cx,cy)` 정본 하나 · 청크 생성과 같은 함수 · 사본 0) · 전쟁(T284 회부 ①)과 생산 캐논(ⓗ)의 선행 (세션5 · T288 뒤 · 2026-09-18)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T301.md` · 레포 `지시/지시_T301.md`)

---

T288(`batch/hunt-wood-server-0912` 9397bfcc · `economy-sim-v2` 두 줄 · 누계 칸 둘 `_subsUsed`·`_decayed` · 여덟 수 3시드 비트 동일 · subs 11종·decay 30종 전부 장부에 · 남은 무장부 자리 = `tickSubsistence` 둘째 루프 무기·갑옷 마모 · 09-18 지연 처리 — PM 이 번들을 못 본 것) 통과 — 전부 인정. 족보 (231): "장부는 세계를 안 움직이고도 닫을 수 있다 — 칸이 없는 자리를 먼저 센다". T288 은 T244 넷째 판 위로 PM 이 리베이스해 착지한다(같은 엔진 파일 · 순서 PM). **#37 `:2729` 는 그대로**(재민 위임 → PM).

**왜 이 카드인가.** 존의 나무·바위·광맥은 **청크가 켜질 때** 생기는 시드 자원이라 관측자에 따라 있다 없다 한다(T284 회부 ① — 전투가 읽으면 결정성이 깨진다) · 새 캐논 "생산은 행위다"(`설계/설계_생산_실체.md` §2-ⓗ)에서 관측자 없는 마을의 나무꾼·광부가 **실제 나무·광맥**에 가야 한다. 둘 다 같은 것을 요구한다: **청크 없이 셀을 물으면 그 자리에 무엇이 있는지 답하는 정본 함수.**

**카드 T301.** **PM 판정**(재민 거부권): ① 청크 생성이 자원을 놓는 함수(`zone.js` 시드 자원 루프 · 자리 표)를 **순수 함수**로 가른다: `resourceAt(zoneId, cx, cy) → {type, r, h, sp…} | null` — 같은 시드·같은 셀 = 같은 답 · **청크 생성이 이 함수를 부른다**(사본 0 · 생성물이 한 바이트도 안 바뀐다 = 자). 베인 나무·캔 광맥 같은 **변경분**은 기존 상태(`qtResources` · 그루터기 행 · DB)가 정본이고, 색인은 "원래 무엇이었나"만 답한다(변경분 덮어쓰기 규칙 표). ② 전쟁 어댑터 `_warWorld.blocked/losBlocked/coverAt` 가 나무를 이 함수로 읽게 한 줄씩(T284 가 비워 둔 자리 · 세션10 과 접점 — `war-live.js` 그 세 줄만 · 나머지 무접촉) · 결정성(관측자 켬/끔 세 시드 동일 · `test-war-world` ⓓ)이 나무를 넣고도 서나. ③ 하네스 `test-resource-index.js`: ⓐ 활성 청크 100개 전수 — 청크가 놓은 자원 = 색인 답(바이트) ⓑ 미활성 셀 질의가 청크를 **안 켠다**(청크 수 불변) ⓒ 자명 통과 금지(시드를 바꾸면 다르다) ⓓ 부하(셀 질의 1회 µs · 마을 반경 16셀 전수 ms). ④ 답 한 줄. 이틀. 질문 금지.

★검증 규약: 청크 생성물 바이트 동일(활성 청크 전수) · econ 무접촉 · 3시드 면제(랩 적재 밖 — `lab-modules --diff` 로 보임) · `test-war-world` 30/0 유지 · `test-hunt-vis` · **T295(세션10 `war-core`·`villages.js` 전쟁 절 — `war-live` 세 줄만 접점 · 리베이스 때 둘 다) · T298(세션4)** · 접점 심볼(`qtResources|resourceAt|chunk|treeBlockerAt|_warWorld|losBlocked|coverAt|isTerrainBlockedLocal|test-war-world`).

## 0. §0 실측 (넷)
ⓐ 함수 자리 표 + 변경분 규칙. ⓑ 전쟁 세 줄 + 결정성. ⓒ 하네스 ⓐ~ⓓ. ⓓ 한 줄 답.

## 1. 절대 규칙
사본 0(청크 생성이 이 함수를 부른다) · 새 수 0 · 배포 재민만 · ★푸시 2판(맥 VM · 가지 `batch/resource-index-0918` **가지만** · main 은 PM) · force 0.

## 2. 작업 (넷)
① 정본 함수 ② 전쟁 세 줄 ③ 하네스 ④ 한 줄 답.

## 3. 검증
바이트 동일 · 청크 불변 · 결정성 · 접점.

## 4. 회부만
`tickSubsistence` 둘째 루프(무기·갑옷 마모 칸) · #31 25벌(#6 실기 뒤) · 물고기 개체(설계 ⓕ · 재민).

## 5. 완료
커밋 [승인 대기 · T301] · 가지 푸시 · `보고/T301_<날짜>.md`(머리 15줄) · 채팅 한 줄. 실기 0.
