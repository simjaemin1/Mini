# 구현 지시 — T379: 우클릭 홀드(조준)가 캔버스 밖·패널 위로 나가면 풀린다(재민 실기 09-23: "우클릭을 드래그하다가 다른 UI 위로 올라가거나 브라우저 밖으로 이동하면 취소되어 화면이 제자리로 돌아온다") — `30-n-net.js:505` `mouseleave → _aimOff` 가 원인 후보 · **포인터 캡처**(`setPointerCapture` · 우클릭 누른 순간 캔버스가 포인터를 잡아 밖으로 나가도 `pointerup` 을 받는다 · `mouseleave` 제거 · `blur` 만 유지) · "우클릭이 제대로 안 먹는다"를 **실기 대본으로 재현**해 무엇이 안 먹는지 표(브라우저 셋 · Safari 포함 — 재민은 Safari) · e2e (세션9 · 클라 · 2026-09-23)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T379.md` · 레포 `지시/지시_T379.md`)

---

재민 실기 09-23: **"브라우저라 그런지 우클릭이 제대로 안 먹고, 우클릭을 드래그하다가 다른 UI 위로 올라가거나 브라우저 밖으로 이동하면 우클릭이 취소되어서 화면 제자리로 돌아와버린다."** 코드: 우클릭 홀드 = 조준(`_aiming` · 카메라가 조준 방향을 본다)이고 `30-n-net.js:503~506` 이 `mouseup(2)`·**`mouseleave`**·`blur` 셋에서 끈다. 패널(HUD·사이드)은 캔버스 위 DOM 이라 포인터가 그 위로 가면 캔버스에 `mouseleave` 가 오고 조준이 풀린다 — 카메라가 제자리로. 창 밖도 같다. **추측이다 — 재현 먼저.**

**카드 T379 — 셋. PM 판정**(재민 거부권): ① **재현 표** — 실기 대본(우클릭 누름 → 드래그 → ⓐ 패널 위로 ⓑ 창 밖으로 ⓒ 캔버스 안에서 뗌 ⓓ 우클릭 짧게(동사 메뉴 `46-h-verbs`) ⓔ 우클릭 홀드 600ms ⓕ 배치 모드 회전) × 브라우저(Safari · Chrome · Firefox — 헤드리스로 되는 것만 e2e, 나머지는 재민 실기 칸) · 무엇이 되고 안 되나 · 이벤트 순서 로그(`mousedown/contextmenu/mouseleave/mouseup/auxclick` — T321 문법). ② **고침** — 우클릭 `pointerdown(button 2)` 에서 `canvas.setPointerCapture(e.pointerId)` · `pointerup`/`pointercancel` 에서 해제+`_aimOff` · `mouseleave` 리스너 **제거** · `blur` 유지(탭 전환은 풀리는 게 맞다) · 조준 중엔 패널이 포인터를 못 받아도 되는지(캡처 중 클릭은 캔버스로 간다 — 조준 중 좌클릭 = 공격이니 맞다 · 표) · `46-h-verbs` 의 `mouseup(2)` 홀드 판정이 캡처 뒤에도 같은 순서로 오는지(①ⓓⓔ 다시) · "제대로 안 먹는다"가 ①에서 다른 원인으로 나오면 그것도(한 자리씩 · 표). ③ **e2e** — `e2e-aim`(있으면 그것 · 없으면 신설): 캔버스 밖으로 포인터가 나가도 조준 유지 · 뗄 때 해제 · `test-client-globals` 26. ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 클라 파일 둘(`30-n-net.js` 조준 리스너 · 필요하면 `46-h-verbs.js` 한 자리)만 · 서버 0 · `e2e-move` 37 · `e2e-onboarding` 74 · `test-client-globals` 26 · `test-harness-lint` 57 · 접점 심볼(`_aiming|_aimOff|mouseleave|setPointerCapture|pointerup|pointercancel|contextmenu|auxclick|46-h-verbs|placementMode`).

## 1. 절대 규칙
새 UI 0 · 조준 의미 무변(우클릭 홀드 = 조준 · 좌클릭 = 공격) · 배치 회전 무변 · `bgm/`·`sfx/` 무접촉(이 카드는 소리 아님) · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/aim-capture-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 5. 완료
커밋 [승인 대기 · T379] · 가지 푸시 · `보고/T379_<날짜>.md`(**머리 15줄에 재현 표**) · `인계/M-이동입력.md` 갱신 · 채팅 "가지 올렸다 <해시> — 재현 n/6 · 캡처 · e2e n". 실기: 재민이 Safari 로 우클릭 드래그 패널 위·창 밖.
