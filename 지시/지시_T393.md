# 구현 지시 — T393: T378 이 남긴 셋 — ① 재민 실기 ①의 나머지 반 "스폰하자마자 숲이 너무 많다" 의 **영토 밖** 몫: 스폰 지점 시야 안(컬링 1,500 · T392) 나무 수를 51마을 전수로 세고(지형 `forests` 정본 · `forestSpacing` 60~96 · `FOREST_MIN_COV 1.5`) **값은 재민(#71)** — 표만 ② 유령 나무 — `_takeResourceEntity`·개간이 `resourcesByDbId` 에서 안 빼 `_shapePlantedAll` 이 지운 나무를 `resource_spawn` 으로 다시 방송할 수 있다(T378 §1-5 · 코드로만 읽음) → **재현 먼저**(하네스로 유령을 세운다) · 재현되면 두 문에 같은 한 줄 ③ `_actEntitiesAtCell` 이 예외를 삼킨다(T378 §1-3) → 삼키지 않게(한 번 이름 · 정본 하나) (세션2 · T378 뒤 · 2026-09-25)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T393.md` · 레포 `지시/지시_T393.md`)

---

T378(`batch/forest-clear-0923` → main · 개간이 관측자 없는 마을에선 한 그루도 못 벴다 — 뿌리 둘(`qtResources` 활성 청크만 · 부팅 TDZ 를 `_actEntitiesAtCell` 이 삼킴) + 구멍 하나(부팅엔 `qtResources` 가 없어 DB 나무도 안 잡힘) · 영토 안 6,893 → 0 · 40일 9,105 → 0 · 관측자 게이트 1,465·1,307·872 → 0 · 영토 밖 씨 하나까지 같다 · 카드 괄호 뒤집힘(나무꾼 네모는 영토 안 — T325 켜는 날의 충돌) · 자기가 틀린 것 다섯을 §2 에 적음 · 하네스 다섯 초록) **통과** — "구멍은 하나가 아니라 셋이었고 자가 없는 세계를 재지 않았는지부터 물었다"가 이 카드의 문장이다(족보 308). ★PM: 재민 실기 ①("마을 안에 숲") 은 이 카드로 닫혔다 — **배포 뒤 스폰 마을 영토 안엔 나무 대신 그루터기**(T122 · 그림만 · 안 막는다). 나머지 반("스폰하자마자 숲이 너무 많다")은 **영토 밖**이고 그건 값이다.

**카드 T393 — 넷. PM 판정**(재민 거부권): ① **영토 밖 숲 밀도 표** — 51마을 스폰 지점(온보딩 도착 지점 · T19 정본)마다: 시야 상자(반경 1,500 · T392 컬링 팔 기준 · 지금 650 도 나란히) 안 나무 수 · 그중 영토 밖 수 · 그 자리 `getForestMultiplier`·`forestSpacing` 값 · 51마을 분포(중앙값 · 최대 마을 이름) · **한 장 그림**(스폰 지점 중심 3,000×3,000px 색인 점 찍기 · 마을 셋: 중앙값 · 최대 · 최소 — `~/Mini/산그림/T393_*.png`) · 값(간격 · `FOREST_MIN_COV`)은 **안 바꾼다** — 재민이 그림을 보고 #71 로 한 글자 · 바꾸면 세계가 바뀌므로 그 뒤는 3시드 카드. ② **유령 나무 재현** — T378 §1-5 가 **코드로만** 읽은 것: 하네스로 DB 나무를 심고 → `_takeResourceEntity`(벌목) 또는 개간으로 뺀 뒤 → `_shapePlantedAll` 이 단계를 바꾸는 시각까지 시계를 돌려 → 클라 프로브(또는 방송 로그)에 `resource_spawn` 이 그 id 로 **다시** 오나 · 오면 유령(재현 ○) → 두 문에 **같은 한 줄**(`resourcesByDbId.delete` · 정본 하나 — 두 문이 한 함수를 부르게 할 수 있으면 그게 먼저) · 안 오면 "재현 ✗ · 왜" 한 줄로 끝(고치지 않는다). ③ **`_actEntitiesAtCell` 예외** — 삼키는 `try` 를 **한 번 이름**(같은 이유는 한 번만 · T378 색인 갈래가 한 문법) 으로 · 부팅에 부르는 호출부 표(지금 몇 곳 · 부팅 때 TDZ 에 걸릴 수 있는 것) · `gameDayNow()` 의 `_e2eClock` TDZ 자체를 없앨 수 있으면(`let` → 선언 위치) **그게 뿌리** — 한 줄이면 한다 · 게이트: 부팅 개간 로그가 "못 물었다" 와 "0" 을 가른다. ④ 한 줄 답 + T325 켜는 날 충돌 표(§0-ⓕ 그대로 · 나무꾼 네모를 영토 밖으로 · 회부 PM). 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(①은 표·그림만 · `chunk.js` 무접촉) · `sim/` 무접촉 · ②③은 `zone.js` 그 자리만 · `test-resource-index` 20 · `test-trees` 91 · `test-wood-act` 90 · `test-regrow` 100 · `test-terr-persist` 8 · `e2e-nature` · `t378-forest-in-village` 자 재실행(영토 안 0 유지) · `test-harness-lint` 61 · 접점 심볼(`forestSpacing|FOREST_MIN_COV|getForestMultiplier|resourcesByDbId|_takeResourceEntity|_shapePlantedAll|resource_spawn|_actEntitiesAtCell|gameDayNow|_e2eClock|clearTreesInCells|t378-forest-in-village`).

## 0. §0 실측 (넷)
ⓐ 51마을 표 + 그림 셋. ⓑ 유령 재현 ○/✗ + 한 줄. ⓒ 예외 한 번 이름 + 호출부 표 + TDZ 뿌리. ⓓ 한 줄 답 + T325 충돌 표.

## 1. 절대 규칙
값 안 바꾼다(#71 재민) · 세계 무변 · 새 손잡이 0 · **T394(세션3 `villages.js` 일터 · `zone.js` A*)·T368(세션1 농부)·T385(세션7 순회) 무접촉** — `zone.js` 는 자원 절만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/forest-outside-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 밀도 표·그림 ② 유령 재현 ③ 예외 ④ 한 줄 답 + 충돌 표.

## 3. 검증
세계 diff 0 · 하네스 여덟 · 접점 목록.

## 4. 회부만
#71 영토 밖 숲 값(재민 · 그림) · 그루터기 캐기(재민 실기 뒤) · 영토 안 재생 술어(PM · T378 §0-ⓖ) · T325 나무꾼 숲(PM · 켜는 날) · `INSERT OR IGNORE`(PM).

## 5. 완료
커밋 [승인 대기 · T393] · 가지 푸시 · `보고/T393_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 시야 안 나무 중앙값·최대 + 유령 ○/✗**) · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 스폰 시야 나무 중앙값 n(최대 마을 …) · 유령 ○/✗ · 예외 한 번 이름". 실기: 재민 배포 뒤 스폰 마을(그루터기 · 밖 숲).
