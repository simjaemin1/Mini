# 구현 지시 — T323: 소리판 — 게임에 있는 모든 소리를 표 하나로, 클릭하면 들린다(`public/sfx-board.html` · `manifest.json`·`render-meta.json`에서 만든다 · 손 목록 0) · 층을 지나서 듣기/파일 그대로 듣기 두 단추 · ○/✗/메모 칸 + 표 복사 → 재민 귀 판정이 이 한 장에서 끝난다 (세션9/SOUND · 재민 09-19 · 2026-09-19)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T323.md` · 레포 `지시/지시_T323.md`)

---

재민 09-19: **"게임 내에 존재하는 모든 효과음을 표로 정리해서 클릭하면 들어볼 수 있는 시스템 만들어야 해."** 지금 귀 판정은 실기표 26항(`문서/실기_소리_2026-09-14.md`)을 들고 게임 안을 한 바퀴 돌아야 해서 안 된다. 소리마다 그 자리를 찾아가는 비용이 판정을 막았다.

**카드 T323 — 페이지 한 장. PM 판정**(재민 거부권): ① **`public/sfx-board.html`**(정적 · 서버 라우트 새로 안 만든다 — `index.html` 옆 · 배포하면 `<호스트>/sfx-board.html`) — 열면 `assets/sfx/manifest.json` 과 `assets/audio/bgm/render-meta.json` 을 **fetch 해서** 표를 만든다(손으로 적은 키 0 · 매니페스트가 정본 · 키가 늘면 표도 는다). 행 = 키 하나: 이름 · 파일(`.ogg`/`.m4a`) · 쓰이는 자리(`keys[k]._훅`·`mobs`·`inventoryWhere`·`resourceHit` 등 매니페스트가 이미 가진 배선 표에서 역으로 · 없으면 "배선 없음") · 반경 · 출처/라이선스(CREDITS 표가 json 이면 그것 · md 뿐이면 매니페스트 `sources` 칸) · 상태(파일 있음 / `state:"missing"` / 배선 있는데 무음 — T320 ⑦ 그 셈). BGM 13곡은 아래 표 둘째(곡 · `measured` 초·RMS·피크 · 장면). ② **듣기 두 단추** — 「파일」= `new Audio(url)` 그대로(원본) · 「층」= 진짜 `window.__sfx`(`48-a-audio.js` 를 그 페이지가 그대로 싣는다 — 사본 0 · 곡선·버스·볼륨 표를 지난 소리 · 반경은 0 거리로) · BGM 은 「층」이 `bgm.js` 를 통해 그 곡 장면을 켜고 끈다 · 한 번에 하나만(누르면 앞 것은 멈춘다) · 재생 중 표시. ③ **판정 칸** — 행마다 ○/✗ 단추 + 메모 한 줄 · 「표 복사」 단추가 markdown 표(키 · ○/✗ · 메모)를 클립보드에 · 저장은 `localStorage` 한 키(사라져도 되는 것 · 복사가 정본) · 실기표 26항의 "어디서 나야 하나" 문장을 그 키 행에 **같이** 보이게(`문서/실기_소리_2026-09-14.md` 를 json 으로 한 번 옮기지 말고 — 페이지가 md 를 fetch 해서 키 이름으로 줄을 붙인다 · 못 붙는 줄은 표 끝에). ④ **하네스** — `test-audio` 절 하나: 페이지가 매니페스트의 **모든** 키를 행으로 낸다(키 수 = 행 수 · 정적 · 자명 통과 금지: 키 하나 지우면 행도 준다) · `e2e-audio-probe` 절 하나: 헤드리스에서 「층」 단추가 실제로 `__sfx.recv`/`play` 를 부른다(호출 수 · 족보 226) · `test-client-globals`(새 전역 0 — 페이지 스크립트는 그 파일 안에서만) · `test-doors`(새 라우트 0 · 정적 파일 하나가 62 표에 어긋나지 않는지). ⑤ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 제품 `48-a-audio.js`·`bgm.js` **무접촉**(페이지가 싣기만) · 서버 0 · 새 전역 0 · 매니페스트 무접촉(읽기만) · 배포는 재민 · 접점 심볼(`manifest.json|render-meta.json|__sfx|bgm.js|sfx-board.html|실기_소리|localStorage|test-audio|e2e-audio-probe`).

## 0. §0 실측
ⓐ 행 수 = 키 수(효과음 n · BGM 13 · 무음 n · 배선 없음 n). ⓑ 두 단추 호출 수. ⓒ 실기표 26항 중 키에 붙은 줄 수 / 못 붙은 줄. ⓓ 한 줄 답 + 스크린샷 한 장(페이지 · 하네스가 찍는다).

## 1. 절대 규칙
새 코드는 그 페이지 한 파일 + 하네스 절 · 제품 무접촉 · 자산 무접촉(세션8) · 손 목록 0(정본에서 만든다) · ★푸시 2판(가지 `batch/sfx-board-0919` → main ff · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
페이지 주소를 게임 메뉴에 걸지(재민 · 지금은 주소로만) · #44 볼륨 · #45 정악 · #50 낙하 키 · #34 녹음 — 전부 이 페이지에서 듣고 정한다.

## 5. 완료
커밋 [승인 대기 · T323] · `보고/T323_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 행 n · BGM 13 · 두 단추 · 26항 중 n 붙음". 실기: 재민이 배포 뒤 `<호스트>/sfx-board.html` 한 장.
