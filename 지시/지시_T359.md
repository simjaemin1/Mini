# 구현 지시 — T359: 군락은 마을 계획이 아니라 지형의 것 — 덤불·풀 군락을 나무처럼 `generateChunkResources` 가 지형(숲 덮임·물가)에서 낳게 · 밀도는 T357 필요 K 를 생활권 면적으로 나눠 **유도**(새 수 0 · 빌린 값 0) · 손잡이 `T359_GROVE_TERRAIN` 기본 끔(끔 = 지금 링 군락 그대로 · 3시드·실서버 비트 동일) · 켬 판: 개체 수 · 채집 30일 등가(T347 자) · 사람당 µs · 종은 덤불·풀 둘만(새 종 넷은 ART 뒤) (세션1 · #58 ⓐ′ 재민 확정 09-23 · 2026-09-23)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T359.md` · 레포 `지시/지시_T359.md`)

---

T357(`batch/grove-need-0923` → main · 필요 K **10,059**(지금 221 의 45.5배) · 도보 15초 링의 기하 상한 2,037(마을당 최대 54) · 놓을 수 없는 마을 32/45 · r 은 T122 `REGROW` 주기(덤불 1년 · 풀 0.5년)에서 `r = 4/(T·Y)` 로 유도 — 빌린 값 0 · 세계 무변) **통과** — "결정의 축은 군락 수가 아니라 규칙"이 이 카드의 문장이다(족보 286). ★재민 결정 #58 **ⓐ′**(09-23): **군락을 나무처럼 지형이 낳게 한다.** 링 군락(`plan-village-forage.js`)은 플레이어 첫 15초용이었고 그대로 둔다 — NPC 경제의 밑변은 지형에서 온다. 새 종 넷(버섯·나물·벌집·야생포도)은 표 열두 칸이 비어 있으니 **이 카드 밖**(ART 뒤).

**카드 T359 — 넷 · 순서가 카드다. PM 판정**(재민 거부권): ① **밀도 유도** — 나무는 `forestSpacing(fCov)` 가 숲 덮임에서 간격을 낸다. 군락도 같은 꼴: 마을 생활권(`_maxRPx` 의 원 · 나무 스캔 `T325_R` 과 같은 자) 안에 T357 판 A 의 K(마을별)가 서려면 셀당 밀도가 얼마인가 — `K ÷ 생활권 셀 수` 를 51마을에서 내고 **중앙값 하나**를 간격으로(간격 = 1/√밀도 · 유도 표 · 마을별 값이 아니라 지형별 값이어야 한다 — 덤불은 숲 가장자리(`fCov` 띠) · 풀은 초지·물가(지형 술어가 이미 가르는 그 이름) · 어느 지형이 어느 군락을 내나 표) · 비교: 나무 간격 60~96px 과 나란히. ② **생성** — `generateChunkResources` 의 나무 자리 옆에 군락 갈래 하나(같은 결정론 씨 · 같은 `harvestedSet` 회계 · `kind` = 지금 `groves` 가 받는 그 꼴 `berry_bush`·`herb` · 새 스프라이트 0 · 새 전리품 표 0) · 손잡이 `T359_GROVE_TERRAIN` **기본 끔** · 끔 = 청크 산출 비트 동일(3시드 `sim/` 무접촉이면 면제 · 실서버 `test-nippon-boot`·`test-trees` 무변 · **청크 해시 자**: 끔 판 청크 512개 해시 = 지금) · 링 군락(`groves` 정본)은 **무접촉**(둘이 겹치면 청크 쪽이 링 자리를 피한다 — 색인이 이미 안다). ③ **켬 판** — 실서버 51마을 200일(T326 자) 켬/끔 두 팔: 군락 개체 수(마을당 · 합 · T357 K 대비) · 채집 30일 등가(T347 `t347-forage-day` 자 · 비 1.2~2.4% → ?) · 걷는 몫 대비 · 곳간 품목(`twig`·`herb`) · 사람당 µs(T345 자 · 색인이 커진 값) · 3시드 800일 켬 팔(랩이 군락을 보면 — 안 보면 "면제" 한 줄) · T252 자 · **켜지 않는다**. ④ 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 끔 비트 동일(청크 해시 · 3시드 · 실서버) · `test-forage-act` 86 · `test-trees` 91 · `test-resource-index` 20 · `test-nippon-boot` 38 · `lab-wiring-check` 실패 0 · `audit-village-forage` 51/51 무변 · 새 절: ⓐ 밀도가 지형 술어에서 정적으로 읽힌다 ⓑ 켬 판 군락 수 > 0 · 끔 판 = 0(청크 갈래) ⓒ 링 군락과 겹침 0 · 접점 심볼(`generateChunkResources|forestSpacing|fCov|groves|berry_bush|herb|harvestedSet|resourcesAtCell|T359_GROVE_TERRAIN|_maxRPx|T325_R|t347-forage-day|forageActItems`).

## 0. §0 실측 (넷)
ⓐ 밀도 유도 표(지형별 간격 · 나무와 나란히). ⓑ 끔 비트 동일(청크 해시 · 3시드 · 실서버). ⓒ 켬 판 두 팔 표(개체 · 등가 · µs). ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0(유도만) · 새 종 0 · 새 스프라이트 0 · 손잡이 기본 끔 · 켜지 않는다 · 링 군락 정본 무접촉 · **T356(세션3 `zone.js` 걸음)·T358(세션9)·T364(세션10 전쟁) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/grove-terrain-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · ③ 표) · `berry→fruit` 동의어(PM · 켠 뒤) · 새 종 넷(ART · 재생 주기 값은 재민) · `kgOf('walnut')`·kcal 빈칸(표 주인) · 링 군락을 접을지(플레이어 첫 15초 규약 · 재민).

## 5. 완료
커밋 [승인 대기 · T359] · 가지 푸시 · `보고/T359_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 간격 … · 끔 동일 · 켬 개체 … (K 대비 …%) · 등가 …% · µs …". 실기 0.
