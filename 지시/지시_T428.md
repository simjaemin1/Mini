# 구현 지시 — T428: 존 셋을 같이 띄운다 — #76(T408 §5-4): 라이브가 한반도 존만 돌아 T408 뒤 선 너머(닛폰·중원북 땅)가 **비어 보인다** · 맞는 그림 = 이웃 존이 제 나무를 낸다 = 그 존들도 뜬다 ⇒ 실측: 한 호스트에서 한반도+닛폰+중원북 **셋 동시 기동**(프로세스 셋 · 포트 3020·3021·3016 · central 하나) · 부팅 시간·메모리·idle CPU(T410 전/후 둘 다 — 지금은 idle 스킵이라 거의 0) · **경계를 걸어서 넘는 e2e**(한반도 → 닛폰 · 한반도 → 중원북 · 존 이동 정본 · 시야 안 이웃 존 나무가 보이나 — 클라 이웃 관측 `PEEK_THRESHOLD` 900 < 시야 1,500(T392) 이 T408 §5-4 에 걸려 있다) · `scripts/redeploy-hanbando.sh --all` 이 어느 존을 띄우나(표 · 스크립트 무변 — 재민 배포 손) · 표만 + 클라 한 줄이면 한 줄 (세션5 · T409 뒤 · 2026-09-26)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T428.md` · 레포 `지시/지시_T428.md`)

---

T409(`batch/jungwon-n-0926` → main · 중원북 후보 25 → 게이트 12 → 시딩 11 · 다리 92셀 55/55 · 광맥 4 → 754 · 3시드 인구 1,736/1,599/1,792 소멸 0/11 · 돌 바닥 11/11 · **노동권 안 큰광맥 0/11 — 광산 후보 셋이 식량 하한 탈락**(#78) · 이름 = 줄기 + 업종 · `plan-ore-clusters` 가 T406 캐시를 정본에 흘리던 결함 고침 · 한반도·닛폰 diff 0 · `test-jungwon-boot` 38/0) **통과** — "중원북이 산다 — 그런데 광산이 하나도 안 선다"가 이 카드의 문장이다(족보 343). #78 은 재민(T429 세션6 이 광산 마을 식량 표를 낸다). 그 다음은 **존 셋이 한 세계로 보이나**다.

**카드 T428 — 넷. PM 판정**(재민 거부권): ① **셋 동시 기동 실측** — 한 호스트(컨테이너 · 맥은 수만): central + `hanbando`·`nippon`·`jungwon_n` 존 셋(있는 기동 문법 · `fixture-boot` 정본) · 부팅 시간 셋(T406 뒤 값) · RSS 메모리 셋 · idle CPU(사람 0 — 지금 idle 스킵 · T410 이 열면 그 값은 T410 표) · 존 사이 central 왕복(길드 금고·소문 — 있는 e2e 문법) · 오류 0. ② **경계 넘기 e2e** — `e2e-zone-cross.js`(있으면 그것 · 없으면 신설 · `e2e-move` 문법): 한반도 동쪽 경계에서 닛폰으로 걸어 넘는다(존 이동 정본 · 좌표 연속 · 몸·짐 보존) · 서쪽에서 중원북으로 · **넘기 전 시야(1,500)에 이웃 존 나무가 보이나**(`PEEK_THRESHOLD` 900 — T408 §5-4 · 클라 이웃 관측이 900px 안에서만 이웃 존을 엿본다면 1,500 시야에 600px 빈 띠가 남는다 → **한 줄**(`PEEK_THRESHOLD` = 시야 반경과 같은 상수 · 새 수 0 · T392 문법) · 그림 전/후 · 넘은 뒤 되돌아오기. ③ **배포 스크립트 표** — `scripts/redeploy-hanbando.sh` 가 `--all` 로 어느 존을 띄우나(코드 그대로 · 존 목록 · 포트 · systemd/pm2 단위) · 닛폰·중원북을 띄우려면 무엇이 빠져 있나(스크립트 무변 · 표만 · 재민 손 #76). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변 · 서버 무접촉(클라 `PEEK_THRESHOLD` 한 줄뿐) · `e2e-move` 37 · `e2e-zone-cross` 신설 · `test-nippon-boot` 42 · `test-jungwon-boot` 38 · `test-far-trees` 19 · `test-client-globals` 26 · `test-harness-lint` 61 · 접점 심볼(`PEEK_THRESHOLD|VIEW_RADIUS|zone-config|worldOffsetX|findZoneAt|fixture-boot|redeploy-hanbando|central|e2e-zone-cross|_seamRefs`).

## 1. 절대 규칙
서버 0 · 스크립트 무변 · 새 수 0 · **T426(세션2)·T427(세션3)·T410(세션1) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/three-zones-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#76 존 띄우기(재민 배포 · ③ 표) · #78 광산 게이트(재민 · T429) · 도령재·대숲내·고개(재민 손) · 옛 `_bbox` 117(PM).

## 5. 완료
커밋 [승인 대기 · T428] · 가지 푸시 · `보고/T428_<날짜>.md`(템플릿 네 칸) · `인계/Z-존서버.md`·`N-네트워크.md` 갱신 · 채팅 "가지 올렸다 <해시> — 셋 기동 …s/…MB · 경계 넘기 ○ · 빈 띠 600 → 0 · --all 이 띄우는 존 n". 실기: 재민 배포 뒤 경계.
