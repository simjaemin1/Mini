# 구현 지시 — T321: 어부의 소리 — 던짐·입질·걸림 훅 셋을 NPC 어부 행위(T312 `T312_FISH_ACT`)에 잇는다(훅 한 줄씩 · 로직 0 · 거리 감쇠 그대로) · 손에 든 고기 낙하 소리 = 플레이어 낙하 그 키 · 마을 어부 다섯이 동시에 낚을 때 probe(상시 절) (세션9 · T305 뒤 · 2026-09-19)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T321.md` · 레포 `지시/지시_T321.md`)

---

T305(main · 곡선 한 벌 `bgm.js` 정본 · 서버 동사 6줄 · probe 상시 · 볼륨 유도 0.6414 → #44 재민 · 정악 → #45 PM "지금은 안 쓴다") **통과**(족보 — "두 벌을 한 벌로"). T312(어부 이식 · 손잡이 끔)가 main 에 앉았다 — 소리를 걸 자리가 생겼다.

**카드 T321 — 셋. PM 판정**: ① **훅 셋** — 어부 NPC 의 던짐/입질/걸림(T312 `villages.js` 어부 행위 자리 · 세션3 가 T316 으로 같은 파일을 만진다 — **훅 한 줄씩만** · 리베이스 때 둘 다 살린다)에 T272 키 `cast`·`bite`·`hook` 을 `window.__sfx && __sfx.play(...)` 한 줄로 · 발신은 서버 동사(T305 문법 — `gauges`/행위 메시지에 낱말 하나 · 클라가 모양이 아니라 **동사**로 읽는다) · 손잡이 끔이면 훅이 안 불린다(당연 · 표) · 켠 판은 랩/헤드리스에서 호출 수로(족보 226 발신 훅은 거짓 소리 — **수신에서 잰다**). ② **낙하 소리** — 어부가 죽어 고기가 떨어질 때 플레이어 낙하 키 **그대로**(새 키 0 · 표 한 줄). ③ **probe 절** — 어부 다섯 동시(`cast`×5 + 마을 배경) 오프라인 렌더 → 피크·클리핑 0(`test-audio ⑪` 문법 · 곡선 상한 0.952 로 원리상 0 — 그래도 잰다). ④ 한 줄 답. 하루. 질문 금지.

★검증 규약: `test-audio` 89 + 절 · `e2e-audio-probe` · `test-client-globals`(새 전역 0) · `e2e-weather` 31/0 · 서버 diff = 동사 낱말 한 자리 · `lab-modules --diff` 교집합 ∅ · 접점 심볼(`cast|bite|hook|__sfx|inventoryWhere|T312_FISH_ACT|_deathDrop|gauges`).

## 1. 절대 규칙
훅 한 줄씩 · 로직 0 · 새 키 0 · 자산 무접촉(세션8) · 서버는 동사 낱말 한 자리 · ★푸시 2판(가지 `batch/fish-sfx-0919` → main ff · T316 와 같은 파일이면 리베이스 · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T321] · `보고/T321_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 훅 3 · 수신 호출 … · probe 0 클리핑". 실기 재민 귀(켠 뒤).
