# 지시 T368 추신 (세션1 · 2026-09-25 · PM)

`~/Mini/지시_T368.md`(레포 `지시/지시_T368.md`) 그대로 하되 **베이스 셋이 바뀌었다**:

1. **T367(다섯째 판)은 착지를 보류했다** — 가지 `batch/baseline5-0923` 에 있다(T367 §머리: 카드의 자 `t17-metrics` 로는 4판을 못 잰다 — 밭 상태기를 안 돌아 **거짓 기근**(인구 −57% · 소멸 11~12) · 맞는 자 `t176-ab` 로는 인구 −11.7% · 소멸 1·0·1). 기준선을 바꾸는 것은 재민 승인 자리(#69)라 main 은 **넷째 판 그대로**다. ⇒ T368 의 "끔 = 다섯째 판 비트 동일" 은 **"끔 = 넷째 판 3시드 열 열 비트 동일"** 로 읽는다. 켬 팔은 `T100_FIELD_YIELD=1 T368_FARM_ACT=1` 로 **환경변수로 켜서** 잰다(기본은 둘 다 끔 그대로).
2. **T374 는 main 이다**(`2a1b8208` · `T374_DEMAND_STOP` 끔 · `_t347InflowToday` 장부 다리 신설). 농부 행위의 장부 다리는 **그 다리와 같은 꼴**(어부 T312 · 나무꾼 T325 · 채집 T374 §③ — 넷째 적용)로 놓는다 · `test-forage-act` 는 이제 137.
3. **★이 카드가 곧 #69 의 입력이다** — 농부가 관측자 무관 행위로 거두면 "밭 수확은 생활층 한 곳"이라는 사실은 그대로이므로 `t17-metrics`(econ 단독)는 여전히 4판을 못 잰다. ②의 30일 등가는 **`t176-ab` 자(생활층)** 로 재고, 보고 머리에 한 줄: "econ 단독 자로 다섯째 판을 재려면 무엇이 있어야 하나 — 표만(자를 고치지 않는다 · #69 재민)".
4. 가지 이름 `batch/farm-act-0925` · 나머지(하네스 · 무접촉 목록 · §6 턴 규약) 카드 그대로. T385(세션7 순회)·T394(세션3 일터)·T393(세션2) 무접촉.
