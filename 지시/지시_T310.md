# 구현 지시 — T310: 배포 뒤 확인 표(#27 · 09-18 배포본 — `/check_username` #2 · `?as=` #9 · 문 62 라우트 `test-doors` 실서버 팔 · `/perf` 한 줄) · `인계/E-사건장부.md` 15-벽 → 10 정정 · 남은 열 열 개에 "없는 동사" 자리 표(코드 0) (세션7 · T302 뒤 · 2026-09-19)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T310.md` · 레포 `지시/지시_T310.md`)

---

T302(`batch/fromecon-five-0918` `38b075b7` → PM 이 main 에 착지 · `PV_DEPOSIT_MAP` 다섯 줄 한 줄 · `test-events` ㊽ 272→284/0 · 시드 1020 열 열 동일 · 3시드 반사실 0·0·1 · **"낼 수 있게는 됐는데 아직 아무도 안 물어본다 — 벽과 게시는 다른 문"**) **통과**. 벽 15→10 을 닫으면서 게시 0/0/0 인 이유(`_consEMA` 0/51)를 같이 낸 것이 답이다. #33 은 재민 칸 그대로.

**카드 T310 — T302 회부 ③⑤② · 코드 0. PM 판정**(재민 거부권): ① **배포 뒤 확인 표** — 재민이 09-18 에 `redeploy-hanbando.sh --all` 로 올렸다. 실서버(주소는 `DEPLOY.md`·`Caddyfile` 정본 · 새로 짓지 않는다)에 대고 **읽기만**: #2 `/check_username`(있는 이름/없는 이름/빈 값 셋) · #9 `?as=`(T245 가 닫은 문이 실서버에서도 닫혔나 — 401/403 한 줄) · `scripts/test-doors.js` 를 실서버 팔로(있으면 그 env · 없으면 로컬 62 라우트 기대표를 curl 로 한 바퀴 · **쓰기 라우트는 안 친다** · 열쇠는 안 보낸다) · `/perf` 한 줄(존 틱 p50 · 관측자 수 · 마을 수 — 재민 #6 `docker exec` 한 줄 대신 HTTP 로 읽히면 그것) · **T290 회부 "#27 배포 뒤 실기" 는 이 표로 닫힌다**(재민 손 실기는 눈·귀 것만 남는다). ② **`인계/E-사건장부.md` 15-벽 표 → 10** — T302 가 main 에 올랐으니 이제 고쳐도 거짓말이 아니다(그 줄만 · 날짜·해시). ③ **남은 열의 "없는 동사" 자리 표** — `wheat`·`rice`·`barley`·`vegetable`·`mushroom`·`chestnut`·`walnut`·`acorn`·`grape`·`mulberry_fruit` 열 개마다 "플레이어 손에 오는 길이 없다" 가 정확히 어느 동사인지(수확 `harvest` 가 있는데 품목이 안 실리나 / 채집 `forage` 표에 없나 / 요리만 있나) 자리·파일·줄 — **코드 0 · 표만**. 생산 캐논(설계_생산_실체 · 어부 → … → 채집 → 농부)이 그 자리를 실체로 옮길 때 이 표가 지도다. ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: **코드 0**(인계 한 줄 + `보고/` 하나 · 계측 curl 스크립트는 `scripts/` 에 `@regress` 없이 허용) · 실서버는 **읽기만**(POST·쓰기 0 · 열쇠 0 · 응답에 키·토큰 있으면 마스킹) · 3시드 면제(만진 정본 0) · 접점 심볼(`check_username|?as=|test-doors|/perf|PV_DEPOSIT_MAP|fromEcon|harvest|forage|E-사건장부`).

## 0. §0 실측 (넷)
ⓐ 실서버 확인 표(#2 · #9 · 62 라우트 · `/perf`). ⓑ 장부 정정 diff 한 줄. ⓒ 열 개 동사 자리 표. ⓓ 한 줄 답.

## 1. 절대 규칙
코드 0 · 실서버 쓰기 0 · 배포 재민만 · `villages.js` 무접촉(T305 세션9 서버 두 자리 · T298 세션4 · T295 세션10 접점) · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/deploy-check-0919` → main ff · 통로 클론 무접촉 · 403 이면 `~/Mini/_inbox/` 번들) · force 0.

## 2. 작업 (넷)
① 확인 표 ② 장부 한 줄 ③ 동사 표 ④ 한 줄 답.

## 3. 검증
코드 0 · 읽기만 · 접점 목록.

## 4. 회부만
#33 캐논(재민) · `nippon` 시딩(Z · PM) · 동사 없는 열 실체화(생산 캐논 순서 · PM).

## 5. 완료
커밋 [승인 대기 · T310] · 가지 푸시 · `보고/T310_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> — #2 … · #9 … · 라우트 n/62 · perf p50 … · 동사 표 10". 실기 0.
