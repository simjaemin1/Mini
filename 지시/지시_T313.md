# [Sonnet] 지시 — T313: 자산·문서·확인 둘째 — `test-assets-audit` 가 BGM 표를 `measured` 열로 읽게(하네스 한 줄) · 공통 §6 완료 기준에 "보고 머리 15줄 결론" 을 한 줄로 · `지시/README` 배정표 갱신 · 세션10 T295 회부(전쟁 세 줄 자리 `villages.js:2982`) 한 줄 전달문 · `_bundles_pushed/_MANIFEST.txt` 정리 (세션11 · T306 뒤 · 2026-09-19)

아래 전문을 **세션11** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T313.md` · 레포 `지시/지시_T313.md`) **`[Sonnet]` 카드 — 판정 0 · 새 코드 0(하네스 한 줄 · 문서) · 정본 무접촉 · 답은 표와 파일.**

---

T306(`batch/doc-audit-0919` → main · 13곡 ffmpeg 한 자 재측 2/2 동일 · 합성기 대비 최대 Δ 23.58s/4.02dB/3.75dB · 옛 기준선 흔적 0 · 번들 main 밖 0 · 보고 머리 규약 위반 8/13) **통과** — Sonnet 첫 카드가 표만으로 끝났다. ★PM 결정: **`measured` 열이 정본**(T257 이 "합성기 값은 ogg 와 안 맞는다" 고 이미 적었다 · 합성기 열은 지우지 않고 `_synth` 로 이름만 남긴다 — 자가 둘임을 보이게) · 과거 보고 소급 정정 **안 한다**(앞으로만) · `_inbox` T284 번들 둘은 PM 이 옮긴다.

**카드 T313 — 다섯.** ① **`test-assets-audit`** — BGM 표를 읽는 자리가 있으면 `measured` 를 읽게 **한 줄**(없으면 "읽는 자리 없음" 한 줄 · 새 절 만들지 않는다) · `render-meta.json` 의 합성기 세 열 이름을 `seconds_synth`·`rms_db_synth`·`peak_db_synth` 로(값 무변 · `_T313` 메모 한 줄 "정본 = measured · PM 09-19") · 초록 확인. ② **공통 §6 완료 기준** — `인계/공통.md` §6 에 한 줄: "보고 `보고/T◯◯◯_<날짜>.md` 는 **머리 15줄 안에 한 줄 답과 착지 해시**(PM·야간이 그 15줄만 읽는다 · T306 점검 8/13 위반)". 그 밖 무접촉. ③ **`지시/README.md` + `지시/세션배정_2026-09-19.md`** — 09-19 낮 배정으로 갱신(세션1 T311 · 세션2 T314 · 세션3 T312 · 세션5 T309 · 세션7 T310 · 세션8 T308 · 세션11 T313 · 나머지 그대로 — 보드 `~/Mini/태스크보드_v1.md` §5 `grep "진행("` 이 정본). ④ **세션10 전달문 한 줄** — T301 회부 ①(전쟁 세 줄의 자리는 `war-live.js` 가 아니라 `villages.js:2982~2996` · 붙일 세 줄은 `보고/T301_2026-09-19.md` §0ⓑ)과 야간 09-19 `test-granary-add` 빨강(PM 이 하네스 쪽 ⓐ 로 닫았다 · T284 제품 무접촉)을 **세션10 이 붙여넣을 한 문단**으로 `지시/지시_T295_추신.md` 에 쓴다(판정 0 · 사실만 · 출처 줄 번호). ⑤ **`_MANIFEST.txt` 정리** — `~/Mini/_push/_MANIFEST.txt` 와 `~/Mini/_bundles_pushed/_MANIFEST.txt` 둘을 **하나**(`_push/_MANIFEST.txt`)로 합친다(시간순 · 줄 무손실 · 옛 파일은 `_MANIFEST_old.txt` 로 이름만) · `~/Mini` 루트의 `T*.bundle` 24개는 `git bundle list-heads` 해시가 전부 origin/main 조상인지 표(T306 ⓒ 문법) — 옮기지 않는다(PM). ⑥ 한 줄 답.

★검증 규약: 코드 = 하네스 한 줄 + json 이름 + 문서 · `test-assets-audit` 초록 · `git diff --stat origin/main` 을 보고에 · 읽기는 `head`·`grep`·`sed -n` 만.

## 0. §0 실측
ⓐ 하네스 diff 한 줄 + 초록. ⓑ §6 한 줄 diff. ⓒ 배정표. ⓓ 추신 문단. ⓔ 장부 표. ⓕ 한 줄 답.

## 1. 절대 규칙
판정 0 · `sim/`·`server/`·`public/client` 무접촉 · 통로 클론 무접촉 · 배포 0 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/doc-audit2-0919` 가지만 · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · PAT 출력 금지.

## 5. 완료
커밋 [승인 대기 · T313] · 가지 푸시 · `보고/T313_<날짜>.md`(**머리 15줄에 한 줄 답**) · 채팅 "가지 올렸다 <해시>". 실기 0.
