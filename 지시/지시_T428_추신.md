# T428 추신 — 세션5 · `e2e-nature` 나무ⓗ 가 main 에서 빨갛다 — T408 의 **옳은 답**이 자의 옛 픽스처를 뒤집었다 (2026-09-26 · PM)

`~/Mini/지시_T428.md` 본문은 그대로다. 아래 한 절을 ⓪ 으로 먼저 한다(한 줄 · 반나절).

---

**사실(PM 이 밟았다 · land = main + 6가지)**: `e2e-nature` 144/1 — 빨간 줄은 **나무ⓗ "시더가 내는 나무가 전부 종을 들고 있다 — 0/0"** 하나(T398 보고 §1-7 이 main `7b7d4b90` 워크트리에서 먼저 봤다 · `057d40c1` 에선 초록). 심기①(밤·도토리)은 이번 판에 안 빨갛다.

**뿌리(코드)**: `scripts/e2e-nature.js:541` 이 `generateChunkResources('hanbando','forest', 300+k, 300+k, …)` 로 묻는다 — 청크 좌표는 **존 안 좌표**이고 한반도는 70,016×130,016px = 청크 **69×127**(cx 0~68 · cy 0~126)이다. (300,300)은 존 밖 307,200px 다. T408 전엔 숲 격자가 **아무 땅에나** 나무를 낳아서(그것이 T408 이 잡은 "남의 땅 1,610그루" 병) 존 밖 청크도 나무를 냈고 자가 그것으로 초록이었다. T408 뒤 `chunk.js` "개체는 제 땅에만"(`e.x < W && e.y < H`) 이 존 밖을 걸러 0 — **답이 옳고 픽스처가 틀렸다.** PM 이 확인한 값: (20,40) 5그루 종 5/5 · (30,60) 2/2 · (68,126) 2/2 · (69,127) 0(존 밖 · 맞다).

**할 것(넷 · 자만 · 제품 무접촉)**: ① 픽스처를 존 안 청크로(예 `20+k, 40+k` — 값은 `zone-config` 의 `zoneWidth/zoneHeight ÷ CHUNK_SIZE` 에서 유도 · 새 수 0). ② **미끼 한 줄**: 존 밖 청크(`⌈W/1024⌉, ⌈H/1024⌉`)를 물으면 **0 이어야 한다**(T408 규칙이 살아 있다 — 종전 비트를 먹이면 빨갛다). ③ 같은 꼴로 존 밖 청크를 묻는 자가 또 있나 `grep -n "generateChunkResources(" scripts/` 전수 표(있으면 같은 줄). ④ `e2e-nature` 145/0 · T398 §1-7 회부 닫힘 한 줄을 T428 보고 머리에.

가지는 T428 그대로(`batch/three-zones-0926`) · 커밋은 따로 하나 `[승인 대기 · T428 ⓪] e2e-nature 나무ⓗ 픽스처 존 안으로 + 존 밖 0 미끼` · 제품 코드 0.
