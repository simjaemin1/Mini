# 구현 지시 — T401: 11곡의 정본을 되찾는다 — T391: 배포 13곡 중 11곡은 **07-30 판**이고 그 판 코드(`tracks2.py`/`render2.py`)는 레포에 없다(두 zip 모두 07-31) ⇒ 정본 없는 배포 자산 · **07-31 코드(`코드백업.zip`)로 11곡을 다시 구워** 배포본과 SNR·패킷 대조(어느 곡이 얼마나 가까운가 · 07-31 판이 07-30 판과 같은 곡인가) · 지금 정본 파이프라인(`compose.py`/`gugak.py` · 뱅크 12묶음)으로 11곡을 구운 팔 → **브금판 A/B 행 11**(배포 안 함 · 로컬 청취본 · 재민 귀 #72) · 값 0 · 배포 파일 무변 (세션8/ART · T391 뒤 · 2026-09-26)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T401.md` · 레포 `지시/지시_T401.md`)

---

T391(`batch/bgm-restore-0925` → main · Python 기본값 `450c6e44` 로 복원(GPT 규칙은 `ARTICULATION_CONTRACT=False` 스위치 뒤) · **재굽기 해시 2/13 — 파일 바이트까지 같다**(`village_day_trad`·`village_day_jeongak` · vorbis 패킷 6517/6517 · numpy 2.3 이상 · Ogg 일련번호만) · GPT 기본값은 같은 뱅크에서 SNR −2~+3dB 의 다른 소리 · **11곡은 07-30 판 — 코드가 어디에도 없다** · `.pyc` 0 + `test-assets-audit ⑧` · `sigim()` 은 13곡 모두를 지난다(T353 정정) · 장구 궁편은 샘플(6조각 · `janggu_gung:'합성'` 은 그 2곡에 틀렸다 · 값 무변) · A/B 행 2) **통과** — "기본값을 돌리니 배포 두 곡이 파일 바이트까지 되살아났다 — 그리고 11곡은 코드가 없다"가 이 카드의 문장이다(족보 316). ★PM: 배포 11곡이 **정본 없는 자산**이라는 것이 이 카드의 물음이다 — "정본 하나" 규약 위반이고, 되찾거나(07-31 코드가 같은 곡을 내면 그것이 정본) 다시 굽거나(지금 파이프라인 · 소리가 바뀐다 → 재민 귀) 둘 중 하나다. **판정은 재민(#72)** · 이 카드는 두 팔을 나란히 세운다.

**카드 T401 — 넷 · 배포 무변. PM 판정**(재민 거부권): ① **07-31 코드 재굽기** — `코드백업.zip`(맥 · T391 이 찾은 그 둘 · 레포 밖 `~/Mini/_bgm_code/` 격리 풀기 · 원본 무변)의 `tracks2.py`/`render2.py` 로 11곡 굽기 · 씨앗이 `hash()`(PYTHONHASHSEED)라면 **씨앗 표**(어느 값에서 배포본과 가까워지나 — 0~n 훑기 · 새 수 아님 · 찾는 것) · 곡마다 배포본 대비 SNR·vorbis 패킷 일치율·길이 차 · **판정 표**: 같은 곡(패킷 ≥ 99%) / 같은 씨 다른 환경(SNR 16~22 · 07-30 미리듣기 바닥 20.3 안) / 다른 곡 — 곡마다 하나 · 같은 곡이 n/11 이면 그 코드를 **정본으로 레포에**(`public/assets/audio/bgm/legacy/` · 배포 폴더 밖 · 굽기 스크립트 + 씨앗 표 + `render-meta.json` 한 칸) · 안 되면 "정본 없음 · 왜" 한 줄. ② **지금 파이프라인 팔** — `compose.py`/`motif.py`/`arirang.py` → `gugak.py` + 뱅크 12묶음으로 같은 11곡 이름·장면·편성을 굽는다(악보가 없는 곡은 "악보 없음 · 07-30 판이 악보를 안 남겼다" 표 · **작곡하지 않는다**) · 곡마다 배포본 대비 SNR·RMS 차 · 길이. ③ **브금판 A/B 행 11** — `bgm-board.html`(T386·T391) 행마다 배포본 / 07-31 재굽기 / 지금 파이프라인 세 버튼(있는 것만) · 로컬 청취본(`~/Mini/_bgm_ab/T401/` · 배포 안 함 · sha256 으로 그 파일인지 잰다 — T391 문법) · ○/✗ 칸(재민 귀). ④ 한 줄 답 + #72 표(곡마다: 정본 되찾음 / 다시 굽는다 / 그대로 둔다(정본 없음 표기) — 권고 칸 · 판정 재민). 이틀. 질문 금지 — 답은 표.

★검증 규약: **배포 13곡 파일 무변**(diff 0 · 새 소리 배포 0) · `bgm.js` 무변 · `sfx/**` 무접촉(세션9 T402) · 새 파일 = `legacy/` 스크립트(①이 정본을 찾은 경우만) + 브금판 행 + `scripts/t401-bgm-legacy.js`(러너 밖) · `test-audio` 158 · `test-bgm-phrase` 16 · `test-assets-audit` PASS(+⑧ · legacy 폴더가 배포 폴더 밖인지 절 하나) · `test-render-score-articulation` 14 · `e2e-audio-probe` 58 · `test-client-globals` 26 · `test-harness-lint` 61 · 접점 심볼(`tracks2|render2|코드백업|PYTHONHASHSEED|render-meta|compose.py|gugak.py|bk_|_bgm_ab|_bgm_code|bgm-board|legacy|t401-bgm-legacy|t386-bgm-hash`).

## 0. §0 실측 (넷)
ⓐ 07-31 재굽기 표(11곡 × 씨앗 · SNR · 패킷 · 판정). ⓑ 지금 파이프라인 표(11곡 · 악보 유무 · SNR). ⓒ 브금판 행 11. ⓓ 한 줄 답 + #72 표.

## 1. 절대 규칙
배포 무변 · 작곡 0(있는 악보만) · 값 0 · 원본 zip·뱅크 무변(격리) · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/bgm-legacy-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 07-31 재굽기 ② 지금 파이프라인 ③ 브금판 ④ 한 줄 답 + #72.

## 3. 검증
배포 diff 0 · 하네스 일곱 · 접점 목록.

## 4. 회부만
#72 11곡(재민 · 브금판) · #70 무표기 재발음(재민) · #66 GPT 청취본(재민) · `janggu_gung` 값(재민 · T391 ○) · AI Hub·4060(#64 ②③).

## 5. 완료
커밋 [승인 대기 · T401] · 가지 푸시 · `보고/T401_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T401]` · 베이스 해시**) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 07-31 로 같은 곡 n/11 · 지금 파이프라인 n/11(악보 없음 n) · 브금판 행 11 · #72 표". 실기: 재민 브금판.
