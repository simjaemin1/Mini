# 구현 지시 — T324: 걷는 몸의 값 — 이동 한 걸음이 왜 0.25ms 인가(프로파일 · 자리별 ms) · 가장 싼 자름(길·충돌 질의를 마을 단위로 캐시 · 예산 커서 이어 돌기) · 목표 = 51마을 1,476명 전원 걷기 존 틱 p50 < 33ms(30Hz 한 틱) 또는 그 바닥이 얼마인지 (세션3 · T316 뒤 · 2026-09-19)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T324.md` · 레포 `지시/지시_T324.md`)

---

T316(`batch/fish-act2-0919` → main · 문이 둘이었다(결정 문 + 이동 문) · **51마을 1,476명 전원 걷기 = 존 틱 p50 2.47 → 374.6ms(152배) · lag 37% · drop 675** — T297 외삽 띠(10~32분/일) 안 · `_carry` 흡수 행동 무변 · 끔 3시드 동일 · 결정 문 15ms 예산이 앞에서부터 자르고 이동 문엔 예산이 없다는 비대칭) **통과** — 소스만 봤으면 "걷게 했다" 고 적었을 자리를 세계 위 자가 잡은 것(족보 250). 회부 ①(예산 비대칭)은 **설계 판정이 아니라 측정이 먼저다** — 375ms 가 어디서 나는지 모르면 LOD 든 예산이든 값을 정할 수 없다.

**카드 T324 — 셋. PM 판정**(재민 거부권): ① **프로파일** — 켠 팔(전원 걷기) 존 틱 한 번의 375ms 를 **자리별로**: 결정 문(`npcStep` 앞 · 목표 고르기 · `_jobSites` 질의) · 이동 문(`movePlayerStep` — 그 안에서 지형 술어 `isTerrainBlockedLocal`·물 `isWaterTileLocal`·청크 조회·충돌·길찾기 `pathfind` 각각) · 방송(`others` 갱신 · 관측자 없는 마을이 왜 방송 비용을 내나) · 장부(`fishToGranary` 등). 걸음당 µs 와 호출 수(족보 223). **한 걸음 0.25ms 가 어디에 쓰이는지** 표 한 장. ② **가장 싼 자름 둘 — 세계 무변**: ⓐ 관측자 없는 마을이 방송·`others`·시야 갱신을 **안 한다**(볼 사람이 없다 — 위치는 그대로 움직인다 · 세계 무변 · 관측자가 오면 그 틱부터 방송) ⓑ 마을 안 길(집↔강가·숲·곳간)은 **마을 단위 길 캐시**(같은 출발·도착 셀 쌍은 한 번만 찾는다 · 무효화 = 건물·나무 변경 때 · 사본 0 — `pathfind` 그 함수가 캐시를 갖는다) · 충돌은 동적 개체와만. ⓒ(허용 · 설계 아님) 결정 문 15ms 예산을 **커서 이어 돌기**로(앞줄만 일하는 결함 수리 · 전원이 순서대로 · 세계 무변은 아니다 — 결정 시점이 미뤄진다 · 그래서 표에 켠 팔 어획 합 전/후). **LOD·저Hz·마을별 상한은 이 카드에서 안 한다**(재민 설계 칸 #53). ③ **자** — 세계 위(T316 문법 `t316-fish-perf.js`) 끔/켬 두 팔 p50/p95 · lag · drop · **켠 팔 첫날 어획 합 = 수식**(30Hz 굶지 않은 틱 위에서 · T316 회부 ②) · 목표 **p50 < 33ms** — 못 닿으면 **바닥이 얼마인지**와 남은 비용의 주인(자리)을 표에(그게 #53 의 입력이다). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 끔 팔 3시드 비트 동일 · 관측자 켬 판 화면 무변(`e2e-fogray`·`e2e-weather` 그대로 · 관측자 있는 마을은 종전 경로) · `test-fish-act` 54 + 절(ⓐ 관측자 없는 마을 방송 0 · 관측자 진입 틱부터 방송 · ⓑ 길 캐시 = 무캐시와 같은 경로 200쌍) · `test-granary-haul` · `test-psite-server` · `test-war-world` 54(전쟁 병사도 같은 이동 문을 탄다 — 무변) · 접점 심볼(`npcStep|movePlayerStep|pathfind|isTerrainBlockedLocal|isWaterTileLocal|others|_jobSites|T312_FISH_ACT|VILLAGE_DAY_MS|t316-fish-perf`).

## 1. 절대 규칙
새 수 0 · 배수·상태 스위치 0 · LOD 0(#53 뒤) · 사본 0 · 손잡이는 `T312_FISH_ACT` 하나 그대로 · **T295(세션10 전쟁 절)·T315(세션4)·T325(세션1 나무꾼 — 같은 이동 문을 쓴다 · 캐시는 이 카드가 정본)** 무접촉 · ★푸시 2판(가지 `batch/walk-cost-0919` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
#53 걷기 예산 설계(재민 · 이 표 뒤) · `G_CARRY` 3 vs 6(재민) · `VILLAGE_DAY_MS` 자 규약(PM) · 얇은 물 15마을 30일(①이 선 뒤).

## 5. 완료
커밋 [승인 대기 · T324] · `보고/T324_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 375ms 의 주인 … · 자름 뒤 p50 … · 바닥 …".
