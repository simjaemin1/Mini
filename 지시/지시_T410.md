# 구현 지시 — T410: 존이 자도 걷는다 — T368 이 잡았다: 존에 사람 player 도 관측자도 없으면 존 틱이 **NPC 루프 앞에서 돌아간다**(`zone.js:11812` "14.49-e3-perf5 idle zone skip · 26 zone 중 25개가 idle이면 CPU 거의 0") ⇒ 아무도 접속 안 한 서버의 몸은 한 걸음도 안 걷고(걸음 0 · 수확 0) 어부·나무꾼·채집·농부 행위가 전부 **일괄로 떨어진다** — 캐논 ⓑ① "관측자 없어도 실걸음" · 재민 `767b827e` "아무도 안 보는 마을도 일과가 작동해야" 가 **존 단위에서** 깨져 있었다 ⇒ ★PM 결정(위임): idle skip 을 **연다**(손잡이 `ZONE_IDLE_SKIP` 되돌림 · 기본 = 안 건너뜀) · **값이 카드다**: 사람 0 존의 틱 ms(낮·밤) · 26존이 한 호스트에 있을 때 대 존마다 제 서버(재민 확정 플랫폼 방향)일 때 · 잠든 존이 깨어 있는 존의 틱을 먹지 않는지 · T368 B(몸 XOR 일괄) 판정은 그대로 두되 "존이 잠"인 팔이 사라지는지 실측 (세션1 · T368 뒤 · **T385(세션7 틱 본체) 착지 뒤 리베이스** · 2026-09-26)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T410.md` · 레포 `지시/지시_T410.md`)

---

T368(`batch/farm-act-0925` → main · 품목 항등 안 선다(200일 13.5~14.5% · 채소 66% · 곡물 품질 내림 59~64%) · 켜면 200일 인구 −36~−47% · 30일 등가는 선다 · 헤드리스 농부 절 켜면 호출 0 · **존이 통째로 자면 몸도 잔다**(B: 몸 XOR 일괄) · 사람당 1.818 → 3.150µs(예산 95%) · 끔 = 넷째 판 `t176-ab` 전수 차 0 · `test-farm-act` 59/0) **통과** — "품목 항등은 안 선다 — 그 차가 발견이고, 켜면 세계가 작아진다"가 이 카드의 문장이다(족보 326). ★PM: 켜기·앵커↔작물 표 틈은 #69·#75(재민). **존이 잠드는 문**은 캐논 문제라 PM 이 판정한다 — 그 문이 있는 한 "관측자 없어도 실걸음"은 존이 깨어 있을 때만의 말이다.

**카드 T410 — 넷. ★PM 결정(위임 · 재민 거부권)**: ① **문을 연다** — `zone.js:11812` `if (!hasHuman && !hasObserver) return;` 를 손잡이 `ZONE_IDLE_SKIP`(기본 **0** = 안 건너뜀 · `=1` 이면 종전) 뒤로 · 5초 유지보수 갈래는 그대로 · 열면 `updateActiveChunks` 가 관측자 0 에서 어떤 청크를 켜나(T316 `_t316WalkAlways` 가 연 비활성 청크 문 둘과 맞물림 — **마을 청크는 몸이 있으면 켜진다**가 정본이어야 한다 · 표) · `_t368ZoneAwake` 는 그대로(열면 늘 참 · 하네스 ⑤ 가 그것을 문다). ② **값** — 사람 0 · 관측자 0 존 하나(한반도 51마을 · 몸 1,4xx): 틱 p50·p95(낮·밤) · 사람당 µs(T356 자) · CPU% · 메모리 — 잠든 판(종전) 대 깨운 판 · **26존 한 호스트** 팔(9존 부팅 · T373 자 · 한 프로세스씩 · 합 CPU) · 재민 플랫폼 방향(존마다 제 서버)에서는 이 값이 곧 "빈 서버 한 대의 부하" 라는 한 줄 · 예산(캐논 3.3µs · 틱 33.3ms)에 닿나. ③ **행위 넷 실측** — 사람 0 · 관측자 0 · 200일(T368 자 `t368-farm-day.js` 그대로 + 어부·나무꾼·채집 켬 팔): 걸음 `walk.steps` 0 → n · 수확·어획·벌목·채취 건수 · **일괄 호출 0**(하네스) · 곳간 곡선이 관측자 1 팔과 같은가(T297 자 · 관측자 무관 게이트). ④ 한 줄 답 + T368 B 판정 갱신(존이 잠드는 팔이 없어지면 "몸 XOR 일괄"은 "몸" 하나 — 표만 · 코드는 그대로 두고 회부). 사흘. 질문 금지 — 답은 표.

★검증 규약: **T385(세션7 · `zone.js` 틱 본체 순회) 착지 뒤 그 위에서** — `zone.js` 는 idle 문 한 자리만 · 3시드 면제 근거(존 틱 · econ 무접촉) 한 줄 · 되돌림 `ZONE_IDLE_SKIP=1` = 종전 비트 동일 · `test-move-soa` 129+ · `test-farm-act` 59 · `test-fish-act` 73 · `test-wood-act` 90 · `test-forage-act` 137 · `e2e-village` · `e2e-move` 37 · `test-nippon-boot` 32(닛폰도 잠에서 깬다 — 그 존의 값 한 줄) · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`hasHuman|hasObserver|_idleSkipAt|ZONE_IDLE_SKIP|updateActiveChunks|_t316WalkAlways|_t368ZoneAwake|anyViewerNear|t368-farm-day|t297-observe|t356-tick-anatomy`).

## 0. §0 실측 (넷)
ⓐ 문 diff + 청크 표. ⓑ 값 표(존 하나 · 26존 · 예산). ⓒ 행위 넷 실측(걸음 · 건수 · 일괄 0 · 곳간 곡선). ⓓ 한 줄 답 + B 갱신.

## 1. 절대 규칙
새 수 0 · 손잡이 하나(되돌림) · `zone.js` idle 문만 · **T385 뒤 리베이스**(그 전엔 ②③ 계측 팔만 먼저) · **T398(세션2)·T399(세션3)·T400(세션4)·T407(세션6) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/zone-awake-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 긴 창은 `setsid nohup … & disown`)**.

## 2. 작업 (넷)
① 문 ② 값 ③ 행위 넷 ④ 한 줄 답.

## 3. 검증
되돌림 동일 · 하네스 열 · 접점 목록.

## 4. 회부만
#69(재민 · 4판 켜기 — 이 카드 ③ 곳간 곡선이 입력) · #75 앵커↔작물 표 틈(재민) · 텃밭 하한(재민) · 26존 부하 값(재민 · 플랫폼) · T368 B 코드 정리(PM · ④ 뒤).

## 5. 완료
커밋 [승인 대기 · T410] · 가지 푸시 · `보고/T410_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — idle 문 열림 · 빈 존 틱 p50 … ms(낮) · 26존 합 CPU …% · 걸음 0 → n · 일괄 0 · 곳간 곡선 관측자 1 과 같음 ○/✗". 실기 0.
