# 구현 지시 — T378: 마을 안에 숲이 들어와 있다(재민 실기 09-23) — `clearTreesInCells` 가 **활성 청크의 개체**(`qtResources`)만 베므로 부팅·영토 성장 때 관측자가 없는 마을은 한 그루도 안 베어지고, 청크가 나중에 켜질 때 그 셀에 나무가 선다 · 자로 확인(51마을 영토 셀 안 나무 수 — T301 색인 `resourcesAtCell` · 관측자 무관) · 고침: 개간을 **색인 기준**으로(영토 셀의 씨 나무 `seedKey` 를 `_markHarvested` — 청크가 켜지든 말든 안 선다 · 같은 함수 · 사본 0) · 3시드 면제 · 실서버 게이트 (세션2 · T355 뒤 · 2026-09-23)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T378.md` · 레포 `지시/지시_T378.md`) **⚠T355 착지 뒤에 붙인다.**

---

재민 실기 09-23: **"마을에 스폰되자마자 숲이 너무 많고, 애초에 마을 안에 숲이 들어와 있으면 안 되지."** 캐논은 이미 있다 — `villages.js:2680` "★[11차 재민 확정] 마을 안엔 숲이 없다 — 영토 셀의 나무를 벤다(개간)". 그런데 그 함수 `zone.js:2903 clearTreesInCells` 는 `qtResources.queryCircle` 로 **지금 서 있는 개체**만 찾는다 — 개체는 **활성 청크**(관측자 1,200px 안)에만 있다. 부팅(`:2682`)·영토 성장(`:2510`)·`:1386`·`:4482` 네 자리가 다 이 함수를 부르는데, 관측자 없는 마을에선 `queryCircle` 이 빈 배열이라 **0그루**를 베고 끝난다("멱등" 주석은 벤 게 있을 때만 참이다). 나중에 플레이어가 오면 청크가 켜지며 `generateChunkResources` 가 그 셀에 나무를 세운다 — 재민이 본 것이다. **추측이다 — 먼저 잰다.**

**카드 T378 — 셋. PM 판정**(재민 거부권): ① **자** — 51마을 영토 셀(`terrSet`) 안의 씨 나무 수를 **색인**(`resourcesAtCell(zoneId, cx, cy, harvestedSeeds, day)` · T301 · 관측자 무관)으로 센다(마을별 · 합 · 영토 셀 수 대비) · 부팅 로그의 "영토 개간 — 나무 n그루" 줄이 마을마다 몇이었나(0 이 몇 마을) · 그 둘이 맞물리면 진범 확정 · 계측기 `scripts/t378-forest-in-village.js`(러너 밖). ② **고침** — `clearTreesInCells` 가 **색인**으로 그 셀의 씨 나무를 찾아 `seedKey` 를 `_markHarvested` 한다(개체가 서 있으면 지금처럼 지우고 방송 · 안 서 있으면 장부만 — 청크가 켜질 때 `harvestedSet` 이 막는다 · `regrowStageOf` 규율: 벤 나무는 T122 대로 다시 자란다 → **영토 안에선 다시 안 자라야 한다** — `_terrGrow` 셀은 `_markHarvested` 뒤에도 재생 갈래에서 제외(영토 셀 집합을 재생이 본다 · 새 수 0 · 어느 술어가 정본인지 표) · DB 나무(`r.dbId`)도 같은 길 · 새 손잡이 0(캐논 이미 확정 · 되돌림은 커밋). ③ **게이트** — 실서버 51마을 부팅 뒤 관측자를 마을 셋에 차례로 세워 청크를 켜고 영토 안 나무 = **0**(전/후 표 · `test-terr-persist` 문법) · 영토 밖 나무 수 무변 · `test-trees` 91 · `test-wood-act` 87(나무꾼 스캔 반경이 영토를 넘으므로 값이 조금 변할 수 있다 — 표) · `test-resource-index` 20 · 3시드 면제(`sim/` 무접촉). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: `sim/` 무접촉 · 실서버 전/후 표 · 하네스 넷 · 접점 심볼(`clearTreesInCells|qtResources|resourcesAtCell|_markHarvested|harvestedSeeds|regrowStageOf|_terrGrow|terrSet|generateChunkResources|t378-forest-in-village`).

## 1. 절대 규칙
새 수 0 · 새 손잡이 0(캐논 확정 사항의 결함 수정) · **T370(세션3 `zone.js` 경로)·T374(세션1)·T375(세션7 틱 머리) 무접촉 — `clearTreesInCells` 몸통과 `_terrGrow` 호출부만** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/forest-clear-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
스폰 직후 "숲이 너무 많다"(영토 **밖** 숲 밀도 — 지형 `forests` 정본의 문제면 Z · 표만) · 영토 안 재생 술어(PM).

## 5. 완료
커밋 [승인 대기 · T378] · 가지 푸시 · `보고/T378_<날짜>.md`(**머리 15줄에 전/후 표**) · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 영토 안 나무 … → 0 · 마을 n · 밖 무변". 실기: 재민이 스폰 마을 한 바퀴.
