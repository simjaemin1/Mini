# 세션 배정 — 2026-09-25 (PM 정본 · 카드 T389 표 그대로 · main `462b4acc` 기준 · 09-25 낮)

각 세션 채팅에 **아래 한 줄**만 붙여넣으면 된다. 카드 전문은 `~/Mini/지시_T◯◯◯.md` 와 레포 `지시/지시_T◯◯◯.md`(정본 · 세션이 `git pull --ff-only origin main` 뒤 읽는다). 요약은 안내일 뿐, 정본은 각 카드 원문이다.

| 세션 | 모델 | 카드 | 상태 | 붙여넣을 한 줄(카드 H1 요약) |
|---|---|---|---|---|
| 1 | Opus | T374(진행) → T368(대기) | `batch/demand-stop-0923` 미착 | T374: 채취는 수요가 멈춘다(등가 4,106%→152.6% · 군락 96.2% 보존 · 켜지 않았다). 뒤이어 T368(T374·T367 뒤 대기): 농부 행위 완성 — 수확 품목이 곳간에 그대로(새 수 0) · 관측자 무관 실걸음 · 손잡이 `T368_FARM_ACT` 기본 끔. |
| 2 | Opus | T378(진행) | `batch/village-forest-0923` 미착 | 마을 안 숲 — 개간이 관측자 없는 마을에선 한 그루도 못 벴다(뿌리 둘+DB 나무 구멍 하나) · 색인으로 베게 고쳤다 · 영토 안 나무 → 0. |
| 3 | Opus | T381(진행) | `batch/pf-anatomy-0923` 미착 | 1.55ms 의 주인은 "1500칸을 다 태우고 null"(호출 37%·시간 93%) · 목표 칸이 애초에 못 가는 칸이 절반 · `localPath` 끝점 순서 결함 고침(`T381_PATH_DEAD` 기본 끔) · A* −48~56%. |
| 4 | Opus | **T383**(새) | — | 밤 빨강 둘의 귀속 — `test-site-memo`(연속 6밤)·`test-terraincache`(연속 3밤)를 자 결함/제품 결함으로 가른다(T382 GPT 별도 클론 재현과 대조) · `e2e-verbs` 간헐은 표만. |
| 5 | Opus | **T384**(새) | — | 띠는 클라 혼자 켠다 — T380 이 밝힌 650~1,500px 띠 나무는 자료가 있는데 그리기가 버린 것 · 띠 기본 켬(세계 무변·방송 0) · 원경 방송은 손잡이 그대로 · 색인 문 17.8ms 해부 표. |
| 6 | Opus | T367(진행) | 다섯째 판 | 손잡이 셋(`T100_FIELD_YIELD`·`T193_LEDGER`·`T195_TOOL_WEAR=0.9`) 기본 켬 · 되돌림이면 넷째 판 비트 동일 · 3시드 800일 다섯째 판 기준선 표 · 공통.md §3 갱신. |
| 7 | Opus | **T385**(새) | — | 순회 일곱을 하나로 — T371 회부 1-ⓑ(순회 한 바퀴 ≈0.21~0.25µs/사람) · 의존 표가 본체(합칠 수 있는 것만) · 게이트=T375 ⑨ 그대로 · `stairs` 문자열 키→정수 키. |
| 8 | Opus | **T386**(새) | — | GPT BGM R&D(`codex/bgm-rnd-01` 86커밋→main 스쿼시 `462b4acc`) 검수 — `bgm.js` opt-in 경계 감사(13곡 해시로 legacy 비트 동일 실증) · `설계/BGM_RND_*` 11장 채택/버림 표 · DDSP-Gugak 라이선스 표. |
| 9 | Opus | **T387**(새) | — | 사람/전투 갈래 11자리 확보 — T365 갈래 표에서 소리가 하나도 없는 유일한 갈래(`arrow_spawn`·`hp_changed`·`player_attacked` 등 11자리) 붙을 자리만 확보(상태 동기화 메시지는 제외). |
| 10 | Opus(다른 기기) | **T388**(새) | — | 군량 = 행위 — 설계 표만(코드 0) · 지금 군량은 장부(출정 때 한 번 싣고 시계로 깎는다)인데 캐논은 "생산은 행위" → 군대가 먹는 것도 지고 간 것을 몸이 먹는 행위여야 한다. |
| 11 | **Sonnet** | **T389**(이 카드) | — | 배정표 09-25 신설 · 보고 머리 15줄 점검 셋째(T358~T382 · 위반은 세기만) · `nightly-known.txt` 대 야간 두 밤 대조(올리지 않는다) · README 날짜 한 줄. |
| 12 | **Sonnet** | **T369-b**(대기→진행) | — | 민물고기 10종 가중 고증 둘째 판 — 세션13 첫 판(채택 0)을 이어 열리는 출처(환경부 물환경정보시스템·국립생물자원관·KCI/RISS)를 먼저 확보해 실제 표를 옮긴다 · 출처 둘 이상 같은 단위면 평균 가중. |
| 13 | GPT(별도 클론) | **T390**(새) | — | T382 의 빨강 12 중 환경 6(`playwright` 미설치 4·`test-nightly` 낡은 pid·`test-redeploy` VPS 스크립트)을 자기 클론에서 닫고 다시 돈다 · `test-harness-lint` 60/1 의 그 하나 정체 · 가지 `codex/repro-0925`. |

* 이 표는 카드 T389 본문에 PM 이 이미 적어 준 09-25(낮) 배정을 그대로 옮긴 것이다 — `~/Mini/태스크보드_v1.md`(정본 §5)는 이 기기에도 없다(T313·T366 때와 같은 결손 · 카드 자신이 표를 본문에 박아 줬다).
* "붙여넣을 한 줄"은 각 카드 첫 줄(H1)을 세션11 이 요약한 것이다 — 원문은 반드시 `지시/지시_T◯◯◯.md`(또는 `T369-b`) 전문으로 확인한다(요약은 안내일 뿐 정본이 아니다).
* T366(`batch/doc-assign-0923` → main · 배정표 09-23 신설 · 보고 머리 15줄 점검 둘째 T347~T357 위반 6/11) **통과** — 카드 T389 본문에 PM 이 이미 적었다.
