# 구현 지시 — T383: 밤 빨강 둘의 귀속 — `test-site-memo`(연속 6밤) · `test-terraincache`(연속 3밤) — 야간 러너는 세기만 했고 T382(GPT 독립 재현)가 같은 둘을 별도 클론에서 다시 잡았다 · **자의 결함이면 자를, 제품의 결함이면 제품을** — 어느 쪽인지 먼저 표 · `e2e-verbs` 간헐(1밤 · 단독 초록)은 표만 (세션4 · T376 뒤 · 2026-09-25)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T383.md` · 레포 `지시/지시_T383.md`)

---

T376(`batch/body-cap-0923` → main · **답: 예 — 40 을 풀면 예산 안**(무제한 몸 2,515 = econ 전부 · 낮 p50 23.9ms = 72% · p95 94% · drop 0 · lag 0 · 소멸 0 · 집·침상 불변) · 값은 비선형(40→80 +6.1ms · 80→무제한 −0.4ms — "사람당 6.60µs" 는 끝점 차분일 뿐이라 스스로 적음) · 시공 자 1.25배는 노동량이 아니라 **"하루" 단위의 불일치**(낮으로 맞추면 0.88배) · 제품 무접촉 · 계측기 `t376-body-cap.js`) **통과** — "답이 예인데 값이 선형이 아니면 그 수는 기울기가 아니다"가 이 카드의 문장이다(족보 300). 켜기는 #54 재민(표가 값을 냈다).

이번 카드는 세션4 의 영역(집터 · 지형 캐시 — T342·T333 이 세션4·세션3 것이었지만 지금 세션3 은 T381 `pfFindPath` 에 있다)이라 세션4 가 받는다. 밤 러너 `보고/야간러너_2026-09-25.md` §1 이 **상시 목록 밖 빨강 3건**을 세기만 하고 "귀속은 사람 몫"이라 넘겼고, GPT T382 가 별도 클론에서 111종을 단독으로 돌려 같은 둘을 다시 잡았다(PM 이 land 에서 재실행: `test-terraincache` **18/4** · `test-site-memo` 는 PM 컨테이너에선 포트 충돌로 못 봄 · `test-body`·`test-hunt-vis`·`test-itemlabel` 은 초록 — GPT 쪽 흔들림).

**카드 T383 — 셋 · 귀속이 본체. PM 판정**(재민 거부권): ① **`test-terraincache` 4 빨강 귀속** — 실패 넷은 전부 "정적 검사"다: `isWaterTileLocal`·`isRockTileLocal` 에 `terrain` 호출이 **캐시 가지 1 + 종전 가지 1 = 2개** 여야 하는데 **1개**이고, 두 가지의 인자식이 글자까지 같아야 한다는 절. 즉 자가 **T333 이전의 모양**(캐시 가지 · 종전 가지 둘)을 글자로 떠서 재고 있다. 표: 그 두 함수의 지금 본문(줄 번호 · 어느 카드가 바꿨나 — `git log -L` 로 T333 `8c266bc4` · T345 `faae1b04` · T347 `94df1ef` 중 어느 것) · 자가 묻는 "두 가지" 가 지금도 있어야 하는 계약인가(T333 이 캐시를 정본으로 올려 종전 가지를 지운 것이면 **자의 결함** → 자를 지금 모양에 맞춘다 · 캐시가 종전과 다른 점을 묻게 됐으면 **제품의 결함** → 제품을 고친다) · 판정 근거는 **동작 게이트**(같은 좌표 1,000점을 캐시 켬/끔으로 물어 답 비트 동일 — 그게 자가 원래 재려던 것)로 세운다 · 정적 절은 그 동작 절이 초록일 때만 지운다. ② **`test-site-memo` 귀속** — 빨강 여덟 줄(① 종전 판이 하루 5회 이상 집터를 훑는다 19회/9일 · 빈손 0 · 종전 60ms > 채택 63ms · ③ 스킵 0 · 훑은 셀 111,900 < 111,900 · ④ 스킵 0)은 전부 **"종전 판과 채택 판이 같다"** 를 말한다 — 대조군(종전)이 채택본과 같은 코드를 돌고 있거나(픽스처가 손잡이를 못 켬 · T355 가 하네스 기동을 `fixture-boot` 로 옮기며 env 가 안 넘어갔을 수 있다 — 후보 `4d7d810`) 집터 메모(T342)가 실제로 안 건너뛰거나(제품). 표: 대조군 팔이 실제로 다른 코드를 도는지(env 도착 여부 · 손잡이 이름 · `process.env` 를 찍는 한 줄) → 자의 결함이면 자를 · 제품이면 **왜 안 건너뛰는지**(메모 키가 안 맞나 · 훑는 자리가 다른 함수로 옮겨 갔나 · T347 `94df1ef` 이 집터 절을 만졌나) · 6밤 전 첫 빨강 날짜(`보고/야간러너_*.md` 역추적)와 그날 착지한 카드. ③ **`e2e-verbs` 간헐**(1밤 · 18분 · "화면 안의 그 사람에게 메뉴가 열렸다 — 아무도 안 눌렸다" · 단독 초록) — **표만**: 밤에 어떤 순서로 돌았고(앞 하네스가 남긴 서버·포트) 단독과 무엇이 다른지 한 줄 · 고치지 않는다. ④ 한 줄 답 + **`nightly-known.txt` 에 올릴지 표**(둘 다 "귀속됐고 고쳤다" 면 올릴 것 0 · 못 고친 것은 이름·이유 한 줄 — 올리는 것은 PM). 이틀. 질문 금지 — 답은 표.

★검증 규약: 제품을 고치면 **그 자리만**(한 함수 · diff 줄 수를 보고에) · `sim/` 무접촉(만지면 3시드) · 자를 고치면 **자명 통과 금지**(고친 자가 옛 결함 모양의 미끼를 문다는 반례 한 줄) · `test-terraincache` n/0 · `test-site-memo` n/0 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`isWaterTileLocal|isRockTileLocal|terrain-tilecache|test-terraincache|test-site-memo|siteMemo|fixture-boot|nightly-known|야간러너|e2e-verbs`).

## 0. §0 실측 (넷)
ⓐ terraincache 귀속 표(자/제품 · 근거 · 동작 게이트 결과). ⓑ site-memo 귀속 표(대조군 팔 실재 · 첫 빨강 날짜 · 카드). ⓒ e2e-verbs 순서 표. ⓓ 한 줄 답 + nightly-known 표.

## 1. 절대 규칙
귀속 먼저 · 고치는 것은 귀속된 자리 하나 · 새 수 0 · 손잡이 신설 0 · **T381(세션3 `zone.js` A*)·T378(세션2 `clearTreesInCells`)·T374(세션1 채집)·T385(세션7 순회) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/night-reds-0925` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 긴 실행은 `setsid nohup … & disown`)**.

## 2. 작업 (넷)
① terraincache ② site-memo ③ e2e-verbs 표 ④ 한 줄 답 + nightly-known 표.

## 3. 검증
하네스 넷 · 자명 통과 반례 · 접점 목록.

## 4. 회부만
`nightly-known.txt` 올리기(PM) · #54 40 풀기(재민 · T376 표) · 집터 메모 값 자체(T342 · 재민).

## 5. 완료
커밋 [승인 대기 · T383] · 가지 푸시 · `보고/T383_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 귀속 둘 한 줄씩**) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — terraincache 자/제품 · site-memo 자/제품 · n/0 · n/0 · nightly-known 올릴 것 n". 실기 0.
