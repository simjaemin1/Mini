# 구현 지시 — T372: 군락 종 넷의 실체 — 버섯밭·나물·벌집·야생포도(`mushroom`·`vegetable`·`honey`·`grape` — 수식이 내는데 세계에 없는 넷 · T347 §7-가 · T357 §2 표 열두 칸) · 스프라이트 넷(ART 렌더 파이프라인 정본 `scripts/*_render.py` · PNG+앵커 · 손편집 0) · 개체 표(HP · 전리품 → 그 품목 · 재생 주기 — **값은 출처에서**: 버섯·나물·산포도 계절성, 벌집 재생은 문헌 · 없으면 빈칸 회부) · `chunk.js groves kind` 가 받는 꼴로 **정의만**(세계에 심는 건 T359 청크 생성 뒤 · 기본 끔) · `render-meta.json.source.janggu_gung` 정정(T353 §0-ⓐ 증명) (세션8/ART · T353 뒤 · 2026-09-23)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T372.md` · 레포 `지시/지시_T372.md`)

---

T353(`batch/bgm-curves-0922` → main · **카드 전제 셋이 코드와 안 맞음을 실측으로 잡음**: 배포 13곡 중 `sigim()` 을 지나는 곡 0 · 샘플 뱅크가 레포에도 `~/Mini` 에도 없어 13곡을 못 굽는다 · `compose.py` 는 뱅크 없이 혼자 못 돈다 ⇒ `render-meta.source.janggu_gung: "합성"` 도 틀렸다 · 게이트를 "끔이면 새 코드가 import 조차 안 된다"로 바꿔 세움 · `phrase.py`(글라이드 = 어택 시간 한 줄 근거) · `tools/ddsp/` 스모크 초록 · 데이터 표 7행 · A/B 는 대금 단선율만) **통과** — "못 잰 것을 잰 척하지 않는다 · 전제가 틀리면 전제를 고친다"가 이 카드의 문장이다(족보 293). BGM 다음 걸음은 **재민 손**(#64: 샘플 뱅크 위치 · AI Hub 승인 · 4060 학습 한 줄)이라 세션8 은 그동안 **ART 본업**으로.

**카드 T372 — 넷. PM 판정**(재민 거부권): ① **개체 표 넷** — 종마다: `kind` 이름(`chunk.js groves` 문법) · HP(`RESOURCE_HP_TABLE` 의 덤불·풀 값에서 **같은 자리**를 고른다 — 새 수 0 · 어느 값을 빌렸는지) · 전리품(`lootOfResource` 표 꼴 · 품목은 그 넷 그대로 · 양은 덤불 `berry` 의 양과 같은 자리 — 빌린 값 표) · **재생 주기 `REGROW`**(T122 문법 `BUSH_Y=1`·`HERB_Y=0.5` 옆에 `MUSHROOM_Y`·… — 값은 **출처**: 버섯은 계절(가을 발생) · 나물은 봄 · 산포도는 연 1회 · 벌집은 문헌(야생 꿀 채취 주기) — 출처 없으면 **빈칸 + 회부**, 짓지 않는다) · 어느 지형에 서나(숲 가장자리·초지·물가 — `decideVillageType` 술어 이름). ② **스프라이트 넷** — ART 정본 파이프라인(`scripts/*_render.py` · `.py` 가 정본 · PNG+앵커 JSON · `.blend` 손편집 0 · T205 종별 문법) · 덤불·풀과 같은 화소 자(T308) · `test-assets-audit`·webp 자 통과 · 지금 덤불 스프라이트 옆에 넷 · 잠금 표. ③ **정의만** — `chunk.js` 의 종 표에 넷을 **정의**(kind·HP·전리품·재생) 하되 **생성 갈래에 넣지 않는다**(세계 무변 · 3시드·청크 해시 동일 · T359 청크 생성이 켜지는 날 그 밀도 유도에 넷이 든다 — 세션1 회부) · `render-meta.json.source.janggu_gung` 을 T292 문법으로 정정(증명은 T353 §0-ⓐ · 값 무변 · 메모 한 줄). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(청크 해시 · `test-trees` 91 · `test-forage-act` 86 · `test-nippon-boot` 39) · `test-assets-audit` 초록 · `test-audio ⑨l`(render-meta ↔ README 일치) · 3시드 면제(`sim/` 무접촉) · 접점 심볼(`groves|kind|RESOURCE_HP_TABLE|lootOfResource|REGROW|BUSH_Y|HERB_Y|decideVillageType|*_render.py|icons.lock|render-meta|janggu_gung`).

## 1. 절대 규칙
새 수 0(빌린 값·출처 값만 · 빈칸은 빈칸) · 생성 갈래 무접촉(정의만) · `.blend` 손편집 0 · `bgm/` 은 #64 뒤 · **T359(세션1 `chunk.js` 생성 갈래 — 리베이스 때 둘 다)·T354~T358(세션9 sfx) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/grove-kinds-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
재생 주기 값(출처 없는 칸 · 재민) · 생성 갈래에 넷 넣기(세션1 T359 뒤) · 샘플 뱅크·AI Hub·4060(#64 재민) · 13곡 재굽기(뱅크 뒤).

## 5. 완료
커밋 [승인 대기 · T372] · 가지 푸시 · `보고/T372_<날짜>.md`(**머리 15줄에 표 넷 요약**) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 종 4 · 빈칸 n · 스프라이트 4 · janggu 정정". 실기: 재민이 스프라이트 넷을 `산그림/` 에서 본다.
