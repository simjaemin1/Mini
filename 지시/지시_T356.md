# 구현 지시 — T356: 사람당 5.48µs 의 해부 — 게이트(T350) 아래 **갈래 여섯**(결정 · 걸음 · A* · 틱 본체 순회 · 델타 직렬화/방송 · 생활층)의 사람당 µs 를 갈래 끄기/고정으로 **각각** 잰다(T352: 평평한 걸음 산술은 0.42µs 뿐 — 나머지 92% 의 주인 이름) · 그 표 위에서 **SoA 1층**(T352 계약 초안 확정 · 걸음 갈래만 열 순회 · 셀당 비트 · 셀 경계 쿼드트리 · 바이트 동일 게이트 4,200틱) · 새 수 0 · 손잡이 기본 끔 (세션3 · T350 뒤 · 2026-09-23)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T356.md` · 레포 `지시/지시_T356.md`)

---

T350(`batch/dice-zero-0922` → main · **주사위 15 → 0** · 존 세계 자리 165 → 0(남은 15 는 세계 밖 · 린트가 정적으로 건다 — 착지본에선 절 번호 **⑫**(⑪은 T349 fetch 마감)) · **게이트가 섰다: 주민 408 × 7게임일 × 4,200틱 좌표 1,713,600 자리 비트 동일** · 씨가 순수 함수라 **순서 무관** — SoA·워커가 딛을 바닥 · `bandits.js` 아홉 자리가 `denRng` 를 안 쓰고 있었다 · 주입 자리 기본 가지로 떨어지던 둘 · 하네스 둘이 전역 `Math.random` 을 갈아 세계를 조종하고 있었다 · χ² 분포 무변) **통과** — "못 걸던 게이트가 섰다"와 "진짜 값은 순서 무관"이 이 카드의 두 문장이다(족보 281). ★착지 때 PM 이 만진 것: `zone.js` 덤불 씨앗 줄은 T347 의 **자리 해시**(`_gidHash` · 덤불의 성질)를 남겼다(T350 의 `_dt()` 틱 흐름 대신 — 같은 덤불은 몇 번 물어도 같은 답이어야 한다).

★**T352(세션7 · 같은 날)가 낸 수를 먼저 읽어라**: 지형 비트·벽 격자·나무 CSR 위에 **평평하게 다시 쓴 걸음 문**은 JS 0.42µs · C 0.22µs(10,000명) — 그런데 T345 실서버는 5.48µs 다. ⚠단 T352 의 ⓐ 는 제품 `npcStep` 이 아니라 벤치 안의 사본이라 "걸음이 7.6%" 는 **평평한 걸음**의 몫이지 **지금 제품 걸음**(메모 Map · 쿼드트리 · 문자열 키 Set)의 몫이 아니다 — T345 프로파일로는 걸음 계열이 바쁜 시간의 **약 1/3**, 틱 본체 순회 **약 1/5**, A* **약 1/10** 이다. 어느 쪽이든 결론은 같다: **10배는 걸음 산술에 없고, 갈래별로 재지 않으면 다음 카드가 엉뚱한 자리를 판다.**

**카드 T356 — 둘 · 순서가 카드다(해부 → SoA). PM 판정**(재민 거부권): ① **해부 표** — 실지도 51마을 1,464명 존에서 **같은 씨·같은 국면**(T350 게이트 · 밤 창 — T345 §3-ⓑ 가 낮은 ±8% 라 했다) 으로 갈래를 하나씩 **끄거나 고정**해 사람당 µs 를 뺀다: ⓐ 결정(`decideNpcBehavior` — 목표열을 미리 녹음해 재생 · T352 문법) ⓑ 걸음(`npcStep`+술어 넷 — 목표에 순간 도달로 고정 · 계측 팔에서만 · 제품 아님) ⓒ A*(`_searchStep`·`gGet` — 막힘 0 인 지형 팔) ⓓ 틱 본체 순회(`zone.js:11255` — 주민 수 N 을 0·½·1 로 두고 C+N×s 의 s) ⓔ 델타 직렬화/방송(`makeEntry` · 관측자 0 팔 대 관측자 1 팔) ⓕ 생활층(`npcLifeTick` — 낮 팔에서만 · 밤은 0) — 각 갈래 **사람당 µs 와 합이 5.48 에 닿는지**(닿지 않는 나머지 = "못 가른 것" 한 줄 · 지어내지 않는다) · 표 한 장 · 계측기 `scripts/t356-tick-anatomy.js`(러너 밖 · 손잡이는 **계측기 안에서만** · 제품 손잡이 0). ② **SoA 1층** — ①이 걸음 계열을 주인으로 확인한 경우에만(아니면 ②는 표만 내고 회부): T352 `설계/설계_주민_SoA_계약.md` 초안을 **확정**(`active` 열 — `activeChunkKeys` 문자열 Set 에서 비트 유도(T333 규율) · `_stuckPos` 열 넷) · 걸음 갈래만 열 순회(`Float64Array` 좌표 · `Uint8Array` 층·활성) · 지형 = 셀당 비트(T333 상자 그대로) · 벽 = 셀당 1바이트(bit0 N · bit1 E) · 나무 = 셀 경계 넘을 때만 질의(T345 §4) · 손잡이 `T356_SOA` 기본 끔 · **게이트: 켬/끔 두 판 4,200틱 좌표 비트 동일**(T350 그 자 · `test-move-soa` 절 추가) · 값: T352 ⓑ 0.37µs 를 제품 안에서 얼마나 닿나(밤 창 · 등 맞댄 워크트리 짝 · T345 §3-ⓒ 문법). ③ 한 줄 답. 나흘. 질문 금지 — 답은 표.

★검증 규약: `sim/` 무접촉(3시드 면제 · 만지면 면제 없음) · 끔 = 실서버 비트 동일 · `test-move-soa` 45 + 게이트 절 · `test-fish-act` 73 · `test-wood-act` 87 · `test-forage-act` 86 · `test-war-world` 56 · `test-harness-lint` 57 · `e2e-move` 37 · `lab-wiring-check` 실패 0 · 접점 심볼(`decideNpcBehavior|npcStep|_searchStep|gGet|makeEntry|npcLifeTick|activeChunkKeys|terrain-tilecache|Float64Array|T356_SOA|test-move-soa|t356-tick-anatomy|설계_주민_SoA_계약`).

## 0. §0 실측 (셋)
ⓐ 해부 표(갈래 여섯 · 사람당 µs · 합 대 5.48 · 못 가른 나머지). ⓑ SoA 게이트 두 판 + 밤 창 짝 값(또는 표만+회부). ⓒ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 제품 손잡이는 `T356_SOA` 하나(기본 끔) · 계측 팔의 "고정"은 제품에 들어가지 않는다 · LOD 0 · 낮은 Hz 0 · 워커 0 · 결정론 유지(`Math.random` 0 · 린트 ⑫) · **T347(세션1 · `villages.js` 채집 절 · `zone.js` 채집 실행부)·T329(세션10)·T354(세션9 클라) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/tick-anatomy-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 긴 창은 `setsid nohup … & disown`)**.

## 2. 작업 (셋)
① 해부 ② SoA 1층(조건부) ③ 한 줄 답.

## 3. 검증
끔 비트 동일 · 게이트 · 하네스 여덟 · 접점 목록.

## 4. 회부만
C 층 접속(T352 커널 · WASM 권고 · #53 재민) · 틱 본체가 주인이면 그 카드(순회 → 열 · PM) · A* 가 주인이면 그 카드(경로 캐시 · PM) · `NPC_CAP_PER_VILLAGE = 40`(#54) · 두 프로세스 게이트(시계 손잡이 · T350 §3-ⓒ · 재민).

## 5. 완료
커밋 [승인 대기 · T356] · 가지 푸시 · `보고/T356_<날짜>.md`(ⓐ~ⓒ · **머리 15줄에 해부 표 한 줄 요약**) · `인계/M-이동입력.md` 갱신 · 채팅 "가지 올렸다 <해시> — 해부 ⓐ…ⓕ µs(합 …/5.48) · SoA 게이트 n/n · 밤 … → … µs". 실기 0.
