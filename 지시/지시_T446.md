# 구현 지시 — T446: 배포에 실리는 것 전수 — T422 ⑩ 이 세운 자(두 Dockerfile `COPY server public sim` · `.dockerignore`)를 표로 벌린다: 이미지에 실리는 파일 전수(수 · MB · 폴더별) × (게임이 참조하나 — `public/**` 는 HTML/JS 가 부르나 · `server/**` 는 `require` 되나 · `sim/**` 는 번들이 읽나) · 안 참조되는데 실리는 것(이름 · MB · 보관소 후보인지 · 지우기 후보는 **표만**) · 소리 `sfx/**` 63키 파일 대 매니페스트(고아 파일 · 매니페스트에 있는데 없는 파일) · BGM 13곡 외 오디오 · `_sfx_in/` 원본이 레포 밖에 있음 확인 · 판정 0 (세션8/ART · T437 뒤 · 2026-09-27)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T446.md` · 레포 `지시/지시_T446.md`)

---

T437(`batch/bgm-ab70-0927` → main · #70 청취본 넷(끔 = 배포 바이트 동일 · 켬 83음/곡) · 브금판 #70 두 줄 · `48-a-audio.js` 머리말 한 줄 · DDSP-Gugak 주인 `ieay4a/DDSP-Gugak`·`sweetcocoa/ddsp-pytorch` · sha 7/7 · MIT · 가중치 라이선스 미상 · 13곡 26/26) **통과** — "청취본은 배포가 아니라 귀의 입력이다"가 이 카드의 문장이다(족보 363). 브금판 세 판이 다 섰으니 재민 귀를 기다린다 — 그 사이 ART 는 **배포 자산 전수**.

**카드 T446 — 셋 · 표만 · 세계 무변 · 코드 0.** ① **실리는 것 전수** — Dockerfile 둘의 `COPY` 와 `.dockerignore` 를 `test-assets-audit ⑩` 그 자로 읽어 이미지에 드는 파일 목록(수 · MB · 폴더 깊이 2 까지 표) · `public/**` 참조 그래프(HTML `<script>`·`<link>`·`fetch`·`new Audio`·매니페스트 → 파일 · 정적 문자열만 · 동적 경로는 "동적" 표시) · `server/**` `require` 그래프(진입 `central`·`zone` 에서 닿는 파일) · `sim/**` 번들 입력 · **닿지 않는데 실리는 것** 목록(이름 · MB · 마지막 커밋 · 어느 카드가 만들었나 · 보관소 후보/지우기 후보/모름 — 판정 0). ② **소리 자산 대조** — `sfx/manifest.json` 63키 × 파일(있음/없음 · 크기 · 형식) · 폴더에 있는데 매니페스트에 없는 파일(고아) · 13곡 외 오디오 파일 · `_sfx_in/` 이 레포 밖인지(`.gitignore` 줄 · `git ls-files` 0) · CREDITS 와 매니페스트 출처 칸 대조(빠진 출처 n · #14ⓒ 입력). ③ **한 줄 답** + 이미지 MB 합 · 안 닿는 MB 합 · 고아 n · 출처 빠짐 n. 이틀.

★규약: 코드 0 · 파일 이동 0(표만 · 이동은 다음 카드) · 배포 무변 · 판정 0 · **T445(세션9 `sfx/`)·T434(세션12) 무접촉** · `test-assets-audit` PASS · `test-audio` 180 · `test-client-globals` 26 · `test-harness-lint` 61 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/deploy-assets-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 0. §0 (셋)
ⓐ 실리는 것 전수 + 안 닿는 것. ⓑ 소리 대조 + 고아 + 출처. ⓒ 한 줄 답.

## 1. 회부만
지우기/보관소 이동(PM · 표 뒤) · #14ⓒ(재민) · #60·#66·#70·#72(재민 · 귀).

## 2. 완료
커밋 [승인 대기 · T446] · 가지 푸시 · `보고/T446_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 이미지 … MB · 안 닿는 n(… MB) · 고아 n · 출처 빠짐 n". 실기 0.
