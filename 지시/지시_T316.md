# 구현 지시 — T316: 어부 둘째 — 관측자 없는 마을도 걷는다(캐논 ⓑ 빚) · 세계 위 자(켠 팔 첫날 등가 3시드 · 16마을 30일 뒤 어부 수 · 존 틱 p50/p95 · 걷는 어부 수 · 틱 빚) · `_carry` 흡수(수확·지게를 `inventory.grain` 으로 · 밭 등가 자 옆에 두고) (세션3 · T312 뒤 · 2026-09-19)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T316.md` · 레포 `지시/지시_T316.md`)

---

T312(`batch/fish-act-0919` → main · **새 체계 0 — NPC 는 태어날 때부터 `inventory` 를 가졌다**(`zone.js:1705`) · 낙하는 플레이어 정본 그대로 · 셀 예산 = 수식 ÷ 강가 셀 수(식 · 값 0) · `freshfish.js` 종 정본 · 끔 3시드 넷째 판 동일 · **못 한 둘을 머리에 적었다**: 관측자 없는 마을이 안 걷는다 · 세계 위 부하·켠 팔 등가를 `t17-metrics` 로는 못 잰다) **통과** — 카드를 실측이 줄인 것(인벤토리 이미 있음)과 못 한 것을 먼저 적은 것 둘 다 답이다(족보 243). ⚠시드 42 게시 2,356→2,361 은 네 줄이 아니다 — T302 착지분 · PM 이 기준선 표를 고쳤다. 종 가중은 재민 칸(#46).

**카드 T316 — 셋. PM 판정**(재민 거부권): ① **관측자 없는 마을이 걷는다** — 존 NPC 루프가 관측자 없는 마을에도 돌게(`_lifeHeadlessDay` 의 어부 칸을 지우고 같은 걸음·같은 예산·같은 곳간 함수) · 손잡이 `T312_FISH_ACT` 안에서(둘째 손잡이 만들지 않는다) · 켬 = 관측자 유무와 무관하게 같은 씨 같은 어획(`test-fish-act` ⓐ 가 이미 재는 그 자 — 관측자 끔 팔을 진짜 "몸이 걷는" 팔로) · 비용은 ③이 잰다. ② **`_carry` 흡수** — 수확 `:4439`·곳간 `:5291`·지게 `:5792` 를 `inventory.grain`(kg 상한 = `carry.js` 같은 함수)으로 · 행동 무변(수확 3칸 = 곡물 n 의 kg 상한에서 유도 · 새 수 0) · **밭 등가 자(T297 §1 · 소수 다섯 자리)를 옆에 두고** 흡수 전/후 같은 씨 30일 `food` 동일(반례: 상한을 바꾸면 달라진다). ③ **세계 위 자(T284 문법 · 존 서버 위 · 실지도 51마을 · 관측자 하나)** — 끔/켬 두 팔: 존 틱 p50/p95 · 걷는 어부 수 · 틱 빚 · 켠 팔 **첫날 어획 합 = 수식**(마을 51 · 3시드 · 폭은 T297 하루 잡음) · `land.water < 0.833` 16마을 **30일 뒤 어부 수**(그림자가격이 옮기나) · **관측자 없는 마을 전부가 걷는 값**(T297 브라우저 외삽 10~32분/일의 서버 답). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 끔 3시드 비트 동일(게시 42 = 2,361 이 기준) · 게이트 셋 · `test-fish-act` ⓐ~ⓔ + ①②의 새 절 · `test-econ-fieldyield` · `test-farm-life` · `test-psite-server` · 접점 심볼(`T312_FISH_ACT|_lifeHeadlessDay|_carry|inventory|carry.js|harvestToGranary|fishToGranary|_fishOutLast|freshfish|_jobSites`).

## 1. 절대 규칙
새 수 0 · 사본 0 · 배수·스위치 0 · 손잡이 기본 끔 · 켜지 않는다 · **T315(세션4 `_lifeAddHouseSite`·`_mapBeds`)·T295(세션10 전쟁 절)·T311(세션1 랩)** 무접촉 · ★푸시 2판(가지 `batch/fish-act2-0919` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
켜기·다섯째 판(재민) · 종 가중(#46 재민) · 나무꾼(다음 · 같은 문법) · 광부(#43·#47 재민).

## 5. 완료
커밋 [승인 대기 · T316] · 가지 푸시 · `보고/T316_<날짜>.md`(**머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> — 걷는 마을 51/51 · 등가 … · p50 …/… · 어부 16마을 …". 실기 0.
