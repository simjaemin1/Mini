# 구현 지시 — T346: `e2e-onboarding` 65/6 — `_mapBeds` 켬(T326) 뒤 새 플레이어 마을 빈터가 "중심에서 너무 멀다" — 제품인가 대본인가 · 제품이면 빈터 탐색을 정본 영토·집 간격 15 에서(사본 0) · `NPC_CAP_PER_VILLAGE = 40` 몸 수 상한 표(#54 · 언제 풀 수 있나) (세션4 · T342 뒤 · 2026-09-21)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T346.md` · 레포 `지시/지시_T346.md`)

---

T342(`batch/site-cache-0921` → main · 거부 캐시 → 집 493 · 인구 2,725 · 정지 1 · `test-shelter` 의 18 은 하네스 사본 · **1,718 은 인구가 아니라 몸 수(`NPC_CAP_PER_VILLAGE = 40`)**) **통과**(족보 272). #54 는 "주거 게이트" 가 아니라 **몸 수 상한** 이었다 — 재민 칸 갱신.

**카드 T346 — 둘.** ① T344 이분: `e2e-onboarding` 이 `T315_MAPBEDS=0` 에서 75/0 · 켬에서 65/6 — 실패 6 절이 무엇을 기대하나(빈터 거리 · 어느 함수가 빈터를 주나 · 켬 세계에선 집이 55채 늘어 빈터가 멀어진 게 맞나) · **새 플레이어가 실제로 집을 못 받나**(제품) · 못 받으면 빈터 탐색을 **정본**(`_lifeSiteFilters` · `LIFE_HOUSE_GAP`) 으로 · 대본 문제면 대본 갱신 · `_mapBeds` 켬은 **유지**(PM · ⓑ) · 75/0 · ② `NPC_CAP_PER_VILLAGE = 40` — 어디서 누가 왜(주석·커밋) · 캐논(관측자 없어도 실걸음 = econ 인구 전부가 몸을 가진다) 대비 지금 몸 수/econ 인구 비 · 40 을 풀면 무엇이 늘고 무엇이 깨지나(T333 사람당 µs 로 셈) — **표만 · 안 푼다**(T345 재작성 뒤). ③ 한 줄 답. 하루. 질문 금지.

★검증 규약: `e2e-onboarding` 75/0 · `test-psite-server` · `test-shelter` 47 · 3시드 면제 · 접점 심볼(`onboarding|_lifeSiteFilters|LIFE_HOUSE_GAP|_mapBeds|NPC_CAP_PER_VILLAGE|npcPids`).

## 1. 절대 규칙
새 수 0 · 사본 0 · **T345(세션3 이동 루프)·T347(세션1)** 무접촉 · ★푸시 2판(가지 `batch/onboarding-site-0921` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T346] · `보고/T346_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 제품/대본 … · 75/0 · 40 표".
