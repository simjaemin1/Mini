# 구현 지시 — T426: 개간이 다시 벤다 — 참으로 만든다(★PM · #74: T398 §1-1 이 "지금은 거짓"이라 잡았다 — 색인 갈래가 장부 씨를 건너뛰고(`harvestedSeeds.has → continue`) 성목만 본다 ⇒ 영토 안 그루터기가 243일에 전부 성목으로 돌아와도 개간이 한 그루도 안 벤다) · 길 ⓐ: 장부 씨라도 **지금 서 있으면**(성목·묘목 둘 다 — 묘목도 벤다: 영토는 개간이다 · ★PM #74) 다시 적는다(장부 날 갱신) + `INSERT OR IGNORE` 를 갱신으로(T378 §1-4 · 메모리 새 날 ↔ DB 옛 날 어긋남) · 재부팅 게이트(다시 벤 그루가 옛 단계로 돌아오지 않는다) · T398 자(`t398-regrow-recut.js`) 40일 판에서 다시 벤 그루 0 → n · 그리고 `e2e-nature` 나무ⓗ 가 `7b7d4b90` 에서 새로 빨갛다(청크 300~305 나무 0 · T408/T418 사이) → 귀속 (세션2 · T398 뒤 · 2026-09-26)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T426.md` · 레포 `지시/지시_T426.md`)

---

T398(`batch/wood-ring-0926` → main · 고리로 볼 나무 8 → 10,528 · 0그루 마을 0/50 · 걸음 358 → 1,124px · 등가 152%(하루 100초 — 6초 날은 왕복이 안 선다) · **개간은 다시 자란 나무를 안 벤다**(#74 발견) · 고리 비용 ×5.8 · idle 스킵이 청크를 안 끈다(→ T410 추신) · 3시드 넷째 판-b 동일 · `test-wood-act` 106/0) **통과** — "고리로 옮기니 숲은 섰는데 개간은 다시 자란 나무를 안 벤다"가 이 카드의 문장이다(족보 341). ★PM 이 T398 카드에 "개간이 다시 벤다"고 적은 것은 코드를 안 보고 적은 거짓이었다 — 세션2가 잡았다. #74 판정(★PM · 재민 거부권): **묘목도 벤다** — 영토는 마을이 개간한 땅이고 거기 서는 나무는 종이 무엇이든 단계가 무엇이든 마을이 다시 벤다(그게 실체 · 술어 0).

**카드 T426 — 넷. PM 판정**(재민 거부권 #74): ① **다시 적는다** — `clearTreesInCells` 색인 갈래: 장부에 있는 씨라도 **지금 그 셀에 서 있으면**(색인이 낸 단계 ≥ 묘목 · `_T378_TREE = { tree: 1 }` 를 묘목까지 — 단계 이름은 T122 정본에서) 벤다 · 장부는 **날을 갱신**(같은 씨 두 번 적는 게 아니라 "마지막으로 벤 날"이 정본 — 그게 T122 재생 시계의 입력) · `_markHarvested` 그 문 하나(사본 0) · `INSERT OR IGNORE` → 갱신(`ON CONFLICT … UPDATE day` 꼴 · 있는 DB 문법) · 부팅 갈래(T378 −1 날)도 같은 문. ② **재부팅 게이트** — T398 자 40일 + 시계 당긴 팔(첫 묘목 619일 · 그 뒤 검문): 다시 벤 그루 0 → n · 장부 날 바뀐 행 n · **재부팅 뒤 그 그루가 옛 단계로 돌아오지 않는다**(DB 날 = 메모리 날 · T378 §1-4 의 그 병) · 영토 밖 씨 단위 무변(T378 ⓓ 자) · 관측자 0 팔 = 관측자 1 팔. ③ **`e2e-nature` 나무ⓗ** — `7b7d4b90` 에서 청크 300~305 에 나무 0(`057d40c1` 초록) · T408(경계 참조 — 그 청크가 경계 띠인가) / T418(문서 · 아닐 것) 중 귀속 · T408 이면 그 청크의 나무가 **이웃 존 것으로 걸러진 것**인지(맞는 동작 · 자를 옮긴다) 아니면 결함인지 · 고침은 자 또는 그 자리 하나. ④ 한 줄 답 + 고리 비용 표(T398 §1-4 ⓐⓑ 두 길 · 표만 · 고치지 않는다). 이틀. 질문 금지 — 답은 표.

★검증 규약: 영토 밖 무변(씨 단위) · 3시드 넷째 판-b(zone 절 · 면제 근거 한 줄 · `villages.js` 만지면 돌린다) · `test-wood-act` 106 · `test-trees` 91 · `test-regrow` 100 · `test-ghost-tree` 10 · `test-index-named` 7 · `test-terr-persist` 8 · `test-resource-index` 20 · `e2e-nature` 145(ⓗ 초록) · `t378-forest-in-village` 자 · `test-harness-lint` 61 · 접점 심볼(`clearTreesInCells|_markHarvested|harvestedSeeds|insertHarvestedSeed|INSERT OR IGNORE|_T378_TREE|regrow|t398-regrow-recut|e2e-nature|_seamRefs`).

## 1. 절대 규칙
술어 0(영토 안은 다 벤다) · 새 수 0 · 손잡이 0(정본 규칙) · **T427(세션3)·T410(세션1 idle)·T419(세션4) 무접촉** — `zone.js` 자원 절만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/recut-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#71 숲 값(재민) · T325 켜기(재민 · T398 ② 표 · 수요 문 같이) · 고리 비용 캐시(PM · ④ 표 뒤) · 관측 마을 주민 옛 채집 400px(PM · T398 §1-6).

## 5. 완료
커밋 [승인 대기 · T426] · 가지 푸시 · `보고/T426_<날짜>.md`(템플릿 네 칸) · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 다시 벤 그루 0 → n · 재부팅 동일 · e2e-nature ⓗ = … · 고리 비용 표". 실기 0.
