# 구현 지시 — T411: BGM 정리 셋 — ① `e2e-bgm-phrase` ⑦⑧ 물려받은 빨강 10/2(T386 §회부 7 · 허용치가 이 상자의 OAC 떨림보다 좁다 — "압축기/잔향 탓" 귀속은 안 선다) → 자를 떨림 바닥에서 유도(T391 이 잰 −91.2dB 그 바닥 · 새 수 0) ② `janggu_gung: '합성'` 칸 정정 — T391 이 궁편 실음원 6조각 · 배포 2곡이 샘플 84타로 굽힌 것을 **바이트 동일로 증명**했다 ⇒ 표는 사실을 적는다(`'샘플'` · 값이 아니라 사실 정정 · 11곡은 "미상") ③ GPT `tools/` 24 폴더 — T386 표의 "쓰는 것/안 쓰는 것/못 돌린 것" 을 **실행으로** 확인(각 폴더 진입점 한 번 · 돌면 결과 한 줄 · 안 돌면 이유) · 버림 권고 폴더는 `tools/_rnd_archive/` 로 이동 제안 **표만**(이동은 PM) (세션8/ART · T401 뒤 · 2026-09-26)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T411.md` · 레포 `지시/지시_T411.md`)

---

T401(`batch/bgm-legacy-0926` → main · 07-31 코드로 같은 곡 0/11(44판) · 반주 9/11 같은 파형 · 선율 11/11 다름 — 씨앗 문제가 아니라 선율 연주 코드가 없다 · 지금 파이프라인 악보 11/11 있으나 11/11 다른 곡 · 브금판 #72 11줄 세 단추 · 청취본 44장 sha256 44/44 · 권고 "그대로 둔다" 11/11) **통과** — "07-31 코드로 다시 구워도 같은 곡은 0/11 — 반주는 같고 선율이 다르다"가 이 카드의 문장이다(족보 327). #72 는 재민 브금판. 그 사이 BGM 의 남은 빚 셋을 닫는다.

**카드 T411 — 셋. PM 판정**(재민 거부권): ① **`e2e-bgm-phrase` ⑦⑧** — 무엇을 재나(같은 판 두 번 구운 차? 전후 차?) · 허용치가 어디서 온 수인가(GPT 가 적은 수면 "지어낸 수") · T391 §ⓐ 결정론 표(numpy 2.3 → 패킷 100% · Chromium OAC 같은 판 두 번 −91.2dB)에서 **바닥을 자로**: 같은 판 두 번의 차를 먼저 재고 그 바닥 × 있는 계수 0(= 바닥 자체)을 문턱으로 · 미끼(다른 씨 · 다른 판)는 문다 · 10/2 → 12/0 · 자명 통과 반례. ② **`janggu_gung` 정정** — 정본 표(T353 `설계/` BGM 문서 · `render-meta.json` 그 칸)의 `'합성'` 을 T391 증명대로 **`'샘플(궁편 6조각 · 84타)'`** 로 — 그 2곡만 · 11곡은 `'미상(07-30 판 · 코드 없음)'` · 값 0 · 사실 정정 · T372 가 "추론이 안 서서 안 고쳤다"(족보 297)는 그 자리 — 이제 실측이 섰다. ③ **GPT tools 24 실행 확인** — T386 §ⓒ 표의 24 폴더마다: 진입점(README·main·스크립트) · 한 번 실행(입력은 레포 안 것만 · 네트워크 0 · 체크포인트 없는 것은 "체크포인트 없음") · 결과 한 줄(돌았다 / 무엇이 없어 안 돌았다 / 5분 넘어 끊음) · 권고 칸 세 값(쓴다 · 참고 · 버림) 갱신 · 버림 목록을 `tools/_rnd_archive/` 로 옮기는 **제안 표**(이동은 PM · 이 카드 diff 0). 이틀. 질문 금지 — 답은 표.

★검증 규약: `bgm.js` 무변 · 배포 13곡 무변 · `tools/` 무변(③ 표만) · `sfx/**` 무접촉(세션9) · `e2e-bgm-phrase` 12/0 · `test-audio` 160 · `test-bgm-phrase` 16 · `test-assets-audit` PASS · `test-render-score-articulation` 14 · `test-client-globals` 26 · `test-harness-lint` 61 · 접점 심볼(`e2e-bgm-phrase|janggu_gung|render-meta|설계_BGM|tools/|BGM_RND_|t386-bgm-hash|t401-bgm-legacy`).

## 0. §0 실측 (셋)
ⓐ ⑦⑧ 자 유도 + 12/0 + 반례. ⓑ 정정 diff(2곡 샘플 · 11곡 미상). ⓒ 24 폴더 실행 표 + 이동 제안.

## 1. 절대 규칙
값 0(사실 정정만) · 새 소리 0 · `tools/` diff 0 · 네트워크 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/bgm-tidy-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#72 11곡(재민 · 브금판) · #66·#70(재민) · `tools/` 이동(PM · ③ 표 뒤) · GPT 다음 BGM 카드(PM) · AI Hub·4060(#64 ②③).

## 5. 완료
커밋 [승인 대기 · T411] · 가지 푸시 · `보고/T411_<날짜>.md`(템플릿 네 칸) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — ⑦⑧ 12/0 · janggu 정정 2곡 · tools 24 = 돈다 n · 안 돈다 n · 버림 n". 실기 0.
