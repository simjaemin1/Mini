# 구현 지시 — T298: 영토 + 방아쇠를 켠다 — `T230_TERR_HOUSING`·`T219_HOUSE_TRIGGER` 서버 기본 켬(정본 읽는 자리 하나 · 되돌림 `=0` 비트 동일) · 3시드 800일 다섯째 판 · `_mapBeds` 살리기 준비 표 (세션4 · T287 뒤 · 2026-09-18)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T298.md` · 레포 `지시/지시_T298.md`)

---

T287(main `d6c0203f` · 나루터 폴백 — 실패한 건 `okAt` 이 아니라 반경 6 · C 땅 27곳) 통과 · T278(영토 영속 · 한 자리 · 재부팅 50/50) **main 착지**(PM 09-18 · T244 넷째 판과 같이 — 공통 기준선 절 갱신됨). **재민 #22 "켜".** 먼저 `git fetch && git log --oneline -5 origin/main` — 베이스는 T244·T278 이 앉은 main 이다.

**카드 T298 — 켠다. T244 문법 그대로(정본 읽는 자리 하나에서 기본 켬 · `villages.js` 에 안 심는다 — 자·랩이 끈 세계를 재는 함정 T221).** **PM 판정**(재민 거부권): ① `T230_TERR_HOUSING`·`T219_HOUSE_TRIGGER` 를 **기본 켬**으로 — 손잡이를 읽는 자리(T230/T219 가 심은 곳 · 표에 자리 셋 이상이면 하나로 모은다)에서 `=== '0'` 이면 끔 · 미설정 = 켬 · 되돌림 `=0` 둘이면 **넷째 판과 비트 동일**(3시드 열 열 전부 `cmp`). ② **다섯째 판** 3시드 800일 51마을 `t17-metrics`(여덟 수 + `vpop`) → 넷째 판(8,318/7,935/7,853) 대비 짝 Δ 를 T252 자로 · 소멸 · 침상/인구 · 영토 밖 집 0 · **T267 의 83%·15.7/25 가 넷째 판 위에서도 서나**. ③ 하네스: 기존 `test-terr-persist` · T219/T230 하네스가 "켠 판 = 끈 판과의 관계"로 서게(T244 ⑧ 문법 · 상수 베끼기 0) · `lab-wiring-check` [H] 등록. ④ `_mapBeds` 살리기 **표만**(어디서 죽었나 · 살리면 무엇이 바뀌나 · 다음 카드). ⑤ 답 한 줄. 하루. 질문 금지 — 답은 표.

★검증 규약: 되돌림 `=0` 비트 동일(넷째 판 열 열) · 3시드 켬 표 · `build-econ-bundle --check` · `inline-engine --check` · `lab-wiring-check` · `test-farm-life` · `test-terr-persist` · `test-psite-server` · **T295(세션10 `villages.js` 전쟁 절 · 리베이스 때 둘 다 살린다) · T288(세션5 `economy-sim-v2`) 무접촉** · 접점 심볼(`T230_TERR_HOUSING|T219_HOUSE_TRIGGER|_terrGrow|_terrSet|_mapBeds|terr|village_buildings|test-terr-persist`).

## 0. §0 실측 (다섯)
ⓐ 손잡이 자리 표 + 되돌림 비트 동일. ⓑ 다섯째 판 3시드 표(넷째 판 대비 · T252 자). ⓒ 하네스. ⓓ `_mapBeds` 표. ⓔ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 켬은 기본값 뒤집기뿐(수식 0) · 배포 재민만 · ★푸시 2판(**맥 VM `device_bash`** · `$HOME/pushrepo` · 가지 `batch/terr-on-0918` **가지만** · main 은 PM(세계를 움직이는 카드) · `~/mnt/Mini/durango-mini` 무접촉 · 403 이면 `~/Mini/_inbox/` 번들) · force 0.

## 2. 작업 (다섯)
① 켬 ② 다섯째 판 ③ 하네스 ④ `_mapBeds` 표 ⑤ 한 줄 답.

## 3. 검증
비트 동일 · 3시드 · 게이트 셋 · 하네스 · 접점 목록.

## 4. 회부만
`_mapBeds` 살리기(다음 카드) · 마을별 장·간격 18(#22 ⓐ · 안 급함) · 첫 의뢰 #18.

## 5. 완료
커밋 [승인 대기 · T298] · 가지 푸시 · `보고/T298_<날짜>.md`(머리 15줄) · 채팅 "가지 올렸다 <해시> — 다섯째 판 …/…/… · 소멸 … · 침상 …% · 되돌림 동일". 실기 0.
