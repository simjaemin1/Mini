# 구현 지시 — T350: 주사위 0 — `decideNpcBehavior` 의 `Math.random` 15회(걸음마다 · T340 계수)를 씨 해시(pid·셀·게임일·틱 — T340 어부 시도가 이미 쓴 그 문법)로 · 존 전수 `Math.random` 감사(세계를 움직이는 자리 전부 · 연출·소리는 제외 표) · 그러면 "같은 씨 같은 좌표" 게이트가 선다 — 그 게이트를 `test-move-soa` 에 건다(7게임일 · 주민 전수 · 틱마다 바이트 동일) (세션3 · T345 뒤 · 2026-09-22)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T350.md` · 레포 `지시/지시_T350.md`)

---

T345(`batch/move-soa-0921` → main · 재기 전에 안 고쳤다 → 걸음당 쓰레기 27개 중 넷 제거 · GC −27% · 사람당 5.48µs · **"10배는 할당에 없었다 — 주인은 호출 수와 포인터 순회"** · **카드의 게이트가 성립하지 않는다: 세계가 실행 간 결정론이 아니다** — `decideNpcBehavior` 가 걸음마다 `Math.random` 15번 · 술어 단위 전수 대조로 바꾼 것 · 낮은 못 쟀다고 적은 것) **통과** — 카드가 시킨 SoA 를 안 하고 "먼저 세니 자리가 바뀌었다" 고 적은 것이 맞다(족보 276). **주사위 금지는 이 게임의 첫 캐논이다**(07-12 · 재민) — 걸음마다 15번 굴리는 자리가 살아 있었다는 게 이 카드의 진짜 발견이다.

**카드 T350 — 둘 · 재작성의 1번.** ① **`decideNpcBehavior` 주사위 15 → 0** — 씨 = `hash(pid, cx, cy, gameDay, tick)`(T340 `_t340Try` 가 쓴 그 해시 함수 · 사본 0 · 새 수 0) · 결정 분포는 그대로(같은 확률 · 굴림만 결정적 — 자: 10만 굴림 히스토그램이 `Math.random` 판과 T252 자로 못 가름) · ② **존 전수 감사** — `server/**` 의 `Math.random` 전부: 세계를 움직이는 자리(위치·결정·산출·전투)와 안 움직이는 자리(연출·소리·로그 표본)를 표로 · 움직이는 자리는 이 카드가 전부 씨 해시로(있으면 · 수 표) · 안 움직이는 자리는 표만 · `test-harness-lint` 에 절 하나: `server/` 세계 자리 `Math.random` 0(정적 · 예외 목록은 표에서 유도) · ③ **게이트** — `test-move-soa` 에 "같은 씨 · 51마을 7게임일 · 주민 전수 좌표 틱마다 바이트 동일" 절(두 판 대조 · 자명 통과 금지: 씨를 바꾸면 다르다) · ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 3시드 면제(`sim/` 무접촉이어야 한다) · `test-move-soa` 24 + 게이트 · `test-fish-act` 73 · `test-wood-act` 87 · `test-war-world` 54(전투 주사위는 T284 가 이미 씨 해시 — 확인) · `e2e-rumor`·`e2e-events` 무변 · 접점 심볼(`decideNpcBehavior|Math.random|_t340Try|hash|seedRand|test-move-soa`).

## 1. 절대 규칙
새 수 0 · 분포 무변(굴림만 결정적) · 손잡이 0 · **T347(세션1 채집 — 같은 결정 함수를 부를 수 있다 · 리베이스 때 둘 다)·T329(세션10)** 접점 표 · ★푸시 2판(가지 `batch/dice-zero-0922` · main 은 PM · 403 → `~/Mini/_inbox/`) · force 0.

## 4. 회부만
SoA + 정수 격자 + 셀 경계 쿼드트리(다음 카드 · 이 게이트 위에서) · `activeChunkKeys` 정수 키(같이) · 미리 굽기 상자 밖(#53) · 2층 WASM(재민).

## 5. 완료
커밋 [승인 대기 · T350] · `보고/T350_<날짜>.md`(**머리 15줄**) · 채팅 "가지 올렸다 <해시> — 주사위 15→0 · 존 세계 자리 n→0 · 게이트 두 판 동일".
