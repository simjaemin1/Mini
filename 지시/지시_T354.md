# 구현 지시 — T354: 소리판 첫 귀 판정(재민 09-22 · 16키 중 ✗ 6) — ✗ 여섯 **재확보**(`bronze_hit`·`deer_call`·`eat`·`water`·`wind`·`wolf_howl`) · ○인데 자리 틀린 둘(`step_dirt`↔`step_grass` 지면 바로잡기) · 가벼운 비 한 판 · `axe`·`tiger_growl`·`bite` 변주(한 키 여러 파일 — 층 한 줄) · 미확보 둘(`harvest`·`boar_grunt`) · **그리고 "빠진 소리" 전수 표**(서버 방송·`act` 낱말·클라 훅을 전부 걸어 소리 있음/없음 — 지어내지 않는다 · 확보는 다음 카드) (세션9/SOUND · 이 카드에 한해 `public/assets/sfx/**` 위임 · `bgm/` 은 세션8 T353 · 2026-09-22)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T354.md` · 레포 `지시/지시_T354.md`)

---

T323 소리판(`public/sfx-board.html`)에서 재민이 09-22 첫 귀 판정을 냈다. **이 표가 정본이다** — 세션의 취향이 아니라 재민의 귀가 판정이고, 바뀐 소리는 다시 소리판에서 재민이 듣는다.

| 대상 | ○/✗ | 재민 메모 | 이 카드가 하는 것 |
|---|---|---|---|
| `key:axe` | ○ | 다양한 소리가 있다면 좋을 것 | 변주 2~3 파일 추가(④) |
| `key:bird` | ○ |  | 무변 |
| `key:bite` | ○ | 대체제가 있다면 좋을 듯.. 다소 어색함 | 후보 1~2 더 찾아 소리판에 나란히(재민이 고른다) |
| `key:bronze_hit` | ✗ | 제대로 된 걸 찾아야 함.. | **재확보** — 지금은 "둔탁한 쿵"(`commons_pd_dullthud`)인데 쓰이는 자리가 셋이다: 돌·광석·운석 때리기(`resourceHit`) · 바닥에 물건 떨어짐(`groundDrop`) — **자리마다 다른 소리여야 한다**(돌 캐기 = 돌 부딪는 소리 · 떨어짐 = 가벼운 낙하) ⇒ 키를 가른다: `rock_hit`(돌·광석·운석) · `drop`(바닥) — #50 재민 결정과 같은 방향 |
| `key:cast` | ○ | 언짢긴 하지만 ㅇㅋ.. | 무변(후보 하나 더 나란히는 허용) |
| `key:deer_call` | ✗ | 울부짖는 거 말고 일반적인 소리가 필요 | **재확보** — 지금은 엘크 울음(`commons_pd_elk`) · 사슴이 평소 내는 낮은 소리·풀 밟는 기척 쪽 |
| `key:eat` | ✗ | 마인크래프트도 아니고 이게 뭐야.. 제대로 된 거 찾아야 해 | **재확보** — 짧은 씹는 소리 하나(만화풍 ✗) · 고기/열매를 가를지는 회부(지금 훅은 허기 상승 하나) |
| `key:fire` | ○ | 굿 | 무변 |
| `key:hook` | ○ |  | 무변 |
| `key:rain` | ○ | 이건 폭풍 버전인 거 같은데.. 가벼운 빗소리도 찾아야 함.. | `rain` 은 남기고 **`rain_light` 확보** · 층이 `ambient('rain', precip)` 의 세기로 두 파일을 가른다(문턱은 `_sfxWx.precip` 이 이미 갖는 눈금에서 · 새 수 0 · 유도 표) |
| `key:step_dirt` | ○ | 좋긴 한데, 돌바닥 걷는 소리야 | 파일을 **`step_stone`** 키로 옮긴다(지면에 돌이 있으면 훅 · 없으면 후보로만) |
| `key:step_grass` | ○ | 이게 흙바닥에 가까운 거 같은데? | 파일을 **`step_dirt`** 로 옮기고 `step_grass` 는 **새로 확보** — CREDITS 의 "지면은 가설" 경고(Kenney 팩에 바닥 표시 없음)가 재민 귀로 닫힌 것 |
| `key:tiger_growl` | ○ | 이것도 지금 어색하고, 다양한 소리가 필요할 듯.. | 변주 2~3 + 후보 |
| `key:water` | ✗ | 오줌 싸는 소리야.. 끔찍해.. 시냇물 소리 반드시 찾아 | **재확보** — 흐르는 시냇물 루프(`amb:water` 루프라 **이음새 없는 루프**여야 한다 · 루프 경계 실측) |
| `key:wind` | ✗ | 바람소리가 아니라 그냥 공기 소리인데..? | **재확보** — 나뭇잎·풀 스치는 바람 루프 · 세기 두 층이면 좋다(`ambient('wind', w.wind)` 세기가 이미 있다) |
| `key:wolf_howl` | ✗ | 최악. 다시 찾아.. | **재확보** — 늑대 울음 + 으르렁 변주 |
| `harvest` · `boar_grunt` | 미확보 | (T283 이후 무음) | **확보** — 수확은 뜯는/베는 짧은 소리 · 멧돼지 꿀꿀·콧김 |

**카드 T354 — 넷. PM 판정**(재민 거부권): ① **재확보 여섯 + 새 넷 + 지면 둘 바로잡기** — 위 표 그대로. 확보 규약은 T262 그대로: **파일마다 그 파일의 라이선스 페이지에서 읽는다**(검색어의 "CC0" 를 안 믿는다) · CC0 또는 CC-BY(표기) · 원본은 `~/Mini/_sfx_in/`(레포 밖) · 레포엔 `.ogg` + `.m4a`(사파리) · `manifest.json` `sources` 칸 + `CREDITS.md` §2-b 행(출처·저자·라이선스·날짜·잘라낸 구간) · 헤드룸 `_실측` 갱신 · **출처 없는 소리 0**(`test-assets-audit`). 후보 출처(순서 없음): Freesound(CC0 필터 · 파일 페이지에서 재확인) · Kenney · OpenGameArt · Wikimedia Commons · Sonniss GDC 번들(상업 가능 · 팩 라이선스 문서를 표에) · ⚠BBC Sound Effects 는 비상업 조건이라 **안 쓴다**. **재민 판정이 정본이므로 ✗ 한 키에 후보 2~3개를 소리판에 나란히 올린다**(키 하나에 `_후보` 행 — 재민이 고르면 다음 카드에서 정리) · 이 카드가 고르는 건 "1번 후보" 뿐. ② **변주 — 층 한 줄** — `manifest.json` 키 칸에 `files: [..]` 배열을 허용하고 `48-a-audio.js` 가 배열이면 **씨 해시로 고른다**(`Math.random` 금지 — 존 캐논과 같다 · 씨 = 사건의 좌표·틱 · 같은 사건은 같은 파일) · 단일 `file` 은 그대로(하위 호환 · 기존 키 비트 동일) · 소리판은 파일마다 행. ③ **빠진 소리 전수 표** — 재민: "이거 말고 다른 소리도 더 필요한 거 아냐? 아직 부족한 효과음 많지 않아?" → **지어내지 않는다**: 소리가 붙을 수 있는 자리를 코드에서 **전수로** 뽑는다 — 서버 브로드캐스트 종류 전부(`zone.js` `broadcast(`·`send(` 의 `type` 낱말) · `players[].act` 낱말 전부(`villages.js _lifeAct` 가 세우는 값) · `inventory.where` 낱말 전부 · 클라 UI 사건(열기/닫기/획득/거래/알림 — `public/client/*` 의 훅 자리) · 날씨(`_sfxWx` 칸 전부 · 천둥·눈) · 지면 종류(걷기 표면 — 클라가 아는 지형 이름 전부) · 건물 종류(`buildings` 표 — `campfire` 뿐이다) · 개체 종류(`animals.js` 전부) — 표: 자리 · 지금 소리 있음/없음 · 후보 키 이름 · 우선순위 칸은 **비워 둔다(재민)**. 이 표가 다음 확보 카드의 입력이다 — 이 카드는 **확보하지 않는다**(①의 것만). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: **이 카드에 한해 `public/assets/sfx/**` 를 세션9 에 위임**(자산 영역 세션8은 T353 으로 `bgm/` 에 있다 — `bgm/` 무접촉 · `CREDITS.md` 는 §2-b 효과음 절만) · 제품 코드 = `48-a-audio.js` 두 자리(파일 배열 · 비 세기 · 지면 키)뿐 · 기존 키의 단일 파일 경로 **비트 동일**(`e2e-audio-probe` 호출 수 그대로) · `test-audio` 105 + 새 절(배열 키 씨 해시 결정성 · `rain_light` 문턱 유도 · `step_*` 재배치) · `test-assets-audit`(출처 없는 소리 0 · 새 파일 전부 표에) · `test-client-globals` 26 · `test-harness-lint` 47 · 소리판 행 수 = 키·파일 수(T323 ④ 자명 통과 금지 그대로) · 접점 심볼(`manifest.json|sources|files|resourceHit|groundDrop|mobs|ambient|_sfxWx|step_|rain_light|rock_hit|drop|sfx-board|CREDITS|test-assets-audit|_sfx_in`).

## 0. §0 실측 (넷)
ⓐ 재확보·새 확보 표(키 · 파일 · 출처 · 라이선스 · 구간 · 헤드룸) + 후보 수. ⓑ 층 한 줄 diff + 결정성 절. ⓒ 빠진 소리 전수 표(자리 n · 있음 n · 없음 n). ⓓ 한 줄 답.

## 1. 절대 규칙
출처 없는 소리 0 · 라이선스는 파일 페이지에서 · BBC ✗ · 원본 레포 밖 · `bgm/` 무접촉(세션8) · `Math.random` 0 · 기존 키 비트 동일 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-ear1-0922` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 재확보·새 확보·지면 ② 변주 층 한 줄 ③ 빠진 소리 전수 표 ④ 한 줄 답.

## 3. 검증
하네스 넷 · 비트 동일 · 소리판 행 수 · 접점 목록.

## 4. 회부만
후보 중 고르기(재민 · 소리판) · 빠진 소리 우선순위(재민) · `eat` 고기/열매 가르기(훅이 하나 — 세션9 뒤 카드) · #50 낙하 키(`drop` 으로 닫히는지 재민) · 전투 소리(T329 세션10 뒤).

## 5. 완료
커밋 [승인 대기 · T354] · 가지 푸시 · `보고/T354_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 재확보 n · 새 n · 후보 n · 변주 층 한 줄 · 빠진 자리 n(없음 n)". 실기: 재민이 배포 뒤 소리판에서 둘째 귀 판정.
