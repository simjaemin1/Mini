# 구현 지시 — T309: 색인이 섰다 — 청크 경계로 가르는 코드 전수 감사(같은 결함이 또 있나 · 코드 0) · 나무 술어 `treeBlockerAt` 의 메모 자리 표(T295 세션10 이 붙일 세 줄 + 메모 · 코드 0) · 관측자 없는 마을 나무꾼·광부의 현장 후보 표(`resourceAt` 반경 16셀 · 마을 51 전수 · 코드 0) (세션5 · T301 뒤 · 2026-09-19)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T309.md` · 레포 `지시/지시_T309.md`)

---

T301(`batch/resource-index-0918` → PM 이 main 에 착지 · `resourceAt` = 청크 생성 함수 그 자체 `onlyCell` 인자 하나 · 300청크 바이트 동일 · 하네스 12/0 · 셀 15µs · 반경 16셀 4ms · 질의는 청크를 안 켠다 · **카드의 "3시드 면제" 전제가 틀렸음을 잡고 규약대로 돌려 넷째 판과 비트 동일** · 전쟁 세 줄은 `war-live.js` 가 아니라 `villages.js:2982` 라 안 건드리고 표로 낸 것) **통과**. "가르지 않고 인자 하나를 냈다 — 셀 루프로 다시 짜면 씨 순번이 바뀌어 세계가 바뀐다" 가 이 카드의 발견이고 족보감이다.

**카드 T309 — T301 회부 ②③④ · 코드 0 · 표 셋. PM 판정**(재민 거부권): ① **청크 경계 감사** — T301 ⓘ "청크는 자기 밖에도 낳는다(`ft1020_0` @ x=1032.5)" 를 받아, 청크 경계·청크 id 로 무엇을 **가르거나 거르는** 코드를 `server/`·`public/client` 에서 전수로 찾는다(`chunkKey|chunkOf|cx*|CHUNK_SIZE|inChunk|chunk.x` 등 — 검색어를 표에) · 자리마다 "경계 밖 개체를 놓치나 / 두 번 세나 / 상관없나" 셋 중 하나 + 근거 한 줄 · **고치지 않는다**(결함이면 회부 · 카드는 PM). ② **나무 술어 메모 자리 표** — T301 회부 ②: `treeBlockerAt` 칸당 11.7µs · 전장 한 판 42ms 를 병사×틱으로 부르면 비싸다 → `_warTerrBlocked` 의 셀 메모(비우는 시점 · 키 · 크기)를 읽어 **같은 자리·같은 규약**으로 나무 술어를 태우는 diff 를 **표로만**(붙일 줄 · 비우는 줄 · 예상 부하 = 칸 수 × 11.7µs 한 번) — T295(세션10) 가 그대로 붙일 수 있게 · `villages.js:2982~2996` 무접촉. ③ **현장 후보 표** — 관측자 없는 마을이 실걸음이 될 때(설계_생산_실체 ⓑ) 나무꾼·광부가 갈 자리: 51마을 전수로 `resourceAt` 반경 16셀 질의 → 마을별 나무 셀 수 · 광맥 셀 수 · 가장 가까운 것까지 셀 거리 · **0 인 마을**(현장이 없는 마을 = 그 직업이 걸을 데가 없다 — 지금 수식은 그래도 곳간을 채운다 · 이게 첫 발견 후보) · 51 × 4ms 벽시계. ④ 한 줄 답("경계 갈래 n자리 중 결함 후보 n · 메모 자리 … · 현장 0 마을 나무 n/광맥 n"). 하루. 질문 금지 — 답은 표.

★검증 규약: **코드 0**(`보고/` 한 파일 + 계측 스크립트 하나 허용 · `@regress` 없음 · `scripts/` 에만) · `test-resource-index` 12/0 그대로 · 3시드 면제(만진 파일 0) · 접점 심볼(`resourceAt|generateChunkResources|onlyCell|treeBlockerAt|_warTerrBlocked|_jobSites|chunkKey|CHUNK_SIZE`).

## 0. §0 실측 (넷)
ⓐ 경계 감사 표. ⓑ 메모 자리 diff 표. ⓒ 51마을 현장 표 + 0 마을. ⓓ 한 줄 답.

## 1. 절대 규칙
코드 0 · 새 수 0 · `villages.js` 전쟁 절(T295 세션10) 무접촉 · `chunk.js` 무접촉 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/chunk-audit-0919` → main ff · 통로 클론 무접촉 · 403 이면 `~/Mini/_inbox/` 번들) · force 0.

## 2. 작업 (넷)
① 경계 감사 ② 메모 표 ③ 현장 표 ④ 한 줄 답.

## 3. 검증
코드 0 · 하네스 그대로 · 접점 목록.

## 4. 회부만
경계 결함 후보 고치기(PM 카드) · 전쟁 세 줄 붙이기(T295 세션10 · `test-war-world ⓓ` 재실행) · 현장 0 마을의 처리(설계_생산_실체 · PM) · `tickSubsistence` 둘째 루프 무기·갑옷 마모 칸 · #31 · #37.

## 5. 완료
커밋 [승인 대기 · T309] · 가지 푸시 · `보고/T309_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · `인계/Z-존.md`(또는 제 영역 인계) 갱신 · 채팅 "가지 올렸다 <해시> — 경계 n/n · 메모 … · 현장 0 마을 n". 실기 0.
