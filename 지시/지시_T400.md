# 구현 지시 — T400: 집도 행위다 — 1층 구현(T361 설계 표 넷 · 재민 #51 ⓐ 확정 09-23) · 자재 축은 ★PM 권고대로 **서버 `HUT_STAGES` 정본**(기둥 6·서까래 8·풀 6·이엉 8 = 통나무 22·풀 38 · 석재 0 — #61 재민 거부권) · 짓는 이는 **여유 크루 그대로**(`LIFE_CREW` 2 · `builder` 신설 0 — #62 PM 권고) · 어부·나무꾼·채집·농부 문법 다섯째 적용: 크루가 곳간에서 자재를 **손에 들고**(`inventory` · 짐 25kg · 3짐) 집터로 걸어가 **놓는 순간** 단계가 오른다(`_lifeAdvanceSite` 가 그 순간에만) · 관측자 무관 실걸음 · 손잡이 `T400_BUILD_ACT` 기본 끔 · 끔 = 넷째 판 비트 동일 · 켬 30일 등가(T361 자 `t361-build-days.js` · 집 수 +119 그대로여야 한다 — 수요가 정한다) (세션4 · T395 뒤 · 2026-09-26)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T400.md` · 레포 `지시/지시_T400.md`)

---

T395(`batch/frame-owner-0925` → main · 330ms 의 주인 = 환경(SwiftShader 가 물 셰이더를 화면 전체에 칠한다 · 마을 366 중 358ms · GL 두 층 끄면 4.7ms) · 제품 0줄 · 맥 GPU 판은 권한에 걸려 못 돌렸고 우회 안 함 · 콘솔 한 줄 → 재민 #73) **통과** — "결정 판은 못 돌렸다고 적었다"가 이 카드의 문장이다(족보 318).

T361(세션4 · 09-23 · main)이 설계 표 넷을 냈다: 한 채 = `HUT_STAGES` 4단계(기둥 6 · 서까래 8 · 풀 6 · 이엉 8 = 57.8kg = 통나무 22 · 풀 38) · 운반은 낮의 7%(병목 아님) · 병목은 시공(서버 5,760인·초 ↔ 랩 4,600 · 2.86일/채) · **30일 집 수는 수요가 정한다**(`houseSiteWant` 9.6일 간격 > 4일/채 · 세 팔 +119 같음) ⇒ 행위로 옮겨도 집 수는 안 변하고 **누가 짓느냐**만 바뀐다. 재민 #51 ⓐ "집도 행위" 확정(09-23). #61·#62 는 PM 권고(서버 `HUT_STAGES` 정본 · 크루 그대로) — 재민 거부권 그대로 살아 있고 이 카드는 그 권고 위에 선다(뒤집히면 자재 표 한 줄만 바뀐다 — 그렇게 짠다).

**카드 T400 — 다섯 · 어부 문법(T312) 다섯째 적용. PM 판정**(재민 거부권): ① **자재 정본 하나** — econ 표(`HOUSE_STONE` · 목재 9·석재 15)와 서버 `HUT_STAGES` 가 다른 물건을 말한다(T361 §1) → `HUT_STAGES` 를 **하나의 표**로 두고 econ 의 집 자재 수요가 그 표에서 **유도**되게(econ 이 `HUT_STAGES` 합을 읽는다 · 사본 0 · `HOUSE_STONE` 은 대조 자로만 남김) · 석재 수요가 집에서 사라지는 만큼 광산 수요가 준다 — **그 차가 3시드 열에 어떻게 나오나 표**(켬 팔 · 끔은 무변) · 자재 표 한 줄이 바뀌면 전부 따라오게. ② **행위 1층** — 크루(`LIFE_CREW` 2 · 직업 무관 · #62)가 **곳간에서 자재를 꺼내 손에 들고**(`actToGranary` 의 역 · 짐 25kg → 3짐 · T361 §ⓑ) 집터 셀로 걸어가 **놓는 순간** `_lifeAdvanceSite` 가 그 자재만큼 단계를 올린다(시공 시간은 그대로 그 안에서 · `LIFE_STAGE_PDAY`) · 관측자 없는 마을도 같은 걸음(T347·T368 문법 · `_lifeHeadlessDay` 집 절은 켬이면 호출 0) · 손잡이 `T400_BUILD_ACT` 기본 끔 · 끔 = **3시드 넷째 판 비트 동일** · 씨 흐름 · `Math.random` 0. ③ **등가** — T361 자 `t361-build-days.js` 켬/끔 30일 · 집 수 +119 ↔ n(수요가 정하니 같아야 한다 · 다르면 그 차가 발견) · 낮 시간 +7% 안인가(걸음 비용 실측) · 크루가 짓는 동안 제 일을 못 하는 몫(어부가 집을 짓는 동안 물고기 −n · 그게 세계다 · 표만). ④ **관측자 게이트** — 관측자 0 팔 대 관측자 1 팔 집 수·단계 시각 비트 동일(T297 자 문법). ⑤ 한 줄 답. 나흘. 질문 금지 — 답은 표.

★검증 규약: **3시드 800일 열 열 넷째 판 동일**(끔 · `sim/`·`villages.js` 접촉) · 켬 3시드는 표에(새 기준선 아님) · `test-econ-fieldyield` 199 · `test-forage-act` 137 · `test-wood-act` 90 · `test-fish-act` 73 · `test-farm-life` · `test-hall-door-path` · `e2e-village` · `t361-build-days` 켬/끔 · `lab-wiring-check` 실패 0 · `build-econ-bundle --check` 8 · `inline-engine --check` · `test-harness-lint` 61 · 접점 심볼(`HUT_STAGES|HOUSE_STONE|houseSiteWant|_lifeAdvanceSite|LIFE_CREW|LIFE_STAGE_PDAY|_lifeHeadlessDay|actToGranary|inventory|_lifeAct|T400_BUILD_ACT|t361-build-days|t297-observe|vacantZoneOf`).

## 0. §0 실측 (다섯)
ⓐ 자재 정본 하나 diff + 켬 3시드 광산 열 차. ⓑ 행위 1층 diff + 끔 3시드 동일. ⓒ 등가 표(집 수 · 낮 % · 직업 손실). ⓓ 관측자 게이트. ⓔ 한 줄 답.

## 1. 절대 규칙
새 수 0(자재는 `HUT_STAGES` 그 수) · `builder` 신설 0 · 손잡이 하나(끔) · 관측자 무관 · `Math.random` 0 · **T368(세션1 밭 절 · `_lifeHeadlessDay` 농부 절)·T398(세션2 `_t325Scan`)·T399(세션3 A*)·T385(세션7 틱 본체) 무접촉** — `villages.js` 는 집 절만 · `_lifeHeadlessDay` 는 집 절만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/build-act-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6 · 3시드는 `setsid nohup … & disown`)**.

## 2. 작업 (다섯)
① 자재 정본 ② 행위 1층 ③ 등가 ④ 관측자 게이트 ⑤ 한 줄 답.

## 3. 검증
3시드 · 하네스 열둘 · 접점 목록.

## 4. 회부만
#61·#62 뒤집기(재민 · 한 글자) · 켜기(재민 · ③ 표 뒤 · #69 와 같이) · 플레이어가 짓는 집과 같은 표인지 실기(재민) · 석재 실물 수요(화덕·석기·석축 — PM · ① 표 뒤).

## 5. 완료
커밋 [승인 대기 · T400] · 가지 푸시 · `보고/T400_<날짜>.md`(ⓐ~ⓔ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T400]` · 베이스 해시**) · `인계/V-마을배치.md`·`F-제작건축.md` 갱신 · 채팅 "가지 올렸다 <해시> — 자재 정본 하나 · 행위 1층 끔 3시드 동일 · 켬 집 수 119 ↔ n · 낮 +…% · 관측자 게이트 n/n". 실기: 재민 배포 뒤 마을에서 크루가 자재를 나르는지.
