# 구현 지시 — T363: `/check_username` 닫기 — 이름 존재 노출 엔드포인트를 지우고 가입 응답에서만 "이미 있는 이름" 을 알린다(T310 회부 · #48 ★PM 결정 ⓑ) · `central-client.js:71` 호출자 · 클라 가입 흐름이 그 답을 어떻게 썼나 표 · `test-doors` 62 표에서 문 하나 줄기 · `~/Mini/실서버.txt`(#49) 는 재민 손 (세션7 · T356 대기 중 작은 카드 · 2026-09-23)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T363.md` · 레포 `지시/지시_T363.md`)

---

T352(C 커널 · 게이트 4/4 · WASM 권고) **통과** — ★PM 결정 #53(위임): **WASM**. 접속은 T356(세션3 해부)이 걸음이 주인이라고 확인한 뒤 카드가 나간다 — 그때까지 세션7 은 이 작은 카드. ★PM 결정 #48(위임): **ⓑ 닫는다** — `POST /check_username { username } → { taken }` 은 이름 존재를 아무에게나 알려주는 문이다(T310 §4). 가입 응답이 이미 "이미 있는 이름" 을 낼 수 있으면 그 문은 필요 없다.

**카드 T363 — 셋. PM 판정**(재민 거부권): ① **호출자 표** — `central.js:962` 문 · `central-client.js:71` · 클라 가입 흐름(`public/client/*` 어느 파일이 어느 순간 부르나 · 답을 UI 에 어떻게 썼나 — "이름 확인" 단추? 입력 중 실시간?) · 문을 닫으면 UI 가 어떻게 되나(가입 제출 뒤 응답으로 옮긴다 · 새 UI 0 · 있는 오류 표시 자리 그대로). ② **닫기** — 라우트 제거 · 클라 호출 제거 · 가입 응답(`/register` 류 · 이름이 뭐든 그 문)이 `taken` 을 이미 내면 그대로, 안 내면 **그 응답에 한 칸**(새 라우트 0) · `test-doors` 62 → 61(표 갱신 · 문 하나 줄었다는 절) · `e2e-onboarding` 74 그대로(가입 흐름을 실클라로 한 번). ③ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: `test-doors` 표 갱신 · `e2e-onboarding` 74 · `test-client-globals` 26 · `test-harness-lint` 57 · 새 라우트 0 · 접점 심볼(`check_username|central-client|register|taken|test-doors|onboarding`).

## 1. 절대 규칙
새 라우트 0 · 새 UI 0 · 배포 재민만 · `zone.js` 무접촉(세션3 T356) · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/close-username-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
`~/Mini/실서버.txt` 한 줄(#49 · 재민 손) · C 층 접속 카드(PM · T356 뒤 · WASM).

## 5. 완료
커밋 [승인 대기 · T363] · 가지 푸시 · `보고/T363_<날짜>.md`(**머리 15줄**) · `인계/N-네트워크.md` 갱신 · 채팅 "가지 올렸다 <해시> — 문 62→61 · 호출자 n · 가입 응답 taken ○". 실기 0.
