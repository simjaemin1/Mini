# 구현 지시 — T444: T394 의 +0.45ms 는 어디서 오나 — T433 이 한 손잡이씩 끈 팔 셋으로 갈랐다: T385 −1.83ms · T421 −0.41 · **T394(일터·배회 목표에 지형) +0.45ms**(켜면 비싸다 · 세계를 바꾸는 손잡이라 값만으로 판정 안 함 — 기록) ⇒ 그 0.45 의 해부: 되짚기(`_t394OpenTarget`)가 틱당 몇 번 · 한 번에 µs · 술어(`isTerrainBlockedLocal` → 능선 색인 T406 뒤 0.36µs)가 몇 번 · 어느 갈래(일터 배정 · 배회 · 튕김 도약 T399 ②)가 주인인지 · **되짚기 답이 같은 몸·같은 목표면 같은가**(캐시 후보 — T421 문법 "같을 때 안 세운다" · 비트 동일이 게이트) · 값이 줄면 그 만큼 · 안 줄면 "세계 값이다" 한 줄 (세션7 · T433 뒤 · 2026-09-27)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T444.md` · 레포 `지시/지시_T444.md`)

---

T433(`batch/spatial-on-0927` → main · `T421_SPATIAL_INC` 기본 켬 · spatial 0.323 → 0.197µs · 26존 합 65.4 → 61.6% · 틱 자의 규약 한 장(`lib-tick-rule.js` · 자 넷 × 다섯 20/20) · #54 켬 팔 p50 22.0ms(66%) · p95 28.5(85%) · 36/36 예산 안 · 세 손잡이 가름) **통과** — "한 손잡이씩 끄면 가른다 — 그리고 하나는 켜면 비싸다"가 이 카드의 문장이다(족보 361). ★PM: T394 는 캐논(일터는 닿는 곳 · T427 이 그 위) 이라 끄지 않는다 — 비용을 **해부**해서 값싼 자리가 있으면 그것만.

**카드 T444 — 넷. PM 판정**(재민 거부권): ① **해부** — 낮 창(규약 그대로 · 첫 경계 뒤 · 같은 띠 · 같은 씨) `T394_WORK_TERRAIN` 켬에서: 되짚기 호출 수/틱 · 갈래별(일터 배정 `_t394WorkSite` · 배회 · 튕김 T399 ② · T427 닿는 현장이 착지했으면 그 갈래) · 호출당 µs(p50·p95) · 술어 호출 수 · 술어당 µs(색인 켬) · 합이 0.45ms 에 닿나(닿지 않으면 나머지 주인 — 목표가 달라져 걸음이 길어진 몫? T433 자로 걸음 수 켬/끔). ② **캐시 후보** — 같은 몸 · 같은 목표 셀 · 세계 무변(그 청크 변경 사건 0)이면 되짚기 답이 같다 — T421 문법으로 "같을 때 안 되짚는다" · 게이트 = 되짚기 답 비트 동일(자 하나 · 미끼: 청크가 바뀌었는데 캐시를 쓰면 빨갛다) · 손잡이 `T444_OPEN_CACHE`(기본 끔 · 켬은 비트 동일 뒤 PM) · 켬 값(0.45 → n). ③ **T427 맞물림** — T427(세션3 · 닿는 현장 `T427_SITE_REACH`)이 착지했으면 그 위에서 · 두 손잡이 켬/끔 2×2 틱 값 표(같은 자) · 서로 갉아먹나. ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 새 수 0 · 손잡이 하나(기본 끔) · 되짚기 답 비트 동일 · 세계 무변(`T394` 켬 그대로) · `sim/` 무접촉 · **T432(세션1 `zone.js` 청크 문)·T440(세션2 `zone.js` 자원 절)·T427(세션3) 위에서 · 자리 다름** · `test-move-soa` 178 + 절 · `test-war-world` 116 · `e2e-move` 37 · `test-nippon-boot` 42 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`T394_WORK_TERRAIN|_t394OpenTarget|_t394WorkSite|isTerrainBlockedLocal|unstuckNpc|T399|T427_SITE_REACH|T444_OPEN_CACHE|t356-tick-anatomy|t371-tick-rest|lib-tick-rule`).

## 0. §0 실측 (넷)
ⓐ 해부 표(갈래 × 호출 × µs). ⓑ 캐시 diff + 비트 동일 + 값. ⓒ 2×2 표. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 손잡이 하나 · 비트 동일 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/t394-anatomy-0927` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 해부 ② 캐시 ③ 2×2 ④ 한 줄 답.

## 3. 검증
비트 동일 · 하네스 다섯 · 접점 목록.

## 4. 회부만
#54(재민 · T433 값 그대로) · #53 C 층(재민) · 캐시 켬(PM · ② 뒤).

## 5. 완료
커밋 [승인 대기 · T444] · 가지 푸시 · `보고/T444_<날짜>.md`(`지시/_보고머리_템플릿.md` 네 칸) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — 0.45 의 주인 = … (…%) · 캐시 켬 … ms · 비트 동일 ○ · 2×2 표". 실기 0.
