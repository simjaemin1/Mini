# 구현 지시 — T402: "맞음"의 낱말은 이미 있다 — `damagePlayer(p, dmg, source)` 의 `source` 가 호출부마다 다르다(`arrow:<id>` · `mob:<type>` · `player:<name>` · `fall` · `food:<item>` · `extreme:<why>` — `zone.js` 4086·5843·10282·10456·12298·12359·12582) 그런데 `setHp` 로 넘어갈 때 `'damage'` 하나로 뭉개진다 ⇒ **`source` 의 앞 낱말을 `why` 로 그대로 나른다**(새 낱말 0 · 사본 0 · 서버 그 자리 한 줄) · 층 표 `hpWhy` 에 `arrow`·`mob`·`player`·`fall` → `hit_body`(T397 확보) · `extreme`·`food` 는 안 운다 · **남이 쓰러지는 소리** — `player_down_state` 네 자리(4992·10651·10961·10982)가 쓰러짐·업힘·내려놓음·재접속을 같은 꼴로 보낸다 → 이미 있는 칸(`carriedBy` · `isDown`)과 호출부 이름으로 `why` 한 칸(서버 네 줄) · 층은 쓰러짐만 운다(`downed` · 남에게도) (세션9/SOUND · T397 뒤 · 서버 다섯 줄 위임 · 2026-09-26)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T402.md` · 레포 `지시/지시_T402.md`)

---

T397(`batch/sfx-hit-surface-0925` → main · **pid 충돌 ○ — 이웃 존 관전 연결의 p1 이 나를 텔레포트시켰다**(`e2e-pid-collide` 7/3 → 10/0 · `30-n-net.js` 세 줄) · `why` 두 줄 실었으나 `damagePlayer` 가 극단 감소(추위 3.6초마다 1HP)까지 `'damage'` 로 보내 "맞음"을 못 갈라 `hit_body` 는 확보만 · ⑧g 잰 미끼 · 28 vs 30 = 자의 줄 번호 · 표면 셋 · 눈이면 빗소리 0 · 천둥은 세계에 사건 없음 · `test-audio` 158 · `e2e-audio-probe` 58) **통과** — "pid 는 존마다 p1 부터 센다"가 이 카드의 문장이다(족보 319). ★PM: T397 §머리 3 이 "서버가 극단 감소를 다른 낱말로 보내는 날" 이라 했는데 — **그 낱말은 이미 있다.** `damagePlayer(p, dmg, source)` 의 셋째 인자가 호출부마다 다르고(위 일곱 자리) `setHp` 로 갈 때만 `'damage'` 로 뭉개진다. 새 낱말을 짓는 게 아니라 **있는 것을 나르는 것**이라 세션9 에 위임한다(서버 한 줄 + `player_down_state` 네 줄 = 다섯 줄).

**카드 T402 — 넷. PM 판정**(재민 거부권): ① **`why` = `source` 의 앞 낱말** — `damagePlayer` 안에서 `setHp(p, v, why)` 를 부를 때 `'damage'` 대신 `source.split(':')[0]`(`arrow`·`mob`·`player`·`fall`·`food`·`extreme` — 여섯 · 전수 표: 호출부 7 자리 · 낱말 · 방송에 실리는 값) · **한 줄** · 다른 `setHp` 호출부(회복·먹기)는 무변 · 방송 크기 +낱말 하나(T397 이 실은 그 칸). ② **층 표** — `hpWhy`: `arrow`·`mob`·`player`·`fall` → `hit_body`(T397 1번 후보 · 2번 나란히 그대로) · `extreme`·`food` → 안 운다(추위 속 3.6초마다 맞는 소리 0 — e2e 로 못 박는다: `extreme:cold` 전문 미끼 0회) · 곁 사람에게도 들리나(방송) — 거리 감쇠는 있는 규칙(`mobs`·`step_*` 문법 그대로) · `e2e-audio-probe` 에 맞음 호출 1(`mob:wolf`) + 극단 0. ③ **남이 쓰러지는 소리** — `player_down_state` 네 자리에 `why` 한 칸: 4992·10651 = `down`(쓰러짐) · 10961 = `carried`(업힘 · `carriedBy` 이미 있음) · 10982 = `set`(내려놓음/재접속 동기) — 호출부가 이미 아는 이유 · 새 낱말 0(이름은 호출부 함수 이름에서) · 층: `why === 'down'` 만 `downed`(T387 키 · 남에게도 · 거리 감쇠) · 나머지 0 · `relogin` 거름은 T387 그대로 · `e2e-pid-collide` 10 + 남의 쓰러짐 1(관전 아님 · 같은 존 · 운다) · 재접속 0. ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 서버 = `damagePlayer` 한 줄 + `player_down_state` 네 줄 **다섯 줄만**(diff 줄 수 보고에) · 다른 서버 무접촉 · `30-n-net.js` 무접촉(T397 로 끝) · 기존 32키 비트 동일 · `test-audio` 158 + 절 · `e2e-audio-probe` 58 + 2 · `e2e-pid-collide` 10 + 1 · `e2e-aim` 15 · `test-client-globals` 26 · `test-assets-audit` PASS · `test-harness-lint` 61 · `test-body` 306(`setHp` 접점) · 접점 심볼(`damagePlayer|setHp|source|hp_changed|why|hpWhy|hit_body|player_down_state|carriedBy|downed|48-a-audio|e2e-pid-collide|e2e-audio-probe`).

## 0. §0 실측 (넷)
ⓐ 호출부 7 표 + 한 줄 diff. ⓑ `hpWhy` 표 + 극단 0 미끼. ⓒ `player_down_state` 네 줄 + 남의 쓰러짐 1 · 재접속 0. ⓓ 한 줄 답.

## 1. 절대 규칙
새 낱말 0(있는 `source`·호출부 이름) · 서버 다섯 줄 · 출처 없는 소리 0(새 확보 0 — T397 것 배선만) · `bgm/` 무접촉(세션8 T401) · `Math.random` 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-why-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① `why` 한 줄 ② 층 표 ③ 쓰러짐 네 줄 ④ 한 줄 답.

## 3. 검증
하네스 아홉 · 비트 동일 · 접점 목록.

## 4. 회부만
#60 둘째 귀 판정(재민 · 배포 뒤 · T354·T358·T387·T397·이 카드 한 번에) · 군대 전투 소리 `war_*`(재민 #60) · `self_stat`(재민) · 녹음 회부 둘(재민 폰 · 깨어나는 숨 · 눈) · CC-BY 표시(재민 `/크레딧` #14ⓒ) · 천둥 사건(세계 · PM · K 층).

## 5. 완료
커밋 [승인 대기 · T402] · 가지 푸시 · `보고/T402_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T402]` · 베이스 해시**) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — why 여섯 낱말 · hit_body 운다 1 · 극단 0 · 남의 쓰러짐 운다 1 · 재접속 0 · 서버 5줄". 실기: 재민 배포 뒤 소리판 + 늑대에게 맞아 보기.
