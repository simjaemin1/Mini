# 구현 지시 — T336: `nippon` 시딩 진단(코드 0) — 닛폰 존에 마을이 서려면 무엇이 있어야 하나(지형 정본 `*-terrain.json` · 시딩 함수 `seedAllVillages` 가 읽는 것 · 한반도와 갈리는 자리 전수) · 지금 띄우면 어디서 서나 (세션5 · T327 뒤 · 2026-09-20)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T336.md` · 레포 `지시/지시_T336.md`)

---

T327(main `e2d82743` · 승계 정본 "도착 순" → `_returnBriefDone` · 두 순서 같은 답 · known −1) **통과**(족보 261).

**카드 T336 — 진단 · 코드 0.** 재민이 닛폰·중원을 다음 존으로 둔다(존 = 지역 = 서버 하나 · 한반도는 안 쪼갠다). ① `ZONES.nippon` 이 지금 무엇을 갖고 있나(지형 json · 강·호수 표 · 광맥 · `isOcean` · 크기) · `seedAllVillages` 와 생활층·econ 이 한반도 전용으로 읽는 자리 전수(`hanbando-terrain.json`·`hanbando-minerals.js`·상수·이름) — 자리마다 "존 인자로 받으면 되나 / 한반도 하드코딩인가" · ② **지금 `ZONE_ID=nippon` 으로 띄우면** 어디서 서나(부팅 로그 첫 오류 · 서면 마을 수) — 컨테이너에서 한 번 · ③ 닛폰 첫 마을이 서기까지 필요한 카드 목록(순서 · 크기 추정) · 한 줄 답. 하루. 질문 금지 — 답은 표.

## 1. 절대 규칙
코드 0 · 배포 0 · ★푸시 2판(가지 `batch/nippon-audit-0920` → main ff(문서만) · 403 → `~/Mini/_inbox/`) · force 0.

## 5. 완료
커밋 [승인 대기 · T336] · `보고/T336_<날짜>.md`(**머리 15줄**) · 채팅 "main 올렸다 <해시> — 하드코딩 n자리 · nippon 부팅 … · 카드 n장".
