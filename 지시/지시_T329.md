# 구현 지시 — T329: 위협 함수 T 가 봉쇄를 대신한다 — `_siegeBlock`·`_siegeOutMul`·`_recomputeSiege`(산 칸) 제거 · 대비 = 마을 안에서만 생산(R_out = 영토 × (1−T) · 연속 · 배수 0) · `WAR_OPS=0` 손잡이 제거 · 포로 몸값 품목 = 섭식 정본 함수 · 캐러밴은 같은 T (세션10/WAR · T295 뒤 · 2026-09-19)

아래 전문을 **세션10** 에 붙여넣으면 된다. (지시서: 레포 `지시/지시_T329.md` — `git pull --ff-only origin main` 먼저 · 푸시 403 이면 번들을 채팅에)

---

T295(`batch/war-cost-0918` → PM 이 main 에 착지 · pid↔econ 1:1 결속 · `_laborMul` 동원 항 삭제(T284 회부 ②의 "미소비"는 오독 — 실제로 걸려 있었다 · 그래서 이중 방지로 지움) · 군량 한 번 싣고 넷 다 같은 문으로 환급 · 품목은 `consumeFood` 그 함수 주입 · 3시드 바이트 동일 · `test-war-world` 54/0 · 손잡이 끔 비트 동일 · **`_siegeBlock`·`_siegeOutMul`·`_recomputeSiege` 는 산 칸**이라는 답) **통과** — "오독이었다" 를 대조로 잡고 이중 감산을 막은 것이 값이다(족보 255). T301 회부 ①(전쟁 세 줄 자리 `villages.js:2982` · 붙일 세 줄은 `보고/T301` §0ⓑ · 메모 한 칸은 `_warTerrMemo` — `보고/T309` §0ⓑ)도 이 카드에 넣는다.

**캐논(`설계_전쟁_플레이어.md` 4-3·6-8 · 재민 09-18)**: 봉쇄 상태는 없다 · 배수 없다(포로 ×0.7 뿐) · 대비 = 위협 T(거리 · 접근 속도 −ḋ · 병력비 · 연속) 에 따라 **마을 밖 생산 반경이 줄어든다** — R_out = 영토 × (1−T) · 문턱은 기존 `WAR_ALERT_R`·행군 속도·`pDef` 에서 **유도** · w 셋은 랩 A/B 표만(값은 재민).

**카드 T329 — 넷. PM 판정**(재민 거부권): ① **T** — 함수 하나 `threatOf(vil, now)`(전쟁 절 · 새 수 0 · 유도 목록 표) · 매 게임일 경계 + 군대 이동 때(O(전쟁 수)) · 캐러밴은 같은 T(실체 조우는 `caravan-battle` 그대로). ② **R_out** — 생활층의 야외 직업 현장 후보를 R_out 안으로 거른다(`_jobSites` 그 자리 · T312/T325 어부·나무꾼도 같은 거름 — 세션3·1 과 접점 · **거르개 한 함수** · 사본 0) · 안으로 못 가는 사람은 그림자가격이 옮긴다(배수 0) · `_siegeBlock`·`_siegeOutMul`·`_siegeM`·`_recomputeSiege` **제거**(economy-sim 생산 줄의 `_siegeM` 곱 삭제 — 끔 팔 비트 동일이 자 · 옛 손잡이·상수 죽은 칸 정리). ③ **`WAR_OPS=0` 손잡이 제거** · **포로 몸값** `WAR_CAP_RANSOM_FOOD` 를 `storage.food` 한 칸에서 `consumeFood` 순서로(T295 ③ 문법) · 전쟁 세 줄 + 메모 한 칸(T301·T309) 붙이고 `test-war-world ⓓ` 재실행. ④ 세계 위 부하(T284 자 · W0/W6 · T307 이 T295 전 기준을 냈다 — 나란히). ⑤ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 3시드 바이트 동일(랩엔 전쟁 0 → T=0 → R_out = 영토 · 비트 동일이어야 한다) · 게이트 셋 · `test-war-world` 54 + 절 넷(ⓛ T 단조: 가까워지면 오른다 · 멀어지면 내린다 · 병력비 ⓜ R_out 이 연속(칼로 나뉘는 값 0 · 두 T 사이 R_out 차이 ≤ 그 T 차이의 비례) ⓝ `_siege*` 심볼 0(정적) ⓞ 몸값 품목 = `consumeFood` 순서) · 접점 심볼(`threatOf|WAR_ALERT_R|pDef|_jobSites|_siegeBlock|_siegeOutMul|_siegeM|_recomputeSiege|WAR_OPS|WAR_CAP_RANSOM_FOOD|consumeFood|treeBlockerAt|_warTerrMemo|caravan-battle`).

## 1. 절대 규칙
새 수 0 · 배수·상태 스위치 0(공통 §1) · 사본 0 · **T324(세션3 이동 문·캐시)·T325(세션1)·T326(세션4)·T328(세션7 `PV_DEPOSIT_MAP`)** 접점 무접촉 — 거르개 함수는 이 카드가 정본이고 어부·나무꾼은 그 함수를 부른다(리베이스 때 둘 다 살린다) · 배포 재민만 · 가지 `batch/war-threat-0919` · 403 → 번들 채팅 · force 0.

## 4. 회부만
w 셋 값(재민 · 랩 A/B 뒤) · 대치 n초(#40) · 사기 항 값 · ⑤ 무기 재고·화살 · ⑥ 선포 무관 동원 · `FOLLOW_CAP` · 소대 배분 UI(설계 2-3b) · 외교 창.

## 5. 완료
커밋 [승인 대기 · T329] · 가지 푸시 또는 번들 · `보고/T329_<날짜>.md`(**머리 15줄**) · `인계/X-전쟁.md` X-3 · 채팅 "가지 올렸다 <해시> — T … · R_out 연속 · siege 0 · 54+4 · p50 …".
