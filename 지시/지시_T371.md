# 구현 지시 — T371: '그 밖' 25.7% 의 이름 — T356 해부에서 이름 없이 남은 갈래(econ 하루 틱 · 몹 AI(wildlife) · 게이지 · 계단 · 청크 활성 · 공간 인덱스 갱신 · 그 밖의 나머지)를 같은 자(`t356-tick-anatomy` · 한 판 안 시계 · 자가 자를 잰다)로 여섯 이상으로 가른다 · 합이 25.7%(2.11µs)에 닿는지 · **시계 자리만**(`zone.js` 갈래 입구에 시계 · 제품 동작 무접촉 · 손잡이 0) · 표만 (세션7 · T363 뒤 · 2026-09-23)

아래 전문을 **세션7** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T371.md` · 레포 `지시/지시_T371.md`)

---

T363(`/check_username` 을 지우지 않고 **안 문**으로 — 지웠으면 게스트가 승계 계정 이름을 달 수 있었다(`zone.js:4191` 게스트 갈래의 `findAccount` 술어는 다른 문이 안 쓴다) · 로비는 `/auth` 응답 · 문 62 그대로(공개 15 · 안문 41 · 남음 0) · `test-doors` 44/0 · `GET /player/<이름>` 200/404 축은 남았다 → 회부) **통과** — "문은 닫았는데 지우지는 못했다 — 부르는 쪽이 클라만이 아니었다"가 이 카드의 문장이다(족보 289). ★T356 회부 2 가 이 카드다: **'그 밖' 25.7% 가 A\* 보다 큰데 이름이 없다.** 세션3 은 T370(A\*)을 한다 — 같은 `zone.js` 지만 자리가 다르다(시계 입구 vs 경로 조건).

**카드 T371 — 셋 · 표만. PM 판정**(재민 거부권): ① **갈래 이름 붙이기** — 틱 본체(`zone.js:11255` 부근 틱 루프)에서 T356 여섯 갈래 밖에 남는 호출을 **전수 나열**(함수 이름 · 호출/틱 · 어느 루프 안인가): econ 하루 틱(`SimVillages` 일 경계 · 마을당) · 몹 AI(`wildlife.js` 틱 · 개체 수) · 게이지(허기·체온·`kcal.js`) · 계단·층 · 청크 활성/비활성(`activeChunkKeys` 갱신) · 공간 인덱스 갱신(`qtResources`·`qtPlayers` 재삽입) · 저장(`_nextSaveAt`) · 방송 밖 네트워크(핑·큐) · 그 밖. ② **시계** — T356 계측기의 그 문법(`performance.now()` · 할당 0 · `clockNs` 계수 빼기 · 관측자 규약)으로 갈래마다 시계 · 낮/밤 창(T356 과 같은 조각 길이 · 같은 씨) · 표: 갈래 · µs/사람 · %(합 8.22 기준) · 호출/틱 · **합이 2.11µs(25.7%)에 닿는지 — 못 닿는 나머지는 "못 가른 것" 한 줄**(지어내지 않는다) · 시계 자 값(자가 잰 자) 한 줄. ③ 한 줄 답 + 회부: 가장 큰 이름이 무엇이고 그 카드는 누구 것인지(판정 0). 이틀. 질문 금지 — 답은 표.

★검증 규약: **제품 동작 무접촉**(시계 입구 줄만 · 손잡이 0 · 시계는 T356 이 켠 그 스위치 아래 — 끔이면 호출 0) · 끔 비트 동일(T350 자) · `test-move-soa` 59 · `test-war-world` 56 · `test-harness-lint` 57 · `lab-wiring-check` 실패 0 · 접점 심볼(`t356-tick-anatomy|clockNs|performance.now|npcLifeTick|wildlife|kcal|activeChunkKeys|qtResources|_nextSaveAt|zone.js:11255`).

## 1. 절대 규칙
표만 · 새 수 0 · 손잡이 0 · **T370(세션3 `zone.js` 경로 조건 · 리베이스 때 둘 다 살린다)·T359·T364 무접촉** · 배포 0 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/tick-rest-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
`GET /player/<이름>` 200/404 축(T363 회부 · PM · N 카드) · 가장 큰 갈래의 카드(PM) · C 층(#53).

## 5. 완료
커밋 [승인 대기 · T371] · 가지 푸시 · `보고/T371_<날짜>.md`(ⓐ~ⓒ · **머리 15줄에 갈래 표**) · 채팅 "가지 올렸다 <해시> — 갈래 n · 가장 큰 것 … µs(…%) · 합 …/2.11". 실기 0.
