# 구현 지시 — T315: 집 간격 18 은 어디서 왔나 — 값이 아니라 발자국·통로에서 유도한 최소 간격으로(새 수 0 · 배수 0) · 켠 세계(T298)에서 거부 사유 66% 가 얼마나 남나 · 그 뒤 `_mapBeds` 살리기(서버 한 줄 · 심는 날 게이트: 즉시 출생 정지 0) (세션4 · T298 뒤 · 2026-09-19)

아래 전문을 **세션4** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T315.md` · 레포 `지시/지시_T315.md`)

---

T298(`batch/terr-on-0918` → main · 영토+방아쇠 **서버 기본 켬** 자리 둘 · 되돌림 `=0` · 3시드 `cmp` 동일 — **자가 생활층을 안 타서다** · 카드의 "villages.js 에 안 심는다" 전제를 정정한 것 · `_mapBeds` 표: 켬 위에서 막히는 마을 27→9~12 · 남은 병목은 **집 간격 18 하나**(거부 사유 65.8~66.2%)) **통과** — "다섯째 판 = 넷째 판" 이 수가 같아서가 아니라 자가 그 층을 안 재기 때문이라고 적은 것이 정직하다(족보 241). ★PM: 공통 기준선 주석·`V-마을배치.md:15` 는 PM 이 고쳤다. T299(세션6 twogate)는 이제 이 위에서 리베이스한다.

**카드 T315 — 셋 · 순서가 카드다(간격 → 살리기). PM 판정**(재민 거부권): ① **간격 18 의 출처와 유도** — `18` 이 어느 상수에서 왔는지(`village-layout` · `_lifeAddHouseSite` · `houseSiteWant` 의 거부 문 — 자리 표) · 캐논 "새 수 0 · 배수 0" 이므로 **마을 규모에 맞춘 값**을 만들지 않는다 — 대신 **유도**: 집 발자국(`_vbFootprint('hut')` 실제 크기) + 통로 한 칸 + 마당(밭 셀이 붙는 규칙이 있으면 그 칸) 에서 **최소 간격**을 식으로 세운다 · 그 식이 18 보다 작으면 왜 18 이었는지(T-이력 · 주석)를 표에 · 같으면 "18 은 유도값이었다" 한 줄 · **켠 세계(T298 기본)에서 50마을 × 200일 실서버 판** 을 유도 간격으로 다시 돌려 거부 사유 표(66% → ?) · 침상/인구 · 즉시 출생 정지 마을 수 — 손잡이 `T315_HOUSE_GAP` **기본 끔**(끔 = 지금 18 그대로 · 켬 = 유도식) · 값은 없다. ② **`_mapBeds` 살리기** — T298 ⓓ③ 그대로: 서버 한 줄(`serializeEcon` 이 쓰는 그 econ 객체에 완공층 × `village-layout.HOUSE_CAP_PER_FLOOR` — 랩 `_bf * L_FLOORCAP` 과 **같은 정본 상수** · 사본 0) · 손잡이 `T315_MAPBEDS` 기본 끔 · **심는 날 게이트**(켬 판): 즉시 출생 정지 마을 **0** · 침상/인구 ≥ 100% 마을 수 · 소멸 0 · **①을 켠 판 위에서만** 켠다(순서 위반 = 큰 마을 9~12곳 그날 정지 — 그 표를 반례로 같이 낸다: ① 끔 + ② 켬 = 정지 n곳). ③ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 손잡이 둘 기본 끔 = **실서버 판 비트 동일**(T298 ⓞ 문법 `__probe.handles()` · 3시드 800일은 자가 이 층을 안 타므로 면제 — 다만 `economy-sim.js` 를 만지면 면제 없음 · ②는 서버 쪽 한 줄이라 `sim/` 무접촉이어야 한다) · `test-psite-server` · `test-terr-persist` · `lab-wiring-check` · 새 절: ⓐ 유도식 = 발자국+통로 심볼에서 정적으로 읽힌다 ⓑ ① 끔+② 켬 정지 n>0 · ① 켬+② 켬 정지 0(반례 쌍) ⓒ `_mapBeds` 값 = 완공층 × 정본 상수(한 마을 손셈) · 접점 심볼(`_lifeAddHouseSite|houseSiteWant|_wantSite|_vbFootprint|HOUSE_CAP_PER_FLOOR|L_FLOORCAP|_mapBeds|_hcap|serializeEcon|T230_TERR_HOUSING|T219_HOUSE_TRIGGER|T315_HOUSE_GAP|T315_MAPBEDS`).

## 0. §0 실측 (셋)
ⓐ 간격 출처·유도식·켠 판 거부 사유 표. ⓑ 살리기 한 줄 + 게이트 + 반례 쌍. ⓒ 한 줄 답.

## 1. 절대 규칙
새 수 0(유도만 · 유도 목록 회부) · 배수·상태 스위치 0 · 사본 0 · 손잡이 기본 끔 · 켜지 않는다 · **T312(세션3 어부 · `villages.js` 생활층 어부 자리·`_carry`)·T295(세션10 전쟁 절)·T305(`zone.js`) 무접촉 — 리베이스 때 둘 다 살린다** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/house-gap-0919` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0.

## 2. 작업 (셋)
① 간격 유도 ② 살리기 ③ 한 줄 답.

## 3. 검증
끔 비트 동일 · 하네스 셋 + ⓐⓑⓒ · 접점 목록.

## 4. 회부만
켜기(재민 · ①② 같이) · 첫 의뢰(#18) · `nippon` 시딩(Z).

## 5. 완료
커밋 [승인 대기 · T315] · 가지 푸시 · `보고/T315_<날짜>.md`(ⓐ~ⓒ · **머리 15줄에 결론**) · `인계/V-마을배치.md` 갱신 · 채팅 "가지 올렸다 <해시> — 간격 … · 거부 66%→…% · 살리기 정지 0 · 반례 n". 실기 0.
