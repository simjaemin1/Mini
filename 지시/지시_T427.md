# 구현 지시 — T427: 못 닿는 현장을 안 준다 + 게임 시계를 기동에 묶는다 — T399 회부 1·2: ① 1500 × 뭍의 뿌리는 직업 현장(`_workSite`) 배정이 **반경 안에서 닿는지 안 본다**(사냥꾼·어부 92% · 새 main 아침 30쌍 반경 밖) → 배정 순간 A* 의 그 술어와 반경(64셀 원 · 있는 수)으로 **닿는 현장만** 준다(안 닿으면 다음 후보 · 없으면 표) · 되묻기 0 · 손잡이 `T427_SITE_REACH` 끔 · 게이트 = T381 자 켬/끔 8/8 · 도착 무변 ② **시계 손잡이** — 게임 시계가 벽시계에 묶여 부팅 11초 ↔ 50초가 아침 세계를 바꾼다(T399 §3-4 · T413 이 하네스에서 본 시계 둘 · T381 회부 6): 존 기동 순간의 게임일·틱을 **기동에 묶는 손잡이**(계측·하네스용 · 제품 기본 무변) → T406 켬/끔 아침 세계가 **같아지는지** 실측(같아지면 T399 의 "T406 이 세계를 바꾼다"는 시계였다 · 안 같으면 그 차가 발견) · 그 뒤 `T399_CELL_CAP` 켬 재판정 (세션3 · T399 뒤 · 2026-09-26)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T427.md` · 레포 `지시/지시_T427.md`)

---

T399(`batch/cell-cap-0926` → main · 1500 × 뭍 = 직업 현장 출근 92% · 예산만 풀면 잰 베이스에선 2,864/2,864 닿는다(필요 칸 p50 3,643 · 우회 ×4.8) · 1,500 은 반경 64 와 따로 온 수 → 반원 6,434 로 유도하면 A* −60~67% · 도착 무변 · **그러나 새 main 아침엔 반경 밖 사냥터 30쌍이라 켬이 3배 비싸다 → 켜지 마라** · 튕김 도약에 지형(T394 손잡이에 얹음 · 막힘 47% → 0) · ★**T406 을 켜면 아침 세계가 바뀐다**(술어 174,405칸 동일 · 부팅 11 ↔ 50초) · `test-move-soa` 149/0) **통과** — "1,500 은 반경 64 와 따로 온 수였다"가 이 카드의 문장이다(족보 342). ★PM: T406 은 되돌리지 않는다 — 술어가 같고 해시가 같다 · 바뀐 것은 **시계**다(그것을 이 카드가 세운다).

**카드 T427 — 넷 · 순서가 카드다. PM 판정**(재민 거부권): ① **닿는 현장만** — `_workSite` 배정 자리(`villages.js` 직업별 현장 고르기 · 사냥터·어장·벌목·채집·광맥)에서 후보를 고를 때 **A* 가 쓰는 그 술어 + 반경 64셀 안 실제 도달**(`findPath` 한 번 · 배정은 하루 한 번이라 비용은 표에 · 새 탐색 함수 0)로 걸러 닿는 현장만 · 안 닿으면 다음 후보 · 전부 안 닿으면 **집(회관)** 이 현장(그 마을·직업은 표 — 그게 발견 · T407 소외 표의 그 열) · 손잡이 `T427_SITE_REACH` 기본 끔 · 게이트 T381 자(같은 씨 두 팔 · 8/8): 1500 × 뭍 호출 · 반경 밖 쌍 30 → n · A* ms/틱 · 물가 정지 · 도착률·도착 시간 무변 이상 · 3시드 면제 근거 한 줄(현장 배정은 생활층 — `villages.js` 를 만졌으니 그래도 돌린다 · 넷째 판-b). ② **시계 손잡이** — 존 기동 때 `gameDayOf(Date.now())`·`state.lastGameDay`·`_e2eClock` 류가 읽는 벽시계 원점을 **기동 시각에 고정하는 손잡이**(`ZONE_CLOCK_ANCHOR=boot` 꼴 · 하네스·계측 팔 전용 · 제품 기본 = 종전 벽시계 · 새 수 0) · 표: 벽시계를 읽는 자리 전수(파일:줄) · 손잡이가 덮는 자리/못 덮는 자리. ③ **T406 재판정** — ②의 손잡이 아래에서 `TERRAIN_SEG_INDEX` 켬/끔 아침 창(T399 자): 비취침 · 반경 밖 쌍 · A* · 물가 정지 — 같아지나(8/8) · 같으면 "T406 은 세계를 안 바꿨다 · 시계였다" 한 줄 · 다르면 남는 차의 이름. ④ **`T399_CELL_CAP` 재판정** — ①켬 + ②고정 아래에서 켬/끔(T399 자): 켬이 이제 싸면 **기본 켬**(★PM 위임 · 되돌림 `=0`) · 아니면 표 · 한 줄 답. 사흘. 질문 금지 — 답은 표.

★검증 규약: 3시드 넷째 판-b 동일 · `test-move-soa` 149 + 절(닿는 현장 · 시계 고정 · 미끼: 못 닿는 현장을 일부러 주면 빨개진다) · `_path-core-test` 42 · `test-fish-act` 73 · `test-wood-act` 106 · `test-forage-act` 137 · `test-war-world` 116 · `e2e-move` 37 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`_workSite|findPath|isTerrainBlockedLocal|searchRadiusCells|T427_SITE_REACH|T399_CELL_CAP|gameDayOf|lastGameDay|_e2eClock|ZONE_CLOCK_ANCHOR|TERRAIN_SEG_INDEX|t381-path-cost`).

## 1. 절대 규칙
새 수 0 · 손잡이 둘(현장 · 시계 — 제품 기본 무변) · 새 탐색 0 · **T410(세션1 idle 문)·T421(세션7 공간 색인)·T426(세션2 자원 절) 무접촉** — `villages.js` 는 현장 배정 자리만 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/site-reach-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · ①④ 표) · 반경 밖 사냥터 마을 값(재민 · ① 표 — 집이 현장인 직업이 생기면) · C 층(#53).

## 5. 완료
커밋 [승인 대기 · T427] · 가지 푸시 · `보고/T427_<날짜>.md`(템플릿 네 칸) · `인계/M-이동입력.md`·`V-마을배치.md` 갱신 · 채팅 "가지 올렸다 <해시> — 반경 밖 쌍 30 → n · A* … → … · T406 아침 같음 ○/✗ · CELL_CAP 켬/표". 실기 0.
