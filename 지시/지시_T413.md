# 구현 지시 — T413: `test-war-world` ⓛ 간헐 빨강의 뿌리 — 세 세션이 봤다(T394 첫 두 판 94/3·96/1 · T403 14판 중 2 · PM 컨테이너 96/1 → 97/0 · main 에서도 재현) · "숲에서도 판이 끝난다" 절이 교전 1427~1800틱에서 `ended` 가 안 서는 판 — 자의 결함(시간·부하 단언)인가 제품의 결함(숲 교전이 정말 끝나지 않는 판이 있다 — 대치 교착)인가 **먼저 가른다** · 자면 결정적 자로(씨 고정 · 틱 수 단언 → 상태 단언) · 제품이면 그 교착의 표(어느 편성 · 어느 지형 · 무엇이 안 끝나나) + 손잡이 0 고침 · 그리고 X-1 회부 ⑥ **선포 무관 동원**(아무 때나 소집 = 기습) 표만 (세션10/WAR · T403 뒤 · 2026-09-26)

아래 전문을 **세션10** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T413.md` · 레포 `지시/지시_T413.md`)

---

T403(`batch/arms-ledger-0926` → main · 무기 세 축 서로 안 봄 · `weapQ` 는 숙련 평균(이름만 대장간) · 품목 0/7 · 장부 1층 소집 −9/복귀 +9 · 재고 5.6 에 병력 11 이면 반출 5 · 민병 6 · 끔 3시드 바이트 동일 · `test-war-world` 110/0 · ⚠ⓛ 간헐) **통과** — "무기의 세 축은 서로를 안 본다"가 이 카드의 문장이다(족보 329). #68 세 길·민병 값은 재민. 남은 것 중 **자가 흔들리는 것**이 먼저다 — 흔들리는 자로는 다음 카드(짐이 먹는다 · 손에 든다)를 못 잰다.

**카드 T413 — 셋. PM 판정**(재민 거부권): ① **ⓛ 귀속** — 실패 판을 **재현 가능하게** 잡는다: 씨(`warId` · 세계 씨)를 실패 판의 것으로 고정하고 10판 → 같은 씨에서 매번 빨간가(결정적 = 제품 · 그 씨가 표) 아니면 같은 씨에서도 갈리나(비결정 = 자·시계·부하 — 어디서 `Date.now`/벽시계/`setTimeout` 이 끼나 표) · 실패 판의 교전 1427~1800틱 상태 덤프(양편 사기 · 남은 병력 · 대치/교전 위상 · `_opCheckSurrender`·`M_BREAK` 조건이 왜 안 서나) · **판정 한 줄**: 자 / 제품. ② **고침** — 자면: 틱 수 단언을 상태 단언으로(끝나는 조건이 서는지를 묻는다 · 상한은 표에만 · T355 규율) · 씨 고정 · 10/10 초록 + 미끼(끝 조건을 끄면 빨개진다) · 제품이면: 교착의 뿌리 하나(숲 지형에서 대형이 못 닿나 · 사기가 바닥에서 안 깨지나 · 궤주 문턱) · 손잡이 0 · 정본 규칙 하나 · `test-war-world` 110 + 절 · 3시드(t17 엔 전쟁 없음 — 자명 동일 한 줄). ③ **⑥ 선포 무관 동원 표** — 지금 `war-core` 가 선포 없이 소집·행군을 허락하는 자리(코드 줄) · 캐논(명분 → 선포 → 대치 → 결단)에서 기습이 어디에 서나 · 어부 문법으로 "선포 = 사자가 걸어간다"(전령 몸 · 도착해야 상대가 안다 — T7 소문 문법 그대로) 설계 한 줄 · 표만 · 판정 재민. 이틀. 질문 금지 — 답은 표.

★검증 규약: `sim/war-core.js`·`battle-core.js` 만지면 3시드(자명 동일이면 그 한 줄) · `test-war-world` 110 → 10판 연속 초록 · `e2e-war`(있으면) · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · `inline-engine --check` · 접점 심볼(`test-war-world|ended|M_BREAK|_opCheckSurrender|warId|Date.now|setTimeout|createWar|war-core|battle-core|_warEngage`).

## 0. §0 실측 (셋)
ⓐ 귀속 표(씨 고정 10판 · 상태 덤프 · 자/제품). ⓑ 고침 diff + 10/10 + 미끼. ⓒ ⑥ 표.

## 1. 절대 규칙
새 수 0 · 손잡이 0 · 자를 고치면 자명 통과 반례 · **T388 구현(#68 뒤)·T400(세션4)·T410(세션1) 무접촉** · ★푸시 2판(자기 기기 · 가지 `batch/war-flaky-0926` · main 은 PM) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#68 세 길(재민) · 민병 값(재민) · 기습 규칙(재민 · ③ 표) · 손에 드는 2층(PM) · 군량 ⓐ 구현(PM · #68 뒤).

## 5. 완료
커밋 [승인 대기 · T413] · 가지 푸시 · `보고/T413_<날짜>.md`(템플릿 네 칸) · `인계/X-전쟁.md` X-8 · 채팅 "가지 올렸다 <해시> — ⓛ = 자/제품 · 씨 … 10/10 · 뿌리 … · ⑥ 표". 실기 0.
