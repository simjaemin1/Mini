# [Sonnet] 지시 — T306: 자산·문서·확인 첫 카드 — BGM 13곡을 한 자(ffmpeg)로 다시 재서 `render-meta.json` 에 `measured` 열 · 인계 문서의 "옛 기준선" 흔적 표 · `_bundles_pushed/_MANIFEST.txt` ↔ main 대조 · 보고 머리 15줄 규약 점검 (세션11 · 새 세션 · 2026-09-19)

아래 전문을 **세션11** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T306.md` · 레포 `지시/지시_T306.md`) **이 카드는 `[Sonnet]` 카드다 — 판정 0 · 새 코드 0 · 정본(`sim/`·`server/`·`public/client`) 무접촉 · 답은 표와 파일.**

---

**먼저 읽는다(부분만 · 전문 금지):** `인계/공통.md` §0(`sed -n 1,130p`) · §2 토큰 규약 · `인계/족보.md` `tail -n 12`. 너는 **세션11 = 자산·문서·확인** 자리다(공통 §0 모델 규약). 하는 일은 **재기·대조·정정** — 판단이 필요한 것은 판단하지 말고 표에 "PM" 이라 적는다. 질문 금지 — 답은 표.

**카드 T306 — 넷.** ① **BGM 13곡 한 자 재기** — `public/assets/audio/bgm/*.ogg` 13곡을 **맥 VM**(`device_bash` · ffmpeg 있음)에서 같은 명령(`ffmpeg -i x.ogg -af astats,volumedetect -f null -` · 명령 전문을 보고에)으로 재서 `seconds`·`rms_db`·`peak_db` 를 **`render-meta.json` 에 새 열 `measured`** 로 넣는다(기존 합성기 값은 **안 지운다** · 열이 둘이면 자가 둘이라는 T257 지적이 그대로 보이게 · `village_day_jeongak` 빈 세 칸도 `measured` 에만 채운다 · T292 가 137.00 이라 잰 `seconds` 와 맞는지 표). `_T306` 메모 한 줄: "합성기 값 ↔ ogg 실측 · 자가 둘 · 정본은 실측(PM)". ② **"옛 기준선" 흔적 표** — `인계/*.md`·`설계/*.md`·`지시/README.md` 에서 셋째 판 수(`6,889`·`7,148`·`6,259`)가 **"옛"·"셋째 판" 표식 없이** 기준선처럼 적힌 줄을 `grep -n` 으로 전부 찾아 표(파일:줄 · 문장 앞 60자) — **고치지 않는다**(공통 §2 기준선 절이 정본 · 넷째 판 8,318/7,935/7,853 · 소멸 0·0·0 · 고칠지는 PM). ③ **번들 장부 대조** — `~/Mini/_push/_bundles_pushed/_MANIFEST.txt` 와 `~/Mini/_bundles_pushed/_MANIFEST.txt`(둘 다 있으면 둘 다)의 해시마다 `git merge-base --is-ancestor <해시> origin/main` 을 돌려 **main 에 든 것/안 든 것** 표 · `~/Mini/_inbox/`·`~/Mini/*.bundle` 에 남은 번들 목록(있으면 이름·크기·`git bundle list-heads`) — 밀지 않는다(PM). ④ **보고 머리 규약 점검** — `보고/T29*.md`·`보고/T30*.md`·`보고/야간러너_*.md` 마다 머리 15줄(`head -n 15`)에 "한 줄 답/결론" 이 있는지 · 착지 해시가 있는지 · 그 해시가 origin/main 조상인지 표(있음/없음/해시 미상). ⑤ 한 줄 답("13곡 재측 · 자 둘 차이 최대 …s/…dB · 옛 기준선 흔적 n줄 · 번들 main 밖 n · 보고 머리 규약 위반 n").

★검증 규약: **코드 0**(`.json` 한 파일 + `보고/` 한 파일만 · `git diff --stat origin/main` 을 보고에) · `node scripts/test-assets-audit.js` 초록 그대로(있으면 · `render-meta.json` 스키마가 새 열을 받는지 — 빨강이면 열을 넣지 말고 표에만) · 재기 두 번 같은 값(결정성) · 읽기는 `head`·`grep`·`sed -n` 만.

## 0. §0 실측 (다섯)
ⓐ 13곡 표(합성기 값 · 실측 · 차). ⓑ 옛 기준선 흔적 표. ⓒ 번들 장부 표. ⓓ 보고 머리 표. ⓔ 한 줄 답.

## 1. 절대 규칙
판정 0 · 새 코드 0 · `sim/`·`server/`·`public/client`·`scripts/` 무접촉 · 문서 정정 0(표만 · 고치는 카드는 따로) · 통로 클론 `~/mnt/Mini/durango-mini` 무접촉 · 배포 0 · ★푸시 2판(맥 VM `device_bash` · `$HOME/pushrepo` 얕은 클론 · 가지 `batch/doc-audit-0919` **가지만** · main 은 PM · 403 이면 `~/Mini/_inbox/` 번들) · force 0 · `.lock` 0 · PAT 출력 금지(`M` 마스크).

## 2. 작업 (다섯)
① 13곡 재기 ② 옛 기준선 grep ③ 번들 대조 ④ 보고 머리 ⑤ 한 줄 답.

## 3. 검증
diff 파일 둘 · `test-assets-audit` · 재기 두 번 동일.

## 4. 회부만
실측 열을 정본으로 삼을지(PM) · 옛 기준선 줄 고치기(PM) · main 밖 번들 처리(PM).

## 5. 완료
커밋 [승인 대기 · T306] · 가지 푸시 · `보고/T306_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 한 줄 답**) · 채팅 "가지 올렸다 <해시> — 13곡 … · 흔적 n · 번들 n · 머리 n". 실기 0.
