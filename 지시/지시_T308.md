# 구현 지시 — T308: 화소 해시 자 정규화 — 완전투명 아래 RGB 를 0 으로 맞춰 두 자(PNG·webp)가 압축기에 둔해지게 · 전수 재잠금 557 · 반례 하네스(무손실 재압축 effort 0/4/6 = 같은 해시 · 보이는 화소 하나 = 다른 해시) (세션8/ART · T303 뒤 · 2026-09-19)

아래 전문을 **세션8** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T308.md` · 레포 `지시/지시_T308.md`)

---

T303(main `dfb2fee6` · `wolf_howl` 개명 바이트 0 · 비움 셋 · `.dockerignore` 0.28% · webp 88 화소 해시 `@cwasm/webp`↔sharp 88/88 · **그리고 자가 투명 아래 RGB 에 둔하지 않다는 것을 스스로 잰 것**) **통과** — "표로 배선한 값은 개명 비용이 0" 이 `48-a-audio.js` 0줄로 증명된 것, `test-audio ⓐ-2` 가 철자를 재던 것을 배선을 재게 고친 것 다 인정. #30 ⓒ · #32 ⓐ 닫힘.

**카드 T308 — T303 회부 ①. PM 판정**(재민 거부권): 잠금의 뜻은 "**보이는 그림**이 바뀌었나" 다 — 안 보이는 화소(완전투명 아래 RGB)는 인코더 몫이라 자가 거기 반응하면 재압축마다 거짓 빨강이 난다. ① **정규화** — `scripts/asset-lock.js` 의 화소 해시 한 자리에서 `a === 0` 인 화소의 RGB 를 0 으로 놓고 잰다(PNG·webp **같은 함수** · 사본 0 · 새 수 0 · 반투명 `0 < a < 255` 는 **안 만진다** — 그건 보인다). ② **전수 재잠금** — `icons.lock.json` 557장(PNG 452 + webp 88 + 나머지) 값 갱신 · 파일 바이트 **0 변경**(sha1 전수 표로 증명 — 잠금 값만 움직였다) · T303 표 "PNG 404/557 이 움직인다" 와 실제 움직인 수 대조. ③ **반례 하네스** — `test-assets-audit` 에 절 둘: ⓐ 표본 넷(PNG 둘 · webp 둘 · 투명 있는 것)을 무손실 재압축(sharp `effort` 0/4/6 · webp `lossless`)해 **새 자로 셋 다 같은 해시**(옛 자로는 달랐다 — T303 §0-ⓓ 그대로 재현해 둘 다 표에) ⓑ 보이는 화소 **하나**(`a>0`)의 RGB 를 1 바꾸면 **다른 해시**(자가 둔해진 게 아니라 안 보이는 데만 둔하다는 증거) ⓒ 투명 아래 RGB 만 바꾸면 **같은 해시**. ④ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: `asset-lock --check` 557 · 0 · `test-assets-audit` 기존 절 초록 + ⓐⓑⓒ · **파일 바이트 0 변경**(`git diff --stat` 에 `.png`·`.webp` 0 · sha1 표) · `npm ci` 시간 무변(새 패키지 0 — sharp 는 이미 devDep 이면 그대로 · 아니면 `@cwasm/webp` 만으로 ⓐ 를 짠다 — 재압축은 하네스 픽스처를 **미리 구워 저장소에 넣지 말고** 런타임에 굽는다 · 못 구우면 그 절은 "sharp 없음 · 건너뜀" 으로 표) · 접점 심볼(`asset-lock|pixelHash|icons.lock.json|test-assets-audit|@cwasm/webp|sharp`).

## 0. §0 실측 (넷)
ⓐ 정규화 diff(한 자리). ⓑ 재잠금 표(움직인 수 · 바이트 sha1 무변). ⓒ 반례 하네스 셋. ⓓ 한 줄 답.

## 1. 절대 규칙
자산 바이트 0 변경 · 지우기 0 · 새 수 0 · 사본 0(해시 함수 하나) · `public/client`·`server`·`sim` 무접촉 · 세션9(SOUND · T305) 의 `manifest.json`·`test-audio` 무접촉 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/pixel-norm-0919` → main ff · 통로 클론 무접촉 · 403 이면 `~/Mini/_inbox/` 번들) · force 0.

## 2. 작업 (넷)
① 정규화 ② 재잠금 ③ 반례 셋 ④ 한 줄 답.

## 3. 검증
`asset-lock --check` · `test-assets-audit` · sha1 전수 · 접점 목록.

## 4. 회부만
`harvest` 직접 녹음 · #29 6장 · 실기표 26항(재민) · `render-meta.json` `measured` 열은 **세션11 T306** — 무접촉.

## 5. 완료
커밋 [승인 대기 · T308] · 가지 푸시 · `보고/T308_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · `인계/ART-자산.md` 갱신 · 채팅 "가지 올렸다 <해시> — 정규화 … · 재잠금 n장 · 반례 3/3". 실기 0.
