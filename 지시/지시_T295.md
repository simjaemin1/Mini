# 구현 지시 — T295: 동원의 대가가 세계에 닿는다 — 징발자는 생산에서 빠진다(pid ↔ econ npc 결속) · 군량은 규모 비례로 싣고 부족하면 사기가 떨어지고 남으면 곳간으로 돌아온다 · 곳간 품목을 섭식 정본 순서로 · `_laborMul` 죽은 칸 정리 (세션10/WAR · T284 뒤 · 2026-09-18)

아래 전문을 **세션10** 에 붙여넣으면 된다. (지시서 파일: 레포 `지시/지시_T295.md` — 시작 전 `git pull --ff-only origin main`)

---

T284(main `af77b49b` 머지 · 재민 09-18 · 코드 `1fac6e13` · 보고 `보고/T284_2026-09-16.md`) **통과** — 미러·로컬 전장·가짜 건물·판·강제결판·headless·서브루프를 다 걷어내고 병사가 존 좌표의 NPC 로 존 틱 안에서 싸우게 한 것, 전이 표(전진/교전/대치/정산이 행위로)와 n 의 유도식을 값 후보 셋과 같이 적은 것, 성능을 세계 부하 위(50마을 · 2,677명)에서 팔 열 개로 재고 **첫 판 max 스파이크가 제 카드의 결함(교전마다 24만 행 색인 · ~300ms)이었음을 스스로 잡아 1.5ms 로 내린 것**, 실서버 첫 판의 "곧은 선 전진이 산맥에 걸려 대치⇄출격 무한" 을 행군로로 푼 것, 관측자 켬/끔 세 시드 한 글자 동일 + 씨 다르면 다름(대조), `test-war-world` 30/0(PM 재실측 in-memory 25/0 · 서버 절은 재민 착지로 대신), 3시드 800일 바이트 동일(PM 재실측: 시드 1020·7 `t17-metrics` JSON `cmp` 동일), 그리고 **회부 ①~⑦ — 동원의 대가가 세계에 안 닿는다는 감사**(`인계/회부.md` WAR 절) — 전부 인정. 족보 (228)(229)로 올린다. 푸시 403 → 번들 → 재민 손 머지는 규약대로였다(공통 §0 에 그 길을 적었다). ⚠세션7 에 갔던 T284 카드는 회수됐다 — 전쟁 영역은 세션10 이다.

**카드 T295 — 회부 ①~④(+②의 정리). 재민 09-17 대화가 캐논이다: "일을 안 하니 자연히 동결" · "규모 비례 적재 → 부족하면 사기↓ → 복귀 시 잔량 곳간 복귀(끝난 방식과 무관)". ⑤ 무기 재고 · ⑥ 선포 무관 동원 · `FOLLOW_CAP` · `WAR_OPS=0` 손잡이는 이 카드에 안 넣는다(다음).** **PM 판정**(재민 거부권): ① **징발자는 생산에서 빠진다** — 걷는 병사(`villages._warDraftPids`)와 경제 인구(`econ.npcs` · `war-core.conscript`)를 **1:1 로 결속**한다(어느 쪽이 정본인지 먼저 표로: pid 가 뽑히면 그 npc 가 징발자 · 반대 아님) · 결속된 npc 는 `tickWorldV2` 의 생산 계산에서 **그 기간 제외**(직업 자리 비움 · 돌아오면 복귀 · 죽으면 econ 사망 정본 경로로 — 사본 0) · 이것이 ②의 `_laborMul` 감산을 **대체**하므로 `_laborMul` 은 죽은 칸이 된다 → `war-core _recomputeLabor` · `villages.js:5763` 의 쓰기와 economy-sim-v2 의 "미소비" 주석을 **지운다**(부상 노동력이 `_laborMul` 에 얹혀 있으면 그건 남기고 표에 — 지우지 말고 회부). ② **군량은 규모 비례로 싣는다** — `warMobilize` 행군분 + `_opPackLoad` 공성 팩을 **한 적재**로(병력 × 일수 × `WAR_RATION` — 기존 상수 · 새 수 0) · **부족하면 사기↓**: battle-core `mrl` 목표치에 군량 항 하나(수식 추가 = 손잡이 `T295_RATION_MRL` 기본 **끔** · 켬은 랩 A/B 표만 · 값은 재민) · **복귀 시 잔량은 곳간으로**(궤주·항복·철수·무혈 **어느 길이든** 같은 함수 한 곳 — 지금은 철수·항복만 환급) · 죽은 병사 몫은 안 돌아온다(들고 있던 것 — 캐논 고증). ③ **곳간 품목을 본다** — 적재·환급·약탈·공납·조공이 `storage.food` 한 칸을 보던 것을 **섭식 정본의 식사 순서 함수**(economy-sim 이 마을이 먹을 때 품목을 고르는 그 함수 — 이름·자리를 표에 · **사본 0 · 같은 함수를 부른다**)로 품목별 차감·환급 · `food` 0 이어도 쌀·생선이 있으면 팩이 찬다 · 환급은 뗀 품목 그대로. ④ **부하** — 게임일 경계에서 전쟁 수 × 병력 O(n) 이 전부 · 틱마다 도는 것 0 → `war-world-perf.js` W0/W6 두 팔로 존 틱 p50 변화를 적는다(T284 표와 나란히 · 비율만). ⑤ 답 한 줄. 이틀. 질문 금지 — 답은 표.

★검증 규약: **econ 엔진을 연다**(`economy-sim-v2` 생산 계산에서 징발자 제외 · 섭식 순서 함수 호출) → **3시드(1020·7·42) 800일 51마을 여덟 수 비트 동일**(랩엔 전쟁이 없다 → 징발자 0 → 동일해야 한다 · 다르면 그 자체가 결함) · `build-econ-bundle --check` 8/8 · `inline-engine --check` · `lab-wiring-check` 0/0 · 손잡이 `T295_RATION_MRL` 끔 = battle-core 수식 비트 동일(하네스 · `_allocKnob` 문법) · **`test-war-world` 30/0 그대로** + 새 절 넷: ⓗ 징발 기간 그 npc 의 생산 기여 0 · 복귀 뒤 재개(대조: 결속 끄면 계속 생산) ⓘ 환급 합 = 적재 − 소비 − 전사자 몫(궤주·항복·철수·무혈 넷 다 · 곳간 품목별) ⓙ 품목 순서가 섭식 정본과 **같은 함수**(심볼 하나 · 정적) ⓚ `_laborMul` 쓰기 0(정적 · 부상 노동력 예외는 표) · `test-farm-life` · `test-psite-server` · `lab-modules --diff origin/main` 교집합 목록 · 접점 심볼(`_warDraftPids|conscript|econ.npcs|tickWorldV2|_laborMul|_recomputeLabor|warMobilize|_opPackLoad|_opPackRefund|WAR_RATION|storage.food|mrl|M_RATE|warResolveBattle|_warWalkoverOutcome|_opDoSurrender|T295_RATION_MRL`).

## 0. §0 실측 (다섯)
ⓐ 결속 표(어느 쪽이 정본 · 징발/복귀/사망 경로) + 생산 제외 실측(한 마을 30일 · 징발 전/중/후 곳간 증가율). ⓑ 군량 표(적재 · 소비 · 환급 · 전사자 몫 — 네 끝맺음별) + 사기 항 손잡이 끔 비트 동일 + 켬 랩 A/B 한 판(값 후보). ⓒ 품목 표(같은 함수 이름 · 순서 · `food` 0 판). ⓓ 부하(W0/W6 p50 · T284 대비). ⓔ 한 줄 답.

## 1. 절대 규칙
새 수 0(`WAR_RATION`·`M_RATE` 등 기존 상수 · 값은 재민) · 사본 0(섭식 순서 함수 · 사망 경로 · 환급 함수 한 곳) · 손잡이 기본 끔 · 켜지 않는다 · **`war-core` 를 연다 — 다만 선포·명분·항복 결단 로직은 무접촉**(군량·환급·결속 자리만) · **T288(세션5 `economy-sim-v2.js` 누계 칸 — 같은 파일 · 리베이스 때 둘 다 살린다) · T286(세션3 `lab-alloc-server` 캐시 — 무접촉) · 세션1~9 의 `villages.js` 접점(T278 `_terrGrow` · T287 `onboarding`) 무접촉** · 배포 재민만 · ★푸시: 클론 → `git pull --ff-only origin main` → 가지 `batch/war-cost-0918` **가지만** · `git push` 403 이면 `git bundle create T295.bundle origin/main..HEAD` 를 채팅에(공통 §0 ⓑ) · force 0 · main 은 재민.

## 2. 작업 (다섯)
① 결속·생산 제외·`_laborMul` 정리 ② 군량 적재·사기 항(끔)·환급 한 곳 ③ 품목 = 섭식 정본 함수 ④ 부하 두 팔 ⑤ 한 줄 답.

## 3. 검증
3시드 비트 동일 · 게이트 셋 · 손잡이 끔 비트 동일 · `test-war-world` 30 + ⓗ~ⓚ · 접점 목록.

## 4. 회부만
대치 n초 값(재민 · #40) · 사기 항 값(재민 · 랩 A/B 뒤) · ⑤ 무기 재고·화살 · ⑥ 선포 무관 동원(설계 §1-4) · `FOLLOW_CAP` 순간이동 · `WAR_OPS=0` 손잡이 제거(PM) · **`_siegeBlock`·`_siegeOutMul`·`_recomputeSiege` 제거(재민 09-18 "봉쇄 상태 없음 · 배수 없음" — 위협 함수 카드가 같이 · 이 카드에서 `_laborMul` 처럼 죽은 칸이면 지우고 산 칸이면 표에)** · 나무 콜라이더 색인(C·Z) · 발자국 통짜(설계 §6) · 설계 §2~§6 재민 칸 · VPS `/perf` 실측(재민 · 배포 뒤).

## 5. 완료
커밋 [승인 대기 · T295] · 가지 푸시 또는 번들 · `보고/T295_<날짜>.md`(ⓐ~ⓔ · **머리 15줄에 결론**) · `인계/X-전쟁.md` X-2 절 · 채팅 "가지 올렸다 <해시> — 결속 … · 환급 넷 … · 품목 함수 … · p50 … · 하네스 …/34". 실기 0.
