# 구현 지시 — T299: `twogate` 를 켠다 — T255 ⓐ(재민 09-18 위임 · PM 권고) · `L_CARGO_TWO`+`L_CARGO_TWO_GATE` 서버 기본 켬(정본 자리 하나 · 되돌림 비트 동일) · 관문은 안 얹는다(T280/T289) · 3시드 800일 여섯째 판 (세션6 · T289 뒤 · 2026-09-18)

아래 전문을 **세션6** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T299.md` · 레포 `지시/지시_T299.md`)

---

T289(`batch/lab-cargo-two-0912` bdc6f255 · 코드 0) 통과 — 소멸 1/15 은 잡음(셋이 다른 시드·다른 마을 · 넷 다 관문 팔 중앙값이 끔보다 높다) · ⓒ 상한의 읽기는 최대가 아니라 **중앙** · 어촌을 관문이 못 구하는 이유는 첫째 화물이 이미 생선을 29~39% 내보내기 때문 — 전부 인정. **#26 결정: ⓐ `twogate` · 관문은 안 얹는다**(어촌은 관문의 물음이 아니라 첫째 화물의 물음이라 다른 카드). 옛 `~/Mini/지시_T255.md` 는 이 카드로 대체한다. 먼저 `git fetch` — 베이스는 **T244 넷째 판이 앉은 main**(공통 기준선 절 갱신됨 · 같은 엔진 파일이라 순서가 이것이다).

**카드 T299 — T244 문법 그대로.** **PM 판정**(재민 거부권): ① `L_CARGO_TWO`·`L_CARGO_TWO_GATE`(twogate = 둘째 화물 + 관문 · `cargoTwoBest` 는 **끔 그대로**)를 정본 읽는 자리 하나에서 **기본 켬** · `=0` 이면 끔 · 되돌림 둘 `=0` = 넷째 판 비트 동일(3시드 열 열 `cmp`) · `villages.js` 에 안 심는다. ② **여섯째 판** 3시드 800일 `t17-metrics`(+`vpop`) → 넷째 판 대비 짝 Δ(T252 자) · 소멸 · 지니 · `<20명` · 작은 17곳/큰 17곳 — T265/T271 의 15시드 읽기(twogate 소멸 0/15 · 지니 최저)가 **넷째 판 위에서** 서나(3시드로 못 가르면 `t17-seeds.js --seeds 15` 한 판 — 세션3 러너). ③ 하네스 `test-lab-cargo`(있는 것) 를 "켠 판 = 끈 판과의 관계"로 · [H] 등록. ④ 답 한 줄. 하루. 질문 금지.

★검증 규약: 되돌림 비트 동일 · 3시드(15시드 보조) · 게이트 셋 · **T298(세션4 · 같은 자에 앉는다 — 리베이스 순서는 PM: T298 뒤에 이 카드) · T295(세션10) 무접촉** · 접점 심볼(`L_CARGO_TWO|L_CARGO_TWO_GATE|L_CARGO_TWO_BEST|cargoTwo|cargoTwoGate|cargoTwoBest|_allocKnob|t17-seeds`).

## 0. §0 실측 (넷)
ⓐ 손잡이 자리 + 되돌림. ⓑ 여섯째 판 표. ⓒ 하네스. ⓓ 한 줄 답.

## 1. 절대 규칙
새 수 0 · `twobest` 끔 그대로 · 관문 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/cargo-on-0918` **가지만** · main 은 PM) · force 0.

## 2. 작업 (넷)
① 켬 ② 여섯째 판 ③ 하네스 ④ 한 줄 답.

## 3. 검증
비트 동일 · 3시드 · 게이트 · 접점.

## 4. 회부만
첫째 화물의 생선(어촌 — 다른 카드) · `e2e-rumor` 잠든 빨강(09-18 야간 · 소문은 세션6 영역 · 세션2 T304 가 귀속만) · #12 값.

## 5. 완료
커밋 [승인 대기 · T299] · 가지 푸시 · `보고/T299_<날짜>.md`(머리 15줄) · 채팅 한 줄. 실기 0.
