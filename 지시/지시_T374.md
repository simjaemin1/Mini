# 구현 지시 — T374: 채취는 수요가 멈춘다 — 행위 층(채집 · 그리고 나무·물고기도 같은 규칙인지)이 **그날 마을이 쓸 만큼**(수식이 내는 그날의 몫 D — 이미 정본에 있는 수)까지만 따고 멈춘다 · 새 상한 수 0(ⓓ "첫날 하루 합 = 수식 · 그 뒤로는 실체가 내는 만큼" 의 반대쪽 절반 — 실체가 더 낼 수 있어도 수식보다 더 안 낸다) · 손잡이 `T374_DEMAND_STOP` 기본 끔 · 켬 판(T359 지형 군락 켬 위에서): 30일 등가 4,106% → ? · 군락 곡선 · 부하 p95 325ms → ? · 3시드 면제(`sim/` 무접촉이면) (세션1 · T359 뒤 · T368 앞 · 2026-09-23)

아래 전문을 **세션1** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T374.md` · 레포 `지시/지시_T374.md`) **⚠T368(농부)보다 먼저 — T359 를 켤 수 있게 만드는 카드다.**

---

T359(`batch/grove-terrain-0923` → main · 간격 유도 덤불 58px·풀 107px(나무 띠 60~96 아래·위) · 끔 청크 459개 해시 동일 · **생활권 안 군락 13,854 = 필요 K 의 137.7%** · 못 채운 마을 19/45 · **★켜면 30일 등가 4,106%(수식의 44배) · 군락 30일에 절반 · 군락 0 마을 20~27 · p95 3.4 → 325ms** — "밀도 다음 카드는 채취 상한이다") **통과** — "자원이 있으니 한도가 실제로 섰고 그 한도(걸음)는 MSY 보다 훨씬 위다"가 이 카드의 문장이다(족보 294). **켜지 않았다 — 맞다.**

★**상한은 새 수가 아니다.** 설계_생산_실체 ⓓ 가 "옮긴 첫날 하루 합 = 수식 · 그 뒤로는 실체가 내는 만큼(강이 멀면 덜 잡는다)" 이라 했다 — 그 문장의 반대쪽 절반이 빠져 있었다: **실체가 더 낼 수 있어도 마을은 쓸 만큼만 딴다.** 채집꾼이 숲을 한 철에 비우는 이유는 캐논이 아니라 "멈출 이유가 코드에 없어서"다. 멈출 이유는 이미 정본에 있다 — 수식이 내는 그날 마을의 채집 몫 D(T347 `_t347MixShare` · `formulaActPerDay` 그 수)다. 나무꾼·어부에게서 안 보였던 건 자원이 모자라서였다(T341 "숲에 있는 만큼만" 이 유일 상한).

**카드 T374 — 넷. PM 판정**(재민 거부권): ① **멈춤 규칙** — 채집 행위 층에 "그날 마을 목표 = D(수식 · 그 마을 · 그날)" 을 걸고, 손에 든 것 + 오늘 곳간에 넣은 것이 D 에 닿으면 **채집꾼은 그날 채집을 멈춘다**(귀가 · 그 뒤는 생활층이 다른 일을 준다 — 새 행동 0 · 지금 "낮이 끝나면 귀가" 갈래 그대로) · D 가 0 인 마을은 안 딴다 · 손잡이 `T374_DEMAND_STOP` 기본 끔 · **끔 = 비트 동일**(T359 끔 위에서 청크·실서버 · 3시드는 `sim/` 무접촉이면 면제 — 만지면 면제 없음) · 나무꾼(T341)·어부(T312)도 **같은 규칙**을 같은 자리(`actToGranary` 몸통 옆 · 사본 0)에 — 그쪽은 자원이 모자라 값이 안 변할 것이다(표로 보인다 · 등가 무변이 증거). ② **켬 판** — T359 켬 + T374 켬 두 손잡이(계측 팔) 51마을 200일(T326 자) + 30일 등가(T347 자): 등가 4,106% → ?(100% 근처가 답이다 — 첫날 등가 원칙) · 군락 곡선(20,856 → ? · 되살아난 개체 수 · 군락 0 마을 수) · 한도 0 인 날 수 · 사람당 µs · p95·drop·lag(3.414ms 로 돌아오나) · T252 자. ③ **못 채운 마을 19/45** — 간격이 중앙값 하나라서 생긴 분포 문제는 **이 카드 밖**(표만 갱신 · 지형 술어 쪼개기는 T359 회부 그대로). ④ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 끔 비트 동일(청크 해시 · 실서버 · 3시드 조건부) · `test-forage-act` 103 + 절(ⓐ D 에 닿으면 그날 0 ⓑ D=0 마을 0 ⓒ 나무·물고기 등가 무변) · `test-wood-act` 87 · `test-fish-act` 73 · `test-trees` 91 · `lab-wiring-check` 실패 0 · 접점 심볼(`_t347MixShare|formulaActPerDay|forageToGranary|actToGranary|_t341TripsPerDay|T347_FORAGE_ACT|T359_GROVE_TERRAIN|T374_DEMAND_STOP|t347-forage-day|t359-grove-density`).

## 1. 절대 규칙
새 수 0(D 는 정본의 그 수) · 새 행동 0 · 손잡이 기본 끔 · 켜지 않는다 · **T370(세션3 `zone.js`)·T372(세션8 `chunk.js` 종 정의 — 리베이스 때 둘 다)·T364(세션10) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/demand-stop-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
T359·T374 켜기(재민 · ② 표) · 분포(지형 술어 쪼개기 · PM) · 숲 가장자리 띠(둘째 문턱 = 새 수 · 재민) · `berry→fruit`(PM) · 링 군락 접기(재민) · T368 농부(이 뒤).

## 5. 완료
커밋 [승인 대기 · T374] · 가지 푸시 · `보고/T374_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 등가 전/후**) · `인계/C-채집자연물.md` 갱신 · 채팅 "가지 올렸다 <해시> — 등가 4,106% → …% · 군락 … · p95 … · 나무·물고기 무변". 실기 0.
