# 구현 지시 — T364: 지나가는 군대를 옆 마을이 본다 — T329 의 T 는 **목표 마을**에만 걸리는데 경보 거리(`WAR_ALERT_R`) 안에 있는 **모든** 마을이 그 군대를 봐야 맞다(T329 회부 ②) · T_마을 = 경보 거리 안 모든 군대의 max(목표 여부 무관 · 같은 세 항 · 새 수 0) · 행군로 A\* 가 숲을 안 본다(T329 회부 ③ · T295 후속이 나무를 존의 것으로 만들었다 — 행군로도 같은 술어를 주입) · w 셋은 고름 유지(#59 ★PM 결정) · 3시드 비트 동일 · `test-war-world` 85 + 절 (세션10/WAR · T329 뒤 · 2026-09-23)

아래 전문을 **세션10** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T364.md` · 레포 `지시/지시_T364.md`)

---

T329(`batch/war-threat-0919` → main · 봉쇄 상태·배수 0 → 위협 T 하나 · R_out = 생활권×(1−T) · `WAR_OPS` 제거 · 몸값 품목 · 나무 세 줄+메모(T295 후속) · 3시드 비트 동일(PM 재실측 열 열) · `test-war-world` 85/0) **통과** — "봉쇄는 상태가 아니라 반경"이 이 카드의 문장이다(족보 285). ★PM 결정 #59(위임): **w 셋 고름 `1,1,1` 유지** — 유도 근거 없는 가중은 새 수다 · 실측에서 어느 항이 세계를 흔드는지 나오면 그때. ★착지 때 PM 이 `villages.js` 에서 T347(채집)·T350(주사위)과 합쳤다 — `_jobSites` 에 `tmul` 과 `t325Trees` 둘 다 있다 · 세션10 은 `git pull` 뒤 그 꼴에서 시작.

**카드 T364 — 셋. PM 판정**(재민 거부권): ① **옆 마을이 본다** — `_threatOfArmy(w, vil, now)` 를 목표 마을만이 아니라 **경보 거리 안 모든 마을**에 건다(`_warAfterDaily`·`tickWarBodies` 갱신 자리 그대로 · 부하: 군대 × 경보 거리 안 마을 수 — 실측 표 · 마을 51 전수 스캔이 아니라 거리 술어로 걸러) · 세 항 그대로 · `odds` 는 **그 마을의** 방어 승산(`_opDefOdds(그 마을, 병력)`) · 표: 행군 한 판(랩 A/B · T329 §0-ⓑ 자)에서 목표 마을 외에 T > 0 이 된 마을 수·값·R_out · 지나간 뒤 T 가 0 으로 돌아오나(잔류 0). ② **행군로가 숲을 본다** — 행군 A\*(`war-live.js` 경로) 가 T295 후속의 나무 술어(존이 주입 · `treeBlockerAt` 계열)를 안 받는다 → 같은 술어를 **주입**(사본 0 · 새 코스트 0 — 나무는 벽과 같은 "막힘"이지 배수가 아니다 · 숲 우회 길이 표: 세 행군로 전/후 셀 수) · 병사 콜라이더는 T295 후속 그대로. ③ 한 줄 답. 이틀. 질문 금지 — 답은 표.

★검증 규약: 3시드 800일 열 열 비트 동일(`sim/war-core.js` 만지면 면제 없음 · `war-core` 는 주입 rng 만 · `Math.random` 0 · 린트 ⑫) · `test-war-world` 85 + 절 둘(ⓐ 옆 마을 T > 0 · 지나간 뒤 0 ⓑ 숲 우회 길이 > 직선 · 나무 술어 없으면 옛 길) · `war-world-perf` W6 p50(T330 4.19ms 와 나란히) · `lab-wiring-check` 실패 0 · 접점 심볼(`_threatOfArmy|_warAfterDaily|tickWarBodies|WAR_ALERT_R|_opDefOdds|_warOutMul|treeBlockerAt|war-live|_searchStep|T329_THREAT_W`).

## 0. §0 실측 (셋)
ⓐ 옆 마을 T 표 + 부하 + 잔류 0. ⓑ 행군로 전/후 셀 수 표. ⓒ 한 줄 답.

## 1. 절대 규칙
새 수 0 · w 고름 · 배수 0 · 상태 스위치 0 · `Math.random` 0 · **T356(세션3 `zone.js` 걸음)·T359(세션1)·T361(세션4 표) 무접촉** · 배포 재민만 · ★푸시 2판(그 계정 권한 · 가지 `batch/war-neighbor-0923` · 403 이면 번들 채팅 → PM 이 가지 푸시) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
군량 = 행위(설계 회부 · 어부 문법 · PM) · p95(값 판정 · 재민) · w 가중(실측이 항을 가르면 · 재민).

## 5. 완료
커밋 [승인 대기 · T364] · 가지 푸시 · `보고/T364_<날짜>.md`(ⓐ~ⓒ · **머리 15줄에 결론**) · `인계/X-전쟁.md` 갱신 · 채팅 "가지 올렸다 <해시> — 옆 마을 T n곳 · 잔류 0 · 우회 +n셀 · 3시드 동일". 실기 0.
