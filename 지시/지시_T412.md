# 구현 지시 — T412: 도구/작업 갈래 57 중 **이웃 키가 있는 자리**(판정 불필요 — 있는 키의 이웃) 확보 — `hook`·`bite`·`cast`·`harvest`·`axe`·`rock_hit`·`fire` 이웃: 건물 짓기·부수기(`building_added/damaged/removed` · `hut_site`·`wall`·`fence`·`door`·`stair`), 화덕·가마·숯가마(`furnace`·`kiln_site`·`charcoal_kiln` — `fire` 이웃), 제작 줄(`craft_queue`·`tools`·`equipment`), 궤·줍기(`chest`·`pickup`·`ground_item_removed` — `drop` 이웃), 심기·묘목·그루터기(`sapling`·`plant_menu`·`stump` — `harvest`·`axe` 이웃), 보존(`preserve`) — 자리 표 먼저(붙는 자리 / 상태 동기화라 안 붙는 자리 — T387 ⓐⓑ 문법) · 붙는 자리만 확보 규약 T354 · 후보 1~2 · 소리판 · `48-a-audio.js` 만 (세션9/SOUND · T402 뒤 · 2026-09-26)

아래 전문을 **세션9** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T412.md` · 레포 `지시/지시_T412.md`)

---

T402(`batch/sfx-why-0926` → main · `why` = `damagePlayer` 출처 앞 낱말 일곱(카드의 여섯 + `wild`) · `hit_body` 운다 · 극단 0 · `player_down_state` 네 낱말(4992 는 재접속이라 `relogin`) · 서버 여섯 줄(`HP_PEER` 한 줄 — 안 넣으면 다침이 방송에서 빠진다) · `e2e-pid-collide` 13/0 · `test-audio` 160) **통과** — "카드가 여섯이라 한 낱말은 일곱이었고, 넷이라 한 자리 중 하나는 쓰러짐이 아니었다"가 이 카드의 문장이다(족보 328). #60(우선순위)은 재민 몫이지만 **이웃 키가 있는 자리**는 판정 없이 채울 수 있다(T397 ⑤ 문법) — 도구/작업 57 이 그 갈래다(이웃 7종).

**카드 T412 — 셋. PM 판정**(재민 거부권): ① **자리 표 57** — 자리마다: 메시지/낱말이 무엇을 말하나(코드 줄) · ⓐ 사건(붙는다: 짓기 단계 오름 · 부숨 · 불 붙음/꺼짐 · 제작 완료 · 궤 열기/닫기 · 줍기 · 심기 · 그루터기 캐기 · 보존 시작) ⓑ 상태 동기화(안 붙는다: `*_spawn`·`rooms_update`·`craft_queue` 갱신 · `chest_state` · `claim_*` · `floor_changed` 는 걷기 갈래 T397 것) ⓒ 애매(표만) · 이웃 키 이름(어느 있는 키 옆인가). ② **확보** — ⓐ 자리만 · 키 이름은 자리 표가 정한다(`build_hit`·`build_done`·`fire_lit`·`craft_done`·`chest_open`·`plant`·`stump_pull` 같은 꼴) · 확보 규약 T354·T358(파일 페이지 라이선스 · CC0/CC-BY 표기 · BBC·Macaulay ✗ · `_sfx_in/` · ogg+m4a · `sources`·CREDITS §2-b · 헤드룸 `_실측` · 루프면 이음새 자) · 키마다 후보 1~2 나란히(1번만 배선) · 없으면 "없음 · 녹음 회부" · 변주는 `files:[…]` 씨 해시 · 훅은 `48-a-audio.js` 만(서버·`30-n-net.js` 무접촉 · 메시지는 이미 있다) · 거리 감쇠는 있는 규칙. ③ 한 줄 답 + 빠진 소리 전수 갱신(170 · 있음 40 → n). 사흘. 질문 금지 — 답은 표.

★검증 규약: 출처 없는 소리 0 · `bgm/` 무접촉(세션8 T411) · 서버·`30-n-net.js` 무접촉 · 기존 42키 비트 동일 · `test-audio` 160 + 새 키 절 · `test-assets-audit` PASS · `e2e-audio-probe` 61 + n · `e2e-aim` 15 · `e2e-pid-collide` 13 · `test-client-globals` 26 · `test-harness-lint` 61 · 소리판 행 수 = 키·파일 수 · 헤드룸 최악 조합 재측정(T397 ⑧h) · 접점 심볼(`manifest.json|sources|files|building_added|building_damaged|furnace|kiln|charcoal_kiln|craft_queue|chest|pickup|sapling|stump|preserve|48-a-audio|sfx-board|CREDITS|_sfx_in|sfx-census`).

## 0. §0 실측 (셋)
ⓐ 자리 표 57(ⓐ/ⓑ/ⓒ · 이웃). ⓑ 확보 표(키 · 파일 · 출처 · 라이선스 · 구간 · 헤드룸 · 후보 · 없음 행). ⓒ 한 줄 답 + 전수 갱신.

## 1. 절대 규칙
출처 없는 소리 0 · 상태 동기화에 소리 안 붙인다 · 서버 0 · `Math.random` 0 · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/sfx-work-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
#60(재민 · 배포 뒤 소리판 한 번에) · 동물 40 · UI/알림 21(재민 #60 우선순위) · 녹음 회부 행(재민 폰) · CC-BY 표시(#14ⓒ) · 천둥 사건(PM · K 층).

## 5. 완료
커밋 [승인 대기 · T412] · 가지 푸시 · `보고/T412_<날짜>.md`(템플릿 네 칸) · `인계/SOUND-소리.md` 갱신 · 채팅 "가지 올렸다 <해시> — 붙는 자리 n/57 · 확보 n(후보 n) · 없음 n · 전수 있음 40 → n". 실기: 재민 배포 뒤 소리판.
