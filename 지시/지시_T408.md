# 구현 지시 — T408: 존 경계 접합 — 재민 09-26: "닛폰과 새벌(한반도) 사이에 강·숲·호수가 걸쳐 있을 때 **어색하게 잘려 있는 곳**이 있다" → 지형 정본은 세계 좌표(`hanbando-terrain.json` 의 `pos` 는 월드 px)인데 피처가 **존 목록 하나에만** 들어 있으면 이웃 존은 그 피처를 모른다 — 경계(한반도 x=48,000 동쪽 = 닛폰 · x=41,000 서쪽 = 중원북 · 북·남도)에서 잘린 피처 **전수**(강·호수·능선·숲·골짜기·군락 · 경계 셀에 닿는 것 · 이웃 존에 짝이 있나 · 폭·색·나무 간격이 경계에서 뛰나) · 표 + 그림(경계 띠 확대) · **고침은 정본 규칙 하나**: 경계에 걸친 피처는 **두 존 목록에 같은 객체**(사본 아닌 참조 — 빌더가 세계 좌표로 자르지 않게) 또는 빌더가 이웃 존 피처를 경계 띠만큼 읽는다 — 어느 쪽이 정본인지 표 · 세계 무변 게이트(한반도 안쪽 청크 해시 동일 · 경계 청크만 바뀐다) · 이것이 **중원북(T409) 앞**이다 — 서쪽 경계도 같은 병이 날 자리 (세션5 · T406 뒤 · 2026-09-26)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T408.md` · 레포 `지시/지시_T408.md`) **⚠T406 끝난 뒤.**

---

T406(진행 · 능선 색인 켬)이 끝나면 이 카드다. 재민이 다음 존을 **중원북(`jungwon_n` · 하란 북부 · 한반도 바로 서쪽 x 31,000~41,000 · 같은 y 5,000~18,000)** 으로 정했다(#65 닫힘 · `europa` 아님). 그 전에 재민이 본 것: 닛폰(x 48,000~53,000)과 한반도 사이 경계에서 강·숲·호수가 **어색하게 잘려 있다**. 중원북은 한반도 서쪽 경계 10,000×13,000 을 통째로 맞대니 같은 병이 두 배로 난다 — **경계 규칙을 먼저 세운다.**

지형 정본(코드 그대로): `hanbando-terrain.json` 은 존 이름별 목록(`hanbando`·`nippon`·`jungwon_n`…)이고 `pos` 는 **월드 px**(예: 한여울강 `[41600, 38400]`). 존 하나는 자기 목록만 읽는다(`build-terrain-v3.js` · `isBoundaryRiver` 는 경계 400px 안 강의 굴곡 진폭만 줄인다 — **이웃 존 피처를 읽는 자리는 없다**). 그래서 강이 한반도 목록에만 있고 경계를 넘으면 닛폰 쪽엔 강이 없다 — 물이 벽에서 끝난다. 숲 격자(`getForestMultiplier` 타원)·호수·능선도 같다.

**카드 T408 — 넷. PM 판정**(재민 거부권): ① **전수 표** — 한반도 네 경계(동 닛폰 · 서 중원북 · 북 · 남 — 이웃이 바다면 바다도 적는다) + 닛폰 네 경계: 경계 셀 띠(± 1 청크 = 1,024px)에 닿는 피처 전부(강 path 점 · 호수 다각형 · 능선 선분 · 숲 타원 · 골짜기 · 군락 · 광맥) · 이웃 존 목록에 **같은 피처의 짝**이 있나(이름 · 끝점 거리) · 경계에서 뛰는 값(강 폭 · 숲 배수 · 지면 색 `groundColor` 두 존이 다르다 — 한반도 `#9a9670` · 중원북 `#9aa860` · 닛폰?) · **그림**: 경계 띠 2,000px 폭 확대 셋(동·서 · 문제 자리) `~/Mini/산그림/T408_경계_*.png` · 재민이 말한 "어색한 자리" 를 그림에서 이름 붙인다. ② **정본 규칙 하나** — 후보 둘: ⓐ 빌더가 **이웃 존 피처를 경계 띠(± n 청크 · n 은 가장 넓은 강 폭에서 유도)만큼 읽는다**(정본은 한 목록 · 사본 0 · 존이 커지면 자동) ⓑ 경계 피처를 두 목록에 넣는다(사본 — 캐논 위반 · 기각 근거 한 줄) · ⓐ 로 간다(PM 권고 · 표에서 뒤집히면 그 근거) · `build-terrain-v3`·`terrain.js`(`getForestMultiplier`·`isWaterCellLocal` 이 존 목록을 읽는 자리)·`chunk.js` 어디가 읽는지 표 · 지면 색 경계는 **띠 안에서 섞는다**(있는 tint 문법 · 새 수 0 · 안 되면 표만). ③ **게이트** — 한반도·닛폰 **경계 밖 청크 해시 비트 동일**(T406 자 그대로 · 바뀌는 청크는 경계 띠 안 것만 · 목록으로) · 3시드 한반도(econ 은 지형 셀을 안 읽으면 면제 근거 · 읽으면 돌린다) · `test-terraincache` 26 · `test-nippon-boot` 32 · 그림 전/후. ④ 한 줄 답 + **중원북 경계 예고 표**(서쪽 경계에 걸친 한반도 피처 n · 중원북 목록의 짝 n — T409 의 입력). 사흘. 질문 금지 — 답은 표.

★검증 규약: 경계 밖 세계 무변(해시) · 새 수 0(띠 폭은 강 폭에서) · 사본 0 · `sim/` 무접촉 · `test-terraincache` 26 · `test-resource-index` 20 · `test-trees` 91 · `test-nippon-boot` 32 · `e2e-terrain` · `test-far-trees` 19 · `test-harness-lint` 61 · `lab-wiring-check` 실패 0 · 접점 심볼(`hanbando-terrain.json|build-terrain-v3|isBoundaryRiver|getForestMultiplier|isWaterCellLocal|worldOffsetX|zoneWidth|findZoneAt|groundColor|tintColor|test-nippon-boot|audit-terrain-quality|generateChunkResources`).

## 0. §0 실측 (넷)
ⓐ 전수 표(경계 여덟 · 피처 · 짝 · 뛰는 값) + 그림 셋. ⓑ 규칙 ⓐ diff + 읽는 자리 표. ⓒ 게이트(경계 밖 해시 · 바뀐 청크 목록 · 전/후 그림). ⓓ 한 줄 답 + 중원북 예고 표.

## 1. 절대 규칙
경계 밖 무변 · 사본 0 · 새 수 0 · 닛폰 정본 값 무변(피처를 지우거나 옮기지 않는다 — 잇기만) · **T407(세션6 자)·T398(세션2)·T400(세션4) 무접촉** · 배포 재민만 · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/zone-seam-0926` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 2. 작업 (넷)
① 전수·그림 ② 규칙 하나 ③ 게이트 ④ 한 줄 답 + 예고.

## 3. 검증
해시 · 하네스 여덟 · 그림 전/후 · 접점 목록.

## 4. 회부만
재민 실기(배포 뒤 닛폰 경계 걸어 보기) · 지면 색 경계 값(재민 · 그림) · **T409 중원북**(PM · 이 카드 뒤 · T348 순서: 지형 → 다리(45쌍 끊김) → 광맥 → 마을 → `test-jungwon-boot`) · 닛폰 수동 지형(재민 · T407 그림 뒤).

## 5. 완료
커밋 [승인 대기 · T408] · 가지 푸시 · `보고/T408_<날짜>.md`(ⓐ~ⓓ · **머리 15줄 네 칸 전부: 한 줄 답 · 가지 해시 · `[승인 대기 · T408]` · 베이스 해시**) · `인계/Z-존서버.md`·`W-월드질의.md` 갱신 · 채팅 "가지 올렸다 <해시> — 경계 걸친 피처 n(짝 없음 n) · 규칙 ⓐ · 바뀐 청크 n(경계 밖 0) · 중원북 예고 n". 실기: 재민 배포 뒤 경계.
