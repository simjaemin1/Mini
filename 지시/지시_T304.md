# 구현 지시 — T304: 야간 09-18 빨강 둘의 자 — `e2e-charsprite` 대조군(무장착 화소 1857~2030 ≤ 8) 잡음 바닥부터(족보 80) · `e2e-rumor` 잠든 빨강(⑦d2 부재 일수 · 존 입장 경로) 귀속만 · `nightly-known.txt` 갱신 (세션2 · T293 뒤 · 2026-09-18)

아래 전문을 **세션2** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T304.md` · 레포 `지시/지시_T304.md`)

---

T293(main · `nightly-run.sh`·`nightly-report.js`·`nightly-known.txt` · CI 게이트 셋) 통과 — 09-18 야간이 이 기계로 **묶음 A 를 처음 완주**했다(115종 3h24m · 108/7). T276(상시 흔들림 셋 · npcsprite ②′·charsprite ⑤ 자 결함) 통과. **그런데 09-18 야간에 `e2e-charsprite` 가 다시 빨강이고 이번엔 첫 줄 대조군이 깨졌다**(무장착 화소 1857~2030px ≤ 8 · 잡음 바닥이 임계값의 230배) · `e2e-rumor` ⑦d2 부재 일수가 두 판 다 어긋난다(보고 4일 vs 실제 32일 · 6 vs 23 — 판마다 달라 경주) · 코드는 09-15 이후 안 움직였다(회귀일 수 없다).

**카드 T304.** **PM 판정**(재민 거부권): ① `e2e-charsprite` — 대조군 "무장착 = 화소 ≤ 8" 이 **무엇을 재는지**부터: 같은 빌드 두 판 A/A 로 잡음 바닥(족보 80) · 1857~2030 이 나오는 경로(옷 스프라이트가 무장착에도 그려지는가 · T276 뒤 무엇이 바뀌었나 `git log -- 42-r2-char.js 41-h-char.js`) → 자 결함이면 대조군을 **비율**로(`(A vs B)/(A vs A')`) · 제품 결함이면 회부(귀속 카드 번호). ② `e2e-rumor` — **귀속만**: ⑦d2 실패 문구 "입장 직후 재접속이 기준일을 지운다" 가 문는 자리(`30-n-net.js` 재접속 · 09-15 `e2e-cold` "존 입장 129ms" 같은 자리?)를 `git log -3` 로 후보 카드/세션 · 고치지 않는다(소문 = 세션6 영역 · 존 입장 = Z). ③ `nightly-known.txt` — 상시로 확정된 것만 등재(자 결함으로 닫힌 건 등재 0 · 잠든 빨강은 "귀속 미상 · 카드 T◯◯◯" 로) · 러너 출력 문법 그대로. ④ 답 한 줄. 하루. 질문 금지.

★검증 규약: 제품 코드 0(하네스·목록만 · 제품 결함이면 회부) · A/A 두 판 · `test-harness-lint` · 야간 등록 무변 · 접점 심볼(`e2e-charsprite|e2e-rumor|nightly-known|nightly-report|42-r2-char|41-h-char|30-n-net|e2e-cold`).

## 0. §0 실측 (넷)
ⓐ charsprite A/A 잡음 바닥 + 경로 + 자/제품 판정. ⓑ rumor 귀속 표. ⓒ known 갱신 diff. ⓓ 한 줄 답.

## 1. 절대 규칙
제품 0 · 배포 재민만 · ★푸시 2판(맥 VM · `batch/night-red-0918` → ff → main(하네스·목록)) · force 0.

## 2. 작업 (넷)
① charsprite ② rumor 귀속 ③ known ④ 한 줄 답.

## 3. 검증
A/A · 린트 · 접점.

## 4. 회부만
rumor 제품 결함(세션6/Z) · 망고링고 v3(PM · 오늘) · 옛 손 포즈 키(#10 뒤).

## 5. 완료
커밋 [재민 위임 2026-09-18 · T304] · main 푸시 · `보고/T304_<날짜>.md`(머리 15줄) · 채팅 한 줄. 실기 0.
