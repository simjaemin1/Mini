# [Sonnet] 지시 — T330: 계측 러너 둘째 — 새 main(오늘 아홉 착지 뒤)에서 3시드 열 열 독립 재현 · `war-world-perf` W0/W6 두 판(T295 뒤 · T307 표와 나란히) · `t316-fish-perf` 끔/켬 두 판 재현 — 표만 · 판정 PM (세션12 · T307 뒤 · 2026-09-19)

아래 전문을 **세션12** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T330.md` · 레포 `지시/지시_T330.md`) **`[Sonnet]` — 판정 0 · 코드 0 · 표만.**

---

T307(`batch/measure-0919` → main · 시드 42 = 7,853 여덟 수 일치 · 15시드 전부 방향만 · W0/W6 2.48/4.60ms · 잡음 0.2%/1.5%) **통과** — 잡음을 두 판으로 잰 것이 맞다. ⚠기준선 표의 시드 42 `게시` 는 T302 착지로 2,356→2,361 이 됐다(공통 §2 · PM 갱신) — 네 표는 착지 전 main 이라 2,356 이 맞았다.

**카드 T330 — 셋.** `git pull --ff-only origin main` 뒤 해시를 머리에. ① **3시드 열 열** — `t17-metrics` 800일 51마을 1020·7·42 → 공통 §2 넷째 판 표(게시 42 = 2,361)와 **열 열 전부** `cmp`(인구·소멸만 보지 않는다 — PM 이 그렇게 놓쳤다 · 족보 247) · JSON 은 `계측/T330_seed<n>.json`. ② **`ARMS=W0,W6 node scripts/war-world-perf.js`** 두 판 → T307 표(T295 전)와 나란히 · 같은 팔 두 판 차(잡음). ③ **`node scripts/t316-fish-perf.js`** 끔/켬 두 판(T316 §3 문법 · 인자는 파일 머리) → T316 표(p50 2.47/374.6 · lag 37%)와 나란히 · 잡음. ④ 한 줄 답. 컨테이너 · 동시에 하나 · 판마다 파일로 · `sleep 300` 뒤 `tail -n 2`.

## 1. 절대 규칙
판정 0 · 코드 0(`계측/` + `보고/`) · `pkill -f` 금지 · ★푸시 2판(가지 `batch/measure2-0919` 가지만 · 403 → `~/Mini/_inbox/`) · force 0 · PAT 출력 금지.

## 5. 완료
커밋 [승인 대기 · T330] · `보고/T330_<날짜>.md`(**머리 15줄에 한 줄 답**) · 채팅 "가지 올렸다 <해시> — 3시드 열 열 …/30 일치 · W0/W6 … · fish …".
