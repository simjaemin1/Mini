# 구현 지시 — T297: 생산은 행위다 — 첫 자를 세운다: 랩 관찰 모드(실걸음) 하루 합 ↔ 빨리감기 수식 차이 3시드 · 어부·광부 수식의 행위 역산(청동기 하루로 가능한가) · 51마을 실걸음 부하 추정 · 코드 0 (세션3 · T286 뒤 · 2026-09-18)

아래 전문을 **세션3** 에 붙여넣으면 된다. (지시서 파일: `~/Mini/지시_T297.md` · 레포 `지시/지시_T297.md`)

---

**캐논이 섰다 — `설계/설계_생산_실체.md`(main) §1 을 먼저 읽는다.** 재민 09-18: *"npc 가 낚시하는 순간 물고기가 들어가고, 나무를 베는 순간 통나무가 생기고, 광질하는 순간 광석이 쌓이는 거라고"* · 낚는 순간 손에 → 귀환하면 곳간(죽으면 그 자리에 낙하) · 관측자 없어도 실걸음. 서버는 지금 `economy-sim.js:2111 tickVillage` 가 npc 마다 `addProduce(품목, base × land × skill)` 로 **행위 없이** 곳간을 채운다 — 랩 관찰 모드가 이미 가진 실물 층(농부 즉시 수확 · 채집 T135 · 나무꾼 `_wcut` · 사냥꾼 `_hkill` — econ 이 다음 날 읽는다 `:9741`)을 이식이 안 가져오고 추상 기본값(`L_HUNTINCOME`·`L_WOODINCOME` = 'abstract')을 가져왔다. **어부·광부는 랩에서도 수식이다**(`:2881`). T286(`lab-alloc-server` 캐시 · 15시드) 통과 — 그 캐시가 이 카드의 도구다.

**카드 T297 — 코드 0 · 자 셋. 옮기기 전에 얼마나 다른지, 그리고 지금 수식이 사람의 하루로 가능한 수인지부터.** **PM 판정**(재민 거부권): ① **관찰 ↔ 빨리감기 차이 3시드(1020·7·42)** — 랩을 관찰 모드로(실물 손잡이 전부 켬: `L_HUNTINCOME=real` · `L_WOODINCOME=real` · T135 · T100 4판) 같은 마을 셋(농촌·어촌·산촌 하나씩 — 51마을 실지도에서 고른다 · 이유 표)을 **30일** 돌리고, 같은 씨 빨리감기 30일과 **직업별 하루 산출 합**(식량·목재·고기·채집·돌)을 나란히 → 짝 Δ 를 T252 자로(부호 3/3 + |평균| > 폭 → 가름) · 관찰 모드가 30일에 얼마나 걸리는지(벽시계 · 800일이면 몇 시간인지) 적는다. 관찰 모드는 브라우저라 헤드리스 Chromium(`lab-alloc-server` 문법 · Playwright)으로 돌린다 — 이미 있는 계측기가 관찰 모드를 못 돌리면 그 사실이 첫 표다(회부 · 새 계측기는 다음 카드). ② **어부·광부 역산** — 지금 수식이 1인 하루에 내는 양(`land.water × 1.2 × sk` · 광부 `jd.base × land.ore …` — 자리 표)을 **행위 단위**로 뒤집는다: 낚시터까지 거리(`_jobSites.fisher` 실좌표 · 마을 중심에서 셀) · 걸음 속도(`movePlayerStep` NPC 정본 · 새 수 0) · 왕복 시간 · 하루 낮 길이(24분 · 캐논) → 하루 가능한 왕복 수 → 한 번에 잡아야 하는 마리 수 → **청동기 어부 하루 조업으로 가능한 수인가**(고증 근거 한 줄 · 못 대면 "못 댔다"). 광부 같은 표(광맥 `qtResources` ore 실좌표). 무게 모델(2026-08-27 · 어부가 한 번에 질 수 있는 kg)과 대조 — **손에 들고 귀환**(캐논 ⓐ)이면 한 번에 지는 상한이 곧 하루 상한이다. ③ **부하 추정** — 관측자 없는 51마을이 전부 실걸음일 때(캐논 ⓑ): 지금 관측 마을 하나의 생활층 틱 비용(`_lifeSub` 계측 · `/perf`)에서 마을 수 × 실걸음 인원으로 외삽 — **실측 하나 + 외삽**임을 적는다(실측은 다음 카드가 세계 위에서). ④ 답 한 줄("차이: 직업별 짝 Δ … 가름/못 가름 · 관찰 800일 = …시간 · 어부 하루 = 왕복 n × m 마리 = 가능/불가능 · 광부 … · 51마을 실걸음 외삽 p50 …ms"). 하루~이틀. 질문 금지 — 답은 표.

★검증 규약: 코드 0(`sim/`·`server/`·`lab/` 무접촉 · 계측기·env 만) · 3시드 · 관찰 모드 30일은 같은 씨로 두 번 돌려 결정성(같으면 자 · 다르면 그 폭이 잡음 바닥 — 족보 80) · 새 수 0(전부 기존 상수에서 유도 · 유도 목록 회부에) · 러너 전수 안 돌린다 · 접점 심볼(`lifeDayAll|doBulk|L_HUNTINCOME|L_WOODINCOME|forageTakeFn|_wcut|_hkill|huntIncomeReal|woodIncomeReal|tickVillage|addProduce|JOB_OUT|_jobSites|movePlayerStep|_lifeSub|lab-alloc-server|LAB_SEEDCACHE`).

## 0. §0 실측 (넷)
ⓐ 관찰 ↔ 빨리감기 직업별 30일 합 3시드 표 + 벽시계. ⓑ 어부·광부 행위 역산 표 + 고증 한 줄 + 무게 상한 대조. ⓒ 51마을 실걸음 부하 외삽. ⓓ 한 줄 답.

## 1. 절대 규칙
코드 0 · 새 수 0 · 켜지 않는다(표만) · **T288(세션5) · T295(세션10 · war-core/econ) · T285(세션1) — 무접촉** · 배포 재민만 · ★푸시 2판(**맥 VM `device_bash` 에서** `$HOME/pushrepo` 얕은 클론 + PAT 파일 · 가지 `batch/produce-act-0918` **가지만** · `~/mnt/Mini/durango-mini` 무접촉 · 403 이면 `~/Mini/_inbox/` 번들) · force 0 · `.lock` 0.

## 2. 작업 (넷)
① 차이 3시드 ② 역산 ③ 부하 외삽 ④ 한 줄 답.

## 3. 검증
코드 0 · 결정성 두 판 · 시드 목록 · 접점 목록.

## 4. 회부만
설계 §2 ⓔ~ⓗ(재민) · 어부 옮기기(다음 카드 · 이 표 뒤) · 관측자 무관 자원 색인(ⓗ · Z) · 관찰 모드 헤드리스 계측기(없으면).

## 5. 완료
커밋 [승인 대기 · T297] · 가지 푸시(맥 VM) · `보고/T297_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 결론**) · 채팅 "가지 올렸다 <해시> — 차이 … · 어부 … · 광부 … · 부하 …". 실기 0.
