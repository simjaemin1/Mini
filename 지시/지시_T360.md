# 구현 지시 — T360: 닛폰 "대양 분리"의 정체 — 1,529,254셀 덩어리(마을 4곳)를 본토에서 가르는 경계가 **물인가 바위인가**(T348: 경계 셀 물 6,051 · 바위 6,172 · 계획기는 "200셀 내 도하 없음"만 봤다) · 재민 09-23: "아직 배는 없다 · 닛폰도 전부 대륙으로 연결돼 있을 텐데?" · 실셀 BFS 로 경계를 지도에 찍어 갈림의 종류별 최단 폭 표 · 바위(산맥)면 **고개(`passes` 정본 · 닛폰에 2개 있다)** 로 잇는 자리 · 물이면 최단 도하 폭과 "200셀" 규칙의 출처 · 한반도 광산2 도 같은 자로 · 세계 무변 표만 → 놓을 것이 정본 한 줄이면 놓는다 (세션5 · #56 · 2026-09-23)

아래 전문을 **세션5** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T360.md` · 레포 `지시/지시_T360.md`)

---

T351 이 "남은 것은 배 하나"라 했는데 재민이 되물었다: **"아직 배는 없다. 닛폰도 전부 대륙으로 연결되어 있을 텐데?"** 다시 보니 T348 §0-ⓐ 표가 답을 반쯤 갖고 있다 — 그 덩어리의 경계는 **물 6,051셀 · 바위 6,172셀 · 존밖 753셀**이다. 계획기는 "200셀 안에 본토로 건너는 **물** 도하가 있나"만 물었고(다리는 물만 건넌다 · 바위는 안 뚫는다 `zone.js:821`), 없으니 "대양"이라 답했다. **바위가 가른 것이면 그건 바다가 아니라 산맥이고, 답은 배가 아니라 고개다.** 닛폰 정본엔 `passes` 가 2개 있다(한반도는 0).

**카드 T360 — 넷 · 표만 · 세계 무변. PM 판정**(재민 거부권): ① **경계의 정체** — 실셀 BFS(T348 자 그대로 · 서버가 보는 물·바위 술어)로 그 덩어리와 본토 사이 경계 셀을 종류별로 찍어 **어느 방위·어느 구간이 물이고 어느 구간이 바위인지** 지도 한 장(PNG · `scripts/` 계측기가 그린다 · `~/Mini/산그림/` 에도) + 표: 본토까지 **최단 폭**을 세 갈래로(물만 건너면 n셀 · 바위만 넘으면 n셀 · 섞으면 n셀) — "200셀" 규칙이 어디서 왔는지(코드 줄 · 유도인지 상수인지). ② **고개** — 바위 갈래의 최단 자리가 `passes` 정본의 꼴(`ridges`·`passes` 가 지형 생성에서 어떻게 읽히나 · 한반도 `passes` 0 인데 산을 어떻게 넘나 — 산 아크·협곡 정본 `durango-mountain-35m` 문법)로 놓일 수 있나 · 닛폰 `passes` 2개는 지금 어디를 잇나(그 덩어리와 관계) · 놓을 수 있으면 **후보 자리 표**(좌표 · 폭 · 양끝 뭍) · 놓는 것은 정본 한 줄(T351 `plan-zone-villages` 규율: 꼴 보존 · diff 최소)이면 **놓고** 도달 쌍 재계산(21쌍 중 몇 쌍이 산다) · 정본 밖이면 표만. ③ **한반도 광산2** — 같은 자로 경계 종류 · 최단 폭 · 고개/다리 후보. ④ 한 줄 답: "닛폰은 대륙으로 이어져 있나" — 예/아니오 + 수. 이틀. 질문 금지 — 답은 표.

★검증 규약: 세계 무변(정본 JSON diff 0 — ②에서 고개를 놓으면 그 한 줄만 · 놓기 전/후 `test-nippon-boot` 38 + 도달 쌍 표) · 3시드 면제(`sim/` 무접촉) · 한반도 무변(`hanbando` 절 JSON 동일 — T351 자) · 접점 심볼(`passes|ridges|isWaterCellLocal|isRockCellLocal|plan-zone-bridges|plan-zone-villages|test-nippon-boot|도하|대양|광산2`).

## 0. §0 실측 (넷)
ⓐ 경계 종류 지도 + 최단 폭 세 갈래 + 200셀 출처. ⓑ 고개 후보 표(+ 놓았으면 도달 쌍 전/후). ⓒ 광산2 표. ⓓ 한 줄 답.

## 1. 절대 규칙
표만(놓는 것은 정본 한 줄뿐) · 새 수 0 · 배 층 0(재민: "아직 배는 없다") · 마을 정본 무접촉 · **T359(세션1 청크)·T356(세션3) 무접촉** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/nippon-border-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
고개를 놓을지(재민 · ⓑ 표) · 200셀 규칙(유도 없으면 회부) · 항해 층(재민 · "아직" — 물이 진짜 가르는 곳이 있으면 그 표가 그날의 입력) · 다음 존(재민).

## 5. 완료
커밋 [승인 대기 · T360] · 가지 푸시 · `보고/T360_<날짜>.md`(ⓐ~ⓓ · **머리 15줄에 예/아니오 + 수**) · `인계/Z-존서버.md` 갱신 · 채팅 "가지 올렸다 <해시> — 경계 물 n/바위 n · 최단 폭 … · 고개 후보 n · 광산2 …". 실기 0.
