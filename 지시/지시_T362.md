# 구현 지시 — T362: `consumeFood` 에 `_cons` 한 줄 — 식량 소비가 `_consEMA` 에 잡혀 의뢰 게시판이 식량 부족을 본다(T337: 0/51 은 구조 — `consumeFood` 가 `_cons` 를 안 불렀다 · 07-12 판정) · T258 손잡이 뒤 · 끔 = 넷째 판 비트 동일 · 켬 팔 3시드 800일 열 열 + 게시 열(식량 의뢰 수) · T252 자 · 켜지 않는다 (세션6 · #33 ⓑ 재민 확정 09-23 · 2026-09-23)

아래 전문을 **세션6** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T362.md` · 레포 `지시/지시_T362.md`)

---

T337(`_consEMA` 0/51 은 구조 · 세 팔 3시드 표)·T339(밀도 캐논은 목표치 · twogate 끔 유지) 뒤 세션6 대기. ★재민 결정 #33 **ⓑ**(09-23): `consumeFood` 에 `_cons(v, r, amt)` 한 줄 — 손잡이는 T258 이 만든 그것 뒤(`sim/economy-sim.js:500` `_cons` 정본 · 광석 `:1995`·보존 `:2656`·제련 `:2765` 이 이미 부르는 꼴 그대로 · 사본 0).

**카드 T362 — 셋. PM 판정**(재민 거부권): ① **한 줄** — `consumeFood(v, need)` 가 실제로 뺀 식량 품목·양을 `_cons` 로 적는다(어느 품목이 빠지나 — `consumeFood` 가 고르는 품목 순서 그대로 · 품목별로 `_cons` · **"food" 뭉뚱그리기 0**) · T258 손잡이 이름 그대로 · 기본 끔 · 끔 = 넷째 판 3시드 열 열 비트 동일(`build-econ-bundle --check` 8/8 · `inline-engine --check`). ② **켬 팔** — 3시드 800일 열 열 + **게시 열을 품목별로**(식량 의뢰가 몇 건 · 어느 마을 · 어느 계절) · `_consEMA` 가 0 에서 얼마로 · 가격 `flowT` 가 움직이나(v2 target) · T252 자로 팔 대 팔 짝 Δ(가름/방향만/못 가름) · 소멸 0/51 · **켜지 않는다**. ③ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 끔 비트 동일(3시드 열 열 · `sim/` 만짐 = 면제 없음) · `test-econ-fieldyield` 197 · `test-lab-alloc` · `test-granary-add` · `lab-wiring-check` 실패 0 · 접점 심볼(`consumeFood|_cons|_consDay|_consEMA|flowT|T258|의뢰|게시`).

## 0. §0 실측 (셋)
ⓐ 한 줄 diff + 끔 동일. ⓑ 켬 팔 3시드 열 열 + 게시 품목별 + T252. ⓒ 한 줄 답.

## 1. 절대 규칙
새 수 0 · 손잡이 T258 그대로(새 손잡이 0) · 켜지 않는다 · `server/` 무접촉 · **T359(세션1 `economy-sim.js` 채집 절 — 만지지 않는다 · 리베이스 때 둘 다 살린다)** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/cons-food-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**.

## 4. 회부만
켜기(재민 · ② 표) · ⓒ 여섯 품목(재민 · ② 뒤) · 밀도 띠를 무는 자(PM · #26).

## 5. 완료
커밋 [승인 대기 · T362] · 가지 푸시 · `보고/T362_<날짜>.md`(ⓐ~ⓒ · **머리 15줄에 결론**) · `인계/T-거래.md` 갱신 · 채팅 "가지 올렸다 <해시> — 끔 동일 · 켬 게시 식량 n건 · EMA … · T252 …". 실기 0.
