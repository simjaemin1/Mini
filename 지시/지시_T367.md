# 구현 지시 — T367: 다섯째 판 — 손잡이 셋을 기본 켬으로(`T100_FIELD_YIELD`(밭 수확 순간 곳간 · T100 4판) · `T193_LEDGER`(장부) · `T195_TOOL_WEAR=0.9`(도구 마모 속도 — 재민 09-23 "물리 파라미터 배수는 맞다" · T200 자)) · 되돌림 `=0`/`=1` 셋이면 넷째 판 비트 동일 · 3시드 800일 열 열 **새 기준선 표** · `인계/공통.md` §3 기준선 절 갱신(넷째 판 표는 지우지 않고 아래에 다섯째 판) · `t17-metrics` 자 무접촉 (세션6 · T362 뒤 · #11·#12 재민 확정 09-23 · 2026-09-23)

아래 전문을 **세션6** 에 붙여넣으면 된다. (지시서: `~/Mini/지시_T367.md` · 레포 `지시/지시_T367.md`) **⚠T362 가 착지한 뒤에 붙인다.**

---

재민 09-23: **"규약은 배수 금지가 아니라 추상 없애기다 · 배수 해도 돼 · 농부가 직접 밭 갈고 수확하는 순간 그 작물이 플러스되기로 했잖아."** ⇒ #11(T100 켜기 · 09-12 부터 열림)과 #12(마모 ×0.9 · T195/T200 실측)를 한 판에 닫는다. 공통 §1 은 PM 이 정정했다(`58df1a27`). T100 4판은 **이미 지어져 있다**(09-05 · 관측 마을에서 농부가 밭 셀에서 수확하는 순간 `harvestToGranary` · econ 의 추상 `addProduce(farmer)` 는 걷어냄 · 실측 T193/T207: 소멸 0/51 · 적자 마을 29% 그대로 · 인구 −10~15% 는 밭 탓이 아니라 연료가 목재의 91% 를 태워 집을 못 짓는 것) — 켜지 않았을 뿐이다.

**카드 T367 — 셋. PM 판정**(재민 거부권): ① **기본값 셋** — 정본 읽는 자리 하나씩(`economy-sim.js:927` `T100_FIELD_YIELD` · `T193_LEDGER` 그 자리 · `T195_TOOL_WEAR` 정본 자리 `batch/tool-wear-port-0912` 가 세운 그것 — main 에 있나 확인 · 없으면 그 가지를 먼저 리베이스) 을 **기본 켬**으로(T244 넷째 판 문법: 되돌림 `=0`·`=1` 이면 넷째 판 **비트 동일** — 하네스 절로) · `_clearedFrac`·`_paddyShare` 는 **끔 그대로**(#24 · T198 제3안 · 새 수). ② **다섯째 판 기준선** — 3시드 800일 51마을 `t17-metrics` 열 열(인구·소멸·무기Q·확장셀·게시·도구Q·보존식·생곡·밀도 둘) 표 + 넷째 판 대비 Δ(T252 자로 읽되 판정은 "새 기준선" 한 줄 — 좋고 나쁨이 아니다) · `build-econ-bundle --check` 8/8 · `inline-engine --check` · 랩 두 장 재인라인 · `인계/공통.md` §3 에 **다섯째 판 절 추가**(넷째 판 절은 그대로 두고 "다섯째 판 = 넷째 판 + 손잡이 셋 켬 · 되돌림 셋" 한 줄 + 표). ③ 한 줄 답. 하루. 질문 금지 — 답은 표.

★검증 규약: 되돌림 셋 = 넷째 판 열 열 비트 동일(하네스 절) · `test-econ-fieldyield` 197 · `test-granary-add` · `test-lab-alloc` · `lab-wiring-check` 실패 0 · 접점 심볼(`T100_FIELD_YIELD|T193_LEDGER|T195_TOOL_WEAR|harvestToGranary|T100_K|t17-metrics|공통 §3`).

## 1. 절대 규칙
새 수 0(0.9 는 T195/T200 이 잰 값) · 손잡이 셋만 · `_clearedFrac`·`_paddyShare` 끔 · **T359(세션1 `economy-sim.js` 채집 절)·T362(자기 앞 카드) 위에서** · ★푸시 2판(맥 VM · `$HOME/pushrepo` · 가지 `batch/baseline5-0923` · main 은 PM · 403 이면 `~/Mini/_inbox/`) · force 0 · **★턴 = 카드 끝까지(공통 §6)**. ⚠착지 뒤 PM 이 진행 중 카드들에 "기준선 = 다섯째 판" 추신을 낸다 — 세션6 은 그건 안 한다.

## 5. 완료
커밋 [승인 대기 · T367] · 가지 푸시 · `보고/T367_<날짜>.md`(**머리 15줄에 다섯째 판 표**) · 채팅 "가지 올렸다 <해시> — 되돌림 동일 · 다섯째 판 인구 …/…/… · 소멸 …". 실기 0.
