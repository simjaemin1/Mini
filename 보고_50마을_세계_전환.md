# 배치 16 — 50마을 세계 전환 (2026-08-04b)

기준: `7f7b438` 이후. 재민 확정: **"해봐."**
전제는 배치 15 실측이다 — 후보 51곳을 전부 심어도 살고, 병목은 땅이 아니라 선별 게이트였다.

---

## 0. 재민용 — 라이브 반영 (실행은 재민)

**리셋이 필요하다.** 시딩은 `villages` 테이블이 비었을 때만 돌기 때문에, 코드만 올리면
기존 18곳이 그대로 남고 새 32곳이 안 생긴다. 절차 전문은 **`라이브반영_런북_v3_50마을.md`**.

```sh
ssh root@141.164.35.114
cd /srv/durango/hanbando && docker stop durango-zone-hanbando
TS=$(date +%Y%m%d-%H%M); for f in world-hanbando.db world-hanbando.db-wal world-hanbando.db-shm; do cp $f $f.bak-$TS 2>/dev/null; done
rm -f world-hanbando.db world-hanbando.db-wal world-hanbando.db-shm
cd /opt/Mini && git pull && bash scripts/redeploy-hanbando.sh --central
```

> ★★**첫 부팅에 1.5~2시간 걸린다.** 라이브 18곳 시딩이 45분이었고 로컬 실측 배율이 **2.1배**다
> (18곳 143초 → 50곳 303초). 그동안 존은 안 뜬다 = 접속 불가. **밤에 돌려라.**
> 진행: `watch -n 60 'docker logs durango-zone-hanbando 2>&1 | grep -c "시딩:"'` → 50 이 되면 곧 뜬다.
>
> ★**`cleanup-legacy.js` 는 이번엔 돌리지 마라** — 리셋이 world DB 를 통째로 지우므로 레거시
> 고아 행도 같이 사라진다.

기대 로그 — 이 줄들이 나와야 성공이다:

```
🏘️ 마을 시딩 시작 — 후보 51 → 선별 51 (전수(seedAllVillages))
🏘️ [어촌6] 중심 (65086,126466) 주변 24셀 내 뭍 없음 — 스킵        ← 정상. 물 위엔 못 세운다
🏘️ 마을 시딩 완료 — 후보 51 → 선별 51 → **시딩 50** (터 미달 스킵 1곳)
🏘️ Stage4A 실물화: 큰집 50·움집 100채(6×4)·곳간 → buildings 10078행
```

**"상한 20" 이나 "선별 19" 가 뜨면 전환이 안 된 것이다.** 원인은 둘뿐이다 — 코드가 옛것이거나,
컨테이너에 **`VILLAGE_MAX` env 가 남아 있는 것**이다(명시한 env 상한이 존 설정을 이기도록 했다.
하네스가 1곳만 심어 빨리 돌 수 있게 한 규칙이다). `docker exec durango-zone-hanbando env | grep VILLAGE_MAX`
가 비어 있어야 한다. 런북 §6 에 대처가 있다.

스모크: 접속하면 **첫 화면이 농촌22 광장**(지붕 있는 큰집 북쪽·움집 2채 남쪽) ·
`sqlite3 world-hanbando.db "SELECT COUNT(*) FROM villages;"` → **50**.

---

## 1. 한눈에

| | 전(18곳) | **후(50곳)** |
|---|---|---|
| 시딩 마을 | 18 | **50** (후보 51 → 터 미달 어촌6 1곳 물리 스킵) |
| 세계 인구(800일·3시드) | — | seed1020 **6,280** · seed42 **6,072** · seed7 **5,283** |
| 소멸(800일·3시드) | — | **0 · 0 · 0** ✅ |
| 좀비(<10명, 800일·3시드) | — | **0 · 0 · 0** ✅ |
| 1,600일 seed1020(라이브 시드) | — | 소멸 **0** · 좀비 **0** · 총인구 **9,765** · 최소 인구 **72** |
| 부팅(로컬 2코어) | 211초 | **455초** |
| Stage4A 실물화 | 3,576행 | **10,078행** (큰집 50·움집 100) |
| 스폰 | 벌말 광장(빈 들판) | **농촌22 광장** — 중심성 1위·3시드 최소 인구 101 |

지형 수정은 **못 5개 + 숲 1개**가 전부다. 마을 좌표·광맥·강·능선·다른 존은 **바이트 동일**.

---

## 2. ① 프로덕션 50곳 전환

### 스위치

```js
// server/zone-config.js — hanbando 만
seedAllVillages: true,   // 품질 게이트(식량 하한·간격·상한) 우회
villageMax: 51,
```

`pickSeedVillages(all, ta, opts)` 에 세 번째 인자를 냈고 `seedVillages` 가 존 설정을 넘긴다.
**우선순위**: 명시한 `VILLAGE_MAX` env > 존 설정 > 기본. 하네스·프로브가 `VILLAGE_MAX=1` 로 빨리
도는 길을 막지 않기 위해서다(probe-roof.js 선례). 프로덕션 컨테이너엔 그 env 가 없다
(Dockerfile.zone·redeploy 스크립트 전수 확인).

★**모듈 상수 `VILLAGE_MAX` 는 안 건드렸다.** 그 값은 `PV_MAX`(**플레이어** 마을 상한)의 기본값이기도
해서, NPC 시딩 상한을 올리려다 플레이어 마을 상한이 조용히 20→51 로 바뀌는 걸 막았다.

### 우회하는 것과 안 하는 것

우회는 **품질 게이트뿐**이다: 식량 하한 2.0 · 간격 12,000px · 상한.
**터 미달 스킵은 그대로 산다** — `findOpenCenter` 가 중심 주변 24셀에 뭍이 없으면 건너뛴다.
그건 품질 기준이 아니라 **물리**다(물 위엔 마을을 못 세운다). 그래서 후보 51 → 시딩 **50** 이고,
재민이 말한 "50개"와 정확히 맞는다.

### 랩도 같은 세계를 재게 했다

`econ-lab-real.js` 가 `pickSeedVillages(hard, ta)` 로만 부르고 있었다 — 그대로 두면
**프로덕션 50곳 · 랩 19곳**이 되어 이 프로젝트가 다섯 번 당한 오진이 재발한다.
존 설정을 넘기도록 고치고, `lab-wiring-check` 에 **검사를 추가**했다(다음에 누가 빼면 붉게 뜬다):

```
✅ 시딩 정책 = 존 설정 전달(seedAllVillages/villageMax) — 랩이 프로덕션과 같은 마을 수를 잰다
```

리팩터가 비트 안전한지도 실측했다 — 지형 보강 **전** 상태로 시드 42 를 다시 돌려
배치 15 덤프(`all51_42.json`)와 **바이트 완전 동일**을 확인했다.

### 검증(로컬 새 DB 부팅 1회)

```
🏘️ 마을 시딩 시작 — 후보 51 → 선별 51 (전수(seedAllVillages))
🏘️ [어촌6] 중심 (65086,126466) 주변 24셀 내 뭍 없음 — 스킵
🏘️ 마을 시딩 완료 — 후보 51 → 선별 51 → **시딩 50** (터 미달 스킵 1곳)
```
`SELECT COUNT(*) FROM villages` → **50**.

---

## 3. ② 땅 보강 — 6곳

### 무엇을 만질 수 있는가 (재민 질문의 뒷조각)

**비옥도는 저장된 레이어가 아니다.** `server/fertility.js` 가 지형에서 유도한다:

```
fert = 0.12 + 0.62·exp(-dw/80) + 0.18·min(1, dr/60) + 0.08·임상
       (dw = 물까지 셀 · dr = 바위까지 셀 · 임상 = 반경 8셀 캐노피 비율)
```

⇒ "비옥도를 칠한다"는 조작은 **존재하지 않는다.** 실제 손잡이는 **물 · 바위 · 숲** 셋뿐이고,
이 배치는 그중 **물과 숲만** 만졌다(바위는 광산6 의 광맥이 거기 있어서 안 건드렸다).

### 처방과 지물 — `scripts/migrate-terrain-sw4.js`

배치 15 의 K 분해(kDbg)가 처방을 정했다. **병목에 맞춰 최소만.**

| 마을 | 셀 | 병목 | 지물 |
|---|---|---|---|
| 광산6 | (227, 3078) | prod(식량) | 못 반경 12셀 · 중심에서 56셀 |
| 농촌11 | (161, 3489) | prod | 못 |
| 농촌21 | (428, 3853) | prod | 못 |
| 농촌12 | (161, 3853) | **fuel**(연료) | 숲 반경 63셀 ×1.55 |
| 농촌6 | (161, 210) | prod | 못 ← **1차 실측 뒤 추가** |
| 농촌7 | (161, 939) | prod | 못 ← **1차 실측 뒤 추가** |

못은 마을 영토(반경 ~33셀) **밖**(56셀)에 놓았다 — 경작지를 한 셀도 안 먹으면서
물거리만 줄인다. 자리는 **원판 전체가 뭍**인 곳만 고른다.

### 왜 6곳이 됐나 — 억지가 아니라 같은 병에 같은 약

처음 4곳만 고치고 **라이브 시드(1020)** 를 다시 재니 좀비가 둘 남았다:
농촌6(인구 7 — **보강 전에도 좀비였다**)과 농촌7(10 → 7).

내 보강이 망가뜨린 게 아님을 실측으로 갈랐다:

| | 새 지물까지 | `kDbg.slot`(땅에서 오는 항) |
|---|---|---|
| 농촌6 | **2,912셀** | 99 → **99** (완전 동일) |
| 농촌7 | **2,184셀** | 109 → **109** (완전 동일) |

land 스캔 반경은 140셀이다 — 둘 다 그 밖이고 땅이 한 비트도 안 바뀌었다.
땅값을 보니 **원래 물이 먼 자리**였다(농촌6 물 0.08·식량 1.14 — 배치 15 3시드 평균 인구 11.0 으로
51곳 중 최저 / 농촌7 물 0.46·식량 1.86). 실제로 이 여섯은 전부 **존 서쪽 열(cx 161~428)** 이다 —
서쪽 가장자리가 통째로 마른 땅이다. 같은 진단이라 같은 처방을 줬다.

### 지물이 실제로 한 일 (정본 함수 재측정)

| 마을 | dw(셀) | land 비옥 | land 물 | 사냥 | 나무 |
|---|---|---|---|---|---|
| 광산6 | 149 → **43** | 0.26 → **0.82** | 0.08 → **1.07** | 0.45 | 0.45 |
| 농촌11 | 162 → **41** | 0.45 → **1.02** | 0.08 → **1.11** | 0.65 | 0.67 |
| 농촌21 | 97 → **42** | 0.68 → **1.01** | 0.50 → **1.09** | 0.51 | 0.54 |
| 농촌12 | 71 → 71 | 0.84 | 0.79 | 0.45 → **0.73** | 0.45 → **0.91** |
| 농촌6 | 138 → **42** | 0.45 → **1.02** | 0.08 → **1.11** | 0.52 | 0.57 |
| 농촌7 | 101 → **41** | 0.66 → **1.04** | 0.46 → **1.13** | 0.45 | 0.48 |

실부팅에서도 확인된다 — 광산6 은 시딩 때 **논이 0 → 35칸** 생겼다
(`land(F0.9/W1.07/S1.38/O1.13/우드0.45)`).

### 국소성 — 다른 마을 무접촉

스크립트가 적용 전에 **다른 마을 반경 140셀(land 스캔 반경) 안에 새 지물이 들어가면 중단**한다.
겹침 **0건**. 적용 후 무접촉 검사도 통과:

```
무접촉 배열 검사: rivers·ridges·passes·ores·villages·valleys → 전부 바이트 동일 ✅
다른 존(10개) 무접촉: 전부 바이트 동일 ✅
```

바뀐 것은 `hanbando.lakes` 9 → 14 · `hanbando.forests` 42 → 43 뿐이다.
전부 `_tag: 'sw4'` 이름표가 붙어 있어 **멱등**(재실행하면 제 것만 갈아끼운다)이고,
`--revert --apply` 로 그 지물만 걷어낼 수 있다. 드라이런 기본 · 백업 선행.

### 채택 기준 판정 — `scripts/compare-village50.js`

| 시드 | before(보강 전) | **after(보강 후)** |
|---|---|---|
| **1020**(라이브) | 소멸 [농촌21] · 좀비 [농촌6] | **소멸 없음 · 좀비 없음** ✅ |
| 42 | 소멸 없음 · 좀비 [농촌21] | **소멸 없음 · 좀비 없음** ✅ |
| 7 | 소멸 [광산6] · 좀비 없음 | **소멸 없음 · 좀비 없음** ✅ |

**비대상 마을 상태 전이: 나빠짐 0건 · 좋아짐 1건**(농촌6 좀비 7 → 생존 34).
세계 총인구 5,397 → 6,190(+14.7%) / 5,046 → 5,983(+18.6%) / 6,276 → 5,970(−4.9%).

보강 6곳 — 18개(6마을 × 3시드) 조합이 **전부 생존**:

| 마을 | seed1020 | seed42 | seed7 |
|---|---|---|---|
| 광산6 | 37 → 33 | 17 → 54 | **0(소멸) → 143** |
| 농촌11 | 66 → 128 | 17 → 57 | 12 → 11 |
| 농촌12 | 12 → 30 | 13 → 14 | 14 → 25 |
| 농촌21 | **0(소멸) → 106** | **9(좀비) → 21** | 41 → 27 |
| 농촌6 | **7(좀비) → 34** | 13 → 46 | 13 → 30 |
| 농촌7 | 10 → 26 | 39 → 41 | 82 → 27 |

**1,600일 장기(시드 1020)**: 마을 50 · 소멸 **0** · 좀비 **0** · 총인구 **9,765** ·
최소 인구 **72**(광산1). 800일의 칼날 평형이 장기엔 전부 자립으로 굳는다.

### ★내가 만든 오진 하나 — 계측기를 자작했다

1차 비교에서 **"비대상 마을 3곳이 새로 좀비가 됐다"**(광산7·농촌6·농촌9)고 나왔다. 틀렸다.
내가 `최근 300일 최고 인구 ≤ 12` 라는 **자작 잣대**를 썼고, 그 셋은 12↔15명을 오가는 마을이라
문턱을 스친 것뿐이었다. 정본 잣대는 `scripts/ab-summary.js` 의 **"최종 인구 < 10"**(재민 기준
"10명 미만 장기 고착")이다. 바꿔 재니 세 건 중 둘은 애초에 상태 변화가 없었다.

이 프로젝트가 이미 적어 둔 그 함정이다 — *"하네스가 틀리면 조용히 통과하는 게 아니라
없는 결함을 보고한다. 기준을 먼저 검증해라. 계측기도 사본 금지다."* 같은 실수를 한 번 더 했다.

---

## 4. ③ 스폰 이동 — 농촌22 광장

50곳 배치를 놓고 골랐다(3시드 800일 실측):

| 후보 | 중심성(다른 49곳 평균 거리) | 3시드 최소 인구 | 평균 인구 |
|---|---|---|---|
| **농촌22** | **1,281셀 (1위)** | **101** | **115** |
| 농촌17 | 1,294셀 | 36 | 90 |
| 농촌16 | 1,307셀 | 16 | 39 |
| 농촌1 | 1,344셀 | 114 | 185 |

농촌22 는 중심성 1위이고(세계 중심 셀 1094,2032 바로 옆) 소멸·좀비 이력이 없다.
자리는 큰집(9×9, 중심 ±4셀) **남쪽 7셀**의 마당 셀 — 클라 시야 650px(≈20셀) 안에
큰집(북 7셀)과 움집 2채(남 9셀)가 함께 들어온다.

```js
mainSquare: { x: 3084.8, y: 5987.2, name: '농촌22 광장' },   // ×WORLD_SCALE(10) → 스냅 → 셀(964,1871)
```

겹침 실측(배치 15 에서 관측자 페이로드로 전수): 스폰 셀 위 통행 차단 건물 **0** · 사유지 **0**
(마당 타일 `vtile` 1개뿐 — 높이 2, 통행 판정 대상 아님) · 물 아님.

실화면(새 DB 첫 부팅 · 재시작 없음 · 진짜 클라 · 404 **0건**): `/tmp/b16-shots/spawn.png`

---

## 5. 검증

| 하네스 | 결과 |
|---|---|
| `lab-wiring-check.js` | **실패 0 · 경고 0** (시딩 정책 검사 1건 추가) |
| `test-alloy` / `test-valuechain` / `test-armor-cap` / `test-picker-tables` / `test-building-anchor` | 전부 PASS |
| `test-granary-add` / `test-guild-treasury` / `test-mining` / `test-assay` | 전부 PASS |
| `test-village-found.js` | **47 / 0** |
| `test-distmatrix.js` | **9 / 0** |
| `test-furnace.js` | **85 / 0** (아래 ★) |
| `test-guest-identity.js` | **62 / 0** |
| **실클라 E2E** `e2e-village.js` | **32 / 0** |
| **실클라 E2E** `e2e-metallurgy.js` | **20 / 0** |
| **실클라 E2E** `e2e-guest-reconnect.js` | **40 / 0** |
| v2 CLI 회귀(30마을) | 인구 **2,616** · 무기Q **651** · 소멸 **0/30** — 기준선 동일 |
| 50곳 3시드 800일 | 소멸 **0/50** · 좀비 **0** (★새 기준선) |
| 50곳 1,600일(시드 1020) | 소멸 **0** · 좀비 **0** · 총인구 **9,765** |

`sim/` 은 **한 파일도 안 건드렸다**(`git diff --stat -- sim/` 공집합) — econ 무접촉이 정상이다.

### 새 기준선 (다음 세션이 지켜야 할 수치)

| | 값 |
|---|---|
| 시딩 | 후보 51 → 선별 51 → **시딩 50**(터 미달 어촌6 1곳 스킵) |
| 50곳 800일 3시드 인구 | 6,280(1020) · 6,072(42) · 5,283(7) — 어촌6 포함 랩 수치 |
| 50곳 800일 소멸·좀비 | **0 · 0** (3시드 전부) |
| 50곳 1,600일(1020) | 소멸 0 · 좀비 0 · 총인구 9,765 · 최소 인구 72 |
| v2 CLI(불변) | 2,616 · 무기Q 651 · 소멸 0/30 |

실화면 4장(`/tmp/b16-shots/`): `spawn.png`(농촌22 광장 첫 화면) · `boosted.png`(광산6 — 보강 마을) ·
`pond.png`(광산6 방죽 — 새로 넣은 못) · `normal.png`(임업2 — 보강 무관 일반 마을).

### ★하네스 하나가 없는 결함을 보고했다 — 고쳤다

`test-furnace` 가 처음 **49/2 실패**로 떴다. 원인은 게임이 아니라 하네스다:
`findClearSpot()` 이 `spot` 둘레 4×4 만 검사하고 파생 자리(`spot+(6,6)` 등)는 안 봤다.
배치 16 이 농촌6 곁에 못을 놓자 `spot=(200,206)` 은 통과했는데 `spot4=(206,212)` 가 물에 잠겼고,
숯가마가 안 지어진 채 뒤따르는 진척 검증이 "완공 실패"를 찍은 것이다.
**쓰는 범위를 다 검사하도록** 고쳤다(`SPOT_SPAN = 9`, 탐색 범위 900→1200셀). → 85/0.

실클라 E2E 3종은 대기 예산을 **240초 → 900초**로 올렸다. 50곳 전수 시딩으로 존 첫 부팅이
로컬 2코어에서 ~7.6분이라(18곳 시절 ~3.5분) 240초면 시딩 도중에 "기동 실패"로 끊긴다.

---

## 6. 회부 — 재민 판단 사항

**(가) ★랩과 프로덕션이 마을 수가 다르다 — 51 vs 50.**
`어촌6`(존 남동 해안)은 프로덕션에선 `findOpenCenter` 가 스킵하는데 **랩에선 심긴다.**
원인은 **해안선 물 strip** 이다: `zone.js` 가 이웃 바다에서 생성하는 `WATER_TILES` 는
`terrain.js` 에 없어서 랩의 지형 어댑터가 못 본다. 이 배치가 만든 문제가 아니라
해안선이 도입된 이래의 구조적 간극이고, 이번 판정에는 어촌6 을 빼고 50곳으로 셌다.
고치면 **실지도 랩 기준선이 전부 이동**하므로(b13/b14/b15 비교 불가) 별도 결정이 필요하다.

**(나) 시딩 게이트의 코드는 살려 뒀다.** `FOOD_FLOOR`·`MIN_SPACING_PX` 는 지우지 않고
`seedAllVillages` 로 우회만 한다. 다른 존은 종전대로 그 게이트로 심는다.
한반도에서 되돌리려면 `seedAllVillages` 한 줄을 지우면 된다.

**(다) 첫 부팅 1.5~2시간.** 리셋 때마다 이 비용이 든다. 시딩을 배치·재개 가능하게 만들지
(부팅 후 백그라운드 시딩), 아니면 시딩 결과를 파일로 굽어 배포에 싣을지는 별도 설계 판단이다.

**(라) 주기적 tick_max 수백 ms 히치**(배치 15 §5) — 여전히 원인 미확정. 전용 배치 필요.

**(마) 한반도 Q키 시세 패널이 프로토타입 20마을을 보여준다**(배치 15 §7-마) — 미해결.

---

## 7. 이번 배치가 바꾼 것 / 안 바꾼 것

| | |
|---|---|
| 바꿨다 | `zone-config.hanbando`: `seedAllVillages:true` · `villageMax:51` · `mainSquare`(농촌22 광장)<br>`hanbando-terrain.json`: 호수 9→14 · 숲 42→43 (전부 `_tag:'sw4'`)<br>`pickSeedVillages(all, ta, opts)` 3번째 인자 · 시딩 완료 로그 |
| 안 바꿨다 | `sim/` **전체**(econ 무접촉) · 마을 좌표·광맥·강·능선·계곡 · 다른 존 10개 · 모듈 상수 `VILLAGE_MAX`(=플레이어 마을 상한 기본값) · 다른 존의 시딩 게이트 |
| 하네스 수정 | `test-furnace` 자리 탐색(없는 결함 보고 수리) · E2E 3종 대기 예산 · `lab-wiring-check` 검사 1건 추가 · `econ-lab-real` 시딩 정책 전달 |
| 새 도구 | `scripts/migrate-terrain-sw4.js`(지형 보강 — 멱등·백업·드라이런·되돌리기)<br>`scripts/compare-village50.js`(보강 전/후 전수 비교 — 정본 잣대) |
