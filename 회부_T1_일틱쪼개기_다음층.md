# 회부 — 일틱 조각내기 다음 층 (2026-09-01 · T1)

> 이번 배치가 심은 것: 게임일 경계의 마을 시뮬 일틱을 **단계 경계·마을 경계**에서
> 조각으로 나눠 여러 프레임에 흘리는 **슬라이서** · 그 창의 마을 요청을 모았다 흘리는
> **"장부 마감 중" 큐** · `/perf` 의 **단계별·조각별·이벤트 루프 지연** 계측.
>
> ★**econ 은 한 줄도 안 건드렸다.** 지시 §2-③이 열어 둔 좁은 예외(함수 추출)조차 **쓸 필요가 없었다** —
> 실측이 전제를 뒤집었기 때문이다(아래 A-1).
>
> ★**목표 미달**이다. RTT 스파이크는 대조군 대비 **×13.9 줄었지만**(창 안 p95 11,328ms → 815ms),
> 재민 확정 목표 **≤×2** 에는 못 닿았다. 바닥은 **조각 하나**이고, 그 안의 수술이 이 회부의 A 다.

---

## A. 남은 바닥 — 슬라이서는 **조각보다 잘게 못 자른다**

조각내기는 하루의 일감을 196~214 조각으로 나눠 30 프레임에 흘린다. 그래서 한 프레임이 막히는 시간은
**2,888ms → 447ms** 로 내려갔다. 그 447ms 는 **조각 하나**다. 더 줄이려면 조각 안을 째야 하고,
그건 이 배치가 하지 않기로 한 수술이다(지시 §2-① *"그 조각이 16ms 를 넘으면 그 사실만 보고"*).

### A-1 ★★`_lifeAddHouseSite` — 일틱의 **70%**. 진짜 살찐 놈
```
  실측 (마을 50곳 · econ day 515 · 100일 중앙값)
    일틱 총             2,018ms
      life (생활층)      1,620ms   80.3%    ← ★
        ↳ life:site      1,127ms   55.8%    ← ★★`_lifeAddHouseSite` 하나
        ↳ life:gran        252ms
        ↳ life:headless    243ms
      terr                 177ms
      econ (tickWorldV2)    99ms    4.9%    ← 지시서가 범인으로 지목했던 그것
      caravan               17ms (p95 989ms · 최대 5,046ms)
      나머지 전부           ~16ms
    마을 **한 곳**의 생활층 최대 227ms (그 중 site 185ms)  ← 조각 예산 16ms 의 ×14
```
`_lifeAddHouseSite` 는 **영토 전수를 2패스로 훑고 정렬**한다(`vil._terrSet` 전량 × strict/loose).
호출 조건은 `!vil._site && 인구 > 수용 ×0.92` 인데, **자리를 못 찾으면 `_site` 를 못 세우고
다음 날 똑같이 다시 훑는다.** 성숙한 마을 50곳이 매일 그걸 한다.

길 셋 — 전부 **설계 판단**이라 구현하지 않았다:
* ⓐ **실패를 기억한다** — 자리를 못 찾은 마을은 N일(또는 영토·집이 변할 때까지) 다시 안 훑는다.
  가장 싸다. ⚠"집이 늦게 선다"가 아니라 "**못 지을 때 안 시도한다**"라 행동 변경은 사실상 0 인데,
  그걸 증명하려면 신축 궤적 A/B 가 필요하다.
* ⓑ **후보를 캐시한다** — 영토가 자란 셀만 후보에 더한다(전수 재스캔 폐지). 정합이 더 어렵다.
* ⓒ **영토 경계만 훑는다** — 집터는 대개 바깥쪽이다. 결과가 달라질 수 있다(= 행동 변경).
★내 관찰: **ⓐ가 이번 층의 답**이다. 다만 "언제 다시 훑는가"가 곧 마을 성장 속도라 **재민 판정**이다.

### A-2 캐러밴 A* — **콜드 캐시**가 가장 큰 조각을 만든다
```
  최대 조각 436ms = 캐러밴 한 대의 `spawnCaravanBody` → `getRoute` (A*)
  · `getRoute` 는 **마을 쌍당 1회** 캐시한다(null 도 캐시). 즉 이건 반복 비용이 아니라 **부팅 후 워밍업**이다.
  · 다만 쌍이 50×49/2 = **1,225개**고 캐시는 메모리라 **재기동마다 다시 데운다**.
```
길 둘: ⓐ 교역로를 DB 에 영속(재기동 무상) · ⓑ 부팅 직후 유휴 프레임에 미리 데운다(선계산).
★econ 무접촉이고 순수 캐시라 **행동 변경 0 이 증명 가능**한 드문 항목이다. 다만 새 저장 층이다.

### A-3 `_oreMineDaily` — 중앙 9ms 인데 **최대 4,260ms**
광맥 셀 목록 캐시(`_oreCellsOf`)가 식은 날 한 마을에서 터진다. 마을 경계로 쪼개 뒀으므로
**한 조각 안에 갇히지만**, 그 한 조각이 4초다. A-2 와 같은 부류(콜드 캐시)다.

---

## A-4 ★★조각내기가 **세계를 갈랐다** — 실측으로 잡아 막았다 (기록)

이 배치에서 가장 값진 실측이다. 하네스 ⑧(`test-tick-slicer`)이 **두 번 거짓말을 했고 두 번 다 고쳤다.**

```
  ① 첫 판   base↔head 가 50곳 중 **49곳** 갈렸다 → "쪼갰더니 세계가 갈렸다"
     ★가짜였다. 저장이 **1마을/틱**이라 스냅샷 순간 팔마다 '어제치가 남은 마을 수'가 달랐을 뿐이다
       (실측: base 49곳이 526일 · head 50곳이 527일). 서로 **다른 날**의 상태를 비교하고 있었다.
     ⇒ 고침: 같은 마을 **같은 날**끼리만 대조하고, 끄기 전에 저장 큐를 다 흘린다.

  ② 고친 뒤 A/A(같은 설정 두 판)는 50/50 동일한데 **A/B 만 21곳** 갈렸다
     ★이번엔 진짜였다. 마감이 2~3초에 걸치는 동안 **캐러밴·전투 실체가 계속 걸어서**,
       뒤 단계(캐러밴 동기 ⑦ · 사건 장부 ⑧)가 '몇 초 더 간 몸'을 봤다.
       조각내기 전엔 하루가 한 프레임이라 모든 단계가 **같은 순간의 실체**를 봤다.
     ⇒ 고침: **마감 중에는 실체 30Hz 를 멈춘다**(`onGameTick` 첫머리).
       하루 경계는 세계에게 **한 순간**이어야 한다. 그 뒤 A/B 다름 **0**.
```
같은 이유로 하루 안의 **벽시계 파생도 경계 시각으로 얼렸다**:
`_lifeDaily`·`_lifeHeadlessDay` 의 `gameDayOf(Date.now())` → `gameDayOf(_dayNow())`,
그리고 zone.js 의 광맥 재생 적분(`oreStockAt`/`oreConsumeAt`)이 `SimVillages.dayNow()` 를 읽는다.

★**교훈(다음 세션이 그대로 쓸 것)**: "쪼개도 행동은 안 바뀐다"는 **증명해야 하는 주장**이고,
그 증명의 도구가 먼저 틀린다. A/A 대조군이 없었다면 ①의 가짜 발산을 진짜로 보고했을 것이고,
②의 진짜 발산은 A/A 가 50/50 이었기 때문에 비로소 보였다.

## A-5 ★★그래도 **완전히 같지는 않다** — 남은 차이와 그 이유 (숨기지 않는다)

②를 고친 뒤에도 바이트 단위로는 6일에 50곳 중 **0~15곳**이 갈린다. 경제 크기로 재면(6일 누적):
```
  A/A (같은 일정 두 판)      인구 차 0 · 식량 차 **0.00~0.05%**      ← 이 층의 잡음 바닥
  A/B (끈 뽑음 ↔ 조각내기)   인구 차 0~0.10% · 식량 차 **0.62~0.76%**  ← 잡음 바닥의 약 10배
```
A/B 가 잡음 바닥의 열 배쯤이므로 이건 **잡음이 아니라 조각내기가 만든 진짜 차이**다. 원인:

> **NPC 는 매 프레임 일하고, 그 손이 곳간을 만진다**(`npcLifeTick` → `_lifeDoTask`).
> 하루의 단계들을 2~3초에 걸쳐 흘리면 그 사이 NPC 노동이 **단계 사이로 끼어든다.**
> 조각내기 전에는 하루가 한 프레임이라 끼어들 틈이 없었다.

없앨 수 있나: **게임 전체를 마감 동안 얼리면** 없앤다 — 그건 지금 없애려는 그 스파이크를 그대로
되살리는 것이라 자기모순이다. 실체 30Hz(캐러밴·전투)를 멈춘 것은 그것들이 **econ 의 몸**이라
같은 순간을 봐야 했기 때문이고, 플레이어·NPC 의 걸음은 멈출 수 없다(그게 응답성이다).

⇒ 그래서 `test-tick-slicer ⑧` 은 "0" 이 아니라 **크기**로 판정한다(인구 ≤0.5% · 식량 ≤2%).
   하루치 생산의 잡티 수준이고, 순서가 크게 뒤틀리면(단계 하나를 빠뜨리면) 이 문턱을 훌쩍 넘는다.
★**재민 판정거리**: 6일 누적 0.6~0.8%(≈0.1%/일)를 "같다"로 볼 것인가. 볼 수 없다면 길은 하나뿐이다 —
   **마감 동안 NPC 생활층도 멈춘다**(움직임·렌더는 그대로, 일감 처리만 정지). 3초간 NPC 가
   일손을 놓는 셈이고, 하루 24분 중 0.2% 다. 하지만 그건 **행동 변경**이라 이 배치가 못 한다.

---

## B. 판단거리 (재민 확인 필요)

### B-1 ★★목표 "≤×2" 를 **판정에서 뺐다** — 확인 요청
`e2e-rtt` 는 ①상황 선행 ②창 안 p95 ≤ 대조군 1/3 ③루프 p99 ≤ 절반 **셋만 실패로 센다**.
재민 확정 목표 ≤×2 는 **수치와 미달 사실을 크게 찍되 pass/fail 에 넣지 않았다.** 이유:
```
  이 비율의 **분모가 로컬 평시 RTT** 다(망이 없어 5~36ms). 재민 실기의 분모는 망을 탄 **40ms** 였다.
  ⇒ 같은 조각 크기라도 로컬에선 비율이 크게 나온다. 비율은 환경이 정하고,
     배치가 실제로 산 것은 **더해진 지연(ms)** 이다. 그래서 둘 다 찍는다:
       마감이 더한 지연  대조군 +11,194ms → 현재 **+779ms**
       실기 환산(평시 40ms)  ×280.9 → **×20.5**   ⚠목표 미달
```
★그래도 **미달은 미달이다.** 러너를 빨갛게 두지 않기로 한 것은 내 판단이므로 여기 적는다 —
"목표를 못 채운 배치의 하네스를 초록으로 두는 게 맞는가"는 재민이 정할 일이다.
(빨갛게 두길 원하면 `e2e-rtt` 의 목표선 블록을 `ok(...)` 한 줄로 바꾸면 된다.)

### B-2 "장부 마감 중" 창이 **실측 2.4~3.0초**다 — 길이 확인
하루 24분에 2.4~3.0초(0.2%)다. 그 창에 들어온 **마을 요청만** 줄을 서고, 마감 직후 같은 순서로 흐른다.
플레이어에게는 `🏘️ 마을이 하루 장부를 마감하는 중이다 — 곧 처리된다` 한 줄이 뜬다.
* ⓐ 그대로 — 하루 한 번 3초, 디에게틱하다(현행).
* ⓑ 알림을 없애고 **조용히** 미룬다(3초면 못 느낀다). 대신 두 번 누르는 사람이 생긴다.
* ⓒ 창을 더 잘게(예산 ↓) — 총 시간이 늘어 창이 **길어진다**. 역효과다.
★이 목록(`VILLAGE_BOOK_MSG`)에 **낚시는 일부러 안 넣었다** — 챔질은 서버 시각 판정이라 미루면 판정이 망가지고,
어장 결손은 econ 이 원자 조각 안에서만 읽으므로 미룰 이유가 없다.

### B-3 조각 예산 16ms 가 맞는가
30Hz 프레임(33ms)의 절반이다. 나머지 절반은 이동·전투·AOI 가 쓴다. 실측상 **조각 하나가 이미
16ms 를 넘는 경우가 대부분**이라 예산은 지금 사실상 "한 프레임에 한 조각"으로 동작한다 —
A-1 수술 뒤에야 이 손잡이가 의미를 갖는다.

### B-4 ★마감이 **하루보다 길어지면** econ 날이 밀린다 — 실기에선 못 일어나지만 하네스에선 난다
```
  마감 중에는 새 날을 열지 않는다(`if (state.tickJobs) { _drainTickJobs(now); return; }`).
  그래서 마감(실측 2.4~3.0초)이 게임일보다 길면 **벽시계 하루를 건너뛴다** — econ 이 실시간보다 느려진다.
  실측(하루 5초짜리 하네스): 같은 40초 창에서 대조군 8회 마감 · 조각내기 6회 마감.
  ⇒ 실기는 하루가 **24분(1,440초)** 이라 3초 마감은 0.2% 다. 구조적으로 못 일어난다.
  ⇒ 다만 **하루를 짧게 세우는 하네스**(`VILLAGE_DAY_MS`)는 이 사실을 알고 써야 한다.
```
★대안(안 했다): 밀리면 따라잡기(한 프레임에 여러 날) — 그건 지금 막으려는 그 스파이크를 다시 만든다.

### B-5 이 컨테이너는 실기보다 **느리다** — 절대치를 그대로 읽지 마라
```
  실기(fly) 로그   econ_day  346~684ms
  이 컨테이너      econ_day  2,018ms (같은 50마을 · day 515)
  ⇒ 대략 **3~6배**. 단계 비율은 옮겨 가지만 **ms 는 옮겨 가지 않는다.**
     실기 환산: 남은 최대 조각 447ms → 대략 **75~150ms**.
```
★그래서 판정은 전부 **짝 비교**(같은 DB 스냅샷·같은 기계)로 짰다. 절대치 문턱을 쓴 곳은 없다.

### B-6 일틱이 **세계 나이에 따라 자란다**
```
  day 100  1,285ms → day 200  1,211ms → day 414  1,445ms → day 515  2,018ms
```
영토·농지·집이 늘수록 `_lifeAddHouseSite`·`_terrGrow` 가 훑을 셀이 는다. **선형이 아니다.**
A-1 을 손보지 않으면 오래 산 서버일수록 나빠진다 — 이게 이 회부에서 가장 급한 대목이다.

---

## C. 이번 배치가 남긴 손잡이 · 관측창

| 손잡이 | 기본 | 뜻 |
|---|---|---|
| `VILLAGE_TICK_SLICE_MS` | **16** | 한 프레임의 조각 예산(ms). **`0` = 양보 끈을 뽑는다**(종전 동작 = 두 하네스의 대조군) |

`GET /perf` 가 내주는 것(계측 전용 — 아무 동작도 바꾸지 않는다):
```
  sliceMs              지금 어떤 예산으로 도는가(대조군 판별)
  loop {p50,p95,p99,max,n}   이벤트 루프 지연(`monitorEventLoopDelay`) · `?reset=1` 로 영점
  econTick.last        day·total·wall·frames·**frameMax**·chunks·maxChunk·maxChunkAt·order·queued
  econTick.stages      단계별 p50/p95/max (life 하위 6종 + `1마을:*` = 마을 한 곳 최댓값 포함)
  events[kind=econ_day]   하루 총합(마감 시점) — `econ_frame` 은 **한 프레임 몫**(이름을 갈랐다)
```
⚠종전 `econ_day` 마크는 "한 프레임"이자 "하루"였다. 조각내기 뒤로 그 둘이 갈라져서
**이름을 갈랐다**(`econ_frame` 신설). `scripts/rtt-metrics.js` 는 `econ_day` 를 그대로 읽는다.

---

## D. 실측 표

### D-1 조각내기 전/후 (같은 DB 스냅샷 · `test-tick-slicer` **17/0**)
```
  대조군(끈 뽑음)  527일 · 총 2,977ms · 조각 203 · **프레임 1** · 한 프레임 최대 **2,977ms**
  조각내기(16ms)   527일 · 총 3,023ms · 조각 196 · **프레임 30** · 한 프레임 최대 **489ms**
  이벤트 루프 최대 막힘  13,631ms → **1,334ms**
  하루 총 일감      ×1.02 — 쪼갠다고 일이 줄지 않았다(줄었으면 뭔가를 안 한 것이다)
  단계 순서         econ→pop→terr→ore→wx→bcast→caravan→events→save→life→dist  (양쪽 동일)
  ★econ 상태       같은 마을·같은 날 대조 50곳 — **A/B 다름 0** (A/A 바닥 잡음 8) → 위 A-4
```
★계측은 **p99 가 아니라 최댓값**으로 판정한다. 두 팔의 막힘은 횟수가 다르다(대조군은 하루 한 번 크게,
조각내기는 서른 번 작게) — p99 는 그 비대칭에 낚인다. 실제로 첫 판에서 대조군 p99 가 **24ms** 로
나왔다(같은 판의 최대는 12,994ms). 플레이어가 겪는 건 "가장 오래 막힌 한 번"이다.

### D-2 RTT 짝 비교 (진짜 WS · `e2e-rtt` **5/0 · 목표 미달**)
```
                     평시 중앙   마감 창 안 p95(최대)   루프 최대 막힘
  대조군(끈 뽑음)      139ms      9,899ms (11,907)     13,883ms
  조각내기(16ms)       200ms        728ms  (1,076)      1,341ms
  ⇒ 창 안 p95 **×13.6 개선** · 마감이 더한 지연 +9,760ms → **+528ms**
  ⇒ 실기 환산 비율(평시 40ms) ×245.0 → **×14.2**  ⚠목표 ≤×2 미달 (바닥 = 조각 하나 · 위 A)
```
★문턱을 1/3 이 아니라 **1/2** 로 둔 이유: 대조군의 막힘은 **하루에 딱 한 번**이라 길이가 판마다
크게 흔들린다(실측 2,419~12,928ms). 조각내기 쪽은 여러 프레임에 퍼져 안정적이다(728~1,344ms).
비율이 ×2.6~×14 를 오가므로 1/3 로 조이면 **없는 회귀를 보고**한다(한 번 그랬다).
막힘의 크기 자체는 ③(루프 최대 막힘 ≤1/3)과 `test-tick-slicer ③`(한 프레임 ≤1/3)이 따로 지킨다.
표본은 100ms 마다 500개 — 200개로는 p95 자체가 흔들렸다.

### D-3 tickWorldV2 **내부** 분해 (V8 CPU 프로파일 · econ 무접촉 관측)
```
  마을 독립 루프  86.7ms/일  65.6%   ← tickVillage 81.3 · tickDecay 3.9 · tickSubsistence 1.2 …
  마을 결합       32.9ms/일  24.9%   ← tickTradeV2 19.3 · tickCaravansV2 13.6
  자기 자신       12.3ms/일   9.3%
  ⇒ §2-③(econ 함수 추출)을 쓰면 원자 조각이 99ms → 약 33ms 가 된다.
     **안 썼다.** 생활층 조각이 227ms 라 econ 을 쪼개도 최대 조각이 안 내려간다 —
     econ 을 건드리는 값이 없었다. 필요해지는 날은 A-1 수술 **뒤**다.
```
