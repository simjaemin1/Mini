# durango-mini 다음 세션 인계 (2026-08-03)

아래 전문을 새 세션 첫 메시지로 붙여넣으면 된다.

---

너는 durango-mini(한국 청동기 송국리 문화기 오픈월드 경제 시뮬) 작업을 이어받는다.
아래 규칙·원칙·상태를 전부 읽고, [작업 배치]를 위에서부터 순서대로 진행해라. 질문하지 말고
네가 판단해서 진행하되, [회부만 할 것]은 절대 구현하지 말고 문서만 써라. 튜닝은 자유.
**정확성이 속도보다 우선.** 다 되면 결과를 정리해서 보고해라.

## 0. 부트스트랩

* 레포: `https://github.com/simjaemin1/Mini.git` → `/root/minirepo` 로 클론, `npm install`.
* GitHub PAT: 디바이스 파일 `~/Mini/durango-mini/.git/claude-pat.txt` (93바이트).
  `device_stage_files` 로 스테이징 → 셸 변수로만 읽고 즉시 `unset`. PAT 를 채팅·레포·메모리에
  절대 남기지 마라. push 출력은 반드시 `sed "s/${PAT}/***/g; s/github_pat_[A-Za-z0-9_]*/***/g"` 로 거른다.
* 시작하자마자 `git log --oneline -5`. 기준 커밋: **`f0a1c2d` 급(2026-08-02 배치 완료 시점)** —
  실제 HEAD 를 확인하고 그 뒤 커밋들을 먼저 읽어 상태 인식을 갱신해라.
* 먼저 읽을 것: 이 파일 + `보고_야금과시대축_배치.md` + 회부 4종
  (`회부_구리부존과_원석적체.md` · `회부_시딩변경과_DB리셋.md` · `회부_마을소멸_시딩.md` ·
   `회부_플레이어_제련_노모델.md`) + 설계안 2종(`설계_배합과교역_한단위_통합.md` · `설계_철검_위세축.md`)
   + 조사 1종(`조사_말_사역_배선.md`).

## 1. 절대 규칙 (사용자 확정 — 위반 금지)

1. **배포는 사용자만 한다.** SSH 시도 금지. fly 배포 명령 금지.
2. **force push 금지.** ~~DB 리셋 금지~~ → ★**2026-08-02c 재민 확정 "db 초기화해도 돼" 로 해제**:
   **로컬·디바이스 DB 는 `DB리셋_절차.md` 대로 백업 후 리셋 가능**하다(이번 배치에 실행함).
   **라이브(fly)의 `world-hanbando.db` 리셋은 여전히 재민만** — 플레이어 건축물·사유지가 전부 사라진다.
   디바이스는 `rm` 이 안 되므로 `_to_delete/` 로 **이동**한다.
3. NPC 로직은 **랩 verbatim 이식만**. econ 계수 임의 보정 금지
   (A/B 재현용 env 손잡이는 허용 — 기본값이 채택값이어야 하고 `lab-wiring-check [A2]` 가 검사한다).
4. 디바이스(`~/Mini`)의 레포 파일 동기 금지 — **랩 2종(전쟁실험실·마을실험실.html)과
   map-editor.html 만** mtime 가드로 편집한다. 그 외 디바이스 파일은 읽기 전용.
5. 스케줄 작업은 claude-code-remote MCP 만(로컬 cron 금지).
6. 이주(`tickMigration`)는 폐지됐다(2026-07). 켜지 마라. 강제소개(`tickForceEvacuation`)도 회부 없이 켜지 마라.
7. **설계 판단은 구현 전에 회부. 반쪽 구현보다 이월.**

## 2. 검증 원칙

* **본 게임이 이미 가진 것을 랩에서 다시 만들지 마라.** 실지도 랩(`scripts/econ-lab-real.js`)은
  `villages.__labProbe`·`createWorldV2`·`tickWorldV2` 를 그대로 부른다.
* 모든 회귀·측정 전에 `node scripts/lab-wiring-check.js`. `scripts/econ-regress.js` 가 이를 선행 실행한다.
* 엔진(specialty/economy-sim/-v2/era) 수정 후: `node sim/build-econ-bundle.js`(`--check` 8/8) →
  `node sim/inline-engine.js <랩들>` → `node scripts/lab-smoke.js` → 디바이스 동기.
  번들·랩 인라인 블록을 **손으로 기우지 마라**(자동 생성물).
* 하네스 전원 PASS 유지:
  `test-alloy` · `test-valuechain` · `test-mining`(⑨ 다광종 E2E 포함) · `test-armor-cap` ·
  `test-picker-tables` · **`test-furnace`**(노·숯가마 실서버 E2E) · **`test-assay`**(감정 몬테카를로) ·
  `test-building-anchor`.
* A/B 는 최소 3시드(`LAB_SEED=n`). 좀비 마을 칼날 평형 때문에 단일 시드는 판정 불가.
* **커밋 즉시 푸시.** 컨테이너가 재활용되면 미푸시 커밋이 날아간다.
* `pkill` 함정: `pkill -f "econ-lab-rea[l]"` 처럼 브래킷을 쓰고 같은 명령줄에 대상 문자열을 넣지 마라.
* ★**하네스가 틀리면 조용히 통과하는 게 아니라 없는 결함을 보고한다.** 이번 세션에
  test-assay 가 두 번(FP 누설 기준·낱말 기저), test-mining 이 한 번(셀/픽셀 단위) 그렇게 틀렸다.
  **기준을 먼저 검증해라.**
* ★**그리고 반대로도 틀린다 — 자명한 상황을 골라 조용히 통과한다**(2026-08-02c).
  `test-valuechain` ⑥ 1차 작성이 순수출가가 **음수**인 상황을 골라 `max(0,·)` 에 뭉개진 `0 = 0`
  으로 통과했다. **검사 상황이 실제로 그 코드를 밟는지**(여기선 값의 양수성)를 assert 로 걸어라.
* ★**인계 문서의 진단도 실측으로 검증해라**(2026-08-02c). 배치 4 인계가 적어 준
  "좀비 덫 3중 잠금"은 틀렸다 — 좀비 마을은 굶지 않았고(국고 현금 15,000·캐러밴 61회),
  지목된 게이트를 이미 통과하고 있었다. **손잡이를 만들기 전에 궤적부터 재라.**
* ★★**계약도 실행도 멀쩡한데 화면에서 도달 못 하는 층이 하나 더 있다**(2026-08-02d).
  서버 E2E 56/0 이 통과하는 동안 **플레이어는 노를 한 번도 지을 수 없었다** — 진입 버튼이
  `display:none` 컨테이너 안에만 있었다(키 바인딩 0곳). `scripts/e2e-metallurgy.js` 가 그 층이다.
  **UI 를 건드리는 배치는 실클라 E2E 로 끝내라.**
* ★★**계측기도 '사본 금지' 대상이다**(2026-08-02f — 내가 낸 오진의 원인).
  배치 7 이 "지도에 철 0개"라는 **틀린 회부**를 냈다. 원인은 계측 스크립트가
  `hanbando-terrain.json` 을 **직접 파싱**하면서 다광종 dict(`o.minerals`)만 훑은 것이다.
  정본 판독기 `terrain.oreMineralAt` 는 dict 가 없으면 `o.mineral` **문자열로 폴백**하는데
  그 갈래를 안 밟아 **단일광종 광맥 362개(철 자잘 184개 전부)를 통째로 놓쳤다.**
  엔진에는 "사본 금지, 정본 함수를 불러라"를 그렇게 지켜 놓고 **계측기에서 어겼다.**
  ⇒ 지형·부존·가격을 읽는 계측기는 **JSON 을 직접 열지 말고 정본 함수를 불러라.**
  ⇒ 그리고 같은 실험에서 구리·납 광맥만 캐 놓고 "철은 한 톨도 안 나온다"고 **일반화**했다 —
     바로 아래 줄의 "전수로 검사해라"를 정확히 반대로 어긴 것이다. **일반화 전에 그 종을 직접 캐라.**
* ★**전수 조사가 필요한 게이트는 전수로 검사해라**(2026-08-02d). "말이 막히나"가 아니라
  **"말만 막히나"** 가 기준이다 — 화이트리스트로 짰으면 13종 중 11종이 조용히 막혔을 것이다.
* ★★**"무언가가 변하는가"를 재는 하네스는 — 변할 수 있는 대상을 골랐는지 먼저 확인해라**(2026-08-27 거래소).
  `e2e-trade ⑤`가 "내가 판 물건 값이 떨어지는가"에서 **두 번 헛짚었다**:
  ⓐ 처음엔 "마을이 팔 수 있는 양이 가장 많은" 품목을 골랐는데, 그건 곧 **가장 과잉인 품목**이라
     그림자가격이 `PRICE_ADJ_MIN` **바닥에 붙어** 재고가 늘어도 값이 안 움직이는 쪽이었다.
     **자기가 못 움직일 걸 골라 놓고 "안 움직인다"고 보고한 것**이다.
  ⓑ 그리고 "판 것 ↓ **그리고** 산 것 ↑"을 **한 짝에서 동시에** 요구했더니, 바닥 품목 하나가
     두 판정을 다 죽였다(실측: 가죽이 0.03226→0.0164 로 반토막 났는데 상대가 바닥이라 전체 실패).
     ⇒ **판정은 쪼개고, 각자 후보를 순회하게 해라.**
* ★★**하네스는 자기 행동이 실제로 일어났는지 먼저 확인해라**(2026-08-27 거래소).
  같은 하네스가 세 번째로 헛짚은 이유: 받을 것 후보 6종 전부 `재고 10.41 → 10.41` 이었는데
  그건 "값이 안 움직였다"가 아니라 **교환 자체가 거절당한 것**이었다(비싼 물건을 내려는데 마을이
  값을 못 치르면 거절된다). 게다가 **실패엔 서버가 시세표를 다시 안 보내서 화면도 그대로**라,
  "아무것도 안 변했다"와 픽셀 하나 다르지 않게 보인다.
  ⇒ 전후 비교 전에 **행동의 성사 여부를 독립적으로 세라.** 그리고 그 카운트를 **알림 로그로 세지 마라** —
    클라 알림은 40칸 링버퍼라 앞을 버린다(성사한 거래가 조용히 0건으로 읽힌다). 재고처럼 **상태로** 세라.
* ★★**하네스는 "몇 초 걸었다"가 아니라 "얼마나 갔다"로 적어라**(2026-08-27 무게).
  `test-guest-rejoin` 이 "미저장 드리프트 > 200px" 를 만들려고 **4초 고정**으로 걸었는데,
  무게 모델이 들어오며 검사 플레이어가 216% 과적(×0.58)이 되어 250px → **145px** 로 줄었고 전제가 깨졌다.
  **제품은 옳았고 하네스가 자기 걸음 속도를 상수로 박아 둔 것**이었다.
  ⇒ 목표를 **거리로** 적고 도달할 때까지 걸어라. 이동 배율을 건드리는 배치마다 안 깨진다.
* ★**"표에 없는 값"은 지어내지 말고 유도하고, 유도한 목록을 회부에 적어라**(2026-08-27 무게).
  고증표가 안 덮는 플레이어 전용 아이템 7종의 kg 을 표의 앵커에서 유도하고 근거를 한 줄씩 남긴 뒤
  `회부_무게_다음층.md` B-3 에 그 목록을 그대로 옮겼다. 조용히 정한 수는 나중에 아무도 못 고친다.
* 좌표 단위: 광맥 `center`·`radius` 와 `terrain` 의 ore 함수는 **픽셀**. 플레이어 격자는 셀(=px/32).
* CPU 는 **2코어**다. 800일 랩 한 판이 ~4분. 6판을 동시에 던지면 각 15분씩 걸린다 — 순차가 낫다.

## 3. 현재 상태

* **노(爐)·숯가마**: 완결. 사유지 셀 단위 판정(`_claimFootprint`) · 3단계(괴련로는 별도 자재) ·
  숯가마 2단계(통나무 3→숯 4) · 3D 스프라이트 6종 · `test-furnace.js` 56/0.
  클라 메뉴는 `zonePublicMeta().era.furnaces` 로 **알려진 노만** 노출.
* **운철**: 지표 청크 자원(결정론 해시 · 대륙 42개) · 제련 불필요 · 등급 0.904(합금 모델 파생).
* **시대 축**: `era.js` 그대로. `scripts/era-rehearsal.js` 로 열기 전 리허설 가능.
  ★실측 — **시대를 열어도 NPC 경제는 0.0% 변화**(NPC 주요 광맥에 철 0).
* **econ**: 땅맞춤 초기 부존 채택(소멸 −55%·인구 +46%). `LANDFIT=0` 으로 채택 전 재현.
* ★★**소멸 0 달성(2026-08-02c)** — 실지도 3시드 800일 **소멸 0/19 · 좀비 0 · 인구 3,415**
  (배치 시작 1,610 · 소멸 1.33 · 좀비 8.33). 채택 손잡이 **STONE_NET**(재료 결손이면 채집꾼을
  기근보다 먼저) + **PRODK_CAP**(prodK 를 용량 기준으로 = fuelK 동형). **순간이동 미사용.**
  기각: TOOLBOOT(비트 동일=무효) · SWITCH2(무효). 상세 `보고_소멸0과_한단위통합.md`.
* ★**배합↔교역 한 단위 통합 채택** — `ALLOY_OPP` 기본 ON. `opp(r)=max(현지가, 최고 순수출가)`,
  순수출가는 `tickTradeV2` 발주 EV 의 항 그대로(v2 가 `world.netExportFn` 주입 — 사본 금지).
  합금 등급 평균 **0.79 → 1.21**(표준 청동 1.0 을 넘겼다). 무기 총량 −3~8%. 되돌리기 `ALLOY_OPP=0`.
* **v2 CLI 5시드 회귀 기준선(갱신)**: 인구 **2,079** · 무기 1,560 · 갑옷 132 · 소멸 **0/30**.
  (배치 전 2,001 / 1,834 / 198 / 0-30. `STONE_NET=0 PRODK_CAP=0 ALLOY_OPP=0` 으로 정확 재현 가능)
* **DB**: 로컬·디바이스 리셋 **완료**(2026-08-02c). 로컬은 재시딩 실증(선별 19 → 18곳 시딩,
  땅맞춤 부존 실발효 확인). 디바이스 원본은 `_to_delete/db-reset-20260802-0413/`, 백업은 `.bak-…` 로 원위치.
  **라이브(fly) 리셋은 여전히 재민만.**
* ★**배치 5(2026-08-02d) 채택**: **SHIELD_AGE**(보호막을 마을 나이 기준으로 — 랩 비트 동일, 라이브 구멍 수리) +
  **SHIELD_SOFT**(보호막이 삼키지 않고 ×0.25 감쇠 — 절벽→경사). 실지도 3시드 **인구 3,424 · 소멸 0/19 · 좀비 0**.
  `SHIELD_AGE=0`/`SHIELD_SOFT=0` 으로 채택 전 재현.
* ★**era.canTame 배선 완료**(말만 차단 — UNLOCK 표 기준, 하드코딩 0) · `test-tame.js` 11/0.
* ★**실클라 E2E 신설**: `node scripts/e2e-metallurgy.js` (18/0, 진짜 Chromium). 실행하면 central 3010 ·
  zone 3020 을 임시 DB로 띄운다. **이 하네스가 "플레이어가 노를 지을 방법이 없다"를 잡았다** — 회귀 가드다.
* **v2 CLI 5시드 회귀 기준선(갱신)**: 인구 **2,120** · 무기 1,369 · **품질보정 무기 422** · 갑옷 135 · 소멸 **0/30**.
* ★★**기준선 — 이 세 줄을 이어받아라(2026-08-02f 갱신)**:
  ```
    실지도 3시드   800일 : 인구 3,549 · 소멸 0/19 · 좀비 0 · 무기Q 594 · 산골부(재화) 379.0 · 확장셀 3,756 · 현금/인 39.9
    실지도 3시드 1,600일 : 인구 4,502 · 소멸 0/19 · 좀비 0 · 무기Q 1,150 · 산골부(재화) 517.3 · 확장셀 16,684 · 현금기울기 53.1
    실지도 3시드 2,400일 : 인구 4,916 · 소멸 0/19 · 좀비 0 · 무기Q 1,409 · 확장셀 29,167 · 현금/인 127.5
    v2 CLI 5시드   800일 : 인구 2,616 · 소멸 0/30 · 무기Q 651 · 확장셀 13,473
  ```
  ★**2,400일 줄 추가(2026-08-03c 배치 10).** 인구는 아직 완전 평탄은 아니지만 **감속 수렴**한다
  (구간 증가 93.7 → 18.9 → 6.6 → 6.1 → **2.9%**). 소멸 0 · 좀비 0 은 **2,400일 전 구간 유지**.
  ★★**현금 기울기가 되돌아온다** — 구간별 26.5 → 46.8 → **60.6** → 51.5 → **60.0**.
  배치 8 이전(`TREASURY_BUY=0`)이 59.8 이었으니 **장기에서는 사실상 같다.**
  배치 8 의 −11% 는 800~1,600일 구간의 일시적 이득이었다. 셀 비용이 `size^1.3` 으로 커져
  곳간 잉여로 못 채우게 되고, 원자적 규약 때문에 매수가 통째로 실패로 돌아서기 때문이다.
  ⇒ **확장은 국고 현금의 충분한 소비처가 아니다**(`회부_국고_2차_소비처.md` §4 — 내 의견이 바뀌었다).
  ★**2026-08-03a `TREASURY_BUY` 채택으로 갱신된 값이다.** 채택 전 세 줄(3,424 / 3,991 / 2,120)은
  `TREASURY_BUY=0` 으로 **정확히 재현**된다. 800일 무기Q·산골부는 채택 전보다 **낮다** —
  확장 투자 국면의 일시적 비용이고 1,600일에 3/3 시드 역전한다(보고_국고현금_배선.md §2·§3).
  ★**1,600일이 종점이 아닐 수 있다** — 기준선은 1,200일에 평탄해졌는데 채택본은 1,200→1,600 이
  아직 +6.2% 다(확장이 K 를 밀어올린다). 다음에 장기를 잴 땐 2,400일도 후보다.
  ★★**"산골 1인당 부 18.1" 은 폐기된 값이다** — 배치 6 의 그 지표는 `FE(v.storage)` 가
  `totalFoodEquivalent(v)` 에 곳간을 마을인 척 넘겨 **항상 0을 던지고 catch 에 먹혀**,
  결과적으로 `_cash`(엔진에서 **쓰기 전용**인 누적 교역세) 하나만 세고 있었다.
  지금은 `BASE_VALUE` 환산 **재화**만 세고 현금은 별도 열이다(`산골현금`). 두 열을 섞지 마라.
  ★**1,600일까지 소멸 0 이 유지된다**(400/800/1200/1600 = 1,833/3,427/3,980/3,984 — 평탄화이지 붕괴 아님).
  장기 러너: `AB_DAYS=1600 scripts/ab-extinct.sh <태그>` → `node scripts/ab-longcurve.js <태그> 400`.
  ★**무기는 수량이 아니라 품질보정 총량 Σ(수량×`_weapQ`)로 판정한다**(배치 6 ②). 수량만 보면
  합금 등급 개선(0.79→1.21)을 퇴보로 오독한다. `ab-summary.js`·`econ-regress.js` 가 그 열을 찍는다.
* ★**배치 6 채택**: 말 사역(`haulMul` — 닫힌 시대 ×1 = 비트 동일 · 열리면 운반비 ×0.62) ·
  노·숯가마 **조업 진척 계약**(장입→시간→출탕, 시간의 근거는 노 온도) · 야금 아이콘 8종 · 석재 **기재** 수정.
* **미해결(회부 대기)**: ①광산촌 시딩 배제 ②구리 부트스트랩 닫힌 고리 ③~~수입 원석 제련 불가~~(해결)
  ④라이브 DB 리셋 ⑤~~배합↔교역 통합~~(채택) ⑥철검 위세축(설계 완료) ⑦말 사역 소비처
  ⑧~~day365 보호막~~ **(해결 — SHIELD_AGE+SHIELD_SOFT 채택)**
  ⑨~~석재 sink 미규명~~ **(규명 완료 — 정상 교역이었다.** 다만 원장이 **이중 차감**을 잡았고 그건 회부 중:
  `회부_석재_이중차감.md`)
  ⑩~~노 조업 시간 계약~~ **(해결 — 진척 계약 신설, 시간의 근거는 노 온도)**
  ⑪~~무기 총량 하락 추세~~ **(종결 — 품질보정 총량으로 재니 배치 3 대비 +63%)**
  ⑫~~야금 아이콘 8종~~ **(배포 완료)**
  ⑬~~구리 축 3건 미착수~~ **(2026-08-02f 전건 실측 완료 — 셋 다 기각)**:
  ①광맥 슬롯 `VEINSLOT=2` 인구 −18.7%(구리조차 순감) ②구리 파생 투입수요 `COPPER_DERIV` — §아래
  ③원석 교역 금지 `ORE_TRADE_BAN=1` 청동무기 −71%인데 원석 적체는 −4%뿐.
  손잡이 3종 전부 코드에 남아 있고 기본 OFF 다(재설계 시 재측정용).
  ⑬-b ~~부얼타운 재민 대기~~ **★재민 확정(2026-08-02f): 보존·OFF 유지. 켜지 마라.**
  판정 근거를 정확히 적는다 — "값이 비싸다"가 아니라 **"목표를 못 이룬다"**다:
  광산6 은 3/3 시드 **생존하지만 55명으로 연명하고 주조를 못 한다**. 늘어난 주조 +20% 는
  **농촌2**에서 났다(목적이 아니라 부작용). 즉 무기Q −30% 를 치르고 사는 것이
  "광산 도시"가 아니라 "남의 마을 주조 한 곳"이다. 코드·손잡이는 남긴다.
  ★**광산 도시를 정말 원하면 필요한 건 시딩 슬롯이 아니다.** 부존 실측:
  구리 광맥 126(면적 56,029) vs 주석 15(12,815) = **4.4:1** — 표준 청동 Cu88Sn12 가 요구하는
  7.3:1 보다 **구리가 오히려 부족**하다(econ 재고도 주석 318 > 구리 167). 주석은 남는데
  `TIN_EXPORT_KEEP_PC=1.2`/`SURPLUS_PC=0.3` 이 **의도적으로 묶어 둔** 것이다
  ("무산지 마을은 만성 주석 기근" = 청동 희소성 장치). COPPER_DERIV 가 등급을 깎은 것도 이 때문 —
  구리만 퍼뜨리고 주석은 묶여 있으니 무산지 마을이 얇은 배합으로 무른 검을 만든다.
  ⇒ **구리 축과 주석 유통은 짝이라 따로 재면 둘 다 기각으로 나온다.** 다시 잰다면
  `COPPER_DERIV` × `TIN_EXPORT_KEEP_PC` **2×2**(3시드 4판 ≈ 48분)여야 하고, 그건
  "청동 = 소수 엘리트 표지"를 직접 푸는 것이라 **그 자체가 세계관 판단**이다.
  ⑬-c **구리 파생 투입수요는 게이트가 옳아야 산다** — 순동 등급 0.466 < 마제석검 0.5 라
  **첫 구리 한 덩이는 정말 살 값어치가 없다**. 구리는 합칠 것(주석)이 있을 때만 값을 한다.
  현재 형태는 "가진 금속으로 짤 최선 배합"으로 판정한다(`_meltSearch` 공용 헬퍼).
  ⑯~~노 조업 시간 상수 미측정~~ **(해결 — `pace-metallurgy.js` 실측. 180/240초 그대로 채택.**
  **두 대기 모두 채광 한 덩이(60초)보다 길어 '다녀올 일'이 되고 합계 6.5분 ≤ 15분)**
  ⑰ ~~국고 현금이 쓰기 전용~~ **★★완전 종결(2026-08-03d — 재민 세금 캐논)**:
  *"교역 세금은 물물교환할 때 드는 물건이고, 방문한 쪽이 더 낸다."*
  ⇒ 실물 차원은 이미 그렇게 구현돼 있고(방문자 매출 −TAU·매수 +TAU, 차액이 도착 마을 곳간에 남음),
  `_cash` 는 화폐가 아니라 **미상환 세곡 채권 장부**다. 장부는 쌓이는 게 정상이라
  배치 10 의 "기울기가 60 으로 되돌아온다"도 결함이 아니다. **소비처를 만들 필요가 없다** —
  `회부_국고_2차_소비처.md` 의 선택지 (가)~(라) 전부 무효. `TREASURY_BUY` 는
  "국고가 채권으로 곳간에서 실물을 찾아가는 **상환**"으로 재해석(코드 무변).
  ★UI 에는 표시하지 않는다 — 배치 6 이 이 값을 "부"로 오독한 전례가 있다.
  ㉑ ★**마을 건립 초기 인구** — `회부_마을건립_초기인구.md`. **재민 결정 대기(1순위).**
  이것만 정해지면 건립 계약은 터 잡기 선례로 바로 간다.
  (아래는 배치 8 시점 기록)
  ~~⑰ 국고 현금 — `TREASURY_BUY` 채택(기본 ON)~~:
  확장 정지 줄에서 현금으로 곳간 잉여를 사서 채운다. 새 재화·직업·효과 0.
  1,600일 인구 +12.8% · 무기Q +10.5%(3/3) · 소멸 0 유지 · 현금 기울기 59.8 → 53.1(꺾였다).
  규약 4개: 그림자가격 정본 사용 · 현금 실감소 · **가계 유보(RESERVE_PC) 초과 잉여만** · 원자적(외상 금지).
  ⚠남은 것: 현금은 **여전히 순증**한다(기울기가 −11% 로 꺾였을 뿐 0 이 아니다).
  ★**회부 완료(2026-08-03b)**: `회부_국고_2차_소비처.md`. 내 의견은 **지금은 (라) 유지** —
  확장이 아직 자라는 중이라 최종 잔여량이 확정 안 됐고(1,600일이 종점이 아니다),
  공공 건축은 **시대 축과 얽혀** 지금 정하면 시대와 함께 또 바뀐다.
  **2,400일에서 확장이 멈춘 뒤의 기울기**를 보고 정하는 것이 순서다.
  (아래는 채택 전 기록)
  ~~★**국고 현금(`_cash`)이 쓰기 전용이다**~~ — 교역세로만 늘고 줄어드는 경로가 코드에 없다
  (1,600일에 1인당 55.5 → 104.0, 시간에 대략 선형). 행동 무영향이라 버그는 아니다.
  ★★**재민 확정(2026-08-02f): "확장에 현금 배선" — 1순위.** 이건 화폐 설계가 **아니다**:
  **재화 국고는 이미 공공 사업 재원이다.** `economy-sim.js:1003-1006` 이 영토 확장(마을 셀 개척)에
  `treasury.food/wood/stone` 을 쓰고, 모자라면 그날 `return` 으로 멈춘다. `_cash` 만 그 회로 밖에 있다.
  ⇒ **확장이 재화 부족으로 멈추는 그 줄에서, 현금으로 그 재화를 사게 한다.**
    새 재화·새 직업·새 효과 **0** — 교역 기구를 그대로 재사용한다.
  · 손잡이 `TREASURY_BUY` **기본 OFF** + 실지도 3시드 A/B
  · 판정 열 **2개 추가**: **확장 셀 수** · **현금 잔고 기울기**(1,600일에 선형이던 게 꺾이는가)
  · 기준선 2줄(인구·소멸·좀비·무기Q·산골부)은 그대로 지켜야 채택. 확장이 빨라져 인구가 오르면
    그건 개선이지만 **기준선 갱신**이므로 1,600일 재측정까지 같이 해야 한다.
  · ⚠과확장 주의 — 슬롯K 가 늘면 식량 압박이 생길 수 있다. `ab-summary` 에 확장 셀 수를 꼭 찍어라.
  · 이게 실패하면 그때 **세금 폐지(TAU 를 iceberg 손실로 흡수하고 `_cash` 제거)**를 검토한다.
    순서가 이래야 하는 이유: 폐지는 되돌리기 어렵고 배선은 손잡이로 되돌아간다.
  ⑲ ★★**시대 SCHEDULE — 재민 확정 "지금 안 열어"(2026-08-03). 결정 대기 아님.**
  `early_iron` 줄은 주석 처리된 채로 둔다. **SCHEDULE 을 건드리지 마라.** 말 분리 조정도 보류.
  그래서 배치 6 말 사역 · 배치 7 노 사슬 · 철검 위세축은 **의도적으로 잠들어 있다**(결함 아님).
  ~~(구 문구) 시대 SCHEDULE 이 아직 안 열려 있다~~ — `server/era.js` 의 `SCHEDULE` 에서
  `early_iron` 줄이 **주석 처리**돼 있다. 그래서 배치 6 **말 사역**(운반비 ×0.62), 배치 7 **노 사슬**
  (철기 도가니로 2장입 5분), **철검 위세축**이 전부 **잠들어 있다** — 만든 것 중 상당 부분이
  아직 한 번도 세계에서 작동한 적이 없다. 인계 §5 "회부만 할 것"이라 세션은 못 연다. **재민 결정.**
  ⑳ **2,400일 장기** — 배치 8 채택본이 1,600일에 아직 자라는 중이다(1,200→1,600 +6.2%,
  기준선은 +0.1%). 확장이 멈추는 `s*` 와 그 뒤 현금 기울기가 ⑰-b(국고 2차 소비처)의 입력이다.
  ⑱ ~~노에 넣을 것이 이 세계에 없다~~ — **오진으로 철회**(2026-08-02f 재민 검증 세션).
  전수 스크립트가 다광종 dict(`o.minerals`)만 훑어 **단일광종 광맥 362개(철 자잘 184개 전부)를
  놓쳤다**. 정정 후 실채광 실증: 철 자잘 광맥에서 채광→선광→정광→노 출탕까지 주입 0 으로 통과
  (pace-metallurgy 12/0). 노의 입력은 처음부터 세계에 있었다 — 배치 1 설계 그대로.
  `회부_노의_입력이_지도에_없다.md` 의 철회 머리말 참조. ★교훈: **전수 조사는 정본 판독기와
  같은 fallback 을 밟아야 한다**(oreMineralAt 은 dict 없으면 `o.mineral` 로 떨어진다).
  ⑭ **`ALLOY_OPP` 의 무기 대가** — 품질보정 −6%(실지도)/−18%(CLI) vs 합금 등급 +53%. 되돌리기 `ALLOY_OPP=0`. 재민 판단 대기.
  ⑮ **석재 손잡이 2종**(`STONE_MAINT`·`STONE_MAINT_UNIFY`) 기본 OFF 보존 — 석재 시장 재설계 시 재측정.
  (⑯은 위에서 해결로 표시했다)

## 3-b. ★2026-08-03b 배치 9 — 청동 축 종결 · 문서 유효성 점검

### 청동 희소성의 원인이 규명됐다 — **"구리가 안 움직인다"** 하나다

가설 셋(①재료 부족 ②매칭 실패 ③부트스트랩)이 **전부 틀렸다.** 실지도 3시드 800일 실측:

```
  시드   구리보유  주석보유  배합성립  관문통과  구리 발주수출  주석 발주수출
  1020     2        3        2        2         0.0          0.0
     7     1        4        1        1         0.0          0.0
    42     1        3        1        1         0.0          0.0
```

* **관문(`_smithBeatsMason`)은 안 막는다** — 구리를 가진 마을은 100% 통과(청동비용 0.006~0.084 vs 석검 337~362).
* **주석은 퍼진다** — 부존 0 인 어촌2 가 주석 219.6 보유. 주석은 병목이 아니다.
  ★배치 7 이 적은 "구리 축과 주석 유통은 짝"은 **틀렸다** — 정정했다.
* **금속은 발주 교역에 안 나온다**(3시드 전부 0). `exportBy` 키가 전부 식량·원료다.
  금속은 **귀환화물(대금)로만** 움직이는데, 구리 산지는 캔 구리를 **전량 주조에 써서** 글럿이 안 나고,
  글럿이 없으면 대금 후보(`ratio = pFrom/pTo` 최대)로도 안 뽑힌다. 주석은 남으니까 실려 나간다.
* ⇒ **구리는 산지에서 태어나 산지에서 죽는다.** 회부 ①의 선택지 (다) *"청동은 산지의 독점,
  교역은 완제품(청동검)으로만"* 이 **이미 실현된 상태**다. 고를 것이 없다.
* 배치 7 의 두 기각(COPPER_DERIV·BOOMTOWN)도 이걸로 설명된다 — 둘 다 구리를 **더 만들거나 더 원하게**
  했지 **움직이게** 하지는 않았다. 상세: `회부_구리부존과_원석적체.md` §⑤.

### 세계관 판단 — **"청동 = 소수 엘리트 표지" 유지**(설계된 것이지 사고가 아니다)

바꾸려면 산지의 소비를 줄이거나(=주조를 없애거나) 금속을 발주 교역 후보로 올려야 하는데,
후자가 곧 그 설계를 뒤집는 것이다. **재민 결정이 필요하면 그 한 줄이 전부다.**

### ★문서 유효성 점검 — 5종이 낡아 있었다(머리말 달고 종료)

| 문서 | 낡은 전제 | 지금 |
|---|---|---|
| `회부_노_조업시간_계약` | "시간 계약이 없다" | 배치 6 채택·배치 7 페이싱 실측 완료 → **종료** |
| `회부_마을소멸_시딩` | "소멸 6/20" | 소멸 0/19, 1,600일까지 유지 → **종료** |
| `회부_주석과_대장장이` | "주석 마을 0/51 · 대장장이 0명" | 주석 산지 3곳 · 대장장이 20명 → **종료** |
| `회부_플레이어_제련_노모델` | "플레이어 UX 미정" | 배치 5·6·7 완료 → **종료** |
| `설계_금속수급_조정안` | "청동 생산량 0 · 합금비 고정" | 주조 3~4곳 · `_alloyMelt` 이윤 탐색 → **보류(재작성 필요)** |

★교훈: **회부 문서도 낡는다.** 세계가 고쳐지면 그 문서의 전제가 먼저 죽는데,
목록에는 그대로 남아 "남은 일"로 보인다. 배치 7 의 오진(이미 있는 철을 없다고 함)과 같은 결이다.
⇒ **새 배치를 짜기 전에 관련 회부 문서의 첫 전제를 실측으로 확인해라.**

## 3-c. ★2026-08-03d 배치 11 — 마을 건립 **기반 완료 · 건립 자체는 회부**

### 완료 (NPC 경제 비트 동일 실증 — `f11base` = `tbuy` 전 컬럼 일치)

| | 내용 |
|---|---|
| **출처 필드** | `createVillage(opts.founder)` — 기본 `null`(NPC 시딩) · 문자열(플레이어 창설) |
| **필멸 배선** | `if (!v.founder && _shieldT < SHIELD_DAYS)` — **플레이어 마을은 기근 보호막 미적용** |
| **소멸 지표 분리** | `소멸(NPC)` / `소멸(P)` 두 열 — `ab-summary`·`econ-regress`·랩 덤프 |
| **거리행렬 증분화** | 전쌍 15,271ms → **증분 612ms**(25배) · 결과 **완전 일치**(하네스 9/0) |
| **철 채광 주체** | 코드 무변으로 성립 실증(하네스 11/0) — 캐논 무변 + 수요 자동 형성 |
| **국고 회부 종결** | 재민 세금 캐논으로 `_cash` = **미상환 세곡 채권 장부** 재해석 |

★**필멸은 플레이어 마을에만 적용된다.** NPC 시딩 마을의 소멸 0 기준선은 그대로다 —
`founder == null` 이라 보호막 조건이 항상 참이고, 800일 3시드가 **전 컬럼 비트 동일**로 그걸 실증했다.

### ★이월 — `회부_마을건립_초기인구.md` (건립 자체가 여기서 막혔다)

배치 지시가 미리 못박은 조건에 정확히 걸렸다: *"NPC 정착 여부·인구 유입 규칙이 설계안에 없다면
그건 새 설계 판단이다 — 구현하지 말고 회부."*

`tickVillage` 첫 줄이 `if (v.npcs.length === 0) return` 이라 **인구 0 마을은 존재하지만 살아 있지 않다**
(생산·소비·교역 0, 그런데 소멸 판정에는 걸려 태어나자마자 죽은 것으로 집계된다).
그렇다고 시딩처럼 8명을 그냥 주면 설계안 §1이 지적한 "클릭 한 번에 마을이 아니라 **선물**"이 된다.

⇒ **"사람이 어디서 오는가"가 새 세계 규칙**이다. 이주는 폐지됐고 순간이동은 캐논상 금지라
그냥 만들 수 없다. 선택지 4종·추천((가) 플레이어가 시간을 들여 데려온다 — 모촌·자촌 분화 고증)과
검증 계획은 회부서에 있다.
★**막힌 것은 계약이 아니다** — 결정만 나면 건립은 터 잡기 선례(노·숯가마)를 그대로 쓴다.
**길드 재고 UI 도 함께 이월**한다(대상 마을이 0곳이라 검증할 것이 없다).

## 3-d. ★2026-08-03e 배치 12 — 마을 건립 **완성**: 식량이 사람을 부른다

재민 확정 **(마)**: *"플레이어가 농사지어서 식량 확보하면 그냥 늘어나는 거 아냐?"*
⇒ `회부_마을건립_초기인구.md` **종결**. 회부의 (가)~(라)는 전부 *새 기구를 만드는 안*이었고,
답은 **있는 기구(`tickRecovery`)를 인구 0 플레이어 마을까지 넓히는 것**이었다. 새 상수 0.

| | 내용 |
|---|---|
| **건립 계약** | `village_site` → 3단계(터 다지기·환호·굴립주) → 완공 = 마을 등록. 노·숯가마 계약(`_siteStart`/`_siteAdvance`) 그대로 + **완공 훅 한 줄**(`spec.onDone`) |
| **인구 0 창설** | `createVillage(initialPop: 0)` — 초기 부존이 전부 `initN` 배라 **자동 0**(선물 없음). ★`opts.initialPop \|\| 8` 이 0 을 8 로 바꾸던 결함 수리(기존 호출부 전부 1↑ → 비트 동일) |
| **인구 유입** | `tickRecovery` 게이트 2줄: `npcs.length===0 && !founder` 컷 유지 · 문턱 `max(1,N)×15` |
| **식량 정본 통일** | 회복 게이트가 `storage.food`(곡식)만 보던 것을 **`totalFoodEquivalent`** 로 — 실클라에서 플레이어 산출은 베리(`fruit`)라 곡식이 0 이었다. 3시드 800일 **바이트 완전 동일** |
| **빈 터 ≠ 소멸** | `_everPop` — ≥1 도달 뒤 0 이 된 마을만 `소멸(P)`. 지표 4곳(ab-summary·regress·랩·v2 CLI) 정합 |
| **곳간에 넣기** | `village_deposit` — 플레이어 인벤↔econ 재화 대응표 **한 곳**(`PV_DEPOSIT_MAP`). 환산율 1:1 **잠정·손잡이** |
| **재고 UI** | 회관 클릭 = 재고. **`_cash` 미표시**(`_` 필드 통째 제외) · 화면 재계산 0 · 권한은 `_furnaceCanUse` 그대로 |
| **건립 시 정지 제거** | 건립이 서버를 **10.5초** 멈추던 것(코스 셀 14.3만칸 지형 판정)을 기존 `distDirty` 지연 경로 + 증분으로. 코스 격자 메모는 캐러밴 A* 격자와 **같은 훅**으로 캐시(41,088ms → 1,584ms) |

**검증**: `test-village-found` 42/0 · `test-distmatrix` 9/0 · `test-frontier-iron` 11/0 ·
`test-furnace` 85/0 · wiring 실패 0 · v2 CLI 2,616·무기Q 651·소멸 0/30 ·
실지도 3시드 800일 **`f11base` = `b12base` = `b12fe`**(3,549·소멸 0·무기Q 594) ·
실클라 E2E `e2e-village.js`. 상세: `보고_마을건립과_식량유입.md`.

**실클라 E2E `e2e-village.js`: ~~29 통과 / 1 실패~~ → ★**32 통과 / 0 실패**(2026-08-03e 재민 검증 세션 수리).**

### ~~다음 배치 1순위 — 곳간음수~~ ★규명·수리·종결 (2026-08-03e 재민 검증 세션)
E2E 의 그 1 실패는 **econ `tickDecay` 의 무클램프 과잉재고 가속**이었다 — 접근자 계측(probe)으로
쓰기 지점을 스택 귀속하니 `economy-sim-v2.js tickDecay` 한 곳: rate = base×(1+excess×5) 가
**1을 넘을 수 있어** `s×(1−rate)` 가 음수가 됐다(실측 fruit 202.43 → −103.68, rate 1.512).
"econ 결백" 판정은 **면죄 하네스가 빈 마을에 재고를 안 넣어서** 생긴 오판이었다(s≤0 은 부패
루프를 건너뛴다 — "자명한 상황을 골라 조용히 통과"의 재발. ②-d 는 교역만 검사했다).
수리 2건: ① rate 클램프 0.95(부패는 있는 것보다 많이 못 썩는다 — NPC 세계 비트 동일 실증)
② **창설 대기(founder && 인구 0) 마을은 excess 가속 면제**(기본 부패만 — 첫 주민 대기 비축은
글럿이 아니라 바로 그 용도다. 재민 확정 (마)의 성립 조건). 하네스 ②-e 가 두 갈래 다 지킨다
(면제 중 보존 · 인구 1 서면 가속 복귀 · 어느 쪽도 음수 불가). test-village-found 47/0.
계측기는 `scripts/probe-negstorage.js` 로 남긴다(곳간 쓰기 스택 귀속 — 재발 시 재사용).

### ★회부 2 (구현 안 함)
**게스트는 재접속하면 제 건물의 주인이 아니게 된다** — `anon_*` playerId 가 접속마다 새로
발급되는데 소유 판정이 playerId 대조다. 노·숯가마·사유지에 **이미** 해당하는 성질이고
배치 12 가 만든 것이 아니다. 인증 경로 전체를 건드리므로 별도 판단.

## 3-e. ★2026-08-03f 배치 13 — **게스트 영속 신원**: 소유가 접속을 넘어 산다

재민 확정: **"네 추천대로 하자"** — 배치 12 회부 2 종결(`보고_게스트_영속신원.md`).

**문제**: 소유 판정이 전부 playerId 대조인데(사유지 `ownerPid`·건물 `ownerId`·노/숯가마/회관
`data.owner`·마을 `founder`) 게스트 playerId 는 접속마다 새로 발급되는 1회용이었다. ⇒ 잠깐
끊겼다 붙으면 **제가 세운 마을의 주인이 아니게 됐다.**

**해결**: 불투명 토큰 하나. **계정 체계 아님**(비밀번호·이메일 없음), **`anon_` 접두사 유지**
(코드 전역 28곳이 그걸로 "등록 계정 아님"을 판정 — 그 정책은 무변). 바뀐 건 **접미사가 접속을
넘어 같아진다**는 것뿐.

| | |
|---|---|
| 저장 | central `players.guest_token`(신규 칼럼 + 부분 유니크 인덱스). 기존 마이그레이션 관용 그대로. **새 테이블 0** |
| 발급·대조 | `POST /guest {token?}` → `{player_id, token, isNew}` |
| 전달 | 클라 `localStorage['durango_guest_token']` → ws 쿼리 `guest_token` → welcome 으로 회신 |
| 부수 수리 | `welcome.playerId` 신설 — 클라가 제 영속 신원을 알게 됐다(종전엔 사유지 목록이 `myUsername` 대조라 게스트는 제 사유지도 남의 것으로 보였다) |
| **보안** | 토큰 = 암호학적 난수 32B · **로그·알림·채팅·화면 어디에도 안 찍는다**(하네스가 서버 로그 전문을 값으로 검사) · 비밀번호 생긴 행은 게스트 토큰으로 못 연다 |
| 폴백 | central 불가 시 종전 1회용 신원으로 떨어진다 — 입장을 막지 않는다. 배포 순서 제약 0 |

**검증**: `test-guest-identity` 39/0(프로토콜 층 실서버) · `e2e-guest-reconnect` 26/0(실클라 —
브라우저 컨텍스트를 통째로 닫고 localStorage 만 들고 재입장) · `e2e-metallurgy` 20/0 ·
`test-furnace` 85/0 · `test-village-found` 47/0 · wiring 실패 0 ·
v2 CLI 2,616·무기Q 651·소멸 0/30 · 실지도 3시드 800일 **`b12tr` = `b13` 바이트 완전 동일**.
★이 배치는 `sim/` 을 **한 파일도 안 건드렸다**(econ 무접촉이 정상).

### ★재민 결정 대기 — 라이브 반영
이 변경은 무언가를 고아로 만들지 **않는다**. 라이브의 `anon_*` 소유 자산은 그 세션이 끊긴
순간 **이미 고아**였다(1회용 id). 결정할 건 "이미 있는 고아를 어떻게 하나" 하나다 —
(가)그냥 배포(고아 사유지가 영영 못 쓰는 땅으로 남는다) / **(나)라이브 DB 리셋과 함께(추천)** /
(다)고아 일소 스크립트 신설. 상세·근거는 보고서 §0. **실행은 재민.**

### 남은 것(구현 안 함)
게스트 인벤·좌표는 여전히 안 남는다(`savePlayer` 가 `anon_*` 를 건너뛴다 — 정책 변경이라 별도 판단) ·
게스트→등록 계정 playerId 승계(지시상 범위 밖) · 토큰 회수·만료 없음(복구 코드 = 계정 체계) ·
오래 안 온 게스트 행 prune 없음.

## 3-f. ★2026-08-03g 배치 14 — 게스트의 **몸과 이름**: 승계 + 인벤·좌표 영속

재민 확정 **"그래 그러자."** 계정 시스템 확장은 **플랫폼 결정까지 보류**
(스팀=티켓 어댑터 / 웹=OAuth 어댑터 — 어느 쪽이든 **playerId 발급 어댑터 하나**로 붙는 현 구조 유지).
플랫폼과 무관하게 공통으로 필요한 2건만 했다. 상세: `보고_게스트의_몸과_이름.md`.

### ① 승계 — 등록해도 playerId 가 안 바뀐다
종전엔 게스트가 계정을 만들면 `/auth` 가 **새 행**(`player_id = username`)을 만들어
사유지·건물·노·마을 `founder` 를 **한꺼번에 잃었다** — 배치 13 의 보람이 등록하는 순간 사라지는 역설.
⇒ `POST /promote`: 게스트 토큰으로 그 행을 찾아 **이름·비밀번호만 얹는다**(행 교체 금지).
`guest_token` 은 동시에 NULL — 배치 13 의 "비밀번호 생긴 행은 못 연다" 게이트와 **벨트와 멜빵**.
클라도 `welcome.promoted` 를 보고 죽은 토큰을 지운다.

★**거절 사유를 둘로 가른 것이 안전핀**이다: `username_taken`(남의 계정 — 막는다) vs
`not_promotable`(토큰 무효·이미 계정·**내 기존 계정** — 막지 말고 평소 로그인으로 흘린다).
후자가 없으면 옛 토큰이 남은 브라우저에서 **제 계정에 못 들어가는** 사고가 난다.

★딸려 온 것: 이제 **`player_id ≠ name` 인 계정**이 처음 생긴다 ⇒ `/auth` 로그인 조회와
`/check_username` 중복 검사를 정본 술어 `findAccount`(player_id **또는** name, 비밀번호 있는 행)로
바꾸고, 계정 이름에 **부분 유니크 인덱스**를 걸었다(조회 + DB 제약 둘 다).
그리고 **이름이 바뀌는 첫 사례**이므로 배치 13 의 "소유 판정에 이름 쓰는 곳 0곳" 전수를 재실행 — 여전히 0곳.

### ② 몸의 영속 — 신원만 남고 몸이 초기화되던 반쪽
`savePlayer` 의 `anon_` 가드가 정책이었는데, 배치 13 뒤로 그 접두사는 **영속 게스트**와
**1회용 폴백** 둘을 뭉뚱그린다. ⇒ 판정을 `canPersist(player)` **한 함수**로 모으고
`player.persistent` 를 본다. ★**호출부 사본 17곳을 걷어냈다** — 가드 한 줄만 풀었다면
대부분의 경로에서 게스트 저장이 **조용히 안 켜졌을 것**이다.
복원은 새 경로 0: `/guest` 가 행 전체를 주고 zone 은 **등록 계정과 같은 복원 블록**을 탄다
(접속 핸들러를 `신원 해석(분기) → 공통 복원(한 곳)` 으로 재구성). 핸드오프에도 `persistent` 를 실어
**존을 넘어도** 저장이 이어진다.

**검증**: `test-guest-identity` **62/0**(⑤승계·⑥몸 신설) · `e2e-guest-reconnect` **40/0**
(실클라 — 몸 기준선 → 컨텍스트 종료 → 인벤·좌표 그대로 → 승계 → 비밀번호 로그인 → 전부 내 것 →
구 토큰은 다른 사람) · `test-furnace` 85/0 · `test-village-found` 47/0 · wiring 0 ·
v2 CLI·3시드 800일 비트 동일(econ 무접촉).

### 남은 것
**토큰 회수·만료** — 플랫폼 결정 뒤 과제(복구 코드를 만들면 그게 곧 계정 체계다).
오래 안 온 게스트 행 prune 없음. 라이브 런북 §6 에 승계·영속 스모크 1줄 추가 — **실행은 재민.**

## 3-g. ★2026-08-04a 배치 15 v2 — **50마을 세계 처방전** + 라이브 마감

재민 확정 **"최종적으로는 50개가 있어야 하거든."** 재민 질문 **"마을 위치를 바꿔야 해? 아니면
비옥도를 수정해야 하나?"** → 상세는 `보고_50마을_처방전.md`.

**답: 둘 다 거의 아니다.** 후보 51곳을 전부 심어도 3시드 800일에 **생존 47 · 좀비 2 · 소멸 2**.
손이 필요한 자리는 4곳(광산6·농촌21·농촌11·농촌12)뿐이고 위치를 옮길 자리는 0곳
(51곳을 다 심어도 **영토 겹침 0쌍** — 최단 쌍 3,217px vs 겹침 문턱 2,121px).
32곳이 빠진 진짜 이유는 땅이 아니라 **선별 게이트**다: `MIN_SPACING_PX=12000`(영토 반경의 11.3배)과
`FOOD_FLOOR=2.0`. 상한 `VILLAGE_MAX=20` 은 **병목이 아니다**(20→51 로 올려도 19곳 그대로).

★**하한 2.0 은 생존을 예측하지 못한다** — 하한 미달 14곳 중 문제는 3곳(21%)뿐이고
광산1(식량 0.59·인구 34)·광산3(0.81·48)·임업4(1.73·81)는 멀쩡히 산다. 반대로 하한을 통과한
농촌12(2.52)가 좀비다. 51곳 세계는 인구 +38~88%, 주조 마을 3/19→8/51, 구리 재고 1→217(시드 42).

★**비옥도 레이어를 칠하면 시딩이 바뀐다는 건 틀렸다.** 선별 시점엔 영토가 없어
`extractLandParamsApprox` 가 비옥도 **필드**를 안 읽고 프록시(`0.4 + 물×0.9 − 바위×1.5`)를 쓴다.
선별을 움직이는 것은 **물 거리와 바위 밀도**뿐이다(칠한 비옥도는 심긴 뒤 생산에는 반영된다).

**용량 판정(2코어)**: 50곳 세계 부팅 329초 · 시딩 303초 · 거리행렬 50마을 1,225쌍 26초 ·
실물화 10,078행 · welcome 영토 102KB · RSS 207MB · tick_avg 5.2ms → **라이브 VPS 감당 가능**.
어촌6 은 주변 24셀에 뭍이 없어 시딩 스킵 — 그래서 실제 상한이 **50곳**이다.

**라이브 마감 4건**: ⑴ 레거시 33곳 끄기(`zone-config.hanbando.legacyVillages:false` — NPC 342→144,
claims 33→0, 후보 51→선별 19 공급·디듀프 무변, `scripts/cleanup-legacy.js` 신설) ·
⑵ 첫 부팅 `bld=5` 는 **결함이 아니었다**(옛 스폰이 마을에서 212셀 밖 = 빈 들판. 재시작해도 동일 — 실측) ·
⑶ 아이콘 4종(silver·gold·nickel·jade_raw, bpy 모듈 렌더가 커밋본과 **바이트 동일**) ·
⑷ central 상시 CPU 의 정체 = 프로토타입 econ 월드 2개(v1 20마을·canadia v2 7마을 `검은숲·연어강·늑대골`).
canadia 판은 소비처가 전부 `primaryZoneId==='canadia'` 뒤라 실효 0 → **지연 시작**(3.22% vs 9.02%).

### 새 손잡이(전부 미설정이 채택값)
`SEED_ALL=1`(랩 전수 시딩) · `ENABLE_CANADIA_ECON`(1 즉시 / 0 정지 / 미설정 지연) · `ENABLE_PROTO_ECON=0`

### 남은 것(재민 결정 — 보고서 §7)
(가) 시딩 게이트 완화 수치 · (나) 광산6 을 지형으로 살릴지 교역으로 둘지 · (다) 어촌6 좌표(유일한 이동 필요) ·
(라) **주기적 tick_max 수백 ms 히치 — 원인 미확정**(레거시 ON/OFF·18곳/50곳 전부에서 남. 전용 배치 필요) ·
(마) 한반도 Q키 시세 패널이 프로토타입 20마을을 보여주는 문제 · (바) 스폰 이동(50곳 전환 시점에 결정 — 실측은 끝)

## 3-h. ★2026-08-04b 배치 16 — **50마을 세계 전환** (재민 "해봐")

상세: `보고_50마을_세계_전환.md` · 라이브 절차: `라이브반영_런북_v3_50마을.md`(옛 이름 라이브반영_런북_2026-08-03.md).

**세계가 18곳 → 50곳이 됐다.** `zone-config.hanbando.seedAllVillages:true` 로 품질 게이트
(식량 하한 2.0 · 간격 12,000px · 상한)를 우회한다. **터 미달 스킵은 그대로 산다** —
어촌6 은 중심 주변 24셀에 뭍이 없어 물리적으로 못 세운다. 그래서 후보 51 → **시딩 50**.
우선순위는 **명시한 `VILLAGE_MAX` env > 존 설정 > 기본** — 하네스가 `VILLAGE_MAX=1` 로 빨리 도는
길을 남겼다. ⚠라이브 컨테이너에 그 env 가 남아 있으면 전환이 조용히 안 된다(런북 §6 에 대처).
모듈 상수 `VILLAGE_MAX` 는 안 건드렸다 — 그건 `PV_MAX`(플레이어 마을 상한)의 기본값이기도 하다.

**땅 보강 6곳** — `scripts/migrate-terrain-sw4.js`(멱등·백업·드라이런·`--revert`).
비옥도는 레이어가 아니라 지형 유도값이라(`fert = 0.12 + 0.62·exp(-dw/80) + 0.18·min(1,dr/60) + 0.08·임상`)
만질 수 있는 건 **물·바위·숲**뿐이다. 못 5개(반경 12셀·중심에서 56셀 = 영토 밖) + 숲 1개가 전부고,
`rivers·ridges·passes·ores·villages·valleys` 와 다른 존 10개는 **바이트 동일**이다.
대상은 서쪽 열 6곳 — 광산6·농촌11·농촌12·농촌21(배치 15 의 칼날 4곳) + **농촌6·농촌7**
(라이브 시드 1020 재측정에서 드러난 둘. 새 지물에서 2,184~2,912셀 밖이고 `kDbg.slot` 이
before/after 완전 동일이라 **내 보강이 망가뜨린 게 아니라 원래 물이 먼 자리**임을 실측으로 갈랐다).

**★새 기준선**: 50곳 800일 3시드 **소멸 0 · 좀비 0** · 인구 6,280(1020)/6,072(42)/5,283(7) ·
1,600일 시드 1020 **소멸 0 · 좀비 0 · 총인구 9,765 · 최소 인구 72**. v2 CLI 는 불변(2,616·651·0/30).

**스폰**: 벌말 광장 → **농촌22 광장**(중심성 1위 1,281셀 · 3시드 최소 인구 101 · 겹침 0 실측).

**랩 정합**: `econ-lab-real` 이 존 시딩 정책을 받도록 고치고 `lab-wiring-check` 에 검사를 넣었다
— 안 그러면 프로덕션 50곳 / 랩 19곳이 된다. 리팩터 비트 안전은 배치 15 덤프와 바이트 동일로 확인.

### 이번에도 계측기가 먼저 틀렸다(둘)
1. 비교 스크립트에 `최근 300일 최고 인구 ≤ 12` 라는 **자작 좀비 잣대**를 써서 "비대상 3곳이 새로
   좀비가 됐다"는 **없는 결함**을 보고했다. 정본은 `scripts/ab-summary.js` 의 **최종 인구 < 10**.
2. `test-furnace` 의 `findClearSpot` 이 파생 자리(`spot+(6,6)`)를 안 봐서, 새 못이 그 자리를 덮자
   "노·숯가마 완공 실패" 2건을 찍었다. 쓰는 범위를 다 검사하도록 고쳐 85/0.
   실클라 E2E 3종은 대기 예산 240초 → 900초(50곳 시딩으로 존 첫 부팅 ~7.6분).

### 남은 것(재민 결정 — 보고서 §6)
(가) ★**랩 51 vs 프로덕션 50** — 어촌6. zone.js 의 해안선 water strip 을 terrain.js 가 모른다.
고치면 실지도 랩 기준선이 전부 이동한다(b13/b14/b15 비교 불가) · (나) 시딩 게이트 코드는 살려 둠
(다른 존은 종전대로) · (다) 첫 부팅 1.5~2시간 — 시딩을 백그라운드/사전 굽기로 바꿀지 ·
(라) 주기적 tick_max 수백 ms 히치(배치 15 §5, 원인 미확정) · (마) Q키 시세 패널이 프로토타입 20마을

## 3-i. ★2026-08-04c 배치 17 — 실내 컷어웨이 · 낚시터 방황 (재민 실플레이 관측)

상세: `보고_배치17_실내컷어웨이와_낚시터.md` · 실화면 증거: `증거_배치17/`.
**라이브 반영에 `--central` 이 필요하다**(central.js 변경).

### ① 실내 컷어웨이 — 좌표계가 어긋나 있었다
건물 사각형(`data.hut`·`bld`·`gran`)은 **존 로컬 셀**인데 판정에 쓰던 `myAbsPredicted` 는
**월드 절대 px** 였다(한반도 원점 409984,49984 → 셀 번호가 12,812 만큼 크게 나온다).
그래서 "안에 들어갔다"가 **한 번도 참이 안 됐다.** `public/client.js` 4곳 전부 수리
(큰집·움집 지붕 · 실내 NPC 가림 · 벽 눕히기). 벽 눕히기는 렌더러블에 **존 원점을 실어**
건물마다 다시 환산한다 — 한 번 계산해 돌려 쓰면 다른 존에서 또 어긋난다.
가드: `scripts/e2e-cutaway.js` **15/0** — 계약층(`__cutawayDbg`) + 화면층(이엉 픽셀 밖 11.6% → 안 1.5%).

### ② 낚시터 방황 — 원인 셋. ★셋째는 엔진 층이다
1. 어부가 **매 결정 틱마다** 목표를 ±20px 새로 뽑았다(한 셀도 안 되는 거리 → 영원히 달달거림)
2. 그 ±20px 가 **물에 빠질 수 있었다**(어장 앵커는 물가 셀 = 반폭만 벗어나면 물)
3. ★`detectStuck` 이 **도착해서 서 있는 NPC를 stuck 으로 셌다** — 4.5초마다 `unstuckNpc` 가
   무작위 80px 로 튕겨 낸다. 어부만이 아니라 **가만히 서는 모든 NPC**(취침·대기 포함)에 걸렸다.
   수리: 목표에 도착(경로 소진 + 12px)했으면 stuck 아님.

실측(`probe-nightlife` 3초 간격): 이동 중앙 12.8px·목표 변경 **100%** → **0.0px · 22%**
(체류 9~17초를 3초로 재면 이론값 23% — 남은 22% 는 결함이 아니라 설계값).
실화면(`probe-fishing-shots`, `git worktree` 로 뽑은 수리 전 트리와 A/B): 움직인 프레임 90% → 47%,
**대조군(일반 NPC 보행 ≈50px/s)은 불변** — 얼려서 통과한 게 아니다.

### ②-나 밤 어부 — 재현 실패. **회부**
전 마을 50곳 · 휴면 포함 **0/14,112 표본**. 게임 23:18 낚시터 실화면 = 무인.
가설 넷을 코드로 배제: ⓐ~~화면 조명이 경도를 무시~~ **틀린 전제였다** — client.js `worldPhase()` 는
`_lonView`(카메라 기준)를 더한다. 마을 앞 플레이어와 그 마을은 차이가 **0.015초**다.
ⓑ `AOI_RADIUS 800` < `CHUNK_ACTIVE_RADIUS 1200` → **보이는 NPC 는 전부 활성**(휴면 얼음 불가능)
ⓒ 취침 게이트는 집 유무를 안 본다 · `_dOff` 는 기상만 **늦춘다**
ⓓ 액션 라벨은 최초가시 + 1.2초(≈25틱 중복) 전송.
⇒ 다시 보이면 **마을 이름 · 화면 시계 · 라벨** 셋만 있으면 갈린다(`/lifedbg` 에 `lon·fv·beh·tgt·dHome·dBed·on·inN` 추가됨).
유력한 잔여 설명은 **이번에 고친 방황 그 자체**(해질녘 귀가 중 80px 튐 = "아직 밖에 있다").

### ③ central 유휴 CPU 0 — v1 도 지연 시작
v1(`economyWorld`·20마을)은 실효가 **있다**(클라 Q키 시세 패널). 그래서 끄지 않고 캐나디아와
같은 **첫 요청에 시작**으로. 소비처 3곳 전수에 훅. 실기동 검증: 유휴 0.4→0.1% · 첫 `/economy/prices`
에 day 0 반환 후 기동 → 5초 뒤 day 8 · 캐나디아 미기동. 손잡이 `ENABLE_PROTO_ECON`(미설정=지연).

### 남은 것(재민 결정 — 보고서 §6)
~~(가) 게스트 1회용 폴백~~ ★**2026-08-05 재민 확정 — "폴백 유지(지금 그대로)". 종결.**
central 이 잠깐 죽으면 토큰을 든 게스트가 **딴 사람**이 되어 들어오고(`zone.js` 2530행) 그 세션 동안
제 사유지·마을·노의 주인이 아니게 된다 — 이 성질을 **알고 유지한다.** 입장 가능성을 소유권 보전보다
위에 둔 판단이다(central 장애가 곧 입장 불가가 되는 쪽이 더 큰 퇴보). ⇒ **고치지 마라.** 대안 둘
(입장 대기 · 존이 토큰→playerId 캐시)은 기각됐다. 재검토는 재민이 다시 열 때만.
(나) 프로토 시세 패널(배치 15 회부 재게시)

## 3-j. ★2026-08-04d 배치 18 — **건축 제대로**: 방 판정 · 자동 지붕 · 다층 (재민 "건축 제대로 하자")

상세: `보고_배치18_건축_방과층.md` · 실화면: `증거_배치18/`.
~~라이브 반영에 `--central` 불필요(central.js 무변)~~ ★검증 세션 정정(2026-08-05): **`--central` 필요.**
central.js 는 무변이지만 클라(`public/client.js` 298줄 — 층 렌더 문법)는 **central 이 서빙**한다
(`central.js:1049` · `Dockerfile.central: COPY public`). 배치 15 아이콘 404 와 같은 갈래 —
"central.js 무변 = --central 불필요"는 **틀린 등식**이다. 판정 기준은 "central **이미지에 들어가는
파일**(server/central*.js + public/ 전부)이 바뀌었나"다. DB 스키마 변경 0 · 마이그레이션 0.

### ① 방 판정 — 서버가 정본
**방 = 벽·문으로 닫힌 셀 집합 + 그 셀 전부에 바닥.** 한 줄이라 설명된다 — "바닥을 다 깔아야 방이 된다".
울타리 마당·回자 안마당이 방이 아닌 이유가 이 한 줄로 나온다(안마당에 바닥을 깔면 **별도의 방**).
★**문은 열려 있어도 경계**다 — 콜라이더는 그대로(열린 문 통과). `_edgeScan(…, roomMode)` **한 순회 두 술어**.
  종전엔 술어가 하나라 열린 문에서 방이 샜다(그래서 마을 건물은 발자국 렉트로 우회했다).
  부수 효과: **문을 여닫아도 방이 안 바뀐다** → 토글마다 전 맵 재구축하던 비용·버그가 함께 사라졌다.
`server/rooms.js` 신설 · 국소 재계산만 · **방 id 는 셀 집합에서 결정론적**(`r<층>:<minCx>,<minCy>`)
⇒ **저장하지 않는다**(파생값을 또 저장하면 두 진실). 클라 BFS 는 삭제 — 받아 쓰기만 한다.
마을 정형 건물은 시드에서 제외 → **회귀 0**(현행 렉트 경로 그대로).

### ② 자동 지붕
닫힌 방 위에 지붕이 저절로. **최대 직사각형 그리디 분해 → 렉트마다 맞배 한 채** — ㄱ자면 2채가
모서리에서 만난다(한국 ㄱ자 집 그대로. 셀 타일 폴백보다 낫다). 기존 `_bakeRoof` 를 임의 크기로
재사용(새 화법 0). ★**앵커 규약 실측 확정**: 발자국 `[x0,y0,…]` 의 지붕 로컬 원점 = **`(x0-1,y0-1)` 셀 좌상단**
— 움집·큰집·곳간에 손으로 박혀 있던 오프셋 3종이 전부 이 한 식과 일치한다.

### ③ 다층 (재민 확정 문법)
내 층 = 컷어웨이 · 위층 = **완전 숨김** · 아래층 = 그대로 · **밖 = 전체 복원**.
옛 `aboveCutawayCells` BFS 제거(내 칸 바로 위에 천장이 있을 때만 켜지던 반쪽 규칙 + 밖에서도 위층을 지웠다).
계단 깜빡임 방지 **히스테리시스 250ms**(`_viewFloor` — 보이는 층만. 충돌은 `myFloor`).
★**공중 건축 금지**: 위층은 **아래층이 '방'인 칸 위에만**(종전엔 허공에 벽 한 장이면 됐다).
층 이동은 **기존 계단**이 이미 한다 — 새 개체 안 만들었다.

### ★이번 배치의 교훈 — 계측기가 **다섯 번** 먼저 틀렸다
① 접속 전에 질의(청크 미활성 → 방 0 을 결함으로 오독) · ② 벽이 없는 자리를 골라 자명 통과 직전 ·
③ 카메라가 다른 두 장을 비교(실내 바닥 판자가 이엉과 같은 카키) · ④ ★**해가 움직였다**
(대조군 절대차 0.0→3.4, e2e-cutaway 는 5.5→**72.3** 으로 1건 실패) ⇒ **하네스 래퍼에서 하루를 멈춘다**
(`dayLengthMs` 24h + epoch 을 정오에 앵커). 스크린샷 두 장을 몇 분 간격으로 찍는 하네스는 전부 이걸 해라.
⑤ 이엉 판별식이 **밤에 맞춰져** 있어 대낮 지면까지 잡았다(판별력 0) → 실측으로 다시 골랐다.

그 하네스가 잡은 **코드 결함 셋**: 벽 캐시 재빌드가 서버 방 캐시를 지움 · ★**스테일 인덱스**
(`qtBuildings` 는 틱마다 재구축 → 건축 직후 재판정하면 증상이 **거꾸로** 나온다 ⇒ 다음 틱 flush) ·
방 지붕 층 z 리프트 누락.

### 남은 것(재민 결정 — 보고서 §6)
(가) **마을 건물 통합** — 마을 문은 개구라 방이 안 잡힌다. 통합하려면 50마을 문 자리에 `door` 행을
심는 마이그레이션(열린 문 = 물리 불변). 이번엔 인계 지시대로 플레이어 건축 전용으로만 얹었다.
(나) 다층 NPC·전투·경로탐색(범위 밖 — NPC 는 아직 0층만 산다) · (다) 지붕 재료·형식(지금은 이엉 맞배 1종)

## 3-m. ★2026-08-06d 배치 20 B — **물가 렉 수리 · 타일 상태계** (완료)

상세: `보고_배치20_산과_타일상태.md`. **라이브 반영에 `--central` 필요**(public/ 변경).
다음 배치 인계서: `다음세션_인계_2026-08-06d.md`.

| | 내용 |
|---|---|
| **물가 렉** | 재민 실기 제보. 진범은 셰이더가 아니라 **`Terrain.isWaterCellLocal`**(흐름 텍스처 한 장 435ms 중 **399ms**). 그 함수는 콜라이더 정본이라 **무접촉** — 렌더 쪽에서 3겹으로 해결: ①강 구간 공간 격자 색인(동치 7,000표본 불일치 0) ②직전 창 겹침 재사용(76%) ③프레임당 9ms 시간 예산·가운데부터·반경 40셀. **걷는 중 388→25ms · 새 땅 1355→12ms · 그림 동일(물 21.46%→21.45%)** |
| **타일 상태계** | 5축(지질·채굴 거울·비옥도·경작·답압) 연속 램프. `public/soil-base.js` = 기준 토양치 **공용 1부**(서버가 이 파일을 require — 사본 없음) · `server/soil.js` = 희소맵+게으른 리젠+`tile_state` 방송 · `soil_cells` 테이블(추가 전용). 바뀐 셀이 걸친 **타일만** 재베이크 |
| **산터 램프** | 재민 "돌만 놓으면 그게 산터냐" — 기본 재질 암반, 이끼(400~)·틈새 풀(780~)만 늘고 **풀밭이 안 된다**. 실측 토양치 1000 에서 초록 **18.4%** vs 같은 값 일반 지질 **93.6%** |
| **검은 지그재그 띠** | **기존 안개로 확정**(19/20 결함 아님). 색이 정확히 `rgb(0,0,0)`(배경 `#0a0d10`·UI `(15,19,23)` 과 다름) · 가장자리 기울기 **행당 ±2px** = 셀 다이아 계단 · 안개 코드가 배치 19 직전과 **diff 0줄**. 처리(블러 vs 옅은 안개색)는 **재민 확정 대기** |
| **하네스 신설** | `e2e-waterperf` 15/0(**걷는다** — 고정 지점 촬영이 이 결함을 놓친 이유) · `e2e-tilestate` 26/0 · `probe-wfcost`(비용 분해) · `run-regressions.sh`(순차 전용) · `dev-boot.sh` |
| **회귀** | terrain 27 · rooms 55 · cutaway 15 · metallurgy 20 · village 32 · guest 40 · wiring 0 · econ-regress **2,616·651·13,473**(비트 동일) · econ-lab-real **6,280·0/51** |

**이월**: `e2e-mountain.js`(배치 20 §C) · 물 프래그먼트 셰이더 비용(실 GPU 측정 선행) ·
안개 경계 처리(재민 확정) · 배치 19 C(물가 술·자연물 재렌더·들꽃) · 산 부수기 메커니즘 · 숲산 톤.

## 3-k. ★2026-08-05b 배치 19 — **지형 실장 1**: 지면 질감 · 물 (A·B 완료 / **C 이월**)

상세: `보고_배치19_지형실장.md`. **라이브 반영에 `--central` 필요**(public/ 변경).
재민 확정 문법(시안 왕복 13회)을 실게임에 이식했다. `scripts/mock-terrain-v4.js` 가 이식 기준이었다.

| | 내용 |
|---|---|
| **지면** | 게임 카메라 각도 베이크 **씨멜리스 풀 질감** + 저주파 마스크로 뚫은 맨땅 뙈기. iso 512×256 타일로 한 번 굽고 blit — **지면 비용 3.34 → 0.08ms/f** |
| **물** | WebGL 흐름맵 셰이더(rivers path 방향·하구 감쇠·호수/바다는 해안 거리장) · 수면 −5px · 얕은물 투명(wa=0.42+0.58×수심) · 물밑 **진흙** · 포말 시간 고정 |
| **물가** | 블록 프리즘 단면(남·동 변, 벽 셰이드) — **벡터**로. 마스크는 NEAREST(각진 블록), 흐름·수심만 LINEAR |
| 손잡이 | `__terrain19 { legacy, waterOff, prismOff, freezeT }` — 기본값이 채택값 |

★**씨멜리스의 수학**(bake-terrain-tex.py 머리말이 원문): w2i 에서 월드 (P,0)/(0,P) 의 합·차가
iso (0,32P)/(64P,0) ⇒ **iso 64P×32P 직사각형이 주기**. P=8 → 512×256px, 셀 = 정확히 64×32px.
실측 이음새: 풀 9.9 vs 내부 11.0 · 마른땅 11.1 vs 11.9 · 진흙 0.5 vs 0.6.

★★**이번 결함은 전부 부동소수 정밀도였고 증상이 셋 다 똑같았다** — "물이 흐르지도 반짝이지도
않는 뿌연 판". ⓐ`worldNow()/1000`(1.7e9)을 셰이더에 넘겨 `ADV*uT`=1.1e11 → highp float 유효숫자
7자리에서 파동·노이즈 인자가 뭉개짐 ⇒ 기준시각 빼기. ⓑ값 노이즈 해시 인자가 월드 좌표(수만 px)라
`fract(sin(dot))` 뭉개짐 ⇒ 국소 좌표 + **주기 노이즈**(값 노이즈는 평행이동 불변이 아니라서
격자를 512 의 약수 주기로 감아야 원점 이동에 불변. **사인 위상은 전체 좌표 그대로** — 국소로
바꾸면 원점 이동 때 위상이 튄다). ⓒ베이크 직사각형 네 귀퉁이가 빈다(월드 정사각형은 iso 에서
마름모) ⇒ 격자벡터 이동으로 감아 채움. **Blender 절차 노이즈는 주기가 안 맞아 바닥 판에 쓰면 안 된다.**

★성능 실측이 낭비를 잡았다: 화면 전체 셰이더 오버레이가 **물이 없는 초원에서도 137ms/f**.
⇒ 물 셀 바운딩 박스를 화면에 투영해 **scissor**, 물이 화면에 없으면 draw 자체 생략(137 → 0.05).
⚠headless 는 SwiftShader 라 물 130~145ms/f 는 **하한**이다. 실사용(GPU) 확인이 필요하다.

**검증**: 신설 `e2e-terrain` 26/1(남은 1건은 문턱 교정 — 본문 참조) — 흐름 이동 (−7,+4)·흐름축
투영 +7.99(반례: 같은 시각 0,0 · 뭍 0,0) · 얕은물 상관 r=0.875(반례: 깊은 데 0.518) · 단면 켜끔
0.10%/밝기 −45.8(반례: 초원 0.00%) · 질감 '같은 색' 6.9% vs legacy 86.5% · 결정론 절대차 0.000.
기존 5종 **전부 통과**(metallurgy 20 · rooms 55 · cutaway 14 · village 32 · guest 40).
★**cutaway·rooms 의 "빈 땅" 대조군 오염은 안 일어났다** — 다음 배치가 물가 술·소품(덩어리 큰
스프라이트)을 얹으면 **그때 다시 확인해라**. econ 바이트 동일(2,616·651·13,473 / 6,280·0/51 · wiring 0).

### ★이월 — C(자연물) 전체. 다음 배치가 그대로 이어받는다
1. **물가 술 자연 군락화** — 재민 미해결 지적("일부러 심은 느낌") 그대로 숙제다: 저주파 밀도 변주 ·
   높이·색 변주 · 안쪽 1~2셀 감쇠 산포 · 갈대/부들 섞기 · **균일 간격 금지**.
   ★이식 자리는 열려 있다: 물 → 프리즘 **다음**이 술 자리다(물 뒤에 그려야 절단선을 가린다).
   그리고 지면 데코는 **안개 마스크 앞**에 그려야 한다(안 그러면 못 본 땅 위에 풀이 뜬다).
2. **나무 12종·수풀 전면 재렌더**(재민 확정 "전면 통일") — Blender 정본 씬 한국 수종, 1셀=1m.
3. **들꽃·풀숲 결정론 산포** — claims·farmland·roads 회피(판정은 이미 클라에 온다).
E(선택) 미착수: GIF 동영상 검증 · 다리 밑 물 정합 · 미니맵 물색 정합.

### ★C 는 배치 21 이 해소했다 (아래 3-l)

### 회부 / 주의
- **물길 파기** → `설계_물길파기_관개수로.md` 신설(구현 금지). 동적 레이어 · 수위/흐름은 취수구
  거리장 **파생**(저장 금지) · 셰이더는 마스크 OR 만 = 렌더 비용 0 · 그리핑 4규칙 ·
  ★논 생산 결합과 그리핑은 **재민 결정** 대기.
- **산(배치 20)과 병합 충돌은 확정적이다** — 같은 타일 루프를 구조 재편했다. 바위 분기는
  `_bakeGroundTile` 안 한 블록에 주석 달아 모아 뒀으니 **그 블록만** 가져가면 된다. 색·문법 무변.
- 다른 존 25곳 미검증(사막·설원에 초록 풀 질감이 맞는지). 한반도 단독 운영이라 안 건드렸다.

## 3-l. ★2026-08-06b 배치 21 — **지형 실장 2**: 물가 술 군락 · 자연물 전면 재렌더 (배치 19 C)

상세: `보고_배치21_자연물.md` · 증거 `증거_배치21/`. **라이브 반영에 `--central` 필요**(public/ 변경).
만진 곳은 셋뿐 — 스프라이트 에셋 교체 · 물가 술 산포 · 들꽃 산포. **타일 루프·산 세그먼트 블록 무접촉**.

| | 내용 |
|---|---|
| **에셋** | `scripts/nature_render.py` 신설(씬 정본 = building_render.py 그대로). 나무 12종 교체(소나무3·잣나무2·참나무3·밤나무2·버드나무2, 실측 3.6~6.8m) · bush/herb 6종씩 교체 · **신설** 풀포기4·갈대3·부들3·들꽃4. `rock/mossrock/ore` 는 지시대로 유지 |
| **앵커** | `public/assets/nature/nature_anchors.json` — 크롭 뒤 `{w,h,ox,oy,ppu,m}`. 클라가 `(64/√2)/ppu` 로 되돌려 축소(산 앵커와 같은 규약). `m` = 실측 높이(1셀=1m 규격 검사용) |
| **후처리** | `scripts/nature-postprocess.py` — 알파 문턱 10 크롭 → `public/assets/{trees,nature}/` 배치 |
| **물가 술** | 파장 5셀 값 노이즈 밀도 변주(**문턱 아래는 0포기**) · 안쪽 1~2셀 감쇠(2.8/1.3/0.5) · 물 쪽으로 12~28px 밀어 **수면 위 최대 12px** = 절단선 은폐 · 갈대/부들은 맞닿은 셀의 빽빽한 구간(q>0.62)만 |
| **초원 소품** | 자리 해시 문턱 0.978~0.923(저주파). 회피는 **클라가 이미 받는 값만**(roads·claims·simVillages·buildings) — 새 판정 0 |
| **비용** | 8×8셀 청크 정적 캐시(무효화는 `_waterCellCache.clear()` 3곳과 같은 지점) · 청크당 물 판정 14×14 **1회**(셀별 7×7 재조회 3,136 → 196) · `NAT_VIEW_PAD` 1500 컬링 |
| 손잡이 | `__terrain19 { natOff, fringeOff, propOff, propNoAvoid, frFloor }` · 계측기 `__natDbg` · `__natProbe()` |

★**렌더가 네 번 틀렸고 네 번 다 비교 프레임이 잡았다**: ⓐ변위 이코스피어 = 저해상도에서 **초록 베개**
⇒ 잎 카드 수백 장 껍질. ⓑ**그 카드의 법선이 옆을 보고 있었다**(길이축을 반지름 방향으로 잡은 탓) —
태양을 정면으로 못 받아 캐노피가 통째로 납작·어두웠다. ⓒ월드 앰비언트의 파랑이 들어와 **탈색된 초록**
(평균 B 가 지면보다 15 높음) ⇒ 알베도에서 파랑 0.74·초록 1.66 (월드·태양 정본 무접촉).
ⓓ풀포기가 **용설란 로제트** ⇒ 얇게·많이·속잎·크기 하향.

★★**1패스 실사고 — AOI 상수 `VIEW_RADIUS`(650)는 렌더 함수 지역 상수다.** 모듈 스코프에서 참조해
매 프레임 ReferenceError → **엔티티 패스가 통째로 죽었다**. 그런데 지면·물·프리즘은 그 앞에서 이미
그려져 **스크린샷은 그럴듯했다**. `__natDbg` 가 undefined 인 걸로 잡혔고, 하네스에
"**`__natDbg` 존재 = 렌더 패스 생존**" 판정을 넣어 가드했다(배치 18 "도달 못 하는 층"과 같은 계보).

★★**내 층이 남의 계측기를 오염시켰다 — 판정은 안 낮췄다.** `e2e-terrain` 이 25/2 로 떨어졌다.
물 흐름을 재는 `bestShift` 가 **수면을 21.9% 덮은 정지한 술** 때문에 최적 이동을 (0,0)으로 눌렀다
(배치 19 §5-b 예고가 cutaway 가 아니라 여기서 터졌다). ⇒ **기준은 그대로 두고**(≥3px·투영>2.0)
`e2e-terrain` 이 `natOff` 로 층을 격리해 재게 했고, **왜 그런지를 `[ⓞ 계측 격리]` 로 숫자까지 출력**한다
(자연물 ON = (0,0) / OFF = (−7,4)·투영 7.99). 자연물 층 회귀는 `e2e-nature` 가 따로 본다.

★**취향 갈림은 실측이 잘랐다.** 물가 술 밀도 바닥값 `frFloor` 3안 시안(증거 폴더). 눈으로는 B(0.25)가
제일 낫지만 **빈 블록 31%→15%** 로 떨어져 `e2e-nature` ⓑ(≥18%, 재민의 "빈 구간이 셀 몇 개 단위로
교대"를 옮긴 판정)를 못 넘는다. **판정을 낮춰 취향을 통과시키지 않았다** ⇒ A(0) 채택.
재민이 B 를 원하면 `frFloor: 0` → `0.25` 한 글자. 단 그때는 ⓑ 판정도 **함께 재정의**해야 한다.

**검증**: 신설 `scripts/e2e-nature.js` **43/0** — 은폐(수면 90,902px 중 19,933=21.9% ↔ 반례 초원 2) ·
군락(회랑 48블록 CV 1.21 vs 균일 기대 0.08 · 빈 블록 31%) · 나무(12장 디코드 · draw 840회 ·
그린 높이=h×1.3 · 높이 7단계) · 회피(소품 160 중 위반 0 ↔ **반례 propNoAvoid 로 끄면 298 중 138 위반**) ·
결정론(시각 고정 두 프레임 0px) · **안개(미탐사 위 자연물 ON−OFF 차이 0)**. 기존 회귀 **전부 통과**(최종 트리 전수 재실행): terrain **27** · rooms **55** ·
cutaway **15** · metallurgy **20** · village **32** · guest **40** = **232 판정 0 실패**. econ 무접촉(2,616·651·13,473 / 6,280·0/51 · wiring 0).
★cutaway·rooms 의 "빈 땅" 대조군 오염은 **이번에도 안 일어났다**(예고는 terrain 에서 실현됐다).

★★**수리(재민 지적) — 자연물이 "한 번도 못 본 셀" 위로 떠 있었다.** 안개 마스크 합성은
**엔티티 렌더 앞**이다(`14.49-e7ae` — 엔티티가 마스크 위에 오게 하려고 일부러). 1패스에서
자연물을 `renderables` 에 태웠더니(배치 20 산 세그먼트 본보기를 따랐다) 자연물도 마스크 위로 올라갔다.
**왜 이 층에서만 터졌나**: 서버 엔티티(나무·건물)는 AOI **650px** 안에서만 오고 그 범위는 사실상
항상 '본 곳'이다. 자연물은 **클라가 1,500px 까지 스스로 만들어** 미탐사 영역까지 뻗는다.
실측(판정 정본 = `window._shadowMask` 알파 248↑): 미탐사 450,566px 중 밝은 픽셀
**자연물 ON 1,142 / OFF 9 → 수리 후 ON 9(=OFF)**. 떠 있던 것의 99.2%가 이 층이었다.
⇒ 자연물 draw 를 **마스크 합성 직전**으로 옮겼다. 지면과 똑같은 3단계를 그대로 받는다.
⇒ **배치 19가 남긴 "지면 데코는 안개 마스크 앞" 계약이 이 뜻이었다** — 배치 20의 z 정렬 본보기와
   충돌하면 **안개 계약이 이긴다**(대가: 엔티티가 항상 자연물 위. 지면 데코는 그 편이 낫다).
⇒ 회귀 가드 `e2e-nature` ⓕ 2판정 신설(ON−OFF ≤ 40). **e2e-nature 32 → 36/0.**

★**안개 3단계 규격**(재민 질문 — 코드 실측): 미탐사 = 검정 alpha 1.0(**셀 단위** iso 다이아) ·
봤지만 시야 밖 = alpha 0.2(살짝 어둠, **셀 단위**) · 지금 보임 = alpha 0. ★**보이는 영역만 셀 단위가
아니다** — 벽·나무·산을 광선으로 자른 **가시성 폴리곤**(매끈한 부채꼴). '봤다' 기록은 그 폴리곤을
1px=1셀 격자에 래스터화해 **커버리지 25%↑** 셀만(`_seenChunks` 16셀 청크, 기록 반경 48셀).

★★**재민 확정 — 엔티티 시야 재정의 · 광맥은 노두(露頭)**(안개 수리에서 이어진 대화).
**엔티티 시야는 안개와 별개 시스템이다**: `entityVisibility()` = 현재 **facing 부채꼴** × **벽 LoS**.
`dist<80px` 무조건 보임 · facing 없으면 360° · 정면에서 약 101° 뒤면 0.
★**"본 적 있는가"는 여기 안 들어간다** — 그래서 미탐사 셀 위에도 엔티티가 그려질 수 있다.
재민 원문 *"나무, 바위 등 식물과 무생물은 항상 그려져야 해.. 시체도.. 바닥템도.."* ⇒

| `entityVisibility` **적용** | **비적용 = 항상 그려진다** |
|---|---|
| `player`(본인 제외) · `mob`(동물) | `resource`(나무·바위·광맥·덤불·약초) · `ground_item` · `corpse` |

거리 vignette `max(0.15, 1−(d/650)^1.4)` 는 **양쪽 다 유지**(재민 확정 — AOI 경계 팝인 완충).
원래부터 비적용: `building`(집·경작지·노) · `road` · `bridge` · `claim` · `simvil` · `mtseg`(산) 등.

★**광맥은 '구역'이다** — 재민 *"광맥은 눈에 보이는 게 아니라 그냥 구역인데"*. 맞다.
`terrain.ores` 는 보이지 않는 구역이고 스폰 밀도·광종만 정한다. 그런데 `ore01..06` 은 바위에
**주황 결정이 솟은 게임식 표식**이었다 ⇒ `scripts/ore-outcrop.py` 신설, **노두**로 재생성:
`rock01..06` 을 몸통 그대로(실루엣·알파 동일) + **은은한 광물 변색**(적갈·녹청) 저주파 얼룩,
피복 22~61%(1패스 68~87%는 '칠한 바위'로 읽혀 낮췄다). **캐 봐야 광물이 나온다.**
증거 `증거_배치21/광맥_노두_전후.png`. `rock`·`mossrock` 은 유지.

★★**재민 확정 — "한 번도 안 가본 곳은 그 어떤 것도 보여서는 안 된다"**(§6-c). **두 번 틀렸다**:
①안개 마스크를 월드 렌더 전체 뒤로 → 미탐사 위 픽셀 0 은 됐는데 **지붕·산이 자기 뒤 미탐사 셀에
잘렸다**(e2e-rooms 이엉 29.0%→2.8%). 가시성 폴리곤은 **지면**에서 만드는데 높은 물체는 화면상
한참 위에 그려지기 때문이다. ⇒ 규칙의 뜻은 "픽셀 금지"가 아니라 **"사물 금지"**.
②개체 단위 게이트로 바꿨는데 **앵커 한 칸**으로 재서 이엉 3.2% — **밖에서 지은 집은 안에 들어가
본 적이 없다**(벽이 시야를 막아 내부 셀이 영영 미탐사). 내가 지은 내 집 지붕이 사라졌다.
③채택 = **발자국 단위**. 앵커 + 반경 R셀 8방위 9칸(building/hutroof/claim/banditcamp R=4 ·
simvil R=10 · stair_cell R=1), 1칸짜리는 그 셀만. 모든 push 에 `wx/wy` 를 달고 없는 건
`__fogGateDbg.missing` 으로 **세어서 내보낸다**(하네스가 0 요구 — 조용한 구멍 금지).
손잡이 `fogGateOff` · 계측기 `__fogGateProbe()`(자리만 내보내고 판정은 하네스가 `_seenChunks` 로 직접).
★cutaway 지붕 신호는 오히려 41.6 → **46.2** 로 올랐다.

~~회부: 건물·경작지의 미탐사 셀 가리기~~ → 지금은 안 가본 곳의 남의 집·논밭이
미리 보인다(같은 원인 — 엔티티가 마스크 위). 가릴지, 어디까지(집만? 논밭까지? 마을 영토선까지?)는
게임플레이 결정이다. 농촌22 광장에서는 재현 안 됨(건물이 AOI 650px 밖) — **마을 가까이서
안 가본 쪽**을 봐야 재현된다.

★★**물가 풀 넘김**(재민 질문 "3D가 아니라서 못 고치나?" → **아니다**). 원인은 클리핑·순서였다:
지면 베이크가 풀을 깔고 **물 셀을 진흙 다이아몬드로 덮어써** 잎이 셀 변에서 칼로 잘리고, 그 위에
물이 또 덮는다. ⇒ `_bakeShoreTile` 신설 — **물 셀마다 이웃 뭍 다이아몬드를 물 쪽으로 d px 밀어**
물 셀 안으로 클립해 **그 뭍의 자기 풀 패턴**으로 채운다. ★**세 번 만에 맞췄다**(두 번은 재민이 눈으로 반려):
①반투명 3층 → *"어색한 띠"* — 반투명 초록을 밝은 물에 얹으면 **뿌연 안개 띠**이고, 다이아몬드를
통째로 미니 한 셀 변 전체가 **같은 폭**이라 띠다. ②불투명 + 합성 얼룩(2px 격자) → **디더 격자 무늬**.
규칙 반복 마스크는 무조건 '무늬'로 읽힌다. ③채택 = **풀 텍스처 자신의 잎 모양으로 깎기** —
`grass_angled` 휘도 중앙값으로 잎 알파를 만들어 틈을 파낸다(문턱 고정 안 함 → 텍스처 교체에 따라옴).
`translate` 로 텍스처째 밀어 마스크와 정합. 넘김 최대 7px(길면 띠가 된다).
지면 타일과 같은 격자·캐시·무효화 지점.
순서: 물 → 프리즘 → **넘김** → 술 → 안개. 손잡이 `shoreOff`.
**심는 게 아니다** — 갈대에 기대지 않고 술을 꺼도 경계가 부드럽다.
실측: 강가 넘김 108조각·타일 15장, 수면 픽셀 90,902 → **87,354**(약 3,500px 을 풀이 덮음).
`e2e-terrain` 격리 목록에 `shoreOff` 추가(§5-a 와 같은 이유, 기준 무변). terrain 27/0 · nature 43/0.

★★**물가 "테두리" 의 진범 — 수면이 뭍 위로 흘러넘치고 있었다 (수리)**(§6-e).
재민 *"여전히 물가에 테두리가 그어진 느낌인데 그건 왜 그래?"* → **앞 답(프리즘 립)은 내 오진**이었다.
근거가 '코드를 읽었다' 뿐이어서 틀렸다. 이번엔 **뺄셈으로** 다시 쟀다:
①프리즘 ON/OFF 화면 전체 차이 **1,975px**(립 색대 490) — 화면을 가로지르는 띠를 만들 양이 아니다.
②물가 경계에서 **뭍 쪽으로** 수직 프로파일 + 물 레이어 ON/OFF 차분 → 파랑 **d0 +27.3 · d1 +25.9 ·
d4 +21.9 · d8 +10.3 · d11 +6.3**. **물이 뭍을 11px 넘어 파랗게 칠하고 있었다** = 푸른 후광 = "테두리".
★원인: 수면은 `WATER_DROP=5`px **아래로** 그린다 → 수면 픽셀은 자기 자리보다 위쪽 셀의 물이고,
남·동 물가에선 **뭍 위에 얹힌다**. 셰이더에 그걸 막는 코드가 **주석만 있고 없었다** —
`vec2 uvg = cellUV(wg)` 를 계산해 놓고 `grep uvg` 하면 **정의 한 줄뿐**. 배치 19 가 빠뜨린 것이고
셰이더는 실패하지 않으니 조용히 한 배치를 살아남았다. ⇒ 의도한 한 줄을 마저 걸었다:
`if(uvg 범위내 && texture2D(uMsk,uvg).a < 0.5) discard;` (그 자리는 프리즘 단면이 덮는다).
**수리 후 재계측: d1 +3.9 · d4 +1.3 · d11 +0.5** (d0 +17.7 은 1px 안티에일리어싱 — 남는 게 맞다).
휘도는 d0 에서 −9.2 = 프리즘 접지 그림자, 의도한 그림. 증거 `증거_배치21/물가테두리_수리_전후.png`.
**교훈: 주석은 계약이 아니다. "이렇게 막는다"고 써 있으면 `grep` 으로 그 변수가 실제로 쓰이는지 봐라.**
회귀 전수 재실행: terrain **27** · nature **43** · cutaway **15** · rooms **55** · metallurgy **20** ·
village **32** · guest **40** = **232/0**. econ 무접촉(`git diff --name-only sim/` = 0 파일 · wiring 실패 0).
★`village` 가 첫 실행에서 1 실패(`central save: socket hang up`)했는데 **내가 `metallurgy && village` 를
한 셸에 이어 돌린 탓**이다(2코어 단독 실행 규율 위반). 단독 재실행 **2회 연속 32/0**.

★★**물가 여백 — "풀이 어색하게 잘리지 않게" 3번째 시도, 채택**(§6-f). 재민 재지적 뒤 **확대해서 다시 봤다**:
풀 → **완전 직선 대각선** → 물. 전이 0px. 북·서 물가는 프리즘조차 없어 **잎이 그대로 잘린다**.
★두 번 반려된 길(`_bakeShoreTile` = **물 위에 초록 띠 추가**)은 다시 안 갔다. 대신 **정반대로** 판다:
`_shoreMarginBake` — 뭍 셀이 물과 맞닿는 변에서 **뭍 쪽으로** 최대 13px 젖은 모래 여백.
**`_bakeGroundTile` 안**이라 새 레이어 0장·프레임 비용 0. 폭은 두 옥타브 저주파(파장 5셀+10셀)×셀별
0.35~1.30배로 **0↔13px 을 오간다**. 안쪽 경계는 `grass_angled` 자신의 **잎 알파**(밝은 곳=잎, `_shBladeMask(true)`)로
모래를 파내 잎이 삐져나온다(지면 바탕과 같은 위상 → 실제 잎 자리와 맞는다). 물에 닿는 42%만 젖은 색.
프리즘 상단 립은 **위에 있는 것의 색**을 따라가고(여백 있으면 모래색) 높이 1.3~3.2px·그늘 알파도 흔든다.
⇒ 직선은 남지만 **모래↔물**이다. 물가선이 날카로운 건 정상이다.
★1패스 실패: 여백 폭을 **술 밀도와 같은 장**에 묶었더니(개념은 예뻤다) 값 노이즈가 0.5 근처에 몰려
화면 전체 여백이 **36조각**뿐 — 지적된 직선이 그대로 남았다 ⇒ **자기 장**(salt 6120/6121)으로 분리.
★2패스 실패: 문턱 0.26 → 물가 변의 **91%** 에 여백 = 그게 곧 띠다. `e2e-nature` ⓗ(빈 구간 ≥20%)를
**9%** 로 못 넘었다 ⇒ **판정을 낮추는 대신 문턱을 0.40 으로 올렸다**(결과 40%). frFloor 때와 같은 규율.
실측: 물가 변 133 · 여백 60% · **폭 0인 변 40%** · 평균 4.88px · **변동계수 1.01** · 손잡이 끄면 0(반례).
손잡이 `shMargin`(숫자, 0=끔 · 1=채택 · 1.75=과다). **타일 캐시가 손잡이 바뀌면 자동 무효화**된다
(`_gtKnob`) — 안 그러면 하네스가 같은 프레임 A/B 를 못 얻는다.
시안 `증거_배치21/물가여백_시안_A없음_B채택_C과다.png` · 전후 `물가여백_전후_확대.png`.

★★**바람 흔들림 — 실장. 단 "날씨에 따라"는 지금 못 한다**(§6-g). 재민 질문에 대한 정직한 답:
**게임에 날씨 상태가 없다**(`server/`·`sim/`·`public/` 전수 grep `weather|rain|storm|날씨` **0건**).
⇒ **바람 세기의 정본 함수 하나**(`_windAt`)를 세우고 지금은 **게임 시계**로 채웠다.
`__terrain19.windForce` 가 **주입구**다 — 날씨 시스템이 생기면 **그 한 곳만** 먹이면 전부 따라온다.
하네스가 그 훅이 죽었는지까지 본다(0.37 주면 정본이 0.37).
채운 값 = **일주기 미풍**(새벽 잔잔→한낮 최대→밤 잔잔) × **큰 숨**(주기 다른 두 사인의 곱).
실측 곡선 0~120s: 최소 0.176 · 최대 0.975 · 평균 0.608.
흔드는 법: **전단**(밑동 고정, 꼭대기만) + **진행하는 파**(위상에서 `바람방향·자리`를 빼 파장 ≈6.5셀로
들판을 훑는다 — 전부 같은 위상이면 '화면이 흔들린다'). 진폭 tan: 풀포기 .155 · 갈대 .265 · 부들 .235 · 들꽃 .125.
★`setTransform` 아니라 `transform`+save/restore (메인 캔버스 변환을 항등으로 되돌리면 뒤가 다 깨진다).
★비용 실측(상시 계측기 `__natDbg.ms`, 755장): 바람 끔 **0.913ms/f** → 켬 **1.157ms/f** = **+0.24ms/f**.
안 흔든 것: **지면 풀 텍스처**(재베이크/WebGL 필요 — 비싸다) · **나무**(서버 엔티티 · 줄기는 원래 안 흔들린다).
★하네스 함정: `freezeT` 는 **물 셰이더도 굴린다**. 물을 안 끄면 수면이 흐른 픽셀을 '풀이 흔들렸다'로
오독한다 ⇒ ⓖ 는 `waterOff` 로 재고, **자연물 OFF 대조군**(NPC·HUD 잡음 바닥)까지 깐다.
실측: 시각 100→100.9 에 강가 **48,257px** 움직임 / 무풍 **0px** / 대조군 **0px**.

★**검증 전수**(순차 단독): `nature` **58/0**(43→58, ⓖ바람 8 + ⓗ여백 7 신설) · `terrain` **27/0** ·
`cutaway` **15/0** · `rooms` **55/0** · `metallurgy` **20/0** · `village` **32/0** · `guest` **40/0**
= **247 판정 0 실패**. econ 무접촉(`git diff --name-only sim/` = 0 파일 · wiring 실패 0).
★`terrain` ③ 프리즘 단면 숫자가 립 변경으로 움직였다: 0.10%(763px, −45.8) → **0.09%(742px, −38.8)**.
**기준(>0.05%, <−8)은 그대로 두고** 통과했다.
★`guest` 가 연달아 돌린 첫 실행에서 2 실패 — **단독 재실행 2회 연속 40/0**. 2코어 규율 위반이 원인이다.

★★★**리베이스 합류 — 산 세션과 처음 한 트리에서 만났다**(2026-08-07). `origin/main` 이
`11be12f → 210218a` 로 움직여 있었다(병행 세션의 산 8커밋: 덮개 배치·기슭·가림 반투명).
내 8커밋을 그 위로 리베이스했다. **client.js 충돌 4건은 전부 손잡이 목록**이었지만,
진짜 위험은 충돌이 **안** 난 곳에 있었다 — 두 세션이 **같은 이름·같은 뜻**의 것을 각자 만들었다:

| 겹친 것 | 증상 | 처리 |
|---|---|---|
| `_gtKnob` (타일 캐시 손잡이 지문) | 두 곳이 서로의 캐시를 지우며 **매 프레임 재베이크** | 지문을 **한 곳**으로 합침 |
| `_natT0` | 산=수집 계측 / 내 것=바람 시각 원점. 섀도잉 | 내 것을 `_windT0` 로 개명 |
| `mtseg` 의 `wx/wy` | 산 세션이 `_mtCollectCover` 로 다시 쓰며 내 5차가 단 자리가 **사라짐** | 다시 달고 **면제 명시** |

★★**산(mtseg)은 안개 게이트에서 뺐다 — 근거는 실측이다.**
자리를 달고 게이트를 걸었더니 **산이 사라졌다**: 앵커만(R=1) → 산 앞에 서도 115장 중 **10장**,
발자국 반경 5 → **16장**. 대조군(origin/main)은 `e2e-mtocc` **10/10**.
원인은 명확하다 — **가시성 폴리곤이 바위에서 끊긴다.** 그래서 바위 셀은 사실상 영영 '본 셀'로
기록되지 않고, 어떤 반경을 줘도 게이트는 '안 가본 산'이 아니라 **모든 산**을 지운다.
이 층에 게이트는 **틀린 도구**다. ⇒ `_GATE_FREE = { mtseg: 1 }` 로 **이름을 적어** 면제하고,
`e2e-nature` 가 그 목록을 **자기 사본과 대조**한다(조용한 면제 금지 — 누가 말없이 하나 더 빼면 깨진다).
★**재민 판단 회부**: "안 가본 곳엔 그 어떤 것도"는 원래 **남의 집·논밭·영토** 얘기였다.
산은 지형이고 실제로 수십 km 밖에서 보인다. 산에도 적용하려면 게이트가 아니라
**안개 마스크 앞에 그리기**가 맞고, 그건 §6-c 1패스에서 지붕이 잘렸던 문제를 산에서 다시 푸는 별도 작업이다.

★★**바람이 남의 계측기 둘을 오염시켰다 — 판정은 안 낮췄다.**
`e2e-mtfoot` ⑧ 과 `e2e-tilestate` 정지 대기는 "두 프레임이 같다"를 요구하는데,
바람이 들어오면 화면은 이제 **카메라가 멈춰도 안 멈춘다**. 둘 다 **재는 층을 격리**했다
(`windOff`), 기준은 그대로. mtfoot 에는 **오염값과 대조군 판정**까지 새로 넣었다
(바람 ON |Δ| 0.055 > OFF 0.003).
★★**그리고 격리하다 한 번 더 틀렸다 — 이게 이번 배치에서 제일 값진 교훈이다.**
`e2e-tilestate` 에서 정지 대기 **직전**에 `knob({windOff:true})` 로 껐더니, 그 헬퍼 안의
`sleep(1200)` 이 실험 타이밍을 밀어 **뙈기 선택 자체가 바뀌었다**(offset 5,-9 → 11,-3).
그 결과 `stateOff` A/B 가 **10.36 → 3.23** 으로 떨어져 "회귀"처럼 보였다 — 회귀가 아니었다.
⇒ **격리는 실험을 건드리면 안 된다.** 측정 시작 **전에**, `sleep` 없이 껐더니 뙈기가 대조군과
같은 (5,-9) 로 돌아오고 |Δ| **8.42**(대조군 8.40) 로 붙었다.
※진단 방법도 남긴다: `git worktree add /tmp/wt-base origin/main` + `node_modules` 심링크로
**대조군 트리를 그 자리에서 돌렸다.** "내 탓인가"는 추측하지 말고 이렇게 재라.

★**합류 후 회귀 전수(12종, 순차 단독)**: `nature` **60/0**(면제 목록 대조 2판정 추가) ·
`terrain` **27/0** · `mountain` **20/0** · `mtfoot` **9/9**(대조군 1판정 추가) · `mtocc` **10/10** ·
`tilestate` **39/0** · `waterperf` **17/0** · `cutaway` **15/0** · `rooms` **55/0** ·
`metallurgy` **20/0** · `village` **32/0** · `guest` **40/0** = **344 판정 0 실패**.
econ 무접촉(`git diff --name-only sim/` = 0 파일 · wiring 실패 0).

★★★**풀 카펫 흔들림 — 재민 정정: 내가 틀린 대상을 흔들었다**(§6-h).
재민 원문: *"물가 근처에 풀 개체들을 추가로 넣고 걔네를 흔들라는 게 아니라 **모든 초원에 그려져 있는
그 풀** 말이야."* 나는 **흩뿌린 포기 스프라이트**만 흔들고, 지면 카펫은 같은 보고서에 **내 손으로**
"비싸다, 거기까진 하지 마라"고 적어 회부했다. **그게 요청 대상이었다.**

★구조를 쪼갰다(재베이크 없음):
```
바탕 cv = 지금 그대로. 단 ① 단계 풀 텍스처를 **평탄한 한 색**으로
잎  bl = (풀텍스처 − 평탄색) × 투과율 T
매 프레임: 바탕 blit → 잎을 **가로 띠(16px)로 어긋나게 'lighter'(가산) blit**
```
★**근사가 아니라 항등식**이다 — 알파 합성은 배경에 선형이라
`sharp = fixed + T·tex`, `flat = fixed + T·평탄색` ⇒ `sharp = flat + T·(tex−평탄색)`.
T 는 추정하지 않는다: **굽는 그 자리에서 같은 도형·같은 알파로 지우며**(`destination-out`) 만든다
(뙈기·물·바위·얼음·존 틴트·물가 여백·상태 레이어 전부). 새 함수 `_covDia`, `_gtDiamond(...,gm)`,
`_gtPaintState(...,gm)`, `_shoreMarginBake(...,gm)`.
진폭 2.2px · 파장 ≈370px(11셀) · 각속도 1.15rad/s · 위상은 **iso 세로(월드)** 좌표.
띠마다 가산 알파 0.90~1.00 도 흔든다(돌풍이 지나갈 때 빛이 훑는 결 — 비용 0).
바람 세기·시각은 포기 흔들림과 **같은 정본**(`_windAt`/`_windT`) → 날씨 훅도 그대로 먹는다.
잎 층 때문에 타일 메모리 2배 ⇒ 캐시 상한 `GT_MAX` 140 → `GT_MAX_WIND` **80**(화면은 ~30장).
손잡이 `windGrassOff`(잎 층만 끔) · `windOff`(둘 다). **끄면 ① 이 종전 선명 텍스처로 구워진다** = 옛 경로.

★비용은 **설계 전에** 벤치로 쟀다(1400×900·타일 30장): 지금 0.038 · 띠32px 0.307 · **띠16px 0.566** ·
띠8px 1.063 ms/f ⇒ 16px 채택(**+0.53ms/f**). 실클라 전체 프레임 19.5↔17.0ms(SwiftShader).

★검증 `e2e-nature` ⓘ 10판정 — **제1판정이 항등식**이다(구조만 쪼갰지 그림은 안 바꿨다는 증명):
무풍 vs 옛 경로 평균 |Δ| **0.370/0.395**, >10 픽셀 0.69%/0.49% ·
카펫 움직임(자연물·물 끔) **241,989 / 258,633px** · 무풍 반례 **0px** · 띠 240 ↔ 끄면 **0**.
※남은 오차의 출처도 특정했다 — 평탄색 백분위 6%→2%→0.5% 실측(4,221→**3,694**→4,116)에서
**더 내려도 안 좋아졌다** = 잘림이 아니라 **상태 레이어 투과율 근사**(도로·경작지·판 자리)다.

★★**남의 계측기 다섯을 오염시켰다 — 판정은 하나도 안 낮췄다.** 지면이 저절로 움직이니
서로 다른 시각의 두 프레임을 픽셀 비교하는 하네스는 전부 깨진다. 전부 `windOff` 로 **층을 격리**:
`mtfoot` ⑧(0.055→**1.96**, 오염값+대조군 판정 신설) · `tilestate` 정지 대기 · `mountain` ⓑ 반례(**33.4%**) ·
`waterperf` 그림 동일(**3.16**>3.0) · `rooms` "빈 땅 대조군 정지"(**3.8**>2 — ★배치 19 인계가 **예고했던**
그 오염이 드디어 실현됐다).
★격리는 **측정 전에, sleep 없이** 걸어라(§합류 교훈). `rooms` 는 페이지 객체가 셋(page/p2/p3)이라
한 이름으로 일괄 치환하면 죽는다.

★★**덤으로 잡은 잠복 버그 — 타일 캐시 지문에 `waterOff` 가 빠져 있었다.**
항등식이 **강가에서만** 평균 |Δ| 6.18(12.6%)로 깨졌다(초원 0.395). 범인은 내 층이 아니라
"물을 껐다 켜도 옛 타일이 그대로 남는" 지문 누락이었다. 지문에 넣자 0.370 으로 붙었다.
**손잡이 A/B 를 쓰는 모든 판정이 이 버그의 영향권이었다.**

★★**현재 `origin/main`(3603f69) 상태 — 내가 전수로 재확인했다.** 내 카펫 커밋(5bf061c)은
**이미 main 에 들어가 있고**, 그 위에 산 세션이 3커밋(b97126e·725ff4d·3603f69)을 더 올렸다.
그 트리에서 12종을 다시 돌린 결과:

| 하네스 | 결과 | 비고 |
|---|---|---|
| nature | **70/0** | 카펫 항등식 0.370/0.395 그대로 |
| terrain · tilestate · mtfoot · mountain · waterperf · rooms · cutaway · metallurgy · village | **전부 0 실패** | mountain 은 23판정으로 늘어 있다 |
| **mtocc** | ✗ **①에서 중단** | 아래 |
| **guest-reconnect** | ✗ **3 실패** | 아래 |

★★**`e2e-mtocc` — 산 세션의 최신 커밋에서 깨졌다(내 탓 아님, 대조군으로 확인). 2c38f11 에서도 그대로다.**
`① 가려지는 자리를 찾았다(반례 성립) — 가린 산 0장` 에서 멈춘다. 계측값:
`{n:0, faded:0, front:139}` (같은 자리에서 **5bf061c 는 `{n:1, faded:115, front:115}` · 10/10**).
즉 `3603f69` "산 여유 2.0 채택(살짝은 침범당해도 돼)" 로 산이 플레이어를 **덜 덮게** 되면서
그 하네스의 **전제**(나를 덮는 산이 있다)가 성립하지 않는다.
⇒ 산 세션이 판단할 일이다: 촬영 자리를 옮길지, 여유 2.0 에서 가림이 정말 사라진 게 맞는지.
   **나는 남의 층을 안 고쳤다.**

★★**`e2e-guest-reconnect` ⑤ 몸 승계 3판정 — 두 세션 어느 쪽 탓도 아니다.**
대조군을 두 번 돌려 확인했다: **오늘 40/0 이던 `37f30c5`** 에서도, `5bf061c` 에서도 **똑같이 3 실패**한다.
같은 코드가 아침엔 통과하고 지금은 재현성 있게 실패한다 ⇒ 코드 변경이 아니라 환경/타이밍이다.
계측(진단판을 임시로 끼워 실측): 전제가 깨지는 자리에서 `window.__getInv()` 가
**`{wood:0, stone:0}`** 만 돌려준다 — `berry` **키 자체가 없다**. 그런데 재접속 뒤에는 `berry 200`
(= 1막의 첫 `__e2e_give` 값)이 읽힌다. ⇒ **서버 진실엔 200 이 있는데 클라 인벤에 안 실려 있다.**
가설(미검증): 증분 인벤 갱신이 추적 집합 밖 품목을 흘리고, welcome 전체 스냅샷에서만 복구된다.
⇒ **별도 조사 항목.** 하네스가 아니라 **클라 인벤 동기화**를 봐야 한다.
   (진단 방법: `invOf` 를 `{__has: typeof window.__getInv, ...inv}` 로 바꿔 찍으면 바로 보인다.)
★★**후속 — 해결됐다. 그리고 내 귀속 판단은 절반 틀렸다.**
   병행 세션이 `2c38f11` "e2e-guest 하네스 오류 13건째: **워치독 재연결 × central 순단 폴백 레이스** 수리"
   를 올렸고, 그 트리에서 **40/0** 으로 돌아왔다.
   내가 맞은 것: **두 세션의 이번 코드 변경 탓이 아니다**(대조군 두 개로 확인했다).
   내가 틀린 것: 거기서 멈추고 **"환경/타이밍"** 이라고 이름 붙인 것 — 실제로는 **진짜 레이스 버그**였다.
   "내 탓이 아니다"까지가 대조군이 증명해 주는 전부다. **"그러면 환경 탓"은 증명이 아니라 추측이다.**
   ⇒ 다음에 같은 자리에 서면, 귀속을 끝냈다고 조사를 멈추지 마라.

★★**푸시 3연속 거절의 원인은 매번 달랐다 — 기록해 둔다.**
① `origin/main` 이 내 기준점보다 앞서 있었다(산 8커밋) → **리베이스**
② 실패한 시도가 남긴 `bundle-import` **찌꺼기 브랜치** → `git branch -D bundle-import`
   (스크립트 68행 `git fetch "$BUNDLE" "$REF:refs/heads/bundle-import"` 에 **`+`** 를 붙이면 재발 안 한다)
③ **이미 푸시돼 있었다** — 그 사이 산 세션이 3커밋을 더 올려 내 브랜치가 *뒤* 가 됐다
근본 원인은 하나다: **두 세션이 같은 브랜치에 민다.** ⇒ 번들은 **만들기 직전에 fetch→리베이스**하고,
재민은 **받자마자 바로** 밀어야 창이 좁다.

★★**재민 확정 2건(2026-08-07 저녁)**
① **물가 술 반려 — 기본값 `fringeOff: true`.** 원문 *"물가 근처에 추가적으로 배치하는 풀은 없애줘"*.
   이 층의 존재 이유는 **절단선 은폐**였는데 §6-f 물가 여백이 그 문제를 직접 푼다(가리는 게 아니라
   안 잘리게 한다) ⇒ 가리개가 불필요해졌고 남은 건 "일부러 심은 풀"로 읽혔다.
   코드는 손잡이 뒤에 남겼다. **초원 소품은 그대로 산다**(없애라고 한 건 '물가 근처'뿐).
   하네스에 **기본값 판정** 신설: 손잡이 무접촉 상태에서 술 0 · 소품 122/160(반례).
② **카펫 흔들림 증폭 — "여전히 안 흔들려".** 1패스가 **너무 느리고 작았다**(2.2px · 주기 5.5초).
   ⇒ 진폭 **3.4px** · 1차 주기 **3.0초** · **2차 파 신설**(주기 1.2초 · ×0.38) · 밝기 0.84~1.00.
   2차 파가 없으면 '미끄러지는 그림'이지 풀이 아니다.
   ★★**교훈 — 판정 단위를 사람 눈에 맞춰라.** 1패스 판정은 "시각 100→101.4 에 바뀐다"였는데
   그건 **1.4초** 간격이다. 사람은 그 사이 그림이 몇 번 바뀌는지를 본다.
   ⇒ **0.25초 간격**으로 다시 쟀다: 지면 상자 52만px 중 **12만px(≈23%)가 매 0.25초 바뀐다.**
   "숫자상 움직인다"와 "눈에 움직인다"는 다른 판정이다.

★★★**물가선을 굽혔다 — "여전히 물에 풀이 잘린다"의 진짜 원인**(§6-j).
12배 확대해서 보고 나서야 알았다: **풀↔모래 경계는 이미 잎 모양으로 너덜한데
모래↔물 경계가 자로 그은 완전 직선**이었다. ⇒ **지면 쪽을 아무리 다듬어도 그 위를
직선 물 폴리곤이 덮어 다시 자른다.** 나는 계속 지면만 손댔고, **자르는 칼은 물 쪽에 있었다.**
수리(물 셰이더 2가지):
① **굽이** — 셀 마스크를 `fuzzW(w)`(월드 좌표를 값노이즈로 흔든 자리)에서 읽는다.
   파장 ≈8월드px → 디더가 아니라 **물결치는 선**. 잡음 규약은 기존 `vn`(스케일 512 약수)라 카메라 불변.
② **피복률 4탭** — 굽이만으론 1px 만에 뚝 끊겼다. 네 점 중 물인 개수로 `wa *= cov`.
   수면 높이·지면 높이 **둘 다** 재고 `min`. 경계가 0/¼/½/¾/1 로 젖어들 듯 끝난다.
★배치 19 "각진 블록 — 셀 경계 그대로" 규약을 **의도적·좁게** 깼다(블록감은 유지, 바깥 윤곽만).
손잡이 `edgeFuzz`(기본 6.0 · 0 이면 옛 그림). 실측 경계 이동 평균 1.05px(최대 27) · 12.0 은 과하다.
회귀 terrain 27/0 · waterperf 17/0 · nature 73/0.

★★**1패스 — 셰이더가 조용히 죽었다. 그리고 그걸 잡아낸 건 '변하지 않는 숫자'였다.**
`fuzzW` 를 `main()` **안에** 정의했다(GLSL 중첩 함수 금지) → 링크 실패 → `try/catch` 가 삼켜
`_wgl.ok=false` → **단색 물 폴백**. 화면엔 여전히 물색이 있어 스크린샷은 그럴듯했다.
단서: `uFuzz` 를 0/3/4.5/7 로 돌려도 **측정값이 소수점까지 전부 동일**했다.
⇒ **손잡이를 돌렸는데 아무것도 안 변하면 "효과가 약한" 게 아니라 그 층이 안 돌고 있는 것이다.**
   하네스가 `__waterDbg.webgl` 을 함께 읽게 했다.
   (배치 18 '도달 못 하는 층' · 배치 21 `VIEW_RADIUS` ReferenceError 와 같은 계보.)

★★★**흐름이 반대로 흐르는 띠 — 진범은 "가장 가까운 강이 주인"**(§6-k).
재민: *"물이 북→남으로 흐르는데 (-1,1) 방향으로 흐르는 띠가 존재한다."*
**내가 세 번 헛다리 짚었다 — 셋 다 움직임의 '크기'를 쟀다**(열별 표준편차·경사 이음매·모션 히트맵).
**뒤집힌 띠는 이웃과 속도가 똑같아서 크기 계측기로는 영원히 안 보인다.** 하네스 오류 8건째이고,
이번엔 판정이 아니라 **잰 물리량**이 틀렸다. ⇒ 교훈: *결함을 '방향'으로 말하면 방향을 재라.*
진범: `_flowAtCell` 이 **생거리 최근접**으로 흐름 주인을 골랐다. 합류부에서
  한여울강(width **1,027** · 545px · u=1.06 = **본류 물속**) vs 닛폰대천(width **235** · 575px · u=**4.89**)
  ⇒ 30px 차로 **지류가 본류 한복판을 빼앗고**, 두 강의 이등분선에서 방향이 한 칸 만에 뒤집힌다.
  세기 감쇠도 **절대 거리**(700/1400)라 폭 96px 실개천이 700px 밖까지 물들였다.
수리: 거리를 **강폭 단위**로 잰다(`u = 거리/반폭`, 주인은 `d²/hw²` 최소 · `w = u>1 ? 2−u : 1`).
  `FLOW_R` 1400 → **1800**(가장 넓은 강 width 1,783 ⇒ 영향 끝 1,784px. 안 넓히면 **공간 색인이
  전수 순회와 갈린다** — 색인 동치의 증명이 "반경 밖은 가중치 0"에 걸려 있다).
  대조군 손잡이 `flowRawDist`(= 출시본 그대로). 바꾼 뒤 **`__wfReset()` 필수**.
실측: 제 물길 밖 역류 칸 **155 → 0** · 화면 역류 668 → 469 · 이음매(이웃 30°초과) 59 → 38 ·
  실제 픽셀 64px 블록 역류 14% → 5%. 회귀 terrain **32/0** · waterperf 17/0 · nature 73/0.

★★**폐기한 1안 — "방향을 섞자"는 이 셰이더에서 쓸 수 없다.**
거리 가중 평균(1/d²)으로 섞으니 이음매는 **0쌍**이 됐는데 **파도가 통째로 무너졌다.**
이유: 파도 위상이 `dot(w, dir)` 인데 `w` 가 **절대 월드 좌표(≈4.6e5)** 다. `dir` 이 공간에 따라
조금만 변해도 위상 기울기가 `|w|·|∇dir|` 로 증폭된다 — 실측 ∇dir 0.33°/셀 ⇒ 배율 **83** ⇒
파장 64px 가 0.77px 로 접혀 완전 에일리어싱. 물 픽셀 |라플라시안| **3.39 → 6.81(2배)**,
8배 확대하면 파도 줄무늬가 **모래알**이 된다(`증거_배치21/파도_확대_A정상_B섞기폐기안.png`).
⇒ **이 셰이더는 `dir` 이 구역 안에서 상수(∇dir=0)라는 가정 위에 서 있다.**
   방향을 매끄럽게 하고 싶으면 파도를 **흐름 포텐셜**로 다시 짜야 한다(§7 회부).
   ※같은 이유로 `uLin` 을 NEAREST 로 바꿔도 안 된다 — 셀 경계마다 위상이 튄다.

★**합류 주의 — 병행 세션이 카펫 흔들림을 되돌렸다.** 내가 재민 지시로 `GT_GRASS_AMP` 2.2→3.4 +
2차 파를 넣었는데, 그쪽이 **짝 비교로 16.3ms/f** 를 재고 2.2 로 되돌리며 2차 파를 빼고 목적지를
정수로 반올림·화면 밖 띠를 잘라냈다. 리베이스에서 **그쪽을 취했다**(성능 수리를 조용히 되돌릴 수 없다).
실측상 손해는 아니었다 — `nature` ⓘ 초원 카펫 움직인 픽셀 282,120 → **291,667**.
소수 목적지의 이중선형 재샘플링이 오히려 잎을 뭉개고 있었던 듯하다. **세기 판단은 재민 몫.**

★**적신호 귀속 — `mountain` ⓓ 한 건은 원인을 특정 못 했다.** 리베이스 후 12종 전수 **369/5**.
`mtocc` 는 산 세션이 고쳐 **13/13** 이 됐다. 남은 적신호 5건 중 4건은 깨끗한 `26af16a` 워크트리에서
같은 숫자로 재현 ⇒ 산 세션. 나머지 한 건(`mountain` ⓓ "판 자리 재계산")은 내 트리 3/3 실패 ·
대조군 2/2 통과였다. 더 파 보니:
  · **파괴 전 화면은 두 트리가 픽셀 단위로 같다**(상자 |Δ| 0.00) — 그림이 아니라 **재건 진행도**가 다르다.
  · `mt3budget = 1`(프레임당 산 청크 1장)인데 그 2.2초에 실제로 그린 프레임이 **내 5장 · 대조 4장**이다(≈2fps).
  · 하네스에 스크린샷 한 장(≈100ms)을 끼우자 **대조군도 |Δ| 1.78 로 실패**했다.
  ⇒ 판정이 고정 `sleep(2200)` 안의 4~5프레임에 걸려 **불안정**하다. 커밋 단위로도 내 앞 두 커밋
    (문서 + `fringeOff`·카펫 상수)에서 이미 재현돼 물 커밋과는 무관하다.
  ⇒ **"환경 탓"이라고 쓰지 않는다.** 원인 미특정으로 산 세션에 넘긴다. 고칠 거라면 고정 sleep 대신
    **조건 대기(더티 청크 0)** 나 시험용 `mt3budget` 상향.

★★★**다이아몬드 이음매 — 지면 베이크 버그였다**(§6-l). 재민: *"동에서 서로 1셀 두께로 줄무늬,
위치가 엇갈려 있다"* · *"저 물방울처럼 생긴 건 뭐야"*. 내가 §6-k(흐름) 로 설명하려 했는데 **틀렸다** —
물 셰이더 항을 하나씩 꺼서 갈랐더니 **두 겹**이었다.
  ① **깨끗한 1px 격자 = 지면 다이아몬드 이음매.** 셀을 한 장씩 칠하면 맞닿은 변에서 안티앨리어싱이
     양쪽 다 부분 피복만 내고 겹쳐도 0.5+0.5·0.5 = **0.75** 라 25% 가 빈다. 실측 픽셀의 **7.07%**.
     초원은 풀이 가려 안 보이는데 **반투명 얕은 물 밑은 배경이 매끈해 그대로 드러난다.**
     ⇒ 수리: **불투명 칠(alpha ≥ 0.999)만** 도형을 수직 `GT_DIA_GROW = 0.75px` 부풀려 이웃과 겹친다.
       물 셀 진흙은 클립 도형도 부풀렸다. **반투명 층은 절대 부풀리지 마라** — 겹친 자리가 두 번
       섞여 반대 부호의 줄이 생긴다. 실측 물 끈 지면 고주파 **1.08 → 0.35(−68%)**.
  ② **그 위 점묘 = 물결. 못 고쳤다.** 가설 다섯 기각(§6-l 표): 반짝임·포말·굽이·잔결 격자·디더 전부 무효.
     잔결 **세기**만 먹히는데(3.89→2.64) **물이 안 움직이게 된다** — `e2e-terrain` ① 이동 8.1px → **0.0px**.
     ⇒ **점묘를 만드는 성분 = 흐름을 보이게 하는 성분**이다. 판정을 낮추는 대신 안을 버렸다.
     전부 출시값 원복 · **손잡이만 남겼다**(`specOff`/`foamOff`/`sineOff`/`ripOff`/`ripAmp`/`ripScale`/
     `ripScale1`/`ripW`/`ripW1`/`dither`). **§6-l 표를 먼저 읽어라 — 기각한 안을 다시 하지 마라.**
  ③ **"물방울" = 물밑 지면이 비치는 것.** 물 끄면 −7.5, 파도만 끄면 −10.5, 기본 −13.8.
     시각 100→101→102 에 **자리는 고정**이고 세기만 −13.8→−11.3→−9.2. 배치 19 의 "얕은물 투명"이
     작동한 결과지, 결함이 아니다.

★★★**물살 얼룩 띠 — 흐름 방향을 LINEAR 로 읽고 있었다**(§6-m). 재민: *"아니 세로줄"* ·
*"계속 물살 이야기하는데 왜 갑자기 풀로 넘어갔냐"* — **정당한 지적이다. 나는 풀에서 답을 찾아 내밀었다.**
물만 놓고 재니 두 가설이 먼저 죽었다: ⓐ 정지 화면 물 안의 세로 구조 → 최장 8~12행(풀은 98행) = 없다.
ⓑ 호수 폴백 방향 → 각도 스냅 0/16/8/1 이 **소수점까지 동일** = 그 분기가 안 돈다.
진범: `uLin` 은 **셀 = 텍셀**인데 **LINEAR** 라, 방향이 다른 두 셀 사이에서 dir 이 한 셀에 걸쳐 돈다.
파도 위상이 `dot(w,dir)`, w ≈ 4.6e5 ⇒ `|w|·|∇dir|` 증폭 ⇒ **그 한 셀 폭(화면 ~64px)이 디더 얼룩 띠**.
띠가 꺾이는 자리 = 강 폴리라인 꼭짓점. **§6-k 에서 방향 섞기를 폐기한 것과 같은 병**인데,
LINEAR 가 이미 경계 한 셀에 같은 짓을 하고 있는 걸 그때 못 봤다.
수리: `uMsk`(NEAREST · 알파만 쓰던 텍스처)의 **RG 채널에 방향을 실어** 거기서 읽는다.
**수심은 그대로 LINEAR**(그라데이션은 매끄러워야 한다). 실측 물 고주파 **3.139 → 1.762(−44%)**.
대조군 `dirLinear: true`. ⇒ **교훈: iso 셀 = 텍셀인 데이터는 '값'만 LINEAR 로 읽어라. 방향은 안 된다.**

★★**그 수리의 대가 — "번개모양 경계"**(§6-m 2패스). NEAREST 는 방향을 셀 경계에서 뚝 바꾸는데,
위상이 `dot(w,dir)`·w≈4.6e5 이라 **6° 차이도 위상 46,000 튐 = 파장 대비 난수** ⇒ 다이아몬드 격자를
따라 계단(번개)이 남는다. ⇒ **위상의 정본을 '강을 따라 잰 거리 s'로 바꿨다**(회부했던 흐름 포텐셜).
  `_riverSegs` 에 누적 호장 · `_flowAtCell` 이 s 반환 · `msk.b` 에 `s mod 64` · 셰이더 `al = s + dot(w−셀중심,dir)`.
  호수는 `-depth*192`(물가 거리)를 위상으로. 마루 기울임 `dot(w,perp)` 은 국소 잡음으로 교체.
  잔결은 `uDirC`(창 중심 흐름) **한 방향**으로만 흘린다(픽셀마다 dir 로 흘리면 경계에서 또 튄다).
  ★**효과를 못 보였다**: 고주파 NEAREST만 1.760 vs 호장위상 1.786 — 사실상 같고, 9배 확대에서도
  둘 다 깨끗했다(이 카메라의 셀 간 방향 차이 **최대 6°**). 재민이 본 자리는 촬영 밖.
  ⇒ 이론상 맞고 손해 없어 넣되 **"고쳤다"고 쓰지 않았다.** 손잡이 `phaseLegacy: true` = 옛 위상.

★★★**번개 경계 — 3패스에서 실제로 잡았다**(§6-m 3패스). 재민: *"해결해야 해.. 해결해봐"*.
2패스가 못 보인 이유는 두 개였고 둘 다 **계측 쪽**이었다.
  ㉮ **CPU 손잡이는 `_wfCache` 를 안 깬다.** `phaseRelax`/`dirRawCell` 을 바꿔도 흐름 텍스처를
     다시 안 구워 C·D 가 **바이트까지 같은 그림**이 나왔다(2패스 표의 1.619 두 번이 그거다).
     `window.__wfReset()` + 안정 4초를 넣고서야 갈렸다.
     ⇒ **교훈: A/B 는 "손잡이를 돌렸다"가 아니라 "그림이 실제로 달라졌다"까지 확인해야 성립한다.**
  ㉯ **자리를 안 찾고 쟀다.** `__flowMap` 으로 창 안 물 셀 방향을 전부 받아 **이웃과 각차가 최대인 셀**을
     고르니 (14289,4024)에서 **98.3°** — 한여울강과 지류가 정면으로 부딪치는 곳이었다.
     2패스 카메라에서는 최대 6° 뿐이라 애초에 보일 수가 없었다.
     ⇒ **자리는 그린 그림이 아니라 흐름장에서 골라야 한다**(그림 보고 고르면 자명 통과다).
수리 두 가지:
  ⓐ **방향을 CPU 에서 다시 매끄럽게**(3×3 단위벡터 평균 2패스, 세기 보존). §6-k 에서 폐기했던 바로
     그 안인데, **위상이 이제 `w−셀중심`(≤23px)만 쓰므로 `|w|≈4.6e5` 증폭이 없어 안전해졌다.**
  ⓑ **위상을 연속 포텐셜 Φ 로 푼다.** 호장은 *한 강 안에서만* 이어져 합류부에서 그대로 튄다.
     창마다 `∇Φ = 흐름방향` 인 Φ 를 **가우스-자이델 36회**로 푼다(초기값 = 호장) ⇒ 강이 몇 개 만나든
     위상이 한 장으로 이어진다. `Φ mod 64` 를 `msk.b` 에, 셰이더는 `al = Φ_cell + dot(w−셀중심,dir)`.
실측 — 계측기가 **자리를 안 고른다**: 물 픽셀을 셀 안 좌표로 되돌려 ㉠센 경계(각차 ≥20°, 5,513px) ·
㉡약한 경계(<3°, 12.3만px) · ㉢안쪽으로 나눠 잰다.

| 안 | ㉠센 경계 | ㉡약한 경계 | ㉠/㉡ | 물 전체 |
|---|---|---|---|---|
| A 옛 LINEAR | 4.988 | 5.339 | 0.934 | 5.344 |
| B 지금 번들(NEAREST만) | 1.443 | 1.551 | **0.931** | 1.580 |
| C 호장만 | 1.047 | 1.501 | 0.697 | 1.506 |
| **D Φ완화(채택)** | **1.112** | 1.557 | **0.714** | 1.620 |

㉡ 는 B·C·D 가 같다 = 전체를 뭉갠 게 아니다. **㉠ 만 −23%** 떨어진다.
A 의 ㉠/㉡ 이 B 와 같은 것이 ★반례 구실을 한다 — LINEAR 의 병은 셀 폭 전체에 퍼져 경계에 안 몰린다.
그림 `증거_배치21/물살_번개경계_4안_9배.png`: **우상 B 에만 딱딱한 대각선 쐐기**(재민이 본 번개), C·D 없음.
★**자리 셋에서 다시 쟀다**(강 데이터의 합류 후보 중 각차 큰 순, 서로 4,000px 이상 떨어진 곳):
  · 한여울강×지류 98.3° — ㉠ 1.465 → **1.094 (−25%)**
  · 자란천×옥돌천 174.9° — ㉠ 1.710 → **1.290 (−25%)**
  · 복사꽃천×뒷내 179.4° — ㉠ 2.011 → 2.353 (**+17%**) ← **숫자가 거꾸로 나와 그림을 봤더니
    숲 속 실개천이라 물이 거의 전부 잎·풀 아래였다. 계측 불가로 적었다**
    (`증거_배치21/물살_숲속실개천_계측불가_7배.png`). 반지름 4 원판 필터로도 못 걸렀다.
  ※폭 420px 넘는 합류부는 온 세계에 한 곳뿐 — 넓은 합류부가 드물어 위 둘이 대표 표본이다.
★**정직하게**: ㉡(보통 물)은 1.562 → 1.723(+10%), 물 전체 1.580 → 1.620(+2.5%) **늘었다**.
  방향이 셀 안 상수가 아니라 서서히 휘어 마루가 곡선이 되기 때문이다.
★**전단선 보호**(맞부딪치는 두 강에서 단위벡터가 상쇄돼 방향이 난수가 되는 것 막기) —
  대조군 `shearRaw: true` 로 재니 **세 자리 모두 소수 셋째까지 동일**. 논리는 맞고 비용은 0 에
  가깝지만 **이득을 못 보였다**. 보호막으로만 남겼다 — §6-j 교훈에 걸리는 자리라 그대로 적는다.
비용: 최악의 장 **33ms 중 방향매끈+Φ 5.5ms(17%)** — 수리 전 228/148 대비 7배 빠르고 예산 안.
  (흐름 텍스처는 카메라가 512px 움직일 때만 다시 굽는다 — 매 프레임 비용이 아니다.)
  `__wfPhiMs` 가 방향매끈+Φ 를 함께 재고 `e2e-waterperf` 가 장당 찍는다.
손잡이: `phaseRelax:0`=호장만 · `dirRawCell:true`=방향 안 매끈 · `phaseLegacy:true`=옛 위상.

★★**풀 카펫 세로줄 — 타일 경계마다 잎 빛이 지워졌다**(§6-n, 별개 결함).
잎 층을 `off` px 밀어 가산으로 얹는데 타일을 **한 장씩 (바탕→잎)** 그린다 ⇒ A의 잎이 B 영역으로
넘어간 뒤 **B의 불투명 바탕이 그걸 덮는다** ⇒ 타일 경계마다 폭 |off| 의 세로 검은 줄.
실측 최장 세로 연속 **98행 @x=988 · 89행 @x=476**(간격 512 = GT_W) · 바람 0 이면 사라짐 ·
강풍이면 굵어지고 이동. 수리 = **2패스**(바탕 전부 → 잎). 수리 후 47행 = 잡음 바닥.

★계약 판정 1건 정정: `e2e-terrain` 의 `tex === 3` → 병행 세션이 **이름 판정**(`texNames`)으로 더 낫게 고쳐 놨다.
원인은 **병행 세션이 `db055cb` 에서 `rock_angled` 를 더해 4종**이 된 것 — 계약이 바뀐 것이지 결함이 아니다.
리베이스에서 그쪽 판정(있어야 할 텍스처 **이름**을 다 확인)을 취했다. 개수만 세면 무엇이 빠졌는지 모른다.

### 회부 (구현 안 함)
- ★★**물가 풀 넘김은 반려됐다**(재민 3차 판정 *"after가 여전히 더 심해"*). 기본값 `shoreOff: true`.
  넘김을 어떻게 다듬든 **띠가 하나 더 생기는 것**이 본질이었다. 코드는 손잡이 뒤에 남아 있다 — 켜지 마라.
- ~~**띠의 진범 = 프리즘 상단 립**~~ → **내 오진이었다. 진범은 물 넘침이고 수리했다** (아래 ★★).
  립(`#4e6b40` 2.2px)은 화면 전체 **1,975px** 밖에 안 바꾼다 — 띠를 만들 양이 아니다.
  물 넘침이 사라진 화면을 보고 재민이 립까지 손댈지 판단하면 된다(없애기/잎 마스크/윗변 폐지 3갈래).
- ~~**바람 흔들림**~~ → **포기도 카펫도 실장했다**(위 두 항목). 남은 회부는 **나무 캐노피**뿐이다
  (스프라이트를 줄기/수관으로 나눠 구워야 한다). ~~지면 풀 텍스처는 비싸다~~ → **틀렸다. 했다.**
- ★**바람 흔들림(옛 기록) — 앞 인계의 "정적 캐시와 충돌"은 내가 틀렸다.** 청크 캐시는 **자리**를 담지
  픽셀을 담지 않는다. 실측(강가 headless): 자연물 패스 **0.317ms/f**(755장). 전단 변환을 걸어도
  0.7~1.0ms/f — 60fps 예산 안에서 싸다. **술·들꽃·나무는 흔들 수 있다.**
  위상은 자리 해시, 시각은 **게임 시계**(물 셰이더와 같은 `freezeT` 경로)라 결정론 판정도 안 깨진다.
  지면 풀 텍스처만 비싸다(재베이크/WebGL 이관 필요) — 거기까진 하지 마라.
- ★★**날씨 시스템** — 재민이 물었고(*"날씨에 따라 풀의 운동이 변하게 할 수 있나?"*) **지금 게임에 없다.**
  만든다면: 서버가 존별 날씨 상태(맑음/흐림/비/바람세기)를 틱으로 돌리고 welcome+broadcast 로 보내면,
  클라는 `__terrain19.windForce` 자리에 그 값을 먹이는 것으로 **바람은 끝난다**(이미 정본 함수 하나로 모았다).
  비·구름 그림자·수면 파문은 별개 작업이다. 순서상 **경제·농사에 날씨를 물릴지**가 먼저 결정돼야 한다.
- **계절**(낙엽·설경) — 시계 연동은 별개 시스템. 지금은 여름 한 벌.
- **다른 존 25곳 수종 차별화** — 사막·설원 미검증(배치 19 지면 질감 회부와 같은 자리).
- **물가 술의 게임플레이**(갈대 채집 = 이엉 재료?) · **rock/mossrock/ore 재렌더**(광맥 아이콘 정합).


## 3-z. ★★2026-08-25 배치 — **사건 레이어 토대 3종** (완료 · 3커밋, 산 아크 종결 `762939b` 위)

> 설계 정본: 레포 `설계_게임성_사건레이어_TODO.md` (§3.1~§3.3 · §10.1). 회부: `회부_사건레이어_다음층.md`.
> 착수 순서 §11 의 ②③ 을 한 배치로 묶었다.

### 무엇이 생겼나

* **`server/events.js` — 사건 장부**(신규, 단일 원천). 하루 경계 1회 판정, 사건 6종:
  `STOCK_SHORTAGE` · `STOCK_GLUT` · `PRICE_SPIKE` · `PRICE_DROP` · `CARAVAN_LATE` · `SEASON_CHANGE`.
  * **에지 트리거 + 히스테리시스** — "오늘도 소금이 없다"는 뉴스가 아니다. 거짓→참 전이 날 1건.
  * `mag` 은 전 타입 공통 **관측값÷기준값**. 심각도 정렬은 `|ln(mag)|` 하나로 전 타입 비교 가능.
  * 영속: `village_events`(마을당 90일 링버퍼) · `village_requests`(`(zone,vid,item)` UNIQUE).
* **촌장 브리핑** — 마을 중심 260px 안에 들어가면 **접근만으로** 촌장이 말을 건다(하루 한 번).
  마을 앵커 위 **말풍선**(기존 `drawSpeechBubble` 재사용) + HUD 한 줄(`#notice`). **새 패널 0.**
  자기 마을 것만 준다(전 서버 브로드캐스트 0).
* **게시판 납품 의뢰** — `Shift+G` 열람 · `Shift+N` 납품(품목은 **서버가** 고른다).
  보상은 **물물**(시세 등가 +20%). 납품은 서버 권위·원자적(잔여 초과분 거절).

### ★★이 배치의 제1 규약 — **장부는 관측자다**

econ 을 건드리지 않는다. 세 겹으로 지켰고 **실측으로 증명**했다:
같은 틱 스트림에 장부를 **0개 / 3개 / 5개** 달고 800일을 돌려 최종값을 비교 —
**인구 6280 · 소멸 0/51 · 무기Q 821 · 거래 5000 으로 전부 동일**(시드 1020).
`test-events` ⑧ 이 그걸 회귀 가드로 건다(scanDay 5회 후 econ 상태 비트 동일).

### 문턱 채택값 — 실지도 51마을 3시드(1020·7·42) 800일

장부가 관측자라 **같은 세계에 후보를 동시에 얹어** 쟀다(카오스 잡음 0 인 A/B — `scripts/ev-density.js`):

| 후보 | 마을당 몇 일에 1건 (1020 / 7 / 42) | 판정 |
|---|---|---|
| A `±40% · H1.35` | 1.94 / 1.92 / 1.92 | 목표 밖(너무 잦다) |
| B `±55% · H1.60` | 2.08 / 2.08 / 2.07 | 목표 하단 |
| **C `±70% · H1.60`** | **2.21 / 2.22 / 2.22** | **채택** (재민 목표 "2~3일에 1건" 한가운데) |

되돌리기: `EV_PRICE_UP=0.40 EV_PRICE_DOWN=0.40 EV_HYST=1.35`.
★`SHORT_DAYS`(부족 정의 = 게시판 의뢰의 정의)는 **밀도 튜닝에 쓰지 않았다** — 밀도 맞추자고
흔들면 "부족"의 뜻이 바뀐다. 밀도는 가격 문턱과 히스테리시스로만 맞췄다.

**하루 경계 비용 2.5 ms/일**(51마을) = econ 틱 **346 ms/일**의 **0.7%**.
지시의 "<1ms/일" 은 마을 19곳 가정이었다 — 19곳 환산은 0.9 ms/일이라 그 기준도 든다.

**타입 분포**(시드 1020, 채택값): 부족 2752 · 글럿 2736 · 급등 6344 · 급락 6194 · 계절 408 · 지연 0.
**편중**: 마을당 90일 보유 최다 126 · 중앙 39 · 최소 3 → 회부 B-3.

### ★기준선 — 인계의 "19마을" 은 낡았다

지시서는 "소멸 0/19 유지"를 요구했지만 **현 레포의 실지도는 51곳**이다
(존 설정 `seedAllVillages` — 배치 16 의 50곳 전환). 실측 기준선(장부 부착 전후 동일):

```
  실지도 51마을 800일 · 시드 1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 거래 5,000
  실지도 51마을 800일 · 시드    7 : 인구 5,993 · 소멸 0/51 · 무기Q 755
  실지도 51마을 800일 · 시드   42 : 인구 6,072 · 소멸 0/51 · 무기Q 784
```
장부는 이 값을 **한 자리도 바꾸지 않는다**(0개 부착 실행과 비트 동일).

### 하네스

* **신규** `scripts/test-events.js` **55/0** — 지시 ①~⑦ + 자체 ⑧~⑬.
  전제 assert 를 먼저 걸고(고갈 **전** 재고>문턱 / **후** <문턱), **반대 방향도** 건다
  (③c: 소비EMA>0 이면 글럿이 실제로 난다 — ③이 "아무것도 안 난다"로 자명 통과하지 않게).
* **신규** `scripts/e2e-events.js` **29/0** (실클라 Chromium) — 접근→말풍선→게시판→납품→보상 왕복.
  이 검사만 `ENABLE_VILLAGES` 를 켠다(`VILLAGE_MAX=2 · VILLAGE_DAY_MS=500` 로 부팅 단축).
* **신규** `scripts/ev-density.js` — 밀도 실측·문턱 A/B. `EV_BASE=1` 이면 장부 0개 기준선.
* `scripts/econ-lab-real.js` 에 `EV_SCAN=1` 훅(랩 복제 아님 — 같은 모듈을 그 루프에 건다).
* 회귀 전원 통과: mountain **29/0** · mtcorridor **15/0** · mtocc **13/13** · nature **73/0** ·
  rooms **55/0** · cutaway **15/0** · metallurgy **20/0** · village **32/0** · guest **40/0**.
* ★**산 아크 종결(`762939b`) 위로 리베이스한 뒤 교차 재검증**(둘 다 `public/client.js` 를 건드렸다):
  test-events **55/0** · e2e-events **29/0**(내 쪽 클라 경로) · e2e-mtocc **17/17** · e2e-mountain **29/0**
  (산 아크 쪽 클라 경로). 텍스트 병합이 깨끗했다는 것과 **동작이 멀쩡하다는 것은 다른 말**이라 실제로 돌려 봤다.

### ⚠다음 세션이 먼저 알아야 할 것 2건 (내 배치와 무관)

1. **`e2e-terrain` 이 이 컨테이너에서 29/3 이다 — 그러나 코드 회귀가 아니다. 환경이다.**
   **서로 다른 코드 위에서 세 판**을 돌렸는데 셋 다 29/3 이고 실패 항목·수치까지
   **한 자리도 안 틀리고 동일**했다(4.7% · 0.811 · 0.05%):
   ① 내 배치 얹은 판 ② 깨끗한 `origin/main`(8efa007) 별도 worktree
   ③ **산 아크 종결 tip(762939b) + 내 배치** — 즉 병행 세션이 **32/0 을 보고한 바로 그 코드**.
   ⇒ 코드 갈래로는 설명이 안 된다. 실패 3건이 전부 **SwiftShader 화소 비율** 판정이고
     이 컨테이너 playwright 는 **1.56.0**(캐논 핀 **1.56.1**)이다. 베이크 누락도 아니다
     (`public/assets/terrain` 커밋돼 있음).
   ⇒ **다음 세션이 할 일**: playwright 를 **1.56.1 로 맞추고 재측정**. 그때 32/0 이면 환경으로 종결,
     그래도 29/3 이면 그때가 진짜 규명 시작이다. **지금 "회귀"라고 적으면 그게 추정이다.**
2. **`pngjs` 가 `package.json` 에 없었다.** 새로 클론해 `npm install` 하면
   mountain·terrain·nature·mtocc·rooms·cutaway **6종이 조용히 크래시**한다(MODULE_NOT_FOUND).
   이번에 의존성에 추가했다. — 하네스가 "실패"가 아니라 **크래시**로 죽으면 러너가 못 알아본다.
   (실제로 내 1차 러너가 `tail` 의 종료코드를 읽어 **전부 통과로 오독**했다. 러너도 하네스다.)

### 남은 회부 — `회부_사건레이어_다음층.md`

A(지시가 명시): 소문 물리 전파 · 마을 연대기 · 복귀 브리핑 · 플레이어 의뢰 게시 · 도적/포식자 · NPC 잡담.
B(실측으로 새로 나옴): **B-1 보상 수량**(`나무 1 → 가죽 55~64` 실측 — 손잡이 `EV_REQ_REW_CAP` 는 넣었고 기본 OFF) ·
B-2 시딩 중 접속하면 마을 목록이 빈다 · B-3 사건 밀도 편중 · B-4 CARAVAN_LATE 는 실서버 전용 ·
B-5 검출기 상태 비영속 · **B-6 게스트 입장 직후 재접속이 워프·초기 상태를 씻는다**(원시 WS 프로브로
서버·클라 각각 결백 확인 + pid 변경까지 잡았다 — 온보딩 §9 에 직결) · B-7 하네스 고정 포트 충돌.

---

## 3-y. ★★2026-08-26 배치 — **B-6 재접속 몸 승계 · B-1 보상 물리 상한 · terrain 판정 확정**

> 직전 배치(사건 장부)의 잔여 3건. 회부: `회부_사건레이어_다음층.md` B-1·B-6·B-8 절이 **해소로 갱신**됐다.

### ① B-6 — 재접속이 몸을 잃는다 (해소)

**진단이 절반 틀렸다.** 회부 원문은 "입장 직후 재접속"을 원인처럼 적었는데, 신규 하네스
`scripts/test-guest-rejoin.js`(원시 WS, 판당 ~1분)로 갈라 보니 **닫고 잇는 경로는 멀쩡하다**
(close 플러시가 새 세션의 central 읽기보다 먼저 착지). 깨지는 건 **옛 소켓이 아직 살아 있는 채로
새 소켓이 붙는 갈래 하나뿐**이다 — 클라 `ensurePrimaryConnection` 이 같은 틱에 close→connect 한다.
뿌리는 `savePlayer` 에 **주기 저장이 없다**는 것(접속·종료·핸드오프·행동 때만) — 걷기만 한 몇 초는 어디에도 없다.

**수리**: 같은 신원의 세션이 **아직 이 존 메모리에 서 있으면 그 몸이 진실이다**(저장본을 안 본다).
중복 차단 루프가 그 몸을 붙잡아 위치·인벤·도구·장비·숙련·원석장부·허기·체력·층을 승계하고,
밀려난 옛 세션의 `close` 는 **저장하지 않는다**(낡은 좌표가 나중에 도착해 덮어쓰는 것 차단).
신원·사회 정보는 central 정본 그대로 — **몸만** 이어받는다.

수리 전후 짝: 겹친 재접속 **256px 손실 → 0px**.
`test-guest-rejoin` **26/0** · `test-guest-identity` 62/0 · `e2e-guest-reconnect` 40/0.

★★**하네스 1차 작성이 자명 통과했다(25/25)** — 위치를 워프로 옮기고 물건을 `__e2e_give` 로 줬는데
**그 픽스처가 `savePlayer` 를 부른다**. 테스트하려던 "미저장 상태"를 하네스가 스스로 저장해 버린 것이다.
다시 짜서 물건 먼저 → **진짜 이동 입력**으로 걷기 → **central 저장 좌표를 직접 읽어 "미저장 드리프트가
실제로 있다"를 선행 assert** 하고서야 증상이 잡혔다. 파일 머리에 그 사고를 적어 뒀다.

**남은 것(회부)**: ~~주기 저장 없음~~ **✅2026-08-26 배치가 해소**(`SAVE_INTERVAL_MS` 기본 30초 —
아래 **3-x ②**) · `ensurePrimaryConnection` 의 같은 틱 close→connect · `checkOrphan` 2초 문턱.
남은 둘은 **접속 층 별도 배치**다 — 이번엔 "그래서 몸을 잃는다"만 끊었다.

### ② B-1 — 보상 물리 상한 (재민 확정 반영)

종전 판은 `rewQty = min(등가, 재고×비율)` — **조용히 덜 주고 있었다.** 게시판 값과 실제 값이
갈리면 그게 보이지 않는 손이다. 재민 확정대로 비율 캡(`EV_REQ_REW_CAP`)은 **기본 OFF 유지**하고,
**보상을 깎지 않는다. 못 갚으면 구하는 양을 줄인다. 그것도 안 되면 안 건다:**
ⓐ 전액 갚을 수 있는 잉여로(글럿 우선 → 지불가능량 순, **후보 전체 훑기**) ⓑ 요청 qty 축소 ⓒ 미게시.
물리 상한 `EV_REW_STOCK_FRAC`(기본 **0.25**) = 한 의뢰가 그 품목 재고의 이 비율을 넘겨 못 준다.
지킴 두 겹: **하루 경계 재검증**(갚을 수 없게 되면 거두거나 받은 만큼으로 닫는다) +
**납품 시 물건을 받기 전** 지불 가능성 확인(못 갚으면 거절·원자적 되돌림).

**실측(3시드 800일 · 51마을)** — 사건 밀도·기준선 **불변**(물리 상한은 의뢰층만 건드린다):

```
  시드 1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 채택값 2.21일/건
  시드    7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 채택값 2.22일/건
  시드   42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 채택값 2.22일/건
```
의뢰 게시는 **오히려 늘었다**(1,279→1,561 · 1,147→1,487 · 1,279→1,535, +20~30%) —
종전엔 최다 재고 품목 하나가 못 갚으면 그냥 실패했는데 이제 ⓐ 가 후보 전체를 훑고 ⓑ 가 줄여
**정직하게 걸 수 있는 마을이 늘었다**. 시드 42 계수: 축소 373 · 못갚아미게시 44,875(시도 기준) · 재검증철회 671.

`test-events` **71/0** — ⓐⓑⓒ 를 각각 **실제로 밟는** 상황으로 잰다.
★ⓑ 를 밟게 하는 데 두 번 헛짚었다: `qty=1` 이면 축소가 성립하지 않고(최소 1),
**보상 재고를 낮추면 그 품목 시세가 같이 올라** 지불가능량과 필요량이 나란히 줄어 창을 지나친다.
**가격을 고정한 채 상한 비율만** 조여야 잡힌다.

### ③ e2e-terrain 29/3 — **판정 확정: 회귀가 아니다. 하네스의 미확인 전제다**

세 단계로 갈랐고 마지막은 **눈**이 잘랐다.
1. **코드 아님** — 서로 다른 코드 세 판에서 전부 29/3, 수치까지 동일(4.7%·0.811·0.05%).
2. **playwright 버전 아님** — 1.56.1 로 핀해도 **똑같이 29/3**. 그럴 수밖에 없다:
   **1.56.0 과 1.56.1 이 같은 `chromium-1194` 바이너리를 쓴다**. 앞 배치의 버전 가설은
   화소만 보고 세운 추정이었고 그래서 틀렸다(원칙 ㊶ — 추정을 원인으로 쓰지 마라).
3. ★**실패 화면을 눈으로 봤다** — `#downPanel`("☠️ 다운 · 사망")이 화면 한가운데를 덮고
   **HP −1/100**, 장면이 어둡다. 물 화소가 준 건 물 렌더가 아니라 **모달이 덮고 어두워졌기 때문**이고,
   나머지 두 실패도 전부 그 하나의 하류다(밝기가 눌리면 상관도 무너지고 5px 단면 밴드도 사라진다).

**이번에 한 것**: `ENABLE_WILDLIFE=0` 추가 + **생존 전제 assert** 추가 — 이제 죽으면
화소 3건이 아니라 **"플레이어가 죽었다"** 한 줄이 말한다(그래서 지금 이 컨테이너에선 **29/4** 다).
~~**남은 것(회부)**: 야생을 꺼도 여전히 죽는다~~ → **✅2026-08-26 배치가 해소**했다:
죽인 것은 **늑대**였고(`☠️ … by mob:wolf` 실측), `ENABLE_WILDLIFE` 의 뜻을 **적대 개체 전부 OFF**
로 통일해 끊었다. **33/0**. 자세한 건 아래 **3-x ①**.

### ④ 회귀 러너를 레포에 넣었다 — `scripts/run-regress.sh`

직전 배치에서 손으로 짠 러너가 **크래시 6종을 통과로 오독**했다(`node … | tail` 뒤 `$?` 는 `tail` 것).
이제 ①종료코드를 **파이프 앞에서** 잡고 ②하네스 사이에 서버를 정리하고(고정 포트 3010/3020 충돌 방지)
③**PASS 줄이 없으면 실패로 센다**. `bash scripts/run-regress.sh --selftest` 가 **일부러 크래시를 내서
러너 자신을 검사**한다(통과 확인함). **러너도 하네스다.**

### 회귀 결과 (순차 단독 · 러너 경유)

`test-events` 71/0 · `test-guest-rejoin` 26/0(신규) · `test-guest-identity` 62/0 ·
`e2e-events` **30/0** · `e2e-guest-reconnect` 40/0 · `e2e-mountain` 29/0 · `e2e-mtcorridor` 15/0 ·
`e2e-mtocc` 17/17 · `e2e-nature` 73/0 · `e2e-rooms` 55/0 · `e2e-cutaway` 15/0 ·
`e2e-metallurgy` 20/0 · `e2e-village` 32/0 · `e2e-terrain` **29/4**(위 ③ — 전제 실패 1건이 진실을 말한다).

⚠`e2e-events` 는 하네스 쪽을 세 군데 고쳐야 했다(전부 **경제가 계속 돈다**는 성질 때문):
하루 0.5→1.2초 · 의뢰가 철회되면 다시 세우는 `ensureBoard()` · **납품 직전의 행**으로 재료를 받기
(첫 목록의 품목을 쥐고 있으면 헛낸다 — `약초` 의뢰에 `나무` 를 내고 있었다) ·
촌장 브리핑이 같은 `#notice` 를 덮으므로 게시판 표시는 짧게 여러 번 본다.

---

## 3-x. ★★2026-08-26 배치 — **정리 소배치: terrain 사망 원인 · 주기 저장 · e2e 시간 손잡이**

> 새 기능 없음. **회귀를 전부 녹색으로 만들고 저장 구멍을 닫는 것**이 전부다.
> 라이브 기본 동작은 한 줄도 안 바뀐다 — 이번에 생긴 손잡이는 전부 테스트에서만 켜진다.

### ① e2e-terrain 29/3 → **33/0** — 죽인 것은 늑대였다

앞 배치는 "하네스의 미확인 전제"까지만 밝혔다. 이번엔 **원인을 이름으로** 잡았다.
`damagePlayer` 의 사람 갈래에 사망 출처 로그를 넣고 재현했더니:

```
☠️ 여행자(p401) 사망 — by mob:wolf @ (…) hp0 배고픔 98 목마름 97
```

배고픔 98·목마름 97 이므로 아사도 낙하도 아니다. **늑대다.**
`ENABLE_WILDLIFE=0` 은 `wildlife.js` 만 껐고 **레거시 `mobs`(늑대·자칼 무리)는 안 껐다**.

**수리 — 플래그를 새로 만들지 않았다.** 두 개가 되면 다음 세션이 하나만 끄는 사고를 그대로
재생산한다. `ENABLE_WILDLIFE=0` 하나의 뜻을 **적대 개체 전부 OFF** 로 넓혔다.

**적용 범위(blast radius) — 게이트 4곳**:

| 자리 | 무엇이 막히나 |
|---|---|
| 시딩 블록 `while (HOSTILES_ON && aggressive…)` | 공격 개체 무리 **최초 스폰** |
| 리스폰 `if (HOSTILES_ON \|\| !ANIMALS[t].aggressive)` | 죽은 뒤 **재생성** |
| 사냥 리스폰(두 번째 자리) | 같은 규약 |
| 몹→플레이어 공격 `damagePlayer(t, …, 'mob:'+m.type)` | **피해 0** |

평화 개체(사슴·양)는 그대로다 — 적대가 아니다.
**이 값을 끄는 곳은 전부 하네스**다(운영 사용처 0): `e2e-terrain` · `test-tame` · `test-mining` ·
`test-furnace` · `pace-metallurgy` · `era-rehearsal` · (이번에 추가) `test-save-periodic`.
전부 "야생 없는 결정론"을 원해서 끄던 것이라 의미가 넓어지면 **더 조용해질 뿐** 깨지지 않는다.

**결과**: `e2e-terrain` **33/0**. 물 화소 4.7% → **11.2%** — 앞 배치가 "물 렌더 결함"으로 의심했던
수치는 **사망 모달이 화면을 덮어 어두워진 것**이었다. 생존 전제 assert 를 같이 넣어 두어서,
다음에 또 죽으면 화소 3건이 아니라 **"플레이어가 죽었다"** 한 줄이 먼저 말한다.

### ② 주기 저장 — 걷기만 한 진행이 크래시를 넘는다

`savePlayer` 는 접속·종료·핸드오프·행동 때만 돌았다. **걷기만 한 몇 분은 어디에도 없었다.**
(B-6 수리는 "두 세션이 겹치는" 갈래만 막았고 크래시 갈래는 그대로였다 — 3-y ①의 남은 것.)

`SAVE_INTERVAL_MS` **기본 30000**. 설계는 넷 다 의도적이다:
**틱당 최대 한 명**(스파이크 금지) · **사람마다 다음 저장 시각을 흩어 둠**(같은 순간에 안 몰리게) ·
**움직인 사람만**(`SAVE_MOVE_EPS=2px` — 인벤·행동은 원래 그 자리에서 저장한다) ·
**밀려난 세션 제외**(`_supersededBy` — 낡은 좌표가 나중에 도착해 덮어쓰는 것 차단, B-6 과 한 규약).

**비용 실측** — `scripts/cost-save.js`(zone 을 같은 프로세스에 띄워 `__testBind()._saveStats()` 정본을 읽는다):

```
  사람 8명 · 70초 · SAVE_INTERVAL_MS 6000(포화시킴)
  저장 84건 · 누적 22ms → **1회 0.262ms**
  60ms 표본 1155개 중 한 표본 최다 저장 **1건** ← 틱당 1건 상한이 실제로 지켜진다
  서버가 저장에 쓴 시간 ≈ 0.03%
```
★인원이 늘어도 이 값은 안 는다 — **틱당 1건이 구조적 상한**이라 인원이 늘면 한 사람당 주기가
길어질 뿐이다. 상한을 다 쓰는 최악에도 틱(50ms)의 **0.52%**.

**하네스 신규** `scripts/test-save-periodic.js` **20/0** — 자명 통과를 A/B 로 막았다:
ⓐ 간격을 999초로 두면 걷기만으로는 central 좌표가 **0px 움직인다**(픽스처 결백) ·
ⓑ 4초로 두면 같은 걷기가 **510px 따라온다** · ⓒ **소켓을 안 닫고 SIGKILL** 뒤 재기동 —
**9px 안**으로 복구 · ⓓ 승계 뒤의 주기 저장은 **새 몸**의 좌표를 쓴다.

### ③ e2e 시간 손잡이 — **데울 땐 흐르고, 상호작용 땐 얼린다**

`e2e-events` 가 계속 깨진 뿌리는 하나였다: **경제가 검사를 앞지른다**(0.5초/게임일이라 상호작용
몇십 초 사이에 60일이 지나 의뢰가 제 발로 철회되고 품목이 바뀐다).

여기서도 **새 env 플래그를 만들지 않았다** — 이미 있는 테스트 픽스처 게이트(`E2E_GIVE`) 위의
런타임 메시지로 냈다. `__e2e_day_freeze` → `villages.__e2eDayFreeze(on)`:
`state.dayFreeze` 가 서 있으면 하루 경계를 통째로 건너뛰고, **해동할 때 `lastGameDay` 를 지금으로
다시 앵커**한다(안 그러면 얼려 둔 날이 한꺼번에 쏟아진다).

쓰는 하네스: **`e2e-events` 하나**. 파일 머리에 시간 모드를 한 줄로 적어 뒀다
("데울 땐 흐르고, 상호작용 땐 얼린다"). 시간 자체가 검사 대상인 하네스는 **그대로 흐른다**.
앞 배치의 하루 0.5→1.2초 완화는 **되돌렸다**(얼리기가 그 자리를 대신하므로 데우기는 빠른 게 낫다).

### ④ 이 배치가 잡은 **하네스 결함 3건** (제품 결함 아님 — 그래서 더 위험했다)

**ⓐ `e2e-events` — 픽스처가 자기가 재려는 것을 망가뜨렸다.**
단독 실행은 31/0 인데 **다른 하네스 뒤에서만** 3~5건이 깨졌다. 알림이 원인을 그대로 말하고 있었다:
`📋 마을이 지금 갚을 것이 없다`. 게시판에 `돌 2 → 나무 9` 가 걸린 상태에서
`__e2eForceShortage` 가 **wood 를 55.6 → 1.83 으로 깎았다** — 마을이 약속한 보상을 픽스처가
없애 버린 것이다(게임일을 얼려 둔 구간이라 `revalidate` 의 자정도 안 돈다).
제품은 **옳게** 굴렀고(B-1 의 정당한 거절 + 원자적 되돌림) 하네스만 빨개졌다.
· 수리 1(서버 픽스처): **열린 의뢰가 약속한 보상 품목은 후보에서 뺀다.**
· 수리 2(서버 픽스처): 재고를 `문턱×0.1`(사실상 0)이 아니라 **`문턱×0.9`** 로 내린다 —
  바닥까지 비우면 그 품목 시세가 폭발해 ⓐ·ⓑ 가 둘 다 못 갚고 **의뢰가 아예 안 걸린다**
  (실제로 `wood 0→1.55` 인 판에서 30회를 걸어도 게시판이 비었다). 부족의 **정의**는 그대로 지킨다.
· 수리 3(하네스): `ensureBoard` 가 **먼저 보고** 비었을 때만 픽스처를 부른다.
· 그리고 회귀를 **이름으로** 잡는 assert 한 줄을 추가했다 —
  `★마을이 약속한 보상을 갚을 수 있다`. 이게 없으면 아래 세 개(인벤 차감·보상 수령·잔여 감소)가
  같이 무너지면서 "왜" 를 말하지 않는다. → `e2e-events` **32/0**.

**ⓑ `e2e-nature` — 사슴 한 마리가 결정론 판정을 깼다.**
같은 코드로 한 판은 73/0, 다음 판은 72/1. **두 프레임을 눈으로 대조**했더니
두 번째 프레임에 **사슴이 걸어 들어와 있었다**(1007화소). 무풍 잡음 바닥 판정도 같은 이유로
202 > 153 이 났다. 이 판정들의 뜻은 *자연물 자리 해시가 순수 함수인가*이지 **짐승이 멈춰 있는가**가 아니다.
· 수리: 클라가 **자기 변환으로** 생물이 그려진 화면 자리를 내주고(`window.__entBoxes()`,
  `__terrain19.entBoxes` 로 켜는 읽기 전용 훅 · 기본 꺼짐 · 라이브 비용 0),
  하네스는 **그 자리만 가리고** 견준다. 하네스가 화면 변환을 베끼지 않는다(사본 계측기 금지).
· ★**반례 장치**를 같이 넣었다 — 사슴이 지나가 주기를 기다리지 않고, **생물 자리에 가짜 얼룩**을
  찍어 "안 가리면 3600px 잡히고, 가리면 0px" 을 매 판 증명한다. 가리기가 죽으면 그 줄이 먼저 깨진다.
· 가린 넓이(4.2%)를 **같이 보고**하고 25% 를 넘으면 실패시킨다 — 다 가려 놓고 0 이라 말하는 자명 통과 차단.
→ `e2e-nature` **76/0**(73 → 76, 늘어난 3건이 위 반례·전제·가림넓이).

**ⓒ 러너가 실패한 줄의 이름을 안 보여 줬다.** `72/1` 만 보이고 **무엇이** 실패했는지는
로그를 다시 돌려야 알 수 있었다(이번에 두 번 그랬다). 이제 `✗` 줄을 **먼저, 따로** 찍는다.

### 회귀 결과 (순차 단독 · `scripts/run-regress.sh` 경유 · `--selftest` 선통과)

`test-events` 71/0 · `test-guest-rejoin` 26/0 · **`test-save-periodic` 20/0(신규)** ·
`test-guest-identity` 62/0 · `e2e-events` **32/0** · `e2e-guest-reconnect` 40/0 ·
`e2e-mountain` 29/0 · `e2e-mtcorridor` 15/0 · `e2e-mtocc` 17/17 · **`e2e-terrain` 33/0** ·
`e2e-nature` **76/0** · `e2e-rooms` 55/0 · `e2e-cutaway` 15/0 · `e2e-metallurgy` 20/0 · `e2e-village` 32/0.

⚠**미해결 1건 — `e2e-village` 가 세 판 중 한 판에서 31/1**. 단독 재실행 32/0,
앞 하네스(`e2e-metallurgy`)를 앞세운 같은 순서 재현도 32/0 으로 **두 번 다 재현 실패**했다.
그 판 로그에 `central save 실패 (e2echon): socket hang up` 이 있었다(하네스 종료 순서상 central 이
먼저 죽는 자리). **원인 미상으로 남긴다 — 추정을 원인으로 쓰지 않는다.**
다음에 나면 러너가 이제 **✗ 줄을 찍으므로** 어느 판정인지 바로 나온다. 그 줄부터 보라.

### 3시드 800일 기준선 — **한 자리도 안 움직였다**

```
  시드 1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,561   (기록값과 동일)
  시드    7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,487   (기록값과 동일)
  시드   42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,535   (기록값과 동일)
```

★단 하나 달라진 수치는 **장부 하루 경계 비용**이다: 기록 2.5 ms/일 → 이번 4.2~4.7 ms/일.
**코드가 아니라 기계다.** 같은 순간에 같은 판(200일·시드 1020)을 **HEAD(`18da87c`) 워크트리**와
작업 트리에서 나란히 돌렸더니 **4.079 vs 4.438 ms/일**이고 인구·사건수·밀도·분포·게시 건수는
**전부 비트 동일**이었다. 즉 이 컨테이너가 그때보다 느리다.
⇒ 인계의 ms 수치는 **기계 의존**이다. 비교할 때는 **같은 순간의 A/B** 로만 비교해라.

---

## 3-w. ★★2026-08-26 배치 — **낚시 v2: 판단·위험·손맛** (재미 층 첫 시제품)

> 설계 근거: `설계_게임성_사건레이어_TODO.md` §2·§10.1.
> 원칙: **"입력이 매번 같고 결과가 확실하면 게임이 아니라 진행바다."**

### ★먼저: 지시서의 전제가 틀렸다 — "v2" 가 아니라 **v1** 이었다

지시서는 "현행 낚시(배치 17~18 낚시터)"를 전제했는데, 배치 17~18 의 낚시터 작업은
**NPC 어부 길찾기**였지 플레이어 동사가 아니다. 플레이어 낚시의 실체는 `tryGather`(E키) 안의 **세 줄**이었다:

```js
// (걷어내기 전) Phase 5-11: 어업 — 50% 확률로 자원 획득
if (Math.random() < 0.5) { ... fishList[random] ... }
```

물가에서 E → 목 축이고 **동전 던지기 한 번**. 자리·수심·시간·기술·고갈이 전부 없었다.
지시서 §2 가 말한 "입력이 같고 결과가 확실한" 진행바의 교과서적 사례라, **그 세 줄을 걷어내고** 새로 세웠다.

### 무엇이 생겼나

| 층 | 파일 | 요지 |
|---|---|---|
| 물리 정본 | **`server/fishing.js`**(신규 ~330줄) | 자리 판정 · 셀 재고(로지스틱) · 결손 확산 · 입질/크기/창 대본 |
| 왕복 | `server/zone.js` | `fish_cast`/`fish_strike`/`fish_reel` · 입질 폴링 · 어획 정산 · 기록 |
| econ 접점 | `server/villages.js` | `waterVillageAt` · `refreshFishSustain` — **econ 은 한 줄도 안 고쳤다** |
| 영속 | `server/zone-local-db.js` | `fish_cells` 테이블(`mined_cells` 와 같은 문법) |
| 손맛 | `public/client.js` | 찌(대기 잔물결 / 입질 잠김) · 잡은 크기 표시 · **Shift+F** |

### ① 판단 — 자리가 어획을 **실제로** 가른다 (꾸며낸 보너스 0)

차등의 근거는 전부 `hanbando-terrain.json` 의 **실제 지형**이다. 강의 **폭 단위 거리**
`u = 중심선까지 거리 / 반폭` 하나에서 세 가지가 나온다:

* `depth01 = 1 − u` (한복판일수록 깊다) → **크기**
* `flow01` (물길 안 1, 반폭 2배에서 0) → 흐름의 세기
* `seam01` (u≈1 둘레) · `conflu`(물길 둘 이상 겹침) → **입질 빈도**

★**클라 물 셰이더가 쓰는 바로 그 `u`** 다. 그래서 **화면의 물살이 곧 힌트**이고, 힌트를 따로 안 그린다.
`e2e-fishing ①` 이 같은 칸에서 서버 `u` 와 클라 `__flowMap` 의 `u` 를 맞대 본다(**0.002 ↔ 0.05**) —
갈리면 화면이 거짓말을 하게 되므로 그 줄이 먼저 깨진다.

**실측 맞바꿈**(`scripts/fish-metrics.js`, 표본 20,000/자리):

```
자리                  대기ms  창ms  사이클s  중앙kg  상위5%kg  월척율   놓침율(반응 200/300/400/500ms)
호수 물가(얕음)         6067   640    6.7    0.58    1.64    2.4%    0% /  3% / 10% / 19%
호수 한복판(깊음)        6067   439    6.5    0.96    2.71   11.9%    0% / 12% / 31% / 47%
강 흐름 경계           3734   612    4.3    0.63    1.77    3.1%    0% /  3% / 12% / 22%
강 한복판(깊음)         6070   416    6.5    1.01    2.87   13.9%    0% / 14% / 34% / 51%
합류부/하구 ★명당        3241   574    3.8    0.69    1.95    4.4%    0% /  5% / 15% / 27%
```

읽는 법: **경계·합류부는 자주 무는데 잔챙이**(사이클 3.8~4.3초 · 월척 3~4%),
**한복판은 뜸한데 큰 놈**(사이클 6.5초 · 월척 12~14%)이고 **대신 놓친다**(반응 400ms에서 31~34%).
어디에 앉을지가 곧 "잔챙이를 빨리 여러 마리냐, 대어를 노리고 놓칠 각오냐"의 저울질이다.

### ② 명당은 고갈된다 — 그리고 **자리는 돌아오는데 어장 총량은 안 돌아온다**

`server/sustain.js` 는 이미 어장 MSY(`r·K/4`)를 물리고 있었지만, 그 파일이 스스로 이렇게 적어 뒀다:
**"이 어장이 지금 몇 마리 남았나는 아직 랩만 안다 — 상한은 물리되고 재고는 미이식."**
낚시 v2 가 채운 게 정확히 그 미이식분이다. 새 물리를 발명하지 않았다 —
**같은 로지스틱의 셀 단위 재고**를 서버에 들여놨고, 모든 셀이 만땅이면 기존 식과 **같은 값**이 나온다.

* 한 자리(반경 3셀 = 29셀) **34.8kg · 32~50마리**면 바닥난다 → 대략 3~5분.
* 회복은 **두 층**이다:

```
 1분 뒤 — 자리 재고 11% · 어장 총 결손 100.0%
 5분 뒤 — 자리 재고 29% · 어장 총 결손  99.9%
10분 뒤 — 자리 재고 40% · 어장 총 결손  99.6%
30분 뒤 — 자리 재고 62% · 어장 총 결손  98.4%
60 게임일 뒤 —          어장 총 결손  30.3%   (정본 로지스틱 r=0.02/일)
```

★**자리가 돌아오는 건 번식이 아니라 옆에서 물고기가 들어오기 때문**이다. 그건 총량을 안 늘린다.
그래서 결손 d=K−s 를 **총합 보존한 채** 이웃으로 번지게 한다(`diffuse`). 총량이 주는 유일한 길은
정본 r 뿐이다. `test-fishing ③` 이 **둘 다** 검사한다("자리는 회복하는데 총 결손은 그대로다").

⚠수리 하나: 1차 판은 dt 를 통째로 한 걸음에 밀어서 **번짐이 흐른 시간이 아니라 틱 주기에 좌우**됐다.
명시적 확산이라 한 걸음 비율이 0.5 를 넘으면 불안정하기도 하다 ⇒ **a ≤ 0.4 로 쪼개** 푼다.

### ③ 위험 — 서버 시각이 권위다

입질 시각도 창 폭도 **서버가 정하고 서버가 잰다.** 클라는 "지금 챘다"만 보낸다.
`tryFishStrike(player)` 는 **인자를 하나만** 받는다 — 클라 시각을 받을 자리가 애초에 없고,
`test-fishing ②ⓓ` 가 시각 필드를 실어 보내 **거절되는 것**을 확인한다.

**창 곡선**(채택): `창 = 350ms × kg^(−1.25)`, `[90, 900]` 로 자름. +레이턴시 여유 150ms.
```
0.3kg → 900ms(상한)   0.58kg → 695ms   1.0kg → 350ms   2.0kg → 147ms   3kg↑ → 90ms(하한)
```
★1차 판은 `1/√kg` 였는데 너무 완만해 **반응 500ms 에서도 놓침 0~2%** 였다 —
재민이 준 시작점 20~30% 와 멀고, 무엇보다 "놓치는 대어"가 안 생겨 §2 의 **위험**이 빈칸이 됐다.

크기는 **로그정규**(광맥 등급 산포 선례). ln(kg) 왜도 −0.034 ≈ 0 으로 확인.

### ④ 일관성 — NPC 어부와 **같은 물**을 쓴다 (econ 무수정)

엔진은 이미 `v.land.fishSustain` 을 읽어 어부 산출을 물린다(`_fishScale`). 그러니
**플레이어가 퍼 간 만큼 그 키를 낮추면** 그게 곧 같은 물이다. 환산 계수는 `sustain.js` 정본 그대로.

```
농촌1: 기준 19.99/일 · 결손 15.18 stock → 감산 0.19 → 지금 상한 19.80
⇒ NPC 가 체감하려면 플레이어가 **1,599 stock(≈1,919kg)** 을 긁어야 한다
```

★★**그런데 그 키가 원래부터 사장돼 있었다** — `createVillage` 의 land 화이트리스트가
`fishSustain`·`woodSustain`·`forageSustain`·`marginalQ` 를 통째로 버린다(11차 배선이 통째로 죽어 있다).
**이번 배치에서 고치지 않았다**(경제 결정이다) → **`회부_MSY상한_사장.md`**.
낚시는 값만 들고 있는 길로 우회했다(`ev.land._fishBase`) — 플레이어가 그 물을 긁기 전까지
`land.fishSustain` 은 여전히 `undefined` 라 **NPC 산출은 오늘과 비트 동일**하다.

### ⑤ 손맛 — 새 패널 0

찌를 **세계 안**에 그린다(촌장 말풍선과 같은 규약 · 렌더러블 루프 뒤).
대기는 잔물결 + 느린 1px 상하, 입질은 빠른 흔들림 + 아래로 잠김. 잡으면 **크기가 화면에 보인다**(대어는 크게).
키는 **Shift+F** 하나로 던지기→챔질을 다 한다(서버가 상태로 가른다).

⚠**여기서 결함이 하나 났고 화소가 잡았다** — 서버가 보내는 좌표는 **존 로컬**인데 렌더러는
**절대 월드**를 쓴다. 변환을 빼먹어 찌가 **40만 px 밖**에 그려졌다.
상태·알림·인벤은 **전부 통과**했고 **화면만 비어 있었다**(배치 5 의 그 층이다).
⇒ `e2e-fishing ⑦` 이 시계를 얼리고 **대조군(안 던진 두 장) 0px vs 던진 뒤 714px** 로 못 박는다.

### 하네스

* **신규 `scripts/test-fishing.js` 49/0** — 지시 ①~⑥ + ⑦ 픽스처 결백.
  자리 차등은 **웰치 t 검정**(표본 4,000×2): 크기 t=31.3 · 대기 t=−34.9,
  그리고 **반례**(같은 자리 두 표본 |t|=1.2)로 "그 t 가 자리가 만든 것"임을 보인다.
* **신규 `scripts/e2e-fishing.js` 31/0** — 실클라. 물살 힌트 대조 · Shift+F 배선 · 입질 · 창 안/밖 · 거리 게이트 ·
  **찌 화소**(대조군 있음).
* **신규 `scripts/fish-metrics.js`** — 대리 지표(위 표). 하네스 아니라 **손잡이 돌릴 때 보는 자**다.

### ★재민이 직접 잡아 볼 3줄

1. `cd ~/Mini/durango-mini && node server/central.js` 와 `ZONE_ID=hanbando PORT=3020 node server/zone.js` 를 띄우고
   브라우저로 `http://localhost:3000` → **월드 입장**.
2. 강가로 걸어가 물이 **흐르는 게 보이는** 자리에 선다(셰이더의 물살이 힌트다).
   **한복판 쪽**이면 큰 놈·뜸함, **가장자리/합류부**면 잔챙이·잦음.
3. **Shift+F** 로 던지고 → 찌가 **확 잠기면** 다시 **Shift+F**. 잔챙이는 여유롭고 월척은 손이 빨라야 한다.
   같은 자리를 30마리쯤 긁으면 눈에 띄게 안 물린다 — 그때 자리를 옮겨 보라.

### 손잡이 (전부 env · 미설정이 채택값)

| 손잡이 | 채택값 | 무엇이 바뀌나 |
|---|---|---|
| `FISH_WIN_AT_1KG` | **350** | 1kg 물고기의 창. **난이도의 주 손잡이** |
| `FISH_WIN_POW` | **1.25** | 클수록 대어가 급격히 어렵다(놓침율의 기울기) |
| `FISH_WIN_MIN_MS` / `FISH_WIN_BASE_MS` | 90 / 900 | 월척의 하한 · 잔챙이의 상한 |
| `FISH_WIN_LAT_MS` | 150 | 레이턴시 여유(넷코드 공정성 — 회부 B-2) |
| `FISH_WAIT_BASE_MS` | 6000 | 사이클 길이(자리 점수로 나눈다) |
| `FISH_SIZE_MU` / `FISH_SIZE_SIGMA` | −0.55 / 0.62 | 크기 중앙값·꼬리(월척율) |
| `FISH_DEPTH_W` / `FISH_SEAM_W` | 0.85 / 0.90 | 수심→크기 · 경계→빈도의 세기 |
| `FISH_DRAW_R` | **3** | 자리 수명(반경 2 = 13마리로 너무 짧았다) |
| `FISH_KG_PER_STOCK` | 1.2 | 어획 → 재고 차감 환산 |
| `FISH_DIFF_PER_DAY` | **18** | 자리 회복 속도(6이면 30분에 27% — 일방통행이었다) |
| `FISH_TICK_MS` | 60000 | 확산·정리 주기(값에 무관하게 결과가 같도록 하위 걸음으로 쪼갠다) |

### 회귀 17/17 · 실패 0 (순차 단독 · `run-regress.sh` · `--selftest` 선통과)

```
test-events 71 · test-fishing 49(신규) · test-guest-rejoin 26 · test-save-periodic 20 ·
test-guest-identity 62 · e2e-events 32 · e2e-fishing 31(신규) · e2e-guest-reconnect 40 ·
e2e-mountain 29 · e2e-mtcorridor 15 · e2e-mtocc 17 · e2e-terrain 33 · e2e-nature 76 ·
e2e-rooms 55 · e2e-cutaway 15 · e2e-metallurgy 20 · e2e-village 32
```
※직전 배치가 남긴 `e2e-village` 간헐 31/1 은 **이번 판에서 안 났다**(32/0). 여전히 원인 미상으로 둔다.

### 3시드 800일 기준선 — **한 자리도 안 움직였다**

```
1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,561   (기록값과 동일)
   7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,487   (기록값과 동일)
  42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,535   (기록값과 동일)
```
당연하다 — 낚시는 **플레이어가 있어야** 도는 층이고 헤드리스 랩엔 플레이어가 없다. econ 도 안 고쳤다.

★덤: 장부 하루 경계 비용이 **2.46~2.66 ms/일**로 나왔다. 직전 배치에서 같은 스크립트가 4.2~4.7 을 냈고
"코드가 아니라 기계"라고 적었는데, 이번 판이 **기록값 2.5 로 돌아오면서 그 판정을 확증**했다.
⇒ ms 수치는 기계 의존이다. 같은 순간 A/B 로만 비교하라(원칙 (52)).

### ★병행 세션과의 교차 재검증 (원칙 ㊺)

커밋 뒤 `origin/main` 이 **네 커밋 앞서 있었다**(`30f85fb` — 병행 세션의 산 계측기 수리 3건 + waterperf).
저쪽도 `public/client.js` 를 **591줄** 고쳤다. 리베이스는 텍스트로 깨끗했지만 그건 동작이 멀쩡하다는 말이 아니다
⇒ **양쪽 하네스를 다 돌렸다**: `e2e-fishing` 31/0(내 쪽 클라 경로) · `e2e-mtocc` 18/18 ·
`e2e-mtcut` 26/26(저쪽 신규) · `e2e-mtfuzz` · `e2e-nature` 76/0 · `e2e-mountain` 29/0 — **6/6 전원 통과**.

⚠**러너가 두 개가 됐다**: 내 `scripts/run-regress.sh` 와 저쪽 `scripts/run-regressions.sh`(복수형).
다음 세션이 하나만 돌려 나머지 하네스를 통째로 빼먹기 딱 좋다 — **합치거나 하나를 지워라.**

### 회부 (구현 금지)

`회부_낚시_다음층.md` — 미끼·낚싯대 티어 · 어종 확장 · 어군 실체화 · 감정 연동 ·
이력서 패널 UI · 효과음 · 대회/의뢰 연동 + v2 가 드러낸 판단거리(레이턴시 보정 · 자리의 사회성 · 곳간 경로 · 밤낚시).
`회부_MSY상한_사장.md` — **랩 MSY 상한 4종이 econ 에 닿지 않는다**(별건 · 경제 결정).

---

## 3-v. ★★2026-08-26 배치 — **신체 상태(§7) + UI 골격(§8.2·8.3·8.6)** + 러너 통합

> 헌법 두 줄: **"상태는 생존 압박이 아니라 경제·리듬의 접속면"** · **"속은 연속, 겉은 계단."**

### ★§0 인벤토리 — 지시서 전제와 다른 게 셋이었다 (전부 "이미 있다")

이 규약이 두 배치 연속으로 값을 했다. 실측 결과:

1. **배고픔·목마름은 이미 연속값으로 돌고 있었다** — 접속 중에만 감쇠(틱이 `players` 만 돈다),
   스프린트 게이트·HP회복 게이트·밤 추위 허기 가속까지. **아사는 재민 확정으로 이미 폐지**돼 있었다.
   ⇒ 오프라인 감쇠 금지는 **이미 지켜지고 있었다**(새로 막을 게 아니라 **지켜지는지 못 박을** 일이었다).
2. **장비 슬롯도 있었다** — `equipSlots{clothes,armor,weapon,tool}` + `clothes.attrs.warmth` 가
   이미 밤 추위를 막고 **옷이 닳았다**. ⇒ 지시 §0 의 "슬롯 없으면 추위 옷 저항 회부" 분기는 **해당 없음**.
3. **좌측 기둥·패널 프레임도 있었다** — `#sidebar` 6버튼 · `#sidePanel` · `openSide/closeSide` ·
   ESC · 논모달까지. ⇒ 이번 일은 "프레임 세우기"가 아니라 **규약 점검 + 칸 하나 채우기**였다.

**없던 것**: 추위/피로/부상/사기 축 · **연속 효과 곡선** · 무들 · 상태 패널 ·
그리고 **마을에서 식품을 사는 경로**(→ 회부 B-1).

### 무엇이 생겼나

| 층 | 파일 | 요지 |
|---|---|---|
| 정본 | **`server/body.js`**(신규 ~250줄) | 5축 · piecewise linear 곡선 · 단계+히스테리시스 · 바닥 · 저장/전송 |
| 배선 | `server/zone.js` | 생존 틱을 `Body.tick` 으로 이관 · 부상 문턱 · 노동 훅 3곳 · 주기 저장 dirty |
| 화면 | `public/client.js`·`index.html`·`style.css` | 상태 탭(Shift+H) · 무들 · 비네트 · **예측 속도에 moveMult** |

### ① 축 다섯 + 사기 — §7 확정 그대로

**심심함·스트레스는 넣지 않았다**(§7 기각). 대신 **사기가 당근**이다 — 조리식 +0.55 · 생식 +0.10,
10분에 식고, 작업속도를 **올린다**(벌이 아니라 상).

* **배고픔·목마름** — 연속. `BODY_HUNGER_SEC` **2700**(45분) · `BODY_THIRST_SEC` 1200.
  ★채택 근거(실측): 1800s 면 조리 고기 기준 **4끼/시간**으로 잦았다. 2700s 에서 **2끼/시간** = 재민 목표(2~3) 안.
* **추위** — 밤·계절이 올리고 불·실내·옷이 내린다. 계절은 `events.seasonOf` **정본을 부른다**(겨울 1·봄가을 0.35·여름 0).
* **피로** — 노동 훅(채광 1.0 · 챔질 0.6 · 건설 1.5)으로 오르고 정지·**실내(×2)**에서 내린다.
* **부상** — ★**주사위가 아니라 피해량 문턱**(일관성 원칙). 12 미만은 안 다치고, 초과분 1당 0.02.
  회복은 시간 + **약초(herb) ×6**. `herb` 를 `FOOD_EFFECTS` 에 넣어 먹을 수 있게 했다(§7 약초 수요의 실배선).

### ② 속은 연속 — 곡선과 **바닥**

효과 = piecewise linear(제어점 4~6, 축마다 이속/작업 두 벌). **문턱 절벽 없음**을 하네스가 잰다:
0.002 폭 스윕에서 인접 표본 최대 도약 **0.0014 < 0.0024**.

★**1단계 경계를 상수로 박지 않았다** — 곡선에서 **이속 −5% 가 되는 x 를 유도**해 쓴다(§8.3 "1단계는 효과 체감점").
결과: 배고픔·목마름·피로 0.700 / 추위 0.650 / 부상 0.228.

**죽음의 나선 방지**(§7 확정): 곱 합산에 바닥 `0.6/0.6`. 전 축 최악이면 곱 원값 **0.353/0.286 → 0.6/0.6**,
그리고 **바닥이 걸렸다는 사실을 상태 패널이 말한다**(투명성). 동시 표시 무들은 **3개** 상한.

### ③ 겉은 계단 — 히스테리시스

단계 전환에만 ±0.04. 경계를 200회(서버)·14회(실클라) 오가도 **전환 0회**.
★자명 통과 금지로 "확실히 넘기면 실제로 켜지고 꺼진다"를 같이 잰다 — 무들이 죽어 있어도 0회는 나오니까.

### ④ 화면 — 새 패널 0, 프레임에 칸 하나

상태 탭(🫀 **Shift+H**)을 기존 기둥에 더했다. 패널은 §8.6 그대로 **"왜 내가 지금 이렇지"에 답한다**:

```
지금 걸린 효과
  🍖 이속 -9% · 작업 -14% (배고픔 0.82)
  😮‍💨 이속 -6% · 작업 -14% (피로 0.75)
합계  이속 ×0.69 · 작업속도 ×0.61
```

무들은 화면 오른쪽 모서리, 단계 색만. 아날로그 채널은 **최소만** — 3단계에서 가장자리 비네트 하나.

⚠**결함 하나를 화면이 잡았다**: 패널 제목이 영문 키 `body` 로 떠 있었다(제목 표에 안 넣어서).
스크린샷을 열어 본 순간 보였고, 그래서 `e2e-ui` 에 **제목이 한글인지** 묻는 줄을 넣었다.

★★**다음 패널 구현자를 위한 3줄** — 탭 하나는 **세 군데**를 다 밟아야 완성된다:
① `index.html` 의 `#sidebar` 에 `.sb-icon[data-side="이름"]` 한 줄(**단축키 병기** — §8.2)
② `client.js openSide` 의 제목 표에 한글 이름(**빠뜨리면 영문 키가 그대로 제목에 뜬다**)
③ `renderSide` 에 분기 + 렌더 함수. 하나라도 빠지면 반쪽 탭이 된다.

### ⑤ 넷코드 — 이속 배율은 **양쪽이 같은 수**를 써야 한다

몸이 걸음을 늦추므로 서버가 곱한 배율을 `gauges.body.moveMult` 로 실어 보내고
클라 `predictStep` 이 **같은 수**를 쓴다. 안 맞추면 매 틱 보정이 나서 **러버밴딩**이 된다
— 그 줄의 원래 주석이 경고하던 바로 그 함정이다.

### ⑥ 러너 통합 (작업 ③) — **빼먹고 있던 5종을 찾았다**

`run-regress.sh`(내 것)와 `run-regressions.sh`(병행 세션) 두 목록이 갈려 있었고,
어느 쪽을 돌리든 상대편 것을 통째로 빼먹고 있었다. 실제 차집합:

* 내 러너에 **없던 것 5종**: `e2e-mtcut` · `e2e-mtfoot` · `e2e-mtfuzz` · `e2e-tilestate` · `e2e-waterperf`
* 저쪽에 없던 것 7종: events · body · fishing · save-periodic · guest 계열 · terrain · nature

⇒ **`run-regress.sh` 하나로 통합**(24종). `run-regressions.sh` 는 `exec` 위임 한 줄로 남겼다
(문서·습관에 호출처가 있어서 지우지 않았다 — grep 으로 확인). `--selftest` 는 위임 경유로도 동작한다.

### 대리 지표 (`scripts/body-metrics.js`)

```
① 1단계(이속 −5%) 도달        배고픔 31.5분 · 목마름 14.0분 · 피로 22.4분(채광 1타/초)
                              추위 6.5분(맨몸·한겨울 밤) · 부상 = 늑대 한 대(15)는 0.060 → 무증상
② 1시간 식사 횟수             조리 고기 평온 2끼 · 한겨울 밤 4끼 · 산딸기 11끼   ← 목표 2~3 안
③ 단독 최악 이속              배고픔 ×0.82 · 목마름 ×0.80 · 추위 ×0.85 · 피로 ×0.88 · 부상 ×0.72
   전부 최악                  곱 0.353 → **바닥 0.6** · 무들 3개(상한)
   현실 조합(하루 내내 채광)   허기 20 · 피로 0.75 → 이속 ×0.69 · 작업 ×0.64 = 저녁엔 36% 느리다
```

### ★재민이 직접 확인할 3줄

1. `node server/central.js` + `ZONE_ID=hanbando PORT=3020 E2E_GIVE=1 node server/zone.js` 띄우고 월드 입장.
2. **Shift+H** 로 상태 패널을 연다(연 채로 걸어 보라 — 안 닫힌다). 20~30분 놀다 보면 🍖 무들이 뜬다.
3. 빨리 보고 싶으면 개발자 콘솔에
   `__sendPrimary({type:'__e2e_body',hunger:10,fatigue:.9,injury:.5,cold:.6})` —
   무들·비네트·"이속 −n% (원인)" 문장이 한꺼번에 뜬다. 되돌리려면 같은 명령에 `hunger:100,fatigue:0,injury:0,cold:0`.

### 손잡이 (전부 env · 미설정이 채택값)

| 손잡이 | 채택값 | 무엇이 바뀌나 |
|---|---|---|
| `BODY_HUNGER_SEC` / `BODY_THIRST_SEC` | **2700** / 1200 | 식사 리듬(실측 2끼/시간) |
| `BODY_COLD_RISE_SEC` / `BODY_COLD_SEASON_W` / `BODY_COLD_HUNGER_EXTRA` | 600 / 1.0 / 0.6 | 겨울의 매운맛 |
| `BODY_FATIGUE_PER_LABOR` / `BODY_FATIGUE_REST_SEC` / `_INDOOR_MULT` | 0.0008 / 900 / 2.0 | 하루의 마디 |
| `BODY_INJURY_DMG` / `BODY_INJURY_PER_DMG` / `BODY_INJURY_HERB_MULT` | **12** / 0.02 / 6 | 위험의 이빨(회부 B-2) |
| `BODY_MORALE_COOKED` / `BODY_MORALE_SEC` / `BODY_MORALE_WORK` | 0.55 / 600 / 0.12 | 당근의 세기 |
| `BODY_MOVE_FLOOR` / `BODY_WORK_FLOOR` / `BODY_SHOW_MAX` | 0.6 / 0.6 / 3 | 죽음의 나선 방지 |
| `BODY_STAGE_HYST` / `BODY_DIRTY_EPS` | 0.04 / 0.01 | 깜빡임 · 갱신 빈도 |

### 회귀 23/24 — ★**러너를 합치자마자 한 건이 드러났다**

```
test-events 71 · test-body 45(신규) · test-fishing 49 · test-guest-rejoin 26 · test-save-periodic 20 ·
test-guest-identity 62 · e2e-events 32 · e2e-ui 29(신규) · e2e-fishing 31 · e2e-guest-reconnect 40 ·
e2e-mountain 29 · e2e-mtcorridor 15 · e2e-mtocc 17 · e2e-terrain 33 · e2e-nature 76 · e2e-rooms 55 ·
e2e-cutaway 15 · e2e-metallurgy 20 · e2e-village 32 · e2e-mtcut 26 · e2e-mtfuzz · e2e-tilestate · e2e-waterperf
```

⚠**`e2e-mtfoot` 8/9 실패** — `⑤ ★기슭은 산보다 납작하다 (기슭 vy 중앙값 0.34 < 산 0.45 의 72%)`.
★**이 배치 탓이 아니다.** 배치 **전** 커밋(`b6ba2cb`)을 워크트리로 꺼내 **같은 순간에** 돌려 보니
**수치까지 똑같은 8/9**였다. 즉 **원래 깨져 있었고, 내 러너가 이 하네스를 한 번도 안 돌려서 아무도 몰랐다.**
러너를 합친 첫 판에 바로 드러난 것이고, 그게 합친 이유다.
⇒ **고치지 않았다** — 산 아크(병행 세션) 소관이고, 기슭 기울기 판정은 그쪽 설계 판단이다. 다음 세션이 가져가라.

### 3시드 800일 기준선 — 한 자리도 안 움직였다

```
1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,561
   7 : 인구 5,993 · 소멸 0/51 · 무기Q 755
  42 : 인구 6,072 · 소멸 0/51 · 무기Q 784
```
당연하다 — 신체 상태는 **플레이어 전용 층**이고 헤드리스 랩엔 플레이어가 없다. econ 도 안 고쳤다.

### 회부 (구현 금지)

`회부_신체UI_다음층.md` — 긴장·공포(전투 층) · 사기 확장 · 타인 외형 · 인벤/스킬/제작 패널 내용
(★**스킬 패널은 지금 하드코딩 프로토타입**이다 — 빈 버튼은 아니지만 가짜 값을 보여 준다) ·
옷 warmth 품목표 + 만들며 드러난 판단거리:
**B-1 식사가 아직 마을 경제에 안 닿는다**(§7 채택 이유의 절반 — 마을에서 식품 사는 경로가 없다) ·
B-2 늑대 한 대는 "긁힘"이다 · B-3 겨울의 세기 · B-4 이속 배율과 넷코드 · B-5 무들 아이콘은 발판.

---

## 3-u. ★★2026-08-27 배치 — **마을 거래소: 플레이어 물물교환**

> 재민 원문: *"마을마다 거래소가 있어서 거기서 모든 경제학이 계산되고 npc끼리 교역하잖아 —
> 거기서 플레이어도 물물교환할 수 있어야 해."*
> 제1 규약: **가격 사본을 만들면 그 순간 실패다.** 가격 반응은 **공짜다** —
> 재고가 움직이면 시세는 저절로 따라온다. "가격 갱신" 코드를 짜고 있다면 사본을 만들고 있는 것이다.

### ★§0 인벤토리 — 지시서 전제와 다른 게 둘이었다

1. **곡식(`food`)에는 플레이어 아이템이 없다.** 곳간 대응표(`PV_DEPOSIT_MAP`)가 정본인데
   `food` 를 가리키는 아이템이 한 개도 없다. ⇒ 지시서의 "물고기→**곡물**"은 **문자 그대로는 실행 불가**다.
   대신 두 뜻을 다 지켰다: **표시는 곡식 환산**(`PRICE_NUMERAIRE=food`)이고 **내는 건 물고기**다.
   받을 품목은 그 마을 시세표에서 **살아 있는 것**을 하네스가 골라 쓰고 무엇을 골랐는지 로그에 남긴다.
   (회부 B-2 — 곡식을 들 수 있게 할 것인가.)
2. **물리 상한 부품이 `events.js` 안에 인라인**돼 있었다(B-1 배치). 두 벌을 만들지 않으려고
   **`events.payableQty(stock, frac)` 로 추출**하고 게시판 보상도 그 함수를 쓰게 바꿨다 —
   한쪽만 고쳐질 길이 구조적으로 없다(`test-trade ①` 이 양쪽 소스를 다 확인한다).

### 무엇이 생겼나

| 층 | 파일 | 요지 |
|---|---|---|
| 정본 | **`server/trade.js`**(신규 ~270줄) | 교환비 · 스프레드 · **조각 적분** · 금지/잠금 · 원자적 실행 |
| 정본 부품 | `server/events.js` | `payableQty`(B-1 과 공용) · `pricesFresh`(지금 재고 시세) |
| 배선 | `server/villages.js` | `villageTradeBoard/Quote/Exec` — 셋 다 **첫 줄이 260px 게이트** |
| 배선 | `server/zone.js` | `village_trade` · `village_trade_quote` · `village_trade_exec` |
| 화면 | `public/client.js`·`index.html`·`style.css` | §8 프레임 **둘째 칸**(Shift+T) · 세 클릭 문법 |

### ① 교환비 = 정본 그림자가격의 비율 (사본 0)

* 값은 `econV2.computeShadowPrices(v)` **한 함수**에서만 온다. `trade.js` 에는
  가격을 계산하는 줄이 **하나도 없고**, `computeShadowPrices` 를 직접 부르지도 않는다(접근자만).
* 마을이 살 땐 `p×(1−s)`, 팔 땐 `p×(1+s)`. **`TRADE_SPREAD` s = 0.10** 채택.
  근거 ⓐ 왕복 무한 차익 봉쇄 ⓑ "교역 세금 = 물물" 캐논의 플레이어 판(마진은 **물자로** 마을에 남는다).
* **물리 상한**: 한 거래가 그 품목 재고의 `TRADE_STOCK_FRAC`(=`EV_REW_STOCK_FRAC` 0.25)을 못 넘는다.
  넘치면 **거절이 아니라 절삭**이다(살 수 있는 만큼은 산다).

### ② ★★조각 적분(`planSliced`) — 하네스가 잡은 진짜 결함

1차 실장은 **거래 전 시세로 전량**을 계산했다. `test-trade ②`(스프레드 0 대조군)가 잡았다:
**나무 14 → 돌 45 → 나무 18.** 없던 나무 4개가 생겼다.
원인: 내가 팔면 그게 싸지고 사면 그게 비싸지는데, 되팔 때 **두 다리가 둘 다 유리**해진다 —
내 거래가 만든 가격 변화를 내가 그대로 주워 먹는다(한 번에 재고의 25%를 움직이니 폭이 s=10%보다 크다).
⇒ **곡선을 따라 적분**한다. 거래를 12조각으로 나눠 조각마다 **그때의 시세**로 값을 매긴다.
지금: s=0 에서 **정확히 본전**(19→46→19), s=0.10 에서 왕복 **−30%**.
※ 견적의 `maxGive` 는 곡선이라 나눗셈으로 못 내서 **이분 탐색 24회**로 낸다.

### ③ ★★가격 반응이 화면에 보이는가 — `e2e-trade ⑤` 가 잡은 것

**하루 캐시를 쓰고 있었다.** `board/quote` 가 `Events.pricesOf`(=`v._priceCache`, NPC 교역 틱이
**하루 한 번** 덮는 값)를 봐서, 같은 날 안에서는 재고가 어떻게 움직여도 같은 값을 돌려줬다.
별도 프로브 실측(캐시 warm → 날 얼림 → 대량 매도):

```
  나무  재고 19.95 → 26.95 (+35%)   표시 1.208 → 1.208   견적가 2.734022 → 2.734022
  돌    재고 150.26 → 116.26 (−23%) 표시 0.161 → 0.161   견적가 0.363397 → 0.363397
  해동 후 하루:                      표시 1.208 → 1.098   ← 그제서야 움직인다
```

결과가 둘이었다 — ⓐ **이 배치의 존재 이유가 화면에서 사라진다**(첫 30분 ② "내가 판 물건에 값이
움직인다") ⓑ **표시된 비율과 실제 수령량이 갈린다**(수령량은 `planSliced` 가 지금 재고로 낸다).
⇒ 거래소가 보는 시세는 **지금 재고의 함수**다. 하루 캐시는 NPC 교역 틱의 **성능 장치**일 뿐
정본의 정의가 아니다. `Events.pricesFresh` 로 고쳤다.
⚠**econ 변경이 아니다** — NPC 교역은 종전대로 제 하루 캐시를 읽는다(그 경로는 한 줄도 안 건드렸다).
플레이어가 움직인 재고는 다음 날 캐시가 다시 계산될 때 NPC 쪽에도 그대로 반영된다.

같이 잡힌 것: **표시를 `toFixed(3)` 으로 하고 있었다.** 한 마을 안 시세가 47.79 부터 0.0047 까지
걸치는데 소수점 고정이면 싼 쪽 네 품목이 화면에서 전부 "0.005"로 **같아 보인다**.
⇒ **유효숫자 4자리**로 바꿨다. 값이 같아 보이는 표는 정보가 아니다.

### ④ ★계측기가 자기 눈을 가린 사례 **세 번** — 전부 하네스 결함이었다

⑤는 **네 번 만에** 통과했다. 첫 실패만 제품 결함(위 ③)이고 **나머지 셋은 계측기**였다. 셋 다 남긴다:

* **ⓑ 가격 바닥을 골랐다.** `computeShadowPrices` 는 희소도를 `PRICE_ADJ_MIN..maxAdj` 로 **클램프**한다 —
  과잉 재고 품목은 바닥에 붙어 **재고가 늘어도 값이 안 움직인다**(글럿 바닥의 존재 이유 그 자체).
  1차 하네스는 "마을이 팔 수 있는 양이 가장 많은" 품목을 골랐는데 그건 곧 **가장 과잉인 품목**이라
  바닥일 확률이 가장 높은 선택이었다. **자기가 못 움직일 걸 골라 놓고 "안 움직인다"고 보고했다.**
* **ⓒ 두 판정을 한 짝에 묶었다.** "판 것 ↓ **그리고** 산 것 ↑"을 한 짝에서 동시에 요구했더니,
  러너 실행에서 `가죽→고기` 의 가죽이 **0.03226 → 0.0164 로 반토막 났는데도** 고기가 바닥이라 전체 실패였다.
  ⇒ 판정을 쪼개고 **각자 후보를 순회**하게 했다.
* **ⓓ 거래가 안 일어난 걸 "값이 안 움직였다"로 읽었다.** 받을 것 후보 6종이 전부
  `재고 10.41 → 10.41` 이었는데, 그건 **교환이 거절당한 것**이었다(비싼 물건을 내려는데 마을이
  값을 못 치르면 거절된다). 게다가 **실패엔 서버가 시세표를 다시 안 보내니 화면도 그대로**라
  "아무것도 안 변했다"와 구별이 안 된다. ⇒ `push()` 가 **성사 건수를 재고 변화로** 세고(알림 링버퍼는
  40칸이라 못 믿는다), 0 이면 서버 거절 문구를 찍은 뒤 다음 후보로 넘어간다.
  짝을 고를 때 **마을이 값을 치를 수 있는지**(`재고상한 × 시세 > 한 개 값 × 1.3`)를 먼저 본다.

지금 하네스는 후보를 순회하며 **실측으로** 고르고, 어떤 짝으로 성공했는지·건너뛴 이유·거절 문구를
전부 찍는다. 독립 2회 실행이 **서로 다른 마을 상태에서 각각 40/0**:
`나무 1.568→1.199`(팔았다) · `1.199→1.395`(샀다) / `약초 1.064→0.7309` · `0.7309→0.8148`.

### ⑤ 정보 비대칭 · 게시판 우위 · 금지 품목

* **원격 시세 조회 API 를 안 만들었다.** `board/quote/exec` **셋 다** 첫 줄이 `_villageNear`(260px,
  브리핑과 같은 게이트)다 — 이웃 마을 시세는 **걸어가서 보는 것**이고 그 비대칭이 콘텐츠다(§3.2).
  `test-trade ⑦` 이 3/3 을 구조로, `e2e-trade ①` 이 **클라 우회 시도**로 확인한다
  (vid 를 알고 직접 물어도 거절 · 읽기만 막고 쓰기를 여는 실수도 따로 확인).
* **게시판이 늘 낫다**: ×1.200(프리미엄 0.2) > ×0.818(스프레드 0.1). 둘 다 실제로 걸려 있음도 확인.
  (게시 시점 고정이라는 단서는 회부 B-7.)
* **잠긴 품목**: 열린 의뢰가 **보상으로 약속한 품목**은 마을이 안 판다 — 안 그러면 플레이어가 낸 뒤에
  "갚을 게 없다"를 듣게 된다(게스트보상 배치의 족보). 내부 필드(`_` 로 시작)도 재화가 아니다.
* **원자적**: ①견적 ②**재고를 먼저 잡는다** ③내 물건 차감 ④곳간 입고(정본 `playerVillageDeposit`)
  ⑤실패하면 되돌린다. 동시 경쟁은 ②가 관문 — 둘이 같은 마지막 재고를 못 가져간다(실측 확인).

### ⑥ 화면 — §8 프레임의 **둘째 칸**

패널을 새로 만들지 않았다. 3-v 가 세운 프레임에 **탭 하나**를 등록했다(그 3단계가 문서대로 돌아가는지의 실증):
`#sidebar` 아이콘 한 줄 + 제목표 한글 + `renderSide` 분기. **Shift+T.**
문법은 세 클릭 — 📤 낼 것 → 📥 받을 것 → **바꾼다**. 수치는 전부 서버가 준 것을 그대로 그린다.
열 때 **서버에 다시 물어본다**(클라가 시세를 캐시하면 그게 곧 낡은 시세다).

### 검증

```
  test-trade   38/0   (구조 판정: 왕복비 곱 = ((1−s)/(1+s))² · 소스에 computeShadowPrices 없음 · 상한 공용 함수)
  e2e-trade    40/0   (실클라: 반경 밖 거절 2겹 → 접근 → 세 클릭 → 인벤 반영 → **화면에서 값이 움직임**)
  러너 26종     실패 2 : e2e-trade(당시 구판 하네스 · 위 ⓒ) · **e2e-mtfoot 8/9(이 배치 무관 · 3-v 에 기록된 그것)**
```

⚠`e2e-trade` 는 **마을을 켜는 두 번째 실클라 하네스**다(`e2e-events` 와 같이 `VILLAGE_MAX=2`).
시간 모드: **데울 땐 흐르고(0.5초/일) 상호작용 땐 얼린다**(`__e2e_day_freeze`) —
⑤의 판정이 "**내 거래가** 시세를 움직였는가"라 날이 흐르면 원인이 섞인다.

**3시드 800일 기준선 — 한 자리도 안 움직였다**(`payableQty` 추출이 게시판 보상을 안 건드렸다):

```
1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,561   (기록값과 동일)
   7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,487   (기록값과 동일)
  42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,535   (기록값과 동일)
```

### 대리 지표 — `scripts/trade-metrics.js` (신규 · **계측기지 하네스가 아니다 · 러너에 넣지 마라**)

**① 곡식 환산 시세는 마을마다 크게 다르다**(111 게임일 데운 뒤 4마을):

```
   품목      농촌1     어촌2     임업2     광산4     최대/최소
   나무     1.059    647.5    1.002    95.27      ×646
   가죽     0.015    1.238   0.4185    7.686      ×512
    돌      75.07    5.755    262.1   0.8317      ×315
   광석   0.01264   0.0811   0.4501  0.07331      ×35.6
```

⇒ **걸어가서 사고팔 이유가 실제로 크다**(§3.2 정보 비대칭 = 콘텐츠).
⚠갓 시딩한 마을은 재고가 서로 똑같아 **8종이 ×1.0** 으로 나온다 — 그건 "안 다르다"가 아니라
**아직 살아 보지 않았다**는 뜻이다(1차 측정이 그렇게 나왔다). 데운 뒤 재라.

**② 왕복 손실 vs 스프레드** (광산4 · 고기↔생선 40개 왕복):

```
     s     측정 손실   스프레드만   가격충격 몫
  0.00      32.5%      0.0%      32.5%p
  0.05      32.5%     18.1%      14.4%p
  0.10 ★    35.0%     33.1%       1.9%p
  0.20      57.5%     55.6%       1.9%p
```

⇒ 단조 증가하고, 채택값 s=0.10 에서는 손실이 **거의 스프레드 그대로**다(가격충격 1.9%p).
⇒ **s=0 에서도 32.5% 손실** — 곡선 적분이 무한 차익을 봉쇄한다는 것의 실측.
⚠1차 측정은 거래 크기를 재고에 견주지 않아 `열매 1,761개 ↔ 가죽 11개`(재고의 10배)가 나왔고
손실이 81→73→75→78→77% 로 **널뛰었다** — 스프레드가 아니라 **내가 만든 가격 붕괴**를 재고 있었다.
계측기는 이제 **결론을 데이터에서 낸다**(단조성을 스스로 판정해 문구를 바꾼다).

**③ 물리 상한은 거의 항상 문다**: 마을×품목 34칸 중 **24칸(71%)이 "한 개도 못 내준다"**.
같은 상한을 쓰는 게시판 쪽 800일 실측이 그걸 더 크게 보여 준다 —
**못 갚아서 못 게시한 의뢰 44,875~54,953건** 대 실제 게시 1,487~1,561건.
⇒ 작은 마을 거래소가 대부분 비어 보이는 건 결함이 아니라 B-1 캐논이다(회부 B-5).


## 3-t. ★★2026-08-27 배치 — **플레이어 무게 모델 + 곡물 품목화 + 넘침 표시 + T/Y 제거**

> 재민 원문: *"모든 아이템은 좀보이드처럼 무게를 가져야 해."*
> 인벤 3층 확정: **개체**(하나하나 kg 가 다름) · **로트**(취득일이 다르면 다른 로트) · **무기한 벌크**(수량만).
> 앵커: `물품_무게_고증표_kg.md` — **주식 곡물 0.70 kg/단위**.

### ★§0 인벤토리 — 지시서 전제와 다른 게 셋이었다

이 규약이 네 배치 연속으로 값을 했다. 실측 결과:

1. **`specialty.js` 의 `weight` 는 정말 아무도 안 읽는다** — 전수 grep 으로 소비자 **0곳** 확인
   (검출된 두 건은 동명의 지역 파라미터였다). 문서의 "미사용"이 확인됐다.
   ⚠단 **`specialty.js` 는 econ 번들에 인라인된다** — 고치면 3사본 규약(번들 재생성 + 랩 2종 재인라인)이 걸린다.
2. **★낚시 v2 의 물고기는 개체가 아니었다** — `zone.js` 가 `n = round(gotKg / KG_PER_ITEM)` 로
   **kg 을 개수로 뭉개고** 있었다(2.4kg 한 마리 → 0.8kg 세 마리). 낚시 배치가 애써 낸 개체 무게가
   그 한 줄에서 사라지고 있었다. ⇒ 이번에 **한 마리는 한 마리**로 고쳤다.
3. **`FOOD_EFFECTS` 에 곡물이 없었다** — 그리고 **구운 생선도 없었다**(먹을 수 없는 조리식이었다).
   플레이어 인벤은 **스칼라 수량 맵**이고, 개체형은 도구·장비·요리 셋뿐이었다.
   영속은 `tools_json` 블롭이라 **스키마 무변경**으로 얹을 수 있었다(`oreLedger` 가 그 선례이고 이미 kg 장부다).

### 무엇이 생겼나

| 층 | 파일 | 요지 |
|---|---|---|
| 정본 | **`server/weights.js`**(신규) | kg 정본 — specialty 를 **읽고**, 없는 것(코어 19 + 플레이어 전용)만 스스로 정한다 |
| 정본 | **`server/carry.js`**(신규) | 합산·용량·과적 곡선·개체 kg 장부·합산 바닥 |
| 정본 | **`server/lots.js`**(신규) | 식품 로트(취득일) — 부패 곡선이 앉을 자리 |
| 카탈로그 | `server/specialty.js` | 197종 weight 를 고증표 값으로 (**89건 변경** · 나머지 107건은 이미 같음) |
| 배선 | `server/zone.js` | `moveMultOf` 정본 · 게이지에 carry · 저장/승계 · 낚시 개체화 · 버리기/줍기 kg · **T/Y 제거** |
| 배선 | `server/villages.js`·`events.js`·`trade.js` | `unitsOf` 환산(거래소=게시판 **같은 함수**) · **넘침 판정** |
| 화면 | `public/*` | HUD kg · 무들 "무거움" · 상태 패널 짐 칸 · 인벤 kg · 거래소 초과 경고 · **넘침 딱지** |

### ① 무게 정본은 하나다

* specialty 재화(197종)의 kg 는 **`specialty.js` 가 정본**이고 `weights.js` 는 **읽기만** 한다.
  `weights.js` 가 스스로 정하는 건 specialty 에 **없는 것**뿐이다 — 코어 19종(food·fish·meat·hide·ore…)과
  플레이어 전용(기둥·서까래·이엉·씨앗…). ⇒ 겹치는 값이 두 곳에 있는 일이 **구조적으로 없다**.
* `test-weight ①` 이 그걸 구조로 확인한다: 카탈로그 242종 전수 kg>0 · specialty 값과 **한 자리도 안 다름** ·
  **소스에 옮겨 적은 줄이 없음**. (실제로 `plank` 한 줄이 걸려서 지웠다 — 하네스가 자기 일을 했다.)
* **클라는 표를 안 갖는다** — welcome 이 `itemWeights` 카탈로그를 실어 보낸다.

### ② 과적 — 하드 컷 없이, 연속으로

```
   r(=소지/용량)   이속     피로     무들
      1.00       ×1.000   ×1.00     —
      1.15       ×0.950   ×1.18     —      ← 1단계 경계(곡선에서 **유도** · 이속 −5% 체감점)
      1.50       ×0.800   ×1.60     1
      2.00       ×0.600   ×2.20     2
      3.00       ×0.400   ×3.40     3      ← 과적 바닥. 그 위로는 **피로만** 는다
```

채택값: `CARRY_CAP_KG` **25** · 과적 바닥 **0.4**(신체 0.6보다 깊다 — 과적은 **선택**이라) ·
**신체×과적 합산 바닥 0.35**(곱 폭주 방지). 걸음 배율의 정본은 **`moveMultOf()` 하나**이고
서버 이동과 클라 예측이 그 수를 똑같이 쓴다 — `e2e-weight` 가 **위치 델타로** 확인한다
(실측: 가벼움 38px vs 과적 32px = **−17%**, 서버 배율 **−17%** — 딱 맞는다).

### ③ 인벤 3층

* **개체** — `p.kgLedger[item] = [kg, …]`(FIFO). `oreLedger` 와 같은 문법이다. 낚시가 첫 소비자다.
* **로트** — `p.lots[item] = [{d: 취득일, n: 수량}]`. 같은 날은 합치고 **다른 날은 안 섞는다**.
  신선도는 **조회 시 (오늘−취득일)** 로 유도한다(타이머 없음 · 틱 비용 0).
  ★불변식 **`inventory[item] === floor(Σ로트)`** — 수량 정본은 여전히 인벤이고 로트는 **나이 분포**를 적는다.
  로트를 안 거친 취득 경로가 있어도 `reconcile` 이 차이를 메워(오늘 것으로) **수량이 틀리는 일이 구조적으로 없다**.
  초당 1회 쓸개가 돌아 그 근사를 사실로 만든다.
* **무기한 벌크** — 그대로. 돌·장작엔 나이가 뜻이 없다.
* **부분 소비** — `doEat(p, item, 0.25)`. 회복량·무게가 **정확히 비례**한다. 소수 몫은 인벤 정수에 안 올라간다
  (`oreCarry` 규약 — "0.75 남은 곡식은 아직 못 판다").

### ④ 곡물 품목화 — 한 줄이 다 했다

`PV_DEPOSIT_MAP` 에 `food: 'food'` 한 줄. 그러면 `events.buildDeliverable` 이 그 표에서 파생되므로
**게시판 의뢰·보상·거래소가 특별 취급 코드 없이** 곡물을 다룬다(사본을 안 만드는 설계의 값).
여태 곡식은 "표시 기준(numeraire)인데 정작 표에 없는" 재화였다(거래소 배치 회부 B-2 해소).
먹기: **생곡 7 · 익힌 곡식 34** — 그 격차가 화덕 수요의 실체다. `fish_cooked`(32)도 이번에 먹을 수 있게 했다.

### ⑤ 개체 환산 — 큰 물고기가 실제로 비싸다

2kg 물고기 = 표준 0.9kg 짜리 **2.22단위**. 이 환산을 **실물 이동의 정본 하나**(`playerVillageDeposit`)에 얹어
**거래소와 게시판이 같은 환산**을 쓴다(둘이 다르면 그게 보이지 않는 손이다).
실측(`test-weight ⑧`): 2.2배 무거운 개체가 **4개 → 9개**(×2.25 vs 무게비 ×2.20).

### ⑥ 넘침 딱지 — 상수를 옮겨 적지 않는다

가격 바닥에 붙은 품목("아무리 팔아도 값이 안 내려간다")에 딱지를 붙인다.
★판정은 `PRICE_ADJ_MIN` 을 옮겨 적는 대신 **정본에게 물어본다** — 그 품목 재고를 잠깐 크게 늘려 보고
값이 안 움직이면 바닥이다(`planSliced` 와 같은 문법 · `finally` 복원).
실측: 농촌1 11품목 중 **4품목이 넘침**(철·고기·열매·생선). 하네스가 세 번 속았던 그 자리에서
플레이어도 속지 않게 하는 것이 이 딱지의 존재 이유다.

### ⑦ T/Y 제거

서버 핸들러·함수·클라 키·액션·버튼·README 까지 전부. `test-weight ⑥` 이 소스에 안 남았는지 확인하고,
`e2e-weight ⑦` 이 **실제로 눌러 보고** 인벤이 안 움직이는지 본다. 대체 설계는 `회부_무게_다음층.md` P항.

### ★★실측 발견 — 새 플레이어가 태어나자마자 과적이다

```
  시작 지급: 톱·망치·도끼(3.6kg) + 판자 10장(30.0kg) = 33.6kg  vs  용량 25kg → 134% · ×0.87
```

**손대지 않았다**(용량은 지시서가 "시작점"으로 준 값이고, 셋 중 무엇을 고를지는 실기 판정이다).
대신 `e2e-weight` 가 이 사실을 **판정으로 박아 뒀다** — 잊히지 않게. 선택지는 회부 B-1.

### 검증

```
  test-weight   57/0   (전수 스윕 · 합산 · 곡선 연속/바닥/클램프 · 과적 줍기 · 곡물 · T/Y 부재 · 로트 · 개체 환산)
  e2e-weight    32/0   (실클라: HUD kg · 무들 · **위치 델타로 감속 실측** · 패널 · T/Y 무반응 · 넘침 딱지)
  test-trade    38/0 · test-fishing 49/0 (개체화·환산이 기존 계약을 안 깼다)
  econ 번들     --check 8/8 재생성(specialty 89건 변경 · econ 소비자 0곳이라 동작 불변)
```

### ★★3시드 800일 기준선 — **econ 은 비트 동일, 게시판은 의도대로 움직였다**

```
  시드   인구      소멸    무기Q    게시(기록→이번)    못갚아미게시(기록→이번)
  1020   6,280 ✓  0/51 ✓  821 ✓   1,561 → **1,647**   46,303 → **11,194**
     7   5,993 ✓  0/51 ✓  755 ✓   1,487 → **1,583**   54,953 → **10,787**
    42   6,072 ✓  0/51 ✓  784 ✓   1,535 → **1,679**   44,875 → **10,489**
```

**econ 지표(인구·소멸·무기Q)는 한 자리도 안 움직였다** — `specialty.weight` 89건 교체가 econ 에
스미지 않았다는 실증이다(지시서가 말한 "최종 심판"). 게시판 수치만 크게 달라졌고, 원인을 **추정하지 않고 갈랐다**:

★**단일 손잡이 A/B**(같은 기계·같은 시드 1020 · `food: 'food'` 한 줄만 빼고 재실행):
```
  게시 1,561 · 철회 1,498 · 축소 367 · 못갚아미게시 46,303   ← 기록값과 **정확히 일치**
```
⇒ 게시판 변화는 **전부 곡물 품목화 한 줄** 때문이다. 지시서가 요구한 "특별 취급 코드 없이 자연 편입"이
그대로 일어난 것이다 — 곡식은 마을에서 **가장 흔한 재화**라, 보상으로 갚을 수 있는 의뢰가 급증한다
(못 갚아 미게시 **−76%**, 요청 축소 367 → 13).
⚠**이건 경제가 아니라 게시판의 변화다**(econ 상태는 비트 동일). 다만 **플레이어가 체감할 변화는 크다** —
의뢰가 훨씬 자주 걸리고, 덜 잘리고, 훨씬 자주 보상이 나간다. **새 기준선은 1,647/1,583/1,679 이다.**

⚠**`test-guest-rejoin` 이 이 배치 때문에 한 번 빨개졌다** — 제품이 아니라 **하네스**였다.
그 검사는 "미저장 드리프트 > 200px" 를 전제로 4초 걸었는데, 무게가 들어오며 검사 플레이어가
**216% 과적(×0.58)** 이 되어 250px → **145px** 로 줄었다. ⇒ **시간이 아니라 거리로** 멈추게 고쳤다
(앞으로 걸음 배율이 또 바뀌어도 안 깨진다). ★족보에 추가: **하네스가 걸음 속도를 상수로 박으면
이동 배율을 바꾸는 배치마다 깨진다 — 목표는 시간이 아니라 거리로 적어라.**


## 4. 작업 배치 — 재민 결정 대기 항목이 많다

**★먼저 재민의 회부 답을 확인해라.** 답이 있으면 그것부터. 없으면 아래를 순서대로:

### A. 회부와 무관하게 지금 해도 되는 것

1. **`era.canTame` 배선** — 지금 `breeding:true` 13종이 시대와 무관하게 먹이 3번이면 길들여진다
   (`era.canTame` 호출부 0곳). `tryFeed` 에 한 줄. era.js "송국리기엔 기마 없음" 의도가 코드로는
   안 지켜지고 있다. 하네스도 같이(`test-tame.js` 소품).
2. **노 조업 시간** — 지금은 클릭 1회 = 즉시 1회 제련이다. 움집처럼 진척도(시간)를 두는 게
   계약상 자연스러운지 검토. `설계_플레이어_NPC_집건설_해석안.md` 의 진척 공식 재사용.
3. **숯가마 조업 배치** — 지금 1회 3통나무. 여러 번 클릭이 지루하면 "가득 채우기" 한 번에.
4. **`iron` 라벨 정합** — `specialty.RESOURCES.iron.ko = '철광석'` 인데 econ 의 `iron` 은 **제련된 금속**이다.
   NPC 시장 UI 가 금속을 "철광석"이라 부른다. 바꾸면 econ 표시만 바뀌는지 확인 후 수정.
5. **플레이어 야금 사슬 실플레이 검증** — 로컬 풀스택(Playwright 실클라)으로
   채광→선광→숯가마→노→단조까지 **사람이 하듯** 한 번 통과시켜라. 서버 함수 E2E 는 됐지만
   클라 UI(건축 메뉴·2×2 고스트·클릭 판정·인벤 표시)는 아직 실화면 검증이 없다.

### B. 회부 답이 오면

6. 광산촌 특화 슬롯(`회부_구리부존과_원석적체` ①) — 시딩에 슬롯 예약
7. 구리 파생 투입수요(②) — **채택하면 청동 희소성 설계가 바뀐다.** 3시드 A/B 필수
8. 원석 교역 금지(③ 나) — 추천안. 상태 증가 0
9. DB 리셋/새 존(`회부_시딩변경과_DB리셋`)
10. 배합↔교역 통합 · 철검 위세축 · 말 사역

### C. 계측·위생 (상시)

11. 랩 2종 재인라인 + 스모크 + 디바이스 동기 — 엔진 건드리면 매번
12. `보고_*.md` 종합 보고 작성·푸시

## 5. 회부만 할 것 (구현 금지)

강제소개/이주 계열 전부 · 배합↔교역 통합 구현 · 아이템 위세축 구현 · **DB 리셋/존 id 변경** ·
~~NPC 철기 산업 콘텐츠(주요 광맥에 철 넣기·새 마을)~~ **[2026-08-03c 재민 확정 — 길이 닫혔다.
철기는 플레이어 마을 건립으로 자연해소. 설계_플레이어_마을건립.md 참조]** ·
**플레이어 마을 건립 구현**(설계안만 존재 — 결정표가 채워지기 전엔 구현 금지) ·
산 부수기 시스템 · 자잘 광맥 미니맵 기록 ·
**시대 SCHEDULE 실제 설정**.

★배치별 회부 문서 — **구현 전에 먼저 읽어라**:
`회부_사건레이어_다음층.md` · `회부_MSY상한_사장.md` · `회부_낚시_다음층.md` ·
`회부_신체UI_다음층.md` · `회부_거래소_다음층.md` · **`회부_무게_다음층.md`**(2026-08-27 신규).

무게 회부(재민 명시) — **NPC 캐러밴 무게 모델(M · 랩 A/B 선행)** · 지게(용량 확장구) ·
부패 곡선·상함·보존 가공 · **플레이어 간 거래 제안·수락(P — T/Y 를 대신할 것)** ·
곳간 인출·마을 소속 · 아이템 상세 표(§8.1 전체) · MSY 상한 A/B · 낚시·신체·거래소 손잡이.
무게 회부(실측으로 새로 나옴) — **B-1 시작 지급(33.6kg)이 기본 용량(25kg)을 넘는다** ·
B-2 낚시 한나절 115kg(용량의 462%) · **B-3 표에 없어 내가 유도한 kg 7종(확인 바람)** ·
B-4 거의 모든 "한 짐"이 용량을 조금씩 넘는다 · B-5 로트 상한에서 한 번은 나이를 뭉갠다 ·
B-6 인벤 정수 vs 소수 몫 표시 · B-7 개체 환산을 게시판에도 걸었다.

거래소 회부(재민 명시) — 곳간 인출 · 마을 소속/멤버십 · 거래소 **실체 건물** · 흥정/평판 할인 ·
원거리 위탁·캐러밴 동승 · 시세 기록/차트 UI · MSY 상한 A/B · 낚시·신체 손잡이 튜닝(재민 실기 대기).
거래소 회부(실측으로 새로 나옴) — **B-1 장마당 고증**(기능은 재분배인데 이름만 근대적) ·
**B-2 곡식이 교환 품목이 아니다**(기준 품목인데 손에 못 쥔다) · **B-3 가격 바닥에 붙은 품목** ·
B-4 하루 캐시 vs 지금 시세 비대칭 · B-5 작은 마을에선 거래소가 사실상 빈다 ·
**B-6 T/Y `trade_offer` 가 경제를 통째로 우회한다**(나무1↔돌1 · 상대 동의 없음 · 이전부터 있던 것) ·
B-7 게시판 부등식은 게시 시점 기준.

## 6. 완료 기준

항목마다: 실측 수치(전/후) 또는 하네스 PASS → 커밋(사용자 지시 인용 포함) → **즉시 푸시**.
마지막에 `보고_*.md` 종합 보고 작성·푸시하고, 랩 2종 재인라인·스모크·디바이스 동기까지 확인.

---

## 3-s. ★★2026-08-28 배치 — **빈손 시작: 시작 지급 전면 제거**

> 재민 원문: *"지급 아이템은 없어야 할 거 같은데"* · *"돌멩이를 줍고 나뭇가지를 줍고"*
> 채집 방식 확정: **낙하물 스캐터 금지 — 이미 렌더된 자연물이 채집원이다.**
> 고갈 = **개체별 lazy 번영도**(낚시 자리·광맥과 **같은 문법**) · 리필은 티어에 비례(시작 재료는 분 단위).
> 근거 캐논: 온보딩 §9 — "나루터에서 온 이방인"은 **빈손으로 배고픈 채** 도착한다. 공짜 세례 금지.

### ★§0 인벤토리 — 지시서 전제와 다른 게 둘이었다

1. **지급은 "시작"이 아니라 매 로그인 수도꼭지였다.** `ensureStart(saw/hammer/axe)` 와
   `if (!inventory.plank) inventory.plank = 10` 은 **접속할 때마다** 돌았다 — 즉 도구를 다 쓰고
   다시 들어오면 새 도구가 나왔다. "시작 지급"이 아니라 **무한 공급**이었다. 둘 다 철거.
   (덤: 그 지급 33.6kg 은 용량 25kg 을 **134%** 로 넘겨서, 새 사람은 **태어나자마자 과적**이었다.)
2. **`stoneMult` 는 죽은 신호다.** 1차 실장은 `getStoneMultiplier` 로 자갈 자리를 갈랐는데
   풀이 **한 번도** 안 나왔다. 실측하니 이 세계의 `stoneMult` 는 **어디서나 1.00 고정**
   (3,000표본 min=max=1.00) — 판별에 못 쓴다. ⇒ 숲 배수 + 바위/물 링으로 갈아 끼웠다.
   ★족보 (62): **지형 술어를 쓰기 전에 분포부터 재라.** 죽은 신호를 읽는 규칙은 조용히 한쪽으로만 답한다.

### 지급 제거 내역

| 없앤 것 | 있던 자리 | 성질 |
|---|---|---|
| 톱·망치·도끼 3종 | `ensureStart()` — 접속 때마다 | 무한 공급 수도꼭지 |
| 판자 10장(30.0kg) | `if (!inventory.plank) inventory.plank = 10` | 무한 공급 수도꼭지 |
| T/Y 치트 키 | (앞 배치에서 제거) | 경제 우회 |

⇒ 새 사람의 인벤은 이제 **`{wood:0, stone:0}` — 합계 0**이고 HUD 는 **🎒 0.0/25kg** 다(실클라 실증).
E2E 픽스처는 전부 `E2E_GIVE=1` 게이트 뒤의 `__e2e_give` 로 옮겼다(기본 부팅에선 **분기 자체가 없다**).

### 맨손 동사 표 — **도구는 문이 아니라 능률이다**

| 동사 | 맨손 | 조잡한 석기 | 정품 | 비고 |
|---|---|---|---|---|
| 나무 채집 | ✅ ×1 | ×2 (`crude_axe`) | ×3 (`axe`) | 막히지 않는다 |
| 돌 채집 | ✅ ×1 | ×2 (`crude_pick`) | ×3 (`pickaxe`) | |
| 전투 | ✅ ×1 | ×1.5 (`crude_blade`) | ×2 (`sword`) | |
| 도축·손질 | ✅ | — | — | **도구가 아예 관여하지 않는다**(`butcherCorpse` — 거리 판정뿐) |
| 광맥 채굴 | ❌ | ❌ | 곡괭이 필요 | **원래부터 도구 게이트**(이 배치가 만든 게 아니다) |
| 지형 채집(잔가지·자갈·풀) | ✅ | — | — | 도구가 관여하지 않는다 |
| 덤불(열매·풀·잔가지) | ✅ | ✅ | ✅ | 파괴형 |
| 낚시 | ✅ | — | — | 도구 요구 없음(회부 A-4) |
| 조잡한 석기 제작 | ✅ **시설 없이** | — | — | §8.5 "맨손 = 소목록" |

★평생 산출: **맨손 ∞ · 조잡 50 · 정품 300**(정품이 조잡의 **×6.0**) — 자급자족 병 방지선.
손잡이는 두 개뿐이다: `CRUDE_EFF_FRAC=0.5` · `CRUDE_DURA_FRAC=0.25`.
**두 번째 마법 상수 표를 만들지 않았다** — 조잡본 효과·내구는 정품 값에서 *유도*한다.

### 조잡한 석기 3종 (맨손 제작)

```
  crude_axe   자갈2 · 잔가지1 · 풀2   내구 25(정품 100)   나무 ×2(정품 ×3)
  crude_pick  자갈3 · 잔가지1 · 풀2   내구 25(정품 100)   돌   ×2(정품 ×3)
  crude_blade 자갈2 · 잔가지1 · 풀1   내구 20(정품  80)   공격 ×1.5(정품 ×2)
```
다 쓰면 **부서져 사라진다** — 그게 이 층의 리듬이다(재제작 루프를 하네스가 실증).

### 채집 — 이미 렌더된 자연물에 **판정만** 얹었다

`server/forage.js`(신규) 가 정본이다. 낙하물을 흩뿌리지 않았다.

```
  갈대 군락(물 바로 옆)         E → 풀 fiber      ※갈증 <95% 면 물 마시기가 먼저
  숲 바닥(forestMult > 1.5)     E → 잔가지 twig
  물가 바위밭·자갈 지형(링 판정) E → 자갈 pebble
  덤불(개체 · 파괴형)            E → 열매2 · 풀1 · 잔가지1   ★"덤불 E = 잔가지"를 여기서 지켰다
  들판 한복판                   E → "여긴 주울 게 없다"      ★어디서 주울까가 판단이 되게
```
고갈은 **개체별 lazy 번영도** — 안 건드린 자리는 **저장하지 않는다**(없으면 만땅), 시간은 조회할 때
닫힌 해로 한 번에 적분한다(틱 비용 0). `mined_cells`·`fish_cells` 와 **같은 문법을 세 번째로** 쓴 것이다.
반독점: 전역 공유 풀 없음 · 개체 용량 4 · 만땅까지 3분 · 잔가지는 덤불+숲 바닥 **두 소스**.

### ★★빈손 → 첫 도구 실측 (`scripts/emptystart-metrics.js`)

**스폰 광장(농촌22 광장) 기준 — 새 사람이 실제로 눈 뜨는 자리:**
```
  반경      잔가지  자갈   풀 (지형 칸) │ 덤불  나무  바위 (개체)
  300px        0     0     0          │   0     0     0     ← 완전한 공백
  600px        0   141    21          │   0     4     0
  1200px       0   363    71          │   0    14     0
  2400px       0  1428   287          │   8    49     2     ← 잔가지는 여기서 처음
```
⇒ 자갈·풀은 600px(≈9초)에 있지만 **잔가지는 2,400px 까지 나가야 한다**(≈37초 편도, 자리를 아는 사람 기준).
조잡한 돌도끼는 잔가지 1이 필수 ⇒ **첫 도구까지 최소 왕복 ~75초 + 훑기 4.5초**.
손동작 자체는 `5줌 × 900ms = 4.5초`뿐이다 — **비용의 거의 전부가 "어디로 갈까"** 다.

**마을 51곳 반경 300px(274칸) 채집 밀도:**
```
  채집 사막(0자리)   20/51 · 한 종류 이하 33/51 · 중앙값 82자리 · 최대 274(전 칸)
  세 재료가 1,600px 안에 다 있는 마을  2/51
```
⇒ 반독점 규약("옆 자리가 있다")은 **자리가 있는 마을에선 넉넉히 성립**한다(82자리 · 3분 리필).
그러나 **없는 마을에선 자리 자체가 0**이다. 원인은 규칙이 지역 단위 술어라 마을이 숲 **안** 아니면 **밖**이기 때문.
⚠**이건 값이 아니라 설계다 — 손대지 않고 `회부_빈손_다음층.md` B-1·B-2 로 회부.**

### kg 7종 + 조잡 3종 고증표 등재

`물품_무게_고증표_kg.md` **§11**(디바이스 파일에 추가 — 레포엔 untracked 유지, 재민 `git pull` 안 깨지게):
표에 없어서 유도한 값 7종(풀·이엉·기둥·서까래·씨앗·물병·건축부재)에 **한 줄 근거 + 확신 등급**,
그리고 §11-b 에 조잡한 석기 3종(`crude_axe 1.40 · crude_pick 1.50 · crude_blade 1.10` — **전부 "추정"**).
★조잡본이 정품(`tool` 1.20)보다 **무겁다**: 다듬지 않아 균형이 나쁘고 날 몫이 두껍다. ⇒ 재민 확인 대상.

### 검증

```
  test-emptystart  41/0  (지급 0 · 로그인 반복 무지급 · 맨손 동사 전수 · 줍기→자작→장착→파손→재제작 · 자작vs구매)
  e2e-emptystart   18/0  (실클라: 빈손 입장 · HUD 0/25kg · 세 자리에서 실채집 · 제작 탭 소목록 ·
                          맨손 자작 · 장착 · 마을 장인 정품 구매 · 정품 내구 245 vs 조잡 25)
  러너 등록        run-regress.sh 목록에 두 하네스 추가 · --selftest 통과
  전체 회귀        29/30 통과 · 실패 1 = `e2e-mtfoot` 8/9 (**산 아크 소관** — 인계 1847줄의 기존 항목)
```

⚠**`e2e-trade` 가 러너에서 두 번 빨개졌다 — 둘 다 하네스였다**(제품은 옳았다).
① **살 수 없는 쌍을 골랐다**: 받을 품목을 "마을이 내줄 수 있는 양이 가장 큰 것"으로만 골랐더니
생선(0.004728)×12 = 0.057 곡식 → **곡식(1) 0개**. 견적은 정상으로 서고 버튼만 죽는다.
시세는 판마다 다르므로(부팅 지연만큼 게임일이 흐른다) **어떤 판은 통과하고 어떤 판은 실패**했다.
⇒ **살 수 있는 쌍인지 먼저 확인**한다(족보 (56) 재적용).
② **대조가 독립성을 가정했다**: "무관 품목이 전부 움직였으면 날이 흐른 것"이라고 읽었는데,
그림자 가격은 **바구니 전체를 한꺼번에** 푸는 값이라 **큰 거래 하나면 무관 품목도 같이 움직인다**
(`push` 는 일부러 수량 100000 을 6번 민다). 약초가 뽑히면 통과, 익힌 음식이 뽑히면 실패였다.
⇒ 대조를 **널 거래**로 바꿨다 — 거래 없이 같은 시간 기다렸다가 다시 읽어 **0 이동**을 요구한다.
★족보 (66): **대조는 "덜 움직였다"가 아니라 "안 했으면 안 움직인다"로 세워라.**
"덜 움직였다"는 결합 계산(그림자 가격·최적화 해)에서는 아무것도 증명하지 않는다.

### ★3시드 800일 기준선 — **한 자리도 안 움직였다**

```
  시드   인구      소멸    무기Q   게시    못갚아미게시
  1020   6,280 ✓  0/51 ✓  821 ✓  1,647 ✓  11,194 ✓
     7   5,993 ✓  0/51 ✓  755 ✓  1,583 ✓  10,787 ✓
    42   6,072 ✓  0/51 ✓  784 ✓  1,679 ✓  10,489 ✓
```
당연하다 — 이 배치는 **플레이어 층 전용**이고 헤드리스 랩엔 플레이어가 없다. econ 도 안 고쳤고
`specialty.js` 도 안 건드렸다(번들 재생성 불필요). `CARGO_PER_TRIP=100` flat 그대로.

### 하네스가 세 번 먼저 틀렸다 (족보 (63)~(65))

* **(63)** `e2e-emptystart ②` — 숲 바닥 좌표엔 **나무 개체**가 서 있어서 `tryGather` 가 그걸 먼저 집는다.
  "그 자리가 채집원인 것"과 "거기서 채집이 열리는 것"은 **다른 명제**다. ⇒ 종류마다 **자리 6곳을 밟아** 판정.
* **(64)** 도착 판정에서 **좌표계를 섞었다** — `teleport_debug` 는 존 로컬, `__getMyAbs()` 는 월드 절대.
  멀쩡한 워프가 전부 "미도달"로 찍혔다. 오프셋은 **정본**(`zone-config.js`)에서 읽는다.
* **(65)** `e2e-emptystart ⑤` — **공지로 셌다.** `__notices` 는 40칸 링버퍼라 채집 공지만으로 포화돼
  `slice(n0)` 가 늘 비었고, **성공한 구매가 실패로 찍혔다.** ⇒ 상태(장비 수)로 센다.
* 그리고 제품 쪽 하나: **갈대가 실클라에서 한 번도 안 잘렸다.** 물가 E 의 갈래 조건이
  `thirst >= THIRST_MAX` 였는데 갈증은 매 틱 줄어서 **정확히 100인 순간이 거의 없다**.
  ⇒ `>= THIRST_MAX * 0.95`. 죽은 갈래를 살린 것이지 값 튜닝이 아니다(회부 B-6 로 보고).

### 회부 (구현 금지)

`회부_빈손_다음층.md` — A: 자연물 시각 변화 · 수색 모드 · 마을 공유지 반경 · 맨손 낚시/조잡한 낚싯대 ·
시설 제작창(★정품 자작 경로가 아직 없다) · 온보딩 대본 · (지게·부패·플레이어 간 거래·NPC 캐러밴 M·MSY A/B 는 기존 회부 유지) ·
B: **스폰 광장 채집 공백**(B-1) · **지형 술어의 전부-아니면-전무**(B-2) · 덤불 전리품 표 두 벌(B-3) ·
조잡 석기 kg 확인(B-4) · 정품 자작 부재로 인한 재제작 유인(B-5) · 갈대 문턱 95%(B-6).

---

## 3-r. ★★2026-08-29 소배치 — **채집 사막 수리: 배산임수 정합 감사**

> 재민 판정: 빈손 배치가 실측으로 잡은 "스폰 광장 300px 안에 주울 게 없고 51마을 중 20곳이 채집 사막"은
> **의도된 마찰이 아니라 배산임수 캐논 위반**이다 — 마을은 원래 물가와 숲 옆에 선다
> (`설계_마을_배산임수_레이아웃.md`). 처방은 **스캐터가 아니라(기각) 마을 인근 자연물 배치 보정**이다.

### 감사 기준 (채택값 · env 손잡이)

```
  마을 중심에서 도보 15초 = MOVE_SPEED 64px/s × 15s = 960px = 30셀
  그 안에 시작 재료 3종(잔가지·자갈·풀) 각각 소스 ≥ 2개 + 식수 1개
  손잡이: AUDIT_WALK_SEC(15) · AUDIT_NEED_EACH(2)
```
⚠**직선거리 대리다.** 실클라 실측은 그 두 배쯤 걸린다(아래 · 회부 B-3).
소스는 하네스가 다시 정의하지 않는다 — 지형은 `forage.sourceAt`, 개체는 `chunk.generateChunkResources`,
개체가 무엇을 주는지는 `zone.lootOfResource` **정본에게 물어본다**.

### ★§0 인벤토리 — 지시서 전제와 다른 게 셋이었다

1. **잔가지가 없던 게 아니라, 서 있는 나무를 안 보고 있었다.** 임업3 은 반경 안에 **나무가 564그루**인데
   잔가지 소스가 0이었다 — 채집 판정이 **지형 술어**(`forestMult > 1.5`)만 봤기 때문이다.
   ⇒ 지시서 §2 마지막 줄("배치가 아니라 판정 목록을 넓히는 게 답일 수 있다 — 옮기기 전에 확인")이 맞았다.
2. **자연물 전리품 표가 두 벌이었다** — 플레이어 채집과 NPC 채집이 각자 표를 들고 있었고,
   빈손 배치가 덤불에 잔가지를 넣을 때 **한쪽만** 고쳐 놨었다(회부 B-3 로 남겨 뒀던 것). ⇒ 이번에 합쳤다.
3. **웅덩이(`water_pool`)는 이미 마실 수 있는 개체였다** — `tryGather` 가 hp 를 안 깎고 갈증만 채운다.
   감사 1차본이 그걸 안 세어서 식수 부족을 5곳 부풀려 보고했다(계측기 결함 · 같은 계측기로 다시 쟀다).

### 수리 ① 판정 목록 — **이미 렌더된 것이 제 몫을 내게**

`lootOfResource()` 정본 하나로 합치고, 감사가 시킨 두 줄을 넣었다:
```
  나무 tree → 목재 + **삭정이 1**   (가지를 치면 잔가지가 딸려 나온다)
  바위 rock → 석재 + **자갈 2**     (돌을 깨면 잔돌이 나온다)
```
★새 개체도 새 스프라이트도 새 지형장도 **0**. 수량은 **부산물 급**이다(주 경로는 덤불·지형).
이 한 층만으로 **합격 15/51 → 26/51**, 잔가지 부족은 **14곳 → 0곳**이 됐다.

### 수리 ② 배치 — 마을 어귀 **채집 군락** 48개

`scripts/plan-village-forage.js`(plan-* 계보) 가 계산해 `hanbando-terrain.json` 의 `groves` 에 적고,
`chunk.generateChunkResources` 가 개체로 실체화한다. **데이터 손편집 0 · 결정론 · 재실행하면 같은 결과.**
```
  둠벙 water_pool 20 · 자갈밭 rock 17 · 덤불 berry_bush 11   = 군락 48 · 개체 104
  링 700~840px(골목 400px·회관 여유 528px 밖 · 군락 반경 110 더해도 950 < 960)
  군락 모양 = 극좌표 r·√u — 중심 촘촘·가장자리 성김(**균일 간격 금지**)
  지형 정합 = 자갈은 물가·들 · 둠벙은 물이 먼 곳에만 · 물/바위 위 금지
  회귀 장면 보호구 2곳 반경 900px — e2e-nature·e2e-terrain 의 반례 자리를 안 건드린다
```
⚠**예외 하나를 보고한다**: 임업 6곳 + 농촌1 은 반경 960px 이 **전부 숲**이라 "자갈은 들에만"을 지킬
자리가 없다. 그 일곱에만 **숲이 가장 성긴 자리**에 `너덜겅`으로 놓았다(`forestException: true`). 회부 B-1.

### ★★감사 전후 짝 (같은 계측기)

```
  합격        전 15/51  →  판정 목록 확장 뒤 26/51  →  군락 배치 뒤 **51/51**
  부족한 재료  잔가지 14 · 자갈 24 · 풀 11 · 식수 20*  →  0 · 0 · 0 · 0
      (*식수는 웅덩이를 세는 같은 계측기로 다시 잰 값. 1차본은 25곳이라 했다.)

  첫 도끼 3재료를 다 모으려면 나가야 하는 반경(3종 최근접의 최댓값)
      전  중앙값 1,133px(17.7초) · 상위25% 3,104px(48.5초) · 최악 측정 한계 밖(>60초)
      후  중앙값   444px( 6.9초) · 상위25%   774px(12.1초) · 최악 890px(13.9초)
      도보 15초 안에 3종이 다 있는 마을: **24/51 → 51/51**
```
스폰 광장(농촌22)은 잔가지 최근접 **1,531px → 319px** — 빈손 배치가 보고한 "첫 도끼까지 2,400px"이
**319px** 로 줄었다.

### 검증

```
  audit-village-forage       51/51 합격 (전 15/51)
  e2e-forage-village         12/0   옛 사막 농촌10 에서 **걸어서** 세 재료 → 조잡한 돌도끼 → 둠벙에서 물
  전체 회귀                  **30/31 통과** (실패 1 = `e2e-mtfoot` 8/9 · 산 아크 소관, 인계 1847줄의 기존 항목)
                             ★`e2e-trade`·`e2e-village`·`e2e-waterperf` 는 이번엔 러너에서도 전원 통과
  3시드 800일 기준선          한 자리도 안 움직임 (아래)
```
```
  1020  인구 6,280 ✓ 소멸 0/51 ✓ 무기Q 821 ✓ 게시 1,647 ✓ 못갚아미게시 11,194 ✓
     7       5,993 ✓      0/51 ✓      755 ✓      1,583 ✓            10,787 ✓
    42       6,072 ✓      0/51 ✓      784 ✓      1,679 ✓            10,489 ✓
```
당연하다 — 군락은 **개체**이고 econ 은 마을 시뮬이다. 지형 문서에서도 `groves` **키 하나만** 늘었고
기존 층(rivers·lakes·ridges·forests·ores·villages·valleys)은 **바이트 단위로 그대로**다(대조 확인).

### 실클라 도보 실측 (`e2e-forage-village` · 농촌10)

```
  잔가지 141px → 2.1초 · 자갈 402px → 6.6초 · 풀 809px → 7.9초 · 둠벙 821px → 20.0초
  평균 걸음 52px/s = 정본 64px/s 의 82% (남은 18% = 8방향 양자화 + 경로 우회)
```
⚠**이 표는 2026-08-29 제작창 배치 §0 에서 고쳐 적은 값이다.** 처음엔 "30px/s = 47%" 로 적었는데
그건 **계측기의 사실**이었다 — 하네스가 손으로 쏜 입력이 **클라의 33ms 정지 입력과 싸웠다**.
키보드로 걸으면 탁 트인 땅에서 **65.1px/s = 정본의 102%** 다(탐침 실측).
⇒ **감사 기준 960px = 도보 15초는 그대로 맞다**(회부 B-3 해결).

### 하네스가 세 번 먼저 틀렸다 (족보 (67)~(69))

* **(67)** `groves` 48개를 적고 재감사했는데 **한 곳도 안 변했다**. `terrain._getZoneTerrain` 이
  손그림 문서에서 **알려진 키만 골라 복사**하기 때문 — 화이트리스트에 한 줄을 안 넣으면
  **데이터는 있고 세계엔 없다**. (그리고 클라 `public/terrain.js` 도 같은 거울이다.)
* **(68)** `e2e-forage-village` 가 `fiber 1/2` 로 멈췄다 — 덤불은 **한 번 캐면 사라지는데**
  같은 좌표로 다시 걸어갔다. ⇒ 종류마다 후보를 8곳까지 잡고 다음 자리로 옮겨 간다.
* **(69)** 하네스가 **세계의 절반 속도로 걸었다**(26px/s vs 64px/s). 입력 한 개마다
  `page.evaluate` 왕복을 해서 초당 ~17개밖에 못 보냈기 때문(넷코드는 입력 1개 = 1스텝).
  ⇒ 걸음 루프를 **페이지 안에서** 돌렸다(3.0/11.7/16.4초 — 위 실측은 고친 뒤 값이다).

### 장면 의존 하네스 — 전후 짝으로 귀속을 갈랐다

`e2e-nature`·`e2e-terrain` 이 셀 (1490,2477)·(965,1919) 를 반례로 박아 두고 있어 **보호구**로 피했다 →
둘 다 통과. `e2e-tilestate` 의 "속살 초록"이 95.3% → **87.2%** 로 내렸는데, 이 하네스는 뙈기를
**스스로 골라 쓴다**(고정 장면이 아니다) 그리고 문턱은 `> 22` 다 — 즉 군락이 시야에 하나 들어온
정도이고 판정 여유가 크다. **회귀가 아니다.**

### 회부 (구현 금지)

`회부_배산임수_다음층.md` — A: 이방인 환영 배지 자격 · **시설 제작창**(플레이어 장인이 NPC 장인과
같은 시세표에서 경쟁 — 접속면은 이미 있다) · 수색 모드 · 자연물 시각 변화 · 마을 공유지 반경.
B: **임업 6곳엔 들이 없다(자갈밭 숲 예외)** · **마을 25곳이 임수를 어기고 있다(둠벙은 증상 처치)** ·
15초 기준이 직선 대리라는 것 · 잔가지 하나에 나무를 벤다 · 군락과 농지 겹침 · **하네스가 지도 두 자리를 잠그고 있다**.

---

## 3-q. ★★2026-08-29 대배치 — **시설 제작창(§8.5 본편) + 정품 도구 자작 경로**

> 재민 확정: **"제작창 = 시설의 창"** — 화덕=요리 · 작업대=도구 · 노=제련. **전 레시피 대목록 금지.**
> 헌법 둘: 일관성(**제작은 마법 메뉴가 아니라 시설 앞의 물리 행위**) ·
> 상호의존(**자작이 구매를 죽이면 안 된다 — 전문화가 이겨야 한다**).

### ★§0 잔무 — B-3 "걸음이 정본의 47%"는 **계측기의 사실이었다**

배산임수 배치가 남긴 회부 B-3(기준의 단위를 px 로 둘지 초로 둘지)을 먼저 갈랐다.
세 갈래(ⓐ 지형·경로 ⓑ 계측기 ⓒ 신체·무게 오적용)를 탐침으로:

```
  ① 정본 배율(빈손 새 플레이어)  신체 1.0 · 과적 1.0 · 합산 1.0    ⇒ ⓒ 아님
     플레이어는 길 배속(_rdMul)에서 **명시적으로 제외**된다 — 지형 속도 배율 자체가 없다  ⇒ ⓐ 중 '배율' 없음
  ② **키보드로** 탁 트인 땅 12초: 66 64 66 64 66 64 66 64 66 64 64 66 px/s
     정상 구간 평균 **65.1px/s = 정본 64 의 102%** (rAF 47fps — 30Hz 고정 스텝에 충분)
  ③ 하네스가 쓰던 **수동 입력**: 30px/s → 어떤 판에선 **0px/s**
```
원인: 클라는 키가 안 눌린 동안 **33ms마다 정지 입력**을 보낸다
(`if (!moving && now-lastInputSentAt>33) sendStepInput(seq,0,0)`). 손으로 쏜 입력이 그것과 **번갈아** 들어가 상쇄됐다.
⇒ **ⓑ 계측기.** `e2e-forage-village.walkTo` 를 **키보드**로 고쳤다(2.1/6.6/7.9초 · 평균 52px/s = 82%,
남은 18%는 8방향 양자화 + 경로 우회). **감사 기준 960px = 도보 15초는 그대로 맞다** — 단위를 바꿀 이유가 없다.
★족보 (70): **하네스는 클라의 입력 루프와 싸우지 마라 — 사람이 하는 대로(키보드로) 시켜라.**

### ★§0 인벤토리 — 지시서 전제와 다른 게 넷이었다

1. **정품 자작 경로가 "없다"는 건 틀렸다.** `doCraftEquipment` 가 **이미 있었고**,
   `EQUIPMENT_RECIPES.tool.accepts` 에 `stone` 이 들어 있어 **돌 3개로 정품 도구가 나왔다**.
   문제는 경로의 부재가 아니라 **① 시설 게이트가 없고 ② 즉석이며 ③ 장비 서랍에 묻혀 있다**는 것이었다.
   (빈손 배치 회부의 "정품 자작 경로가 아예 없다"는 **오진**이었다 — 여기서 정정한다.)
2. **작업대만 없었다.** 화덕(`campfire`)·노(`furnace`)·숯가마(`charcoal_kiln`)는 다 있고 **플레이어가 짓는다**
   (노·숯가마는 사유지 필수 + 다단 공정 + 조업까지 실장돼 있다). 도구를 만드는 시설만 없었다.
3. **품질 정본은 하나다** — `PlayerItems.craftItem(type, 숙련, 재료)` = `qSkill(lvl) × matGrade(재료)`.
   구매는 `materializeFromVillage(type, 마을Q)`. **같은 타입·같은 속성 구조**라 "플레이어제" 별도 품목이 없다.
4. **★도구는 econ 재화가 아니다.** `specialty` 에 `tool`/`weapon` 이 없고 `PV_DEPOSIT_MAP`(14종)에도 없다 —
   즉 **거래소에서 도구를 사고팔 수 없다**. 지시 D의 "같은 정본 가격으로 거래된다"는 전제가 성립하지 않는다(회부 B-1).

### 심은 것

**`server/facility.js`(신규) = 시설·대기열 정본.**
```
  시설 표      campfire→요리 · workbench→도구 · furnace/charcoal_kiln→제련 · 반경 96px(env)
  제작 시간    요리 20초 · 도구 3분 · 제련 2분 (전부 env · **벽시계**다)
  대기열       시설에 붙는다 · 하나씩 만든다(뒤엣것은 앞엣것 뒤에) · 상한 5개
  lazy 시간    틱을 안 돈다. 넣을 때 doneAt 을 적고 **볼 때** 지금과 견준다
               ⇒ **오프라인에도 진행된다**(가마는 밤새 탄다) · 수령만 접속해서
```
· **작업대**(신규 건물 · 통나무 4 + 석재 2 · **망치 불필요** — 빈손 사다리가 끊기지 않게 · 20kg).
· `doCraftEquipment` 에 **시설 게이트**: 단일 재료 → 작업대 · 배합 주조 → 노. 조잡한 석기는 **그대로 맨손**.
· `doCook` 의 손으로 적은 96px 비교를 걷어내고 **정본에 물어본다**(같은 수가 두 곳에 있던 것을 없앴다).
· **제작창 UI**(`facility` 패널 · Shift+K · **시설에 들어서면 저절로 열리고 나가면 닫힌다** — §8.2 의 유일한 예외).
  정렬은 서버가 낸다: **가능 → 재료 하나 모자람 → 나머지**. "하나 모자람"이 오늘의 할 일이고 장에 갈 이유다.
· **재료 선택 = 판단**: 같은 도구라도 `stone 24% / iron 34% / bronze 40%` 로 **예상 품질이 화면 값**이다
  (품질 공식을 다시 쓰지 않고 `craftItem` 을 미리 한 번 돌려 보여 준다).
· **제작 이력** `player.craftLog = {tool:{n,bestQ}}` 영속(`tools_json` 블롭) — §8.4 스킬 패널이 읽을 씨앗.

### ★★자작 vs 구매 손익 (상호의존 방어선 — `scripts/craft-metrics.js`)

```
  숙련      자작 품질   자작 내구  │  마을Q 0.45   마을Q 0.60   마을Q 0.75
  Lv0        0.240       206    │     0.348       0.456       0.564
  Lv4        0.384       221    │     0.348       0.456       0.564
  Lv7        0.456       231    │     0.348       0.456       0.564
  Lv10       0.600       245    │     0.348       0.456       0.564
  역전 숙련: 마을Q 0.45 → Lv4 · 0.60 → **Lv7** · 0.75 → Lv10
  거기까지 42개(레벨당 6개) × 3분 = **126분**
```
재료비는 **같다**(둘 다 3개). 손익을 가르는 건 **숙련과 시간**뿐이고, 자작에는 **작업대 선투자**가 있다.
⇒ **초보는 사는 게 낫고(0.24 < 0.46), 도구를 업으로 삼은 사람만 만드는 게 남는다.** 듀랑고병은 안 난다.
⚠단 이건 **품질 축**의 손익이다 — 도구가 econ 재화가 아니라 **가격 축 비교는 아직 성립하지 않는다**(회부 B-1).

### 사다리 (완성)

```
  맨손         —                      시설 없음   즉시    배수 1     내구 ∞
  조잡         자갈2·잔가지1·풀2       시설 없음   즉시    배수 2     내구 25      (2.2kg)
  정품(돌)     석재3                   작업대     3분     효율 축    내구 206~245  (12.0kg)
  정품(청동)   청동3                   작업대     3분     효율 축    내구 223~288
  [청동 주조 배합 · 전문 장비 = 회부]
```

### 검증

```
  test-craft        37/0   시설 게이트(반경·소유) · 품질 정본 일치 + 대조 · 대기열(순서·오프라인·상한·환불) ·
                           자작=구매 같은 품목 · 시장 전제 실측 · 손익 방어선 · 화덕도 같은 문법 ·
                           창이 자기 레시피만(요리 안 섞임) · 조잡 석기는 여전히 맨손
  e2e-craft         25/0   실클라: 시설 없으면 창 없음 → 작업대 건축 → **창 자동 개방** → 재료 선택(품질 표시) →
                           대기열 → 완성 표시 → 수령 → 장착 → 멀어지면 닫힘 → **서버가 게이트**(직접 요청 거절)
  전체 회귀         33종 중 **32 통과** · 남은 실패 1 = `e2e-mtfoot` 8/9(산 아크 소관)
                           ★러너 1회차에 4개가 빨개졌고 **그중 하나는 진짜였다**:
                             · `e2e-nature` — 새 인벤 아이템 `item_workbench` 의 **아이콘 PNG 404**(하네스가 잡았다) → 고친 뒤 **76/0**
                             · `test-guest-rejoin` · `e2e-waterperf` — 러너 연속 실행에서만 흔들리는 기존 항목(단독 재실행 26/0 · 17/0)
  마을 채집 감사     **51/51** (배산임수 기준 불변 — B-3 이 계측기 문제였으므로 기준을 안 바꿨다)
  3시드 800일 기준선  한 자리도 안 움직임 (아래)
```
```
  1020  인구 6,280 ✓ 소멸 0/51 ✓ 무기Q 821 ✓ 게시 1,647 ✓ 못갚아미게시 11,194 ✓
     7       5,993 ✓      0/51 ✓      755 ✓      1,583 ✓            10,787 ✓
    42       6,072 ✓      0/51 ✓      784 ✓      1,679 ✓            10,489 ✓
```
당연하다 — 제작창은 **플레이어 층 전용**이고 econ NPC 생산 경로는 한 줄도 안 건드렸다.

### 하네스·구현이 두 번 먼저 틀렸다 (족보 (71)·(72))

* **(71)** 시설 탐색을 `qtBuildings`(활성청크 인덱스)로만 짰더니 **테스트 부팅에서 시설을 못 찾았다** —
  청크에 안 실린 건물은 인덱스에 없다. 매번 전 건물(3만+채)을 훑을 수도 없다.
  ⇒ **드문 종류만 모아 두는 작은 목록**(건물 수 변화 · 5초로 무효화).
* **(72)** `Facility.nearest` 가 "가장 가까운 것"만 골라서, 내 작업대와 남의 작업대가 붙어 서면
  **남의 것이 뽑혀** "내 것이 아니다"가 떴다. ⇒ **쓸 수 있는 것 중 가장 가까운 것**을 고른다.

### 회부 (구현 금지)

`회부_제작창_다음층.md` — A: 청동 주조(노 UI 자리만) · 남의 시설 사용권·사용료 · 마을 시설 이용 ·
레시피 은닉·전수 · 제작 스킬 패널 UI(데이터는 쌓는 중) · 임수 7곳 · 시설 스프라이트(지금은 벡터 발판).
B: **도구가 econ 재화가 아니다(가격 경쟁이 아직 없다 — 여는 방법 셋과 각각의 대가)** ·
방어선이 품질 축에서만 수치화됨 · 제작 시간 3분은 찍은 값(실기 판정) · 대기열이 건물과 함께 사라짐 ·
시설 선택 규칙(B-5) · 조잡/정품이 다른 축이라 직접 비교 불가.

---

## 3-n. ★★2026-08-30 튜닝 일괄 배치 — **신체 3층 재배선 · 달력 · 인벤 마무리 · 버그 4건**

> 재민 실기 1차 판정 반영. 커밋 [재민 확정 2026-08-30] · 베이스 `f7f9c2f`(접속 진단) 위.
> ★선후: `f1659af`(인벤정비) → `ed1efa7`(이동조준·병행) → `f7f9c2f`(접속진단) → **이 배치**.

### ★§0 실측 — 손대기 전의 사실

| | 실측 결과 |
|---|---|
| ⓐ 신체 배선 | 달리기 관문 = `hunger>5 && thirst>5`(zone.js) · 이속 = `CURVES.hunger/thirst.move` 가 직접 곱함 · HP 회복 = `hunger>10 && thirst>10` **하드 게이트** · 달리면 허기 1.5배 |
| ⓑ 추위 | **누적식**. 노출>0 이면 `cold` 가 1 까지 **끝없이 올랐다**. 낮이 와도 불·실내 없이는 안 내려감 |
| ⓒ econ 계절 정본 | `sim/economy-sim-v2.js:210 seasonOf(day)` — 365일 / 90·90·90·95. `server/events.js` 가 거울(동기 계약 · `test-events ③`이 감시) |
| ⓓ 시세 창 day | **표기 버그 아님**. 시계가 **둘**이다(아래) |
| ⓔ 안개 위 논밭 | 영토(`simvil`)가 `renderables` 에 **마을 중심 한 점**으로 실림 → 게이트가 그 점만 봄 |

### ★★① 신체 3층 재배선 — 스태미나 / 피로 / 허기·갈증

```
  스태미나(신설)  단기 자원. 달리기가 쓴다. 짐 무게가 소모에 가중. 서면 찬다.
                  ★★**달리 수 있는지는 이것이 정한다 — 허기가 아니라.**
  피로            그대로(장기 노동 누적 · 하루의 마디)
  허기·갈증       ★이속·달리기에 **직접 페널티 없음**. 스태미나·HP **회복 배율**로만 작용.
                  "배고파도 뛸 수는 있는데 숨 고르기가 안 된다."
```

**채택값**(전부 env · `BODY_*`)

| 손잡이 | 값 | 뜻 |
|---|---|---|
| `HUNGER_SEC` | **2880** | 만복→공복 = 게임 2일 = 실시간 48분 |
| `THIRST_SEC` | **1440** | 게임 1일 = 24분 |
| `DECAY_SPLIT` / `DECAY_TOP_FRAC` | 0.5 / ⅓ | **위 절반이 전체 시간의 ⅓** — 배부름은 금방 꺼지고 진짜 배고픔은 천천히 깊어진다(대사 고증 + 유예). 위쪽 감쇠율이 아래의 **정확히 2배** |
| `STAM_SPRINT_SEC` | 22 | 빈손 전력질주 가득→바닥 |
| `STAM_REST_SEC` / `STAM_MOVE_MULT` | 30 / 0.35 | 서면 30초에 가득 · 걸으면 그 35% |
| `STAM_LOAD_W` | 1.2 | 적재율 1 이면 소모 ×2.2 |
| `STAM_MIN` / `STAM_RESUME` | 0.02 / 0.25 | 바닥나면 **빗장** — 25% 차야 다시 달린다(깜빡임 방지) |
| `COLD_TAU_SEC` | 240 | 추위 **수렴 시정수** |
| `COLD_NIGHT_W`/`DAY_W`/`SEASON_W` | 0.45 / 0 / 0.7 | 목표점 성분 |
| `COLD_INDOOR_MULT` / `COLD_FIRE_TARGET` | 0.30 / 0.05 | 실내·불이 목표점을 내린다 |

**추위 = 평형 수렴**(누적식 폐기). 주변이 **목표점**을 만들고 몸이 지수 수렴한다:

```
  목표점 실측 — 여름낮 0 · 여름밤 0.45 · 겨울낮 0.7 · 겨울밤 1.0
                옷(warmth 50) → 0 · 모닥불 → 0.05 · 실내 → ×0.3
  ⇒ 밤에 오르고 **낮이 오면 해소 행동 없이도 내려간다.**
    겨울엔 평형점 자체가 높아 옷·불 없이는 안 내려간다(그게 겨울의 뜻이다).
```

**★아사 폐지 캐논 유지**(재민 재확정: 죽음 설계 배치 전까지 보류).
극단(0)에서 회복 배율이 **0** = 회복 정지. **HP 는 한 점도 안 깎인다** — `test-body ⑫` 가
10분 방치 후 HP 불변을 못 박고, `body.js` 소스에 HP 를 깎는 줄이 **아예 없음**까지 확인한다.

★사장된 상수 둘: `SPRINT_MIN_GAUGE__DEAD_SEE_body_canSprint`(관문은 `Body.canSprint`),
`SPRINT_DRAIN_MULT__DEAD_SEE_body_js`(종전 배치). **이름으로 죽여 둔다** — 지우면 다음 사람이
"여기가 정본"이라 믿고 고치고 아무 일도 안 일어난다.

### ★② 달력 — 원천은 econ 계절 정본 **하나**

HUD 시각 옆 `📅 0년 봄 1일`. **새 매핑 상수를 한 개도 안 적었다** —
`Events.calendarOf` 가 `seasonOf` 만 불러 한 해 길이·계절 시작일·계절 길이를 **전부 유도**한다
(`test-calendar ③`이 달력 코드에 90/180/270/365 가 **0개**임을 소스로 확인한다).
날짜는 **econ 게임일**(`SimVillages.econDay()`)을 쓴다 — 마을 재고창과 같은 시계.
※`econDay()` 는 `null` 을 낸다(0 이 아니라): **day 0 은 정당한 값**이라 `||0` 로 뭉개면
첫날에 벽시계로 잘못 떨어진다(하네스에서 "3402년 봄 1일"로 실제로 그랬다).

```
  ★수치 보고 — 재민 확정 "첫 겨울 = 실시간 2~3주차"와 **어긋난다**:
    첫 겨울 = econ day 270 → 24분/일 기준 **실시간 4.5일 = 0.64주차**
    한 해 365일 → 실시간 6.1일
  ⇒ 조정(계절 주기 또는 하루 길이)은 **회부**. 이 배치는 수치만 낸다.
```

### ★③ 인벤 마무리

· **빈 행 제거** — 수량 0 은 **페이로드에서** 턴다(`invPayload`). 살아 있는 맵엔 0 이 남지만
  (옛 경로가 `-=` 로 그래 왔고 지우면 순회가 조용히 바뀐다) 클라는 0 을 **못 받는다**.
· **도구 통일 목록 편입 + 드롭** — 옛 `toolRowsHtml`(도구 전용 표) **삭제**.
  도구는 태생이 개체라 하위 줄이 곧 그 도구다. 착용·단축키는 **줄 우클릭**(좌클릭=착용/해제).
  `drop_item {toolId}` 로 버리면 바닥에 **내구도까지** 실려 떨어지고, 주우면 그대로 돌아온다.
  ⚠도구는 상자에 못 넣는다(상자는 수량 맵이라 개체를 못 담는다 — 화면이 그렇게 말한다).
· **드래그 수정키 복원** — 정비 배치에서 사라졌던 것(회부 B-2). 재민 확정("부모 드래그=전량")과
  **양립**시켰다: 수정키 **없으면 전량** · Shift=10 · Ctrl/Alt/Meta=99.
· **★상자 원장** — 왕복 보존을 상자까지. `b.data._led[item] = [{kg,d?}]`.
  밑줄 접두사라 기존 두 소비자가 **이미** 걸러낸다(`granaryItems` 의 `_` 스킵 · 철거 루프의
  `typeof v === 'number'`) ⇒ 곳간 회계·철거를 안 건드리고 얹혔다. **그 둘이 안 걸렀다면 회부감이었다.**
  철거 반환 경로도 원장을 돌려준다(안 그러면 철거가 **무게 세탁기**가 된다).
  ⚠상자 안 개체엔 **주소가 없다**(FIFO 로만 나온다) — 개별 인출은 회부.

### ★④ 버그·조사 4건

**ⓐ 안개 위 논밭 — 재현·수리·회귀 걸었다.**
기전: 영토가 `renderables` 에 **마을 중심 한 점**으로 실려, 중심을 한 번 본 마을이면
**반경 1,200px 의 경계 셀 전부**가 그려졌다. 배치 21이 자연물에서 고친 것과 **같은 결함의 다른 층**
(클라가 스스로 넓게 그리는 것은 개체 하나로 게이트하면 반드시 샌다).
⇒ 경계 셀·크립 링·라벨을 **셀마다** `_seenChunks` 로 판정. 실측:

```
  초원 화면: 경계 후보 251셀 · 그중 **미탐사 212셀**
    수리본  안 본 셀에 그림 **0**
    대조군  안 본 셀에 그림 **212** (`simvilCellGateOff` — 자명 통과 금지용 손잡이)
```

**ⓑ 시세 창 day — 표기 버그가 아니다. 시계가 둘이었다.**

```
  ⓐ zoneGameDay()      벽시계 파생(에폭부터 흐른 실시간 / 하루 길이)
  ⓑ state.world.day    econ 틱 카운터(하루 경계마다 +1 · **따라잡기 없음**)
  ⇒ 서버가 꺼져 있던 동안 ⓐ는 뛰고 ⓑ는 안 뛴다 = **영구히 벌어진다.**
```
마을 재고창은 ⓑ 를 그린다. 재민이 본 "몇 초마다 day 5씩"은 **그 서버의 하루가 짧았던 것**
(`VILLAGE_DAY_MS`)이다 — 라이브 기본은 24분이라 재현되지 않는다.
⇒ 달력도 ⓑ 를 쓰게 통일했고, 마을 재고창에 **달력을 같이** 적는다(둘이 같으면 시계가 하나라는 증거).
★로컬 테스트 서버는 하루가 빠르다 — `VILLAGE_DAY_MS` 를 준 판에서 날짜가 튀는 건 정상이다.

**ⓒ RTT 스파이크 — 상관 확인됨(수리는 회부).** `scripts/rtt-metrics.js`(신규 계측기 · 러너 금지):

```
  150초 · 하루 6초(econ 경계를 자주 만들려고 줄임) · 51마을
    RTT 표본 748 — 중앙 39ms · p95 3,088ms · p99 6,868ms
    econ_day  48회 · 중앙 684ms · 최대 8,410ms
    [econ_day] ±600ms 창 안 RTT 평균 1,195ms vs 바깥 110ms  → **×10.9  ★상관 있음**
    [tick]     ±600ms 창 안 868ms vs 바깥 2.5ms             → ×350
  ⇒ econ 하루 틱이 이벤트 루프를 붙잡는 동안 RTT 가 그대로 밀린다.
    라이브 영향은 **게임일마다 한 번**(24분에 1회) 수백 ms~1초 급 정지.
  ★수리(econ 틱 분산)는 **회부** — 지시대로 이번엔 계측만.
  ※하루 6초는 빈도를 부풀린 설정이다. 1회당 비용과 인과는 그대로지만 빈도는 라이브와 다르다.
```
서버에 `/perf` 엔드포인트(무거운 작업 링 400건)와 `perfMark()` 를 심었다 — **관측자**다(동작 불변).

**ⓓ 게스트 안내** — 로비에 한 줄 + **새로 시작** 단추(토큰 폐기 = 새 몸).
저장된 게스트 몸이 있고 계정명을 안 적었을 때만 뜬다. ★토큰은 화면에 안 찍는다(배치 13 규약).
옛 몸은 서버에 **남긴다**(지우지 않는다 — 되돌릴 수 없는 일은 안 한다).

### 하네스

| 하네스 | 결과 | 비고 |
|---|---|---|
| `test-body` v2 | **87 / 0** | 45 → 87 |
| `test-calendar`(신규) | **12 / 0** | 전 계절 경계 스윕 · 사본 아님 소스 검사 |
| `e2e-inv` | **71 / 0** | 57 → 71 |
| `e2e-nature` | **80 / 0** | 안개 위 논밭 4판정 추가 |

★`test-body` **갱신 목록**(재배선과 충돌해 스펙을 바꾼 판정):
· ⓪ "다섯 축 전부 이속 곡선" → **EFFECT_AXES 셋만** + RECOVER_AXES 둘의 회복 곡선 검사로 대체
· ③ 연속성 스윕 대상에 **회복 곡선 추가**, 도약 상한 `STEP×1.2` → `STEP×4`(회복 곡선이 더 가파르다)
· ③ 자명 통과 금지 절이 이속·회복 **둘 다** 실제로 깎는지 확인
★**신설**: ⑨ 스태미나 · ⑩ 허기·갈증 재배선 · ⑪ 감쇠 실측 · ⑫ 아사 폐지 · ⑬ 추위 평형

### 대리 지표

```
  식사 — 만복→공복 48.0분(허기) · 24.0분(갈증) · 위 절반이 전체의 33%
         ⇒ 1시간 세션에 **식사 1~2회**(재민 목표대로)
  스태미나 — 빈손 전력질주 **22초** · 서서 **8초** 쉬면 재개(25% 문턱)
             적재율 1.0 이면 5초에 0.773 → 0.500 (소모 ×2.2)
  추위 평형 — 목표점 여름낮 0 / 여름밤 0.45 / 겨울낮 0.7 / 겨울밤 1.0
             밤 10분 → 0.2 이상 · 낮 15분 → 절반 이하로 **저절로** 내려감
  RTT — 위 ⓒ 표
```

### 회귀 · 기준선

러너 39종 중 **36 통과 · 3 실패**, 셋 다 **이 배치와 무관**:
· `e2e-mtfoot` ⑤ — 지시서가 "확인만"이라 한 산 아크 건. 그대로 이월.
· `e2e-events` · `e2e-waterperf` — **긴 전수 판의 부하 흔들림**.
  둘 다 단독·러너 순서 재실행에서 **32/0 · 17/0** 으로 통과했다
  (waterperf 는 성능 문턱 판정 7.0배 vs 요구 8배 — 기계 의존 · 인계에 이미 적힌 성질).

**3시드 800일 기준선 — 한 자리도 안 움직였다**(신체는 플레이어 층 · 달력은 읽기 전용):

```
  1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,647   (기록값과 동일)
     7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,583   (기록값과 동일)
    42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,679   (기록값과 동일)
```
`sim/build-econ-bundle.js --check` **8/8 일치** · 변경 파일에 `sim/`·`specialty.js` **0건**.

### 회부

`회부_튜닝1_다음층.md` — 죽음 설계(아사 재론) · **econ 틱 분산(수치 첨부)** ·
**계절 주기 조정(첫 겨울 4.5일 vs 목표 2~3주)** · MOVE_MODEL 기본 전환 · 도구 슬롯 가산 ·
낚시·거래소·무게·제작 손잡이 · 부패·보존 · P2P 거래 · 상자 개체 개별 인출 · 두 시계 통합.

---

## 3-o. ★★2026-08-30 접속 진단 배치 — **조용한 무한 재접속을 없앤다**

> 재민 실기 원문: *"자꾸 언제는 기다리면 됐다가 언제는 안 됐다 그래.. 이게 기다리면 되는 건지,
> 진짜 에러인지 사용자한테 구분 가게 해야 하는 거 아냐? 물론 에러는 수정하고.."*
> 커밋 [재민 확정 2026-08-30] · 베이스 `f1659af`(인벤 정비).

### ★실기 로그가 말한 것

```
  [recover] ping 후 15초간 pong 한 번도 못 받음 — ws 좀비, 강제 close
  [recover] primary 재연결 hanbando
  [ws] close hanbando role=primary code=1006 wasClean=false      ← 이 셋이 무한 반복
```
줄번호(6023·6477·7998)가 **HEAD 와 정확히 일치** → 라이브는 이미 인벤 정비 배치가 올라간 상태였다.
`ping 후 15초` 분기는 `readyState === 1` 일 때만 돈다 ⇒ **WS 핸드셰이크는 성공했다.**
그런데 `welcome` 도 `pong` 도 안 왔다.

### ★원인 구조 — **에러 경계가 없었다**

`wss.on('connection', async (ws, req) => { … })` 에 **try/catch 가 없었다.** 중간 어디서든 던지면:

```
  · 소켓은 열린 채 남는다(클라 readyState = 1)
  · `attachPlayerHandlers` 에 도달 못 해 **메시지 핸들러가 아예 안 붙는다**
  · 그래서 welcome 도 pong 도 영영 안 온다  = 완벽한 침묵
  · 프로세스 차원 핸들러도 없어서 **로그에도 아무것도 안 남았다**
```
침묵은 **진단 불가능한 상태**다. 화면도 없고 로그도 없으니, 재민에게는
"기다리면 되는 것"과 "진짜 에러"가 **똑같이 보였다**. 그게 결함의 본체다.

★`server.listen` 은 파일 맨 끝(8345줄)이라 **부팅 중엔 연결을 아예 안 받는다** —
따라서 "소켓이 열렸다"는 건 부팅 완료를 뜻하고, "부팅 중이라 그렇다"는 가설은 이 로그에선 배제된다.

### ★수리 — 서버(4겹)

1. **본문을 함수로 빼고 `.catch`** — `_acceptConnection(ws, req, C)`. 던지면
   `conn_error{ref, stage, msg}` 를 보내고 **스택을 로그에 남기고** 4001 로 닫는다.
2. **접속 즉시 `conn_hello`** — 인증·로드보다 **먼저** "받았다"를 말한다.
3. **조기 ping 응답** — `attachPlayerHandlers` 가 붙기 전에도 `pong{stage}` 를 돌려준다.
   `ws.__ready` 가 서면 손을 뗀다(이중 pong 금지).
4. ★★**마감액**(`CONN_DEADLINE_MS`, 기본 30초) — try/catch 가 못 잡는 **반쪽**을 막는다.
   `await` 가 영영 안 돌아오면 **던지지도 않는다**(central 이 느린 경우가 그렇다) —
   그때도 화면은 똑같이 침묵한다. 시간으로 끊고 **멈춘 단계를 이름으로** 말한다.
   ※던지는 실패와 **안 끝나는 실패는 다른 결함**이다. 하네스도 둘을 따로 만든다.

단계 표식 `C.stage`: `accepted → auth → load → spawn → welcome → handlers → ready`
(관찰자는 `observer`). 실패하면 이 이름이 **로그·화면 둘 다에** 뜬다. `ref`(c17 등)로 맞춘다.

부수 수리 둘:
· **반쪽 등록 정리** — `spawn` 뒤에 실패하면 `players` 에 **아무도 조종 않는 유령 몸**이 남았다.
  이제 `_connCleanupHalf` 가 지우고 `player_left` 를 방송한다(인원수·`PLAYER_CAP` 도 안 갉아먹는다).
· **프로세스 차원 로그** — `unhandledRejection`·`uncaughtException` 에 스택을 남긴다.
  **동작(죽는다)은 그대로** 둔다 — 여기서 삼키면 반쯤 죽은 서버가 남아 더 나쁘다. 토큰은 마스킹.

### ★수리 — 클라(상태 넷을 화면이 말한다)

판정 재료는 **서버가 준다**(클라가 추측하지 않는다):

| 화면 | 색 | 뜻 | 근거 |
|---|---|---|---|
| 서버 응답을 기다리는 중 — 잠시 기다리면 대개 들어가진다 | 노랑 | 기다릴 만하다 | `conn_hello` 도 못 받음 |
| 서버가 받았다 — 입장 처리 중 | 노랑 | 기다릴 만하다 | `conn_hello`/`pong{stage}` 는 옴 |
| 서버가 응답하지 않는다 / 입장 처리를 못 끝내고 있다 | **빨강** | 오류일 수 있다 | 위 상태가 25초/20초 넘음 |
| 서버에 들어가지 못했다 — **기다려도 해결되지 않는다** | **빨강** | 확정 오류 | `conn_error` 수신 |
| 연결 끊김 — 재접속 중 | **빨강** | 들어갔다가 끊김 | 서버 틱 미수신(직전 배치) |

· 빨강일 때만 **새로고침 단추**가 뜬다. 경과 초·시도 횟수·단계·`ref` 를 같이 적는다.
· ★시계는 **이번 끊김 전체**(`connOutageAt`)를 잰다. 1차 실장이 시도별로 쟀더니
  백오프 재시도마다 0초로 돌아가 **6번째 시도인데 "0초 경과"** 가 떴다(영원히 노랑).
· ★**백오프**(1·2·4·8·15초 상한 · 확정 오류면 처음부터 15초). 종전엔 매 프레임 즉시 재연결이라
  15초마다 붙었다 끊었다를 **영원히** 반복했다 — 서버에도 부담이고 화면엔 아무 말도 없었다.
  자동 회복은 그대로 산다(느려질 뿐 — `e2e-conn` ⑥이 새로고침 없이 복귀하는 걸 지킨다).
· 배너는 **한 함수(`paintConnBanner`)만** 그린다. 상태 넷을 두 곳에서 판정하면 갈린다.
· ★**유예 2초**(`CONN_QUIET_MS`) — 정상 접속·존 핸드오프는 1초 안에 끝난다. 유예가 없으면
  **문 하나 지날 때마다 경고가 번쩍인다**(늑대 소년). 2초 넘게 못 들어갔을 때만 말한다.
· ★새 소켓을 여는 순간은 **언제나** `connecting` 이다. 1차 실장이 `connPhase !== 'ready'` 로
  막았더니 **잘 놀다가 끊긴 경우엔 상태가 ready 로 굳어** 시도 횟수도 안 세고 배너도 안 떴다 —
  재민이 본 침묵의 한 갈래가 정확히 이것이었다.

### ★★따라 나온 발견 — 기존 하네스 다수가 **자명 통과**였다

입장 게이트로 널리 쓰이던 관용구

```js
  for (…; !(await page.evaluate(() => !!(window.__getMyAbs && window.__getMyAbs()))); …)
```
는 **언제나 참**이다 — `let myAbsPredicted = { x: 0, y: 0 };` 로 시작하기 때문이다.
`e2e-conn` ②에서 "확정 오류인데 입장 판정이 true" 로 잡혔다.
⇒ `window.__inWorld()`(= welcome 을 받았는가)를 새로 냈고 **`e2e-conn` 은 그걸 쓴다**.
※기존 하네스들은 뒤따르는 `sleep` 덕에 실제로는 통과해 왔다 — **판정만** 무의미했다.
  일괄 교체는 이 배치 범위 밖이라 **회부**(`회부_접속진단_다음층.md` B-1).

### ★★유령 클라 진단 — 이제 3단이 아니라 **화면을 읽으면 된다**

```
  0단 · 화면       배너 색이 곧 판정이다. 노랑=기다려라 · 빨강=오류(단계·ref 가 적혀 있다).
  1단 · 새로고침   빨강이면 단추를 눌러라. 옛 번들을 보고 있던 것이면 여기서 끝난다.
  2단 · 서버 로그  화면의 ref 로 곧장 찾는다:
                     docker logs --tail 200 durango-zone-hanbando | grep -E "접속 처리 실패|시간 초과|ref=c"
                   ✗✗ 줄에 **단계와 스택**이 있다. 종전엔 이 줄이 아예 없었다.
  3단 · 콘솔       window.__connState()  → {phase, attempts, hello, stage, ref, reason}
                   window.__tickGap()    → 마지막 틱 이후 ms (들어간 뒤의 문제일 때)
```

### 하네스

| 하네스 | 결과 | 재는 것 |
|---|---|---|
| `e2e-conn`(신규) | **41 / 0** | 정상 무소음 · 던짐→확정오류 · **멈춤→마감액** · 침묵→노랑 · 노랑→빨강 전환 · 백오프 · 자동 회복 |

러너 36종 중 **34 통과 · 2 실패**, 둘 다 **이 배치와 무관**:
· `e2e-mtfoot` ⑤ — 지시서가 "확인만" 이라 한 산 아크 건. 그대로 이월.
· `test-guest-rejoin` ② — **부하 A/B 로 귀속을 갈랐다**(아래).

### ★★`test-guest-rejoin` ② — 부하에서만 나는 **B-6 승계 경합**(선재 결함)

러너 안에서 1회 실패했다(`걷던 자리와 155px · 낡은 저장 좌표와 101px` = 승계 실패).
단독으로는 HEAD·직전 커밋 **둘 다 3/3 통과** — 그래서 **CPU 2코어를 태우고** 다시 갈랐다:

```
  직전(f1659af) + 부하 : ✓ 20px / ✗ 254px(저장 좌표와 0px) / ✓ 20px   ← **직전 커밋도 실패한다**
  지금(HEAD)   + 부하 : ✓ 18px / ✓ 16px / ✓ 20px
```
⇒ **선재 결함이다.** 이 배치가 만든 것이 아니다(오히려 이번 판에선 HEAD 가 3/3).

기전: `A.close()` 직후 같은 토큰으로 B 가 붙을 때,
· A 의 소켓 close 처리(로컬 이벤트 — `players.delete`)와
· B 의 승계 스캔(`await central.guestIdentity` **뒤에** 있다 — HTTP 왕복)
이 경합한다. 부하가 걸리면 central 왕복이 늘어져 **A 의 close 가 먼저 이긴다** →
승계할 몸이 이미 없어 저장본 좌표로 태어난다. `_supersededBy` 는 *반대 방향*(옛 close 가 새 좌표를
덮어쓰는 것)만 막지, **몸이 먼저 사라지는 갈래**는 안 막는다.
⇒ 수리 방향(짧은 유예 동안 몸을 붙잡아 두기)은 **B-6 재민 확정 구조를 건드린다** — 회부했다
(`회부_접속진단_다음층.md` B-6).

★자명 통과 금지: ②는 `E2E_CONN_FAIL=<단계>` 로 **일부러 던지게** 하고,
②-b는 `E2E_CONN_HANG=<단계>` 로 **일부러 안 끝나게** 한다. 실패를 못 만들면
"잡는다"는 판정은 아무것도 증명하지 않는다. 둘 다 `E2E_GIVE=1` 일 때만 사는 분기다.

### ★남은 것 — **라이브의 그 실패가 무엇이었는지는 아직 모른다**

재현하지 못했다. 옛 서버(4b54b88)가 쓴 저장본을 새 서버가 읽는 **업그레이드 경로**를
워크트리로 만들어 돌려 봤지만 정상 접속했다. 라이브에만 있는 조건(51마을 · 큰 welcome ·
central 부하)이 남아 있고, **서버 로그 없이는 단정할 수 없다** — 추정을 원인으로 쓰지 않는다.
⇒ 이 배치가 한 일은 **다음 번에 그 답이 화면과 로그에 저절로 뜨게 만든 것**이다.
재발하면 배너의 `단계`·`ref` 와 위 2단 grep 한 줄이면 끝난다.

---

## 3-p. ★★2026-08-30 정비 배치 — **인벤·바닥 목록 통일(원장 승격) · 비네트 원인 축 · 접속 안정성**

> 실기 1차가 잡은 **구조 결함 셋**을 고친 정비 배치다. 게임 손잡이는 **한 값도 안 건드렸다**
> (기록지가 아직 안 닫혔다). 커밋 [재민 확정 2026-08-30] · 베이스 `4b54b88`(시설 제작창).

### ★결함 ① 인벤·바닥 목록 비대칭 — 자료구조가 UI 로 새어 나온 것

재민 원문: *"바닥에 떨어진 아이템 목록과 인벤토리 안의 아이템 목록이 같은 종류의 인스턴스가 아닌가봐?"*
맞았다. 실체는 UI 취향이 아니라 **두 벌로 짜인 코드**였다:

```
           바닥                              인벤
  원본   드롭 인스턴스 배열                  품목→개수 **스칼라 맵**
         {id, item, count, kg}               {fish: 12}
  렌더   인스턴스 1개 = 1줄                  품목 1종 = 1줄
  개별kg 인스턴스마다 있음                   없음(kgLedger 가 **수의 배열**이라 FIFO 만)
```

재민 판정(ⓑ · 좀보이드 문법): *"기본적으로 한 줄로 나오되, 펼칠 수 있게. 펼칠 수 있어지면
각 줄을 따로 드롭도 가능하고, 원래 한 줄을 드래그하면 모든 아이템이 드롭되고."*

**A. 서버 — `kgLedger` 를 주소 가능한 개체 목록으로 (`server/carry.js`)**

```js
  p.kgLedger[item] = [{ id, kg, d? }, …]     // 옛 형태: [kg, kg, …] (수의 배열)
```
· `id` 는 **그 플레이어의 원장 안에서만** 유일하다(저장 blob 이 플레이어별이라 전역 유일 불필요).
  `_nextId` 는 복원 때 **최대값+1** 로 되짚는다 — 재접속 뒤 새 개체가 옛 id 와 안 겹친다.
· `takeByIds(p, item, ids)` — **FIFO 강제 해제**. 한가운데 0.4kg 물고기만 골라 뺀다.
· `_norm` 이 읽는 모든 길목에서 **옛 저장(수의 배열)을 항목 모양으로 승격**한다(호환 자동).
· `toSave` 도 항목 모양으로만 내보낸다 — 저장 전후 구조가 갈리면 "재접속을 넘어 그대로인가"
  판정이 **자기 직렬화 때문에** 실패한다(로트가 이미 겪은 함정이다).

**★불변식**: 원장이 있으면 `L[item].length === inventory[item]`.
· `reconcile` 이 **짧으면 표준 kg 으로 채우고**(무게 중립 — `peekKg` 가 어차피 표준으로 세던 몫),
  길면 자른다. **채움이 있어야 낱개마다 주소가 생긴다**(안 채우면 "표준 몫"은 골라서 못 버린다).
· `assertInvariant` 는 **쓸개보다 먼저** 돈다. 뒤에 두면 `reconcile` 이 이미 고친 뒤라
  **무엇을 검사하든 항상 통과한다 — 이빨 없는 검사다.** `CARRY_ASSERT_THROW=1` 이면 던진다.

**★★소비 정본 하나 — `consumeItem(player, item, n)`**
`player.inventory[x] -= n` 직접 차감이 **10곳**에 흩어져 있었다. 스칼라만 깎으면 쓸개가
**뒤에서 잘라** 메우므로 *"2kg 물고기를 먹었는데 0.4kg 짜리가 사라지는"* 일이 난다 —
수량은 맞는데 **무게가 틀리고**, 화면으론 절대 안 보인다. 전부 이 함수로 모았다
(먹기·요리 투입·가공·제작·건축 배치·터 선납 3곳·상자 넣기).
원장 없는 벌크에는 무해한 무동작이라 **모든 소비 경로가 써도 안전하다.**

**★★인벤 전송 정본 하나 — `sendInventory(player, where)`**
`send(ws, {type:'inventory', …})` 직접 호출 **46곳**을 전부 여기로. 스칼라·원장·로트가
**다른 메시지로 갈리면 화면이 반드시 어긋난다**(전리품 표 두 벌 사고의 전송판).
한 메시지 = 한 스냅샷: `{ inventory, ledger, lots }`.

**★"무엇이 개체형인가"는 어디에도 안 적는다 — 구조가 정의한다.**
품목 이름표를 새로 만들면 그게 두 벌째 표이고 어종이 늘 때마다 갈린다.
**원장이 있으면 개체형이다.** 원장은 `noteInstance` 로만 생기고, 그건 **실제로 잰 kg 이 있는
경로**(낚시)에서만 불린다. 바닥 왕복도 이름표를 안 쓴다 — 버릴 때 뺀 항목을 바닥템에 싣고(`gi.led`),
주울 때 그게 있을 때만 원장으로 되돌린다.
※옛 결함: 줍기가 **무조건** 원장을 만들어, 잔가지를 버렸다 주우면 벌크에 원장이 생겼다(3층 캐논 위반).

**B. 클라 — 목록 컴포넌트 한 벌 (`ulRenderRows` / `ulSend`)**

세 컬럼(내 인벤·바닥·상자)이 **같은 함수**를 쓴다. 사용법:

```js
  ulRowsMine(inventory) / ulRowsGround(gItems) / ulRowsChest(data, cid)
      → [{ item, count, kg, kids, drag }]        // 모양은 하나
  ulRenderRows(rows, col, { act, actTitle, empty })   // 렌더 — DOM 구조가 곧 계약
  ulSend(drag, target, amount|null)                    // amount=null → 그 줄 전부
```
· ▶ 는 **펼칠 것이 둘 이상일 때만** 돋는다(하나면 펼쳐도 같은 줄이라 뜻이 없다).
· 하위 줄 = 개체(개별 kg) **또는** 로트(취득일·나이). **어느 쪽인지는 서버 페이로드가 정한다** —
  원장이 오면 개체, 없고 로트가 오면 로트, 둘 다 없으면 하위 줄 없음(무기한 벌크).
· 바닥은 **덩이가 둘 이상일 때** 펼친다(실제로 서로 다른 더미가 둘이라는 사실 그대로).
· **버튼 = 1개 · 드래그 = 그 줄 전부**(부모면 전량 — 재민 확정).
· 짐 모양: `{kind, item, cid?, n, ids?, lotDay?, giIds?}`. 줄이 **제 주소를 들고 다닌다** —
  "몇 개"가 아니라 **"어느 것"** 을 말할 수 있어야 펼친 줄이 따로 움직인다.
· 펼침 상태는 `ulOpen`(Set) 에 남아 재렌더를 넘어 산다(인벤 메시지마다 패널이 다시 그려진다).
· 새 메시지 인자: `drop_item {ids}` · `drop_item {amount, lotDay}` · `pickup_item {giIds}`.
  **옛 인자는 그대로 산다**(`item`+`amount`, `giId`) — 하네스·옛 클라가 안 깨진다.

### ★결함 ② 비네트가 "심각함"만 외치고 **원인을 안 말했다**

실기 실측: 화면이 붉은데 재민이 원인을 **무게로 오독**했다. 방금 판자를 버린 사람이 붉은 화면을
보면 당연히 무게를 의심한다 — 그런데 무게는 1단계였고 3단계는 다른 축이었다.
· 3단계 축 아이콘을 **화면 좌하단에 크게**(`#vgAxes` — 기존 무들 이모지 재사용 · **새 아트 없음**).
· 비네트 **색조를 축 계열로**(`--vg-rgb`): 갈증=청 90,150,220 · 허기=황 · 추위=하늘 · 피로=보라 ·
  부상=적 · 과적=흙빛. 기본값이 옛 색이라 **끄면 종전과 똑같다**(`VIGNETTE_AXIS_TINT=0`).
· §7 동시 표시 상한(`BODY_SHOW_MAX`, 기본 3)을 큰 아이콘도 **그대로** 쓴다.
· 색조는 **가장 심한 축 하나**만 쓴다(섞으면 진흙색 — 회부 B-4).

### ★결함 ③ 유령 클라 — 서버 틱 0인데 화면이 "돌아갔다"

실기 실측: `보정 0회` / 초당 틱 0 이 10초 넘게 이어지는 동안 클라 예측 + 선수신 청크로
화면이 멀쩡히 돌아갔고, **그 상태에서 매긴 판정이 전부 오염됐다**(1차의 ①·⑤).

★**왜 종전 감시가 못 잡았나 — 실측으로 갈랐다.**
`checkOrphan`(내 pid 2초 미수신)은 **정상적으로 자가 치유한다**. 탐침에서 틱을 막자 2초 만에
재연결해 새 소켓으로 틱이 돌아왔다. 못 잡는 건 **재연결해도 틱이 안 오는** 상태다 —
`checkOrphan` 은 발동하며 `lastTickWithMyPidAt = 0` 으로 **자기 자신을 끄기** 때문이다.

수리: `lastTickAt`(`checkOrphan` 이 리셋하지 않는 값)을 **상시** 감시한다.
· 문턱 `GHOST_STALL_MS`(기본 5000 · env). 넘으면 → `#netLost` "연결 끊김" 표시 +
  **입력 예측 정지**(`netStalled` 를 `_selfGone` 과 나란히 게이트) + 미ack 입력 폐기 + 소켓 강제 close
  → 기존 `ensurePrimaryConnection` 이 재연결(같은 토큰 = 같은 몸 · B-6 규약).
· 틱이 돌아오면 자동 해제·예측 재개.

### ★★유령 클라 진단 3단 (재발 시 **누구나 같은 순서로**)

```
  1단 · 새로고침       페이지 새로고침 한 번. 옛 번들을 보고 있던 것이면 여기서 끝난다.
                       (실기 1차가 실제로 이 경우였다 — "옛 버전을 보고 있어서 그랬나")
  2단 · 클라 자가진단  F12 콘솔에서:
                         window.__netStalled()   // true 면 클라가 이미 알고 있다 = 서버/망 쪽
                         window.__tickGap()      // 마지막 틱 이후 ms. 30Hz 면 <100 이 정상
                         window.__corrN          // 보정 누적. 10초에 200±면 정상, 0 이면 틱 미수신
  3단 · 서버 확인      docker logs --tail 50 durango-zone-hanbando | grep -E "tick_avg|GC"
                         tick_avg 6~8ms 가 정상. sendBuf 가 쌓이면 송신 병목.
                       서버가 멀쩡한데 클라만 0 이면 **그 소켓이 좀비**다 → 새로고침(1단)이 답.
```
※`window.__netStalled()` 가 **false 인데 화면이 안 움직이면** 그건 접속이 아니라 렌더/입력 문제다.

### 하네스

| 하네스 | 결과 | 재는 것 |
|---|---|---|
| `test-ledger`(신규) | **46/0** | 원장 id·지목 제거 · 불변식 9경로 스윕 · 왕복 보존 · 벌크 원장 미생성 · 로트 펼침 · 저장 왕복 · 페이로드 |
| `e2e-inv`(신규) | **57/0** | 접힘·▶·펼침·개별 드롭·부모 전량·왕복 · **DOM 구조 지문 동일성** · 비네트 축 · 유령 클라 |

★`test-ledger` ②는 쓸개(`reconcile`) **전후를 따로** 잰다 — 후만 맞는 건
"뒤에서 잘라 메웠다"는 뜻이고, 그건 어느 개체의 kg 이 사라졌는지 임의라는 뜻이다.
★`e2e-inv` ④의 지문 비교는 **같은 종류의 줄끼리**만 견준다(컬럼마다 든 물건이 달라
줄 구성은 당연히 다르다 — 두 벌인지는 "`ul-row` 가 어디서나 같은 칸 구성인가"로만 갈린다).
★`e2e-inv` ⑧은 **WebSocket 자체**를 패치해 앞으로 열릴 소켓의 틱까지 막는다.
지금 소켓 하나만 막으면 `checkOrphan` 이 먼저 자가 치유해 **"감지 못 했다"로 오독**한다(실제로 그랬다).

### 대리 지표 · 기준선

```
  200마리 낚시 후 인벤 메시지 = 5,541 B (항목당 27.7 B · 32KB 상한의 17%)  ⇒ 압축 회부 불필요
```

**3시드 800일 기준선 — 한 자리도 안 움직였다**(원장 승격이 econ 에 안 스몄다):

```
  1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,647   (기록값과 동일)
     7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,583   (기록값과 동일)
    42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,679   (기록값과 동일)
```
★**비트 수준 증거 하나 더**: `econ-regress`(800일·6마을) 가 다시 쓴 `sim/out/simv2-{7,42}-800d.json` 이
`git diff` 에 **한 줄도 안 뜬다** — 레포에 커밋된 덤프와 **바이트 동일**하게 재생성됐다.
`sim/build-econ-bundle.js --check` **8/8 일치**, 변경 파일에 `sim/`·`specialty.js` **0건**.

### 회귀

러너 35종 중 **33 통과 · 2 실패**, 둘 다 **이 배치와 무관**:
· `e2e-mtfoot` ⑤(기슭 기울기) — 지시서가 "**확인만**"이라 한 산 아크 건. 그대로 이월.
· `e2e-village` — **알려진 간헐 실패**(2026-08-26 에 같은 판정·같은 로그 signature 로 기록됨:
  `central save 실패 (e2echon): socket hang up`). **단독 32/0 · 러너에서 앞 하네스(`e2e-metallurgy`)를
  앞세운 같은 순서 재현도 32/0** — 두 번 다 재현 실패. 원인 미상으로 **다시 남긴다**.

### 회부

`회부_인벤정비_다음층.md` — A: ⓐ 전면 인스턴스화 **불필요로 판정(닫음)** · 도구 슬롯 가산 통일 ·
손잡이 튜닝 전부 · 건축물 시설 게이트 · 부패 곡선 · P2P 거래 · 도구 econ 편입.
B: **상자가 개체 정체를 못 담는다**(넣었다 빼면 표준 kg) · 드래그 수정키(Shift/Ctrl) 사라짐 ·
도구 줄이 통일 목록 밖 · 비네트 색조는 첫 축만 · 유령 문턱 5초 vs `checkOrphan` 2초 · 페이로드.

---

## 3-o. ★★2026-08-30 배치 — **가속 기반 이동 모델 + 조준 모드** (병행 세션: 인벤 정비 `f1659af` **위에 리베이스**)

> 재민 확정: **좀보이드식 관성**. 설계 근거 = 일관성 원칙의 **운동 버전** —
> *몸은 순간이동으로 최고속에 도달하지 않는다.*
> 이번 배치는 **몸만** 만든다. 전투의 살은 회부(`회부_이동조준_다음층.md`).

### ★★★이동을 고치려면 `public/move-model.js` **만** 고쳐라

```
  public/move-model.js   ← 이동 모델 **정본 하나**
     ├── server/zone.js   require('../public/move-model.js')     ← 같은 파일
     └── public/client.js <script src="move-model.js">           ← 같은 파일
```

`soil-base.js` 가 세운 그 규약이다(**사본 금지**). zone.js·client.js 는 **부르기만** 한다.
콜라이더(벽·나무·지형)는 호출자의 몫 — 모듈은 위치를 모르고 `{vx, vy, dx, dy}` 만 낸다.

★**왜 이게 급소인가**: legacy 시절에도 적분이 두 벌이었다(zone.js 7409 · client.js 7310).
곱셈 **순서**가 달라 마지막 ULP 가 어긋났지만 legacy 는 속도가 입력의 함수라 상태가 없어 안 쌓였다.
**가속 모델은 속도가 상태다** — 그 어긋남이 누적되고, 리컨실리에이션이 매 틱 되돌린다 = **떨림**.
⇒ 한 함수로 못 박았다. 이제 클라·서버가 **같은 줄**을 쓴다.

### ★§0 인벤토리 — 지시서 전제와 다른 게 셋이었다

1. **"클라 60fps 예측 vs 서버 10Hz 권위"가 아니다.** 실제는 **양쪽 다 30Hz 고정 스텝**이고,
   클라가 `PRED_STEP=1/30` 마다 입력 **1개**를 보내면 서버가 그걸 **1:1 로 재생**한다
   (Gabriel Gambetta 모델 · 틱당 최대 8개 catch-up). `moveDt = 1/TICK_HZ` 고정 — 가변 dt 가 아니다.
   ⇒ "60fps 적분 vs 10Hz 적분" 이라는 갈림 자체가 **이 코드엔 없다**.
   그래도 dt 불변 검사는 넣었다 — **프레임레이트 상수가 몰래 기어드는 걸 막는 자물쇠**로서.
2. **정본 이속은 65 가 아니라 `MOVE_SPEED = 64`** (2m/s · 32px=1m). 65.1 은 B-3 실측값이었다.
3. **배율은 이미 한 곳에 모여 있었다** — `moveMultOf(p) = Carry.combinedMove(신체, 과적)`.
   흩어져 있던 건 **곱하는 자리**(서버 1곳 · 클라 1곳)였고 그 둘이 이제 모듈 한 줄이 됐다.

그리고 **④ 공격은 무방향이다** — `tryAttack` 은 범위 안 가장 가까운 mob 을 친다(무수정 계약).
**⑤ 카메라는 `_camAbs`(월드) 하나로 15곳이 물려 있다** — 안개 원점·내 스프라이트·물 흐름·컬링이
전부 그 값이다. ⇒ 시야 밀기를 **월드에 더하면** 안개가 따라 걷힌다. 그래서 **화면 변환에만** 더했다.

### 손잡이 표 (전부 env · 기본 legacy)

| 손잡이 | 기본 | 뜻 |
|---|---|---|
| `MOVE_MODEL` | `legacy` | `legacy`(즉시 최고속 · 종전) / `accel`(등가속) |
| `MOVE_ACCEL_T` | `0.20` | 0 → **현재** 최고속 도달 초 (B-1 회부: "현재"의 뜻) |
| `MOVE_DECEL_T` | `0.15` | 키 뗌 → 정지 초 (30Hz 격자에선 실측 0.1667s — B-3) |
| `MOVE_AIM_SPEED_FRAC` | `0.45` | 조준 중 이속 배율 (64 → 28.8px/s) |

★**기본값은 legacy 다.** 회귀 전원이 legacy 로 돈다. 손맛 확정 후 **튜닝 배치**에서 전환한다.
★손잡이 표는 **서버가 `welcome.moveCfg` 로 실어 보낸다** — 클라가 표를 들면 그게 두 번째 사본이다.

### ★★보정은 위치+속도 **한 쌍**이다 (재민 질문에서 확정 → 실장)

가속 모델에서 속도는 상태다. **위치만 스냅하면 스냅 직후 두 적분 곡선이 다시 벌어져
보정이 영원히 반복된다.** ⇒ 서버 tick 이 `selfVx/selfVy` 를 실어 보내고 클라가 둘 다 앵커한 뒤
미ack 입력을 replay 한다. legacy 에선 안 보낸다(상태가 없다 — 페이로드 바이트 동일).

★실장하며 걸린 것: **33ms 정지 키프레임**. legacy 는 그걸 기록도 시뮬도 안 했다(정지 입력이
위치를 안 바꾸니 무해했다). accel 에선 **서버만 한 스텝 더 감속해 곧바로 갈린다.**
⇒ 불변식대로 기록·시뮬한다. 같은 이유로 빈 틱에서 `p.vx=0` 도 accel 에선 **안 한다**(상태를 지우면 갈린다).

### 실측 — 보정 짝 비교 (같은 각본 · 실클라 키보드)

```
                최고속      미끄러짐    보정 횟수   최종 어긋남
  legacy        64.00px/s    0.00px      200회       0px
  accel         64.00px/s    5.93px      200회       0px      ← ★같다
  accel 상승곡선  456ms:32 → 486ms:42.67 → 1071ms:64   (legacy 는 첫 표본부터 64)
```
★**accel 이 legacy 보다 나빠지지 않았다 — 떨림 없음의 실증.**
(둘 다 최종 0px 인 건 이 아키텍처가 입력 1:1 재생이라 **원래** 0 이기 때문이다.
 accel 이 상태를 들고도 0 을 지켰다는 게 이 표의 뜻이다.)

### 조준 모드 — 몸만

* 우클릭 **홀드** = 조준. ⓐ 페이싱이 **커서**를 따른다(이동은 WASD 그대로 → 옆걸음·뒷걸음이 자연 발생)
  ⓑ 이속 ×0.45 ⓒ 카메라가 커서 쪽으로 **최대 180px** 밀린다(이징 τ=0.12s · 뚝 이동 없음).
* 좌클릭(조준 중) = 공격. **배선까지만** — `player.aimX/aimY` 에 싣고 `tryAttack` 판정은 **무수정**.
* ★★**안개 계약 지킴**: 오프셋은 `camX/camY`(화면 변환)에만 더한다. `_camAbs`(월드)는 불변 ⇒
  안개 원점 이동 **0.000px**(`e2e-move` 실측). **커서로 안개를 걷지 못한다.**
  단 **부채꼴 방향은 커서를 따라 돈다** — 그건 카메라가 아니라 **몸이 실제로 도는 것**이다(회부 A-3).
* 새 패널 0 · 새 키 0(마우스 버튼만).

### 하네스

* **`scripts/test-move.js`**(신규 · 38건) — 공유 모듈 **직접**. ⓪검사 상황 선행 assert(서버·클라가
  정말 이 파일을 부르는지 · index.html 이 싣는지) → ④**legacy 비트 동일**(옛 서버 식과 160/160 조합
  `Object.is`) → ①dt 불변(60↔30 0.533px · 60↔10 2.667px < 보정 임계 48px, **등속에선 안 자란다**)
  → ②곡선·관성 → ③배율이 최고속·가속 **둘 다** → ⑤델타 상한 → ⑥NaN 방어.
* **`scripts/e2e-move.js`**(신규 · 37건) — 실클라. **legacy 한 판 · accel 한 판**을 스스로 띄워 견준다.
  속도는 **모델 상태**(`__moveDbg().speed`)로 읽는다 — 화면 위치를 미분하면 렌더 보간을 재게 된다.
* 둘 다 `run-regress.sh` 목록에 넣었다(하네스를 추가하면 **거기 목록에만** 넣는다).

### 하네스가 두 번 먼저 틀렸다 (족보 (73)·(74))

* **(73)** 조준 커서를 화면 우상단(0.80, 0.30)에 놓았더니 `mousedown` 이 캔버스에 **안 왔다** —
  거기엔 `DIV.mini-label`(미니맵)이 덮여 있다. "조준이 안 켜진다"는 **제품의 결함이 아니라
  계측기가 HUD 를 누른 것**이었다. ⇒ `elementFromPoint` 로 **캔버스가 맨 위인 자리**를 확인하고 쓴다.
  ★족보 (73): **자리는 고르지 않고 잰다 — 화면 위에도 지형이 있다.**
* **(74)** "화면 오른아래 = 월드 남동"이라 적었다가 실패했다. 이 아이소 투영에선
  `dwx = 0.5·dpx + dpy · dwy = dpy − 0.5·dpx` 라 오른아래는 **거의 순수 +x** 다(실측 [1.00,−0.06]).
  ⇒ 방향 부호를 눈대중으로 적지 말고 **역변환을 하네스가 손으로 다시 세워** 견준다(+ 반대편 뒤집힘 검사).
* 그리고 남의 빚 하나: `/favicon.ico → 404` 는 **깨끗한 main 에서도 난다**(정적 라우트에 없다).
  계측기가 그걸 자기 실패로 세지 않게 걸렀다.

### ★리베이스 — 병행 인벤 정비 세션이 먼저 밀었다 (`f1659af`)

번들을 만든 뒤 `git fetch` 하니 `origin/main` 이 `4b54b88` → **`f1659af`**(3-p 정비 배치)로 움직여 있었다.
규약대로 **나중 쪽이 리베이스**했다 — 최종 커밋 `d9b0a38`, 베이스 **`f1659af`**.

충돌 4곳, 전부 **양쪽 다 살렸다**:

| 자리 | 갈림 | 결정 |
|---|---|---|
| `zone.js` welcome | 저쪽 `ledger`/`lots`/`uiCfg` vs 내 `moveCfg` | 둘 다 — 셋 다 "표는 서버가 준다" 같은 규약이다 |
| `run-regress.sh` | `test-ledger` vs `test-move` | 둘 다 |
| `다음세션_인계.md` | 둘 다 `## 3-p` | 먼저 민 쪽이 3-p, 내 절은 **3-o** 로 |
| `client.js` `predictStep` | ★아래 | ★아래 |

★**진짜 갈림은 `predictStep` 하나였다.** 저쪽이 접속 안정성으로 `netStalled` 를 `_selfGone` 옆에 세웠는데,
그 자리가 정확히 내가 **관성을 끊는** 자리다. 합친 결론: **예측이 멈추면 관성도 멈춘다.**

```js
  if (myIsDown || _selfGone || netStalled) { myVel.vx = 0; myVel.vy = 0; return; }
```
재개 첫 틱에 서버 `selfVx/selfVy` 로 다시 앵커되므로 안전하고, legacy 에선 `myVel` 이 안 쓰여 무영향이다.
같은 이유로 저쪽 `setNetStalled` 의 "쌓인 미ack 입력을 버린다" 줄에도 **관성 정지 한 짝**을 붙였다 —
거기서 입력만 버리고 속도를 남기면 재접속 순간 몸이 미끄러진다.

### 검증

```
  test-move (신규)            38/0   공유 모듈 직접 — legacy 비트 동일 160/160 · dt 불변 · 곡선 · 배율 · 델타 상한
  e2e-move  (신규)            37/0   실클라 legacy·accel 두 판 — 상승곡선 · 미끄러짐 · 보정 짝 · 조준 전항
  전체 회귀 (러너 37종)      35/37  실패 2 = **둘 다 이 배치 무관 · 베이스 커밋에서 재현**
                             ★리베이스 **뒤** 수치다 — 저쪽 신규 `test-ledger 48/0` · `e2e-inv` 도 내 트리에서 통과
  3시드 800일 기준선          다섯 수 **전부 불변** (아래)
```

**실패 2건은 베이스 커밋(`4b54b88`)을 워크트리로 꺼내 같은 기계에서 재현했다** — 족보 규약대로.

* `e2e-mtfoot` **8/9** — `⑤ ★기슭은 산보다 납작하다 (기슭 vy 중앙값 0.34 < 산 0.45 의 72%)`.
  베이스에서 **수치까지 똑같이** 8/9. 인계 1847줄의 그 항목이고 **산 아크 소관**이다.
* `e2e-waterperf` — `★★수리본이 8배 이상 빠르다` **성능 비율** 판정. 2코어에서 판마다 흔들린다:
  ```
    내 트리 : 6.4배(러너 연속) · 6.8배 · 7.9배 · **9.4배(17/0 통과)**
    베이스  : 8.5배 · 8.8배
  ```
  **내 트리도 단독 재실행에서 17/0 통과**했다(인계 2619줄이 기록한 그 습성).
  분포가 겹치므로 기계 부하 잡음으로 본다 — 다만 "단독이면 늘 통과"가 아니라
  **"흔들리는 폭이 판정선을 물고 있다"**가 더 정확한 서술이다. 그렇게 적어 둔다.

### ★3시드 800일 기준선 — 다섯 수 전부 한 자리도 안 움직였다

```
  시드   인구      소멸    무기Q   게시    못갚아미게시
  1020   6,280 ✓  0/51 ✓  821 ✓  1,647 ✓  11,194 ✓
     7   5,993 ✓  0/51 ✓  755 ✓  1,583 ✓  10,787 ✓
    42   6,072 ✓  0/51 ✓  784 ✓  1,679 ✓  10,489 ✓
```
당연하다 — 이 배치는 **플레이어 전용**이고 헤드리스 랩엔 플레이어가 없다.
그리고 그게 **NPC 이동 무수정의 심판**이다: `followNpcPath`·`sepNpcs`·전쟁 대형에 손을 댔다면
여기가 먼저 움직였을 것이다. econ·specialty 무접촉 ⇒ 번들 재생성 불필요.
(`node scripts/ev-density.js 800 <시드>` · 채택 문턱 C 행)

### 실기 확인 3줄 (재민)

```
  1) 플래그 켜기 — zone 서버를 MOVE_MODEL=accel 로 띄운다
       MOVE_MODEL=accel node server/zone.js     (기본은 legacy — 안 켜면 종전 그대로다)
  2) 관성 느끼기 — 탁 트인 땅에서 D 를 길게 누르고 **탁 놓는다**. 반 발짝(약 6px) 미끄러진다.
       그리고 달리다(Shift) 반대 키를 눌러 봐라 — 0.4초 동안 몸이 안 뒤집힌다.
  3) 조준 — **우클릭 누른 채** 마우스를 돌리며 둘러보고(카메라가 커서 쪽으로 밀린다),
       그 상태로 WASD 로 옆걸음·뒷걸음, **좌클릭으로 친다**. 놓으면 전부 제자리로 돌아온다.
```
★손잡이는 전부 env 다: `MOVE_ACCEL_T` · `MOVE_DECEL_T` · `MOVE_AIM_SPEED_FRAC`.
★**튜닝하지 않았다** — 실기 기록지를 기다린다(B-1 "무게가 시간으로는 안 느껴진다"가 첫 질문이다).

### 회부 (구현 금지)

`회부_이동조준_다음층.md` — A(지시서 이월): 전투의 살 · 각가속도/연속 페이싱 · 조준 시야 확장 ·
스태미나 · **기본값 전환** · NPC 이동. B(새로 드러난 것): **`MOVE_ACCEL_T` 의 "최고속"이 상태마다
다르다(무게가 시간으로는 안 느껴진다)** · 달리기 스풀업 부재 · 감속 시간 스텝 양자화 ·
조준 대상 표시 부재 · **우클릭이 건축 회전과 겹친다** · 커서→월드 투영 어긋남(이번에 고쳤다·사후보고) ·
**벽 밀면 속도가 쌓인다** · 관전 카메라와 겹침.

---

## 3-n. ★★2026-08-30 배치 — **캐릭터 스프라이트 파이프라인**: Blender 소체 → 시트 → 애니 상태기계

> 재민 확정: **게임엔 3D 가 아니라 스프라이트로 들어간다**(좀보이드 구빌드·디아블로 방식).
> Blender 는 **굽는 기계**일 뿐 — 런타임엔 PNG 시트와 메타 JSON 만 간다.
> 백로그의 "동물·사람 스프라이트시트" 중 **사람 절반**이다. 기본값 OFF.

### ★★★클립 하나 추가하려면 — 파이프라인 사용법

```
  1) scripts/char_render.py 의 CLIPS 에 한 줄:  ("fish_cast", 6, False, 12.0)
  2) 같은 파일에 포즈 함수 하나:               def _pose_fish_cast(u): return {본: (rx,ry,rz)}
     그리고 POSE_FN 에 등록. ★루프 클립은 u∈[0,1) 순수 삼각함수로 — 시작=끝이 자동 정합된다.
  3) blender --background --python scripts/char_render.py -- fish_cast     (~2분/클립)
  4) node scripts/test-charsheet.js      → ⑤ 해시 기록이 빨개진다. 새 시트를 확인하고 lock 갱신.
  5) 육안: python3 scripts/char-preview.py fish_cast --scale 3 --out /tmp/x.png
```
옷·도구 한 종 추가도 같은 문법이다 — `CLOTH`/`TOOL_BUILDERS` 에 한 줄, `LAYERS` 가 자동으로 문다.

★★**`char_render.py` 가 정본이다.** `assets-src/char_body.blend` 는 그 스크립트가 **써 내는 산물**이고
재민이 열어 보라고 커밋한다. 손으로 고치면 다음 렌더가 덮는다(자산 손편집 금지 캐논 동형).

### 시트 규격 계약 (`public/assets/char/char_meta.json`)

★**클라는 규격을 하나도 하드코딩하지 않는다** — 전부 메타에서 읽는다.

| 항목 | 값 | 뜻 |
|---|---|---|
| `frameW/H` | 109 × 90 | 프레임 하나(클라 좌표계). 전 클립·전 방향·**전 레이어** 공유 |
| `anchorX/Y` | 54.5 / 81.36 | **발밑**(지면 원점)의 프레임 내 픽셀. 클라가 캐릭터 좌표에 이 점을 맞춘다 |
| `dirs` / `rowOrder` | 8 / 행 0=방향 0 | `d = round(atan2(fy,fx)/45°) mod 8` · d=0 은 **월드 +x** |
| `clips` | idle4·walk8·run8·swing6(원샷)·aim2 | 프레임 수·loop·fps |
| `layers` | body · clothes_hemp · tool_axe · tool_rod | 그리는 순서 |
| `pxPerMeterH` | 32.0 | 자산 정본 축척 — 키 1.7m → **54.4px** |
| `ss` / `ppu` | 3 / 45.255 | ×3 로 굽고 **알파 가중 박스필터**로 줄여 저장(런타임 축소 없음) |

★★**공유 프레임 박스가 픽셀 정렬의 근거다.** 자연물 렌더는 개체마다 bbox 에 맞춰 자르지만
애니에 그러면 프레임마다 상자가 달라져 **스프라이트가 덜덜 떤다**. 여기선 모든 조합의 화면 bbox
합집합을 한 번 재고 그 하나의 상자·앵커로 전부 굽는다 ⇒ 런타임은 **오프셋 계산 없이 겹치기만** 한다.

### 씬 정본 — 자연물과 같은 빛·같은 자

`nature_render.py` 와 **글자 그대로 같다**: Cycles · film_transparent · ORTHO · SAMPLES 64 ·
Standard · 태양 52°/−35° energy 3.6 · 월드 (0.52,0.56,0.6)@0.55 · PPU0 45.255 · ZSQ 0.8165 ·
**좌우 FLIP**. 그림이 한 몸이어야 하므로 이 값을 캐릭터 쪽에서 바꾸지 않았다.
고증: 서민 복장 = **물들이지 않은 삼베**(화려함 금지 — 청동 위세재와 대비되는 수수함이 정체성).

### 리그 — 12본 · **강체 웨이팅**

자동 웨이트(automatic weights)를 **안 쓴다**: 54px 스프라이트에서 스킨 품질은 안 보이고,
자동 웨이트는 헤드리스에서 결과가 흔들려 **결정론 계약을 깬다**. 파트마다 정점 100% 를 본 하나에.

### 애니 상태기계 — **새 네트워크 필드 0**

입력은 전부 **이미 오는 값에서 유도**한다:
속도(나: 이동 모델 `myVel` / 남: tick 의 `vx,vy`) → idle/walk/run ·
방향(`myFacingVx/Vy` · 조준 중엔 커서 / 남: `_fvx/_fvy`) → 시트 행 ·
공격(`lastAttackAt` 에지) → swing **원샷**(끝나면 복귀) · 조준(`_aiming`) → aim.
프레임 진행은 rAF, 상태는 서버 권위 값에서 유도 — 둘을 섞지 않는다.
★이동 배치와 맞물린 한 줄: legacy 모드에서도 `myVel` 이 **지금 속도의 진실**이 되게 정지 시 0 을 쓴다
(legacy 는 상태가 없어 이동은 한 비트도 안 바뀐다).

### 손잡이 (전부 env · 기본 OFF)

`CHAR_SPRITE=on|off`(기본 off) · `CHAR_WALK_MIN` 4 · `CHAR_RUN_MIN` 102(px/s).
표는 서버가 `welcome.uiCfg` 로 실어 보낸다(정비 배치가 세운 그 규약 — 클라 상수 금지).

### ⚠서버를 두 줄 건드렸다 (보고 항목)

1. `makeEntry` 첫 가시 메타에 **`npc` 1비트**. 없으면 클라가 NPC 와 사람을 못 가르고,
   그러면 **회부된 "NPC 주민 적용"을 몰래 하는 것**이 된다. 애니 상태가 아니라 **신원**이다.
2. `uiCfg` 에 위 손잡이 셋.
NPC 렌더·econ·신체·HUD·인벤·달력은 **한 줄도** 안 건드렸다.

### 검증

```
  test-charsheet (신규)      19/0   시트↔메타 · 레이어 같은 박스 · 옷 화소 86.8% 가 몸 위(정렬 실증) ·
                                     알파 위생(검은 프린지 0.0%) · 해시 기록(재렌더 diff 0 강제)
  e2e-charsprite (신규)      34/0   실클라 **두 클라 짝**
  전체 회귀 (러너 40종)      37/40  실패 3 — ★세 건 다 이 배치 무관(아래 이분법)
  3시드 800일 기준선          다섯 수 전부 불변 (6280/5993/6072 · 0/51 · 821/755/784 ·
                                                1647/1583/1679 · 11194/10787/10489)
```

**★가림(occlusion) 실측** — 화가 순서 합성 vs **깊이 정본**(몸+옷+도끼를 한 번에 구운 대조군):
```
  d0 7.1 · d1 6.4 · d2 7.4 · d3 10.6 · d4 7.6 · d5 6.9 · d6 7.5 · d7 10.9   (전체 7.78%)
```
지금은 쓸 만하다(54px 에서 도끼 자루 몇 화소). 방향별 z 순서·홀드아웃은 회부 B-1.

**성능** — 라이브 rAF 짝 비교(같은 화면·같은 순간에 플래그만 뒤집어):
```
  프레임 간격 중앙값  ON 31.8ms · OFF 32.1ms   (p90 38.5 / 38.8)   ⇒ ON−OFF **−0.3ms** = 잡음 이하
```
(레이어 2장 × 2명 · 헤드리스 SwiftShader. 마을 규모 N 명 비용은 NPC 적용 배치의 몫 — 회부 A-1.)

### ⚠실패 3건 — 전부 이 배치 무관 (워크트리 이분법으로 못 박았다)

* `e2e-mtfoot` **8/9** — 산 아크 소관(인계 1847줄). 베이스에서 수치까지 동일.
* `e2e-waterperf` — 2코어 성능 비율 잡음(인계에 기록된 습성).
* ★★**`e2e-emptystart` ⑤ 16/18 — 병행 세션의 `f7f9c2f`(접속 진단)가 넣은 회귀다.**
  ```
    f1659af (접속 진단 이전)  → 18/18 PASS
    f7f9c2f (접속 진단)       → 16/18 · ✗⑤ "마을 장인에게서 정품 도구를 살 수 있다" 0→0
    f7f9c2f + 이 배치         → 16/18 (같은 두 줄)
  ```
  ⇒ **그 배치 소관이다.** 빈손 캐논의 "둘째 단"(구매 경로)이 닫혔다는 뜻이라 가볍지 않다 — 다음 세션이 가져가라.

### 실기 확인 3줄 (재민)

```
  1) 플래그 켜기 —  CHAR_SPRITE=on node server/zone.js      (기본 off · 안 켜면 종전 도형 그대로)
  2) 걷다가 **Shift 로 달려라** — walk(10fps) → run(14fps) 로 클립이 갈린다.
     방향을 바꾸면 8행이 돈다. 멈추면 0.4초 안에 idle.
  3) 도끼를 들고(인벤에서 장착) **우클릭 홀드로 조준**하다 **좌클릭** — swing 한 번 치고 되돌아온다.
```
★손잡이는 전부 env: `CHAR_SPRITE` · `CHAR_WALK_MIN`(4) · `CHAR_RUN_MIN`(102).
★**튜닝하지 않았다** — fps·문턱·조형 전부 실기 기록지 대기(회부 B-7·B-8).

### 회부 (구현 금지)

`회부_캐릭시트_다음층.md` — A(이월): NPC 주민 적용 · 16방향/연속 페이싱 · 동물 시트 ·
추가 클립 목록 · 옷 확장 · 피격/사망 · 기본값 전환.
B(새로 드러남): ★**가림(occlusion) 실측 7.78%/최악 10.9%** — 화가 순서는 깊이를 모른다 ·
**남의 착장·조준이 네트워크에 없다** · 프레임 박스가 가장 긴 도구에 묶인다 ·
다운/전쟁/포로는 여전히 도형 경로(한 화면에 섞인다) · 그림자가 도형 타원 ·
swing 이 도구·무기 겸용 1종 · 찍은 fps·문턱 · **.blend 정본 여부 판정**.

---

## 3-산. ★2026-08-25 **산 아크 종결** (재민 확정 3건 반영 완료)

산은 여기서 닫는다. 아래가 **확정 구조**이고, "하지 말 것"은 다시 열지 마라 —
전부 실측으로 닫힌 것들이라 다시 파면 같은 함정을 다시 밟는다.

### 확정 구조 (전부 라이브 기본값)

| 층 | 확정 | 근거 |
|---|---|---|
| 표면 | **WebGL 높이장 메쉬** (`_mt3GlBand`). 캔버스 2D 폴리곤은 폴백 | 격자 3층(세분 그물·셀 주기·등고선 계단) 수리 |
| 정점 | 위치(2) + 셀좌표(2) + **모드**(1) = stride 20 | 벽/바닥/표면을 한 버퍼로 |
| 모드 축 | 0=표면 · 1~4=벽 법선(±x/±y) · 5=갱 바닥 | 갱 기하 |
| 마스크 | **이원화** `MT3_DUAL=1` — 높이·렌더는 **원본** 마스크, 통행·부수기는 현재 마스크 | 1셀 파면 35m 협곡(안 그러면 3.5m 도랑) |
| 거리장 | **`MT3_PAD=24` · `MT3_DCAP=24`** | 청크마다 dE 를 자기 창에서만 풀어 이음매가 벌어지던 **갈색 리본**을 닫았다(지면색 화소 28776 → 0) |
| 높이 | `MT3_HMAX=35` · `MT3_LAM=12` → 실측 옆벽 **35.2m** | DCAP 반영 후 값. 37.5m 는 창 잘림이 부풀린 값이었다 |
| 텍스처 | `MT3_HTEX=64` — **`N = CH + 2·PAD ≤ 64` 이므로 PAD 24 가 상한** | 더 키우려면 텍스처도 같이 키워야 한다(안 그러면 셰이딩이 조용히 깨진다) |
| 가림 투명화 | 그룹 알파 한 겹 · **`MT_OCC_ZB=500`** | 편향 재스윙: 통로 안에선 값 차이가 무의미하고, 낮추면 **뒤에 있는 산까지 흐려진다**(대조군 발동률 5.2% → 32.2%) |
| 오버드로 | **띠 병합 `MT3_MERGE=1`** — 자락도 통로도 없는 청크만. 앵커 = **가장 작은 k** | 띠 877 → 127 · 프레임 51.00 → **27.86ms (−45.4%, 유의)** · 켬/끔 화소 차이 **1**(바닥) |
| 자락 | **`MT3_FRINGE=1`** (경계선 잡음). 높이의 함수로 게이트 → 이음매 없음, 마루 무접촉 | judge-grid 0.78/1.03 (현행 0.92/1.35보다도 깨끗). 2안(알파 페더)은 세분6 봉우리 8.12 로 **기각** |
| 안개 계약 | 산은 `kind:'mtseg'` 로 renderables · **마스크 뒤** · `_GATE_FREE` 에 **명시** 면제 | 게이트를 걸면 가시성 폴리곤이 바위에서 끊겨 **모든 산**이 사라진다 |

### 판정 도구 (전부 정본 훅을 부른다 — 사본 금지)

* 회귀: `e2e-mountain`(29) · **`e2e-mtcorridor`(15, 신설)** · `e2e-mtocc`(13)
* 프로파일/시안: `prof-mt3merge`(8) · `shot-mtfringe` · `sweep-mtzocc` · `shot-fixedcam`
* 프로브: `probe-mergeorder` · `probe-fogcolumn` · `probe-mtpad` · `measure-mtscale`
* 시각 판정기: `judge-grid.py`(전 대역 격자) · `judge-orient.py`(기울기 히스토그램)
* 손잡이(전부 반례 장치로 **유지**): `__mt3merge` · `__mt3mergez` · `__mt3fringe` · `__mtZOcc` ·
  `__mt3skirt` · `__mt3pad` · `__mt3dcap` · `__mt3view` · `__mt3htop` · `__mt3mpad` · `__mt3gl` ·
  `__mt3trees` · `__mtDual` · `__mt3MeshAt` · `__mtWhoCovers` · `__mtPaintAt` · `__mtStage`

### 아크 밖으로 넘긴 것 2건 (원인까지 특정해 뒀다)

1. **서버 바위 파괴 이벤트 (§A-6)** — 없다. `__mtDestroy` 는 **렌더 층만** 연다
   (`server/terrain.js` "산은 완벽한 콜라이더다" · 통행은 `isTerrainBlockedLocal` 하나로 모인다).
   그래서 "파낸 통로를 걸어서 통과"가 **오늘 규격에서 불가능**하다.
   `e2e-mtcorridor` ⓓ 가 그 **부재를 판정으로 고정**하고 있다 — 서버 이벤트가 생기면 그 판정이
   뒤집혀 알려 준다. 만들 때 `_mtDestroyed` 를 서버 이벤트로 채우고 ⓓ 를 뒤집어라.
2. **나무 시야 그림자가 '미탐사'로 남는다** — 지면 베이크가 아니라 **안개/가시성** 계약이다
   (검은 화소는 순흑 0,0,0 이고 `_seenChunks` 에 없다 · 두 칸 걸으면 2090 → 0).
   추천은 ⓒ안: 가까운 반경(`CLOSE_RADIUS` 128px) 안에서는 오클루더를 무시하고 '본 셀'로 기록.
   재민 미결정. 안개 아크에서 다뤄라.

### ★하지 말 것 (전부 실측으로 닫힌 길이다)

* **격자·리본 계통 재수리** — 세분 그물 7.98→2.85 · 등고선 3.54→0.66 · 리본 28776→0 화소.
  다시 열려면 `judge-grid` 전 대역 + 기울기 히스토그램 + 5배 확대 **셋 다** 통과시켜라.
* **치마(skirt) 실장** — 기각됐다. 시야 안 6963셀의 카메라 쪽 변 13926개 중 비바위와 맞닿은 건
  **139개(1%)** 뿐이라 깎아 낼 테두리가 없다. 코드는 `__mt3skirt` 반례 장치로 남아 있다(기본 0).
* **`MT3_PAD > 24`** — `MT3_HTEX=64` 를 같이 안 키우면 높이 텍스처를 넘겨 **셰이딩이 조용히 깨진다**.
* **깊이버퍼 단일 패스** — 화가 순서 계약(mtseg 가 엔티티와 같은 z 축에서 정렬)을 깬다.
* **`isWaterCellLocal`/`isRockCellLocal` 을 느리다고 고치기** — 덜 부르는 쪽으로 풀어라(캐시 2종).

### 이 아크가 남긴 계측 교훈 (같은 함정을 또 밟지 마라)

계측기 오진이 **10건** 나왔고 전부 이쪽 것이었다. 반복된 족(族)이 셋이다:

1. **창 밖을 INF 로 보는 거리장** — `_mt3Field` · `pickSite` · `measure-mtscale` · `probe-mtpad` ·
   `measure-digwall` · `mt3d-engine`. 창 밖은 **땅(0)** 으로 봐라(깊이의 하한).
2. **고정 sleep 으로 굽기를 기다림** — 청크 예산이 프레임당 1개라 같은 트리에서 |Δ| 34.94 와 1.78 이
   번갈아 나온다. **연속 두 장이 같아질 때까지** 기다려라(`settleShot` / `settle`).
3. **재현 바닥을 안 재고 문턱을 상수로 박음** — HUD(핑·시각)가 프레임마다 바뀌어 바닥이 1788화소로
   부풀면 **실패가 통과로 읽힌다**. HUD 를 빼고, 문턱은 **그때 잰 바닥**과 비교해라.

그리고 이 아크에서 두 번, **추정을 그대로 원인으로 쓴 게 틀렸다**(치마 · 병합 돌출목).
원인은 **손잡이 하나를 뒤집어** 갈라라 — 그게 안 되면 아직 원인을 모르는 것이다.

---

## 3-산-ⓐ. ★2026-08-26 **픽셀 절단 v4 — 수직 평면** (재민 확정 · 흐림 명세 완결)

### 왜 v3(화면 평행 평면)에서 바꿨나

v3 는 **화면과 평행한 평면**(깊이 `s + 32h`)으로 잘랐다. 기하적으로는 정확히 작동했지만,
카메라가 비스듬히 내려다보므로 그 평면은 **캐릭터 머리 위로 뒤로 기울어져** 있다. 그래서
발자국이 내 뒤인 산이라도 충분히 높으면 봉우리가 그 평면을 뚫고 나와 "앞"으로 분류됐다 —
**나를 절대 못 가리는 산이 흐려졌다**(실측: 그런 띠가 v3 에서 **432장**).

**나를 가릴 수 있는 산은 정확히 무엇인가** — 한 줄로 증명된다:
산 조각이 내 스프라이트 화소에 오려면 `화면y = s − 32h` 가 내 화면y 와 같아야 하는데,
발자국이 내 뒤(`s < 내 s`)면 `h < 0` 이어야 해서 **불가능하다**.
⇒ 그 집합을 정확히 가르는 면은 **캐릭터 행을 지나는 수직 평면**(`s = 내 s`)이다.
두 평면 다 캐릭터 점을 지나는 유일한 평면이고 **지면에서는 같은 가로줄**을 지난다 — 차이는 산 위에서다.

**덤**: 화가 알고리즘의 그리기 순서도 지면 `s` 기준이라, 수직 평면으로 바꾸면 v3 가 남긴
"명세는 깊이인데 순서는 지면 z"라는 어긋남이 **소멸한다**. 흐림과 순서가 같은 자를 쓴다.

★교훈: **직관 둘("화면 평행 평면으로 자른다" · "날 못 가리는 건 불투명")이 충돌하면 진짜 의도부터 정하라.**
이 카메라에서 둘은 다른 답을 낸다.

### 경계식

```
화면 y = s − 32h                    (그리는 식)
⇒ 지면 깊이 s = 화면y + 32h
v4(채택) 판정 s > c   ·  경계 = **화면y = c − 32h**   ← 지형 따라 올라가는 등고선
v3(반례)  판정 d > c  ·  d = 화면y + 64h · 경계 = 화면y = c − 64h
```

h=0 에서는 **곧은 가로줄**(두 안이 여기선 같다). 문턱 `c = 내 지면 z + 32`(`MT_FADE_ZOFF`, 채택값 유지).

### 확정 구조 (전부 라이브 기본값)

| 층 | 확정 | 근거 |
|---|---|---|
| 정점 | 위치(2)+셀좌표(2)+모드(1)+**높이 hh**(1) = **stride 24** | `p.y = s−32h` 라 `(화면y + 32·vH)` 가 보간 s 와 **항등** |
| 절단면 | **`MT_FADE_PLANE=0`** 수직(s) · 1 = 화면 평행(옛 v3, 반례 장치) | 뒤 산 흐림 **432 → 0** · 덮을 수 있는 띠 **2793/2793 유지** |
| 셰이더 | 계수 `uCutK` — **32**(v4) · 64(v3) | 바뀐 건 **기준 함수 하나**뿐, 절단 기계는 그대로 |
| 분류 | 띠의 `[sLo,sHi]` 로 3분류 — 전부앞/전부뒤/**걸친 띠만** 화소 판정 | 흐림 겹은 "불투명으로 모아 한 번 알파" 그대로 |
| 가름 | `uCutSide=2` **가리개 한 판**(알파 0/1) + 캔버스 합성(destination-in/out) | 이진이라 **배타·빈틈없음이 정의상** 성립 |
| 경계 행 | v4 `[c − 32·hMax, c]`(**위=불투명·아래=흐림**) · v3 `[2sLo−c, 2sHi−c]`(**반대**) | 방향이 반대라 `topSide` 를 같이 낸다 — 헷갈리기 쉬운 자리 |
| 전송 | 가리개는 **readPixels + putImageData** | GL→2D `drawImage` 는 **표면 전체**를 스냅샷한다(1152×1280 → 호출당 15.3ms) |
| 캐시 | 경계 띠를 세그먼트에 담고 열쇠는 **문턱만**(카메라 무관, pb 기준) | 화면으로 자르면 걸을 때마다 열쇠가 깨진다 |
| 문턱 눈금 | **`MT_FADE_CQ=8`** | v4 이동 중 0→902.5±93.7 · **8→41.3±4.0** · 24→37.2±2.2 (끔 정지 16.92) |

### 판정 — `e2e-mtcut` **25/25** (전부 정본 훅·정본 함수)

* **ⓕ v4 의 핵심** — 발자국이 내 뒤인 띠는 **0장** 흐려진다 · 반례로 옛 v3 에서는 **432장** ·
  나를 덮을 수 있는 띠는 **2793/2793 전부** 흐림(기능 손실 없음).
* **ⓐ 경계식** — 셰이더가 낸 셀 좌표로 **정본 `F.corS`** 를 그 화소의 매개변수에서 다시 불러
  `s = 화면y + 32h` 를 **독립으로** 세우고 실제 두 판과 맞춘다. `|s−c|>1px` 어긋남 **0**.
  계수는 경계 화소를 높이 칸으로 나눈 **중앙값**으로 본다(OLS 는 꼬리에 끌린다):
  **K=32 에서 7칸(폭 26m) 전부 |중앙값| ≤ 0.22px**, 반례 K=64 는 크게 어긋난다.
* **ⓑ 지면 구간** — 문턱을 14자리로 훑어 모은다(띠 하나 안에서는 자락 깊이가 거의 한 값이라
  경계가 보이는 건 문턱에 걸린 띠 한둘뿐이다). 공차 밖 지면 화소는 **예외 없이** 제 행 쪽.
* **ⓒ** 겹침 0 · 빠짐 0 · 지름길 = 전 화소 가리개(0화소 차) · 흐림 겹 합성 1회/프레임 ·
  화면색이 '한 번 섞은 식'과 일치. **ⓒ2c 는 재현 바닥을 같이 잰다**(같은 걸 두 번 그려 비교) —
  첫 호출이 나무 가장자리 알파에서 11화소 어긋나는 **워밍업**이 있어 바닥 없이는 오독한다.
* **ⓓ** 3분류 · **ⓔ** 절단 끔 반례 장치.

### 정확도 버그 3건 (v3 라운드에서 잡았고 v4 에도 그대로 산다)

1. **GL 뷰포트를 캔버스 위(0, H−bh)에 뒀던 것** — 공유 캔버스가 커지면 원점이 바뀌고 창 좌표에
   큰 상수가 더해져 float32 하위 비트가 흔들려 **변 위 화소가 덮임을 뒤집었다**(12화소).
   ⇒ 뷰포트 **(0,0) 고정**.
2. **문턱 뺄셈을 셰이더에서** — `y0` 가 29만대라 ULP 0.031px 이고 두 판이 다르게 반올림해
   문턱 근처 조각이 **양쪽에서** 잘렸다. ⇒ `uCutBase` 에 문턱을 **CPU 배정도로** 미리 뺀다.
3. **`discard` 로 가르던 것** — 잘린 자리에 **밑에 깔린 조각**이 드러나 화가 순서가 뒤집혔다(70화소).
   ⇒ 블렌딩이 꺼져 있으니 **투명을 써넣는다**(맨 위 조각이 결정).

### 손잡이

`__mtFadeCut` · `__mtFadePlane`(0 수직 · 1 화면 평행) · `__mtFadeCQ` · `__mtCutN` ·
`__mtCutList` · `__mtCutProbe` · `__mtCutProf` · `__mtFadeClip`(가로줄 반쪽, 기본 끔)

### ★하지 말 것 (추가)

* **걸친 띠를 통째로 되그리기** — 띠마다 두 판을 음영까지 다시 그리면 이동 중 33 → **1025ms**.
  가리개 한 판 + 경계 구간만 갈라라.
* **GL 캔버스를 2D 로 `drawImage` 해서 작은 조각 옮기기** — 표면 전체를 스냅샷한다.
  작은 사각형은 `readPixels` + `putImageData`.
* **가리개를 화면 기준으로 자르고 캐시하기** — 카메라가 열쇠에 들어가면 매 프레임 깨진다.
* **여러 프레임의 rect 를 마지막 문턱 하나로 재기** — 걷는 동안 분류가 어긋난다(429 중 99만 split).
  rect 마다 **그 프레임의 fz** 를 같이 적어라.

### ★계측기 두 건 — 둘 다 "**가라앉기 전에 쟀다**" 계열이었다 (2026-08-26)

픽셀 절단을 켜자 **다른 아크의 하네스 둘**이 깨졌다. 둘 다 절단의 결함이 아니라
**하네스가 프레임 예산을 안 기다린 것**이었다. 갈라내는 방법은 같다 —
손잡이로 켬/끔 A/B → 곡선(시간에 따라 값이 자라는가) → 화소 상자.

**① `e2e-mtfuzz` P6 결정론 — 수리했다 (8/8 → 10/10)**

* 증상: 절단 켜면 `|Δ| 2.691`, 끄면 `0.000`. 재현된다(같은 값 3회).
* 원인: 청크 굽기 예산은 **프레임당 1장**이다(`MT3_BUDGET=1`). 부수기 직후엔 아직 안 구운
  청크가 남는다(실측 켬 **4장** · 끔 **6장**). 옛 P6 은 그 **반쯤 구운 첫 장**과 900ms 뒤
  다 구운 장을 비교했다 — 재던 게 결정론이 아니라 **"가라앉았나"** 였다.
  · 다른 화소 **183,582** · 상자 `0,260 ~ 482,859`
  · **첫 장 하나만** 튀고 그 뒤 프레임끼리는 16장 연속 전부 `0.000`
* ★**끄면 통과하던 건 고쳐져서가 아니라 경주에서 이겨서**다 — 끈 쪽 굽기 잔량이 오히려
  더 많았다(6 > 4). 옛 P6 은 **요행으로** 통과 중이었다.
* 수리(엄격도 0.05 그대로 — 완화 아님): `settle()` 로 굽기 잔량이 연속 3회 그대로일 때까지
  기다린 뒤 잰다. 자명 통과를 막으려고 둘을 붙였다 —
  · `P6b` 가라앉히기가 실제로 일을 했다(잔량 > 0). 0 이 되면 이 수리가 무의미해진 것이니 다시 봐라.
  · `P6c` **반례** — 청크 캐시만 비워(`__mt3pad` 에 현재값 재주입) 같은 상태를 다시 만들고
    가라앉기 전에 재면 `|Δ| 27.599`(문턱의 **550배**). 계측기는 여전히 날카롭다.

**② `e2e-tilestate` ⓖ `dOff > 4` — 내 것이 아니다. 하네스가 흔들린다 (이관)**

* 처음엔 내 것으로 보였다: 절단 켬 `2.48`(실패) · 끔 `7.57`(통과).
* 그러나 **같은 설정(절단 켬)으로 다시 돌리니 `6.95` 로 통과**했다(39/0). 흔들린다.
* 직접 반례: ⓖ 시점에 `__mtFadeCut` 을 켰다 껐다 하며 짝을 재면
  **두 경우 다 `|Δ| 2.48`**, 절단이 바꾼 속살 화소 **0개(0.0%)**.
  ⇒ 절단은 이 판정에 **닿지 않는다**.
* 진짜 원인 후보: ⓖ 의 `dOff` 는 앞에서 **동적으로 고른 시험 상자**(`cands[0]`)의
  상태 세기에 그대로 올라탄다. 약한 자리가 뽑히면 4를 못 넘는다.
  (굽기 지연은 **아니다** — 끈 뒤 0.4초 간격 14장이 전부 `2.48` 로 평평했다.)
* ⇒ 타일 상태계 아크에서 **ⓖ 가 제 시험 자리를 직접 만들도록** 고쳐라. 문턱을 낮추지 마라.

### 그림 — `scripts/shot-mtcutline.js` (v3↔v4 짝)

옛 판은 경계가 **화면 밖**인 구도를 내보냈다("왜 경계선이 대각선이야?" 는 산 제 모서리였다).
⇒ 이제 **자리를 고르지 않고 잰다**: 걸음마다 v3/v4 를 둘 다 찍어 다른 화소를 세고 최대인 자리를 쓴다.
표시색은 **하나도 쓰지 않는다** — 네 칸 전부 실제 화면 화소다. HUD 는 차이 상자에서 뺀다.

실측(`2150,1959` · 8걸음): 다른 화소 **42,913** · 상자 `522,140 ~ 1019,494`

| | v3(평행 평면) | v4(수직 평면) |
|---|---|---|
| 흐린 띠 | 87장 | **77장** |
| 걸친 띠 | 13장 | **5장** |

차 **10장** = 내 뒤라 나를 못 가리는 산. 걸친 띠 13 → 5 는 명세 예측 그대로다(띠 안 `s` 퍼짐이 작다).

★**경계가 비스듬한 건 맞다.** v4 경계는 `화면y = c − 32h` 라 **등고선**이다 —
평지에서만 가로줄이고 사면에서는 높이만큼 위로 들린다.

`probe-mtcover` 는 **다시 만들지 않았다.** 그 계측(못 덮는데 흐려짐)은 `e2e-mtcut` ⓕ1/ⓕ2/ⓕ3 에서
이미 **판정**으로 서 있다. 같은 걸 재는 탐침을 따로 두면 계측기 사본이 된다.

**③ `e2e-mtcut` ⓒ5b — 판정 상자를 **걸친 띠**에서 잡고 있었다 (수리 · 25 → 26종)**

* 증상: 회귀에서 `한 번 섞기 23.35 vs 두 번 32.36`(비 1.39). 문턱은 `e1 < 8` 이고 `e2 > e1 × 2.5`.
* 원인: 상자를 `pr`(= `_mtSplitSegs`, **걸친 띠만**)에서 잡았다. 걸친 띠의 사각형은
  위가 불투명(α=1)·아래가 흐림(α=0.34)이라 **"한 번 섞기" 모형이 애초에 성립하지 않는다.**
  옛 판이 통과하던 건 상자(사각형 아래 55~98%)가 우연히 흐림 구간에 다 들어갔을 때뿐 —
  **기하 제비뽑기**였다.
* 수리: 상자를 **통째로 흐려진 띠**(`faded && !split`)에서 잡는다. 그룹 알파가 말하는 대상이 그것이다.
  `__mt3RectsGet()` 의 정본 기록을 그대로 쓴다(사본 금지). 문턱은 그대로.
  · 고친 뒤 한 번 **1.06** vs 두 번 **7.56**(비 7.1) · ⓒ5a 자명 금지 추가(통째 흐림 띠 2880장).

★**이번 라운드에 계측기가 셋 먼저 틀렸다** — mtfuzz P6 · mtcut ⓒ5b · tilestate ⓖ.
셋 다 모양이 같다: **우연히 통과하고 있었다.** 절단을 켜자 우연이 깨졌을 뿐이다.
계측기가 "왜 지금까지 통과했는가"를 설명 못 하면 그 통과는 증거가 아니다.

### ★`e2e-waterperf` ⓔ `8배 이상` — 내 것이 아니다. **문턱이 잡음 바닥에 있다** (이관)

회귀에서 사슬 7.2배 · 솔로 7.9배로 빨간 줄이 났다. 짝 프로파일(각 4회, 순서 교대·포트 정리):

| | 배율 | 대조 최악(ms) | 수리본 최악(ms) |
|---|---|---|---|
| 절단 **켬** | 7.9 · 9.9 · 5.5 · 9.1 → **8.10 ± 1.92** (SE 0.96) | 235 · 234 · 197 · 217 → 220.8 | 30 · 24 · 36 · 24 → 28.5 ± 5.7 |
| 절단 **끔** | 11.3 · 9.1 · 9.7 · 8.7 → **9.70 ± 1.14** (SE 0.57) | 309 · 311 · 240 · 210 → 267.5 | 27 · 34 · 25 · 24 → 27.5 ± 4.5 |

* 배율 차 **−1.60**, 2σ = **2.23** → **잡음**(유의 아님).
* 내 변경이 닿을 수 있는 쪽(**수리본**) 차 **+1.0ms**, 2σ = **7.30** → 잡음.
* 흔들리는 건 **대조군의 절대 최악값**이다 — 197~311ms(±25%)로 뛴다. 배율은 그 수로 나눈 값이라
  문턱 8 이 그 잡음 폭 **안에** 앉아 있다. 절단 켬 4회 중 둘은 통과, 둘은 실패했다.
* ⇒ 판정 자체가 **한 번의 최악값**으로 서 있는 게 문제다. **문턱을 낮추지 마라.**
  물가 렉 아크에서 탐침을 **≥5회 반복해 중앙값 배율**로 재도록 고쳐라 — 8배 기준은 그대로 두고.
