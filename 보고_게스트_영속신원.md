# 보고 — 게스트 **영속 신원**: 소유가 접속을 넘어 살아남는다

> 2026-08-03f 배치 13. 기준 `7372c6e`. 재민 확정: **"네 추천대로 하자"** (배치 12 회부 2).

---

## 0. ★재민 결정 사항 — 라이브에 나갈 때 기존 게스트 건물은 어떻게 되나

**먼저 오해를 하나 걷어낸다: 이 변경이 무언가를 고아로 만드는 게 아니다.**
라이브에 이미 있는 `anon_xxxxxxxx` 소유 건물·사유지는 **그 세션이 끊긴 순간 이미 고아였다** —
그 id 는 아무도 다시 가질 수 없는 1회용이었기 때문이다. 이 배치는 **앞으로 생길 고아를 없앨 뿐**,
과거의 고아를 되살리지도 새로 만들지도 않는다.

그래서 결정할 것은 **"이미 있는 고아를 어떻게 할 것인가"** 하나다. 셋 중 재민이 고른다:

| | 안 | 결과 | 대가 |
|---|---|---|---|
| **가** | **그냥 배포**(DB 손 안 댐) | 새 게스트부터 신원이 이어진다. 새 파손 없음 | 옛 고아 구조물이 **영구 잔존**한다. 특히 **고아 사유지**가 문제다 — 해제(`tryUnclaim`)는 소유자만 되므로 **그 땅은 영영 아무도 못 쓴다**(땅이 서서히 썩는다) |
| **나** | **라이브 DB 리셋과 함께 배포** | 가장 깨끗하다. 고아 0 · 모두가 첫날부터 영속 신원 | 플레이어 건축물·사유지가 전부 사라진다(등록 계정 것도). 재민만 실행 가능 |
| **다** | 배포 + **고아 일소 1회 스크립트** | 등록 계정 것은 살리고 `anon_*` 소유 행만 지운다 | 스크립트를 새로 만들어야 하고, 이번 배치엔 없다(요청이 없었다) |

**내 추천: (나) — 리셋과 같이 내보내는 게 깔끔하다.** 이유 —
① 지금 라이브에 남아 있을 게스트 자산은 **어차피 주인이 없다**(고아). 지켜 줄 사람이 없다.
② 고아 사유지는 시간이 갈수록 **못 쓰는 땅**을 늘린다 — 마을 건립이 들어온 지금은 특히 아프다
   (건립은 남의 영토를 피해야 하는데, 그 '남'이 존재하지 않는 유령이다).
③ 마을 건립·식량 유입·곳간 수리가 배치 11~12 에 연달아 들어갔다. **새 세계로 시작하는 편이
   지표도 깨끗하다.**
⚠단 **(나)는 등록 계정의 건축물도 지운다.** 그게 아까우면 (다)를 만들어 달라고 하면 된다 —
반나절 일이고, 이번 배치에 넣지 않은 이유는 요청 범위 밖이기 때문이다.

**실행은 재민.** `DB리셋_절차.md` 대로. (참고 실측: 로컬 `world-hanbando.db` 에는 지금
`anon_*` 소유 행이 **0건**이다 — 건물 5,350행은 전부 NPC 마을(`npc_*`), 3행이 이름 계정.
라이브 수치는 재민만 볼 수 있다.)

---

## 1. 무엇이 문제였나 — 한 줄

이 세계의 소유 판정은 **전부 playerId 대조**다(사유지 `ownerPid` · 건물 `ownerId` ·
노·숯가마·회관 `data.owner` · 마을 `founder`). 그런데 게스트의 playerId 는 **접속마다 새로**
발급되는 1회용이었다(`anon_${Math.random()...}`). 그래서 게스트는 잠깐 끊겼다 붙는 것만으로
**제가 지은 것의 주인이 아니게 됐다.** 마을 건립이 들어온 지금 그건 불편이 아니라
**마을을 통째로 잃는 구멍**이다.

## 2. 무엇을 했나 — 토큰 하나

**계정 체계를 만들지 않았다.** 비밀번호도 이메일도 없다. 바뀐 것은 **접미사가 접속을 넘어
같아진다**는 것 하나다.

```
브라우저(localStorage: durango_guest_token)
   └─ ws://…?guest_token=<64자 hex>  ─▶ zone
                                        └─ POST /guest {token} ─▶ central
                                                                   └─ players.guest_token 대조
                                        ◀─ { player_id: anon_<고정> , token }
   ◀─ welcome { playerId, guestToken }        (토큰은 이 한 곳으로만 흐른다)
```

### 설계 판단 셋 — 왜 이렇게 했나

1. **`anon_` 접두사를 유지한다.** 코드 전역 **28곳**이 그 접두사로 "등록 계정이 아니다"를
   판정한다(길드 생성·거래소·`savePlayer` 등). 접두사를 바꾸면 그 28가지 정책이 **한꺼번에**
   뒤집힌다. 이 배치는 신원의 영속화지 정책 변경이 아니다.
2. **저장은 central 의 `players` 에** — 새 테이블을 만들지 않았다(배치 지시: 최후 수단).
   기존 마이그레이션 관용(`PRAGMA table_info` → `ALTER TABLE`, `central.js:163~`)을 그대로 따라
   `guest_token` 칼럼 하나와 부분 유니크 인덱스를 더했다. zone 로컬 DB 는 **존별**이라
   존을 넘으면 신원이 끊긴다 — 그래서 후보가 아니었다.
3. **`savePlayer` 는 안 건드렸다.** 게스트의 인벤·좌표는 여전히 저장되지 않는다.
   이 배치의 구멍은 **소유**이고, 소유는 zone DB(사유지·건물 행)에 이미 영속돼 있었다 —
   끊긴 건 그걸 가리키는 **이름**뿐이었다. 인벤 영속은 별개 판단이라 손대지 않는다(§7).

### 보안 규약 — 토큰 유출 = 계정 탈취

* **토큰을 로그·알림·채팅·화면에 절대 찍지 않는다.** central 발급 로그는 `playerId` 만 남긴다.
  하네스가 **서버 로그 전문(30,394자)을 모아 토큰 문자열이 한 번이라도 나오는지** 값으로 검사한다.
* **비밀번호가 생긴 행은 게스트 토큰으로 못 연다.** 게스트가 나중에 등록 계정이 되면
  그 토큰은 죽는다 — 아니면 옛 토큰이 계정 열쇠가 된다. 하네스가 DB 를 직접 조작해 검증한다.
* 토큰은 **암호학적 난수 32바이트**(`crypto.randomBytes`). 핸드오프 토큰(`Math.random`, 5초 일회용)과
  달리 **영구 신원**이라 추측 가능해선 안 된다.
* 형식 위반·없는 토큰은 **에러가 아니라 새 신원**이다 — 입장을 막지 않는다.

### 클라가 제 신원을 알게 됐다 (부수 수리)

`welcome` 은 종전에 **세션 손잡이 `pid`**(p1·p2…)만 줬고 영속 신원은 안 줬다. 그래서 클라의
사유지 목록은 `myUsername`(로그인 입력값)과 대조하고 있었는데, **게스트는 그게 빈 문자열**이라
**제 사유지도 남의 것으로 보였다.** 등록 계정은 `player_id === username` 이라 우연히 맞았을 뿐이다.
⇒ `welcome.playerId` 를 실어 보내고 클라가 그걸로 대조한다(`myPlayerId`).

---

## 3. 소유 판정 전수 — **고칠 것이 없어야 정상이다**

토큰으로 playerId 가 고정되면 소유 판정 자체는 손댈 게 없다. 그걸 **주장이 아니라 검사로** 못박았다
(“말만 막히나”의 역방향 — 전수로 확인하고 끝낸다):

| 갈래 | 판정 | 어떻게 확인했나 |
|---|---|---|
| 사유지(해제) | `c.ownerPid !== player.playerId` | 소스 + **실서버에서 남이 해제 시도 → 거부** |
| 사유지(발자국 권리) | `c.ownerPid === player.playerId` | 소스 + 재접속 후 4칸 유지 실측 |
| 노 · 숯가마 · 회관 | `_furnaceCanUse` (`d.owner === player.playerId`) | 소스 + **재접속 후 노 조업 통과 · 회관 재고 열림** |
| 움집터 | `b.data.owner !== player.playerId` | 소스 + **재접속 후 시공 통과 · 남은 거부** |
| 마을 창설자 | `founder: player.playerId` | 소스 + 게스트가 세운 마을이 재접속 후에도 제 것 |
| 길드 | `player.tribeId === d.tribeId` | 소스(게스트는 길드 정책상 대상 아님 — 무변) |
| **이름 대조 누수** | **0곳** | `ownerName`·`owner_name`·`tameOwnerName` 로 소유를 판정하는 줄이 서버 전체에 **하나도 없다**(표시 전용) |

★부수로 드러난 것: **stable id 덕분에 "같은 zone 중복 접속 차단"이 게스트에도 실제로 걸린다**
(로그 `동일 zone 중복 차단`). 종전엔 게스트 id 가 매번 달라 이 규칙이 사실상 꺼져 있었다.
등록 계정과 같은 규칙이 적용되는 것이므로 **의도된 정합**이다.

---

## 4. 검증

| 검사 | 결과 |
|---|---|
| `test-guest-identity.js`(신규 — 프로토콜 층 실서버) | **39 / 0** |
| `e2e-guest-reconnect.js`(신규 — 실클라 Chromium) | **26 / 0** |
| `e2e-village.js` | §4.3 |
| `e2e-metallurgy.js` | §4.3 |
| `test-furnace.js` · `test-village-found.js` · `test-distmatrix.js` · `test-frontier-iron.js` | §4.3 |
| `lab-wiring-check.js` | **실패 0 · 경고 0** |
| v2 CLI 회귀 · 실지도 3시드 800일 | §4.2 — **econ 무접촉이 정상**이다 |

### 4.1 실클라 재접속 — 사람이 하듯

진짜 Chromium 이 **이름도 비밀번호도 없이** 들어가 → 2×2 사유지 → **노 3단계 완공** →
사유지 4칸 회수 → 옆으로 걸어가 다시 2×2 → **마을 회관 완공(마을 등록)** →
**브라우저 컨텍스트를 통째로 닫고**(localStorage 만 `storageState` 로 들고) → 새 컨텍스트로 재입장.

* `anon_m_kMaezpaTW5` → 재접속 후에도 `anon_m_kMaezpaTW5` — **같은 사람**
* **제 마을 재고를 연다**(회관 클릭 → 재고 패널)
* **제 노를 쓴다** — 응답이 *"철 정광이 없다"*(재료 부족)이지 *"이 노의 주인이 아닙니다"*가 아니다
* **사유지 4칸 그대로**
* 토큰 없는 **새 컨텍스트**(다른 사람)는 회관 재고가 **안 열리고**, 거부 사유가 **권한**이다
  (거리가 아니다 — 남도 회관 앞까지 걸어가게 해서 두 사유를 갈랐다)
* 토큰이 **화면·알림 어디에도 안 보인다**(첫 접속·재접속 둘 다 확인)

★하네스를 만들며 **세 번 틀렸고 세 번 다 실측이 고쳤다**(전부 주석으로 남김):
① `welcome` 이 와야 채워지는 값을 한 번만 읽어 **거짓 실패**를 냈다(→ 폴링).
② 재접속은 마을광장에서 시작하는데 회관·노에는 거리 제한(200px·120px)이 있다 —
   **"너무 멀리"를 권한 거부로 오독할 뻔했다**(→ 걸어가서 누른다. 남도 걸어가게 해서 사유를 가른다).
③ 마을 등록 직후 클라 사유지 맵이 잠깐 비는 순간을 읽어 **'0칸'이라는 거짓 기준선**을 만들었다(→ 수렴 대기).

### 4.2 NPC 세계 — **econ 무접촉**

이 배치는 `sim/` 을 **한 파일도 건드리지 않았다**(`git diff --stat -- sim/` 이 비어 있다).
그래도 주장 대신 재 봤다 — §4.3 표 참조.

### 4.3 실측 표

| 검사 | 결과 |
|---|---|
| `test-guest-identity.js`(신규) | **39 / 0** |
| `e2e-guest-reconnect.js`(신규 · 실클라) | **26 / 0** |
| `e2e-village.js`(실클라) | **32 / 0** |
| `e2e-metallurgy.js`(실클라) | **20 / 0** |
| `test-furnace.js` | **85 / 0** |
| `test-village-found.js` | **47 / 0** |
| `test-distmatrix.js` | **9 / 0** |
| `test-frontier-iron.js` | **11 / 0** |
| `lab-wiring-check.js` | **실패 0 · 경고 0** |
| v2 CLI 회귀(30마을) | 인구 **2,616** · 무기Q **651** · 소멸 **0/30** · 소멸(P) **0/0** |

**실지도 3시드 800일** — `b12tr`(배치 12 종료 시점) 대비:

| 태그 | 인구 | 소멸(NPC) | 소멸(P) | 좀비 | 도구 | 확장셀 | 무기Q |
|---|---|---|---|---|---|---|---|
| `b12tr` | 3,549 | 0 | 0/0 | 0 | 5,325 | 3,756 | 594 |
| `b13` | **3,549** | **0** | **0/0** | **0** | **5,325** | **3,756** | **594** |

덤프 전문 비교: **3/3 시드 바이트 단위 완전 동일.** `sim/` 을 안 건드렸으니 당연하지만,
"당연하다"는 주장이 아니라 **실측**으로 적는다.

★랩 2종 재인라인 — 디바이스 사본이 **배치 12 엔진으로 낡아 있었다**(`lab-wiring-check` 가 잡았다).
현재 번들로 다시 인라인하고 스모크(콘솔 에러 0) 뒤 디바이스에 동기했다.

---

## 5. 손잡이·스키마

| 이름 | 값 |
|---|---|
| `players.guest_token` (central) | 신규 칼럼 + 부분 유니크 인덱스. 기존 DB 는 마이그레이션으로 자동 추가 |
| `POST /guest` (central) | `{token?}` → `{ok, player_id, token, isNew}` |
| `guest_token` (ws 쿼리) | 클라 → zone. **등록 로그인일 땐 안 보낸다**(토큰이 계정 열쇠가 되지 않게) |
| `welcome.playerId` / `welcome.guestToken` | zone → 클라. 토큰이 흐르는 **유일한** 경로 |
| `localStorage['durango_guest_token']` | 클라 보관. 화면에 입력칸도 표시도 없다 |

**되돌리기**: central 이 `/guest` 를 안 주면(구버전) zone 이 예외를 먹고 **종전 1회용 신원**으로
떨어진다. 클라가 토큰을 안 보내도 동작은 종전과 같다. ⇒ 배포 순서에 제약이 없다.

---

## 6. 배치 12 회부 2 — **종결**

*"게스트는 재접속하면 제 건물의 주인이 아니게 된다"* — 막았다. 근거는 §3·§4.1.

## 7. 남은 것 (구현 안 함)

* **게스트 인벤·좌표는 여전히 안 남는다.** `savePlayer` 가 `anon_*` 를 그대로 건너뛴다.
  소유는 이어지는데 배낭은 안 이어지는 상태다. central 에 게스트 행이 생겼으므로 **가드 한 줄만
  풀면 되지만**, 그건 "게스트도 저장한다"는 정책 변경이고 규모(모든 게스트의 주기적 central 쓰기)가
  달라진다 — 별도 판단.
* **게스트 → 등록 계정 승격 시 playerId 승계**는 이 배치 범위 밖(지시 명시). 지금은 승격하면
  새 계정이 되고 게스트 시절 자산은 옛 `anon_` 에 남는다. 승계를 원하면 별도 배치.
* **토큰 회수·만료 없음.** 브라우저를 비우면 그 신원은 영영 못 돌아온다(비밀번호가 없으니 당연).
  "복구 코드"를 원하면 그게 곧 계정 체계다 — 이번 지시가 금지한 방향.
* 오래 안 온 게스트 행 정리(`last_seen` 기준 prune)는 아직 없다. 행이 브라우저 수만큼 는다.
