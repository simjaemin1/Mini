# BENCHMARK — 존 수용 인원 실측 (2026-09-01)

> **이 문서의 규칙**: 안 해본 건 "안 해봤다"고 적는다. 추측은 추측이라고 적는다.
> 측정 원본(JSON)은 각 실행이 남긴 `결과_<시나리오>_<N>명_<타임스탬프>.json` 이다.

---

## 1. 측정 환경

| | |
|---|---|
| **서버(측정 대상)** | Vultr · **1 vCPU** (Intel Xeon Cascadelake) · RAM **1.9 GiB** (+swap 5.3 GiB) · Ubuntu 22.04.5 · Docker 29.5.2 |
| **부하생성기** | **별도 머신**(4코어 · RAM 3.8 GiB · Node v22.23.2) — 서버가 1코어라 같은 곳에서 돌리면 측정이 무의미하다 |
| 네트워크 | 생성기 → 서버 인터넷 경유. `/health` 왕복 **54ms** |
| 배포 커밋 | `42764e2` |
| 존 tick | **30 Hz** (`server/zone.js:471 TICK_HZ = 30` · `:8079 TICK_MS = 1000/30 = 33.33ms`) |
| 이상 tick 간격 | **33.3 ms** |
| 성능 플래그 | `TERRAIN_SEG_INDEX` · `TERRAIN_TILE_CACHE` **둘 다 꺼짐**(배포된 상태 그대로) |
| 컨테이너 env | `PORT=3020 · DB_PATH=/data/world-hanbando.db · PLAYER_CAP=150 · ZONE_ID=hanbando · LATENCY_MS=0 · CHAR_SPRITE=on` |

⚠**`README.md` 의 "서버 tick 10Hz" 는 틀렸다.** 코드는 30Hz이고 이동 적분·클라 예측이 모두 거기 묶여 있다.

### 자(尺) — 무엇으로 쟀는지

이 문서에는 CPU 를 재는 자가 **둘** 나온다. 섞어 읽으면 안 된다.

| 자 | 쓴 곳 | 성질 |
|---|---|---|
| `docker stats` 의 CPUPerc | §2 · §3 · §3-B | 1 vCPU 호스트에서 **100%를 넘겨 찍는다**(400~470%). "포화했다" 신호로만 읽어야 한다 |
| `/proc/<pid>/stat` 의 utime+stime 증분 | §4-D · §4-D-4 재측정 | 커널이 센 값. 창을 우리가 정한다. **코어 1개 = 100%** |

배수·비율을 말하는 자리는 전부 아래쪽 자로 통일했다(§4-D-4).
⚠`/proc/stat` 로 머신 전체를 잴 때는 idle(`$5`)·iowait(`$6`)을 **빼야 한다** —
안 빼면 무슨 부하든 100% 가 나온다(이 세션이 처음에 그렇게 틀렸다).

### 측정 방법

* 각 단계 **60초 측정 + 앞 10초 워밍업 폐기**. 단계 사이 20~30초 휴식. **서버 재시작 없음**(누적 부하).
* tick 간격은 **1ms 고정 빈 히스토그램**으로 전 구간 집계(`bench/hist.js`).
  종전 `loadtest.js` 는 배열이 5만을 넘으면 앞을 잘라내(`splice`) p95 가 "최근 구간" 값이 됐고,
  잘리는 정도가 클라 수마다 달라 **단계 비교 자체가 성립하지 않았다.** 그래서 바꿨다.
* **서버 자기 시간**은 존이 스스로 찍는 로그를 읽는다 — `diag tick_avg=… tick_max=…`.
  ⇒ 서버 코드는 **한 줄도 고치지 않았다.**
* ★**부하생성기 자기 CPU를 매 단계 같이 잰다**(`process.cpuUsage`). 생성기가 병목이면
  "존당 수용 인원"이 거짓말이 되므로, 그 선을 숫자로 남긴다.

---

## 2. 시나리오 A — 순수 플레이어 비용 (`ENABLE_VILLAGES=0 ENABLE_WILDLIFE=0 ENABLE_BANDITS=0`)

세계 상태: **NPC 0** · 몹 **449**(⚠`ENABLE_WILDLIFE=0` 인데도 남았다 — 아래 §6) · 건물 3

| 클라 | p50 | p95 | p99 | max | 60ms 초과 | 표본 | 접속(실패/이탈) | 생성기 CPU | 서버 `tick_avg` |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 33 | 46 | 108 | 610 | 3.66% | 1,694 | 1/1 (0/0) | 2.9% | 2.4–6.9 ms |
| 3 | 33 | **37** | 102 | 315 | 2.87% | 5,121 | 3/3 (0/0) | 3.4% | — |
| **5** | 33 | **92** | 185 | 300 | 9.09% | 7,705 | 5/5 (0/0) | 4.7% | 6–11 ms |
| 7 | 36 | 190 | 391 | 712 | 30.98% | 7,388 | 7/7 (0/0) | 4.3% | 16–74 ms |
| 10 | 96 | 315 | 496 | 1,192 | 68.32% | 3,914 | 10/10 (0/0) | 5.9% | 119–305 ms |

### 무릎: **5명**

판정 기준은 "p95 가 이상값의 2배(66.6ms)를 넘기 시작하는 지점"이다.
3명 p95 **37ms**(통과) → 5명 p95 **92ms**(초과). **무릎 전후 단계를 둘 다 쟀다.**

* 10명에서 접속은 10/10 성공하고 **끊기지 않는다** — 무너지는 방식은 "끊김"이 아니라 **지연**이다.
* 생성기 CPU는 전 구간 **6% 미만**이다 ⇒ 어느 단계도 생성기 한계가 아니다(이 표의 숫자는 서버 것이다).

---

## 3. 시나리오 B — NPC 켬 (배포된 실제 설정)

세계 상태: NPC **1,566** · 몹 **449** · 건물 **2,228**

| 클라 | p50 | p95 | p99 | max | 60ms 초과 | 표본 | 접속 | 생성기 CPU | 서버 `tick_avg` |
|---|---|---|---|---|---|---|---|---|---|
| 1 | **515** | 1,081 | 2,199 | 2,199 | **100%** | 107 | 1/1 | 2.1% | 454–668 ms |
| 5 | 508 | 1,078 | 5,301 | 5,301 | 96.06% | 482 | 5/5 | 3.1% | 562–888 ms |

### 무릎: **1명 미만**

**클라 1명에서 이미 무너져 있다.** p50 515ms = 이상값의 **15배**. 60ms 초과가 **100%** —
단 한 번의 tick 도 예산 안에 들어오지 않았다.

그리고 1명 → 5명에서 지표가 **거의 안 움직인다**(515→508ms). 비용이 플레이어 수에 딸린 게 아니라는 뜻이다.

---

## 3-B. 시나리오 B+플래그 — 어젯밤 성능 수리를 켜고 같은 측정

B 와 **완전히 같은 세계**(NPC 1,566 · 몹 449 · 건물 2,228). 다른 것은 env 두 줄뿐:
`TERRAIN_SEG_INDEX=1` · `TERRAIN_TILE_CACHE=1` (커밋 `69ed84b`·`42764e2` — 이미 배포된 이미지에 들어 있고
**꺼져 있었다**).

| 클라 | p50 | p95 | p99 | max | 60ms 초과 | 표본 | 접속 | 생성기 | 서버 `tick_avg` |
|---|---|---|---|---|---|---|---|---|---|
| 1 | **34** | 97 | 194 | 1,015 | 20.77% | 1,377 | 1/1 | 2.9% | 15–28 ms |
| 5 | **35** | 103 | 200 | 1,887 | 38.97% | 5,814 | 5/5 | 5.6% | — |
| 10 | 62 | 107 | 201 | 992 | 51.10% | 9,638 | 10/10 | 7.7% | 31–77 ms |

### B → B+플래그

```
             서버 tick_avg      클라 p50      p95        60ms 초과
  B (꺼짐)    454 ~ 888 ms       515 ms     1,081 ms       100%
  B+플래그     15 ~  28 ms        34 ms        97 ms      20.8%
             ─────────────      ────────    ─────────    ────────
                약 30배           15배         11배        1/5
```

**env 두 줄로 tick 이 예산(33ms) 안으로 들어온다.** 서버 CPU 도 470% → 91%(둘 다 `docker stats` — §1 의 자 표 참조).

★그리고 눈에 띄는 것: **B+플래그 10명(p95 107ms)이 A 7명(p95 190ms)보다 낫다.**
A 는 NPC 를 전부 끈 세계인데도 그렇다 — A 역시 플래그가 꺼진 채로 쟀기 때문이다.
⇒ 이 두 플래그가 **NPC 층 전체가 물리던 비용보다 더 많이 회수한다.**

⚠**남은 것은 꼬리다.** p50 은 이상값에 붙었지만 max 가 여전히 992~1,887ms 다.

### 남은 꼬리의 정체 — **추측이었는데 재서 확인했다**

실서버에서 **32분 연속 관측**(12:57:06Z→13:29:26Z · 봇 1명 · 플래그 **끈** 배포 상태 · 존이 스스로 찍는 `diag` 172표본).
★1회차는 실패했다 — 사람이 0명이면 idle skip 때문에 `diag` 자체가 안 찍힌다(§4-D). 봇을 붙여 다시 걸었다.

관측 창에서 **가장 큰 틱 하나**와, 그 앞뒤 10초 안의 로그:

```
13:12:21~22  🐂 캐러밴 16대 한꺼번에 출발 — 경로 44 · 56 · 78 · 85 · 96 · 104 · 129 · 136 ·
                193 · 200 · 226 · 267 · 325 · 359 정점 (A* 를 16번 돈다)
13:12:22     📜 사건 6건(누적 14 · 게시판 4건) · 188ms
13:12:31     🏘️ 마을 econ day 1670: 인구 7639 · 스폰 NPC 1560 ·
                캐러밴 실체 36/36(+14 도착7 회수1) · 저장큐 50행
13:12:34     diag tick_avg=5991.2ms  tick_max=33404ms      ← 33.4초
```

관측 32분 동안 게임일 경계는 **한 번**(econ day 1670) 있었고, **최대 틱도 한 번**이고, 둘이 13초 안에 붙어 있다.
⇒ §5 에서 "추측"이라고 적어 둔 항목을 **관측으로 바꾼다: 남은 꼬리는 게임일 경계 틱이다.**
그 안의 내역까지 서버가 스스로 적어 준다 — 캐러밴 16대의 A\*, NPC 1,560 스폰, 저장큐 50행, 사건 처리.

**GC 는 범인이 아니다**(가설 하나를 잘라냈다):

```
GC 정지  1,520회 / 32분 · 200ms 초과 4회 · 최대 598ms
```

최대 GC 정지(598ms)가 문제의 틱(33,404ms)의 **1/56** 이다. 규모가 두 자릿수 어긋난다.

### ★다른 계기로 한 번 더 — 밖에서 찔러도 같은 말을 한다

위 33.4초는 **존이 자기를 잰 값**이다(`Date.now()` 로 틱 본문을 감싼 것). 자기 계측만으로 33초를 주장하면
계측이 틀렸을 가능성을 못 배제한다. 그래서 **밖에서 `/health` 를 2초마다 찌르는** 관측을 따로 걸었다
(봇 1명 · 27분 · 표본 442). 두 계기가 같은 순간에 같은 말을 하면 그때 확정이다.

```
14:47:55  diag tick_avg=269.1ms  tick_max=1114ms          ← 평시
14:48:00  /health 요청 나감
14:48:25  🏘️ 마을 econ day 1674 … 저장큐 50행 분산 · 25797ms
14:48:33  diag tick_avg=3781.6ms tick_max=27207ms          ← 존 자기 계측
14:48:34  /health 응답 도착 — **33.78초** (200)             ← 밖에서 잰 값
14:48:44  diag tick_avg=310.0ms  tick_max=504ms            ← 평시로 복귀
```

★`diag` 는 10초마다 찍는데 14:47:55 다음이 **14:48:33**(38초 뒤)이다 — 진단 타이머 자체가 굶었다.
세 번째 증거다. ⇒ **이벤트 루프가 실제로 30초 가까이 막힌다.** 자기 계측 오류가 아니다.

곁가지로 나온 값 하나: 봇 1명이 붙어 있는 동안 `/health` 평균 응답이 **1.23초**(표본 442)다.
도커 헬스체크의 타임아웃은 3초다 — 게임일 경계마다 컨테이너가 `unhealthy` 로 뒤집힐 수 있다.

**설명 못 한 것 하나** — 13:04:26~13:11:58 의 8분 동안 `tick_avg` 가 **180~230ms** 로 떨어진다.
앞뒤 구간은 400~900ms 다. 게임일 경계 **직전** 구간이라 캐러밴이 다 도착해 세계가 잠깐 비는 것으로
보이지만, **확인하지 않았다.** 추측으로 남긴다.

### 부팅

```
플래그 켬   7분 10초   (12:02:40 기동 → 12:09:50 /health OK)
플래그 끔  13분 51초   (12:19:40 기동 → 12:33:31 /health OK)   ← 원복 뒤 실측
```

끈 쪽의 부팅 로그가 내역을 스스로 찍는다 — 한 줄이 197초다:

```
[hanbando] 🏘️ 교역 BFS 거리행렬: 50마을 1225쌍 197001ms
           (그리드 547×1016 · step 4셀 · 지형판정 476074 · 다리구제 75)
```

지형 통행 판정 **47.6만 번**이 저 197초 안에 들어 있다 — §3-B 의 두 플래그가 겨냥하는 바로 그 술어다.
⇒ 이 두 숫자는 **같은 서버·같은 이미지·같은 세계에서 env 두 줄만 다르게 하고 잰 것**이다.

⚠재기동 중에는 플레이어가 못 들어온다. 헬스체크는 300초에 포기하고 컨테이너는 그 뒤로도
`unhealthy` 인 채 12분을 더 떠 있었다(오늘 실제로 그랬다). → `KNOWN_ISSUES.md` N.7

---

## 4. A vs B — 이 측정의 핵심 숫자

```
                        서버 tick_avg      클라 p50      수용 인원(무릎)
  A (NPC 없음·플래그 끔)   2.4 ~ 11 ms        33 ms          5명
  B (NPC 1,566·플래그 끔) 454 ~ 888 ms       515 ms        1명 미만
  B+플래그 (같은 NPC)      15 ~  28 ms        34 ms      10명에서도 p50 62ms
                        ─────────────      ────────      ──────────
```

**부하의 주범은 플레이어가 아니라 세계(NPC·마을 시뮬)다.**
같은 하드웨어·같은 코드에서 NPC 층만 껐다 켜는 것으로 tick 비용이 **두 자릿수 배** 달라진다.

★보강 관찰: 봇이 **0명**이면 존은 idle skip 으로 CPU **2.92%** 다(`server/zone.js:8302~8314` — 사람 player·
observer 가 둘 다 0이면 tick 본문을 건너뛴다). 같은 존에 봇 **1명**을 붙이면 **92.92%** 로 뛴다.
(둘 다 2026-09-01 14:05~14:07 실서버 재측정 · `/proc/<pid>/stat` 의 utime+stime 증분 · 40초 창 · 코어 1개 기준)
즉 NPC 비용은 **사람이 한 명이라도 붙는 순간** 전부 청구된다.

---

## 4-C. 시나리오 C — 존 핸드오프 **정합성** (로컬 2존)

> ⚠**이건 수용량 측정이 아니다.** 핸드오프에서 물어야 할 것은 "몇 명까지 되나"가 아니라
> **"넘어갈 때 사람이 온전히 넘어가나"** 다. 그래서 실서버가 아니라 컨테이너(2코어)에서 쟀다.
> 여기 tick 숫자는 **하드웨어 성능의 근거로 쓰면 안 된다** — 부하가 없는 환경이다.

### 판(setup)

```
hanbando rect=(409984,49984, 70016×130016)  ← 동쪽 끝 480000
nippon   rect=(480000,49984, 49984×130016)  ← 서쪽 끝 480000   (실좌표가 맞닿음)
마을·야생·도적 OFF (정합성 측정 — 부하를 변수에서 뺐다)
출발 존(hanbando)에만 E2E_GIVE=1 · 도착 존엔 없음
  ⇒ 도착 존에서 보이는 물건은 **핸드오프가 옮긴 것**이다(도착 존이 만들어 줄 방법이 없다)
봇마다 다른 바구니: pillar 1·2·3·4 / stone 21·22·23·24 / thatch 11·12·13·14 + 도끼·곡괭이
기동: bash scripts/bench-2zone.sh start
```

### C-1. 왕복 — 잃는가·복제되는가 (봇 4 · 240초)

```
핸드오프          28회 (클라가 받은 handoff 메시지)
handoffs_out      hanbando 16 + nippon 12 = 28
handoff_acks      hanbando 16 + nippon 12 = 28      ⇒ ACK 100%
handoff_timeouts  0
접속 실패          0 · 재접속 실패 0
클라 tick p50 33ms · p95 34ms · max 97ms  (핸드오프가 tick 을 흔들지 않았다)
```

**28번 넘나드는 동안 스칼라 인벤토리는 한 번도 틀리지 않았다.**
봇마다 다른 바구니를 줬는데 섞임 0 · 증발 0 · 복제 0:

```
bot1  hanbando pillar:2,stone:22,thatch:12  →  nippon 같음(×5)  →  hanbando 같음(×4)
bot2  hanbando pillar:3,stone:23,thatch:13  →  nippon 같음(×5)  →  hanbando 같음(×4)
bot3  hanbando pillar:4,stone:24,thatch:14  →  nippon 같음(×5)  →  hanbando 같음(×4)
```

### C-2. 히스테리시스 대조군 (봇 4 · 180초)

`HANDOFF_COMMIT = 256`(zone.js:570)은 "경계 양쪽 256px 는 겹침 띠 — 그 안에선 핸드오프 안 함"이다.
그 주장을 그대로 시험했다: 봇을 **경계선 위에 세우고 ±100px 진자 운동**을 시켰다(진폭 < 256).

```
경계선(x = zoneWidth) 통과   71회
관측된 최대 경계 초과        101px   (COMMIT 256 안)
핸드오프                      0회     ✅
중도 이탈                     0
```

⇒ 히스테리시스가 없었다면 71번의 핸드오프(=71번의 재접속)가 났을 자리다. **핑퐁 0.**

### C-3. ★그런데 **도구는 사라진다** (측정으로 확인)

인벤토리는 온전한데 **도구 인스턴스(`toolItems`)가 존을 넘는 순간 없어진다.** 28회 전부.

```
① 출발(hanbando)   inv=fish:3,pillar:3,thatch:7  도구=axe/100+pickaxe/100
                   kg원장=fish[2·0.4·1.1]   게스트토큰=있음
② 도착(nippon)     inv=fish:3,pillar:3,thatch:7  도구=(없음)
                   kg원장=(빈)   (물고기 1마리 버린 뒤)
③ 재접속(hanbando) inv=fish:2,pillar:3,thatch:7  도구=(없음)
```

읽는 법: ②의 `kg원장` 을 보려면 인벤을 한 번 건드려야 해서(도착 존은 welcome 에 원장을 안 싣는다)
물고기 1마리를 버렸다 — ③의 `fish:2` 는 **그 버린 것**이지 핸드오프가 잃은 게 아니다.
잃은 것은 `도구` 와 `kg원장` 둘이다.

(원본: `bench/결과/시나리오C_프로브_2026-09-01.txt` — `node scripts/probe-handoff-tools.js` 출력 그대로)

③이 결정적이다 — **재접속해도 안 돌아온다.** 도착 존이 빈 몸으로 계정 행을 덮기 때문이다
(도착 경로 `zone.js:2771~2804` — 인벤 대입은 `:2790` — 은 `pending` 페이로드만 읽고 central 행을 **아예 안 읽는다**.
계정 행 `acct` 를 읽는 곳은 그 아래 `else` 분기뿐이다).

코드도 같은 말을 한다. `fireHandoff` 페이로드(`zone.js:9218~9243`)에 있는 것은
`inventory · tools · equipped · hunger · thirst · vp · tribe · pvp · floor · home` 뿐이고,
`toolItems`(도구 인스턴스) · `equipment` · `craftSkill` · kg 원장 · 로트 · 신체 상태는 **없다**.
(welcome 은 `tools` 를 두고 스스로 `// 옛 호환 (사용 X)` 라고 적어 뒀다 — 즉 **죽은 필드만 건너가고 산 필드가 빠진다.**)

* **실측한 것**: `toolItems` 소실(영구) · kg 원장 소실.
* **코드로만 확인한 것**: `equipment` · `craftSkill` · 로트 · 신체 상태(추위·피로·부상·사기)도 같은 페이로드에 없다.
  이 넷은 **실제로 넣고 넘겨 보지 않았다.**

측정 배치 규칙대로 **고치지 않았다.** → `KNOWN_ISSUES.md` N.6.

### C 에서 **안 한 것**

* 실서버(1 vCPU)에서의 핸드오프 — **안 했다.** 로컬 2코어 결과다.
* 부하가 걸린 상태(NPC 1,566)에서의 핸드오프 — **안 했다.**
* 동시 다발 핸드오프(수십 명이 한꺼번에) — **안 했다.** 봇 4명이 전부다.
* 존 사이 네트워크가 느리거나 끊길 때의 ACK 타임아웃 경로 — **안 했다**(타임아웃 0회라 그 경로가 안 돌았다).
* `equipment` · `craftSkill` · 로트 · 신체 상태의 소실 — **코드만 봤고 안 재 봤다.**

### 곁가지 — 경계가 통째로 육지가 아니다

한반도 동쪽 끝(x=69616)을 2048px 간격 63곳 찍어 보니 **육지 29 · 물 34** 였다
(`scripts/probe-edge.js` · 출력은 `bench/결과/시나리오C_프로브_2026-09-01.txt`).
그래서 봇은 "찍히는 자리"가 아니라 "실제로 넘어지는 자리"를 찾아야 했다
(텔레포트 314회 중 276회가 물로 거절). 이건 버그가 아니라 지형이다 — 기록만 남긴다.

---

## 4-D. 시나리오 D — 존 26개를 한 호스트에 (idle skip)

> ⚠**세계 시뮬을 끄고 쟀다**(`ENABLE_VILLAGES=0 ENABLE_WILDLIFE=0 ENABLE_BANDITS=0`).
> 26개를 마을까지 켜서 띄우면 부팅만 몇 시간이다(§3-B 부팅 참조 — 한 존이 7~14분).
> 그래서 이 절이 재는 것은 **프로세스·지형·틱 뼈대의 값**이지, 마을 든 존의 값이 아니다.
> 마을 든 존의 값은 실서버 hanbando 한 곳으로 따로 잰다(아래 마지막 표).
> 판: 컨테이너 **2코어 · 8 GiB**. `bash scripts/bench-26zone.sh start`

### D-1. 26개가 뜨나 — 뜬다

```
기동          central + 존 26개 = 7초 (동시 spawn)
RSS 합계      2,602 MiB   (존 26개 · 평균 100 MiB)
  최대        kongra 189 MiB · oseania 162 · nippon 140
  최소        바다 존 7개가 각 72.0~72.6 MiB  (isOcean — 지형 생성이 없다)
봇 19명 붙인 상태 RSS 합계  3,723 MiB (node 프로세스 30개 전체)
```

### D-2. 깨운 존 수 사다리 (봇 = 존당 1명 · 각 단계 30초 안정 후 40초 창)

```
  깨운 존   존 CPU 합계(코어 1개 기준)   머신(2코어 중)   존당 tick/s
     0            24.0 %                  15.2 %            —
     1            48.1 %                  29.2 %          31.0      ← canadia. 대표성 없음(아래)
     4            53.2 %                  30.5 %          31.0
     8            68.0 %                  38.3 %          31.0
    19           127.3 %                  69.5 %          30.2
```

**19곳을 동시에 깨워도 존마다 30.2 tick/s 가 나온다.** 2코어 박스에서 코어 1.27개어치다.

### D-3. 한 창 안에서 잰 깨어 있는 존 vs 자는 존

★**사다리(D-2)와 다른 회차다.** 사다리의 19 칸은 존별 내역을 안 남겨서 19곳을 **다시 깨워** 잰 것이고,
그 회차의 합계는 **112.5%(머신 62.8%)** 로 사다리의 127.3%(69.5%)보다 낮다 — 같은 조건의 두 회차가
그만큼 흔들린다는 뜻이다(2코어 박스). 아래 내역은 **112.5% 회차 한 창 안의 값**이고,
"깨어 있는 존 대 자는 존" 비교는 그 한 창 안에서만 하는 것이라 회차 흔들림의 영향을 안 받는다.
나머지 7곳은 바다 존이라 봇을 안 붙였다 — 같은 창의 대조군이 공짜로 생겼다:

```
  깨어 있는 존   canadia 35.40 · jungwon_n 8.18 · sibara 8.05 · hanbando 5.67 · bering 4.88
                 centaria 4.28 · nubiano 4.05 · europa 3.85 · nippon 3.62 · kongra 3.48
                 jungwon_s 3.45 · hindgang 3.30 · sahar 3.12 · mayan 3.12 · amazonia 3.00
                 oseania 2.88 · nordan 2.77 · nanyang 2.77 · patagona 2.50
  자는 존        pacific 0.62 · east_sea_s 0.62 · pacific_arctic 0.60  (…7곳 전부 0.6대)
  ─ 이 창의 합계 112.5 % · 머신 62.8 %(2코어 중) · 접속 19/19 · 존당 30.2 tick/s
```

⇒ **자는 존 0.6% · 깨어 있는 존 2.5~8.2%** — 같은 순간·같은 머신에서 **4~13배**다.
봇 하나만 붙였을 때의 단독 측정도 같은 자리에 떨어진다: hanbando **6.66%**(30.8 tick/s), 자는 이웃 0.65~0.85%.

★**canadia 35.4% 는 이상치가 아니라 canadia 다.** `zone.js:9466·9468` 이 canadia 에만
전용 타이머 둘(영토 동기 30초·농지 단계 20초)을 건다. 그래서 사다리의 "1개" 칸(48.1%)도
canadia 를 깨운 값이라 **대표값이 아니다** — 대표 한 곳의 값은 위 hanbando 6.66% 다.

### D-4. idle skip 은 세계가 무거울수록 크게 번다

두 자리에서 잰 같은 효과(자는 존 → 깨어 있는 존):

```
                                   자는 존      깨어 있는 존(사람 1명)     배수
  로컬 2코어 · 세계 시뮬 OFF        0.60 ~ 0.85 %     2.50 ~ 8.18 %        4 ~ 13배
  실서버 1 vCPU · 세계 시뮬 ON      2.92 %            92.92 %              32배
```

⚠**두 줄 다 같은 자로 쟀다** — `/proc/<pid>/stat` 의 utime+stime 증분 · 40초 창 · 코어 1개 기준.
아랫줄은 2026-09-01 14:05~14:07 에 실서버에서 **다시 잰 값**이다(존 RSS 294.7 MiB · 봇 붙였을 때 tick_avg 584ms).
§2·§3 의 `docker stats` 값(470%)과 자릿수가 다른 건 **자가 다르기 때문**이다 —
1 vCPU 호스트에서 `docker stats` 는 100%를 넘겨 찍는다(그 절에도 그렇게 적어 뒀다).
배수를 말할 때는 **같은 자로 잰 이 표**를 쓴다.

⇒ idle skip 이 아끼는 것은 "프로세스가 도는 값"이 아니라 **그 존의 세계 시뮬 전체**다.
사람 하나가 붙는 순간 1 vCPU 가 사실상 다 찬다(92.92%).

### D-5. 그래서 실서버(1 vCPU · 1.9 GiB)에 26개가 올라가나 — **RAM 이 먼저 막는다**

```
  잰 값 ①  세계 시뮬 OFF 존 1개 RSS   100.1 MiB  (로컬 26개 평균 · 합계 2,602.4 MiB)
  잰 값 ②  세계 시뮬 ON  존 1개 RSS   294.7 MiB  (실서버 hanbando · 사람 0명 · /proc VmRSS)
  산수     26 × 294.7 MiB = 7,662 MiB = 7.48 GiB   ← 서버 총 램은 1.9 GiB
           26 × 100.1 MiB = 2,603 MiB = 2.54 GiB   ← 이것도 1.9 GiB 를 넘는다
```

**두 RSS 는 잰 값이고, 곱셈은 산수다 — 26개를 실서버에 띄워 보지는 않았다.**
로컬 8 GiB 박스에서는 세계 시뮬 OFF 26개가 2.54 GiB 로 실제로 올라갔다(D-1).

### D 에서 **안 한 것**

* **세계 시뮬을 켠 26개** — 안 했다. 부팅만 몇 시간이라 이 세션에서 못 한다.
* **실서버에 26개** — 안 했다(위 산수대로 램이 모자란다).
* **idle skip 을 코드로 끄고 A/B** — 안 했다. 측정 배치라 서버 무수정이 규칙이라,
  "깨운 존 수를 바꿔 가며 밖에서 잰" 값으로 대신했다.
* 봇은 존당 **1명**이다. 여러 명이 한 존에 몰릴 때의 값은 §2·§3 가 따로 잰다.

---

## 5. 병목 추정 — 근거와 추측을 나눠서

**근거 있는 것**

* 서버 CPU가 실제로 포화한다. `docker stats` 가 B/1명에서 **470%**, A/10명에서 **401%** 를 찍는다
  (1 vCPU 호스트라 100% 초과는 도커 계산 방식 탓이다 — "포화" 신호로만 읽어야 한다).
* 네트워크가 아니다. tick 페이로드는 **2.4 KB**(AOI 안 players 25 · mobs 0), 실효 **0.02 Mbps** 였다.
* 생성기가 아니다. 전 단계에서 생성기 CPU **6% 미만**.
* B 에서 지표가 클라 수에 거의 무관하다(1명 515ms ≈ 5명 508ms) ⇒ **고정 비용**이다.

**추측(확인 안 함)**

* NPC 1,566명의 이동·AI·시야 계산이 그 고정 비용의 대부분일 **가능성이 높다.**
  `CRITICAL_BUGS.md` 의 A\* / quadtree 항목과 결이 같다. **다만 이번 측정에서 프로파일을 뜨지 않았다.**
* ★**성능 플래그 둘을 켜고 실서버에서 다시 쟀다(§3-B).** `tick_avg` 454~888ms → **15~28ms**.
  ⇒ 위의 "NPC 가 주범"이라는 결론은 **"NPC 층이 부르는 지형 통행 판정이 주범"** 으로 좁혀진다.
* ★**남은 꼬리는 재서 확인했다(§3-B "남은 꼬리의 정체").** 32분 관측에서 최대 틱 33.4초가
  게임일 경계(econ day 1670)와 13초 안에 붙어 있었고, 그 경계에서 캐러밴 16대의 A\* ·
  NPC 1,560 스폰 · 저장큐 50행이 한 틱에 몰린다. GC 는 아니다(최대 정지 598ms = 1/56).
  **다만 그 틱 안에서 어느 함수가 몇 ms 인지는 여전히 프로파일을 안 떠 봤다.**

---

## 6. 곁가지 관측 (측정 중 발견 · 고치지 않음)

* **`ENABLE_WILDLIFE=0` 인데 몹 449마리가 남는다.** 시나리오 A 가 "완전히 빈 세계"는 아니다.
  A 의 수치는 "NPC 없음 + 몹 449" 로 읽어야 한다.
* **`welcome` 페이로드가 2,249 KB** 다. 접속 후 **첫 tick 이 4.4초 뒤**에 온다.
* **부팅이 매우 길다.** 마을 50개 재시딩 + 교역로 A\*(쌍당 최대 **3,806ms**)로 존 하나가 뜨는 데 수 분.
* `/metrics` 에 tick 소요 시간이 없다(로그의 `diag` 에는 있다).

---

## 7. 안 해본 것 — **명시**

* ~~시나리오 C(핸드오프)~~ → **했다. §4-C.** 다만 **로컬 2코어 · 정합성만**이다.
  실서버에서의 핸드오프, 부하 있는 상태의 핸드오프, 동시 다발 핸드오프는 **안 했다.**
* ~~시나리오 D(26개 존 idle skip)~~ → **했다. §4-D.** 단 **로컬 2코어·세계 시뮬 OFF** 다.
  실서버에는 안 띄웠다 — 램이 모자란다(§4-D-5 의 산수). 세계 시뮬을 켠 26개도 안 했다.
* **A 의 2명 단계**: 실행이 중간에 끊겨 JSON 이 안 남았다. **표에 넣지 않았다.**
* 프로파일링(`--cpu-prof`)으로 NPC 비용의 내역을 쪼개는 일: **안 했다.**

---

## 8. 재현 방법

```bash
# ① 부하생성기(서버와 다른 머신에서)
git clone <repo> && cd durango-mini && npm install
HOST=<서버IP> PORT=3020 CLIENTS=5 DURATION=60 WARMUP_S=10 \
  SCENARIO=A NOTE="설명" node loadtest.js
#    → bench/결과_A_5명_<타임스탬프>.json

# ② 시나리오 A 로 서버 전환(원복용 백업을 먼저 만든다)
docker inspect durango-zone-hanbando --format '{{range .Config.Env}}{{println .}}{{end}}' \
  | grep -v -E '^(PATH|NODE_VERSION|YARN_VERSION|NODE_ENV|HOME|HOSTNAME)=' > zone.env.orig
grep -v -E '^(ENABLE_VILLAGES|ENABLE_WILDLIFE|ENABLE_BANDITS)=' zone.env.orig > zone.env.A
printf 'ENABLE_VILLAGES=0\nENABLE_WILDLIFE=0\nENABLE_BANDITS=0\n' >> zone.env.A
docker rm -f durango-zone-hanbando
docker run -d --name durango-zone-hanbando --restart unless-stopped \
  -p 3020:3020 -v /srv/durango/hanbando:/data --env-file zone.env.A durango-zone
#    원복: 같은 명령을 zone.env.orig 로

# ③ 서버 자기 시간(코드 수정 불필요)
docker logs --since <ISO시각> durango-zone-hanbando 2>&1 | grep 'diag tick'

# ④ 통계 검증
node scripts/test-hist.js     # 백분위수를 정렬 배열 브루트포스와 대조 (21/0)

# ⑤ 시나리오 C — 핸드오프 정합성(로컬 2존)
bash scripts/bench-2zone.sh start           # central + hanbando + nippon (3초)
MODE=handoff CLIENTS=4 DURATION=240 WARMUP_S=10 RAMP_MS=500 \
  HOST=127.0.0.1 PORT=3020 CENTRAL_URL=http://127.0.0.1:3010 \
  METRICS_URLS=http://127.0.0.1:3020/metrics,http://127.0.0.1:3021/metrics \
  SCENARIO=C_handoff node loadtest.js
MODE=jitter  CLIENTS=4 DURATION=180 ... SCENARIO=C_jitter node loadtest.js   # 히스테리시스 대조군
node scripts/probe-handoff-tools.js         # 도구 소실이 전송 누락인지 영구 소실인지
node scripts/probe-edge.js                  # 경계 근처 육지/물 분포
bash scripts/bench-2zone.sh stop

# ⑥ 시나리오 D — 존 26개 idle skip(로컬)
bash scripts/bench-26zone.sh start          # central + 26개 존, 세계 시뮬 OFF (7초)
N=0  DURATION=60 node scripts/bench-spread.js   # 대조군(아무도 안 붙는다)
N=19 DURATION=120 node scripts/bench-spread.js  # 땅 존 19곳에 1명씩
PICK=hanbando DURATION=110 node scripts/bench-spread.js   # 대표 한 곳만
#   CPU 는 /proc/<pid>/stat 의 utime+stime 증분으로 잰다(창을 우리가 정한다).
#   ⚠/proc/stat 로 머신 전체를 잴 땐 idle($5)·iowait($6)을 **빼야** 한다 —
#     안 빼면 무슨 부하든 100% 가 나온다(처음 판이 그래서 '97.6%'를 찍었다).
bash scripts/bench-26zone.sh stop
```

⚠**부하생성기를 서버와 같은 머신에서 돌리지 마라.** 1 vCPU 에서는 생성기가 코어를 나눠 가져
"서버 한계"가 아니라 "둘의 합"을 재게 된다. 그래서 이 측정은 생성기를 분리했고,
매 단계 생성기 CPU를 같이 기록해 그 선을 확인했다(전 구간 6% 미만).
