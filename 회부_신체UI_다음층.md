# 회부 — 신체 상태·UI 다음 층 (2026-08-26 · §7·§8 시제품 뒤)

> 이번 배치가 심은 것: 5축(배고픔·목마름·추위·피로·부상) + 사기 · 연속 효과 곡선 ·
> 무들(겉은 계단) · 상태 패널(§8.6) · 패널 프레임 정리 · 러너 통합.
> 아래는 재민이 §6 에서 **구현 금지**로 못 박은 것 + 만들며 드러난 판단거리다.

## A. 재민이 명시적으로 회부한 것 (구현 금지)

### A-0 옷 장비 슬롯·추위 저항 — ★**해당 없음**(이미 있다)
지시 §0 은 "장비 슬롯이 없으면 추위의 옷 저항은 회부"라는 분기를 뒀는데, **실측 결과 이미 있었다**:
`equipSlots{clothes,armor,weapon,tool}` + `clothes.attrs.warmth` 가 이미 밤 추위를 막고 **옷이 닳는다**
(`COLD_CLOTH_WEAR_MS`). 이번 배치는 그 배선을 `body.js` 로 옮겨 **계절까지** 물렸다(겨울 1 · 봄가을 0.35 · 여름 0).
⇒ 회부로 남는 건 **저항 아이템의 종류**다: 지금은 `warmth` 하나짜리 축이라
가죽옷·모피·짚신이 다 같은 수 하나로 표현된다. 옷감 수요를 진짜로 만들려면 품목별 warmth 표가 필요하다.
그건 제작·교역 표와 함께 봐야 한다(별건).

### A-1 긴장·공포 (전투 층)
§7 이 "전투 층 착수 시 채택 후보"로 미뤄 뒀다. 공포 → 명중률 저하(좀보이드 검증 공식).
★지금 구조는 축을 하나 더 붙이기 쉽게 돼 있다 — `body.js` 의 `AXES`·`CURVES` 에 한 줄씩이면 된다.
다만 **바닥(MOVE_FLOOR/WORK_FLOOR)이 곱 합산에 걸리므로**, 축이 늘수록 바닥에 더 빨리 닿는다.
축을 추가할 땐 바닥값도 같이 재검토하라(지금 5축 전부 최악이면 곱 원값 0.35 → 바닥 0.6).

### A-2 사기 확장 (축제·위세재)
지금 사기는 **조리식 한 갈래**뿐이다(조리 +0.55 · 생식 +0.10 · 10분에 식는다).
§7 이 말한 "위세재·사치 소비의 수요 엔진"이 되려면 축제·새 옷·좋은 그릇이 같은 축에 들어와야 한다.
★판단거리: 사기가 **작업속도만** 올리는 지금 구조를 유지할지(단순·투명), 아니면 사회적 효과
(NPC 호감·거래가)로 넓힐지. 넓히면 "당근"이 경제 축으로 승격되므로 econ 접점 설계가 필요하다.

### A-3 타인 외형 반영 (수척 스프라이트)
전송 설계는 **이번에 이미 정해 뒀다** — `Body.peerPayload()` 가 **단계만** 내보낸다(연속값은 본인 전용).
소비자가 없어 아직 아무도 안 부르지만, 지금 정해 두지 않으면 나중에 연속값이 새어 나간다(§8.3 공학 확정).
붙일 때 그 함수만 쓰면 된다.

### A-4 인벤·스킬·제작 패널 내용 (§8.1·§8.4·§8.5)
프레임만 이번이다. ★**다음 패널 구현자를 위한 3줄**은 `다음세션_인계.md` 3-v 절에 적어 뒀다.
⚠**스킬 패널은 지금 하드코딩 프로토타입**이다(`PROTO_SKILLS` — "UI only, hardcoded values").
빈 버튼은 아니지만 **가짜 값을 보여 준다**. §8.4 의 "이력서 패널"로 갈 때 통째로 갈아야 하고,
그 첫 데이터는 이미 쌓이고 있다(낚시 `fishStats`: 마릿수·최대·놓친 최대).

### A-5 MSY 상한 A/B · 낚시 손잡이 튜닝
전자는 `회부_MSY상한_사장.md`. 후자는 **재민 실기 판정 대기**라 이번에 손대지 않았다(지시 §6).

## B. 만들며 드러난 판단거리 (재민 결정 필요)

### B-1 ★**식사가 아직 마을 경제에 안 닿는다** — §7 의 절반
§7 의 채택 이유가 "플레이어 식사 = **마을 재고의 진짜 _cons**(NPC 소비와 대칭)"인데,
지금 플레이어는 **자기가 주운 것만** 먹는다. 실측: 마을에서 식품을 사는 경로가 **없다**
(`doShopBuy` 는 장비 상점이고, 게시판 의뢰는 **플레이어가 주는** 방향뿐이다).
⇒ 판단: ⓐ 장마당에 식품 매대를 여는가(새 상점 UI — 이번 배치 금지 대상이었다)
ⓑ 게시판 의뢰의 **역방향**(마을이 파는 것)을 사건 레이어 다음 층에서 여는가
ⓒ 곳간 인출 권한(마을원이면 꺼내 먹기)으로 푸는가.
★ⓒ가 가장 싸고 세계관에 맞아 보인다(마을원이 곳간에서 먹는 건 자연스럽다) —
그러면 `_cons` 반영도 기존 `playerVillageDeposit` 의 역경로 하나로 끝난다.

### B-2 부상의 세기 — 늑대 한 대는 "긁힘"이다
실측: 늑대 한 대(15 피해) → 부상 **0.060**, 1단계 문턱 0.228 ⇒ **무증상**.
네 대쯤 맞아야 1단계다. 이건 "잔타는 안 다친다"는 설계 의도의 결과지만,
§2 가 말한 "위험의 이빨"로 충분한지는 **실기 판정**이 필요하다.
손잡이: `BODY_INJURY_DMG`(문턱 12) · `BODY_INJURY_PER_DMG`(0.02/점).
⚠문턱을 낮추면 잔타도 다치게 되고, 배율을 올리면 한 방에 반신불수가 된다. **둘을 같이** 보라.

### B-3 겨울이 얼마나 매워야 하나
실측: 맨몸·한겨울 밤이면 추위 1단계가 **6.5분**, 식사가 2끼/시간 → **4끼/시간**으로 뛴다.
"겨울 준비(옷·불·비축)"라는 판단을 만드는 건 의도인데, 그 세기가 적당한지는 실기 판정이다.
손잡이: `BODY_COLD_RISE_SEC`(600) · `BODY_COLD_SEASON_W`(1.0) · `BODY_COLD_HUNGER_EXTRA`(0.6).

### B-4 이속 배율과 넷코드
몸 상태가 걸음을 늦추므로 **서버와 클라가 같은 배율을 써야 한다**(안 맞추면 러버밴딩).
지금은 `gauges.body.moveMult` 로 실어 보내고 클라 `predictStep` 이 같은 수를 쓴다.
⚠**그 값이 도착하기 전 한두 틱**은 클라가 1 을 쓴다(호환 기본값). 지금은 감쇠가 느려 무해하지만,
급격히 변하는 축(전투 층의 공포 등)을 붙이면 그 틈이 보일 수 있다. 그때 재검토하라.

### B-5 무들 아이콘은 이모지 **발판**이다
§8.3 이 요구한 "아이콘 3~4단계"를 이모지로 냈다(새 아트 제작이 이번 범위 밖이라).
교체 대상임을 `style.css` 주석에 적어 뒀다. 아트가 오면 `#moodles` 의 `.mo-emo` 만 갈면 된다.
