# 산 — 왜 엉터리였나, 무엇을 고쳤나 [2026-08-07]

재민 제보 두 줄이 이 문서의 전부다.

> *"산이 완전 엉터리야.. 산이 없고 색칠만 되어 있는 산 타일이 많고,
> 산의 서쪽이나 북쪽에 있어서 화면에 가려질 때에는 산은 투명해져야 해"*
> *"갈색 타일이 보이면 안 될 정도로 산이 덮어야지.. 게다가 검은색 띠는 대체 왜 있는 거야?"*

세 가지가 나왔다. **두 개는 한 뿌리**였고, 하나는 별개였다.

---

## 0. 결론 먼저

| 제보 | 원인 | 상태 |
|---|---|---|
| 색칠만 된 산 타일 | 시안을 **게임이 쓰지 않는 배율**로 판정 → 배치가 밴드를 못 덮음 | 시안 C 완성 · **재민 판단 대기** |
| 갈색 타일이 보인다 | 위와 **같은 뿌리** | 위와 동일 |
| 검은 띠 | 전장의 안개(안 가 본 땅). 산이 덮으면 바위 위 띠는 사라지고 **풀 위 띠는 남는다** | 설명 완료 · 취향 3안 회부 |
| 산이 나를 가린다 | 플레이어 z 편향 +500 밖의 큰 산이 화면 위로 뻗어 덮음 | **고침 · 하네스 6/6 · 커밋 완료** |

---

## 1. 원인 — 12차의 시안 왕복이 전부 틀린 배율에서 이뤄졌다

`scripts/mock-mountain.js:155`

```js
const scale = 0.22;
```

시안은 장면을 **22%로 축소**해 보여줬다. 게임은 **줌이 없다** — 1 iso px = 1 화면 px 다.
같은 배치를 배율 1.0 으로 다시 뽑으니 **라이브 그림이 그대로 재현됐다.**

⇒ 스프라이트도, 앵커 JSON 도, 배치 로직도 **시안과 라이브가 md5 동일**하다.
   구현은 충실했다. **판정 조건이 틀렸다.**

### 그래서 실제로 무슨 일이 벌어졌나

능선 밴드는 실제로 **중앙값 53셀**이다. 현행 배치는 그 폭을 스프라이트 **한 장**에 맡긴다:

```js
const scW = Math.max(0.6, bc.n / MT_CROSS_U * 0.96);   // 53/10.1 ≈ 5.0
```

배율 중앙값 **5.04**, 99.6%가 1.6배를 넘는다. 한 장으로 53셀을 덮으려니 5~9배 확대다.

## 2. ★두 번째 원인 — 단위당 화소가 스프라이트마다 다르다

`bake-mountain.py` 는 `ortho_scale = dim*1.3` 으로 **메시 크기에 맞춰 카메라를 벌린다.**
해상도는 전부 512 로 같은데 담는 월드 폭이 다르니 **px/단위가 제각각**이 된다:

| | ppu (512 굽기) | 화면이 요구하는 값 | 결과 |
|---|---|---|---|
| `mt_S1` (잔 봉우리) | **60.9** | 45.25 × 배율 | 배율 1.34까지 **선명** |
| `mt_M1` (매듭) | 38.2 | 〃 | 배율 0.84까지 |
| `mt_G0v0` (산맥 본체) | **24.5** | 〃 | 배율 **0.54**까지 |

⇒ **작은 봉우리는 처음부터 선명했고 큰 산맥만 흐렸다.**
   그런데 배치는 **큰 산맥을 제일 많이 확대**했다(5.8배). 벌이 두 번 겹쳤다.

이건 배율을 어떻게 정하든 남는 문제라 **먼저 고쳐야 한다.**

---

## 3. 고친 것 ① — 산이 나를 가리면 그 자리만 뚫는다 (완료)

재민 관찰이 정확했다. 실측으로 이유가 나온다:

- 플레이어 renderable z 에는 **+500 편향**이 있다 → **31셀 안쪽** 산은 이미 내 뒤로 간다.
- 문제는 그보다 **멀리 남동쪽**에 있는 큰 산이다. 발치는 화면 아래인데 **몸통이 위로
  2000px 넘게** 뻗어 올라와 나를 덮는다.
- 즉 **"내가 산의 서/북쪽"일 때만** 생긴다 — 재민이 말한 그대로다.

**숨기지 않고 뚫는다.** 집 지붕은 '미표시'(좀보이드 문법)지만 재민 지시는 *투명*이고,
산이 통째로 사라지면 지형이 없어져 방향 감각이 깨진다. 산은 남고 내 자리만 비운다.

### ★판정은 상자가 아니라 알파로

스프라이트 프레임의 **86%가 투명 여백**이다(`pad=1.3` 정사각에 삼각형 하나).
상자로 재면 닿지도 않은 산이 흐려진다 — 실제로 이 덫에 빠졌고, 상자 판정은
가림 자리를 **149곳을 2640곳으로** 부풀렸다.

### 하네스 `e2e-mtocc` 6/6 (대조군 = `occOff` 손잡이 · 같은 세션 · 같은 시계)

| 판정 | 수 |
|---|---|
| ① 가려지는 자리를 찾았다(반례 성립) | 가린 산 1장 |
| ② 대조군에서 내 자리가 산에 덮여 있다 | 차 27.7 |
| ③ 구멍이 뚫렸다 | 차 19.4 |
| ④ 뚫은 쪽이 "산 없는 그림"에 더 가깝다 | 8.3 < 27.7×0.7 |
| ⑤ ★구멍 **밖** 산은 손대지 않았다 | 밖 차 0.00 · 그 상자 산 함량 122.1 |
| ⑥ 산 반대쪽으로 가면 가림이 준다 | 1장 → 0장 |

> ★하네스 덫 13번 — ⑤의 '구멍 밖 상자'를 눈대중으로 잡았더니 산이 **한 화소도 없는
> 빈 하늘**이 걸렸다. 그러면 ⑤는 무엇을 재도 통과하는 자명 판정이 된다.
> 판정을 완화하지 않고 **상자를 측정으로 고르게** 계측기를 고쳤다.

---

## 4. 시안 — 덮개 [★재민 판단 필요]

### 규격 (눈이 아니라 수)

> **시야 안 바위 셀은 전부 산 스프라이트 아래 있어야 한다.**
> 통과선 = 맨 바위 셀 **2% 아래**. 라이브 실측 출발점은 **70.9%**.

### 후보 C — 크기를 "밴드 폭"이 아니라 "가장자리까지의 거리"로 정한다

현행은 밴드 폭을 한 장에 맡겨 5~9배 확대를 만든다.
C 는 **바위 덩어리 가장자리까지의 거리**로 계층을 나눈다:

- **안쪽**(거리 ≥ 9셀) → 큰 봉우리, 성기게
- **어깨**(≥ 3.5셀) → 중간
- **자락**(< 5셀) → 잔 봉우리, 촘촘히
- 높이 배율도 같은 거리에 태운다 → 실루엣이 저절로 **높은 등성이 · 낮아지는 어깨 · 잔 자락**
- 남은 틈은 **알파로 찾아** 메운다(눈이 아니라 측정)

### 측정 (장면 둘 · 1:1)

| 장면 | 바위 비중 | 후보 | 장수 | **맨 바위** | 화면 내 | 덮어쓰기 |
|---|---|---|---|---|---|---|
| 굽이 | 99.5% | A 현행 | 9 | **20.0%** | 9장 | 167MP |
| 굽이 | 99.5% | **C 계층** | 129 | **0.0%** | 99장 | 222MP |
| 마을옆 | 48.3% | A 현행 | 8 | **39.9%** | 8장 | 43MP |
| 마을옆 | 48.3% | **C 계층** | 84 | **0.0%** | 62장 | 69MP |

★**마을옆 장면이 중요하다.** 빽빽한 능선에서만 되는 덮개는 고친 게 아니다.
  바위가 절반뿐인 자리에서도 C 는 0.0% 다.

### 아직 남은 것 — 선명도

위 시안은 **아직 흐리다.** §2 의 px/단위 문제 때문이다.
지금 45장을 다시 굽고 있다(같은 씨앗 · 같은 재질 — 재현성 검증 완료:
평균 화소차 0.076, 앵커 동일). 굽고 나면 `pack-mountain.py` 가
**px/단위를 한 값으로 맞추고 · 투명 여백을 잘라내고 · webp 로 묶는다.**

실측한 포장 이득:

| | 지금 (512 PNG) | 포장 후 (2048 → 자름 → webp) |
|---|---|---|
| 스프라이트 한 장 | 150KB | **25KB** |
| 45장 합계 | 6.7MB | **~1.1MB** |
| 불투명 화소 비중 | 14% | **~100%** |

⇒ 더 선명해지면서 **파일은 6배 작아지고**, 매 프레임 `drawImage` 가 훑는 화소도 준다.

---

## 5. 검은 띠 — 설명 [취향 3안 회부]

세 번 재서 같은 답이 나왔다. 그 띠는 **전장의 안개의 '안 가 본 땅' 채움**이다.

- 색이 정확히 `rgb(0,0,0)` — 배경은 `#0a0d10`, UI 는 `rgb(15,19,23)`. **둘 다 아니다.**
- 경계 기울기가 정확히 **2px/행** = 셀 다이아 계단(iso 격자).
- 안개 코드는 배치 19 이전과 **0줄 차이** — 새로 생긴 게 아니다.
- 실측 화면에서 순수 검정이 **31.8%**.

**바위 위 검은 띠는 덮개를 고치면 사라진다**(산이 안개 위에 그려지므로).
**풀 위 검은 띠는 남는다** — 그건 그냥 안 가 본 땅이다.

그래서 남는 질문은 하나뿐이고, 이건 취향이라 구현하지 않고 회부한다:

| 안 | 내용 |
|---|---|
| ① | **현행 유지** — 순수 검정 · 계단 경계 |
| ② | 검정은 그대로, **경계만 흐림** — 계단만 없앤다 |
| ③ | **배경색 `#0a0d10`** + 경계 흐림 — "칠해진 검은 물체"가 아니라 "아직 안 그린 세상"으로 읽힌다 |

목업: `scripts/mock-fogband.js` → `/tmp/fog-1..3.png`

---

## 6. 곁가지로 드러난 것 — 산이 안개 위에 그려진다

`client.js` 안개 마스크는 5552행, `mtseg` 는 5636행. **산이 안개보다 나중에 그려진다.**
⇒ **안 가 본 산도 훤히 보인다.** 그리는 순서의 부작용이지 의도가 아니다.

덮개를 고치면 이게 더 두드러진다(산이 더 많은 화면을 차지하므로).
★재민 판단 필요: 안 가 본 산도 보이는 게 맞나, 안개에 묻혀야 하나.

---

## 7. 내가 틀렸던 것

- **배치가 시안과 다르다고 했다 — 틀렸다.** 시안의 배율 분포를 직접 재 보니
  중앙값 5.80 으로 라이브와 같았다. 코드를 고치기 전에 재서 잡았다.
- **가림 시험 자리를 상자로 골랐다 — 틀렸다.** 알파를 안 봐서 149곳을 2640곳으로 셌고,
  그중 최상위 자리에서 하네스가 "가림 0장"을 냈다. 계측기를 고쳐서 잡았다.
