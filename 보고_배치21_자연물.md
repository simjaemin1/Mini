# 보고 — 배치 21: 자연물 실장 (물가 술 군락 · 나무 12종·수풀 재렌더 · 들꽃 산포)

> **재민이 먼저 할 일 — 배포**
> ```
> bash scripts/redeploy-hanbando.sh --central
> ```
> `public/` 만 바뀌었다(클라 스크립트 + 에셋). 서버 로직·DB·econ 무접촉이라 **리셋 불필요**.
> 배포 뒤 브라우저 강력 새로고침(⇧⌘R) — 스프라이트가 캐시에 남아 있으면 옛 나무가 보인다.

배치 19 가 A(지면 질감)·B(물 셰이더)를 실장하고 **C(자연물)를 이월**했다. 이 배치가 그 C 다.
만진 곳은 셋뿐이다: **①스프라이트 에셋 교체 ②물가 술 산포 ③들꽃 산포**.
`client.js` 의 **타일 루프·산 세그먼트 블록은 손대지 않았다**(배치 20 B 병행 대비).

---

## 0. 한눈에

| 항목 | 결과 |
|---|---|
| 신설 하네스 `scripts/e2e-nature.js` | **43 통과 / 0 실패** |
| 기존 회귀 | terrain · rooms · cutaway · metallurgy · village · guest — §5 표 |
| econ 무접촉 | `wiring-check` 0 · `econ-regress` **비트 동일**(v2 5시드 인구 2,616 · 무기Q 651 · 확장셀 13,473 · 소멸 0/30) · `econ-lab-real 800` **인구 6,280 · 소멸 0/51** |
| 새 에셋 | 38장 (나무 12 교체 · 수풀 6 교체 · 풀숲 6 교체 · **신설** 풀포기 4 · 갈대 3 · 부들 3 · 들꽃 4) |
| `rock`·`mossrock` | **교체 안 함**(질감 사실풍이라 안 튄다) |
| `ore` 광맥 | ★**노두(露頭)로 재생성** — 재민 확정 §6-b (바위 + 은은한 광물 변색, 결정 표식 제거) |
| 엔티티 시야 | ★**재정의** — 식물·무생물·시체·바닥템은 항상 그려진다 §6-b |
| 안개 | ★**안 가본 곳엔 그 어떤 것도 안 보인다** — 개체 단위 게이트(발자국 판정) §6-c |
| 물가 절단선 | ★**풀 넘김** — 뭍의 자기 풀을 물 위로, 물보다 나중에 §6-d → **재민 반려, 기본 OFF** |
| ★물가 "테두리" | ★★**진범 = 수면이 뭍 위로 11px 흘러넘침**(배치 19 가 `discard` 한 줄을 빠뜨림). **수리** §6-e |
| ★물가 여백 | ★★**풀이 칼로 잘리던 직선 해소** — 물 위가 아니라 **뭍 쪽으로** 판 젖은 모래 여백. 새 레이어 0장 §6-f |
| ★바람 흔들림(포기) | 술·들꽃이 **진행하는 파**로 흔들린다(+0.24ms/f). 날씨는 게임에 **없다** — 주입구만 세웠다 §6-g |
| ★★바람 흔들림(**카펫**) | ★재민 정정 — 흔들려야 할 건 **초원에 깔린 지면 풀**이었다. 바탕/잎 분리 + 띠 blit, **+0.53ms/f**, 무풍이면 옛 그림과 **항등** §6-h |

---

## 1. 에셋 — `scripts/nature_render.py`

씬 정본은 `building_render.py` 머리말 그대로다. **한 줄도 바꾸지 않았다**:
Cycles · `film_transparent` · ORTHO · 태양 52°/−35° energy 3.6 · 월드 (0.52,0.56,0.6)@0.55 ·
SAMPLES 64 · OIDN 부재 자동 감지 · PPU 45.255 · ZSQ 0.8165 · 좌우 FLIP.

**크기 규약 = 1셀 1m.** 모델을 미터로 짓고 `render()` 가 화면 px 로 환산한다(1m 높이 = 32 게임px).
슈퍼샘플은 나무 ×4, 소품 ×3 이고 `nature_anchors.json` 에 **실제 ppu 를 적어** 클라가 되돌려 축소한다
(산 `mountain_anchors.json` 과 같은 규약). 후처리는 `scripts/nature-postprocess.py`
— 알파 문턱 10 으로 크롭하고, 크롭 뒤 좌표계의 `{w,h,ox,oy,ppu,gw,gh,m}` 을 내보낸다.

### 수종 (청동기 후기 중부 한반도)

| 파일 | 종 | 실측 높이 | 형태 근거 |
|---|---|---|---|
| tree01·02·03 | 소나무 *Pinus densiflora* | 5.5 / 4.8 / 7.2 m | 굽은 줄기 · 위로 몰린 판상 우산 수관 · 붉은 판상 수피(Voronoi) |
| tree04·05 | 잣나무 *P. koraiensis* | 5.2 / 4.3 m | 곧은 줄기 · 원뿔 5층 · 짙은 청록 |
| tree06·07·08 | 참나무 *Quercus* | 5.7 / 4.8 / 6.3 m | 굵고 짧은 줄기 → 4가지 분기 · 넓은 둥근 수관 |
| tree09·10 | 밤나무 *Castanea crenata* | 5.8 / 4.6 m | 곧은 줄기 · 세로로 긴 타원 수관 · 긴 잎 |
| tree11·12 | 버드나무 *Salix koreensis* | 3.7 / 3.6 m | **물가 수종** · 기운 줄기 · 늘어진 가지 22줄 |

물가 술은 **갈대 1.6~3.3m · 부들 1.4~2.4m** 로 굽혔다. 지시대로 풀대로 때우지 않았다 —
갈대는 원추화서(깃털)를, 부들은 갈색 원통 이삭과 칼 모양 잎을 실제로 모델링했다.

### ★실측으로 고친 것 — 전부 비교 프레임 A/B 로 잡았다

렌더는 **한 번에 안 나왔다**. 네 번 틀렸고, 네 번 다 *실화면 대조*가 잡았다.

1. **변위 이코스피어 = 초록 베개.** 잎 무리를 이코스피어 + 구름 변위로 만들었더니
   게임 해상도(나무 130px)에서 실루엣이 **매끈한 공**이 됐다. → 잎을 **작은 카드 수백 장**으로
   바꿔 껍질에 흩뿌렸다(전부 한 메시로 합쳐 렌더 비용은 그대로).
2. **★그 카드의 법선이 옆을 보고 있었다.** 카드의 *길이축*을 반지름 방향으로 잡는 바람에
   면 법선이 접선 방향이 됐다 — 위에서 오는 태양을 **한 장도 정면으로 못 받아** 캐노피가
   통째로 납작하고 어두웠다. 법선을 바깥/위로 돌리자 비로소 윗면이 밝고 아랫면이 그늘졌다.
3. **월드 앰비언트의 파랑이 초록을 탈색시킨다.** 1패스 실측 — 새 나무 평균 RGB (72,89,62) vs
   지면 질감 `grass_angled` (64,82,47). **B 가 15 높았다.** 청회색 월드(0.52,0.56,0.6)를 위로 향한
   면이 많이 받는 데다 sRGB 인코딩이 낮은 채널을 크게 들어올린다. **월드·태양 정본은 손대지 않고**
   재질 알베도에서 파랑을 0.74배, 초록을 1.66배로 되밀었다.
4. **풀포기가 용설란이었다.** 잎이 두껍고 뻣뻣해 로제트(agave)처럼 보였고, 폭이 2~3셀이라
   풀이 아니라 관목이었다. → 얇게·많이(33~38장)·속잎 추가, 높이 0.5~0.9m 로 하향.

증거: `증거_배치21/자연물_스프라이트_시트_실크기.png`(실제 게임 크기로 38장 전부),
`증거_배치21/나무교체_전후_스폰_농촌22.png`.

---

## 2. 물가 술 — 왜 있고, 어떻게 안 심은 티를 냈나

**존재 이유는 절단선이다.** 물가에서 지면 풀 텍스처가 수직으로 잘린다. 술은 그 선 위로 넘어와
경계를 흐트러뜨리는 게 일이다 — 그래서 **물 위로 민다**(셀 반폭 16px 기준 12~28px 밀어
수면 위로 최대 12px). 실측: **수면 픽셀 90,902 중 19,933(21.9%)이 술로 덮인다.**

**재민 지적("변 전체 균일 밀식 = 일부러 심은 느낌")을 깨는 건 밀도가 아니라 빈 구간의 존재다.**

- 파장 **5셀** 값 노이즈로 밀도를 저주파 변주한다. **문턱(0.34) 아래는 한 포기도 안 선다.**
  문턱 위에서 0→1 로 부드럽게 빽빽해진다 → 군락과 공백이 셀 몇 개 단위로 교대한다.
- 물가 안쪽 **1~2셀 감쇠 산포**(2.8 → 1.3 → 0.5 포기/셀). 물가 선에만 몰리면 목걸이가 된다.
- 포기별 크기(×0.66~1.06) · 높이(×0.86~1.16) · 종 변주. 갈대·부들은 **물과 맞닿은 셀의
  빽빽한 구간(q>0.62)에만** 선다 — 저습지 군락의 고증이다.
- **전부 자리 해시.** `Math.random` 0회. 시각을 고정하면 두 프레임이 **픽셀 단위로 동일**하다.

정량화(§4 ⓑ): 물가 회랑 48블록의 술 밀도 **CV 1.21** vs **균일(포아송) 산포의 기대 CV 0.08**.
빈 블록 **31%**. 균일 밀식이면 CV 가 0.08 근방이고 빈 블록은 0 이다.

### 그리는 순서 = 계약

```
물 셰이더(WebGL) → _drawPrisms(단면) → ★술·소품 → 안개 마스크
```
★**안개 마스크 합성 직전**에 전용 패스로 그린다(자체 z 정렬). 1패스는 `renderables` 에 태웠는데
그게 §6-a 의 결함이었다 — 마스크 합성이 엔티티 렌더 **앞**이라 자연물이 안개 위로 떠올랐다.

### 비용

술은 개수가 많다(강가 화면 633 포기). 그래서:
- **8×8셀 청크 단위 정적 캐시**(지형은 정적 — 무효화는 `_waterCellCache.clear()` 3곳과 같은 지점).
- 청크당 물 판정을 **14×14 로 한 번만** 훑는다. 셀마다 7×7 재조회하면 청크당 3,136회인데 196회로 끝난다.
- 뷰포트 컬링(`NAT_VIEW_PAD` 1500px).

---

## 3. 들꽃·풀숲 — 회피는 "이미 클라에 온 값"만 쓴다

초원 소품은 자리 해시 문턱(0.978~0.923, 저주파 변주)으로 낮게 깔았다 —
스폰 지점 농촌22 광장이 첫인상이라 과밀은 금물이다(실측 시야 내 160개).

**회피 대상은 새로 판정하지 않는다.** 길(`c.roads` 셀 키) · 사유지(`c.claims` 사각) ·
마을 영토(`simVillages` 중심+반경) · 경작지/건축물(`c.buildings` 셀) — 전부 서버가 이미 보내는
값이다. 자리는 정적이라 캐시하고 **회피만 수집 시점에** 건다(길·사유지는 생겼다 없어진다).

---

## 4. 검증 — `scripts/e2e-nature.js` (신설) **43 / 0**

```
[0] 자산 규격        나무 12종 앵커 · 파일 존재·치수 일치 · 성목 3~8m · 술 10종 · 갈대/부들 >1.3m
[1] 계약             술 14종 로드 · 강가 술 633 포기 / 초원 0.3% · 자산 404 0 · 스크립트 예외 0
[2] ⓐ 은폐          수면 90,902px 중 19,933(21.9%) 피복  ↔ 반례: 초원 2px
[3] ⓑ 군락          회랑 48블록 CV 1.21 vs 균일 기대 0.08 · 빈 블록 31%
[4] ⓒ 나무          12장 디코드 · 스프라이트 경로 draw 840회 · 그린 높이 = h×1.3 규약 · 높이 7단계
[5] ⓓ 회피          소품 160개 중 위반 0  ↔ 반례: propNoAvoid 로 끄면 298개 중 138 위반
[6] ⓔ 결정론        시각 고정 두 프레임 차이 0px · 손잡이 on/off 차이 43,632px(술)·6,212px(소품)
[7] ⓕ 안개          그린 개체 중 '안 본 셀' 위 = **0**  ↔ 반례: fogGateOff 로 끄면 강가 1·초원 2 위반
                    지면 데코도 미탐사 위 밝은 픽셀 ON−OFF = 0 · 자리(wx,wy) 없는 종류 0
```

**자명 통과를 막은 장치**

- ⓐ 는 "초록 픽셀이 있다"가 아니라 **"술을 끄면 물이던 픽셀이 켜면 초록이 된다"**를 잰다.
  같은 프레임·같은 시계에서 손잡이로만 갈라 얻으므로 에셋을 되돌린 "before"(다른 세계)가 아니다.
- ⓑ 는 임의 문턱이 아니라 **균일 산포가 낼 CV(=1/√λ)** 와 대조한다. 과분산이어야 군락이다.
- ⓓ 는 **하네스가 회피 판정을 다시 짜지 않는다**(그게 사본이고 자명 통과다 — 배치 7 오진의 형태).
  `window.__natProbe()` 가 **원자료**(길 셀·사유지 사각·마을 원·경작지 좌표)를 그대로 내보내고
  하네스가 독립 재계산한다. 그리고 `propNoAvoid` 로 회피를 끄면 **위반이 실제로 나온다** — 판정이
  일하고 있다는 증거다.
- ⓒ 는 "로드됐다"가 아니라 **draw 호출수와 그린 픽셀 높이**를 잰다.

### ★1패스 실사고 — 하네스가 잡았다

`_natCollect` 가 AOI 상수 `VIEW_RADIUS` 를 참조했는데, 그건 **렌더 함수 안의 지역 상수**였다.
매 프레임 `ReferenceError` → **엔티티 패스가 통째로 죽었다.** 그런데 지면·물·프리즘은 그 앞에서
이미 그려져서 **스크린샷은 그럴듯했다**(플레이어가 없다는 것만 이상했다).
`window.__natDbg` 가 `undefined` 인 걸로 잡혔고, 하네스에 **"`__natDbg` 존재 = 렌더 패스 생존"**
판정을 넣어 재발을 가드했다. 배치 18 의 "계약도 실행도 멀쩡한데 화면에서 도달 못 하는 층"과 같은 계보다.

---

## 5. 기존 회귀 — 전부 순차 단독 실행

| 하네스 | 결과 | 비고 |
|---|---|---|
| `lab-wiring-check` | **0** | |
| `econ-regress` (v2 5시드) | **비트 동일** | 인구 2,616 · 무기Q 651 · 갑옷 149 · 확장셀 13,473 · **소멸 0/30** |
| `econ-lab-real 800` | **인구 6,280 · 소멸 0/51** | 기준선과 일치 |
| `e2e-rooms` | **55 / 0** | |
| `e2e-cutaway` | **15 / 0** | "빈 땅" 대조군 오염 없음(예고됐지만 안 터졌다) |
| `e2e-metallurgy` | **20 / 0** | 광맥 노두 교체 후에도 채광 사슬 무변 |
| `e2e-village` | **32 / 0** | |
| `e2e-guest-reconnect` | **40 / 0** | |
| `e2e-terrain` | **27 / 0** | §5-a — 계측 격리가 필요했다 |

★§6-a(안개 순서 수리)·§6-b(엔티티 시야 재정의 · 광맥 노두)까지 반영한 **최종 트리에서 7종 전수 재실행**:
`cutaway 15 · rooms 55 · nature 43 · terrain 27 · metallurgy 20 · village 32 · guest 40`
= **232 판정 0 실패**(순차 단독). 특히 `rooms`·`cutaway` 의 "빈 땅 대조군"이 그대로 통과했다 —
자원을 항상 그리게 바꿨는데도 대조군이 오염되지 않았다.

### ★5-a. e2e-terrain — 내 층이 남의 계측기를 오염시켰다 (수리함)

1패스에서 `e2e-terrain` 이 **25 / 2** 로 떨어졌다. 깨진 건 물 흐름 판정 둘:

```
✗ ★★물이 움직인다 (이동 0.0px ≥ 3)
✗ ★★그 이동이 하류 방향이다 (흐름축 투영 0.00 > 2.0)
```

**물은 멀쩡하다.** 원인은 하네스의 계측기다. `bestShift` 는 "물로 분류된 픽셀"을 골라
두 시각 사이 최적 정합 이동을 찾는데, 배치 21 의 술이 **수면을 21.9% 덮으면서 그 위에
정지한 풀 가장자리**를 만들었다. 어떤 이동을 시도해도 정지 픽셀이 SAD 를 때려 최적 이동이
(0,0)으로 눌린다. 배치 19 보고 §5-b 가 예고한 "대조군 오염"이 cutaway·rooms 가 아니라
**terrain 의 물 흐름**에서 터진 것이다.

**판정은 한 글자도 완화하지 않았다**(기준 그대로 ≥3px · 투영 >2.0). 계측 대상 층만 격리했다 —
`e2e-terrain` 은 이제 `__terrain19.natOff = true` 로 잰다. 그리고 **왜 그래야 하는지를 숫자로
남겼다**: 하네스가 자연물 ON 상태의 이동도 함께 측정해 `[ⓞ 계측 격리]` 로 출력한다
(주석만 믿지 말라는 뜻이다). 자연물 층 자체의 회귀는 `e2e-nature` 43판정이 따로 본다.

격리 후 실측(같은 하네스, 같은 셀):

```
[ⓞ 계측 격리]  자연물 ON 으로 재면 강가 물 이동 = (0,0) |0.0|px   ← 오염
[ⓑ ① 흐름]     natOff 로 재면 (-7,4) SAD 2.6(무이동 3.1) · 흐름축 투영 7.99 > 2.0  ✓
=== 지형 실장 E2E: 27 통과 / 0 실패 ✅ ===
```

> 부수 효과로 `e2e-terrain` 의 지면 질감 판정은 **더 엄격해졌다** — 술·소품이 만드는 색 변화가
> 빠져서 순수한 지면 텍스처만 재게 됐다. 그래도 27/0 이다.
>
> ※ 배치 시작 직전에 잰 기준선은 26/1 이었는데, 그 1 실패(지면 성능)는 **내가 같은 컨테이너에서
> Blender 를 동시에 돌린 탓**이었다. 단독 순차 실행에서는 이번에도 `×0.01` 로 통과한다.
> 2코어 컨테이너에서 **E2E 는 반드시 단독으로** — 배치 20 인계의 경고가 성능 판정에도 그대로 적용된다.

---

## 6. 취향 갈림 — 시안 3안, 그리고 실측이 고른 답

`증거_배치21/시안_물가술_밀도바닥_A0_B025_C05.png`

물가 술의 **밀도 바닥값**(`__terrain19.frFloor`)이 유일한 취향 갈림이었다.

| 안 | 값 | 화면 | 술 수 |
|---|---|---|---|
| **A (채택·기본)** | 0 | 빈 구간은 **완전히** 빈다. 군락 대비가 가장 세다. 대신 빈 구간에서는 절단선이 보인다 | 633 |
| B | 0.25 | 빈 구간에도 최소 밀도. 절단선이 **어디서나** 덮이고 군락도 남는다 | 865 |
| C | 0.5 | 물가 전 구간이 밀식에 가까워진다 — "일부러 심은 느낌"이 되돌아온다 | 1,086 |

**눈으로는 B 가 제일 낫다.** 절단선을 어디서나 덮으면서 군락 대비도 살아 있다.
그래서 B 를 채택하려고 **재 봤더니 — 떨어졌다**:

```
A:  CV 1.21 (균일 기대 0.08) · 빈 블록 31%   → e2e-nature ⓑ 통과
B:  CV 0.89 (균일 기대 0.07) · 빈 블록 15%   → ✗ 빈 블록 ≥18% 미달 (31 통과 / 1 실패)
```

`빈 블록 ≥18%` 는 재민이 글로 못박은 **"빈 구간이 셀 몇 개 단위로 교대"** 를 그대로 옮긴 판정이다.
**내 취향을 통과시키려고 판정을 낮추지 않았다.** ⇒ **A 를 기본값으로 간다.**

되돌리는 비용은 0 이다 — 재민이 B 가 낫다고 보면 `client.js` 의 `frFloor: 0` 을 `0.25` 로
한 글자 바꾸면 된다(재렌더·재배포 외 작업 없음). 그때는 `e2e-nature` 의 ⓑ 빈-블록 판정도
**함께 재정의해야 한다** — 판정만 낮추는 건 금지다.

---

## 6-a. ★수리 — 자연물이 "한 번도 못 본 셀" 위로 떠 있었다 (재민 지적)

**증상**: 미탐사(새까만) 셀 위에 풀·꽃이 그대로 보인다.

**원인은 그리는 순서다.** 안개 마스크 합성은 **엔티티 렌더 앞**에 있다(`14.49-e7ae` — 엔티티가
마스크 위에 오게 하려고 일부러 그렇게 뒀다). 1패스에서 자연물을 `renderables` 에 태웠더니
(배치 20 산 세그먼트 본보기를 그대로 따랐다) 자연물도 **마스크 위**로 올라갔다.

**왜 이 층에서만 새로 터졌나 — 실측이 답한다.** 서버 엔티티(나무·건물·NPC)는 AOI **650px**
안에서만 오고, 그 범위는 사실상 항상 '본 곳'이라 이 결함이 드러날 일이 없었다. 그런데 자연물은
**클라가 1,500px 까지 스스로 만든다**(`NAT_VIEW_PAD`) — 미탐사 영역까지 뻗는다.

**실측**(`/tmp/fogprobe.js` — 판정 정본은 `window._shadowMask` 의 알파. 알파 248↑ = 미탐사):

| | 미탐사 픽셀 | 그 위의 밝은 픽셀 |
|---|---|---|
| 수리 전 · 자연물 ON | 450,566 | **1,142** |
| 수리 전 · 자연물 OFF | 450,566 | 9 |
| **수리 후 · 자연물 ON** | 450,566 | **9** (= OFF 와 동일) |

즉 떠 있던 픽셀의 **99.2%가 이 층**이었고, 수리 후 **0** 이 됐다.

**수리**: 자연물을 **안개 마스크 합성 직전**으로 옮겼다. 그러면 지면과 **똑같은 3단계**를 그대로 받는다 —
미탐사 = 완전히 가려짐 · 봤지만 시야 밖 = 지면과 같은 20% 어둠 · 시야 안 = 밝음.
배치 19가 남긴 **"지면 데코는 안개 마스크 앞에 그려라"** 계약이 바로 이 뜻이었다(내가 배치 20의
z 정렬 본보기를 우선해서 어긴 것이다. 두 지침이 충돌할 때 **안개 계약이 이긴다**).

**대가**: 엔티티가 항상 자연물 위에 그려진다(사람이 갈대 뒤에 서도 앞으로 나온다).
풀·꽃은 지면 데코라 이 편이 낫다 — 산 세그먼트처럼 큰 물체였다면 반대였을 것이다.

**회귀 가드**: `e2e-nature` 에 ⓕ 판정 2개 추가(강가·초원) — 자연물 ON/OFF 의 '안개 위 밝은 픽셀'
차이가 40 이하여야 한다. 판정 정본은 하네스가 시야를 다시 계산하지 않고 **마스크 알파를 직접 읽는다**.

### 곁가지 — 안개 3단계 규격 (재민 질문에 대한 코드 실측 답)

- **한 번도 못 본 셀**: 완전 검정 alpha 1.0 → 아무것도 안 보인다. **셀 단위**(iso 다이아몬드).
- **봤지만 지금 시야 밖**: alpha 0.2(검정 80% 제거) = 살짝 어둡다. 역시 **셀 단위**.
- **지금 보이는 곳**: alpha 0 = 밝다. ★**여기만 셀 단위가 아니다** — 벽·나무·산을 광선으로 자른
  **가시성 폴리곤**이라 경계가 계단이 아니라 매끈한 직선/부채꼴이다.
- '봤다' 기록은 가시성 폴리곤을 1px=1셀 격자에 래스터화해 **커버리지 25% 이상**인 셀만 남긴다
  (`_seenChunks`, 16셀 청크). 기록 반경 48셀.

---

## 6-b. ★재민 확정 — 엔티티 시야 규칙 재정의 · 광맥은 노두(露頭)로

§6-a 를 계기로 **엔티티 시야가 안개와 별개 시스템**이라는 게 드러났다(재민 질문에서 시작).

### 무엇이 별개인가

안개(`_seenChunks`)는 **지면만** 칠한다. 엔티티는 `entityVisibility()` 라는 다른 판정을 쓴다 —
**현재 facing 부채꼴** × **벽 line-of-sight**. `dist<80px`면 무조건 보임, facing 이 없으면 360°,
정면에서 약 101° 넘게 뒤로 가면 0. ★**"본 적 있는가"는 여기 안 들어간다** — 그래서
미탐사 셀 위에도 엔티티가 그려질 수 있다(논밭이 새까만 땅에 뜨는 이유).

### 재민 확정 — 살아 움직이는 것에만 건다

| | 적용 전 | **확정 후** |
|---|---|---|
| `player`(본인 제외) · `mob`(동물) | 부채꼴×벽 | **그대로 유지** |
| `resource`(나무·바위·광맥·덤불·약초) | 부채꼴×벽 | **항상 그려진다** |
| `ground_item`(바닥템) | 부채꼴×벽 | **항상 그려진다** |
| `corpse`(시체) | 부채꼴×벽 | **항상 그려진다** |

재민 원문: *"나무, 바위 등 식물과 무생물은 항상 그려져야 해.. 시체도.. 바닥템도.."*
뒤돌았다고 숲이 사라지면 안 된다. 내가 떨군 물건을 뒤돌아서 못 찾으면 안 된다.
**거리 감쇠(650px에서 15%까지)는 남긴다**(재민 확정) — AOI 경계 팝인을 무르게 하는 장치라
빼면 걸을 때마다 화면 가장자리에서 나무가 툭툭 나타난다.

### 광맥 — 보이는 건 바위다

재민 지적: *"광맥은 눈에 보이는 게 아니라 그냥 구역인데.."* 맞다.
`terrain` 의 광맥(`ores`)은 **보이지 않는 구역**이고 자원 스폰 밀도와 광종만 정한다.
그런데 기존 `ore01..06` 스프라이트는 **바위에 주황 결정이 솟은 게임식 표식**이었다.

⇒ `scripts/ore-outcrop.py` 신설 — **노두(露頭)**로 재생성:
`rock01..06` 을 몸통으로 그대로 쓰고(실루엣·알파 동일), 표면에 **은은한 광물 변색**만 얹는다
(적갈 = 산화철 · 녹청 = 공작석 계열, 저주파 얼룩 마스크 · 파일 인덱스 시드로 결정론).
얼룩 피복 **22~61%**. 1패스는 68~87%였는데 '광물이 낀 바위'가 아니라 **칠한 바위**로 읽혀 낮췄다.
⇒ 눈에 보이는 건 '바위'다. **캐 봐야 광물이 나온다** — 발견의 재미가 살고 고증에도 맞다.
(`rock`·`mossrock` 은 그대로 유지.)

---

## 6-c. ★재민 확정 — "한 번도 안 가본 곳은 그 어떤 것도 보여서는 안 된다"

§6-b 에서 드러난 마지막 구멍을 닫는다. 규칙은 절대적이다. **두 번 틀리고 세 번째에 맞췄다.**

### 1패스 — 안개 마스크를 월드 렌더 전체 뒤로 (틀렸다)

가장 단순한 방법이다. 그리고 **미탐사 위 밝은 픽셀이 0** 이 됐다(실측). 그런데 회귀가 잡았다:

```
e2e-rooms  이엉 픽셀 29.0% → 2.8%     ← 집 옆에 서 있는데 내 지붕이 캄캄해졌다
e2e-cutaway 지붕 신호 41.6 → 28.2
```

**원인은 기하다.** 가시성 폴리곤은 **지면**에서 벽을 광선으로 잘라 만드는데, 지붕·산·나무는
자기 셀보다 화면상 한참 **위**에 그려진다. 화면 픽셀로 막으면 그 높은 물체가 **자기 뒤편
미탐사 셀에 잘린다.** ⇒ 재민 규칙의 뜻은 "안 가본 셀 위 **픽셀** 금지"가 아니라
**"안 가본 자리의 사물 금지"** 다.

### 2패스 — 개체 단위 게이트, 앵커 한 칸 (또 틀렸다)

개체의 자리(`wx,wy`)가 '본 셀'이 아니면 안 그린다. 이엉은 3.2% 로 여전히 죽어 있었다.
**밖에서 지은 집은 안에 들어가 본 적이 없다.** 벽이 시야를 막아 **내부 셀이 영영 '본 셀'이
안 되고**, 지붕 앵커가 그 내부라 **내가 지은 내 집 지붕이 사라진** 것이다.

### 3패스 — 발자국 단위 (채택)

구조물은 **발자국 어느 한 칸이라도 봤으면** 보인다. 앵커 + 반경 R셀 8방위 = 9칸을 본다
(`building`·`hutroof`·`claim`·`banditcamp` R=4 · `simvil` R=10 · `stair_cell` R=1).
1칸짜리(자원·바닥템·동물·사람·길·소품)는 그 셀 하나로 판정한다.
안개의 목적은 **안 가본 땅**을 가리는 것이지 내가 지나쳐 본 건물을 숨기는 게 아니다.

지면 데코(자연물)는 종전대로 **마스크 앞**에 그리므로 자동으로 규칙을 따른다(§6-a).

### 구멍 금지 · 검증

`renderables` 의 **모든 push 에 `wx/wy` 를 달았다**(21곳 + 플레이어 2곳). 자리가 없는 항목은
**세어서 내보낸다**(`__fogGateDbg.missing`) — 하네스가 0 을 요구한다. 조용히 새는 종류가 없다.

`e2e-nature` ⓕ 판정 재작성(**43/0**):
- 그려진 개체의 자리를 `__fogGateProbe()` 로 받아 하네스가 **`_seenChunks`(정본 저장소)를 직접 읽어**
  독립 재계산한다. 클라 판정 함수를 부르면 사본이고 자명 통과다.
- 반례: `__terrain19.fogGateOff` 로 게이트를 끄면 **위반이 실제로 나온다**(강가 1 · 초원 2).
- ★자명 통과 금지 문턱을 한 번 잘못 잡았다 — "개체 20개 이상"을 요구했는데 두 지점 다 황무지라
  프레임당 2~6개뿐이었다. **없는 결함을 보고했다.** 본뜻은 "게이트가 걸릴 상황인가"이므로
  **반례가 나오는지**로 재도록 고쳤다.

### 최종 회귀 — 최종 트리 7종 전수, 순차 단독

```
cutaway 15 · rooms 55 · nature 43 · terrain 27 · metallurgy 20 · village 32 · guest 40
= 232 판정 0 실패
```
★`cutaway` 의 지붕 신호는 오히려 **41.6 → 46.2 로 올랐다** — 안 본 곳의 개체가 사라지면서
안/밖 대비가 더 선명해졌다.

---

## 6-d. ★물가 풀 넘김 — "3D가 아니라서 못 고치나?" (아니다)

재민 질문: *"물가에 풀이 있을 때, 풀의 튀어나온 부분이 물에 가려지는 문제… 3D 모델이 아니라서
결코 고칠 수 없는 거야? 갈대를 추가하거나 물 옆에 인위적으로 풀을 더 심는 거 말고."*

**3D 문제가 아니다. 클리핑과 순서 문제였다.** 코드로 확인한 원인:

1. `_bakeGroundTile` 이 풀 텍스처를 깔고, **물 셀 자리를 진흙 다이아몬드로 덮어쓴다**
   ⇒ 풀잎이 셀 다이아몬드 변에서 **칼로 자른 듯** 끊긴다.
2. 그 위에 물 셰이더가 또 덮는다.

⇒ 고치는 법: **같은 풀 텍스처를 물 쪽으로 몇 px 더 그리되, 물보다 나중에** 그린다.
풀 텍스처(`grass_angled`)는 **이미 게임 카메라 각도로 구워져** 있어 잎이 제대로 누워 있다 —
할 일은 그 잎을 **셀 경계에서 자르지 않는 것**뿐이다.

### 실장 — `_bakeShoreTile`. ★**세 번 만에 맞췄다**(두 번은 재민이 눈으로 반려)

| 패스 | 무엇을 했나 | 왜 틀렸나 |
|---|---|---|
| 1 | 반투명 3층(α .30/.55/.85)을 셀 다이아몬드째 밀어 깔았다 | 재민: *"물가에 어색한 띠… 너무 부자연스럽잖아."* **반투명 초록을 밝은 물 위에 얹으면 풀이 아니라 뿌연 안개 띠**가 된다. 게다가 다이아몬드를 통째로 미니 **한 셀 변 전체가 같은 폭** = 띠 |
| 2 | 불투명하게 깔고 **합성 얼룩(2px 격자 해시)** 으로 바깥을 갉았다 | 띠는 없앴는데 **디더 격자 무늬**가 보였다. 규칙적으로 반복되는 마스크는 무엇을 해도 '무늬'로 읽힌다 |
| **3 (채택)** | 불투명 + **풀 텍스처 자신의 잎 모양으로 깎는다** | 어두운 곳(잎 사이 틈)을 파내고 밝은 곳(잎)만 남기니 실루엣이 **진짜 잎 끝**을 따라간다 |

- 물 셀마다, 이웃 **뭍 셀 다이아몬드를 물 쪽으로 밀어**(최대 7px, 자리 해시로 ±) 물 셀 안에 클립.
  `translate` 로 밀어 **텍스처째** 움직인다 — 그래야 마스크와 정합된다.
- 마스크는 `grass_angled` 에서 만든 **잎 알파**(휘도 중앙값 기준 — 문턱을 고정하지 않아 텍스처를
  갈아도 따라간다). 끝쪽은 마스크 100%(잎만 남음), 안쪽은 50%(성글게).
- **길수록 띠가 된다** — 7px 을 넘기지 않는다.
- 지면 타일과 **같은 격자·같은 캐시**(정적, `_shoreTiles`, 무효화 지점도 동일). 물 셀 없는 타일은 캔버스를 안 만든다.
- 순서: 물 셰이더 → `_drawPrisms` → **넘김** → 물가 술 → 안개. 손잡이 `__terrain19.shoreOff`.

**심는 게 아니다.** 포기를 새로 얹지 않고, 갈대에 기대지도 않는다 — 뭍이 원래 가진 풀을
물 위로 넘길 뿐이다. 물가 술(§2)과 **독립**이라 술을 꺼도 경계가 부드럽다(증거 사진이 그 상태다).

**실측**: 강가 화면에서 넘김 조각 108개 · 타일 15장. 수면 픽셀이 90,902 → **87,354** 로 줄었다
(=약 3,500px 을 풀이 덮었다). `e2e-terrain` 의 물 흐름 계측에는 이것도 정지 무늬라
§5-a 와 같은 이유로 **`shoreOff` 를 격리 목록에 추가**했다(기준은 그대로).

**회귀**: `e2e-terrain 27/0` · `e2e-nature 43/0`.

### ★★결론 — **반려. 기본값 OFF** (`shoreOff: true`)

3패스(잎 마스크)까지 가도 재민 판정은 *"after가 여전히 더 심해"* 였다.
즉 넘김을 어떻게 다듬든 **물가에 띠가 하나 더 생기는 것**이 문제의 본질이다.
코드는 손잡이 뒤에 남겼다(`__terrain19.shoreOff = false` 로 켜진다). **켜지 마라.**

★그리고 재민이 같이 짚었다 — *"before도 좀 있고"*. 맞다. **넘김을 꺼도 남는 얇은 띠**가 있다.
나는 그 진범을 배치 19 `_drawPrisms` 의 '상단 풀 넘김 립'(`#4e6b40` 2.2px)으로 지목했다 —
**이 지목은 틀렸다.** 재민이 *"여전히 테두리가 그어진 느낌인데 그건 왜 그래?"* 라고 다시 물어
그제서야 **재기 시작했고**, 실측이 다른 답을 냈다 → **§6-e**.

증거: `증거_배치21/물가풀넘김_전후.png` · `물가풀넘김_확대_전후.png`(픽셀 확대) — 둘 다 **술을 끈 상태**의 순수 A/B.

---

## 6-e. ★★물가 "테두리" 의 진범 — **수면이 뭍 위로 흘러넘치고 있었다** (수리함)

재민: *"여전히 물가에 테두리가 그어진 느낌인데 그건 왜 그래?"*

### 내 앞 답이 틀렸다 — 그래서 이번엔 지목 대신 **뺄셈**부터 했다

앞(§6-d)에서 나는 진범을 프리즘 상단 립이라고 **단정했다**. 근거는 코드를 읽은 것뿐이었다.
이번엔 **립을 끄고 화면을 뺐다**:

| 잰 것 | 값 |
|---|---|
| 프리즘 ON/OFF 화면 전체 변화 픽셀 | **1,975px** (그중 립 색대 490px) |
| 재민이 말한 "테두리" 가 깔린 길이 | 물가 전 구간(수천 px) |

**1,975px 로는 화면을 가로지르는 테두리를 만들 수 없다.** 립은 진범이 아니다.

### 그럼 뭐냐 — 물가에 **수직 프로파일**을 찍었다

물가 경계선에서 **뭍 쪽으로** 수직으로 걸어가며 픽셀을 평균냈다.
그리고 같은 뭍 픽셀을 **물 레이어 ON / OFF** 로 두 번 찍어 뺐다.

| 경계에서 거리 | 파랑(B) 증가 = 물이 뭍에 칠한 양 |
|---|---|
| d=0 | **+27.3** |
| d=1 | **+25.9** |
| d=4 | **+21.9** |
| d=8 | +10.3 |
| d=11 | **+6.3** |

**물이 뭍을 11px 넘어서까지 파랗게 칠하고 있었다.** 그게 물가를 따라 이어지는 푸른 후광 —
재민이 계속 보고 있던 **"테두리"** 다. 풀도 립도 아니었다.

### 원인 — 배치 19 가 주석만 쓰고 `discard` 를 안 걸었다

수면은 지면보다 `WATER_DROP = 5`px **아래로** 그린다(물이 파인 느낌을 내려고).
그래서 수면 픽셀 하나하나는 **자기 자리보다 위쪽 셀** 의 물을 그리고 있고,
남·동쪽 물가에서는 그 픽셀이 **뭍 위에 얹힌다**. 원래 설계는 그걸 막게 되어 있었다:

```js
// 내 자리(지면 높이)가 뭍이면 그건 프리즘 면이 덮을 자리다 — 물을 그리지 않는다
'  vec2 wg = vec2((2.0*sy+ix)*0.5,(2.0*sy-ix)*0.5);',
'  vec2 uvg = cellUV(wg);',
'  vec4 L = texture2D(uLin,uv);',   // ← uvg 를 **쓰지 않는다**
```

`grep uvg client.js` → **정의된 줄 하나뿐.** 주석은 있는데 코드가 없다.
셰이더는 실패하지 않는다. 조용히 안 걸린 채 1개 배치를 살아남았다.

### 수리 — 의도했던 그 한 줄

```js
'  if(uvg.x>=0.0&&uvg.y>=0.0&&uvg.x<=1.0&&uvg.y<=1.0&&texture2D(uMsk,uvg).a < 0.5) discard;',
```

**같은 계측을 다시 돌린 결과:**

| 경계에서 거리 | 수리 전 | 수리 후 |
|---|---|---|
| d=1 | +25.9 | **+3.9** |
| d=4 | +21.9 | **+1.3** |
| d=11 | +6.3 | **+0.5** |
| d=0 | +27.3 | +17.7 ← 1px 경계 안티에일리어싱. 남는 게 맞다 |

밝기(휘도)는 d=0 에서 오히려 **−9.2** — 프리즘 단면이 만드는 **접지 그림자**다. 의도한 그림이다.

### 회귀 — 물 레이어를 건드렸으니 전수

`e2e-terrain` **27/0** (①물 흐름 ②얕은물 투명 ③프리즘 단면 ④지면 질감 ⑤결정론 ⑥성능 — 전부 유지).
③ 은 이 수리로 **프리즘 단면이 실제로 드러나는** 층이라 특히 봤다: 강가 0.10%(763px) · 초원 0%(대조군).
나머지: `nature` 43/0 · `cutaway` 15/0 · `rooms` 55/0 · `metallurgy` 20/0 · `village` 32/0 · `guest` 40/0.

★정직하게: `village` 는 **첫 실행에서 1 실패**했다(`central save 실패: socket hang up`).
원인은 내가 `metallurgy && village` 를 **한 셸에 이어 돌린 것** — 2코어 단독 실행 규율을 내가 어겼다.
단독으로 **두 번 연속 재실행 → 32/0 · 32/0**. 코드 문제가 아니라 내 실행 방식 문제였다.

econ 무접촉 — `git diff --name-only sim/` = **0 파일**, 배선 검사 실패 0.

증거: `증거_배치21/물가테두리_수리_전후.png` (둘 다 `freezeT=100` · 자연물 OFF · 넘김 OFF 순수 A/B).

---

## 6-f. ★★물가 여백 — "풀이 어색하게 잘리지 않게" 세 번째 시도 (채택)

재민: *"이제 다시 풀이 어색하게 잘리지 않도록 해볼 수 있을까..?"*

### 먼저 — 수리된 화면을 **확대해서** 다시 봤다

§6-e 로 물 넘침을 걷어냈으니 남은 그림이 무엇인지 다시 재는 게 순서였다.

| 잰 것 | 값 |
|---|---|
| 뭍 풀의 가로 표준편차(질감) | σ **29~45** |
| 프리즘 상단 립 행 | σ **24.9** ← 프로파일 전체 **최솟값** = 가장 띠답다 |
| 프리즘 그늘(d=3~4) 휘도 | **38~40** ↔ 바로 옆 물(d=6) **106** |
| 북·서 물가 | 프리즘조차 없다 — **풀잎이 대각선에 그대로 잘린다** |

확대 이미지(`증거_배치21/물가여백_전후_확대.png` 위쪽)를 보면 명확하다:
**풀 → 완전 직선 대각선 → 물.** 전이 구간이 **0px** 다.

### 두 번 반려된 길은 다시 가지 않았다

반려된 것(`_bakeShoreTile`)은 **물 위에 초록 띠를 하나 더 얹는 것**이었다. 남은 교훈 셋:

> ⓐ 물 위에 새 층을 얹지 마라 · ⓑ 폭이 일정하면 띠다 · ⓒ 규칙적 마스크는 무늬로 읽힌다

### 채택 — **뭍 쪽으로** 판다 (`_shoreMarginBake`, 지면 베이크 안)

실제 물가에는 풀이 물까지 자라지 않는다. 젖은 모래·자갈 **여백**이 있다. 그걸 만든다.

- **새 레이어 0장.** `_bakeGroundTile` 안이라 **프레임 비용 0**(타일에 구워진다). 물 위엔 아무것도 안 얹는다.
- 뭍 셀이 물과 맞닿는 변에서 **뭍 쪽으로** 최대 13px. 이웃 물 다이아몬드를 뭍 쪽으로 밀어
  겹치는 부분이 곧 폭 m 띠다(반려안의 **정확히 반대 방향**).
- **폭이 저주파로 0↔13px 을 오간다.** 두 옥타브(파장 5셀 + 10셀) × 셀별 0.35~1.30 배.
- **안쪽 경계는 풀잎 모양으로 깎는다** — `grass_angled` 자신의 잎 알파(밝은 곳=잎)로 모래를
  파내 잎이 모래 쪽으로 삐져나온다. 마스크는 지면 바탕과 **같은 위상**이라 실제 잎 자리와 맞는다.
- 물에 닿는 42% 만 **젖은 모래**로 어둡게(여백 전체를 어둡게 하면 그게 또 띠다).
- 프리즘 상단 립은 **위에 있는 것의 색**을 따라간다(여백이 있으면 모래색) + 높이 1.3~3.2px 로 흔든다.

⇒ 직선은 남지만 그 직선은 이제 **모래↔물**이다. **물가선이 날카로운 건 정상이다** — 잘리는 게 풀이 아니다.

### ★1패스 실패 — 개념이 예뻐서 틀렸다

1패스는 여백 폭을 **술 밀도와 같은 노이즈장**에 묶었다("갈대밭=여백0, 빈 구간=모래톱"). 개념은
좋았는데 **값 노이즈가 0.5 근처에 몰려서** 화면 전체에 여백이 **36조각**뿐이었다 —
재민이 지적한 그 직선이 거의 그대로 남았다. ⇒ **자기 장**으로 분리했다.
(술과 겹치지 않는다: band0 술은 셀 중심에서 물 쪽으로 12~28px 밀려 **물 위**에 서고,
여백은 변에서 **뭍 쪽**이다. 갈대는 모래톱 끝 물에 선다.)

### ★2패스 — **판정을 낮추지 않고 코드를 고쳤다**

문턱 0.26 으로 깔았더니 물가 변의 **91%** 에 여백이 생겼다 = 그게 곧 띠다.
`e2e-nature` ⓗ(빈 구간 ≥20%)를 **9% 로 못 넘었다**.
⇒ 문턱을 0.40 으로 올려 **코드를 고쳤다**. 결과 **40%**. (같은 규율: §6 의 `frFloor` B안 기각)

### 시안 3안 — `증거_배치21/물가여백_시안_A없음_B채택_C과다.png` (위→아래)

| 안 | `shMargin` | 실측 | 판정 |
|---|---|---|---|
| **A** 여백 없음(현행) | 0 | 여백 변 0% | 재민이 지적한 그 직선 |
| **★B 채택** | 1 | 여백 변 **60%** · 폭 0인 변 **40%** · 평균 6.0→**4.9px** · 변동계수 **1.01** | 굽이마다 나타났다 사라진다 |
| **C** 과다 | 1.75 | — | 폭이 커져 **다시 띠로 읽힌다** |

손잡이 `__terrain19.shMargin` 에 숫자를 넣으면 런타임에 갈아 끼워진다(타일 캐시 자동 무효화).

### 검증 — `e2e-nature` ⓗ 신설 6판정

물가 변 **133** · 여백 있는 변 80(60%) · **폭 0인 변 53(40%)** · 평균 4.88px · 표준편차 4.95 ·
**변동계수 1.01** · 손잡이 끄면 여백 변 **0**(반례) · 화면 차이 **6,180px** ·
대조군 **초원엔 물가 변 0**(여백은 물가에서만 생긴다).
폭의 정본은 클라의 `_shoreMargin` 을 하네스가 **그대로 부른다**(다시 짜면 사본 = 자명 통과).

---

## 6-g. ★★바람 흔들림 — 그리고 "날씨에 따라" 는 **지금 못 한다** (정직하게)

재민: *"바람 흔들림을 먼저 할까? (날씨에 따라 풀의 운동이 변하게 할 수 있나?)"*

### 먼저 답 — **게임에 날씨 상태가 없다**

`server/` · `sim/` · `public/` 전수 grep: `weather|rain|storm|날씨` **0건**.
없는 걸 있다고 보고하지 않는다. 그래서 이렇게 했다:

- **바람 세기의 정본 함수 하나**(`_windAt`)를 세웠다. 지금은 **게임 시계**가 채운다.
- `__terrain19.windForce` 가 **주입구**다. 날씨 시스템이 생기면 **그 한 곳만** 먹이면 전부 따라온다.
- 하네스가 그 훅이 **죽어 있지 않은지**까지 본다 — `windForce 0.37` 을 주면 정본이 정확히 0.37 이 돼야 한다.

지금 채운 값은 **일주기 미풍**이다: 새벽 잔잔 → 한낮 최대 → 밤 다시 잔잔(실제 지표풍의 일변화).
거기에 주기가 다른 두 사인의 곱으로 **큰 숨**(돌풍이 왔다 갔다)을 겹쳤다.
실측 곡선(게임시각 0~120s, 40표본): 최소 **0.176** · 최대 **0.975** · 평균 **0.608**.

### 흔드는 법

- **전단(shear)**: 밑동 고정, 꼭대기만 민다. 회전이 아니다 — 회전은 밑동이 땅에서 뜬다.
- **진행하는 파**: 위상에서 `바람방향·자리` 를 빼 들판을 물결처럼 훑고 지나간다(파장 ≈ 6.5셀).
  전부 같은 위상으로 흔들면 '풀이 아니라 화면이 흔들리는' 그림이 된다.
- 종별 진폭(기울기 tan): 풀포기 0.155 · **갈대 0.265** · 부들 0.235 · 들꽃 0.125 (≈9°~15°).
- 시간은 **게임 시계** — 물 셰이더와 **같은 `freezeT` 경로**라 결정론 판정이 안 깨진다.
- ★`setTransform` 이 아니라 `transform` + save/restore 다. 메인 캔버스가 이미 변환을 걸고
  있을 수 있어 항등으로 되돌리면 그 뒤 그림이 다 깨진다.

### 비용 — 실측

자연물 그리기 패스에 상시 계측기를 달았다(`__natDbg.ms`, 32프레임 이동평균).
같은 장면(술 633 + 소품 122 = **755장**, headless):

| | ms/프레임 |
|---|---|
| 바람 끔 | **0.913** |
| 바람 켬 | **1.157** |
| **차이** | **+0.24 ms/f** |

60fps 예산 16.7ms 안에서 **싸다**. 청크 캐시는 **자리**를 담지 픽셀을 안 담으므로 무효화도 없다.

### 안 흔든 것 — 이유와 함께

- **지면 풀 텍스처**: 매 프레임 재베이크나 WebGL 이관이 필요해 비싸다(회부, 배치 21 8차 결론 그대로).
- **나무**: 서버 엔티티라 그리는 자리가 다르고(안개 게이트 경유) 줄기는 원래 안 흔들린다.
  캐노피만 흔들려면 스프라이트를 나눠 구워야 한다 → 회부.

### 검증 — `e2e-nature` ⓖ 신설 8판정

★**물을 끄고 잰다** — `freezeT` 는 물 셰이더도 굴린다. 안 끄면 수면이 흐른 픽셀을
"풀이 흔들렸다"로 오독한다(§5-a 와 같은 계측기 격리).

| 판정 | 강가 | 초원 |
|---|---|---|
| 시각 100→100.9, 바람 0.85 | **48,257px 움직임** | **4,905px** |
| 반례 — 무풍(force 0) | **0px** | **0px** |
| 대조군 — 자연물 OFF(NPC·HUD 잡음 바닥) | 0px | 0px |

세기 곡선 [0,1] 이탈 없음 · 진폭 0.799(한 값 고정 아님) · **날씨 훅 살아 있음**(0.37 → 0.37).
그리고 기존 ⓔ **시각 고정 두 프레임 차이 0px** 이 그대로 유지된다 — 바람이 순수 함수라는 증거.

증거: `증거_배치21/바람_4프레임.png` (게임시각 100 · 100.55 · 101.1 · 101.65 — 갈대·부들이 눕는 각도가 바뀐다)

### 회귀

`nature` **58/0**(43→58, 신설 15판정) · `terrain` **27/0** · `cutaway` **15/0** · `rooms` **55/0** ·
`metallurgy` **20/0** · `village` **32/0** · `guest` **40/0** = **247 판정 0 실패**.
econ 무접촉 — `git diff --name-only sim/` = **0 파일** · 배선 검사 실패 0.
★`guest` 는 첫 실행에서 2 실패(`metallurgy`→`village`→`guest` 를 연달아 돌린 뒤였다).
**단독 재실행 2회 연속 40/0.** 2코어 단독 실행 규율을 지키면 안 난다 — 내 실행 방식 문제다.

★`terrain` ③ 프리즘 단면은 립 색·높이·그늘 알파를 바꿨으므로 숫자가 움직였다:
**0.10%(763px, 밝기 −45.8) → 0.09%(742px, −38.8).** 기준(>0.05%, <−8)은 **그대로 두고** 통과했다.

---

## 6-h. ★★★풀 **카펫** 흔들림 — 내가 틀린 대상을 흔들었다 (재민 정정)

재민: *"물가 근처에 풀 개체들을 추가로 넣고 걔네를 흔들라는 게 아니라… **모든 초원에 그려져 있는
그 풀** 말이야."*

### 내가 뭘 틀렸나

§6-g 에서 나는 **흩뿌린 포기 스프라이트**(풀숲·들꽃·갈대)만 흔들었다. 그리고 같은 보고서에
*"지면 풀 텍스처는 매 프레임 재베이크나 WebGL 이관이 필요해 **비싸다** — 거기까진 하지 마라"*
라고 **내 손으로 적어놓고** 그걸 설명으로 끝냈다. 그게 요청 대상이었다. 비용을 이유로 미룰 일이 아니었다.

### 구조를 쪼갰다 — 재베이크 없이

지면은 512×256 iso 타일로 **한 번 구워 blit** 한다(0.04ms/f). 구운 그림은 못 움직인다. 그래서:

```
바탕 cv = 지금 굽던 그대로. 단 ① 단계의 풀 텍스처를 **평탄한 한 색**으로 깐다
잎  bl = (풀텍스처 − 평탄색) × **투과율 T**
매 프레임: 바탕 blit → 잎을 **가로 띠로 어긋나게 'lighter'(가산) blit**
```

★**이건 근사가 아니라 항등식이다.** 알파 합성은 배경에 대해 선형이므로
`sharp = fixed + T·tex`, `flat = fixed + T·평탄색` ⇒ `sharp = flat + T·(tex − 평탄색)`.
여기서 T = "① 위에 칠해진 모든 것이 남긴 투과율"이고, **추정하지 않는다** — 굽는 그 자리에서
같은 도형·같은 알파로 지우며(`destination-out`) 만든다. 뙈기·물·바위·얼음·존 틴트·물가 여백·
상태 레이어 전부.

- 띠 높이 **16px**, 진폭 **2.2px**, 파장 ≈ 370px(≈11셀), 각속도 1.15 rad/s(게임 시계).
- 위상은 **iso 세로 좌표**(월드)로 준다 — 화면 기준이면 카메라를 움직일 때 파도가 따라온다.
- 밀림만으로는 '미끄러짐'으로 읽혀서, 띠마다 **가산 알파를 0.90~1.00 으로 흔들었다**
  (실제 밀밭은 돌풍이 지나갈 때 빛도 함께 훑는다). 비용 0.
- 바람 세기·시각은 §6-g 와 **같은 정본**(`_windAt`/`_windT`) — 날씨 훅도 그대로 먹는다.
- 잎 층 때문에 타일 메모리가 2배라 캐시 상한을 140 → **80** 으로 낮췄다(화면은 ~30장).

### 비용 — 설계 전에 쟀다

먼저 **띠 blit 자체**를 격리해 벤치했다(headless, 1400×900, 타일 30장):

| 방식 | ms/프레임 |
|---|---|
| 지금(타일 1 blit) | **0.038** |
| 띠 32px (240 blit) | 0.307 (+0.27) |
| **띠 16px (480 blit) ← 채택** | **0.566 (+0.53)** |
| 띠 8px (960 blit) | 1.063 (+1.03) |

실클라 전체 프레임(SwiftShader 물이 대부분을 먹는 환경)에서도 **19.5 ↔ 17.0 ms/f** 였다.

### 검증 — `e2e-nature` ⓘ 신설 10판정. **제1판정은 항등식**

| 판정 | 강가 | 초원 |
|---|---|---|
| **항등식** — 무풍이면 옛 그림과 같다(평균 \|Δ\|) | **0.370** | **0.395** |
| 눈에 띄게 다른 픽셀(>10) | 0.69% | 0.49% |
| **카펫이 흔들린다**(자연물·물 끈 상태, 시각 100→101.4) | **241,989px** | **258,633px** |
| 반례 — 무풍 | **0px** | **0px** |
| 잎 띠 수 ↔ 손잡이 끄면 | 240 ↔ **0** | 240 ↔ **0** |

★남은 오차의 출처도 밝혀 둔다: 평탄색 백분위를 6%→2%→0.5% 로 바꿔가며 쟀더니 2% 가 가장 좋았고
(4,221 → **3,694** → 4,116), **더 내려도 안 좋아졌다** = 남은 오차는 `max(0,tex−평탄색)` 의
잘림이 아니라 **상태 레이어 투과율을 두 알파로 근사한 것**이다. 그 근사는 도로·경작지·판 자리에만 있다.

### ★남의 계측기 넷을 오염시켰다 — 판정은 하나도 안 낮췄다

지면이 **저절로 움직이게** 됐으니, 서로 다른 시각의 두 프레임을 픽셀로 비교하는 하네스는 전부 깨진다.

| 하네스 | 증상 | 처리 |
|---|---|---|
| `e2e-mtfoot` ⑧ 결정론 | \|Δ\| 0.055 → **1.96** | `windOff` 격리 + **오염값·대조군 판정 신설** |
| `e2e-tilestate` 정지 대기 | 영영 안 멈춤 | 측정 **전에** 격리 |
| `e2e-mountain` ⓑ 반례 | 비바위 상자 **33.4%** | 격리 |
| `e2e-waterperf` 그림 동일 | \|Δ\| **3.16** > 3.0 | 격리 |
| `e2e-rooms` "빈 땅 대조군 정지" | \|Δ\| **3.8** > 2 | 격리 — ★배치 19 인계가 **예고했던 그 오염**이 드디어 실현됐다 |

### ★덤으로 잡은 잠복 버그 — `waterOff` 가 타일 지문에 없었다

항등식 판정이 **강가에서만** 평균 \|Δ\| 6.18(12.6%)로 깨졌다(초원은 0.395). 범인은 내 층이 아니라
**타일 캐시 지문에 `waterOff` 가 빠져 있던 것**이었다 — 물을 껐다 켜도 옛 타일(진흙 물밑)이 그대로
남는다. 지문에 넣자 0.370 으로 붙었다. 손잡이 A/B 를 쓰는 모든 판정이 이 버그의 영향권이었다.

증거: `증거_배치21/풀카펫_흔들림_4프레임.png`(왼쪽부터 옛 경로 · 시각 100 · 101.4 · 102.8 —
마른 뙈기는 제자리, 잎만 움직인다)

---

## 6-i. ★재민 확정 2건 — 물가 술 **반려** · 카펫 흔들림 **증폭**

### ① 물가 술 반려 — 기본값 OFF (`fringeOff: true`)

재민: *"일단 물가 근처에 추가적으로 배치하는 풀은 없애주고"*

이 층의 **원래 존재 이유는 절단선 은폐**였다(§2 첫 줄: "존재 이유는 절단선이다"). 그런데 그 문제는
이제 **지면 베이크 안의 물가 여백**(§6-f)이 직접 푼다 — 가리는 게 아니라 **애초에 안 잘리게** 만든다.
가리개는 필요가 없어졌고, 재민 눈에는 남은 것이 **"일부러 심은 풀"** 이었다.

- 코드는 손잡이 뒤에 남긴다(`__terrain19.fringeOff = false` 로 켜진다). 켤 이유는 지금 없다.
- **초원 소품(들꽃·풀숲)은 그대로 산다** — 없애라고 한 건 '물가 근처'다.
- 하네스에 **기본값 판정**을 신설했다: 손잡이를 하나도 안 건드린 상태에서 물가 술 **0**,
  초원 소품 **122/160**(반례 — 없앤 건 물가뿐이라는 증거).
  ⓐ은폐·ⓑ군락 절은 `fringeOff:false` 로 **일부러 켜서** 잠든 코드가 살아 있는지 계속 본다.

### ② 카펫 흔들림 — "여전히 안 흔들려"

1패스 값이 **너무 느리고 작았다**: 진폭 2.2px · 각속도 1.15 rad/s(주기 **5.5초**).
잎 끝이 2.7초에 걸쳐 2px 움직이면 사람 눈에는 정지다. 숫자로만 "흔들린다"고 한 셈이다.

| | 1패스 | 채택 |
|---|---|---|
| 진폭 | 2.2px | **3.4px** |
| 1차 파 주기 | 5.5초 | **3.0초** |
| 2차 파(잔떨림) | 없음 | **주기 1.2초 · 진폭 ×0.38** |
| 띠 밝기 흔들림 | 0.90~1.00 | **0.84~1.00** |

★2차 파를 겹친 이유: 큰 결 하나만 있으면 **미끄러지는 그림**으로 읽힌다. 잎이 떠는 잔결이 있어야 풀이다.

**사람이 보는 속도로 잰 값** — 게임시각 **0.25초** 간격 6프레임, 지면 상자 52만px 중 바뀐 픽셀:
**125,594 · 125,101 · 119,534 · 119,794 · 122,119** = 매 0.25초마다 **약 23%** 가 바뀐다.
(1패스는 이 값을 아예 안 쟀다 — "시각 100→101.4 에 바뀐다"만 봤고, 그건 **1.4초** 간격이었다.)

증거: `증거_배치21/풀카펫_흔들림_0.25초간격_6프레임.png`

### 회귀 — 12종 전수, 순차 단독

`nature` **73/0**(기본값 판정 3 신설) · `terrain` 27/0 · `tilestate` 39/0 · `mtfoot` 9/9 ·
`mountain` 23/0 · `waterperf` 17/0 · `rooms` 55/0 · `cutaway` 15/0 · `metallurgy` 20/0 · `village` 32/0.
`mtocc`·`guest` 2건은 §합류 기록의 그 적신호 그대로(둘 다 내 층이 아님 — 대조군으로 귀속 확인).

---

## 6-j. ★★물가선을 굽힌다 — "여전히 물에 풀이 잘리는데 해결 불가능?"

**불가능하지 않았다.** 12배로 확대해 보고 나서야 남은 게 무엇인지 알았다.

### 확대해서 본 것

- **풀↔모래 경계는 이미 잎 모양으로 너덜하다**(§6-f 가 한 일). 여기는 문제가 아니다.
- **모래↔물 경계가 자로 그은 완전 직선**이었다. 셀 다이아몬드 변을 그대로 쓰니 당연하다.
- ⇒ **지면 쪽에서 아무리 너덜하게 만들어도, 그 위를 직선 물 폴리곤이 덮어 다시 자른다.**
  지금까지 나는 계속 **지면만** 손댔다. 자르는 칼은 물 쪽에 있었다.

### 고친 법 — 물 셰이더에서 두 가지

```glsl
vec2 fuzzW(vec2 p){ return p + (vec2(vn(p/8.0,64.0), vn(p/8.0+(37.3,11.9),64.0))-0.5)*uFuzz; }
```

1. **굽이** — 셀 마스크를 **월드 좌표를 흔든 자리**에서 읽는다. 파장 ≈8 월드px 라
   점점이 흩어지는 디더가 아니라 **물결치는 선**이 된다. 잡음은 이 셰이더의 기존 규약
   (`vn`, 스케일 512 의 약수)이라 카메라를 움직여도 무늬가 안 튄다.
2. **피복률(4탭)** — 굽이만으로는 여전히 **1px 만에 뚝 끊겼다.** 그래서 흔든 자리 주변 네 점 중
   몇 개가 물인지를 세어 `wa *= cov` 로 곱한다. 경계가 0/¼/½/¾/1 로 **젖어들 듯** 끝난다.
   수면 높이 테스트와 지면 높이 테스트 **둘 다** 같은 방식으로 재고 `min` 을 취한다.

★배치 19 의 *"각진 블록 — 셀 경계 그대로"* 규약을 **의도적으로, 좁게** 깼다:
블록감(수심·흐름의 셀 단위)은 그대로 두고 **바깥 윤곽만** 굽혔다. 손잡이 `edgeFuzz = 0` 이면 옛 그림이다.

### 실측

| | fuzz 0(옛 그림) | **6.0(채택)** | 12.0 |
|---|---|---|---|
| 경계선 y 가 옛 그림과 다른 정도(평균) | — | **1.05px**(최대 27) | 1.65px |

시안: `증거_배치21/물가선_굽이_A0_B6_C12.png` (왼쪽부터 0 / 6 / 12 — 12는 과하다)

### ★1패스 실패 — 셰이더가 **조용히** 죽었다

`fuzzW` 를 `main()` **안에** 정의했다. GLSL 은 중첩 함수 정의를 허용하지 않아 링크에 실패했고,
`try/catch` 가 그걸 삼켜 `_wgl.ok=false` → **단색 물 폴백**으로 조용히 떨어졌다.
그런데 화면엔 여전히 '물색'이 있어서 스크린샷은 그럴듯했다.
**단서는 측정값이었다 — `uFuzz` 를 0/3/4.5/7 로 바꿔도 숫자가 소수점까지 전부 똑같았다.**
손잡이를 돌렸는데 아무것도 안 변하면 그건 "효과가 약한" 게 아니라 **그 층이 안 돌고 있는 것**이다.
⇒ 하네스가 `__waterDbg.webgl` 을 함께 읽도록 했다. (배치 18 "도달 못 하는 층", 배치 21 `VIEW_RADIUS`
ReferenceError 와 **같은 계보** — 화면이 그럴듯해도 층은 죽어 있을 수 있다.)

### 회귀

`terrain` **27/0**(①물 흐름 8.1px · ②얕은물 r=0.836 · ③단면 0.09%) · `waterperf` **17/0** · `nature` **73/0**.

---

## 6-k. ★★★흐름이 **반대로 흐르는 띠** — 진범은 "가장 가까운 강이 주인" 이었다

> 재민: *"근데 중간에, 얇게 수직으로 흐름이 생기는 띠가 존재하는데, 그건 왜 그래?"*
> → 내가 화면 세로줄로 알아듣고 헛다리 세 번.
> 재민 정정: *"아니... 화면 상에서 세로라는 말이 아니라, 물이 지금 북→남으로 흐르고 있잖아..
> 그러니까 화면 상에서는 (-1,-1) 방향으로 흐르는 건데, (-1,1) 방향으로 흐르는 띠? 같은 게 존재한다고.."*

### ★내가 세 번 헛다리 짚은 이유 — 셋 다 **크기**를 쟀다

정정 전에 60프레임(강가 · 기본 손잡이 · `freezeT` 0.05초 간격)으로 세 번 쟀고 셋 다 "없다"고 보고했다.

| 계측 | 결과 | 왜 못 봤나 |
|---|---|---|
| 열별 시간 표준편차 | 중앙 2.98 · p95 3.61 · **1.6배 넘는 열 0개** | 움직임의 **크기** |
| 가로 경사 이음매 | x≈520 에서 중앙의 1.3~1.5배 — 손잡이 7종 전부 동일 | 정지 프레임의 **밝기 경사** |
| 모션 히트맵 | 움직임은 전부 풀 · 물은 거의 정지 | 움직임의 **크기** |

**뒤집힌 띠는 이웃과 속도가 똑같다.** 크기만 재는 계측기로는 영원히 안 보인다.
재민이 "방향"이라고 못박아 준 뒤에야 **방향을 재는 계측기**를 만들었다.
— 배치 21 하네스 오류 8건째. 이번엔 판정이 아니라 **잰 물리량**이 틀렸다.

### 방향을 재는 계측기 두 개

1. **흐름장** — `window.__flowMap(R)` 신설. 정본 `_flowAtCell` 을 셀마다 그대로 물어
   `[cx, cy, dx, dy, u]` 를 돌려준다(사본 금지). 화면 좌표는 렌더러와 같은 식(iso = wx−wy, (wx+wy)/2).
2. **실제 픽셀** — `e2e-terrain ①` 의 `bestShift` 를 **64px 블록별로** 돌려 블록마다 최적 이동을 낸다.
   흐름장이 아니라 **눈에 보이는 것**을 재는 쪽이다.

첫 판에서 바로 잡혔다 — `증거_배치21/흐름방향_이등분선_이음매.png`:
화면 안 물 셀 **709개 중 228개**가 화면 위로 흘렀고, 경계는 **자로 그은 직선**이었다.
픽셀로도 같았다 — 64px 블록 75개 중 **9개가 −150°**(나머지 66개는 150°).

### 진범 — 강의 **폭**을 안 봤다

카메라 자리(월드 457,680 / 129,264)는 **한여울강 × 닛폰대천 합류부**다.

| 강 | width | 반폭 | 중심선까지 | 폭 단위 거리 u |
|---|---|---|---|---|
| 한여울강 (본류, 북→남) | **1,027** | 513 | 545px | **1.06** ← 이 칸은 본류 **물속**이다 |
| 닛폰대천 (지류, 동→서) | **235** | 118 | 575px | **4.89** ← 제 물길 밖 **반폭 4.9배** |

`_flowAtCell` 은 **생거리 최근접**으로 주인을 골랐다. 545 vs 575 — **30px 차이**로,
폭 235짜리 지류가 폭 1,027짜리 본류의 한복판을 빼앗는다.
그리고 두 강의 **이등분선**을 경계로 방향이 한 칸 만에 뒤집힌다 = 재민이 본 띠.

세기 감쇠도 같은 병이었다: 옛 식은 **절대 거리** 700px 까지 1, 1400px 에서 0.
폭 96px 짜리 실개천이 제 물길 밖 700px 까지 온 사방을 제 방향으로 물들였다.

### ★폐기한 1안 — "방향을 섞자" (파도가 무너진다)

먼저 거리 가중 평균(1/d² 커널, FLOW_R 에서 0)으로 방향을 **섞어** 봤다. 이음매는 사라졌다
(이웃 30° 초과 59쌍 → **0쌍**, 최대 63.2° → 21.8°). **그런데 파도가 통째로 무너졌다.**

이 셰이더의 파도 위상이 `al = dot(w, dir)` 인데 `w` 가 **절대 월드 좌표(≈4.6e5)** 다.
`dir` 이 공간에 따라 조금이라도 변하면 위상 기울기가 `|w|·|∇dir|` 만큼 증폭된다:
섞기 후 실측 ∇dir 중앙값 0.33°/셀 = 1.8e-4 rad/px ⇒ 4.6e5 × 1.8e-4 = **83**.
즉 파장 64px 가 **0.77px** 로 접혀 완전 에일리어싱된다.

- 실측: 물 픽셀 |라플라시안| **3.39 → 6.81 (2배)**
- 눈으로: `증거_배치21/파도_확대_A정상_B섞기폐기안.png` — 8배 확대. 왼쪽은 사선 파도 줄무늬,
  오른쪽은 **모래알**이다.
- 부작용 지표: 64px 블록 최적 이동이 잡히는 블록 수가 78 → 40 으로 반토막(무늬가 강체 이동이 아니게 됐다).

**승자독식은 구역 안에서 ∇dir = 0 이라 안전하다.** 이 셰이더에서 방향 섞기는 쓸 수 없다.
(대안은 파도를 흐름 **포텐셜**로 다시 짜는 것 — 배치 규모가 다르다. §7 회부.)

### 채택 — 거리를 **강폭 단위**로 잰다

```js
const hw = Math.max(48, (폭을 t 로 보간) * 0.5);   // 반폭
const sc = d² / hw²;                               // 주인은 이 값이 가장 작은 구간
...
const u = dist / hw;
w = dist > FLOW_R ? 0 : (u > 1 ? max(0, 2 - u) : 1);   // 물길 안 1 · 반폭 2배에서 0
```

- 본류 물속에서는 본류가 이긴다. 지류는 **제 물길 근처에서만** 이기고, 거기선 지류 방향이 실제로 맞다.
- 마디 두 끝의 `width` 를 투영 파라미터 `t` 로 보간한다(폭은 상·하류로 서서히 변한다).
- **`FLOW_R` 1400 → 1800.** 가장 넓은 강이 width 1,783(반폭 892)이라 영향 끝이 1,784px 다.
  1400 으로 두면 그 강의 바깥 링에서 후보가 잘려 **공간 색인이 전수 순회와 갈린다**
  (색인 동치의 증명이 "반경 밖은 가중치 0" 에 걸려 있다).
- 대조군 손잡이 `__terrain19.flowRawDist = true` = **출시본 그대로**(생거리 최근접 + 700/1400 절대 감쇠).
  바꾼 뒤에는 `__wfReset()` 을 불러야 셀 캐시가 식는다.
- 곁다리: 느린 대조군(`slowFlow`)과 색인 경로가 **같은 본문**(`_one`)을 쓰도록 합쳤다.
  둘이 갈리면 그 A/B 가 무의미해진다.

### 실측 — `증거_배치21/흐름방향_A옛규칙_B강폭규칙.png` (위 = 옛 규칙 · 아래 = 채택)

| | 대조군(옛 생거리) | 채택(강폭 단위) |
|---|---|---|
| 흐르는 물 셀 | 3,465 | 3,465 |
| 화면에서 **위로** 흐르는 칸 | 668 | **469** |
| 그중 **제 물길 밖**(u>1) | **155** | **0** |
| 방향 이음매(이웃 30°초과) | 59 / 6,727 · 최대 63.2° | **38** / 6,727 |
| 실제 픽셀 — 64px 블록 | 78블록 중 **11개 역류(14%)** | 92블록 중 **5개(5%)** |
| 파도 뭉개짐 \|라플라시안\| | 3.363 | **3.486** (섞기 안은 6.81) |

남은 469칸·38쌍은 **전부 닛폰대천의 진짜 물길 안**이다(표본 셀 u=0.60 — 중심선에서 72px, 반폭 120).
지류가 동→서로 흐르는 건 사실이고, 합류부의 전단선은 실제 강에서도 보인다. **거짓이 아니라 남은 사실**이다.
화면에서도 그 띠는 위쪽 가장자리로 밀려나 거의 안 보인다.

### 검증 — `e2e-terrain` ⑦ 신설 5판정 · 계약 판정 1건 정정

```
⑦ 흐름 방향 — 합류부(한여울강 × 닛폰대천)에서 물이 갈라지지 않는가
  ✓ ★자명 통과 금지 — 이 창에 흐르는 물 칸이 실제로 많다 (3465)
  ✓ ★★반례 — 대조군에서는 제 물길 밖에서 역류하는 칸이 실제로 나온다 (155) = 계측기가 결함을 본다
  ✓ ★★본류 물속에서 지류 방향을 따르는 칸이 0 (155 → 0)
  ✓ ★화면에서 역류하는 칸이 줄었다 (668 → 469)
  ✓ ★방향 이음매가 줄었다 (59 → 38)
```

정정 1건: `지면 텍스처 3종 로드` 가 `tex === 3` 이었다. 코드의 정본 문턱은 `_gtexReady >= 3`
(`_LEG = ... || _gtexReady < 3`)이고, **병행 세션이 `db055cb` 에서 `rock_angled` 를 더해 4종**이 되면서
이 판정만 빨개졌다(내 층 아님). 판정을 **완화**한 게 아니라 정본과 어긋난 사본을 정본에 맞췄다.

### 회귀

리베이스 **전** 트리(b083425): `terrain` **32/0** · `waterperf` **17/0**(흐름 텍스처 장당 흐름 계산
1.1ms · 최악 13ms — `FLOW_R` 확대 비용이 예산 안이다) · `nature` **73/0** · `tilestate` 39/0 ·
`cutaway` 15/0 · `metallurgy` 20/0 · `rooms` 55/0 · `village` 32/0 · `guest-reconnect` 40/0.
적신호 3종(`mountain` 21/2 · `mtfoot` 8/9 · `mtocc` ①)은 **깨끗한 b083425 워크트리에서 같은 판정·
같은 숫자로 재현**했다(|Δ|40.16 · vy 0.34/0.45 · 가린 산 0장) ⇒ 내 것이 아니다. 산 세션 층이라 안 건드렸다.

리베이스 **후**(origin/main 26af16a 위) 12종 전수, 순차 단독 — **369 통과 / 5 실패**:
`terrain` **32/0** · `nature` **73/0** · `waterperf` **17/0** · `tilestate` **39/0** ·
`mtocc` **13/13**(산 세션이 고쳤다 — 전 적신호 해소) · `cutaway` 15/0 · `metallurgy` 20/0 ·
`rooms` 55/0 · `village` 32/0 · `guest-reconnect` 40/0.
적신호: `mountain` 20/3 · `mtfoot` 8/9 · `mtfuzz` 5/6.

### ★적신호 귀속 — 두 건은 확정, 한 건은 **원인을 특정 못 했다**(그대로 적는다)

깨끗한 `26af16a` 워크트리(같은 기계·순차 단독)에서 같은 하네스를 돌려 비교했다.

| 판정 | 내 트리 | 대조 26af16a | 귀속 |
|---|---|---|---|
| `mtfoot` ⑤ 기슭이 산보다 납작 | ✗ vy 0.34/0.45 | ✗ **같은 숫자** | 산 세션 |
| `mtfuzz` P4 덮개 | ✗ ① 2% 초과 9회 | ✗ **같은 문구** | 산 세션 |
| `mountain` ⓑ2 산이 덮은 뭍 셀 0 | ✗ | ✗ | 산 세션 |
| `mountain` ⓓ 되돌리면 그대로 | ✗ \|Δ\| 1.78 | ✗ \|Δ\| 1.78 | 산 세션 |
| **`mountain` ⓓ 판 자리 재계산** | ✗ \|Δ\| 1.78 (3/3 재현) | ✓ \|Δ\| 34.94 (2/2) | **미특정** |

마지막 한 건은 내 트리에서 3번 다 실패하고 대조군에서 2번 다 통과했다. 그래서 더 팠다:

- **파괴 전 화면은 두 트리가 픽셀 단위로 같다** — 상자 \|Δ\| **0.00**, 화면 전체 0.02.
  즉 그리는 그림 자체는 안 바뀌었고, 달라진 건 **파괴 뒤 2.2초 동안 얼마나 다시 지었나**다.
- `mt3budget = 1` — 3D 산 청크를 **프레임당 1장씩** 다시 짓는다. 그런데 그 2.2초 동안 실제로 그린
  프레임이 **내 트리 5장 · 대조군 4장**이다(≈2fps, headless SwiftShader). 내 쪽이 오히려 많다.
- 하네스에 스크린샷 한 장(≈100ms)을 끼워 넣자 **대조군도 똑같이 \|Δ\| 1.78 로 실패**했다.
  ⇒ 이 판정은 **고정 `sleep(2200)` 안에 우연히 그려지는 4~5프레임**에 걸려 있다.
- 커밋 단위로도 갈랐다 — 내 5커밋 중 **가장 앞선 두 개**(문서 + `fringeOff`·카펫 상수)에서 이미 재현된다.
  물 관련 커밋 두 개는 이 결과를 바꾸지 않는다. 그 두 커밋이 프레임 비용을 올리지 않는다는 건
  위 프레임 수(5 vs 4)가 말한다.

**결론: 내 층에 원인을 특정하지 못했고, 판정 자체가 프레임 수에 걸려 불안정하다.**
"이건 환경 탓"이라고는 말하지 않는다 — 대조군이 통과했다는 사실은 남아 있다.
산 세션에 넘긴다. 고칠 거라면 `sleep(2200)` 대신 **조건 대기**(더티 청크 0)나 시험용 `mt3budget` 상향이
정답이다. (배치 21 §6-h 의 교훈과 같은 계보: *대조군이 통과했다고 내 것이 아님이 증명되는 건 아니고,
대조군이 실패했다고 환경 탓인 것도 아니다.*)

### ★합류 — 병행 세션이 카펫 흔들림을 되돌렸다. **실측상 손해는 아니었다**

내가 §6-i 에서 재민 지시("여전히 안 흔들려")로 `GT_GRASS_AMP` 2.2 → 3.4 + 2차 파를 넣었는데,
병행 세션이 그 뒤 **짝 비교로 카펫 흔들림 하나가 16.3ms/f** 임을 재고 되돌렸다(2.2 · 2차 파 제거 ·
목적지 정수 반올림 · 화면 밖 띠 잘라내기). 리베이스에서 **그쪽을 취했다** — 성능 수리를 조용히
되돌릴 수는 없다. 그런데 움직임 자체는 줄지 않았다:

| | 내 3.4 + 2차 파 | 병행 세션 2.2 + 정수 목적지 |
|---|---|---|
| `nature` ⓘ 초원 카펫 움직인 픽셀 | 282,120px | **291,667px** |
| `nature` ⓘ 강가 카펫 | 268,xxx급 | 260,043px |

소수 목적지의 이중선형 재샘플링이 오히려 잎을 뭉개고 있었던 것으로 보인다.
**재민이 라이브에서 다시 보고 판단하면 된다** — 세기 손잡이는 `GT_GRASS_AMP` 하나다.

---

## 6-l. ★다이아몬드 **이음매** — "1셀 두께로 줄무늬, 위치가 엇갈려 있다"

> 재민: *"디더없음 확대에, 동에서 서로 1셀 두께로 흐름이 있는 게 그거 때문이라는 거야?
> 심지어 그 1셀도 위치가 엇갈려있는 모양새야.. 그리고 줄무늬 모양.."*
> 그리고: *"저 물방울처럼 생긴 건 뭐야..?"*

**아니었다.** §6-k(합류부 흐름)와 **원인도 층도 다르다.** 재민 지적이 맞았고 나는 다른 걸 고쳐 놓고
그걸로 설명하려 했다. 그래서 물 셰이더의 항을 **하나씩 꺼서** 갈랐다(손잡이 신설:
`specOff` · `foamOff` · `waveOff` · `sineOff` · `ripOff` · `ripAmp` · `ripScale`/`ripW` · `ripScale1`/`ripW1` · `dither`).

### 갈라 보니 **두 겹**이었다

| 껐을 때 | 줄무늬 띠 고주파(\|라플라시안\|) |
|---|---|
| 기본 | 4.37 |
| 반짝임(spec) | 4.14 — 아님 |
| 포말 | 4.37 — 아님 |
| 물가선 굽이 0 | 4.37 — 아님 |
| **파도(사인+잔결)** | **1.92** — 여기 있다 |
| 물 통째로 | 1.08 ← **이 1.08 이 지면이다** |

**⇒ ① 깨끗한 1px 격자선 = 지면 베이크의 다이아몬드 이음매** (물을 꺼도 남는 1.08).
**⇒ ② 그 위의 점묘 = 물결.**

### ①은 고쳤다 — 셀을 한 장씩 칠하면 변마다 1px 이 빈다

캔버스 안티앨리어싱은 맞닿은 두 다이아몬드의 경계 픽셀에 각각 ~50% 피복을 준다.
`source-over` 로 겹쳐도 0.5 + 0.5·0.5 = **0.75** 라 25% 가 비고, 그게 **1px 짜리 이음매**로 남는다.
실측: 물밑 상자에서 픽셀의 **7.07%** 가 가로 어두운 선이었다.
초원에서는 풀 질감이 가려 안 보이는데 **반투명한 얕은 물 밑은 배경이 매끈해서 그대로 드러난다.**

수리: **불투명하게 칠하는 경우에만**(`alpha ≥ 0.999`) 도형을 수직으로 `GT_DIA_GROW = 0.75px`
부풀려 이웃과 겹친다. 물 셀의 진흙 무늬는 클립 도형도 같이 부풀렸다(무늬가 월드 정렬 반복이라
겹쳐 그려도 같은 그림이다). **반투명 층은 안 건드렸다** — 부풀리면 겹친 자리가 두 번 섞여
**반대 부호의 줄**이 생긴다.

실측: 물 끈 지면에서 그 상자 고주파 **1.08 → 0.35 (−68%)**. 눈으로도 격자가 완전히 사라졌다
(`증거_배치21/물층_분해_기본_파도끔_물끔.png` 오른쪽 열이 수리 전, 아래 §회귀의 그림이 수리 후).

### ②는 **못 고쳤다** — 가설 다섯을 실측으로 기각했다

사인파(s1+s2)와 잔결(sn)을 따로 껐다: 기본 3.89 · 사인끔 3.34 · **잔결끔 2.36** · 둘다끔 1.19.
⇒ 잔결이 사인파의 **3배**다. 그래서 잔결을 네 방향으로 건드려 봤다.

| 안 | 줄무늬 띠 | 결과 |
|---|---|---|
| n2 격자 3.2 → 4.0 → 6.4 → 8.0 | 4.37 → 4.23 → 4.02 → 3.94 | **기각** — 손잡이는 먹었는데(A↔D \|Δ\| 1.33) 눈으로 구분 불가. ※주범은 n2 가 아니라 **n1**(가중 0.55)이었다 |
| n1 격자 8 → 12.8 → 16 → 25.6 | 3.89 → 3.94 → 3.91 → 3.91 | **기각** — 무효 |
| 잔결 **세기** 1.0 → 0.65 → 0.40 → 0.20 | 3.89 → 3.04 → 2.64 → 2.44 | **먹힌다. 그런데 폐기** — 아래 |
| 8비트 계단 디더 ±0.5/1/1.5 LSB | 3.89 → 4.01 → 4.32 → 4.76 | **기각** — 구조가 안 깨지고 노이즈만 는다 |

**세기 안을 폐기한 이유는 하네스가 잡았기 때문이다.** `ripAmp = 0.40` 으로 채택했다가
`e2e-terrain` ①이 **"물이 안 움직인다"**로 빨개졌다 — 0.12초 최적 이동 **8.1px → 0.0px**,
SAD 2.45/3.07 → 1.9/1.9(무이동과 동일). 즉 **점묘를 만드는 성분과 "물이 흐르는 것처럼 보이게"
하는 성분이 같은 것**이다. 사인파는 파장 32·64px 의 저주파라 그 역할을 못 한다.
⇒ **판정을 낮추는 대신 안을 버렸다.** 전부 출시값으로 원복(잔결 1.00 · 격자 8.0 · 디더 0)했고,
**손잡이만 남겼다** — 다음에 다시 붙을 때 처음부터 재지 않아도 된다.

### "물방울" — 물밑 지면이 비치는 것

| | 국소 대비(어두울수록 음수) |
|---|---|
| 기본 | −13.8 |
| 파도만 끔 | −10.5 |
| **물 통째로 끔** | **−7.5** ← 출처는 지면 |
| 시각 100 → 101 → 102 | −13.8 → −11.3 → −9.2 (**자리는 고정**, 세기만 오르내림) |

⇒ 물밑 지면의 자갈·얼룩이 **반투명한 얕은 물을 통해 비치고**, 그 위를 지나가는 물결이 세기를
흔든다. 배치 19 가 넣고 하네스 ②가 지키는 "얕은물은 물밑을 따라간다"(r=0.836)가 작동한 결과다.
①과 같은 계열 — **배경이 매끈한 물 밑이라 도드라져 보인다**.

### 회귀

`terrain` **32/0** · `nature` **73/0** · `tilestate` **39/0** · `waterperf` **17/0** ·
`cutaway` 15/0 · `rooms` 55/0 · `village` 32/0 · `metallurgy` 20/0 · `guest-reconnect` 40/0
— **323 통과 / 0 실패**.

---

## 7. 회부 (구현 안 함)

- ★**얕은 물의 물결 점묘**(§6-l ②) — 가설 다섯을 실측으로 기각했고 **원인 미특정**이다.
  확실한 것: 잔결 노이즈가 주범이고, 그 세기를 줄이면 점묘와 함께 **흐름의 인상도 같이 죽는다**.
  다음에 붙을 사람에게: 손잡이는 다 있다(`ripOff`/`ripAmp`/`ripScale1`/`dither` …). 남은 갈래는
  ⓐ 얕은 물에서만 잔결을 **키워** 1~2 LSB 가 아니라 제대로 된 물결로 만들기(wa 로 나눠 보정),
  ⓑ 잔결을 화면 격자가 아니라 **월드 격자에 맞춰** 렌더(다운샘플 텍스처), ⓒ 그대로 두기.
  ※§6-l 의 표를 먼저 읽어라 — 이미 기각한 안을 다시 하지 마라.
- ★**합류부 전단선 — 파도를 '흐름 포텐셜'로 다시 짜기**(§6-k 의 남은 38쌍). 지금 남은 방향 이음매는
  **닛폰대천의 진짜 물길 경계**라 거짓은 아니다. 더 매끄럽게 하려면 방향을 섞어야 하는데,
  이 셰이더에선 그게 파도를 무너뜨린다(§6-k 폐기 1안). 제대로 하려면 파도 위상을 `dot(w, dir)` 대신
  **스칼라 포텐셜 Φ(∇Φ ≈ dir)** 로 바꿔야 한다 — 흐름장에 Φ 채널을 굽고 셰이더는 그걸 읽는다.
  배치 규모가 다르고, 지금 화면에서 그 띠는 위쪽 가장자리로 밀려 거의 안 보인다. **권고: 두고 본다.**
- ★**하구 감쇠** — `_riverSegs` 의 5번째 값(하류 진행도)이 **선언만 되고 안 쓰인다**(배치 19부터).
  주석의 의도는 "하구 근처는 흐름을 감쇠(호수·바다는 무방향)"였다. 지류가 본류에 드는 자리도
  이걸로 부드럽게 할 수 있다. 다만 '잔잔한 물 패치'가 생겨 또 다른 인상을 준다 — 시안이 필요하다.

- **계절** — 낙엽수 겨울 변형·설경. 시계 연동은 별개 시스템이다. 지금은 여름 한 벌.
- **프리즘 '상단 풀 넘김 립'** — §6-d 에서 내가 "띠의 진범"으로 지목했으나 **실측이 부정했다**
  (화면 전체 1,975px). 진범은 §6-e 의 물 넘침이었고 **수리했다**. 립은 단색 2.2px 선이라
  여전히 미세하게 균일하긴 하다 — **물 넘침이 사라진 지금 화면을 보고 재민이 판단**하면 된다.
  건드릴 거라면: 없애거나 · 잎 마스크로 끊거나 · 프리즘 윗변 자체를 없애는 세 갈래.
- ~~**바람 흔들림 애니**~~ → **실장했다**(§6-g). 아래는 그때의 비용 예측이고, 실측이 그걸 확인했다
  (예측 0.7~1.0ms/f · 실측 **+0.24ms/f**). 남은 회부는 **지면 풀 텍스처**와 **나무 캐노피** 둘뿐이다.
- **바람 흔들림 애니(옛 기록)** — ★앞서 "정적 캐시와 정면 충돌"이라 적었는데 **그건 내가 틀렸다.**
  청크 캐시는 **자리**를 담지 픽셀을 담지 않는다. 그리기는 어차피 매 프레임이다.
  실측(강가, headless): 자연물 패스 **0.317 ms/프레임**(술 633 + 소품 122 = 755장, 장당 ~0.42µs).
  스프라이트마다 전단 변환을 걸면 2~3배 → **0.7~1.0 ms/f**. 60fps 예산 16.7ms 안에서 싸다.
  ⇒ **술·들꽃·나무는 흔들 수 있다.** 지면 풀 텍스처(베이크 blit 0.04ms/f)는 매 프레임 재베이크나
  WebGL 이관이 필요해 **비싸다** — 거기까지는 하지 마라.
- **다른 존 25곳 수종 차별화** — 사막·설원에 온대 활엽수가 서는 건 여전히 미검증이다
  (배치 19 §8 의 지면 질감 회부와 같은 자리). 한반도 단독 운영이라 이번에도 안 건드렸다.
- **물가 술의 게임플레이** — 갈대 채집(지붕 이엉 재료?)·부들. 지금은 순수 데코다.
- **`rock`·`mossrock` 재렌더** — 유지(질감 사실풍이라 안 튄다). `ore` 는 §6-b 로 노두 재생성됐다.
- ~~건물·경작지의 미탐사 셀 가리기~~ → **§6-c 로 해소**(재민 확정: 안 가본 곳엔 그 어떤 것도).
