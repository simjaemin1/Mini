# 설계안(회부) — "이 주석을 무기에 쓸까, 팔까"를 한 자리에서 비교하기

> 2026-08-02 배치 B11. **구현 금지 · 설계 판단 회부**(규칙 7 "설계 판단은 구현 전에 회부").
> 아래는 현행 코드를 읽고 정리한 진단과, 같은 단위로 놓기 위한 설계 후보다.

---

## 1. 지금 두 결정이 어디서 따로 내려지는가

| | 결정 | 코드 | 목적함수 |
|---|---|---|---|
| **주조** | 이 배합으로 무기를 만들까 | `sim/economy-sim.js` `_alloyMelt()` | `산출량 × castF × p(weapon) × min(1.2, grade) − Σ 재료 × p(재료)` |
| **교역** | 이 재화를 어디로 보낼까 | `sim/economy-sim-v2.js` 캐러밴 화물 선택 | `p_to×(1−τ)×(1−손실) − p_from×(1+τ) − 운반비` |

두 식 **모두 마을의 그림자가격 `p` 를 쓴다.** 그래서 얼핏 이미 통합된 것처럼 보인다.
문제는 **어느 쪽도 상대방의 기회비용을 안 본다**는 것이다:

- 주조는 주석의 비용을 **자기 마을 그림자가격**으로 친다. 이웃 마을이 그 주석을 세 배로 사 준다는
  사실은 이 식 어디에도 없다. 주석 산지는 주석이 흔해 `p(tin)` 이 낮고, 그래서 주조가 **항상**
  남는 장사로 보인다.
- 교역은 잉여(`surplus`)를 판다. 그 잉여 정의에 "대장장이가 내일 녹일 몫"이 빠져 있으면
  같은 주석을 두 결정이 각자 다 쓴 셈이 된다(선착순으로 하루가 정해진다 — 틱 순서 의존).

⇒ **한 재화의 기회비용이 결정마다 다르다.** 이건 계수 문제가 아니라 목적함수의 자리 문제라 회부한다.

### 실측 근거 (2026-08-02, 실지도 800일 · 3시드)

- 구리·주석이 **캐러밴에 거의 안 실린다**: 최근 400건 중 구리 0~1건, 주석 1~9건.
  같은 창에서 나무 81~131건 · 식량 66~78건.
- 주석 산지 어촌1(주석 91%)의 800일 재고: 주석 38~41 · 원석 199~359. 대장장이 1명.
  주조는 임업2·광산4 두 곳에서만 일어난다(§2 참조 — 그건 부존 문제라 별개다).
- 즉 지금은 "팔까 쓸까"가 갈등하지도 않는다 — **둘 다 거의 안 한다.** 통합의 값어치는
  갈등을 해결하는 데 있는 게 아니라, **금속이 움직이기 시작할 때 엉키지 않게** 하는 데 있다.

---

## 2. 같은 단위로 놓는 방법 — 후보 셋

공통 아이디어: **투입재의 기회비용을 "쓸 수 있는 가장 좋은 대안"으로 정의한다.**

```
  opp(r) = max( p_local(r),  max_over_dest[ p_dest(r)×(1−τ)×(1−손실) − 운반비 ] )
```

### (가) 주조식의 재료비만 `opp` 로 바꾼다 — **최소 변경**

`_alloyMelt` 의 `cost` 를 `Σ mix[k] × _MELT_TOTAL × opp(k)` 로 바꾼다. 교역 쪽은 손대지 않는다.

- 장점: 한 줄. 회귀 위험이 가장 작다. "수출가가 높으면 안 녹인다"가 바로 성립한다.
- 단점: 비대칭이다. 교역은 여전히 "대장장이가 쓸 몫"을 모른다. 주조가 물러선 자리를 교역이
  가져가므로 **결과는 대체로 맞지만**, 반대 방향(교역이 물러서고 주조가 가져가는 경우)은 안 생긴다.
- ⚠부작용 후보: 주석 산지가 주석을 **안 녹이고 다 팔게** 될 수 있다. 그러면 "청동 = 산지의 특산"
  이라는 현재 그림이 "청동 = 아무도 안 만듦"으로 뒤집힐 위험이 있다. 3시드 A/B 필수.

### (나) 하루 예산을 먼저 쪼갠다 — **배분 문제로 승격**

마을이 아침에 재고 `S(r)` 를 두 용도로 나눈다: `S_cast + S_export = S`.
한계수익이 같아지는 점에서 멈춘다(등한계 원리).

- 장점: 진짜 정합. 두 결정이 같은 제약을 공유한다.
- 단점: 새 상태(하루 예산)와 새 틱 순서가 생긴다. `tickWorldV2` 진입점 계약을 건드린다.
  ★규칙 3("NPC 로직은 랩 verbatim 이식만")과 충돌 소지 — 랩에 없는 새 구조다.

### (다) 그림자가격에 수출 옵션을 얹는다 — **가격 층에서 해결**

`computeShadowPrices` 가 재화별로 `p = max(내부 수요 기반 p, 최고 순수출가)` 를 내도록 한다.
그러면 주조식도 교역식도 **아무것도 안 고치고** 같은 숫자를 본다.

- 장점: 접점이 한 곳(가격 함수). 모든 소비처가 자동 정합 — 무기·도구·의복까지.
- 단점: **가장 위험하다.** 가격은 교역 EV 의 입력이기도 해서 되먹임 고리가 생긴다
  (수출가가 내 가격을 올리고, 올린 가격이 수출 EV 를 깎는다). `smeltTarget` 이 시장가로
  게이트를 걸었다가 자기 꼬리를 문 전례가 이미 있다(economy-sim.js §제련 게이트 주석).
  → 하려면 **감쇠(EMA)와 상한**이 세트로 필요하다.

---

## 3. 내 의견

**(가)를 3시드 A/B 로 재 보고, 결과가 "청동 0"으로 뒤집히면 기각**하는 게 값싸고 안전하다.
(나)는 값이 크지만 랩 정본과 어긋나고, (다)는 되먹임 전례가 나쁘다.

다만 **지금 당장의 값은 낮다** — §1 실측대로 금속 교역 자체가 거의 없어서, 통합해도 바뀌는 게 없다.
이 설계가 실제로 필요해지는 시점은 둘 중 하나다:

1. 구리 부존이 늘어 주조 마을이 여러 곳이 될 때(→ `회부_구리부존과_시딩.md`)
2. 플레이어가 철·운철을 마을에 팔기 시작해 금속이 실제로 흐를 때

⇒ **그때까지 이월을 권한다.** 지금 넣으면 검증할 현상이 없어서 A/B 가 시드 분산에 묻힌다.

---

## 4. 결정이 필요한 것

- [ ] (가)/(나)/(다)/이월 중 무엇
- [ ] (가)라면: 주석 산지가 주석을 다 파는 결과가 나와도 **그게 옳은가**
      (고증으론 옳다 — 산지는 원료를 팔고 청동은 소비지에서 만든다. 다만 현재 세계엔
      살 사람이 없어 그냥 재고만 쌓인다)
