# 보고 — 라이브 존이 몇 초씩 멎는 이유와 수리 [2026-08-31 밤]

> 전날 `다음세션_인계.md` 3-s 에 **관측만** 남겨 둔 건이다. 그때 후보 셋을 적었다:
> ① 마을 시뮬 틱 ② 교역로 A\* ③ 부패/작물 신규 틱.
> **①이 맞다. 그런데 ① 안에서 지목한 자리가 예상과 달랐고, ②는 생각보다 훨씬 작았다.**

---

## 0. 한 줄

게임일 틱의 **생활층 신축 터 탐색**이 **비싸고 캐시도 없는 지형 통행 판정**을 게임일마다
3만~9만 번 부르고 있었다. 1회 ≈ 50µs 라 그것만으로 하루치 0.8~2.5초.

비싼 이유와 자주 부르는 이유가 **따로**였고, 그래서 수리도 둘이다 — 둘 다 **기본 꺼짐**이다.

| 수리 | 없애는 것 | env |
|---|---|---|
| **ⓐ 타일 지형 메모** | 같은 셀을 **다시** 묻는 비용 | `TERRAIN_TILE_CACHE=1` |
| **ⓑ 선분 격자 색인** | **처음** 묻는 비용(강·산맥 path 선형 주사) | `TERRAIN_SEG_INDEX=1` |

둘 다 켜면(같은 DB 스냅샷 · 야생·도적 켬):

```
존 기동~서버 열림   52.2초 → 12.1초   (4.3배)
게임일 틱 중앙값     879ms →  160ms   (5.5배)
게임일 틱 최대      6,534ms →  935ms   (7.0배)
멎음(부팅 제외)   23회 25.8초 → 1회 0.9초
```

**켜는 건 재민 판단.** 등가성은 51,474점 비트 대조로 증명했다(§2-다).

---

## 1. 어떻게 범인을 지목했나 (짐작 금지)

### 1-가. 재현

로컬에 실운영과 같은 구성으로 존을 띄웠다 — `ENABLE_VILLAGES=1 VILLAGE_MAX=51 ENABLE_ROADS=1`,
게임일만 `VILLAGE_DAY_MS=3000` 으로 줄여 하루를 3초로 압축.
바깥에서 `/health` 를 찔러 재는 프로버와, 안에서 이벤트 루프 지연을 재는 계측기를 **동시에** 걸었다.
둘 다 같은 그림을 냈다 — **멎음이 정확히 3초 주기**로 왔다. 곧 게임일 경계다.

```
16:28:12 STALL 1104ms · 16:28:15 STALL 1209ms · 16:28:18 STALL 1177ms · 16:28:21 STALL 966ms
```

### 1-나. 프로파일 — "무엇이 막았나"

V8 CPU 프로파일러를 **이벤트 루프 지연계와 같은 시계**로 묶어, **막힌 구간의 표본만** 셌다.
(두 시계가 같은 `CLOCK_MONOTONIC` 원점인지 하네스가 먼저 검사한다 — 어긋남 0.4ms.)

막힌 동안의 포함 시간:

| 층 | 비중 |
|---|---|
| `onGameTick` (villages.js:2586) | **95.5%** |
| └ `_lifeDaily` (villages.js:3780) | **76.7%** |
| &nbsp;&nbsp;└ `_lifeAddHouseSite` (villages.js:3496) — 신축 터 탐색 | **49.2%** |
| &nbsp;&nbsp;&nbsp;&nbsp;└ `reject` → `isBlocked` → `isTerrainBlockedLocal` (zone.js) | 36.4% |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└ `_isPointInRiver` → `_pointToSegmentDist` (terrain.js:138) | **말단 최대** |
| └ `syncCaravanBodies` → `routePath` (**교역로 A\***) | 10.1% |

**A\* 는 10% 였다.** 후보 ②를 먼저 의심했는데 아니었다. 후보 ③(부패·작물)은 상위 40위 안에 없다.

### 1-다. 계수 — "몇 번 물었나"

제품 코드를 안 건드리고 `terrain` 의 export 를 감싸 셌다:

```
게임일 1일당 지형 질의 32,900 ~ 88,700회 · 소요 756 ~ 2,471ms
그 중 같은 셀 재질문 27 ~ 71%  (하루 안에서만. 날마다 같은 영토를 다시 훑으므로 날을 건너면 사실상 전부)
```

미시측정으로 1회 비용도 쟀다 — **균등 표본 50.4µs/회, 국소 블록 평균 54.0µs/회.**

### 1-라. 왜 그렇게 비싼가

`_isPointInRiver` 는 bbox 조기기각 뒤 path 를 **선형 주사**한다.
v7 스무딩 이후 **강 55개 4,241점 · 산맥 11개 2,140점** 이라, bbox 안에 든 점 하나가
수백 개 선분과 거리 계산을 한다. 그리고 **같은 셀을 다시 물어도 처음부터 다시 센다** — 캐시가 없었다.

### 1-마. 결정적 대조 — 멎음 = 게임일 틱, 1:1

야생·도적까지 켜고(실운영에 더 가깝게) 다시 재니:

```
멎음 37회 · 합계 42.0초
게임일 틱 37회 · 합계 41.8초      ⇒ 99.5% 일치
```

**야생·도적·몹은 멎음에 사실상 기여하지 않는다.** 멎음은 전부 게임일 틱이다.

---

## 2. 수리 ⓐ — 타일 지형 판정 메모 (`server/terrain-tilecache.js`, 기본 꺼짐)

**없애는 것: 같은 셀을 다시 묻는 비용.**

### 2-가. 왜 이 자리인가 (등가성의 근거)

`zone.js` 의 `isWaterTileLocal` / `isRockTileLocal` 은 입력을 **이미 타일로 양자화한 뒤**
그 타일의 **중심 한 점**만 terrain 에 묻는다 — 곧 `(tx, ty)` 의 순수 함수다.
지형 원천(`terrain.js _getZoneTerrain`)은 기동 시 1회 적재 후 런타임에 바뀌지 않는다
⇒ **무효화 규약이 필요 없다.**

★`terrain.js` 안쪽(`isWaterCellLocal` 자체)에 걸면 **안 된다**:
`chunk.js:447` 이 `x±D` 오프셋 점을, `fishing.js` 가 임의 점을 묻는다. 거기서 양자화하면 **답이 달라진다**.
그래서 캐시는 '이미 양자화된 두 호출자'에만 걸었다.

### 2-나. 모양

타일당 4비트(물-계산됨/물-값/바위-계산됨/바위-값)를 `Uint8Array` 하나에.
`2188 × 4063 = 8,889,844` 타일 = **8.5MB** (존 힙 8GB 대비 무시).
해양 존은 배열조차 안 만든다(`isWaterTileLocal` 이 상수 `true` 라 무의미).

### 2-다. 하네스 `scripts/test-terraincache.js` — 21/0

* ① **등가**: 재질문을 섞은 10,500회 질의가 원본과 전부 일치(불일치 0).
  ★자명 통과 금지 — 참·거짓이 둘 다 넉넉히 나왔는지 센다(물 474/10026, 바위 724/9776).
* ② **적중**: 원본 계산이 **고유 타일 수만큼만** 일어났다(compute 11,996 = 고유 5,998 × 2). 적중률 42.9%.
* ③ **이득**: 같은 표본 2회차 1ms vs 1회차 138ms(134배).
  ★대조군 — 캐시 없는 원본은 2회차도 안 싸진다(0.97배). 이게 없으면 "두 번째가 원래 빠르다"와 구별이 안 된다.
* ④ **배선**: `zone.js` 의 캐시 가지와 종전 가지에서 terrain 호출의 **인자식을 뽑아 글자까지 비교**한다.
  `terrain.js` 가 캐시를 안 문다는 것과, `chunk.js` 가 실제로 오프셋 점을 묻는다는 대조 근거도 검사.

---

## 2-B. 수리 ⓑ — 선분 격자 색인 (`server/terrain-segindex.js`, 기본 꺼짐)

**없애는 것: 처음 묻는 비용.** ⓐ 는 재질문만 없앤다 — 그래서 ⓐ 만 켜면 **부팅이 여전히 25초**였다.
프로파일이 그 자리를 정확히 짚었다:

```
부팅 구간: SimVillages.init 24.6초 중 computeAndInjectDistMatrix 24.2초 (71.8%)
  → isBlk → coarseOpen → isBlocked → isTerrainBlockedLocal → _isPointInRiver
  전부 **최초 질의**라 메모가 한 번도 안 듣는다.
```

### 2-B-가. 무엇을 바꿨나

`_isPointInRiver` 는 bbox 조기기각 **뒤** path 를 선형으로 훑는다(강 최대 674선분·산맥 733선분).
선분을 512px 격자에 미리 담아 두고, 질의점이 속한 칸의 **후보만** 본다.

실측: **bbox 안 질의에서 후보 평균 1.3개 vs 전체 평균 96개 (1.41%)**.

### 2-B-나. ★등가성의 근거 (왜 답이 안 바뀌나)

원본은 "어떤 선분 i 에 대해 `dist(p, seg_i) < halfWidth_i(t)` 인가"를 묻는 **불리언**이다.
구간 i 위에서 halfWidth 의 최대값은 `max(w1, w2)/2` 이므로, 그 조건을 만족하는 p 는
반드시 `bbox(seg_i) + max(w1,w2)/2` 안에 있다.
⇒ 그 **확장** bbox 가 덮는 칸에 선분 i 를 넣어 두면, 질의점 칸의 후보 목록은
**답이 참일 수 있는 모든 선분을 포함**한다. 검사 순서만 달라지고 결과는 같다.

### 2-B-다. 하네스 `scripts/test-segindex.js` — 14/0

★플래그는 **모듈 적재 때** 읽히므로 한 프로세스로는 on/off 를 못 겹친다.
⇒ **자식 프로세스 둘**을 띄워 각자 진짜 제품 경로로 답을 내고, 부모가 **비트 단위**로 맞춘다.

* 표본 **51,474점** — 강·산맥 path 점 주변을 halfWidth의 0·0.25·0.5·0.75·0.95·1.0·1.05·1.3·2.0배
  거리에 뿌려 **경계에 몰았다**(무작위만 쓰면 99%가 물 밖이라 등가성이 공짜가 된다).
  **셀 중심이 아닌 점이 과반**(36,282개) — `chunk.js:447`(x±D)·`fishing.js` 의 호출 모양 그대로.
* **답이 비트까지 동일** · 참 개수도 동일(물 15,507 · 바위 5,591).
  ★참·거짓이 둘 다 넉넉히 나왔는지 센다.
* 후보 수 1.41% · **최대 후보 20개**(0만 나오면 빈 격자라는 뜻이라 그것도 센다).
* 자식 소요 2,149ms → 221ms — **9.7배**(색인 구축 비용 포함).
* 배선: 거리 계산이 **한 곳뿐**임을 검사(색인 가지와 종전 가지가 갈리면 언젠가 어긋난다).

### 2-B-라. ★사슬을 닫았다 — "꺼짐"이 **원본과** 같은가

위 ①은 *켬 vs 끔*을 비교한다. 그것만으로는 **끔이 원래 코드와 같은지**는 안 밝혀진다
(반복문 모양을 바꿨기 때문이다). 그래서 `git show HEAD:server/terrain.js` 를 그 자리에 꺼내
같은 51,474점을 먹였다:

```
대조 51,474점 · 불일치 0 · 물 참 15,507
⇒ 변경 전 == 변경 후(꺼짐) == 변경 후(켬)
```

---

## 3. 결과 (같은 DB 스냅샷 · 같은 env · 플래그만 다름 · 야생·도적 **켬**)

| | 기동~서버 열림 | 부팅 멎음 | 게임일 틱 중앙 | 게임일 틱 최대 | 정상 멎음(부팅 제외) |
|---|---|---|---|---|---|
| 둘 다 끔(현행) | 52.2초 | 51.8초 | 879ms | 6,534ms | 23회 · 25.8초 |
| ⓑ 색인만 | 13.1초 | 12.4초 | 204ms | 1,088ms | 1회 · 1.1초 |
| **ⓐ+ⓑ 둘 다** | **12.1초** | **11.4초** | **160ms** | **935ms** | **1회 · 0.9초** |

읽는 법:

* **부팅을 4.3배 줄인 건 ⓑ 다.** ⓐ 는 최초 질의를 못 도와서 부팅에 무력하다.
* **틱 최대값(꼬리)을 죽인 것도 ⓑ 다** — ⓐ 만 켰을 때 남아 있던 5.9초짜리 이상치가
  ⓑ 를 켜니 1.1초로 내려왔다. 그 이상치는 **새 영토를 처음 훑는 날**(전부 미적중)이었다.
* ⓐ 는 그 위에서 중앙값을 한 번 더 깎는다(204ms → 160ms). 둘은 **겹치지 않고 포갠다.**

참고 — ⓐ 만 켰을 때(야생·도적 켬, 앞선 판):
멎음 37회 → 4회 · 합계 42.0초 → 6.9초 · 틱 중앙 966ms → 171ms.
**벽시계 중 멎어 있던 비율 45.1% → 0.9%**(프로파일 표본 기준).

★A\* 도 같이 싸졌다 — `path-core` 의 `blocked` 콜백이 결국 같은 술어를 부르기 때문이다.
부팅 로그의 `교역로 A*: 광산1↔광산6 603정점 11610ms` 도 이 경로다.

## 4. 아직 안 고친 것 · 안 밝힌 것 (정직하게)

### 4-가. 부팅 — **범인은 지목했고 4.3배 줄였다. 다만 아직 12초다**

프로파일에서 `SimVillages.init` 24.6초 중 **`computeAndInjectDistMatrix` 가 24.2초**였다
(마을 전쌍 BFS 거리행렬. 그 밑은 전부 지형 술어이고 전부 최초 질의다).
⇒ ⓑ 색인으로 **52.2초 → 12.1초**. 남은 12초는 시딩·영토 실물화·거리행렬의 순수 계산이다.
더 줄이려면 **비동기 분할**이라는 구조 변경이 필요해서 **손대지 않았다** — 밤중에 혼자 정할 크기가 아니다.
접속에는 무해하고 재시작 시간만 늘린다.

### 4-나. 실운영 33초 간격이 무엇인지는 **아직 확정 못 했다**

`Dockerfile.zone` 은 `--interval=30s --timeout=3s --retries=3`.
실패한 검사는 3초를 쓰므로 **다음 검사는 33초 뒤**에 온다 —
관측된 `16:00:08` · `16:00:41` 은 정확히 **연속된 두 검사**다.
즉 **33초 주기의 멎음원이 있는 게 아니라, 한 번의 긴 멎음이 두 검사를 덮은 것**으로 읽는 게 자연스럽다.

다만 실운영 게임일은 **24분**이라, 게임일 틱만으로는 24분에 한 번만 멎어야 한다.
`unhealthy` 는 연속 3회 실패라야 붙으므로, 그러려면 한 번의 멎음이 **60초 이상** 이어야 한다.
로컬 최악이 8.3초(마을 18 · NPC 607)였고 실운영은 **마을 51 · NPC 1,569** 이니 가능성은 충분하지만
**재지 않고 단정하지 않는다.**

**실기에서 한 번만 재면 끝난다.** 서버에서 이걸 붙여 넣으면 크기와 시각이 같이 나온다:

```bash
# 존 컨테이너 밖에서 30분간 재기 — 멎음의 '크기'와 '시각'을 같이 남긴다
docker exec durango-zone-hanbando sh -c 'echo ok' >/dev/null 2>&1 || echo "컨테이너 이름 확인"
end=$(( $(date +%s) + 1800 ))
while [ "$(date +%s)" -lt "$end" ]; do
  s=$(date +%s%N)
  curl -sf -m 20 -o /dev/null http://localhost:3020/health || echo "$(date -Is) 응답없음"
  e=$(date +%s%N); ms=$(( (e-s)/1000000 ))
  [ "$ms" -gt 500 ] && echo "$(date -Is) STALL ${ms}ms"
  sleep 0.5
done | tee /tmp/zone-stall.log
```

`STALL` 줄의 **간격이 24분**이면 게임일 틱이 맞고 이번 수리로 끝난다.
간격이 그보다 훨씬 짧으면 **다른 원인이 하나 더 있다** — 그때 그 로그를 가져오면 된다.

---

## 5. 재민이 결정할 것

1. **두 플래그를 실운영에 켤까?** (`TERRAIN_SEG_INDEX=1 TERRAIN_TILE_CACHE=1`)
   등가성은 51,474점 비트 대조 + 전체 회귀로 증명했고, 비용은 메모리 8.5MB(ⓐ)와 수십 KB(ⓑ)뿐이다.
   기본값을 켜짐으로 바꿀지도 같은 질문. — 자세한 선택지는 `회부_존_멎음_다음층.md` A.
2. **남은 부팅 12초(§4-가)** 를 다음 배치에서 다룰까(비동기 분할).
3. **§4-나 실측** — 위 스크립트를 한 번 돌려 줄 수 있나. 그거면 남은 물음이 닫힌다.

---

## 6. 이번에 얻은 교훈 (족보 후보)

> **"비싼 술어는 캐시가 아니라 호출 횟수로 죽는다 — 시간과 횟수를 짝으로 세라."**
> 프로파일이 `_pointToSegmentDist` 를 가리켰을 때, 그 함수를 최적화하는 길로 갈 뻔했다.
> 실제 답은 **함수가 아니라 반복**이었다(재질문 27~71%). 자기 시간 상위만 보면 "무엇이 느린가"는 알아도
> **"왜 그렇게 자주 불리는가"** 는 안 보인다. 프로파일(시간)과 계수기(횟수)는 짝으로 읽어야 한다.

> **그리고 그 다음 — "재질문을 없앴는데도 안 빨라지는 자리가 있으면, 거기가 '처음'이다."**
> ⓐ 를 켜고도 부팅 25초와 틱 최대 5.9초가 남았다. 그 둘의 공통점은 **전부 최초 질의**라는 것이었다.
> 캐시는 최초 질의를 절대 못 도운다 — 남은 게 있으면 그건 **1회 비용 자체**를 줄이라는 신호다(⇒ⓑ).

> **하네스 쪽: "플래그가 모듈 적재 때 읽히면, 한 프로세스로는 A/B 를 못 한다."**
> `test-segindex` 는 자식 프로세스 둘을 띄워 **진짜 제품 경로**로 답을 내게 하고 비트로 맞춘다.
> 같은 프로세스에서 색인을 흉내 내면 **내 사본을 검사**하게 된다.
> 그리고 켬/끔 비교만으로는 부족하다 — **끔이 원본과 같은지**도 따로 재야 사슬이 닫힌다(§2-B-라).
