# 보고 — 회부 전건 일괄 해소 (2026-08-02e 배치 6)

> 재민 "최대한 전부 해결하고 개선되는 방향으로 해봐, 작업량 많아도 돼. DB는 리셋해도 된다고 했어"
> 기준 `1751cf8` → `03d7c98`(인계) 이후. **모든 숫자는 실측이다.**
> 실지도 랩 = `scripts/econ-lab-real.js` 800일 · 19마을 · 시드 1020/7/42.

---

## 0. ★재민이 바로 쓸 것 — 라이브 DB 리셋 명령 블록

> `fly ssh`/`fly console` 은 **클로드가 시도하지 않는다**(절대 규칙 1 — 배포·SSH 채널은 재민 전용).
> 아래를 위에서 아래로 한 줄씩 붙여넣으면 된다. `<ZONE_APP>` = `fly.korea.toml` 의 app 이름.
> **순서가 곧 안전장치다: 세고 → 끄고 → 백업하고 → 지우고 → 켠다.** 전문은 `DB리셋_절차.md` §5-0.

```sh
# ① 무엇을 잃는지 먼저 센다
fly ssh console -a <ZONE_APP> -C "sqlite3 /data/world-hanbando.db 'SELECT type, COUNT(*) FROM buildings GROUP BY type ORDER BY 2 DESC;'"
fly ssh console -a <ZONE_APP> -C "sqlite3 /data/world-hanbando.db 'SELECT kind, COUNT(*) FROM claims GROUP BY kind;'"

# ② 백업 — 반드시 -wal 까지 (WAL 모드라 최근 쓰기가 거기 있다)
fly ssh console -a <ZONE_APP> -C "sh -c 'cd /data && TS=\$(date +%Y%m%d-%H%M) && cp world-hanbando.db world-hanbando.db.bak-\$TS && cp world-hanbando.db-wal world-hanbando.db-wal.bak-\$TS 2>/dev/null; ls -la /data | tail -20'"

# ③ 끄고  ④ 지우고  ⑤ 켠다
fly scale count 0 -a <ZONE_APP> -y
fly ssh console -a <ZONE_APP> -C "sh -c 'rm -f /data/world-hanbando.db /data/world-hanbando.db-wal /data/world-hanbando.db-shm && ls -la /data'"
fly scale count 1 -a <ZONE_APP> -y
fly logs -a <ZONE_APP> | grep -E "마을 시딩 시작|시딩:|zone server up"
```

기대 로그: `🏘️ 마을 시딩 시작 — 후보 51 → 선별 19` → 마을별 `시딩:` 18줄 → `zone server up` (부팅 4~5분).
**`central.db` 는 안 건드린다** — 계정·인벤·장비가 거기 있다.

---

## 1. 한눈에 — 손잡이별 판정

| 항목 | 손잡이 | 판정 | 근거(실측) |
|---|---|---|---|
| ① 석재 이중차감 | `STONE_MAINT=0` | **기각·이월** | 인구 3,424 → 3,274(−4.4%) |
| ① 석재 총량보존 | `STONE_MAINT_UNIFY=1` | **기각·이월** | 인구 3,424 → 3,049(−11%) |
| ① 기재 수정 | (코드 아님) | **★채택** | 이중 기재가 아니라 **분할 기재**였다 |
| ② 무기 하락 | (판정 지표 신설) | **종결** | 품질보정 총량 배치3 377 → **616 (+63%)** |
| ③ 구리 축 3건 | — | **이월** | §4 — 시간이 모자랐다(정직) |
| ④ 철검 위세축 | (이미 배선됨) | **검증 완료** | 수요 무게이트 · 공급만 시대 게이트 |
| ⑤ 노 조업 진척 | (계약 신설) | **★채택** | test-furnace 59 → **85/0** |
| ⑥ 말 사역 | `haulMul` | **★채택** | 열기 전 **3시드 비트 동일** · 연 뒤 운반비 ×0.62 |
| ⑦ 야금 아이콘 8종 | — | **★배포** | 실클라 E2E 20/0(96×96 로드 확인) |
| ⑧ DB 리셋 | — | **로컬 완료 · 라이브 절차 제공** | 재시딩 18마을 실증 |

**기준선 무손상**: 실지도 3시드 소멸 **0/19** · 좀비 **0** · 인구 3,424 ·
v2 CLI 5시드 소멸 **0/30** · 인구 **2,120**.

---

## 2. ① 석재 이중차감 — 실측이 두 수리안을 **모두 기각**했다

배치 지시대로 판정 지표에 **산골(land.stone ≥ 1.0) 마을의 1인당 부**(곳간+국고 식량환산+현금)를 넣었다.

| | 인구 | 소멸 | 좀비 | **산골 1인당 부** | 돌수출 | 무기Q |
|---|---|---|---|---|---|---|
| 현행(v1 0.02 + v2 0.04) | **3,424** | 0 | 0 | 18.1 | 5,640 | 616 |
| `STONE_MAINT=0` | 3,274 (−4.4%) | 0 | 0 | **21.4 (+18%)** | 3,961 (−30%) | 624 |
| `STONE_MAINT_UNIFY=1` | 3,049 (**−11%**) | 0 | 0 | **25.5 (+41%)** | 4,897 | 607 |

### 지표를 하나 더 넣으라는 지시가 정확히 옳았다 — 다만 결론은 반대쪽이었다

배치는 "소득 축이 꺾이면 안 된다"고 했고, 그래서 1인당 부를 재게 했다.
**그 지표는 두 안 모두에서 좋아졌다**(+18%, +41%). 지시의 조건만 보면 채택이다.
그런데 **총인구가 떨어졌다.** 둘은 모순이 아니다 —

> 석재 유지 수요는 낭비가 아니라 **채집·채광 노동을 먹여 살리는 수요**다.
> 걷으면 돌 산지는 인구가 줄어 1인당이 오르고, 세계 전체는 그만큼 작아진다.

총량 보존안이 **더 나쁜** 이유도 같은 결이다: 총 소비는 같은데 v2 값(0.04→0.06)이
**소비이자 동시에 가격·부패 목표(`subs×30`)의 입력**이라, 목표가 50% 뛰고 그 신호가 노동을 석재로 끌어당긴다.

### 결론 — 코드가 아니라 **기재**를 고쳤다

실측이 밝힌 진실: **이중 기재가 아니라 분할 기재였다.**
v2 0.04 = 주거 본체(집·구들·바닥) · v1 0.02 = 부대 시설(담장·숫돌) · **총 0.06/인/일**.
문제는 계산이 아니라 두 주석이 각자 "주거 유지 전부"인 척한 것이었다. 두 주석을 그렇게 고쳐 적었다.
손잡이 둘은 기본 OFF 로 보존 — 석재 시장을 다시 설계할 때 쓸 도구다.

---

## 3. ② 무기 하락 추세 — 지표를 바꾸니 **+63%**. 종결

배치 지시대로 판정 지표를 **품질보정 총량 Σ(수량 × `_weapQ`)** 로 바꿨다
(`_weapQ` = 마을 무기 스톡 품질 EMA, 막석검 0.2 ~ 명장 청동 1.0+). `ab-summary`·`econ-regress` 양쪽에 열을 넣었다.

| 실지도 3시드 | 품질보정 무기 | 무기 수량 | 인구 |
|---|---|---|---|
| 배치 3 기준(`dz`) | **377** | 1,170 | 1,610 |
| 배치 4(`k_opp`) | 592 | 1,836 | 3,415 |
| 배치 5·6(현행) | **616 (+63%)** | 2,017 | 3,424 |

★"−8%/−12%"는 **직전 설정과의 비교**였고 6마을 CLI 한정이었다.
배치 3 기준선 대비로는 큰 상승이다 — 마을이 안 죽으니 **무장할 사람이 생겼다.**
배치 지시의 종결 조건("배치 3 대비 유지·상승")을 충족하므로 튜닝 없이 종결한다.

**원인 분리**(지시대로): 하락 기여는 `ALLOY_OPP` 하나다 — 실지도 품질보정 631 → 592(−6.2%),
6마을 CLI 518 → 422(−18.5%). `SHIELD_*` 가 그 절반을 되돌린다(592 → 616).
합금 등급 0.79 → 1.21 의 대가로 받아들인 거래이고, **`ALLOY_OPP=0` 한 줄로 되돌릴 수 있다.**

---

## 4. ③ 구리 축 3건 — **이월(미착수)**

정직하게 적는다. 이 배치에서 **손대지 않았다.**

세 건(광산촌 시딩 슬롯 · 구리 파생 투입수요 · 원석 교역 금지)은 지시대로 **각각 개별 손잡이·개별 A/B**가
필요하고, 실지도 3시드 800일 한 판이 약 12분(2코어라 2판 동시가 상한)이다.
셋이면 최소 36분 + 구현·검증인데, 이 배치의 앞 항목들(①의 A/B 2회 + ⑤ 진척 계약 구현 + ⑥ 비트동일 실측)이
그 시간을 먼저 썼다. **반쪽으로 넣고 "A/B 했다"고 적는 것보다 안 한 것을 안 했다고 적는 게 낫다**(배치 규칙).

다음 배치가 그대로 이어받을 수 있게 준비는 해 뒀다:
- 판정 지표가 이제 셋이다 — 인구·소멸·좀비 + **산골 1인당 부** + **품질보정 무기**(`ab-summary.js`).
  ③-2(구리 파생 투입수요)의 기각 조건("합금 등급 평균 1.0 미만")은 랩 로그가 이미 매 판 찍는다.
- ③-3(원석 교역 금지)은 `_oreMixEff`(배치 3 채택분)와 정면으로 얽힌다 — **먼저 정합을 확인해야 한다.**
  `_oreMixEff` 는 "수입 원석에 광종을 싣는" 장치이고, 원석 교역을 막으면 그 경로가 통째로 죽는다.
  즉 ③-3 은 배치 3 채택분을 **되돌리는** 변경일 수 있다. 그 판단이 A/B 보다 먼저다.

---

## 5. ④ 철검 위세축 — 이미 배선돼 있었고, **캐논을 검증**했다

실서버(`doSellIronRelic` ← `sell_relic`)와 실클라("위세품 판매" 버튼, 마을 상점 패널) 양쪽에
배치 2 가 이미 완결해 뒀다. 이번에 한 것은 배치가 요구한 **시대 축 검증**이다(`test-relic` ⑦ 신설):

- 판매(수요) 경로에 시대 게이트가 **없다** — 소스 검사로 확인. 수요는 안 자른다(캐논).
- `economy-sim.js` 에 `iron_relic` 언급 **0건** — NPC 는 생산하지 않는다. 통로는 플레이어뿐.
- 공급만 시대가 연다: 같은 도가니로 철 수율 **청동기 3.4% → 초기철기 67.8%**.
- ★**닫힌 시대에도 0 이 아니다** — 재민 "어렵게는 만들 수 있어야 한다"가 코드로 지켜진다.

---

## 6. ⑤ 노·숯가마 조업 진척 계약 — 신설

배치 5 에서 회부했던 것을 이 지시로 풀었다. **장입 → 시간 → 출탕.**

- **노**: 장입(정광 1·숯 즉시 차감) → `data.job{startedAt, until, yield}` → 완료 후 클릭 = 출탕
- **숯가마**: 같은 계약. 배치 n회분은 **시간도 비례**한다
  (가득 채우기가 시간을 공짜로 압축하면 클릭 수가 물리를 바꾸는 셈 — 배치 5 수지 불변 원칙의 시간판)
- **이탈해도 진척 보존** — 벽시계라 접속을 끊어도, 서버가 재시작해도 흐른다(농지 `readyAt` 선례)
- 조업 중 재클릭은 남은 시간만 알려주고 재료를 더 먹지 않는다(연타 치트 차단)

### 회부 문서의 결론을 지켰다 — NPC 크루 공식은 **안 썼다**

`_crewD/L_BUILDSEC`(현장 체류 초)은 플레이어 축에 입력 변수가 없다. 대신 **시간의 근거를 온도에 뒀다**:

```
  t = 180초 × (1150 / T)^1.5,  하한 45초
  도가니로 150초 · (시대 열림) 괴련로 127초 · 미해금 노는 기본값 폴백
```

상수가 자의적이지 않고 `era.js` 물리와 한 몸이 된다 — **뜨거운 노일수록 빨리 끝난다.**
출탕 수율은 **장입 시점** 물리로 고정한다(중간에 시대가 열려도 소급 없음 — 소급되면 계약이 아니라 도박이다).

UI: 노·숯가마 위에 진척 게이지 + 라벨이 상태를 말한다(클릭=장입 / 조업 중 N초 / 클릭=출탕·수거).
서버 `_jobProgress` 와 클라가 **같은 식**을 쓴다.

`test-furnace` 59 → **85/0**. ⚠하네스는 3분을 기다리지 않는다 — `job.until` 을 과거로 당긴다
(기다리는 하네스는 아무도 안 돌린다 = 회귀 가드가 아니라 장식이 된다).

### 구현 중 실서버가 잡은 결함 둘

1. 수거 분기를 **재료 검사 아래**에 뒀더니, 수거 시점엔 통나무가 0이라 "통나무 부족"으로 막혀
   **숯을 영영 못 꺼냈다** → 분기를 재료 게이트 위로 올렸다.
2. 하네스 기준도 한 번 틀렸다 — 닫힌 시대에선 괴련로가 미해금이라 폴백 시간을 쓰는데,
   그대로 비교해 "뜨거운 노가 더 느리다"는 **거짓 결함**을 냈다 → 시대를 열고 비교하도록 수정.

---

## 7. ⑥ 말 사역 — 열기 전 **비트 동일**, 연 뒤 작동

소비처를 **교역 EV 안**에 뒀다. `netExportValue`·발주 EV 가 이미 운반비 항을 갖고 있으므로,
말은 그 항을 낮추는 것으로 자연 편입된다 — **새 재화도, 새 직업도, 새 상태도 만들지 않았다.**
(고증: 등짐 → 바리·수레. 육상 운반비 하락이 말 보급의 실체다.)

```js
  haulMul() = era.canTame('horse') ? 0.62 : 1
```

**닫힌 시대의 반환값이 곱셈 항등원 `1`** 인 것이 설계의 핵심이다 — IEEE 에서 `x * 1 === x` 라
궤적이 1비트도 안 갈린다. (`0.999` 같은 '거의 1'을 쓰면 그 순간 갈린다.)

### 실측 — 배치가 요구한 양쪽 다

| | 인구 | 소멸 | 좀비 | 도구 | 산골 부 | 돌수출 | 무기Q | 무기수 |
|---|---|---|---|---|---|---|---|---|
| 말 사역 **전**(배치 5 최종) | 3,424 | 0 | 0 | 5,217 | 18.1 | 5,640 | 616 | 2,017 |
| 말 사역 **후**(닫힌 시대) | 3,424 | 0 | 0 | 5,217 | 18.1 | 5,640 | 616 | 2,017 |

**전 열 완전 일치 · 3/3 시드 비트 동일.** 열기 전 영향 0 — 배치 조건 충족.

연 뒤 작동은 두 겹으로 실측했다:
- **산술**(`test-valuechain` ⑦): 순수출가 상승분이 **운반비 절감분과 정확히 같다**(오차 < 1e-9).
  다른 항은 안 건드렸다는 증명이다.
- **세계**(`era-rehearsal --lab --flip=400`): 시대를 열면 인구 **+1.0%** · 주조 마을 3 → 4.
  (배치 2 리허설에서는 **0.0%** 였다 — 그때는 시대를 열어도 세계가 안 변했고, 지금은 변한다.)

---

## 8. ⑦ 야금 아이콘 8종 · ⑧ DB 리셋

**⑦** `ore_chunk·iron_ore·charcoal·iron·meteoric_iron·copper·tin·lead` — 기존 파이프라인
(`icon_render.py` → `icons-postprocess.js` → 96×96)에 모델 8종·재질 7종 추가.
고증색: 적철석 검붉음 · 연철 회흑 무광 · 운철 비드만슈테텐 결 · 납 청회 · 주석 은백 ·
잉곳은 도가니 바닥에서 굳은 **빵떡 잉곳**.
★3차까지 다시 렌더했다 — 1차 4각뿔대 잉곳이 ISO 뷰에서 **초가지붕**으로, 운철 매끈구가 **골프공**으로,
숯이 **회색**으로 읽혔다. 실클라 E2E 에 "8종 전부 이미지로 로드 · 96×96 규격" 검사를 넣었다(**20/0**).

**⑧** 로컬 DB 리셋·재시딩 실증: 마을 **18**곳, 땅맞춤 부존 발효(stone 0.25 → 석재 190·도구 60 /
stone 2.07 → 석재 40·목재 152), `_bornDay` 저장 확인(SHIELD_AGE 필드), `mined_cells`·`harvested_seeds` 0(운철 부활).
디바이스는 배치 5에서 이미 `_to_delete/db-reset-20260802-0413/` 로 이동 완료(백업은 원위치 `.bak-…`).
라이브는 §0 블록으로 재민이 직접.

---

## 9. ⑨ 검증

| 검사 | 결과 |
|---|---|
| 하네스 11종 | **전부 PASS** (`test-furnace` **85/0** · `test-tame` 11/0 · `test-relic` ⑦ 추가) |
| 실클라 E2E | **20/0** (`node scripts/e2e-metallurgy.js`) |
| `lab-wiring-check` | **실패 0** |
| 번들·랩 2종 | 재생성 · 재인라인 `--check` 최신 · 브라우저 평가 통과 · 디바이스 동기 |
| v2 CLI 회귀 | 인구 **2,120** · 품질보정 무기 **422** · **소멸 0/30** |
| 실지도 3시드 | 인구 **3,424** · **소멸 0/19** · **좀비 0** · 품질보정 무기 **616** |

### ★다음 배치 기준선 (이 줄을 이어받아라)

```
  실지도 3시드 800일 : 인구 3,424 · 소멸 0/19 · 좀비 0 · 품질보정 무기 616 · 산골 1인당 부 18.1
  v2 CLI 5시드 800일 : 인구 2,120 · 소멸 0/30 · 품질보정 무기 422
```

---

## 10. 남은 것

1. **③ 구리 축 3건** — 미착수(§4). 다음 배치 1순위. ③-3 은 착수 전에 `_oreMixEff` 정합 판단이 먼저다.
2. **`ALLOY_OPP` 의 무기 대가** — 품질보정 −6%(실지도)/−18%(CLI). 합금 등급 +53% 와의 거래.
   되돌리기는 `ALLOY_OPP=0`. 재민 판단 대기.
3. **석재 손잡이 2종** — 기본 OFF 보존. 석재 시장 재설계 시 재측정 대상.
4. **운철 아이콘 판독성** — 은빛 각면이 화면에서 수정(水晶)처럼 읽힌다. 기능 무해, 미세 조정 후보.
5. **노 조업 시간 상수** — 180초/45초는 이번에 처음 정한 값이다. 실플레이 체감은 아직 안 재 봤다.
