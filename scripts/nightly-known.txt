# ═══ scripts/nightly-known.txt — 상시 빨강 목록(정본 하나) [T293 2026-09-14] ═══
#
# `scripts/nightly-report.js` 가 이 파일만 읽고 야간 보고의 **귀속 칸**을 채운다.
# 여기 있는 하네스가 빨개지면 "상시(<이유>)" 로 적히고, **여기 없는 빨강만** "귀속: 모델 몫"
# 으로 비워 둔다 — 아침에 사람이 볼 것은 그 칸뿐이다.
#
# ★사본 0: 이 목록은 이 파일 하나다. `인계/공통.md` §2 는 이 파일을 **가리키기만** 한다.
# ★올리고 내리는 것은 PM 이다(세션이 제 카드 빨강을 여기 넣어 조용히 만들면 안 된다 —
#   그건 "안 쟀는데 초록"과 같은 자리다). 근거 칸에 **판단한 카드·보고**를 반드시 적는다.
#
# 꼴: <하네스 이름> | <이유 한 마디> | <근거>
#   · 이름은 러너가 찍는 그대로(`test-body.js`) — `.js` 는 있어도 없어도 된다.
#   · `#` 로 시작하는 줄과 빈 줄은 무시한다.

test-tick-slicer.js | 기계 의존 | 공통.md §2 ⑥ (T185 2026-09-12) · 부하 아래서만 · 야간 09-14 단독 22/2
e2e-rtt.js          | 기계 의존 | 공통.md §2 ⑥ (T185 2026-09-12)
test-body.js        | 물때 두 판정 | 공통.md §2 ⑥ · ⑯㉧ 갯벌 문구·짠물 기운 쌍 · `TIDE_FREEZE_MS=372603` 이면 306/0 (보고/T171 · 야간 09-13·09-14)

# ── [T304 2026-09-19] 09-18 야간 빨강 둘을 가른 결과 ───────────────────────────
#   · `e2e-charsprite` 는 **안 넣는다** — 자 결함이었고 그 카드가 닫았다(보고/T304 §0-ⓐ).
#     대조군이 "같은 애니 판"이라는 전제를 말없이 어긴 것이지 제품도 상자도 아니었다.
#     (굶은 상자에서 그 절은 이제 **유보** 한 줄로 선다 — 증상 셋이 아니라.)
#   · `e2e-rumor` 도 **내렸다**(T327 2026-09-19) — 그 경주를 고쳤다. 승계는 이제 도착 순에
#     안 기대고(`zone.js` close 의 `_lastSeenDayToSave`), `⑦d2` 가 **두 순서를 다 돌려** 같은 답을 본다.
#     근거: 보고/T327 §0 — 고치기 전 conn 먼저 8일 vs close 먼저 2일, 고친 뒤 둘 다 누락 0일.
