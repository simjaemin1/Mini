# ~~회부~~ **철회** — 전제가 틀렸다. 철 자잘 광맥 184개가 실재한다

> ★★[2026-08-02f 재민 검증 세션 — 철회] 이 문서의 §1("철이 든 광맥 = 0개")은 **오진**이다.
> 전수 스크립트가 다광종 dict(`o.minerals`)만 훑어 **단일광종 광맥 362개를 통째로 놓쳤고**,
> 그 안에 철 자잘 184개 전부가 있었다(배치 4 CHECKLIST 기록과 일치: "자잘 724개 … 그중 철 184개").
> 정본 판독기(terrain.js oreMineralAt)는 dict 가 없으면 `o.mineral` 문자열로 떨어진다 —
> 전수 조사가 그 fallback 을 밟지 않았다. §1의 채광 실험도 구리·납 광맥만 캤다 —
> 철 광맥은 한 번도 캐 보지 않고 "철은 한 톨도 안 나온다"고 적었다.
>
> **정정 후 실증**(pace-metallurgy 수리판, 12 통과/0 실패): 철 자잘 광맥에서
> 채광 181타 → 선광 → **정광 2개(주입 0)** → 노 장입 → 출탕(청동기 수율 3.4%).
> 노의 입력은 처음부터 세계에 있었다. 플레이어가 자잘 철 광맥을 **탐사로 찾아** 캐는 것 —
> 그게 바로 배치 1 설계("철은 플레이어가 탐험해서 찾는 거야")다. 설계는 온전했다.
>
> §5 의 선택지 (가)~(라)는 전부 무효다 — (가)가 요구한 "자잘에 철 넣기"는 이미 되어 있다.
> 살아남는 것: §4 페이싱 판정(⑯ 해결 — 상수 180/240초 유지)과 "청동 사슬은 노를 안 탄다"는
> 관찰(선광 = 간이 제련 추상. 노에 태울지는 별도 설계 판단 — 현행 유지가 기본값).
> 아래 원문은 오진의 기록으로 남긴다.

---

# (원문 — 철회됨) 회부 — 노(爐)는 지을 수 있는데 넣을 것이 이 세계에 없다

> 2026-08-02f 배치 7 ②. `scripts/pace-metallurgy.js` 실측. **구현 금지 항목에 걸려 회부한다**
> (인계 §5 "NPC 철기 산업 콘텐츠(주요 광맥에 철 넣기)" = 회부만).

---

## 1. 사실 — 787개 광맥에 철이 **0개**다

`server/hanbando-terrain.json` 전수:

```
  광맥 787개 (주요 63 · 자잘 724)
  전 광맥에 등장하는 광종의 합집합 = { lead, silver, copper, gold }
  철이 든 광맥 = 0개
```

`tryFurnaceSmelt` 의 유일한 입력은 `iron_ore`(철 정광)이고, 정광은 `trySortOre` 가
**광맥 조성에 철이 있을 때만** 만든다. 조성에 철이 없으므로 정광은 **어떤 경로로도 안 나온다.**

실측으로도 확인했다 — 구리 광맥 400타, 납 광맥 400타를 캐고 선광한 결과:

```
  copper  "광맥168"  타400 덩이6  장부{silver 2.5, copper 7.1, stone 10.3} → 인벤 {copper 2}
  lead    "광맥72"   타400 덩이6  장부{lead 8.4, stone 7.6}                → 인벤 {lead 2}
```

**철은 한 톨도 안 나온다.**

## 2. 그래서 지금 노는 무엇인가

| | 상태 |
|---|---|
| 지을 수 있나 | **된다** (배치 5 에서 UI 배선을 고쳤다 · `e2e-metallurgy` 가 지킨다) |
| 조업 계약이 있나 | **있다** (배치 6 장입→시간→출탕 · `test-furnace` 85/0) |
| 넣을 것이 있나 | **없다** |

즉 배치 5·6 이 고친 두 층(UI 도달 · 시간 계약)은 정확히 고쳐졌고, 그 **아래 한 층**이 비어 있다.
이 프로젝트가 세 번째로 만나는 같은 모양의 결함이다:

1. (배치 5) 계약도 실행도 멀쩡한데 **화면에서 도달 못 했다** — 진입 버튼이 `display:none` 안에만 있었다
2. (배치 6) 화면에서 도달하는데 **시간 계약이 없었다** — 클릭 1회 = 즉시 제련
3. (지금) 시간 계약도 있는데 **입력이 세계에 없다**

> 인계 문서의 "★실측 — 시대를 열어도 NPC 경제는 0.0% 변화(NPC 주요 광맥에 철 0)" 은
> **NPC 쪽 이야기**로만 적혀 있었다. 플레이어 쪽도 같은 이유로 막혀 있다는 건 이번에 처음 잰다.
> (자잘 광맥 724개는 플레이어 전용 발견 요소인데, 거기에도 철이 없다 — 조성 집합이 같다.)

## 3. 지금 철을 만지는 유일한 길 — 운철

운철(meteoric_iron)은 지표 청크(대륙 42개)이고 **제련이 필요 없다**(녹이지 않고 두들긴다).
`EQUIPMENT_RECIPES.weapon.accepts` 에 들어 있어 단조로 바로 간다. 즉 **노를 우회한다.**

## 4. 페이싱 실측 — 상수는 문제가 아니었다

| 사슬 | 단계 | 실시간 |
|---|---|---|
| **청동**(도달 가능) | 채광 → 선광 → 단조 | **10~15분** · 100% 가 채광 · **노를 안 탄다** |
| **철**(입력 없음) | 숯가마 1회분 | 240초 |
| | 노 1장입 | 150초 |

그리고 같은 노를 시대만 바꿔 보면:

| 시대·노 | 수율 | 철 1덩이(3.5kg)까지 | 실시간 |
|---|---|---|---|
| 청동기 도가니로 | 3.4% | **30장입** | 75분 |
| 철기 도가니로 | 67.8% | 2장입 | 5.0분 |
| 철기 괴련로 | 88.1% | 2장입 | 더 짧다(노가 더 뜨거워 1장입 시간도 준다) |

**⑯의 답: 180초·240초 상수는 옳다.** 철기 시대에 2장입 = 5분이면 사슬이 성립하고,
청동기의 30장입(75분)은 버그가 아니라 **고증 그대로**다 — 청동기에는 철을 못 녹였다.
대기 판정도 통과한다: 숯가마 240초·노 150초는 둘 다 **채광 한 덩이(60초)보다 길다** —
"자리를 지키는 벌"이 아니라 다녀올 일이 되는 길이다.

⇒ 고칠 것은 상수가 아니라 **지도에 철이 없다는 것**이다.

## 5. 결정이 필요한 것

- [ ] **(가) 자잘 광맥 일부에 철을 넣는다** — 플레이어 전용이라 **NPC 경제 무영향**이 보장된다
      (캐논: "npc는 자잘광산은 평생 못 봐"). 노가 살아나고, 시대가 열리면 2장입 = 5분으로 성립한다.
      ★NPC 무영향은 가정이 아니라 코드다: `terrain.js` 가 `if (o.minor) continue` 로 두 곳에서 잘라낸다.
      ⚠다만 **minor 플래그를 떼는 승격은 금지**다(캐논). 조성만 바꾸는 것이므로 승격이 아니다.
- [ ] (나) **주요 광맥에 철을 넣는다** — NPC 철기 산업이 열린다. 인계 §5 회부 전용 항목 그대로.
      경제 기준선(인구·소멸·무기Q)이 전부 움직이므로 3시드 A/B 필수.
- [ ] (다) **그대로 둔다** — 노는 "철기 시대를 위해 미리 짓는 건물"로 남는다.
      지금은 못 쓰지만 시대가 열려도 여전히 정광이 없으므로 **영원히 못 쓴다**. 이 선택지는
      사실상 "노와 숯가마를 폐기한다"와 같다.
- [ ] (라) **운철을 노에 넣을 수 있게 한다** — 상태 증가 0. 다만 고증에 어긋난다
      (운철은 단조 재료다 — 녹이지 않는다). 그리고 운철은 이미 노 없이 쓸 수 있어 노의 이유가 안 된다.

내 의견: **(가)**. 노를 살리는 최소 변경이고, 캐논이 보장하는 **NPC 무영향**이 그대로 성립하며,
"플레이어는 자잘 광맥을 본다"는 설계 의도(발견 요소)와 정확히 맞는다.
(나)는 세계관 변경이라 별도 배치가 맞다. (다)는 배치 5·6 의 수리를 사문으로 만든다.

## 6. 왜 이번 배치에서 안 고쳤나

인계 §5 "구현 금지: NPC 철기 산업 콘텐츠(**주요 광맥에 철 넣기**)·자잘 광맥 미니맵 기록".
(가)는 자잘 광맥의 **조성**만 바꾸므로 문자 그대로의 금지 대상은 아니지만,
"지도에 철을 넣는다"가 회부 항목의 취지이고 **시대 축 전체의 전제를 바꾸는 판단**이라
규칙 7(설계 판단은 구현 전에 회부)에 따라 회부한다.
