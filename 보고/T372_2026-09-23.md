# T372 — 군락 종 **넷이 섰다**(정의만) · 스프라이트 넷 · 빈칸 **0** · `janggu_gung` 은 **안 고쳤다**(고칠 근거가 안 선다)

가지 `batch/grove-kinds-0923` · **측정 베이스 `e37f5ba6` · 착지 베이스 `28c722ff`**(도는 동안 main 이 4커밋 전진 — 리베이스 1회 · **충돌 0** · 그 4커밋이 건드린 파일과 내 영역 교집합 **0** ·
리베이스 뒤 게이트·청크 해시 전부 다시 쟀고 **같다**) · **force 0** · main 은 PM ·
**세계 무변 — 청크 해시 `2a4997298345c319` 가 main(`28c722ff`) 과 같다**(존 26 · 청크 936 · 개체 **7,746** · 전수 대조) ·
`sim/` 무접촉(3시드 면제) · **T359(세션1)·T354~T358(세션9) 무접촉** · `.blend` 손편집 0 · `bgm/` 소리 자산 무접촉

| 종 | `kind` | HP ← 빌린 자리 | 전리품 ← 빌린 양 | `REGROW` ← **출처** | 지형(`decideVillageType` 술어) | 스프라이트 |
|---|---|---|---|---|---|---|
| 버섯밭 | `mushroom_patch` | **2** ← 덤불(여러 번 딴다) | `{ mushroom: 2 }` ← 덤불 `berry` | `MUSHROOM_Y` **1년** ← 「송이버섯」*"해마다 가을이면"* · Matsutake *"once-yearly"* | `forest`(`getForestMultiplier > 2.0`) | `mushroom01` |
| 나물 | `greens_patch` | **1** ← 풀(한 줌) | `{ vegetable: 2 }` ← 덤불 `berry` | `GREENS_Y` **반 해** ← 나물 = **여러해살이 풀 그 자체**(T122 그 자리) · 고사리 매뉴얼은 더 빠름 | `plain`(술어 없음 = 초지·숲 가장자리) | `greens01` |
| 벌집 | `beehive` | **1** ← 풀(**통째로 뗀다** — 출처의 그 문장) | `{ honey: 2 }` ← 덤불 `berry` | `HONEY_Y` **1년** ← *"1년에 딱 한 번만 채취"* · *"only once per year … the entire comb"* ⚠한봉 기록 | `forest` | `hive01` |
| 야생포도 | `wild_vine` | **2** ← 덤불 | `{ grape: 2 }` ← 덤불 `berry` | `GRAPE_Y` **1년** ← **집 안 정본** `trees.json grape.fy`(연간 수율)·`fs 2`(가을) | `riverside`(`isWaterCellLocal` 220px) | `vine01` |

**빈칸 0 · 새 수 0**(HP 는 `RESOURCE_HP_TABLE` 을 **읽고**, 양은 `BUSH_BERRY_N` 을 **읽고**, 주기는 `REGROW.BUSH_Y()`·`HERB_Y()` 를 **읽는다** — 숫자 한 자 안 적었다)
게이트: `test-assets-audit` **PASS**(자산 768 · 잠긴 것 698) · 잠금 **506장 어긋남 0** · `test-audio` **125/0**(⑨l 무변)
`test-trees` 91/0 · `test-forage-act` **104/0** · `test-nippon-boot` 39/0 · `test-itemlabel` **58/0** · `test-regrow` 100/0 · `test-crops` 170/0 · `test-wood-act` 87/0
`e2e-nature` **145/0** · `e2e-forage-village` 12/0 · `test-charsheet` 192/0 · `test-harness-lint` **57/0** · **빨강 0 · 물려받은 빨강 0**

## ★ 답 한 줄

**종 4 · 빈칸 0 · 스프라이트 4 · janggu 는 값 무변 — T353 §0-ⓐ ③ 의 추론이 안 서는 것을 실측으로 잡았다(내가 쓴 전제다)**

---

## §0-ⓐ ③ 뒷절 — **`janggu_gung: "합성"` 을 못 고친다. T353 이 틀렸다.**

카드는 *"증명은 T353 §0-ⓐ"* 라고 적었다. 그 증명을 다시 밟았더니 **안 선다** — 그리고 그 §0-ⓐ 는 **내가 쓴 것**이다.

| # | T353 이 적은 것 | T372 실측 |
|---|---|---|
| ① | `compose.py:123` 이 `janggu_gung(…, art="both")` 인데 `gugak` 엔 `art` 가 없다 | **맞다.** AST 실측 — `janggu_gung(dur,amp,tune,seed,strike,stick,damp)` · `janggu_chae(dur,amp,tune,seed,strike,open_)` · 둘 다 `**kw` 없음 |
| ② | *"⇒ 굽던 자리에 janggu 뱅크가 있었다"* | **안 따라 나온다.** 그 추론은 **이 표가 배포 굽기를 적는다**를 전제하는데, 의심스러운 게 바로 그 전제다 |
| ③ | *"⇒ `source.janggu_gung: '합성'` 도 틀렸다"* | **못 잰다.** 틀렸다고도 맞다고도 못 한다 — 아래 넷 |

**실측 넷 — 표와 코드는 같은 판에서 나올 수 없다.**

* ⓐ 이 `source` 는 **`sampler.install()` 이 낸 값**이다. 키 차례가 `install()` 의 `PITCH + PERC` 열한 개와
  **글자 그대로 같고**(`gayageum geomungo daegeum danso piri janggu_gung janggu_chae buk jing kkwaenggwari bak`),
  `render_samples.py` 가 `{"source": used, "tracks": meta}` 로 적는다.
* ⓑ `install()` 은 `bank.has(inst)` 가 **거짓일 때만** `"합성"` 을 적고 — **그때는 함수를 안 감싼다**.
* ⓒ 그러면 `compose.py:123`·`:134` 가 **TypeError 로 죽는다.** 그리고 그 둘은 **여섯 장단이 다 지난다**:
  `G`(=`art="both"`) 타법 **6/6** · `STROKE_ART`(=`ring=`·`art=`) 타법 **6/6**(굿거리·중중모리·세마치·자진모리·휘모리·진양).
* ⓓ ⇒ **이 `source` 열과 지금 `compose.py` 는 같은 판에서 나올 수 없다.** `tracks` 열도 같은 말을 한다 —
  `_T257` 이 이미 이 표의 `seconds` 가 배포 ogg 와 **12곡 전부 안 맞는다**고 쟀다(`village_day_trad` 109.02 ↔ 108.28).

⇒ **값은 그대로 둔다**(`"합성"`). 메모 `_T372` 한 줄만 더했다 — 못 잰 것을 잰 척하지 않는다.
`test-audio ⑨l`(표 ↔ README 목록)도 **무변 125/0** 이다.

★**고쳤다면 무엇이 깨졌나 — 이것이 값을 안 고친 두 번째 근거다.** ⑨l 의 한글 표는
`janggu_gung: '장구'` **와** `janggu_chae: '장구'` 다(둘이 한 낱말을 쓴다). 궁 하나만 '합성' 에서 빼면
'장구' 는 README 목록에 **있으면서 없어야** 한다 — **만족 불가능**이다. 자를 고치거나 채까지 건드려야 하는데,
둘 다 **근거가 없는 편집**이다(§위 ⓓ).

★**잴 수 있는 자리 하나** — 뱅크가 있는 자리(재민 데스크톱 · #64)에서 `render_samples.py` 를 한 번 돌리면
`install()` 이 이 열을 **제 손으로 다시 적는다**. 그게 이 칸의 유일한 자다 ⇒ **회부**.

★**덧붙임(메모에도 적었다).** `_T292` 가 `daegeum` 칸에서 본 결함은 참이지만, 그 뒤로 이 열은
**기계 출력과 손 수정이 섞인 표**가 됐다 — 기계는 `"샘플"`·`"합성"` **두 낱말만** 적는데
지금 `gayageum`·`daegeum` 칸엔 긴 문장이 있다. 섞인 표는 스스로 자가 못 된다.

## §1 ① 개체 표 넷 — 빌린 값과 출처 값뿐

### 새 수 0 을 **코드가 증명한다**

숫자를 표에 옮겨 적지 않았다. 세 자리가 다 **읽는다**:

```js
// chunk.js — HP 는 읽어서 넣는다(이 줄 자체가 "2·1 을 옮겨 적지 않았다"는 증명이다)
for (const _k of Object.keys(GROVE_KINDS)) RESOURCE_HP_TABLE[_k] = RESOURCE_HP_TABLE[GROVE_KINDS[_k].hpFrom];
// chunk.js — 주기도 자리를 읽는다(숫자 한 자 없다)
MUSHROOM_Y: () => _rgNum('T372_MUSHROOM_Y', REGROW.BUSH_Y()),
GREENS_Y:   () => _rgNum('T372_GREENS_Y',   REGROW.HERB_Y()),
// zone.js — 양도 한 칸을 읽는다
const gk = GROVE_KINDS[t];
if (gk)                 return { [gk.item]: BUSH_BERRY_N };
```

실측: `RESOURCE_HP_TABLE` 8종 → **12종**(`mushroom_patch` 2 · `greens_patch` 1 · `beehive` 1 · `wild_vine` 2) ·
`regrowStageOf` 가 넷 다 `gone → mature` 를 낸다(0.4년 `gone` / 1.1년 `mature`, 나물은 0.5년이 문턱).

### 재생 주기의 출처 — **넷 다 찼다. 빈칸 0.**

| 종 | 값 | 출처 | 결 |
|---|---|---|---|
| 버섯밭 | **1년**(덤불 자리) | 한국민족문화대백과사전 「송이버섯」 *"해마다 가을이면(19℃ 정도) 균사의 군데군데가 팽대하여 싹이 생기고"* · Wikipedia *Matsutake* *"the once-yearly harvest of mushrooms"* | 둘이 같은 말 |
| 나물 | **반 해**(풀 자리) | 나물은 **여러해살이 풀 그 자체**라 T122 가 이미 적은 자리(*"여러해살이 풀은 한 철이면 돌아온다"*). 한국임업진흥원 「고사리」 재배·관리 매뉴얼 *"3~4회/년 정도 수확"* · *"2년째 봄부터 … 10년 정도 수확이 가능함"* | 출처가 **더 빠르다** ⇒ 반 해는 느린 쪽 · 어긋나지 않는다 |
| 벌집 | **1년**(덤불 자리) | 지리산한봉 「꿀이야기」 *"토종꿀은 서리가 지난 뒤 10월 하순경 1년에 딱 한 번만 채취하기 때문에 수확량이 매우 적습니다"* · Raw Honey Guide *"Harvest happens only once per year, in autumn, and takes the entire comb"* | ⚠**한봉(재래 양봉) 기록**이다 — 야생 채취 주기를 바로 적은 문헌은 못 찾았다(한국민속대백과사전 「양봉」은 robots 로 이 자리에서 못 읽음) ⇒ **회부** |
| 야생포도 | **1년**(덤불 자리) | **집 안 정본** — `server/trees.json` 의 `grape` 가 `fy`(연간 열매 수율) 0.9 · `fs` 2(가을) | 같은 종을 두 자로 재지 않는다 |

★**HP 를 고른 근거도 출처다.** 벌집이 **풀 자리(1)** 인 것은 위 출처의 *"takes the entire comb"* 때문이다 —
한 번에 통째로 떼는 것이라 두 번 따는 물건이 아니다. 나물도 한 줌이라 풀 자리, 버섯밭·머루는 여러 번 따니 덤불 자리.

★**`fiber`·`twig` 는 안 따라온다.** 그건 *"덤불을 헤치면 마른 가지가 딸려 나온다"* 는 **덤불의 몸**이지
이 넷의 몸이 아니다(카드 — *"품목은 그 넷 그대로"*).

## §2 ② 스프라이트 넷 — `.py` 가 정본 · 새 형상은 **셋**

| 키 | 크기(배포·크롭) | 앵커 | 실치수 | 잠금(화소 해시) | 빌더 |
|---|---|---|---|---|---|
| `mushroom01` | 108×81 | (61.2, 62.5) | 0.84m | `afc48a54a0295b51` | `mushroom_patch` **새 빌더** |
| `greens01` | 126×102 | (74.0, 95.3) | 1.06m | `ed742d65e1a54d46` | `greens_patch`+`_fiddlehead` **새 빌더** |
| `hive01` | 65×86 | (37.1, 82.6) | 0.90m | `85faf8ef9df697b5` | `beehive` **새 빌더** |
| `vine01` | 246×191 | (130.2, 183.7) | 1.99m | `944aa31cc89b317b` | `tree_grape` — **새 빌더가 아니다** |

★**넷째는 새 부름이 아니다.** 야생포도는 이미 `tree_grape`(머루 *Vitis coignetiae* · 덩굴을 관목 문법으로 짓는
T129 빌더)가 있다 — 같은 종을 두 번 짓지 않는다. `PROP_BUILD` 에서 **작게** 부를 뿐이다(T156 열매 아이콘이 쓴
그 규약: *같은 부름 · 자리만 다르다*). 손잡이는 `fruit=None` 하나를 더했고 **안 주면 종전 그대로**다
(`autumn` 이 결정) ⇒ 나무층 `tree15`·`tree15_a` 는 **한 화소도 안 바뀐다**(잠금 506장 어긋남 0 이 그 증명이다).

★**1패스가 둘 틀렸고 실화면이 잡았다.**

* **벌집이 벌집이 아니었다.** 층 넷을 사이를 띄워 쌓았더니 **돌무더기**로 읽혔다(회색 기둥 위의 접시 셋).
  고친 것은 색이 아니라 **실루엣**이다 — ⓐ 층을 **겹쳐** 한 덩어리로, ⓑ 반지름을 **물방울꼴**로
  (`rr = 0.30h·√(1−t²)`), ⓒ 받침 가지를 **집 위로** 올려 매달린 것이 보이게. 1패스는 가지가 집 아래라
  '기둥 위에 얹힌 것' 이었다.
* **머루 송이가 안 보였다.** `autumn=True` 면 잎이 붉은 자주인데 송이는 청흑이라 **명도가 겹친다** —
  T129 2패스가 밤송이에서 이미 잰 그 함정이다(*"가을 노란 잎과 명도가 겹쳐 대조표에서 안 보였다"*).
  ⇒ 군락 판은 **푸른 잎 + 짙은 송이**로 간다(`autumn=False, fruit=True`).
* **나물 새순이 잎에 묻혔다.** 새순 높이를 잎과 같게 뒀더니 안 보였다 ⇒ 잎보다 높게(`1.18~1.52h`)·굵게.
  **안 보이는 표식은 표식이 아니다.**

★**땅속 0** — `render()` 의 정점 검사(T129 규약)를 넷 다 통과했다. 대를 세울 때 **첫 마디를 수직**으로 둔 것이
그 근거다(`tube` 는 첫 점의 고리를 진행 방향에 수직으로 놓으므로 밑동부터 기울이면 고리가 지면 아래로 내려간다).
★**납작한 머리는 `blob()` 으로 안 짓는다** — `blob` 은 **무작위로 회전**해서 눌린 축이 제멋대로 눕는다.
들꽃 꽃머리가 쓰는 규약(이코스피어 + `ob.scale` · 회전 0)을 그대로 썼다.

★**배치는 `--only` 로만.** `nature-postprocess.py --only=…` 네 키만 — 앵커 **더함 4 · 뺌 0 · 값 바뀐 키 0**
(T129 가 손잡이를 단 그 이유 그대로: 안 그러면 옛 산출물이 배포 자산을 조용히 갈아치운다).
`tree_species.json` **무변**(굽기가 그 표를 다시 쓰지만 바이트 동일을 확인했다).

## §3 ③ 앞절 — **정의만** · 세계 무변

`chunk.js` 에 `GROVE_KINDS` 표 하나(+`REGROW` 넷 · `RESOURCE_HP_TABLE` 파생) · `zone.js` `lootOfResource` 갈래 하나 ·
`itemlabel.js` 동사 넷. **생성 갈래는 한 줄도 안 지난다** — `groves` 자료에 이 `kind` 가 적히는 날에야 개체가 난다
(그 자료는 `plan-village-forage.js` 가 쓰고, 켜는 것은 T359 세션1이다 ⇒ **회부**).

**세계 무변의 자 — 청크 자원 전수 해시.** 존 26 × 청크 36 = **936 청크** · 개체 **7,746** ·
`id|type|hp|maxHp|x|y|r` 전수 sha1 = **`2a4997298345c319`**. **main 과 같다** — `e37f5ba6`(측정 베이스)·`28c722ff`(착지 베이스) 둘 다에서 쟀다.
(HEAD 판 세 파일을 그 자리에 놓고 다시 재고, 되돌린 뒤 sha1 일치까지 확인했다.)

## §4 접점 하네스 둘이 물었다 — **둘 다 자가 낡았던 것**

| 하네스 | 무엇이 물었나 | 판정 |
|---|---|---|
| `test-itemlabel ⑭` | 동사 표에 **없는 종류 넷**이 있다 | **자가 틀렸다.** ⑭ 는 종류 정본을 `chunk.js` 소스의 **리터럴을 정규식으로** 긁었다. 이 절 머리말이 적은 뜻은 *"스폰이 그 표로 hp 를 준다"* 인데, 스폰이 보는 건 리터럴이 아니라 **모듈이 내주는 표**다. T372 가 넷을 **파생으로** 넣자 두 자가 갈렸다(리터럴 8종 ↔ 실제 12종) ⇒ **살아 있는 표를 읽게 고쳤다** |
| `test-forage-act ⑩` | 덤불 전리품 리터럴 `{ berry: 2, … }` 이 없다 | 덤불 양이 **이름을 갖게 됐다**(`BUSH_BERRY_N`) — 군락 넷이 그 한 칸을 읽기 때문이다. 자를 **이름을 지나 수를 읽도록** 고쳤고(`BUSH_BERRY_N=2` 확인), *"넷이 그 같은 칸을 읽는다"* 는 줄을 **하나 더했다**(102→104) |

★**이빨 확인** — `beehive` 의 동사 한 줄을 빼면 ⑭ 가 **그 하나만** 문다(실측: `✗ … beehive` · 58 → 57/1).
고치고 되돌린 뒤 58/0.

## §5 회부

1. **벌집 재생 주기의 더 나은 앵커**(재민) — 지금 값 1년은 **한봉 기록**에서 왔다. 야생 벌집 채취 주기를
   바로 적은 문헌(한국민속대백과사전 「양봉」 등 · robots 로 이 자리에서 못 읽음)이 나오면 갈 칸.
2. **`render-meta.source` 를 기계로 다시 적기**(재민 · #64) — 뱅크가 있는 자리에서 `render_samples.py` 한 번.
   그게 `janggu_gung` 칸의 **유일한 자**다. T353 §0-ⓐ ③ 은 이 보고로 **정정**한다.
3. **생성 갈래에 넷 넣기**(세션1 · T359 뒤) — 표는 섰다. `groves` 밀도 유도에 넷이 드는 자리.
4. 종전대로 — `harvest` 직접 녹음(#34 · 재민) · #29 6장 · 실기표 26항 · AI Hub 71470 승인·다운로드 ·
   농현 속도 4.4Hz · 대륙사슴 CC BY-SA 채택 여부 · `test-resource-index.js:167` 벽시계 문턱(T301).

## §6 카드 전제 하나 — 수가 달랐다

카드 ★검증 규약은 `test-forage-act` **86** 이라 적었는데 **베이스에서 이미 103** 이었다(내 변경 전 실측).
T347 이 79 로 세운 뒤 자란 것이다 — 카드의 수가 낡았다. 지금은 **104**(위 §4 의 한 줄).
