# T171 — `codeOnly` 스무 사본 → 정본 하나(acorn) · 뒤처리 둘 (2026-09-11)

베이스 **`104dcc65`**(T183 · 리베이스 1회 · 충돌 `인계/회부.md` 하나 — 양쪽 보존) ·
헤드 **`87f173a4`** · 가지 `batch/code-only-0911` · **미푸시(3판 — 바깥길 닫힘 · 번들만)**
게임 코드 **주석 한 줄**(행동 0) · 서버 diff **0** · **자산 무변**(삭제 지시 하나는 재서 회부 — §2 ③) ·
새 손잡이 0 · 사본 **24 → 0** · 러너 전수 **143종 초록 124**.

---

## §0-ⓐ 스물넷 사본 — 스무 파일 · 판 넷

`grep -l codeOnly scripts/*.js` = **20 파일**, 그 안의 정의는 **24개**(`test-crops` 안에만 넷,
`test-itemlabel` 안에 둘). 구현이 네 갈래로 갈라져 있었다.

| 판 | 몇 | 어디 | 무엇을 먼저 지우나 |
|---|---|---|---|
| **A 줄필터(행 통째)** | 4 | `e2e-charsprite:250` · `e2e-npcsprite:294` · `test-itemlabel:89,104` | `^\s*(//|*|/*)` 로 **시작하는 줄만** 통째로 버린다 |
| **B 블록 먼저** | 12 | `e2e-friends:338` · `e2e-guild:275` · `test-body:71`(함수) · `test-calendar:21` · `test-carrier:28` · `test-emptystart:37` · `test-farm-metrics:19` · `test-inv-ground:27` · `test-kcal:25` · `test-tidal:32` · `test-trade:28` · `test-weight:29` | `/*…*/` → 그다음 `//…$` |
| **C 줄 먼저**(T100 5판·T163 수리본) | 5 | `test-crops:334,410,782,842` · `test-farm-life:26` | `//…$` → 그다음 `/*…*/` |
| **D 블록 먼저 + `http://` 가드** | 3 | `test-hunt-port:26` · `test-hunt-vis:24` · `test-regrow:26` | B 와 같되 `(^|[^:])//` |

**사본이 사본을 낳았다** — T100 5판이 C 를 만들자 T163 이 그 C 를 또 복사했고, D 는 B 를 복사해
`http://` 만 덧댔다. 네 판이 같은 파일에 서로 다른 답을 낸다.

## §0-ⓑ 각 판이 삼키는 양 — **남은 코드 글자**로 잰다

길이 비교는 판마다 뜻이 달라(지우기 vs 덮기) **공백이 아닌 글자 수**로 견줬다.

| 소스 | 정본(acorn) | A | B | C | D |
|---|---|---|---|---|---|
| **`server/villages.js`** | **181,489** | 197,086 (109%) | **90,779 (50%)** | 181,489 (100%) | **90,779 (50%)** |
| `server/zone.js` | 351,251 | 368,691 (105%) | 351,211 | 351,211 | 351,251 |
| `server/body.js` | 15,038 | 16,065 (107%) | 15,038 | 15,038 | 15,038 |
| `public/client/42-r2-char.js` | 10,400 | 11,261 (108%) | 10,400 | 10,400 | 10,400 |
| (나머지 여덟) | — | 101~107% | 100% | 100% | 100% |

**셋이 갈린다.**

1. **B·D 열다섯 판은 `villages.js` 의 코드 글자 절반을 삼킨다.** 기전은 "68% 를 지운다"가 아니라
   **짝이 어긋나는 것**이다: 20번 줄의
   `//     econ/레이아웃 모듈 require도 init 안 lazy — 플래그 off면 sim/* 로드 자체가 없음.`
   이 `/*` 를 연 것으로 읽혀 **20 → 480 줄**(461줄)이 한 덩어리로 사라지고, 그 뒤로 모든 블록 주석의
   여닫이 짝이 뒤집혀 **코드와 주석이 역할을 바꾼다**. 그래서 절반이다.
2. **A 는 반대로 덜 지운다**(101~109%) — 코드 줄 뒤에 붙은 주석을 **그대로 남긴다**. "주석은 증거가
   아니다"라는 애초의 목적(T140 `test-body` 머리말)이 A 에서는 지켜지지 않는다.
3. **C 도 완전하지 않다** — `zone.js` 에서 정본보다 40글자를 더 지운다. 잃는 것은
   `` `http://localhost:${PORT}/x` `` 같은 **문자열 속의 `//`** 다(토큰 대조: `$`·`x`×3·`PORT`·`localhost`).
   D 의 가드는 그 한 경우만 막고 1번 문제는 그대로 안고 있다.

**터졌나?** 아직이다. `villages.js` 를 B·D 로 훑는 하네스는 둘(`test-trade ⑦` · `test-hunt-port ⑫`)인데,
그 둘이 세는 앵커(`villageTrade*` 게이트 3 · `_cellOfPx(` 7 · `Math.round` 되읽기 0)는 **삼켜진 구간
밖**에 있어 정본과 수가 같다. 즉 **폭탄은 장전돼 있었고 아직 안 터졌다** — 실제로 터졌던 자리는
T100 5판이 이미 C 로 막은 `test-crops ⑦ⓓ`·`test-farm-life ⑥ⓓ` 다.

## §0-ⓒ 정본(acorn)으로 덮었을 때 — 앵커는 제자리인가

`acorn.tokenizer` 의 `onComment` 로 주석 구간을 받아 **공백으로 덮는다**(줄바꿈 보존 · 정규식 0).

| 잰 것 | 값 |
|---|---|
| 길이 | `villages.js` 375,573 = 375,573 **불변** |
| 줄 수 | 6,489 = 6,489 **불변** |
| 앵커 `indexOf('function _pestAt')` | 원본 227,079 = 정본 227,079 **제자리** |
| 앵커 전수(9개 중) | 8 제자리 · 1 이동 — 그 하나는 `body.js` 의 `COVER_MAX` 로 **주석 안의 글자**였다(제거기가 제 일을 한 것) |

⇒ 덮기는 옛 판이 흔들던 오프셋까지 없앤다. `src.indexOf(...)` 로 찾은 자리를 그대로
`src.slice(i, …)` 에 쓰는 하네스(`test-hunt-port:48`)가 더는 안 흔들린다.

**해시뱅은 주석이 아니다** — acorn 은 `#!/usr/bin/env node` 를 주석으로 넘겨주는데, 덮으면
`scripts/farm-metrics.js` 첫 줄이 사라진다(`test-farm-metrics` 가 그 소스를 훑는다). 정본은
그 한 경우를 **예외로 둔다**(실측으로 찾았다 · 토큰 대조에서 `usr bin env node` 가 사라진 것).

---

## §2 작업

### ① `scripts/code-only.js` — 정본 하나

`scripts/client-src.js`·`fixture-clock.js` 와 같은 자리·같은 문법. 구현은 열 줄이고 **정규식 0**이다.
파편(함수 본문 한 토막)이 들어와 토크나이저가 던지면 **그때까지 모인 주석만 덮고** 나머지는 원문 그대로
돌린다 — 조용히 전부 포기하지 않는다.

### ② 스물넷 → 정본 호출 · 전/후 표

각 정의 줄을 그 자리에서 `require('./code-only.js')` 로 바꿨다(이름·스코프 보존 · 판정 로직 무변).

| 하네스 | 전 | 후 | 판정 |
|---|---|---|---|
| e2e-friends · e2e-guild · e2e-npcsprite | 80/0 · 83/1 · 34/0 | 80/0 · 83/1 · 34/0 | 같다 |
| test-calendar · carrier · crops · emptystart | 13/0 · 54/0 · 170/0 · 41/0 | 같음 | 같다 |
| test-farm-life · farm-metrics · hunt-port · hunt-vis | 33/0 · 46/0 · 78/0 · 32/0 | 같음 | 같다 |
| test-inv-ground · itemlabel · kcal · regrow | 28/0 · 45/0 · 64/0 · 94/1 | 같음 | 같다 |
| test-tidal · trade · weight | 123/0 · 67/0 · 63/0 | 같음 | 같다 |
| **test-body** | 304/**2** | 306/**0** | ★다르다 — 사라진 둘은 `⑯㉧` **물때** 쌍이다(`인계/공통.md` 상시 빨강 목록 · `TIDE_FREEZE_MS` 로 초록). 내 변경과 무관 |
| **e2e-charsprite** | 6/1 | 96/0 | ★다르다 — **기계 의존**(같은 상시 목록). 같은 나무에서 6/1(부하) · 95/1(단독 대조) · 96/0 이 나온다 |

**17/20 이 한 수도 안 움직였다.** 움직인 셋은 전부 `공통.md` 가 이미 "상시 빨간 것"으로 적어 둔
물때·기계 의존이고, 셋 다 **HEAD 대조**로 귀속했다(T135 §3-b).

### ③ 부스러기 둘

* `public/client/40-r2-sprites.js:111` — "(Kenney Nature Kit, 초록 recolor)" 를 **사실대로** 고쳤다.
  그 나무는 배치19 에서 Blender 정본 씬으로 전부 다시 구웠고 배포본에 Kenney 파일은 0장이다(T168 §0-ⓐ).
  **주석만** — 그림도 행동도 0.
* `public/assets/char/probeall_walk.png` — **안 지웠다. 부스러기가 아니었다** ⚠

  카드는 "잠금표 밖 탐침 부스러기"라 적었고 T162 §0-ⓐ·T168 ⑦ 도 그렇게 읽었다(나도 그렇게 적었다).
  지우기 전에 **누가 쓰나 재 봤더니** 셋이 나왔다.

  | 잰 것 | 답 |
  |---|---|
  | 누가 읽나 | `scripts/test-charsheet.js:218` — **③ 가림 오차의 대조군**이다(몸+옷+도끼를 한 번에 구운 깊이 정본) |
  | 지우면 | ③ 이 `if (!fs.existsSync(pp)) ok(true, '(대조군 없음 — 측정 생략)')` 로 빠진다 — **assert 둘이 자명 통과로 바뀐다** |
  | 그 둘이 내던 수 | 합성 vs 깊이정본 어긋남 **5.31%** · 최악 d2 **6.6% < 12%**(실측 · 파일을 되돌려 다시 돌렸다) |
  | 왜 잠금표 밖인가 | 부스러기라서가 아니라 **굽기가 일부러 뺀다** — `char_render.py:1630,1647` 이 `probeall` 을 메타·잠금에서 제외한다(검사용 대조군이라서) |
  | 배포 비용 | **0** — `char_meta.json.sheets` 에 없으니 클라가 한 번도 청구하지 않는다(T162 §0-ⓐ 의 선적재 목록이 그 증거) |

  ⇒ 지우면 얻는 것은 레포 194 KB 이고 잃는 것은 **assert 둘**이다. 자명 통과 금지 규약과 정면으로
  부딪쳐 **손대지 않고 회부**한다(§5 ⑥). 지우기로 정하면 `char_render.py --probe` 로 언제든 되굽는다.

---

## §3 검증 — `test-harness-lint` ⑦절 (21/1 → **31/0**)

| 못 박은 것 | 이 판의 수 |
|---|---|
| **⑦a `codeOnly` 정의는 정본 하나뿐** — 사본이 생기면 빨개진다 | 사본 0 |
| ⑦b **자명 통과 금지** — 같은 자로 사본 한 줄을 넣으면 잡는다 | 잡는다 |
| **⑦c `// … sim/* …` 가 블록을 열지 않는다**(T152 그 줄을 픽스처로) | 뒤 코드 살아 있다 |
| ⑦d 그 줄의 주석 자체는 지워진다 | ✓ |
| **⑦e 문자열 안의 `//` 를 안 지운다** | `http://localhost:3010/x` 보존 |
| **⑦f·g 오프셋 보존** | 375,573 = 375,573 · 6,489줄 |
| **⑦h 앵커 제자리** | 227,079 |
| **⑦i 대조군** — 옛 "블록 먼저" 판이면 코드 글자가 절반 아래 | 90,779 < 181,489 × 0.6 |

⚠**1차 실행에서 ⑦a 가 자기를 잡았다**(`test-harness-lint.js:279` — 미끼 줄을 통째로 적어 둔 탓).
T143 이 물렸던 자기-일치 함정이라, 미끼를 **조각을 이어 붙여** 만들도록 고쳤다.
작업 중엔 빨강 하나(`e2e-friends.js:185` 판정 자리 이모지)가 남아 30/1 이었는데, **리베이스한 새
베이스(T183)에서 그 줄이 이미 고쳐져 있어 31/0** 이다 — 내 접점 밖이었고 T174(세션7)가 닫았다.

접점: `codeOnly` · `acorn` · `onComment` · `code-only.js` · `client-src` · `probeall_walk` · `Kenney`.

**리베이스 뒤 재확인**(베이스 `104dcc65`): `test-harness-lint` **31/0** · `test-client-globals` **26/0**(카드 요구) ·
`test-charsheet` **192/0**(대조군 파일이 살아 있으니 ③ 도 그대로 잰다).

## §4 러너 전수 — **143종 · 초록 124 · 빨강 19**

이 카드가 하네스 스물을 만졌으므로 전수를 돌렸다(청크 · 병렬 0 · 하네스 사이 서버 정리).
빨강 열아홉을 **HEAD 대조**로 갈랐다(T135 §3-b).

| 갈래 | 몇 | 어느 것 | 근거 |
|---|---|---|---|
| **내 접점 안 · 전/후 같다** | 4 | `e2e-guild`(83/1 = 83/1) · `test-regrow`(94/1 = 94/1) · `test-body`(물때 ⑯㉧) · `e2e-charsprite`(기계) | §2 ② 전/후 표 |
| **내 접점 안 · 전보다 나아졌다** | 1 | `test-harness-lint` 21/1 → **30/1**(전수 당시) → 리베이스 뒤 **31/0** | 직접 실행 |
| **내 diff 밖** | 14 | `e2e-chronicle` · `e2e-events` · `e2e-fishing` · `e2e-hp` · `e2e-mountain` · `e2e-mtfuzz` · `e2e-mtocc` · `e2e-preserve` · `e2e-rtt` · `e2e-thirst` · `e2e-weight` · `test-events` · `test-route-persist` · `test-tick-slicer` | 아래 셋으로 확인 |

**"내 diff 밖"을 말로 안 하고 확인한 셋**
1. 열넷 중 **내 diff 에 든 파일 0개**(`git diff --name-only` 대조).
2. 열넷 중 **`code-only.js` 를 부르는 것 0개**(`grep -l code-only`).
3. 부스러기 둘의 접점: `probeall_walk` 를 읽는 하네스는 `test-charsheet` 하나뿐이고 **초록**,
   `Kenney` 낱말은 주석에만 있다(코드 0).

⚠`인계/공통.md` 의 "상시 빨간 것"에 이미 적힌 것: `test-tick-slicer` · `e2e-charsprite` · `e2e-rtt`
(기계 의존) · `test-body ⑯㉧`(물때). 나머지 열하나는 **이 카드가 귀속을 못 한 빨강**이다 —
2코어 상자에서 전수를 돌리면 부하로 빨개지는 것들이 섞인다(러너 머리말이 적어 둔 EADDRINUSE·포트 함정).
지어내지 않는다: **못 쟀으면 못 쟀다**. 야간 러너가 main 에서 같은 표를 매일 내므로 거기서 갈린다.

## §5 회부

1. 알림 수명 `ms`(재민 캐논) · 2. 크레딧 화면(재민) · 3. BVH 원본·출처 미상 둘(판정대기 #14) ·
4. 옛 손 포즈 키 정리(#10 뒤) · 5. `e2e-friends.js:185` 판정 자리 이모지(T174 자리).
6. **`probeall_walk.png` — 지울까 둘까(재민).** 카드는 삭제를 지시했지만 재 보니 부스러기가 아니라
   `test-charsheet ③` 의 **대조군**이었다(§2 ③ 표). 지우면 assert 둘이 자명 통과가 되고, 두는 비용은
   레포 194 KB 뿐이다(배포 트래픽 0). 그래서 **손대지 않았다** — 이 한 줄이 이 카드가 지시를 안 따른 유일한 자리다.
7. **전수 빨강 열하나 미귀속** — §4. 2코어 부하와 섞인다. 야간 러너 표와 대조가 필요하다.
