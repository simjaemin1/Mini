# 보고 — T344: 야간 09-20·09-21 상시 밖 빨강 전수 귀속 · 기동 증인을 "아이의 입"으로 · 연속 칸·주기 기계 확인 (2026-09-21)

**가지 `batch/night-attr-0921` → main ff · 제품 0 · 하네스 15 + 정본 1 + 린트 1 · force 0**

1. **★`e2e-onboarding` 의 뿌리를 댔다 — `c85eab41`[T326] 의 `_mapBeds` 기본 켬이다.** 이분 넷으로 좁혔다: `d88acbec^`(T319 직전) **75/0** · `d88acbec`(T319) **75/0** ⇒ **야간이 지목한 T319 는 무죄** · `c85eab41^` **75/0** · `c85eab41`(T326) **65/6 빨강**(오늘 main 과 같은 사슬). 손잡이를 갈라 확인: `T315_HOUSE_GAP=0` 만 → **71/3 여전히 빨강**(집 간격 15 는 무죄) · `T315_MAPBEDS=0` 만 → **75/0 초록**. **되돌림 한 값이 그대로 듣는다.** 제품 판단이라 **회부**(§1).
2. **★`test-terraincache` 는 자 결함이었다 — 3초 · 결정적 · 부하 무관.** ④ 두 줄이 `TERRAIN_TILE_CACHE === '1'`(옛 계약)을 물고 있었는데 `9b61d49f`[T324 · 09-19]가 **기본을 켬으로 돌렸다**(`!== '0'` · 되돌림 `=0`). 계약을 옮기고 **자명 통과 금지 한 줄**(옛 글자가 남아 있지 않다)을 더했다 — **22/0 두 판**.
3. **★기동 증인을 바꿨다 — 포트가 답하는 것은 "내 서버가 떴다"의 증인이 아니다.** 산·지형 계열 열셋이 central 을 띄우고 `sleep(2500)` 로 기다렸다. 그런데 `waitHttp(/zones)` 로 바꾸는 것은 **더 깊은 자명 통과**다. 쟀다: 앞 판 central 이 포트를 쥔 채면 새 central 은 **`EADDRINUSE` 로 즉시 죽고**(종료코드 1 · `boot()` 은 stderr 를 안 본다), `waitHttp` 는 **앞 판의 central** 에게 200 을 받아 "기동 성공"이라 답한다. 앞 판이 내려가면 `page.goto` → `ERR_CONNECTION_REFUSED` — **한가한 상자에서 두 번 재현**했다.
4. **⇒ 정본 하나(`scripts/fixture-boot.js` · `waitUp`)** — 증인은 아이가 제 `listen` 콜백에서 찍는 `central server up on` 줄이다(남의 서버로는 만들 수 없다). 아이가 죽으면 **EADDRINUSE 를 이름 붙여** 돌려준다. 효과: 겹쳐 띄운 판이 **50초 뒤 알 수 없는 죽음 → 0초 만에 "포트를 이미 누가 쥐고 있다"**. 열셋 전부 **단독 초록 재확인**(§0-ⓒ 표).
5. **린트 ⑩ 신설(표 · 판정 아님)** — `central 기동 증인 — 아이의 입 13 · 포트 응답 35 · 맹목 잠 2`(`e2e-bigmap-live`·`test-guest-rejoin`). 서른다섯은 **다음 카드 몫**이다(전수를 막으면 쓸 자가 없다 — 예산형 표와 같은 자리). `test-harness-lint` **47/0**.
6. **귀속 전수(넘긴 셋 빼고 열)** — 부하 거짓 빨강 **6** · 실빨강 **2** · 자 결함 **1** · 기전만 **1**. 표는 §0-ⓐ.
7. **`e2e-village` — 기전은 댔고 재현은 못 했다.** 단독 두 판 **초록**(165·139초)인데 야간은 단독 재실행도 빨강이었다. 그 하네스도 `waitHttp(/zones)` 족(3010 공유)이라 3의 자명 통과에 노출돼 있다 — 점유 상태에서 돌리니 **`✓ central 기동` 이 그대로 찍혔다**. 다만 village 의 그 빨강 줄까지 재현하진 못했다.
8. **`e2e-events`·`e2e-winter` — 단독 76~82초.** 야간은 **RC=124(40분 끊김)**. 30배다 — **느린 게 아니라 막혔다.** 그 자리는 **못 쟀다**(§1 회부).
9. **② 연속 n밤 칸 — 찍혔다. 기계를 고칠 자리 없다.** 09-21 표에 `연속 1밤` 넷 + `연속 **2밤** (상시 밖)` 하나(`test-site-memo`)와 ⚠줄까지 났다. `test-nightly` **41/0**.
10. **② 묶음 주기 — 맞는다.** 기계에게 물었다: 09-19 **B** · 09-20 **C** · 09-21 **A** · 09-22 **B** · 09-23 **C**. 보고 머리의 "묶음 A" 와 같다(주기 3 · 나흘째 되돌아옴은 `test-nightly` ⑦ 이 이미 문다).
11. **`nightly-known.txt` ±0.** 규약(두 밤 연속 **실**빨강만) 적용: 09-20 ∩ 09-21 상시 밖 교집합은 `test-site-memo` **하나**뿐이고 그것은 **PM 이 세션4 T342 로 넘긴 것**이다 — 올리면 내일 보고에서 사라져 그 카드가 눈을 잃는다. **안 올렸다.**
12. **카드 수 정정 한 줄** — 카드는 "09-21 5건"이라 했으나 보고의 상시 밖은 **4건**이다(다섯째 `test-body` 는 **상시**(물때)). 내가 잰 것은 넷이다.
13. **제품 0.** 바뀐 것은 `scripts/` 열다섯 + 정본 하나 + 린트 하나. 되돌리려면 이 가지를 빼면 된다.
14. **내가 틀릴 뻔한 것** — 3에서 처음엔 `waitHttp(/zones)` 로 고쳤고 **초록이 났다**. 한 번 더 겹쳐 돌려 보고서야 그게 **고침이 아니라 더 나은 은폐**임을 알았다(§2).
15. 열린 회부 다섯 — §1.

---

## §0 잰 것

### §0-ⓐ 귀속 전수 표 (단독 두 판 · 한가한 상자 · 러너 미호출)

| 하네스 | 밤 | 야간 | 내 단독 두 판 | 판정 | 귀속 |
|---|---|---|---|---|---|
| `test-terraincache` | 09-21 | 빨강 · 단독도 빨강 | **22/0 · 22/0**(고친 뒤) | **자 결함** | **`9b61d49f`[T324]** — 기본을 켬으로 돌렸는데 자가 옛 글자를 물었다. **닫았다** |
| `e2e-onboarding` | 09-21 | 빨강 · 단독도 빨강 | **66/5 빨강**(415초) → `T315_MAPBEDS=0` 에서 **75/0** | **실빨강(제품)** | **`c85eab41`[T326] `_mapBeds` 기본 켬** — 이분 넷으로 확정. **회부** |
| `e2e-cold` | 09-20 | 빨강 · 단독 초록 | **56/0 · 56/0**(T338) | 부하 거짓 빨강 | **`51959d9e`[T331]** 이 닫은 자리 · 자는 T338 이 고쳤다 |
| `e2e-events` | 09-20 | RC=124(40분) · 단독 초록 | **60/0 · 60/0**(77·76초) | 부하 거짓 빨강 | 하네스 무접촉(`6a3905c`[T238]) · **막힌 자리는 못 쟀다** |
| `e2e-winter` | 09-20 | RC=124(40분) · 단독 초록 | **29/0 · 29/0**(80·82초) | 부하 거짓 빨강 | 위와 같다 |
| `e2e-move` | 09-20 | 빨강 · 단독 초록 | **37/0 · 37/0**(82·84초) | 부하 거짓 빨강 | 하네스 무접촉 · 기전은 §0-ⓑ |
| `e2e-mtfoot` | 09-20 | 빨강(0장/0장) · 단독 초록 | **12/0 · 12/0**(84·84초) | 부하 거짓 빨강 | **기동 기전 확정(§0-ⓑ)** · 자를 고쳤다 |
| `e2e-mtocc` | 09-20 | 빨강(가린 산 0장) · 단독 초록 | **18/0 · 18/0**(101·102초) | 부하 거짓 빨강 | 위와 같다 |
| `e2e-village` | 09-20 | 빨강 · **단독도 빨강** | **초록 · 초록**(165·139초) | 부하/환경 | **기전만 댔다**(§0-ⓑ) · 그 줄 재현 실패 |
| `test-route-persist`·`test-shelter`·`test-site-memo` | — | — | — | — | **PM 이 넘겼다**(세션3 T340② · 세션4 T342) — 안 건드렸다 |

⚠`e2e-village` 의 "단독 재실행도 빨강"은 **한가한 상자의 단독과 다른 것**이다 — 러너의 재실행은 121종이 지나간 상자에서 돈다. 이 구분을 표에 남긴다.

### §0-ⓑ 기동 증인 — 포트는 증인이 아니다

재현(한가한 상자 · 러너 미호출):

```
1) e2e-mtocc 바로 뒤에 e2e-mtfoot     → page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3010/  (50초 뒤)
2) 같은 일을 한 번 더                  → 같은 죽음 (4초)
3) 직접 확인: 3010 을 쥔 채 central 하나 더 → 종료코드 1 · Error: listen EADDRINUSE 0.0.0.0:3010
4) 그 상태에서 waitHttp(/zones)        → 200 (앞 판의 central 이 답한다)
5) e2e-village 를 점유 상태로 돌림      → 로그에 `✓ central 기동` 이 **그대로 찍힌다**
```

central 이 `/zones` 에 답하기까지: **112·119·127ms**(한가) · **193·240·265ms**(3코어 부하). 맹목 예산 2500ms 는 평소 20배 여유이고, **앞 하네스가 안 내려간 순간에만** 무너진다 — 야간은 그 순간이 기본값이다(121종 연속).

⇒ 정본 `scripts/fixture-boot.js`:
* 증인 = 아이가 제 `listen` 콜백에서 찍는 `[central] 🏛️ central server up on :PORT` — **그 프로세스가 그 포트를 쥐었다**는 뜻이고 남의 서버로는 못 만든다.
* 아이가 죽으면 그 자리에서 이름을 붙인다 — `★central 이 뜨지도 못하고 죽었다 — 종료코드 1 · **포트를 이미 누가 쥐고 있다(EADDRINUSE)**`.
* 상한(120초)은 **표에만** — 시간을 재서 판정하지 않는다(족보 ⑩).

### §0-ⓒ 고친 열셋 — 단독 재확인 (전부 초록)

`e2e-mtfoot` 12/0(84초) · `e2e-mtocc` 18/0(101초) · `e2e-mtcut` 26/0(162초) · `e2e-fogray` 12/0(88초) · `e2e-rooms` 66/0(125초) · `e2e-mtcorridor` 15/0(112초) · `e2e-tilestate` 39/0(245초) · `e2e-waterperf` 16/0(186초) · `e2e-cutaway` 15/0(266초) · `e2e-mountain` 51/0(213초) · `e2e-nature` 145/0(479초) · `e2e-terrain` 33/0(218초) · `e2e-mtfuzz`(T338 11/11 두 판).

### §0-ⓓ ② 기계 확인 둘

```
묶음 주기 —  NIGHT_DATE=2026-09-19 → B · 09-20 → C · 09-21 → A · 09-22 → B · 09-23 → C
             09-21 보고 머리 "오늘 묶음 A" 와 같다.  test-nightly ⑦ 41/0 (사흘 합 50/50 · 주기 3)
연속 n밤 —   09-21 표: `연속 1밤` ×4 · `연속 **2밤** (상시 밖)` ×1(test-site-memo)
             ⚠줄까지 났다: "연속 2밤 이상인데 상시 목록에 없는 것 1건"
             ⇒ nightly-report.js 를 고칠 자리 **없다**.
```

## §1 안 한 것 · 회부

* ★**`e2e-onboarding` — `_mapBeds` 기본 켬(T326)을 어떻게 할지는 제품 판단이다(PM).** 갈래 셋: ⓐ `T315_MAPBEDS=0` 으로 되돌린다(한 값 · 즉시 초록) · ⓑ 켠 채로 온보딩 대본을 새 마을 배치에 맞춘다(빈터가 "마을 중심에서 너무 멀다") · ⓒ 켠 채로 두고 이 절만 유예. **나는 제품을 안 만졌다.**
* **`waitHttp(/zones)` 로 기다리는 하네스 35** — 같은 자명 통과에 노출돼 있다. 린트 ⑩ 이 수를 센다. 정본으로 옮기는 카드 하나면 된다(기계적).
* **맹목 잠 둘** — `e2e-bigmap-live`(`boot` 꼴이 다르다) · `test-guest-rejoin`.
* **`e2e-events`·`e2e-winter` 의 RC=124** — 단독 76~82초인데 야간 40분에 잘렸다. **막힌 자리를 못 쟀다**(기동 자명 통과가 후보다 — `waitHttp` 의 상한은 600초이므로 그것만으로 2400초를 설명하지 못한다).
* **`e2e-village` 의 그 줄** — 기전은 댔으나 재현 실패.
* 종전 회부 그대로: `e2e-rumor` 경주(Z) · 굶긴 상자 장착 층(R2) · 고아 감시 2초를 틱 간격에서 유도할지(PM).

## §2 내가 틀린 것

* **`waitHttp(/zones)` 로 한 번 고쳤고, 초록이 나서 하마터면 그대로 보고할 뻔했다.** 겹쳐 돌려 보고서야 그것이 **고침이 아니라 더 나은 은폐**임을 알았다(앞 판의 서버에게 200 을 받는다). 첫 고침의 주석까지 커밋 직전에 갈아 끼웠다 — 그 경위를 §0-ⓑ 에 그대로 남긴다.
* **야간의 "귀속 후보 T319"를 그대로 믿을 뻔했다.** 두 판(`d88acbec^` 75/0 · `d88acbec` 75/0)이 **T319 를 무죄로** 만들었다. 후보는 판정이 아니다.
* `while pgrep -f "e2e-mtfoo[t]"` 로 기다리다 **자기 명령줄을 물어** 10분을 날렸다(족보의 `pgrep -f` 함정이 또 다른 꼴로 왔다). 이후로는 순차 실행만 썼다.

## §5 푸시 — **못 밀었다(맥이 안 잡힌다)**

* 가지 `batch/night-attr-0921` = **`8a02b1e6`**(하네스 15 + 정본 1 + 린트 1) + **`b4cf4745`**(보고) — 베이스 `af0bc9a3`.
* **main ff 를 못 했다.** 푸시 2판의 첫 칸(맥 VM)이 안 잡힌다 — `device_*` 가 전부 "device not connected". PAT 는 맥에만 있으므로 컨테이너에서는 밀 수 없고, 403 대체 길인 `~/Mini/_inbox/` 도 **같은 다리**라 못 쓴다. 12분 동안 네 번 다시 걸었다.
* ⇒ 번들을 **채팅으로 냈다**: `T344_2026-09-21.bundle`(24,699바이트 · `af0bc9a3..batch/night-attr-0921`). 맥이 돌아오면 한 줄이다:

```bash
cd ~/pushrepo && git fetch ~/Mini/T344_2026-09-21.bundle 'HEAD:refs/heads/batch/night-attr-0921'
git fetch --depth 30 origin main && git merge-base --is-ancestor origin/main batch/night-attr-0921 \
  && git push "https://x-access-token:$(cat ~/mnt/Mini/durango-mini/.git/claude-pat.txt)@github.com/simjaemin1/Mini.git" \
     'batch/night-attr-0921:main'
```

* **force 0 · `.lock` 0.** 베이스 `af0bc9a3` 는 이 글을 쓰는 시점의 `origin/main` 이다 — 그 사이 main 이 움직였으면 위 `merge-base` 가 **먼저 막는다**(ff 가 아니면 안 민다).
