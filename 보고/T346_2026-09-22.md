# T346 — 카드의 전제 셋을 정정한다: ① `마을 중심에서 너무 멀다` 는 **통과하는 검사가 스스로 기대한 거절문**이고, 6 빨강은 제품도 대본도 아니라 **T342 가 이미 닫은 거부-캐시 결함의 하류 증상**이었다(이분: pre-T342 66/5 → T342 **74/0**) ② 빈터를 주는 함수는 집 간격을 **보지 않는다**(도착 지점 반경 320px 고정 — 집 55채가 움직일 수 없는 자다) ③ **판정 수는 게이트가 아니다**(같은 오늘 main 이 74/0 과 75/0 을 다 낸다 — 흔드는 갈래 둘을 대본이 스스로 찍게 했다) ④ `NPC_CAP_PER_VILLAGE = 40` 몸 수 상한 표(10마을 세계의 1착륙 보수값 · 주석 없음 · 지금 61.4%)

**[승인 대기 · T346]** 제품 **무접촉**(`server/` 0줄) · 하네스 **한 자리**(`scripts/e2e-onboarding.js` 판정 수 가독 한 줄 + 주석) ·
새 수 **0** · 새 손잡이 **0** · 사본 **0** · `sim/`·`lab/`·`public/`·`설계/` 무접촉 ·
T345(세션3)·T347(세션1) 무접촉 · 실기 0 · `_mapBeds` 켬 **유지**(PM · ⓑ).

**한 줄 답 — 6 빨강은 빈터 거리가 아니었다. 다섯 줄 전부가 **첫 납품 한 번**에 매달린 도미노이고,
그 납품을 막던 세계(집 438 · 정지 3 · 잉여에 먹을 것 없음)를 **T342 가 이미 고쳤다**.
오늘 main 은 `e2e-onboarding` **FAIL 0** 이다 — 두 판(74/0 · 75/0)에서. 고칠 제품도 대본도 없었다.**

**★전제 정정 셋.**
① *"새 플레이어 마을 빈터가 중심에서 너무 멀다"* — **그 문구는 실패한 검사가 낸 게 아니다.**
   `마을 중심에서 너무 멀다`(= `_villageNear` · `EV_BRIEF_PX` 260px)는 **`구역 밖은 종전 규칙 그대로
   거절된다` 가 스스로 기대하는 거절문**이고, 그 검사는 두 팔 **다 통과**한다(§1-ⓒ). 빨강 다섯 줄은
   이 문구를 한 번도 찍지 않았다.
② *"켬 세계에선 집이 55채 늘어 빈터가 멀어진 게 맞나"* — **아니다. 구조적으로 못 멀어진다.**
   빈터를 주는 자는 `Onboarding.vacantZoneOf`(`onboarding.js:850`) 이고, 중심은 **도착 지점**
   (`arrivalOf(vid)`), 반경은 **`CFG.VACANT_R` 10셀 = 320px 고정**이다 — `_lifeSiteFilters` 도
   `LIFE_HOUSE_GAP` 도 **부르지 않는다**. 두 팔의 기하는 픽셀까지 같았다(§1-ⓑ).
③ *"75/0"* — **판정 수는 게이트가 아니다.** 오늘 main 이 74/0(기본)과 75/0(`T315_MAPBEDS=0`)을 다 낸다.
   차이는 **잃은 검사가 아니라 갈래 하나**다(촌장에게 걸린 첫 의뢰가 있었나 → 판정 2개 vs 1개).
   §1-ⓓ 가 이름표까지 대조했다. ⇒ 불변량은 **FAIL 0** 하나이고, 대본이 이제 그걸 스스로 찍는다.

---

## 1. ① 이분 — 어디서 빨강이 죽었나

### ⓐ 이분 표 (`node scripts/e2e-onboarding.js` · 각 팔 1판 · 실서버 + 실브라우저)

| 팔 | 커밋 | `_terrGrow → lifeSiteReset` | 결과 | 판정 수 |
|---|---|---|---|---|
| T344 기록 (T326 착지) | `c85eab41` | 없음 | 65/6 · 66/5 (두 판) | 71 |
| T344 기록 · `T315_MAPBEDS=0` | `c85eab41` | 없음 | 75/0 | 75 |
| **pre-T342**(T344 커밋) | `8a02b1e6` | **0** (grep 0) | **66 / 5** | 71 |
| **T342**(main 착지) | `76bf2648` | **1** (grep 1) | **74 / 0** | 74 |
| 오늘 main | `98a27edf` | 1 | **74 / 0** | 74 |
| 오늘 main · `T315_MAPBEDS=0` | `98a27edf` | 1 | **75 / 0** | 75 |

★두 팔은 `git worktree` 로 **같은 디스크·같은 `node_modules`** 에서 돌렸다(`/tmp/pre342` · `/tmp/at342`).
★그리고 T344 의 두 판이 **같은 커밋에서 65/6 · 66/5 로 갈렸다** — 빨강 수 자체가 세계 상태의 함수다.

### ⓑ 실패 다섯 줄이 **무엇을** 기대하나 — 전부 **첫 납품 한 번**의 하류다

| # | 줄 | 기대 | 왜 죽었나 |
|---|---|---|---|
| 1 | `누적 기여 0/3`(`e2e:435`) | `__onbState.contrib ≥ need`(3) | **첫 도미노.** 8라운드 돌았는데 한 번도 성립 안 함 |
| 2 | `★훅 대사가 왔다`(`:436`) | 알림에 `빈터 하나 내어줌세\|밥이라도` | 훅은 **첫 납품 한 번**만 나온다 |
| 3 | `★[T62] 촌장이 쉼터를 말한다` | `window.__onbGreet.lines` 에 `쉼터` | 그 읽기가 **납품 루프 안**에 있다(`:426` 주석) |
| 4 | `빈터 권리가 섰다 (52592,12304) r=320` | `vacantZoneOf` 가 섰다 | 권리는 `Onboarding.onDeliver` 가 `LOT_AFTER=3` 에서 준다 |
| 5 | `★마을 어귀 빈터에 내 땅을 걸었다` | `claims 0→1` | 권리 없으니 **폴백 규칙**(`개인 사유지는 길드 영토 안에만`)이 받는다 |

**다섯 줄 중 거리를 재는 줄은 하나도 없다.** 넷은 1번의 하류이고, 1번은 게시판이 낸 의뢰의 함수다.

### ⓒ 두 팔의 기하는 **픽셀까지 같았다** — 집 55채가 아무것도 안 움직였다

| 잰 것 | pre-T342 | T342 |
|---|---|---|
| 추천 마을 | `vid=2`(어촌2) | `vid=2`(어촌2) |
| 도착 지점 | `Δ0px · dock` | `Δ0px · dock` |
| 중심까지 | **288px** | **288px** |
| 쉼터 자리 | **(1628,370)** | **(1628,370)** |
| 빈터 구역 | **(52592,12304) r=320** | **(52592,12304) r=320** |
| 도착 허기 | 56.0 | 56.0 |
| `마을 중심에서 너무 멀다` | **있다**(통과 검사의 기대 거절문) | **있다**(같음) |

⇒ 빈터 반경도 중심도 안 움직였다. `VACANT_R`(=10셀·320px)은 `_num('ONB_VACANT_R', 10)` 이고
중심은 `arrivalOf(vid)` 다 — **집·영토·간격을 보는 항이 식에 없다**(`onboarding.js:850~860`).

### ⓓ 74 vs 75 — 잃은 검사가 아니라 **갈래**다 (이름표 대조)

판정 이름표를 뽑아 `diff` 했다. 오늘 main 74/0 ↔ 오늘 main 75/0 의 차이는 **한 자리, 두 줄**뿐이다:

```
< 급한 일이 없으면 쉼터를 권한다(빈 의뢰를 지어내지 않는다)        ← 의뢰가 없던 갈래 (판정 1)
---
> 촌장이 첫 의뢰를 제안했다                                       ← 의뢰가 있던 갈래 (판정 2)
> 안내는 대사 속 방향뿐이다(마커 0)
```

이 갈래는 **대본이 이미 알고 쓴 것**이다(`e2e:360` 주석 *"첫 의뢰는 게시판이 실제로 낸 것이라
마을 사정에 따라 없을 수 있다"*). pre-T342 ↔ T342 의 71 → 74 도 같은 종류다(갈래 ⓑ: 첫 납품이
성립하면 `납품이 화면 알림으로 돌아온다` · `★"곳간에 쌓였다"` 두 판정이 **더** 붙는다 — `:428~429`).

⇒ **제품 0줄 · 대본 한 자리.** 절대 수로 회귀를 읽으면 이 갈래를 결함으로 읽는다. 그래서 대본이
   판정 수와 흔드는 갈래 둘을 **스스로 찍게** 했다(새 판정 0 · 느슨해진 게이트 0):

```
── 판정 74개(갈래 ⓐ 의뢰 유무 · ⓑ 첫 납품 성립에 따라 사람마다 ±1) — 게이트는 FAIL 0 하나다 ──
=== PASS 74 / FAIL 0 ===
```

### ⓔ 제품인가 대본인가 — **둘 다 아니다**

* **제품 아니다.** 새 플레이어는 집(빈터 내 땅)을 **받는다** — 오늘 main 두 판 모두 `claims 0→1` ·
  `빈터 권리가 섰다 ✓` · `HUD "오늘: 납품 1 · 빈터 권리 얻음"`. 빈터 탐색을 정본으로 옮길 일이 없다
  (옮길 대상이 애초에 간격을 안 본다 — ⓒ).
* **대본 아니다.** 오늘 main 에서 빨강 0 이다. 게이트를 느슨하게 할 이유가 없다.
* **주인은 T342 다.** pre-T342 세계는 집 438 · **즉시 정지 3곳** · 어촌2 가 그 셋 중 하나였고
  (T335 §·T342 §①), 그 세계의 게시판이 낸 의뢰(`나무 3 → 돌 20` · 보상에 **먹을 것 없음**)에서
  첫 납품이 8라운드 내내 성립하지 못했다. T342 뒤 같은 마을이 `돌 6 → 곡식 7452`(**밥 성립**)를 내고
  3/3 이 한 번에 찬다. ⇒ **온보딩 빨강은 거부 캐시 결함의 하류 증상**이었다.

### ⓕ 남는 취약점 — **회부**(고치지 않았다)

`e2e:435` 의 `contrib >= need` 는 **의뢰가 채울 수 있는 것이라고 가정**한다. 그런데 같은 대본이
`:360` 에서는 *"의뢰가 없을 수 있다"* 를 갈래로 인정한다 — **§④ 는 관용, §⑤ 는 무관용**이다.
세계가 나쁘면(정지 마을 · 잉여에 먹을 것 없음) 이 한 줄이 **온보딩 결함처럼 보이는 빨강**을 낸다.
고르는 길은 둘이고 **PM 칸**이다: ⓐ 하네스가 채울 수 있는 의뢰를 **보장**한다(계측 · 제품 0) —
그러면 검사가 세계의 사정과 무관해지지만 "나쁜 세계"를 못 잡는다 · ⓑ 지금처럼 둔다 —
빨강이 **세계의 신호**로 남지만 온보딩 결함으로 오독된다(이번 카드가 그 사례다). 안 건드렸다.

---

## 2. ② `NPC_CAP_PER_VILLAGE = 40` — 어디서·누가·왜 · 캐논 대비 · 풀면 무엇이

### ⓐ 출처 — 주석이 **없는** 한 줄

```js
server/villages.js:82  const VILLAGE_MAX = …'20'…          // ★기본 20(밀도 캐논 §2.4·§3b … 10은 1착륙 보수값이었음)
server/villages.js:83  const NPC_CAP_PER_VILLAGE = Math.max(1, parseInt(process.env.VILLAGE_NPC_CAP || '40', 10));
server/villages.js:84  const POP_SYNC_PER_DAY = 2;          // 인구 반영은 완만: 게임일당 마을당 ±2명까지
```

* **누가** — `git log -L 83,83:server/villages.js` → **`99386737`**(2026-07-07)
  *"본체 이식 §4-4 Stage1~3: NPC 마을 경제 착륙 — villages/village_buildings 테이블,
  hanbando 하드코딩 50→**선별 10 시딩** … NPC=기존 spawnNpc 재사용"*. 생활층 **첫 커밋**이다.
* **왜** — 그 커밋의 세계는 **마을 10곳**이었다. 40 × 10 = **몸 400개**가 천장이었다.
  이웃 두 줄은 근거 주석을 달고 있는데 **이 줄만 없다** — 성능 보수값이 설명 없이 남은 자리다.
* **한 번도 재검토되지 않았다.** 시딩이 10 → 50 으로 가면서 천장은 400 → **2,000** 이 됐고
  `VILLAGE_MAX` 는 주석까지 고쳐 20 으로 올랐는데(밀도 캐논) **83행은 그대로 40** 이다.
* 쓰는 자: `syncVillagePop`(`:760`) — `target = min(econ.npcs.length, NPC_CAP_PER_VILLAGE)`.
  부르는 자 둘: `:2799` 부팅(`Infinity`) · `:3695` 하루(`POP_SYNC_PER_DAY` = ±2/마을/일).

### ⓑ 캐논 대비 — 지금 몸 수 / econ 인구

캐논(`설계/설계_생산_실체.md:25`): *"**세계는 항상 실체다** — 관측자가 없어도 마을은 돈다
**[재민 확정 09-18]**"*, `:46`: *"ⓑ① 이면 51마을 **2,677명**이 매 틱 걷는다"* ⇒ **econ 인구 전부가 몸을 가진다.**
지금(T342 FIX1 팔 · 200일 · 50마을 · econ 2,725): **몸 1,672 / econ 2,725 = 61.4%** — **1,053명이 몸이 없다.**
넘는 마을 **21/50**, 최대 마을 econ **225**(몸 40 — 185명이 econ 에만 있다).

### ⓒ 풀면 무엇이 늘고 무엇이 깨지나 (T333 정본: 상수 **1.620 ms** · 한계 **4.79 µs/사람** · 낮 5.24 · 밤 3.78 · 예산 **33.2 ms**)

| 상한 | 몸 수 | econ 대비 | 넘는 마을 | 틱(하루평균) | 예산비 | 낮 | 밤 |
|---|---|---|---|---|---|---|---|
| **40**(지금) | 1,672 | **61.4%** | 21/50 | **9.63 ms** | 29% | 10.4 | 7.9 |
| 60 | 2,035 | 74.7% | 12/50 | 11.37 ms | 34% | 12.3 | 9.3 |
| 80 | 2,207 | 81.0% | 6/50 | 12.19 ms | 37% | 13.2 | 10.0 |
| 100 | 2,308 | 84.7% | 5/50 | 12.68 ms | 38% | 13.7 | 10.3 |
| 120 | 2,396 | 87.9% | 4/50 | 13.10 ms | 39% | 14.2 | 10.7 |
| 160 | 2,556 | 93.8% | 4/50 | 13.86 ms | 42% | 15.0 | 11.3 |
| 200 | 2,683 | 98.5% | 2/50 | 14.47 ms | 44% | 15.7 | 11.8 |
| **없음**(캐논) | **2,725** | **100.0%** | 0/50 | **14.67 ms** | **44%** | 15.9 | 11.9 |

**읽는 법 셋.**
1. **예산은 안 깨진다.** 캐논까지 풀어도 낮 **15.9 ms < 33.2 ms**(48%). 40 → 없음의 대가는 **+5.04 ms**다.
2. **싼 구간이 앞에 있다.** 40 → 80 이 몸 **+535**(61.4 → 81.0%)를 **+2.56 ms** 로 산다 — 뒤 절반
   (80 → 없음)은 몸 +518 에 **+2.48 ms** 로 거의 같은 값이니, 곡선이 아니라 **분포**가 싸게 만든다
   (넘는 마을이 21 → 6 으로 먼저 준다).
3. **깨질 수 있는 건 예산이 아니라 두 곳이다.** ⓐ T333 이 관측한 곡선은 **위로 휜다**(사람 +15% → 사람당 +3.6%)
   — 표의 한계값은 **하한**이다. ⓑ `POP_SYNC_PER_DAY = 2` 때문에 상한을 풀어도 **즉시 안 온다**:
   econ 225 마을이 40 → 225 로 차려면 **약 93 게임일**이다(하루 ±2). 즉 상한 해제는 **한 판의 값이 아니라
   3개월치 램프**다 — 재측정 없이 켜면 "왜 안 늘지"로 읽힌다.

**표만 낸다 — 안 풀었다**(카드 · T345 재작성 뒤 · 손잡이 `VILLAGE_NPC_CAP` 는 이미 있다).

---

## 3. ③ 한 줄 답

**빈터는 도착 지점 반경 320px 고정이라 집이 늘어도 안 멀어진다 — 6 빨강은 전부 **첫 납품 한 번**에
매달린 도미노였고, 그 납품을 막던 세계를 T342 가 이미 고쳤다(오늘 main **FAIL 0**). 고칠 제품도 대본도
없었고, 고친 것은 **절대 수를 게이트로 읽던 우리 눈**이다. 그리고 `NPC_CAP_PER_VILLAGE = 40` 은
**마을 10곳 세계의 주석 없는 보수값**이라 지금 econ 인구의 **61.4%** 만 몸을 갖는다 — 캐논까지 풀어도
예산의 **48%**(15.9/33.2 ms)지만 `POP_SYNC_PER_DAY = 2` 탓에 **93일 램프**다.**

---

## 4. 게이트

| 게이트 | 결과 |
|---|---|
| `e2e-onboarding`(오늘 main + 이 가지) | **PASS 74 / FAIL 0** — 판정 수 갈래는 §1-ⓓ |
| `e2e-onboarding` · `T315_MAPBEDS=0` | **PASS 75 / FAIL 0**(차이 = 갈래 ⓐ 두 줄) |
| `e2e-onboarding` · pre-T342(`8a02b1e6`) | 66 / **5** — 이분의 반례 팔 |
| `test-shelter` | **PASS 47 / FAIL 0**(T342 가 고친 그 수 그대로) |
| `test-psite-server` | **PASS**(판정 96 · 빨강 0 — ⓐ-5 가 `LIFE_HOUSE_GAP` 15 · `_mapBeds` 켬을 자식 프로세스로 재확인) |
| 3시드 800일 A/B | **면제** — `lab-modules.js --diff`: 랩 적재 17 ∩ 내 diff 1(`scripts/e2e-onboarding.js`) = **∅** |
| 접점 심볼 | `onboarding` · `_lifeSiteFilters` · `LIFE_HOUSE_GAP` · `_mapBeds` · `NPC_CAP_PER_VILLAGE` · `npcPids` — **읽기만** |
| force / `.lock` | **0 / 0** |
