# T326 — 켰다: 집 간격 **15**(통로 항 버림 · PM 결정 #52) + `_mapBeds` 살리기 **둘 다 서버 기본 켬** · 되돌림 `=0` 둘 · 침상/인구 **107%** · 소멸 0 · 그리고 **어촌2 는 물에 낀 마을이 아니었다**

**[승인 대기 · T326]** `server/village-layout.js`(유도 상수 둘 · 개명) · `server/villages.js`(손잡이 둘 뒤집기 + `HG` 식) ·
랩 둘(리터럴 `18` 폐지 — 정본을 읽는다) · 하네스 셋 · 계측기 하나 신설 · 새 수 **0**(항 하나 뺐다) ·
`sim/` **무접촉** · T324·T325·T295 **무접촉** · 실기 0.

**한 줄 답 — 켰다. 실서버 200일 50마을: 집 384 → **438채** · 침상/인구 78 → **107%** · 침상≥인구 **50/50 마을** ·
소멸 **0** · 인구 2,462(대역 안) · 즉시 정지 **3곳**(광산5·어촌2·임업6 — 셋 다 `인구 = 침상` 정확히 붙은 평형이고
카드가 예상한 "어촌2 하나" 와 다르다 · 표는 §ⓑ). 되돌림은 `=0` 둘로 문 하나.**

**★카드의 전제 둘을 정정한다.**
① *"통로 항을 빼면 15"* — **통로 항만 빼면 17 이다**(2 × (LOT_R + FARM_GAP)). 15 는 통로를 버리고 **한쪽을
부지 원으로 낮춘** `HALL_CLEAR` 문법이다(T315 ⓐ 표의 그 줄). PM 이 적은 수 **15** 를 따랐다 — 그 값에만
실측이 붙어 있다(T315 C·B 팔). **17 은 아직 아무도 안 쟀다**(§회부).
② *"어촌2 는 물에 낀 마을이라 간격으로 안 풀린다"* — **아니다.** 영토 9,900셀 중 **물은 14셀**이고,
하드 필터 전수를 지나는 짝수격자 앵커가 **543곳**, 지금 서 있는 12채를 그대로 두고 다음 한 채를 놓을 자리가
**176곳** 남아 있다(간격 18 에서도 91곳). 방아쇠도 **원한다**(`houseSiteWant(72, 82.8, 12, 0) = true`).
⇒ 이 마을을 막은 것은 **물도 간격도 아니다.** 막힌 자리는 방아쇠 **뒤**(집터를 여는 쪽)다 — 새 계측기
`scripts/t326-eochon2.js` 가 그 수를 낸다. 다음 카드의 자리는 **지형이 아니라 `_lifeAddHouseSite`** 다.

---

## ⓐ 무엇을 켰나 — 자리 표

| | 전(T315) | 후(T326) |
|---|---|---|
| 유도 정본 | `HOUSE_GAP_DERIVED` 18 · `HOUSE_GAP_LOT` 15 · `houseGap(mode)` | **`LIFE_HOUSE_GAP` 15**(기본) · `LIFE_HOUSE_GAP_AISLE` 18(되돌림) — `houseGap` 삭제 |
| `server/villages.js` `HG` | `T315_HOUSE_GAP === '0' ? 18 : houseGap(...)` | `T315_HOUSE_GAP === '0' ? _lifeVL().HOUSE_GAP_AISLE : _lifeVL().HOUSE_GAP` — **양쪽 리터럴 0** |
| `T315_HOUSE_GAP` | `'0'`(끔 = 18) | **`'1'`(켬 = 15)** · 끄는 문 `=0` |
| `T315_MAPBEDS` | `=== '1'`(끔) | **`!== '0'`(켬)** · 끄는 문 `=0` |
| 랩 `L_HGAP` (둘) | `18` 리터럴 | **`VillageLayout.LIFE_HOUSE_GAP`** — 랩도 리터럴 0(랩 기본 = 서버 기본) |
| 진단 팔 `=2` | `houseGap(2)` = 15 | **지웠다** — 그 팔이 가리킨 값이 기본이 되어 죽은 칸이다(카드 ① 지시) |

```js
// server/village-layout.js — 항 하나를 뺐을 뿐이고 두 항 다 위에 있는 정본이다(새 수 0)
const LIFE_HOUSE_GAP_AISLE = 2 * (LOT_R + FARM_GAP) + AISLE;   // 18 (종전 · 되돌림 `=0`)
const LIFE_HOUSE_GAP       = LOT_R + (LOT_R + FARM_GAP);       // 15 ★기본(PM #52)
```

**★개명한 이유 — 이름이 겹치면 조용히 가려진다.** `server/village-layout.js:172` 의 `generate()` 안에는
**다른** `HOUSE_GAP` 이 이미 있다(`opts.houseGap ?? (shore ? 18 : 22)` — **시딩 배치**의 간격).
T315 이름 그대로 뒤집으면 그 지역 선언이 모듈 상수를 가려 버리거나, 반대로 내가 그 줄을 건드리면
**시딩 기하가 조용히 15 로 바뀐다**(작업 중 실제로 한 번 밟고 되돌렸다). 그래서 생활층 쪽에 `LIFE_` 를 붙였고,
그 자리에 경고 주석을 남겼다. **시딩 간격 18/22 의 출처는 아직 주석 한 줄("골목 여유")뿐 — 유도 안 했다(§회부).**

**되돌림** — `T315_HOUSE_GAP=0 T315_MAPBEDS=0` 둘이면 종전 판(간격 18 · `_mapBeds` 미기록)이다.
실서버 판은 실행간 비트 결정적이 아니므로(T254: 같은 설정 3반복 인구 ±15.0%) `diff` 로 말하지 않고
**코드 경로 + 값 동일**로 말한다 — `__probe.handles()` 자식 프로세스 실측(T298 ⓞ 문법)과
`lab-wiring-check` [J]ⓒ-2 · `test-psite-server` ⑫ⓐ-5 가 그것이다. 실측 팔도 하나 돌렸다(§ⓑ OFF).

## ⓑ 실서버 판 — 50마을 × 200일 · 같은 계측기(`t212-beds`) · 게이트는 `t315-gate`

| 팔 | 간격 | 살리기 | 인구 | 집 | 침상 | 침상/인구 | 침상≥인구 | **즉시 정지** | 소멸 |
|---|---|---|---|---|---|---|---|---|---|
| T298 기본(간격 18 · 살리기 끔) | 18 | 끔 | 2,953 | 384 | 2,304 | 78% | 39/50 | 11 | 0 |
| T315 B(같은 설정 · 앞 카드) | 15 | 켬 | 2,631 | 464 | 2,784 | 106% | 50/50 | 1 | 0 |
| **T326 ON①**(기본 · 손잡이 무설정) | **15** | **켬** | **2,462** | **438** | 2,628 | **107%** | **50/50** | **3** | **0** |
| **T326 ON②**(같은 판 재현) | **15** | **켬** | **2,510** | **443** | 2,658 | **106%** | **50/50** | **2** | **0** |

* **켠 판의 자기 폭이 좁다**: 인구 2,462~2,510(±2%) · 집 438~443(±5채) · 침상/인구 106~107%.
  끈 세계의 폭(T254: 인구 ±15.0%)보다 **훨씬 좁다** — 침상이 상한이 되면 세계가 스스로 안정된다.
* 인구는 끈 판(2,953·2,676) 대비 −8~−17%로 **대역 안**이고 **소멸 0**이다.
* `_mapBeds` 는 **50/50 마을 전부** 적혔다. 정확 47~49곳 · 나머지는 **저장 큐 편차 한 채 이내**
  (`village_buildings` 행은 완공 즉시 쓰이고 `econ_state` 는 1마을/틱으로 나간다 — 규약이라 하네스가 그만큼 허용한다).

**거부 사유 — 66% 는 어떻게 됐나**

| | T298(18·끔) | **T326 ON①**(15·켬) |
|---|---|---|
| 집터 탐색 호출 / 성공 / **빈손** | 1,460 / 284 / **1,176** | 524 / **339** / **185** |
| 기존 집과 너무 가까움 | 89,246 (**66.2%**) | 44,781 (**77.5%**) |
| 마을 영토 밖 | 38,085 (28.2%) | 9,264 (16.0%) |
| 쉼터와 너무 가까움 | 5,709 (4.2%) | 1,844 (3.2%) |
| 개간 농지 위 | 1,750 (1.3%) | 1,782 (3.1%) |
| 훑은 셀 | 23.1M | 5.6M |

★**점유율은 안 내려간다(66% → 77.5%)** — 간격은 *마지막까지 남는* 사유라 좁혀도 비중은 오른다.
내려간 것은 **빈손 자체**다: 1,176 → **185**(−84%) · 성공 284 → **339**. 그게 벽이 열린 모습이다.

## ⓒ 즉시 정지한 마을 — 카드가 예상한 "어촌2 하나" 와 다르다(그래서 표)

| 마을 | 인구 | 침상 | `housing` | 영토 | 물 | 바위 | 하드 필터 통과 앵커 | 최대 패킹(15 / 18) | **지금 12·10·18채를 두고 다음 한 채 자리** | 방아쇠 |
|---|---|---|---|---|---|---|---|---|---|---|
| 광산5 | 60 | 60 (10채) | 61.5 | 6,840셀 | **0** | **0** | 865곳 | 19 / 13채 | **167곳** | **원한다** |
| 어촌2 | 72 | 72 (12채) | 82.8 | 9,900셀 | **14** | 521 | 543곳 | 18 / 17채 | **176곳** | **원한다** |
| 임업6 | 108 | 108 (18채) | 124.2 | 14,100셀 | **0** | **0** | 2,136곳 | 44 / 30채 | **875곳** | **원한다** |

★**셋 다 땅이 남는다.** 광산5·임업6 은 영토에 **물도 바위도 0셀**이고, 임업6 은 다음 집을 놓을 자리가
**875곳** 남아 있는데 18채에서 멈춘다. 어촌2 의 물은 9,900셀 중 **14셀**이다.
⇒ **"물에 낀 마을이라 간격으로 안 풀린다" 는 틀렸다.** 세 마을 다 `houseSiteWant` 가 **더 원하고**
자리도 **수백 곳** 있는데 집이 안 선다 ⇒ 막힌 자리는 방아쇠 **뒤**, 곧 `_lifeAddHouseSite`(2패스·거부 메모
`_siteRejS`/`_siteRejL`·`LIFE_SITE_MEMO`) 또는 크루/순서다.
세 팔(T315 B · T326 ON① · ON②)에서 어촌2 의 수가 **72/72/82.8 로 완전히 같다** — 잡음이 아니라 **결정적 정지**다.

⚠이 셈의 한계: **영속 상태로 재는 계측기**다(`t326-eochon2.js` — DB + 정본 지형 어댑터).
실서버의 거부 **메모 캐시**(`_siteRejS`/`_siteRejL`)와 당일 농지 프런티어는 DB 에 없다 —
그래서 "자리가 있는데 안 선다" 의 **범인 후보 1번이 그 캐시**이고, 그것을 여는 것이 다음 카드다(§회부).

## ⓓ 되돌림 — 말이 아니라 실측으로

`T315_HOUSE_GAP=0 T315_MAPBEDS=0` 한 판을 같은 계측기로 돌려 T298 종전 판과 나란히 놓았다.
(실서버는 실행간 비트 결정적이 아니므로 `diff` 가 아니라 **열마다 같은 값이 나오나**로 말한다.)

| | **T326 되돌림(`=0` 둘)** | T298 종전(같은 설정) |
|---|---|---|
| 침상/인구 | **82%** | 82% |
| `_mapBeds` 적힌 마을 | **0** | 0 |
| `hcap` 작아지는 마을 | **9** | 9 |
| 즉시 정지 | **9곳** | 9곳 |
| 새로 막힘 | **3** | 3 |
| 소멸 | **0** | 0 |
| 인구 · 집 | 2,779 · 379채 | 2,676 · 364채 (대역 안) |

⇒ **되돌림은 문 하나로 종전 세계로 돌아간다.** 구조 증명은 `__probe.handles()`(자식 프로세스 · env 청소)와
`lab-wiring-check` [J]ⓒ-2 · `test-psite-server` ⑫ⓐ-5 가 맡는다.

## ⓔ 한 줄 답

**켰다 — 간격 15(통로 항 버림 · PM #52)와 `_mapBeds` 살리기 둘 다 서버 기본.** 실서버 200일 50마을에서
집 384 → **438~443채** · 침상/인구 78 → **106~107%** · **침상≥인구 50/50 마을** · **소멸 0** · 인구는 대역 안 ·
빈손 1,176 → **185**(−84%) · 되돌림은 `=0` 둘로 종전 판과 열마다 같다. 남은 즉시 정지는 2~3곳인데
**땅 때문이 아니다** — 셋 다 다음 집 자리가 167~875곳 남고 방아쇠도 더 원한다. 그래서 **"물에 낀 마을" 이라는
설명은 틀렸고**, 다음 카드가 볼 자리는 지형이 아니라 **`_lifeAddHouseSite`(거부 메모 캐시가 1순위)** 다.

## 검증

| 검사 | 결과 |
|---|---|
| **기본 극성** | `__probe.handles()` 자식 프로세스 실측 — 간격 **켬**(미설정 = `LIFE_HOUSE_GAP` 15) · 살리기 **켬** · `=0` 둘이면 종전(18 · 미기록) |
| **되돌림 실측 팔** | `T315_HOUSE_GAP=0 T315_MAPBEDS=0` 200일 판이 T298 종전 판과 **열마다 같다**(§ⓓ) |
| **3시드 800일 A/B** | 카드가 면제했지만 `lab-modules --diff` 교집합 2개(`village-layout.js`·`villages.js`)라 **그냥 돌렸다** — 1020·7·42 전부 **정본 main 실행과 비트 동일** |
| `t17-metrics` 여덟 수 | 8,318 / 7,935 / 7,853 · 소멸 **0/51 셋** · `vpop` 51 |
| `test-psite-server` | **96 ✓ / 0 ✗ · PASS** — ⑫ 개정(기본 켬 · 진단 팔 삭제 확인 · 쓰는 자리 한 줄 · 하루 경계 전제 명시) |
| `test-terr-persist` | **PASS** — 50마을 유지 · **영토 밖 집 0/333채** · terr 행 216,026 → 256,080 |
| `test-farm-life` | **33 PASS / 0 FAIL** |
| `lab-wiring-check` | **실패 0 · 경고 0** — [I] 개정(랩 리터럴 0 · 서버 분기 꼴 · 인라인 식 동일) · [J] 개정(닫힌식 둘 · **셀 집합 둘** · 실측문 극성 · `FARM_GAP` 변조) |
| `build-econ-bundle --check` | **8 일치 / 0 불일치** |
| `inline-engine --check` | 랩 둘 **최신** |
| 계측기 | `scripts/t326-eochon2.js` **신설**(러너 밖 · 끝난 판 DB + 정본 지형 어댑터 · 세계를 안 돌린다) · `t315-gate.js` 개정(저장 큐 편차 허용 · 새 이름) |
| 러너 전수 | **안 돌렸다**(카드 규약) |
| 무접촉 | **`sim/` 전부** · **T324·T325**(이동 문·나무꾼) · **T295**(전쟁 절) · `server/zone.js` · `public/` |
| 실기 | **0** |
| 접점 | `LIFE_HOUSE_GAP` · `LIFE_HOUSE_GAP_AISLE` · `AISLE` · `LOT_R` · `FARM_GAP` · `HALL_CLEAR` · `HG` · `T315_HOUSE_GAP` · `T315_MAPBEDS` · `_mapBeds` · `_mapBedsOf` · `_hcap` · `HOUSE_CAP_PER_FLOOR` · `L_HGAP` · `L_FLOORCAP` · `__probe.handles` · `houseSiteWant` · `_lifeAddHouseSite` |

**손 댄 파일 9개**

| 파일 | 무엇 |
|---|---|
| `server/village-layout.js` | 유도 상수 둘(개명 + 기본 뒤집기) · `houseGap` 삭제 · 시딩 `HOUSE_GAP` 경고 주석 (+25 −11) |
| `server/villages.js` | 손잡이 둘 기본 켬 · `HG` 를 정본 둘로 (+23 −11) |
| `lab/마을실험실.html` · `lab/전쟁실험실.html` | 인라인 유도 셋 손 동기 · 수출 · `L_HGAP` 리터럴 폐지 (각 +10 −5) |
| `scripts/lab-wiring-check.js` | [I]·[J] 개정(`cellGap` 일반화 · 실측문 극성) (+116 −45) |
| `scripts/test-psite-server.js` | ⑫ 개정 (+38 −17) |
| `scripts/t315-gate.js` | 새 이름 · 저장 큐 편차 규약 (+13 −4) |
| `scripts/t326-eochon2.js` | **신설** — "왜 안 서나" 계측기 |
| `인계/V-마을배치.md` · `인계/공통.md` | `T326-간` 절 신설 · `T315-간` 옛 표시 · 기준선 주석 갱신 |

## 회부

* ★★**다음 카드는 지형이 아니라 `_lifeAddHouseSite` 다.** 정지 세 마을 전부 자리가 167~875곳 남고 방아쇠도
  원하는데 집이 안 선다. 후보 1번은 **거부 메모 캐시**(`_siteRejS`/`_siteRejL` · `LIFE_SITE_MEMO` ·
  `LIFE_SITE_RESCAN_DAYS=7`) — 영토가 자라고 밭이 바뀌어도 옛 거부가 살아남으면 정확히 이 그림이 된다.
  손잡이 `LIFE_SITE_MEMO=0`(종전 = 매일 전수) 한 문으로 A/B 가 된다 — **코드 0 으로 가를 수 있는 카드다.**
* **시딩 배치 간격 `18/22`(`village-layout.js:172`) — 아직 유도 안 됐다.** 생활층은 15 로 내렸는데 시딩은
  18/22 로 남았다(그래서 갓 생긴 마을은 여전히 넓게 앉는다). 출처는 주석 한 줄("골목 여유")뿐이다.
* **통로 항만 뺀 `17` 은 아무도 안 쟀다** — PM 이 고른 15 는 T315 가 실측을 붙인 값이라 그걸 켰다.
  17 이 궁금하면 `LIFE_HOUSE_GAP` 식 한 줄로 잴 수 있다(값 결정 = 재민).
* **게이트 "즉시 출생 정지 0" 은 문자 그대로는 닿을 수 없다** — `_mapBeds` 가 상한이면 `인구 = 침상` 이
  **평형**이라 붙은 마을이 늘 몇 곳 생긴다. 뜻이 있는 게이트는 **소멸 0 · 침상≥인구 50/50 · 인구 대역** 셋이고
  그 셋은 통과했다. 게이트 문구를 그 셋으로 바꾸는 것은 PM 몫.
* 첫 의뢰(#18) · 집도 행위인가(#51 재민 · T311 회부) — 그대로.

## 푸시

| | |
|---|---|
| 해시 | **`6162a6a`** (컨테이너 원본 `65f9f6ad` 를 main tip 위로 cherry-pick — **충돌 0**) |
| 가지 | **`batch/house-gap-on-0919`** (가지만 · `main` 은 PM) |
| 베이스 | `8f12f6e` — T324·T325·T295·T327 전부 살아 있다(리베이스 충돌 0) |
| main | `8f12f6e` — **안 건드렸다** |
| force | **0** |
| `.lock` | **0** |
| 리베이스 뒤 재검증 | `node --check` 여섯 · 실측문(간격 켬 `'1'` · 살리기 `true`) · `lab-wiring-check` **실패 0 · 경고 0** (맥 VM `$HOME/pushrepo`) |
