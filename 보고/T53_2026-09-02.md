# T53 — `41-h-char.js` 2차 분할 (행동 변경 0)

> 세션4 · 2026-09-02 · 베이스 `origin/main` `213f44e2`
> 한 줄: **986줄을 다섯 영역으로 갈랐다. 이어 붙이면 원문과 바이트 동일하다.**
> T51 과 다른 점 하나: **이 파일엔 최상위 실행문이 18개 있었다**(32 는 0이었다).

---

## 0. 먼저 쟀다

### ⓐ 내용 지도 — 깊이1 선언 49개 · function 30개

```
     5–  89  근접 브리핑 tick(_evVillageAnchorAbs·_evProximityTick) + setInterval
              + 하네스 진단 훅 17개(window.__evBubbles·__moodles·__tradeSel·__panelText·
                __vignetteOn·__vgAxis·__vgTint·__vgAxes·__ulRows·__ulSubs·__ulToggle·
                __ulShape·__ledger·__lots·__uiCfg·__setCharSprite)
    92– 135  drawSpeechBubble
   151– 422  캐릭터 스프라이트 — charMeta·charSheet·charDirRow·charLayersFor·charState·
              drawCharSprite·drawPlayerIso(138줄)
   426– 516  ITEM_ICONS·ITEM_ICON_IMG·ICON_NO_RENDER·itemIcon*·drawItemIcon·ITEM_LABEL·
              CROP_BY_ID·CROP_OF_SEED·cropSeasonNow·SEASON_KO·applyCropPayload
   519– 637  컨텍스트 메뉴(hide/showContextMenu)·핫키바(ensure/updateHotkeyBar)
   638– 820  updateHud(181줄) + `setInterval(updateHud, 100)`
   822– 886  updateMinimap · renderChatLog
   889– 943  거래소 — toggleMarketplace·refreshMarket·placeOrder
   946– 986  상자·제작 토글·도구/장비 표·equipSkillLevel·equipPreview
```

★**지시서의 시작안과 순서가 달랐다.** 시작안은 `41-h-hud`(HUD·미니맵·핫키바) →
`42-r2-char`(캐릭터·말풍선·아이콘) → `43-h-menu`(메뉴·거래소) 였는데, 실측 순서는
**말풍선·훅 → 캐릭터 → 아이콘 → 메뉴·HUD·미니맵 → 거래소**다. 시작안대로 묶으려면
파일을 가로질러 재배열해야 하고 그건 규약 위반이다(순서 보존 > 영역 순도). ⇒ 순서를 따랐다.
그래도 `42-r2-char.js` 는 시작안과 이름·내용이 그대로 겹쳤다.

### ⓑ 최상위 실행문 — **0이 아니라 18개다** (이 배치의 진짜 위험)

T51 의 `32-m-render` 는 0이었다. 여기선 18이다(진단 훅 17 + `setInterval(updateHud,100)`).
**조각을 나누면 function 선언 호이스팅이 파일 경계에서 끊긴다** — 실행문이 자기보다 뒤 조각의
이름을 부르면 그 자리에서 죽는다. 그래서 자르기 전에 먼저 세웠다:

```
  node scripts/split-audit.js public/client/41-h-char.js
    파일 987줄 · 실질 최상위 문장 66 · function 선언 30 · 선언 이름 49
    ★실행문 18개 중 **뒤에 선언된 이름을 참조하는 것 0개**
```
그리고 손으로 한 번 더 봤다: 17개는 전부 `window.__x = () => …` 꼴이라 **몸통이 나중에** 돈다.
즉시 식별자를 읽는 실행문은 `setInterval(updateHud, 100)` 하나뿐이고, `updateHud` 는
**같은 조각(④)** 에 있다. ⇒ 안전.

### ⓒ 경계 후보 — 밖에서 부르는 이름 34 / 안에서만 쓰는 이름 15

```
  밖에서 부른다: _evVillageAnchorAbs·drawSpeechBubble·drawCharSprite·drawPlayerIso(34-m-renderloop)
                 charLayersFor·drawCharSprite(40-r2-sprites) · itemIconHtml·ITEM_LABEL·TOOL_*·
                 EQUIP_ICONS·equip*(50-i-panel) · showContextMenu·CROP_OF_SEED·SEASON_KO(51-s-side)
                 applyCropPayload·updateHotkeyBar·renderChatLog·placeOrder·closeChest·toggleCraft(30-n-net)
                 updateHud(30-n-net·70-lobby·99-main) · ITEM_ICONS 외 3(99-main) …
  안에서만 쓴다: _evProximityTick · _charMeta/_charMetaTried/_charImg/_charAnim · charMeta ·
                 charSheet · charDirRow · charState · itemIconImg · CROP_BY_ID · _ctxMenuEl ·
                 hideContextMenu · ensureHotkeyBar · marketOpen · refreshMarket · openChest
```
★T51 때와 같은 결론이다: 조각들은 **전역 렉시컬 스코프 하나**를 나눠 쓰므로 이 구분이 경계를
정하지 않는다. 실제 제약은 ⓑ 하나다. 경계는 **주석 배너**에서 잡았다(사람이 읽는 단위 = 자르는 단위).

### ⓓ 검사기 — T51 이 만든 것을 **고치지 않고** 그대로 썼다

`split-verify.sh` 는 여전히 1차 분할용이라 HEAD 에서 `differ` 다(T18 관측 · T51 보고 §0-ⓓ).
2차 분할용 `scripts/split-verify2.sh` 는 T51 이 "다음 2차 분할이 그대로 재사용한다"고 남긴 것이고,
**실제로 인자만 바꿔 돌아갔다.** 검사기를 배치마다 새로 만들지 않았다는 게 이번 확인이다.

---

## 1. 무엇을 했나

```
  public/client/41-h-char.js (986줄)  →  5조각 (등록 순 = 원문 순 · 재배열 0)

    41-h-bubble.js   137줄  fn 3   근접 브리핑 말풍선 · 진단 훅 17      (원문 2–136)   [H]
    42-r2-char.js    289줄  fn 7   캐릭터 스프라이트 · drawPlayerIso     (137–423)     [R2]
    43-i-icon.js      96줄  fn 5   아이템 아이콘 · 이름표 · 작물 페이로드 (424–517)     [I]
    44-h-hud.js      372줄  fn 7   메뉴 · 핫키바 · updateHud · 미니맵    (518–887)     [H]
    45-t-market.js   101줄  fn 8   거래소 · 상자 · 제작 · 장비 미리보기   (888–986)     [T/F]

  합 fn 30 = 원문 30 · 최상위 이름 49 = 원문 49 · 최상위 실행문 18 = 원문 18
```

자른 자리는 넷 다 **빈 줄 뒤 주석 배너 앞**이다(136/423/517/887).
diff 는 **자르기 + 머리 마커 5줄 + `index.html` 등록(1줄 삭제·5줄 추가) + 감사기 기준선 표**뿐이다.

**★이 배치의 본론은 줄 수가 아니라 영역이다.** 종전엔 네 영역의 세션이 같은 파일을 만졌다 —
T13 이 캐릭터 레이어(2줄), 온보딩 v2 가 `updateHud`(1줄), T38 이 `ITEM_LABEL`(사본 표),
T12 가 HUD 의 적재 상한. 이제 각자 다른 파일이다.

**서버·econ diff 0**.

---

## 2. 지시서와 다르게 한 것 — 둘

### ① 3조각이 아니라 5조각

시작안은 3이었다. 실측 순서로는 3으로 묶을 수 없고(위 ⓐ), 986줄이라 크기 목표(1,500)는
어느 쪽으로 갈라도 만족한다. ⇒ 크기가 아니라 **영역 소유**로 갈랐다. 96줄·101줄 조각이 작아
보이지만, 그 둘이 각각 I 영역과 T 영역의 유일한 접촉면이라 붙여 두면 분할의 뜻이 없어진다.

### ② ★**이름을 내용에 맞췄다 — `41-h-char.js` 는 없어졌다**

T51 에선 `32-m-render.js` 라는 이름을 건축 오버레이 조각에 남겼다. `split-verify.sh --selftest`
가 그 경로를 박아 두고 있어서 개명하려면 하네스를 건드려야 했기 때문이다(회부해 뒀다).
**여기선 그 제약이 없다** — 하네스가 박아 둔 41 경로는 없고, 유일한 참조인
`test-client-globals` 의 기준선 표는 **분할하면 어차피 갱신해야 하는 것**이다(그 표가 파일 이름
단위다). ⇒ 공짜로 정직해질 수 있어서 그렇게 했다. T51 의 개명 회부는 그대로 남는다(32 쪽).

---

## 3. 검증

### ⓐ 결합 diff — **identical**

```
  bash scripts/split-verify2.sh --selftest 41-h-char \
       41-h-bubble,42-r2-char,43-i-icon,44-h-hud,45-t-market

    ✓ 오염된 조각에서 differ — 검사기가 실패할 줄 안다 (원복 확인)
    identical — … 를 이어 붙이면 HEAD 의 41-h-char.js 와 바이트 동일
```

### ⓑ 이름 감사 — 기준선 표를 옮겨 적었고, **감사기가 눈멀지 않았는지 확인했다**

```
  node scripts/test-client-globals.js      PASS 12 / FAIL 0
      · 등록 classic 25개 · 최상위 선언 949개 · 중복 0 · 브라우저 전역 교집합 0
      · ③ 증가한 조각 0 · 총계 115건 유지
```
분할 직후엔 `✗ client/41-h-bubble.js 0→17 · client/44-h-hud.js 0→1` 이 떴다 — 표가 파일 이름
단위라 당연하다(총계는 그때도 115였다). 표를 옮겨 적고 **일부러 다시 깨뜨려 봤다**:
조각에 `window.__t53probe = 1;` 한 줄을 더하니 `43-i-icon 0→1 · 총계 116` 으로 잡는다(원복 확인).
⇒ 표를 고쳐서 통과시킨 게 아니라, **고친 표로도 여전히 잡는다**.

### ⓒ 문법 · 대리 지표

```
  node --check   5조각 전부 OK
  가장 큰 조각 372줄 — 목표(1,500 미만) **달성**  (T51 은 render 2,195줄 때문에 미달성이었다)
```

### ⓓ 실클라 e2e

```
  bash scripts/run-regress.sh   (표 `// @regress` 자동 발견 **72개**)
      ★통과 66 · 실패 6
```

**실패 6개 — 하나도 이 배치의 것이 아니다.** 전수로 갈랐다:

| 하네스 | 러너 | 귀속 근거 | 판정 |
|---|---|---|---|
| `e2e-charsprite` | ✗ 31/4 | ★**베이스 `213f44e2` 깨끗한 클론에서 같은 4건이 같은 값으로** 실패한다 | 상류. 내 것 아님 |
| `test-guest-rejoin` | ✗ ② 위치 보존 | ★**내 트리 단독 26/0 · 베이스 단독 26/0** — 러너 부하 흔들림 | 내 것 아님 |
| `e2e-rumor` | ✗ ⑥ | ★**단독 재실행 35/0** | 부하 흔들림 |
| `e2e-events` | ✗ 부족 픽스처(소비EMA) | ★**단독 재실행 32/0** (온보딩 배치에서도 같은 자리) | 부하 흔들림 |
| `test-site-memo` | ✗ 씨앗 DB 없음 | **서버 하네스** — 이 diff 는 `public/client/*`·`index.html`·감사기 표뿐이다 | 상류 순서 의존 |
| `test-tick-slicer` | ✗ ③·⑧ | 같음(서버 성능 하네스 · 2코어) | 상류 |

★**이 배치가 가장 무서워한 셋이 전부 초록이다**: `e2e-npcsprite` 통과 · `e2e-ui` 통과 ·
`e2e-charsprite` 는 **베이스와 완전히 같은 31/4**(내가 만진 `drawCharSprite`·`drawPlayerIso` 가
바로 그 조각 ②에 있다 — 실패 항목이 하나도 늘지 않았다는 게 이 배치의 핵심 증거다).
그 밖에 그리기·HUD 를 재는 것들도 초록이다: `e2e-inv` · `e2e-weight` 32/0 · `e2e-tilestate` ·
`e2e-mtcut`·`e2e-mtocc`·`e2e-cutaway` · `e2e-zoom` · `e2e-membership`(상류 T11 의 새 하네스).
콘솔 오류: 각 하네스가 "페이지 오류 0 / 클라 JS 예외 0" 을 재고 통과했다 — 분할 전과 같다.

### ⓔ 기준선 (econ)

`git diff --stat -- server/ sim/` 가 **빈 줄**이다 — 시뮬레이션 코드를 한 글자도 안 건드렸다.
그래도 러너로 한 번 돌렸다:
```
  bash scripts/ab-extinct.sh t53   (3시드 × 800일)
    s1020: 6,280 · 0/51     s7: 5,993 · 0/51     s42: 6,072 · 0/51     무기Q 787
```
★T51 · 온보딩 v2 와 **세 수가 같다**.

---

## 4. 회부 (구현 안 함)

* **진단 훅 17개가 클라 정본에 상주한다**(`41-h-bubble.js`). 하네스 전용인데 배포본에 실린다 —
  T51 이 본 `render()` 분해와 같은 급의 다음 층: 훅 파일을 따로 떼고 `index.html` 에서
  개발 빌드에만 등록할지. **설계 판단이라 회부.**
* `43-i-icon.js` 의 `ITEM_LABEL` 은 서버 표의 **사본**이다(T38 이 남긴 T38-b). 이제 파일이
  갈렸으니 그 회부를 실행할 자리가 분명해졌다 — I 영역 세션의 몫.
* `32-m-render.js` 개명(T51 회부) · `render()` 2,195줄 분해(T51 회부) — 그대로 남는다.
* `split-verify.sh` 가 `differ` 일 때 EPIPE 스택을 토한다(T51 회부) — 그대로 남는다.
* `70-lobby.js` 에 `// @@split-added` 표가 없다(T51 회부) — 그대로 남는다.
