# T237 — 비옥도 자리: `_terrGrow` 가 `land.fertility` 를 안 덮는다 (랩 문법 ⓑ · 한 자리)

**[재민 확정 2026-09-13 · T237]** `server/villages.js` **한 자리** · 손잡이 **0** · 새 수 **0** · 실기 0.

**한 줄 답 — 덮기 자리 **지웠다**(한 자리 · `arable` 은 남김) · econ 랩 3시드 전문 diff **0·0·0** ·
켬 팔 인구 Δ **−41.9% → −9.0%** · 비옥도 **29종 유지 ○**(0.70 마을 0) · 살려도 되는 마을 **9/25** ·
ⓐ 메모리 **61 KB/마을 · 50마을 2.98 MB**.**

**★카드의 전제 하나를 정정한다 — 이 결함은 잠들어 있지 않았다.**
끔 세계에서도 영토는 200일에 **63~67셀** 자라고, 그 자란 마을이 정확히 **셋**이다.
그 셋은 세 판(T212·T219·T230) **전부 같은 셋**이고 전부 `0.70` 이었다 — 그리고 세계에서 **가장 비옥한 땅**이었다:

| 마을 | 종전(덮힌 값) | 고친 뒤(창설 값) | 끔 세계 인구 |
|---|---|---|---|
| 어촌2 | 0.70 | **1.26** | 111 → **252** |
| 임업6 | 0.70 | **1.23** | 144 → **212** |
| 농촌2 | 0.70 | **1.17** | 110 → 73 |

⇒ **끔 세계도 안 같다.** 이 카드는 비트 동일이 아니라 **세 마을을 고친다**(끔 팔 세계 인구 2,390 → **2,782**).

---

## 푸시

| | |
|---|---|
| 커밋 | `473ff62b` (컨테이너 원본 `12dd26b3` · 리베이스로 해시만) |
| 베이스 | 측정 `aa382c69` → 푸시 시점 `0a15c9b` |
| 브랜치 | `batch/fert-0913` → **ff → main** |
| 원격 main | `473ff62b` |
| force | **0** · `.lock` 없음 · 얕은 작업 클론(`$HOME/pushrepo` · `--depth 120`) · `~/mnt/Mini/durango-mini` 무접촉 |
| 번들 | `~/Mini/_inbox/T237_2026-09-13.bundle` (베이스 `aa382c69` → 헤드 `12dd26b3`) |
| 손잡이 | 이 카드는 **0** · `T230_TERR_HOUSING` 은 main 에 **끔** 그대로 |
| 실기 | **0** |

---

## ⓐ 덮기 자리 — 누구를 위해 있었나

**도입**: `6412516a`(2026-07-30) *"영토 확장 실동 — econ이 산 땅만큼 실제로 넓히고 바로 개간한다 [재민 지적]"*.
주석은 *"지형 재측정 → econ 반영(비옥도는 영토 실측 평균 × 1.4 — 씨딩과 같은 환산)"*.
**뜻은 리카도였다** — 한계지를 삼키면 평균 지력이 내려간다. 옳은 뜻이고, 재료가 틀렸다.

### `_FF`(비옥도 장)를 읽는 자리 전수

| 자리 | 언제 도나 | `_FF` 가 제 것인가 |
|---|---|---|
| `villages.js:413·415` `extractLandParamsApprox` | **창설 때**(같은 루프에서 `prepareFert` 직후) | **✓ 옳다** |
| `villages.js:455` `marginalQOf` | **창설 때**(위와 같은 자리) | **✓ 옳다** |
| `village-layout.js:124` `lval`(generate 안) | **창설 때** | **✓ 옳다** |
| `villages.js:2430·2432` `_terrGrow` **후보 점수** | 런타임(영토가 자랄 때) | **✗ 중립 0.5** — "어느 셀을 삼키나"만 바뀐다(econ 무접촉) → **회부** |
| `villages.js:2459·2462` `_terrGrow` **`land.fertility` 덮기** | 런타임 | **✗ 중립 0.5 ⇒ 늘 0.70** → **이 카드가 지운다** |
| `onboarding.js:322` `fert` 를 어댑터에 실어 보냄 | 도착 기하 | **한 번도 안 읽는다**(`axisAt`=elev · `nearestBank`=isWater) |

**뿌리** — `let _FF = null`(`:193`) **하나**. `prepareFert`(`:194`)가 부를 때마다 **갈아 끼운다**.
시딩 루프(`:1150`)가 마을마다 부르므로 **부팅 뒤엔 마지막 마을 상자 하나**만 남고,
`fert = (x,y) => (_FF ? _FF.at(x,y) : 0.5)`(`:202`)가 나머지 마을에 **중립 0.5** 를 준다.
하네스가 이 사실을 **실측으로** 세운다(⑪ⓑⓒ · 같은 어댑터로 `fert(A) 0.631 → 0.5`).

### `land.fertility` 를 읽는 쪽 (덮으면 무엇이 움직이나)

econ 엔진의 뼈대다 — 농부 산출(`JOBS.farmer.landBoost`) · 채집(`(fert+wood+stone)/3`) ·
`foodCapOf`(식량 상한) · `marginalQ`/`mbDaily`(한계지 투자) · 직업 매력(`jobValue`) · K.
⇒ **0.70 으로 평탄화하면 세계가 굶는다**(T230 실측 −42%).

### ⓐ(마을별 장)의 메모리 셈 — 셈만, 심지 않는다

| | 값 |
|---|---|
| 상자 | 반경 62 ⇒ 125 × 125 = **15,625셀** |
| 자료 | `Float32Array` — `buildField` 가 남기는 건 `raw` 하나(WD·RD·적분영상 `ii` 는 생성 중에만 산다) |
| 마을당 | **61.0 KB** |
| 50마을 | **2.98 MB** · 51후보 2.98 MB |
| 100마을(플레이어 마을 포함) | 5.96 MB |

싸다. 다만 **새 자료구조**라 캐논은 재민(회부).

---

## ⓑ 한 자리 — 랩 문법 ⓑ(안 덮는다)

```js
//   (종전) land.fertility = max(0.1, min(2.0, (Σ fert / n) × 1.4));
let tw = 0;
for (const k of own) { …; if (ta.isWater(x, y)) tw++; }
const n = own.size;
land.arable = +((n - tw) / n).toFixed(2);      // ★arable 은 남긴다 — `isWater` 로만 세므로 장과 무관, 늘 옳았다
```

랩 정본이 그 문법이다 — `growTerritory` 머리 주석: *"land.size는 lifeInit서 1회 고정 → **경제 영향 0**(렌더/정착 경계만)"*.
**랩 영토는 경제에 안 닿는다.** 이제 서버도 같다: 창설 때 `prepareFert` 가 **제 상자를 들고** 잰 값이 그대로 산다.

| 비트 동일 | 결과 | 무엇을 증명하나 |
|---|---|---|
| econ 랩 3시드 800일 전문 diff | **0 · 0 · 0** (베이스 `aa382c69`) | `_terrGrow` 는 econ 랩 경로에 **없다** ⇒ 랩 무영향. **"끔 세계 무변"의 증명은 아니다** |
| 실서버 끔 팔 | **안 같다** — 세 마을이 창설 값을 되찾는다(위 표 · 세계 인구 2,390 → 2,782) | 그게 이 카드가 고치려던 것이다 |
| `test-farm-life` | **33 PASS / 0 FAIL** | |
| `test-psite-server` | **PASS**(⑪ 절 8줄 신설) | |
| `lab-wiring-check` | **실패 0 · 경고 0**([I] 절 무변) | |
| `test-lab-psite` | **PASS**(한가한 기계 · `B 13/13`) | 랩은 이 카드가 손도 안 댔다 |

### 하네스 ⑪ — 고치기 전 빨강

| 줄 | 실측 |
|---|---|
| ⑪ⓐ (전제) 상자가 안 겹치는 두 자리 | A(900,1800) · B(1400,2300) |
| ⑪ⓑ (전제) 제 상자를 깔면 A 는 중립이 아니다 | `fert(A) = 0.631` ≠ 중립 0.5 |
| ⑪ⓒ ★**장은 하나다** — B 상자를 깔면 A 가 중립으로 떨어진다 | **0.631 → 0.5** |
| ⑪ⓓ 그래서 종전 덮기의 답은 늘 | **0.70**(중립 × 1.4 — 모듈 `FERT.NEUTRAL` 로 검산 · 하네스가 0.5 를 안 적는다) |
| ⑪ 영토가 자라도 `land.fertility` 를 안 덮는다 | ✓(함수 본문 · 주석 제거 후) |
| ⑪ⓕ `land.arable` 은 그대로 갱신한다 | ✓ |
| ⑪ⓖ [자명 통과 금지] 덮기를 되돌린 판을 만들면 문다 | ✓ |
| **고치기 전 실행** | **✗ 빨강 1건 · `결과: FAIL`** (되돌린 판으로 확인) |

---

## ⓒ `T230_TERR_HOUSING` 켬 팔 재측정 (실서버 200일 · 50마을 · 시드 하나)

| | T230 끔 | T230 켬 | **T237 끔** | **T237 켬** |
|---|---|---|---|---|
| 인구 | 2,390 | 1,389 | **2,782** | **2,531** |
| **인구 Δ(제 끔 대비)** | — | **−41.9%** | — | **−9.0%** |
| `land.fertility` 종 | 28 | **2** | **29** | **29** |
| 0.70 인 마을 | 3 | **49** | **0** | **0** |
| 비옥도 중앙 | 0.96 | 0.70 | **0.99** | **0.99** |
| 실체 집 | 235 | 264 | 236 | **316** |
| 침상 | 1,410 | 1,584 | 1,416 | **1,896** |
| 침상/인구 | 59% | 114% | 51% | **75%** |
| 영토 중앙 | 3,450 | 4,200 | 3,450 | **5,100** |
| 집터 성공 / 빈손 | 135 / 795 | 130 / 687 | 136 / 780 | **216** / 1,613 |
| `_hcap<housing` 마을 | 23 | 2 | **25** | **16** |
| **살려도 되는 마을** | — | 21/23 | — | **9/25** |
| 농부 없는 마을 | 7 | 27 | **3** | **7** |
| econ `housing` | 2,390 | 1,126 | 2,684 | 2,367 |

**★인구 −42% 는 사라졌다.** 그리고 켬/끔의 **마을별 비옥도가 한 수도 안 다르다**(0.30=0.30 · 0.94=0.94 · 1.26=1.26)
— 손잡이가 이제 econ 을 **안 건드린다**. 그게 이 카드의 증명이다.

**남는 Δ 는 −9.0% 다.** 비옥도가 아니라 **땅을 삼키는 일 자체의 값**이다:
새 셀 개간(나무 제거) · `_jobSites = null`(현장 후보 재계산) · `arable` 변화 · 카오스 갈라짐
(T219 가 잰 팔 간 잡음이 ±3 마을 규모이므로 −9% 중 일부는 잡음이다 — 시드 하나 판이라 분리 못 한다).

**살려도 되는 마을이 21/23 → 9/25 로 준 이유**는 나쁜 소식이 아니다:
비옥도를 지키니 **인구가 살아 있어서**(1,389 → 2,531) 침상이 더 많이 필요해졌을 뿐이다.
큰 마을(인구 50↑ 17곳)은 여전히 침상/인구 **53%** — 기하가 더 필요하다.

---

## 검증

| 항목 | 결과 |
|---|---|
| 서버 diff | `server/villages.js` `_terrGrow` **한 자리**(비옥도 덮기 제거 · `arable` 유지) |
| 클라 diff | **0** |
| 손잡이 | **0**(이 카드) · `T230_TERR_HOUSING` 은 **끔 그대로**(켬은 계측기 env 로만) |
| 새 수 | **0** |
| econ 랩 3시드 800일 diff | **0 · 0 · 0**(베이스 `aa382c69`) |
| 하네스 | `test-psite-server` **PASS**(⑪ 신설 · 고치기 전 **FAIL 1건** 확인) · `test-farm-life` **33/0** · `lab-wiring-check` 실패 0·경고 0 · `test-lab-psite` **PASS** |
| 러너 전수 | **안 돌렸다**(규약 ②) |
| 무접촉 | T241(세션5 `villages.js` 사냥·자) · T242(세션7 `zone.js`) — 내 diff 는 `_terrGrow` 한 블록뿐 |
| 접점 | `_terrGrow` `land.fertility` `_FF` `prepareFert` `fert(` `territoryTarget` `T230_TERR_HOUSING` `_lifeSiteFilters` `houseSiteWant` `t212-beds` `T212_ENV` |

## 회부

- **#22 켜기 순서 — 다음은 영토다.** 비옥도가 풀렸으니 `T230_TERR_HOUSING` 을 켜는 판단이 선다:
  켜면 실체 집 236 → **316채** · 침상/인구 51 → **75%** · 살려도 되는 마을 **9/25** · 인구 −9.0%.
  −9% 를 받을 값인지, 아니면 기하(집 간격 18 · 쉼터 이격)를 먼저 손댈지는 **재민**.
- **★`_terrGrow` 후보 점수도 중립을 읽는다**(`:2430`·`:2432` · `fertW = 3.5`). econ 엔 안 닿지만
  **"어느 셀을 삼키나"의 비옥도 tiebreak 가 통째로 중립화**돼 거리항만 남는다(영토가 지형과 무관한 블롭이 된다 —
  `:180` 주석이 옛날에 같은 병을 적어 뒀다). 고치려면 **마을별 장(ⓐ)** 이 필요하다. → **캐논 · 재민**
- **ⓐ 마을별 비옥도 장** — 61 KB/마을 · 50마을 **2.98 MB**. 싸지만 새 자료구조. → **캐논 · 재민**
- **`test-lab-psite ⑧` 은 여전히 기계 의존이다** — 이 판에서도 부하 아래 `B 11/13` 으로 빨갛고,
  한가할 때 `B 10/10`·`13/13` 으로 초록이다(T219 에서 같은 베이스 대조로 이미 가름).
  랩은 이 카드가 손도 안 댔다(서버 `_terrGrow` 한 자리뿐). 한가한 기계에서 다시 돌리니 `B 13/13` **PASS**.
  → **공통 §2 상시(기계 의존) 목록 후보**
- 서버 고유 둘(쉼터·환호) 그대로 · 첫 의뢰(#18) · 추천 자격(#19) · 16방향 · `nippon` 시딩(Z).

## 실행

```
node scripts/test-psite-server.js                                  # ⑪ 비옥도 절 포함
node scripts/t212-beds.js                                          # 끔 팔(200일)
T212_ENV=T230_TERR_HOUSING=1 node scripts/t212-beds.js             # 켬 팔
LAB_SEED=1020 node scripts/econ-lab-real.js 800                    # econ 랩 무영향
```
