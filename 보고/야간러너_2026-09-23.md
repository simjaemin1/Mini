# 보고 — 야간 러너 2026-09-23 (+0900)

**기준 커밋 `df16a312`** · 시작 **10:37** → 종료 **12:48** (2시간 11분)
· 오늘 묶음 **C** · 목록 **122종** · 돈 것 **122** · 통과 **117** · 실패 **5** · 끊김 **0** · 미측정 **0**
· 이 문서는 `scripts/nightly-report.js` 가 로그만 읽어 냈다 — **모델 판단 0** · 같은 로그면 바이트가 같다
· 로그 `/tmp/night/regress.log` · 단독 재실행 `/tmp/night/regress.log.retry` · 상시 목록 `scripts/nightly-known.txt`
· 직전 밤 `보고/야간러너_2026-09-22.md` — 빨강 5종 · "연속 n밤"은 **세기만** 한 수다(판정 0)

## ★ 한 줄 — **상시 밖 빨강 5건** — `test-audio` · `test-site-memo` · `test-terraincache` · `e2e-npcsprite` · `e2e-winter`

## 1. 실패 표

| 하네스 | 연속 | 첫 판 | 소요 | 단독 재실행 | 귀속 |
|---|---|---|---|---|---|
| `test-audio` | 연속 **1밤** | RC=1 · 결과줄 127개 · ⑫b ★서버 어부 자리가 `낚음` 을 세운다  hook | 0분 | 여전히 빨강 (RC=1 · 결과줄 127개 · ⑫b ★서버 어부 자리가 `낚음` 을 세운다  hook) | **귀속: 모델 몫** |
| `test-site-memo` | 연속 **4밤** (상시 밖) | RC=1 · 결과줄 22개 · ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  13회 / 9일 | 9분 | 여전히 빨강 (RC=1 · 결과줄 22개 · ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  13회 / 9일) | **귀속: 모델 몫** |
| `test-terraincache` | 연속 **1밤** | RC=1 · 결과줄 24개 · isWaterTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개 | 0분 | 여전히 빨강 (RC=1 · 결과줄 24개 · isWaterTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개) | **귀속: 모델 몫** |
| `e2e-npcsprite` | 연속 **1밤** | RC=1 · 결과줄 42개 · ★★4초 동안 **안 깜빡인다** — 9명 중 바뀐 주민 1명 (신원으로 고르기 때문) | 2분 | ★**단독 초록** (RC=0) | **귀속: 모델 몫** |
| `e2e-winter` | 연속 **1밤** | RC=1 · 결과줄 30개 · ③d 공표가 **촌장의 말**로 만들어졌다(사건 meta → briefLine)  ["요즘 생선이 달리는군.","생선은 곳간에 쌓여 썩을 지경이야."] | 1분 | ★**단독 초록** (RC=0) | **귀속: 모델 몫** |

⚠**연속 2밤 이상인데 상시 목록에 없는 것 1건** — `test-site-memo`(4밤). T304 규약대로 **PM 이 `nightly-known.txt` 에 올릴지** 정한다(이 기계는 세기만 한다).

## 3. 미측정 0종

없다 — 오늘 목록을 다 돌았다.

## 4. 아침에 세션으로 넘길 빨강

아래는 **상시 목록에 없는** 빨강이다 — 귀속 카드를 찾아 세션에 넘긴다(이 칸만 사람 몫이다).

* `test-audio` — RC=1 · 결과줄 127개
  * ⑫b ★서버 어부 자리가 `낚음` 을 세운다  hook
  * ⑫c ★★셋이 **서로 배타**다(안 물림 / 물고 놓침 / 건짐) — 한 틱에 둘이 서지 않는다  `!_sp` · `else` · 건짐
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 127개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-audio.js` 와 그 하네스가 무는 제품 파일
* `test-site-memo` — RC=1 · 결과줄 22개
  * ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  13회 / 9일
  * ① [상황] 헛수고(빈손)가 실제로 있다  빈손 0
  * ③ [전제] 실제로 건너뛰었다  스킵 0
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 22개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-site-memo.js` 와 그 하네스가 무는 제품 파일
* `test-terraincache` — RC=1 · 결과줄 24개
  * isWaterTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개
  * isWaterTileLocal: 두 가지에 **글자까지 같은 인자식** — 캐시가 다른 점을 묻지 않는다
  * isRockTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 24개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-terraincache.js` 와 그 하네스가 무는 제품 파일
* `e2e-npcsprite` — RC=1 · 결과줄 42개
  * ★★4초 동안 **안 깜빡인다** — 9명 중 바뀐 주민 1명 (신원으로 고르기 때문)
  * ★②′ 자명 통과 금지 — 한 사람의 옷을 바꿔 끼우면 같은 셈이 **한 명으로 센다**  — 2명
  * ★②′ 자명 통과 금지 — 한 사람의 옷을 바꿔 끼우면 같은 셈이 **한 명으로 센다**  — 2명
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-npcsprite.js` 와 그 하네스가 무는 제품 파일
* `e2e-winter` — RC=1 · 결과줄 30개
  * ③d 공표가 **촌장의 말**로 만들어졌다(사건 meta → briefLine)  ["요즘 생선이 달리는군.","생선은 곳간에 쌓여 썩을 지경이야."]
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-winter.js` 와 그 하네스가 무는 제품 파일

## 5. 통과 117종

```
test-alloy  test-armor-cap  test-assay  test-body  test-bridge-path  test-bridge-sprite  test-building-anchor  test-calendar  test-carrier  test-charsheet  test-chron-icons  test-claim-v2  test-client-globals  test-clothes  test-craft  test-crops-world  test-crops  test-distmatrix  test-ditch  test-doors  test-downed  test-econ-diet  test-econ-fieldyield  test-econ-rawgrain  test-emptystart  test-events  test-farm-life  test-farm-metrics  test-fish-act  test-fishing  test-frontier-iron  test-furnace  test-granary-add  test-granary-haul  test-guest-identity  test-guest-rejoin  test-guild-treasury  test-hall-door-path  test-handoff-body  test-harness-lint  test-headless-parity  test-hist  test-hunt-port  test-hunt-vis  test-icons  test-imsu  test-inv-ground  test-itemlabel  test-kcal  test-lab-alloc  test-lab-happywork  test-lab-hunt  test-lab-stone  test-lab-trees  test-lab-wood  test-ledger  test-membership  test-mining  test-move-soa  test-move  test-msy  test-newcomers  test-nightly  test-nippon-boot  test-notice  test-npc-clothes  test-onboarding  test-oremix  test-path-core  test-picker-tables  test-preserve  test-props  test-psite-server  test-reanchor  test-redeploy  test-regrow  test-relic  test-render-common  test-resource-index  test-rock-los  test-rooms  test-route-persist  test-salt  test-save-periodic  test-segindex  test-shelter  test-spoil2  test-tame  test-terr-backfill  test-terr-persist  test-terrain-memo  test-tick-slicer  test-tidal  test-trade  test-trees  test-valuechain  test-village-found  test-villager-stuck  test-war-world  test-weather  test-weight  test-winter  test-wood-act  e2e-chronicle  e2e-cold  e2e-events  e2e-hp  e2e-membership  e2e-move  e2e-mtcut  e2e-mtfoot  e2e-mtocc  e2e-rooms  e2e-rtt  e2e-thirst  e2e-village  e2e-weather
```

## 6. 귀속 — 모델 몫 (A-3 · 야간 세션이 손으로 채운 절)

`grep -n "모델 몫"` 에 걸린 하네스 5종에 대해 `git log -3 --format=%s -- scripts/<하네스>.js` 로 **하네스 파일을 마지막에 만진 커밋**만 적는다. 이것은 귀속 **후보**이지 판정이 아니다 — 제품 파일 쪽 귀속과 실제 원인은 아침 세션 몫이다. 못 쟀으면 못 쟀다고 적었다.

| 하네스 | 단독 재실행 | 하네스 파일 최근 커밋(카드) | 귀속 후보 |
| --- | --- | --- | --- |
| `test-audio` | 여전히 빨강 | `보고/T299` (문서만 · 코드 무접촉) — 그 위로 하네스 수정 없음 | **후보 없음** · 하네스는 T299 이후 무접촉 → 원인은 제품 파일 쪽. 못 쟀다 |
| `test-site-memo` | 여전히 빨강 (연속 **4밤** · 상시 밖) | `[승인 대기 · T349]`, `[승인 대기 · T342]` | **T349 · T342** (T342 가 `test-site-memo 19/0` 을 직접 건드림 · 연속 4밤이라 T342 가 더 이른 후보) |
| `test-terraincache` | 여전히 빨강 | `[승인 대기 · T344]` | **T344** |
| `e2e-npcsprite` | ★단독 초록 (RC=0) | `[승인 대기 · T349]` | **T349** — 단독 초록이므로 하네스 결함보다 **동시 실행 간섭** 쪽. 못 쟀다 |
| `e2e-winter` | ★단독 초록 (RC=0) | `[승인 대기 · T349]` | **T349** — 단독 초록 · 위와 같음. 못 쟀다 |

* 세션 번호는 위 커밋 제목에 적힌 카드 번호를 그대로 옮긴 것이다 — 야간 세션이 붙인 것이 아니다.
* `e2e-npcsprite` · `e2e-winter` 는 단독으로 초록이므로 **묶음 실행 중 간섭**이 후보다. 야간 러너는 간섭을 재지 않는다 — 못 쟀다.
* `test-audio` 는 하네스 파일이 T299(문서만) 이후 손대지 않았다 — 하네스 쪽 귀속 후보 **없음**.
