# T95 — 옷 여섯 · 계단·바닥·농지도 렌더 둘 (2026-09-05 · 세션8 ART)

지시서 `~/Mini/지시_T95.md` · 브랜치 `batch/icons-clothes-0905` · 굽는 기준 `origin/main d295fb3f`

**푸시 완료 `e06169c9`** (2026-09-05 · `batch/icons-clothes-0905` → ff → main).
굽는 기준에서 main 이 **안 움직였다** — 리베이스 없음 · 충돌 없음.
**force push 0회** · 맥 클론 **`.git/*.lock` 0개**.

옷 여섯을 굽고, **T67 이 남긴 마지막 셋**(계단·바닥·농지)에 캐논을 적용했다.
`36-r2-building.js` 에서 벡터 도형이 **208줄 빠지고 44줄 들어왔다**.

---

## §0-ⓐ 옷 아이콘 키 — 갈래가 아니라 **재질**이다

| 잰 것 | 결과 |
|---|---|
| 지금 무엇으로 그리나 | `itemPic(EQUIP_ICONS[inst.type])` = **`'clothes'` 한 키** — 여섯 재질이 다 같은 그림 |
| 그 키에 파일이 있나 | **없다** → 여섯 다 **점선 빈 칸**이었다 |
| 재질은 어디 있나 | 인스턴스의 `inst.mat` · 조합/구매 줄은 선택 재료 `sel` |
| 서버 정본 | `server/clothes.js CLOTHES` — `fur·ramie·leather·hide·fiber·hemp` 여섯 |

⇒ 키를 **`clothes_<mat>`** 으로 내렸다. 재료 아이콘(`hide`·`fiber` …)을 재활용하면 안 된다 —
**가죽과 가죽옷은 다른 물건**이고, 그 키엔 이미 원물 그림이 있다.

`equipPicKey(type, mat)` 를 `43-i-icon.js`(그림 함수가 사는 곳)에 두고 `50-i-panel.js` 의 **네 자리**가
그걸 부른다. 갈래 아이콘은 무기·도구처럼 **재질이 그림을 안 바꾸는** 것에만 남는다.

## §0-ⓑ 계단·바닥·농지 — 셋이 서로 다른 문제였다

| 물건 | 종전 | 실측 | T95 |
|---|---|---|---|
| **농지** | 흙 다이아 + 초록 막대 + 빨간 베리(폴백) | **몸은 이미 스프라이트**다(T79c 8군 × 4단계). 벡터는 대역 | 대역을 **점선 빈 칸**으로. 아이콘만 새로 — `fields_render.py` 가 **같은 `soil_bed`** 로 |
| **바닥** | 셀 꽉 채운 단색 다이아 + **층별 색**(DEBUG) | 1×1칸 · `BUILDING_HEIGHT 4px` | 스프라이트 1장. 층 단서는 **테두리 한 줄**로 남겼다(정보는 지키고 몸은 하나) |
| **계단** | **24 소단을 프레임마다 벡터로**(슬랩마다 네 귀 + 챌판) · 146줄 | 3칸 · 소단 24(칸당 8) · 깊이 0.125m · 폭 1칸 · 0→2m · `dir ∈ {N,E,S,W}` | 그 수치 그대로 모델로. **변형 넷** |

★계단이 **둘이 아니라 넷**인 이유: 벽·문은 `N`/`E` 둘로 족했지만(대칭) 계단은 **오르는 쪽이 반대**라
`N` 과 `S` 가 다른 그림이다.

★농지를 `props_render.py` 에 또 만들지 않았다 — 빈 밭의 세계 스프라이트는 T79c `crops/grain_0` 이고,
여기 또 만들면 **사본**이다. 그래서 아이콘을 `fields_render.py` 가 같은 모델로 굽는다.
`test-props ④` 도 그 예외를 이름 붙여 안다(농지의 몸은 `drawPropBody` 가 아니라 `cropSprite`).

## §0-ⓒ 옷 색 — **시트가 정본**

`char_render.py` 의 `CLOTH_MATS` 여섯(색·거칠기)과 상수 둘(`TRIM_K 0.85`·`PLACKET_K 0.56`)을
그대로 옮겼다(출처 주석 동반). 짐 창의 갖옷과 몸에 걸친 갖옷이 **같은 물건**으로 읽혀야 한다 —
색이 갈리면 그 순간 두 물건이 된다.

⚠**재질이 두 파일에 있다**(이중화 · 공용 모듈 편입은 회부). 그때까지 `test-icons ⑨` 가 두 표를
**값으로 견준다** — 한쪽만 고치면 빨개진다. 돌연변이로 확인했다(`ramie` 색 한 자리를 어긋내니 잡았다).

## 옷 모델 — 개어 놓은 한 벌 · 3패스

시트는 **몸에 걸친 옷**을 그린다. 짐 창의 옷은 **물건**이라 개켜 쌓인 꼴이라야 한다.

| 패스 | 읽힌 것 | 원인·고침 |
|---|---|---|
| 1 | **나무 궤짝** | `box` 로 쌓고 끈을 위에 얹었다 — 각진 모서리 + 손잡이처럼 뜬 막대 |
| 2 | **알·번데기** | 이코스피어로 누르니 이번엔 돔이 됐다 |
| 3 ✓ | **갠 천** | 납작한 층을 **어긋나게** 포개고, 앞·옆 모서리마다 **접힌 말이**(가는 원통)를 얹었다. 그 말이가 "각진 궤짝"과 "둥근 알" 사이의 답이다 |

여섯을 가르는 것: 색(시트 정본) · **털**(갖옷 — 윗면 전체로 곤두선다) · **엮은 올**(풀옷) ·
**윤**(무두질 가죽) · 두께(갖옷 0.085 vs 모시 0.040).

## 굽기 · 배포

| 것 | 값 |
|---|---|
| 옷 6 아이콘 | 23초 |
| 시설 — 세계 5(바닥 1 + 계단 4) + 아이콘 2 | 50초 |
| 농지 아이콘(`fields_render.py`) | 6초 |
| 배포 | 아이콘 **144장**(138 + 옷 6 · `item_*` 셋은 새 모델로 덮음) · props **19장**(14 + 5) · 앵커 19키 |
| 잠금 | `icons.lock.json` — icons 신규 6·변경 3(`item_farmland/floor/stair` — 새 모델이니 당연) · props 신규 5·변경 0 · crops 변경 0 |

앵커 계약(`test-props ③`) — `floor` zmax 4.34 ≒ body 4 · `stair_*` zmax 64.96 ≒ body 64 (둘 다 ±1 안).

## 하네스

* **`test-icons ⑨` 신설** — 옷 여섯의 색·거칠기가 **시트와 같은가**(두 파일을 파싱해 값으로 견준다) ·
  상수 둘도 같은가 · 키가 `server/clothes.js` 여섯과 같은가 · 파일 여섯 · 배선(`equipPicKey`,
  `50-i-panel` 에 갈래 아이콘 직접 호출 0).
* **`test-props ④` 를 셋으로 넓혔다** — `farmland`·`floor`·`stair` 가 들어왔다. 예외는 **이름을 붙였다**:
  농지 3(마을 시뮬 카펫 타일 2 + 그림자 1) · 바닥 4+3(움집 실내 거적·화덕 — **바닥이 아니라 실내 가구**).
* **박아 둔 수를 걷어냈다** — `아이콘 8종`·`가구 8종` 이 하드코딩이라 물건이 늘자 **검사기가 먼저
  거짓말했다**. 이제 `props_render.py PROPS` 의 항목 수에서 유도한다.

돌연변이 2건 검출: 시트 색 한 자리를 어긋내니 ⑨ 가 잡았고, 계단 절을 도형으로 되돌리니
`test-props ④` 가 잡았다(`fill 1/0 · fillRect 1/0`).

## 검증 (공통 §2 · 통합 러너 전수 없음)

| 하네스 | 결과 |
|---|---|
| `test-icons`(⑨ 신설) · `test-props`(④ 확장) | **PASS** · 돌연변이 2건 검출 · `test-props --selftest` PASS |
| `test-crops-world` · `test-render-common` · `test-crops` · `test-itemlabel` | **PASS** |
| `test-client-globals` · `test-harness-lint` · `test-chron-icons` | **PASS** |
| `e2e-inv` | **94 통과 / 0 실패** |
| `e2e-rooms` | **66 통과 / 0 실패** |

* 게임 코드 diff — `36-r2-building.js` **−184/+24**(계단 146줄 → 8줄이 큰 몫) ·
  `43-i-icon.js` +12(`equipPicKey` + 목록) · `50-i-panel.js` 4자리. `server/`·`sim/` **무접촉**.
* 랩 로드 18파일 교집합 **∅**. T93(세션2 `37-r1-weather`·`34-m-renderloop`) · T90(세션4 `05-u-icon`·`44-h-hud`) 무접촉.

## 푸시 위생

* **force push 0회** — 모든 푸시가 `merge-base --is-ancestor` 로 fast-forward 를 먼저 확인했다.
* 맥 클론 **`.git/*.lock` 0개**(푸시 뒤 확인) · `tmp_*` 팩은 `~/Mini/_to_delete/` 로 격리.
* 맥 클론에 인덱스를 쓰는 git 명령은 쓰지 않았다(`fetch`·`push`·`log`·`rev-parse`·`merge-base` 만).

## 회부

1. **[ART] 옷 재질 이중화** — `char_render.py CLOTH_MATS` 와 `props_render.py _CL` 이 같은 값을 두 벌
   갖는다. 공용 모듈(`render_common`)로 올리면 한 벌이 된다. 그때까지 `test-icons ⑨` 가 견딘다.
2. **[ART/설계] 마을 시뮬 경작지 카펫 타일** — `farmland` 절에 남은 벡터 층이다(T6 이 의도적으로 남겼다:
   "셀을 꽉 채워 띠가 이어져 보이게"). 8군 세트로 덮을지, 띠 렌더를 유지할지 **설계 판정**.
3. **[ART] 움집 실내(거적 침상·화덕)** — `floor` 절에 남은 벡터다. **바닥이 아니라 실내 가구**라
   물건 하나 = 렌더 둘의 대상이고, 침상·화덕을 props 로 올리는 카드가 따로 필요하다.
4. **[ART] nature/building/char 5.0.1 재굽기** — 각자 카드(굽는 기계 정본 · `인계/ART-자산.md` §13).
5. **[설계] 종별 34세트**(T79c 회부 2) · **[ART] 상추/아욱 26px**(T79b 회부 3 · 재민).
6. **[ART] 지게 등짐** — 아이콘은 T72 에 있다(`carrier`). 등에 진 모습은 T81 시트 쪽이다.

## 실기 (재민)

`산그림/디자인B/아이콘4차_옷_시설.png`

1. **짐 창의 갖옷 아이콘과 입은 모습이 같은 물건으로 보이는가** (윗줄 = 시트 · 아랫줄 = 아이콘).
2. **계단·바닥이 세계와 짐에서 같은 그림인가** (아래 표 — 아이콘 / 세계 나란히).
