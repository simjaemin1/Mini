# T417 — 동물 40 · 헤드룸 조합을 실측에서 · 제작 완료 한 줄 (세션9 · 2026-09-26)

## 결론 (머리 15줄)

1. **한 줄 답** — 동물 40자리 중 **7이 운다**(새로 6 · 이미 1) · 곰·꿩·메추라기 울음과 짐승 맞음·죽음·길들임을 이었고, 헤드룸 최악 조합은 이제 **진짜 한 판(30분)에서 실제로 겹친 묶음**이며(`axe`+`wind` **0.4213 · −7.51 dB** · 문턱 0.8 안), 제작·보존을 **받으면** `craft_done` 이 운다(서버 한 줄).
2. **가지 해시** `____` (가지 `batch/sfx-animals-0926`) · **`[승인 대기 · T417]`** · **베이스** `057d40c1`.
3. ★**반경 0 결함 셋을 찾았다**: `axe`·`harvest`(`resourceHit` 이 자원 자리로 부른다 — 주민 벌목이 존 어디서든 귓가에서 최대 볼륨) · `downed`(T402 가 남의 쓰러짐에 걸면서 자리 있는 소리가 됐는데 반경 0 — **내 실수**). 반경을 줬다(320·320·384). 자기 소리(자리 없이 부름)는 그대로. `test-audio ⑱a` 가 자리로 부르는 표의 키 29개에 반경 0 을 막는다(미끼: 종전 `axe` 를 먹이면 그 하나를 잡는다).
4. 동물 확보: 곰 `bear_growl`(NPS PD · 2번 후보) · 꿩 `pheasant_call`(PD) · 메추라기 `quail_call`(CC BY 3.0 — 셋째 CC-BY) · 아이벡스 대체 `goat_bleat`(PD · **후보만**). 무스·여우 = **없음**(PD/CC0/CC-BY 녹음 없음 — 전부 BY-SA) → 녹음 회부. 북극토끼 = 거의 소리를 안 내는 짐승(무음이 맞다).
5. ★종 범위: 뜬 존(새벌 = forest · 닛폰 = mountain)에서 **실제로 나는 종**만 확보했다(늑대·곰·무스·사슴·멧돼지·여우·꿩·메추라기·호랑이·아이벡스 + wildlife 북극토끼). 나머지 25종은 그 바이옴 존이 뜰 때 → ⓒ.
6. 짐승 사건(새 파일 0 · 이웃 키): 맞음 `mob_damaged` → `hit_body`(**직전 hp 보다 줄 때만** — 먹여 회복 `tryFeed` 도 같은 메시지) · 죽음 `corpse_added` → `downed` · 길들임 `mob_tamed` → 그 종 울음.
7. ★**헤드룸 실측**(★PM): `scripts/sfx-cooccur.js` 신설(러너 밖) — 진짜 central·zone·클라 한 판 30분, 나무를 찾아가 베며 200ms 창마다 소리 층 장부(`__sfx.tap` 읽기 전용)를 긁었다. 창 **8998** · 서로 다른 묶음 **4** · 울림 62 · 최악 `axe`+`wind` **0.4213**(39창) → `_실측.worstCombo` · `test-audio ⑧k` 가 이 칸을 읽고, 비면 빨갛다. 손 조합은 `handCombo` 로 **대조**만.
8. ★**실측은 얇다 — 정직하게**: 헤드리스에선 캐릭터 시트가 안 떠 **발자국이 0**이고, 스폰 마을 곁에서 30분 동안 주민 사건이 오지 않았다. 한 판이 잰 것은 "혼자 나무 베는 사람 + 날씨"다. 손으로 겹친 표(`handCombo` 0.7967 · 숲 짐승 한 판 1.0407)는 **대조 자**로 남겼고, 짐승 한 판은 **문턱 밖**이다 — 그런 겹침이 실제로 오는지는 이 자로 아직 못 봤다(회부).
9. 자의 착시 하나를 잡았다: 첫 30분 판이 반복 소리를 **긁은 창에만** 적어서, 긁기 사이 단발이 "반복 없이 혼자"로 세졌다(`axe` 단독 0.4304 두 창). 반복을 지난 긁기 이후 모든 창에 주게 고쳐 **다시 30분** 돌렸다.
10. 제작 완료 한 줄: `doCraftCollect`(받은 게 있을 때만 닿는 자리)의 `sendInventory(player)` → `sendInventory(player, 'craft')`. 보존·요리도 같은 수령 자리라 **한 줄로 끝**. 맡김(`_enqueueCraft`)·"아직 만드는 중" 무변. 서버에 취소 길은 없다.
11. 하네스: `test-audio` 170→**180/0** · `e2e-audio-probe` 71→**76** · `e2e-aim` **15/0** · `e2e-pid-collide` 10→**13/0**(p2 자리 한 줄 — `downed` 반경) · globals 26 · lint 61 · assets-audit PASS · lock 592/0 · 소리판 행 = 키 63.
12. 빠진 소리 전수 **171자리 · 있음 58**(T412 51 · +7: `mob_damaged`·`mob_tamed`·`corpse_added`·곰·꿩·메추라기·`where:craft` · 자리 +1 은 새 낱말 `craft`).
13. 기존 58키 중 **셋이 바뀌었다**(`axe`·`harvest`·`downed` 반경 — 결함 수리) · 나머지 55 비트 동일 · 음원 파일 무변.
14. 실기: 재민 배포 뒤 소리판(새 행 5) + 곰 곁 · 짐승 치기 · 제작 받기.

---

## ⓐ 자리 표 40

판정: **ⓐ 운다(이 카드)** · **ⓐ′ 이미 운다** · **ⓑ 상태 동기화** · **ⓒ 애매/범위 밖**. 착시 검사(T412 ⑧): 갈래 표의 "건물 `wolf`" 는 건물이 아니라 몹 타입(`villages.js` · 갈래 표가 이미 적어 둠) — 몹으로 본다. 나머지 39는 진짜 메시지·종 이름이다.

| # | 자리 | 코드가 말하는 것 | 판정 | 키(이웃) |
|---|---|---|---|---|
| 1 | `corpse_added` | `spawnCorpse` 한 자리 — 짐승이 죽어 사체가 생김(사람 사냥 · 화살 · 주민) | **ⓐ** | `downed`(이웃) |
| 2 | `corpse_removed` | `butcherCorpse` 도살 **또는** 30초 청소의 썩음 — 한 꼴 | ⓒ(가를 칸 없음) | |
| 3 | `corpses_init` | 입장 때 목록 | ⓑ | |
| 4 | `mob_damaged` | `tryAttack`·`stepArrows`(맞음) · `npcStep`(주민이 침) · **`tryFeed`(먹여 회복 · hp 오름)** | **ⓐ**(hp 가 줄 때만) | `hit_body`(이웃) |
| 5 | `mob_removed` | 죽음·사라짐·청크 내림·wildlife 정리 — 한 꼴 | ⓑ(죽음은 #1 이 운다) | |
| 6 | `mob_spawn` | 스폰·시야 들어옴 | ⓑ | |
| 7 | `mob_tamed` | `tryFeed` 길들이기 성공 | **ⓐ** | 그 종 울음(`mobs` 표) |
| 8 | 건물 `wolf` | **착시** — 몹 타입(`wolf_howl` 이 이미 운다) | ⓐ′ | `wolf_howl` |
| 9 | `bear` | forest·taiga·mountain — 새벌·닛폰에 난다 | **ⓐ** | `bear_growl` 새 |
| 10 | `moose` | forest·taiga — 새벌에 난다 | ⓐ 자리 · **없음**(녹음 회부) | |
| 11 | `red_fox` | forest·plains — 새벌 | ⓐ 자리 · **없음**(녹음 회부) | |
| 12 | `pheasant` | forest·plains — 새벌 | **ⓐ** | `pheasant_call` 새 |
| 13 | `quail` | forest·plains — 새벌 | **ⓐ** | `quail_call` 새 |
| 14 | `ibex` | mountain — 닛폰 | ⓐ 자리 · 대체 후보만(`goat_bleat` · 판정 재민) | |
| 15 | `arctic_hare` | wildlife 🐇 · taiga·tundra·plains | ⓑ — 거의 울지 않는 짐승(무음이 맞다) | |
| 16~40 | `arctic_fox`·`bee`·`camel_domestic`·`chicken`·`cow`·`crocodile`·`duck`·`elephant`·`giraffe`·`goat`·`hippo`·`horse`·`hyena`·`jackal`·`leopard`·`lion`·`llama`·`pig`·`reindeer_domestic`·`reindeer_wild`·`sheep`·`silkworm`·`wild_camel`·`wolverine`·`yak` | 뜬 두 존(forest·mountain)의 스폰 표(`huntableInBiome`)에 없고 가축은 스폰 길이 없다(바다 존 대체 양만) | ⓒ 범위 밖(그 존이 뜰 때) | |

셈(40): **운다 7** = 이 카드 6(#1 `corpse_added` · #4 `mob_damaged` · #7 `mob_tamed` · #9 곰 · #12 꿩 · #13 메추라기) + 이미 1(#8 늑대 — 호랑이·멧돼지·사슴은 T354·T358 이 이었고 갈래 표에 없다) · **ⓐ 자리지만 소리 없음 3**(#10 무스 · #11 여우 · #14 아이벡스) · **ⓑ 4**(#3·5·6·15) · **ⓒ 26**(#2 + 범위 밖 25종).

## ⓑ 확보 표

라이선스는 파일마다 Commons 파일 페이지(API `LicenseShortName`)에서 읽었다. 원본 맥 `~/Mini/_sfx_in/_T417/`. 가공: 구간 → 피크 −1.5 dBFS → Vorbis q4 / AAC 96k · 모노.

| 키 | 배선 | 원본 | 출처 · 라이선스 | 구간 | 길이 | 피크 ogg/m4a | 메모 |
|---|---|---|---|---|---:|---|---|
| `bear_growl` | ○ `mobs.bear` | Yellowstone sound library — Grizzly Bears Roar 001 | NPS & MSU Acoustic Atlas · **PD** | 2.067~4.250 | 2.18s | −1.42/−1.59 | 원본이 작아 **+10.8dB**(바닥 잡음도 오름) |
| `bear_growl_b` | 후보 | … Grizzly Bear vocalizations 001 | 〃 · **PD** | 22.750~22.999 | 0.25s | −1.83/−2.38 | 콧김 |
| `pheasant_call` | ○ `mobs.pheasant` | Phasianus colchicus.ogg | 작가 미상 · **PD** | 통째 | 1.00s | −1.20/−1.50 | 1번뿐 |
| `quail_call` | ○ `mobs.quail` | Kohoutek-krepelky.ogg | Martin Hajda · **CC BY 3.0**(표시) | 2.174~3.236 | 1.06s | −0.95/−1.38 | 1번뿐 · CREDITS §1 |
| `goat_bleat` | 후보(배선 0) | Herd of goats bleating.ogg | stephan · **PD** | 2.950~3.950 | 1.00s | −1.51/−1.50 | 아이벡스 대체 판정용 |
| (무스) | **없음** | Commons `Moose mate.ogg`(PD)는 받았으나 잡음 바닥 −14 dB(비디오 녹음 · 신호 대 잡음 ~8 dB) — 귀 없이 가를 수 없어 뺐다 | | | | | 녹음 회부 |
| (여우) | **없음** | Commons 여우 5장 전부 CC BY-SA(1장은 "Copyrighted free use" — 집 규칙 밖) | | | | | 녹음 회부 |

짐승 사건은 새 파일 0(이웃 키 `hit_body`·`downed`·종 울음).

## ⓒ 실측 조합 표 + 반례

`node scripts/sfx-cooccur.js --minutes 30`(시드 1020 · 480×320 · 나무 찾아 E) — 반복은 지난 긁기 이후 모든 창에 준다(9번 착시 고침 판).

| 묶음 | 창 | 단발 | 리미터 없이 | dB | 끼고 | 무릎 넘음 |
|---|---:|---:|---:|---:|---:|---|
| **`axe` + `wind`** | 39 | 1 | **0.4213** | −7.51 | 0.4213 | 0/400 |
| `axe` + `rain_light` + `wind` | 23 | 1 | 0.4206 | −7.52 | 0.4206 | 0/400 |
| `wind` | 8043 | 0 | 0.2402 | −12.39 | 0.2402 | 0/400 |
| `rain_light` + `wind` | 893 | 0 | 0.2346 | −12.59 | 0.2346 | 0/400 |

`_실측`: `worstCombo ['axe','wind']` · `worstPeak 0.4213` · `worstSource {tool, minutes 30, seed 1020, windows 8998, distinct 4, plays 62, rows, _한계}`.

**대조(손 조합 · 종전 자)**: `handCombo`(T412 11키) 0.7967 · `handBeasts`(숲 짐승 한 판 11키) **1.0407**(리미터 22/400 · 끼고 0.7649 · 클리핑 0) · 소리 키 63개 전부 한꺼번에 1.5001(클리핑 456 — 오지 않는 순간 · 상한 참고).

**반례(실측이 손보다 작다 = 자가 이긴 게 아니다)**: 실측 최악 0.4213 은 손 조합 0.7967 의 절반 — "한 사람 + 날씨"만 잰 탓이다. 한계 셋: ① 헤드리스에서 `__charMeta` 가 안 떠 **발자국 0** ② 스폰 마을 곁 30분 동안 주민·짐승 사건 0 ③ 8분쯤 **울림 62 에서 멈춤**(E 는 계속 누르는데 도끼가 안 운다 — 짐 25kg 무게로 벌목이 막힌 듯 · 막힌 나무 건너뛰기를 넣어도 같다). 그래서 `worstCombo` 는 **실제로 본 것 중 최악**이지 **올 수 있는 최악**이 아니다 — `handBeasts` 를 `test-audio ⑧k3` 가 대조로 들고 있다(회부 1).

첫 판(착시): `axe` 단독 0.4304(2창) — 반복을 긁은 창에만 적어 단발이 혼자로 세졌다. 고친 뒤 사라짐(위 표).

## ⓓ 제작 완료 한 줄 + e2e

```
-  sendInventory(player);
+  sendInventory(player, 'craft');   // ★[T417] 받았다(끝난 것이 하나라도 있을 때만 여기 온다)
```
자리: `doCraftCollect` — `Facility.collect` 가 빈 배열이면 위에서 `return`(“아직 만드는 중”/“받을 게 없다”), 그러니 이 줄에 닿으면 **받은 것**이다. 보존(`kind:'preserve'`)·요리(`dish`)·도구·장비 모두 같은 수령 자리 → 한 줄. 맡김(`_enqueueCraft` → `sendCraftQueue`)은 낱말 없음. 서버에 취소 함수는 없다(`craft_cancel` 0).
층: `inventoryWhere.craft → craft_done`(T412 확보 키). e2e 51: `where:craft` **1** · 낱말 없는 인벤·줄 갱신 **0**. `test-audio ⑱d`: `'craft'` 는 서버에 딱 한 번 · `doCraftCollect` 안 · `_enqueueCraft` 에는 없다.

---

## §3 검증

| 자 | 전 | 후 |
|---|---:|---:|
| `test-audio` | 170 | **180/0** (⑧k·⑧k2·⑧k3 실측 자 · ⑱a·⑱a2 반경 · ⑱b 종 · ⑱c·⑱c2 hp 줄 때만 · ⑱d·⑱d2 `craft`) |
| `e2e-audio-probe` | 71 | **76/0** (㊽ 맞음 1/회복 0/모름 0 · ㊾ 길들임 1 · ㊿ 죽음 1 · 51 `craft` 1/낱말 없음 0 · 52 꿩·메추라기 소리판 ≥1 · 무스 0) |
| `e2e-pid-collide` | 13 | **13/0** (⑥a p2 자리 (40,0)→(520,500) — `downed` 반경 384 · 멀면 안 들리는 게 맞다) |
| `e2e-aim` | 15 | **15/0** |
| `test-client-globals` · `test-harness-lint` · `test-assets-audit` | 26 · 61 · PASS | 26 · 61 · PASS |
| lock | 582 | **592/0**(+10 · 새 음원 5 × ogg·m4a) |
| `sfx-census` | 170 · 51 | **171 · 58** |

`sfx-cooccur` 는 러너 밖(30분 · 결과 `/tmp/sfx-cooccur.json` → 표에 옮김). `Math.random` 0 · `bgm/` 무접촉 · `30-n-net.js` 무접촉 · 서버 1줄(`zone.js` `doCraftCollect`).

**접점** `animals.js` · `mobs` · `mobEvents` · `mob_damaged` · `mob_tamed` · `corpse_added` · `mob_spawn` · `wildlife` · `48-a-audio` · `__sfx.tap` · `sendInventory` · `doCraftCollect` · `craft_queue` · `craft_done` · `worstCombo` · `handCombo` · `worstSource` · `sfx-cooccur` · `sfx-board` · `CREDITS` · `_sfx_in` · `sfx-census` · `radius`.

## §4 회부

1. **[PM] 실측 한 판이 얇다** — 한 사람·한 마을·헤드리스(발자국 0). 짐승 떼·주민 일터 곁의 겹침은 못 봤다. 손 조합 "숲 짐승 한 판"(곰·늑대·호랑이·멧돼지·사슴·꿩·메추라기·맞음·쓰러짐·새·바람)은 없이 **1.0407**(리미터가 22/400 창에서 문다 · 끼고 0.7649 · 클리핑 0). 그런 순간이 실제로 오는지 재려면 대본이 짐승 곁으로 가야 한다(`sfx-cooccur` 에 목적지 한 칸) — 다음 카드 거리.
2. **[재민 폰] 녹음** — 무스·여우 울음(+ 종전: `dig` 2번 · 깨어나는 숨 · 눈).
3. **[재민] 아이벡스** — 염소 울음(`goat_bleat`)을 걸지 판정.
4. **[재민] CC-BY 셋째**(`quail_call`) — `/크레딧` #14ⓒ 에 묶인다.
5. **[세션?] 헤드리스 캐릭터 시트** — `__charMeta` 가 안 떠 발자국 훅이 안 불린다(실측 자가 발자국을 못 센다).
6. #60 둘째 귀 판정 · 주민 수확 · UI/알림 21 · 천둥 — 그대로.
