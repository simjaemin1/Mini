# 보고 — T49 하네스 위생 일괄

| 커밋 | 내용 | 검증 한 줄 |
|---|---|---|
| `14cf63b` | 27개 등재 · 씨앗 독립 · 절대 문턱 → 비율의 비율 · 러너 선행 점검 · split-verify 중립 | 신규 27개 러너 안에서 전원 초록 · 사보타주 3종이 각각 빨감 |
| `f39e048` | `test-site-memo` 에 **자기 크기의 씨앗**(150초 성장 · tag `sitememo`) | 5/9 → **14/0**(빈손 98 · 감사 15,844회) |
| `709ab72` | 잡음이 크면 **판정하지 않는다** — `e2e-rtt` ②a·③ · `test-tick-slicer` ③·⑥ | 정상 rc=0 · 강제 유보 rc=0 · 사보타주 rc=1 |

**게임 코드 diff 0** — `git diff --name-only <베이스>..HEAD -- server/ sim/ public/` 결과 **0줄**.
(카드의 합격 조건이자 이 배치의 유일한 절대 제약이었다. 예외 없었다.)

---

## 0. 등록 후 러너 개수 — **확정치 92**

```
  디스크의 test-*.js · e2e-*.js        96개
  러너 밖에 남긴 것                     4개
      test-lab-market.js  ┐ 레포 밖 `~/Mini/전쟁실험실.html` 을 연다.
      test-lab-mining.js  ├ 컨테이너·CI 에선 영원히 못 돈다 — PM 판정 "러너 밖 유지".
      test-lab-psite.js   ┘
      test-psite-server.js  제품 결함으로 빨감 — 세션3 T48 이 제품 쪽을 수리한 뒤 등재.
  ─────────────────────────────────────
  러너 자동 발견                      ★92개   (`[자동 발견] 92개 (표 // @regress 기준)`)
```

★**세는 방법을 두 번 확인했다.** `grep -l "@regress" scripts/*.js` 는 **94**를 내는데,
그중 둘(`rumor-metrics.js`·`t9-overlap.js`)은 표를 **문장 안에서 언급**할 뿐이고
러너의 자는 `grep -l '^// @regress' scripts/{test,e2e}-*.js` 다. ⇒ 러너가 실제로 도는 것은 92.

### README 상태 블록 — **고쳤다가 리베이스에서 물렸다. 상류가 더 낫다**

카드가 "README 상태 블록도"라고 했기에 `러너 51개` → `회귀 러너 92개` 로 고쳤다. 그런데
푸시 직전 리베이스에서 상류(`0c84e837` README 재작성)와 부딪혔고, 상류는 **수를 아예 뺐다**:

```
  내 것    ├── scripts/   # 회귀 러너 **92개**(`// @regress` 자동 발견) + 측정·배포 하네스
  상류     ├── scripts/   # 하네스(`// @regress` 자동 발견) · 계측기 · 빌드(지형·작물) · 배포
```

⇒ **상류를 그대로 받았다.** 아래 §0 이 말하는 병(손으로 유지되는 수는 반드시 어긋난다)의
올바른 처방은 수를 고치는 게 아니라 **수를 안 적는 것**이다. 내 고침은 다음 등재에서 또 틀렸을 것이다.
확정치 92 는 **날짜가 붙은 기록**으로 남긴다 — 이 보고서와 `인계/공통.md` 의 T49 절.

⚠**51 은 쓴 그 순간부터 틀렸다 — 그리고 쓴 사람은 나다.** 그 줄이 들어온 커밋(`99a35cf`,
부하 실측 배치의 "README 현행화")에서 **실제 등록은 56개**였다. 세어 봤다:

```bash
  for f in $(git ls-tree -r --name-only 99a35cf scripts/ | grep -E 'scripts/(test|e2e)-.*\.js$'); do
    git show "99a35cf:$f" | grep -q '^// @regress' && n=$((n+1)); done      # → 56
```

같은 커밋의 `인계/공통.md` 는 또 **"자동 발견 49개"**라고 적고 있었다(전환 시점의 수를 그대로 둔 것).
⇒ **한 레포가 같은 날 51·56·49 세 수를 말하고 있었다.** 손으로 유지되는 수는 반드시 어긋난다.
지금 수는 러너 출력(`[자동 발견] 92개`)에서 그대로 옮겼고, 세는 명령도 위에 적어 뒀다.

---

## 1. 등재 27개 — 넣고 나서 **한 번도 안 빨갰다**

PM 승인대로 T9 전수표의 통과 27개를 넣었다. 각 파일 머리에 `// @regress` 한 줄뿐이다(diff `1 +`).

```
  test-alloy  test-armor-cap  test-assay  test-bridge-path  test-bridge-sprite
  test-building-anchor  test-distmatrix  test-ditch  test-frontier-iron  test-furnace
  test-granary-add  test-granary-haul  test-guild-treasury  test-hall-door-path
  test-headless-parity  test-mining  test-oremix  test-picker-tables  test-reanchor
  test-relic  test-rock-los  test-rooms  test-tame  test-terr-backfill
  test-valuechain  test-village-found  test-villager-stuck
```

★**넣자마자 15개가 빨갰다 — 그리고 그건 하네스 잘못이 아니었다.**
러너는 `rc=0` 인데 `✗ RC=0 · 결과줄 0개 → 실패로 센다` 를 냈다. 원인은 T9 때 내가 이미 한 번
틀린 것과 **같은 자리**다: 러너의 요약 문법(`통과` / `PASS <숫자>`)은 **이 러너가 길러 온**
하네스의 말투일 뿐이고, 러너 밖에서 자란 15개는 `결과: PASS` 로 끝난다.

⇒ 그때는 **내 집계 스크립트**를 고쳤고, 이번엔 고칠 자리가 **러너**였다.
15개 하네스의 출력을 러너 말투로 바꾸는 길도 있었지만 그건 **하네스 15개를 고치는 일**이고,
러너가 좁은 것이 원인이므로 좁은 쪽을 넓혔다.

```bash
  marks="$(echo "$out" | grep -cE '^[[:space:]]*(✓|✔|✗|✘)')"     # ★1차 근거 = 검사 표시의 존재
  words="$(echo "$out" | grep -cE '통과|PASS|FAIL|실패|결과')"
  summary=$(( marks + words ))
  if [ "$rc" -ne 0 ] || [ "$summary" -eq 0 ]; then …실패…
```

"결과줄 0개 = 크래시 = 실패"는 **그대로 남겼다.** 넓힌 것은 무엇을 결과줄로 보느냐이지,
조용한 크래시를 봐주는 게 아니다.

---

## 2. `test-site-memo` — 앞 하네스가 만든 것에 기대고 있었다

### 증상

신선한 러너에서 **영원히 못 통과했다**: `✗ 씨앗 DB 없음 — test-tick-slicer 를 먼저 한 번 돌려라`.
러너는 **이름순**으로 도는데 `site-memo` 가 `tick-slicer` 보다 앞이다. 순서를 규약으로 삼는 순간
그 규약은 이름 하나로 깨진다. 족보 ㊾("앞 절이 상태를 남기면 뒤 절이 깨진다")의 **러너판**이다.

### 고친 길 — 권장안(자기 픽스처로 독립)

시딩 코드를 양쪽에 복사하면 그게 사본이다(이 레포의 제1 금기). 그래서 **한 곳**에 뒀다:

```
  scripts/slicer-seed.js       씨앗 정본 — 경로 상수 · ensureSeed({tag, growMs})
  test-tick-slicer.js  ─┐
  test-site-memo.js    ─┴─ 둘 다 이 함수를 부른다. 없으면 자기가 만든다. 순서 무관.
```

### 씨앗을 독립시켰더니 **여전히 빨갰다** — 그리고 그게 옳았다

```
  ✗ ① [상황] 헛수고(빈손)가 실제로 있다      빈손 0
  ✗ ② [전제] 감사가 실제로 돌았다              0회
  ✗ ③ [전제] 실제로 건너뛰었다                  스킵 0
```

이건 하네스가 **자기 상황 전제를 스스로 거절한 것**이다 — "헛수고를 줄였다"를 재려면
헛수고가 **있는 세계**여야 하는데, 기본 씨앗(20초 성장)은 마을이 너무 어려서 집터가 남아돈다.
⇒ 판정을 무르게 하는 대신 **씨앗을 그 하네스 크기로** 줬다(tag `sitememo` · 150초 성장).

```
  전  5 통과 / 9 실패     빈손 0 · 감사 0회 · 스킵 0
  후  14 통과 / 0 실패    빈손 98 · 감사 15,844회 · 훑은 셀 110,400 → 47,847
```

★**이 자리가 T49 전체에서 제일 중요한 한 수다.** 빨간 하네스를 초록으로 만드는 길은 둘인데
(전제를 무르게 한다 / 전제가 성립하는 세계를 준다) 카드가 금지한 건 앞의 길이다.
그리고 뒤의 길을 택하자 **하네스가 원래 재려던 것을 실제로 쟀다**(47,847 vs 110,400 = 57% 감소).

---

## 3. 절대 문턱 셋 → 비율의 비율 — **처방과 사보타주**

카드가 지목한 셋이다. 처방은 족보 ⑻⓪(waterperf 판): **같은 실행 안에서 같은 조건을 두 번**
재서 잡음 바닥을 먼저 얻고, 효과비를 그 잡음비로 나눈다. 절대 ms/px 는 **참고 출력**으로 남긴다.

| 하네스 | 종전 | 지금 | 사보타주 스위치 |
|---|---|---|---|
| `test-tick-slicer` ③⑥ | 한 프레임 최대 **< 절대 ms** | `base` vs `base2` 잡음비 → `SLICER_RR_K=1.8` | `SLICER_SABOTAGE=1` (조각내기 끈을 뽑는다) |
| `e2e-rtt` ②a③ | 막힘 **< 절대 ms** | 같은 짝 → `RTT_FRAME_K=1.4` · `RTT_LOOP_K=1.6` | `RTT_SABOTAGE=1` |
| `e2e-weight` ④⑤ | 이동 거리 **< 절대 px** | 가벼운 걷기 두 번 → 잡음 바닥 · 문턱은 **서버 배율에서 유도** | `WEIGHT_SABOTAGE=1` |

### 사보타주가 실제로 빨갰다 — 셋 다

```
  test-tick-slicer  ✗ ③ 한 프레임 최대가 줄었다 — 비율의 비율 0.96 > 1.8   (효과비 1.00)
  e2e-rtt           ✗ ②a 한 프레임 막힘이 줄었다 — 비율의 비율 0.70 > 1.4  (효과비 0.89)
  e2e-weight        ✗ ④ 실제로 느려졌다 — 효과비 1.000 > 필요 1.106        (짐을 버렸다)
```

### `e2e-weight` — 문턱을 **눈으로 찍지 않았다**(족보 ⑺⒋)

```js
  const expected  = 1 / Math.max(0.01, c2.moveMult);          // 서버가 말한 배율의 역수
  const need      = noiseR + (expected - 1) * 0.5;            // 잡음 위 + 약속 효과의 절반
  const resolvable = (expected - 1) > (noiseR - 1) * 2 + 0.05; // 이 판에서 가를 수 있는가
```

실측: 잡음비 1.000(45px vs 45px) · 기대 0.211 · 실측 −17% vs 서버 배율 −17%.
**클라 예측과 서버 배율이 같은 수를 쓰는지**까지 이 한 줄이 잡는다.

### ★사보타주 1차 시도가 **아무 일도 안 했다** — 그것도 실패다

`drop_item amount: 9999` 를 보냈는데 서버가 거절했다(가진 것보다 많다). 짐은 그대로였고
하네스는 **초록**이었다 — "사보타주를 했는데 안 빨갰다"가 아니라 **사보타주가 안 일어났다**.
⇒ `__getInv()` 로 **실제 보유 개수를 읽어** 그만큼 버리게 고쳤다(`돌 9개`). 그제서야 ④ 가 빨갰다.
**대조군이 실패할 줄 아는지를 확인할 때, 대조군 자체가 no-op 인지 먼저 봐라.**

---

## 4. 러너 — 의존성이 없으면 **크게 죽는다**(스킵하지 않는다)

`node_modules` 가 없으면 하네스는 `MODULE_NOT_FOUND` 로 조용히 죽고, 러너는 그걸
"결과줄 0개"로만 말한다 — 화면에는 **92개가 줄줄이 빨간** 제품 회귀처럼 보인다.

```bash
  preflight() { … package.json 의 dependencies 를 실제로 require.resolve 해 본다 … }
  preflight || exit 2      # 하네스를 한 개도 안 돌리고 사유를 말하며 죽는다
```

스킵을 안 고른 이유: **스킵은 "안 쟀는데 초록"이라 더 나쁘다.** 회귀 러너의 침묵은
"괜찮다"로 읽힌다.

★이 절에서 내가 한 번 틀렸다 — 안내문의 백틱(`` `package.json` ``)이 **큰따옴표 안**에 있어
명령 치환으로 실행됐다(`package.json: command not found`). 홑따옴표로 바꿨다.

---

## 5. `split-verify.sh` — 이건 **일회용 증명**이지 상시 게이트가 아니다

계약이 "조각을 붙이면 **분할 전** 정본과 바이트 동일"이라, 클라를 한 글자라도 고치면
**영원히 differ** 다. T38 이 `50-i-panel.js` 를 고친 순간부터 이 스크립트는 빨갰고,
그건 결함이 아니라 **계약의 수명이 끝난 것**이었다.

지금은 스크립트가 그 사정을 **스스로 말하고 중립 종료(0)** 한다:

```bash
  SPLIT="${DEL:-}"                                   # 분할 커밋 = client.js 를 지운 커밋
  CHANGED="$(git diff --name-only "$SPLIT" -- public/client/)"
  if [ -n "$CHANGED" ]; then
    echo "분할 커밋 이후 조각이 바뀌었다 — **이 검사는 분할 커밋 전용이라 differ 가 정상이다.**"
    exit 0
  fi
```

조각이 **안 바뀐 채** differ 면 그때만 빨갛게 죽는다. `--selftest` 는 그대로 살아 있다.
⇒ 볼 것은 '같은가'가 아니라 **'의도한 줄만 바뀌었는가'** 다. (`인계/공통.md` ⓒ 절에 적었다.)

★부수 수리: `cmp -s` 가 첫 차이에서 끝나 결합 스크립트가 EPIPE 스택을 토했다 —
`process.stdout.on("error", () => {})` 한 줄.

---

## 6. 족보 — 지시한 다섯 + 두 줄, **글자 그대로**

`인계/공통.md` 에 (86)~(90) 과 T4·T14 두 줄을 T5/T12/T4/T14 보고서에서 **옮겨 적지 않고 그대로** 넣었다.
러너 절도 갱신했다(자동 발견 92 · 이름순은 의존 순서가 아니다 · 선행 점검 · 탐지기 · split-verify).

★**한 글자만 다르다 — 밝혀 둔다.** (89) 원문은 "**이** 배치가 그 전제를 깼다"인데
공통.md 에는 "**그** 배치가"로 넣었다. T12 보고서 안에서 "이 배치"는 T12 자신을 가리키지만
족보로 옮겨 오면 가리킬 것이 없어진다. **지시는 "글자 그대로"였으므로 어긋난 것을 그대로 적는다** —
원문이 옳다고 보시면 되돌리시면 된다(`인계/공통.md` (89) 둘째 줄).

---

## 7. 검증이 **셋을 더 잡았다** — 하나는 내 것이었다

첫 전수(92개 · 2시간 38분)에서 기지 밖 빨강이 셋 나왔다. **셋을 한 덩어리로 "깜빡인다"고 적지 않고
하나씩 갈랐다.** 러너의 빨강은 "제품이 회귀했다"를 뜻해야 하고, 그 뜻을 지키는 값은
빨강마다 주인을 찾아 주는 데서 나온다.

```
  회차 1 실패 5: test-guest-rejoin  e2e-charsprite  e2e-rumor  e2e-weight  e2e-zoom
                 └ 기지 3 ┘         └ 기지 밖 3 ────────────────────────────┘
```

### ⒜ `e2e-weight` — **내가 이 배치에서 만든 것이었다**

```
  ✗ ★전제 — 이 판에서 그 효과를 **가를 수 있다**   기대 0.211 vs 잡음 0.167
```

`resolvable`(가를 수 있는가)을 `ok()` 로 걸어 뒀다. 그러면 **"못 쟀다"가 빨강으로 새 나간다** —
형제 둘(`e2e-rtt`·`test-tick-slicer`)에는 유보 갈래를 만들어 놓고 여기만 안 맞춰 놓은 것이다.
러너의 빨강이 제품 회귀를 뜻하지 않게 되면, 사람은 빨강을 무시하는 법을 배운다.

**그런데 파 보니 진짜 원인은 따로 있었다 — 첫 걸음이 짧다.**

```
  잡음 바닥을 두 번 잰 기록:  38 vs 45 · 32 vs 32 · 45 vs 45 · 38 vs 45
                              └ 짧은 쪽은 **늘 앞**이었다
```

페이지가 막 뜬 뒤라 입력 큐·렌더가 아직 안 데워진 것이다. 그 한 표본이 잡음비를 1.17 로
부풀려 **판정을 유보시켰다** — 즉 이 하네스가 제 머리 검사를 못 하게 만들고 있었다.
⇒ 워밍업 한 걸음을 버리고 그 다음 둘로 잰다.

```
  워밍업 뒤 4회 연속   잡음비 **1.000**(45 vs 45) · ④ 가름 · 33/33 rc=0
  사보타주             ④ 효과비 1.000 → ✗ (rc=1)          ← 자기 실패 검사기 그대로 산다
  유보 갈래            바쁜 고리를 걸어 강제로 밟아 확인    ← 죽은 코드가 아니다
```

★**"유보하게 고쳤다"에서 멈추지 않은 게 이 절의 요점이다.** 유보는 못 재는 걸 정직하게 말할 뿐이고,
잴 수 있게 만드는 것과 다르다. 원인이 워밍업이라는 걸 찾고 나서야 **실제로 재게** 됐다.

### ⒝ `test-guest-rejoin ②` — **제품 경합이다. 고치지 않았다**

손으로 18판을 더 돌려 귀속을 갈랐다(`/root/benchlocal/t49_rejoin_attr.sh`):

```
  ① 한가한 기계 · 지금 하네스 ×6        빨강 2       ← 부하와 무관
  ② 바쁜 기계   · 지금 하네스 ×6        빨강 2
  ③ 바쁜 기계   · **T10 수리 전 옛 판** ×6  빨강 2   ← 내 수리가 만든 것이 아니다
```

결과가 **두 갈래로만 갈린다 — 중간이 없다.**

```
  통과   걷던 자리와 14~20px   · 낡은 저장 좌표와 233~244px    ← 몸을 물려받았다
  실패   걷던 자리와 250~256px · 낡은 저장 좌표와 **0px**      ← 낡은 행에서 다시 태어났다
```

자리는 `zone.js:3125~3145` 의 B-6 "살아 있는 몸 인수인계"다. 그건 **옛 세션이 아직 `players` 에
서 있을 때만** 돈다. close 가 먼저 처리되면(`zone.js:4341~` 이 `players.delete`) 물려받을 몸이
없어 central 을 읽는데, 그 읽기가 close 가 같이 던진 `savePlayer`(fire-and-forget)와 **또 경합**한다.

⇒ `KNOWN_ISSUES.md` **N.11** · 영역 Z 회부. **게임 코드 diff 0 이 이 카드의 합격 조건이라 손대지 않았다.**
설계 갈래 둘을 적어 뒀고, **N.6 과 같은 물음**("몸의 정본이 어디인가")이라 같이 판정하는 편이 낫다.

★**오늘 생긴 결함이 아니다.** 옛 하네스도 같은 비율로 빨갛다. 전수 세 번에서 안 보인 건
3분의 1 확률이 세 번 다 비껴간 것뿐이다(0.67³ ≈ 0.30). ⇒ 깜빡이는 하네스를 "가끔 빨갛다"로
넘기면 그 밑의 제품 경합이 그만큼 안 보인다.

### ⒞ `e2e-zoom ⑤` — **재현 못 했다. 그대로 적는다**

```
  전수 회차 1        평균 화소 차 **8.315**   (문턱 1.0)
  단독 6판           0.058 · 0.077 · 0.094 · 0.078 · 0.084 · 0.102   → 6/6 초록
```

평소엔 문턱의 **1/10** 이다. 한 번의 8.315 가 무엇이었는지 **모른다** — 화면은 이미 덮어써졌다.
다만 자의 모양은 위태롭다: `base` 와 `back` 사이에 1분 가까이 흐르고 그동안 **세계는 살아 있다**
(바람만 껐다). 짐승 하나가 지나가면 그만큼 차가 난다. **등재 하네스라 내 카드 밖이므로 회부만 했다.**

★**기지 목록에 넣지 않았다.** 한 번 본 것을 기지로 올리면 목록이 헐거워지고,
그때부터 기지 목록은 "고칠 것"이 아니라 "안 볼 것"이 된다.

---

## 8. 검증 — **3회 연속 전수 초록은 못 만들었다. 그대로 적는다**

수리 뒤 전수를 **처음부터 다시** 3회 돌렸다(92개 × 3 · 13:44~22:00 UTC · 회당 2시간 40분).

```
  회차 1   실패 4:  e2e-charsprite  e2e-rumor  e2e-village  e2e-weight
  회차 2   실패 3:  e2e-charsprite  e2e-trade  test-tick-slicer
  회차 3   실패 3:  e2e-charsprite  e2e-rumor  test-tick-slicer
```

| 하네스 | 1 | 2 | 3 | 기지? | 무엇이 떨어졌나 |
|---|:-:|:-:|:-:|---|---|
| `e2e-charsprite` | ✗ | ✗ | ✗ | 기지 | 절대 2800ms 대기 — T49 처방이 그대로 들어갈 자리(카드 밖) |
| `e2e-rumor` | ✗ | ✓ | ✗ | 기지 | 두 화면을 **따로** 받아 비교 — 시점 문제(T7 영역) |
| `test-handoff-body` | ✓ | ✓ | ✓ | 기지 | 이번 3회는 안 떨어졌다 — 깜빡이는 것이 확인된다 |
| `test-guest-rejoin` | ✓ | ✓ | ✓ | 기지·N.11 | 3분의 1 확률이 이번엔 세 번 다 비껴갔다 |
| `e2e-village` | ✗ | ✓ | ✓ | **밖** | 재접속 뒤 회관까지 **걸어 돌아가기 실패**(200px 게이트) |
| `e2e-weight` | ✗ | ✓ | ✓ | **밖** | ⑦ 마을 반경 진입 실패 — ④(내가 고친 자리)는 초록 |
| `e2e-trade` | ✓ | ✗ | ✓ | **밖** | 마을 반경 진입 실패 — **24,749px**(게이트 260) |
| `test-tick-slicer` | ✓ | ✗ | ✗ | **밖** | ⑧ [전제] A/B 대조할 마을 **0곳**(건너뜀 50) |

**⇒ 카드가 요구한 "3회 연속 전수 초록(기지 표기 제외)"은 달성하지 못했다.** 초록으로 만들 수 있는
길이 둘 있었지만 둘 다 안 갔다: ⓐ 기지 목록에 넷을 더 넣기 — 그러면 기지 목록이
"고칠 것"이 아니라 **"안 볼 것"**이 된다. ⓑ 카드 밖 하네스 넷을 고치기 — 그건 승인 없는 확장이고,
게임 코드 diff 0 을 지키는 배치에서 하네스 diff 를 몰래 늘리는 일이다.
⇒ **못 만든 것을 못 만들었다고 적고, 넷을 각각 귀속해 회부한다.** 카드 ①의 문구가
"새로 빨간 것은 등록 철회가 아니라 **귀속 후 표기**"였으므로, 귀속이 이 자리의 일이다.

### ⒜ 셋이 **같은 자리**에서 떨어졌다 — 마을 반경 진입

`e2e-village`·`e2e-weight`·`e2e-trade` 는 서로 다른 하네스인데 **떨어진 곳이 같다.**

```
  e2e-trade    ✗ 마을 반경 안에 들어왔다   vid=null · **24,749px** (게이트 260)
  e2e-weight   ✗ ★전제 — 마을 반경 안에 들어왔다      (그 뒤 거래소 시세표도 안 옴)
  e2e-village  ✗ 재접속한 창설자가 자기 마을 재고를 다시 볼 수 있다 → 그 뒤 전부 null
```

24,749px 은 "조금 모자랐다"가 아니라 **한 발짝도 안 갔다**는 뜻이다. 셋의 공통 구조를 읽었다:

```js
  const rows = db.prepare('SELECT id,name,cx,cy FROM villages WHERE zone=?').all('hanbando');
  const V = rows[0];                       // ★첫 마을을 **무조건** 고른다
  for (let i=0;i<25 && !tpOk;i++) { …teleport_debug(V)… }   // 25번 시도하고
  ok(near.vid != null, '마을 반경 안에 들어왔다');            // ★워프 성공 여부는 **안 잰다**
```

그리고 서버는 `teleport_debug` 를 **물 위면 거절한다**(`zone.js:3470` — `🌊 강·바다 위로는 텔레포트 불가`).
⇒ 세계는 판마다 새로 생기므로 `rows[0]` 이 막힌 칸에 앉는 판이 있다. 그러면 25번이 **모두** 거절되고,
하네스는 그걸 "마을에 못 들어갔다"로 적는다 — **원인(워프 거절)이 증상(반경 밖)으로 둔갑한다.**

★**가설이다. 증명하지 않았다.** 화면도 로그도 덮어써졌고 판마다 세계가 달라 되짚을 수 없다.
다만 이 레포에는 **이미 같은 병을 고친 자리가 있다** — 부하 실측 배치의 `scripts/bench-spread.js` 는
서버가 보낸 🌊/🌀 알림을 읽어 **다음 마을로 물러서는 되풀이**를 한다. 처방은 그것이다:
① 워프가 먹었는지를 **먼저 assert** 하고 ② 안 먹으면 `rows[1]`·`rows[2]` 로 물러선다.
⇒ 영역 V(마을배치)·T(거래) 회부.

### ⒝ `test-tick-slicer ⑧` — **내 팔이 원인일 수 있어서 갈랐다**

```
  회차 2   ✗ ⑧ [전제] 같은 날끼리 대조할 마을이 충분하다   A/A 50곳(건너뜀 0)  · A/B **0곳**(건너뜀 50)
  회차 3   ✗ ⑧ [전제] …                                    A/A 22곳(건너뜀 28) · A/B **0곳**(건너뜀 50)
```

⑧ 은 내가 **안 건드린** 자리다(`git diff` 에 ⑧ 줄 0). 하지만 나는 이 하네스에 **세 번째 팔**
(`base2` — 잡음 바닥용 대조군)을 넣었고, 그게 팔들의 시간 간격을 바꿨다. ⑧ 은 하네스 자신의 주석이
말하듯 **저장 커서의 위상**(1마을/틱)에 달린 자다. ⇒ **배제할 수 없으므로 대조군을 돌린다**
(`t49_slicer_attr.sh` — 지금 판 ×3 · T49 이전 옛 판 ×3, 같은 기계에서 나란히).

```
  ① 지금 판(base2 팔 **있음**) ×3   빨강 0    A/B 36곳 · 26곳 · 23곳
  ② 옛  판(base2 팔 **없음**) ×3   빨강 1    A/B 32곳 · **2곳(✗)** · 35곳
```

⇒ **내 팔 탓이 아니다.** 오히려 옛 판이 먼저 떨어졌다. ⑧ 은 원래부터 **시점에 달린 자**이고,
단독으로는 양쪽 다 대체로 초록이다 — **러너 안에서만** 떨어진다. ⇒ 러너의 부하가 두 팔의
게임일 진행을 서로 다르게 늦추고, 그러면 마을별 저장일이 통째로 어긋나 A/B 가 0 이 된다.

⇒ 영역 Z(존서버·일틱) 회부. 처방은 T49 가 셋에 쓴 것과 같은 족이다:
**대조를 "같은 날"이 아니라 "같은 진행량"으로 맞추거나**, 팔을 끝낼 때 저장 커서를 한 바퀴
돌려 놓고 스냅샷을 떠라(위상을 없앤다). 카드가 지목한 ③·⑥ 은 셋 다 초록이었다.

---

## 9. 게임 코드 diff 0 — 카드의 합격 조건

```bash
  git diff --name-only <베이스>..HEAD -- server/ sim/ public/
  (0줄)
```

이 배치가 만진 것은 `scripts/` · `인계/` · `보고/` · `README.md` · `KNOWN_ISSUES.md` 뿐이다.
제품 결함을 둘 발견했지만(N.11 · 마을 워프 거절 가설) **둘 다 안 고치고 회부**했다.

---

## 10. 회부 — `인계/회부.md` 에 절 둘

* **T49 절**: `e2e-charsprite`(절대 2800ms) · `e2e-rumor`(두 시점) · `test-handoff-body`(경합) ·
  `e2e-rtt ②a`(꼬리 분위수가 필요 — T47 데이터 모델)
* **검증 중 새로 드러난 것**: `test-guest-rejoin`/**N.11**(제품 경합 · 영역 Z) ·
  `e2e-zoom ⑤`(재현 못 함) · 마을 반경 진입 셋(영역 V·T) · `test-tick-slicer ⑧`(영역 Z)

## 11. 남긴 것


* **러너 밖 4개**: `test-lab-*` 셋(레포 밖 파일을 연다 — PM 판정대로 유지) ·
  `test-psite-server`(세션3 T48 이 제품 쪽 수리 후 등재).
* **기지 목록**에 `test-guest-rejoin` 을 **근거와 함께** 넣었다(N.11). `e2e-zoom` 은 **안 넣었다** —
  한 번 본 것을 기지로 올리면 목록이 헐거워진다.
* **재민 판정 대기**: 카드 밖 하네스 넷(`e2e-charsprite`·`e2e-rumor`·마을 진입 셋·`⑧`)에
  T49 처방을 마저 적용할지. 넷 다 **같은 병**이고 처방은 이미 이 레포 안에 있다.

---

## 12. 이 배치에서 내가 틀린 것 — 넷

1. **러너 밖을 러너의 자로 쟀다**(T9 때와 같은 실수의 재발). 이번엔 고칠 자리가 러너였다.
2. **안내문의 백틱을 큰따옴표 안에 뒀다** — 명령 치환으로 실행됐다.
3. **사보타주가 no-op 이었다** — `drop_item 9999` 를 서버가 거절했고, 그래서 "사보타주를 했는데
   안 빨갛다"가 아니라 **사보타주가 안 일어났다**. 대조군 자체가 살아 있는지를 먼저 봐야 한다.
4. **`resolvable` 을 `ok()` 로 걸었다** — "못 쟀다"를 러너의 빨강으로 내보냈다.
   그리고 그 밑에 진짜 원인(첫 걸음이 짧다)이 있었는데, 유보로 덮으면 **영영 못 볼 뻔했다.**
   ⇒ 유보는 정직하지만 수리가 아니다. 유보를 붙였으면 **왜 못 재는지**를 이어서 물어야 한다.

---

## 13. 리베이스 — 상류가 31커밋 앞서 있었다. **손으로 병합한 자리 둘**

푸시 직전 `origin/main` 이 `4d93a7ac` → `f14a9f66` 로 움직여 있었다(31커밋). 충돌 넷 중 둘이 코드다.

### 둘 다 "같은 문제를 서로 다르게 고친" 자리였다 — **양쪽을 다 남겼다**

상류(T42-b)는 같은 병(절대 문턱이 잡음에 걸린다)을 **다른 길**로 고쳐 뒀다:
문턱을 그 판의 **조각 크기에서 유도**한 무조건선(`한 프레임 ≤ 조각 하나 + 예산`)이다.
그건 눈대중 절대값이 아니라 **족보 74**(정본 수에서 유도)의 모범이고 잡음에 안 흔들린다.

```
  남긴 것   상류의 무조건선            e2e-rtt ②a·③ · test-tick-slicer ⑥a
  바꾼 것   상류의 비율 판정(②a'·③'·⑥b) → **비율의 비율**(내 것)
  내린 것   그 비율들의 "여지" 수치     → 참고 출력(같은 것을 두 자로 두 번 재지 않는다)
```

### 병합 뒤 실측 — **전환이 필요했다는 게 그 자리에서 드러났다**

```
  e2e-rtt   rc=0
    ✓ ③ 루프 최대 막힘이 가장 큰 조각 안 (상류 무조건선)   1935ms ≤ 1911×2+300
    [참고] **절대 문턱 1/3 · ★못 넘음**  (1934.62ms vs 1929.4ms)   ← 5ms 차이로 빨갰을 자리
    ✓ ③ 비율의 비율 2.91 > 1.6                                    ← 지금은 옳게 초록
    [참고] T42-b ②a' 여지 ×0.9 — 3배 미만이라 **애초에 유보됐을** 판이다
  e2e-weight  33/33 rc=0 · 잡음비 **1.000**(64px vs 64px)          ← 워밍업 수리가 상류 뒤에도 산다
  test-tick-slicer  ③ 3.08 · ⑥ 2.76 초록  ·  ⑥ 의 절대 문턱은 **★못 넘음**
```

⇒ **1/3 문턱은 5ms 차이로 떨어졌을 것이다.** 카드 ③의 처방이 실제로 필요했다는 증거가
리베이스 직후에 그대로 나왔다.

### 리베이스 뒤 남은 빨강 하나 — `test-tick-slicer ⑧`(상류 것)

상류가 ⑧ 을 통째로 새로 썼다(내 diff 에 ⑧ 줄 **0**). 그런데 그 새 ⑧ 이 **내가 e2e-weight 에서
고친 것과 똑같은 모양**이다 — 잡음 바닥을 `ok()` 로 걸어 뒀다:

```
  ✗ ⑧ [대조군] 같은 일정 두 판의 잡음 바닥이 문턱보다 작다   식량 차 **0.967%** ≤ 0.5%
```

기계가 바쁘면 잡음 바닥이 0.5% 를 넘고, 그러면 **제품이 멀쩡해도 빨갛다.** 영역 Z 회부.
(전수 3회 때 본 `⑧ A/B 0곳` 은 상류가 그 자리를 다시 쓰면서 사라졌다 — 그 귀속 작업은
`t49_slicer_attr.sh` 에 기록으로 남긴다: **내 세 번째 팔 탓이 아니었다**.)

★**전수 3회는 리베이스 *전* 나무에서 잰 것이다.** 리베이스 뒤에는 손으로 병합한 셋만 다시 돌렸다.
전수 재검증은 푸시 뒤에 이어서 돌린다 — **그대로 적는다.**
