# 보고 — T103 범프 복원 재굽기(아이콘 144 · 가구 21) · 건물 12 편입 (세션8 · 2026-09-05)

브랜치 `batch/bump-restore-0905` → ff → `main`. **force push 0회.**

---

## 0. §0 실측 (셋)

### ⓐ `ShaderNodeBump` 전수 — 기본값에 기대던 자리가 **7곳** 남아 있었다

| 파일 | 만드는 곳 | `Distance` 명시 | 처분 |
|---|---|---|---|
| `render_common.py` | 2 (`striped_mat`·`bumped_mat`) | ✓ 2/2 (T101) | — |
| `nature_render.py` | 4 (`leaf_mat`·`bark_mat`·`rock_mat`×2) | ✓ 4/4 (T101) | — |
| `building_render.py` | 1 (`striped_mat`) | ✗ | **공용으로 편입**(이 카드 ②) |
| `bridge_render.py` | 2 | ✗ | **이 카드에서 명시**(코드만 · 재굽기는 회부) |
| `bake-mountain.py` | 2 | ✗ | 〃 |
| `bake-terrain-tex.py` | 3 | ✗ | 〃 |
| `char_render.py` | **0** | — | **이 병에 안 걸렸다** — T96 에 전달(회부) |
| `icon_render.py`·`props_render.py`·`models_crops.py`·`fields_render.py` | 0 | — | 공용을 쓴다 ⇒ **코드 변경 0** |

⇒ 13곳 전부 명시로 바꿨다. 값은 `render_common.BUMP_DIST` **한 곳**이 정본이고,
`test-render-common ⑦` 이 (ㄱ) 명시 누락 0 (ㄴ) 다른 파일에 숫자 사본 0 을 지킨다.

### ⓑ 범프 없는 자산 = **공짜로 딸려 온 대조군**

아이콘 144 중 **9장이 재굽기 뒤 IDAT 불변**:
`copper·gold·iron·lead·nickel·silver·tin`(금속 7) · `eggplant` · `item_farmland`(T101 에서 이미 복원).
금속 일곱은 소스에서 확인했다 — 쓰는 재질이 `*met`·`silvertar` 뿐이고 전부 `simple_mat`(범프 없음).

★가장 날카로운 것은 **가지**다. 같은 `_fruit` 모델인데
`m_eggplant`(수확) **불변** · `m_seed_eggplant`(씨앗) **바뀜**.
`_fruit` 이 `seed=True` 일 때만 `_seed_dish()` 를 부르고 접시 재질 `M['dish']` 가 범프이기 때문이다.
⇒ 이번 재굽기가 **범프 있는 면만** 건드렸다는 뜻이다. 다른 것이 섞였으면 금속도 바뀌었을 것이다.
가구 21 은 21장 전부 바뀌었다(나무결·막돌·이엉이 안 들어간 가구가 없다).

### ⓒ 건물 프레임 표 — 세로가 긴 것이 **0/12** (굽기 전 예언)

프레임은 발자국 공식이다: `Wpx = (W+1 + D+1)·32 + 8` · `Hpx = (W+1 + D+1)·16 + 용마루·32 + 12`.

| 키 | 발자국 | 용마루 m | Wpx | Hpx |
|---|---|---|---|---|
| `hut_roof` | 6×4 | 3.90 | 392 | 328 |
| `hall_roof` | 8×8 | 5.10 | **584** | **463** |
| `granary` | 5×3 | 3.60 | 328 | 287 |
| `hut_s1/2/3` | 6×4 | 0.45/2.35/3.85 | 392 | 218/279/327 |
| `furn_s1~3`·`furnace` | 2×2 | 0.30~1.55 | 200 | 117~157 |
| `kiln_s1`·`charcoal_kiln` | 2×2 | 0.30/1.25 | 200 | 117/148 |

**12장 전부 가로가 길다** ⇒ `sensor_fit` 을 붙여도 프레임·앵커가 안 바뀐다.
자연물이 15/38 잘려 있던 그 함정이 건물엔 **한 장도 없다**. 구운 뒤 12장 전부 무변으로 확인 — 예언 적중.
⇒ 클라의 손으로 적은 앵커표(`20-r2-visibility.js` 의 `A`)를 안 건드렸다. **클라 접점 0.**

---

## 1. ① 아이콘 144 + 가구 21 — 코드 변경 0, 굽기만

`icon_render.py`(28) · `models_crops.py`(68) · `props_render.py`(가구 10 + 손도구 37 + 세계 21) 를
그대로 다시 구웠다. T101 이 이미 `render_common` 에 `BUMP_DIST` 를 명시했으므로 **소스 diff 0**.

### 결과가 4.0.2 를 향해 갔다 — 두 자로 쟀다

카드가 요구한 "4.0.2 배포본 대비 |Δ|" 는 **아이콘·가구엔 잴 baseline 이 없다**(T77·T79 에서 이미
5.0.1 로 다시 구웠다 — 남아 있던 4.0.2 는 건물뿐이었다). 그래서 두 곳에서 쟀다:

**(가) 자연물 23장 — T97 이 `5.53/255` 라 잰 바로 그 집합, 4.0.2 배포본 대비**

| | T97(복원 전) | T103(복원 후) |
|---|---|---|
| 수풀 6 | 6.68~8.03 | **4.61~5.26** |
| 풀 4 | 3.25~4.31 | **2.70~3.30** |
| 약초 6 | 4.25~4.92 | **2.87~3.44** |
| 나무 7 | 5.44~6.81 | **3.13~4.67** |
| **평균** | **5.53/255** | **3.74/255** |

**23/23 전부 내려갔다.** 한 장도 예외가 없다.

**(나) 건물 12장 — 4.0.2 배포본 대비 (이 카드가 처음 잰다)**

| 무리 | 평균 Δ |
|---|---|
| 노·숯가마 6(작고 범프 면적이 작다) | 1.22~1.54 |
| 움집 공정 3 | 2.05~2.54 |
| 지붕·곳간 3(이엉이 큰 범프 면) | 3.58~4.45 |
| **전체 평균** | **2.24/255** |

⇒ 남은 2~4/255 가 **진짜** 4.0.2 → 5.0.1 차이(샘플러·난수)다.
T97 이 "기계 차이"라 부른 5.53 중 **3분의 2 는 사실 잃어버린 범프**였다.

---

## 2. ② 건물 12 — 편입 + 5.0.1

`building_render.py` 가 씬·헬퍼 한 벌을 갖고 있던 **마지막** 렌더 스크립트였다(627 → 519줄).

| 단계 | 무엇 | 결과 |
|---|---|---|
| 대조군 | 옛 `building_render.py` · 이 컨테이너 · 5.0.1 | 12장 + 앵커 |
| **패스 A** | 편입본 + 옛 거동 재현(`rc.BUMP_DIST = 0.001` · `sensor_fit='AUTO'`) | **IDAT 12/12 동일 · 앵커 0 차이** |
| 패스 B | 정본(`BUMP_DIST = 1.0` · `sensor_fit='HORIZONTAL'`) | 배포본 |

* `render()` 만 남겼다 — 프레임이 **발자국 공식**이라 세계 프리셋(bbox 맞춤)을 못 쓴다.
  나머지(씬·재질 문법·기하 헬퍼·변환 굽기·z 압축·PNG 후처리)는 전부 공용 것이다.
* 갈린 기본값 잠금(T77 §0-ⓐ 문법): `cyl.verts` 14↔12 → 생략 1곳에 `verts=14` ·
  `cyl.smooth` (없음)↔True → **호출 28곳 전부**에 `smooth=False`.
* ★`OBJS = []; globals()['OBJS'] = OBJS` 는 **이름을 다시 묶는** 것이라 `import *` 로 들어온 리스트와
  갈라진다 ⇒ `OBJS.clear()`.
* `__main__` 가드를 붙여 대조 하네스가 빌더만 꺼내 쓸 수 있게 했다(T101 문법).
* `scripts/building_renders/` 7파일이 저장소에 **커밋돼 있었다**(중간 산출 — T79b 의 그 함정).
  `.gitignore` 에 넣고 인덱스에서 뺐다. 읽는 곳이 없음을 전수 grep 으로 확인했다.

---

## 3. ③ 굽는 규약 — `인계/ART-자산.md §19`

```
정본:  render_common.BUMP_DIST = 1.0
규약:  ShaderNodeBump 를 만드는 자리는 반드시 inputs["Distance"] 를 BUMP_DIST 로 채운다.
       숫자를 다른 파일에 다시 적지 않는다.
지킴이: test-render-common ⑦ (전수 소스 검사 + 사본 검사 + --selftest 오염 검출)
```

---

## 4. 검증 (러너 전수 없음 — 공통 §2 ②)

접점 파일로 뽑은 목록(`grep -l "<파일>" scripts/test-*.js scripts/e2e-*.js`):

| 접점 | 부르는 하네스 | 결과 |
|---|---|---|
| `icon_render.py`·`render_common.py` | `test-render-common` | **PASS** · selftest 6/6 |
| `props_render.py`·`models_crops.py` | `test-icons` · `test-props` · `test-render-common` | PASS · PASS · PASS |
| `building_render.py` | `test-building-anchor` · `test-charsheet` · `test-render-common` | **PASS** · 26/0 · PASS |
| `icons.lock.json` | `test-icons` · `test-props` | PASS · PASS |
| `props_anchors.json` | `test-props` | PASS |
| `bridge_render.py`·`bake-*.py`·`building_anchors.json` | (부르는 하네스 없음) | — |
| (카드 지정) | `e2e-inv` · `e2e-rooms` · `e2e-nature` | **94/0** · **66/0** · **86/0** |
| 랩 모듈 적재 | 적재 17파일 · 내 diff 4 | **교집합 ∅** |

새 계약 `test-render-common ⑦`(범프 기본값 의존 0)은 `--selftest` 에서
`render_common.py` 소스에 `Distance` 없는 `ShaderNodeBump` 를 한 줄 심어 잡히는 것을 확인했다
(문턱도 5 → 6 으로 올렸다). `test-icons ⑧` 에 `buildings` 무리와 `_범프 = 복원 완료` 줄 검사를 더했다.

### 굽는 시간 (2코어 · bpy 5.0.1)

| 판 | 장 | 시간 |
|---|---|---|
| 아이콘 `icon_render` | 28 | 약 7분 |
| 작물 `models_crops` | 68 | 약 8분 |
| 가구 `props_render`(아이콘 47 + 세계 21) | 68 | 약 9분 |
| 건물 대조군 · 패스 A · 패스 B | 12×3 | 각 ~3분 |

---

## 5. 실기 2줄 (재민)

1. `산그림/디자인B/범프복원_아이콘가구_전후.png` — **짐 창의 흙·돌·나무결이 다시 거칠어 보이는가.**
   왼쪽 전(범프 죽음) · 오른쪽 후. `copper` 는 **대조군**이다 — 민 재질만 써서 전후가 같다.
2. `산그림/디자인B/건물_재굽기_전후.png` — **움집 지붕 이엉이 전과 같은가.**
   왼쪽 전(맥 4.0.2) · 오른쪽 후(5.0.1 · 편입 · 범프 복원). 프레임·앵커 12장 전부 무변.

## 6. 회부

`인계/회부.md` `0-범프끝` 절 — 아직 4.0.2 인 셋(산·지면 질감·다리 — **코드는 이미 고쳤고 재굽기만**) ·
`char_render.py` 는 `ShaderNodeBump` 0곳이라 이 병에 안 걸렸다(T96 에 전달 · 단 `sensor_fit` 은 별개) ·
자연물·건물 재질 접두 · `e2e-*` 포트 선점 감지 · legacy 덤불·약초 죽은 코드 · 상추/아욱 · 종별 34세트 · 침상 생활층.

## 7. 푸시

* 커밋 **`db7864ab`** `[재민 확정 2026-09-05 · T103]` (밑동 `55cfd301` — 푸시 직전 리베이스 1회 · 충돌 0)
* 리베이스: `origin/main` 이 `0fff0007` → `55cfd301`(PM 의 CI 카드 — `.github/workflows/regress-unit.yml` ·
  `README.md` · `인계/공통.md §2 ⑥) 로 움직여 있었다. 내 파일과 교집합 ∅.
  ⓘ 그 CI 의 `core` 23종에 **내 하네스 다섯이 들어 있다**(`test-render-common`·`test-icons`·`test-props`·
  `test-crops-world`·`test-charsheet`). 리베이스 뒤 여섯을 다시 돌려 전부 PASS —
  `batch/**` push 에서 CI 가 도니 브랜치를 밀면 거기서도 같은 답이 나온다.
* **force push 0회.**

⚠**푸시가 아직 안 됐다 — 맥이 브리지에 안 붙어 있다.** 번들은 만들어 채팅으로 보냈고
(`t103.bundle` · `55cfd301..db7864ab`), 맥이 돌아오면 정본 절차대로 민다:
`git fetch <번들> HEAD:refs/temp/t103` → `merge-base --is-ancestor origin/main` 확인 →
`batch/bump-restore-0905` 먼저 → 원격 main 재확인 → main. 그 뒤 `.lock` 전수와 해시를 여기 채운다.
