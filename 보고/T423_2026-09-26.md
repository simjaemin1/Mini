# 보고 — T423 짐이 먹는다 · 군량 = 행위 ⓐ 구현 (2026-09-26)

* **한 줄 답**: 팩은 이제 **병사 몸의 짐**이다 — 곳간에서 뗀 품목 그대로 몸마다 나눠 싣고(몸당 = 병력 ÷ 몸), 종전 `_packRem −= 1` 이 돌던 **그 하루 경계**에 몸이 제 짐에서 먹고, `_packRem` 은 짐 합의 거울이라 결단은 한 글자도 안 바뀌고(날마다 장부와 같은 수), 집에 닿은 몸이 `_warFoodGive` 문으로 내려놓고, 전사·포로의 짐은 드랍이며, 원정군은 마을 식사 명부에서 빠진다(1인 하루 2단위 → 1). 항등 ○ · 결판 무변 ○ · 끔 비트 동일.
* **가지** `batch/ration-act-0926` · 코드 커밋 **`c193c6c2` [승인 대기 · T423]** · **베이스 `origin/main 7b7d4b90`** · 실행 세션 = 세션10(WAR).
* 짐 싣기: 하네스 픽스처 **몸당 18단위 = 곡물 12.6 kg**(병력 12 · 짐꾼 8) · 실서버 **몸당 13단위 ≈ 11.4 kg**(병력 2 · 짐꾼 2 · 품목 7종 섞임) — 둘 다 짐 상한 25 kg 아래. 넘치는 판은 표(§0-ⓐ)에만.
* 먹음: 하루에 **병력 × `WAR_RATION`**(하네스 12 · 실서버 2) — 짐꾼 몸이 몫씩.
* 이중 0: 켬이면 마을이 먹는 사람 수가 **N − 병력**(30 → 21 · 병력 9) · ★대조 끔은 30.
* 항등 ○(세 판 + 실서버): 곳간에서 나간 양 = 장부 + 몸이 먹음 + 내려놓음 + 드랍 — 144 = 0+24+120+0 · 144 = 0+12+16.5+115.5 · 18 = 0+12+6+0 · **실서버 28.0 = 2.0 + 20.0 + 6.0 + 0.0**.
* 사기 곡선: `ration` = 오늘 몫이 짐에 있나(0~1)를 **매일** 다시 적는다(T295 결함 둘 닫힘 — 일수를 비율로 쓰던 것 · 한 번만 적던 것) · 굶주림 판 0.5 → 0.5 · 결단(철수)은 장부와 같은 날.
* 결판 무변 ○: 포위·돌격·굶주림 세 판 끔/켬 끝·해시·전이·정산 한 글자 동일(사기 손잡이 `T295_RATION_MRL` 끔이라 전투 식이 짐을 안 읽는다).
* 끔: `test-war-world` 126/0(116 + ⓥ 10) · **3시드 800일 `t17-metrics` JSON 바이트 동일 3/3**(인구 8023/8116/8010 — PM 넷째 판-b 그대로) · 번들 8/0 · 랩 둘 재인라인 최신 · harness-lint 61 · lab-wiring 0.
* ⚠두 발견(구현이 막히진 않았다): ① **`doEat` 은 군량 품목 대부분을 못 먹는다** — econ 이름(`fish`·`meat`·`cooked_food`·`fruit`·`vegetable`·`mushroom`·해산물)이 플레이어 `FOOD_EFFECTS` 에 없다 ⇒ 몸의 먹기는 **싣기와 같은 문**(`_warFoodTake` = 섭식 정본 `consumeFood`)을 몸의 짐에 부른다 ② **NPC 몸은 허기 면제**(`zone.js:12338` · 사용자 확정) ⇒ 짐이 비어도 허기는 안 오른다 — 굶주림이 세계에 닿는 길은 결단(철수)과 사기 항이다.
* 재민 판정 칸: 켜기 · 허기 면제를 병사에게 풀지(T388 ⓐ-4) · `doEat` 품목 다리.
* 회부: ⓑ 길에서 채집 · ⓒ 약탈(PM · ⓐ 등가 뒤) · 전사자 짐의 땅 위 낙하(`_deathDrop` 주입 — 지금은 장부의 드랍) · 무기 손에 2층 · 기습 규칙 · 민병 값.

---

## §0 실측

### ⓐ 싣기 — 팩이 몸의 짐이 된다

| 판 | 병력 | 짐꾼(몸) | 몸당 몫 | 몸당 짐 | kg(weights.js) | 상한 25 kg |
|---|---|---|---|---|---|---|
| 하네스 픽스처(곡물만) | 12 | 8 | 1.5명 | 18 단위 | 곡물 0.70 → **12.6** | 안 |
| 실서버 `siegehold`(어촌2 → 광산2) | 2 | 2 | 1명 | 13 단위 | 곡물 16.5 · 생선 7.7 · 연어 2.7 · 해조 2.3 · 새우 1.9 · 굴 1.1 · 게 0.9 ÷ 2 → **11.4** | 안 |
| 표만 — 병력 44 · 짐꾼 34 · 14일 | 44 | 34 | 1.29명 | 18.1 단위 | 곡물 12.7 · 생선 16.3 · 고기 18.1 · **연어만이면 27.2** | **연어만일 때 2.2 kg 넘침** |

넘치는 몫은 지금 **값을 안 짓는다**(과적 이속 `carry.js` 는 몸 걸음에 안 걸려 있다 — 행군은 시간표로 걷는다 · T388 §0-ⓒ).

**어디서 싣나** — `_warInstantiateAttackers`(`villages:3329`) → `_warRationLoad`(`:3558`) → war-core `packSplit`(`:788`): 남은 팩(잔량 일수 × 하루치 ÷ 적재 — 환급 식과 같은 비)을 품목 그대로 몸 수로 나눈다 · 한 번만 · 팩은 이제 몸의 것(`_packItems` 비움). 몸이 서기 전에 지난 날은 종전 장부로 깎인 몫으로 적는다(`ledgerEaten` — 실서버 2.0).

### ⓑ 먹는다 — 하루 경계 · 몸이 제 짐에서

| 자리 | 무엇 |
|---|---|
| war-core `_packTick`(`:775`) | 종전 `_packRem −= 1` **세 자리**(행군 `:1091` · 주둔/포위 `:1096` · 귀환 `:1108`)가 이 함수 하나 — 켬이고 짐이 몸에 있으면 호스트 `rationEat` 을 부르고 그 값을 `_packRem` 에 · 아니면 종전 그대로 −1 |
| 호스트 `_warRationEat`(`villages:3571`) | 살아 있는 짐꾼마다 몫(병력 ÷ 짐꾼 × `WAR_RATION`)을 `bodyEat`(`:800` = `_warFoodTake` — 싣기와 **같은 문** · 섭식 정본이 품목을 고른다)으로 · 짐은 **팩 품목 칸만** 본다(어부가 들고 있던 제 고기는 군량이 아니다) · 거울 = 남은 짐 식량등가 ÷ (병력 × 하루치) |
| 사기 항 | `ration` = 남은 짐 ÷ (짐꾼 병력 × 하루치) · 0~1 · 교전 몸이 있으면 그 판의 `ctx.sides.A.ration` 에 **매일** |
| 교전 중 | 종전처럼 안 먹는다(`phase 'battle'` 은 daily 무관여 — 거울도 멈춘다) |

**실서버 한 원정**(존 부팅 · `WAR_FIXTURE=siegehold` · 관측자 하나 · 로그 그대로):

```
⚔️ D1 어촌2→광산2 선전포고[trade·full] 병력2 … 군량 14.0일분
⚔️ D2 어촌2 → 광산2 포위 유지 결단(정책)
⚔️ D3 어촌2 짐이 먹는다 — 짐꾼 2 · 먹음 2.0 · 남은 짐 24.0(12.00일) · ration 1.00
⚔️ D4 … 남은 짐 22.0(11.00일) …   (D5~D11 하루 2.0씩)
⚔️ D12 어촌2 짐이 먹는다 — 짐꾼 2 · 먹음 2.0 · 남은 짐 6.0(3.00일) · ration 1.00
⚔️ D12 어촌2 → 광산2 철수(군량 소진(봉쇄 전용 정책)) — 도보 귀환
⚔️ D12 어촌2 짐을 곳간에 내려놓는다 — 6.0
⚔️ D13 어촌2 짐 장부 — 적재 28.0 = 장부 2.0 + 몸이 먹음 20.0 + 내려놓음 6.0 + 드랍 0.0
```

짐이 줄어 3일이 되자 **같은 결단 식**이 철수를 골랐다(종전 장부와 같은 날 — 하네스 ⓥ 거울 칸).

### ⓒ 빠진다 · 내려놓는다 · 드랍

| 문 | 자리 | 무엇 |
|---|---|---|
| **마을 식사 명부에서 빠진다** | econ `tickVillage` `foodNeed = _t423EatN(v, N) × 1.0`(`economy-sim:3302` · 함수 `:505`) | 표식 `_warPack ∧ _warDraft` 인 사람을 뺀다 · 표식은 war-core `warDraftFill`(`:743`)이 **켤 때만 · 공격 징발에만** 붙인다(방어 소집은 제 마을 곳간을 먹는다) · 풀면(`_warDraftUnmark`) 같이 지운다 |
| 내려놓기 | 집에 닿아 풀리는 몸 `_warCleanupBody` → `_warRationLayDownBody`(`villages:3613`·`:3591`) · war-core 귀환 도착 `_rationLayDownAll`(`:819`) → 호스트 `_warRationCollect`(`:3598`) | 둘 다 `rationLayDown`(`:810`) → **`_warFoodGive`**(환급과 같은 문 · 품목 그대로 · 세금 0) |
| 드랍 | 전사자 `_warEndFight` → `_warBagDrop`(`villages:3482`·`:3604`) · 포로로 끌려가는 공격 병사도 | 짐은 몸과 함께 전장에 — 곳간으로 안 돌아온다(종전 "전사자 몫 제외" 와 같은 값) · 장부 `drop` 에 적는다 |

**이중 0 실측**(하네스 ⓥ · `_mkCostWorld` · 곡물 5000): 끔 — 그날 먹은 합 **30**(명부 30) · 켬 — **21 = 30 − 병력 9**.

### ⓓ 한 원정 등가 표(T388 §2-ⓒ 문법 · 같은 씨 끔/켬)

| 줄 | 포위(항복 · 13) | 돌격(궤주 · 11) | 굶주림(팩 18 · 포위만 · 19) | 판정 |
|---|---|---|---|---|
| 적재 | 144 / 144 | 144 / 144 | 18 / 18 | 같다 |
| 하루별 `_packRem`(끔 = 켬) | 11 · 10 · 0 | 11 · 11 · 0 | 0.5 · 0 | **같다** |
| 결단 · 끝 · 해시 · 전이 · 정산 | 항복 | 궤주 B | 조용한 철수 | **같다** |
| 몸이 먹음 | 24 | 12 | 12 | 하루 12 = 병력 × 하루치 |
| 내려놓음 | 120 | 16.5 | 6 | — |
| 드랍 | 0 | **115.5** | 0 | 전사 몸의 짐 |
| 항등(적재 = 장부 + 먹음 + 내려놓음 + 드랍) | 144 ✓ | 144 ✓ | 18 ✓ | ○ |
| 사기 `ration` | 1 · 1 | 1 | **0.5 · 0.5** | 다르다 — 의도(종전은 늘 1) |
| 허기 | 변화 0 | 0 | 0 | NPC 면제(`zone.js:12338`) — 발견 ② |
| 공격 마을 곳간(귀환 뒤) | 966 / 966 | 857 / **862.5** | 18 / 18 | 돌격만 +5.5 — 아래 |

**돌격 판 +5.5**: 종전 환급은 `잔량 × 병력 몫 × (전투 생존 ÷ 병력)`(사람 비)이고, 켬은 **살아 돌아온 짐꾼의 짐 그대로**다. 생존 짐꾼 1 이 1.5명 몫(병력 12 ÷ 짐꾼 8)을 지고 온다 — 표본 몸이 사람보다 적을 때의 차이이고, 켬이 실체다(짐을 진 몸이 돌아왔다).

### ⓔ 한 줄 답

**팩은 병사 몸의 짐이 됐고, 몸이 하루 경계마다 제 짐에서 먹고, 집에 닿은 몸이 내려놓고, 죽은 몸의 짐은 떨어진다 — 결단은 짐의 거울을 보아 종전과 같은 날에 서고, 원정군은 마을 식사에서 빠져 이중 계산이 닫혔다. 끔이면 한 글자도 안 다르다.**

| 재민 판정 칸 | 후보 | PM 권고 |
|---|---|---|
| 켜기 | ① 켠다(`T423_RATION_ACT=1`) ② 끈 채 | ① — 항등·결판 무변·이중 0 이 섰다 |
| 병사 허기 면제(T388 ⓐ-4) | ① 유지(굶주림 = 결단·사기) ② 병사만 풀어 짐이 비면 허기가 오른다 | ① — 풀면 캐러밴·포로까지 같은 물음이 번진다 |
| `doEat` 품목 다리 | ① 지금(섭식 정본을 몸 짐에 · econ 이름 그대로) ② econ → 플레이어 품목 다리(`ITEM_ALIAS` 역방향)를 세워 `doEat` 으로 | ① 먼저 — ② 는 품목 이름 층 카드(ART·ECON) |

## §1 무엇을 고쳤나

| 파일 | 무엇 |
|---|---|
| `sim/war-core.js` | `_rationActOn` · `_packTick`(세 자리 하나로) · `packSplit` · `bodyEat` · `bagFE` · `rationLayDown` · `_rationLayDownAll` · `warDraftFill` 표식 · `_warDraftUnmark` 표식 지움 · 로그 셋(먹음 · 내려놓음 · 장부) · 내보내기 |
| `server/villages.js`(전쟁 절) | createWar 훅 둘 · `_warRationLoad` · `_warRationEat` · `_warRationLayDownBody` · `_warRationCollect` · `_warBagDrop` · 싣기/드랍/내려놓기 부르는 세 줄 · 켬 알림 한 줄 · 하네스 노출 |
| `sim/economy-sim.js` | `_t423EatN` + `foodNeed` 한 줄 |
| `sim/economy-engine.browser.js` · `lab/마을실험실.html` · `lab/전쟁실험실.html` | 번들·인라인 재생성(econ 한 줄의 사본 규약) |
| `scripts/test-war-world.js` | 훅 둘 · `opts.pack/packFood/siegeOnly/untilHome` · 판 기록(`packDays`·`book`·`bagLeft`) · 절 ⓥ 10칸 |

## §2 검증

| 판 | 결과 |
|---|---|
| 3시드(1020·7·42) 800일 `t17-metrics` 끔 vs `origin/main 7b7d4b90` | **JSON `cmp` 바이트 동일 3/3** · 인구 8023/8116/8010 · 소멸 0/51 · 무기Q 1327/1293/1341 |
| `scripts/test-war-world.js`(서버 절 포함) | **126/0**(116 + ⓥ 10) · 켬 환경으로도 126/0 |
| `sim/build-econ-bundle.js --check` | 8 일치 / 0 불일치 |
| `sim/inline-engine.js --check` | 마을실험실 · 전쟁실험실 최신 |
| `scripts/test-harness-lint.js` | 61 PASS / 0 FAIL |
| `scripts/lab-wiring-check.js` | 실패 0 · 경고 0 |
| `scripts/test-body.js`(허기 접점) | 304/2 — **main 도 같은 2칸**(⑯㉧ 갯벌·짠물 · 이 카드 무관) |
| 실서버 한 원정 | 위 로그(§0-ⓑ) — 항등 28.0 = 2.0 + 20.0 + 6.0 + 0.0 |
| 접점 심볼 | `_opPackLoad`(무변) · `_packRem`(거울) · `ration`(매일) · `consumeFood`(몸 먹기의 품목) · `doEat`(발견 ①) · `inventory`(짐) · `_warFoodGive`(내려놓기) · `_warEngage`(무접촉) · `M_BREAK`(무접촉) · `T423_RATION_ACT` · `test-war-world` ⓥ · t364 판(`siegehold` 실서버) |

## §3 규칙 지킴

새 수 0(몫 = 병력 ÷ 짐꾼 · 하루치 = `WAR_RATION` · 상한 25 kg 은 표만) · 손잡이 하나(끔) · 사본 0(싣기·먹기 = `_warFoodTake` · 내려놓기 = `_warFoodGive` · 품목 = `consumeFood` · 식량등가 = `totalFoodEquivalent`) · `Math.random` 0 · T403 2층·T400·T410 무접촉 · main 무접촉 · force 0.

## §4 회부

* **켜기**(재민) · 허기 면제(재민 · T388 ⓐ-4) · `doEat` 품목 다리(ART·ECON · 이름 층).
* 전사자 짐의 **땅 위 낙하** — 지금은 장부의 드랍(곳간으로 안 돌아온다). 땅에 품목으로 떨어지려면 `_deathDrop` 주입(존)과 NPC 전사 경로(`_warDespawnPid`)가 만나야 한다 — T403 무기 드랍과 한 카드.
* ⓑ 길에서 채집 · ⓒ 약탈(PM · ⓐ 등가 뒤 별도 카드) · 무기 손에 2층(PM) · 기습 규칙 · 민병 값(재민).
* `test-body` ⑯㉧ 두 칸 빨강(main 도) — 갯벌·짠물 · 담당 세션에.

## §5 푸시

* 가지 `batch/ration-act-0926` · 베이스 `origin/main 7b7d4b90`.
* 컨테이너 푸시는 403(프록시 정책) ⇒ 재민 PC 셸에서 저장된 토큰으로 민다(출력 마스킹). main 은 PM.
