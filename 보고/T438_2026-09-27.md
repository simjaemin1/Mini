# T438 보고 — 회부 색인 (세션11 · 2026-09-27)

- **한 줄 답**: 회부 **전수 1000** · 열림 **933** · 닫힘 **67** · 낡음 **0** · ⓐ(판정대기 누락) **0** · ⓑ(닫힘 미기재) **36** · 되풀이 최다 = "#26" ×**14** · `회부.md` 본문 **무변**.
- 가지 `batch/referral-index-0927` · 내용 커밋 **`5f0f0367`**(+ 이 줄을 적은 해시 커밋 하나) · **[승인 대기 · T438]** · 베이스 **`092f920e`**(origin/main)

## ① 회부 전수 표 + 되풀이 최다 상위 10

`인계/회부_색인.md` 신설(새 파일 하나). `인계/회부.md`(4,427줄 · 171절 · 원문 무변)를 전수 파싱했다 — 항목 경계는
번호(`1. `)·불릿(`* `)·원문자 볼드(`①②③…`·`㉠㉡㉢…`)·표 행 넷을 다 잡았다(첫 시도는 번호·표 행만 잡아 171절 중
86절이 0항목으로 비었다 — 원문자 볼드 방식을 쓰는 절이 태반이라 놓쳤던 것 · 두 번째 시도로 39절까지 줄이고
세 번째로 원문자 범위를 넓혀 **0절**로 닫았다). 이 세 규칙에도 안 걸린 절 3개(「5. 회부만 할 것」·「★[0번 분할배치]
새로 연 회부」·「T194 닫은 것 하나」)는 절 전체를 단일 행으로 접었다(† 표시). 인용은 원문 그대로 ≤40자.

출처는 `회부.md` 절 하나로 통일했다 — 이 파일이 `보고/T*.md` 125장의 회부 절을 이미 카드별로 모아 둔 통합
로그라서, 원본 보고 파일을 또 파싱하면 중복 집계가 된다. (`보고/T*.md` 파일에 회부 절이 있는데 `회부.md`에
없는 "누락"이 있는지 정규식으로 교차 검증도 해봤으나, T90처럼 실제로는 회부 절이 있는 카드까지 "누락"으로
잘못 잡는 오탐이 태반이라 **폐기**했다 — 믿을 수 없는 숫자를 표에 올리느니 빼는 쪽을 골랐다.)

되풀이 최다 상위 10(같은 `#NN` 판정번호로 묶음): **#26 ×14**(T215×2·T223·T231·T233·T239·T248·T252·T256·
T271·T299·T362·T396) · #22 ×8 · #11 ×6 · #12 ×6 · #69 ×5 · #47 ×3 · #73·#71·#25·#63 ×2.

## ② 대조 표

상태는 기록만 대조해 정했다(판정 0). 원문 자체에 취소선/확정 각주/"— 닫혔다"가 있으면 **닫힘**(원문 명시).
그 외 `#NN`이 있으면 판정대기 사본(`지시/_판정대기_2026-09-27.md` — 볼드=열림·취소선=방금닫힘)과 판정기록
사본(`지시/_판정기록_2026-09-27.md` — 1~55 스냅샷 행)을 대조한다. `#NN`이 없거나 대조로도 못 정하면 카드
규칙대로 **열림**이 기본값이다. **낡음**은 원문 `[낡음? 확인 필요]` 표에만 매겼는데, 실측하니 그 문자열은
파일 범례 문장 한 곳에만 있고 실제 항목에는 **0건** 붙어 있었다.

- **ⓐ 판정대기에 없는데 회부는 열린 항목**: **0건**. 회부.md에 등장하는 `#NN`은 전부(11·12·22·25·26·29~34·
  47·60·63·67·69·71·73·74·76·77·78) 판정대기 사본(열림/방금닫힘) 또는 판정기록 사본(1~55) 어느 한쪽에 이미
  잡혀 있었다 — PM이 놓친 판정번호는 못 찾았다.
- **ⓑ 판정기록엔 닫혔는데 회부 원문은 열린 채인 항목**: **36건**(색인 ② 절에 행 번호 전부 나열). 원문은
  손대지 않았고(무변) 색인 표의 상태 칸에만 "닫힘"으로 반영했다.
- **ⓒ 판정자 "미지정" 항목**: **878건**(전수의 88%). 회부.md 대다수 항목이 판정자를 괄호로 안 적기 때문 —
  이 자체가 발견이다(판정자 표기 관행이 약하다). 행 번호는 색인 표에.

## ③ 역표

같은 파일 `## ③ 역표` 절 — 판정번호 #11~#78(등장한 것만 · 없는 번호는 안 찍음) → 회부 행, 카드 → 회부 행
(T424 문법 그대로 · 회부를 실제로 낸 카드만 실었다).

## `인계/README.md` · `지시/README.md`

`인계/README.md` 회부 행 옆에 `회부_색인.md` 포인터 한 줄 추가(낡은 줄 삭제 0). `지시/README.md`에 T438
날짜 줄 + 한 줄 답 추가.

## 검증

- `git diff --stat origin/main -- .` → `인계/README.md`(+1) · `지시/README.md`(+2) · `인계/회부_색인.md`(신규)뿐.
  `인계/회부.md`·`지시/_판정대기_2026-09-27.md`·`지시/_판정기록_2026-09-27.md` 전부 diff 0(원문 무변 확인).
  `T434`(세션12)·`T437`(세션8) 무접촉 확인.
- `node scripts/test-harness-lint.js` → **61/61 PASS**(변경 전후 동일).
- 코드 접촉 0 · 정본(`sim/`·`server/`·`public/client`) 무접촉.

## 한계(정직하게 적는다)

- 판정자 검출은 원문에 적힌 괄호/문구 그대로만 읽었다(추론 0) — 878건 "미지정"은 판정자가 실제로 안 적혔다는
  뜻이지, 누군지 몰라서 못 찾았다는 뜻이 아니다.
- 되풀이 집계는 `#NN` 공유로만 잡았다 — 번호 없이 같은 물음을 다른 말로 반복한 경우는(있다면) 못 잡는다.
- ⓐ가 0인 것은 "이 카드가 다루는 회부.md 범위 안에서" 그렇다는 뜻이다 — 회부.md 밖(예: 카드 지시서
  `지시_T*.md` 자체의 회부만 절)까지 뒤진 숫자는 아니다(카드가 읽으라고만 했고, 표에는 회부.md 전수만
  올리기로 스코프를 좁혔다 — 아래 참고).
