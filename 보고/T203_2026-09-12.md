# 보고 — T203: 등록 계정의 생애 첫 접속도 도착이다 + `founderName` 한 낱말

* **베이스** `965888a4`(origin/main · 리베이스 후 · 측정 시작은 `890c22ee`) · **헤드** `e5d28025` · 가지 `batch/first-arrival-0912`
* 접촉 **다섯**: `server/onboarding.js`(`arriveFor` 한 자리) · `server/zone.js`(넘기는 한 줄) · `server/villages.js`(한 낱말 + 영속) · `scripts/e2e-onboarding.js`(절 하나 + ⑦b) · `scripts/e2e-friends.js`(낡은 전제 둘). **클라 0줄**
* 새 수 0 · 새 컬럼 0 · 새 메시지 0 · **3시드 800일 A/B 전문 diff 0**

---

## §0-ⓐ `arriveFor` 호출자 전수 + 등록 계정 첫 접속 실측

| 물음 | 실측 |
|---|---|
| 호출자 | **하나** — `server/zone.js:3941` (접속 핸들러) |
| 그 자리가 무엇을 쥐나 | `_loadLastSeenDay`(`zone.js:3640` 선언 · `3841` 등록/게스트 복원 · `3703` 핸드오프) — **이미 쥐고 있었다** ⇒ 넘기는 한 줄이면 된다 |
| 종전 판정 | `acct.last_zone === zoneId && typeof acct.last_x === 'number'`(또는 `home_*`) = **좌표가 있나** |
| 왜 틀렸나 | central `/auth` 신규 가입 갈래가 **그 순간** `home_zone`·`home_x/y`·`last_zone`·`last_x/y` 를 박는다(`central.js` — *"신규 가입자에게 home + last_zone 동시 세팅 — 첫 입장은 home에서"*) ⇒ **가입 = 이어하기** |
| "처음 온 사람"의 정본 | `lastSeenDay`(T7 · `zone.js:412` *"없으면 처음 온 사람 — 복귀 브리핑은 안 나간다"*). 저장은 **늘** 그 칸을 쓴다(`_lastSeenDayToSave` · `zone.js:392`) ⇒ 한 번이라도 저장된 몸은 유한 · 가입만 한 몸은 `null` |

### ★빨강 먼저 — 고치기 전, 같은 하네스 절을 순정 베이스에서 돌렸다
```
✗ [T203] 등록 계정의 첫 접속이 도착 지점에 선다 — Δ∞ (도착 반경 안에 없다 — 마을 광장에 발생했다)
✗ [T203] 그리고 여행자로 온다 — 허기 100
✓ [T203] 게스트 첫 접속은 종전 그대로 도착 지점에 선다 — Δ0px      ← 게스트만 나루터에 왔다
✓ [T203] 게스트 시작 허기도 종전 그대로 — 56
```
⇒ 온보딩 v2 의 첫 문장("빈손으로, 배고픈 채 도착한다")이 **등록한 사람에겐 거짓**이었다.

## §0-ⓑ `founderName` 흐름 표

| 단계 | 자리 | 무엇을 하나 |
|---|---|---|
| 넘긴다 | `zone.js:8554` | `foundPlayerVillage({ …, founderName: player.name, … })` |
| **안 앉는다** | `villages.js:1284` | 그 값을 **마을 이름 짓는 데만** 쓴다(`"◯◯의 마을"`) — `econ` 에 안 적는다 |
| 읽는다 | `zone.js:2789` | `e.founderName = (vil && vil.econ && vil.econ.founderName) \|\| ''` ⇒ **영영 `""`** |
| econ 표 | `sim/economy-sim.js createVillage` | 화이트리스트에 `founder` 는 있고 `founderName` 은 **없다**(랩 파일 — 안 만진다) |

실측(T199): 마을 이름은 `"founder의 마을"`인데 시작 화면 칸은 `""`. **T178 `r.species` 와 같은 모양.**

---

## §1 만든 것 — 서버 세 자리

```
server/onboarding.js  arriveFor(…, lastSeenDay)
                      _hasPos && Number.isFinite(lastSeenDay)   ← 곱(좌표 항을 지우지 않는다)
                      인자 없으면 _seen = _hasPos               ← 옛 호출부와 글자 그대로 같다
server/zone.js        그 한 줄에 `_loadLastSeenDay` 를 넘긴다
server/villages.js    ev.founderName = opts.founderName         ← 읽는 칸에 앉힌다(한 낱말)
                      회관 행 data 에 founderName               ← T197 tribeId 문법 · 새 열 0
                      복원 시 있을 때만 ev 에 얹는다             ← NPC 마을엔 칸이 없다 ⇒ 랩 불변
```

| 약속 | 지켰나 |
|---|---|
| 새 수 0 · 새 컬럼 0 · 새 메시지 0 | ○ |
| 클라 0 | ○ — `public/` 접촉 0 |
| **게스트 비트 동일** | ○ — 좌표가 생기는 그 저장이 곧 `lastSeenDay` 를 쓰는 저장이라 두 항이 함께 선다(하네스가 잰다) |
| 진짜 이어하기 | ○ — 떠나 있던 자리로 돌아온다(896px · 도착으로 다시 안 끌려간다) |
| `start_vid` 기록(T115) | ○ — `e2e-friends ④` 초록(`friendsHere 1` · `bob 시작마을 2 vs 2`) |
| 첫 의뢰 가드·`RECOMMEND_POP` | ○ 무접촉(#18·#19 재민 몫) |
| econ 랩 | ○ — **3시드 800일 전문 diff 0** |

---

## §2 검증

### ⓒ 하네스 걸음 표 — `e2e-onboarding` 새 절(8건)
| 걸음 | 전(순정) | **후** |
|---|---|---|
| 등록 첫 접속 자리 | ✗ 도착 반경 밖(마을 광장) | **✓ Δ0px** `(14832,78704)` = 도착 |
| 등록 첫 접속 허기 | ✗ 100 | **✓ 56**(§9.4 결핍 ①) |
| (상황) 끊기 전에 도착을 떠났다 | — | ✓ 900px |
| 등록 둘째 접속 = 이어하기 | ✓ | **✓** 떠나 있던 자리(도착에서 896px) |
| (상황) 둘째 접속 허기 = 저장본 | — | ✓ 56(시작값 재적용 0) |
| 게스트 첫 접속 자리 | ✓ Δ0px | **✓ Δ0px** |
| 게스트 첫 접속 허기 | ✓ 56 | **✓ 56** |
| 돌아온 게스트 | ✓ | **✓** 허기 56(이어하기) |

| 하네스 | 결과 |
|---|---|
| `e2e-onboarding`(기본) | **PASS 74 / FAIL 0** (T203 절 8건 신설) |
| `e2e-onboarding`(`ARC_FIRST=1 ARC_FOUND=1`) | **호 20걸음 중 20 초록** — ⑦b `[T203] 시작 화면에 창설자 이름이 있다` ○ `founderName = "arcfounder"` · 마을 이름 `"arcfounder의 마을"` |
| `e2e-friends` | **80 / 0** (④ `start_vid` 표지 초록) |
| `test-newcomers` | **56 / 0** |
| `test-harness-lint` | **31 / 0** |
| **3시드 800일 A/B** | `LAB_SEED=1020` diff **0** · `7` diff **0** · `42` diff **0** (출력 통째 diff · `econ-lab-real 800`) |
| `node --check` | 다섯 파일 통과 |

### ★하네스가 같이 배운 것 둘 — 제품이 옳고 하네스가 낡았다
`e2e-friends` 가 순정 80/0 → 내 판에서 **75/5** 로 떨어졌다. 귀속을 갈랐다:
1. **② `sleep(1500)`** — bob 이 이제 **다른 마을 어귀**에서 태어나 그 소켓에 청크 방송이 먼저 흐른다.
   쌍은 섰는데(②b·②c 초록) 알림만 빈 배열이었다 ⇒ **들을 때까지** 기다리게 고쳤다(공통 §2 ⑩).
2. **③ 전제** — "alice 시야에 bob 과 carol 이 둘 다 있다"는 **셋이 같은 광장에 나던 시절**의 전제다
   (실측 `bob false · carol true`). 시야 표지를 재려면 시야 안에 있어야 하고, 그건 **하네스가 만들 상황**이다
   ⇒ `teleport_debug` 로 모아 놓고, 볼 때까지 기다린다. → **80/0 복구**

---

## §3 회부 (고치지 않았다)
첫 의뢰 정본(#18 재민) · 갓 선 유저 마을의 추천 자격(#19 재민) · 16방향 · 이송 실체 · 알림 이력 패널(캐논) · `nippon` 시딩(Z).

## §4 실기 1줄 (재민)
새로 가입한 계정으로 마을을 골라 들어가면 **나루터에, 배고픈 채** 서는가.

## §5 푸시 — **main 까지 갔다** (정본 2판 · 맥 VM)
```
작업 클론   $HOME/pushrepo (--depth 200 --branch main --single-branch · 맥 VM device_bash)
            ~/mnt/Mini/durango-mini 무접촉
가지        batch/first-arrival-0912 → e5d28025   (BRANCH_OK)
main        e5d28025                               (PUSHED_MAIN · ff 확인 두 번)
원격 main   e5d28025 (푸시 뒤 재확인)   ·   force 0   ·   .lock 없음
번들        ~/Mini/_inbox/T203_2026-09-12.bundle (965888a4..e5d28025 · 증거)
리베이스    T208(`696e2a5d`)이 같은 `e2e-friends`·`zone.js` 를 만졌다 — 충돌 0, 그리고 겹친 파일
            하네스를 **다시 돌렸다**: `e2e-friends` **93 / 0**(T208 의 13건 + 내 절 전부 초록)
```
