# T383 — 밤 빨강 둘은 **둘 다 자의 결함**이었다 · 제품 **0줄** · `test-terraincache` **26/0** · `test-site-memo` **22/0**(새 씨앗) · `nightly-known` 올릴 것 **0**

**[승인 대기 · T383]** 제품 **무접촉**(`server/`·`sim/` diff **0**) · 하네스 둘(`scripts/test-terraincache.js` +81/−10 · `scripts/test-site-memo.js` +48/−2) ·
새 수 **0** · 손잡이 신설 **0**(고정한 셋은 전부 제품의 **되돌림 문**) · T381·T378·T374·T385 무접촉 · 실기 0.

* **① `test-terraincache` = 자의 결함.** `faae1b04`[T345 · 09-22]가 걸음당 클로저를 없애려고 캐시 가지의 terrain 호출을
  모듈 수준 함수(`_computeWaterCell`·`_computeRockCell` · `zone.js:969·970`)로 **한 칸 옮겼다** — 본문엔 호출이 하나만 남는다.
  자는 여전히 "본문에 두 번 · 글자까지 같게" 를 셌다. 계약(캐시가 다른 점을 묻지 않는다)은 **그대로 참**이다 —
  실제 zone.js 두 판(캐시 켬/끔)이 같은 1,000점에 **비트 동일**(물 0/1000 · 바위 0/1000 다름)이고, 셀 중심 대신 모서리를 묻는
  미끼는 같은 표본에서 **물 13 · 바위 3 점**을 가른다. ⇒ 정적 절을 T345 모양(캐시 가지 → 계산 함수 → 셀 중심)으로 옮겼다.
* **② `test-site-memo` = 자의 결함(픽스처).** 대조군은 **다른 코드를 돈다**(env 는 도착한다 — `LIFE_SITE_MEMO=0` 팔은 스킵 0).
  두 팔이 같아진 까닭은 **기억이 아낄 헛수고가 세계에서 사라진 것**이다: `8441adaa`[T298 · 09-19]가 방아쇠(`T219_HOUSE_TRIGGER`)를
  기본 켬으로 돌려 **모자랄 때만 훑게** 됐고(첫 빨강 밤 09-20 · 기준 `8f12f6e1` 에 T298 **포함** · 직전 초록 밤 09-19 `b0696b94` 엔 **없음**),
  `c85eab41`[T326]의 간격 15 와 T342 의 거부 캐시 수리가 빈손을 더 줄였다. 제품은 옳다 — **일이 위층으로 옮겨 갔을 뿐**이다.
  ★로컬에서 안 보인 까닭: 씨앗 캐시(`/tmp/slicer-seed-sitememo-*`)가 **09-03 에 만든 포화 세계**라 T342 의 19/0 이 그 위였다.
  같은 하네스 · 두 씨앗: **옛 씨앗 19/0 · 새 씨앗 14/5**(밤과 같은 모양). ⇒ 손잡이 셋(`T230`·`T219`·`T315` = `'0'` · 전부 되돌림 문)을
  **한 표(`PINS`)** 로 씨앗과 팔에 같이 주고, 씨앗 꼬리표에 **제품 지문**(파일 넷 + 표의 해시)을 붙였다 ⇒ 새 씨앗 **22/0**.
* **③ `e2e-verbs`** — 표만. 밤엔 이름순으로 `e2e-tilestate` 바로 뒤, **같은 포트(3010/3020)**에서 돌았다. 단독과 다른 것은 앞 판이 남긴
  2코어 부하와 "220px 안 NPC" 를 기다리는 60초 창이다 — 같은 절이 T214 에서 이미 "러너 청크 빨강 · 단독 초록"으로 적힌 자리다.
* **④ 한 줄 답** — 둘 다 **자**였고 둘 다 **고쳤다**. 제품 결함 0. `nightly-known.txt` 에 올릴 것 **0**
  (`e2e-verbs` 는 1밤이라 T304 규약의 "연속 2밤" 문턱 아래 — 올릴지 말지는 PM).

---

## ⓐ `test-terraincache` — 귀속 표

| 항목 | 값 |
|---|---|
| 빨강 넷 | `isWaterTileLocal`·`isRockTileLocal` 각각 "terrain 호출 = 캐시 가지 1 + 종전 가지 1 = 2개 → **1개**" · "두 가지 인자식 글자 동일" |
| 지금 본문 | `zone.js:971` `isWaterTileLocal` · `zone.js:1003` `isRockTileLocal` — 캐시 가지는 `_TERR_CACHE.water(tx, ty, _computeWaterCell)` 한 줄 |
| 계산 함수 | `zone.js:969` `_computeWaterCell = (tx, ty) => _terrain.isWaterCellLocal(ZONE_ID, tx*32+16, ty*32+16)` · `:970` 바위 같은 꼴 |
| 바꾼 카드 | `git log -L`: **`faae1b04` T345(09-22)** — "걸음당 클로저 0"(그 전 `8c266bc4` T333 은 비트 색인 · `9b61d49f` T324 는 기본 켬) |
| 첫 빨강 밤 | **09-23**(T345 착지 다음 밤) → 09-24 → 09-25 = **연속 3밤** ✓ (09-21 의 빨강은 다른 절 — T324 기본 켬 뒤 옛 `=== '1'` 글자 · 이미 닫힘) |
| 밤 러너의 후보 `94df1ef`(T347) | **아니다** — 그 커밋은 `보고/T347` **문서 하나**만 바꿨다 |
| 계약이 아직 있어야 하나 | **예**(캐시가 다른 점을 묻지 않는다) — 다만 그 자리가 본문이 아니라 **계산 함수**로 옮겨 갔다 |
| **판정** | **자의 결함** — 자가 T345 이전 모양을 글자로 떴다 |

**동작 게이트(자가 원래 재려던 것)** — 실제 `zone.js` 를 자식 둘로 띄운다(`TERRAIN_TILE_CACHE` 는 적재 때 읽힌다 · `PORT=0` 으로 포트 충돌 0):

| | 값 |
|---|---|
| 표본 | 1,000점(무작위 400 + 강 가장자리 300 + 산 가장자리 300 · 픽셀 좌표 · 타일 안 오프셋 포함) |
| 끈 판 답 분포 | 물 197/803 · 바위 189/811 (참·거짓 둘 다 넉넉 — 자명 통과 아님) |
| 켬 ↔ 끔 다른 점 | **물 0/1000 · 바위 0/1000** |
| 미끼(셀 중심 대신 모서리) | 같은 표본에서 **물 13 · 바위 3 점**이 갈린다 ⇒ 위 0 은 공짜가 아니다 |
| 정적 절(새 모양) | 캐시 가지 → 모듈 수준 계산 함수 → 셀 중심 `(ZONE_ID, tx * 32 + 16, ty * 32 + 16)` · 미끼 소스(`tx * 32`)를 **거절** |
| 결과 | **PASS 26 / FAIL 0**(종전 18/4) |

## ⓑ `test-site-memo` — 귀속 표

| 항목 | 값 |
|---|---|
| 빨강(밤 09-25) | ① 종전 판 훑기 19회/9일(<45) · 빈손 0 · ③ 스킵 0 · 훑은 셀 111,900 < 111,900 · ④ 스킵 0 |
| 대조군 팔이 다른 코드를 도나 | **예** — env 는 `boot()` 의 `Object.assign(process.env, env)` 로 도착한다(`LIFE_SITE_MEMO=0` 팔은 스킵 0 · 채택 팔은 옛 씨앗에서 스킵 21) · `4d7d8101`[T355] 은 **기동 증인만** 바꿨다 |
| 첫 빨강 밤 | **09-20**(기준 `8f12f6e1`) — 직전 초록 밤 09-19(기준 `b0696b94`) |
| 그 사이 착지 | **`8441adaa` T298**(영토+방아쇠 **기본 켬**) · T315(손잡이 끔 · 무변) 등 — `c85eab41` T326 은 **그 밤 뒤** |
| 밤 러너의 후보 | `4d7d8101`(T355) — 첫 빨강 **뒤**다 · `94df1ef`(T347) — 문서만 |
| 제품이 안 건너뛰나 | **아니다** — 감사(②)는 매 판 위반 0 · 기억 코드는 T342 뒤로 **무변**(`git log -G` 0건) |
| 왜 같아졌나 | 방아쇠가 **모자랄 때만** 훑게 해 헛수고가 먼저 사라졌다 + 간격 15·거부 캐시 수리로 빈손이 드물다 |
| 로컬 초록의 정체 | 씨앗 캐시가 **09-03** 의 포화 세계(빈손 33/47) — 제품이 바뀌어도 안 새로 길렀다 |
| **판정** | **자의 결함(픽스처)** — "마을이 제 땅을 다 쓴 세계" 라는 상황을 세계 운에 맡겼고, 씨앗이 낡아 가렸다 |

**같은 하네스 · 씨앗만 바꿔 잰 사슬**(`SITE_SEED_TAG` 로 씨앗 고정 · 각 한 판)

| 판 | 씨앗 | 팔 고정 | 훑기/9일 | 빈손 | 스킵 | 결과 |
|---|---|---|---:|---:|---:|---|
| 옛 씨앗 | 09-03 포화 | T230 | 47 | 33 | 21 | **19/0**(T342 가 본 그 초록) |
| 새 씨앗 | 오늘 코드 · 기본 | T230 | 17 | 1 | 0 | **14/5**(밤과 같은 모양) |
| 새 씨앗 | 〃 | T230 + **T219** | 38 | 2 | 1 | 17/4 |
| 새 씨앗 | 〃 | T230 + T219 + **T315** | 41 | 7 | 5 | 18/4 |
| **고친 판** | **같은 표로 기른 씨앗 · 지문 꼬리표** | **`PINS` 셋** | **279** | **278** | **211** | **22/0** |

고친 판의 값: 종전 집터 단계 **351ms → 채택 6ms** · 훑은 셀 1,921,650 → 465,750 · 감사 43,408회 **위반 0** · 안전망 하루 12.7곳.

**자명 통과 반례** — 같은 새 씨앗에서 **고정 없는 자 = 14/5**, **고정한 자 = 22/0**: 고친 자는 옛 결함 모양(방아쇠·간격이 헛수고를 지운 세계)을
① 절이 그대로 **거절**한다. 그리고 ⑤ 에 문 절 셋을 새로 걸었다 — 제품의 되돌림 문이 `!== '0'`·`=== '0' ?` 가 아닌 글자로 바뀌면
`'0'` 이 아무것도 안 끄는데, 그때 이 절이 빨개진다(미끼 `=== '1'` 을 거절 · 확인).

## ⓒ `e2e-verbs` — 순서 표(고치지 않는다)

| 항목 | 값 |
|---|---|
| 빨강 | `[상황] 화면 안의 그 사람에게 메뉴가 열렸다 — 아무도 안 눌렸다`(81줄 중 하나 · 18분) · 단독 재실행 **초록** |
| 밤 순서 | 묶음 B · **이름순** — 바로 앞 `e2e-tilestate`(통과) · 바로 뒤 `e2e-waterperf`(통과) |
| 포트 | 셋 다 **3010/3020** 을 같이 쓴다 — 앞 판의 잔여 프로세스는 `fixture-boot.waitUp`(T349·T355)이 "아이의 입"으로 가른다 |
| 단독과 다른 것 | 앞 판이 남긴 **2코어 부하**와, `_nearNpc`(220px 안 NPC)가 설 때까지의 **60초 창**(`tapUntilMenu` 상한) |
| 이력 | 같은 절이 **T214** 주석에 "러너 4종 청크에서 세 판 연속 빨강 · 단독 초록(77/0)"으로 이미 적힌 자리 |
| 한 줄 | **밤의 순서가 만든 부하 간헐**이다 — 제품·자 어느 쪽도 이번 판으로 못 가른다(1밤) · 고치지 않는다 |

## ⓓ 한 줄 답 + `nightly-known` 표

**둘 다 자였고 둘 다 고쳤다 — 제품 0줄.** terraincache 는 T345 가 옮긴 호출을 자가 못 따라갔고, site-memo 는 T298 이 헛수고를
위층(방아쇠)으로 옮긴 세계에서 픽스처가 상황을 못 만들었으며 낡은 씨앗이 그걸 로컬에서 가렸다.

| 하네스 | 올릴까 | 이유 |
|---|---|---|
| `test-terraincache` | **아니오** | 귀속됐고 고쳤다(26/0) |
| `test-site-memo` | **아니오** | 귀속됐고 고쳤다(22/0 · 씨앗 지문으로 재발 차단) |
| `e2e-verbs` | **아니오**(PM 판단) | 1밤 · 단독 초록 — T304 문턱(연속 2밤) 아래 |

**올릴 것 0.**

## 회부만

1. `nightly-known.txt` — 올릴 것 0(PM).
2. **집터 메모의 값 자체**(재민 · T342) — 출하 세계(방아쇠 켬 · 간격 15)에서는 새 씨앗 9일에 빈손이 **1/17** 이다.
   기억은 옳지만 **아낄 헛수고가 거의 없다** — T41 이 풀려던 문제(거부의 92%가 `<18`)를 T298·T326·T342 가 위에서 풀었다.
   기억을 남길지 걷을지는 이 하네스가 묻는 것이 아니다.
3. 씨앗 지문은 **이 하네스 꼬리표에만** 걸었다(`slicer-seed.js` 무접촉) — `test-tick-slicer` 의 기본 씨앗도 같은 낡음에 노출돼 있다(세션7 몫 · 한 줄).
4. #54 40 풀기(재민 · T376 표).

## 게이트

| 게이트 | 결과 |
|---|---|
| 제품 무접촉 | `git diff -- server/ sim/` = **0줄** · 3시드 면제 |
| `test-terraincache` | **26 / 0**(종전 18/4) |
| `test-site-memo` | **22 / 0**(새 씨앗 · 종전 같은 조건 14/5) |
| `test-harness-lint` | **61 / 61** |
| `lab-wiring-check` | **실패 0 · 경고 0** |
| 접점 심볼 | `isWaterTileLocal`·`isRockTileLocal`·`terrain-tilecache`·`test-terraincache`·`test-site-memo`·`fixture-boot`·`nightly-known`·`야간러너`·`e2e-verbs` — 제품은 **읽기만** |
| force / `.lock` | **0 / 0** |
