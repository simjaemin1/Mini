# T402 — why = damagePlayer 출처 · 남이 쓰러지는 소리 (세션9 · 2026-09-26)

## 결론 (머리 15줄)

1. **한 줄 답** — 서버가 `damagePlayer` 출처의 앞 낱말(여섯 + `wild`)을 `why` 로 실어, 늑대·화살·주먹·낙하에 맞으면 `hit_body` 가 울고 추위 극단 감소는 0번 울며, 곁 사람이 쓰러지면(`player_down_state why:down`) `downed` 가 울고 업힘·내려놓음·재접속은 안 운다.
2. **가지 해시** `____` (가지 `batch/sfx-why-0926`) · **`[승인 대기 · T402]`** · **베이스** `c445983c`.
3. ★**서버 여섯 줄**(카드 다섯 + 하나): `damagePlayer` 한 줄 + `player_down_state` 네 줄 + **`HP_PEER` 한 줄**. 여섯째는 뺄 수 없었다 — `setHp` 는 `HP_PEER.has(why)` 로 방송을 정하는데, `why` 가 `'damage'` 에서 `'mob'` 등으로 바뀌면 **다침이 방송에서 빠진다**(곁 사람 HP 바가 안 준다). 종전에 `damage` 로 방송되던 낱말만 더했다(`e2e-hp ⓪` 가 호출부에서 긁어 대조 · 규약 무변).
4. ★**출처 낱말은 여섯이 아니라 일곱이다** — `wildlife.js:801` 의 `'wild:<행동>'`(야생 습격)을 카드가 빠뜨렸다. `hit_body` 로 보냈다.
5. ★**카드의 `player_down_state` 네 자리 중 4992 는 쓰러짐이 아니다** — 재접속 복원(바로 위 `player_downed` 가 `source:'relogin'`)이다. `down` 을 달면 남이 **접속할 때마다** 쓰러지는 소리가 난다 ⇒ 옆 줄의 그 낱말 `relogin` 을 달았다. 네 낱말: `relogin`(4992) · `down`(10651) · `carried`(10961) · `set`(10982). 깨어남(10999 · `isDown:false`)은 무접촉(낱말 없음 = 안 운다).
6. 층: 표 `hpWhy` = arrow·mob·player·fall·wild → `hit_body` · extreme·food 없음. 표 `combat` 에 `player_down_state → downed` + 새 표 `combatOnly`(`why:'down'` · `_남만` — 내 쓰러짐은 `player_downed` 가 이미 운다).
7. 미끼: `why:extreme` 열 번 = **0** · 옛 전문·먹기·구조 = 0 · 업힘·내려놓음·재접속·일어남·낱말 없음 = 0 · 내 `down_state` = 0.
8. 진짜 수신 길(`handleMessage` 머리): 같은 존 p2 쓰러짐 **1** · 재접속 0 · 관전 연결 p1 쓰러짐 0(`e2e-pid-collide ⑥`).
9. 새 확보 0 — T397 의 `hit_body` 1번을 배선만(2번 후보 그대로). 출처 없는 소리 0 · `bgm/`·`30-n-net.js` 무접촉 · 기존 41키 중 `hit_body` 한 키만 바뀜(후보 → 배선 · 파일 무변).
10. `hpWhy.msgType` → `_msgType` 개명: 표가 배선 표가 되자 그 값(`hp_changed`)이 키로 읽혀 ⑦a 가 물었다.
11. 하네스: `test-audio` 158→**160** · `e2e-audio-probe` 58→**61** · `e2e-pid-collide` 10→**13** · `e2e-aim` **15/0** · `e2e-hp` **26/0**(두 줄 고침 — 아래) · `test-body` **306/0** · globals 26 · lint 61 · assets-audit PASS.
12. ⚠흔들림 둘(회부): `test-body` 가 `e2e-aim` 과 병행한 판 304/2 → 단독 두 번 306/0 · `e2e-hp ①b` 가 한 판 25/1(`hp_changed` 가 3.9초 걸려 오는 사이 만복 회복이 화면을 63 으로 올림 · `debug` 경로 = 내 줄 무관) → 다음 판 26/0.
13. 빠진 소리 전수 **170자리 · 있음 40**(T397 38 + `hp_changed` · `player_down_state`).
14. 실기: 재민 배포 뒤 늑대에게 맞아 보기 · 곁 사람 쓰러짐(둘이 있을 때).

---

## ⓐ 호출부 표 + 서버 줄 diff

**`damagePlayer(p, dmg, source)` 호출부 전수**(`grep damagePlayer(` · 서버 둘 파일):

| 자리 | 출처 | `why`(앞 낱말) | 나감 | `hpWhy` |
|---|---|---|---|---|
| `zone.js:4086` `/cross_damage` | `arrow:<공격자>` | `arrow` | 방송 | `hit_body` |
| `zone.js:5843` 독 음식 | `food:<item>` | `food` | 방송 | 안 운다(먹어 회복과 같은 낱말) |
| `zone.js:10282` 화살 명중 | `arrow:<id>` | `arrow` | 방송 | `hit_body` |
| `zone.js:10456` 근접(PvP) | `player:<이름>` | `player` | 방송 | `hit_body` |
| `zone.js:12298` 낙하 | `fall` | `fall` | 방송 | `hit_body` |
| `zone.js:12359` 극단 감소 | `extreme:<축>` | `extreme` | 방송 | **안 운다**(추위 3.6초마다 1HP) |
| `zone.js:12582` 짐승 | `mob:<종>` | `mob` | 방송 | `hit_body` |
| **`wildlife.js:801` 야생 습격** | `wild:<행동>` | `wild` | 방송 | `hit_body` — ★카드 누락 |

**diff(서버 · 6줄)**:
```
- const HP_PEER = new Set(['damage', 'food', 'dish', 'debug']);
+ const HP_PEER = new Set(['damage', 'food', 'dish', 'debug', 'arrow', 'mob', 'player', 'fall', 'extreme', 'wild']);
-   setHp(p, p.hp - dmg, 'damage');
+   setHp(p, p.hp - dmg, String(source || 'damage').split(':')[0]);
-     broadcast({ type: 'player_down_state', pid: player.pid, isDown: true });                       (4992 · 재접속)
+     broadcast({ type: 'player_down_state', pid: player.pid, isDown: true, why: 'relogin' });
-     broadcast({ type: 'player_down_state', pid: p.pid, isDown: true });                            (10651 · 쓰러짐)
+     broadcast({ type: 'player_down_state', pid: p.pid, isDown: true, why: 'down' });
-   broadcast({ …, isDown: true, carriedBy: rescuer.pid });                                          (10961 · 업힘)
+   broadcast({ …, isDown: true, carriedBy: rescuer.pid, why: 'carried' });
-     broadcast({ …, isDown: !!target.isDown });                                                     (10982 · 내려놓음·놓침)
+     broadcast({ …, isDown: !!target.isDown, why: 'set' });
```
`HP_PEER` 줄의 이유(머리 3): 이 줄이 없으면 다침이 자기에게만 간다. `e2e-hp ①`(다침이 **방송으로** 400ms 안에 온다)이 실제 서버로 확인 — 3199ms 판에서 방송 ○.

**남의 하네스 두 줄**(`e2e-hp.js` · HP 문 T109/T131): ⓪ "방송 갈래 = 종전 넷" → "종전 넷 + 피해 출처 낱말(호출부에서 긁음)" · ④ `setHp(p, p.hp - dmg, 'damage')` 철자 → 새 줄 철자. 둘 다 **같은 불변식**(다침은 방송이다)을 새 글자로 잰다. 첫 판 25/1(④) → 고친 뒤 26/0.

## ⓑ `hpWhy` 표 + 극단 0 미끼

| `why` | 키 | 근거 |
|---|---|---|
| `mob` · `wild` · `arrow` · `player` · `fall` | `hit_body` | 맞음 |
| `extreme` | — | 추위·갈증·허기 극단 감소 — 소리가 아니다 |
| `food` · `dish` · `rescue` · `debug` | — | 회복·구조·시험(독 음식도 `food` — 가를 낱말 없음) |
| `regen` · `respawn` · `takeover` | — | 원래 안 나간다(HP_QUIET) |

`e2e-audio-probe`: ㉟ `mob` 1 · ㊱a extreme·옛 전문·food·dish·rescue 0 · ㊱b **extreme 열 번 0** · ㊱c 관전 p1 0 · ㊱d 남(p2) 화살 1(그 자리 · 거리 감쇠). `test-audio ⑯b2` 가 `extreme`·`food`·`damage` 가 표에 들면 빨개진다 · ⑯b5 가 맞음 낱말이 서버 호출부에 실제로 있는지 본다.

## ⓒ `player_down_state` 네 줄 + 남의 쓰러짐

| 자리 | 사건 | `why` | 층 |
|---|---|---|---|
| 4992 | 쓰러진 채 재접속(복원) | `relogin` | 0 |
| 10651 | **쓰러짐**(`damagePlayer` hp 0) | `down` | **`downed`**(남만 · 그 자리) |
| 10961 | 업힘 | `carried` | 0 |
| 10982 | 내려놓음·놓침(`_dropCarried`) | `set` | 0 |
| 10999 | 깨어남(`_wakeUp` · `isDown:false`) | (무접촉) | 0 |

`e2e-audio-probe ㊴`: p2 down 1 · carried/set/relogin/일어남/낱말 없음 0 · 내 down 0. `e2e-pid-collide ⑥`(진짜 `handleMessage` 길): 같은 존 p2 down **1** · relogin 0 · 관전 B 의 p1 down 0.

## ⓓ 한 줄 답

**늑대·화살·주먹·낙하·야생 습격에 맞으면 `hit_body` 가 울고(추위 극단 감소는 0), 곁 사람이 쓰러지면 `downed` 가 그 자리에서 울며(업힘·내려놓음·재접속 0) — 서버는 이미 있던 출처 낱말과 호출부 이름을 나르는 여섯 줄이다.**

---

## §3 검증

| 자 | 전 | 후 |
|---|---:|---:|
| `test-audio` | 158 | **160/0** (⑮a 다섯 · ⑮d0 · ⑯b 새 전제 · ⑯b5) |
| `e2e-audio-probe` | 58 | **61/0** (㉟ ㊱a~d ㊴a~c · ㉞ 다섯) |
| `e2e-pid-collide` | 10 | **13/0** (⑥a~c) |
| `e2e-aim` | 15 | **15/0** |
| `e2e-hp` | 26 | **26/0** (⓪·④ 철자 갱신 · 세 판: 25/1 ④ 철자 → 25/1 ①b 시계 → 26/0) |
| `test-body` | 306 | **306/0** (단독 두 번 · 병행 판 304/2) |
| `test-client-globals` · `test-harness-lint` · `test-assets-audit` | 26 · 61 · PASS | 26 · 61 · PASS |

**접점** `damagePlayer` · `setHp` · `source` · `HP_PEER` · `hp_changed` · `why` · `hpWhy` · `_msgType` · `hit_body` · `player_down_state` · `carriedBy` · `relogin` · `combatOnly` · `downed` · `48-a-audio` · `e2e-pid-collide` · `e2e-audio-probe` · `e2e-hp` · `sfx-census`.

## §4 회부

1. **[PM] 서버 여섯째 줄**(`HP_PEER`) — 카드 다섯 줄 밖이다. 빼면 다침이 방송에서 빠진다. 받을지 판정.
2. **[재민] 독 음식** — `food:<독>` 피해와 먹어 회복이 같은 `food` 다. 맞음 소리를 원하면 낱말이 하나 더 필요하다(서버).
3. **[세션?] 흔들림 둘** — `test-body` 병행 304/2(단독 306/0 · 어느 줄인지 못 잡음) · `e2e-hp ①b` "화면이 깎인 값(< 60)" 은 `hp_changed` 도착이 3.2~3.9초로 흔들려 만복 회복(초당 ~10hp)에 먹힌다 — 문턱이 도착 시각에 달렸다.
4. T397·T387 회부 그대로 — #60 둘째 귀 판정 · `war_*` · `self_stat` · 깨어나는 숨·눈 녹음 · CC-BY 표시 · 천둥 사건 · 남 pid 전역 지도(`downStates`) 존 겹침.
