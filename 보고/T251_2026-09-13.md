# T251 — 두 자가 다른 땅을 세운 이유: **`deps` 한 칸**. 그리고 **기준선 자는 제품 땅에 서 있었다** (세션5 · 2026-09-13 · **가지만**)

> 카드 `~/Mini/지시_T251.md` · 가지 `batch/hunt-wood-server-0912` · **측정 베이스 `c35bdf72`**
> 만진 것: `scripts/t222-life800.js`(술어 배선) · `scripts/zone-preds.js`(신설 · 계측기 전용) ·
> `scripts/t251-land-diff.js`(신설 · 계측기) — **제품 코드 0** · `t176-ab` **무접촉**.
>
> **답 한 줄 — 갈리는 자리 = `makeTerrainAdapter(terrain, ZONE, deps)` 의 **`deps` 입력**(함수도 시딩도 아니다) ·
> 갈리는 키 **2개**(`fertility` 40/50 · `water` 43/50; 파생 `woodSustain` 17/50) ·
> **제품 정본 = `zone.js:570~650` → `zone.js:2651` → `villages.js:2501`** ·
> 실서버 대조 **실기 대기**(재민 · §0ⓒ 한 줄) · **기준선 자 제품 땅 ○**(50마을 중 39곳 전 열 동일 · 중앙 fert 0.99 vs 제품 1.00).**

---

## §0ⓐ 두 경로를 코드로 가른다 — 갈림은 **땅을 세우는 두 줄이 아니다**

| | 제품 (`Villages.init`) | 기준선 자 (`t176-ab`) | 헤드리스 자 (T232 판) |
|---|---|---|---|
| 술어를 **만드는** 곳 | `server/zone.js:570·599·643` | `scripts/t176-ab.js:218~226` | `scripts/t222-life800.js`(T232) |
| 술어를 **넘기는** 줄 | `zone.js:2651` `SimVillages.init({… isTerrainBlockedLocal, isWaterTileLocal …})` | `t176-ab.js:226` `P.makeTerrainAdapter(T, ZONE, {…})` | `deps` 리터럴 |
| 어댑터 | `villages.js:2501` `makeTerrainAdapter(terrain, ZONE, deps)` | 같은 함수 | 같은 함수 |
| 시딩 | `villages.js:1142~1156` | `t176-ab.js:234~241` | 제품 줄(같다) |

시딩 네 줄(`findOpenCenter` → `prepareFert(…,62)` → `VillageLayout.generate(ta, …, INITIAL_POP, {})` →
`extractLandParamsApprox`)은 **세 경로가 바이트 동일**하다. 갈림은 그 위 **한 칸**, `deps` 다.

### 그 한 칸이 어떻게 비옥도를 반으로 만드나 (코드 사슬)

```
deps.isTerrainBlockedLocal ─→ ta.isBlocked   (villages.js:148~157)
deps.isWaterTileLocal      ─→ ta.isWater     (villages.js:158~162)
       │
       ├─→ village-layout.generate      : 영토 확장·부지 판정 (village-layout.js:61·117·130·138·145)
       ├─→ ta.prepareFert → fertility.buildField({ isWater, isRock, … })   (villages.js:194~200)
       └─→ extractLandParamsApprox      : `ta.nearestWaterDist` → land.water (villages.js:405~407)
                                          `ta.fert` 영토 평균 × 1.4 → land.fertility (villages.js:413~417)
```

`server/fertility.js:45~54` 의 셀 식은
`0.12 + 0.62·exp(−dw/80) + 0.18·min(1, dr/60) + 0.08·임상` 이다.
T232 판은 `isWaterTileLocal` 이 픽셀 인자에 `Math.floor(x/32)` 를 넣어 **물을 사실상 못 보고**(dw→∞),
`isTerrainBlockedLocal` 이 `() => false` 라 **영토가 물·바위를 뚫고 자란다**. 그러면 물 항이 통째로 사라져

* 숲 없는 마을 `(0.12 + 0.18) × 1.4 = **0.42**`
* 숲 있는 마을 `(0.12 + 0.18 + 0.08) × 1.4 = **0.53**`

가 **정확히** 나온다 — 실측 표의 0.42·0.53 과 소수점까지 맞는다.
`land.water` 도 같은 이유로 전부 바닥값 `max(0.05,…) × 1.6 = **0.08**` 이다.

> ⚠**T247 §0 의 내 추측을 정정한다.** "옛 근사 갈래(`0.4 + water·0.9 − rockD·1.5`)로 떨어졌다"가 아니다.
> 비옥도 **장은 정상적으로 깔렸고**(diff 표: B 도 `fertField` 51곳), 그 장에 **물이 없었을 뿐**이다.

### 언제 갈렸나

| 커밋 | 날 | 무엇 |
|---|---|---|
| `620261a1` | 2026-09-11 · T176 | 기준선 자(`t176-ab`)가 술어 두 개를 **자기 안에 적었다**(해안선 띠·다리 없음) |
| `c5b6d9d0` | 2026-09-13 · T222 | 헤드리스 자 신설 — `isTerrainBlockedLocal: () => false`, `isWaterTileLocal` 인자 오용 |
| `0f6aa5e2` | 2026-09-13 · T228 | 빈 `deps` 를 이름으로 세면서 이 칸을 **"이을 정본이 없다"로 분류**(그 판단이 틀렸다) |

## §0ⓑ 51마을 땅 값 전수 diff — 세 벌의 술어

`scripts/t251-land-diff.js` 가 한 프로세스 안에서 `deps` 만 갈아 끼우고 제품 시딩 줄을 그대로 부른다.

* **C = 제품 정본** — `zone.js:570~650`(해안선 물 띠 393,686타일 + 강·호수 · 바위 **타일 중심** · 다리 836셀 통행)
* **A = 기준선 자** — `t176-ab.js:218~226`(강·호수만 · 바위 **픽셀 그대로** · 다리 없음)
* **B = T232 헤드리스 자** — `isTerrainBlockedLocal: () => false`

| | 선별 | 세움 | 중심 없음 | `fertField` |
|---|---|---|---|---|
| C 제품 정본 | 51 | **50** | 1 (`어촌6` — 해안선 띠가 뭍을 덮는다) | 50곳 |
| A 기준선 자 | 51 | 51 | 0 | 51곳 |
| B T232 | 51 | 51 | 0 | 51곳 |

| 키 | C 중앙 | A 중앙 | B 중앙 | C 범위 | **A≠C** | **B≠C** |
|---|---|---|---|---|---|---|
| `fertility` | **1.00** | 0.99 | **0.42** | 0.30~1.26 | 11/50 | **40/50** |
| `water` | 1.36 | 1.26 | **0.08** | 0.08~1.59 | 2/50 | **43/50** |
| `woodSustain` | 12.61 | 12.61 | 13.14 | 0.45~480.49 | 1/50 | 17/50 |
| `wood` | 0.58 | 0.57 | 0.57 | 0.45~2.50 | 0/50 | 0/50 |
| `game` | 0.52 | 0.52 | 0.52 | 0.45~2.25 | 0/50 | 1/50 |
| `stone` | 0.25 | 0.25 | 0.25 | 0.25~2.50 | 0/50 | 1/50 |
| `size` | 138 | 138 | 138 | 138~138 | 0/50 | 0/50 |
| `terr`(영토 셀) | 3,450 | 3,450 | 3,450 | — | 0/50 | 0/50 |

⇒ **갈리는 키는 둘이다**(`fertility`·`water`) — 그리고 그 둘만이 물 술어를 읽는 키다.
숲·사냥터·석재·영토 크기는 물 술어를 안 읽어서 **셋 다 같았다**(T247 이 "숲·사냥터는 같다"고 본 그 자리).

**전 열 동일**: 세 벌 5/50 · **C=A 39/50**. 마을별 전수표는 `scripts/t251-land-diff.js` 출력 그대로다(50행).
대표 행(값 = C / A / B):

| 마을 | fertility | water | woodSustain |
|---|---|---|---|
| 광산1 | 0.30 / = / **0.28** | = | = |
| 어촌2 | 1.26 / **1.21** / **0.53** | 1.58 / = / **0.08** | 460.18 / = / 496.83 |
| 농촌12 | 1.23 / **0.90** / **0.53** | 1.43 / **0.79** / **0.08** | 22.78 / **31.42** / 31.42 |
| 농촌22 | 1.11 / **1.12** / **0.42** | 1.47 / = / **0.08** | = |

`fertility ≤ 0.50` 인 마을: C **11곳** · A **11곳** · B **44곳**.

## §0ⓒ 참값 판정 — **③-후자**(헤드리스 자의 조립이 틀렸다)

제품 정본은 `zone.js` 다. 근거는 코드 한 줄이다: 실서버가 `Villages.init` 에 넘기는 술어를 만드는 곳이
`zone.js:570·599·643` 이고, 넘기는 줄이 `zone.js:2651` 이다. 계측기 둘은 그 술어를 **각자 다시 적었고**,
하나(A)는 거의 맞혔고 하나(B)는 크게 틀렸다.

**기준선 자는 제품 땅에 서 있다(○)** — 50마을 중 39곳 전 열 동일, 중앙 비옥도 0.99 vs 제품 1.00.
남은 11곳의 차는 **A 에 해안선 물 띠와 다리가 없어서**다(양쪽 다 물 술어 계열). **셋째 판 기준선은 유효하다.**

### 실서버 한 줄 — `fertMed`·`stoneMed`·`fert042` 를 더했다(재민 실기 · 자 코드 0 · 새 문 0 · 읽기만)

```sh
sudo docker exec durango-zone-hanbando node --experimental-sqlite -e '
const db=require("./server/zone-local-db");const r=db.getVillagesByZone("hanbando");
let pop=0,dead=0,ever=0,h=0,lj=0,wood=0;const g=[],w=[],f=[],s=[],wa=[];
for(const v of r){if(!v.econ_state)continue;const e=JSON.parse(v.econ_state);const n=(e.npcs||[]).length;const L=e.land||{};
 pop+=n;if(e._everPop){ever++;if(n<=0)dead++;}
 g.push(L.game||0); w.push(L.wood||0); f.push(L.fertility||0); s.push(L.stone||0); wa.push(L.water||0);
 wood+=(e.storage||{}).wood||0; h+=(e.counts||{}).hunter||0; lj+=(e.counts||{}).lumberjack||0;}
const md=a=>{a.sort((x,y)=>x-y);return a[a.length>>1];};
console.log(JSON.stringify({day:r[0]&&r[0].day,villages:r.length,pop,dead,ever,
 hunter:h,lumberjack:lj,woodStock:+wood.toFixed(1),
 gameMed:md(g),woodMed:md(w),fertMed:md(f),stoneMed:md(s),waterMed:md(wa),
 fert042:f.filter(x=>x<=0.5).length}));'
```

**읽는 법 — 이 한 줄이 §0ⓒ 를 끝낸다.**

| 실서버가 이렇게 나오면 | 뜻 |
|---|---|
| `villages` **50** · `fertMed` ≈ **1.0** · `waterMed` ≈ **1.36** · `fert042` ≈ **11** | **이 자가 맞다.** 제품 술어 판이 참값이고 `어촌6` 은 제품에서도 안 선다 |
| `villages` **51** · `fertMed` ≈ **0.99** · `fert042` ≈ **11** | 해안선 띠가 실서버에선 `어촌6` 을 안 덮는다 — 나머지는 이 자와 같다(A 판에 가깝다) |
| `fertMed` ≈ **0.42** · `fert042` ≈ **44** | 뒤집힌다 — 제품도 물을 못 보고 있다는 뜻이고, 그건 **세계의 결함**이다(자 아님) |
| `lumberjack` **0** | T247 의 그 물음 — 제품도 나무꾼을 안 둔다(그러면 다음 카드는 세계 쪽) |

## §0ⓓ 자를 제품 경로로 고치고 **끔 팔 200일 재측정**(자 코드만 · 제품 0)

고친 것은 `deps` 세 칸뿐이다 — `isTerrainBlockedLocal`·`isWaterTileLocal` 을 `zone.js` 와 같은 값으로,
그리고 제품이 함께 넘기는 `isBridgeLocal`(`zone.js:2661`)을 이었다.
술어 자체는 **`scripts/zone-preds.js` 한 벌**에만 둔다(계측기마다 다시 적는 게 이번 결함의 뿌리다).

| 200일 · 시드 1020 · 끔 팔 | **고친 자(C 제품 술어)** | 앞판(A 기준선 술어) | T247 판(`T232_RAW=1`) |
|---|---|---|---|
| 마을 | **50** | 51 | 51 |
| 인구 | **2,894** | 2,718 | 1,167 |
| 소멸 | **0/50** | 0/51 | 0/51 |
| 확장 | **157** | 61 | 0 |
| 도구Q | **2,058** | 2,007.7 | 644 |
| 생곡 | **6,710.2** | 6,136 | 4.6 |
| 나무꾼(200일째) | **224** | 188 | **0** |
| 목재 재고(200일째) | **629.4** | 3,017.2 | **0** |
| **목재 첫 0 날** | **없다** | 없다 | **100** |
| `fert` 중앙 | **1.00** | 0.96 | 0.42 |
| 굶주림(200일째) | **0** | 0 | 41 |

20일 궤적(고친 자 · 목재 열):

| 날 | 인구 | 목재 재고 | 일 유출 | 나무꾼 | 연료충당 | 도구Q |
|---|---|---|---|---|---|---|
| 1 | 400 | 5,938.5 | 36.66 | 0 | 0.98 | 3,756.8 |
| 20 | 851 | 4,235.7 | 92.23 | 0 | 0.98 | 3,213.6 |
| 40 | 1,430 | 1,451.6 | 106.39 | **76** | 0.65 | 2,889.0 |
| 60 | 1,763 | 1,126.7 | 93.61 | 151 | 0.49 | 2,559.3 |
| 100 | 2,217 | 890.8 | 124.13 | 203 | 0.53 | 1,896.1 |
| 140 | 2,543 | 270.5 | 115.92 | 226 | 0.46 | 1,591.7 |
| 200 | 2,894 | **629.4** | 161.80 | 224 | 0.50 | 2,058.0 |

### econ 자(`t176-ab`)와 나란히 — 같은 씨 · 같은 200일

| | econ 자 (`t176-ab` · 51마을) | **고친 헤드리스 자 (50마을)** | 차 |
|---|---|---|---|
| 인구 | 3,126 | **2,894** | **−7.4%** |
| 소멸 | 0/51 | 0/50 | 같다 |
| 도구Q | 2,535.0 | 2,058.0 | −18.8% |
| 생곡 | 5,455.0 | **6,710.2** | **+23.0%** |
| 개간(밭칸) | 16,067 | 19,111 | +18.9% |

⇒ 앞판(A 술어)의 −13.1% 가 **−7.4%** 로 줄었고, 마을이 하나 적은 것(50 vs 51)만으로도 −2% 안팎이 설명된다.
두 자는 이제 **같은 땅 위에 서 있다**. 남은 차는 생활층이 실제로 도는 것(개간·건축·사냥 장부)의 몫이다.

⇒ **무너짐이 사라졌다.** 목재는 날 30 부터 나무꾼이 붙어 바닥을 치지 않고(최저 270, 200일엔 회복),
굶주림 0, 소멸 0, 인구는 200일에 2,894 로 **단조 증가**다. T222~T247 의 파국은 **세계가 아니라 자의 조립**이었다.

> ⓘ `wProd`(일 유입) 열은 고친 판에서도 **0 으로 찍힌다**. 나무꾼은 224명이고 재고는 회복하는데 열만 0 이므로,
> 이건 세계가 아니라 **계측 칸**의 문제다(`dailyProductionBuf.wood` 를 헤드리스 갈래가 안 채우는 자리).
> 이 카드에서 고치지 않았다 — **회부한다**(새 수 0 · 사본 0).

## §1 검증

* **T247 비트 동일 재현**: `T232_RAW=1` 판을 다시 돌려 T247 JSON 과 **전 열 diff 0**(비교한 잎 1,359 · 벽시계 열 제외).
  → 이번 배선이 옛 팔의 비트를 **한 칸도 안 건드렸다**.
* **같은 씨 두 판**(고친 자 · 200일 · 시드 1020): **전 열 diff 0**(비교한 잎 1,342 · `emitted` 포함).
* `scripts/t176-ab.js` **무접촉**(`git diff` 0 — 읽기만 했다).
* `server/**` **diff 0** · 하네스 신설 0 · 새 손잡이 0 · 제품 문 0 · 실서버 접속 0.
* 접점: `scripts/t222-life800.js`(수정) · `scripts/zone-preds.js`(신설) · `scripts/t251-land-diff.js`(신설).

## §2 회부(구현 0)

1. ★★★**[해결 · 확정] T222→T247 의 파국은 세계가 아니라 자의 조립이었다.**
   `Villages.init(deps)` 의 `isTerrainBlockedLocal`(=`() => false`)·`isWaterTileLocal`(인자 오용) 두 칸이
   비옥도 장에서 물 항을 통째로 지웠다 — `(0.12+0.18)×1.4 = 0.42` 가 소수점까지 맞는다.
   고치니 200일 인구 1,167 → **2,894**, 나무꾼 0 → **224**, 목재 첫 0 **없음**, 굶주림 41 → **0**.
   ⇒ **T247 회부 1·2 를 철회한다**("두 자가 다른 땅" = 자 결함 · "목재가 안 들어온다" = 그 파생).
   ⇒ **T241·T232·T228 의 "무너지는 축" 계열 관측은 전부 이 인공물 위에 있었다** — 다시 물을 필요가 없다.

2. ★★★**[새 카드 · PM · 이게 진짜 회부다] 계측기 18벌이 지형 술어를 각자 다시 적는다.**
   `grep -l isTerrainBlockedLocal scripts/*.js` → 자체 정의 **18개 파일**이고, 그중
   **해안선 물 띠를 가진 것 0개 · 다리를 가진 것 0개**다(`zone.js` 는 둘 다 본다).
   즉 **레포의 모든 계측기가 제품과 조금씩 다른 땅 위에서 측정해 왔다**. T251 은 그 사본 중
   가장 크게 틀린 하나를 잡았을 뿐이다. 처방은 이 카드에서 이미 한 벌 만들어 뒀다 —
   `scripts/zone-preds.js`(정본 조각만 부른다). **나머지 17벌을 여기로 모을지는 PM 결정**(구현 금지).
   ⚠`zone.js` 는 require 가 곧 서버 기동이라 계측기가 못 부른다 — 그래서 "한 벌"이 최선이고,
   더 나은 답은 **zone.js 가 술어 셋을 내보내는 것**이다(제품 변경 = 승인 게이트).

3. ★★**[자] `wProd`(목재 일 유입) 열이 고친 판에서도 0 이다.**
   나무꾼 224명 · 재고 회복인데 열만 0 이므로 세계가 아니라 **계측 칸**이다
   (`dailyProductionBuf.wood` 를 헤드리스 갈래가 안 채우는 자리). T247 §0ⓐ 표의 "유입 0" 결론은
   **이 칸의 결함일 가능성이 크다** — 다음 카드에서 칸을 먼저 고치고 다시 읽어라.

4. ★**[실기 · 재민] §0ⓒ 한 줄.** `villages` 가 **50 인지 51 인지**가 해안선 띠를 가르고,
   `fertMed` 가 **1.0 인지 0.42 인지**가 이 카드 전체를 가른다. `lumberjack` 은 T247 의 남은 물음이다.

5. ⓘ**`어촌6` 은 제품 술어에서 안 선다**(해안선 물 띠가 중심 주변 24셀을 덮는다 — `villages.js:1144` 스킵).
   실서버가 51곳이면 이 줄이 틀린 것이고, 50곳이면 **제품도 원래 50마을 세계**다.
   어느 쪽이든 `zone-config` 의 `어촌6` 좌표는 손볼 값이 있다(구현 금지 · 새 카드).

6. ⓘ**앞 카드 함정 전부 유효**(하루 1ms 금지 · `T146_BUILD_MS` 벽시계 예산 · 시계 고정 · `pkill -f` 금지).

---

## 부록 — ⓑ 51마을 전수표 원문(`scripts/t251-land-diff.js` 출력 · 값 = C / A / B · 같으면 `=`)

| # | 마을 | C 좌표 | fertility | wood | game | stone | water | size | woodSustain | terr |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 광산1 | 2120,2117 | 0.30 / = / 0.28 | = | = | = | = | = | = | = |
| 2 | 광산2 | 2007,218 | 0.94 / = / 0.30 | = | = | = | 1.26 / = / 0.08 | = | 2.07 / = / 2.47 | = |
| 3 | 광산3 | 1425,794 | = | = | = | = | = | = | = | = |
| 4 | 광산4 | 1285,1330 | 0.99 / = / 0.27 | = | = | = | 1.47 / = / 0.08 | = | = | = |
| 5 | 어촌1 | 1296,2357 | 0.96 / = / 0.30 | = | = | = | 1.52 / = / 0.08 | = | = | = |
| 6 | 광산5 | 1129,2948 | = | = | = | = | 0.62 / = / 0.08 | = | 107.71 / = / 129.04 | = |
| 7 | 광산6 | 227,3078 | 0.90 / = / 0.31 | = | = | = | 1.07 / = / 0.08 | = | = | = |
| 8 | 광산7 | 2034,3099 | = | = | = | = | 0.58 / = / 0.08 | = | = | = |
| 9 | 어촌2 | 1643,393 | 1.26 / 1.21 / 0.53 | = | = | = | 1.58 / = / 0.08 | = | 460.18 / = / 496.83 | = |
| 10 | 임업1 | 891,3516 | 0.39 / = / 0.37 | = | = | = | = | = | = | = |
| 11 | 임업2 | 1188,3094 | 1.15 / = / 0.53 | = | = | = | 1.27 / = / 0.08 | = | 342.16 / = / 418.75 | = |
| 12 | 임업3 | 1793,1910 | 1 / = / 0.53 | = | = | = | 0.91 / = / 0.08 | = | 480.49 / = / 490.81 | = |
| 13 | 농촌1 | 438,2438 | 0.85 / = / 0.53 | = | = | = | 0.69 / = / 0.08 | = | 400.50 / = / 435.77 | = |
| 14 | 임업4 | 326,1304 | = | = | = | = | = | = | = | = |
| 15 | 임업5 | 980,690 | 0.77 / = / 0.32 | = | = | = | 0.85 / = / 0.08 | = | 90.21 / = / 93.48 | = |
| 16 | 어촌3 | 566,2616 | 1.12 / = / 0.42 | = | = | = | 1.57 / = / 0.08 | = | 16.13 / = / 18.66 | = |
| 17 | 임업6 | 1581,496 | 1.23 / = / 0.53 | = | = | = | 1.51 / = / 0.08 | = | 264.69 / = / 299.10 | = |
| 18 | 농촌2 | 621,3449 | 1.17 / = / 0.44 | = | = | = | 1.51 / = / 0.08 | = | = | = |
| 19 | 농촌3 | 2011,3574 | 1.15 / = / 0.42 | = | = | = | 1.50 / = / 0.08 | = | = | = |
| 20 | 어촌4 | 683,724 | 1.08 / = / 0.42 | = | = | = | 1.47 / = / 0.08 | = | = | = |
| 21 | 농촌4 | 78,518 | 1.11 / = / 0.42 | = | = | = | 1.43 / = / 0.08 | = | = | = |
| 22 | 농촌5 | 1378,2085 | 1.12 / = / 0.42 | = | = | = | 1.46 / = / 0.08 | = | = | = |
| 23 | 어촌5 | 196,2119 | 1.16 / 1.11 / 0.42 | = | = | = | 1.54 / = / 0.08 | = | 5.83 / = / 7.98 | = |
| 24 | 어촌7 | 1509,173 | 1.16 / 1.12 / 0.42 | = | = | = | 1.55 / = / 0.08 | = | 32.67 / = / 34.11 | = |
| 25 | 어촌8 | 51,1793 | 1.14 / 1.11 / 0.42 | = | = | = | 1.54 / = / 0.08 | = | = | = |
| 26 | 어촌9 | 803,548 | 1.11 / = / 0.42 | = | 0.48 / = / 0.49 | 0.70 / = / 0.71 | 1.59 / = / 0.08 | = | 2.93 / = / 3.30 | = |
| 27 | 어촌10 | 848,895 | 1.12 / = / 0.42 | = | = | = | 1.54 / = / 0.08 | = | = | = |
| 28 | 어촌11 | 1722,2734 | 1.16 / 1.14 / 0.42 | = | = | = | 1.57 / = / 0.08 | = | = | = |
| 29 | 어촌12 | 738,3274 | 1.16 / 1.13 / 0.42 | = | = | = | 1.54 / = / 0.08 | = | = | = |
| 30 | 어촌13 | 399,3318 | 1.13 / 1.12 / 0.42 | = | = | = | 1.54 / = / 0.08 | = | = | = |
| 31 | 어촌14 | 1797,648 | 1.15 / 1.11 / 0.42 | = | = | = | 1.53 / = / 0.08 | = | 8.21 / = / 8.77 | = |
| 32 | 어촌15 | 326,775 | 1.13 / = / 0.42 | = | = | = | 1.59 / = / 0.08 | = | = | = |
| 33 | 농촌6 | 161,210 | 0.99 / = / 0.42 | = | = | = | 1.11 / = / 0.08 | = | 8.12 / = / 8.28 | = |
| 34 | 어촌16 | 161,575 | 1.14 / = / 0.42 | = | = | = | 1.58 / = / 0.08 | = | = | = |
| 35 | 농촌7 | 161,939 | 0.96 / = / 0.42 | = | = | = | 1.13 / = / 0.08 | = | = | = |
| 36 | 농촌8 | 161,1667 | 0.95 / = / 0.42 | = | = | = | 0.97 / = / 0.08 | = | = | = |
| 37 | 농촌9 | 161,2396 | = | = | = | = | = | = | = | = |
| 38 | 농촌10 | 161,2760 | = | = | = | = | = | = | = | = |
| 39 | 농촌11 | 161,3489 | 0.95 / = / 0.42 | = | = | = | 1.11 / = / 0.08 | = | = | = |
| 40 | 농촌12 | 161,3853 | 1.23 / 0.90 / 0.53 | = | = | = | 1.43 / 0.79 / 0.08 | = | 22.78 / 31.42 / 31.42 | = |
| 41 | 농촌13 | 428,210 | 0.82 / = / 0.42 | = | = | = | 0.83 / = / 0.08 | = | 20.87 / = / 22.44 | = |
| 42 | 농촌14 | 428,575 | = | = | = | = | 0.56 / = / 0.08 | = | = | = |
| 43 | 농촌15 | 428,939 | = | = | = | = | = | = | = | = |
| 44 | 농촌16 | 428,1667 | 0.75 / = / 0.42 | = | = | = | 0.73 / = / 0.08 | = | 11.54 / = / 13.14 | = |
| 45 | 농촌17 | 428,2032 | 1.10 / = / 0.42 | = | = | = | 1.39 / = / 0.08 | = | = | = |
| 46 | 농촌18 | 428,2760 | = | = | = | = | 0.25 / = / 0.08 | = | = | = |
| 47 | 농촌19 | 428,3124 | 0.93 / = / 0.42 | = | = | = | 1.07 / = / 0.08 | = | = | = |
| 48 | 농촌20 | 428,3489 | = | = | = | = | 0.35 / = / 0.08 | = | = | = |
| 49 | 농촌21 | 428,3853 | 1.11 / 0.97 / 0.42 | = | = | = | 1.36 / 1.09 / 0.08 | = | = | = |
| 50 | 농촌22 | 964,1864 | 1.11 / 1.12 / 0.42 | = | = | = | 1.47 / = / 0.08 | = | = | = |

