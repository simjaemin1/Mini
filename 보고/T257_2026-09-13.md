# 보고 — T257 자산 자 셋 (2026-09-13 · 세션8/ART)

**베이스 `9bd39869`** · 고친 파일 **다섯**(새 `scripts/asset-lock.js` · `test-icons.js` · `test-assets-audit.js` ·
`public/assets/icons.lock.json` 재생성 · `CREDITS.md` · `public/assets/audio/bgm/render-meta.json`)
**그림·소리 무변** — 미디어 671장(png·webp·ogg·m4a) sha1 전수 `c10ec48c…` **카드 전후 동일**
**배포 설정·클라 무접촉** — `.dockerignore`·`Dockerfile*`·`index.html`·`client/*` diff **0줄** · 지우기 0 · 실기 0

> ★자산 트리 전체 해시(`d40918e4…`)는 **바뀐다** — 이 카드가 `icons.lock.json` 과 `render-meta.json` 을
> 고치라고 시켰기 때문이다. 그 둘은 표지 그림이 아니다. 그래서 "자산 무변"을 **미디어 671장**으로 재서 적는다.

## ★ 답 한 줄

**자 화소 해시 452 재생성(PNG 364 바뀜 · webp 88 그대로) · 빨강 넷 → ★0(넷은 드리프트가 아니었다) · 고아 55→56 · 13곡**

---

## §0-ⓐ 잠금 자 — 화소 해시

자는 **`scripts/asset-lock.js` 하나**다. `test-icons.js` ⑧ 도 `test-assets-audit.js` ② 도 기록기(`--write`)도
같은 함수를 부른다 — 자를 두 벌 적으면 그 순간 실패다(T243 1차 판이 자를 두 벌 들고 char 192장 거짓 빨강을 봤다).

```
PNG  : sha1("<w>x<h>|" + 디코드한 RGBA) [:16]     ← pngjs 로 디코드(재구현 0)
webp : sha1(파일 전체) [:16]                       ← ⚠아직 화소 해시가 아니다(아래)
```

### 자명 통과 금지 — 셋

| 시험 | 화소 해시(새 자) | IDAT(종전 자) | 파일 해시(webp 자) |
|---|---|---|---|
| **① 같은 화소 · 압축 수준만 다름**(PIL lvl 1/6/9 · 63196/58847/58305 바이트) | `b31f29eb` **하나** | `f2887774`·`6f841957`·`1dfe38bd` **셋** | `a947f6b6`·`17ec1d7e`·`52bf9966` **셋** |
| **② 한 화소만 바꿈**(R+1) | `b31f29eb` → `1f2252cb` **문다** | — | — |
| **③ 꼴만 다름**(4×2 ↔ 2×4 · 같은 바이트열) | `182c097a` ↔ `7785a1e3` **가른다** | — | — |

①이 이 카드의 전부다: **IDAT 는 그림이 아니라 압축기를 잰다.** zlib 판이나 PNG 라이터가 바뀌면
잠금 364장이 **그림은 그대로인 채** 통째로 빨개질 자리였다. ③ 때문에 크기를 해시에 넣는다 —
같은 화소열이라도 68×38 과 38×68 은 다른 그림인데 바이트만으로는 안 걸린다.

> ⚠**방법 주의**: `pngjs` 의 `deflateLevel` 은 안 먹는다(세 수준이 같은 IDAT 를 냈다). 압축기를 **실제로**
> 바꾸려면 다른 인코더로 구워야 한다 — 그래서 ①은 PIL 로 구웠다. 첫 판은 이걸 몰라 "IDAT 도 하나"라는
> 거짓 초록을 봤다. 대조군이 안 갈리면 시험이 아니라 자명 통과다.

### 잠금표 재생성 — 452장

```
$ node scripts/asset-lock.js --write     → 잠금표 재생성 452장
$ node scripts/asset-lock.js --check     → 대조 452장 · 어긋남 0
```

| 무리 | 키 | 값 그대로 | 값 바뀜 |
|---|---:|---:|---:|
| mountains(webp) | 88 | **88** | 0 |
| bridge·buildings·crops·icons·nature·props·terrain·trees(PNG) | 364 | 0 | **364** |
| **합** | **452** | 88 | 364 |

키 집합 **동일**(새 키 0 · 없앤 키 0) · `_` 메타 키 **보존** · `_규약` 은 새 자를 적도록 다시 썼다.
**그림은 한 비트도 안 바꿨다** — 바뀐 것은 자뿐이고, 그래서 PNG 364장의 값이 전부 새 값이 된 것이다.

### ⚠webp 88장은 아직 화소 해시가 아니다 (회부)

이 저장소에 webp 디코더가 없다(`pngjs` 뿐). `test-icons` 는 **CI 단위 23종** 안이라 `npm ci` 만으로
돌아야 하는 게 규약이고, 자 하나 고치자고 CI 에 새 의존성을 다는 것은 이 카드보다 큰 결정이다.
그래서 산 88장은 파일 해시 그대로 두고 **표가 스스로 어느 자를 썼는지 말하게** 했다(`rulerOf` · `_규약`).
위 표 ①이 보여주듯 파일 해시도 압축기에 흔들린다 — 같은 위험이 88장에 남아 있다.

### ★★T243 표본 넷 — **드리프트가 아니었다**

카드는 "넷은 새 자로도 여전히 빨강이어야 한다 → 알려진 드리프트로 표에만 적어라"고 했다.
**다시 재 보니 넷 다 초록이다.** 카드의 전제가 아니라 실측을 적는다.

| 표본 | 굽는 길 | 바이트 | 파일 sha1 | IDAT | **화소 해시** |
|---|---|---|---|---|---|
| `nature/bush01` | `nature_render -- bush01` → `nature-postprocess --only` | 같다 | 같다 | 같다 | **같다** |
| `icons/acorn` | `NAT_ICONS=1 … -- acorn` → `icons-postprocess` | 같다 | 같다 | 같다 | **같다** |
| `crops/lettuce_2` | `FIELDS_SPECIES=lettuce fields_render` | 같다 | 같다 | 같다 | **같다** |
| `crops/lettuce_3` | 〃 | 같다 | 같다 | 같다 | **같다** |

**자기 비교가 아니라는 증거**: 굽기는 임시 워크트리 안에서 돌았고 후처리가 그 트리의 배포 자리를
**실제로 다시 썼다**(mtime 13:36:47·13:36:54·13:37:05 — 워크트리 생성 13:36:15 뒤). 그런데도
그 트리의 `git status -- public/assets` 가 **비었다** ⇒ 새로 쓴 바이트가 커밋본과 똑같다.

**그러면 T243 의 0/4 는 무엇이었나.** 그 판의 생굽기는 화소 해시 `b8a8e18f`, 이 판의 생굽기는
`0eba77eb` 다 — 같은 커밋, 같은 스크립트(`git diff aa382c69 HEAD -- scripts/*_render.py …` **0줄**),
같은 자산(`public/assets` diff **0줄**), 같은 `bpy 5.0.1`. 그런데 그림이 다르다.

* **판 안에서는 결정적이다** — T243 은 두 번 구워 같았고, 이 판은 **세 번** 구워 같았다.
* **스레드 수 탓이 아니다** — `taskset -c 0`(1코어)과 2코어가 **같은 값**을 냈다.
* ⇒ 갈리는 것은 **판과 판 사이**(상자가 바뀐 자리)다. 지금 상자는 `Intel Xeon @2.80GHz · avx512f/vnni`.
  T243 때의 상자 정보는 안 남겼으므로 **원인은 못 잡았다.** 지어내지 않는다.

⇒ **T246 이 "렌더 드리프트"라고 적은 줄은 틀렸다.** 배포판은 (적어도 오늘 이 상자에서) 지금 코드로
**바이트까지 재현된다**. 고칠 배포판이 없으니 **재굽기 카드도 지금은 근거가 없다**(회부 갱신).
남은 참말은 하나다: **굽기는 판 안에서만 결정적이고, 상자가 바뀌면 화소가 미세하게 흔들린다**
(T243 실측으로 |Δ|>24 가 0.49% · 눈엔 같다). 그게 잠금이 살아 있어야 하는 이유다.

## §0-ⓑ `test-assets-audit` 거짓 양성 — 55 → **56**

고친 것은 **두 줄**이다(하나로는 안 됐다).

1. **키로 안 불리는 확장자는 어간 대조에서 뺀다** — `.py`·`.zip`·`.md`·`.txt`·`.json`·`.gitignore`.
   클라가 `/assets/icons/<key>.png` 를 키 목록에서 조립하므로 **그림·소리는 어간이 곧 참조**다(그렇게 걸린 103장 전부 정당).
   키가 없는 소스 파일만 어간이 흔한 영단어라 남의 지역 변수에 걸린다.
2. **주석은 쓰임이 아니다** — ①만 넣었더니 **여전히 55** 였다. 범인이 이 하네스의 **제 주석**이었다:
   내가 설명하려고 `compose.py` 라고 적은 줄을 제 검사기가 읽고 "쓰임 있음"이라 답했다.
   ⇒ 토큰을 뽑기 전에 주석을 걷어낸다(문자열 안의 `://` 는 지킨다).

> 이 집이 **세 번째로 밟은 같은 지뢰**다 — T182 `test-itemlabel` ⑩ · T205 `test-crops-world` · 그리고 여기.
> **검사 범위를 넓히면 검사가 거짓말한다.**

| | 고아 | 새로 고아가 된 것 | 고아에서 빠진 것 | 참조 있음 |
|---|---:|---|---|---:|
| 전(T243·T246) | 55 | — | — | 656 |
| **후** | **56** | `audio/bgm/compose.py` **하나** | **없다** | 655 |

**자명 통과 금지**: `T257_NOSTEM=0`(빼기 전 판)으로 되돌리면 **55** 를 낸다 — 고친 줄이 실제로 무는 자리다.
키로 부르는 자산은 하나도 안 흔들렸다(참조 있는 655장 전부 `audio/` 밖 · 고아에서 빠진 것 0).

## §0-ⓒ 12곡 → 13곡

| 어디 | 전 | 후 |
|---|---|---|
| `CREDITS.md:33` | `*.ogg`·`*.m4a` **12곡** | **13곡** (고친 근거를 같은 칸에 적었다) |
| `render-meta.json.tracks` | 12키 | **13키** — `village_day_jeongak` 추가 |

**세 칸(`seconds`·`rms_db`·`peak_db`)은 비웠다(`null`) — 못 재기 때문이고, 지어내지 않는다.**
그 이유를 재서 파일 안에 적어 뒀다(`_T257`):

* 이 표의 수는 `compose.py:676` 이 **합성한 float 배열**에서 재는 값이다. 배포된 `.ogg` 에서 잰 값이 아니다.
* 대조 실측 — `village_day_trad`: 표 **109.02 / −14.98 / −1.29** ↔ `ffmpeg` 로 ogg 를 재면 **108.28 / −17.20 / −0.09**.
  **12곡 전부 안 맞는다**(재현 0/12). 자가 다르다는 뜻이고, ogg 에서 잰 수를 이 표에 섞으면 정본이 둘이 된다.
* 그 음원 조각은 저장소에 없다 — `bgm/.gitignore` 가 `bk_*.zip`(133MB)을 빼고 `samples/` 엔
  "여기에 음원을 넣으세요" 한 장뿐이다. 채우려면 `render_jeongak.py` 를 음원 조각과 함께 돌려야 한다.

★같은 칸의 다른 수(`가야금 음 1574개 · 합성 대체 0`)는 **재지 않았다** — 그래서 안 건드렸다.
★`CREDITS.md:70` §5("도구는 배포물에 안 들어간다")는 카드 지시대로 **안 고쳤다**(#30 의 `.dockerignore` 뒤).

## §1 검증

| | |
|---|---|
| 그림·소리 | **미디어 671장 sha1 전수 `c10ec48c…` 카드 전후 동일** — png·webp·ogg·m4a 한 바이트도 안 바뀌었다 |
| 배포 설정·클라 | `.dockerignore`·`Dockerfile*`·`index.html`·`client/*` **diff 0줄** |
| `test-assets-audit` | **PASS** · 고아 **56** · 되돌림 판 55(자명 통과 금지) |
| `asset-lock --check` | **452장 · 어긋남 0** |
| `test-icons` | **PASS**(⑧ 잠금 절이 새 자로 돈다) |
| `test-props` | **PASS**(잠금표 키 수 절 무변) |
| `test-harness-lint` | **42/0** — T250(세션2) 하네스 규약 무접촉 |
| 러너 목록 | **143** 그대로 · `asset-lock`·`assets-audit` 은 목록 밖(야간 창 무변) |
| 3시드 | 면제 — diff ∩ 랩 적재 17파일 = ∅ |
| 접점 | `icons.lock.json` · `_규약` · `IDAT` · `sha1` · `test-assets-audit` · `stem` · `render-meta.json` · `tracks` · `village_day_jeongak` · `CREDITS.md` |

## §2 회부

1. **재굽기 카드는 근거가 없어졌다(PM)** — 표본 넷이 오늘 바이트까지 재현된다. T246 의 "렌더 드리프트"
   줄과 `인계/ART-자산.md` 의 같은 줄을 **이 보고로 갱신**했다. 남은 참말: 굽기는 **판 안에서만** 결정적이다.
2. **상자가 바뀌면 그림이 흔들린다 — 원인 미상(PM)** — 같은 커밋·같은 `bpy`·같은 스레드 수인데 판이 다르면
   생굽기가 다르다(T243 `b8a8e18f` ↔ T257 `0eba77eb`). 굽는 카드는 **상자 정보를 보고에 남기는 것**이
   값이 싸다(CPU 모델·플래그). 그걸 규약으로 세울지는 PM 몫.
3. **webp 88장 자(PM)** — 화소 해시로 가려면 디코더가 필요하고 `test-icons` 는 CI 단위다. 갈래 둘:
   ⓐ CI 에 디코더 의존성 추가 · ⓑ 산 잠금만 CI 밖 하네스로 옮김.
4. **#30 처분(재민)** · #29 대표↔평균(재민) · 소리 층 다시 잇기(ⓑ 판정 뒤) · #14 크레딧 ⓐⓑⓒ ·
   먹선 · 상추/아욱 26px — 그대로.
