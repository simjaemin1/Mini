# 보고 — T188: `trees.json.ko` 는 아무도 안 읽는다 (세션8 · 사본 1→0 · 굽기 0)

베이스 `12033de5` · 가지 `batch/trees-ko-copy-0912`.
만진 것 여섯: 랩 `TREES` 표 · `build-trees.js` · `server/trees.json`(구워서) · `nature_render.py`(`fruit_ko` 제거) ·
`lab-trees.js`(계측기) · 하네스 넷. **굽기 0 · 배포 PNG 33장 sha1 무변 · 3시드 800일 바이트 동일 · 새 이름 0.**

---

## 0. §0 실측 (셋)

### ⓐ `trees.json.ko` 를 읽는 곳 — **0**

| 자리 | 무엇을 하나 |
|---|---|
| `server/trees.js koOf` | T182 가 그림 표로 옮겼다 — 주석에만 남았다 |
| `scripts/build-trees.js` | 랩 표에서 **전사**한다(읽는 게 아니라 굽는다) |
| `scripts/lab-trees.js` | 계측기 표에 **찍는다**(러너 밖 · `@regress` 없음) |
| `scripts/test-itemlabel ⑯` | T182 가 *"갈려도 그림 표를 따른다"* 를 증명하려고 **일부러** 읽었다 |
| 랩 인라인 엔진 | ★**한 번도 안 읽는다** — `lab/전쟁실험실.html` 안에 `.ko` 사용 **0**(전수) |
| 서버·클라·sim | **0** |

⇒ 셈에 드는 자리가 하나도 없다. **갈릴 자리만 남아 있었다.**

### ⓑ `fruit_ko` ↔ `RESOURCES.ko` — 지금 갈림 0, **그러나 갈릴 수 있다**

| 종 | `fruit_ko`(그림 표) | 열매 품목 | `RESOURCES.ko` | |
|---|---|---|---|---|
| oak | 도토리 | acorn | 도토리 | 같음 |
| chestnut | 밤 | chestnut | 밤 | 같음 |
| mulberry | 오디 | mulberry_fruit | 오디 | 같음 |
| grape | 머루 | grape | 머루 | 같음 |
| **hazel** | **개암** | **null** | — | ★**세계에 없는 열매를 주장**(T129 이래) |

둘은 **독립으로 손으로 적은 리터럴**이다(굽기의 `FRUITING` dict ↔ `specialty.RESOURCES`).
지금 0 이어도 언젠가 갈린다 — 나무 이름이 정확히 그렇게 넷이 갈렸다.

★**카드는 "갈릴 수 있으면 파생을 유도(굽기가 `RESOURCES` 에서 읽어 적는다)"라 했는데, 안 실는 쪽을 골랐다.**
이유 셋:
1. 파이썬 굽기가 `RESOURCES` 를 읽으려면 `specialty.js` 를 파싱해야 하는데, 넷 중 **둘이 코드가 만든다**
   (`RESOURCES.chestnut = { ...nuts, ko: '밤' }`). 그 파싱은 값이 아니라 **정본의 모양**을 베끼는 사본이다.
2. 읽는 곳이 **하나**뿐이고 그마저 하네스 로그 장식이었다(전수: `test-props` 353행 한 줄).
3. 필요하면 **이을 수 있다**: `trees.json.fruit` → `RESOURCES.ko`(둘 다 정본) — 저장할 이유가 없다.
   곁가지로 `hazel` 의 거짓 주장도 같이 닫혔다.
⇒ PM 이 "그래도 실어라" 면 되돌리는 비용은 굽기 한 줄이다(회부).

### ⓒ 3시드 800일 — **바이트 동일**

랩 적재 파일(`lab/전쟁실험실.html`)을 만졌으므로 공통 §2 ① 대로 같은 판 A/B 를 돌렸다.

```
  node scripts/t17-metrics.js 800 <시드>   (계측기 · 각 ~7.5분 · 총 12판)

  ① 베이스 12033de5           A(원본) ↔ B(이 카드)
     시드 1020  sha1 3768e008709a   ✓ 바이트 동일
     시드    7  sha1 d196cbeabc4f   ✓
     시드   42  sha1 74dad11f6339   ✓

  ② ★리베이스 뒤 다시 쟀다 — 베이스 c5bd6d60(T179·T180·T171·T181·T186·T187 이 얹힌 판)
     시드 1020  sha1 3768e008709a   ✓ 바이트 동일
     시드    7  sha1 d196cbeabc4f   ✓
     시드   42  sha1 74dad11f6339   ✓
```

이름 칸은 셈에 안 든다 — **잰 값이 그것을 말한다**(구조적 논증만으로 안 끝냈다).
★족보 (138)대로 **리베이스 뒤 다시 쟀다**: 랩 파일이 겹쳤기 때문이다(아래 §2).
곁가지 실측 하나 — 두 베이스의 sha1 이 **여섯 판 모두 같다**. 그 사이에 얹힌 카드들도
3시드 기준선을 한 바이트도 안 움직였다는 뜻이다(`인계/공통` 의 "이 표는 그대로 유효하다" 와 일치).

---

## 1. 무엇이 바뀌었나

| 자리 | 바뀐 것 |
|---|---|
| `lab/전쟁실험실.html` `TREES` | 여덟 줄에서 `ko:` 제거 — 표가 **축만** 갖는다(인라인 엔진 무접촉) |
| `scripts/build-trees.js` | `AXES.ko` 제거 · 전사에서 `ko` 제거 · `--table` 은 이름을 **그림 표에서 읽는다** |
| `server/trees.json` | 굽기로 다시 냈다 — 종마다 `ko` 없음 · `_axes` 여섯 |
| `scripts/nature_render.py` | `FRUITING`(한국어 dict) 제거 ⇒ `fruit_ko` 가 표에서 사라진다. 열매종 판정은 **표가 스스로**(열매판 칸이 있는 종). 칸 순서를 못 박았다 |
| `public/assets/trees/tree_species.json` | **5줄만 빠졌다**(굽기 0 · `ONLY` 문법 · PNG 33장 sha1 무변) |
| `scripts/lab-trees.js` | 계측기 표의 이름을 그림 표에서 읽는다 |
| 하네스 넷 | `test-trees ⑧` 신설 · `test-itemlabel ⑯ⓒ` 갈아끼움 · `test-lab-trees ④` 축 갱신 · `test-props` 로그 한 줄 |

★**T182 는 "안 읽는다"로 막았고, T188 은 "있을 수가 없다"로 막았다.** 원천(랩 표)까지 지워야 끝난다 —
굽기만 고치면 원천이 남아 다음 굽기에 되살아난다. 하네스가 **원천 리터럴**도 본다.

---

## 2. 검증

| 자 | 결과 |
|---|---|
| `test-trees` | **91 / 0** — ⑧ 신설 11줄 |
| `test-itemlabel` | **58 / 0** — ⑯ⓒ 가 "갈려도 따른다" → "**갈릴 자리가 없다**" 로 |
| `test-lab-trees` | **44 / 0** — ④ 축이 일곱 → 여섯 |
| `test-props` · `lab-wiring-check` | **PASS** · **실패 0 · 경고 0** |
| `e2e-nature`(픽셀) | **125 / 0** |
| 배포 PNG | `public/assets/trees/*.png` **33장 sha1 무변** |
| 서버 코드 | `server/*.js` **diff 0**(바뀐 건 굽힌 `trees.json` 뿐) · 클라 **diff 0** |
| 3시드 800일 | **바이트 동일 3/3**(ⓒ) |
| `build-trees.js --check` | 랩 표 ↔ 파일 **최신 ✅**(사본 감시자가 여전히 문다) |

### 리베이스 — 겹친 파일 넷, 전부 다시 돌렸다

착지 직전 원격이 `12033de5` → `c5bd6d60` 로 움직였다(T179·T180·T171·T181·T186·T187 · 문서 넷).
겹친 파일 **넷**: `lab/전쟁실험실.html` · `scripts/test-itemlabel.js` · `scripts/test-lab-trees.js` · `인계/R2-개체렌더.md`.
충돌 0 · 내 패치는 **한 글자도 안 변했다**(blob 해시·행 위치만 옮겨 갔다 — 본문 diff 0).
겹친 파일의 하네스를 다시 돌려 `test-itemlabel` 58/0 · `test-lab-trees` **52/0**(다른 카드가 절을 더했다) ·
`test-trees` 91/0 · `lab-wiring-check` 실패 0, 그리고 **3시드 A/B 를 새 베이스에서 다시** 쟀다(§0-ⓒ ②).

---

## 3. 회부

* **[PM] `fruit_ko` — 실을지 말지.** 이 카드는 **안 싣는 쪽**을 골랐다(§0-ⓑ 이유 셋). 카드의 지시는
  "파생을 유도" 였다. 되돌리려면 굽기가 `specialty.js` 를 파싱해야 하고, 넷 중 둘이 코드 생성이라
  파싱이 정본의 **모양**을 베낀다. 되돌림 비용은 굽기 한 줄.
* **[영역 econ · 작은 것] `hazel` 의 열매.** 그림엔 열매판이 있고 서버는 `fruit: null` 이다(T129 이래).
  이 카드가 그림 표의 거짓 주장(`fruit_ko: 개암`)을 없앴을 뿐, **어느 쪽이 옳은지는 그대로 열려 있다**
  — 값·kg·kcal 출처가 서면 `hazel.fruit` 에 아이디가 들어간다(새 수 0 규약).
* `tree14_a` 처분 · 오디 위치(#15 재민) · 3D 둥글기·T116·MakeHuman(#1~3) · 먹선 · 종별 34세트 · 갯벌 채집물.

---

## 4. 푸시

| 항목 | 값 |
|---|---|
| 해시 | **`50c4b352`** |
| 베이스 | `00f376f1` — 푸시 직전 `ls-remote` 로 **무변 확인** |
| 브랜치 | `batch/trees-ko-copy-0912` **먼저** → 원격 main 무변 재확인 → `main` |
| main 전진 | `00f376f1..50c4b352` — 보통 ff push |
| force | **0회** |
| 리베이스 | **2회** — ① `12033de5` → `c5bd6d60`(겹친 파일 **넷** ⇒ 하네스 재실행 + 3시드 A/B 재측정 · §0-ⓒ ②) · ② `c5bd6d60` → `00f376f1`(겹친 파일 **0** ⇒ 재측정 없음 · 공통 §2 ② 규약 그대로). 두 번 다 **패치 본문 diff 0**(blob 해시·행 위치만 옮겨 갔다) |
| 방식 | 2판 — 맥 `$HOME/pushrepo188` 얕은 작업 클론(`--depth 200 --single-branch` · 팩 1개). `~/mnt/Mini/durango-mini` **무접촉** |
| `.lock` | 작업 클론 전수 **0** · `tmp_*` 팩 **0** |
