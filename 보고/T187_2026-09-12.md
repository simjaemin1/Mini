# 보고 — T187: 심은 나무 둘째 판 (종별 햇수 · 씨앗 표 유도 · 걸음 진단)

* **베이스** `16b437cb`(origin/main · 리베이스 후 · 측정 시작은 `12033de5`) · **코드 커밋/헤드** `2b43ebbf` · 가지 `batch/planted-trees2-0912`
* 접촉 파일 **셋**: `server/zone.js`(+24 −2) · `scripts/test-regrow.js`(+19 −1) · `scripts/e2e-nature.js`. **클라 0줄**.
* 새 수 0 · 사본 0(씨앗 표는 종 표에서 유도) · econ 랩 적재 교집합 **∅** · 걸음은 **진단만**(고치지 않았다)

---

## §0 실측 (셋)

### ⓐ 종별 햇수 — 심은 자리만 종을 안 넘겼다

| 물음 | 실측 |
|---|---|
| 정본 | `Trees.stageYearsOf(id, sY, fY)` — 표의 `mature` × T122 비율. **새 수 0** |
| 시더(벤 자리) | `chunk.js:462` `_stage(seedKey, type, _spAt(x,y))` → `regrowStageOf(…, species)` — **종을 넘긴다** |
| 심은 자리 | `zone.js _shapeRegrown` → `regrowStageOf('tree', elapsed)` — **종이 없다** |
| ⇒ | 같은 참나무가 "벤 자리냐 심은 자리냐"로 **다른 햇수를 사는 세계**였다 = 사본 |
| 건너뛰기 | 심기는 그루터기 단계를 건너뛴다 — 더하는 기간도 **그 종의 것**이어야 한다(전역 22년을 더하면 머루(1.7년)는 심자마자 성목이 된다) |

**전/후 성숙 실일**(심은 날부터 · `Y=365`):

| 종 | 성목년(표) | 종별 [그루터기, 성목] | **전**(종 없음) | **후**(종 넘김) |
|---|---|---|---|---|
| 머루 grape | 4 | [1.69, 4] | 10,950 | **842** |
| 개암 hazel | 6 | [2.54, 6] | 10,950 | **1,264** |
| 버들 willow | 7 | [2.96, 7] | 10,950 | **1,474** |
| 산뽕 mulberry | 8 | [3.39, 8] | 10,950 | **1,685** |
| 밤 chestnut | 12 | [5.08, 12] | 10,950 | **2,527** |
| 소나무 pine | 25 | [10.58, 25] | 10,950 | **5,264** |
| 잣 jat | 30 | [12.69, 30] | 10,950 | **6,317** |
| 참나무 oak | 40 | [16.92, 40] | 10,950 | **8,423** |

전은 **종과 무관하게 한 값**이다 — 그게 종 표를 반만 쓰던 자리다. 후는 전부 표에서 나온다(손으로 적은 수 0).
성목 햇수 자체(#4 · 재민)는 이 카드가 안 건드린다 — 여기서 바뀐 건 "**어느 표를 보느냐**"뿐이다.

### ⓑ 씨앗 유도 표 — 넷이다(다섯이 아니다)

| 종 | 표의 열매 품목(`Trees.fruitOf`) | 씨앗이 되나 | 이름표 |
|---|---|---|---|
| 참나무 oak | `acorn` | ○ | 도토리 |
| 밤나무 chestnut | `chestnut` | ○ | 밤 |
| 산뽕 mulberry | `mulberry_fruit` | ○ | 오디 |
| 머루 grape | `grape` | ○ | 머루 |
| 소나무·잣·버들 | `null` | ✗ | — |
| **개암 hazel** | **`null`** | **✗** | — |

⇒ **전제 정정 하나**: 지시서 ②가 `hazelnut→hazel` 을 들었지만 **정본(`server/trees.json`)의 개암은
열매 품목이 없다**(`fruit: null · fy: 0`). 없는 품목을 지어내면 그게 사본이고, econ 쪽 품목 표까지
따라 만들어야 한다 ⇒ **개암은 빼고 넷**으로 유도했다. 개암에 열매 품목을 주는 일은 표의 주인(T188 랩 TREES)이 할 일이다 — 회부.

⇒ 아이템 표 접점 **0**: 넷 다 `ItemLabel` 에 이미 이름표가 있다(T124 가 `acorn`, T182 가 나머지를 닫았다).
클라도 접점 0 — 씨앗 목록은 진작부터 `welcome.plantSeeds` 로 온다(사본 0 · T124).

### ⓒ 걸음 8배 — **속도는 정상이었다. 막힌 것이었다.**

같은 입력 문법으로 **숲**과 **빈터**에서 10.6초씩 걸었다(입력 300개 = 이론 640px):

| 자리 | 이동 | 실측 속도 | 입력/ack | 200ms 표본(이론 12.8px) |
|---|---|---|---|---|
| 숲 (54784,54784 · 청크당 204그루) | **8.5px** | **0.8 px/s** | 300 / **300** | 49개 전부 ≈0 · 10px 넘는 구간 **0** |
| 빈터 (51712,85504 · 앞길 700px 무장애) | **640.0px** | **60.2 px/s** | 300 / **300** | 49개 **전부 12.8px** |

* 배율: `Body.effects().moveMult = 1` · `Carry.effects().moveMult = 1` · `combinedMove = 1` — **배율 아님**
* 입력 문법: `ackSeq` 가 보낸 수와 **정확히 같다**(300/300) — 서버가 다 먹었다. **하네스 문법 아님**
* 빈터 표본이 **전부 12.8px**(= 입력 6개 × `MOVE_SPEED/TICK_HZ` = 6×2.133) — **`MOVE_SPEED=64` 가 그대로 나온다**

⇒ **콜라이더**다. `movePlayerStep` 은 축마다 `isBlockedByTree` 로 막는데, 입력이 한 축(+x)뿐이면
막힌 순간 **미끄러질 성분이 0** 이라 그 자리에 선다. 숲 표본이 0과 12.8의 혼합이 아니라 **전부 0**인 것이
그 증거다(첫 나무에 닿고 멈췄다 · 8.5px 만 갔다).
⇒ **T178 이 회부한 "걸음 7.7px/s" 는 속도가 아니었다** — 그 판의 자리에서 첫 나무까지 간 거리였다.
정정한다: 걸음 속도는 캐논대로다. 남는 물음은 "**나무에 닿으면 안 미끄러진다**"이고, 그건 M 영역이다(§3 회부).
고치지 않았다(카드가 진단만 시켰다).

---

## §1 만든 것 — 서버 **두 자리**

```
server/zone.js  PLANT_SEEDS    종 표에서 유도(Trees.fruitIds × fruitOf) · 되돌림이면 종전 한 줄
                _shapeRegrown  r.sp 를 regrowStageOf 에 넘기고, 건너뛰는 그루터기 기간도 그 종의 것으로
```

| 약속 | 지켰나 |
|---|---|
| 새 수 0 | ○ — 햇수는 표의 `mature` × T122 비율, 씨앗 이름은 표의 열매 품목 |
| 사본 0 | ○ — 씨앗 표를 손으로 안 적는다. `test-regrow ⑦ⓐ2` 의 `'["acorn"]'` 사본도 같이 지웠다 |
| 클라 0 | ○ — `public/` 접촉 0(씨앗 목록은 `welcome.plantSeeds`, 그림은 `sp`) |
| 되돌림 | ○ — `T135_TREES=0` 이면 씨앗 한 줄 · 공통 햇수(비트 동일) |
| T182·T188 무접촉 | ○ |
| econ 무접촉 | ○ — 랩 적재 17 ∩ 내 diff 3 = **∅** |

---

## §2 검증 — `e2e-nature` **134 / 0** (125 → 134)

| 검사 | 실측 |
|---|---|
| 심기⓪ | 세계가 내준 씨앗 목록 `[acorn,chestnut,grape,mulberry_fruit]` = 종 표 유도치 (손편집 0) |
| 심기⓪ | 열매 없는 종은 목록 밖 — 소나무·잣나무·개암나무·버드나무 · 표에 없는 씨앗은 「심을 수 있는 씨앗이 아니다」 |
| 심기① | 밤과 도토리를 **같은 날**(day 955) 이웃 칸에 심었다 |
| 심기② | `chestnut`→밤나무 · `acorn`→참나무 (씨앗이 종을 정한다) |
| 심기③ | 문턱 **5일 전**(day 3476) — 둘 다 묘목 |
| 심기③ | **밤나무가 먼저 선다** — 심은 지 **2,527일**(12년 · 표) · day 3487 |
| **심기③** | **그런데 참나무는 아직 묘목** — 같은 날 심었는데 **8,423일**(40년)이 필요하다 ⇒ ★**종을 안 넘기면 둘 다 성목이라 이 줄이 빨개진다**(돌연변이 손잡이) |
| 심기③ | 참나무도 제 날에 선다 — day 9715 |
| 심기④ | 자란 뒤에도 종 그대로 · **둘 다 열매 비트** `true`(가을) |
| 심기⑥ | 밤나무 → `tree09_a`(표 `chestnut.autumn`) · 비트 false 면 `tree09` / 참나무 → `tree07_a` · `tree07` |

### 접점 하네스
| 하네스 | 결과 |
|---|---|
| `test-regrow` | **100 / 0** (97 → 100 · ⑦ⓐ2 의 `'["acorn"]'` 사본을 정본 대조로) |
| `test-trees` | **81 / 0** |
| `test-lab-trees` | **44 / 0** |
| `test-client-globals` | **26 / 0** (클라 무접촉) |
| `test-harness-lint` | **22 / 0** |
| `node --check` | 셋 다 통과 |

### 3시드 면제 근거 — 랩 적재 추적
```
econ 랩 적재 17파일 ∩ 내 diff 3파일(server/zone.js · scripts/test-regrow.js · scripts/e2e-nature.js) = ∅
```

---

## §3 회부 (고치지 않았다)
1. **나무에 닿으면 안 미끄러진다** — `movePlayerStep` 은 축마다 막는데 한 축 입력이면 슬라이드 성분이 0 이라 그 자리에 선다. 실측: 숲 0.8px/s ↔ 빈터 60.2px/s(같은 입력). **속도는 정상**이다(ⓒ 표). → **M-이동입력 영역 · 카드 하나**
2. **개암에 열매 품목이 없다** — 표에 `fruit: null` 이라 심을 수 없다. 지시서는 `hazelnut` 을 들었지만 품목이 실재하지 않는다. 주려면 econ 품목 표까지 함께 온다. → **T188(랩 TREES) · 표의 주인**
3. 성목 햇수 캐논 자체(#4) — 재민 판정 · 이 카드는 "어느 표를 보느냐"만 고쳤다
4. 16방향 · 이송 실체

## §4 실기 1줄 (재민)
밤을 심을 수 있고, 자라면 밤나무로 서나 — 그리고 **참나무보다 먼저** 서나.

## §5 푸시 — **main 까지 갔다** (정본 2판)
```
작업 클론   $HOME/pushrepo (--depth 200 --branch main --single-branch · 맥) · ~/mnt/Mini/durango-mini 무접촉
가지        batch/planted-trees2-0912 → 2b43ebbf   (BRANCH_OK)
main        2b43ebbf                                (PUSHED_MAIN · ff 확인 두 번)
원격 main   2b43ebbf (푸시 뒤 재확인)   ·   force 0   ·   .lock 없음
번들        ~/Mini/_inbox/T187_2026-09-12.bundle (16b437cb..2b43ebbf · 증거)
```
