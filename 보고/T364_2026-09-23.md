# 보고 — T364 지나가는 군대를 옆 마을이 본다 · 행군로가 숲을 본다 (2026-09-23)

한 줄: **커밋 `19fd51b` [승인 대기 · T364] — T329 의 위협 T 가 **목표 마을**에만 걸리던 것을 **경보 거리 안 모든 마을**로 넓히고(세 항 그대로 · w 고름 유지 · 새 수 0),
T295 후속이 만든 나무 술어를 **행군로 A\*** 에도 주입했다(같은 탐색 함수에 술어 하나 · 새 코스트 0 · 교역 캐시 무접촉).
옆 마을 T 최대 **0.606**(R_out 배수 0.394) · 지나간 뒤 **잔류 0** · 행군로는 숲을 만나면 **+16.5셀** 돌아가고 나무 칸을 **37 → 0** 으로 줄인다 ·
3시드(1020·7·42) 800일 51마을 `t17-metrics` **바이트 동일** · `test-war-world` **97 통과 / 0 실패**(옛 85 + 절 둘 12).**

* 실행 세션 = **세션10**(WAR) · 카드 `지시/지시_T364.md` · 베이스 `origin/main ce0802a` · 가지 `batch/war-neighbor-0923`.
* **새 수 0** — T 의 세 항·경보 거리·행군 속도·`_opDefOdds` 는 T329 가 쓰던 그대로. w 셋은 **고름 `1,1,1`**(★PM 결정 #59 — 유도 근거 없는 가중은 새 수다).
* **사본 0** — 행군로는 `computeRoutePts` 에 `extraBlk` 하나를 더 받는 **같은 탐색**이고, 나무 술어는 T295 후속의 `_warTreeCell`(존이 주입한 그 색인)이다.
* ⚠**T329 의 접근 속도 표본에 결함이 있었다** — 한 군대가 여러 마을에 걸리면 `body._thD`(마을까지 거리)가 서로를 지운다. 이 카드가 **몸의 속도 벡터**로 바꿨다(§0-ⓐ).
* 회부(§4): 군량 = 행위(설계 · PM) · p95(값 판정 · 재민) · w 가중(실측이 항을 가르면) · 숲을 지나는 대형 속도.

---

## §0 실측

### ⓐ 옆 마을 — 목표가 아니어도 본다

**무엇이 바뀌었나**

| | T329 | T364 |
|---|---|---|
| T 가 붙는 마을 | 그 전쟁의 **방어 마을** 하나 | **경보 거리(`WAR_ALERT_R`) 안 모든 마을** — 목표 여부 무관 |
| 접근 속도 표본 | `body._thD` = **그 마을까지의 거리**(마을마다 덮어씀 — 여러 마을이면 엉킨다) | `body._thX/_thY` = **몸의 자리** → 속도 벡터 한 번 · 마을마다 **그 방향에 투영** |
| `odds` | 방어 마을의 `_opDefOdds` | **그 마을의** `_opDefOdds`(옆 마을이면 옆 마을 승산) |
| 부하 | 전쟁 수 | 전쟁 수 × 마을 수(**hypot 한 번**) + 걸린 마을만 세 항 |

투영으로 바뀌면서 **정면으로 다가오는 군대의 close 는 예전과 같은 수**이고, 스쳐 지나가는 군대는 ~0 이 된다(내적).

**행군 한 판 실측**(하네스 ⓠ · 군대가 A→B 직선을 21틱에 지나간다 · 옆 마을을 행군선에서 경보 거리 0.35 배 옆에 둠):

| 마을 | 목표인가 | T 최대 | 그때 R_out 배수(1−T) | 지나간 뒤 |
|---|---|---|---|---|
| 방어 마을 | ○ | **0.896** | 0.104 | **0** |
| 옆 마을(경보 안) | ✕ | **0.606** | 0.394 | **0** |
| 먼 마을(경보 밖 3배) | ✕ | **0** | 1 | 0 |

* **잔류 0** — 군대가 경보 밖으로 나가면 그 마을의 `_warThreat` 은 **지워진다**(다음 쓰기의 첫 줄이 지난 자국을 거둔다).
* 부하: 마을 4곳 × 21틱 = 거리 술어 84회 · **1ms 미만**. 51마을 세계에서도 전쟁 하나당 51 hypot 이다(§0-ⓒ).

### ⓑ 행군로가 숲을 본다

| 행군로(마을쌍) | 숲을 모를 때 | 숲을 볼 때 | 길이 차 | 지나는 나무 칸 |
|---|---|---|---|---|
| 공격A → 방어B | 165.7셀 | **182.2셀** | +16.5 | 37 → **0** |
| 방어B → 공격A | 165.7셀 | **182.2셀** | +16.5 | 37 → **0** |
| 공격A → B 북쪽 | 172.3셀 | **178.9셀** | +6.6 | 37 → **0** |

* 판정 규칙은 지형과 **같다** — 코스 노드(4셀) 한가운데 셀 하나를 본다(`coarseOpen` 이 그렇게 본다). 새 코스트·배수는 안 지었다: 나무는 벽과 같은 **막힘**이다.
* **대조** — 술어를 안 주면 길이 **한 점도 안 다르다**(교역·캐러밴·거리행렬·`travelDays` 무변).
* 전쟁 몸은 이제 **제 캐시**(`_warRouteCache` · 하루 단위)를 쓴다. 교역로 캐시(`routeCache`·DB)는 건드리지 않는다.
* 숲이 아주 막으면(길이 없으면) **옛 길로 떨어진다** — 길 없음보다 낫다(폴백 한 줄).

### ⓒ 부하

> 계측기 `scripts/war-world-perf.js` W6(전쟁 6 · 틀 세계 51마을 · 관측자 하나 · 창 420초 · 컨테이너 2코어).

| 판 | 존 틱 p50 | p95 | drop | 전쟁 틱 p50 | 병사 최대 | 교전/대치/궤주 |
|---|---|---|---|---|---|---|
| **T364**(이 가지) | **4.115** | 7.62 | **0** | 1.59 | 283 | 51/13/35 |
| T330 기준(카드가 대라고 한 수) | 4.19 | — | 0 | — | — | — |

* **기준 안**이다(4.115 ↔ 4.19). 이 카드가 더한 일은 둘 다 값이 작다:
  ① T 쓰기 = 전쟁 하나당 **마을 수만큼 hypot**(51회) + 경보 안 마을만 세 항 — 틱마다 돌지만 산술 51번이다.
  ② 행군로는 **몸 하나 생길 때 한 번**(그 뒤 하루 캐시) — 틱과 무관하다.
* 전이 통계(교전 51 · 대치 13 · 궤주 35)는 T329 판(60/33/21)보다 **대치가 줄고 궤주가 늘었다** — 길이 숲을 피해 나면서
  군대가 제 시간에 닿고 판이 제대로 끝난다는 뜻이다(나무가 들어온 뒤 대치가 늘었던 것의 되돌림).

### ⓓ 한 줄 답

**군대는 이제 지나가는 것만으로도 마을을 움츠러들게 하고(경보 안 모든 마을 · 잔류 0), 그 군대의 길은 숲을 돌아간다(+16.5셀 · 나무 칸 37 → 0).
둘 다 T329·T295 후속이 이미 세운 값과 술어를 그대로 쓴다 — 새 수도, 새 코스트도, 교역로 변화도 없다.**

---

## §1 무엇을 고쳤나

| 파일 | 무엇 |
|---|---|
| `server/villages.js` | `_warBodyVel`(몸의 속도 한 번) · `_threatOfArmy(w, vil, now, vel)`(투영) · `threatOf`(경보 안 모든 군대) · `_warWriteThreats`(전쟁 × 마을 거리 술어) · `_routeBegin/computeRoutePts` 에 `extraBlk` 인자 · `_warRouteTreeBlk`·`_warRoutePts`(전쟁 행군로 · 하루 캐시) · `_warEnsureBody` 가 그 길을 쓴다 · 하네스 노출 셋 |
| `scripts/test-war-world.js` | 절 ⓠ·ⓡ(12칸) + 프로브 둘(옆 마을 훑기 · 행군로 전/후) · 숲 덩이 픽스처 |

## §2 검증

| 판 | 결과 |
|---|---|
| 3시드(1020·7·42) 800일 51마을 `t17-metrics` vs `origin/main` | **cmp 바이트 동일 ✅ 3/3** |
| `scripts/test-war-world.js`(서버 절 포함) | **97 통과 / 0 실패** |
| `scripts/lab-wiring-check.js` | 실패 0 · 경고 0 |
| `scripts/test-farm-life.js` | 33 PASS / 0 FAIL |
| `scripts/test-harness-lint.js` | 57 PASS / 0 FAIL |
| `war-world-perf` W6 | §0-ⓒ |
| `Math.random` in `sim/war-core.js` | 주입 rng 만(`rng \|\| Math.random` 한 줄 — 호출측이 항상 준다) · 이 카드는 war-core 무접촉 |

## §3 규칙 지킴

* 새 수 0 · w 고름 유지 · 배수 0 · 상태 스위치 0 · 사본 0 · `Math.random` 0(이 카드가 더한 코드에).
* T356(세션3 `zone.js` 걸음) · T359(세션1) · T361(세션4) 무접촉 — 이 카드는 `server/villages.js` 와 하네스만 만졌다.
* main 무접촉 · force 0 · 배포 재민만.

## §4 회부

* **숲을 지나는 대형의 속도** — 길은 돌아가게 됐지만, 그래도 숲을 지나야 할 때 대형이 느려지는 규칙은 없다(지금은 막히면 우회 슬롯).
* **군량 = 행위**(설계 회부 · 어부 문법 · PM) · **p95 값 판정**(재민) · **w 가중**(실측이 항을 가르면 재민).
* 옆 마을의 T 는 **대비**(R_out·캐러밴 위험)까지만 간다 — 소집·피난 같은 행위는 아직 없다.

## §5 푸시

* 가지 `batch/war-neighbor-0923` · 베이스 `origin/main ce0802a` · 커밋 `19fd51b` [승인 대기 · T364].
* 컨테이너 푸시는 403(프록시 정책) ⇒ 재민 PC 셸에서 저장된 토큰으로 민다(토큰은 그 PC 밖으로 안 나간다 · 출력 마스킹). main 은 재민.
