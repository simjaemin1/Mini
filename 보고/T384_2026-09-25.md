# T384 — 띠는 클라 혼자 켠다 + 17.8ms 해부 (세션5 · 2026-09-25 · **가지만**)

> 카드 `~/Mini/지시_T384.md` · 가지 `batch/far-band-0925` · 베이스 `03030560`(main) ·
> 만진 것: `public/client/38-r1-fartree.js`(서버 답 검사를 ⓑ 앞으로 · 계수기) · `scripts/test-far-trees.js`(ⓐ5·ⓐ6 · ⓑ4 고침) ·
> `scripts/t384-index-cost.js`(신설 · 계측기 · 러너 밖) · `scripts/t384-two-arms.js`(신설 · 계측기 · 러너 밖)
> — **`server/` 0 · 정본 JSON 0 · 개체 0 · `VIEW_RADIUS`·`T380_FAR_MAX`·`__farScale` 값 무변 · 원경 손잡이 끔 그대로.**

## ★ 머리 15줄

| # | 물음 | 답 |
|---|---|---|
| ⓐ | 띠를 서버 없이 켤 수 있나 | **켰다(기본).** 서버 답(`_farOn`) 검사를 ⓑ 원경 **앞으로만** 옮겼다. 숲 한복판: 띠 안 나무 **1,437 = 모은 1,437 = 그린 235 + 화면 밖 1,202 + 상한 0** · 상한 4,000 에 닿은 프레임 **0**(최대 1,437) |
| ⓐ' | 띠가 프레임 어디쯤인가 | 층 자체 JS **0.9~1.3ms**(자연물 층 0.08ms) — 그런데 **render 는 3.3 → 9.1ms(+5.8)** 로 는다. 스프라이트 래스터는 `drawImage` 뒤로 미뤄지므로 층의 ms 는 비용을 **덜 센다**. 진짜 값은 render 차이다 |
| ⓑ | 띠(원경 층) 대 컬링 1,500(개체) | 숲: **A0 3.3 · A1 9.1 · B 5.1ms**(두 판 모두 같은 순서). 컬링 팔이 **더 싸다** — 개체 길은 안개 게이트를 지나 **본 셀의 나무만**(프레임당 15~103그루) 그리는데, 띠는 마스크 **앞**이라 235그루를 그리고 대부분이 미탐사 안개 밑에 묻힌다 |
| ⓑ' | 마을·빈 초지 | 세 팔 모두 **330~376ms/f** — 띠와 무관한 무언가가 이 상자에서 프레임을 먹는다(순서를 뒤집어도 같다). 팔 사이 차이는 그 잡음 안이다(가르지 않았다 · 회부) |
| ⓒ | 17.8ms 는 누가 먹나 | 숲 청크 (56,59) 오프라인 **10.2ms** 중 **`isRockCellLocal` 9.06ms(88.4%)** · `isWaterCellLocal` 0.40 · `speciesAt` 0.11 · `getForestMultiplier` 0.10 → **넷 9.66ms(94.3%)** · 못 가른 것 0.58ms. 넓은 판 169청크도 바위 **94%** |
| ⓒ' | 비트(T333 상자)로 바꿀 수 있나 | **바꾸면 나무가 움직인다.** 술어는 지터된 **점**으로 불리고 상자는 **셀 중심** 답을 든다: 물 0.16~0.45% · 바위 0.12%(참 130 중 1) · 숲 0.13% 가 답이 갈린다. 같은 답을 내며 싸지는 길은 **능선 선분 색인**(능선 11 · 선분 2,129)이다 — 표만 |

**한 줄**: 띠는 이제 **서버 없이 기본으로 켜져** 숲 한복판에서 1,437그루를 모아 화면에 든 235그루를 그린다(render +5.8ms) —
그런데 같은 자리에서 `VIEW_RADIUS` 를 넓히는 팔이 **더 싸다**(+1.0~1.8ms · 안개 밑 나무를 안 그리므로) · 17.8ms 의 **88%는 바위 술어 하나**다.

## §1 ⓐ 띠 켬

```
  T380   if (!_farOn) return;                ← 띠 **앞**. 서버가 T380_FAR_TREES=1 로 답한 적이 없으면 띠도 0
  T384   if (_farBlocked) return;            ← 대조군(`__farOff(1)`)만 둘 다 끈다
         … ⓐ 띠(항상) …
         if (_farOn) for (… _farStore …)     ← 서버 답은 **ⓑ 원경에만** 문다
```

* 요청(`_farRequest`)도 그대로 — 답이 없으면 `_farStore` 가 빌 뿐이다. 서버 무접촉.
* 겹침 규칙(개체 상자 `viewR` 안은 건너뜀) 그대로 · 안개 3단계 그대로(마스크 **앞**) · 세계 무변 · 개체 0.
* 계수기 — **모은 수 = 그린 수 + 화면 밖 + 상한에 잘린 수** 항등식 · 상한에 닿은 프레임 수 · 한 프레임 최대 모은 수.
* `__farForce(1)` 은 이제 `_farOn` 을 **서버가 답했을 때만** 되살린다(끔 서버에서 부르면 ⓑ 가 켜져 "끔 비트 동일"이 하네스 손에 깨진다).

### 프레임 표 — 띠 대 자연물 층(같은 자 · EMA)

| 자리 | 띠 모은 / 그린 | 띠 층 ms(중앙값 · EMA) | 자연물 층 ms(EMA) | render A0 → A1 | 상한 닿은 프레임 |
|---|---:|---:|---:|---:|---:|
| 숲 한복판(임업3) | 1,437 / 235 | 1.00 · 1.19 (둘째 판 0.90 · 1.30) | 0.08 | 3.3 → 9.1 (4.1 → 9.4) | 0 |
| 마을(농촌2) | 462 / 95 | 0.50 · 0.52 | 0.03~0.04 | 358 → 359 (332 → 341) | 0 |
| 빈 초지 | 7 / 1 | 0.10 | 0.09~0.10 | 352 → 362 (376 → 344) | 0 |

⚠카드가 적은 "1.239ms/f"는 **층의 JS 시간**이다. 래스터가 미뤄지므로 그 수는 비용의 일부다 — 숲에서 render 는 **+5.8ms** 다.

## §2 ⓑ 두 팔 — `scripts/t384-two-arms.js`

* **같은 서버 · 같은 자리 · 같은 프레임 수**(120 · 둘째 판 60 · 순서 뒤집음). 하늘·바람 끔.
* 자: `render()` 한 번(31-m-move 가 이름으로 부르는 그것을 감쌌다 — 틱 간격이 아니다).
* 컬링 팔 **B** 는 계측 페이지로 가는 `34-m-renderloop.js` 만 `page.route` 로 `650 → 1500` 고쳐 보낸다 — **레포 무변**.
  B 에서 띠 층이 모은 수 **0 · 0 · 0**(viewR 이 1,500 이 되면 띠 상자가 빈다 — 두 팔이 겹쳐 세지 않았다).

| 자리 | A0 띠 끔 | A1 띠 켬 | B 컬링 1,500 | 나무 그리기 호출/프레임 A0 · A1 · B |
|---|---:|---:|---:|---:|
| 숲 한복판 | **3.30** (p90 5.10) | **9.10** (12.10) | **5.10** (6.70) | 6 · 241 · 103 |
| 숲 한복판(둘째 판) | 4.10 (5.80) | 9.40 (11.40) | 5.10 (7.10) | 6 · 241 · 15 |
| 마을 | 358.2 / 332.4 | 358.5 / 340.6 | 343.6 / 326.8 | 1 · 96 · 23 |
| 빈 초지 | 351.8 / 375.8 | 362.1 / 343.5 | 355.1 / 370.9 | 1 · 2 · 4 |

**왜 B 가 싼가** — 개체 길은 `_seenFor`(안개 게이트)를 지나므로 **본 셀의 나무만** 그린다(15~103).
띠는 "지면 데코는 마스크 앞" 계약이라 235그루를 **다 그리고** 마스크가 그 대부분을 덮는다.
B 는 그 대신 개체가 **클릭·채집이 되고** 길·다리·도랑·바닥 아이템도 1,500 까지 같이 그린다. 값은 고르지 않았다(재민 #67).

## §3 ⓒ 색인 문 해부 — `scripts/t384-index-cost.js`

숲 한복판 청크 (56,59) · 나무 218그루 · 오프라인 **10.2ms**(라이브 17.8ms 와의 차는 서버 부하 — 가르지 않았다).

| 술어 | 이 청크 호출 | 회당 µs | 추정 ms | 청크 대비 |
|---|---:|---:|---:|---:|
| `terrain.isRockCellLocal` | 244 | 37.1 | **9.06** | **88.4%** |
| `terrain.isWaterCellLocal` | 244 | 1.63 | 0.40 | 3.9% |
| `trees.speciesAt` | 244 | 0.43 | 0.11 | 1.0% |
| `terrain.getForestMultiplier` | 259 | 0.39 | 0.10 | 1.0% |
| **넷의 합** | | | **9.66** | **94.3%** |
| 못 가른 것 | | | 0.58 | 5.7% |

넓은 판(둘레 169청크 · 청크당 5.86ms): 바위 5.52ms(**94%**) · 물 0.25 · 숲 0.07 · 종 0.06 → 넷 100.7%.
능선 옆(먹뫼산맥 · 169청크 · 0.46ms/청크): 바위 회당 **69.8µs** · 물 35.4µs(물이 두꺼운 곳에선 물도 비싸다).

**왜 바위가 비싼가** — `isRockCellLocal` 은 능선 11개를 `_isPointInRiver` 로 훑는다. bbox 조기 기각은 **있지만**
능선이 길어(한울대간 733점) bbox 가 넓고, 그 안의 점은 **선분 전부**(능선 선분 2,129)를 잰다. 그리고 참이면 물까지 또 잰다.

### 비트(T333 상자)로 바꿀 수 있나 — **표만**

| 술어 | 몫 | 셀 중심 답과 갈리는 점(넓은 판) | 판정 |
|---|---:|---|---|
| 바위 | 88~94% | 숲: 0 / 25,139(**참 0 — 자명**) · 능선: **1 / 817(참 130)** = 0.12% | 비트로 바꾸면 **나무가 움직인다**. 같은 답으로 싸지는 길 = 능선 **선분 색인**(격자 버킷) |
| 물 | 4% | 숲: 42 / 25,576(참 437) = 0.16% · 능선: 4 / 891(참 74) = 0.45% | 같음 — 비트면 움직인다 |
| 숲 배율 > 1.5 | 1% | 41 / 30,769(참 26,860) = 0.13% | T333 상자에 숲 층이 **없다** |
| 종 | 1% | — (이미 **셀 좌표** 해시) | 바꿀 것이 없다 |

⚠1차 판이 스스로 틀렸다: 모듈 전체를 감쌌다 푼 직후 인자를 `f(...a)` 로 다시 불러 바위를 **115µs** 로 쟀고
합이 청크의 **279%** 가 됐다. 합이 100% 를 넘으면 자를 먼저 의심한다(족보 156/170) — 169청크를 덥힌 뒤 재는 판으로 바꿨다.

## §4 하네스

```
  test-far-trees       19/0   (17 + ⓐ5 띠 항등식 + ⓐ6 대조군)
    ⓐ4  손잡이 끔 = 원경(ⓑ) 0그루 · 받은 청크 0        ← "끔 비트 동일" 은 ⓑ 에만 남는다
    ⓐ5  띠 안 나무 1,437 = 모은 1,437 = 그린 235 + 화면 밖 1,202 + 상한 0   (하네스가 같은 상자로 다시 센다)
    ⓐ6  대조군 — 나무 그리기 호출 프레임당 254 → 19(준 235 = 띠 235)
    ⓑ9  대조군은 띠도 원경도 끈다
  test-client-globals  26/0   (최상위 실행문 무변)
  e2e-move             37/0
  test-harness-lint    61/61
```

**하네스가 T380 에서 운으로 통과하던 자리 하나를 고쳤다(ⓑ4).** 큰지도 반경을 날 요청(`r: 6000`) 한 번으로 물었는데
클라가 4초마다 보내는 화면 반경 요청(1,500)이 서버 큐를 **갈아 끼웠고**, 끝 판정 `done >= 2` 를 **화면 반경 악수 둘이 채웠다**.
T384 첫 판이 그 모양을 냈다(ⓑ4 녹색 · ⓑ5 조각 0통). ⇒ 제품 길(`_farWant` — 80-bigmap 이 부르는 그것)로 묻고,
**조각을 받은 뒤의** 끝 표식을 기다린다. 띠가 덜 찬 프레임(22그루 · 3그루)을 재던 것도 "숲이면 500그루 이상" 을 기다리게 했다.

## §5 회부 — 구현 금지

1. **#67 값 넷**(재민) — `T380_FAR_MAX` 8192 · `__farScale` 1 · `VIEW_RADIUS` 650 · 활성 반경 1,200. §2 표가 입력이다.
2. ★**띠가 안개 밑 나무까지 그린다** — 숲 한복판에서 235그루를 그리지만 컬링 팔은 같은 자리에서 15~103그루만 그린다.
   미탐사 셀은 마스크가 **완전히** 덮으므로 거기 나무를 건너뛰면 화소는 같고 값은 싸진다(마스크가 한 프레임 늦게 오는
   가장자리는 따로 재야 한다). 이 카드는 안개 규격을 **그대로** 두었다.
3. **열린 땅에서 render 330~376ms/f**(헤드리스 소프트웨어 렌더 · 세 팔 모두 같다 · 순서 무관) — 띠와 무관. 무엇인지 **가르지 않았다**.
4. **색인 문 고치기**(PM · `europa` 앞) — 88% 가 바위 하나다. 비트 치환은 답을 바꾸고, 선분 색인은 답을 안 바꾼다.
5. 원경 방송 켜기(재민 · 17.8ms 내린 뒤) · Safari 실기(재민 · 배포 뒤 멀리 나무가 보이는지).
