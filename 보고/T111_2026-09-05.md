# T111 — MakeHuman(MPFB) 몸 스크린 (2026-09-05 · 배포 0)

**된다. 26px 에서 확실히 갈린다. 그런데 옷을 통째로 다시 세워야 한다.**
판정은 재민 눈이다 — 이 카드는 재고 그림으로 냈다. 그림: `산그림/디자인B/캐릭터_MPFB_스크린.png`.

배포본 무접촉을 먼저 증명한다: `public/assets/char/` **73개 파일 sha256 전부 동일**(작업 전/후 대조) ·
`test-charsheet` 69/0 · `e2e-charsprite` **81/0** · 게임 코드 diff 0.

---

## 0. §0 실측 (넷)

### ⓐ 된다 — MPFB 2.0.17 이 bpy **5.0.1 헤드리스**에서 돈다

| 물음 | 실측 |
|---|---|
| 배포 형태 | GitHub `makehumancommunity/mpfb2` (커밋 `437dd513`) · **pypi 에는 없다** |
| 버전 | MPFB **2.0.17** · `blender_version_min = "4.2.0"` (상한 없음) |
| 설치 자리 | ⚠**레거시 `scripts/addons/` 로는 안 된다** — `bpy.utils.extension_path_user()` 가 *"The package does not name an extension"* 로 죽는다. **확장 형식**이어야 한다: `~/.config/blender/5.0/extensions/user_default/mpfb` · 모듈명 `bl_ext.user_default.mpfb` |
| 켜기 | `addon_utils.enable("bl_ext.user_default.mpfb")` → 모듈 534개 · `HumanService` 산다 |
| 외부 내려받기 | **필요 없다** — 기본 메시·타깃·리그·텍스처 73MB 를 애드온이 다 갖고 있다(온라인 접속 preference 는 꺼 둔 채로 동작) |
| 몸 만들기 | `HumanService.create_human()` — **0.2초** · GUI 0 |
| 리그 붙이기 | `HumanService.add_builtin_rig(bm, "game_engine")` — 0.5초 · 53뼈 · 정점그룹 204 |

⇒ **대안 경로(4.x 컨테이너 · `.glb` 왕복)는 필요 없었다.** 그 표는 안 만든다.

### ⓑ 물릴 수 있다 — 그런데 **거리 가중이 아니다**

| 물음 | 실측 |
|---|---|
| 메시 | 정점 **19,158** · 면 18,486 (프리즘 몸 **880** 정점 = **22배**) |
| 키 | **1.6594 m** → 정본 1.70m 로 **1.02447배** 스케일 |
| 좌표계 | MPFB: `+x` = 캐릭터의 왼쪽 · **`−y` = 앞**(발끝 `ball_l` 방향 (−0.066,−0.997,0.047)) · `+z` = 위 ⇒ **z축 +90°** 회전 |
| 휴식 포즈 | A 포즈(`upperarm_l` 방향 (0.666,−0.003,−0.746) = 아래·바깥 42°) |
| 상자 | 정본 프레임 **327×270 에 그대로 들어간다**(T107 상자 못박기가 걸린 채로 구웠다) |
| 화면 키 | 발밑→정수리 **57.66px**(프리즘 55.66px) — **2px 더 크다** |

★★**거리 가중으로 12뼈에 새로 붙일 필요가 없다.** 카드가 물은 그 선택지(강체 100% 규약을 깨나)는
**답이 "안 깨도 된다"** 다: `game_engine` 리그 53뼈가 우리 12뼈와 거의 1:1 이고
(`pelvis`·`spine_02`·`head`·`upperarm_*`·`lowerarm_*`·`hand_r`·`thigh_*`·`calf_*`),
거기엔 **MakeHuman 이 만든 제대로 된 스킨 웨이트**가 이미 붙어 있다.
⇒ 우리 12뼈를 포즈한 뒤 **뼈의 방향만 옮긴다**(`pb.matrix` 를 아마추어 공간에서 직접 세운다 —
MPFB 뼈의 롤·부모축을 하나도 안 베낀다). **실측 오차 0.0000** (12뼈 전부).
⇒ **T96 `poses.json` 은 그대로 돈다** — 그 표는 우리 12뼈의 오일러이고, 이 경로는 그걸 포즈한
**결과 방향**만 읽기 때문이다. 리타깃을 다시 만들 일이 없다.

### ⓒ 옷은 **안 맞는다** — 삼베·모시가 화소의 **29%** 를 잃는다

T81 CLOTH 기하는 프리즘 몸에 맞춘 로프트다. 해부학적 몸 위에 올리면 몸이 옷을 뚫고,
홀드아웃이 그 자리의 옷 화소를 잘라 낸다. idle 8방향 4프레임 합:

| 옷 | 프리즘 옷 화소 | MPFB 옷 화소 | 잃은 몫 |
|---|---|---|---|
| 삼베 hemp | 11,787 | **8,394** | **−28.8%** |
| 모시 ramie | 11,787 | **8,394** | **−28.8%** |
| 갖옷 fur | 14,637 | **12,559** | −14.2% |

(갖옷이 덜 잃는 건 `FUR_PAD 0.03` 만큼 더 부풀려 놨기 때문이다 — 그 여유가 정확히 완충이 됐다.)
그림 ⑥ 이 그 모습이다: 튜닉이 어깨·옆구리에서 조각나고 몸이 드러난다.
⇒ **채택하면 옷 기하를 다시 세워야 한다**(몸 표면에서 오프셋으로). 비용은 아래 표.

### ⓓ 26px 에서 **갈린다** — 61~75%

몸+옷 합성 · 8방향 · 화소차 24 초과를 "갈렸다"로 센다(`e2e-nature` 문법).

| 클립·옷 | 갈린 화소 | 실루엣만 다른 화소(프리즘만 / MPFB만) | 평균 휘도 |
|---|---|---|---|
| idle 삼베 | 16,300 / 26,802 = **60.8%** | 3,389 / 3,967 | 80.1 → 84.3 (Δ4.2) |
| idle 갖옷 | 17,133 / 28,293 = **60.6%** | 3,000 / 3,021 | 63.2 → 69.4 (Δ6.1) |
| idle 모시 | 20,088 / 26,802 = **74.9%** | 3,389 / 3,967 | 86.8 → 88.7 (Δ1.9) |
| walk 삼베 | 36,661 / 55,690 = **65.8%** | 7,500 / 9,472 | 79.6 → 84.0 (Δ4.4) |
| walk 갖옷 | 36,748 / 58,870 = **62.4%** | 6,897 / 7,263 | 63.0 → 69.1 (Δ6.0) |
| walk 모시 | 41,803 / 55,683 = **75.1%** | 7,563 / 9,465 | 86.2 → 88.5 (Δ2.4) |

★**키 2px 차이 때문이 아니다.** MPFB 를 1~3px 내려 정수리를 맞춰 봤더니 차이가 오히려 **늘었다**
(60.8% → 65.8 → 69.1 → 72.2%). dy=0 이 이미 최적 정렬이다 ⇒ **형상이 진짜로 다르다.**

---

## 1. 채택하면 드는 값 — 비용 표

| 일 | 무엇 | 크기 |
|---|---|---|
| **자산** | MPFB 확장(73MB)을 컨테이너에 두는 일 · 생성은 코드 한 줄이라 **몸을 커밋할 필요 없다**(재생성 0.2초). `assets-src/body/` 에는 **만드는 스크립트**만 두면 된다 | 저장소 증가 ≈ 0 |
| **카드 1** | 옷 기하 재작업 — 튜닉·치마·허리끈을 **몸 표면 오프셋**으로 다시 세운다. 지금은 프리즘 반지름 표(`TORSO_R`·`ARM_R`·`LEG_R`)에 매달려 있다 | 이게 제일 크다 |
| **카드 2** | 도구·등짐 자리 재확인 — `handR`·`spine` 에 강체로 물린 것들이 새 몸의 손·등 위치에 맞나 | 작다 |
| **카드 3** | 잠금·시트 전면 재굽기 | **약 3시간** (전체 1h27m × 정점 22배는 아니다 — 실측: 이 스크린이 768장에 16분 32초 = 장당 1.29초로 프리즘과 비슷하다. Cycles 는 19k 정점을 어려워하지 않는다) |
| **되돌림** | 프리즘 몸 코드는 그대로 두고 스위치로 가른다(`char_render.py` 의 `BODY` 를 고르는 자리 하나) | 작다 |

★**굽는 시간이 안 는다**가 이 표의 놀라운 값이다. 스크린 768장 **992초 = 장당 1.29초** —
정본 전체 굽기(3,360장 1h27m = 장당 1.55초)보다 오히려 빠르다(옷 층이 몸보다 가벼워서다).
**정점 22배가 굽는 시간을 안 늘린다.**

## 2. 검증

| 검사 | 결과 |
|---|---|
| **배포 무접촉** | `public/assets/char/` **73개 파일 sha256 전부 동일**(작업 전/후 대조) |
| `char_sheets.lock.json` | **무변**(위 73개에 포함) |
| `test-charsheet` | **69 / 0** |
| `e2e-charsprite` | **81 / 0** |
| 스크린 스크립트 결정론 | 두 번 돌려 **8장 바이트 동일** |
| 게임 코드 diff | **0** (`public/client/` · `server/` 무접촉) |
| 새 파일 | `scripts/char_mpfb_screen.py` **하나**(정본 `char_render.py` 무접촉 — import 해서 쓴다) |
| 굽는 시간 | 스크린 768장 **992초** |

★**공정한 비교를 위해 바꾼 변수는 몸 하나다.** 같은 씬·조명·카메라·리그 포즈·먹선+셀4 후처리를
쓰려고 `char_render.py` 를 **import** 했다(`--only-meta` 면 굽기 루프를 건너뛰므로 씬 구축까지만
돈다 — 실측 0.8초). 임시 자리에 **정본 `char_meta.json` 을 미리 복사**해 T107 상자 못박기가 걸리게
했다 — 안 그러면 다른 자(294×252)로 잰 그림이 됐다.

## 3. 회부

* **채택 카드** — 재민 판정 뒤. 위 비용 표 그대로.
* **성별·체형 축** — 이 스크린은 중립 몸 하나만 봤다(캐논 "청동기 복장 하나"에 맞춰 몸도 하나).
  MPFB 는 `gender`·`african/asian/caucasian` 등을 0~1 손잡이로 준다 — 쓸지는 설계 판단.
* **키 2px** — MPFB 가 정수리까지 57.66px, 프리즘이 55.66px. 채택하면 스케일을 0.965 로 한 번
  더 조여 54.4px 규약에 맞춘다(이 스크린은 **1.70m 기준**으로만 맞췄다).
* idle·swing 모캡 · 먹선 공용(T106 뒤) · 옷 재질 공용 · 상자 못박기 공용 · NPC 업기 · 16방향 · 크레딧.

## 4. 완료

```
브랜치   batch/mpfb-screen-0905 → main (ff)
커밋     d429626ff168c596d9841e389209f986125444ff
푸시     9bca9c28..d429626f   d429626f… -> main   (fast-forward)
force    0
리베이스 1회 — 뜬 시점의 기반은 d426c25e 였는데 그 사이 origin/main 이 **9bca9c28**(T106 보고)
         까지 앞서 있었다. 리로그가 start→pick→finish 뿐이라 **충돌 0**(충돌이 났으면
         `rebase (continue)` 가 남는다 — T96 때 두 번 남았던 그 줄이 여기엔 없다).
재검     리베이스로 얹힌 것은 T112(비·밭)와 T106(범프·산·지면) 두 덩이 84파일인데,
         이 카드가 건드린 세 파일(`scripts/char_mpfb_screen.py` · 보고 · 인계)과
         **겹치는 경로가 0**이다(`comm -12` 로 두 목록을 맞대 확인).
         그 84파일 중 `public/assets/char/` 는 **0개** — 이 카드의 머릿돌인
         "배포 무접촉"은 리베이스 뒤에도 그대로 선다.
         하네스는 리베이스 **전** 판이 위 표의 숫자이며, 이 카드는 굽기 산물을
         하나도 안 바꾸므로(스크린은 임시 자리에만 굽는다) 다시 안 돌렸다.
브랜치 이름 갈아끼우기(-r2 …)가 필요 없었다 ⇒ **force push 0회**. 이 세션 통틀어 0회다.
번들은 `~/Mini/_bundles_pushed/T111.bundle` 로 옮겼다.
```

**`.lock` 전수 — 격리소 밖 0. 그대로 적는다.**

```
find .git -name "*.lock"                                  →  5
find .git -name "*.lock" -not -path "*/_stale_locks/*"     →  **0**
  다섯 전부 .git/_stale_locks/ 안이다(t109 둘 · t113 둘 · 이름 없는 것 하나 07:55).
ref 자물쇠(HEAD.lock · packed-refs.lock · refs/heads/main.lock) → **0**   ← 재민의 git 을 막는 종류는 없다
```

★한 줄 정직하게 — 내 푸시(14:31) 직후 `_stale_locks/maintenance-t113.lock`(14:32)이 생겼다.
그게 **내 푸시가 낸 것을 뒤이은 세션이 제 이름으로 격리한 것인지**, 그 세션 제 것인지는 파일
이름만으로 가릴 수 없다. 어느 쪽이든 지금 격리소 밖은 0이고, ref 자물쇠는 0이다.
T109 회부는 그대로 산다 — PM 클론에 `git config maintenance.auto false` 한 줄.

## 5. 실기 한 줄 (재민)

**대조표에서 26px 로 봤을 때 갈아 끼울 값이 있는가** — 수치로는 갈린 화소 61~75%, 실루엣만 다른
화소가 ~15%다. 그림에서는 ①②(몸만 · idle)와 ③④(walk)가 그 질문의 답이고,
⑤⑥ 이 **값을 치르는 자리**(옷이 몸에 먹히는 모습)다.
