# T51 — `32-m-render.js` 2차 분할 (행동 변경 0)

> 세션4 · 2026-09-02 · 베이스 `origin/main` `77ec3352`
> 한 줄: **3,516줄 한 덩이를 함수 경계에서만 5조각으로 갈랐다. 잘라낸 조각을 도로 붙이면 원문과 바이트 동일하다.**

---

## 0. 먼저 쟀다 (지시서 §0)

### ⓐ 내용 지도 — 깊이1 선언 35개 · 구간

```
     2–  63  isPlacementBlocked            65– 203  drawPlacementGhost
   205– 215  ensureBuildProgressEl        216– 224  updateBuildProgressEl
   232– 236  _reconnectDelayMs            237– 257  ensurePrimaryConnection
   262– 275  let ×12 (lastTickWithMyPidAt … chatSetup)
   289– 325  applyServerCorrection        329– 344  checkOrphan
   348–2542  ★render (2,195줄)           2544–2588  drawNeighborArrow
  2592–2712  전쟁 10종 (pruneWarBattles · drawBattleIndicators · focusCameraOnBattle ·
             spectateBattle · stopSpectate · setCommand · toggleCommand · _warBtnCss ·
             ensureWarHud · updateWarHud)
  2715–2725  drawDiamond   2727  const WALL_HEIGHT
  2729–2777  drawStairCellPart            2779–3516  drawBuildingIso (738줄)
```

★**지시서의 출발 제안과 순서가 달랐다.** 제안은 `32-m-renderloop` → `33-m-build-overlay` →
`34-x-war` 였는데, 실측하면 **건축 오버레이가 파일 머리**다(2–224). 순서 보존이 규약이므로
번호를 내용 순서에 맞췄다.

### ⓑ 최상위 실행문 — **0** (T0-b 가 이미 99 로 옮겼다)

```
  node scripts/split-audit.js public/client/32-m-render.js
    파일 3517줄 · 실질 최상위 문장 35 · function 선언 23 · 선언 이름 35 · 실행문 0개 중 뒤 참조 0개
```
⇒ **어디서 잘라도 로드 시점 부작용이 없다.** 이 한 줄이 이 배치를 안전하게 만든 전제다.

### ⓒ 경계 후보 — 파일 밖에서 부르는 이름 vs 안에서만 쓰는 이름

35개를 전수로 갈랐다(`public/client/*.js` 에서 32 자신을 뺀 grep):

```
  밖에서 부른다(22) : isPlacementBlocked · drawPlacementGhost · updateBuildProgressEl ·
                      ensurePrimaryConnection · lastTickWithMyPidAt · initialWelcomeReceived ·
                      worldWidth · worldHeight · lastRttMs · correctionVel · correctionUntil ·
                      correctionIgnoreWall · kicked · loopStarted · chatSetup ·
                      applyServerCorrection · checkOrphan · render · focusCameraOnBattle ·
                      stopSpectate · updateWarHud · WALL_HEIGHT
  안에서만 쓴다(13) : ensureBuildProgressEl · _reconnectDelayMs · drawNeighborArrow ·
                      pruneWarBattles · drawBattleIndicators · spectateBattle · setCommand ·
                      toggleCommand · _warBtnCss · ensureWarHud · drawDiamond ·
                      drawStairCellPart · drawBuildingIso
```

★그런데 **이 구분은 자를 자리를 정하지 않는다.** classic 스크립트 조각들은 전역 렉시컬 스코프
하나를 나눠 쓴다(T0-b 실측) — 어느 조각에 있든 서로 부른다. 실제 제약은 ⓑ 하나뿐이다:
"로드 시점에 실행되는 문장이 없으면 TDZ 사고가 없다." 그래서 **경계는 이름이 아니라 주석 배너**로
잡았다. 사람이 읽는 단위와 자르는 단위를 같게 두는 편이 낫다.

### ⓓ `split-verify.sh` 로 2차 분할을 잴 수 있는가 — **못 잰다** (그래서 새로 만들었다)

그 검사기는 등록된 조각을 **전부** 이어 붙여 **1차 분할 전 `client.js`** 와 비교한다.
T18·온보딩 v2·T13 이 조각에 접촉 줄을 더한 지금은 **HEAD 에서 이미 `differ`** 다(T18 이 그
주석을 자기 손으로 달아 뒀다: "이 검사기는 일회성 증명이지 상시 게이트가 아니다").
실제로 확인한 차이 5곳 — 전부 남의 줄이다:

```
  30-n-net  Shift+J 가리개(T18) · onbLobbyInit(온보딩) · start_vid(온보딩) ·
            village_chronicle 분기(T13) · …
```

⇒ 재는 대상을 바꿔야 한다: "**부모 조각 하나가, 그 자리를 이어받은 조각들과 바이트 동일한가**".
새 파일 `scripts/split-verify2.sh` 가 그것만 한다.

★그리고 **`split-verify.sh` 의 판정은 T51 전후가 같다**(`differ` · 같은 5곳). 조각을 더 갈라도
결합본은 그대로이기 때문이다 — 그 자체가 "행동 변경 0"의 부수 증거다.

---

## 1. 무엇을 했나

```
  public/client/32-m-render.js (3,516줄)  →  5조각 (등록 순 = 원문 순 · 재배열 0)

    32-m-render.js       226줄  fn 4   건축 배치 판정·고스트·진행바        (원문 2–225)
    33-m-conn.js         123줄  fn 4   재연결 백오프·연결 상태 12개·서버 보정·orphan (226–346)
    34-m-renderloop.js  2245줄  fn 2   ★render(2,195줄)·인접 존 화살표     (347–2589)
    35-x-war.js          126줄  fn 10  전쟁 관전·지휘·지시자·HUD           (2590–2713)
    36-r2-building.js    805줄  fn 3   그리기 헬퍼·계단 셀·건물 아이소      (2714–3516)

  합 fn 23 = 원문 23 · 최상위 이름 35 = 원문 35 · 최상위 실행문 0 = 원문 0
```

diff 는 **자르기 + 머리 마커 5줄 + `index.html` 등록 4줄**뿐이다. 코드 한 글자도 안 옮겼다.
자른 자리는 넷 다 **빈 줄 뒤 주석 배너 앞**이다(225/346/2589/2713).

**손댄 파일**: `public/client/32-m-render.js`(잘림) · 새 조각 4 · `public/index.html`(등록 4줄) ·
`scripts/split-verify2.sh`(새 검사기) · `인계/공통.md`(분할 규약 절) · 이 보고서.
**서버·econ diff 0** (`git diff --stat -- server/ sim/` 가 빈 줄이다).

---

## 2. 지시서와 다르게 한 것 — 둘

### ① 3–4조각이 아니라 **5조각**

지시서는 3–4를 제안했다. ⓐ 실측이 배너 경계를 다섯으로 갈랐고, 넷으로 줄이려면
**연결·보정(33)** 을 렌더 루프에 붙이거나 **전쟁(35)** 을 건물 그리기에 붙여야 한다.
둘 다 성격이 다른 코드를 한 파일에 묶는 것이라 분할의 목적(충돌 지점 제거)에 어긋난다.
§2 가 "ⓐ 실측으로 확정"이라 했으므로 실측을 따랐다.

### ② **`32-m-render.js` 라는 이름이 이제 건축 오버레이를 담는다** — 개명은 회부

원문 머리가 배치·고스트였고 **순서 보존이 이름보다 위**다. 이름을 옮기려면
`split-verify.sh --selftest` 가 박아 둔 경로(`public/client/32-m-render.js`)를 같이 고쳐야
하는데, 이번 배치의 diff 규약은 "자르기+마커+등록뿐"이다. ⇒ **안 건드렸다. 회부한다**
(개명 + 하네스 1줄을 한 배치로).

---

## 3. 검증

### ⓐ 결합 diff — **identical** (이 배치의 심장)

```
  bash scripts/split-verify2.sh --selftest 32-m-render \
       32-m-render,33-m-conn,34-m-renderloop,35-x-war,36-r2-building

    ✓ 오염된 조각에서 differ — 검사기가 실패할 줄 안다 (원복 확인)
    identical — … 를 이어 붙이면 HEAD 의 32-m-render.js 와 바이트 동일
```
★자명 통과 금지를 먼저 통과했다: 조각 하나의 `const` 를 `donst` 로 한 글자 바꾸면 `differ` 다.
그 확인 없이는 "identical" 이 아무 뜻이 없다(족보 (79)(80)).

### ⓑ 조각별 문법 · 이름 감사

```
  node --check                     5조각 전부 OK
  node scripts/test-client-globals.js   ★PASS 12 / FAIL 0
      · 등록 classic 25개 · 최상위 선언 934개 (빈 검사 아님)
      · 중복 선언 0 · 브라우저 전역(실측 964) 교집합 0
      · ③ 99-main 밖 새 최상위 실행문 — **증가한 조각 0 · 기준선 합계 115 유지**
      · ④ 픽스처로 셋 다 일부러 깨뜨려 잡히는지 + 깨끗한 대조 통과
```

### ⓒ 실클라 e2e

```
  node scripts/e2e-ui.js     PASS 29 / FAIL 0   (패널·무들·히스테리시스 · 클라 JS 예외 0)
  node scripts/e2e-zoom.js   통과 28 / 실패 0   (배율 표·오프스크린·페이지 오류 0)

  bash scripts/run-regress.sh   (표 `// @regress` 자동 발견 **68개**)
      ★통과 63 · 실패 5
```

**실패 5개 — 하나도 이 배치의 것이 아니다.** 추측으로 정하지 않고 전수로 갈랐다:

| 하네스 | 러너 | 귀속 근거 | 판정 |
|---|---|---|---|
| `test-site-memo` | ✗ `씨앗 DB 없음` | **서버 하네스** — 이 배치의 diff 는 `public/client/*` 와 `index.html` 뿐이다. 닿을 경로가 없다 | 상류 순서 의존. 내 것 아님 |
| `test-tick-slicer` | ✗ ③ 프레임 상한 · ⑧ 전제 | 같음(서버 성능 하네스 · 2코어에서 불안정) | 내 것 아님 |
| `e2e-charsprite` | ✗ 31/4 | ★**베이스(`77ec3352`) 깨끗한 클론에서 같은 4건이 같은 값으로 실패**한다 | 내 것 아님 |
| `e2e-cold` | ✗ 존 입장 실패 | ★**단독 재실행 44/0** | 러너 부하 흔들림 |
| `e2e-trade` | ✗ `vid=null · 24749px` | ★**단독 재실행 41/0** | 러너 부하 흔들림 |

★그리고 **`e2e-move` · `e2e-rumor` 는 이번엔 통과했다** — 상류가 고쳤다(내 몫 아님, 기록용).
★렌더를 직접 재는 것들이 전부 초록이다: `e2e-ui` 29/0 · `e2e-zoom` 28/0 · `e2e-tilestate` 39/0 ·
`e2e-mtcut`·`e2e-mtocc`·`e2e-mtfuzz`·`e2e-cutaway`(건물·가림·계단 그리기) · `e2e-rooms` · `e2e-bigmap-live`.
콘솔 오류: 그 하네스들이 각자 "페이지 오류 0 / 클라 JS 예외 0" 을 재고 통과했다 — 분할 전과 같다.

```
```

### ⓓ 기준선 (econ)

`git diff --stat -- server/ sim/` 가 **빈 줄**이다 — 이 배치는 시뮬레이션 코드를 한 글자도
건드리지 않았다. 기준선이 달라질 경로가 없다. 그래도 러너로 한 번 돌렸다:

```
  bash scripts/ab-extinct.sh t51        (3시드 × 800일)
  node  scripts/ab-summary.js t51

    s1020: 6,280 · 0/51     s7: 5,993 · 0/51     s42: 6,072 · 0/51     무기Q 787
```
★**온보딩 v2 배치의 값과 세 수가 같다**(6,280 / 5,993 / 6,072 · 소멸 0/51 · 무기Q 787).
econ 무접촉의 실증이다.

---

## 4. 대리 지표 (지시서 §3)

```
  가장 큰 조각 2,245줄 — **목표(2,000 미만) 미달성.**
```
★이유를 숨기지 않는다: **`render()` 한 함수가 2,195줄**이다. §1 이 "함수 경계에서만"이라 했으므로
이 함수를 쪼개지 않는 한 2,000 아래로 못 내려간다. 함수 내부를 가르는 건 자르기가 아니라 **리팩터**다
— 행동 변경 0 배치에서 할 일이 아니다. ⇒ **`render()` 분해는 회부**(아래 §5).

나머지 넷은 123·226·805·126줄 — 파일 하나를 열어 전부 읽을 수 있는 크기가 됐다.
충돌 지점 기준으로는 이게 본론이다: 전쟁 세션과 건축 세션이 이제 **다른 파일**을 만진다.

---

## 5. 회부 (구현 안 함 — 지시서 §4 + 이 배치가 새로 본 것)

* ES 모듈 전환(T31) · 영역 순도 재배열 · `41-h-char` 2차 분할 · IIFE-2 껍데기 — 지시서 §4 그대로.
* ★**`render()` 2,195줄 분해** — 2차 분할이 여기서 멈춘 진짜 이유. 함수 안을 갈라야 하므로
  "행동 변경 0" 배치로는 불가능하다. 별도 리팩터 배치 + 픽셀 대조군(공통.md 의 "같은 빌드 두 번")이 필요하다.
* ★**`32-m-render.js` 개명** — 내용이 건축 오버레이인데 이름이 render 다.
  `split-verify.sh` 의 `--selftest` 경로 1줄과 함께 옮겨야 한다.
* ★**`split-verify.sh` 는 `differ` 일 때 EPIPE 스택을 토한다**(`cmp -s` 가 먼저 파이프를 닫는다).
  판정 자체는 맞지만, 처음 보는 사람은 검사기가 죽은 줄 안다. `rebuild` 에 EPIPE 무시 한 줄.
* ★**`70-lobby.js` 에 `// @@split-added` 표가 없다**(온보딩 v2 가 만든 파일 · `65-s-chronicle.js` 는 달려 있다).
  지금은 `split-verify.sh` 가 어차피 `differ` 라 티가 안 나지만, 1차 분할 검사기를 다시 초록으로
  만들 배치에선 이게 걸린다. **한 줄 추가 — 그 배치에서 같이.**
  (⚠반대로 **T51 의 다섯 조각엔 이 표를 달면 안 된다.** 그건 "분할 뒤 새로 만든 파일"의 표고,
   이 다섯은 잘라낸 조각이라 결합에 들어가야 맞다.)

---

## 6. 남긴 것

```
  scripts/split-verify2.sh    2차 분할 전용 결합 검사 (부모 1 vs 자식 N · 자가 검사 포함)
                              다음 2차 분할(41-h-char 등)이 그대로 재사용한다
```

---

## 7. 리베이스 — 푸시 직전에 상류가 3커밋 전진했다

번들을 맥으로 옮긴 자리에서 `origin/main` 이 `77ec3352` → `16cd4a71` 이었다
(T48 집 의뢰 · T44 HP 캐논 변경 · T44 보고). **리베이스했다 — 충돌 0.**
상류 3커밋은 `public/client/` 와 `index.html` 을 **한 줄도** 안 건드렸다
(`git diff --stat 77ec3352 16cd4a71 -- public/client/ public/index.html` 이 빈 줄이다)
⇒ 리베이스 전에 잰 러너 결과가 그대로 유효하다. 그래도 다시 쟀다:

```
  BASE=HEAD~1 bash scripts/split-verify2.sh 32-m-render 32-m-render,33-m-conn,…
      identical                                  ← 새 베이스에서도 바이트 동일
  node scripts/test-client-globals.js            PASS 12 / FAIL 0
  node scripts/e2e-ui.js         PASS 29 / FAIL 0
  node scripts/e2e-zoom.js       통과 28 / 실패 0
  node scripts/e2e-tilestate.js  통과 39 / 실패 0
  node scripts/e2e-mtcut.js      판정 26/26 전부 통과   (산 절단 — 가장 그리기에 예민한 것)
  기준선 3시드×800일             6,280 / 5,993 / 6,072 · 소멸 0/51 · 무기Q 787  ← 리베이스 전과 같다
  git diff --stat 16cd4a71 HEAD -- server/ sim/  빈 줄 (서버·econ diff 0)
```

⚠**러너에서 초록이던 `e2e-mtcut` 이 단독 첫 판에 전제(⓪ 걸친 띠 0장)에서 죽었다.**
같은 트리에서 한 번 더 돌리니 26/26 이다. 스폰 자리에 따라 "경계에 걸친 산 띠"가 없을 수 있는
하네스라 그렇다 — 이 배치의 것이 아니지만, **자기 전제를 못 세우면 그냥 멈추는** 좋은 하네스다(기록용).

**푸시**: `batch/T51` 먼저 → `main`. `16cd4a71..79a5f033` **패스트포워드 · force 0**.
⚠맥 중계 저장소에서 `git update-ref -d` 가 lock 파일 unlink 권한이 없어 실패한다
(`t51-import`·`t51b-import` 가 로컬에 남았다). 중계본일 뿐이라 무해하다 — 다음 사람이 놀라지 말라고 적는다.
