# 보고 — T147 따라가기: 어디 있는지, 그리고 그리로 (2026-09-07)

베이스 `<착지 직전 기입>` · 브랜치 `batch/follow-0907` → ff → `main` · 커밋 `[재민 확정 2026-09-07 · T147]`
**해시 `<푸시 뒤 기입>`** · **force push 0** · **`.git/*.lock*` 0**(T143 정정 패턴 · 푸시 뒤 전수)

한 줄: 회부 A-1 의 마지막 칸. **새 패널 0 · 새 표 0 · 새 수 0 · 새 타이머 0 · central 새 문 0.**
거리·방위·화살·반경 둘이 **전부 남의 정본**이고, 이 카드가 만든 것은 배선과 말뿐이다.

---

## §0 실측 (셋)

### ⓐ T110 화살의 규약 — 그리고 **이 카드가 처음 만나는 것**

| 물음 | 실측 |
|---|---|
| 무엇이 세우나 | `notice{kind:'downed', downed:{pid,x,y,at,windowMs}}` → `30-n-net.js` 가 `window.__downedCries` 에 담는다 |
| 무엇이 끄나 | **둘**: 창 소진(`drawDownedArrows` 안) · `player_down_state{isDown:false}` |
| 표적이 움직이면 | **안 갱신된다** — `x,y` 가 고정이다. **쓰러진 사람은 안 움직이기 때문이다** |
| 몇 개까지 그리나 | 맵의 **모든 항목**(상한 없음) ⇒ 자리를 따로 두면 외침과 따라가기가 **같이 뜬다** |
| 그리는 함수 | `drawWorldArrow(tx,ty,text,color)` — 100px 안이면 안 그린다(방향이 없다) |

⇒ **움직이는 표적은 이 카드가 처음이다.** 그래서 갱신 자리를 하나 정해야 했다(ⓒ).

### ⓑ 다른 존의 벗 — 존은 남의 존 사람을 **모른다**

* 존이 아는 사람은 자기 `players` 맵뿐이다(핸드오프 유령 제외).
* central 은 `players.last_zone` 을 갖고 있고 **`GET /player/<id>` 가 이미 그걸 준다**(T115·T128 이 쓰는 그 문).
  ⇒ **새 문 0.** 좌표(`last_x/last_y`)도 그 행에 있지만 **안 쓴다** — 존 간 좌표는 캐논 회부다.
* 존 id → 이름은 `zone-config.ZONES[<id>].displayName` 이고 `zone.js` 가 **이미 그 표를 들고 있다**.
  ⇒ "다른 곳에 있다 — 캐나디아 북림" 한 마디가 새 문 없이 나온다.

### ⓒ 갱신 주기 — **초당 하나 나가는 `gauges`** 에 얹었다

| 후보 | 판정 |
|---|---|
| 알림(`notice`) | ✗ 초당 한 줄이면 `window.__notices` 규약이 더러워진다(**28개 하네스**가 그 배열을 읽는다) |
| 틱(30Hz · AOI) | ✗ 초당 30번 보낼 이유가 없다. 게다가 AOI 밖의 벗은 틱에 안 실린다 |
| **`gauges`(초당 1회 · self 전용)** | **○** — `zone.js:11209` 에 이미 있다. **새 창구 0 · 새 타이머 0** |

`follow` 칸은 **따라갈 때만** 실리고, 끄는 그 순간 한 번 `null` 이 실린다(화살을 거두라는 말).

---

## §1 만든 것

```
  server/zone.js        followChat() — 채팅 분기 **한 줄**로 붙는 명령 다섯
                        _followPayload() — 갱신·끄기가 **여기 한 자리**(도착·사라짐·숨음)
                        /followdbg — 관측창(`/friendsdbg`·`/guilddbg` 와 같은 규약 · 읽기 전용)
  server/friends.js     friendByName() — 캐시의 **수락된 쌍**에서 이름 하나
  30-n-net.js           `'follow' in msg` 한 줄 — `gauges` 에 실려 온 자리
  34-m-renderloop.js    drawFollowArrow() — **T110 의 `drawWorldArrow` 를 그대로 부른다**(붓질 0줄)
  scripts/e2e-friends.js ④-2절 + ⑦ 소스 아홉(43 → 80건)
```

## §2 수를 하나도 안 만들었다

| 무엇 | 어디서 오나 | 값 |
|---|---|---|
| "멀다"의 경계 | `Rescue.shoutRange()` = `MOVE_SPEED × RESCUE_WINDOW × HEAR_FRAC` | 2,880px |
| 도착 | `RESCUE_RANGE_PX`(T43) | 80px |
| 걸음 | `Rescue.steps()` | 그대로 |
| 방위말 | `Onboarding.dirWord()`(T56) | 그대로 |
| 화살 | `drawWorldArrow()`(T110) | 그대로 |

⑦k2 가 `_followPayload` 안에 **손으로 적은 수가 0개**임을 못 박는다.

## §3 프라이버시 — 두 자리

1. **친구만.** 친구가 아니면 `자네의 벗이 아니다` 하나뿐 — **그 사람이 있는지조차 말하지 않는다**
   (`friendByName` 이 `null` 을 주고, "모름"과 "아님"을 여기서 가르지 않는다. 가르면 그 차이가 곧 누수다).
2. **`/숨기`** — 세션 플래그 하나(DB 0). 숨으면 같은 존에 있어도 **"다른 곳"까지만** 보이고,
   따라가기도 안 걸린다. 이미 따라가던 화살은 다음 초에 거둬진다.

## §4 검증

```
  scripts/e2e-friends.js    80 / 0   ★43 → 80 (④-2절 + ⑦ 소스 아홉)
  scripts/e2e-downed.js     35 / 0   ★카드 지정 — T110 화살 규약 무변
  scripts/e2e-onboarding.js 62 / 0   ★카드 지정 (아래 ⚠ 참고)
```

**픽셀 하네스 열여덟 — 전부 돌렸다**(공통.md §2 ⑦ · `34-m-renderloop` 을 만졌으므로):
```
  bigmap-live 11/0 · charsprite 90/0 · cutaway 15/0 · downed 35/0 · fishing 31/0
  mountain 40/0 · mtcut 26/26 · mtfoot 12/12 · mtfuzz 10/10 · mtocc 18/18
  nature 86/0 · rooms 66/0 · terrain 33/0 · tilestate 39/0 · ui 125/0
  waterperf 16/0 · weather 31/0 · zoom 28/0            ⇒ 열여덟 **전부 초록**
```
⚠**1차 일괄 실행에서 넷이 빨갰다**(`mtcut`·`mtfuzz`·`rooms`·`mtfoot`). 원인은 카드가 아니라
**앞 하네스가 남긴 서버**였다 — `EADDRINUSE 0.0.0.0:3010`. 하네스끼리 3010/3020 을 공유하는데
`timeout` 으로 끊긴 판이 자식 서버를 남긴다. ⇒ **판 사이에 포트 임자를 PID 로 죽이고**(`pkill -f` 금지 · 족보)
다시 돌리니 넷 다 초록. 상자 사정이지 카드의 것이 아니다.

⚠**정직 보고 — `e2e-onboarding` 이 세 판 중 한 판 빨갰다**: `58/4`(빈터 권리 · 사유지 · 허기 문턱)
그리고 `62/0` · `62/0`. 같은 폴더에서 **베이스(`HEAD~1`)는 61/0**. 빨간 세 줄은
`텔레포트 → 사유지 걸기` 순서와 `심각도 0.500 < 0.490`(문턱 **바로 옆**)이라 둘 다 판이 느릴 때
흔들리는 자리이고, **이 카드의 접점(채팅·`gauges` 한 칸·화살)과 닿지 않는다**.
T139 때도 같은 세 줄이 한 번 나고 재현이 안 됐다(그때도 베이스 61/0 · 내 것 61/0).
⇒ **상자 부하로 본다.** 틀렸다면 이 문단이 틀린 자리다.

### ★★리베이스 뒤 재측정 (main 이 `814b9e6d` 까지 갔다 — T135 나무층 이식 · T146·T148-A·T149·T150·T154·T156)

겹치는 파일 **둘**: `server/zone.js` · `public/client/30-n-net.js`. **충돌 0**(내 접점은 분기 한 줄 · 함수 둘 ·
관측창 하나 · `gauges` 한 칸 · 클라 한 줄이고, 상류가 만진 절과 안 겹친다).
```
  e2e-friends 80/0 · e2e-ui 127/0(상류가 둘 늘렸다) · e2e-downed 35/0
```

**적재 추적**: `node scripts/lab-modules.js --diff HEAD~1` → **교집합 0개**
(내 diff 다섯: `zone.js` · `friends.js` · `30-n-net.js` · `34-m-renderloop.js` · `e2e-friends.js`)
⇒ 3시드 800일 A/B 갈음(공통.md §2 ①).

### ★★자명 통과 금지 — 이 절이 실제로 재는 것

| 하네스 | 무엇을 못 박나 |
|---|---|
| ④-2ⓐ | 친구가 아니면 **거절한다**(존재 여부도 안 흘린다) |
| ④-2ⓑ·ⓒ | 같은 존이면 방위+걸음 · **T110 반경 밖이면 방향만**(걸음을 말하지 않는다) |
| ④-2ⓓ·ⓓ2 | 화살 자리가 `gauges` 로 오고 그게 **벗의 지금 자리**다 |
| ★④-2ⓔ | **표적이 움직이면 화살도 따라 돈다**(두 자리를 대조 · 이 카드의 핵심) |
| ④-2ⓕ | **도착하면 저절로 거둬진다**(반경도 T43 것) |
| ④-2ⓖ·ⓖ2 | 숨은 벗은 **"다른 곳"까지만** 보이고 **따라갈 수도 없다** |
| ④-2ⓗ | 벗이 나가면 거둬진다 |
| ★★④-2ⓘ | 따라가는 동안 **알림이 안 쌓인다**(`__notices` 규약 무변) — 그리고 그동안 `gauges` 는 여러 번 왔다(자명 통과 방지) |
| ⑦i·⑦i2 | 화살은 **T110 함수를 그대로 부른다**(붓질 0줄) |
| ⑦j·⑦j2 | 자리가 **둘**이고 **같은 프레임에 둘 다** 그린다 |
| ⑦k·⑦k2·⑦l | 반경·걸음이 전부 정본 · **손으로 적은 수 0** · 갱신이 **알림으로 안 나간다** |
| ⑦m·⑦n | 클라는 `null` 을 "거두라"로 읽는다 · 클라에 이름→벗 대응이 **없다** |

## §5 무접촉 확인

`T135`·`T146`(`zone.js` 다른 절) · `T148`(클라 나무 렌더) — 0줄. 내 `zone.js` 접점은 **채팅 분기 한 줄 ·
함수 둘 · 관측창 하나 · `gauges` 한 칸**이고, `34-m-renderloop.js` 접점은 **T110 화살 절** 하나다(나무 절 무접촉).
central — **0줄**(문을 하나도 안 냈다). 게임 행동 변경 0 — 안내와 화살뿐이다.

## §6 회부

1. **존 간 좌표·이동**(캐논) — 지금은 존 이름까지다.
2. **`/숨기` 영속** — 세션 동안만(DB 0).
3. 부름 만료(재민 값 판정) · 길드 패널(캐논) · 길드 벌점 층 · 유저 마을 소개문 끝까지 검증.

## §7 실기 두 줄 (재민)

* **친구가 있는 쪽으로 화살이 서고 친구가 움직이면 따라 도는가** — `/따라가기 <벗이름>` 뒤 그 벗이 걷는다.
* **친구가 `/숨기` 하면 안 보이는가** — 그 벗이 `/숨기` 한 뒤 `/어디 <그 벗>`.
