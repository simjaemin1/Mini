# BGM R&D(세션13/GPT · `codex/bgm-rnd-01` · 86커밋 → 한 커밋으로 착지) — PM 착지 메모 2026-09-25

**무엇이 들어왔나.** 재민이 PM 부재 중 GPT(Codex)에게 BGM R&D 를 시켰다. 결과: 16마디·34.8초 아리랑 대금 청취본 **다섯**(B0 평음 · B1 문맥 규칙 요성 · B2-R 국악원 산조 F0 윤곽 참조 · B2-Rd 깊이 보정 · B2-Rdf 긴 fade) — 파일은 git 밖(`/Users/simjaemin1/.codex/worktrees/durango-mini-bgm-rnd-01/_bgm_rnd/` · 3.4GB · **지우지 말 것**). 공개된 **DDSP-Gugak 대금 체크포인트**를 찾아 fail-closed CPU 렌더러(`tools/ddsp-gugak-public-runtime/`)를 만들었다 — ⚠학습·게임 이용 권리 미확인(`BLOCKED`).

**코드.** `public/assets/audio/bgm/bgm.js` +433(프레이즈 계획 `planPerformancePhrase` · `variant:'phrase'` **opt-in** · 기본 `legacy` 경로 무변 — `test-bgm-phrase` 16/0 · `e2e-bgm-phrase`) · `tools/` 24개 폴더(파이썬 계측·계획 JSON · 7만 줄) · `scripts/` 14(테스트 py/js). `server/`·`sim/`·배포 13곡·`sfx/`·`render-meta`·CREDITS **무접촉**. 게이트(PM 컨테이너): `test-audio` 125 · `test-client-globals` 26 · `test-harness-lint` 61 · `test-assets-audit` PASS · `lab-wiring-check` 0 · 3시드 면제.

**규약과 어긋난 것(기록).** ① 카드 0 · 커밋 86 · 설계 문서 11장을 `설계/`(재민 확정 정본 폴더)에 썼다 — **전부 R&D 미확정**으로 읽는다(제목 `BGM_RND_*`). ② 제품 파일 `bgm.js` 를 SOUND 영역(세션9) 카드 없이 만졌다 — opt-in 이라 기본 비트 동일이지만 **세션9 검토 카드**가 필요하다. ③ 결론을 안 냈다("어느 것도 최종안 확정 안 함") — 맞다, 그건 재민 귀다(#66). ④ 새 수: 요성 3.45Hz(잠정 · 근거 없음) · 18cents(`motif.py` 규칙표 값) · 0.72s 박 · 0.18s 진입/감쇠 — 문서가 "잠정" 이라 적었다.

**다음.** #66 재민이 다섯 파일을 듣는다 → 방향 하나 → 세션9 검토 카드(bgm.js opt-in 유지/제거) · 세션8 BGM 다음 걸음(뱅크 #64 와 같이) · DDSP-Gugak 체크포인트 라이선스 확인(재민).
