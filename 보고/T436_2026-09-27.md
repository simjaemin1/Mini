# T436 보고 — 시딩 게이트가 교역을 본다 (★PM #78 ⓐ · 세션6 · 2026-09-27)

**한 줄 답:** 게이트에 교역 잠재 항 하나를 넣었다(`T436_GATE_TRADE` · 기본 끔). 한반도에서 유도한 계수 **K = 0.154081** 로 한반도 탈락 9곳이 **9/9 통과**하고 통과 42곳은 **42/42 그대로**다. 그런데 그 K 로는 **중원북·닛폰 시딩이 한 곳도 안 바뀐다** — 중원북은 1곳(양농촌)이 하한을 넘지만 간격(6,900 < 12,000)에 걸리고, 광산 후보(창광산·카토쿠·지옹다·지옹사키)는 필요 K 가 0.278~0.428 이다. 켬 3시드 = 끔 3시드(바이트 동일) · 끔 = 세 존 시딩 JSON 동일 · 한반도 여덟 수 동일.

* 가지 `batch/gate-trade-0927` = 내용 커밋은 아래 해시 커밋 줄이 적는다 · **[승인 대기 · T436]** · 베이스 `092f920e`(main)
* 새 수 0(계수는 유도 · 문턱 2.0 무변 · 격자·스냅·시계는 있는 것) · 손잡이 하나 · 기본 끔 · DB 무접촉 · `villages.js` 게이트 자리(+ `coarseOpen` 선택 인자 · 관측 훅 하나) · `economy-sim-v2` 내보내기 한 줄.
* ⚠카드는 "닛폰 후보 21" 이라 적었지만 정본은 **16**(T351 이 정본 칸에 적은 수 · `siteCandidates`)이다. 16 으로 쟀다.

| 판 | 한반도 51 | 닛폰 16 | 중원북 25 |
|---|---|---|---|
| 끔(현행) 하한 통과 | 42(게이트는 전수라 우회 · 시딩 50) | 10 → 시딩 7 | 16 → 시딩 11 |
| **켬(K 0.154081)** 하한 통과 | **51** · 시딩 50(무변) | **10** · 시딩 **7(무변)** | **17**(+양농촌) · 시딩 **11(무변 — 간격)** |
| 광산 후보 필요 K | 광산1 0.154 ← 이 값을 묶는 자리 | 카토쿠 0.329 · 지옹다 0.370 · 지옹사키 0.428 · 후지로 ∞(닿는 짝 0) | 창광산 0.278 · 단광산 ∞(주머니 · 닿는 짝 0) |

---

## 0. §0 실측

### ⓐ 항 diff + 계수 유도(9/42)

**`server/villages.js`**
1. `pickSeedVillages` — 식 한 자리 `foodOf(lp)`(종전 식 그대로 · 산수 순서 무변) · `_tradeOf`(켬일 때만) · 하한 줄:
   `if (food < FOOD_FLOOR && !(_tradeOf && food + T436_K × 교역잠재 ≥ FOOD_FLOOR)) return 0;` — **통과 뒤 점수는 종전 그대로 `food × (0.5 + 도구 접근)`**.
2. `_gateTradePotential(all, ta, foodFn, floor)` — 후보마다 Σ(닿는 이웃 후보의 `max(0, food − 하한)` ÷ `travelDaysForDistance(유클리드 × 2.5)`).
   * 닿음 = 교역 거리행렬과 **같은 코스 격자**(`DIST_STEP` 4셀)·**같은 열림 판정**(`coarseOpen`)·8방·코너 절단 금지·같은 나선 스냅(반경 6노드 · 16방) → 성분이 같으면 닿는다.
   * 다리는 **존 설정 `bridges`** 에서 읽는다(`coarseOpen` 에 선택 인자 `brSet` — 안 주면 종전 그대로 · 거리행렬·캐러밴은 안 준다). 서버는 어댑터에 다리 훅이 꽂혀 있고 계측기·랩은 안 꽂혀 있다. 이 인자로 두 자리가 **같은 답**을 낸다.
   * 시계 = `economy-sim-v2.travelDaysForDistance`(캐러밴·소문 T7 이 쓰는 그 시계 · 이 카드가 내보내기 한 줄을 더했다).
3. `const T436_K = 0.154081` — 유도값(아래). `opts._scoredOut`(관측 전용 · 표 기계가 정본 `_land > 0` 을 읽는다) · `__labProbe._gateTradePotential`·`T436_K`.

**계수 유도 — 한반도 후보 51**(`scripts/t436-gate-trade.js` · 하한 미달 9곳이 전부 통과하는 가장 작은 K = max((2.0 − food) / 교역잠재)):

| 미달 후보 | food | 교역 잠재 | 필요 K |
|---|---|---|---|
| **광산1** | 0.589 | 9.158 | **0.15408** ← 묶는 자리 |
| 광산3 | 0.809 | 13.709 | 0.0869 |
| 농촌10 | 1.198 | 12.112 | 0.0662 |
| 농촌15 | 1.212 | 15.449 | 0.0510 |
| 농촌9 | 1.415 | 14.428 | 0.0405 |
| 농촌18 | 1.509 | 13.987 | 0.0351 |
| 임업4 | 1.732 | 12.743 | 0.0210 |
| 농촌20 | 1.740 | 15.109 | 0.0172 |
| 광산7 | 1.855 | 9.294 | 0.0156 |

* 유도값 0.1540802… → 코드 `0.154081`(소수 여섯째에서 올림 — 광산1 이 부동소수 오차로 떨어지지 않게 · 표 기계가 코드 상수와 견준다: **같음**).
* **통과 42곳은 무변**이다 — 항은 음수가 아니고 하한 미달에만 쓰이며, 점수 식은 그대로라 42곳의 점수·순서가 한 자리도 안 움직인다. "두 자리가 겹치는 값" 은 **있다**(K ≥ 0.15408 전부).
* 한반도 교역 잠재는 51곳 전부 6.56 이상(중앙 13.1)이다. 본토 한 덩어리 · 잉여 있는 이웃이 많다.

**정본 대조** — 켠 **자식 프로세스**에서 정본 `pickSeedVillages(후보 전부, { seedAll:false, _scoredOut })` 의 `_land > 0` = 표의 통과:

| 존 | 끔 통과(표 = 정본) | 켬 통과(표 = 정본) | 교역 잠재 계산 시간(계측기 · 메모 없음) |
|---|---|---|---|
| 한반도 | 42 = 42 ✓ | 51 = 51 ✓ | 10.9초 |
| 닛폰 | 10 = 10 ✓ | 10 = 10 ✓ | 4.4초 |
| 중원북 | 16 = 16 ✓ | 17 = 17 ✓ | 6.4초 |

### ⓑ 재시딩 표 + 두 존 3시드 켬/끔

**시딩(`T17_ZONE` 자 · 캐시 새로 뜸)** — 켬과 끔의 시딩 JSON 이 **세 존 전부 바이트 동일**이다:

| 존 | 끔 시딩 | 켬 시딩 | 왜 안 바뀌나 |
|---|---|---|---|
| 한반도 | 50 | 50 | `seedAllVillages` — 게이트를 안 탄다 |
| 닛폰 | 7 | 7 | 하한을 넘는 새 후보 **0** — 미달 여섯의 필요 K 0.329~0.546(후지로 ∞) |
| 중원북 | 11 | 11 | 새로 넘는 **양농촌**(food 1.478 · 잠재 5.831 · 필요 K 0.090)이 **소농촌에서 6,900px**(간격 12,000 미만)라 선별에서 빠진다 |

**3시드 800일**(`t17-metrics` · 켬 JSON = 끔 JSON 바이트 동일):

| 존 | 인구 1020 / 7 / 42 | 소멸 | 광산 마을 | 비고 |
|---|---|---|---|---|
| 중원북 끔 = 켬 | **1,736 / 1,599 / 1,792** | 0/11 ×3 | 0 | T409 표와 글자 같다 |
| 닛폰 끔 = 켬 | **1,020 / 1,092 / 1,072** | 0/7 ×3 | 0 | T429 표(T408 뒤)와 같다 · T407 표(934/1,097/1,086)는 T408 전 |

광산 마을이 없으니 T429 자의 광산 교역 비율 칸은 **빈 칸**이다(계측기는 중원북 켬 3시드를 돌렸다 — 광산 0곳).

**필요 K 사다리**(참고 · 판정 0 — 광산이 서려면 K 가 얼마여야 하나 · 가장 가까운 시딩 마을까지 거리):

| 존 | 후보 | 유형 | food | 교역 잠재 | 필요 K | 가장 가까운 시딩 마을(px) |
|---|---|---|---|---|---|---|
| 중원북 | 양농촌 | plain | 1.478 | 5.831 | 0.090 | 6,900 ✗ 간격 |
| | 태농촌 | plain | 1.499 | 2.908 | 0.172 | 22,584 |
| | **창광산** | mining | 0.801 | 4.313 | **0.278** | 22,358 |
| | 초농촌 · 천농촌 · 조농촌 · 촉농촌 | plain | 1.086 | 2.68~3.04 | 0.301~0.341 | 4,254~24,137 |
| | 옥농촌(광맥 2.02) | plain | 0.816 | 2.402 | 0.493 | 18,640 |
| | **단광산** | mining | 1.692 | **0** | ∞ | 20,939 — 주머니(T409 §2 · 닿는 짝 0) |
| 닛폰 | **카토쿠** | mining | 0.610 | 4.222 | **0.329** | 13,185 |
| | **지옹다** | mining | 1.005 | 2.693 | 0.370 | 7,162 ✗ 간격 |
| | 쿠라마 | plain | 0.666 | 3.478 | 0.384 | 10,922 ✗ 간격 |
| | **지옹사키** | mining | 1.037 | 2.249 | 0.428 | 16,700 |
| | 타카노 | plain | 1.156 | 1.545 | 0.546 | 18,415 |
| | **후지로** | mining | 0.568 | **0** | ∞ | 12,123 — 설 자리 없음(T373) |

* 작은 존의 교역 잠재는 **한반도의 1/3~1/2**(닛폰·중원북 1.5~5.8 · 한반도 미달 9곳 9.2~15.4)이다. 잉여를 내는 이웃이 적고 멀다. T429 의 "식량 짝 22~29곳" 이 여기에는 **없다**는 것이 이 수의 뜻이다.
* 한반도에서 유도한 K 는 한반도에서 **사는** 마을을 다 들이는 가장 작은 값이다. 그 K 로 작은 존이 안 열리는 것은 게이트 탓이 아니라 **작은 존의 교역 잠재가 작기** 때문이다. 더 큰 K 는 한반도에서 근거가 없는 수다(판정 0 · 회부 ②).

### ⓒ 게이트

* **끔 = 세 존 시딩 JSON 비트 동일** — main 워크트리(`092f920e`) 대 가지: hanbando `8ca1e560…` · nippon `7137afe3…` · jungwon_n `2b13f606…`(sha256 앞 12) 셋 다 `cmp` 동일. 부팅 하네스 둘(`test-nippon-boot` 42/0 · `test-jungwon-boot` 38/0)도 끔에서 초록이고, `test-jungwon-boot` 은 **켬에서도 38/0** 이다.
* **한반도 3시드 여덟 수 동일** — `villages.js` 를 만졌으니 돌렸다: main 대 가지 JSON `cmp` 동일 ×3, 그리고 넷째 판-b(T420 `/tmp/t420/t17`)와도 동일(8,023 / 8,116 / 8,010). 면제 근거는 한 줄이다 — 한반도는 `seedAllVillages` 라 게이트(와 이 항)를 **안 탄다**. econ 은 이 항을 모른다.
* **한반도가 문을 탄다면**(재민이 `seedAllVillages` 를 끄면) — 켬에서 하한 **51/51 통과**(표 = 정본). 간격·상한(20)은 그 뒤 따로 걸린다(이 카드 밖).
* `build-econ-bundle --check` 8/0 · `inline-engine --check` 최신(내보내기 한 줄 반영) · `test-econ-fieldyield` 202/0 · `test-farm-life` 33/0 · `test-harness-lint` 61/0 · `lab-wiring-check` 실패 0 · 경고 0.

### ⓓ 한 줄 답 + 켜기 절차

한 줄 답은 머리와 같다.

**켜기 절차**(재민 배포 · 이 카드는 표만 · DB 무접촉):
1. 켜는 것은 **env 한 줄** `T436_GATE_TRADE=1` 을 **닛폰·중원북 존 컨테이너**에 준다(한반도는 줘도 무변 — 전수). `redeploy-zones.sh` 는 지금 존마다 env 를 고정으로 넘긴다(`ZONE_ID`·`PORT`·`DB_PATH`) — 이 한 줄을 넣는 자리는 T428(존 셋 기동) 쪽 배포 문법이다.
2. 시딩은 **그 존 DB 의 `villages` 가 비어 있을 때 한 번**만 돈다(`villages.js` Stage 2 · `dbRows.length === 0`). 이미 뜬 존을 다시 시딩하려면 `/srv/durango/<zone>/world-<zone>.db` 를 새로 떠야 한다. 한반도 DB 는 무접촉이다.
3. ⚠**지금 K 로는 켜도 두 존의 시딩이 안 바뀐다**(ⓑ 표). 켜기의 값은 K 를 다시 정하거나(회부 ②) 지형이 바뀐 뒤(닛폰 수동 지형 · 재민)에 생긴다. T428 이 두 존을 처음 띄울 때 켜 두면 재시딩이 따로 필요 없다.

## 1. 검증

* 새 수 0 · 손잡이 하나(`T436_GATE_TRADE`) · 기본 끔 · 끔 비트 동일(시딩 JSON 셋 · `t436-gate-trade` 끔 = T429 표 42 · 10 · 16) · 한반도 3시드 여덟 수 동일.
* 하네스 여섯: `test-nippon-boot` 42 · `test-jungwon-boot` 38(끔·켬) · `test-econ-fieldyield` 202 · `test-farm-life` 33 · `test-harness-lint` 61 · `lab-wiring-check` 0.
* T435(세션4 `zone.js`)·T432(세션1 `zone.js`)·T428(세션5) 무접촉.
* 실행 판: 시딩 9(세 존 × main/끔/켬) · 한반도 800일 6 · 두 존 800일 12(+ 광산 계측 3) · 병렬 0.

## 2. 접점

`pickSeedVillages`·`landScore`·`FOOD_FLOOR`(무변)·`seedAllVillages`(읽기) · `T436_GATE_TRADE`(신설) · `coarseOpen`(선택 인자 · 기본 무변) · `travelDaysForDistance`(내보내기) · `t429-gate-food`(끔 대조) · `t429-mine-food`(켬 계측) · `plan-bridges-v2`·`audit-reachability`(무접촉 · 다리는 존 설정에서 읽는다) · `path-core`(무접촉 — 거리행렬과 같은 코스 격자를 썼다) · `T17_ZONE`(자) · `fixture-boot`(부팅 하네스 둘을 거쳐) · `caravan` 시계.
새 파일: `scripts/t436-gate-trade.js`(표 기계 · 러너 밖).

## 3. 회부 (→ `인계/회부.md`)

1. **켜기 = 존 재시딩 — 재민**(배포 · T428 과 같이 · 지금 K 로는 두 존 무변).
2. ★**[PM/재민] K 를 무엇에서 정하나** — 한반도에서 유도한 K 는 작은 존을 못 연다(광산 필요 K 0.278~0.428). 더 큰 K 를 쓰려면 한반도가 아닌 근거가 있어야 한다(예: 그 존에서 교역으로 사는 마을의 실측 · 아직 없다 — 그 존엔 그런 마을이 서 본 적이 없다). 새 수라 이 카드는 안 정했다.
3. **[재민] 단광산·후지로는 교역 잠재 0** — 닿는 짝이 없다(주머니 · 설 자리 없음). 게이트로는 못 연다. 지형(고개 · T409 회부 ②) 일이다.
4. #76 · 닛폰 수동 지형(재시딩 전에 넣으면 한 번에) · #69 · #75 — 재민(그대로).
5. **T429 보고 정정 한 칸** — T429 §0-① 표의 닛폰 "탈락 7(+물 위 2 · 간격 1)" 은 **식량 하한 6**(+물 위 2 · 간격 1)이다(같은 표 기계 `gate-nippon.json` 의 탈락 6 · 이 카드 끔 통과 10 = 16 − 6).

증거: `/tmp/t436/{seed,hbM,hbB,jOff,jOn,nOff,nOn,mf}/` · `gate.json` · `run.log`.
