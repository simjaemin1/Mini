# 보고 — T211 게스트도 길드에 불린다 · 로비 이름칸의 별명은 어디로 갔나 (2026-09-12)

**푸시 완료 `de0e447`** (main) · 가지 `batch/guest-tribe-0912` → ff → main · **force 0** · **.lock 0**
베이스 `6b829fca`(리베이스 1회 · 겹친 파일 0) · 푸시 정본 2판(맥 VM `device_bash` · `$HOME/pushrepo` 얕은 클론 ·
`~/mnt/Mini/durango-mini` 무접촉 · PAT 는 맥에서만) · 접촉 파일 **4**

한 줄: **게스트 길드 부름 ○ · 별명은 지금 "조용히 버려진다(다만 남의 계정 이름이면 입장을 막는다)" · 길 셋은 #23.**
central **한 줄**(`findAccount` → `findPerson`) · 클라 0 · 새 규약 0 · 랩 적재 교집합 **0**.
`e2e-guild` **89/8 → 97/0**(빨간 8건은 전부 새 게스트 절 · 종전 89건 비트 동일).

---

## §0-ⓐ 표 하나 — 부름 사슬에서 **막혀 있던 자리는 하나뿐이었다**

| 자리 | 무엇으로 사람을 찾나 | 게스트 | T211 |
|---|---|---|---|
| `/tribe/invite` (`central.js:1164`) | **`findAccount`**(예약 술어 · `password_hash IS NOT NULL`) | **✗ 못 부른다** | → `findPerson` |
| `/tribe/invites` (`:1179`) | `player_id` | ○ 처음부터 멀쩡 | 무변 |
| `/tribe/invite_accept` (`:1183`) | `player_id` | ○ 처음부터 멀쩡 | 무변 |
| 밀린 부름(T139 `pendingLines`·`calledLine`) | `invitesOf(pid)` | ○ 처음부터 멀쩡 | 무변 |
| `/promote` 뒤 소속 | `players.tribe_id`(행을 안 갈아치운다 · 배치 14 ①) | ○ | 무변 |

⇒ **이름으로 지목하는 그 한 줄**만 벽이었다. T208 이 친구 셋을 옮길 때 여기 하나가 남았고,
그래서 **벗은 될 수 있는데 길드에는 못 불리는** 사람이 생겼다 — 게스트는 영속 신원인데(T115) 문이 아니라 벽이었다.

**실측 (고치기 전 → 고친 뒤 · 같은 하네스 절 · 베이스 워크트리)**

| | 베이스 | T211 |
|---|---|---|
| `e2e-guild` 전체 | **89 / 8** | **97 / 0** |
| ⑨ 게스트를 부른다 | ✗ `'여행자5jPP' 은 없는 이름이다` | ○ `여행자d_yF 을(를) [돌칼] 로 불렀다` |
| ⑨b 불린 게스트에게 알림 | ✗ `[]` | ○ `[돌칼] 이(가) 자네를 부른다` (`kind: guild`) |
| ⑨ `/수락` 한 마디로 든다 | ✗ `자네를 부른 길드가 없다` | ○ `[돌칼] 에 들었다` |
| ⑨d `players.tribe_id` | ✗ `null` | ○ `1` |
| ⑨ **밀린 부름**(꺼져 있는 게스트) | ✗ | ○ T139 문법 그대로 |
| ⑨ **승계 뒤 길드 유지** | ✗ `null` | ○ `1` |
| ⑨c·⑨f·⑨g(전제·대조·자명 통과 금지) | ○ 둘 다 | ○ |

⚠**초대제 길드에 들었다** — §② 가 `/길드 초대제` 를 걸어 둔 뒤라, 부름이 실제로 문 노릇을 하는 자리에서 쟀다.

## §0-ⓑ 표 둘 — 로비 이름칸이 지나는 자리 (게스트 / 등록 계정)

| # | 자리 | 게스트(비밀번호 0) | 등록 계정 |
|---|---|---|---|
| 1 | `30-n-net.js:238` `inputName` | 사람이 친 글자 | 〃 |
| 2 | `:240` `myName = inputName \|\| '여행자'` | **클라 안에서만 산다** — 서버는 `name` 파라미터를 **안 읽는다**(zone 에 `searchParams.get('name')` 이 없다) | 〃 |
| 3 | `:241` `myUsername = inputName` | | **로그인 이름** |
| 4 | `:677` `if (myUsername) params.set('username', …)` | 보내긴 한다 | 〃 |
| 5 | `zone.js:3722` `inUsername` | 받긴 한다 | 〃 |
| 6 | `zone.js:3733` `if (inUsername && inPassword)` | 비번이 없어 **안 탄다** | 인증·승계 경로 |
| 7 | **`zone.js:3775` `checkUsernameTaken(inUsername)`** | **여기서만 쓰인다 — 남의 계정 이름이면 입장 거절** | 해당 없음 |
| 8 | `zone.js:3817`(T208) `name = acct.name` | **버려진다** | `acct.name` |
| 9 | `zone.js:3906` 1회용 폴백(central 불가) | `inUsername \|\| 여행자N` — **여기선 아직 쓰인다** | 해당 없음 |

**실측 (이 카드가 탐침으로 직접 쟀다)**

```
ⓐ 이름칸 비움                              → welcome · central 이름 "여행자gth2"
ⓑ 이름칸에 '별명이야'(비번 0)               → welcome · central 이름 "여행자KfDz"   ← 친 이름은 조용히 사라진다
ⓒ 이름칸에 'alice'(비번 0 · 남의 계정 이름) → auth_error: username_taken · 입장 거절 ← 버릴 이름 때문에 막힌다
```

⇒ **T208 뒤 게스트의 이름칸은 "아무 일도 안 하거나, 입장을 막거나" 둘 중 하나다.**
쓰이는 자리는 `#7`(막는 문)과 `#9`(central 이 죽은 판의 1회용 이름) 둘뿐이고, 앞의 것은 **버릴 이름으로 사람을 막는다.**

## §0-ⓒ 표 셋 — 게스트가 별명을 갖는 길 셋 (**고르는 건 재민 · `판정대기` #23**)

| 길 | 무엇을 한다 | 딸려 오는 것 | 크기 |
|---|---|---|---|
| **ⓐ 이름칸을 게스트에게 숨긴다/비활성** | 로비에서 이름칸을 로그인 전용으로 만든다 | 별명 **없음**(게스트는 `여행자XXXX`) · `#7` 의 이상한 거절이 **사라진다** · 사본 0 · 예약/지목 충돌 0 · **central 무접촉** | 클라 한 줄 |
| **ⓑ 개명 문 (`/이름 <별명>`)** | central 이 게스트 행의 `name` 을 바꾼다 | **새 규약**(개명 엔드포인트) · 유일성을 누가 보장하나(발급 때의 재시도 루프는 발급에만 있다) · **예약 술어와의 관계**(별명이 등록 이름을 선점하면 안 된다 ⇒ `findAccount` 는 그대로, 대신 개명이 `findPerson`+`findAccount` 둘을 봐야 한다) · **지목 충돌**(별명이 남의 별명과 겹치면 `/친구`·`/초대` 가 엉뚱한 사람을 집는다 — `LIMIT 1`) · 개명 주기 제한(사칭 방지)이 딸려 온다 | central 한 문 + 클라 + 하네스 |
| **ⓒ 승계만이 이름을 갖는 길 (지금)** | `/promote` 로 계정이 되면 그때 이름이 생긴다 | 아무것도 안 한다 · 다만 **이름칸이 거짓말을 계속한다**(쳐도 안 쓰이고, 가끔 막는다 — §0-ⓑ ⓒ행) | 0 |

**PM 권고 아님 — 이 표는 자료다.** 다만 ⓒ 를 고르더라도 `#7`(버릴 이름으로 입장을 막는 자리)은
따로 볼 값이 있다: 지금은 **게스트가 남의 계정 이름을 쳤다는 이유로 못 들어온다.**

## §1 안 고친 것

* **클라 0** — ⓐ 도 표만이다(고르면 다음 카드 · 카드 §1).
* `zone.js` 접속 경로 **무접촉**(T203) · `#7`·`#9` 무변 · 예약·인증 축(`/check_username`·`/auth`·`/promote`) 무변.

## §2 접점

`server/central.js`(`/tribe/invite` 의 지목 술어 한 줄) · `scripts/e2e-guild.js`(`connectGuest` + §⑨ 13건).
심볼: `findAccount` `findPerson` `/tribe/invite` `tribe_invites` `invitesOf` `pendingLines` `calledLine`
`/수락` `/promote` `inUsername` `myUsername` `acct.name` `checkUsernameTaken`.

## §3 회부

1. ★**별명 길 셋**(§0-ⓒ) — `판정대기` #23. 재민 한 줄.
2. **버릴 이름으로 입장을 막는다**(§0-ⓑ `#7`) — 게스트가 남의 계정 이름을 치면 `username_taken` 으로
   **못 들어온다**. 그 이름은 어차피 버려지므로(T208) 지금은 막을 이유가 사라졌다. #23 과 같이 보면 된다.
3. K-1·탈퇴 시 소속(#17) · 시작 화면의 인증(회부 7) · 존을 넘는 `start_vid` · 첫 의뢰(#18) ·
   `nippon` 시딩(Z) · 길드 패널(캐논).

## §4 실기 한 줄 (재민)

* 게스트 창에서 `/초대` 를 받아 `/수락` 이 되나 — 그리고 그 사람 이름이 `여행자XXXX` 인 게 괜찮은가(#23).
