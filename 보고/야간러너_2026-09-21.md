# 보고 — 야간 러너 2026-09-21 (+0900)

**기준 커밋 `9dd4d731`** · 시작 **10:00** → 종료 **12:13** (2시간 13분)
· 오늘 묶음 **A** · 목록 **121종** · 돈 것 **121** · 통과 **116** · 실패 **5** · 끊김 **0** · 미측정 **0**
· 이 문서는 `scripts/nightly-report.js` 가 로그만 읽어 냈다 — **모델 판단 0** · 같은 로그면 바이트가 같다
· 로그 `/tmp/night/regress.log` · 단독 재실행 `/tmp/night/regress.log.retry` · 상시 목록 `scripts/nightly-known.txt`
· 직전 밤 `보고/야간러너_2026-09-20.md` — 빨강 11종 · "연속 n밤"은 **세기만** 한 수다(판정 0)

## ★ 한 줄 — **상시 밖 빨강 4건** — `test-shelter` · `test-site-memo` · `test-terraincache` · `e2e-onboarding`

## 1. 실패 표

| 하네스 | 연속 | 첫 판 | 소요 | 단독 재실행 | 귀속 |
|---|---|---|---|---|---|
| `test-body` | 연속 **1밤** | RC=1 · 결과줄 327개 · ★⑯㉧ 화면이 왜 안 되는지 말한다(서버 문구 · 클라 무접촉)  ["갯벌(한사리 썰물(갯벌이 드러났다))에서 해조 1 (남은 양 3.0)"] | 1분 | ★**단독 초록** (RC=0) | **상시(물때 두 판정)** — 공통.md §2 ⑥ · ⑯㉧ 갯벌 문구·짠물 기운 쌍 · `TIDE_FREEZE_MS=372603` 이면 306/0 (보고/T171 · 야간 09-13·09-14) |
| `test-shelter` | 연속 **1밤** | RC=1 · 결과줄 55개 · ② 기존 집과 안 겹친다  42/50 | 2분 | 여전히 빨강 (RC=1 · 결과줄 55개 · ② 기존 집과 안 겹친다  42/50) | **귀속 후보 없음(못 쟀다)** — 하네스 `scripts/test-shelter.js` 최종 손댐 `afe8a0b` [재민 확정 2026-09-03 · T62], 그 뒤 무변. 빨강이 하네스 변경에서 온 것은 아니다. 제품 코드 쪽 귀속은 이 러너가 재지 않는다 — **세션이 아침에 잰다** |
| `test-site-memo` | 연속 **2밤** (상시 밖) | RC=1 · 결과줄 18개 · ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  19회 / 9일 | 9분 | 여전히 빨강 (RC=1 · 결과줄 18개 · ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  19회 / 9일) | **귀속 후보 없음(못 쟀다)** — 하네스 `scripts/test-site-memo.js` 최종 손댐 `02590b4` [재민 확정 2026-09-12 · T214], 그 뒤 무변. 2밤 연속이며 하네스는 두 밤 사이 손대지 않았다 — 원인은 **못 쟀다** |
| `test-terraincache` | 연속 **1밤** | RC=1 · 결과줄 22개 · 기본 꺼짐 — env TERRAIN_TILE_CACHE='1' 일 때만 켜진다 | 0분 | 여전히 빨강 (RC=1 · 결과줄 22개 · 기본 꺼짐 — env TERRAIN_TILE_CACHE='1' 일 때만 켜진다) | **귀속 후보(약함)** — 하네스 최종 손댐 `9e79d60` [재민 확정 2026-09-01] · 그 전 `42764e2`·`69ed84b`(타일 지형 메모 · 기본 꺼짐). 첫 판 문구가 `TERRAIN_TILE_CACHE` 전제이므로 env 전제 차이로 보이나 **못 쟀다**. 기준 커밋 앞 `9b61d49`[T324]가 "있던 메모를 켰다"고 적은 것은 **인접 사실일 뿐 귀속 아님** |
| `e2e-onboarding` | 연속 **1밤** | RC=1 · 결과줄 77개 · [첫 사람 · 추천 원클릭] 누적 기여 1/3 — 하나의 카운터로 쌓인다(T11 재사용 축) | 6분 | 여전히 빨강 (RC=1 · 결과줄 74개 · [첫 사람 · 추천 원클릭] 누적 기여 0/3 — 하나의 카운터로 쌓인다(T11 재사용 축)) | **귀속 후보** — 하네스 `scripts/e2e-onboarding.js` 최종 손댐 `d88acbe` [승인 대기 · T319] · 그 전 `6a3905c`[T238] · `8aa16a0`[T242]. 단독 재실행에서 누적 기여가 1/3→0/3 으로 더 내려갔다(러너 관측). **원인은 못 쟀다** — T319 는 후보이지 판정이 아니다 |

⚠**연속 2밤 이상인데 상시 목록에 없는 것 1건** — `test-site-memo`(2밤). T304 규약대로 **PM 이 `nightly-known.txt` 에 올릴지** 정한다(이 기계는 세기만 한다).

## 3. 미측정 0종

없다 — 오늘 목록을 다 돌았다.

## 4. 아침에 세션으로 넘길 빨강

아래는 **상시 목록에 없는** 빨강이다 — 귀속 카드를 찾아 세션에 넘긴다(이 칸만 사람 몫이다).

* `test-shelter` — RC=1 · 결과줄 55개
  * ② 기존 집과 안 겹친다  42/50
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 55개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-shelter.js` 와 그 하네스가 무는 제품 파일
* `test-site-memo` — RC=1 · 결과줄 18개
  * ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  19회 / 9일
  * ① [상황] 헛수고(빈손)가 실제로 있다  빈손 0
  * ① [상황] 그 헛수고가 잴 만큼 무겁다(중앙 ≥100ms/일)  53ms
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 18개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-site-memo.js` 와 그 하네스가 무는 제품 파일
* `test-terraincache` — RC=1 · 결과줄 22개
  * 기본 꺼짐 — env TERRAIN_TILE_CACHE='1' 일 때만 켜진다
  * 해양 존은 캐시를 아예 안 만든다(isWaterTileLocal 이 상수 true 라 무의미)
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 22개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-terraincache.js` 와 그 하네스가 무는 제품 파일
* `e2e-onboarding` — RC=1 · 결과줄 77개
  * [첫 사람 · 추천 원클릭] 누적 기여 1/3 — 하나의 카운터로 쌓인다(T11 재사용 축)
  * [첫 사람 · 추천 원클릭] 빈터 권리가 섰다  (52592,12304) r=320
  * [첫 사람 · 추천 원클릭] ★마을 어귀 빈터에 내 땅을 걸었다(목표 ③ 도달)  claims 0→0 · "마을 중심에서 너무 멀다 \| [E2E] 재료 지급 \| 텔레포트 → (53552,12304) \| 개인 사유지는 길드 영토 안에만 설치 가능 (길드 가입 또는 임시 "
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 74개)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-onboarding.js` 와 그 하네스가 무는 제품 파일

## 5. 통과 116종

```
test-alloy  test-armor-cap  test-assay  test-audio  test-bridge-path  test-bridge-sprite  test-building-anchor  test-calendar  test-carrier  test-charsheet  test-chron-icons  test-claim-v2  test-client-globals  test-clothes  test-craft  test-crops-world  test-crops  test-distmatrix  test-ditch  test-doors  test-downed  test-econ-diet  test-econ-fieldyield  test-econ-rawgrain  test-emptystart  test-events  test-farm-life  test-farm-metrics  test-fish-act  test-fishing  test-frontier-iron  test-furnace  test-granary-add  test-granary-haul  test-guest-identity  test-guest-rejoin  test-guild-treasury  test-hall-door-path  test-handoff-body  test-harness-lint  test-headless-parity  test-hist  test-hunt-port  test-hunt-vis  test-icons  test-imsu  test-inv-ground  test-itemlabel  test-kcal  test-lab-alloc  test-lab-happywork  test-lab-hunt  test-lab-stone  test-lab-trees  test-lab-wood  test-ledger  test-membership  test-mining  test-move  test-msy  test-newcomers  test-nightly  test-notice  test-npc-clothes  test-onboarding  test-oremix  test-path-core  test-picker-tables  test-preserve  test-props  test-psite-server  test-reanchor  test-redeploy  test-regrow  test-relic  test-render-common  test-resource-index  test-rock-los  test-rooms  test-route-persist  test-salt  test-save-periodic  test-segindex  test-spoil2  test-tame  test-terr-backfill  test-terr-persist  test-terrain-memo  test-tick-slicer  test-tidal  test-trade  test-trees  test-valuechain  test-village-found  test-villager-stuck  test-war-world  test-weather  test-weight  test-winter  test-wood-act  e2e-charsprite  e2e-conn  e2e-cutaway  e2e-fogray  e2e-guest-reconnect  e2e-guild  e2e-metallurgy  e2e-mtcorridor  e2e-mtfuzz  e2e-rumor  e2e-spoil2  e2e-tidal  e2e-trade  e2e-ui  e2e-weight  e2e-zoom
```

## 아침에 세션으로 넘길 빨강 (상시 밖 · 카드별 한 줄)

- `test-shelter` — ② 기존 집과 안 겹친다 42/50 (단독도 빨강 · 1밤) → 하네스 무변, 제품 쪽에서 재야 한다.
- `test-site-memo` — ① 종전 판이 하루 5회 이상 집터를 훑는다 19회/9일 (단독도 빨강 · **2밤 연속**) → 상시 목록 등재 여부는 PM 몫(T304 규약).
- `test-terraincache` — 기본 꺼짐 · `TERRAIN_TILE_CACHE='1'` 전제 (단독도 빨강 · 1밤) → env 전제/기본값 쪽을 먼저 본다.
- `e2e-onboarding` — [첫 사람 · 추천 원클릭] 누적 기여 1/3, 단독 재실행 0/3 (단독도 빨강 · 1밤) → T319 접점 확인.

※ 위 네 줄은 **세기와 귀속 후보까지**다. 합격·불합격 판정과 지시서는 아침에 재민과 PM 이 한다.
