# T228 — "기준선이 얼어 있었다"를 반증한다 (세션5 · 2026-09-13 · **가지만**)

> 카드 `~/Mini/지시_T228.md` · 가지 `batch/hunt-wood-server-0912` · **측정 베이스 `c5b6d9d0`**
> 만진 것: `scripts/t222-life800.js`(계측 항만) · `server/villages.js` **읽기 전용 한 줄**(사건 장부).
> **답 한 줄 — 끔 45/51 은 ⓑ(자의 인공물)다. `land.game` 고갈이 아니다.**
> **T222 §0ⓑ 의 기전 설명은 틀렸다 — 이 카드가 그것을 반증한다.**

---

## §0ⓐ `land.game` 회복 감사 — **오르내리는 자리는 하나뿐이다**

| | 답 |
|---|---|
| `land.game` 을 **쓰는** 자리 | **한 곳**(`villages.js:5337`) — `_lifeGameDay` ③, **14일마다** |
| 식 | `land.game = _baseGame × clamp(gsum / gameTot0, 0.05, 1)` — 오르는 것도 내리는 것도 **개체 합 비**뿐 |
| 회복 항 | **있다** — ② 주 단위 로지스틱(`day % 7`) + ③ 꼬리의 이웃 재정착(`g < 2` 셀에 +2) |
| 무엇에 걸려 있나 | `_t146()` 손잡이 · `_huntBandBuild(vil)` 가 밴드를 돌려줄 것 — **그 둘뿐** |
| 관측자·청크·`qtResources`·계절·인구에 걸리나 | **아니다.** `_lifeGameDay` 는 `anyViewerNear` 게이트보다 **앞**이다(`:5370` < `:5387`) |
| `FLOOR.game = 0.45` 의 자리 | `livelihood.landOf` 의 **창설식**(`FLOOR + GAIN × huntShare`)이다 — `land.game` 의 하한이 **아니다**. 실제 하한은 `_baseGame × 0.05` |

★**그래서 T222 의 "20/51 이 바닥 0.45 에 닿았다"는 읽기가 틀렸다.** 0.45 는 **창설값**이다.

| 끔 팔(시드 1020) | 값 |
|---|---|
| 800일째 `land.game` 이 **한 번도 안 바뀐** 마을 | **30 / 51** — 그리고 그 30곳이 **전부 죽었다** |
| 그 30곳의 창설 `land.game` 중앙 | **0.45**(= 초지 바닥 — 사냥터가 애초에 없는 땅) |
| 바뀐 21곳의 `800일 / 창설` 비 | 중앙 **0.973** · 최소 **0.905** |
| 800일째 0.45 인 마을 20곳 중 **창설도 0.45** 였던 곳 | **20 / 20** |

⇒ **어느 마을도 사냥으로 고갈되지 않았다.** 가장 많이 깎인 곳도 −9.5% 다(켠 팔에서도 최소 0.743).
죽은 30곳은 **장부가 한 번도 안 돈** 곳이다(밴드가 안 서서 ③ 재쓰기에 못 들어간다) — 고갈의 반대다.

### deps 21개 — 800일 동안 누가 몇 번 불렀나 (인공물 후보)

| deps | 호출 수 | 자가 돌려준 것 | 제품이라면 |
|---|---|---|---|
| `isWaterTileLocal` | 9,716,827 | `false` | 실제 물 판정 |
| `isTerrainBlockedLocal` | 5,592,627 | `false` | 실제 지형 |
| `isPositionActive` | 2,905,350 | `false` | 활성 청크 |
| `ioBusy` | 48,800 | `false` | I/O 부하 |
| `anyViewerNear` | 40,800 | `false` | **의도한 것**(헤드리스 갈래) |
| ★**`oreProbAt`** | **29,125** | **`0`** | **그 칸의 광맥 확률** |
| `broadcast` | 9,351 | — | 클라 방송(무해) |
| `liveBuildRow` | 5,375 | `null` | 실체 건물 행 |
| `spawnNpc` | 1,428 | pid 객체(**의도한 것**) | 〃 |
| `perfMark` | 800 | — | 무해 |
| `clearTreesInCells` | 51 | `0` | 벤 나무 수 |
| `worldPhase` | 42 | `0.5` | 하루 위상 |
| `qtResources`·`dayPhaseRatio`·`isBlockedByWall`·`onVillageAdded`·`ioQuietMs` | **0** | — | (관측 갈래 전용 — 헤드리스에선 안 불린다) |

★**범인 후보 하나가 이름을 갖는다: `oreProbAt → 0`.** `_oreMineDaily` 는 **헤드리스에서도 매일** 돌고
(`add('ore', …)` 슬라이스) 그 칸의 광맥 확률을 이 문으로 묻는다. 자는 늘 **0** 을 줬다 ⇒ **NPC 채굴 산출 0**.
끔 팔의 무기Q **5.9**(econ 자 1,012) · 도구Q **171.7**(11,892)이 그 자리를 그대로 가리킨다 —
금속이 안 들어오니 도구가 안 서고, 도구가 없으니 모든 노동의 `toolBoost` 가 주저앉는다.
`qtResources → 0회`는 벌목·채광·채집 **현장 채집이 헤드리스에선 아예 안 돈다**는 뜻이다(관측 갈래 전용).

## §0ⓑ 실서버 대조 — **못 쟀다(실기 1줄)**

이 세션은 배포에 손을 못 대고(규약 §132 "SSH 시도 금지"), 실서버는 재민의 VPS 안
(`durango-zone-hanbando` 컨테이너 · `WORKDIR /app` · `DB_PATH=/data/world.db` ← 볼륨 `/srv/durango/hanbando`)이다.
컨테이너 밖에서 읽을 **있는 문**은 `/lifedbg`(GET · CORS 개방 · `lifeDebug()` 그대로)뿐인데 거기엔
`land.game` 도 econ 인구도 없다(마을별 `pop`=시각 NPC · `econCounts` · `farm`/`pot`).

⇒ **새 문을 만들지 않았다.** 재민이 VPS 에서 **한 줄**만 돌리면 표가 선다(읽기만 · 쓰기 0):

```sh
sudo docker exec durango-zone-hanbando node --experimental-sqlite -e '
const db=require("./server/zone-local-db");const r=db.getVillagesByZone("hanbando");
let pop=0,dead=0,ever=0,h=0;const g=[];
for(const v of r){if(!v.econ_state)continue;const e=JSON.parse(v.econ_state);const n=(e.npcs||[]).length;
 pop+=n;if(e._everPop){ever++;if(n<=0)dead++;}g.push((e.land||{}).game||0);h+=(e.counts||{}).hunter||0;}
g.sort((a,b)=>a-b);
console.log(JSON.stringify({day:r[0]&&r[0].day,villages:r.length,pop,dead,ever,hunter:h,
 gameMed:g[g.length>>1],gameMin:g[0],game045:g.filter(x=>Math.abs(x-0.45)<0.005).length}));'
```

같은 날(`day`)의 자(끔 팔 · 같은 `villageSeed`)와 나란히 놓으면 ⓐ/ⓑ 가 갈린다.
**자 쪽 예측**(위 §0ⓐ 로부터): 실서버는 `oreProbAt` 가 진짜 값을 주므로 **훨씬 덜 죽어야 한다**.
`gameMed` 가 창설값 근처(−10% 이내)라면 "고갈은 없다"가 실서버에서도 확인된다.

## §0ⓒ 예산 판 — `T146_BUILD_MS` 는 범인이 아니다

`T146_BUILD_MS` **제품 값(20ms) 그대로**, 끔 팔 · 시드 1020 · 두 판:

| 판 | 인구 | 소멸 | 사냥꾼 | 장부 마을·일 | `land.game` 무변 |
|---|---|---|---|---|---|
| 예산 판 A | 70 | **47/51** | 63 | 7,958 | 29/51 |
| 예산 판 B | 50 | **46/51** | 41 | 7,821 | 29/51 |
| (T222 자 · 예산 크게) | 82 | **45/51** | 72 | 8,355 | 30/51 |

⇒ 예산을 제품 값으로 되돌려도 **소멸은 46~47 로 오히려 한둘 더 많다**. 밴드를 첫날에 만재로 세운 것이
세계를 죽인 게 **아니다**. 두 판이 갈리는 폭(인구 70 vs 50 · 사냥꾼 63 vs 41)이 곧 T222 가 잡은
**벽시계 예산의 비결정성**이고, 그래서 자는 예산을 키워 쓴다.

## §0ⓓ 사건 장부 열 — 여덟 수 **8/8**

`__labProbe._ledgerStats` **읽기 전용 한 줄**(제품 동작 0)로 자가 읽는다:

| 인구 | 소멸 | 무기Q | 확장 | 게시 | 도구Q | 보존식 | 생곡 |
|---|---|---|---|---|---|---|---|
| 82 | 45/51 | 5.9 | 0 | **784** | 171.7 | 0 | 0 |

(`emitted` 33,620 도 함께 선다 — ㉮㉯ 는 이 수와 마을 수로 자가 나눠 쓴다.)

## §1 답 — ⓐ/ⓑ/ⓒ

**ⓑ 다(자의 인공물).** 근거 셋:
1. **고갈이 없다** — `land.game` 은 어디서도 −9.5% 를 넘지 않았고, 죽은 30곳은 장부가 **한 번도 안 돈** 곳이다.
   0.45 는 바닥이 아니라 **창설값**이었다(20/20 일치).
2. **예산이 아니다** — 제품 값으로 되돌려도 소멸 46~47.
3. **빈 deps 하나가 이름을 갖는다** — `oreProbAt → 0` 이 헤드리스에서 매일 29,125번 불렸고,
   무기Q 5.9 · 도구Q 171.7 이 그 결과다. 금속·도구가 없는 세계는 econ 자와 같은 세계가 아니다.

⇒ **T222 §0ⓑ 의 수(82/85/52 · 45/45/48)는 맞고, 거기 붙인 기전 설명(`land.game` 고갈)은 틀렸다.**
이 카드가 그것을 되돌린다. `FLOOR.game`·회복·추상식 처방은 **이 표로는 못 연다**(회부 1).
T222 §0ⓒ 의 사냥 켠 팔 Δ(인구 ×12~21 · 사냥꾼 ×7~13)는 **두 팔이 같은 인공물을 안고** 잰 차이라
방향은 남지만 **크기는 이 자가 고쳐진 뒤 다시 재야 한다**(회부 2).

## §2 검증

* **T222 §0ⓑ 표 비트 동일 재현** — 끔 82 / 85 / 52 · 소멸 45 / 45 / 48 · 켬 972 / 1,062 / 1,107 · 소멸 31 / 27 / 28.
* 같은 씨 두 판 diff 0 유지(③ 예산 판은 예외 — 갈림을 위 표에 적었다).
* `server/villages.js` diff **3줄**(주석 2 + 읽기 전용 1) · 제품 갈래 무변 · 하네스 신설 0.
* 접점: `scripts/t222-life800.js` · `server/villages.js`(읽기 한 줄) · `scripts/t176-ab.js`(견줌).

## §3 회부(구현 0)

1. ★★★**[자 · 선행] `oreProbAt`·`qtResources` 를 실제로 주는 자** — 지형 어댑터(`state.ta`)가 이미 `isOre` 를 안다.
   자가 `deps.oreProbAt` 를 그 정본에 이어 주면 금속·도구가 서고, **그때 비로소 econ 자와 나란히 놓을 수 있다**.
   그 전까지 이 자의 끔 팔은 **기준선이 아니다**(T222 회부 2 를 이 문장으로 정정한다).
2. ★★**[재민 #6] 사냥 켠 팔 Δ 재측정** — 방향은 랩(T172)과 같지만 크기는 ①이 고쳐진 뒤다.
3. ★**[실기 1줄 · 재민] 실서버 한 줄**(위 §0ⓑ) — 돌려서 붙이면 ⓐ/ⓑ 가 최종적으로 갈린다.
4. **[보류] `FLOOR.game` 0.45 · 회복 속도 · 추상식 폐기** — **이 표로는 못 연다**(고갈이 관측되지 않았다).
   T222 회부 3 은 **철회**한다.
5. **[그대로] 벌목 장부 · `_fieldCells` 읽는 쪽 · `_clearedFrac`(#24) · 기준선 넷째 판(PM).**
