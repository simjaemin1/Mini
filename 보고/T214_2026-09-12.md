# T214 — 부하 흔들림 다섯 · `test-events ⑲c` 고침 · `CENTRAL_URL` 한쪽 (2026-09-12)

베이스 `4fa35af2` · 가지 `batch/harness-stable2-0912` ·
① 게임 코드 0(하네스만) · ② `sim/economy-sim-v2.js` **한 줄** + 번들·랩 재생성 · ③ env 배관 한 자리 ·
새 손잡이 0 · 자산 무변 · force 0.

---

## §0-ⓐ 다섯은 **한 병이 아니었다** — 자리가 다섯 군데다

판정 자리엔 벽시계가 없다(`test-harness-lint ⑧a` 146개 훑음 · 0건). 그래서 다섯은 **픽스처·전제**
자리에서 밀린다. 기전은 각각 다르다 — 한 이야기로 안 덮는다.

| 하네스 | 러너 빨강(실측 인용) | 어느 줄이 시간을 가정하나 | 기전 | 새 증인 |
|---|---|---|---|---|
| `test-site-memo ②` | `선택된 집터 로그가 두 판에서 같다  1건 vs 2건` (단독 `1건 vs 1건`) | `arm()` 의 `for(i<300){sleep(2000); if(days>=DAYS) break}` | 하루가 3.5초인데 폴링이 2초다. 부하에서 한 폴링이 여러 날을 건너뛰면 **팔마다 끝난 게임일이 다르다**. 로그는 누적이라 더 오래 산 팔이 한 줄 더 갖는다 — *다른 답*이 아니라 *다른 길이* | 줄마다 **날이 박혀 있다**(`day:vil:x,y:s\|l`) ⇒ **공통 날 창**(`min(A.days,B.days)`)까지만 견준다. 자르는 수는 세계가 말한 것 — 새 수 0 |
| `e2e-downed [A] 존 입장` | `✗ [A] 존 입장` → 43건 중 12/31 (단독 35/0) | `for (i<60) sleep(500)` = **30초** | T140 이 `e2e-cold`·`e2e-thirst` 에서 걷어낸 그 꼴의 **세 번째 사본**. T140 주석 그대로: *"늦는 것과 안 되는 것은 다른 일이다"* | **정본** `fixture-clock.waitInWorld` — `__inWorld()` 가 참이 될 때까지(상한 180초 · 안전망) |
| `e2e-membership ④-0` | `게임일 정지(상호작용 구간) …  ""` (단독 25/0) | `sendPrimary(__e2e_day_freeze)` → `sleep(900)` → **한 번만** 본다 | 서버 회신 알림이 900ms 안에 안 온다 | 알림 링에 **그 문장이 뜰 때까지**(0.1초마다 · 0.9초마다 다시 보낸다) |
| `e2e-verbs ⑧·⑬` | `★⑧ 도는 중엔 메뉴가 **멈추기**라고 말한다  null` · `[상황] … 메뉴가 열렸다  아무도 안 눌렸다` (단독 77/0) | `for(i<3\|5\|20\|40)` 시도 횟수 + `menuWithin(2000)` | 2코어·실클라 셋에서 CDP `mousedown→auxclick` 이 **제품의 400ms 탭/홀드 문턱**을 넘겨 옳은 탭이 홀드로 읽힌다. DOM 을 더 오래 봐도 안 고쳐진다 — **다시 누르는 수밖에 없다** | `tapUntilMenu` — **클라가 메뉴를 DOM 에 올릴 때까지** 누른다. 자리도 함수로 받아 사람이 걸어 나가면 다시 고른다 |
| `e2e-zoom ⑤` | `1→1.5→2→1 … 평균 화소 차 5.765` (바 1.0 · 단독 28/0) | `setZ(1); sleep(900); shot()` | 900ms 안에 다시 그리기가 안 끝나 **그리는 중간 프레임**이 찍힌다 | **화면 자신** — 연속 두 장이 서로 멎을 때까지 찍는다(`shotSettled`). 문턱도 안 지어냈다: **판정이 쓰는 그 바(1.0)** 를 그대로 쓴다 |

### 같이 걸린 세 자리 (다섯 밖 · 이 카드가 만지며 드러났다)

| 자리 | 무엇 | 어떻게 |
|---|---|---|
| `e2e-hp` 입장 ×2 | ⑧c 가 잡은 **같은 30초 사본** — 아직 안 걸렸을 뿐 | 정본 `waitInWorld` |
| `e2e-verbs ②` 전제 | 재접속으로 pid 가 갈리면 **기다림으로 안 풀린다** | 기다리는 동안 **멱등 픽스처 재전송**(①의 규약) |
| `e2e-verbs ③` 크래시 | `labels` 가 null 인 판에서 `labels.findIndex` 가 **하네스를 통째로 끊었다**(결과줄 15개) | 방어 한 줄 — T90 이 클릭에 이미 건 규약을 여기도 |

### `test-harness-lint` — 픽스처 자리도 보게 했다(⑧c)

카드가 준 모양(`sleep(상수)` 뒤 한 번만 보고 판정)을 **먼저 쟀다**: 146개 중 **171건**이다.
⇒ 0 게이트로 못 세운다(대부분 정당하다 — 입력을 쏘고 한 틱을 기다리는 건 검사가 아니다).
**세울 수 있는 자리**는 따로 있었다: 이번에 실제로 문 병은 *"기다리는 방식의 사본"* 이다.

| ⑧c | |
|---|---|
| 못 박은 것 | **제 손으로 `__inWorld` 폴링 루프를 안 세운다** — 정본은 `fixture-clock.waitInWorld` 하나 |
| 고치기 전 사본 | `e2e-downed`×2(30초 · 러너 빨강) · `e2e-hp`×2(같은 30초 · **아직 안 걸렸을 뿐**) · `e2e-verbs`×2(제 손으로 180초) |
| 지금 | 146개 훑음 · **사본 0** · 미끼 한 루프를 넣으면 잡는다 |
| 참을성 | `e2e-verbs` 는 한 글자도 안 줄었다 — 정본 기본 상한이 마침 **180초**다 |

`test-harness-lint` **36/0**(T185 33/0 → +3).

---

## §0-ⓑ `⑲c` — 어긋난 것은 캐러밴 하나, 움직인 것은 **한 줄**

### 어느 캐러밴인가 (3시드 240일 전수)

| | |
|---|---|
| 표본 | 시드 1020·7·42 · 240일 · 캐러밴 **16건** · 어긋남 **1건** |
| 그 한 건 | 시드 42 · `#461` · `농촌…` wood 100 → `_abandoned=true` · `state=inbound` |
| 수 | `departDay 240 · arriveDay 240 · travelDays 1` ⇒ `arr−dep = 0 ≠ 1` (**−1일**) |
| `returnArriveDay` | 241 ⇒ `returnArriveDay − arriveDay = 1 = travelDays` ✓ (귀환 구간은 멀쩡하다) |

### 셋만 그런가 — **아니다. 갈래가 하나다**

`arriveDay−departDay ≠ travelDays` 는 **빈손 귀환(`_abandoned`)** 갈래에서만 난다.
버림 전환을 틱 사이에서 13건 잡아 전후를 찍었다:

```
  #166 전: departDay 90 · arriveDay 92 · travelDays 2 · distance 925.09 · arr−dep 2   ✓
       후: departDay 92 · arriveDay 92 · travelDays 2 · distance 925.09 · arr−dep 0   ✗
       거리 대칭: villageDist(to,from) = villageDist(from,to) = 925.0909  ⇒ travelDays 는 **안 바뀐다**
```
13건 전부 같은 모양이다. ⇒ **실제로 움직인 것은 `c.departDay = day;` 한 줄뿐**이다
(`c.distance`·`c.travelDays` 재계산은 거리가 대칭이라 값이 그대로다).

### `travelDays` 를 정하는 자리 전수 · `departDay` 를 읽는 자리 전수

| 자리 | 구간 | `departDay` 를 옮기나 | 옳은가 |
|---|---|---|---|
| 출발 `economy-sim-v2.js:820` | outbound 시작 | 세운다 | ✓ |
| 재routing `:961-979` | **여전히 outbound** | 옮긴다(T17 ④) | ✓ 셋이 같이 움직인다 |
| 빈손 귀환 `:993-1002` | **inbound 로 바뀐다** | 옮겼다 | ✗ **여기가 결함** |
| 존 `villages.js:2045-2056` | inbound(고립 화물보존) | 옮긴다 | △ 회부 ② — `arriveDay` 가 **미래**라 물음이 다르다 |
| 존 `_clockPush` `villages.js:6217` | phase 로 가른다 | inbound 면 **안 건드린다** | ✓ T60 ③ 이 적은 그 규약 |

**읽는 자리 전수**(`grep departDay`): `central.js:669·670·690` · `villages.js:1984·2017` · `zone.js:12493`.
셋 다 **outbound 일 때만** 읽는다 — inbound 구간은 전부 `arriveDay → returnArriveDay` 다.
⇒ **inbound 캐러밴의 `departDay` 를 읽는 자리는 한 곳도 없다.** 죽은 대입이 규약만 깼다.

### 고친 자리 — 한 줄

```js
  // sim/economy-sim-v2.js  빈손 귀환
- c.departDay = day;
  c.travelDays = travelDaysForDistance(c.distance);
```
T60 ③(재민 확정)이 적은 규약 그대로다: *"돌아오는 구간의 지연은 `arriveDay − departDay`(가는 구간)를
안 건드린다."*

### T185 의 귀속을 한 줄 고친다

`363c38cb`(T135 3판)은 **표본이 이 갈래를 처음 밟게** 한 커밋이지 결함을 넣은 커밋이 아니다.
실측: 버림 전환은 **켠 판 13건 · `T135_TREES=0` 9건** — 둘 다 일어난다. 다만 끈 판에서는
240일 스냅샷 안에 **비행 중인 버림 캐러밴이 없어** 표본이 그 갈래를 안 밟는다
(끈 판 19건 중 어긋남 0 · 켠 판 16건 중 1). 결함은 빈손 귀환 갈래가 생긴 이래 있었다.

### `⑲d` 도 같이 고쳤다(계약을 더 세게)

옛 `⑲d` 는 두 줄의 **근접 정규식**(600자 창)이라 주석 한 줄이 늘면 코드가 그대로여도 깨진다
(실측: 위 주석이 그렇게 ⑲d 를 빨갛게 했다). ⇒ **구간 잘라내기**(양끝이 코드 문장)로 바꾸고,
새 규약을 계약으로 박았다: **빈손 귀환 구간에 `departDay` 대입 0** · 자명 통과 금지 미끼 포함.

`test-events` **275/0**(272/1 → 판정 셋 늘었다).

### 3시드 800일 — 여덟 수는 **한 바이트도 안 움직였다**

A = `4fa35af2`(깨끗한 worktree) · B = 같은 판 + 이 카드. `t17-metrics 800 <시드>` JSON `cmp`:

| 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | ㉮ 일/건 | A vs B |
|---|---|---|---|---|---|---|---|---|---|
| 1020 | 6,889 | 0/51 | 1,012 | 5,509 | 2,539 | 11,892.1 | 803.6 | 1.93 | **한 바이트 동일** |
| 7 | 7,148 | 0/51 | 1,030 | 7,830 | 2,714 | 12,039.3 | 551.1 | 1.88 | **한 바이트 동일** |
| 42 | 6,259 | 0/51 | 978 | 5,946 | 2,648 | 11,013.2 | 663.3 | 1.95 | **한 바이트 동일** |

⇒ **게이트 아님 · main 까지 간다.**
덤으로: 이 수들은 `인계/공통.md` 의 **T163 셋째 판 기준선과 한 자도 안 다르다** —
그 표가 `4fa35af2` 에서도 유효함을 이 카드가 첫 줄에서 확인한 셈이다(족보 (142)).

---

## §0-ⓒ `CENTRAL_URL` — **읽는 쪽에 이었다**

| 세어 본 것 | 수 |
|---|---|
| `CENTRAL_URL` 을 존에 넘기는 스크립트 | **90** |
| `server/` 에서 읽는 자리 | **0** (존은 `CENTRAL_HOST`/`CENTRAL_PORT`, 기본 3010) |
| 그중 `CENTRAL_PORT` 를 같이 안 넘기는 것 | **88** — 그리고 **88개 전부 `CPORT = 3010`** ⇒ 기본값과 **우연히 같아서** 돌고 있었다 |
| 먼저 적은 사람 | T10(2026-09-01) · 다시 회부 T208 §3 |

**왜 잇는 쪽인가** — 지우는 쪽은 88자리를 건드려야 하고 그중 여럿이 **진행 중 카드의 파일**이다.
잇는 쪽은 **한 자리**다. 그리고 이미 그 뜻으로 쓰는 자리가 있었다: `e2e-guild ⑤`(T139)가
자식 프로세스에 `CENTRAL_URL='http://127.0.0.1:1'` 을 넣어 *죽은 포트*를 겨누는데, 아무도 안 읽어서
**그 의도가 한 번도 선 적이 없다**. 이으면 그제야 선다.

```
  server/zone-config.js — CENTRAL_HOST/CENTRAL_PORT 가 **명시되면 그쪽이 이긴다**.
  CENTRAL_URL 은 둘이 없을 때의 기본값 자리만 채운다(스킴도 함께).  깨진 URL 은 경고 한 줄 + 무시.
```
실측 — 오늘 동작은 한 글자도 안 바뀐다:

| 환경 | `CENTRAL` |
|---|---|
| 아무것도 없음 | `{localhost, 3010, http}` (종전과 같다) |
| `CENTRAL_URL=http://localhost:3999` | `{localhost, **3999**, http}` (이제 먹는다) |
| 위 + `CENTRAL_PORT=3010` | `{localhost, **3010**, http}` (명시가 이긴다) |
| `CENTRAL_URL='not a url'` | `{localhost, 3010, http}` + 경고 한 줄 |

---

## §2 검증 — **러너 안에서** 재는 것이 판정이다

청크 4종씩 · 병렬 0 · 청크 사이 잔류 프로세스 정리. A팔은 **같은 청크를 `origin/main`(`4fa35af2`)
워크트리에서** 돌린 것이다(T135 §3-b · HEAD 대조).

| 하네스 | 야간/전수 때 | **A팔 = `origin/main` 같은 청크** | **B팔 = 이 판(러너 안)** | 단독 |
|---|---|---|---|---|
| `test-events` | 271/1 (⑲c) | — | ★**275/0** | 275/0 |
| `test-harness-lint` | 33/0 | — | ★**36/0** | 36/0 |
| `test-site-memo` | 13/1 | — | ★**15/0** (공통 9일 · 종전 9일 · 채택 9일) | 14/0 |
| `e2e-downed` | 12/31 | — | ★**35/0** | 35/0 |
| `e2e-membership` | 24/1 | **25/0** | ★**25/0** | 25/0 |
| `e2e-verbs` | 66/10 | **69/8** (⑬ 통째로 무너짐) | ★**77/0** | 77/0 |
| `e2e-zoom` | 27/1 | **28/0** | ★**31/0** (`dBack` **0.056** · 바 1.0) | 31/0 |
| `e2e-hp`(⑧c 때문에 같이 만졌다) | 26/0 | **26/0** | ★**26/0** | 26/0 |

⚠**정직하게 — 이 4종 청크는 `e2e-membership`·`e2e-zoom` 의 옛 빨강을 A팔에서 재현하지 못했다.**
그 둘의 원래 빨강은 **143종 전수**의 무게에서 났다. 그러니 그 둘에 대해 "러너 초록"은 필요조건이지
증명은 아니다 — 근거는 §0-ⓐ 의 기전 표와 아래 두 실측이 진다.
재현된 것은 **`e2e-verbs`** 다: 같은 청크에서 A팔 **69/8** → B팔 **77/0**.
그리고 `e2e-zoom` 은 기전을 **화소로 직접 쟀다**(아래).

### 이 카드가 **재서** 알아낸 것 둘 (지어낸 이야기 0)

1. **`e2e-zoom ⑤` 의 진짜 주인은 배율이 아니라 빛이었다.** 빨간 두 장(`01-z1` vs `03-back-to-1`)을
   화소로 뜯었다: 평균 RGB 가 통째로 **(−4.67, −4.65, −1.75)** 움직였고 20 넘게 다른 화소는
   **0.12%** 뿐 · 차 분포는 0~15 에 99.9% 가 몰렸다. **움직인 게 아니라 밝아진 것**이다.
   이 존은 하루가 0.5초라 ①~⑤ 를 도는 사이 낮밤이 수십 번 돈다 ⇒ `__e2e_day_freeze`(있는 손잡이)로
   날을 세우고, 얼었다는 말을 **세계에서 듣고** 다음으로 간다. 결과: `dBack` **4.391 → 0.056**.
2. **`e2e-verbs ②` 는 기다림으로 안 풀리는 자리였다.** 하네스 주석이 이미 적어 둔 기전
   (*재접속 → 새 pid → R 의 `downStates` 는 옛 pid 로만 차 있다*)은 **시간이 지난다고 안 풀린다**.
   실측: 이 `[상황]` 이 청크에서 세 판 연속 빨갰고 단독은 초록이었다. ⇒ 기다리는 동안
   ①이 쓰는 **멱등 픽스처를 다시 보낸다**(①의 `i % 20` 규약 그대로). 그러면 지금 붙어 있는 pid 가 눕는다.

### ② 되돌림 · 배선

| | |
|---|---|
| `T135_TREES=0` · 시드 42 · 800일 | A/B **한 바이트 동일** |
| `lab-wiring-check` | 실패 0 · 경고 0 (번들·랩 인라인 재생성 포함) |
| `test-lab-trees` | **52/0** |
| `sim/build-econ-bundle.js` · `sim/inline-engine.js` | 재생성 — 손으로 기운 자리 0 |

---

## §3 회부

1. **존 쪽 같은 자리** — `server/villages.js:2055` 가 고립 화물보존 귀환에서 `c.departDay = world.day`
   를 똑같이 쓴다. **다만 물음이 다르다**: 거기선 캐러밴이 `to` 에 닿기 **전에** 돌아서므로
   `arriveDay` 가 **미래**고, 그러면 `central.js` 의 inbound 보간(`num = day − arriveDay`)이 음수다.
   `test-events` 표본 밖(존 상태)이라 이 카드는 **안 쟀다** — 재는 카드 몫이다.
2. **`sleep(상수) → 한 번 읽기 → 판정` 171건**(§0-ⓐ) — 0 게이트로는 못 세운다. 라쳇(늘지 않게)이든
   자리별 카드든 PM 판정거리. 이 카드는 **세어서 적는 데까지** 했다.
3. 상시 목록 · 야간 창(#25 · PM) — T185 회부 ②③ 그대로.
4. 손잡이 기본값 `ARC_*`(러너 무게) — 이 카드가 안 쟀다.
