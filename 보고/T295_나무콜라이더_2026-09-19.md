# 보고 — T295 후속(세션10/WAR): 나무는 이제 존의 것이다 — 전쟁 콜라이더·시야·엄폐에 나무를 붙였다 (2026-09-19)

한 줄: **커밋 `0bdc4fb` [승인 대기 · T295 후속] — T284 가 "관측자에 따라 답이 갈린다"며 미뤘던 나무를 T301 의 관측자 무관 색인 위에 붙였다(T309 §0ⓑ 표 그대로) —
장애물 한 곳(`_warBlockedCell`)에 나무를 넣고 시야·엄폐도 나무를 보며, 술어는 **존이 주입**(`deps.treeCellBlocked` — 청크가 켜질 때 쓰는 그 생성 함수·그 장부)하고
생활층은 지형 메모와 **같은 자리**에 태운다. `test-war-world` **38 통과 / 0 실패**(옛 30 + 나무 절 8) · 관측자 켬/끔 3시드 한 글자 동일 · 청크 색인 = 칸마다 묻는 길 **1,000칸 전수 동일**.
⚠**부하는 늘었다**: 존 틱 p50 4.05 → **6.27ms** · p95 8.7 → **48.5ms**(전쟁 6건 · 병사 283 · drop 0). 값 판정은 재민(§0-ⓑ).**

* 실행 세션 = **세션10**(WAR 영역). 카드 번호 없음 — **T309 §0ⓑ**("T295(세션10)가 그대로 붙일 diff")와 **T317 머리**("메모 한 칸은 T295(세션10)가 붙인다")가 이 세션에 넘긴 일이다.
* 베이스 `origin/main 5e67863` · 가지 `batch/war-trees-0919`. **T295 가지(`batch/war-cost-0918`)와 겹치는 줄 0** — 서로 다른 함수다(어느 쪽을 먼저 머지해도 된다).
* 새 수 0 — 넘침 상한은 `chunk.forestSpacing(FOREST_MIN_COV)` 에서 유도(96px) · 메모 키·비우는 시점은 지형 메모 그대로.
* 사본 0 — 나무 판정은 `chunk.generateChunkResources`(청크가 켜질 때 부르는 그 함수)가 낸 생성물의 색인이다. 하네스가 **칸마다 묻는 길(`treeBlockerAt`)과 1,000칸 전수 대조**한다.
* 회부(§4): 행군로 A* 가 숲을 모른다(빽빽한 숲은 판을 길게 만든다) · 부하 p95(값 판정) · 포로 몸값 `storage.food`(T295 회부 그대로).

---

## §0 실측

### ⓐ 무엇을 붙였나 — T309 §0ⓑ 표와 견줘

| 자리 | T309 표(제안) | 실제로 붙인 것 | 왜 다른가 |
|---|---|---|---|
| 메모 비우는 줄 | `_warBuildRectIndex` 에 `state._warTreeMemo = new Map()` 한 줄 | **그대로** | — |
| 술어 | `villages` 가 `require('./chunk').treeBlockerAt(state.zoneId, …)` 직접 | **존이 주입**(`deps.treeCellBlocked`) · 생활층은 메모만 | 벤 나무 장부(`harvestedSeeds`)와 게임일을 쥔 쪽이 **존**이다. 생활층이 청크를 직접 부르면 그 둘을 못 넘겨 **벤 나무가 계속 막는다** |
| 어댑터 `blocked` | `_warBlockedCell(ix,iy) \|\| _warTreeCell(ix,iy)` | **`_warBlockedCell` 안에 넣었다**(한 곳) | 어댑터에만 넣으면 대형이 병사를 숲 안에 세우고 풀어 주는데, battle-core 몸 클램프는 *"이전 자리도 막힘"* 이라 그 병사가 **그 칸에 갇힌다** — 실측 **518/980틱**. 장애물은 한 곳에서 답해야 한다 |
| `losBlocked` | 나무 칸이면 막는다 | **그대로** | 화살이 나무에 막힌다 |
| `coverAt` | 나무 칸이면 엄폐 | **그대로** | 나무 뒤가 엄폐다 |

### ⓑ 부하 — 같은 세계 위 W6(전쟁 6건 · 병사 283 · 창 420초 · 컨테이너 2코어)

| 판 | 존 틱 p50 | p95 | drop | 전쟁 틱 p50 | p95 | 교전 | 대치 | 궤주 |
|---|---|---|---|---|---|---|---|---|
| main `5e67863` | **4.05** | 8.72 | 0 | 1.42 | 2.26 | 56 | 24 | 29 |
| T295 가지(나무 없음) | 4.17 | 8.23 | 0 | 1.39 | 2.35 | 55 | 19 | 30 |
| 나무 ① 칸마다 묻기 | 7.24 | 54.5 | 0 | 2.98 | 4.18 | 66 | 37 | 23 |
| 나무 ② 청크 통째 캐시 | 6.62 | 61.0 | 0 | 2.74 | 3.46 | 64 | 37 | 22 |
| 나무 ③ 캐시를 **날마다만** 비움 | 6.22 | 59.6 | 0 | 2.59 | 3.36 | 62 | 33 | 24 |
| **나무 ④ 이웃 청크 건너뜀(채택)** | **6.27** | 48.5 | 0 | 2.76 | 3.62 | 65 | 41 | 19 |

술어 자체의 값(실측):

| | 값 | 비고 |
|---|---|---|
| 칸마다 묻기(`treeBlockerAt`) | **27.5µs/칸** | 한 칸에 이웃 9청크를 다시 낳는다 |
| 청크 한 판 생성 | 1.68ms / 1,024칸 = **1.6µs/칸** | 채택 — 같은 함수·같은 인자, 결과를 칸 색인으로 |
| 전장 한 판이 묻는 칸 | 1,500~3,600칸 | 하네스 실측 상자 49×31 ~ T309 가정 60×60 |

**읽는 법 — 늘어난 2.2ms 는 술어 값이 아니다.** ①→④ 로 술어를 17배 싸게 만들어도 p50 은 7.24 → 6.27 밖에 안 내려갔다.
남는 것은 **나무가 실제로 세계를 바꾼 값**이다: 판이 길어지고(대치 24 → 41 · 궤주 29 → 19) 병사마다 장애물 판정이
늘었다(battle-core 몸 클램프는 막히면 축별로 두 번 더 묻는다). 즉 "빈 들판에서 싸우던 것"과 "숲에서 싸우는 것"의 차다.
⚠**p95 48.5ms 는 틱 예산(33ms)을 넘는다** — `drop` 은 0(루프가 따라잡는다)이지만 교전 중 끊김이 보일 수 있다. 값 판정은 재민:
줄이려면 ⓐ 줄기만 막기(굵기 문턱) ⓑ 행군로 A* 가 숲을 피하기 ⓒ 교전 시작 때 상자를 미리 채우기(대신 그 틱이 무거워진다) 중 하나다.

### ⓒ 하네스 — 무엇을 물게 했나(`test-war-world` 절 ⓛ · 8칸)

| 판 | 결과 |
|---|---|
| 전제 — 두 팔 다 교전 | 켬 1,317틱 · 끔 972틱 |
| **숲에서도 판이 끝난다** | 궤주/B — 숲은 판을 **늘릴 뿐**(1,317 vs 972틱) |
| 나무 술어 켬: 나무 칸에 선 전투 병사 틱 | **0 / 1,317** |
| ★대조 — 술어 미주입(이 카드 전) | **532 / 972** (숲 띠가 실제로 돌격로 위에 있다) |
| 관측자 켬/끔 3시드 | **3/3 한 글자 동일**(나무를 보면서도) |
| 메모 비우는 줄이 지형 메모 옆 한 군데 | 정적 ✅ |
| 생활층이 청크를 직접 안 부른다 | 정적 ✅ (술어는 존 주입) |
| 존 술어가 청크 생성 함수·벤 나무 장부를 쓴다 | 정적 ✅ |
| ★대조 — 청크 색인 = 칸마다 묻는 길 | **다른 칸 0 / 1,000**(그 중 나무 칸 152) |

**함정(하네스가 잡은 것)**: 숲을 **빽빽한 벽**(모든 칸)으로 깔면 두 군대가 서로 못 닿아 **판이 안 끝난다**(12분 창에 정산 0).
존의 숲은 격자 간격 2~3셀(`forestSpacing` 60~96px)이라 사이에 길이 남는다 — 하네스도 그 꼴(간격 2셀)로 깐다.
⇒ 진짜 벽 같은 숲이 생기면 **행군로가 숲을 피해야** 한다(회부).

### ⓓ 한 줄 답

**나무는 이제 존의 것이다 — 관측자가 없어도 같은 자리에 서 있고, 병사는 그 칸을 밟지 않으며, 화살은 그 뒤를 못 맞히고, 그 뒤는 엄폐다.
값은 술어가 아니라 **숲에서 싸운다는 사실**이 치른다(존 틱 p50 +2.2ms · p95 48.5ms · drop 0).**

---

## §1 무엇을 고쳤나

| 파일 | 줄 | 무엇 |
|---|---|---|
| `server/zone.js` | +30 | `warTreeCellBlocked` — 청크 한 판을 통째로 낳아 나무 칸 색인(같은 함수·같은 인자) · 날마다 비움 · 넘침 상한만큼만 이웃 청크 · `SimVillages.init` 에 `treeCellBlocked` 한 칸 |
| `server/villages.js` | +18 | `_warTreeCell`(메모 · `_warNoCollide` 게이트) · `_warBuildRectIndex` 에 메모 비우는 줄 · `_warBlockedCell` 에 나무 · 어댑터 `losBlocked`·`coverAt` 에 나무 |
| `scripts/test-war-world.js` | +40 | 절 ⓛ(위 표) · 숲 띠 픽스처(격자 2셀) · `treeTicks` 계측 · `WW_SEED/WW_TREES/WW_TICKS` 손잡이 |

## §2 검증

| 판 | 결과 |
|---|---|
| `scripts/test-war-world.js` (서버 절 포함) | **38 통과 / 0 실패** |
| 관측자 켬/끔 3시드(나무 위) | 3/3 한 글자 동일 |
| 청크 색인 = `treeBlockerAt` 전수 | 다른 칸 0 / 1,000 |
| 3시드(1020·7·42) 800일 51마을 `t17-metrics` vs `origin/main` | **cmp 바이트 동일 ✅ 3/3** |
| `scripts/test-resource-index.js` | **11 통과 / 0 실패 ✅** |
| 부하 W6 | 위 표(drop 0) |

## §3 규칙 지킴

* 새 수 0 · 사본 0 · 손잡이 없음(**되돌림 손잡이를 안 만들었다** — T284 캐논 "장애물은 존의 것" 위반을 메우는 일이라 끔 팔이 없다).
* `chunk.js` 무접촉(T301·T317 영역) · `villages.js` 는 **전쟁 절만**(어부·생활층 무접촉) · `zone.js` 는 init 한 칸 + 함수 하나(T305 `sendInventory`/`doEat` 자리 무접촉).
* main 무접촉 · force 0 · 배포 재민만.

## §4 회부

* **행군로 A\* 가 숲을 모른다** — 빽빽한 숲이 길을 막으면 판이 길어지거나(실측 972 → 1,317틱) 극단적으로는 안 끝난다(벽 판). 경로가 숲을 피하거나, 숲을 지나는 대형이 느려지는 규칙이 필요하다.
* **부하 p95 48.5ms**(예산 33ms 초과 · drop 0) — 줄이는 세 길은 §0-ⓑ. 값 판정 재민.
* 그루터기·묘목은 안 막는다(`RESOURCE_HP_TABLE` 그대로) — 벤 자리는 지나간다. **그날 벤 나무는 다음 날 교전부터** 반영된다(건물 발자국·지형 메모와 같은 규약).
* T295 회부 그대로: 무기 재고(⑤) · 선포 무관 동원(⑥) · 포로 몸값 `storage.food` · `_siege*` 는 산 칸.

## §5 푸시

* 가지 `batch/war-trees-0919` · 베이스 `origin/main 5e67863` · 커밋 `0bdc4fb` [승인 대기 · T295 후속].
* ⚠**머지 순서 한 가지** — `인계/X-전쟁.md` 에서 이 가지(X-3)와 T295 가지(X-2)가 **같은 자리**("### 남은 P4 순서" 앞)에 절을 넣는다.
  코드 충돌은 0이고(서로 다른 함수), 이 파일만 한 군데 충돌한다 — **둘 다 살리고 X-2 → X-3 순서로** 두면 된다.
* 컨테이너 푸시는 403(프록시 정책) ⇒ 재민 PC 셸에서 저장된 토큰으로 밀었다(토큰은 그 PC 밖으로 안 나간다 · 출력 마스킹). main 은 재민.
