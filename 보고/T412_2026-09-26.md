# T412 — 도구/작업 소리 57자리 (세션9 · 2026-09-26)

## 결론 (머리 15줄)

1. **한 줄 답** — 도구/작업 57자리 중 **20자리가 운다**(이 카드로 새로 14 · 이미 다른 방송으로 울던 6) · 상태 동기화 30 · 애매 7. 빠진 소리 전수 **있음 40 → 51**.
2. **가지 해시** `____` (가지 `batch/sfx-work-0926`) · **`[승인 대기 · T412]`** · **베이스** `67a86098`.
3. 확보 **9키 배선 · 후보 8 · 없음 1**(`dig` 2번) — 건물 섬 `build_place` · 벽 침/고침 `build_hit` · 허묾 `build_break` · 문 `door_open`/`door_close` · 줍기 `pickup` · 궤 `chest_use` · 가마에서 꺼냄 `craft_done` · 삽질(심기·밭 일굼·씨 넣음) `dig`. 전부 Kenney CC0 · OGA CC0(원천 사슬까지 읽음) · 새 CC-BY 0.
4. 새 파일 0 으로 이은 이웃 셋: 노·숯가마 불소리 = `fire`(**불이 들었을 때만** — `data.job`) · 그루터기 = `axe` · 주민 밭 일굼 = `dig`.
5. ★**수신 머리의 직전 값**으로 사건을 가른다(서버 0 · `30-n-net.js` 0): 지워지는 건물의 **타입**(터가 다음 단계로 바뀌는 지움은 조용) · 깎이기 전 **hp** · 문 `open`·가마 `job`·밭 `crop` 의 **켜짐/꺼짐** · 줍히는 물건의 **자리** · 묘목이 **처음 보는 id** 인가(자람·열매 갱신은 같은 id).
6. ★**두 번 울지 않는다**: 내 줍기는 `inventory where:pickup` 이 아니라 방송(`ground_item_removed`) 하나로 · 사람 수확은 `where:harvest` 하나로(밭 `crop` 꺼짐은 안 잇는다) · 가마 불 넣음은 단발 없이 반복 불소리가 맡는다.
7. ★`chest_state` 는 이름은 상태지만 **넣기·꺼내기 뒤에만** 나간다(서버 두 자리) → 사건. `ground_item_removed` 는 **줍기 함수에서만** 나간다 → '누가 주웠다'로 읽어도 된다(`test-audio ⑰a·⑰b` 가 서버 함수 이름으로 지킨다).
8. ★갈래 표의 "서버 메시지" 여섯은 **메시지가 아니다**(`carrier`·`chest`·`guild_granary`·`preserve`·`sapling`·`wall` — 장비·건물·자원 타입 낱말이나 클라→서버 요청 이름을 긁은 착시) → ⓒ.
9. **제작 완료 소리는 없다** — 제작 줄(`craft_queue`)은 완료와 취소가 같은 꼴(줄에서 빠짐)이고 따로 알리는 메시지가 없다. 보존(`preserve`)도 제작 줄의 한 종류라 같다 → 회부(서버 낱말).
10. 헤드룸: 58키 재측정 — **최악이 바뀌었다**: T292 여섯 + 공사 다섯을 한 표본에 겹치면 없이 **0.7967**(종전 0.7957 보다 0.01 dB 위) → 새 `worstCombo`. 여유 **0.03 dB**. 공사판 한 판 0.6305.
11. 자: `test-audio` 가 표 안 **규칙 목록**(`buildEdge` 의 on/off)까지 한 겹 내려가 키를 센다(안 그러면 다섯 키가 "아무도 안 부르는 키"로 거짓 빨강).
12. 하네스: `test-audio` 160→**170** · `e2e-audio-probe` 61→**71** · `e2e-aim` **15/0** · `e2e-pid-collide` **13/0** · globals 26 · lint 61 · assets-audit PASS · lock 582/0 · 소리판 행 58 = 키 58.
13. 기존 41키 비트 동일(41/41) · 출처 없는 소리 0 · `bgm/`·서버·`30-n-net.js` diff 0.
14. 실기: 재민 배포 뒤 소리판(새 행 17) + 문 여닫기·벽 짓기·줍기·밭 일구기.

---

## ⓐ 자리 표 57

판정: **ⓐ 운다**(이 카드) · **ⓐ′ 이미 운다**(다른 방송이 그 사건을 이미 울린다) · **ⓑ 상태 동기화**(안 붙인다) · **ⓒ 애매/착시**(표만). 서버 자리는 `server/zone.js` 줄 · 함수 이름.

| # | 자리 | 코드가 말하는 것 | 판정 | 키(이웃) |
|---|---|---|---|---|
| 1 | `building_added` | 12자리: `_tryBuildAt` 놓기 · `tryHutStart`/`_siteStart` 터 · `tryHutAdvance`/`_siteAdvance`/`tryKilnAdvance` 단계 오름 · `npcStep` 주민 밭 · `_addBuildingForHouse` 주민 집 · `setupCanadiaVillage` | **ⓐ** | `build_place`(밭 = `dig` · `vtile` 조용) |
| 2 | `building_damaged` | `tryAttack` 벽 치기(10501) · `tryRepairBuilding` 고치기(8637) `{id,hp}` | **ⓐ** | `build_hit`(직전 hp 와 견줌) (`rock_hit`) |
| 3 | `building_removed` | `doDismantleBuilding` 철거(+연쇄) · `*Advance` 셋 = 터를 다음 단계로 **바꿈** · `initCanadiaPrototype` | **ⓐ** | `build_break` · 터 타입은 조용 |
| 4 | `building_updated` | `doDoorToggle`(`open`) · `tryKilnBurn`/`tryFurnaceSmelt`(`job` 생김/없어짐) · `_plantInto`(`crop`) · `tryHarvest`·`npcStep` 수확 · 틱 `farmStage` | **ⓐ**(가장자리 셋만) | `door_open`/`door_close` · `craft_done` · `dig` |
| 5 | `buildings_removed` | 청크 내림 일괄 | ⓑ | |
| 6 | `buildings_spawn` | 청크 올림·주민 일괄 | ⓑ | |
| 7 | `carrier` | **메시지 아님** — 지게 장비 타입(`equip: [{type:'carrier'}]`) | ⓒ 착시 | |
| 8 | `cast_preview` | 주조 미리보기(UI 응답) | ⓑ | |
| 9 | `chest` | **메시지 아님** — 궤 건물 타입. 궤 사건은 #10 | ⓒ 착시 | |
| 10 | `chest_state` | `tryChestPut`·`tryChestTake` 뒤에만 `{buildingId,data}` | **ⓐ** | `chest_use`(그 궤 자리) (`drop`) |
| 11~13 | `claim_added/removed/updated` | 사유지·밭 단계 동기 | ⓑ | |
| 14 | `craft_queue` | 제작 줄 통째(`sendCraftQueue`) | ⓑ — 완료 운반자 없음(회부) | |
| 15 | `dishes` | 요리 목록 | ⓑ | |
| 16 | `equipment` | 장비 목록 | ⓑ | |
| 17 | `facility` | 시설 창 목록 | ⓑ | |
| 18 | `floor_changed` | 내 층(`myFloor`) — 걷기 갈래(T397) | ⓑ | |
| 19 | `ground_item_removed` | `tryPickupItem` 네 자리에서만 `{id}` | **ⓐ** | `pickup`(물건 자리) (`drop`) |
| 20 | `guild_granary` | **메시지 아님** — DB 행 타입 | ⓒ 착시 | |
| 21 | `plant_menu` | 심기 창(UI) | ⓑ | |
| 22 | `preserve` | **클라→서버 요청 이름** · 보존은 제작 줄 한 종류(`kind:'preserve'`) | ⓒ — #14 와 같은 회부 | |
| 23 | `preserve_menu` | 보존 창(UI) | ⓑ | |
| 24 | `resource_spawn` | 묘목 심기(7874 · 새 id) · 자람(1927 · 같은 id 타입 바뀜) · 열매(7939 · 같은 id) | **ⓐ**(새 id + `sapling` 만) | `dig` (`harvest`) |
| 25~26 | `resources_removed/spawn` | 일괄(청크·숲) | ⓑ | |
| 27 | `rooms_update` | 방 판정 | ⓑ | |
| 28 | `sapling` | **메시지 아님** — 자원 타입 낱말(`insertResource`) | ⓒ 착시 | |
| 29 | `tools` | 도구 목록 | ⓑ | |
| 30 | `wall`(메시지) | **메시지 아님** — 건물 타입(`insertBuilding`) | ⓒ 착시 | |
| 31 | 경작(act) | 주민이 밭을 만든다 → `building_added` farmland | **ⓐ**(경유 · 이번에 운다) | `dig` |
| 32 | 낚시(act) | 상위 동사 — 결말 셋(T321)이 운다 | ⓑ | |
| 33 | 벌목(act) | 주민 벌목 = `resource_update`(3055) → `axe` | ⓐ′ | `axe` |
| 34 | 수색(act) | 상태 라벨 | ⓑ | |
| 35 | 수확(act) | 주민 수확 = 밭 `crop` 꺼짐 — 사람 수확도 같은 꼴(사람은 `where:harvest` 가 운다) · 잇으면 사람 수확이 두 번 | ⓒ | |
| 36 | 출근(act) | 상태 라벨 | ⓑ | |
| 37~41 | `drop:amount·equip·ids·lot·tool` | 떨어진 물건마다 `ground_item_added` → `drop`(T321) | ⓐ′ | `drop` |
| 42~43 | `pickup` · `pickup:tool` | 줍기 = #19 방송이 운다(where 는 안 잇는다 — 두 번 0) | **ⓐ**(경유 · 이번에 운다) | `pickup` |
| 44 | 숯가마 `charcoal_kiln` | `data.job` 이 있을 때 탄다 | **ⓐ** | `fire` 반복(이웃) · 꺼냄 `craft_done` |
| 45 | 문 `door` | `data.open` | **ⓐ** | `door_open`/`door_close` |
| 46 | 울 `fence` | 구조물 — 사건은 #1~3 이 운다 | ⓑ | |
| 47 | 노 `furnace` | `data.job` | **ⓐ** | `fire` 반복 · `craft_done` |
| 48 | `furnace_site` | 터 — 단계 오름 = #1 | ⓑ | |
| 49 | `guild_granary`(건물) | 구조물 | ⓑ | |
| 50~52 | `hut_site`·`kiln_site`·`shelter_site` | 터 — #1(오름)·#3(바뀜 조용) | ⓑ | |
| 53 | `stair` | 구조물 | ⓑ | |
| 54 | `village_hall` | 구조물 | ⓑ | |
| 55 | `village_site` | 터 | ⓑ | |
| 56 | `wall`(건물) | 구조물 — #1~3 | ⓑ | |
| 57 | 그루터기 `stump` | 캐기 = `resource_update`/`removed` | **ⓐ** | `axe`(이웃 · 새 파일 0) |

**셈**: ⓐ 14(이 카드 — #1·2·3·4·10·19·24·31·42·43·44·45·47·57) + ⓐ′ 6(#33·37~41) = **20/57 운다** · ⓑ 30 · ⓒ 7(착시 6 + 수확).

## ⓑ 확보 표

라이선스는 파일마다 페이지에서 읽었다(Kenney = 팩 `License.txt` · OGA = 페이지 License(s) 칸 · 원천이 있으면 그 페이지까지). 원본 맥 `~/Mini/_sfx_in/_T412/`(+`SOURCE.txt`). 가공: 머리·꼬리 −40 dB 다듬기 → 피크 −1.5 dBFS → Vorbis q4 / AAC 96k · 모노.

| 키 | 배선 | 원본 | 출처 · 라이선스 | 구간 | 길이 | 피크 ogg/m4a |
|---|---|---|---|---|---:|---|
| `build_place` | ○ `buildAdded.기본` | `impactPlank_medium_000` | Kenney Impact · CC0 | 0.000~0.174 | 0.17s | −1.58/−1.50 |
| `build_place_b` | 후보 | `impactPlank_medium_002` | 〃 | 0.000~0.187 | 0.19s | −4.49/−1.62(AAC 넘침 → 목표 −4.5) |
| `build_hit` | ○ `buildDamaged` | `impactPlank_medium_001` | 〃 | 0.000~0.386 | 0.39s | −1.51/−1.53 |
| `build_hit_b` | 후보 | `impactWood_heavy_003` | 〃 | 0.000~0.171 | 0.17s | −1.37/−1.46 · 중심 123Hz |
| `build_break` | ○ `buildRemoved.기본` | `bfh1_wood_breaking_03` | OGA 75 CC0 breaking · rubberduck · CC0 | 0.000~0.286 | 0.29s | −1.84/−1.47 |
| `build_break_b` | 후보 | `bfh1_wood_breaking_04` | 〃 | 0.016~0.228 | 0.21s | −2.17/−1.52 |
| `door_open` | ○ `buildEdge` door | `doorOpen_1` | Kenney RPG · CC0 | 0.003~0.881 | 0.88s | −1.41/−0.33 |
| `door_open_b` | 후보 | `doorOpen_2` | 〃 | 0.030~1.364 | 1.33s | −1.23/−1.56 |
| `door_close` | ○ `buildEdge` door | `doorClose_1` | 〃 | 0.001~0.431 | 0.43s | −1.09/−0.56 |
| `door_close_b` | 후보 | `doorClose_2` | 〃 | 0.006~0.503 | 0.50s | −1.41/−1.51 |
| `pickup` | ○ `groundPick` | `cloth2` | 〃 | 0.035~0.357 | 0.32s | −1.41/−1.73 |
| `pickup_b` | 후보 | `handleSmallLeather` | 〃 | 0.028~0.221 | 0.19s | −1.49/−1.44 |
| `chest_use` | ○ `work.chest_state` | `creak3` | 〃 | 0.016~0.283 | 0.27s | −1.63/−1.41 |
| `chest_use_b` | 후보 | `creak1` | 〃 | 0.056~0.625 | 0.57s | −1.43/−1.43 |
| `craft_done` | ○ `buildEdge` job 꺼짐 | `impactMetal_light_000` | Kenney Impact · CC0 | 0.000~0.190 | 0.19s | −1.63/−1.30 |
| `craft_done_b` | 후보 | `metalPot2` | Kenney RPG · CC0 | 0.107~0.738 | 0.63s | −0.52/−0.74 |
| `dig` | ○ `resourceNew`·`buildAdded.farmland`·`buildEdge` crop | `shovel.ogg` | OGA Shovel Sound · themightyglider · **CC0**(원천 freesound 503672 RavenWolfProds 'Shovel in dirt' 도 **CC0** — 페이지에서 확인) | 0.000~0.722 | 0.72s | −1.23/−1.00 |
| (`dig` 2번) | **없음** | — | 다른 CC0/PD 삽 소리 못 찾음(`Digging Underground` 는 CC-BY 3/4 + 연락 요구) | | | 녹음 회부 |

**헤드룸 `_실측`**(58키 · `__sfx.probe` 4초 오프라인):

| 조합 | 키 | 리미터 없이 | 끼고 | 문턱 넘는 창 |
|---|---:|---|---|---|
| **T292 여섯 + 공사 다섯 ← 새 `worstCombo`** | 11 | **0.7967**(−1.97) | 0.7610 | 0/400 |
| T292 여섯(종전 최악) | 6 | 0.7957 | 0.7600 | 0/400 |
| 공사판(9 + 불·바람) | 11 | 0.6305 | 0.6305 | 0/400 |
| 미끼(⑧g) | 7 | 0.8755 | 0.7632 | 2/400 |
| 배선 38키 전부 | 38 | 1.3524 · 클리핑 40 | 0.7788 · 0 | 19/400 |
| 58키 전부 | 58 | 1.3905 · 290 | 0.7871 · 0 | 36/400 |

## ⓒ 한 줄 답 + 전수 갱신

**도구/작업 57자리 중 20이 운다(새로 14 — 짓기·치기·허묾·문·궤·줍기·심기·밭 일굼·가마 불과 꺼냄·그루터기), 30은 상태 동기화라 조용하고, 7은 메시지가 아니거나(6) 사람 것과 겹쳐 가를 수 없다(수확) — 제작 완료는 알리는 메시지가 없어 회부다.**

`node scripts/sfx-census.js`: **170자리 · 있음 51 · 없음 119**(T402 40 → 51 · +11: 서버 메시지 `building_added/damaged/removed/updated`·`ground_item_removed`·`resource_spawn`·`chest_state` 7 · 건물 `door`·`furnace`·`charcoal_kiln` 3 · 자원 `stump` 1). 자는 `work` 표와 `buildEdge` 도 읽게 고쳤다(철자로 부재를 재지 않는다).

---

## §3 검증

| 자 | 전 | 후 |
|---|---:|---:|
| `test-audio` | 160 | **170/0**(⑰a~⑰g2 10 · 표 안 규칙까지 키를 센다) |
| `e2e-audio-probe` | 61 | **71/0**(㊵~㊼ 10) |
| `e2e-aim` · `e2e-pid-collide` | 15 · 13 | **15/0 · 13/0** |
| `test-client-globals` · `test-harness-lint` · `test-assets-audit` | 26 · 61 · PASS | 26 · 61 · PASS |
| `asset-lock` | 548 | **582 · 어긋남 0** |
| 소리판 행 | 41 | **58 = 키 58**(⑲) |

**비트 동일** — 기존 41키 41/41 · 바뀐 최상위: `keys`(새 17) · 새 표 `work`·`buildAdded`·`buildRemoved`·`buildDamaged`·`buildEdge`·`groundPick`·`resourceNew`·`buildingsWhen` · `buildings`(+`furnace`·`charcoal_kiln` → `fire`) · `resourceHit`(+`stump` → `axe`) · `_실측` · `sources`(새 `oga_shovel` + 셋 `_원본` 글).

**접점** `manifest.json` · `sources` · `building_added` · `building_damaged` · `building_removed` · `building_updated` · `buildEdge` · `furnace` · `charcoal_kiln` · `buildingsWhen` · `chest_state` · `ground_item_removed` · `groundPick` · `resource_spawn` · `resourceNew` · `sapling` · `stump` · `craft_queue` · `preserve` · `48-a-audio` · `sfx-board` · `CREDITS` · `_sfx_in` · `sfx-census` · `test-audio ⑰`.

## §4 회부

1. **[PM · 서버 낱말] 제작·보존 완료** — 완료를 알리는 메시지가 없다(줄에서 빠짐 = 완료 또는 취소). 완료 한 줄(예: `sendInventory(player, 'craft')` 꼴 · T305 문법)이면 표 한 줄.
2. **[재민] 주민 수확 소리** — 밭 `crop` 꺼짐으로 이으면 사람 수확이 두 번 운다. 주민 것만 가르려면 서버가 누가 거뒀는지 싣거나(한 칸) 사람 쪽 `where:harvest` 를 걷어내야 한다.
3. **[재민 폰] 녹음** — `dig` 2번(삽질).
4. **[세션9] 헤드룸 여유 0.03 dB** — 다음 키 묶음에서 ⑧c 가 물 수 있다. 그땐 문턱이 아니라 조합의 뜻(평소에 한 표본에 모이는가)을 PM 과 다시 정한다.
5. #60 둘째 귀 판정(재민 · 새 행 17) · 동물 40 · UI/알림 21 · CC-BY 표시 · 천둥 사건 — 그대로.
