# 보고 — T174 온보딩 전체 호를 한 하네스가 걷는다 (2026-09-11)

베이스 **`6e236bc7`**(origin/main) · 헤드 = **이 번들의 HEAD**(`git bundle list-heads`) · 가지 `batch/onboarding-arc-0911`
⚠헤드 해시를 이 파일에 적지 않는다 — 적는 순간 커밋이 바뀌어 **그 해시가 틀려진다**(세 번 쫓다 멈췄다).
3판 규약대로 **착지 뒤 PM 이 기입**한다.
**푸시: 미푸시(3판)** · 번들 `~/Mini/_inbox/T174_2026-09-11.bundle` · **force push 0 · 푸시 시도 0**
**게임 코드 0줄** — 하네스 둘만 만졌다.

한 줄: 호는 **11걸음**이고 그중 **7이 초록 · 4가 미검증**이다. 이 카드가 한 일은 *"끝까지 걸었다"* 가
아니라 **"어디까지 걸었고 어디가 비었는지 한 표에 세운 것"** 이다 — 세 카드가 각자 회부로 넘긴 자리를
이제 **한 줄씩 이름으로** 부를 수 있다.

---

## §0-ⓐ 호의 걸음 표 — 지금 어느 하네스가 무엇을 잡고 있나

| # | 걸음 | 누가 재나 | 판정 |
|---|---|---|---|
| ⓪ | ws 손님 하나(대본 뒤 세계) | 이 절 | ○ |
| ① | 기여 3회 | **대본 절**(`누적 기여 n/3`) | ○ |
| ② | 빈터 권리 · 어귀에 내 땅 | **대본 절**(`빈터 권리가 섰다` · `어귀 빈터에 내 땅을 걸었다`) | ○ |
| ①′②′ | 위 둘의 **ws 재현** | 이 절 | ● (아래 ⚠) |
| ③ | **건립**(`village_start`→`village_advance`×3→`foundPlayerVillage`) | **아무도** | ● 회부 |
| ④ | **인구**(곳간 식량 → 주민이 깃든다) | **아무도** | ● 회부 |
| ⑤ | **곳간 3일치**(자립일수 ≥ 3) | **아무도** | ● 회부 |
| ⑥ | `/이방인 받기` | `test-newcomers ⑧`(T167) | ○ |
| ⑦ | 시작 화면에 뜸 | `test-newcomers ⑦`(T19 `listable`) · `⑧e3`(줄 필터) | ○ |
| ⑧ | 소개문이 인사로 | `test-newcomers ⑧e2`(T167 · **소스** · 실판 미검증) | ○△ |
| ⑨ | 인출이 **곳간을 실제로 준다** | `e2e-guild ⑦e2`(T159) | ○ |

⇒ **비어 있는 것은 ③④⑤ 셋**이다. 그 셋이 "유저 마을이 **실제로 서는**" 구간이고,
T128·T159·T167 이 세 번 다 그 자리에서 회부를 적었다. **이제 이름이 붙었다.**

⚠**①′②′ — ws 재현은 대본 뒤 세계에서 안 선다(실측 · 세 판).**
`기여 0/3 · 납품성사 0`. 게시판이 **닫힌 게 아니라 비어 있다**(게이트 오류 문구가 한 번도 안 잡혔다 —
그걸 잡으려고 `_boardErr` 를 넣어 뒀다). 대본이 위에서 수백 게임일을 돌린 뒤라 그 마을에 **열린 의뢰 줄이 없다**.
1차 판에서는 `납품성사 1` 까지 갔다가 판이 닫히면 그 자리에서 멈췄었다(그 `break` 를 고쳐 기다리게 했다).
⇒ **①② 의 판정은 대본 절의 그 세 줄이 갖는다.** ws 재현은 그 사실을 적는 자리로 남겼다(지우지 않았다 —
언젠가 판이 다시 열리는 세계에서 이 줄이 초록이 되면 그게 정보다).

## §0-ⓑ 호를 끝까지 걸으려면 필요한 픽스처 (있는 문법 · 새 픽스처 0)

| 걸음 | 있는 문법 | 비고 |
|---|---|---|
| 기여 | `village_board` → `rows[].give`(낼 수 있는 **품목**) → `__e2e_give` → `village_deliver` | ⚠`rows[].item` 은 **재화**다 — 그걸로 내면 납품 0(초안이 그랬다) |
| 빈터 | `onboarding_state` 의 `lotOk`·`lot` | 새 관측창 0 |
| 건립 | `village_start`(atX,atY) → `village_advance`(buildingId)×3 | 재료 돌 30 / 돌 40+통나무 20 / 통나무 60 · **곡괭이** 필요 |
| 인구 | `village_deposit`(buildingId, want) → 게임일 경과 | `VILLAGE_DAY_MS=500` 이면 실시간 몇 초 |
| 받기 | `village_welcome`(buildingId,on) 또는 `/이방인 받기` | T167 |
| 시작 | `?start_vid=<vid>` 로 접속 · `onboarding_greet` | T19 |
| 인출 | `/곳간 <품목> <양>` | T159 |

**픽스처는 게임분이다 — 잠(sleep)으로 시간을 사지 않았다**(공통 §2 ⑩). 위 표의 대기는 전부
`VILLAGE_DAY_MS` 가 도는 실제 게임일 경계다.

## §0-ⓒ 걸음별 통과/깨짐 — **게임 코드 결함 0건**

호를 걸으며 **게임 쪽이 깨진 자리는 하나도 안 나왔다.** 못 걸은 ③④⑤ 는 *"깨졌다"* 가 아니라
*"아무도 안 걸어 봤다"* 다. 이 카드가 만든 빨강도 전부 **하네스 쪽**이었고 그 자리에서 고쳤다:

| 고친 것 | 무엇이 틀렸나 |
|---|---|
| 납품 품목 | `rows[].item`(재화)로 냈다 → `rows[].give[0]`(품목)로 |
| 판이 닫히면 | 그 자리에서 `break` → **기다렸다가 다음 게임일 줄을 본다** |
| 워프 후보 | 한 걸음 37px 씩 24번(900px 밖 → 게시판 게이트 닫힘) → **72px 안 링** |

## §1 만든 것

```
  scripts/e2e-onboarding.js  [T174] 호 절 하나(ws · 브라우저 0) + 걸음 표 출력 + 자명 통과 금지 한 줄
  scripts/e2e-friends.js     판정 자리에서 이모지 한 자리 제거(`/🌀|텔레포트 →/` → `/텔레포트 →/`)
  scripts/e2e-guild.js       ⑦f2 의 전제 한 자리 — 거절은 **짐**으로 잰다(곳간은 주민이 먹어 줄어든다)
```
**서버 diff 0 · 클라 diff 0.**

## §2 검증

```
  scripts/e2e-onboarding.js   64 / 1   ★호 절 포함 — 빨강 한 줄은 **아래 ⚠**(이 카드 것이 아니다)
  scripts/test-harness-lint.js 22 / 0   ★21/1 → 22/0 (판정 자리 이모지 0)
  scripts/test-newcomers.js   56 / 0   ★호 ⑥⑦⑧ 을 잡고 있는 하네스
  scripts/e2e-guild.js        84 / 0   ★호 ⑨ 를 잡고 있는 하네스(⑦f2 를 고쳐 다시 84/0 · 아래)
```
⚠**`e2e-onboarding` 의 빨강 한 줄** — `심각도 0.500 < 0.490`(허기 문턱 **바로 옆**). T159 §2 가
**베이스에서도 난다**를 같은 폴더에서 증명해 둔 그 줄이다(베이스 61/0 · 58/4). 이 카드 것이 아니다.

⚠**`e2e-guild ⑦f2` 가 빨개져서 고쳤다 — 하네스의 전제였다.**
그 줄은 *"거절했으면 곳간도 그대로다"* 를 `stock2 === stock1` 로 쟀는데, **마을은 살아 있다** —
두 번 읽는 사이 주민이 먹어 `341 → 340` 이 됐다(진짜 인출은 `343 → 341`). 재는 자리를 **짐**으로 옮겼다:
거절당한 인출은 **짐이 한 톨도 안 는다**. 곳간은 떠내려가고 짐은 안 떠내려간다.
⇒ **제품 0줄 · 검사의 전제 한 줄**(T98 비·T128 그림·T147 문구와 같은 종류의 고침이다).

**적재 추적**: 하네스만 만졌다 — 랩 적재 밖(서버·클라 diff 0).

**자명 통과 금지**: 걸음 표가 **전부 초록이면 빨갛게** 만들어 뒀다(`미검증 걸음이 실제로 남아 있다`).
표가 "다 됐다"고 말하는 순간 그 표는 거짓말이기 때문이다.

## §3 회부

1. ★**호 ③④⑤ — 유저 마을이 실제로 서는 구간**(건립 · 인구 · 곳간 3일치). 아무 하네스에도 없다.
   §0-ⓑ 에 **필요한 픽스처 문법을 다 적어 뒀다** — 다음 카드는 그 표대로 이으면 된다.
2. **①′②′ 가 초록이 되려면** 대본 뒤 세계가 아니라 **제 판**이 필요하다(별도 존 기동 · 이 카드 밖).
3. 유저 마을 첫 의뢰 정본(T167 회부 4) · 부름 만료(재민 #8) · 길드 패널(캐논) · 인출 공식(T159 회부 1).

## §4 실기 두 줄 (재민)

* 새 캐릭터로 **네 길드 마을**에서 시작해 소개문이 인사로 뜨는가.
* 거기서 `/곳간 보리 5` 가 실제로 나오는가(그리고 `/곳간` 이 말하는 재고가 그만큼 주는가).
