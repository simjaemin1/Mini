# T391 — 기본값 복원 · 재굽기 해시 **2/13**(파일 **바이트 동일** 4/26 — `village_day_trad`·`village_day_jeongak` 의 .ogg·.m4a) · 11곡은 **레포 밖 07-30 판**이라 재현 불가 · `.pyc` 0 · A/B 행 2 · 장구 ○

가지 `batch/bgm-restore-0925` · 베이스 **`259dadb6`** · 되돌린 기준 `450c6e44`(GPT 착지 `462b4acc` 바로 앞) · **force 0** · main 은 PM
**`bgm.js` diff 0 · phrase opt-in 무변 · `sfx/**` 무접촉 · 배포 소리 파일 diff 0**(재굽기가 바이트까지 같아 올릴 것이 없다 · 새 소리 0) · 값 무변(`render-meta.json` 은 메모 `_T391` 한 칸만)

| 넷 | 결론(실측) |
|---|---|
| ⓐ ① 기본값 | `render_score.py`·`render_jeongak.py` 기본 = **`450c6e44` 본문 그대로** · GPT 규칙은 `_play_contract` 로 남기고 스위치 **`ARTICULATION_CONTRACT = False` 한 자리**. **끔 재굽기 = 450c6e44 재굽기 2/2 · 켬 재굽기 = GPT 판(259dadb6) 2/2 — 비트 동일**. `sampler.py`(`entry_mode='auto'` = 옛 규칙)·`score_village_day.py`(빈 표) 무변 |
| ⓐ 재굽기 해시 | **2/13 — 파일 바이트까지 같다.** 되돌린 기본 + 복원 뱅크 + numpy 2.3 이상으로 구워 libvorbis `-q:a 4` 로 싸면 vorbis 패킷 **6517/6517 · 6262/6262**, Ogg 일련번호(먹서가 난수로 뽑는 한 칸)만 맞추면 **.ogg sha256 같다** · .m4a(aac 128k)는 **그대로 같다** |
| ⓐ 결정론 | 같은 판·같은 뱅크·같은 환경이면 두 번 구워 같다. 환경 셋이 **표본 끝자리**를 흔든다: numpy 1.26 → 패킷 91% · 2.0~2.2 → 99.5% · **2.3/2.4 → 100%** · AVX-512 끔 → 99.4% · scipy 1.15~1.17 은 무관 |
| ⓐ GPT 기본 | 같은 뱅크로 GPT 기본값을 구우면 배포와 **다른 소리**: SNR −1.9 / +3.0 dB · 패킷 1/6517 · 1/6262. **차 RMS 한 줄**: 정악 −14.57 dBFS(복원 대비 **+1.91 dB**) · 산조 −20.43 dBFS(**−3.02 dB**) — 음량은 0.1 dB 안인데 파형이 어긋났다 |
| ⓐ 11곡 | 굽는 코드를 **길이 표본까지** 짚었다 — `tracks2.py`(`render2.py`) · 레포에 없다(`코드백업.zip` 안). 배포 11곡은 **07-30 12:33 미리듣기**(`두랑고BGM_통합.html`)와 mp3 바닥 안에서 같다(SNR 16.1~21.6 dB · 바닥 20.3) ⇒ **07-30 판**인데 그 판 코드는 어디에도 없다(두 zip 모두 07-31) · 씨앗이 `hash()`(PYTHONHASHSEED) · 뱅크 넷(징·북·박·꽹과리) 없음 |
| ⓑ `.pyc` | 4장 `git rm --cached` · `.gitignore` `__pycache__/` · `test-assets-audit ⑧` — 착지판에 들이대면 **⑧a 4장·⑧c ✗** · 미끼 **+1** · `--selftest` 셋 다 문다 |
| ⓒ A/B | ★`sigim()` 은 배포 **13곡 모두**를 지난다(T353 "0곡"은 틀렸다 — 내 판). `village_day_ari` 켬/끔 **해시 다르다**(`fb4aeb2e…`↔`10c6a5a2…` · 차 −1.86 dB · RMS 0.04 dB 차) ⇒ 브금판 **로컬 청취본 2행**(배포 안 함 · `~/Mini/_bgm_ab/T391/` · 판이 sha256 으로 그 파일인지 잰다) |
| ⓒ 장구 | **○ 궁편 실음원이 있다**(6조각) · 배포 2곡은 궁편 **84타씩** 샘플로 굽혔다(**바이트 동일 재현으로 증명**) · 장구 뱅크 없이는 TypeError(실행) ⇒ `janggu_gung: '합성'` 은 그 2곡에 **틀렸다** · **값 무변**(카드) · 11곡은 못 잰다 |

게이트 `test-audio` 145/0 · `test-bgm-phrase` 16/0 · `test-assets-audit` **PASS(+⑧)** · `--selftest` PASS · `e2e-audio-probe` 44/0 · `test-client-globals` 26/0 · `test-harness-lint` 61/0 · `test-render-score-articulation` **14/0**(⑪ 뒤집음) · 접점 파이썬 **12 초록** · 2 못 돎(원음 모음이 bk_* 밖)

## ★ 답 한 줄

**기본값을 착지 전으로 돌렸더니 배포 두 곡이 파일 바이트까지 되살아났다(2/13) — GPT 기본값으로는 같은 뱅크에서도 SNR −2~+3 dB 의 다른 소리다 · 나머지 11곡은 레포 밖 07-30 판이라 코드가 없다 · `.pyc` 0 · A/B 2행 · 장구 궁편은 샘플이다(○ · 값은 안 고쳤다)**

---

## §ⓐ ① Python 굽기 기본값 — 되돌리고, GPT 규칙은 스위치 뒤에

### ⓐ-1 무엇을 되돌렸나 (네 파일 표)

| 파일 | GPT(`462b4acc`)가 **카드 없이** 바꾼 기본 | T391 |
|---|---|---|
| `render_score.py` | `play()`: 붙은 음 자동 이음(앞 음 끝에서 **0.05소박** 안 → 어택 지우고 70ms 맞물림) · 장단 머리 `tie_first` · 음마다 떨림 7ms(이음 3ms) → **명시 계획**(`plan_sustained_score`) · 무표기 = **재발음**(head · 35ms 안에서 닫힘) · 떨림 0. `build()`: 20ms 창·맺음 규칙 → 계획 | 기본 = **`450c6e44` 본문 그대로**(줄 단위 대조 — 빠진 줄은 `else` 로 들여 쓴 것뿐). GPT 본문은 이름만 `_play_contract` 로 바꿔 **남겼다**(지우지 않는다). 스위치 **`ARTICULATION_CONTRACT = False`** 한 자리 — #70 이 켜면 `False`→`True` 한 낱말. `XFADE` 는 두 벌로 갈랐다(기본 0.070 · 계약 쪽은 `render_articulation.XFADE` 0.070) — R&D 가 계약 쪽 값을 만져도 **배포 규칙이 안 따라 움직이게** |
| `render_jeongak.py` | 같은 `build()` 교체 | 같은 스위치를 **부를 때** 읽는다(`RS.ARTICULATION_CONTRACT`) — 켜는 자리는 하나 |
| `sampler.py` | `note(entry_mode=)`·`install` 이 넘김·합성 폴백에 `entry_mode` 막음·`_supports_explicit_sample_entry` | **무변.** `'auto'` 는 옛 규칙과 같다 — **증명**: 이 판 그대로(GPT `sampler.py`) 끔으로 구운 것이 `450c6e44`(옛 `sampler.py`)로 구운 것과 **비트 동일**(아래 표) |
| `score_village_day.py` | `DAEGEUM_ARTICULATION = {}` 빈 표 | **무변** — 끔일 때 안 읽힌다 |

★CLI 요약은 GPT 판(항목 길이에 안 넘어지는 `item[0] for item in ENTRY`)을 **남겼다** — 기본 경로는 옛 두 칸, 계약 경로는 네 칸을 적는데 둘 다 읽는다(굽는 소리와 무관한 인쇄 줄).

### ⓐ-2 실증 — 끔은 옛 판, 켬은 GPT 판 (WAV 파일 sha256 앞 16 · 같은 복원 뱅크)

| 굽기 | numpy 1.26.4 정악 | 산조(`village_day_trad`) | numpy 2.3.5 정악 | 산조 |
|---|---|---|---|---|
| `450c6e44` (착지 전) | `94f92bee8c46516f` | `d2ce3f2c5542f305` | `aa5668b7c8c409d6` | `a792ba8484c4b0c6` |
| **T391 기본(끔)** | `94f92bee8c46516f` ✓ | `d2ce3f2c5542f305` ✓ | `aa5668b7c8c409d6` ✓ | `a792ba8484c4b0c6` ✓ |
| T391 켬(`sed` 로 한 낱말) | `8049c42f6643115d` | `81e7957dd91b4b9a` | — | — |
| GPT 판 `259dadb6` | `8049c42f6643115d` ✓ | `81e7957dd91b4b9a` ✓ | — | — |

⇒ **스위치 하나가 두 판을 정확히 가른다** — 끔 = 착지 전 · 켬 = GPT. 그리고 numpy 2.3 줄의 두 값이 **배포 파일을 바이트까지 되살린다**(§ⓐ-3).

### ⓐ-3 차 RMS 한 줄 — GPT 기본으로 구운 것과

**정악: 차 RMS −14.57 dBFS(복원 −16.49 대비 +1.91 dB) · 산조: −20.43 dBFS(복원 −17.41 대비 −3.02 dB)** — 전체 음량 차는 0.10 / 0.01 dB 뿐인데 파형은 사실상 다른 판이다.
배포와 견주면 더 분명하다: 되돌린 기본은 **코덱 바닥**(SNR 27.5 / 23.6 dB = 제 WAV 의 vorbis 왕복 바닥과 같다), GPT 기본은 **SNR −1.9 / +3.0 dB · vorbis 패킷 1/6517 · 1/6262**.
까닭(코드 읽기): 붙은 음이 이어 불기에서 **재발음**으로 바뀌고, 음마다 7ms 떨림이 빠지면서 **같은 씨에서 뽑는 난수 차례가 밀려** 뒤의 모든 음의 농현 씨가 바뀐다. "한 글자도 안 움직였다"와 "들리는 것이 안 바뀌었다"가 **다르다**는 족보 312 의 문장이 수로 선다.

## §ⓐ 재굽기 해시 — 13곡

### ⓐ-4 굽는 경로를 **길이 표본까지** 짚었다

| 곡 | 배포 길이(표본) | 식 | 굽는 코드 | 레포에 | 재굽기 = 배포 |
|---|---|---|---|---|---|
| `village_day_trad` | 4,775,148 | 22장단×12×0.395 + 4.0 | `render_score.py` | ○ | **○ 바이트 동일** — .ogg `1c2e033b29776089` · .m4a `9752f3a3241498bc` |
| `village_day_jeongak` | 6,041,700 | 22×12×0.50 + 5.0 | `render_jeongak.py` | ○ | **○ 바이트 동일** — .ogg `134f172b941fb11f` · .m4a `70a0834e078e41a8` |
| `village_day_amb`·`_ari` | 5,028,282 | 23주기×4.74 + 5.0 | `tracks2.py` ← `render2.py` | ✗(`코드백업.zip` 안) | ✗ — 07-30 미리듣기와 SNR 19.7·19.8 dB |
| `village_night_*` ×3 | 4,983,299 | 15×7.20 + 5.0 | 〃 | ✗ | ✗ — 18.7 · 21.6 · 19.7 dB |
| `battle_*` ×3 | 4,978,008 | 58×1.86 + 5.0 | 〃 | ✗ | ✗ — 16.1 · 16.2 · 19.6 dB |
| `journey_*` ×3 | 4,977,346 | 51×2.115 + 5.0 | 〃 | ✗ | ✗ — 18.5 · 18.7 · 19.6 dB |

★**README 의 "다시 만들려면" 세 줄은 배포 경로가 아니다.** `render_samples.py samples_gaya` 는 **TypeError** 로 죽는다(`compose.py:123` `janggu_gung(art=…)` · 실행 확인 — T353·T372 가 정적으로 짚은 것을 이번엔 뱅크를 놓고 돌려 봤다), 그리고 그 경로(`compose.py`·`arirang.py`)의 길이는 배포 12곡과 **12/12 안 맞는다**(`_T257` 이 잰 synth 열 — 이제 까닭을 안다: 배포 11곡은 그 두 파일에서 오지 않았다).

### ⓐ-5 결정론 — 무엇이 표본 끝자리를 흔드나 (정악 · 12:22판 조각 · 같은 CPU)

| 환경 | WAV | 배포와(vorbis q4 패킷) |
|---|---|---|
| 같은 판 두 번(다른 프로세스) | **같다** | — |
| numpy 1.26.4 · scipy 1.15.3 / 1.16.3 / 1.17.1 | `94f92bee` ×3 | 5917/6517 (90.8%) · 산조 5733/6262 |
| numpy 2.0.2 / 2.1.3 / 2.2.6 · scipy 1.16.3 / 1.17.1 | `efd98477` ×4 | 6482/6517 (99.46%) |
| numpy 2.2.6 · **AVX-512 끔** | `63843052` | 6480/6517 |
| **numpy 2.3.5 / 2.4.6** · scipy 1.16.3 / 1.17.1 | **`aa5668b7` ×3** | **6517/6517 · PCM 같다** · 산조 **6262/6262** |
| 조각을 **레포 `sampler.py`(13:01판)로** 풀면 | `6aff65f1` | SNR 15.3 dB · 산조 2.9 dB (다른 조각) |

* **scipy 는 무관**, numpy 는 **세 줄**(1.26 · 2.0~2.2 · 2.3~2.4)이 다르고 AVX-512 켬/끔도 다르다 — 7월 굽기 자리는 **numpy 2.3 이상 · AVX-512 경로**로 좁혀진다(이 상자 Xeon 과 같다). 차는 전부 **귀 밖**(코덱 바닥 안)이다.
* ★**조각은 레포 `sampler.py` 로 풀면 안 된다.** 배포 굽기는 `split_notes(max_len=4.0)` 판(`코드백업_최신.zip` 의 12:22 `sampler.py`)으로 푼 조각을 썼다. 레포 판(13:01)은 `max_len=9.0` 이다 — 정악대금(jdae)을 9초로 풀려고 바꾼 것인데, 그 뒤로 다른 뱅크를 다시 풀면 **다른 조각**이 나온다(위 줄). jdae·jgaya 는 `_trimmed` 가 묶음에 들어 있어 풀지 않는다.
* **인코더**: vorbis `-q:a 4`(VBR) — `-b:a 128k` 로는 산조는 같지만 정악은 소리 패킷 4장(434·2018·2747·4824번)이 달라 배포는 **q4** 다. Ogg 일련번호는 ffmpeg 먹서가 **난수**로 뽑는다(`av_get_random_seed` · bitexact 면 `Lavf60.16.100` 벤더가 바뀌어 다른 파일) — 그래서 파일 해시는 그 한 칸(정악 137장 · 산조 110장 페이지의 CRC 포함)을 배포 값으로 맞춰 대조했다(`ogg_reserial` · 소리 바이트 무접촉). .m4a 는 native aac 128k 가 **그대로** 같다.

### ⓐ-6 11곡은 왜 안 되나 — 넷

1. **코드가 없다.** `render2.py`·`tracks2.py`·`garak.py` 는 레포 어디에도 없고 `코드백업.zip`·`코드백업_최신.zip`(둘 다 07-31 스냅숏) 안에만 있다. 그 셋이 부르는 `gugak`·`compose`·`motif`·`sampler` 는 **07-30 12:26 뒤에 다 고쳐졌다**(zip 시각 07-31 07:01~13:01).
2. **배포 11곡은 07-30 판이다.** `두랑고BGM_통합.html`(07-30 12:33 · `build2.py` 가 `out_samples/*.wav` 앞 34초를 mp3 80k 로 박은 판 · 맥 bgm 폴더 · gitignore)의 12개와 배포를 견주면 11곡이 **SNR 16.1~21.6 dB · 어긋남 0표본** — mp3 80k 바닥(배포를 80k 로 왕복) **20.3 dB** 와 같은 띠다. 대조: 다른 곡끼리 −1.4 dB · zip 판 `tracks2` 재굽기 ↔ 미리듣기 **−2.1 dB**. `village_day_trad` 만 −2.3 dB 인데, 그 곡은 07-31 에 `render_score` 판으로 **갈아 끼웠기** 때문이다(미리듣기 114.0초 ↔ 배포 108.28초).
3. **씨앗이 프로세스마다 다르다.** `seed=abs(hash((scene, mood)))` — `PYTHONHASHSEED` 가 없으면 매번 다른 곡(0 과 1 로 구우면 `sigim` 호출 123 ↔ 125 · 해시 `fb4aeb2e` ↔ `2a815198`). 7월 굽기의 값은 기록이 없다.
4. **뱅크 넷이 없다.** `render2.ROOTS` 열 가운데 `samples_jing`·`_buk`·`_bak`·`_kkwaenggwari` 는 bk_* 12묶음에 없다(그 판은 열 개를 다 열어야 돈다).

⇒ **지금 증거로는 영구 재현 불가.** 계보(편곡)는 선다 — zip 판으로 구운 `village_day_ari` 와 배포의 50ms 포락 상관 **0.840**(대조: 배포 amb 0.700 · trad 0.254 · journey_ari 0.488).

## §ⓑ `.pyc` 4장

| | 착지판 `259dadb6` | 이 가지 |
|---|---|---|
| 추적되는 `public/**/__pycache__/*.pyc` | **4장**(articulation·gugak·phrase·sampler · cpython-311) | **0**(`git rm --cached`) |
| `.gitignore` | `scripts/__pycache__/` 만 | + **`__pycache__/`**(어느 폴더든 · 주석은 제 줄에 — T156 규약) |
| `test-assets-audit ⑧` (새 절) | ⑧a **✗ 4장** · ⑧c **✗ 안 무시** | ⑧a 0 · ⑧b 0 · ⑧c ✓ |
| 미끼(절 안 상주) | 추적 목록에 `.pyc` 한 장 → ⑧a **4→5** · 무시 안 된 목록 → ⑧b 0→1 · 대조 `.py` → 그대로 | 0→1 · 0→1 · 그대로 |
| `--selftest` | — | 없는 참조·잠금 어긋남·**미끼 `.pyc`** 셋을 주입 → **셋 다 문다** |

* 왜 "디스크에 있나"가 아니라 **"실리나"** 인가: 배포는 **서버 checkout** 에서 이미지를 굽는다(`redeploy-hanbando.sh` = git pull · `.dockerignore` 는 `bgm/*.py` 만 뺀다) ⇒ **추적되는 `.pyc` 가 실린다.** 디스크의 무시된 캐시는 bgm 모듈을 import 하는 파이썬 하네스·도구가 돌 때마다 생긴다(12개 넘는다) — 그걸 빨강으로 두면 하네스가 제 발에 걸린다. 그래서 **표만**(추적·무시 안 됨·무시됨 셋으로 갈라 센다).
* 내가 고친 하네스(`test-render-score-articulation.py`)는 `sys.dont_write_bytecode = True` 를 달았다 — 돌려도 캐시를 안 떨군다.

## §ⓒ 뱅크 셋

### ⓒ-0 격리 복원 (원본 무변)

| | |
|---|---|
| 묶음 | 맥 `bgm/bk_*` **12**(zip 10 + `bk_jdae_aa/ab.part`) — sha256 **전/후 같다**(`6aa28487…`·`86e1e03e…`·`eb228672…`·`6b6a3610…`·`b28d93b5…`·`536aa5db…`·`6d9688ed…`·`1d403bf2…`·`ffdb31fa…`·`18b5afdf…`·`e27229ea…`·`38a4a5e1…`) · CRC 전부 통과 |
| 맥 `~/Mini/_bgm_bank/` | 8폴더 **243 MB**(산조대금 6 · 단소 3 · 산조가야금 21 · 거문고 5 · 장구 19 · 정악대금 115 · 정악가야금 102 · 피리 3) — **굽기는 여기서 안 했다**(`Bank` 가 `_hold.json` 을 뿌리에 쓴다) |
| 굽기 자리 | 컨테이너 사본(같은 sha256 · 레포 밖 스크래치) |
| 색인 재생성 | 장구: **백업 색인과 같다**(뿌리 칸 빼고 · 두 판 스캐너 모두) · 단소 82→108 · 거문고 70→72 — 백업 색인은 **03:12 스캔**이고 그 스캐너는 어느 zip 에도 없다(배포 굽기는 그 뒤 12:22판으로 다시 푼 조각을 썼다 — §ⓐ-5 가 바이트로 증명) · 산조가야금·산조대금·피리는 색인이 묶음에 없어 12:22판으로 풀었다 |

### ⓒ-1 ⓑ `village_day_ari` A/B — `sigim()` 켬/끔

★**먼저 정정 — T353 §0-ⓐ ① "`sigim()` 은 배포 13곡 중 0곡" 은 틀렸다(내 판).** 그 표는 배포 12곡이 `compose.py`·`arirang.py` 에서 온다고 **가정**했다. 실측(§ⓐ-4): 11곡은 `tracks2.py` 이고 그 선율은 `garak.voice()`·`motif.voice()` 로 가는데 **둘 다 `sigim()` 을 부른다**(garak.py:126 · motif.py:232). `village_day_trad`·`_jeongak` 은 `render_score.play()` 가 부른다. ⇒ **13/13 이 `sigim()` 을 지난다.**

그래서 A/B 는 배포 계보(`tracks2.py`)에서 **`sigim()` 하나만** 켜고 끈다 — 끔 = `motif.sigim`·`garak.sigim` 을 (농현 0 · 꺾기 0 · 주법 없음)으로. 음표·반주·장단 무변 · `PYTHONHASHSEED=0` 고정(결정적 — 두 번 구워 같다).

| | sigim 켬 | sigim 끔 |
|---|---|---|
| WAV sha256(자료) | `fb4aeb2e52426f25` | `10c6a5a2c0155e4d` |
| RMS | −17.60 dBFS | −17.64 dBFS |
| 차 | RMS −1.86 dB(켬 대비) · 50ms 포락 상관 0.996 — **같은 곡의 다른 결** | |
| 청취본(bitexact) | .ogg `1bd8b4d986671835` · .m4a `9fdd30b11c731ec1` | .ogg `add2f73f60ac6c34` · .m4a `64979c822b01ad52` |

**해시가 다르다 ⇒ 브금판 A/B 행 2.** 다만 카드 §1 **새 소리 0** 과 부딪힌다 — 행이 서려면 소리 파일이 배포돼야 한다. 그래서 **파일은 싣지 않고 판이 고르게** 했다: `village_day_ari` 아래 「sigim 켬」「sigim 끔」 두 줄 · 「고르기」로 `~/Mini/_bgm_ab/T391/`(맥 · 레포 밖 · T353 A/B 와 같은 자리)의 파일을 열면 판이 **sha256 으로 그 파일인지**(○ 그 파일 / ✗ 다른 파일)와 RMS·피크·길이를 재고, 둘 다 열리면 「음량 맞춤」이 잰 RMS 로 큰 쪽을 낮춘다 · 「표 복사」에 **어느 파일을 들었는지**까지 적힌다.
⚠이 두 줄은 **배포판이 아니다** — 씨앗(해시)과 07-30 판 코드가 달라 배포 `village_day_ari` 와 다른 연주다. 뜻은 "sigim 하나의 몫"뿐이다.
브라우저 실측(Playwright · 헤드리스 Chromium): 행 **17**(곡 13 · A/B 4) · .ogg 두 장 ○ · 켬 칸에 끔 파일 → **✗ 다른 파일** · 재생·맞춤 이득·표 복사 줄 · 오류 0. ⚠이 Chromium 빌드는 AAC 를 못 푼다(.m4a → `EncodingError` 를 칸에 적는다 · Safari·Chrome 은 푼다).

### ⓒ-2 ⓒ `janggu_gung: '합성'` 칸 — 실측

| | 실측 |
|---|---|
| 뱅크에 궁편 실음원 | **○ 6조각** — 덩(양손) 강·중·약 · 쿵(지속음) 강·중·약 (`장구궁_janggu_gung_{deong,kung}_{loud,mid,qui}`) + 채편 12 |
| `village_day_trad` | 궁편 **84타**(지속음 63 · 덩 21) · 채편 84(겹채 42 · 굴림 42) — 샘플 |
| `village_day_jeongak` | 궁편 **84타** — 조각 셋(쿵 중 50 · 덩 강 21 · 쿵 강 13) · 채편 0(정악 장단은 궁편만) — 샘플 |
| 증명의 세기 | 두 곡 다 **재굽기가 배포 파일과 바이트 동일**이다 — 샘플로 구운 것이 배포다 |
| '합성' 이었다면 | 장구 뱅크를 빼고 `install` 하면 `source` 는 '합성' 을 적고 `compose.play_jangdan` 이 **TypeError**(`art`) — 그 두 곡은 합성 장구로 **안 구워진다**(실행) |
| 11곡 | `render2.ROOTS` 에 `samples_janggu` 가 있다(샘플 의도) — 재현 불가라 **못 잰다** |

⇒ **`janggu_gung: '합성'` 은 배포 2곡에 대해 틀렸다.** 같은 까닭으로 `geomungo`·`danso`·`piri`·`janggu_chae` 의 '합성', `daegeum` 의 "12곡에서는 합성" 도 그 두 곡에 틀렸다(trad 대금 79 · 거문고 36 · 단소 11 · 피리 8 — 전부 샘플). **값은 안 고쳤다**(카드 · T372 가 안 고친 그 자리) — `render-meta.json` 에 메모 `_T391` 한 칸만. `README-BGM.md` "아직 합성음인 악기" 절과 `test-audio ⑨l` 이 이 열을 맞대 놓아 **둘이 같은 판에서 같이 움직여야** 한다 ⇒ 회부.

## §ⓓ 한 줄 답

**가지 올렸다 — 기본값 복원(끔=착지 전 · 켬=GPT 판 비트 동일) · 재굽기 해시 2/13(파일 바이트 동일 · 11곡은 레포 밖 07-30 판) · .pyc 0 · A/B 행 2 · 장구 ○**

## §검증

| 하네스 | 결과 | 비고 |
|---|---|---|
| `test-audio` | **145/0** | `render-meta.json` 에 `_T391` 을 더한 뒤 다시 |
| `test-bgm-phrase` | **16/0** | |
| `test-assets-audit` | **PASS** · `--selftest` **PASS(셋 다 문다)** | ⑧ 새 절 5줄 · 착지판 대조 ⑧a ✗4 · ⑧c ✗ |
| `e2e-audio-probe` | **44/0** | |
| `test-client-globals` | **26/0** | |
| `test-harness-lint` | **61/0** | |
| `test-render-score-articulation` | **14/0**(12 → 14) | ⑪ 뒤집음 · ⑪b 끔 = 배포 규칙 · ⑪c 켬 = 계약(가짜 악기로 **불러서** 잰다) — 착지판에 들이대면 ⑪·⑪b **빨강** |
| `test-articulation-plan` · `test-daegeum-transition-bank` | 13/0 · 8/0 | |
| `test-village-day-ari-{sanjo,native-pitch,level-matched}-audition` | 13/0 · 10/0 · 8/0 | |
| `test-daegeum-entry-mode --bank <복원 jdae>` | **7/0** | 복원 뱅크로 돌렸다(뱅크 없으면 인자 오류로 멈추는 게이트) |
| `test-village-day-ari-sanjo-audition-render --bank <산조대금>` | **18/0** | ★**레포 `sampler.py`(13:01판)로 푼 조각**에서만 선다 — 배포 조각(12:22판)을 주면 `_trimmed/…scale_sus_04__001` 이 "음정 바뀜(71.15)"으로 멈춘다. GPT 감사표가 **레포 스캐너 판**에 맞춰졌다는 뜻 — §ⓐ-5 의 두 판과 같은 말 |
| `…-native-pitch-audition-render` · `…-level-matched-audition-render` | **못 돎** | `sanjo_deageum_scale_chung_34.wav` 를 요구한다 — bk_* 산조대금은 6파일이고 그 파일이 없다(GPT 가 쓴 원음 모음은 더 크다) · `render_score` 를 import 하지 않아 이번 diff 에 안 닿는다 |
| `tools/*/test_*.py` 셋 | OK | |
| `test_curves.py` | PASS | |
| 브금판 Playwright | 17행 · 로컬 두 줄 ○/✗ · 오류 0 | |
| 3시드 800일 A/B | **해당 없음** — diff 가 러너 적재 목록에 안 닿는다(공통 §2 ①) | |

접점 심볼 → 만진 파일: `render_score|render_jeongak` → 두 파일 · `sampler.py|score_village_day` → **무변** · `__pycache__|.gitignore|test-assets-audit` → 셋 · `sigim|village_day_ari` → `bgm-board.html`(로컬 두 줄) · `janggu_gung` → `render-meta.json` 메모 한 칸 · `bk_|_bgm_bank` → 레포 밖(맥 `~/Mini/_bgm_bank/`) · `village_day_jeongak` → 무변(바이트 동일) · `t386-bgm-hash` → 무변(러너 밖 · 이번 카드는 Python 굽기라 안 불렀다).

## §재현 — 배포 2곡을 바이트까지

```
# 1) 뱅크: bk_* 12 → samples_* (jdae 는 aa+ab 를 이어 푼다)
# 2) 조각: samples_{gaya,daegeum,piri,danso,geomungo,janggu} 를 12:22판 sampler.py(코드백업_최신.zip · split_notes max_len=4.0)로
#          `python3 sampler.py scan samples_X`  — 레포 sampler.py(max_len=9.0)로 풀면 다른 조각이다
# 3) 환경: numpy 2.3 이상(AVX-512 CPU) · scipy 1.15~1.17 아무거나
# 4) 굽기: python3 render_score.py      → out_samples/village_day_score.wav   (= village_day_trad)
#          python3 render_jeongak.py    → out_samples/village_day_jeongak.wav
# 5) 싸기: ffmpeg -i X.wav -c:a libvorbis -q:a 4 X.ogg   (Ogg 일련번호는 난수 — 배포 값 441839687 · 577416829)
#          ffmpeg -i X.wav -c:a aac -b:a 128k X.m4a
```

## §회부

* **#70 무표기 재발음 규칙**(재민) — 켜려면 `render_score.py` 의 `ARTICULATION_CONTRACT = False` 한 낱말. 켜면 배포 두 곡이 **다른 소리**가 된다(§ⓐ-3 — 다시 구워 올리는 카드가 따로 필요하다).
* **#66 청취**(재민) — 브금판 「sigim 켬/끔」 두 줄(파일 `~/Mini/_bgm_ab/T391/`) + 기존 「legacy/phrase」.
* **`render-meta.json.source` 열 고치기**(PM) — 2곡에 대해 틀렸다는 것이 **바이트 동일로 증명**됐다. `README-BGM.md` 절·`test-audio ⑨l` 과 한 판에서 같이(§ⓒ-2).
* **README "다시 만들려면" 세 줄**(PM) — TypeError 로 죽고 배포 경로도 아니다. 제대로 된 레시피는 위 §재현(2곡) — 11곡은 레시피가 없다.
* **11곡**(PM·재민) — 07-30 판 코드·씨앗·뱅크 넷이 없다. 되살리려면 ① `tracks2.py` 계보를 레포에 올리고 씨앗을 고정하는 **새 굽기**(= 새 소리 · 재민 청취)거나 ② 배포 파일을 **정본 그 자체**로 두는 것(지금 사실상 그렇다).
* **조각 스캐너의 두 판**(PM) — 레포 `sampler.py` 는 `max_len=9.0`, 배포 조각은 4.0. 뱅크를 다시 풀 카드가 오면 이 차이부터(§ⓐ-5).
* 설계 11장 판정 · 체크포인트 라이선스 · `e2e-bgm-phrase` ⑦⑧ 허용치 · AI Hub·4060(#64 ②③) — 카드대로 회부만.

★배운 것 하나 — **"못 잰다"를 두 번 적은 자리에서 둘 다 틀렸다.** T353 은 "뱅크가 없다"(있었다 · T386 이 찾음), 그리고 "`sigim` 은 13곡 중 0곡"(13곡) — 둘 다 **README 가 말한 경로를 배포 경로로 믿은** 탓이다. 이번엔 길이를 **표본 단위로** 대 보는 것 하나가 경로를 가려 줬다. 경로를 가정하지 말고 **산출물에서 거꾸로 짚는다.**
