# 보고/야간러너 2026-09-07 — 전수 125/10

**기준 커밋** `718b4924` (보고/T145 + 인계 R1 다섯 절 · 2026-09-06 17:50 UTC)
**시작** 2026-09-07 04:10 KST · **종료** 10:18 KST (단독 재실행 포함 · 약 6시간)
**총 종수** 135 (`grep -l '^// @regress' scripts/test-*.js scripts/e2e-*.js`)
**통과 125 · 실패 10** — 단독 재실행 뒤 **진짜 남은 빨강은 5종**(상시 3 · 회부 1 · 순서 의존 1)

> 컨테이너 새 클론(`/tmp/minirepo`)·순차 단독·2코어·병렬 0.
> 러너 자가 검사 통과 후 시작(`--selftest`).

---

## 1. 실패 10종 · 단독 재실행 · 귀속

| 하네스 | 전수 결과 | 단독 재실행 | 귀속 |
|---|---|---|---|
| `test-tick-slicer` ⑥a·⑥·⑧ | 빨강 | **빨강 유지**(같은 셋) | **상시(기계 의존)** — 루프 막힘 6,119ms(2코어) · ⑧ 2.05% (문턱 2%) |
| `e2e-rtt` ③×2 | **크래시**(씨앗 DB 없음) | 6/2 — 루프 막힘 판정 둘 | **상시(기계 의존)** + ★**순서 의존 결함**(아래 §2) |
| `test-body` ⑯㉧×2 | 304/2 | `TIDE_FREEZE_MS=372603` → **306/0 초록** | **상시(물때)** — 공통.md 대로 |
| `e2e-cold` ⑦㉣ | 51/1 | 빨강 유지 · **freeze 를 걸면 오히려 5건 더 빨강** | **상시(물때)** — ★손잡이가 이 하네스엔 안 듣는다(§2) |
| `e2e-salt` ③ | 31/1 | 31/1 (freeze 에서도 **동일**) | **회부 D-1** — `brine` 이 클라 `ITEM_LABEL` 에 없어 영문 키로 뜬다. 물때 무관 · 귀속 카드 없음(미착수 회부) |
| `e2e-thirst` ① | 20/1 | **21/0 초록** | 픽스처 흔들림 → **T140**(진행중 · 세션4 · 픽스처 시계 얼림이 이 카드다) |
| `e2e-winter` ⑥-0 | 28/1 | **29/0 초록** | 러너 연속 실행 흔들림(전제 절 · 곳간 채우기) |
| `test-guest-rejoin` ② | 25/1 | **26/0 초록** | 러너 연속 실행 흔들림(위치 보존 256px) |
| `test-handoff-body` ③×2 | 19/2 | **21/0 초록** | 러너 연속 실행 흔들림(central 행 폴백) |
| `test-route-persist` | **크래시**(씨앗 DB 없음) | **52/0 초록** | ★**순서 의존 결함**(§2) — 회귀 아님 |

**상시 목록 밖 빨강은 하나** — `e2e-salt ③`. 그리고 그것도 새 회귀가 아니라
`인계/회부.md` 가 오래 들고 있는 **D-1(`brine`·`item_salt_kiln` 클라 라벨 없음)** 이다.
**밤새 착지한 카드에 귀속되는 진짜 회귀는 0건이다.**

## 2. 이번 판이 새로 드러낸 것 둘 (둘 다 하네스 쪽 · 제품 아님)

**ⓐ 씨앗 DB 순서 의존 — 알파벳 전수에서 두 종이 반드시 크래시한다.**
`e2e-rtt` 와 `test-route-persist` 는 `test-tick-slicer` 가 만드는 씨앗 DB를 먼저 요구한다
(`✗ 씨앗 DB 가 없다 — node scripts/test-tick-slicer.js 를 먼저 한 번 돌려라`).
러너 목록은 `LC_ALL=C sort` 라 `test-tick-slicer` 가 **둘 다보다 뒤**에 온다 ⇒ 전수를 돌릴 때마다
이 둘은 **자동으로 빨강**이고, 그건 회귀가 아니라 순서다. 단독 재실행에서 `test-route-persist` 는
52/0 으로 즉시 초록이었다. ⇒ 러너가 씨앗 종속을 알게 하거나(선행 표식), 두 하네스가 씨앗을
스스로 만들게 해야 한다. **하네스 카드 한 장.**

**ⓑ `TIDE_FREEZE_MS=372603` 은 `e2e-cold` 에 안 듣는다.**
공통 처방대로 걸었더니 ⑦㉣ 대신 `①·②·④…` 다섯 절이 새로 빨개졌다
(`한겨울 밤이 혹한급이다 0.5634` · `모닥불이 정본 배치 경로로 놓였다 → "이미 0F에 건축물이 있습니다"`).
같은 손잡이가 `test-body` 는 306/0 으로 완전히 고친다. ⇒ **얼린 시각이 겨울밤 픽스처와 충돌한다** —
`e2e-cold` 용 값은 따로 잡아야 한다. T140 이 여는 문 안이다(§3).

## 3. 아침에 세션으로 넘길 빨강

* **T140 (세션4 · 진행중)** — 「야간 전수에서 `e2e-thirst ①` 이 또 빨갰다(추위 0.8647 "혹한" — T131 보고 §4 의 순정 클론 값과 **같은 수**). 단독 재실행은 21/0 초록이다. 이 카드가 얼리려는 그 픽스처 시계가 맞다. 겸사겸사 `e2e-cold` 도 봐 달라 — `TIDE_FREEZE_MS=372603` 을 걸면 ⑦㉣ 대신 ①·②·④ 다섯이 빨개진다(얼린 시각이 겨울밤 픽스처와 충돌). `e2e-cold` 전용 값이 필요하다.」
* **PM (하네스 카드 신설 후보)** — 「전수 알파벳 순서에서 `e2e-rtt`·`test-route-persist` 두 종이 **씨앗 DB 부재로 반드시 크래시**한다(`test-tick-slicer` 가 뒤에 온다). 단독은 초록. 러너에 선행 표식을 두거나 두 하네스가 씨앗을 스스로 만들게 해야 한다.」
* **회부 D-1 (귀속 카드 없음 · PM 배정거리)** — 「`e2e-salt ③` 이 `brine` 을 영문 키로 본다. 회부.md 849행 `brine: '짠물'` · `item_salt_kiln: '소금가마'` + `ICON_NO_RENDER` 두 키 — 클라 세 줄이다. T38 과 같은 족(회부 0-염).」
* **상시 셋은 넘기지 않는다** — `test-tick-slicer`(기계) · `e2e-rtt`(기계) · `test-body ⑯㉧`·`e2e-cold ⑦㉣`(물때). 규약대로 표기만 한다.

## 4. 규약 확인

* 코드 무접촉 — 이 커밋은 **이 문서 하나**다.
* `e2e-charsprite` 는 **상시 빨강 목록 밖**(공통.md 족보 (125))이고 이번에도 **90/0 초록**이다.
* 러너 두 개 띄우지 않음 · `pkill -f` 미사용 · 병렬 0.
