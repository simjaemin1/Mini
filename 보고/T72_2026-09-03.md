# 보고 — T72 아이콘 1차: 도구 일곱 · 손에 드는 것 여섯 (2026-09-03 · 세션8 · 영역 ART)

**커밋 `<푸시 뒤 기입>`** · 베이스 **`684dd8bc`** 위 fast-forward(force push 0회 · `batch/icons1-0903` 먼저 → ff 재확인 → main).
⚠**리베이스 1회** — 작업 중 origin/main 이 `1e5663fd` → `684dd8bc` 로 **5커밋** 움직였다(T64 여름 · T70 렌더 위생 · T59 식량 열량).
겹친 파일은 **`인계/회부.md` 하나**(양쪽이 맨 위에 절을 더한 **더하기 충돌** — 둘 다 남겼다 · 최신이 위).
**코드 겹침 0** ⇒ 재실행한 것은 내 하네스뿐이고 전부 그대로 통과했다(`test-icons` PASS · `test-props` PASS).
**게임 코드 diff 0** — 서버 `0`줄 · econ `0`줄 · 클라 `0`줄. 만진 것은 `scripts/props_render.py`(모델 표 추가) ·
`scripts/test-icons.js`(신규) · `public/assets/icons/*.png` 13장 · 문서 셋.

---

## §0 실측 (셋) — **둘이 지시서와 달랐다**

### ⓐ 도구 아이콘은 **품목당 하나**다 (재료당이 아니다)

| | 실측 |
|---|---|
| `RECIPES.axe / pickaxe / sword`(`zone.js:877~`) | 재료가 **고정**이다 — `axe {wood 5, stone 2}` · `pickaxe {wood 3, stone 5}` · `sword {wood 2, stone 8}`. 등급이 없다 ⇒ **키 하나 = 그림 하나** |
| `EQUIPMENT_RECIPES.tool / weapon` | 여기가 재료로 갈리는 층이다(`stone·bronze·copper·iron·meteoric_iron·wood·bone`). 그런데 그리기는 `EQUIP_ICONS[inst.type]`(`45-t-market.js:85`) — **타입당 하나**(`tool: '🔧'`)라 **재료를 이미 안 본다** |
| 결론 | 지시서의 대비대로 **품목당 하나로 굽고 재료별은 회부**(§4-1) |

⚠**`sword` 는 청동검이 아니라 돌검이다.** 지시서는 *"`sword` 는 청동검"* 이라 했는데
레시피가 `wood 2 + stone 8` 이라 **정본이 돌이라고 말한다**. 그리고 마제석검(간 돌검)은
송국리 문화기의 **표지 유물**이라 고증도 같은 답을 낸다 ⇒ **마제석검**으로 구웠다(등날·나뭇잎꼴 검신).

### ⓑ 지게 — 아이콘만이 맞다

`EQUIPMENT_RECIPES.carrier`(`zone.js:1040`)는 슬롯을 `Carry.CARRIER_SLOT` 에서 받는 **장비 인스턴스**다.
등에 진 모습은 **캐릭터 시트 규약**(레이어 홀드아웃)이라 이 카드가 만들 물건이 아니다 ⇒ 회부(§4-2).
⚠그리고 `carrier` 는 `EQUIP_ICONS` 에도 없다 — 지금 화면엔 **기본 🎒** 가 뜬다. 배선은 §3 회부에 함께.

### ⓒ **상대 크기 규약은 없다** — 지시서 전제가 틀렸다

지시서: *"아이콘 크기 문법(96px 안에서 큰 것/작은 것의 상대 크기 — T67 이 `icons-postprocess.js` 에 둔 규약)"*.
실측: **T67 은 그 파일을 한 줄도 안 건드렸다**(`git log` 확인). 그리고 그 파일은 예나 지금이나
`scale = 96 / max(bb.w, bb.h)` — **모든 아이콘이 96px 를 꽉 채운다.** 자갈 한 줌과 지게가 같은 크기다.

⇒ 규약을 새로 만들지 않았다(키마다 배율 표가 필요하고 **기존 36종을 전부 다시 구워야** 한다).
대신 기존 36종이 이미 쓰는 문법을 따랐다: **작은 것은 크기가 아니라 개수와 그릇으로 말한다** —
소금은 토기 접시 위 결정 무더기, 자갈·잔가지는 한 줌·한 단(`stone`·`ore_chunk`·`gold` 가 그 선례다).
규약 자체는 `인계/ART-자산.md` §9 에 적었고, 바꾸려면 별도 카드다(회부 §4-6).

무게(정본 `server/weights.js kgOf`): 생선 0.90 · 구운생선 0.55 · 소금 1.00 · 짠물 1.00(=`salt.CFG.BRINE_KG`) ·
잔가지 0.40 · 자갈 0.60 · 도끼·곡괭이 1.20 · 검 1.50 · 조잡한 셋 1.40/1.50/1.10 · 지게 3.00.

---

## 1. 만든 것

`scripts/props_render.py` 에 **`ITEMS` 표**를 더했다 — 파일을 새로 파지 않았다.
새로 파면 씬·재질·헬퍼가 **두 벌**이 되고, 그게 이 레포가 반복해서 데인 자리다(사본 금지).
구조는 T67 그대로라, 나중에 손에 들리거나 바닥에 떨어질 때 `world=[…]` 만 붙이면 **같은 모델**에서 세계 렌더가 나온다.

| 키 | 무엇 | 고증 |
|---|---|---|
| `crude_axe` `crude_pick` `crude_blade` | 조잡한 석기 셋 | **뗀석기 급조** — 자갈을 깨 날을 세워 갈라진 잔가지에 풀로 동여맸다(`RECIPES` 의 자갈·잔가지·풀 그대로) |
| `axe` `pickaxe` | 도끼 · 곡괭이 | **간석기** — 갈아 낸 매끈한 날 + 다듬은 자루 + 새끼 결속 |
| `sword` | 검 | **마제석검** — 나뭇잎꼴 검신 + 등날(척) + 자루 |
| `carrier` | 지게 | 가지 둘을 A 자로 세우고 세장 셋을 지르고 밀삐(짚 끈)를 걸었다 |
| `fish` / `fish_cooked` | 생선 / 구운 생선 | **같은 모델**(`_fish(cooked)`) — 날것은 어두운 등·밝은 배, 구운 것은 그을음과 꼬치 |
| `salt` | 소금 | 자염으로 졸인 굵은 결정 무더기(토기 접시 위) |
| `brine` | 짠물 | **물병과 같은 물건** — 서버가 갯벌에서 `water_bottle` 을 `brine` 으로 바꾸고 가마가 되돌린다. 표주박 병에 함수가 차 있고 아가리에 소금이 앉았다 |
| `twig` `pebble` | 잔가지 · 자갈 | 불쏘시개 한 단 · 냇돌 한 줌(조잡한 석기의 재료 둘) |

`ico()` 에 **`smooth` 인자를 열었다**(생선·표주박은 둥근 몸이라 스무스라야 한다).
기본값이 종전과 같아 **T67 가구 14장은 한 픽셀도 안 바뀐다**(다시 굽지 않았다 — `SKIP_PROPS=1`).

**굽는 시간: 13장 43초**(2코어 · SAMPLES 64 · 512² → 96px). 일부만: `ITEMS_ONLY=fish,brine` 로 9초.

---

## 2. 육안 3패스 — 96px 에서 무엇이 안 읽혔나

| 패스 | 무엇이 틀렸나 | 고친 것 |
|---|---|---|
| 1 | **도끼가 삽으로 읽혔다** — 날이 넓고 납작하고 자루 **가운데**에 붙어 있었다 | 날을 자루 **끝**으로 옮기고 **쐐기**(등이 두껍고 인부가 얇다)로 · 자루보다 작게 |
| 1 | **조잡한 돌도끼가 곤봉으로** 읽혔다(같은 이유) | 갈라진 가지 끝에 날을 물리고 결속을 그 자리로 |
| 1 | 도끼와 괭이의 **실루엣이 겹쳤다** | 괭이 날을 자루와 **직각**으로 아래를 향하게(찍는 자세) |
| 1 | 조잡한 돌칼이 **창끝**으로 읽혔다 | 날을 줄이고 자루를 절반 이상으로 |
| 2 | **생선이 흰 비행선**이었다(은백 비늘) | 비늘을 어두운 청올리브로 · **어두운 등**(카운터셰이딩) · 눈알 · 아가미뚜껑 |
| 2 | **짠물이 그냥 박**이었다(마개가 속을 가렸다) | 마개를 빼고 아가리를 넓혀 **함수 수면**을 드러내고, 흘러내린 자국과 앉은 소금을 더했다 |
| 2 | **자갈이 소금과 헷갈렸다**(각지고 희었다) | 냇돌로 — 둥글게(스무스) 어둡게(갈빛) |
| 3 | 함수 수면이 **쇠뚜껑**으로 읽혔다(밝은 회색 평면) | 짠물을 어둡고 젖은 색으로 · 소금 왕관을 낮게 |

★**26px(실제 인벤 행 크기)에서 13종이 서로 구별되는지**를 마지막에 따로 확인했다 —
96px 에서 예쁜 것과 26px 에서 읽히는 것은 다른 명제다(대조표 셋째 열).

---

## 3. 클라 배선은 **회부했다** — 카드가 시킨 대로, 그리고 예상보다 크다

카드 §1: *"`ICON_NO_RENDER` 거부 목록에서 13키 제거만 — T66 세션4 가 `43-i-icon.js` 를 만지는 중이면
그 한 줄은 회부하고 T66 뒤에 · §0 에서 확인"*.

**§0 확인 결과 T66 은 아직 안 내려왔다**(`origin/main` 에 T66 커밋 없음 · `43-i-icon.js` 최근 변경은 T61) ⇒ **회부**.

그리고 **한 줄이 아니다**(실측):

```
  ICON_NO_RENDER 에 있는 내 키 :  salt 하나뿐
  ITEM_ICONS 에 아예 없는 내 키 :  나머지 12개 전부
```
`99-main.js:386` 의 적재기는 `Object.keys(ITEM_ICONS)` 를 돌므로, **표에 키가 없으면 파일이 있어도 안 부른다.**
⇒ 필요한 것은 **두 줄**이다: ⓐ `ITEM_ICONS` 에 12키 추가 ⓑ `ICON_NO_RENDER` 에서 `salt` 제거.
(+ 지게는 `45-t-market.js` 의 `EQUIP_ICONS` 에도 `carrier` 가 없다 — 그 줄까지 셋.)

⚠**404 는 안 난다**: 파일을 더했을 뿐 요청 키를 안 늘렸다. `test-icons ④` 가 그것을 소스 층에서 검사하고,
`e2e-nature`("자산 요청 404 없음")가 실화면에서 다시 검사한다. **T67 과 같은 자리를 두 번째로 밟았다** —
`인계/회부.md 0-가구 1` 과 합쳐 한 곳에 모아 두었다(0-아이콘).

---

## 4. 회부 — `인계/회부.md` `0-아이콘`

1. **[I 인벤 / T66]** 위 세 줄(ITEM_ICONS 12키 · ICON_NO_RENDER salt · EQUIP_ICONS carrier).
2. **[ART / T65 계보]** 지게 **등짐 스프라이트**(캐릭터 시트 홀드아웃 규약).
3. **[ART]** **재료별 도구 아이콘** — 지금은 품목당 하나다. `EQUIP_ICONS` 가 타입당 하나라 화면이 이미 재료를 안 본다;
   재료를 보이게 하려면 **그리는 쪽 규약부터** 정해야 한다(설계 판정).
4. **[ART]** 옛 `item_*` 셋(계단·바닥·농지) — 세계 스프라이트가 없어 아직 `icon_render.py` 에 남아 있다.
5. **[ART 다음 카드]** 보존식 6 · 갯벌 3+민물 · 어종 8(T73) → 작물 34+씨앗 34(`crop_render.py` 와 짝 · T74).
6. **[ART / 설계]** **아이콘 상대 크기 규약**(§0-ⓒ) — 만들려면 키마다 배율 표 + 기존 36종 재굽기. 별도 카드.

---

## 5. 검증 — 개정된 §2 규약대로(러너 전수는 PM 야간 1회)

접점 파일: `scripts/props_render.py` · `public/assets/icons/**` · `scripts/test-icons.js`. 기계로 뽑은 목록:

```
$ grep -l "props_render" scripts/test-*.js scripts/e2e-*.js
    scripts/test-icons.js  scripts/test-props.js
$ grep -l "assets/icons" scripts/test-*.js scripts/e2e-*.js
    scripts/e2e-metallurgy.js
```
★아이템 **키**(`axe`·`fish` …)를 부르는 하네스 30여 종은 이 목록에 넣지 않았다 —
그것들은 키를 **게임 데이터**로 쓰고, 이 배치는 **게임 코드를 한 줄도 안 만졌다**(diff 0).
넣었어야 할 자리는 T67 처럼 클라 표를 만졌을 때이고, 이번엔 그 표를 회부했다.

| 하네스 | 결과 |
|---|---|
| **`test-icons`(신규 · `@regress`)** | **PASS** — 13키 파일·96×96 · 알파 살아 있음 · 서버 품목 키(`weights.kgOf`) · 요청 36키 404 0 · 배선 표기 |
| `test-icons --selftest` | **PASS(오염을 잡았다)** — 투명 화소 0(꽉 찬 사각형)을 ②가 잡는다 |
| `test-props` | **PASS** — T67 가구 14장 앵커·치수 불변(다시 굽지 않았다) |
| `e2e-inv` | **94 / 0** ✅ (아이콘 로드 수 불변 — 요청 키를 안 늘렸으니 그게 맞다) |
| `e2e-nature` | **81 / 0** ✅ (자산 요청 404 없음) — ★1회차가 빨갰다. 아래 §5-★ |
| `e2e-metallurgy` | **20 / 0** ✅ (아이콘을 부르는 유일한 실클라 하네스) |
| **랩 모듈 적재 추적** | 랩이 무는 저장소 파일 **18개**(T67 과 같은 목록). 내 diff 에 `server/`·`sim/` 파일이 **0개**라 교집합은 **구조적으로 ∅** |

### ★`e2e-nature` 1회차 빨강 — **내 것이 아니었다**(귀속)

1회차: `9 통과 / 3 실패 ❌ (렌더 패스 사망)` · 콘솔에 `PAGEERROR ReferenceError: onbLobbyInit is not defined`.
`onbLobbyInit` 은 `70-lobby.js` 의 **최상위 함수 선언**이고 `30-n-net.js boot()` 이 런타임에 부른다 —
스크립트가 순서대로 다 실행된 뒤라 정상 경로에선 있을 수 없는 오류다. ⇒ **그 조각이 아예 안 실려 왔다**(정적 파일 전송 실패).

귀속(족보 ㊾):

| 판 | 결과 |
|---|---|
| **[A] 같은 트리 재실행** | **81 / 0 ✅** — 재현 안 됨 |
| **[B] 깨끗한 `origin/main` 워크트리**(내 13장 없음) | **81 / 0 ✅** — 베이스도 같은 수. 내 델타 0 |

⇒ **흔들림이다.** 그리고 원인이 짚인다: 내가 급조한 순차 스크립트가 하네스 사이 배수(drain)를 **6초**로 줬는데
정본 러너(`run-regress.sh`)는 **8초**다. 그 러너가 존재하는 이유가 바로 이것이다 —
*"앞 하네스 서버가 안 죽은 채 다음이 뜨면 … 그게 '실패'로 보여 **없는 회귀를 보고**하게 된다."*
**손으로 짠 러너가 세 번째로 같은 자리를 밟았다.** ⇒ 급조 순차 스크립트도 `DRAIN_SEC` 을 러너와 같게 하거나,
아예 `bash scripts/run-regress.sh <하네스…>` 로 부르는 게 맞다(§7 족보 제안).

## 6. 대조표

**`~/Mini/산그림/디자인B/아이콘1차.png`** — 전(이모지 · 대부분 **기본 📦**) / 후 96px / **실제 인벤 26px** /
이름 · 키 · 재료·출처 · 무게. 13행.


---

## 7. 족보 제안

* **(97) ★급조 러너는 정본 러너의 배수 시간을 물려받아라 — 안 그러면 없는 회귀를 본다.**
  `run-regress.sh` 의 머리글이 이미 그 사고를 두 번 적어 뒀는데(파이프 종료코드·고정 포트),
  T72 가 **세 번째**를 더했다: 배수 6초로 돌린 `e2e-nature` 가 `70-lobby.js` 를 못 받아
  "렌더 패스 사망"이라는 **없는 결함**을 냈다(같은 트리 재실행 81/0). 하네스 몇 개만 골라 돌릴 때도
  `bash scripts/run-regress.sh <하네스…>` 를 쓰거나 `DRAIN_SEC` 을 맞춰라.
* **(98) ★아이콘을 굽는 것과 아이콘이 화면에 닿는 것은 다른 명제다** — 족보 (83) 의 자산판.
  `/assets/icons/<key>.png` 는 **파일명 규약처럼 보이지만** 실제 적재기는 `Object.keys(ITEM_ICONS)` 를 돌고
  `ICON_NO_RENDER` 로 한 번 더 거른다. **표에 키가 없으면 파일이 있어도 안 부른다.**
  T67·T72 두 카드가 같은 자리에서 배선을 회부했다 — 자산 카드의 §0 은 **적재기부터** 읽어야 한다.
