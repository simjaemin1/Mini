# 보고 — T178: 심은 나무도 종이 있다 (`sp` 하나로) · `test-regrow ⑤` 귀속

* **베이스** `14582f59`(origin/main · 리베이스 후 · 카드 발행 시점은 `a638202d`) · **헤드** `7b52784e` · 가지 `batch/planted-sp-0912`
* 접촉 파일 **셋**: `server/zone.js`(+7 −2) · `scripts/test-regrow.js`(+21 −3) · `scripts/e2e-nature.js`(+94 −2). **클라 0줄**.
* 새 필드 0 · 새 수 0 · 주사위 0 · 새 표 0(DB 마이그레이션 0) · econ 랩 적재 교집합 **∅**

---

## §0 실측 (셋)

### ⓐ `species` 를 쓰는 자리 전수 — **읽는 곳이 없었다**

| 자리 | 무엇 | 판정 |
|---|---|---|
| `server/zone-local-db.js:95,98,106` | DB **열** `species`(스키마 · `insertResource` 인자) | **그대로 둔다** — 열 이름은 스키마다(새 표 0) |
| `server/zone.js:6933,6975,6979` | `tryPlantTree` 의 지역변수 → DB 인자 `species` | **그대로** — DB 열에 주는 값이다 |
| **`server/zone.js:1389`** | `_liveResourceRow` 가 개체 행에 찍는 **필드** `r.species` | ★**여기가 유일한 갈림** → `r.sp` |
| `scripts/test-regrow.js:360,453,465,516` | DB 행의 `species` **열**을 본다 | 그대로 유효(열은 안 바뀐다) |
| `server/chunk.js:213,221` | `regrowStageOf(type, days, species)` **인자 이름** | 그대로(필드가 아니다) |
| 클라 `34-m-renderloop.js:1514,1517` · `40-r2-sprites.js` | 전부 **`sp`** 를 읽는다 | 무접촉 |

⇒ **`r.species` 를 읽는 곳이 한 군데도 없었다.** 심은 나무는 종을 들고서도
종별 그림(T148-B)도 열매 비트(T170)도 영영 못 받았다 — 값이 아무도 안 보는 칸에 앉아 있었다.
고친 것은 **한 낱말**이고, 나머지 표는 전부 "바꾸지 않는다"가 답이다(새 이름 0 · 마이그레이션 0).

### ⓑ `test-regrow ⑤` 가 보는 것 — **소스 문자열**이었다(회귀가 아니다)

| 물음 | 실측 |
|---|---|
| 빨간 줄 | `ok(/assets\/trees\/stump01\.png/.test(sp) && /assets\/trees\/sap_/.test(sp), …)` — `40-r2-sprites.js` **소스에 그 문자열이 있는가** |
| 왜 사라졌나 | T148-B 가 `SAP_SPRITES` 여덟 줄(표의 사본)을 지우고 경로를 `'/assets/trees/' + 표가 준 이름 + '.png'` 로 **조립**하게 했다 |
| 묘목 그림은 뜨나 | **뜬다.** `drawSaplingIso` → `TREE_SPECIES.byId[sp].sapling` → `_sprite()` → `/assets/trees/sap_oak.png`. 화면이 실제로 받아 갔다: **8/8장**(`e2e-nature` 심기⑤ · 네트워크가 증인) |
| 표가 댄 그림은 실재하나 | **8/8 실재**(`test-regrow ⑤` 새 판정) |

⇒ **후자**다 — 하네스가 낡은 소스 계약을 봤다. 그래서 판정을 **정본에게 묻게** 갈았고
(표가 이름을 대고, 클라는 경로만 조립하는지 · 표가 댄 그림이 전부 실재하는지 · 종 전수),
"그린 판이 실제로 그 파일인가"는 **화면에게 묻는 자리**(`e2e-nature`)로 옮겼다.
`test-regrow` **95건 중 94/1 → 97건 중 97/0**.

### ⓒ 심기 경로의 종 결정 — **씨앗이 정한다**(자리가 아니다)

`PLANT_SEEDS = { acorn: 'oak' }` → `tryPlantTree` 가 그 값을 DB 열에 적는다.
`Trees.speciesAt`(야생 종축 = 자리의 함수)은 **안 부른다 — 그게 맞다**: 도토리를 심으면 참나무가 난다.
주사위 0 은 "씨앗 → 종" 이라는 표 하나로 지켜진다(자리로 물으면 심은 게 참나무가 아닐 수 있다).
⚠지금 심을 수 있는 씨앗은 **도토리 하나**다 ⇒ 지시서의 "심은 밤나무"는 실기로 불가능하고,
검증은 **참나무**로 했다(참나무도 열매종 · `fruitSeasonOf('oak') = autumn` · 열매판 `tree06_a`). 씨앗 표 확장은 T124 가 이미 회부해 둔 것이다.

---

## §1 만든 것 — 서버 **두 줄**

```
server/zone.js  _liveResourceRow   r.species → r.sp        (개체 행의 필드 하나 · DB 열 무변)
                _shapePlantedAll   묘목→성목 그 순간 _fruitStamp(r)  ("볼 때"가 여기다 · 같은 행에 같이 실린다)
```

| 약속 | 지켰나 |
|---|---|
| 새 필드 0 | ○ — `sp` 는 T135 가 만든 그 필드다. 이름을 **하나로 줄였다** |
| 새 수 0 · 주사위 0 | ○ — 종은 씨앗 표, 햇수는 T122 정본(`TREE_FULL_Y`·`TREE_STUMP_Y`) |
| 새 표 0 | ○ — DB 열 `species` 그대로(마이그레이션 0 · `test-regrow ⑦ⓕ4` 그대로 초록) |
| 클라 0 | ○ — `public/` 접촉 0(클라는 진작부터 `sp` 를 읽었다) |
| T182·T175 무접촉 | ○ |
| econ 무접촉 | ○ — 랩 적재 17 ∩ 내 diff 3 = **∅** |

---

## §2 검증 — `e2e-nature` **125 / 0** (116 → 125 · 심기 절 9건 신설)

| 검사 | 실측 |
|---|---|
| 심기① | 도토리를 심었다 — `r1` (54864,54800) |
| 심기② | 그 묘목이 **종을 들고 온다** — `sp=oak`(씨앗 `acorn` → 참나무 · 자리 해시가 아니다) · `isSeed=false` |
| 심기③ | **자란다** — day 955 → 12270(성목까지 **30년** = `TREE_FULL_Y−TREE_STUMP_Y` · 정본) · 자란 뒤에도 `sp=oak` |
| 심기④ | **심은 나무에도 열매 비트가 온다** — 가을 · `fruitNow=true`(표 `autumn`) |
| 심기⑤ | 화면이 **표가 댄 묘목 그림을 전부 받아 갔다** — **8/8장**(`sap_oak` ○ · 네트워크가 증인) |
| 심기⑥ | **종이 맞는 열매판으로 선다** — `tree06_a`(표 `oak.autumn`) · 비트 false 면 `tree06`(표 `oak.sprites`) |

### ★하네스가 두 번 틀렸다 — 실측이 갈랐다("볼 때"를 어떻게 다시 만드나)
단계 정산(`_shapePlantedAll`)은 **청크가 켜지는 순간**에만 돈다(`activateChunk` · zone.js:248).

| 시도 | 실측 | 판정 |
|---|---|---|
| ① 걸어서 청크를 넘는다 | 입력 **2,694걸음(90초)** 에 새 청크 방송 **0건**. 따로 재 보니 걸음이 **초당 7.7px**(300입력에 76.8px — 64px/s 가 아니다) | ✗ 하네스가 못 도착한다 |
| ② 선을 끊고 다시 붙는다 | 사람이 0 이면 틱이 **idle 로 조기 return**(`updateActiveChunks` **앞**이다) ⇒ 청크가 아예 안 꺼진다 | ✗ 다시 붙어도 새로 켜질 게 없다 |
| ③ **관전자의 시선** | `updateActiveChunks` 가 `observers` 를 같이 훑는다 ⇒ `?observer=1` + `viewport_update` 한 번에 **다시 봄 1회**로 자랐다 | ○ 있는 문법 · 새 손잡이 0 |

⇒ ③을 썼다. 정해진 초를 안 잔다 — 세계가 "성목"이라 말할 때까지 시선을 옮긴다(픽스처 규약 공통 §2 ⑩).

### 접점 하네스
| 하네스 | 결과 |
|---|---|
| `test-regrow` | **97 / 0** (전: 95건 중 94/**1** — ⑤ 소스 문자열) |
| `test-client-globals` | **26 / 0** (클라 무접촉 — 기준선 그대로) |
| `test-trees` | **81 / 0** |
| `test-lab-trees` | **44 / 0** |
| `test-harness-lint` | **22 / 0** — 상류 T147 빨강은 **세션7 T174 가 고쳤다**(내 T170 보고의 그 한 줄이 닫혔다) |
| `node --check` | 셋 다 통과 |

### 3시드 면제 근거 — 랩 적재 추적
```
econ 랩 적재 17파일 : econ-lab-real, chunk, era, events, fertility, livelihood, specialty, sustain,
                      terrain-gen, terrain, trade, village-layout, villages, winter, zone-config,
                      economy-sim-v2, economy-sim
내 diff 3파일       : server/zone.js · scripts/test-regrow.js · scripts/e2e-nature.js
교집합              : ∅  → 3시드 800일 A/B 를 갈음한다(공통.md §2 ①)
```

---

## §3 회부 (고치지 않았다)
1. **심을 수 있는 씨앗이 하나**(`PLANT_SEEDS = {acorn:'oak'}`) — 종 카탈로그가 여덟인데 심기는 참나무뿐이다. 표 한 줄씩이면 되지만 **아이템(씨앗) 쪽 표**가 같이 따라온다. → 카드 하나
2. **심은 나무는 종별 성장 햇수를 안 쓴다** — `_shapeRegrown` 이 `regrowStageOf('tree', elapsed)` 를 **종 없이** 부른다(시더 쪽은 `_spAt` 을 넘긴다). `Trees.stageYearsOf` 가 종별 햇수를 아는데 심은 것만 못 본다. 고치면 **성숙 시점이 바뀐다** ⇒ 손잡이 튜닝이라 이 카드에서 안 했다. → 재민 판정
3. **플레이어 걸음이 초당 7.7px** — 실측(입력 300개 = 76.8px · `ackSeq` 는 300 전부 먹었다). `MOVE_SPEED=64` 와 **8배 넘게** 어긋난다. 콜라이더인지 배율(`moveMultOf`)인지 못 갈랐다 — **못 쟀으면 못 쟀다**고 적는다. 실기 체감(느린 걸음)과 이어질 수 있다. → M-이동입력 영역 · 카드 하나
4. 이름표 어긋남(`koOf('chestnut')='밤'` ↔ 표 `'밤나무'` · `ITEM_LABEL_SERVER['chestnut']` 없음) — **세션8 T182**

## §4 실기 1줄 (재민)
도토리를 심고 시간이 지나면, 그 나무가 **참나무 그림**으로 서고 가을에 열매가 보이나.

## §5 푸시 — **main 까지 갔다** (정본 2판 복귀)
```
작업 클론   $HOME/pushrepo  (git clone --depth 200 --branch main --single-branch · 맥)
            ~/mnt/Mini/durango-mini 무접촉 — fetch 도 안 했다
가지        batch/planted-sp-0912  →  7b52784e   (BRANCH_PUSHED)
main        7b52784e                              (PUSHED_MAIN · ff 확인 두 번: 리베이스 직후 · 푸시 직전)
원격 main   7b52784e  (푸시 뒤 재확인)
force       0        ·   .lock  없음
번들        ~/Mini/_inbox/T178_2026-09-12.bundle (14582f59..7b52784e · 증거)
```
맥 이그레스는 09-12 에 돌아와 있었다(`github.com` → 200) — 그래서 3판이 아니라 2판으로 갔다.
