# 보고 — 야간 러너 2026-09-24 (+0900)

**기준 커밋 `dfe59628`** · 시작 **04:09** → 종료 **06:33** (2시간 24분)
· 오늘 묶음 **A** · 목록 **124종** · 돈 것 **124** · 통과 **116** · 실패 **8** · 끊김 **0** · 미측정 **0**
· 이 문서는 `scripts/nightly-report.js` 가 로그만 읽어 냈다 — **모델 판단 0** · 같은 로그면 바이트가 같다
· 로그 `/tmp/night/regress.log` · 단독 재실행 `/tmp/night/regress.log.retry` · 상시 목록 `scripts/nightly-known.txt`
· 직전 밤 `보고/야간러너_2026-09-23.md` — 빨강 5종 · "연속 n밤"은 **세기만** 한 수다(판정 0)

## ★ 한 줄 — **상시 밖 빨강 8건** — `test-downed` · `test-guest-rejoin` · `test-move-soa` · `test-site-memo` · `test-terraincache` · `test-war-world` · `e2e-guest-reconnect` · `e2e-rumor`

## 1. 실패 표

| 하네스 | 연속 | 첫 판 | 소요 | 단독 재실행 | 귀속 |
|---|---|---|---|---|---|
| `test-downed` | 연속 **1밤** | RC=1 · 결과줄 204개 · ★★③ **N초 붙들면 일어난다 — HP 는 소폭**(§12)  hp 20.39 (0.2) | 4분 | ★**단독 초록** (RC=0) | 후보 T347 (세션 표기 없음) |
| `test-guest-rejoin` | 연속 **1밤** | RC=1 · 결과줄 27개 · ★② 위치 보존 — 걷던 자리에서 이어진다(낡은 저장 좌표로 되돌아가지 않는다)  걷던 자리와 256px · 낡은 저장 좌표와 0px | 0분 | ★**단독 초록** (RC=0) | 후보 T355 · T347 (세션 표기 없음) |
| `test-move-soa` | 연속 **1밤** | RC=1 · 결과줄 82개 · ⑤ ★★**T252 자로 못 가른다** — 평균 차의 부호가 3/3 이면서 \|평균\| > 폭 인 경우가 아니다  부호일치 true · 평균 -4.25e-4 · 폭 3.84e-4 | 0분 | ★**단독 초록** (RC=0) | 후보 T370 · T356 · T347 (세션 표기 없음) |
| `test-site-memo` | 연속 **5밤** (상시 밖) | RC=1 · 결과줄 22개 · ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  16회 / 9일 | 10분 | 여전히 빨강 (RC=1 · 결과줄 22개 · ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  16회 / 9일) | 후보 T355 · T347 (세션 표기 없음) |
| `test-terraincache` | 연속 **2밤** (상시 밖) | RC=1 · 결과줄 24개 · isWaterTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개 | 0분 | 여전히 빨강 (RC=1 · 결과줄 24개 · isWaterTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개) | 후보 T347 (세션 표기 없음) |
| `test-war-world` | 연속 **1밤** | RC=1 · 결과줄 91개 · ⓛ 숲에서도 판이 끝난다(행위 정산 — 숲은 판을 늘릴 뿐)  끝 undefined · 교전 1450틱(빈 들판 972) | 4분 | ★**단독 초록** (RC=0) | 후보 T355 · T329 · T295후속 (세션 표기 없음) |
| `e2e-guest-reconnect` | 연속 **1밤** | RC=1 · 결과줄 41개 · ★★★승계해도 **같은 playerId** —  === anon_2skqB4SFgTJd (등록하는 순간 소유를 잃던 역설이 사라졌다) | 10분 | 여전히 빨강 (RC=1 · 결과줄 41개 · ★★★승계해도 **같은 playerId** —  === anon_r8EVXL14uNfg (등록하는 순간 소유를 잃던 역설이 사라졌다)) | 후보 T355 · T347 (세션 표기 없음) |
| `e2e-rumor` | 연속 **1밤** | RC=1 · 결과줄 54개 · ⑨ ★새 유형도 **소문을 타고 걸어온다**(도달표를 유형별로 나누지 않았다)  (150회 폴링 뒤에도 미도착) | 6분 | 여전히 빨강 (RC=1 · 결과줄 60개 · ⑤ 며칠 뒤 그 사건이 **B 게시판에 나타난다**(캐러밴이 걸어온 뒤에 들린다)  (120회 폴링 뒤에도 미도착)) | 후보 T355 · T347 (세션 표기 없음) |

⚠**연속 2밤 이상인데 상시 목록에 없는 것 2건** — `test-site-memo`(5밤) · `test-terraincache`(2밤). T304 규약대로 **PM 이 `nightly-known.txt` 에 올릴지** 정한다(이 기계는 세기만 한다).

## 3. 미측정 0종

없다 — 오늘 목록을 다 돌았다.

## 4. 아침에 세션으로 넘길 빨강

아래는 **상시 목록에 없는** 빨강이다 — 귀속 카드를 찾아 세션에 넘긴다(이 칸만 사람 몫이다).

* `test-downed` — RC=1 · 결과줄 204개
  * ★★③ **N초 붙들면 일어난다 — HP 는 소폭**(§12)  hp 20.39 (0.2)
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/test-downed.js` 와 그 하네스가 무는 제품 파일
* `test-guest-rejoin` — RC=1 · 결과줄 27개
  * ★② 위치 보존 — 걷던 자리에서 이어진다(낡은 저장 좌표로 되돌아가지 않는다)  걷던 자리와 256px · 낡은 저장 좌표와 0px
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/test-guest-rejoin.js` 와 그 하네스가 무는 제품 파일
* `test-move-soa` — RC=1 · 결과줄 82개
  * ⑤ ★★**T252 자로 못 가른다** — 평균 차의 부호가 3/3 이면서 \|평균\| > 폭 인 경우가 아니다  부호일치 true · 평균 -4.25e-4 · 폭 3.84e-4
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/test-move-soa.js` 와 그 하네스가 무는 제품 파일
* `test-site-memo` — RC=1 · 결과줄 22개
  * ① [상황] 종전 판이 하루 5회 이상 집터를 훑는다  16회 / 9일
  * ① [상황] 헛수고(빈손)가 실제로 있다  빈손 0
  * ① [상황] 그 헛수고가 **잴 만큼 있다** — 대조군 집터 단계가 채택본보다 무겁다(T342: 절대 문턱 폐지)  종전 51ms > 채택 73ms
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 22개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-site-memo.js` 와 그 하네스가 무는 제품 파일
* `test-terraincache` — RC=1 · 결과줄 24개
  * isWaterTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개
  * isWaterTileLocal: 두 가지에 **글자까지 같은 인자식** — 캐시가 다른 점을 묻지 않는다
  * isRockTileLocal: terrain 호출이 캐시 가지 1 + 종전 가지 1 = 2개  1개
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 24개)
  * 귀속 찾는 법: `git log -3 -- scripts/test-terraincache.js` 와 그 하네스가 무는 제품 파일
* `test-war-world` — RC=1 · 결과줄 91개
  * ⓛ 숲에서도 판이 끝난다(행위 정산 — 숲은 판을 늘릴 뿐)  끝 undefined · 교전 1450틱(빈 들판 972)
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/test-war-world.js` 와 그 하네스가 무는 제품 파일
* `e2e-guest-reconnect` — RC=1 · 결과줄 41개
  * ★★★승계해도 **같은 playerId** —  === anon_2skqB4SFgTJd (등록하는 순간 소유를 잃던 역설이 사라졌다)
  * ★승계와 동시에 브라우저의 게스트 토큰이 지워진다 — "451db5b618f85fb0bb0205b5116d25f604142a24f67c1afe85c358586d1c5840"
  * ★★비밀번호 로그인도 **같은 playerId** —
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 41개)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-guest-reconnect.js` 와 그 하네스가 무는 제품 파일
* `e2e-rumor` — RC=1 · 결과줄 54개
  * ⑨ ★새 유형도 **소문을 타고 걸어온다**(도달표를 유형별로 나누지 않았다)  (150회 폴링 뒤에도 미도착)
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 60개)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-rumor.js` 와 그 하네스가 무는 제품 파일

## 5. 통과 116종

```
test-alloy  test-armor-cap  test-assay  test-audio  test-body  test-bridge-path  test-bridge-sprite  test-building-anchor  test-calendar  test-carrier  test-charsheet  test-chron-icons  test-claim-v2  test-client-globals  test-clothes  test-craft  test-crops-world  test-crops  test-distmatrix  test-ditch  test-doors  test-econ-diet  test-econ-fieldyield  test-econ-rawgrain  test-emptystart  test-events  test-farm-life  test-farm-metrics  test-fish-act  test-fishing  test-forage-act  test-frontier-iron  test-furnace  test-granary-add  test-granary-haul  test-guest-identity  test-guild-treasury  test-hall-door-path  test-handoff-body  test-harness-lint  test-headless-parity  test-hist  test-hunt-port  test-hunt-vis  test-icons  test-imsu  test-inv-ground  test-itemlabel  test-kcal  test-lab-alloc  test-lab-happywork  test-lab-hunt  test-lab-stone  test-lab-trees  test-lab-wood  test-ledger  test-membership  test-mining  test-move  test-msy  test-newcomers  test-nightly  test-nippon-boot  test-notice  test-npc-clothes  test-onboarding  test-oremix  test-path-core  test-picker-tables  test-preserve  test-props  test-psite-server  test-reanchor  test-redeploy  test-regrow  test-relic  test-render-common  test-resource-index  test-rock-los  test-rooms  test-route-persist  test-salt  test-save-periodic  test-segindex  test-shelter  test-spoil2  test-tame  test-terr-backfill  test-terr-persist  test-terrain-memo  test-tick-slicer  test-tidal  test-trade  test-trees  test-valuechain  test-village-found  test-villager-stuck  test-weather  test-weight  test-winter  test-wood-act  e2e-charsprite  e2e-conn  e2e-cutaway  e2e-fogray  e2e-guild  e2e-metallurgy  e2e-mtcorridor  e2e-mtfuzz  e2e-onboarding  e2e-spoil2  e2e-tidal  e2e-trade  e2e-ui  e2e-weight  e2e-zoom
```

## 6. 아침에 세션으로 넘길 빨강 (상시 목록 밖 · 카드별 한 줄)

- **T347** — `test-terraincache` 여전히 빨강(2밤 연속): `isWaterTileLocal` 이 terrain 을 캐시 가지 1 + 종전 가지 1 = 2회 불러야 하는데 1회만 부른다(단독 재실행도 빨강).
- **T355** — `test-site-memo` 여전히 빨강(5밤 연속 · 최장): 종전 판이 하루 5회 이상 집터를 훑는다(16회 / 9일 · 단독 재실행도 같은 수).
- **T355** — `e2e-guest-reconnect` 여전히 빨강: 승계 뒤 `playerId` 가 익명 id 그대로 남는다(단독 재실행에서도 id 만 바뀐 같은 실패).
- **T355** — `e2e-rumor` 여전히 빨강: 소문 도달이 폴링 한도 안에 오지 않는다(첫 판 ⑨ · 재실행은 ⑤ 로 실패 지점이 옮겨감 — 흔들림 의심).
- **T347/T355/T370 (후보)** — `test-downed` · `test-guest-rejoin` · `test-move-soa` · `test-war-world` 는 **단독 재실행 초록**(RC=0) — 전수 안에서만 빨강이므로 간섭·순서 의존 의심, 결함 확정 아님.

> 귀속 칸의 카드는 해당 하네스 파일을 최근 만진 커밋에서 뽑은 **후보**일 뿐이다 — 커밋 제목에 세션 표기가 없어 세션은 못 쟀다. 원인 판정은 하지 않았다.
