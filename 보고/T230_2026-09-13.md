# T230 — 집을 막는 기하: 수는 누구 것인가 · 갈리는 항 하나(영토 주택 압력) · 랩 사본 0

**[재민 확정 2026-09-13 · T230]** 새 수 **0**(랩에 있는 값만) · 손잡이 `T230_TERR_HOUSING` **기본 끔** · 실기 0.

**한 줄 답 — 거부 사유 10종 · 서버 고유 2(쉼터·환호) · 랩 정본·동일 8 · 랩 고유 1(길) · 갈리는 항 1(영토 주택 압력)
→ A/B 살려도 되는 마을 **23 → 2**(21곳이 안전해진다) · 랩 사본 0.**

**그런데 그 21곳의 대가가 인구 −42% 다.** 손잡이가 **잠들어 있던 결함 하나를 깨운다**:
`_terrGrow` 는 셀을 더할 때마다 `land.fertility` 를 덮어쓰는데, 비옥도 장 `_FF` 는 **마을마다 갈아 끼우는 하나**라
부팅 뒤엔 마지막 마을 상자만 남고 나머지는 중립 0.5 를 읽는다 ⇒ 켬 팔 50마을 중 **49곳이 정확히 0.70**(=0.5×1.4).
끔 세계에선 영토가 목표에 붙어 있어 `_terrGrow` 가 셀을 안 더한다(200일 **67셀**) — 그래서 이 코드가 잠들어 있었다.
랩이 같은 항을 안전히 쓴 이유도 같은 자리다: 랩 주석은 *"land.size는 lifeInit서 1회 고정 → **경제 영향 0**"* 라고
못박는다 — **랩 영토는 경제에 안 닿고 서버 영토는 닿는다.** ⇒ 손잡이는 끔으로 두고, 비옥도 자리를 회부한다.

---

## ⓐ 거부 사유·기하 수의 출처 (코드 0)

서버 `_lifeSiteFilters.reject()`(`villages.js:4276~4310`) 전수 + 후보 단계 제약.

| # | 서버 항 | 서버 값 | 랩에 있나 | 랩 값 | 정본 |
|---|---|---|---|---|---|
| 1 | 기존 집과 너무 가까움 | `HG = 18` | ✓ `L_HGAP` | **18** | **같다**(랩 정본) |
| 2 | **쉼터와 너무 가까움** | `HG = 18` | **✗** — 랩엔 쉼터가 없다(`shelter`·`쉼터` 0회) | — | **서버 고유 규칙** · 수는 18 재사용 |
| 3 | 공사 중인 마을 집터와 가까움 | `HG = 18` | ✓ 등가 — 랩은 집터를 `s.houses` 에 즉시 넣어 `L_HGAP` 이 함께 덮는다 | 18 | 같다 |
| 4 | 다른 의뢰 집터와 가까움 | `HG = 18` | ✓ 등가(`placePlayerSite` 도 같은 배열) | 18 | 같다 |
| 5 | 마을 영토 밖 | `LOT_CELLS ⊂ _terrSet` | ✓ `fpOk` own | 같음 | 같다 |
| 6 | 부지 불가(물·바위) | `isBlocked` | ✓ `fpOk` water/rock | 같음 | 같다 |
| 7 | 개간 농지 위 | `farmAt`(strict=잠재농지) | ✓ `isFarm`(strict=`potSet`) | 같음 | 같다 |
| 8 | 물가 완충 침범 | `LOT_GUARD` 물 | ✓ `farWater` | 같음 | 같다 |
| 9 | 곳간과 겹침 | ±(6,1)/(2,4) | ✓ `granClear` — **같은 식** | 같음 | 같다 |
| 10 | **환호 도랑 위** | ditch 셀 | **✗** | — | **서버 고유** |
| — | (랩만) **길 위 금지** | — | ✓ `roadLevel(x,y) ≥ 1` | — | **랩 고유**(서버 미이식) |
| 후보 | 큰집 마당 비침범 | `HALL_CLEAR = 16.5` | ✓ 같은 모듈 | 16.5 | 모듈 정본 |
| 후보 | 짝수 격자 | ✓ | ✓ | — | 같다 |
| 점수 | 물가 페널티 | `W_PEN_K = 2000` | ✓ | **2000** | 같다 |
| 대안 | **빈 자리 없음 → 층 증축** | **✗ 서버엔 경로 자체가 없다** | ✓ `L_MAXFL` | **1**(봉인) | 랩 정본 · 값이 1이라 켜도 무변 |
| 영토 | **목표** | `round(land.size × 25)` | ✓ + **주택 압력** `ceil(housing/(6×1))×600 + 1500` | 600·1500 | **★갈린다 — 랩 정본** |
| 영토 | 하루 상한 | `TERR_GROW_MAX_PER_DAY = 60` | 빨리감기 300 · 관찰 6게임시 1셀 | — | 서버 고유 속도(목표가 병목이라 무관) |

**⇒ 거부 사유 10종 · 서버 고유 2 · 랩 정본·동일 8 · 랩 고유 1 · 갈리는 항 1.**
`HG = 18` 은 **새 수가 아니다** — 랩 `L_HGAP = 18` 이 같은 값이다(T219 의 `0.92` 는 서버 고유였지만 이 셋은 아니다).
`600`·`1500` 도 랩 정본의 수다. **새 수 0.**

### 집 몇 채가 들어갈 수 있는 땅인가 (수용 상한 셈)

| 셈 | 값 |
|---|---|
| 집 하나가 닫는 원판 π·18² | **1,018셀** (그 안엔 다른 집 중심이 못 온다) |
| 최밀 육각 배치에서 한 채의 실제 몫 (√3/2)·18² | **281셀** |
| 큰집 마당 원판 π·16.5² (제외) | 855셀 |
| 마을 영토 중앙(실측 50마을) | **3,450셀** |
| ⇒ 기하 상한 (3,450 − 855) / 281 | **≈ 9채 = 55침상** (농지·물 완충 빼기 전) |
| 실측 완공 중앙 | **5채 = 30침상** — 나머지는 농지가 먹는다 |
| 랩 목표의 채당 예산 | **600셀** = 육각 몫의 **2.14배**(농지 몫까지 미리 잡는다) |

즉 지금 영토는 **인구 30명분 침상**이 최대다. 인구 200 마을(T219 어촌2)이 4~5채인 것은 고장이 아니라
**땅이 그만큼밖에 없는 것**이다 — 그리고 그 땅을 늘리는 손잡이가 econ 압력만 듣고 있었다.

---

## ⓑ A/B — 갈리는 항 하나만 손잡이로 (실서버 200일 · 50마을 · 시드 하나)

`T230_TERR_HOUSING` (기본 끔) · 같은 계측기 `scripts/t212-beds.js` · `T212_ENV` 로 손잡이만 넘김 · 같은 지형 시드 ·
게임일 4,000ms × 200일 · 관측자 없음 · `ENABLE_BANDITS/ROADS/WILDLIFE=0`.

| | **끔**(종전) | **켬**(랩 값) | Δ |
|---|---|---|---|
| 영토 성장 | **67셀** / 50마을 / 200일 | **53,100셀** | **×793** |
| 영토 중앙 | 3,450 | 4,200 | +750 |
| 실체 집 | 235채 | **264채** | **+29** |
| 침상(층×6) | 1,410 | **1,584** | +174 |
| **침상/인구** | **59%** | **114%** | **+55%p** |
| 집터 탐색 호출 / 성공 / **빈손** | 930 / 135 / **795** | 247 / **164** / **83** | 빈손 **85.5% → 33.6%** |
| 훑은 셀 | 6,025,434 | 1,519,320 | −75% |
| 거부 1위 | 집 간격 18 — 78.1% | 집 간격 18 — 80.3% | (여전히 1위) |
| **`_hcap<housing` 마을** | **23** | **2** | **−21 ★** |
| 어긋난 마을 | 50/50 (침상<housing 23) | 50/50 (**침상<housing 2 · 침상>housing 48**) | 방향이 뒤집힌다 |
| 출생 멈춤(집터가 막은 마을) | 0 | 0 | 0 |
| **인구** | **2,390** | **1,389** | **−42% ⚠** |
| econ `housing` | 2,390 | 1,126 | −53% |
| 농부 없는 마을 | 7 | **27** | +20 |
| `land.fertility` 서로 다른 값 | **28종** (중앙 0.96) | **2종 — 49곳이 정확히 0.70** | 평탄화 ⚠ |

**★기하는 풀렸다.** 침상이 인구를 처음으로 넘고(59% → 114%), 빈손이 1/3 로 줄고, `_mapBeds` 를 살릴 때
상한이 작아지는 마을이 **23 → 2**. 즉 **살려도 되는 마을 21/23** 이 생긴다 — #22 의 조건은 이 항으로 선다.

**⚠그런데 같은 손잡이가 잠든 결함을 깨운다.**

| 자리 | 무엇 |
|---|---|
| `villages.js:2461` | `land.fertility = max(0.1, min(2.0, (Σ fert / n) × 1.4))` — `_terrGrow` 가 **셀을 더할 때마다 덮어쓴다** |
| `villages.js:193` | `let _FF = null;` — 비옥도 장은 **하나**다 |
| `villages.js:194` | `prepareFert(ccx, ccy, R)` 가 `_FF` 를 **갈아 끼운다**(마을당 62셀 상자) |
| `villages.js:1150` | 시딩 루프가 마을마다 부른다 ⇒ 부팅 뒤 `_FF` 는 **마지막 마을 것 하나** |
| `villages.js:202` | `fert = (x,y) => (_FF ? _FF.at(x,y) : 0.5)` — 나머지 마을에선 중립 **0.5** |

⇒ `0.5 × 1.4 = 0.70`. 실측 그대로다: 켬 팔 **49/50 마을이 0.70**, 제 값을 지킨 건 1곳(1.07)뿐.
비옥 1.15·0.99 였던 마을(인구를 떠받치던 곳)이 0.70 으로 떨어져 세계 인구가 42% 준다.

**끔 세계에선 이 코드가 한 번도 안 돈다** — 영토가 50마을 전부 `land.size × 25` 에 붙어 있어
`_terrGrow` 가 첫 줄에서 `return 0` 한다(200일 67셀). 그래서 결함이 보이지 않았다.

**랩은 왜 같은 항을 안전하게 쓰나** — 랩 `growTerritory` 머리 주석:
> `land.size는 lifeInit서 1회 고정 → 경제 영향 0(렌더/정착 경계만)`

**랩 영토는 경제에 안 닿는다. 서버 영토는 닿는다.** 같은 수를 옮겨도 같은 세계가 아닌 이유가 이것이다.

⇒ **손잡이는 끔으로 둔다.** 켜는 조건은 값이 아니라 **비옥도 자리**다(회부).

---

## ⓒ 랩 사본 0 — 방아쇠 세 줄 → 한 호출

| | 전 | 후 |
|---|---|---|
| `lab/전쟁실험실.html` | `targetFloors` 수식 + 조건 두 항 | `VillageLayout.houseSiteWant(s.pop, econ.housing, builtFl, sites)` **한 호출** |
| `lab/마을실험실.html` | 같음 | 같음 |
| 인라인 `VillageLayout` | `houseSiteWant` 없음 | 정본과 **한 글자도 같은** 본문 + 수출 1 |

`lab-wiring-check.js` **[I] 절** 신설 — 손 동기 규약을 기계가 지킨다:

| 검사 | 결과 |
|---|---|
| 랩 인라인 `houseSiteWant` 본문 = 정본(공백·주석 제거 후 문자열 동일) | ✅ 두 랩 |
| 방아쇠가 그 함수를 부른다 | ✅ 두 랩(각 1곳) |
| 옛 방아쇠 수식(`builtFl+sites<targetFloors`)이 남아 있지 않다 | ✅ 두 랩 |
| 랩 `L_MAXFL` = 정본 `HOUSE_MAX_FLOORS` | ✅ 1 = 1 |
| 랩 `L_HGAP` = 서버 `HG` | ✅ 18 = 18 |
| 랩 주택 압력 두 수 = 정본 `TERR_PER_LOT`·`TERR_CORE` | ✅ 600·1500 |
| **판정 동등** — 옛 랩 수식 vs 정본 함수 **43,344점 전수** | **다른 점 0** (참 30,790점 — 자명 통과 아님) |
| **자명 통과 금지** — 정본을 `max`→`min` 으로 비틀면 | **14,689점이 갈린다**(비교가 실제로 문다) |

랩 행동 비트 동일(`test-lab-psite` B팔): 지정 day **122** · 완공 day **125** · `_mapBeds` **54** · 마을 완공 **9채** —
T219 판과 한 수도 안 다르다. 전 팔 **PASS**.

부수 정리: `village-layout.js` 의 층 상한이 `generate()` 안 지역 상수(`MAX_FLOORS = 1`)였다 —
모듈 정본 `HOUSE_MAX_FLOORS` 로 올렸다(값 그대로 · 사본 하나 제거 · 위 [I] 절이 랩 `L_MAXFL` 과 대조한다).

---

## 검증

| 항목 | 결과 |
|---|---|
| 서버 diff | `village-layout.js` — `HOUSE_MAX_FLOORS`·`territoryTarget` 추가(+수출) · `villages.js` — `_terrGrow` **한 자리** + 손잡이 선언 |
| 클라 diff | **0** |
| 손잡이 | `T230_TERR_HOUSING` · 기본 **끔** · 끔 경로 = `territoryTarget().econ` = `round(land.size×25)` (종전과 한 수도 안 다르다) |
| 3시드 800일 전문 diff(손잡이 미설정) | 시드 1020 **0** · 7 **0** · 42 **0** — 정결 베이스 `f92f65bd` |
| 새 수 | **0** — 18·2000·16.5 는 랩과 같은 값, 25·600·1500 은 랩 정본, 1 은 옮긴 상수 |
| 사본 | **0** — 방아쇠 규칙 하나 · 영토 목표 하나 · 랩 인라인은 `lab-wiring-check [I]` 가 문자열로 대조 |
| 하네스 | `lab-wiring-check` **실패 0 · 경고 0** · `test-psite-server` **PASS** · `test-lab-psite` **PASS** · `test-farm-life` **33/0** |
| 러너 전수 | **안 돌렸다**(규약 ②) |
| 무접촉 | T228(세션5 `villages.js` 사냥) · T226(세션3 랩 나무 층) — 내 diff 는 `_terrGrow` 한 자리 · 랩은 방아쇠 한 줄 + 인라인 layout 한 함수 |
| 접점 | `_lifeSiteFilters` `reject` `houseSiteWant` `HOUSE_CAP_PER_FLOOR` `_houseCells` `_site` `shelter` `territory` `T219_HOUSE_TRIGGER` `T230_TERR_HOUSING` `t212-beds` `T212_ENV` `lab-wiring-check` |

## 회부

- **★비옥도 자리(새 회부 · 이 카드가 잡았다).** `_terrGrow` 의 `land.fertility` 재측정은
  `_FF` 가 **마을당 하나**라서 영토가 자라는 순간 49/50 마을을 0.70 으로 평탄화한다.
  고칠 길 둘: ⓐ 마을별 비옥도 장을 들고 있게(메모리 ↑) ⓑ `_terrGrow` 가 비옥도를 **안 덮게**
  (랩처럼 영토를 경제에 안 닿게). **랩은 ⓑ 쪽이다**(주석에 명기). 판정은 재민.
  **이 자리가 풀리기 전엔 `T230_TERR_HOUSING` 을 켜지 않는다.**
- **#22 `_mapBeds` 살리기 — 조건이 보인다.** 영토 주택 압력을 켠 세계에서 살려도 되는 마을이
  **21/23** 생긴다(`_hcap<housing` 23 → 2 · 침상/인구 59% → 114%). 순서 확정: **비옥도 → 영토 → 방아쇠 → 살리기.**
- **서버 고유 규칙 둘은 그대로 둔다** — 쉼터 이격(랩엔 쉼터가 없다) · 환호 도랑(랩엔 없다).
  거부의 12.0% 를 쉼터가 먹지만, 쉼터는 T62 의 제품 결정이고 값은 랩 `L_HGAP` 과 같다 ⇒ **새 수 아님 · 손댈 것 없음**.
- **랩 고유 `roadLevel ≥ 1`(길 위 금지)는 서버에 없다** — 서버 판에서 `ENABLE_ROADS=0` 이라 지금은 무해하지만,
  길을 켜면 부지가 길을 깔고 앉는다. → **작은 카드**
- **`sim/regression-check.js` 가 랩을 못 찾는다** — `ROOT/../마을실험실.html` 을 읽는데 랩은 2026-09-05 에
  `lab/` 으로 들어왔다. 지금은 조용히 기본값으로 도니 13/26 이 무슨 뜻인지 아무도 모른다. → **하네스 회부**
- 첫 의뢰(#18) · 추천 자격(#19) · 16방향 · 이송 실체 · `nippon` 시딩(Z) — 그대로.

## 실행

```
node scripts/lab-wiring-check.js                                   # [I] 방아쇠 절 포함
T212_ENV=T230_TERR_HOUSING=1 node scripts/t212-beds.js             # 켬 팔(200일)
node scripts/t212-beds.js                                          # 끔 팔
LAB_SEED=1020 node scripts/econ-lab-real.js 800                    # 비트 동일(손잡이 미설정)
```
