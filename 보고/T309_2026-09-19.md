# T309 — 경계 감사·메모 자리·현장 후보 (세션5 · 2026-09-19 · **코드 0 · 가지만**)

> 카드 `~/Mini/지시_T309.md` · 가지 `batch/chunk-audit-0919` · **베이스 `c9b40d87`(main · T301 착지본)**
> 만진 것: `보고/T309_2026-09-19.md` + 계측 스크립트 **둘**(`scripts/t309-chunk-boundary-audit.js` ·
> `scripts/t309-jobsite-scan.js` — `@regress` 없음) — **제품 코드 0** · `chunk.js`·`villages.js` 전쟁 절 무접촉.

## 결론 (머리 15줄)

1. **경계 갈래 8자리 중 결함 후보 2** — 둘 다 **같은 한 가지**다: 청크가 **자기 밖에 낳은 개체**를
   낳은 청크가 아니라 **이웃 청크 바구니**에 넣고, 비활성화는 **바구니 기준**으로 지운다.
2. 넘치는 양: 청크 400개 · 개체 12,461 중 **933개(7.49%)가 제 청크 밖**이다. **전부 나무**.
   동 480 · 남 435 · 동남 18(서·북으로는 안 넘친다 — 격자가 0 에서 시작한다). 최대 넘침 **91.1px**.
3. **이웃 청크는 그 나무를 스스로 안 낳는다 — 162/162.** 그래서 "낳은 청크가 꺼지면 사라지고,
   이웃만 켜져 있으면 처음부터 없다". **관측자 의존이 남아 있다**(T284 ①이 고치려던 바로 그것).
4. 다른 6자리(`chunkKey` 색인 · `activeChunkKeys` 순회 · `materializeBuildingsInChunk` 사각 ·
   `groves` 중복 금지 · `wildlife._chunkForest` · `zone.js:2642` 셀 거르개)는 **상관없다** — 근거는 ⓐ 표.
5. **메모 자리는 하나로 족하다** — `_warTerrBlocked` 의 `state._warTerrMemo`(키 `ix*65536+iy` ·
   비우는 곳 **한 군데** `_warBuildRectIndex`). 나무 술어를 **그 메모에 같이 태우면** 새 규약도 새 수도 없다.
   예상 부하: 전장 한 판(60×60=3,600칸) **첫 채움 42ms 한 번**, 그 뒤 틱은 Map 조회.
6. **현장 후보 — 나무는 거리 문제, 광맥은 아니다.**
   반경 16셀: 나무 중앙 **4칸** · 나무 0 마을 **3곳**(광산4·어촌1·광산7) · 광맥층 0 마을 **43/51**.
   반경 48셀로 넓히면 나무 0 마을은 **0**(중앙 30)이 되는데, **광맥층 0 마을은 43 그대로다.**
7. ⇒ **43마을의 광부는 걸어갈 데가 없다.** 그래도 지금 수식은 `land.ore` 바닥값으로 곳간을 채운다 —
   행위 생산으로 가면 **그 43마을의 광부는 0 이 되거나, 광맥이 없는 채로 캐야 한다**. 설계 칸이다(회부).
8. 검증: `test-resource-index` **12/0** 그대로 · 제품 `git diff` **0** · 3시드 면제(만진 제품 파일 0).

---

## §0ⓐ 청크 경계 감사 — 갈래 8자리

검색어(전수): `chunkKey` · `/(chunkSize|CHUNK_SIZE|cs)` · `(cx|ccx|qx)*(chunkSize|cs)` · `activeChunk` ·
`!== (cx|cy|ccx|ccy)` · `for (… chunk`. 대상 `server/*.js` · `public/*.js` · `public/client/*.js` · `lab/` · `sim/`.
**걸린 줄 50 · 파일 3**(`server/chunk.js` 28 · `server/zone.js` 19 · `server/wildlife.js` 3).
의미 있는 **자리**로 묶으면 여덟이다.

| # | 자리 | 무엇을 가르나 | 판정 | 근거 한 줄 |
|---|---|---|---|---|
| 1 | `chunk.js:56~100` `insertResource/Mob/Building` → `_chunkKey` | 개체를 **좌표가 든 청크** 바구니에 넣는다 | **결함 후보** | 낳은 청크와 **다른** 바구니에 들어간다(넘침 7.49%) — 지우는 쪽이 바구니 기준이라 3번과 짝이 된다 |
| 2 | `zone.js:276~306` `deactivateChunk` — `c.resources` 중 `isSeed` 제거 | **바구니 기준**으로 지운다 | **결함 후보** | 청크 (1,0) 이 꺼질 때 청크 (0,0) 이 낳은 나무까지 지운다. (0,0) 은 아직 켜져 있는데 그 나무가 사라진다 |
| 3 | `zone.js:256~266` `activateChunk` — 낳은 것을 전부 insert | 안 가른다(전부 넣는다) | 상관없다 | 넘친 개체도 넣긴 넣는다 — 문제는 **어디에** 넣느냐(1번)와 **누가 지우냐**(2번) |
| 4 | `chunk.js:554` groves `Math.floor(x/cs) !== cx → continue` | 군락 점을 **제 청크에서만** 낳는다 | 상관없다 | 중복 금지 가드가 **정확히** 있다. 나무 그리드엔 그 가드가 없다(그래서 넘친다) |
| 5 | `zone.js:2642` `Math.floor(r.x/32) !== cx` | **셀** 거르개(청크 아님) | 상관없다 | 32px 셀 단위 · 개체는 이미 쿼드트리로 찾았다(`queryCircle`) |
| 6 | `zone.js:203~206` `materializeBuildingsInChunk` 사각 `[cx·cs, (cx+1)·cs)` | 건물 행을 사각으로 읽는다 | 상관없다 | 건물은 **DB 행 좌표**가 정본이라 넘침이 없다(시드 지터가 없다) |
| 7 | `zone.js:308~345` `activeChunkKeys` 순회(`activeChunkBuildings`·qt 적재·밭 단계) | 활성 청크만 훑는다 | 상관없다 | 의도가 그것이다(AOI). 자원 존재 자체를 가르지 않는다 |
| 8 | `wildlife.js:379~445` `_chunkForest`(청크별 숲 밴드 캐시) | 청크 사각을 셀로 훑어 캐시 | 상관없다 | 입력이 **지형장**(`getForestMultiplier`·`isRockTileLocal`)이라 청크 밖 개체와 무관 |

### 실측 — `scripts/t309-chunk-boundary-audit.js`

```
청크 400개(숲 62 · 들 338) · 개체 12,461 · 제 청크 밖 933 (7.49%)
방향: 동 480 · 남 435 · 동남 18            종류: tree 933 (전부 나무)
최대 넘침 91.1px (격자 간격 상한 FOREST_SP_MAX=96 안)
표본 60_0_ft1023_651 @ 62555.1,705.0 (청크 60,0 이 낳았는데 청크 61,0 땅)

넘친 개체를 이웃 청크가 스스로 낳나: 검사 162 · 낳는다 0 · 안 낳는다 162
```

**왜 나무만인가** — 일반 자원은 `cx*cs + 16 + r*(cs−32)` 라 청크 안에 갇히고, 운철도 같다.
군락은 4번의 가드가 있다. **숲 그리드만** `cx*cs + gx + j1*SP`(gx 최대 `cs−1`)라 지터가 경계를 넘는다.

### 고치는 길 셋 (구현 금지 · PM 카드)

| 안 | 무엇 | 대가 |
|---|---|---|
| ㉠ 낳을 때 가드 | 숲 그리드에도 4번과 같은 `Math.floor(x/cs) !== cx → continue` | **세계가 바뀐다**(나무 933/12,461 이 사라진다) · 기준선 재측정 |
| ㉡ 지울 때 기준 | `deactivateChunk` 가 **바구니**가 아니라 **`seedKey` 의 낳은 청크**로 지운다 | 세계 무변 · `seedKey` 파싱 한 줄 · 이웃이 안 켜져 있으면 여전히 없다 |
| ㉢ 켤 때 이웃까지 | `activateChunk` 가 이웃 한 겹의 넘침만 같이 낳는다(색인이 이미 쓰는 그 규칙) | 세계 무변 · 관측자 의존 **완전 해소** · 활성화 비용 ×(1+α) |

⚠**㉠은 세계를 바꾼다.** 넘친 나무는 **지금 존재하는 나무**다(양쪽 청크가 다 켜져 있으면 보인다).

## §0ⓑ 나무 술어 메모 자리 — `_warTerrBlocked` 와 **같은 자리·같은 규약**

| | 지금(`_warTerrBlocked`) | 나무 술어에 그대로 |
|---|---|---|
| 메모 | `state._warTerrMemo` (`Map`) | **같은 Map 을 쓴다**(키만 다르게) 또는 `state._warTreeMemo` 하나 더 |
| 키 | `ix * 65536 + iy` (`villages.js:2895`) | **같은 식** — 새 수 0 |
| 값 | `0/1` | 같다 |
| 비우는 곳 | **한 군데** — `_warBuildRectIndex`(`villages.js:2888`, 하루 한 번·교전 시작) | **같은 줄에 한 칸 더** |
| 비우는 이유 | "그 사이 판 도랑(환호)도 다음 교전에 반영된다" | 그 사이 **벤 나무**도 다음 교전에 반영된다 — 같은 논리 |
| 첫 채움 비용 | 존 술어 ~0.05ms/셀 | `treeBlockerAt` **11.7µs/셀**(T301 실측) |
| 전장 한 판(60×60=3,600칸) | — | **42ms 한 번** · 그 뒤 틱은 Map 조회(≈0) |

### T295(세션10)가 그대로 붙일 diff — **표만**(이 카드는 `villages.js` 무접촉)

```js
// ① 메모 비우는 줄 — villages.js:2888 (_warBuildRectIndex 안, 기존 줄 옆에 한 칸)
   state._warTerrMemo = new Map();
+  state._warTreeMemo = new Map();      // ★나무도 같이 — 그 사이 벤 나무가 다음 교전에 반영된다

// ② 술어 한 줄 — _warTerrBlocked 바로 아래 (같은 꼴 · 같은 키)
+function _warTreeCell(ix, iy) {
+  const M = state._warTreeMemo || (state._warTreeMemo = new Map()); const k = ix * 65536 + iy;
+  let v = M.get(k);
+  if (v === undefined) { try { v = require('./chunk').treeBlockerAt(state.zoneId, ix, iy) ? 1 : 0; } catch (_) { v = 0; } M.set(k, v); }
+  return v === 1;
+}

// ③ 어댑터 세 칸 — villages.js:2987 / :2990 / :2993
   blocked(x, y)            { const ix = toCell(x), iy = toCell(y);
-                             return _warBlockedCell(ix, iy); },
+                             return _warBlockedCell(ix, iy) || _warTreeCell(ix, iy); },
   losBlocked(x1,y1,x2,y2)  { … if (_warRectHas(cx, cy) || _warRockCell(cx, cy)
-                                 ) return true; … },
+                                 || _warTreeCell(cx, cy)) return true; … },
   coverAt(x, y, pad)       { … /* 건물 상자 뒤 */
-                             return false; },
+                             return _warTreeCell(Math.floor(x / MPC), Math.floor(y / MPC)); },
```

⚠붙인 뒤 **`test-war-world ⓓ`(관측자 켬/끔 3시드)를 다시 돌려야 한다.** 색인 쪽 결정성은 T301 이
이미 쟀지만(전장 3,600/3,600 동일), 전투 **결과**가 나무로 갈리는지는 그때 재는 것이다.
⚠**변경분 규칙**(T301 §0ⓐ): 살아 있는 것(`qtResources`) > 장부(`harvestedSeeds`) > 색인.
전쟁이 활성 청크 안에서 벌어지면 `qtResources` 가 정본이다 — 위 술어는 **비활성 구역**의 답이다.

## §0ⓒ 51마을 현장 후보 — `resourceAt` 반경 16셀 전수

`scripts/t309-jobsite-scan.js` · 마을 중심은 **지형 정본**(`terrain.getZoneVillages`)이 적어 둔 좌표다.
(제품이 실제로 세우는 중심은 `findOpenCenter` 가 몇 셀 옮긴다. 그러려면 술어 한 벌이 필요한데
`scripts/zone-preds.js` 는 **아직 main 에 없다**(T259 가지 · PM 미착지) — 다시 적으면 사본이라 **안 적었다**.
반경 16셀 안의 그림은 몇 셀 옮겨도 거의 같다. 그래서 **후보 표**다.)

| 반경 | 벽시계 | 나무 중앙 | **나무 0 마을** | 광맥층 중앙 | **광맥층 0 마을** | 바위 중앙 | 덤불 중앙 |
|---|---|---|---|---|---|---|---|
| **16셀**(1,089칸) | **1.5초** / 51마을(마을당 29ms) | **4** | **3** — 광산4 · 어촌1 · 광산7 | 0 | **43 / 51** | 0 | 0 |
| 48셀(9,409칸) | 12초 / 51마을 | **30** | **0** | 0 | **43 / 51** | 3 | 7 |

* **나무는 거리 문제다.** 16셀에서 0 이던 세 마을이 48셀에서는 전부 나무를 만난다(중앙 4 → 30).
  ⇒ 나무꾼의 설계값은 "있나 없나"가 아니라 **왕복 거리**다(설계 §2-ⓑ 의 그 수).
* **광맥은 거리로 안 풀린다.** 48셀(1.5km)로 넓혀도 **43마을이 0** 그대로다.
  광맥이 있는 8곳은 `광산1~7 · 어촌1` — 고증대로 **소수 산지**다.
* ⚠**그런데 지금 수식은 그 43마을에도 광물을 낸다**(`land.ore` 바닥값 · `livelihood.landOf`).
  행위 생산으로 가면 **그 43마을 광부는 갈 데가 없다**. 셋 중 하나를 골라야 한다(설계 칸 · 회부):
  ㉠ 그 마을엔 광부를 안 둔다(직업 정원 0) · ㉡ 교역으로만 광물을 얻는다 · ㉢ 광맥 분포를 다시 깐다.

대표 행(전수는 `scripts/t309-jobsite-scan.js` 출력 · JSON `T309_JSON=`):

| 마을 | 중심(셀) | 나무 | 광맥층 | 씨광맥 | 최근접 나무 | 최근접 광맥층 |
|---|---|---|---|---|---|---|
| 광산1 | 2120,2117 | 2 | 4 | 4 | 6 | **1** |
| 광산4 | 1285,1330 | **0** | 4 | 4 | **—** | 6 |
| 어촌1 | 1296,2357 | **0** | 4 | 4 | **—** | 3 |
| 광산7 | 2034,3099 | **0** | 6 | 6 | **—** | 4 |
| 임업1 | 891,3516 | **236** | 0 | 0 | 0 | — |
| 임업6 | 1581,496 | 192 | 0 | 0 | 0 | — |
| 농촌20 | 428,3489 | 1 | 0 | 0 | 16 | — |
| 농촌22 | 964,1864 | 4 | 0 | 0 | 10 | — |

## §0ⓓ 답 한 줄

> **경계 갈래 8자리 중 결함 후보 2**(둘이 한 짝 — 넘친 나무를 **이웃 바구니**에 넣고 **바구니 기준**으로 지운다 ·
> 넘침 **933/12,461 = 7.49%** 전부 나무 · 이웃은 **162/162 안 낳는다** ⇒ 관측자 의존이 남아 있다) ·
> **메모 자리는 `_warTerrMemo` 하나**(키 `ix*65536+iy` · 비우는 곳 `_warBuildRectIndex` 한 군데 ·
> 전장 한 판 첫 채움 42ms 한 번) · **현장 0 마을 — 나무 3(48셀이면 0) · 광맥 43(48셀에서도 43)**.

## §1 검증

* **제품 코드 0** — `server/`·`sim/`·`lab/`·`public/` `git diff` **0**. 새 파일은 `보고/` 하나 + `scripts/` 계측 둘(`@regress` 없음).
* `test-resource-index` **12 통과 / 0 실패**(그대로 — 만진 게 없다).
* 3시드 면제 — **만진 제품 파일 0**(`lab-modules --diff` 교집합 ∅).
* 접점: `resourceAt`·`resourcesAtCell`·`generateChunkResources`·`onlyCell`·`treeBlockerAt`·
  `_warTerrBlocked`·`_warTerrMemo`·`_warBuildRectIndex`·`chunkKey`·`CHUNK_SIZE`·`activeChunkKeys`·
  `deactivateChunk`·`isMajorOreAt`.

## §2 회부(구현 0)

1. ★★★**[새 카드 · PM] 넘친 나무가 관측자에 따라 있다 없다 한다.** 자리 둘(`chunk.js:56` 바구니 ·
   `zone.js:276` 바구니 기준 제거) · 넘침 **7.49%** · 이웃은 **안 낳는다(162/162)**.
   고치는 길 셋(㉠낳을 때 가드=세계 변경 · ㉡`seedKey` 기준 제거=세계 무변 · ㉢켤 때 이웃까지=완전 해소)은 §0ⓐ 표.
   **T284 회부 ①(전투가 읽으면 결정성이 깨진다)이 아직 살아 있다** — 색인은 섰지만 **실체 쪽은 안 고쳐졌다**.
2. ★★**[T295 세션10] 전쟁 세 줄 + 메모 한 칸** — §0ⓑ 에 붙일 diff 그대로. 붙인 뒤 `test-war-world ⓓ` 재실행.
3. ★★★**[설계 · PM/재민] 광맥 없는 43마을의 광부를 어떻게 할 것인가.**
   48셀(1.5km)로 넓혀도 43/51 이 0 이다. 행위 생산이 서면 그 마을 광부는 **걸어갈 데가 없다**.
   ㉠직업 정원 0 · ㉡교역으로만 · ㉢분포 재배치 — 설계 칸이다(`설계_생산_실체` ⓑ 와 같이 읽어라).
4. ★**[자] `scripts/zone-preds.js` 가 main 에 없다**(T259 가지 · PM 미착지). 그래서 이 카드의 현장 표는
   제품 중심이 아니라 **지형 정본 좌표** 위에 섰다. T259 가 착지하면 중심을 제품 경로로 바꿔 한 번 더 재라.
5. ⓘ**앞 카드 회부 유효**: `tickSubsistence` 둘째 루프(무기·갑옷 마모 칸) · #31 25벌(#6 실기 뒤) ·
   #37 `:2729` · 물고기 개체(설계 ⓕ · 재민).
