# 보고 — T295 동원의 대가가 세계에 닿는다: 징발자는 생산에서 빠진다 · 군량은 한 번 싣고 한 곳에서 돌려준다 · 곳간 품목을 본다 (2026-09-19)

한 줄: **커밋 `c95d033` [승인 대기 · T295] — 걷는 병사(pid)와 econ 사람을 1:1 로 묶어 징발 기간 생산에서 빼고(옛 `_laborMul` 동원 항은 삭제),
행군분+공성분을 **한 번에** 싣고 궤주·항복·철수·무저항 **넷 다 같은 문**으로 돌려주며(전사자 몫 제외), 그 품목을 **마을이 먹는 그 함수**(`consumeFood`)가 고르게 했다 —
3시드(1020·7·42) 800일 51마을 `t17-metrics` JSON **바이트 동일**, `test-war-world` **54 통과 / 0 실패**(옛 30 + 새 절 ⓗ~ⓚ 24), 손잡이 `T295_RATION_MRL` 끔 = battle-core 비트 동일.**

* 실행 세션 = **세션10**(WAR) · 카드 `지시/지시_T295.md` · 영역 인계 `인계/X-전쟁.md` **X-2**.
* **새 수 0** — 군량은 기존 상수(`WAR_RATION`·`WAR_SIEGE_PACK`·`marchDays`)의 곱이고, 사기 항의 가중치는 **손잡이 값 자체**다(기본 끔 · 값은 재민 · §0-ⓑ 표).
* **사본 0** — 식사 순서(`economy-sim.consumeFood`)·식량등가(`totalFoodEquivalent`)는 **주입해서 그대로 부른다**(war-core 안에 순서표 복제 0 · 하네스 ⓙ 가 정적·동적 둘 다로 문다). 사망은 `_warRemoveNpc` 한 곳.
* ⚠**T284 회부 ②의 "`_laborMul` 엔진 미소비" 는 오독이었다**(§0-ⓚ 대조): v2 는 `v1.tickVillage` 를 부르고 그 생산 줄이 `(v._laborMul || 1)` 을 읽는다. 즉 동원 감산은 **실제로 걸려 있었다** — 그래서 이 카드는 그 항을 지우고(이중 방지) 결속만 남겼다.
* **푸시**: 가지 `batch/war-cost-0918` 만. 컨테이너 푸시는 403(프록시 · 이 계정 레포 권한 없음) → 번들 `T295.bundle`(`origin/main 8c0b250..HEAD`)을 채팅에 냈다 → §5. main 은 재민.
* 회부(§4): 무기 재고(⑤) · 선포 무관 동원(⑥) · `FOLLOW_CAP` · `WAR_OPS=0` · 포로 몸값의 `storage.food` 한 칸 · `_siegeBlock`/`_siegeOutMul`/`_recomputeSiege` 는 **산 칸**(표) · 대치 n 값 · 사기 항 값.

---

## §0 실측

### ⓐ 결속 — 어느 쪽이 정본이고, 어느 길로 들어가고 나오나

**정본은 존 pid 다**(PM 판정). econ 이 먼저 사람을 고르고 pid 를 찾는 길은 안 만들었다.

| 계기 | 문(함수) | 무슨 일 | 어디 |
|---|---|---|---|
| 동원(선포) | `warDraftFill(V, force, w.id)` | econ 에서 **병력 전량**만큼 자리를 비운다 — `npc._warDraft = w.id` · `counts[job]--` | `war-core warMobilize` |
| 걷는 병사 뽑기 | `warDraftBind(vil, pid, simJob, w.id)` | 그 pid 가 **이미 비운 자리 하나**를 집는다(같은 직업 우선 · `npc._warPid = pid`) · 동원을 안 거친 길(방어 소집)이면 그 자리에서 새로 비운다 | `villages._warDraftPids` |
| 생산 | `if (npc._warDraft) continue` | 그 사람의 그날 산출 전부 0(부산물 포함) — 교역 원정과 같은 자리·같은 문법 | `economy-sim` 생산 루프 |
| 복귀 | `warDraftReleasePid` / `warDraftReleaseWar` | 마크를 풀고 `counts[job]++` — **제 직업 자리로** | `villages._warReleasePid` · war-core 해제 |
| 전사(표본 pid) | `warDraftReleasePid` | 결속만 푼다. **econ 사망은 `warResolveBattle` 의 `warKill` 하나가 정본**(이중 사망 금지) | `villages._warDespawnPid` |
| 사망 일반 | `_warRemoveNpc(e, npc)` | `npcs` 에서 빼고, **징발자면 `counts` 를 또 빼지 않는다**(자리는 이미 비었다) | `war-core`(warKill 이 부른다) |

부수 게이트 셋(없으면 `counts` 합이 인구를 넘는다 — 실측 30명 마을에 31):
`switchNPCJob` 첫 줄(징발자는 전환 대상 아님) · 기근 사망 후보에서 제외 · 이주 후보에서 제외.

**생산 제외 실측** — 같은 마을 두 벌(같은 씨·같은 창설값), 30일 창 셋. 팔 X = 둘째 창에 인구 3할 징발 · 팔 C = 대조(징발 0).

| 창(30일) | 팔 X 곳간 증가율(식량등가/일) | 팔 C | X/C |
|---|---|---|---|
| ① 징발 전 | 29.64 | 29.64 | **100%**(전제 — 두 팔이 같다) |
| ② 징발 중(18명 = 인구 61 의 30%) | **−33.64** | 8.62 | −390% (곳간을 까먹는다) |
| ③ 복귀 뒤 | 22.55 | 13.85 | 163% (밀린 몫을 메운다) |

인구 X 30→72 · C 30→75. ②에서 마을이 **곳간을 헐어 먹는다** — 이것이 "동원의 대가" 다.
하네스 ⓗ 의 대조(같은 날·같은 세계에서 결속만 끔)는 켬 24.7 < 끔 55.5.

### ⓑ 군량 — 적재 · 소비 · 환급 · 전사자 몫 · 사기 항

| 항목 | 종전(main `8c0b250`) | T295 |
|---|---|---|
| 적재 | 행군분 `force×marchDays×2×WAR_RATION` 을 `warMobilize` 가 곳간에서 뺌 **+** 공성팩 `force×12일` 을 `_opPackLoad` 가 또 뺌(두 장부) | **한 적재** — `force × (marchDays×2 + WAR_SIEGE_PACK) × WAR_RATION`(새 수 0) · 뗀 품목을 `w._packItems` 에 적는다 |
| 소비 | 주둔·포위 1일분/일(행군 중엔 0 — 이미 뗐으니까) | 행군·주둔·포위·귀환 **매일 1일분** |
| 결단이 보는 수 | `_packRem`(공성분) | `_packRem − marchDays`(귀환 몫 예약) ⇒ 도착일 값은 **종전과 같다** |
| 환급 | 철수·항복만(전이 시점) · 궤주·무저항은 **소멸** | **복귀(해제) 한 문** `warRationRefund` — 넷 다 · 잔량비 × **생존비** · 두 번 불러도 한 번 |
| 품목 | `storage.food` 한 칸 | 섭식 정본 순서로 품목별 · 환급은 **뗀 품목 그대로** |
| 사기 | 무관(결단 문턱에만) | `T295_RATION_MRL`(기본 **끔**) — 켜면 `tgt -= w × (1 − ration)` |

**환급 실측(하네스 ⓘ)** — 병력 9 · 팩 14일 · 사흘 원정(잔량 11/14) · 궤주 판만 생존 6/9:

| 끝맺음 | 적재(식량등가) | 환급 | 기대(적재 × 11/14 × 생존비) |
|---|---|---|---|
| 궤주(rout) | 126 | **55** | 55 |
| 항복(surrender) | 126 | **99** | 99 |
| 철수(withdraw) | 126 | **99** | 99 |
| 무저항(walkover) | 126 | **99** | 99 |

두 번째 호출은 0(게이트). `food` 0 · 생선 400 마을: 팩 14.0일(뗀 품목 `{fish:126}`) → 환급도 생선으로(274→400 · 곡물 0 그대로).

**사기 항 A/B(랩 코어 그대로 · 같은 시드 40판 · 공 18 vs 방 17)** — 값은 재민.

| 손잡이 | ration 1.0 | 0.5 | 0.0 |
|---|---|---|---|
| **끔(기본)** | 승률 92.5% · 공생존 9.5 | 92.5% · 9.5 | 92.5% · 9.5 ← **셋이 같다 = 비트 동일** |
| 0.10 | 92.5% | 85.0% | 67.5% |
| 0.20 | 92.5% | 67.5% | 42.5% |
| 0.30 | 92.5% | 57.5% | 30.0% |

(공 궤주자 평균: 끔 2.1 → 0.30·ration 0 에서 6.5.) 0.10 은 "굶으면 조금 흔들린다", 0.30 은 "굶으면 진다" 에 가깝다.

### ⓒ 품목 — 같은 함수인가

* war-core 는 `opts.food.consumeFood` **하나만** 부른다. 파일 안에 사다리(cooked→어육→곡→채집→보존식) 복제 **0**(하네스 ⓙ 정적).
* 서버는 `food: { consumeFood: econ.consumeFood, totalFoodEquivalent: econ.totalFoodEquivalent }` 를 그대로 주입(`server/villages.js` createWar).
* 동적 대조: 같은 곳간(`food/fish/meat/cooked_food` 각 100)에서 **같은 양**(126)을 뗐을 때 —
  군량이 뗀 품목·양 = 섭식 정본이 고른 품목·양(`cooked_food, fish` · 오차 0).
* `food` 0 판: 팩이 찬다(위 ⓑ).
* **미주입 팔**(랩·v1 CLI): `food` 한 칸만 본다 — 생선 300 → 300(안 건드림) = 종전 비트.
* 같은 문으로 바뀐 곳: 적재 · 환급 · 약탈(전투승·무저항) · 공납(무혈 항복) · 조공. **남은 곳**: 포로 몸값(`WAR_CAP_RANSOM_FOOD`) — 회부.

### ⓓ 부하 — 같은 세계 위 두 팔(W0/W6)

> 계측기 `scripts/war-world-perf.js`(T284 와 같은 틀 세계 · 실지도 51마을 · 생활층 · 야생 · 캐러밴 · 시드 1020 · 관측자 하나 · 창 420초).
> ⚠컨테이너 2코어 — VPS 와 절대값이 다르다(비율만).

| 팔 | 판 | 존 틱 p50(ms) | p95 | 틱 빚 drop | 전쟁 틱 p50 | p95 | 동시 병사 최대 |
|---|---|---|---|---|---|---|---|
| **W0**(전쟁 0) | main `8c0b250` | 2.615 | 18.146 | 0 | — | — | 0 |
| **W0** | **T295** | 2.678 | 6.728 | 0 | — | — | 0 |
| **W6**(전쟁 6) | main ① | 4.101 | 8.554 | 0 | 1.564 | 2.597 | 283 |
| **W6** | **T295 ①** | 4.699 | 8.863 | 0 | 1.728 | 2.598 | 283 |
| **W6** | main ② | 4.050 | 8.723 | 0 | 1.417 | 2.256 | 283 |
| **W6** | **T295 ②** | 4.173 | 8.227 | 0 | 1.392 | 2.348 | 283 |

읽는 법 — **두 판을 나란히 본 이유가 이 표의 답이다.** 첫 판만 보면 T295 가 W6 을 +0.6ms(+15%) 올린 것처럼 보이는데,
같은 팔을 한 번 더 돌리면 main 4.050 · T295 4.173 이다(차 0.12ms). 즉 **차이는 팔 사이 잡음 안**이다(같은 컨테이너에서
main 자신도 4.101 ↔ 4.050 으로 흔들리고, W0 p95 는 18.1 ↔ 6.7 로 흔들린다 — 2코어 공유 환경).

**왜 늘 이유가 없나**(코드로 본 답): 이 카드가 더한 일은 전부 **게임일 경계**다 —
징발/복귀 마크는 전쟁 하나당 병력 수 O(n)·선포와 해제 때 각 한 번, 군량 적재·환급은 전쟁당 한 번(`consumeFood` 한 호출),
생산 루프는 `if (npc._warDraft)` **비교 한 번**(인구 × 하루 1회)이다. **틱마다 도는 것은 0** —
교전 스텝에 더한 것은 사기 항 하나인데 그건 손잡이가 꺼져 있으면 비교 한 번에서 끝난다(`_ratW > 0`).
T284 표(같은 계측기 · 다른 날): W0 3.6~3.9 · W6 5.4~6.0 — 절대값은 그날의 컨테이너 부하라 **비율만** 본다(W6/W0 ≈ 1.5~1.8배).

**drop 0 · 병사 283명 · 전쟁 몸 5**(두 판 다) — T284 가 세운 한계(전쟁 25건 · 병사 983명 · drop 0)는 이 카드가 안 건드렸다.

### ⓔ 한 줄 답

**동원의 대가는 이제 세계에 닿는다 — 사람이 빠지고(곳간 증가율이 대조 대비 마이너스로), 군량은 곳간 품목에서 규모대로 나갔다가
살아 돌아온 만큼 그 품목 그대로 돌아오며, 그 값을 마을이 먹는 것과 **같은 함수**가 정한다. 전쟁 없는 세계는 3시드 800일 바이트 동일이고, 부하는 잡음 안이다.**

---

## §1 무엇을 고쳤나(파일별)

| 파일 | 줄 | 무엇 |
|---|---|---|
| `sim/economy-sim.js` | +7 | 생산 루프 `_warDraft` 한 줄(교역 원정 옆) · 전환/기근사망/이주에서 징발자 제외 셋 |
| `sim/war-core.js` | +약 190 | 결속 문 다섯(`warDraftBind/Fill/ReleasePid/ReleaseWar/Count`) · `_warRemoveNpc`(사망 한 곳) · 군량 한 적재/환급 한 곳(`_opPackLoad(…,days,w)` · `warRationRefund`) · 품목 이동(`_warFoodTake/Give/Move`) · `_recomputeLabor` **삭제** |
| `server/villages.js` | +약 25 | `_warDraftPids(…, warId)` 에서 결속 · 해제/전사에서 결속 해제 · econ 섭식 함수 주입 · `_laborMul` 에서 동원 항 제거 · 교전 `ration` 주입 · `_warNoArmy` 의 조기 환급 제거 |
| `sim/battle-core.js` | +12 | 사기 항 손잡이 `T295_RATION_MRL`(기본 끔 · 켤 때만 식에 손댄다) |
| `scripts/test-war-world.js` | +172 | 절 ⓗ~ⓚ |
| `sim/economy-engine.browser.js` · `lab/*.html` | 재생성 | `build-econ-bundle` → `inline-engine` · `inline-battle`(자동 생성물) |

## §2 검증

| 판 | 결과 |
|---|---|
| 3시드(1020·7·42) 800일 51마을 `t17-metrics` JSON vs `origin/main` | **cmp 바이트 동일 ✅ 3/3** |
| `sim/build-econ-bundle.js --check` | **8 일치 / 0 불일치 ✅** |
| `sim/inline-engine.js --check` | 마을실험실·전쟁실험실 **최신 ✅** |
| `scripts/lab-wiring-check.js` | **실패 0 · 경고 0** |
| `scripts/test-war-world.js` | **54 통과 / 0 실패**(옛 30 + ⓗ~ⓚ 24) |
| `T295_RATION_MRL` 끔 = battle-core 비트 동일 | A/B 표 첫 줄(ration 세 값이 한 수) ✅ |
| `scripts/test-farm-life.js` | 33 PASS / 0 FAIL |
| `scripts/test-psite-server.js` | PASS |
| `scripts/test-client-globals.js` | 26 PASS / 0 FAIL |
| `scripts/test-harness-lint.js` | 42 PASS / 0 FAIL |
| `scripts/lab-modules.js --diff origin/main` | 교집합 2개(`server/villages.js` · `sim/economy-sim.js`) → 그래서 위 3시드 판을 돌렸다 |

**접점 심볼**(카드 목록 그대로): `_warDraftPids` `conscript` `econ.npcs` `tickWorldV2` `_laborMul` `_recomputeLabor` `warMobilize` `_opPackLoad` `_opPackRefund` `WAR_RATION` `storage.food` `mrl` `M_RATE` `warResolveBattle` `_warWalkoverOutcome` `_opDoSurrender` `T295_RATION_MRL`.

## §3 규칙 지킴

* 새 수 0 · 사본 0 · 손잡이 기본 끔(켜지 않았다) · 배포 0.
* `war-core` 는 열었지만 **선포·명분·항복 결단 로직은 무접촉**(군량·환급·결속 자리만).
* T288(세션5 `economy-sim-v2.js`) · T286(세션3 `lab-alloc-server`) · 세션1~9 의 `villages.js` 접점(T278 `_terrGrow` · T287 `onboarding`) 무접촉 — 이 카드는 `economy-sim-v2.js` 를 **한 줄도 안 건드렸다**(v1 생산 루프 한 줄로 끝났다).
* force push 0 · main 무접촉 · `~/mnt/Mini/durango-mini` 무접촉.

## §4 회부(구현 안 함)

* **⑤ 무기 재고·화살** — 소집 반출 / 복귀 반납 / 없는 무기는 없다(민병 강등). 품목 칸(창·도끼·화살)이 곳간에 없으면 그것부터.
* **⑥ 선포 무관 동원(기습)** — 설계 §1-4 · §7-3·4.
* **포로 몸값**(`WAR_CAP_RANSOM_FOOD`)이 아직 `storage.food` 한 칸 — ③의 남은 자리.
* `FOLLOW_CAP` 순간이동(T284 함정 1) · `WAR_OPS=0` 손잡이 제거(PM).
* **`_siegeBlock`·`_siegeOutMul`·`_recomputeSiege` 는 죽은 칸이 아니다 — 산 칸이다**(카드 §4 질문에 대한 답): `economy-sim` 생산 줄이 `_siegeOutMul` 을 야외 직업에만 곱하고(`_siegeM`), `_recomputeSiege` 가 매일 그 상태를 세운다. 재민 09-18 *"봉쇄 상태 없음 · 배수 없음"* 을 실행하려면 **위협 함수 카드와 같이** 걷어내야 한다(이 카드에서 지우면 포위의 경제 효과가 통째로 사라진다).
* 대치 n초 값(#40) · 사기 항 값(랩 A/B 뒤 재민) · 나무 콜라이더 색인 · VPS `/perf` 실측(배포 뒤).

## §5 푸시

* 가지 `batch/war-cost-0918` · 베이스 `origin/main 8c0b250`.
* 커밋 `c95d033` [승인 대기 · T295].
* 컨테이너 `git push` = **403**(프록시: 이 세션의 인가 레포 집합 밖) ⇒ 카드 규약대로 `git bundle create T295.bundle origin/main..HEAD` 를 채팅에.
  받는 쪽: `git fetch <번들> batch/war-cost-0918:batch/war-cost-0918` → 확인 → main 은 재민.
