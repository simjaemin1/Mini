**푸시 — 착지 `8aa16a0` · 바탕 `f2efb90` · 가지 `batch/routes-audit-0913` → ff → main · force 0 · `.lock` 0 · 접촉 4 파일(+432/−3) · 배포 대기 `--all`(T217+T225+T235)**

# 보고 — T242 central·존 HTTP 문 전수표 · 기계 파수꾼 (2026-09-13)

한 줄: **라우트 62 · 공개 39 · 안 문 18 · 본인 3 · 투영 2 · ⚠ 남음 25 · 파수꾼 ○ · 흔들림 둘 자리 ○○.**
**★카드의 전제가 틀렸다** — 카드는 *"남는 것은 #2·#9 뿐이어야 한다"* 고 적었는데, 실측은 **25** 다.
#2·#9 를 빼고 **23 이 더 남아 있다**(쓰기 17 · 읽힘 6). 그게 이 카드의 발견이다.
코드 0(하네스뿐 — `test-guest-identity` §⑧ 절 하나 + `e2e-onboarding` ⑧ 한 줄) · 표는 **손으로 세지 않는다**(§0-ⓑ 파수꾼이 코드에서 뽑는다).

---

## §0-ⓐ 라우트 전수표 (실측)

세는 법: `server/central.js`·`server/zone.js` 는 `http.createServer` 한 덩이라 **`if (req.url … && req.method …)` 한 줄이 곧 라우트**이고,
`server/dispatcher.js` 는 express 라 `app.get/post/…` 다. **문법 둘뿐**이고, 파수꾼이 그 둘을 정규식으로 뽑는다(손 목록 0).
정적 파일 서버(central 1452줄)와 디스패처의 `app.use(centralProxy)` 는 라우트가 아니라 **나머지 전부**라서 표 밖이다.

갈래 넷 · 판정 셋:
- 갈래 — **공개**(문턱 없음) · **안문**(`internal-door.isInternal`) · **본인**(자격증명을 들고 온다: 비밀번호·게스트 토큰) · **투영**(바깥엔 줄여서 준다 · T217)
- 판정 — **○** 공개가 뜻 · **✗** 닫혔다 · **⚠** 남았다

| # | 서비스 | 메서드 | 경로 | 갈래 | 남 읽힘 | 남 바뀜 | 부르는 쪽 | 판정 |
|---|---|---|---|---|---|---|---|---|
| 1 | central | GET | `/health` | 공개 | ✗ | ✗ | 클라 `30-n-net.js:647` · 디스패처 | ○ 공개가 뜻 |
| 2 | central | GET | `/economy/villages` | 공개 | ✗ | ✗ | 없음(대시보드용 · 클라 0) | ○ 공개가 뜻 |
| 3 | central | GET | `(접두) /economy/prices/` | 공개 | ✗ | ✗ | 클라 `51-s-side.js` | ○ 공개가 뜻 |
| 4 | central | GET | `/economy/canadia/villages` | 공개 | ✗ | ✗ | 클라 `60-t-market.js:42,195` | ○ 공개가 뜻 |
| 5 | central | GET | `/economy/canadia/prices` | 공개 | ✗ | ✗ | 클라 `60-t-market.js:43` | ○ 공개가 뜻 |
| 6 | central | GET | `/economy/canadia/tradelog` | 공개 | ✗ | ✗ | 클라 `60-t-market.js:68` | ○ 공개가 뜻 |
| 7 | central | GET | `/economy/canadia/caravans` | 공개 | ✗ | ✗ | 클라 `60-t-market.js:188` | ○ 공개가 뜻 |
| 8 | central | GET | `/economy/prices` | 공개 | ✗ | ✗ | 클라 | ○ 공개가 뜻 |
| 9 | central | GET | `/zones` | 공개 | ✗ | ✗ | 클라 `30-n-net.js:66,143,221,1556` | ○ 공개가 뜻 |
| 10 | central | POST | `/auth` | 본인 | ✗ | ✗ | 클라(로비) · 존 | ○ 자격증명이 문 |
| 11 | central | POST | `/guest` | 본인 | ✗ | ✗ | 존 · 클라(로비) | ○ 자격증명이 문 |
| 12 | central | POST | `/promote` | 본인 | ✗ | ✗ | 존 | ○ 자격증명이 문 |
| 13 | central | POST | `/check_username` | 공개 | ✗ | ✗ | 클라 `30-n-net.js:179` | ⚠ 남음 #2 |
| 14 | central | POST | `/friend/req` | 공개 | ✗ | **○** | **존만**(`central-client`) | ⚠ **남음 — 남의 것이 바뀐다** |
| 15 | central | POST | `/friend/del` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 16 | central | POST | `/friend/pending` | 안문 | ✗ | ✗ | 존만 | ✗ 닫힘 |
| 17 | central | GET | `(접두) /friends/` | 투영 | ✗ | ✗ | 존 · 클라(수만) | ✗ 줄여서 준다(T217) |
| 18 | central | GET | `(접두) /player/` | 투영 | ✗ | ✗ | 존 · 클라 `30-n-net.js:186` | ✗ 줄여서 준다(T217) |
| 19 | central | POST | `(접두) /player/` | 안문 | ✗ | ✗ | 존 · 클라 `30-n-net.js:186` | ✗ 닫힘 |
| 20 | central | GET | `/market/orders` | 공개 | **○** | ✗ | 클라 `45-t-market.js:15` | ⚠ 남음 — 남의 것이 읽힌다 |
| 21 | central | POST | `/market/order` | 안문 | ✗ | ✗ | 존(T235) | ✗ 닫힘 |
| 22 | central | POST | `/market/cancel` | 안문 | ✗ | ✗ | 존(T235) | ✗ 닫힘 |
| 23 | central | GET | `/tribes` | 공개 | **○** | ✗ | 클라 `50-i-panel.js:560,617` | ⚠ 남음 — 남의 것이 읽힌다 |
| 24 | central | GET | `(접두) /tribe/` | 공개 | **○** | ✗ | 클라 `50-i-panel.js:530` | ⚠ 남음 — 남의 것이 읽힌다 |
| 25 | central | POST | `/tribe/add_vp` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 26 | central | POST | `/tribe/treasury` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 27 | central | GET | `/wars/active` | 공개 | ✗ | ✗ | 클라 `50-i-panel.js:550,631` | ○ 공개가 뜻 |
| 28 | central | POST | `/war/declare` | 안문 | ✗ | ✗ | 존(T235) | ✗ 닫힘 |
| 29 | central | POST | `/war/end` | 안문 | ✗ | ✗ | 존(T235) | ✗ 닫힘 |
| 30 | central | POST | `/tribe/npc_upsert` | 공개 | ✗ | **○** | **존만**(NPC 길드 시딩) | ⚠ **남음 — 남의 것이 바뀐다** |
| 31 | central | POST | `/tribe/invite` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 32 | central | POST | `/tribe/invites` | 안문 | ✗ | ✗ | 존만 | ✗ 닫힘 |
| 33 | central | POST | `/tribe/invite_accept` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 34 | central | POST | `/tribe/granary` | 공개 | **○** | ✗ | **존만** | ⚠ 남음 — 남의 것이 읽힌다 |
| 35 | central | POST | `/tribe/granary_set` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 36 | central | POST | `/tribe/mode` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 37 | central | POST | `/tribe/intro` | 공개 | ✗ | **○** | **존만** | ⚠ **남음 — 남의 것이 바뀐다** |
| 38 | central | GET | `/tribe_intros` | 공개 | ✗ | ✗ | 존(`startinfo`) | ○ 공개가 뜻 |
| 39 | central | POST | `/tribe/create` | 공개 | ✗ | **○** | **클라 `50-i-panel.js:651`** | ⚠ **남음 — 남의 것이 바뀐다** |
| 40 | central | POST | `/tribe/join` | 공개 | ✗ | **○** | **클라 `50-i-panel.js:658`** | ⚠ **남음 — 남의 것이 바뀐다** |
| 41 | central | POST | `/tribe/leave` | 안문 | ✗ | ✗ | 존(T235) | ✗ 닫힘 |
| 42 | central | GET | `/terrain.json` | 공개 | ✗ | ✗ | 클라 `99-main.js:70` | ○ 공개가 뜻 |
| 43 | zone | GET | `(접두) /perf` | 공개 | **○** | ✗ | 하네스만 | ⚠ 남음 — 남의 것이 읽힌다 |
| 44 | zone | GET | `/health` | 공개 | ✗ | ✗ | 클라 `30-n-net.js:647` · 디스패처 | ○ 공개가 뜻 |
| 45 | zone | GET | `(접두) /routedbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 46 | zone | GET | `(접두) /bodydbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 47 | zone | GET | `(접두) /claimdbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 48 | zone | GET | `(접두) /followdbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 49 | zone | GET | `(접두) /friendsdbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 50 | zone | GET | `(접두) /guilddbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 51 | zone | GET | `(접두) /shelterdbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 52 | zone | GET | `(접두) /welcomedbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 53 | zone | GET | `(접두) /startinfo` | 공개 | ✗ | ✗ | 클라 `70-lobby.js:49` | ⚠ 남음 #9 |
| 54 | zone | GET | `(접두) /lifedbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 55 | zone | GET | `(접두) /roomdbg` | 안문 | ✗ | ✗ | 하네스만 | ✗ 닫힘 |
| 56 | zone | GET | `/metrics` | 공개 | **○** | ✗ | 프로메테우스(클라 0) | ⚠ 남음 — 남의 것이 읽힌다 |
| 57 | zone | POST | `/ghost_sync` | 공개 | ✗ | **○** | **이웃 존** | ⚠ **남음 — 남의 것이 바뀐다** |
| 58 | zone | POST | `/cross_damage` | 공개 | ✗ | **○** | **이웃 존** | ⚠ **남음 — 남의 것이 바뀐다** |
| 59 | zone | POST | `/handoff_prepare` | 공개 | ✗ | **○** | **이웃 존** | ⚠ **남음 — 남의 것이 바뀐다** |
| 60 | zone | POST | `/kick_player` | 공개 | ✗ | **○** | **이웃 존 · central** | ⚠ **남음 — 남의 것이 바뀐다** |
| 61 | zone | POST | `/handoff_ack` | 공개 | ✗ | **○** | **이웃 존** | ⚠ **남음 — 남의 것이 바뀐다** |
| 62 | dispatcher | GET | `/health/zones` | 공개 | ✗ | ✗ | 디스패처 자체 | ○ 공개가 뜻 |

### 실측 — 바깥에서 두드려 본 것(헤더 없음 · `CENTRAL_SECRET` 잡힌 판 = 브라우저가 보는 그것)

**쓰기 — 남의 것이 실제로 바뀌었다**

| 문 | 바깥에서 보낸 것 | 답 | 실제로 바뀐 것 |
|---|---|---|---|
| `POST /friend/req` | `{player_id:"피해자", name:"남"}` | 200 `state:requested` | 남의 밀린 요청함에 **"피해자" 가 꽂혔다** |
| `POST /friend/del` | 같은 둘 | 200 `had:true` | 그 요청을 **바깥이 도로 지웠다** |
| `POST /tribe/create` | `{player_id:"피해자", name:"훔친길드"}` | 200 | 길드가 생기고 **leader_id = 피해자** |
| `POST /tribe/treasury` | `{tribe_id:1, delta:{wood:999}}` | 200 | 금고 `{"wood":999}` |
| `POST /tribe/add_vp` | `{tribe_id:1, amount:77}` | 200 | vp 0 → **77** |
| `POST /tribe/intro` | `{player_id:"피해자", intro:"바깥이 쓴 소개문"}` | 200 | 소개문이 **바뀌었다**(리더 판정이 본문 `player_id` 다) |
| `POST /tribe/mode` | `{player_id:"피해자", mode:"invite"}` | 200 | 가입 모드가 바뀌었다 |
| `POST /tribe/granary_set` | `{player_id:"피해자", open:false}` | 200 | 곳간이 **닫혔다** |
| `POST /tribe/invite` | `{player_id:"피해자", name:"남"}` | 200 | 피해자의 이름으로 **남을 불렀다** |
| `POST /tribe/invite_accept` | `{player_id:"남", tribe_id:1}` | 200 | **남이 길드에 들어갔다**(`member_count` 2) |
| `POST /tribe/join` | `{player_id:"무소속", tribe_id:1}` | 200 | 미소속이던 사람이 **남의 길드에 들어갔다** |
| `POST /tribe/npc_upsert` | `{name:"가짜NPC", tier:"strategic"}` | 200 | NPC 길드가 **생겼다** |
| `POST /kick_player`(존) | `{player_id:<접속자>}` | 200 | **접속 중인 플레이어가 끊겼다**(`kicked` 1건 · `ws.readyState=3`) |
| `POST /cross_damage`(존) | `{targetId:<접속자>, dmg:55}` | 200 | **hp 100 → 45**(`hp_changed` 방송까지 확인) |
| `POST /ghost_sync`(존) | `{srcZone:"가짜존", players:[유령]}` | 200 | 없는 존 이름으로 **유령·건물 주입** |
| `POST /handoff_prepare`(존) | 임의 토큰·이름 | 200 | **입장 토큰 위조** |
| `POST /handoff_ack`(존) | 임의 토큰 | 200 | 진행 중인 인계를 **취소** |

**읽기 — 남의 것이 실제로 읽혔다**

| 문 | 바깥이 본 것 |
|---|---|
| `GET /tribes` | 모든 길드의 `leader_id` · `treasury_json` · `vp` · `member_count` |
| `GET /tribe/<id>` | **멤버 전원의 `player_id`·`name`·`color`** (이름 열거 창) |
| `GET /market/orders` | 호가창 전체 — 주문마다 `player_id` 가 실린다 |
| `POST /tribe/granary` | 남의 길드 곳간 개폐 상태 |
| `GET /metrics`(존) | 접속자·관측자·건물·사유지 수 |
| `GET /perf`(존) | 존 내부 상태(`econTick` · `siteLog`) |

**대조군(닫힌 것이 닫힌 채인가)** — `안문` 18 문을 바깥에서 전부 두드렸다: `/friend/pending` 401 · `POST /player/` 401 ·
`/market/order`·`/market/cancel`·`/war/declare`·`/war/end`·`/tribe/leave` 404 · `/tribe/invites` 401 · 관측창 열 전부 404.
T217·T225·T235 가 닫은 것은 **하나도 안 돌아왔다**.

### 왜 이만큼 남았나 — 한 줄

T235 가 닫은 다섯은 *"클라가 직접 부르던 문"* 이었다. 남은 **17 쓰기 문 중 15 는 존(또는 이웃 존)만 부른다** —
즉 **클라 접점 0** 이라 눈에 안 띄었을 뿐, 문법은 다섯과 똑같다(본문의 `player_id`·`tribe_id` 를 그대로 믿는다).
남은 둘(`/tribe/create`·`/tribe/join`)만 클라가 직접 부르고, 그 둘은 T235 의 `__centralCall` 문법으로 옮기면 된다.
**새 인증은 여기서도 필요 없다** — ws 접속이 본인이다(족보 179).

⚠존의 다섯(`ghost_sync`·`cross_damage`·`handoff_prepare`·`kick_player`·`handoff_ack`)은 **존↔존** 문이라
`internal-door` 가 그대로 맞는 자리다(클라가 부르지 않는다). 존의 관측창 열을 T225 가 옮긴 그 한 줄이면 된다.

## §0-ⓑ 기계 파수꾼 (`test-guest-identity` §⑧ · 6 검사)

- 코드에서 라우트를 **정규식으로 뽑는다** — central·zone 의 `if (req.url … && req.method …)` + dispatcher 의 `app.<메서드>('…')`.
- 뽑힌 62 를 표와 **양방향** 대조한다: 코드에 있는데 표에 없으면(= 새 문) 빨강 · 표에 있는데 코드에 없으면(= 지워진 문) 빨강.
- ★자명 통과 금지 — 없는 라우트(`central POST =/__t242_없는_문__`)를 목록에 끼워 넣어 **대조가 실제로 무는지** 확인한다.
- 표가 `안문` 이라 적은 라우트는 **코드에도 `isInternal` 이 붙어 있다**(표만 고치고 문은 안 닫는 일이 없다).
- 표가 `안문` 이라 적은 **18 문을 실제로 바깥에서 두드려** 전부 404·401 인지 본다(표가 아니라 서버에 묻는다).
- 셈은 파수꾼이 찍는다 — 답 한 줄의 수를 **사람이 세지 않는다**.

`test-guest-identity` **92/0 → 98/0**.

## §0-ⓒ 대본 절 흔들림 둘 귀속

카드가 지목한 둘(T235 §0-ⓒ ⚠): `납품이 화면 알림으로 돌아온다` · `심각도 0.500 < 0.490`.
**같은 판 셋**(`ARC_FIRST=1 ARC_FOUND=1`)으로 다시 돌렸다.

| 판 | 결과 | `납품 알림` | `심각도` 실측 | 호 |
|---|---|---|---|---|
| 1 | 75/0 | ✓ | `0.480 < 0.490` | 28/28 |
| 2 | 75/0 | ✓ | `0.480 < 0.490` | 28/28 |
| 3 | 75/0 | ✓ | `0.480 < 0.490` | 28/28 |

**재현률 0/3.** (덧붙여 `ARC` 없는 판 하나도 75/0.) 둘 다 이 판에서는 안 났다 — 그래서 **눈으로 고치지 않는다.**
대신 **여유를 쟀다**, 그게 귀속의 근거다.

### ② `심각도` — **읽는 시점**이다(문턱 아님) · 한 줄 고쳤다

| 값 | 실측 | 출처 |
|---|---|---|
| 도착 허기 | **56.0** (세 판 전부 · 판 불변) | `[T203]` 이 정한 시작값 |
| ⑧ 시점 허기 | **52.0** (심각도 0.480) | 납품 루프·쉼터·하루 정산을 다 지난 뒤 |
| 문턱 | **51.0** (심각도 0.490) | `body.js` `STAGE_AT.hunger[0] + STAGE_HYST` 정본 |
| **여유** | **1.0** | ← 여기가 원인 |

⇒ 문턱이 틀린 게 아니라 **읽는 자리가 틀렸다.** 그 줄이 말하는 것은 *"**도착하자마자** 배고픈 게 아니다"* 인데,
읽는 자리는 도착이 아니라 **한참 뒤**였고 그 사이 허기가 4 떨어져 여유가 1.0 밖에 안 남았다.
앞 절(납품 라운드 수·쉼터·냉각 대기)이 조금만 길어지면 넘는다 — T235 2판의 `0.500`(허기 50.0)이 그것이다.

**한 줄 고침(T202 ⑩ 문법 — 재려는 순간을 제대로 잡는다):** ③ 이 **이미 읽은 도착 게이지**(`gauges`)를 ⑧ 이 쓴다.
다시 읽는 줄 하나가 사라지고, 여유가 1.0 → **5.0**(56 vs 51)이 되며, 시작값이 고정이라 **판 흔들림이 0** 이 된다.


### 고친 뒤 — 여유가 5배

`심각도 0.440 < 0.490 · 도착 허기 56.0` (여유 0.010 → **0.050**). 시작값이 고정이라 판마다 같은 수다.

### ⚠이 카드가 새로 본 흔들림 하나 — 호 ⑩ (고치지 않았다)

여섯 판 중 **한 판**에서 **호 27/28** 이 났다(나머지 다섯은 28/28 · 고친 뒤 두 판 포함). 걸린 걸음:
`⑩ 무길드 마을 — 소개문은 빈 칸 · 길드 잠금은 안 걸린다`.
실측 상세: `무길드 intro="" · 길드 마을 intro="돌칼 사람들은 강가에 산다" · 잠금=false ·
["오늘 몫은 다 꺼냈다 (한도 0)", …]` — **재려던 것(잠금이 안 걸린다)은 다 참인데**,
조건이 `(w4.gave || w4.famine)` 라 **한도 0** 으로 물러난 판을 못 센다.

⇒ **T202 ⑩ · T235 ⑪ 과 똑같은 자리다** — 인출을 막는 갈래는 셋(`locked`/`famine`/`quota`)이고
그중 **잠금만이 이 걸음의 대상**이다. 한 줄(`|| w4.quota`)이면 닫힌다.
⚠이 카드는 **코드 0** 이고 ③ 의 한 줄은 대본 절 몫이라, **여기는 손대지 않고 회부한다**(§1-5).

### ① `납품 알림` — **읽는 창/시점**이다 · 귀속만(안 고친다)

읽는 자리: `village_deliver` 를 보낸 **1600ms 뒤**에 `(window.__notices || []).slice(-12)` 를 본다.
⚠**바로 다음 줄**은 같은 순간을 **상태로** 잰다 — `window.__onbFxN`(`70-lobby.js:345`, 주석: *"알림은 40칸
링버퍼라 놓칠 수 있다 … **상태로 센다**"*). 즉 **이 프로젝트는 이 읽기가 못 미덥다고 이미 판정했고**,
옆 줄만 고쳐 두고 이 줄은 남겨 뒀다.

두 갈래가 남는다 — (ⓐ) 알림이 1600ms 안에 **안 왔다**(시점) · (ⓑ) 왔는데 **뒤 12칸 밖으로 밀렸다**(창).
T235 1판의 증거(같은 로그 뒤쪽에 `"곳간에 쌓였다."` 와 `deliv:1` 이 **있다**)는 ⓐ 쪽이지만,
**이 판에서 안 나므로 둘을 가를 수 없다.** ⇒ **귀속만 하고 고치지 않는다**(못 재는 것을 고치면 그게 주사위다).

**고칠 때의 한 줄은 적어 둔다:** `__onbFxN` 과 같은 자리에 납품 알림용 상태 훅을 하나 두고
(`showNotice` 옆 한 줄), 하네스는 `fxSeen` 처럼 **라운드를 가로질러** 그 상태를 센다.
⚠그건 **클라 한 줄**이라 이 카드(코드 0)의 몫이 아니다 — 회부 §1-5.

## §1 회부

1. **⚠ 남은 23**(§0-ⓐ) — 카드 하나로 닫을 수 있다. 쓰기 17 중 15 는 **존만 부르므로 `internal-door` 한 줄씩**(클라 diff 0),
   `/tribe/create`·`/tribe/join` 둘만 T235 의 `__centralCall` 문법으로 옮기면 된다. 읽기 6 은 **판정이 먼저**다
   (`/tribes`·`/tribe/<id>`·`/market/orders` 는 "길드 명부·호가창은 공개가 뜻인가"가 PM 판정이고, `/metrics`·`/perf` 는 운영용이다).
2. **#2 `/check_username`** · **#9 `/startinfo?as=`** — 종전대로 #27 로비 순서에서 재민이 판정한다.
3. `CENTRAL_SECRET`(재민) · 별명(#23) · `nippon` 시딩(Z) · 첫 의뢰 정본(#18).
5. **`납품 알림` 흔들림의 클라 한 줄**(§0-ⓒ ①) — `__onbFxN` 옆에 납품용 상태 훅 하나. 이 카드는 코드 0 이라 안 했다.
   그리고 **호 ⑩ 의 `|| w4.quota` 한 줄**(§0-ⓒ) — T202 ⑩ · T235 ⑪ 과 같은 문법. 둘 다 `e2e-onboarding` 한 줄씩이다.
6. **배포 #27 은 `--all` 그대로** — T217+T235 가 아직 안 올라갔다. 이 카드는 코드가 하네스뿐이라 배포를 새로 만들지 않는다.

## §2 검증

| 하네스 | 결과 |
|---|---|
| `test-guest-identity` (§⑧ 신설 6건) | **92/0 → 98/0** |
| `e2e-onboarding` (`ARC_FIRST=1 ARC_FOUND=1`) | 75/0 · **호 28/28** — **6판 중 5판**(1판만 §0-ⓒ 의 ⑩ 한도 0 · `● 미검증`) |
| `e2e-friends` | 93/0 |
| `e2e-guild` | 97/0 |
| `e2e-ui` | 133/0 |
| `test-client-globals` | 26/0 (새 전역 0 — 클라 무접촉) |
| `test-harness-lint` | **41/0** |
| `lab-modules --diff origin/main` | 내 diff 2 · **교집합 0** ⇒ 3시드 800일 A/B 갈음(공통.md §2 ①) |

접촉 파일 넷: `scripts/test-guest-identity.js`(§⑧) · `scripts/e2e-onboarding.js`(⑧ 한 줄) ·
`인계/N-네트워크.md`(N-전수표) · `보고/T242_2026-09-13.md`(새 파일).
**`server/**` 무접촉 · `public/client/**` 무접촉 · `sim/**` 무접촉.**

⚠`test-harness-lint` ② 가 처음에 빨갰다 — 표의 판정 값에 `⚠` 를 썼더니 *"판정 자리에 이모지 0"* 에 걸렸다.
판정 코드를 글자(`공개뜻`/`닫힘`/`쓰기남음`/`읽힘남음`/`회부2`/`회부9`)로 바꿔 닫았다. 표시용 기호는 **보고에만** 쓴다.
