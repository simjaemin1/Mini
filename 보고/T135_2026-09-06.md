# T135 — 이식: 나무 종 · 열매 · 벌목 부등식 · 채집 실체화 **(승인 대기)** (2026-09-06 · 세션3)

> 커밋 `[승인 대기 · T135]` · 베이스 `31e2bd1b`(리베이스 1회 · 충돌 1건 — `인계/회부.md` 병합) ·
> 브랜치 **`batch/trees-0906`** · **main 금지**(승인 게이트) · **force 0회**
> 접촉 `sim/economy-sim.js`(**대체 1자리** + 부산물 게이트 1줄) · `server/trees.js`(신설 · 정본) ·
> `server/trees.json`(구운 표 · 손편집 금지) · `server/chunk.js`(종 축 · 종별 재생 시계 · 숲 밀도 정본) ·
> `server/villages.js`(주입 · 부등식 · 채집 현장) · `server/zone.js`(따기 동사) · `server/itemlabel.js`(곁표) ·
> 클라 셋(`30-n-net`·`43-i-icon`·`46-h-verbs` — **표만 읽는다** · 종 목록 0).
> 신규 하네스 `scripts/test-trees.js`(`@regress` · **54 PASS / 0 FAIL**).

---

## 0. 한 줄 — **답이 갈렸다. 승인 게이트에 그대로 올린다.**

랩(T123)이 시킨 대로 **대체**로 넣었다(얹지 않았다). 그리고 3시드가 이렇게 답했다:

| 시드 | 인구 | 소멸 |
|---|---|---|
| 1020 | 6,459 → **6,336** (−1.9%) | 0/51 → **0/51** |
| 7 | 7,125 → **7,197** (+1.0%) | 0/51 → **0/51** |
| 42 | 6,848 → **6,208** (−9.3%) | 0/51 → **1/51** ⚠ |

**시드 1020 스크린은 통과(소멸 0/51)했고, 3시드에서 42가 마을 하나를 잃었다.**
자동 승인하지 않는다 — 카드가 정한 대로 **표와 귀속을 붙여 재민 판정에 올린다**(§4).

그리고 손해가 **어디서 오는지는 한 줄로 말할 수 있다**: `land.wood` 가 바닥(0.45)인 마을 **17곳**은
나무가 0 그루로 유도되어 **열매를 한 톨도 못 받는다**. 그 17곳이 −16.5%, 나머지 34곳은 −6.4%다.

---

## 1. §0 실측 (셋)

### ⓐ 무엇을 걷어내야 하나 — **카드가 지목한 `fruit` 은 나무의 것이 아니다**

카드 ①은 *"추상 `fruit`(`fert×0.8+0.25`)를 걷어내라"* 였다. 그런데 그 가중치는 **비옥도**에 달려 있다 —
숲(`land.wood`)이 아니라. 같은 표에서 숲에 달린 것은 `mushroom`·`twig` 다.
⇒ econ 의 `fruit` 은 **들·풀밭의 야생 열매**이고, 그걸 내는 실체는 **덤불**(`berry_bush`)이다.
그리고 덤불은 숲 그리드에 없다 — 청크당 0.75그루 vs 나무 340그루.

**그래서 `fruit` 은 안 걷었다.** 걷으면 공급원 없이 바구니의 20%를 지우는 셈이다.
걷은 것은 **나무가 실제로 대는 품목뿐**이다(T123 이 어휘를 econ 정본과 맞춰 둔 그 넷):

| 걷어낸 것 | 어디서 | 몫 |
|---|---|---|
| `chestnut`(밤) | 채집 바구니 | 4.1 ~ 4.6% |
| `grape`(머루) | 채집 바구니 | 1.3 ~ 2.6% |
| `acorn`(도토리) | **벌목꾼 부산물** 0.06/일 | — |
| `mulberry`(뽕잎) | (바구니에 원래 없다) | — |

바구니에서 빠지는 몫 **5.9 ~ 7.0%**(척박→비옥). 남는 것은 재정규화로 **×1.06 ~ ×1.08 늘어난다** —
채소·버섯·약재·돌은 **안 끊긴다**(T123 마을5 사고가 여기서 재발하지 않는 이유다).
1산출당 식량환산은 0.2359 → 0.2222(−5.8%) · 0.2472 → 0.2350(−4.9%) · 0.2573 → 0.2445(−5.0%).

> **덤불 실체화는 별도 카드로 회부한다**(§5-1). `fruit` 을 실체로 바꾸려면 덤불이 먼저 숲만큼 모델링돼야 한다.

### ⓑ 종을 싣는 자리

나무는 **두 곳에서 난다**(T122 가 적어 둔 그대로): 일반 자원 루프와 **숲 그리드**(청크당 ~340그루 · NPC 가 베는 본체).
둘 다 `_spAt(x, y)` 하나를 부른다 — **자리 × 존의 함수**(`Crops.h32` 호출 · 사본 0 · 주사위 0).
⚠`seedKey` 가 아니라 **좌표**로 묻는다: 키는 그리드 인덱스라 간격이 바뀌면 같은 나무의 종이 바뀐다.

T122 는 이 자리에 회부를 적어 뒀다 — *"종은 아직 하나(`tree`)다. 종별 표는 T123 카탈로그가 온 뒤다."*
**그 회부가 여기서 닫힌다**: `regrowStageOf(type, elapsed, species)` 가 종을 하나 더 받는다(안 주면 종전 그대로).

### ⓒ ★랩 값 ↔ 서버 값 — **두 시계가 25배 어긋난다** (재민 판정 자리)

| 시계 | 성목까지 | 실시간 | 출처 |
|---|---|---|---|
| **랩** 로지스틱 회복(`r=0.004` · `K=100`) | 50%→95% **2.02년** · 25%→95% 2.77년 · 10%→95% **3.52년** | 12~21일 | 전쟁실험실 `L_WOODREGLOG` |
| **T123 표**(종별) | 머루 4 · 개암 6 · 버들 7 · 산뽕 8 · 밤 12 · 소나무 **25** · 잣 30 · 참나무 **40** | 24~243일 | `server/trees.json` |
| **T122 논문**(소나무 · 모든 종 공통) | 그루터기 22 → 성목 **52년** | 316일 | Lee et al. 2018 |

카드 지시대로 **둘 다 살려 두고 손잡이 하나로 고른다**:
`T135_MATURE=lab`(**기본** · 스크린도 이 값) — 표의 종별 `mature`, 그루터기 기간은 **T122 의 비율(22/52)** 그대로.
`T135_MATURE=t122` — 종전 그대로(모든 종 22/52).
⇒ 참나무는 그루터기 16.9년 → 성목 40년, 머루는 1.7년 → 4년. **새 수 0**(표의 값과 T122 의 비율뿐).

---

## 2. 넣은 것 (넷)

### ① 대체 — `economy-sim.js` **한 자리**

```js
const _replSet = (_realFn && _W.forageRealItems) ? _W.forageRealItems : null;
let repW = 0; for (...) if (_replSet.indexOf(r) >= 0) repW += w;      // 걷어낸 몫
const repShare = repW / sumW;
for (const [r, w] of Object.entries(yields)) {
  if (_replSet && _replSet.indexOf(r) >= 0) continue;                 // ★걷어낸다
  addProduce(r, baseAmt * (w / sumW));
}
if (_realFn && repShare > 0) {                                        // ★그 자리를 실물이 채운다
  const got = _realFn(v, baseAmt * repShare);
  if (got) for (const r in got) if (got[r] > 0) addProduce(r, got[r]);
}
```

**얹지 않는다** — 실체는 *걷어낸 몫만큼만* 들어온다. **새 수 0** — 무엇을 걷을지는 주입된 목록이,
얼마나 걷을지는 **걷어낸 몫 자체**가 정한다. 부산물 루프에도 같은 게이트 한 줄(벌목꾼 도토리).
`foragerYieldsFor` 표 자체는 **안 건드렸다** — 표가 아니라 *쓰는 자리*에서 거른다.

### ② 종 축 · 열매 · 따기 동사

* 표는 `server/trees.json`(랩 표의 전사 · `build-trees.js` 가 굽는다 · 손편집 금지 · `--check`).
  T123 때 `lab/trees.json` 이던 자리를 **옮겼다** — 굽는 대상은 하나뿐이다.
* `server/trees.js` 가 **읽고 파생만** 한다(`server/crops.js` ← `crops.json` 과 같은 문법 · econ 무접촉).
* 열매 규약 넷은 랩 그대로: 재고 `fy × 크기` · **연 1회**(결실철 첫 정산) · **겨울 소멸** · **볼 때 정산**(틱 0).
* **따기 동사** — `RESOURCE_VERBS_ALT = { tree: '열매 따기' }`. 곁표를 둔 이유는 본표의 키 집합이
  `RESOURCE_HP_TABLE` 전수와 맞대어져 있기 때문이다(`test-itemlabel ⑭`). **새 자연물 종류를 안 만들었다** —
  같은 나무의 다른 손짓이고, **`hp` 를 한 번도 안 만진다**(따도 나무는 안 죽는다).

### ③ 벌목 부등식 — 새 수 0

`w(목재)×목재수율 ≥ 성목햇수 × w(열매)×연간열매수율` ⇒ 벤다. 사전식: 목재 전용 종이 먼저,
없을 때만 열매종을 부등식에 건다. `w` 는 **econ 정본**(`world.priceFn`)을 부르기만 한다 — 표를 다시 안 만든다.
값을 모르면 종을 안 가린다(랩과 같은 계약 = 종전 행동).

### ④ 채집 현장 — 결실철엔 열매나무를 **먼저** 본다

⚠랩이 여기서 한 번 틀렸다(T123 §2④): 열매를 따도 채집 재고가 안 줄어 채집꾼이 스스로 자리를 안 옮긴다.
그래서 "고갈되면 옮긴다"가 아니라 **결실철엔 먼저 본다**로 넣었다.

---

## 3. 검증

| 검사 | 결과 |
|---|---|
| `scripts/test-trees.js`(신설 · `@regress`) | **54 PASS / 0 FAIL** |
| `scripts/test-regrow.js`(T122 무변) | **58 PASS / 0 FAIL** |
| `scripts/test-itemlabel.js`(키 집합 규약) | **45 PASS / 0 FAIL** |
| `scripts/test-farm-metrics.js`(T117 무변) | **46 PASS / 0 FAIL** |
| `scripts/test-crops.js` | **170 PASS / 0 FAIL** |
| `scripts/e2e-nature.js`(`@pixel`) | **86 통과 / 0 실패** |
| `scripts/lab-wiring-check.js` | 실패 0 · 경고 0 |
| `sim/inline-engine.js --check` · `inline-path --check` | 두 랩 다 `최신 ✅` |
| `scripts/build-trees.js --check` | `최신 ✅` |
| **러너 전수** | 134종 전수 + 빨강 17종 **HEAD 기준선 대조** — 아래 §3-b (T135 가 만든 빨강 **0**) |
| **되돌림 `T135_TREES=0`** | 3시드 여덟 수가 **T86 기준선과 한 자도 안 다르다**(아래 표 OFF 열) |

### 3-b. 러너 전수 — 빨강 넷은 **전부 이 컨테이너의 것**이다

러너를 통째로 돌렸다(자동 발견 134종). 빨강 넷이 나왔고, 넷 다 **단독 재실행 + 되돌림 대조**로 갈랐다:

| 하네스 | 러너에서 | 단독 ON | 단독 OFF(`T135_TREES=0`) | 판정 |
|---|---|---|---|---|
| `test-site-memo ③` | ✗ 113ms ≤ 59ms | ✗ **116ms** | ✗ **115ms** | **양 팔 동일** — 컨테이너 성능(2코어) |
| `test-tick-slicer` | ✗ 8건 | ✗ 8건 | ✗ **4건** | **양 팔 빨강** · 하네스 **자기 전제가 깨진다**(이벤트 루프 막힘 16.1초 · 대조 마을 0곳 · 두 팔이 다른 날까지 돎) ⇒ **여기선 판정 불가** |
| `e2e-cold` | ✗ 2/52 | ✗ **1/52**(⑦㉣ 짠물 — 그때 **한사리 썰물**이라 갯벌 채집이 먼저 걸렸다) | ✗ **1/52**(① 한겨울 밤 0.5634) | 두 팔이 **서로 다른 항목**에서 빨강 ⇒ 조석·계절 의존 흔들림 |
| `e2e-downed` | ✗ (전제: B 가 반경 밖으로 못 나갔다 48px) | ✓ **35 PASS / 0 FAIL** | — | **부하 인공물** — 단독으로는 초록 |

이어서 남은 32종(전부 `e2e-*`)을 2차로 돌렸고 17종이 빨갰다.
**여기서 방법을 한 단계 올렸다** — 되돌림 손잡이(`T135_TREES=0`)로는 못 가르는 자리가 있다:
`require('./trees')` 자체가 터지면 두 팔이 **똑같이** 빨갛기 때문이다.
⇒ `git worktree` 로 **`origin/main`(31e2bd1b) 을 그대로 깔고** 같은 17종을 같은 순서로 돌려 **기준선**을 떴다.

| 하네스 | 내 브랜치(러너 부하 중) | **HEAD(origin/main)** | 내 브랜치 **단독** | 판정 |
|---|---|---|---|---|
| `e2e-membership`·`e2e-mountain`·`e2e-mtfoot`·`e2e-mtocc`·`e2e-rtt`·`e2e-rumor`·`e2e-salt`·`e2e-thirst`·`e2e-verbs`·`e2e-weight` (10종) | ✗ | **✗ 같음** | — | **HEAD 도 빨강 — T135 무관** |
| `e2e-inv` | ✗ ⓪ | ✓ | **✓** | 부하 인공물(다른 존이 포트를 쥐어 손잡이 env 가 안 닿았다) |
| `e2e-mtcut` · `e2e-npcsprite` · `e2e-preserve` · `e2e-weather` | ✗ | ✓ | **✓** | 부하 인공물 |
| `e2e-mtfuzz` | ✗ | ✓ | **✓ 10/10**(직전엔 `ERR_CONNECTION_REFUSED :3010`) | 포트 점유 — 인프라 |
| `e2e-move` | ✗ | ✓ | **✓ 37/0 (3회 중 2회)** · 1회는 미끄러짐 0.5px 차 | 하위픽셀 흔들림 |

⇒ **T135 가 만든 빨강은 0** 이다. 모든 빨강이 ⓐ **HEAD 에서도 빨갛거나** ⓑ 단독 재실행에서 초록이다.
그리고 **T135 가 실제로 만지는 층**은 전부 초록이다 —
`test-trees 54/0` · `test-regrow 58/0` · `test-crops 170/0` · `test-farm-metrics 46/0` ·
`test-itemlabel 45/0` · `test-lab-trees 44/0` · `test-client-globals 20/0` · `e2e-nature 86/0` · `lab-wiring-check 실패 0`.

> **족보 후보:** *"되돌림 손잡이는 자기 코드가 **도는** 동안만 가른다. `require` 가 터지면 두 팔이 같이 빨갛다 —
> 그때 가르는 것은 손잡이가 아니라 **`origin/main` 을 깔아 놓은 worktree** 다."*

⚠**PM 야간 러너(맥)에서 한 번 더 봐 주기 바란다.** 이 컨테이너(2코어)는 e2e·픽셀 층을 못 잰다:
`test-tick-slicer` 는 이벤트 루프가 16초 막히고, 산 렌더는 "0장"이며, 마을 시딩은 "0곳"이다 — 전부 HEAD 도 같다.

### ★되돌림이 곧 증명이다

OFF 열(`T135_TREES=0`)이 **T86 기준선 정본**과 정확히 같다 — 인구 6,459/7,125/6,848 · 소멸 0/51 ·
무기Q 939/1,049/1,003 · 확장셀 6,365/5,946/6,003 · 도구Q 10,803.3/12,009.0/11,405.3 · 보존식 740.7/606.3/870.3.
⇒ 이 카드가 움직인 것은 **손잡이 하나 안에 전부 들어 있다.**

---

## 4. ★승인 게이트 — 표 셋

### ⓐ 여덟 수 · 3시드 A/B (800일 · 51마을)

| 수 | 1020 OFF | 1020 ON | 7 OFF | 7 ON | 42 OFF | 42 ON |
|---|---|---|---|---|---|---|
| 인구 | 6,459 | **6,336** (−1.9%) | 7,125 | **7,197** (+1.0%) | 6,848 | **6,208** (−9.3%) |
| 소멸 | 0/51 | **0/51** | 0/51 | **0/51** | 0/51 | **1/51** ⚠ |
| 무기Q | 939 | 974 (+3.7%) | 1,049 | 1,163 (+10.9%) | 1,003 | 866 (−13.7%) |
| 확장셀 | 6,365 | 5,861 (−7.9%) | 5,946 | 7,077 (+19.0%) | 6,003 | 4,212 (−29.8%) |
| 게시 | 2,563 | 2,878 | 2,710 | 2,582 | 2,760 | 2,688 |
| 일/건 | 1.70 | 1.60 | 1.70 | 1.73 | 1.65 | 1.71 |
| 도구Q | 10,803.3 | 11,184.4 | 12,009.0 | 12,659.4 | 11,405.3 | 11,274.2 |
| 보존식 | 740.7 | 477.1 | 606.3 | 949.1 | 870.3 | 653.6 |

### ⓑ 귀속 (셋)

1. **걷어낸 몫** — 채집 바구니의 **5.9~7.0%**(밤·머루) + 벌목꾼 부산물 도토리 0.06/일.
   남는 것은 재정규화로 ×1.06~1.08 **늘어난다** — 채소·버섯·약재·돌은 안 끊긴다.
2. **실체가 채운 몫** — 나무가 있는 마을(34/51)의 연간 예산 **중앙 127단위**(최대 679).
   나무가 0 인 마을(17/51)은 **0**. 실체는 있는 만큼만 온다 — 그게 공짜 점심을 걷었다는 뜻이다.
3. **손해가 그 선을 정확히 따라간다** — 시드 42:
   나무 있는 34곳 인구 4,838 → 4,530(**−6.4%**) · 나무 0 인 17곳 2,010 → 1,678(**−16.5%**).
   그 17곳은 전부 `land.wood == 0.45` — **`livelihood.FLOOR.wood` 바닥값**이다.
   즉 손해는 "열매가 없어서"가 아니라 **"숲 몫의 역함수가 바닥에서 0 을 낸다"**에서 온다.

### ⓒ ±30% 마을 표 (시드 42 · 17/51곳)

| 마을 | ON→OFF | Δ | land.wood | 나무 | 연간예산 | 채집꾼 ON/OFF |
|---|---|---|---|---|---|---|
| 농촌12 | **0 → 42** | **−100%(소멸)** | 0.91 | 1,232 | 254 | 0 / 6 |
| 농촌16 | 27 → 126 | −78.6% | 0.65 | 535 | 110 | 6 / 28 |
| 농촌15 | 21 → 73 | −71.2% | 0.63 | 482 | 99 | 3 / 12 |
| 임업4 | 46 → 139 | −66.9% | 1.68 | 3,293 | 679 | 3 / 15 |
| 어촌8 | 38 → 101 | −62.4% | **0.45** | **0** | **0** | 6 / 31 |
| 농촌9 | 39 → 102 | −61.8% | 1.05 | 1,606 | 331 | 20 / 31 |
| 농촌4 | 46 → 93 | −50.5% | **0.45** | **0** | **0** | 8 / 17 |
| 어촌11 | 100 → 186 | −46.2% | **0.45** | **0** | **0** | 22 / 42 |
| 농촌22 | 67 → 117 | −42.7% | **0.45** | **0** | **0** | 3 / 20 |
| 어촌1 | 123 → 189 | −34.9% | **0.45** | **0** | **0** | 22 / 33 |
| 광산6 | 70 → 101 | −30.7% | **0.45** | **0** | **0** | 14 / 2 |
| 농촌13 | 136 → 100 | **+36.0%** | 0.78 | 883 | 182 | 27 / 29 |
| 농촌18 | 46 → 29 | **+58.6%** | 0.60 | 402 | 83 | 16 / 7 |
| 광산4 | 155 → 95 | **+63.2%** | **0.45** | **0** | **0** | 29 / 11 |
| 농촌3 | 163 → 99 | **+64.6%** | 0.47 | 54 | 11 | 36 / 29 |
| 농촌8 | 72 → 31 | **+132.3%** | 0.46 | 27 | 6 | 25 / 10 |
| 농촌21 | 125 → 36 | **+247.2%** | 0.54 | 242 | 50 | 41 / 8 |

마을별 Δ 분포: 최저 −100% · **중앙 −6.3%** · 최고 +247%.
⇒ **재분배가 크다.** 잃은 마을의 노동·교역이 이웃으로 옮겨 간다(시장이 도는 증거이기도 하고,
그만큼 이 변경이 마을 단위로는 거칠다는 뜻이기도 하다).

### ⓓ 소멸 하나 — 농촌12

⚠**열매가 없어서 죽은 게 아니다.** 농촌12 는 나무 1,232그루 · 예산 254단위로 상위권이다.
OFF 에서 42명(하위권 · 한계 마을)이었고, ON 에서 **채집꾼이 6 → 0** 이 되며 식량 1,872 → 0 으로 무너졌다.
직업 배분이 채집을 버린 자리다 — T123 이 랩에서 본 그 연쇄(가격 → 직업 → 바구니 전체)의 **약한 판**이다.

---

## 5. 재민 판정거리 (셋)

1. **`land.wood` 바닥의 역함수** — `forShare = (wood − 0.45) / 2.3` 은 바닥 마을에 **나무 0** 을 준다.
   바닥은 원래 *"숲이 없어도 땔감은 좀 난다"* 는 뜻이지 *"나무가 한 그루도 없다"* 가 아니다.
   ⇒ 바닥 마을에 최소 나무를 줄 것인가, 아니면 **정말로 없는 게 맞다**고 볼 것인가. (17/51 곳의 −16.5% 가 여기 달렸다.)
2. **재생 앵커** — §0-ⓒ 의 두 시계. 기본은 랩 값(`T135_MATURE=lab`)으로 세워 뒀다. 논문값으로 갈면 손잡이 하나다.
3. **승인 여부** — 시드 42 의 소멸 1/51 을 받아들일 것인가. 받아들이지 않는다면 1번이 첫 후보다.

---

## 6. 회부 (넷)

1. **덤불 실체화** — econ `fruit`(들·풀밭 야생 열매)의 실체는 덤불이다. 지금 덤불은 숲 그리드에 없다
   (청크당 0.75 vs 나무 340). 그 층이 서야 `fruit` 도 대체할 수 있다.
2. **심기(T124)** — 성목까지 실시간 24~243일. 심기가 서면 부등식 오른쪽이 *"잃는 것"* 에서 *"미루는 것"* 으로 바뀐다.
3. **목재 품목 분할** — `specialty.js` 에 `pine_log`·`oak_log`·`birch_log`·`cedar_log` 가 **이미 있다**.
   나누면 `w(목재)` 가 종별로 갈리고 `char`(숯 수율) 축이 비로소 쓰인다.
4. **도토리 사료(목축)** — `w(acorn)` 이 오르면 부등식 오른쪽이 커져 **목축이 참나무를 지킨다**.
   숯(참나무 `char` 1.00 — 최고)과 정면으로 부딪히는 자리다.

---

## 7. 푸시

* 브랜치 **`batch/trees-0906`** · **main 금지**(승인 게이트) · force push **0회**
* 베이스 `31e2bd1b` · 커밋 **`b9191ac4`** · 리베이스 1회(충돌 1건 — `인계/회부.md`, 양쪽 절을 나란히 두는 병합)
* 원격 `refs/heads/batch/trees-0906` = `b9191ac4…` · **원격 `main` 은 `31e2bd1b` 그대로**(한 번도 안 밀었다)
* push 뒤 `.lock` 전수 **8건 — 전부 `.git/_stale_locks/`** · `refs/**`·`objects/**` 새 잠금 **0건**
* 번들 `_bundles_pushed/t135.bundle` + `_MANIFEST.txt` 기입
* ⚠컨테이너에서는 못 민다(git proxy 403) — 번들 → 디바이스 → PAT askpass 절차(T123 과 동일)

---
---

# T135 **2판** — 예산이 나무를 한 곳만 셌다 (2026-09-06 · 같은 브랜치 이어서)

> 커밋 `[승인 대기 · T135 2판]` **`f55f9315`** · 베이스 `86fcdd55`(같은 브랜치 위에 · 리베이스 0회) ·
> **main 금지** · **force 0회** · push 뒤 `.lock` 전수 **8건 전부 `.git/_stale_locks/`** · `refs/**`·`objects/**` 새 잠금 **0**
> 원격 `refs/heads/batch/trees-0906` = `f55f9315…` · 원격 `main` 은 그동안 `7813d9d5` 로 움직였다(**우리가 안 밀었다**)
> 접촉 `server/trees.js`(`treeCountOf` **한 자리** — 두 항의 합 + biome 조회) ·
> `server/chunk.js`(밀도 정본 둘 수출 — `scatterTreesPerCell`·`treeShareOf`) · `scripts/test-trees.js`(⑪ 절).
> **`economy-sim.js` 대체 자리 무접촉 · `annualFruitBudget` 이하 무변 · 랩 무접촉 · 재생 손잡이 무변.**

## 2-0. PM 판정이 맞았다 — 그리고 실측이 그걸 뒤집었다가 다시 세웠다

PM: *"판정거리 1번은 '바닥 마을에 나무를 줄까'가 아니다. 예산 유도가 나무를 한 곳만 셌다."*
맞다. 1판 보고 §0ⓑ 가 스스로 *"나무는 두 곳에서 난다"* 고 적어 놓고, 예산은 **숲 그리드만** 셌다.

⚠**그런데 §0 을 두 번 재야 했다.** 처음엔 청크를 **256px** 로 굽고 쟀다 —
라이브는 `zone.js:511` 이 `new ChunkManager(w, h)` 를 chunkSize 없이 부르므로 **1024px** 다(`CHUNK_SIZE` 기본값).
청크당 자원 수(`RESOURCES_PER_CHUNK = 5`)는 **청크 하나당**이라 256 으로 재면 밀도가 **16배**로 부푼다.
그 판으로는 "일반 루프가 실물의 58.8%" 라는 수가 나왔고, 그건 **거짓**이다. 라이브 값으로 다시 재니 **9.2%** 다.

> **족보 후보:** *"청크당 상수를 셀당으로 바꿀 땐 청크 크기를 코드에서 읽어라 —
> 그 수는 하네스가 쓰는 청크가 아니라 **서버가 부르는 청크**의 것이다."*

## 2-1. §0 실측 (셋)

### ⓐ 일반 자원 루프의 나무 밀도 — 실제 청크를 굽고 셌다 (청크 1024 · 240청크)

| 구간 | 청크 | **일반 루프 나무/청크** | (숲 그리드/청크) | 물·바위 기각 |
|---|---|---|---|---|
| 숲 없음(fCov ≤ 1.0) | 120 | **3.367** | 0.0 | 4.2% |
| 숲(fCov > 1.5 · 그리드 있음) | 120 | **3.242** | 98.0 | 5.8% |

**정본 유도** = `RESOURCES_PER_CHUNK(5) × P(tree | forest)(0.70)` = **3.5/청크**.
실측 3.24~3.37 = 유도의 **93~96%** — 차이는 물·바위 셀 기각(4~6%)이다. **식이 맞다.**
`P(tree|biome)` 는 표를 옮겨 적지 않는다 — `pickResourceType` 을 촘촘히 불러 **그 표 자신에게 물어본다**.
⇒ 셀당 밀도 = 3.5 ÷ (1024/32)² = **0.003418 그루/셀**(숲 그리드는 0.10 — 29배 성기다).

### ⓑ 17곳의 실제 나무 — 스캔 원(πR² = 61,575셀)을 통째로 굽고 셌다

| 구간 | 마을 | **실물 계**(숲그리드+일반) | 1판 유도 | 1판/실물 |
|---|---|---|---|---|
| 바닥(`land.wood == 0.45`) | 17 | **184** (1 + **183**) | **0** | **0.0%** |
| 그 밖 | 34 | 2,943 (2,757 + 186) | 1,594 | 54.2% |
| 전체 | 51 | 2,024 (1,838 + 185) | 1,063 | 52.5% |

**바닥 17곳 나무의 99.5%가 일반 루프 나무다.** 1판은 그 마을들을 "나무 0 그루"로 적었다 —
`FLOOR.wood` 가 뜻하는 *"숲이 없어도 땔감은 좀 난다"* 의 실체가 바로 그 흩어진 나무들인데.

⚠**첫 항도 아래로 잡혀 있다** — 숲 마을의 그리드 실물 2,757 vs 유도 1,594(58%).
`forestTreesPerCell()` 을 **가장 성긴 간격**(SP 상한 96)으로 부르는데 실제 숲 청크는 더 촘촘하다.
2판은 이걸 **안 고쳤다**(카드가 지목한 곳은 둘째 항이고, 첫 항은 커버리지 함수를 새로 물어야 한다) — 회부.

### ⓒ 예산 변화 — 두 항을 더하면

| 구간 | 실물 | 1판 | **2판** | 1판/실물 | **2판/실물** |
|---|---|---|---|---|---|
| 바닥 17곳 | 184 | 0 | **210** | 0.0% | **114.1%** |
| 그 밖 34곳 | 2,943 | 1,594 | **1,750** | 54.2% | **59.5%** |
| 전체 51곳 | 2,024 | 1,063 | **1,237** | 52.5% | **61.1%** |

연간 열매 예산(2판): **중앙 107.4 · 최대 1,136.7 · 최소 43.4**.
바닥 17곳 **0 → 43.4**(마을당·해당) · 그 밖 34곳 **중앙 +39.3**.
(바닥 마을이 실물의 114% 인 것은 물·바위 기각을 안 세기 때문이다 — 첫 항이 58% 로 아래로 잡히므로 **합은 여전히 아래**다.)

## 2-2. 고친 것 — `treeCountOf` 한 자리

```js
return forestCells * CH.forestTreesPerCell()                    // ① 숲 그리드
     + Math.max(0, all - forestCells) * CH.scatterTreesPerCell(biome);   // ② 흩어진 나무 ★2판
```

**새 수 0** — 둘째 항의 밀도는 청크 생성기 정본이 답한다(`RESOURCES_PER_CHUNK` × `pickResourceType` 의 `tree` 몫 ÷ 청크 셀 수).
`server/trees.js` 에는 그 수가 한 자도 없다(하네스가 검사한다). biome 도 안 적는다 — 존 설정이 답한다.
숲 셀에도 일반 루프 나무가 겹쳐 서지만 **안 센다**(아래로 잡는다).

## 2-3. 승인 게이트 표 — 3시드 재측정

### ⓐ 여덟 수 (800일 · 51마을 · OFF = `T135_TREES=0`)

| 수 | 1020 OFF | 1020 **2판** | 7 OFF | 7 **2판** | 42 OFF | 42 **2판** |
|---|---|---|---|---|---|---|
| 인구 | 6,459 | **6,640** (+2.8%) | 7,125 | **7,264** (+2.0%) | 6,848 | **6,549** (−4.4%) |
| 소멸 | 0/51 | **0/51** | 0/51 | **0/51** | 0/51 | **1/51** ⚠ |
| 무기Q | 939 | 1,091 (+16.2%) | 1,049 | 1,174 (+11.9%) | 1,003 | 1,000 (−0.3%) |
| 확장셀 | 6,365 | 4,976 (−21.8%) | 5,946 | 7,980 (+34.2%) | 6,003 | 5,314 (−11.5%) |
| 게시 | 2,563 | 2,687 | 2,710 | 2,654 | 2,760 | 2,409 |
| 일/건 | 1.70 | 1.63 | 1.70 | 1.71 | 1.65 | 1.71 |
| 도구Q | 10,803.3 | 12,580.9 | 12,009.0 | 13,404.1 | 11,405.3 | 11,113.5 |
| 보존식 | 740.7 | 747.7 | 606.3 | 1,150.7 | 870.3 | 563.6 |

### ⓑ 1판 → 2판 (같은 OFF 기준선 위에서)

| 시드 | 1판 인구 | **2판 인구** | 1판 소멸 | 2판 소멸 |
|---|---|---|---|---|
| 1020 | 6,336 (−1.9%) | **6,640 (+2.8%)** | 0/51 | 0/51 |
| 7 | 7,197 (+1.0%) | **7,264 (+2.0%)** | 0/51 | 0/51 |
| 42 | 6,208 (−9.3%) | **6,549 (−4.4%)** | 0/51 | **1/51** |

**둘째 항이 두 시드를 플러스로 돌리고 시드 42 의 손해를 절반으로 줄였다.** 그런데 **소멸 1/51 은 남았다.**

### ⓒ 소멸 하나 — 농촌12 **귀속**(카드 지시대로 고치지 않고 재기만 했다)

**열매가 아니라 석재다.** 10일마다 뜬 스냅:

| 게임일 | 인구 | 채집 | 석공 | **석재** | **도구** | 식량 |
|---|---|---|---|---|---|---|
| 0 | 8 | 0 | 0 | **189** | 60 | 368 |
| 40 | 26 | 5 | 1 | 138 | 48 | 635 |
| 80 | 26 | 5 | 1 | **5** | 41 | 913 |
| 120 | 26 | 5 | 1 | **0** | 24 | 1,130 |
| 160 | 26 | 6 | 2 | 0 | 8 | 1,280 |
| 240 | 26 | 5 | 2 | 0 | **0** | 354 |
| 320 | 26 | 2 | 2 | 0 | 10 | 219 |
| 360 | 26 | **0** | 0 | 0 | 0 | **42** |
| 400 | **3** | 1 | 0 | 0 | 1 | 0 |
| **470** | **0** | 0 | 0 | 0 | 0 | 0 |

* `land.stone = **0.25** = `livelihood.FLOOR.stone`` — **바닥이다.**
* 궤적: 석재 189 → 0(일 120) → 도구 60 → 0(일 240) → 생산 ×0.25 → 식량 1,280 → 42(일 360) → 인구 26 → 0(일 470).
* **채집꾼이 먼저 사라진 게 아니다** — 채집은 일 320 까지 2~10명으로 살아 있었고, **식량이 무너진 뒤인 일 360 에** 0 이 됐다.
* ⇒ 카드가 의심한 *"직업 배분이 바구니 −5% 에 채집을 통째로 버린다"* 는 **이 마을에선 아니다.**

★그리고 이건 **이미 알려진 궤적**이다. `sim/economy-sim.js:3374`(STONE_NET 주석)이 **농촌12 를 이름으로 적어 뒀다**:
> *"살아남지 못한 마을은 **예외 없이 land.stone = 0.25** 였고(**농촌12**·농촌19·어촌6·농촌13·농촌16), 전부 궤적이 같다 —
> 석재 189 → 0 → 도구 0 → 생산 ×0.25 → 만성 기근 → 기근 게이트가 영구 점유 → 석재 영영 0."*

⇒ **T135 가 만든 결함이 아니라, T135 가 그 마을을 문턱 너머로 반 걸음 민 것이다**(OFF 에선 42명으로 살아남는다).
석공 2명이 있어도 석재가 0 이면 못 만든다 — STONE_NET 이 이 판을 못 잡는다. **별도 판정거리**(§2-4 회부).

## 2-4. 검증

| 검사 | 결과 |
|---|---|
| `scripts/test-trees.js` | **63 PASS / 0 FAIL**(⑪ 절 신설 — 바닥 마을 > 0 · 숲 마을 1판 값 이상 · 밀도 사본 0) |
| ★돌연변이(`T135_SCATTER=0` · 자식 프로세스 + env) | 바닥 마을이 **0 으로 돌아간다** = 1판 결함 재현 · 숲 마을은 정확히 1판 값 |
| `scripts/test-lab-trees.js` | 44 PASS / 0 FAIL (무변) |
| `scripts/test-regrow.js` · `test-imsu` · `test-mining` | 58/0 · 10/0 · PASS (접점 심볼로 뽑은 하네스) |
| `scripts/lab-wiring-check.js` | 실패 0 · 경고 0 |
| 러너 전수 | **안 돌렸다**(카드 지시 — 1판이 이미 갈랐다) |

접점 심볼(`treeCountOf|annualFruitBudget|generateChunkResources|forestTreesPerCell|scatterTreesPerCell`)로 뽑은 하네스:
`test-imsu` · `test-mining` · `test-regrow` · `test-trees` · `e2e-charsprite` · `e2e-forage-village` · `e2e-move`.

**랩 무접촉** — `전쟁실험실` 은 제 나무를 `L_WOOD*` 로 센다. 랩 동형은 **원칙**(실체 = 대체)이지 나무 수 동일이 아니다.
`lab-wiring-check` 가 인라인 엔진·path 를 그대로 통과한다(`trees.js` 는 랩 적재 밖이다).

## 2-5. 재민 판정거리 (2판 뒤)

1. **소멸 1/51 을 받아들일 것인가** — 둘째 항으로 절반이 줄었지만 농촌12 는 남는다.
   원인은 열매가 아니라 **`land.stone` 바닥의 도구 죽음의 나선**(이미 알려진 궤적)이다.
2. **첫 항의 58%** — 숲 그리드 유도가 실물의 58% 다(성긴 간격으로 부른다). 고치면 34곳 예산이 더 오른다.
3. **재생 앵커** — 1판 그대로(`T135_MATURE=lab` 기본).

## 2-6. 회부 (둘 추가)

5. **`land.stone == FLOOR.stone` 마을의 도구 죽음의 나선** — STONE_NET 이 못 잡는 판이 있다(농촌12 실측 궤적 §2-3ⓒ).
   `economy-sim.js:3374` 주석이 이름까지 적어 둔 그 자리다. **econ 영역 판정.**
6. **숲 그리드 유도를 커버리지 함수로** — `forestTreesPerCell()` 을 성긴 간격 대신 그 마을의 실제 `fCov` 로 부른다.
   실물 대비 58% → 100% 에 가까워진다. `forShare`(면적 몫)에서 `fCov`(배율)를 유도하는 자리를 세워야 한다.

---
---

# T135 **3판** — 첫 항도 실물대로 · 열매 셋이 품목이 된다 (2026-09-06 · 같은 브랜치 이어서)

> 커밋 **`573a551d`** `[승인 대기 · T135 3판]` · 베이스 `1d3594d0` · 브랜치 **`batch/trees-0906`**
> 푸시 `1d3594d0..573a551d` (fast-forward) · **force 0회** · **main 무접촉**(`26da0eb5` 그대로 · `git ls-remote` 로 확인)
> `.lock` 전수: **신규 0** (기존 8개는 이미 `.git/_stale_locks/` 격리분)
> 접촉 `server/chunk.js`(`forestTreesPerCellMean` 수출) · `server/trees.js`(첫 항 한 자리) ·
> `server/specialty.js`(품목 셋 + `grape` 한글) · `lab/전쟁실험실.html`(표의 오디 아이디) ·
> `server/trees.json`·`sim/economy-engine.browser.js`·`lab/*.html`(재굽기) · `scripts/test-trees.js`(⑫ 절).
> **둘째 항 무변 · 대체 자리 무변 · 농촌12 무접촉 · 재생 손잡이 무변 · 아이콘 0.**

## 3-0. 한 줄 — **소멸이 세 시드 모두 0/51 이 됐다**

승인 게이트가 처음부터 요구한 조건(`소멸 0/51`)이 3판에서 처음으로 세 시드 모두 섰다.
값은 치렀다 — 인구 −1.9% / −1.3% / **−9.2%**.

## 3-1. §0 실측 (셋)

### ⓐ `forShare` 에서 `fCov` 는 **유도되지 않는다** — 그래서 구간의 평균을 쓴다

카드는 *"`forShare`(면적 몫)에서 `fCov` 를 유도하는 자리 하나"* 를 지시했다. **실측이 그걸 거절했다:**

| 마을 | forShare | 숲 셀 평균 `fCov` |
|---|---|---|
| 어촌2 | 1.000 | 3.20 |
| 임업2 | 1.000 | **2.60** |
| 농촌2 | 0.331 | **3.20** |
| 농촌8 | 0.005 | 1.57 |

**상관계수 r = 0.437** — 예측하지 못한다. 지형을 다시 훑지 않고 마을별 `fCov` 를 맞히는 길은 없고,
그건 `trees.js` 가 안 하는 일이다(econ 무접촉).

★대신 **구간의 평균**을 쓴다. `forestSpacing` 은 SP 를 `[60, 96]` 에서 고르고, SP 가 그 안에서 고르다면
밀도의 평균은 **정확히 1/(하한×상한)** 이다:
`⟨GAP·c²/SP²⟩ = GAP·c²·(1/(96−60))∫₆₀⁹⁶SP⁻²dSP = GAP·c²/(60×96)` = **0.16 그루/숲셀**.
**새 수 0** — 하한·상한·빈자리 셋 다 그리드가 이미 쓰던 값이다.

| 첫 항 유도 | 34곳 Σ | 실물(93,760) 대비 |
|---|---|---|
| 1·2판 — 간격 **상한**(가장 성긴 숲) 0.1000 | 60,276 | **64.3%** |
| **3판 — 구간 평균 0.1600** | 96,441 | **102.9%** |

(실효 밀도 실측 0.1556 · 숲 셀 Σ 602,758.)
⚠**마을별로는 흔들린다** — 유도/실물이 72%~414%(중앙 135%)다. 총량이 맞을 뿐 마을마다 맞지는 않는다.
`forShare`(>1.2)와 그리드 문턱(>1.5)이 **같은 셀 집합을 고른다**는 것은 확인했다(차이 0.0000).

### ⓑ 품목 셋 — 형제를 그대로 편다 (새 수 0)

따면 `weights.kgOfOrDefault` 의 기본값 **1 kg** 이 붙던 것이 닫힌다.

| 새 품목 | 형제 | kg | 값 | 기여 | 철 |
|---|---|---|---|---|---|
| `acorn` 도토리 | `nuts` 견과류 | 0.3 | 2 | subsistence 0.4 · happiness 0.2 | foraging |
| `chestnut` 밤 | `nuts` | 0.3 | 2 | 〃 | foraging |
| `mulberry_fruit` 오디 | `fruit_berries` 산딸기 | 0.25 | 1.5 | subsistence 0.3 · happiness 0.2 | foraging |
| `grape` 머루 | (무변) | 0.6 | 2 | happiness 0.4 | foraging |

세 줄에 **수가 한 자도 없다**(`{ ...RESOURCES.nuts, ko: '도토리' }`) — 하네스가 그걸 검사한다.
**영양의 종별 차이는 안 적었다** — `economy-sim.js` 의 `FORAGE_FOOD_FACTOR`(도토리 0.25 · 밤 0.5)가 이미 쥔 축이다.
`grape` 는 아이디·값 전부 무변, 한글만 '포도' → **'머루'**(sim 이 이미 "산포도"로 부르던 그 품목이다).
`hazel`(개암)은 **목재 전용 그대로** — 열매 값의 출처가 아직 없다(회부).

**아이콘은 굽지 않았다**(카드 §4 회부). `test-icons ③` 은 *"아이콘 키가 서버 품목인가"* 를 보지
*"품목마다 아이콘이 있는가"* 를 보지 않는다 — 그래서 **초록**이다(45/0 · PASS).

### ⓒ 오디는 새 아이디다 — 잎을 안 건드린다

`mulberry` 는 **뽕나무 잎**이고 `harvest: 'farming'`(양잠 층)이다. 열매가 그 자리를 빼앗으면
잎을 읽는 자리가 조용히 뒤바뀐다. ⇒ 열매는 `mulberry_fruit`. 접점은 표 하나(`lab` `TREES` → `server/trees.json`)와
거기서 파생되는 `treeFruitKo`(클라 표)뿐이다 — 클라는 종 목록을 안 적으므로 코드 변경 0.

### ⓓ ★품목표는 손잡이 밖에서 값을 움직인다 — 되돌림을 지키려고 게이트에 넣었다

처음엔 세 품목을 그냥 더했다. 그랬더니 **`T135_TREES=0` 인 OFF 열이 6,459 → 6,580 으로 갔다.**
품목표는 econ 의 `TRADABLE` 집합이라, 항목이 늘면 나무 층과 무관하게 값과 교역이 움직인다.
그러면 *"되돌림이 곧 증명"* 이라는 이 카드의 증거가 무너진다.
⇒ 세 품목을 손잡이 안에 넣었다 — **열매가 없으면 열매 품목도 없다.** OFF 열이 T86 기준선으로 돌아왔다.

> **족보 후보:** *"품목표에 한 줄 더하는 것은 데이터 변경이 아니라 **세계 변경**이다 —
> `TRADABLE` 이 늘면 값이 움직인다. 되돌림 손잡이가 있는 카드면 품목도 그 안에 넣어라."*

## 3-2. 승인 게이트 표 — 3시드 (OFF = T86 기준선 · 1·2·3판 나란히)

| 시드 | OFF(T86) | 1판 | 2판 | **3판** |
|---|---|---|---|---|
| 1020 인구 | 6,459 | 6,336 (−1.9%) | 6,640 (+2.8%) | **6,337 (−1.9%)** |
| 1020 소멸 | 0/51 | 0/51 | 0/51 | **0/51** |
| 7 인구 | 7,125 | 7,197 (+1.0%) | 7,264 (+2.0%) | **7,030 (−1.3%)** |
| 7 소멸 | 0/51 | 0/51 | 0/51 | **0/51** |
| 42 인구 | 6,848 | 6,208 (−9.3%) | 6,549 (−4.4%) | **6,218 (−9.2%)** |
| 42 소멸 | 0/51 | 0/51 | **1/51** | **0/51** ✅ |

**여덟 수 (3판 ON)**

| 수 | 1020 OFF → ON | 7 OFF → ON | 42 OFF → ON |
|---|---|---|---|
| 인구 | 6,459 → 6,337 | 7,125 → 7,030 | 6,848 → 6,218 |
| 소멸 | 0/51 → **0/51** | 0/51 → **0/51** | 0/51 → **0/51** |
| 무기Q | 939 → 984 | 1,049 → 1,211 | 1,003 → 863 |
| 확장셀 | 6,365 → 5,406 | 5,946 → 6,464 | 6,003 → 4,391 |
| 게시 | 2,563 → 2,675 | 2,710 → 2,535 | 2,760 → 2,807 |
| 일/건 | 1.70 → 1.60 | 1.70 → 1.72 | 1.65 → 1.69 |
| 도구Q | 10,803.3 → 11,225.1 | 12,009.0 → 12,180.6 | 11,405.3 → 10,562.7 |
| 보존식 | 740.7 → 694.1 | 606.3 → 609.3 | 870.3 → 590.0 |

### ★두 변경을 갈랐다 (`T135_FOREST_MEAN=0` = 2판 첫 항 + 품목 셋)

| 시드 | 2판(품목 없음) | **품목 셋만 더함** | 3판(첫 항까지) |
|---|---|---|---|
| 1020 | 6,640 | **6,361** (−4.2%) | 6,337 (−0.4%) |
| 7 | 7,264 | **6,614** (−8.9%) | 7,030 (**+6.3%**) |
| 42 | 6,549 (소멸 1) | **6,818** (+4.1% · 소멸 1) | 6,218 (−8.8% · **소멸 0**) |

⇒ **둘 다 시드마다 부호가 다르다.** 그리고 시드 42 의 소멸을 지운 것은 **첫 항**이다(품목만으론 소멸 1 이 남는다).
공급이 늘수록 인구가 주는 방향이 보인다 — T123 이 랩에서 본 **가격 붕괴 연쇄의 공급 쪽 판**이다.
2판이 소멸을 못 지우고 3판이 지운 것도 같은 기전의 반대편이다(숲 마을 예산이 커져 시장이 재배분됐다).

## 3-3. 검증

| 검사 | 결과 |
|---|---|
| `scripts/test-trees.js` | **81 PASS / 0 FAIL**(⑫ 절 신설) |
| ★돌연변이 `T135_FOREST_MEAN=0` | 2판(성긴 간격)으로 정확히 돌아간다 |
| ★돌연변이 `T135_TREES=0` | 세 품목이 **아예 안 선다**(OFF = T86) |
| `test-lab-trees` · `test-regrow` · `test-imsu` · `test-mining` | 44/0 · 58/0 · 10/0 · PASS |
| `test-icons` · `test-itemlabel` | PASS · 45/0 (**새 품목에 아이콘을 요구하지 않는다**) |
| `lab-wiring-check` · `inline-engine/path --check` · `build-trees --check` | 실패 0 · 최신 · 최신 |
| 러너 전수 | 안 돌렸다(카드 지시) |

**랩 적재 추적**(`scripts/lab-modules.js`) — 17개. 내 diff ∩ 적재 = **`server/chunk.js` · `server/specialty.js`**
⇒ 3시드 A/B 가 규약상 필요했고, 그래서 돌렸다.
접점 심볼(`treeCountOf|forestTreesPerCell|forShare|fCov|kgOf|RESOURCES|mulberry|grape|acorn|chestnut`)로 뽑은 하네스는 위 표 그대로다.

## 3-4. 재민 판정거리 (3판 뒤)

1. **소멸 0/51 을 샀다 — 인구 −1.9/−1.3/−9.2% 를 치르고.** 승인할 것인가.
2. **공급이 늘수록 인구가 준다** — 품목 셋만 더해도 시드 7 이 −8.9% 다. T123 의 가격 붕괴 연쇄가
   서버에서도 보인다. 다음 층(덤불 실체화·목재 품목 분할)을 넣기 전에 이 방향을 먼저 판정해야 한다.
3. **첫 항이 마을별로는 흔들린다**(72~414% · 중앙 135%) — 총량만 맞다. 마을별로 맞히려면 지형을 다시 훑어야 한다.
4. **재생 앵커** — 1판 그대로(`T135_MATURE=lab`).

## 3-5. 회부 (셋 추가)

7. **열매 아이콘 셋**(세션8 · 품목이 섰으니 이제 구울 수 있다) · **오디 여름판 그림 이름**(`_a` = 가을).
8. **개암 열매** — 값의 출처가 생기면 `hazel.fruit` 를 채운다(재민).
9. **공급 증가 → 인구 감소 방향** — §3-2 의 분리 표. econ 판정거리(가격 붕괴 연쇄의 서버 판).

---

## 3-6. 푸시 (규약대로)

| 항목 | 값 |
|---|---|
| 커밋 | **`573a551d`** `[승인 대기 · T135 3판] 두 항 다 실물대로 · 열매 넷이 품목이 된다` |
| 브랜치 | **`batch/trees-0906`** (`1d3594d0..573a551d` · fast-forward) |
| force | **0회** (reflog 전부 `fast-forward`) |
| main | **무접촉** — `26da0eb5` 그대로 (`git ls-remote origin refs/heads/main` 로 확인 · `git fetch` 안 썼다) |
| `.lock` 전수 | **신규 0**. `.git` 아래 남은 8개는 전부 이전 판에서 이미 `.git/_stale_locks/` 로 격리한 것 |
| 번들 | `t135c.bundle` → `~/Mini/_bundles_pushed/` · `_MANIFEST.txt` 한 줄 |

### ★번들 fetch 가 죽었다 — 저장소 손상이 아니라 **열린 파일 한도**였다

첫 fetch 가 `fatal: ... Too many open files` 로 죽고 `did not send all necessary objects` 를 뱉었다.
번들을 의심하기 쉬운 자리인데, 번들은 멀쩡했다. 원인은 맥 저장소의 **팩이 324개**라는 것이다 —
이 저장소는 `unlink` 가 막혀 있어 `git gc` 가 못 돌고, 번들 fetch 한 번에 팩이 하나씩 는다.
git 은 팩마다 `.idx` 를 열어 두므로 `ulimit -n` 1024 를 넘는 순간 읽기가 실패한다.
`ulimit -n 8192` 한 줄로 그대로 지나갔다.

> **족보 후보:** *"번들 fetch 가 `Too many open files` 로 죽으면 **번들이 아니라 팩 수**를 봐라.
> `unlink` 가 막혀 `gc` 를 못 도는 저장소는 팩이 단조증가한다 — `ulimit -n` 을 올리면 지나간다.
> `did not send all necessary objects` 는 그 상황에서 **거짓 증상**이다."*

⚠**팩 324개는 단조증가한다** — 언젠가 맥에서 `git gc` 를 한 번 돌려야 한다(브리지 밖 · 재민 손).

### ★그리고 `push` 도 같은 벽에 부딪혔다 — 우회로는 **긁은 클론**이다

`ulimit -n` 을 65536 까지 올려도 두 번째(문서) 판은 `push` 가 `Too many open files` 로 죽었다
(`pack-objects` 가 팩 324개를 다 열어야 한다 — 셸 한도가 아니라 브리지 마운트 쪽 한계로 보인다).

**우회로**(다음 세션이 그대로 쓸 것):

```
  # mnt 밖 — 여기선 unlink 가 된다
  git clone --bare --depth 1 --branch <브랜치> --single-branch <url> $HOME/pushrepo.git
  cd $HOME/pushrepo.git
  git fetch <번들> 'refs/heads/<브랜치>:refs/heads/tmp-doc'
  git push origin <해시>:refs/heads/<브랜치>
```

팩이 **2개**뿐인 저장소라 그냥 지나간다. 맥 저장소는 `fetch` 로 이력만 받아 두고(그건 통과한다),
**보내는 일은 긁은 클론에 맡긴다.** 긁은 클론은 `$HOME` 밖 mnt 가 아니라서 유저 폴더를 안 더럽히고 세션과 함께 사라진다.

> **족보 후보:** *"맥 저장소가 팩 300개를 넘으면 `fetch` 는 `ulimit -n` 으로 지나가도 `push` 는 안 지나간다 —
> **`--depth 1 --bare` 로 긁은 클론에서 보내라.** 보내는 쪽은 이력이 필요 없다."*

---

# T135 **승인** — 리베이스 · 재실행 · main (2026-09-07)

> **재민 승인 2026-09-07.** 카드 지시: *"main 까지 밀어라(리베이스 · 재실행 · ff)."*

## 4-1. 리베이스 **3회** — 1차 충돌 5파일(전부 "둘 다 살린다") · 2차 0 · 3차 1(회부 꼬리)

베이스가 `26da0eb5` → **`4e273f95`** 로 갔다(밤사이 main 에 T145 둥근 산 · T140 픽스처 시계 ·
야간러너 보고가 들어왔다). `git rebase origin/main batch/trees-0906` 한 번 — 충돌은 첫 커밋에서만 났다.

| 파일 | 무엇과 부딪혔나 | 어떻게 풀었나 |
|---|---|---|
| `public/client/30-n-net.js` | T124 `plantSeeds` 수신 한 줄 | 인접 추가 — **둘 다** |
| `public/client/43-i-icon.js` | T124 `plantSeeds` 선언 | 인접 추가 — **둘 다** |
| `server/zone.js` ×3 | T124 `plantSeeds`/`plant_tree` 라우트/`tryPlantTree` 블록 | 둘 다(함수 블록은 HEAD 를 닫고 이어 붙였다) |
| `인계/C-채집자연물.md` | T124 절이 같은 자리에 들어왔다 | 둘 다 · **T135 → T124 → T123 → T122** 로 날짜순 정렬 |
| `인계/회부.md` | T137·T140·T145 절이 꼬리에 붙었다 | 둘 다(내 절을 맨 뒤로) |

**한 줄도 안 버렸다.** 두 카드는 같은 통로(`welcome` 표 · 동사 라우트)를 쓰되 **다른 줄**을 쓴다 —
T90 이 세운 *"서버 표 하나 · 클라 폴백 0"* 규약이 이런 충돌을 자동으로 인접 추가로 만든다.
`node --check` 10파일 통과.

★**3시드를 돌리는 25분 사이에 main 이 또 갔다** — `4e273f95` → **`90a232e1`**(T142 게시 시세 · **T146 사냥 이식**).
T146 은 `server/villages.js`·`server/zone.js` 를 만져 내 파일과 겹치는데 **리베이스 2회차는 충돌 0** 이었다
(같은 파일 다른 절). 그래서 **3시드를 한 번 더 돌렸다** — 아래 표는 새 베이스(`90a232e1`)의 것이다.

> **족보 후보:** *"승인 리베이스에서 재실행은 **베이스를 고정한 뒤** 시작해라. 3시드가 25분이면
> 그 사이 main 이 간다 — 잰 수가 어느 베이스의 것인지 표에 적어라."*

★그리고 **한 번 더** 갔다 — `90a232e1` → **`6b2dc28d`**(T146 2판 사냥터 밴드 · T148-A · T149 포로 자세 · T150 산 둥글기).
3차 리베이스는 충돌 1(`인계/회부.md` 꼬리 · 둘 다 살림)로 끝났다. **여기서 경주를 끊었다** —
빠른 하네스로 확인하고 **main 에 먼저 앉힌 뒤**, 착지한 main 위에서 3시드를 한 번 더 재 §4-7 에 적는다.
(main 에 앉으면 다음 카드가 나를 딛고 리베이스한다 — 그게 경주를 끝내는 유일한 수다.)

> **족보 후보:** *"밤 대기열에서 25분짜리 재측정은 main 을 못 따라잡는다.
> **빠른 하네스로 앉히고, 앉은 자리에서 다시 재라.** 재느라 못 앉으면 영원히 못 앉는다."*

## 4-2. 재실행 (베이스 `90a232e1` · T142·T146 위)

| 수 | 1020 OFF → ON | 7 OFF → ON | 42 OFF → ON |
|---|---|---|---|
| 인구 | 6,459 → **6,337** | 7,125 → **7,030** | 6,848 → **6,218** |
| 소멸 | 0/51 → **0/51** | 0/51 → **0/51** | 0/51 → **0/51** |
| 무기Q | 939 → 984 | 1,049 → 1,211 | 1,003 → 863 |
| 확장셀 | 6,365 → 5,406 | 5,946 → 6,464 | 6,003 → 4,391 |
| 게시 | 2,390 → 2,508 | 2,602 → 2,336 | 2,616 → 2,625 |
| 일/건 | 1.96 → 1.86 | 1.95 → 1.98 | 1.91 → 1.95 |
| 도구Q | 10,803.3 → 11,225.1 | 12,009.0 → 12,180.6 | 11,405.3 → 10,562.7 |
| 보존식 | 740.7 → 694.1 | 606.3 → 609.3 | 870.3 → 590.0 |

★**여덟 수 중 일곱이 3판 표와 한 자도 안 다르다.** 인구·소멸·무기Q·확장셀·도구Q·보존식이 전부 같다.

★**움직인 건 `게시` 한 축뿐이고, 그건 내 것이 아니다** — OFF 열이 2,563/2,710/2,760 → **2,390/2,602/2,616**
으로 같이 내려갔다(사건 총건수 20,822/20,881/21,320 은 무변). **T142**(약속의 값도 하역 뒤를 본다 — 게시 시세)가
main 에 앉은 결과다. 양쪽 열이 같이 움직였으니 **T135 의 델타는 그대로**다.

⚠**그래서 OFF 열은 이제 T86 기준선과 게시 축에서 갈린다.** 다음 카드는 T86 이 아니라
**이 표의 OFF 열**(베이스 `90a232e1`)을 기준선으로 써라.

하네스 재실행(새 베이스): `test-trees` **81/0** · `test-lab-trees` 44/0 · `test-regrow` **95/0** ·
`test-imsu` 10/0 · `test-itemlabel` 45/0 · `test-icons` PASS · `test-mining` PASS ·
`test-lab-hunt` 30/0 · `test-hunt-port` 58/0 (T146 것) · `lab-wiring-check` 실패 0 ·
`build-trees`/`inline-engine`/`inline-path` `--check` 최신.
`test-events` **271/1** — §4-5.

## 4-3. ★★§4-5 를 먼저 읽어라 — `test-events ⑲c` 가 빨갛다 (내 결함이 아니다)

승인 리베이스에서 **하네스 하나가 빨개졌다.** 결론부터: **T135 가 만든 결함이 아니고, 고치는 방향이
자명하지 않아 안 고쳤다**(회부). 아래 §4-5 가 실물이다.

## 4-4. 푸시

| 항목 | 값 |
|---|---|
| 리베이스 | **3회** (1차 충돌 5파일 · 2차 0 · 3차 1(`인계/회부.md` 꼬리 · 둘 다) · 버린 줄 0) |
| force | **0회** |
| 베이스 | `26da0eb5` → `4e273f95` → `90a232e1` → **`6b2dc28d`** |
| main | `6b2dc28d` → **fast-forward** (§4-6 에 착지 해시) |
| `.lock` 전수 | §4-6 |
| ⚠원격 `batch/trees-0906` | **`8e734c1a` 에 그대로 둔다**(리베이스 전 해시 · **임시 ref**). 그 가지에 새 해시를 얹으면 force 다 — 안 한다. |

## 4-5. ★★★빨강 하나 — 빈손 귀환 레코드가 자기모순이다 (econ · T17 ④ 의 나머지 반쪽)

`test-events` **271 PASS / 1 FAIL**. 깨진 건 ⑲의 **전제** 한 줄이다:

```
  ✗ ⑲c 전제: 대조에 쓰는 레코드가 econ 안에서 정합하다(arriveDay−departDay = travelDays)
  ✓ ⑲  소문 시계 = econ 캐러밴 시계   거리 160~892px · 16건 전수
  ✓ ⑲d 시계를 미는 두 자리가 travelDays 를 같이 갱신한다(소스 계약)
```

### 가른 것 — 세 갈래로 물었다

| 물음 | 답 |
|---|---|
| 되돌림 손잡이 `T135_TREES=0` | **272/0 초록** |
| `origin/main` worktree(`90a232e1`) | **272/0 초록** |
| 내 가지 ON | **271/1** · 재실행 2회 같은 자리 |

그래서 "내 층이 켜지면 난다"까지는 맞다. **그 다음이 중요하다** — 무엇이 나는가:

| 표본(7시드 × 4길이) | 캐러밴 | 빈손귀환(`_abandoned`) | ⑲c 위반 |
|---|---|---|---|
| 기준선 | 149 | **0** | 0 |
| `T135_TREES=0` | 149 | **0** | 0 |
| **T135 ON** | 146 | **1** | **1** |

★**위반 집합 = 빈손 귀환 캐러밴 집합이다.** 빈손귀환이 아닌 레코드는 어디서도 안 깨진다.
T135 는 그 갈래를 **처음으로 도달 가능하게** 만들었을 뿐이다(공급이 바뀌어 목적지가 값어치를 잃고,
대안도 없어 빈손으로 돌아섰다). 시드 42 · `depart 240 · arrive 240 · travelDays 1` 한 건.

### 왜 깨지나 — 한 줄이 빠졌다

`sim/economy-sim-v2.js` **984~993**(빈손 귀환)은 `distance`·`departDay`·`travelDays`·`returnArriveDay` 를
갱신하는데 **`arriveDay` 만 왕로의 것으로 남는다.** 바로 위 재routing 자리(**952~968**)는 넷을 다 맞춘다 —
T17 ④ 가 고친 쪽이다. 빈손 귀환 자리의 주석에도 *"빈손 귀환도 같은 규약"* 이라 적혀 있다.
**의도는 있었고 한 줄이 빠졌다.**

### 그런데 고치는 방향이 자명하지 않다 — 그래서 안 고쳤다

`arriveDay` 를 같이 옮기면 **`server/central.js:658`** 의 귀환 보간이 깨진다:
`denom = returnArriveDay − arriveDay` · `num = day − arriveDay` — 그 자리는 `arriveDay` 가
**왕로 도착일**인 것에 기대고 있다(화면 위 캐러밴 위치가 그걸로 그려진다). 둘 중 하나다:

* ⓐ **빈손 귀환에서는 `departDay`·`travelDays` 를 안 덮는다** — 귀환 구간은 `arriveDay`↔`returnArriveDay`
  쌍이 이미 온전히 적고 있다. ⑲c 도 화면도 같이 옳아진다.
* ⓑ 네 값을 다 옮기고 `central.js` 보간도 같이 고친다.

**econ 정본의 판단이고 T17 을 낸 세션 몫이다**(내 카드는 `economy-sim-v2.js`·`central.js` 무접촉).
회부에 자리·줄번호·두 갈래까지 적었다.

> **족보 후보:** *"새 층이 하네스를 빨갛게 하면 **위반 집합**을 먼저 세어라.
> 위반이 한 갈래에만 모이면 그 층은 결함을 만든 게 아니라 **그 갈래를 도달 가능하게** 만든 것이다 —
> 기준선에서 그 갈래의 표본이 0 인지 세면 갈린다."*

