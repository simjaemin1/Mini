# 보고 — 야간 러너 전수 2026-09-12 (KST)

**기준 커밋 `6e236bc7`** (`인계/공통 — 기준선 셋째 판(T163 · 2b9f724e) 정본 · 푸시 정본 3판(임시) · 족보 (143)~(145) [PM 2026-09-11]`)
· 컨테이너 새 클론(`/tmp/minirepo`) · `npm install` · 러너 자가 검사 **통과**
· 시작 **04:09** → 종료 **07:48** (KST · 3시간 39분)
· 전수 목록 **143종**(`grep -l '^// @regress'`) · **돈 것 108** · 통과 **101** · 실패 **7** · **미측정 35**(전부 e2e — 아래 §3)
· 병렬 0 · 러너 동시 2개 0 · `pkill -f` 0(`kill <pid>` 만) · 코드·하네스 수정 0

⚠**목록이 143종이다.** 규약 §2 ②가 적은 "110종 · ~3h"보다 33종 많다. 이 컨테이너에서 e2e 한 종이
2~14분이라 3시간 39분으로 108종까지만 닿았다. 단위 하네스 **95종은 전수 완주**했고(빠르다),
남은 35종은 전부 e2e다. ⇒ PM 판정거리: 야간 창을 늘리든지, e2e 를 이틀로 쪼개든지(§4).

---

## 1. 실패 표 — 7종

| 하네스 | 통과/실패 | 단독 재실행 | 귀속 |
|---|---|---|---|
| `test-tick-slicer` | 결과줄 0 (280초 초과) | 단독도 같음 | **상시(기계 의존)** — 공통.md 목록 그대로 |
| `test-route-persist` | 51/1 (`⑦d` 31초 동안 0쌍) | ★**52/0 초록** | **픽스처 흔들림(부하)** — 연속 실행에서만 빨갛다. T89 회부 "`test-route-persist ⑦` 재작성"이 이 자리다 |
| `e2e-emptystart` | 16/3 (`★전제 — 농촌1 마을에 미도달`) | ★**19/0 초록** | **픽스처 흔들림(부하)** — 걸어서 도착하는 전제가 부하에서 안 선다 |
| `test-harness-lint` | 21/1 (`②` 판정 자리에 이모지) | 빨강 재현 | ★**진짜** · **T174**(진행중 · 세션7 · 카드 제목에 `e2e-friends` 이모지가 그대로 적혀 있다) · 원인 줄 `scripts/e2e-friends.js:185` `/🌀\|텔레포트 →/` · 그 파일 최근 커밋 `fb69d264 [T147]` |
| `test-regrow` | 94/1 (`★★⑤` 그림은 T129 가 구운 것을 그대로 쓴다) | 빨강 재현 | ★**진짜** · **T148-B(`bfc3028f` · 세션4 · 어젯밤 착지)** — 아래 §2 |
| `test-events` | 271/1 (`⑲c` arriveDay−departDay = travelDays) | 빨강 재현 | ★**진짜 · 귀속 미상** — 아래 §2 |
| `e2e-events` | 59/1 | 빨강이되 **다른 절** | ★**진짜이되 절이 흔들린다** — 전수에선 `④-c`(납품 버튼이 고른 행), 단독에선 `⑤-b`(심은 문장이 화면에). 같은 하네스가 두 번 다른 자리에서 빨갛다 |

## 2. 귀속 근거 (실측 — 지어낸 것 0)

### `test-regrow ★★⑤` → **T148-B `bfc3028f`**
하네스 257행은 `public/client/40-r2-sprites.js` 안에 **`assets/trees/sap_` 문자열이 있는지**를 본다.
`bfc3028f`(나무가 제 종으로 선다 — T148-B)가 그 줄을 **지웠다**:
```
- const SAP_SPRITES = SAP_SPECIES.map((sp) => { … im.src = '/assets/trees/sap_' + sp + '.png'; … });
```
T148-B 보고 §0-ⓑ 가 `SAP_SPECIES` 를 *"표의 사본"* 이라 적고 없앤 그 줄이다. 지금 파일에 남은 것은
`stump01.png`(246행)뿐이고 `sap_` 은 0곳이다.
⇒ **묘목 그림이 실제로 안 뜨는지, 아니면 하네스의 계약 문장이 낡은 것인지**는 T148-B 세션이 판정할 자리다.
(그 카드가 묘목을 `sp` 로 고르게 바꿨다면 하네스 ⑤를 새 계약으로 다시 적어야 하고, 안 그랬다면 진짜 회귀다.)

### `test-events ⑲c` → **귀속 미상 — 못 쟀다**
`arriveDay − departDay = travelDays` 는 econ 캐러밴 **레코드 자체의 정합** 전제다(`scripts/test-events.js:604`).
접점 파일(`server/events.js`·`server/villages.js`·`sim/economy-sim.js`·`server/central.js`)의 최근 커밋을 봤으나
**어젯밤 착지 둘(`811b86a8` T166 · `cb054554` T163)은 캐러밴 레코드를 안 만졌다** — 둘 다 주입 문 한 자리씩이고
미주입이면 비트 동일이다(diff 확인). 그 밖의 접점 커밋은 09-07 이전(T158·T159)이다.
⇒ **최근 커밋과의 접점 0.** 없는 결함을 지어내지 않는다 — 후보만 적는다: 하루 경계를 만진 **T153(틱 빚)**,
동적 `land.game` 을 넣은 **T146**. 둘 다 09-07 착지라 그 뒤 전수가 이 절을 봤어야 하는데
**09-07 야간(125/10) 이후 전수가 안 돌았다** — 그 사이 어디서든 들어올 수 있었다.

### 상시 빨간 것 대조 (`인계/공통.md`)
* `test-tick-slicer` — 목록 그대로 **상시(기계)**.
* `e2e-charsprite` — 족보 (125)가 목록에서 **뺐다**. 이번 전수에서 **96/0 초록**(예약 프롬프트의 목록이 이 한 줄만큼 낡았다).
* `e2e-rtt` · `test-body ⑯㉧` · `e2e-cold ⑦㉣` — `e2e-cold` 는 이번에 **55/0 초록**(물때 얼림 없이도). `e2e-rtt`·`test-body` 는 §3 미측정.
  ⇒ 물때(`TIDE_FREEZE_MS=372603`) 재실행은 **쓸 자리가 없었다**(해당 절이 안 빨갰다).

## 3. 미측정 35종 — 전부 e2e (순서상 뒤)

```
e2e-guest-reconnect guild hp inv membership metallurgy mountain move
mtcorridor mtcut mtfoot mtfuzz mtocc nature npcsprite onboarding
preserve rooms rtt rumor salt spoil2 terrain thirst tidal tilestate
trade ui verbs village waterperf weather weight winter zoom
```
★단위 하네스를 먼저 돌리도록 순서를 바꿨다(알파벳 순이면 e2e 48종이 앞에 서서 단위 95종이 통째로 안 돌 뻔했다).
그 덕에 **단위는 100% 돌았다**. 미측정은 "초록"이 아니라 **안 쟀다**이다.

## 4. 아침에 세션으로 넘길 빨강 (그 세션에 붙여넣을 문장)

**T148-B (세션4)**
> 야간 전수(2026-09-12 · 기준 `6e236bc7`)에서 `test-regrow ★★⑤` 가 빨갛다(단독 재현). 네 착지 `bfc3028f` 가
> `40-r2-sprites.js` 의 `SAP_SPRITES` 줄(`'/assets/trees/sap_' + sp + '.png'`)을 지웠고, 하네스 257행은 그 문자열의
> 존재로 "묘목 그림이 T129 가 구운 것 그대로"를 본다. **묘목이 실제로 종별 그림으로 뜨는지 먼저 재고**,
> 뜨면 하네스 ⑤를 새 계약(표 경유)으로 다시 적고, 안 뜨면 회귀다. 그림 파일 셋(`stump01`·`sap_pine`·`sap_oak`)은 그대로 있다.

**T174 (세션7)**
> 야간 전수에서 `test-harness-lint ②` 가 빨갛다(단독 재현) — `scripts/e2e-friends.js:185` 의 `.test()` 수신자
> `/🌀|텔레포트 →/` 가 판정 자리 이모지다. 네 카드가 이미 그 자리를 안고 있다(보드: "`e2e-friends` 이모지").
> 고치면 러너 전수에서 한 종이 초록으로 돌아온다.

**E 영역 / PM (귀속 미상 — 카드 배정 필요)**
> `test-events ⑲c`(272건 중 1 실패 · 단독 재현)가 빨갛다: econ 캐러밴 레코드가 `arriveDay − departDay ≠ travelDays`.
> 어젯밤 착지 둘은 접점이 0이고, **09-07 전수(125/10) 이후 전수가 처음 돈 판**이라 그 사이 어디서든 들어왔을 수 있다.
> 이분(bisect)할 자리다 — 09-07..09-11 의 econ·캐러밴 접점 커밋이 후보다.

**하네스 안정화 (PM)**
> `test-route-persist ⑦d` 와 `e2e-emptystart ★전제` 는 **단독이면 초록**이고 러너 연속 실행에서만 빨갛다
> (족보 ⑩ "하네스는 정해진 초를 자지 않는다"의 그 병). `e2e-events` 는 두 판에서 **서로 다른 절**이 빨갰다.
> 셋 다 같은 약이 필요한 자리로 보인다.
