# 보고 — T97 자연물 편입·5.0.1 재굽기 · 움집 실내 둘 · 경작지 벡터 띠 삭제 (세션8 · 2026-09-05)

브랜치 `batch/nature-rebake-0905` → ff → `main`. **force push 0회.**

---

## 0. §0 실측 (셋)

### ⓐ `nature_render.py` 헬퍼 두 벌 차이 (718줄)

AST 로 이름·서명·본문 해시를 떠서 `render_common` 과 맞댔다.

| 이름 | 서명 | 본문 | 처분 |
|---|---|---|---|
| `principled` | 같음 | **같음** | 공용 것으로 — 삭제 |
| `simple_mat` | `(name,color,rough=0.8)` ↔ 공용 `(…, metal=0, emit=None, emit_str=0)` | 다름(공용이 상위집합) | 공용 것으로 — **기본값이 같아 잠글 호출부 0** |
| `cleanup` | 같음 | 다름 — nature 는 `bpy.data.textures` 고아도 쓴다 | **공용에 흡수**(아래) |
| `_flip_png` | — | `_post_png(ss=1, flip=True)` 와 같은 일 | 공용 것으로 — 삭제 |
| 씬 블록(최상위 30줄) | — | 값 전부 같음 · **`sensor_fit` 만 없다** | `rc.build_scene("nat")` |
| `render(key, ss, margin)` | — | `render_world_pass` 와 **규격이 다르다**(아래) | 공용에 손잡이 둘 추가 |
| `_mix2`·`leaf_mat`·`bark_mat` | nature 전용 | — | 남는다(재질표는 파일 것 · T77 §0-ⓒ) |
| `add_mesh`·`tube`·`arc_pts`·`blade`·`leaf_shell`·`blob`·`class R` | nature 전용 | — | 남는다(모델 문법) |

**기본값 잠금 자리 수 = 0.** `simple_mat` 의 갈린 인자는 전부 뒤에 붙은 새 인자(기본값이 무해)라
호출부가 손댈 것이 없었다. `ico`/`cyl` 은 nature 가 **아예 안 쓴다**(0곳) — 자기 `add_mesh` 계열을 쓴다.

`cleanup` 의 텍스처 순회는 진짜다 — `blob()` 이 DISPLACE 용 `bpy.data.textures.new(…,'CLOUDS')` 를 만든다.
저장소 렌더 스크립트 중 **그걸 만드는 곳은 nature 하나**라(전수 grep) 공용 `cleanup()` 에 무조건
넣어도 다른 파일엔 **빈 순회**다. `test-render-common ⑥` 이 "만드는 곳 하나"를 지킨다.

**실루엣 잘라내기는 `render_common` 후처리로 표현되지 않는다 — 그대로 둔다.**
자연물은 개체마다 알파 bbox 로 자르고(`nature-postprocess.py` · `ALPHA_MIN 10`) 그 **크롭 오프셋만큼
앵커를 옮겨** `nature_anchors.json` 에 적는다. 공용 `_post_png` 는 축소·FLIP 만 한다(크롭 없음).
자르는 일이 배치·앵커 기록과 한 몸이라 렌더 프리셋이 아니라 **후처리 스크립트의 일**이다.

### ⓑ 침상·화덕 — 서버에 시설 정의가 **없다** → 렌더 하나

* `server/zone.js` 에 `bed`/`hearth` 건물 타입 없음. `BUILDING_TYPE_TO_ITEM` 은 `BUILDING_RECIPES`
  에서만 만들어지고, `doDismantleBuilding(player, id)` 는 `b.type` 으로 돈다.
* 클라는 `building.data.hut` 의 실내 좌표로 그린다 — 침대 6 = `[[-4,-4],[-3,-4],[-2,-4],[-1,-4],[-4,-3],[-1,-3]]`,
  화덕 = `(-2,-3)`. 소스 주석: *"시각 전용(durango-consistency: 물리 실체 침대→리스폰·NPC 취침 연동은 생활 층 이관 묶음)"*.
* ⇒ **짐에 안 들어가고 해체 대상이 아니다. 아이콘은 회부**(만들면 쓸 데 없는 사본) · **세계 스프라이트만**.
* 그래서 `props_render.py` 에 표가 둘이다 — `PROPS`(건물 행 10) · `DECOR`(장식 2 · `btype`/`icon` = null).
  같은 표에 넣으면 `btype` 칸이 거짓말하거나 `test-props ③`(서버 `BUILDING_HEIGHT` 대조)이 없는 키를 찾는다.

### ⓒ 경작지 띠 — 무엇을 그렸고, 타일로 덮으면 이음새가 맞는가

옛 `data.sim` 층: 마름모 `x±16, y±8` + (밭)이랑 2줄 / (논)물빛 + 모 3포기.

| 층 | 화면 가로 폭 | 셀 다이아 64px 대비 |
|---|---|---|
| 셀 다이아(정본) | 64px | 기준 |
| 옛 벡터 띠 | **32px** | 좌우 각 16px 빔 — **반 칸**이다 |
| T79c 8군 타일 | `drawImage(…,48,48)` · 알파 내용 58~60/64 → **43.5~45.0px** | 좌우 각 ~9.5px 빔 |

옛 주석은 *"셀 꽉 채워 띠가 이어져 보임"* 이라 했으나 **실측이 아니었다.** 띠는 원래도 안 이어졌다.

★★그리고 이건 크기 문제가 아니다: 밭 타일은 `render_icon_pass`(아이콘 시선 `ISO_DIR` · z 압축 없음 ·
FLIP 없음)로 굽고 64px 로 줄인 **정사각 그림**이다. 64×32 셀 다이아가 아니라서 키워도 안 맞는다.
T67 이 벽·울타리에서 푼 *"모델이 셀 경계에서 끝나게"* 규약은 **밭 스프라이트엔 서 있지 않다.**
⇒ 이 카드는 PM 판정대로 벡터 층을 지우고 층을 하나로 만들었고, **이음새는 회부 2**로 남긴다.
그림 `산그림/디자인B/경작지_이음새.png`.

---

## 1. ① 자연물 재굽기 — "45장" 은 38 + 18 이다

카드의 45는 `ls public/assets/nature/` 줄 수(PNG 44 + JSON 1)다. 실측:

| 무엇 | 몇 | 굽는 코드 | 이 카드에서 |
|---|---|---|---|
| 나무 `tree01..12` → `assets/trees/` | 12 | `nature_render.py TREE_BUILD` | **5.0.1 재굽기 ✓** |
| 수풀·풀·물가 술·들꽃 → `assets/nature/` | 26 | `nature_render.py PROP_BUILD` | **5.0.1 재굽기 ✓** |
| 바위 12 (`rock`·`mossrock`) | 12 | **저장소 밖** `~/Mini/rock_render.py` | 회부 1 |
| 광맥 6 (`ore`) | 6 | `ore-outcrop.py` — 바위에서 PIL 파생 | 바위를 따른다 → 회부 1 |

바위 12장은 `tree_render.py` 씬(지금 정본 아님)·SAMPLES 48·512² 고정 프레임으로 구웠고 앵커 JSON 에도
없다. **모델 본문이 저장소에 없으니 편입이 아니라 새로 짓는 일**이라 안 넣었다.
`icons.lock.json._기계_예외` 가 18장 이름을, `_기계_예외_까닭` 이 그 까닭을 적어 뒀고
`test-props ⑤` 가 그 집합을 못 박는다.

### 대조군 — 편입은 그림을 안 바꿨다

저장소 PNG 는 4.0.2 라 대조군이 못 된다(T77 §1 의 함정). **같은 컨테이너·같은 bpy 5.0.1 · 옛 코드**로 구웠다.

| 단계 | 무엇 | 결과 |
|---|---|---|
| 대조군 | 옛 `nature_render.py` · 5.0.1 | 38장 |
| **패스 A** | 편입본 · `sensor_fit='AUTO'`(옛 거동) | **IDAT 38/38 동일 · `nature_raw_anchors.json` 0 차이** |
| 패스 B | 편입본 · `sensor_fit='HORIZONTAL'`(정본) | IDAT **23 동일 / 15 다름** |

★`_flip_png`(제자리 갱신) ↔ `_post_png(ss=1)`(새 이미지 생성)이 같은 바이트를 내는지 먼저 따로 쟀다 —
`tree01` 로 A/B, IDAT 같음. 이걸 안 재고 갔으면 38장 어긋남의 원인을 못 짚었을 것이다.

### ★★`sensor_fit` 누락이 실제로 자른 것 — 세로가 긴 15장

굽기 **전에** 프레임 표(가로×세로)로 15장을 짚었고, 굽고 나니 정확히 그 15장이 달랐다:

`tree04·05·06·09·10` · `reed01·02·03` · `cattail01·02·03` · `flower01·02·03·04`

`ortho_scale` 이 AUTO 에서 **긴 변**을 잡으므로 이 15장은 `Hpx/Wpx` 배(최대 `flower01` 1.45배)로
**확대돼 사방이 잘려** 있었다. 잘린 만큼 알파 bbox 가 프레임까지 부풀어 여백(margin)이 사라졌다 —
그래서 후처리 뒤 크기가 **정확히 margin×2 만큼**(나무 16px · 소품 10px) 줄었다.

| | 전 | 후 |
|---|---|---|
| `flower01` | 64×93 · 0.97m | 54×84 · **0.87m** |
| `cattail02` | 87×154 · 1.60m | 77×144 · **1.50m** |
| `tree09` | 586×739 · 5.77m | 570×722 · **5.64m** |

### ⚠ 카드의 "`nature_anchors.json` 무변 확인" 은 실측에 안 맞았다

**23키 무변 · 15키 변경.** 바뀐 것이 옳다 — 그 15장은 지금까지 **잘린 그림을 제 크기라고 적고** 있었다.
클라는 JSON 을 읽으므로(사본 0 · `test-props ④`) 배선은 안 바뀐다.
`nature_raw_anchors.json`(크롭 전)은 **38키 전부 무변** — 프레임 계산은 기하에서 나오고 카메라와 무관하다.

### 4.0.2 → 5.0.1 기계 차이 (틀이 안 바뀐 23장만)

평균 **5.53/255** · 최소 3.25(`grass01`) · 최대 8.03(`bush04`) · 완전 동일 0장.
잎이 많을수록 크다(수풀 6.7~8.0 · 풀 3.3~4.3) — T79 가 작물에서 본 서명과 같다.

### 세계 패스에 손잡이 둘 — `ppu_mul` 과 `ss`

여태 `render_world_pass` 는 한 규격만 알았다. 자연물은 다른 규격을 쓴다:

| 규격 | `ppu_mul` | `ss` | 뜻 |
|---|---|---|---|
| 가구·밭 | 1 | 3 | 게임 해상도(45.255px/m)로 배포 · 굽기만 3배 후 박스 축소 |
| 자연물 | 4(나무)/3(소품) | 1 | 135~181px/m **고해상으로 배포** · 클라가 앵커 `ppu` 로 줄인다 |

기본값 `(1, SS)` 가 옛 거동 그대로라 기존 호출부(`props_render.py` 1곳) 무변.

---

## 2. ② 움집 실내 둘 — 벡터 → 스프라이트

`36-r2-building.js` 의 `floor` 절 실내 층. 침상 8줄·화덕 9줄의 벡터가 `drawPropBody` 두 줄이 됐다.

* **거적 침상** — 짚 오리 9 + 말린 끝단 2 + 새끼 2줄 + 목침. 51×30px · zmax 4.5px.
  ★`box` 한 장으로는 '종이'다(T79b 잎 교훈) — 오리를 나란히 눕혀 **엮은 결**을 만든다.
* **화덕(노지)** — 재 + 그을린 불자리 + 막돌 6 + 탄 장작 2 + 숯 5. 45×28px · zmax 3.7px.
  **잉걸빛은 코드가 얹는 상태**로 남겼다 — 모닥불 불꽃과 **같은 경계**(T67 ⓒ).
  불이 꺼진 화덕도 같은 몸이어야 하므로 발광 재질을 모델에 넣지 않았다.

### 화덕은 세 패스 — 답은 지우려던 벡터가 갖고 있었다

| 패스 | 재 r / 고리 r | 읽힌 것 |
|---|---|---|
| 1 | 0.42 / 0.395 | **회색 접시 위 돌무더기** — 재가 고리보다 넓었다 |
| 2 | 0.355 / 0.408 | **꽃잎 여섯** — 고리가 재 밖에서 떴다 |
| 3 ✓ | 0.400 / 0.355 | 불자리(0.265) 테두리 밖 · 재 안쪽 — 어두운 불자리가 주인공 |

1패스는 모닥불 수치를 옮겨 쓴 것이었다. 옛 벡터의 비례(불자리 24×12px · 막돌 6.4×4.4px · 고리 13px)가
**이미 옳았다.** ⇒ 지우려는 벡터도 실측 대상이다.

---

## 3. ③ 경작지 벡터 띠 삭제

`data.sim` 절 26줄(마름모 + 이랑/물빛 + 모)을 지우고 **플레이어 밭과 같은 8군 타일**로 바꿨다.
단계는 옛 벡터가 그리던 것 그대로 — 밭(`dry`) = 이랑만 → 단계 0 · 논 = 물+모 → 단계 1.
같은 '경작지'가 소유자에 따라 달리 보이면 정본이 둘이다. 이음새 표·그림은 §0-ⓒ.

---

## 4. 검증 (러너 전수 없음 — 공통 §2 ②)

접점 파일로 뽑은 목록 (`grep -l "<파일>" scripts/test-*.js scripts/e2e-*.js`):

| 접점 파일 | 부르는 하네스 |
|---|---|
| `scripts/nature_render.py` | `test-charsheet` · `test-props` · `test-render-common` |
| `scripts/render_common.py` | `test-render-common` |
| `scripts/props_render.py` | `test-icons` · `test-props` · `test-render-common` |
| `scripts/nature-postprocess.py` | (없음) |
| `public/client/36-r2-building.js` | `test-crops-world` · `test-props` |
| `props_anchors.json` | `test-props` |
| `nature_anchors.json` | `test-props` · `e2e-nature` |
| `icons.lock.json` | `test-icons` · `test-props` |

| 하네스 | 결과 |
|---|---|
| `test-render-common` (⑥ 자연물 편입 신설) | **PASS** · `--selftest` 오염 5건 잡음 |
| `test-props` (②③ 장식 분기 · ④ 조임 · ⑤ 자연물 신설) | **PASS** · `--selftest` PASS |
| `test-icons` (⑧ 에 `nature`·`trees` 추가) | **PASS** |
| `test-crops-world` | **PASS** |
| `test-charsheet` | 26 통과 / 0 실패 |
| `e2e-nature` | **86 통과 / 0 실패** (자산 404 0 · 약 8분) |
| 랩 모듈 적재 추적 | 적재 17파일 · 내 diff 8 · **교집합 ∅** |

### 리베이스 뒤 재실행 (공통 §2 — 겹치는 파일만)

푸시 직전 `origin/main` 이 **두 번** 움직였다(세션 밖) — `2ce2e095` → `7d08b677`(T86·T91) →
`cb97b8a3`(T86 승인 기입 · 공통 기준선 갱신 · 족보 123~126). 두 번 다 `git rebase origin/main`,
**충돌 0** (둘째는 문서만이라 `인계/회부.md` 가 내 절 33줄만 위에 얹혔다). 파일 단위 교집합은
첫 번째도 ∅ 이지만, 상류가 만진
`server/zone.js`·`server/crops.js`·`server/villages.js` 는 **내 하네스가 읽는 정본**이라
그 셋을 읽는 하네스만 다시 돌렸다:

| 상류가 만진 파일 | 그것을 읽는 내 하네스 | 재실행 |
|---|---|---|
| `server/zone.js` | `test-props`(BUILDING_HEIGHT) · `e2e-nature` | PASS · **86/0** |
| `server/crops.js` | `test-icons` · `test-crops-world` | PASS · PASS |
| `server/villages.js` | `e2e-nature` | **86/0** |
| `sim/economy-sim.js` | (내 하네스 없음) | — |

새 하네스 절이 눈멀지 않았는지 — `test-render-common ⑥` 에 `def simple_mat…` 오염을 넣어 잡히는 것을
`--selftest` 로 확인(4건 → 5건으로 문턱도 올렸다). `test-props ④` 는 예외 수를 **정확히** 조였다:
농지 `fill 1/1`(그림자만) · 바닥 `fill 1/1`(잉걸빛) `fillRect 2/2`(잉걸 두 점) — 헐거우면 검사기가
먼저 거짓말한다(T95 교훈). 그리고 바닥 절이 `bed`·`hearth` 를 **이름으로** 부르는지도 본다.

### 굽는 시간 (2코어 컨테이너 · SAMPLES 64 · bpy 5.0.1)

| 판 | 장 | 시간 |
|---|---|---|
| 대조군(옛 코드) | 38 | 약 13분 |
| 패스 A(편입 · AUTO) | 38 | 약 13분 |
| 패스 B(편입 · HORIZONTAL · 배포본) | 38 | 약 13분 |
| 침상·화덕 | 2 | 3초 |

나무 1장 ≈ 30~45초(802×740 ~ 957×883). 자연물 한 판은 13분짜리다.

---

## 5. 실기 2줄 (재민)

1. `산그림/디자인B/자연물_재굽기_전후.png` — **세로가 긴 15장이 잘려 있었다**(①), 틀이 안 바뀐
   23장의 기계 차이(②), 그리고 `flower01` 4배 확대(③ — 꽃이 위로, 잎이 왼쪽·아래로 잘려 있다).
2. `산그림/디자인B/경작지_이음새.png` — 벡터 띠(반 칸)와 8군 타일(43.5~45px)이 **둘 다** 셀 경계에
   못 닿는 것. 그리고 움집 안 침상·화덕이 다른 가구와 같은 결로 보이는가(스프라이트 둘).

### 상류가 갚아 준 회부 하나

T95 회부 4-b(`e2e-charsprite` 62/4 상시 빨강 · 세션2 영역)는 **T87 이 풀었다** — 진범은 쿼드트리가
아니라 **시야 뿔**(등 뒤에 세운 짝은 못 본다)이었고, 둘째를 첫째 옆 60px 에 세우니 81/0.
`인계/공통.md` 족보 (125)가 그것을 적었고 상시 빨강 목록에서 뺐다.

## 6. 회부

`인계/회부.md` `0-자연물` 절 — 바위 12장 굽는 코드 부재(+광맥 6 파생) · 밭 타일 세계 패스 재굽기 ·
`char_render.py` 5.0.1(T96 뒤 · 옷 재질 이중화 같은 카드) · `building_render.py` 5.0.1 ·
자연물 재질 datablock 접두 · 침상의 생활층 연동 · 상추/아욱 26px.

## 7. 푸시

* 커밋: `[재민 확정 2026-09-05 · T97]` — 해시는 §8 (리베이스 2회 · 밑동 `cb97b8a3`)
* 브랜치 `batch/nature-rebake-0905` 먼저 → `main` ff. **force push 0회.**
* `ls .git/*.lock` — 결과는 아래 §8.
* 잠금 파일 `public/assets/icons.lock.json` — `icons` 144 · `props` **21** · `crops` 32 ·
  **`nature` 44** · **`trees` 12** = 253키. `_기계` = `pip bpy 5.0.1` · `_기계_예외` 18장.

## 8. 푸시 결과

(푸시 뒤 채움)
