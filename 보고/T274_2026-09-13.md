# 보고 — T274 게시판 품목 집계(유도 → 집계) · 좁은 팔 "게시판만 식사를 본다" (2026-09-13)

한 줄: **집계: 비등재 7종 중 `fruit` 만 게시된다(800일 51마을 · 의뢰 **49 / 0 / 42**건) · `vegetable`·`walnut`·`grape`·`twig` **0건** ⇒
"게시판이 과일 부족을 못 낸다"는 **✗ — 거의 못 낸다가 맞다**(T270 의 유도를 집계가 정정했다) ·
좁은 팔: **여덟 수가 base 와 완전히 같다**(인구·소멸·도구Q·확장셀·보존식 무변 · 3시드) · 게시 **+1.7 / −1.3 / −1.2%** ·
**일/건 −44.4 / −41.8 / −43.3%** · 소멸 **0** · **T263 재현: 일/건 ○(−43.9% 거의 그대로) · 게시 ✗(+18.6% 는 안 온다)**.**

★★**분해가 됐다.** T263 의 `일/건 −43.9%` 는 **게시판 둘에서 나온 것이 맞다**(좁은 팔이 −43.2% 로 거의 전부 재현).
그런데 T263 의 `게시 +18.6%` 는 **게시판이 아니다** — 좁은 팔은 ±2% 다. 그건 세계가 움직여(가격·부패가 바뀌어)
**실제로 부족이 늘어서** 난 것이다. 한 카드가 묶어 놓았던 둘이 갈렸다.

---

## §0-ⓐ 게시판 품목 집계 — **유도가 아니라 집계**

계측기에 문 둘을 달았다(관측만 · `onEvent`·`onRequest` — 장부가 이미 주는 훅). 끔 팔 3시드 800일 51마을:

| 품목 | subs | 부족 에지 | 의뢰 게시 | 실제로 먹힌 양 | 낼 수 있나(`fromEcon`) |
|---|---|---|---|---|---|
| `fish` | 없음 | 444 / 561 / 690 | **453 / 554 / 691** | 31.7/마을·일 | ✅ |
| `meat` | 없음 | 416 / 456 / 303 | **416 / 466 / 305** | 10.8/마을·일 | ✅ |
| `fruit` | 없음 | 49 / 0 / 41 | **49 / 0 / 42** | 2.3/마을·일 | ✅ |
| `vegetable` | 없음 | 6 / 0 / 0 | **0 / 0 / 0** | 2.0/마을·일 | ✗ |
| `walnut` | 없음 | 0 | **0** | 0.008/마을·일 | ✗ |
| `grape` | 없음 | 0 | **0** | 0.05/마을·일 | ✗ |
| `twig` | 없음 | 0 | **0** | 0(안 먹는다) | ✗ |

★**T270 의 유도가 반쯤 틀렸다.** "게시판이 과일 부족을 **못** 낸다"고 적었는데, 집계는 `fruit` 의뢰
**49 / 0 / 42건**을 낸다 — `_consEMA > 0` 인 그 두어 마을(T263 실측 2 / 0 / 1곳)이 내는 것이다.
정확한 문장은 **"거의 못 낸다 — 낼 수 있는 마을이 51곳 중 0~2곳뿐이다"** 다.

★그리고 집계가 **둘째 벽**을 보여 줬다: `vegetable`·`walnut`·`grape`·`twig` 는 부족 에지가 서도(좁은 팔에서 210·61·53건)
**의뢰가 0건**이다 — `playerVillageDepositMap`(정본 16종)에 없어서 **플레이어가 낼 수 없는 품목**이기 때문이다.
*"낼 수 없는 의뢰는 의뢰가 아니라 벽이다"*(`events.js` 주석). ⇒ **EMA 를 고쳐도 그 넷은 게시판에 안 온다.**

## §0-ⓑ 좁은 팔 — 서버 한 자리(`events.js`) · 기본 끔

⚠**먼저 정정**: T263/T270 이 "가드 없는 자리"로 센 `server/villages.js:6322` 는 **프로덕션이 아니다** —
`__e2eForceShortage`(E2E 픽스처 · `zone.js` 가 `E2E_GIVE` 로 게이트)다. ⇒ **프로덕션 게시판은 `events.js` 하나**이고,
좁은 팔도 **한 파일 세 자리**(`itemsOf`·`prime`·`scanDay` 의 `_consEMA` 읽는 줄)로 끝난다.

```js
//  server/board-food-ema.js (새 파일 · 한 벌) — 엔진 무접촉
const EMA_N = 30;                                  // ★정본 참조(economy-sim.js 일 경계 폴드의 α=1/30) · 새 수 0
function boardFoodOn() { … process.env.T274_BOARD_FOOD … }   // 미설정/'0' = 끔
function foldDay(s, v) { … v._foodEaten → s.foodEma … }      // 하루 한 번 · 상태는 **장부 안**
function view(s, v)    { … 끔이면 v._consEMA **그 객체 그대로** … }
```

★**엔진에 새 상태를 안 심었다.** 식사 EMA 는 장부(`events.js` 의 `s`) 안에 살고, 읽는 장부는 정본 `v._foodEaten` 이다.
★**끔이면 `view` 가 정본 객체를 그대로 돌려준다** ⇒ 비트 동일.
★`lab-wiring-check` — 랩은 `events.js` 를 **안 싣는다**. 그래서 [H] 표(랩 HTML 스캔)가 아니라 **서버 소스 기본값 절**을
새로 달았다(자명 통과 금지 포함): *"서버 `T274_BOARD_FOOD` 기본이 끔이다"*.

### 끔 = 비트 동일 · 켬 = **세계 무변**

| 시드 | base 여덟 수(셋째 판) | **board 팔** |
|---|---|---|
| 1020 | 6,889 · 0/51 · 11,892.1 · 5,509 · 803.6 | **전부 동일** ✅ |
| 7 | 7,148 · 0/51 · 12,039.3 · 7,830 · 551.1 | **전부 동일** ✅ |
| 42 | 6,259 · 0/51 · 11,013.2 · 5,946 · 663.3 | **전부 동일** ✅ |

★**좁은 팔은 세계를 한 글자도 안 움직인다.** 장부의 §제3규약 그대로다 — *"관측은 개입이 아니다."*
⚠**단 이 판에서만 그렇다**: 이 계측기는 `ENABLE_VILLAGES=0`(플레이어 없음)이라 게시판이 되먹임하지 않는다.
**실서버에서는 게시판이 플레이어가 무엇을 내는지를 바꾸므로 되먹임이 있다** — 그건 이 표가 못 재는 자리다.

### 게시판 쪽 — 눈을 떴다

| 재는 것 | 1020 | 7 | 42 | T263 `food` 팔 |
|---|---|---|---|---|
| 부족 에지(전체) | 4,230 → **10,242** | — | — | — |
| 게시(의뢰) | 2,539 → 2,582 (**+1.7%**) | 2,714 → 2,679 (−1.3%) | 2,648 → 2,615 (−1.2%) | **+28.1 / +15.2 / +12.3%** |
| **일/건** | 1.93 → 1.07 (**−44.4%**) | 1.88 → 1.10 (−41.8%) | 1.95 → 1.10 (−43.3%) | **−44.9 / −42.8 / −44.0%** |
| `fruit` 부족 → 의뢰 | 49→199 · 49→**230** | 0→188 · 0→**202** | 41→190 · 42→**224** | — |
| `vegetable` 부족 | 6 → **210** | 0 → 232 | 0 → 235 | — |
| `walnut`·`grape` 부족 | 0 → **61 · 53** | 0 → 12 · 55 | 0 → 55 · 52 | — |
| 소멸 | 0 → 0 | 0 → 0 | 0 → 0 | 0 → 0 |

★**T263 재현 — 일/건은 ○, 게시는 ✗.** 좁은 팔이 `일/건` 의 **거의 전부**를 재현한다(−43.2% 평균 vs T263 −43.9%)
⇒ **T263 의 읽기가 맞았다**. 반대로 `게시 +18.6%` 는 좁은 팔에서 **안 온다**(±2%) ⇒ 그건 게시판이 아니라
**세계가 바뀌어 실제 부족이 늘어서** 난 것이다.

⚠**뜻밖의 것 하나** — 좁은 팔에서 `fish`·`meat` 의 부족 **건수가 줄었다**(444→288 등). 건수는 **에지 수**이지
"부족한 날 수"가 아니다. EMA 가 커지면 문턱 `ema × SHORT_DAYS` 도 커져 래치가 **안 풀리고**, 그래서 새 에지가
덜 난다(의뢰도 `s.reqs.has(item)` 로 한 품목 하나라 같은 이유). **더 오래 부족한 상태로 걸려 있다**는 뜻이다 —
이 자를 "부족이 줄었다"로 읽으면 안 된다.

## §0-ⓒ 답 한 줄

> **집계: 비등재 7종 중 게시되는 것은 `fruit` 뿐(49/0/42건) — "못 낸다"가 아니라 "거의 못 낸다"** ·
> 그리고 `vegetable`·`walnut`·`grape`·`twig` 는 **플레이어가 낼 수 없어** EMA 를 고쳐도 의뢰가 0 이다 ·
> **좁은 팔: 여덟 수 무변 · 게시 ±2% · 일/건 −43.2% · 소멸 0** · **T263 재현 — 일/건 ○ · 게시 ✗**.

## §1 회부

1. **#33 캐논(재민)** — 이 카드가 더한 것 셋:
   - **좁은 팔은 세계를 안 움직인다**(여덟 수 무변 · 3시드). 넓은 팔(T263)의 인구 −9.3% 는 **가격·부패 쪽**에서 왔다.
   - **게시판만 켜면 `일/건` 이 반토막**이고 그게 T263 이득의 거의 전부다.
   - ⚠**그런데 게시 총수는 안 는다** — 품목 **구성**만 바뀐다(`fruit` 이 들어오고 `fish`·`meat` 가 준다).
   ⇒ 셋 중 고르는 문제다: **여섯 자리 같이(T263 넓은 팔) / 게시판만(이 카드) / 그대로**.
2. **`fromEcon` 이 둘째 벽이다** — `vegetable`·`walnut`·`grape`·`twig` 는 `playerVillageDepositMap` 에 없다.
   게시판 캐논을 켜기로 하면 *"그 품목을 플레이어가 낼 수 있게 할 것인가"* 가 **따로** 물어져야 한다(재민).
3. **T263/T270 의 ⓒ 표 정정** — `villages.js:6322` 는 프로덕션이 아니라 `__e2eForceShortage` 픽스처다.
   가드 없는 프로덕션 자리는 **둘이 아니라 하나**(`events.js`)다. 인계에 옮길 자리(문서 카드).
4. **되먹임은 못 쟀다** — `ENABLE_VILLAGES=0` 이라 게시판→플레이어→곳간 고리가 이 판에 없다. 실서버 되먹임은 별건.
5. #21 ⓑ · `CENTRAL_SECRET`(#27 배포 뒤 확인) · #2 · #9 · `nippon` 시딩(Z).

## §2 검증

- 3시드(1020·7·42) × 2팔 = **6판 전부 종료코드 0** · 800일 51마을.
- **끔 = 비트 동일**(셋째 판) · **켬 = 여덟 수 무변**(base 와 한 자도 안 다름) — 둘 다 3시드 ✅.
- 게이트 셋 — 번들 `--check` **8/0** · 인라인 `--check` 둘 다 최신 · 배선 **실패 0 · 경고 0**
  (+ 새 절: 서버 `T274_BOARD_FOOD` 기본 끔 · 자명 통과 금지 포함).
- **엔진 무접촉** — `sim/` 은 한 글자도 안 바뀌었다. 바뀐 것: `server/board-food-ema.js`(새 파일) ·
  `server/events.js`(require 1 + 읽는 자리 3) · `scripts/lab-wiring-check.js`(서버 기본 절) · 계측기 · 보고.
- 계측기 `scripts/t274-board-ab.js` — **러너 밖**(`@regress` 없음) · 소스 치환 0(팔이 진짜 손잡이) ·
  자기검사: 정본 손잡이 셋(`foodConsOn`·`sideConsOn`·`boardFoodOn`)이 팔과 안 맞으면 **종료코드 2**.
- 정본 재구현 0(α·문법은 정본 참조 · `fromEcon` 은 `playerVillageDepositMap` 에서 읽는다) · **새 수 0** · 켜지 않았다.
- 러너 전수 안 돌렸다(카드 규약).
- 접점 심볼: `_consEMA` · `_consDay` · `_foodEaten` · `eaten` · `events.js:450` · `events.js:595` ·
  `villages.js:6322`(= `__e2eForceShortage` · 정정) · `SUBSISTENCE_PER_NPC` · `T274_BOARD_FOOD` · `_allocKnob` · `T263_FOOD_CONS`.
