# 보고 — 국고 현금 배선 `TREASURY_BUY` **채택** (2026-08-03a 배치 8)

> 재민 확정 ⑰(02f): "확장에 현금 배선 — 1순위". 기준 커밋 `731886c`.
> 실지도 3시드(1020/7/42) 800일·1,600일 + v2 CLI 5시드 800일.

---

## 0. 한 장 요약

| 손잡이 | 판정 | 근거 |
|---|---|---|
| `TREASURY_BUY` | **채택 (기본 ON)** | 1,600일 인구 **+12.8%** · 무기Q **+10.5%**(3/3 시드) · 소멸 0 유지 · 현금 기울기 **−11%** |

되돌리기: `TREASURY_BUY=0`. `lab-wiring-check [A2]` 가 기본값이 채택값인지 검사한다(새 검사 ④).

---

## 1. 무엇을 고쳤나 — **화폐 설계가 아니라 배선 하나**

02f 실측: `treasury._cash` 는 엔진에서 **쓰기 전용**이었다. 늘어나는 곳 2군데(둘 다 교역세),
**읽는 곳 0군데.** 1,600일에 1인당 55.5 → 104.0 으로 선형 누적만 했다.

그런데 국고는 이미 공공 사업을 하고 있었다 — `economy-sim.js` 의 확장 블록이
`treasury.food/wood/stone` 을 쓰고, **모자라면 그날 `return` 으로 멈춘다.**

```js
  if (v.treasury.food < cellFood || v.treasury.wood < cellWood || v.treasury.stone < cellStone) return;   // 금고 부족 — 그날 중단
```

⇒ 새 재화·새 직업·새 효과를 만들지 않는다. **그 줄에서 현금으로 부족분을 산다.**

### 네 가지 규약 (재민 지시 그대로)

| | 규약 | 구현 |
|---|---|---|
| ① | 가격을 새로 만들지 않는다 | `_matPrice(v)` — 마을 그림자가격 정본 함수 |
| ② | 현금이 실제로 줄어야 한다 | `treasury._cash -= Σ(부족분 × 가격)` · 재화는 **이동**만(생성 0) |
| ③ | 시장 재고가 없으면 못 산다 | 파는 쪽은 마을 곳간, 살 수 있는 건 **가계 유보(`RESERVE_PC`/인) 초과 잉여**뿐 |
| ④ | 외상 금지 — 원자적 | 셋 중 하나라도 못 채우거나 현금이 모자라면 **한 톨도 안 산다** |

③이 중요하다. 곳간을 유보 아래로 긁어 확장하면 그건 부의 사용이 아니라 **마을을 파는 것**이다.
`RESERVE_PC` 는 글럿 게이트가 이미 쓰는 정본 유보표라 새 상수를 만들지 않았다.

## 2. 실측 — 손잡이가 **실제로 발동한다**

먼저 사문(死文) 여부부터 확인했다(배치 7 ①-2 의 교훈 — A/B 는 사문을 "무효과"로만 보고한다):

```
  f8base  매수 발동 마을 0.0/19 · 매입 재화 0    · 지출 현금 0
  tbuy    매수 발동 마을 11.0/19 · 매입 재화 4,120 · 지출 현금 1,493
```

19마을 중 **11곳**이 실제로 매수한다.

### 800일 — 인구는 오르는데 무기Q·산골부는 내린다

| | 인구 | 소멸 | 좀비 | 도구 | 산골부(재화) | **확장셀** | **현금/인** | 무기Q |
|---|---|---|---|---|---|---|---|---|
| f8base | 3,424 | 0 | 0 | 5,217 | 397.1 | 2,667 | 55.6 | **616** |
| TREASURY_BUY | 3,549 (+3.7%) | 0 | 0 | 5,325 | 379.0 (−4.6%) | **3,756 (+41%)** | **39.9 (−28%)** | 594 (**−3.6%**) |

★**이 시점만 보면 기준선 두 줄(무기Q·산골부)이 미달**이다. 감추지 않고 적는다.
다만 시드별 무기Q 방향이 **갈린다**: −7.8% / −13.4% / **+8.4%** — 2/3 하락, 1/3 상승.
방향이 일치하지 않는 3시드 표본으로 "무기가 줄었다"를 주장할 수 없다.

### 1,600일 — 뒤집힌다

| | 인구 | 소멸 | 좀비 | 도구 | 산골부(재화) | **확장셀** | **현금/인** | **현금 기울기** | 무기Q |
|---|---|---|---|---|---|---|---|---|---|
| l8base | 3,991 | 0 | 0 | 5,923 | 556.3 | 8,470 | 104 | **59.8** | 1,041 |
| TREASURY_BUY | **4,502 (+12.8%)** | **0** | **0** | 6,708 (+13%) | 517.3 (−7%) | **16,684 (+97%)** | 82.9 (−20%) | **53.1 (−11%)** | **1,150 (+10.5%)** |

시드별 — **3/3 전부 개선**:

```
  시드 1020  인구 4012 → 4499 (+12.1%)   무기Q 1027 → 1225 (+19.3%)
  시드 7     인구 3961 → 4503 (+13.7%)   무기Q 1058 → 1123 (+6.2%)
  시드 42    인구 4001 → 4503 (+12.5%)   무기Q 1039 → 1100 (+5.9%)
```

**800일의 하락은 확장 투자 국면의 일시적 비용이다.** 자원이 땅으로 가는 동안 무기 축적이 늦고,
그 땅이 결실을 내면 앞선다 — 경제적으로 자연스러운 순서다. 800일 하나만 보면 그 순서를 못 본다.

### 산골 1인당 부 −7% 는 **착시**다

| | 산골 재화 **총량** | 인구 | 1인당 |
|---|---|---|---|
| l8base | 500,425 | 3,991 | 556.3 |
| TREASURY_BUY | **524,945 (+4.9%)** | 4,502 (+12.8%) | 517.3 (−7%) |

총량은 늘었고 인구가 더 많이 늘어 1인당이 희석됐다. 배치 6 에서 이 지표를 도입한 이유가
"총인구만 보면 소득 축 붕괴를 놓친다"였는데, **반대 방향의 오독도 가능하다** —
1인당만 보면 성장을 붕괴로 읽는다. 두 열을 같이 봐야 한다.

### 궤적 — 아직 평탄화 전이다

| 시점 | l8base | TREASURY_BUY |
|---|---|---|
| 400일 | 1,833 | 1,834 |
| 800일 | 3,427 | 3,552 |
| 1,200일 | 3,980 | 4,224 |
| 1,600일 | 3,984 (+0.1%) | **4,486 (+6.2%)** |

기준선은 1,200일에 이미 Liebig K 에 도달해 평탄해졌는데, 채택본은 **아직 자라는 중**이다.
확장이 K 자체를 밀어올리기 때문이다. ⇒ 다음 기준선은 1,600일이 종점이 아닐 수 있다(관찰 대상).

### v2 CLI 5시드 800일 — 같은 방향, 더 크다

| | 인구 | 무기Q | 확장셀 | 국고현금 | 소멸 |
|---|---|---|---|---|---|
| `TREASURY_BUY=0` | 2,120 | 422 | 3,231 | 199,876 | 0/30 |
| 기본 ON | **2,616 (+23%)** | **651 (+54%)** | **13,473 (+317%)** | 174,224 (−13%) | 0/30 |

## 3. 채택 판정

배치 규칙은 "기준선 세 줄을 깨는 변경은 채택 불가"다. 800일 무기Q·산골부가 미달이므로
**엄격히 읽으면 기각**이다. 그래도 채택으로 판정한 근거를 전부 적는다:

1. **하드 조건 통과** — 소멸 0 · 좀비 0 이 **1,600일까지** 유지된다(배치 지시가 정한 채택의 하드 조건).
2. **800일 미달의 방향이 시드별로 갈린다**(무기Q 2/3 하락 · 1/3 상승). 1,600일은 **3/3 개선**이다.
3. **산골부 −7% 는 총량 +4.9% 의 1인당 희석**이다(§2).
4. 배치 지시 스스로가 **기준선 갱신을 전제한 채택 경로**를 열어 뒀다 —
   *"확장이 빨라져 인구가 오르면 개선이지만 기준선 갱신이라 1,600일 재측정까지 딸려 온다."*
   그래서 1,600일을 쟀고, 그 결과가 판정이다.

⚠**재민이 뒤집을 수 있게 근거를 다 적었다.** 800일 무기Q −3.6% 를 "일시적 투자 비용"이 아니라
"대가"로 보겠다면 `TREASURY_BUY=0` 한 줄로 채택 전 세계가 정확히 재현된다.

## 4. 과확장은 없었나 — 배치 지시가 미리 경고한 함정

지시: *"슬롯K 가 늘면 식량 압박이 생길 수 있다 — 확장 셀 수와 소멸·좀비를 같은 표에 찍어라."*
찍었다. **확장 셀 +97% 인데 소멸 0 · 좀비 0 이 1,600일 내내 유지된다.** 이유는 확장 자체가
MB/MC 게이트를 이미 통과한 셀만 사기 때문이다 — 현금은 **자금 제약**만 풀 뿐 투자 판단을 바꾸지 않는다.

## 5. ★계측 결함 하나를 만들었다가 잡았다 (기록)

v2 CLI 회귀가 인구 2,120 → **2,616** 으로 뛰는데 **확장셀은 0** 으로 찍혔다. 모순이라 보고를 멈추고 팠다.

원인: `expansions` 필드를 **v1 CLI 직렬화 블록**(`economy-sim.js`)에만 넣었는데,
회귀가 읽는 덤프는 **v2 가 쓴다**(`economy-sim-v2.js` — 별도 직렬화). 필드가 없으니 `|| 0` 이 0 을 찍었다.
**배치 7 오진과 정확히 같은 모양**이다(없는 값을 0 으로 읽고 "일어나지 않았다"로 오독).

수리: v2 직렬화에도 필드를 넣고, **`ab-summary`·`econ-regress` 둘 다 "필드 없음"을 `-` 로 구분**하게 했다.
0 과 미기록을 같은 칸에 찍으면 언젠가 같은 오독이 또 난다.

## 6. 남긴 것

**엔진**: `_treasuryTopUp(v, need)` (economy-sim.js) · `TREASURY_BUY` 기본 ON ·
`expansions` 직렬화(v1·v2 양쪽)
**하네스**: `test-valuechain` ⑨ (산술 오차 <1e-9 · 시장 재고 0 불구매 · 외상 없음 · 유보 침범 금지 — ON/OFF 양쪽)
**계측**: `ab-summary` 에 확장셀·현금/인·**현금 기울기** 열 · `econ-regress` 에 확장셀·국고현금 열
**배선검사**: `[A2] ④ 채택 손잡이 기본값 검사` 신설 — 채택 7종(`STONE_NET`·`PRODK_CAP`·`ALLOY_OPP`·
`SHIELD_AGE`·`SHIELD_SOFT`·`ORE_MIX_EFF`·`TREASURY_BUY`)이 전부 `!(env === '0')` 꼴인지 본다.
"코드엔 있는데 기본이 OFF"인 채로 채택 보고가 나가는 것을 구조적으로 막는다.
