# 보고 — T338: 부하 거짓 빨강 둘을 조건 대기로 · 예산형 92자리 우선순위 · 야간 09-20 귀속 (2026-09-20)

**가지 `batch/harness-budget-0920` → main ff · 제품 0 · 하네스 다섯 · force 0**

1. **① `e2e-mtfuzz` — 예산이 한가한 상자에서도 처음부터 모자랐다.** 단계마다 자던 `sleep(1700)` 을 굽기 조건 대기로 바꾸고 **실측**: 가라앉는 데 **6358~17831ms**(중앙 9494ms) — **10단계 전부** 1700ms 를 넘겼다. 즉 P1~P5 는 내내 **반쯤 구운 그림** 위에서 돌고 있었고, 09-18 밤 `P4 ① 2% 초과 1회`(단독 초록)가 그 자국이다. 맹목 `sleep(24000)`(입장)도 정본 `waitInWorld` 로 — 실측 **2707ms**. **11/11 두 판 · 245초**(옛 256초 — 늘지 않았다).
2. **① `e2e-conn` — 예산형 6자리 → 0자리.** 전부 T322 문법(`waitFor` 하나 · 상한은 표에만 · 회마다 상황줄). ②-b 의 맹목 `sleep(3000)` 도 조건 대기로: 마감액 6000ms 대비 여유가 **3000ms → 5378ms**. **42/0 두 판 · 280·282초.**
3. **② 카드 전제를 뒤집는다 — 예산형 92자리는 밤 빨강의 원인이 아니었다.** 09-13~09-19 야간 보고 `grep` 대조: 그 창의 빨강 **11종** 중 **예산형 자리가 원인인 것은 `e2e-conn ②`(24초) 하나**뿐이고 T322 가 이미 닫았다. `e2e-mtfuzz` 는 빨갰지만 그 자리가 **예산형 자에 아예 안 걸린다**(`for×sleep` 이 아니라 맨 `sleep`). 나머지 아홉은 각자 다른 뿌리다(상시 셋 · 자 결함 둘 · 제품 셋 · T331 하나).
4. **② 그 대조가 대신 찾아낸 것 — `e2e-cold` 의 네 밤 빨강이 전부 같은 한 줄이다.** 09-13·09-14·09-15·**09-20** 모두 `존 입장 — 월드 안이다`, 기다림은 **129ms · 2277ms · 5625ms** 로 제각각이다 ⇒ **"늦게 들어와서"가 아니다**. 모양은 하나: **들어왔다가 1.8초 안에 다시 나갔다** — T331 이 닫은 그 자리와 같은 모양이고, **네 밤의 베이스에는 T331 이 없었다**(`8f12f6e1` 의 `33-m-conn.js` 에 T331 0건). ⚠**재현은 못 했다**(부하를 못 만든다) — 대조는 다음 C 밤(09-23)이다.
5. **③ 기계가 묶음 C 를 돌렸다 — 자로 쟀다.** 야간 베이스 `8f12f6e1` 에서 `nightly-split.sh C --list-only` 가 내는 목록이 **단위 102 + e2e 16 = 118종**, 보고 머리의 수와 **같다**. e2e 16종은 보고의 통과 8 + 빨강 8 과 **차집합 0**. (T314 ①이 물은 "기계를 돌렸나"에 대한 답: **돌렸다**.)
6. **③ 09-20 상시 밖 빨강 9건 귀속** — 단독 초록 6(`e2e-cold`·`e2e-events`·`e2e-move`·`e2e-mtfoot`·`e2e-mtocc`·`e2e-winter`) · 단독도 빨강 3(`test-route-persist`·`test-site-memo`·`e2e-village`). **`e2e-cold` 만 뿌리를 댔다**(위 4). 나머지 여덟은 이 카드에서 **안 쟀다** — 지어내지 않는다.
7. **예산형 92 → 83자리**(`e2e-conn` 6 · `e2e-charsprite` 2 · `e2e-chronicle` 1). 남은 83자리는 **표에 둔다** — 위 3 의 대조가 "이 모양이 밤을 빨갛게 한다"는 증거를 안 줬다.
8. **`test-harness-lint` 44/0** — ⑧a 벽시계 단정 **0건** 유지 · ⑧c 입장 기다리기 **사본 0** 유지(`e2e-mtfuzz` 가 정본으로 들어왔다).
9. **제품 0.** 다섯 파일 전부 `scripts/`. 되돌리려면 이 가지를 빼면 된다.

---

## §0 무엇을 쟀나 (하나씩)

### §0-ⓐ ① `e2e-mtfuzz` — 예산 두 개, 둘 다 조건 대기로

| 자리 | 종전 | 지금 | 실측 |
|---|---|---|---|
| 월드 입장 | `sleep(24000)` (맹목) | 정본 `FC.waitInWorld` (⑧c 사본 0) | **2707ms** — 21초를 그냥 놀고 있었다 |
| 단계마다(14단계 루프) | `sleep(1700)` (맹목) | `settle()` — `__mtDbg.mt3bakeN`·`mt3chunks` 가 멈출 때까지 | **6358~17831ms** (중앙 9494ms) |

★**이 카드가 실제로 알아낸 것**: `sleep(1700)` 은 **부하 때문에 모자랐던 게 아니라 처음부터 모자랐다.**
한가한 상자 두 판 모두 **10단계 전부** 1700ms 를 넘겼고(최소가 6358ms — 1700 의 3.7배), 그 말은
P1(팝)·P2(단조)·P3(국소)·P4(덮개)·P5(정합)가 내내 **반쯤 구운 그림**을 비교하고 있었다는 뜻이다.
야간 09-18 의 `P4 ★덮개 … ① 2% 초과 1회`(단독 초록)는 그 여유 없음이 부하에서 터진 자국이다.
굽기 예산이 **프레임당 1장**(`MT3_BUDGET=1`)이라 부순 양이 많을수록 오래 걸린다 — 717셀을 부순
단계가 17.8초였다. 정해진 초로는 맞출 수 없는 종류다(족보 ⑩).

`settle()` 은 두 단계로 기다린다 — ⓐ 움직이기 **시작**할 때까지(부수기가 큐에 들어가기 전에 "멈춰 있다"고
속지 않게 · 상한 2.5초) · ⓑ 그 뒤 **연속 3회 그대로**일 때까지(상한 90초 = 실측 최대의 5배).
상한은 **시간 판정이 아니라 멈춤 감지**다 — 걸리면 아래 P 들이 이유 없이 빨개지는 대신 **P7 이 이름을 붙인다**.

⚠**P6b 를 다시 조준했다(자 수리).** 옛 P6b 는 `baked1 > 0` — "부수기 직후 남은 굽기가 있다" 였다.
단계마다 가라앉히면 P6 이 받을 잔량은 **0 이 정상**이라 그대로 두면 **거짓 빨강**이 된다(실제로 한 판 물었다).
그래서 증거 자리를 옮겼다: "가라앉히기가 일을 한다"는 **P6c**(반례 · 되구운 21장 · |Δ| 27.209)와
**P7**(단계 합 103장)이 들고, P6b 는 그 대신 **"P6 은 잔량 0 인 그림에서 쟀다"**(=경주가 아니다)를 단정한다.
판정을 완화한 것이 아니다 — 같은 엄격도(0.05)에서 |Δ| **0.000** 이다.

**검증 — 단독 두 판**: `11/11` · `11/11` · **245초**(T314 가 잰 옛 한가한 판 **256초** — 늘지 않았다).
단계 실측도 두 판이 겹친다(6375~17740 / 6358~17831).

### §0-ⓑ ① `e2e-conn` — 예산형 6자리 → **0자리**

T322 가 이 파일에 이미 넣어 둔 `waitFor` 하나(사본 0 · `CAP=120000` 은 표에만)를 나머지 여섯 자리에 그대로 썼다.

| 자리 | 종전 예산 | 실측(한가한 상자) |
|---|---|---|
| ① 정상 입장 | 60×500ms = 30초 | 2~3초 |
| ②-b 마감액 뒤 `error` | 40×400ms = 16초 | **5273ms**(마감액 6000ms) |
| ③ 배너 노랑 | 60×300ms = 18초 | **2447ms** |
| ④ 노랑→빨강 | 80×1000ms = 80초 | **22917ms** |
| ⑥ 먼저 오류에 빠뜨리기 | 60×400ms = 24초 | 7ms |
| ⑥ 스스로 복귀 | 70×1000ms = 70초 | **7550ms** |

★**②-b 의 맹목 `sleep(3000)` 도 고쳤다 — 여기가 제일 얇았다.** 마감액이 6000ms 인데 3000ms 를 자고
읽었으니 여유가 **3000ms** 뿐이었다. 2코어 야간이면 잠+왕복이 6초를 넘어 `phase` 가 이미 `error` 가 되고,
그러면 ★★②-b 가 **제품과 무관하게** 빨개진다. 조건 대기로 바꾼 뒤 여유 **5378ms**(1.8배).
⚠한 번 틀렸다: 처음엔 "첫 pong(hello)"을 기다렸는데 그 순간 `stage` 가 아직 `accepted` 라
그 다음 ★②-b(막힌 단계)가 빨개졌다. 기다릴 것은 첫 pong 이 아니라 **재려는 그 상태**(`stage === 'welcome'`)다 —
고치고 초록. (실측 622ms.)

⚠**⑤ 의 `sleep(30000)` 은 안 건드렸다** — 그건 예산이 아니라 **재는 창 자체**다(정해진 창 안에 몇 번
두드리나 = 백오프). 조건으로 바꾸면 세는 대상이 사라진다. 그 뜻을 코드에 주석으로 박았다.

**검증 — 단독 두 판**: `42/0` · `42/0` · 280·282초.

### §0-ⓒ ② 09-13~09-19 야간 보고 `grep` 대조 — **카드 전제를 뒤집는다**

| 밤 | 빨강 | 예산형 자리 있나 | 그 빨강의 실제 뿌리 |
|---|---|---|---|
| 09-13 | `test-tick-slicer` · `e2e-cold` · `e2e-rtt` | ○ / ○ / ✗ | 상시(기계) / **입장 체류**(아래 ⓓ) / 상시(기계) |
| 09-14 | `test-body` · `test-tick-slicer` · `e2e-cold` | ✗ / ○ / ○ | 상시(물때) / 상시 / **입장 체류** |
| 09-15 | `e2e-charsprite` · `e2e-chronicle` · `e2e-cold` | ○ / ○ / ○ | 하네스 예외(`TypeError`) / ⑧ 260px 게이트 / **입장 체류** |
| 09-18 | `test-tick-slicer` · `e2e-charsprite` · `e2e-conn` · `e2e-fogray` · `e2e-mtfuzz` · `e2e-rumor` · `e2e-trade` | ○○○✗✗○○ | 상시 / 자 결함(T276·T304 닫음) / **★예산형 24초**(T322 닫음) / 프레임 카운터(T322) / **맹목 `sleep`**(위 ⓐ) / 제품 결함(T304 회부) / `checkOrphan`(T331 닫음) |
| 09-19 | `test-granary-add` | ✗ | 제품(T284 · `ReferenceError`) |

★**결론: 예산형 92자리가 원인인 밤 빨강은 `e2e-conn ②`(24초) 딱 하나다** — 그리고 T322 가 이미 닫았다.
`e2e-mtfuzz` 도 예산이 원인이지만 그 자리는 **예산형 자에 아예 안 걸린다**(맨 `sleep`, `for×sleep` 이 아니다).
나머지 아홉은 각자 다른 뿌리이고, **예산형이 있다는 사실과 빨개졌다는 사실이 서로 관계가 없었다.**
⇒ 카드가 시킨 순서("밤에 빨갰던 것부터")를 따라가 보니 **줄이 거의 비어 있다**. 남은 83자리는 **표에 둔다**
(카드의 "나머지는 표" 그대로). 지어서 고치지 않는다 — 족보 86·88.

고친 셋은 "밤에 빨갰고 예산형이 있는" 목록의 위에서부터다(원인이 아니어도 **상한에 걸렸을 때 말이 없던** 것은 고쳤다):

| 하네스 | 밤 | 고친 자리 | 실측 |
|---|---|---|---|
| `e2e-charsprite` | 09-15 · 09-18 | 입장 단추(200×250ms) · 시트 도착(180×1000ms) · 전환(60×2000ms) → 이름 붙인 상한 | 시트 192/192 **5371ms** · 전환 **2298ms** |
| `e2e-chronicle` | 09-15 | 존 인구/정원(90×1000ms) → 이름 붙인 상한 | **1012ms** |
| `e2e-conn` | 09-18 | 위 ⓑ 여섯 | 표 ⓑ |

예산형 **92 → 83자리**. `test-harness-lint` **44/0**.

### §0-ⓓ ② 대조가 대신 찾아낸 것 — `e2e-cold` 의 **네 밤이 전부 같은 한 줄**

| 밤 | 빨강 줄 | 기다림 | 단독 |
|---|---|---|---|
| 09-13 | `존 입장 — 월드 안이다` | (보고 §2) | **55/0 초록** |
| 09-14 | 같은 줄 | **5625ms** | **55/0 초록** |
| 09-15 | 같은 줄 | **129ms** | **55/0 초록** |
| 09-20 | 같은 줄 | **2277ms** | **초록** |

★**기다림이 129ms 인 판도 빨갰다** — 그러면 "늦게 들어와서"가 아니다. 종전 코드가 하던 일은 이랬다:
`waitInWorld` 로 입장을 확인 → **맹목 1800ms** 를 자고 → `__inWorld()` 를 **한 번** 읽는다.
그 한 번이 `false` 였다는 것이고, 그 모양은 **들어왔다가 1.8초 안에 다시 나갔다** 하나뿐이다.
**T331 이 닫은 자리와 같은 모양이다**(고아 감시가 느린 틱을 고아로 오진해 primary 를 다시 물었다).
⚠**네 밤의 베이스에 T331 이 없었다**: 09-20 야간 기준 커밋 `8f12f6e1` 의 `33-m-conn.js` 에 `T331` **0건**.

고친 것(하네스만 · 제품 0):
* 한 번 읽지 않는다 — **1.8초를 연속으로 안에 있을 때까지** 기다린다(상한 60초는 표에만). 판정 완화가 아니다: 잠깐 들어왔다 나가면 여전히 통과 못 한다.
* **자명 통과 금지 한 줄을 더했다** — `입장 뒤 월드 밖으로 튕긴 적이 0회`. "재접속을 기다려서 통과"와 "애초에 안 튕김"은 다른 사실이라 한 줄로 뭉개지 않는다.
* 클라가 **스스로 적는** 재접속 사유를 줍는다(T331 이 `e2e-trade` 에 단 상황줄 그대로) — 다음에 빨개지면 하네스가 추측하지 않고 **클라의 말**을 그대로 싣는다.

**검증 — 단독 두 판**: `56/0` · `56/0`(55 → 56 은 새 단정 한 줄) · 600·596초.
입장 **2276ms / 2237ms** — 09-20 야간의 **2277ms** 와 사실상 같다. **나간 횟수 0회 · 재접속 사유 0건.**
⚠**부하 재현은 못 했다**(2코어 상자에서 러너를 부르면 족보 247 에 걸린다). 대조는 **다음 C 밤 = 09-23**.

### §0-ⓔ ③ 야간 09-20 — 기계가 묶음 C 를 돌렸나 (T314 ①)

**돌렸다. 자로 쟀다.**

```
야간 베이스 8f12f6e1 에서:  nightly-split.sh C --list-only
  → [야간 C] 단위 102종 + e2e 16종 = 118종
보고 머리:                    오늘 묶음 C · 목록 118종 · 돈 것 118
e2e 16종 vs 보고의 통과 8 + 빨강 8:  diff → 차집합 0
```

(오늘 main 에서 같은 명령은 **104 + 16 = 120종** 이다 — 그 사이 단위 하네스 둘이 붙었다. 목록이 기계에서
나온다는 증거이지 어긋남이 아니다.) 09-19 밤이 못 잰 46종의 원인(**기계를 안 돌렸다**)은 이 밤에 **없다**.

### §0-ⓕ ③ 09-20 상시 밖 빨강 9건 — 귀속

| 하네스 | 단독 | 귀속 |
|---|---|---|
| `e2e-cold` | 초록 | **★댔다** — 위 ⓓ. 네 밤 같은 한 줄 · T331 없는 베이스 · 자는 이 카드가 고쳤다 |
| `e2e-events` · `e2e-winter` | 초록 (첫 판 RC=124 · 40분 끊김) | **안 쟀다.** 둘 다 40분에 잘렸다 = 러너 창 문제일 수 있으나 재보지 않았다 |
| `e2e-move` · `e2e-mtfoot` · `e2e-mtocc` | 초록 | **안 쟀다.** 셋 다 "0장/0장" 꼴(표본이 안 모였다) — `e2e-mtfuzz` 와 같은 **덜 구운 그림** 냄새가 나지만 **재보지 않았다** |
| `test-route-persist` · `test-site-memo` · `e2e-village` | **여전히 빨강** | **안 쟀다.** 부하와 무관하다 = 제품 또는 자의 실결함. 각각 카드가 필요하다 |

★**여덟은 지어내지 않는다.** 냄새는 위에 적었고, 그 이상은 재지 않았으므로 말하지 않는다.

## §1 안 한 것 · 회부

* **남은 예산형 83자리** — 표에만 둔다(위 ⓒ). 큰 넷은 `test-terr-persist:87`(6000초) · `test-route-persist:119`(800초) · `test-site-memo:87`(600초) · `test-tick-slicer:74`(600초) — 앞의 셋은 **09-20 에 실제로 빨갰고 단독에서도 빨갰다**(=부하가 아니다). 그 셋은 예산 이야기가 아니라 **제품/자 카드**다.
* **09-20 빨강 여덟의 귀속** — 위 ⓕ. 특히 `e2e-move`·`e2e-mtfoot`·`e2e-mtocc` 의 "0장" 꼴은 이 카드가 `e2e-mtfuzz` 에서 연 문(가라앉기 조건 대기)과 같은 자리일 수 있다. **한 카드로 묶을 값이 있다.**
* **`e2e-cold` 의 두 밤째 대조** — 09-23(다음 C 밤). T331 이 든 베이스로 도는 첫 C 밤이다.
* **`e2e-events`·`e2e-winter` 의 RC=124(40분)** — 러너 창 상한인지 하네스가 진짜 그만큼 도는지 안 쟀다.
* 종전 회부 그대로: `e2e-rumor` 경주(Z) · 굶긴 상자 장착 층(R2) · `e2e-trade ①` 알림 세기 · 고아 감시 2초를 틱 간격에서 유도할지(PM).

## §2 내가 틀린 것

* **`e2e-conn ②-b` 를 한 번 잘못 고쳤다** — "첫 pong 을 기다린다"로 짰는데 그 순간 `stage` 가 `accepted` 라 다음 단정이 빨개졌다(42건 중 1). 기다릴 것은 **재려는 그 상태**였다. 고치고 두 판 초록.
* **`e2e-mtfuzz P6b` 를 한 번 깼다** — 단계마다 가라앉히면 P6 의 잔량이 0 이 되는데 옛 P6b 가 `> 0` 을 물었다(11건 중 1). 자를 다시 조준했다(위 ⓐ).
* 둘 다 **고치고 난 뒤의 판만** 보고에 수로 남겼다 — 깬 판도 여기 적는다.
