# T259 — 계측기 **25벌이 전부 같은 한 벌**이었고, 그 한 벌은 제품 땅이 아니다 · `wProd` 칸을 고치니 T247 의 "유입 0" 이 사라졌다 (세션5 · 2026-09-13 · **가지만**)

> 카드 `~/Mini/지시_T259.md` · 가지 `batch/hunt-wood-server-0912` · **측정 베이스 `333aeb6e`**
> 만진 것: `scripts/t222-life800.js`(`wProd`·교역 열) · `scripts/t259-preds-audit.js`(신설 · 계측기) ·
> `scripts/t259-t17-cland.js`(신설 · 사본 러너) — **제품 코드 0** · `t17-metrics`·`t176-ab` **무접촉** · 17벌 **무변**.
>
> **답 한 줄 — 25벌: 제품 땅 **0** · A급 **25** · B급 **0**(술어가 **바이트 한 벌**이다 — 25벌이 전부 같은 값을 낸다) ·
> 기준선 자 `t17-metrics`·`t176-ab`·econ 랩 `econ-lab-real` **셋 다 A급** ·
> `t17-metrics` C-땅 3시드 인구 **6,814 / 6,394 / ―**(A 6,889 / 7,148 / 6,259) ·
> `wProd` 재독 = **"유입 0" 은 계측 칸의 결함이었다**(고치니 날 30부터 유입이 서고 다른 열은 **전부 비트 동일**).**

---

## §0ⓐ 술어 전수표 — 25벌이 `makeTerrainAdapter` 에 무엇을 넘기나

`scripts/t259-preds-audit.js` 가 **각 파일의 자기 바이트**를 잘라 `new Function` 으로 세우고,
`makeTerrainAdapter` 를 가로채 **그 파일이 실제로 넘기는 `deps`** 를 받아 낸다(옮겨 적은 줄 0).
받은 술어를 제품 정본 C(`scripts/zone-preds.js` ← `zone.js:570~650`)와 **존 전체 12만 점**에서 맞댔다.

| 술어 벌 | 파일 수 | 해안선 물 띠 | 다리(836셀) | 물 셀중심 | 바위 | `deps` 키 | **차단≠C** | **물≠C** | 등급 |
|---|---|---|---|---|---|---|---|---|---|
| **A** — 자체 정의 | **25** | ✗ | ✗ | ○ | 픽셀 그대로 | `isTerrainBlockedLocal`·`isWaterTileLocal` | **4,954/120,000 (4.13%)** | **4,863 (4.05%)** | **A급** |
| **C** — `zone-preds` | 2 | ○ | ○ | ○ | 타일 중심 | +`isBridgeLocal` | 0 (0.00%) | 0 (0.00%) | **제품 땅** |
| **B** — `() => false` | 0 | — | — | — | — | — | (T251 이 고쳤다) | | — |

**★25벌은 서로 바이트 동일한 값을 낸다** — 25개 파일 전부 차단 4,954 · 물 4,863 으로 **한 자도 안 다르다**.
이름만 다르다(`isW`/`isWater`/`isWaterTileLocal`, `isB`/`isBlocked`/`isTerrainBlockedLocal`).
⇒ **"18벌이 제각각"이 아니라 "한 벌이 25번 복사됐다"** 가 참이다. 모으는 비용은 그만큼 싸고,
   모아서 바뀌는 값은 **모든 자에서 똑같다**(ⓓ 한 판이 25벌 전부의 답이다).

**이 한 벌이 C 와 갈리는 이유는 둘뿐이다.**

1. **해안선 물 띠 393,686타일을 안 본다**(`zone.js:553` `chunk.generateCoastlineWaterTiles`) — 존의 **4%가 물인데 뭍으로 보인다**.
   차단율 **11.10%(A) vs 15.14%(C)** 가 그 차이다.
2. **다리 836셀을 안 본다**(`zone.js:2661` `isBridgeLocal`) — 물 위 통행이 막힌다(A 가 더 많이 막는 쪽).
   두 효과가 반대 방향이라 순 차이는 4.13%로 남는다.

### 파일별(25벌 · 값은 전부 같다 — 무엇을 재는 자인가만 다르다)

| 파일 | 무엇을 재나 | 러너(`^// @regress`) |
|---|---|---|
| `scripts/t17-metrics.js` | **기준선 정본** — 여덟 수(`인계/공통.md §2`) | × (계측기) |
| `scripts/t176-ab.js` | **기준선 A/B** — T100 손잡이 3시드 | × (계측기) |
| `scripts/econ-lab-real.js` | **실지도 econ 랩** — 본 게임 배선 그대로 | × (계측기) |
| `scripts/test-distmatrix.js` | 거리행렬 증분 **하네스** | **○ 러너가 돈다** |
| `scripts/test-frontier-iron.js` | 변경 철 **하네스** | **○ 러너가 돈다** |
| `scripts/test-onboarding.js` | 온보딩 **하네스** | **○ 러너가 돈다** |
| `scripts/farm-metrics.js` | 밭 계측기(T117) | × |
| `scripts/ev-density.js` · `ev-sources.js` | 사건 밀도·원천 | × |
| `scripts/t100-fieldbase.js` · `t130-grainprice.js` · `t196-happy.js` · `t204-cook.js` | 카드별 A/B 계측기 | × |
| `scripts/t60-extinct.js` · `t60-fishperf.js` · `t86-attrib.js` | 소멸·낚시·귀속 계측기 | × |
| `scripts/lab-supply-chain.js` · `stone-trade-diag.js` · `rumor-metrics.js` · `winter-metrics.js` | 진단 계측기 | × |
| `scripts/_t138-cleared.js` · `probe-seedcands.js` · `plan-village-50.js` | 개간·시딩 후보 조사 | × |
| `scripts/migrate-db-endowment.js` · `migrate-terrain-sw4.js` | **마이그레이션**(DB 를 실제로 고친다) | × |

⚠**러너가 도는 하네스 셋이 A 땅 위에 있다**(`test-distmatrix`·`test-frontier-iron`·`test-onboarding`).
회귀 판정이 제품 아닌 땅에서 내려지고 있다는 뜻이라, 모을 때 **이 셋은 단정 뒤집힘을 먼저 봐야 한다**(회부).
⚠**마이그레이션 둘은 DB 에 값을 쓴다** — 이 둘이 A 땅이면 **이미 쓰인 부존 값이 A 땅 값**이다(회부).

## §0ⓑ 파일별 땅 diff — 등급

ⓐ 가 25벌의 술어가 **바이트 동일**임을 12만 점에서 보였으므로, 51마을 땅 값도 **25벌이 같다**.
그 한 벌의 땅 값은 T251 §0ⓑ 가 이미 전수로 쟀다(`scripts/t251-land-diff.js` · 같은 문법 · 시딩 줄 무접촉).

| | 세움 | `fertility` ≠C | `water` ≠C | `woodSustain` ≠C | 전 열 동일 | `fert` 중앙 | `fert ≤ 0.5` |
|---|---|---|---|---|---|---|---|
| **C 제품 정본** | **50** (`어촌6` 스킵) | — | — | — | — | **1.00** | 11곳 |
| **A — 25벌 전부** | 51 | **11/50** | 2/50 | 1/50 | **39/50** | 0.99 | 11곳 |
| B — T232 옛 헤드리스 | 51 | 40/50 | 43/50 | 17/50 | 5/50 | 0.42 | 44곳 |

**★기준선 자 둘과 econ 랩의 등급(카드가 요구한 첫 줄):**
`t17-metrics` **A급** · `t176-ab` **A급** · `econ-lab-real` **A급** — 셋 다 **거의 제품 땅**(50마을 중 39곳 전 열 동일)이고
**제품 땅은 아니다**(마을 하나 더 세우고 11곳의 비옥도가 다르다).

## §0ⓒ `wProd` 계측 칸 — **"유입 0" 은 세계가 아니라 칸이었다**

### 왜 0 이었나 (한 줄)

`scripts/t222-life800.js` 는 하루 끝에 **DB 행의 `econ_state`** 를 읽는다. 그런데 `dailyProductionBuf` 는
`villages.js:645~651` 의 **`SERIALIZE_SKIP`** 에 들어 있다 — 주석 그대로 "매 틱 리셋 버퍼라 복원 시 재생성".
⇒ `econ_state` 에 **그 키가 아예 없다**. 자는 `undefined || 0` 을 800일 내내 더하고 있었다.
(실측 확인: T251 판 DB 에서 `JSON.parse(econ_state).dailyProductionBuf === undefined` · 51/51 마을.)

### 고침 (계측기 안에서 · 제품 0 · 새 문 0)

제품이 **이미 내주는 읽기 전용 문**(`villages.js:4654` `villageByDbId(dbId)` → 산 마을 객체)으로 바꿔 달았다.
`live.econ.dailyProductionBuf.wood` 를 읽는다 — `t176-ab.js:338` 이 econ 랩에서 읽는 **그 칸 그대로**다(사본 0).
교역 항 둘(`tradeStats.woodImported` · `tradeStats.exportBy.wood`)도 같은 자리에서 같이 읽는다.

### 전/후 (200일 · 시드 1020 · 끔 팔 · T251 §0ⓓ 와 같은 씨)

| 날 | **유입 전** | **유입 후** | 유출(연료+건축) | 재고 | 나무꾼 | 버퍼 읽힌 마을 |
|---|---|---|---|---|---|---|
| 1 | 0 | 0 | 36.66 | 5,938.5 | 0 | 50/50 |
| 20 | 0 | 0 | 92.23 | 4,235.7 | 0 | 50/50 |
| **30** | 0 | **16.85** | 114.26 | 2,281.8 | 8 | 50/50 |
| 40 | 0 | **87.83** | 106.39 | 1,451.6 | 76 | 50/50 |
| 60 | 0 | **128.93** | 93.61 | 1,126.7 | 151 | 50/50 |
| 100 | 0 | **152.41** | 124.13 | 890.8 | 203 | 50/50 |
| 140 | 0 | **153.51** | 115.92 | 270.5 | 226 | 50/50 |
| 200 | 0 | **206.61** | 161.80 | 629.4 | 224 | 50/50 |

* **자명 통과 금지**: 고치기 전 판은 **전 구간 0** 이다(위 왼쪽 열 — T251 §0ⓓ JSON 그대로).
* **다른 열은 전부 비트 동일**: 고친 판과 T251 §0ⓓ 판을 전 열 diff — 비교한 잎 **1,393** 중
  다른 곳 **62**, 그 **62 전부가 새 열**(`wProd`·`wProdVil`·`wImpCum`·`wExpCum`)이고 **그 밖 0**.
* 나무꾼이 붙는 날(30)과 유입이 서는 날이 **같다** — 칸이 이제 실체를 본다.

### T247 §0ⓐ 재독 — **"유입 0" 은 철회한다. 그리고 T207 의 "유출은 둘뿐"도 반쪽이었다**

| T247 §0ⓐ 가 쓴 것 | T259 가 재서 본 것 |
|---|---|
| 헤드리스 자의 목재 **유입이 800일 내내 0** | **거짓** — 칸이 안 읽힌 것. 날 30부터 서고 200일에 일 **206.6** |
| 유출은 연료+건축 **둘뿐**(T207) | **반쪽** — `_consDay` 기준으론 맞지만 **교역이 제3항**이다 |
| 재고가 날 100 에 0 | T251 이 이미 철회(땅이 틀렸다) — 고친 판은 **0 에 안 닿는다** |

200일 누계 교역: **수입 2,213.7 · 수출 4,334.6 ⇒ 순유출 2,120.9**.
캐러밴이 목재를 **내보내는 쪽**이고, 그 양이 같은 기간 벌목 유입과 같은 자릿수다.
⇒ 목재 수지를 볼 때 `_consDay` 만 보면 **절반을 못 본다**(T207 규약의 사각 — 회부).

## §0ⓓ 모으면 기준선이 얼마나 움직이나 — `t17-metrics` 를 **C 땅 위에** 세운 3시드 800일 (계산만 · 적용 0)

`scripts/t259-t17-cland.js` 가 `t17-metrics.js` 의 **자기 바이트**를 읽어 `makeTerrainAdapter(...)` **한 줄만**
갈아 끼운 임시 사본을 만들고 자식 프로세스로 돌린다(족보 — 돌연변이는 자식 프로세스+임시 파일).
**`t17-metrics.js` 는 한 바이트도 안 바뀐다**(팔 A 로 돌리면 바꾼 줄 0개).

**자명 통과 금지 — 팔 A(원본 술어)가 `인계/공통.md §2` 기준선 표를 재현하나:**
시드 1020 → 인구 **6,889** · 소멸 **0/51** · 무기Q **1,012** · 확장셀 **5,509** · 게시 **2,539** ·
도구Q **11,892.1** · 보존식 **803.6** · 생곡 **37,240.0** · ㉮ **1.93** · ㉯ **2.02** — **열 개 전부 한 자도 안 다르다.**
⇒ 사본 러너는 충실하다. 아래 C 팔의 차이는 **전부 술어에서 온 것**이다.

| 시드 | | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 | ㉮ | ㉯ |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **1020** | A(기준선) | 6,889 | 0/51 | 1,012 | 5,509 | 2,539 | 11,892.1 | 803.6 | 37,240.0 | 1.93 | 2.02 |
| | **C(제품 땅)** | **6,814** | **0/50** | **1,119** | **7,250** | **2,424** | **13,186.4** | **542.0** | **43,662.4** | **2.01** | **2.11** |
| | 차 | **−1.1%** | — | +10.6% | **+31.6%** | −4.5% | +10.9% | **−32.6%** | **+17.2%** | +0.08 | +0.09 |
| **7** | A(기준선) | 7,148 | 0/51 | 1,030 | 7,830 | 2,714 | 12,039.3 | 551.1 | 37,779.9 | 1.88 | 1.97 |
| | **C(제품 땅)** | **6,394** | **0/50** | **954** | **5,605** | **2,564** | **10,319.1** | **456.2** | **43,473.9** | **1.99** | **2.09** |
| | 차 | **−10.5%** | — | −7.4% | **−28.4%** | −5.5% | −14.3% | −17.2% | **+15.1%** | +0.11 | +0.12 |
| **42** | A(기준선) | 6,259 | 0/51 | 978 | 5,946 | 2,648 | 11,013.2 | 663.3 | 38,902.1 | 1.95 | 2.04 |
| | **C(제품 땅)** | **6,902** | **1/50** ⚠ | **1,015** | **6,966** | **2,435** | **11,865.2** | **972.4** | **41,851.4** | **1.97** | **2.07** |
| | 차 | **+10.3%** | **0 → 1** | +3.8% | +17.2% | −8.0% | +7.7% | **+46.6%** | **+7.6%** | +0.02 | +0.03 |

**읽는 법 — 세 줄.**

1. **인구는 시드마다 방향이 다르다**(−1.1% / −10.5% / +10.3%). 평균으로는 거의 안 움직이지만
   **분산이 커진다** — 땅이 달라지면 어느 마을이 좋은 자리인지가 바뀌기 때문이다.
   ⇒ "기준선이 조금 나빠진다/좋아진다"가 아니라 **다른 표**가 된다.
2. **한 방향으로 확실히 움직이는 열이 둘 있다**: 생곡 **+7.6~17.2%**(세 시드 전부 증가) ·
   밀도 ㉮ **1.93/1.88/1.95 → 2.01/1.99/1.97**(세 시드 전부 느려짐 · 캐논 2~3일 안쪽 유지).
   해안선 띠가 물을 물로 보게 하니 비옥도가 오르고(§0ⓑ 중앙 0.99→1.00), 곡물이 늘고, 사건이 성긴다.
3. ⚠**소멸이 0 이 아니게 된다**(시드 42 · 0/51 → **1/50**). 기준선 표의 "소멸 0" 은
   **A 땅에서만 참**이었다. 이 한 칸이 PM 판정의 값이다.

**그리고 세계가 50마을이 된다** — `어촌6` 은 제품 술어에서 중심 주변 24셀이 전부 물이라 안 선다
(`villages.js:1144` 스킵 · T251 §0ⓑ). 세 시드 전부 `인구있는 마을 50`.
⇒ 모으면 기준선 표의 분모(`/51`)도 **`/50` 으로 바뀐다**. 실서버가 51곳이면 이 줄이 틀린 것이고,
50곳이면 **제품은 원래 50마을 세계**이고 기준선 표가 여태 51마을이었던 것이다(재민 실기 · T251 §0ⓒ).

### 항등식 — **안 닫힌다**(그리고 그게 결과다)

궤적은 표본 날만 찍으므로 항등식을 못 센다. 그래서 자가 **하루도 빠짐없이** 네 항을 더하게 했다
(산 마을에서 `dailyProductionBuf.wood` · `_consDay.wood` · `tradeStats.woodImported` · `tradeStats.exportBy.wood`).

```
창설부존 6,001.60 + 유입 24,726.59 − 유출(연료+건축) 22,245.48 + 수입 2,213.68 − 수출 4,334.56
  = 6,361.83        (실제 끝 재고 629.39)        ⇒ **잔차 +5,732.44** (유입의 23.2%)
```

잔차가 **양수**다 = 장부에 잡힌 것보다 **목재가 더 많이 나간다**. 유출 항이 덜 세어진다는 뜻이고,
코드에서 그런 자리 하나를 찾았다:

* `sim/economy-sim.js:2729` — **사냥꾼 활 자급**. `v.storage.wood -= _bAmt * SELF_BOW_WOOD` 를 하면서
  **`_cons(v,'wood',…)` 를 안 부른다.** 같은 파일의 다른 두 목재 차감(연료 `:3027` · 주거 `:3046`)은
  바로 다음 줄에서 `_cons` 를 부른다 — **이 한 자리만 빠졌다.**
  (200일 사냥꾼 224명 규모면 활대만으로 수백 단위다 — 잔차 전부는 아니다.)

⇒ **T207 의 "목재 유출은 연료+건축 둘뿐" 규약은 세 번 틀렸다**: 교역이 셋째 항이고,
활 자급이 넷째이며, 잔차 5,732 은 아직 이름이 없는 다섯째가 더 있다는 뜻이다. **회부한다**(구현 0).

## §0ⓔ 답 한 줄

> **25벌: 제품 땅 0 · A급 25 · B급 0**(술어가 **바이트 한 벌**이다 — 25파일이 12만 점에서 한 값도 안 다르다) ·
> **기준선 자 `t17-metrics`·`t176-ab`·econ 랩 `econ-lab-real` 셋 다 A급**(거의 제품 땅 — 50마을 중 39곳 전 열 동일) ·
> **`t17-metrics` C-땅 3시드 인구 6,814 / 6,394 / 6,902**(A 6,889 / 7,148 / 6,259 — 방향이 시드마다 다르고 **소멸이 0→1**) ·
> **`wProd` 재독: "유입 0" 은 계측 칸의 결함**(`dailyProductionBuf` 가 `SERIALIZE_SKIP`) — 고치니 날 30부터 서고
> 다른 열은 **전부 비트 동일**, 그리고 **목재 항등식이 5,732(23%) 안 닫힌다**.

## §1 검증

* **제품 코드 0** — `server/`·`sim/`·`lab/`·`public/` `git diff` **0**. `zone.js` 술어 내보내기 **안 했다**(#31 재민 뒤).
* **17벌 무변** — 25개 계측기 파일 `git diff` **0**(표만 냈다 · ④ 는 임시 사본).
* **`t17-metrics`·`t176-ab` 무접촉** — `git diff` 0. ④ 사본은 `scripts/.t259-t17-*.js` 로 만들고 **즉시 지운다**
  (팔 A 로 돌리면 바꾼 줄 **0개** 라고 자가 스스로 찍는다).
* **자명 통과 금지** — ③ 고치기 전 판은 `wProd` **전 구간 0** · ④ 팔 A 는 `인계/공통.md §2` 기준선 **열 개 전부 재현**.
* **비트 동일(③ 다른 열)** — 고친 판 vs T251 §0ⓓ 판: 잎 1,394 중 다른 곳 **63**, **63 전부 새 열**, 그 밖 **0**.
* **러너 전수 안 돌렸다**(카드 규약) · 실서버 접속 **0**.
* 접점: `isTerrainBlockedLocal`·`isWaterTileLocal`·`isBridgeLocal`·`makeTerrainAdapter`·`zone-preds`·
  `t251-land-diff`·`t222-life800`·`dailyProductionBuf`·`t17-metrics`·`t176-ab`·`econ-lab-real`·
  `prepareFert`·`extractLandParamsApprox`.

## §2 회부(구현 0)

1. ★★★**[#31 · PM/재민] 모으는 비용은 생각보다 싸고, 대가는 생각보다 크다.**
   싸다 — 25벌이 **바이트 한 벌**이라 `zone-preds.js` 로 바꾸는 것은 **파일마다 한 줄**이다.
   크다 — 기준선 표가 **다른 표**가 된다(인구 −10.5%~+10.3% · 생곡 +7.6~17.2% 전 시드 증가 ·
   **소멸 0 → 1** · 마을 51 → **50**). 셋 다 §0ⓓ 표에 있다. **기준선을 새로 세울 각오가 곧 비용이다.**
2. ★★★**[러너 · 먼저 볼 것] A 땅 위에서 회귀 판정이 내려지고 있다.**
   러너가 실제로 도는 하네스 셋(`test-distmatrix`·`test-frontier-iron`·`test-onboarding`)이 A 술어를 쓴다.
   모으기 전에 **이 셋의 단정이 C 땅에서도 서는지**를 먼저 봐야 한다(안 서면 모으기가 회귀로 보인다).
3. ★★**[데이터 · 조용한 것] 마이그레이션 둘이 A 땅으로 DB 를 썼다.**
   `scripts/migrate-db-endowment.js`(부존 소급) · `scripts/migrate-terrain-sw4.js` 가 A 술어다 —
   **이미 쓰인 값이 A 땅 값**이다. 실서버 부존이 §0ⓒ 실기와 어긋나면 여기가 먼저 의심 대상이다.
4. ★★★**[새 카드] 목재 장부가 안 닫힌다 — 잔차 5,732(유입의 23%).**
   확인된 한 자리: `sim/economy-sim.js:2729` **활 자급이 `_cons` 를 안 부른다**(이웃 두 줄은 부른다).
   나머지는 아직 이름이 없다. `_consDay` 만 보는 모든 목재 판단(T207 규약 · T247 §0ⓐ)이 이 위에 있다.
   ⇒ **T247 §0ⓐ 의 "유입 0" 은 철회**(계측 칸) · **"유출은 둘뿐"은 정정**(교역이 셋째 · 활이 넷째).
5. ★**[실기 · 재민] T251 §0ⓒ 한 줄이 아직 안 왔다.** `villages` **50/51** 이 §0ⓓ 의 분모를 정하고,
   `fertMed` **1.0/0.42** 가 T251 전체를 정한다. **#31 판정은 이 값 뒤가 안전하다.**
6. ⓘ**`어촌6` 좌표**(T251 회부 5) — C 땅에선 세 시드 전부 안 선다. 실기가 50 이면 좌표가 아니라 **표의 분모**가 문제다.
