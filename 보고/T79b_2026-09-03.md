# T79b — 작물 아이콘 b (2026-09-03 · 세션8 ART)

지시서 `~/Mini/지시_T79b.md` · 브랜치 `batch/crops-icons-b-0903` · 굽는 기준 `origin/main d2f4b4fd`

**푸시 완료 `d16c9460`** (2026-09-03 · `batch/crops-icons-b-0903` → ff → main).
굽기 기준 `d2f4b4fd` → 착지 전 `fd9b2cc8` 로 리베이스(T81 옷 층 + 보고 2커밋).
겹친 파일은 `인계/ART-자산.md` 하나 — 둘 다 절을 덧붙인 충돌이라 **양쪽을 다 남겼다**
(origin/main 대비 삭제 줄 0). T81 이 만진 것 중 내 자산·잠금과 겹치는 것은 **없다**
(`char/*` · `char_sheets.lock.json` · `char_render.py` — 내 `icons.lock.json` 과 별개).
리베이스 뒤 재검: `test-icons`·`test-render-common`·`test-props`·`test-itemlabel`·`test-crops` PASS ·
`e2e-inv` 94/0.

★**`e2e-charsprite` 가 62/4 로 빨갛다 — 내 커밋 탓이 아니다.** 귀속을 재 봤다:
`origin/main`(내 커밋 **없이**) 을 별도 워크트리로 떼어 돌려도 **똑같이 62 통과 · 4 실패**다.
실패 넷은 전부 "남이 보인다/남의 애니" 절(`[A] 화면에 남이 스프라이트로 보인다 0명` 외 셋) —
T81(세션2) 영역이다. 회부에 적었다.

채소 9 · 양념 3 · 박과 2 · 특용 4 · 구황 2 = **수확물 20 + 씨앗 20**.
이로써 작물 **34종이 전부 아이콘을 갖는다**(68장).

---

## §0-ⓐ 특용 4종의 씨앗 — **전부 실재한다. 굽지 않을 것이 없다**

| 잰 것 | 결과 |
|---|---|
| `isFood` | 삼·쪽·뽕·차 **false**(비식품 — `NON_FOOD_GROUPS`) |
| `isSeed('seed_hemp_plant')` … | **넷 다 true** · `cropOfSeed` 왕복 일치 |
| `weights.kgOf(씨앗)` | **0.02** (`SEED_KG` — 다른 30종과 같다) |
| 야생 채종 `wildSeedAt` | 1년·625칸 표본에서 **34종 전부** 나온다 — 특용 4종 포함 |
| 수확 반환 `zone.js:4247` | `seedOf(_cid)` 를 **식품 여부와 무관하게** 돌려준다 |
| `CHEST_ALLOWED_ITEMS` · `LOT_CORE` | `Crops.list()` 전수에 `seedOf` 를 더한다 |

⇒ **20종 전부 굽는다.** "없는 품목에 그림 0"(T66 규약)에 걸리는 것이 없다.

★뽕은 실물로는 **잎을 따는 나무**라 묘목이 맞지만, 이 게임의 모델에서는 `growDays 70` 짜리
한해살이 작물이고 씨앗으로 심는다(`plantMenuPayload` 가 34종을 다 돈다). **서버 모델을 따랐다** —
나무로 갈 거면 그건 그림이 아니라 작물 층의 판정이다(회부).

## §0-ⓒ 씨앗 아닌 넷 — 접시 문법을 깬다

| 작물 | 실제로 심는 것 | 그림 |
|---|---|---|
| 마늘 | **쪽**(통마늘이 아니다) | 쪽 셋 · 붉은 기 도는 껍질 |
| 생강 | **뿌리줄기 조각** | 마디 둘 + 눈(싹) |
| 토란 | **씨토란** | 작은 갈색 덩이 |
| 마 | **덩이 토막** | 짧게 자른 울퉁불퉁한 토막 |

키(`seed_<id>`)는 서버 정본이라 **그대로** 두고 그림만 실물을 따랐다.
⇒ 계보가 오히려 더 팽팽해졌다 — 심는 것이 **그 물건 자체**라 같은 빌더(`_tuber`)가 그대로 쓰인다.
소스로 못박았다: `NOT_SEEDS = ['garlic','ginger','taro','yam']` · `test-icons ⑦` 이 빌더 본문을 읽어
"그 넷은 접시를 안 쓰고 나머지 30종은 쓴다"를 잰다.

### 씨앗 20종의 색 — 실물에서(안 그러면 접시 위가 전부 갈색 점이다)

**검은 좁쌀** 배추·순무·부추·대파·쪽 · **흰 납작** 오이·박·참외(박씨가 가장 크다) ·
**갈색** 무·미나리·뽕 · **잿빛 얼룩에 윤** 삼 · **수레바퀴꼴** 아욱 · **옅은 납작** 가지·상추 ·
**굵은 갈색 알** 차(도토리만 하다 — 20종 중 알이 가장 크다).

## §0-ⓑ 26px 눈 검사 — 아슬아슬한 셋

| 짝 | 96px | 26px |
|---|---|---|
| 배추 · 상추 · 아욱 | 갈린다 | 배추만 갈린다(속통이 밝다) |
| **상추 vs 아욱** | 갈린다(상추는 활짝 · 아욱은 **잎자루가 보인다**) | ★**가장 아슬아슬** — 둘 다 초록 덩이 |
| 무 vs 순무 | 갈린다 | 갈린다(길이 · 자줏빛 어깨) |
| 마늘 · 생강 · 대파 | 갈린다 | 갈린다(흰 통 · 누런 마디 · 흰 대) |
| 씨앗 20종 서로 | 갈린다(색 · 알 크기) | 안 갈린다 — 전부 갈색 원반 |

★마지막 줄은 **의도한 것**이다. 26px 이 지는 일은 "수확물이냐 씨앗이냐"이고(선 것 vs 눕은 원반),
어느 씨앗이냐는 96px 과 이름표가 진다. 크기로 못 가르는 이유는 T79 §0-ⓐ 그대로다.

## 눈으로 잡은 것 — 4패스

| 패스 | 잡은 것 | 고침 |
|---|---|---|
| 1 | **잎 여덟 종이 전부 "초록 종이"** | `plane` 은 모난 사각형이다. `_blade()` 신설 — 납작하게 누른 이코스피어(끝이 좁아지는 둥근 판) + **잎맥 한 줄**. 잎맥이 96px 에서 '잎'을 못박는다 |
| 1 | 배추가 **초록 보에 싼 달걀** | 속을 줄였다가 3패스에서 **다육식물**이 됐고, 4패스에 **통을 몸으로** 삼고 겉잎 몇 장만 밑에 눕혀 해결 |
| 2 | 부추와 대파가 같은 **초록 별** | 대파 잎은 **둥근 관**이다 — `tube=True` 를 열었다(누르지 않는다). 부추는 납작하고 거의 곧게 |
| 2 | 박씨와 참외씨가 같다 | 박씨를 **훨씬 크게**(실물이 그렇다) |
| 3·4 | 상추 vs 아욱이 같은 덩이 | 아욱에 **잎자루(stalk)** 를 줘 잎이 대에서 옆으로 벌어지게 |

## 굽기 · 배포

| 스크립트 | 장수 | 시간 |
|---|---|---|
| `models_crops.py` (68장 전수) | 수확물 34 + 씨앗 34 | **4분 20초** |
| `icons-postprocess.js` 68장 | — | 2초 미만 |

배포 `public/assets/icons/` **138장**(98 + 신규 40). `icons.lock.json` 갱신 — 신규 40 · **기존 변경 0**.

★**기존 98장 IDAT 무변이 잠금 대조로 증명된다** — 새 빌더 넷과 `_blade` 를 넣고 68장을 전수로
다시 구웠는데 작물 a 28장과 나머지 70장의 화소 해시가 잠금표와 **한 자도 안 달라졌다**.

## 하네스

`test-icons ⑦` 을 68키 전수로 넓혔다:

* `CROPS_A`(14) + `CROPS_B`(20) = **34 = `crops.list().length`** — 종이 늘면 여기가 먼저 빨개진다
* 68장 96×96 · 알파 · 서버 작물/씨앗 규약 왕복 · **비식품 4종의 씨앗도 서버 품목임**을 확인
* 계보 **34짝** 같은 빌더 · `NOT_SEEDS` 넷은 접시를 안 쓰고 나머지 30은 쓴다(빌더 본문으로 판정)
* 68키 배선 확인 · ⑧ 잠금 대조(138 + 14)

돌연변이 2건으로 눈멀지 않았음을 확인:
마늘 씨앗을 접시로 바꾸니 계보와 접시 검사가 **둘 다** 잡았고(`garlic(tuber) vs seed_garlic(leaf)`),
`CROPS_B` 에서 하나를 빼니 20종·34종·"남은 것 없음" 셋이 잡았다.

## 검증 (공통 §2 · 통합 러너 전수 없음)

| 하네스 | 결과 |
|---|---|
| `test-icons`(⑦ 68키 확장) | **PASS** · 돌연변이 2건 검출 |
| `test-render-common` · `test-props` · `test-crops` · `test-itemlabel` | **PASS** |
| `test-chron-icons` · `test-client-globals` | **PASS** |
| `e2e-inv` | **통과 94 · 실패 0** |

* 게임 코드 diff — `public/client/43-i-icon.js` **+12줄(목록)** 뿐. `server/`·`sim/` 무접촉.
* 랩 로드 18파일 교집합 **∅**. T82(세션4)·T81(세션2) 무접촉.
* 적재 추적 — `models_crops.py` 는 `render_common` 만 물고 게임 코드를 안 문다.

### ★T79 이 흘린 것을 여기서 치웠다

`scripts/crop_icon_renders/` **28장이 T79 커밋에 딸려 들어가 있었다** — PM 판정 ③(중간 산출 삭제)과
같은 종류의 실수를 내가 새로 만든 것이다. `git rm --cached` 로 추적에서 빼고 `.gitignore` 에
`scripts/crop_icon_renders/` 를 넣었다(T79 가 넣은 `icon_renders/`·`crop_renders/` 옆에).

## 푸시 위생 (T79 보고에서 빠졌던 것 — PM 지적)

* **force push 0회.** 이 세션은 `--force`/`-f` 를 **한 번도** 쓰지 않았다(T67·T72·T76·T77·T79·T79b 전부).
  푸시는 언제나 `merge-base --is-ancestor` 로 fast-forward 를 먼저 확인하고, 아니면 멈추고 리베이스했다.
* **맥 클론 `.git/*.lock` — 매 푸시 뒤 확인, 언제나 0.** T79 확인 결과 `lock 없음 ✓ · tmp 팩 없음 ✓`.
  남은 `tmp_idx_*`/`tmp_pack_*` 은 device 가 unlink 를 막아 생기는 것이라 `~/Mini/_to_delete/` 로 격리했다
  (T77 4개 · T79 이후 0개). 맥 클론에 인덱스를 쓰는 git 명령(`merge`·`checkout`·`status` …)은 **쓰지 않았다** —
  `fetch`·`push`·`log`·`rev-parse`·`merge-base` 만.
* 브랜치 먼저(`batch/…`) → main 재확인 → main. main 이 움직였으면 멈추고 리베이스(T77·T79 실제로 각각 1회).

## 회부

1. **[ART] T79c — 세계 8군 세트**(PM 판정). `crops.json` 의 `group` 으로 고른다(좌표 해시가 아니라).
   4.0.2 작물 스프라이트 8장 5.0.1 재굽기를 **같이** 해야 하는데, T79 회부 3 이 아직 살아 있다 —
   **512² → 64px 줄이는 단계가 문서에 없다.** 그것부터 찾아 적어야 화풍이 안 바뀐다.
2. **[작물 층] 뽕은 나무인가 한해살이인가.** 지금 서버는 `growDays 70` 한해살이로 다루고 씨앗으로 심는다.
   실물은 잎을 따는 나무(묘목·꺾꽂이)다. 그림은 서버를 따랐다 — 바꿀 거면 작물 층의 판정이 먼저다.
3. **[ART] 상추 vs 아욱이 26px 에서 안 갈린다**(§0-ⓑ). 잎채소를 더 벌리려면 색을 갈라야 하는데
   둘 다 초록이라 고증과 부딪친다. 판단이 필요하면 재민.
4. **[ART] 옷 아이콘 여섯**(T81 뒤 · 같은 재질) · **옛 `item_*` 셋**(바닥·계단·농지) · **지게 등짐**.
5. **[ART] nature/building/char 5.0.1 재굽기** — 각자 카드. 굽는 기계 정본은 `인계/ART-자산.md` §13.
6. **[ART] `nature_render.py:333` · `char_render.py:801` `sensor_fit` 없음** (T67·T77·T79 회부 유지).

## 실기 (재민)

`산그림/디자인B/아이콘3차_작물b.png` (96px 위 · 26px 아래 · 빨강 = 씨앗이 아닌 넷 / 아슬아슬한 셋)

1. **마늘·생강·토란·마의 "씨앗" 칸이 쪽·뿌리줄기·덩이로 읽히는가** — 접시를 깬 넷이다.
2. **상추와 아욱이 26px 에서 갈리는가** — 내 눈에는 안 갈린다(회부 3).
