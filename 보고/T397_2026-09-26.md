# T397 — pid 충돌 · 맞음 낱말 · ⑧g · 28/30 · 표면·날씨 (세션9 · 2026-09-26)

## 결론 (머리 15줄)

1. **pid 충돌 ○(재현됐다)** — 이웃 존 관전 연결의 `p1` 이 내(`p1`) HP 를 7 로 바꾸고, 부활로 나를 `(100050,50)` 에 **텔레포트**시키고, 그 사람의 쓰러짐은 "나"로 읽혀 **버려졌다**(`e2e-pid-collide` 신설 · 고치기 전 7/3 → 후 **10/0**). `30-n-net.js` 세 줄에 `&& c.role === 'primary'`.
2. **`why` 두 줄** — `zone.js setHp` 의 `hp_changed` 방송·자기 송신이 받던 `why` 를 싣는다(새 낱말 0). 
3. ★★**그런데 `damage` 로는 "맞음"을 못 가른다** — 호출부 전수: `damagePlayer` 가 짐승·화살·주먹·낙하·독 음식에 **극단 감소**(추위 3.6초·갈증 21.6초·허기 43초마다 1HP)까지 전부 `'damage'` 하나로 보낸다. 이으면 추위 속에서 3.6초마다 맞는 소리가 나고 방송이라 곁 사람도 듣는다.
4. ⇒ `hit_body` 는 **확보만**(1번·2번 후보) · 표 `hpWhy` 에 `damage` 줄은 **비워 뒀다**. 서버가 극단 감소를 다른 낱말로 보내는 날 표 한 줄이면 운다 — e2e ㊱ 이 그 한 줄을 넣어 **운다(1) · 옛 전문·회복은 안 운다(0)** 를 이미 쟀다.
5. ⑧g 항진 → **잰 미끼**로: 옛 줄 `!(x*0.9 > x)` 는 값과 무관하게 참이었다. 이제 ⑧c 와 **같은 술어**가 `_실측.bait`(문턱 넘는 옛 조합 0.8755)를 빨갛게, 최악 0.7957 을 초록으로 판정해야 통과.
6. **28 vs 30 = 자의 변화** — `sfx-census` 가 자원 칸을 **줄 번호 1513~1526** 으로 잘랐는데 그 위에 두 줄이 늘어 `ore`·`meteorite` 가 창 밖으로 밀렸다(T354 착지 `83a42c2c` 와 main `259dadb` 행 대조 — 나머지 차이는 새 메시지 `far_trees` 둘과 T387 전투 넷뿐). 꼴로 긁게 고치고 둘을 자 검증에 넣었다.
7. **표면 셋 배선** — 표 `surface`(`floor`→`step_floor` · `vtile`→`step_yard` · `farmland`→`step_farm` · 겹치면 바닥 > 마당 > 밭 · 바닥은 내 층만). 후보 2번 셋.
8. ★**눈이면 빗소리가 났다** — 종전엔 기온과 무관하게 강수면 빗소리. 이제 그리는 층의 판정(`kind:'snow'`)을 읽어 빗소리 0. 눈 자체 소리는 **없음**(PD/CC0 에서 못 찾음 · 실제로도 거의 무음).
9. **천둥 = 세계에 사건이 없다** — 서버·클라 날씨에 칸 0(카드의 "`_sfxWx` 칸이 이미 있다"는 틀렸다). 파일만 후보 둘(PD · 훅 0). Commons 천둥 8장은 BY-SA 7·BY 1 이라 뺐다.
10. `water_pool` 후보 둘(PD · 8.0s 반복 · 이음새 −49.8/−24.5 dB).
11. 확보 **12파일**(배선 3 · 후보 9) · 출처 없는 소리 0 · 새 CC-BY 0 · `bgm/` 무접촉 · 서버 두 줄 · `30-n-net` 세 줄 · 기존 29키 비트 동일.
12. 하네스: `test-audio` 145→**158** · `e2e-audio-probe` 44→**58** · `e2e-pid-collide` **10/0**(신설) · `e2e-aim` **15/0** · `test-client-globals` 26 · `test-harness-lint` 61 · `test-assets-audit` PASS · lock 548/0.
13. 헤드룸: 41키 재측정 — 최악(후보 뺀 여섯) 0.7957 그대로 · 발자국 셋은 조합에 안 든다(넣어 재도 0.7944) · 41키 전부 없이 1.2706/클리핑 132 → 끼고 0.7791/0.
14. 빠진 소리 전수: **170자리 · 있음 38 · 없음 132**(T354 30 → 38: 전투 넷 · 눈 · 표면 셋 · ore/meteorite 는 되찾음).

---

## ⓐ pid 충돌 (①)

**전제 둘(코드 꼴로 확인)** — `let nextPid = 1` · `` const pid = `p${nextPid++}` ``(존 프로세스마다 1 부터) · `broadcast` 는 `observers.keys()` 에도 보낸다. 그러니 두 존이 떠 있으면 각 존의 첫 사람은 둘 다 `p1` 이다.

**재현 하네스** `scripts/e2e-pid-collide.js`(@nightly B · 정적 페이지 · 존 서버 0): 진짜 클라의 `handleMessage` 에 연결 둘(주 `zA` · 관전 `zB`, `connect()` 가 만드는 그 모양)을 세우고 `myPid='p1'` 로 둔 뒤 진짜 모양의 전문을 먹인다.

| 전문(관전 B · pid p1) | 고치기 전 | 고친 뒤 |
|---|---|---|
| `hp_changed {hp:7}` | **내 HP 100 → 7** ✗ | 100 ○ |
| `player_respawn {x:50,y:50}` | **내 좌표 (500,500) → (100050,50)** — 남의 부활이 나를 이웃 존으로 텔레포트 ✗ | (500,500) ○ |
| `player_down_state {isDown:true}` | `downStates` 에 **안 들어감**(나로 읽혀 버려짐 — 그 사람이 쓰러져 보이지 않는다) ✗ | 들어감 ○ |
| 대조 — 같은 전문을 **주 연결**로 | HP 7 · 좌표 (50,50) · 남 목록 X | 그대로 ○ |
| 다른 pid(p2) 관전 연결 | 남으로 기록 | 그대로 ○ |

고친 것 — `30-n-net.js` 1326·1333·1371 세 줄에 `&& c.role === 'primary'`(이미 있는 구분 · T387 이 소리 층에서 쓴 그 규칙). T379 포인터 캡처 줄 무접촉.

⚠실제로 터지는 조건: 존이 둘 이상 떠 있고 내가 경계 근처라 관전 연결이 열려 있을 때. 지금 운영은 존 하나라 **잠복**이다.

## ⓑ `why` 두 줄 + `setHp` 호출부 표 + `hit_body`

**서버 두 줄**(`zone.js setHp`): `hp_changed` 의 방송(HP_PEER)·자기 송신에 `why: why || ''`. 새 낱말 0 · 부르는 쪽 무변.

**`setHp` 호출부 전수**(짓지 않았다 — 코드 그대로):

| `why` | 자리 | 뜻 | 나감 | 소리? |
|---|---|---|---|---|
| `damage` | `damagePlayer` (10563) | **모든 피해** — 아래 여덟 출처 | 방송 | ★보류 |
| · `mob:<종>` | 12546 | 짐승이 물었다 | | 맞음 |
| · `wild:<행동>` | `wildlife.js` 801 | 야생 습격 | | 맞음 |
| · `arrow:<id>` | 10246 · 4047(`/cross_damage`) | 화살 | | 맞음 |
| · `player:<이름>` | 10420 | 주먹·무기(PvP) | | 맞음 |
| · `fall` | 12262 | 계단에서 떨어짐 | | 맞음에 가깝다 |
| · `food:<item>` | 5804 | 독 있는 음식 | | ? |
| · **`extreme:<축>`** | **12323** | **추위·갈증·허기 극단 감소** — 1HP 씩, 추위 최대 3.6초마다 | | **아니다** |
| `food` · `dish` | 5805 · 6047 | 먹어서 회복 | 방송 | 아니다 |
| `rescue` | 10958 | 구조로 일어남 | 자기에게 | 아니다(`wake_up` 이 운다) |
| `debug` | 5193 | 시험 | 방송 | 아니다 |
| `fall`(NPC) | 12264 | 주민 낙하 | 아무 데도(ws 없음) | — |
| `regen` · `respawn` · `takeover` | 12363 · 10584 · 4787 | 조용한 문(HP_QUIET) | 안 나감 | — |

⇒ `why` 만으로는 "맞음"과 "굶주림 틱"이 **같은 글자**다. 피해량으로 가르는 것은 짐작이고 틀린다(`wildlife` 는 `Math.round(d)` 라 1 이 나올 수 있다). 그래서:

* `hit_body`(1번 `impactPunch_medium_001` · 중심 801Hz) · `hit_body_b`(2번 `…_003` · 1029Hz) **확보 · 후보**.
* 표 `hpWhy` = `{ msgType: 'hp_changed' }` 뿐 · `_damage_보류` 칸에 이유. 층은 표를 읽는 코드가 이미 있다(`t === HW.msgType` — 메시지 이름도 표가 댄다 · ⑮g 무위반).
* **풀리는 길**(PM · 서버 한 줄): `damagePlayer` 가 `source` 가 `extreme:` 이면 `setHp(…, 'extreme')` 처럼 다른 낱말을 대면, 표에 `"damage": "hit_body"` 한 줄로 끝. ⚠그러면 `extreme` 은 `HP_PEER` 밖이라 **자기에게만** 가게 된다(남에게 굶주림 틱이 안 간다 — 오히려 맞다) — 방송 규약이 바뀌므로 PM 판정.
* `test-audio ⑯b2` 가 지킨다: `damage` 가 극단 감소를 나르는 동안 `hpWhy.damage` 를 채우면 빨강.

## ⓒ ⑧g 반례

| | 옛 줄 | 새 줄 |
|---|---|---|
| 식 | `!((worstPeak*0.9) > worstPeak)` | `!kneeHolds(bait.peak) && kneeHolds(worstPeak)` — `kneeHolds` 는 ⑧c 가 쓰는 **그 술어** |
| worstPeak=5(말도 안 되는 값) | **참**(항진) | 거짓 |
| 미끼 | 없음 | `_실측.bait` = 옛 T292 조합(후보 `bronze_hit` 포함) · 리미터 없이 **0.8755** · 문턱 넘는 창 2/400 |
| 판정 | — | 미끼 → **빨강** · 최악 0.7957 → 초록 ○ |

⑧g2: 미끼 조합의 키가 전부 표에 있다(없는 키로 잰 미끼는 거짓).

## ⓓ 28 vs 30

`sfx-census` 를 T354 착지(`83a42c2c` · worktree · 그때의 자)와 main `259dadb`(지금의 자 · T387 이 `combat` 표를 읽게 한 판)에 돌려 **행 단위로** 맞댔다(T387 전투 넷 − → O 는 제외 — 그건 소리가 붙은 것이다):

| 행 | T354 | main |
|---|---|---|
| 자원/`ore` | O(`bronze_hit`) | **행 없음** |
| 자원/`meteorite` | O | **행 없음** |
| 서버 메시지/`far_trees` · `far_trees_done` | 행 없음 | −(새 메시지 · 무음) |

자리 수는 168 그대로(−2 +2)였고 "있음"만 30 → 28. **자의 변화**다: 자원 칸을 `34-m-renderloop.js` 의 `.slice(1512,1526)` — 줄 번호로 잘랐는데, 그 위에 두 줄이 늘어 `ore`·`meteorite` 두 줄(1527·1528)이 창 밖으로 밀렸다. 자리는 멀쩡히 `rock_hit` 으로 울고 있었다.
고침: 꼴(`item.r.type === '…'`)로 파일 전체를 긁는다 · `ore`·`meteorite`·`farmland` 를 자 검증(빠지면 exit 1)에 넣었다 · 문서(`문서/빠진소리_전수_2026-09-23.md`) 머리에 정정 단락.

## ⓔ 갈래 둘 확보 표

라이선스는 파일 페이지에서 읽었다(Kenney = 팩 안 `License.txt` · Commons = API `LicenseShortName`). 원본 맥 `~/Mini/_sfx_in/_T397/`(+`SOURCE.txt`).

| 키 | 배선 | 원본 | 출처 · 라이선스 | 구간 | 길이 | 피크 ogg/m4a | 메모 |
|---|---|---|---|---|---:|---|---|
| `step_floor` | ○ `surface.floor` | `footstep_carpet_003` | Kenney Impact · CC0 | 0.000~0.067 | 0.07s | −1.66/−1.50 | 돗자리 걸음(중심 2519Hz) |
| `step_floor_b` | 후보 | `footstep_wood_003` | 〃 | 0.000~0.074 | 0.07s | −1.37/−1.58 | 나무 — **중심 171Hz**(노트북에선 거의 무음) |
| `step_yard` | ○ `surface.vtile` | `footstep_concrete_000` | 〃 | 통째 | 0.10s | −1.52/−1.44 | 다짐 바닥 · ⚠텃밭(`vtile.kind=garden`)도 이 소리 |
| `step_yard_b` | 후보 | RPG `footstep02` | Kenney RPG · CC0 | 0.010~0.212 | 0.20s | −1.65/−1.68 | 흙 계열 |
| `step_farm` | ○ `surface.farmland` | RPG `footstep09` | 〃 | 0.002~0.215 | 0.21s | −2.19/−1.48 | 안 쓴 걸음 중 가장 둔함(1127Hz) |
| `step_farm_b` | 후보 | RPG `footstep06` | 〃 | 0.000~0.208 | 0.21s | −1.69/−1.52 | 1450Hz |
| `hit_body` | 후보(보류) | `impactPunch_medium_001` | Kenney Impact · CC0 | 0.000~0.218 | 0.22s | −1.29/−1.52 | ⓑ |
| `hit_body_b` | 후보 | `impactPunch_medium_003` | 〃 | 0.000~0.228 | 0.23s | −1.53/−1.51 | |
| `thunder` | 후보(사건 없음) | `Thunder and rain on a v.ogg` | Commons **PD** · ezwa | 5.800~8.800 | 3.0s | −1.96/−1.97 | 단발 상한 3s — 꼬리 1.2s 페이드 · 빗소리 깔림 |
| `thunder_b` | 후보 | `Rain and thunder (1).ogg` | Commons **PD** · ezwa | 3.400~6.400 | 3.0s | −2.11/−2.17 | 원본이 작아 **+20.7dB**(빗소리도 같이 오른다) |
| `water_pool` | 후보(카드) | `Water bubbles chortling.ogg` | Commons **PD** · stephan | 35.75~43.75 +1.0s 겹침 | 8.0s 반복 | −6.17/−6.09 | 이음새 **−49.8 dB** |
| `water_pool_b` | 후보 | `Water over rocks as tide recedes.ogg` | Commons **PD** · earthcalling | 1.50~9.50 +1.0s | 8.0s 반복 | −5.91/−6.06 | 이음새 −24.5 dB |
| (눈) | **없음** | — | Commons "snow" 검색은 걸음(BY-SA)·스노볼뿐 · 실제 눈 내림은 거의 무음 | — | — | — | 대신 **눈이면 빗소리 0**(결함 수리) |

**안 쓴 것**: Commons 천둥 `Thunder 01`·`Thunder crack 2B`·`Thunder Claps`·`Thunder.wav`·`Booming Thunder`·`Very close thunder cracks`·`Summer thunderstorm…`(전부 CC BY-SA) · `Rhumphries thunder-storm`(CC BY 3.0 — PD 가 있어 뺐다) · `Water drops dripping`(BY-SA) · `Man walking on the snow`(BY-SA).

**배선 규칙**
* 표면: `sfxGroundKey` 가 **표면 먼저, 지형 나중**. 셀 환산은 지면 그리기의 식 그대로(`Math.floor(b.x/32)` · `10-r1-terrain _tsFarmSet`). `floor` 는 `b.floor === myFloor` 일 때만. 캐시는 (셀 · 건물 수 × 층)이 바뀌면 다시 접는다(㊲f — 밭을 놓는 즉시 소리가 바뀐다). ⚠건물 수를 안 바꾸는 교체(한 개 지우고 한 개 놓기가 한 틱에)는 다음 셀 이동까지 옛 소리 — 회부.
* 눈: `37-r1-weather` 의 `kind: snow ? 'snow' : 'rain'`(어는점 하나)을 `__rainDbg()` 로 읽는다 — 한 프레임 늦고 실내·무강수면 비므로 **마지막 판정**을 쥔다. 층에 `tempC < 0` 사본 0(⑯d2).

---

## §3 검증

| 자 | 전 | 후 |
|---|---:|---:|
| `test-audio` | 145 | **158/0** (⑧g·⑧g2 · ⑮i 문구 · ⑯a~⑯e 12) |
| `e2e-audio-probe` (@nightly B) | 44 | **58/0** (㉟ · ㊱a~d · ㊲a~f · ㊳a~c) |
| `e2e-pid-collide` (신설 · @nightly B) | — | 고치기 전 **7/3** → 후 **10/0** |
| `e2e-aim` (@nightly C) | 15 | **15/0** (`30-n-net` 세 줄 뒤 재실행) |
| `test-client-globals` | 26 | 26/0 |
| `test-harness-lint` | 61 | 61/0 |
| `test-assets-audit` | PASS | PASS |
| `asset-lock` | 524 | **548 · 어긋남 0** (sfx 68→92) |
| `sfx-census` 자 검증 | 6 | 9 · 성하다 |
| 소리판 행 | 29 | **41 = 키 41**(⑲) |

**비트 동일** — 기존 29키 `keys.*` 29/29 동일 · 기존 음원 무변 · 바뀐 최상위: `keys`(새 12) · `surface`·`hpWhy`(새) · `_실측`(재측정 + `bait`) · `rainSplit`(`_T397_눈` 글 한 칸) · `sources`(새 4 + Kenney 둘 `_원본` 글).

**접점** `hp_changed` · `setHp` · `why` · `damagePlayer` · `extreme:` · `player_respawn` · `player_down_state` · `myPid` · `c.role` · `30-n-net` · `48-a-audio` · `hpWhy` · `surface` · `sfxGroundKey` · `sfxSurfaceKeyAt` · `step_` · `farmland` · `floor` · `vtile` · `_sfxWxKind` · `__rainDbg` · `thunder` · `snow` · `water_pool` · `sfx-census` · `test-audio ⑧g` · `bait` · `sfx-board` · `CREDITS` · `_sfx_in` · `e2e-pid-collide`.

## §4 회부

1. **[PM · 서버 한 줄] 극단 감소 낱말** — `damagePlayer` 가 `extreme:` 출처를 `'damage'` 가 아닌 낱말로 보내면 `hit_body` 가 표 한 줄로 산다. 방송 규약(`HP_PEER`)이 따라 바뀌니 PM 판정.
2. **[재민] `food:<독>` 이 맞음이냐** — 독 음식 피해도 지금은 `damage` 다(극단 감소를 가른 뒤에도 남는 질문).
3. **[M/네트] 남 pid 도 존을 넘으면 겹친다** — 이 카드는 '나' 셋만 막았다. `downStates`·`onCarryState` 는 **pid 하나를 열쇠로** 쓰는 전역 지도라 A 존의 p2 와 B 존의 p2 가 한 칸을 나눠 쓴다(열쇠에 존을 붙여야 한다 · `30-n-net`/`46-h-verbs`).
4. **[재민] `vtile` 셋(yard·plaza·garden)** — 표는 타입 하나만 본다. 텃밭을 밭 소리로 가르려면 `surface` 에 `vtile:garden` 꼴 한 칸(층 코드 한 줄).
5. **[재민] 천둥** — 세계가 번개를 안 보낸다. 후보 둘은 판정용. 붙이려면 날씨가 사건을 대야 한다(서버).
6. **[재민 폰] 눈 소리 녹음** — 원하면(대개 무음이 맞다).
7. **[세션9] 표면 캐시** — 건물 수가 같은 교체는 다음 셀까지 옛 소리. 드물어서 뒀다.
8. #60 둘째 귀 판정(재민 · 소리판 새 행 12) · 남이 쓰러지는 소리 · `war_*` · `self_stat` · 깨어나는 숨 · CC-BY 둘 · 어부 다섯 +3.88 dBFS — T387 회부 그대로.
