# T414 보고 — 지시 폴더 이동 표 · 인계 README 09-26 정정 (세션11 · 2026-09-26)

- **한 줄 답**: `지시/` 전수 129장 이동 표 작성 완료(완료·main 115 · 진행 12 · 대기 1 · 보류 1) — **옮김 후보는 0장**(최근 7일 규칙 · ★참고 2건은 본문 참조). `인계/README.md` 영역 표 09-26 정정 5줄 추가(삭제 0). 이동(`git mv`) 자체는 **안 함**(PM 몫).
- 가지 `batch/doc-tidy-0926` · 내용 커밋 **`<해시>`**(+ 이 줄을 적은 해시 커밋 하나) · **[승인 대기 · T414]** · 베이스 **`<베이스해시>`**(origin/main)

## ① 이동 표

`지시/_이동표_2026-09-26.md` 신설. 방법: 이 기기 레포에 `태스크보드_v1.md` 가 없어 카드 지시대로 `보고/` 폴더 ADD-커밋을 **대량 1회 조회**(`git log --diff-filter=A --name-only -- 보고/`)로 129장 전부와 교차확인했고, 보고가 아직 없는 14장은 카드 본문·최근 배정표(T389·T404) 텍스트로 상태를 판정했다.

**★참고 (판정 아님 · 사실 확인 · PM 회부) — 표 파일에도 동일하게 적어 두었다:**
1. 카드 제목은 "지시/ 에 카드가 400장 넘게 쌓였다(T1~T412)"고 하지만, 이 레포의 `지시/` 폴더에는 **T295부터 129개**(추신/-b 포함)만 물리적으로 있다. T1~T294 는 이 레포에 미러된 적이 없어 보인다.
2. 완료 판정 115장의 보고 날짜가 전부 **2026-09-19~09-26(최근 7일)** 안이었다 — `지시/` 폴더 자체가 T295(09-19)부터 시작해 딱 최근 7일 폭이라, 문자 그대로 규칙을 적용하면 지금 시점엔 **옮길 카드가 0장**이다. 버그가 아니라 폴더의 시작점이 최근이라 생긴 결과.

## ② 인계/README.md 정정

`## 어디를 볼 것인가` 표 앞에 `세션배정_2026-09-26.md`·`_보고머리_템플릿.md` 포인터 한 단락 추가, SOUND 위임 줄에 09-26 재확인 각주 추가(sfx 세션9 · bgm 세션8), 아카이브 행 뒤에 세션13 GPT(`codex/*`·카드 없이 착지 0) 신설 행 추가, `## 규약` 끝에 판정대기.md(열림)/판정기록.md(아카이브·레포 밖) 구분 줄 추가(이 기기엔 두 파일이 안 보였다는 점도 회부로 명시). **낡은 줄 삭제 0** — 전부 추가만.

## ③ 지시/README.md

`[T414 2026-09-26]` 날짜 줄 + 한 줄 답 추가.

## 검증

- `git diff --stat origin/main -- .` → `인계/README.md`(+5) · `지시/README.md`(+2) · `지시/_이동표_2026-09-26.md`(신규) — 접점(`지시/|README|인계`) 안. `sim/`·`server/`·`public/client` 무접촉.
- `node scripts/test-harness-lint.js` → **61건 중 PASS 61 · FAIL 0** (전/후 동일).
- 판정 0 · 새 코드 0 · 파일 이동 0(표만, `git mv` 안 함).
