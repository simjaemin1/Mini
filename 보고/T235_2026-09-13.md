**푸시 — 착지 `5d1886f` · 바탕 `4fa443e` · 가지 `batch/central-posts-0913` → ff → main · force 0 · `.lock` 0 · 접촉 11 파일(+244/−19) · 배포 대기 `--all`**

# 보고 — T235 남은 무인증 POST 다섯 — 새 인증 0 · 부르는 쪽을 존으로 (2026-09-13)

한 줄: **다섯 문 안 문 ○ · 존 경유 본인만 ○ · 클라 diff 30줄(+30/−16 · 새 화면 0) · `test-guest-identity` 87/5 → 92/0 · ⑪ 초록.**
**새 인증 0 · 새 컬럼 0 · 새 규약 0** — ws 접속이 본인이라는 T225 §0-ⓑ 를 그대로 썼다(족보 179).
배포는 #27 에 얹는다(**`--all`** — 존·클라도 만졌다).

---

## §0-ⓐ 표 하나 — 다섯 문을 **남의 이름으로** 부르면 (고치기 전 · 전부 실측 · `200`)

| 문 | 클라 호출 자리 | 남의 이름으로 부르면 | 바뀌는 정본 |
|---|---|---|---|
| `/market/cancel` | `45-t-market.js:32` | **남의 주문이 물린다** `200 {"ok":true}` (남은 주문 1 → 0) | `orders` |
| `/market/order` | `45-t-market.js:48` | **남의 이름으로 주문이 선다** `200 order_id:2` | `orders` |
| `/war/declare` | `50-i-panel.js:592` | **남의 길드로 전쟁 선포** `200 war_id:1 tier:clean` | `wars` |
| `/war/end` | `50-i-panel.js:600` | **아무 전쟁이나 종료** `200 {"ok":true}` | `wars.ended_at` |
| `/tribe/leave` | `50-i-panel.js:609` | **남을 길드에서 빼낸다** `200` · `vic.tribe_id` **→ null** | `players.tribe_id` |

⚠`/market/cancel` 에는 `o.player_id !== playerId → 403` 이 **있었다**. 그런데 그 비교는 주문의 주인을
**부르는 쪽이 스스로 적어 넣은 이름**과 맞춘다 — 피해자 이름을 적으면 그냥 통과한다. **문지기가 신분증을
발급자에게 물어본 셈**이다. 겉보기에 검사가 있어서 여태 안 보였다.

## §0-ⓑ 표 둘 — 한 문법 (다섯이 같은 길로 간다)

| 층 | 무엇 | 자리 수 |
|---|---|---|
| 클라 | `fetch('/…')` → **`window.__centralCall(door, extra)`** 한 개 | 호출 5자리 + 도우미 1개(`30-n-net.js`) · **새 화면 0 · 새 표 0** |
| 존 | `msg.type === 'central_call'` 한 분기 — `door` 로 갈라 **`player.playerId`·`player.tribeId`** 를 붙여 안 문을 부른다 | `zone.js` 한 자리(24줄) |
| central-client | 있는 `_softDoor` 문법 그대로 다섯 줄 | 5줄 |
| central | `if (!isInternal(req)) return denyOutside(res);` | 문마다 **한 줄** |
| 답 | `{type:'central_result', door, ok, data}` **한 종류** | 클라 분기 1 |

⇒ **문마다 다른 길이 아니다**(T225 §1 이 경고한 그것). 새 문을 낼 사람은 `door` 하나만 늘리면 된다.
⚠**전쟁의 공격자 길드도 클라가 못 고른다** — 존이 `player.tribeId` 를 쓴다. 그게 §0-ⓐ ③ 의 구멍이었다.
⚠게스트 차단(`anon_` 거절)은 central 의 종전 규약 그대로다 — 여기서 다시 정하지 않았다.

## §0-ⓒ 표 셋 — 하네스

| | 베이스 | T235 |
|---|---|---|
| `test-guest-identity` | **87 / 5** | **92 / 0** |
| ⑦ 다섯 문 바깥 404 | ✗ (다섯 전부 200) | ○ |
| ⑦b 피해자가 길드에 그대로 | ✗ (`tribe_id` null 이 됐다) | ○ |
| ⑦b2 주문이 그대로 하나 | ✗ (물렸고 늘었다) | ○ |
| ⑦c·⑦c2 안 문에서는 산다 | ✗(앞의 공격이 이미 다 망가뜨려서) | ○ |

**그 밖**: `e2e-ui` **133/0**(거래소·길드 패널을 그리는 그 하네스) · `e2e-friends` **93/0** · `e2e-guild` **97/0** ·
`test-client-globals` **26/0** · `test-notice` **21/0** · `test-newcomers` **56/0** · `test-handoff-body` **21/0** ·
`test-harness-lint` **36/0** · `lab-modules --diff` 교집합 **0**.

**`e2e-onboarding` 호 28걸음 중 28 초록**(두 판 모두) — ⑪ 이 초록이 됐다.
⚠대본 절은 판마다 **다른 상시 흔들림**이 하나씩 났다: 1판 `납품이 화면 알림으로 돌아온다`(같은 로그 뒤쪽에
`"곳간에 쌓였다."` 와 `deliv:1` 이 **있다** — 읽는 시점이 이른 것이다) · 2판 `심각도 0.500 < 0.490`(T139 부터의 그것).
**둘 다 central 과 무관**하고 서로 다른 판에서 났다. 둘 다 적는다 — 한 판만 적으면 표가 거짓말이 된다.

## §1 ⑪ 기근 한 줄 (카드 ③)

T202 가 ⑩ 에서 배운 것을 ⑪ 에도 걸었다: **기근은 잠금이 아니다.**
⑪ 이 재는 것은 **소속**(`/곳간` 이 곳간 이름을 부르는가)이고, 밥이 나오느냐는 **마을의 형편**(T159 기근 문)이
따로 정한다. 갓 선 마을은 굶는 날이 있고, 그걸 소속의 실패로 세면 거짓 빨강이다(T225 §0-ⓒ 가 본 그 ●).

## §2 접점

`server/central.js`(다섯 문 `denyOutside` 한 줄씩) · `server/central-client.js`(다섯 door) ·
`server/zone.js`(`central_call` 한 분기) · `server/internal-door.js`(**무변** — 규칙은 이미 하나) ·
`public/client/30-n-net.js`(`__centralCall` + `central_result`) · `45-t-market.js` · `50-i-panel.js` ·
`scripts/test-guest-identity.js`(§⑦ 8건) · `scripts/e2e-onboarding.js`(⑪ 한 줄) ·
`scripts/test-client-globals.js`(기준선 13→14 · 그 표를 늘리는 것이 그 검사의 쓰임이다).
심볼: `/market/order` `/market/cancel` `/war/declare` `/war/end` `/tribe/leave` `denyOutside` `isInternal`
`internal-door` `central_call` `central_result` `__centralCall` `_softDoor` `x-zone-secret`.

## §3 회부

1. **배포**(#27 · 재민) — T217·T225 와 **한 번에**. `--all`(존·클라도 만졌다):
   `cd ~/Mini/durango-mini && bash scripts/redeploy-hanbando.sh --all`
2. #9 `?as=`(#27 로비 순서 ⓐ/ⓑ) · `CENTRAL_SECRET`(재민 · 양쪽 통) · 별명(#23) ·
   `nippon` 시딩(Z) · 유저 마을 첫 의뢰 정본(#18).
3. **대본 절의 상시 흔들림 둘**(§0-ⓒ ⚠) — 판마다 하나씩 나고 둘 다 읽는 시점/문턱 문제다.
   호 절과 달리 **아무도 귀속을 안 했다**. 러너가 같은 줄로 빨개지면 여기를 보라.

## §4 실기 (재민 · 배포 뒤)

* `<central>/market/order` 를 바깥에서 POST 하면 **404** 인가.
* 그리고 게임 안에서 **거래소 주문·취소·길드 탈퇴가 여전히 되는가**(존 경유로 바뀌었다).
