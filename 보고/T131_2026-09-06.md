# 보고 — T131: 클라 잔무 셋 — `hp_changed` 개명 · `data-action` · 낡은 주석 (2026-09-06)

> 한 줄: **게임 행동은 한 톨도 안 바뀐다.** 바뀐 것은 **이름 하나**(`player_damaged` → `hp_changed`)와
> **거짓이 된 문장들**이다. 그리고 지시서의 두 전제를 §0 이 고쳤다 — 접점은 27조각이 아니라 **셋**이고,
> `data-action` 은 **치울 것이 없었다**.

---

## 0. §0 실측 (셋) — 지시서 전제 둘을 고쳤다

### ⓐ 개명 접점 전수 — `grep -rl player_damaged` (지시서: "서버 5곳 + 클라 27조각")

`HEAD`(36a9cc6b) 기준 전수. **코드**는 실행되는 줄, **문구**는 하네스 판정문 문자열, **주석**은 설명.

| 파일 | 총 | 코드 | 문구 | 주석 |
|---|---:|---:|---:|---:|
| `server/zone.js` | 3 | **2** (`setHp` 의 broadcast · send) | 0 | 1 |
| `public/client/30-n-net.js` | 1 | **1** (핸들러 분기) | 0 | 0 |
| `scripts/e2e-hp.js` | 9 | 4 (소스 정규식 · 프레임 카운터 셋) | 2 | 3 |
| `scripts/e2e-thirst.js` | 1 | 0 | 0 | 1 |
| `scripts/e2e-cold.js` | 1 | 0 | 0 | 1 |
| **합** | **15** | **7** | **2** | **6** |

⇒ ★**"서버 5곳 + 클라 27조각"은 T109 이전의 그림이다.** T109 가 hp 를 쓰는 열 자리를
`setHp(p, v, why)` 한 문으로 모으면서 **송신 자리는 두 줄로 줄었고**, 클라는 처음부터
**핸들러 한 갈래**였다(T109 가 "클라 접점 0" 으로 통과한 그 사실). 실제 개명 접점은
**서버 2 · 클라 1 · 하네스 3파일**이다.

문서 쪽(코드 아님)은 따로 셌다 — `인계/N-네트워크.md` 3 · `인계/H-HUD신체.md` 1 ·
`인계/B-신체서버.md` 1 · `인계/회부.md` 3 · `회부/회부_극단HP_다음층.md` 1.
그중 **지금 규약을 말하는 것**(N 1-표5 셋 · H 상자 · B ⓑ)만 고쳤고, **`보고/` 넉 파일은 안 건드렸다** —
보고서는 그때의 기록이라 고치면 거짓이 된다.

★**옛 이름 폴백 0.** 두 이름을 동시에 보내면 그게 사본이고, 클라가 둘 다 읽으면 두 벌이 된다.
한 번에 갈아 끼웠다. `e2e-hp ⓪` 이 **옛 이름도 새 타입도 없음**을 소스로 못 박는다.

### ⓑ `data-action` — ★**치울 것이 없었다**

지시서 ②는 "죽은 버튼 · 떠도는 분기를 치운다" 였다. 실제로 재 보니:

| 잰 것 | 답 |
|---|---|
| `index.html` 의 `data-action` 버튼 태그 | **23개** (고유 **22종**) |
| `99-main.js` 의 `a === '…'` 분기 | **22개** (고유 22종) |
| 눌러도 아무 일 없는 버튼(버튼만 있고 분기 없음) | **0** |
| 아무도 안 부르는 분기(분기만 있고 버튼 없음) | **0** |
| 중복 버튼 | **1종** — `furnace_start` 둘, **`data-kind` 로 갈린다**(의도된 둘) |
| 버튼 줄의 이모지 | **0** (화면 규칙 B) |

⇒ **삭제할 코드가 0 이다.** 그래서 이 항목은 **지우는 대신 재서 못 박았다** —
`scripts/test-client-globals.js` 에 블록 ⑦ 을 새로 뒀다(20/0 → **26/0**).
다음에 버튼을 더하고 분기를 잊거나, 분기를 지우고 버튼을 남기면 **그때 빨개진다**.
"지금 깨끗하다"를 문장으로 적는 것과 **하네스로 붙잡아 두는 것**은 다른 일이다.

★자명 통과 금지(⑦f): 버튼 목록에서 하나를 빼면 "떠도는 분기 1" 이 잡히는 것을 같은 블록이 확인한다.

### ⓒ 낡은 주석 — 무엇이 거짓이었나

| 자리 | 종전 문장 | 사실 |
|---|---|---|
| `e2e-thirst.js:86` · `e2e-cold.js:421` | "서버는 `player_damaged` 로만 hp 를 보내고 자연 회복은 브로드캐스트가 없다 · `self.hp` 는 welcome 때 한 번뿐" | **T61 이 닫았다** — `gauges`(초당 1건 · self)가 `hp`·`maxHp` 를 나른다 |
| `인계/H-HUD신체.md:74` · `인계/B-신체서버.md:780` | 같은 문장(당시 §0 실측) | 같음 |

★**판정 코드는 한 줄도 안 건드렸다.** 두 하네스의 "연속 관측의 단조 감소" 판정은 **지금도 옳다** —
낡은 것은 **왜 그렇게 재는가의 이유**뿐이었다. 이유가 거짓이면 다음 사람이 **없는 결함을 좇는다**.

---

## 1. 한 일 — 셋

```
① 개명   server/zone.js            setHp 의 송신 2줄 → 'hp_changed'
         30-n-net.js               핸들러 분기 1줄 → 'hp_changed'
         e2e-hp.js                 소스 정규식 · 프레임 카운터 · 판정문 전수
         인계/N-네트워크.md 1-표5 · 인계/H-HUD신체.md · 인계/B-신체서버.md  ← 규약 문서
② 계측   scripts/test-client-globals.js  블록 ⑦ 신설 (버튼 ↔ 분기 맞물림 · 6건)
③ 정정   e2e-thirst.js · e2e-cold.js 주석 (판정 무접촉)
         인계/회부.md — 닫힌 항목에 ✅ 표기 일곱 자리
```

### ③ 의 ✅ 표기 — 어디에

닫힌 것은 셋인데 회부에는 **여러 번 실려 있었다**(카드마다 "그대로 유지" 로 이어 적힌 탓).
살아 있는 목록에서 전부 찾아 표기했다:

| 닫힌 항목 | 닫은 카드 | ✅ 를 붙인 자리 |
|---|---|---|
| HP 자연 회복이 화면에 안 실린다 | T61(실은) · T109 확인 | `0-터 5` **(신규)** · 1167 · 1276(둘은 T109 가 이미 함) |
| 지게 낙하 — 바닥템이 장비 인스턴스를 | T102 ① | `0-터` 꼬리 · `0-커 2` (둘 다 ~~취소선~~만 있고 ✅ 가 없었다) |
| 꺼진 청크 움집 문간 — DB 조회 | T121 | `0-바람 4` · `0-터 1` · `0-커 3` · **`0-처 5`(원천)** |

그리고 **T109 가 올린 회부 둘을 이 카드가 닫는다** — 하네스 주석 거짓(③) · 개명(①).

---

## 2. 게임 행동 변경 0 — 어떻게 아나

| 근거 | 값 |
|---|---|
| `e2e-thirst`·`e2e-cold` diff | **주석뿐** (`git diff` 에 실행되는 줄 0) |
| `server/zone.js` diff | 문자열 리터럴 두 개 + 주석. 조건·분기·수 **무변** |
| `30-n-net.js` diff | 분기 문자열 하나 + 주석 |
| econ 랩 적재 ∩ 내 diff | §5 |
| 손잡이·수·표 | **0건** 건드림 |

---

## 3. 검증 — 돌린 것 전부

### ⓐ 내 카드 · 접점(심볼) 하네스

| 하네스 | 결과 | 비고 |
|---|---|---|
| `scripts/e2e-hp.js` | ★**26 / 0** | 개명 접점 본체. ⓪ 이 "옛 이름도 새 타입도 없다"를 소스로 못 박는다 (리베이스 두 번 뒤 **재실행 26/0**) |
| `scripts/test-client-globals.js` | ★**26 / 0** | ⑦ 신설(20 → 26) |
| `scripts/test-harness-lint.js` | **19 / 0** | |
| `scripts/e2e-thirst.js` | 15 / 6 | ★**귀속 = 내 것 아님** — 순정 베이스도 **15 / 6 · 같은 여섯**(§3-ⓑ) |
| `scripts/e2e-cold.js` | 51 / 1 | ★**귀속 = 내 것 아님** — 순정 베이스는 **49 / 3**(§3-ⓑ) |
| `node --check` × 6 | 통과 | zone.js · 30-n-net.js · e2e-hp · e2e-thirst · e2e-cold · test-client-globals |

⚠**자기 신고 하나.** `e2e-hp` 첫 판이 **25/1** 이었다(`✗ [A] 존 입장`). 원인은 내 쪽이다 —
`lab-modules` 를 **같은 2코어에서 동시에** 돌리고 있었다(랩이 한 코어를 98% 물고 있었다).
랩을 멈추고 **단독 재실행 → 26/0**. 이 상자에서 두 개를 겹쳐 돌리면 안 된다는 걸
회부(0-터 7 ⓑ · 족보 111)가 이미 적어 뒀는데 내가 어겼다. 규약은 "그때 마침 급해서"로 지켜지지 않는다.

### ⓑ 귀속 — 순정 베이스 클론 대조

`e2e-thirst` 6건 빨강. 같은 커밋(36a9cc6b)의 **순정 클론**에서 같은 하네스를 돌렸다:

| | 우리 작업본 | 순정 베이스 |
|---|---|---|
| 결과 | **15 / 6** | **15 / 6** |
| 빨간 항목 | ① 상황(한여름 낮) · ③ ×3 · ③ 외삽 · ④ | **같은 여섯** |
| 뿌리 | `★① (상황) 한여름 낮이다` 실패 — 픽스처 시계가 안 앉아 추위 축이 변수로 남았다(우리 0.5634 "추움" · 베이스 0.8647 "혹한") | 같음 |

`e2e-cold` 도 같은 뿌리다 — 빨간 하나가 `★★① 한겨울 밤이 실제로 **혹한급**이다  0.5634`,
즉 **`e2e-thirst ①` 을 죽인 그 수(0.5634)** 다. 순정 베이스에서도 **같은 0.5634 로 빨갛고**,
베이스는 거기에 둘이 더 빨갛다(⑤ `shelter 1` · ⑦㉢ 옷 대조).

| | 우리 작업본 | 순정 베이스 |
|---|---|---|
| `e2e-cold` | **51 / 1** | **49 / 3** |
| 공통 빨강 | ① 0.5634 | ① **0.5634**(같은 수) |
| 베이스만 빨강 | — | ⑤ 야생 판별 · ⑦㉢ 가죽 vs 삼베 |

⇒ **이 카드 밖의 성질**이다(픽스처 시계 · 이 상자의 부하 특성). 내 diff 는 주석뿐이라 실행 경로에
닿지도 않고, 두 하네스에서 **작업본이 베이스보다 나쁘지 않다**(cold 는 오히려 둘 덜 빨갛다 — 간헐).
★그리고 이건 **회부 대상**이다 — §6.

### ⓒ 안 돌린 것과 이유

**통합 러너 전수는 안 돌렸다**(검증 규약 ② — PM 이 밤에 main 에서 한 번).
CI core 23 종은 이 상자에서 순차로 돌리면 브라우저 없이도 수십 분이고, 그중 이 카드가
**닿는 것은 `test-harness-lint` · `test-client-globals` 둘뿐**이다(나머지 21종은 econ·작물·아이콘 계열로
`player_damaged`/`data-action`/`hp` 심볼을 한 번도 안 문다 — `grep -l` 로 확인). 둘 다 돌렸고 초록이다.

### ⓓ 프로세스 위생

`kill <pid>` 만 썼다 — **`pkill -f` 0회**. 판 사이에 `/tmp/e2e-*.db*` 를 지웠고
`zone.js`·`central.js`·chromium 자식이 남지 않은 것을 `ps` 로 확인했다(마지막 판 뒤 0개).

---

## 4. 화면 규칙 B · 사본 금지

* 새 CSS 0 · 새 토큰 0 · 이모지 0(⑦e 가 버튼 줄을 지킨다).
* 사본 금지: 옛 이름 폴백을 **안** 뒀다. 메시지 타입은 여전히 **하나**다.
* 자명 통과 금지: ⑦f(버튼 하나 빼면 잡힌다) · `e2e-hp ⓪`(직접 대입 되살리면 잡힌다).

## 5. econ 랩 적재 추적 — 교집합 **∅**

`node scripts/lab-modules.js --diff` (LAB_DAYS=1 · 계측기는 저장소의 그것 하나 · 사본 0)

```
=== econ 랩이 무는 저장소 파일 — 17개 ===
  scripts/econ-lab-real.js · server/{calendar,chunk,crops,events,fertility,livelihood,
  specialty,sustain,terrain-gen,terrain,trade,village-layout,villages,winter,zone-config}.js
  sim/{economy-sim-v2,economy-sim}.js

=== 내 diff — 6개(.js) ===
   ·  public/client/30-n-net.js   ·  scripts/e2e-cold.js   ·  scripts/e2e-hp.js
   ·  scripts/e2e-thirst.js       ·  scripts/test-client-globals.js   ·  server/zone.js

=== 교집합 0개 ===  ∅
```

⇒ ★**3시드 800일 베이스 A/B 를 갈음한다**(검증 비용 규약 · `인계/공통.md` §2 ①).
`server/zone.js` 는 랩이 **한 번도 안 문다**(랩은 `sim/` 과 `server/villages.js` 계열로 돈다).

## 6. 회부

1. ★**[영역 B · 하네스] `e2e-thirst`·`e2e-cold` 의 픽스처 시계가 이 상자에서 안 앉는다.**
   둘 다 **첫 상황 검사**가 빨갛고 **같은 수 0.5634** 를 낸다 — thirst ①("한여름 낮") ·
   cold ①("혹한급"). thirst 는 뒤 다섯이 따라 죽어 15/6 이 된다. ★**순정 베이스도 동일**
   (thirst 15/6 같은 여섯 · cold 49/3). 판정이 아니라 **상황 설정**의 실패이고,
   부하에 따라 값이 달라진다(thirst 베이스 판은 0.8647). 그 영역 카드 몫.
2. **[영역 클라 · 낱말] `feed` 가 두 뜻이다.** 버튼 `data-action="feed"` 는 **짐승 길들이기**(`tryFeed`)고,
   우클릭 동사 `{name:'feed'}` 는 **쓰러진 사람 먹이기**다. 지금은 서로 안 만나지만 같은 이름이
   두 세계에 있는 것은 다음 사람을 속인다. 낱말 하나를 갈면 끝이다(행동 변경 0).
3. **[PM · CI] `test-client-globals` 가 이제 `index.html` 도 읽는다**(⑦). CI core 23 안에 이미 있으니
   추가 배선은 없지만, **HTML 을 고치는 카드가 이 하네스를 접점으로 세야 한다**는 점만 새롭다.

## 7. 푸시

```
브랜치   batch/client-tail-0906 → main (ff)
커밋     0df46e78cb91530a407baaa1ca086810a57403e8
푸시     9ac8b9a5..0df46e78   _t131r2 -> main   (fast-forward)
         batch/client-tail-0906 은 [new branch]
force    0
리베이스 **2회 · 충돌 0**
         ① 36a9cc6b → **31e2bd1b** (상류 7커밋: T132 · T134 · T136 · 배포 스크립트 고침)
         ② 31e2bd1b → **9ac8b9a5** (상류 2커밋: T128 길드 모집 + 그 보고)
         두 판 다 `server/zone.js` 를 함께 옮겼다 — 다만 내 절과 **5,000줄 이상 떨어진 자리**다
         (T136 은 :11109 부근 · T128 은 :62~4054 · 내 `setHp` 는 :9222).
재검     상류가 접점 파일을 옮겼으므로 **리베이스마다 다시 돌렸다**:
         ① 뒤 `e2e-hp` 26/0 · `test-client-globals` 26/0 · `test-harness-lint` 19/0
         ② 뒤 `e2e-hp` **26/0** · `test-client-globals` 26/0 · `test-harness-lint` 19/0
         개명 심볼 전수도 리베이스마다 다시 셌다 — 상류가 옛 이름을 되살린 자리 **0**.
.lock 전수  find .git -name "*.lock" → **8, 전부 `_stale_locks/` 격리분**(옛 판들 것).
            격리 밖 **0** · ref 자물쇠(HEAD · packed-refs · refs/heads/main) **0**
            — 이번 push 는 하나도 안 남겼다.
```

★**푸시가 열두 시간 늦었다 — 맥이 끊겨 있었다.** 세션은 02:30 에 코드 커밋을 끝내고
그 뒤 **열두 번** 브리지를 두드렸다(25~60분 간격). 10:00 에 한 번 살아나 리베이스 ①·재검증까지 했고,
다시 끊겼다가 **14:10 에 살아나 리베이스 ②·재검증·푸시를 끝냈다.** 작업물은 그동안 커밋된 채 대기했고
`force` 는 한 번도 안 썼다.

⚠**세션 위생 하나 더 배웠다.** 하네스를 `setsid nohup … & disown` 으로 떼어 놓아도, **그것을 띄운
Bash 호출이 2분 타임아웃으로 끊기면 그 판이 같이 죽는다**(두 판을 그렇게 잃었다 — 로그가
"central 기동"에서 멈춘 채 프로세스만 사라진다). ⇒ **띄우는 명령은 짧게 끝내고**(`sleep 15; echo started`)
폴링은 **별도 호출**로 한다. 다음 세션을 위해 적어 둔다.
