# 보고 — T234 T229 회부 둘 닫기 + 공통.md 머리 대조 (2026-09-13 · 세션8/ART)

**베이스 `b56ac82f`** · 고친 파일 **둘** — `README.md`(문장 이동만) · `.github/workflows/regress-unit.yml`(주석 한 줄)
코드 0 · 워크플로 **동작 0**(`on:`·`run:`·`jobs:` 무접촉) · `public`·`server`·`sim`·`scripts` diff 0 · `인계/` **읽기만**
· 굽기 0 · 배포 0 · 실기 0 · 러너 전수 0(`--list` 만) · 3시드 면제(교집합 ∅)

## ★ 답 한 줄

**yml 1 · README 상자 문장 5 이동 · 5/5 · 공통.md 대조 16줄 · 불일치 3(회부)**

세 불일치는 전부 **러너 종 수**다 — `110`(야간 절) · `120`(러너 절) · `16`(픽셀 절). 그중 `120` 은
**바로 다음 줄이 스스로 143 이라고 적는다**. 정본이 자기 안에서 갈린 자리이고, 고치는 것은 PM 몫이라 표만 남긴다.

---

## §0-ⓐ yml 대조 — 세 줄

`regress-unit.yml` 의 수 전부를 T229 방식(주장 → 저장소 출처 → 판정)으로 다시 걸었다.

| 줄 | 주장 | 출처 | 판정 |
|---|---|---|---|
| `:3` | 회귀 하네스 **120종** · 전수 **~3시간** | `bash scripts/run-regress.sh --list \| wc -l` → **143** · `보고/야간러너_2026-09-13.md:5~6`(3h43m 에 112종 · 31 미측정) | **✗ 수도 뜻도 — 고쳤다(§1)** |
| `:5` | 단위 계층 **약 3분** | GitHub Actions `regress-unit` main 성공 런 **12회** 중앙값 **2분 48초**(최소 2:20 · 최대 4:27) | ○ |
| `:42` | 단위 하네스 **23종** | 같은 스텝의 목록을 세면 **23**(아래 명령) | ○ |

`:5` 의 "약 3분" 은 저장소 안엔 출처가 없다 — **GitHub Actions 실행 시간**이라 API 로 쟀다.
`보고/T131:153`("CI core 23 종은 이 상자에서 … 수십 분")은 **이 컨테이너**의 시간이라 자가 아니다. 자를 바꿔야 맞다.

```
$ bash scripts/run-regress.sh --list | wc -l                     → 143   (e2e 48 · test- 95)
$ sed -n '/단위 하네스/,/^$/p' .github/workflows/regress-unit.yml \
    | grep -o 'test-[a-z0-9-]*\.js' | sort -u | wc -l            → 23
$ GET /repos/simjaemin1/Mini/actions/workflows/regress-unit.yml/runs?branch=main&status=success
    12회 · 267 256 140 158 145 154 168 187 185 168 156 167 초
    중앙값 168초 = 2분 48초
```

## §0-ⓑ README 상자 — 문장 다섯을 옮겼다

**더한 줄은 제목 하나**(`## 무엇이 어디서 도나`). **지운 문장 0.**
기계로 확인했다 — 비어 있지 않은 줄 **189 → 190**, 사라진 줄 목록에 남은 것은 목록 표식 `* ` 하나뿐이다:

```
사라진 줄: ['* 존은 자기 DB 만 쓴다. …']
더해진 줄: ['## 무엇이 어디서 도나', '존은 자기 DB 만 쓴다. …']      ← 같은 문장, 목록 밖이라 `* ` 만 떨어졌다
```

| 문장 | 전 | 후 | 옮긴 이유 |
|---|---|---|---|
| 존 서버는 3001~3030 … 라이브는 hanbando 하나(:3020)만 돈다 | L110(실행 방법) | **L183** | "어디서" 의 **프로세스·포트** 칸 |
| 존은 자기 DB 만 쓴다. 존 사이의 공유 상태는 central 을 거친다 | L188(데이터) | **L185** | "무엇이" 의 **상태·권한** 칸 |
| 플레이어의 몸은 `serializeBody` 하나로 … 같은 함수를 쓴다 | L188-189(데이터) | **L185-186** | 위와 **한 문단**이라 같이 옮겼다(쪼개면 문장이 바뀐다) |
| **왜 클라이언트 예측이 있는가** — 서버 tick 주기(30Hz) … | L236(주요 설계 선택) | **L188** | "클라는 무엇을 하나" 칸 |
| … 서버 권위 좌표와 어긋나면 위치+속도 한 쌍으로 보정한다 | L237 | **L189** | 위 문장의 뒷절 |

**옮기지 **않은** 둘 — 왜 안 옮겼나(카드가 든 네 자리 중 하나)**

| 자리 | 무엇 | 왜 두었나 |
|---|---|---|
| L157-160 구조 트리 주석 넷(`zone-config.js` 토폴로지 26개 · `zone.js` 권위 30Hz · `central.js` 신원·`/zones` · `dispatcher.js` 라우팅) | 같은 물음의 답 | **문장이 아니라 디렉터리 목록의 주석**이다. 떼면 트리가 주석을 잃는다(= 지운 문장). 대신 **상자를 트리 바로 아래**(구조 절 끝 · 데이터 절 앞)에 세워 트리와 한자리에서 읽히게 했다 |
| L106-108 코드 울타리(`:3000` dispatcher · `:3010` central) | 같은 물음의 답 | **실행 방법의 붙여넣는 블록**이다. 떼면 그 절이 못 돈다. 상자가 존 포트를 이어받아 셋이 갖춰진다 |

### 다섯 물음 재확인 — 여전히 **5/5**(새 줄 0)

| 물음 | 전(T229) | 후 | |
|---|---|---|---|
| 멀티인가 | L34 | L34 **무변** | ○ |
| 플레이어가 마을을 세울 수 있나 | L34-36·L39 | **무변** | ○ |
| 한반도가 전부인가 | L10-11 | **무변** | ○ |
| **무엇이 어디서 도나** | L106-110 · L157-160 · L188 · L236 **네 자리** | **L181-189 한 절**(+ 트리 L157-160 이 바로 위) | ○ **네 자리 → 한 절** |
| 검증은 무엇으로 하나 | L132-143·L145-148·L220-222 | **무변** | ○ |

### README 를 읽는 하네스 셋 — 3/3 초록

`grep -l 'README' scripts/test-*.js scripts/e2e-*.js` → 셋. 전부 단독으로 돌렸다.

| 하네스 | 결과 | README 를 어떻게 쓰나 |
|---|---|---|
| `test-weight` | **63/0** | `:208` 루트 `README.md` 를 읽어 `⑥ 조작 설명도 갱신됐다`(옛 `**T** — 거래` 가 없어야 한다). 조작 절 무접촉 |
| `test-redeploy` | **12/0** | `:80·:92` `README.md` 를 배포 스크립트의 **파일 이름 인자**로 쓴다(내용 안 읽음) |
| `test-charsheet` | **192/0** | `:462·:1225` 는 루트가 아니라 `assets-src/mocap/README.md` — 다른 파일. 안전 삼아 돌렸다 |

## §0-ⓒ `인계/공통.md` 머리 대조 — **읽기만 · 16줄 · 불일치 3**

★**고치지 않았다.** 공통.md 는 PM 문서이고 이 카드의 규약이 `인계/` 읽기만이다. 아래는 **회부**다.

### 러너 절 (§2 ⑥ · `:195~201`)

| 줄 | 주장 | 저장소·`보고/` | 판정 |
|---|---|---|---|
| `:196` | 단위 하네스 **23종**(약 3분) | yml `:42` + 목록 **23**(세었다) · CI 중앙값 **2분 48초** | ○ |
| `:197` | 전수(**120종**)는 여전히 야간 몫 | `--list` **143** — 그리고 **바로 다음 줄 `:198` 이 스스로 "전수는 143종"** 이라고 적는다 | **✗ 120 ↔ 143** (한 파일 안의 자기 모순) |
| `:198` | 전수 **143종** · **e2e 48종**만 3시간 24분 | `--list` 143 · `grep -c '^e2e-'` **48**(세었다) | ○ |
| `:198` | 야간 창 **3h39m** 은 **108종**까지(**35종** 미측정) | 09-12(T185) 값. 09-13 은 **3h43m · 112종 · 31 미측정**(`보고/야간러너_2026-09-13.md:5~6`) | △ **낡음**(틀린 게 아니라 T185 판 · 갱신거리) |
| `:198` | 상시 빨강 **셋**(`test-tick-slicer`·`e2e-rtt`·`test-body` 물때) | 09-13 야간: tick-slicer **빨강(목록 그대로)** · e2e-rtt **미측정** · `test-body` ★**306/0 초록**(`TIDE_FREEZE_MS` **없이**) | △ 셋 중 하나가 초록으로 바뀌었다 · 갱신거리 |

### 야간 절 (§2 ② · `:188~192`)

| 줄 | 주장 | 저장소·`보고/` | 판정 |
|---|---|---|---|
| `:190` | **전수(110종 · ~3h)** 는 PM 이 밤에 main 에서 한 번 | `--list` **143** · 09-13 야간 **3h43m 에 112종**(31 미측정) | **✗ 110 ↔ 143 · "~3h 면 전수" 는 뜻도 틀리다** |
| `:190` | 빨간 것은 아침에 그 카드 세션으로(`보고/야간러너_<날짜>.md`) | `보고/야간러너_2026-09-13.md` 가 그 꼴로 있다 | ○ |
| `:189` | 접점 하네스는 `grep -l "<파일>" scripts/test-*.js scripts/e2e-*.js` 로 **기계로** 뽑는다 | 이 카드가 그 명령으로 셋을 뽑아 돌렸다 | ○ 실행 확인 |

### 픽셀 절 (§2 ⑦ · `:203~206`) — 카드가 든 넷 밖이지만 같은 §2 블록이라 같이 걸었다

| 줄 | 주장 | 저장소 | 판정 |
|---|---|---|---|
| `:204` | `--list pixel` 전부(**16종**) | `bash scripts/run-regress.sh --list pixel \| wc -l` → **17**(세었다 · T205 에서도 17 이었다) | **✗ 16 ↔ 17** |

### 2판(푸시) 절 (`:20` · `:61~83`)

| 줄 | 주장 | 확인 | 판정 |
|---|---|---|---|
| `:20` | PAT 파일 **93바이트** | `wc -c < …/claude-pat.txt` → **93**(내용 무접촉) | ○ |
| `:64` | 통로 클론 팩 **09-07 현재 356개** | **못 잰다** — 같은 절이 그 클론 **무접촉**(`fetch 도 하지 마라`)을 명령한다. 09-07 시점 값이라 스스로 적어 뒀다 | △ 규약이 재는 것을 금한다(결함 아님) |
| `:70` | `--depth 200` | T229 푸시가 이 깊이로 통과(원격 main `f92f65bd` 에서 ff) | ○ 실행 확인 |
| `:74-75` | **`origin main` 을 번들보다 먼저** | T229: clone(= origin main) → 번들 fetch 순 · 통과 | ○ 실행 확인 |
| `:74` | `HEAD:refs/temp/<tag>` | T229 에서 `refs/heads/main:` 은 **실패**(`couldn't find remote ref`)했고 `HEAD:` 가 통과 — **정본이 옳았다** | ○ 실행 확인 |
| `:77` | `merge-base --is-ancestor` 로 ff 검사 · force 0 | T229 통과 · main 이 `f92f65bd` 를 조상으로 갖는 것 확인 | ○ 실행 확인 |
| `:83` | 통로 클론 `.lock*` 전수 검사는 안 한다 · 보고엔 "작업 클론 · force 0 · 원격 main 해시" | T229 보고가 그 셋을 적었다 | ○ |

### 기준선 표 (`:356~362`)

| 줄 | 주장 | 확인 | 판정 |
|---|---|---|---|
| `:358` | 베이스 **`2b9f724e`** | `git cat-file -t` → `commit` · `merge-base --is-ancestor … HEAD` → **조상** | ○ |
| `:359~362` | 여덟 수 × 3시드(인구·소멸·무기Q·확장셀·게시·도구Q·보존식·생곡) | T229 §0-ⓐ #51 에서 README 요약과 **8/8 자리까지 일치**. **저장소 쪽 재계측은 안 했다** — 재려면 3시드 800일(팔당 ≈45분)이고 이 카드가 면제된다 | △ **이 카드에서 안 쟀다**(면제 · 안 잰 것을 잰 척하지 않는다) |

**대조 16줄 · ○ 10 · ✗ 3 · △ 3.**

## §1 고침 — 둘

| # | 파일·줄 | 전 | 후 | 근거 |
|---|---|---|---|---|
| ① | `regress-unit.yml:3` | 회귀 하네스(**120종 · 전수 ~3시간**) | 회귀 하네스(**143종 · 야간 창 3h43m 에 112종까지**) | `--list` 143 · `보고/야간러너_2026-09-13.md:5~6` |
| ② | `README.md` | "무엇이 어디서 도나" 가 **네 자리**에 흩어짐 | `## 무엇이 어디서 도나` **한 절**(문장 5 이동 · 제목 1줄) | T229 §2 넷째 줄 회부 |

①은 T229 ④와 **같은 고침**이다 — 수(120→143)만이 아니라 뜻("~3시간이면 전수가 돈다")도 바꿨다.
워크플로 **동작은 0**이다: 고친 것은 `#` 주석 한 줄이고 `on:`·`jobs:`·`run:` 은 한 글자도 안 건드렸다.

## §2 검증

```
$ git diff --stat
 .github/workflows/regress-unit.yml |  2 +-
 README.md                          | 17 ++++++++++-------
```

* **접점 심볼**: `README.md` · `regress-unit.yml` · `run-regress.sh --list` · `인계/공통.md` · `test-weight` · `test-redeploy` · `test-charsheet`
* **하네스** 3/3 초록(63 · 12 · 192) · `--list` 만(전수 0) · 3시드 면제(diff ∩ 랩 적재 17파일 = ∅)
* **T225·T226·T227·T230·T232·T233 무접촉** — `git diff --stat` 이 두 파일뿐인 것이 그 증거다.

## §3 회부

1. **`인계/공통.md` 불일치 셋(PM)** — `:197` 120종(자기 다음 줄과 모순) · `:190` 110종·~3h · `:204` 픽셀 16종.
   갱신거리 둘도 같이: `:198` 야간 창 수(09-12 → 09-13) · `:198` 상시 빨강 셋 중 `test-body` 가 초록.
2. **`판정대기` #29(대표↔평균)** — 재민 몫, 그대로.
3. 상추/아욱 26px(재민 눈) · 먹선(T116 뒤) · 갯벌 채집물 · 크레딧 #14 · 오디 #15 — 그대로.
