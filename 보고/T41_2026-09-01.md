# 보고 — T41: 일틱 조각의 바닥 (`_lifeAddHouseSite` · 캐러밴 콜드 A* · `_oreMineDaily` 꼬리)

**[재민 확정 2026-09-01 · 웨이브 1]** · 커밋 `1fed0362` · 영역 V/Z
(잔무 커밋 `df528fc8` — 보고/T1 + 공통 족보 두 줄)

> **한 줄**: 지시서가 지목한 셋 중 **첫째만 고쳤고, 둘·셋은 원인 귀속까지 하고 손대지 않았다.**
> 집터 탐색은 **12일간 238번 훑고 성공이 0** 이었다 — 일틱의 56%가 헛수고였고, 그게 사라졌다.
> 목표(조각 최대 ≤32ms)에는 **못 닿았다.** `e2e-rtt` 의 `≤×2` 는 pass/fail 밖에 그대로 뒀다.

## `git diff --stat` (원문)

```
 scripts/test-site-memo.js                          | 124 +++++++++++++++++
 server/villages.js                                 |  97 ++++++++++++-
 인계/V-마을배치.md                                    |  30 ++++
 인계/Z-존서버.md                                     |  62 +++++++++
 인계/회부.md                                         |  16 +++
 회부_T41_조각바닥_다음층.md                             | 151 +++++++++++++++++++++
 6 files changed, 475 insertions(+), 5 deletions(-)
```
★코드 파일은 **`server/villages.js` 하나**다. `sim/` diff 0 — econ 무수정의 심판.

---

## 1. §0 실측 — 셋의 원인 귀속

`/perf` 에 계측기(`probe`)를 달아 마을 50곳 · econ day 515 세계를 10~12일 돌렸다.

### ⓐ `_lifeAddHouseSite` — 하루 20곳이 훑고 **성공 0**
```
  12일 누적  호출 238 · 성공 0 · 빈손 238 · 마을 20곳 · 합 20,902ms(마을 한 곳 최대 1,085ms)
  거부 사유 248,191건
    기존 집과 너무 가까움(<18)   228,972   92.3%   ← ★
    마을 영토 밖                  17,149    6.9%
    개간 농지 위                   1,795    0.7%
    물가 완충 침범(침수)             275    0.1%
  영토 확장  11일에 21회 · 391셀 — 하루 평균 **1.9곳만** 자란다(대부분은 target 도달)
```
**왜 매일 다시 훑나**: 못 찾았다는 사실이 **어디에도 안 남는다.** 다음 날 같은 조건으로 다시 판다.
**영토가 바뀌는 사건 전수**는 `인계/V-마을배치.md` `## T41.` 표에 있다(열 가지 상태 · 방향 · 영향).

### ⓑ 캐러밴 A* — **캐시는 이미 있었다**
지시서 ②의 앞 절반("마을 쌍 캐시 — lazy · 지형 변경에서 무효")은 이미 구현돼 있다:
`getRoute` → `state.routeCache`(무방향 쌍 키 · `null` 도 캐시) · `invalidateTradeDistances` 가 비운다.
출발·도착은 **마을 쌍으로 고정**이다(캐러밴 실체는 그 경로를 따라간다).
```
  10일 누적  콜드 51 · 적중 53 · 무효화 0 · 합 7,996ms · 한 번 최대 1,265~1,695ms · 평균 157ms
```

### ⓒ `_oreMineDaily` 꼬리 — **부팅 워밍업, 마을당 1회**
```
  콜드 스캔 50회 = 마을 수. day3 이후 **증가 0**(`vil._oreCells` 캐시가 이미 있다).
  합 4,316~4,545ms · 한 번 최대 755ms · 스캔 범위 반경 150셀 원판 ≈ 70,686칸
```
⇒ "최대 4,260ms" 는 반복 비용이 아니라 **부팅 후 첫 하루에 50곳이 한 번씩** 도는 합이다.

### ⓓ 결정론
셋 다 난수를 안 쓴다. 집터는 열 가지 상태의 함수, 경로는 격자 A*(`sim/path-core.js` 정본),
광맥 셀은 지형 술어의 함수 — **같은 입력이면 같은 답**이다. 캐시가 정확해야 하는 근거가 이것이다.

---

## 2. 작업 ① — "못 찾은 자리는 기억한다"

**논증**: `reject` 가 보는 상태 여덟 중 **여섯은 줄기만 한다**(= 더 거부된다).
후보를 늘릴 수 있는 사건은 넷뿐이고, 그것이 표지가 서는 자리다.

| 사건 | 부르는 것 | 캐시 |
|---|---|---|
| `_terrGrow` 가 셀을 더했을 때 | `lifeSiteDirty(vil)` | **유지**(새 셀만 더 본다) |
| `_psite` 소멸 | `lifeSiteReset(vil)` | 파기 |
| 지형 차단 완화(벽·다리) | `lifeSiteResetAll()` | 파기 |
| `_potSet` 에서 삭제만 하는 두 곳 | `lifeSiteReset(vil)` | 파기 |

* **거부 캐시** `_siteRejS`/`_siteRejL` — strict/loose 는 `farmAt` 이 달라 판정이 다르므로 나눴다.
* **안전망** `LIFE_SITE_RESCAN_DAYS`(7)마다 표지 무시 전수. **마을마다 다른 날**(`(day+dbId)%N`) —
  처음엔 전부 같은 날이라 7일마다 20곳이 한꺼번에 돌아 그날이 **7.8초**로 튀었다(실측).

### 전/후 (같은 DB 스냅샷 · 9일 · 마을 50곳)
| | 집터 호출 | 훑은 셀 | `life:site` 중앙 | `1마을:site` 중앙 |
|---|---|---|---|---|
| 종전(`LIFE_SITE_MEMO=0`) | 177 | 1,227,510 | **1,209ms** | 205ms |
| 채택 | **54** | **376,710** | **6ms** | **2ms** |

**같은 답인가** — `LIFE_SITE_AUDIT=1` 이 건너뛴 셀을 실제로 다시 판정: **35,819회 · 위반 0.**
(선택된 집터 로그 비교는 이 세계에서 성공이 0 이라 **자명 통과**다 — 그래서 감사가 본검사다.)

---

## 3. 작업 ②③ — 원인 귀속까지 하고 **손대지 않았다**

* **②** 재개 가능 A* 는 ⓐ `sim/path-core.js`(랩 `tradePath` 와 **같은 정본**) 를 상태기계로 고치고
  ⓑ `spawnCaravanBody` 가 경로를 동기로 못 받게 되어 **캐러밴 실체 생성이 지연된다**(행동 변경).
  둘 다 이 배치의 절대 규칙에 걸린다 ⇒ 회부. 더 싼 길 둘(경로 DB 영속 / 부팅 선계산)도 새 층이라 판단 대상.
* **③** 부팅 워밍업이고 마을당 1회다. 지시서 경고대로 **원인 서술 없이 상한을 박지 않았다**.

## 4. 작업 ④ — 목표 복귀 **못 함**

조각 최대는 이제 **캐러밴 콜드 A* 1,265~1,695ms** 다(복귀 조건은 ≤32ms).
`e2e-rtt` 의 `≤×2` 는 pass/fail 밖에 그대로 뒀다(T1 회부 B-1 의 조건 미충족).

```
  ①을 넣은 뒤 조각 최대 후보
    캐러밴 콜드 A*        1,265~1,695ms   ← ★새 바닥
    집터 첫날 캐시 빌드      ~1,050ms      부팅 1회(그 뒤 2ms)
    광맥 콜드 스캔             755ms       부팅 1회
    생활층 헤드리스 결산        226ms       ★**매일** — 다음 후보
    곳간 증설                  70ms/마을   ★매일
```

---

## 5. 하네스

* **`scripts/test-site-memo.js` 14/0** — 팔 넷(종전 · 채택 · 감사 · 표지 고장 픽스처).
  ★**비용은 감사 없는 팔로, 정확성은 감사 팔로** 잰다. 감사는 건너뛴 셀마다 `reject` 를 다시
  부르므로 그 팔로 비용을 재면 "1/5 로 못 줄었다"는 **가짜 실패**가 난다(첫 판에서 실제로 그랬다).
  ★`test-tick-slicer` 에 안 붙인 이유: 그 하네스는 이미 세 팔을 돌린다. 넷을 더하면 한 하네스가
  12분을 먹고 **두 주제**가 섞여, 실패해도 무엇이 깨졌는지 이름으로 못 안다.
* **`scripts/test-tick-slicer.js` 21/0** — 그대로 통과(조각내기 구조 무변).

## 6. 손잡이 (env · 기본값 = 채택값)

| 손잡이 | 기본 | 뜻 |
|---|---|---|
| `LIFE_SITE_MEMO` | **1** | `0` = 종전 동작(매일 전수) — **되돌리는 스위치이자 하네스 대조군** |
| `LIFE_SITE_RESCAN_DAYS` | **7** | 안전망 주기(마을마다 다른 날) |
| `LIFE_SITE_AUDIT` | 0 | `1` = 단조성 런타임 증명(**느리다** · 하네스 전용) |
| `LIFE_SITE_NODIRTY` | 0 | `1` = 표지를 일부러 안 세운다(안전망 픽스처 · 하네스 전용) |

## 6b. 기준선 · 러너

* **econ 무수정** — `sim/` diff **0**(지시서가 정한 심판) · 건드린 코드 파일은 `server/villages.js` 하나.
  v2 CLI 5시드 회귀 **2,616 · 무기Q 651 · 확장셀 13,473 · 소멸 0/30** — 기준선 그대로.
  실지도 3시드 800일은 `econ-lab-real` 이 `__labProbe`+`tickWorldV2` 만 부르고 **생활층을 안 탄다**
  (이 배치가 만진 층이 그 경로에 없다) — T1 에서 같은 `sim/` 으로 6,280/5,993/6,072 · 소멸 0/51 실측.
* **통합 러너 56개 — 통과 54 · 실패 2**, 둘 다 이 배치와 무관:
  * `e2e-mtfoot ⑤` — 0.34/0.45/72%. `회부_추위2_다음층.md` B-8 에 적힌 **그 값 그대로**(지형 문제).
  * `e2e-weight ④` — **부하 흔들림**. 같은 빌드를 세 번 돌려 `가벼움` 이 45/38/45px 로 갈렸다
    (`과적` 은 37 고정). 두 기능을 **모두 끈** 대조군도 45/32px 로 같은 폭을 보였다.
    ```
      HEAD          45 vs 37  PASS  ·  38 vs 37  FAIL  ·  45 vs 37  PASS
      기능 둘 다 OFF  45 vs 32  PASS
    ```
    ⇒ 40px 남짓한 변위 둘의 **비**를 고정 벽시계 창(2,200ms)에서 재는 구조라 2코어에서 흔들린다.
      하네스 소유 영역(무게)의 판단거리로 남긴다 — 이 배치가 고치지 않았다.

## 7. 회부

`회부_T41_조각바닥_다음층.md` — §C 캐러밴 A* · §D 광맥 · §E 남은 바닥 수치 · §F 판단거리 셋
(안전망 주기 7일 / 집터가 구조적으로 없는 마을 20곳 / 손잡이).
`인계/회부.md` `## T41.` 에 세 줄로 올렸다.

## 8. 실기 기록지 (재민 — 세 줄)

1. **HUD RTT 를 보며 게임일 경계를 세 번** 넘겨라. 마감 순간 RTT 가 얼마나 튀는지(T1 기준
   실기 환산 ×14 대) — 조각이 줄었으니 T1 때보다 나아야 한다.
2. **마감 순간 캐러밴이 멈칫하는지** 봐라. T1 이 마감 동안 실체 30Hz 를 멈췄다(2~3초).
   눈에 띄면 그 자체가 회부거리다(하루 24분 중 0.2% 라 안 보여야 정상).
3. **집이 계속 지어지는지** — 며칠 뒤 마을을 둘러봐라. 표지를 놓치면 집이 안 서는 조용한 결함이 되고,
   안전망(7일)이 그걸 잡게 돼 있다. 안 지어지는 마을이 있으면 그건 **자리가 원래 없는 것**일 수도
   있다(실측 20곳 · 회부 §F-2) — 그 판정을 같이 부탁한다.
