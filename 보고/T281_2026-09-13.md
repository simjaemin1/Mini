# 보고 — T281 인계 정정 셋 · `fromEcon` 벽 · 배포 뒤 절 (2026-09-13 · 문서만)

한 줄: **인계 정정 — 가드 없는 프로덕션 자리는 `events.js` **하나**(`villages.js:6322` 는 `__e2eForceShortage` 픽스처) ·
`fromEcon` 벽 **15종**(사다리 24 중) → 게시판 캐논을 켜면 "낼 수 없는 부족"이 **15 → 29종**(새로 드는 14 가 전부 식량) ·
그중 **다섯(`salmon`·`shrimp`·`crab`·`oyster`·`seaweed`)은 잡을 수는 있는데 낼 수가 없다**(표 다섯 줄이면 닫힌다) ·
배포 뒤 확인 절 한 쪽(바깥 404 목록 35 · 200 목록 · 실기 넷 · 파수꾼 재집계).**

---

## §0-ⓐ 인계 정정 — `인계/E-사건장부.md` 에 두 절

**14-흐름눈** — 장부가 무엇으로 부족을 아는가(`_consEMA` 여섯 자리 · 가드 비대칭 · 집계 · 좁은 팔이 가른 둘 · #33 셋 갈래)
**15-벽** — 낼 수 없는 의뢰는 의뢰가 아니라 벽이다(`fromEcon` 전수 · 세 집합 · 벽에 부딪히는 품목 수 · 얻을 수는 있나)

### 정정 셋 (T263·T270 이 적었던 것 → 오늘)

| # | 그때 | 지금 | 근거 |
|---|---|---|---|
| ⓐ | "`_consEMA` 가드 없는 자리 **셋**(게시판 **둘** + `ramie`)" | **가드 없는 프로덕션 자리는 `events.js` 하나**(+`ramie`) | `villages.js:6322` 는 `__e2eForceShortage` — `zone.js`가 `E2E_GIVE` 로 게이트하는 **E2E 픽스처**다 |
| ⓑ | "게시판이 과일 부족을 **못** 낸다"(유도) | **"거의 못" 낸다** — `fruit` 의뢰 **49 / 0 / 42건** | T274 집계(`onEvent`·`onRequest`). EMA>0 인 마을이 51곳 중 0~2곳이라 그만큼 난다 |
| ⓒ | `일/건 −43.9%` 와 `게시 +18.6%` 가 한 카드에 묶여 있었다 | **갈렸다** — `일/건` 은 게시판(좁은 팔 −43.2% 재현 ○) · `게시` 는 세계(좁은 팔 ±2% · ✗) | T274 좁은 팔 3시드 |

★그리고 **좁은 팔은 세계를 안 움직인다**(여덟 수가 base 와 완전히 같다 · 3시드) — *"관측은 개입이 아니다"*(장부 §제3규약)가
숫자로 확인된 자리다. ⚠단 계측 판은 `ENABLE_VILLAGES=0` 이라 **게시판→플레이어 되먹임이 없다** — 실서버는 다르다.

## §0-ⓑ `fromEcon` 벽 표 (코드 0)

정본은 `villages.playerVillageDepositMap()` **하나**(19쌍 → econ 재화 **16종**). 장부는 그 표를 **주입받는다**(사본 금지).

| 집합 | 수 |
|---|---|
| 낼 수 있다(`fromEcon`) | **16** — `food`·`fish`·`meat`·`fruit`·`cooked_food`·보존식 넷·`herb`·`hide`·`wood`·`stone`·`ore`·`iron`·`salt` |
| 식사 사다리(`consumeFood` 가 빼는 것) | **24** |
| 둘 다(게시될 수 있는 식량) | **9** |
| **사다리인데 못 낸다** | **15** — `wheat`·`rice`·`barley`·`vegetable`·`mushroom`·`chestnut`·`walnut`·`acorn`·`seaweed`·`grape`·`salmon`·`shrimp`·`crab`·`oyster`·`mulberry_fruit` |
| 낼 수 있는데 사다리 밖 | 7 — 먹는 것이 아니다(정상) |

### ★벽에 부딪히는 품목 — 캐논을 켜면 **15 → 29종**

"부족 에지는 서는데 의뢰가 0" 인 품목(3시드 800일 51마을 · T274 집계):

| 팔 | 품목 수 | 성격 |
|---|---|---|
| 지금(끔) | **13~15** | `hemp`·`fur`·`leather`·`clay`·`clothes`·`bone`·`ramie`·`tool`·`silver`·`copper`·`weapon`·`obsidian`·`tin`·`jade` — **전부 식량이 아니다** |
| 게시판 캐논 켬 | **28~30** | **새로 드는 14가 전부 식사 사다리** — `salmon` 810~915건 · `wheat` 941~991 · `rice` 882~945 · `barley` 696~747 · `crab` 507~569 · `shrimp` 440~516 · `seaweed` 414~470 · `oyster` 241~272 · `vegetable` 210~235 · `mushroom` 171~188 · `walnut`·`grape`·`chestnut`·`acorn`·`mulberry_fruit` |

⇒ 게시판은 그 품목을 **의뢰로 걸지 않는다**(코드가 `fromEcon` 으로 막는다 — 옳다).
그런데 **부족 "사건"은 난다** — 촌장이 *"미역이 없다"* 고 말하는데 **플레이어가 미역을 낼 길이 없다.**
**말은 늘고 할 일은 안 는다.** 캐논을 켜기로 하면 이 벽을 같이 정해야 한다.

### 얻을 수는 있나 — **둘로 갈린다**

| 품목 | 얻나 | 낼 수 있나 | 값 |
|---|---|---|---|
| `salmon`·`shrimp`·`crab`·`oyster`·`seaweed` | ✅ **낚시·갯벌**(`server/fishing.js` `SPECIES_BY_BIOME` · 품목 id 가 econ 재화 **그대로**) | ✗ | ★**잡을 수는 있는데 낼 수가 없다** — `playerVillageDepositMap` 에 **줄 다섯**이면 닫힌다(새 동사 0) |
| `mushroom`·`vegetable`·`wheat`·`rice`·`barley`·`chestnut`·`acorn`·`walnut`·`grape`·`mulberry_fruit` | ✗ (채집 정본 `forage.KO` 는 `twig`·`pebble`·`fiber`·`brine` 넷뿐) | ✗ | **새 동사**가 필요하다 — 이 영역이 아니다 |

★**둘은 값이 완전히 다르다.** 앞의 다섯은 표 다섯 줄, 뒤의 열은 게임 동사. 같은 "벽"으로 묶어 말하면 판정이 흐려진다.

## §0-ⓒ 배포 뒤 확인 절 — `인계/N-네트워크.md` **N-배포뒤**

`--all` 로 올린 **뒤 한 번** 돌리는 한 쪽:

- **① 바깥 404** — central 쓰기 18문 + 읽기 2문 + 존 관측창 10 + 존↔존 5 + `perf`·`metrics` = **35문**, `curl` 루프 넷.
- **② 200 이어야 하는 것** — `/zones`·`/health`·`/tribes`·`/wars/active`·존 `/health`,
  그리고 **투영이 실제로 걷어내는지**(`/tribes` 에 `leader_id`·`treasury_json` **0건** · `/player/<이름>` 에 `guest_token`·`password_hash`·`home_x` **0건**).
- **③ 게임 안 실기 넷** — 길드 만들기 · 가입 · 거래소 호가창/주문·취소 · 길드 패널 멤버·금고.
- **④ 파수꾼 재집계** — `node scripts/test-guest-identity.js | tail -3` →
  `라우트 62 · 공개 16 · 안문 40 · 본인 3 · 투영 3 · 남음 2` · `116 통과 / 0 실패`.
- **⑤ `CENTRAL_SECRET`** — 지금은 **안 넣어도 된다**(N-비밀 절을 가리키기만).

⚠절 머리에 **바깥에서 돌려라**를 박았다 — 서버 안에서 `localhost` 로 두드리면 사설 주소라 **안 문이 열린다**.
그건 다른 검사(N-비밀 ②)이고, 여기서 섞으면 "닫혔다"를 거짓으로 읽는다.

## §0-ⓓ 답 한 줄

> **인계 정정 — 가드 없는 프로덕션 자리 `events.js` 하나**(`villages.js:6322` 는 픽스처) · 게시판은 과일 부족을 **"거의"** 못 낸다 ·
> **`fromEcon` 벽 15종**, 캐논을 켜면 낼 수 없는 부족이 **15 → 29종**(새 14 가 전부 식량) ·
> 그중 **다섯은 표 다섯 줄이면 닫히고**(낚시로 잡는다) 열은 **새 동사**가 필요하다 ·
> **배포 뒤 절 한 쪽**(바깥 404 35문 · 200 목록 · 투영 확인 · 실기 넷 · 파수꾼 재집계).

## §1 회부

1. **#33(재민)** — 셋 갈래 표가 `인계/E-사건장부.md` **14-흐름눈** 끝에 있다(그대로 / 게시판만 / 여섯 같이).
   이 카드가 더한 것: **게시판만 켜면 말은 느는데 할 일은 안 는다**(벽 15→29종).
2. **`fromEcon` 품목 추가(재민 · 캐논)** — **다섯만 먼저**(`salmon`·`shrimp`·`crab`·`oyster`·`seaweed` ·
   낚시·갯벌이 이미 주는 품목 · 표 다섯 줄 · 새 동사 0)가 한 갈래다. 나머지 열은 별건(게임 동사).
3. **#27 배포(재민)** — VPS 에서 `bash /opt/Mini/scripts/redeploy-hanbando.sh --all`,
   그 뒤 **N-배포뒤** 한 쪽. T217+T225+T235+T245 넉 장이 한 번에 올라간다.
4. #21 ⓑ · #2 `/check_username` · #9 `?as=` · `nippon` 시딩(Z).

## §2 검증

- **코드 0** — 바뀐 것은 문서 셋뿐: `인계/E-사건장부.md`(절 둘 신설) · `인계/N-네트워크.md`(N-배포뒤) · 이 보고.
- `test-harness-lint` **42/0**(하네스 무접촉 · 참조 검사 통과).
- 3시드 면제(카드 규약 · 코드 0).
- 표의 수는 **전부 실측에서 옮겼다** — `fromEcon` 은 `playerVillageDepositMap()` 을 직접 불러 셌고(19쌍 · 16재화),
  사다리 24종은 정본 표 넷(`FORAGE_FOOD_FACTOR`·`RAW_GRAINS`·`PRESERVED_FOODS` + 넷)에서 모았으며,
  벽 품목 수는 T274 의 3시드 집계 JSON 에서 셌다. **손으로 적은 목록 0.**
- 접점 심볼: `playerVillageDepositMap` · `fromEcon` · `__e2eForceShortage` · `events.js:450` · `events.js:595` ·
  `_consEMA` · `N-네트워크` · `파수꾼`.
