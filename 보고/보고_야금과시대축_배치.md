# 배치 보고 — 야금 사슬 완결 · 부존 규명 · 계측 위생 (2026-08-02)

> 재민 지시: "[작업 배치]를 위에서부터 순서대로 전부 진행해라. 질문하지 말고 네가 판단해서
> 진행하되, [회부만 할 것] 항목은 절대 구현하지 말고 문서만 써라. 정확성이 속도보다 우선."
> 커밋: `3985e4e` → `a14bc72` → `187671c` → `5074eeb` → (이 보고). 전부 푸시 완료.

---

## 0. 한 줄 요약

배치 20항 중 **18항 완료 · 2항 기각/무효 판정**. 그 과정에서 **버그 3건**을 잡았는데
그중 하나는 **직전 커밋의 기능이 통째로 죽어 있던 것**이다. 남은 것은 전부 재민의 설계 판단이다.

---

## 1. ★가장 중요한 것 — 노(爐)를 한 번도 지을 수 없었다

`1744e0e` 에서 만든 노 건설은 **전 경로가 죽어 있었다.**

```js
// 옛 코드
const cl = _furnaceClaimOf(player, atX, atY);         // 클릭 지점을 덮는 사유지 '한 장'
…
if (!(px >= cl.x && px < cl.x + cl.w && …))            // 그 한 장 안에 2×2 가 들어가나?
```

사유지 한 장은 `w = h = BUILDING_SIZE` = **정확히 1셀(32px)** 이다(`tryClaim:4111`. 마을 영토도
1셀짜리를 원형으로 깔아 만든다). **1셀 사각에 2×2 는 영원히 안 들어간다.**

⇒ `_claimFootprint` 로 **셀마다** 권한을 판정한다(네 칸 전부). 소유 판정도 여기서 확정 —
개인/임시 사유지가 한 칸이라도 걸치면 **개인 노**(타인 관여 불가), 전부 길드 땅일 때만 길드 노.

**교훈**: 기존 서버 하네스(`test-psite-server.js`)는 **소스 텍스트**를 읽어 계약이 실재하는지 본다.
그 방식은 "규약이 이식 중에 빠지는 것"은 잡지만 **좌표 기하**는 못 잡는다 — 코드를 읽으면
멀쩡해 보인다. `scripts/test-furnace.js`(실서버를 임시 DB·임시 포트로 띄우고 진짜 함수를
왕복시키는 E2E, **56 통과 / 0 실패**)가 이걸 처음 잡았다.

---

## 2. A. 노·철 콘텐츠 (6/6 완료)

| # | 항목 | 결과 |
|---|---|---|
| A1 | 노 E2E 하네스 | `test-furnace.js` 56/0. 착공→시공→완공→조업 왕복 · 권한 거부 3종 · **거부 시 상태 무변화**(외상 없음) · 시대 전환 수율 |
| A2 | 노 스프라이트 | 3D 6종(`furn_s1~s3`·`furnace`·`kiln_s1`·`charcoal_kiln`). 움집과 **같은 앵커 계약**, `test-building-anchor` 12건 PASS |
| A3 | 숯가마 | 노와 같은 2×2 사유지 계약, 2단계. 통나무 3 → **숯 4**(제작창 노천 탄화 3→2) |
| A4 | 운철 | 지표 청크 자원(결정론 해시 0.006/청크 → **대륙 42개** 실측). 제련 불필요 |
| A5 | 노 티어 | `FURNACE_KINDS` kind 파라미터화. 게이트는 `era.hasTech` **하나뿐** |
| A6 | 다광종 선광 E2E | 실서버 몬테카를로 27만 타 — **납 84.8~86.8% / 은 13.2~15.2%**(표기 85/15) |

**A2 육안 3패스 교정** — 렌더는 한 번에 안 나온다:
① 발광 9.0 은 Standard 뷰에서 하얗게 타 불이 아니라 **백색 원반**이 됐다(2.6 으로)
② 풀무가 노 그림자에 묻혀 베이지 얼룩이 됐다(키우고 앞 모서리로)
③ UV 구를 얹은 봉토 둔덕은 아랫반구가 그대로 보여 **'공'**이 됐다 — 렌더엔 지면 가림막이 없다.
   반구 메시(`dome()`)를 새로 만들었다.

**A4 등급 원칙** — 운철 등급을 손으로 안 적었다. 합금 모델의 **니켈 프리미엄**(Fe93Ni7 ÷ 순철
= 1.064)을 철 앵커 0.85 에 곱해 **0.904**. 결과:
`철 0.85 < 운철 0.904 < 흑요석 0.95 < 청동 1.0` — "초기 철기가 청동을 못 이겼다"가 수치로 선다.

**A6 실측** — 은 단독 광맥 폐지 뒤 "고대 은의 출처는 방연석"이 실제로 성립함을 처음 확인:
**은 290덩이 산출** · 표기 밖 광종 누설 0 · 선광 질량 보존 오차 <0.01%.

> ★단위 함정: 광맥 `center`·`radius` 와 terrain 의 ore 함수는 **픽셀**이다. 첫 시도에 셀과 뒤섞어
> "광맥 안"이라 믿은 자리가 실은 32배 밖이었고 장부에 맥석만 980kg 쌓였다.
> **단위가 틀리면 하네스는 조용히 통과하는 게 아니라 엉뚱한 걸 잰다.**

---

## 3. B. 경제 — 규명 4건 + 채택 1건

### ★B10 채택 — 땅맞춤 초기 부존 (소멸 −55% · 인구 +46%)

소멸의 사인은 "도구 아사"였다. 그런데 초기 재고를 전 마을 **일괄**로 주는 건 그 땅을 못 본 것이다.
⇒ **못 주는 만큼만 얹는다**(기준 돌 1.0·나무 1.0. 좋은 땅은 한 톨도 더 안 받는다).

| | 인구 | 소멸 | 무기 | 갑옷 |
|---|---|---|---|---|
| 기준 | 1,118 | 6.0/19 | 786 | 50 |
| **채택** | **1,630(+46%)** | **2.7/19(−55%)** | 1,119 | 84 |

전 시드 개선(9→3 · 4→2 · 5→3). v2 CLI 5시드 회귀: 인구 2,078→2,001(5시드 sd 안) · 갑옷 183→198 ·
**소멸 0/30 유지**. `LANDFIT=0` 으로 채택 전 동작을 **비트 단위 재현**(2,078·183·0/30 정확히 일치).

> ★회부 문서는 "완화일 뿐 — 도구는 또 닳는다"고 봤고 나도 그렇게 봤다. **틀렸다.**
> 소멸은 정상상태 문제가 아니라 **경로 의존** 문제다 — 초기 도구가 버티는 동안 인구가 임계를
> 넘으면 석공을 감당할 수 있게 되고 교역 상대로도 값이 생겨 돌이 들어온다. 한 번 넘기면
> 안 돌아간다. **그래서 개입 시점이 t=0 이어야 한다**(앞서 기각된 "기근 마을 반출 금지"는
> 이미 늦은 시점의 개입이라 시드 분산에 묻혔던 것이다).

### B7 — 주조 2/19 는 사슬이 아니라 **지도**다

구리 부존 마을 수 = 주조 마을 수. **전환율 100%, 전 시드.** 구리 나는 곳은 빠짐없이 주조하고
안 나는 곳은 한 곳도 안 한다. 그리고 후보엔 5곳이 있는데 **부유 시딩이 3곳을 떨어뜨린다** —
세계 최고 구리 자리 **광산6**(지수 1.017 = 임업2 의 4.5배)이 비옥 0.31 로 식량 하한에 걸려 탈락.
광산촌은 본래 척박한 데 선다(코드도 안다: "광산 부얼타운 = 식량 전량 수입").

부트스트랩이 **닫힌 고리**임도 실측: 구리 30 을 쥐여 주면 **19/19 전부가 800일 내내 주조**한다.
막는 건 능력이 아니라 `target(copper) = flowT` — 써 본 적 없으면 수요 0 → 가격 바닥 → 영원히 안 온다.
⚠단 그 세계는 무기 786→581 · 갑옷 50→23 으로 **줄어든다**("청동 = 소수 엘리트 표지"와 충돌).
**세계관 선택이라 회부.**

### B8 — 원석 적체. **제련 캡은 원인이 아니다**

`SMELT_CAP` 0.05 → 0.10 A/B: **결과가 비트 단위로 동일**(원석 4,672 · 대장 75 · 인구 4,889).
캡은 한 번도 물린 적이 없다 — 한계가치 게이트가 먼저 0을 낸다. ⇒ 캡 조정 **기각**.

갈래 셋: (가) 주석 채굴의 부산물 더미[정상 — 광부는 원석이 아니라 **주석** 때문에 뽑힌다]
(나) ★**수입 원석은 영원히 못 녹인다** — econ 의 `ore` 는 조성 없는 스칼라고 조성은 '땅'에만 있다.
추천 처방은 **원석 교역 금지**(고증: 광석은 무겁고 싸서 멀리 안 갔다 — 오간 건 잉곳이다). 상태 증가 0.
(다) 유산 더미[무해].

### B9 — 끊김②(석공→대장장이 순서) **기각**

생존 31곳 중 **10곳이 석공·대장장이를 동시에** 굴린다(대장장이만 있는 곳 0). 석공만 있는 20곳 중
원석·광종 보유 6곳도 전부 (나)나 "원석 0" 탓이고 **순서 때문에 막힌 곳은 0곳**. 항목 종료.

### B11 — 배합↔교역 통합: **이월 권고**

지금은 금속 교역 자체가 없다(최근 400건 중 구리 0~1건). 통합해도 **잴 현상이 없어** A/B 가
시드 분산에 묻힌다. 후보 3안과 실측 근거를 `설계_배합과교역_한단위_통합.md` 에 남겼다.

---

## 4. C. 시대·야생

### C12 `scripts/era-rehearsal.js` — "열까?" 할 때 바로 돌리는 리허설

① 경로 3종(setEra / WORLD_ERA / SCHEDULE) 능력 집합 일치 — SCHEDULE 은 **파일을 안 고치고**
메모리에서만 한 줄 얹었다 원복(리허설이 세계를 안 바꾼다)
② 실서버 부팅 스모크 — 시대만 바꿔 임시 DB·임시 포트로 띄운다
③ `--lab` 실지도 800일 플립 전/후 자동 비교 + **재민 기준 자동 판정**
   ("언제 여는지에 따라 흥망성쇠가 갈리면 안 돼" → 인구 ±25% · 소멸 +1.0곳 이내)

**★실측: 모든 지표 0.0% 변화**(인구 1,603 → 1,603 · 소멸 2.5 → 2.5 · 주조 2.0 → 2.0).
배선 고장이 아니다 — **NPC 가 닿는 주요 광맥에 철이 0**이다(철 광종 마을 0/19. 1744e0e 설계
그대로: 철은 플레이어 전용 자잘 광맥 184개에만 있다). 시대가 바꾸는 건 **플레이어 축뿐**이다:
괴련로 해금 · 도가니로 수율 3.4% → 67.8% · 말 길들이기.
⇒ **지금 시대를 열어도 세계가 안 무너진다.** 재민이 걱정한 위험은 현재 0이다.
   반대로 "NPC 철기 산업"을 원하면 시대만으로는 안 되고 **지도를 바꿔야** 한다(회부 대상).

### C13 말 사역 조사 — 구현 없음, 보고서만

- 한반도 biome = `forest`, 말 spawn_biome = `['plains']` → **말이 0마리다**
- `era.canTame` 는 정의만 있고 **호출부 0곳** — `breeding:true` 13종은 시대와 무관하게 길들여진다
- 길들인 뒤 할 수 있는 게 **없다**(탈것·운반·견인 전부 부재) · 먹이 `oats` 는 플레이어 획득 경로 없음
- ⇒ 게이트 연결은 지금 해도 되는 위생 · 사역 소비처는 **짐말**(`CARRY_MAX_KG`) 추천

### C14 철검 위세축 — 설계안(회부)

연철 0.75 < 청동 1.09 는 **고증이 맞다**(초기 철기는 성능으로 청동을 못 이겼다). 수치를 손보는 건
틀렸고, 값은 **위세축**(세계 재고 기반 희소 프리미엄)으로 표현하는 게 맞다.
⚠단 "안 쓰는 철에 프리미엄"은 2026-08-01 에 걷어낸 유령 수요가 이름만 바꿔 돌아오는 것이다 —
**소비 흐름 게이트와 세트**여야 한다.

---

## 5. D. 계측·문서 위생

### D15 `scripts/test-assay.js` — 감정 몬테카를로

표≡실측 · 비대칭(FN ≫ FP) · ★**FP 누설 0 을 통계로 확인**(8개 낱말 |z| < 1.1) · 문구 단조성 ·
혼동 행렬(바보의 금).

> ★**하네스가 두 번 틀렸고 두 번 다 없는 결함을 보고했다.** 기록해 둔다:
> (1) "FP 문구에 진짜 광종명이 한 번이라도 나오면 누설" → **1,569건 오보**. lvl6 문구는
>     "A 아니면 B"라 **추측의 혼동 상대**까지 말하고, 그게 우연히 진짜와 같을 수 있다.
> (2) 그걸 고치며 광종 이름끼리 견줬더니 은(銀)만 z = 29.9 로 또 오보 — '은'은 한 글자라
>     조사('돌은')에 걸려 기저가 40%다. **같은 낱말끼리** 비교해야 한다.
> 결론: 하네스를 믿기 전에 **기준을 먼저 검증**해야 한다.

★덤으로 실제 결함 하나: 한글 조사 — **"금로 보인다" · "구리이다"**. 종성 판정을 넣어
금→**금으로** · 구리→**구리로** · 구리→**구리다**.

### D16 `lab-wiring-check` 확장 — 이번에 실제로 겪은 함정을 항목으로

- 시딩 인자 `pickSeedVillages(hard, **ta**)` 누락 검사(빠지면 조용히 옛 시딩으로 돌아간다)
- 번들 **내용물**: era 축(`npcKnows`·`smeltYield`) 포함 · **process shim** 존재 · 손잡이 실림
- **[A2 신설] 엔진 A/B 손잡이 기본값 = 채택값** — 기본을 바꿔 커밋하면 회귀는 통과하는데
  라이브만 딴 세계가 된다(가장 조용한 사고 경로)

### D17·D18 문서

`CHECKLIST.md` 캐논 1항 추가 · `DESIGN_DOC.md` **§10 시대(era)** 절 신설 ·
`다음세션_인계.md` 교체(옛 파일 삭제).

---

## 6. E. 본 게임 접점

### E19 실서버 부팅 스모크 — **통과**

새 DB 전체 부팅: 후보 51 → 선별 19(**랩과 정확히 같은 선별**) · 18곳 시딩 · 서버 기동 ·
**예외 0**(`TypeError`/`ReferenceError` 없음). 노·숯가마 핸들러, 다광종 광맥, 운철 청크 스폰이
부팅 경로에서 예외를 안 낸다. 부팅에 약 4분 30초(마을당 5~19초 — 무거운 건 생활층 실체화).

★**같은 DB 로 다시 띄우면 시딩이 0줄이다** — 시딩은 DB 가 빌 때 딱 한 번 돌고 그 뒤는 전부
복원 경로다. 그러므로 부유 시딩 + 땅맞춤(합쳐서 **소멸 11.7 → 2.7(−77%) · 인구 696 → 1,630(+134%)**)이
**라이브에는 하나도 반영되지 않는다.** → `회부_시딩변경과_DB리셋.md`(리셋은 금지 규칙이라 안 했다).

### E20 플레이어 아이템 정합 — 결함 3건 수정

1. ★**`iron_ore`·`meteoric_iron` 에 무게가 없었다** → 미등록 기본 0.5kg 로 떨어져 정광을
   지게에 **56덩이**(28÷0.5) 지고 다닐 수 있었다. 원석 8덩이 상한과 정면 충돌. → 3.5kg(=`CHUNK_KG`)
2. `charcoal` 무게가 두 표에서 달랐다(RESOURCES 1.0 vs EXTRA_WEIGHT 0.6 — 후자는 **죽은 값**). 정리
3. 클라 라벨·아이콘·분류 누락 — 인벤 창에 **영문 키가 그대로** 뜨던 야금 재화 전부 채움

---

## 7. 재민 결정 대기 (요약)

| # | 문서 | 결정할 것 |
|---|---|---|
| 1 | `회부_구리부존과_원석적체` ① | 광산촌 특화 슬롯 / 시딩 점수 변경 / 그대로 |
| 2 | 〃 ② | 구리 파생 투입수요 — **채택하면 "청동 = 엘리트 표지" 설계가 바뀐다** |
| 3 | 〃 ③ | 원석 교역 금지(추천) / 조성 장부 / 그대로 |
| 4 | `회부_시딩변경과_DB리셋` | DB 리셋 / 마을만 재시딩(새 코드) / 새 존 / 그대로 |
| 5 | `설계_배합과교역_한단위_통합` | 이월 권고 |
| 6 | `설계_철검_위세축` | 희소 프리미엄(추천) / 세계 최초 낙인 / 그대로 |
| 7 | `조사_말_사역_배선` | 사역 소비처 — 짐말 추천 |
| 8 | `회부_플레이어_제련_노모델`(기존) | 남은 UX 질문(실패 연출 등) |

## 8. 검증 상태

`test-furnace` 56/0 · `test-mining`(⑨ 다광종 포함) PASS · `test-assay` PASS · `test-alloy` ·
`test-valuechain` · `test-armor-cap` · `test-picker-tables` · `test-building-anchor` 전부 PASS ·
번들 `--check` **8/8** · 배선 검사 **실패 0 · 경고 0**(랩 인라인 포함) ·
랩 2종 재인라인 + 헤드리스 스모크(**콘솔 에러 0**) + 디바이스 동기 완료.
