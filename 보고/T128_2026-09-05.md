# 보고 — T128 길드 모집: 초대 · 승인제 · 마을 소개문 (2026-09-05 · 야간)

베이스 **`31e2bd1b`**(T136 착지본) · 브랜치 `batch/guild-invite-0905` → ff → `main` · 커밋 `[재민 확정 2026-09-05 · T128]`
**해시 `eb5b6fd6`** · **force push 0** · 리베이스 **1회**(`cd722eaf`→`31e2bd1b` · 겹치는 파일 0 · 충돌 0)
**`31e2bd1b..eb5b6fd6` fast-forward 착지**(§8).

한 줄: T62 다음 층. **T115(친구)와 같은 문법**으로 지었다 — 표 하나 · 채팅 통로 · 새 패널 0 · 이모지 0.
둘이 다르면 그게 사본이라서다.

---

## §0-ⓐ 길드 가입 현행 (실측)

| | 실측 |
|---|---|
| 만들기 | `POST /tribe/create {player_id, name}` — 만든 사람이 `tribes.leader_id` |
| **가입** | `POST /tribe/join {player_id, tribe_id}` — ⚠**문이 없다.** 소속만 없으면 **아무나 아무 길드에** 든다 |
| 나가기 | `POST /tribe/leave` — 길드장이 나가고 **남은 멤버가 0 이면 길드가 해체된다**. 남아 있으면 가장 일찍 가입한 사람에게 이양 |
| 길드장 판별 | `tribes.leader_id === playerId` **하나** |
| 벌점(T19) | `tribes.vp` — `newcomers.js` 가 '청정' 경계(30)로 읽는다. 이 카드는 **안 건드린다** |
| NPC 길드 | `is_npc` 이고 `leader_id='npc_leader'` 면 첫 인간 가입자가 자동 승격(Phase 14.11) |

⇒ 승인제는 **`join` 에 문 하나**를 다는 일이다. 새 가입 경로를 만들지 않았다.

## §0-ⓑ 알림 kind 표 (실측)

`server/notice.js` 가 경계다: 접두 이모지 → `kind`, **호출부가 이미 `kind` 를 실었으면 그걸 존중한다**(:88).
종전 열 종: `village gather fishing craft board rescue downed combat dev info`.
클라 표는 `50-i-panel.js NOTICE_ICO` **하나**(T126 이 만진 그 표 — 여기에 **줄 하나만** 얹었다).

⇒ `guild` 하나를 더했다(열한 종). **`village` 에서 갈라낸 이유**는 T110 이 `downed` 를 `rescue` 에서
갈라낸 이유와 같다: 받는 쪽이 **그 한 종류만 보고** 다음 동작(`/수락`)을 한다.

⚠**그림은 `people` 이다.** `guild` 그림을 쓰려다 물렸다 — **`combat` 이 이미 그걸 쓰고 있고**
`test-itemlabel ⑮` 가 "종류마다 다른 그림"을 못 박는다. 싸움 쪽 그림을 새로 정하는 건 이 카드가 아니다(회부).
부름은 사람 사이의 일이니 `people` 이 뜻으로도 맞다.

## §0-ⓒ `startInfo` 줄 + 소개문 자리

줄의 필드: `vid·name·cx·cy·pop·popBand/Ko·ch/chKo/chEmo·news·board·busy/busyKo·arrive·welcome`
+ T19 `player`·`founderName` + T115 `friendsHere`. ⇒ 소개문은 **그 옆 칸**(`intro`)이다.
마을과 길드를 잇는 것은 **`_tribeId` 하나**(`villages.js:1316` — `newcomers.js:151` 이 이미 쓰는 그 값).

---

## §1 만든 것

```
  central.js        tribe_invites 표 하나 · tribes.join_mode·intro 컬럼 둘
                    라우트 여섯(/tribe/invite · /tribe/invites · /tribe/invite_accept ·
                                /tribe/mode · /tribe/intro · /tribe_intros)
                    그리고 **기존 /tribe/join 에 문 하나**
  central-client.js 문 여섯 — 실패는 예외가 아니라 빈 답(T115 규약)
  server/guild.js   (신설) 채팅 넷 + 소개문 캐시. 판정은 안 한다 — central 이 한다
  server/zone.js    채팅 분기 한 줄 · Guild.init(tellPlayer·refreshTribe) · 온보딩 훅 · /guilddbg
  server/notice.js  KINDS 에 `guild` 하나
  50-i-panel.js     NOTICE_ICO 에 줄 하나(`guild: 'people'`)
  server/onboarding.js  줄에 `intro` 한 칸
  70-lobby.js       소개문 두 자리 + **`onbEsc` 이스케이프 한 자리**
  e2e-guild.js      (신설) 이 카드 하네스 35건
  e2e-verbs.js      ⑫ 의 전제 한 자리 — 제가 심은 문구를 걷어내고 잰다(§3-4)
```

**표 하나에 부름이 산다** — 행이 있으면 부름이고, 지우는 것이 수락이자 거절이다. 상태 컬럼을 만들면
"부름인데 수락됨" 같은 어긋난 값이 존재할 수 있게 된다.

**`join_mode` 기본이 `open`** 이라 이미 있는 길드의 행동이 **한 줄도 안 바뀐다**(하네스 ②b 가 대조로 못 박는다).

## §2 사람이 쓴 문장이 처음으로 화면에 온다

로비는 `innerHTML` 로 줄을 짓는다. 서버는 **길이만** 자르고(60자 · 자르는 자리 하나), 꺾쇠는 **그리는 쪽**이 막는다
(`onbEsc`). 서버가 꺾쇠까지 자르면 그건 길이 제한이 아니라 **문장을 바꾸는 것**이다.
⇒ 자르기와 막기를 **다른 층**에 둔 이유가 그것이다. 하네스 ⑤c·⑤d 가 둘 다 못 박는다(로비가 다시 자르지 않는다).

## §3 하네스가 잡은 것 넷 (전부 실측)

1. **대조용 열린 길드가 사라졌다** — 남이 세우고 바로 나갔더니 `{"error":"길드 없음"}`.
   길드장이 나가고 아무도 안 남으면 **해체된다**(종전 규약 · §0-ⓐ). ⇒ 네 번째 사람이 세워서 지킨다.
2. **`GET /tribe/intros` 가 404 였다** — 옛 `GET /tribe/<id>`(central:988)가 먼저 걸린다.
   ⇒ `/tribe_intros`. **새 라우트를 옛 접두 아래로 밀어 넣지 마라.**
3. **컬럼 검사가 0건을 봤다** — 블록 주석 지우개(`/\*…\*/`)가 문자열 안의 `/*` 를 만나 그 뒤를 통째로 먹었다.
   ⇒ SQL 문을 셀 땐 **원문 그대로** 본다(주석 지우개는 `//` 줄에만 쓴다).
4. **`e2e-verbs ⑫` 가 제가 심은 글자에 제가 걸렸다** — 그 절은 종류마다 `'알림 ' + kind` 를 띄워 놓고
   토스트에 **아이콘 이름**이 새지 않았는지 본다(T66 의 함정). 그런데 `guild` 는 T78 부터 `combat` 의
   **그림 이름**이고, T128 에서 **종류 이름**이 됐다 — 같은 낱말이 두 자리에 있으니 아무것도 새지 않아도 빨개진다.
   ⇒ 재기 전에 **제가 심은 문구를 걷어내고** 남은 것만 본다. 검사의 뜻은 그대로다(문구가 살아 있는지는
   바로 아래 '자명 통과 금지' 줄이 따로 지킨다). **제품이 아니라 하네스의 전제를 고쳤다.**
   ※ `e2e-verbs` 는 T90 의 하네스이고 이 카드의 무접촉 목록(T126 `NOTICE_ICO` · T125)에 없다.

## §4 검증

```
  scripts/e2e-guild.js       35 / 0   ★신설 — 이 카드 하네스(ws 손님 넷 · 브라우저 없음)
  scripts/test-itemlabel.js  45 / 0   ★알림 그림 표 — **이 하네스가 그림 충돌을 잡았다**(§0-ⓑ)
  scripts/test-client-globals.js 20 / 0  ★`onbEsc` 를 더했는데 전역 감사가 초록
  scripts/test-newcomers.js  46 / 0   scripts/test-onboarding.js 28 / 0
  scripts/test-harness-lint.js 19 / 0
  scripts/test-notice.js     18 / 0   ★`notice.js` 직접 접점(종류 열하나가 됐다)
  scripts/e2e-friends.js     39 / 0   ★카드 지정   scripts/e2e-onboarding.js 62 / 0  ★카드 지정
  scripts/test-membership.js 61 / 0   scripts/test-guild-treasury.js PASS  ★길드 종전 규약
  scripts/e2e-ui.js 125 / 0 · e2e-verbs 77 / 0 · e2e-downed 35 / 0  ★알림·HUD 접점
                                     ↑ e2e-verbs 는 **고친 뒤** 초록이다(§3-4)
```

**픽셀 하네스 열여섯은 안 돌렸다** — 공통.md §2 ⑦ 의 대상은 **렌더 층**(`3x-r*`·`34-m-renderloop`·`37-r1-*`·
지면·산·자연물 그림)이고, 이 카드가 만진 클라는 `50-i-panel.js`(알림 그림 표 한 줄)와 `70-lobby.js`(로비 줄)뿐이다.
**그 판단을 여기 적어 둔다** — 틀렸다면 이 줄이 틀린 자리다.

**접점 목록은 기계로 뽑았다**: `grep -l "handleChat\|httpStartInfo\|startInfo\|tribe\|notice"` 가 50개를 문다
(`notice` 가 너무 넓다 — 거의 모든 하네스가 알림을 읽는다). ⇒ **직접 접점**만 골라 돌렸다:
`test-notice`(경계 그 자체) · `test-itemlabel`(그림 표) · `test-membership`·`test-guild-treasury`(길드) ·
`test-newcomers`·`test-onboarding`·`e2e-onboarding`(시작 화면) · `e2e-friends`(같은 문법의 이웃) ·
`e2e-ui`·`e2e-verbs`·`e2e-downed`(알림 화면).
**새로 `require` 한 모듈**(§2 ⑧): `server/guild.js` — 신설이라 남의 하네스가 없고, 그 하네스가 `e2e-guild` 다.

**적재 추적**: 교집합 **0개** ⇒ 3시드 800일 A/B 갈음.

**리베이스 뒤 재측정**(main 이 `31e2bd1b`(T136·쉼터 지붕)까지 전진해 있었다 — 겹치는 파일 **0**):
`e2e-guild` **35/0** · `e2e-onboarding` **62/0**(T136 이 한 건 더했다 · 61→62). 충돌 0.

⚠**정직 보고 — 안 잰 것**: **유저 마을이 실제로 선 판에서 소개문이 시작 화면 줄에 뜨는 것**을 끝까지 못 봤다.
하네스가 재는 것은 central 저장 · 존 캐시 · 줄의 칸(모든 줄에 `intro` 가 있고 NPC 마을은 빈 칸)까지다.
유저 마을을 세우려면 기여 3회 → 빈터 권리 → 건립까지 가야 해서 이 하네스의 몫이 아니다 ⇒ 회부.

## §5 무접촉 확인

`T126 NOTICE_ICO`·`T125` — **표에 줄 하나만** 얹었다(그 표의 다른 줄은 안 건드렸다).
`itemlabel.js` · `sim/**` · NPC · 길드 벌점(`tribes.vp`) — 0줄.
`zone.js` 는 채팅 분기 한 줄 · `Guild.init` · 온보딩 훅 · `/guilddbg` 만 열었다.

## §6 회부

1. **`combat` 과 `guild` 의 그림** — 지금 `combat` 이 `guild` 그림을 쓴다(T78). 싸움 쪽을 새로 정해야 한다.
2. **부름 알림함** — 접속 중이 아니면 못 듣는다(T115 와 **같은 자리** · 한 카드로 묶는 게 맞다).
3. **길드 패널**(캐논 판정) · **길드 벌점 층**(§9.3) · **따라가기**.
4. **유저 마을 소개문의 끝까지 검증** — §4 의 안 잰 것.
5. 여러 부름 중 **고르기** — 지금 `/수락` 은 가장 최근 것으로 든다(몇 건을 지웠는지는 말해 준다).

## §7 실기 두 줄 (재민)

* **부르고 든다** — 길드에 든 사람이 `/초대 <이름>`, 불린 사람이 `/수락`. 문을 걸려면 길드장이 `/길드 초대제`
  (그 뒤로는 부름 없이 못 든다 · `/길드 공개` 로 되돌린다).
* **마을이 자기를 소개한다** — 길드장이 `/소개 <문장>` 을 쓰면 그 길드가 세운 마을 줄에 그 한 줄이 뜬다.
  60자에서 잘리고, 꺾쇠를 써도 화면이 안 깨진다.

## §8 푸시 — **착지**

| 항목 | 값 |
|---|---|
| 커밋 | **`eb5b6fd6`** ("길드 모집 — 부름과 문, 그리고 마을이 자기를 소개한다") |
| 브랜치 | `batch/guild-invite-0905` → `main` (브랜치 먼저 · 원격 main 재확인 · 그 뒤 main) |
| 갱신 | `31e2bd1b..eb5b6fd6` — **fast-forward** |
| **force** | **0회** |
| 리베이스 | **1회** — `cd722eaf` → `31e2bd1b`(T136 쉼터 지붕). 충돌 **0**(겹치는 파일 0) |
| 리베이스 뒤 재측정 | `e2e-guild` 35/0 · `e2e-onboarding` 62/0 · `test-notice` 18/0 · `test-itemlabel` 45/0 · `test-client-globals` 20/0 · `test-membership` 61/0 |
| `.lock` 전수 | `find .git -name "*.lock" -not -path "*/_stale_locks/*"`(`-maxdepth` 없음 · 족보 124) → **아래 §8-ⓐ** |
| `gc.auto` | `0` (이미 꺼져 있었다 — T91 회부가 든 그 설정) |
| 번들 | `~/Mini/_bundles_pushed/t128_2026-09-06.bundle` (17,813바이트) |
| 임시 참조 | `refs/temp/t128` 남겨 둔다 |

⚠**밤새 맥이 잠들어 있었다** — 코드·검증은 09-05 밤에 끝났고, 푸시만 09-06 10:04 UTC 에 됐다.
그 사이 45분 간격으로 여섯 번 깨워 확인했다(자동 재시도). **그동안 main 은 안 움직였다**(밀기 직전 재확인).

### §8-ⓐ `.lock` 전수 (푸시 뒤)

```
  find .git -name "*.lock" -not -path "*/_stale_locks/*"   →  0개
  find .git -name "tmp_*"  -not -path "*/_stale_locks/*"   →  6개
      ⇒ .git/_stale_locks/*.t128 으로 옮겨 **0개**
  다시 세어 .lock 0 · tmp_ 0.
```
`gc.auto=0` 이 이미 들어 있어 T91 이 본 77개짜리 자국(auto-gc 의 `pack-refs`)은 **안 났다**.
번들이 남긴 `tmp_idx_*` 만 6개였다. **재민의 git 을 막는 것은 이제 없다.**

배포는 하지 않았다(**재민만** · `redeploy-hanbando.sh`).
