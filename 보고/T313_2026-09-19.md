# 보고 — T313 자산·문서·확인 둘째 (세션11 · Sonnet · T306 뒤 · 2026-09-19)

**한 줄 답: `test-assets-audit` 는 BGM 표를 읽는 자리가 없다(표만 · 새 절 0) · `render-meta.json` 합성기 열 `_synth` 개명 완료(초록) · 공통§6·배정표·세션10 전달문 갱신 · `_MANIFEST.txt` 는 이 기기에 존재 자체가 없어 병합 대상 0 · 루트 `*.bundle` 도 0(대신 `_inbox`/`_bundles_pushed` 4건 조상표로 대체).**

판정 0 · 새 코드 0(하네스 한 줄도 아님 · 읽는 자리가 없었다) · 정본 무접촉 · 아래는 표와 파일만.

---

## ⓐ 하네스 diff 한 줄 + 초록

`scripts/test-assets-audit.js` 전수 grep(`render-meta`·`audio/bgm`·`bgm`) — **BGM 표를 읽는 코드가 없다**(유일한 매치는 395줄 주석: "매니페스트 안쪽 — 값·상한·BGM은 `test-audio`(세션9) 몫"). 카드 규칙대로 **새 절을 만들지 않고 "읽는 자리 없음" 한 줄로 기록**한다. 하네스 diff = 0줄.

`public/assets/audio/bgm/render-meta.json`: 합성기 세 열 `seconds`·`rms_db`·`peak_db` → **`seconds_synth`·`rms_db_synth`·`peak_db_synth`** (13트랙 전부 · 값 무변) · `measured` 오브젝트는 그대로 · `_T313` 메모 한 줄 추가("정본 = measured · PM 09-19"). 다른 소비처 확인: 레포 전체에서 `render-meta`를 쓰는 곳은 `scripts/test-audio.js`(세션9 SOUND 영역) 하나뿐 — 코드를 읽어 보니 `meta.tracks`(개수)·`t.tag/tags/mood/scene`(무관 필드)·`Object.keys(t)`(정보 출력용)만 쓰고 `.seconds`/`.rms_db`/`.peak_db`를 필드명으로 직접 참조하지 않는다 — 개명이 그 하네스를 깨지 않는다(실행 확인: **PASS 105/0**, 영역 밖이라 수정 없이 읽기 실행만).

검증: `node scripts/test-assets-audit.js` → **PASS**(npm install로 `@cwasm/webp` 등 신규 의존 2개 추가 설치 후 — 그 사이 커밋된 T308이 새 의존을 추가했다 · 내 변경과 무관).

## ⓑ 공통 §6 한 줄 diff

`인계/공통.md` §6(완료 기준)에 추가:
> **[T313 2026-09-19]** 보고 `보고/T◯◯◯_<날짜>.md` 는 **머리 15줄 안에 한 줄 답과 착지 해시**를 넣는다(PM·야간이 그 15줄만 읽는다 · T306 점검 8/13 위반).

그 밖 무접촉(§6의 기존 두 줄 그대로).

## ⓒ 배정표

`지시/세션배정_2026-09-19.md` — 09-19 낮 배정으로 갱신:

| 세션 | 카드 | 비고 |
|---|---|---|
| 세션1 | T300→**T311** | 갱신 |
| 세션2 | T304→**T314** | 갱신 |
| 세션3 | T297→**T312** | 갱신 |
| 세션5 | T301→**T309** | 갱신 |
| 세션7 | T302→**T310** | 갱신 |
| 세션8 | T303→**T308** | 갱신 |
| 세션11 | T306→**T313** | 갱신(이 카드) |
| 세션4·6·9·10·12·13 | 그대로 | 아래 참고 |

⚠**정본 보드 `~/Mini/태스크보드_v1.md` 가 이 기기에 없다**(레포에도 루트에도 없음 — device_list_dir 전수 확인) — §5 `grep "진행("` 대조를 못 했다. 세션4·6·9·10·12·13 은 **이전 세션배정 값을 그대로 유지**했을 뿐, 보드와 대조된 것이 아니다 — PM 회부. 참고로 이 갱신 시점에 origin/main 은 이미 `5e678633`(T323 착지 · "밤" 배치까지 진행)까지 나가 있었다 — 이 표는 카드가 지정한 **낮 구간 7개 세션만** 갱신했다.

`지시/README.md`에도 한 줄 추가: 배정표는 하루 여러 번 갱신될 수 있으니 세션은 매번 다시 pull 해서 읽으라는 안내.

## ⓓ 추신 문단

`지시/지시_T295_추신.md` 신설(세션10 붙여넣기용 한 문단, 판정 0 · 사실 + 출처 줄번호):

1. T301 회부① — 전쟁 세 줄 자리는 `war-live.js` 가 아니라 **`server/villages.js:2982~2996`**(T295/세션10 영역). `blocked`/`losBlocked`/`coverAt` = `:2987`/`:2988`/`:2993`, 술어 `_warBlockedCell`/`_warRockCell` = `:2900`/`:2906`(출처: `보고/T301_2026-09-19.md` §0ⓑ). 붙일 나무 판정 코드는 그 보고 §0ⓑ에 그대로 있음 — 반영은 세션10 몫.
2. 야간 09-19 `test-granary-add` 크래시(`ReferenceError: _vbFootprint is not defined`, `villages.js:5383`) — 원인은 하네스가 함수를 원문 그대로 잘라 돌리기 때문(제품은 멀쩡, `:906`의 top-level 함수). **PM 이 처방 ⓐ(하네스 쪽에서 같이 잘라 넣는다)로 판정 완료**(족보 (240)) — T284 제품 코드 무접촉, 세션10 조치 불필요(참고 전달).

## ⓔ 장부 표

`~/Mini/_push/_MANIFEST.txt` · `~/Mini/_bundles_pushed/_MANIFEST.txt` — **둘 다 이 기기에 없다**(`_push/` 디렉터리 자체가 없음 · `_bundles_pushed/`는 있으나 `_MANIFEST.txt` 없음). **병합 대상 0** — `_MANIFEST_old.txt` 로 옮길 옛 파일도 없다.

`~/Mini` 루트 `T*.bundle` — **0개**(카드가 말한 24개는 이 기기에 없음 · 아마 재민 맥 쪽 파일). 대신 이 기기에서 실제로 찾은 번들 전부의 조상표:

| 번들 | head 커밋 | origin/main 조상? |
|---|---|---|
| `_inbox/T284_war-world-0914.bundle` | `49b8040c` | 예 — 이미 들어감 |
| `_inbox/T284_war-world-0914_2.bundle` | `e9db3897` | 예 — 이미 들어감 |
| `_inbox/T295.bundle` | `12d0371c` | **아니오** — 아직 main 밖(병합 대기) |
| `_bundles_pushed/T307_measure-0919_2d9ad60.bundle` | `2d9ad603` | **아니오** — 아직 main 밖(병합 대기) |

옮기지 않았다(PM). `_push/`·루트 24개 번들·두 `_MANIFEST.txt` 는 실물이 있는 기기(재민 맥으로 추정)에서 이 항목을 다시 확인해야 한다 — 회부.

## ⓕ 한 줄 답

**`test-assets-audit`는 BGM을 안 읽는다(표만) · `render-meta.json` `_synth` 개명 완료(PASS) · 공통§6/배정표/세션10 전달문 갱신 완료 · `_MANIFEST.txt` 이 기기엔 부재(병합 0) · 루트 번들 0(대신 4건 조상표 — main 밖 2건 확인).**

---

## 회부(PM)

* `~/Mini/태스크보드_v1.md`·`_push/_MANIFEST.txt`·`_bundles_pushed/_MANIFEST.txt`·루트 `T*.bundle` 24개 — 전부 이 기기(다른 기기·계정)엔 없다. 재민 맥에서 §5 대조·매니페스트 병합·번들 처분을 직접 확인해야 한다.
* `_inbox/T295.bundle`(`12d0371c`)·`_bundles_pushed/T307_measure-0919_2d9ad60.bundle`(`2d9ad603`) — 아직 main 밖, 병합 대기중인 것으로 보임(세션10/12 작업물로 추정 — 확인 필요).
* 세션4·6·9·10·12·13 배정 — 보드 미대조 상태로 이전 값 유지. 보드 확인 뒤 재갱신 필요.

## 검증 규약

* 코드 = 하네스 diff 0줄(읽는 자리 없음) + json 열 이름만(값 무변) + 문서 5개.
* `git diff --stat origin/main`: `render-meta.json`·`인계/공통.md`·`지시/README.md`·`지시/세션배정_2026-09-19.md`(수정 4) + `지시/지시_T295_추신.md`(신규 1) — 5개 파일만.
* `node scripts/test-assets-audit.js` → PASS. `node scripts/test-audio.js`(영역 밖 · 읽기 확인만) → PASS 105/0(무변경 확인).
* 읽기: `head`·`grep`·`sed -n`·`git bundle list-heads`·`git merge-base --is-ancestor` 만 사용. 정본(`sim/`·`server/`·`public/client`) 무접촉.
