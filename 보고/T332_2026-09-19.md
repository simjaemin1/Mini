# 보고 — T332 실기표 키 낱말 붙이기 · village_night_trad 피크 표 (세션11 · Sonnet · T313 뒤 · 2026-09-19)

**한 줄 답: 실기표 붙음 3→24/30(키 없음 6: A1·A2·A3·A4·D3·H2) · 머리 "총 26항"→"총 30항" 정정 · `village_night_trad` 피크 +0.89dB 가 13곡 중 유일 양수(2위와도 0.98dB 차) — 표만, 재렌더는 세션8/ART 몫.**

판정 0 · 코드 0(`sfx-board.html`·`manifest.json` 무접촉) · `public/문서/실기_소리_2026-09-14.md` 문서 한 파일만 수정.

---

## ⓐ 실기표 붙음 3 → 24

`public/sfx-board.html`의 `parsePractice()`(줄 224~240)를 그대로 읽었다 — 정본 규칙은 **행 텍스트 전체에서 `manifest.json`의 `keys` 18종을 단어 경계 정규식**(`(^|[^A-Za-z0-9_])k([^A-Za-z0-9_]|$)`)으로 찾는 것뿐이다(절 주석·표 밖 문장은 안 본다 · `^|\s*[A-Z]\d+\s*\|` 로 시작하는 행만). 이 규칙을 그대로 흉내 낸 스크립트로 붙기 전/후를 쟀다:

```
붙기 전: rows=30 attached=3   (D4→bronze_hit · D6→harvest · F2→wolf_howl — 보고/T323 §ⓒ 그대로)
붙은 뒤: rows=30 attached=24
```

각 줄이 "말하는 소리"의 키를 찾아 `✗ 면 고칠 자리`(또는 이미 키가 박힌 줄은 그대로 두고) 칸 끝에 `· 키 \`이름\`` 형식으로 적었다(기존 3줄의 문장 안 표기 방식과 겹치지 않게, 새로 붙인 21줄은 통일된 접미사로 — 구분이 쉽도록). 결과:

| 항 | 붙은 키 | 근거 |
|---|---|---|
| B1 | `step_dirt`·`step_grass` | 발자국 박자 — 특정 지면 안 밝힘, 둘 다 해당 |
| B2 | `step_dirt`·`step_grass` | "흙과 풀" 직접 비교 |
| B3 | `step_dirt`·`step_grass` | 발자국 크기 — 둘 다 해당 |
| C1 | `fire` | 모닥불 거리 |
| C2 | `fire` | 모닥불 여럿 |
| C3 | `wind`·`rain`·`bird`·`fire`·`water` | "바람·비·새가 멎고 모닥불·물만 작게" — 다섯 다 명시 |
| C4 | `fire`·`water`·`bird`·`wind`·`rain` | "모닥불·물·새 1초 · 바람·비 2초" — 다섯 다 명시 |
| D1 | `axe` | 도끼질 시점 |
| D2 | `axe` | 남이 베는 나무(도끼질) |
| D3 | **키 없음** | "헛손질" 일반론 — "수확"은 예시일 뿐, 특정 키로 단정하면 오도(D6 harvest 와 혼동 위험) |
| D4 | `axe`(+기존 `bronze_hit`) | 곡괭이 ↔ 도끼 **비교** — 둘 다 주제 |
| D5 | `eat` | 먹기 |
| D6 | (기존 `harvest`) | 변경 없음 |
| E1 | `cast`·`bite`·`hook` | 던짐·입질·걸림 배역 셋 |
| E2 | `bite` | 입질 시점 |
| F1 | `deer_call` | 사슴 울음 |
| F2 | (기존 `wolf_howl`) | 변경 없음 |
| F3 | `tiger_growl` | 호랑이 잔향 |
| F4 | `boar_grunt` | 멧돼지 |
| G1 | `water` | 물 |
| G2 | `bird` | 새 |
| G3 | `rain` | 비 들어옴 |
| G4 | `wind` | 바람 세기 |
| G5 | `wind`·`bird` | "바람에 새소리" 둘 다 주제 |
| H1 | `rain`·`water`·`fire`·`axe` | "비 오는 날·강가·모닥불 옆·도끼질" 넷 다 명시된 시나리오 |
| A1·A2·A3·A4 | **키 없음** | 시스템 켬/끔·볼륨·BGM — BGM 은 `manifest.json`의 `keys`(효과음 18종)에 없다(별도 `bgm`/`render-meta.json` 자리) |
| H2 | **키 없음** | "전체 균형" — 특정 키 아닌 일반론 |

**설계 판단 한 가지 적어 둔다(판정 아님 · 확인용):** 규칙이 한 줄에 여러 키를 허용하길래(코드가 `keys.forEach`로 전부 검사) C3·C4·E1·H1 처럼 **그 줄이 실제로 복수 소리를 동시에 시험하는 경우** 여러 키를 다 붙였다 — 엉뚱한 낱말이 우연히 걸리는 것(카드가 경고한 "eat/rain" 오탐)과는 다르게, 전부 그 줄 문장에 **명시된 소리**만 골랐다. 이견 있으면 되돌리면 된다(문서 한 줄씩이라 코드 0으로 뺄 수 있다).

머리 "**총 26항**" → "**총 30항**"으로 정정(카드 지시대로). 그 옆 "그 밖 18(배선 판정)"도 30-★8=22 라 같이 정정했다(★8은 그대로 맞다 — B2·D2·D4·E1·F1·F3·G5·H1).

`e2e-audio-probe.js` ⑲~㉓을 읽어 봤으나 실기표 붙음 수를 재는 절이 없다(⑲는 소리판 행수=키수 · ㉒㉓은 단추 배선만 잰다) — **돌릴 수 있는 기존 하네스가 없어 표만** 냈다(위 붙기전/후 카운트가 그 표다). 새 assertion 은 코드 0 규칙상 추가하지 않았다.

## ⓑ `village_night_trad` 피크 표

`render-meta.json`의 `measured`(T306 실측) 기준, peak_db 내림차순:

| 트랙 | seconds | rms_db | peak_db |
|---|---|---|---|
| **village_night_trad** | 113.0 | -17.63 | **+0.89** |
| village_day_trad | 108.28 | -17.20 | -0.09 |
| journey_ari | 112.86 | -17.43 | -0.17 |
| journey_trad | 112.86 | -17.22 | -0.30 |
| village_day_ari | 114.02 | -17.57 | -0.48 |
| village_night_ari | 113.0 | -17.97 | -0.53 |
| village_day_jeongak | 137.0 | -16.75 | -0.84 |
| village_day_amb | 114.02 | -19.85 | -0.96 |
| battle_trad | 112.88 | -16.66 | -1.38 |
| journey_amb | 112.86 | -19.21 | -1.40 |
| battle_ari | 112.88 | -17.22 | -1.60 |
| battle_amb | 112.88 | -18.87 | -1.90 |
| village_night_amb | 113.0 | -18.84 | -4.21 |

**13곡 중 유일한 양수**이고 2위(`village_day_trad` -0.09)와도 0.98dB 차이다 — 디스크 파일(`gugak.py` 파이썬 렌더러 산출물)이 넘쳐 있다는 T323 회부③ 그대로. 런타임(`bgm.js`)은 절차적 엔진이라 이 파일을 안 읽으므로 게임 내 소리와는 무관 — 크레딧·배포 산출물에만 남은 결함. 재렌더 여부는 세션8/ART·PM 몫, 이 카드는 표만.

## ⓒ 한 줄 답

**실기표 붙음 3→24/30(키 없음 6) · 총 26항→30항 정정 · `village_night_trad` 피크 +0.89dB 유일 양수(표 확인) — 코드 0.**

---

## 회부

* D3 를 "키 없음"으로 남긴 것에 대한 이견 — 원한다면 `harvest`(예시로 든 동사)를 붙일 수 있다(PM/다음 카드 판단).
* C3·C4·E1·H1 처럼 한 줄에 여러 키를 붙인 것이 과한지 — 문서 한 줄씩이라 되돌리기 쉽다.
* `village_night_trad` 재렌더 여부(세션8/ART).

## 검증 규약

* 코드 0 · `git diff --stat origin/main`: `public/문서/실기_소리_2026-09-14.md` 한 파일만(`sfx-board.html`·`manifest.json` 무접촉 확인).
* 붙음 카운트는 `sfx-board.html`의 `parsePractice()` 정규식을 그대로 옮긴 별도 스크립트로 전/후 측정(파일에 남기지 않음 — 코드 0 규칙).
* 읽기: `grep`·`sed -n`·`node -e`(읽기 전용 확인 스크립트, 저장 안 함)만 사용.
