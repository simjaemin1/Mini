# 배치 15 v2 — 50마을 세계 처방전 + 라이브 마감 (2026-08-04a)

기준: `684bff7` 이후. 재민 확정: **"최종적으로는 50개가 있어야 하거든."**
재민 질문: **"마을 위치를 바꿔야 해? 아니면 비옥도를 수정해야 하나?"**

---

## 0. 결론 먼저 — 재민 질문에 대한 답

> **둘 다 거의 아니다. 후보 51곳을 전부 심어도 47곳이 800일을 그냥 살고, 1,600일엔 51곳 전부가 산다.**
> 손이 필요할 수 있는 자리는 **4곳뿐**이고 전부 존 남서쪽 한 구석에 몰려 있다.
> 위치를 옮길 자리는 **0곳** — 51곳을 다 심어도 영토가 겹치는 쌍이 하나도 없다.
> (유일한 예외: **어촌6** 은 주변 24셀에 뭍이 없어 시딩 자체가 스킵된다 — 그래서 실제 상한이 **50곳**이다.)

바꿔야 하는 것은 지도가 아니라 **선별 규칙**이다. 32곳이 빠진 이유는 땅이 나빠서가 아니라
`MIN_SPACING_PX=12000`(영토 반경의 11.3배)과 `FOOD_FLOOR=2.0` 두 게이트 때문이고,
그 둘 다 실측이 **과보수**임을 보여 준다.

| 실측 | 값 |
|---|---|
| 51곳 전수 시딩 800일 · 3시드 | **생존 47 · 좀비 2 · 소멸 2** (전부 1/3 시드 — 3/3 실패는 0곳) |
| 51곳 전수 시딩 **1,600일** · 시드 42 | **소멸 0 · 좀비 0 · 총인구 9,100** (최소 인구 40) |
| 세계 인구 (19곳 → 51곳) | 3,749→5,158 / 3,394→6,392 / 3,503→5,492 (**+38% ~ +88%**) |
| 주조 마을 | 3/19 · 4/19 · 3/19 → **8/51 · 9/51 · 8/51** |
| 구리 재고(시드 42) | **1 → 217** |
| 영토가 겹치는 후보 쌍 | **0쌍** (최단 쌍 농촌4–어촌16 3,217px vs 겹침 문턱 2,121px) |
| 상한 `VILLAGE_MAX=20` 이 병목인가 | **아니다** — 20→51 로 올려도 선별 결과가 19곳 그대로다 |

---

## 1. 왜 32곳이 빠졌나 — 오라클 3판

배제 원인을 추론하지 않고 **정본 함수(`pickSeedVillages`)를 손잡이만 바꿔 세 번 불러** 집합 차이로 갈랐다
(`scripts/plan-village-50.js`). 알고리즘을 스크립트에 베끼면 조용히 갈라진다 — 그래서 오라클로 쓴다.

| 판 | 조건 | 선별 |
|---|---|---|
| A | 현행 (`VILLAGE_MAX=20`) | **19곳** |
| B | 상한만 해제 (`VILLAGE_MAX=51`) | **19곳** ← 상한은 병목이 아니다 |
| C | `SEED_ALL=1` (하한·간격·상한 전부 우회) | **51곳** |

| 분류 | 곳 | 원인 |
|---|---|---|
| 선별 | 19 | 현행 |
| **간격밀림** | **18** | 땅은 하한을 통과했는데 `MIN_SPACING_PX=12000` 에 걸렸다 |
| **하한미달** | **14** | `식량 = 비옥×1.5 + 물×1.2 + 사냥×0.7 < 2.0` |

---

## 2. 51곳을 전부 심으면 누가 죽나 — 3시드 800일 실측

`SEED_ALL=1` 로 `scripts/econ-lab-real.js 800` 을 3시드(42·7·1020) 돌렸다. 한 판 ~355초.

```
요약: 생존 47 · 좀비 2 · 소멸 2 / 총 51곳

시드별 원장
  seed 42    소멸 없음        · 좀비 농촌21
  seed 7     소멸 광산6       · 좀비 농촌11
  seed 1020  소멸 농촌21      · 좀비 농촌12
```

문제 자리는 **어느 것도 3/3 시드에서 실패하지 않는다** — 전부 1/3 이다.
이 프로젝트가 이미 아는 "좀비 마을 칼날 평형"의 범위 안이고, 그래서 **단일 시드로는 판정 불가**다.

### ★1,600일 장기 (시드 42) — 문제 자리가 스스로 회복한다

| | 800일 | **1,600일** |
|---|---|---|
| 총인구 | 5,158 | **9,100** |
| 소멸 | 0/51 | **0/51** |
| 인구 ≤20 (좀비 후보) | 농촌21 | **없음** |
| 최소 인구 5곳 | — | 농촌20 40 · **광산6 48** · 농촌8 52 · 농촌18 53 · 농촌6 56 |
| 주조 마을 | 8/51 | **9/51** |

800일에 좀비였던 농촌21 도, 다른 시드에서 죽었던 광산6 도 1,600일엔 40~48명으로 선다.
⇒ **§3 의 4곳 처방은 "800일 시점의 칼날 평형"이고, 장기적으로는 51곳 전부가 자립한다.**
(다만 1,600일은 시드 42 하나만 돌렸다 — 다른 두 시드의 한계 사례는 장기로 안 재 봤다. 있는 그대로 적는다.)

### ★시딩 하한(식량 2.0)은 생존을 예측하지 못한다

| | 곳 | 그중 문제 |
|---|---|---|
| 하한 **미달**(심지 않던 자리) | 14 | **3곳 (21%)** |
| 하한 **통과** | 37 | **1곳 (3%)** |

방향은 맞지만 문턱이 너무 높다. 하한 미달인데 멀쩡히 사는 자리들:

| 마을 | 식량 점수 | 3시드 평균 인구 |
|---|---|---|
| 광산1 | **0.59** | 34.3 |
| 광산3 | **0.81** | 47.7 |
| 농촌10 | 1.20 | 35.0 |
| 임업4 | 1.73 | **81.0** |
| 광산7 | 1.85 | 44.3 |

반대로 하한을 통과한 **농촌12(2.52)** 는 좀비가 됐다.
⇒ 하한 2.0 은 **건강한 마을 11곳을 버려서 위태로운 3곳을 걸러내는** 잣대다.

---

## 3. 마을별 처방전 — 재민 에디터 작업 지시서

**손이 필요한 자리는 4곳이다. 나머지 47곳은 `그대로 OK`.**

| 마을 | 셀 | 시딩잣대 | 식량 | 평균 인구 | 판정 | K 병목 | 처방 |
|---|---|---|---|---|---|---|---|
| **광산6** | (227, 3078) | 하한미달 | 0.80 | 18.0 | 소멸 1/3 | **prod**(식량 흐름) | ★**물 접근** — 최근접 물을 약 **52셀** 당겨라(개천·못). 또는 **그대로 두고 교역으로 먹인다**(2/3 시드는 그렇게 버텼다) |
| **농촌21** | (428, 3853) | 하한미달 | 1.98 | 16.7 | 소멸 1/3 | **prod** | **물 1셀** — 하한에 0.02 모자란다. 물길을 1셀만 당기면 통과 |
| **농촌11** | (161, 3489) | 하한미달 | 1.23 | 31.7 | 좀비 1/3 | **prod** | **물 33셀** 당기기(사냥터 밴드 숲↑ 로도 대체 가능) |
| **농촌12** | (161, 3853) | 선별(현행) | 2.52 | 13.0 | 좀비 1/3 | ★**fuel**(연료 흐름) | 식량이 아니라 **나무**다 — 나무 0.45·돌 0.25 로 둘 다 바닥인 민둥 평야다. 반경 140셀에 **숲을 심어라** |

나머지 47곳: **`그대로 OK` — 지형을 만지지 마라.**

★**네 곳은 전부 존 남서쪽 한 구석에 몰려 있다** — x 7~20% · y 76~95%(존 2,188×4,063셀 기준).
흩어진 4개 작업이 아니라 **사실상 한 지역**이다. 그 일대에 물길 하나와 숲 한 덩이를 넣으면
네 자리가 함께 풀린다(광산6 만 52셀로 멀다 — 별도 판단).

### 처방 수치의 근거 — 무엇을 만지면 무엇이 움직이나

시딩 선별이 쓰는 비옥도는 **영토가 없는 프록시**다(`extractLandParamsApprox` 는 `layout.territory` 가
비면 비옥도 필드를 안 읽는다 — `pickSeedVillages` 는 항상 `{territory: []}` 로 부른다):

```
비옥도(프록시) = clamp(0.4 + 물원시×0.9 − 바위밀도×1.5, 0.1, 2.0)
물             = 물원시 × 1.6,   물원시 = 1 − 최근접물거리 / 140셀
식량           = 비옥도×1.5 + 물×1.2 + 사냥×0.7
⇒ d(식량)/d(물원시) = 1.35 + 1.92 = 3.27   ⇒ 물 1셀 당기면 식량 +0.0234
⇒ 필요분 Δ 를 채우려면 물을 Δ × 42.8 셀 당기면 된다
```

★★**중요 — 비옥도 레이어를 칠하는 것은 시딩 선별을 바꾸지 않는다.**
선별 시점엔 영토가 없어 비옥도 **필드**를 안 읽고 위 프록시를 쓴다. 프록시를 움직이는 것은
**물 거리**와 **바위 밀도**뿐이다. (칠한 비옥도는 심긴 뒤의 실제 생산에는 반영된다 —
`seedVillages` 가 실영토로 다시 재기 때문이다. 두 잣대를 섞지 마라.)

⇒ 재민 질문의 직답: **"비옥도를 수정"하는 것으로는 마을이 안 심긴다. 물을 당기거나 바위를 걷어야 한다.**
그리고 이번 실측대로라면 **그 작업조차 광산6 한 곳 말고는 필요 없다.**

### 위치 이동은 필요 없다

| | 값 |
|---|---|
| 후보 51곳 최단 쌍 | 농촌4–어촌16 **3,217px** (101셀) |
| 영토 반경(3,450셀 기준) | 1,060px |
| 겹침 문턱(2R) | 2,121px |
| **영토가 겹칠 수 있는 쌍** | **0쌍** |
| 현행 `MIN_SPACING_PX` | 12,000px = 영토 반경의 **11.3배** |

51곳은 물리적으로 그대로 들어간다. 간격 규칙만 예컨대 3,000px 로 낮추면 된다
(겹침 문턱 2,121px 보다 여유 있고, 최단 쌍 3,217px 를 살린다).

> ⚠단 **지금 바꾸지 않았다.** 시딩 기본값 변경은 라이브 리셋과 함께 가는 결정이라 재민 몫이다.
> 이 배치는 `SEED_ALL=1`(랩/진단 전용 손잡이)만 추가했고 프로덕션 선별은 한 비트도 안 바뀐다.

### 전환 시 권장 순서 (재민)

1. 광산6 일대에 물길/못 — 최근접 물 52셀 당기기 (선택: 안 해도 2/3 시드는 버틴다)
2. 농촌21 물 1셀 (거의 공짜)
3. `MIN_SPACING_PX` 12,000 → 3,000, `FOOD_FLOOR` 2.0 → 0.5(또는 폐지), `VILLAGE_MAX` 20 → 51
4. 라이브 리셋(런북 동형) → 50곳 세계 시작
5. **스폰 이동은 이때 정한다**(v1 ② 이월 — 아래 §6)

---

## 4. 서버 용량 — 2코어가 50곳을 감당하나

로컬(2코어·8GB 컨테이너)에서 `SEED_ALL=1` 로 실제 존을 띄웠다. **감당한다.**

| | 18곳(현행) | **50곳** |
|---|---|---|
| 시딩 총시간(첫 부팅) | 143초 | **303초** |
| 교역 BFS 거리행렬 | 18마을 153쌍 **23.6초** | 50마을 1,225쌍 **26.0초** |
| 부팅 전체(프로세스 시작→`zone server up`) | 211초 | **329초** |
| Stage4A 실물화 | 큰집 18·움집 36 · **3,576행** | 큰집 50·움집 100 · **10,078행** |
| welcome 영토 페이로드 | 4,352셀 ≈ **38KB** | 11,575셀 ≈ **102KB** |
| econ 인구 / 스폰 NPC | 144 | **400** |
| tick_avg (관측자 1·사람 0) | 5.98ms | **5.23ms** |
| 존 프로세스 RSS | — | **207MB** |

판정: **라이브 VPS(1.9GB / 2코어)에서 문제없다.** RSS 207MB 는 여유가 크고, 쌍이 8배로 늘어난
거리행렬도 26초로 끝난다(그리드 서브샘플 방식이라 마을 수에 거의 안 붙는다).

주의할 지점 둘:

* **첫 부팅 5.5분** — 라이브 재배포 후 존이 뜨기까지 그만큼 걸린다(리셋 시 1회).
* **welcome 영토 페이로드 102KB** — 접속 1회당이다. 동접이 늘면 이 값이 먼저 아플 수 있다
  (경계 셀만 보내는 이미 최적화된 경로다. 더 줄이려면 LOD 가 필요 — 지금은 회부만).

후보 51곳 중 **어촌6 한 곳은 시딩 자체가 스킵된다**: `중심 (65086,126466) 주변 24셀 내 뭍 없음`.
그래서 "51곳"의 실제 상한은 **50곳**이다 — 재민이 말한 "50개"와 정확히 맞는다.
(어촌6 을 살리려면 좌표를 뭍으로 옮겨야 한다 — 이것이 **유일한 '이동 필요' 후보**다.)

---

## 5. ★발견 — 주기적 tick_max 히치 (수백 ms) · **원인 미확정 · 회부**

용량 측정 중 `tick_max` 가 주기적으로 **660~780ms** 튀는 것을 발견했다. 좁혀 봤지만
**단정할 만큼은 못 좁혔다.** 잰 것을 그대로 적는다:

| 조건 | tick_avg(중앙값) | tick_max |
|---|---|---|
| 50곳 · 야생 ON | 4.5ms | **707ms** |
| 50곳 · 야생 ON · **central 정지** | 4.7ms | **777ms** ← central 경합이 아니다 |
| 50곳 · **야생 OFF**(`ENABLE_WILDLIFE=0`) | 5.9ms | **13ms** ← 사라졌다 |
| 18곳 · 야생 OFF · 레거시 ON | 4.9ms | **719ms** ← **다시 났다** |

세 번째 줄만 보면 야생 층이 범인이지만 네 번째 줄이 그 결론을 깬다.
⇒ **원인 미확정.** 여기서 "야생 층이 원인이다"라고 적으면 이 프로젝트가 여러 번 당한
그 오진(배치 7 "지도에 철 0개")과 같은 종류가 된다.

확실한 것만 적으면:

* 이 배치의 변경(레거시 끄기)이 만든 것은 **아니다** — 레거시 ON/OFF 양쪽, 18곳/50곳 양쪽에서 다 난다.
* **평균이 아니라 스파이크만의 문제**다(평균은 4.5~6ms 대역에서 안 움직인다).
* 2코어 컨테이너라 내 다른 작업과의 경합을 완전히 배제하지 못했다 — 한 표본에서는 5,803ms 도 찍혔다.

**회부(라)** — 전용 배치에서 다른 부하 없이 잡아야 한다. 후보 관측 지점:
`wildlife _rebuildHabitat`/`S._wild` 재계산 · 60초 주기 `reconcileGuildGranaries` · 저장 큐 플러시.
`--cpu-prof` 로 스파이크 창을 떠서 스택으로 확정하는 것이 정공법이다.

---

## 6. 라이브 마감 4건 (v1 유지분)

### ⑴ 레거시 마을 33곳 끄기 — 완료

`zone.js` 의 `VILLAGES` 는 **레거시 실체화 전용 목록**이다. 존 설정 한 줄로 이 목록을 비우면
소비처 6곳이 자연히 0이 된다(`grep VILLAGES server/zone.js` 전수):

| 소비처 | 껐을 때 |
|---|---|
| `spawnVillagers()` | 한옥·농지·NPC 6명/마을 → 0 |
| `registerVillageGuilds()` | central NPC 길드 33 등록·금고·영토 → 0 |
| `isNearVillage()` | 늑대·맹수 팩 스폰 제외 원 → 없음 |
| Wildlife `legacyVillages` | 서식 밴드 완충·앵커 → **시뮬 마을만**(정합) |
| `_villageAt()` · NPC 부활 좌표 | 레거시 NPC·길드가 없으니 도달 불가 |
| `zonePublicMeta().villages` | welcome 페이로드 → `[]`. **클라 미사용**(실클라에서 `__getZonesMeta().hanbando.villages === null` 확인) |

```js
// server/zone-config.js — hanbando 만
legacyVillages: false,
// server/zone.js
const LEGACY_VILLAGES_ON = !(ZONE.legacyVillages === false);   // 키 없으면 ON — 다른 존 회귀 0
const VILLAGES = (ZONE_ID === 'canadia' || !LEGACY_VILLAGES_ON) ? [] : (…종전…);
```

**자르면 안 되는 뿌리 둘 — 실측으로 확인:**

1. 시딩 후보 공급: `seedVillages` 는 `terrain.getZoneVillages()` 를 **직접** 부른다. 새 DB 첫 부팅 로그가
   `후보 51 → 선별 19` 로 종전과 한 글자도 다르지 않다.
2. 레거시 디듀프: 같은 DB 에 래퍼로 **되켜서** 확인 —
   `레거시 디듀프(§4-4 Stage4A): 시뮬 마을과 중복 18곳 스킵 · 레거시 유지 33곳`. 무변.

**A/B 실측**(같은 DB, 래퍼로 플래그만 교체):

| | 레거시 ON | 레거시 OFF |
|---|---|---|
| players(NPC 포함) | **342** | **144** (−198 = 33 × 6) |
| claims | **33** | **0** |
| buildings DB `npc_house_*` / `npc_farm_*` | 1,408 / 366행 | **0 / 0** |
| Stage4A 실물화(시딩분) | 3,576행 · 큰집 18 · 움집 36 | **동일** |
| central NPC 길드 등록 | 33건 | 0건 |

**tick_avg 는 이 조건에서 구별되지 않는다.** 같은 DB·같은 관측 위치·야생 OFF 로 연속 A/B 한 결과
중앙값 **OFF 5.2ms vs ON 4.9ms**(표본 16씩, 최소는 양쪽 다 4.5ms) — 차이가 잡음보다 작고
방향도 뒤집혀 있다. 사람 0명·관측자 1명이라 **휴면 NPC 198명은 틱 비용을 거의 안 쓰기 때문**이다
(diag 의 `act8` 이 그 증거 — 활성 NPC 는 양쪽 다 8명). 줄어든 것은 상시 틱이 아니라
**엔티티 수·welcome 페이로드·부팅 시간·메모리**다:

| | 레거시 ON | 레거시 OFF |
|---|---|---|
| 존 프로세스 RSS(같은 스크립트 연속 측정) | **310MB** | **280MB** (−30MB) |
| tick_avg 중앙값 | 4.9ms | 5.2ms (구별 불가) |

> ⚠1차 측정에서는 OFF 4.75ms / ON 6.20ms 가 나왔는데 **그 값은 틀렸다.** 그때 같은 2코어에서
> 아이콘 렌더와 central 프로토타입 시뮬이 돌고 있었고, 측정이 플래그가 아니라 **경합**을 재고 있었다.
> (§2 "기준을 먼저 검증해라" — 그래서 위 표는 야생·경합을 끄고 연속으로 다시 잰 값이다.)

**`scripts/cleanup-legacy.js`** — 라이브 고아 행 청소. 레거시가 DB 에 쓰는 곳은 정확히 둘이다
(`npc_house_<zone>_<v>_<h>` · `npc_farm_<zone>_<v>`). 길드 영토는 메모리에만 만들어진다
(`spawnGuildClaimsForVillage` 에 `db.insertClaim` 호출이 없다 — 유일한 호출부는 플레이어 `tryClaim`).

스크립트 원칙: **전수 census 먼저**(화이트리스트 가정 금지, MIXED 나오면 중단) · **음성 대조로 실측**
("플레이어 걸 안 문다"를 주장하지 않고 술어가 무는 비레거시 owner 수를 **세서 0 이어야** 진행) ·
드라이런 기본 · 백업 선행 · 트랜잭션 · 사후 검증 · 멱등.

자명한 통과를 막으려고 **보존 대상이 실제로 있는 상황**을 만들어 검사했다 —
`anon_TESTGUEST` 벽 1채 + 등록 계정 `jaemin` 궤 1채를 주입한 뒤:

```
② 삭제 대상  buildings 1,775행 · claims 0행
② 안전 대조  비레거시 owner 0종 · 시딩 3,576행 중 피격 0 · 비 npc_* 5행 중 피격 0
④ 삭제       1,775행
⑤ 사후       남은 레거시 0 · 시딩 3,576→3,576 OK · 플레이어 5→5 OK   ✅  (재실행 → "지울 것이 없다")
```

> 참고: 존은 부팅마다 `DELETE FROM buildings WHERE owner_id LIKE 'npc_%'` 를 돌린다 —
> 레거시 한옥·농지는 **재배포만으로도 사라진다**(로컬 실측 5,350행 삭제 → 시딩분 3,576행만 재기록).
> 이 스크립트는 재부팅 없이 즉시 치우거나, 남아 있던 양을 숫자로 확인할 때 쓴다.

### ⑵ 첫 부팅 건물 미탑재 — **전제가 틀렸다** (수리 대상 아님)

인계 관측: *"첫 부팅에서 실물화가 DB 에 행을 쓰지만 메모리 건물 맵에는 안 올라가(bld=5) 재시작이 필요했다."*
고치기 전에 **재현부터** 했다. 재현되지 않았다.

| 실험 | 관측 위치 | 적재 |
|---|---|---|
| 새 DB **첫 부팅**(시딩 포함) · 재시작 없음 | 농촌22 | **703채** (wall 82·floor 127·farmland 42·vtile 452, 전부 `npc_simvil_*`) |
| 같은 첫 부팅 | 벌말 광장(옛 스폰) | **0채** (`bld=3`) |
| **재시작 후** | 농촌22 | 703채 |
| **재시작 후** | 벌말 광장 | **0채 — 첫 부팅과 동일** |

**재시작은 아무것도 바꾸지 않는다.** `bld=5` 의 정체는:

> 옛 스폰에서 가장 가까운 마을이 **6,780px(212셀)** 떨어져 있었다. 청크 활성 반경은 **1,200px** 다.
> 그 자리엔 올릴 건물이 애초에 없었다.

건물은 설계상 지연 적재다(`activateChunk → materializeBuildingsInChunk`). 사람 없는 빈 들판에서
`bld` 가 한 자리인 것은 정상이고, 그래서 재시작해도 그대로다. **코드는 한 줄도 안 고쳤다 — 고칠 게 없었다.**

인계가 요구한 검증 기준("새 DB 부팅 1회 → 재시작 없이 실화면에 큰집·움집")은 그대로 충족했다:
새 DB 첫 부팅 · 재시작 없음 · 진짜 클라 접속 → 클라 메모리 건물 **703채**, 화면에 지붕 있는 큰집과 움집
(`/tmp/b15-shots/spawn.png` · `hall.png`), 404 응답 **0건**.

### ⑶ 아이콘 4종

클라 `ITEM_ICONS` **36키**와 `public/assets/icons/*.png` 전수 대조 → 파일 없는 키가 **정확히 4개**
(`silver` `gold` `nickel` `jade_raw`), 키 없는 파일 0개.

**정본 파이프라인 그대로** 썼다. 이 컨테이너엔 blender 바이너리가 없지만 `bpy` 모듈(4.2.0)이 있어
`python3 scripts/icon_render.py` 로 같은 스크립트를 돌린다. 사본이 아님을 증명하려고 기존 `copper` 를
다시 렌더해 커밋본과 비교: **1,048,576바이트 중 다른 바이트 0**(바이트 동일).

| 키 | 형태 | 고증 근거 |
|---|---|---|
| silver | 작은 빵떡 잉곳 3개 + 황화 변색 띠 | era.js 청동기 `npcMetals` 에 은(REDUCTION_T 800). 산출이 적어 소형 |
| gold | 빵떡 덩이 1 + 굵은 사금 알갱이 4 | `REDUCTION_T.gold = 0` — 환원 불필요한 **자연금** |
| nickel | 세워 놓은 각괴 2개 + 조각 | era.js `steel` 대 금속(REDUCTION_T 1300) |
| jade_raw | 풍화 회백 자갈 + 쪼갠 면의 초록 속살 | 송국리기 곡옥·관옥 원료 **연옥 원석**(금속 아님) |

96px 대조 시트를 실제로 만들어 **두 번 고쳤다**(이 파일이 이미 겪은 "잉곳→초가지붕", "운철→골프공"과 같은 함정):
금이 올리브색으로 떠서 metal 0.95→0.72(월드 반사가 색을 덮었다) · 니켈이 iron 과 실루엣이 겹쳐 세로 빌릿으로 ·
옥의 초록이 선으로 읽혀 쪼갠 면을 덩이 절반 크기로.

검증: `curl /assets/icons/{silver,gold,nickel,jade_raw}.png` **전부 200** · 실클라 404 **0건**.

### ⑷ central 옛 시뮬 — 코드로 규명

`server/central.js` 는 부팅 시 **프로토타입 경제 월드 2개**를 만들어 각각 **1초 = 1일**로 돌린다.

| | 무엇 | 소비처 | 실효 |
|---|---|---|---|
| `economyWorld` | `economy.createWorld({seed:42, villageCount:20})` (v1) | `/economy/villages` · `/economy/prices*` | **있다** — 클라 시세 패널(**Q키**)이 캐나디아 아닌 존에서 `/economy/prices` 를 부른다 |
| `canadiaWorld` | `economyV2.createWorldV2({seed:4242, villageCount:7, namePool:['단풍','늑대골','얼음호수','검은숲','강철광산','연어강','대평원']})` | `/economy/canadia/{villages,prices,tradelog,caravans}` | **없다** — 그 넷을 부르는 클라 코드가 전부 `primaryZoneId === 'canadia'` 뒤(client.js 8323·8416·8442·8563·8569). 한반도 단독 운영에선 성립 불가 |

재민이 본 `🌈풍요/💀캐러밴 약탈 … 검은숲·연어강·늑대골` 은 **저 `namePool`** 이다.

**비용 실측:**

| | 1일당 | 1Hz 상시 환산 |
|---|---|---|
| v1(20마을) | 17.5 → 12.7 → 12.0ms (D0-500 / D500-3000 / D3000-6000) | 1.2 ~ 1.8% (평탄) |
| v2(7마을) | 21.6 → 31.7 → 41.3 → 45.0 → 43.5ms (D0-500 … D4500-6000) | **2.2 → 4.3%** (자라다 평탄) |

central 프로세스(`/proc/<pid>/stat` 50초 표본): canadia 지연 대기 **3.22%** vs 가동 **9.02%**.
⇒ central 상시 CPU 는 사실상 이 두 시뮬이 전부다. 라이브 "~50%"는 코어 성능 배율 차이다
(로컬에서 50%가 재현되진 않았다 — 있는 그대로 적는다).

**조치 — 무조건 끄지 않고 첫 요청에 켠다:**

```js
// 미설정(채택값) = 지연 시작 · ENABLE_CANADIA_ECON=1 → 부팅 즉시 · =0 → 완전 정지
if (req.url.startsWith('/economy/canadia/')) ensureCanadiaEcon(`요청 ${req.url}`);
```

캐나디아가 돌아오면 자동으로 되살아난다(죽은 세계를 응답하지 않는다). 실측 확인 — 부팅 후
`/economy/canadia/prices` 가 `day:0` 이다가 그 요청과 동시에 `▶ 시뮬 시작` 이 뜨고 1초=1일로 흐른다.
v1 은 실효가 있어 **기본 ON 유지** + 손잡이(`ENABLE_PROTO_ECON=0`)만 달았다.

---

## 7. 회부 — 재민 판단 사항

**(가) 시딩 게이트 완화 — 얼마나?** 실측은 하한 2.0 이 과보수(건강한 11곳을 버려 위태로운 3곳을 거른다)
라고 말한다. `FOOD_FLOOR` 를 0.5로 낮출지 폐지할지, `MIN_SPACING_PX` 를 3,000으로 할지는 리셋과 묶인
결정이라 손대지 않았다. §3 전환 순서 참조.

**(나) 광산6 을 지형으로 살릴까, 교역으로 둘까.** 2/3 시드는 교역으로 버틴다(인구 18). 이 자리는
세계 최고 구리(구리 지수 1.017)라 살려 두는 값이 크다 — 51곳 세계에서 구리 재고가 1→217 로 뛴 주된 이유다.

**(다) 어촌6 좌표.** 주변 24셀에 뭍이 없어 시딩 자체가 스킵된다 — 51곳 중 **유일한 '이동 필요'** 자리다.

**(라) 야생 층 700ms 히치** — §5. 이 배치가 만든 것은 아니나 마을 완충이 줄면 커질 수 있다.

**(마) 한반도의 Q키 시세 패널이 프로토타입 20마을을 보여준다.** 이 세계에 없는 마을들의 시세다.
(a) 그대로 · (b) 시딩 마을 실제 시세로 교체 · (c) 패널을 감추고 `ENABLE_PROTO_ECON=0`.
(b)가 옳아 보이나 **UI 계약 변경**이라 임의로 하지 않았다.

**(바) 스폰 이동(v1 ② 이월).** 50곳 전환 시점에 정한다. 실측은 끝나 있다 — 현 스폰(벌말 광장)은
가장 가까운 마을이 212셀 밖이고, 농촌22 광장(base `{x:3084.8, y:5987.2}`)은 큰집 남쪽 7셀 마당으로
**통행 차단 건물 0 · 사유지 0**(실측)이며 첫 화면에 큰집과 움집이 함께 들어온다
(`/tmp/b15-shots/spawn.png`). 한 줄 변경이다.

---

## 8. 검증

| 하네스 | 결과 |
|---|---|
| `lab-wiring-check.js` | **실패 0 · 경고 0** |
| `test-alloy` / `test-valuechain` / `test-armor-cap` / `test-picker-tables` / `test-building-anchor` | **전부 PASS** |
| `test-granary-add` / `test-guild-treasury` / `test-mining` / `test-assay` | **전부 PASS** |
| `test-village-found.js` | **47 / 0** |
| `test-furnace.js` (노·숯가마 실서버 E2E) | **85 / 0** |
| `test-guest-identity.js` | **62 / 0** |
| **실클라 E2E** `e2e-village.js` | **32 / 0** |
| **실클라 E2E** `e2e-metallurgy.js` | **20 / 0** |
| **실클라 E2E** `e2e-guest-reconnect.js` | **40 / 0** |
| v2 CLI 회귀(30마을) | 인구 **2,616** · 무기Q **651** · 소멸 **0/30** — 기준선 동일 |
| **실지도 3시드 800일** | `b14` 대비 **3/3 시드 바이트 완전 동일** |

`sim/` 은 **한 파일도 안 건드렸다**(`git diff --stat -- sim/` 공집합). econ 무접촉이 정상이고,
접촉이 생겼다면 그 자체가 경고다.

**실화면 4장** (`/tmp/b15-shots/`, 진짜 클라 · 새 DB 첫 부팅 · 재시작 없음):

| 파일 | 내용 | 클라 메모리 건물 |
|---|---|---|
| `spawn.png` | 농촌22 광장 첫 화면 — 지붕 있는 큰집·움집·마당·밭·곳간 사다리 | 703채 |
| `hall.png` | 북쪽으로 걸어 큰집 정면 | 703채 |
| `legacy.png` | 구 레거시 농촌5 자리 — **빈 들판** | 0채 |
| `oldspawn.png` | 옛 스폰 벌말 광장 — **빈 들판**(§6-⑵ 의 `bld=5` 정체) | 0채 |

**이번 배치가 프로덕션 기본값을 바꾼 것 / 안 바꾼 것**

| | |
|---|---|
| 바꿨다 | `zone-config.hanbando.legacyVillages: false` (hanbando 만) · central canadia 프로토 시뮬 **지연 시작** |
| 바꾸지 않았다 | 시딩 선별(`FOOD_FLOOR`·`MIN_SPACING_PX`·`VILLAGE_MAX`) · `mainSquare` · `sim/` 전체 · v1 프로토 시뮬 기본 ON |
| 새 손잡이(전부 미설정이 채택값) | `SEED_ALL=1`(랩 전수 시딩) · `ENABLE_CANADIA_ECON`(1 즉시/0 정지/미설정 지연) · `ENABLE_PROTO_ECON=0` |

**새 도구**

| 파일 | 쓰임 |
|---|---|
| `scripts/plan-village-50.js` | 후보 51곳 땅값 전수 + 배제 원인(오라클 3판) + 간격 실측 → `/tmp/village50-plan.json` |
| `scripts/analyze-village50.js` | 전수 시딩 랩 덤프 → 마을별 생존·사인·처방 → `/tmp/village50-rx.json` |
| `scripts/cleanup-legacy.js` | 라이브 레거시 고아 행 청소(드라이런 기본·백업 선행·멱등·플레이어 무접촉) |
| `scripts/probe-spawn.js` | 임의 좌표 스폰 실화면 스크린샷(probe-roof 선례 · 래퍼 자가 생성) |
| `처방_50마을_지도오버레이.json` | 51행 전부 — 좌표(존 로컬 px·셀)·땅값·판정·처방·색. 지도 위 표시용 |

**`처방_50마을_지도오버레이.json` 읽는 법** — `villages[]` 각 항목:

```json
{ "name":"광산1", "type":"mining", "x":67840, "y":67753, "cx":2120, "cy":2117,
  "seedCls":"하한미달", "food":0.59, "need":1.41,
  "land":{"fertility":0.1,"water":0.08,"game":0.49,"stone":2.5,"wood":0.52,"ore":2.47,"vein":1.025},
  "verdict":"생존", "popAvg":34.3, "kmin":"prod", "rx":"그대로 OK", "color":"#4caf50" }
```

`x,y` 는 **존 로컬 px**(셀 = /32) — 지도 에디터와 같은 좌표계다.
색: 초록 `#4caf50` 그대로 OK · 주황 `#fb8c00` 좀비 1/3 · 빨강 `#e53935` 소멸 1/3.
`map-editor.html` 에 로더를 붙이는 것까지는 하지 않았다(디바이스 파일 편집이라 재민 확인 후가 맞다).
