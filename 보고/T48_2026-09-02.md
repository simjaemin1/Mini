# 보고 — T48 `test-psite-server` 진짜 빨강 수리 (2026-09-02)

> 베이스 `4d93a7ac` · 리베이스 0회 · **econ diff 0줄** · **클라 diff 0줄** · 3시드 기준선 **불변**.

---

## 0. 한 줄

카드가 지목한 결함(**차감 순서**)은 **없었다**. 그런데 카드가 시킨 **실서버 재현**이
같은 함수에서 **훨씬 나쁜 것**을 찾았다 — **집 의뢰 한 번이 존 서버 전체를 내리고 있었다.**

---

## 1. §0 실측 — 세 갈래로 갈렸다

T9 전수표는 러너 밖 하네스 31개 중 **유일한 진짜 빨강**으로 `test-psite-server` 를 지목하며
*"차감이 자리 확정 전에 일어난다"* 고 적었고, 카드는 그걸 **제품 결함 후보**로 넘겼다.

### ⓐ 하네스가 검사하는 것과 실패 assert 의 정확한 주장

`psite` = **플레이어가 마을 크루에게 의뢰하는 집터**(11차 T4). 하네스는 **소스 문자열**만 본다.
빨간 줄은 §⑤의 이것 하나:

```js
const iPay = fn.indexOf('player.inventory[it] -= amt');
chk(iPay > iPlace, '★차감은 자리 확정 뒤 — 배치 실패 시 재료가 사라지지 않는다');
```

### ⓑ 차감·확정의 현행 순서 — **옳다**

```js
// tryRequestVillageHouse (zone.js)
거리 게이트 → 마을 영토 게이트 → 재료 검사(lack) → lifeRequestPlayerSite(자리 확정) → consumeItem(차감)
   iLack=745        iPlace=958                                                          i=1203
```
`iPlace(958) < i(1203)`. **차감은 확정 뒤가 맞다.**

### ★왜 빨갰나 — **하네스가 낡았다. 그리고 실패 문구가 거짓말을 했다**

`f1659af9`(2026-08-30 원장 승격)가 `player.inventory[it] -= amt` → `consumeItem(player, it, amt)` 로
바꿨고 하네스는 **옛 문자열**을 찾고 있었다.

```
fn.indexOf('player.inventory[it] -= amt')  →  -1
-1 > 958                                   →  false   ⇒ 빨강
```

★★**`indexOf` 를 `>` 로 바로 비교하면 "못 찾음"과 "순서 틀림"이 같은 빨강이 된다.**
그리고 그 빨강에 달린 문구는 **있지도 않은 제품 결함**을 지목한다. T9 전수표는 그 문구를 그대로 읽었다.
(족보 (84)의 사촌: **"없다"는 두 가지다** — 여기선 *순서가 없는 것*과 *문자열이 없는 것*.)

**증거**: 옛 하네스를 **지금의(옳은) 제품 코드**에 그대로 돌리면 여전히 같은 빨강이 난다.
```
✗ ★차감은 자리 확정 뒤 — 배치 실패 시 재료가 사라지지 않는다
결과: FAIL (1건)
```

### ⓒ 실서버 재현 — ★★**진짜는 따로 있었다**

카드가 시킨 대로 실서버를 띄우고 **진짜 메시지 경로**로 의뢰했더니, 재료는 멀쩡한데
**함수가 던졌다**:

```
TypeError: Cannot read properties of null (reading 'HALL_CLEAR')
    at Object.lifeRequestPlayerSite (server/villages.js:3540:60)
    at tryRequestVillageHouse (server/zone.js:6821:25)
    at Object.handlePlayerInput (server/zone.js:3516:50)
```

---

## 2. ★★진짜 결함 — 플레이어 진입점만 lazy 가드가 없었다

`villages.js` 생활층은 `village-layout` 을 **lazy require** 한다(*"시뮬 off면 sim 모듈 무로드"* 설계 계약).
읽기 전에 `_lifeVL()` 를 불러야 하고, **NPC 쪽 진입점 셋은 전부 부르고 있었다**:

| 진입점 | 가드 |
|---|---|
| `_lifeHeadlessDay` | ✅ `_lifeVL();` — *"lazy 로드 방어(단독 호출 경로 — `5e8d5f5` 'is not defined' 클래스 재발 금지)"* |
| `_lifeDaily` | ✅ |
| `npcLifeTick` | ✅ |
| **`lifeRequestPlayerSite`** (플레이어) | ❌ **없다** |

**이 클래스는 한 번 물렸고(`5e8d5f5`), 그때 NPC 경로 셋에만 가드를 달았다.**
플레이어 경로는 **나중에 생겨서**(`12a2229a` · 11차 T4 집 건설) 그 교훈을 못 물려받았다.

### 폭발 반경 — 존 서버 전체

```
ws.on('message', raw => handlePlayerInput(player, raw))   ← try/catch 없음
process.on('uncaughtException', e => { console.error(...); process.exit(1); })
```

**실측(자식 프로세스 · 실소켓 · 실메시지 · 수리 전):**
```
→ 지급·텔레포트·집 의뢰를 한 묶음으로 @ 14032 78032
되돌아온 안내: 🌀 텔레포트 → (14032,78032)      ← 의뢰 응답이 없다(요청이 증발)
존 프로세스: ★죽었다 (code=1)
```
**수리 후 같은 시나리오:**
```
되돌아온 안내: 🌀 텔레포트 → (14032,78032) | 의뢰 불가 — 큰집 마당 침범(r<16.5)
존 프로세스: 살아 있다
```

### 창의 길이 — **3분 넘게 열려 있다**(추정 아님, 실측)

부팅 후 0.5초 간격으로 180초 폴링 — **끝까지 THROW**. `npcLifeTick` 은 **활성 NPC**만 도니까
**아무도 근처에 없는 마을은 lazy 가 안 채워진다.** `_lifeDaily` 는 게임일 경계(24분)라 그전까진 열려 있다.

★그리고 **재료 검사가 방패였다.** 재료 없는 사람은 그 앞에서 거절돼 못 닿는다 —
**기둥 6·서까래 8·이엉 8 을 다 모아 온 사람만** 존을 내린다.

---

## 3. 수리 — 가드를 하나 더 다는 대신 **잊을 수 있는 이름을 없앴다**

```diff
-let VillageLayout = null;
-const _lifeVL = () => VillageLayout || (VillageLayout = require('./village-layout'));
+let _vlMod = null;
+const _lifeVL = () => _vlMod || (_vlMod = require('./village-layout'));
```
생활층의 `VillageLayout.X` **18곳을 전부 `_lifeVL().X`** 로 바꿨다.
⇒ **맨 이름이 없다.** 새 진입점을 추가하며 가드를 잊는 일이 **구조적으로 불가능**하다.

★가드 한 줄(`_lifeVL();`)만 넣는 편이 diff 는 작다. 그걸 안 한 이유:
**이 클래스는 이미 두 번 났고**(`is not defined` → `null`), 둘 다 *"새 진입점이 가드를 물려받지 못했다"* 였다.
세 번째 진입점이 생길 때 또 같은 일이 난다. **가드는 기억에 기대고, 이름 제거는 안 기댄다.**

### ⚠`villages.js` 는 econ 이다 — 리네임임을 기준선이 증명한다

```
LAB_SEED=1020  인구 6280 · 소멸 0/51
LAB_SEED=7     인구 5993 · 소멸 0/51
LAB_SEED=42    인구 6072 · 소멸 0/51
```
**열 번째 재확인, 한 수도 안 움직였다.**

---

## 4. 곁다리 — 같은 족 전수 수색에서 나온 것

### ⓐ 건축 비용: **지불 5줄 / 환불 3줄** (`zone.js` · 수리함)

`_tryBuildAt` 은 `plank·wood·stone·seed·fiber` 다섯을 깎는데, 계단 실패 롤백은 **셋만** 되돌린다.
오늘 안 터지는 이유는 **계단 비용이 판자뿐**이기 때문 — *다른 곳의 우연*에 기댄 안전이다.
⇒ `_buildCostPay(player, cost, ±1)` 하나가 **부호만 바꿔 같은 표를 두 번 걷는다.**

### ⓑ 다른 lazy 둘 — 안전 확인

| lazy | 판정 |
|---|---|
| `PathCore` (villages.js:91) | ✅ 읽는 **그 자리**에서 `if (!PathCore) PathCore = require(...)` |
| `Events` (villages.js:4413) | ✅ `state.ledger` 가 `Events` 대입 **뒤에만** 서고, 플레이어 함수 전부가 `if (!state.ledger) return {err}` 로 게이트 |

**`village-layout` 만 이 족이었다.**

### ⓒ 다른 차감 지점 전수 — 전부 옳다

`doCraft` · `doCraftBuilding` · `doPlaceBuilding`(★`_tryBuildAt(skipCost:true)` **성공 시에만** 차감) ·
`doCraftItem` · `tryHutStart/Advance` · `_siteStart/_siteAdvance` · `doPreserve` · `doBoilSalt` — 검사 → 확정 → 차감.

---

## 5. 하네스 — 소스 검사는 **있는지**, 실행 검사는 **도는지**

### §⑤ 교정 — "못 찾음"과 "순서 틀림"을 **다른 문구로** 가른다

```js
const mPay = /consumeItem\(player,\s*it,\s*amt\)|player\.inventory\[it\]\s*-=\s*amt/.exec(fn);
chk(!!mPay,  `차감 호출을 **찾았다**(정본 이름이 바뀌면 여기부터 빨개진다) — ${mPay ? mPay[0] : '못 찾음'}`);
chk(!!mPay && mPay.index > iPlace, '★차감은 자리 확정 **뒤**(소스 순서) — 실행 증명은 ⑨');
```

### §⑨ 신설 — **실서버 실행**

진짜 존을 띄우고, 진짜 마을에, 진짜 메시지 경로(`handlePlayerInput`)로 의뢰한다.
**★생활틱을 일부러 안 돌린 채 첫 요청을 보낸다 — 그게 T48 이 잡은 창이다.**

* ⓐ 확정 실패(큰집 마당 침범) → **던지지 않는다** · **거절 사유를 돌려준다** · **재료 그대로**
* ⓑ 성사 → **선납분만 정확히** 빠진다(전제: 성사가 실제로 일어났는지 `lifeDebug().psite` 로 먼저 센다)
* ⓒ 중복 의뢰 → 거절 · **재료 그대로**
* ⓓ **구조**: 생활층에 lazy 모듈의 **맨 이름이 없다**(소스 수색 — 다시 만들면 빨개진다)

★개발 중 **내 프로브가 아무것도 안 하고 있었다** — `handlePlayerInput` 은 `JSON.parse(raw.toString())`,
즉 **문자열**을 받는데 객체를 넘겼다. 파싱이 조용히 실패해 통과처럼 보였다(족보 (57)). 하네스 주석에 남겼다.

### 대조군 (카드 요구: 종전 코드 빨강 / 새 코드 초록)

**새 하네스를 베이스 `4d93a7ac` 워크트리에 그대로 얹어** 돌렸다:

| | 종전 제품 코드 | 새 제품 코드 |
|---|---|---|
| ★★확정 실패해도 던지지 않는다 | ✗ `Cannot read properties of null (reading 'HALL_CLEAR')` | ✓ |
| ★거절 사유를 돌려준다 | ✗ `""` | ✓ `의뢰 불가 — 큰집 마당 침범(r<16.5)` |
| (전제) 유효 자리가 실제로 성사됐다 | ✗ | ✓ |
| ★★맨 이름이 남아 있지 않다 | ✗ | ✓ |
| **결과** | **FAIL (4건)** | **PASS** |

★그리고 **재료가 그대로다**는 종전에도 ✓ 였다 — 차감 순서는 처음부터 옳았다는 증거가 대조군에도 남는다.

### 러너 등록

`// @regress` 머리표 부여. 실행 시간 소스 검사 0.1초 → **실서버 포함 ~40초**.

### 통합 러너 — **66종 · 통과 62 · 실패 4** · 전수 귀속

| 하네스 | 판정 |
|---|---|
| `test-psite-server` | ✅ **통과** — 이 카드가 고친 것. 러너 등록 완료. |
| `test-site-memo` | **베이스 결함**(T7 소관 · T12 배치에서 베이스 워크트리로 확인 · 스킵 0·집터 중앙값) |
| `test-tick-slicer` | **베이스 결함**(T1 소관 · 성능 문턱) |
| `e2e-charsprite` | **베이스 결함**(31/4 · 남의 스프라이트가 A 화면에 0명) |
| `e2e-village` | **흔들림 — 베이스도 같은 분포**(아래) |

★그리고 종전 상시 빨강 둘이 사라졌다: `test-client-globals`(acorn 설치) · `e2e-waterperf`(T10 이 판정을
"비율의 비율"로 바꿨다). `e2e-mtfoot` 도 통과(T14 종결).

### ⚠`e2e-village` — 내 영역이고 실패 모양이 방금 고친 것과 같아서 전수 측정했다

러너에서 `✗ 거부 사유가 한국어 계약 메시지다 — (없음)` 하나로 빨개졌다.
**빈 거절 문구는 방금 고친 그 결함의 모양**(던져서 응답이 증발)이라 흔들림으로 넘기지 않고 **5회 A/B** 했다:

| | 결과 |
|---|---|
| 내 트리 | 32/0 · 32/0 · 31/1 · 32/0 · 32/0 |
| 베이스 `4d93a7ac` | 32/0 · **31/1** · 32/0 |
| 러너 안(내 트리) | 31/1 |
| 러너 **직후**(내 트리 · 기계 부하) | 19/6 |

**같은 분포다** ⇒ 내 변경과 무관한 **부하 의존 흔들림**(기지 목록에 이미 있다).
★부하가 크면 19/6 까지 벌어진다는 건 새 관측이다 — 러너 직후에 재실행하면 그 자체가 오염이다.

---

## 6. 회부 (구현 안 함)

* **★★플레이어 입력 예외의 폭발 반경 — 재민 판정.** 지금은 어떤 메시지 핸들러의 어떤 `TypeError` 든
  존 전체를 내린다. **근본은 고쳤지만 반경은 그대로다.**
  *"여기서 삼키면 반쯤 죽은 서버가 남는다 — 더 나쁘다"* 는 재민 확정 방침이라 **안 건드렸다**.
  중간 지대(입력 핸들러만 잡고 **크게** 로그 + 그 플레이어에게만 알림)가 필요한지는 판정 사항이다.
* **T41 `lifeSiteDirty` 접점** — `lifeRequestPlayerSite` 는 `_potSet` 을 줄인 뒤 `lifeSiteReset(vil)` 을
  부른다(T41 ① 규약). **그 줄은 손대지 않았다** — 리네임이 그 위를 지나가지도 않았다.

---

## 7. 족보 제안

* **(91) ★★★`indexOf` 결과를 `>` 로 바로 비교하지 마라.** −1(못 찾음)과 순서 오류가 **같은 빨강**이 되고,
  실패 문구는 **있지도 않은 결함**을 지목한다. T9 전수표가 그 문구를 그대로 믿어 "제품 결함 후보"로 올렸다.
  ⇒ **"찾았나"와 "순서가 맞나"를 두 판정으로 갈라라.** (족보 (84)의 하네스판.)
* **(92) ★★소스 검사는 규약이 *있는지*를 보고, 실행 검사는 그 규약이 *도는지*를 본다 — 둘은 대체재가 아니다.**
  `test-psite-server` 는 1년 가까이 소스만 봤고, 그동안 **존을 죽이는 코드가 그 함수 안에 있었다.**
* **(93) ★★가드는 기억에 기대고, 이름 제거는 안 기댄다.** 같은 lazy 클래스가 두 번 났다
  (`is not defined` → `null`) — 둘 다 *새 진입점이 가드를 물려받지 못해서*다.
  ⇒ **잊을 수 있는 이름 자체를 없애라.** 진입점은 앞으로도 늘어난다.
