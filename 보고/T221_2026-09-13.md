# 보고 — T221: **랩은 어느 손잡이를 켠 채 뜨나** — 랩 기본 ↔ 서버 기본 전수 감사 · 맞추기 · 판 이름 정정

> **한 줄 — 갈리는 자리 둘. 랩 기준선을 움직이는 것 하나. 맞춘 뒤 랩 기준선은 인구 721 / 758 / 293(8마을 3시드 800일)이다.**
> ① **`L_HAPPYWORK` 0.24** — 랩이 T157 을 **켠 채** 떴다(서버는 안 심는다). 이것이 랩 기준선을 **+17.6% / −54.6% / +19.1%**
> 로 흔들었다 — **부호까지 시드마다 갈린다.** → **맞췄다**(랩 부팅 기본 0 · 켜려면 토글이나 `window.L_HAPPYWORK`).
> ② **T135 나무 층**(`world.forageRealItems`·`forageTakeFn`) — **서버는 켜고 랩은 없다**. 이건 손잡이가 아니라 **층이 없는 것**이라
> 이 카드로는 못 맞춘다(랩은 제 숲 격자를 쓴다) — **회부**.
> 나머지 여섯(`L_ALLOC_REAL`·`L_STONEREAL`·`L_STONE_TRADE`·`L_TOOL_WEAR`·`L_LANDMUL`·`L_HUNTINCOME`·`L_WOODINCOME`)은 **처음부터 서버와 같았다**.
> 엔진 env 손잡이 **36개**는 랩에서 `process={env:{}}` shim 을 타므로 **전부 미설정 기본** = 서버 기본 — 구조적으로 안 갈린다.
>
> **한 줄 2 — 이 하나가 T196 랩 표를 잘못 읽게 했다.** 계측기가 "손잡이 전부 미설정"이라고 찍어도 랩이 스스로 켜고 떴다.
> 그래서 T157·T196 보고의 랩 표 머리에 **판 이름 한 줄**을 달았다(수 무변 · 문서만). T209 보고는 이미 그 줄을 갖고 있다.

* 베이스 **`c7fe4d27`**(main · 09-13) · 가지 `batch/lab-defaults-0912` → ff → main
* **랩 파일만 만졌다** — `git diff origin/main -- sim/ server/ public/` 이 **빈 칸**이다 ⇒ 서버는 비트 동일(3시드 A/B 갈음 · 공통.md §2 ①)

---

## 1. §0-ⓐ 전수 — 랩이 세계에 심는 것 ↔ 서버가 심는 것

**서버가 세계를 세울 때**(`server/villages.js:2519~2529`): `createWorldV2({seed, villageCount, picker:'rational', infoRange:5000, raidPer100:0.005})`
+ `world.villages=[]` · `world.events=[]` · `require('./trees').attachToWorld(world)` — **주입 함수는 나무 층 둘뿐**이다
(`server/trees.js:272~273` `forageRealItems`·`forageTakeFn` · `T135_TREES` 기본 켬).

| # | 자리(주입) | 랩 부팅 기본 | 줄 | 서버 기본 | 갈림 |
|---|---|---|---|---|---|
| ① | `world.happyWorkW`(T157) | **0.24 — 켬** (`L_HAPPYWORK_BASE`) | `전쟁실험실.html:10027~10028` | **없음(끔)** | **★갈린다 → 맞췄다** |
| ② | `world.forageRealItems`·`forageTakeFn`(T135) | **없음**(랩은 제 숲 격자) | — | **켬**(`attachToWorld`) | **★갈린다 — 층이 없다 · 회부** |
| ③ | `world.allocFn`(T161·T164) | `L_ALLOC_REAL=0` → 안 심음 | `:11129` | 없음 | 같다 |
| ④ | `world.stoneBudgetFn`(T163) | `L_STONEREAL=0` | `:11214` | 없음 | 같다 |
| ⑤ | `world.returnPullFn`(T173) | `L_STONE_TRADE=0` | `:11258` | 없음 | 같다 |
| ⑥ | `world.toolWearMul`(T180) | `L_TOOL_WEAR=1` → 문을 안 엶 | `:11296` | 없음 | 같다 |
| ⑦ | `world.huntIncomeFn`(T154) | `L_HUNTINCOME='abstract'` → `undefined` | `:9570` | 없음 | 같다 |
| ⑧ | `world.woodIncomeFn`(T166) | `L_WOODINCOME='abstract'` → `undefined` | `:9510` | 없음 | 같다 |
| ⑨ | `world.priceFn` | 안 심음(읽기만) | `:11490` | 없음 | 같다 |
| ⑩ | 경작칸 `L_LANDMUL`/`L_LANDNEED` | `1` / `LAND_BASE×1 = 12` | `:9930~9932` | 정본 `LAND_NEED=12` | 같다(T100 절이 이미 대조) |
| ⑪ | 도적·전쟁 훅(`banditRouteRisk`·`banditGang`·`onBanditLoot`·`_banditStats`·`_warStats`) | **항상 심음** | `:10034~10036` 외 | **서버엔 그 층이 없다** | 랩 전용 층(도적 0이면 0 반환 — 설계 주석) |
| ⑫ | 엔진 env 손잡이 **36개**(`T100_GARDEN`·`T17_*`·`T73_*`·`T86_DIET`·`STONE_NET` …) | 번들 shim `process={env:{}}` ⇒ **전부 미설정 기본** | `economy-engine.browser.js` 머리 | 미설정 기본 | 같다(구조적 보장) |
| ⑬ | `마을실험실.html` | `happyWorkW` 주입 **없음**(이름은 인라인 엔진 주석에만) | — | 없음 | 같다 |

## 2. §0-ⓑ 갈리는 것의 값 — 랩 8마을 3시드 800일

`L_HAPPYWORK`(①) — **{랩 기본 0.24 / 서버 기본 0}** 두 팔(같은 판 `c7fe4d27` · 같은 계측기):

| 시드 | 랩 기본(켬) 인구 | 서버 기본(끔) 인구 | Δ(랩 기본이 만든 왜곡) | 살아있는 마을 | 행복 중앙(최종) | 행복<0.5 곳 |
|---|---|---|---|---|---|---|
| 1020 | 848 | **721** | **+17.6%** | 7 ↔ 6 | 0.603 ↔ 0.580 | 3 ↔ 1 |
| 7 | 344 | **758** | **−54.6%** | 5 ↔ 7 | 0.172 ↔ 0.626 | 6 ↔ 3 |
| 42 | 349 | **293** | **+19.1%** | 6 ↔ 6 | 0.133 ↔ 0.197 | 8 ↔ 8 |

* **부호가 시드마다 갈린다** — 랩 기준선이 한 방향으로 치우친 게 아니라 **판마다 다르게** 어긋나 있었다.
* ②(T135 나무 층)는 **랩에서 A/B 를 못 한다** — 끌 손잡이가 아니라 **없는 층**이라서다. 서버에서 읽으려면 `T135_TREES=0` 팔이고,
  그건 "되돌리는 스위치 다섯"(인계/공통 셋째 판 절)의 한 칸이다 — **이 카드 밖**(회부).
* ③~⑩ 은 갈리지 않으므로 두 팔이 같은 세계다(A/B 할 것이 없다).

## 3. §0-ⓒ 맞춘 뒤 — 기준선 · 하네스 · 판 이름

**맞추기(랩 기본값만 · 로직 0)** — `lab/전쟁실험실.html` 두 자리:

```js
const L_HAPPYWORK_BASE=0.24;   // ★출처 유도값 — **켤 때의 값**이지 기본값이 아니다
let L_HAPPYWORK=(typeof window!=='undefined'&&window.L_HAPPYWORK!=null)?+window.L_HAPPYWORK:0;   // ★[T221] 랩 부팅 기본 = 서버 기본
```
+ 눈에 보이는 토글도 같은 자리에서 뜬다(`끔` 버튼이 `class="on"` · 표시 `행복→작업량 H=0 (끔)`).
`L_HAPPYWORK_BASE` 는 **그대로 남긴다** — 토글·A/B 가 켤 때 쓰는 그 수이고, `test-lab-happywork` ②가 출처에서 다시 유도해 대조한다.

**맞춘 뒤 랩 기준선(8마을 · 800일 · 손잡이 전부 미설정 = 이제 참말)**: 인구 **721 / 758 / 293** · 살아있는 마을 6 / 7 / 6 ·
행복 최종일 중앙 0.580 / 0.626 / 0.197.

**하네스** — `lab-wiring-check` 에 **[H] 절** 신설: 랩 부팅 기본을 **손잡이 여덟 개 전수**로 읽어 서버 기본과 대조하고,
켠 채 뜨는 변조판(`L_HAPPYWORK=0.24`)을 만들어 **실제로 빨개지는지**까지 본다(자명 통과 금지).
서버가 세계에 직접 다는 주입이 늘면 경고한다(`world.*=` 전수 ↔ 허용 목록 셋).

**판 이름 정정(문서만 · 수 무변)** — `보고/T157_2026-09-07.md` 랩 표 머리 · `보고/T196_2026-09-12.md` 항별 분포 표 머리에
"이 표의 랩 판은 **T157 켬**" 한 줄씩. `보고/T209`(가지 `batch/lab-happy-floor-0912`)는 이미 그 줄을 갖고 있어 손대지 않았다(무접촉 규약).

## 4. 검증 · 해시

| 검사 | 결과 |
|---|---|
| `lab-wiring-check` | **실패 0 · 경고 0** — A~G + **[H] 신설**(손잡이 8 전수 · 자명 통과 금지 · 서버 주입 목록 감시) |
| `test-lab-happywork` | **32 PASS / 0 FAIL**(토글·훅·3사본 절 그대로 — 픽스처는 명시로 켜므로 기본이 바뀌어도 초록) |
| `test-lab-alloc` | 19 PASS / 0 FAIL |
| `test-lab-stone` | 통과 67 · 실패 0 |
| `test-lab-trees` | 52 PASS / 0 FAIL |
| `build-econ-bundle --check` | **8 일치 / 0 불일치** · `inline-engine --check` 랩 둘 다 **최신**(인라인 엔진 블록 무접촉) |
| **서버 비트 동일** | `git diff origin/main -- sim/ server/ public/` **빈 칸** · `lab-modules --diff` **교집합 0개** ⇒ 3시드 A/B 갈음(공통.md §2 ①) |
| 랩 계측기 | `scripts/t196-happy.js` 랩 팔 주입을 `T196_HW` **설정 여부**로 바꿨다(0 을 명시로 넣을 수 있게) — 계측기, 러너 밖 |
| 러너 전수 | 안 돌렸다(카드 지시) · 실기 0 |
| 접점 | `L_HAPPYWORK`·`L_ALLOC_REAL`·`L_STONEREAL`·`L_STONE_TRADE`·`L_TOOL_WEAR`·`L_LANDMUL`·`L_HUNTINCOME`·`L_WOODINCOME` · `forageTakeFn`·`attachToWorld`·`createWorldV2` |

| 항목 | 값 |
|---|---|
| **측정 베이스** | **`c7fe4d27`** (main · 09-13) — 두 팔 여섯 판 전부 이 판에서 쟀다(족보 141·142) |
| 착지 베이스 | **`2994088c`** — 재는 동안 들어온 T217 은 문서만(리베이스 1회 · 겹침 0) |
| 브랜치 | `batch/lab-defaults-0912` → **ff → main** |
| force · 리베이스 | **0회 · 1회** |

## 5. 회부

* **T135 나무 층이 랩에 없다(새 회부 · ②)** — 서버 기준선은 나무 층이 **켜진** 세계이고 랩 기준선은 **없는** 세계다.
  랩으로 채집·열매·먹이사슬을 판정하면 그 차이만큼 틀린다. 랩에 층을 다는 일(랩 숲 격자 ↔ `forageTakeFn` 계약)은 **카드 하나**다.
* **랩 전용 층(도적·전쟁)** — 서버엔 없다. 지금은 도적 0 이면 무해하지만, 랩 표를 서버 판정에 쓸 때는 **그 층이 켜져 있다**는 것을 적어야 한다.
* T165+하한 1 켜기(#13 ② · PM 권고) · 문턱 0.5(#13 ③) · 조리식(#21) · 광부 ⓑ · 광산 궤적 · MSY · 덤불 · 목재 분할 — 그대로.
