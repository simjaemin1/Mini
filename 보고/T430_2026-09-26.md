# T430 — BGM 정본 한 장 · `daegeum-transitions` 권리 표 · 브금판 한 자리 [승인 대기 · T430] (2026-09-26 · 세션8/ART)
**한 줄 답:** `README-BGM.md` 를 정본 한 장으로 다시 썼다. 게임은 `bgm.js` 절차 엔진을 틀고 13곡 파일은 판만 튼다 · 정본 코드는 2/13 · 굽는 명령은 한 줄이다. 옛 문서는 아래에 그대로 두었다. 배포 13곡 해시는 **26/26 전후 같다**. 권리 표는 **25행**이고 판정 칸은 비워 두었다. 브금판 맨 위에 「재민 판정 세 판 — 한 자리」를 세웠다(#66 2줄 · #72 11줄을 옮기기만 · 새 줄 0). ⚠**#70 은 판에 줄이 없다** — 카드 전제와 다르다. T391 이 올린 두 줄은 sigim 켬/끔이고, 규칙을 켠 판 소리는 어디에도 없다(회부).
가지 `batch/bgm-readme-0926` = `952a86a` · 베이스 `527f655`(main) · force 0 · main 은 PM
**배포 13곡 바이트 무변(26/26) · `bgm.js` 무접촉 · `sfx/**` 무접촉 · `tools/` 무접촉 · 새 소리 0 · 새 줄 0 · 판정 0** · 바꾼 파일 둘 = `README-BGM.md` · `bgm-board.html`

| 넷 | 결론(실측) |
|---|---|
| ⓐ README 한 장 | 새 부분 66줄(한눈에 · 입력 · 굽는 명령 한 줄 · 13곡 해시 표 · 다시 굽기 조건 · 합성음 목록 · 표기 · 보관소·시험). 옛 문서 62줄은 `## 옛 문서` 아래 **글 그대로**(제목 층만 두 칸 내림). `test-audio ⑨k·⑨l` 초록(목록 줄 = 새 장의 것 · 180/0 = 베이스 180) |
| ⓑ 권리 표 | `tools/daegeum-transitions` **24 파일** + `ddsp-gugak-public-runtime` 1 = **25행**. 24 파일 모두 GPT 코드(첫 커밋 `462b4ac` 스쿼시) · 라이선스 표기 0 · 게임 참조 0(**미반입**). 원음을 만지는 것은 셋이다(받기 `fetch` · 읽고 조각 쓰기 `build` · 조각 뽑기 `phrase_span_audition`). 후보 문서가 **이름으로** 부르는 파일 0 · **뜻으로** 닿는 파일 7(08 ×5 · 04 ×1 · 02 ×1 — 그중 `build` 는 08·02 둘 다). 판정 칸 25 전부 빈 채 |
| ⓒ 브금판 한 자리 | 맨 위 표 `#judge`: **#66 2줄 → #72 11줄**(곡 이름 차례). 아래 표: 13곡 「파일」 + sigim 2줄 = 15줄. **모두 28줄 그대로**. 판정 열쇠(`ab:*` · `t401:*`)가 그대로라 적어 둔 답이 그대로 뜬다(헤드리스: ○ 하나·메모 하나 적고 다시 열어 둘 다 남음 · 「표 복사」에 둘 다 · 페이지 오류 0). 스크린샷 `~/Mini/산그림/T430_브금판.png` |
| ⓓ #70 | 판에 줄 **0** · 청취본 **0**(`~/Mini/_bgm_ab/` 에 T353 2장 · T391 sigim 4장 · T401 44장뿐). 새 줄·새 소리를 안 만들고 표 머리에 그 사실을 적었다 ⇒ 회부(#70 청취본 — T391 레시피로 두 곡 × 켬 판을 구우면 된다) |

게이트 `test-bgm-phrase` 16/0 · `e2e-bgm-phrase` 12/0 · `test-daegeum-transition-bank` 8/0 · `test-assets-audit` PASS · `--selftest` PASS · `test-client-globals` 26/0 · `test-harness-lint` 61/0 · `lab-wiring-check` 실패 0 · 접점 `test-audio` 180/0 · `e2e-audio-probe` 76/0 · `test-render-score-articulation` 14/0 · `test-daegeum-entry-mode.py` 7/0(T391 이 복원한 `samples_jdae` 로)

---

## §ⓐ `README-BGM.md` — 정본 한 장

| 절 | 적은 것 | 근거 |
|---|---|---|
| 한눈에 | 게임이 트는 것 = **`bgm.js` 절차 엔진**(파일 0장 · `48-a-audio.js`) — 13곡 파일은 게임이 **안 튼다**(브금판·소리판만) · 정본 코드 2/13 · 11곡은 "정본 코드가 없는 굽기 결과물"(한 문장) | `48-a-audio.js` 머리말 · T391 · T401 |
| 입력 | 뱅크 `bk_*` 12묶음(맥 · 격리 복원 · 대금 4묶음은 `bgm-samples --verify` 121파일) · **12:22판** `sampler.py` 스캔(레포 13:01판은 다른 조각) · 악보·굽기 코드 다섯 · `score-expression` 은 **굽기 입력이 아니다** | T391 · T422 |
| 굽는 명령 | `python3 render_score.py && python3 render_jeongak.py` + ffmpeg 두 줄(q4 · aac 128k · numpy 2.3+) · 기본값 = T391 복원값 · GPT 규칙 = 손잡이 `ARTICULATION_CONTRACT`(#70) · 옛 `render_samples.py` 는 TypeError | T391 |
| 출력 | 13곡 × `.ogg`·`.m4a` sha256 앞 16자 · 굽힌 코드 · "다시 구우면" 칸(○ 2 · ✗ 11) | 이번 잰 해시 = T391 보고의 두 곡 해시와 같다 |
| 다시 굽기 | **재민 #72 답 전엔 13곡 무변** · 두 곡은 같은 판 두 번의 차 안일 때만 — Python 굽기에서 그 차는 **0**(바이트 동일) · 브라우저 엔진은 떨림 바닥(T411) · 해시가 다른 새 소리는 표로만 | T391 · T411 |
| 합성음 목록 | 목록 줄 그대로(`장구 채편`) · 궁편은 샘플(T411) · 다섯 칸 미정정 경고 | T411 |
| 표기 | `국악기 음원 제공 — 국립국악원 (공공누리 제1유형)` | 옛 문서 |
| 보관소 · 시험 | `_rnd_archive` 배포 밖(`COPY server public sim` · `git archive` 0 · `test-assets-audit ⑩`) · 시험 넷 + `test-audio ⑨k·⑨l` | T422 |

* **한 장 = 새 부분 66줄.** 13곡 표가 14줄을 먹는다. 옛 문서(62줄)는 그 아래 `## 옛 문서 (T430 이전 — 지우지 않았다 · 제목 층만 내렸다)` 로 **글 그대로** 남겼다.
* ⚠`test-audio ⑨k` 는 파일에서 **처음** 나오는 "아직 합성음인 악기"를 읽는다. 새 장의 절이 앞에 오므로 옛 문서 쪽 목록은 안 읽힌다. 두 목록은 지금 같은 글자다.
* ⚠`48-a-audio.js` 머리말은 13곡이 "`compose.py` 로 뽑아 둔 것"이라 적는다. 그 말은 틀렸다(2곡은 `render_score`·`render_jeongak`, 11곡은 07-30 `tracks2.py` — T391·T401). 그런데 제품 파일이라 **안 고쳤다**(코드 0 · 회부).

## §ⓑ `daegeum-transitions` 권리 표 (파일마다 · 판정 칸 빈 채)

**공통**:
- **출처** — 24 파일 모두 GPT 가 만든 코드·문서다. 첫 커밋은 `462b4ac`(`codex/bgm-rnd-01` 86커밋 스쿼시 — 원래 커밋은 레포 밖)이고, 녹음·합성 오디오 파일은 이 폴더에 **0** 이다(`.gitignore`).
- **라이선스** — 파일 머리 표기 0 · 폴더 README 표기 0 · 레포 뿌리 `LICENSE` 없음 ⇒ 전부 **"없음"**.
- **게임 안** — `bgm.js`·`bgm-loops.js`·`public/client`·`sfx/**`·`server` 에서 파일 이름·폴더 이름 grep **0** ⇒ 전부 **미반입**. 레포 안에서 읽는 것은 하네스 하나다(`scripts/test-daegeum-transition-bank.py` 가 스키마·`build.py` 글을 읽는다).
- **데이터 권리**(표의 "만지는 데이터"가 가리키는 것) — 국립국악원 「확장 다운로드」 대금산조 192(CREDITS §2 의 「단음」과 다른 상품). `ngc-rights-audit` 규칙은 이렇다: 1유형 표기는 표기 조건부 상업 이용·비ML 가공을 허용하고, AI 학습은 AI 유형 표기나 서면 허락이 없으면 막힌다. 이 집은 그 감사를 한 번도 못 돌렸다(목록·네트워크 — T411).

| # | 파일 | 무엇 | 만지는 데이터 | 후보 문서(02·04·08)가 말하나 | 재민 판정(들인다 / 참고 / 버린다) |
|---|---|---|---|---|---|
| 1 | `README.md` | 도구 안내 · 명령 예 | 문서(원음 받기·굽기 명령을 적는다) | — | |
| 2 | `build.py` | R&D-06 전이 **후보** bank 빌더 | **원음 WAV 를 읽고** 특징 캐시·후보·**검토용 WAV 조각**(파생물)을 쓴다 | 08 뜻 — 우선순위 3 *"대금 control cache"* · 02 뜻 — *"긴 산조 원본에서 실제 연속 전이를 찾아 청취·라벨"* | |
| 3 | `exhaustive_score_span_search.py` | 악보 하나 ↔ 캐시 전 스팬 전수 | 특징 캐시 + 레포 계획(WAV 안 연다) | — | |
| 4 | `fetch_ngc_extended_daegeum.py` | 국악원 확장 목록·원음 **내려받기**(명시 ID · 한 번 30 · `--all` 없음) | **원음 받기**(네트워크) · 받은 파일마다 KOGL 표기를 목록에 적는다 | — (참고 `NGC_EXTENDED_INGEST`·06) | |
| 5 | `native_wav.py` | WAV 를 원래 시간축 그대로 읽는 라이브러리 | WAV 머리·표본 | 08 뜻 — 1단계 *"native sample rate/channels/frame count"* | |
| 6 | `phrase_boundary_triage.py` | 궤적 하나 둘레의 악구 경계 좌표 | 특징 캐시 좌표만 | **08 뜻 — 우선순위 2** *"phrase boundary triage + source-led stem"* | |
| 7 | `phrase_pool.py` | source-led 원음 스팬 목록(보고만) | 특징 NPZ + 국악원 목록(WAV 안 연다) | — | |
| 8 | `phrase_span_audition.py` | 고른 경로를 **원음 WAV 조각 하나**로(A 바이트 그대로 · P Rubber Band 합성) | **원음 조각을 쓴다**(파생물) | — (참고 07) | |
| 9 | `score_phrase_compatibility.py` | 악보 계획 ↔ 스팬 적합도 | 계획 + 특징 캐시(WAV 안 연다) | — | |
| 10 | `source_led_contour.py` | 한 스팬의 윤곽 뼈대(반주 설계용) | 특징 캐시(WAV 안 연다) | **08 뜻 — 우선순위 3** *"source-led contour scaffold"* | |
| 11 | `target_audition.py` | 악보 목표별 후보 검토 큐 | bundle 후보 + 원음(검토 조각) | 04 뜻 — *"대금부터 target 음역의 breath / tongue / stable body"* | |
| 12 | `trajectory_retrieval.py` | 같은 녹음 안 연속 F0 궤적 검색 | 특징 캐시(WAV 안 연다) | 08 뜻 — *"raw phrase retrieval"* | |
| 13 | `transition-bank.schema.json` | 전이 bank 계약 | 데이터 0 | — (참고 06) | |
| 14 | `test_build.py` | 자기 시험 | 합성 픽스처(녹음 0) | — | |
| 15 | `test_exhaustive_score_span_search.py` | 〃 | 〃 | — | |
| 16 | `test_fetch_ngc_extended_daegeum.py` | 〃(네트워크 0 · 가짜 응답) | 〃 | — | |
| 17 | `test_native_wav.py` | 〃 | 〃 | — | |
| 18 | `test_phrase_boundary_triage.py` | 〃 | 〃 | — | |
| 19 | `test_phrase_pool.py` | 〃 | 〃 | — | |
| 20 | `test_phrase_span_audition.py` | 〃 | 〃 | — | |
| 21 | `test_score_phrase_compatibility.py` | 〃 | 〃 | — | |
| 22 | `test_source_led_contour.py` | 〃 | 〃 | — | |
| 23 | `test_target_audition.py` | 〃 | 〃 | — | |
| 24 | `test_trajectory_retrieval.py` | 〃 | 〃 | — | |
| 25 | **`ddsp-gugak-public-runtime`**(폴더 · 한 행) | 공개 DDSP-Gugak 대금 체크포인트로 계획 렌더(R&D) | 체크포인트 17.4 MB · 두 저장소 checkout — **레포 밖** | —(설계 0 · 보고 착지메모) | |

**25행 라이선스 칸**(인용 · 15단어 안):
- 코드: README *"Both source repositories have verified MIT `LICENSE` files."*
- 체크포인트: 런타임 :2746 *"no explicit separate checkpoint/game/distribution license is established by this tool"*
- ⚠`LICENSE` 파일 자체는 레포에 **없다**. 런타임이 sha256 만 고정한다(DDSP-Gugak `6b3a4d6a…` · ddsp-pytorch `b215a267…` — :42·:46). DDSP-Gugak 저장소 URL 은 착지물에 없다(T386 회부 5).

* "뜻으로"는 문서 글과 파일 docstring 이 같은 일을 말한다는 뜻이다. 파일 이름으로 부르는 곳은 후보 문서 셋에 0 이고 참고 문서에만 있다(06·07·NGC·SOURCE_LED·triage).
* 판정 칸은 **25 전부 빈 채**다(판정 0). 들이면 원음 조각(2·8)과 받기(4)가 권리 판단의 몸통이다.

## §ⓒ 브금판 — 한 자리

### ⓒ-1 세 판이 어디 흩어져 있었나 (T430 전)

| 판정 | 줄 | 자리(T430 전) | 소리 |
|---|---|---|---|
| **#66** 프레이즈 A/B | 「legacy」「phrase」 **2줄**(T386) | 28줄 표 한가운데 · `village_day_ari` 「파일」 바로 아래 | `renderAriPreview` 가 그 자리에서 굽는다(파일 0) |
| **#70** 무표기 재발음 A/B | **0줄** | — | **없다** — 규칙을 켠 판(`ARTICULATION_CONTRACT = True`) 소리가 어디에도 없다 |
| **#72** 11곡 | 「배포」「07-31」「지금」 **11줄**(T401) | 11곡 「파일」 줄마다 그 아래에 한 줄씩 흩어져 있었다 | 배포 = 디스크 곡 · 07-31·지금 = `~/Mini/_bgm_ab/T401/` 을 골라 쓰는 청취본 |
| (판정 번호 없음) sigim 켬/끔 | 2줄(T391 · T353 회부 ⓑ) | `village_day_ari` 아래 | `~/Mini/_bgm_ab/T391/` 청취본 — **#70 이 아니다** |

### ⓒ-2 모은 뒤

| 표 | 줄 | 차례 |
|---|---|---|
| **재민 판정 세 판 — 한 자리**(`#judge` · 맨 위) | **13** = #66 2 + #72 11 | #66 legacy → phrase → #72 곡 이름 차례(battle_amb … village_night_trad) · 머리에 "#70 0줄(판 없음)" |
| 13곡 파일 · sigim 청취본(`#bgm`) | **15** = 파일 13 + sigim 2 | 곡 이름 차례(그대로) |
| 합 | **28**(T401 과 같다) | 새 줄 0 |

* **바꾼 코드는 자리뿐이다.** `draw()` 에서 #66·#72 줄을 `tb.appendChild` 대신 두 배열에 담고, 끝에 `#judge` 에 붙인다(`j66.concat(j72)`). 줄을 만드는 코드·단추·재생·잰 값·판정 열쇠는 전부 그대로다. #66 줄 이름 앞에 `#66 · ` 를 붙였다. `#72 청취본 고르기` 막대와 #66 설명문은 그 표 위로 옮겼다. 머리말·소개 문단에 T430 한 문단씩 더했다. `bgm-board.html` +29 −6.
* **답이 남는 자리**(있는 문법 그대로): ○/✗·메모는 `localStorage` `durango.bgmboard.v1` 에 남는다(이 브라우저에만 · 열쇠 `ab:legacy`·`ab:phrase`·`t401:<곡>:0731|now|memo`). 판정의 정본은 맨 아래 「표 복사」(markdown)다.
* **헤드리스 확인**(Playwright · Chromium · 1320×1000):
  - 한 자리 13줄 · 아래 15줄 — 첫 두 줄 `#66 · village_day_ari · legacy|phrase` · 셋째 `battle_amb · #72`
  - ○ 하나 + 메모 하나를 적고 다시 열면 **둘 다 남는다**(열쇠 `t401:battle_amb:0731` · `ab:legacy`)
  - 「표 복사」에 둘 다 나온다 · 페이지 오류 **0**
  - 스크린샷 `~/Mini/산그림/T430_브금판.png`
* 실기(재민 배포 뒤): `<호스트>/bgm-board.html` 을 열고 맨 위 한 자리에서 위에서 아래로 — #66 두 줄(「소리 깨우기」 뒤 legacy/phrase) → #72 11줄(「청취본 폴더 고르기」로 `~/Mini/_bgm_ab/T401/` 을 한 번 고른 뒤).

## §카드 전제 대조

| 카드가 적은 것 | 실측 |
|---|---|
| "#66 · #70 · #72 행이 **이미 있다**(T401·T411)" | #66 = T386 · #72 = T401 은 있다. **#70 은 없다** — T391 이 올린 A/B 두 줄은 `sigim()` 켬/끔(T391 카드 ③ⓑ · T353 회부)이고, T411 은 판에 줄을 안 올렸다. 새 줄 0 이라 #70 은 "판 없음"으로 표 머리에 적었다(회부) |
| "입력 = `bgm-samples` 121파일 · `score-expression`" | `bgm-samples` 는 입력 쪽 도구다(대금 4묶음 · 121파일). **`score-expression` 은 굽기 입력이 아니다** — 두 곡은 `score_village_day.py` 악보로 굽고, `score-expression` 계획은 청취 묶음·R&D 렌더러의 입력이다. 한 장에 그렇게 적었다 |
| "굽는 명령 **하나**" | 스크립트는 둘이다(`render_score` · `render_jeongak`). 새 코드 0 이라 감싸는 스크립트를 안 만들고 **한 줄**(`&&`)로 적었다. 11곡은 명령이 없다(코드 없음) |
| "`bgm.js` 가 읽는 자리" | **`bgm.js` 는 13곡을 안 읽는다**(절차 엔진 · 파일 0장). 13곡을 트는 곳은 브금판(`bgm-loops.js`)·소리판뿐이다. 한 장의 첫 줄이 그 말이다 |
| "문턱 = T411 같은 판 두 번의 차" | Python 굽기에서 그 차는 **0**(바이트 동일 · T391)이다. T411 의 바닥은 브라우저 엔진 것이다(RMS −93 ~ −85 dB). 두 줄 다 적었다 |
| "`test-audio`" (검증 목록 밖) | README 를 읽는 하네스라 접점으로 돌렸다 — 베이스 180 → 뒤 **180** |

## §검증

* **13곡 해시 전/후** — `public/assets/audio/bgm/*.ogg|*.m4a` 26장 sha256 이 **26/26 같다**(README 표의 16자와 같은 값 · 두 곡은 T391 보고 값과 같다).
* 바꾼 파일은 둘이다 — `README-BGM.md` +73 −8 · `bgm-board.html` +29 −6. `bgm.js`·`bgm-loops.js`·`sfx/**`·`tools/`·`public/client/**`·`render-meta.json` diff **0**.
* 게이트 — `test-bgm-phrase` 16/0 · `e2e-bgm-phrase` 12/0 · `test-daegeum-transition-bank` 8/0 · `test-assets-audit` PASS(⑧⑨⑩) · `--selftest` PASS(3건) · `test-client-globals` 26/0 · `test-harness-lint` 61/0 · `lab-wiring-check` 실패 0 · 경고 0.
* 접점 — `test-audio` 180/0(⑨k·⑨l — 새 장의 목록 줄) · `e2e-audio-probe` 76/0(소리판 BGM 행 = `tracks` 13) · `test-render-score-articulation` 14/0 · `test-daegeum-entry-mode.py` 7/0(T391 스크래치의 `samples_jdae` 로 · 없으면 `--bank` 가 없어 멈추는 하네스다). `render-bgm-phrase-ab.js`·`t386-bgm-hash.js`·`t411-oac-floor.js` 는 러너 밖 도구다(무변).
* 브금판 헤드리스 — §ⓒ-2.
* §2 ① 3시드 A/B — diff ∩ 랩 적재 목록 = ∅(문서·판 HTML) ⇒ 해당 없음. 랩 배선은 `lab-wiring-check` 실패 0.
* **리베이스 한 번** — 일하는 사이 main 이 `9970bc5` → `527f655`(T410·T421·T425 착지 · 17 파일)로 곧게 나아갔다. 겹치는 파일 0 이라 코드 커밋 하나를 그 위로 옮겼다(patch-id 같다 `d7b83fb`). 옮긴 뒤 위 게이트 8 · 접점 넷 · 해시 26/26 · 브금판 헤드리스를 **다시 돌려 같은 값**이다.

## §재현

```sh
(cd public/assets/audio/bgm && sha256sum *.ogg *.m4a)                   # 26줄 — README 표의 앞 16자
node scripts/test-audio.js | grep -E "⑨k|⑨l"                            # 새 장의 목록 줄
for f in $(git ls-files tools/daegeum-transitions); do                   # 권리 표 — 첫 커밋 · 게임 참조
  echo "$f $(git log --follow --diff-filter=A --format=%h -- $f | tail -1) \
  $(grep -rlF "$(basename $f)" public/assets/audio/bgm/bgm.js public/client public/assets/sfx server | wc -l)"; done
# 브금판: 정적 서버로 public/ 를 열고 /bgm-board.html — 맨 위 표 13줄 · 아래 15줄
```

## §회부

1. **#66 · #72**(재민 · 브금판 맨 위 한 자리) — 배포 뒤 실기.
2. **#70 청취본**(PM · 다음 카드) — 판에 줄이 없다. T391 레시피로 두 곡을 `ARTICULATION_CONTRACT = True` 로 굽고, T391 문법(로컬 청취본 · sha256 확인)으로 판에 두 줄을 올리면 된다. 이 카드는 새 줄·새 소리 0 이라 안 했다.
3. **`daegeum-transitions` 판정**(재민 · §ⓑ 25행 — 판정 칸 빈 채) · 데이터 권리 감사(`ngc-rights-audit`)는 목록·네트워크가 있는 자리에서만 돈다.
4. **설계 11장 후보 3 판정**(재민·PM) — T422 §② + 이 카드 §ⓑ.
5. **`48-a-audio.js` 머리말의 "`compose.py` 로 뽑아 둔 것"**(제품 파일 · 주석 한 줄 · 카드로) — 2곡은 `render_score`·`render_jeongak`, 11곡은 07-30 `tracks2.py`.
6. **#64 ②③**(재민 · AI Hub · 4060).
