# 보고 — T266 효과음 8종 2판 (2026-09-13 · 세션8/ART)

**베이스 `9c993114`** · 자산 커밋 `c16fc300`(**맥 VM 트리에서** · 이미 main 착지) + 이 문서·잠금 커밋
**그림·BGM 무변** — `git diff 9c993114..` 의 `public/assets` 변경은 **`sfx/` 넷뿐**(png/webp 0줄 · `audio/` 0줄)
배포 설정 0 · 지우기 0 · 출처 없는 파일 0

## ★ 답 한 줄

**8종 중 2 확보(`wind`·`fire` — Commons 퍼블릭 도메인) · 미확보 6(`step_dirt`·`step_grass`·`wolf_growl`·
`harvest`·`eat`·`axe` — "못 받아서"가 아니라 **출처가 없어서**) · 쌍 4 파일 · 크레딧 2줄 · 잠금 ○(456 · 어긋남 0)**

---

## §0-ⓐ 출처 — **길이 열렸다**

T262 가 "컨테이너는 이진 파일을 못 받는다"로 끝났던 자리를 **맥 VM 이 받아서** 이었다. PM 실측대로
`curl`·`ffmpeg`·`ffprobe`·`python3`·`node`(v22) 가 다 있고 Commons 에 닿는다.

★**찾는 자를 바꾼 것이 이 카드의 값이다.** 검색 결과 페이지는 robots.txt 로 막혀 있지만(T262),
**Commons API** 는 열려 있고 `extmetadata` 로 **라이선스·작성자·날짜·직링크**를 한 번에 준다 —
페이지를 긁을 필요가 없고, 라이선스가 **기계로 읽힌다**.

### 파 본 자리와 그 결과

| 자리 | 결과 |
|---|---|
| Commons **검색**(`generator=search`) | 된다. 다만 게임 SFX 는 **얇다** — "footsteps" 를 찾으면 Lingua Libre 가 그 **단어를 발음한** 녹음(`LL-Q1860…footsteps.wav` CC0)이 나온다. **이름만 보고 받으면 안 된다.** |
| Commons **`Category:Sound effects`** | 거의 사진이다(폴리 작업실 사진 등). SFX 창고가 아니다. |
| `Category:Audio files of animal sounds` | 새·개구리·긴팔원숭이는 있는데 **늑대는 없다**. 대부분 CC-BY-SA. |
| ★**`pdsounds.org` 미러**(검색어 `pdsounds.org`) | **PD 음원 175개**가 Commons 에 있다. 이 카드의 둘이 여기서 나왔다. |

### 확보 둘 — 값은 전부 API 가 준 것(지어낸 칸 0)

| 키 | 파일 페이지 | 작성자 | 라이선스 | 녹음 | 원본 | 원천 |
|---|---|---|---|---|---|---|
| `wind` | [Gentle breeze and birds singing.ogg](https://commons.wikimedia.org/wiki/File:Gentle_breeze_and_birds_singing.ogg) | ezwa | **Public domain** | 2007-04-18 | ogg mono 44.1k **32.55s** 422,735 B | pdsounds #1 |
| `fire` | [Dry grass burning in open fireplace.ogg](https://commons.wikimedia.org/wiki/File:Dry_grass_burning_in_open_fireplace.ogg) | ezwa | **Public domain** | 2007-06-17 | ogg mono 44.1k **25.50s** 511,116 B | pdsounds #3 |

받은 바이트가 **API 가 적은 크기와 정확히 같다**(422,735 / 511,116) — 받다 만 파일이 아니다.

### 미확보 여섯 — **"출처가 없었다"**

`step_dirt` · `step_grass` · `wolf_growl` · `harvest` · `eat` · `axe`.
로그인 없는 출처(Commons CC0/PD · pdsounds 미러 175개)를 훑어 **쓸 만한 것이 없었다.**
`Category:Sound effects` 와 pdsounds 목록을 발자국·으르렁·씹기·도끼·풀 키워드로 걸렀지만
나온 것은 `Monster growls`(늑대 아님) · `Splinter of wood falls on ground` · `Chainsaw`(현대 기계 — 금지) 따위다.
freesound CC0 후보 둘(T262 §0-ⓐ `206030` · `370938`)은 **로그인이 필요해** 이 세션이 못 받는다 —
재민이 `~/Mini/_sfx_in/<key>/` 에 넣어 주면 다음 판이 잇는다.

## §0-ⓑ 받기 (맥 VM)

```bash
curl -sL --max-time 90 -A "durango-mini-asset-sourcing/1.0" \
  -o ~/mnt/Mini/_sfx_in/fire/Dry_grass_burning_in_open_fireplace.ogg \
  https://upload.wikimedia.org/wikipedia/commons/d/d8/Dry_grass_burning_in_open_fireplace.ogg
# wind 도 같은 꼴 (…/0/0e/Gentle_breeze_and_birds_singing.ogg)
```
원본은 `~/Mini/_sfx_in/<key>/` 에 **`SOURCE.txt`**(key·file·page·direct·artist·license·date·fetched)와 함께 둔다.
레포 밖이라 커밋에 안 들어간다. ⚠직링크의 해시 경로는 **추측하면 안 된다** — 첫 시도에 `2/2c/` 로 찍었다가
**106바이트**(오류 문서)를 받았다. API 가 주는 `url` 을 그대로 쓴다.

## §0-ⓒ 가공 (맥 VM · T262 §0-ⓑ 관로 그대로)

```bash
# ① 11초 뜨기(모노 유지) → ② 끝 1초를 머리에 크로스페이드 → 10.0초 반복
ffmpeg -y -ss <시작> -t 11 -i <원본> -ac 1 -ar 44100 -c:a pcm_s16le src.wav
ffmpeg -y -i src.wav -filter_complex \
 "[0]atrim=0:10,asetpts=N/SR/TB[a];[0]atrim=10:11,asetpts=N/SR/TB[b];[a][b]acrossfade=d=1:c1=tri:c2=tri" \
 -c:a pcm_s16le loop.wav
# ③ 피크 재고 → 이득 = −1.0 − 잰피크 → 두 꼴
ffmpeg -i loop.wav -af astats -f null -                      # Peak level dB
ffmpeg -y -i loop.wav -af "volume=<G>dB" -c:a libvorbis -q:a 4                  <key>.ogg
ffmpeg -y -i loop.wav -af "volume=<G>dB" -c:a aac -b:a 96k -movflags +faststart <key>.m4a
```

| 키 | 자른 구간 | 잰 피크 → 이득 | `.ogg` | `.m4a` | 길이 | 이음새(머리↔꼬리 RMS) |
|---|---|---|---:|---:|---:|---|
| `wind` | 32.55s 중 **11.0~22.0s** | −12.45 dB → +11.45 dB | **93,551 B** | **123,449 B** | 10.00s | −43.08 ↔ −44.24 = **1.16 dB** |
| `fire` | 25.50s 중 **3.0~14.0s** | −8.41 dB → +7.41 dB | **104,607 B** | **123,531 B** | 10.00s | −43.24 ↔ −42.65 = **0.59 dB** |

넷 다 **150KB 안** · 길이 상한 안. 부호화 뒤 피크는 −0.93 / −1.01 / −2.14 / −1.20 dB
(T262 가 예고한 ±0.25 dB 흔들림보다 `fire.ogg` 가 더 내려갔다 — 넘치지는 않으니 그대로 둔다).

⚠**`fire` 를 5초에서 뜨면 9초가 나왔다** — 원본이 25.50s 라 5+11 이 끝을 넘지 않는데도 뜬 wav 가
10.99s 였다(ogg 이음새). 3초로 옮겨 11.01s 를 확보했다. **길이는 뜬 뒤에 재야 한다.**

### `wind` 의 새소리 — 귀가 아니라 자로 골랐다

원본 설명에 **Blackcap · Chaffinch** 가 적혀 있다. 들을 수 없으므로 **2kHz 이상 대역의 에너지 비율**을
1초 단위로 재고, 그 비율이 가장 낮은 10초 창을 골랐다:

| | 값 |
|---|---|
| 고른 창(11~21s) | **0.02%** |
| 파일 전체 평균 | 0.07% |
| 가장 나쁜 창 | 0.40% |

고른 창이 최악 창의 **1/20** 이다. 그래도 **새가 아주 없다고는 못 한다 — 실기(듣기)는 재민.**

## §0-ⓓ 커밋 · 잠금 · 크레딧

**순서: 맥 자산 커밋 → 컨테이너 문서·잠금 커밋**(카드 지시 · 두 벌 방지).

* 자산 `c16fc300`(맥 트리) — `public/assets/sfx/{wind,fire}.{ogg,m4a}` + `CREDITS.md` §2-b(파일마다 한 줄) · §7 한 줄.
* 잠금 — `asset-lock.js --write` → **456장 · 어긋남 0**(452 + sfx 4).
* 감사 — `test-assets-audit` **PASS** · 고아 **56 → 60**(늘어난 넷이 `sfx/` · **맞는 답**이다).
* 매니페스트 — 세션9 꼴이 아직 없어(`인계/SOUND-소리.md`·`public/assets/sfx/manifest.json` 없음)
  **`인계/ART-자산.md` §1-b-3 에 표로** 남겼다. 세션9 가 붙인다.

### ★코드 0 을 못 지켰다 — 세 곳, 이유를 적는다

카드 규약은 코드 0 인데, **소리를 잠그려니 자가 안 맞아서** 세 곳을 고쳤다. 재민이 보고 물릴 수 있게 적는다.

| # | 어디 | 무엇 | 왜 |
|---|---|---|---|
| ① | `asset-lock.js` | 잠그는 확장자에 `ogg`·`m4a`(`ASSET_EXT`) + **`keyOf`** | T262 가 회부한 "파일이 생기는 판에서 연다"가 이 판이다. 안 열면 새 자산이 **안 잠긴다** |
| ② | `asset-lock.js` | **키는 한 파일 하나** — 소리만 파일 이름 그대로 | 어간을 키로 두니 `.ogg`/`.m4a` 가 한 칸을 다퉈 **2장이 거짓 빨강**이었다(실측) |
| ③ | `test-assets-audit.js` | `ogg`·`m4a` 를 어간 대조에서 뺌 + **`*.lock.json` 은 참조 원천이 아님** | 아래 |

**③ 이 이 카드에서 배운 가장 큰 것이다.**
소리 키는 `wind`·`fire`·`eat`·`axe` 처럼 **짧은 영단어**라 코드 아무 데나 걸린다 — 갓 넣은 `sfx/fire.ogg` 가
`building_render.py`·`test-craft.js` 에, `sfx/wind.ogg` 가 `zone.js`·`e2e-weather.js` 에 걸려
**쓰는 데가 없는데 "쓰임 있음"** 이 됐다(T257 이 `.py`·`.zip` 에 한 것과 같은 줄로 막았다).
그리고 잠그고 나니 이번엔 **감사기가 제 잠금표를 보고** "쓰임 있음"이라 답했다 —
**잠금은 파일에서 유도된 검사값이지 파일을 부르는 자리가 아니다.** 그러면 "잠갔으니 쓴다"가 되어
아직 아무도 안 부르는 자산이 영영 안 보인다.
실측: `*.lock.json` 을 빼도 **그림은 하나도 안 흔들리고** 고아가 56 → 60(늘어난 넷이 정확히 `sfx/`).

**자명 통과 금지**: `T257_NOSTEM=0`(어간 빼기 되돌림)이면 **55** 를 낸다 — 고친 줄이 실제로 무는 자리다.

## §1 검증

| | |
|---|---|
| 그림·BGM | `git diff 9c993114..HEAD -- public/assets` = **`sfx/` 넷뿐** · png/webp **0줄** · `audio/` **0줄** |
| 상한 | 파일당 ≤150KB **4/4** · 반복 ≤10초 **4/4** |
| 출처 | 파일마다 Commons 페이지 실측(API `extmetadata`) · 출처 없는 파일 **0** · NC/ND/Pixabay **0** |
| `asset-lock --check` | **456 · 어긋남 0** |
| `test-assets-audit` | **PASS** · 고아 60(= 56 + sfx 4) · 되돌림 판 55 |
| `test-icons` / `test-props` / `test-harness-lint` | PASS / PASS / **42-0** |
| 러너 목록 | **143** 그대로 |
| 3시드 | 면제 — diff ∩ 랩 적재 = ∅ |
| 접점 | `public/assets/sfx` · `CREDITS.md` · `asset-lock` · `rulerOf` · `keyOf` · `manifest.json`(아직 없음) · `ffmpeg` · `ffprobe` · `curl` · `_sfx_in` |

## §2 회부

1. **미확보 여섯(재민)** — freesound CC0 둘(`206030` `370938`)은 로그인이 필요하다.
   `~/Mini/_sfx_in/<key>/` 에 넣어 주면 다음 판이 §0-ⓒ 관로로 굽는다.
   ★또는 **freesound API 토큰을 PAT 와 같은 문법으로 파일에 두면** 세션이 직접 받는다(카드 §4 의 그 갈래).
2. **`wind` 새소리 · 실기(재민 귀)** — 자로는 0.02% 까지 내렸다. 쓸 만한지는 들어야 안다.
3. **코드 0 위반 셋(재민 물릴 수 있음)** — 위 표. 특히 ③ 은 감사기의 뜻을 바꾸는 줄이라 봐 주길.
4. 다음 소리 묶음(물·비·새·청동 도구·움집 안·낚시 3종) — pdsounds 175개에 물·새는 있어 보인다.
5. #32 webp ⓐ · #30 ⓒ · #29 · #14 — 그대로.
