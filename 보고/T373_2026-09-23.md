# T373 — Z 정리 셋 + 다음 존 현황 (세션5 · 2026-09-23 · **가지만**)

> 카드 `~/Mini/지시_T373.md` · 가지 `batch/zone-next-0923` · 베이스 `e37f5ba6`(main) ·
> 만진 것: `scripts/plan-bridges-v2.js`(집계 한 자리) · `scripts/t360-border.js`(같은 자리) ·
> `server/zone-config.js`(**주석만** · 값 diff 0) · `scripts/test-nippon-boot.js`(ⓚ 신설)
> — **정본 JSON diff 0 · 제품 로직 0 · 새 수 0 · 배 0 · 고개 0.**

## ★ 머리 15줄 — 다음 존 후보 순위(판정 0 · 재민이 고른다)

전부 **오류 0 으로 뜨고 마을이 선다**(9존 실측 부팅). 그래서 순위를 가르는 것은 "도는가"가 아니라
**T348 순서(지형 → 광맥 → 마을)에서 무엇이 얼마나 남았나**다.

| 순 | 존 | 부팅 실측(후보→시딩·econc·도달) | 정본 채움 | 남은 일 | 왜 이 자리 |
|---|---|---|---|---|---|
| **1** | **`europa`** | 25 → **11** · 88명 · **55/55 도달** | riv 32 · lak 6 · rid 6 · for 28 · **ore 5** · vil 0 | 광맥 · 마을 정본 | 지형이 가장 두껍고(강 32·숲 28) **다리 없이 이미 전부 이어져 있다** — 닛폰이 T348·T360 두 카드를 쓴 그 일이 **0카드** |
| **2** | **`centaria`** | 14 → **6** · 48명 · **15/15 도달** | riv **42** · lak 10 · for **35** · **ore 2** · vil 0 | 광맥 · 마을 정본 | 강·숲은 한반도 다음으로 두껍다. 마을이 6곳뿐이라 세계가 작다 |
| **3** | **`jungwon_s`** | 16 → **6** · 48명 · **15/15 도달** | riv 18 · lak 2 · for 8 · ore 2 · vil 0 | 광맥 · 마을 정본 | 이어져 있고 크기가 적당하다. 숲이 얇다 |
| **4** | **`hindgang`** | 16 → **5** · 40명 · **10/10 도달** | riv 5 · lak **0** · for 13 · ore 4 · vil 0 | 광맥 · 마을 정본 · 물 | 작고(3.5M셀) 이어져 있다. **호수 0 · 강 5** — 물가 마을이 안 선다 |
| **5** | **`jungwon_n`** | 25 → **11** · 88명 · **10/55 도달(불능 45)** | riv 39 · lak 12 · rid 7 · for 13 · ore 4 · vil 0 | **다리** · 광맥 · 마을 정본 | 가장 크고(12.7M셀) 마을도 많은데 **끊겨 있다** — 닛폰과 같은 다리 카드가 먼저다 |
| 6 | `nordan` | 8 → **3** · 24명 · 1/3(불능 2) | riv 12 · lak 12 · for 13 · ore 3 | 다리 · 광맥 · 마을 | 작고 마을 3곳. 끊김 있음 |
| 7 | `sibara` | 8 → **2** · 16명 · 1/1 | riv 7 · lak 7 · for 8 · ore 3 | 광맥 · 마을 | 마을 2곳 — 교역이 한 쌍뿐이라 경제가 안 산다 |
| 8 | `bering` | 4 → **2** · 16명 · 1/1 | riv 14 · lak 10 · for 15 · ore 2 | 후보 · 광맥 · 마을 | **후보가 4곳뿐** — 지형이 사람을 안 받는다 |
| **—** | **`sahar`** | 8 → 선별 1 → **시딩 0** · `villages: null` | riv **2** · lak 1 · rid 3 · for **1** · ore 3 | **지형부터** | ★**유일하게 마을이 하나도 안 선다** — 사막이고 강 2·숲 1이라 품질 게이트를 아무도 못 넘는다 |

**한 줄**: 다음은 **`europa`** 가 가장 싸다 — 지형이 다 있고 **이미 전부 이어져 있어** 닛폰이 쓴 다리 카드가 통째로 없다.
가장 비싼 것은 **`sahar`**(지형부터) 이고, `jungwon_n` 은 **크지만 다리 카드가 먼저**다.

1. ① **섬 수가 정직해졌다** — 플러드 **0셀은 섬이 아니다**. 후보가 물·바위 위에 앉아 설 자리가 한 칸도
   없는 것이고, `findOpenCenter` 가 어차피 스킵하므로 **세계엔 무해**하다. 거짓말을 한 것은 **보고 수**뿐이고,
   그래서 사람이 없는 바다를 걱정하게 됐다(닛폰 "대양 4섬" 중 **넷 다** 이것이었다).
   ⇒ 계획기·계측기 둘 다 섬에서 빼고 **`설 자리 없는 후보 n`** 으로 따로 센다. 지우지 않았다 — **이름이 다른 것**이다.
2. ② **낡은 주석 둘을 정정했다 · 값은 한 칸도 안 바꿨다**(`zone-config` diff = 주석 16줄).
   · **광산2 "대양 분리 · 배가 답"** — T2(07-28) 그날의 판정이고 지금은 **본토 안**이다(최단 폭 **0칸**).
     후보 51곳 중 **50곳이 본토 한 덩어리**, 떨어진 것은 어촌6 하나. **한반도엔 배가 필요한 자리가 없다.**
   · **어촌6 "뭍이 없어 스킵"** — 맞다. 수를 붙였다: 그 자리는 **20셀짜리 조각**이다.
     "뭍이 없다"가 아니라 **"설 자리가 20칸뿐"** — 다리로 풀 일이 아니다(①과 같은 종류).
   · 덤으로 닛폰 "놓지 않은 것 다섯" 줄에 T360·T373 정정을 붙였다(그중 넷은 섬이 아니었다).
   ★**낡은 줄을 지우지 않았다** — 왜 그렇게 적혔는지가 이력이고, 지우면 다음 사람이 같은 길을 또 간다.
3. ③ **`MAX_SPAN = 200` — 유도 근거는 없다. 그러나 출처는 찾았다**(§0-ⓒ).
   도입 커밋 `319dcb19`(2026-07-28 · T2 계획기 v2)가 v1 의 한계를 넷 적었는데 그중 하나가
   **"도하 60셀 미만만 인정"**(`plan-bridges.js:102` 의 하드코딩 `60`)이다. 200 은 그 **60 을 넉넉히 푼 값**이고,
   커밋 본문에도 코드에도 **왜 200 인지는 없다**. ⇒ **상수 · 근거 없음**(값 무변 · 회부).
   ⚠그리고 T360 이 보였듯 **이번 건에선 200 이 문제가 아니었다**(물 60 · 바위 40). 문제는 착지 조건이었다.
4. ④ **존 11개 전수 표**(§0-ⓓ) · 9존 **실측 부팅** — 전부 **오류 0**.
5. **`test-nippon-boot` 39 → 42/0** (ⓚ 섬 수 정직 — 소스 + **자명 통과 방지** 실측 한 줄).

---

## §0-ⓐ 섬 수 — 무엇이 거짓이었나

```
[T348·T360 닛폰 보고]  다리 가능 1섬 / 대양 4섬
                        항해 층 필요: 이즈사키(0셀) · 후지로(0셀) · 카토쿠(0셀) · 히마쿠(0셀)
```
**괄호 안의 `0셀` 이 이미 답이었다** — 플러드가 0이라는 것은 **24셀 반경에 뭍이 한 칸도 없다**는 뜻이다.
그건 섬이 아니라 **설 자리가 없는 자리**이고, 다리를 아무리 놓아도 마을이 안 선다.

| | 종전 | 고친 뒤 |
|---|---|---|
| `plan-bridges-v2` | `islands.push(...)` 에 0셀도 들어가 `ocean` 으로 집계 | `if (!f.n) { drySkip.push(name); continue; }` · 결과 줄에 **`(섬 아님 — 설 자리 없는 후보 n: …)`** |
| `t360-border` | 덩어리 `#0` 을 다른 덩어리와 같은 줄로 찍음 | `(섬 아님 — 설 자리 없는 후보 n곳: …)` 한 줄로 가른다 |
| 세계 | — | **무변**(`findOpenCenter` 가 종전에도 스킵했다) |

★자(ⓚ)는 **말만 안 본다**: 소스에 가드가 있는지 보고(ⓚ·ⓚ2), 그런 자리가 **실재하는지**를
정본 술어로 직접 잰다(ⓚ3 — 닛폰 후보 16곳 중 **4곳**이 물·바위 위다). 0곳이면 위 둘은 자명 통과다.

## §0-ⓑ 낡은 주석 둘 — 정정(값 무변)

| 줄 | 종전 | 실측(T360) | 한 일 |
|---|---|---|---|
| `zone-config.js:240` | "광산2(90,515셀)는 200셀 내 본토 도하 없음 = **대양 분리**. 배가 답" | 광산2(셀 2007,218)는 **본토 #1 7,298,105셀 안** · 최단 폭 **0칸** · 후보 51 중 **50이 본토** | 낡은 줄 **유지** + `⚠[T373 정정]` 다섯 줄 |
| `zone-config.js:202` | "어촌6 은 중심 주변 24셀에 뭍이 없어 스킵" | 맞다 — 그 자리는 **20셀 조각**이다 | 수를 붙이고 **①과 같은 종류**라고 적었다 |
| `zone-config.js:352`(닛폰) | "놓지 않은 것 다섯 — 배가 답" | T360 이 셋을 놓아 **21/21** · 나머지 넷은 **섬이 아니었다** | `⚠[T360 정정]`·`⚠[T373 정정]` 두 줄 |

**값 diff 0** — `hanbando.bridges` 836 · `nippon.bridges` 208 · 정본 JSON **diff 0**.

## §0-ⓒ `MAX_SPAN = 200` — 출처는 있고 근거는 없다

| | |
|---|---|
| 도입 | `319dcb19` **2026-07-28** "T2 계획기 v2(성분 기준·실셀)" |
| 그 커밋이 v1 의 한계로 적은 것 | ① 마을 중심 ±260셀 박스 ② 축 4방 직선만 ③ **도하 60셀 미만만 인정** ④ 코스 격자 성분 라벨이 과탐지 |
| v1 의 그 수 | `plan-bridges.js:102` `while (len < 60 && waterCell(...))` — **하드코딩 60**(역시 근거 없음) |
| 200 의 유도 | **없다.** 커밋 본문에도 코드 주석에도 "왜 200"이 없다. v1 의 **60 을 넉넉히 푼 값**으로 읽힌다 |
| 지금 판정 | **상수 · 근거 없음.** 값은 **안 건드렸다** |
| 왜 지금은 안 문제인가 | T360 실측 — 닛폰 최대 덩어리는 물 **60칸** · 바위 **40칸**이다. 둘 다 200 훨씬 안쪽이고, 막던 것은 **착지 조건**이었다 |

⇒ **회부**: 다음 존에서 200 에 실제로 물리면 그때 **수가 아니라 근거**를 세운다(그날의 입력은 그 존의 최단 폭 표).

## §0-ⓓ 존 11개 — 정본 채움 전수

`server/hanbando-terrain.json` 이 가진 존은 **11개**다(육지 26존 중 나머지 8개 — `canadia`·`nubiano`·
`mayan`·`amazonia`·`patagona`·`kongra`·`nanyang`·`oseania` — 는 **정본에 절이 아예 없다**. 절차 지형만 돈다).

| 존 | 셀 | riv | lak | rid | pas | val | for | gro | **ores** | **vil** | brg | 후보 | 광맥/10만셀 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **hanbando** | 8,889,844 | 55 | 14 | 11 | 0 | **24** | 43 | **53** | **787** | **51** | 836 | 51 | **8.85** |
| **nippon** | 6,346,406 | 17 | 5 | 10 | 2 | 0 | 16 | 0 | **55** | **16** | 208 | 16 | 0.87 |
| europa | 11,425,156 | 32 | 6 | 6 | 0 | 0 | 28 | 0 | 5 | 0 | 0 | 25 | 0.04 |
| jungwon_n | 12,696,875 | 39 | 12 | 7 | 0 | 0 | 13 | 0 | 4 | 0 | 0 | 25 | 0.03 |
| hindgang | 3,515,625 | 5 | **0** | 4 | 0 | 0 | 13 | 0 | 4 | 0 | 0 | 16 | 0.11 |
| sibara | 7,810,000 | 7 | 7 | 5 | 0 | 0 | 8 | 0 | 3 | 0 | 0 | 8 | 0.04 |
| nordan | 4,392,344 | 12 | 12 | 4 | 0 | 0 | 13 | 0 | 3 | 0 | 0 | 8 | 0.07 |
| sahar | 5,272,500 | **2** | 1 | 3 | 0 | 0 | **1** | 0 | 3 | 0 | 0 | 8 | 0.06 |
| bering | 7,810,000 | 14 | 10 | 4 | 0 | 0 | 15 | 0 | 2 | 0 | 0 | **4** | 0.03 |
| jungwon_s | 5,859,375 | 18 | 2 | 4 | 0 | 0 | 8 | 0 | 2 | 0 | 0 | 16 | 0.03 |
| centaria | 7,618,125 | **42** | 10 | 5 | 0 | 0 | **35** | 0 | 2 | 0 | 0 | 14 | 0.03 |

**읽는 법**: `vil`(정본 마을 칸)이 0인 존은 **후보 문의 둘째 갈래**(절차 배치기)로 돈다 — T343 이 연 길이다.
`ores` 가 한 자리인 존은 **광맥이 사실상 없다**(T348 의 닛폰이 그랬다: 5 → 55).
`gro`(채집 군락)·`val`(계곡)은 **한반도에만** 있다 — 다른 존은 아직 그 층이 없다.

**9존 실측 부팅**(`ZONE_ID=<존>` · 다른 손잡이 0 · `test-nippon-boot` 과 같은 문법):

| 존 | 후보 → 선별 → 시딩 | econ 인구 | 교역 | 도달불능 | 물 위 스킵 | 오류 |
|---|---|---|---|---|---|---|
| europa | 25 → 12 → **11** | 88 | 55쌍 | **0** | 1 | **0** |
| jungwon_n | 25 → 13 → **11** | 88 | 55쌍 | **45** | 2 | **0** |
| jungwon_s | 16 → 7 → **6** | 48 | 15쌍 | **0** | 1 | **0** |
| centaria | 14 → 6 → **6** | 48 | 15쌍 | **0** | 0 | **0** |
| hindgang | 16 → 7 → **5** | 40 | 10쌍 | **0** | 2 | **0** |
| nordan | 8 → 4 → **3** | 24 | 3쌍 | **2** | 1 | **0** |
| sibara | 8 → 2 → **2** | 16 | 1쌍 | **0** | 0 | **0** |
| bering | 4 → 2 → **2** | 16 | 1쌍 | **0** | 0 | **0** |
| **sahar** | 8 → 1 → **0** | — | — | — | 1 | **0** |

★**T343 이 연 문이 존 전체에서 돈다** — 9존 전부 오류 0 이고 8존에서 마을이 섰다.
★**sahar 만 0곳**이다: 강 2 · 숲 1 인 사막이라 품질 게이트(식량 하한)를 아무도 못 넘는다.
  `시딩 스킵` 이 아니라 **선별 1 → 시딩 0**(그 하나도 물 위) — "지형부터"인 존이다.

## §3 검증

| | |
|---|---|
| `test-nippon-boot` | **42 / 0**(ⓚ 신설 — 소스 가드 + 실측 전제) |
| 정본 JSON | **diff 0**(`hanbando-terrain.json` 무변 — `ores` 787/55 · `villages` 51/16 그대로) |
| `zone-config.js` | **주석만**(+16줄) · 값 무변(`bridges` 836 / 208) |
| 제품 로직 | **0**(`server/*.js` 중 바뀐 것은 `zone-config` 주석뿐) |
| 3시드 | **면제**(`sim/` 무접촉 · 정본 무변) |
| 부팅 실측 | 9존 · **오류 0** |
| T359 · T372 | **무접촉** · 배포 **0** · force **0** · 실기 **0** |

**접점 심볼**: `plan-bridges-v2` · `MAX_SPAN` · `t360-border` · `zone-config` · `광산2` · `어촌6` ·
`hanbando-terrain.json` · `plan-zone-villages` · `siteCandidates` · `findOpenCenter`

## §4 회부만 (구현 0)

1. ★**다음 존을 고르는 것은 재민**(머리 표). 싼 순서는 `europa` → `centaria` → `jungwon_s`,
   비싼 쪽은 `jungwon_n`(다리 먼저) · `sahar`(지형부터) · `bering`(후보 4곳).
2. ★**`MAX_SPAN = 200` 은 상수 · 근거 없음**(§0-ⓒ). 값은 안 건드렸다. 다음 존에서 물리면 그때 근거를 세운다.
3. **정본에 절이 아예 없는 육지 존 8개**(`canadia`·`nubiano`·`mayan`·`amazonia`·`patagona`·`kongra`·
   `nanyang`·`oseania`) — 절차 지형만 돈다. 그 존들은 **지형 정본부터** 필요하고, 그건 에디터 일이다.
4. **`groves`(채집 군락)·`valleys`(계곡)는 한반도에만 있다** — 다른 존엔 그 층이 아직 없다.
   `plan-small-forests`·`plan-village-forage` 가 존 인자를 받으니(T336·T348 에서 고쳤다) 돌리면 된다.
5. (이어짐) 고개(재민 · T360 §0-ⓑ) · #31 계획기·랩이 서버와 다른 물을 본다(25벌) · #47.

## §5 푸시

| | |
|---|---|
| 가지 | **`batch/zone-next-0923`** (가지만 — main 은 PM) |
| 베이스 | `e37f5ba6` |
| 길 | 맥 VM `device_bash` · `$HOME/pushrepo` 얕은 클론 · PAT 는 셸 변수만 · 즉시 `unset` |
| force | **0** |
