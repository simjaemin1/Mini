# 보고 — T432: 마을 청크는 몸이 있으면 켜진다 — 손잡이 `T432_BODY_CHUNKS`(기본 끔) · 켜면 빈 한반도가 **한 코어를 다 쓴다**(사람당 22.4µs · 캐논의 6.8배) — 값의 주인은 몸이 아니라 **켜진 청크가 올리는 짐**(A* 벽·나무 · 건물 격자 · 충돌) · 넷 켬(4.05 → 5.94µs)의 주인은 **걷기** · T368 B 는 이미 한 갈래

**한 줄 답:** 청크 문을 만들었다 — `T432_BODY_CHUNKS`(기본 끔)를 켜면 `updateActiveChunks` 가 **주민 몸이 선 청크**를 켠다(둘레 0 · 겹침 띠 = `HANDOFF_COMMIT` · 끄면 T410 착지본과 집합 차례까지 같다). **켜면 빈 한반도(1,329몸 · 관측자 0)가 한 코어를 다 쓴다** — 틱 p50 29.7ms · 사람당 **22.4µs = 캐논 3.3 의 6.8배**(결정 예산이 거의 매 틱 차는 하한 · T421 을 얹어도 20.0) · 활성 청크 257 · 건물 6.3만 · 시드 1.2만 ⇒ **켬 권고 ✗(재민)**. 값의 주인은 몸이 아니라 **켜진 청크가 올리는 짐**이다 — A* 안 벽 질의(밭 타일까지 든 건물 격자) 18~25% · 격자 유지 31%(T421 켬 14%) · 나무 충돌 8~11%. 넷 켬(4.05 → 이 호스트 **5.94µs** — 올린 것은 호스트 · 같은 벽시계에선 `527f655b` 가 T410 트리보다 −4.7%)의 주인은 **걷기 ~78%**(행위 본체 16% · 청크 0). T368 B 는 T410 이 이미 한 갈래로 세웠다(코드 무변 · ⑤ 63/0). 26존 동시(청크 켬)는 8GB · 2코어 한 호스트에 **안 든다**(한반도 OOM · 합 52 → ≈ 224% 한 코어). ★발견: 헤드리스 결산 문이 `767b827e` 부터 **늘 거짓**(관측 마을도 새벽 일괄 — 이중 생산은 아니다).
[승인 대기 · T432] · 가지 `batch/body-chunks-0926` = `3ff1fdc6`(내용) · 베이스 `b87a140b`(main · 낮 착지 — 리베이스 · 충돌 둘 풀었다 · §5) · 잰 트리 = `527f655b` + 같은 패치(`zone.js` patch-id 같다) · force 0 · `.lock*` 0 · 실기 0 · main 안 밀었다
★호스트가 바뀌었다 — 이 컨테이너는 14:01 에 새로 떴다(2코어 · Xeon 2.1GHz). T410 의 값은 **이 호스트 · 그때 main(`527f655b`)** 에서 다시 쟀다(ⓐ 1.24 → 1.48ms · ⓒ 4.05 → 5.94µs) —
  같은 벽시계에 두 트리를 나란히 돌려 보니 **올린 것은 호스트다**(T410 트리 6.27 · 지금 트리 5.98µs · §1-ⓒ). 표는 전부 이 호스트 안의 값이다.

> **ⓐ 청크 문** — `updateActiveChunks` 한 블록(손잡이 한 자리 · 주민 = `simVillageId` · 선 청크는 곧바로 · 놓기는 그 청크 밖 256px 초과 · 붙잡힌 차례 유지) · 끔 = 착지본 집합 **비트 동일**(480/480 · 해시 같음) · 켬 = 활성 257(몸이 선 청크 233 + 띠 24 · 둘레 0) · 주민 1,329 전부 활성 · 기본 손잡이에서도 걸음 0 → 217만/분 · 되풀이 켜기 10~12/분(한 청크 최다 17번).
> **ⓑ 값 분해** — 사람당(낮 · 이 호스트) ⓐ 종전 **1.12** · ⓑ 청크 켬 **22.4** · ⓒ 행위 넷 **5.94** · ⓓ 둘 다 22.1 · ⓑ' 청크 + T421 20.0 · ⓓ' 20.7µs · 청크 켬 팔은 결정 예산(15ms)이 분당 1,038~1,570틱 찬다(하한) · 주인 = 켜진 청크의 짐(A* 벽 질의 · 격자 · 나무) · 4.05(→5.94)의 주인 = **걷기** · 캐논 안은 **ⓐ 하나**.
> **ⓒ B · 26존** — B 는 이미 한 갈래(코드 무변 · `villages.js` 무접촉 → 3시드 면제) · ★결산 문(`_lifeDaily` · `anyViewerNear(x, y, r)`)이 `767b827e` 부터 늘 거짓 — 관측 마을도 새벽 일괄이 하루를 하고 몸은 순찰(청크 켬에서 몸이 더한 일 +0.02%) · 26존 동시(청크 켬)는 한 호스트에 **안 든다**(커널 OOM 이 한반도를 죽였다 · 나눠 잰 합 = 같은 호스트 청크 끔 52.3% → **≈ 224%** 한 코어 · ≈ 8.2 GB).
> **ⓓ 켬 권고** — **✗ · 재민**(캐논의 6배). 켜기 전 셋: A* 벽 질의·건물 격자에서 밭 타일 빼기 · 결정 예산 · 결산 문.

* 새 코드 = `server/zone.js`(손잡이 · `updateActiveChunks` 한 블록 · 차례 표 `_t432Order`) · 하네스 `scripts/test-body-chunks.js`(신설 · 27/0) · 계측기 `scripts/t432-probe.js` · `scripts/t432-prof.js`(신설 · 러너 밖) · `scripts/t410-zone-awake.js`(팔 넷 · 들여다보기)
* 하네스(리베이스 트리) `test-body-chunks` **27/0** · `test-farm-act` **63/0**(⑤ 그대로) · `test-move-soa` 178/0 · `test-fish-act` 73/0 · `test-wood-act` 106/0 · `test-forage-act` 137/0 · `test-build-act` 63/63 · `test-hunt-vis` 32/0 · `test-war-world` 126/0 · `test-recut` 12/0 · `test-ghost-tree` 10/0 · `test-granary-add` PASS · `test-stone-real` 25/25 · `e2e-move` **37/0** · `test-nippon-boot` 42/0 · `test-jungwon-boot` 38/0 · `e2e-village` 9/8 · `e2e-zone-cross` 28/4(둘 다 main 과 같은 빨강) · `test-harness-lint` 61/0 · `lab-wiring-check` 0/0
* 3시드 **면제**: `lab-modules --diff b87a140b` 교집합 **∅**(내 diff = `zone.js` · 스크립트뿐) · `villages.js` 무접촉 · 존 틱 · econ 무접촉
* 회부: #79 행위 넷 값(재민 · 호스트마다 다시 — 비로 읽기) · **청크 켬의 짐**(PM · 밭 타일 · A* · 격자) · **결산 문**(PM · 767b827e) · 되풀이(새 수) · 26존 배치(재민) · canadia 우회(PM) · 자의 호스트(PM) · #69·#75

---

## 0. ⓐ 청크 문 — diff · 무엇이 사나 · 겹침 띠

### 0-ⓐ diff — `zone.js` 는 청크 문 한 자리(`updateActiveChunks`) · `villages.js` 무접촉

| 자리 | 전(T410 착지본) | 후 |
|---|---|---|
| 손잡이 | — | `const T432_BODY_CHUNKS = process.env.T432_BODY_CHUNKS === '1'` — **기본 끔** · 읽는 곳 **한 자리** |
| `updateActiveChunks` | 사람 player 둘레 + 관측자 둘레(반경 `CHUNK_ACTIVE_RADIUS` 1,200px = 5×5 청크) | 그 **뒤에** 한 블록 — 켬이면 주민 몸(`simVillageId` · hp>0)마다 **붙잡은 청크**(≤ 4 · 모서리)를 집합에 더한다. 둘레 0 |
| 겹침 띠 | — | 선 청크는 **곧바로** 붙잡는다(선 청크가 꺼지면 몸이 청크 문에 걸려 못 걷는다) · 붙잡은 청크는 몸이 그 청크 밖으로 **`HANDOFF_COMMIT`(256px) 넘게** 벗어나야 놓는다 — Phase 5-K2 존 경계 핸드오프의 "겹침 띠" 문법 · 그 수 · **새 수 0** |
| 집합 차례 | — | 붙잡힌 청크 키를 **처음 붙잡힌 차례**로 든다(`_t432Order` · 판 표지만 고친다) ⇒ 몸마다 목록이 바뀌어도 집합이 같으면 차례가 같다 — T421 증분 격자의 "집합이 차례까지 같은가" 가 안 무너진다 |
| 켜기/끄기 전이 | `activateChunk` · `deactivateChunk`(T309/T317 규약) | **무접촉**(새로 켜진 키 · 꺼진 키만 넘어간다 — 종전 두 줄 그대로) |

끔 = **T410 착지본 비트 동일**: 블록이 손잡이 한 번 뒤에 통째로 선다 — 하네스 `test-body-chunks` ② 가 원문에서 블록을 도려낸 종전 함수와
120판 × 4틱(사람 0~2 · 관측자 0~1 · 주민 0~59 · 도적 섞음 · 걸음)을 나란히 돌려 **집합 차례까지 480/480 같다**(청크 집합 해시 `7c2da5a8` = `7c2da5a8`) ·
켜기/끄기 호출 차례도 같다. `test-move-soa` 178/0(착지본 그대로).

### 0-ⓑ 무엇이 사나 — T410 §0-ⓑ 표에 열 하나 더(빈 한반도 1,329몸 · 관측자 0 · 기본 손잡이 · 실측은 §1 판 ⓐ·ⓑ)

| 무엇 | 관측자 0 · 문 닫힘(종전) | 관측자 0 · 문 열림(T410) | **관측자 0 · 문 열림 · 청크 켬(T432)** | 관측자 1 |
|---|---|---|---|---|
| 틱 본문 | 0 | 전부 | 전부 | 전부 |
| 활성 청크 | 얼어 있다 | **∅** | **몸이 선 청크 + 띠** — 낮 257 · 밤 271(몸이 선 청크 233 · 229 · 띠 안의 몸 462) · 둘레 0 | 관측자 둘레 25 |
| 주민 결정·이동 문 둘(T316) | 0 | 걷기 술어 참인 몸만(기본 0명 · 걸음 0) | **주민 전부**(활성 1,329/1,329 · 기본 손잡이에서도 걷는다 — 걸음 0 → **217만/분**) | 둘레 + 술어 |
| 공간 인덱스 · 비키기(`sepNpcs`) | 0 | 0(활성 몸 0) | 주민 전부 + 활성 청크 건물 **6.3만**(틱마다 재삽입 — T421 끔) | 둘레 |
| 낙하 | 0 | 0 | 주민 전부(탈 일은 드물다 — 계단·높이) | 둘레 |
| HP 회복 | — | — | **주민은 원래 안 탄다**(`simVillageId` 면 일일 규칙만 · `_hpRegenStep`) — 무변 | 사람만 |
| 몹 AI | 0 | 0 | 활성 청크 안의 몹만(449 중 **1.5~2.5**) | 둘레 |
| 야생(`Wildlife` 뷰) | 0 | 0 | **돈다** — 뷰 = 활성 청크 전부를 덮는 상자(한반도 거의 전역) · 서식 재합성은 집합이 바뀔 때 · 틱의 2.5% | 둘레 |
| 시드 자원 개체 | 얼어 있다 | 0 | **1.19만**(낮) · 1.28만(밤) | 둘레 |
| 건물 개체(DB → 메모리) | 얼어 있다 | 3~4 | **6.33만** — 마을 시각 타일 3.44만 · 마을 밭 타일 1.17만(둘 다 비영속 `vb` 타일) · 바닥 1.01만 · **벽 0.68만** · 궤 3 | 둘레 |
| 헤드리스 농부 절(T368 켬) | 일괄 | 몸 | 몸 | 몸 |
| 헤드리스 결산(T368 끔 · 개간·신축·작물) | 일괄 | 일괄 | 새벽 일괄 + 몸은 순찰(§2-ⓑ) | 새벽 일괄 + 몸(★결산 문이 죽어 있다 · §2-ⓑ) |
| 메모리(heap · GC 뒤) | — | 139 MB | **190 ~ 202 MB** | — |

⇒ 켜면 **주민 1,329 가 전부 활성**이 되어 기본 손잡이에서도 걷는다 — 캐논 ⓑ① "관측자 없어도 실걸음" 의 나머지 반(청크 문)이 열린다.
게이트 "활성 청크 수 = 몸이 선 청크 수 + 둘레" — 둘레 0 · 활성 257 = 몸이 선 청크 233 + 띠 안 몸이 붙잡은 옆 청크 24(하네스 ③ 이 몸 셋 · 관측자 하나로 수를 맞춘다 · 마을 사이 빈 땅은 꺼져 있다).

### 0-ⓒ 겹침 띠 — 켜고 끄기가 되풀이되지 않나(T309/T317 · 몸이 움직여 청크가 바뀌면 지난 청크는 언제 꺼지나)

* **규칙**: 들어선 청크는 곧바로 · 놓는 것은 그 청크 밖 **COMMIT px 초과**(256px — 존 경계 핸드오프와 같은 수). 띠 안에선 옛 청크 · 새 청크가 **같이** 켜져 있다.
* **하네스 ④**: 경계를 50번 넘나들면 켜기 **한 번** · 끄기 **0** · 띠 끝(256px)까지 안 놓고 257px 에서 **한 번** 놓는다 · 모서리 넷을 돌면 넷 · 멀어지면 셋을 놓는다 · 순간이동은 옛 청크를 곧바로 놓는다.
* ⚠**첫 판은 틀렸다(자기수리)**: "붙은 청크 하나 + 띠 안이면 선 청크도" 로 짰더니 몸이 경계 안쪽으로 돌아오는 순간 옆 청크를 놓아 **넘나들 때마다** 켜고 껐다
  (하네스 ④ 꼴로 50번 넘나들면 50번). ⇒ 붙잡은 **목록**으로 바꿨다(양쪽 다 띠 규칙 · 50번 → 한 번).
  다만 **실측 되풀이는 거의 같다**(첫 판 연기 · 밤 13/분 ↔ 고친 판 · 낮 10~12/분) — 되풀이의 대부분은 경계를 스치는 몸이 아니라 256px 보다 **크게** 드나드는 출퇴근이라 띠가 막을 몫이 작다.
* **실측(낮)**: 켜기 9.9~12.3/분 · 끄기 11.2~12.8/분 — 판 하나(부팅부터 24분)에 켜진 적 있는 청크 284~290 가운데 **두 번 이상** 켜진 청크 54~55 · 한 청크 최다 **17번**(ⓑ'·ⓓ') — 남은 되풀이는 COMMIT 보다 **크게** 드나드는 몸이다(밭 ↔ 집 · 물가 ↔ 곳간 출퇴근이 청크 경계를 256px 넘게 가로지른다).
* **값**: 켜기 한 번 = 그 청크 건물 DB 읽기 + 시드 자원 생성 + 방송 ⇒ 청크 켬 팔의 조각별 최대 틱(중앙) **180~190ms**(끈 팔 11~38ms). 되풀이를 더 줄이려면 띠를 넓히거나 시간 문을 둬야 하는데 **둘 다 새 수다** — 회부.

## 1. ⓑ 값 분해 — 네 팔(+ T421 증분 격자 짝 둘) · 4.05 의 주인

### 1-ⓐ 판 — 한반도 · 같은 틀(T368 b 대조 팔 200일 DB · 50마을 · 몸 **1,329**) · 실제 날 · 관측자 0 · 자의 규약(T410 §2-⓪): 부팅 뒤 **첫 하루 경계 뒤** 조각만 · 메모리는 **GC 뒤**

자 = `scripts/t410-zone-awake.js load`(T410 자 그대로 + 팔 넷 · `T410_PROBE=1` 이면 `scripts/t432-probe.js` 를 예비 적재해 조각 끝마다 GC 뒤 heap · 활성 청크 ·
시드 · 건물 · 몹 · 켜기/끄기를 읽는다 — 레포 `zone.js` 무접촉). 판마다 새 하루 새벽부터 60초 조각 20개 — 경계 조각(0)과 프로필 창이 걸친 조각 둘을 뺐다(낮 13 · 밤 2).
무거운 팔은 **혼자** 돌렸다(2코어 — 한 팔이 다른 팔의 틱을 먹지 않게). ⓐ·ⓒ 는 가벼워 T410 처럼 나란히.
틱 자의 규약(T433 이 한 장으로 모은 다섯 · 인계 `Z-자`)에 대어 보면: ⓐ 첫 경계 뒤 ✓ · ⓑ 메모리 = **GC 뒤** heap(들여다보기가 `gc()` 를 부른다) ✓ · ⓒ 실제 날 ✓ ·
ⓓ 같은 틀 · 같은 국면(새벽 → phase 0.83) ✓ — 무거운 팔은 벽시계가 다르다(혼자 돌렸다) · 주인은 **한 판 안의 프로필**로 갈랐다(§1-ⓑ 뺄셈 표는 크기만) ·
ⓔ 사람당 µs 의 분모 = **마을 명부 1,329**(자의 `L.bodies` — `npcs.size` 가 아니다).

| 팔 | 청크 | 행위 넷 | T421 격자 | 틱 p50 낮 · 밤 | p95 낮 · 밤 | **사람당 µs** 낮 · 밤 | CPU(한 코어) 낮 · 밤 | 틱/분 | 결정 예산 찬 틱/분 | 활성 청크 | 시드 | 건물 | 몹(활성/전체) | heap GC 뒤 |
|---|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| **ⓐ 종전**(T410) | 끔 | 끔 | 끔 | 1.48 · 1.50 ms | 2.20 · 2.25 | **1.12 · 1.13** | 7.1 · 7.2% | 1,813 | 0 | 0 | 0 | 4 | 0/449 | 139 MB |
| **ⓑ 청크 켬 · 행위 끔** | 켬 | 끔 | 끔 | **29.7 · 26.0** | 42.2 · 35.8 | **22.4 · 19.5** | **98.3** · 94.5% | 1,629 | **1,473** · 1,696 | 257 · 271 | 1.19만 · 1.28만 | 6.33만 | 1.5/449 | 190 MB |
| **ⓒ 청크 끔 · 행위 넷**(T410 의 4.05) | 끔 | 켬 | 끔 | 7.89 · 6.61 | 15.3 · 12.0 | **5.94 · 4.97** | 34.2 · 29.2% | 1,811 | 24 · 9 | 0 | 0 | 3 | 0/449 | 153 MB |
| **ⓓ 청크 켬 · 행위 넷** | 켬 | 켬 | 끔 | 29.4 · 26.0 | 42.1 · 36.3 | **22.1 · 19.6** | 98.9 · 94.0% | 1,630 | 1,570 · 1,684 | 255 · 272 | 1.25만 · 1.34만 | 6.26만 | 2.5/449 | 198 MB |
| ⓑ' 청크 켬 · T421 켬 | 켬 | 끔 | **켬** | 26.5 · 18.6 | 37.7 · 29.7 | **20.0 · 14.0** | 83.6 · 66.1% | 1,742 | 1,038 · 502 | 242 · 231 | 1.11만 · 1.23만 | 6.29만 | 2.6/449 | 193 MB |
| ⓓ' 청크 켬 · 행위 넷 · T421 켬 | 켬 | 켬 | **켬** | 27.6 · 19.2 | 37.0 · 31.4 | **20.7 · 14.4** | 85.9 · 68.0% | 1,753 | 1,274 · 636 | 248 · 227 | 1.21만 · 1.14만 | 6.28만 | 1.9/449 | 201 MB |

* ⚠**청크 켬 팔의 값은 하한이다** — 존의 주민 결정 루프는 틱당 예산(15ms · T324 "예산은 두되 이어 돌기로")을 두는데, 켬 팔은 **분당 1,473~1,696 틱**(거의 매 틱)이
  예산에 닿는다. 넘친 결정은 다음 틱으로 밀린다(몸은 걷되 다시 고르는 것이 늦다) — 예산이 없다면 틱이 더 길다. 틱 수도 1,813 → 1,629/분(−10%)으로 준다.
* 밤은 조각 둘(프로필 창 하나를 빼서)이라 **낮을 본다**. 청크 켬의 밤은 낮보다 가볍다(ⓑ' 14.0µs) — 걷는 몸이 줄어서다.
* RSS 는 GC 뒤에도 V8 이 안 돌려준 몫이라(ⓐ 358 · ⓒ 698 · ⓑ 989 MB) **heap(GC 뒤)** 로 읽는다: 청크 켬 = **+51~59 MB**(건물 6.3만 · 시드 1.2만 개체).

### 1-ⓑ 주인 — 뺄셈(같은 호스트 · 같은 틀) + 프로필(낮 창 60초 · 존 틱 함수 아래 갈래)

| 뺄셈(낮 p50) | ms/틱 | 사람당 µs | 읽기 |
|---|---:|---:|---|
| ⓒ − ⓐ (행위 넷) | +6.41 | +4.82 | 몸 1,329 가 매 틱 걷고 일을 고른다 |
| ⓑ − ⓐ (청크) | **+28.2** | **+21.2** | 켜진 청크의 짐 + 모든 주민이 걷는다(기본 일과) — **하한**(예산) |
| ⓓ − ⓒ (행위 위에 청크) | +21.5 | +16.2 | 같은 짐 — 행위가 켜져 있어도 거의 같다 |
| ⓓ − ⓑ (청크 위에 행위) | −0.3 | −0.2 | **0** — 청크가 켜지면 몸은 이미 다 걷는다(행위는 무엇을 하는지만 바꾼다) · 존은 예산에 걸려 있다 |
| ⓑ' − ⓑ (T421 격자) | −3.2 | −2.4 | 격자 재삽입 몫이 준다(31% → 14%) — 나머지는 그대로 |

(규약 ⓓ — 뺄셈은 팔 사이 **크기**만 본다. 갈래(주인)는 아래 프로필이 **한 판 안에서** 가른다.)

프로필(`scripts/t432-prof.js --tree` · V8 표본 · 틱 함수의 포함 시간을 100 으로):

| 갈래(틱 함수 아래) | **ⓒ 행위 넷**(청크 끔) | **ⓑ 청크 켬** | ⓑ' 청크 켬 · T421 | ⓐ 종전 |
|---|---:|---:|---:|---:|
| 주민 결정 `npcStep` | 47.7% | 31.9% | 43.6% | — |
| ┗ A*(`computeNpcPath` — 벽·나무 검사 포함) | 15.0% | **28.3%** | **37.8%** | — |
| 　┗ 그 가운데 벽 질의(건물 격자 `queryRect` — 밭 타일까지 든 격자) | 1.7% | **17.7%** | **25.1%** | — |
| ┗ 직업·일과 결정(`decideNpcBehavior` → `npcLifeTick`) | 15.7% | 2.7% | 3.3% | — |
| ┗ 결정 루프 몸통 · 막힘 검사 | 16.2% | 0.9% | 2.5% | — |
| 이동 `movePlayerStep` | 12.3% | 15.1% | 19.1% | — |
| ┗ 나무 충돌(`isBlockedByTree`) · 벽(`isBlockedByWall`) | 3.6% · 0.9% | **8.5% · 3.9%** | **10.8% · 5.4%** | — |
| 공간 격자 `rebuildSpatialIndex` | <1% | **31.4%**(재삽입 20.8) | 14.1%(증분 유지 12.5) | <1% |
| 비키기 `sepNpcs` | 2.2% | 4.2% | 5.0% | **37.6%** |
| 청크 문 `updateActiveChunks`(이 카드의 블록) | <1% | 3.2% | 1.6% | <1% |
| 야생 `Wildlife.tick` | <1% | 2.5% | 2.9% | <1% |
| 틱 몸통(몸마다 도는 순회 — 계단·낙하·게이지 한 바퀴가 인라인) | **32.8%** | 8.9% | 11.1% | 44.6% |

⇒ **4.05 의 주인(지금 5.94µs)은 걷기다** — 몸 1,329 가 매 틱 도는 값(틱 몸통 순회 33% · 결정 루프 몸통·막힘 16% · A* 15% · 이동 12% · 비키기 2% ≈ **78%**) ·
  **행위 본체**(직업·일과 결정 — 밭 칸 고르기 · 낚시 자리 · 나무 · 채집)는 **16%** · 청크 **0**(끈 팔).
⇒ **청크 켬의 주인은 몸이 아니라 켜진 청크가 올리는 짐이다** — A*(28~38% · 그 가운데 **건물 격자 벽 질의만 18~25%** — 꺼진 청크에선 1.7%) ·
  건물 격자 유지(밭 타일 포함 6.3만 개체 · T421 끔 31% / 켬 14%) · 이동 충돌(나무 1.2만 · 벽 · 15~19%). 청크 문 블록 자체(몸 1,329 의 붙잡은 목록)는 1.6~3.2%.
  ⇒ 같은 몸이 **꺼진 청크에서 걸을 때(ⓒ)는 벽·나무가 없다**(격자가 비어 있다 — T316 의 "값싼 몸") — 청크가 켜지면 그 몸이 **진짜 세계**와 부딪친다. 그 값이다.

### 1-ⓒ 4.05 → 5.94 는 어디서 — T410 트리(`b36851b9`) 대 지금 트리(청크 끔) · 같은 벽시계에 나란히 · 행위 넷 팔

| 같은 벽시계 · 이 호스트 · 새 하루 아침 11조각(경계 조각 뺌) · 두 존 나란히 | 틱 p50 | p95 | 사람당 µs | CPU |
|---|---:|---:|---:|---:|
| T410 트리 `b36851b9`(4.05 를 잰 그 트리) | 8.33 ms | 15.9 | **6.27** | 31.5% |
| 지금 트리(`527f655b` + 이 카드 · 청크 끔) | 7.94 ms | 15.0 | **5.98** | 30.1% |

⇒ **4.05 → 5.94 는 호스트다** — 같은 호스트에선 T410 트리가 오히려 4.7% 무겁다(`527f655b` 는 T385 한 바퀴 기본 켬 등으로 조금 가볍다).
  T410 의 4.05 는 옛 컨테이너의 값이고, 이 컨테이너(14:01)에선 같은 트리가 **6.27µs** 다(+55%). 행위 넷의 값(#79)은 **호스트마다 다시 재야 한다** — 표는 한 호스트 안의 비로 읽는다
  (ⓒ/ⓐ = 5.3배 · ⓑ/ⓐ = 20배).

### 1-ⓓ 캐논 3.3µs 에 닿는 팔

| 팔 | 사람당 µs(낮) | 캐논 대비 | 틱 p50 / 33.3ms |
|---|---:|---:|---:|
| ⓐ 종전 | 1.12 | **34% ○** | 4.4% |
| ⓒ 행위 넷 | 5.94 | 180% ✗ | 24% |
| ⓑ 청크 켬 · 행위 끔 | **22.4(하한)** | **678% ✗** | 89% |
| ⓓ 청크 켬 · 행위 넷 | 22.1(하한) | 670% ✗ | 88% |
| ⓑ' 청크 켬 · T421 | 20.0(하한) | 605% ✗ | 80% |
| ⓓ' 청크 켬 · 행위 넷 · T421 | 20.7(하한) | 629% ✗ | 83% |

⇒ **캐논 안은 ⓐ 하나뿐이다.** 청크 켬은 행위를 끄고 T421 을 켜도 캐논의 6배 — 한 존이 코어 하나를 다 쓰고 결정이 밀린다.

## 2. ⓒ T368 B 한 갈래 · 헤드리스 결산 문 · 26존 동시 판

### 2-ⓐ T368 B — "문이 열린 세계에선 몸 하나 · 되돌림 `=1` 에서만 XOR — 코드로 한 갈래" · A/B(고른 것 A)

T410 이 이미 그 꼴로 세웠다: 판정은 `villages.js _t368ZoneAwake` = 주입된 **존의 idle 문 그 함수**(`zone.js zoneAwake`) 하나 · 부르는 곳은
`_lifeHeadlessDay` 의 `const _body = _t368Walk() && _t368ZoneAwake(vil);` **한 줄** · 마을·일마다 **한 번** · 문이 열려 있으면 늘 참(켠 팔 일괄 0) · `=1` 이면 잠든 날만 일괄.

| | **A · 코드 무변(고름)** | B · `_body` 에 청크 항을 더한다 | C · 결산 문(`_lifeDaily`)을 고친다 |
|---|---|---|---|
| 한 갈래(`zoneAwake()` 를 묻는 자리 하나 · 사본 0) | ○ — T410 착지본 그대로(이 카드 실행: 켠 판 · 존 깸 → 판정 **한 번** · 일괄 0) | ○ | — |
| 문 열림 → 몸 하나 · 되돌림에서만 XOR | ○ | ○ | — |
| `test-farm-act` ⑤ 그대로 | ○ **63/0** | ✗ — `const _body = …` 글자가 바뀐다(⑤ 가 그 글자를 문다) | ○ |
| 새 술어 · 새 주입 | 0 | 청크 켬 여부를 생활층이 알아야 한다(새 주입) | 0 |
| `villages.js` | **무접촉** ⇒ 3시드 면제 | 만진다 | B 갈래 **밖** · 기본 세계(관측 마을)의 동작이 바뀐다 — 카드 밖 |
| 되돌림 비트 동일 | ○(코드 0) | ○ | — |

⇒ **A.** 청크를 켰을 때 "몸이 걷는데 일괄이 같이 도는" 일은 B 갈래(농부 절 · T368 켬에서만 선다)가 아니라 **결산 문**의 일이다 — 그리고 그 문은 따로 죽어 있었다(§2-ⓑ).
  실측(청크 켬 · 행위 넷): 헤드리스 농부 절 **몸 50 · 일괄 0** 마을·일 · 밭 일 1,109 → 7,668(몸 · 낮 동안) — ⓒ(청크 끔) 2,292 → 7,617 과 같은 꼴.

### 2-ⓑ ★발견 — 헤드리스 결산 문이 `767b827e`(첫 헤드리스 결산)부터 **죽어 있다**

`villages.js _lifeDaily`:
```js
const anyNear = state.deps.anyViewerNear;
if (!(anyNear && anyNear(vil.ccx * SZ + SZ / 2, vil.ccy * SZ + SZ / 2, (vil._maxRPx || 800) + 1600))) {
  try { _lifeHeadlessDay(vil); } …
```
`zone.js` 가 주입하는 `anyViewerNear(center, r)` 는 **점 객체**를 받는다(`center.x`). 여기선 수 셋을 넘긴다 ⇒ `center` = x좌표(수) · `r` = y좌표 ·
`center.x` = undefined ⇒ 거리 NaN ⇒ **늘 거짓** ⇒ 헤드리스 결산이 **관측 마을까지 모든 마을에서** 돈다. 주석("활성 마을=스킵 · 실걸음 크루 소유 · 이중 계상 방지")은
한 번도 선 적이 없다. 증거: 원문 `anyViewerNear` 를 뽑아 사람을 그 점에 세우고 부르면 `({x,y}, r)` → **참** · `(x, y, r)` → **거짓**. `767b827e` 의 `zone.js` 도 같은 꼴이었다.

| 그 하루(새벽 경계 → 낮 19조각) | 새벽 일괄(밭 일 · 개간 셀 · 신축 단계) | 낮 동안 몸이 더한 것 | 읽기 |
|---|---|---|---|
| ⓐ 종전(몸 안 걸음) | 8,337 · 1 · 2 | 0 · 0 · 0 | 일괄이 다 한다 |
| **ⓑ 청크 켬 · 행위 끔**(주민 전부 걸음) | 8,095 · 1 · 2 | **+2 · +2 · 0** | 이중 생산은 **아니다**(0.02%) — 새벽 일괄이 그날 일감을 먼저 다 하고 **몸은 순찰한다** |
| ⓑ' 청크 켬 · T421 | 8,084 · 1 · 2 | +2 · +2 · 0 | 같다 |
| ⓒ·ⓓ 행위 넷(T368 켬) | 농부 절 0(`_body`) | 밭 일 **몸 7,617 · 7,668** | 몸이 한다 |

⇒ 청크를 켜도 **이중 생산은 없다** — 그러나 **몸이 걷는데 일은 새벽 일괄이 한다**(T368 끔). 관측 마을도 같다(문이 죽어 있으므로) — 재민이 보는 마을의 농부도
  새벽에 이미 끝난 밭을 순찰한다(`경작` 라벨). 이 문을 살리면 관측 마을(그리고 청크 켬이면 모든 마을)의 하루를 몸이 가져간다 — 기본 세계의 동작이 바뀐다 ⇒ **회부**(PM · 손잡이 하나 · 이 카드 밖).

### 2-ⓒ 26존 동시 판 — 청크 켬(T410 자 `host` 그대로 · 모든 존 `T432_BODY_CHUNKS=1`)

**① T410 자(`host`) 그대로 한 판 — 안 든다.** 한반도(틀 · 관측자 1 · 청크 켬)를 띄워 둔 채 빈 25존을 잠 → 깸(청크 켬) → 잠으로 갈아 띄웠다(모든 존 `T432_BODY_CHUNKS=1`).

| 판 | 빈 25존 | 한반도 틱 p50 · p95 · max | 한반도 CPU | 25존 CPU 합 | 호스트 전체(코어 합 · 200% = 두 코어) | 메모리 |
|---|---|---:|---:|---:|---:|---|
| 0 | 잠 | 30.2 · 45.3 · 219 ms | **99.4%**(포화) | 18.3% | 126.8% | 25존 3.79 GB |
| 1 | 깸 · 청크 켬 | — | — | — | — | 25존이 청크를 켜며 뜨는 데 **590초**(T410 판 52초) · 그사이 커널 OOM 이 **975 MB(한반도)** · 637 MB(작은 존 하나) 프로세스를 죽였다 → 판을 멈췄다 |

⇒ **8 GB · 2코어 한 호스트에 26존 청크 켬은 안 든다**(메모리가 먼저 넘친다 · 한반도 하나가 이미 한 코어를 다 쓴다).

**② 같은 자(`zones`)로 나눠 잰 합 — 깸 + 청크 켬은 여덟·아홉 존씩 세 판 · 청크 끔(깸)·잠은 25존 한 판씩(새 DB · 데우기 90초 · 창 240초 · 같은 호스트) + 한반도(§1 ⓐ·ⓑ)**

| 존 | 마을 · 몸 | CPU 깸 · 청크 끔 → **깸 + 청크 켬** | 틱 p50 끔 → 켬 | RSS(켬) |
|---|---|---:|---:|---:|
| **한반도**(틀 · §1 ⓐ → ⓑ) | 50 · 1,329 | 7.1 → **98.3%(포화 · 하한)** | 1.48 → 29.7 ms | 990 MB |
| canadia(레거시 우회 걷기 + 청크) | 3 · 24 | 11.0 → **16.4%** | 2.81 → 3.83 ms | 232 MB |
| jungwon_n · europa · nubiano | 11~12 · 88~96 | 2.0~2.1 → 9.8~13.3% | 0.36~0.50 → 2.3~3.1 ms | 318~705 MB |
| nippon · jungwon_s · nanyang · amazonia · hindgang · centaria | 5~8 · 40~64 | 1.2~2.4 → 6.1~8.4% | 0.18~0.33 → 1.2~1.7 ms | 166~735 MB |
| kongra · oseania · mayan · nordan · sibara · bering | 2~4 · 16~32 | 1.4~2.0 → 3.2~5.7% | 0.26~0.30 → 0.7~1.2 ms | 139~**1,046** MB |
| 마을 0 육지 둘(sahar · patagona) | 0 · 0 | 1.4 → 1.6~2.3% | 0.24~0.26 → 0.25 ms | 162~176 MB |
| 바다 일곱 | 0 · 0 | 0.59~0.63 → 0.86~0.89% | 0.05~0.07 → 0.06 ms | 80~82 MB |
| 25존 합 | | **45.2 → 125.6%**(×2.8) | | 4.31 → **7.19 GB** |
| **26존 합** | | **52.3 → ≈ 224%**(×4.3 · 한 코어 기준 · 두 코어 = 200%) | | **≈ 8.2 GB** |
| (참고) 25존 잠 · 이 호스트 두 판 | | 19.7 · 20.1% | | 3.98 · 3.95 GB |

* ⚠**작은 존 칸은 판 사이 ±0.3~1%p 로 읽어라** — 바다·마을 0 존은 청크 켬이 할 일이 없는데(주민 0) 0.6 → 0.87% 로 올랐다: 9존 판(set 3)은 호스트가 한가했다(틱 수는 같다 — 틱마다 깨는 값이 달랐다).
  합의 판정(200% 초과)은 그 흔들림(25존 합 몇 %p)보다 크고, 한반도 98.3% 는 결정 예산에 걸린 **하한**이다. 메모리는 창 끝 RSS(GC 전) 합이다 — 판정은 실제 OOM(①)이 한다.
* 경계: 여덟 존 판 둘째(set 2)는 창 끝 40초에 하루 경계(17:36)가 들었다. 경계의 값은 틱 한 번의 뾰족이다 — 한반도 판에서 경계 조각의 CPU 는 여느 조각과 같았다(ⓑ 97.7 ↔ 중앙 98.2% · ⓐ 6.2 ↔ 7.2%) ⇒ 240초 합엔 거의 안 든다.
  잠·청크 끔 판(17:46~17:50 · 18:01~18:05 · 18:09~18:13)의 창엔 경계가 없다(경계 17:36 · 18:00 · 18:24 — 18:00 은 부팅 중).
* 이 호스트는 빈 존의 값이 옛 호스트의 두 배쯤이다 — 잠 존당 0.63~0.93%(canadia 2.8% 빼고 · T410 옛 호스트 0.35~0.42%) · 바다 존은 잠 0.63~0.64 ↔ 깸 0.59~0.63% 로 **차가 없다**(값이 타이머 깸이다). 판 사이 비교는 한 호스트 안에서만.

⇒ **26존을 한 호스트에서 청크 켬으로 돌리면 두 코어(200%)도 · 8 GB 도 넘는다** — 같은 호스트 청크 끔(깸) **52.3%**(25존 45.2 + 한반도 7.1)가 **≈ 224%** 가 된다.
  (T410 의 26존 45.2% 는 옛 호스트 값이다 — 이 호스트 25존 깸 45.2% 와 수가 같은 것은 우연이다.)
  ★재민 플랫폼 방향(존마다 제 서버)에서도 한반도급 존 하나가 **한 코어를 다 쓴다**(결정 예산에 걸린 채) — 빈 서버 한 대가 "한 코어의 7%"(ⓐ)에서 "한 코어 전부"가 된다.
  작은 존도 청크를 켜면 걷는 몸 16~96 에 **3~13%**(청크 끔 1.2~2.4% 의 2~6배) · 메모리는 존에 따라 1 GB 를 넘는다(kongra — 청크가 켜지면 그 존 숲·밭 타일이 다 올라온다).

## 3. ⓓ 한 줄 답 + 켬 권고

**한 줄 답**: 머리 그대로.

**켬 권고 — ✗ · 재민(값)**. 카드 ④ 의 문턱은 "청크 켬 · 행위 끔 팔(ⓑ)이 캐논 안이면 PM 이 켬을 닫는다" 다.

| 문턱 | 값 | 판 |
|---|---|---|
| ⓑ 청크 켬 · 행위 끔 — 사람당 µs ≤ 3.3 | **22.4µs(하한)** = 6.8배 · 한 코어 98% · 결정 예산이 분당 1,473틱 찬다 | ✗ |
| ⓑ' 거기에 T421 증분 격자(T433 이 **기본 켬**으로 착지 — 지금 main 에서 이 손잡이를 켜면 이 값) | 20.0µs(하한) = 6.0배 · 83.6% | ✗ |
| ⓓ · ⓓ' 행위 넷까지 | 22.1 · 20.7µs | ✗ |
| 이중 생산 | 없다(새벽 일괄이 먼저 다 한다 · +0.02%) | ○ |
| 되풀이 | 켜기 11~12/분 · 한 청크 최다 17번/24분 · 켜기 한 번이 틱 최대 100~190ms | △ |

⇒ PM 이 닫을 근거가 **없다**(캐논의 6배) — 켜기는 재민의 값 판정이다. 켜려면 먼저 셋(회부 1~3): ① A* 벽 질의와 건물 격자에서 **밭 타일**을 빼거나
  몸 청크에선 건물을 올리지 않기(격자 6.3만 · A* 안 벽 질의 18~25%) ② 결정 예산이 매 틱 차는 자리(A* — 꺼진 청크에선 1.7%) ③ 결산 문(§2-ⓑ — 몸이 하루를 가져가게).
  손잡이는 **기본 끔 그대로** — 끄면 T410 착지본과 집합까지 같다.

## 4. 회부 (고치지 않았다 · 켜지 않았다)

1. **#79 행위 넷 값**(재민) — `527f655b` · 이 호스트에서 **5.94µs**(캐논 180% · T410 의 4.05) · 주인 = **걷기 ~78%**(몸 순회 · 결정 루프 · A* · 이동) · 행위 본체(직업 결정) 16% · 청크 0.
   4.05 → 5.94 는 **호스트**다(같은 벽시계 A/B — T410 트리 6.27 · 지금 트리 5.98µs · §1-ⓒ) — 값 판정은 한 호스트 안의 비로(ⓒ/ⓐ 5.3배).
2. **청크 켬의 짐**(PM · 켜기 전 카드) — A* 안 **벽 질의 18~25%**(건물 격자 6.3만 — 73% 가 마을 시각·밭 타일 · 벽은 0.68만 — 벽이 아닌 개체까지 질의한다) · 건물 격자 유지(T421 로 31 → 14%) ·
   나무 충돌(시드 1.2만 · 8.5~10.8%). 몸 청크에서 무엇을 올릴지(시각 개체는 사람·관측자 둘레만?)는 캐논 판정이다.
3. **헤드리스 결산 문이 죽어 있다**(PM · 손잡이 하나 · 기본 세계 동작이 바뀐다) — `villages.js _lifeDaily` 가 `anyViewerNear(x, y, r)` 로 부르는데 정본은 `(점, r)` ⇒ 늘 거짓(`767b827e` 부터).
   관측 마을도 새벽 일괄이 하루를 하고 몸은 순찰한다. 살리면 관측 마을 · (청크 켬이면) 모든 마을의 하루를 몸이 가져간다 — 이중 생산 방지 문이 **처음으로** 선다.
4. **되풀이**(PM · 새 수 자리) — 겹침 띠(COMMIT 256px) 뒤에도 켜기 11~12/분 · 두 번 이상 켜진 청크 54/284 · 한 청크 최다 17번(24분) — 출퇴근이 청크 경계를 256px 넘게 가로지른다.
   띠를 넓히거나 시간 문(몇 초 뒤 끄기)은 **새 수**라 이 카드가 안 골랐다. 켜기 한 번(건물 DB 읽기 + 시드 생성 + 방송)이 틱 최대 100~190ms 를 만든다.
5. **26존 배치**(재민 · 플랫폼) — 청크 켬이면 26존 한 호스트는 **안 든다**(같은 호스트 청크 끔 52.3% → ≈ 224% 한 코어 · ≈ 8.2 GB · 실제로 OOM) · 존마다 제 서버여도 한반도급은 한 코어를 다 쓴다. 청크 끔(기본)의 값은 그대로다(이 호스트 26존 깸 52.3% · T410 옛 호스트 45.2%).
6. **canadia 레거시 걷기**(PM · 지우기 카드) — canadia 주민은 `simVillageId` 가 없어 이 문에 안 든다(여전히 청크 문 우회로 걷는다). 청크 문이 생겼으니 우회 갈래를 지울 자리다.
7. **자의 호스트**(PM · 자) — 컨테이너가 14:01 에 새로 떴다. 같은 자로도 절대값이 T410 과 다르다 — 판 사이 비교는 같은 벽시계 A/B 로만(§1-ⓒ).
8. #69 · #75(재민) — 이 카드 무접촉.

## 5. 검증

가지 칸 = **리베이스 트리**(`b87a140b` 위)에서 다시 돌린 값(돌린 트리 `c03078dc` → 가지 `3ff1fdc6` 은 계측기 `t410-zone-awake.js` 팔 두 칸만 다르다 — 어느 하네스도 그 파일을 안 부른다 · lint 61/0 다시) · main 칸 = 이 호스트에서 `b87a140b` 를 따로 떠(worktree) 돌린 값(★) 또는 PM 착지 글의 수.

| 하네스 | main(`b87a140b`) | 가지 |
|---|---|---|
| `test-body-chunks`(**신설** · ① 손잡이·주민·띠 · ② 끔 = 종전 집합 해시 · ③ 게이트 · ④ 히스테리시스 · ⑤ 차례) | — | **27/0** |
| `test-move-soa` | 178 | **178/0** |
| `test-farm-act`(⑤ B 그대로) | 63 | **63/0** |
| `test-fish-act` · `test-wood-act` · `test-forage-act` · `test-build-act` | 73 · 106 · 137 · 63★ | 73/0 · 106/0 · 137/0 · 63/63 |
| 청크 심볼을 무는 나머지 — `test-hunt-vis` · `test-war-world` | 32★ · 126★ | 32/0 · 126/0 |
| main 이 새로 만진 `zone.js` 자리 — `test-recut`(T426) · `test-granary-add`(T435) · `test-ghost-tree` · `test-stone-real`(T419) | 12 · PASS★ · 10★ · 25 | 12/0 · PASS · 10/0 · 25/25 |
| `e2e-move` | 37 | **37/0** |
| `test-nippon-boot` · `test-jungwon-boot` | 42 · 38 | **42/0** · **38/0** |
| `e2e-village` | 9/8★ | 9/8 — **같은 빨강**(여덟 줄 글자까지 · 재료 지급 0 → 회관이 안 선다 · 이 카드 무관) |
| `e2e-zone-cross`(T428) | 28/4★(PM 착지 글 "알려진 되돌기 병 · T439") | 28/4 — **같은 넷**(ⓑ5·ⓑ6 × 동·서 · 좌표·계정 id 만 다르다) |
| `test-harness-lint` · `lab-wiring-check` | 61 · 0/0★ | **61/0** · 실패 0 경고 0 |

* **main 이 두 번 움직였다**(`527f655b` → `092f920e` → `b87a140b` · `zone.js` 는 T419·T426·T433·T435 가 만졌다 — 제작 표 · 개간 · 격자 손잡이 · 곳간 — 청크 문과 먼 자리).
  **리베이스했다** — 충돌 둘: `scripts/t410-zone-awake.js` 조각 출력 한 줄(T433 규약 ⓑ·ⓔ 꼬리표 "(GC 의 값 · 규약 ⓑ)"·"(마을 명부)" + 이 카드의 들여다보기 꼬리 — 둘 다 살렸다) ·
  `인계/Z-존서버.md` 머리(T433 `Z-자` · T428 · T436 절 — T432 절은 `Z-자` 바로 뒤 · 그들 글자 무접촉). `zone.js` 는 자동 병합 — 청크 문 패치의 patch-id 가 리베이스 전후 같다(`8d9e7430`) ·
  하네스·계측기 세 파일은 바이트 같다. ⇒ 잰 값(§0~§2)은 `527f655b` + 이 패치 트리의 값이고, 가지 트리와 청크 문 글자가 같다.
  ⚠T433 이 T421 격자 증분을 **기본 켬**으로 착지했다 — 지금 main 에서 이 손잡이를 켜면 ⓑ'(20.0µs · 캐논 6.0배) 자리다(§3 · 결론 같다).
  ⇒ 자가 표를 그대로 다시 내도록 청크 두 팔(`chunks`·`chunksacts`)에 `T421_SPATIAL_INC: '0'` 을 박았다(잰 때의 기본과 같은 값) · ⓐ·ⓒ(T410 팔)은 밖에서 `T421_SPATIAL_INC=0` 을 주면 표의 칸이다.
* **끔 = T410 착지본 비트 동일**: 블록이 손잡이 한 번 뒤에 통째로 선다 · 하네스 ② 가 원문에서 블록을 도려낸 종전 함수와 480틱을 나란히 돌려 **집합 차례 · 켜기/끄기 호출까지** 같다
  (청크 집합 해시 `7c2da5a8` = `7c2da5a8`) · 실측 판 ⓐ(끔) = 활성 청크 0 · 걸음 0 — T410 과 같은 꼴.
* **3시드 면제**: `node scripts/lab-modules.js --diff b87a140b`(리베이스 트리 · 리베이스 전 `--diff 527f655b` 도 ∅) — 내 diff(`server/zone.js` · `scripts/t410-zone-awake.js` + 새 파일 셋 `scripts/test-body-chunks.js`·`t432-probe.js`·`t432-prof.js`) ∩ 랩 적재 목록(`villages.js` · `chunk.js` · `sim/**` …) = **∅** ·
  `villages.js` **무접촉**(B 는 코드 무변 — §2-ⓐ) · 존 틱 · econ 무접촉.
* **접점 파일(`zone.js`)을 글자로 무는 `@regress` 하네스는 162종**(리베이스 트리 · `grep -l 'zone\.js'` ∩ `@regress`)이다 — 끔이 집합 비트 동일(②)이라 청크 문을 지나는 판은 종전과 같다.
  카드 목록 + 청크 심볼(`updateActiveChunks|activeChunkKeys|isPositionActive`)을 무는 **다섯 전부**(`test-body-chunks` · `test-move-soa` · `test-fish-act` · `test-hunt-vis` · `test-war-world`) + main 이 새로 만진 `zone.js` 자리의 넷을 돌렸다 · 전수는 PM 밤.
* **접점 심볼**: `updateActiveChunks`(블록 하나) · `CHUNK_ACTIVE_RADIUS`(무변 — 둘레 그대로) · `activeChunkKeys`(무변 · 켬이면 몸 청크가 뒤에 붙는다) · `isPositionActive`(무변) ·
  `_t316WalkAlways`(무변 — 주민 칸 `simVillageId` 를 같이 본다) · `zoneAwake` · `ZONE_IDLE_SKIP`(무변 — 문은 T410 그대로) · `T432_BODY_CHUNKS`(신설 · 한 자리) · `_t368ZoneAwake`(무변 — §2-ⓐ) ·
  `t410-zone-awake`(팔 넷 + 들여다보기) · `t356-tick-anatomy`(무접촉 — 사람당 µs 는 그 자의 정의 p50 ÷ 몸 · 갈래는 V8 프로필로 갈랐다) · `canadiaVillage`(무변 — 우회 갈래 그대로 · 회부 6).
* **계측기**(러너 밖 · `@regress` 없음): `scripts/t432-probe.js`(예비 적재 · 창 함수 · 켜기/끄기 세기 · SIGUSR2 → GC → 파일 · CPU 프로필 창) · `scripts/t432-prof.js`(자기/포함 시간 · 틱 나무) · `scripts/t410-zone-awake.js`(팔 넷 · 들여다보기 · 프로필 경로).
* ⚠자기신고 ①: 겹침 띠 첫 판이 틀렸다(붙은 청크 하나 + 띠 안 선 청크 — 안쪽으로 돌아오면 놓아 넘나들 때마다 켜고 껐다) → 붙잡은 **목록**으로 고쳤다(§0-ⓒ · 하네스 ④).
* ⚠자기신고 ②: 판마다 밤 조각이 둘뿐이다(20조각 = 새벽 → phase 0.83 · 프로필 창 하나를 뺐다) — 표는 **낮**을 읽는다. 청크 켬 팔은 결정 예산에 걸려 **하한**이다.
* ⚠자기신고 ③: 호스트가 바뀌었다(14:01 새 컨테이너). T410 의 수와 절대값이 다르다 — 이 카드의 모든 표는 **이 호스트 · 한 판 안 · 같은 틀**이고, 트리 사이 비교는 같은 벽시계 A/B 다(§1-ⓒ).
