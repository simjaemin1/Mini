# T342 — 빨강 셋을 닫았다: ① 거부 캐시가 이제 영토 성장을 본다(집 438→493 · 어촌2·임업6 이 다시 짓는다) ② `test-shelter` 의 `18` 은 **하네스의 사본**이었다 50/50 ③ `test-site-memo` 두 빨강의 주인 ④ **1,718 은 인구가 아니라 몸 수다**(마을당 40 상한)

**[승인 대기 · T342]** 제품 `server/villages.js` **두 자리**(`_terrGrow` 한 줄 · `lifeSiteReset` 한 줄) ·
하네스 둘 · 새 수 **0** · 새 손잡이 **0** · 사본 **−2** · `sim/`·`lab/`·`public/` 무접촉 ·
T340·T341·T329 무접촉 · 실기 0.

**한 줄 답 — 영토 성장은 `영토 밖` 거부를 **뒤집는** 사건인데 `_terrGrow` 는 표지만 세우고 캐시를 안 버렸다.
`lifeSiteReset` 으로 바꾸니 실서버 200일에서 집 438~443 → **493채** · 인구 2,462~2,510 → **2,725** ·
침상/인구 107 → **109%** · 즉시 정지 3 → **1곳** · **세 판 내내 결정적으로 멈춰 있던 어촌2·임업6 이
마지막 날 다시 짓고 있다**. 그리고 싸졌다 — 빈손 185 → **94** · 성공률 64.7 → **80.8%**.**

**★카드의 전제 둘을 정정한다.**
① *"쉼터가 제 18 을 갖고 있나"* — **제품엔 사본이 없었다.** `pickShelterSpot` 은 이미 정본 필터
(`_lifeSiteFilters.reject` → `LIFE_HOUSE_GAP` 15)를 부르고, ③ 절이 그 거절 문구 `(<15)` 까지 찍는다.
**`18` 은 하네스 `scripts/test-shelter.js:124` 에 박혀 있었다** — 42/50 은 제품이 아니라 그 줄이 만든 빨강이다.
② *"1,718 vs 2,462 — 같은 세계인가"* — **같은 세계이고 다른 자다.** 1,718 은 `/lifedbg totals.pop`
(= Σ `vil.npcPids.length` · **스폰된 몸 수**)이고 2,462 는 `econ_state.npcs.length` 합(**econ 인구**)이다.
그리고 그 몸 수의 주인은 주거 게이트가 아니라 **`NPC_CAP_PER_VILLAGE = 40`** 이다(§④ 가 네 판에서 등식으로 증명).

---

## ① 거부 캐시가 영토 성장을 본다 — 제품 두 자리

```js
// server/villages.js  _terrGrow — 영토를 넓힌 **바로 다음**
- lifeSiteDirty(vil);    // 표지만 — 옛 거부는 그대로 남는다
+ lifeSiteReset(vil);    // 표지 + **거부 캐시 파기**(다른 세 자리가 이미 쓰는 그 함수 · 사본 0)

// server/villages.js  lifeSiteReset — 표지를 직접 안 쓴다
- function lifeSiteReset(vil) { if (vil) { vil._siteDirty = true; … } }
+ function lifeSiteReset(vil) { if (vil) { lifeSiteDirty(vil); … } }
```

**왜 둘째 줄까지 고쳤나** — `lifeSiteReset` 이 `_siteDirty = true` 를 **직접** 쓰고 있었다. 그건
`lifeSiteDirty` 의 사본이고, 무엇보다 픽스처 손잡이 `LIFE_SITE_NODIRTY` 를 **몰래 무시**한다(종전에도
그 함수를 부르는 세 자리에서 `test-site-memo ④` 의 "표지 고장" 픽스처가 새고 있었다). T342 가 `_terrGrow`
를 이 함수로 옮기면 그 새는 자리가 하루 수천 번이 되므로 같이 닫았다. **사본 −2 · 새 수 0 · 새 손잡이 0.**

**왜 단조성 논증이 깨지나**(T335 가 감사문으로 잰 그 10%): `reject` 는 앵커의 **부지 원판 124셀 전부**가
영토 안이어야 통과시킨다 ⇒ 경계에서 폭 ~7셀 띠의 앵커는 *셀 자신은 영토 안*이라 후보로 올라가
`마을 영토 밖` 으로 거부되고 캐시에 박힌다. 영토가 그 너머로 자라면 그 앵커는 **유효해지는데** 캐시가 가린다.
T298 이 영토 성장을 켠 뒤로 이게 매일 쌓였다.

**실서버 200일 · 50마을**

| | 전(T326 ON①) | 전(T326 ON②) | **T342 고침** | (참고) 캐시 통째로 끔 |
|---|---|---|---|---|
| 실체 집 | 438 | 443 | **493채** | 510 |
| 인구(econ) | 2,462 | 2,510 | **2,725** | 2,844 |
| 침상/인구 | 107% | 106% | **109%** | 108% |
| 침상≥인구 마을 | 50/50 | 50/50 | **50/50** | 50/50 |
| **즉시 정지** | 3 | 2 | **1곳**(광산2) | 1 |
| 소멸 | 0 | 0 | **0** | 0 |
| 집터 호출 / 성공 / **빈손** | 524/339/**185** | 527/343/**184** | 490/396/**94** | 466/411/55 |
| 성공률 | 64.7% | 65.1% | **80.8%** | 88.2% |
| 훑은 셀 | 5.6M | 5.5M | **4.3M** | 3.5M |
| 마지막 날 공사 중 | 1곳 | 3곳 | **3곳 — 어촌2 · 임업6 · 어촌4** | — |

★**세 판(T315 B · T326 ON①·ON②) 내내 수가 완전히 같던 어촌2·임업6 이 마지막 날 다시 짓고 있다.**
그리고 **싸졌다**: 빈손 −49% · 훑은 셀 −23% · 성공률 +16pp. 캐시를 통째로 끈 판(T335)의 이득을
대부분 되찾으면서 캐시는 살아 있다(영토가 다 자란 뒤에는 다시 건너뛴다).

**거부 사유의 모양이 바뀐다** — 캐시가 경계 띠를 더는 가리지 않으니 그 앵커들이 **실제로 판정되고 통과**한다:

| 사유 | T326 ON①(전) | **T342 고침** |
|---|---|---|
| 기존 집과 너무 가까움(<15) | 44,781 (77.5%) | 598,077 (**95.1%**) |
| 마을 영토 밖 | 9,264 (16.0%) | 24,606 (**3.9%**) |
| 개간 농지 위 | 1,782 (3.1%) | 3,289 (0.5%) |
| 쉼터와 너무 가까움 | 1,844 (3.2%) | 2,821 (0.4%) |

(총 판정 수가 는 것은 캐시가 건너뛰던 셀을 이제 **매번 다시 보기** 때문이다 — 그 값이 위 표의 −23% 훑은 셀과
같이 가는 이유는, 판정은 늘었지만 **성공이 빨라져** 하루 탐색이 일찍 끝나기 때문이다.)

## ② `test-shelter ②` 42/50 — 사본은 **하네스**에 있었다

**카드의 물음은 "쉼터가 옛 18 을 따로 갖고 있나(사본!) 아니면 발자국 계산이 다른가" 였다. 답은 셋째다:**
제품은 둘 다 아니었고 **하네스가 옛 수를 들고 있었다.**

```js
// scripts/test-shelter.js:124  (전)
for (const h of vil._houseCells) if (Math.hypot(h.cx - sh.cx, h.cy - sh.cy) < 18) far = false;
```

* `pickShelterSpot` 은 **정본 필터 하나**(`_lifeSiteFilters(vil).reject`)만 쓴다 — 간격은 그 안의 `HG`,
  곧 `LIFE_HOUSE_GAP`(T326 이 15 로 내린 그 수)다. 같은 하네스 ③ 절이 그 필터의 거절 문구
  `기존 집과 너무 가까움(<15)` 까지 찍고 있었다 — **증거가 두 줄 아래 있었다.**
* 그래서 42/50 은 "쉼터가 집과 겹쳤다" 가 아니라 **"쉼터가 15~17 에 섰다"** 였다.

**고친 것 둘**(하네스만 · 제품 무수정):
1. 간격은 **정본 수**를 묻는다 — `VL.LIFE_HOUSE_GAP`. 수를 하네스에 적지 않는다.
2. 이 줄의 이름이 주장하던 **"안 겹친다"** 는 간격이 아니라 발자국 이야기다 ⇒ **따로 잰다**:
   `__probe.vbFootprint('shelter'|'house')` 정본 렉트 둘이 한 셀도 안 겹치나.
   (15 에서 둘은 **구조적으로** 안 겹친다 — 같은 6×4 는 `|Δx|<6 · |Δy|<4` 에서만 겹치고 그 최대 hypot 은 √52 ≈ 7.21.)

| | 전 | 후 |
|---|---|---|
| `② 기존 집과 안 겹친다` | ✗ **42/50** | ✓ **50/50**(정본 간격 15 이상) |
| `② 발자국이 한 셀도 안 겹친다` | (없던 검사) | ✓ **50/50** (신설) |
| `test-shelter` 전체 | PASS 45 / **FAIL 1** | **PASS 47 / FAIL 0** |

## ④ #54 — **1,718 과 2,462 는 싸우지 않는다. 다른 것을 센 수다**

| | T333 의 1,718 | T326 의 2,462 |
|---|---|---|
| 잰 자 | `scripts/t324-warm.js` | `scripts/t212-beds.js` |
| 읽은 문 | `/lifedbg` → `totals.pop` | 존 DB `villages.econ_state` |
| 그 값의 정의 | Σ `vil.npcPids.length` — **존에 실제로 스폰된 몸 수**(`villages.js:4512`) | Σ `econ.npcs.length` — **econ 인구** |
| 게임일 | 805 | 200 |
| 게임일 길이 | 2s(`VILLAGE_DAY_MS`) | 4s |
| 마을 | 50 | 50 |
| 손잡이 | 같다(둘 다 `53b1ebcb` 계열 · 간격 15 · 살리기 켬) | 같다 |

**그 몸 수의 주인은 주거 게이트가 아니라 `NPC_CAP_PER_VILLAGE = 40` 이다**(`villages.js:83` ·
`syncVillagePop` 이 `target = min(econ.npcs.length, 40)`). 네 판에서 **등식으로** 맞는다:

| 판 | econ 인구 | **Σ min(마을 인구, 40)** | `/lifedbg totals.pop` 실측 | 40 넘는 마을 | 최대 마을 |
|---|---|---|---|---|---|
| T298(끔) | 2,953 | **1,633** | **1,633** | 22/50 | 223 |
| T326 ON① | 2,462 | **1,617** | **1,616** | 19/50 | 166 |
| T326 ON② | 2,510 | **1,631** | **1,631** | 23/50 | 155 |
| T335 캐시끔 | 2,844 | **1,641** | **1,641** | 23/50 | 216 |

(한 판의 1 차이는 `POP_SYNC_PER_DAY = 2` 의 반영 지연이다 — 하루 ±2명씩만 따라간다.)

⇒ **"300일께 포화" 의 주인은 상한 40 이다.** 천장은 50 × 40 = **2,000** 이고 T333 의 1,718 은 그 86%다.
econ 인구는 그 뒤로도 계속 는다(T342 고침 판에서 **2,725**). 주거 게이트는 `침상/인구 106~109% · 침상≥인구
50/50 마을` 이므로 **지금 인구를 묶고 있지 않다** — 묶는 것은 `housing`(목재)과, 몸 수로는 상한 40 이다.

★이것이 #53·#54 의 입력을 바꾼다: *"10,000명은 이 세계가 도달 못 하는 수"* 는 **몸 수 기준일 때만** 참이고,
그 벽은 설계가 아니라 **env 문 하나**(`VILLAGE_NPC_CAP`)다. 성능 곡선을 8,318·10,000 에서 찍으려면
주거 게이트를 풀 필요가 없다 — 상한을 올리고 econ 인구를 따라가게 하면 된다(비용은 스폰된 몸 수에 비례).

## ③ `test-site-memo` 두 빨강 — 하나는 ①이 고쳤고, 하나는 **옛 세계의 문턱**이었다

카드의 물음: *"간격 15 로 집터 훑기가 늘었나 · 하네스 기대가 18 을 전제했나 — 어느 쪽인지 표"*.
**답: 훑기는 늘지 않았다. 21배 줄었다. 문턱만 옛 세계에 묶여 있었다.**

| 빨강 | 실측 | 주인 |
|---|---|---|
| `② 단조성 위반 8` | → **위반 0 / 감사 4,023회** | **①이 고쳤다**(영토 성장 시 캐시 파기). T335 가 200일 판에서 10.0%(101,836/1,018,430)로 쟀던 그 위반이 0 이 됐다 |
| `① 하루 5회 이상 훑는다 26회/9일` | 영토 켬 세계에서 **구조적으로 0** · 영토 정지 세계에서 **46~47회/9일 ✓** | **세계가 갈렸다**(아래) |

**왜 영토가 자라는 세계에서는 기억이 아무것도 안 아끼나 — 그게 계약이다.**
①의 고침은 "영토가 자란 날엔 다시 훑어라" 다. 이 하네스의 씨앗 세계는 **9일 내내 영토가 자란다**
⇒ 매일 캐시가 버려지고 `스킵 0` ⇒ ①③이 재는 "싸짐" 이 0 이 된다. **결함이 아니라 계약이다.**
기억이 실제로 일하는 구간은 **영토가 다 자란 뒤**다. 그 구간을 결정적으로 만드는 문이 이미 있다 —
`T230_TERR_HOUSING=0`(영토 목표가 종전 `land.size × 25` 로 · T219 §0 실측 200일 50마을 합쳐 63셀 성장 = 사실상 정지).
⇒ 네 팔 전부에 그 문 하나를 걸었다(**제품 기본값 무접촉** · 하네스만). 그러면 `스킵 21` · `감사 4,023회` 로 돌아온다.

**그리고 남은 절대 문턱 셋을 계약으로 바꿨다 — 새 수 0**

| 문턱 | 옛 형태(T41 세계) | 왜 죽었나 | 새 형태 |
|---|---|---|---|
| ① 무게 | `life:site p50 ≥ 100ms` | T41 때 이 단계는 **하루 1,170ms** 였다. 지금 **52ms**(21배 싸다) — 일이 줄어서 빨강 | **대조군 > 채택본**(52 > 28) |
| ③ 셀 | `≤ 종전의 ½` | ½ 는 T41 이 본 이득이고 그때는 스킵 비율이 압도적이었다(하루 20곳 훑고 성공 0). 지금은 성사가 흔해 스킵이 46%(21/46) — 이득이 스킵을 따라가는 건 **옳은 동작** | **줄었다**(172,500 < 310,500) **+ 훑기 한 번당 셀 수가 안 늘었다**(6,635 ≤ 6,750셀/회) |
| ③ 시간 | `≤ 종전의 ⅕`(하한 20ms) | 위와 같다. 52ms 짜리에 ⅕ 를 걸면 하한 20ms 가 지배한다 | **낮다**(28 < 52) |

새 형태는 기억의 계약을 **수 없이** 그대로 적은 것이다: *"한 번의 훑기 값은 그대로 두고, 훑기 자체를 줄인다."*

**⑤ 절 신설 — T342 의 계약을 소스로 건다**(세계를 안 돌린다): `_terrGrow` 가 `lifeSiteReset` 을 부르고 ·
그 자리에 옛 `lifeSiteDirty` 가 없고 · `lifeSiteReset` 의 표지가 `lifeSiteDirty` 를 통해 선다.
되돌리면 ②의 감사가 다시 위반을 내기 **전에** 이 셋이 빨개진다.

| | 전 | 후 |
|---|---|---|
| `test-site-memo` | 9 통과 / **6 실패**(카드 시점 2 실패 → 간격 15 로 4 더 벌어졌다) | **19 통과 / 0 실패** |
| 단조성 위반 | 8 (T335 200일 판: 101,836 = 10.0%) | **0 / 4,023** |

## ⑤ 한 줄 답

**영토 성장은 `마을 영토 밖` 거부를 뒤집는 사건인데 `_terrGrow` 는 표지만 세우고 캐시를 남겼다.**
`lifeSiteReset` 으로 바꾸니 실서버 200일에서 집 438~443 → **450·493채** · 인구 → **2,558·2,725** ·
침상/인구 → **106·109%** · 즉시 정지 3 → **1곳** · **어촌2·임업6 이 마지막 날 다시 짓는다** ·
그리고 **싸졌다**(빈손 185 → 42·94 · 성공률 64.7 → 80.8·89.3% · 훑은 셀 5.6M → 2.7·4.3M).
`test-shelter` 의 42/50 은 제품이 아니라 **하네스에 박힌 `18`** 이었고(50/50 · 발자국 검사 신설),
`test-site-memo` 의 두 빨강은 하나는 이 고침이 닫았고(단조성 위반 8 → **0/4,023**) 하나는
**T41 세계에 묶인 절대 문턱**이라 계약으로 바꿨다(19 통과 / 0 실패). 그리고 **1,718 은 인구가 아니라 몸 수**다 —
`NPC_CAP_PER_VILLAGE = 40` 이 주인이고(네 판 등식 일치), econ 인구는 그 뒤로도 늘어 **2,725** 다.

## 검증

| 검사 | 결과 |
|---|---|
| `test-shelter` | **PASS 47 / FAIL 0** (전 45/1) — 사본 제거 + 발자국 검사 신설 |
| `test-site-memo` | **19 통과 / 0 실패** (전 9/6) — 단조성 감사 4,023회 **위반 0** · ⑤ 절 신설 |
| `test-psite-server` | **96 ✓ / 0 ✗ · PASS** |
| `test-terr-persist` | **PASS** — 50마을 유지 · **영토 밖 집 0/362채** · terr 행 224,160 → 268,200 |
| 실서버 200일 팔 | 고침 **2반복**(FIX①② · 위 표) + 감사 팔 1(`auditN 0 · auditBad 0` — 성장 중 세계는 건너뛸 게 없다) |
| 3시드 800일 | **면제**(카드 지시 · `sim/` 무접촉) |
| 러너 전수 | 안 돌렸다 |
| 무접촉 | **T340·T341·T329** · `sim/`·`lab/`·`public/`·`server/zone.js` |
| 실기 | **0** |
| 접점 | `_terrGrow` · `lifeSiteDirty` · `lifeSiteReset` · `_siteRejS`/`_siteRejL` · `LIFE_SITE_MEMO` · `LIFE_SITE_AUDIT` · `LIFE_SITE_NODIRTY` · `LIFE_HOUSE_GAP` · `pickShelterSpot` · `_vbFootprint` · `NPC_CAP_PER_VILLAGE` · `syncVillagePop` · `VILLAGE_DAY_MS` · `_hcap` |

**손 댄 파일 넷** — 제품 `server/villages.js`(두 자리) · 하네스 `test-shelter.js`·`test-site-memo.js` · 보고서.

## 회부

* **`NPC_CAP_PER_VILLAGE = 40` 은 이제 성능 곡선의 벽이다**(§④). 8,318·10,000 점을 찍는 길은
  주거 게이트를 푸는 게 아니라 이 상한을 올리는 것이다 — **env 문 하나**(`VILLAGE_NPC_CAP`), 코드 0.
  비용은 스폰된 몸 수에 비례하므로 T333 의 사람당 µs 곡선을 그 축으로 다시 그릴 수 있다. → #53·#54 · PM
* **기억(memo)의 값이 줄었다** — 영토가 자라는 동안은 옳게 아무것도 안 아끼고(스킵 0), 성사율이 80~89% 로
  올라 `_siteMissDay` 자체가 드물다. 200일 판에서 `siteSkip 0`. **살릴지 지울지**는 값 판단이다:
  지우면 `_siteRejS/_siteRejL`·`_siteMissDay`·손잡이 셋이 같이 사라진다(코드 −, 위험 −).
  남길 근거는 영토가 다 자란 만년 세계뿐이다(그 구간은 `test-site-memo` 가 지금 재고 있다). → 재민
* **`test-site-memo` 는 이제 `T230_TERR_HOUSING=0` 세계에서 잰다**(하네스만). 기본 세계의 계약은 ⑤ 절이
  소스로 건다. 기본 세계에서 "싸짐" 을 재는 하네스가 필요하면 그건 **영토가 다 자란 씨앗**을 굽는 카드다. → PM
* 첫 의뢰(#18) · 집도 행위인가(#51) — 그대로.
