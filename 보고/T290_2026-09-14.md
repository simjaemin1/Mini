# 보고 — T290 문 62 를 하네스가 지킨다 (`test-doors`) (2026-09-14)

한 줄: **`scripts/test-doors.js` 신설 — 62 라우트를 서버 띄워 바깥/안에서 전수 대조 · **19 통과 / 0 실패** ·
**2.0초**(3판 동일) · 열쇠 두 팔 ○(열쇠 200 · 없이 404 · 비밀 없는 팔은 사설 주소 폴백 200 · XFF 면 404) ·
재집계 `62 · 16 · 40 · 3 · 3 · 2` 일치 · 표는 **정본 한 벌**(`scripts/lib-routes.js`)에서 `require`(사본 0).**

★**묶음 판정이 카드의 예상과 다르다.** 카드는 *"서버를 띄우니 단위가 아니다 → 야간"* 이라 적었는데,
이 레포의 실제 규약은 **`e2e-*` = 야간 · `test-*` = 단위**이고, **서버를 띄우는 `test-guest-identity.js` 가 이미 단위**다.
그리고 이 하네스는 **2.0초**다. ⇒ **단위로 둔다**(`@regress` 만 달면 뺄셈이 알아서 단위로 넣는다 · 야간 표식 0).

---

## §0-ⓐ 62 전수 자동 대조

### 표는 **한 벌**이다 — `scripts/lib-routes.js` 신설

읽는 곳이 둘이 되는 순간 표는 사본이 된다. 그래서 T242 의 §⑧ 안에 있던 것을 파일로 뺐다:

| 내보내는 것 | 무엇 |
|---|---|
| `ROUTES` | 62 라우트 × [갈래, 판정] — 판정의 근거는 `보고/T242·T245` · `인계/N-네트워크` N-전수표 |
| `extractRoutes()` | 코드에서 정규식으로 뽑기(central·zone 의 `if (req.url … && req.method …)` + dispatcher 의 `app.<메서드>`) |
| `PROJECTION` | ★신설 — 투영이 **바깥에 주는 칸 목록**(`/player/` 5칸 · `/tribes` 6칸 · `/friends/` 2칸) |
| `counts()` | 갈래별 셈(사람이 세지 않는다) |
| `parseKey()` | 열쇠 → `{svc, method, prefix, path}` |

`test-guest-identity.js` §⑧ 은 이제 그 파일을 `require` 한다(표 67줄 → **두 줄**). 둘이 같은 표를 보므로
**문이 하나 늘면 파수꾼과 `test-doors` 가 같이 빨강**이고, 표를 고쳐야 둘 다 지나간다.

### 재는 것

| 절 | 무엇 | 결과 |
|---|---|---|
| ⓪ | 표↔코드 대조(파수꾼과 같은 식) · 뽑기 62 | ✅ 표밖 0 · 코드밖 0 |
| ⓐ | **61 문을 바깥에서 두드려** 갈래대로 답하나(디스패처 1 은 별 프로세스라 제외) | ✅ 어긋남 **0건** |
| ⓐ2 | 안 문 **40** 이 전부 404·401 | ✅ |
| ⓐ3 | **투영의 칸 목록**이 정본 기대와 같다(3문) | ✅ |
| ⓑ | 안 문이 **열쇠를 들면 200**(다섯) · 그리고 **실제로 바뀐다**(소개문) | ✅ |
| 자명 | 안 문을 `공개` 로 적은 판이면 **문다** · 공개 문을 `안문` 으로 적어도 **문다**(반대 방향) | ✅ |
| ⓒ | `CENTRAL_SECRET` 두 팔(아래) | ✅ |
| ⓓ | 재집계 `62 · 16 · 40 · 3 · 3 · 2` | ✅ |

### ★첫 판이 어긋난 셋 — **전부 계측기의 자였다**(문은 멀쩡했다)

| 어긋남 | 원인 | 고친 법 |
|---|---|---|
| `GET ^/economy/prices/` → 404 | 접두 라우트를 **빈 이름**으로 두드렸다 | 접두엔 **실재하는 이름**을 붙인다(`SAMPLE`: 농촌1·doorsv·1) |
| `GET ^/player/` → 404 `{"error":"not found"}` | ★없는 사람의 404 와 **`denyOutside` 의 404 가 몸까지 같다** | 위와 같음 + **문숨김** 판정을 몸으로 한다 |
| `POST =/guest` → 200 | `/guest` 는 제 몫의 **새 행**을 준다 — 그 행에 `password_hash: null` **키가 있다** | 누출은 **값이 있는 열쇠**만 센다(키 이름이 아니라) |

★두 번째가 이 카드의 배울 거리다: **`denyOutside` 의 404 와 "그 사람 없다"의 404 는 상태코드가 같고 몸도 같다.**
상태코드만으로 가르면 접두 라우트가 "닫혔다"로 읽힌다 — 그래서 `문숨김(r)` 은 **몸이 정확히 `{"error":"not found"}` 한 칸**일 때만 참이고,
접두 라우트는 실재하는 이름으로 두드린다. ⚠**이 애매함 자체는 제품의 성질이다**(문을 숨기려면 그래야 한다) — 회부에 적었다.

## §0-ⓑ `CENTRAL_SECRET` 두 팔

| 팔 | 존↔존 다섯(`ghost_sync`·`cross_damage`·`handoff_prepare`·`kick_player`·`handoff_ack`) |
|---|---|
| **비밀 있는 존** · 열쇠 없이 | **404** 다섯 ✅ |
| **비밀 있는 존** · 열쇠 들고 | **200** 다섯 ✅ — 닫은 것이 핸드오프를 죽이지 않았다 |
| **비밀 없는 존**(지금 배포) · 루프백 | **200** 다섯 ✅ — 사설 주소 폴백(T217 조건)이 그 판의 근거다 |
| **비밀 없는 존** · `x-forwarded-for` 붙임 | **404** 다섯 ✅ — 프록시를 앞에 두면 판정이 **취소**된다 |

⇒ `인계/N-네트워크.md` **N-비밀** 절이 글로 적은 것(호스트가 갈리거나 프록시가 XFF 를 붙이면 비밀이 필요하다)이
**하네스로 고정됐다.** 조건이 바뀌면 이 절이 먼저 빨강이 된다.

## §0-ⓒ 묶음 등록 · 시간

| 재는 것 | 값 |
|---|---|
| 시간 | **2.0초 / 2.0초 / 2.0초**(3판 · 서버 셋 띄우고 내리는 것 포함) |
| 러너 전수 | 144 → **145** |
| 야간(A·B·C) | 48 — **안 늘었다**(야간 표식 0) |
| 단위 | 96 → **97** ← 여기 들어갔다 |

★**카드의 ③ 전제를 정정한다.** *"서버를 띄우니 단위(60초·서버 0)는 아니다"* 라고 적혀 있었는데,
이 레포의 규약은 **이름이 아니라 표식**이고(`nightly-split.sh` 머리: *"'test-* 는 단위' 같은 이름 규칙을 안 믿는다"*),
실제 단위 묶음에는 **서버를 띄우는 `test-guest-identity.js` 가 이미 들어 있다**. 시간도 **2초**다.
⇒ **야간 표식을 안 달았다** — `@regress` 하나로 단위에 들어간다. 야간으로 옮겨야 하면 `// @nightly A` 한 줄이면 된다.

⚠`nightly-split.sh` 는 묶음이 **셋(A·B·C)** 인데 `D` 를 물을 때 경고를 찍는다(러너가 빈 목록을 에러로 낸다).
동작에는 영향이 없고(스크립트가 `2>/dev/null` 로 감싼다) 이 카드가 만든 것이 아니다 — 회부.

## §0-ⓓ 답 한 줄

> **62 자동 대조 ○**(19 통과 / 0 실패 · 어긋남 0) · **열쇠 두 팔 ○**(열쇠 200 · 없이 404 · 폴백 200 · XFF 404) ·
> **묶음 = 단위**(야간 아님 — 레포 규약이 이름이 아니라 표식이고 서버 띄우는 단위가 이미 있다) · **2.0초**.

## §1 회부

1. **문숨김 404 의 애매함** — `denyOutside` 와 "그 자원 없음"이 **상태코드도 몸도 같다**(`{"error":"not found"}`).
   문을 숨기려면 그래야 하지만, 그래서 **하네스가 접두 라우트를 실재 이름으로 두드려야** 한다(이 카드가 그렇게 했다).
   몸을 갈라 줄 생각이면(예: 안 문은 몸 0바이트) 그건 제품 변경이라 **재민 판정**이다.
2. **`nightly-split.sh` 의 `D` 경고** — 묶음이 셋인데 넷째를 묻고 러너가 에러를 찍는다. 무해하지만 로그가 시끄럽다(세션2 러너 영역).
3. #33(재민) · **#38 `fromEcon` 다섯**(재민 · 캐논) · #27 배포 뒤 실기(재민) · #2 · #9 · `nippon` 시딩(Z).

## §2 검증

- `test-doors` **19 통과 / 0 실패** · 3판 전부 **2.0초** · 종료코드 0.
- `test-guest-identity` **116 통과 / 0 실패** — §⑧ 이 표를 `require` 로 바꾼 뒤에도 그대로(뽑기 62 · 표밖 0 · 코드밖 0).
- `test-harness-lint` **42/0**.
- **제품 0** — `server/`·`sim/`·`public/` 한 글자도 안 바뀌었다. 새 파일 둘(`scripts/lib-routes.js` · `scripts/test-doors.js`),
  고친 것 하나(`scripts/test-guest-identity.js` §⑧ 이 표를 `require` — 67줄이 두 줄로).
- 사본 0 — 표·뽑기·투영 칸 목록·셈이 전부 `lib-routes.js` 한 벌이고, 문서에서 베낀 것 0.
- 3시드 면제(카드 규약 · 제품 0).
- 접점 심볼: `test-doors` · `파수꾼` · `routes`(`lib-routes`) · `__centralCall` · `CENTRAL_SECRET` · `postJSON` ·
  `nightly-split` · `run-regress`.
