# 보고 — T225 남은 바깥 문 — 관측창 여덟과 이름 갈래 둘을 안 문으로 (2026-09-13)

**푸시 완료 `d17ea9a`** (main) · 가지 `batch/central-doors-0913` → ff → main · **force 0** · **.lock 0**
베이스 `f86bbe3b`(리베이스 1회 · 겹친 파일 0) · 접촉 파일 **6** · 푸시 정본 2판(맥 VM · PAT 는 맥에서만)
★**배포 대기** — T217 과 **같이**, 다만 이번엔 `--all`(존도 만졌다 · §3 회부 1).

한 줄: **바깥에서 남의 것이 나오는 문 13 중 2 — 둘 다 이 카드가 일부러 안 한 것이다**(#2 로그인 UX · #9 는 #27).
나머지 **11 은 전부 ✗**. 안 문은 산다(하네스 무변).
`test-guest-identity` **83/2 → 85/0**(같은 파일을 베이스에서 먼저 돌린 빨강) · 클라 **0** · 새 컬럼 **0** · 랩 교집합 **0**.
★**T216 이 센 열셋보다 더 있었다** — 관측창은 셋이 아니라 **열 개**고, 그중 `/followdbg` 는
**접속자 전원의 `playerId`·이름·정확한 좌표**를 인증 0 으로 줬다(실측).

---

## §0-ⓐ 표 하나 — 남은 문과 **부르는 쪽** (전수 · 실측)

| T216 # | 문 | 고치기 전 바깥 응답 | 부르는 쪽 |
|---|---|---|---|
| 6 | `POST /friend/pending` | 밀린 요청을 보낸 사람들의 **이름** | **존만**(`central-client.friendPending`) · 클라 0 |
| 7 | `POST /tribe/invites` | 받은 부름의 **길드 이름** | **존만**(`tribeInvites`) · 클라 0 |
| 11 | `GET Z /welcomedbg` | 유저 마을 자격 내부값 | 하네스 7 |
| 12 | `GET Z /guilddbg` | 소개문 캐시 | 하네스 1 |
| 13 | `GET Z /lifedbg` | 생활층 내부값 | 하네스 9 |
| **+** | **`GET Z /followdbg`** | **접속자 전원 `playerId`·이름·`x,y`** | 하네스 1 |
| + | `GET Z /friendsdbg?pid=` | 그 사람의 벗 캐시 | 하네스 1 |
| + | `GET Z /claimdbg` | 사유지·부재 상태 | 하네스 0 |
| + | `GET Z /shelterdbg` | 마을 쉼터·도착 지점 | 하네스 3 |
| + | `GET Z /roomdbg` | 방 판정 | 하네스 2 |
| + | `GET Z /routedbg` · `/bodydbg` | (이미 `E2E_GIVE=1` 게이트 뒤 — 운영에선 꺼져 있다) | 하네스 18·6 |

⇒ **관측창은 셋이 아니라 열이다.** T216 은 세 개만 두드려서 그렇게 셌다 — 이 카드가 전수했다.
⇒ **클라는 관측창을 한 곳도 안 부른다**(전수 확인) ⇒ 닫아도 **클라 0**.
⇒ 하네스 아홉이 쓰지만 전부 **되돌이/사설 주소**라 안 문을 그냥 지난다 — **깨지는 것 0**.

## §0-ⓑ 표 둘 — "본인만"의 있는 문법이 **없다**

| 있나 | 무엇 |
|---|---|
| 세션·베어러 | **없다** — central 의 HTTP 는 상태가 없다 |
| 비밀번호 | `/auth` 한 번뿐(매 요청에 실을 것이 아니다) |
| 게스트 토큰 | 있지만 **열쇠**다 — 신분증으로 쓰면 T216 이 잡은 그 사고를 되풀이한다 |
| **ws 접속** | **있다** — 존은 접속 때 이미 본인을 안다(`pendingLines` 가 그렇게 쓴다) |

⇒ #6·#7 은 **본인 확인을 새로 만들지 않는다.** 부르는 쪽이 존뿐이므로 **안 문으로 옮기면 끝**이고,
사람에게 가는 길은 종전 그대로다(존이 ws 로 본인을 알고 대신 묻는다 · 클라 0).

## §0-ⓒ 표 셋 — 열셋 재실측 (고친 뒤 · 하네스가 매번 다시 잰다)

| # | 문 | 바깥 | 판정 |
|---|---|---|---|
| 2 | `/check_username` | `{taken:true}` | ○ **연 채로 둔다** — 로그인 UX(이름이 비었나)이고 사람당 정보는 존재 여부뿐 |
| 3 | `/player/<id>` | 투영 다섯 칸 | ✗ (T217) |
| 4 | `?by=name` | `{n}` | ✗ (T217) |
| 5 | `/friends/<id>` | `{n}` | ✗ (T217) |
| 6 | `/friend/pending` | **401** | ✗ (T225) |
| 7 | `/tribe/invites` | **401** | ✗ (T225) |
| 8 | `/tribe_intros` | 소개문 | ○ **공개가 뜻이다** — 길드장이 시작 화면에 보이라고 쓴 문장 |
| 9 | `?as=<이름>` | 벗/소속 마을 | ○ **이 카드가 안 한다** — #27 로비 순서(대안 ⓐ/ⓑ 는 T216 표) |
| 11·12·13 + 다섯 | 관측창 여덟 | **404** | ✗ (T225 · 존재도 안 알린다) |

⇒ **남의 것이 나오는 문 = 2**(#2 · #9). 둘 다 **판단해서 남긴 것**이고 근거가 위에 있다.
"0/13" 이라고 적을 수 있었지만 그건 사실이 아니다 — #9 는 열려 있고, 그걸 닫는 것이 #27 이다.

**하네스**: `test-guest-identity` **85/0**(베이스 **83/2**) · `e2e-friends` **93/0** · `e2e-guild` **97/0** ·
`e2e-onboarding` **75/0** · `test-handoff-body` **21/0** · `test-newcomers` **56/0** · `test-guest-rejoin` **26/0** ·
`test-harness-lint` **36/0** · `lab-modules --diff` 교집합 **0**.
⚠`e2e-onboarding` 호 표에서 ⑪ 한 걸음이 ●다 — **소속은 초록**(`/곳간` 이 곳간 이름을 부른다 · `아직 마을 사람이 아니다` 없음)이고
**밥이 안 나왔을 뿐**이다(`식량 재고 0 · 마을이 굶는 중이다` — T159 **기근** 문). 이 카드와 무관한 세계 변덕이고,
T202 가 ⑩ 에서 이미 겪어 고친 그 자리다(**기근은 잠금이 아니다**). ⑪ 도 같은 한 줄이면 된다 —
**이 카드가 안 고친다**(보안 카드에 하네스 손대지 않는다) · 다음 카드 몫으로 적어 둔다.

## §1 판정 하나 — 규칙을 **한 곳**에 뒀다

T217 이 `isInternal` 을 central 안에 적었다. T225 는 **존도 같은 규칙**이 필요했다(관측창 여덟).
두 파일에 같은 열 줄을 적으면 그게 사본이고, 한쪽이 느슨해지는 날 그게 다음 구멍이다.
⇒ **`server/internal-door.js` 하나**로 옮기고 central·zone 이 그것을 부른다(사본 0 · 새 컬럼 0).
폴백 규칙(사설 주소 · `x-forwarded-for` 취소)의 근거는 그 파일 머리에 산다.
바깥에는 **404** 를 준다 — 401 은 "여기 문이 있다"를 알려 준다.

## §2 접점

`server/internal-door.js`(새 파일 · 판정 정본) · `server/central.js`(사본 제거 · `/friend/pending`·`/tribe/invites` 안 문) ·
`server/zone.js`(관측창 **열 개**에 빗장 · `require` 한 줄) · `scripts/test-guest-identity.js`(§⑥ 9건).
심볼: `isInternal` `denyOutside` `projectPublic` `x-zone-secret` `/welcomedbg` `/guilddbg` `/lifedbg`
`/followdbg` `/friendsdbg` `/claimdbg` `/shelterdbg` `/roomdbg` `/friend/pending` `/tribe/invites`
`/check_username` `pendingLines`.

## §3 회부

1. **배포**(#27 · 재민) — T217 과 **같이** 나가면 된다. 한 줄은 T217 보고 §4 그대로:
   `cd ~/Mini/durango-mini && bash scripts/redeploy-hanbando.sh --central`
   ⚠**이번엔 `--all` 이 맞다** — T225 는 `server/zone.js` 도 만졌다(관측창 빗장).
2. **#9 `?as=`** — 로비 순서(#27 ⓐ/ⓑ). 이 카드가 안 한다.
3. **남은 무인증 POST 다섯**(T217 회부 1 · `/market/order`·`/market/cancel`·`/war/declare`·`/war/end`·`/tribe/leave`) —
   클라가 직접 부르므로 **클라 인증**이 필요하고 그건 #27 과 한 몸이다.
4. `e2e-onboarding` ⑪ 의 기근 갈래(위 ⚠ · 한 줄) · 별명(#23) · `CENTRAL_SECRET` 넣을지(재민 · 양쪽 통).

## §4 실기

없음 — 배포 뒤 T217 실기 한 줄과 같이(`<central>/player/<네 id>` 에 해시·토큰이 안 보이나).
추가로 한 줄 볼 수 있다: `<zone>/followdbg` 가 **404** 면 관측창이 닫힌 것이다.
