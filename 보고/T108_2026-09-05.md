# T108 — 옛 벽시계 밭 철거 · `RESOURCE_HP` 사본 제거 (2026-09-05 · 영역 Z 주 · C 접점)

> 커밋 `[재민 확정 2026-09-05 · T108]` · 베이스 `320c3997` · 푸시 **`a12b5044`**(`batch/legacy-farm-0905` → ff → main · **force 0회**)
> 접촉 `server/zone.js`(밭 절·자연물 hp 한 줄) · `server/chunk.js`(**export 한 줄**) ·
> `public/client/36-r2-building.js`(**밭 단계 계산 삭제**) · `scripts/test-crops.js`(⑬ 신설).
> **econ diff 0줄 · 새 술어 0 · 주사위 0 · 새 메시지 타입 0.**
> **게임 행동 변경 둘**(아래 §4에 명시): 운석 hp **3 → 6** · 밭 그림이 **심자마자 익음 → 실제 단계**.

---

## 0. 한 줄

**밭에 시계가 둘이었고, 그래서 이미 갈라져 있었다.** T58a 가 밭을 게임일로 옮긴 뒤에도
`Date.now()` 로 자라는 밭이 살아 있었고 — 더 나쁜 건 **클라가 아직 벽시계로 단계를 계산**해서
정본 밭이 **심자마자 "수확가능"** 으로 그려지고 있었다는 것이다. 아무도 그걸 안 보고 있었다.

---

## 1. §0ⓐ — 두 경로의 생사 (그리고 세 번째를 찾았다)

| 경로 | 살아 있나 | 근거 |
|---|---|---|
| ① 마을 시딩 `buildVillageFarmland`(`:1789`) | **한반도 0회 · 다른 존 살아 있음** | `spawnVillagers()` 안에서만 불리고, 그 함수가 `LEGACY_VILLAGES_ON` 에서 조기 반환한다. `zone-config.js:196 legacyVillages:false` 는 **한반도에만** 있다 |
| ② NPC 심기(`:2304`) | **살아 있음** | `spawnNpc` 가 **모든 NPC** 에게 `seed_berry: 2` 를 준다(`:1496`). `decideNpcBehavior` ③ 이 `behavior='plant'` 를 세우고, 그 자리는 `npcLifeTick` 이 거짓을 줄 때 닿는다(영토 없는 마을 · `LIFE_ON` 꺼짐 · canadia NPC) |
| ③ **클라 렌더**(`36-r2-building.js:316`) | **살아 있었고 이미 틀려 있었다** | 아래 |

### ★★③ — 이 카드가 §0 에서 찾아낸 결함

```js
const readyAt = data.readyAt || 0;          // ← 정본 밭엔 readyAt 이 **없다**
const isReady = now >= readyAt;             // ← now >= 0  ⇒ 언제나 참
const growProgress = readyAt > data.plantedAt ? … : 1;   // ⇒ 1
const _st = isReady ? 3 : …;                // ⇒ 3 (익음)
```

⇒ **T58a 이후 모든 플레이어 밭이 심는 순간부터 "익음 · 수확가능"으로 그려졌다.**
서버는 게임일로 옳게 세고 있었고 클라는 벽시계로 세고 있었다 — 시계가 둘이면 언제나
이렇게 **조용히** 갈라진다. 카드가 지시한 철거의 진짜 값은 여기 있었다.

### DB 행 수는 못 셌다 — 그래서 승격을 모양으로 판단하게 썼다

컨테이너의 `world-hanbando.db` 는 4KB 빈 것이다(`farmland` 0행). 실서버 DB 는 배포 호스트에 있다.
⇒ **행 수를 가정하지 않았다.** 승격은 `readyAt`·`plantedAt` 이 있거나 `cropType` 이 카탈로그에
없으면 옛 행으로 판정한다 — 몇 개든, 없든 옳게 돈다.

---

## 2. 무엇을 지웠고 무엇으로 갈아 끼웠나

| 자리 | 종전 | T108 |
|---|---|---|
| 마을 시딩 `:1789` | `readyAt = now + (3-stage)*15초` | **빈 밭**(개간 구획만 — 무엇을 심을지는 철이 정한다) |
| NPC 심기 `:2304` | `cropType:'berry'` + 60초 | **`Crops.wildSeedAt`** 이 그 자리·그 철에 내주는 씨앗(결정론 · 새 술어 0 · 겨울엔 안 심는다) |
| NPC 수확 판정 `:1899` | `now >= readyAt` | `Crops.isReady`(게임일) |
| NPC 수확 실행 `:2316` | 베리 3 + **밭 삭제** | `Crops.harvestUnits` + `SimVillages.cropAfterHarvest`(**밭은 남는다** · 다년생은 그루터기) |
| 매 틱 `ready` 마크 `:9968` | 전 활성 청크 × **매 틱** | **하루 경계 한 번**(밭 그림이 바뀔 수 있는 순간이 거기뿐이다) |
| 플레이어 농지 폴백 `:4168` | 60초 베리밭 | **빈 밭** |
| `tryHarvest` 레거시 꼬리 | 베리 3 + **밭 삭제** | 삭제(승격이 옛 행을 없앤다) |
| `CROP_GROW_MS` 상수·export | 60,000 | **삭제** |
| `cropType` 거울 필드 4곳 | `crop` 과 같은 값 | **삭제**(클라는 `data.crop` 을 읽는다 · T79c) |

### 승격 — 로드 때 한 번, 되돌림 없음

```js
function _promoteLegacyFarm(d, dbId, type) {
  if (type !== 'farmland' || !d) return d;                       // 밭 행만
  const legacy = d.readyAt != null || d.plantedAt != null || (d.cropType != null && !Crops.get(d.cropType));
  if (legacy) { …빈 밭으로… }                                     // 자리·물은 지킨다
  if (d.crop && Crops.get(d.crop) && d.plantedDay == null) { …오늘을 심은 날로… }
}
```

★`'berry'` 는 **카탈로그 34종에 없다**(작물이 아니라 채집물이다) ⇒ 심긴 것으로 못 옮긴다.
**빈 밭**으로 남긴다 — 잃는 것은 자라던 베리 세 알이고, 얻는 것은 **그 자리에 34종을 심을 수 있게 되는 것**이다.
★`readyAt` 은 버린다(서버가 몇 번 재시작했는지 모르니 게임일로 환산할 방법이 없다).
★로그 한 줄: `🌾 옛 벽시계 밭 승격 누계 N칸 — 밭 시계는 게임일 하나다(T108)`.

### 밭 단계는 **정본의 사영**이다 — 클라가 다시 안 센다

```js
function _farmStageOf(d, today) {          // 0갈은흙 1어린싹 2자람 3익음
  if (!d || !d.crop || !Crops.get(d.crop)) return 0;
  const need = Math.max(1, Crops.growDaysOf(d.crop)), got = Crops.grownDays(d.crop, d.plantedDay, today);
  return got >= need ? 3 : Math.min(2, Math.floor(got / need * 3));
}
```

서버가 `data.farmStage` 로 실어 주고(하루 경계 + 밭을 만질 때마다), 클라는 **읽기만** 한다.
**새 메시지 타입 0** — `building_updated` 에 필드 하나다.

---

## 3. `RESOURCE_HP` — 표는 하나, 운석은 여섯

| | zone.js(사본) | chunk.js(정본) |
|---|---|---|
| tree·rock·berry_bush·water_pool·herb·ore | 3·4·2·999·1·5 | 같음 |
| **meteorite** | **없음 → `\|\| 3` 폴백** | **6** |

⇒ **같은 운석이 어디서 났느냐에 따라 hp 가 달랐다.** 청크가 깐 것은 6, `spawnOneResource` 로 나온 것은 3.
zone 의 표를 지우고 `chunk.js` 가 `RESOURCE_HP_TABLE` 을 내주게 했다(export 한 줄). zone 이 읽는 자리는 **1곳**.

---

## 4. ★게임 행동 변경 둘 (명시)

1. **운석 hp 3 → 6** — 사본이 빠뜨린 값을 정본이 채운 결과다(카드가 예고한 그 하나).
2. **밭 그림이 "심자마자 익음" → 실제 단계** — §0ⓐ③ 이 찾은 결함의 수리다. 카드에 없던 것이라
   여기 따로 적는다: 이건 새 기능이 아니라 **클라가 돌리던 두 번째 시계를 걷어낸 결과**다.

그 밖은 전부 같은 일을 다른 시계로 하는 것이라 행동 변경이 아니다 — 단, NPC 밭은 이제
**베리가 아니라 철에 맞는 작물**이 서고(카탈로그 34종), **수확해도 밭이 안 사라진다**(T58b 규약).

---

## 5. 검증

| 하네스 | 결과 |
|---|---|
| `test-crops`(⑬ 신설 · ⑧ 둘 갱신) | **170 / 0** (T99 뒤 150 → +20) |
| `test-farm-life` | 16 / 0 |
| `test-crops-world` · `test-icons` · `test-props` | PASS · PASS · PASS |
| `test-oremix` · `test-mining` · `test-rock-los` | PASS · PASS · PASS |
| `test-itemlabel` | 45 / 0 |
| `test-weight` | 63 / 0 |
| `test-tidal` | 123 / 0 |

**접점 하네스(심볼 `cropType|CROP_GROW_MS|RESOURCE_HP|npc_farm_|seed_berry|farmStage`)**:
`test-crops` · `test-itemlabel` · `test-weight` — **셋 다 돌렸다.**
`zone.js` 를 부르는 하네스는 88개지만 접점이 **밭 절과 `spawnOneResource` 한 줄**이라 러너 전수는 안 돌렸다(규약 · PM 야간).

### ★소스 검사 + 돌연변이

* `CROP_GROW_MS` **0건** · `readyAt`·`plantedAt`·`cropType` 은 **승격 함수의 읽기 한 줄에만**(총 4건 = 그 줄 4건).
* 밭 절에 `Date.now()` **0건**.
* 돌연변이 셋: 표를 한 줄 되살리면 ⓓ 빨강 · `CROP_GROW_MS` 를 되살리면 ⓐ 빨강 · 치환 지점 실재 확인.

### 랩 모듈 적재 추적

```
내 diff 4개 → 교집합 1개: server/chunk.js  ⇒ 베이스 A/B 를 돌렸다
```

⚠카드는 *"`zone.js` 만 — 랩 적재 밖"* 이라 했지만, `RESOURCE_HP_TABLE` 을 내주려고
**`chunk.js` 의 export 한 줄**을 건드리면서 랩 적재 파일이 들어왔다. 식별자 하나를 내보내는 것뿐이라
랩 행동이 바뀔 수 없지만, **그건 논증이고 규약은 측정이다** ⇒ 3시드 A/B 를 돌렸다.

### 3시드 800일 A/B — 여덟 수

계측기 `scripts/t17-metrics.js` · 정본 시드 **1020 · 7 · 42** · 800일 · 베이스 `320c3997`.

```
  === 원시 diff 0줄 ===
```

| 수 | 시드 1020 | 시드 7 | 시드 42 | 공표 기준선(T86 · T94 밀도) |
|---|---|---|---|---|
| 인구 | 6,459 | 7,125 | 6,848 | ✅ |
| 소멸 | 0/51 | 0/51 | 0/51 | ✅ |
| 무기Q | 939 | 1,049 | 1,003 | ✅ |
| 확장셀 | 6,365 | 5,946 | 6,003 | ✅ |
| 게시 의뢰 | 2,563 | 2,710 | 2,760 | ✅ |
| 마을당 일/건 | 1.70 | 1.70 | 1.65 | ✅ (T94) |
| 도구Q | 10,803.3 | 12,009.0 | 11,405.3 | ✅ |
| 보존식 재고 | 740.7 | 606.3 | 870.3 | ✅ |

★이번 0줄은 **자명하다** — 밭은 econ 에 안 닿고(T58a·T91·T99 가 세 번 증명했다),
자연물 hp 는 랩이 안 본다. 그래도 랩 적재 파일을 만졌으니 **재 봤고**, 그게 규약이다.

---

## 6. 착지

* 리베이스 **2회** — 상류(T96 캐릭터 자연화 · T104 하네스 안전장치 · **PM 이 T99 제안 둘을 족보
  (127)~(129)로 올린 공통 갱신**). 충돌 0회 · 내 파일 겹침 0 · 랩 적재 무접촉.
  ⚠1차 시도에서 **작업본을 커밋 안 한 채 리베이스를 걸었다가 거절**당했고, 그 사이 시작한 A/B 는
  베이스와 작업본이 서로 다른 상류를 보고 있었다 ⇒ **죽이고 stash → 리베이스 → 재시작**했다.
  (족보: A/B 를 걸기 전에 **작업본이 최신 베이스 위에 있는지부터** 확인하라.)
* `batch/legacy-farm-0905` → ff 확인 → `main`. **force push 0회.**
* **맥 클론 `.lock` 전수** — push 뒤 `find .git -name "*.lock" -not -path "*/_stale_locks/*"` 전수:
  **3개**(fetch 가 남긴 pack/idx 임시) → `~/Mini/_to_delete/gittmp_0905_t108/` 로 격리 · 재검 **0건**.
  `gc.auto = 0` 확인(T91 이 제안하고 PM 이 건 그 설정 — 201 → 9 → **3** 으로 계속 줄고 있다).

---

## 7. 회부 (구현 금지)

`회부/회부_작물_다음층.md` **⓪-e**. G·T58b 회부 3·T90 회부가 전부 닫혔고, **새로 하나** 열었다:

> **[Z] 클라가 아직 도는 두 번째 시계가 더 있나.** 밭에서 하나를 찾았다(§0ⓐ③).
> 같은 모양이 가마·불·건축 진척에도 있는지는 안 봤다. 그쪽은 벽시계가 **옳은 판단**일 수 있으니
> 회부는 *"어디가 어느 시계인지 한 번 세어 적어라"* 다 — 고치라는 게 아니다. (우선순위 낮음)

