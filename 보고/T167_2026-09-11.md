# 보고 — T167 이방인 받기: 유저 마을을 시작지로 (회부 A-4) (2026-09-11)

베이스 **`2b9f724e`** · 브랜치 `batch/newcomer-guild-0910` → ff → `main` · 커밋 `[재민 확정 2026-09-10 · T167]`
**컨테이너 해시 `afaa0b82`** · **force push 0** · 리베이스 **0회**(상류와 겹치는 파일 0)
⚠**푸시는 아직 못 했다 — 맥 VM 의 바깥길이 막혔다**(§5).

한 줄: **두 카드 연속으로 실측이 전제를 뒤집었다.** 회부 A-4 는 **T19 가 이미 닫았다** —
`server/newcomers.js` 머리말이 그 회부 문장을 그대로 인용하고 있고, 시작 화면도 이미 유저 마을을 싣는다.
⇒ 이 카드는 **정말로 없던 셋**만 지었다.

---

## §0-ⓐ★ 있던 것 / 없던 것 (실측 · 이 카드의 첫 판단)

| 카드가 만들라는 것 | 실측 | 이 카드가 한 일 |
|---|---|---|
| 받기 스위치 | **있다** — `village_hall.data.welcomeStrangers`(T19 제1 규약: 새 표 0 · 새 컬럼 0) | 말만 더했다(아래) |
| central 컬럼 `accept_newcomers` | — | ★**안 만들었다**(아래 ⚠) |
| `startInfo` 에 유저 마을 | **있다** — `if (v.player && !(nc && nc.listed)) continue;`(T19) | **안 건드렸다** |
| 줄의 필드(`founderName`·`intro`·`friendsHere`) | **전부 있다**(T19·T128·T115) | **안 건드렸다** |
| 자격(쉼터·벌점·활동·도착지점) | **있다** — `newcomers.eligibility`, 수는 전부 남의 정본 | **안 건드렸다** |
| 이방인 규약(`tribes.vp` 청정 경계 30) | **있다** — `warDialFromGuildVp` 의 그 선 그대로 | **안 건드렸다**(사본 0) |
| **`/이방인 받기\|막기`** | 종전은 `켜`·`꺼`·`on`·`off` | ★같은 인자 표에 **두 낱말** 추가 |
| **첫 의뢰 NPC 마을만** | **뜻이 없었다**(판이 비어 우연히 null) | ★**명시 게이트** |
| **소개문이 인사로** | **없었다** | ★만들었다 |

⚠★**central 에 컬럼을 만들지 않았다 — 그 판단을 여기 적는다.**
카드는 `accept_newcomers` 컬럼 하나를 지시한다. 그런데 스위치는 **이미 회관에 산다**
(T19 제1 규약이 "새 저장소를 만들지 않는다"고 못 박고, `data.welcomeStrangers` 가 `updateBuildingData` 로 영속된다).
컬럼을 더하면 **스위치가 둘**이 되고, 둘이 갈리는 날 *"켰는데 왜 안 뜨지"* 가 된다 —
그게 이 저장소가 사본을 금지하는 바로 그 이유다. ⇒ **회부 1**(재민 판정).

## §0-ⓑ 유저 마을에서 시작하면 어디가 깨지나 (셋을 따로 쟀다)

| 자리 | 실측 | 판정 |
|---|---|---|
| 도착 지점 | `arrivalOf(vid)` 는 **마을 목록에 있으면** 굽는다(NPC/유저 구분 없음). 게다가 `newcomers.eligibility` 가 **도착 지점 없음**을 자격에서 이미 뺀다 | **안 깨진다** |
| 첫 의뢰 | `pickFirstQuest` → `ledger.board(vid)`. 유저 마을은 econ 게시판이 없어 **빈 판** ⇒ `q = null` | **안 깨지지만 뜻이 없었다** ⇒ 게이트를 명시 |
| 촌장 인사 | `greetLines` 가 `q` 없으면 *"지금은 급한 일이 없네…"* 로 떨어진다 — **유저 마을엔 촌장이 없는데 촌장 말을 한다** | ★**여기가 진짜 구멍**이었다 |
| 안전지대 | 자격 첫 항이 **쉼터 보유**다(`hasShelter`) — 쉼터 없는 마을은 애초에 안 실린다 | **안 깨진다** |

⇒ 셋 중 **하나만** 진짜였다. 그 하나를 고쳤다.

## §0-ⓒ 받기/막기의 영속 · 해체

* 영속: 회관 `data` → `db.updateBuildingData`(건물 영속 정본). central 왕복 0.
* **길드 해체 시 자동 막기는 안 넣었다.** T19 의 자격은 *"길드가 없으면 벌점도 없다 —
  무길드를 죄로 삼지 않는다"* 로 정해져 있다. 해체를 곧 자격 박탈로 만들면 그 규약이 뒤집힌다.
  창설자가 안 오면 **최근 활동** 항이 이미 마을을 내린다(축이 하나다). ⇒ **회부 2**.

---

## §1 만든 것

```
  server/newcomers.js   인자 표 한 줄 — `받기`·`막기`(종전 `켜`·`꺼` 와 **같은 자리**)
  server/onboarding.js  _isPlayerVillage() 하나 · pickFirstQuest 첫 줄 게이트 · greetLines 첫 줄 소개문
  scripts/test-newcomers.js  ⑧절(46 → 56)
```
**클라 0줄.** 시작 화면 줄 표가 그대로 그린다(카드의 목표였고, 실제로 한 줄도 안 필요했다).
**컬럼 0 · 표 0 · 패널 0 · 새 수 0.**

## §2 검증

```
  scripts/test-newcomers.js   56 / 0   ★46 → 56 (⑧ 받기·막기 · 의뢰 게이트 · 소개문 인사)
  scripts/test-membership.js  77 / 0   scripts/test-client-globals.js 26 / 0
  scripts/test-onboarding.js  28 / 0   ★온보딩 서버 계약 정본(첫 의뢰·인사 절을 그 자리에서 잰다)
  scripts/e2e-guild.js        84 / 0   ★카드 지정(소개문 정본이 사는 곳)
```

### ★★자명 통과 금지

| 하네스 | 무엇을 못 박나 |
|---|---|
| ⑧·⑧b | `받기`·`막기` 가 **스위치를 실제로 움직인다**(회관 `data` 까지) |
| ⑧c | 종전 `켜` 도 **그대로 산다**(같은 자리에 더했지 새 경로가 아니다) |
| ★⑧d | 모르는 말은 **스위치를 안 만진다**(형편만 말한다) |
| ★★⑧e | 첫 의뢰가 **유저 마을에서 막힌다** — *판이 비어서가 아니라 뜻으로* |
| ★★⑧e2 | 인사 자리가 **T128 소개문 정본을 부른다**(문장의 집이 하나다) |
| ⑧e3 | 시작 화면은 **받기인 유저 마을만** 싣는다(T19 의 그 줄 — 이 카드가 안 건드렸다) |

⚠**끝까지 못 본 것**: **유저 마을이 실제로 선 판**에서 시작 화면 → 시작 → 소개문 인사까지 이어 보지 못했다.
유저 마을 하나를 세우려면 기여 3회 → 빈터 권리 → 건립 → 인구 → 곳간 3일치까지 가야 하고
그게 `e2e-onboarding` 대본의 **전체 호**다. T128 이 같은 자리에 남긴 회부와 **같은 것**이다 ⇒ 회부 3.

**적재 추적**: `node scripts/lab-modules.js --diff` → **교집합 0개**
(내 diff 셋: `newcomers.js` · `onboarding.js` · `test-newcomers.js`) ⇒ 3시드 800일 A/B 갈음(공통.md §2 ①).

## §3 회부

1. ★**`accept_newcomers` 컬럼** — 스위치가 이미 회관에 있다. 둘로 만들지 마라(§0-ⓐ ⚠). **재민 판정**.
2. **길드 해체 시 자동 막기** — T19 의 "무길드를 죄로 삼지 않는다"와 부딪힌다(§0-ⓒ).
3. **유저 마을 실판 검증** — T128 과 같은 회부.
4. 유저 마을 **첫 의뢰 정본**(지금은 뜻으로 막았다) · 부름 만료(재민) · 길드 패널 · 길드 벌점 층 ·
   인출 공식 재검(T159 회부 1).

## §4 실기 두 줄 (재민)

* 길드장이 회관 앞에서 `/이방인 받기` → 새 캐릭터 시작 화면에 그 마을이 뜨는가(자격을 갖췄다면).
* 거기서 시작하면 **길드장이 쓴 소개문**이 인사로 뜨는가(`/소개` 를 안 썼으면 종전 인사).

## §5 푸시 — **막혔다**(맥 VM egress 403)

| 항목 | 값 |
|---|---|
| 컨테이너 커밋 | **`afaa0b82`** |
| 베이스 | `2b9f724e`(원격 main · 그 뒤로 안 움직였다) · 겹치는 파일 **0** ⇒ 리베이스 불필요 |
| **force** | **0회** |
| 번들 | `~/Mini/t167_2026-09-11.bundle` (9,489바이트 · `2b9f724e..afaa0b82`) |
| 막힌 곳 | 맥 VM 에서 **바깥으로 나가는 길이 통째로 닫혔다** |

```
  git clone https://github.com/…      → fatal: Received HTTP code 403 from proxy after CONNECT
  curl https://github.com             → 000
  curl https://api.github.com         → 000
  curl https://www.anthropic.com      → 000     ← github 만의 문제가 아니다
  (~/mnt/Mini/durango-mini 의 `git ls-remote` 도 같은 403)
```
★**이 세션 안에서 바뀌었다** — T159(같은 날 오전)는 같은 명령으로 얕은 작업 클론을 받아 **첫 시도에 밀었다**.
⇒ 코드·번들·리베이스 문제가 아니라 **그 VM 의 egress 허용 목록**이다.

⚠**컨테이너에서 밀지 않는다.** 컨테이너는 읽기는 되지만, 미는 데 필요한 PAT 는 **맥에서만 읽는다**는 것이
규약이다(토큰이 컨테이너를 거치지 않는다). 길이 막혔다고 그 규약을 깨지 않는다.

**남은 일은 순서 그대로다**(바깥길이 열리는 즉시):
```
  ① git clone --depth 200 … "$HOME/pushrepo"        ② git fetch origin main
  ③ git fetch <번들> refs/heads/main:refs/temp/t167  ④ checkout -b batch/newcomer-guild-0910 → rebase
  ⑤ merge-base --is-ancestor 로 ff 확인(force 0)     ⑥ push batch → fetch origin main → push main
```
⇒ **밀리기 전까지 이 카드는 안 끝난 것으로 센다.** 해시 줄은 착지 뒤에 다시 적는다.
