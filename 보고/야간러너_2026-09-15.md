# 보고 — 야간 러너 2026-09-15 (전수 미완 · 측정 90 / 147)

**기준 커밋 `b8d05d5b`** (`origin/main` · 도는 내내 안 움직였다 — 끝에 다시 `fetch` 해 확인) ·
시작 **04:09 KST** · 종료 **08:20 KST**(약 4시간 10분) ·
등재 **147종**(`grep -l '^// @regress'`) · **측정 90 · 미측정 57** ·
**통과 87 · 실패 3 — 그리고 실패 3 은 전부 단독 재실행에서 초록이다.**

> ⚠**전수가 아니다.** 창이 모자랐다(아래 §3). 미측정 57종은 "초록"이 아니라 **안 쟀다**.
> 공통.md §2 ⑥ 이 이미 적어 둔 대로다 — "전수 143종 · 야간 창은 108~112종까지".

---

## §1 빨강 표

| 하네스 | 러너 연속 | 단독 재실행 | 귀속 |
|---|---|---|---|
| `e2e-charsprite.js` | 실패 (22건 중 ✗6 + 하네스 예외 `TypeError … reading 'slice'`) | **통과 98 / 0** | **픽스처 흔들림**(연속 실행에서만) — 상시 목록 **밖** |
| `e2e-chronicle.js` | 실패 (62건 중 PASS 61 · ⑧ 260px 게이트) | **통과 62 / 0** | **픽스처 흔들림** — 상시 목록 **밖** |
| `e2e-cold.js` | 실패 (55건 중 PASS 54 · **"존 입장 — 월드 안이다 129ms"**) | **통과 55 / 0** (`TIDE_FREEZE_MS=372603`) | **픽스처 흔들림(존 입장 경주)** — 물때 절(⑦㉣)이 **아니다** |

**상시 목록 대조** — 정본은 이제 `scripts/nightly-known.txt` **한 파일**이다(T293 09-14 신설 · 공통.md §2 는 가리키기만 한다).
그 목록은 셋뿐이다: `test-tick-slicer`(기계 의존) · `e2e-rtt`(기계 의존) · `test-body`(물때 두 판정).
**오늘 빨간 셋은 전부 그 목록 밖이다.** 다만 셋 다 단독에서 초록이므로 **제품 회귀의 증거는 없다** — 연속 실행 부하의 자국이다.

⚠`e2e-charsprite`·`e2e-cold` 는 예약 프롬프트의 옛 상시 목록엔 들어 있으나, **정본(`nightly-known.txt`)엔 없다**.
프롬프트 쪽이 낡았다 — 목록은 한 벌이어야 한다(§4 PM 몫).

**귀속 후보 카드** (접점 파일 `git log -3`):

| 하네스 | 최근 접점 커밋 | 카드 |
|---|---|---|
| `e2e-charsprite` | `f5378124` · `c90930f6` · `6cefe5fc` | **T276**(charsprite ⑤ 를 자 결함으로 닫음) · T261 · T250 |
| `e2e-chronicle` | `a66fbb79` · `6a3905c9` · `95bbffbf` | **T268**(⑪b 자 결함) · T238 · T220 |
| `e2e-cold` | `63523050` · `6a3905c9` · `5d1886fe` | **T245**(안 문 23) · T238 · **T235**(무인증 POST 다섯 → 존 경유) |

> ⚠귀속은 **후보**다. 셋 다 단독 초록이라 **"이 카드가 깨뜨렸다"는 증거는 없다.** 못 쟀으면 못 쟀다고 적는다.
> 다만 `e2e-cold` 의 실패 자리가 **존 입장**이고 최근 접점이 **존 경유 인증 이동(T235·T245)** 인 것은 한 줄 값이 있다.

## §2 통과 87종

단위(`test-*`) **84종 전원 초록** · e2e 3종 초록(`e2e-bigmap-live` 11/0 외).
`test-doors`(T290 신설) · `test-audio`(T292 확장) · `test-nightly`(T293 신설) 포함 — **새로 든 하네스 셋 다 초록**이다.

## §3 왜 전수가 안 됐나 — 창과 순서

- e2e 는 한 종이 **5~17분**이다(`e2e-cold` 17분 · `e2e-cutaway` 12분+). 사전순으로 e2e 부터 돌면 **1시간 45분에 7종**이었다.
- 그래서 **순서를 바꿨다**: 단위 `test-*` 98종을 먼저(10분에 21종) → e2e 는 남은 창으로. 같은 시간에 **7종 → 90종**이 됐다.
- 그래도 e2e 42종이 남았다. **이 판은 "단위 전수 + e2e 6종"이다.**
- ★**이 일은 이미 기계가 있다** — T293(`7f262ee`)이 `scripts/nightly-run.sh`·`nightly-report.js`·`nightly-known.txt` 를 09-14 에 착지시켰다.
  **이번 판은 그걸 안 썼다**(예약 프롬프트가 `run-regress.sh` 손 청크를 지시한다). 다음 판부터 `nightly-run.sh` 로 갈아타는 것이 맞다 — §4.

**미측정 57종**: e2e 42 — `downed emptystart events fishing fogray forage-village friends guest-reconnect guild hp inv membership metallurgy mountain move mtcorridor mtcut mtfoot mtfuzz mtocc nature npcsprite onboarding preserve rooms rtt rumor salt spoil2 terrain thirst tidal tilestate trade ui verbs village waterperf weather weight winter zoom` ·
단위 15 — `site-memo spoil2 tame terr-backfill terraincache tick-slicer tidal trade trees valuechain village-found villager-stuck weather weight winter`.

## §4 아침에 세션으로 넘길 빨강

**넘길 제품 빨강은 없다.** 셋 다 단독 초록이다. 대신 **PM 몫 셋**:

- **T276(세션2)** — `e2e-charsprite`: T276 이 ⑤ 를 "자 결함으로 닫음" 했는데, 연속 실행에서 **하네스가 예외로 죽는다**(`TypeError: Cannot read properties of undefined (reading 'slice')` · ✗6). 단독은 98/0. **닫힌 것은 ⑤ 한 자리고, 연속 실행 경로는 아직 무방어다** — 상시 목록에 올릴지 자 결함으로 또 닫을지 판정 필요.
- **PM** — `nightly-known.txt` 가 정본이 됐는데 **예약 러너 프롬프트의 상시 목록이 낡았다**(`e2e-charsprite`·`e2e-cold` 를 상시로 적고 있다). 사본 0 규약대로 프롬프트가 그 파일을 **가리키게** 고칠 것.
- **PM** — 야간 창 처리(`판정대기` #25). 오늘 실측: **사전순 e2e 선행 = 7종 / 단위 선행 = 90종.** T293 의 `nightly-run.sh` 로 갈아타고 e2e 는 이틀 분할(T220 `95bbffbf` 가 이미 소요로 가른 표를 갖고 있다).

## §5 이 판의 사실

- 러너 자가 검사(`--selftest`) **통과** 뒤 시작.
- 병렬 0(2코어 · 순차 단독). 러너 두 개가 잠깐 겹쳤던 자리 하나 — 첫 손 청크에서 `timeout` 이 바깥 bash 만 죽이고 `node` 자식이 살아남아 다음 청크와 **고정 포트(3010/3020)를 두고 겹쳤다**. 발견 즉시 `kill <pid>` 로 정리하고(pkill 0) **그 구간 결과는 버렸다** — 위 수는 겹침 없는 판의 것이다. ⇒ 족보 값: **`timeout` 은 프로세스 그룹을 안 죽인다.**
- 코드 무접촉(이 보고 파일 하나) · 맥 클론 무접촉.
