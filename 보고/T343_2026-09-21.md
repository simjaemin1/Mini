# T343 — 닛폰 첫 마을 (세션5 · 2026-09-21 · **가지만**)

> 카드 `~/Mini/지시_T343.md` · 가지 `batch/nippon-first-0921` · 베이스 `af0bc9a3`(main) ·
> 만진 것: `server/terrain.js`(함수 하나 신설 + export) · `server/villages.js`(두 자리 — 조기 반환 삭제 · 후보를 정본 문으로) ·
> `scripts/test-nippon-boot.js`(신설) — **새 수 0 · 새 표 0 · 사본 0 · 손잡이 0 · 배포 0.**

## ★ 한 줄 답 (머리 15줄)

> **닛폰에 마을이 섰다 — `villages.js` 조기 반환 한 줄을 지우고, 후보를 묻는 문을 하나로 모았다.
> 마을 **8**곳 · econ 인구 64 · 장부 · 교역행렬 8마을 28쌍 · **부팅 오류 0**.
> 한반도는 **한 칸도 안 바뀌었다** — 3시드 여덟 수 전부 기준선 넷째 판과 일치, 후보 배열은 **같은 객체**.
> 결손 셋은 그대로다: 광맥 `O0.1` 8/8 · 도달불능 **19/28쌍** · 마을 정본 0곳. 새 하네스 22/0.**

1. **제품 접촉은 두 자리뿐이다.**
   · `villages.js:2533` `if (ZONE_ID !== 'hanbando') return;` **삭제**(대신 아무 게이트도 안 놓았다).
   · `villages.js:1138` 후보를 `terrain.getZoneVillages` → **`terrain.siteCandidates`** 로 묻는다.
2. **`siteCandidates` 는 새 표가 아니라 있던 두 길에 세운 문이다**(`terrain.js`):
   정본 json 에 **찍어 둔 칸**이 있으면 그것(한반도 51) · 없으면 **같은 꼴을 내는 절차 배치기**
   (`chunk.generateVillagesForZone` — 존 하나당 `villageSeed` 결정론). `zone.js:645`(레거시 마을)가
   **이미 그 규칙을 쓰고 있었다** — 베끼지 않고 이름만 붙였다.
3. **한반도는 첫 갈래라 한 칸도 안 움직인다.** 하네스가 그것을 **같은 배열 객체**(`===`)로 잰다 —
   "길이가 같다"로는 사본도 통과하기 때문이다.
4. **새 게이트를 안 놓았다.** 바다 존 **7개 전부 후보 0곳**이다 —
   `generateVillagesForZone` 첫 줄이 `if (zone.isOcean) return []` 이라 **스스로 빠진다**.
   존 이름 조건을 한 번 더 쓰면 그게 사본이다.
5. **닛폰 실측**(제품 기본값 · env 손잡이 0): 후보 16 → 선별 **10**(품질 게이트가 6곳을 뺐다) →
   **시딩 8**(물 위 2곳 — 물리) · `사건 장부 마을 8곳 프라이밍` · `교역 BFS 8마을 28쌍` ·
   `마을 시뮬 준비: 마을 8, econ 인구 64` · **로그 오류 0 · stderr 예외 0**.
6. ⚠**T336 실험판(14곳)과 다르다 — 그때는 `SEED_ALL=1` 이었다.** 제품 기본값은 품질 게이트가 살아 있어 **8곳**이다.
   닛폰에 `seedAllVillages` 를 달지 **않았다**: 그건 "전수로 갈 것인가"라는 **정책**이고, 한반도는 51곳 실측
   (800일 3시드 생존 47) 을 근거로 켰다. 닛폰엔 그 근거가 아직 없다 — 새 수 0 규약 그대로 **안 지어냈다**.
7. **결손 셋은 이 카드가 안 고쳤다**(카드 지시): 광맥 `O0.1` **8/8** · 도달불능 **19/28쌍(68%)** · 마을 정본 0곳.
8. ⚠**카드 전제 하나가 레포와 달랐다**(§0-ⓐ) — "한반도 51 도 그 함수로 뽑았나"의 답은 **아니다**.
   에디터 v9 에서 **사람이 찍은 것**이다. 그래서 "같은 함수"는 없었고, 있는 것은 **같은 꼴을 내는 두 길**이었다.

---

## §0-ⓐ ⚠카드 전제 대조 — "한반도 51 도 그 길로 뽑았나"

| | 카드가 상정한 것 | 레포 실제 |
|---|---|---|
| 후보 함수 | 지형 정본 `villages` 칸이 0 이면 **그 함수가 지형에서 뽑는다** | 그런 함수는 **없었다**. `getZoneVillages` 는 정본 칸을 **읽기만** 한다 |
| 한반도 51 의 출처 | 같은 함수? | **에디터 v9 — 사람이 찍었다**(`zone-config.js:182` · `villages.js:6` 주석이 그렇게 적어 뒀다) |
| 근거 | — | 이름이 `광산1~7 · 어촌1~16 · 농촌…` 로 **손수 지은 이름**이다. 절차 배치기는 `makeVillageName`(음절 표)으로 `이즈사키·지옹로` 같은 이름을 낸다 |
| 그럼 무엇이 있었나 | — | **같은 꼴을 내는 둘째 길**이 이미 있었다: `chunk.generateVillagesForZone` — `[{name,x,y,type}]` · 타입도 같은 넷(plain·riverside·forest·mining) · `zone.js:645` 가 레거시 마을에 이미 쓰고 있다 |

⇒ **없는 함수를 지어내지 않고, 있던 두 길에 문 하나를 세웠다.** 새 표 0 · 새 수 0.
`getZoneVillages` 의 **뜻은 안 바꿨다** — 랩·계획기 20여 곳(`econ-lab-real`·`plan-bridges`·`build-map`·
`audit-terrain-quality`…)이 "정본 json 이 가진 칸"이라는 뜻으로 부르고 있어서, 거기에 절차 마을이 섞이면
**아무도 안 시킨 세계가 하나 생긴다**.

## §0-ⓑ 고친 두 자리 — 전문

```js
// server/terrain.js  (신설)
function siteCandidates(zoneId) {
  const hard = getZoneVillages(zoneId);
  if (hard && hard.length) return hard;              // ← 한반도: 그 배열 **그대로** 돌려준다
  const metas = _getZonesMeta(); const z = metas && metas[zoneId];
  if (!z) return [];
  try { return require('./chunk').generateVillagesForZone(Object.assign({ id: zoneId }, z)) || []; }
  catch (e) { return []; }
}
```
```diff
// server/villages.js  seedVillages
-  const hard = terrain.getZoneVillages(state.zoneId) || [];
-  if (!hard.length) { … `🏘️ 시딩 스킵 — 하드코딩 마을 없음`; return []; }
+  const hard = terrain.siteCandidates(state.zoneId) || [];
+  if (!hard.length) { … `🏘️ 시딩 스킵 — 마을 후보 0곳`; return []; }

// server/villages.js  init
-  if (ZONE_ID !== 'hanbando') return; // §4-4: 1차 이식은 hanbando 존만 (일반화는 Stage 4+)
+  // (지웠다. 새 게이트 0 — 바다 존은 후보 0곳으로 스스로 빠진다)
```

## §0-ⓒ 닛폰 부팅 — 실측

| | T336(진단 · 정본 그대로) | T336 실험(`SEED_ALL=1` · 사본) | **T343(제품 · env 0)** |
|---|---|---|---|
| 후보 | — (문이 안 열렸다) | 16 | **16** |
| 선별 | — | 16(게이트 우회) | **10**(품질 게이트가 6곳을 뺐다) |
| 시딩 | — | 14 | **8**(물 위 2곳 — 물리) |
| econ 인구 | — | 112 | **64** |
| `/health villages` | **null** | 14 | **8** |
| 교역행렬 | — | 14마을 91쌍 · 도달불능 60 | **8마을 28쌍 · 도달불능 19** |
| 부팅 오류 | 0 | 0 | **0** |

**시딩한 8곳의 땅**(`land(F/W/S/O/우드)`):

| 마을 | F(비옥) | W(물) | S(돌) | **O(광맥)** | 우드 | 논/밭 |
|---|---|---|---|---|---|---|
| 노라마 | 1.12 | 1.58 | 0.25 | **0.1** | 0.45 | 42/18 |
| 이즈이 | 1.10 | 1.58 | 0.25 | **0.1** | 0.58 | 41/18 |
| 지옹로 | 1.10 | 1.55 | 0.25 | **0.1** | 0.45 | 41/19 |
| 타카쿠 | 1.08 | 1.36 | 0.25 | **0.1** | 0.45 | 44/24 |
| 쿠라다 | 0.99 | 1.55 | **2.5** | **0.1** | 0.94 | 48/22 |
| 네코로 | 0.94 | 0.88 | 0.25 | **0.1** | 0.90 | 0/87 |
| 지옹사키 | 0.83 | 0.86 | 0.25 | **0.1** | 0.45 | 0/93 |
| 마트노 | 0.53 | 0.65 | 0.25 | **0.1** | **1.77** | 0/139 |

⇒ **식량은 된다**(논이 깔린 곳 5). **광맥은 8/8 바닥** — 도구·청동 사슬이 영영 안 돈다.
돌도 7/8 이 바닥(쿠라다만 2.5), 임업은 마트노 하나뿐. **다리가 없어 28쌍 중 19쌍이 서로 못 간다.**
이 셋은 카드가 "고치지 않는다"고 한 것이고, T336 §0-ⓓ 의 카드 ②·③·④ 몫이다.

## §0-ⓓ 새 하네스 `scripts/test-nippon-boot.js` — 22 / 0

두 가지를 재고, **섞지 않는다**: ⓐ~ⓓ 후보 정본(부팅 0 · 값싸다) · ⓔ 실서버(존 하나 부팅 · 도는지를 본다).

| 절 | 재는 것 | 실측 |
|---|---|---|
| ⓐ | 한반도 후보 = 정본 칸 **같은 배열 객체**(사본 0) | 51곳 · 동일 객체 |
| ⓑ | 정본 칸 0곳인 존에도 후보가 서고 **꼴이 같다** · 두 번 물어도 같다 | 닛폰 16곳 · 타입 넷 안 · 결정론 |
| ⓒ | 바다 존은 스스로 빠진다(새 게이트 0) | **7개 전부 0곳** |
| ⓓ | 존 이름 조기 반환이 코드에 없다 · 시딩이 정본 문으로 묻는다 | 0건 |
| ⓔ | **실서버 부팅** — 시딩 줄 · 장부 · 교역행렬 · `villages ≥ 1` · 로그 오류 0 · stderr 예외 0 | `villages=8` · econ 64 · 오류 0 |
| ⓔ10 | 선별−시딩 차이 = **물 위 스킵 수**(조용히 사라진 마을 0) | 10−8 = 2 · 스킵 줄 2 |

★**자명 통과 금지**가 이 자의 뼈대다 — "마을 0곳인데 오류도 0"을 초록으로 읽지 않으려고
전제 셋(ⓔ2·ⓔ3·ⓔ4)을 먼저 건다. T336 이 잡은 상태가 정확히 그 **조용한 0** 이었다.

⚠**이 자의 1차 판이 스스로 틀렸다**(족보 156/170 — 자를 먼저 의심하라). `stderr 가 비어 있다`를 걸었는데
`console.warn` 이 거기로 간다 — 그래서 **제품이 옳게 일한 줄**(`[이즈사키] 주변 24셀 내 뭍 없음 — 스킵`)이
빨강이 됐다. 그건 품질 기준이 아니라 **물리**다(한반도도 어촌6 을 그렇게 건너뛴다).
⇒ 재는 것을 바로잡았다: stderr 에서 보는 것은 **예외의 자취**(스택 프레임·Error 꼴)뿐이고,
스킵은 ⓔ10 에서 **세어서** 판정한다. (린트 ②도 한 번 물었다 — 판정 정규식의 이모지 셋을 말로 바꿨다 · T84.)

## §3 검증

| | |
|---|---|
| **한반도 3시드**(`t17-metrics` 800일) | **여덟 수 전부 기준선 넷째 판과 일치** — 아래 표 |
| `test-nippon-boot`(신설) | **22 / 0** |
| `test-psite-server` | **PASS**(무변) |
| `test-resource-index` | **20 / 0**(무변) |
| `test-war-world` | **54 / 0**(무변) |
| `test-harness-lint` | **44 / 0**(새 하네스 포함 154파일) |
| `lab-wiring-check` | 실패 0 · 경고 0 |
| 제품 diff | `server/terrain.js`(+28) · `server/villages.js`(+16/−3) · `public`·`sim` **빈 칸** |
| T340·T341·T342·T329 | **무접촉** |
| 배포 | **0** · force **0** |

**한반도 3시드 — 비트 동일**

| 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 |
|---|---|---|---|---|---|---|---|---|
| 1020 | **8318** | **0/51** | 1306 | 9602 | 2504 | 16596.5 | 4284.2 | 74094.9 |
| 7 | **7935** | **0/51** | 1275 | 9892 | 2151 | 14638.6 | 4719.3 | 74291.4 |
| 42 | **7853** | **0/51** | 1356 | 9790 | **2361** | 15293.9 | 3885.9 | 76117.9 |

⇒ 기준선 넷째 판(`인계/공통.md §2`: 1020=8,318 · 7=7,935 · 42=7,853 · 소멸 0/51 · 42 게시 2,361)과 **한 칸도 안 다르다**.
당연한 결과다 — 한반도는 `siteCandidates` 의 첫 갈래라 **같은 배열을 같은 순서로** 받는다(ⓐ가 그걸 `===` 로 잰다).

**접점 심볼**: `ZONES.nippon` · `hanbando-terrain.json` · `seedAllVillages` · `villages.js:2533` ·
`siteCandidates` · `getZoneVillages` · `generateVillagesForZone` · `pickSeedVillages` · `seedVillages` · `ores` · `bridges`

## §4 회부만 (구현 0)

1. ★**닛폰에 `seedAllVillages` 를 켤 것인가** — 지금은 품질 게이트가 살아 있어 후보 16 → 시딩 8 이다.
   한반도는 **51곳 800일 3시드 실측**(생존 47)을 근거로 켰다. 닛폰엔 그 근거가 아직 없다 ⇒ 재지 않고 안 켰다.
   재는 카드가 먼저다(800일 랩을 닛폰 땅으로 한 번).
2. **결손 셋**(T336 §0-ⓓ 카드 ②③④): 광맥 정본 · 마을 정본 · 다리. 이 카드는 **손대지 않았다**.
3. 닛폰 지형이 이름과 어긋난다(열도 아님 · 산 아님) — **재민 결정 사항**(T336 회부 그대로).
4. `getStoneMultiplier` 가 정본 `ridges` 를 안 읽는다 — 한반도도 같다(T336 회부 그대로).
5. 청크 경계 감사거리(T317 ①) · 광맥 없는 43마을(#47).

## §5 푸시

| | |
|---|---|
| 가지 | **`batch/nippon-first-0921`** (가지만 — main 은 PM) |
| 베이스 | `af0bc9a3` |
| 길 | 맥 VM `device_bash` · `$HOME/pushrepo` 얕은 클론 · PAT 는 셸 변수만 · 즉시 `unset` |
| force | **0** |
