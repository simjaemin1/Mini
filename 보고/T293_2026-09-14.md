# T293 — 야간 러너를 **판단 없는 기계**로 (2026-09-14)

**푸시 해시 `<PUSH>`** · 가지 **`batch/nightly-machine-0914`** · 측정 베이스 `f6a0a537` ·
**CI 초록** <https://github.com/simjaemin1/Mini/actions/runs/34831963255> (`core` success · 게이트 셋 **55초** · 단위 25종 155초) ·
**제품 0** · 러너 둘(`run-regress.sh` · `nightly-split.sh`) **diff 0** · force 0 · `.lock` 0.

## ★ 한 줄 답

> **nightly-run 실측 — 짧은 목록 3종 44초 · `state` 가 `starting → running k/N · <이름> → done` 으로 흐른다 ·
> 빨강 자동 단독 재실행 1/1 · 이중 실행 거부 rc=3(로그 0줄) · `auto` 는 KST 로 묶음 C 114종을 뽑았다 ·
> report 픽스처 2/2(하네스 29/0 · 같은 로그 1938B 바이트 동일) · CI 게이트 셋 55초 초록**

**야간 세션에 남는 일은 셋뿐이다**: ① `nohup bash scripts/nightly-run.sh &` ②
`cat /tmp/night/regress.log.state` 를 드물게 ③ 보고의 **"귀속: 모델 몫"** 칸 채우기(09-14 실측 0건).
통과 세기 · 단독 재실행 · 상시 목록 대조 · 표 작성은 **기계가 한다**.

---

## §0-ⓐ `nightly-run.sh` 실측

| 잰 것 | 값 |
|---|---|
| 짧은 목록 2종(`test-calendar` · `test-kcal`) | 시작 19:02:25 → `done` **27초**(러너 `drain` 8초×3 포함) · 보고 자동 생성 |
| 짧은 목록 3종 · `nohup` 백그라운드 | **44초** · stdin 안 읽음(멈춤 0) |
| `$LOG.state` 전이(4초마다 폴링) | `starting` → `running 0/3 · (기동)` → `running 1/3 · test-calendar.js`(12초) → `2/3 · test-kcal.js`(20초) → `3/3 · test-icons.js`(28초) → **`done`**(44초) |
| 빨강 자동 단독 재실행 | 일부러 빨간 하네스를 목록에 넣으니 `--reds` 가 집어 `$LOG.retry` 에 단독 1판 · 보고 표에 `여전히 빨강` + `**귀속: 모델 몫**` |
| 이중 실행 거부 | 둘째 **rc=3** · `이미 돌고 있다(pid …)` · **로그를 한 줄도 안 썼다** |
| 낡은 `.pid`(죽은 프로세스) | **그냥 돈다** — "언제나 잠긴 문"이 아니다(자명 통과 금지 · 하네스 ⑥) |
| `auto` 경로(`NIGHT_DRY=1`) | `[auto] 2026-09-14 · 묶음 3개 → **C**` · `단위 98 + e2e 16 = **114종**`(전수 146종) · **KST 로 뽑았다** |

**설계 세 줄.**
* **목록을 안 들고 있다** — `nightly-split.sh auto --list-only` 에게 묻고 **글자**(`C`)를 받아 그 글자로 돌린다.
  두 번 물어 목록이 갈리는 자리를 없앴다(사본 0 · 러너 둘 무접촉).
* **줄마다 시각을 찍는다** — 보고의 시작·종료가 여기서 나온다(모델이 안 센다).
  ⚠`awk` 로 찍으면 안 된다: 입력 버퍼링 때문에 머리가 몇 분 늦는다(T220 실측). bash `printf '%(...)T'` 는 포크가 0이다.
* **빨강 판정을 제가 안 한다** — `nightly-report.js --reds` 에게 묻는다. 파서가 둘이면 갈린다(러너 둘이 갈렸던 08-26 이 선례).

**TZ.** 기본을 `Asia/Seoul` 로 잡았다 — 09-14 야간 보고가 적은 그 자리다(컨테이너 기본 UTC 로 두면
`auto` 가 **하루 어긋난 묶음**을 돈다). 스크립트가 아니라 **환경변수**로 준다(`NIGHT_TZ`).

## §0-ⓑ `nightly-report.js` — 픽스처 둘

`scripts/test-nightly.js`(@regress · 서버 0 · **3.9초** · **29/0**)가 잰다.

| 픽스처 | 넣은 것 | 나온 것 |
|---|---|---|
| ① | 빨강 셋 — ⓐ 상시 목록에 있는 것 · ⓑ 단독에서 초록이 되는 것 · ⓒ 남는 것 · + 미측정 1 · 끊김 1 | 한 줄 답 **상시 밖 빨강 1건** · ⓐ `**상시(기계 의존)**` · ⓑ `★**단독 초록**` · ⓒ `**귀속: 모델 몫**` + §4 에 그 하나만 |
| ② | 전부 초록 | **0건** · 표 `빨강 0종` · ①의 이름이 안 샌다 |

**자명 통과 금지 넷**(이 절이 진짜 자인지):
1. ②가 0건이라고 답한다 — ①의 초록이 "언제나 1건"이 아니다.
2. **상시 목록에서 한 줄을 빼면** 그 하네스가 `모델 몫`으로 옮겨 간다(1건 → **2건**) — 목록을 진짜로 읽는다.
3. 같은 로그 두 번 = **1938B 바이트 동일** · 다른 로그면 다르다(1938B vs 833B).
4. 낡은 `.pid` 면 이중 실행 문이 **열린다**.

**결정성의 근거를 코드에서 센다** — 보고기 **코드**에 `Date.now()`·`new Date()`·난수가 하나도 없다
(주석은 빼고 센다. 주석까지 세면 걸린다는 것도 같이 잰다 — 그래서 뺐다는 증거다).
시각은 전부 로그 도장에서 온다.

**이름 규칙을 안 믿는다** — `test-`·`e2e-` 로 안 가른다. `이상한이름.js` 도 같은 자로 센다.
**끊김은 빨강이 아니다** — 열렸는데 안 닫힌 블록(창이 닫힌 자리)은 재실행 목록에 **안** 넣는다.

## §0-ⓒ CI 게이트 셋

| 스텝 | 결과 | 시간 |
|---|---|---|
| 러너 자가 검사 | success | 0초 |
| **게이트 셋**(`build-econ-bundle --check` && `inline-engine --check` && `lab-wiring-check`) | **success** | **55초** |
| 단위 하네스 **25종**(24 → 25 · `test-nightly.js`) | success | 155초 |

지금까지 **카드마다 세션이 손으로 돌리고 출력을 대화에 쌓던** 셋이다. 야간 표식(`@nightly`)엔 안 얹었다.
⚠같은 판에서 `watch` 잡(`test-tidal` · `test-events`)도 **초록**이었다 — `core` 로 옮길지는 PM(사실만 적는다).

## §0-ⓓ 한 줄 답

> **nightly-run 실측 3종 44초 · state 전이 확인 · 재실행 1/1 · 이중 실행 rc=3 · report 픽스처 2/2 ·
> CI 게이트 셋 55초 초록**

---

## §3 검증

| | |
|---|---|
| `test-harness-lint` | **42 / 0** |
| `test-nightly` (새 하네스) | **29 / 0** · 3.9초 · 서버 0 |
| `run-regress.sh --selftest` | 초록 |
| `--list "nightly A\|B\|C"` | **16 / 16 / 16** — 무변 |
| 러너 둘 diff | **0** (`run-regress.sh` · `nightly-split.sh`) |
| 제품 diff | **0** |
| 만진 것 | `scripts/nightly-run.sh`(새) · `scripts/nightly-report.js`(새) · `scripts/nightly-known.txt`(새) · `scripts/test-nightly.js`(새) · `.github/workflows/regress-unit.yml`(스텝 1 + 목록 1줄) |
| 접점 하네스 | `grep -lE "(exec|spawn)\(…(run-regress\|nightly-)…"` → `test-nightly.js` 하나. ⚠단순 `grep -l "run-regress"` 는 **132종**을 문다(거의 모든 하네스 머리에 러너 이름이 적혀 있다) — 그 목록은 접점이 아니라 주석이다. 그래서 **부르는 자리**로 좁혔고, 그와 별도로 `test-harness-lint`(러너 `--list`·`nightly-split` 을 읽는다)도 돌렸다. |
| 무접촉 | `T290`(세션7 `test-doors`) · `T292`(세션9 `test-audio` — 같은 CI 파일 · 그 줄 그대로 살렸다) · `~/mnt/Mini/durango-mini` |

**접점 심볼**: `nightly-split` · `run-regress` · `nightly-run` · `nightly-report` · `nightly-known` ·
`regress-unit.yml` · `build-econ-bundle` · `inline-engine` · `lab-wiring-check` · `TIMEOUT_SEC` · `DRAIN_SEC`

---

## §4 회부 / PM 몫

### ★ 공통 갱신 제안 (PM 이 옮긴다 — 이 카드는 `인계/공통.md` 를 안 만졌다)

`인계/공통.md` §2 ⑥(:200)의 이 대목

> 상시 빨강은 셋(`test-tick-slicer` · `e2e-rtt` · `test-body` 물때 — `e2e-charsprite`·`e2e-cold` 는 초록이라 뺀다).

를 **한 줄로** 바꾼다(사본 0 · 목록의 정본은 파일 하나다):

> 상시 빨강 목록의 정본은 **`scripts/nightly-known.txt`** 하나다(`nightly-report.js` 가 그것만 읽어 야간 보고의 귀속 칸을 채운다). 올리고 내리는 것은 PM 이다 — 근거 칸에 판단한 카드·보고를 적는다.

지금 그 파일에 옮겨 둔 셋(그대로 옮겼다 · 내가 더하거나 빼지 않았다):
`test-tick-slicer`(기계 의존) · `e2e-rtt`(기계 의존) · `test-body`(물때 두 판정 `⑯㉧` · `TIDE_FREEZE_MS` 로 306/0).

### 그 밖

1. **전수 종 수가 또 늘었다 — 146종**(공통.md 세 곳의 "143종" · 09-14 야간 보고가 144 로 정정 제안한 그 자리). PM.
2. **T276 이 올린 상시 후보 넷**(`npcsprite` 프레임 비용 · `weather ⓕ` · `charsprite` 시트 래치 · `test-lab-psite ⑧`)은
   **안 넣었다** — 올리는 것은 PM 몫이라고 파일 머리에 적었다. 넣으면 그 밤부터 자동으로 "상시"로 적힌다.
3. **[H] 랩 기본 = 서버 기본 대조가 CI 에서 도는 순간부터 "게이트 셋" 줄은 "CI 초록 링크"로 갈음** — 그 규약 문장은 PM 이 §2 에(카드 지시 그대로 · 이 카드는 스크립트만 냈다).
4. **망고링고 v3 프롬프트**(PM · 재민 "ㅇ" 뒤) — 지금 프롬프트는 청크로 러너를 부른다. 셋으로 줄일 수 있다(위 한 줄 답).
5. 야간 러너를 **맥 self-hosted 러너**로 옮기는 것(재민 · 에이전트 설치가 사람 손) · 3시드 800일 CI 잡(분 예산·공개 여부 재민).
6. `watch` 잡 둘이 이 판에서 **초록**이었다 — `core` 로 옮길지 PM.

## §5 푸시

<PUSHSEC>
