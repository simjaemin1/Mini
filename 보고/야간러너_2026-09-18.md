# 보고 — 야간 러너 2026-09-18 (+0900)

**기준 커밋 `b8b49a16fdf478ddb64e67106aa0f17ef8cf175e`** · 시작 **06:24** → 종료 **09:48** (3시간 24분)
· 오늘 묶음 **A** · 목록 **115종** · 돈 것 **115** · 통과 **108** · 실패 **7** · 끊김 **0** · 미측정 **0**
· 이 문서는 `scripts/nightly-report.js` 가 로그만 읽어 냈다 — **모델 판단 0** · 같은 로그면 바이트가 같다
· 로그 `/tmp/night/regress.log` · 단독 재실행 `/tmp/night/regress.log.retry` · 상시 목록 `scripts/nightly-known.txt`

## ★ 한 줄 — **상시 밖 빨강 6건** — `e2e-charsprite` · `e2e-conn` · `e2e-fogray` · `e2e-mtfuzz` · `e2e-rumor` · `e2e-trade`

## 1. 실패 표

| 하네스 | 첫 판 | 소요 | 단독 재실행 | 귀속 |
|---|---|---|---|---|
| `test-tick-slicer` | RC=1 · 결과줄 27개 · ⑥a ★조각내기의 루프 최대 막힘이 **가장 큰 조각 안**이다(무조건 · 슬라이서가 소유하는 선)  11718.89ms ≤ 창 전체 최대 조각 926ms(ore) × 2 + 300 = 2152ms | 9분 | 여전히 빨강 (RC=1 · 결과줄 27개 · ⑥a ★조각내기의 루프 최대 막힘이 **가장 큰 조각 안**이다(무조건 · 슬라이서가 소유하는 선)  11131.68ms ≤ 창 전체 최대 조각 899ms(ore) × 2 + 300 = 2098ms) | **상시(기계 의존)** — 공통.md §2 ⑥ (T185 2026-09-12) · 부하 아래서만 · 야간 09-14 단독 22/2 |
| `e2e-charsprite` | RC=1 · 결과줄 107개 · ★★대조군 — **장착 안 하면 그 자리 화소가 안 바뀐다**(같은 애니 프레임 · 무풍)  2030px ≤ 8 | 3분 | 여전히 빨강 (RC=1 · 결과줄 107개 · ★★대조군 — **장착 안 하면 그 자리 화소가 안 바뀐다**(같은 애니 프레임 · 무풍)  1857px ≤ 8) | **귀속: 모델 몫** |
| `e2e-conn` | RC=1 · 결과줄 48개 · ★★② 클라가 **확정 오류**를 받았다(침묵이 아니다)  entering | 6분 | ★**단독 초록** (RC=0) | **귀속: 모델 몫** |
| `e2e-fogray` | RC=1 · 결과줄 11개 · R1: 손잡이가 실제로 갈렸다 (A 통 true · B 통 true) | 8분 | ★**단독 초록** (RC=0) | **귀속: 모델 몫** |
| `e2e-mtfuzz` | RC=1 · 결과줄 11개 · P4 ★덮개 — 남은 바위는 여전히 산 아래  — ① 2% 초과 1회 | 10분 | ★**단독 초록** (RC=0) | **귀속: 모델 몫** |
| `e2e-rumor` | RC=1 · 결과줄 56개 · ⑦d2 부재 일수가 **실제로 비운 기간**에 맞는다(입장 직후 재접속이 기준일을 지우지 않는다)  보고 4일 vs 실제 32일 | 5분 | 여전히 빨강 (RC=1 · 결과줄 56개 · ⑦d2 부재 일수가 **실제로 비운 기간**에 맞는다(입장 직후 재접속이 기준일을 지우지 않는다)  보고 6일 vs 실제 23일) | **귀속: 모델 몫** |
| `e2e-trade` | RC=1 · 결과줄 14개 · 마을 반경 안에 들어왔다  vid=null · 24749px (게이트 260) | 4분 | ★**단독 초록** (RC=0) | **귀속: 모델 몫** |

## 3. 미측정 0종

없다 — 오늘 목록을 다 돌았다.

## 4. 아침에 세션으로 넘길 빨강

아래는 **상시 목록에 없는** 빨강이다 — 귀속 카드를 찾아 세션에 넘긴다(이 칸만 사람 몫이다).

* `e2e-charsprite` — RC=1 · 결과줄 107개
  * ★★대조군 — **장착 안 하면 그 자리 화소가 안 바뀐다**(같은 애니 프레임 · 무풍)  2030px ≤ 8
  * ★A 자기 화면에서 도끼 층을 쓴다  ["body","clothes_ramie"]
  * ★★★ⓙ **둘째 클라가 첫째의 도끼를 본다** — 손에 든 것이 네트워크를 탄다  ["body","clothes_ramie"]
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 107개)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-charsprite.js` 와 그 하네스가 무는 제품 파일
* `e2e-conn` — RC=1 · 결과줄 48개
  * ★★② 클라가 **확정 오류**를 받았다(침묵이 아니다)  entering
  * ★★② 문구가 **기다림이 소용없음**을 명시한다  서버가 입장 처리를 못 끝내고 있다 — 오류일 수 있다 / 24초 경과 · 단계 welcome · ref c5 / 새로고침
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-conn.js` 와 그 하네스가 무는 제품 파일
* `e2e-fogray` — RC=1 · 결과줄 11개
  * R1: 손잡이가 실제로 갈렸다 (A 통 true · B 통 true)
  * R1: 반례 — 통 끔은 검사가 1.0배 많다 (A rsi 4877 · B rsi 4877 · ≥3 이어야 통이 일한 것)
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-fogray.js` 와 그 하네스가 무는 제품 파일
* `e2e-mtfuzz` — RC=1 · 결과줄 11개
  * P4 ★덮개 — 남은 바위는 여전히 산 아래  — ① 2% 초과 1회
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-mtfuzz.js` 와 그 하네스가 무는 제품 파일
* `e2e-rumor` — RC=1 · 결과줄 56개
  * ⑦d2 부재 일수가 **실제로 비운 기간**에 맞는다(입장 직후 재접속이 기준일을 지우지 않는다)  보고 4일 vs 실제 32일
  * 단독 재실행: 여전히 빨강 (RC=1 · 결과줄 56개)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-rumor.js` 와 그 하네스가 무는 제품 파일
* `e2e-trade` — RC=1 · 결과줄 14개
  * 마을 반경 안에 들어왔다  vid=null · 24749px (게이트 260)
  * 단독 재실행: **초록** (RC=0)
  * 귀속 찾는 법: `git log -3 -- scripts/e2e-trade.js` 와 그 하네스가 무는 제품 파일

## 5. 통과 108종

```
test-alloy  test-armor-cap  test-assay  test-audio  test-body  test-bridge-path  test-bridge-sprite  test-building-anchor  test-calendar  test-carrier  test-charsheet  test-chron-icons  test-claim-v2  test-client-globals  test-clothes  test-craft  test-crops-world  test-crops  test-distmatrix  test-ditch  test-doors  test-downed  test-econ-diet  test-econ-fieldyield  test-econ-rawgrain  test-emptystart  test-events  test-farm-life  test-farm-metrics  test-fishing  test-frontier-iron  test-furnace  test-granary-add  test-granary-haul  test-guest-identity  test-guest-rejoin  test-guild-treasury  test-hall-door-path  test-handoff-body  test-harness-lint  test-headless-parity  test-hist  test-hunt-port  test-hunt-vis  test-icons  test-imsu  test-inv-ground  test-itemlabel  test-kcal  test-lab-alloc  test-lab-happywork  test-lab-hunt  test-lab-stone  test-lab-trees  test-lab-wood  test-ledger  test-membership  test-mining  test-move  test-msy  test-newcomers  test-nightly  test-notice  test-npc-clothes  test-onboarding  test-oremix  test-path-core  test-picker-tables  test-preserve  test-props  test-psite-server  test-reanchor  test-redeploy  test-regrow  test-relic  test-render-common  test-rock-los  test-rooms  test-route-persist  test-salt  test-save-periodic  test-segindex  test-shelter  test-site-memo  test-spoil2  test-tame  test-terr-backfill  test-terraincache  test-tidal  test-trade  test-trees  test-valuechain  test-village-found  test-villager-stuck  test-weather  test-weight  test-winter  e2e-cutaway  e2e-guest-reconnect  e2e-guild  e2e-metallurgy  e2e-mtcorridor  e2e-onboarding  e2e-spoil2  e2e-tidal  e2e-ui  e2e-weight  e2e-zoom
```

---

## 6. 귀속 — 모델 몫 (A-3 ③ · 야간 세션이 손으로 채운 절)

> 기계가 낸 위 표는 건드리지 않았다(결정적 · 바이트 동일). 이 절만 사람/모델 판단이다.
> **없는 결함을 지어내지 않는다** — 못 쟀으면 못 쟀다고 적었다.

**오늘 판의 전제:** 기준 커밋 `b8b49a16` 은 09-15 야간 보고 커밋이고, **그 뒤로 main 이 한 번도 안 움직였다**(코드 무변경 3일). 그러므로 오늘의 빨강은 **코드 회귀일 수 없다** — 남는 설명은 ⓐ 기계/부하 자국 ⓑ 픽스처 흔들림 ⓒ **그동안 안 재서 못 봤던 잠든 빨강** 셋뿐이다.

| 하네스 | 단독 | 귀속 (야간 세션 판단) |
|---|---|---|
| `test-tick-slicer` | 빨강 | **상시(기계 의존)** — `nightly-known.txt` 등재 · 판단 없음 |
| `e2e-conn` · `e2e-fogray` · `e2e-mtfuzz` · `e2e-trade` | **초록** | **픽스처 흔들림(러너 연속 실행 부하)** — 2코어에서 앞 하네스 정리와 서버 둘 세우기가 겹친 자국. 제품 회귀의 증거 **없음**. 족보 167 · 09-13·09-14 야간의 `e2e-cold` 와 같은 모양. |
| `e2e-charsprite` | **빨강** | **자(하네스) 결함 쪽 — 귀속 후보 `T276`**(직전 접점 `f5378124` · 그 다음 `T250 6cefe5fc` · `T238 6a3905c9`). 근거: 첫 줄이 **대조군**이다 — "장착 안 하면 그 자리 화소가 안 바뀐다"가 `1857~2030px ≤ 8` 로 깨졌다. 즉 **잡음 바닥이 임계값의 230배**라 뒤따르는 도끼·등짐 판정 여섯은 **잴 수 있는 상태가 아니었다**(공통.md §2 "하네스가 틀리면 조용히 통과하는 게 아니라 없는 결함을 보고한다"). ⚠09-15 야간엔 **단독 98/0 초록**이었고 코드는 그때와 한 글자도 안 다르다 ⇒ **기계 의존**이 유력하다. 프롬프트의 옛 상시 목록엔 `e2e-charsprite` 가 들어 있고 정본 `nightly-known.txt` 엔 없다 — **올릴지 내릴지는 PM 몫**(09-15 보고 §4 가 이미 낸 숙제, 사흘째 미처리). |
| `e2e-rumor` | **빨강** | **잠든 빨강 · 귀속 미상(후보 셋)**. `⑦d2 부재 일수` 가 두 판 다 어긋났다(**보고 4일 vs 실제 32일** · 단독 **6일 vs 23일**) — 값이 판마다 다르니 **경주(race)** 다. ★이 하네스는 **09-12·09-13·09-15 야간에 전부 미측정**이었다(묶음 B 쪽 · 창 부족) ⇒ 오늘이 오랜만의 첫 실측이고, **언제부터 빨갰는지 모른다**. 접점 `server/villages.js:5918`(`absentDays: rb.absent`) 의 최근 커밋은 T237·T230·T219, `server/zone.js`·`server/central.js` 의 최근 커밋은 **T245 `63523050` · T235 `5d1886fe` · T225 `d17ea9a9`**(09-13 보안 문 정리). 실패 문구가 *"입장 직후 재접속이 기준일을 지우지 않는다"* 라 **존 입장 경로**를 무는데, 09-15 보고의 `e2e-cold` "존 입장 129ms" 도 같은 트리오를 가리켰다 — **같은 자리를 두 하네스가 두 밤에 걸쳐 가리킨 것**이 오늘 유일하게 값 있는 한 줄이다. 그래도 **증거는 아니다**(이분 안 했다). |

## 7. 아침에 세션으로 넘길 문장 (붙여넣기용)

* **→ 세션(존/보안 · T245·T235 계열):** `e2e-rumor ⑦d2` 가 main `b8b49a16` 에서 두 판 연속 빨갛다 — "부재 일수 보고 4일 vs 실제 32일 / 6일 vs 23일"(판마다 값이 달라 경주다). 실패 문구가 *입장 직후 재접속이 기준일을 지운다* 라 09-13 존 경유 인증 이동(`5d1886fe`·`63523050`)의 뒷자리일 수 있다. **이 하네스는 09-12 이후 야간에서 계속 미측정이었다 — 언제부터인지 이분(bisect)부터 해 달라.**
* **→ 세션(하네스/ART · T276 계열):** `e2e-charsprite` 가 단독에서도 빨갛다. 다만 **대조군이 먼저 깨졌다**(무장착 화소 변화 `1857~2030px ≤ 8`) — 도끼·등짐 판정 여섯은 잴 수 있는 상태가 아니었다. 09-15 단독은 98/0 초록이고 코드는 동일하다. **자의 잡음 바닥부터 다시 재 달라.**
* **→ PM:** ⓐ `nightly-known.txt` 에 `e2e-charsprite` 를 올릴지(09-15 보고 §4 숙제 · 사흘째) ⓑ `e2e-rumor` 처럼 **묶음 B 가 며칠째 미측정**인 하네스가 있는지 — 오늘 A 는 115종을 3h24m 에 완주했으니 **창은 이제 A 를 다 덮는다**. B 도 같은지 확인 필요.
