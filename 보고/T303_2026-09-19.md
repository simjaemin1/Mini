# 보고 — T303 소리 자산 정리: 개명 · 비움 확정 · `.dockerignore` · webp 화소 해시 (2026-09-19 · 세션8/ART)

**측정 베이스 `be310c6f` · 착지 베이스 `8c0b2505`**(도는 동안 설계 문서 하나가 먼저 들어와 리베이스 · 충돌 0 · 내 영역 0줄 · 리베이스 뒤 하네스 전부 재실행 · force 0) · 자산 **바이트 0 변경**(개명은 이름만 · sha1 그대로) · 지우기 0 · 배포 설정 0
**PNG 452 잠금 무변** · webp 88 잠금은 **자가 바뀌어** 움직였다(파일은 HEAD 와 88/88 동일)
`package.json` 은 **한 줄**(`@cwasm/webp`) · `48-a-audio.js` **0줄** · 하네스 다섯 PASS · `asset-lock --check` 486 · 0

## ★ 답 한 줄

**`wolf_howl` 개명 ○**(파일·잠금·매니페스트·`mobs`·CREDITS·하네스 두 자리 · 코드 0줄) **· 비움 셋 확정**(`state:"missing"` 둘 + `hare` 키 안 만듦)
**· `.dockerignore` 세 줄 = 0.55MB(0.28%)** **· webp 88장 화소 해시(sharp 와 88/88)** — 단 **그 자가 투명 아래 RGB 에 안 둔하다는 것도 같이 쟀다**(별 카드 회부)

---

## §0-ⓐ 개명 — 움직인 것 전수 · 그리고 카드가 하나 틀렸다

| 자리 | 움직인 것 |
|---|---|
| 파일 | `wolf_growl.{ogg,m4a}` → `wolf_howl.{ogg,m4a}` · **sha1 `d9176d70…` / `bede1a0f…` 그대로**(바이트 0 변경) |
| `icons.lock.json` | 키 둘 · **값 둘은 글자 하나 안 바뀜**(바이트가 같으니 당연하고, 그게 증거다) |
| `manifest.json` | `keys` 키 · `file`/`fileAlt` · `_이름` · `sources.commons_pd_wolf._주의` |
| `manifest.mobs` | `wolf: "wolf_growl"` → `"wolf_howl"` — **한 칸**(카드가 허락한 그 칸) |
| `CREDITS.md` §2-b | 표 한 줄 + 측정표 한 줄 + 설명 한 문단 |
| `test-audio.js` | ⓐ-2 기대값 · `_실측.worstCombo` 이름표 |
| **`48-a-audio.js`** | **0줄** ← 아래 |

★**카드의 전제 하나가 틀렸다 — 고칠 칸이 아예 없었다.** 카드는 *"`48-a-audio.js` 는 **키 한 칸만**"* 이라 적었는데
그 파일에 `wolf_growl` 은 **0회** 나온다. 나오는 것은 주석 한 줄뿐이다:
*"코드에 박히지 않는다(1차 판은 `'wolf'`·`'campfire'` 두 낱말이 여기 있었다)"* —
세션9 가 T283 에서 키를 표로 빼 뒀기 때문이다. **표로 배선한 값은 개명 비용이 0 이다**가 여기서 증명됐다.

### ★하네스가 개명을 도중에 잡았다 — 그리고 한 자리는 철자를 재고 있었다

`test-audio` 가 **두 곳에서 빨개졌다**(PASS 72 / FAIL 2). 둘의 성격이 달랐다:

| | 무엇 | 판정 |
|---|---|---|
| ⑥ⓐ-2 | 기대값이 `/_(growl\|grunt\|call)$/` — **이름 철자**를 재고 있었다 | **자가 틀렸다** ⇒ `mobs` 표에서 읽게 고쳤다(손 목록 0 · 다음 개명에 안 흔들린다) |
| ⑧e | `_실측.worstCombo` 에 옛 이름 | **데이터가 맞다** ⇒ 이름표만 갈았다. ⚠`worstPeak`·`clippedAtWorst` 는 **다시 재지 않았고 한 자도 안 고쳤다**(같은 파일이다) |

그리고 **삼자 대조(⑤)가 개명 도중 상태를 실제로 잡았다** — CREDITS 만 옛 이름이던 순간
`⑤b 4개`(없는 파일 둘 + 크레딧 없는 파일 둘) · `⑤c 1개`. T291 이 이 절을 세운 값이 여기서 나왔다.
⇒ 그 순간을 **시험 ⓔ 로 굳혔다**: 크레딧에만 옛 이름이 남으면 ⑤b **넷 더** · ⑤c **하나 더**.
(⚠T291 이 돌연변이를 **늘어난 수**로 고쳐 둔 덕에, 빨간 판에서도 ⓐ~ⓓ 가 그대로 섰다.)

## §0-ⓑ 비움 셋 — 대기가 아니라 결정

| 키 | 한 것 | 왜 |
|---|---|---|
| `harvest` | `state: "missing"` | 훅이 **이미 걸려 있다** ⇒ 파일만 오면 코드 0. 키가 있어야 한다 |
| `boar_grunt` | `state: "missing"` | 〃 (`mobs` 가 `wild_boar` 를 보낸다) |
| `hare` | **키를 안 만들었다** | 훅도 `mobs` 줄도 없다 ⇒ 빈 키는 **대기 칸 하나를 늘릴 뿐**이고, 세션9 칸(볼륨·반경·쿨다운)을 지어내야 한다 |
| `ui_click` | 키 0 · 파일·크레딧 유지 | **없음 확정** — UI 는 세계의 소리가 아니다 |

★**빈 키를 세우는 것과 안 세우는 것은 다른 말이다.** 그래서 `_파일없음` 이 넷을 각각 적고,
삼자 대조는 **라벨을 중립으로** 바꿔 `state` 칸을 그대로 찍는다(`harvest(missing) boar_grunt(missing)`) —
하네스가 키 이름을 손으로 들고 있으면 결정이 바뀔 때마다 하네스도 고쳐야 한다.

## §0-ⓒ `.dockerignore` — 수가 작다

세 줄을 넣고 `docker build` 없이 `.dockerignore` 규칙으로 배포물을 셌다(`git ls-files` 전수):

| | 장수 | 크기 |
|---|---:|---:|
| 지금 | 1,463 | **199.39 MB** |
| 세 줄 더한 뒤 | 1,441 | **198.84 MB** |
| 줄어든 것 | **22** | **0.55 MB (0.28%)** |

★**크게 적지 않는다.** `bgm/` 45.73MB 중 `*.py` 0.24 · `*.zip` 0.31 · `samples/` 0.00MB 뿐이고
나머지 **45.2MB 는 배포되는 13곡**이라 이 세 줄로는 안 줄어든다. 줄이려면 곡을 건드려야 하는데 이 카드 밖이다.
**지우지 않았다** — 레포엔 남고 이미지에만 안 들어간다(그 문장을 `.dockerignore` 주석에 적어 뒀다).

## §0-ⓓ webp 화소 해시 — 켰고, 그 자의 한계도 같이 쟀다

T260 이 디코더 넷을 재서 `@cwasm/webp` 를 권했고 못 켠 이유는 **"저장소에 디코더가 없다"** 하나였다.

| | |
|---|---|
| 두 디코더 대조 | `@cwasm/webp` ↔ `sharp`(libvips) **88장 중 88장 일치 · 불일치 0** (T260 값을 독립 재현) |
| 잠금표 | webp 88 값이 움직였다 — **자가 바뀌어서다**. 파일 바이트는 HEAD 와 **88/88 동일** |
| PNG 452 | **한 값도 안 움직였다** |
| `npm ci` | 3판 중앙 **5.67s → 5.93s** · +120 KB · +2 패키지 |
| ⚠잡음 | 판마다 5.54~5.95s(전) / 5.88~5.95s(후)로 **겹친다** ⇒ **+0.26s 는 잡음과 안 갈린다**. T260 의 "+0.0s" 를 뒤집지 않는다 |

### ★그리고 자를 바꾸며 **그 자가 무엇에 둔한지**를 쟀다 — 둔하지 않았다

같은 RGBA 를 `effort` 0/4/6 으로 **무손실** 재압축했는데 화소 해시가 **셋 다 달랐다.**
처음엔 내 대조가 틀린 줄 알고(원본→재압축 경로라 sharp 가 알파를 만질 수 있다) **raw RGBA 에서 다시 구웠는데도 달랐다.**
범인은 **완전투명 아래의 RGB** 다 — 안 보이니 인코더가 고쳐 쓴다(webp alpha cleaning):

| 실측 `mt_F0v0` (933×562 · 524,346화소) | |
|---|---|
| 완전투명 | **265,717 (50.7%)** |
| eff0 ↔ eff4 다른 화소 | 전체 85,602 · **투명 빼면 0** |
| eff0 ↔ eff6 다른 화소 | 전체 93,237 · **투명 빼면 0** |
| 원본 ↔ eff4 다른 화소 | 전체 91,153 · **투명 빼면 0** |

⇒ 이 자는 **"배포된 파일의 화소가 움직였나"는 정확히 잡는다.** 못 하는 것은 PNG 자가 하는 일 하나 —
"압축기만 갈았을 때 같다고 말해 주기" 다. 고치는 길은 **투명 아래 RGB 를 0 으로 정규화**(자는 한 벌 유지)인데,
값이 **PNG 557장 중 404장**(698,959화소)이라 **452 PNG 잠금이 같이 움직인다** ⇒ 이 카드의 "그림 무변" 밖 ⇒ **별 카드**(회부 ①).

★**T246 의 줄이 한 번 더 나왔다** — *"IDAT 는 그림이 아니라 압축기를 잰다"*. 이번엔 **투명 아래 RGB 도 그렇다.**
자를 바꿀 때는 새 자가 **무엇에 둔한지**를 같이 재야 한다. 안 쟀으면 "webp 도 화소 해시가 됐다"로 끝났을 것이다.

## §1 검증

| | |
|---|---|
| 자산 | 개명 둘 · **바이트 0 변경**(sha1 동일) · 새 파일 0 · 지운 파일 0 |
| 잠금 | `--check` **486 · 어긋남 0** · 값이 움직인 키 **88(전부 `mountains` webp · 자 교체)** · PNG **0** · sfx 키 둘 개명 |
| `package.json` | **+1줄**(`@cwasm/webp`) · `package-lock.json` +18줄 · `sharp` 는 **레포에 안 들어갔다**(전역 설치본으로 대조만 했다 · lock 에 0회) |
| `test-audio` | **PASS 74 / FAIL 0** (개명 도중 72/2 → 고친 뒤 74/0) |
| `test-assets-audit` | **PASS** — 삼자 대조 ⑤a·b·c ○ · 돌연변이 **다섯**(ⓔ 추가) |
| `test-icons` · `test-props` · `test-harness-lint` | PASS · PASS · PASS |
| 3시드 | 면제 — diff ∩ 랩 적재 17 = ∅ |
| 접점 | `wolf_growl`·`wolf_howl`·`manifest.json`·`CREDITS.md`·`asset-lock`·`.dockerignore`·`@cwasm/webp`·`package.json`·`test-audio` |

## §2 회부

1. ★**[ART 또는 PM] webp 자 정규화 — 별 카드.** 투명 아래 RGB 를 0 으로 맞추면 두 자가 압축기에 둔해진다.
   값: **PNG 404/557장이 움직인다** ⇒ 전수 재잠금. 표는 §0-ⓓ.
2. **[세션9 알림]** `test-audio ⓐ-2` 기대값을 `mobs` 표에서 읽게 고쳤다(철자 대신 배선을 잰다) ·
   `_실측.worstCombo` 이름표만 갈았고 **수는 안 건드렸다**.
3. **[재민]** `harvest` 직접 녹음(#34 남은 하나) · #29 6장 · 다음 묶음 · 실기표 26항 한 바퀴.
4. **#30 ⓒ · #32 ⓐ 닫힘** — 단 ⓒ 의 값은 **0.28%** 라는 것을 같이 적어 둔다.
