# 보고 — 야간 러너 전수 2026-09-13 (KST)

**기준 커밋 `62b5f125`** (`인계/공통 — 족보 (169)(170): 남의 이름 하나로 어디까지 가나 · 랩이 켠 채 뜨는 손잡이 (T216·T209 · 문서만)`)
· 컨테이너 새 클론(`/tmp/minirepo`) · `npm install` · 러너 자가 검사 **통과**
· 시작 **04:09** → 종료 **07:52** (KST · 3시간 43분)
· 전수 목록 **143종**(`grep -l '^// @regress'`) · **돈 것 112** · 통과 **110** · 실패 **2** · **미측정 31**(전부 e2e — §3)
· 병렬 0 · 러너 동시 2개 0 · `pkill -f` 0(`kill <pid>` 만) · 코드·하네스 수정 0

## ★ 한 줄 — **상시 밖 빨강 0건.** 단위 95종은 전수 완주(95/95), e2e 는 17/48 까지 닿았다.

빨강 둘 중 하나(`test-tick-slicer`)는 공통.md 목록 그대로 **상시(기계 의존)**,
다른 하나(`e2e-cold`)는 **단독 재실행에서 55/0 완전 초록** — 러너 부하가 만든 거짓 빨강이다(족보 111·167).
⇒ **오늘 아침 세션으로 되돌릴 빨강은 없다.**

---

## 1. 실패 표 — 2종

| 하네스 | 통과/실패(전수) | 단독 재실행 | 귀속 |
|---|---|---|---|
| `test-tick-slicer` | 20/4 (⑥ 효과비 1.02 · ⑧ 전제 A/B 0곳 · 인구 0) | **단독도 20/4 동일** | **상시(기계 의존)** — 공통.md 목록 그대로. 새 결함 아님 |
| `e2e-cold` | 27/28 | ★**단독 55/0 초록** | **부하 거짓 빨강** — 제품 회귀 아님. 아래 §2 |

### `e2e-cold` — 왜 전수에서만 빨갰나 (재지 않은 것을 지어내지 않기 위해 적는다)

전수 판의 실패 줄은 세계가 **안 돈 꼴**이었다:

* `① (픽스처) 시계가 실제로 얼었다` — `day 315 · 기대 -2.6℃ · 실측 -2.6℃` 인데 **`60008ms`** 로 ✗ (60초 벽시계 초과)
* `② 몸이 실제로 언다` — `0.0052 → 0.0052` (한 눈금도 안 움직였다)
* `③ 마을 완충` — `shelter undefined` · `cut undefined`
* `⑤ 갖옷 vs 삼베` — `삼베 Δ0.0000 vs 갖옷 Δ0.0000`

단독 판에서는 같은 절들이 **전부 실수(實數)를 낸다**:
`① 혹한급 0.9278` · `④ 불 없음 -0.0020 → 불 옆 0.0618` · `⑤ 삼베 Δ-0.0040 vs 갖옷 Δ-0.0207` ·
`⑦㉠ 능선 0 · 골 0`(전수에선 `undefined`였다) · `⑧ⓒ 추위 0.8117`.

⇒ 값이 `undefined`·`0.0000` 이던 것이 단독에서 실수가 된다 = **서버 루프가 굶은 것**이지 계산이 틀린 게 아니다.
2코어에 e2e 가 서버 둘을 세우는 동안 앞 하네스 정리가 겹치면 60초 창을 못 채운다.
**이것이 족보 (167)이 말한 바로 그 꼴이다** — 벽시계 상수로 판정하는 절이 러너에서만 빨개진다.
`e2e-cold` 는 T185(09-12)에서 상시 목록에서 **뺀** 하네스이고 어제 전수도 55/0 이었다 ⇒ 목록을 되돌릴 이유 없다.
다만 **①의 `60008ms` 는 T185 가 고친 꼴이 한 자리 남아 있다는 뜻**이다 → §4 회부.

### 상시 빨간 것 대조 (`인계/공통.md` §2 ⑥ · T185 판)

| 공통.md 목록 | 오늘 |
|---|---|
| `test-tick-slicer` (기계) | **빨강 — 목록 그대로** |
| `e2e-rtt` (기계) | **미측정**(§3) |
| `test-body ⑯㉧` (물때) | ★**306/0 초록** — `TIDE_FREEZE_MS` **없이** 재측정한 값이다 |
| (`e2e-charsprite`·`e2e-cold` 는 T185 가 뺌) | `e2e-charsprite` **81/0 초록** · `e2e-cold` 위 §2 |

⚠**자인**: 첫 청크 5종(`test-alloy`·`test-armor-cap`·`test-assay`·`test-body`·`test-bridge-path`)은
실수로 `TIDE_FREEZE_MS=372603` 을 켠 채 돌았다. 그래서 그중 물때를 타는 `test-body` 를
**얼림 없이 단독 재측정**했고 306/0 이다(위 표). 나머지 넷은 물때를 안 탄다.

---

## 2. 통과 110종

* **단위 95/95 전수 완주** — 어제와 같다(단위는 빠르다).
* **e2e 16종** 초록: `bigmap-live` · `charsprite` · `chronicle` · `conn` · `craft` · `cutaway` · `downed` ·
  `emptystart` · `events` · `fishing` · `forage-village` · `friends` · `guest-reconnect` · `guild` · `hp` · `inv` · (+`cold` 단독)

## 3. 미측정 31종 — 전부 e2e

```
e2e-membership  e2e-metallurgy  e2e-mountain   e2e-move       e2e-mtcorridor
e2e-mtcut       e2e-mtfoot      e2e-mtfuzz     e2e-mtocc      e2e-nature
e2e-npcsprite   e2e-onboarding  e2e-preserve   e2e-rooms      e2e-rtt
e2e-rumor       e2e-salt        e2e-spoil2     e2e-terrain    e2e-thirst
e2e-tidal       e2e-tilestate   e2e-trade      e2e-ui         e2e-verbs
e2e-village     e2e-waterperf   e2e-weather    e2e-weight     e2e-winter
e2e-zoom
```

**미측정은 "초록"이 아니라 "안 쟀다"이다.** 어제 35종 → 오늘 31종(단위를 먼저 돌려 4종 더 닿았다).
T185 실측(e2e 48종 = 3h24m)이 그대로 확인된다 — 창 3h43m 으로는 **e2e 절반도 못 닿는다**.

### 오늘 바꾼 것 — 순서 (다음 판도 이대로 권함)

목록은 `LC_ALL=C sort` 라 `e2e-*` 가 앞이다. 그대로 돌면 **창을 e2e 가 다 먹고 단위는 한 종도 못 잰다.**
오늘은 **단위 95 → e2e** 순으로 돌려 단위를 전수 완주시켰다. 판정 가치가 큰 쪽(빠르고 수가 많은 쪽)을 먼저 확보하는 순서다.
`판정대기` #25(창 처리 — 단위 매일 + e2e 이틀 분할)의 자료로 쓸 수 있다.

## 4. 회부 (구현 금지 · 아침 판정거리)

* **ⓐ `e2e-cold ①` 의 `60008ms`** — T185 가 "하네스가 잰 시간을 상수와 견주지 마라"(족보 167)로 갈아 끼운 꼴이
  이 절에 **한 자리 남아 있다**. 세계가 말하는 증인(게임일 예산 · `/perf`)으로 바꾸면 이 거짓 빨강이 사라진다.
  → **T185 귀속 · 하네스 한 줄**
* **ⓑ `test-tick-slicer ⑧` 의 전제가 `인구 0`** — "잴 만큼 큰 경제"가 0곳이라 전제부터 못 선다.
  기계 의존으로 묶여 있지만 ⑧은 **기계가 아니라 픽스처가 안 선 것**일 수 있다. 한 번은 갈라 봐야 한다.
  → `판정대기` #25 묶음 후보
* **ⓒ 창 3h43m 은 143종에 못 닿는다**(이틀 연속 실측: 108 · 112). #25 판정 없이는 매일 31~35종이 계속 안 재진다.

## 5. 규약 준수

| 항목 | |
|---|---|
| 병렬 | **0** — 순차 단독만 |
| 러너 동시 2개 | **0** (`ps` 확인) |
| `pkill -f` | **0** — `kill <pid>` 만 |
| 코드·하네스 수정 | **0** |
| 배포·예약·지시서 | **0** |
| 커밋 | 이 문서 **하나** (코드 무접촉) |
