# 보고 — T106 범프 끝: 산 45 · 지면 질감 3 · 다리 21 (세션8 · 2026-09-05)

브랜치 `batch/bump-end-0905` → ff → `main`. **force push 0회.**
이걸로 **4.0.2 산출물 0** — 배포하는 그림 335장을 전부 이 저장소가 5.0.1 로 굽는다.

---

## 0. §0 실측 (셋)

### ⓐ 파이프라인 표

| 무리 | 굽는 스크립트 | 중간 | 배포 | 앵커/잠금 | 클라가 읽는 자리 | 4.0.2 baseline |
|---|---|---|---|---|---|---|
| 산 45 | `bake-mountain.py` (**`MT_RES=2048`**) → `pack-mountain.py` | PNG 2048² | `mountains/*.webp` | `mountain_anchors.json` · `mountain_alpha.json` | `99-main.js:122` (fetch) | 있다 ✓ |
| 지면 4 | `bake-terrain-tex.py -- angled` (3) · `bake-rocktex.py` (1) | — | `terrain/*_angled.png` | 없음 | `00-const.js:698` **런타임 Image 로드** → 캔버스 패턴 | 있다 ✓ |
| 다리 21 | `bridge_render.py` | — | `bridge/*.png` | 없음(셀 규약 고정) | `40-r2-sprites.js` | 있다 ✓ |

**ⓐ-1 지면 질감은 런타임 로드다** — 빌드 산출물이 아니다. 재굽기가 하류를 안 건드린다(카드 ⓑ 답).
**ⓐ-2 산은 `MT_RES=2048`** 이다(스크립트 기본 512 아님). `pack-mountain.py` 의 기본 SRC 가 `/tmp/mt2048`
인 게 흔적이었고, 2048 로 구워 팩한 두 장의 **크기·앵커가 배포본과 정확히 같아** 확정했다.
**ⓐ-3 `SPECS` 85 ↔ 배포 45** — 둥근 `mt_R*` 40종은 안 쓴다(회부). `MT_ONLY` 로 45만 구웠다.

### ⓑ 프레임 · 범프 면

* **`sensor_fit` 노출 0.** 산은 정사각(2048²), 다리도 정사각(256²) — AUTO 와 HORIZONTAL 이 같다.
  지면은 `_angled_camera` 가 이미 `HORIZONTAL` 을 못 박아 뒀다. ⇒ T97 이 자연물에서 겪은 잘림이 없다.
* **범프 면 실측 — `BUMP_DIST` 만 0.001 → 1.0 (단일 변수)**

| 무리 | 범프만의 Δ (/255) |
|---|---|
| 산 `mt_F0v0` / `mt_S1` | 0.47 / 0.53 |
| 다리 `bridge_mid_x` / `gran_pile1` | 0.96 / 0.27 |
| **지면 `mud_angled`** | **0.00** |

★지면은 **범프 노드를 애초에 안 쓴다.** `bake-terrain-tex.py` 의 `ShaderNodeBump` 셋은 전부
`topdown` 갈래(`bake_water`·`bake_dirt`·`bake_rock`)에 있고, 배포되는 `*_angled.png` 를 굽는
`bake_grass_angled`·`bake_flat_angled` 는 `principled` 만 쓴다.
⇒ T103 이 거기 채운 `Distance` 는 **옳지만 지금 배포본엔 안 닿는다**(회부).

### ⓒ 성능 잡음 바닥

`prof-mt3merge ⑶`(라이브 rAF 중앙값 · 교대 5회)로 **같은 빌드를 두 번** 돌려 바닥을 먼저 잡았다 — §4.

---

## 1. 편입 증명 — T103 코드 편집은 셋 다 무해했다

절차(T97·T101·T103): 옛 코드(=`d6e48590~1`) 대조군 ↔ 현재 코드에 `BUMP_DIST = 0.001` 만 덮은 패스A.

| 무리 | 표본 | 결과 |
|---|---|---|
| 산 | `mt_F0v0` · `mt_S1` | **IDAT 2/2 동일** |
| 다리 | `bridge_mid_x/y` · `gran_pile1` | **IDAT 3/3 동일** |
| 지면 | `mud_angled` | **IDAT 1/1 동일**(씨앗 고정 뒤) |

★**대조군이 버그를 하나 잡았다.** 지면은 처음에 대조군 ↔ 패스A 가 **달랐다**.
원인은 내 편입이 아니라 `bake_flat_angled` 의 `random.seed(hash(name) & 0xffff)` —
파이썬 3 의 문자열 `hash()` 는 프로세스마다 다르다(T101 이 `rock_render.py` 에서 잡은 그 함정).
⇒ **`mud_angled.png` 은 재현이 불가능했다.** 정수로 못 박았고(`_SEEDS = {'mud_angled': 37335}`),
그러자 IDAT 이 같아졌다. ⚠대가로 조약돌 배치가 배포본과 달라진다 — 옛 배치는 되살릴 수 없다.

---

## 2. Δ 표 — 4.0.2 배포본 대비

| 무리 | 평균 Δ (/255) | 그중 범프 몫 | 나머지 |
|---|---|---|---|
| 지면 `grass_angled` | **0.79** | 0 | 순수 기계 |
| 지면 `dry_angled` | **0.71** | 0 | 순수 기계 |
| 지면 `mud_angled` | 1.38 (최대 55) | 0 | 기계 + **조약돌 재배치** |
| 다리 21 | **1.24** | ~0.5 | 기계 ~0.75 |
| 산 45(크기 같은 33장) | **2.34** | ~0.5 | 기계 + webp 비가역 압축 |

★**순수 4.0.2 → 5.0.1 기계 차이 = ~0.75/255.** 범프가 하나도 없는 면이 그 값이고,
다리에서 범프 몫을 빼도 같은 값이 나온다(교차 확인).
⇒ T97 이 "기계 차이"라 부른 **5.53 중 86%가 잃어버린 범프**였다. T103 의 "3분의 2" 를 여기서 좁힌다.

### 산 재굽기의 실측 — 앵커가 **무변이 아니다**

12/45 가 **≤1px** 움직였다(최대 |Δw|=|Δh|=1). 크롭이 알파 bbox 기준인데 실루엣 가장자리의
Cycles 표본 잡음이 1화소를 살렸다 죽였다 한다. `mountain_alpha.json` 도 그만큼 바뀐다.
클라는 앵커 JSON 을 읽으므로(`99-main.js:122`) 자리 자체는 맞다 — 클라 소스 접점 **0**.

```
mt_F0v1  844x587 → 843x587   mt_G1v0  684x785 → 685x785
mt_F1v0  672x615 → 672x616   mt_G1v2  716x632 → 715x632
mt_F4v1  957x510 → 957x511   mt_G2v2  514x649 → 514x650
mt_G0v1  990x611 → 991x611   mt_G3v0  719x622 → 718x622
mt_G3v2  648x631 → 648x630   mt_G5v1  999x607 → 999x606
mt_G5v2 1106x523 → 1105x523  mt_G6v1 1221x548 → 1220x548
```

---

## 3. 성능 — 짝 비교 (족보 80 · 잡음 바닥 먼저)

`prof-mt3merge ⑶` — 라이브 rAF 중앙값 · 교대 5회 · SE 와 2σ 판정.

| 판 | 병합 끔 | **병합 켬(라이브 기본)** |
|---|---|---|
| 새 자산 1회차 | 41.72±4.66ms (SE 2.08) | **24.44±3.16ms (SE 1.41)** |
| 새 자산 2회차 | 48.60±4.69ms (SE 2.10) | **24.30±2.91ms (SE 1.30)** |
| 옛 자산(4.0.2) | 45.10±5.63ms (SE 2.52) | **24.88±2.32ms (SE 1.04)** |

* **잡음 바닥**(같은 빌드 두 번) — 끔 |Δ| 6.88ms · **켬 |Δ| 0.14ms**
* **옛 ↔ 새(켬)** |Δ| = **0.51ms** — SE(1.0~1.3)보다 작다 ⇒ **성능 변화 없음**
* webp 총 바이트 2.04 → **2.16MB (+5.8%)** — 한 번의 적재 비용이고 프레임 경로는 앵커·크기가 같다.
* 병합 효과 자체는 세 판 모두 유의(−41.4% · −50.0% · −44.8%) — 기구가 살아 있다.

---

## 4. 검증 (러너 전수 없음 — 공통 §2 ②)

### ⓐ 단위·영역 하네스

| 하네스 | 판정 |
|---|---|
| `test-icons` (CI core) | 157/0 |
| `test-props` (CI core) | 166/0 |
| `test-render-common` (CI core) | 51/0 |
| `test-crops-world` (CI core) | 31/0 |
| `test-charsheet` (CI core · 상류가 +282행) | 69/0 |
| **`test-bridge-sprite`** (다리 자산 접점 — 이번에 다시 뽑아 넣었다) | **57/0** |
| `test-building-anchor` | 25/0 |
| `test-harness-lint` | 19/0 |
| `e2e-mtcorridor` | 15/0 |
| 랩 모듈 적재 | 교집합 ∅ |

접점 파일로 뽑은 목록(`grep -l`): `icons.lock.json` → `test-icons`·`test-props` ·
`render_common.py` → `test-render-common`·`test-charsheet` ·
산/다리/지면 자산 → `test-bridge-sprite`·`test-icons`.
`bake-terrain-tex.py`·`bake-mountain.py`·`pack-mountain.py`·`mountain_anchors.json` 을 부르는 하네스는 **0개**다
(굽는 쪽은 아무도 안 본다 — 잠금표가 유일한 문지기라는 뜻이다 · 회부).

`test-icons ⑧` 에 `bridge`·`terrain`(PNG·IDAT) 과 **`mountains`(webp — IDAT 이 없어 파일 전체 sha1)** 를
더했고, `_기계` 가 "4.0.2 산출물 0" 이라고 적혀 있는지도 본다. 잠금표 아홉 무리 **335키**.

### ⓑ 픽셀 하네스 열여섯 — `run-regress.sh --list pixel` 전부

렌더 층을 바꾼 카드이므로 **표에 걸린 열여섯을 하나도 빼지 않고** 돌렸다(T104 가 세운 `@pixel`).
**각 하네스를 한 번에 하나씩** — 3010/3020 이 완전히 빌 때까지 기다리고, 끝나면 청소하고 6초 쉰다.

| 하네스 | 판정 | | 하네스 | 판정 |
|---|---|---|---|---|
| `e2e-bigmap-live` | 11/0 | | `e2e-nature` | 86/0 |
| `e2e-charsprite` | 81/0 | | `e2e-rooms` | 66/0 |
| `e2e-cutaway` | 15/0 | | `e2e-terrain` | **33/0** |
| `e2e-fishing` | 31/0 | | `e2e-tilestate` | 39/0 |
| `e2e-mountain` | **29/0** | | `e2e-waterperf` | 16/0 |
| `e2e-mtcut` | 26/26 | | `e2e-weather` | 31/0 |
| `e2e-mtfoot` | 12/12 | | `e2e-zoom` | 28/0 |
| `e2e-mtfuzz` | 10/10 | | `e2e-mtocc` | 18/18 |

**532 판정 · 실패 0.**

### ★기준 스냅샷은 **없다** — 그래서 "갱신 대 은폐"가 성립하지 않는다

카드가 갈라 두라고 한 두 갈래(기준 갱신 / 회귀 은폐)를 재기 전에 **기준이 있는지부터 봤다**:

* 열여섯 중 커밋된 "정답 그림"과 대조하는 하네스 **0개**. 전부 **한 판 안에서** 찍은 두 장을 견준다
  (전/후 · 켬/끔 · 같은 시각 두 프레임).
* 저장소 그림을 읽는 유일한 하네스 `e2e-nature` 도 읽는 건 **기하**(앵커 ↔ PNG 크기)지 **생김새**가 아니다.

⇒ 그러니 나는 문턱을 만질 일도, 스냅샷을 갈아 끼울 일도 없었다 — **한 줄도 안 고쳤다**.
   뒤집으면 이게 이 기구의 구멍이다: **재굽기가 생김새를 바꿔도 하네스는 아무도 모른다.**
   생김새를 붙드는 것은 `icons.lock.json`(sha1 335키)과 §5 의 **사람 눈 두 줄**뿐이다 — 회부에 적었다.

### ★측정기가 또 거짓말했다 (세 번째) — 이번엔 **넷 다** 거짓이었다

열여섯을 처음 돌렸을 때 넷이 빨갰다. 넷 다 내 것이 아니었다 — **재는 자리**였다.

| 빨강 | 처음(연속) | 단독(내 나무 `a119b172`) | 깨끗한 `origin/main` `d426c25e` | 판정 |
|---|---|---|---|---|
| `e2e-charsprite` | 80/1 `clip=idle speed=0` | **81/0** `clip=walk 63.64` | 81/0 `clip=walk 63.64` | 하네스의 `sleep(1400)` 벽시계 |
| `e2e-cutaway` | 제한시간(600s) | **15/0** | 15/0 | 내 러너의 600초 뚜껑이 짧았다 |
| `e2e-terrain` | 중앙 `ECONNREFUSED` 뒤 충돌 | **33/0** | 33/0 | 연속 실행 오염 |
| `e2e-zoom` | 왕복 화소 차 **9.717**(문턱 1.0) | **0.107** | 0.111 | 〃 |

* `e2e-charsprite` 가 걸린 자리는 **그 파일이 스스로 금지한 문법**이다 — 바로 위 주석이
  "시계로 기다리지 마라, 조건으로 기다려라"인데 정작 그 판정만 `keyboard.down` 뒤 `sleep(1400)` 이다. 회부.
* `e2e-zoom` 왕복은 **정적 그림으로는 절대 안 흔들리는 판정**이다(z=1 전 · z=1 후 · 같은 텍스처).
  9.717 이 나왔다는 건 그림이 아니라 **시간**이 샜다는 뜻이고, 단독 0.107 이 그걸 확인해 준다.
* ★그 옆 숫자가 덤으로 증언한다 — 확대 때 화소 차 **내 나무 5.72 vs 깨끗한 나무 5.68**.
  산 45 · 지면 3 · 다리 21 을 통째로 다시 구웠는데 화면 평균 차이가 **0.04/255** 다.
  §2 의 "범프 몫이 산에선 0.5/255" 를 **하네스가 독립적으로 되짚어 준 셈**이다.
* T101(`EADDRINUSE`) · T106 1차(`mtocc`/`mtfuzz`) 에 이어 **세 번째**다. 이번엔 러너에
  "포트가 빌 때까지 기다린다 + 끝나면 청소 + 6초" 를 넣었더니 그 둘이 **연속 실행에서도 18/18 · 10/10** 으로 붙었다.
  ⇒ 처방이 확인됐다: 하네스를 고칠 게 아니라 **러너가 자리를 씻어야 한다**(회부).

⇒ 코드를 의심하기 전에 **재는 자리부터 씻는다.** 그리고 빨간 것은 반드시
   **깨끗한 `origin/main` 나무에서 한 번 더** 돌려 남의 것과 내 것을 가른다.

### 굽는 시간 (2코어 · bpy 5.0.1)

| 판 | 장 | 시간 |
|---|---|---|
| 산 대조군·패스A | 2×2 | 각 ~2.5분 |
| 산 전수(2048²) | 45 | 약 **65분** (장당 ~75초) |
| `pack-mountain`(crop·ppu 정규화·webp) | 45 | 약 3분 |
| 다리 대조군·패스A·전수 | 3+3+21 | 각 ~1분 · 전수 ~2분 |
| 지면 대조군·패스A·전수 | 1+1+3 | 각 ~8분 · 전수 ~14분 |

---

## 5. 실기 2줄 (재민)

`산그림/디자인B/범프끝_산지면다리_전후.png` — 왼쪽 전(4.0.2) · 오른쪽 후(5.0.1).

1. **산 바위 결이 전과 같은가.** (범프 몫이 0.5/255 라 거의 같아야 정상이다 — 산은 큰 기하가 그림을 만든다)
2. **지면 흙·풀 질감이 전보다 거칠어졌는가.** ★**아니어야 맞다** — 지면은 애초에 범프를 안 썼다(§0-ⓑ).
   `mud_angled` 만 조약돌 자리가 바뀐다(재현 가능하게 씨앗을 못 박은 대가).

## 6. 회부

`인계/회부.md` `0-범프끝2` 절. 이번에 **네 건이 늘었다** — 셋은 기구(器具) 쪽이다.

| 회부 | 요지 |
|---|---|
| `bake-terrain-tex` 의 `topdown` 갈래 | 배포본을 하나도 안 만든다 — 죽은 갈래인지 지면 2차인지 판단 필요 |
| 둥근 산 `mt_R*` 40 · 주봉 `mt_X*` 3 | 굽는 표엔 있고 배포엔 없다(재민 08-07 "둥근 거 위주로"가 안 닿았다) |
| ★`e2e-*` 연속 실행 오염 **+ 처방** | 포트 대기·정리·6초를 넣으니 1차 빨강 둘이 연속에서도 붙었다 · 600초 뚜껑도 짧다 |
| ★`e2e-charsprite` ⑤ 의 `sleep(1400)` | **그 파일이 바로 위 주석에서 금지한 문법**이다 — 폴링으로 바꾸면 끝난다(char 영역이라 안 만졌다) |
| ★픽셀 하네스에 **기준 그림이 0장** | 회귀를 감출 길이 없다는 좋은 성질이자, **재굽기가 생김새를 바꿔도 아무도 모른다**는 구멍 |
| ★**굽는 스크립트를 부르는 하네스 0개** | `hash(str)` 비결정성 두 건 모두 하네스가 아니라 대조군이 잡았다 — 결정성 하네스 하나면 닫힌다 |

그 밖(그대로): 자연물·건물 재질 접두 · 옷 재질 공용(T96 뒤) · 그루터기 · 종별 34세트 · 침상 생활층 · 상추/아욱.

## 7. 푸시

(푸시 뒤 채움 — 해시 · 브랜치 · force 0 · `.lock` 전수)
