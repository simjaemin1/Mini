# T168 — 크레딧: 빌린 것의 이름을 적는다 (2026-09-10)

브랜치 `batch/credits-0910` · **자산 무변** · 서버 diff **zone.js 두 줄 + 새 모듈 하나** ·
클라 diff **0** · 새 패널 **0** · 새 클라 조건 **0** · 문구 사본 **0**(정본 `CREDITS.md` 하나).

---

## §0-ⓐ 빌린 것 전수 — **어떻게 찾았나, 그리고 무엇이 없었나**

훑은 자리: `assets-src/` 전부 · `scripts/*.py` 의 출처 주석 · `public/assets/` 의 README·메타 ·
`git log`(에셋 커밋 메시지) · 잠금표 둘 · `package.json` + `node_modules/*/package.json` 의 `license` ·
`public/style.css` 의 글꼴 선언 · `public/index.html` 의 `<script src>` 전수.

### 찾은 것 — 여섯

| 무엇 | 어디서 | 어디에 들어갔나 | 카드 |
|---|---|---|---|
| **CMU 모캡** 다섯 클립 | mocap.cs.cmu.edu (cgspeed BVH 변환본 · una-dinosauria 미러 `09a07f54`) | `assets-src/mocap/*.bvh` → 포즈표 → 캐릭터 시트 192장 | T96 · T155 |
| **국립국악원 단음** 실제 녹음 | gugak.go.kr/digitaleum | BGM 12곡 — 산조가야금(조각 262) · 정악가야금 · 정악대금(조각 113) | 배치 시절(2026-07-29~31) |
| npm 실행 의존성 다섯 | npm | 서버 | — |
| SQLite | `better-sqlite3` 안 | 서버 DB | — |
| 글꼴 이름 둘 | — | `--font`/`--mono` **이름만** | — |
| (문헌 여덟) | 논문·교범 | 수치 앵커 — **게임 크레딧이 아니다**(카드 지시) | 여러 |

### **없었던 것** — 이 카드의 절반은 음성 결과다

* **밖에서 온 그림 0장.** 배포 이미지 612장 중 **611장이 잠금표 안**이다
  (`icons.lock.json` 419키 + `char_sheets.lock.json` 192키 = 611). 남은 한 장은
  `public/assets/char/probeall_walk.png` — 탐침 부스러기다(자산이 아니다 · 회부).
* **웹폰트 파일 0.** `.woff/.ttf/.otf` 0개 · `@font-face` 0 · `fonts.googleapis` 0.
  CSS 는 `'Noto Sans KR'`·`'IBM Plex Mono'` 를 **이름으로만** 부르고 시스템 글꼴로 떨어진다
  ⇒ **배포하는 글꼴이 없으므로 라이선스 의무가 없다.**
* **벤더링한 JS 0.** `index.html` 의 `<script src>` 는 전부 제 파일이다(CDN 0).
* **Kenney Nature Kit 은 더는 안 나간다.** 나무 1판이 그 recolor 였는데 "전면 통일"로
  Blender 정본 씬에서 12종을 다시 구웠다(배치19). 남은 것은 `40-r2-sprites.js:111` 의 **낡은 주석**뿐.

### 출처 미상 둘 — **미상으로 적는다**

| 파일 | 커밋 | 쓰이나 | 없는 것 |
|---|---|---|---|
| `assets-src/rd-nature-sheet.png` | `6ec41689` (2026-07-27) "브라우저 경유 업로드" | **안 쓴다** — 참조는 주석 한 줄뿐 | 생성 도구·저작자·라이선스 |
| `assets-src/legacy_mac/leaf.png` | 2026-06-24 맥에서 이관 | **배포에는 안 쓴다** — 보관된 옛 스크립트만 본다 | 출처·라이선스 |

⚠둘 다 게임에는 안 나가지만 **저장소를 공개하면 같이 나간다**. 지울지 출처를 찾을지는 재민 몫(회부).

---

## §0-ⓑ 라이선스 원문 요구 — **원문을 받아 확인했다**

| 출처 | 상업 사용 | 변형 | 요구 문구 | 확인한 자리 |
|---|---|---|---|---|
| CMU 모캡 | **가능** — "You may include this data in commercially-sold products" | 가능 | **있다**(아래 그대로) | mocap.cs.cmu.edu |
| | ⚠단서: "you may not **resell this data directly, even in converted form**" | | | 같은 자리 |
| cgspeed 변환본 | 제한 없음 — "I (Bruce) place **no additional restrictions**" | | 없음(CMU 문구로 충분) | cgspeed README |
| 국립국악원 단음 | **가능** | 가능 | **출처표시** | gugak.go.kr — "공공누리 제1유형(출처표시)" |
| 공공누리 1유형 | "상업적, 비상업적 이용가능" | "변형 등 2차적 저작물 작성 가능" | 출처표시 | kogl.or.kr |
| npm 다섯 | MIT(각 패키지 `LICENSE` 실측) | | 라이선스 전문 동봉 | `node_modules` |

요구 문구 둘(원문 그대로 · `CREDITS.md` §1 이 정본):

```
The data used in this project was obtained from mocap.cs.cmu.edu. The database was created with funding from NSF EIA-0196217.
국악기 음원 제공 — 국립국악원 (공공누리 제1유형)
```

⚠**CMU 두 판이 다르게 적혀 있다.** cgspeed README 는 CMU 조건을 "free for use in research **and
commercial** projects worldwide" 로 인용하는데, 지금 mocap.cs.cmu.edu 는 "free for use in research
projects. You may include this data in commercially-sold products, but you may not resell this data
directly, even in converted form" 이다. **지금 자리의 글이 정본**이라 보고 뒤쪽(§2 ③)의 판정은 그걸 따랐다.

---

## §0-ⓒ 요구 문구를 띄우는 자리 — 알림 스택은 **셋**이다

| 잰 것 | 값 | 자리 |
|---|---|---|
| 스택 줄 수 | **3** | `server/notice.js NOTICE_MAX`(정본) · `50-i-panel.js` 가 같은 수 |
| 요구 문구 | **2줄** | ⇒ **머리말을 안 붙인다** — 붙이면 맨 앞 줄이 밀려 사라진다 |
| 판 너비 | 560px · 12px 글자 · `white-space: pre-line` | 긴 영문 한 줄은 **보기에만** 여러 줄이고 스택은 한 칸 |
| 알림 수명 | **2500ms 고정** | 서버 `notice` 에는 `ms` 칸이 없고 클라가 `showNotice(text, undefined, kind)` 로 부른다 |

⇒ 긴 CMU 문구가 2.5초만 떠 있다. 더 오래 두려면 **클라가 `ms` 를 읽어야 한다 = 새 클라 조건**이라
이 카드에서 안 했다(회부). 지금은 다시 치면 다시 뜬다.

---

## §2 작업

### ① `CREDITS.md` (레포 루트 · 새 파일)

여덟 절: 요구 문구 블록 · 빌린 것 표 · 출처 미상 · 더는 안 쓰는 것 · 도구 · 아직 아닌 것(MPFB) ·
**라이선스 충돌 표** · 참고문헌. 원칙 셋을 머리에 박았다 — 찾은 것만 · 요구 문구는 원문 그대로 ·
**이 파일이 정본**(게임이 여기서 읽는다).

### ② `/크레딧` — 새 패널 0 · 사본 0

```
CREDITS.md  §1 블록(<!-- 요구문구:시작/끝 -->  사이의 `- ` 줄)
   ↓ 읽는다 (mtime 캐시 — 파일이 바뀌면 서버 재시작 없이 다시 읽는다)
server/credits.js  requiredLines() → handleChat()
   ↓ 이미 있는 통로
{ type:'notice', text }  × 줄 수   →  알림 스택
```

서버 diff: `zone.js` **두 줄**(require 하나 · 분기 하나 — `followChat` 바로 아래, 이웃 규약 그대로) +
새 모듈 `server/credits.js` 하나. **클라는 한 글자도 안 건드렸다.**

### ③ 라이선스 충돌 표 (`CREDITS.md` §7)

상업 배포에서 실제로 걸리는 것은 **하나**다.

* **CMU "원본 데이터의 직접 재판매 금지(변환본 포함)"** — 게임이 파는 것은 시트(파생물)라 허용
  범위 안이다. 다만 이 저장소는 `assets-src/mocap/` 에 **원본 BVH 다섯을 그대로 담아** 배포한다.
  저장소를 공개·유료 배포하면 BVH 자체가 함께 나간다 ⇒ **재민 판정**: 원본을 뺄지(포즈표만 남긴다)
  둘지. 게임 바이너리만 팔면 지금 그대로 문제 없다.
* MIT 다섯은 **묶어 팔 때 전문 동봉**이 필요하다(소스 배포면 그대로 따라간다).
* 국립국악원·글꼴·그림은 걸리는 것이 없다.

---

## §3 하네스 — `e2e-ui` ⑮절 (133건 PASS 133 · FAIL 0)

| 못 박은 것 | 이 판의 수 |
|---|---|
| 검사 상황 — `CREDITS.md` 에 블록이 있다 | 2줄 |
| **⑮a 알림이 요구 문구 수만큼** 온다(머리말 0) | 2 / 2 |
| **⑮b 글자가 파일 그대로다** — 문자열 대조 | 통과 |
| ⑮c 스택(3) 안에 든다 | 2 ≤ 3 |
| **⑮d 클라 소스에 문구 사본 0** — `public/` 의 js·html·css 전수 | 35파일 훑음 · 0건 |
| ⑮e **자명 통과 금지** — 같은 훑기로 있는 문자열(`showNotice`)은 찾는다 | 통과 |

접점: `handleChat` · `CREDITS` · `showNotice` · `__notices` · `server/credits.js`.

곁 검사: `test-client-globals` **26/0** · `test-harness-lint` **21/1**.
⚠그 빨강 하나는 `e2e-friends.js:185`(판정 자리 이모지)다 — **이 카드가 안 건드린 파일**이고,
변경 전 나무에서도 똑같이 21/1 이다(`git stash` 로 대조). 내 diff 는 네 파일뿐이다.

---

## §4 회부

1. **게임 안 크레딧 화면** — 캐논 판정(재민). 이 카드는 `/크레딧` 알림 최소만 했다.
2. **알림 수명 2.5초** — 긴 문구를 읽기엔 짧다. 늘리려면 `notice` 에 `ms` 칸 = **새 클라 조건**.
3. **출처 미상 둘**(`rd-nature-sheet.png` · `legacy_mac/leaf.png`) — 지울지 출처를 찾을지 재민.
4. **`assets-src/mocap/` 의 원본 BVH** — 저장소 공개·유료 배포 시 CMU 재판매 조항(§2 ③). 재민.
5. **MPFB/MakeHuman 크레딧** — 채택 판정 뒤.
6. **낡은 주석 한 줄** — `40-r2-sprites.js:111` 의 "(Kenney Nature Kit, 초록 recolor)" 는
   배포본과 안 맞는다(그 그림은 다시 구웠다). 클라 무접촉 카드라 안 고쳤다.
7. **`public/assets/char/probeall_walk.png`** — 잠금표 밖 탐침 부스러기 한 장(T162 §0-ⓐ 에서도 나왔다).

---

## §5 접점

`CREDITS.md`(새 파일) · `server/credits.js`(새 파일) · `server/zone.js`(두 줄) ·
`scripts/e2e-ui.js`(⑮절) · `인계/회부.md`(T168 절).
