# T287 — 나루터가 물가에 못 붙으면 조용히 남지 않는다 (`onboarding.js` 한 자리 + 하네스 C 팔)

**[재민 확정 2026-09-14 · T287]** 제품 `server/onboarding.js` **한 자리** · `villages.js`·`sim/` 무접촉 ·
새 수 **0** · 새 손잡이 **0** · 실기 재민.

**한 줄 답 — 나루터 **7 → 0** (제품 술어 C · 27곳 중) · 폴백은 **ⓐ 7곳 / ⓑ 0곳**(넓혀 찾기로 일곱 곳 다 붙었다) ·
하네스 **전 7 / 후 0**(C 팔 `27 PASS / 1 FAIL` → `28 / 0`) · A 팔은 **전·후 모두 0/26 · 28/0**(회귀 0).**

**★§0 이 카드의 전제를 좁힌다 — 실패한 건 `okAt` 이 아니라 `b2` 다.**
카드는 *"`if (b2 && okAt(...))` 가 실패해도"* 라고 적었는데, 일곱 곳 **전부** `b2` 자체가 **null** 이었다:
물이 **18~30셀** 밖인데 `nearestBank` 반경이 **6** 이었다. 그리고 넓혀서 찾은 물가는 **일곱 곳 다 통행 가능**이다.
⇒ 고칠 것은 "도달 불가 처리"가 아니라 **반경**이고, ⓑ(이름 낮추기)는 이 땅에서 **한 번도 안 탄다**(그래도 꼬리로 남겼다).

---

## ⓐ 재현 — 제품 술어(C)로 27곳 전수

`scripts/zone-preds.js`(T279 · 제품 `zone.js:570~650` 의 조각을 그대로 부르는 술어 한 벌)로 도착 지점을 전수했다.

| 마을 | 도착 지점 | **물까지** | `nearestBank` | 넓히면 |
|---|---|---|---|---|
| 어촌2 | (1643,355) | **30셀** | r6 **null** | r40 에 (1643,384) · **통행 가능** |
| 어촌5 | (196,2079) | 21셀 | r6 null | r20 에 (215,2099) · 통행 가능 |
| 어촌7 | (1474,173) | 22셀 | r6 null | r28 에 (1495,173) · 통행 가능 |
| 어촌8 | (30,1761) | 18셀 | r6 null | r20 에 (34,1779) · 통행 가능 |
| 어촌11 | (1722,2692) | 24셀 | r6 null | r28 에 (1727,2718) · 통행 가능 |
| 어촌12 | (702,3274) | 20셀 | r6 null | r20 에 (722,3288) · 통행 가능 |
| 어촌14 | (1816,612) | 24셀 | r6 null | r28 에 (1805,637) · 통행 가능 |

| 땅 | 도착 지점 | 나루터 | 물에서 2셀 초과 | `kind` 분포 |
|---|---|---|---|---|
| **A**(하네스 근사 술어) | 51 | 26 | **0** | road 25 · dock 26 |
| **C**(제품 정본 술어) | 50 | 27 | **7** | road 23 · dock 27 |

**왜 A 가 가려 왔나** — A 에는 해안선 물 띠(`generateCoastlineWaterTiles`)가 없다. 그래서 A 땅에서는
도착 지점이 물에 더 가깝게 잡히고 r6 안에 물가가 들어온다. **제품 정본은 C** 이고(`zone.js:2651` 이 넘긴다),
실서버는 여기에 도랑(`DITCH_CELLS`)까지 더해 **더 막힌다** ⇒ 실기는 재민(아래 ⓓ).

**왜 도착 지점이 물에서 멀어졌나** — 어귀 고르기가 *"걸어 들어갈 거리를 먼저 요구"*(`GATE_MIN_FRAC`)하기 때문이다.
배산임수 취락은 물 쪽이 납작해서 먼 어귀는 **뭍 쪽**에 잡히고, 그 자리에서 r6 으로는 물에 못 닿는다.
`onboarding.js:352~356` 주석이 *"물가 마을이면 그 자리에서 다시 물가로 붙이므로 나루터는 그대로 물가에 선다"* 라고
적어 둔 그 가정이 **반경 6 에서 깨진다**.

---

## ⓑ 폴백 — 제품 한 자리

```js
const _gd = Math.hypot(gate[0], gate[1]);
// ★[T287] 그 반경을 이름 붙여 둔다 — 아래 나루터 폴백이 **같은 값**을 쓴다(사본 0 · 새 수 0).
const _reachR = Math.round(Math.max(_gd, Rcell)) + CFG.ARRIVE_SEARCH + CFG.ARRIVE_OUT + 6;
const reach = _reachSetFrom(ta, ccx, ccy, _reachR);
…
if (kind === 'dock') {
  let b2 = VillageLayout.nearestBank(ta, picked[0], picked[1], 6);
  if (!(b2 && okAt(b2.cx, b2.cy))) b2 = VillageLayout.nearestBank(ta, picked[0], picked[1], _reachR);   // ⓐ
  if (b2 && okAt(b2.cx, b2.cy)) picked = [b2.cx, b2.cy];
  else {                                                                                                // ⓑ
    kind = 'road';
    console.log(`[onboarding] ⚓ ${v.name}: 도달 가능한 물가를 못 찾았다(반경 ${_reachR}) — 나루터 대신 길목으로 내린다`);
  }
}
```

| | |
|---|---|
| 술어 | `VillageLayout.nearestBank` **그대로**(사본 0 — 물가 판정을 다시 안 적었다) |
| 넓히는 폭 | **새 수가 아니다** — 도달 집합을 만든 그 반경 `_reachR`. 그 밖은 `okAt` 이 어차피 거짓이다 |
| 종전 한 줄 | **그대로 먼저 돈다**(r6) — 붙던 곳은 한 곳도 안 움직인다 |
| ⓑ가 타는 조건 | 넓혀도 도달 가능한 물가가 없을 때 — **이 땅에선 0곳** |
| 결과 | 나루터 **7 → 0** · `kind` 분포 **불변**(road 23 · dock 27) |

---

## ⓒ 하네스 — `test-onboarding` 에 술어 팔(`ONB_PREDS=a|c`)

| 팔 | 고치기 전 | 고친 뒤 |
|---|---|---|
| **A**(기본 · 종전 그대로) | `✓ 0/26` · **28 PASS / 0 FAIL** | `✓ 0/26` · **28 PASS / 0 FAIL** |
| **C**(`ONB_PREDS=c` · 제품 정본) | **`✗ 7/27`** · **27 PASS / 1 FAIL** | **`✓ 0/27`** · **28 PASS / 0 FAIL** |

자명 통과 금지 — C 팔은 고치기 전 판에서 **실제로 빨갛다**(위 왼쪽 칸).

**⚠C 팔의 술어 한 벌은 아직 main 에 없다.** `scripts/zone-preds.js` 는 세션5 가지(T279 · `batch/hunt-wood-server-0912`)에 있다.
이 카드는 그 파일을 **베껴 오지 않았다**(사본 0 · 그 가지 무접촉). 대신 하네스는 없으면 **조용히 A 로 안 돈다** —
`⚠ ONB_PREDS=c 를 받았지만 scripts/zone-preds.js 가 없다 — C 팔을 **안 돌렸다**` 를 찍고, 그 줄을 **초록으로 세지 않는다**.
세션5 가지가 main 에 올라오면 C 팔이 **자동으로 켜진다**. (위 C 팔 수는 그 파일을 작업본에 놓고 잰 값이다.)

---

## ⓓ 실기 줄 + 한 줄 답

실서버는 A 도 C 도 아니다 — **도랑까지 있는 땅**이라 더 막힌다. 배포 뒤 한 줄로 확인한다(**재민**):

```
docker exec <zone> node -e "const O=require('/app/server/onboarding');const a=O.arrivals();\
let d=0,f=0;for(const v of a.values()){if(v.kind!=='dock')continue;d++;}\
console.log('도착',a.size,'나루터',d)"
# 또는 열린 문으로: curl -s localhost:<존포트>/startinfo | jq '[.villages[]|{name,kind:.arrive.kind}]|group_by(.kind)|map({k:.[0].kind,n:length})'
```
확인할 것: **나루터 수가 A/C 와 같은가 · `⚓ … 길목으로 내린다` 로그가 몇 줄 찍혔나**(ⓑ가 실서버에서 타면 그 줄이 남는다).

> **한 줄 답 — 나루터 7 → 0 · 폴백 ⓐ 7 / ⓑ 0 · 하네스 전 7 / 후 0 · A 팔 회귀 0.**

---

## 검증

| 항목 | 결과 |
|---|---|
| 제품 diff | `server/onboarding.js` **한 자리**(+18줄 — `_reachR` 이름 붙이기 + 나루터 폴백) |
| `server/villages.js` · `sim/` | **무접촉** |
| 새 수 · 새 손잡이 | **0 · 0** |
| `lab-modules --diff` | **교집합 0개** — `onboarding.js` 는 econ 랩 밖이다(카드 예측 그대로) ⇒ 3시드 800일 A/B **갈음** |
| `test-onboarding` A | **28 / 0**(전·후 동일) |
| `test-onboarding` C | 전 **27 / 1**(`✗ 7/27`) → 후 **28 / 0**(`✓ 0/27`) |
| `e2e-onboarding` | **PASS 75 / FAIL 0** |
| 러너 전수 | **안 돌렸다** |
| 무접촉 | T288(세션5) · T285(세션1) · T284(세션10 `villages.js` 전쟁 절) |
| 접점 | `onboarding.js:398` `okAt` `kind` `dock` `zone-preds` `isWaterTileLocal` `isBridgeLocal` `test-onboarding` `ARC_FIRST` |

## 회부

- **`scripts/zone-preds.js` 를 main 으로** — C 팔은 그 한 벌에 달려 있다(세션5 T279 가지).
  올라오기 전까지 `ONB_PREDS=c` 는 "안 돌렸다"를 찍는다. → **세션5 착지 · PM**
- **#22 영토+방아쇠 켬**(재민 · PM 권고 켬 · T278 착지 뒤) · **영토 행 RLE**(캐논 · 재민) ·
  첫 의뢰(#18) · 추천 자격(#19) · **#31 실기**(위 ⓓ 한 줄).
- **어귀가 물에서 멀어지는 것 자체**는 안 건드렸다 — `GATE_MIN_FRAC` 이 "걸어 들어갈 거리"를 지키는
  의도된 규약이고, 나루터는 이제 그 자리에서 물가로 **붙는다**. 어귀 규약을 바꿀지는 **캐논 · 재민**.

## 실행

```
node scripts/test-onboarding.js                 # A 땅(기본)
ONB_PREDS=c node scripts/test-onboarding.js     # C 땅(제품 정본 술어 — zone-preds.js 필요)
node scripts/e2e-onboarding.js                  # 실클라 호
```
