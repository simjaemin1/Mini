# T185 — 러너에서만 빨간 셋 · `test-events ⑲c` 이분할 (2026-09-12)

측정 베이스 `16b437cb` · **푸시 베이스 `976dd192`**(리베이스 1회 · 겹침 0) ·
가지 `batch/harness-stable-0912` · **게임 코드 0**(하네스만) · 서버 diff 0 · 자산 무변 · 새 손잡이 0 · force 0.

---

## §0-ⓐ 흔들리는 전제 — 셋 다 **같은 병**이었다

족보 ⑩: *하네스는 정해진 초를 자지 않는다.* 셋 다 조용한 판에서 고른 벽시계 수를 판정의
근거로 삼고 있었다. 러너 안(2코어 · 열 몇 종 동시)에서는 그 수가 안 선다.

| 하네스 | 어느 줄이 시간을 가정하나 | 부하에서 왜 깨지나 | 실측 |
|---|---|---|---|
| `test-route-persist ⑦d` | `ok(hcMax <= 1500, …)` — 하네스가 `/health` 를 100ms 마다 찔러 **응답 간격**을 재고 상수와 견준다 | 그 수에는 **하네스 제 이벤트 루프 지연·fetch 왕복**이 섞인다. 서버 정책과 무관하게 커진다 | 러너 **1506ms**(6ms 차로 빨강) · 단독 **472ms** — 같은 코드·같은 정책 |
| `e2e-emptystart warp` | 텔레포트를 보내고 `sleep(900)` → 한 번 보기 × 20회(총 18초) | 900ms 안에 서버 틱이 권위 좌표를 돌려준다는 전제. 밀리면 20회를 다 쓰고 `null` → "마을에 도착했다" **전제**가 빨강 | 러너 빨강 · 단독 19/0 |
| `e2e-events ensureBoard` | 예산이 **시도 횟수**(30 · 회당 ~1.8초 벽시계) | 픽스처가 기다리는 것은 **게임일**(0.5초/일)로 자라는 소비EMA 다. 존 틱이 밀리면 같은 벽시계에 게임일이 덜 흘러 EMA 미성숙 | "성립 실패 — 소비EMA 미성숙" |

### 고친 방법 — **판정은 그대로, 증인만 바꿨다**

| 하네스 | 옛 증인(하네스가 잰 것) | 새 증인(세계가 말하는 것) |
|---|---|---|
| `⑦d` | `/health` 응답 간격 ≤ 1500ms | **서버 자신의 이벤트 루프 지연**(`/perf` 의 `loop` · 창을 열 때 `?reset=1` 로 영점). 문턱도 안 지어냈다 — **같은 창에서 서버가 기록한 가장 무거운 작업**(`events` 링 최대 ms)과 견준다: 조각나 있으면 `loop.max < evMax`, 슬라이서를 빼면 둘이 같아진다 |
| `warp` | 보내고 900ms 자고 한 번 | 보낸 뒤 **권위 좌표를 120ms 마다 들여다보고 도착하면 즉시 끝**(한 번에 3초까지 기다려 준다). 빠른 판은 종전보다 빠르고 느린 판은 기다려 준다 |
| `ensureBoard` | 시도 30회 | **게임일 예산**(`window.__evGameDay`). 같은 수(30)를 **게임일**로 읽는다 — 새 수 0 |

⚠**`e2e-events` 는 부하 문제만이 아니었다.** 게임일로 바꾸고도 단독에서 빨갰다. 재 보니
`sh`(픽스처 증인) 를 **픽스처를 부른 직후 마지막 세 줄**에서만 찾고 있었는데, 마을이 제 힘으로
부족해지면 고리가 **게시판 경로로 먼저 돌아가** 그 줄을 안 본다 — 그 알림은 그 뒤에 도착한다
(실측: `농촌1 wood 0→6.334 (문턱 7.038 · 갚을거 food 100000)` 이 **게임일 정지 뒤**에 찍혔다).
⇒ 판정 직전에 **알림 이력 전체**(40건 규약)를 훑는다. 서버가 한 말은 사라지지 않는다.

### 고친 뒤 — 단독

| 하네스 | 전 | 후 |
|---|---|---|
| `test-route-persist` | 52/0(단독) · 러너 빨강 | **53/0** — ⑦d 가 `루프 max 860.88ms < 가장 무거운 작업 2361ms` 로 선다 |
| `e2e-emptystart` | 19/0(단독) · 러너 빨강 | **19/0** — "농촌1 마을에 도착했다(서버 권위)" |
| `e2e-events` | 59/1 | **60/0** — `농촌1 wood 21.511→5.059 (문턱 5.621 · 갚을거 food 100000)` |

(⑦d 가 한 줄 는 것은 판정을 **상황 + 판정** 두 줄로 갈랐기 때문이다 — "창 안에서 서버가 무거운
작업을 실제로 했다"가 0이면 아래가 자명 통과라서.)

---

## §0-ⓑ `test-events ⑲c` 이분할

`git bisect run` — 범위 `4e273f95`(09-07 야간 · 초록) ~ `6e236bc7`(09-12 · 빨강) · 85 커밋 · 7단계.

| | |
|---|---|
| **첫 빨간 커밋** | **`363c38cb`** (2026-09-06) |
| 제목 | `[승인 대기 · T135 3판] 두 항 다 실물대로 · 열매 넷이 품목이 된다` |
| 카드 · 세션 | **T135 3판** · 세션3(보고 `보고/T135_2026-09-06.md` 머리) · 재민 승인 2026-09-07 → main |
| 만진 자리 | `sim/economy-engine.browser.js` · `server/chunk.js` · `server/specialty.js` · `server/trees.js`·`.json` |
| **기전(한 줄로 확인)** | `T135_TREES=0` 으로 되돌리면 **⑲c 가 초록**이다. 그 커밋이 되돌림 손잡이 안에 넣은 **품목 셋(acorn·chestnut·mulberry_fruit)** 이 TRADABLE 을 늘리고, 늘어난 캐러밴 중 하나의 `arriveDay−departDay ≠ travelDays` 다 |
| 이 카드가 한 것 | **이분할까지.** 고치지 않았다 — E 영역(사건·소문) 밖이고 그 카드 몫이다(회부 ①) |

---

## §0-ⓒ 러너 전수 — **143/143 · 초록 134 · 빨강 9**

베이스 `16b437cb` + 이 판 diff(하네스 4) · 청크 4종씩 · 병렬 0 · 청크 사이 `kill -9` 로 잔류
크로뮴·존·중앙 정리 · 미측정 0(야간 09-12 는 108종까지만 닿아 **35종 미측정**이었다).

### 판정 — 이 카드가 고친 셋이 **러너 안에서** 초록인가

| 하네스 | 야간 09-12(`6e236bc7`) | **이 판 러너 안** | 단독 |
|---|---|---|---|
| `test-route-persist` | **51/1** (`⑦d /health 최대 1506ms`) | ★**53/0** (`⑦ 한 걸음 최대 1622ms` 를 안고도 통과) | 53/0 |
| `e2e-emptystart` | **16/3** (`★전제 — 농촌1 마을에 미도달`) | ★**19/0** | 19/0 |
| `e2e-events` | **59/1** | ★**60/0** | 60/0 |

⇒ 카드의 판정 조건(**셋이 러너 안에서 초록**)이 섰다. 단독에서만 초록이던 것이 아니다.

### 빨강 9종 전부 — HEAD 대조 귀속 (T135 §3-b)

| 하네스 | 러너 | 단독 재실행 | 귀속 |
|---|---|---|---|
| `test-tick-slicer` | 19/5 | — | **상시(기계)** — `인계/공통.md` 목록 그대로 |
| `e2e-rtt` | 8/2 | — | **상시(기계)** — 목록 그대로 |
| `test-body` ⑯㉧×2 | 304/2 | — | **상시(물때)** — 09-07 야간이 `TIDE_FREEZE_MS=372603` 으로 306/0 임을 이미 쟀다 |
| `test-events` ⑲c | 271/1 | 빨강 재현 | ★**T135 `363c38cb`** — §0-ⓑ. 이 카드는 이분할까지 |
| `test-site-memo` ② | 13/1 | ★**14/0** | 부하 흔들림 |
| `e2e-downed` | 12/31 | ★**35/0** | 부하 흔들림 |
| `e2e-membership` ④-0 | 24/1 | ★**25/0** | 부하 흔들림 |
| `e2e-verbs` | 66/10 | ★**77/0** | 부하 흔들림 |
| `e2e-zoom` | 27/1 | ★**28/0** | 부하 흔들림 |

**상시 · T135 · 부하 흔들림 밖의 빨강 = 0.** 야간이 진짜라 적었던 둘(`test-regrow ★★⑤` →
T148-B · `test-harness-lint ②` → T174)은 그 사이 착지해 **각각 97/0 · 33/0 초록**이다.

**이 판 diff 와의 접점 0(구조로).** 만진 파일은 하네스 넷뿐이고, 빨간 아홉 중 어느 것도 그 넷을
`require` 하지 않는다 — 이름이 뜨는 두 곳(`test-site-memo.js:59` · `e2e-verbs.js:955`)은 **주석**이다.

### 부하 흔들림 다섯 — 같은 병인가

다섯 다 **러너 안에서만** 빨갛고 단독이면 초록이다. 다만 §2 의 자(`ok()` 첫 인자에 벽시계 상수)로는
**0건**이다 — 즉 판정 자리가 아니라 **픽스처·전제 자리**에서 밀린다(존 입장 · 메뉴 열림 ·
`게임일 정지` 가 `""` · 두 판 결정성 · 화소 차). 기전은 각자 다르고 **이 카드는 안 쟀다** → 회부 ④.

### 리베이스 뒤 다시 쟀다 (족보 (138))

전수 표는 **`16b437cb`** 에서 뜬 것이다. 그 사이 main 이 **47커밋** 움직여(밭 배율 T190 ·
장부 T193 · 도착 T203 · 게스트 이름 T208 · 나무 슬라이드 T194 …) 세계가 달라졌다.
**겹침 0**(그 47커밋은 내 파일 넷을 안 만졌다) 이지만, 한 이야기로 덮지 않고 **넷을 다시 쟀다**:

| 하네스 | `16b437cb` | **`976dd192` 리베이스 뒤** |
|---|---|---|
| `test-harness-lint` | 33/0 (⑧a 146개 훑음 · 0건) | **33/0** (⑧a 146개 훑음 · **0건** — 새로 들어온 `e2e-onboarding` 622줄 포함) |
| `test-route-persist` | 53/0 | **53/0** — `루프 max 815.79ms < 가장 무거운 작업 3669ms` |
| `e2e-emptystart` | 19/0 | **19/0** |
| `e2e-events` | 60/0 | **60/0** |

⚠**전수 143종은 리베이스 뒤에 다시 안 돌렸다** — 이 상자에서 한 판이 3시간 24분이다(아래).
다시 돈 것은 이 카드가 만진 넷뿐이고, 위 표가 그 전부다. 못 쟀으면 못 쟀다고 적는다.

### 이 상자에서 전수가 얼마나 걸리나 (§3-3 용 실측)

| | |
|---|---|
| 03:49:04 **80종** → 07:12:40 **143종** | **63종에 3시간 24분** (그중 **e2e 48종**) |
| 단위 95종 | 빠르다 — 야간도 완주했다 |
| 야간 창 3시간 39분 | 108종까지 · **35종 미측정** |

⇒ 야간 창을 e2e 가 다 도는 데 필요한 만큼 늘리려면 **3시간으로는 안 된다**(e2e 48종만 3시간 넘음).

---

## §2 검증 — `test-harness-lint` ⑧절

판정 자리에 **"하네스가 잰 시간 vs 상수"** 가 없는지 AST 로 본다(낱말 grep 아님 — `ok()` 의
**첫 인자**만, 비교 연산자, 한쪽이 시간 이름(`hcMax`·`elapsed`·`took`…) 다른 쪽이 100 이상 숫자).

| 못 박은 것 | 이 판의 수 |
|---|---|
| **⑧a 판정 조건에 벽시계 0** | 146개 훑음 · **0건** |
| ⑧b 자명 통과 금지 — 미끼 한 줄을 넣으면 잡는다 | 1건 |
| **진짜 대조군** — HEAD 의 옛 `test-route-persist.js` 에 이 자를 대면 | `305줄 ok(hcMax <= 1500, …)` 를 **잡는다** |

`test-harness-lint` **33/0**(T171 31/0 → +2).

## §3 회부

1. **`test-events ⑲c` 고침** — 귀속 카드 **T135**(세션3). 이 카드는 이분할까지만 했다.
   기전은 `T135_TREES` 안의 품목 셋이 늘린 캐러밴이다(위 표).
2. **상시 목록 갱신**(PM · `인계/공통.md`) — `e2e-charsprite`·`e2e-cold` 는 초록이니 뺀다.
3. **야간 창**(143종/3h · PM) — 이 상자에서 전수는 3시간이 아니라 **훨씬 길다**(§0-ⓒ 표).
   e2e 48종만 3시간을 넘는다. 창을 늘리든 e2e 를 이틀로 쪼개든 PM 판정거리.
4. **부하 흔들림 다섯**(`test-site-memo ②` · `e2e-downed` · `e2e-membership ④-0` · `e2e-verbs` ·
   `e2e-zoom`) — 러너 안에서만 빨갛고 단독이면 초록이다(§0-ⓒ 표). 이 카드가 고친 셋과 **같은 계열로
   보이지만 자리가 다르다** — 판정 자리엔 벽시계가 0건이고(§2 의 자) 픽스처·전제 자리에서 밀린다.
   기전은 **안 쟀다** — 다음 하네스 안정화 카드 몫이다.
