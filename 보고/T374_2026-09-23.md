# T374 — **채취는 수요가 멈춘다** — 등가 4,106% → 152.6% · 군락 96.2% 보존 · 군락 0 마을 0 · 새 수 0

가지 `batch/demand-stop-0923` = **`2a1b8208`**(코드 1커밋 · 그 위 문서 커밋은 이 해시 채우기뿐 · **새 가지**) · 베이스 `450c6e44`(카드 베이스 `e37f5ba6` 의 자손 · 푸시 때 main `03030560`) ·
**force 0** · `.lock*` **0**(맥 VM·컨테이너) · 실기 0 · main 은 PM — 안 밀었다
손잡이 `T374_DEMAND_STOP` **기본 끔** · **켜지 않았다** · `chunk.js`·`zone.js` **무접촉** ·
**T370(세션3)·T372(세션8)·T364(세션10) 무접촉** · 새 수 0 · 새 행동 0

> **한 줄 ① — 상한은 지어낸 수가 아니다. 수식이 그날 내겠다고 한 몫이다.** 설계 ⓓ 의 빠진 절반
> (*실체가 더 낼 수 있어도 마을은 쓸 만큼만 딴다*)이고, 그 "쓸 만큼" 은 이미 정본 관측 칸이다 —
> 채집 `_forageOutLast × _t347MixShare` · 나무 `_woodOutLast` · 물고기 `_fishOutLast`. 몸통 하나를 셋이 쓴다.
>
> **한 줄 ② — 켜니 숲이 산다. 등가 4,106% → 152.6%(곳간 기준).** 군락 **96.2% 보존**(T359 는 절반이
> 사라졌다) · **군락 0 마을 20~27 → 0** · 행위 마을 **50 유지** · 되살아난 554~815 ·
> **걸음 한도 0 인 날 16~21/31 → 0/31**(멈추는 것이 걸음이 아니라 **수요**다) · drop **5 → 0**.
> ⚠100% 가 아니다 — `D` 는 **econ 하루**, 따는 것은 **생활 하루**라 오늘치가 econ 하루에 ≈1.5번 리셋된다.
>
> **★한 줄 ③ — 그 실측이 T347 의 빚을 찾았다.** `_t347InflowToday` 가 리셋되지도 장부에 오르지도 않았다
> (T312·T325 에 있는 장부 다리가 없었다) ⇒ 채집 입고가 `dailyProduction` 에 한 번도 안 올랐고, 수요 문이
> 첫날 뒤로 항상 0 이 됐다(첫 판 등가 **12.4%**). 그 다리를 놓았다(잠재 자리 7→8 · 이름으로 못 박음).
>
> **★한 줄 ④ — 카드의 예상과 달랐다: 나무는 무변이 아니라 절반(100.7% → 54.9%).** 대신 숲이 더 남는다
> (30일 손실 406 → 209그루). 같은 시계 어긋남이 **반대 방향**으로 문 것 — 두 자원이 같은 배율(≈1.5)의
> 반대쪽으로 움직였다는 것이 한 몸통으로 물렸다는 증거다. ⇒ 다음 한 줄은 **수요 문의 시계를 생활 하루로**.

* 새 코드 = `economy-sim.js` 수요 문 둘(손에 든 것 인자 하나) + 품목별 오늘치 + **채집 장부 다리**(T347 빚) ·
  `villages.js` 멈춤 두 자리(헤드리스 `break` · 관측 **퇴근 한 줄** `_t374Done` → `_lifeGoHome(npc, '휴식')`)
* 하네스 `test-forage-act` **137/0**(main 104 + **② 2 · ⑭ 31** 신설 — 관측 퇴근은 문자열 핀이 아니라 **정본을 실제로 부른다**: `_t374Done` 4건 · **`npcLifeTick` 그대로** 3건(`__labProbe._t374Probe`)) · `test-wood-act` **90/0**(main 87 + 3) · `test-fish-act` 73/0 ·
  `test-econ-fieldyield` **199/0**(main 197 · 잠재 자리 7→8) · `test-trees` 91/0 · `test-resource-index` 20/0 ·
  `test-harness-lint` 61/0 · `lab-wiring-check` 실패 0 경고 0 · 3사본 `--check` 8/8 · inline·path·trees 최신 ·
  `npcLifeTick`·`__labProbe` 를 읽는 이웃 하네스 13종(러너 `run-regress.sh` · psite·crops·distmatrix·farm-metrics·frontier-iron·headless-parity·hunt-port·membership·move-soa·terr-backfill·village-found·villager-stuck·onboarding) **전부 RC=0**
* 착지 시험: 그 사이 main 이 `03030560` 으로 13커밋 움직였다(겹친 파일 `villages.js` 하나 — T364 전쟁 경로·위협 칸, 내 자리와 멀다).
  이 가지를 그 위에 얹는 시험 병합이 **충돌 0** 이고 그 트리에서 forage 137 · wood 90 · fish 73 · fieldyield 199 · trees 91 ·
  psite · lint 61 · 배선 0/0 · 3사본 `--check` **전부 녹색**(가지는 베이스 `450c6e44` 그대로 올린다 — 3시드가 그 트리의 증거다)
* 끔 비트 동일: 청크 459개 해시 `2e3fb1c9…db25` = **베이스 `450c6e44` 와 같은 글자**(`chunk.js` 무접촉) ·
  `sim/` 을 만졌으므로 3시드 면제 **없음** → 3시드 800일은 §6 에 적었다
* ★자기신고: 컨테이너 초기화로 첫 판 `94db6dd8` 이 사라져 다시 세웠고, 다시 세우며 **관측 갈래 결함 셋**을 고쳤다
  (멈춤이 `return false` = `zone.js` ④ 아무 자원 채집 · 손에 든 것을 안 셈 · 자리 — §0). 존 실측 §2~§4 는 관측 갈래를 안 지나 무변
* 회부: **수요 문의 시계(생활 하루)** · 알갱이 잔재 1~7% · T359·T374 켜기(재민) · 분포·가장자리·`berry→fruit`·링 접기·T333 스캔

---

## 0. 자기신고 — 컨테이너가 초기화됐다

첫 구현 커밋 `94db6dd8` 은 **번들을 뜨기 직전에 컨테이너가 초기화되어 사라졌다**(원격에 한 번도 안 올라갔다 —
`batch/demand-stop-0923` 이 원격에 없음을 확인했다). 이 가지는 **같은 세션의 패치를 그대로 다시 적용해
현재 main `450c6e44` 위에 다시 세운 것**이다. 그 사이 main 이 18커밋 움직였고 내 파일 중 겹친 것은
`test-forage-act.js` ⑩(T372 · 덤불 양의 이름)뿐이며 내 패치와 자리가 안 겹친다. 다시 세운 트리의 하네스·청크 해시는
위 목록 그대로 전부 녹색이다.
⚠**존 위 실측(§2~§4)의 원자료 JSON 도 같이 사라졌다.** 표의 수는 그 실행들이 이 세션에 찍은 출력을 옮겨 적은 것이다
(실행 자체는 초기화 전에 끝났다 · 30일 창 존 실측은 벽시계 지터가 커서 다시 돌리면 같은 판도 수가 달라진다 — T341 §3).

**★다시 세우며 관측 갈래에서 결함 셋을 찾아 고쳤다**(`94db6dd8` 에 있던 것 — 원격에 안 올라갔으므로 이 가지가 첫 판이다):

| | `94db6dd8`(사라진 판) | 이 가지 |
|---|---|---|
| ⓐ 무엇으로 멈추나 | `return false` — "종전 갈래 · 새 행동 0" 이라 적었다 | ★**틀렸다.** 그 폴스루는 `zone.js` ④ **"가까운 자원 채집"**(반경 400px 아무 자원)이다 — 멈춤이 아니라 **딴 데서 딴다**. ⇒ `_lifeGoHome(npc, '휴식'); return true;` — 농부의 "창 밖 = 오늘 휴무" 와 **같은 한 줄**(새 행동 0 · 위 배정이 먼저 돌므로 "그 뒤는 생활층이 다른 일을 준다" 도 그대로) |
| ⓑ 손에 든 것 | 안 셌다(곳간 오늘치만) | ★카드 ① 은 **"손에 든 것 + 오늘 곳간에 넣은 것"** 이다. 관측 갈래는 낮 내내 손에 쥐고 `_lifeDaily` 에 곳간에 넣으므로, 곳간만 보면 **낮 동안 멈춤이 한 번도 안 선다.** ⇒ 몸통에 인자 `held` 하나 · 손은 `_t374Held` 가 곳간 다리와 **같은 사람 목록·같은 품목**으로 센다(쓰기 0) |
| ⓒ 자리 | 두 줄이 레거시 현장 갈래(`lumberjack‖miner‖forager`) 안 | ★나무꾼 줄은 **T325 갈래 뒤**라 행위 마을 나무꾼에게 한 번도 안 닿았고, 두 줄 다 **행위 층이 안 켜진 마을**에도 걸렸다(그 마을은 `_t347MixShare` 0 ⇒ D 0 ⇒ 채집꾼이 늘 폴스루). ⇒ 직업 실작업 머리 **한 자리**(배정 뒤 · 두 갈래 앞) · `_t374Done` 안에서 `forageActOn`·`woodActOn` · 손잡이가 **첫 줄**(끄면 손 합도 안 센다) |

⚠**§2~§4 존 실측은 이 수리에 안 움직인다** — 그 판의 손잡이는 계측기가 켜는 하나(`t347-forage-day` → `T347_FORAGE_ACT` ·
`t334-wood-day` → `T325_WOOD_ACT`)에 카드의 두 팔(`T359`·`T374`)뿐이었고 접속자 0 이다. `T312_FISH_ACT` 가 꺼져 있으면
마을 주민은 `npcLifeTick` 을 **한 번도 안 지난다**(`zone.js` `_t316WalkAlways` 거짓 ∧ `isPositionActive` 거짓 ⇒ 걸음 건너뜀).
헤드리스 하루(`_lifeDaily`)만 돌았고 그 자리는 안 바뀌었다. 관측 갈래가 실제로 도는 세계는 **관측자가 있거나
`T312_FISH_ACT` 를 켠 걷는 세계**(T361·T370 자)다 — 그 세계의 켬 판은 이 카드가 재지 않았다(회부 §5).

## 1. ① 멈춤 규칙 — 몸통 하나, 자리 둘

```
남은 수요 = D − 오늘 넣은 것 − 손에 든 것   (D 가 0 이면 0 ⇒ 안 딴다)
D — 채집 `_forageOutLast × _t347MixShare` · 나무 `_woodOutLast` · 물고기 `_fishOutLast`   ← 전부 정본 관측 칸
끄면 Infinity                                (자르는 문이 아예 없다 ⇒ 비트 동일)
```

| 자리 | 무엇 |
|---|---|
| `actDemandLeft(v, D, todayKey, held)` | 남은 수요 — 몸통 하나(`held` = 손에 든 것 · 안 주면 0) |
| `actDemandCap(v, units, D, todayKey)` | 곳간 입구에서 **한 번 더** 자른다(손을 안 뺀다 — 넣는 그것이 그 손이다) |
| `forageDemandLeft`·`woodDemandLeft`·`fishDemandLeft` | `D` 를 어느 칸에서 읽나 — **그것만** 다르다 |
| 생활층 · 헤드리스 | 채집·나무 픽 루프가 수요 0 이면 `break`(그날 끝 · 손은 먼저 비웠으므로 0) |
| 생활층 · 관측 | 직업 실작업 머리 한 줄 `if (_t374Done(vil, job)) { _lifeGoHome(npc, '휴식'); return true; }` — **퇴근**(농부 "창 밖 = 오늘 휴무" 와 같은 줄 · 새 행동 0) · 행위 마을·그 직업만 · 손까지 센다 |

## 2. ② 켬 판 — 등가 전/후

존 위 틀 셋(45/60/75일) · 30일 창 · `DAY_MS 6,000` · 51마을.

| 판 | 딴 개체 | 수식 걷은 몫 합 | **비** | 군락 시작→끝 (K) | 되살 | 한도 0 인 날 | 행위 마을 | 군락 0 마을 |
|---|---|---|---|---|---|---|---|---|
| T347 끔(군락 없음) | 25 / 16 / 28 | 1,360 / 1,707 / 1,915 | **2.4 / 1.2 / 1.9%** | 238→213 등 | 0 | 29 / 29 / 28 | 49→44 등 | 1→6 등 |
| T359 켬(수요 문 없음) | 10,980 / 10,910 / 12,512 | 438 / 612 / 468 | **4,441 / 3,152 / 4,725%** | 20,856→**10,024** 등 | 135~345 | 21 / 19 / 16 | 50→**27** 등 | 0→**23** 등 |
| T374 켬(★빚 고치기 전) | 121 / 100 / 126 | 1,591 / 1,482 / 1,879 | **13.5 / 12.0 / 11.9%** | 20,856→20,799 | 46~70 | **0** | 50→50 | **0** |
| **T374 켬** | 1,330 / 1,476 / 1,606 | 1,407 / 1,692 / 1,792 | **167.5 / 154.6 / 158.9%** | 20,856→**20,027~20,106** | **554~815** | **0 / 0 / 0** | **50→50** | **0 / 0 / 0** |

평균 **1.8% → 4,106% → (12.4%) → 160.3%**(딴 개체 기준). 3/3 같은 부호 ⇒ T252 자로 **가름**.

**곳간 기준으로 다시** — 이 팔은 행위 마을이 50→50 으로 **안 변하므로** 누계(`delivered`)가 유효하다:

| 판 | 곳간에 든 단 | 수식 걷은 몫 합 | **비(곳간)** | 딴 개체 × w̄(세계 비용) | 차(못 든 단) |
|---|---|---|---|---|---|
| 45 | 2,339 | 1,407.0 | **166.2%** | 2,357 | 18 |
| 60 | 2,443 | 1,692.2 | **144.4%** | 2,616 | 173 |
| 75 | 2,639 | 1,791.7 | **147.3%** | 2,846 | 207 |

⇒ **곳간 기준 평균 152.6%.** 세계 비용과 곳간의 차 **1~7%** 는 딴 개체의 마지막 알갱이가 상한에 걸려 못 드는 몫이다(§5).

**왜 100% 가 아닌가 — 시계가 둘이다.** `D` 와 오늘치 리셋은 **econ 틱**이고, 따는 것은 **생활 하루**(`_lifeDaily`)다.
한 econ 하루에 생활 하루가 여러 번 오면 그만큼 `D` 를 여러 번 받는다. 두 실측이 **같은 배율**을 가리킨다:

| 자원 | 수요 문 없음 | 수요 문 켬 | 배율 |
|---|---|---|---|
| 채집 | (상한 없음) | **152.6%** | ×1.53 초과 |
| 나무 | 100.7% | **54.9%** | ×0.55 부족 |

채집은 한 생활날에 `D` 를 **넘게** 낼 수 있어 넘치고, 나무는 한 생활날에 `D` 보다 **적게** 내므로 econ 하루로 자르면
throttle 이 된다 — 부호가 반대인 것이 같은 원인의 두 얼굴이다. ⇒ **수요 문의 시계를 생활 하루로 맞추는 한 줄**이 다음이다.

## 3. 군락 곡선과 부하

T359 는 30일에 군락이 절반이 됐다(20,856 → 8,034~10,024 · 군락 0 마을 20~27). T374 는 **96.2% 가 남는다**
(20,027~20,106 · 군락 0 마을 **0** · 행위 마을 50). 되살아난 개체가 **554~815** 로 는 것은 딴 수가 줄어 재생이 따라붙은 것이다.

| 수 | 채집 팔 끔 | 채집 팔 켬(T359+T374) | T359 단독(참고) |
|---|---|---|---|
| 존 틱 p50 (ms) | 2.543 | 2.891 | 2.332 |
| p95 / max (ms) | 3.826 / 152.0 | **467.8 / 1,742** | 325.0 / 1,377.8 |
| **drop** | 0 | **0** | **5** |
| lag | 0% | **1.765%** | 2.121% |
| 군락 개체 / 인구 | — | 20,361 / 2,044 | 9,163 / 2,050 |
| 사람당 µs | — | **1.414**(예산 3.3 의 43%) | 1.138 |

⇒ **drop 이 5 → 0**, lag 2.121% → 1.765%. 그런데 **p95 는 커졌다**(325 → 468) — 수요 문이 군락을 **살려서**
하루 경계 스캔이 훑을 개체가 9,163 → 20,361 로 는 것이다. 그 스캔 수술(반경 훑기 → 격자 역산)은 T347 §7-자 의 **T333 칸**이다.

## 4. ④ 나무·물고기 — **카드의 예상과 달랐다**

| 판 | 입고 합(단) | 수식 합 | **비** | 나무 시작→끝 | **30일 손실** | 되살 | 벤 합 |
|---|---|---|---|---|---|---|---|
| T341(수요 문 없음) 45 / 60 / 75 | 2,946 / 2,634 / 4,467 | 3,192 / 3,146 / 3,546 | **92.3 / 83.7 / 126.0%** | 1,873→1,467 등 | **406 / 331 / 600** | 147~203 | 564 / 464 / 799 |
| **T374 켬** 45 / 60 / 75 | 2,195 / 1,825 / 1,728 | 4,701 / 3,015 / 3,001 | **46.7 / 60.5 / 57.6%** | 1,884→1,675 등 | **209 / 165 / 153** | 120~137 | 357 / 298 / 280 |

평균 **100.7% → 54.9%**. 카드는 *"나무는 자원이 모자라 값이 안 변할 것"* 이라 했는데 **절반이 됐다**. 대신 **숲이 더 남는다**.
⇒ **"무변이 증거다" 는 성립하지 않는다.** 증거는 다른 쪽에 있다 — 두 자원이 **같은 배율의 반대 방향**으로 움직였다는 것이
규칙이 한 몸통으로 물렸다는 증거다(사본이면 한쪽만 움직였을 것이다).
**물고기** — 곳간 입구에만 문을 걸었다(걷는 갈래는 T312·T340 몫이라 안 만졌다). `test-fish-act` **73/0** 무변.
어부에게 멈춤 줄이 따로 없는 이유: 셀 예산(`fishBudgetPerCell` = `_fishOutLast ÷ 강가 셀` · `_actTake` 가 하루마다 깎는다)의
하루 합이 **정의상 D** 다(T312 ⓓ①) — 멈춤이 이미 예산 안에 있다. 나무·채집은 예산을 걷어냈으므로(T341) 이 카드가 필요했다.

## 5. ③ 못 채운 마을 19/45 · 회부

**못 채운 마을 19/45 는 이 카드 밖**이다(T359 §3 그대로). 다만 T374 를 켠 판에서 **군락 0 마을이 0** 이고 행위 마을이
50 을 유지하므로, 분포 문제의 증상(마을이 게이트에서 빠지는 것)은 사라졌다.

* ★**수요 문의 시계** — `D` 는 econ 하루, 따는 것은 생활 하루. 실측 배율 ≈1.5(채집 +52% · 나무 −45%).
  **생활 하루로 맞추는 한 줄**이 다음이고, 두 시계의 주인 칸이다(T324·T347 하루 시차 규약).
* **알갱이 잔재 1~7%**(18~207단) — 개체를 **먼저 빼고 나중에 자르기** 때문이다(T334 부류).
  "이 개체를 담을 수요가 남았나" 를 **빼기 전에** 묻는 한 줄 — 위 시계 카드와 같이 가는 게 싸다.
  관측 갈래도 같은 부류다: 멈춤이 서기 직전의 마지막 한 번(한 그루·한 포기)이 손에 들고, 곳간 다리가 상한까지만 받고
  **손을 0 으로 비운다**(`_t325Deliver`·`_t347Deliver`) — 남는 몫을 손에 두는 것은 두 다리의 계약("손은 그때 비운다")을 바꾸는 일이라 같은 회부다.
* **T359·T374 켜기(재민)** — §2·§3 표가 그 입력이다. 지금도 숲은 살지만 등가가 152.6% 다.
* **걷는 세계의 켬 판** — 관측 갈래의 퇴근 한 줄은 **하네스로만** 섰다(정본 `_t374Done` 4건 · 정본 `npcLifeTick` 3건 · §0). 존 실측 §2~§4 는
  헤드리스였다. `T312_FISH_ACT` 를 켠 걷는 세계(T361 자)에서 T347·T325·T359·T374 를 같이 켠 판은 아직 안 쟀다.
* 분포(지형 술어 쪼개기 · PM) · 숲 가장자리 띠(둘째 문턱 · 재민) · `berry→fruit`(PM) · 링 군락 접기(재민) ·
  군락 스캔 수술(T333) · T368 농부(이 뒤).

## 6. 3시드 800일 — `sim/` 을 만졌으므로 면제 없음

`scripts/t176-ab.js 800 <시드>`(손잡이 **전부 끔** · 생활층 계측 · 여덟 수 = 인구 · 소멸 · 무기Q · 확장셀 · 게시 · 도구Q · 보존식 · 생곡).
두 팔이 **같은 시딩 캐시**(`LAB_SEEDCACHE` · 51곳 배치)를 읽는다 — 가지 = 이 커밋의 트리, 베이스 = `450c6e44` 깨끗한 워크트리.
⚠끔 팔은 이 커밋의 **최종 트리**에서 돌렸다(`sim/economy-sim.js` 14:41 · `server/villages.js` 14:48 저장 → 끔 팔 시작 15:04 ·
그 뒤로 `sim/`·`server/` 무변 — 그 뒤 고친 것은 하네스·문서뿐). 수리 전 트리의 1020 한 판도 같은 표였다(JSON 다른 칸 0).

| 시드 | 넷째 판(기준선) | 깨끗한 베이스 `450c6e44` | **이 가지 끔 팔** | 판정 |
|---|---|---|---|---|
| 1020 | 8,318 · 0 · 1,306 · 9,602 · 2,504 · 16,596.5 · 4,284.2 · 74,094.9 | 8,318 · 0 · 1,306 · 9,602 · 2,504 · 16,596.5 · 4,284.2 · 74,094.9 | **8,318 · 0 · 1,306 · 9,602 · 2,504 · 16,596.5 · 4,284.2 · 74,094.9** | ★세 줄 같다 |
| 7 | 7,935 · 0 · 1,275 · 9,892 · 2,151 · 14,638.6 · 4,719.3 · 74,291.4 | 7,935 · 0 · 1,275 · 9,892 · 2,151 · 14,638.6 · 4,719.3 · 74,291.4 | **7,935 · 0 · 1,275 · 9,892 · 2,151 · 14,638.6 · 4,719.3 · 74,291.4** | ★세 줄 같다 |
| 42 | 7,853 · 0 · 1,356 · 9,790 · 2,361 · 15,293.9 · 3,885.9 · 76,117.9 | 7,853 · 0 · 1,356 · 9,790 · 2,361 · 15,293.9 · 3,885.9 · 76,117.9 | **7,853 · 0 · 1,356 · 9,790 · 2,361 · 15,293.9 · 3,885.9 · 76,117.9** | ★세 줄 같다 |

**JSON 전수 비교**(`T176_JSON` 을 재귀로 펼쳐 공통 칸 전부 · 마을별 `per` 51곳 · 곳간 표본 d0·d40·d55·d100·d200·끝 포함):

| 시드 | 칸(가지) | 칸(베이스) | 공통 | **다른 칸** | 한쪽만 |
|---|---|---|---|---|---|
| 1020 | 30,453 | 30,453 | 30,453 | **0** | 0 / 0 |
| 7 | 30,450 | 30,450 | 30,450 | **0** | 0 / 0 |
| 42 | 30,445 | 30,445 | 30,445 | **0** | 0 / 0 |

⇒ **세 시드 모두 넷째 판 = 베이스 = 이 가지.** 30,445~30,453 칸 전부 같은 글자다. 손잡이를 끄면 이 카드는 **한 비트도 안 남는다**.
(`t176-ab` 는 `npcLifeTick` 을 안 지나므로 관측 갈래는 여기서 안 보인다 — 그 끔은 하네스 ⓐ `[실행 · 끔]` 이 본다.)

