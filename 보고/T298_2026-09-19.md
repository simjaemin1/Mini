# T298 — 영토 + 방아쇠를 **켠다**: 서버 기본 켬(자리 둘 · 되돌림 `=0`) · 다섯째 판 = 넷째 판 **비트 동일** · `_mapBeds` 살리기 표

**[승인 대기 · T298]** `server/villages.js` **세 자리**(손잡이 기본값 둘 + 실측문 하나) · 하네스 셋 ·
새 수 0 · 수식 0 · `sim/`·`lab/`·`public/` 무접촉 · T288(`economy-sim-v2`)·T295(전쟁 절) **무접촉** · 실기 0.

**한 줄 답 — 켰다. 그리고 기준선 표는 한 바이트도 안 움직인다(3시드 열 열 전부 `cmp` 동일) —
자가 이 층을 안 타기 때문이고, 그래서 다섯째 판 = 넷째 판이다. 움직이는 것은 실서버뿐이다.**

**★카드의 전제 하나를 정정한다 — *"`villages.js` 에 안 심는다(T221 함정)"*.**
그 문장은 **T244 의 자리**에서 참이고 **여기선 성립하지 않는다.** 이 두 손잡이가 무는 것은 **생활층**
(`_terrGrow`·`_lifeAddHouseSite`)이고, 생활층은 `server/villages.js` **밖에 존재하지 않는다**.
기준선 자(`scripts/t17-metrics.js:84·168`)는 `createWorldV2`+`tickWorldV2` 를 제 손으로 부르고
`Villages.init` 을 **안 부른다** ⇒ 생활층을 한 번도 안 탄다. 랩(브라우저)엔 `villages.js` 가 없고 제
`growTerritory`·인라인 방아쇠를 따로 갖는다 — 그쪽엔 **끄는 문이 아예 없다**(늘 켠 쪽이다).
⇒ **자·랩이 "끈 세계"를 재는 T221 함정이 여기엔 없다.** 증명은 ⓐ 의 `cmp` 세 줄이다: 뒤집어도 자의
표가 한 바이트도 안 움직인다. 그래서 손잡이는 **읽는 자리 그대로**(T230/T219 가 심은 곳) 뒤집었다 —
자리를 옮기면 오히려 "생활층이 없는 곳에서 생활층 손잡이를 읽는" 사본이 하나 생긴다.

---

## ⓐ 손잡이 자리 표 + 되돌림 비트 동일

**자리는 둘이다(하나로 모을 필요가 없다 — 셋 이상이 아니다).** 전 레포 `grep` 전수:

| 손잡이 | 기본값 읽는 자리 | 쓰는 자리(분기) | 자리 수 | 그 밖 |
|---|---|---|---|---|
| `T230_TERR_HOUSING` | `server/villages.js:2430` | `:2435` `_terrGrow` 목표 선택(`_tt.target` ↔ `_tt.econ`) | **1** | 0 |
| `T219_HOUSE_TRIGGER` | `server/villages.js:5108` | `:5747` `_wantSite`(정본 `houseSiteWant` ↔ 종전 `인구 > 침상 × 0.92`) | **1** | 0 |

```js
// :2430
const T230_TERR_HOUSING = process.env.T230_TERR_HOUSING !== '0';
// :5108
const T219_HOUSE_TRIGGER = process.env.T219_HOUSE_TRIGGER !== '0';
```
바뀐 것은 **`=== '1'` → `!== '0'` 두 글자쌍**이다. 수식 0 · 새 수 0 · 새 손잡이 0.
미설정 = **켬** · 끄는 문은 명시 `=0` **하나**.

**신설 하나 — 실측문 `__probe.handles()` (`server/villages.js:6844`).**
하네스가 극성을 **정규식으로 짐작하지 않게** 값을 그대로 내준다. 왜 필요했나: `test-psite-server:178` 이
`/=== '1'/` 를 박고 있었고, 기본을 뒤집는 순간 **하네스만 빨강**이 됐다(그리고 정규식이 낡으면 조용히
초록이 될 수도 있다). env 를 지운 자식 프로세스에서 이 문을 부르면 그것이 **서버 기본**이다(족보 128).

**되돌림 `=0` = 넷째 판 비트 동일 — 3시드 열 열 전부 `cmp`**

| 시드 | `cmp` 켬(미설정) ↔ 끔(`=0` 둘) | 여덟 수(다섯째 판) | 넷째 판 | Δ |
|---|---|---|---|---|
| 1020 | **동일**(전문 8,651B 한 바이트도 안 다르다) | 8,318 · 0/51 · 1,306 · 9,602 · 2,504 · 16,596.5 · 4,284.2 · 74,094.9 | 같음 | **0.00%** |
| 7 | **동일** | 7,935 · 0/51 · 1,275 · 9,892 · 2,151 · 14,638.6 · 4,719.3 · 74,291.4 | 같음 | **0.00%** |
| 42 | **동일** | 7,853 · 0/51 · 1,356 · 9,790 · 2,356 · 15,293.9 · 3,885.9 · 76,117.9 | 같음 | **0.00%** |

밀도 두 열도 같다 — ㉮/㉯: 1.94/2.04 · 2.07/2.18 · 1.96/2.06. `vpop` = **인구있는 마을 51** 세 시드 전부.
소멸 **0/51 · 0/51 · 0/51**.

★이 표는 두 가지를 **동시에** 증명한다: ① 되돌림이 비트 동일하다(카드가 요구한 것) ②**켠 판도**
비트 동일하다 ⇒ 자는 이 층을 안 탄다(위 전제 정정의 증거). 그래서 **다섯째 판의 여덟 수는 넷째 판의
여덟 수다** — 카드 ② 가 기대한 "짝 Δ" 는 이 자에서는 **원리적으로 0** 이고, 켜서 달라지는 것은
실서버(생활층이 실제로 도는 세계)뿐이다. 그 표가 ⓑ 다.

**검증 규약 ①** — `node scripts/lab-modules.js --diff`: 내 diff 4개 중 **교집합 1개**
(`server/villages.js` ★적재됨) ⇒ 규약대로 **3시드 800일 A/B 를 돌렸다**(위 표 = 그 A/B · diff 0·0·0).

## ⓒ 하네스 — "켠 판"을 **끈 판과의 관계**로 말한다(T244 ⑧ 문법 · 상수·식 베끼기 0)

기본이 뒤집히면 **픽스처가 거짓말을 한다.** 세 하네스를 그 문법으로 고쳤다.

**① `scripts/test-psite-server.js` ⑩ — 빨강이던 둘을 관계로 바꿨다**

| 전 | 후 |
|---|---|
| `:178` `/const T219_HOUSE_TRIGGER = process\.env\.\w+ === '1'/` 정규식으로 극성을 읽는다 → **T298 이 뒤집는 순간 빨강** | 삭제. 극성은 **실측문**에 물어본다(주석으로 왜 지웠는지 남겼다) |
| `:203` ⓕ `process.env.T219_HOUSE_TRIGGER !== '1'` — *"이 판은 끔으로 돌았다"* → **거짓이 됐다** | ⓕ 네 갈래로 다시 세웠다(아래) |

```
ⓕ-0 실측문 `__probe.handles()` 가 산다 — 극성을 소스 문자열로 짐작하지 않는다
ⓕ-1 서버 기본 `T219_HOUSE_TRIGGER` = 켬 (미설정 · env 지운 자식 프로세스 실측)
     서버 기본 `T230_TERR_HOUSING`  = 켬 (같은 문으로 · 둘은 같이 켠다)
ⓕ-2 되돌림은 명시 `=0` 둘 — 그 판만 종전 경로(문 하나)
ⓕ-3 켠 규칙과 끈 규칙이 **실제로 갈리는** 픽스처가 있다(인구 20·housing 40·4채) — 없으면 ⑩ 전체가 자명 통과
[자명 통과 금지] ⓕ-1 의 주장을 **끈 판**에 대면 거짓이다
```
ⓐ~ⓔ(데드락 픽스처 · `housing` 무시 방지 · 포화 정지 · 정원 경계 · 동시 상한)는 **손 안 댔다** —
그 절들은 손잡이 극성이 아니라 **규칙 함수의 답**을 재기 때문에 뒤집기와 무관하다.

**② `scripts/test-terr-persist.js` — 제 손으로 켜지 않는다**
T278 판은 `{ T230_TERR_HOUSING: '1' }` 을 **스스로 넣고** 돌았다. 그러면 기본이 다시 끔으로 뒤집혀도
이 하네스는 초록이고, 정작 **제품이 도는 세계**(기본값 세계)는 안 재는 셈이 된다.
⇒ 두 판 다 `{}` 로 돌리고(= 제품 세계), 앞에 ⓞ 를 붙였다:
```
ⓞ 실측문이 산다 · 서버 기본이 켬이다 → 그래서 아래 판은 손잡이를 안 주고 돈다
ⓞ 끄는 문은 명시 `T230_TERR_HOUSING=0` 하나 — [자명 통과 금지] 그 판에 위 주장을 대면 거짓
```

**③ `scripts/lab-wiring-check.js` [H] 등록 — ⓒ 절 신설**
[H] 는 *"랩 부팅 기본 = 서버 기본"* 을 묻는 절이고 두 갈래였다(ⓐ 아직 안 켠 손잡이 6개 · ⓑ T244 로
켜진 셋). 이 둘은 **어느 쪽에도 안 들어간다** — 랩엔 대응 손잡이가 **없다**(생활층이 랩에 없다).
그래서 ⓒ 를 새로 세웠다:

* **ⓒ-1** 서버 기본이 켬이냐 — 소스 정규식이 아니라 **실측문**으로, env 지운 자식 프로세스에서.
  끄는 문이 명시 `=0` 하나임도 같은 문으로. 자명 통과 금지: 끈 판에 ⓒ-1 을 대면 거짓.
* **ⓒ-2** 랩엔 **끄는 문이 없다** — `window.L_TERR*`/`L_HOUSESITE*`/`L_HOUSE_TRIGGER*` 대입이 랩 둘에
  0곳 ⇒ 랩은 늘 켠 쪽 = 서버 기본. (**값**이 같으냐는 [I] 절이 이미 43,344점 전수로 본다 —
  여기서 다시 적지 않는다. 사본 0.)

## ⓑ 켠 세계는 어디서 달라지나 — 실서버 200일 · 50마을 · 같은 계측기(`scripts/t212-beds.js`)

기준선 자는 이 층을 안 타므로(위 ⓐ) **켬의 값은 실서버에서만 잰다.** 팔 셋 — 켬(미설정) 2반복 ·
끔(`=0` 둘) 1반복. 끔 팔의 잡음 대역은 T254 가 이미 3반복으로 쟀다: **인구 ±370(15.0%) · 집 ±2채**.

| | 끔(`=0` 둘) | 켬 ①(기본) | 켬 ②(기본) | Δ 켬−끔 | 잡음 안? |
|---|---|---|---|---|---|
| 인구(50마을 합) | 2,690 | 2,953 | 2,676 | **−14 ~ +263** (0을 물고 있다) | 예 — 대역 15.0% 안 |
| 실체 집(채 = 층합) | 238 | **384** | **364** | **+126 ~ +146채 (+53~61%)** | **아니다 — 대역 ±2채의 63~73배** |
| 침상(= 완공층 × 6) | 1,428 | 2,304 | 2,184 | +756 ~ +876 | 아니다 |
| **침상/인구** | **53%** | **78%** | **82%** | **+25 ~ +29pp** | 아니다 |
| housing/인구 | 94% | 95% | 94% | 0 ~ +1pp | 예(무변) |
| 큰 마을(인구 50↑) 침상/인구 | 33%(20곳) | 66%(20곳) | 67%(19곳) | **2배** | 아니다 |
| `_hcap` 이 작아지는 마을(= `_mapBeds` 못 살리는 마을) | **27/50** | **12/50** | **9/50** | **−15 ~ −18곳** | 아니다 |
| 어긋난 마을(침상≠housing) | 49/50 | 50/50 | 48/50 | — | — |
| 휴면 마을 | 0 | 0 | 0 | 0 | — |
| 집터 탐색 성공 | 138 | 284 | 265 | **+92~106%** | 아니다 |
| 마지막 날 공사중 마을 | 0/50 | 0/50 | 0/50 | 0 | — |

**켬 팔 두 반복의 자기 폭**: 인구 2,676~2,953(±9.8%) · 집 364~384(±20채) · 침상/인구 78~82%.
⇒ 아래 결론은 그 폭보다 **큰 것만** 말한다.

★**인구는 값을 안 치른다.** 켬 두 반복이 끔을 **위아래로 물고 있다**(2,676 < 2,690 < 2,953) —
켬·끔 차가 잡음 대역 안이라는 말의 가장 강한 형태다. 대신 **집이 53~61% 더 선다**(대역의 60배 넘는 폭).

★**T267 의 세 수가 넷째 판 위에서도 선다** — T237(비옥도)·T278(영속)·T287(나루터)가 앉은 main 에서:

| T267 이 말한 것 | 이 판(켬 2반복) | 판정 |
|---|---|---|
| 집 **+130채** | +126 ~ +146채(평균 **+136**) | **선다** — T267 값이 대역 가운데 |
| 침상/인구 **83.1%** | 78% · 82%(평균 **80%**) | **선다** — 3pp 차 · 켬 팔 자기 폭(4pp) 안 |
| 살려도 되는 마을 **15.7/25** | 끔 27곳 막힘 → 켬 9·12곳 ⇒ **15~18곳/27**(평균 16.5) | **선다** |
| 인구 대가 **0** | −14 ~ +263 (0을 문다) | **선다** |

**남은 병목이 바뀌었다 — 이제 땅이 아니라 간격이다.** 빈손 사유(서버가 적은 수 그대로):

| 거부 사유 | 끔 | 켬 ① | 켬 ② |
|---|---|---|---|
| 기존 집과 너무 가까움(<18) | 51,527 (**78.0%**) | 89,246 (**66.2%**) | 76,168 (**65.8%**) |
| 마을 영토 밖 | 4,596 (7.0%) | 38,085 (28.2%) | 32,385 (28.0%) |
| 쉼터와 너무 가까움(<18) | 8,060 (12.2%) | 5,709 (4.2%) | 5,381 (4.6%) |
| 개간 농지 위 | 1,692 (2.6%) | 1,750 (1.3%) | 1,720 (1.5%) |
| 부지 불가(물·바위) | 169 (0.3%) | 45 (0.0%) | 45 (0.0%) |
| 물가 완충 침범 | 35 (0.1%) | 20 (0.1%) | 23 (0.0%) |
| 훑은 셀(분모) | 6,163,108 | 23,125,950 | 19,560,690 |

⇒ 켠 뒤에도 **최대 거부 사유는 `집 간격 18`** 이다(65.8~66.2% · 두 반복이 같은 말을 한다).
영토 밖 비율이 7% → 28% 로 **오른** 것은 영토가 커져 **훑는 셀이 6.2M → 19.6~23.1M(3.2~3.8배)** 로
늘었기 때문이다(분모가 커졌다 · 절대 성공은 138 → 265·284 로 두 배가 됐다).
**다음 병목은 간격 18 하나** — 카드 §4 의 회부(`마을별 장·간격 18` · #22 ⓐ)가 정확히 그 자리다.
집 하나가 닫는 원판은 π·18² ≈ **1,018셀**이고 최밀 육각 몫으로도 (√3/2)·18² ≈ **281셀**이다 —
영토를 더 주는 것으로는 이 상수를 못 이긴다.

## ⓓ `_mapBeds` 살리기 — **표만**(심지 않았다 · 셈만)

**① 어디서 죽었나**

| | 자리 | 수 |
|---|---|---|
| 읽는 자리(정본) | `_hcap = min(housing, _mapBeds)` — `sim/economy-sim.js:3140` → 출생 게이트 `:3141`·출생 루프 가드 `:3156`·직업 고르기(나무꾼) | 1 (사본 3: `sim/economy-engine.browser.js:4356` + 랩 둘 인라인 — 같은 줄) |
| 쓰는 자리(발행) | `lab/마을실험실.html:10033` · `lab/전쟁실험실.html:11758` (`_bf*L_FLOORCAP`) | **2 — 둘 다 랩** |
| 쓰는 자리 — `server/` · node `sim/` | **없다** | **0** |
| 실행시(이 판 · 50마을 × 200일) | `econ_state` 에 `_mapBeds` 가 든 마을 | **0** (T207 재현 · 켬/끔 둘 다) |

⇒ **죽은 자리는 "쓰기"다.** 읽는 쪽은 살아 있고(`min` 이 매일 돌아간다) 서버에서 그 인자가 늘
`undefined` 라 `_hcap = housing` 으로 접힌다. econ 은 **서버가 실제로 지은 집을 못 본다.**
(⚠`grep -r` 은 랩 HTML 을 바이너리로 건너뛴다 — T207 의 "레포 전수 0" 은 그 인공물이었다.
`t212-beds` 는 안 건너뛴다: 대입문 5곳 = 랩 2 + `보고/T212` 문서 3.)

**② 살리면 무엇이 바뀌나 — 켜기가 그 값을 절반 이상 갚았다**

| | 끔(T298 전 판) | **켬(T298 = 지금 기본)** |
|---|---|---|
| `_hcap` 이 작아지는 마을 | **27/50** | **9~12/50** |
| 그 중 인구가 이미 새 상한 이상 ⇒ **즉시 출생 정지** | 27 | 9~11 |
| 지금은 안 막히는데 **새로** 막히는 마을 | 5 | 1~3 |
| 살려도 **안 막히는** 마을 | 23/50 | **38~41/50** |
| 침상/인구 | 53% | **78~82%** |
| 지우면(`_hcap` 에서 `_mapBeds` 를 뺀다) | **무변**(한 수도 안 다르다) | **무변** |

★T212 는 이 표를 **25/50 막힘**으로 적었다. 그때는 손잡이 둘이 다 끔이었다(그리고 T237 전이었다).
T298 의 켬 위에서는 **9~12/50** 이다 — **살리기 비용이 절반 이하로 떨어졌다.** 남은 9~12곳은
전부 **큰 마을**(어촌2 223명 · 임업3 166 · 임업6 153 · 어촌3 142 · 농촌12 126 · 농촌2 112 …)이고,
그 마을들의 빈손 사유는 위 표대로 **간격 18** 이다.

**③ 다음 카드(회부 — 여기서 심지 않는다)**
1. **간격 18 을 마을 규모에 맞게** (#22 ⓐ · 카드 §4 가 이미 회부한 그 자리) — 이걸 풀지 않고 `_mapBeds`
   를 심으면 큰 마을 9~12곳이 **그날 출생 정지**한다. 순서는 **간격 → 살리기**다.
2. 살리는 자리는 **서버 한 줄**이다(`serializeEcon` 이 쓰는 그 econ 객체에 완공층 × `HOUSE_CAP_PER_FLOOR`).
   수는 새로 만들 게 없다 — 랩이 쓰는 식 그대로(`_bf * L_FLOORCAP`)이고 `L_FLOORCAP` 의 정본은
   `village-layout.HOUSE_CAP_PER_FLOOR` 다.
3. 심는 날의 게이트: **침상/인구 ≥ 100% 마을 수** · 즉시 출생 정지 **0곳** · 3시드 800일(자는 안 움직인다 ·
   실서버로 잰다) · 소멸 0.

## ⓔ 한 줄 답

**켰다 — 그리고 기준선 표는 한 바이트도 안 움직인다**(자가 생활층을 안 타므로 다섯째 판 = 넷째 판 ·
3시드 열 열 `cmp` 동일 = 되돌림 비트 동일의 증명과 같은 증명). **실서버에서는 집이 238 → 364~384채(+53~61%)
· 침상/인구 53% → 78~82% · `_mapBeds` 못 살리는 마을 27 → 9~12곳으로 줄고, 인구는 값을 안 치른다
(켬 두 반복이 끔을 위아래로 문다)** — T267 의 네 수가 넷째 판 위에서 그대로 서고, **남은 병목은 간격 18** 이다.

## 검증

| 검사 | 결과 |
|---|---|
| **되돌림 `=0` 비트 동일** | 3시드 800일 전문 `cmp` — **1020·7·42 전부 한 바이트도 안 다르다**(열 열 전부) |
| **다섯째 판 3시드 켬 표** | 8,318 / 7,935 / 7,853 · 소멸 **0/51 · 0/51 · 0/51** · `vpop` 51 · 넷째 판 대비 Δ **0.00%**(같은 이유로) |
| 실서버 팔 셋(200일 · 50마을) | 끔 1 · 켬 2 — 집 238 → 364·384채 · 침상/인구 53% → 78·82% · 휴면 0 |
| `test-terr-persist` | **PASS** — ⓞ 신설(기본 켬 실측 · 손잡이 안 줌) · 50마을 전부 유지 · **영토 밖 집 0/310채** · terr 행 216,120 → 262,020 |
| `test-psite-server` | **80 ✓ / 0 ✗ · PASS** — ⑩ⓕ 전면 개정(실측문 · 관계 문법 · 자명 통과 금지) |
| `test-farm-life` | **33 PASS / 0 FAIL** |
| `lab-wiring-check` | **실패 0 · 경고 0** — **[H]ⓒ 신설**(생활층 손잡이 둘 · 자식 프로세스 실측 · 랩엔 끄는 문 0) |
| `build-econ-bundle --check` | **8 일치 / 0 불일치** |
| `inline-engine --check` | 랩 둘 **최신** |
| 검증 규약 ① | `lab-modules --diff` 교집합 **1개**(`server/villages.js` ★적재됨) ⇒ 3시드 800일 A/B 돌렸다(diff 0·0·0) |
| 검증 규약 ② | 러너 **전수 안 돌렸다**(카드 지시) |
| 무접촉 | **T295**(세션10 `villages.js` 전쟁 절) · **T288**(세션5 `economy-sim-v2`) · `sim/` · `lab/` · `public/` — `git diff` 가 그 파일들을 한 줄도 안 건드린다 |
| 실기 | **0** |
| 접점 | `T230_TERR_HOUSING` · `T219_HOUSE_TRIGGER` · `__probe.handles` · `_terrGrow` · `_terrSet` · `_mapBeds` · `_hcap` · `terr` · `village_buildings` · `houseSiteWant` · `territoryTarget` · `test-terr-persist` · `test-psite-server` · `lab-wiring-check` · `t17-metrics` · `t212-beds` |

**손 댄 파일 4개 / +124 −11**

| 파일 | 무엇 |
|---|---|
| `server/villages.js` | `:2430`·`:5108` 기본값 두 글자쌍 · `:6844` 실측문 `__probe.handles()` (+19 −3) |
| `scripts/test-psite-server.js` | ⑩ 극성 정규식 삭제 · ⓕ 네 갈래 신설 (+33 −5) |
| `scripts/test-terr-persist.js` | ⓞ 신설 · 두 판을 기본값으로 (+28 −3) |
| `scripts/lab-wiring-check.js` | [H]ⓒ 신설 (+44 −0) |

## 회부

* **`_mapBeds` 살리기 — 순서가 정해졌다.** 위 ⓓ③: **간격 18 먼저, 살리기 나중.** 지금 심으면 큰 마을
  9~12곳이 그날 출생 정지한다. (#22 다음 카드)
* **마을별 장·간격 18**(#22 ⓐ · 카드 §4 가 회부한 그 자리) — **이제 급하다.** 켠 뒤 최대 거부 사유가
  그것 하나이고(65.8~66.2%), 영토를 더 주는 것으로는 π·18² ≈ 1,018셀을 못 이긴다.
* **기준선 공통.md `기준선 넷째 판` 절 — PM.** 다섯째 판은 **수가 같으므로** 표를 갱신할 게 없다.
  다만 그 절의 ⚠주석(*"영토·방아쇠 손잡이는 아직 끔 · 다음 카드가 켠다 · 그때 다섯째 판"*)은
  **"T298 이 켰다 · 자는 이 층을 안 타므로 표는 그대로다"** 로 고쳐야 한다(문구만 · 내가 안 건드렸다).
* **`인계/V-마을배치.md:15`** — *"손잡이 `T219_HOUSE_TRIGGER` — 기본 끔"* 이 이제 틀렸다(문서 한 줄 · PM/다음 카드).
* 첫 의뢰 #18 — 그대로.
