# 보고 — T170: `fruitNow` 한 비트 (가을에 밤이 보인다)

* **베이스** `6e236bc7`(origin/main · 2026-09-11) · **코드 커밋** `6a8bd89f` · **헤드** = 이 보고 커밋(= 번들 tip) · 가지 `batch/fruitnow-0911`
* **푸시 — 미푸시(3판)**. 재민 확정 09-12(바깥길 닫힘): 클론·푸시·재시도 예약 **0회**.
  번들 `~/Mini/_inbox/T170_2026-09-11.bundle` (`origin/main..HEAD`) · force 0 · `~/mnt/Mini/durango-mini` 무접촉.
* 접촉 파일 **둘**: `server/zone.js` (+43 −1) · `scripts/e2e-nature.js` (+188 −1). 클라 **0줄**.

---

## §0 실측 (셋)

### ⓐ `fruitSettle` 의 답이 개체에 어떻게 앉나 — **여태 안 앉았다**

| 물음 | 실측 |
|---|---|
| 정산기 | `server/trees.js:104 fruitSettle(store,key,id,day,size)` — 규약 넷(연 1회·겨울 소멸·볼 때 정산·크기 비례) |
| 재고를 쥔 곳 | **둘**: 마을 `vil._fruitStore`(NPC 채집꾼 · `villages.js:4149`) · 존 `_fruitStore`(사람 따기 · `zone.js:6995`) |
| 존에서 그걸 **부르는 자리** | **하나뿐** — `tryPickFruit`(= `fruitTake` → `fruitSettle`). 즉 **아무도 안 물으면 아무도 모른다** |
| 개체 행에 실린 열매 필드 | **없다.** `sp`·`szf`·`seedKey` 는 실리고(T135) `fruitNow` 는 어디에도 없었다 |
| "뒤집히는" 순간 | 정산기 안에서 `e.n` 이 바뀌는 자리는 **셋** — 겨울 소멸 · 결실철 첫 정산(익음) · `fruitTake`(따감) |
| ★날짜 시계 | **갈려 있었다** — `tryPickFruit` 만 `zoneGameDay()`(벽시계 파생)였고 달력·온도·계절은 전부 `gameDayNow()` 다(`zone.js:5899` "날짜 시계는 하나다"). ⇒ **한 낱말 정정**(§1 ③) |

### ⓑ 개체 갱신 방송의 **있는 문법** — 새 유형 0

| 유형 | 어디서 | 클라(`30-n-net.js:1112~`) |
|---|---|---|
| `resources_spawn`(배치) | `activateChunk` — 청크 켤 때 수백 그루 | `set(id, row)` **전체 덮어쓰기** |
| `resource_spawn`(낱개) | 심기(`6984`) · **`_shapePlantedAll`(`1425`)** | `set(id, row)` **전체 덮어쓰기** |
| `resource_update` | 캐는 중 `hp` | `r.hp = msg.hp` — **`hp` 한 칸만 받는다** |
| `resources_removed` / `resource_removed` | 청크 끔 · 캐냄 | `delete` |

⇒ 고를 자리는 `resource_spawn` 이다. `_shapePlantedAll` 이 이미 **같은 뜻으로** 쓴다 —
"모양이 바뀌면 다시 보낸다 — `resource_spawn` 은 클라에서 `set` 이라 덮어쓰기다"(`zone.js:1425` 주석).
`resource_update` 를 쓰면 **클라 한 줄**이 필요했다(`hp` 만 받으니까). 그래서 안 썼다 — 지시서의 "클라 0" 이 성립하는 자리는 이쪽 하나다.

### ⓒ 가을 픽스처 — 하네스가 "가을"을 어떻게 세우나

| 물음 | 실측 |
|---|---|
| 손잡이 | `__e2e_clock {day,night}`(`zone.js:4425` · `E2E_GIVE` 게이트) → `_e2eClock` |
| 무엇이 갈리나 | **`gameDayNow()` 와 `bodyNight()` 뿐**. `zoneGameDay()` 는 **안 갈린다** |
| ⇒ 그래서 | 따기가 `zoneGameDay()` 를 보는 한 **가을 픽스처를 세워도 따기는 여름을 산다**(ⓐ 마지막 줄의 그 갈림) |
| 기다리는 법 | 공통 §2 ⑩ — 정해진 초를 안 잔다. 증인 **둘**: ① 서버가 되돌리는 `🧪 시계 세움 — {"day":…}` 알림 ② 개체 행의 비트 |
| 날짜 고르기 | 손으로 안 적는다 — `Trees.seasonOfDay` 가 그 철의 날들을 말하고 하네스는 그 **가운뎃날**을 쓴다 |

---

## §1 만든 것 — 서버 **세 자리**, 클라 0

```
server/zone.js  _fruitStamp(r)        정산기의 답을 개체 행에 찍는다(값은 `Trees.fruitSettle` — 규약 재기술 0)
                _fruitSeasonSweep()   해+철이 바뀌는 날 한 번 쓸고, **바뀐 행만** 방송
                activateChunk         스폰 방송에 비트가 실린다(= "볼 때 정산"의 그 "볼 때")
                tryPickFruit          ① `zoneGameDay()` → `gameDayNow()` (시계 하나)
                                      ② 딴 직후 비트가 뒤집혔으면 그 행 재방송
                게임 틱 (`Soil.onGameTick` 옆)  `_fruitSeasonSweep()` 한 줄
```

| 약속 | 지켰나 |
|---|---|
| 새 수 0 | ○ — 재고·철·수율은 전부 `server/trees.js` 표에서 온다 |
| 새 규칙(정산기) 0 | ○ — `fruitSettle` 을 **부른다**. 규약 넷을 여기 다시 안 적었다 |
| 새 메시지 유형 0 | ○ — `resource_spawn` / `resources_spawn` (ⓑ 표) |
| 새 타이머 0 | ○ — 있는 30Hz 훅에 `Soil` 과 같은 규약으로 얹었다 |
| 매 틱 방송 0 | ○ — 평시엔 낱말 비교 하나(**0.17µs** · 33ms 예산의 0.0005%) |
| 클라 0줄 | ○ — `public/` 접촉 0 |
| 철을 다시 말하지 않는다 | ○ — `fs` 판정은 서버 종 표가 정본. 비트는 "달렸나" 하나 |
| econ 무접촉 | ○ — 랩 적재 17파일 ∩ 내 diff 2파일 = **∅** |

### ★전제 정정 하나 — 열쇠는 **철 이름**이 아니라 **해+철**이다
1차 실장은 `_fruitSeason` 에 철 이름만 담았다. 실측이 바로 깨뜨렸다: **가을에 따 간 나무가 이듬해 가을에 안 돌아왔다**
(`autumn` → `autumn` 이라 쓸기가 안 돌았다). `fruitSettle` 의 재고는 `yr` 이 바뀔 때도 뒤집힌다(연 1회 규약) ⇒ 열쇠도 `해:철`.

---

## §2 검증 — `e2e-nature` **116 / 0** (97 → 116 · 열매 절 19건 신설)

| 검사 | 실측 |
|---|---|
| ① 세계가 선으로 싣는다 | 밤나무 428 · 소나무 444 · 산뽕 439 — 열매 종 **428그루 전부 boolean** |
| ⓐ 겨울 | 밤 0/428 · 산뽕 0/439 (day 317) |
| ⓑ 봄 | 밤 0/428 (day 410) |
| ⓒ 여름 | **산뽕 439/439**(표 `summer`) · 밤 0/428(표 `autumn`) — 비트가 철을 다시 안 말한다 |
| ⓓ 가을 | **밤 428/428 `fruitNow=true`** (day 590) · 그 순간 개체 방송 **1건** |
| ⓔ 소나무 | 비트가 **아예 없다**(444그루 `undefined`) |
| ⓕ 매 틱 0 | 철 고정 6초 동안 개체 방송 **0건** ↔ 뒤집힐 땐 1건(대조군) |
| ⓖ 따감 | 7.1px 에서 실제로 땀(「밤에서 chestnut 4」) · 그 행만 true→false · **hp 3→3** · 428→**427**그루 |
| ⓗ 이듬해 | day 955 가을에 **428/428 다시** |
| ⓙ 화면 | 세계가 실은 그 비트 → `__treeSpriteFor` → **`tree10_a`**(표의 `autumn`) · false 면 `tree10`(표의 `sprites`) |
| ⓚ 소나무 그림 | 비트가 와도 `tree02` 그대로 |

* **자명 통과 금지** — 같은 나무·같은 판정이 겨울·봄·여름엔 false, 가을엔 true 였다(ⓘ). 방송 수도 대조군(1건)을 같이 잰다.
* **소스를 안 읽는다** — 실클라가 쓰는 **그 웹소켓**으로 붙어 서버가 실제로 보낸 행을 읽는다. 자리·날짜·판 이름은 전부 정본이 고른다.
* 하네스 자체 결함 하나 실측으로 잡음: 30초 입력이 없으면 존이 좀비로 보고 선을 끊는다(`STALE_WS_MS`) ⇒ 숨쉬기 한 줄.

### 접점 하네스
| 하네스 | 결과 |
|---|---|
| `test-trees` | **81 / 0** |
| `test-lab-trees` | **44 / 0** |
| `test-client-globals` | **26 / 0** (클라 무접촉 — 기준선 그대로) |
| `test-harness-lint` | 21 / **1** — `e2e-friends.js:185` 이모지. **내 것이 아니다**(상류 T147 · 세션7 T174 몫) |
| `node --check` | `server/zone.js` · `scripts/e2e-nature.js` 통과 |

### 3시드 면제 근거 — 랩 적재 추적
```
econ 랩이 무는 저장소 파일 17개 : econ-lab-real, chunk, era, events, fertility, livelihood, specialty,
                                  sustain, terrain-gen, terrain, trade, village-layout, villages,
                                  winter, zone-config, economy-sim-v2, economy-sim
내 diff 2개                      : server/zone.js · scripts/e2e-nature.js
교집합                            : ∅  → 3시드 800일 A/B 를 갈음한다(공통.md §2 ①)
```

### 대조 스크린샷 (재민 ④)
`산그림/디자인B/나무_종별_가을.png` — HUD **「2년 가을 46일」** · 밤나무 428/428 열매판(`tree10_a`) · 소나무는 그대로.

---

## §3 지나가는 길에 고친 것 하나
`e2e-nature` T148-B ⓗ 가 시더를 `512` 로 불렀다 — 존은 `CHUNK_SIZE = 1024` 다. 종만 세던 절이라 답은 안 틀렸지만
**정본에서 오지 않은 수**라 `CH.CHUNK_SIZE` 로 바꿨다(이번 절이 같은 시더로 **자리**를 고르기 때문에 이 수가 틀리면 자리가 틀린다 — 실측으로 밟았다: 광장에서 113px 떨어진 나무를 골라 따기가 거리로 거절당했다).

## §4 회부 (고치지 않았다)
1. **심은 나무는 종이 없다** — `_liveResourceRow` 는 `r.species` 를 쓰고 시더는 `r.sp` 를 쓴다. 그래서 심은 나무는 종별 그림도, 열매 비트도 못 받는다(T148-B 부터 있던 구멍). → **R2/W 영역**
2. **이름표 둘이 어긋난다** — `trees.js koOf('chestnut')='밤'` ↔ `tree_species.json ko='밤나무'`, 그리고 `ITEM_LABEL_SERVER['chestnut']` 이 없어 알림이 「밤에서 chestnut 4」로 난다. → **세션8 T175 표 정리**에 붙임
3. `tree14_a`(가을 잎 오디)가 `mulberry.sprites` 에 들어 있다 — 성목판이 아니라 철판이다. → **세션8 T175**
4. `e2e-friends.js:185` 이모지 — **세션7 T174**

## §5 실기 1줄 (재민)
가을에 밤나무·참나무에 열매가 보이고 소나무엔 없나 — 그리고 하나 따면 **그 나무만** 열매가 사라지나.
