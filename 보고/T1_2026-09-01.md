# 보고 — T1: 일틱 조각내기 (RTT 스파이크 제거)

**[재민 확정 2026-09-01 · 웨이브 0]** · 커밋 `476eda7d` · 영역 Z(존 서버)

> 규칙 11(배치마다 보고 파일) 이 생기기 전에 끝난 배치라, **이미 쓴 것을 옮긴 것**이다.
> 새 서술은 없다 — 커밋 본문 전문 + 회부 링크 + 재민 판정 결과.

## 문서

* 회부: [`회부_T1_일틱쪼개기_다음층.md`](../회부_T1_일틱쪼개기_다음층.md) — §A 남은 바닥 · §A-4 발산 사건 · §A-5 남은 차이 · §B 판단거리 · §D 실측표
* 인계: [`인계/Z-존서버.md`](../인계/Z-존서버.md) `## T1.` 절 · [`인계/회부.md`](../인계/회부.md) `## T1.` 절
* 하네스: `scripts/test-tick-slicer.js`(21/0) · `scripts/e2e-rtt.js`(5/0 · 목표 미달 명시)

---

## 재민 판정 (2026-09-01 · T41 카드와 함께 내려옴)

| 회부 항목 | 판정 |
|---|---|
| **§A-5** 남은 차이 0.1%/일 | **"같다"로 본다.** 조각난 하루가 이제 **정본**이고, NPC 노동이 단계 사이에 끼어드는 건 세계가 살아 있다는 뜻이지 결함이 아니다. **마감 동안 생활층을 멈추지 않는다.** |
| **§B-1** 목표선을 pass/fail 에서 뺀 것 | **승인.** 단 후속 카드(T41)가 조각을 줄인 뒤 `e2e-rtt` 의 `≤×2` 를 pass/fail 에 **다시 넣는다.** |
| **§B-2** 마감 창(2.4~3.0초)·알림 | **승인** — 그대로 둔다 |
| **§B-3** 조각 예산 16ms | **승인** — 그대로 둔다 |
| **§B-4** 마감이 하루보다 길면 날이 밀린다 | 하네스 전용 사실이라 **기록만** |
| **§A-1~A-3** 남은 바닥 셋 | 후속 카드 **T41** 로 — `_lifeAddHouseSite` · 캐러밴 콜드 A* · `_oreMineDaily` 꼬리 |

---

## 커밋 본문 (전문)

```
일틱 조각내기: 범인은 econ 이 아니라 생활층이었다 [재민 확정 2026-09-01 · T1]

★전제 정정 둘 (실측이 지시서를 뒤집었다)
  ① 일틱 구동부는 `server/zone.js` 가 아니다 — zone.js:8117 은 한 줄 래퍼고
     하루 블록은 `server/villages.js onGameTick()` 안에 있다.
  ② 범인은 econ 이 아니다. 마을 50곳·econ day 515·100일 중앙값:
       일틱 총 2,018ms · **생활층 1,620ms(80%)** · terr 177ms · **econ 99ms(5%)**
       그 중 `_lifeAddHouseSite` 하나가 1,127ms(56%) — 자리를 못 찾은 마을이
       매일 영토 전수를 2패스로 다시 훑는다.
  ⇒ econ 은 **한 줄도 안 건드렸다.** 지시 §2-③이 열어 둔 좁은 예외(함수 추출)조차
     쓸 필요가 없었다(생활층 조각이 227ms 라 econ 99ms 를 쪼개도 최대 조각이 안 내려간다).

무엇을 했나
  · **슬라이서** — 하루 경계에서 그날 할 일을 조각 목록으로 세우고(`_openDayJobs`)
    프레임마다 예산(`VILLAGE_TICK_SLICE_MS`, 기본 16ms)만큼 꺼내 돈다(`_drainTickJobs`).
    조각 경계는 **단계 경계 또는 마을 경계**뿐. econ·캐러밴 쓸기·방송은 원자.
  · `syncCaravanBodies` 를 `_caravanSyncOne`(한 대)+`_caravanSyncSweep`(쓸기)로 갈랐다
    (겉함수는 그대로 둘을 순서대로 부른다 — 기존 호출부 무변).
  · **"장부 마감 중" 큐** — 마감 창(2.4~3.0초)에 들어온 마을 요청만 모았다 같은 순서로 흘린다.
    버리지 않는다. 낚시·이동·전투·제작은 안 걸린다.
  · **마감 중 실체 30Hz 정지** + 하루 안 벽시계 파생 동결(`_dayNow`·광맥 재생 적분).
    이게 없으면 쪼갠 것만으로 세계가 갈린다(아래).
  · `/perf` — 단계별·조각별·이벤트 루프 지연(`monitorEventLoopDelay`·`?reset=1`).
    `econ_day` 는 이제 마감 시점의 하루 총합(+wall), 한 프레임 몫은 `econ_frame` 신설.

★★쪼갠 게 세계를 갈랐다 — 실측으로 잡아 막았다
  ① 첫 판 base↔head 50곳 중 49곳 불일치 → **가짜였다**(저장 1마을/틱 탓에 서로 다른 날을 비교).
     ⇒ 같은 마을·같은 날끼리만 대조 + 끄기 전 저장 큐 배수.
  ② 고친 뒤 A/A 는 50/50 인데 A/B 만 21곳 → **진짜였다**(마감 중 캐러밴·전투 실체가 계속 걸어
     뒤 단계가 '더 간 몸'을 봤다). ⇒ 실체 30Hz 정지. 그 뒤 A/B 0.
  ③ 남은 차이 — 6일 누적 식량 0.62~0.76%(잡음 바닥 0.00~0.05%의 열 배). 원인은 NPC 가
     매 프레임 일하고 그 손이 곳간을 만진다는 것. 없애려면 마감 동안 생활층까지 멈춰야 하고
     그건 행동 변경이라 안 했다 — **회부 §A-5 재민 판정**.

실측 (같은 DB 스냅샷 짝 비교)
  대조군(끈 뽑음)  총 2,996ms · **프레임 1** · 한 프레임 최대 **2,996ms**
  조각내기(16ms)   총 3,053ms · **프레임 30** · 한 프레임 최대 **447ms**
  이벤트 루프 최대 막힘 13,631ms → 1,334ms · RTT 마감 창 p95 11,021ms → **766ms**(×14.4)
  하루 총 일감 ×1.02 · 단계 순서 양쪽 동일 · 인구 차 0.10% · 식량 차 0.76%
  ★판정은 p99 가 아니라 **최댓값**으로 한다 — 두 팔은 막힘 횟수가 달라 p99 가 낚인다
    (대조군 p99 24ms 인 판의 최대는 12,994ms 였다).

⚠**목표 미달** — 재민 확정 ≤×2 에 못 닿았다(실기 환산 ×19.9). 바닥은 **조각 하나**다:
  마을 한 곳의 생활층 227ms · 캐러밴 한 대의 콜드 A* 434ms. 슬라이서는 조각보다 잘게 못 자른다.
  그 안의 수술은 회부 §A(★`_lifeAddHouseSite` 가 1순위 — 일틱은 세계가 늙을수록 자란다:
  day 100 1,285ms → day 515 2,018ms). `e2e-rtt` 는 목표선을 수치로 찍되 pass/fail 에 안 넣었다
  (분모가 로컬 평시 RTT 라 환경이 비율을 정한다 — 그 판단의 근거·되돌리는 법은 회부 §B-1).

하네스
  · `scripts/test-tick-slicer.js` **21/0** — 구조·순서 불변·마감 완주·루프 최대 막힘·경제 크기.
    ①이 상황 선행 assert(대조군이 정말 한 프레임에 모는지 먼저 확인 — 자명 통과 방지선).
  · `scripts/e2e-rtt.js` **4/0(목표 미달 명시)** — 진짜 WS 짝 비교 + 양보 끈 대조군.

검증
  · 러너 51개 — 통과 46 · 실패 5, 전부 재실행/A-B 로 무관 확인:
      test-guest-rejoin·e2e-charsprite(마을 시뮬 OFF · 재실행 통과) ·
      e2e-weight·e2e-zoom(단독 실행 통과 — 컨테이너 부하 흔들림) ·
      e2e-mtfoot ⑤(0.34/0.45/72% — 회부_추위2 B-8 에 적힌 그 값 그대로, 지형 문제)
  · 행동 바꾼 뒤 마을 접점 11종 재실행 전원 통과
    (test-events/trade/ledger/body · e2e-events/trade/village/cold/forage-village/metallurgy/waterperf)
  · **econ 무수정** — `sim/` diff 0 · 번들 8/8 · v2 CLI 5시드 2,616·651·13,473 ·
    실지도 3시드 800일 **6,280/5,993/6,072 · 소멸 0/51** (전부 기준선과 동일)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JuHsbfxFXZmmCyzvZJZ4At

```
