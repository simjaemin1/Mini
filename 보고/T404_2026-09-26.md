# 보고 — T404 자산·문서·확인 (세션11 · Sonnet · 2026-09-26)

**한 줄 답:** 보고 머리 규약을 템플릿 한 장으로 세웠다 — `지시/_보고머리_템플릿.md`(네 칸 빈칸꼴 + T364 합격 예시 + T381 불합격 예시 · 새 점검 자 0) · `인계/공통.md` 안 T313 문장 옆에 포인터 한 줄(새 규칙 0) · 배정표 `지시/세션배정_2026-09-26.md` 신설 · **이 보고 자체가 템플릿 첫 합격 사례**다.

가지 `batch/report-template-0926` = **`118f1d14`**(1커밋 · **새 가지**) [승인 대기 · T404] · 베이스 `c445983c`(main) ·
**force 0** · `.lock*` **0** · 실기 0 · main 은 PM — 안 밀었다 · `test-harness-lint` **61/61**(무변 확인).

판정 0 · 새 코드 0 · 정본(`sim/`·`server/`·`public/client`) 무접촉 · 아래는 표와 파일만.

---

## ① `지시/_보고머리_템플릿.md`

* 네 칸(ⓐ 한 줄 답 ⓑ 가지 해시 ⓒ `[승인 대기 · T◯◯◯]` ⓓ 베이스 해시)을 빈칸 꼴로 적었다.
* **점검 자는 새로 안 만들었다** — T366·T389 가 실제로 쓴 방법(`head -n 15` · 낱말 근접 · `git cat-file -e`/`git merge-base --is-ancestor`)을 글로 옮겼을 뿐이다.
* **합격 예시**: `보고/T364_2026-09-23.md` 머리 15줄을 한 글자도 안 고치고 인용했다(그 파일 자체는 무접촉).
* **불합격 예시**: T389 표에서 위반 칸이 가장 많았던(4/4) 장 중 하나(`보고/T381_2026-09-25.md`)의 머리를 인용하고, 어느 칸이 왜 빠졌는지만 적었다 — **세션 이름은 안 적었다**(카드 지시대로).

## ② `인계/공통.md` §5 — ⚠자리 정정 회부

카드는 "§5 보고 절"이라 적었지만, **실제 파일엔 `## 5.` 절이 없다**(`## 0.`→`## 1.`→`## 2.`→`## 3.`→`## 6.` 로 번호가 4·5를 건너뛴다 — `grep -n "^## [0-9]" 인계/공통.md` 로 확인). "보고 머리 15줄" 문장은 실제로 `## 6. 완료 기준` 절, T313 이 적은 그 줄(609행) 바로 옆에 있다. **그 실제 자리에 포인터 한 줄을 붙였다**(문장은 안 늘렸다 · 새 규칙 0) — §5 라는 이름은 카드 작성 시점의 착오로 보여 회부(§4)에 적었다.

## ③ 배정표 09-26

* `지시/세션배정_2026-09-26.md` 신설 — 카드 본문의 PM 정본 표(세션 1~13)를 그대로 옮기고 각 카드 H1 한 줄 요약 첨부.
* `~/Mini/태스크보드_v1.md`(정본 §5)는 이 기기에도 없다(T313·T366·T389 때와 같은 결손).
* `지시/README.md` 에 날짜 한 줄 추가.

## ④ 한 줄 답 (재게)

**템플릿 한 장 · 공통 문장 옆 포인터 한 줄(§5 이름은 착오 — 실제는 §6) · 배정표 09-26.**

---

## 회부만(PM)

* 카드가 "공통.md §5"라 적었는데 그 절은 존재하지 않는다(`## 4.`·`## 5.` 둘 다 없음) — 앞으로 카드에서 §번호를 쓸 때 `grep -n "^## [0-9]" 인계/공통.md` 로 먼저 확인하는 관행을 둘지 PM 판단.
* 템플릿의 "불합격 예시"가 T381 하나뿐이다 — T369·T377·`BGM_RND_착지메모`도 4/4 위반이었으나 그건 PM 메모/GPT 세션 산출물이라 세션 작성 관행 예시로는 T381 이 더 대표적이라 골랐다(다른 후보로 바꿀지는 PM).

## 검증 규약

* 코드 0 — `git diff --stat origin/main`: `지시/_보고머리_템플릿.md`(신규) · `인계/공통.md`(1줄) · `지시/README.md`(1줄) · `지시/세션배정_2026-09-26.md`(신규) · `보고/T404_2026-09-26.md`(신규) 다섯뿐.
* `test-harness-lint` 61/61(무변 확인) — 새 파일 = 템플릿 + 배정표뿐, 하네스 무접촉이라 당연하지만 카드가 요구해 재확인했다.
* 읽기: `head`·`grep`·`sed -n` 만 사용. 정본 파일 수정 0.
