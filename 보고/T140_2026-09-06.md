# 보고 — T140: 픽스처 시계가 앉는다 · `feed` 는 한 뜻 (2026-09-06)

> 한 줄: **얼리는 손잡이는 멀쩡했다. 틀린 것은 기다리는 방식이었다.**
> 새 손잡이 0 · 서버 무접촉 · 게임 행동 변경 0.

---

## 0. §0 실측 (셋) — 지시서의 전제 하나를 고쳤다

### ⓐ 0.5634 는 **무엇의 값**인가 — 역산이 답했다

| 물음 | 답 |
|---|---|
| ①이 읽는 수 | `window.__wx().cold` = 서버 `weatherFor(p)` 의 `cold` = **`weather.outdoorCold(day, night, elev)`** |
| 그 수를 정하는 입력 | `(day, night, elevKm)` **셋뿐** — 순수 함수다(캐시 한 칸 · 벽시계 없음) |
| 벽시계가 새는 자리 | `gameDayNow()` → econ `world.day`(랩 정지 시 **0**) · `bodyNight(now)` → `isNight(now)` **← 여기** |
| 0.5634 를 내는 (day,night) | ★**정확히 `(0, 낮)`** — 전 366일을 훑어 찾았다 |
| 0.8647 를 내는 (day,night) | ★**정확히 `(0, 밤)`** |
| 얼렸을 때 기대값 | 여름낮(day 133) **cold 0 · 29.2℃** · 겨울밤(day 315) **cold 0.9278 · −2.6℃** |
| 두 하네스가 상황을 세우는 코드 | **사본이었다** — 각자 `__e2e_clock` 을 보내고 각자 `sleep()` 했다 |

⇒ ★**두 관측은 "얼기 전의 세계" 하나였다.** day 0(정지한 econ 날) + 벽시계 밤낮 — 부하가 밤낮을
갈랐을 뿐이라 0.5634 와 0.8647 로 보였다. **날씨 모델도 손잡이도 결백하다.**

### ⓑ 얼리는 문법 전수 — ★**새 손잡이 0**

| 손잡이 | 어디 | 범위 | 이 카드가 쓰나 |
|---|---|---|---|
| `__e2e_clock {day, night}` | `zone.js:4145` (`E2E_GIVE` 게이트) | 몸·날씨가 보는 날짜/밤 (`gameDayNow`·`bodyNight`) | ★**그대로 쓴다** |
| `__e2e_day_freeze {on}` | `zone.js:4154` | 존 게임일 + 마을 시뮬 날 동결 | 안 쓴다(날을 **고정**하면 되지 얼릴 필요 없다) |
| `TIDE_FREEZE_MS` | `tidal.js:50` (env) | 물때 | 무관 |
| `WEATHER_SEED` | `weather.js:36` (env) | 편차 잡음 씨앗 | 무관(이미 전 존 고정) |
| `__rainForce` | 클라 훅 | 강수 렌더 | 무관 |

⇒ 계절/시각을 얼리는 문법은 **이미 있었다**(`__e2e_clock`). 그래서 **서버 한 줄도 안 고쳤다** ·
새 env 0 · `body.js`·`weather.js`·`zone.js` **무접촉**.

★실증(§0 프로브 · 실서버 한 판): 보내기 전 `cold 0.5634` → 보낸 뒤 **`cold 0`** ·
서버 회신 `🧪 시계 세움 — {"day":133,"night":false}`. **손잡이는 정확히 작동한다.**

### ⓒ `feed` 접점 표 — 두 뜻

| | 뜻 A · **짐승 길들이기** | 뜻 B · **쓰러진 사람 먹이기** |
|---|---|---|
| 버튼 | `index.html:200` `data-action="feed"` | — |
| 클라 분기 | `99-main.js:396` `a === 'feed'` | `46-h-verbs.js:144` `{type:'verb', name:'feed'}` |
| **선에 나가는 이름** | ★`{ type: 'feed' }` — **메시지 타입 자체** | `verb` 봉투 안의 `name` |
| 서버 | `zone.js:4284` → `tryFeed(player)` | `rescue.js:308` → `Rescue.feed(...)`(`/먹이기` 와 같은 정본) |
| 하네스 | `e2e-ui ⑧` 2줄 · `test-tame`(함수로 부른다) | — |

⇒ **양쪽 다 서버 프로토콜에 이름이 걸려 있다.** 지시서 규칙("프로토콜 이름이 걸린 쪽은 안 건드린다")을
지키면서 두 뜻을 가르는 자리는 **하나뿐**이다 — 브라우저 밖으로 나가지 않는 **버튼의 낱말**.
⇒ `data-action="feed"` → **`tame_feed`**. **선에 나가는 이름은 그대로 `{ type: 'feed' }`** 다.

---

## 1. 한 일 — 셋

```
① 픽스처 정본  scripts/fixture-clock.js  (신설 · 하네스가 공유하는 헬퍼 · `client-src.js` 선례)
                 anchorDays()   여름 골·겨울 봉우리를 econ 곡선에서(하네스가 날짜를 안 고른다)
                 expectAt(d,n)  그 시계의 **기대 cold·tempC**(순수 함수 — 부하와 무관)
                 setClock(page) 보내고 **세계가 그렇게 말할 때까지 기다린다**(증인 둘)
                 waitVillages() 마을이 심길 때까지 · waitInWorld() 입장할 때까지
② 두 하네스가 그걸 쓴다  e2e-thirst(①·⑥) · e2e-cold(①·⑧ⓑ·⑧ⓐ) — `sleep()` 다섯 자리가 사라졌다
③ 낱말 하나  index.html · 99-main.js · e2e-ui  (`data-action`: `feed` → `tame_feed`)
```

### ①이 고친 것 — **시간이 아니라 판정**

```
  옛:  __e2e_clock 보낸다 → sleep(2000) → 읽는다        ← 2초 안에 안 오면 얼기 전 값을 읽는다
  새:  __e2e_clock 보낸다 → **세계가 그 시계를 말할 때까지** 읽는다(3초마다 다시 보낸다)
```

★**증인이 둘인 이유(실측이 가르쳤다).** 처음엔 `tempC` 하나로 잡았더니 **`e2e-cold` 가 ①만 통과하고
그 뒤 17건이 `shelter undefined` 로 죽었다.** 판 중간에 클라가 다시 붙으면 **새 `welcome` 이 얼린
날씨를 실어 오기 때문**이다 — 얼린 것과 **받고 있는 것**은 다른 물음이었다.
⇒ 증인 ② = `shelter` 가 **있다**(그 칸은 초당 `gauges`(`weatherFor`)에만 있고 `welcome` 에는 없다).

### 왜 2초로는 모자랐나 — 실측한 수

이 상자에서 시계를 세운 뒤 **첫 `gauges` 가 오기까지 17.5 ~ 21.4초**였다(아래 표). `sleep(2000)` 은
그 창을 **한 번도** 못 덮는다. 그래서 T131 이 본 여섯 빨강은 "가끔"이 아니라 **거의 항상**이었다.

### 같은 병의 두 얼굴 — 같이 고쳤다(픽스처 정본 안에서)

| 증상 | 옛 코드 | 실측 | 고침 |
|---|---|---|---|
| `마을이 시딩됐다 (0곳)` → 하네스 통째로 중단 | 존 뜨자마자 DB 읽기 | 같은 커밋이 한 판 50곳 · 다음 판 **0곳** | `waitVillages()` |
| `존 입장` 빨강 | `for(i<60) sleep(500)` = **30초** | 부하 판에서 **24,758ms** 걸렸다(30초 턱걸이) | `waitInWorld()` |

★판정은 **하나도 안 바꿨다** — "끝내 못 들어오면 빨강 · 마을 0곳이면 빨강"은 그대로다.
바꾼 것은 **얼마나 기다리는가**뿐이다.

---

## 2. 게임 행동 변경 0

| 근거 | 값 |
|---|---|
| `server/` diff | **0줄** |
| `body.js`·`weather.js`·`zone.js` | **무접촉** |
| 새 env·새 손잡이 | **0** |
| 클라 diff | `index.html` 1낱말 · `99-main.js` 1낱말(+주석) — **선에 나가는 메시지 무변** |
| T135·T124·T139 자리 | **무접촉** |

## 3. 검증

### ⓐ 목표 — 15/6 · 49/3 → **n/0**

| 하네스 | T131 이 본 것 | 지금 |
|---|---|---|
| `e2e-thirst` | **15 / 6** | ★**23 / 0** (단독) · 22/1 (부하 · §3-ⓒ) |
| `e2e-cold` | **49 / 3** | ★**55 / 0** (단독) · ★**55 / 0** (부하) |

### ⓑ ★★여섯 번의 첫 상황 수 — **같다**

부하 = 코어 하나를 100% 로 채운 자식 프로세스(이 상자는 2코어다 — T131 이 실제로 겪은 그 부하).
★(전) 표시는 끝까지 돌린 판 · 나머지는 **①까지만 돌리고 멈춘 판**(필요한 것이 그 수뿐이라서).

| 판 | 부하 | `e2e-thirst ①` (day 133 · 기대 29.2℃ / cold 0) | `e2e-cold ①` (day 315 · 기대 −2.6℃ / cold 0.9278) |
|---|---|---|---|
| 1 | 0 | **29.2℃ · 추위 0** (17,639ms) ★(전) 23/0 | **−2.6℃ · 0.9278** (17,565ms) ★(전) 55/0 |
| 2 | 0 | **29.2℃ · 추위 0** (19,785ms) | **−2.6℃ · 0.9278** (18,653ms) |
| 3 | 0 | **29.2℃ · 추위 0** (18,308ms) | **−2.6℃ · 0.9278** (18,574ms) |
| 4 | 1코어 100% | **29.2℃ · 추위 0** (20,993ms) ★(전) 22/1 | **−2.6℃ · 0.9278** (2,842ms) ★(전) 55/0 |
| 5 | 1코어 100% | **29.2℃ · 추위 0** (20,969ms) | **−2.6℃ · 0.9278** (23,571ms) |
| 6 | 1코어 100% | **29.2℃ · 추위 0** (21,236ms) | **−2.6℃ · 0.9278** (26,789ms) |

⇒ ★**여섯 번 모두 같은 수다.** 그리고 그 수는 손으로 고른 문턱이 아니라
`weather.outdoorCold(day, night, 0)` 가 내는 **계산된 기대값**이다(하네스가 그 함수와 대조한다).
⇒ T135 §3-b 가 적은 흔들림(`e2e-cold` 1/52 가 판마다 다른 항목)은 **사라졌다**.

★기다린 시간은 2.8초 ~ 26.8초로 **열 배 가까이 흔들린다** — 그게 바로 `sleep(2000)` 이
못 덮던 창이고, 지금은 그 흔들림이 **판정에 닿지 않는다**.

### ⓒ 부하 판의 빨강 하나 — 정직 보고

`e2e-thirst` 부하 판 22/1. 빨간 하나는
`★★③ 외삽하면 실시간 36분에 HP 100 이 빈다 — 외삽 50.7분 vs 표 36.0분` 이다.
이것은 **시계 픽스처가 아니라 속도**다: 코어가 눌리면 존 틱 자체가 느려져 벽시계 초당 감소가 준다.
세계가 실제로 느리게 돈 것이라 픽스처로 덮을 것이 아니다 — **회부 1**로 올린다.
(단독 판에서는 초록 · 지시서가 요구한 "같은 수"는 ① 이고 그건 여섯 번 같다.)

### ⓓ ★자명 통과 금지 — **돌연변이가 잡는다**

되돌림 손잡이를 켠 **자식 프로세스 + env** 로 한 판:

```
  T140_FIXTURE_WAIT=0 node scripts/e2e-thirst.js      ← 옛 방식(정해진 초를 자고 읽는다)
  ✗ ★★① (픽스처) 시계가 실제로 얼었다   day 133 · 기대 29.2℃ · **실측 12.6℃** · 2657ms
  ✗ ★① (상황) 한여름 낮이다            **추위 0.5634 · 추움**
```

⇒ ★**T131 이 보고한 바로 그 수(0.5634)가 되살아난다.** 이 검사는 실패할 수 있고,
실패하는 이유가 정확히 그 옛 결함이다.

### ⓔ 접점 하네스 전수

| 하네스 | 결과 | 왜 접점인가 |
|---|---|---|
| `scripts/e2e-thirst.js` | ★**23 / 0** | 이 카드 본체 |
| `scripts/e2e-cold.js` | ★**55 / 0** | 이 카드 본체 |
| `scripts/e2e-ui.js` | ★**125 / 0** | `feed` → `tame_feed` 접점(⑧이 버튼과 선을 동시에 못 박는다) |
| `scripts/test-client-globals.js` | **26 / 0** | ⑦ 이 `index.html` 의 `data-action` 을 센다 |
| `scripts/test-harness-lint.js` | **19 / 0** | 하네스 파일을 고쳤다 |
| `scripts/e2e-verbs.js` | 72 / 5 | 우클릭 동사 이름 — ★**귀속 = 내 것 아님**(아래) |
| `node --check` × 5 | 통과 | fixture-clock · e2e-thirst · e2e-cold · e2e-ui · 99-main |

**귀속(`e2e-verbs`).** 순정 베이스 클론(5b45c862)에서 같은 하네스를 돌렸더니
**②에서 다섯이 빨갛고 그 뒤 `TypeError: … 'findIndex' of null` 로 통째로 죽었다**(회부 0-터 7 ⓑ 그대로).
우리 판은 끝까지 돌아 72/5 였고 빨간 자리도 **다른 절**(⑬ NPC 지목 — T126 이 회부한 그 간헐)이다.
⇒ 이 하네스는 이 상자에서 판마다 다른 곳이 빨갛다. **T140 diff 는 우클릭 동사에 한 줄도 안 닿는다.**

### ⓕ econ 랩 적재 추적 — 교집합 **∅**

```
내 diff 6개(.js/.html): 99-main.js · index.html · e2e-cold.js · e2e-thirst.js · e2e-ui.js · fixture-clock.js
랩 적재 17개: server/{calendar,chunk,crops,events,fertility,livelihood,specialty,sustain,
              terrain-gen,terrain,trade,village-layout,villages,winter,zone-config}.js · sim/*.js
=== 교집합 0개 ===  ∅
```

### ⓖ 안 돌린 것 · 위생

통합 러너 전수는 **안 돌렸다**(검증 규약 ②). `kill <pid>` 만 썼다 — **`pkill -f` 0회**.
판 사이에 `/tmp/e2e-*.db*` 를 지우고 `zone.js`·`central.js`·chromium 자식을 pid 로 걷었다.

## 4. 회부

1. ★**[영역 B · 하네스] 속도를 재는 절은 부하에 눌린다.** `e2e-thirst ③` 의 외삽(36분 표)이
   코어가 눌린 판에서 50.7분으로 나온다. 세계가 실제로 느리게 돈 것이라 픽스처로 덮을 수 없다.
   길 둘: ⓐ **게임분으로 재고 벽시계로 안 잰다**(서버가 이미 게임일을 안다) ·
   ⓑ 표와의 대조를 **비율 구간**으로 넓힌다(그건 검사를 무르게 만든다).
   ⓐ 가 옳아 보이지만 그건 그 하네스 소유의 판단이다.
2. ★**[영역 클라 · 낱말 — 남은 절반] 선에 나가는 `feed` 는 여전히 두 뜻이다.**
   이 카드는 지시서대로 **프로토콜을 안 건드렸다**: `{ type: 'feed' }`(짐승)와
   `{ type: 'verb', name: 'feed' }`(사람)가 둘 다 남아 있다. 봉투가 달라 서버는 안 헷갈리지만
   **읽는 사람은 헷갈린다**. 프로토콜을 가는 카드가 따로 있어야 한다(폴백 0 · 한 번에).
3. **[PM · 공통.md 한 줄 제안 — 하네스 픽스처 규약]**
   > **하네스는 정해진 초를 자지 않는다 — 세계가 그렇게 말할 때까지 기다린다.**
   > 픽스처를 세운 뒤의 `sleep(n)` 은 전부 결함이다: 그 n 은 부하가 없을 때만 맞는 수이고,
   > 부하가 있으면 하네스는 **픽스처 이전의 값**을 읽는다(T131 이 그렇게 여섯 번 빨갰다).
   > 기다림의 증인은 **그 픽스처에서만 나오는 값**이어야 한다(같은 값을 내는 다른 상태가 있으면 안 된다).
   > 공유 헬퍼는 `scripts/fixture-*.js` 에 두고 하네스마다 다시 적지 않는다(사본 금지).
4. **[영역 하네스] `e2e-verbs` 는 이 상자에서 판마다 다른 곳이 빨갛다** — 베이스도 그렇다.
   T126 이 이미 회부했고(0-터 7), 이번에 베이스가 `TypeError` 로 죽는 것까지 재확인했다.
   "입장 실패를 전제로 세워 즉시 멈춘다"는 그 회부가 여전히 값이 있다.

## 5. 푸시

```
브랜치   batch/fixture-clock-0906 → main (ff)
커밋     8e9bb7950c54d05e5e3e4f4314a5fb9bc7ea90d5
푸시     718b4924..8e9bb795   _t140b -> main   (fast-forward)
         batch/fixture-clock-0906 은 [new branch]
force    0
리베이스 **1회 · 충돌 0** — 5b45c862 → **718b4924**(상류 19커밋: T124·T133·T137·T139·T141·T143·
         T144·T145 + 문서). 상류가 `server/zone.js`·`30-n-net.js`·`46-h-verbs.js` 를 옮겼지만
         **내 파일 여섯과는 겹침 0**(`git diff --name-only` 로 확인).
재검     그래도 세계가 움직였으므로 접점을 다시 돌렸다 —
         `e2e-thirst` **23/0** · `e2e-cold` **55/0** · `e2e-ui` **125/0** ·
         `test-client-globals` 26/0 · `test-harness-lint` 19/0 · `node --check` ×5.
.lock 전수  `*.lock` 격리 밖 **0** · **`*.lock*` 격리 밖도 0**(T143 이 "패턴이 샌다"고 정정한 그 패턴까지)
            · ref 자물쇠(HEAD · packed-refs · refs/heads/main) **0**
            · 격리분 252건은 전부 옛 판의 `_stale_locks/` 다.
```
