# T278 — 영토 영속: `_terrGrow` 가 늘린 땅을 남긴다 (제품 한 자리 + 하네스 하나)

**[승인 대기 · T278]** `server/villages.js` **한 블록**(쓰기) · 읽는 자리는 **손댈 게 없었다** ·
새 수 0 · 새 손잡이 0 · `sim/`·`lab/`·`public/` 무접촉 · 켜지 않았다(손잡이 둘 기본 끔 그대로) · 실기 0.

**한 줄 답 — 영속은 **한 자리**(쓰기만 · 읽기는 이미 있었다) · 1차 부팅은 T267 끔 폭 안 ·
재부팅 뒤 영토 **50마을 전부 유지**(terr 행 213,180 → 251,400) · 영토 밖 집 **0채** ·
하네스 **전 빨강 2건 / 후 초록** · 3시드 800일 전문 diff **0·0·0**.**

**★카드의 전제 둘을 정정한다.**
① *"부팅 시 `terr` 행을 읽어 영토를 세우는 자리도 고친다"* — **고칠 게 없다.** `villages.js:2584` 가
이미 `terr` 행을 **그대로** `_terrSet` 에 넣는다. 쓰기만 없었다 ⇒ **제품은 정확히 한 자리**.
② *"`villages.js` 는 econ 랩 밖이라 교집합 0 일 것"* — **아니다.** `lab-modules --diff` 가
**교집합 1개 (`server/villages.js`)** 로 "★적재됨" 을 찍는다 ⇒ 규약대로 **3시드 800일 A/B 를 돌렸다**(diff 0·0·0).

---

## ⓐ 쓰기·읽기 자리 + 1차 부팅

**쓰기(신설 · `_terrGrow` 안 · 셀을 `own` 에 더한 바로 다음)**
```js
try {
  if (state.db && state.db.insertVillageBuilding && vil.dbId != null) {
    for (const k of added) {
      const ci2 = k.indexOf(','), ax = +k.slice(0, ci2), ay = +k.slice(ci2 + 1);
      state.db.insertVillageBuilding({ village_id: vil.dbId, type: 'terr', cx: ax, cy: ay, floors: 0, data: null });
    }
  }
} catch (e) { console.error(`[${state.zoneId}] 🏘️ 영토 영속 실패(${vil.name}):`, e.message); }
```
집(`:4858`)·곳간(`:4275`)·쉼터(`:4578`)·환호가 이미 쓰는 **그 길** 그대로다 — 새 문법 0 · 새 수 0.
쓰기가 실패해도 하루 틱을 안 죽인다(이 파일의 다른 영속 자리와 같은 규약).

**읽기(무변)** — `villages.js:2584`:
```js
else if (b.type === 'terr') terrSet.add(b.cx + ',' + b.cy);
```
자란 셀도 `terr` 행이므로 **이 줄이 그대로 세운다.** 손댈 게 없다.

### 1차 부팅(끔 세계 · 200일 · 50마을) — T267 끔 팔과 나란히

| 판 | 인구 | 집 | 침상/인구 | `_hcap<h` | 영토 중앙 | 비옥 종 | 집터 성공 | 빈손 | **`terr` 행** | 영토 밖 집 |
|---|---|---|---|---|---|---|---|---|---|---|
| T267 끔#1 | 2,782 | 236 | 51% | 25 | 3,450 | 29 | 136 | 780 | 172,500 | 0 |
| T267 끔#2 | 2,474 | 237 | 57% | 24 | 3,450 | 29 | 137 | 669 | — | 0 |
| T267 끔#3 | 2,844 | 235 | 50% | 27 | 3,450 | 29 | 135 | 740 | — | 0 |
| **T278 끔** | **2,538** | **238** | 56% | **27** | 3,450 | **29** | 138 | 680 | **172,592** | **0** |

- 인구 2,538 — T267 끔 폭(2,474~2,844) **안 ○** · `_hcap<h` 27 — 폭(24~27) **안 ○** · 비옥 종 29 **같다**
- 집 238 — T267 끔 폭이 235~237 이라 **+1채**. 세 판의 폭이 3채뿐이라 이 자의 분해능 안이다
  (이 블록은 **DB 쓰기만** 한다 — 메모리 판단·`own`·`added`·`_probe` 어느 줄도 안 바꿨다. 위 diff 가 전부다).
- **`terr` 행 172,500 → 172,592 (+92)** ⇒ **끔 세계에서도 자라는 그 몇 셀이 이제 남는다.**
  T237 이 이름 붙인 세 마을(어촌2·임업6·농촌2)의 땅이 재부팅을 넘긴다 — 카드가 말한 "그게 맞는 동작" 그대로다.
  ⇒ **1차 부팅은 같고, 2차 부팅부터 다르다**(아래 ⓒ).

---

## ⓑ 재부팅 대조 하네스 — `scripts/test-terr-persist.js` (신설 · `@regress`)

한 판(40게임일 · 영토 손잡이 켬 — 자라는 마을을 많이 만들어 아래가 자명 통과가 안 되게) → **DB 를 닫고 다시 연다** →
자란 값이 그대로인가 · 집이 제 영토 밖에 서지 않는가. 영토 규칙·거부 규칙은 **다시 안 적는다** —
자란 값은 세계에게(`/lifedbg` terr), 남은 값은 DB 에게(`village_buildings`) 묻고 둘을 견줄 뿐이다.

| 검사 | **고치기 전**(블록을 되돌린 판) | **고친 뒤** |
|---|---|---|
| ① (전제) 영토가 실제로 자란 마을이 있다 | ✓ 50곳 · 최대 4,920셀 | ✓ 50곳 · 최대 4,920셀 |
| ① 판이 40일을 돌았다 | ✓ | ✓ |
| ② ★**다시 열어도 영토가 안 줄어든다** | **✗ 줄어든 마을 2곳 · 최악 농촌16 4,200 → 3,900셀** | **✓ 50마을 전부 유지** |
| ③ ★제 영토 밖에 서는 집이 0채 | **✗ 15/272채** | **✓ 0/300채** |
| ③ⓑ `terr` 행이 줄지 않았다 | ✓(172,500 → 172,500 — **한 행도 안 늘었다**) | ✓ **213,180 → 251,400** |
| 결과 | **FAIL (2건)** | **PASS** |

②가 "2곳"에 그친 이유도 표에 적는다: 2차 판도 손잡이가 켜져 있어 **다시 자란다** —
되돌아간 땅을 며칠 만에 되찾으므로 ②는 약한 증인이고, **③이 날카로운 증인**이다(집 15채가 남는다).
`terr` 행이 고치기 전에는 **172,500 그대로**(한 행도 안 늚)라는 것이 원인을 한 줄로 말한다.

---

## ⓒ 2차 부팅 표

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 1차 판의 살아 있는 영토 | 최대 4,920셀 | 최대 4,920셀 |
| 1차 판이 DB 에 남긴 `terr` 행 | **172,500**(= 시딩분 그대로) | **213,180** |
| 2차 부팅 직후 영토 | **3,450 으로 되돌아감**(줄어든 마을 2곳 관측) | **그대로** |
| 2차 판 끝의 `terr` 행 | 172,500 | **251,400** |
| 2차 판 끝 · 제 영토 밖에 서는 집 | **15/272채** | **0/300채** |
| 끔 세계(200일) `terr` 행 | 172,500 | **172,592** (+92 — 자라는 세 마을) |
| 끔 세계 영토 밖 집 | 0 | **0** |

---

## ⓓ 한 줄 답

> **영속은 한 자리(쓰기만 — 읽기는 이미 있었다) · 1차 부팅 T267 끔 폭 안 ·
> 재부팅 뒤 영토 50마을 전부 유지 · 영토 밖 집 0채 · 하네스 전 빨강 2 / 후 초록 · 3시드 diff 0·0·0.**

---

## 검증

| 항목 | 결과 |
|---|---|
| 제품 diff | `server/villages.js` **한 블록**(+16줄 · 쓰기만) — 읽기 자리 무변 |
| `sim/` · `lab/` · `public/` | **무접촉** |
| 새 수 · 새 손잡이 | **0 · 0** (`T230_TERR_HOUSING`·`T219_HOUSE_TRIGGER` 기본 **둘 다 끔** 그대로) |
| `lab-modules --diff` | **교집합 1개 — `server/villages.js` ★적재됨** ⇒ 카드 전제 정정 · 규약대로 A/B 를 돌렸다 |
| 3시드 800일 전문 diff | **0 · 0 · 0** (베이스 `756fe35a` · `econ-lab-real.js 800`) |
| `test-terr-persist`(신설) | **PASS** · 되돌린 판에서 **FAIL 2건**(자명 통과 금지) |
| `test-psite-server` | **PASS** |
| `test-farm-life` | **33 PASS / 0 FAIL** |
| `lab-wiring-check` | **실패 0 · 경고 0** |
| 러너 전수 | **안 돌렸다** |
| 무접촉 | T279(세션5) · T275(세션1) |
| 접점 | `_terrGrow` `insertVillageBuilding` `village_buildings` `terr` `territoryTarget` `T230_TERR_HOUSING` `T219_HOUSE_TRIGGER` `_mapBeds` |

## 회부

- **#22 영토 + 방아쇠 켬 — 막는 것이 없어졌다(재민).** T267 표: 둘 다 켬이면 집 236 → **366채** ·
  침상/인구 52.7 → **83.1%** · 살려도 되는 마을 **15.7/25** · 인구 대가 **없음**(잡음 안).
  이 카드가 고친 것으로 "재부팅 뒤 영토 밖 집 101채" 도 **0** 이 된다. **PM 권고: 켠다.**
- **실서버 재부팅 실기(재민 · 배포 뒤)** — `village_buildings` 의 `terr` 행 수가 시딩값(172,500)보다
  커졌는지 한 번 보면 된다. 쓰기량은 하루 최대 60셀/마을(`TERR_GROW_MAX_PER_DAY`)이고
  실측 켬 세계 200일 누계가 50마을 합 **약 79,000행**(213,180 − 172,500 + 2차분)이다 — 행 수가 는다는 것을 적어 둔다.
  줄이고 싶으면 **한 마을의 영토를 한 행(범위·RLE)으로** 접는 길이 있다(지금은 집·곳간과 같은 문법을 지켰다). → **캐논 · 재민**
- ⓐ 마을별 비옥도 장(후보 점수 중립 · 캐논 · 재민) · 집 간격 18(**안 급함**) ·
  첫 의뢰(#18) · 추천 자격(#19) · 씨앗 env(재민) · `test-lab-psite ⑧` 기계 의존(공통 §2 후보).

## 실행

```
node scripts/test-terr-persist.js                     # 재부팅 대조(40게임일 · 두 번 부팅)
TP_DAYS=15 node scripts/test-terr-persist.js          # 짧게
LAB_SEED=1020 node scripts/econ-lab-real.js 800       # 3시드 비트 동일
```
