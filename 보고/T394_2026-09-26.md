# 보고 — T394: **몸 비비기의 뿌리를 뽑았다** — 물 위 일터 **162 → 0** · 막힌 목표로 가는 여행 **10.0 % → 0.7 %** · 물가 정지 **36.0 → 19.9명**(8/8) · `localPath` 끝점 비대칭 **0** · `T381_PATH_DEAD` **기본 켬** · 3시드 800일 **30칸 동일**(가지 위)

> **머리 표 — 카드 전(main `259dadb6`) ↔ 카드 뒤(이 가지 · 기본값 그대로) · 같은 벽시계 두 팔 · 낮 창 7,200틱 · 비취침 1,117 · drop 0**
>
> | | 전 | 뒤 | 가름(T252 · 조각 8) |
> |---|---:|---:|---|
> | **물·바위 위 일터**(주민 전수) | **162 / 1,475** | **0 / 1,475** | 태어나기 1,475 = 첫 점 1,313 + 다시 뽑기 162 · 마당 0 |
> | **막힌 칸이 목표인 여행** | **8,005(10.0 %)** | **548(0.7 %)** | 조각마다 959~1,071 → 60~78 |
> | ↳ 그중 ⑤ 배회 **일터 상자**에서 온 것 | 7,401(92.5 %) | **0** | 남은 548 의 94 %는 튕겨냄 무작위 도약(회부) |
> | **물가 정지**(5초에 32px 도 못 간 · 먼 목표 · 물가 1셀) | **36.0명**(8.2 %) | **19.9명**(4.9 %) | **8/8 음 · −96 · 폭 58 → 가름** |
> | 튕겨냄 `unstuckNpc`/틱 | 0.524 | 0.328 | 8/8 음 → 가름 |
> | 강으로 꽂는 직선(beeline)/틱 | 1.611 | 0.533 | 8/8 음 → 가름 |
> | A\* 호출/틱 · ms/틱(사람당) | 1.325 · 3.324(2.25 µs) | 0.674 · 1.513(**1.03 µs**) | 8/8 음 → 가름 |
> | 존 틱 p50 | 21.00 ms | 18.88 ms | 8/8 음 · −2.12 · 폭 1.05 → 가름 |
> | 반례: 열린 목표 도착률 · 도착 시간 중앙(p75) | 20.7 % · 1.625 s(4.875) | 20.8 % · **1.375 s**(4.625) | 여행 수 71,782 → 77,389(+7.8 %) |
>
> **④ 켬/끔**(뒤 판에서 `T381_PATH_DEAD` 만 끈 팔과 같은 벽시계): 물가 정지 18.9 → 18.6명 · 도착률 21.4 = 21.4 % · 도착 시간 중앙 1.375 = 1.375 s ·
> 존 틱 p50 −0.08 ms(부호 섞임 · **못 가름**) — ③ 이 값을 먼저 가져갔으므로 **켬 = 끔**(좌표로는 같은 답 · 켬은 이중 자물쇠) · **3시드 800일 열 열 넷째 판 30칸 전부 동일**(1020 · 7 · 42).

* 베이스 **`259dadb6`**(main) · 가지 `batch/worksite-terrain-0925` · 손잡이: **`T394_WORK_TERRAIN`**(①② · **기본 켬** · 되돌림 `=0`) · **`T381_PATH_DEAD` 기본 켬**(④ · 되돌림 `=0`) · ③ 은 정본 규칙(손잡이 없음 · 안 넘기면 종전)
* 고친 제품 파일 넷: `server/villages.js`(태어나기 절만) · `server/zone.js`(⑤ 한 줄 + 되짚기 함수 + 손잡이 두 줄) · `sim/path-core.js`(`localPath` 첫 줄 둘) · `server/pathfind.js`(노드 판정 한 번 적기 + 넘기기) · 랩 둘 재인라인(`inline-path.js` · 정본 블록만)
* 하네스: `test-move-soa` **107 → 129 / 0**(⑪ 신설 20절 · ⑥ 에 ② 싣기 · ⑩ 을 ③④ 에 맞춤) · `sim/_path-core-test` **34 → 42 / 42**(T394 절 8) · 나머지 아홉 초록(§4)

---

## 0. 한 줄들

> **ⓐ 물 위 일터 162 → 0.** 주민이 태어날 때 받는 일터(`npcWorkX/Y` · 회관 둘레 180~320px 도넛)를 **A\* 가 간선에 대는 그 술어**
> (`isTerrainBlockedLocal` · 존이 `deps` 로 주입하는 그것 · 셀 중심)로 거르고, 막히면 **같은 도넛 · 같은 씨 흐름**으로 다시 뽑았다
> (상한 = 도넛 바깥 둘레 셀 수 63 · 새 수 0 · `Math.random` 0). 162명 전부 다시 뽑아 뭍을 찾았다 — 마당 폴백 0.
> 몸 자리 굴림은 **종전 순서 그대로**(다시 뽑기는 그 뒤 흐름)라 첫 점이 열린 1,313명은 일터까지 **비트 동일**이다.
>
> **ⓑ 막힌 목표로 가는 여행 10.0 % → 0.7 %.** ⑤ 배회가 일터 ±50/80 을 줄 때 목표 셀이 막혔으면 **일터 쪽으로 32px 씩 되짚어**
> 첫 열린 칸으로 옮긴다(새 탐색 0 · 주사위 0 · 열린 목표 무변). 일터 상자에서 온 막힌 여행 7,401 → **0**. 남은 548 은
> **94 %가 튕겨냄 무작위 도약**(3연속 막힘의 ±80px — 카드 밖 · 회부 1).
>
> **ⓒ `localPath` 끝점 비대칭 0.** 첫 줄을 `routePath` 첫 줄과 **글자까지 같게**(`const blocked = opts.blocked || (() => false); if (blocked(sx, sy) || blocked(gx, gy)) return null;`).
> 어댑터(`pathfind.js`)가 간선에 쓰던 노드 판정(물·바위 — 다리 예외)을 **한 번만** 적어 간선과 끝점이 같이 쓴다.
> 열린 끝점 **40쌍 비트 동일**(T89 자) · 막힌 목표가 사전순으로 앞선 쌍에 나던 길 **33 → 0** · 규칙을 빼면 되살아난다(미끼).
>
> **ⓓ `T381_PATH_DEAD` 기본 켬 · 물가 정지 36.0 → 19.9명.** ③ 뒤로는 켬/끔이 **같은 답**이다(둘 다 막힌 목표에 `null`) — 같은 벽시계
> 두 팔에서 물가 정지 18.9 ↔ 18.6 · 도착률 21.4 ↔ 21.4 % · p50 −0.08 ms(**못 가름**). 켬이 없애는 것은 탐색 전에 끝나던 호출 591건(끔 · 0.08/틱)뿐이다.
> 물가 정지는 카드 전/후 짝에서 **8/8 · 가름**. 남은 19.9명은 막힌 목표가 아니라 **1500 × 뭍**(같은 덩어리인데 1500칸 안에 못 닿는 목표 →
> `null` → 강을 가로지르는 직선)에서 온다 — T381 회부 3 · 다음 카드.
>
> **ⓔ 한 줄 답** — §5.

## 1. ① 일터 도넛에 지형 — `villages.js` 태어나기

```js
const T394_WORK_TERRAIN = process.env.T394_WORK_TERRAIN !== '0';
const _T394_TRIES = Math.ceil(2 * Math.PI * (180 + 140) / SZ);   // 도넛 바깥 둘레 셀 수 = 63
function _t394Blocked(x, y) { … state.deps.isTerrainBlockedLocal(셀 중심) … }
function _t394WorkSite(vil, cxPx, cyPx, wAng, wR) {
  첫 점(종전 식) → 열렸으면 그대로 · 막혔으면 _dv() 로 (각·반경) 다시 뽑기 ≤ 63 → 다 막히면 남문 앞 마당(ccx, ccy+6)
}
// spawnOneNpc: wAng · wR · 몸 x · 몸 y (종전 네 굴림 순서 그대로) → 그다음 일터
```

| | 끔(= 종전) | 켬 |
|---|---:|---:|
| 물·바위 위 일터(주민 전수 · 실서버 7,200틱 내내 같은 수) | **162 / 1,475** | **0 / 1,475** |
| 태어나기: 첫 점 열림 · 다시 뽑아 열림 · 마당 | — | 1,313 · **162** · 0 |

* **술어는 그 함수다** — A\* 가 `isWaterFn` 으로 받는 `isTerrainBlockedLocal`(존 정본 · `deps` 주입)을 `findPath` 의 `cpx` 와 같은 **셀 중심**에 댄다. 새 술어 0.
* **몸·pid·econ 무변** — 다시 뽑기는 몸 자리 굴림(셋째·넷째) **뒤**에서 같은 흐름을 잇는다. 첫 점이 열린 몸은 일터도 뒤따르는 흐름도 비트 동일(⑪-b 2,923/2,923).
  pid·집·침대·몸 자리는 막혔던 162명도 그대로다. econ 은 `npcWorkX` 를 안 읽는다(읽는 자리는 zone ⑤ 하나 · `bandits.js` 는 제 도적 몸).
* **카드가 가리킨 `_actEntitiesAtCell` 은 나무·군락 셈 문**이라 문간 셀을 갖지 않는다. 대신 **같은 함수(`spawnOneNpc`)가 집 폴백에 이미 쓰는
  "남문 앞 마당"**(`(ccx, ccy+6)` — 큰집 8×8 남벽 밖 마당)을 그대로 썼다(같은 식 · 사본 아님). 이번 판에선 한 번도 안 썼다.
* **3시드 800일 열 열 넷째 판 — 30칸 전부 동일**(`t17-metrics` · `villages.js`·`path-core.js` 를 만졌으니 면제 대신 돌렸다):

  | 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 | ㉮ | ㉯ |
  |---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
  | 1020 | 8,318 | 0/51 | 1,306 | 9,602 | 2,504 | 16,596.5 | 4,284.2 | 74,094.9 | 1.94 | 2.04 |
  | 7 | 7,935 | 0/51 | 1,275 | 9,892 | 2,151 | 14,638.6 | 4,719.3 | 74,291.4 | 2.07 | 2.18 |
  | 42 | 7,853 | 0/51 | 1,356 | 9,790 | 2,361 | 15,293.9 | 3,885.9 | 76,117.9 | 1.96 | 2.06 |

  (이 자는 생활층을 안 탄다 — 태어나기 절도, 걸음 경로도 안 부른다. 그래서 같다는 것이 **예상이자 확인**이다.)

## 2. ② ⑤ 배회 목표 — 막힌 칸이면 일터 쪽 첫 열린 칸

```js
// decideNpcBehavior ⑤ — 일터 ±50/80 을 뽑은 **다음 줄**
if (T394_WORK_TERRAIN) _t394OpenTarget(npc, npc.npcWorkX, npc.npcWorkY);
function _t394OpenTarget(npc, ax, ay) {   // 목표 셀이 막혔으면 목표→일터 직선을 32px 씩 되짚어 첫 열린 점
  …isTerrainBlockedLocal(셀 중심)… for (i = 1..ceil(거리/32)) { 열린 점이면 목표로 · return true } · 일터마저 막혔으면 그대로
}
```

* **새 탐색 0** — 길찾기가 아니라 한 줄 되짚기다(상자 대각선 113px 이면 네 걸음 · 걸음 폭은 `straightPathClear` 의 그 32px).
  카드가 적은 "`findPath` 가 이미 아는 그 칸"은 A\* 가 셀 중심에 대는 **그 판정**으로 읽었다 — 같은 술어 · 같은 자리.
* **주사위 0** — 흐름을 한 알도 안 쓴다(⑤ 의 두 굴림 **뒤**에만 선다 · 뒤따르는 결정의 굴림이 안 밀린다).
* **열린 목표는 안 건드린다**(⑪-e 12,000/12,000 비트 동일) · 막힌 목표는 **전부** 열린 칸으로(5,125/5,125) · 옮긴 자리는 **선분 위**(5,125/5,125).
* 일터마저 막혔으면 목표를 **그대로** 둔다 — beeline 을 넣지 않는다(T381 §4-5). ① 뒤로 그런 주민은 0 이다.
* 실서버: 되짚기가 틱마다 **0.164번** 섰다 · 일터 상자에서 온 막힌 여행 **7,401 → 0**.

| 막힌 여행의 출처(결정 때 잰다 · 손잡이와 무관) | 전 | 뒤 |
|---|---:|---:|
| 합 | 8,005(여행의 10.0 %) | **548(0.7 %)** |
| ⑤ 배회로 떨어진 결정 | 7,536(94.1 %) | 49(8.9 %) |
| 일터 상자(±80) 안 | 7,401(92.5 %) | **0** |
| 튕겨냄 무작위 도약(±80px) 3초 안 | 1,470(18.4 %) | 517(**94.3 %**) |

## 3. ③ `localPath` 끝점 · ④ 켬

### 3-ⓐ ③ — 첫 줄을 `routePath` 와 같게

```js
function localPath(sx, sy, gx, gy, opts) {
  opts = opts || {};
  const blocked = opts.blocked || (() => false);          // ← routePath 첫 줄과 글자까지 같다
  if (blocked(sx, sy) || blocked(gx, gy)) return null;
  return _search(…);
}
// pathfind.js — 노드 판정을 한 번만 적고 간선·끝점이 같이 쓴다(부르는 순서 벽 → 물 → 다리 그대로)
const nodeBlocked = (x, y) => { const X = cpx(x), Y = cpx(y); return isWater(X, Y) && !(isBridge && isBridge(X, Y)); };
const blockedStep = (fx, fy, tx, ty) => { if (isBlocked(…)) return true; return nodeBlocked(tx, ty); };
PathCore.localPath(…, { blocked: nodeBlocked, blockedStep, … })
```

`sim/_path-core-test.js` T394 절(30×30 · 막힌 칸 34 %):

| | 값 |
|---|---|
| ⓐ `localPath`·`routePath` 첫 줄이 **글자까지 같다** | ✓ |
| ⓑ 열린 끝점 **40쌍** — 규칙 있음/없음 **비트 동일**(T89 자) | **40/40** |
| ⓒ 커널은 방향으로는 안 갈린다((A→B)·(B→A) 거울) | 0 어긋남 |
| ★ⓒ 결함 증인(미끼) — 규칙 없으면 막힌 목표가 사전순 **앞**인 쌍에 길 · **뒤**인 쌍엔 0 | **33** · 0 |
| ⓒ 규칙 있으면 막힌 목표엔 **자리와 무관하게** null | **0** |
| ⓓ `blocked` 안 주면 종전(랩 `bfsPath` 는 제 목표 검사를 이미 한다 — 동작 무변) | 0/40 |

* `test-move-soa ⑩-b` 가 서버 어댑터로 같은 것을 문다: 뒤집힌 막힌 목표 **623/623 null**(T381 에선 365 에 길) · 규칙 빼고 커널 직접 → **379 에 길**(미끼).
* **랩 재인라인** — `node sim/inline-path.js` → 두 랩 `PATH-CORE` 블록만 +7줄 · `--check` 둘 다 **최신 ✅** · `inline-engine --check` ✅.
* ⚠ **출발 칸도 본다**(`routePath` 와 같게). 몸이 막힌 칸 **안**에 서 있으면 이제 `null` 이다 — T381 판에서 그런 호출은 **0**이었고
  (콜라이더가 막힌 칸에 못 들게 한다), `computeNpcPath` 는 A\* 앞에서 **직선 검사**를 먼저 하므로 도랑이 발밑에 파여도 빠져나올 길이 남는다.
  대상 캐러밴(`caravanBlockedResponse`)도 같은 문을 쓴다 — 물칸 위에서 벽에 막힐 일은 없어 실측 영향은 못 봤다(지켜볼 자리 · §6).

### 3-ⓑ ④ `T381_PATH_DEAD` 기본 켬 — 게이트(T381 §3 그 자 · 같은 벽시계 두 팔 · 낮 창 · 조각 8)

| | 끔(`T381_PATH_DEAD=0`) | **켬(기본)** | 가름 |
|---|---:|---:|---|
| `pfFindPath` 호출/틱 | 0.771(그중 **끝점 null 591** = 0.082/틱) | 0.663(끝점 null 0 — 손잡이가 먼저 돌려보낸다) | — |
| A\* ms/틱(사람당) | 1.439(0.98 µs) | 1.256(0.85 µs) | 1500 칸 호출 수 차(1,506 ↔ 1,385)가 판 흔들림 안 |
| **물가 정지** | 18.9명(4.78 %) | 18.6명(4.76 %) | 부호 섞임 → **못 가름** |
| 마른 곳 정지 | 37.5명 | 37.2명 | 못 가름 |
| 열린 목표 도착률 · 도착 시간 중앙(p75) | 21.4 % · 1.375 s(4.375) | 21.4 % · 1.375 s(4.375) | **같다** |
| 막힌 목표 여행 | 0.76 % | 0.73 % | 못 가름 |
| 튕겨냄 · beeline /틱 | 0.351 · 0.548 | 0.327 · 0.535 | 둘 다 못 가름 |
| 존 틱 p50 | 16.61 ms | 16.53 ms | 조각 차 −0.08 · 폭 0.51 · 부호 섞임 → **못 가름** |

⇒ **③ 이 값을 먼저 가져갔다.** 끝점이 막힌 질문은 켬이든 끔이든 탐색 전에 `null` 이고(끔은 `findPath` 에 들어갔다가 `localPath` 첫 줄에서,
켬은 그 앞에서), 좌표로는 같은 답이다. 켬이 남기는 것은 **이중 자물쇠** — 정본(`path-core.js`)이 되돌려져도 1500칸을 태우지 않는다.
T381 이 잰 값(A\* −48~56 % · p50 −1.3~1.8ms)은 지금 **①②③ 쪽에** 있다(머리 표 A\* 3.32 → 1.51 ms/틱 · p50 −2.12 ms).

* **끔 되돌림 = 종전 비트 동일** — `T381_PATH_DEAD=0` 이면 블록 첫 항에서 단락(⑩-a · 0회). 제품 글자는 `!== '0'`(옛 `=== '1'` 0 · ⑩ 이 건다).

## 4. 검증

| 하네스 | 결과 | 비고 |
|---|---|---|
| **3시드 800일 열 열**(`t17-metrics` 1020·7·42) | **30/30 동일** | 넷째 판 표 그대로 · 478~495초 |
| `test-move-soa` | **129 / 0** | 107 → 129(⑪ 20절 · ⑥ 에 ② 실음 +1 · ⑩-b 미끼 +1) |
| `sim/_path-core-test`(`test-path-core`) | **42/42**(3 통과) | T394 절 8 · 40쌍 40/40 |
| `inline-path --check` · `inline-engine --check` | **✅ ✅** | 두 랩 |
| `test-fish-act` · `test-wood-act` · `test-forage-act` | 73/0 · 90/0 · 137/0 | |
| `test-war-world` | **97 / 0**(셋째 판) | ⚠첫 두 판 94/3 · 96/1 — **흔들림**이다(아래) |
| `e2e-move` | 37 / 0 | |
| `test-harness-lint` | 61 / 0 | |
| `lab-wiring-check` | 실패 0 · 경고 0 | |
| `test-hall-door-path`(`findPath` 쓰는 자) | RC 0 | |

★**`test-war-world` 흔들림 — 이 카드가 아니다(재어 봤다).** 가지에서 94/3 · 96/1 · **97/0**, main 에서 97/0 · 97/0. 갈린 절은 ⓓ(관측자 켬/끔 한 글자) ·
ⓛ(숲 판이 끝나나) — 야간러너 09-24 가 main 에서 같은 ⓛ 를 "단독 초록"으로 적은 그 절이다.
* **①은 이 하네스에서 한 번도 안 섰다** — 임시 탐침으로 셌다: 다시 뽑기 **0**(모든 일터가 첫 점에서 열렸다) ⇒ 태어나기는 main 과 비트 동일.
  ②④ 는 존 `zone.js` 안이라 이 하네스의 판(생활층 in-process)에 안 탄다. ③ 은 캐러밴 재경로만 쓰는데 'assault' 판엔 막힌 캐러밴이 없다.
* **흔들림의 원천**: `spawnOneNpc` 가 씨에 `gameDayOf(Date.now())` 를 넣는다(T350) — 하네스는 제 시계(`dayMs` 30초)로 판을 돌리는데 태어나기만 **벽시계**를
  읽어, 30초 칸이 바뀌면 병사가 다른 자리에서 태어난다. 그 판이 숲 판의 결말을 가른다(켬 1,450~4,293틱). ⇒ 회부 3(시계 손잡이 — 200일 자와 같은 자리).

접점: `npcWorkX|npcWorkY|isTerrainBlockedLocal|decideNpcBehavior|computeNpcPath|pfFindPath|localPath|routePath|path-core|T381_PATH_DEAD|T394_WORK_TERRAIN|_t394WorkSite|_t394OpenTarget|seed-rand|t381-path-cost`.
무접촉: T385(`zone.js` 틱 본체) · T368(`villages.js` 밭 절) · T393(자원 절) — `villages.js` 는 태어나기 절만, `zone.js` 는 ⑤ 한 줄·되짚기 함수·손잡이 두 줄·A\* 주석만.

## 5. 한 줄 답 · 회부

> **한 줄 답 — 몸 비비기의 뿌리 둘을 같은 술어(A\* 의 `isTerrainBlockedLocal`) 하나로 뽑았다: 물 위 일터 162 → 0(같은 씨 흐름으로 다시 뽑기 ·
> 몸 무변) · ⑤ 배회의 막힌 목표는 일터 쪽 첫 열린 칸으로(새 탐색 0) → 막힌 목표 여행 10.0 → 0.7 % · 물가 정지 36.0 → 19.9명(8/8) ·
> 튕겨냄 −37 % · A\* −54 % · 열린 목표 도착 무변. `localPath` 첫 줄을 `routePath` 와 같게(비대칭 0 · 열린 40쌍 비트 동일).
> `T381_PATH_DEAD` 는 켰다 — ③ 뒤로는 켬 = 끔(이중 자물쇠). 3시드 30칸 동일. 남은 물가 정지의 주인은 1500 × 뭍(다음 카드).**

| # | 무엇 | 근거 | 누구 |
|---|---|---|---|
| 1 | **튕겨냄 무작위 도약**(3연속 막힘 → ±80px)이 남은 막힌 목표의 94 %다 — 같은 술어로 거르거나 되짚기(②)를 거기에도 | §2 표 | M |
| 2 | **1500 × 뭍** — 남은 물가 정지 19.9명의 주인(같은 덩어리인데 1500칸 안에 못 닿는 목표 → `null` → 강을 가로지르는 직선) | §0-ⓓ · T381 회부 3 | M(다음 카드) |
| 3 | **시계 손잡이** — `spawnOneNpc` 의 씨가 벽시계를 읽어 `test-war-world` ⓓⓛ 가 흔들린다(200일 자 결정성과 같은 자리) | §4 | PM(자) · T350 §3-ⓒ |
| 4 | 몸이 막힌 칸 **안**에 있을 때(도랑이 발밑에 · 캐러밴이 물 위) `localPath` 가 `null` — 실측 0 이지만 지켜볼 자리 | §3-ⓐ ⚠ | M |
| 5 | C 층 접속(#53) | 카드 §4 | — |

## 6. 자 규율 · 덧

8. **하네스가 흔들리면 "내 것이 섰나"부터 센다.** `test-war-world` 가 두 판 내리 붉었을 때 main 두 판(초록)과 견주고 끝내지 않고,
   **내 갈래가 그 하네스에서 한 번이라도 섰는지**를 임시 탐침으로 셌다(다시 뽑기 0). 섰는지 모르면 "흔들림"이라 부를 자격이 없다.
