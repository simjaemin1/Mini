# T411 — BGM 정리 셋 [승인 대기 · T411] (2026-09-26 · 세션8/ART)
**한 줄 답:** `e2e-bgm-phrase` ⑦⑧ 은 **12/0** 이다(20번 돌려 20/20). 문턱은 이 상자의 떨림 바닥, 곧 같은 판을 두 번 구운 차(RMS) **그 자체**이고, 미끼는 그 바닥보다 80 dB 위에서 문다. `janggu_gung` 은 **2곡 '샘플(궁편 6조각 · 84타)' · 11곡 '미상'** 으로 사실만 고쳤다(값 0). tools 24 폴더를 한 번씩 돌렸다: **돈다 5 · 안 돈다 19 · 끊음 0** · 권고 **쓴다 1 · 참고 9 · 버림 14**(이동 제안 표 · 스크래치로 옮겨 보니 남는 10 폴더는 시험 무변).
가지 `batch/bgm-tidy-0926` = `bc5d0ea` · 베이스 `057d40c`(main) · 잰 베이스 `67a8609`(리베이스 1회 — T407·T412·T415 착지 · 겹친 파일 `scripts/test-audio.js` 하나 · 다른 줄이라 충돌 0 · 게이트 새로 잼) · force 0 · main 은 PM
**`bgm.js` 무변 · 배포 13곡 파일 diff 0 · `tools/` diff 0 · `sfx/**` 무접촉** · 새 파일 = `scripts/t411-oac-floor.js`(러너 밖) · 값 0 · 새 소리 0 · tools 실행은 전부 `unshare -n`(네트워크 0) · 5분 상한

| 셋 | 결론(실측) |
|---|---|
| ① ⑦⑧ 의 자 | ⑦ 은 **같은 판 두 번**을, ⑧ 은 feature-off ↔ on 의 **반주**(`lead:false` 두 판)를 잰다(착지 전후 차가 아니다). 옛 허용치 `Δmax 1.5e-6 · RMS 1e-7` 은 GPT 상자 관측(1.01e-6 · 4e-8)에 붙인 여유였다. 이 상자의 같은 판 쌍은 **480/480** 이 그 허용치를 넘는다. 새 자: **바닥 = 같은 판 두 번 RMS 중 큰 쪽 · 문턱 = 바닥 자체**. ⑦ 은 기록이 같고 `Math.random` 0 번이며 미끼(다른 씨 · 다른 판)가 바닥을 넘는지 본다. ⑧ 은 반주 차가 바닥 안이고 미끼(반주 다른 씨 · 대금 켠 판)가 넘는지 본다 → **10/2 → 12/0** |
| ① 왜 떨리나 | 압축기·잔향 탓이 아니다. **한 자리에 셋 이상이 더해지면** 판마다 더하는 순서가 달라진다. 노드 입력 1·2개는 0/15(다섯 번 잼)이고, 3개부터 9~12/15, 5개는 14~15/15 가 다르다. AudioParam 은 제 값까지 세어 항이 셋(입력 둘)이면 0~8/15, 넷이면 12~14/15 다 |
| ① 자명 통과 반례 | `--selftest` 주입 셋이 **기대한 절만** 빨갛다. 새는 난수 → ⑦ · 바닥 부풀림 → ⑦⑧ · 반주 음량 → ⑧. 반주 **자기** 바닥을 문턱으로 쓰면 교차 쌍의 **50.0%** 가 넘는다(동전). ⑦ 의 바닥을 쓰면 **0/3,686,400** 이다 |
| ② `janggu_gung` | `render-meta.json` 칸은 `'합성'` → **`'샘플(궁편 6조각 · 84타) — village_day_trad·village_day_jeongak 만 · 11곡은 미상(07-30 판 · 코드 없음)'`** 이고 메모 `_T411` 을 달았다. `README-BGM.md` 목록 줄은 '장구' → **'장구 채편'** 이다. `test-audio ⑨l` 은 궁편과 채편을 따로 맞댄다. 셋이 한 판에서 같이 움직였다 → ⑨l 초록, 되돌림 미끼 넷 전부 문다 |
| ③ tools 24 | 진입점을 한 번씩 돌렸다. **돈다 5**: `score-expression`(컴파일 · 계획 5장 재생성 비트 동일) · `midi-ddsp-score-adapter`(MIDI + 다리 JSON — T386 의 "못 돌림" 정정) · `gugak-corpus-inventory`(레포 오디오 118파일) · `expressive-synthesis`·`midi-ddsp-transfer-train`(준비 판정만 — "준비 안 됨"). **안 돈다 19**: 입력이 `_bgm_rnd`/원음/가중치 zip/AI Hub/맥 경로에 있다. 그중 **체크포인트 없음** 3이고, **exit 0 인데 아무것도 안 읽은 것** 2(`native_wav.py`·`midi-ddsp-daegeum-adapter`). 자기 시험은 230 중 229 초록(`midi-ddsp-daegeum-adapter` 5/6 — 레포 밖 파일을 단언) |
| ③ 이동 제안 | 버림 14 = MIDI-DDSP 줄기 7 · 청취 묶음 줄기 5 · `source-led-stem-mixer` · `tools/ddsp`(T353 — 내 것). **표만**(이동은 PM). 스크래치 clone 에서 옮겨 보았다: 남는 10 폴더 시험 **무변** · `scripts/test-daegeum-transition-bank.py` 8/0 · 옮긴 쪽 6폴더 시험이 옆 폴더 경로로 깨진다(한 줄씩) · 제품 쪽 흔적은 `phrase.py` 3줄(`tools/ddsp` — 모델 0 이라 동작 무변) |

게이트 `e2e-bgm-phrase` **12/0**(베이스 10/2) · `--selftest` PASS(주입 3/3) · `test-audio` **170**/0(카드 160 · 새 베이스 170 — T412 가 +10) · `test-bgm-phrase` 16/0 · `test-assets-audit` PASS · `--selftest` PASS · `test-render-score-articulation` 14/0 · `test-client-globals` 26/0 · `test-harness-lint` 61/0 · 접점 `e2e-audio-probe` **71**/0(새 베이스 71) · `test-daegeum-transition-bank.py` 8/0(커밋 뒤 — §검증)

---

## §① `e2e-bgm-phrase` ⑦⑧ — 자를 떨림 바닥에서

### ①-1 무엇을 재나 · 허용치는 어디서 왔나

| 절 | 재는 것 | 옛 판정 | 옛 허용치의 출처 |
|---|---|---|---|
| ⑦ | **같은 판 두 번**(같은 variant·seed · 같은 페이지 · legacy 둘 · phrase 둘) | score/control 기록 같다 + 파형 `Δmax ≤ 1.5e-6 · RMS ≤ 1e-7` | 하네스 주석: *"실측 max 1.01327896e-6, RMS < 4e-8 … 그 관측 상한보다 조금 큰"* — GPT 상자 **한 번의 관측 위에 붙인 여유**(×1.48 · ×2.5)다. 근거 줄 0 ⇒ **지어낸 수** |
| ⑧ | feature-off ↔ on 의 **반주**(legacy `lead:false` ↔ phrase `lead:false`) · 악보가 두 판에서 같은가 | 악보·반주 기록 같다 + 반주 파형 같은 허용치 | 같다 |

⇒ 둘 다 착지 전후 차가 **아니다**(그건 T386 자 ①이 명령 기록으로 12/12 쟀다). 그리고 둘 다 "같은 그래프를 두 번 구운 차"가 허용치 밑이기를 바랐는데, 이 상자에선 그 차가 **늘** 허용치 위다(아래 ①-2) ⇒ 물려받은 빨강 10/2 는 **자의 결함**이었다(제품 무관 — `bgm.js` 무변).

### ①-2 바닥 — 같은 판 두 번 (`scripts/t411-oac-floor.js --n 16` · Chromium 141.0.7390.37 · 판마다 16번 · 4마디 preview)

| 무엇 | 쌍 | RMS 차(최소 ~ 중앙 ~ 최대) | 신호 대비 | Δmax | 무리 |
|---|---|---|---|---|---|
| 같은 판 legacy | 120 | 2.08e-6 ~ 2.20e-6 ~ 2.32e-6 | −93.5 ~ −92.9 ~ −92.5 dB | 1.07e-5 ~ 2.66e-5 | 1 |
| 같은 판 phrase | 120 | 2.91e-6 ~ 4.97e-6 ~ 7.63e-6 | −93.1 ~ −88.5 ~ −84.8 dB | 1.31e-5 ~ 3.71e-4 | **여럿**(RMS 차가 3.0·5.0·6.5·7.5e-6 네 층) |
| 같은 판 반주 legacy | 120 | 1.53e-6 ~ 1.62e-6 ~ 1.73e-6 | −92.6 ~ −92.1 ~ −91.5 dB | 8.71e-6 ~ 1.65e-5 | 1 |
| 같은 판 반주 phrase | 120 | 1.56e-6 ~ 1.64e-6 ~ 1.75e-6 | −92.5 ~ −92.0 ~ −91.4 dB | 8.67e-6 ~ 1.82e-5 | 1 |
| **반주 교차** legacy×phrase | 256 | 1.55e-6 ~ 1.63e-6 ~ 1.74e-6 | −92.5 ~ −92.1 ~ −91.5 dB | 8.74e-6 ~ 2.00e-5 | — |
| 미끼 다른 씨 · 다른 판 · 반주 다른 씨 · 대금 켬 | 1씩 | 7.90e-2 · 1.28e-1 · 8.00e-2 · 1.18e-1 | −5 ~ +5 dB | 0.33 ~ 0.74 | — |

* 옛 허용치(RMS 1e-7)는 이 상자 바닥보다 **15~76배** 좁다 · 같은 판 쌍 **480/480** · 반주 교차 **256/256** 이 옛 자로 빨갛다.
* 4마디 preview 의 바닥(−92.9 dB · phrase 는 −84.8 dB 까지)은 T386 이 40초 전체 믹스 12조합에서 잰 **−91.2 dB**(중앙값)와 같은 띠다.
* ★**반주 교차 256쌍 = 반주 같은 판 240쌍의 분포**(최소·중앙·최대가 0.02e-6 안에서 겹친다) — 반주 두 판은 이 자로는 **같은 소리**다.

### ①-3 왜 떨리나 — 한 자리에 더해지는 항의 수 (같은 최소 그래프 6번 · 쌍 15)

| 자리 | 입력 | 더하는 항 | 다른 쌍(다섯 번 잰 범위) | Δmax |
|---|---|---|---|---|
| 노드 입력(GainNode 에 발진기) | 1 | 1 | **0/15** | 0 |
| 〃 | 2 | 2 | **0/15** | 0 |
| 〃 | 3 | 3 | 9~12/15 | 2.4e-7 |
| 〃 | 5 | 5 | 14~15/15 | 9.5e-7 |
| AudioParam(반송파 주파수에 변조) | 1 | 2(제 값 + 1) | **0/15** | 0 |
| 〃 | 2 | 3 | 0~8/15 | 3.9e-7 |
| 〃 | 3 | 4 | 12~14/15 | 3.9e-7 |
| 〃 | 5 | 6 | 14~15/15 | 7.5e-7 |

⇒ **셋 이상이 한 자리에 더해지면 더하는 순서가 판마다 바뀐다**(둘은 교환법칙이라 늘 같다). 끝자리 차(~1e-7)가 필터·발진기 위상 적분을 지나 preview 에서 ~1e-5 로 자란다. T386 이 이분법으로 본 "노드 한 종씩은 결정적 · 압축기·잔향을 빼도 남는다"와 맞는다 — 한 종씩 떼어 보면 더하는 자리가 없고, 큰 그래프엔 어디에나 있다.
`설계/BGM_RND_01.md`·옛 하네스 주석의 "native compressor/convolver 의 LSB drift" 귀속은 **안 선다**(주석에서 지웠다). 바닥이 여러 층으로 갈리는 것은 phrase 판뿐이다(legacy·반주는 한 무리 · 층 사이 Δmax 3.6e-4) — 순서가 몇 갈래로 갈리는 자리가 phrase 목소리 쪽에 있다.

### ①-4 자 셋 — 같은 분포에 대 보면

| 자 | 반주 교차 쌍이 넘는 비율 | 뜻 |
|---|---|---|
| 옛 자(GPT `1.5e-6 · 1e-7`) | **256/256**(같은 판 480/480 도) | 거짓 빨강 — 10/2 의 정체 |
| 반주 **자기** 바닥(반주 같은 판 쌍 하나를 문턱으로) | **50.0%**(240 × 256 조합) | **동전** — T386 §ⓐ-3 이 겪은 그 꼴(같은 분포의 두 표본을 서로 대면 반은 넘는다) |
| **⑦ 의 바닥**(legacy·phrase 같은 판 쌍 중 큰 쪽) | **0/3,686,400**(120 × 120 × 256 조합) | 가장 낮은 문턱 2.91e-6 ↔ 가장 큰 교차 1.74e-6 = **+4.5 dB** 여유 · 가장 높은 문턱 7.63e-6 ↔ 가장 작은 미끼 7.90e-2 = **+80.3 dB** |

★⑧ 의 문턱을 ⑦ 의 바닥으로 두는 까닭은 **구조**다. 두 판(반주 + 대금)은 반주 위에 대금 자리를 얹은 그래프라 더하는 자리가 늘 더 많고, 그래서 떨림도 늘 더 크다. 계수를 새로 짓지 않았다(문턱 = 잰 바닥 · 새 수 0).

### ①-5 새 ⑦⑧ — 무엇을 보나

| 절 | 조건(전부 참이어야 초록) | 이빨 |
|---|---|---|
| ⑦ | legacy·phrase 기록 **정확히 같다** · preview 9판의 native `Math.random` 호출 **0** · 미끼 **다른 씨**(`seed 20260925` · ⑪ 의 씨)와 **다른 판**(legacy↔phrase)의 RMS 차 **> 바닥** | 옛 ⑦ 이 파형 허용치로 지키려던 것(주석 *"do not retain native Math.random()"*)을 **센다**(정확한 자). 바닥이 미끼만큼 부풀면(같은 판이 같지 않으면) 미끼가 못 넘어 빨갛다 |
| ⑧ | 악보(feature-off score) 같다 · 반주 기록 같다 · 반주 차 **≤ 바닥** · 미끼 **반주 다른 씨**와 **대금 켠 판**(반주 phrase ↔ phrase)의 차 **> 바닥** | 반주가 바닥 위로 달라지면 빨갛다 · 바닥이 부풀어 ⑧ 이 느슨해지면 ⑧ 의 미끼가 못 넘어 **같이** 빨갛다 |

* Δmax 는 **적기만** 한다 — 한 표본의 꼬리라 같은 판 두 번에서도 28배 흔들린다(phrase 1.3e-5~3.7e-4). 판정은 RMS 로만 한다(T386 자 ② 와 같은 통계).
* 렌더는 7 → **9판**(미끼 둘) · 하네스 9.1 → 9.7초.
* **20번 돌림**: 20/20 **12/0** · 바닥(문턱) 2.98e-6 ~ 7.59e-6 · 반주 차 1.61e-6 ~ 1.71e-6 · 여유 **+4.9 ~ +13.4 dB** · 미끼 ↔ 문턱 ≥ **+80.3 dB**.

**자명 통과 반례 — `--selftest`**(주입마다 **기대한 절만** 빨개야 PASS · 음량 .01 은 ⑩⑪ 의 값 · 기록엔 master 음량이 안 들어가 파형 자만 문다):

| 주입 | 빨강 | 까닭(그 판의 줄) |
|---|---|---|
| `random` — 두 번째 legacy 판 안에서 `Math.random` 한 번 | **⑦ 만** | `Math.random 1` · 기록 같다 · 미끼는 문다 — 카운터만 물었다 |
| `floor` — 두 번째 phrase 판을 음량 .01 로 | **⑦ ⑧** | 바닥 1.31e-1 로 부풂 → ⑦ 미끼 7.90e-2·1.28e-1 **못 문다** · ⑧ 반주 차 1.63e-6 ≤ 1.31e-1 로 통과하지만 ⑧ 미끼 8.00e-2·1.18e-1 **못 문다** → 빨강(⑧ 이 부푼 바닥 뒤에 못 숨는다) |
| `acc` — phrase 반주 판을 음량 .01 로 | **⑧ 만** | 반주 차 6.46e-2 > 문턱 6.52e-6 · 기록 같다 — 파형 자만 물었다 |

### ①-6 한계(못 보는 것 · 지어서 메우지 않는다)

* ⑦ 은 바닥의 **크기를 수로 묶지 않는다** — 묶는 순간 그 수가 지어낸 수다(옛 1.5e-6 이 그 꼴). 대신 줄마다 바닥을 dB 로 **적고**, 미끼와의 거리로 자가 살아 있음을 보인다. `Math.random` 말고 다른 새는 소스(예: 시계)가 preview 에 생기면 ⑦ 의 파형 이빨은 **미끼 수준**에서만 문다 — 지금 preview 경로에 시계 읽기는 0이다(`_offlineStart` 가 렌더 전에 전부 예약 · `currentTime` 0).
* ⑧ 의 해상도는 바닥이다 — 반주가 −87 dB(바닥) 밑에서 달라지면 못 본다(듣기엔 무의미한 크기).
* 결정적 엔진(바닥 0)에서는 ⑧ 이 반주 두 판의 **비트 동일**을 요구한다. 이 상자와 GPT 상자는 둘 다 바닥이 0 보다 크다(GPT 는 1.01e-6).

## §② `janggu_gung` — 사실 정정(값 0)

| 자리 | 전 | 후 |
|---|---|---|
| `render-meta.json` `source.janggu_gung` | `"합성"` | `"샘플(궁편 6조각 · 84타) — village_day_trad·village_day_jeongak 만 · 11곡은 미상(07-30 판 · 코드 없음)"` |
| `render-meta.json` 메모 | — | `_T411` 한 칸(근거 · 다섯 칸 회부) — `tracks`·다른 `source` 칸 **무변** |
| `README-BGM.md` "아직 합성음인 악기" 목록 줄 | 거문고 · 단소 · 피리 · **장구** · 북 · 징 · 꽹과리 · 박 | 거문고 · 단소 · 피리 · **장구 채편** · 북 · 징 · 꽹과리 · 박 + ⚠문단 하나(궁편은 합성이 아니다 · 11곡 미상 · 같은 증거가 선 다섯 칸) |
| `test-audio ⑨l` 대응 | 궁편·채편 → 둘 다 '장구' | 궁편 → '장구 궁편' · 채편 → '장구 채편'(주석 ★[T411]) — 절 수 무변(160) |

* **근거는 T391 이 세웠다**(바이트 동일 재현): 뱅크에 궁편 실음원 **6조각**(덩·쿵 × 강·중·약) · `village_day_trad` 궁편 84타(쿵 63 · 덩 21) · `village_day_jeongak` 궁편 84타(쿵 중 50 · 덩 강 21 · 쿵 강 13) · 장구 뱅크 없이는 `compose.py:123` TypeError(합성 장구로는 두 곡이 **안 구워진다**). 11곡은 07-30 판 코드가 없어(#72) **못 잰다** — `render2.ROOTS` 에 `samples_janggu` 가 있는 것은 **의도**지 잰 것이 아니라 '샘플'로 안 적었다.
* T372 가 "추론이 안 서서 안 고친"(족보 297) 그 칸이다 — 이제 실측이라 고쳤다. `daegeum` 칸("… 12곡에서는 합성")과 같은 꼴로, 곡마다 다른 두 사실을 한 줄에 적었다.
* ★⑨l 을 **한쪽만** 고치면 빨개진다(T391 이 짚은 그 맞댐). 셋을 한 판에서 같이 움직였고, 되돌림 미끼 넷으로 이빨을 확인했다(`⑨l` 의 술어를 같은 파일에 대 봄):

| 되돌림 | ⑨l 이 무는 칸 |
|---|---|
| 지금 | 0 |
| ⓐ 표만 '합성' 으로 되돌림 | `janggu_gung` |
| ⓑ README 만 옛 줄('장구') | `janggu_chae` |
| ⓒ README 에 '장구 궁편' 을 넣음 | `janggu_gung` |
| ⓓ 옛 ⑨l(둘 다 '장구')로 새 표를 잼 | `janggu_gung` — 옛 대응으로는 궁편·채편을 못 가른다 |

* ⚠**같은 증거가 선 칸 다섯은 안 고쳤다**(카드 범위 — 궁편 한 칸): `janggu_chae`(trad 84타) · `geomungo`(36) · `danso`(11) · `piri`(8) 의 '합성' · `daegeum` 의 '12곡에서는 합성'(trad 산조대금 79). 그 두 곡에서는 전부 샘플이다(T391). ⇒ 회부.
* 제품은 `render-meta.json` 을 **안 읽는다**(`public/client`·`bgm-loops.js`·`server/` grep 0) — 읽는 것은 브금판·소리판(둘 다 `tracks` 만)과 하네스뿐이다.

## §③ tools 24 — 진입점 한 번씩

규칙: README 가 처음 시키는 명령이 진입점이다. 레포 clone(67a86098) 안에서 **`timeout 300 unshare -n`**(네트워크 네임스페이스 · 5분)으로 **한 번** 돌렸다. 입력은 레포 안 것만 썼다 — README 가 레포 밖(`_bgm_rnd`·맥 경로·자리표시)을 가리키면 **그대로** 넣어 어떻게 멈추는지 봤다. 레포 안에서만 도는 모드(`--dry-run`·`--check`·레포 계획)가 있으면 그것도 한 번 돌렸다. 설치·내려받기는 0이다. 실제 레포의 `tools/` 는 한 번도 안 돌렸다(diff 0).
자기 시험은 폴더마다 `unittest discover` 한 번씩이다. ⚠이 상자는 전역 git 설정이 커밋 서명을 켜 두어서, 네트워크 0 안에서는 서명기(127.0.0.1)가 안 닿아 git 커밋을 만드는 픽스처 다섯 폴더가 에러를 낸다 — 서명을 끈 판(`GIT_CONFIG_*` 환경 · 파일 무변)으로 쟀다.

| # | 폴더 | 진입점 | 한 번 실행 — 결과 한 줄 | 없는 것 | 자기 시험 | 권고 |
|---|---|---|---|---|---|---|
| 1 | aihub-paired-ingest | `validate_bundle.py` | 안 돈다 — *"--bundle-manifest must name one readable local JSON manifest"* | AI Hub 로컬 번들(#64 ② 승인 전) | 5/5 | **참고** — #64 ② 가 풀리면 쓰는 반입 게이트 · `expressive-synthesis` 준비 판정이 이 파일을 센다 |
| 2 | bgm-samples | `restore_backups.py --verify` | 안 돈다 — *"source directory does not exist: /Users/…/bgm"* · 레포 기본 경로로는 *"missing required backup: bk_daegeum_a.zip"* | 맥의 `bk_daegeum_a/b.zip`·`bk_jdae_aa/ab.part`(해시가 코드에 박힘) | 시험 없음 | **참고** — 대금 백업 4묶음만 다룬다(12묶음 중) · T391 은 12묶음을 손으로 격리 복원했다 |
| 3 | daegeum-transitions | `native_wav.py`(README 첫 명령) | 안 돈다 — ⚠**exit 0 · 출력 `[]`**(없는 경로를 wav 가 아니라며 조용히 건넌다 · 거짓 성공). `build.py`·`target_audition.py`·`exhaustive_…py --dry-run` 은 계약 JSON 만 찍는다 · `fetch_ngc_…` plan-only 는 **네트워크**(국악원 목록)라 멈춤 | 원음 WAV(`$GUGAK_ROOT`) · `_bgm_rnd` 묶음 | 61/61 | **참고** — `scripts/test-daegeum-transition-bank.py` 가 이 폴더의 스키마·빌더 소스를 **읽는다**(옮기면 그 하네스가 깨진다) · `native_wav` 를 4폴더가 import |
| 4 | daegeum-vibrato-reference | `reference_yoseong.py` | 안 돈다 — *"bundle path is not a directory"* | 192 source 특징 캐시(`_bgm_rnd/daegeum-transition-bank-ngc-…`) | 6/6 | **참고** — 커밋된 산출 JSON 을 `score-expression` 컴파일러(:84)·계획 셋이 읽는다 |
| 5 | ddsp-gugak-public-runtime | `…runtime.py --check` | 안 돈다 — **체크포인트 없음** · *"DDSP-Gugak source root must be an explicit directory"* | DDSP-Gugak·ddsp-pytorch checkout · `daegeum.pth`(17.4 MB) · `--execute` 엔 torch | 30/30 | **참고** — 라이선스 미확인(T386 회부 5) · `score-expression` 시험이 이 런타임을 import |
| 6 | ddsp(T353) | `train.sh daegeum ~/소리/대금_단선율/` | 안 돈다 — venv 를 **폴더 안에** 만들고(`tools/ddsp/venv/` · ignored) pip 가 PyPI 로 나가려다 *"No matching distribution found for torch==2.4.1"* | torch·librosa·soundfile(pip·네트워크) · wav 폴더 · GPU | 시험 없음 | **버림** — `train.py` 는 f0 를 **392 Hz 로 고정**하고 배음 분포·노이즈만 맞춘다(조건부 모델이 아니다) · `phrase.py` 백엔드 B 가 부르는 `ddsp_backend` 모듈은 **어디에도 없다** · GPT `expressive-synthesis` 가 "archived ddsp" 로 부르며 안 쓴다고 시험으로 못 박았다 |
| 7 | expressive-synthesis | `renderer_readiness.py` | **돈다(판정만)** — 레포를 읽고 *"ready": false* · 막힌 것 넷(체크포인트 없음 · 짝 말뭉치 없음 · torch+CUDA 없음 · nvidia-smi 없음). README 꼴(보고 폴더 지정)은 부모 폴더가 없어 거절 | 체크포인트 · 짝 데이터 · 4060(#64 ③) | 21/21 | **참고** — BGM_RND_08(정본 후보)의 틀 · ⚠`render_synthetic_preview.py` 는 16초 넘는 제어를 거절한다(34.8초 계획 → *"frame_times_seconds is not aligned"* · 6.72초는 돈다) |
| 8 | full-ari-yoseong-audition-pack | `make_…spec.py` → `build_…py` | 안 돈다 — *"BLOCKED: output parent directory must already exist"* | `_bgm_rnd` B0/B1 렌더 보고·WAV · ★기준 커밋 **`03a54f5c` 가 이 레포에 없다**(스쿼시 착지 `462b4acc`) — 입력이 다 와도 조립 불가 | 4/4 | **버림** |
| 9 | full-ari-reference-contour-audition-pack | 〃 | 안 돈다 — 같은 BLOCKED 두 줄 | B1/B2-R 렌더 보고 · `03a54f5c` | 7/7 | **버림** |
| 10 | full-ari-reference-depth-audition-pack | 〃 | 안 돈다 — 같다 | + B2-Rd · `03a54f5c` | 7/7 | **버림** |
| 11 | full-ari-reference-fade-audition-pack | 〃 | 안 돈다 — 같다 | + B2-Rdf · `03a54f5c` | 8/8 | **버림** — 네 묶음이 `sys.path` 로 줄줄이 import(fade→depth→contour→yoseong) ⇒ **함께** 옮긴다 · #66 청취본은 맥 `_bgm_rnd` 에 이미 있다 |
| 12 | gugak-corpus-inventory | `inventory.py` | **돈다** — 레포 `public/assets/audio`·`sfx` 를 넣으면 118파일 sha256 · 중복 0 · wav 0 이라 전부 "uninspected". README 꼴(`/explicit/audio-root`)은 *"audio root is unavailable"* | 국악 원음 말뭉치 | 9/9 | **참고** — #64 뱅크 작업의 목록기 |
| 13 | midi-ddsp-audition-pack | `build_audition_pack.py` | 안 돈다 — *"--report-dir must name an existing successful runtime report directory"* | 런타임 보고·A/B WAV(곧 **체크포인트 없음**) | 2/2 | **버림** |
| 14 | midi-ddsp-daegeum-adapter | `adapter.py`(192 파일 blocked 판) | 안 돈다 — ⚠**exit 0 · "blocked"**(README 가 기대하는 답과 같은 글자지만 까닭은 *"controlled NGC manifest is absent"* — 메타데이터 0장 읽음). 합성 픽스처로는 돈다(4음 계약 확인) | NGC 192 목록 · 전이 묶음 | **5/6** — 빨간 절이 레포 밖 `_bgm_rnd` 파일이 **있다**고 단언 · 다른 한 절은 파일이 없어 **공짜로** 초록 | **버림** |
| 15 | midi-ddsp-daegeum-timbre | `build_timbre_audition.py` | 안 돈다 — *"output parent directory does not exist"*(출력 폴더를 만들어 줘도 *"missing NGC corpus manifest"*) | 권리 감사 · NGC 목록+192 WAV · 런타임 B WAV | 2/2 | **버림** |
| 16 | midi-ddsp-runtime | `run_official_flute_rnd.py` | 안 돈다 — **체크포인트 없음**(점수 어댑터 산출을 넣어 줘도 *"official weight ZIP is absent"*) · 그 뒤엔 TF·ddsp·midi_ddsp 도 없다 · `--execute` 없이 부르면 traceback | URMP 가중치 zip(48.4 MB) · TF 2.7 환경 | 5/5 | **버림** |
| 17 | midi-ddsp-score-adapter | `adapter.py --plan <레포 계획>` | **돈다** — *"wrote authorial_score.mid + midi_ddsp_authorial_bridge.json; notes=6"* · 두 번 돌려 바이트 같다(T386 "못 돌림" 정정) | — | 4/4 | **버림** — 줄기의 머리(돌지만 받는 쪽이 전부 체크포인트 없음) |
| 18 | midi-ddsp-transfer-train | `transfer_readiness.py --dry-run` | **돈다(판정만)** — 레포 안 설정·잠금 파일은 통과 · *"ready": false* 막힌 것 넷(py3.8 환경 · nvidia-smi · 가중치 zip · 승인 데이터). `cpu_synthetic_smoke.py` 는 `passed: true`(산수만) | WSL py3.8·TF 2.7 · 4060 · 가중치 · 데이터 | 7/7 | **버림** |
| 19 | model-provenance | `midi_ddsp_official.py` | 안 돈다 — **체크포인트 없음** · *"archive is unavailable"* | URMP 가중치 zip | 3/3 | **버림** — MIDI-DDSP 가중치 전용(권리 "not proven" 문단은 T386 이 인용해 두었다) |
| 20 | ngc-rights-audit | `audit.py` | 안 돈다 — *"manifest does not exist"*(그 앞 단계에서 멈춤 · 목록을 줘도 NGC·KOGL 조회는 네트워크라 막힌다 — *"Network is unreachable"*) | NGC 192 목록 · 네트워크 | 3/3 | **참고** — 권리 판단 틀(1유형 비ML ↔ AI 학습 차단)이 값지다 · 판정은 재민 |
| 21 | ngc-source-receipts | `validate_receipt.py` | 안 돈다 — *"receipt file does not exist"* | 국악원 영수증 JSON + WAV | 3/3 | **참고** — ⚠`api.gugak.go.kr` 호스트만 받는다(확장 목록은 `www.gugak.go.kr` — 두 국악원 도구가 안 맞물린다) |
| 22 | score-expression | `compile_expression.py`(README 첫 블록은 `build_full_ari_plans.py`) | **돈다** — 레포 계획을 넣으면 NPZ(배열 19) + manifest v2(B2-Rdf 34.80초 · 3,481프레임). `build_full_ari_plans.py --check` 0 · `--check` 없이 돌리면 계획 5장을 다시 쓰는데 **바이트 같다**(git diff 0) | — | 26/26 | **쓴다** — 레포 안에서 결정적으로 도는 유일한 표현 층 · 다섯 폴더가 기댄다 · R&D-02(정본 후보)의 계약 |
| 23 | source-led-stem-mixer | `source_led_stem_mixer.py` | 안 돈다 — *"missing lead manifest"* | `span_audition.json`(전이 도구 산출) + 48 kHz 원음 스템 | 6/6 | **버림** — 산출 소비자 0 · 입력은 R&D-07 이 "source-faithful 악구 없다"로 닫은 방향의 것 |
| 24 | yoseong-audition-pack | `make_blocked_b2_spec.py` | 안 돈다 — *"BLOCKED: spec output parent must already exist"* | `_bgm_rnd` B0/B1 · **`03a54f5c` 없음** | 5/5 | **버림** — 6.48초 발췌판(16마디 판 8이 대신한다 · import 0) |

**합**: 돈다 **5**(그중 판정만 2) · 안 돈다 **19**(체크포인트 없음 3 · ⚠exit 0 인데 입력 0 은 2) · 5분 끊음 **0** · 가장 긴 실행 18.9초(`train.sh` 의 pip 시도) · 네트워크를 두드린 것 2(`train.sh`→PyPI · `fetch_ngc` plan-only→국악원) — 둘 다 네임스페이스에서 끊겼다 · 레포 안에 쓴 것 3(`tools/ddsp/venv/` · 어댑터 둘의 `_bgm_rnd/` — 전부 ignored 자리).
자기 시험 **230 중 229**(22 폴더 · 시험 없는 폴더 2).

### ③-2 T386 표와 맞대기

| T386 이 적은 것 | 실행으로 |
|---|---|
| **쓰임 0/23** — `public/`·`server/`·`scripts/`·`sim/`·`.github/` 어디서도 안 부른다 | **실행은 0 이 맞다**. 다만 레포 안에서 읽는 것이 둘 있다: `scripts/test-daegeum-transition-bank.py` 가 `tools/daegeum-transitions` 의 스키마·`build.py` 소스를 읽고, `phrase.py:ddsp_model_dir` 가 `tools/ddsp/models/` 경로를 짓는다 |
| **실데이터 경로 23 전부 못 돌렸다** | 19 는 맞다. **5 는 돈다** — `midi-ddsp-score-adapter`(T386 "못 돌림" → 정정) · `score-expression`(컴파일까지 · "렌더는 못 돌림"은 맞다) · `gugak-corpus-inventory` · 준비 판정 둘 |
| 자기 시험 37 파일 중 36 초록 · `midi-ddsp-daegeum-adapter` 5/6 | 같다(시험 단위로 229/230). 빨간 절의 까닭은 **레포 밖 파일이 있다는 단언**이다 — 깨끗한 checkout 에선 어디서나 빨갛다 |
| `bgm-samples` 시험 없음 | 같다(+ `tools/ddsp` 도 시험 없음) |

### ③-3 이동 제안 — 버림 14 → `tools/_rnd_archive/` (**표만** · 이동은 PM · 이 카드 `tools/` diff 0)

| 묶음 | 폴더 | 함께 옮길 까닭 | 스크래치에서 옮겨 본 결과(시험) |
|---|---|---|---|
| MIDI-DDSP 줄기 7 | `midi-ddsp-score-adapter` → `midi-ddsp-runtime` → `midi-ddsp-audition-pack` · `midi-ddsp-daegeum-timbre` · `midi-ddsp-daegeum-adapter` · `midi-ddsp-transfer-train` · `model-provenance` | `설계/BGM_RND_08`: *"MIDI-DDSP 공개 구현은 archived 상태이므로 그대로 제품 의존성으로 채택하지 않고 … 원칙과 eval 설계만 참고한다"* · 가중치 권리 not proven · 뒤 R&D(09-25)는 DDSP-Gugak 만 쓴다 | runtime 5/5 · audition 2/2 · transfer 7/7 · provenance 3/3 **무변** · adapter 5/6 **무변**(원래 빨강) · ⚠score-adapter **import 에러**(`compile_expression` — 옆 `score-expression` 을 `parents[1]` 로 찾는다) · ⚠timbre **1 에러**(`native_wav` 경로) |
| 청취 묶음 줄기 5 | `yoseong-audition-pack` · `full-ari-yoseong-…` → `…contour…` → `…depth…` → `…fade…` | 기준 커밋 `03a54f5c` 가 레포에 없어 **이 레포에선 영영 조립 불가** · 청취본(#66)은 맥 `_bgm_rnd` 에 이미 있다 · 네 판은 `sys.path` 로 서로 import | yoseong 5/5 · full-ari-yoseong 4/4 **무변** · ⚠contour 7 · depth 7 · fade 8 **에러**(`daegeum-vibrato-reference` JSON 을 `parents[2]/tools/…` 로 찾아 `tools/tools/…` 가 된다) |
| 홀로 1 | `source-led-stem-mixer` | 산출 소비자 0 · R&D-07 음성 결과 | ⚠**import 에러**(`native_wav` — 옆 `daegeum-transitions`) |
| 홀로 1 | `ddsp`(T353 — 내 것) | 조건부 모델이 아니다(f0 392 Hz 고정) · 백엔드 B 연결 모듈 없음 · `expressive-synthesis` 가 "archived" 로 부른다 | 시험 없음 · 제품 쪽 흔적 `phrase.py` **3줄**(:167 주석 · :174 주석 · :182 오류 문구) — `ddsp_model_dir` 가 옛 자리를 보고 None 을 돌려준다(모델 0 이라 **동작 무변**) |

* **남는 10 폴더**(쓴다 1 · 참고 9)는 옮긴 뒤에도 자기 시험 **전부 무변**이고, `scripts/test-daegeum-transition-bank.py` 도 **8/0** 이다. 남은 코드에서 옮긴 자리를 부르는 경로 문자열은 `phrase.py` 3줄뿐이다(grep `tools/[!_]*`·`scripts`·`public`·`server`·`sim`·`.github`).
* ⚠옮긴 쪽 6폴더의 시험은 옆 폴더를 상대 경로로 찾아서 깨진다. 고르는 길은 PM 몫이다 — ⓐ 보관소 시험은 안 돌린다(보관 = 안 돌보는 자리) ⓑ 한 줄씩 경로를 고친다(`parents[1]` → `parents[2]`) ⓒ 그 여섯은 안 옮긴다.

### ③-4 돌리다 드러난 결함 — 고치지 않았다(`tools/` diff 0 · 회부)

1. `daegeum-transitions/native_wav.py` — 없는 경로를 조용히 건너 **exit 0 · `[]`**(오타 난 `$GUGAK_ROOT` 가 "wav 0개 · 성공"이 된다).
2. `midi-ddsp-daegeum-adapter` — 입력이 없어도 **exit 0 "blocked"**(진짜 192 파일일 때와 같은 글자) · 시험 하나는 레포 밖 파일을 단언하고 하나는 공짜 초록.
3. 청취 묶음 다섯 — 기준 커밋 `03a54f5c` 가 레포에 없다(시험은 제 픽스처 커밋을 써서 못 잡는다).
4. `expressive-synthesis/render_synthetic_preview.py` — 16초 넘는 제어를 거절(float32 시각 ↔ 허용 1e-6 · 34.8초 계획 거절 · 시험은 0.5초 계획뿐).
5. `phrase.py` 백엔드 B ↔ `tools/ddsp/export.py` — `ddsp_backend` 모듈을 놓는다고 적혀 있는데 export 는 `model.pt`·`meta.json` 만 쓴다(학습이 끝나도 B 는 못 붙는다).
6. `tools/ddsp/train.sh` — 레포 안(`tools/ddsp/venv/`)에 venv 를 만들고 네트워크로 pip 를 부른다(설치 게이트 없음 — GPT `expressive-synthesis` 런처는 `--allow-install` 게이트가 있다).
7. `ngc-source-receipts` ↔ `ngc-rights-audit`·`fetch_ngc` — 받는 호스트가 다르다(api ↔ www).
8. `midi-ddsp-runtime` — `--execute` 없이 부르면 거절이 `try` 밖이라 traceback.

## §카드 전제 대조

| 카드가 적은 것 | 실측 |
|---|---|
| "T391 §ⓐ 결정론 표(… Chromium OAC 같은 판 두 번 −91.2dB)" | −91.2 dB 는 **T386 §ⓐ-2 자 ②**(40초 · 12조합 중앙값)의 수다. T391 §ⓐ 표는 Python 굽기(numpy·패킷) 결정론이다. 이 카드의 4마디 preview 바닥은 legacy −92.9 dB(중앙) · phrase −93.1~−84.8 dB — 같은 띠 |
| "그 바닥 × 있는 계수 0(= 바닥 자체)" | 그대로 — 문턱 = 잰 바닥 · 계수 0 · 새 수 0. 다만 **어느 바닥**이냐가 자를 가른다: 반주 자기 바닥이면 동전(50.0%), ⑦ 의 바닥이면 0/3,686,400(§①-4) |
| "정본 표(T353 `설계/` BGM 문서 · `render-meta.json` 그 칸)" | `설계/` 에 BGM 정본 문서는 **없다**(T386 전수 grep 0 · 이번에도 — BGM 파일은 GPT `BGM_RND_*` 11장뿐이고 `janggu_gung` 0). 그 칸을 맞대는 정본은 `render-meta.json` `source` + `README-BGM.md` 목록 줄(⑨l) — 둘 다 고쳤다 |
| "`test-audio` 160" | 잰 베이스 `67a8609` 160 → 뒤 160 · 리베이스 뒤 새 베이스 `057d40c` **170**(T412 가 +10) → 뒤 **170**(이 카드는 절 수 무변) |
| "`tools/` 24 폴더" | GPT 23 + T353 `tools/ddsp` 1(T386 과 같다) |
| "10/2 → 12/0" | 베이스에서 10/2 재현(Δmax 2.46e-5 / 1.70e-5 · RMS 2.30e-6 / 3.06e-6) → 12/0 |

## §검증

* `e2e-bgm-phrase` **12/0** · 20번 20/20 · `--selftest` PASS(주입 3/3 · 각자 기대한 절만) — 베이스 10/2.
* `test-audio` **170/0**(새 베이스 170 · ⑨k·⑨l·⑨l-2·⑨m 초록 · 잰 베이스에서도 160/0) · `test-bgm-phrase` 16/0 · `test-assets-audit` PASS · `--selftest` PASS(3건) · `test-render-score-articulation` 14/0 · `test-client-globals` 26/0 · `test-harness-lint` **61/0**(새 판정 자리 그림문자 0).
* 접점: `e2e-audio-probe` **71/0**(새 베이스 71 · 잰 베이스 61/0 · 브금판 행 = `tracks` 13 — `source` 무관) · `test-daegeum-transition-bank.py` **커밋 전 7/1 → 커밋 뒤 8/0** — 그 ⑦ 이 `git diff HEAD -- render-meta.json` 으로 **작업트리**만 보기 때문이다(`source` 한 칸 · 제품은 이 파일을 안 읽는다).
* 파일: `bgm.js`·`bgm-loops.js`·13곡 `.ogg`/`.m4a` diff **0** · `tools/` diff **0** · `sfx/**` 무접촉 · `render-meta.json` 은 `source.janggu_gung` 한 칸 + 메모 `_T411`(`tracks` 무변 · JSON 파싱 확인).
* tools 실행은 전부 레포 **clone** 다섯 개(스크래치 · `67a8609`)에서 했다 — 실제 레포 `tools/` 는 한 번도 실행하지 않았고 끝에 clone 을 깨끗이 되돌렸다. `tools/` 는 `67a8609` ↔ `057d40c` 사이 diff 0 이라 표는 새 베이스에서도 같다.
* §2 ① 3시드 A/B — diff ∩ 랩 적재 목록 = ∅(하네스·BGM 메타·문서만) ⇒ 해당 없음.

## §재현

```sh
# ① 바닥·자 셋·더하는 항 실험 (러너 밖 · 판마다 16번 ≈ 46초)
node scripts/t411-oac-floor.js --n 16 [--json <경로>]
# ① 하네스 · 자명 통과 반례
node scripts/e2e-bgm-phrase.js ; node scripts/e2e-bgm-phrase.js --selftest
# ② ⑨l
node scripts/test-audio.js | grep -E "⑨k|⑨l|⑨m"
# ③ 레포 clone 에서(실제 레포 무접촉) — 폴더마다 README 첫 명령을 이 틀로
git clone -q --local <레포> $R && cd $R
PYTHONDONTWRITEBYTECODE=1 timeout 300 unshare -n python3 tools/<폴더>/<진입점> <README 인자>
#   예: score-expression    compile_expression.py --plan tools/score-expression/plans/ari_full_16bar_b2rdf_reference_shape_depth_matched_long_fade_unreviewed_r1.json --output-dir $O --confirm-rnd-only
#       midi-ddsp-score-adapter adapter.py --plan tools/score-expression/plans/ari_source_led_response_r1.json --output-dir $O
#       gugak-corpus-inventory  inventory.py --root public/assets/audio --root-label repo-bgm --root public/assets/sfx --root-label repo-sfx --output $O/inv.json
#       expressive-synthesis    renderer_readiness.py            (인자 없이 — 판정 JSON 이 표준출력으로)
#       midi-ddsp-transfer-train transfer_readiness.py --dry-run <README 자리표시 인자>
# ③ 자기 시험 (서명 끔 — 이 상자 전역 설정 때문)
env GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false \
  timeout 300 unshare -n python3 -m unittest discover -s tools/<폴더> -p 'test_*.py'
# ③ 이동 모의 — clone 에서 버림 14 를 git mv tools/<폴더> tools/_rnd_archive/<폴더> 한 뒤 같은 자기 시험 + scripts/test-daegeum-transition-bank.py
```

## §회부

1. **`tools/` 이동**(PM) — §③-3 표 · 버림 14 · 옮긴 쪽 시험 여섯의 길(ⓐ/ⓑ/ⓒ) · `phrase.py` 3줄.
2. **`source` 다섯 칸**(PM · 재민) — `janggu_chae`·`geomungo`·`danso`·`piri` '합성' · `daegeum` '12곡에서는 합성' — 두 곡(trad·jeongak)에서는 샘플(T391 바이트 동일). 고치면 `README-BGM.md` 목록 줄과 ⑨l 대응도 같은 판에서.
3. **tools 결함 여덟**(GPT 다음 BGM 카드 · PM) — §③-4. 특히 exit 0 거짓 성공 둘과 `03a54f5c`.
4. **#72 11곡**(재민 · 브금판) — 무변.
5. **#66·#70**(재민) — 무변. #66 청취본은 맥 `_bgm_rnd` 에 있고, 그걸 만든 묶음 도구는 이 레포에서 다시 못 돈다(§③ 8~11 · 24).
6. **GPT 다음 BGM 카드**(PM) — 위 표 뒤에만.
7. **AI Hub·4060**(#64 ②③) — `aihub-paired-ingest`·`expressive-synthesis` 가 그 문 앞에 서 있다(둘 다 참고).
