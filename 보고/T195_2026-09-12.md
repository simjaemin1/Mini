# T195 — 도구 마모 배수의 **정본 자리** + 서버 ×0.9 팔 (세션6 · 2026-09-12)

> 베이스 **`cb0522b7`**(리베이스 1회 · 충돌 0 — 처음엔 `00f376f1` 였는데 T188 이 먼저 앉았다) ·
> 헤드 **`eb52c536`**(+ 이 문서 커밋) · 가지 `batch/tool-wear-port-0912` — **가지만**(main 은 #12 값 판정 뒤 PM) · force 0.
> 값이 오기 전에 할 수 있는 둘: **자리를 만들고**(①), **비어 있던 눈금 하나를 재운다**(②).
> 채택값은 **1**(= 현행) — 이 카드는 값을 안 고른다.

---

## 1. §0-ⓐ 서버가 `world` 를 세우는 자리

| 무엇 | 어디 |
|---|---|
| 서버 world 조립 | `server/villages.js:2509` — `econV2.createWorldV2({ seed, villageCount, picker:'rational', infoRange:5000, raidPer100:0.005 })` |
| 그 자리에서 여는 문 | `require('./trees').attachToWorld(world)` (T135) — **이것 하나뿐이었다** |
| `happyWorkW`(T157) | 이 파일에 **없다**. 랩만 준다(그래서 서버는 비트 동일) |
| `allocFn`·`returnPullFn`·`stoneBudgetFn`·`toolWearMul` | 전부 랩/계측기만 열었다 — **서버 세계엔 자리가 없었다** |

⇒ T191 은 배수를 **계측기 한 줄**(`t17-metrics.js`)로 심었다. 서버가 못 쓰는 값이었다.
**자리를 `attachToWorld` 바로 아래 한 줄로 만든다** — 그 자리가 "econ 에 바깥 값을 건네는" 자리다.

```js
// sim/economy-sim.js — 감가율 두 수 바로 아래(그 하향 문장 옆)
const TOOL_WEAR_MUL = (() => {
  const v = parseFloat(process.env.T195_TOOL_WEAR || '');
  return (Number.isFinite(v) && v > 0) ? v : 1;        // ★기본 1 = 현행 채택값
})();

// server/villages.js — attachToWorld 바로 아래 한 줄
if (econ.TOOL_WEAR_MUL !== 1) world.toolWearMul = econ.TOOL_WEAR_MUL;   // ★1 이면 문을 아예 안 연다
```
**새 수 0**(감가율 `0.02`/`0.01` 무변) · **사본 0**(3사본은 빌드) · `!== 1` 가드는 랩 `twInstallHook` 과 **같은 문법**.
★[T191 ㉢] 하향 문장 옆에 한 줄을 적었다 — *"이 상수는 **상한만** 말했다 · **하한**은 T191 표"*.

## 2. §0-ⓑ 되돌림 — **미설정 3시드 비트 동일** · 정본 주입 = 랩 주입

| 시드 | 미설정 `cmp`(기준선 셋째 판) |
|---|---|
| 1020 | **비트 동일** |
| 7 | **비트 동일** |
| 42 | **비트 동일** |

`T195_TOOL_WEAR` 를 안 주면 `world.toolWearMul` 이 **아예 안 붙는다**(하네스 ⑱ · 자식 프로세스로 확인).

| 팔(자식 프로세스 · 4마을 300일) | `world.toolWearMul` | 지문 |
|---|---|---|
| 정본 · env 미설정 | **none** | (기준) |
| 정본 · `T195_TOOL_WEAR=0.9` | 0.9 | `b192bd78f2f2` |
| **랩 문법 직접 주입** `m=0.9` | 0.9 | **`b192bd78f2f2`** |

⇒ **정본 주입과 랩 주입이 한 바이트도 안 다르다** — 문이 하나라는 증거다(사본 0).

---

## 3. §0-ⓒ 서버 **×0.9** 팔 — 3시드 800일 51마을 (T191 §2 의 ×0.75 열과 나란히)

### 여덟 수 + 생곡 + 밀도

| 시드 | 팔 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 | ㉮ | ㉯ |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1020 | 미설정 | 6,889 | 0/51 | 1,012 | 5,509 | 2,539 | 11,892 | 804 | 37,240 | 1.93 | 2.02 |
| 1020 | **×0.9** | **7,183** (+4.3%) | 0/51 | 1,127 | 6,756 | 2,524 | 12,678 | 872 | 38,896 | 1.93 | 2.02 |
| 1020 | (×0.75 · T191) | 8,014 (+16.3%) | 0/51 | 1,326 | 8,829 | 2,696 | 16,207 | 1,165 | 61,790 | 1.84 | 1.92 |
| 7 | 미설정 | 7,148 | 0/51 | 1,030 | 7,830 | 2,714 | 12,039 | 551 | 37,780 | 1.88 | 1.97 |
| 7 | **×0.9** | **7,503** (+5.0%) | 0/51 | 1,260 | 7,775 | 2,665 | 14,304 | **1,482** | 45,987 | 1.89 | 1.98 |
| 7 | (×0.75 · T191) | 7,961 (+11.4%) | 0/51 | 1,247 | 9,304 | 2,585 | 15,247 | 701 | 45,540 | — | — |
| 42 | 미설정 | 6,259 | 0/51 | 978 | 5,946 | 2,648 | 11,013 | 663 | 38,902 | 1.95 | 2.04 |
| 42 | **×0.9** | **6,387** (+2.0%) | 0/51 | 947 | 6,223 | **2,096** ⚠ | 11,259 | 807 | 39,211 | **2.03** | **2.14** |
| 42 | (×0.75 · T191) | 7,968 (+27.3%) | 0/51 | 1,133 | 8,198 | 2,726 | 14,755 | 1,161 | 48,054 | — | — |

### ⓛ 석재 바닥 36마을

| 시드 | 팔 | 돌<0.2(중앙) | **도구≈0 중앙/최대** | **유지율 중앙/최저** | 궤적 진입 |
|---|---|---|---|---|---|
| 1020 | 미설정 | 426 | 3 / **223** | 100% / **47%** | **1곳** |
| 1020 | **×0.9** | 460 | 4 / 247 | 99% / **75%** | **0곳** |
| 7 | 미설정 | 360 | 2 / 203 | 100% / 74% | 0곳 |
| 7 | **×0.9** | 316 | **1** / 189 | 97% / 76% | 0곳 |
| 42 | 미설정 | 400 | 20 / **308** | 100% / **56%** | 0곳 |
| 42 | **×0.9** | 460 | **2** / **120** | 100% / **78%** | 0곳 |

### ★한 줄 답

> **×0.9 서버: 인구 +4.3 / +5.0 / +2.0% · 최저 유지율 47→75 · 74→76 · 56→78% ·
> `도구≈0` 최대 223→247 · 203→189 · 308→120 · 소멸 0/51 내내 · 궤적 진입 1곳→0곳.**

**읽는 법 넷:**
1. **서버 ×0.9 는 얌전하다** — 인구 +2~5%(×0.75 는 +11~27%). 눈금이 **선형이 아니다**는 랩의 결론이
   서버에서는 **반대로** 보인다: 서버에선 ×0.9 가 작고 ×0.75 가 크다. 랩은 문턱(소멸)을, 서버는 기울기를 잰다(족보 144).
2. **그런데 꼬리는 ×0.9 로도 산다** — 가장 나쁜 마을의 유지율이 **47→75% · 56→78%** 로 오르고,
   시드 1020 의 **궤적 진입 마을이 1곳 → 0곳**이 된다. 시드 42 의 `도구≈0` 최대가 **308→120** 이다.
   ⇒ **평균은 조금, 꼬리는 크게.** 이게 재민이 값을 고를 때 볼 자리다.
3. ⚠**시드 42 만 게시가 2,648 → 2,096 으로 준다**(−21%)·밀도 ㉮ 1.95→2.03(**캐논 2~3일 안으로 들어온다**).
   도구가 살면 부족 사건이 줄어드는 방향이라 설명이 되지만, **게시는 여덟 수의 열**이다 — 값 판정에 같이 봐야 한다.
4. `돌<0.2` 는 여기서도 **방향이 안 맞는다**(426→460 · 360→316 · 400→460). 톱니(족보 150) — 이 축의 지표가 아니다.

## 4. 검증

* **미설정 3시드 800일 `cmp` 비트 동일**(§2) — 기준선 셋째 판과 한 바이트 안 다르다.
  ★**리베이스 뒤(`cb0522b7` · T188 착지본)에도 시드 1020 을 다시 재서 비트 동일을 확인했다**
  (T188 은 `trees.json.ko` 사본 삭제 — econ 여덟 수 무변). **기준선 셋째 판 여전히 유효.**
* **`test-lab-stone` — 82/0**. T195 절 둘 신설:
  ⑰ 상수·손잡이·서버 한 줄(접점 1줄 · 기본 1 · 감가율 두 수 무변 · T191 ㉢ 주석) ·
  ⑱ **자식 프로세스**(족보 ⑨): env 없으면 `toolWearMul` 이 **안 붙고**, `=0.9` 면 **랩 주입과 지문이 같다**(`b192bd78f2f2`).
  ★T180 의 ⑬ "서버는 이 문을 안 연다" 는 **"정본이 1 이 아닐 때만 연다"** 로 갱신했다(채택값 1 = 라이브 무변).
* **`sim/build-econ-bundle.js --check` — 8 일치 / 0 불일치** (3사본 규약).
* **`lab-wiring-check` — 실패 0 · 경고 0**(F·G).
* **러너 전수는 안 돌렸다**(카드 §★).
* 접점 심볼: `toolWearMul|TOOL_WEAR_MUL|T195_TOOL_WEAR|DAILY_TOOL_WEAR_PER_FARMER|DAILY_TOOL_WEAR_PER_OTHER|_stCost|toolBoostShared|STONE_NET_STOCK`

### 만진 파일

| 파일 | 무엇 |
|---|---|
| `sim/economy-sim.js` | **상수 한 줄**(`TOOL_WEAR_MUL` · env `T195_TOOL_WEAR` · 기본 1) + 수출 + T191 ㉢ 주석 |
| `server/villages.js` | **한 줄** — `attachToWorld` 아래에서 정본을 읽어 건넨다(`!== 1` 가드) |
| `sim/economy-engine.browser.js` · `lab/*.html` | 번들 재생성·재인라인(3사본 규약 · 랩 로직 무변) |
| `scripts/t17-metrics.js` | 계측기가 **서버와 같은 줄**을 쓰게(사본 0) — T191 손잡이는 명시 덮어쓰기로 뒤에 |
| `scripts/test-lab-stone.js` | ⑰⑱ 신설 · ⑬ 한 줄 갱신 |
| `보고/T195_2026-09-12.md` · `인계/회부.md` | 문서 |

## 5. 회부 (판정은 재민 · #12)

| # | 회부 |
|---|---|
| ㉠ | **값** — 서버 표가 이제 둘이다(×0.9 · ×0.75). **평균은 ×0.75 가 크고, 꼬리는 ×0.9 로도 산다**(유지율 47→75% · 궤적 1→0곳). 랩 표(T191 §3)와 같이 읽을 것 |
| ㉡ | ⚠**시드 42 의 게시 −21%**(2,648→2,096 · ㉮ 1.95→2.03) — 여덟 수의 열이 움직인다. 값 판정에 포함 |
| ㉢ | **이식 뒤 기준선 넷째 판**(값 뒤 PM 카드 · 3시드 재측정 · `인계/공통.md` 기준선 절) |
| ㉣ | `돌<0.2` 는 이 축의 지표가 아니다(톱니 · 족보 150) — 직접 지표는 `도구≈0` **최대**(서버) · **중앙**(랩) |
| ㉤ | 수출 문턱 EMA(T180 ㉠) · `FLOOR.stone` 근거 — 이월 |

## 6. 푸시

(푸시 뒤 기입)
