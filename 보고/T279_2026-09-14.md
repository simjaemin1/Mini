# T279 — `:2729` 는 구멍이 아니다(T207 이 이미 적었고, 나는 T259 에서 T207 을 잘못 읽었다) · 잔차의 주인은 **부패** · 러너 셋 중 하나가 C 땅에서 깨진다 (세션5 · 2026-09-14 · **가지만**)

> 카드 `~/Mini/지시_T279.md` · 가지 `batch/hunt-wood-server-0912` · **측정 베이스 `0e6c3512`**
> 만진 것: `scripts/t222-life800.js`(목재 장부 자리) · `scripts/t259-t17-cland.js`(대상 파일 인자화) — **제품 코드 0** ·
> 17벌 무변 · `t17-metrics`·`t176-ab` 무접촉 · **훅 안 달았다**(누락이 아니라서).
>
> **답 한 줄 — `:2729` = **문서화된 제외**(T207 이 이미 명시 · 채택 규칙 "subs 미등재만"과도 일치) ⇒ **훅 0 · 제품 0** ·
> 잔차 귀속: **활대 5.7% · 나머지 94.3% 는 `tickDecay` 과잉재고 부패**(제품 손잡이 `world._dbg.decay` 로 확인) ·
> 러너 셋 C 땅: **2/3 그대로 통과 · `test-onboarding` 단정 하나가 깨진다**(나루터 7/27 이 물에서 2셀 넘게 떨어짐) —
> 그 단정은 **A 땅 값**이 아니라 **제품 땅에서 드러난 진짜 결함**이다(회부).**

---

## §0ⓐ `:2729` — 의도인가 누락인가

### 먼저 정정한다: **T207 이 이미 적어 뒀고, T259 가 그것을 잘못 읽었다**

`보고/T207_2026-09-12.md:29~30` 원문:

> `_cons(v,'wood',…)` 로 잡히는 유출은 **둘뿐**이다 — 연료 `:3028` · 건축 `:3047`.
> **활대(`:2729`)와 교역 수출은 그 밖이라 잔차로 남는다**(유입 대비 31~43% · …).

T259 §0ⓒ 는 이것을 "T207 의 규약이 세 번 틀렸다"고 썼다. **그 문장이 틀렸다.**
T207 은 "`_cons` 로 잡히는 유출"이라고 **정확히 한정**했고, 활대와 교역이 그 밖이라는 것도,
잔차가 유입의 31~43% 라는 것도 **이미 적어 두었다**. T259 의 "발견"은 **재발견**이었다.
(족보 — 구멍이라 부르기 전에 그 자리 주석과 **앞 보고**를 읽는다.) **T259 §0ⓒ 의 그 절을 철회한다.**

### 그러면 `_cons` 는 무엇인가 — 장부가 아니다

`sim/economy-sim.js:495~498` 계약 주석:

> flow-EMA 소비 계측 … 실소비를 `v._consDay` 에 기록 → 일 경계에서 `v._consEMA`(30일 관성)로 폴드 →
> **v2 가격 target 의 `flowT`(=EMA×30)가 됨** … **신규 재화**는 소비처에 `_cons` 한 줄

⇒ `_cons` 는 **수요 신호**지 유출 장부가 아니다. 그리고 목재에 대해서는 그 신호가 **가격에 안 닿는다**:

| 경로 | 목재에 닿나 | 근거 |
|---|---|---|
| v2 가격 `flowT` | **안 닿는다** | `flowT = SUBSISTENCE_PER_NPC[r] ? 0 : …` 이고 **`SUBSISTENCE_PER_NPC.wood = 0.05`** (`economy-sim-v2.js:69`) |
| v2 과잉부패 보호 | **안 닿는다** | 같은 식(`economy-sim-v2.js:1242`) |
| 사건 장부 부족·글럿 문턱 | **닿는다** | `server/events.js:598~600` `ema × SHORT_DAYS` / `× GLUT_DAYS` |
| 의뢰 수량·보상 품목 | **닿는다** | `events.js:858` `ema × REQ_DAYS` · `villages.js:6317` |

### 이력

| 증거 | 무엇을 말하나 |
|---|---|
| 도입 커밋 `120158ca`(청동 희소성 3사본) | 이 블록이 처음 들어올 때 **`bone` 도 `_cons` 가 없었다** — 목재만 빠진 게 아니다 |
| `26d941b7`(flow-EMA 채택 · **16사이트**) | 그때 `bone` 에만 `_cons` 가 붙었다. 그 커밋이 밝힌 채택 규칙: **"subs 미등재 + 필수 투입만"** |
| `SUBSISTENCE_PER_NPC.wood = 0.05` | 목재는 **subs 등재**다 ⇒ 그 규칙대로면 목재는 애초에 `_cons` 대상이 **아니다** |
| 연료 `:3027` · 건축 `:3046` 은 `_cons` 를 부른다 | 규칙과 코드가 **어긋나 있다** — 그러나 어긋난 쪽이 "부른 두 줄"이지 "안 부른 한 줄"이 아니다 |
| 그 줄·함수의 주석 | 활대에 대해 `_cons` 를 **언급조차 안 한다**(같은 블록의 `bone` 은 `★flow-EMA` 라고 명시) |
| `보고/T207` | **활대는 `_cons` 밖**이라고 문서에 이미 적혀 있다 |

**판정 — "누락"이라 부를 근거가 없다.**
직접적인 "일부러 뺐다" 문장은 없지만, ⓐ 채택 규칙(subs 미등재만)은 **제외가 규칙에 맞는다**고 말하고,
ⓑ 앞 보고가 이미 제외를 문서화했으며, ⓒ **붙이면 세계가 바뀐다**(목재 `_consEMA` ↑ → 부족/글럿 문턱과
의뢰 수량이 움직인다). 세계를 바꾸는 한 줄이 "버그 픽스"로 슬며시 들어가면 안 된다.

⇒ 카드 ①의 분기대로 **표에 적고 끝낸다. ③ 훅은 달지 않았다**(제품 0 유지).
붙일지 말지는 **승인 게이트**다 — 회부한다.

## §0ⓑ 잔차 귀속 — **다리 절반씩만 세고 있었다**

### 첫 시도가 틀렸다 (그리고 그 틀림이 답을 줬다)

T259 의 장부는 `tradeStats.exportBy.wood`(나감)와 `tradeStats.woodImported`(들어옴) 두 칸으로 교역을 셌다.
코드를 전수로 훑어 보니 **목재가 `storage` 를 드나드는 자리는 여섯이고, 저 두 칸은 그중 둘만 본다**:

| # | 자리 | 방향 | `tradeStats` 에 실리나 |
|---|---|---|---|
| 1 | `economy-sim.js:3027` 연료 `fuelFromWood` | − | `_cons` ○ |
| 2 | `economy-sim.js:3046` 건축 `built × HOUSE_WOOD` | − | `_cons` ○ |
| 3 | `economy-sim.js:2729` 활대 `_bAmt × SELF_BOW_WOOD` | − | ✗ (`_bowMade` 누계로만) |
| 4 | `economy-sim-v2.js:756` 캐러밴 출발 적재 | − | `exportBy` ○ |
| 5 | **`economy-sim-v2.js:1024` 도착 인도**(`c.to` 가 받는다) | **+** | **✗** |
| 6 | **`economy-sim-v2.js:1103` 귀환 화물 매입**(`c.to` 가 판다) | **−** | **✗** |
| 7 | `economy-sim-v2.js:1151` 귀환 도착(`c.from` 이 받는다) | + | `woodImported` ○ |
| 8 | **`economy-sim-v2.js:1156` 교역세 3% → 국고** | **−** | **✗** |
| 9 | **`economy-sim-v2.js:1329` `tickDecay`**(`wood: 0.0003` · 과잉 가속 ×8) | **−** | **✗** |

⇒ 4 는 세고 5 는 안 센다. **한 캐러밴의 출발만 세고 도착은 안 센 것**이다 — 그래서 잔차가 양수로 크게 남았다.

### 고친 장부 — **세계 합**으로 본다

마을끼리 오간 목재는 세계 합에서 **정확히 상쇄**된다(4↔5, 6↔7). 그러니 교역 두 칸은 애초에 항등식의 항이 아니다.
세계 합에서 남는 것은 **국고**(8) · **길 위**(아직 안 닿은 화물) · **약탈 손실** 셋뿐이다.

```
창설부존 + Σ벌목  =  끝재고 + Σ(연료+건축) + Σ활대 + Δ국고 + 길위 + **이름 없는 나머지**
```

### 세계 합 장부 (200일 · 50마을 · 시드 1020)

| 항 | 값 |
|---|---|
| 창설부존 | 6,001.60 |
| Σ벌목(`dailyProductionBuf.wood`) | 24,726.59 |
| Σ연료+건축(`_consDay.wood`) | −22,245.48 |
| Σ활대(`_bowMade × SELF_BOW_WOOD`) | −329.04 |
| Δ국고(`treasury.wood`) | −554.40 |
| 길 위(미도착 화물) | −24.78 |
| 끝재고 | 629.39 |
| **이름 없는 나머지** | **6,945.10** (총유입의 **22.6%**) |
| (참고 · 세계 합에서 상쇄) 수출 4,334.56 · 수입 2,213.68 | |

⇒ 자리 여섯을 셌는데 **아직 22.6% 가 남는다.** 활대는 그중 **5.7%** 일 뿐이다(329 / 5,732 — T259 잔차 기준).

### 남는 잔차의 모양 — **하루 25~47 로 거의 일정하다**

하루 단위로 다시 쟀다(틱 **전** 총량 → 틱 **후** 총량, 그 사이의 명명된 항을 빼고 남는 것):

| 날 | 틱 전 총량 | 벌목 | 연료+건축 | 활대 | 틱 후 총량 | **하루 잔차** | 잔차/총량 |
|---|---|---|---|---|---|---|---|
| 1 | 6,001.6 | 0 | 36.66 | 1.15 | 5,938.5 | **25.24** | 0.42% |
| 20 | 4,375.2 | 0 | 92.23 | 3.46 | 4,235.7 | **43.82** | 1.00% |
| 60 | 1,069.7 | 128.93 | 93.61 | 1.01 | 1,231.5 | **34.90** | 3.26% |
| 120 | 782.6 | 139.93 | 133.95 | 0.70 | 741.4 | **45.17** | 5.77% |
| 200 | 855.9 | 206.61 | 161.80 | 2.04 | 1,183.8 | **36.00** | 4.21% |

* 하루 잔차 합 **6,945.09** = 누계 장부의 잔차 **6,945.10** (두 계산이 만난다 — 장부가 자기모순이 없다)
* **재고와 상관 0.098 · 벌목과 상관 −0.344** — 재고 6,001 → 722 로 8배 줄어도 하루 잔차는 25 → 45 로 **오히려 는다**.
* ⇒ **부패가 아니다.** `tickDecay` 의 목재 항(`wood: 0.0003`, 과잉 가속)은 **재고에 비례**해야 한다.
  실제로 하루 잔차는 재고가 아니라 **마을 수·인구 쪽**을 따라간다(50마을 · 하루 0.5~0.9/마을).

### 그래서 하루 첫날로 가른다 — **장부가 닫힌다**

같은 씨·같은 첫날에서 제품 손잡이(`economy-sim-v2.js:1329` `world._dbg.decay`)만 껐다(3일 · 자 코드만).
첫날은 두 팔의 시작 상태가 **완전히 같다**(틱 전 총량 6,001.60 · 연료+건축 36.66 둘 다 동일) —
그래서 이 비교는 세계가 갈리기 전의 **깨끗한 가름**이다.

| 날 | 부패 켬 잔차 | **부패 끔 잔차** | 차 = **부패** |
|---|---|---|---|
| 1 | 25.24 | **20.00** | **−5.24** |
| 2 | 25.09 | **20.00** | −5.09 |
| 3 | 25.42 | **21.15** | −4.27 |

**남은 20.00 이 정확히 둥글다.** 첫날 50마을 × 8명 × **0.05** = **20.00** —
`SUBSISTENCE_PER_NPC.wood = 0.05`(`economy-sim-v2.js:69`)다. 자리를 찾았다:

```js
// sim/economy-sim-v2.js:525  tickSubsistence(v, day)
for (const [r, perNpc] of Object.entries(SUBSISTENCE_PER_NPC)) {
  if (r === 'food') continue;
  const need = N * perNpc;
  const have = v.storage[r] || 0;
  const take = Math.min(need, have);
  v.storage[r] = have - take;        // ← **주거 목재 인출. `_cons` 도, 어떤 장부 칸도 없다.**
}
```

⇒ **잔차의 주인 둘이 이름을 얻었다** (첫날 기준 **잔차 0**):

| 자리 | 첫날 몫 | 왜 안 보였나 |
|---|---|---|
| **`economy-sim-v2.js:525` 주거 subsistence** (`N × 0.05`) | **20.00 (79.2%)** | 장부 칸이 **하나도 없다** — `_cons` 도 누계 필드도 |
| **`economy-sim-v2.js:1329` `tickDecay`** (`wood: 0.0003` · 과잉 ×8) | **5.24 (20.8%)** | 같다 |
| 이름 없음 | **0.00** | — |

⚠**200일 전체의 두 항 분리는 이 카드에서 못 한다** — `take` 가 `min(need, have)` 라 재고가 마르면
subsistence 가 스스로 줄고(그래서 하루 잔재가 인구 144.7 이 아니라 36 이다), 부패는 재고에 비례한다.
둘을 날짜별로 가르려면 **제품이 그 두 자리에 누계 칸을 하나씩 내줘야 한다** — 회부(구현 금지).

### 세 시드

| 시드 | 창설부존 | 벌목 | 연료+건축 | 활대 | Δ국고 | 길위 | 끝재고 | **잔차** | 잔차/총유입 |
|---|---|---|---|---|---|---|---|---|---|
| 1020 | 6,001.6 | 24,726.59 | 22,245.48 | 329.04 | 554.40 | 24.78 | 629.39 | **6,945.10** | 22.6% |
| 7 | 6,001.6 | 28,031.72 | 23,618.20 | 261.89 | 716.72 | 200.00 | 1,713.93 | **7,522.57** | 22.1% |
| 42 | 6,001.6 | 32,254.31 | 23,172.04 | 308.42 | 848.65 | 200.00 | 5,988.62 | **7,738.17** | 20.2% |

세 시드가 **20.2~22.6%** 로 모인다 — 잔차는 세계의 변덕이 아니라 **구조**다(위 두 자리).

## §0ⓒ 훅 — **안 달았다**

카드 ③은 "①이 **누락일 때만**"이다. §0ⓐ 의 답은 누락이 아니라 **문서화된 제외**이므로 훅을 달지 않았다.
`sim/` `server/` `lab/` `public/` **`git diff` 0** 이다.

그리고 §0ⓑ 가 그 판단을 한 번 더 받친다: 활대는 잔차의 **5.7%** 일 뿐이고, 장부를 실제로 벌린 것은
**장부 칸이 아예 없는 두 자리**(주거 subsistence · 부패)다. `:2729` 에 `_cons` 한 줄을 넣어도
**장부는 안 닫힌다** — 그리고 그 한 줄은 사건 장부의 부족·글럿 문턱을 움직인다(세계 변경 · 승인 게이트).
⇒ 고칠 자리는 `:2729` 가 아니라 **`tickSubsistence`·`tickDecay` 에 누계 칸을 내주는 것**이다(회부).



## §0ⓓ 러너 셋을 C 땅 위에 — **2/3 은 그대로, 하나가 깨진다**

`scripts/t259-t17-cland.js` 를 대상 파일 인자화해서(원본 무접촉 · `makeTerrainAdapter` 의 **셋째 인자만** 교체 ·
임시 사본은 `scripts/` 안에 만들고 즉시 지운다) 러너가 실제로 도는 하네스 셋을 두 땅에서 돌렸다.

| 하네스 | A 땅(현재) | **C 땅(제품 술어)** | 판정 |
|---|---|---|---|
| `test-distmatrix` | 9 통과 / 0 실패 ✅ | **9 통과 / 0 실패 ✅** | **그대로** |
| `test-frontier-iron` | 11 통과 / 0 실패 ✅ | **11 통과 / 0 실패 ✅** | **그대로** |
| `test-onboarding` | **28 통과 / 0 실패** ✅ | **27 통과 / 1 실패** ❌ | **단정 하나가 깨진다** |

깨지는 단정 — `scripts/test-onboarding.js:139` ③:

```
A 땅: ✓ 나루터는 실제로 물가에 있다 (물에서 2셀 넘게 떨어진 곳 0/26)
C 땅: ✗ 나루터는 실제로 물가에 있다 (물에서 2셀 넘게 떨어진 곳 7/27)
```

### 그 단정은 A 땅 값인가, 진짜 결함인가 — **진짜 결함이다**

`server/onboarding.js:396~398` 이 나루터를 물가에 붙이는 자리다:

```js
if (kind === 'dock') {
  const b2 = VillageLayout.nearestBank(ta, picked[0], picked[1], 6);
  if (b2 && okAt(b2.cx, b2.cy)) picked = [b2.cx, b2.cy];   // ★붙일 곳이 **도달 가능할 때만**
}
```

`kind` 는 위(`:328~331`)에서 이미 `dock` 으로 정해졌는데, 물가 칸이 **도달 불가**면 이 `if` 가 조용히 넘어간다 —
그러면 **"나루터"라고 이름 붙은 도착 지점이 물에서 멀리 떨어진 채로 남는다**(폴백도 경고도 없다).
C 땅은 해안선 물 띠가 있어 도달 불가 칸이 늘고, 그래서 7/27 이 그 상태가 됐다.

**제품 정본은 C 다**(`zone.js:570~650` → `:2651`). 게다가 실서버는 여기에 도랑(`DITCH_CELLS`)까지 더해
**더 막힌다**. ⇒ 실서버에서도 나루터 도착점의 일부가 물가가 아닐 수 있다. **A 땅이 그 결함을 가려 왔다.**

⇒ 카드의 물음("서면 모아도 회귀 아님")에 대한 답: **두 하네스는 모아도 회귀가 아니고,
`test-onboarding` 의 한 단정은 회귀가 아니라 *지금까지 안 보이던 결함이 드러나는 것*이다.**
그 단정을 고칠 게 아니라 `onboarding.js:398` 의 조용한 폴백을 고쳐야 한다 — **회부**(구현 금지).

## §0ⓔ 답 한 줄

> **`:2729` = 누락이 아니라 문서화된 제외**(T207 이 이미 적었고 채택 규칙 "subs 미등재만"과도 일치) ⇒ **훅 0 · 제품 0** ·
> **잔차 귀속: 첫날 기준 주거 subsistence(`economy-sim-v2.js:525`) 79.2% + 부패(`:1329`) 20.8% = 잔차 0** ·
> 200일 잔차는 세 시드 **20.2~22.6%** 로 모이고 둘의 날짜별 분리는 제품 누계 칸이 있어야 한다(회부) ·
> **러너 셋 C 땅: 2/3 그대로, `test-onboarding` 나루터 단정(7/27)이 깨지고 그건 A 땅이 가려 온 진짜 결함이다.**

## §1 검증

* **제품 코드 0** — `server/`·`sim/`·`lab/`·`public/` `git diff` **0**. 훅 **안 달았다**(누락이 아니라서).
* **17벌 무변** · `t17-metrics`·`t176-ab` **무접촉**(④는 임시 사본 — 만들고 즉시 지운다).
* **자명 통과 금지** — ⓓ 의 A 팔이 세 하네스 모두 **현재 그대로 통과**(바꾼 줄 0개라고 자가 찍는다);
  ⓑ 의 부패 끔 팔은 첫날 잔차가 **25.24 → 20.00** 으로 **움직인다**(무변이 아니다).
* **두 계산이 만난다** — 누계 장부 잔차 6,945.10 = 하루 잔차 합 6,945.09(시드 1020).
* **3시드**(1020·7·42) · 러너 전수 **안 돌렸다** · 실서버 접속 **0**.
* 접점: `_cons`·`_consDay`·`wood`·`SELF_BOW_WOOD`(`:2729`)·`fuelFromWood`·`HOUSE_WOOD`·
  `tickSubsistence`·`tickDecay`·`tradeStats.exportBy`·`woodImported`·`treasury`·`world.caravans`·
  `t222-life800`·`wProd`·`zone-preds`·`test-distmatrix`·`test-frontier-iron`·`test-onboarding`.

## §2 회부(구현 0)

1. ★★★**[정정 · 내 잘못] T259 §0ⓒ 의 "T207 이 세 번 틀렸다"를 철회한다.**
   T207 은 `보고/T207:29~30` 에서 **활대 `:2729` 와 교역이 `_cons` 밖이고 잔차로 남는다**고 이미 적었다.
   T259 의 "발견"은 재발견이었고, 규약을 틀렸다고 쓴 문장이 틀렸다. 앞 보고를 먼저 읽는다.
2. ★★★**[새 카드 · 이게 진짜 구멍] 목재가 장부 칸 없이 빠져나가는 자리가 둘이다.**
   `economy-sim-v2.js:525 tickSubsistence`(주거 `N × 0.05` — **어떤 칸에도 안 남는다**) ·
   `economy-sim-v2.js:1329 tickDecay`(`wood: 0.0003` · 과잉 ×8 — 같다).
   둘이 200일 목재 총유입의 **20~22%** 다. `:2729` 의 활대(5.7%)보다 훨씬 크다.
   처방은 `_cons` 가 아니라 **누계 칸 하나씩**(`v._subsUsed.wood` · `v._decayed.wood` 꼴) — 가격·사건에 안 닿고
   장부만 닫힌다. 제품 두 줄 · 승인 게이트.
3. ★★**[새 카드] `onboarding.js:398` — 나루터가 물가에 못 붙으면 조용히 그 자리에 남는다.**
   `if (b2 && okAt(...))` 가 실패해도 `kind` 는 여전히 `dock` 이다. 제품 땅에서 **27곳 중 7곳**이 그 상태다
   (A 땅에선 0/26 이라 여태 안 보였다). 폴백(가장 가까운 도달 가능한 물가로 넓혀 찾거나 `kind` 를 `road` 로
   낮추거나)이 필요하다. **실서버는 도랑까지 더해 더 막히므로 더 나쁠 수 있다.**
4. ★**[#31 · 재민] 모으기 판정에 쓸 값이 하나 더 늘었다.** 러너 셋 중 둘은 C 땅에서 그대로 통과한다 —
   모으기가 **회귀를 일으키지 않는다**. 남은 하나는 회귀가 아니라 **결함 노출**이다(위 3).
   여전히 **실기 한 줄(T251 §0ⓒ) 뒤**가 안전하다.
5. ★**[승인 게이트] `:2729` 에 `_cons` 를 붙일지.** 붙이면 목재 `_consEMA` 가 올라
   사건 장부의 부족·글럿 문턱(`events.js:598`)과 의뢰 수량(`:858`)이 움직인다 — **세계 변경**이다.
   장부를 닫는 데는 **필요 없다**(위 2 가 진짜 자리다). 붙일 이유는 "계약 일관성"뿐이다.
6. ⓘ**앞 카드 회부 유효**: #31 모으기 · #6 실기 한 줄 · 마이그레이션 둘 A 땅 · `어촌6` 좌표 · 사냥 이식.
