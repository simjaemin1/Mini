# T336 — `nippon` 시딩 진단 (세션5 · 2026-09-20 · **코드 0**)

> 카드 `~/Mini/지시_T336.md` · 가지 `batch/nippon-audit-0920` · 착지 **`5ffd252a`** · 베이스 `badd15c7`(main) ·
> **제품 diff 0 · 하네스 diff 0 · 문서만.** 측정은 컨테이너 부팅 2판(정본 1 · 실험 1 — 실험은 `/tmp` 사본, 레포 무접촉).

## ★ 한 줄 답 (머리 15줄)

> **닛폰은 이미 땅을 갖고 있다 — 막는 것은 `villages.js:2533` 한 줄과 "마을 후보 0"이다.
> 지금 띄우면 **오류 0** 으로 뜨고 레거시 마을 16곳·NPC 192명이 서지만 생활층은 0(`villages:null`).
> 그 한 줄을 풀고 후보만 주면(실험 2줄) **마을 14곳·econ 인구 112·장부·교역행렬이 오류 0 으로 선다.**
> 남는 진짜 결손은 셋 — 광맥(0.1 바닥 14/14) · 다리(도달불능 **60/91쌍**) · 마을 정본 0곳. 카드 5장.**

1. **지형 정본은 이미 다존이다.** `server/hanbando-terrain.json` 은 이름만 한반도고 안은 **존 11개 맵**이다.
   닛폰 항목이 **있다**: rivers 17 · lakes 5 · ridges 10 · passes 2 · forests 16 · **ores 5** · **villages 0**.
2. **제품에서 존을 막는 자리는 딱 하나다** — `server/villages.js:2533`
   `if (ZONE_ID !== 'hanbando') return;`(§4-4 "1차 이식은 hanbando 존만"). 생활층·econ·소문·교역이 통째로 안 돈다.
   나머지 `'hanbando'` 문자열 9건은 전부 **기본값**이거나 **한반도 전용 청소**라 닛폰에 무해하다(§0-ⓐ 표).
3. **지금 띄운 실측**(정본 그대로 · `ZONE_ID=nippon`): **첫 오류 없음.** 존이 정상 기동하고
   `🏘️ 마을 [이즈사키] … type=plain` 식으로 **레거시 마을 16곳**(절차 생성)·NPC **192명**(16×12)이 선다.
   `/health` 가 `villages: null` — 생활층은 **한 번도 안 깨어났다**. `[seed]` 줄도 없다.
4. **게이트를 풀면 어디까지 가나**(`/tmp` 사본 · 두 줄 · 레포 무접촉):
   후보 16 → 선별 16 → **시딩 14**(터 미달 2곳: 이즈사키·히마쿠 — 주변 24셀에 뭍 없음) ·
   `📜 사건 장부 · 마을 14곳 프라이밍` · `🏘️ 교역 BFS 거리행렬: 14마을 91쌍` ·
   `🏘️ 마을 시뮬 준비: 마을 14, econ 인구 112` — **오류 0**.
5. ⚠**그래도 못 산다. 진짜 결손 셋이 숫자로 나왔다:**
   · **광맥** — 시딩 14곳 전부 `O0.1`(바닥). 한반도 정본 ores **787** vs 닛폰 **5**. 표본으로도 27배 차(§0-ⓑ).
   · **다리** — `도달불능 60쌍`(91쌍 중 **66%**). 한반도는 다리 15개를 놓고서야 이어졌다.
   · **마을 정본 0곳** — 한반도 51곳은 에디터 v9 에서 사람이 찍은 것이고, 닛폰엔 그게 없다.
6. **땅 자체는 나쁘지 않다**(§0-ⓑ) — 물 3.3% · 숲배율 1.05 · 논이 깔리는 자리 5곳(F1.1/W1.5대).
   다만 "아사기 **열도**"라는 이름과 `biome: mountain` 에 견주면 실제 지형은 **섬도 산도 아닌 평지**다(plain 148,319/153,930).
7. **도구 5개가 `<존이름>-terrain.json` 을 찾는다** — 정본 파일은 하나뿐이라 `--zone nippon` 이 즉사한다(§0-ⓐ-12).
   한 줄씩 고치면 되는 자리고, 이게 카드 ②의 대부분을 싸게 만든다.

---

## §0-ⓐ 한반도 전용으로 읽는 자리 — 전수

**제품(`server/`·`sim/`·`public/`)에 박힌 `'hanbando'` 문자열은 11건이고, 그중 진짜 게이트는 1건이다.**

| # | 자리 | 무엇을 하나 | 판정 |
|---|---|---|---|
| 1 | **`villages.js:2533`** `if (ZONE_ID !== 'hanbando') return;` | **생활층·econ·장부·소문·교역 전체** | ★**진짜 게이트.** 존 인자가 아니라 **조기 반환**이다 — 지우면 열린다 |
| 2 | `villages.js:1133` `terrain.getZoneVillages(state.zoneId)` | 시딩 후보 공급 | **존 인자다**(하드코딩 아님) · 닛폰은 **자료가 0곳** |
| 3 | `villages.js:429·430` `state.zoneId \|\| 'hanbando'` | `seaDistPx` 기본값 | 기본값 — `state.zoneId` 가 그 앞에서 선다. 무해 |
| 4 | `zone.js:612` · `zone-local-db.js:21` · `trees.js:198` | `process.env.ZONE_ID \|\| 'hanbando'` | 기본값 — env 로 이미 존 인자. 무해 |
| 5 | `zone.js:2865` · `:2876` `if (ZONE_ID === 'hanbando')` | 옛 디버그 방 벽·공용 상자 청소 | **한반도 전용 청소**. 다른 존엔 안 돌고 무해 |
| 6 | `terrain.js:37` `require('./hanbando-terrain.json')` | 지형 정본 적재 | **이름만 한반도** — 안은 존 11개 맵. 닛폰 항목 있다 |
| 7 | `terrain.js:99~111` `hc[zoneId].ridges\|passes\|valleys\|forests\|groves\|ores` | 정본 → 지형 | 전부 `\|\| []` 폴백. **존 무관** |
| 8 | `central.js:1487` `/terrain.json` | 클라에 지형 배포 | 파일 통째를 보낸다 — 존 무관 |
| 9 | `hanbando-minerals.js` | 광종 풀 | 파일 머리부터 **"지역 무관 · 골고루"**. `server/`·`sim/` 참조 **0건**(주석 둘뿐) |
| 10 | `public/client/30-n-net.js:119` · `70-lobby.js:207` | 기본 존 선택 | 기본값 — 로비에서 닛폰 선택 가능. 무해 |
| 11 | **존 설정 키 7개 부재**(`zone-config.js`) | `useHardcodedVillages`·`seedAllVillages`·`villageMax`·`legacyVillages`·`npcVillageHouses`·`npcVillageTerritory`·`npcPerVillage` | **코드가 아니라 자료.** 없으면 종전 기본값(레거시 ON·NPC 12명·집 없음) |
| 12 | **도구 5개** `plan-ore-clusters:41` · `plan-small-forests:42` · `plan-village-forage:59` · `audit-ore-distribution:30` 이 `ZID + '-terrain.json'` · `build-terrain-canon-v3` 경로 셋 | 정본 파일 경로를 **존 이름으로 만든다** | ★**도구 하드코딩.** 파일은 `hanbando-terrain.json` 하나뿐 ⇒ `--zone nippon` 이 파일 없음으로 즉사 |
| 13 | `plan-village-50.js:31` `const Z = 'hanbando'` | 마을 처방전 | 도구 하드코딩(존 인자 없음) |
| 14 | `plan-ore-clusters.js:137` `if (ZID === 'hanbando') HB_MIN = …` | 광종 풀을 한반도만 읽는다 | 도구 하드코딩 — 닛폰이면 풀 없이 돈다 |
| 15 | `plan-bridges.js:14` · `audit-reachability.js:14` `ZID = argv[2] \|\| 'hanbando'` | 다리 계획·도달 감사 | **이미 존 인자다**(기본값만 한반도) |

**읽는 법**: 제품 쪽은 **한 줄(#1)** 과 **자료 둘(#2·#11)** 이 전부다. 나머지는 이름·기본값·한반도 전용 청소다.
비용이 숨어 있는 곳은 오히려 **도구(#12~#14)** 인데, 전부 경로·상수 한 줄짜리다.

## §0-ⓑ 닛폰의 땅 — 한반도와 같은 자로

같은 프로브로 두 존을 쟀다(정본 함수만 호출 · 200px 격자 · 사본 0).

| | hanbando | nippon |
|---|---|---|
| 크기 | 2188×4063셀 | **1562×4063셀** |
| biome / displayName | forest / 새벌 | mountain / **아사기 열도** |
| 표본 | 224,509점 | 159,162점 |
| 물 | 4.3% | **3.3%** |
| 숲배율(뭍 평균) | 1.20 | **1.05** |
| 돌배율(뭍 평균) | 1.00 | 1.00 |
| 큰광맥 위 | **3.20%** | **0.12%** (27배 적다) |
| 물 220px 이내(=riverside 자격) | 4.4% | **2.4%** |
| 타입 분포 | plain 178,463 · forest 20,182 · riverside 9,416 · mining 6,699 | plain **148,319** · forest 1,779 · riverside 3,649 · mining 183 |
| 정본 rivers/lakes/ridges/passes | 55 / 14 / 11 / 0 | 17 / 5 / 10 / 2 |
| 정본 valleys / groves | **24 / 53** | **0 / 0** |
| 정본 ores | **787** | **5** |
| 정본 villages(시딩 후보) | **51** | **0** |
| bridges(zone-config) | 15묶음 · 수백 셀 | **없음** |

⚠**이름과 땅이 어긋난다.** "열도"인데 물이 3.3% 뿐이고, `biome: mountain` 인데 mountain 타입 **0점**이다
(돌배율 1.00은 한반도도 같다 — `getStoneMultiplier` 가 ridges 를 안 읽는다. 닛폰 결손이 **아니다**).
⇒ 닛폰을 "산 많은 열도"로 만들 생각이면 **지형 자체를 더 그려야 한다**(카드 ①).

## §0-ⓒ 지금 띄우면 — 실측 2판

### 판 1 — 정본 그대로 (`ZONE_ID=nippon`)

| | |
|---|---|
| 첫 오류 | **없다.** `Error`·`❌`·`fatal` 0건 (경고는 `⚠️ GC 정지 31ms` 하나) |
| 기동 | `🌏 zone server up … biome=mountain rect=(480000,49984,49984x130016) neighbors=W:✓ E:✓ N:✓ S:✓` |
| 해안선 | `1,016,334 water tiles (ocean=edge only)` · 타일 지형 메모 1562×4063 · 6.1MB |
| 마을 | **레거시 16곳**(`generateVillagesForZone` 절차 생성 — `이즈사키`·`지옹로`·`네코로`…) |
| NPC | **192명**(16×12 — `npcPerVillage` 키가 없어 기본 12) · 전부 `(집 없음)` |
| 광맥 | `광맥 5개 — silver, iron, silver, gold, sulfur` |
| **생활층** | **안 깨어났다.** `[seed]`·`마을 시딩`·`사건 장부`·`거리행렬`·`마을 시뮬 준비` **전부 없음** · `/health` `villages: null` |

⇒ **"어디서 서나"의 답: 아무 데서도 안 선다 — 조용히 건너뛴다.** 오류가 없어서 더 위험하다(족보 130).

### 판 2 — 게이트만 풀면 (`/tmp` 사본 · 두 줄 · 레포·커밋 무접촉)

바꾼 것: ⓐ `villages.js:2533` 조기 반환 제거 ⓑ 정본 마을이 0곳이면 `generateVillagesForZone` 을 후보로.

```
[T336실험] 절차 마을 16곳을 후보로
[seed] ★전수 시딩(env SEED_ALL=1) — 품질 게이트 우회: 후보 16곳 전부
[nippon] 🏘️ 마을 시딩 시작 — 후보 16 → 선별 16 (상한 20)
[nippon] 🏘️ [이즈사키] 중심 (47131,9827) 주변 24셀 내 뭍 없음 — 스킵
[nippon] 🏘️ [히마쿠]   중심 (45228,56844) 주변 24셀 내 뭍 없음 — 스킵
[nippon] 🏘️ 마을 시딩 완료 — 후보 16 → 선별 16 → **시딩 14** (터 미달 스킵 2곳)
[nippon] 📜 사건 장부 — 과거 0건 복구 · 마을 14곳 프라이밍
[nippon] 🏘️ 교역 BFS 거리행렬: 14마을 91쌍 1871ms … 도달불능 **60쌍**
[nippon] 🏘️ 마을 시뮬 준비: 마을 14, econ 인구 112, 스폰 NPC 112, econ day 0
```

**오류 0 · `/health` `villages: 14` · `buildings: 588`.** 배선은 이미 존 무관이다 — 막고 있던 건 그 한 줄이다.

**하지만 땅 파라미터가 그대로 말한다** (시딩 14곳 전수):

| 축 | 값 | 읽기 |
|---|---|---|
| **O(광맥)** | **14곳 전부 `0.1`** (바닥) | 도구·청동 사슬이 **영영 안 돈다** |
| S(돌) | 12곳 `0.25`(바닥) · 쿠라마 1.67 · 쿠라다 2.5 | 석기 재료도 수입 의존 |
| F(비옥) | 0.41~1.12 · 논이 깔린 곳 **5**(지옹로 41 · 노라마 42 · 이즈이 41 · 쿠라다 48 · 타카쿠 44) | 식량은 된다 |
| W(물) | 0.08~1.58 | 5곳만 물가 |
| 우드 | 0.45~1.77 | 마트노(1.77)만 임업 |
| **도달** | **91쌍 중 60쌍 불능(66%)** | 교역이 안 선다 — **다리가 없다** |

## §0-ⓓ 닛폰 첫 마을까지 — 카드 5장 (순서 · 크기)

순서는 **의존**이 정한다. 앞 카드가 없으면 뒤 카드가 잴 수 없다.

| 순 | 카드 | 하는 일 | 크기 | 왜 그 순서인가 |
|---|---|---|---|---|
| **①** | **도구를 존 인자로** | `plan-ore-clusters`·`plan-small-forests`·`plan-village-forage`·`audit-ore-distribution` 의 `ZID + '-terrain.json'` → 정본 한 파일 · `plan-village-50` 의 `const Z` → 인자 · `plan-ore-clusters:137` 광종 풀 게이트 해제 | **반나절**(경로·상수 6줄 · 도구만 · 제품 0) | 이게 없으면 ②~④ 를 **잴 수가 없다**. 가장 싸고 가장 먼저다 |
| **②** | **닛폰 광맥 정본** | `plan-ore-clusters --zone nippon --apply` — ores 5 → 한반도 밀도(787/8.9M셀)에 맞춰. 감사는 `audit-ore-distribution` | **하루** | O0.1 바닥이 풀려야 도구 사슬이 돌고, 그래야 ③의 생존을 잴 수 있다 |
| **③** | **닛폰 마을 후보** | 후보 0 → n곳. 세 길 중 하나를 **표로 고른다**: ⓐ 에디터로 손수(한반도 51곳이 그 길) ⓑ `generateVillagesForZone` 을 후보로 승격(실험이 이미 14곳을 세웠다) ⓒ 새 배치기. `plan-village-50 --zone nippon` 이 처방전을 낸다 | **하루**(ⓑ) ~ **여러 날**(ⓐ·ⓒ) | ②가 서야 땅 점수가 참이다 |
| **④** | **닛폰 다리** | `audit-reachability nippon` → `plan-bridges-v2 nippon` → `zone-config.nippon.bridges`. 지금 **도달불능 60/91쌍** | **하루** | ③의 마을 자리가 정해져야 어디를 잇는지 안다 |
| **⑤** | **존 게이트 해제** | `villages.js:2533` 한 줄 + `zone-config.nippon` 키 7개(`seedAllVillages`·`villageMax`·`legacyVillages:false`·`npcVillageHouses`·`npcVillageTerritory`·`npcPerVillage`·`useHardcodedVillages`) · 회귀는 **한반도 무변**이 본선 | **하루** | ①~④ 가 다 서야 "열었더니 무너졌다"가 안 난다. ★**제품을 만지는 것은 이 카드 하나뿐이다** |

**⑥(선택) 닛폰 지형 다시 그리기** — 이름(열도)·biome(mountain)과 실제 땅(평지 96.9%)이 어긋난다.
지형을 손보려면 ③보다 **앞**이어야 한다(한반도에서 배운 것: *지형을 만지면 통행이 끊긴다* — 다리를 다시 놓게 된다).
크기는 **여러 날**이고, 이건 기술이 아니라 **재민의 결정**이다: 닛폰을 산 많은 열도로 만들 것인가, 지금의 평지로 둘 것인가.

## §3 검증

| | |
|---|---|
| 제품 diff | **0** (`server`·`public`·`sim` 빈 칸) |
| 하네스 diff | **0** · 새 스크립트 **0** |
| 만진 것 | `보고/T336_2026-09-20.md` · `인계/Z-존서버.md` · `인계/회부.md` — **문서만** |
| 부팅 실측 | 정본 1판(`/tmp/nip-z.db`) · 실험 1판(`/tmp/t336lab` 사본 — **측정 뒤 지웠다**) |
| 레포 무접촉 확인 | 실험 전후 `git status --porcelain` 빈 칸 |
| 러너 | 안 돌렸다(코드 0 — 잴 diff 가 없다) |

**접점 심볼**: `nippon` · `ZONES` · `seedAllVillages` · `seedVillages` · `getZoneVillages` · `generateVillagesForZone` ·
`hanbando-terrain.json` · `hanbando-minerals` · `pickSeedVillages` · `plan-ore-clusters` · `plan-bridges` · `audit-reachability`

## §4 회부만

1. **닛폰 지형이 이름과 어긋난다**(열도 아님 · 산 아님) — **재민 결정 사항**(§0-ⓓ ⑥).
2. `getStoneMultiplier` 가 정본 `ridges` 를 안 읽는다 — 두 존 다 돌배율 평균 1.00 · mountain 타입 0점.
   닛폰 결손이 아니라 **한반도도 같다**. 의도인지 구멍인지는 안 팠다(카드 밖).
3. 청크 경계 감사거리(T317 ①) · 광맥 없는 43마을(#47) — 그대로.

## §5 푸시

| | |
|---|---|
| 가지 | `batch/nippon-audit-0920` |
| main | **`5ffd252a`** (ff · 문서만 · 베이스 `badd15c7` · 리베이스 1회 — T339 착지가 사이에 들어왔다 · 겹침 0) |
| 길 | 맥 VM `device_bash` · `$HOME/pushrepo` 얕은 클론 · PAT 는 셸 변수만 · 즉시 `unset` |
| force | **0** |
