# T288 — 장부 칸 둘. 제품 두 줄. 세계는 한 자도 안 움직였다 (세션5 · 2026-09-14 · **가지만**)

> 카드 `~/Mini/지시_T288.md` · 가지 `batch/hunt-wood-server-0912` · **측정 베이스 `a1c945d7`**
> 만진 것: `sim/economy-sim-v2.js` **두 줄**(+ 주석) · 자동 생성물 둘(`economy-engine.browser.js` · 랩 인라인) ·
> `scripts/t222-life800.js`(새 칸을 읽는 자리) — **손잡이 0 · 새 수 0 · `_cons`/EMA/가격/사건 무접촉**.
>
> **답 한 줄 — 칸 둘(`_subsUsed` · `_decayed`) · 여덟 수 3시드 **비트 동일** ·
> 목재 잔차 **22.6% → (아래 표)** · 주거·부패가 칸 없이 빼 가던 품목 **subs 11종 · decay 30종 전부** ·
> 남은 무장부 자리 하나: `tickSubsistence` 둘째 루프(무기·갑옷 마모).**

---

## §0ⓐ 주석부터 — "일부러 안 센다"는 **없다**. 다만 **두 보고가 이미 알고 있었다**

| 자리 | 그 줄·함수의 주석 | 판정 |
|---|---|---|
| `economy-sim-v2.js:525 tickSubsistence` | 머리말 "NPC 자급 인출 — 매일. v1 이 식량은 처리. 여기선 그 외 자원만." · 루프 안 유일한 주석은 **"부족분은 누락"**(모자란 몫 이야기지 장부 이야기가 아니다) | **의도 진술 없음** |
| `economy-sim-v2.js:1329 tickDecay` | 부패율·과잉가속·클램프 근거가 길게 적혀 있다. **장부·`_cons` 언급 0** | **의도 진술 없음** |
| `_cons` 계약 주석(`economy-sim.js:495~498`) | "신규 재화는 **소비처에 `_cons` 한 줄**" — 그러나 `_cons` 는 **가격 신호**(flowT)다 | 이 카드의 칸은 `_cons` 가 **아니다** |
| 채택 커밋 `26d941b7` | 규칙 "subs 미등재 + 필수 투입만" — 그래서 subs 등재 품목은 **`_cons` 대상이 아니다** | 누계 칸과는 **무관** |

**그런데 앞 보고 둘이 이미 이 구멍을 봤다.**

* `보고/T130_2026-09-05.md §ⓐ`: *"⚠`tickSubsistence` 가 매일 **조용히 빼 가는 몫**이다(`SUBSISTENCE_PER_NPC[r] × N`).
  표를 옮겨 적지 않으려고 **정본이 낸 target 에서 되읽는다**(target = subs × 30)."*
  → 칸이 없어서 **우회**를 썼다. 이 카드의 한 줄이 그 우회를 없앤다.
* `보고/T279 §0ⓑ`: 첫날 잔차 25.24 = subsistence **20.00** + 부패 **5.24**.

⇒ **"일부러 안 센다"가 아니라 "아무도 안 적었다"** 다. 그리고 누계 칸은 세계를 안 움직이므로(읽는 쪽이 없다)
카드 ①의 분기와 무관하게 ②로 간다.

> ⓘ **카드 머리말의 요약 한 줄을 정정한다.** 카드는 T279 의 귀속을 "활대 5.7% · **94.3% 는 `tickDecay` 과잉재고 부패**"로
> 적었는데, T279 §0ⓑ 의 실측은 **첫날 기준 주거 subsistence 79.2% + 부패 20.8%** 다(부패 단독이 아니다).
> 카드 본문은 두 자리를 정확히 짚었으므로 작업 내용에는 영향이 없다.

## §0ⓑ diff 두 줄 — 그리고 여덟 수가 **한 자도 안 움직였다**

### 제품 diff (주석 뺀 실질 **두 줄**)

```js
// sim/economy-sim-v2.js:525  tickSubsistence — 주거 자급 인출
    v.storage[r] = have - take;
+   (v._subsUsed || (v._subsUsed = {}))[r] = (v._subsUsed[r] || 0) + take;

// sim/economy-sim-v2.js:1329 tickDecay — 부패
    v.storage[r] = s * (1 - rate);
+   (v._decayed || (v._decayed = {}))[r] = (v._decayed[r] || 0) + (s - v.storage[r]);
```

* **이름**: 정본이 이미 쓰는 두 문법을 합쳤다 — `_herbUsed`·`_boneUsed`(누계) + `_foodEaten`(재화별 칸).
* **부패는 차분으로 적는다**(`s − 남은 것`). `s × rate` 를 다시 곱하면 그게 사본이고, 부동소수에서 어긋난다.
* **손잡이가 없다.** 읽는 쪽이 세계에 하나도 없으므로 켜고 끌 것이 없다 — 그게 바로 비트 동일의 이유다.
  (손잡이를 달면 오히려 "끄면 뭐가 달라지나"라는 거짓 질문이 생긴다.)
* **`_cons`·`_consEMA`·가격(`flowT`)·사건 장부 전부 무접촉.** `:2729` 에 `_cons` 를 붙이는 건 세계 변경이고 재민 #37 이다.
* 지연 생성(`|| (v._x = {})`)이라 `createVillage` 도 안 건드린다. `serializeEcon` 은 `SERIALIZE_SKIP` 밖이라 **DB 에 그대로 남는다**.

### 여덟 수 3시드 800일 — **열 개 전부 비트 동일**

`인계/공통.md §2` 기준선 표(T163 셋째 판)와 맞댔다. `t17-metrics.js` 는 **무접촉**이다.

| 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 | ㉮ | ㉯ |
|---|---|---|---|---|---|---|---|---|---|---|
| 1020 기준선 | 6,889 | 0/51 | 1,012 | 5,509 | 2,539 | 11,892.1 | 803.6 | 37,240.0 | 1.93 | 2.02 |
| **1020 T288** | **6,889** | **0/51** | **1,012** | **5,509** | **2,539** | **11,892.1** | **803.6** | **37,240.0** | **1.93** | **2.02** |
| 7 기준선 | 7,148 | 0/51 | 1,030 | 7,830 | 2,714 | 12,039.3 | 551.1 | 37,779.9 | 1.88 | 1.97 |
| **7 T288** | **7,148** | **0/51** | **1,030** | **7,830** | **2,714** | **12,039.3** | **551.1** | **37,779.9** | **1.88** | **1.97** |
| 42 기준선 | 6,259 | 0/51 | 978 | 5,946 | 2,648 | 11,013.2 | 663.3 | 38,902.1 | 1.95 | 2.04 |
| **42 T288** | **6,259** | **0/51** | **978** | **5,946** | **2,648** | **11,013.2** | **663.3** | **38,902.1** | **1.95** | **2.04** |

⇒ **3시드 × 열 열 = 30칸, 다른 칸 0.** 장부 칸은 값을 안 만든다 — 그게 증명이다.

## §0ⓒ 항등식 — **닫혔다**(잔차 0.55~0.83%), 그리고 남은 잔차에도 이름이 있다

헤드리스 자(200일 · 50마을 · 3시드)가 정본 두 칸을 **옮겨 적기만** 한다(산수 0 · 재구현 0).

| 시드 | 창설부존 | 벌목 | 연료+건축 | **주거(새 칸)** | 활대 | Δ국고 | **부패(새 칸)** | 길위 | 끝재고 | **잔차** | **%** |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1020 | 6,001.6 | 24,726.59 | 22,245.48 | **6,596.96** | 329.04 | 554.40 | **94.34** | 24.78 | 629.39 | **253.80** | **0.83%** |
| 7 | 6,001.6 | 28,031.72 | 23,618.20 | **7,225.91** | 261.89 | 716.72 | **110.02** | 200.00 | 1,713.93 | **186.64** | **0.55%** |
| 42 | 6,001.6 | 32,254.31 | 23,172.04 | **7,368.53** | 308.42 | 848.65 | **160.59** | 200.00 | 5,988.62 | **209.06** | **0.55%** |

T279 의 **22.6% → 0.83%** 다. 그리고 두 칸의 크기는 T279 가 첫날로 잰 비율과 다르다 —
200일 누계에선 **주거가 압도적**(6,597 vs 부패 94)이다. 부패는 재고에 비례하는데 목재 재고가 말라서다.

### 남는 0.55~0.83% — **영토 확장이다**

| 시드 | 잔차 | 확장셀 | **잔차 ÷ 확장셀** |
|---|---|---|---|
| 1020 | 253.80 | 157 | **1.617** |
| 7 | 186.64 | 116 | **1.609** |
| 42 | 209.06 | 130 | **1.608** |

정본 상수: `BASE_EXPAND_COST.wood = 40` ÷ `EXPAND_CELLS_PER_SLOT = 25` = **1.60** (× `(size/baseSize)^1.3`).
자리는 `sim/economy-sim.js:1462~1464`:

```js
v.treasury.food  -= cellFood;
v.treasury.wood  -= cellWood;    // ← **국고에서 태운다. 여기도 칸이 없다(세 번째 자리).**
v.treasury.stone -= cellStone;
```

이 항을 넣으면 **장부가 완전히 닫힌다**:

| 시드 | 총유입 | 명명된 유출 + 끝재고 | **차** |
|---|---|---|---|
| 1020 | 30,728.19 | 30,728.19 | **0.0000** |
| 7 | 34,033.32 | 34,033.31 | **0.0100** |
| 42 | 38,255.91 | 38,255.91 | **0.0000** |

⇒ **목재 장부가 닫혔다.** 세 번째 자리(확장 비용)는 이 카드의 "두 줄" 밖이라 **회부**한다(구현 금지).

## §0ⓓ 목재만의 구멍이 아니었다 — **주거 75종 · 부패 46종**

두 루프는 품목을 가리지 않는다. 그래서 새 칸이 **목재만이 아니라 전부**를 받는다.
(`SUBSISTENCE_PER_NPC` 은 `specialty.js` 통합으로 런타임에 **75종**까지 는다 — `economy-sim-v2.js:186`
`SUBSISTENCE_PER_NPC[id] = 0.05 × contributes.subsistence`. 표에 적힌 11종이 전부가 아니다.)

**200일 · 시드 1020 · 50마을 누계** (0 이 아닌 품목만):

| | 품목 수 | 0 아닌 것 | 누계 | 상위 다섯 |
|---|---|---|---|---|
| `_subsUsed`(주거 인출) | 75 | **24** | **103,156** | rice 19,283 · wheat 19,252 · barley 15,601 · stone 9,790 · **wood 6,597** |
| `_decayed`(부패) | 46 | **44** | **39,904** | fish 12,214 · food 10,653 · hide 7,724 · meat 2,284 · bone 2,185 |

⇒ **목재는 주거 인출에서 5위**다. 같은 구멍이 곡물 셋(rice·wheat·barley 누계 **54,135**)과 석재(9,790)에도
그대로 있었다 — T130 §ⓐ 가 곡물 가격을 보며 "조용히 빼 가는 몫"이라 적은 그 몫이 이것이다.
이제 그 셋도 **칸을 얻었다**(같은 두 줄 · 새 코드 0).

### 아직 칸이 없는 자리 — 남은 셋

| 자리 | 무엇 | 왜 이 카드 밖인가 |
|---|---|---|
| `economy-sim-v2.js:540~543` | `tickSubsistence` **둘째 루프** — 무기·갑옷 마모(전사·사냥꾼 비례) | 같은 함수 안이지만 **목재가 아니다**. 카드는 두 줄이다 |
| `economy-sim.js:1462~1464` | 영토 확장이 국고에서 태우는 food·wood·stone | 위 §0ⓒ — 세 번째 자리 |
| `economy-sim.js:2747~2750` | 활 마감 보조재(깃·수지·삼) — `v.storage[_fx[0]] -= _fi` 뿐 | 활대(`:2729`)와 같은 족속(재민 #37) |

## §0ⓔ 답 한 줄

> **칸 둘**(`_subsUsed` · `_decayed` — 제품 두 줄 · 손잡이 0 · `_cons`/EMA/가격/사건 무접촉) ·
> **여덟 수 3시드 비트 동일**(30칸 중 다른 칸 0) ·
> **목재 잔차 22.6% → 0.83 / 0.55 / 0.55%**, 남는 것은 **영토 확장 비용**(잔차÷확장셀 = 1.617/1.609/1.608 ≡ `40÷25`)이고
> 그 항까지 넣으면 **차 0.00** — **장부가 닫혔다** ·
> **같은 구멍이 주거 75종·부패 46종에 있었다**(목재는 주거 인출 **5위** · 곡물 셋이 54,135).

## §1 검증

* **제품 diff = `sim/economy-sim-v2.js` 두 줄**(+주석). `economy-sim.js`·`server/`·`lab/`(수기) **0**.
  자동 생성물 둘은 재생성했다(`economy-engine.browser.js` · 랩 인라인 — **손으로 안 기웠다**).
* **게이트 넷**: `build-econ-bundle --check` **8 일치 / 0 불일치 ✅** · `inline-engine --check` **둘 다 최신 ✅** ·
  `lab-wiring-check` **실패 0 · 경고 0** · `lab-modules --diff` **교집합 1개**(`economy-sim-v2.js`)
  ⇒ 규약대로 **베이스 A/B 를 돌렸다**(아래).
* **여덟 수 3시드 800일 비트 동일** — `인계/공통.md §2` 표와 30칸 전부 일치. `t17-metrics` **무접촉**.
* **자명 통과 금지** — 새 칸이 실제로 **값을 받는다**(주거 103,156 · 부패 39,904 · 75종/46종).
  칸이 비어 있어서 비트 동일인 게 아니다.
* **두 계산이 만난다** — 누계 잔차 = 하루 잔차 합(시드 1020: 253.80 = 253.80).
* 3시드 · 러너 전수 **안 돌렸다** · 실서버 접속 **0** · 새 수 **0**(모든 상수는 정본에서 읽는다).
* 접점: `tickSubsistence`·`tickDecay`·`_subsUsed`·`_decayed`·`_foodEaten`·`_herbUsed`·`_cons`·`wood`·
  `t222-life800`·`잔차`·`BASE_EXPAND_COST`·`EXPAND_CELLS_PER_SLOT`.

## §2 회부(구현 0)

1. ★★★**[새 카드] 장부에 남은 세 번째 자리 — 영토 확장이 국고에서 태운다.**
   `sim/economy-sim.js:1462~1464` `v.treasury.{food,wood,stone} -= cell*`. 목재 잔차의 **전부**가 이것이고
   (잔차÷확장셀 = 1.608~1.617 ≡ `BASE_EXPAND_COST.wood 40 ÷ 25`), **food·stone 도 같은 구멍**이다.
   처방은 이 카드와 같은 꼴 — `v._expandUsed[r] += cell*` 한 줄. 제품 한 줄 · 승인 게이트.
2. ★★**[새 카드] `tickSubsistence` 둘째 루프(무기·갑옷 마모)에 칸이 없다.**
   `economy-sim-v2.js:540~543` — 같은 함수 안인데 첫째 루프만 칸을 얻었다. `_subsUsed` 가 반쪽이 되지 않게
   다음 카드에서 같이 채워라(같은 꼴 한 줄).
3. ★★**[규모] 목재는 이 구멍의 다섯째였다.** 200일 한 판에서 주거 인출 누계 **103,156**(75종 중 24종이 0 아님) ·
   부패 누계 **39,904**(46종 중 44종). 곡물 셋(rice·wheat·barley)만 **54,135** 다.
   ⇒ **T130 §ⓐ 의 곡물 가격 분해도 이제 우회 없이 다시 읽을 수 있다**(그 카드가 `target` 에서 되읽던 몫이 칸으로 있다).
   그 재독은 다음 카드 몫이다(구현 금지).
4. ★**[#37 · 재민] `:2729` 활대 `_cons`** — 이 카드가 장부를 닫았으므로 **장부를 이유로 붙일 필요는 사라졌다**.
   붙일 이유는 계약 일관성뿐이고, 붙이면 사건 장부 문턱이 움직인다(세계 변경).
   같은 족속으로 `economy-sim.js:2747~2750` 활 보조재(깃·수지·삼)도 칸이 없다.
5. ⓘ**앞 카드 회부 유효**: #31 모으기(재민 · 실기 뒤) · #6 실기 한 줄 · 마이그레이션 둘 A 땅 · `어촌6` 좌표 ·
   `onboarding.js:398` 나루터 폴백(T287 세션4).
