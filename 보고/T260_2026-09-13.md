# 보고 — T260 굽는 상자 표식 · webp 디코더 후보 표 (2026-09-13 · 세션8/ART)

**베이스 `15de6885`** · 고친 파일 넷(`render_common.py` · `asset-lock.js` · `icons.lock.json` · `인계/ART-자산.md`) + 보고
**그림·소리 무변** — 미디어 671장 sha1 `c10ec48c…` 카드 전후 동일 · **`package.json`·`package-lock.json` 무변**
배포 설정·클라 0줄 · 지우기 0 · **적용 0**(② 는 표만) · 실기 0 · 러너 전수 0

## ★ 답 한 줄

**굽기 진입점 13(공용 한 곳에서 한 줄) · 상자 줄 ○ · 미디어 동일 · webp 디코더 후보 4 중 쓸 것 3 · 권고 ⓐ(`@cwasm/webp` · `npm ci` +0.0s/+160KB · 88장 전수 다른 디코더와 일치)**

---

## §0-ⓐ 굽는 상자 표식

### 진입점 — 전수 13, 고친 곳은 **한 곳**

`bpy` 를 부르는 스크립트 10 + `render_common` 을 부르는 후처리 3 = 13. 그중 **12가 `render_common` 을 지난다**.
`build_scene()` 하나가 모든 bpy 판의 문이므로 **거기 한 줄**이면 전부 찍는다 — 진입점마다 고칠 필요가 없다.

| 무리 | 스크립트 | 상자 줄 |
|---|---|---|
| bpy + `render_common` | `bake-mountain` · `bridge_render` · `char_mpfb_screen` · `char_render` · `fields_render` · `icon_render` · `models_crops` · `props_render` · (`nature_render`·`building_render` 는 `rc` 통해 bpy) | **○** `build_scene()` 이 찍는다 |
| `render_common` 만(후처리) | `ink_repost` · `nature-postprocess` | ○ — 씬을 안 지으면 안 찍는다(그림을 안 굽는 판이라 맞다) |
| bpy 인데 `render_common` 밖 | **`bake-terrain-tex.py`** | **✗** — `blender -b -P` 로 도는 독립 판(지형 질감 4장). 이 카드는 공용 한 줄만 손댔으므로 **안 건드렸다**(회부) |

```
$ python3 scripts/nature_render.py -- bush01
[nat] bpy 5.0.1 denoise = False samples = 64 ppu = 45.255 zsq = 0.8165
[box] cpu=Intel(R) Xeon(R) Processor @ 2.80GHz · threads=2 · simd=fma avx avx2 avx512f avx512dq
      avx512bw avx512vl avx512_vnni · bpy=5.0.1 · py=3.11.15 · commit=15de6885
```

### 메타 한 키 — `icons.lock.json._상자`

`asset-lock.js --write` 가 **같은 꼴**을 남긴다(자가 하나이듯 표식도 하나). 이름을 정직하게 붙였다:
그 칸은 **이 표를 다시 쓴 상자**이지 그림을 구운 상자가 아니다.

★**앵커 JSON 에는 안 넣었다 — 넣으면 하네스가 빨개진다.** `test-icons.js:488` 이
`Object.keys(mountain_anchors)` 를 파일 88장과 **전수 대조**한다. 거기 `bakeBox` 키가 하나 붙으면
89 ↔ 88 로 어긋난다. 앵커에 상자를 실으려면 그 대조부터 `_` 키를 거르게 고쳐야 한다 — 이 카드 밖(회부).

### 자명 통과 금지 — 표식이 그림을 건드리는가

| | `bush01` 생굽기 화소 해시 |
|---|---|
| 무표식 판(T257 · 같은 상자) | `0eba77eb` |
| **표식 판**(이 카드 · 같은 상자) | **`0eba77eb`** |

**같다.** 표식은 찍기만 한다. (미디어 671장 sha1 도 카드 전후 `c10ec48c…` 동일.)

### 아는 상자 둘 — 모르는 칸은 비운다

| 판 | cpu | threads | simd | bpy | py | commit | `bush01` 생굽기 |
|---|---|---|---|---|---|---|---|
| **T243** | *(안 남겼다)* | *(모름)* | *(모름)* | 5.0.1 | *(모름)* | `aa382c69` | **`b8a8e18f`** |
| **T257·T260** | Intel Xeon @2.80GHz | 2 | fma avx avx2 avx512f/dq/bw/vl avx512_vnni | 5.0.1 | 3.11.15 | `bae3a110`·`15de6885` | **`0eba77eb`** |

**원인은 여전히 못 잡았다** — T243 칸이 비어 있기 때문이다. 이 규약은 **그 칸이 다시는 안 비게** 하려고 있다.
⚠흔들리는 것은 **생굽기**다. T257 이 후처리까지 거친 결과는 배포판과 **바이트까지 같았다**(4/4) —
이 흔들림이 배포 자산을 망가뜨린 적은 아직 없다.

## §0-ⓑ webp 88장 화소 해시 길 — 후보 표 (#32 자료 · 적용 0)

기준선: 레포 `npm ci` **5.5s · 73,956 KB**(임시 워크트리 · 레포 `package*.json` 무변).
88장 = `public/assets/mountains/*.webp`.

| 후보 | 판 | 종류 | 라이선스 | 단독 설치 | **`npm ci` Δ** | **88장 디코드** | 두 번 같은가 | 오프라인 |
|---|---|---|---|---|---|---|---|---|
| **`@cwasm/webp`** | 0.1.5 | wasm | **MIT** | 168 KB | **+0.0s · +160 KB** | 2,279 ms | ✓ | **✓** |
| `webp-wasm` | 1.0.6 | wasm | MIT | 560 KB | +0.1s · +552 KB | **1,674 ms** | ✓ | ✓ |
| `sharp` | 0.35.4 | 네이티브(libvips) | Apache-2.0 | 29,788 KB | +1.7s · **+46.9 MB** | 2,441 ms | ✓ | ✓ |
| `@jsquash/webp` | 1.5.0 | wasm | Apache-2.0 | 1,068 KB | — | **✗ 실패** | — | **✗ 런타임 `fetch`** |

`@jsquash/webp` 는 실행 시점에 wasm 을 **네트워크로 받는다**(`TypeError: fetch failed`). CI 에서 못 쓴다.

### 디코더가 자가 될 수 있나 — **된다**

손실 webp 는 디코더마다 다를 수 있다는 게 걱정거리였다. **재 봤다**:

* 같은 디코더로 두 번 → **셋 다 같은 값**(자로 쓰려면 이것만으로 충분하다).
* **다른 디코더끼리도 같다** — `sharp`(libvips) ↔ `@cwasm/webp`: **88장 중 88장 일치 · 불일치 0**.
  `webp-wasm` 의 첫 장도 같은 값(`2f261071…`)이다.

⇒ 이 88장에 대해서는 **디코더를 갈아 끼워도 잠금 값이 안 바뀐다.** 자로 쓰기에 안전하다.
(표본은 이 88장이고, 다른 webp 에까지 일반화하지 않는다.)

### 갈래 둘의 값 — 재민에게 줄 한 줄씩

| 갈래 | 무엇 | 값(비용) | 잃는 것 |
|---|---|---|---|
| **ⓐ CI 의존성** | `@cwasm/webp` 를 `devDependencies` 에 하나 · `asset-lock.js` 가 webp 도 화소 해시로 | `npm ci` **+0.0s · +160 KB**(5.5s·73.9MB → 5.5s·74.1MB) · MIT · 오프라인 됨 | 의존성 하나가 는다(`package.json` 변경 = 재민 몫) |
| **ⓑ 야간 묶음 이동**(PM 권고) | 산 88장 잠금 대조를 `test-icons` 에서 떼어 CI 밖 하네스로 | `package.json` 0 | **push 마다 안 본다.** 야간 창은 09-13 실측 3h43m 에 112/143 까지라 산이 그 안에 든다는 보장이 없다 — 산 잠금이 **며칠 안 돌 수 있다** |

★**측정이 권고를 바꾼다.** PM 권고는 ⓑ 였는데, ⓐ 의 실측 비용이 **`npm ci` +0.0초 · +160KB** 다.
ⓑ 가 지키려던 것("CI 를 가볍게")을 ⓐ 가 거의 공짜로 지키면서 **push 마다 보는 것**까지 남긴다.
그래서 이 표는 **ⓐ 를 권한다**(`@cwasm/webp`). 고르는 것은 재민이다(#32).

## §1 검증

| | |
|---|---|
| 그림·소리 | 미디어 671장 sha1 **`c10ec48c…` 카드 전후 동일** |
| `package.json`/`package-lock.json` | **무변**(`git status` 0줄 — 후보 설치는 전부 임시 폴더) |
| 배포 설정·클라 | `.dockerignore`·`Dockerfile*`·`index.html`·`client/*` **0줄** |
| 표식이 그림을 건드리나 | **안 건드린다**(`bush01` 무표식 = 표식 `0eba77eb`) |
| `asset-lock --check` | **452장 · 어긋남 0** |
| `test-icons` / `test-props` / `test-assets-audit`(56) / `test-harness-lint` | 전부 초록(§ 아래 실행 표) |
| 러너 목록 | 143 그대로 · 야간 창 무변 |
| 3시드 | 면제 — diff ∩ 랩 적재 17파일 = ∅ |
| 접점 | `asset-lock` · `rulerOf` · `_규약` · `render-meta.json` · `bakeBox` · `cpuinfo` · `bpy` · `test-icons` · `test-assets-audit` · `package.json` |

## §2 회부

1. **#32 webp 자(재민)** — 위 표. **권고 ⓐ `@cwasm/webp`**(MIT · `npm ci` +0.0s/+160KB · 오프라인 ·
   다른 디코더와 88/88 일치). `package.json` 변경이라 재민 한 글자.
2. **`bake-terrain-tex.py` 는 상자 줄이 없다(세션8 · 다음 굽기 카드)** — `blender -b -P` 로 도는
   독립 판이라 `render_common` 을 안 지난다. 지형 질감 4장을 다시 구울 때 같이 붙인다.
3. **앵커에 상자를 실으려면 `test-icons.js:488` 부터(PM)** — `Object.keys(mountain_anchors)` 전수 대조가
   `_` 키를 안 거른다. 거르게 고치면 앵커에도 상자를 실을 수 있다.
4. **#30 처분(재민 · "ⓒ")** · #29 대표↔평균(재민) · #14 크레딧 ⓐⓑⓒ(재민) · 소리 층 다시 잇기(ⓑ 뒤) ·
   먹선 · 상추/아욱 26px — 그대로.
