# 보고 — T243 배포 자산 전수 감사 (2026-09-13 · 세션8/ART)

**베이스 `aa382c69`** · 새 파일 **둘** — `scripts/test-assets-audit.js`(하네스 하나) · `보고/T243_자산전수_2026-09-13.json`(전수표 711행)
**자산 무변** — `public/assets` 711장 sha1 전수가 카드 전후로 **한 바이트도 안 달라졌다**(아래 §2) · 추가 0 · 삭제 0
굽기 재실행은 **임시 워크트리**(`git worktree`)에서만 · 배포 자산 무접촉 · 배포 0 · 실기 0 · 러너 전수 0

## ★ 답 한 줄

**자산 711 · 78.1 MB · 고아 55(45.7 MB · 전부 `audio/`) · 잠금 불일치 0 · 없는 참조 0 · 재현 표본 IDAT 0/4 · 그림 4/4**

---

## §0-ⓐ 전수표 — 711장

전문은 `보고/T243_자산전수_2026-09-13.json`(행마다 `rel · dir · ext · bytes · sha1 · refs[] · orphan`).

| 디렉터리 | 장 | MB | 잠금 | 고아 |
|---|---:|---:|---:|---|
| `audio/` | 56 | 45.72 | — | **55장 45.7 MB** |
| `trees/` | 34 | 12.68 | 33 | 0 |
| `char/` | 195 | 7.29 | 192 | 0 |
| `mountains/` | 90 | 5.30 | 88 | 0 |
| `nature/` | 45 | 2.48 | 44 | 0 |
| `icons/` | 148 | 1.57 | 148 | 0 |
| `buildings/` | 14 | 1.06 | 13 | 0 |
| `terrain/` | 4 | 0.74 | 4 | 0 |
| `bridge/` | 21 | 0.60 | 21 | 0 |
| `crops/` | 81 | 0.45 | 80 | 0 |
| `props/` | 22 | 0.14 | 21 | 0 |
| (뿌리 `icons.lock.json`) | 1 | 0.02 | — | 0 |
| **합** | **711** | **78.07** | **644** | **55 · 45.7 MB** |

**사용처 판정은 전이적이다**(이게 이 감사의 뼈대다). 뿌리는 `public/client/*.js`·`public/*.{js,html,css}`·`server/**`·`sim/**`·`scripts/**`.
자산 안의 표(`*_anchors.json`·`*.lock.json`·`tree_species.json`)는 **제가 뿌리에 닿아야** 참조 원천이 된다.

> 이 한 줄이 45.7MB 를 갈랐다. 1차 판(전이 없음)은 고아를 **31장 4.7MB** 로 봤다 — `.ogg`·`.m4a` 26장이
> "참조 있음"이었기 때문이다. 그런데 그 참조는 **같은 폴더 안의 `gayageum-usage.json`·`render-meta.json`
> 둘뿐**이었고, 그 둘 자신이 아무 데서도 안 닿는다. **제 폴더 안에서 저희끼리 가리키는 것은 참조가 아니다.**

## §0-ⓑ 판정 열

### ① 고아 55장 · 45.7 MB — **전부 `public/assets/audio/`**

`audio/` 56장 중 **55장이 고아**다(닿는 1장도 같은 폴더의 표라 실질 0). 확인한 것:

```
$ grep -n "bgm\|audio\|\.ogg\|\.m4a" public/index.html      → 0줄
$ grep -rn "bgm\|/assets/audio" public/client/*.js           → 0줄
$ grep -rln "assets/audio\|bgm" server/*.js scripts/*.js     → 0파일
```

**클라가 소리를 한 줄도 안 부른다.** 그런데 `public/**` 는 배포에 그대로 실린다(`redeploy-hanbando.sh:65` —
`public/` 이 바뀌면 `DO_CENTRAL=1`). 즉 **배포 자산의 58.6% 가 게임이 안 쓰는 것**이다.

내역(고아 55장):

| 무엇 | 장 | MB | 비고 |
|---|---:|---:|---|
| `.ogg`/`.m4a` 음원 | 26 | 44.1 | 그중 `village_day_jeongak.{ogg,m4a}` 둘은 **표에도 없다**(다른 24장은 `render-meta.json` 이 적는데 이 둘은 빠져 있다) |
| `.py` 굽는 스크립트 | 20 | 0.24 | `sampler.py`(76KB) · `compose.py`(33KB) · `gugak.py`(31KB) … **소스가 배포에 나간다** |
| `.js` | 2 | 0.06 | `bgm.js`(54KB) · `bgm-loops.js` — `index.html` 이 안 부른다 |
| `.zip` **코드 백업** | 2 | 0.31 | `코드백업.zip` · `코드백업_최신.zip` — 백업이 공개 경로에 있다 |
| `.md`/`.json`/`.txt`/`.gitignore` | 5 | 0.01 | `README-BGM.md` · `국악기_음원_넣는법.md` · `render-meta.json` · `gayageum-usage.json` · … |

★**지우지 않았다.** 카드 규약이고, 옳다 — 이건 "죽은 자산"이 아니라 **"아직 안 배선한 자산"** 일 수 있다
(`bgm.js` 가 54KB 짜리 완성품으로 앉아 있는 것이 그 증거다). 처분은 재민 몫이다(§3 회부).
다만 `.py`·`.zip`·`.md` 는 **소리를 켜더라도 배포에 나갈 이유가 없다** — 그건 별개의 줄이다.

### ② 잠금 불일치 **0** — 잠긴 644장

| 잠금표 | 잠긴 장 | 자 | 불일치 |
|---|---:|---|---:|
| `icons.lock.json` (9군: bridge 21·buildings 13·crops 80·icons 148·mountains 88·nature 44·props 21·terrain 4·trees 33) | 452 | PNG=**IDAT sha1[:16]** · webp=파일 sha1[:16] | **0** |
| `char/char_sheets.lock.json` | 192 | **파일 sha256[:16]** (키는 `char_meta.json.sheets` − `probeall*`) | **0** |

★**자를 틀리게 잡아 없는 결함을 볼 뻔했다.** 1차 판은 `icons.lock` 의 자(IDAT sha1)를 char 시트에 들이대고
**192장 전부 불일치**를 봤다. 틀린 건 시트가 아니라 자였다 — `test-charsheet.js:305` 가 굽는 자는 `sha256` 이다.
그래서 하네스는 이제 **자를 표마다 정본에서 받아 온다**(계약 ②). 표 하나에 자 하나.

안 잠긴 장도 **이름을 댄다**: `char/probeall_walk.png` 하나(잠금 대상 밖 — `test-charsheet` 가 `probeall*` 를 뺀다).
193 − 192 = 1 이 설명 없이 남지 않는다(하네스 ②b).

### ③ 없는 참조 **0** — 표 7개 · 키 644 전수

`icons.lock.json`(9군) · `char_sheets.lock.json` · `crops_anchors` · `nature_anchors` · `props_anchors` ·
`mountain_anchors` · `building_anchors` 가 가리키는 키마다 파일이 있다. 404 는 배포 뒤에야 보이는 것이라 소스 층에서 먼저 잡는다.
(`nature_anchors` 는 나무·풀을 한 표에 담으므로 `nature/`·`trees/` 둘 다 본다 — 안 그러면 나무 키가 거짓 결함이 된다.)

### ④ 재현성 표본 — **IDAT 0/4 · 그림 4/4**

임시 워크트리에서 다시 구워 배포판과 견줬다(배포 자산 무접촉).

| 표본 | 굽는 길 | IDAT | 다른 화소 | \|Δ\|>24 | 평균\|Δ\| | 평균RGB 거리 |
|---|---|---|---:|---:|---:|---:|
| `nature/bush01.png` | `nature_render -- bush01` → `nature-postprocess --only bush01` | ✗ | 50.6% | **0.49%** | 0.909/255 | **0.005** |
| `icons/acorn.png` | `NAT_ICONS=1 nature_render -- acorn` → `icons-postprocess` | ✗ | 11.1% | **0.01%** | 0.057/255 | **0.006** |
| `crops/lettuce_2.png` | `FIELDS_SPECIES=lettuce fields_render` | ✗ | 24.3% | **0.04%** | 0.167/255 | **0.018** |
| `crops/lettuce_3.png` | 〃 | ✗ | 21.8% | **0.07%** | 0.120/255 | **0.005** |

**★대조군이 이 표의 뜻을 뒤집는다.** 같은 코드로 `bush01` 을 **연달아 두 번** 구웠더니 IDAT 가 **같았다**
(`24ed59e93cb2de31` = `24ed59e93cb2de31`). 즉 **굽기는 결정적이다.** 그러므로 위 ✗ 넷은 Cycles 잡음이 아니라
**배포판을 구운 판이 지금 판이 아니라는 뜻**이다(드리프트).

자로 재면 그 드리프트는 **눈에 안 보인다**: `|Δ|>24` 가 0.01~0.49% 로 **T205 잡음 바닥(같은 종·다른 씨앗 5.3%)의
1/10 아래**이고, 평균RGB 거리는 0.005~0.018 로 **T201 문턱 10 의 1/500 아래**다. 크기도 앵커도 같다.

⇒ 두 문장으로 갈라 적는다.
* **그림은 재현된다** — 지금 코드로 구운 것과 배포판이 자로 구분이 안 된다(4/4).
* **바이트는 재현 안 된다** — 그래서 `icons.lock.json` 의 `_` 가 적은 *"다음 재굽기가 이 표와 대조한다"* 는
  **지금은 성립하지 않는 약속**이다. 재굽기는 반드시 이 표를 깬다. 이 잠금이 실제로 잡는 것은
  **"파일이 몰래 바뀌었는가"** 이고, 그 뜻으로는 지금 **초록**이다(②). 문구는 PM 판정거리(§3).

## §0-ⓒ 하네스 — `scripts/test-assets-audit.js`

**`// @regress` 표식을 일부러 안 달았다** — 전수 창이 모자라는 판이라(공통 §2 ⑥ · T185·T234) 야간 목록을 안 늘린다.
확인: `--list` 는 그대로 **143**(`grep -c assets-audit` → 0). 자산을 만지는 카드가 손으로 부른다.

| 절 | 무엇 | 결과 |
|---|---|---|
| ① | 없는 참조 0 (표 7개 · 키 644) | ✓ |
| ② | 잠금 불일치 0 (644장 · 자는 표마다) | ✓ |
| ②b | 안 잠긴 char 시트는 전부 `probeall*` — 설명 없는 자산 0 | ✓ `probeall_walk` |
| ③ | 고아 표 — **빨강 아님**(세어서 표로만) | 55장 45.7 MB |
| ④ | 닿음은 전이적 — 표가 닿아야 그 표의 이름이 참조다 | (①③의 뼈대) |

**자명 통과 금지**(`--selftest`): 없는 참조 하나(`icons/__selftest_ghost__`)와 잠금 어긋남 하나를 **주입**한다.
→ `①✗ ②✗` 둘 다 물었다 · `PASS(검사기가 둘 다 물었다 · 2건)`.

고아를 빨강으로 안 만든 이유를 하네스 주석에 적어 뒀다 — 지우기를 하네스에 맡기면
**"아직 안 배선한 새 자산"이 빨개진다. 그건 결함이 아니라 순서다.**

## §1 검증

* **자산 무변** — 카드 전후 `find public/assets -type f | xargs sha1sum` 파일의 sha1 이 **동일**:
  `d40918e4ba97911c82c06836d5447410c1ffb48b` (711줄) ↔ 같은 값. 추가 0 · 삭제 0.
* `git status --porcelain` → 새 파일 둘뿐. `public/assets` 에 `M` 이 하나도 없다.
* **굽기는 `git worktree` 안에서만** 돌았다 — `fields_render.py` 는 배포 자리로 **복사까지 하는** 스크립트라
  본 트리에서 돌리면 안 된다(카드 규약). 워크트리가 그 위험을 구조로 없앴다.
* `test-harness-lint` **41/0** · `test-crops-world` **PASS** · `test-assets-audit` **PASS** · `--selftest` **PASS**.
* `--list pixel` 17종은 **면제** — 근거는 위 sha1 전수 무변이다(픽셀 하네스가 보는 그림이 한 장도 안 바뀌었다).
* 3시드 면제 — diff ∩ 랩 적재 17파일 = ∅(새 하네스 하나 + 보고 둘).
* **T238(세션2)·T242(세션7) 무접촉** — `git status` 가 새 파일 둘뿐인 것이 증거다.
* 접점 심볼: `public/assets` · `icons.lock.json` · `crops_anchors.json` · `trees.json` · `crops.json` ·
  `fields_render` · `nature_render` · `build-trees` · `assert_pinned_box` · `test-crops-world`

## §2 회부

1. **고아 55장 45.7MB 처분(재민)** — 세 갈래가 **다른 물음**이다:
   ⓐ 음원 26장(44.1MB) — 켤 것인가 뺄 것인가(`bgm.js` 54KB 가 완성품으로 앉아 있다) ·
   ⓑ `.py` 20장·`.js` 2장·`.zip` 2장 — **소리를 켜더라도** 배포에 나갈 이유가 없다(굽는 소스·백업) ·
   ⓒ `village_day_jeongak.{ogg,m4a}` 둘은 `render-meta.json` 에도 없다(표 밖 음원).
2. **`icons.lock.json` 의 `_` 문구(PM)** — *"다음 재굽기가 이 표와 대조한다"* 는 성립하지 않는다(§0-ⓑ ④).
   실제 뜻은 "몰래 바뀜 감지"다. 문구를 그 뜻으로 고칠지, 아니면 재현 가능한 자(예: 화소 문턱)로 바꿀지가 판정거리.
3. **드리프트의 출처** — 지금 코드가 굽는 그림과 배포판이 미세하게 다르다(눈엔 같다). 언제 갈렸는지는 안 팠다.
   다시 구워 배포할 카드가 생기면 그때 이 표가 기준선이 된다.
4. #29(대표↔평균 · 재민) · 상추/아욱 26px(재민 눈) · 먹선(T116 뒤) · 갯벌 채집물 · 크레딧 #14 · 오디 #15 — 그대로.
