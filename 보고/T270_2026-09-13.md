# 보고 — T270 2026-07-12 판정의 근거를 오늘 수로 (2026-09-13)

한 줄: **주석 갱신 두 자리(옛 줄 보존) · 비트 동일 ○ · `s101` 은 레포에 있다 — 시드 101 · CLI 앙상블 ·
**재현 ✗**(오늘 엔진: `425 → 463` **+8.9%** · 네 시드 전부 **소멸 0**) · `_consEMA` 읽는 여섯 자리 중
**가드 없는 자리 셋**(게시판 둘 + `ramie`) · `CENTRAL_SECRET` 절 한 쪽.**

★★**s101 이 오늘 엔진에서 재현되지 않는다.** 2026-07-12 의 `245 → 27`(−89% · 마을이 죽었다)이
같은 시드에서 `425 → 463`(**+8.9%** · 소멸 0)로 나온다. 판정의 **근거 문장은 낡았고**, 그런데
**판정을 뒤집을 근거도 아니다**(네 시드 부호가 갈린다 · T252 자로 **못 가름**). 둘 다 적는다.

---

## §0-ⓐ 주석 갱신 — 두 자리 · 옛 줄은 안 지운다

| 자리 | 종전 | 덧붙인 것 |
|---|---|---|
| `sim/economy-sim.js:596`(T73 생곡 가지) | *"…실측 s101 245→27"* | `★[T270] 그 실측의 오늘 값은 이 함수 끝 주석에 있다(옛 줄은 안 지운다 · 족보)` |
| `:634`(`consumeFood` 끝) | *"실측: 식단 폴드 포함 시 s101 245→27 붕괴(2026-07-12)…"* | T263 실측 4줄 — 인구 −12.4/−13.7/−1.9%(**방향만**) · **소멸 0** · ⚠**다른 세계다**(그때는 CLI 앙상블 6~8마을 · T263 은 실지도 51마을) · **판정 유지** · 손잡이 `T263_FOOD_CONS` 기본 끔 |

**비트 동일** — 시드 1020 · 800일 · 51마을 여덟 수: 인구 **6,889** · 소멸 **0/51** · 무기Q **1,012** ·
확장셀 **5,509** · 게시 **2,539** · 일/건 **1.93** · 도구Q **11,892.1** · 보존식 **803.6** — 셋째 판과 한 자도 안 다름 ✅
(주석은 코드가 아니다 — 생성물 셋은 `--check` 로 최신 확인: 번들 **8/0** · 인라인 둘 다 최신 · 배선 **0/0**.)

## §0-ⓑ `s101` — **찾았다 · 재현은 안 된다**

### ⓑ-1 `s101` 이 무엇인가

`문서/아카이브/CHECKLIST.md:161`(2026-07-12 후속 세션 · flow-EMA target 채택)이 전문을 갖고 있다:

> ①**식단 폴드 금지** — consumeFood는 가용성 기반 *대체* 소비(있는 걸 먹음)라 폴드하면 우연한 믹스가
> 30일 보유 수요로 고착 → 빈곤 마을이 제 어획·채집물을 못 팖(**s101 245→27 붕괴·제외로 243 회복**).
> 구 시도 사이트 목록에 '_foodEaten 폴드' 있었음 — s8 사인의 유력 진범.

같은 줄에 나머지 둘도 있다: ②**subs 가드**(`s505 576→17 붕괴·가드로 542 회복`) ·
③**하급 연료·자갈 제외**(`s202 44→679 회복`). 그리고 같은 배치의 앙상블 표:
`101 191→245(+28%) · 202 477→679 · 303 42→34 · 505 576→623`.

⇒ **`s101` = 시드 101**, "신선 앙상블" 네 시드(101·202·303·505) 중 하나. 오늘 그 시드를 도는 러너는
`scripts/econ-regress.js`(기본 `42,7,19,101,256`)이고, 실제 판은 **`sim/economy-sim-v2.js` CLI · 800일 · 6마을**이다.
⚠**한반도 51마을 실지도가 아니다.** T263 이 잰 세계와 **다른 세계**다 — 그래서 T263 의 −9.3% 와
2026-07-12 의 −89% 는 **애초에 같은 자로 잰 수가 아니었다.**

### ⓑ-2 재현 시도 — 같은 시드 · 오늘 엔진 · `T263_FOOD_CONS` 끔/켬

`node sim/economy-sim-v2.js 800 6 <시드>` (레포 코드 0 · 손잡이만 바꾼다)

| 시드 | 인구 끔 → 켬 | Δ | 마을 생존 | 소멸 |
|---|---|---|---|---|
| **101** | **425 → 463** | **+8.9%** | 6/6 → 6/6 | 0 → 0 |
| 202 | 376 → 341 | −9.3% | 6/6 → 6/6 | 0 → 0 |
| 303 | 599 → 697 | +16.4% | 6/6 → 6/6 | 0 → 0 |
| 505 | 433 → 409 | −5.5% | 6/6 → 6/6 | 0 → 0 |

**재현 ✗.** `245 → 27`(−89%)은커녕 **인구가 늘었다**(+8.9%). 네 시드 부호가 갈리고(+8.9/−9.3/+16.4/−5.5)
**소멸이 한 곳도 안 났다** — T252 자로 **못 가름**.

### ⓑ-3 그 붕괴는 어디로 갔나 — **후보만**(단정 안 한다)

★가장 그럴듯한 자리는 **같은 배치 안**에 있다. 2026-07-12 의 실패 판은 `_foodEaten` 폴드를
**subs 가드 ②·하급 연료 제외 ③ 없이** 넣은 판이었다(위 인용의 "구 시도 사이트 목록"). 오늘 엔진은
②③이 **이미 들어 있고**, T263/T270 의 켬 팔은 그 위에 ①만 얹는다 — **같은 실험이 아니다.**

그 뒤로 사다리를 지난 것들(후보 · `git log -- sim/economy-sim.js`): `T86` 가격 가중 지출 몫(식단 배정 자체를
바꿨다) · `T73` 생곡 편입 · `T17` 보존식 편입 + 식량등가 · 시장 충격 정산(적분) · `T164/T184` 실현 배분 ·
`T100` 밭 산출. ⚠**어느 하나로 못 짚는다** — 짚으려면 각 커밋에서 같은 A/B 를 돌려야 하고 그건 이 카드 밖이다.

## §0-ⓒ 가드 비대칭 — `_consEMA` 읽는 **여섯 자리 중 셋에 가드가 없다**

| # | 기계 | 자리 | `subs` 가드 | 지금(끔) 무엇을 보나 |
|---|---|---|---|---|
| 1 | 그림자가격 목표 재고 | `economy-sim-v2.js:362` | **있음** | 등재 18종은 `subs×30` · 비등재 7종은 **0** |
| 2 | 가격 target(동기 계약) | `:470` | **있음** | 〃 |
| 3 | 과잉부패 보호(`tickDecay`) | `:1252` | **있음** | 〃 |
| 4 | **부족/글럿 판정 → 게시판** | `server/events.js:450·595` | **없음** | `ema > 0 &&` ⇒ **EMA 0 이면 부족도 글럿도 아니다** |
| 5 | **마을 의뢰 품목 선정** | `server/villages.js:6322` | **없음** | `!(e > 0) → continue` ⇒ **후보에서 빠진다** |
| 6 | `ramie` 잉여 스킵 | `economy-sim.js:2677` | 없음 | `ramie` 만 |

★**비대칭의 값**: 가드 있는 셋은 *"등재면 subs 로 대신 본다"* 라 **대체 경로가 있다**. 가드 없는 둘(#4·#5)은
대체 경로가 없어서 **EMA 0 = 그 품목은 이 마을에 존재하지 않는 것**이 된다.

지금(끔) 세계에서 비등재 7종의 `_consEMA > 0` 마을 수 (T263 base 팔 실측 · 살아 있는 51마을 기준):

| 시드 | `fish` | `meat` | `fruit` | `vegetable` | `walnut` | `grape` | `twig` |
|---|---|---|---|---|---|---|---|
| 1020 | 30 | 16 | **2** | **1** | **0** | **0** | **0** |
| 7 | 32 | 19 | **0** | **0** | **0** | **0** | **0** |
| 42 | 29 | 14 | **1** | **0** | **0** | **0** | **0** |

⇒ `fish`·`meat` 는 절반쯤 보인다(**보존식 가지의 `_cons` 덕분**이다 — 말리는 마을만 보인다).
`fruit`·`vegetable`·`walnut`·`grape`·`twig` 는 **사실상 0곳** — 마을이 과일을 먹어도 **게시판은 과일 부족을 못 낸다.**
⚠이 줄은 **EMA 값과 `ema > 0` 게이트에서 유도한 것**이고, 게시판 품목 집계를 직접 센 것이 아니다
(이 카드는 **코드 0** 이라 계측기에 집계를 못 붙였다). 세려면 계측기 한 줄이 필요하다 — 회부 §1-2.

★**캐논 질문(재민)**: 가드를 **여섯 자리에 같이** 둘 것인가, 아니면 **게시판 둘만 식사를 보게** 할 것인가.
후자는 "가격·부패는 종전 그대로 두고 게시판만 눈을 뜬다"는 좁은 처방이고, T263 의 `일/건 −43.9%`(가름)가
**거의 전부 그 둘에서 나온다**는 뜻이기도 하다. 값은 이 보고가 안 정한다.

## §0-ⓓ `CENTRAL_SECRET` 절

`인계/N-네트워크.md` 에 **N-비밀** 한 절을 썼다: 언제 필요한가(호스트가 갈릴 때 · 프록시가 XFF 를 붙일 때 ·
다른 네트워크) · 어디에 어떻게(`redeploy-hanbando.sh` 가 env 를 **물려받으므로 처음 한 번만 손으로** ·
두 컨테이너에 **같은 값**) · 안 넣었을 때의 동작(**지금 그대로** — 안 넣어서 뚫리는 게 아니라 갈리는 날 막힌다) ·
배포 뒤 확인 줄 둘(바깥 `POST /kick_player` **404** · 컨테이너 안에서 **200**) · ②가 404 일 때의 원인 셋.

## §0-ⓔ 답 한 줄

> **주석 갱신 두 자리(옛 줄 보존) · 비트 동일 ○** · **`s101` = 시드 101 · CLI 앙상블 6마을(실지도 아님) ·
> 재현 ✗ — `425 → 463`(+8.9%) · 네 시드 부호 갈림 · 소멸 0** · 가드 없는 자리 **셋**(게시판 둘 + `ramie`) ·
> `CENTRAL_SECRET` 절 한 쪽 · **판정은 유지, 근거 문장만 갱신.**

## §1 회부

1. **#33(재민 · PM 권고 판정 유지)** — 이 카드가 더한 것 둘: (ⓐ) **근거가 낡은 정도가 측정됐다**
   — 같은 시드에서 −89% 가 **+8.9%** 다. (ⓑ) **그래도 뒤집을 근거는 아니다** — 네 시드 부호가 갈린다.
   ⚠그리고 **T263 과 2026-07-12 는 다른 세계였다**(51마을 실지도 vs CLI 6마을). 앞으로 이 판정을 논할 때는
   **어느 세계에서 잰 수인지**를 같이 적어야 한다.
2. **게시판 품목 집계 한 줄**(계측기) — §0-ⓒ 의 "게시판이 과일 부족을 못 낸다"를 **유도가 아니라 집계로**
   확인하려면 계측기에 보드 품목 탤리 한 줄이 필요하다. 이 카드는 코드 0 이라 안 했다.
3. **게시판 가드(캐논 · 재민)** — 여섯 자리에 가드를 같이 둘지, 게시판 둘만 식사를 보게 할지.
4. #21 ⓑ · 부재료 소비량 `0.5` · 열량 회수 캐논 · 흐름 문턱 `3`.
5. **#27 배포 뒤 확인** — `CENTRAL_SECRET` 은 지금 **안 넣어도 된다**(N-비밀 절). 배포 뒤 실기 둘은 T245 그대로.
6. #2 `/check_username` · #9 `?as=`.

## §2 검증

- **코드 0** — 바뀐 것은 `sim/economy-sim.js` 의 **주석 여섯 줄**뿐(그리고 생성물 셋이 따라 바뀐다).
- **비트 동일** — 시드 1020 · 800일 · 51마을 여덟 수가 셋째 판과 한 자도 안 다름 ✅.
- 게이트 셋 — 번들 `--check` **8/0** · 인라인 `--check` 둘 다 최신 · 배선 **실패 0 · 경고 0**([H] 10개).
- s101 재현은 **레포 코드 0** — `sim/economy-sim-v2.js` CLI 를 손잡이만 바꿔 8판 돌렸다(전부 종료코드 0).
  ⚠그 CLI 는 `sim/out/simv2-<시드>-800d.json` 을 쓴다. 추적 파일 하나(시드 101)를 덮어써서
  **`git checkout -- sim/out/` 으로 되돌렸고**, 새로 생긴 셋(202·303·505)은 지웠다 — 레포에 남은 것 0.
- 손잡이 둘 다 **기본 끔** 그대로 · 새 수 **0** · 켜지 않았다.
- 러너 전수 안 돌렸다(카드 규약).
- 접점 심볼: `consumeFood` · `_cons` · `_consEMA` · `s101` · `flowT` · `SUBSISTENCE_PER_NPC` ·
  `events.js` · `CENTRAL_SECRET` · `postJSON` · `redeploy-hanbando`.
