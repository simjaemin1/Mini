# 보고 — 야간 러너 2026-09-14 (KST)

**기준 커밋 `a76c06cd`** (`보고/T268 머리·§5 — 푸시 해시 a66fbb79 · 측정 베이스 99b4a76a · 푸시 베이스 e3a96e0d · 가지 batch/chronicle-line-0913`)
· 컨테이너 새 클론(`/tmp/minirepo`) · `npm install` · 러너 자가 검사 **통과**
· 시작 **04:09** → 종료 **07:31** (KST · 3시간 22분)
· 전수 목록 **144종**(`grep -l '^// @regress'`) · **오늘 묶음 = 야간 C**(`nightly-split.sh auto` · KST 날짜 기준 · 단위 96 + e2e 16 = **112종**)
· **돈 것 99** · 통과 **96** · 실패 **3** · **미측정 13**(전부 e2e — §3)
· 병렬 0 · 러너 동시 2개 0 · `pkill -f` 는 러너 제 `drain()` 것만(내 손은 `kill <pid>`) · 코드·하네스 수정 0 · 커밋은 이 문서 하나

## ★ 한 줄 — **상시 밖 빨강 0건.** 단위 96종은 완주(94/96 · 빨강 둘 다 공통.md 상시 목록), e2e 는 C 묶음 16종 중 3종까지 닿았다.

⇒ **오늘 아침 세션으로 되돌릴 빨강은 없다.**

---

## 1. 실패 표 — 3종

| 하네스 | 통과/실패(전수) | 단독 재실행 | 귀속 |
|---|---|---|---|
| `test-body` | 304/2 (⑯㉧ 갯벌 문구 · ⑯㉧ 짠물 기운) | **`TIDE_FREEZE_MS=372603` 단독 306/0 초록** | **상시(물때)** — 공통.md 목록 그대로. 새 결함 아님 |
| `test-tick-slicer` | 21/3 (⑥a 막힘 11,308ms · ⑥ 효과비 1.00 · ⑧ 잡음 바닥 0.612%) | **단독 22/2**(⑧ 은 단독에선 선다 · ⑥·⑥a 동일) | **상시(기계 의존)** — 공통.md 목록 그대로. 새 결함 아님 |
| `e2e-cold` | 54/1 (`존 입장 — 월드 안이다  5625ms 기다림`) | ★**같은 밤 단독 55/0 완전 초록**(04:40~05:01 · 같은 클론·같은 상자) | **부하 거짓 빨강** — 제품 회귀 아님. 아래 §2 |

빨강 셋 중 어느 것도 최근 24시간 착지에 닿지 않는다 —
`git log -3 -- scripts/test-body.js server/body.js server/tidal.js scripts/test-tick-slicer.js` 의 가장 최근 커밋이
각각 **09-11(T171) · 09-05(T114) · 09-03(T59) · T49 판**이고, `scripts/e2e-cold.js` 의 최근 셋은
**T238·T220 의 `// @nightly` 표식 한 줄**과 T160 이다(판정 절 무접촉). ⇒ 귀속 카드 **없음**.

### `e2e-cold` — 왜 전수에서만 빨갰나 (재지 않은 것을 지어내지 않기 위해 적는다)

실패 줄이 하나이고, 그 줄은 **판정이 아니라 기다림**이다:

* `✗ 존 입장 — 월드 안이다  5625ms 기다림` — 벽시계 상수를 못 채운 줄이다.
* 같은 판의 나머지 54절은 전부 실수(實數)를 냈다(`⑦㉠ 능선 0 · 골 0` · `⑧ⓒ 추위 0.8162 < 문턱 0.930`).
* 같은 밤 **단독 55/0**(`⑧ⓒ 추위 0.8154`) — 값이 거의 같다. 계산이 아니라 **들어가는 데 걸린 시간**만 달랐다.

⇒ 어제(09-13) 야간 보고가 같은 하네스에서 적은 것과 **같은 꼴**이고, 족보 (167)이 말한 자리다:
2코어에서 앞 하네스 정리와 서버 둘 세우기가 겹치면 입장 창을 못 채운다.
어제도 오늘도 단독은 초록이므로 **상시 목록을 되돌릴 이유는 없다**.
다만 **이 줄은 벽시계 상수다** — 공통.md ⑩("하네스는 정해진 초를 자지 않는다")의 대상 후보로 적어 둔다(카드거리 · PM).

---

## 2. 오늘 밤의 묶음 — `nightly-split.sh auto` 첫 실전

* T238(09-13 착지)이 세운 **세 묶음**을 오늘 처음 실전에서 썼다. `auto` 는 `date +%j % 3` 이라 **KST 날짜로 돌렸다**(`TZ=Asia/Seoul`) — 컨테이너 기본 시각은 UTC 라 그대로 두면 하루 어긋난 묶음을 돈다. **이 한 줄이 오늘의 실측**이다(스크립트 무접촉 · 환경변수만).
* 러너 `--list` 가 낸 오늘 수: **전수 144종 · 묶음 C = 단위 96 + e2e 16 = 112종**. 공통.md §2 ⑥·②가 적은 **143** 은 한 종 낡았다(어제 이후 한 종 등재) — 문서 정정거리(PM).
* 그런데 **T238 의 예산(밤 최대 2:45:39)이 이 상자에서는 안 섰다.** 단위 96종 실측이 **1:58**(T238 실측 1:09:23 의 **1.7배**)였다. e2e 는 C 묶음 16종 중 `chronicle`·`cold`·`events` 셋(+ 러너 시작 전 탐침으로 `bigmap-live`·`charsprite` 둘)까지 닿고 창이 닫혔다.
* ⇒ **#25 는 아직 안 닫혔다.** T238 의 수는 그 밤 그 상자의 것이고, 상자 속도가 밤마다 다르다. 판정거리는 "세 묶음이 드나"가 아니라 **"어느 상자에서 드나"**다. → 브리프 경보 · PM

---

## 3. 미측정 13종 (전부 e2e · 오늘 묶음 C 안)

```
e2e-hp  e2e-membership  e2e-move  e2e-mtcut  e2e-mtfoot  e2e-mtocc  e2e-npcsprite
e2e-rooms  e2e-rtt  e2e-thirst  e2e-village  e2e-weather  e2e-winter
```
(묶음 A·B 의 32종은 오늘 몫이 아니다 — 내일·모레가 돈다.)

⚠이 중 `e2e-npcsprite`·`e2e-weather ⓕ`·`e2e-rtt` 는 `인계/회부.md ⑥` 이 **상시 목록 후보**로 올려 둔 셋이다(기계 의존).
오늘 못 쟀으므로 그 회부에 보탤 자료는 **없다**(못 쟀다고 적는다).

## 4. 러너 시작 전 탐침에서 나온 초록 (기록만 · 묶음 밖)

창을 여는 동안 알파벳 순으로 다섯을 먼저 돌렸고, 그중 셋이 오늘 묶음 밖이다 —
`e2e-bigmap-live` ✓ · `e2e-charsprite` **96/0** ✓ · `e2e-chronicle` **62/62** ✓.
`e2e-charsprite` 는 T250 이 고친 뒤의 첫 야간 측정이고 **초록**이다(상시 목록 제외 유지 근거 한 줄).
`e2e-chronicle ⑪b` 도 **초록** — T268(a66fbb79)이 어젯밤 닫은 그 절이다.

---

## 5. 아침에 세션으로 넘길 빨강

**없다.** 셋 다 상시이거나 부하이고, 귀속 카드가 없다.

PM 몫으로만 두 줄 남긴다(세션 발사 아님 · 판정거리):

* `[PM · 공통 §2 ⑥]` 러너 종 수가 **143 → 144** 로 늘었다. 공통.md 의 "143종"이 세 곳(§2 ②·⑥·T185 줄)에 있다 — 한 번에 고칠 것.
* `[PM · 판정대기 #25]` 세 묶음 예산(T238 2:45:39)이 **이 상자에서 3:22 로도 안 닫혔다**(단위만 1.7배). 창을 늘리든 묶음을 넷으로 하든, 판정 전에 **상자 속도 편차**를 표에 한 줄 넣어야 한다.

## 6. 이 밤의 규약 준수

| 규약 | 지켰나 |
|---|---|
| 병렬 0 · 순차 단독 | ○ (러너 하나 · `ps` 로 매 청크 확인) |
| 러너 두 개 금지 | ○ (탐침 → 분할 전환 때 옛 러너·자식 전부 `kill <pid>` 뒤 `ps` 빈 칸 확인) |
| `pkill -f` 금지 | ○ (내 손으로 부른 것 0 · 러너 제 `drain()` 은 러너의 것) |
| 코드·하네스 수정 | 0 |
| main 무접촉 | ○ (이 문서 한 장만 커밋) |
| 맥 클론 `merge`·`checkout`·`pull`·`add`·`status` | 0 |
