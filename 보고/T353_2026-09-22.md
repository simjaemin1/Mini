# 보고 — T353 BGM 프레이즈 곡선 · DDSP 어댑터·학습 스크립트 · A/B (2026-09-22 · 세션8/ART)

**측정 베이스 `045d2a3e` · 착지 베이스 `cf013128`**(도는 동안 main 이 37커밋 전진 — 두 번 리베이스 · 충돌 0 · 내 영역 diff 0 확인 · 다리가 끊겨 하루 늦게 올렸다) · **[승인 대기]**
★**카드 ①⑤ 의 전제가 코드와 안 맞는다 — 셋을 실측으로 잡았다**(§0-ⓐ): `sigim()` 은 **배포 13곡 중 0곡**을 지나고 ·
**이 자리에서 13곡을 못 굽는다**(샘플 뱅크가 레포 밖 · `~/Mini` 에도 없다) · `compose.py` 는 **뱅크 없이 혼자 못 돈다**(`art="both"`)
⇒ 그 결과로 **`render-meta.json.source` 의 `janggu_gung: "합성"` 도 틀렸다**(T292 가 `daegeum` 칸에서 잡은 것과 같은 결함 한 칸 더)
**그래서 게이트를 바꿔 세웠다** — 13곡 해시 대신 **"끔이면 새 코드가 import 조차 안 된다"**(더 강한 증명 · §0-ⓑ)
`phrase.py` 새 파일 · `test_curves.py` **PASS(12/0)** · `tools/ddsp/` 스모크 **초록**(cpu · 710 step · 30.0s)
**제품·배포 자산 diff 0**(`public/client`·`bgm.js`·`bgm-loops.js`·13곡 `.ogg/.m4a`·`render-meta.json` 전부 빈 출력)
A/B 두 파일 `~/Mini/_bgm_ab/` 착지 — ⚠**배포 믹스 아님**(곡선 차이만 남긴 대금 단선율)

## ★ 답 한 줄

**끔 13/13 해시는 이 자리에서 못 잰다(뱅크 없음) — 대신 끔=새 코드 0 호출을 정적으로 못 박았다 · 어댑터 A(B 없음) · 스모크 초록 · 데이터 표 7행 · A/B 파일 ⓐⓑ**

---

## §0-ⓐ 카드 전제 셋이 코드와 안 맞는다

| # | 카드가 말한 것 | 실측 |
|---|---|---|
| ① | "작곡 → **표현(`motif.sigim()`)** → 합성 세 층" | **배포 13곡 중 `sigim()` 을 지나는 것은 0곡.** `compose.py`(8곡)·`arirang.py`(4곡)는 `motif` 를 **import 조차 안 한다**. 부르는 건 `render_score.py`·`render_jeongak.py` 뿐이고 둘 다 `out_samples/` 용이다 |
| ② | "끔이면 **13곡 float 배열 해시 동일**이 게이트" | **이 자리에서 13곡을 못 굽는다.** 굽기가 `samples_*/_index.json`(국악원 녹음)을 요구하는데 레포 **0개** · `~/Mini` **0개** · 맥 VM 엔 scipy 도 없다. `Bank([])` 는 `roots[0]` 에서 죽는다 |
| ③ | (암묵) "`compose.py` 를 돌리면 된다" | **못 돈다.** `compose.py:123` 이 `janggu_gung(…, art="both")` 인데 `gugak.janggu_gung` 엔 `art` 인자가 없다. `art` 를 받는 건 `sampler.wrap_perc` 뿐이고 그건 `bank.has("janggu_gung")` 일 때만 끼워진다 |

★**③에서 따라 나오는 것 하나** — `render-meta.json.source` 의 `janggu_gung: "합성"` 은 **틀렸다.**
그 줄과 배포 `.ogg` 가 **같은 커밋 `f75e8158`(2026-07-31)** 에서 왔는데, 정말 합성이었다면 그 커밋의 굽기가
`TypeError` 로 죽었을 것이다. ⇒ 굽던 자리에 janggu 뱅크가 있었다. **T292 가 `daegeum` 칸에서 잡은 것과 같은 결함이 한 칸 더 있다** ⇒ 회부.

★**12곡의 "표현"은 어디 있나** — 표현 층이 아니라 **호출부에 손으로 적힌 값**이다
(`arirang.py:163` `vib_cents=lead.get("vib", 34)` · `compose.py:264` `vib_cents=34 if d>=3 else 20`).
그래서 "`sigim()` 이 내는 센트를 프레이즈로 잇는다"는 12곡에 대해서는 **이을 것이 없다.**
`sigim()` 을 그 경로에 새로 끼우는 것은 **작곡 층 무접촉** 규칙과 부딪힌다 ⇒ 손대지 않고 회부.

## §0-ⓑ ① 프레이즈 곡선 — 그리고 게이트를 바꿔 세운 이유

`phrase.py` 새 파일. **`gugak.py`·`compose.py`·`arirang.py` 는 한 줄도 안 고쳤다.**

| | |
|---|---|
| 손잡이 | `BGM_CURVES` · **기본 끔** |
| ★끔의 증명 | 13곡 해시 대신 — **굽는 세 파일이 `phrase` 를 `import` 조차 안 한다**(정적 · 하네스 ⓑ). 해시 동일보다 **강한 진술**이다: 같은 값이 나오는 게 아니라 **새 코드가 아예 안 불린다** |
| 13곡 해시 | ⚠**뱅크가 있는 자리(재민 데스크톱)에서만 잴 수 있다.** 못 잰 것을 잰 척하지 않는다 |

**새 수 0 — 유도 표:**

| 값 | 어디서 왔나 |
|---|---|
| 농현 깊이·꺾기 센트 | 부르는 쪽이 준다(`sigim()` 또는 지금 호출부의 `vib_cents`/`bend_end`) — 이 파일이 안 정한다 |
| 농현 속도 | `gugak.py` 악기 기본값 그대로(대금 4.6 · 단소 5.4 · 피리 5.0). **유도도 안 했다** ⇒ 회부 |
| **글라이드 길이** | `attack × GLIDE_OF_ATTACK`, `GLIDE_OF_ATTACK = 1.0` — *"건너가는 데 드는 시간 = 그 악기가 소리를 세우는 데 드는 시간"* 한 줄이 근거. 대금 **85ms** · 단소 100 · 피리 45. ⚠소박의 절반을 넘으면 거기서 자른다 |
| 꺾기 경사 | `release × 1.6`, 지수 1.5 — `flute()` 의 `bend_end` 꼴 **그대로** |
| 램프 | 0.35초 — `flute()` 의 값 그대로 |

★**1차 판이 틀렸고 하네스가 잡았다.** 위상만 이으면 되는 줄 알았는데, **농현 깊이가 음마다 0 에서 다시 올라서**
경계에서 f0 이 **5.10Hz 튀었다**. 깊이·꺾기도 글라이드와 같은 길이로 이으니 **0.01Hz** 가 됐다.
*위상이 이어져도 깊이가 끊기면 소리는 끊긴다.*

**`test_curves.py` — PASS 12/0:**

| 절 | 재는 것 | 값 |
|---|---|---|
| ⓐ | `phrase.py` 악기 표 = `gugak.py` 기본값(사본이 갈라지면 문다) | 9칸 × 3악기 ○ |
| ⓑ | 기본 끔 · 굽는 세 파일이 `phrase` 를 import 안 함 | ○ ○ ○ ○ |
| ⓒ | 새 경로는 경계에서 음량이 안 떨어지고 f0 이 안 튄다 / 옛 경로는 떨어진다 | 음량 1.000 ↔ **옛 0.0000** · f0 **0.01Hz** |
| ⓓ | 자명 통과 금지 — **계단 f0** 과 견준다(상수 0 으로 흉내내지 않는다) | 계단 **48.0·83.2Hz** ↔ 곡선 **0.01Hz** |
| ⓔ | 어댑터가 백엔드 이름을 돌려준다 · 모델 없이 `backend="B"` 면 **죽는다** | `A(합성) · B 없음` ○ |

⚠ⓓ 의 1차 판은 `GLIDE_OF_ATTACK=0` 으로 흉내냈다가 **0.68Hz** 가 나와 문턱 아래였다(2샘플 램프가 남는다).
**상수를 만지는 흉내가 아니라 진짜 반사실(계단 f0)** 을 그 자리에서 만들어 견주게 고쳤다.

## §0-ⓒ ② 어댑터 하나

```python
render_curve(inst, f0_hz, loud, sr, amp, seed, backend=None) -> (float32[], "어느 백엔드")
```
* 지속음 악기(`daegeum`·`danso`·`piri`)만. **가야금·타악 무접촉**(뜯는 악기·타악은 샘플이 맞다 ⇒ 회부).
* 백엔드 A = `gugak.flute()` 의 하모닉·숨·청·필터를 **그대로 부른다**(곡선판 · 수 0).
* 백엔드 B = `tools/ddsp/models/<inst>/` 가 **있으면**.
* ⚠**조용히 A 로 안 떨어진다** — 이름을 돌려주고, `backend="B"` 인데 모델이 없으면 **죽는다**(하네스 ⓔ).

## §0-ⓓ ③ `tools/ddsp/` — 스모크 초록

```
[1/4] 의존성 OK   [2/4] GPU: no   [3/4] wav 폴더 없음 → 합성 톤   [4/4] 학습(스모크 30초)
{"device":"cpu","smoke":true,"steps":710,"loss":0.0297,"seconds":30.0,"chunks":0,
 "note":"★스모크다 — 전 과정이 돌았다는 것만 말한다. 모델 값은 안 쓴다(지어내지 않는다)."}
```

★**`train.sh` 의 1차 판에 결함이 있었고 스모크가 잡았다** — `[ -d venv ]` 로 "폴더 있음 = 다 깔림"을 읽어서,
설치가 중간에 끊긴 venv(2.4GB · torch 없음)로 계속 돌아 `ModuleNotFoundError` 가 났다.
`ready()` 가 **실제로 import 되는지** 보게 고쳤다 — **있음 ≠ 됨.**

**고른 것과 근거**(설치 가능성·유지 상태로 골랐다 · 음질 비교는 **안 했다**):

| 갈래 | 고름 | 왜 |
|---|---|---|
| f0 | **pyin**(librosa) | CREPE 는 모델을 또 받아야 한다 · 단선율 관악엔 pyin 으로 충분 · **의존성이 하나 적다**(스모크가 CPU 30초 안에 돌아야 한다) |
| DDSP | **PyTorch 최소 재구현** | Magenta DDSP 는 TF 1.x 계열 고정이 많아 최신 CUDA·파이썬에서 설치가 자주 깨진다 · 여기 쓰는 건 **하모닉+노이즈 디코더 한 개**라 짧고 핀이 우리 것이다 |

산출물(`models/`·`venv/`·`prepared/`)은 **`.gitignore`** — 커밋 0. venv 5.5GB 는 재 본 뒤 지웠다.

## §0-ⓔ ④ 데이터 표

| 무엇 | 지금 | 이 세션에서 확인했나 | DDSP 에 쓸 수 있나 |
|---|---|---|---|
| 정악대금 국악원 「단음 다운로드」 | 5파일 → 조각 113 (`village_day_jeongak` 에만) | ✗ **파일이 여기 없다** — `render-meta.source`·`CREDITS §2` 의 기록으로만 | ⚠**부족** — 단음은 **음색은 주지만 음과 음 사이가 없다**. 이 카드가 만드는 곡선이 바로 그 구간이라 배울 게 거기 없다 |
| 산조가야금 국악원 | 21파일 (12곡) | ✗ 같음 | — (가야금은 이 카드 밖) |
| `~/Mini/_sfx_in/` | 효과음 키 19종 | ○ 확인 — **국악 음원 0** | ✗ |
| **필요** | **연속 프레이즈 10~20분 단선율 · 반주 0** | — | — |
| 후보 ① AI Hub 「국악 악보 및 음원 데이터」(71470) | 대금·피리·해금 · MIDI+WAV 쌍 · **시김새 라벨** | ✗ | ◎ 가장 맞는다 — **승인·다운로드는 재민**(승인 계정) |
| 후보 ② 국립국악원 아카이브 독주 음원 | 연속 연주 | ✗ | ○ ⚠**라이선스 칸은 비워 둔다 — 재민이 확인**(지어내지 않는다) |
| 후보 ③ 지금 뱅크(`samples_daegeum`) 재활용 | 단음 113 | ✗ | ✗ 위와 같은 이유 |

⚠**음원 원본은 레포 밖**(`_sfx_in` 규약) · **`CREDITS.md` 무접촉**(새 음원이 이 카드에 안 들어왔다).

## §0-ⓕ ⑤ A/B

카드는 `village_day_ari` 를 시켰는데 §0-ⓐ② 때문에 **못 굽는다.** 대신 **같은 음표열**(본조 아리랑 첫 4마디 ·
`arirang.ARIRANG` 표를 읽는다 · 작곡 층 무접촉)을 **대금 단선율**로 두 판 구웠다 — 장단·가야금·리버브를 빼서
**곡선 차이만** 남겼다. 농현·꺾기는 `motif.sigim()` 이 내는 값 그대로(이 카드가 수를 안 정한다).

| 파일 | 무엇 | 초 | peak | rms |
|---|---|---:|---:|---:|
| `~/Mini/_bgm_ab/T353_a_old.wav` | 음마다 한 호출(지금) · 음 17 | 9.22 | −13.69dB | −23.12dB |
| `~/Mini/_bgm_ab/T353_b_curve.wav` | **악구 하나** · 글라이드 16 · 85ms | 9.22 | −12.25dB | −19.83dB |

ⓒ(백엔드 B)는 **모델이 없어 안 만들었다** — 없는 것을 만든 척하지 않는다.
⚠**배포 믹스가 아니다.** 판정은 **재민 귀**.

## §1 검증

| | |
|---|---|
| 제품 | `public/client/**`·`bgm.js`·`bgm-loops.js` **diff 0** |
| 배포 자산 | 13곡 `.ogg`/`.m4a`·`render-meta.json` **diff 0** |
| 굽는 층 | `gugak.py`·`compose.py`·`arirang.py`·`motif.py` **diff 0** (새 파일 둘만 는다) |
| `test_curves.py` | **PASS · 12/0** |
| `tools/ddsp/` 스모크 | **초록** (cpu · 710 step · 30.0s) · 산출물 커밋 0 |
| 13곡 해시 | ⚠**못 쟀다**(뱅크 없음) — 재민 데스크톱 몫 |
| 접점 | `sigim`·`SIGIMSAE`·`cents_fn`·`daegeum`·`danso`·`piri`·`render_curve`·`BGM_CURVES`·`tools/ddsp`·`prep.py`·`train.sh`·`export.py`·`village_day_ari`·`_bgm_ab` |

## §2 회부

1. ★**[PM] 카드 ① 을 다시 써야 한다** — 12곡에 표현 층이 없다. 잇는 게 아니라 **끼우는** 일이고 작곡 층을 건드린다.
2. ★**[PM/ART] `render-meta.json.source.janggu_gung: "합성"` 이 틀렸다** — 증명은 §0-ⓐ. T292 와 같은 줄.
3. **[재민] 샘플 뱅크가 세션에서 안 보인다** — 13곡 해시·`village_day_ari` A/B 는 뱅크가 있는 자리에서만 된다.
4. **[재민] AI Hub 71470 승인·다운로드** · 국악원 아카이브 **라이선스 확인**(칸을 비워 뒀다).
5. **[재민] 농현 속도 4.4/4.6/5.0/5.4Hz** — 유도도 학습도 안 했다. 카드 §4 그대로.
6. **[뒤 카드]** 가야금 조각 이음 · 타악 샘플 · 스템 렌더(세션9) — 그대로.
