# T348 — 닛폰 다리·광맥 (세션5 · 2026-09-22 · **가지만**)

> 카드 `~/Mini/지시_T348.md` · 가지 `batch/nippon-bridge-0921` · 베이스 `98a27edf`(main) ·
> 만진 것: `server/zone-config.js`(닛폰 `bridges` 132셀 + 주석) · `server/hanbando-terrain.json`(닛폰 `ores` 5→55) ·
> `scripts/plan-bridges-v2.js`(물 두 층 · 후보 문) · `scripts/plan-ore-clusters.js`(존 인자 둘 · 꼴 보존) ·
> `scripts/test-nippon-boot.js`(절 셋) — **새 수 0 · 새 표 0 · 한반도 무변.**

## ★ 한 줄 답 (머리 15줄)

> **도달불능 19/28 의 주인은 "존 인자"가 아니라 **계획기가 서버와 다른 물을 본다**는 것이었다 —
> 해안선 띠를 안 봤고, 그 띠가 닛폰에선 존의 16%(한반도 4.4%)다. 같은 물을 보게 하고 같은 함수로 놓으니
> 도하 64셀 · 다리 132셀 · **도달 7→9쌍 / 21** · `다리구제 32`. 나머지 5덩어리는 계획기가 "대양"이라 답했다(배가 답).
> 광맥은 정본 5개(`mineral:null`)가 절차 4개를 덮어 사실상 0이었다 → 같은 계획기로 **55개** · 쿠라다 `O0.1→1.58`.
> ⚠**"O0.1 8/8" 은 절반만 결손이었다** — 노동권에 광맥 있는 마을 비율이 닛폰 14% · **한반도 16%** 로 같다(#47 그 자리).
> `test-nippon-boot` **32/0** · 한반도 3시드 비트 동일.**

1. **다리가 안 도는 이유는 셋 중 둘이 자 결함이었다**(§0-ⓐ):
   ⓐ `plan-bridges-v2.js` 는 **이미 존 인자**다(`argv[2]`) — 카드가 물은 "존 인자? 하드코딩?"의 답은 **둘 다 아니다**.
   ⓑ 후보를 `getZoneVillages` 로 물어 닛폰은 `도달 불가 마을 0` 이라는 **거짓 초록**을 냈다(T343 `siteCandidates` 로 고쳤다).
   ⓒ ★**계획기가 서버와 다른 물을 봤다** — 물은 두 층(`WATER_TILES` 해안선 띠 · 손그림 강·호수)인데 띠를 안 봤다.
2. **왜 한반도에선 안 들켰나** — 띠가 존의 **4.4%**(폭 2188셀)라 가장자리에만 있다. 닛폰은 폭 **1562셀**뿐이라
   띠가 **16.0%** 로 안쪽까지 든다 ⇒ 계획기가 "본토"라 부른 자리가 서버에선 바다다.
3. ⚠**1차 판은 헛다리였다 — 그리고 자가 그걸 잡았다.** 띠 없이 뽑은 도하 5셀·다리 14셀을 놓았더니
   셀은 선언됐는데 서버 로그가 `다리구제 0` 이고 **도달 쌍이 한 쌍도 안 늘었다**. 선언은 판정이 아니다.
4. **고친 뒤 같은 함수가 낸 답**: 쿠라다 덩어리(558,103셀) ↔ 본토(2,060,422셀) **도하 64셀 · 다리 132셀**.
   실셀 BFS 와 코스 격자(교역행렬)가 **같은 답**: 도달 **7 → 9쌍**(21쌍 중) · 서버 `다리구제 32`.
5. **놓지 않은 것 다섯** — 계획기가 "200셀 내 본토 도하 없음 = 대양 분리"라 답했다. 가장 큰 덩어리
   (1,529,254셀 · 네코로·노라마·지옹로·타카쿠)가 거기 있다. **배가 답**이다(항해 층 · 회부).
   한반도 광산2 와 같은 자리 — 못 놓는다고 한 것을 억지로 놓지 않았다.
6. **광맥의 주인**(§0-ⓑ): 닛폰 정본 `ores` 5개는 `mineral:null`·`pk` 없음인데, `terrain.js:138`
   `if (hc[zoneId].ores.length) data.ores = …` 가 **절차 ores 4개를 그 5개로 교체**한다 ⇒ 사실상 0.
7. `plan-ore-clusters.js` 가 **존 인자 두 자리에서 막혀 있었다**(T336 이 적어 둔 자리):
   파일 경로를 `ZID + '-terrain.json'` 으로 만들고(정본은 **한 파일**), 광종 풀을 `ZID === 'hanbando'` 로 가뒀다
   (그 모듈 머리가 **"지역 무관 · 골고루"**라고 적어 뒀는데도). 둘 다 고쳤다 — 한반도는 종전에도 켜져 있어 무변.
8. **같은 계획기로 닛폰 광맥 55개**(기존 5 + 대중소 50 · 존의 7.31%). 쿠라다 `O 0.1 → 1.58`.
9. ⚠**T343 이 "광맥 O0.1 8/8" 을 결손으로 적은 것은 절반만 맞았다.** 노동권(r33) 안에 큰광맥이 있는
   마을 비율은 닛폰 **1/7 (14%)** · 한반도 **8/51 (16%)** — **같다**. 나머지가 바닥인 것은 닛폰 결손이 아니라
   **#47(광맥 없는 43마을)** 그 자리다. 그래서 자는 상한이 아니라 **하한 1곳**만 건다.
10. ⚠**도구가 정본 파일의 꼴을 안 지켰다** — `indent 1` 로 통째 다시 써서 광맥 몇 개 더한 판이
    `127,281줄 삽입 / 1줄 삭제` 로 나왔다. 꼴을 보존하게 고쳤다(diff **1줄**).

---

## §0-ⓐ 다리 — 도달불능 19/28 의 원인과 시공

### ⓐ-1 쌍마다 막는 것 — 실셀 BFS 로 갈랐다

**섬이 아니라 덩어리 셋**이었다(T343 시딩 8곳 기준 · 다리 0):

| 덩어리 | 크기 | 마을 | 경계(물/바위/존밖) |
|---|---|---|---|
| **본토**(스폰 도가이 광장) | 2,962,790셀 | 마트노 · 이즈이 · 지옹사키 | 14,432 / 7,704 / 4,842 |
| — | 1,088,158셀 | **쿠라다** | 3,848 / 342 / 3,062 |
| — | 1,529,254셀 | 네코로 · 노라마 · 지옹로 · 타카쿠 | 6,051 / 6,172 / 753 |

쌍 수: 3×1 + 3×4 + 1×4 = **19** — 교역행렬(코스 격자)이 낸 19와 **정확히 같다**.
⇒ 코스 격자의 근사가 아니라 **지형이 실제로 갈려 있다**. 막는 것은 **물과 바위가 섞여** 있다
(다리는 물만 건넌다 — 바위는 고증·지형 무결로 안 뚫는다: `zone.js:821`).

### ⓐ-2 ★한반도 정본이 닛폰에서 안 돈 이유 — **존 인자가 아니었다**

| 후보 | 사실 | 판정 |
|---|---|---|
| 존 인자가 없나 | `plan-bridges-v2.js:17` `ZID = process.argv[2] \|\| 'hanbando'` — **이미 존 인자다** | 아니다 |
| 다리 적재가 존 전용인가 | `zone.js:791` `(ZONE && ZONE.bridges)` · `isBridgeTileLocal` — **존 무관** | 아니다 |
| 후보 공급 | 계획기가 `getZoneVillages(ZID)` 를 물어 닛폰은 **0곳** ⇒ `도달 불가 마을 0` **거짓 초록** | ★자 결함 |
| **물의 층** | 서버 통행 정본은 물을 **둘** 본다. 계획기는 **한 층만** 봤다 | ★★자 결함(본선) |

```
zone.js:818 isTerrainBlockedLocal
  ├ isRockTileLocal            (바위 — 다리로 안 뚫는다)
  └ isWaterTileLocal           ← 물이 **둘**이다
      ├ WATER_TILES            해안선 띠 (chunk.generateCoastlineWaterTiles)  ← 계획기가 안 봤다
      └ terrain.isWaterCellLocal  손그림 강·호수                              ← 계획기가 본 층
```

| 존 | 해안선 띠 | 존 폭 | 비율 |
|---|---|---|---|
| hanbando | 393,686칸 | 2,188셀 | **4.4%** — 가장자리에만 있어 마을 근처에 안 닿는다 |
| **nippon** | **1,016,334칸** | **1,562셀** | **16.0%** — 안쪽까지 든다 |

⇒ **한반도에선 두 시야가 사실상 같았고, 닛폰에선 갈렸다.** 같은 코드가 존에 따라 다른 진실을 본 것이다.

### ⓐ-3 ⚠1차 판은 헛다리였다 — 짝 비교

| | 계획기 시야(띠 없음 · 1차) | 정본 시야(띠 포함 · 고친 뒤) |
|---|---|---|
| 본토 | 2,962,790셀 | **2,060,422셀** |
| 쿠라다 덩어리 | 1,088,158셀 | **558,103셀** |
| 최단 도하 | 5셀 → 다리 14셀 | **64셀 → 다리 132셀** |
| 물 위 후보(0셀 플러드) | 안 보였다 | **넷**(이즈사키·후지로·카토쿠·히마쿠) |
| 놓은 뒤 서버 `다리구제` | **0** | **32** |
| 놓은 뒤 도달 쌍 | **7 → 7** (안 늘었다) | **7 → 9** |

**선언은 판정이 아니다.** 셀은 `zone-config` 에 들어가 있었는데 세계는 한 쌍도 안 열렸다.
⇒ 자가 `다리구제 > 0` 을 본선으로 건다(ⓕ).

### ⓐ-4 놓은 것 하나 · 놓지 않은 것 다섯

| 덩어리 | 마을 | 계획기 답 |
|---|---|---|
| 558,103셀 | 쿠라다 | **도하 64셀 → 다리 132셀**(폭 2셀 · 양끝 뭍 접지) ★놓았다 |
| 1,529,254셀 | 네코로·노라마·지옹로·타카쿠(+후보 쿠라마·지옹사키) | 200셀 내 도하 없음 → **대양 분리** |
| 0셀 ×4 | 이즈사키·후지로·카토쿠·히마쿠 | 물 위 후보 — `findOpenCenter` 가 어차피 스킵한다 |

**도달률 32% → 43%**(T343 9/28 → T348 9/21). 나머지는 다리가 아니라 **배**다.

## §0-ⓑ 광맥 — `O0.1 8/8` 의 주인

### ⓑ-1 기전

| | |
|---|---|
| 닛폰 정본 `ores` | **5개** · 전부 `mineral: null` · `pk` 칸 없음 |
| 절차 `ores` | 4개(한반도도 3개 — 절차는 원래 거의 안 낸다) |
| 그 둘의 관계 | `terrain.js:138` `if (hc[zoneId].ores && hc[zoneId].ores.length) data.ores = hc[zoneId].ores;` ⇒ **손그림 5개가 절차 4개를 교체**한다 |
| 한반도는 왜 되나 | 787개 — `plan-ore-clusters --apply` 를 **돌린 결과**다(10만셀당 8.85 vs 닛폰 0.08) |

⇒ 주인은 "광맥 시딩 코드"가 아니라 **"닛폰엔 그 계획기를 아직 안 돌렸다"** 다. 그리고 못 돌린 이유가 아래다.

### ⓑ-2 계획기가 존 인자를 못 받던 두 자리 (T336 이 적어 둔 자리)

| 자리 | 종전 | 고친 뒤 |
|---|---|---|
| `plan-ore-clusters.js:41` | `ZID + '-terrain.json'` — `nippon-terrain.json` 은 **없다** ⇒ 즉사 | 정본 한 파일을 가리킨다 |
| `plan-ore-clusters.js:137` | `if (ZID === 'hanbando') HB_MIN = require('./hanbando-minerals')` | 조건 제거 — 그 모듈 머리가 **"지역 무관 · 골고루"** 라고 적어 뒀다 |
| `plan-ore-clusters.js:410` | `JSON.stringify(doc, null, 1)` — 정본은 **한 줄**인데 통째 재포맷 ⇒ diff `127,281줄 삽입 / 1줄 삭제` | 원본 꼴 보존 ⇒ diff **1줄** |

★한반도는 두 자리 모두 **종전에도 켜져 있던 갈래**라 한 칸도 안 바뀐다(자 ⓗ 가 787·836 을 잰다).

### ⓑ-3 같은 함수가 낸 결과

```
=== 광맥 배치 계획 · nippon · 1562×4063셀 ===
기존 광맥 5개 … 총 광맥 55개 (기존 5 + 신규 50) · 원판 면적 합 464,116셀 = 존의 7.31%
  r130셀 × 3 · r70셀 × 12 · r32셀 × 35   (TIERS 그대로 · 새 수 0)
```
`쿠라다 land(… /O1.58/ …)` — 종전 `O0.1`. 서버 부팅 로그 `광맥 55개 — gold, iron, …`.

### ⓑ-4 ⚠"8/8 바닥"은 절반만 결손이었다 — 한반도와 같은 자로 재 보니

노동권 반경(r33) 안에 **큰광맥**(`isMajorOreAt` — NPC 경제가 보는 것)이 있는 마을 비율:

| 존 | 마을 | 광맥 있는 마을 | 비율 |
|---|---|---|---|
| **nippon** | 7 | **1** (쿠라다 32.4%) | **14%** |
| **hanbando** | 51 | **8** (광산1~7 · 어촌1 · 41~56%) | **16%** |

⇒ **같다.** "전 마을에 광맥"은 이 세계의 규약이 아니다 — 나머지 6곳이 바닥인 것은 **닛폰 결손이 아니라
#47(광맥 없는 43마을)** 그 자리다. T343 이 "O0.1 8/8"을 결손 셋에 넣은 것은 닛폰 정본이 사실상 0이었다는
점에서만 옳았다. **고친 것은 그 부분이고, 남은 것은 남의 카드다.**
⇒ 자는 **상한을 안 걸고 하한 1곳**만 건다(ⓖ). 전부 바닥이면 그건 "광맥 정본이 없다"는 뜻이다.

## §0-ⓒ 마을 7곳 — 왜 8에서 줄었나(회귀가 아니다)

| | T343 | T348 |
|---|---|---|
| 후보 → 선별 → 시딩 | 16 → 10 → **8** | 16 → 9 → **7** |
| 빠진 곳 | — | **지옹사키** |

`pickSeedVillages` 는 땅 점수(`landScore` — 광맥을 **안 읽는다**)로 정렬한 뒤,
**광맥 슬롯**(`VEINSLOT_MAX` · `veinScore`)과 **부얼타운**을 *먼저* 뽑는다. 광맥이 실물이 되자
쿠라다가 그 슬롯으로 먼저 앉고, 간격(`MIN_SPACING_PX`)이 지옹사키를 밀어냈다.
⇒ **정본 규칙이 설계대로 돈 것**이다(새 수 0 · 손잡이 0). 도달률은 32% → 43% 로 올랐다.

## §0-ⓓ 하네스 — `test-nippon-boot` 22 → **32 / 0**

| 절 | 재는 것 | 실측 |
|---|---|---|
| ⓐ~ⓓ | (T343) 후보 정본 · 한반도 같은 배열 객체 · 바다 존 7개 후보 0 · 게이트 0 | 무변 |
| ⓔ | (T343) 실서버 — 시딩·장부·행렬·`villages ≥ 1`·오류 0 | 마을 7 · econ 56 |
| **ⓕ** | **다리** — `다리구제 > 0`(★본선) · 존 설정 다리 셀 > 0 · 도달 쌍 ≥ 1 | `다리구제 32칸` · 도달 **9/21** |
| **ⓖ** | **광맥** — 바닥(0.1) 넘는 마을 ≥ 1 · 바닥 아래 0 | **1/7** 쿠라다 O1.58 · 범위 0.1~1.58 |
| **ⓗ** | **한반도 무변**(자료) — 정본 광맥 787 · 다리 836셀 | 787 · 836 |

★ⓕ의 본선을 "다리를 놓았다"가 아니라 **"교역 격자가 그 다리를 본다"**로 둔 이유가 §0-ⓐ-3 이다.
★자명 통과 금지: ⓕ0·ⓕ1·ⓕ2·ⓖ0 이 전제를 먼저 건다(쌍 0·마을 0이면 "불능 0"도 초록이 된다).

## §3 검증

| | |
|---|---|
| **한반도 3시드**(`t17-metrics` 800일) | **여덟 수 전부 기준선 넷째 판과 일치** — 아래 표 |
| `test-nippon-boot` | **32 / 0** |
| `test-resource-index` | **20 / 0**(무변) |
| `test-psite-server` | **PASS**(무변) |
| `test-harness-lint` | **47 / 0**(새 절 포함) |
| 한반도 자료 무변 | 정본 `hanbando` 절 **JSON 동일**(문자열 비교) · `ores` 787 · `bridges` 836셀 · 다른 존 9개도 동일 |
| 제품 diff | `server/zone-config.js`(닛폰 한 블록) · `server/hanbando-terrain.json`(닛폰 `ores` 칸 · **diff 1줄**) |
| 도구 diff | `scripts/plan-bridges-v2.js` · `scripts/plan-ore-clusters.js` |
| T345·T346·T347·T329 | **무접촉** · 배포 **0** · force **0** |

**한반도 3시드 — 비트 동일**

| 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 |
|---|---|---|---|---|---|---|---|---|
| 1020 | **8318** | **0/51** | 1306 | 9602 | 2504 | 16596.5 | 4284.2 | 74094.9 |
| 7 | **7935** | **0/51** | 1275 | 9892 | 2151 | 14638.6 | 4719.3 | 74291.4 |
| 42 | **7853** | **0/51** | 1356 | 9790 | **2361** | 15293.9 | 3885.9 | 76117.9 |

기준선 넷째 판(1020=8,318 · 7=7,935 · 42=7,853 · 소멸 0/51 · 42 게시 2,361)과 **한 칸도 안 다르다**.
당연한 결과다 — 만진 자료는 전부 `nippon` 절이고, 도구 수정은 한반도가 종전에도 밟던 갈래다.

**접점 심볼**: `bridges` · `reachability` · `distmatrix` · `hanbando-minerals` · `ores` · `ZONES.nippon` ·
`plan-bridges-v2` · `plan-ore-clusters` · `WATER_TILES` · `generateCoastlineWaterTiles` · `siteCandidates` · `isMajorOreAt`

## §4 회부만 (구현 0)

1. ★★**계획기·랩이 서버와 다른 물을 본다 — 닛폰만의 일이 아니다.** `plan-bridges-v2` 는 이 카드가 고쳤지만
   `plan-bridges`·`audit-reachability`·`audit-terrain-quality`·`build-map` 등이 같은 시야를 쓴다.
   **통행 술어 사슬을 한 곳에 모으는 일**(#31 "25벌 모으기" · T259 · `scripts/zone-preds.js` 는 아직 main 에 없다)이
   그 답이고, 이 카드는 **사본을 하나 더 만들었다**(계획기 안에 띠를 다시 조립했다). 정직하게 적어 둔다.
2. ★**항해 층** — 닛폰 최대 덩어리(1,529,254셀 · 마을 4곳)는 다리로 못 잇는다. 한반도 광산2 와 같은 자리.
   배가 답이고, **재민 결정 사항**이다.
3. **광맥 밀도가 존 크기에 안 따라간다** — `TIERS` 가 고정 개수(3+12+35)라 작은 존이 더 촘촘해진다
   (닛폰 7.31% vs 한반도 4.49%). 새 수를 지어내지 않으려고 그대로 뒀다 — 규약으로 정할 일.
4. **#47 광맥 없는 43마을** — 닛폰 6/7 도 같은 병이고 비율이 한반도와 같다(§0-ⓑ-4). 같은 카드가 풀 문제.
5. 닛폰 지형이 이름과 어긋난다(열도 아님 · 산 아님 — T336 회부) · `getStoneMultiplier` 가 `ridges` 를 안 읽는다.
6. 청크 경계 감사거리(T317 ①).

## §5 푸시

| | |
|---|---|
| 가지 | **`batch/nippon-bridge-0921`** (가지만 — main 은 PM) |
| 베이스 | `98a27edf` |
| 길 | 맥 VM `device_bash` · `$HOME/pushrepo` 얕은 클론 · PAT 는 셸 변수만 · 즉시 `unset` |
| force | **0** |
