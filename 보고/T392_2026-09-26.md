# T392 — 컬링 팔로 간다 · 330ms 의 주인 · 능선 색인은 이미 있다 (세션5 · 2026-09-26 · **가지만**)

> 카드 `~/Mini/지시_T392.md` · 가지 `batch/cull-radius-0925` · 베이스 `259dadb6`(main) ·
> 만진 것: `public/client/34-m-renderloop.js`(**상수 한 줄** — `VIEW_RADIUS = TILE_RENDER_RADIUS`) ·
> `public/client/38-r1-fartree.js`(띠 끔 — `viewR < tileR` 일 때만) · `scripts/test-far-trees.js`(ⓐ 뒤집기 · ⓑ9~ⓑ12 · 도착 확인) ·
> `scripts/t384-two-arms.js`(도착 확인 · 종류별 · `--product-only` · `--spawn`) · 신설 계측기 둘(`t392-frame-profile.js` · `t392-ridge-index.js` · 러너 밖)
> — **`server/` 0 · 정본 JSON 0 · 새 수 0 · 원경 ⓑ·손잡이 무변 · 안개 규격 무변.**

## ★ 머리 15줄

| # | 물음 | 답 |
|---|---|---|
| ⓐ | 컬링 1,500 | **`VIEW_RADIUS` 는 이제 `TILE_RENDER_RADIUS` 의 별칭**(새 수 0). 띠는 비고(viewR = tileR) 원경 층은 큰지도 전용. 임업3(도착 확인): 개체 길이 그린 나무 **2 → 666**(= 1,500 안 본 셀의 나무 666 · 650 밖 664) |
| ⓐ' | render 전/후(세 자리 · 90프레임 · 도착 확인) | 임업3 **12.0 → 15.9ms** · 농촌2 **370.9 → 376.2** · 빈 초지 **370.6 → 382.1**(전 = main 제품 · 띠 켬 650 / 후 = 이 가지) |
| ⓐ'' | ★T384 두 팔 표는 **다시 서지 않았다** | T384 의 숲 "3.3 · 9.1 · 5.1ms" 는 **도착을 안 물은 판**이었다 — 서버 권위 위치가 ~5초 늦게 오고 한 판은 재접속으로 스폰에 되돌아갔다. 도착을 확인한 임업3(T378 개간 뒤 세계)에선 **A0 13.6 · A1 12.0 · B 13.9** — 컬링이 **싸지 않다**(비슷하거나 조금 비싸다). 컬링 팔의 근거로 남는 것은 **클릭·채집이 된다 · 층 하나** 둘이다 |
| ⓑ | 330~376ms/f 의 주인 | **`drawImage` 93.7%** — 부른 줄은 **`_drawWaterLayer`(10-r1-terrain.js:1071)** 3,889ms/4,281(농촌2) · 빈 초지는 **`_mt3GlDraw`(11-r1-mountain.js:255)** 2,665 + 물 1,151ms. 둘 다 **WebGL 캔버스를 2D 에 붙이는 한 줄**이고 헤드리스 크롬이 "GPU stall due to ReadPixels" 를 찍는다(소프트웨어 GL 의 되읽기) — 물·산이 안 보이는 임업3 은 5~6ms |
| ⓒ | 능선 선분 색인 | **이미 있다** — `server/terrain-segindex.js`(2026-08-31 · `42764e2d`) · `TERRAIN_SEG_INDEX=1` · **기본 꺼짐 · 배포도 꺼짐**. 켜면 숲 청크 **6.75 → 0.71ms(×9.5)** · 바위 술어 **42.7 → 0.36µs(×120)** · 청크 산출 해시 **같다**(능선 옆도 ×8.9 · 같다) |
| ⓓ | 한 줄 | 아래 |

**한 줄**: 컬링을 1,500 으로 올려 멀리 나무가 **개체로** 선다(임업3 2 → 666그루 · render 12.0 → 15.9ms) —
T384 의 "컬링이 더 싸다"는 도착 미확인 판이라 다시 서지 않았다 · 열린 땅 330ms 의 주인은 **WebGL 물·산을 2D 에 붙이는 `drawImage`**(헤드리스 되읽기) ·
17.8ms 를 ×9.5 로 내리는 능선 색인은 **이미 레포에 있고 꺼져 있다**.

## §0 ⓪ 자를 세계 변화와 가른다 — `test-far-trees` **19/0**

PM 컨테이너의 빨강 둘(ⓑ12 5,222 → 5,201 · ⓑ10 그린 56 < 100)은 이렇게 갈렸다:

1. ★**자리가 틀렸다.** 하네스는 텔레포트를 보내고 6초를 잤다. T392 실측(서버 권위 `__getSrvAbs` 를 초마다):
   서버는 즉시 옮기지만 **클라의 권위 위치가 ~5초 늦게 온다**(개체 수천 개가 쏟아지는 동안) · 한 판은 그 사이
   **재접속으로 스폰(농촌22)에 되돌아갔다**(개체 4,557 → 109). 도착을 안 묻고 잰 판은 **다른 자리**를 쟀다.
   ⇒ `goTo` 가 **서버 권위로 도착**할 때까지 기다리고(못 오면 다시 보낸다) 개체가 **더 안 늘 때까지** 기다린다(고정 초 0).
2. ★**세계는 멈춰 있지 않다.** 개간(T378 · `_terrGrow` → `clearTreesInCells`)과 나무꾼이 요청 중에도 나무를 벤다.
   ⇒ ⓑ12 는 "개체 수가 같다"가 아니라 **장부 항등식**으로 잰다 — 요청 동안 서버가 보낸 생김·없어짐을 받아
   `뒤 = (앞 − 없어짐) ∪ 생김` · **장부 밖 변화 0** · **새 청크에서 생김 0**(원경 요청이 청크를 켜지 않았다).
   실측: 개체 5,204 → 5,202 · 없어짐 2(세계) · 장부 밖 0 · 새 청크 0. **반례** — 페이지에서 하나를 장부 없이 지우면 장부 밖 1 로 빨개진다.
3. 문턱 100·500 은 **없앴다** — 두 수가 같은가를 묻는다(그 자리의 수 · 새 수 0).

```
  ⓐ5  ★띠 0 · 컬링 666 — 원경 층 띠 0(viewR 1500 = tileR 1500) · 개체 길이 그린 나무 666 = 1,500 안 본 셀의 나무 666
  ⓐ6  반례 — 옛 650 상자 밖 나무 664(재계산 664)를 개체 길이 그린다 · 나무 그리기 호출 프레임당 666 ≥ 666
  ⓑ9  ★화면에선 원경 0 — 받은 청크 131 · 원경 0 · 띠 0 · 그린 0   (화소: 켬↔켬 잡음 5,155 · 켬↔끔 6,063 = 1.2배)
  ⓑ10 ★큰지도 숲 점 = 받은 원경 나무 — 점 18,154 = 원경 18,154 · 찍힌 숲 화소 1,075   (80-bigmap 이 부르는 그 함수)
  ⓑ11 대조군 — 층을 끄면 큰지도 점 0(화소 0) · 켜면 18,154
  ⓑ12 ★세계 무변(세계 변화와 가름) — 5,204 → 5,202 · 생김 0 · 없어짐 2 · 장부 밖 0 · 새 청크 0 · 반례 장부 밖 1
```

## §1 ⓐ 컬링 1,500

```
  - const VIEW_RADIUS = 650;
    const TILE_RENDER_RADIUS = 1500;
  + const VIEW_RADIUS = TILE_RENDER_RADIUS;      // 개체가 그리는 상자 = 지면이 닿는 상자(새 수 0)
```

`38-r1-fartree.js` 의 띠는 `viewR < tileR` 일 때만 돈다 — 이제 둘이 같아 **안 돈다**(자원 수천 개를 프레임마다 훑지도 않는다). 원경 ⓑ(큰지도) 그대로.

### 무엇이 더 그려지나 — 게이트를 지난 항목 종류별(같은 서버 · 도착 확인)

| 자리 | 전(650 · 띠 켬) | 후(1,500) | 안개가 뺀 수 전 → 후 |
|---|---|---|---:|
| 임업3 | 건물 631 · 나무 **2** · 그루터기 158 · 덤불 2 · 소품 6 · 지붕 4 · 사람 5 · 영토 1 | 건물 **652** · 나무 **666** · 그루터기 **407** · 약초 2 · 덤불 5 · 소품 9 · 지붕 4 · 바위 1 · 웅덩이 1 · 사람 5 · 영토 1 | 136 → **799** |
| 농촌2 | 건물 325 · 그루터기 1 · 덤불 1 · 지붕 1 · 사람 1 · 영토 1 | 건물 **372** · 나무 **13** · 그루터기 10 · 소품 2 · 지붕 2 · 덤불 1 · 사람 1 · 영토 1 | 295 → **814** |
| 빈 초지 | 나무 1 · 광맥 2 · 사람 1 | 나무 **4** · 광맥 2 · 사람 1 | 5 → 32 |

* **사람·짐승** — 서버 AOI(800px)가 먼저 자른다(그 밖은 오지 않는다). 살아 움직이는 것에만 거는 시야 규칙(`entityVisibility` ·
  부채꼴 × 벽 LoS)은 코드 그대로 탄다. 이 세 자리 표본에선 짐승 0 · 사람 5 → 5.
* **안개가 뺀 수가 늘었다**(136 → 799) — 넓어진 상자 안 대부분이 미탐사 셀이다. 개체 길은 **본 셀만** 그린다(T384 §5-2 가 원경 층에 바랐던 것이 여기엔 원래 있다).
* ★**거리 vignette 도 따라온다** — `vis = max(0.15, 1 − (d/VIEW_RADIUS)^1.4)` 가 같은 이름을 쓴다:
  300px 에서 0.66 → **0.90** · 600px 에서 0.15 → **0.72** · 1,200px 에서 → 0.27. 가까운 사물·사람이 **덜 흐려진다**(눈으로 볼 값 · 회부).

### render 전/후 — `t384-two-arms.js`(도착 확인 판) · 90프레임 · 하늘·바람 끔

| 자리 | 전 main A0 띠 끔 | 전 main **A1 제품**(띠 켬 650) | 전 main B 컬링(계측 페이지) | **후 이 가지 제품** | 나무 그리기/프레임 전 → 후 |
|---|---:|---:|---:|---:|---:|
| 임업3 | 13.6 (p90 18.9) | **12.0** (15.5) | 13.9 (19.4) | **15.9** (24.4) | 60 → 666 |
| 농촌2 | 377.0 | **370.9** | 367.8 | **376.2** | 0 → 13 |
| 빈 초지 | 366.6 | **370.6** | 368.1 | **382.1** | 2 → 4 |
| 스폰(농촌22) | 436.8 | 349.5 | 358.5 | 442.8 | 1 → 0 |

⚠열린 땅 세 줄은 §2 의 주인(물·산 되읽기)이 프레임을 먹어 ±90ms 로 흔들린다 — 팔 사이 차이는 그 잡음 안이다.

## §2 ⓑ 330~376ms/f 의 주인 — `t392-frame-profile.js`(CPU 프로파일 · 4초 · 도착 확인)

| 자리 | 프레임/초 | render 중앙값 | 지면 타일 ms/f | 자연물 모으기 ms/f | 안개 선분·광선·교차 |
|---|---:|---:|---:|---:|---|
| 임업3 | 52~59 | 5.4~6.2 | 0.1 | 0.2~0.3 | 76 · 398 · 4,216 |
| 농촌2 | 3.3 | 389~393 | 0.1~0.2 | 0.2 | 72 · 380 · 4,114 |
| 빈 초지 | 1.5~1.7 | 1,246~1,355 | 0.1~0.3 | 0.2~0.3 | 24 · 134 · 1,006 |

| 자리 | CPU 자기 시간 1위 | `drawImage` 를 부른 줄 |
|---|---|---|
| 농촌2 | `drawImage` **93.7%**(4,013 / 4,281ms) | **`_drawWaterLayer` · 10-r1-terrain.js:1071 — 3,889ms** |
| 빈 초지 | `drawImage` **91.1%**(4,825 / 5,297ms) | **`_mt3GlDraw` · 11-r1-mountain.js:255 — 2,665ms** · `_drawWaterLayer` 1,151ms |
| 임업3 | `(program)` 40.8% · `render` 10.8% | 건물 66 · 소품 33 · 그루터기·묘목 18 · 나무 14ms |

**주인 이름 한 줄: WebGL 물 층(`_drawWaterLayer`)과 WebGL 산 메시(`_mt3GlDraw`)를 2D 캔버스에 붙이는 `drawImage`.**
헤드리스 크롬은 이 자리에서 `GL Driver Message … GPU stall due to ReadPixels` 를 찍는다 — 소프트웨어 GL(SwiftShader)이
GL 표면을 **CPU 로 되읽어** 2D 에 옮긴다. 지면·자연물·안개는 1ms 도 안 된다. JS 가 아니라 **합성 한 줄**이므로
GPU 가 있는 브라우저에선 크게 다를 수 있다 — **맥 GPU 한 판은 못 했다**(서버가 컨테이너 안이라 맥 브라우저가 닿지 않는다).
고치지 않았다 → 세션4 T395(클라 프레임 전체)의 입력.

## §3 ⓒ 능선 선분 색인 — `t392-ridge-index.js`

**§0: 이미 있다.** `_isPointInRiver` 가 `TERRAIN_SEG_INDEX=1` 이면 `terrain-segindex.js` 의 512px 격자 후보만 본다
(2026-08-31 · 부팅 52 → 12초로 들인 것 · 등가성 하네스 `test-segindex` @regress). **기본 꺼짐**이고 `BENCHMARK.md` 가
적은 배포 상태도 **꺼짐**이다. 그래서 17.8ms 가 났다. 플래그는 모듈 적재 때 읽히므로 자식 둘(끔/켬)로 쟀다.

| 자리(13×13 청크) | 판 | 청크당 ms | 첫 판(전체) | 바위 호출 | 바위 회당 µs | 청크 산출 해시 |
|---|---|---:|---:|---:|---:|---|
| 숲(임업3) | 끔(배포) | 6.75 | 1,304.7ms | 25,139 | 42.66 | `dd4ac8716320cd76` |
| 숲(임업3) | 켬 | **0.71** | 215.6ms | 25,139 | **0.36** | `dd4ac8716320cd76` |
| | 배율 | **×9.5** | ×6.1 | | **×120** | **같다** |
| 능선(먹뫼산맥) | 끔 | 0.53 | 94.7ms | 817 | 59.66 | `b360d4e3d6206e2c` |
| 능선(먹뫼산맥) | 켬 | **0.06** | 14.4ms | 817 | **0.75** | `b360d4e3d6206e2c` |
| | 배율 | **×8.9** | ×6.6 | | **×80** | **같다** |

* **같은 답(비트)의 조건** — 선분 i 를 `bbox(선분 i) + max(w1,w2)/2` 가 덮는 칸에 전부 넣는다 ⇒ 질의점의 칸 후보는
  참일 수 있는 모든 선분을 포함한다(검사 순서만 다르다 · `terrain-segindex.js` 머리말). 위 해시가 **청크 산출 전체**로 그걸 확인했다.
* **후보 분포** — 512px 칸: 쓴 칸 2,554 · 칸당 후보 중앙 **13** · p90 19 · p99 22 · 최대 28(끈 판은 bbox 를 지나면 그 능선 선분 **전부** — 한울대간 732).
  청크(1,024px) 격자로 쌓으면(계측만): 능선이 닿는 청크 1,298 · 청크당 후보 중앙 **15** · p90 21 · p99 30 · 최대 42.
* 색인은 **게으르게** 짓는다 — 이 판에선 bbox 가 걸린 능선 둘(한울대간 · 먹뫼산맥)만 지어졌다.
* ⇒ **새로 짤 것이 없다. 켤지 말지**다(PM · `europa` 앞). 켜면 원경 청크당 17.8ms 가 대략 ×9 로 준다(라이브는 재지 않았다).

## §4 검증

```
  test-far-trees        19/0 × 세 판   (ⓐ 뒤집음 · ⓑ9~ⓑ12 새 자 · 도착 확인 · 장부 항등식)
  test-client-globals   26/0
  test-harness-lint     61/61
  @pixel 전수 16 + e2e-fogray 12/0 + e2e-move 37/0   → 18 통과
  e2e-nature            132/1 — ★main 에서도 같은 빨강(아래)
```

* **ⓐ5 의 수는 판마다 다르다**(666 · 40 · 40) — 개체 길은 **본 셀의 나무만** 그리고, 도착 직후 안개가 얼마나 걷혔나가 판마다 다르다.
  자는 "게이트를 지난 나무 = 1,500 안 본 셀의 나무"라는 **같음**이고 세 판 모두 같았다(666 = 666 · 40 = 40 · 40 = 40).
* ★**`test-far-trees` 가 전수 러너 첫 판에서 한 번 빨갰다**(ⓑ12 "장부 밖 1") — 내 자의 틈이었다: 스냅샷과 장부 켜기를
  **따로** 불러 그 사이에 온 없어짐이 장부에 안 적혔다. 둘을 **한 evaluate 안에서**(페이지는 한 줄기) 하도록 고쳤고 두 판 19/0.
* ★**`e2e-nature` 심기① "밤과 도토리를 같은 날 심었다"는 main 에서도 빨갛다**(main 워크트리 `259dadb6` 단독 실행:
  "밤 ✗ · 도토리 ✗" · 이 가지: "밤 r5 · 도토리 ✗"). 이 카드는 클라 상수 한 줄과 원경 층만 만졌고 심기는 서버 행위다 —
  **T392 몫이 아니다.** main 에서 이 빨강을 낸 카드는 가르지 않았다(회부).

## §5 회부 — 구현 금지

1. ★**`TERRAIN_SEG_INDEX=1` 을 켤지**(PM/재민) — 능선 색인은 이미 있고 답이 같다(×9.5). 켜는 것이 곧 "능선 선분 색인 구현"이다.
   2026-08-31 에 `TERRAIN_TILE_CACHE` 와 함께 "실운영에 켤까?"로 회부된 채 남아 있다(`인계/Z-존서버.md:1137`).
2. ★**열린 땅 330ms = WebGL 물·산 → 2D `drawImage` 되읽기**(세션4 T395) — GPU 브라우저에서 한 번 재야 헤드리스 몫이 갈린다.
3. **거리 vignette 가 1,500 을 따라간다**(재민 실기) — 가까운 사물이 덜 흐려진다(600px 0.15 → 0.72). 650 의 흐림을 원하면 **이름이 둘** 필요하다(새 수).
4. **T384 두 팔 표는 도착 미확인** — "컬링이 싸다"는 다시 서지 않았다(임업3 A1 12.0 · B 13.9 · 후 15.9). PM 결정의 근거는 클릭·채집 · 층 하나로 남는다.
5. #67 ①②④(재민 · 실기) · Safari 실기(재민 · 배포 뒤 멀리 나무 + 클릭).
6. **`e2e-nature` 심기① 이 main 에서 빨갛다**(밤·도토리 같은 날 심기) — 귀속 안 함(PM · 야간 러너 목록에 올릴 것).
