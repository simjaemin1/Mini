# 보고 — T294 화면 20fps → 시야 광선 O(선분²) 수리 (PM 직접 · 재민 "당장 해결해봐" · 2026-09-14)

**한 줄 답: 범인은 안개 시야 광선이었다 — 광선마다 모든 선분을 재는 O(선분²) 가 마을 안(벽 수백)에서 프레임을 먹었다. 각도 통(72) + 벽 선분 캐시로 검사 수 1/11(선분 112 자리) · 마스크 픽셀 0px 차이 · 제품 한 파일 · 서버 0.**

- 실측(재민 사파리 · 마을 · 건물 2,353): **fps 20 · render 46ms** = 캔버스 API 합 **3~5ms**(drawImage 1,287장 1.5ms) + 시계 넷(tile 0.4 · water 4.9 · mt 0.1 · nat 0.4) **6ms** + **나머지 35ms 가 JS**. 안개 격자·마름모(P2D.lineTo 3,623/f · Set.add 5,545/f)는 아니었다.
- 컨테이너 프로파일(같은 클라 · 766채): 인라인 렌더 JS 와 `rsi`(광선×선분 교차)가 self 시간 상위 — 마을 안 벽 셀 하나 = 선분 하나 · 선분마다 광선 6 · 광선마다 전 선분 ⇒ 벽 500 이면 ~2,600 광선 × 700 선분 ≈ **1.8M rsi/프레임**(≈35ms 와 맞는다). 나무(22그루 132선분)·바위(90)는 상한이 있었고 **벽만 없었다**.
- 수리(`public/client/34-m-renderloop.js` 한 파일 · 그림 로직 0): ⓐ 벽 선분 목록을 **내 셀·층·벽지도 판**으로 캐시(바위 `_rockOccCache` 문법 · 종전은 매 프레임 존 전체 벽 키를 `split`) ⓑ 선분을 원점 기준 **각도 통 72개**에 넣고(양쪽 한 통 여유 · 원점 지나는 선분은 전 통) 광선은 제 통만 본다 — 최소 t 가 같으니 다각형이 같다. 끔 손잡이 `window.__fogNoBucket`(진단 · 종전 전수).
- 하네스 `scripts/e2e-fogray.js`(`@regress` · `@nightly A`) **10/10**: A(통)↔B(전수) 같은 자리 번갈아 6장 → 마스크 알파 **0px 차이**(두 자리) · 결정성 0px · 반례 = B 의 rsi 가 **11.1배 · 10.7배**(4,877 vs 54,320 · 5,784 vs 61,712). 린트 42/42 · `test-client-globals` 26/26.
- ★안 쓴 것: 같은 줄 벽을 한 선분으로 잇는 런 병합(84→21)도 재 봤다 — 끝점 정확히에서 쏘는 광선의 `u<=1` 부동소수 경계가 달라져 그림자 가장자리가 ≤0.2px 움직인다(1,160px · 최대 Δ34). 픽셀 동일이 아니라 뺐다(코드에 적어 뒀다).
- 재민 실기: 배포 뒤 같은 콘솔 한 줄(`fps` · `[render] avg`)로 **60 · avg ≤ 16ms** 인가. ms 는 이 보고에 없다(기계 의존 · 족보 80) — 자는 rsi 호출 수.
- 회부: 안개 격자 `getImageData` 매 프레임(97×97 · Set.add 5.5k/f)은 다음 자리(내 셀이 바뀔 때만) · `welcome` 이 존 자원 전부를 클라에 보내는 것(렌더가 매 프레임 전부를 훑는다 — 이번 실측에선 작았다).
