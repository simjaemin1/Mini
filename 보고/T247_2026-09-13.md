# T247 — 목재는 새는 게 아니라 **안 들어온다**. 나무꾼이 0명이다 (세션5 · 2026-09-13 · **가지만**)

> 카드 `~/Mini/지시_T247.md` · 가지 `batch/hunt-wood-server-0912` · **측정 베이스 `06a654fb`**
> 만진 것: `scripts/t222-life800.js` **만**(수지 열) · **제품 코드 0** · `t176-ab` **무접촉**(T207 열을 그대로 읽었다).
> **답 한 줄 — 갈리는 항은 **유입**(`dailyProductionBuf.wood` = 하루 1부터 끝까지 **0**) · 빼서 사는 하위계 **없음** ·**
> **그 위의 갈림은 `land.fertility` **0.42 vs 0.99** — 두 자가 **다른 땅**을 세운다. 실서버 한 줄에 목재 열 갱신 ○.**

---

## §0ⓐ 목재 수지 — 두 자를 같은 칸으로

두 자 모두 `dailyProductionBuf.wood`(유입)와 `_consDay.wood`(유출 = 연료 + 건축 둘뿐 · T207 이 못 박았다)를 읽는다.
헤드리스 자의 열은 `t176-ab.js:338~339` 와 **같은 식**이다(사본 0).

### 헤드리스 자 (시드 1020 · 51마을 합)

| 날 | 목재 재고 | **일 유입** | 일 유출 | 순 | **나무꾼** | `woodSustain` 합 | 연료충당 |
|---|---|---|---|---|---|---|---|
| 1 | 6,088.5 | **0** | 37.15 | −37.15 | **0** | 0 | 1.000 |
| 5 | 5,827.9 | **0** | 37.86 | −37.86 | **0** | 0 | 1.000 |
| 20 | 4,152.8 | **0** | 64.98 | −64.98 | **0** | 0 | 0.982 |
| 40 | 2,333.9 | **0** | 46.76 | −46.76 | **0** | 0 | 0.522 |
| 60 | 1,104.4 | **0** | 35.90 | −35.90 | **0** | 0 | 0.392 |
| 80 | 331.8 | **0** | 20.19 | −20.19 | **0** | 0 | 0.241 |
| **100** | **0** | **0** | 2.51 | −2.51 | **0** | 0 | 0.094 |
| 200 | 0 | **0** | 0 | 0 | **0** | 0 | 0.083 |

### econ 자 (`t176-ab` · 같은 씨 · 200일 누적)

| woodProd | woodCons | woodBuilt(건축) | woodFuel(연료) | woodImported(교역) | **나무꾼 마을·일** |
|---|---|---|---|---|---|
| **28,756.7** | 24,553.8 | 3,835.8 | 20,718.0 | 4,729.4 | **33,361** |

⇒ **갈리는 항은 유출이 아니라 유입이다.** 헤드리스 자는 800일 내내 목재를 **한 톨도 안 만든다**.
재고는 창설 부존 6,088 에서 연료·건축이 갉아 **날 100 에 0** 이 되고, 그 뒤엔 유출도 0(줄 게 없다) ·
연료 충당률이 1.000 → 0.522(40일) → 0.094(100일)로 떨어진다. 굶주림은 그 뒤를 따라온다(T241 §0ⓐ).

### ★그 위의 갈림 — **두 자가 다른 땅을 세운다**

| 땅 값(중앙) | econ 자 | 헤드리스 자 | |
|---|---|---|---|
| `land.wood` | 0.57 | **0.57** | 같다 |
| `land.game` | 0.52 | **0.52** | 같다 |
| **`land.fertility`** | **0.99** | **0.42** | **절반 이하** |
| `land.woodSustain` | (설정됨) | **전부 null**(합 0) | 다르다 |

숲과 사냥터는 **같은데 비옥도만 절반**이다. 식량 밑변이 반이면 노동이 전부 먹는 데 묶이고
**벌목에 한 명도 안 간다** — 그것이 유입 0 의 위쪽 원인이다(econ 자는 같은 씨에서 나무꾼 33,361 마을·일).
`woodSustain` 이 전부 `null` 인 것은 "상한 미적용" 규약이라 그 자체론 무해하지만,
**두 자가 다른 땅을 세운다**는 두 번째 증거다(`villages.js:439` `extractSustain` 이 채우는 키들).

## §0ⓑ 하위계 빼기 — 자가 뺄 수 있는 것은 둘뿐이고, 둘 다 안 멈춘다

| 하위계 | 자가 뺄 수 있나 | 팔 결과(200일) |
|---|---|---|
| 광맥 `_oreMineDaily` | **뺄 수 있다**(`deps` 를 안 넘기면 된다 = `T232_RAW=1`) | T232 실측 — **무변**(82 · 45/51) |
| T135 나무 층 `attachToWorld` | **뺄 수 있다**(`T135_TREES=0` — 제품 손잡이) | **인구 1,167 · 소멸 0/51 · 목재 첫 0 = 날 100 · 나무꾼 0** — **무변** |
| 전쟁 `war-core`/`war-live` | **못 뺀다** — `Villages.init` 안에서 세운다(손잡이 없음) | — |
| 겨울 `Winter.init` | **못 뺀다** — `villages.js:5804` | — |
| 사건 장부 `_initLedger` | **못 뺀다** — `villages.js:2610` | — |
| 영토 확장 `_terrGrow` | **못 뺀다** — 일일 슬라이스 | — |
| 캐러밴 실체 `tickCaravanBodies` | **못 뺀다** — `onGameTick` 안 | — |
| 도적 `bandits.init` | **이미 안 돈다** — `zone.js` 가 부른다(자는 안 부른다) | — |

⇒ **빼서 사는 하위계는 없다.** 그리고 ⓐ가 이미 답했다 — 갈림은 하위계가 아니라 **세우는 땅**이다.
(생활층 master 를 꺼도 안 멈춘다는 T241 §0ⓑ 와 같은 방향이다.)

## §0ⓒ 실서버 한 줄 — **목재 열을 더했다**(재민 실기 · 자 코드 0 · 새 문 0)

```sh
sudo docker exec durango-zone-hanbando node --experimental-sqlite -e '
const db=require("./server/zone-local-db");const r=db.getVillagesByZone("hanbando");
let pop=0,dead=0,ever=0,h=0,lj=0,wood=0;const g=[],w=[];
for(const v of r){if(!v.econ_state)continue;const e=JSON.parse(v.econ_state);const n=(e.npcs||[]).length;
 pop+=n;if(e._everPop){ever++;if(n<=0)dead++;}
 g.push((e.land||{}).game||0); w.push((e.land||{}).wood||0);
 wood+=(e.storage||{}).wood||0; h+=(e.counts||{}).hunter||0; lj+=(e.counts||{}).lumberjack||0;}
g.sort((a,b)=>a-b); w.sort((a,b)=>a-b);
console.log(JSON.stringify({day:r[0]&&r[0].day,villages:r.length,pop,dead,ever,
 hunter:h,lumberjack:lj,woodStock:+wood.toFixed(1),
 gameMed:g[g.length>>1],woodMed:w[w.length>>1],game045:g.filter(x=>Math.abs(x-0.45)<0.005).length}));'
```

**읽는 법**: `lumberjack` 이 **0 이면 제품**(실서버도 나무꾼을 안 둔다 — 그러면 자가 아니라 세계의 문제다),
**0 이 아니면 자**(헤드리스 조립이 땅을 다르게 세운 것 — §0ⓐ 의 `fertility` 0.42 가 범인 후보).
`woodStock` 이 0 에 가깝고 `woodMed` 는 멀쩡하면 이 자가 본 것과 **같은 모양**이다.

## §1 검증

* T241 표 **비트 동일 재현**: 200일 인구 1,167 · 소멸 0/51 · 도구Q 644 · 생곡 4.6 · 목재 첫 0 = 날 100.
* 같은 씨 두 판(120일) **전 열 diff 0**(수지 열 포함).
* `t176-ab` **무접촉**(T207 이 이미 둔 열을 읽기만 — 여덟 수 비트 동일).
* `server/villages.js` **diff 0** · 하네스 신설 0 · 새 손잡이 0 · 실서버 읽기 0.
* 접점: `scripts/t222-life800.js` · `scripts/t176-ab.js`(읽기) · `server/villages.js`·`server/sustain.js`(읽기).

## §2 회부(구현 0)

1. ★★★**[새 카드 · PM] 두 자가 세우는 땅이 다르다 — `land.fertility` 0.42 vs 0.99.**
   숲(0.57)·사냥터(0.52)는 같은데 비옥도만 절반이고, `woodSustain` 은 헤드리스에서 전부 `null` 이다.
   `Villages.init` 의 `seedVillages`→`extractLandParamsApprox`→`extractSustain` 경로와
   `t176-ab` 의 `pickSeedVillages`→`village-layout.generate` 경로가 **같은 51마을에 다른 값을 준다**.
   **어느 쪽이 제품의 참값인지**가 먼저다 — 실서버 한 줄(§0ⓒ)의 `lumberjack`·`woodMed` 가 그것을 가른다.
2. ★★**[확정] 목재는 새는 게 아니라 안 들어온다** — 유출(연료 20,718 · 건축 3,836)은 econ 자와 같은 꼴이고,
   헤드리스 자의 유입만 **0** 이다. "목재 수지"를 처방으로 여는 카드는 **유입 쪽**만 보면 된다.
3. ★**[실기 · 재민] 위 한 줄** — 돌려 붙이면 "제품이냐 자냐"가 끝난다.
4. **[그대로] #6 값(재민 · 자가 서면) · 벌목 장부 · `_clearedFrac`(#24) · 기준선 넷째 판(T244 · 세션3).**
