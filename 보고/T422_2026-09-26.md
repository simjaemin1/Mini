# T422 — tools 버림 14 → `tools/_rnd_archive/` · 남는 10 의 정본 자리 [승인 대기 · T422] (2026-09-26 · 세션8/ART)
**한 줄 답:** 14 폴더를 `git mv` 로 옮겼다(56장 이름바꿈 · 46장 100% · 10장 99%). 옮긴 폴더 안에서 바꾼 것은 경로 **11줄**이 전부다(`parents[k]` 의 k 만 · 로직 0 · 밖에서는 `phrase.py` 글자 4줄 · 표식 셋 · `test-assets-audit ⑩`). 24 폴더 자기 시험은 **옮기기 전과 같다**(230 중 229). 남는 10 의 자리는 **정본 옆 2 · 아직 모름 4 · 참고 4** 다(판정 0 · 설계 11장 판정의 입력). ⚠T411 정정 둘: `source-led-stem-mixer` 의 버림 근거가 틀렸다(R&D-07·08 은 source-led 편곡을 "즉시 경로"로 적는다 — 되살림 후보로 회부). `phrase.py` 흔적은 3줄이 아니라 4줄이고, 그 함수는 처음부터 `public/tools/…` 를 봤다.
가지 `batch/tools-archive-0926` = `8067ca7` · 베이스 `7b7d4b9`(main) · force 0 · main 은 PM
**`bgm.js`·배포 13곡·`sfx/**` 무변** · 새 파일 = `tools/_rnd_archive/README.md` · `.gitattributes` · 값 0 · 새 소리 0

| 셋 | 결론(실측) |
|---|---|
| ① 이동 | MIDI-DDSP 줄기 7 · 청취 묶음 줄기 5 · `source-led-stem-mixer` · `ddsp`(T353)를 `tools/_rnd_archive/<이름>/` 로 옮겼다. `git log --follow` 가 이어진다(`ddsp/train.py` → T353 `c6b7f7f` · `midi-ddsp-runtime` → GPT `462b4ac`). 경로 11줄: **시험이 깨진 6폴더는 6줄로 다시 섰다**(contour 한 줄이 depth·fade 까지 · timbre 1 · score-adapter 2 · stem-mixer 2). 시험은 안 깨졌지만 레포 뿌리 기본값이 `tools/` 를 가리키게 된 **5줄**도 고쳤다(daegeum-adapter 2 · runtime 1 · transfer-train 2). 남는 10 폴더 시험 **무변** · `test-daegeum-transition-bank` **8/0** |
| ① 흔적·표식 | `phrase.py` **4줄**(:167 · :174 · :182 · :188 — `tools/ddsp` → `tools/_rnd_archive/ddsp`). 모델 0 이라 동작은 무변이다(`test_curves.py` PASS · 백엔드 B 오류 문구만 새 자리). 보관소 `README.md` 한 장. `.gitattributes` 규칙 한 줄(`linguist-vendored export-ignore` — `git archive` 에서 보관소 파일 0). `README-BGM.md` 한 줄. `test-assets-audit ⑩`: 두 Dockerfile 의 `COPY` 를 읽어 보관소 57장 중 이미지에 실리는 것 **0** · 미끼 `COPY . .` 이면 **57/57** 문다 |
| ② 정본 자리 | 남는 10 = **정본 옆 2**(`score-expression` 표현 층 · `bgm-samples` 합성 입력 — **맥에서 돈다**: 백업 4묶음 해시 일치 · 121파일) · **아직 모름 4**(`daegeum-vibrato-reference` #66 · `ddsp-gugak-public-runtime` 라이선스 · `expressive-synthesis` #64 ②③ · `daegeum-transitions` 권리·설계 판정) · **참고 4**(목록·권리·출처·반입 게이트). 설계 11장 × 폴더 맞물림 표 — 후보 3(02·04·08)이 말하는 폴더는 정본 옆 2 · 아직 모름 2 · 참고 4 · **보관소 8**(RND_08 이 source-led stem 과 MIDI-DDSP 를 말한다) |

게이트 `test-audio` **170**/0 · `test-bgm-phrase` 16/0 · `e2e-bgm-phrase` **12**/0 · `test-assets-audit` PASS(+⑩ 넷) · `--selftest` PASS · `test-render-score-articulation` 14/0 · `test-daegeum-transition-bank` **8**/0 · `test-harness-lint` 61/0 · `test-client-globals` 26/0 · 접점 `e2e-audio-probe` 71/0 · `test_curves.py` PASS · tools 24 자기 시험 **230 중 229**(전과 같다)

---

## §① 이동

### ①-1 무엇을 어디로 (T411 §③-3 표 그대로)

| 묶음 | 폴더 → `tools/_rnd_archive/` | 파일 | 자기 시험 전 → 뒤 |
|---|---|---|---|
| MIDI-DDSP 줄기 7 | `midi-ddsp-score-adapter` · `midi-ddsp-runtime` · `midi-ddsp-audition-pack` · `midi-ddsp-daegeum-timbre` · `midi-ddsp-daegeum-adapter` · `midi-ddsp-transfer-train` · `model-provenance` | 27 | 4/4 · 5/5 · 2/2 · 2/2 · **5/6** · 7/7 · 3/3 → **같다** |
| 청취 묶음 줄기 5 | `yoseong-audition-pack` · `full-ari-yoseong-…` · `full-ari-reference-contour-…` · `…-depth-…` · `…-fade-…` | 20 | 5/5 · 4/4 · 7/7 · 7/7 · 8/8 → **같다** |
| 홀로 2 | `source-led-stem-mixer` · `ddsp` | 3 · 6 | 6/6 · 시험 없음 → **같다** |
| 합 | 14 폴더 | **56 이름바꿈**(46장 100% · 10장 99%) | 옮긴 쪽 66 절 중 65(전과 같다) |

* `git mv` 만 썼다 — 이름바꿈이 **내용 무변**으로 잡힌다(`git diff -M --summary`). 옛 자리에는 무시된 찌꺼기도 안 남았다(`tools/` 목록 = 남는 10 + `_rnd_archive`).
* 남는 10 폴더 자기 시험도 **전부 같다**(5 · 61 · 6 · 30 · 21 · 9 · 3 · 3 · 26 · 시험 없음 1). `scripts/test-daegeum-transition-bank.py` **8/0**(스키마·빌더를 `tools/daegeum-transitions` 에서 읽는다 — 그 폴더는 남는다).

### ①-2 경로 11줄 — `parents[k]` 의 k 만 (로직 0 · 줄 수 = 바뀐 줄 전부)

| 폴더 | 줄 | 전 → 뒤 | 까닭 |
|---|---|---|---|
| full-ari-reference-contour | 시험 :18 | `REPOSITORY_ROOT = HERE.parents[1]` → `[2]` | 시험 7 에러(`tools/tools/daegeum-vibrato-reference/…`). depth·fade 시험이 이 모듈을 픽스처로 읽어 **셋이 이 한 줄로 선다**(15 에러 → 0) |
| midi-ddsp-daegeum-timbre | :146 | `parents[1]` → `[2]` | 옆 `daegeum-transitions/native_wav.py` — 시험 1 에러 |
| midi-ddsp-score-adapter | :45 · 시험 :17 | `HERE.parent / "score-expression"` → `HERE.parents[1] / …` | 옆 `score-expression` import — 시험이 import 에서 멈췄다. 모듈 줄은 CLI 가 쓴다(시험만 고치면 CLI 가 숨어서 깨진다) |
| source-led-stem-mixer | :31 · 시험 :17 | `HERE.parent / "daegeum-transitions"` → `HERE.parents[1] / …` | 옆 `native_wav` import — 같은 꼴 |
| midi-ddsp-daegeum-adapter | :81 · 시험 :16 | `parents[2]` → `[3]` · `HERE.parents[1]` → `[2]` | 시험은 안 깨졌다. 그런데 레포 뿌리 기본값이 **`tools/`** 를 가리키게 됐다(빨간 절 1 은 레포 밖 파일 단언 — 전과 같게 빨갛다) |
| midi-ddsp-runtime | :55 | `parents[2]` → `[3]` | 같다(`default_repo_root`) |
| midi-ddsp-transfer-train | :480 · :831 | `parents[2]` → `[3]` | 같다(데이터 목록 뿌리 · `--repository-root` 기본값) |

* 확인: 세 기본값이 이제 레포 뿌리와 **같다**(모듈을 불러 잰다 · transfer-train 기본 설정·잠금 파일이 새 자리에 있다).
* ★**시험이 깨진 6폴더 = 6줄**(카드의 "한 줄씩"). score-adapter·stem-mixer 는 모듈 줄이 하나씩 더 있다 — 시험 줄만 고치면 시험은 서지만 CLI 가 옛 자리를 본다.
* **안 바꾼 것**: 폴더 안 README 의 `tools/<이름>/…` 명령(보관소 README 가 읽는 법을 적는다) · transfer-train 계보 문자열 `tools/midi-ddsp-runtime/run_official_flute_rnd.py`(설정과 검사기가 **같은 글자**를 맞대는 감사 기록 · 파일을 여는 경로가 아니다) · 보고·설계·지시의 옛 경로(역사).

### ①-3 제품 쪽 흔적 — `phrase.py` 4줄 (T411 정정)

| 줄 | 전 → 뒤 | 성격 |
|---|---|---|
| :167 | 독스트링 `tools/ddsp/models/<inst>/` → `tools/_rnd_archive/ddsp/models/<inst>/` | 글자 |
| :174 | 주석 `tools/ddsp/export.py 가 놓는다` → `tools/_rnd_archive/ddsp/…` | 글자 |
| :182 | 오류 문구 `…모델이 없다: tools/ddsp/models/{inst}/` → `tools/_rnd_archive/ddsp/…` | 글자 |
| :188 | 경로 조각 `"tools", "ddsp", "models"` → `"tools", "_rnd_archive", "ddsp", "models"` | 경로 |

* 동작 무변: 전후 모두 `render_curve(backend=None)` → `A(합성) · B 없음` · `backend="B"` → `RuntimeError`(문구만 새 자리) · `test_curves.py` **PASS**.
* ⚠**T411 정정 ①** — T411 은 "`phrase.py` 3줄 · `ddsp_model_dir` 가 옛 자리를 보고 None"이라 적었다. **4줄**이다(:188 은 `"tools", "ddsp"` 가 조각으로 갈라져 있어 `tools/ddsp` grep 에 안 걸렸다). 그리고 그 함수는 **처음부터 `tools/ddsp` 를 안 봤다**: `dirname` 을 4번만 올라가 `public/` 에서 멈춘다 ⇒ `public/tools/…/models/<inst>` 를 본다(T353 `c6b7f7f` 부터 · 모델이 한 번도 없어 드러난 적 없다). 이 카드는 **문자열만** 옮겼다(로직 0) — 한 칸 더 올리는 고침은 회부.

### ①-4 보관소 표식 셋

| 자리 | 한 것 | 확인 |
|---|---|---|
| `tools/_rnd_archive/README.md` | 한 장 — 왜 옮겼나(T386·T411 표 인용) · 14 폴더 표 · 바꾼 줄 11 + `phrase.py` 4 · "R&D 미확정 · 정본 아님" · 되살리는 법(`git mv` 역 + 그 폴더 경로 줄 역) · 알려진 결함 | — |
| `.gitattributes`(새 파일) | 주석 한 줄 + 규칙 **한 줄** `tools/_rnd_archive/** linguist-vendored export-ignore` | `git check-attr` 보관소 set · 남는 폴더 unspecified · `git archive HEAD` 에 보관소 **파일 0**(빈 폴더 줄 하나) · 남는 `tools/` 는 그대로 실린다(90 항목) |
| `README-BGM.md` | 끝에 한 줄 — "쓰는 것·참고 10 폴더는 `tools/`, 버림 14 폴더는 `tools/_rnd_archive/`(T422 · 정본 아님)" | `test-audio ⑨k·⑨l` 초록(목록 줄 무관) |
| `test-assets-audit ⑩`(새 절) | 두 Dockerfile 의 `COPY` 원본(`package*.json server public sim`)을 읽고 ⑨ 의 `.dockerignore` 자로 거른다 | ⑩a 보관소 57장 중 이미지 **0** · ⑩b `public/` 아래 `_rnd_archive` 0 · ⓐ 대조 `bgm.js`·`server/animals.js` 실림 · `보고/` 안 실림 · ⓑ 미끼 `COPY . .` → **57/57** · `.dockerignore` 에 `tools/` 를 더하면 0 |

* ⑨ 의 Docker 규칙 셋(`toRe`·`parse`·`excluded`)을 블록 밖으로 **그대로** 옮겨 ⑩ 과 나눠 쓴다(몸 무변 · ⑨ 출력 무변). `test-assets-audit.js` +64 −3.
* 배포 이미지는 원래 `tools/` 를 안 싣는다(`COPY server public sim`). ⑩ 은 그 사실이 **흔들리면 무는 자**다(누가 `COPY . .` 를 쓰면).

## §② 남는 10 의 정본 자리 (판정 0 — 설계 11장 판정의 입력)

**자리 규칙**(수 0 · 사실에서 갈린다) — T353 3층 = 작곡(`compose.py`·`arirang.py`·`motif.sigim()`) → **표현** → **합성**(`gugak.py`/샘플 뱅크):
* **정본 옆** — 한 층을 건드리고, 도는 자리가 있고(레포 또는 뱅크가 있는 맥), 막는 열린 물음이 없다.
* **아직 모름** — 한 층을 건드리지만 열린 물음(#64·#66·권리·설계 판정)이 막는다.
* **참고** — 층을 안 건드린다(목록·권리·출처·반입 게이트 — 층 앞의 문).

| # | 폴더 | 층 | 실행(T411 · 이번) | 막는 것 | 설계·보고가 말하는 곳 | 자리 |
|---|---|---|---|---|---|---|
| 1 | `score-expression` | **표현** — 악보 → 제어 곡선(F0·세기·숨·유성·몸짓) 컴파일러 · 계획 7장 | **돈다** — B2-Rdf 계획 → NPZ 배열 19 + manifest v2(34.80초 · 3,481프레임) · 계획 5장 재생성 **비트 동일** | — (계획 B2-R 계열은 #66 후보지만 컴파일러는 안 막힌다) | RND_02(후보) score→expression 계약 · RND_08(후보) 4·5단계 · 보고 WIND_SLUR(후보)·SCORE_PHRASE_COMPAT | **정본 옆** — T353 `phrase.py` 와 **같은 층의 둘째 구현**(계획은 `arirang.py` 격자를 손으로 옮겼다 · import 0) |
| 2 | `bgm-samples` | **합성 입력** — `bk_*` 백업 격리 복원 | 컨테이너는 안 돈다(백업이 맥에만) → ★**맥에서 돈다**(이번 · `--verify` 읽기만: 대금 a·b + 정악대금 = 4묶음 해시 일치 · **121파일 · 68.7 MB** · 정악 `_index`·`_hold` 보존) | 대금 4묶음만 다룬다(12묶음 중) · 산조 조각은 12:22판으로 다시 스캔해야 한다(T391 레시피) | RND_02(후보) "`bk_*` 백업 · 정악 113조각은 재-scan 하지 않고 그대로 복원" = 이 도구의 계약 | **정본 옆** |
| 3 | `daegeum-vibrato-reference` | 표현 재료 — NGC 녹음에서 뽑은 요성 참조 곡선(`reference_shape_unreviewed` · 산출 JSON 을 컴파일러 :84·계획 셋이 읽는다) | 안 돈다(192 source 특징 캐시 `_bgm_rnd`) | **#66**(B2-R 계열 청취 — 재민) · YOSEONG_POLICY(정책 재민) | 설계 0 · 보고 YOSEONG_POLICY(참고)·FULL_ARI_REFERENCE_CONTOUR(참고) | 아직 모름 |
| 4 | `ddsp-gugak-public-runtime` | **합성** — 공개 DDSP-Gugak 대금 체크포인트로 계획 렌더 | 안 돈다(체크포인트·checkout 없음) | **라이선스**(T386 회부 5 — 저장소 URL·가중치 조건) | 설계 0 · 보고 착지메모 | 아직 모름 |
| 5 | `expressive-synthesis` | **합성** — 악보 조건 렌더러 틀(기준선·런처·manifest 게이트·미학습 미리듣기) | **돈다(판정만)** — "ready": false · 막힌 것 넷 | **#64 ②③**(짝 데이터 · 4060) | RND_08(후보) 3단계 resynthesis · 우선순위 4 "CPU smoke + 4060 WSL" | 아직 모름 |
| 6 | `daegeum-transitions` | 합성 재료 — 실녹음 전이 bank · 궤적 검색 · 악구 경계 · source-led contour(11 CLI) | 안 돈다(원음) · ⚠`native_wav.py` exit 0 거짓 성공 | NGC 확장 음원 **권리**(비ML 이용 조건 · 재민) · source-led 길의 **설계 판정**(RND_07 참고 ↔ RND_08 후보) | RND_06·07·NGC_EXTENDED_INGEST·SOURCE_LED_CONTOUR·phrase_boundary_triage(참고 5 — 경로로) · RND_08(후보) 우선순위 2·3 | 아직 모름 |
| 7 | `gugak-corpus-inventory` | 층 밖 — 말뭉치 목록(sha256) | **돈다** — 레포 `public/assets/audio`·`sfx` 118파일 · 중복 0 | — | RND_04(후보) 원본 48 collection 목록 정정 · RND_08 우선순위 1 | 참고 |
| 8 | `ngc-rights-audit` | 층 밖 — 권리 게이트(1유형 비ML ↔ AI 학습 차단) | 안 돈다(NGC 192 목록 · 네트워크) | 판정은 재민 | NGC_EXTENDED_INGEST(참고) · RND_08 1단계 | 참고 |
| 9 | `ngc-source-receipts` | 층 밖 — 「단음」 다운로드 영수증 검증 | 안 돈다(영수증 0) | — | RND_02(후보) "백업 안에 다운로드 영수증이 없다" | 참고 |
| 10 | `aihub-paired-ingest` | 층 밖 — AI Hub 짝 데이터 반입 게이트 | 안 돈다(번들 없음) | #64 ② | RND_01(버림)·RND_08 1단계(개념) | 참고 |

**합**: 정본 옆 **2** · 아직 모름 **4** · 참고 **4**. 남는 10 중 **돈다 4** — 컨테이너 3(T411: 1·5·7) + **맥 1**(이번: 2). T411 "돈다 5" 의 나머지 둘(`midi-ddsp-score-adapter` · `midi-ddsp-transfer-train`)은 보관소로 갔다.

### ②-2 설계 11장 × 폴더 — 맞물림 (T386 권고 옆에)

| 문서 | T386 | 경로로 부르는 폴더 | 뜻으로 닿는 폴더 |
|---|---|---|---|
| RND_01 | 버림 | `tools/ddsp`(1 — *"현 DDSP 스캐폴드는 실제 조건부 악기 렌더러가 아니다"*) → **보관소** | `aihub`(AI Hub 언급) — 도구는 `scripts/render-bgm-phrase-ab.js`·`bgm.js` opt-in |
| RND_02 | **후보** | 0 | score→expression 계약 → `score-expression` · `bk_*` 복원 → `bgm-samples` · 영수증 없음 → `ngc-source-receipts` |
| RND_03 | 버림 | 0 | 산조 청취본 V3 → `bgm/render_village_day_ari_sanjo_audition.py`(도구 폴더 아님) |
| RND_04 | **후보** | 0 | 원본 48 collection 목록 → `gugak-corpus-inventory` · V4 → `bgm/render_village_day_ari_native_pitch_audition.py` |
| RND_05 | 참고 | 0 | V5 → `bgm/render_village_day_ari_level_matched_audition.py`(도구 폴더 아님) |
| RND_06 | 참고 | `daegeum-transitions`(6) | 전이 bank 계약 — `scripts/test-daegeum-transition-bank.py` 가 스키마를 지킨다 |
| RND_07 | 참고 | `daegeum-transitions`(1 · `trajectory_retrieval`) | *"Source-led 편곡 (즉시 가능한 경로)"* → 악구 경계(`daegeum-transitions`) + stem 렌더 = **`source-led-stem-mixer`(보관소)** |
| RND_08 | **후보** | 0 | 1단계 권리·manifest → `ngc-rights-audit`·`aihub-paired-ingest` · 우선순위 1 → `gugak-corpus-inventory` · **2 "phrase boundary triage + source-led stem"** → `daegeum-transitions` + **`source-led-stem-mixer`(보관소)** · 3 contour → `daegeum-transitions` · 4 → `expressive-synthesis` · 5 → `score-expression` · *"MIDI-DDSP 공개 구현은 archived … 원칙과 eval 설계만 참고"* → **MIDI-DDSP 줄기 7(보관소)** |
| NGC_EXTENDED_INGEST | 참고 | `daegeum-transitions`(5 · `fetch_ngc_extended_daegeum`) | 권리 → `ngc-rights-audit` |
| SOURCE_LED_CONTOUR | 참고 | `daegeum-transitions`(4 · `source_led_contour`) | — |
| phrase_boundary_triage | 참고 | `daegeum-transitions`(3) | — |

* **후보 3(02·04·08)** 이 말하는 폴더: 정본 옆 2(`score-expression`·`bgm-samples`) · 아직 모름 2(`expressive-synthesis`·`daegeum-transitions`) · 참고 4(`gugak-corpus-inventory`·`ngc-rights-audit`·`ngc-source-receipts`·`aihub-paired-ingest` — 마지막은 RND_08 1단계 "학습용 manifest" 게이트로 뜻으로만) · **보관소 8**(RND_08 의 MIDI-DDSP 7 + source-led stem 1). 남는 10 중 **8** 이 후보 문서에 닿는다.
* **참고 6** 은 거의 다 `daegeum-transitions` 하나를 말한다(5장이 경로로).
* **버림 2** 는 `tools/ddsp`(보관소)와 도구 폴더 밖 청취 스크립트를 말한다.
* 설계 11장이 **안 말하는** 폴더: `ddsp-gugak-public-runtime`·`daegeum-vibrato-reference`(보고서만) · 청취 묶음 5(보고서만).
* ⚠**맞물림이 어긋나는 자리 하나**(T411 정정 ②): RND_08(후보)이 우선순위 2 로, RND_07 이 "즉시 가능한 경로"로 적은 **source-led stem** 의 도구(`source-led-stem-mixer`)는 이 카드로 보관소에 있다. T411 은 버림 까닭을 "R&D-07 이 닫은 방향"이라 적었는데 틀렸다. R&D-07 이 "없다"로 닫은 것은 **아리랑 악보를 충실히 부른 source 악구**(192 source · 15,392 창 · 승격 0)이고, source-led **편곡**은 다음 순서 1 로 열어 두었다. 지금 못 도는 것은 맞다(입력 `span_audition.json`·원음 스템이 레포 밖). 그래서 옮기기는 카드대로 했고 되살림은 회부했다 — `git mv` 역 + 경로 2줄 역이면 된다.

## §카드 전제 대조

| 카드가 적은 것 | 실측 |
|---|---|
| "옮긴 6폴더 시험이 옆 폴더 경로로 깨지는 것은 한 줄씩" | 6폴더가 **6줄**로 선다(contour 1 줄이 depth·fade 까지 · score-adapter·stem-mixer 는 모듈+시험 2줄씩). 시험은 안 깨지지만 뿌리 기본값이 틀어진 **5줄**을 더 고쳤다(같은 꼴 · 경로만) |
| "제품 흔적(`ph…` — T411 이 적은 그것)" | `phrase.py` **4줄**(T411 은 3줄) · 그 함수는 처음부터 `public/tools/…` 를 본다(T411 "옛 자리를 본다" 정정) |
| "`.gitattributes`/`README-BGM.md` 한 줄" | `.gitattributes` 는 **없던 파일**이다 — 새로 만들어 규칙 한 줄(+주석 한 줄) · `README-BGM.md` 한 줄(+빈 줄) |
| "`test-assets-audit` PASS(+ `_rnd_archive` 가 배포 폴더 밖인 절)" | ⑩ 넷(⑩a·⑩b·ⓐ·ⓑ) — 배포는 `docker build` 의 `COPY server public sim` 이라 원래 밖이다. 절은 그 사실을 **Dockerfile 에서 읽어** 지킨다 |
| "남는 10 중 '돈다 5'" | 5 중 **3** 이 남는다(2 는 보관소) + `bgm-samples` 가 맥에서 돈다 ⇒ 남는 10 중 **돈다 4** |
| "`test-audio` 170 · `e2e-bgm-phrase` 12 · `test-daegeum-transition-bank` 8" | 170 · 12 · 8 — 그대로 |

## §검증

* `git mv` 만 — 56 이름바꿈(46장 100% · 10장 99% = 경로 한 줄씩) · `git log --follow` 이어짐(`ddsp/train.py` → `c6b7f7f` T353 · `midi-ddsp-runtime/…` → `462b4ac` GPT).
* 로직 diff **0** — 바뀐 줄 전부: 보관소 경로 **11줄**(10 파일 · 전부 `parents[k]`/`HERE.parent`) · `phrase.py` **4줄**(글자 3 · 경로 조각 1) · 새 절 `test-assets-audit ⑩`(+64 −3 · ⑨ 규칙 셋은 옮기기만) · 새 문서 둘(보관소 README 76줄 · `.gitattributes` 2줄) · `README-BGM.md` +2.
* tools 24 폴더 자기 시험 — 옮기기 전 **230 중 229** → 뒤 **230 중 229**, 폴더마다 같다(`midi-ddsp-daegeum-adapter` 5/6 은 레포 밖 파일 단언 · 전과 같게 빨갛다). 네트워크 0(`unshare -n`) · 서명 끔(이 상자 전역 git 설정 — T411 과 같은 까닭).
* 게이트: `test-audio` 170/0 · `test-bgm-phrase` 16/0 · `e2e-bgm-phrase` 12/0 · `test-assets-audit` PASS · `--selftest` PASS(3건) · `test-render-score-articulation` 14/0 · `test-daegeum-transition-bank` 8/0 · `test-harness-lint` 61/0 · `test-client-globals` 26/0 · `e2e-audio-probe` 71/0 · `public/assets/audio/bgm/test_curves.py` PASS.
* 파일: `bgm.js`·`bgm-loops.js`·13곡 `.ogg`/`.m4a`·`sfx/**`·`public/client/**` diff **0**. `git archive HEAD` 에 보관소 파일 0.
* 맥 `bgm-samples --verify` 는 스크립트를 **맥 VM 홈**(`$HOME/t422` · 사용자 폴더 밖)에 풀어 **읽기만** 돌렸다(sha256 `2a2bbd97…` = 레포 판 · `~/Mini/durango-mini` 에 쓴 것 0 — 그 checkout 엔 이 도구가 아직 없다).
* §2 ① 3시드 A/B — diff ∩ 랩 적재 목록 = ∅(도구 이동·하네스·문서·`phrase.py` 글자) ⇒ 해당 없음.

## §재현

```sh
# ① 옮김 확인
git diff -M --summary 7b7d4b9..8067ca7 | grep -c rename            # 56
git diff -M -U0 7b7d4b9..8067ca7 -- tools | grep -E '^[-+][^-+]'   # 경로 11줄(+보관소 README)
git log --oneline --follow -- tools/_rnd_archive/ddsp/train.py
# ① 자기 시험 24 폴더(네트워크 0 · 서명 끔)
for d in tools/*/ tools/_rnd_archive/*/; do env GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false \
  PYTHONDONTWRITEBYTECODE=1 timeout 300 unshare -n python3 -m unittest discover -s "$d" -p 'test_*.py'; done
python3 scripts/test-daegeum-transition-bank.py
node scripts/test-assets-audit.js | sed -n '/^⑩/,/^$/p'
git archive HEAD | tar -t | grep -c '_rnd_archive/.'                 # 0
# ② bgm-samples(맥 · 백업이 있는 자리 · 읽기만)
python3 tools/bgm-samples/restore_backups.py --source-dir ~/Mini/durango-mini/public/assets/audio/bgm --verify
```

## §회부

1. **설계 11장 판정**(재민·PM) — §② 표(자리 · 맞물림)가 입력이다.
2. **`source-led-stem-mixer` 되살림**(재민·PM · 설계 판정 뒤) — RND_07·08 이 source-led 편곡을 "즉시 경로"로 적는다(T411 정정 ②). 되살리려면 `git mv` 역 + 경로 2줄 역.
3. **`phrase.py:ddsp_model_dir` 한 칸**(ART · 다음 카드) — `dirname` 4번 → 5번이어야 레포 뿌리(T353 부터 `public/tools/…` 를 본다 · 모델 0 이라 동작 무변 · 이 카드는 로직 0 이라 안 고쳤다).
4. **#66·#70·#72**(재민 · 브금판) — 무변. #66 청취본은 맥 `_bgm_rnd` 에 있다.
5. **AI Hub·4060**(#64 ②③) — `aihub-paired-ingest`·`expressive-synthesis` 가 그 문 앞에 있다(아직 모름·참고).
6. **GPT 다음 BGM 카드**(PM · ② 뒤).
