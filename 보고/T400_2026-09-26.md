# T400 — 집도 행위다 1층: 자재 정본 하나 · 크루가 곳간에서 통나무를 **손에 들고** 집터에 **놓는 순간** 단계가 오른다 (2026-09-26 · 세션4)

**한 줄 답:** 집 자재는 이제 표 하나(`server/hut-stages.js` · 통나무 22·풀 38 · 석재 0)이고 zone·쉼터·econ·랩 번들이 그 표를 **읽으며**, 켜면 크루 둘(직업 무관)이 곳간에서 통나무를 3짐(8·8·6) 날라 놓는 순간 단계가 오른다 — 끔 3시드 800일 **JSON 전체 동일 3/3** · 켬 30일 집 **끔 261 ↔ 켬 257**(수요가 정한다 · 잡음 안) · 걸음 **낮 +7.4%** · 관측자 게이트 **결정론 3/3 · 실서버 관측 6마을 6/6**.

**[승인 대기 · T400]** 가지 `batch/build-act-0926` = `16a6a90`(구현 커밋 1 · 이 보고 커밋이 그 위 하나) · 베이스 `057d40c`(main · 첫 판 베이스 `c445983` 에서 리베이스 · 재검 3시드 동일) · 손잡이 `T400_BUILD_ACT` **기본 끔** · force 0 · 배포 0.

* **ⓐ 자재 정본 하나** — `hut-stages.js` 가 정본 · zone(`HUT_STAGES`·`PSITE_COST`·레시피 셋·쉼터 표)과 econ(`hutEconMaterials` · 켬 단가 22/6 · 석재 0)이 **유도** · 표 한 줄을 고치면 따라온다(하네스가 고쳐 봄). 켬 3시드 광산 열(광산 마을 인구 · 석재 부족/과잉)은 **전부 못 가름**(T252) — 석재 실물 수요가 서기 전엔 이 자로 안 보인다.
* **ⓑ 행위 1층** — 모든 마을이 `_lifeDaily` 관측자 문 **앞**에서 `_t400BuildDay` 하나를 지난다(곳간 → 손 → 왕복 → 집터 · 곳간이 비면 안 오른다 · 시공은 `LIFE_STAGE_PDAY` 그대로). 켬이면 `_lifeHeadlessDay` 집 절 호출 0. 끔 3시드 = 8,318 · 7,935 · 7,853(넷째 판).
* **ⓒ 등가** — 실서버 30일(날 90 s): 집 261 ↔ 257 ↔ 켬+관측자 257 · 인구 1,078 ↔ 1,061 · 크루가 올린 107.7채 · 한 채당 걸음 74.7초 = 실제 낮의 7.4%(설계 중앙 7%) · 크루·일 = 전 인·일 1.5%(어부 3.5% — 표만). ⚠압축 날 4 s 자는 켬 팔을 못 잰다(왕복이 낮 2.8초에 안 들어가 50/50 마을 0짐 · 집 150 · 인구 600).
* **ⓓ 관측자 게이트** — 결정론 자 집 수·단계 시각·곳간 **비트 동일 3/3**(끔은 갈린다 — 미끼) · 실서버 켬 ↔ 켬+관측자 257 = 257 · 마을별 46/50(두 끔 팔 잡음 바닥 44/50) · 관측 6마을 집·전진 6/6.
* 게이트: `test-build-act` **51/51(신규)** · fieldyield 202 · forage 137 · wood 90 · fish 73 · farm-life 33 · farm-act 59 · headless-parity · hall-door-path · psite · shelter 47 · itemlabel 58 · lab-wiring 0 · 번들 8/0 · inline 최신 · harness-lint 61 · `e2e-village` 9/17(베이스도 같은 8건 — 상시 빨강).
* 회부만: #61·#62 뒤집기(재민 · `hut-stages.js` 한 줄) · 켜기(재민 · #69 와 같이) · 의뢰 집(`psite`)·플레이어 집이 같은 표인지 실기(재민) · 석재 실물 수요(PM) · 압축 날 계측기 규약(PM).

---

## §0 표

### ⓐ 자재 정본 하나 — diff 와 켬 3시드 광산 열

**diff(무엇이 한 표를 읽나)**

| 자리 | 전 | 후 |
|---|---|---|
| `server/hut-stages.js` | (없음) | **새 파일 · 정본** — `HUT_STAGES`(4단계) · `HUT_RECIPES`(기둥·서까래·이엉) · 유도 `PSITE_COST`·`stageRaw(i)`·`hutRaw()` |
| `server/zone.js` 플레이어 움집 | 표 리터럴 · 레시피 셋 리터럴 · `PSITE_COST` 리터럴 | `HutStages.HUT_STAGES` · `...HutStages.HUT_RECIPES`(키 순서 불변) · `HutStages.PSITE_COST` |
| `server/zone.js` 공용 쉼터 | `SHELTER_STAGES` 리터럴(움집 ②③④ 사본) | `HUT_STAGES.slice(1)` 유도(라벨 번호만 ①②③ · 값·글자 종전과 같음) |
| `sim/economy-sim.js` 집 증축 | `HOUSE_WOOD 1.5` · `HOUSE_STONE 2.5` /인 | 켬: 표 ÷ 채당 정원(`village-layout` 6×1) = 통나무 **22/6 = 3.667** · 석재 **0** · 끔: 종전 상수 그대로(대조 자) |
| 랩 번들 | era·specialty·econ 둘 | + `hut-stages` · `village-layout`(`window` 가림 — `window.VillageLayout` 안 덮음) |
| 하네스 넷(`psite`·`shelter`·`itemlabel`·`t361`) | zone.js 글자를 잘라 읽음 | `hut-stages.js` 를 require · 수 단언 그대로 |

한 채 = 통나무 22 · 풀 38(레시피로 유도) · econ 재화만 = **통나무 22**(풀 `fiber` 는 econ 재화가 아니다 — `RESOURCES` 밖). 단계별 [0 · 18 · 4 · 0].
**표 한 줄을 바꾸면 따라온다** — `test-build-act ①` 이 자식에서 기둥 7·석재 5 로 고치면 econ 이 통나무 25 · 석재 5/6 을 낸다(사본이면 안 따라온다).

**켬 3시드 800일 `t17-metrics`(econ 단독 자 · 켬 = 표에서 유도한 단가 · 이 자엔 생활층이 없어 `_t400Live` 0 — 곳간 차감은 추상 그대로)**

| 시드 | 팔 | 인구 | 광산 마을 인구(7곳) | 석재 부족 | 석재 과잉 | 목재 부족 | 목재 과잉 | 무기Q | 확장셀 | 도구Q |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1020 | 끔 | 8,318 | 864 | 335 | 300 | 597 | 291 | 1,306 | 9,602 | 16,596.5 |
| 1020 | 켬 | 7,999 | 828 | 351 | 331 | 676 | 250 | 1,309 | 8,844 | 16,282.4 |
| 7 | 끔 | 7,935 | 770 | 349 | 281 | 551 | 259 | 1,275 | 9,892 | 14,638.6 |
| 7 | 켬 | 8,192 | 919 | 356 | 335 | 658 | 279 | 1,366 | 11,731 | 15,757.6 |
| 42 | 끔 | 7,853 | 816 | 357 | 335 | 570 | 277 | 1,356 | 9,790 | 15,293.9 |
| 42 | 켬 | 7,961 | 833 | 310 | 305 | 524 | 214 | 1,390 | 8,515 | 15,906 |
| **짝 Δ%** | | −3.8 · +3.2 · +1.4 | −4.2 · +19.4 · +2.1 | +4.8 · +2.0 · −13.2 | +10.3 · +19.2 · −9.0 | +13.2 · +19.4 · −8.1 | −14.1 · +7.7 · −22.7 | +0.2 · +7.1 · +2.5 | −7.9 · +18.6 · −13.0 | −1.9 · +7.6 · +4.0 |
| **T252 자** | | 못 가름 | 못 가름 | 못 가름 | 못 가름 | 못 가름 | 못 가름 | 방향만(+3.3 · 폭 6.9) | 못 가름 | 못 가름 |

소멸 0·0·0(두 팔 모두). 석재·목재 열은 `t17` 의 부족/과잉 사건 수(ⓒ) · 광산 마을 = 이름이 `광산` 으로 시작하는 7곳.


읽기(T252 자): 석재 수요가 집에서 사라져도 **광산 열은 잡음 밖으로 안 나온다** — 광산 마을 인구·석재 부족·석재 과잉 모두 **못 가름**(부호 갈림). 통나무 단가가 2.4배(1.5 → 3.667)가 됐는데도 목재 부족 건수도 못 가름. ⇒ 석재 **실물 수요**(화덕·석기·석축)가 따로 서기 전에는 "광산 수요가 준다"를 이 자로 못 본다(회부 · PM).

### ⓑ 행위 1층 — diff 와 끔 3시드 동일

**diff(`server/villages.js` 집 절만 · `_lifeHeadlessDay` 집 절만 · T368 밭 절·`_t325Scan`·A*·존 틱 본체 무접촉)**

* 새 함수 다섯: `_t400Crew`(명부 앞 `min(LIFE_CREW, 인구)` · 요양·관측 상태 안 봄) · `_t400PerLoad`(`carry.CAP_KG ÷ weights.kgOf` · 통나무 8 = `_t341TreesPerLoad(1)`) · `_t400From`(집터 최근접 곳간 사다리 · 없으면 회관) · **`_t400BuildDay`**(하루) · `_t400BodyStep`(관측 마을 몸 — 보여 주기만).
* `_t400BuildDay`: 모자란 자재가 있으면 짐 하나씩 — `actFromGranary`(곳간 → **손** `inventory`) → 왕복 1(걸음초 = 2·거리 ÷ `moveSpeed`) → 집터에 **놓는다**(손 → `site.mat`) · 짐은 **이 집에 남은 몫까지** 꽉 채워 든다(8·8·6 = 3짐) · 자재가 차고 시공(한 사람 하루 `LIFE_STAGE_PDAY`)이 남으면 **그 순간** `_lifeAdvanceSite` · 곳간이 비면 멈춘다 · 한 사람 하루 왕복 상한 = `_t341TripsPerDay`.
* 부르는 자리: `_lifeDaily` 의 관측자 문(`anyViewerNear`) **앞** 한 줄 — 모든 마을이 같은 줄을 지난다.
* 켬이면 꺼지는 셋: `_lifeHeadlessDay` 집 절(호출 0) · 몸의 마을 집터 시공 진척(과업 내려놓음) · 반일 징발. 의뢰 집터(`psite`)는 종전 길.
* econ: `actFromGranary`(= `actToGranary` 의 역 · `_cons` 흐름 기록) · `buildActOn(v)`(= 켬 && `v._t400Live`) → 증축의 목재 제약·추상 차감을 끈다(차감은 크루가 진다).
* 계측: `/lifedbg` 마을 칸 `t400`(누계 · 끔이면 null) · 하네스 문 `__labProbe._t400Probe`.

**끔 3시드 800일 `t17-metrics` — 베이스 워크트리 ↔ 가지(끔)**

| 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 생곡 | JSON 전체 | 로그 diff |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|---|
| 1020 | 8,318 | 0/51 | 1,306 | 9,602 | 2,504 | 16,596.5 | 4,284.2 | 74,094.9 | **동일** | 0줄 |
| 7 | 7,935 | 0/51 | 1,275 | 9,892 | 2,151 | 14,638.6 | 4,719.3 | 74,291.4 | **동일** | 0줄 |
| 42 | 7,853 | 0/51 | 1,356 | 9,790 | 2,361 | 15,293.9 | 3,885.9 | 76,117.9 | **동일** | 0줄 |


= `인계/공통.md` §2 넷째 판 표 그대로(리베이스 전 베이스 `c445983c` 판도 JSON 3/3 동일).


### ⓒ 등가 — `t361-build-days` 켬/끔 30일(실서버 · 51마을 · 집 = house+phouse+shelter)

| 팔(30일) | 날 | 집 | 인구 | 크루가 올린 채 | 곳간 빈 날(마을·일) |
|---|---|---:|---:|---:|---:|
| 끔 | 4,000 ms | 269 | 1,171 | (헤드리스) | — |
| 끔 · **베이스 워크트리** | 4,000 ms | 269 (house 219 = 219 · 마을별 같음 44/50) | 1,164 | — | — |
| 켬 | 4,000 ms | **150** | **600** | **0**(왕복 0 인 마을 **50/50**) | 0 |
| 끔 | **90,000 ms** | **261** | 1,078 | (헤드리스) | — |
| 켬 | **90,000 ms** | **257**(−4 · −1.5%) | 1,061 | 107.7(전진 323 · 왕복 328 · 통나무 2,395.9) | 12 |
| 켬 + 관측자 6 | **90,000 ms** | **257** | 1,060 | 108.0(전진 324) | 19 |

* **집 수 — 수요가 정한다(같다)**: 끔 261 ↔ 켬 257 ↔ 켬+관측자 257. 두 끔 팔(가지 ↔ 베이스 워크트리)의 잡음 바닥이 마을별 44/50 이라 −4 는 그 안이다.
  ⚠카드의 "+119" 는 T361 판(09-23 main)이 1일 기준선에서 잰 증분이다 — 오늘 main 의 같은 자는 끔 **+42**(4 s)·**+34**(90 s)다(베이스 워크트리도 같은 수 · 이 가지 무관).
* **낮 — 한 채당 걸음 74.7초 ÷ 실제 낮 1,008초 = +7.4%**(T361 설계 같은 식 · 중앙 7% · 최대 12.6%) — 설계 중앙보다 0.4p 크다(설계는 마을 중앙값, 이 수는 107.7채 평균). 압축한 날(낮 63초)에선 크루 낮의 26% 가 걸음이다.
* **제 일을 못 한 몫(표만 · 세계엔 아직 안 빠진다)** — 크루·일 486 = 전 인·일의 **1.5%**: 어부 284(어부 인·일의 **3.5%**) · 농부 154(1.3%) · 사냥꾼 48(1.0%). 크루가 명부 앞 두 사람이라 그 마을의 첫 두 직업에 몰린다. ⚠이 층은 몸만 빼고 econ 산출은 안 뺀다(어부가 집을 짓는 날 물고기 −3.5% 를 물리는 것은 이 카드 밖 · 표만).
* ⚠**압축 날 계측기(4,000 ms/일 · 낮 2.8초)는 켬 팔을 못 잰다** — 곳간↔집터 왕복(중앙 750 px ÷ 64 px/s × 2 = 23초)이 낮에 안 들어가 **50/50 마을이 한 짐도 못 나른다** → 집 0 · 침상이 막혀 인구 600 ↔ 1,171. 세계가 아니라 자의 문제다(실제 낮 1,008초엔 중앙 43왕복/일). 켬 팔은 날 ≥ 약 63초로 잰다 — 위 90,000 ms 판(회부: 계측기 규약 · PM).

### ⓓ 관측자 게이트

| 자 | 켬 | 끔(미끼) |
|---|---|---|
| **결정론 자**(`test-build-act ④` · 같은 세계 두 팔 · 관측자 없는 팔은 `_lifeDaily` 처럼 헤드리스도 부름 · 12일 · 3마을) | **집 수·단계 시각(`_t400Log`)·곳간 재고 비트 동일 3/3** | 갈린다(헤드리스가 관측자 문 뒤) |
| 구조 | `_t400BuildDay` 에 `anyViewerNear` 0 · 부르는 줄이 관측자 문 앞 · 크루 고르기가 요양·관측 상태 안 봄 | — |
| **실서버 90 s**(켬 ↔ 켬+관측자 6) | 집 257 = 257 · 마을별 같음 46/50(두 끔 팔 잡음 바닥 44/50) · **관측한 6마을 집 6/6 · 전진 단계 6/6 같음** | — |

### ⓔ 한 줄 답

위 머리 첫 줄.

---

## 부록 — 게이트 로그 한 줄씩(리베이스 뒤 · 베이스 `057d40c1`)

`test-build-act` 51/51 · `test-econ-fieldyield` 202/0(main 이 199 → 202 로 늘었다) · `test-forage-act` 137/0 · `test-wood-act` 90/0 · `test-fish-act` 73/0 · `test-farm-life` 33/0 · `test-farm-act` 59/0(T368 하네스 — 집 절 글자 읽기에 손잡이 가드 허용 한 줄) · `test-headless-parity` PASS · `test-hall-door-path` 진단 완료 · `test-psite-server` PASS(반일 징발 글자 읽기 가드 허용 한 줄) · `test-shelter` 47/0 · `test-itemlabel` 58/0 · `lab-wiring-check` 실패 0 · 경고 0 · `build-econ-bundle --check` 8/0 · `inline-engine --check` 최신 둘 · `test-harness-lint` 61/61 · `e2e-village` 9/17 — 베이스 워크트리 같은 8건(야간러너 09-26 상시 빨강).

## 부록 — 판 목록

* 3시드: `/tmp/t400/r2/{base-off,br-off,br-on}-{1020,7,42}.json`(리베이스 뒤) · 리베이스 전 판(`c445983c`)도 끔 3/3 동일이었다.
* 실서버: `t361-build-days.js run`(4 s 여섯 팔) · `T212_DAY_MS=90000` 세 팔(`off90`·`act90`·`acteye90` — `t361-build-days.js` 위치 인자로 표) · 베이스 워크트리 끔 4 s 한 팔. 실서버 판은 리베이스 **전** 트리(T400 코드는 같다 · 그 사이 main 은 손잡이 끔 카드와 비트 동일 카드만 앉았다).
