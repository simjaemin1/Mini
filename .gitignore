**/node_modules
**/*.db
**/*.db-shm
**/*.db-wal
**/*.db.legacy
.DS_Store
*.log
.fuse_hidden*
*.bak
*.bak-*

sim/_*.js

# sqlite runtime
*.db
*.db-journal
*.db-wal
*.db-shm

# 10차: 렌더 산출물(에셋 원본은 public/assets 로 복사해 커밋)
scripts/bridge_renders/
scripts/_t4-*.js
\nscripts/_t3-*.js
scripts/_t3-*.js
scripts/_t4b-*.js
scripts/_t4b-*.js

# 배치 19: 지형 질감 베이크 산출물(에셋은 public/assets/terrain 으로 복사해 커밋)
scripts/terrain_tex/

# 배치 20: 산 세그먼트 베이크 산출물(에셋은 public/assets/mountains 로 복사해 커밋)
scripts/mountain_renders/

# 배치 21: 자연물 베이크 산출물(에셋은 public/assets/{trees,nature} 로 복사해 커밋 — nature-postprocess.py)
scripts/nature_renders/
scripts/building_renders/
# ★[T67] 가구 렌더 중간 산물 — 정본은 `scripts/props_render.py`, 배치본은 public/assets/props · icons
scripts/icon_renders/
scripts/crop_renders/
scripts/field_renders/
scripts/crop_icon_renders/
scripts/props_renders/
scripts/props_icon_renders/
# 푸시 번들(맥으로 전달용 · 레포에 넣지 않는다)
미푸시_*.bundle
*.bundle

# ★[T129] 대조표 — 카드마다 재민 눈으로 볼 그림을 여기 짓고 **채팅으로 바로 보낸다**(파일로 전달).
#   위 렌더 산출물과 같은 갈래다: 굽는 표에서 언제든 다시 만들어지는 **파생물**이라 레포에 안 넣는다
#   (T106 `범프끝_산지면다리_전후.png` · T120 `밭_쉼_그루터기.png` · T129 `나무_종_열매.png` — 전부 그랬다).
#   ⇒ 규약이 코드에만 있고 `.gitignore` 엔 없어서 **훅이 카드마다 "커밋 안 한 파일" 이라고 알렸다.**
산그림/

scripts/__pycache__/
scripts/char_renders/
assets-src/*.blend1

# [T107] 굽기 중간 산물 — 후처리 전 float 시트(EXR). 재생성: python3 scripts/char_render.py -- --exronly
assets-src/char_raw/
