# 인계 — SOUND 소리 (세션9)

> 영역: `public/client/48-a-audio.js` · `public/assets/sfx/**` · `public/assets/audio/bgm/`(잇기만 · 음원 무변)
> · `CREDITS.md` 소리 절 · `scripts/test-audio.js` · 이 파일.
> 다른 영역에 쓸 말은 `인계/회부.md` 에 한 줄. `공통.md`·`README.md` 는 PM 만.
> 세운 카드: **T261**(층 하나 · BGM · 첫 8종의 자리 · main `c90930f`) ·
> **T283**(둘째 묶음 훅 · 발신→수신 · 페이드/실내 · 야생 키 4 · main `fd0c3db`) ·
> **T292**(리미터 실측 · BGM 전환 표 · 악기 출처 정정 · 2026-09-14).

---

## 0. 한 줄

**소리가 난다 — 키 18종 중 16종이 이어졌고, 소리는 이제 "눌렀다"가 아니라 "일어났다"에서 난다.**
층·키 표·훅 **4곳**·하네스가 서고 BGM 은 실제로 울린다.
미확보는 **둘**뿐이다(`harvest` · `boar_grunt`) — 무음이고 빨강이 아니다.
★T283 이 도는 사이 **T282 가 야생 셋(`wolf_growl`·`tiger_growl`·`deer_call`)을 닫았다** —
키 행과 훅을 같은 커밋에 넣었더니 그 파일들이 **코드 0줄로** 그대로 울었다(그게 이 층의 계약이다).

★**T283 이 T261 의 회부 둘을 닫았다**:
① **발신 → 수신** — `axe`·`eat`·`harvest` 가 `sendPrimary`(보내는 자리)에 붙어 있어 마을 밖에서 눌러도,
   거절당한 동사도 소리가 났다. 이제 `handleMessage`(**받는 자리**)에서 난다 ⇒ 세계가 그 일이
   일어났다고 말할 때만 울고, **자원 자리에서 나므로 남이 옆에서 베는 나무도 들린다**(발신 판엔 없던 것).
③ **T272 둘째 묶음 일곱**(`water`·`rain`·`bird`·`bronze_hit`·`cast`·`bite`·`hook`)이 디스크에만 있고
   키도 훅도 없던 것 ⇒ 이었다. `test-assets-audit` 의 **`sfx` 고아가 26 → 2** 가 됐다(남은 둘은 `ui_click` — 훅이 없는 후보다).

⚠**이 카드는 25 커밋 뒤에 착지했다.** T261 이 도는 동안 T262·T266·T269·T272 가 먼저 들어가
음원 13개와 `asset-lock`·`test-assets-audit` 확장을 이미 해 뒀다. 그래서 이 문서는
"세션9 가 붙인다"고 저쪽이 적어 둔 자리들을 **실제로 붙인 뒤**의 상태다.

## 1. §0 실측 — 카드의 전제 둘이 틀렸다

| 카드가 말한 것 | 실측 |
|---|---|
| "BGM 은 **음원 13곡 + 완성 재생기**다. 배선만 없다" | **재생기가 파일을 안 받는다.** `bgm.js` 는 `DurangoBGM.create/start/setScene/setMood/setIntensity/setDayPhase/setVolume` 인 **절차적 Web Audio 엔진**이다(악보·장단·아리랑 16마디가 코드 안에 있다). 디스크의 13곡(.ogg/.m4a)은 `compose.py` 가 그 엔진을 **오프라인으로 뽑아 둔 것**이고 **런타임이 한 장도 안 읽는다**. ⇒ "잇기"는 파일을 트는 것이 아니라 엔진을 부르는 것이다. |
| "`50-i-panel` 의 **있는 설정 자리**에 볼륨 칸 셋을 한 줄로" | **설정 자리가 없다.** `public/` 전수에 '설정'·'옵션'·'볼륨' 0곳. ⇒ 남의 파일에 패널을 짓지 않고 소리 층이 **제 칸을 제가** 만든다(오른쪽 아래 `소리` 단추 + `Shift+Y`). 다른 파일 0줄. |
| "수확·먹기·도끼질 훅은 **서버 동사 결과가 오는 `30-n-net.js` 핸들러**" | **그런 메시지가 없다.** `doEat`·`tryHarvest`·`gatherResource` 전수 — 서버는 동사별 결과 타입을 안 보내고 `notice`·`gauges`·`inventory`·`resource_removed` 같은 **공용** 메시지만 낸다. 공용 메시지에 붙이면 **남이 한 일에도 소리가 난다**. ⇒ 발신 정본 한 자리(`sendPrimary`)에 붙였다. 맞바꿈은 **거절당한 동사도 소리가 난다**는 것. ⇒ 회부. |

그리고 카드가 맞힌 것: 클라에 재생 문이 **0** 이었다(`AudioContext`·`<audio`·`.play(` 전부 0곳).
`회부/회부_낚시_다음층.md` A-6 의 "BGM 미배선 유지가 캐논"은 **재민이 09-13 T261 로 뒤집었다**(그 줄을 이 카드가 갱신했다).

## 2. 규약 — 남이 지켜야 할 것

### ★★[T283] 표 셋이 **배선**이다 — 무엇이 어떤 키를 내는지 코드에 안 적는다
1차 판엔 `'wolf'`·`'campfire'` 같은 낱말이 층 코드 안에 있었다. 이제 표다:

| 표 | 무엇 → 키 | 어디서 읽나 |
|---|---|---|
| `resourceHit` | 자원 `type` → 키 | 수신 `resource_update`·`resource_removed`(브로드캐스트 · 자원 자리 ⇒ 거리 감쇠) |
| `mobs` | 개체 `type` → 키 | 프레임 훑기(`scan`) |
| `buildings` | 건물 `type` → 키 | 프레임 훑기(반복 · 개체마다 하나 · 화면 밖이면 멎는다) |
| `fishState` | `fish_state.state` → 키 | 수신(좌표를 같이 보낸다 ⇒ 찌 자리) |

**소리를 더하는 값 = 표 한 줄 + (필요하면) 키 한 칸.** 코드를 고치는 것이 아니다.
`test-audio ⑦a` 가 표의 값이 전부 실제 키인지 본다(오타 한 글자면 그 소리가 **영영 안 난다** — 조용한 결함).
⚠개체 이름과 키 이름은 다르다: 멧돼지는 `wild_boar`(개체) → `boar_grunt`(키). 그래서 키를 개체 이름으로 안 쓴다.

### ★키 표가 정본이다 — `public/assets/sfx/manifest.json`
수는 **코드에 하나도 없다**. 볼륨·반경·반복·동시상한·쿨다운·감쇠식·버스 기본값·저장 키·BGM 페이드까지 전부 표다.
소리를 더하려면 **표에 키를 세우고 훅 한 줄**을 붙인다 — 코드를 고치는 것이 아니다.

```
keys.<key> = {
  file,        // 파일 이름(`public/assets/sfx/` 기준) 또는 null(=미확보 · 무음 · 빨강 아님)
  volume,      // 0..1 (효과음 버스 앞단)
  radius,      // 월드 px (32px = 1셀) · 0 = 위치 없는 소리
  loop,        // true = 환경음(개체마다 하나 · 화면 밖이면 멎는다)
  maxSame,     // 같은 키가 동시에 겹칠 수 있는 수(발자국 겹침 방지)
  cooldownMs,  // 같은 키의 최소 간격
  source,      // sources 의 id 또는 null
  gainK?,      // 세기를 볼륨으로 옮기는 계수(바람 1.4 = WX_TILT_K 와 같은 수 · 비 1.0)
  fade?,       // [T283] 반복 키 필수 — 페이드 **초**(setTargetAtTime 시상수). 세기가 크게 흔들리는
               //        소리(바람·비) 2.0 · 켜고 끄기만 하는 소리(불·물·새) 1.0
  indoorMul?,  // [T283] 반복 키 필수 — 실내 배율. 바람·비·새 0(완전히 멎는다) · 불 0.35 · 물 0.25
  sampleStride?, trees?, treeRadius?   // 그 키만 쓰는 칸(물 훑기 간격 · 새의 숲 문턱)
}
sources.<id> = { license, url, author, received }   // 네 칸 전부 — 하나라도 비면 하네스가 문다
```

* **감쇠는 `linear` 하나**다: `g = volume × max(0, 1 − d/radius)`. 역제곱을 안 고른 이유는 표에 적혀 있다
  (반경 밖이 **정확히 0** 이라 값이 하나다 — 역제곱은 컷오프를 또 정해야 해서 값이 둘이 된다).
* **미확보(`file: null`)는 빨강이 아니다.** "출처 없는 소리 0" 은 *나는* 소리에 걸린 규약이다.
  안 나는 키를 빨갛게 하면 음원이 들어오기 전까지 CI 가 계속 빨간데, 그건 결함이 아니라 순서다
  (`test-assets-audit ③` 의 "고아 표는 빨강이 아니다"와 같은 사고).

### ★음원 꼴 (T262·T266·T269 가 이미 이 꼴로 넣었다 — 다음 묶음도 같다)
* `.ogg` + `.m4a` **쌍** · 자리는 `public/assets/sfx/<key>.{ogg,m4a}`
* 단발 ≤ **3초** · 반복 ≤ **10초**(이음새) · 파일당 ≤ **150KB** (실측 최대 `water.ogg` 127.6KB)
* ★표는 **파일 이름을 그대로** 적는다(`"file": "wind.ogg"` — 어간 `wind` 가 아니다). 두 이유가 겹친다:
  ⓐ `asset-lock.keyOf` 가 소리만 **파일 이름을 키로** 쓴다(한 소리에 두 파일 — T266 이 거짓 빨강 둘로 배웠다).
  ⓑ `test-assets-audit` 이 `ogg`·`m4a` 를 **어간 대조에서 뺐다**(`wind`·`fire`·`eat`·`axe` 는 짧은 영단어라
     코드 아무 데나 걸려 "쓰는 데 없는데 쓰임 있음"이 됐다). **이름이나 URL 로 불러야** 고아가 풀린다.
* ★`.m4a` 는 `fileAlt` 로 같이 적는다. 장식이 아니다 — **사파리는 Ogg Vorbis 를 안 튼다.**
  층이 `canPlayType` 으로 **브라우저에게 물어보고** 고른다(짐작 0 · `test-audio ④` 가 그 한 줄이 있는지 본다).
* `sources.<id>` 네 칸(라이선스·URL·작성자·받은 날)을 채우고 **같은 값을 `CREDITS.md` §2-b 에** 둔다
  (크레딧 절은 세션8/ART 몫 — T261 은 안 건드렸다. 표 둘이 어긋나면 사람이 본다).
* 코드 수정은 **0** 이다. 파일이 들어오면 그대로 난다 — 6종이 실제로 그렇게 이어졌다.

### ★훅 규약 — 남의 파일엔 줄 하나 · 로직 0
훅은 `window.__sfx && window.__sfx.<메서드>(…)` 한 줄이고, **무엇이 울릴지는 소리 층이 고른다**.
`scripts/test-audio.js ③` 이 **파일당 두 줄 이상이면 문다**.

| 파일 | 줄 | 메서드 | 키 |
|---|---|---|---|
| `public/client/30-n-net.js` | **`handleMessage` 머리** | `recv(msg, c)` | `axe`·`bronze_hit`·`harvest`·`eat`·`cast`·`bite`·`hook` |
| `public/client/42-r2-char.js` | `drawCharSprite` 안 | `step(clip, frame)` (`isMe` 일 때만) | `step_dirt` · `step_grass` |
| `public/client/34-m-renderloop.js` | `renderables.sort` 다음 | `scan(renderables, worldCx, worldCy)` | 야생 4 · `fire` · `water` · `bird` |
| `public/client/37-r1-weather.js` | `drawWeather` 안 | `weather(w, indoor)` | `wind` · `rain` |
| `public/client/99-main.js` | `boot()` 다음 | `initAudio()` — **입구 한 줄** | — |

* 전역은 `window.__sfx` **하나**다. `test-client-globals` 는 **기준선을 안 늘렸다**(아래 §3).
* `48-a-audio.js` 는 **최상위 실행문 0** 이다 — 실려도 아무 일이 안 난다. 실행은 `99-main` 한 자리(T0-b).
* **`AudioContext` 는 첫 사용자 제스처(pointerdown·keydown·touchstart)에서만 난다.**
  `test-audio ④` 가 정적으로 검사한다(`new AC()` 는 `sfxWake` 안에만 있어야 한다).
* ★★**[T283] 소리는 발신이 아니라 수신에서 난다.** `sendPrimary` 는 **보내는** 함수다 — 거기서 울리면
  거절당한 동사도 운다. `test-audio ⑦b` 가 **발신 함수 안에 `__sfx` 가 0** 임을 정적으로 지킨다.
* ★★**[T283] 수신 훅은 `handleMessage` 의 첫 줄이어야 한다.** 아래에서 `c.resources.delete(...)` 가
  자원을 지우므로, 그 뒤에 부르면 **자리를 못 찾아 위치 없는 소리**가 된다. 장식이 아니라 계약이고
  `test-audio ⑦d` 가 줄 번호로 지킨다(훅 줄 < 지우는 줄).
* ★★**[T283] 클라가 서버의 성질에 기대면 하네스가 그 성질을 지킨다.** 먹기는 `gauges` 아홉 자리 중
  **`carry` 를 싣는 하나**(`zone.js:5114 doEat`)로 갈린다 — 서버를 안 고치고 소리를 성사에 붙이는 유일한 길이었다.
  그 수가 둘이 되는 날 소리가 **조용히** 틀려지므로 `test-audio ⑦f` 가 서버 소스를 읽어 "정확히 하나"를 센다.
  (이 하네스가 서버를 읽는 유일한 자리다. 서버는 안 만진다.)

### ★사본 금지 — 이 층이 부르는 정본들
| 알고 싶은 것 | 부르는 정본 | 지어내지 않은 것 |
|---|---|---|
| 발밑이 흙이냐 풀이냐 | `window.__tileStateAt(lcx,lcy)` + `SoilBase.biomeOf(존 바이옴).grass` 램프 | 문턱을 새로 짓지 않았다 — 클라가 **땅을 그릴 때 쓰는 그 자**다 |
| 내 자리 | `myAbsPredicted` | — |
| 밤이냐 | `isNight()` (20-r2-visibility) | — |
| 마을 안이냐 | `window.__evNearVid` (41-h-bubble · 260px) | 마을 술어를 새로 짓지 않았다 |
| 바람 | `wxState().wind` (server/wind.js `seasonWind` 의 부호 있는 계절풍) | 계수 1.4 는 빗줄기가 이미 쓰는 `WX_TILT_K` 와 **같은 수** |
| 발 딛는 판 | `char_meta.json` 의 clip frames (판 0 과 판 n/2) | 새 타이머 0 — 간격은 애니 fps 가 정한다 |

### ★BGM — 엔진을 부른다(재구현 0 · 재생기 수정 0)
* `DurangoBGM.create({ context: <소리 층의 AudioContext>, volume })` → `start()` → `setScene(s, fade)`.
  컨텍스트를 넘기므로 **AudioContext 는 여전히 하나**다.
* 장면은 **두 축**이고 고르는 것은 **표**다(`bgm.scenePick` · 네 칸 · T292). 층 코드에 장면 이름이 **없다**.
  ⚠`field:night` 이 `journey` 인 것은 **밤 들판 곡이 엔진에 없어서**다 — 지어내지 않는다.
  ⚠`bgm.js setScene` 은 **모르는 이름을 조용히 되돌린다** ⇒ `test-audio ⑨d` 가 표의 값이 엔진이 받는
  이름인지 대조한다(엔진의 거르는 집합을 읽어서 — 사본 0).
  ⚠**13번째 곡 `village_day_jeongak` 은 어디에도 안 걸린다** — 그 곡은 장면 축이 아니라 편성(정악) 축의
  산물이라 지금 구조로는 게임에서 들을 방법이 없다. 지어내서 걸지 않았다 ⇒ 회부.
* `battle` 은 **미배선**이다 — 클라에 '전쟁 중' 술어가 없다(`warActive`·`inWar` 0곳). 회부.
* `mood`(trad/amb/ari)는 **안 고른다** — `render-meta.json` 에 **태그 칸이 없다**(`seconds`·`rms_db`·`peak_db` 뿐).
  카드가 "표에 이미 있는 태그로 · 없으면 낮/밤만 · **지어내지 않는다**" 고 못 박았다.
  곡 이름의 `_trad`·`_amb`·`_ari` 는 표의 칸이 아니라 파일 이름이다.
* "곡 사이 무음 2~3초"는 **이 엔진에 자리가 없다** — 곡이 끝나는 순간이 없는 연속 엔진이다.
  대응값은 장면 전환 페이드 `bgm.sceneFadeSec = 4.0` 이다. 무음을 넣으려면 엔진을 고쳐야 하는데
  이 카드는 '재생기 수정 0' 이다 ⇒ 회부.

### ★★★[T292] 리미터 — **셈을 버리고 실측을 쓴다**
T261·T283 이 볼륨의 **합**으로 넘침을 셌는데, **합은 상한이지 피크가 아니다** — 파형이 같은 순간에
같은 부호로 겹치지 않는다. 실클라에서 진짜 버퍼를 오프라인 렌더해 전 표본을 세니:

| 조합 | 합 | 리미터 없이 |
|---|---:|---|
| 카드가 지목한 최악(7종) | 4.37 | 피크 **−3.40 dBFS · 클리핑 0** |
| T283 헤드룸 셈의 조합(5종) | 3.20 | −6.31 dBFS · 클리핑 0 |
| 소리 나는 **16종 전부** | 10.64 | +1.10 dBFS · 클리핑 8/352,800 |

⇒ 버스 기본값은 **내릴 이유가 없었다**(그대로). 리미터는 병적 조합용 보험으로 남긴다.

★★**그리고 T283 이 단 곡선은 리미터가 아니었다.** `bgm.js` 에서 그대로 가져온
`tanh(1.35x)/tanh(1.35)*0.93` 의 **원점 기울기가 1.4364(+3.15 dB)** 다 — 작은 신호를 **키운다**.
실측 대조: 모닥불 **하나**에 피크 −15.88 → **−12.90 dBFS**. 넘침을 막는 장치가 평소에 3 dB 키우고 있었다.
⇒ 곡선을 고쳤다 — **문턱 아래는 기울기 정확히 1**(한 톨도 안 건드린다), 위만 1.0 에 점근한다.
문턱은 표(`bus.limiter.knee` = 0.8)이고 **실측 최악 피크(0.6763)보다 1.46 dB 위**여야 한다 —
그래야 평소에 한 번도 안 문다(실측 0/400). **안전장치는 보이지 않아야 안전장치다.**
`test-audio ⑧` 이 `knee > _실측.worstPeak` 와 `_실측.soundingKeys == 지금 소리 나는 키 수`를 지킨다.

★**키를 더하는 카드가 할 일은 곱셈이 아니라 `__sfx.probe()` 한 번**이다. `_실측` 칸을 **셈으로 채우지 마라** —
T261·T283 이 셈으로 채웠다가 둘 다 틀렸다.
⚠펌핑을 잴 때는 **`pa > knee` 인 창에서만** 비율을 봐라. 문턱 아래에서는 `oversample:'2x'` 의 재표본 필터가
±0.1 dB 흔들고, 그 차이가 작은 `pa` 로 나뉘며 **−10.92 dB 짜리 허깨비**를 낸다(1차 판이 실제로 봤다).
⚠`bgm.js` 의 곡선은 **안 고쳤다**(재생기 수정 0) — 음악도 같은 +3.15 dB 를 먹고 있다 ⇒ 회부.

### ★[T283] 헤드룸을 셈으로 세던 판 (T292 가 실측으로 갈아 끼웠다 — 경위로 남긴다)
T261 이 세운 수(최악 동시 합 1.67 × 0.8 × 0.7 = 0.94)는 키 여덟일 때의 것이었다. 반복 셋이 들어오며
최악 합이 **3.2** 가 됐고(`wind .5 + rain .6 + water .55 + fire .75 + 단발 .8` — 새는 비 게이트에 막혀 빠진다)
×0.56 = **1.79 ⇒ 잘린다**. 버스를 0.34 로 내리면 소리 하나가 반토막이 되므로 대신
**효과음 버스 끝에 소프트 리미터**를 뒀다 — `bgm.js buildGraph` 가 같은 문제에 쓴 **tanh 곡선 그대로**다
(자를 두 벌 만들지 않는다 · 상한 0.93). ⇒ 효과음은 0.74 를 안 넘는다.
⚠BGM 은 이 버스를 안 지난다(`bgm.js` 가 `ctx.destination` 에 직접 물리고 제 리미터를 갖고 있다 ·
재생기 수정 0 이라 돌릴 길이 없다). 둘을 다 올린 최악이 1.07 — 거기선 목적지가 스스로 뭉갠다.
**키를 더하는 카드는 매니페스트 `_헤드룸` 의 곱을 다시 세라.** T283 이 세어 보니 T261 의 수가 이미 틀려 있었다.

## 3. 실측 표 — 카드가 예고한 수와 실제

| 카드 | 실제 | 왜 |
|---|---|---|
| `test-client-globals ③` 허용 목록 **14 → 15** | **등록 0 · 기준선 118 그대로** | 소리 층에 최상위 실행문을 **0개**로 지었다(입구는 `99-main` 한 줄). 늘릴 것이 없다. |
| 훅 **8곳** | **4곳**(+ 입구 1) | 키 8개가 훅 8곳을 뜻하지 않는다: 흙/풀은 한 줄이 지형으로 가르고, 늑대·모닥불은 `scan` 한 줄이 개체 목록에서 가르고, 먹기·수확·도끼질은 `sendPrimary` 한 줄이 가른다. 파일당 한 줄 규약이 그렇게 만든다. |
| 오디오 자산을 `asset-lock` 잠금표에 | **이미 돼 있다(T266)** | 이 카드가 더한 음원은 0장이고, 잠금은 **T266 이 먼저 열었다**(`ASSET_EXT` 에 `ogg|m4a` · `keyOf` 는 소리만 파일 이름 그대로). 실측 `--check` 478장 · 어긋남 0. ⇒ 회부할 것이 없어졌다. |
| — | **고아 82 → 66** | 매니페스트가 이름으로 부르자 `sfx` 26 → **14**(줄어든 12 = 이어진 6종 × 두 파일) · `audio/bgm` 55 → **51**(index.html·매니페스트가 `bgm.js`·`render-meta.json` 등을 부른다). 남은 `sfx` 14 = T272 둘째 묶음 일곱 × 2 — **아직 키가 없는 것이 맞다**(다음 카드). |

## 4. 길이 자 — `test-audio` 안에 **하나**

ogg 길이는 마지막 `OggS` 페이지의 granulepos ÷ 표본율로 잰다(Vorbis/Opus 공통).
**자를 먼저 검증했다**(족보: 자가 틀리면 없는 결함을 본다): 레포가 ffmpeg 로 직접 잰 단 하나의 값
`village_day_trad = 108.28초`(`render-meta.json _T257` 주석)를 **정확히 맞힌다**.

⚠**`render-meta.json` 의 `seconds` 에 이 자를 대지 마라.** 그 수는 `compose.py:676` 이 합성한 float 배열에서
잰 값이고 배포된 ogg 에서 잰 값이 아니다(레포가 이미 *"12곡 전부 안 맞는다"* 고 적어 뒀다).
1차 판이 그 표에 자를 대고 **없는 결함 9건**을 봤다.

★13곡 실측(초 · 배포 ogg):

| 장면 | trad | amb | ari | jeongak |
|---|---|---|---|---|
| village_day | **108.28** | 114.02 | 114.02 | 137.00 |
| village_night | 113.00 | 113.00 | 113.00 | — |
| battle | 112.88 | 112.88 | 112.88 | — |
| journey | 112.86 | 112.86 | 112.86 | — |

★읽히는 것 하나: **장면이 같으면 길이가 같다**(같은 마디를 편성만 바꿔 굽는다). 표의 수는 그렇지 않다 — 그게 위 경고의 증거다.
★그리고 `village_day_jeongak` 의 빈 세 칸(T257 이 "지어내지 않는다"고 비워 둔 것) 중 `seconds` 는
이 자로 **137.00** 이 나온다. 다만 그 표의 다른 12줄은 합성기 자로 적혀 있어 섞으면 자가 두 벌이 된다 ⇒ 회부.

### ★[T292] 악기 출처 표가 거짓말을 하고 있었다 — 두 표를 하네스가 맞대 놓는다
`render-meta.json` 의 `source.daegeum` 이 **"합성"** 이라 적혀 있었는데 **정악대금은 국립국악원 실제 녹음**이다
(`render_jeongak.py` 와 `CREDITS.md` §2 가 그렇게 적는다). 커밋 `f75e8158`("정악대금 도입")이 **곡은 넣고
표는 안 고친** 결함의 **세 번째** 흔적이다(곡 수 T246·T257 · `seconds` T257 · 악기 출처 T292).
`README-BGM.md` 도 "12곡"이라 적고 있었고 합성 목록에서 **북이 빠져** 있었다. 셋 다 고쳤다.
⇒ `test-audio ⑨l` 이 `source` 의 "합성" 칸과 README 목록을 **맞대 놓는다**(어긋나면 문다).
★라이선스는 안 달라진다(셋 다 공공누리 제1유형 · 문구는 `CREDITS.md` §1) — 다만 "출처 없는 소리 0"을
기계로 세는 날 이 표를 믿으면 **틀린 답**이 나온다. 그래서 고쳤다.
⚠판정은 **목록 줄 하나**에서만 한다(1차 판이 200자를 읽다 설명 문단에 속았다) — 그리고 그 줄을
그림문자로 거르지 마라(`test-harness-lint ②` 에 물린다).

## 5. 다음 층에게 [T292 갱신]

* **미확보 둘** — `harvest`(세 출처에 작물 뜯기가 없다 · T269) · `boar_grunt`(T282 가 야생 셋을 닫으며
  멧돼지만 못 찾았다). 음원이 생기면 **표의 `file`·`fileAlt`·`source` 세 칸**이 전부다.
  ⚠그 세 칸은 **세션8 자리**다 — 소리 층은 같은 줄을 안 만진다(T283 이 그 규약으로 나란히 갔다:
  세션8 이 세워 둔 `sources` 12줄을 한 칸도 안 고치고 그대로 썼고, 내가 임시로 지었던 id 다섯은 버렸다).
* ★**귀로만 갈리는 것 둘이 더 생겼다**(T282 실측): `wolf_growl` 파일은 **울부짖음(howl)** 이지 으르렁이
  아니고(자로 갈랐다 — 중심 1,100~1,240Hz · 500Hz 아래 0.4%), `deer_call` 은 **와피티(elk)** 다.
  키 이름은 **안 바꿨다** — 키 하나를 고치면 파일 둘·잠금표 둘·크레딧·표가 같이 움직이는데 값이
  "이름이 정확해진다"뿐이고, `mobs` 표가 개체↔키를 흡수하므로 이름이 같을 필요가 없다(`wild_boar → boar_grunt`).
  **어울리는지는 재민 귀.**
* **`ui_click`** 음원이 디스크에 있는데 **키도 훅도 없다**(T282 가 "후보"로 올렸다 · 고아 2장).
  UI 클릭음을 넣을지는 재민 판정이고, 넣기로 하면 **표 한 줄 + 훅 한 줄**이다.
* **밭 수확에는 아직 수신 자리가 없다.** `tryHarvest` 는 `building_updated` 를 브로드캐스트하는데
  그 메시지를 보내는 자리가 서버에 **아홉 곳**이라(NPC 농사·작물 성장 …) 수확만 골라낼 수가 없다.
  `harvest` 키는 지금 **자원 채집**(`berry_bush`·`herb`·`sapling`)에서 난다. ⇒ 회부(서버 카드).
* **다음 소리 묶음**(움집 안 · 청동 도구 다른 결 · UI 클릭음 · 처마 빗소리)은 **표 한 줄 + 키 한 칸**이다.
  이 파일 §2 를 읽고 오면 된다. ⚠키를 더할 때 `_헤드룸` 의 곱을 다시 세라.
* **`battle` 장면은 여전히 미배선** — 클라에 '전쟁 중' 술어가 없다(`warActive`·`inWar` 0곳).
* **`rain` 은 세계가 `precip` 을 보내는 날 저절로 난다** — 지금은 진단 훅 `__rainForce` 로만 켜진다
  (`server/weather.js` 에 강수 정본이 없다 — T93 이 적어 둔 그대로다. 소리 층이 지어내지 않는다).
* 실기는 **재민 귀**다 — 세션은 못 듣는다. 하네스가 잴 수 있는 것은 표·파일·훅·정적 구조·
  그리고 "소리가 실제로 났는가"(진단 계수)까지다.
