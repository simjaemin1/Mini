# 인계 — M 이동·입력 — 예측·보정·조준

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.
## 3-o. ★★2026-08-30 배치 — **가속 기반 이동 모델 + 조준 모드** (병행 세션: 인벤 정비 `f1659af` **위에 리베이스**)

> 재민 확정: **좀보이드식 관성**. 설계 근거 = 일관성 원칙의 **운동 버전** —
> *몸은 순간이동으로 최고속에 도달하지 않는다.*
> 이번 배치는 **몸만** 만든다. 전투의 살은 회부(`회부_이동조준_다음층.md`).

### ★★★이동을 고치려면 `public/move-model.js` **만** 고쳐라

```
  public/move-model.js   ← 이동 모델 **정본 하나**
     ├── server/zone.js   require('../public/move-model.js')     ← 같은 파일
     └── public/client.js <script src="move-model.js">           ← 같은 파일
```

`soil-base.js` 가 세운 그 규약이다(**사본 금지**). zone.js·client.js 는 **부르기만** 한다.
콜라이더(벽·나무·지형)는 호출자의 몫 — 모듈은 위치를 모르고 `{vx, vy, dx, dy}` 만 낸다.

★**왜 이게 급소인가**: legacy 시절에도 적분이 두 벌이었다(zone.js 7409 · client.js 7310).
곱셈 **순서**가 달라 마지막 ULP 가 어긋났지만 legacy 는 속도가 입력의 함수라 상태가 없어 안 쌓였다.
**가속 모델은 속도가 상태다** — 그 어긋남이 누적되고, 리컨실리에이션이 매 틱 되돌린다 = **떨림**.
⇒ 한 함수로 못 박았다. 이제 클라·서버가 **같은 줄**을 쓴다.

### ★§0 인벤토리 — 지시서 전제와 다른 게 셋이었다

1. **"클라 60fps 예측 vs 서버 10Hz 권위"가 아니다.** 실제는 **양쪽 다 30Hz 고정 스텝**이고,
   클라가 `PRED_STEP=1/30` 마다 입력 **1개**를 보내면 서버가 그걸 **1:1 로 재생**한다
   (Gabriel Gambetta 모델 · 틱당 최대 8개 catch-up). `moveDt = 1/TICK_HZ` 고정 — 가변 dt 가 아니다.
   ⇒ "60fps 적분 vs 10Hz 적분" 이라는 갈림 자체가 **이 코드엔 없다**.
   그래도 dt 불변 검사는 넣었다 — **프레임레이트 상수가 몰래 기어드는 걸 막는 자물쇠**로서.
2. **정본 이속은 65 가 아니라 `MOVE_SPEED = 64`** (2m/s · 32px=1m). 65.1 은 B-3 실측값이었다.
3. **배율은 이미 한 곳에 모여 있었다** — `moveMultOf(p) = Carry.combinedMove(신체, 과적)`.
   흩어져 있던 건 **곱하는 자리**(서버 1곳 · 클라 1곳)였고 그 둘이 이제 모듈 한 줄이 됐다.

그리고 **④ 공격은 무방향이다** — `tryAttack` 은 범위 안 가장 가까운 mob 을 친다(무수정 계약).
**⑤ 카메라는 `_camAbs`(월드) 하나로 15곳이 물려 있다** — 안개 원점·내 스프라이트·물 흐름·컬링이
전부 그 값이다. ⇒ 시야 밀기를 **월드에 더하면** 안개가 따라 걷힌다. 그래서 **화면 변환에만** 더했다.

### 손잡이 표 (전부 env · 기본 legacy)

| 손잡이 | 기본 | 뜻 |
|---|---|---|
| `MOVE_MODEL` | `legacy` | `legacy`(즉시 최고속 · 종전) / `accel`(등가속) |
| `MOVE_ACCEL_T` | `0.20` | 0 → **현재** 최고속 도달 초 (B-1 회부: "현재"의 뜻) |
| `MOVE_DECEL_T` | `0.15` | 키 뗌 → 정지 초 (30Hz 격자에선 실측 0.1667s — B-3) |
| `MOVE_AIM_SPEED_FRAC` | `0.45` | 조준 중 이속 배율 (64 → 28.8px/s) |

★**기본값은 legacy 다.** 회귀 전원이 legacy 로 돈다. 손맛 확정 후 **튜닝 배치**에서 전환한다.
★손잡이 표는 **서버가 `welcome.moveCfg` 로 실어 보낸다** — 클라가 표를 들면 그게 두 번째 사본이다.

### ★★보정은 위치+속도 **한 쌍**이다 (재민 질문에서 확정 → 실장)

가속 모델에서 속도는 상태다. **위치만 스냅하면 스냅 직후 두 적분 곡선이 다시 벌어져
보정이 영원히 반복된다.** ⇒ 서버 tick 이 `selfVx/selfVy` 를 실어 보내고 클라가 둘 다 앵커한 뒤
미ack 입력을 replay 한다. legacy 에선 안 보낸다(상태가 없다 — 페이로드 바이트 동일).

★실장하며 걸린 것: **33ms 정지 키프레임**. legacy 는 그걸 기록도 시뮬도 안 했다(정지 입력이
위치를 안 바꾸니 무해했다). accel 에선 **서버만 한 스텝 더 감속해 곧바로 갈린다.**
⇒ 불변식대로 기록·시뮬한다. 같은 이유로 빈 틱에서 `p.vx=0` 도 accel 에선 **안 한다**(상태를 지우면 갈린다).

### 실측 — 보정 짝 비교 (같은 각본 · 실클라 키보드)

```
                최고속      미끄러짐    보정 횟수   최종 어긋남
  legacy        64.00px/s    0.00px      200회       0px
  accel         64.00px/s    5.93px      200회       0px      ← ★같다
  accel 상승곡선  456ms:32 → 486ms:42.67 → 1071ms:64   (legacy 는 첫 표본부터 64)
```
★**accel 이 legacy 보다 나빠지지 않았다 — 떨림 없음의 실증.**
(둘 다 최종 0px 인 건 이 아키텍처가 입력 1:1 재생이라 **원래** 0 이기 때문이다.
 accel 이 상태를 들고도 0 을 지켰다는 게 이 표의 뜻이다.)

### 조준 모드 — 몸만

* 우클릭 **홀드** = 조준. ⓐ 페이싱이 **커서**를 따른다(이동은 WASD 그대로 → 옆걸음·뒷걸음이 자연 발생)
  ⓑ 이속 ×0.45 ⓒ 카메라가 커서 쪽으로 **최대 180px** 밀린다(이징 τ=0.12s · 뚝 이동 없음).
* 좌클릭(조준 중) = 공격. **배선까지만** — `player.aimX/aimY` 에 싣고 `tryAttack` 판정은 **무수정**.
* ★★**안개 계약 지킴**: 오프셋은 `camX/camY`(화면 변환)에만 더한다. `_camAbs`(월드)는 불변 ⇒
  안개 원점 이동 **0.000px**(`e2e-move` 실측). **커서로 안개를 걷지 못한다.**
  단 **부채꼴 방향은 커서를 따라 돈다** — 그건 카메라가 아니라 **몸이 실제로 도는 것**이다(회부 A-3).
* 새 패널 0 · 새 키 0(마우스 버튼만).

#### ★★★[T379 2026-09-23] 조준은 **포인터 캡처**로 잡는다 — `mouseleave` 로 끄지 마라

재민 실기 09-23: *"우클릭을 드래그하다가 다른 UI 위로 올라가거나 브라우저 밖으로 이동하면
취소되어서 화면이 제자리로 돌아온다."* 범인은 `canvas.addEventListener('mouseleave', _aimOff)` 한 줄이었다 —
**패널(HUD·미니맵·사이드)은 캔버스 위에 덮인 DOM** 이라, 포인터가 그 위로 가기만 해도 캔버스가 `mouseleave` 를 받는다.

⚠**그런데 그 줄만 지우면 더 나쁜 것이 남는다.** 재현 로그가 보여 준 한 겹 더:
패널 위에서 손을 떼면 **`pointerup`·`mouseup` 이 패널로 가고 캔버스엔 안 온다** ⇒ **떼어도 안 꺼지는 조준**.
⇒ 누른 순간 `canvas.setPointerCapture(e.pointerId)` 로 포인터를 잡으면 밖으로 나가도 전부 캔버스로 온다.
`pointerup`·`pointercancel` 에서 **캡처를 반드시 반납**하고(`releasePointerCapture`) 끈다.
`blur` 는 **남긴다**(탭 전환은 풀리는 게 맞고, 캡처는 창을 못 넘는다).

⚠⚠**`pointerdown` 에서 `preventDefault()` 를 부르지 마라 [T379 실측 · 1차 고침이 여기서 죽었다].**
`pointerdown` 을 막으면 브라우저가 **호환 마우스 사건을 아예 안 만든다** — 로그에서 `mousedown(2)`·`mouseup(2)` 이
통째로 사라진다. 그런데 **동사 메뉴(`46-h-verbs`)는 누른 시각을 `mousedown(2)` 에서 적고** `auxclick(2)` 에서
홀드를 가른다 ⇒ `_rmbAt` 이 안 갱신돼 **모든 우클릭이 홀드로 읽히고 메뉴가 영영 안 뜬다.**
막는 것은 종전대로 `mousedown`·`contextmenu` 가 한다.

★**자가 그걸 못 잡았다** — 조준 단언은 전부 초록이었고(12/0), 메뉴가 죽은 것은 **사건 로그를 눈으로 보고** 알았다.
⇒ `e2e-aim ⓓ-2` 가 이제 **호환 사건이 로그에 있는지**를 직접 못 박는다. **조준만 보는 자는 조준 밖의 결함을 못 본다.**

고친 뒤 사건 순서(종전 순서 그대로 + 캡처 반납만 끼어든다):
`pointerdown(2) mousedown(2) contextmenu(2) pointerup(2) mouseup(2) lostpointercapture(2) auxclick(2)`

⚠**Safari 는 못 쟀다**(헤드리스가 없다). 재민이 "우클릭이 제대로 안 먹는다" 고 한 앞쪽 절반은
헤드리스 Chromium 에서 **재현되지 않았다** ⇒ 회부(재민 실기).

### 하네스

* **`scripts/test-move.js`**(신규 · 38건) — 공유 모듈 **직접**. ⓪검사 상황 선행 assert(서버·클라가
  정말 이 파일을 부르는지 · index.html 이 싣는지) → ④**legacy 비트 동일**(옛 서버 식과 160/160 조합
  `Object.is`) → ①dt 불변(60↔30 0.533px · 60↔10 2.667px < 보정 임계 48px, **등속에선 안 자란다**)
  → ②곡선·관성 → ③배율이 최고속·가속 **둘 다** → ⑤델타 상한 → ⑥NaN 방어.
* **`scripts/e2e-move.js`**(신규 · 37건) — 실클라. **legacy 한 판 · accel 한 판**을 스스로 띄워 견준다.
  속도는 **모델 상태**(`__moveDbg().speed`)로 읽는다 — 화면 위치를 미분하면 렌더 보간을 재게 된다.
* 둘 다 `run-regress.sh` 목록에 넣었다(하네스를 추가하면 **거기 목록에만** 넣는다).

### 하네스가 두 번 먼저 틀렸다 (족보 (73)·(74))

* **(73)** 조준 커서를 화면 우상단(0.80, 0.30)에 놓았더니 `mousedown` 이 캔버스에 **안 왔다** —
  거기엔 `DIV.mini-label`(미니맵)이 덮여 있다. "조준이 안 켜진다"는 **제품의 결함이 아니라
  계측기가 HUD 를 누른 것**이었다. ⇒ `elementFromPoint` 로 **캔버스가 맨 위인 자리**를 확인하고 쓴다.
  ★족보 (73): **자리는 고르지 않고 잰다 — 화면 위에도 지형이 있다.**
* **(74)** "화면 오른아래 = 월드 남동"이라 적었다가 실패했다. 이 아이소 투영에선
  `dwx = 0.5·dpx + dpy · dwy = dpy − 0.5·dpx` 라 오른아래는 **거의 순수 +x** 다(실측 [1.00,−0.06]).
  ⇒ 방향 부호를 눈대중으로 적지 말고 **역변환을 하네스가 손으로 다시 세워** 견준다(+ 반대편 뒤집힘 검사).
* 그리고 남의 빚 하나: `/favicon.ico → 404` 는 **깨끗한 main 에서도 난다**(정적 라우트에 없다).
  계측기가 그걸 자기 실패로 세지 않게 걸렀다.

### ★리베이스 — 병행 인벤 정비 세션이 먼저 밀었다 (`f1659af`)

번들을 만든 뒤 `git fetch` 하니 `origin/main` 이 `4b54b88` → **`f1659af`**(3-p 정비 배치)로 움직여 있었다.
규약대로 **나중 쪽이 리베이스**했다 — 최종 커밋 `d9b0a38`, 베이스 **`f1659af`**.

충돌 4곳, 전부 **양쪽 다 살렸다**:

| 자리 | 갈림 | 결정 |
|---|---|---|
| `zone.js` welcome | 저쪽 `ledger`/`lots`/`uiCfg` vs 내 `moveCfg` | 둘 다 — 셋 다 "표는 서버가 준다" 같은 규약이다 |
| `run-regress.sh` | `test-ledger` vs `test-move` | 둘 다 |
| `다음세션_인계.md` | 둘 다 `## 3-p` | 먼저 민 쪽이 3-p, 내 절은 **3-o** 로 |
| `client.js` `predictStep` | ★아래 | ★아래 |

★**진짜 갈림은 `predictStep` 하나였다.** 저쪽이 접속 안정성으로 `netStalled` 를 `_selfGone` 옆에 세웠는데,
그 자리가 정확히 내가 **관성을 끊는** 자리다. 합친 결론: **예측이 멈추면 관성도 멈춘다.**

```js
  if (myIsDown || _selfGone || netStalled) { myVel.vx = 0; myVel.vy = 0; return; }
```
재개 첫 틱에 서버 `selfVx/selfVy` 로 다시 앵커되므로 안전하고, legacy 에선 `myVel` 이 안 쓰여 무영향이다.
같은 이유로 저쪽 `setNetStalled` 의 "쌓인 미ack 입력을 버린다" 줄에도 **관성 정지 한 짝**을 붙였다 —
거기서 입력만 버리고 속도를 남기면 재접속 순간 몸이 미끄러진다.

### 검증

```
  test-move (신규)            38/0   공유 모듈 직접 — legacy 비트 동일 160/160 · dt 불변 · 곡선 · 배율 · 델타 상한
  e2e-move  (신규)            37/0   실클라 legacy·accel 두 판 — 상승곡선 · 미끄러짐 · 보정 짝 · 조준 전항
  전체 회귀 (러너 37종)      35/37  실패 2 = **둘 다 이 배치 무관 · 베이스 커밋에서 재현**
                             ★리베이스 **뒤** 수치다 — 저쪽 신규 `test-ledger 48/0` · `e2e-inv` 도 내 트리에서 통과
  3시드 800일 기준선          다섯 수 **전부 불변** (아래)
```

**실패 2건은 베이스 커밋(`4b54b88`)을 워크트리로 꺼내 같은 기계에서 재현했다** — 족보 규약대로.

* `e2e-mtfoot` **8/9** — `⑤ ★기슭은 산보다 납작하다 (기슭 vy 중앙값 0.34 < 산 0.45 의 72%)`.
  베이스에서 **수치까지 똑같이** 8/9. 인계 1847줄의 그 항목이고 **산 아크 소관**이다.
* `e2e-waterperf` — `★★수리본이 8배 이상 빠르다` **성능 비율** 판정. 2코어에서 판마다 흔들린다:
  ```
    내 트리 : 6.4배(러너 연속) · 6.8배 · 7.9배 · **9.4배(17/0 통과)**
    베이스  : 8.5배 · 8.8배
  ```
  **내 트리도 단독 재실행에서 17/0 통과**했다(인계 2619줄이 기록한 그 습성).
  분포가 겹치므로 기계 부하 잡음으로 본다 — 다만 "단독이면 늘 통과"가 아니라
  **"흔들리는 폭이 판정선을 물고 있다"**가 더 정확한 서술이다. 그렇게 적어 둔다.

### ★3시드 800일 기준선 — 다섯 수 전부 한 자리도 안 움직였다

```
  시드   인구      소멸    무기Q   게시    못갚아미게시
  1020   6,280 ✓  0/51 ✓  821 ✓  1,647 ✓  11,194 ✓
     7   5,993 ✓  0/51 ✓  755 ✓  1,583 ✓  10,787 ✓
    42   6,072 ✓  0/51 ✓  784 ✓  1,679 ✓  10,489 ✓
```
당연하다 — 이 배치는 **플레이어 전용**이고 헤드리스 랩엔 플레이어가 없다.
그리고 그게 **NPC 이동 무수정의 심판**이다: `followNpcPath`·`sepNpcs`·전쟁 대형에 손을 댔다면
여기가 먼저 움직였을 것이다. econ·specialty 무접촉 ⇒ 번들 재생성 불필요.
(`node scripts/ev-density.js 800 <시드>` · 채택 문턱 C 행)

### 실기 확인 3줄 (재민)

```
  1) 플래그 켜기 — zone 서버를 MOVE_MODEL=accel 로 띄운다
       MOVE_MODEL=accel node server/zone.js     (기본은 legacy — 안 켜면 종전 그대로다)
  2) 관성 느끼기 — 탁 트인 땅에서 D 를 길게 누르고 **탁 놓는다**. 반 발짝(약 6px) 미끄러진다.
       그리고 달리다(Shift) 반대 키를 눌러 봐라 — 0.4초 동안 몸이 안 뒤집힌다.
  3) 조준 — **우클릭 누른 채** 마우스를 돌리며 둘러보고(카메라가 커서 쪽으로 밀린다),
       그 상태로 WASD 로 옆걸음·뒷걸음, **좌클릭으로 친다**. 놓으면 전부 제자리로 돌아온다.
```
★손잡이는 전부 env 다: `MOVE_ACCEL_T` · `MOVE_DECEL_T` · `MOVE_AIM_SPEED_FRAC`.
★**튜닝하지 않았다** — 실기 기록지를 기다린다(B-1 "무게가 시간으로는 안 느껴진다"가 첫 질문이다).

### 회부 (구현 금지)

`회부_이동조준_다음층.md` — A(지시서 이월): 전투의 살 · 각가속도/연속 페이싱 · 조준 시야 확장 ·
스태미나 · **기본값 전환** · NPC 이동. B(새로 드러난 것): **`MOVE_ACCEL_T` 의 "최고속"이 상태마다
다르다(무게가 시간으로는 안 느껴진다)** · 달리기 스풀업 부재 · 감속 시간 스텝 양자화 ·
조준 대상 표시 부재 · **우클릭이 건축 회전과 겹친다** · 커서→월드 투영 어긋남(이번에 고쳤다·사후보고) ·
**벽 밀면 속도가 쌓인다** · 관전 카메라와 겹침.

---

### ★회귀-수리 기록 (2026-08-31 핫픽스) — `e2e-emptystart` ⑤ 는 **닫힌 경로가 아니라 계측 결함**이었다

> ⚠**앞 세션의 귀속이 틀렸다.** 캐릭시트 세션이 "`f7f9c2f`(접속 진단)가 넣은 회귀"라고 적었는데,
> 그건 **단판 이분법**이었다. 간헐 실패에 단판 비교는 아무것도 증명하지 못한다.

**완성된 이분법** — `f1659af` 18/18 · **`ed1efa7` 18/18**(앞 세션이 **건너뛴** 커밋 · 이번에 채웠다) ·
`f7f9c2f` 16/18. 그런데 **HEAD 에서 단독 10판(2코어 부하 재현 포함) 전부 18/18 통과**했다.
⇒ 커밋 귀속은 성립하지 않는다. 실패는 **타이밍 의존**이다.

**진짜 사실 — 탐침이 잡았다.** 실패 판에서 서버는 텔레포트를 **7번 다 받아들였다**
(클라 공지 `"🌀 텔레포트 → (14032,78032)"` ×7 이 증거). 그런데 하네스가 도착을 판정한
`__getMyAbs()` 는 **클라 예측**이라 **3만px 밖**에 머물러 있었다 — 재접속·orphan 복구 직후
예측이 권위와 갈리기 때문이다. 그 상태로 ⑤가 진행되면 "마을 밖에서 거래"가 되고,
실패는 **"장인에게 못 산다"라는 하류 증상**으로만 찍힌다. 진짜 사실은 **"마을에 못 갔다"**였다.
(구매 코드 경로 자체는 멀쩡하다 — 독립 탐침에서 stone 6→3 · 장비 0→1 · 공지 "농촌1 장인 구매".)

**수리 — 계측기 쪽이다(증상 덮기 아님).**

| | |
|---|---|
| `public/client.js` | **`window.__getSrvAbs()`** 신설 — 서버 권위 위치(읽기 전용). `null` = "아직 내 몸의 틱을 못 받았다"(0,0 자명 통과 금지). `__evDbg` 가 이미 쓰던 `me` vs `srv` 짝을 훅으로 뺀 것. |
| `scripts/e2e-emptystart.js` | `warp()` 이 **권위로 판정**하고, 실패하면 `null` 을 돌려주며 **권위·예측을 나란히** 찍는다. ⑤에 **도착 판정을 세웠다** — 못 갔으면 그 사실이 실패로 찍힌다(18 → **19** 판정). |
| 같은 족 일괄 수색(㊲) | `e2e-move` · `e2e-charsprite`(2곳) · `e2e-craft` · `e2e-forage-village` 의 워프도 같은 결함이었다 — 전부 권위 기준으로. (`walkTo` 는 **걸음 실측**이라 예측이 맞다 — 안 건드렸다.) |

★족보 ㊹ 재적용: **"me(예측) vs srv(권위)를 둘 다 찍어라"** 는 이미 `__evDbg` 주석에 적혀 있었는데
워프 판정은 그걸 안 지키고 있었다. **규약이 문서에만 있고 계측기에 없으면 없는 것이다.**

**검증**

```
  e2e-emptystart   19/19 × 3판 단독  (권위 424016,128016 — 목표에 정확히 도착)
                   19/19 러너 안     ← ★실패하던 바로 그 조건(연속 실행)에서 통과
  test-ledger      48/0   · e2e-inv 57/0   ← 인벤정비 배치 본론 무손상
  러너 40종        37/40  실패 3 = test-guest-rejoin · e2e-mtfoot · e2e-waterperf
                   ↳ 단독 재실행: guest-rejoin **26/0** · waterperf **17/0**(8.2배) — 인계에 기록된 그 습성
                   ↳ e2e-mtfoot 8/9 는 산 아크 소관(1847줄)
  3시드 800일      다섯 수 전부 불변 (6280/5993/6072 · 0/51 · 821/755/784 ·
                                     1647/1583/1679 · 11194/10787/10489)
```

---


---

# T356 — 틱 해부 · 그리고 **지형 = 셀당 막힘 비트** (2026-09-23 · 세션3)

## 이 층을 건드리기 전에 읽을 표 하나 — **사람당 값의 주인**

실지도 51마을 1,464명 · **한 판 안에서 갈래마다 시계**(빼기 0 · `scripts/t356-tick-anatomy.js`):

| 갈래 | 낮 µs/사람 | 낮 % | 밤 µs/사람 | 밤 % |
|---|---:|---:|---:|---:|
| **A\*** `computeNpcPath` | **1.59** | **19.3** | 0.02 | 0.6 |
| **이동 문** `movePlayerStep` | **1.56** | **19.0** | **1.11** | **28.3** |
| 따라가기 `npcStep` 나머지 | 1.16 | 14.1 | 0.44 | 11.2 |
| 순회 껍데기(두 루프) | 0.71 | 8.6 | 0.37 | 9.5 |
| 생활층 `npcLifeTick` | 0.50 | 6.1 | 0.04 | 0.9 |
| 델타·방송 | 0.17 | 2.1 | 0.15 | 3.9 |
| 결정 `decideNpcBehavior` | 0.12 | 1.5 | 0.01 | 0.2 |
| 그 밖(econ·몹·게이지·계단·청크) | 2.11 | 25.7 | 1.46 | 37.2 |
| **합** | **8.22** | | **3.93** | |

★**낮과 밤은 주인이 다르다.** 낮엔 A\* 와 이동 문이 반반, 밤엔 이동 문 하나. A\* 는 **틱마다 899명**이 다시 묻는다
(밤엔 1,354명이 묻지만 전부 `d<48` 조기 반환이라 0.02 µs). **2초 쿨다운이 낮에 안 문다** — 회부 1.

★**결정은 1.5 % 다**(T350 씨 해시 뒤). 이동 층을 고칠 때 결정을 같이 옮길 이유가 없다.

## 손잡이 `T356_SOA`(기본 끔) — 지형 막힘 비트

* **무엇**: 셀당 두 비트(계산됨·값)에 `rock | ditch | (water && !bridge)` 를 굽는다. `2,188×4,063 → 2.1MB`.
* **정본은 술어 넷이다** — 비트는 유도다(T333 규율). `_terrBlocked0` 이 그 넷을 그대로 부른다(정의 **하나**).
* **가드는 술어 넷과 같은 자리**(px 범위·해양). ⚠`tx` 로 가드하면 **존 오른쪽 끝**이 갈린다
  (`zoneWidth` 가 32의 배수가 아니면 tx 범위가 px 범위보다 넓다) — 하네스 ⑦-a 가 그 띠를 표본에 넣는다.
* **무효화**: 런타임에 바뀌는 원천은 환호 하나 ⇒ `refreshDitchCells` 가 통째로 영점. 물·바위·다리는 기동 뒤 무변.
* **게이트**: `test-move-soa ⑦` — 전수 40만 0 다름 · **주민 408 × 4,200틱 좌표 비트 동일** · 자명 통과 금지 둘.
* **실측**: `isWaterTileLocal` 호출 걸음당 **6.02 → 0.0155(388배)** · 프로파일 `_terrBlocked0` **1.743s → 0.000s**.
* ⚠**틱 값은 못 쟀다** — 켬/끔 짝이 두 번 다 같은 국면에 안 떨어졌다(창 안 흐름 ±15 % > 효과 −4 %).
  유도값 ≈ **3 %**. 되풀이 질의에서만 번다(처음 밟는 셀은 어차피 술어 넷을 부른다 — T333 미리 굽기가 답한 그 자리).

## 자를 쓸 때의 규율 둘 (이 카드가 값을 치르고 배운 것)

1. **작고 뜨거운 술어를 감싸면 인라인이 풀린다.** `isTerrainBlockedLocal`·`isBlockedByWall`·`treeBlockerAt` 을
   래퍼로 감싸 재어 보니 틱이 **12.03 → 14.31 ms** 로 뛰고 지형이 **5.14 µs**(이동 문 전체 1.56 보다 크다)로 나왔다.
   ⇒ 술어 단위 시계는 **못 쓴다**. 갈래는 루프·함수 경계에서 재고, 술어는 **호출 수**(`_walk.terrQ` 등)로 본다.
2. **`process.hrtime.bigint()` 로 걸음 문을 재지 마라.** 호출마다 BigInt 를 **할당**한다 — 밤 틱이 3.7 → **9.5 ms**.
   `performance.now()`(double · 할당 0)를 쓰고, `2 × clockNs × 호출수` 를 **계수로** 뺀다(실측 `clockNs = 41.7 ns`).

---

# T370 — A\* 를 **왜** 다시 묻나 · 그리고 값은 거기 없었다 (2026-09-23 · 세션3)

## `needPath` 네 조건 — 낮 창 실측(7,200틱 · 인구 1,475 · 비취침 1,123)

| 첫 참 이유 | /틱 | 몫 |
|---|---:|---:|
| **`pathIndex >= path.length`(경로 끝)** | **723.2** | **98.5 %** |
| `_pathFor !== targetKey` | 8.9 | 1.2 % |
| `now - _pathAt > 5000` | 1.8 | 0.2 % |
| `!npc.path` | 0.5 | 0.1 % |

그때 그 주민: 목표까지 **12px** · 행동 **100 % `wander`** · **72.9 %가 10틱 넘게 연속**.
⇒ **도착한 배회 주민이 다음 결정(0.5~1.5초)까지 매 틱 길을 다시 묻는다.** 뿌리는 **`wander` 에 도착 행동이 없다**는 것.

## ★그런데 값은 거기 없다 — 이 표를 먼저 보라

| | ms/틱 | 몫 | 호출/틱 | 한 번 |
|---|---:|---:|---:|---:|
| `computeNpcPath` 전부 | 2.024 | 100 % | 734.3 | — |
| ↳ **`pfFindPath`** | **1.840** | **90.9 %** | **1.2** | **1.55 ms** |
| ↳ `straightPathClear` | 0.051 | 2.5 % | 12.7 | 4.0 µs |
| ↳ 나머지(`d<48` 되묻기) | 0.133 | 6.6 % | 723.6 | 0.18 µs |

**899 를 없애도 6.6 % 다. 90.9 %는 틱마다 1.2명의 A\* 한 번(1.55 ms)이다**
(`maxCells: 1500` · `searchRadiusCells: 64` — 주민용으로 T324 가 올린 수 · **이 카드는 안 건드렸다**).

## 손잡이 `T370_PATH_REUSE`(기본 끔)

* **무엇**: 네 조건 중 **'경로 끝' 하나만** 참이면 다시 묻지 않는다. 목표가 바뀌면 묻고, 제자리에서 막혀 있으면
  **있던 5000ms 만료**가 깨운다(새 수 0 · `path-core.js`·`pathfind.js` 무접촉 — 고친 건 **부르는 조건**).
* **끔 = 비트 동일**: `_skip` 의 **첫 항이 손잡이**라 끄면 단락. `test-move-soa ⑧` 이 진리표 **16칸** 0 다름 + 미끼.
* **켬 실측**: 묻는 수 **734.3 → 20.7/틱(÷35.5)** · `_skip` 874.8/틱 · 연속 >10틱 **72.9 % → 0** ·
  그 갈래 **0.133 → 0.029 ms/틱**(사람당 **−0.07 µs** ≈ 0.9 %).
* **반례 0**: 튕겨냄 깨어 있는 사람당 4.27e-4 → **3.95e-4**(−7.5 %). 200일 마을 **50/50 · 소멸 0**.
  인구 2,723 → 2,606(−4.3 %)인데 ⚠**T326 끈 팔 둘이 2,953 ↔ 2,676(9.4 %)** 벌어졌으니 **판 편차 안**이다 — 못 가른다.
* **켜지 않았다.** 켤지는 판 여럿 뒤 재민 몫.

## 자 규율 하나 더 (T356 의 둘에 잇는다)

3. **`git worktree add HEAD` 는 커밋된 것만 떠 온다.** 손잡이를 아직 커밋 안 했는데 켠 팔을 돌려 **조각 여덟을 버렸다**.
   ⇒ 계측기는 `git diff --name-only HEAD` 로 **바뀐 파일을 사본에 덮어쓰고**, 무엇을 덮었는지 한 줄 찍는다.
   **자가 재는 것이 지금 고치고 있는 그 글자여야 한다.**

---

# T381 (2026-09-25) — `pfFindPath` 한 번의 안 · 손잡이 `T381_PATH_DEAD`(기본 끔)

## 1.55ms 의 주인 — 이 표를 먼저 보라(끔 · 낮 창 7,200틱 · 비취침 1,119)

| 나간 문 | 호출 몫 | **시간 몫** | 한 번 |
|---|---:|---:|---:|
| **`maxCells` 1500 을 다 태움 → `null`** | 37.3 % | **92.9 %** | **6.42 ms** |
| 찾음 | 36.3 % | 6.8 % | 0.485 ms |
| 힙 빔(1칸) → `null` | 22.6 % | 0.3 % | 0.034 ms |
| `searchRadiusCells` 64 넘음 → `null` | 3.8 % | 0.0 % | 0.002 ms |

* "한 번 1.55 ms"(T370)는 **1500칸 호출과 찾은 호출의 평균**이다 — 판마다 1.55~2.98 로 흔들리지만 주인(1500칸 · 시간의 90~93 %)은 안 흔들린다.
* **목표 셀이 통행 불가인 호출 50~53 % = 시간의 54 %.** 강 **건너**(다른 뭍 덩어리)는 **0 건** — 목표가 강 **속**이다.
* **여행 열에 하나는 막힌 칸이 목표다**(10.8~11.5 %) — 그 여행은 거의 안 끝난다(0.4 %).

## ★`localPath` 는 끝점 막힘을 안 본다 — 끝점 순서가 답을 가른다

커널이 왕복 대칭을 위해 끝점을 **사전순으로 뒤집으면**(`_searchBegin` 의 `rev`) 막힌 목표가 **출발**이 되고,
출발 칸은 아무도 검사하지 않으므로 **물에서 끝나는 길**이 나온다. 실측: 찾은 길의 **17.6 %가 막힌 칸에서 끝난다**.
격자: 정방향 688/688 `null` · 뒤집힘 623 중 **365(58.6 %)에 길**(`test-move-soa ⑩-b` 가 건다).
`routePath` 는 첫 줄 `if (blocked(sx,sy) || blocked(gx,gy)) return null;` 로 이미 막는다 — **걸음 쪽에만 그 한 줄이 없다**(정본 수리는 회부).

## 손잡이 `T381_PATH_DEAD`(기본 끔)

* **무엇**: `computeNpcPath` 에서 `_lastAStarAt = now` 다음, `pfFindPath` 바로 앞 — **목표 셀 중심이 `isTerrainBlockedLocal` 이면 `null`**.
  술어는 A\* 에 `isWaterFn` 으로 주입되는 **그 함수**(다리 칸은 이미 빠진다) · 셀 중심 식은 `findPath` 의 `cpx` · 같은 셀은 뺀다. 새 수 0.
* **끔 = 단락**(⑩-a · 0회). **켬 ≠ 비트 동일** — 뒤집힘 판의 "물에서 끝나는 길"이 `null`(→ beeline)이 된다. 그 자리 **하나만** 바뀐다(⑩-d 봉쇄: 열린 목표 전수 비트 동일).
* **켬 실측**(같은 벽시계 두 팔 · 두 판): 호출 −52~53 % · A\* 시간 −48~52 %(사람당 1.53~1.66 → 0.80 µs) · **존 틱 p50 −1.27 / −1.78 ms(조각 8/8 × 두 판 · T252 가름)**.
* **반례**: 열린 목표 여행 도착률 21.4 → 21.7 % · 11.5 → 11.6 % · 도착 시간 중앙 **같음**. 튕겨냄 못 가름. beeline 은 는다(예상한 자리).
* **켜지 않았다.**

## 자 규율 셋 더 (T356 의 둘 · T370 의 하나에 잇는다)

4. **몸은 `players` 에 있다 — `npcs` 는 pid 의 Set 이다.** `npcs.values()` 는 문자열을 내준다. 자가 두 판 내리 0 을 냈다.
   ⇒ **0 이 나오면 자부터 의심한다** — 모의 세계(`npcs`·`players` 모의)에 넣어 표본이 실제로 차는지 먼저 본다.
5. **탐침 머리는 백틱 템플릿이다** — 주석에 백틱을 쓰면 문자열이 끊긴다(T356 에 한 번 · T381 에 두 번). 박기 전에 머리 안 백틱을 센다(0).
6. **200일 병렬 두 팔은 손잡이가 아니라 "자리"를 잰다.** `t212-beds` 두 판을 같이 띄우면 **둘째 자리가 늘 낮다** — 끔/끔 대조가
   2,723 ↔ **2,558(−6.1 %)**. 첫 자리는 세 판 내리 **2,723 · 2,724 · 2,723**(±1 · T350 주사위 0 이후 결정적). ⇒ 200일 반례는
   **첫 자리 하나로 순차**로 잰다. T370 의 "−4.3 %(못 가름)"도 이 자리였다.

---

# T394 (2026-09-26) — 몸 비비기의 뿌리 · 손잡이 `T394_WORK_TERRAIN`(기본 켬) · `T381_PATH_DEAD` 기본 켬 · `localPath` 끝점

## 뿌리 둘을 뽑았다 — 이 표를 먼저 보라(카드 전 ↔ 뒤 · 같은 벽시계 두 팔 · 낮 창 7,200틱)

| | 전 | 뒤 |
|---|---:|---:|
| 물·바위 위 일터(주민 전수) | 162 / 1,475 | **0** |
| 막힌 칸이 목표인 여행 | 10.0 % | **0.7 %** |
| 물가 정지(5초에 32px 도 못 간 · 먼 목표 · 물가 1셀) | 36.0명 | **19.9명**(8/8 가름) |
| 튕겨냄 · beeline /틱 | 0.524 · 1.611 | 0.328 · 0.533 |
| A\* ms/틱(사람당) | 3.324(2.25 µs) | 1.513(**1.03 µs**) |
| 열린 목표 도착률 · 도착 시간 중앙 | 20.7 % · 1.625 s | 20.8 % · 1.375 s |

* **① 태어나기**(`villages.js` `_t394WorkSite`): 일터 도넛 후보를 **A\* 의 그 술어**(`deps.isTerrainBlockedLocal` · 셀 중심)로 거르고, 막히면 **같은 씨 흐름**으로
  다시 뽑는다(상한 63 = 도넛 바깥 둘레 셀 수) · 다 막히면 남문 앞 마당 `(ccx, ccy+6)`. 몸 자리 굴림 순서는 그대로 — 첫 점 열린 몸은 비트 동일.
* **② ⑤ 배회**(`zone.js` `_t394OpenTarget`): 목표 셀이 막혔으면 **일터 쪽으로 32px 씩 되짚어** 첫 열린 점. 새 탐색 0 · 주사위 0 · 열린 목표 무변.
* **③ `localPath` 첫 줄 = `routePath` 첫 줄**(`opts.blocked` 노드 술어 · 안 주면 종전). `pathfind.js` 가 노드 판정을 한 번 적어 간선·끝점이 같이 쓴다.
  열린 끝점 40쌍 비트 동일 · 사전순 앞 막힌 목표에 나던 길 33 → 0. ⚠출발 칸도 본다 — 몸이 막힌 칸 **안**이면 `null`(실측 0 · 지켜볼 자리).
* **④ `T381_PATH_DEAD` 기본 켬**(되돌림 `=0`). ③ 뒤로는 켬/끔이 **같은 답**이다 — 켬은 정본이 되돌려져도 값을 지키는 이중 자물쇠.
* **남은 것**: 막힌 목표 548 의 94 %는 **튕겨냄 무작위 도약**(±80px) · 남은 물가 정지 19.9명의 주인은 **1500 × 뭍**(T381 회부 3 · 다음 카드).

## 자 규율 하나 더

8. **하네스가 흔들리면 "내 갈래가 거기서 섰나"부터 센다.** `test-war-world` ⓓⓛ 가 두 판 붉었을 때 main 과만 견주지 않고 임시 탐침으로
   ①이 그 하네스에서 선 횟수를 셌다 — **0**. 흔들림의 원천은 `spawnOneNpc` 씨의 벽시계(`gameDayOf(Date.now())` · 하네스 30초 날)였다.
