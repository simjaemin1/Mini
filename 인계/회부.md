# 인계 — 회부 — 전 배치의 "구현 금지" 누적

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.
## 0-재생. [T122 나무 재생 2026-09-05] 다른 영역에 넘기는 것 여섯

> 전문 `보고/T122_2026-09-05.md`. 만진 것: `server/chunk.js`(재생 절) · `server/zone-local-db.js`(열 하나) ·
> `server/zone.js`(Map·문 하나·승격) · `server/itemlabel.js`(동사 하나) · `public/client/*`(두 갈래·동사 게이트) ·
> `scripts/test-regrow.js`(신설 58/0). **`villages.js` 무접촉 — 3시드 800일 A/B 세 시드 diff 0.**

1. ★★★**[재민 판정] 값 재정박 — 성목까지 316 실일은 아무도 못 본다.**
   출처값 그대로 세운 첫 판이다(소나무 22/52 게임년 · Lee et al. 2018). 실시간으로는
   그루터기→묘목 **4.4개월**, →성목 **10.4개월**. 임학이 말하는 크기가 그렇다는 것이 §0 의 답이고,
   **게임이 그 크기를 써야 하는가는 다른 물음**이다. 갈 것은 `T122_STUMP_Y`·`T122_TREE_Y` 둘뿐이다.
   ⚠짧게 하려면 **근거가 먼저**다 — "안 보이니까"는 근거가 아니라 요구다(그 요구가 정당할 수 있다.
   그러면 그건 고증이 아니라 **게임 설계 판정**이라고 적고 그렇게 정하면 된다).
2. ★**[영역 C · T123 랩] 종 카탈로그·열매** — 지금 종은 하나(`tree`)다. T129 가 그림은 여덟 종을
   구웠고 `tree_species.json` 도 있다. 카탈로그가 오면 `regrowStageOf` 에 종 인자 하나면 된다.
3. ★**[영역 C] 참나무는 그루터기에서 맹아로 다시 난다**(coppice) — 소나무는 안 그런다.
   지금은 종이 하나라 소나무 값 하나를 쓴다. 종이 갈리는 날 **그루터기 기간이 종별로 갈릴 자리**가 여기다.
4. **[영역 C · T124] 심기** — 플레이어가 묘목을 심는 길은 이 카드에 없다.
5. **[영역 Z] 하루 틱 승격** — 지금은 청크 활성화 때 한 번만 정산한다. 값이 짧아지면(1번) 한 줄이 필요하다.
6. **바위 재생 없음 — 맞다. 닫는다.**(광맥은 이미 제 재생이 있다 · 운철은 발견물이라 안 난다.)

★**한 줄 더 — 부팅 최상위에서 게임일을 묻지 마라**: `gameDayNow()`/`zoneGameDay()` 가 읽는
`_e2eClock`·`_e2eDayFrozen` 은 파일 아래쪽 `let` 이라 모듈 최상위에서는 **TDZ** 다
(`test-mining` 이 부팅을 통째로 터뜨려 잡았다). 그리고 그 시점엔 econ 시계도 아직 안 서 있다 —
**"부팅 때 오늘을 적는다"는 코드는 대개 틀린 날을 적는다.** 첫 사용 시점으로 미루는 게 맞다.

## 0-문간. [T121 꺼진 청크의 문간 2026-09-05] 다른 영역에 넘기는 것 넷

> 전문 `보고/T121_2026-09-05.md`. 만진 것: `server/zone.js`(문간 절 — 판정 함수 하나 + DB 갈래) ·
> `scripts/test-downed.js`(⑳ 신설 · 134 → 163). **T83 회부 3(꺼진 청크 문간 DB)을 닫는다.**

1. ★★**[영역 Z/B] `_BLOCKED_STAND` 는 벽을 안 본다.** 지형·나무만 본다(`isTerrainBlockedLocal || isBlockedByTree`).
   그래서 사다리 ②(`near`)와 ③(`center`)는 **움집 안을 "설 수 있는 자리"로 친다**. 이 카드는 ①(문간)을
   되살려 그 자리에 안 가게 했지만, 움집이 없는 사유지에선 여전히 벽 안에서 깰 수 있다
   (벽은 `buildings`/`qtBuildings` 소관이라 같은 "꺼진 청크" 문제를 또 만난다 — `_hutDoorRows` 와
   같은 갈래를 쓰면 된다). **작은 카드 하나짜리**다.
2. ★**[영역 V/W] 마을 광장을 "늘 트인 곳"으로 가정한 절이 있으면 틀렸다** — T114 §0 그대로 살아 있다.
3. **[영역 L · 부패] 젖은 짐** · **[영역 B · 옷] 재질별 젖음 열(출처 먼저)** — T105/T114 회부 그대로.
4. ★**[영역 B/K] 여름 그늘(T64)** — 더위 축 · **재민 판정 뒤** · 계수도 다른 수다(T114 회부 1 그대로).

★**한 줄 더 — 값 재정박(실기 뒤)**: 문간 탐색 반경 `BUILDING_SIZE*6`(=192px)은 T83 이 정한 수다.
사유지가 그보다 넓으면(`rpx` 가 큰 길드 사유지) `rpx + BUILDING_SIZE*4` 가 이기지만, 그 둘 다
**잰 값이 아니라 고른 값**이다. 실기에서 "내 집이 있는데 왜 밖에서 깼나"가 나오면 여기다.

## 0-친구. [T115 친구·함께 도착 2026-09-05] 다른 영역에 넘기는 것 일곱

> 전문 `보고/T115_2026-09-05.md`. 만진 것: `central.js`(표 하나·라우트 셋) · `central-client.js`(문 셋) ·
> `friends.js`(신설) · `zone.js`(채팅 분기·`fr` 1비트·`/friendsdbg`) · `onboarding.js`(`friendsHere`·§4 한 줄) ·
> 클라 셋(로비 표지·이름표 낱말·`fr` 승계). 하네스 `e2e-friends` 신설 39/0.

1. ★★**[상류 · 온보딩] 등록 계정은 `start_vid` 가 영영 null 이었다 — 이 카드가 한 줄로 막았다.**
   central `/auth` 가 **신규 가입 그 순간** `home_zone`·`last_zone` 을 박는데 `arriveFor` 는 그걸 보고
   **이어하기**로 분류해 `return null` 하고, 그 한 줄이 **자리 배치와 기록을 같이** 건너뛴다.
   ⇒ 생애 첫 접속이 이미 이어하기였다. T115 는 **자리는 손대지 않고 기록만** 살렸다(한 번만 적는다).
   ⚠남은 물음은 **자리**다: 등록 계정의 첫 접속은 고른 마을 어귀로 가야 하는가, 지금처럼 광장인가.
   그건 온보딩 캐논 판정이라 회부(§9.1 "선택은 문이지 벽이 아니다"와 같이 봐야 한다).
2. ★★**[영역 S · 다음 칸] 따라가기** — 실시간 위치. AOI 규약과 같이 봐야 한다(A-1 의 셋째).
3. ★**[영역 S/N] 요청 알림함** — 지금은 **접속 중일 때만** 듣는다. 오프라인 요청은 `/친구` 를 쳐야 안다.
   수락 전용 명령을 안 만든 것도 같은 이유다(알림 층이 딸려 온다).
4. ★**[영역 S · §8] 친구 목록 패널** — 이 카드는 새 패널 0 이라 채팅으로만 본다. 패널은 캐논 판정.
5. ★**[영역 N · 신원] 게스트끼리는 친구가 안 된다** — `/guest` 가 주는 이름이 **전부 `여행자`** 라
   이름으로 지목할 수 없다(실측). 게스트는 승계해 이름을 가진 뒤부터 친구가 된다.
6. **[영역 N] 존을 넘는 `start_vid`** — 지금은 존의 표에 있어 다른 존에서 시작한 벗은 안 보인다.
7. ★**[영역 N · 사생활] 시작 화면의 인증.** `/startinfo?as=<이름>` 은 "그 이름의 사람에게 벗이 있는가"까지는
   드러낸다(수만 · 이름은 안 준다). 게스트 토큰은 열쇠라 못 싣는다 ⇒ 제대로 닫으려면 시작 화면이
   **인증된 뒤에** 물어야 하고 그건 로비의 순서를 바꾸는 일이다.
## 0-나무. [T114 나무 아래 가림 2026-09-05] 다른 영역에 넘기는 것 다섯

> 전문 `보고/T114_2026-09-05.md`. 만진 것: `server/wind.js`(접근자 하나) · `server/body.js`(젖음 상승 한 줄 + 수 하나) ·
> `server/zone.js`(`coverOf` · `weatherFor` 한 필드 · `Body.tick` 맥락 한 줄) ·
> `public/client/44-h-hud.js`(낱말 하나) · `scripts/test-body.js`(⑳ 신설 · 275 → 306).
> **T105 회부 3 을 닫는다**(나무 아래 = 비 가림). T64 여름 그늘은 **안 열었다** — 아래 1.

1. ★★**[영역 B/K · T64] 여름 그늘 — 더위 축은 이 카드가 한 줄도 안 건드렸다.**
   `Wind.coverAt` 이 이제 있으니 배선은 한 줄이면 되지만, **더위 축의 기준선을 옮기는 판단**이라
   재민 판정이 먼저다. 계수도 다른 수다 — 강수 차단(0.22)과 복사 차단은 같은 물리가 아니다
   (수관 아래 단파 복사는 잎면적지수의 지수 감쇠라 훨씬 크다). 출처를 새로 대야 한다.
2. ★**[영역 L · 부패] 젖은 짐** — T105 회부 2 그대로 살아 있다. 짐은 여전히 안 젖는다.
3. **[영역 B · 옷] 재질별 손실 열** — T105 회부 1 그대로. 출처가 먼저다.
4. **[영역 V/W] 꺼진 청크 문간 DB** — T83 회부 그대로(`qtBuildings` 는 활성 청크만 색인한다).
5. ★**[영역 B/K] 값 재정박 둘 — 실기 뒤.**
   ① `COVER_MAX` 0.22 는 **임분 평균**이다. 실기에서 "숲에 들어가도 티가 안 난다"면 올릴 수 있는
   출처가 있다(Fischer 2023 수관 한복판 점 측정 36.5~95.9%) — 다만 그건 **다른 물음의 답**이라
   "플레이어가 나무 밑에 붙어 선다"를 모델이 표현하게 된 뒤에나 쓸 수 있다.
   ② 젖음 낱말 문턱 0.01 · 차폐 낱말 문턱 0.15 — 마을 낱말과 같은 눈금을 빌린 것이지 잰 값이 아니다.

★**한 줄 더 — §0 이 잡은 세계의 사실**: 51마을 중심의 `fsh` 가 **0 인 곳은 41/51** 이다.
임업 마을 여섯은 `fsh` 0.6~1.0 — 절차적 숲이 마을을 비껴가지 않아 **말 그대로 숲 안에 있다**.
차폐는 "마을이냐 야생이냐"가 아니라 **셀의 성질**이고 그게 옳지만, 마을 광장을 "늘 트인 곳"으로 가정한
다른 절이 있다면 그 가정은 **틀렸다**(영역 V/W 가 볼 자리).

★**한 줄 더 — 기동 시각에 흔들리는 하네스 둘(내 것 아님 · 순수 `origin/main` 에서 재현)**:

1. **조석 위상** — `test-body ⑯㉧` 과 `e2e-cold ⑦㉣` 이 **같은 증상**으로 빨강이 된다:
   `"갯벌(한사리 썰물(갯벌이 드러났다))에서 해조 1 (남은 양 3.0)"`. 자리를 찾는 시점과 마시는 시점 사이에
   조석 위상이 넘어가는 것으로 읽힌다. 베이스 워크트리에서 `test-body` 1회 275/2 → 2회 275/0,
   `e2e-cold` 3판 중 2판. **영역 C/B 가 볼 자리** — 조석 위상을 한 번 얼려서 묻거나,
   자리 찾기와 판정을 같은 위상 안에서 끝내면 된다.
2. **`__e2e_clock` 기동 경합** — `e2e-cold ①` 이 입장 직후 보내는 시계 세움이 **안 걸리는 판**이 있다.
   걸린 판 `wind: 1 · tempC −2.6`, 안 걸린 판 `night: false · tempC 12.6 · wind 0.652`(순수 베이스에서도
   같은 값). 뒤 절들은 같은 판에서도 통과하므로 **첫 전송 한 번의 경합**이다. **영역 Z/N 이 볼 자리** —
   `__e2e_clock` 에 서버 확인(notice)을 기다리는 왕복을 두면 1800ms 짐작이 필요 없어진다.

## 0-젖음. [T105 젖음 2026-09-05] 다른 영역에 넘기는 것 다섯

> 전문 `보고/T105_2026-09-05.md`. 만진 것: `server/body.js`(젖음 절) · `server/zone.js weatherFor`(한 함수) ·
> `public/client/44-h-hud.js`(낱말 하나) · `scripts/test-body.js`(⑲ 신설 · 232 → 275).
> **T98 회부 3(젖음·추위)을 닫는다.** 앵커 둘 다 TB MED 508(US Army) 한 문서에서 나왔다.

1. ★★**[영역 B · 옷] 재질별 단열 손실 열이 없다.** 지금은 **재질 무차별 0.5** 다 — 출처(TB MED 508)에
   BDU 한 벌뿐이고 재질 표가 없어서다. "갖옷은 덜 잃고 삼베는 많이 잃는다"는 맞는 말일 수 있지만
   근거가 없으면 고증이 아니라 소원이다. 재질 열을 넣으려면 **그 표의 출처가 먼저**다
   (`clothes.js` 재질 표에 열 하나 · 이 카드는 그 파일을 안 건드렸다).
2. ★**[영역 L · 부패] 젖은 짐.** 가죽·곡물이 젖으면 상하는 속도가 달라진다. 지금 젖음은 **몸에만** 붙어 있다.
3. ★**[영역 K/B] 나무 아래는 비를 가리는가.** 지금 비를 막는 술어는 `indoor`(지붕) 하나다.
   `wind.js` 의 숲 차폐 `fsh`(0..1)가 정본인데 꺼내려면 접근자 한 줄이 필요하다 —
   **T64 가 여름 그늘에서 남긴 회부와 정확히 같은 자리**라 한 카드로 묶는 게 맞다.
4. **[영역 C · T99 뒤] 밭 물대기 대체** — 비 온 날은 물을 덜 준다. `crops.js` 무접촉.
5. **[영역 K] T98 에서 넘어온 것 그대로** — 지속 앵커(장마 며칠) · 강도 mm 앵커 · 시간대 창(소나기) ·
   눈 쌓임(R1) · 구름 그림자·번개(R1).

★**한 줄 더**: T98 회부 8(픽셀 하네스 목록 규약 · 세션6 T104)이 **우연히 한 겹 메워졌다** —
T98 이 열 개 하네스에 넣은 하늘 끄기 주석이 "weatherFor" 라는 낱말을 담고 있어, 이제
`grep -l weatherFor scripts/*.js` 가 **그 열 개를 자동으로 잡는다**. 우연이지 규약은 아니다.

## 0-강수. [T98 강수 정본 2026-09-05] 다른 영역에 넘기는 것 여덟

> 전문 `보고/T98_2026-09-05.md`. 만진 것: `server/weather.js`(강수 절 신설) ·
> `server/zone.js weatherFor`(한 줄) · `scripts/test-weather.js`(신설 42건). econ 무수정 · 플레이 영향 0.
> **T93 의 "강수 정본이 없다" 회부를 닫는다**(아래 `0-날씨` 절 첫 줄).

1. ★★**[영역 K] 강도에 앵커가 없다.** 지금 강도는 `p − u`(문턱을 넘은 몫)라 장마철이 저절로 세지만
   **비 온 날 평균이 7월/1월 2.0배**다. 같은 기상청 표의 강수량은 **7월 20.0mm/일 vs 1월 3.6mm/일 = 5.6배**.
   그 mm 열둘을 들이면 앵커가 열둘 더 는다 ⇒ 카드 하나. (열둘은 보고 §0-ⓑ 표에 이미 적어 뒀다.)
2. ★★**[영역 K] 비가 이어지지 않는다.** 채택한 생성기는 **날마다 독립**이라 오늘 왔다고 내일이
   더 올 것 같지 않다. 실제 비는 며칠씩 이어진다. **앵커 열둘에 "얼마나 이어지는가"가 없다** —
   넣으려면 그 수의 출처(예: 평년 최대 연속 강수일수)가 먼저다. ⚠매끄러운 잡음(`_fbm`)으로
   이어짐을 흉내 내는 길은 **이미 실측으로 기각됐다**(3년 창에서 달 넷이 통째로 말랐다 — 보고 §1).
3. ~~**[영역 K · body] 젖음이 추위를 만든다.**~~ ⇒ **[T105 2026-09-05 닫힘]** `body.wetStep` 신설 ·
   `warmthInsC(warmth, wet)` · HUD 낱말 하나. ⚠단 **지붕(`indoor`)만** 막는다 — 마을 완충은 지붕이 아니다
   (아래 원문의 `shelter` 짐작이 실측으로 뒤집힌 자리다). 원문은 족보로 남긴다:
3. **[영역 K · body · 닫힘] 젖음이 추위를 만든다.** 비 맞으면 더 춥다 — `precip`×`shelter`(마을·지붕 아래는 안 젖는다)
   ×`insC`(옷이 젖으면 방한이 준다) 가 자연스러운 축. 이 카드는 **플레이 영향 0** 이라 손대지 않았다.
4. **[영역 C · T99 뒤] 밭 물대기 대체.** 비 온 날은 물을 덜 준다. `crops.js` 는 T99 소유라 무접촉.
5. **[영역 K] 시간대 창(소나기).** 첫 판은 **하루 단위**다(카드 지시). 하루 안에서 언제 오는가를 넣으면
   강도 곡선(시작·절정·그침)이 따라온다. `tempAt` 이 이미 `hourFrac` 을 먹으니 문법은 있다.
6. **[영역 K] 경도 시차 — 기온과 *같이* 간다.** 존마다 하늘이 다르면 그건 세계가 아니라 방이다.
   강수만 따로 시차를 주지 마라.
7. **[영역 R1] 구름 그림자 · 번개 · 눈 쌓임 · 비 소리.** T93 회부 그대로 살아 있다(아래 `0-날씨`).
   이제 **값이 오므로** 이것들은 전부 실제 하늘에 물릴 수 있다.

8. ★★**[영역 공통 · 하네스] 접점 목록이 "파일 이름을 부르는 하네스"만 본다.** T98 이 하늘을 켜자
   `e2e-tilestate`(전화면 정지)와 `e2e-nature`(결정론·안개 위 밝은 픽셀)가 빨개졌다. 그런데
   **`e2e-nature` 에는 `weather` 라는 낱말이 없다** — `grep -l "<접점 파일명>"` 으로는 영영 안 걸린다.
   화면 픽셀을 견주는 하네스는 **렌더 층이 하나 늘 때마다** 다 같이 흔들린다.
   ⇒ 「프레임을 픽셀로 재는 하네스 목록」을 한 자리에 두고 그런 카드에서 통째로 돌리는 규약.
   (T98 은 `windOff` 를 부르는지로 **우연히** 열 개를 찾아냈다. 우연을 규약으로 바꿔라.)

★**하네스 쪽 한 줄**: `e2e-weather` 는 `__rainForce` 로 클라 층을 **직접** 켠다(서버 값을 안 본다).
그래서 그 31건은 배선의 증거가 아니다. 배선은 `test-weather ⑧`(zone.js 본문을 구조로 잘라 assert)이 진다.
언젠가 실클라에서 **서버가 보낸** `wx.precip` 을 읽는 한 줄이 있으면 좋다(그날 하늘이 마를 수 있어
`__rainForce` 없이 초록을 보장하기 어렵다 — 그게 T93 이 훅을 둔 이유이기도 하다).

## 0-식. ★★[T86 ECON 수술 2-d 2026-09-05] 다른 영역에 넘기는 것 다섯

> 전문 `보고/T86_2026-09-05.md`. 재민 승인 2026-09-05 · main `7d08b677`.

1. **[T-거래] ★수출 잉여 판정이 식단 예산을 모른다.** 곳간을 먹어 치우니(농촌1 식량등가 17,930 → 1,459)
   내다 팔 잉여가 줄고(유출 125 → 93) → **목재 유입 2,192 → 0** → `housing 281.8 → 80.3` → `gated: true`
   → 성장 정지(246 → 81). ⚠**굶은 게 아니다** — `hunger` 가 양쪽 다 0 이고 식량흐름 EMA 도 양수다.
2. **[생활층] 벌목꾼 슬롯이 왜 안 서나** — `sim/economy-sim.js:3432`(지시서의 :3303 은 T73·T86 삽입으로 밀렸다).
   농촌1 은 **목재 511.4 · 벌목꾼 7명**이라 규칙(`wood < 2N`)이 발동할 이유가 없다 —
   못 지은 건 집 건설식(`:2734`)의 셋째 항 **여유노동**이 0 이라서로 보인다.
   농촌7 은 목재 0 · 조건 둘 다 참인데 안 늘었다 ⇒ **슬롯 포화**로 보인다.
3. **[econ] `jobCapacity` 를 export 하라(한 줄).** 위 표에서 cap 을 정확히 못 채웠다 —
   식을 계측기에 옮겨 적으면 그게 사본이라 안 했다.
4. **[E] 행복 계단 판정** — 3% 문턱 계단을 달성 효용 비로 바꾸는 것은 **카드 하나**다(보고 §2-b 가 근거).
   같은 판에서 켜면 식단과 행복 중 어느 쪽이 인구를 깎았는지 못 가른다(5,933 → 5,524 · 하위25% 1.252 → 0.316).
5. **[품목] 견과·호두·도토리가 `TRADABLE` 밖이라 가격이 없다** — 예산 후보에 못 든다
   (`FORAGE_FOOD_FACTOR` 엔 열량이 있다).

## 0-옷시설. ★★[T95 옷 여섯 · 계단·바닥·농지 2026-09-05 · 세션8 ART] 회부 여섯

> 전문 `보고/T95_2026-09-05.md`. 만진 것: `props_render.py`(옷 6 · 바닥 · 계단) ·
> `fields_render.py`(농지 아이콘) · `icon_render.py`(옛 셋 삭제) · `test-icons ⑨` · `test-props ④` ·
> 클라 3파일. **T67 이 남긴 마지막 셋이 닫혔다** — `36-r2-building.js` 벡터 −184줄.

1. **[ART] 옷 재질이 두 파일에 있다(이중화).** `char_render.py CLOTH_MATS` ↔ `props_render.py _CL`.
   공용 모듈(`render_common`)로 올리면 한 벌이 된다. 그때까지 **`test-icons ⑨` 가 두 표를 값으로
   견딘다** — 한쪽만 고치면 빨개진다(돌연변이로 확인). 올릴 때 상수 둘(`TRIM_K`·`PLACKET_K`)도 같이.

2. **[설계] 마을 시뮬 경작지 카펫 타일** — `farmland` 절에 남은 벡터 층. T6 이 **의도적으로** 남겼다
   ("셀을 꽉 채워 띠가 이어져 보이게 — 스프라이트를 얹으면 논밭 벨트의 연속성이 깨진다").
   8군 세트로 덮을지, 띠 렌더를 유지할지 **설계 판정**이 필요하다. T95 는 안 건드렸다.

3. **[ART] 움집 실내 — 거적 침상 · 화덕.** `floor` 절에 남은 벡터다. 그런데 **바닥이 아니라 실내 가구**라
   물건 하나 = 렌더 둘의 대상이다(침상 6자리 · 화덕 1). props 로 올리는 카드가 따로 필요하다.

4. **[ART] nature/building/char 5.0.1 재굽기** — 각자 카드. 굽는 기계 정본과 IDAT 대조 규약은
   `인계/ART-자산.md` §13, 축소가 필요하면 `rc.downscale_png`(§15).

5. **[설계] 종별 34세트**(T79c 회부 2) · **[ART] 상추/아욱 26px**(T79b 회부 3 · 재민 눈 판정 대기).

6. **[ART] 지게 등짐** — 아이콘은 이미 있다(T72 `carrier`). 등에 진 모습은 T81 시트 쪽이라
   ART 가 아니라 캐릭터 카드다.

★★**검사기에 수를 박지 마라**(`인계/ART-자산.md` §16):
  `test-props` 의 `아이콘 8종`·`가구 8종` 이 하드코딩이라 물건이 열로 늘자 **검사기가 먼저 빨개졌다.**
  코드가 아니라 검사기가 틀린 것이다 — 수는 **굽는 표에서 유도**해라.

## 0-밭. ★★[T79c 밭 8군 세계 세트 2026-09-04 · 세션8 ART] 회부 여섯

> 전문 `보고/T79c_2026-09-04.md`. 만진 것: `fields_render.py`(← `crop_render.py` 대체) ·
> `render_common.downscale_png`(신설) · `test-crops-world.js`(신설) · 밭 32장 · 클라 4파일.
> **T79 회부 1·3 과 T79b 회부 1·3 은 이 카드가 갚았다**(8군 세트 · 축소 단계).

1. **[서버/설계] 마을 칸이 작물을 안 싣는다 — 한 필드면 된다.**
   `villages.js` 의 `vil._crop`(T58b 정본)에는 무엇이 심겼는지 **있다**. 그런데 claim 브로드캐스트엔
   `crop` 이 없어 클라가 못 본다. 그래서 마을 밭은 지금 **곡물로 떨어진다**(플레이어 밭은 제 군으로 뜬다).
   ⇒ `claim.crop` 한 필드면 마을 밭도 제 군으로 그려진다. **클라 쪽은 이미 준비돼 있다** —
     `cropSprite(cl.farmStage, cl.crop)` 이 그대로 맞는다. T79c 는 "게임 코드 = 선택 1곳" 규칙이라 안 건드렸다.

2. **[설계] 종별 34세트** — PM 이 8군 뒤로 미룬 판정. **32px 에서 34종을 가를 수 있는가**가 먼저다
   (T79b 에서 상추/아욱이 26px 에서 안 갈렸다 — 밭은 더 작다). 8군도 익은 단계에서만 확실히 갈린다.

3. **[ART] 다년생 그루터기·겨울 휴면 스프라이트** — T91(세션3) 뒤. 지금 4단계는 한해살이만 그린다.
   `crops.json` 에 `lifecycle` 열이 있다(1년생/다년생) — 그 열이 세트를 하나 더 부를 수 있다.

4. **[ART] 옷 아이콘 여섯**(T81 시트 색 정본) · **옛 `item_*` 셋**(바닥·계단·농지) · **지게 등짐**.

5. **[ART] nature/building/char 5.0.1 재굽기** — 각자 카드. 축소가 필요하면 이제
   `rc.downscale_png` 를 쓰면 된다(구전 끝). 잠금표는 `icons.lock.json` 꼴로 남겨라.

6. **[ART] 상추 vs 아욱 26px**(T79b 회부 3 유지 · 재민 눈 판정 대기).

★★**하네스가 내 주장을 되받았을 때 주장을 고쳐라**(`인계/ART-자산.md` §15 에 전문):
  "맨 흙도 군마다 다르다"는 **거짓이었고**(3/8), 심기 전 밭이 무엇이 심길지 말해 주면 그게 거짓말이다.
  "불투명 화소가 는다"로 자람을 재려 했더니 **액자가 커진 것**을 자람으로 읽었다 —
  못 재는 자는 튜닝하지 말고 **바꿔라**(T76 IoU 와 같은 판단).

## 0-작물b. ★★[T79b 작물 아이콘 b 2026-09-03 · 세션8 ART] 회부 여섯

> 전문 `보고/T79b_2026-09-03.md`. 작물 **34종이 전부 아이콘을 갖게 됐다**(68장).
> 만진 것: `models_crops.py`(빌더 넷 + `_blade`) · `test-icons ⑦`(68키) · `icons.lock.json` ·
> 아이콘 40장 · `43-i-icon.js` 목록. **T79 회부 1 은 이 카드가 갚았다.**

1. **[ART] T79c — 세계 8군 세트**(PM 판정). `crops.json` 의 `group` 으로 고른다(좌표 해시가 아니라).
   ★같이 할 4.0.2 작물 스프라이트 8장 재굽기에 **막힌 것이 아직 있다**(T79 회부 3 유지) —
   `crop_render.py` 는 512² 를 내는데 배포본은 64px 이고 **그 줄이는 단계가 문서에 없다.**
   박스 축소 8× 로 재현이 안 된다(평균 6~19/255). **그 단계부터 찾아 적어야** 재굽기가 화풍을 안 바꾼다.

2. **[작물 층] 뽕은 나무인가 한해살이인가.** 서버는 지금 `growDays 70` 한해살이로 다루고 **씨앗으로 심는다**
   (`plantMenuPayload` 가 34종을 다 돈다 · 야생 채종도 뽕씨를 낸다). 실물은 잎을 따는 **나무**다(묘목·꺾꽂이).
   T79b 는 **서버 모델을 따라** 씨앗으로 그렸다. 묘목으로 갈 거면 그림이 아니라 **작물 층의 판정**이 먼저다.

3. **[ART/설계] 상추와 아욱이 26px 에서 안 갈린다.** 96px 에선 갈린다(상추는 활짝 · 아욱은 잎자루가 보인다).
   더 벌리려면 **색**을 갈라야 하는데 둘 다 초록이라 고증과 부딪친다. 판단이 필요하면 재민.

4. **[ART] 옷 아이콘 여섯**(T81 뒤 · 같은 재질) · **옛 `item_*` 셋**(바닥·계단·농지) · **지게 등짐**.

4-b. **[R2/세션2 — T81 영역] `e2e-charsprite` 가 main 에서 빨갛다(62 통과 · 4 실패).**
   T79b 착지 **전후 모두** 같다 — `origin/main` 을 내 커밋 없이 떼어 돌려도 **똑같이 62/4** 다.
   실패 넷은 전부 "남"에 관한 것: `[A] 화면에 남이 스프라이트로 보인다 0명` · `남도 정지 = idle` ·
   `남이 걸으면 A 화면에서도 walk` · `남이 멈추면 다시 idle`. 옷 층이 아니라 **남의 애니 배선**으로 보인다.
   ⇒ 세션2 가 볼 것. (T79b 는 이 하네스를 안 만졌다 — 귀속만 재서 넘긴다.)

5. **[ART] nature/building/char 5.0.1 재굽기** — 각자 카드.
   굽는 기계 정본과 IDAT 대조 규약은 `인계/ART-자산.md` §13. 옮길 때 자기 잠금표를 남겨라.

6. **[ART] `nature_render.py:333` · `char_render.py:801` 에 `sensor_fit` 없음** (T67·T77·T79 회부 유지).

★★**새 모델을 지을 때 두 번 밟은 함정**(`인계/ART-자산.md` §14 에 전문):
  **잎은 `plane` 으로 못 만든다** — 모난 사각형이라 '초록 종이'로 읽힌다. 납작하게 누른 이코스피어에
  **잎맥 한 줄**을 얹어라(`_blade`). 그리고 `plane(sx,sy,…)` 뒤에 `o.scale` 을 **덮어쓰지 마라**(T79 §②).

## 0-작물a. ★★[T79 작물 아이콘 a 2026-09-03 · 세션8 ART] 회부 여섯

> 전문 `보고/T79_2026-09-03.md`. 만진 것: `models_crops.py`(신설) · `crop_render.py`(편입) ·
> `render_common.py`(`ico` 명시 갱신) · `test-icons`(⑦⑧) · `icons.lock.json`(신설) ·
> 자산 113장 · `43-i-icon.js` 목록 한 줄. **T77 회부 ①②③ 은 이 카드가 갚았다.**

1. **[ART] T79b — 채소 9 · 양념 3 · 박과 2 · 특용 4 · 구황 2 = 20 + 씨앗 20.**
   빌더가 더 필요하다(뿌리·잎·박·덩이). ★특용 4종(삼·뽕·차·쪽)은 `isFood` 가 걸러 내는
   **비식품**이라 씨앗 아이콘이 의미 있는지 먼저 재라.

2. **[설계] ★세계 작물 스프라이트가 작물을 안 본다.**
   `public/client/00-const.js cropSprite()` 는 `grain`/`veg` 를 **좌표 해시 홀짝**으로 고른다 —
   작물의 분류(8군)도 id 도 안 본다. **벼밭이 채소로 그려진다.**
   ⇒ 종별 분화 · 분류별 분화 · "세계는 종을 표현하지 않는다"를 규약으로 못박기 — **설계 판정**.
   (T79 는 카드 지시대로 세계 스프라이트를 안 늘렸다. 종은 아이콘 14장이 혼자 지고 있다.)

3. **[ART] 작물 세계 스프라이트 8장이 아직 4.0.2 다 — 그런데 막힌 것이 있다.**
   `crop_render.py` 는 512² 를 내는데 배포본은 **64px** 다. 그 **줄이는 단계가 문서에 없다.**
   박스 축소 8× 로 재현해 봤으나 배포본과 평균 6~19/255 어긋난다(4.0.2 상류 차이가 섞여 분리 불가).
   ⇒ 다시 굽는 카드는 **그 단계를 먼저 찾아 적어야 한다**. 안 그러면 재굽기가 화풍을 바꾼다.

4. **[ART] 남은 4.0.2 산출 — 자연물·건물·캐릭터.** 굽는 기계 정본은 이제 **pip `bpy` 5.0.1** 이다
   (재민 판정 2026-09-03). 각 카드가 그 파일을 열 때 옮긴다. T81(세션2)이 지금 `char_render.py` 를
   만지고 있다 — T79 는 무접촉했다. 옮길 때 `icons.lock.json` 꼴로 자기 잠금표를 남겨라.

5. **[ART] `nature_render.py:333` · `char_render.py:801` 에 `sensor_fit = 'HORIZONTAL'` 이 없다.**
   (T67·T77 회부 유지 · 아직 미해소.) 자연물 38장 중 15장이 세로가 길다.

6. **[ART] 옷 아이콘 4~6 · 옛 `item_*` 셋(바닥·계단·농지) · 지게 등짐** — T81 뒤, 같은 재질로.

★★**다음 재굽기가 반드시 읽을 것**(`인계/ART-자산.md` §13 에 전문):
  블렌더가 쓴 **원본 PNG 는 같은 코드로 두 번 구워도 바이트가 다르다** — `Date`·`RenderTime`
  tEXt 가 박힌다. 굽는 자리의 대조는 **IDAT(화소 페이로드)** 로 해라. 파일 `cmp` 는
  후처리를 거친 배포 자산에서만 성립한다.

## 0-곡. ★★[T73 ECON 수술 2-a 2026-09-03] 다른 영역에 넘기는 것 여섯

> 전문 `보고/T73_2026-09-03.md`. 재민 판정: ① 기준선 갱신(새 여덟 수) ② `인계/공통.md` 두 줄은 PM 이 간다
> ③ 단일 계수 0.72 는 **잠정** — 품목별은 ECON 2-c 에서.

1. **[PM · 인계/공통.md] 기준선 두 줄을 갈아야 한다.** 새 줄은 `보고/T73_2026-09-03.md` **§9-②** 에
   그대로 적어 뒀다(붙여넣기용). v2 CLI 회귀 줄 + 51마을 3시드 표.
2. **[PM · 계측] ★옛 `econ-regress` 수치는 폐기다.** `scripts/econ-regress.js` 의 `ROOT` 가
   `/root/minirepo`(이 저장소가 **아닌** 낡은 체크아웃 3ee3569)로 못 박혀 있었다. T73 이 고쳤다.
   ⇒ `보고/T17_*.md`·`보고/T60_*.md` 의 "econ-regress 2,616 · 651 · 13,473 불변" 줄은 **안 움직인 게
   아니라 안 본 것**이다. 진짜 T73 직전 값은 **2,415 · 508 · 10,410**(같은 계측기 · 되돌림 판).
3. **[econ — 2-c] 품목별 도정 수율.** 밀 0.750 · 쌀 0.771 은 이미 유도돼 있다(§9-③ 표).
   ⚠막는 칸 하나: **보리 도정 수율에 고증 앵커가 없다.** 보리는 생곡의 25~28% 라 작지 않다.
4. **[econ — 2-b] NPC 농사 `q`·숙련 → 곳간**(T58 표 뒤). 이 카드는 곳간을 **읽는 자**를 고쳤지
   곳간을 **채우는 자**를 안 건드렸다.
5. **[F 제작] 도정·제분 시설.** 이 카드는 생곡을 **곳간 안에서** 식량으로 쳤다. 방앗간이 세워지면
   "곡식 → `food`" 라는 **동사**가 생기고, 그때 이 계수는 그 동사의 수율로 옮겨 가야 한다.
6. **[E 사건장부] 밀도가 캐논에서 더 멀어진다** — 1.92·1.96·1.95 → **1.82·1.92·1.85**(캐논 2~3일).
   T60 §0-ⓔ 실측대로 밀도를 정하는 건 **가격 사건(61.7%)** 이고 문턱은 `EV_PRICE_UP/DOWN`·`HYST` 다.

## 0-렌더공용. ★★[T77 렌더 공용 모듈 2026-09-03 · 세션8 ART] 회부 다섯

> 전문 `보고/T77_2026-09-03.md`. 이 카드가 만진 것: `scripts/render_common.py`(신설) ·
> `icon_render.py` · `props_render.py` · `test-render-common.js`(신설). 그림은 **한 장도 안 바꿨다**.

1. **[ART]** `ico()` 의 **"우연한 뎁스그래프 갱신"을 바로잡는 카드가 필요하다.**
   지금은 `bpy.ops.object.shade_smooth()` 가 오퍼레이터라서 생기는 부작용에 기대
   `o.bound_box` 가 갱신된다. 아이콘 프리셋은 그 bbox 로 프레임을 잡는다.
   명시적 갱신(`bpy.context.view_layer.update()`)이 **더 옳은데, 아이콘 2장이 바뀐다**
   (`hide`·`meat_cooked` — 프레임 중심이 지터 반영 위치로 이동, 평균 4.9/255).
   그림이 바뀌는 변경이라 **눈으로 보고 받는 카드**가 따로 있어야 한다. T77 은 리팩터라 안 했다.

2. **[ART]** `icon_render.py` 산출 **31장이 Blender 4.0.2 시대 굽기**다.
   컨테이너는 pip `bpy` 5.0.1 로 옮겼는데 이 파일은 그 뒤로 다시 안 구웠다
   (T67 은 가구 모델을 **빼기만** 했다). 지금 구우면 **옛 코드로 구워도** 평균 8–26/255 다르다.
   ⇒ 31장을 다시 굽고 대조 시트로 눈에 받는 카드. 굽기 자체는 3분.
   ★그때까지 **"저장소 PNG 와 `cmp` 0"을 합격선으로 쓰지 마라** — 자가 아니다.
     옳은 자는 **같은 컨테이너·같은 bpy·옛 코드 대조군**이다(방법은 `인계/ART-자산.md` §12).

3. **[ART]** `scripts/icon_renders/` 에 **추적되는 중간 산출 12장**이 남아 있다(야금 8 + 4종).
   `props_renders/`·`props_icon_renders/` 는 T67 이 `.gitignore` 에 넣었는데 이쪽은 안 넣었다.
   다시 굽는 세션이 그 폴더를 `rm -rf` 하면 12장이 **삭제로 잡힌다**(T77 도 한 번 밟고 되돌렸다).
   ⇒ `.gitignore` 에 넣고 12장을 정리할지, 남길지 결정 필요.

4. **[ART]** `nature_render.py:333` · `char_render.py:801` 에 **`sensor_fit = 'HORIZONTAL'` 이 없다**
   (T67 회부 유지 · 아직 미해소). 자연물 38장 중 **15장이 세로가 길다** — `ortho_scale` 이
   긴 변을 잡아 확대돼 찍혔을 수 있다. T77 은 카드 금칙이라 두 파일을 안 열었다.

5. **[ART]** `crop_render.py`·`nature_render.py`·`building_render.py`·`char_render.py` 는
   아직 **공용 모듈을 안 쓴다**. 각자 자기 헬퍼 사본을 갖고 있다.
   `crop_render.py` 는 **T79(작물 68장)가 어차피 그 파일을 여니** 같이 옮기는 게 싸다.
   나머지 셋은 각자 카드에서. 옮길 때의 문법과 함정은 `인계/ART-자산.md` §12 에 적어 두었다.

## 0-덥. ★[T89 교역로 마무리 2026-09-04] 넘기는 것 다섯 · **닫은 것 둘**

> 전문 `보고/T89_2026-09-04.md`. 규약은 `인계/Z-존서버.md` `## T42-d`.

★**닫았다 둘** — 위 `0-길` 의 2(랩 인라인)와 3(선계산 정책):
* **랩 사본**: `node sim/inline-path.js` 로 갱신했고(정본 247줄 = 사본 247줄 · 두 랩 다),
  더 중요하게 **`--check` 를 더해 `lab-wiring-check [G]` 가 매번 재게** 했다.
  어긋난 채로 있던 진짜 원인은 재인라인이 늦은 게 아니라 **검사기가 없었던 것**이다.
* **선계산 정책**: 사람이 있어도 데운다(게이트 하나만 바뀜 · 두 수 그대로 · `ioBusy` 유지).

1. **[N] `localPath` 재개형 — 안 한다(유지).** 플레이어 재경로는 짧은 탐색이다. 문 하나만 열면 되니 필요해지면 그때.
2. **[Z] 워커 스레드 — 기각(유지).** 경로는 랩과 서버가 같은 코드로 같은 답을 내야 하는 자리다.
3. **[상자] 17초 루프 막힘 — 회부 §A.** 가장 큰 조각(0.9초)의 17배다. 베이스도 같다.
4. **[Z] 조각 사이 쉼 — 안 산다.** 워밍 한 조각이 **31ms** 인데 관측 잡음(`/health` 최대 간격)이
   547~1,088ms 다 — 지울 대상이 잡음보다 20~35배 작다. 넣으면 완주가 3분 → 10분이 된다.
   되살릴 근거가 생기면 그 간격의 **정체**부터 다시 재라(지금은 일틱 마감이다).
5. **[ART/랩] 랩 HTML 은 저장소 밖이다**(`~/Mini/*.html`). 커밋에 안 들어가고 컨테이너에도 없다 ⇒
   `lab-wiring-check`·`test-lab-market` 은 **인자로 경로를 줄 때만** 잰다.
   `sim/path-core.js` 나 econ 번들을 여는 카드는 `device_stage_files` → 검사·갱신 → `device_commit_files`
   를 **카드 절차에** 넣어라(T89 가 선례). 안 하면 검사기가 있어도 안 돈다.

## 0-길. ★[T85 재개 가능 A* 2026-09-03] 다른 영역에 넘기는 것 다섯 · **닫은 것 하나**

> 전문 `보고/T85_2026-09-03.md`. 규약은 `인계/Z-존서버.md` `## T42-c`.

★**닫았다 — T42 회부 3(`재개 가능 A*`)은 T85 가 갚았다.** `sim/path-core.js` 를 `_searchBegin` +
`_searchStep(S,budget)` 로 가르고 `_search` 를 그 둘의 **동기 래퍼**로 뒀다(서명 무변 · 답 비트 동일 ·
586쌍 실측 · 3시드 800일 diff 0). 캐러밴 한 조각 **1,265~1,695ms → 31ms**.

1. **[N 네트워크] 플레이어 재경로(`localPath`)의 재개형 전환.** 커널은 이미 재개 가능하다 — 문 하나만 더 열면 된다.
   지금은 벽 인지 **짧은** 탐색(4방·`maxNodes` 16,384·대개 `radius` 제한)이라 급하지 않다.
2. ~~**[ART/랩] 랩 2종의 `path-core` 인라인 사본이 낡았다.**~~ **★[T89 2026-09-04] 닫혔다**(위 `0-덥`). (아래는 당시 기록.)
   **[ART/랩] 랩 2종의 `path-core` 인라인 사본이 낡았다.** T85 가 `sim/path-core.js` 를 고쳤는데
   카드 §1 이 **랩 무접촉**이라 재인라인을 안 했다(랩은 저장소 밖 `~/Mini/*.html`).
   ⚠**답은 안 갈린다** — 랩은 동기 문만 쓰고 그 문은 비트 동일이다. 갱신은 한 줄:
   `node durango-mini/sim/inline-path.js`. 다음에 `path-core` 를 여는 카드가 같이 돌려라.
3. ~~**[Z] 선계산 정책**(T42 회부 2 계속)~~ **★[T89 2026-09-04] 닫혔다 — ⓐ(사람이 있어도 데운다)로 갔다**(위 `0-덥`). (아래는 당시 기록.)
   **[Z] 선계산 정책**(T42 회부 2 계속) — 재개형이면 **사람이 있을 때도** 데울 수 있다(한 걸음이 16ms).
   T85 는 정책을 **안 바꿨다**(유예 30초·간격 250ms 그대로 — 고친 건 걸음의 길이뿐).
   표: ⓐ 지금대로(무인만) / ⓑ 사람이 있어도 더 긴 간격 / ⓒ 접속 수에 따라.
   ⚠바꾸면 `test-route-persist ⑦`(유예·간격)의 뜻을 같이 다시 써야 한다.
4. **[Z] 워커 스레드 — 기각(PM 판정 · 기록만).** 경로는 **랩과 서버가 같은 코드로 같은 답**을 내야 하는
   자리라, 워커로 옮기면 정본이 프로세스 경계를 넘으며 **사본이 되고** 결정론이 스레드 스케줄에 얹힌다.
5. **[Z] 재개형 슬롯을 늘리지 마라.** 교역로 격자 scratch 가 하나(gen-스탬프 하나 · `came` 는 스탬프도
   없음)라 두 탐색이 번갈아 쓰면 **서로의 g 를 지운다**(`sim/_path-core-test ④b` 가 그 깨짐을 실측한다).
   늘리려면 격자(547×1016 × 3배열)를 그만큼 더 만들어야 한다 — 이 카드가 산 값보다 비싸다.

## 0-판. ★[T80 게시판 판 2026-09-03] 다른 영역에 넘기는 것 다섯 · **닫은 것 하나**

> 전문 `보고/T80_2026-09-03.md`. 이 카드는 게시판을 판으로 세우고 행마다 납품 버튼을 달았다.

★**닫았다 — 위 3번(`keepAliveTimeout`)은 T80 ⓪-2 가 갚았다.** `server/zone.js` 의 http 서버에
`keepAliveTimeout = 30000`(`ZONE_KEEPALIVE_MS`) · `headersTimeout = +10000` 두 줄.
값은 `test-tick-slicer` ⓑ 실측에서 끌었다(조각내기 최대 조각 1,790ms · 대조군 한 프레임 4,675ms
⇒ 30초 = 각각 ×16 · ×6). **안전망일 뿐이다** — 남은 바닥(caravan 조각)은 아래 §4-A 그대로다.

1. **[E 사건장부 / ECON] 의뢰 걸기** — 플레이어가 게시판에 의뢰를 건다. 값·상한·담보가 econ 판정이라
   판(클라)이 아니라 장부 쪽 카드다. 판은 행 하나만 더 그리면 된다(자리는 이미 있다).
2. **[S/E] 소식 상세 · 연대기와 합치기.** 게시판 판과 연대기 판(`65-s-chronicle`)이 **같은 `news`** 를
   서로 다른 문법으로 그린다(사본은 아니다 — 문장 정본은 `Events.briefLine` 하나다).
   한 줄을 눌러 앞뒤를 보는 문은 아직 없다.
3. **[S] 촌장 대사 → 판 열기 링크.** 촌장은 아직 "(게시판 n건 · Shift+G)" 라고 **말로** 안내한다.
4. **[N/S] 판 둘이 같은 선반을 쓴다.** 회관 판(`villageInvPanel`)과 게시판 판이 둘 다 오른쪽 위다.
   한 번에 하나만 읽는 판이라 지금은 뜻이 통하지만, 셋째 판이 생기면 **선반 규약**(어느 판이 어디에)이 필요하다.
   ⚠**새 판을 만드는 세션은 먼저 이걸 읽어라**: 판에 덮개(`class="modal"`)를 깔지 말고,
   `#sidePanel.open`(x 60~520)을 덮지 마라. 둘 다 §8.2 논모달을 깨고, 깨지면 **그 판의 버튼이 안 눌린다**
   (T80 1차 판이 정확히 그랬고 `e2e-membership` 이 잡았다 · 지금은 `e2e-events` 가 좌표로 잰다).
5. **[Z] `__e2eForceShortage` 는 늘 소비EMA 최댓값 하나만 고른다.** 몇 번을 불러도 **같은 의뢰 한 건**이라
   "부족을 여럿 세운다"가 안 됐다. T80 은 `skip`(뺄 품목) 인자로 우회했다(기본 동작 무변).
   픽스처가 **후보를 순회**하는 편이 옳을 수 있지만, 지금 고치면 기존 하네스의 세계가 달라진다.

## 0-터. ★[T102 바닥템 개체 2026-09-05] 넘기는 것 여섯

> 전문 `보고/T102_2026-09-05.md`. **지게 낙하(0-커 2)는 이 카드가 닫았다** — 다만 답은
> "지게를 떨어뜨린다"가 아니라 **"입은 것은 안 떨어진다"를 규약으로 못 박는 것**이었다(재민 판정 ②).

1. **[B] 꺼진 청크 움집 문간 — DB 조회.** 0-처 5 그대로 유지(안전한 축소로 떨어진다).
2. **[B / 재민 실기 뒤] 값 재정박 둘.** 0-커 4 유지 — 깨어남 지연의 덧셈 항 · 한 끼에 한도가 없다는 점.
3. **[B / 설계] 쓰러진 걸 남이 모른다.** 외침(T56)이 반경 안에만 닿는다 — 소리가 더 멀리 가야 하는지,
   아니면 그게 3분 창의 값인지는 설계 판단이다.
4. **[N / 회부 문서 그대로] 재접속 공짜 풀피가 사라진 건 이 카드 밖**이다(T43·T47 이 닫았고 문서만 남았다).
5. **[N / 클라] HP 자연 회복이 화면에 없다.** 값은 싸다 — 세션4 다음 카드 후보.
6. **[R2] NPC 업기 모션.** 0-커 1 유지.
7. **[T90 / `e2e-verbs` 간헐 — 야간 러너가 여기서 흔들린다]** 두 판을 봤다.
   ⓐ ⑦ "사람 갈래는 우클릭만 켠다"가 `null` — 상대 플레이어가 그 순간 `others` 에 없던 **가시성 경주**.
   ⓑ **`[R] 존 입장` 실패** → 뒤가 통째로 무너지고 `TypeError`(`findIndex` of null)까지. 세 클라 중
      **하나만** 못 들어간 것이라 결정론적 결함이 아니다.
   ★귀속 실측: 순수 `origin/main` 67/0 · 같은 커밋 내 HEAD 단독 재실행 67/0(족보 ㊾) ⇒ **간헐**이다.
   고칠 자리 둘: 입장 실패를 **전제로 세워 즉시 멈추게** 하고(지금은 뒤로 흘러 TypeError 로 죽는다),
   사람 지목은 **상대가 화면에 든 뒤에** 재라. T102 는 그 하네스 소유가 아니라 고치지 않았다.
8. **[PM / CI 한 줄]** `scripts/test-inv-ground.js`(T102 신설 · `@regress`)를 **단위 회귀 목록에**
   넣을 값이 있다 — 마을을 안 켜고 **1초**에 끝나고 브라우저가 없다(지금 23종의 성격 그대로).
   `.github/workflows/regress-unit.yml` 은 PM 소유(공통 §2 ⑥)라 세션이 안 건드렸다.
   ⚠안 넣으면 야간 전수에서만 돌아 **PR 단계에서 개체 왕복 회귀를 못 잡는다**.

★그리고 **닫힌 것 하나**: ~~지게 낙하(바닥템이 장비 인스턴스를 실어야)~~ → **T102 ① 이 실었다.**
바닥템이 `gi.inst` 로 개체를 통째로 싣게 됐고, 가방 안 장비는 죽으면 떨어지며 주워도 그 물건이다.
**입은 장비는 안 떨어진다** — 그건 결함이 아니라 규약이었음을 재민이 판정했다(`인계/B-신체서버.md` 3-하).

## 0-커. ★[T88 쓰러짐 2차 2026-09-04] 넘기는 것 넷

> 전문 `보고/T88_2026-09-04.md`. T83 §4 의 ⓐⓑ 는 이 카드가 **닫았다**(0-처 1·2 참조).

1. **[R2] NPC 가 업어 가는 모션.** 마을 안 구제는 여전히 화면상 사라졌다 나타난다. 서버엔 업기
   배선(`_carriedBy` · `tickDowned` ⓐ)이 이미 있으므로 NPC 가 그 배선을 타면 된다(T87 계보 · 별도 카드).
2. ~~**[I / ART] 지게 낙하 — 바닥템이 장비 인스턴스를 실어야 한다.**~~ → **[T102 2026-09-05] 닫혔다.**
   바닥템이 개체를 통째로 싣는다(`gi.inst`). 다만 **입은 장비는 여전히 안 떨어진다** — 결함이 아니라
   규약이라는 것이 재민 판정이다(0-터 · `인계/B-신체서버.md` 3-하).
3. **[B / 판단거리] 꺼진 청크의 움집 문간 — DB 조회.** T83 0-처 5 그대로 유지. 지금은 안전한
   축소(문 앞 대신 집 안)로 떨어진다.
4. **[B / 재민 실기 뒤] 값 재정박 둘.** ⓐ 지연의 덧셈 항(지금 = 종전 `DOWN_WAKE_GAMEMIN` 그대로)이
   실기에서 지루한지 · ⓑ 한 끼가 곳간 0.28%인데 마을이 여럿을 연달아 살리면 부담이 되는지
   (지금은 한도가 **없다** — 소속 한도를 안 부르므로). 둘 다 손잡이가 이미 있다.

## 0-처. ★[T83 쓰러짐 정리 2026-09-03] 재민 판정 대기 넷 + 발견 하나

> 전문 `보고/T83_2026-09-03.md` §4. **전부 표만 냈고 한 줄도 안 만들었다.**

1. ~~**[B / 재민 판정] 야생 자살 귀환**~~ → **[T88 ① 2026-09-04] 닫혔다** — 지연이 거리의 함수가
   됐다(종전 + 거리÷`MOVE_SPEED`). 아래는 그때의 근거다. 지금 깨어남 지연은 거리와 무관한
   고정 2분(`DOWN_WAKE_GAMEMIN` 120). 정본 시드 세계 표본 121개에서 **20%가 "걸어가는 것보다
   죽는 게 빠르다"**(중앙 161m·1.3분 vs 고정 2분이지만, 3사분위 218m 부터 뒤집힌다).
   PM 제안(새 계수 0): 지연을 **죽은 자리 → 깨어날 자리 거리의 함수**로(`MOVE_SPEED` 에서 유도 ·
   걷는 시간 ×1.5 면 중앙 2.0분·최대 4.0분). ⚠단 짐 절반을 잃으므로 공짜는 아니다 — 값을 매길지는 재민 몫.
2. ~~**[B / 재민 판정] 마을 안 구제의 허기·갈증**~~ → **[T88 ② 2026-09-04] 닫혔다** — 곳간에서
   한 끼를 먹인다(곳간이 비면 죽음). 아래는 그때의 근거다. 누운 동안 게이지가 멎고
   깨어나도 **극단 그대로**라(허기 0 · 갈증 0) T44 식이 다시 깎는다: HP 20 에서 **4.8분 뒤 다시 눕는다**.
   PM 제안: 마을 사람이 **곳간에서 한 끼를 먹인다**(`playerVillageWithdraw` 정본 · 극단 밖까지만).
   실측: `Kcal.DAY_KCAL` 2450 · `food` 대표 아이템 0.7kg · 한 마을 곳간 food 재고 360단위
   ⇒ **한 끼(1단위)는 곳간의 0.28%** — 마을이 못 낼 값이 아니다. 곳간이 비면 못 먹이고, 그때는 죽음.
3. **[B] 로그아웃 부활 — 회부 아님, 이미 닫혀 있다.** T43·T47 이 `serializeBody.vital` 에 `isDown`·`hp` 를
   싣고 `parseBody({vitals:true})` 가 돌려준다. `test-downed ⑦` 이 소스 검사까지 걸어 둔다
   (`vitals:false` 로 읽는 자리 0곳). **한 줄로 끝.**
4. **[R2 / 순간이동 캐논] NPC 가 업어 가는 모션.** 지금 마을 안 구제는 화면상 **사라졌다 나타난다**.
   §12 문장(*"마을 사람들이 당신을 쉼터로 옮겼다"*)이 참이 되려면 옮기는 **장면**이 있어야 한다.
   서버엔 이미 업기 배선(`_carriedBy` · `tickDowned` ⓐ)이 있으므로 **NPC 가 그 배선을 타면 된다** —
   새 규약이 아니라 기존 동사의 NPC 판이다. R2 · 별도 카드.
5. **[B / 판단거리] 움집 행이 메모리에 없으면 문간을 모른다.** `buildings` 도 `qtBuildings` 도
   **로드된 청크만** 갖는데, 깨어나는 자리는 정의상 내가 없는 곳이다 ⇒ `_hutDoorNear` 가 null 이면
   사다리가 ②(근처 설 수 있는 칸)로 내려간다. **안전한 축소**(지형 판정은 절차적이라 물 위엔 안 선다 ·
   최악이 "문 앞 대신 집 안")이지만, 문간을 늘 맞히려면 **DB 의 `buildings` 행을 읽어야** 한다.
   값어치가 있는지는 재민 판정 — 새 실패 모드(DB 조회)를 죽음 경로에 들이는 값이다.
6. **[T 거래 · 발견 하나 · T83 이 고쳤다]** T69 §7 이 적어 둔 `quote.capped` 표시 결함 —
   `Math.floor(plan.gave) < q` 인데 `q` 가 **재화 단위(소수)** 라 늘 참이 됐다. `plan.gave < q - 1e-9`
   한 줄로 고쳤다(값 불변 · 3시드 여섯 판 diff 0 · `test-trade` 67/0). 회부에서 **내린다**.

## 0-저. ★[T69 거래소 v2 2026-09-03] 다른 영역에 넘기는 것 넷

> 전문 `보고/T69_2026-09-03.md`. 이 카드는 시세 기준 품목과 받을 양(올림)만 세웠다.

1. **[econ / ECON 2-c] `food` 라는 뭉뚱그린 재화 자체.** 캐논 0-b 는 "화면에 곡식은 없다 — 벼·보리만"인데
   거래소 행은 아직 `food` 한 줄이다. 곳간 구성이 구체 작물로 갈리는 그 카드에서 행도 갈린다.
   ⚠거래소 쪽은 준비가 돼 있다 — 기준 품목이 이제 **요청마다 오는 인자**라 행이 늘어도 코드가 안 늘어난다.
2. **[T 거래 / 캐논 §3.2] 원격 시세 조회는 만들지 않는다.** 이웃 마을 시세는 걸어가서 보는 것이다
   (`test-trade ⑦` 이 세 함수 전부에 게이트가 서 있음을 검사한다). 회부는 "만들지 마라"의 기록이다.
3. **[T 거래] 시세 이력 그래프.** 지금 화면은 **지금 값**만 말한다. "내가 판 뒤 얼마나 움직였나"는
   플레이어가 기억으로 재고 있다. 사건 장부(E)에 이미 `PRICE_SPIKE/DROP` 이 쌓이므로 그 위에 설 수 있다.
4. **[T 거래] 흥정.** 스프레드는 지금 고정 10%다. 사람에 따라·관계에 따라 달라지는 층은 이 카드 밖이다.
5. **[E 사건장부 / T78]** T78 이 고친 하네스 목록에 **`e2e-trade` 가 빠져 있었다** — 접두 이모지가
   걷히면서 `/🏪/`·`/🧊/` 필터 **네 자리**가 빈손이 됐다(순수 `origin/main` bfe6fc8c 에서 37/4 로 재현 · 귀속 확정).
   T69 가 자기 카드 하네스라 같은 문법(**말로 찾기** · `★[T78]` 주석)으로 고쳤다 — 검사의 뜻은 무변.
   ⚠**이모지로 알림을 찾는 하네스가 더 남아 있을 수 있다**: `scripts/*.js` 의 `__notices` 필터에
   이모지 리터럴이 붙은 자리를 한 번 훑을 값어치가 있다(야간 러너가 빨개지는 자리다).
6. ~~**[T 거래 · 종전 결함]** `quote.capped` 가 재화 단위(소수)를 `Math.floor` 와 맞대 늘 참이 된다~~
   → **[T83 ⓪ 2026-09-03] 닫혔다** — `plan.gave < q - 1e-9` 한 줄. 상세는 0-처 5.

## 0-어. ★★[T60 ECON 수술 1.5 2026-09-03] 다른 영역에 넘기는 것 넷

> 전문 `보고/T60_2026-09-03.md`. 재민 판정 2026-09-03: A 안 켬 · B 껐다 유지 · C 안 켬.

1. **[econ — ECON 수술 2 · 첫 조각이 T73(2-a)] 곳간에 생곡이 있는데 굶는다.**
   `totalFoodEquivalent` 도 `consumeFood` 사다리도 `wheat`·`rice` 를 **안 센다**(둘이 같은 규약이라
   모순은 아니다 — 생곡은 `food` 가 돼야 식량이다). 소멸한 광산1 은 **밀 21 · 쌀 12** 를 두고 죽었다.
   ⇒ 재민 판정: **ECON 수술 2 로.** T59 의 kcal 정본과 같이 세워야 한다(그 카드의 회부 1·2 와 한 묶음).
2. **[V 마을배치] 농지 0 · 숲 0 자리에도 마을이 선다.** 상세와 판단거리 셋은 `인계/V-마을배치.md` **T60-회**.
3. ~~**[Z 존서버] 존 http 의 `keepAliveTimeout` 이 기본 5초다.**~~ **★[T80 ⓪-2 2026-09-03] 닫혔다** — 위 `0-판` 참조.
   (아래는 당시 기록 그대로 둔다.)
   **[Z 존서버] 존 http 의 `keepAliveTimeout` 이 기본 5초다.** 일틱이 그보다 오래 루프를 잡으면 node 가
   **클라이언트가 막 쓴 소켓을 닫고**, 그 요청은 답을 못 받아 undici 기본 300초를 기다리다 터진다.
   이번 판에 하네스 셋이 이걸로 죽었다(`test-route-persist`·`test-site-memo`·`e2e-rtt`) — 하네스 쪽은
   `connection: close` + 재시도로 막았다. **플레이어의 폴링도 같은 창을 맞는다.** 진짜 자리는 `server/zone.js`.
4. **[E 사건장부] 사건 밀도 문턱 재정박.** 밀도의 **61.7%가 가격 사건**이다(PRICE_SPIKE 6,724 + PRICE_DROP 6,422
   / 전체 21,303 · 시드 1020 800일). 문턱은 `EV_PRICE_UP/DOWN`·`HYST` 이고 E 영역 몫이다.
   ⚠T17 이 더한 부족(도구 82 · 소금 50)은 부족 사건의 **3.9%** — 밀도를 정하는 건 그쪽이 아니다.

## 0-아이콘2. ★[T76 아이콘 2차 2026-09-03] 다른 영역에 넘기는 것 넷

> 전문은 `보고/T76_2026-09-03.md`. 규약은 `인계/ART-자산.md` §9-A·§11.

1. **[I 인벤 / T66]** 배선이 이제 **31키**다(T72 13 + T76 18). 목록·필요한 줄은
   `node scripts/test-icons.js` 의 ⑤절이 **매번 출력한다** — 손으로 옮겨 적지 마라.
   ⚠파일은 전부 실재하고 **404 는 안 난다**(요청 키를 안 늘렸다 · `test-icons ④`).
2. **[ART / 설계] 씬이 두 벌이라 원물 모델을 건너 쓸 수 없다.** `icon_render.py` 와 `props_render.py` 가
   씬·재질·헬퍼를 **각자** 갖고 있어서, `dried_fruit`←`berry` · `smoked_meat`←`meat_raw` 를
   "원물 변형"으로 못 만들었다(모델을 옮기면 사본이다). 푸는 길은 공용 모듈
   (`render_scene.py` + 모델 모듈) 하나뿐이고 **별도 카드**다. 지금은 셋만 진짜 계보다.
3. **[ART 다음 카드]** 작물 34 + 씨앗 34(`crop_render.py` 와 **같은 모델** · T77) · 옷(T74 표 뒤) ·
   옛 `item_*` 셋(계단·바닥·농지) 세계 스프라이트 · 지게 등짐(캐릭터 시트).
4. **[ART / 설계]** 아이콘 **상대 크기 규약**은 여전히 없다(T72 §0-ⓒ 유지) — 만들려면 전수 재굽기.

---

## 0-아이콘. ★[T72 아이콘 1차 2026-09-03] 다른 영역에 넘기는 것 여섯

> 전문은 `보고/T72_2026-09-03.md`. 만든 것의 규약은 `인계/ART-자산.md` §8-A·§9·§10.
> ★**0-가구 1번과 같은 자리를 두 번째로 밟았다** — 아이콘을 구워도 `43-i-icon.js` 가 안 열리면 화면에 못 닿는다.
>   두 카드 몫을 여기 합쳐 적는다. **줄 수는 다섯이고 전부 T66 소관이다.**

1. **[I 인벤 / T66 세션4 — 다섯 줄]** 구운 아이콘이 화면에 닿게 하는 배선. 파일은 **전부 실재**한다(`test-icons` 가 검사).
   ```
   43-i-icon.js  ITEM_ICONS 에 키 12 추가(값은 T66 의 새 문법대로 — 이모지든 점선 칸이든):
     crude_axe crude_pick crude_blade axe pickaxe sword carrier fish fish_cooked brine twig pebble
   43-i-icon.js  ICON_NO_RENDER 에서 제거: salt · item_workbench · item_drying_rack   ← 셋 다 PNG 가 이미 있다
   43-i-icon.js  ITEM_ICONS 에 item_salt_kiln 추가(T67 — 지금 키가 없어 기본 📦)
   45-t-market.js EQUIP_ICONS 에 carrier 추가(지금 기본 🎒)
   ```
   ⚠**404 는 안 난다** — 파일을 더했을 뿐 요청 키를 안 늘렸다. 배선이 오면 `test-icons ⑤` 의 '회부 중'이 0 이 된다.
2. **[R1 지형렌더 / C 채집자연물 — 한 줄]** `nature_render.py:333`·`char_render.py:801` 의 `cam_d.sensor_fit = 'HORIZONTAL'`.
   (0-가구 2번 유지 — 자연물 38종 중 15종이 세로가 길다.)
3. **[ART / T65 계보]** **지게 등짐 스프라이트** — 캐릭터 시트 레이어 홀드아웃 규약이라 ART 아이콘 카드가 만들 물건이 아니다.
4. **[ART / 설계]** **재료별 도구 아이콘.** 지금은 품목당 하나다 — `EQUIP_ICONS` 가 **타입당 하나**라
   화면이 이미 재료를 안 본다. 재료를 보이게 하려면 **그리는 쪽 규약부터** 정해야 한다(설계 판정).
5. **[ART / 설계]** **아이콘 상대 크기 규약이 없다.** `icons-postprocess.js` 가 전부 96px 를 꽉 채운다 —
   자갈 한 줌과 지게가 같은 크기다. 만들려면 키마다 배율 표 + **기존 36종 재굽기**. 별도 카드.
6. **[ART 다음 카드]** 보존식 6 · 갯벌 3+민물 · 어종 8(T73) → 작물 34+씨앗 34(`crop_render.py` 와 **같은 모델** · T74) ·
   옛 `item_*` 셋(계단·바닥·농지) 세계 스프라이트.

---
## 0-열. ★★[T59 식량 단위 통일 2026-09-03] 다른 영역에 넘기는 것 여섯

> 상세는 `보고/T59_2026-09-03.md`(옛/새 전수 표 · econ 어긋남 표) · 규약은 `인계/L-로트부패.md` 3-열.
> ★T17 회부 1건(**건어물 입력이 `'fish'` 라 낚은 물고기를 못 말린다**)은 이 배치로 **닫혔다**.

1. **[econ — ECON 수술 2] `FORAGE_FOOD_FACTOR` 를 열량으로 다시 세워라.** kcal 정본과 나란히 재면
   **버섯 19.6× · 새우 15.9× · 해조 8.5× · 채소 6.5× · 생선 5.5× · 굴 5.4×** 과대이고, **곡물만 정확히 1.00** 이다
   (그게 앵커라 그렇다). "유령 박멸" 배치가 계수를 보수적으로 ≤0.5 잡았는데 **방향은 맞았고 크기가 틀렸다.**
   ⇒ 이 카드는 안 고쳤다 — econ 무수정이 절대 규칙이고, 고치면 20마을 800일 **비트 동일 검증이 불가능해진다.**
2. **[econ — ECON 수술 2] 열량을 모르는 econ 전용 재화 다섯.** `acorn`·`chestnut`·`walnut`·`grape`·`cooked_food`
   는 플레이어 손에 오지 않아 이 카드의 사정거리 밖이다. 계수를 세울 때 kcal/kg 도 같이 적어야 한다.
3. **[F 제작 — 설계 판정] 요리 인스턴스의 영양.** `doEatDish` 는 `dish.attrs.nutrition × 신선도` 로 가고
   **`FOOD_EFFECTS` 를 안 읽는다**. 열량으로 세우려면 제작 층의 품질 축과 맞물려야 한다 ⇒ 여기서 안 건드렸다.
4. **[L 부패·보존] `pickled_veg` 를 남새 절임과 생선 절임이 함께 쓴다.** 산출 품목이 같아
   열량 유도가 **첫 갈래를 정본**으로 삼는다(결정론은 지켰다). 갈라야 하는지는 보존 층의 판단.
5. **[클라 — T55 뒤] 포만감 화면 표시.** 지금 클라에는 포만감 사본이 없다(무접촉 확인).
   보이려면 `welcome` 에 실어 보내는 길이고, 그건 T55(이름표 정본 배송) 문법 위에 서야 한다.
6. **[재민 실기 판정] 첫 30분의 결핍 리듬.** 곡식이 **7.14×** 오르고 잎채소·해조·절임이 **0.13~0.37×** 로 깎였다.
   베리 자체는 0.85× 로 거의 그대로지만 **상대적으로 크게 밀렸다** — 온보딩 첫 의뢰 산수가 바뀐다.
   ⚠손잡이는 **`BODY_DAY_KCAL` 하나**다. 품목별 값을 손대면 정본이 다시 갈라진다.

## 0-가구. ★[T67 가구·시설 렌더 2026-09-03] 다른 영역에 넘기는 것 여섯

> 전문은 `보고/T67_2026-09-03.md`. 만든 것의 규약은 `인계/ART-자산.md`.

1. **[I 인벤 / T66 세션4 — 3줄]** `43-i-icon.js` 를 **안 건드렸다**(카드 지시). 그런데 카드의 근거
   *"아이콘은 파일명 규약이라 코드 접점 0"* 은 **실측으로 틀렸다** — 그 파일엔 `ICON_NO_RENDER` 라는
   거부 목록과 `ITEM_ICONS` 라는 키 표가 있다. 그래서 **새로 구운 아이콘 셋이 화면에 못 닿는다**:
   ⓐ `ICON_NO_RENDER` 에서 `'item_workbench'`·`'item_drying_rack'` 을 **빼라**(이제 PNG 가 실재한다).
   ⓑ `ITEM_ICONS` 에 `item_salt_kiln: '🧂'` 한 줄을 **더해라**(지금 키가 아예 없어 기본 📦 로 뜬다.
      T66 이 이모지를 걷는 배치이므로 그 문법에 맞춰 넣으면 된다).
   나머지 다섯(벽·문·울타리·상자·모닥불)은 **이미 뜬다**(키가 있고 거부 목록에 없다).
2. **[R1 지형렌더 / C 채집자연물 — 한 줄]** `nature_render.py:333` 이 `cam_d.sensor_fit` 을 안 못박아
   `ortho_scale` 이 **긴 변**에 걸린다. 자연물 38종 중 **15종이 세로가 길다**(cattail03 163×232 = 1.42배,
   reed01 211×272, flower01~04 …) ⇒ 그만큼 **확대돼 잘려 구워졌을 수 있다**. `char_render.py:801` 도 같다.
   고치는 것은 `cam_d.sensor_fit = 'HORIZONTAL'` **한 줄**이고, 선례가 `bake-terrain-tex.py:256` 에 이미 있다.
   ⚠고치면 **그 15종을 다시 구워야** 하므로 그 영역 소유 세션의 판단이다(T67 은 실측만 하고 안 건드렸다).
3. **[F 제작건축 / 서버 — 표 하나]** `server/zone.js` 의 `BUILDING_HEIGHT` 는 **아무도 안 읽는다**
   (전수 grep 1건 = 선언뿐). T67 이 이 표를 **그림 높이의 정본**으로 삼아 하네스로 읽게 했다.
   ⇒ 서버가 이 값을 실제로 쓸 것인지(가림·시야·충돌), 아니면 지울 것인지 판정이 필요하다. 족보 (89) 의 자리다.
4. **[R2 개체렌더 — 죽은 사본]** `20-r2-visibility.js` 의 `_wallNC`/`_wallEC`(생성 벽 텍스처 스트립)는
   T67 이 벽을 스프라이트로 갈면서 **읽는 곳이 0** 이 됐다(`mkStrip` 두 번 호출은 남아 있다).
   무해하지만 사본이다 — 그 영역 세션이 지우거나, 다른 쓸 곳을 적어라.
5. **[ART 다음 카드]** 아직 세계 스프라이트가 없는 것: **계단·바닥·마을터·포위진지**(도형 그대로) ·
   아이콘 없는 품목 100+(도구·식량·보존식 → 작물 34 → 나머지 · 작물은 `crop_render.py` 와 **같은 모델**에서
   수확물을 뽑는다) · 지게 등짐 · 캐릭터 시트 앞뒤(T65).
6. **[R2 / 설계]** 세계 캔버스의 거래소 이름표가 아직 이모지다(`🏪`). 설계 §1-2 는 **UI 이모지 0** 인데
   T66 범위는 패널·`style.css` 라 캔버스 라벨이 어느 쪽인지 안 정해졌다. PM 판정.

---

## 0-유형2. ★[T50 사건 유형 2차 2026-09-02] 다른 영역에 넘기는 것 다섯

> 상세는 `회부_사건유형_다음층.md`. 만든 것의 규약은 `인계/E-사건장부.md` 4-유형2 절.
> ★T18 회부 1번(**"큰 사건" 유형이 없다**)은 이 배치로 **닫혔다** — 일곱 유형이 등재됐다.
> 남은 것은 §0 에서 **원천이 없다고 판명된 후보**들이고, 그건 곧 "세계에 아직 없는 일"의 목록이다.

1. **[E / B 신체 — 원천 없음] 마을 소멸 확정 사건.** 인구가 0 이 된 마을은 `scanDay` 가 건너뛴다
   (**사람이 없는 마을엔 소식이 없다** — 장부의 규약). 붕괴 **위기**(`POP_COLLAPSE`)는 내려가는 길에
   잡히지만 "그 마을이 끝내 비었다"는 **누가 관측하는가**가 안 풀린다.
   ⇒ 이웃 마을이 관측자가 되게 하려면 "마을 밖의 눈"이라는 새 개념이 필요하다 — 설계 판정.
2. **[X 전쟁 / econ — 원천 없음] 봉쇄(blockade).** `economy-sim.js processEvents` 가 `world.events`
   큐에서 봉쇄를 읽어 마을을 고립시키는데, **이 존에선 그 큐를 아무도 안 채운다**(랩 실측 0건).
   전쟁 층이 그 큐를 쓰기 시작하면 장부는 `v.isolated` 에지 한 줄로 받는다 — 지금은 **원천이 없어** 안 만들었다.
3. **[B 신체 — 원천 없음] 동사·부상 사망.** T43(쓰러짐)·T44(극단 HP) 착지 전이고, econ 인구는
   **죽는 이유를 구분하지 않는다**(`npcs.splice` 뿐). 사망 사건은 그 층이 선 뒤에.
4. **[E — 설계 판정 대기] 의뢰 유형 신설.** 새 일곱 중 **의뢰로 번역되는 것이 없다** —
   지금 의뢰는 "물건을 가져오면 물건으로 갚는다" 하나뿐이라, 약탈은 "순찰 동행", 완공은 "일손"으로
   **새 의뢰 유형**을 만들어야 번역된다. ⇒ 별도 카드.
   ★다만 **흉년·붕괴는 이미 이어져 있다** — 소출이 줄면 식량 재고가 마르고, 그 부족 래치가
   기존 게시판 의뢰를 스스로 건다. 새 배선 없이 사슬이 이미 닿아 있다는 사실은 확인했다.
5. **[T 거래 / econ] `world.tradeLog` 는 5000행에서 잘린다.** `FIRST_GOODS` 는 **오늘 날짜 행만**
   뒤에서 훑으므로 지금 규모(하루 수십 행)에선 무해하지만, 하루 5000행을 넘기는 세계가 오면
   조용히 몇 건을 놓친다. 잘림 상한은 econ 소유라 안 건드렸다.
---

## 0-온. ★[온보딩 v2 2026-09-01] 다른 영역에 넘기는 것 열

> 전문은 레포 `회부_온보딩_다음층.md`. 전부 **한 줄짜리**이거나 **PM 판단**이다.

1. **[E 사건장부 / T 거래]** 게시판 행에 「★첫 의뢰」 배지를 못 붙인다 — `villages.js` `villageBoard()` 가
   필드를 **명시적으로 골라** 다시 담아서 `events.js` 가 얹은 새 필드가 거기서 떨어진다.
   ⇒ 죽은 필드를 안 만들려고 **events.js 를 안 고쳤다**(지시서 §5 가 허용한 한 줄을 안 썼다).
   배지를 원하면 `villageBoard` 매핑에 한 줄. 지금은 `onboarding_quest` 메시지가 클라에 직접 간다.
2. **[V 마을배치]** `villages.js` `territoryBoundary()` 가 export 안 된다 — `onboarding.js` 가
   같은 4방 판정을 자기 안에 한 벌 갖고 있다(하네스 조립 갈래용). **export 한 줄이면 사본이 사라진다.**
3. **[Z 존서버 / 하네스 · PM]** `scripts/split-verify.sh` 는 이제 **`differ` 가 정상**이다.
   "새 기능 = 새 조각 + 등록 1줄"(분할 규약)과 "결합본이 분할 전과 바이트 동일"(그 검사기)은
   **구조적으로 양립 불가**다. 러너 밖이라 회귀 영향 0. 동결/고정/폐기 셋 중 하나를 정해야 한다.
4. **[Z 존서버 / 하네스 · 설계]** 「월드 입장」(마을 미선택)의 기본값 — 지금은 **문을 지난 사람만**
   도착 지점에 앉는다(`start_vid` 명시 접속만). 1차 실장은 미선택도 추천 마을로 보냈고 그게
   `e2e-cutaway`·`e2e-guest-reconnect`·`e2e-cold` 셋을 깼다(그 셋은 `mainSquare` 스폰을 도구로 쓴다).
   미선택도 어귀에 내려놓으려면 그 셋이 `teleport_debug` 로 옮겨야 한다.
5. **[Z 존서버]** ★**배경으로 미루면 안전하다는 게 틀렸다.** 도착 지점 산출(50마을 37초)을
   `setImmediate` 배경으로 돌렸더니 **79초 동안 CPU 를 거의 다 먹어** 존 틱이 굶었고,
   클라가 건물 스트림을 못 받아 `e2e-nature`(마을 0·경작지 0) · `e2e-mtocc`(가린 산 0장) ·
   `e2e-cold`(입장 실패)가 **렌더 결함처럼** 실패했다. 채택은 **부팅 때 `listen` 전에 굽기** —
   그 창엔 접속도 틱도 없다. 남은 판단: 부팅이 37초 길어져도 되는가(`회부_온보딩_다음층.md` B-7).
6. **[Z 존서버 / 하네스]** 로비(`#lobby`)는 `position:fixed` + 세로 가운데라 **스크롤이 없다** —
   카드가 뷰포트보다 길면 「월드 입장」이 화면 밖으로 나가 클릭이 30초 타임아웃으로 죽는다.
   지금은 `70-lobby.js` 가 런타임으로 `overflow-y:auto` 를 얹어 푼다. **style.css 가 옳은 자리다**(B-8).
7. **[Z 존서버 / 하네스]** 로비에 `<canvas>` 를 두면 안 된다 — 하네스들이 게임 화면을
   `page.$('canvas')` 로 잡아서 **로비 캔버스가 먼저 잡히고** 숨겨진 요소라 죽는다(`e2e-metallurgy`).
   시작 화면 지도는 그래서 SVG 다. 하네스가 `#canvas` 로 잡게 바꾸는 게 근본(B-9).
8. **[T10 · 하네스]** ★러너가 **부하에 흔들린다**(2코어). 리베이스 후 67종 판 실패 8 중 7이 이 배치와
   무관이었고, `e2e-emptystart`·`e2e-events` 는 단독 재실행이면 통과 · `e2e-move` 는 러너 단건이면 37/0
   (베이스도 같다) · `test-tick-slicer` 는 성능 하네스다. 재시도/여유·러너 밖·현행 유지 중 판단 필요.
9. **[T10 · 하네스]** `test-site-memo` 는 `test-tick-slicer` 가 먼저 돌아야 하는데 러너는 이름순이라
   **자동 발견 판에서 늘 실패**한다(베이스도 같다).
10. **[Z 존서버]** `package.json` 에 `acorn`·`playwright` 가 없다 — 새 클론에서
   `test-client-globals` 와 실클라 e2e 12종이 `MODULE_NOT_FOUND` 로 죽는다(`pngjs` 함정의 재판).
   ★컨테이너 chromium 은 **1194** 이고 그와 맞는 건 **`playwright@1.56.0`** 이다(실측 — 1.62 는 못 찾는다).
## 0-소체. ★[T13 NPC 소체 시트 2026-09-02] 다른 영역에 넘기는 것 다섯

1. **[온보딩 v2 / V]** ★**촌장 NPC 개체가 없다.** 촌장 말풍선은 마을 앵커(`villageAnchorPx`)가 내지
   지목된 주민이 아니다. 그래서 T13 의 직업 표에 **촌장 줄이 없다** — 표식을 붙일 대상이 없어서다.
   온보딩 v2 가 "마을 앵커 NPC"를 지정하면 `NPC_JOB_TOOL` 에 **한 줄**이면 된다(지팡이 소품은 그때 굽는다).
2. **[R2 / 러너]** `e2e-charsprite` **4건 실패가 물려 내려온다** — "[A] 화면에 남이 스프라이트로
   보인다 0명" 계열. **깨끗한 `origin/main`(214ef54e) 워크트리에서 수치까지 같은 31/4** 로 확인했다
   (T13 무관). 3-n 이 적어 둔 "B 의 텔레포트가 A 의 시야·틱·rAF 를 타고 오는 시간" 의 연장으로
   보이지만 **규명하지 않았다.** 그 하네스 소유 세션이 가져가라.
3. **[ART / R2]** 새 소품 넷은 이 컨테이너의 **bpy 5.0.1** 로 구웠고, 기존 20장을 구운 Blender 와
   판이 다르다(잡음 수준 차이 — 알파 최대차 0~3 · 실루엣 안 평균 0.32~1.17/255). 정렬·빛·메타는 같다.
   **언젠가 전체를 다시 굽는 날** 20장의 바이트가 한꺼번에 바뀐다 — 그날은 lock 을 통째로 갱신하고
   **그 사실을 보고서에 적어야** 한다(조용히 넘어가면 "몰래 바뀜"과 구별이 안 된다).
4. **[R2]** 남의 **착장이 네트워크에 없다**(3-n 회부 그대로) — 그래서 NPC 도 사람도 남은 전부
   기본 삼베다. 옷 확장(T27)이 이 회부를 같이 푼다.
5. **[V / 서버]** 화면 안 NPC 가 23~28명인데 **그려지는 것은 시야에 든 3~6명**뿐이다(가시성 컬링).
   행인 밀도·표시 거리를 서버에서 조절할 자리는 T13 범위 밖이다(렌더만 만졌다).

## 0-연표. ★[T18 연대기 2026-09-01] 다른 영역에 넘기는 것 넷

> 상세는 `회부_연대기_다음층.md`. 만든 것의 규약은 `인계/E-사건장부.md` 3-연표 절.

1. **[E 사건장부] ~~★"큰 사건"이라고 부를 유형이 아직 없다.~~ → ★[T50 2026-09-02] 닫힘**
   (일곱 유형 등재 + 무게 축 둘. `인계/E-사건장부.md` 4-유형2 절. 아래는 그때의 기록 그대로 둔다.)
   지시서는 "재고 바닥 같은 잔사건 vs **캐러밴 습격·풍작·소멸 위기** 같은 큰 사건"으로 갈라 달라고 했는데,
   장부의 6종에 **그런 유형이 없다**(부족·글럿·급등·급락·지연·계절뿐). 그래서 등급이 아니라
   **심각도 문턱**으로 갈랐다(`sev = |ln(관측÷기준)|` — 있는 축은 그것 하나다).
   ⇒ 연표가 지금 "값이 크게 움직인 해"로 읽히고 "무슨 일이 있었던 해"로는 안 읽힌다.
   습격·풍작·소멸 위기를 **사건 유형으로 추가**할지가 설계 판정 대기다(도적·전쟁·인구 층과 함께).
2. **[M 이동입력 / H HUD] ★`99-main.js` 의 키 체인이 Shift 를 안 가린다.**
   글자를 `k === 'l'` 로만 검사해 **`Shift+L` 이 그 분기를 같이 밟는다**(울타리를 짓는다).
   T18 은 연대기 단축키 자리를 열려고 **모닥불(`j`) 한 줄에만** `&& !e.shiftKey` 를 더했다 —
   **나머지 글자는 그대로다.** 전수로 가드를 다는 건 그 파일 소유 영역 몫이다.
   (§8.2 는 사이드바 버튼마다 단축키 병기를 요구하고 `e2e-ui ①` 이 그걸 검사한다 —
    즉 "단축키 없이 아이콘만" 은 선택지가 아니다.)
3. **[T0 클라분할 / 공통] `scripts/split-verify.sh` 는 상시 게이트가 될 수 없다.**
   §8.2 는 새 패널을 붙일 때 `50-i-panel.js` 를 **반드시** 건드리게 돼 있는데, 이 검사기는 등록된
   조각을 전부 이어 붙여 **분할 전 정본과 바이트 비교**한다 ⇒ 첫 기능 배치에서 영원히 differ 가 된다.
   T18 이 그 첫 배치였다. 이번에 둘을 넣었다: ⓐ 새 조각은 머리에 `// @@split-added` 를 달면 결합에서
   빠진다 ⓑ differ 메시지에 "분할 뒤 조각을 고쳤으면 정상"이라는 줄. **재-스코프는 그 영역 몫이다.**
4. **[E] 플레이어 행위의 연대기 편입** — "누가 다리를 놓았다"는 아직 사건이 아니다. 이력서 층과 함께.

## 0-소문. ★[T7 소문 전파 2026-09-01] 다른 영역에 넘기는 것 넷

> T7 은 **서버 전용 · 클라 무접촉**이라 화면 쪽을 못 고쳤다. 그리고 실측 중에 **econ 레코드 결함** 하나를
> 잡았는데 econ 무수정 조건이라 손대지 않았다. 상세는 `회부_소문전파_다음층.md`.

1. **[R2 개체렌더 / H HUD]** 게시판 응답에 **`news`(이 마을이 들은 소식)** 가 실려 나가는데
   **클라가 안 그린다**(지금은 `rows`=의뢰만 그린다). 그래서 촌장이 "그 밖에 n건은 게시판에" 라고 해도
   플레이어가 게시판에서 그걸 볼 수 없다. ⇒ `Shift+G` 알림에 `bd.news` 줄을 이어 붙이는 한 줄,
   또는 연대기 UI(T18). **데이터는 이미 간다**(`window.__evLastBoard.news`).
2. **[T 거래 / econ]** ★**econ 캐러밴 레코드가 재routing 때 자기모순이 된다.**
   `economy-sim-v2.js:949-955` 가 `c.distance` 는 새 목적지로 갈아치우면서 `c.travelDays` 는
   **원래 구간 값 그대로** 둔다(포기 귀환 갈래도 같다). ⇒ `arriveDay − departDay ≠ travelDays` 인
   레코드가 생기고, `tradeLog` 의 도착 행도 그 값을 그대로 찍는다.
   T7 의 시계 동기 계약 하네스(`test-events ⑲`)가 이걸 잡았다. **econ 무수정 조건이라 안 고쳤다.**
   고치려면 한 줄(`c.travelDays = extraDays`)이지만 **econ 3사본 규약**이 걸린다(번들 재생성 + 랩 재인라인).
3. **[E 사건장부 — 설계 판정 대기]** **소문 왜곡**(T34 — 전해지며 내용이 변한다)은 안 만들었다.
   지금 도달일은 **거리의 함수**이고 주사위가 0이다. 왜곡을 넣는 순간 그 규약이 깨지므로
   **재민 판정 뒤**에 한다. 같은 이유로 "행상이 늦어 소문도 늦는다"(사건 지연과 소문 지연의 결합)도 보류.
4. **[E / N] 플레이어별 목격 기록.** 지금은 **현재 위치 마을 기준**만이다 — 먼 마을에서 사건을 직접 보고
   집에 돌아오면, 집 마을이 아직 못 들은 그 소식을 플레이어도 못 본다(집 마을의 눈으로 보므로).
   "내가 본 것"을 플레이어 레코드에 남기는 층은 회부(온보딩·연대기와 같이 설계해야 한다).
5. **[T41 집터 / 러너]** ★`scripts/test-site-memo.js` 가 **5 통과 / 9 실패**다 —
   **깨끗한 `0c2ee0e1` 워크트리에서 짝 비교해도 같은 5/9** 라 T7 과 무관하다(내 배치 탓이 아님을 실측했다).
   그리고 이 하네스는 `test-tick-slicer` 가 만든 씨앗 DB를 전제로 하는데 러너는 **알파벳 순**이라
   `test-site-memo` 가 **먼저** 돈다 ⇒ 러너에선 "씨앗 DB 없음"으로 죽는다(하네스 사이 순서 의존).
   ⇒ 순서 의존을 없애거나(하네스가 스스로 씨앗을 만든다) 실패 9건을 규명해 달라.
## 0-임. ★[T14 임수·mtfoot 2026-09-01] 다른 영역에 넘기는 것 넷

1. **[온보딩 v2 / V]** ★**환호(어귀)는 시범 마을에만 있다** — `_ditch` 가 빈 마을이 대부분이다
   (`villages.js` 의 `ditch: 0 = 시범 마을 아님`). 지시서 §4 의 *"구역 산출은 어귀 링에서"* 를
   글자 그대로 쓰면 **그 마을들에선 빈터 구역이 안 나온다.** 대안: `territoryBoundary()` 가 내는
   **영토 경계 링**(마을당 200~400셀 · 전 마을이 갖는다). 그쪽이 A1 그리핑(중앙 셀 선점)도 같이 막는다.
2. **[R1 산 렌더]** `10-r1-terrain.js` 의 `MT_FIT_TOL = 2.0` 주석이 **"하한눌림 0%"** 라 적어 두었는데
   `_mtFitVy` 쪽 눌림은 **42%**(186장 중 79장)다. 주석이 `_mtFit`(가로 sc) 이야기일 가능성이 크지만
   확인하지 않았다 — 산 렌더를 다음에 만지는 세션이 가를 자리. **하한(`MT_VY_MIN`)은 안 건드렸다.**
3. ~~**[위생]** `test-client-globals` 가 `acorn` 을 require 하는데 `package.json` 에 없다~~
   **→ 해결됨.** 리베이스해 보니 병행 세션이 그 사이 `acorn ^8.18.0` 을 의존성에 넣었다.
   (내 클론이 낡아서 `MODULE_NOT_FOUND` 로 죽었던 것이다 — 회부할 것이 아니었다. 기록만 남긴다.)
   ⇒ 의존성에 넣을지는 러너 소유 세션(T10) 판단.
4. **[V / T1·T41]** `test-site-memo` · `test-tick-slicer` 는 `/tmp/*-seed-*.db` 씨앗 DB 를 요구해
   **새 컨테이너에선 못 돈다**(둘 다 groves·임수와 무관 — 파일에 그 낱말이 0회). 환경 문제이지 회귀가 아니다.
   ★**위 `0-소문` 5번이 같은 것을 더 자세히 적었다**(깨끗한 워크트리에서도 5/9 · 러너 알파벳 순서 의존).
   두 배치가 독립적으로 같은 자리를 짚었으니 그 항목으로 합쳐 읽어라.

## 0-염. ★[T3 자염 2026-09-01] 다른 영역에 넘기는 것 셋

> 자염 배치는 **클라 무접촉** 조건이라 화면 쪽 두 자리를 못 고쳤다. 전부 **한 줄짜리**다.
> 상세는 `회부_자염_다음층.md` D-1·D-2·G.

1. **[R2 개체렌더 / 인벤]** `brine`(짠물)·`item_salt_kiln`(소금가마)이 클라의 `ITEM_ICONS`·`ITEM_LABEL`
   표에 없어 제작창 비용 칸에 **영문 키 그대로** 뜬다. 404 는 안 난다(프리로드가 `ITEM_ICONS` 키만 훑는다).
   ⇒ `brine: '🫙'`/`'짠물'` · `item_salt_kiln: '🧂'`/`'소금가마'` + `ICON_NO_RENDER` 에 두 키.
2. **[F 제작건축]** 클라의 보존 행이 `· 보관 ${shelfDays}일` 을 **무조건** 찍는다. 자염이 그 갈래를
   빌려 타는데 **소금은 안 썩는다** ⇒ 지금 `'∞'` 를 넣어 "보관 ∞일" 로 읽히게 해 뒀다.
   ⇒ `shelfDays != null` 가드 한 줄이면 칸이 지워진다.
3. **[B 신체서버]** ★**바닷물을 마시면 갈증이 회복된다.** `tryGather` 가 바다·강을 안 가린다.
   이 배치가 만든 결함은 아니지만 **바닷가를 목적지로 만든 건 이 배치**라 이제 사람이 그 자리에 선다.
   갈래 ⓐ 거절 ⓑ 갈증이 **더** 심해진다(고증·일관성 원칙에 가장 맞다) ⓒ 그대로 — **설계 판단**이라 안 건드렸다.

---

## T17. ★★2026-09-03 ECON 수술 1 — **승인 뒤 남은 것** (`인계/T-거래.md` `## T17.`)

기준선은 정본이 됐다(`인계/공통.md` · PM 승인 2026-09-03). 남은 판단거리·넘긴 것:

1. **[Z/N] `travelDays` 를 미는 자리가 존 서버에 셋 더 있다** — `server/villages.js` 의 로컬 우회 재경로
   (1799행 언저리)·도착 임박 가드·고립 귀환. econ 파일 밖이라 **3사본 규약 없이** 고칠 수 있다.
   T17 은 econ 두 자리만 맞췄다.
2. **[T/econ] `travelDaysForDistance` 가 export 되지 않는다** — `server/rumor.js` 가 정본을 못 불러
   "한 줄 거울 + 교차 계약 검사"(`test-events ⑲`)를 택했다. export 한 줄에도 3사본 규약이 걸린다.
3. **[Z] 바다 사각형 유도가 두 곳** — `server/zone.js:470`(해안선 생성기)과 `server/villages.js`(T17).
   합치려면 플레이어 층을 만져야 한다.
4. **[V/econ] `woodSustain`·`forageSustain`·`marginalQ` 는 여전히 사장** — T17 은 `fishSustain` 만
   **기본 꺼짐** 스위치(`T17_MSY=1`)로 이었다. 켜면 인구 −48~−61% · 소멸 0 → 14·16곳
   (`보고/T17_2026-09-02.md` §5) ⇒ **재민 판정 B: 안 켠다. 상한 눈금 재정박이 선행.**
5. **[C/낚시] 낚시 v2 는 단방향이다** — 플레이어가 긁으면 `land.fishSustain` 이 내려가는데
   **NPC 어부는 물을 안 줄인다**(`drawStock` 호출자가 `server/zone.js` 한 곳뿐 — 전수 확인).
   MSY 를 켜기 전에 판단할 자리다.
6. **[E/장부] 사건 밀도가 캐논 하한 아래** — 1.92~1.96일/건(캐논 2~3일). 옛도 이미 2.10 이고
   (상류 T50 이 유형을 늘렸다) T17 의 몫은 −0.18일. 문턱은 `EV_*` 손잡이 소관.
7. **[V/econ] 인구 −4~7%(시드 7·42)의 확장셀 귀속** — **재민 판정 C: 수용, 후속 실측으로.**
   소금·보존식이 곳간 잉여를 흡수해 확장 투자 여력이 준 것으로 보인다(가설 — 안 쟀다).

★**건어물 입력 `'fish'` 결함**(낚시 v2 가 어종 id 를 주고 `'fish'` 를 내는 분기가 없다)은
**T59(세션3)** 에 동봉됐다 — 이 카드 밖에서 닫힌다.

---

## T42. ★2026-09-01 교역로 — **재민 판정 대기 둘** (`인계/Z-존서버.md` `## T42.`)

1. **조각 최대가 아직 181~272ms** (예산 16ms 의 ×11~17) — 새 바닥은 `terr`(영토 확장)와
   `1마을 life`(그 중 `_lifeHeadlessDay` 232ms)다. 둘 다 **이미 마을 단위 조각**이라
   슬라이서로는 더 못 자른다. 다음은 그 **안**을 째는 배치이고, 그건 생활층·영토 층의 수술이다.
2. **선계산이 사람 있을 때 멈춘다** — 부팅 직후 무인 구간에 579쌍을 데운다(실측 완주 116초).
   사람이 계속 붙어 있으면 안 데워지고, 그때는 경계에서 판다(`e2e-rtt` 조각 최대 `caravan` 369~768ms).
   대안(느리게라도 데운다 / 접속 전 부팅 단계에서 데운다)은 각각 평시 스파이크 / 부팅 시간(N.7 14분)을
   건드린다 — 판단 필요.
3. ★★**한 걸음(A\*)이 최대 2.4초라, 유예·간격으로 줄일 수 있는 건 빈도뿐이다** [T42-b 2026-09-01].
   1차 선계산이 라이브 결함이었다 — 무인 프레임에 A\* 를 쉼 없이 넣었더니 루프가 70초 동안 100% 막혔고,
   나간 사람의 저장이 소켓 밖으로 못 나가 복귀 브리핑이 "부재 0일"이 됐다(`e2e-rumor ⑦` 이 잡음).
   유예 30초 + 간격 250ms + **날아가는 쓰기·처리 중인 접속 술어**(`ioBusy`)로 **빈도**는 고쳤다.
   그래도 데우는 동안 `/health` 응답 간격 최대가 **2,687ms** 다 — 걸음 하나만큼은 계속 막힌다.
   ⇒ 그걸 없애려면 A\* 를 **중간에 놓을 수** 있어야 한다: 재개 가능 A\*(`sim/path-core.js` 수술 —
     랩·서버 공용 정본이라 이 세션이 못 건드린다) 또는 **워커 스레드**. 둘 다 판단 필요.
   ⚠그때까지는 **선계산이 도는 3분 동안 접속·저장이 최대 2.4초씩 밀릴 수 있다**(무인일 때만).
   ★★**[T85 2026-09-03] 닫혔다 — 재개 가능 A\* 채택 · 워커 기각.** 한 걸음이 조각으로 갈렸다:
     최악 막힘 2,393ms → **31ms**. 위 `0-길` · `인계/Z-존서버.md ## T42-c` 참조.

★그리고 `KNOWN_ISSUES` **N.9 의 내역 두 줄이 로그 오독**이었다(스폰 수·저장큐 길이). 고쳐 적었다.

★★**영역 T7(소문·복귀 브리핑)에 넘기는 관측 한 줄** — `e2e-rumor` 가 **러너 연속 실행에서 흔들린다.**
같은 명령(`run-regress.sh e2e-rtt e2e-rumor`)을 `origin/main`(`1f42f574`)에서 돌리면 **29/6 실패**한다
(⑥ "촌장이 하는 말이 게시판 안에" · ⑦c "접근만으로 말을 건다" · 그 뒤 ⑦ 줄들). 단독 실행은 35/0 이다.
흔들리는 자리는 **브리핑이 두 번 나가는 창**으로 보인다 — 실측 로그: 재접속 뒤 첫 브리핑이
`since=384`(returned:true)로 나가고 **곧바로** 둘째가 `since=null`(returned:false)로 나가,
하네스의 700ms 폴링이 둘째만 잡는 판이 생긴다. 진입점은 `tryVillageBrief` 하나이고 트리거는 클라다.
※T42 의 선계산이 이 하네스를 깨뜨린 건 **별개**이고 고쳤다(`인계/Z-존서버.md ## T42.`) —
  그건 단독 실행에서도 100% 재현됐다.

★★**영역 V/F(마을 건립)에 넘기는 관측 한 줄** — `e2e-village` 의 *"거부 사유가 한국어 계약
메시지다 — (없음)"* 가 **`origin/main`(`92515472`)에서도 떨어진다**(같은 러너 명령 · 31/1 · 같은 문장).
T42 리베이스 **전** 나무에서는 32/0 이었다. 상류(T48 집 의뢰 수리)가 거부 경로의 문구를 바꾼 자리로 보인다.

★★**영역 R2/N 에 넘기는 관측 한 줄** — `e2e-charsprite ⑤`(두 클라 짝)가 **`origin/main` 에서도**
4건 실패한다: *"[A] 화면에 남이 스프라이트로 보인다 0명"* · 남의 idle/walk/idle 3건.
`1f42f574` 를 그대로 체크아웃해 단독 실행한 결과이므로 **부하 flake 가 아니라 그 커밋의 상태**다
(종전 기록은 34/0 · `인계/R2-개체렌더.md`). T42 는 손대지 않았다 — 소유 영역이 판단할 일.

---

## T41. ★2026-09-01 조각의 바닥 — **재민 판정 대기 셋** (`회부_T41_조각바닥_다음층.md`)

1. ~~**캐러밴 콜드 A* (1,265~1,695ms — 새 바닥)**~~ **★[T85 2026-09-03] 닫혔다.** (아래는 당시 기록.)
   **캐러밴 콜드 A* (1,265~1,695ms — 새 바닥)** — 재개 가능 A* 는 `sim/path-core.js`(랩 공유 정본)
   수술 + 캐러밴 스폰 지연(행동 변경)이라 **안 했다**. 더 싼 길 둘: ⓐ 경로 DB 영속(결과 동일 · 새 저장 층)
   ⓑ 부팅 직후 유휴 프레임 선계산(총량은 그대로, 조각을 옮김). §C
   ⇒ T42 가 ⓐⓑ 를 했고, **T85 가 수술을 했다**: 답은 비트 동일(586쌍 · 3시드 diff 0)이고,
     "캐러밴 스폰 지연"은 **하루를 안 넘긴다**(같은 하루의 다음 조각 · `travelDays` 무변). 조각 최대 **31ms**.
2. **집터가 구조적으로 없는 마을 20곳** — 12일간 성공 0. 원인은 집 간격 `HG=18`.
   인구가 수용력 0.92 를 넘어도 집이 안 는다. 그대로 둘지(ⓐ) · `HG` 를 밀도로 낮출지(ⓑ) ·
   분촌으로 풀지(ⓒ)는 **마을 설계 판단**이라 성능 배치가 손대지 않았다. §F-2
3. **안전망 주기 7일** — 표지를 놓쳐도 7일 안에 회복(비용 1/7). 최악은 집이 7게임일(실시간 2.8시간)
   늦게 서는 것. 짧게 하면 안전하고 비싸다. §F-1

★그리고 **다음 바닥은 매일 도는 것**이다: 생활층 헤드리스 결산 226ms/마을 · 곳간 증설 70ms/마을.
  집터·A*·광맥은 부팅 워밍업이지만 이 둘은 하루도 안 쉰다.

---

## T1. ★2026-09-01 일틱 조각내기 — **재민 판정 대기 셋** (`회부_T1_일틱쪼개기_다음층.md`)

1. **`_lifeAddHouseSite` 수술** — 일틱의 **56%**(1,127ms/2,018ms). 자리를 못 찾은 마을이
   매일 영토 전수를 2패스로 다시 훑는다. 길 셋(실패 기억 / 후보 캐시 / 경계만 훑기) 중
   ★ⓐ 실패 기억이 답으로 보이나 "언제 다시 훑는가"가 곧 마을 성장 속도라 **재민 판정**이다.
   ⇒ **이걸 안 하면 T1 의 목표(≤×2)는 못 닿는다.** 그리고 일틱은 세계가 늙을수록 자란다.
2. **교역로 A* 영속/선계산** — 최대 조각 436ms 의 정체. 쌍당 1회지만 재기동마다 1,225쌍을 다시 덥힌다.
   행동 변경 0 이 증명 가능한 드문 항목(순수 캐시). 다만 새 저장 층이다.
3. **조각내기가 남긴 0.1%/일 차이를 "같다"로 볼 것인가** (회부 §A-5) — NPC 노동이 단계 사이로
   끼어들어 생긴다. 없애려면 마감 동안 생활층까지 멈춰야 하고 그건 행동 변경이다.
4. **`e2e-rtt` 의 목표선을 실패로 셀 것인가** — 목표 ≤×2 미달을 pass/fail 에 넣지 않았다(회부 §B-1).
   러너를 초록으로 둔 건 **내 판단**이다. 빨갛게 두길 원하면 목표선 블록을 `ok(...)` 한 줄로 바꾸면 된다.

---

## 4. 작업 배치 — 재민 결정 대기 항목이 많다

**★먼저 재민의 회부 답을 확인해라.** 답이 있으면 그것부터. 없으면 아래를 순서대로:

### A. 회부와 무관하게 지금 해도 되는 것

1. **`era.canTame` 배선** — 지금 `breeding:true` 13종이 시대와 무관하게 먹이 3번이면 길들여진다
   (`era.canTame` 호출부 0곳). `tryFeed` 에 한 줄. era.js "송국리기엔 기마 없음" 의도가 코드로는
   안 지켜지고 있다. 하네스도 같이(`test-tame.js` 소품).
2. **노 조업 시간** — 지금은 클릭 1회 = 즉시 1회 제련이다. 움집처럼 진척도(시간)를 두는 게
   계약상 자연스러운지 검토. `설계_플레이어_NPC_집건설_해석안.md` 의 진척 공식 재사용.
3. **숯가마 조업 배치** — 지금 1회 3통나무. 여러 번 클릭이 지루하면 "가득 채우기" 한 번에.
4. **`iron` 라벨 정합** — `specialty.RESOURCES.iron.ko = '철광석'` 인데 econ 의 `iron` 은 **제련된 금속**이다.
   NPC 시장 UI 가 금속을 "철광석"이라 부른다. 바꾸면 econ 표시만 바뀌는지 확인 후 수정.
5. **플레이어 야금 사슬 실플레이 검증** — 로컬 풀스택(Playwright 실클라)으로
   채광→선광→숯가마→노→단조까지 **사람이 하듯** 한 번 통과시켜라. 서버 함수 E2E 는 됐지만
   클라 UI(건축 메뉴·2×2 고스트·클릭 판정·인벤 표시)는 아직 실화면 검증이 없다.

### B. 회부 답이 오면

6. 광산촌 특화 슬롯(`회부_구리부존과_원석적체` ①) — 시딩에 슬롯 예약
7. 구리 파생 투입수요(②) — **채택하면 청동 희소성 설계가 바뀐다.** 3시드 A/B 필수
8. 원석 교역 금지(③ 나) — 추천안. 상태 증가 0
9. DB 리셋/새 존(`회부_시딩변경과_DB리셋`)
10. 배합↔교역 통합 · 철검 위세축 · 말 사역

### C. 계측·위생 (상시)

11. 랩 2종 재인라인 + 스모크 + 디바이스 동기 — 엔진 건드리면 매번
12. `보고_*.md` 종합 보고 작성·푸시

## 5. 회부만 할 것 (구현 금지)

강제소개/이주 계열 전부 · 배합↔교역 통합 구현 · 아이템 위세축 구현 · **DB 리셋/존 id 변경** ·
~~NPC 철기 산업 콘텐츠(주요 광맥에 철 넣기·새 마을)~~ **[2026-08-03c 재민 확정 — 길이 닫혔다.
철기는 플레이어 마을 건립으로 자연해소. 설계_플레이어_마을건립.md 참조]** ·
**플레이어 마을 건립 구현**(설계안만 존재 — 결정표가 채워지기 전엔 구현 금지) ·
산 부수기 시스템 · 자잘 광맥 미니맵 기록 ·
**시대 SCHEDULE 실제 설정**.

★배치별 회부 문서 — **구현 전에 먼저 읽어라**:
`회부_사건레이어_다음층.md` · `회부_MSY상한_사장.md` · `회부_낚시_다음층.md` ·
`회부_신체UI_다음층.md` · `회부_거래소_다음층.md` · **`회부_무게_다음층.md`**(2026-08-27 신규).

무게 회부(재민 명시) — **NPC 캐러밴 무게 모델(M · 랩 A/B 선행)** · 지게(용량 확장구) ·
부패 곡선·상함·보존 가공 · **플레이어 간 거래 제안·수락(P — T/Y 를 대신할 것)** ·
곳간 인출·마을 소속 · 아이템 상세 표(§8.1 전체) · MSY 상한 A/B · 낚시·신체·거래소 손잡이.
무게 회부(실측으로 새로 나옴) — **B-1 시작 지급(33.6kg)이 기본 용량(25kg)을 넘는다** ·
B-2 낚시 한나절 115kg(용량의 462%) · **B-3 표에 없어 내가 유도한 kg 7종(확인 바람)** ·
B-4 거의 모든 "한 짐"이 용량을 조금씩 넘는다 · B-5 로트 상한에서 한 번은 나이를 뭉갠다 ·
B-6 인벤 정수 vs 소수 몫 표시 · B-7 개체 환산을 게시판에도 걸었다.

거래소 회부(재민 명시) — 곳간 인출 · 마을 소속/멤버십 · 거래소 **실체 건물** · 흥정/평판 할인 ·
원거리 위탁·캐러밴 동승 · 시세 기록/차트 UI · MSY 상한 A/B · 낚시·신체 손잡이 튜닝(재민 실기 대기).
거래소 회부(실측으로 새로 나옴) — **B-1 장마당 고증**(기능은 재분배인데 이름만 근대적) ·
**B-2 곡식이 교환 품목이 아니다**(기준 품목인데 손에 못 쥔다) · **B-3 가격 바닥에 붙은 품목** ·
B-4 하루 캐시 vs 지금 시세 비대칭 · B-5 작은 마을에선 거래소가 사실상 빈다 ·
**B-6 T/Y `trade_offer` 가 경제를 통째로 우회한다**(나무1↔돌1 · 상대 동의 없음 · 이전부터 있던 것) ·
B-7 게시판 부등식은 게시 시점 기준.

---

## ★[0번 분할 배치 2026-09-01] 새로 연 회부

### `public/client.js` 분할 — 전제가 깨져 중단 → `회부_클라분할_다음층.md`
파일이 평평하지 않고 **IIFE 하나**(L56–L15245 · 15,190줄)라 함수 경계에서 잘라도
조각들이 서로 다른 클로저가 된다. 선택지 넷(껍데기 제거 후 분할 / 결합 서빙 / ES 모듈 / 유지)과
실측(다른 스크립트·브라우저 전역과 **이름 충돌 0** · 이사할 실행문 **26개**)을 그 문서에.
★판단이 나기 전까지 **클라 새 코드는 `client.js` 에 계속 붙인다**(임시 분할 금지 — 두 정본 금지).

### 러너 밖 하네스 31개 — 편입 여부는 영역 소유 세션 몫
디스크의 `test-*/e2e-*.js` 80개 중 49개만 러너에 있었다. 순수 글로브였다면 31개가 더 잡힌다.
지시서대로 **임의로 넣지 않았다.** 목록은 `회부_클라분할_다음층.md` D.
편입은 그 파일 머리에 `// @regress` 한 줄이면 끝(러너는 안 건드린다).

### 인계 절 번호가 겹쳐 있었다 — 관측 보고
원문에 `3-n`·`3-o`·`3-p`·`3-q`·`3-r`·`3-s` 가 **각각 두 번씩** 쓰였다(병행 세션들이 같은 날
같은 접미를 골랐다). 이번 이사는 제목 전문으로 갈라 배정했고 **문장은 안 고쳤다**.
⇒ 영역 파일로 갈린 지금은 같은 번호가 서로 다른 파일에 있어 충돌이 사라진다.

---

## 부하 실측. ★2026-09-01 부하 실측 배치 — **재민 판정 대기 넷** (`BENCHMARK.md` · `KNOWN_ISSUES.md` N.0~N.10)

측정 배치라 **게임 로직·밸런스·기능을 하나도 안 고쳤다.** 발견한 것은 전부 기록만 했다.

1. ★**N.0 — 수리가 이미 레포에 있는데 꺼져 있다.** 배포된 이미지에 있는 env 두 줄
   (`TERRAIN_SEG_INDEX=1` · `TERRAIN_TILE_CACHE=1`)만 켜면 같은 세계에서
   `tick_avg` **454~888ms → 15~28ms**, 부팅 **13분 51초 → 7분 10초**. 코드 변경 0.
   ⇒ 판단할 것은 "고칠까"가 아니라 **"켤까"** 다. 배포는 재민 몫.

2. ★**N.6 — 존을 넘으면 도구가 영구히 사라진다.** 28회 왕복 전부 재현. 스칼라 인벤은 온전한데
   `toolItems`(내구도 포함)·kg 원장이 없어지고, 같은 신원으로 재접속해도 안 돌아온다.
   원인 자리 둘 — `fireHandoff` 페이로드(`zone.js:9218~9243`)에 필드가 없고,
   도착 경로(`:2771~2804`)가 `pending` 만 읽고 central 행을 안 읽는다.
   같은 페이로드에 `equipment`·`craftSkill`·로트·신체 상태도 없다(**코드로만 확인 · 실측 안 함**).
   갈래 ⓐ 페이로드에 빠진 필드를 채운다(빠르지만 새 필드가 또 빠진다)
        ⓑ 도착 경로가 계정 행을 읽는다("몸의 정본이 어디인가"를 한 곳으로 — 저장 지연·경합을 봐야 한다).
   → `Z-존서버.md` 영역. 재현: `bash scripts/bench-2zone.sh start && node scripts/probe-handoff-tools.js`

3. ★**N.9 — 게임일 경계에서 틱 하나가 33초.** 플래그를 켜 평시를 15~28ms 로 내려도 하루 한 번 남는다.
   그 경계에서 한 틱에 몰리는 것: 캐러밴 16대의 A\*(44~359 정점)·NPC 1,560 스폰·저장큐 50행·사건 6건.
   GC 는 아니다(32분 정지 1,520회 중 200ms 초과 4회, 최대 598ms = 문제 틱의 1/56).
   갈래 ⓐ 게임일 작업을 여러 틱에 나눈다 ⓑ 틱 밖으로 뺀다. 둘 다 **econ 결정론**을 먼저 봐야 한다.
   ⇒ T1 「일틱 조각내기」·T41 「캐러밴 콜드 A\*」와 **같은 자리**다. 세 회부를 한 배치로 묶는 게 맞을 수 있다.

4. **N.7 — 존 재기동에 14분**(플래그 끔). 그동안 플레이어가 못 들어오고 헬스체크는 300초에 포기한다.
   실제로 오늘 컨테이너가 `unhealthy` 로 12분을 더 떠 있었다. N.0 을 켜면 7분으로 준다(근본 해결은 아니다).

기록만 한 것(판정 불필요): N.2 몹 449 잔존 · N.3 welcome 2.2MB · N.5 `/metrics` 에 tick 없음 ·
N.8 `ZONE_TEST_INV` 가 central 살아 있으면 무력화 · N.10 `canadia` 전용 타이머 둘(존 값 잴 때 표본 금지).

---

## ★[T4 바람 노출 2026-09-01] 다른 영역에 넘기는 것

전문은 `회부_바람노출_다음층.md`. 여기엔 **다른 영역 소유**인 것만 한 줄씩 올린다.

* **클라(T38 잔무)** — HUD 바람 힌트. 서버가 `weather` 페이로드에 `wind`(계절 세기 −1..1)·`exp`(노출 0..1)를
  **이미 숫자로** 보낸다. 배지 툴팁 한 줄이면 끝이고, 클라가 지형·계절 산수를 하면 안 되는 규약은 달력·온도와 같다.
* **산 아크** — 걸을 수 있는 고지. `passes` 가 한반도에 **0개**이고 바위 셀이 통행 불가라 `elevKmAt` 이
  여전히 0 만 낸다. 이번 배치는 **우회**했지 고치지 않았다. 고개가 생기면 `zone.js elevKmAt` 하나만 고치면 된다.
* **C 영역(채집·자염)** — 짠물을 끓여 민물 얻기. 바닷물을 못 마시게 만들었으니 해안 생활의 물 조달이
  판단거리가 됐다. 자염 도구(`vessel`·솥)가 이미 있어 새 도구 없이 열린다.
* **T27(옷)** — 식물 섬유 방한 천장(26)을 두면서 **겹쳐 입기**가 자연스러운 다음 수가 됐다(슬롯은 지금 하나).
* ⚠**T0-b(클라 분할) 소유 · 새 클론이 지금 깨진다** — `test-client-globals.js` 가 `acorn` 을 require 하는데
  `package.json` 에 없다. 러너가 "결과줄 0개 = 실패"로 옳게 센다. `npm install acorn --no-save` 를 넣고
  그대로 돌리면 **12/0 통과** — 코드는 멀쩡하고 **의존성 선언만** 빠졌다(러너 문서가 경고한 pngjs 사고와 같은 부류).
  * ✅**해소됨 [T10 2026-09-01 확인]** — `package.json` 의 `dependencies` 에 `"acorn": "^8.18.0"` 이 이미 있다.
    커밋 `28788e28`(T7 · 같은 날)이 넣었다. 설치본도 8.18.0 으로 선언 범위 안이고, 통합 러너 전수에서
    `test-client-globals` 는 **12/0 통과**했다. ⇒ 이 회부는 닫아도 된다(T0-b 영역 소유자로서 확인).
## T9. ★2026-09-01 러너 밖 하네스 31개 전수표 — **판정은 PM**

지시대로 **등록도 폐기도 하지 않았다.** 한 번씩 돌려 표만 만든다.
방법은 러너와 같은 규약(순차 단독 · 하네스 사이 서버 정리 · 종료코드는 파이프 앞에서) — `scripts/t9-sweep.sh`.

```
  결과   통과 27 · 실행불가 3 · 실패 1        총 실행 시간 167초(2.8분)
```

★**내 자가 먼저 틀렸다(족보 57).** 1차 집계는 "실행불가 19"였다. 러너의 요약 문법
(`통과` / `PASS <숫자>`)을 그대로 썼는데, 러너 **밖** 하네스는 그 규약을 따를 이유가 없어
`결과: PASS` 로 끝난다. 그 12개는 잘 돌고 있었다. 검사 표시(✓/✔/✗)를 1차 근거로 바꿔 다시 쟀다.
⇒ **러너 밖을 러너의 자로 재면 안 된다.**

### 표 (이름 · 결과 · 시간 · 마지막 커밋 · 무엇을 보나 · 중복 · 추천)

| 하네스 | 결과 | 시간 | 마지막 커밋 | 무엇을 보나 | 중복 | 추천 |
|---|---|---|---|---|---|---|
| test-alloy | 통과 | 0s | 2026-08-01 `481e02f` | 합금·주조·광종 배분 | 미확인 | 등록 |
| test-armor-cap | 통과 | 0s | 2026-08-01 `ba06fc2` | 모시 수요-캡 규약 | 미확인 | 등록 |
| test-assay | 통과 | 1s | 2026-08-02 `5cc6fff` | 감정 몬테카를로 | 미확인 | 등록 |
| test-bridge-path | 통과 | **71s** | 2026-07-28 `e6548c2` | 다리로 강 단절 해소(어촌1) | 없음(다리 하네스 유일) | 등록(느림 — 아래) |
| test-bridge-sprite | 통과 | 1s | 2026-09-01 `5c0aafc` | 다리 스프라이트 2칸 전방 판정 | 없음 | 등록 |
| test-building-anchor | 통과 | 0s | 2026-09-01 `5c0aafc` | 재렌더 후 앵커표 갱신 누락 | 없음 | 등록 |
| test-distmatrix | 통과 | **56s** | 2026-08-02 `79b66c9` | 교역 거리행렬 증분 | 없음 | 등록(느림 — 아래) |
| test-ditch | 통과 | 0s | 2026-09-01 `5c0aafc` | 환호(도랑) 규모·콜라이더 | 없음 | 등록 |
| test-frontier-iron | 통과 | 1s | 2026-08-02 `3785b3c` | 철 채광 주체 | 미확인 | 등록 |
| test-furnace | 통과 | 1s | 2026-08-04 `f74e421` | 노·숯가마 | 미확인(`e2e-metallurgy` 인접) | 등록 |
| test-granary-add | 통과 | 0s | 2026-07-27 `a9cb5f3` | 곳간 증설 런타임 | 미확인 | 등록 |
| test-granary-haul | 통과 | 0s | 2026-07-28 `6fc20c1` | 곳간② 서버 이식 | 미확인 | 등록 |
| test-guild-treasury | 통과 | 0s | 2026-07-28 `cb1c2a2` | 길드 곳간 델타 회계 | 없음 | 등록 |
| test-hall-door-path | 통과 | 0s | 2026-07-28 `31bad65` | 회관 문 경로 계약 | 없음(`server/pathfind` 유일) | 등록 |
| test-headless-parity | 통과 | 1s | 2026-07-28 `a5a4917` | 헤드리스↔본게임 파생값 일치 | 없음 | 등록 |
| test-mining | 통과 | 16s | 2026-08-02 `5cc6fff` | 11차 채광 재설계 이식 | 미확인 | 등록 |
| test-oremix | 통과 | 0s | 2026-08-01 `b6a5401` | 유효 제련 조성 | 미확인 | 등록 |
| test-picker-tables | 통과 | 0s | 2026-08-01 `08071de` | picker 가치표 드리프트 | 없음 | 등록 |
| test-reanchor | 통과 | 0s | 2026-07-27 `3e5fcaa` | 재앵커 센티널 충돌 | 없음 | 등록 |
| test-relic | 통과 | 0s | 2026-08-02 `a0a6cbc` | 철제 위세품 | 미확인 | 등록 |
| test-rock-los | 통과 | 15s | 2026-09-01 `5c0aafc` | 바위 시야 차단 정합·비용 | 없음 | 등록 |
| test-rooms | 통과 | 0s | 2026-08-05 `dda653d` | 방 판정 전수(`server/rooms`) | ★`e2e-rooms`(등록)와 **같은 주제·다른 층** | 등록(층이 다르다) |
| test-tame | 통과 | 1s | 2026-08-02 `5cc6fff` | 길들이기 시대 게이트 | 없음 | 등록 |
| test-terr-backfill | 통과 | 1s | 2026-07-28 `f930183` | 영토 백필 | 미확인 | 등록 |
| test-valuechain | 통과 | 0s | 2026-08-02 `6945688` | 가치 사슬 항등식 | 미확인 | 등록 |
| test-village-found | 통과 | 1s | 2026-08-03 `7372c6e` | 마을 건립·식량이 사람을 부른다 | ★`e2e-village`(등록)와 주제 인접 | 등록(층이 다르다) |
| test-villager-stuck | 통과 | 0s | 2026-07-28 `5ffcd33` | 기타직 출근 정체 가드 | 미확인 | 등록 |
| test-lab-market | **실행불가** | 0s | 2026-07-28 `bee69ff` | 랩 장마당(계절 장) | — | **러너 밖 유지** |
| test-lab-mining | **실행불가** | 1s | 2026-07-30 `a05074d` | 랩 채광 | — | **러너 밖 유지** |
| test-lab-psite | **실행불가** | 0s | 2026-07-28 `bee69ff` | 랩 플레이어 의뢰 집 건설 | — | **러너 밖 유지** |
| test-psite-server | **실패** | 0s | 2026-09-01 `5c0aafc` | 의뢰 집 건설(서버판) 계약 | — | **보류(먼저 고쳐야)** |

### 셋만 따로

**① `test-lab-*` 3개는 고장이 아니다 — 레포 밖 파일을 본다.**
셋 다 `~/Mini/전쟁실험실.html`(랩 HTML)을 연다. 이 컨테이너엔 없어서 `ERR_FILE_NOT_FOUND` 로 죽지만
**재민 맥에는 있다**(1.27MB · 2026-08-29 확인). `LAB_FILE` 환경변수를 존중하도록 이미 짜여 있고
파일 머리에 "랩은 레포 밖(디바이스)에 산다"고 적혀 있다.
⇒ 고칠 것 없음. 다만 **컨테이너·CI 에서는 영원히 못 돈다** ⇒ 러너에 넣으면 러너가 항상 빨개진다.
**추천: 러너 밖 유지.** (넣으려면 랩 HTML 을 레포로 들이거나 파일 없을 때 skip 하는 규약이 먼저다 — 설계 판단.)

**② `test-psite-server` 는 진짜 빨갛다.**
`✗ ★차감은 자리 확정 **뒤** — 배치 실패 시 재료가 사라지지 않는다` (결과: FAIL 1건).
오늘 커밋(`5c0aafc`)이 마지막으로 만진 파일이라 **진행 중인 작업일 수 있다.** 손대지 않았다.
등록은 이게 초록이 된 뒤가 맞다.

**③ 27개를 다 넣어도 러너는 2.8분만 길어진다.**
느린 둘(`test-bridge-path` 71s · `test-distmatrix` 56s)이 그 절반이다. 지금 통합 러너는 한 바퀴가
**약 2시간**이므로 2.8분은 2% 남짓이다 — 비용 때문에 못 넣을 이유는 없다.
⇒ 넣는 법은 여전히 각 파일 머리에 `// @regress` 한 줄뿐이다(러너 무접촉).

### 중복 판단에 대하여 — **못 했다고 적는다**

require 하는 `server/*` 모듈 집합으로 겹침을 자동 판정해 봤다(`scripts/t9-overlap.js`).
**쓸 수 없었다** — `server/villages` 하나를 20개 넘는 하네스가 함께 require 해서, 모듈 겹침이
"같은 계약을 검사한다"를 뜻하지 않는다. 위 표의 '중복' 칸은 **이름·주제로 눈으로 본 것**이고,
`미확인` 은 "안 겹친다"가 아니라 **"확인하지 않았다"** 는 뜻이다.
제대로 하려면 하네스마다 "무엇을 assert 하는가"를 읽어야 한다 — 31개분이라 이 카드에서 안 했다.

## 0-지. [T12 지게 2026-09-01] 다른 영역에 남기는 한 줄

* **[클라 · 제작 패널]** ⚠**T38 이 절반만 고쳤다.** 인벤 장비 섹션의 재료 버튼(`data-eqmat`)은 `itemKo()` 를
  타는데, **시설 창의 재료 버튼(`data-fpick`)은 아직 `${o.material}` 을 그대로 찍는다** — 화면에 `wood 28%` ·
  `meteoric_iron 36%` 가 뜬다(리베이스에서 T38 판을 채택한 뒤 실측). ⇒ `50-i-panel.js:1055` 한 줄, `koOf(o.material)`.
  **자염 회부 D-1 · T38 과 같은 족이다.**
* **[클라 · 캐릭터]** 지게를 져도 **등에 아무것도 안 보인다**(`drawPlayerIso` 에 등짐 비트 없음).
  T0-b 직후라 이 배치는 안 건드렸다 — `회부_지게_다음층.md` D.
* **[영역 F 제작건축]** `EQUIPMENT_RECIPES` 에 **`extra`(곁재료) 문법**이 생겼다 — 재료가 둘인 첫 장비.
  새 장비를 넣을 때 쓸 수 있다. ⚠**`ITEM_TYPES` 에도 같이 넣어야** `EQUIPMENT_META` 가 그 타입을 발견한다.
* **[영역 F]** ⚠**클라 시설 창의 갈래는 셋뿐**(`options` / `preserve` / 나머지는 **`cook`**)이다.
  새 행을 넣을 땐 둘 중 하나를 반드시 달아라 — 안 달면 **요리 인스턴스가 나온다**(족보 (83) 의 화면판).
* **[영역 T 거래]** 상한이 플레이어별로 갈렸다 ⇒ **거래·납품 화면에서 상한을 상수로 읽지 마라**
  (`Carry.payload(p).cap`). 지금 `50-i-panel.js` 의 과적 경고는 이미 서버 값을 쓴다 — 그대로 두면 된다.

## 0-집. [T48 집 의뢰 2026-09-02] 다른 영역에 남기는 한 줄

* **[영역 Z 존서버 · 재민 판정]** ⚠**플레이어 메시지 핸들러에서 던지면 존 전체가 내려간다**
  (`ws.on('message')` 에 try/catch 없음 → `uncaughtException` → `process.exit(1)`).
  T48 이 그 경로로 실제로 존을 죽였다(집 의뢰 한 번). **근본은 고쳤지만 폭발 반경은 그대로다** —
  다음 `TypeError` 도 같은 일을 한다. "반쯤 죽은 서버가 더 나쁘다"는 재민 확정 방침이라 손대지 않았다.
  중간 지대(입력 핸들러만 잡고 **크게** 로그 + 그 플레이어에게만 알림)는 판정이 필요하다.
* **[영역 F 제작건축]** `zone.js` 에 **`_buildCostPay(player, cost, ±1)`** 가 생겼다 —
  건축 비용의 **지불과 환불이 같은 표를 걷는다**. 새 비용 키를 넣을 땐 이 함수 한 곳만 늘려라
  (종전엔 지불 5줄 / 환불 3줄이라 `seed`·`fiber` 를 안 되돌렸다 — 계단 비용이 판자뿐이라 잠복해 있었다).
* **[하네스 전 영역]** ⚠**`indexOf` 결과를 `>` 로 바로 비교하지 마라.** −1(못 찾음)과 순서 오류가
  **같은 빨강**이 되고, 실패 문구는 **있지도 않은 결함**을 지목한다. T9 전수표가 그 문구를 그대로 믿었다.
  ⇒ "찾았나"와 "순서가 맞나"를 **두 판정으로** 갈라라.
* **[하네스 전 영역]** ⚠**`handlePlayerInput(player, raw)` 는 문자열을 받는다**(`JSON.parse(raw.toString())`).
  객체를 넘기면 조용히 아무 일도 안 일어난다 — 하네스가 통과하고도 아무것도 안 잰 게 된다(족보 (57)).

---

## ★[T44 극단 HP 감소 2026-09-01] 다른 영역에 넘기는 것

전문은 `회부_극단HP_다음층.md`. 여기엔 **다른 영역 소유**인 것만 한 줄씩 올린다.
(캐논 자체는 `인계/공통.md` §1-8 · 실장은 `인계/B-신체서버.md` 3-사.)

* **T43(쓰러짐)** — 이 카드는 **진입만** 확인했다: 세 축이 극단으로 이어지면 HP 0 까지 가고
  현행 `player_downed` 경로로 들어간다(`e2e-thirst ⑥` 이 실서버에서 쓰러짐 패널까지 봤다).
  구조 창·업기·짐 낙하·후유증·깨어남 자리는 전부 §12 에 있고 T43 소관이다.
* ~~⚠**N/클라 — HP 자연 회복이 화면에 안 실린다.**~~ ✅**닫힘 — T61 이 `gauges`(초당 1건) 에 hp 를 실었고 T109 가 확인했다(2026-09-05).** 서버는 `player_damaged` 로만 hp 를 보내고
  회복은 **브로드캐스트가 없다**(`self.hp` 는 welcome 때 한 번뿐). 캐논 변경 전엔 티가 안 났는데,
  이제는 **굶다가 먹으면 화면이 한참 낡은 값을 들고 있다**("먹었는데 왜 안 낫지"). 고치는 값은 싸다.
* **세계관 판단 — 더위 축 신설.** 지시서는 네 축을 말했지만 `weather.js` 가 *"더위 축은 이 세계에 없다"*
  라고 못 박아 뒀다. r 표에는 남기고 배선하지 않았다. 열려면 여름의 위험을 어떤 동사로 줄지가 같이 정해져야 한다.
* ⚠**T41/T1 — `test-site-memo` 가 러너에서 혼자 못 돈다.** 씨앗 DB 를 `test-tick-slicer` 가 만드는데
  이름순으로 `site-memo` 가 **먼저** 돈다. `tick-slicer` 가 실패하면 `site-memo` 도 따라 죽는다(연쇄).
  하네스가 스스로 씨앗을 만들거나 러너가 순서를 알아야 한다.
* **B 영역이 다음에 값을 만질 때 읽을 것** — 추위 평형(0.9278)과 극단 문턱(0.930)이 **0.002 차이**다.
  `BODY_COLD_TAU_SEC`·`BODY_COLD_WIND_K`·옷 값 어느 하나만 움직여도 "한겨울 밤에 얼어 죽는 해"가
  19/24 에서 0/24 나 24/24 로 뒤집힌다(`test-body ⑭㉧` 이 24년 분포로 감시한다).

## 0-소속. [T11 마을 소속 2026-09-02] 다른 영역에 남기는 한 줄

* **[영역 E 사건장부]** `villages.villageBrief` 의 **복귀 브리핑 반환에 `rows` 를 추가**했다
  (`rb.rows` 그대로). `membership.orderBrief` 가 **순서만** 바꾸는 데 쓴다 — 가시성 술어
  (`ledger.visibleEvents`)는 한 글자도 안 건드렸다. `events.js` **무접촉**(T50 병행 중).
* **[영역 E]** 그래서 복귀 브리핑 줄은 이제 **두 번 그려질 수 있다**(`Events.briefLine` 정본으로 재조립).
  `briefLine` 이 빈 줄을 내는 타입이 생기면 재조립 결과가 비고, 그때 `orderBrief` 는 **손대지 않고 물러난다**.
  새 사건 타입을 넣을 땐 `LINES` 에 줄을 같이 넣어라.
* **[영역 V 마을배치 / T 거래]** `villages.js` 에 셋이 늘었다 —
  `playerVillageWithdraw`(납품의 역연산) · `playerVillageWithdrawStock`(꺼낼 수 있는 개수) ·
  `villageWithdrawGate`(=`_villageNear` 얇은 껍데기). **`playerVillageDeposit` 와 짝이다** —
  한쪽만 고치면 곳간이 환전소가 된다(하네스 `test-membership ⑤` 가 왕복 비트 동일로 잡는다).
* **[영역 V]** `__labProbe._memberProbe` 추가(`_distProbe` 와 같은 규약 · 최소 주입구 하나).
* **[온보딩 v2 소유 세션]** `설계/소속_사유지_기여_설계안.md` §2 는 `(vid, player_id)` 표를 제안했지만
  **실물은 `player_id` 단일키에 `start_vid` 를 곁들인 한 카운터**다. T11 은 문서가 아니라 **실물에 맞췄다.**
  ⇒ 문서 §2 의 표 스키마는 **낡았다**. 다음에 그 문서를 손보는 사람은 실물 쪽으로 고쳐라.
* **[영역 T 거래 / 클라]** 거래소 패널이 이제 `trade.member` 를 읽어 **머리 한 줄**을 더 그린다.
  `myTrade` 를 다루는 코드를 바꿀 땐 이 줄이 같이 산다는 걸 알아 둬라(값은 전부 서버 정본).
* **[영역 B 신체서버 / T47 소유]** `serializeBody` 에 `member` 필드가 하나 늘었다.
  `test-handoff-body` 는 **필드를 세지 않고 구조를 지키므로** 그대로 통과한다(설계대로).

## 0-겨울. [T20 겨울나기 공동 프로젝트 2026-09-02] 다른 영역에 남기는 한 줄

* ⚠**[영역 E주 / 온보딩 v2 소유 · T11 소유] 시계가 둘이다.** `zone.js` 는 `Membership.init` 과
  `Onboarding.init` 에 **`zoneGameDay`**(벽시계 파생)를 넘긴다. 정본은 `gameDayNow`(= `econDay` 우선)다.
  서버가 꺼져 있던 동안 둘은 **영구히 벌어지고**, 다시 붙지 않는다(따라잡는 코드가 없다).
  당장은 티가 안 난다 — 소속·기여는 "며칠"을 안 쓰기 때문이다. 하지만 **날짜를 쓰는 순간 틀린다.**
  T20 은 자기 시계만 `econDay` 로 받고 **저 두 줄은 안 고쳤다**(1줄 접점 규약 · 남의 영역).
* **[영역 V 마을배치 / T 거래] `villageDeliver` 에 갈래가 하나 늘었다.** 열린 의뢰가 없고
  겨울 공표 품목이면 **의뢰 없이 곳간이 받는다**(보상 0 · `done:false` ⇒ 기여 **횟수**는 안 오른다).
  이유는 실측이다 — econ 은 **식량을 `_consEMA` 에 담지 않으므로**(`economy-sim.js:456`)
  `d.short` 가 식량에 안 걸리고, **게시판엔 곡식 의뢰가 영영 안 뜬다**(51마을 실측 0건).
  `playerVillageDeposit` 는 **한 글자도 안 건드렸다** — 갈래는 그 앞에서 갈린다.
* **[영역 T 거래 / T11 소유] `membership.limitOf` 가 인자를 하나 더 받는다**(`vid`).
  더해지는 값은 `Winter.bonusOf(vid)` 한 줄이고, **그 해에 달성한 마을에서만** 0 이 아니다.
  `vid` 를 안 넘기면 예전과 정확히 같다(기본 인자 — 기존 호출부는 무해).
* **[클라 N] 게시판 토스트에 머리줄이 생겼다.** 서버가 `board.head`(문자열 또는 없음)를 보내고
  클라는 그걸 **rows 앞에** 붙인다. **의뢰가 0건이어도 머리줄은 떠야 한다** — 옛 코드는
  `rows` 가 비면 "걸린 의뢰가 없다"로 갈라져 머리줄을 통째로 버렸다(그 갈래도 고쳤다).
* **[설계 문서 소유] `설계/겨울나기_공동프로젝트_설계안.md` 의 두 자리가 낡았다** —
  ① `village_contrib` 표 둘(실물엔 없다) ② `N = _consEMA[res] × D`(식량엔 성립하지 않는다).
  T20 은 문서가 아니라 **실물에 맞췄다.** 다음에 그 문서를 손보는 사람은 실물 쪽으로 고쳐라.
* **[영역 E 자기 회부 · 재민 판정 대기]**
  ① ~~`WINTER_D` 기본값~~ → **[재민 확정 2026-09-03] 기본 5 유지 · 실기 뒤 env 로 조정.** (중앙 마을 목표 550 = 짐 10번)
  ② **[영역 E → T59 열량 정본]** 겨울 목표 품목이 **전 마을 공통 `food`** 다 — 어촌도 곡식을 낸다.
     **[재민 확정 2026-09-03] 이번엔 그대로 두고 회부** — 열량 정본(T59)이 서면
     **마을 특산 계열로 목표를 세는 것**(어촌 = 건어물)이 그 카드 몫이다.
  ③ **기여자 이름은 기억에만 있다**(`_names`, 영속 안 함) — 재기동하면 브리핑에서 이름이 빠진다.
     양(`village_winter`)은 남는다. 이름을 영속할지는 "누가 냈나"를 기록으로 남길지의 문제다.
  ④ **미달 페널티 · 보존식 전환 · 게시판 패널화**는 지시서가 명시적으로 회부한 것들이다(손대지 않았다).
  ⑤ ~~겨울 곳간이 비면 보상이 물리 상한에 가린다~~ → **[재민 확정 2026-09-03 ⓑ] 해소.**
     한도의 **밑변**을 econ 정본 **식량 등가**(`totalFoodEquivalent` · 보존식 포함)로 바꿨다.
     ★*"겨울엔 곳간이 상자보다 못하다"* 는 **캐논이 아니다** — T54 가 말리기를 겨울 비축으로 만든 이유가 그것이다.
     **[영역 T·T11 소유에 남기는 한 줄]** `membership.withdraw` 의 한도 밑변이 이제
     `max(그 품목 재고, 식량 등가)` 다(꺼내지는 **양**은 종전대로 실재고가 정한다 — 한도가 재고를 만들지 않는다).
     `zone._memberLine` 의 패널 "곳간 몫"도 같은 밑변을 쓴다.
  ⑦ **[영역 V·하네스] `e2e-village` 알림 경합 하나를 고쳤다** — `village_inventory` 거절 검사가
     *아무 알림이나* 오면 멈춰서, 촌장 브리핑이 먼저 도착하면 거절이 오기 전에 판정했다.
     **찾는 것이 올 때까지** 기다리게 바꿨다(읽기는 무해). 베이스 43/0 · 수리 뒤 내 트리도 43/0.
  ⑥ **묵은 곡식으로는 겨울 몫을 못 낸다** — T17 `_spoiledGuardRes` 가 가장 오래된 로트가 상했으면
     납품 **전체**를 거절한다(겨울 몫도 같은 입구). 옳은 규칙이라 손대지 않았지만,
     플레이어에게는 *"곳간에 낼 곡식은 성한 것이어야 한다"* 가 새 규칙이다.

## 0-갯. [T52 갯벌 2026-09-02] 다른 영역에 남기는 한 줄

* **[클라 · 인벤]** 인벤 화면이 `oyster`·`seaweed`·`abalone` 를 **영문 키로** 찍는다.
  서버는 `굴`·`해조`·`전복`을 보낸다(`ITEM_LABEL_SERVER` — 정본이 주입) — **클라 하드코딩 표**만 비어 있다.
  **자염 `brine`(회부 0-염) · T38 과 같은 족**이니 함께 닫아라. 클라 세 줄 + 이모지.
* **[영역 B 신체 · 재민 판정]** **물병에 민물 담기**가 아직 없다(물 들고 다니기 불가).
  넣으려면 **세 자리**가 필요하고 그중 하나가 **`doEat` 가 빈 그릇을 돌려주는 한 줄**이다 —
  없으면 마실 때 병이 사라져 T3 캐논("병은 그릇이지 소모품이 아니다")이 깨진다. `회부_갯벌_다음층.md` A.
* **[영역 L 로트·부패]** `Spoil.PRESERVE` 에 **말리기 2종**(마른 미역·건굴)을 넣으면 건조대에서 마른다.
  ⚠단 **`PRESERVED_EFFECTS` 가 zone.js** 라 zone 한 줄이 같이 와야 **먹을 수 있다**. `회부_갯벌_다음층.md` B.
* **[영역 T 거래 · T17]** `oyster`·`seaweed`·`abalone` 가 `PV_DEPOSIT_MAP` 에 없다 ⇒ **팔 수 없다**(소금과 같다).
  표 한 줄이면 열리는데 **게시 건수 기준선이 움직인다**(곡물 품목화 전례).
* **[하네스 전 영역]** ⚠**벽시계에 묶인 판정은 대조군에서도 자명 통과한다.** T52 개발 중 돌연변이 검사가
  "갯벌 판정을 통째로 빼도 초록"인 구멍을 잡았다 — 강가 대조군이 *그때 마침 물이 차서* 통과하고 있었다.
  ⇒ 시각에 기대는 검사는 **양쪽 상태를 다 못 박아라**.
* **★★[영역 E 사건장부 · T50 소유 / 재민 판정]** ⚠**3시드 800일 기준선이 T50 뒤로 움직였다.**
  종전 `6280·0/51 / 5993·0/51 / 6072·0/51` → 지금 `5745·0/51 / 6263·2/51 / 5938·2/51`.
  ⇒ **시드 2·3 에서 소멸이 0 에서 2 로 열렸다.** T52 것이 아님을 **두 번** 증명했다 —
  ⓐ 베이스 워크트리(`1af6ab2f`) A/B 가 세 시드 **전부 같은 수**(내 델타 0),
  ⓑ 랩 모듈 적재 추적이 `zone.js`·`forage.js`·`spoil.js`·`tidal.js` **넷 다 안 실린다**를 보였다
  (랩이 실제로 무는 저장소 모듈은 15개뿐). **소멸 0 은 오래된 불변식이니 판정이 필요하다.**
---

## ★[T43 쓰러짐·구조 v1 2026-09-02] 다른 영역에 넘기는 것

전문은 `회부_쓰러짐_다음층.md`. 여기엔 **다른 영역 소유**인 것만 한 줄씩 올린다.
(실장은 `인계/Z-존서버.md` T43 · 몸 쪽은 `인계/B-신체서버.md` 3-아.)

* **[H HUD / N 클라 · 설계]** §12 의 구조 행동 넷 중 **"먹여주기 · 물 먹이기"에 클라 동사가 없다** —
  이 세계의 먹기·마시기는 **자기 자신에게만** 걸린다(대상 인자가 없다). 서버 갈래 하나 + 클라 동사 하나가
  필요해 이 카드의 클라 예산(2줄)을 넘었다. ★없어도 §12 의 요점은 산다(극단이 남으면 T44 식이 다시 눕힌다).
* **[I 인벤 / F 제작]** ⚠**사망이 착용 장비를 안 떨군다.** 규약이 아니라 **자료 구조** 때문이다 —
  바닥템이 실을 수 있는 건 `{item,n,kg,led,lots,tool{type,d,max}}` 뿐이고 장비 인스턴스는 품질·속성이
  통째로 더 있다(억지로 실으면 **주웠을 때 다른 물건**이 된다). 옳은 순서는 **바닥템이 장비 인스턴스를
  그대로 싣게 만드는 것**이고 그건 한 배치다. 그 전까지 "몸에 걸친 것은 짐이 아니다"가 명시 규약이다.
* **[N 네트워크 / 전 영역]** ⚠**재접속의 공짜 풀피가 사라졌다.** `parseBody(.., {vitals:true})` 한 글자의
  부수 효과다(죽음 설계 부록 B-1 이 회부해 둔 그것). downed 와 무관한 **모든** 플레이어에게 걸린다 —
  전투에서 다치고 잔 사람이 아침에 다친 채 깨어난다. `test-handoff-body ④` 를 뒤집어 못 박았다.
* ~~**[N 네트워크 / 클라]** ⚠**HP 자연 회복이 화면에 안 실린다**~~ ✅**닫힘(T61 · T109 확인 2026-09-05)** — 아래는 옛 문장 —
  이제 재접속에도 낡는다(다친 채 들어오면 welcome 때 한 번 맞고 그 뒤 회복은 화면에 안 온다). 값은 싸다.
* **[설계 · PM]** **쓰러진 걸 남이 모른다.** 쓰러짐은 그 사람 화면에만 뜨고 멀리 있는 사람에게는 아무
  신호도 안 간다 — 창을 3분으로 늘린 근거(*"소리를 듣고 달려오는 사람"*)가 성립하려면 **소리**가 있어야 한다.
  §12 에 외침·표지가 없어 안 지어냈다. 실효 구조율도 여전히 미측정이다.
* **[설계 · PM]** 판단 확인 둘 — ⓐ **마을 불사의 경계**를 완충 정본(`shelterAt > 0`, 영향권 가장 바깥)으로
  잡았다(넉넉한 쪽). ⓑ **깨어남 사다리가 사유지 우선**이라 야생 저 끝의 사유지 하나가 거리와 무관하게 이긴다.
  §12 의 *"개인 클레임 → 없으면 가장 가까운 마을"* 순서 그대로지만 "가장 가까운"이 사유지에 안 걸리는지는 안 적혀 있다.
* **[하네스 전 영역]** ⚠**`test-handoff-body` 의 "비트 동일" 열쇠에 시계로 움직이는 값이 들어 있다** —
  `lots[].e`(부패 진행)·`t`. 왕복 사이에 **게임분 경계**를 넘으면 내구 손실이 하나도 없는데 ②④ 가 빨개진다
  (T43 리베이스 뒤 부하 중 1회 실측: `e 0 → 1.665 · t +1`. 단독 재실행은 우리·베이스 각각 2/2 통과).
  ⇒ 열쇠에서 부패 진행을 빼거나, 비교 전에 하루를 얼려라.

## 0-사유지. [T45 사유지 v2 2026-09-02] 다른 영역에 남기는 한 줄

* **[영역 Z 존서버 · 새 결함 발견]** ⚠**클레임 판정은 전부 bbox 를 본다** — `_furnaceClaimOf` ·
  `findGuildClaimContaining` · 침입·분해 검사가 `x >= c.x && x < c.x + c.w` 다. 그런데 **NPC 마을
  길드 영토는 `cells`(유기적 폴리곤)를 갖고 렌더는 그 셀을 그린다** ⇒ **화면에 안 칠해진 자리가
  서버에겐 길드 영토다**(41×41 bbox 안의 물·바위 칸까지). T45 는 사람 클레임(1셀 = bbox = 셀)만
  다뤄서 이 갈림을 안 건드렸다. 고치려면 `cellsOf` 를 그 네 자리에 태우면 된다(정본은 이미 있다).
* **[영역 S · 길드]** ⚠`tryClaim` 의 길드 갈래에 `// leader 체크는 central 호출 필요` TODO 가 그대로다 —
  **길드원 아무나 길드 메인 영토를 통째로 옮길 수 있다**(기존 것을 지우고 새로 만든다). T45 범위 밖.
* **[영역 S · 길드]** ⚠`tribe_set` 이 여전히 **클라 권위**다(검증 0줄). T46 §0-ⓑ 가 적어 둔 그대로이고,
  이제 `held_by='guild:<id>'` **우선권까지** 그 줄에 매달린다 — 조작된 클라가 남의 우선권을 가져간다.
  ⇒ **서버 권위로 바꾸는 일이 한 칸 더 급해졌다.**
* **[영역 T0/클라]** 설계 문서가 적은 접촉 파일 `public/client/32-m-render.js` 는 **낡았다** —
  T51(`79a5f033`)이 클레임 렌더를 **`34-m-renderloop.js`** 로 옮겼다. 새 메시지 `claim_state` 처리는
  `30-n-net.js` 에 한 절이다.
* **[영역 I 인벤/상자]** 상자는 여태 **누구나 열 수 있었다**(PZ 스타일 · 코드 주석에 그렇게 적혀 있다).
  T45 가 **딱 한 갈래만** 잠갔다 — **보관 중이고 맡은 이가 없는(무주) 사유지 위의 상자**.
  그 밖은 종전 그대로다. 상자 잠금 정책 전반은 그 영역의 몫이다.
* **[영역 B/T8·T43]** ★**부활 지점 결함의 진짜 크기가 드러났다** — `listRespawnOptions` 가
  `c.ownerId`(클레임에 **없는 필드**)를 봐서 **개인·임시 사유지가 처음부터 한 번도 부활 지점이
  된 적이 없다.** 문서 §0-ⓐ-1-ⓒ 는 "재시작 뒤에"라고 적었는데 실측은 더 나빴다. T45 가 고쳤다.
---

## 0-그. [T54 갯벌 3차 2026-09-02] 다른 영역에 남기는 한 줄

* **[영역 Z 존서버 · 재민 판정]** ⚠**물 회복량이 셋이 됐다** — 웅덩이 `WATER_DRINK_AMOUNT` **35**(이름 있는 상수) ·
  물가 `tryGather` **리터럴 30** · 병 한 되 `Tidal.DRINK_THIRST` **30**. 리터럴이라 참조를 못 해서
  **하네스가 소스를 읽어 계약을 잰다**(갈라지면 `test-tidal ⑧` 이 빨개진다). 상수 승격은 **zone 두 줄**이라
  T54 예산(3줄)을 넘어 안 했다. `회부/회부_갯벌_다음층.md` G.
* **[영역 C 자염 · T3 소유]** ⚠**병을 돌려주는 자리가 둘이 됐다** — 가마(`doBoilSalt` → 시설 `items`)와
  마시기(`doEat` 의 새 `returns` 칸). 그릇이 항아리·바가지로 늘면 갈라진다 ⇒ 가마도 `returns` 로 모아라.
  ★그때 `Tidal.VESSELS` 가 **그릇 표**로 승격된다(지금은 민물 한 줄). `회부/회부_갯벌_다음층.md` H.
* **[영역 Z / H HUD]** ★`doEat` 안내가 갈증 전용 품목에도 `(+허기 0)` 을 찍는다(민물 한 되).
  빈 병 안내가 따로 나가 뜻은 통하지만 한 줄이 어색하다 — 갈증 항 문구 갈래(zone 한 줄).
* **[영역 T 거래 · T17]** ★★**말린 것 둘이 늘었다**(`dried_oyster`·`dried_seaweed`) — 조개·해조와 함께
  `PV_DEPOSIT_MAP` 밖이라 **못 판다**. ⚠마른 미역은 **180일**이라 곡물과 같은 저장성이다 —
  거래에 열면 곡물 화폐 옆에 **둘째 저장 자산**이 생긴다(기준선이 움직인다). 회부 C 와 같이 판정해라.
* **[영역 L 로트·부패]** ★`Spoil.PRESERVED_ITEMS` 가 넷 → **여섯**. 이름·보관일은 여기가 정본이고
  **무게·허기·갈증은 `tidal.js` 가 원물에서 유도한다**(두 표가 같은 것을 안 적는다). 다음에 보존식을
  더할 땐 그 갈래를 지켜라 — 원물이 다른 영역 것이면 유도는 그 영역 정본이 한다.
* **[재민 실기 판정]** ★★★**"갯벌은 겨울 비축이 아니다"(T52)가 뒤집혔다** — 한 물때 이론 상한을 전부
  말리면 **52.4일치 · 17.86kg**(`tidal-metrics ⑦`). 세다 싶으면 손잡이는 `SPOIL_D_GRAIN`(마른 미역 180)과
  `TIDE_DRY_RESIDUE`(0.26)다. **채집 시간이 진짜 병목인지**는 실기가 답한다.
* **★★[영역 B 신체 · T4 소유 / 하네스]** ⚠**`e2e-cold ⑦㉣` 이 물때를 안 못 박아 간조에 빨개진다.**
  *"바닷가에서 마시면 화면이 「짠물」이라고 말한다"* 인데, **T52 가 바다 게이트를 바꾼 뒤로** 병 없는
  사람은 간조에 **채집이 먼저**라 그 안내가 안 뜬다. 세 판으로 증명했다 — HEAD 만조 **52/0** ·
  HEAD 간조 51/1 · **베이스(T52) 간조도 51/1(같은 자리)**. ⇒ T54 것이 아니고, **족보 (95)가 자기
  예언을 맞힌 자리**다. 수리는 한 줄(그 절의 zone 을 `TIDE_FREEZE_MS=<만조>` 로 띄우거나 병을 쥐어 준다).
* **★[재민 판정 · 제품]** 간조의 갯벌에서 **목마른 사람이 "짠물이다"를 못 배운다** — T4 캐논
  (바닷물은 목을 안 축인다)은 그대로지만 **가르쳐 주는 자리가 물때에 따라 사라진다**.
  둘 다 주려면 채집 안내에 짠물 한 마디를 얹거나, 갈증이 낮을 때만 채집을 앞세우면 된다(zone 한 줄).
* **★★[영역 E/econ · T17 소유 / PM 판정]** ⚠**T17 이 공표한 새 기준선이 `f949613a` 에서 재현되지 않는다.**
  `인계/공통.md` 는 **6,304/5,740/5,649 · 소멸 0/51 · 무기Q 965/885/774 · 게시 2,241/2,151/2,066** 인데,
  T54 가 T17 뒤의 판(`f949613a` — T17 다음은 **T55 클라 커밋뿐**)에서 **손잡이를 안 건드리고** 재니
  **5964/5631/5399 · 소멸 0/0/1 · 무기Q 886(seed1) · 게시 2001(seed1)** 이 나왔다.
  `econ-lab-real` 과 `ev-density` 가 **서로는 일치**하므로(둘 다 5964·0/51) 계측기 갈림이 아니다.
  ⇒ 확인할 것 셋: ⓐ 잰 커밋이 `906c059c` 가 맞나 ⓑ 새 열 셋의 **기본값**이 공표 때와 같나
  ⓒ **seed 3 의 소멸 1/51**(공표는 0/51) — "소멸 0" 은 오래된 불변식이다.
  ★T54 의 델타는 **베이스 A/B 로 세 시드 전부 0** 이라 이 카드의 판정에는 영향이 없다.

## T49. ★2026-09-02 하네스 위생 일괄 — 다른 영역에 넘기는 것 넷

등재 27개는 PM 승인대로 넣었다(⇒ 자동 발견 **92종**). 아래는 **내 카드 밖**이라 손대지 않고 넘긴다.
셋 다 "절대 문턱" 또는 "두 순간" 문제라 T49 가 셋에 쓴 처방(같은 실행 안 대조 짝 → 비율의 비율)이
**그대로 들어갈 자리**지만, 카드가 지목한 셋에 없어 건드리지 않았다.

* **[영역 R2 개체렌더 · `e2e-charsprite`]** ③ '남이 멈추면 다시 idle' 을 **절대 2800ms** 로 기다린다.
  전수 3회 중 빨간 회차가 있었고, 늦는 것이지 안 되는 것이 아니다 — 기계가 바쁘면 문턱을 넘는다.
  ⇒ 처방은 T49 가 `test-tick-slicer`·`e2e-rtt`·`e2e-weight` 에 쓴 것과 같다:
  **같은 실행 안에서 대조 짝**(같은 조건 두 번)으로 잡음 바닥을 먼저 재고, 그 배수로 판정한다.
  절대 ms 는 참고 출력으로 남긴다. 족보 ⑻⓪.
* **[영역 S 사회스킬 · `e2e-rumor`]** '촌장이 하는 말은 전부 게시판 소식 안에' 를 **두 화면을 따로 받아**
  비교한다. 전수 3회 모두 빨갰고 **문장이 매번 다르다** = 사본 결함이 아니라 **시점** 문제다.
  T7 보고서가 스스로 족보 후보로 적어 둔 자리다("두 화면을 견주려면 같은 순간에 받아라").
  ⇒ 같은 tick 의 스냅샷 하나에서 둘을 뽑거나, 두 화면 사이에 변할 수 있는 필드를 판정에서 빼라.
* **[영역 Z 존서버 · `test-handoff-body`]** ③ '몸 없는 페이로드에서 central 행 폴백' 이 **깜빡인다**
  (전수 1회차 16/2 · 2·3회차 18/0). central 저장이 끝나야 통과하는 자리라 **경합**으로 보인다.
  이 하네스는 다른 세션이 N.6(핸드오프 몸 이전)과 함께 지금 만들고 있다 — **그 세션 소유**다.
  ⇒ 폴백을 재려면 "central 이 저장을 마쳤다"를 하네스가 **기다릴 수 있는 신호**가 필요하다.
* **[영역 N 네트워크 · `e2e-rtt` ②a]** T49 가 비율의 비율로 바꾼 뒤에도 `A.tick.frameMax` 는
  **같은 조건 잡음이 2.0배**다(3037ms vs 1487ms — 두 번 재서 두 번 다). 최댓값 하나짜리 통계라
  꼬리가 통계를 지배한다. 지금은 잡음이 1.5 를 넘으면 **판정을 유보**하고("안 줄었다"가 아니라
  "못 쟀다") "최소한 나빠지지는 않았다"만 재게 해 뒀다 — **잰 척은 안 하지만 못 재는 건 그대로다.**
  ⇒ 이 축을 진짜로 재려면 `frameMax` 대신 **p95/p99 같은 꼬리 분위수**가 필요하다. 그건
  서버가 무엇을 내보내느냐(T47 데이터 모델)에 달려 있어 하네스 혼자 못 고친다.

★ 러너 밖에 **남은 것은 4개뿐**이다: `test-lab-*` 셋(레포 밖 `~/Mini/전쟁실험실.html` 을 연다 —
컨테이너·CI 에선 영원히 못 돈다. 이 셋은 "러너 밖 유지"가 PM 판정이다) ·
`test-psite-server`(제품 결함으로 빨감 — 세션3 T48 이 제품 쪽을 수리한 뒤 등재하면 된다).

### T49 검증 중에 **새로 드러난 것 둘** (2026-09-02 · 둘 다 고치지 않았다)

* **[영역 Z 존서버] `test-guest-rejoin ②` — 3번에 1번 걷던 자리를 잃는다.** ⇒ `KNOWN_ISSUES.md` **N.11**.
  전수 러너에서 처음 빨갰고, 손으로 18판을 더 돌려 귀속을 갈랐다:
  한가한 기계 2/6 · 바쁜 기계 2/6 · **T10 수리 전 옛 하네스도 2/6**.
  ⇒ 부하 탓도, 하네스 탓도 아니다. `zone.js` B-6 "살아 있는 몸 인수인계"가
  **옛 세션이 아직 `players` 에 서 있을 때만** 돌고, close 가 먼저 처리되면 물려받을 몸이 없어
  central 을 읽는데 그 읽기가 close 의 `savePlayer`(fire-and-forget)와 또 경합한다.
  결과가 두 갈래로만 갈린다(걷던 자리와 19px / 낡은 좌표와 **0px**) — 중간이 없다.
  ⇒ **설계 판단이 필요하다**(회부): ⓐ close 가 저장을 기다린 뒤 `players.delete` ·
  ⓑ 최근 종료한 몸을 짧게 붙잡아 두는 유예 창. N.6 과 같은 물음("몸의 정본이 어디인가")이다.
* **[영역 R1 지형렌더 · `e2e-zoom` ⑤]** '1 → 1.5 → 2 → 1 을 돌고 와도 화면이 처음과 같다' 를
  **절대 화소 차 1.0** 으로 판정한다. 전수 회차 1 에서 **8.315** 로 빨갰다.
  단독으로 6판을 돌리니 **6/6 초록**이고 값이 0.058~0.102 였다 — 평소엔 문턱의 1/10 이다.
  ⇒ **재현 못 했다. 그대로 적는다.** 다만 자의 모양이 위태롭다: `base` 와 `back` 사이에
  1분 가까이 흐르고 그 동안 **세계는 살아 있다**(바람만 껐다). 짐승 하나가 화면을 지나가면
  그만큼 차가 난다. 처방은 T49 가 셋에 쓴 것과 같은 족이다 — 바람처럼 **나머지도 격리**하거나,
  기준 화면을 왕복 **직전에** 다시 찍어라. 등재 하네스라 내 카드 밖이다.

### T49 전수 3회에서 더 나온 것 — **마을 반경 진입 셋 · `test-tick-slicer ⑧`** (2026-09-02)

* **[영역 V 마을배치 · T 거래] `e2e-village`·`e2e-weight`·`e2e-trade` 가 같은 자리에서 떨어진다.**
  셋은 서로 다른 하네스인데 떨어진 곳이 같다 — **마을 반경 진입**. 실측:
  `e2e-trade` 는 `vid=null · 24,749px (게이트 260)` 이었다 — "조금 모자랐다"가 아니라 **한 발짝도 안 갔다.**
  ⇒ 셋의 공통 구조: ① `villages` 표에서 **`rows[0]` 을 무조건** 고르고 ② `teleport_debug` 를
  25번 던지고 ③ **워프가 먹었는지는 안 재고** "마을 반경 안에 들어왔다"만 잰다.
  그런데 서버는 물 위 워프를 **거절한다**(`zone.js:3470`). 세계는 판마다 새로 생기므로
  `rows[0]` 이 막힌 칸에 앉는 판이 있고, 그러면 25번이 모두 거절되고 **원인이 증상으로 둔갑한다.**
  ★**가설이다 — 증명 못 했다**(판마다 세계가 달라 되짚을 수 없었다). 그대로 적는다.
  ⇒ 처방은 이미 이 레포 안에 있다: `scripts/bench-spread.js` 가 서버의 🌊/🌀 알림을 읽어
  **다음 자리로 물러서는 되풀이**를 한다. ① 워프 성공을 **먼저 assert** · ② 안 되면 `rows[1]`·`rows[2]`.
* **[영역 Z 존서버 · `test-tick-slicer ⑧`]** '같은 날끼리 대조할 마을이 충분하다' 가 전수 3회 중
  2회 떨어졌다(`A/B 0곳 · 건너뜀 50`). **내가 넣은 세 번째 팔(`base2`) 탓인지 갈랐다:**
```
  지금 판(base2 있음) ×3   빨강 0    A/B 36 · 26 · 23곳
  옛  판(base2 없음) ×3    빨강 1    A/B 32 · **2곳(✗)** · 35곳
```
  ⇒ 내 팔 탓이 아니다 — 오히려 옛 판이 먼저 떨어졌다. 단독으로는 양쪽 다 대체로 초록이고
  **러너 안에서만** 떨어진다. 부하가 두 팔의 게임일 진행을 다르게 늦추면 마을별 저장일이
  통째로 어긋나 A/B 가 0 이 된다(저장은 1마을/틱이라 **위상**이 있다).
  ⇒ 대조를 "같은 날"이 아니라 **"같은 진행량"**으로 맞추거나, 팔을 끝낼 때 저장 커서를
  한 바퀴 돌려 놓고 스냅샷을 떠라(위상을 없앤다).

★**넷 다 같은 병이다** — 절대 문턱이거나 시점에 달린 자. T49 가 셋에 쓴 처방이 그대로 들어갈
자리지만 카드가 지목한 셋에 없어 손대지 않았다. **처방을 마저 적용할지는 PM 판정이다.**

## 0-이방인. [T19 유저 마을 시작지 2026-09-02] 다른 영역에 남기는 한 줄

* **[영역 V 마을배치]** ★**마을 페이로드를 만드는 자리가 둘이 됐다** — 시딩(Stage 4A `clientPayload = map(...)`)과
  `foundPlayerVillage` 의 한 항목. 새 필드를 더할 땐 **둘 다** 고쳐야 한다(한쪽만이면 "재시작해야 보이는 필드"다).
* **[영역 V / N]** 새 메시지 **`sim_village_add`** — 마을 한 곳이 는다. 클라는 `c.simVillages` 에 붙이기만 한다.
  ⚠`simVillages` 는 여전히 **welcome 1회 + 이 통지**뿐이다. 마을이 **사라지는** 경로(소멸)는 아직 통지가 없다.
* **[온보딩 소유 세션]** `arrivalOf` 의 `if (_arrDone) return null;` 은 그대로 두고 **새 문 `noteVillage`** 를 냈다.
  전체 무효화(`invalidate`)는 51곳 37초 정지라 쓸 수 없다(회부 B-7). `invalidate()` 는 여전히 **아무도 안 부른다**.
* **[영역 S · 온보딩]** "쉼터"는 **시설로 존재하지 않는다**(회부 온보딩 A-3). T19 은 대체 술어
  (인구 ≥ 1 + 식량 자립 3일)로 갔다. **쉼터 시설이 생기면 그 술어로 갈아 끼워라** — `newcomers.js` 한 곳이다.
* **[영역 S · 벌점]** §9.3 의 **"신입 자산 보호"** 는 안 만들었다("벌점 층에서 조임"이 설계의 말이다).
  지금은 이방인이 도착한 뒤의 보호가 **0**이다 — 유저 마을을 시작지로 여는 것과 짝이어야 할 층이다.
* **[영역 B/중앙]** ⚠★**`| 0` 을 시각에 쓰지 마라**(족보 77). T19 1차 판이 `p.last_seen | 0` 을 썼고
  epoch ms 가 int32 로 잘려 `e2e-village` 에서 **"촌장이 20679일째 안 온다"**(=56년)가 나왔다 —
  방금 로그인한 사람이었다. **하네스는 못 잡았다**(값을 손으로 꽂아 재느라 `scan()` 을 안 밟았다).
  ⇒ 지금은 `Number(...)` + `> 0` 이고, 하네스에 **그 길을 밟는 절**(⑥-b)을 따로 넣었다.
* **[영역 E/econ · T17 소유 — 위 0-그 ECON 항에 대한 실측 한 줄]** ★T19 이 리베이스 뒤
  `origin/main`(4b6f303b) 과 자기 판을 **같은 계측기로 나란히** 돌렸다(`t17-metrics 800` · 1020·7·42).
  **T17 이 공표한 새 열 여섯은 정확히 재현된다** — 도구Q 10,868/10,152.8/9,546.8 · 부족 82/97/85 ·
  보존식 387/235.5/230.3 · 소금 지역차 3.92/5.37/5.61 (= `인계/공통.md` 표와 일치).
  ⇒ 0-그 의 **확인할 것 ⓑ("새 열 셋의 기본값이 공표 때와 같나")는 같다**로 닫힌다.
  인구·소멸·게시 쪽(ⓐ·ⓒ)은 T54 가 든 그대로 남는다 — 이 실측이 그걸 답한 건 아니다.
* **[영역 T10 · 하네스]** `e2e-village` 는 **부하에서 흔들린다** — 이번 세션에서도 한 판이
  "재접속한 창설자가 자기 마을 재고를 못 본다"부터 무너졌다가 **단독 재실행에서 43/0** 이었다(같은 코드).
  족보 ㊾ 의 그 자리다. ★리베이스 뒤 3판을 더 돌려 **43/0 · 43/0 · 24/12** 를 봤다 —
  빨간 판은 **매번 다른 자리**에서 무너지고 그 뒤는 전부 파생이다. 무너지는 두 자리가
  **재접속 직후**(창설자 재고 재열람)와 **알림 대기**(T50 이 고친 그 절)라, 방아쇠는
  부하 일반이 아니라 **재접속 타이밍**으로 좁혀진다. 손볼 세션에 넘긴다.
---

## ★[T56 쓰러짐 2차 2026-09-02] 다른 영역에 넘기는 것

전문은 `회부/회부_쓰러짐_다음층.md`(T43 것을 **이사·갱신**했다 — 닫힌 항목은 취소선으로 남겼다).
실장은 `인계/Z-존서버.md` T56 · 몸 쪽은 `인계/B-신체서버.md` 3-자.

* **[H HUD · T55 뒤]** **후유증 무들 한 칸** — 재료는 이제 간다(`Body.selfPayload().aftermath = {days, cap}`,
  없으면 `null`). 화면이 "며칠은 숨이 덜 붙는다"를 말할 자리만 남았다. 새 패널 0 안에서 무들 한 칸이면 된다.
* **[영역 B · 재민 판정]** **더위 축은 온도로는 성립하지 않는다** — T56 이 24년 표를 떴다(회부_극단HP B-1):
  존 전체가 **한 곡선**이고(마을별 차이 0 · 설 수 있는 고도 0), 24년 최고가 **33.69℃**, 35℃ 초과 **0회**다.
  `coldOfC` 가 끝나는 29℃ 위로 **4.7℃** 밖에 없어 어떤 표를 놓아도 "매년 극단" 아니면 "24년에 0회"가 된다.
  ⇒ 열려면 온도가 아니라 **여름철 갈증 배율**이 답이다(새 축 0 · 이미 있는 동사가 답이 된다).
* ~~**[영역 I 인벤 / B]** 물병에 민물 담기~~ — **T54 가 같은 날 열었고 `/물` 이 그날 그것을 받았다**
  (들고 온 물이 먼저 · 없으면 물가에서 손으로. 빈 병은 **준 사람** 손에 남는다).
* **★[영역 H 클라 · T53/T55 소유]** ⚠**`onbHudLine` 적재 순서 경쟁** — `updateHud()`(IIFE 안 · `44-h-hud.js`)가
  `70-lobby.js` 의 **전역** 함수를 부른다. 그 파일이 파싱되기 전에 HUD 가 그려지면
  `onbHudLine is not defined` 가 난다. **같은 트리에서 어떤 실행은 초록, 어떤 실행은 빨강**이라
  조용히 지나간다(`e2e-downed` 가 네 번 중 두 번 잡았다 — 클라 무접촉 카드였다).
  선언을 같은 스코프로 들이거나 `typeof` 로 감싸면 **한 줄**이다.
  ⇒ 그때까지 `e2e-downed` 는 이 한 문장만 **묵인 목록**에 넣고 **크게 찍는다**(덮지 않고 가른다).
  **고치면 그 목록을 지워라** — `scripts/e2e-downed.js` 의 `UPSTREAM_KNOWN` 한 줄이다.
* **[하네스 · T52/T54 소유]** ⚠**zone 의 갯벌 접점 예산(3줄)은 다른 카드도 밟는다** — T56 초안이
  `Rescue.init` 에 `require('./tidal').FRESH` 한 줄을 얹었다가 `test-tidal` 이 넷째 줄로 잡았다.
  ⇒ **소비자가 표의 주인에게 직접 묻는 것**이 맞다(zone 을 경유하지 마라). 그 하네스는 잘 작동한다.
* **[하네스 전 영역]** ⚠**화면의 완충값(`__wx().shelter`)은 텔레포트 직후 낡아 있을 수 있다** —
  서버가 완충을 1초 메모하고 날씨는 주기적으로만 실어 보낸다. `e2e-downed` 가 이것 때문에 **마을 한복판을
  "야생"으로 집어 들었다**(그래서 죽는 대신 마을 사람이 옮겼고 판정 넷이 헛돌았다).
  ⇒ 야생을 고르는 하네스는 **마을 중심에서의 거리**(DB)로 한 번 더 잠가라 — 반경 정본 최댓값은 2,152px 다.
* **[설계 · PM]** **실효 구조율은 여전히 미측정** — 소리는 생겼지만 "몇 %가 구조되나"와
  "외침이 도배로 느껴지나"(30초 × 6번)는 사람이 둘 이상 붙는 라이브가 와야 잰다. 손잡이는 env 둘뿐이다.
* **[영역 E 사건장부 · PM 판정 유지]** 외침은 **장부에 안 넣었다**(플레이어 행위의 연대기 편입은 별도 백로그).
  근접 플레이어에게 가는 디에게틱 알림뿐이다.

## 0-밀도. [T63 사건 밀도 재정박 2026-09-03] 다른 영역에 남기는 한 줄

* ★★**[재민 판정 대기] 캐논 문장의 뜻** — *"마을당 2~3일에 1건"* 이 **㉮ 전체 밀도**인가
  **㉯ 값 유형 밀도**인가. 수는 둘 다 쟀다(`보고/T63_2026-09-03.md` ⓒ): ㉮ 1.92~1.96(하한 밑) ·
  ㉯ 2.01~2.06(구간 안). ㉯가 **채택 시점의 수 2.21/2.22/2.22 를 정확히 복원**한다.
  처방 A(가격 문턱 재스윕)의 비용도 실측해 뒀다 — **캐논 한가운데로는 갈 수 없다**(±100 에서 `PRICE_DROP` 이 0).
* ⚠**[영역 E · 다음에 이 자리를 여는 사람에게] 유형별 문턱 표가 커지면 표 자체가 축이 된다.**
  유형마다 손잡이를 주면 다음 사람은 "밀도를 맞추려고" 유형을 골라 깎게 된다 — 그건 관측자가 아니라 편집자다.
  이 배치는 **새 손잡이를 하나도 만들지 않았다**(스윕용 `EV_SWEEP_EXTRA` 는 계측기 인자지 세계의 손잡이가 아니다).
* ⚠**[전 영역] 같은 계측기를 두 세션이 같은 시각에 만들었다** — `scripts/lab-modules.js` 를
  상류(PM · `06dd635e`)와 내가 각각 짰고 리베이스에서 `AA` 로 만났다. **내 것을 버리고 상류 것을 썼다.**
  규약이 도구를 요구하면 **도구도 같이 배포하는 게 싸다**(그러면 세션마다 다시 짜지 않는다).
* ★**[전 영역 · `인계/공통.md` §2 소유] 랩 적재 목록이 자랐다 — 15개가 아니라 16개다.**
  T20 이 `villages.js` 에 `require('./winter')` 를 더한 순간 `server/winter.js` 가 들어왔다.
  ⇒ 규약 §2 ①의 "T52 §7 목록이 정본"을 **"`scripts/lab-modules.js` 로 매번 잰다"** 로 바꿔 두는 게 안전하다.
  목록을 인용만 하면 언젠가 **낡은 목록으로 면제**를 주게 된다(그 계측기를 이 배치가 만들어 뒀다).
  ⚠적재된다고 **불린다**는 뜻은 아니다 — 랩엔 `Winter.init` 호출부가 없어 winter 는 한 줄도 안 돈다.
  그래도 규약은 파일 교집합으로 판정하므로 이 카드는 베이스 A/B 를 **했다**(면제를 스스로 넓히지 않는다).
* **[영역 E → T60(세션1 econ)]** T60 이 econ 품목을 더 늘리면 **㉯도 하한을 넘는다**(지금 여유 0.01~0.06일).
  `ev-density ⓔ` 가 구간 밖이면 스스로 ★경고를 찍게 해 뒀다 — 표를 안 읽어도 계측기가 말한다.
* **[영역 E · T20 회부 ⓪ 해소]** 겨울 기여자 **이름**을 영속했다(`village_winter.name` · `COALESCE` 로
  이름 없는 갱신이 안 지운다 · `test-winter ⑨`). ⚠**표시용**이다 — 판정도 보상도 이 열을 안 읽는다.
* ★★**[영역 클라 · T61 소유 — 내 T20 이 만든 빚이다] 연표 아이콘 표에 겨울 유형 둘이 빠졌다.**
  `test-chron-icons ③`(서버 `TYPES` ∖ 클라 아이콘 표 = ∅)이 **빨갛다** — 빠진 유형은
  `WINTER_KEPT` · `WINTER_SHORT` 이고, 지금은 조용히 `🕰️` 폴백으로 그려진다.
  T55 가 표를 세운 것과 T20 이 유형 둘을 더한 것이 **엇갈렸다**(둘 다 2026-09-02).
  고치는 값은 `public/client/65-s-chronicle.js` 의 `CHRON_TYPE_EMO` 에 **두 줄**이다:
  ```js
  // 겨울나기(T20) — 곳간이 든든한 겨울과 궁한 겨울
  WINTER_KEPT: '🧊', WINTER_SHORT: '🌬️',
  ```
  ⚠**T63 은 클라 무접촉이 절대 규칙이라 손대지 않았다**(병행 T61 과 겹치지 않으려는 규칙이다).
  내 배치가 만든 빚이라는 점을 적어 둔다 — 다음에 클라를 여는 카드가 이 두 줄을 넣어 주면 러너가 초록이 된다.
* **[영역 V·하네스 — T55 소유] `e2e-events ⑤-b` 를 고쳤다(제품 무접촉).**
  *"소식이 의뢰 줄 아래인가"* 검사가 `^ · ` 로 첫 의뢰 줄을 찾는데 **소식 줄도 ` · ` 로 시작**해서,
  게시판에 의뢰가 0건인 판이면 소식 줄을 "첫 의뢰 줄"로 집고 뜻이 뒤집혔다(같은 코드로 베이스 38/0 ·
  내 트리 37/1 이 난 이유가 그것이다 — 두 판의 게시판 상태가 달랐다).
  ⇒ **소식 절 앞쪽만** 보고, 의뢰 0건이면 앞머리가 "걸린 의뢰가 없다"인지로 판정한다.
* **[영역 E주 → T62(세션7)] 시계 둘** — `Membership.init`·`Onboarding.init` 이 `zoneGameDay`(벽시계 파생)를
  받는다. 정본은 `gameDayNow`(=`econDay`). **내 것이 아니라 손대지 않았다**(T20 에서 올린 그 줄 그대로).

## 0-문장. [T71 서버 문장의 이모지 2026-09-03] 영역별 한 줄

재민 확정(09-03): **UI 이모지 0**(`설계/설계_화면규칙_B_먹선.md` §1-2). 클라(T61·T66)가 자기 이모지를 지워도
**서버가 보낸 문장에 박힌 글자**는 못 지운다 — 문장의 주인이 지운다. E 영역 몫은 끝냈고(아래), 나머지는 표로 남긴다.

★세는 법: **AST 로 문자열 리터럴만**(주석 제외 · `\p{Extended_Pictographic}`). 정규식으로 파일을 훑으면
주석의 ⚠·★까지 세어 없는 결함을 보고한다 — `events.js` 는 주석에만 ⚠ 가 22자다(문장은 0).
계량기는 `test-events ㊸` 안에 있다(같은 방식으로 자기 파일 둘을 회귀로 건다).

| 파일 | 리터럴 속 이모지 | 콘솔 로그 | **화면 문장** |
|---|---|---|---|
| ~~`server/events.js`~~ | 0 | 0 | **0** (원래 없었다) |
| ~~`server/winter.js`~~ | ~~1~~ → **0** | 0 | **0** (T71 이 뺐다) |
| **`server/zone.js`** | 175 | 57 | **118** |
| **`server/villages.js`** | 96 | 69 | **27** |
| **`server/rescue.js`** | 16 | 0 | **16** |
| **`server/membership.js`** | 13 | 0 | **13** |
| **`server/bandits.js`** | 12 | 7 | **5** |
| **`server/onboarding.js`** | 5 | 2 | **3** |

* **[영역 Z 존서버 · T62/T64]** `zone.js` 화면 문장 **118**개가 제일 크다. ⚠**그런데 먼저 할 일이 따로 있다** ↓
* ★★**[영역 Z · 선행 조건] `notice` 에 유형 필드가 없다.** `zone.js` 는 322곳에서
  `send(ws, { type:'notice', text:'…' })` 를 보내고 클라는 `showNotice(msg.text)` 로 **문자열만** 그린다.
  ⇒ 그 118개에서 이모지는 **알림 종류를 나르는 유일한 표시**다. 빼면 정보가 준다.
  **`notice` 에 `kind` 를 먼저 실어라** — 그 뒤에 글자를 빼야 뜻이 안 준다.
  (연표는 T50 이 `type` 을 실어 둔 덕에 이 문제가 없다 — T66 이 그 필드로 아이콘을 그린다.)
* **[영역 V 마을 · `villages.js`]** 96 중 **69가 서버 콘솔 로그**다(`🏘 [마을] 시딩…`). 화면 문장은 27.
  로그 줄은 사람 눈에 안 가니 급하지 않다 — 다만 하네스 넷이 그 로그를 `📜`·`🏘` 로 **필터**한다
  (`e2e-chronicle`·`e2e-events`·`e2e-rumor`·`e2e-winter` 의 stdout 필터). **로그에서 뺄 땐 그 필터도 같이 고쳐라.**
* **[영역 B/T56 `rescue.js` 16 · T11 `membership.js` 13 · T62 `onboarding.js` 3 · `bandits.js` 5]**
  전부 화면 문장 쪽이다. 크기가 작아 `kind` 없이도 문장만으로 뜻이 서는지 각 영역이 보면 된다.
* **★★[영역 화면 · T65 소유]** **`#enter` 라벨 개명 "월드 입장" → "나루터로 간다"** — T66 이 짓다가 **되돌렸다**.
  하네스 **29개**가 `button:has-text("월드 입장")` 으로 그 버튼을 집고, 그중 `e2e-charsprite` 는
  T66 §1 이 손대지 말라 한 T65 의 파일이다. ⇒ 개명은 **선택자 29개 일괄 교체 + T65 조율**이 있는 날 한 번에.
* **★[ART]** **구운 아이템 렌더가 52장이다**(T66 착지 시 36 → T67 셋 + T72 열셋을 배선해 52) — 이름표는 300키다. T66 부터 렌더 없는 품목은 어디서나
  **점선 빈 칸**으로 뜬다(이모지 폴백 삭제 · 재민 확정 4·5). 지금 점선인 것: **톱·망치**, 가구 다수,
  짐승 스프라이트, econ 자원 갈래(food·meat·cooked_food …).
  ~~⚠"조잡한 석기와 정품이 한눈에 구별돼야 한다"(2026-08-28)~~ — **T72 가 여섯을 구웠고 T66 이 배선했다** ⇒ 그림으로 갈린다.
  목록은 `public/client/43-i-icon.js ICON_RENDERED`(디렉터리와 같은지 `test-itemlabel ⑪` 이 잠근다).
* **[영역 Z 서버 · 문장]** 서버가 **문장 안에** 넣어 보내는 이모지 — ★위 **0-문장** 절이 정본이다(T71 이 표로 셌다).
  T66 의 몫은 여기까지다: 클라는 따로 오는 `emo`·`chEmo` 를 **안 쓴다**(그림은 축에서 고르고 문장은 `ko` 하나).
  ⇒ 남은 것은 문장의 주인이 지운다. `notice` 의 `kind` 선행 조건도 위 절에 있다.
* **[영역 R1/R2 · 산그림]** **세계 캔버스 색은 리터럴로 남는다** — 그게 맞다(보고/T66 §1:
  `ctx.fillStyle = 'var(--x)'` 는 조용히 무시된다). 세계 팔레트를 손보려면 `산그림` 쪽 카드로 열어라.
  판 토큰(`style.css :root`)과 **섞지 마라**. 지키는 것은 `e2e-ui ⑪ⓕ`(실행 중 캔버스 세터 가로채기).
* **[설계 · PM]** **후유증 단계를 서버가 매길 것인가** — 지금 클라는 늘 1단계로 두고 정도(남은 날 · 눌린 상한)를
  **글자**로 말한다(단계는 서버 몫 §8.3). 3단계가 아니므로 비네트도 안 켜진다.
* **[영역 S 스킬]** **스킬 패널의 값이 가짜다** — 레벨·경험치가 화면 전용 상수다(`51-s-side.js` 표).
  T66 이 아이콘만 선 아이콘으로 갈았다. 값의 정본은 아직 없다.
## 0-쉼터. [T62 공용 쉼터 2026-09-03] 다른 영역에 남기는 한 줄

* **[영역 B 신체 · T43/T56 소유]** ✅**"마을 사람들이 당신을 쉼터로 옮겼다"가 이제 참이다** —
  `nearestVillageWake` 의 앵커가 **쉼터 → 도착 지점 → 마을 중심** 사다리가 됐고 `kind` 를 실어 낸다.
  ⚠**마을 안에서 쓰러지면 `listRespawnOptions` 사다리를 안 탄다**(`resolveDowned` 가 이송으로 끝낸다) —
  회부 쓰러짐 "판단 확인 ②"(사유지 우선 vs 가까운 쪽)가 바꾸는 것은 **야생 사망 뒤 깨어날 자리**뿐이다.
  체감이 그만큼 좁다는 실측을 남긴다. 기본은 **A(사유지 우선) 유지**.
* **[영역 V 마을배치]** ★`village_buildings.type` 에 **`shelter`** 가 늘었다(마을당 하나).
  마을 건물 종류를 훑는 코드를 새로 쓸 땐 이 종류를 빠뜨리지 마라(부팅 로드 · 실체화 둘 다 붙어 있다).
  ★그리고 **6×4 움집 실체를 세우는 코드가 한 곳이 됐다**(`_liveHut6x4`) — 세 번째 사본을 만들지 마라.
* **[영역 F 제작건축]** ★`_siteStart` 에 **`spec.claim`** 이 생겼다 — 허가를 묻는 문을 spec 이 갈아 끼울 수 있다.
  기본 분기는 종전 그대로(`_claimFootprint`)이고 **쉼터만** 쓴다("마을 땅이 곧 허가다").
  비슷한 "마을 것"을 더 지을 때 이 문을 재사용해라 — 새 예외를 세계 전체에 열지 마라.
* **[영역 S · 온보딩]** ✅회부 온보딩 **A-3("쉼터가 시설로 없다")를 닫았다.** T19 의 대체 술어는
  **버리지 않았다**(자격 = 쉼터 ∧ 인구 ∧ 자립). §9.3 의 **"신입 자산 보호"는 여전히 안 만들었다**.
* **[영역 V / N]** ⚠**`sim_village_add` 에 대응하는 제거 통지는 여전히 없다**(T19 §7 이 적은 것).
  **안 만들었다** — 호출자가 없는 짝은 죽은 코드다. **마을 필멸 카드가 열릴 때 그 카드가 만든다.**
* **[영역 B/H · 잠]** ★**잠 동사는 이 세계에 없다.** 쉼터는 "있는 곳"이지 "자는 동사"가 아니다 —
  T62 는 동사를 **안 만들었다**. 회복·시간 건너뛰기를 붙일지는 별개 판정이다(§7 유보).
* **[영역 ART]** 쉼터 스프라이트가 없어 **움집 스킨(`hut` 태그)을 그대로 쓴다** — 화면에서 남의 집과 안 갈린다.
  라벨이 "○○ 쉼터"라 이름으로는 갈리지만, 지붕색 하나만 달라도 훨씬 낫다.
* **★★[영역 Z / 하네스 · 모두에게]** **소스를 재는 하네스는 구조로 잘라라 — 글자 수로 자르지 마라.**
  `test-village-found` 이 `foundPlayerVillage` 를 `+6000자`로 잘라 `distDirty` 를 찾고 있었는데,
  T19 이 그 함수에 20줄을 더하자 그 줄이 **7,083자**로 밀려 **제품이 멀쩡한데 빨개졌다**.
  ⇒ 함수 끝까지 보도록 고쳤다(49/0). **주석 한 뭉치가 판정을 밀어낼 수 있다.**
* **★[영역 E 사건장부 · T20 소유]** ⚠**`test-chron-icons` 가 5/3 로 빨갛다 — 상류 것이다.**
  T62 가 **같은 폴더에서** 베이스(`8b4aa94c`)로 되돌려 돌려 확인했다(같은 두 절: `WINTER_SHORT`
  돌연변이와 그 대조). T20 이 겨울 유형을 늘렸는데 그 하네스의 **돌연변이 절이 아직 안 맞는 모양**이다.
  ⇒ 아이콘 표를 늘릴 때 그 절의 기대값도 같이 갱신해야 한다.
* **★★[하네스 · 모두에게]** **화면이 그리는 자리와 알림에 남는 자리는 다르다.**
  촌장 대사는 말풍선 **3줄**로 그려지는데 `showNotice` 는 **첫 줄만** 띄운다(`70-lobby.js`).
  둘째 줄을 `window.__notices` 에서 찾으면 영영 못 본다 — `window.__onbGreet.lines` 를 읽어라.
## 0-프로브. [T75 하네스 프로브 둘 2026-09-03] 유보와 계측기

* ★**[전 영역 · 족보 후보 — PM 승인 대기] "못 잰다"고 적어 둔 절은 계측기가 죽어 있어도 똑같이 조용하다.**
  유보를 남길 때는 그 유보가 **상황** 때문인지 **계측기** 때문인지 가르는 줄을 같이 남겨라.
  이 카드가 그 값을 두 번 물었다: ⓐ T57 의 `hutCount` 는 `__getAllBuildings()` 가 안 내는 `data` 를
  읽어 **항상 0**이었다(픽스처에 NPC 집이 없는 것도 사실이라 결론만 우연히 맞았다).
  ⓑ 안개 절은 훅 호출 줄을 지우면 `cand`=0 이 되어 "대상이 없다 — 못 잰다"로 **초록**을 찍는다.
  ⇒ 두 절 다 *"대상이 화면에 있는데 훅이 안 불렸으면 빨강"* 을 따로 세웠다. 돌연변이 ⓐ 는
  나머지 82개가 전부 초록인 채 **그 줄 하나로** 잡혔다.
* **[영역 R2 · 하네스 — T57 소유였던 것]** `e2e-rooms ⓔ` 의 짝 비교가 **화면 전체**를 훑고 있었다.
  위쪽 띠는 HUD 라 시계가 한 쌍에 4,400화소씩 흔들리고, 판정이 **19화소 차이**로 뒤집혔다
  (실측: 돌연변이 판에서 4,464 vs 4,445 로 유보 줄이 빨개졌다). `e2e-nature` 의 정본 게임 화면 상자
  `[40,200,1360,880]` 로 격리했다 — 잡음 0. ⚠**픽셀 짝 비교를 새로 쓸 때 HUD 를 빼는 상자를 먼저 정해라.**
* ⚠**[영역 R1 · 하네스 — 소유 미정] `e2e-nature` 의 첫 판은 빈 DB 면 마을 51곳을 전수 시딩한다**
  (이 컨테이너 실측 ~8분). 종전부터 그랬고 T75 는 바꾸지 않았다. `waitHttp` 상한 900초라
  느린 기계에선 **시딩 중 타임아웃**이 날 수 있다. 이 하네스 소유 카드가 열리면 `VILLAGE_MAX` 를
  볼 만하다(하네스·프로브용 상한 env 는 이미 있다 — `probe-roof.js` 가 그렇게 쓴다).
* **[카드 문면]** 지시서가 부른 `scripts/e2e-fog.js` 는 **없다**. 안개 절의 주인은
  `scripts/e2e-nature.js`(`[7] ⓕ` · `[7b] ⓖ` · `[7c] ⓖ2`)이고 `fogprobe` 는 클라 훅(`__fogGateProbe`)
  이름이지 파일이 아니다. 새 파일을 만들면 절이 두 곳으로 갈라지므로 거기 붙였다.

## 0-알림. [T78 알림 `kind` 2026-09-03] 다른 영역에 남기는 한 줄

T71 ⓑ 판정의 답을 **경계 하나**로 넣었다: `zone.js send()` 가 알림을 내보내기 직전
`server/notice.js` 가 접두 이모지를 `kind` 로 옮기고 텍스트에서 이모지를 걷는다.
**호출부 322곳은 한 글자도 안 고쳤다.**

* **[전 영역] 이제 알림에 이모지를 적어도 화면엔 안 나간다.** 원문 청소는 급하지 않다 —
  지울 때 `kind` 를 손으로 달면 경계가 그걸 **존중한다**(`{ type:'notice', kind:'board', text:'…' }`).
  `kind` 8종: `village` `gather` `fishing` `craft` `board` `rescue` `combat` `dev`(+`info`).
  ⚠**새 이모지를 접두로 쓰면** `server/notice.js` 의 `KIND_OF` 에 한 줄 넣어라 — 없으면 `info` 로 떨어진다.
* **[영역 클라 · T66/T68] `kind` 가 이미 프레임에 실려 온다.** `30-n-net.js:1600` 은 아직 `text` 만 읽는다 —
  선 아이콘을 그릴 재료는 **오늘부터 있다**(`msg.kind`). 클라 자체 문장의 이모지도 그쪽 몫이다.
* ★★**[영역 B·온보딩·핸드오프 — 규칙을 넘은 자리] 남의 하네스 세 줄을 내가 고쳤다.**
  지시서는 "남의 영역 하네스는 표 + 회부"였는데, 셋 중 `test-handoff-body` 의 `waitNotice()` 는
  **시한 없는 promise** 라 접두 이모지가 사라지는 순간 **영영 멈춘다**(야간 전수 러너를 통째로 먹는다).
  빨강은 보고로 갚을 수 있어도 **멈춤은 그럴 수 없다** ⇒ 셋 다 한 줄씩 고쳤다(검사의 뜻 그대로 · `★[T78]` 주석):
  - `scripts/test-handoff-body.js` L121 — `startsWith('🌀'|'🌊')` → `kind` + `/텔레포트|물/`
  - `scripts/e2e-downed.js` L145 — `/🌀 텔레포트/` → `/텔레포트/`
  - `scripts/e2e-emptystart.js` L194 — `/🤏/` → 채집 알림 세 갈래의 **말**
  영역 소유를 엄히 지키려면 되돌리고 각 세션에 넘겨도 된다 — 다만 첫 줄은 되돌리면 **다시 멈춘다**.
* **[영역 V] `villages.js` 화면 문장 27** — 게시판 본문처럼 `notice` 가 아닌 자리는 이 경계가 못 걷는다.
* ⚠★**[영역 클라 · T66/T61] `onbLobbyInit is not defined` — 적재 순서가 깨졌다.**
  `30-n-net.js:232` 가 `70-lobby.js:202` 의 함수를 부르는데 실클라에서 정의를 못 찾는다.
  `e2e-downed` 가 그것 때문에 셋 빨갛다(재접속 풀피·쓰러짐 패널·페이지 에러 0).
  ★**베이스(`afe8a0be`)에서 돌리면 더 일찍 죽는다**(`[A] 존 입장` 실패 + `TypeError … reading 'x'`) —
  T78 트리가 오히려 더 멀리 간다. **상류 결함이지 이 카드 것이 아니다**(내 diff 는 클라 무접촉).
* **★[영역 Z 서버 · 한 줄]** **자연물 종류 → 동사 이름표가 서버에 없다**(T82 §0-ⓐ 실측). `tryGather` 는
  나무/바위/약초를 갈라 처리하면서 그 갈래에 **이름을 안 붙인다**(문장은 전리품 이름으로 말한다).
  그래서 T82 가 `public/client/46-h-verbs.js` 에 한 단어 표(`tree:'벌목' · rock/ore:'채굴' ·
  herb/berry_bush:'채집' · water_pool:'물 마시기' · meteorite:'채굴'`)를 뒀다 — **사본 후보**다.
  고침은 서버 한 줄: `welcome` 에 `resourceVerbs` 를 실으면 그날 이 표를 지운다(T55 의 `itemLabels` 선례).
* **[영역 Z · 지목]** **`gather` 는 대상을 못 받는다** — `tryGather` 가 `GATHER_RANGE` 안에서 제일 가까운
  자원을 고른다. T82 의 메뉴는 그래서 **누른 것이 제일 가까울 때만** 동사를 낸다(아니면 안 띄운다).
  `gather{resId}` 를 받게 하면(기본값 = 종전 최근접) 그 제약이 사라진다 — T68 의 `feed(pid)` 와 같은 모양.

## 0-날씨. [T93 비·눈 2026-09-04] 강수 정본 · 훅 이름 충돌

* ~~★★**[영역 K — 설계] 강수 정본이 세계에 없다.**~~ ⇒ **[T98 2026-09-05 닫힘]** `weather.precipAt` 신설 ·
  `weatherFor` 에 `precip` 한 줄 · 화면은 예언대로 **손 안 대고 켜졌다**(`37-r1-weather.js` 무접촉).
  ⚠단 **하루 단위**다(시간대 창은 아래 T98 회부 5). 아래 원문은 족보로 남긴다.
* ★★**[영역 K — 설계 · 닫힘] 강수 정본이 세계에 없다.** `weatherFor(p)` 는 `cold·ko·emo·night·tempC·
  shelter·cut·insC·wind·exp` 뿐이고, `server/weather.js` 는 기온 곡선(`tempAt`·`outdoorCold`·
  `devCOf`·`hintOf`), `server/wind.js` 는 바람(`seasonWind`·`windVec`·`exposureAt`)만 낸다.
  `server/`·`sim/` 전수 `precip|rain|snow|강수` → **0건**(걸린 건 `terrain`·`drain`·`grain`).
  ⇒ *"오늘 비가 오는가"* 를 내는 함수가 필요하다. 무엇으로 정할지(계절·기온 이탈·바람·지형)는
  **설계 판단**이라 R1 카드가 못 정한다. T93 은 짐작으로 지어내지 않았다 — `zone.js elevKmAt` 의
  *"대리값을 넣지 마라, 그건 지형이 아니라 소원이다"* 와 같은 자리다.
  ★그 함수가 서면 `weatherFor` 에 `precip` **한 줄**이면 되고, **화면은 손 안 대고 켜진다**
  (`37-r1-weather.js` 가 이미 `wx.precip` 을 읽는다).
* ★★**[영역 공통 · 하네스] `window.__x = …` 재대입 충돌을 아무도 안 본다.**
  `test-client-globals` 는 ① *선언* 충돌과 ② *브라우저* 전역 충돌만 본다. 조각이 `window` 에 다는
  훅 이름은 **아무 감시도 없다.** T93 이 진단 훅을 `__wxDbg` 로 지었다가
  `34-m-renderloop.js:110` 의 **날씨 사건**(🌵가뭄·🌈풍요) 훅을 첫 프레임에 **조용히 덮었다**
  — `e2e-tilestate`·`e2e-village` 가 그걸 읽는다. 잡은 건 감사기가 아니라 **하네스가 터진 것**이다.
  ⇒ 조각 27개를 훑어 `window.<이름> =` 를 모으고 두 파일 이상이 같은 이름에 대입하면 잡는 검사 하나.
  (기준선 표가 필요할 것이다 — 이미 여러 이름이 여러 자리에서 다시 달릴 수 있다.)
  T93 은 작은 카드라 **자기 하네스 안에 표적 한 줄**(`e2e-weather ⓐ`)만 두고 회부한다.
* **[카드 문면]** 카드가 진단 훅 이름을 `__wxForce` 로 줬는데 `__wx*` 는 위 축이 이미 쓰는 이름표다.
  T93 은 `__rainForce`/`__rainDbg` 로 갈랐다(짝을 한 이름으로 묶으려고). 되돌리는 건 한 줄이다.
* **[영역 R1 · 다음 판]** 눈 **쌓임**(지면 색·발자국)은 지면 정본을 만지는 일이라 이 층 밖이다.
  구름 그림자·번개·안개 결합·젖은 지면 색·비 소리·비가 플레이에 미치는 영향(추위 K)도 그대로 회부.
* **[영역 R1 · 손잡이]** 눈은 비의 **3.6배**다(같은 획 수 · 실측 0.48ms vs 1.74ms — 획이 굵어 래스터
  면적이 크다). 지금은 한 상한(`WX_MAX 1400`)으로 둘을 같이 묶었고 비싼 쪽도 자(밤 오버레이) 아래다.
  눈이 무겁게 느껴지는 날이 오면 **종류별 상한**이 바로 쓸 수 있는 손잡이다(자와 획당 값은 이미 잰다).

## 0-자연물. [T97 자연물 재굽기 2026-09-05] 다른 영역에 남기는 것 · 다음 카드

* ★★**[영역 ART · 다음 카드] 바위 12장은 굽는 코드가 저장소에 없다.**
  `public/assets/nature/rock01..06`·`mossrock01..06` 은 `~/Mini/rock_render.py` 가 구웠고
  그 스크립트는 **저장소 밖**이다. 씬이 지금 정본이 아니고(`tree_render.py` 계열 · SAMPLES 48 ·
  512² 고정 프레임 · `leaf.png` 외부 의존), 앵커 JSON 에도 안 들어간다(클라가 bbox 바닥으로 앵커).
  `ore01..06` 은 그 바위에서 `scripts/ore-outcrop.py` 가 PIL 로 파생하므로 **바위를 따라 움직인다**.
  ⇒ 5.0.1 재굽기에 못 넣었다 — 모델 본문이 없으니 **편입이 아니라 새로 짓는 일**이다.
  ★한 카드로 묶으면 맞다: 막돌·이끼바위 12를 `nature_render.py` 문법으로 다시 짓고(공용 `ico`·
  `bumped_mat` 이 이미 `props` 의 막돌을 만든다), `ore-outcrop.py` 를 다시 돌리면 18장이 함께 산다.
  지금은 `icons.lock.json._기계_예외` 가 18장의 이름과 까닭을 적어 두고, `test-props ⑤` 가
  그 집합을 **못 박아** 지킨다(하나라도 늘거나 줄면 빨개진다).
* ★★**[영역 ART · 다음 카드] 밭 타일은 세계 패스로 구워야 이음새가 맞는다.**
  T79c 32장은 `render_icon_pass`(아이콘 시선 `ISO_DIR` · z 압축 없음 · FLIP 없음)로 굽고 64px 로
  줄인 **정사각 그림**이다. 셀 다이아(64×32)가 아니라서 크기를 키워도 이음새가 안 맞는다
  (실측표 = 보고 T97 §0-ⓒ · 그림 `산그림/디자인B/경작지_이음새.png`).
  PM 판정("타일로 덮는다")은 층을 하나로 만드는 데까지는 이뤘고, **띠가 이어져 보이는 것**은
  세계 패스 재굽기가 있어야 한다. 그때 `render_world_pass(…, ppu_mul=1, ss=3)` 과
  셀 경계에 맞춘 `TILE` 값이 필요하다 — T67 이 벽·울타리에서 푼 그 규약이다.
* **[영역 ART · T96 뒤] `char_render.py` 5.0.1 재굽기 + `sensor_fit`.**
  자연물이 15/38 이었다. 캐릭터 시트도 세로가 긴 프레임이 있으면 같은 자리에서 잘려 있다.
  T96(세션2)이 `char_render.py` 를 크게 만지는 중이라 그 뒤. 옷 재질 이중화(T95 회부 1)도 같은 카드.
* **[영역 ART · 다음 카드] `building_render.py` 5.0.1 재굽기.** 남은 4.0.2 자산이다.
* **[영역 ART · 작은 것] 자연물 재질 datablock 이름에 접두가 없다.**
  `icon` 은 `i_`, `props` 는 `p_/f_/t_` 로 갈라 놨는데(`test-render-common ③`) `nature` 의 22개는
  `bark_pine`·`lf_oak`·`grass` 처럼 맨이름이다. 파일마다 다른 프로세스라 지금 부딪히진 않는다.
  이름을 바꾸는 것은 화소를 안 바꾸지만 **대조군 증명 사슬 밖에서 하면 안 된다** — 다음 자연물
  카드에서 굽기와 같이 하면 한 번의 대조로 끝난다.
* **[영역 생활층] 침상은 아직 시각 전용이다.** 서버에 건물 행이 없어 해체·리스폰·NPC 취침과
  이어져 있지 않다(클라 주석의 *"물리 실체 침대 → 생활 층 이관 묶음"*). 그날 오면 `DECOR` 표의
  `bed` 가 `PROPS` 로 옮겨가고 아이콘이 하나 붙는다 — 표를 둘로 가른 이유가 그것이다.
* **[영역 ART · 재민 눈] 상추/아욱 26px 구별** — T79b 부터 남은 것. 그대로 회부.

## 0-범프. ★★★[T101 바위·밭 2026-09-05] 범프가 죽어 있었다 · 남은 자산 재굽기

* ★★★**[영역 ART · 다음 카드 · 급함] 블렌더 5.0 이 `Bump` 노드 `Distance` 기본값을 1.0 → 0.001 로
  바꿨다 — 범프가 1000분의 1 로 죽는다.** T79 의 "4.0.2→5.0.1 6~19/255"·T97 의 "5.53/255"는
  **4.0.2 의 얼룩이 아니라 5.0.1 의 범프 상실**이었다(T101 §1 · 막돌이 드러냈다 — 표면이 100%
  절차 범프라 제일 먼저 티가 났다. `Distance` 만 1.0 으로 되돌리자 4.0.2 배포본의 돌 표면이 돌아왔다).
  `render_common.BUMP_DIST = 1.0` 을 두고 범프 만드는 자리마다 명시했다.
  ⚠**이 카드가 다시 구운 것만 복원됐다**: 자연물 50 · 밭 32 · 농지 아이콘.
  **아이콘 144 · 가구 21 은 아직 복원 전 그림**이라 코드와 배포본이 갈려 있다.
  ⇒ `icon_render.py`·`props_render.py` 재굽기 한 카드가 필요하다. `icons.lock.json._범프` 가 상태를 적어 뒀고
  `test-props ⑤` 가 그 줄의 존재를 지킨다. **`building_render.py`·`char_render.py` 도 같은 병**이라
  그 재굽기 카드들과 묶으면 한 판이다(아래).
* **[영역 ART · 다음 카드] `building_render.py` 5.0.1 재굽기 + `sensor_fit` + 범프 복원.**
  남은 4.0.2 자산이고, 위 범프 문제도 같이 걸린다. 자연물이 15/38 세로 잘림이었으니 건물도 재야 한다.
* **[영역 ART · T96 뒤] `char_render.py` 5.0.1 + `sensor_fit` + 범프 복원 + 옷 재질 공용 모듈.**
* **[영역 ART · 작은 것] `assets-src/legacy_mac/rock_render.py` 의 덤불·약초 절은 이제 죽은 코드다.**
  `nature_render.py` 가 대체했고(bush01~06 · herb01~06), 그 절만 씨앗 없는 `random.randint` 를 쓴다.
  원본 보존이 목적인 폴더라 **지우지 않았다** — README 가 "버려라" 라 적어 뒀으니 그대로 둔다.
* **[영역 ART] 자연물 재질 datablock 이름 접두 없음**(T97 회부) — 그대로.
* **[영역 ART · 재민 눈] 상추/아욱 26px 구별** · **종별 34세트** · **침상 생활층 연동** — 그대로.
* **[영역 공통 · 하네스] `e2e-*` 는 앞 판이 남긴 포트를 안 본다.** T101 에서 `e2e-nature` 가
  79/7 로 빨갛게 나왔는데 원인은 코드가 아니라 **내가 죽인 앞 판의 zone 프로세스가 3020 을 쥐고 있던 것**
  (`EADDRINUSE` 한 줄이 로그 17행에 있었고 나머지 7 실패는 전부 "0px" 였다). 깨끗이 다시 도니 86/0.
  ⇒ `e2e-*` 기동부가 포트를 잡을 때 **이미 쥔 프로세스가 있으면 먼저 죽거나 알리게** 하면
  "내 코드가 깼나" 를 헛되이 뒤지는 판이 없어진다. T101 은 작은 카드라 회부만 한다.

## 0-범프끝. [T103 범프 복원 완료 2026-09-05] 남은 것

* **[영역 ART] 범프 복원은 끝났다.** 여섯 무리(아이콘 144 · 가구 21 · 밭 32 · 자연물 44 · 나무 12 ·
  건물 12 = 265장)가 전부 `BUMP_DIST = 1.0` 로 구운 그림이고, `icons.lock.json._범프` 가
  "복원 완료" 라고 적는다. **다시 구울 때 `Distance` 를 빼먹으면 `test-render-common ⑦` 이 먼저 빨개진다.**
* **[영역 ART · 다음 판] 아직 4.0.2 배포본인 것 셋** — 산 스프라이트(`bake-mountain.py`) ·
  지면 질감(`bake-terrain-tex.py`) · 다리(`bridge_render.py`). **코드에는 `Distance` 를 이미 채웠으니**
  다시 굽기만 하면 된다. 셋 다 배포본이 4.0.2 라 지금은 범프가 살아 있어 급하지 않다.
  ⚠단, 누가 그 셋 중 하나를 5.0.1 로 다시 구우면 **그때 그림이 좋아진다**(나빠지지 않는다) —
  이제 코드가 옳기 때문이다. 그래도 잠금표에 안 들어 있으니 재굽기 카드에서 넣어라.
* ★**[영역 ART · T96 세션2에 전달] `char_render.py` 는 `ShaderNodeBump` 를 **하나도 안 만든다**(실측 0곳).
  그래서 이 병에 안 걸렸다.** T96 이 5.0.1 로 구울 때 범프 걱정은 없다 —
  다만 `sensor_fit` 은 별개다(자연물이 15/38 이었다). 세로가 긴 프레임이 있으면 T97 §1 문법으로 먼저 재라.
* **[영역 ART] 건물·자연물 재질 datablock 이름에 접두가 없다** — `icon` 은 `i_`, `props` 는 `p_/f_/t_`
  인데 `nature`(22개) · `building`(11개) 은 맨이름이다. 파일마다 다른 프로세스라 지금 안 부딪힌다.
  이름 바꾸기는 화소를 안 바꾸지만 **대조군 증명 사슬 밖에서 하면 안 된다** — 다음 재굽기와 같이.
* **[영역 공통 · 하네스] `e2e-*` 포트 선점 감지**(T101 회부) · **legacy 덤불·약초 절 죽은 코드** ·
  **상추/아욱 26px** · **종별 34세트** · **침상 생활층 연동** — 그대로.
## T90 (2026-09-04) — 닫은 둘 · 새로 올린 넷

**닫았다:** 위의 `[영역 Z 서버 · 한 줄]`(동사 이름표)과 `[영역 Z · 지목]`(`gather{resId}`) 둘 다
이 카드에서 실행했다. 클라 한 단어 표는 **삭제**됐고(`e2e-verbs ⑪` 이 소스 전수로 지킨다),
T82 의 "최근접 아니면 빈 메뉴" 갈래도 사라졌다.

* ★**[영역 Z 서버 · 사본] `zone.js` 의 `RESOURCE_HP` 는 `chunk.js RESOURCE_HP_TABLE` 의 낡은 사본이다.**
  `meteorite` 가 빠져 있다(스폰은 `chunk.js` 로 하고 `zone.js` 는 제 표로 hp 를 되짚는다).
  T90 은 **정본(`chunk.js`)만** 보고 동사 표를 맞췄다 — 고침은 `zone.js` 에서 제 표를 지우고
  `chunk.js` 것을 부르는 한 줄이다. 이 카드에서 안 건드렸다(§1 접점 2 를 넘는다).
* **[설계] NPC · 성한 사람 우클릭 동사** — 아직 빈 배열이다(메뉴를 안 연다). 무엇이 뜰지는 설계 몫.
* **[영역 클라] `data-action` 버튼 정리** — 좌클릭 사슬이 아직 이름 없는 자리로 보내는 곳들.
* **[영역 클라] 알림 스택 UI** — 지금은 토스트 하나가 앞의 것을 덮는다. `kind` 그림이 생겼으니
  여러 개를 세로로 쌓을 재료는 있다(`window.__notices` 가 이미 40개를 들고 있다).
* **[영역 클라 · `pickAt`] 겹칠 때 "가장 가까운 것"이 아니라 "먼저 만난 것"을 고른다.**
  `46-h-verbs.js` 의 자연물 층은 `c.resources` 를 돌다 첫 히트에서 반환한다 — 나무와 광맥이
  겹쳐 있으면 같은 점을 눌러도 판마다 다른 것이 잡힌다(T90 하네스가 실측: `ore` 를 겨눴는데
  메뉴가 "벌목"이었다). 화면 규칙상 **커서에 제일 가까운 것**이 옳다. 사람 층도 같다.
  고침은 층마다 "제일 가까운 것"을 고르는 몇 줄 — T90 접점(2)을 넘어 안 건드렸다.
## 0-캐릭터. [T96 먹선·모캡 2026-09-05] 축이 틀린 클립 둘 · 몸 갈아 끼우기

* ★★**[영역 R2/ART] `swing`·`aim` 이 걸음과 **같은 축 결함**을 안고 있다.**
  T96 이 `depsgraph` 로 쟀다: 리그의 `rx` 는 뼈를 캐릭터의 **좌우(±y)** 로, `rz` 가 **앞뒤(±x)** 로 눕힌다
  (캐릭터는 +x 를 본다). 그런데 `_pose_walk` 는 다리를 `rx` 로만 흔들었다 — 걷는 게 아니라
  **가랑이를 옆으로 벌린 것**이었다(넓적다리 월드 방향의 앞뒤 성분이 **정확히 0**).
  걸음·달리기는 모캡으로 닫았지만 **`_pose_swing`·`_pose_aim` 은 그대로 남았다**:
  `swing` 은 `uarmR` 을 `rx −118° → +62°` 로 움직이므로 도끼질이 **몸 옆 관상면에서** 일어나고,
  `aim` 은 겨눈 팔이 앞이 아니라 **오른쪽 옆**으로 뻗는다. 이 카드 밖이라 한 글자도 안 건드렸다.
  ⇒ 고치는 길 둘: ⓐ 축만 바꿔 적는다(`rx`↔`rz` · 공짜지만 눈으로 다시 잡아야 한다)
    ⓑ CMU 에서 도끼질·활 클립을 골라 같은 리타깃을 태운다(`mocap_retarget.py` 가 이미 선다).
* **[영역 ART] `idle` 도 사인 그대로다** — CMU 에 맞는 '서 있기' 클립을 고르지 않았다(§0-ⓓ).
  걸음과 달리 축 결함은 없다(`spine`·`uarm` 을 아주 조금 흔드는 호흡이라 평면이 안 중요하다).
* **[영역 ART] MakeHuman(MPFB) 몸** — 카드 §4 그대로 다음 판. **이제 잴 자가 있다**:
  리타깃이 마디의 **월드 방향**으로 도니, 뼈 길이·비례가 바뀌어도 같은 표를 그대로 쓴다.
* **[영역 ART · T97 뒤] 자연물·소품 먹선 공용 편입** — `scripts/ink_post.py` 는 캐릭터만 부른다.
  `render_common.py` 는 T97(세션8)이 만지는 중이라 무접촉으로 뒀다(하네스가 import 0 을 상시 검사).
  T97 이 끝나면 두 함수를 그리 옮기고 `nature_render`·`props_render` 가 같이 부르면 된다.
* **[영역 클라 · 알림]** 캐릭터 시트 **71장 바이트가 전부 바뀌었다**(포즈+후처리). 앵커도 0.37px
  움직였다(SS 243.9 → 245.0). 프레임 크기·ppu·프레임 수·클립 수는 무변이고 클라는 앵커를 메타에서
  읽으므로 코드 접점은 0 이다 — 다만 **시트를 캐시하는 무엇이 있으면 그건 갱신돼야 한다.**
* **[크레딧]** CMU 모캡은 표기 의무가 없고 감사 표기를 권한다(`assets-src/mocap/README.md` 에 원문).
  크레딧 화면이 생기면 그 문단을 그대로 넣어라.
* ★★**[영역 ART · T97 세션8] `ShaderNodeBump` 의 Distance 가 기본값에 매달려 있다 — 여섯 파일.**
  bpy **5.0.1 기본값 0.001**(4.0.2 는 1.0 · 이 컨테이너에서 노드를 만들어 읽었다). 아래 여섯이
  Bump 를 만들면서 `Strength` 만 세우고 **Distance 를 안 세운다** ⇒ 기계 판이 바뀌면 범프 깊이가
  **1000배** 달라진다: `render_common.py`(2 · `striped_mat`·`bumped_mat`) · `nature_render.py`(2) ·
  `building_render.py`(1) · `bridge_render.py`(2) · `bake-mountain.py`(2) · `bake-terrain-tex.py`(3).
  ⚠**`striped_mat(dist=3.0)` 의 `dist` 는 Bump 의 Distance 가 아니다** — `ShaderNodeTexWave` 의
  Distortion 이다(`render_common.py:112`). 이름이 겹쳐 보이지만 다른 값이다.
  ⚠**`render_common.BUMP_DIST` 는 아직 없다**(전수 grep 0건) — 그 상수를 세우는 것이 그 카드의 일이다.
  ★**T96(캐릭터)은 무영향**: `char_render.py` 는 `ShaderNodeBump` 0개이고 셰이더 노드를 아예 안 만든다
  (`nodes.new(` 0회 — `mat()` 이 기본 Principled 의 입력만 채운다). 그래서 이 카드는 다시 안 구웠다.

## 0-캐릭터2. [T107 도끼질 축 · EXR 2026-09-05] 남은 것

* **[영역 ART] MakeHuman(MPFB) 몸** — 다음 카드. T96 의 리타깃 표가 마디의 **월드 방향**으로 도니
  뼈 길이·비례가 바뀌어도 같은 표를 그대로 쓴다.
* **[영역 ART] `idle`·`swing`·`aim` 모캡** — CMU 에 도끼질·활 클립이 마땅치 않아 사인을 유지했다.
  축은 T107 이 바로잡았으니, 클립이 생기면 `mocap_retarget.py` 에 클립만 더하면 된다.
  ★`idle` 은 축이 **맞다**(호흡을 관상으로 아주 조금 흔든다 — 2.5°). 고칠 게 없다.
* **[영역 ART · T106 뒤] 자연물·소품 먹선 공용 편입** — `ink_post.py` 는 이제 후처리 본문
  (`post_all`)과 float 입출력을 다 갖췄다. `render_common` 이 T106 에서 풀리면 그대로 옮기면 된다.
* **[영역 ART] EXR 을 다섯 클립 전부로** — 이 카드는 swing·aim 둘만 남겼다(4.4MB).
  나머지 셋은 `python3 scripts/char_render.py -- --exronly` 한 번(≈1시간)이면 채워지고,
  그때부터 **모든 클립**의 세기 조정이 분 단위가 된다. 배포 PNG·메타는 그 명령이 안 건드린다.
* **[영역 공통] 상자 못박기를 자연물·건물에도?** — `char_render` 만 얼렸다. 다른 굽기 스크립트도
  포즈·형상이 바뀌면 같은 문제를 만난다(프레임 규격이 조용히 움직인다). 그 카드들 몫.
## T109 (2026-09-05) — 닫은 하나 · 정정 하나 · 새로 올린 셋

**닫았다:** `[영역 B] HP 자연 회복이 화면에 안 실린다`(T44 B-4 · T83) — ★**실은 T61 이 이미 닫았다**
(`gauges.hp`). T109 §0 이 그걸 재서 확인했고, 다시 열리지 않게 `e2e-hp ②` 가 지킨다
(`gauges` 의 hp 칸을 지우면 화면이 `30 → [30,30,30,30,30,30]` 으로 굳는 것을 실측으로 확인).
남아 있던 진짜 결함은 **hp 를 쓰는 자리가 열 군데로 흩어져 규칙이 제각각**인 것이었고, 그건
`setHp(p, v, why)` 한 문으로 닫았다(`인계/N-네트워크.md` 1-표5).

* ★**[영역 하네스 · 문서 거짓] `e2e-thirst`·`e2e-cold` 의 주석이 낡았다.**
  둘 다 *"서버는 `player_damaged` 로만 hp 를 보내고 자연 회복은 브로드캐스트가 없다 ·
  `self.hp` 는 welcome 때 한 번뿐"* 이라고 적어 뒀는데, T61 뒤로 **사실이 아니다**(`gauges` 가 나른다).
  판정 코드(단조 감소 읽기)는 여전히 옳아서 **안 고쳤다** — 남의 영역 하네스이고, 멈추지도 않는다.
  다만 다음에 그 주석을 읽는 사람이 없는 결함을 좇게 된다 ⇒ 그 영역 카드에서 두 줄만 고치면 된다.
* **[영역 Z · 이름] `player_damaged` 는 이제 "hp 가 바뀜"이다** — 회복도 구조도 이 타입으로 간다.
  `hp_changed` 로 개명하면 이름이 사실과 맞지만 **클라 접점이 넓다**(핸들러·재접속·부활 사슬).
  T109 는 새 타입 0 을 지켜 이름을 안 건드렸다.
* **[영역 마을] `server/villages.js` 의 `p.hp = …` 넉 줄**(2851 · 2878 · 2938 · 4900)은 sim NPC 의 hp 다.
  이 카드의 문은 `zone.js` 소관이라 안 건드렸다(검증 규약도 `zone.js` 만). 같은 문으로 모을지는
  그 영역 몫이다 — 화면에 실릴 일이 없어 급하지 않다.
* ★**[영역 PM · 맥 클론 한 줄] `gc.auto 0` 으로는 `maintenance.lock` 이 안 막힌다.**
  T91 이 `.lock` 진범을 auto-gc 로 잡고 `gc.auto 0` 을 걸었는데, T109 의 push 직후에도
  `.git/objects/maintenance.lock` 이 하나 생겼다(10:01:36 · `maintenance.auto` 는 비어 있다 = 기본 켜짐).
  **ref 를 안 잡아** 재민의 git 을 막지는 않지만, "push 뒤 `.lock` 0" 을 적으려면
  `git config maintenance.auto false` 한 줄이 더 필요하다. 남의 클론 설정이라 안 걸었다.

## 0-범프끝2. [T106 산·지면·다리 2026-09-05] 남은 것

* **[영역 ART] 범프 아크가 닫혔다.** 배포하는 그림 **335장 전부** `pip bpy 5.0.1` 산이고
  `icons.lock.json` 이 아홉 무리를 잠근다(`_기계` = "4.0.2 산출물 0"). 다시 굽는 날
  `Distance` 를 빼먹으면 `test-render-common ⑦` 이 먼저 빨개진다.
* ★**[영역 ART · 작은 것] `bake-terrain-tex.py` 의 `topdown` 갈래는 지금 아무것도 배포하지 않는다.**
  `bake_water`·`bake_dirt`·`bake_rock` 셋만 범프를 쓰는데 배포본 `*_angled.png` 는 그 갈래가 아니다.
  죽은 갈래인지(지우기) 살릴 것인지(지면 2차) 판단이 필요하다 — 세션이 못 정한다.
* **[영역 ART] 둥근 산 `mt_R*` 40종이 굽는 표에는 있는데 배포본엔 없다**(`SPECS` 85 ↔ 배포 45).
  재민 2026-08-07 "둥근 버전도 만들어서 여러 개 혼합해서 쓰자 · 한반도는 둥근 거 위주로" 가
  아직 안 닿았다는 뜻이다. 굽고 배치 손잡이를 여는 카드가 따로 필요하다(굽기 40장 ≈ 50분).
  ⓘ 주봉 `mt_X*` 3종도 같다(`MT_X=1 MT_RES=4096` 별도 굽기).
* ★**[영역 공통 · 하네스] `e2e-*` 를 연달아 돌리면 거짓 빨강이 난다 — 처방까지 실측했다.**
  T106 에서 픽셀 하네스 열여섯을 돌리자 **넷이 빨갰다**(`charsprite` 80/1 · `cutaway` 제한시간 ·
  `terrain` 중간 `ECONNREFUSED` 뒤 충돌 · `zoom` 왕복 화소 차 9.717/문턱 1.0).
  **넷 다 단독으로 다시 도니 초록**이었고(81/0 · 15/0 · 33/0 · 0.107), 깨끗한 `origin/main`
  에서도 같았다 — 즉 내 변경과 무관하다. T101 의 `EADDRINUSE` 와 같은 자리, 세션이 겪은 것만 세 번이다.
  ⓘ **처방:** 러너에 ① 3010/3020 이 **빌 때까지 대기** ② 하네스가 끝나면 듣고 있는 자식 정리
  ③ 6초 쉼 — 이 셋을 넣으니 1차에서 빨갰던 `mtocc`/`mtfuzz` 가 **연속 실행에서도** 18/18 · 10/10
  으로 붙었다. `run-regress.sh` 가 이 셋을 갖는 것이 맞다(세션이 러너를 못 고친다).
  ⓘ 제한시간도 같이 봐야 한다 — `e2e-cutaway`·`e2e-terrain` 은 마을 50곳 시딩을 존마다 다시 해서
  2코어에서 **10분을 넘긴다**. 600초 뚜껑은 짧다.
* ★**[영역 공통 · 하네스] `e2e-charsprite` ⑤ 가 벽시계로 기다린다 — 그 파일이 스스로 금지한 문법이다.**
  바로 위 주석이 족보 ㊾ 를 인용해 "시계로 기다리지 마라 — 조건으로 기다려라" 라고 적어 놓고,
  정작 그 판정만 `B.keyboard.down('KeyS'); await sleep(1400);` 뒤에 `clip` 을 읽는다.
  부하가 걸리면 `clip=idle speed=0` 으로 빨개진다(T106 실측). 앞의 폴링(최대 8초)과 같은 꼴로
  **`clip` 이 walk 가 될 때까지 폴링**하면 끝난다. char 영역(T96) 손이라 세션이 안 만졌다.
* ★**[영역 공통 · 기구] 픽셀 하네스 열여섯 중 커밋된 "정답 그림"과 대조하는 것은 0개다.**
  전부 한 판 안에서 찍은 두 장을 견준다(전/후 · 켬/끔 · 같은 시각 두 프레임). 저장소 그림을 읽는
  유일한 `e2e-nature` 도 읽는 건 기하(앵커↔PNG 크기)지 생김새가 아니다.
  ⇒ 좋은 성질이다(기준 스냅샷을 갈아 끼워 회귀를 감출 길이 없다). 그러나 **뒤집으면 구멍**이다 —
  **재굽기가 생김새를 바꿔도 하네스는 아무도 모른다.** 지금 생김새를 붙드는 것은
  `icons.lock.json`(sha1 335키)과 사람 눈뿐이고, 잠금표는 "바뀌었다"만 말할 뿐 "나빠졌다"는 못 말한다.
  판단이 필요하다 — 소수 정본 그림을 두고 대조할지, 눈 대조를 카드 규약으로 남길지.
* ★**[영역 공통 · 하네스] 굽는 스크립트를 부르는 하네스가 0개다.**
  `bake-terrain-tex.py` · `bake-mountain.py` · `pack-mountain.py` · `mountain_anchors.json` 을
  `grep -l` 로 뽑으면 아무 하네스도 안 나온다(공통 §2 의 "접점 파일" 규약이 굽는 쪽에선 빈손이다).
  `hash(str)` 비결정성이 **두 번** 여기서 나왔는데(T101 `rock_render`·T106 `bake_flat_angled`)
  둘 다 하네스가 아니라 **대조군 재굽기**가 잡았다. 굽는 스크립트의 결정성만 보는 작은 하네스가
  하나 있으면 이 갈래가 닫힌다(같은 씨앗 두 번 → 같은 바이트).
* **[영역 ART] 자연물·건물 재질 datablock 접두** · **옷 재질 공용(T96 뒤)** · **그루터기 그림** ·
  **종별 34세트** · **침상 생활층** · **legacy 덤불·약초 죽은 코드** · **상추/아욱** — 그대로.
## T113 (2026-09-05) — 닫은 하나 · 새로 올린 둘

**닫았다:** `[영역 클라] 알림 스택 UI`(T90 이 올린 것) — `#notice` 가 줄 스택이 됐다
(`인계/H-HUD신체.md` 1-알림 · N=3 은 캔버스에서 유도 · `e2e-ui ⑬` 이 지킨다).

* ★**[영역 ART · 캔버스 갱신 제안] 캔버스의 알림 자리는 상단 가운데인데 실화면은 좌상 HUD 안이다.**
  `디자인B_HUD.jpg` 는 알림을 x 539~1119 · y 17~66 (상단 가운데)에 그렸다. 실화면의 `#notice` 는
  `#hud`(좌상) 안이라 **왼쪽 레일이 첫 글자를 덮는다**(T113 대조 스크린샷에서 "마을에" 의 "마" 가 잘렸다).
  스택 이전에도 그랬다 — 스택이 되니 세 줄 다 잘려 눈에 띈다. 그리고 캔버스의 슬롯은 **한 메시지 2줄**
  크기(50px)라 스택을 안 그렸다 ⇒ 갱신할 때 **셋(67px)** 로 그리면 코드와 그림이 같아진다.
* **[영역 클라] `#notice` 의 세로 패딩이 캔버스와 다르다** — 캔버스 11px · 실화면 6px.
  N 과 무관해서 안 건드렸다(HUD 세로 자리가 밀린다). 캔버스 갱신 때 같이 정하면 된다.

## T118 (2026-09-05) — 닫은 하나 · 새로 올린 둘

**닫았다:** `[영역 ART · 캔버스 갱신 제안] 레일이 알림 첫 글자를 덮는다`(T113) —
알림을 캔버스의 **위 띠 가운데**로 옮겼다. `e2e-ui ⑭` 가 두 해상도에서 **레일 ∩ 알림 = 0** 을 지킨다
(자명 통과 금지: 옛 자리로 두면 3000px² 겹친다).

* ★**[영역 클라/ART] 띠 안 가운데 vs 화면 가운데 — 좁은 화면에서 어긋난다.**
  캔버스는 알림을 **상태 줄 오른쪽**에 두고(1440 판에서 여유 66px), 그 판 중심은 814 로
  화면 중심(720)이 아니다. T118 은 §3 판정대로 **화면 중심**에 세웠고, 그래서
  1280 에선 여유 +36px 이지만 **1024 에선 −92px**(상태 줄 위에 얹힌다). 긴 알림(폭 560)이면
  1280 에서도 닿는다(교차점 대략 화면폭 1508). **토큰만으로는 못 푼다** — 상태 줄 폭을 CSS 가 모른다.
  고치는 길 둘: ① 상태 줄 오른쪽을 재서 CSS 변수로 넣는 JS 한 줄 ② 캔버스가 알림을 화면 중심으로
  옮기고 상태 줄을 줄인다. `e2e-ui ⑭` 가 매 판 그 여유를 숫자로 찍어 둔다.
* **[영역 ART] 캔버스 알림 판의 금색 왼쪽 강조 막대**(x 536~538 · `--accent`)가 실화면엔 없다.
  자리 카드라 안 넣었다 — 넣으면 `border-left: 3px solid var(--accent)` 한 줄이다.

## 0-ART잔무. [T120 옷 재질·죽은 산·밭 단계 2026-09-05] 남은 것

* ★★**[영역 서버 · 큰 것] 밭 단계 정본이 넷인데 그림은 여섯이 됐다 — 사영을 늘려야 한다.**
  T120 이 **쉬는 밭(4)** 과 **그루터기(5)** 를 구웠지만 **클라는 그걸 못 그린다.** §0-ⓒ 실측:
  - `server/zone.js _farmStageOf` 는 `0 갈은흙 · 1 어린싹 · 2 자람 · 3 익음` **넷만** 낸다.
  - **휴면**: 술어는 이미 하나로 서 있다(`server/crops.js _dormant` · `dormantAt` · T99).
    그런데 **사영이 없다** ⇒ 한겨울 보리밭이 여전히 "2 자람"으로 간다.
  - **그루터기**: `data.stumpDay` 를 수확 절 **두 곳에서 쓰기만** 하고 **읽는 데가 0곳**이다.
    표식은 있는데 "언제까지 그루터기인가"라는 규칙이 없다.
  ⇒ 세션이 클라에서 단계를 지어내지 않았다. 지어내면 그게 **시계 둘**이고, T108 이 방금 그 병을
    고쳤다(클라가 `readyAt` 으로 다시 세다가 심자마자 "익음"으로 그리던 자리).
  판단이 필요하다: `_farmStageOf` 가 `dormantAt` 과 `stumpDay` 를 읽어 **4·5 를 내려보낼 것인가**,
  아니면 두 상태를 따로 실을 것인가. 정하면 클라 쪽은 **두 줄**이다
  (`00-const.js` 미리읽기 `i<4`→`i<6` · `cropSprite` 의 `Math.min(3,…)`→`Math.min(5,…)`).
  ⓘ 그루터기의 **끝나는 조건**도 같이 정해야 한다(며칠? 다시 자라기 시작하면?).
* **[영역 ART] 둥근 산 `mt_R*` 40 · 주봉 `mt_X*` 3 은 여전히 안 닿았다.** T120 이 `SPECS` 에서
  **재료표만** 지웠다(85 → 45 · 배포와 1:1). 되살릴 자리는 `git show <T120 커밋>^:scripts/bake-mountain.py`.
  ⓘ `mock-mountain-cover.js` 의 `ROUND_MIX` 갈래는 앵커 가드 안이라 안 켜진다 — **뜻의 기록**이라 안 지웠다.
  굽고 배치 손잡이를 여는 카드는 그대로 필요하다(굽기 40장 ≈ 50분).
* ★**[영역 공통 · 하네스] "부르지 마라" 금칙은 사본을 강제한다.** `test-charsheet` 가
  `char_render.py` 의 `import render_common` **자체**를 금하고 있었다(T97 · 이유는 "그 파일을
  세션8이 만지는 중"). 그 이유가 지나간 뒤에도 금칙이 남아 있었고, 그동안 옷 재질 여섯은
  **두 파일에 따로** 적혀 있었다. T120 이 금칙을 *무엇을 쓰는가*로 좁혔다(쓰는 이름 넷만 허용 ·
  씬은 여전히 char 것). ⇒ **일시적 사정으로 세운 금칙에는 유효기간을 적자.**
* ★**[영역 ART] 캐릭터 시트 70장은 이 컨테이너에서 다시 구우면 배포본과 바이트가 다르다.**
  T120 이 대조군을 세우다 실측했다: 얼린 상자(`char_meta.json`)를 그대로 물려 굽고도
  **평균 |Δ| 0.006~0.010/255 · 다른 화소 0.2~1.8% · 최대 Δ 24~192**(가장자리에 몰린다 —
  T106 의 산과 같은 꼴이다). 같은 코드 두 판은 **바이트까지 같으므로**(대조군 ≡ 패스A 14/14)
  이 차이는 굽기의 흔들림이 아니라 **배포본이 다른 기계/다른 코드 상태에서 왔다**는 뜻이다.
  ⓘ `icons.lock.json _기계` 의 "배포하는 그림 전부 5.0.1"은 그 잠금표가 덮는 **아홉 무리** 이야기다.
    캐릭터 시트는 `char_sheets.lock.json`(sha256 파일 전체)이 따로 잠그고 있어 그 문장 밖이다.
    ⇒ 문장을 좁히든지, 시트를 재굽기 카드로 편입하든지 판단이 필요하다.
  ⚠T120 은 시트를 **한 장도 안 건드렸다**(배포 자산 변경 0). 이건 관측이지 이 카드의 일이 아니다.
* **[영역 ART] 쉬는 밭의 겨울 표(눈·서리)는 아직 안 정했다.** T120 은 마른 볏짚만 깔았다
  (재민 눈으로 정할 자리라고 카드가 적었다). 눈을 얹을지, 서리 기운(푸른 기)을 더할지 판단이 필요하다.
  ⓘ 지금도 여덟 군 전수에서 맨 흙·자람·그루터기와 갈린다(수치는 `보고/T120` §3).
* ★**[영역 공통 · 하네스] `e2e-*` 는 죽기만 하는 게 아니라 *값이 흔들린다*.** T120 에서
  `e2e-mountain` ⓑ2 의 반례 판정("바위 셀이 없는 자리엔 산이 없다")이 **90.0% < 2%** 로 빨갰는데,
  **같은 나무·같은 셀**을 다시 재니 **0.0%** 였다(깨끗한 `origin/main` 도 0.0%).
  T101·T106 의 연속 실행 오염과 같은 계보인데 이건 **더 조용하다** — 죽지도 멈추지도 않고
  숫자만 틀린다. 산 끄기 토글이 화면에 앉기 전에 찍는 것으로 보인다(`mtOff` A/B 사이에
  프레임 대기가 없다) ⇒ **조건으로 기다리게** 하면 닫힌다(족보 ㊾ 문법 · `e2e-charsprite` 회부와 같은 처방).
* **[영역 ART] 그대로 남은 것**: 자연물·건물 먹선·못박기 적용(T116 뒤) · 종별 34세트 ·
  침상 생활층 · `e2e-*` 포트 선점(T106 처방) · 상추/아욱 · 굽는 스크립트를 부르는 하네스 0개.
## T116 (2026-09-05) — 닫은 넷 · 정정 하나 · 새로 올린 둘

**닫았다 넷** (전부 굽기 공용화 카드가 시켜서 실제로 한 것):

* `[영역 ART · T97 뒤] 자연물·소품 먹선 공용 편입` — **함수는 옮겼다.** `ink_post.py` 는 없다
  (본문·상수 전부 `render_common.py` 끝). 그때 "무접촉으로 뒀다"고 적은 이유(T97 세션8이 만지는 중)는
  T106 이 끝나 사라졌다. ⚠**거는 것은 아직 캐릭터뿐이다** — 아래 새 회부로 이어진다.
* `[영역 ART · T106 뒤] 자연물·소품 먹선 공용 편입` — 위와 같은 항목(두 번 올라와 있었다). 같이 닫는다.
* `[영역 ART] EXR 을 다섯 클립 전부로` — 채웠다. `assets-src/char_raw/` 70장(git 밖).
  이제 **모든 클립**의 먹선·셀 세기 조정이 굽기 없이 분 단위다(`scripts/ink_repost.py`).
* `[영역 공통] 상자 못박기를 자연물·건물에도?` — **헬퍼는 공용이 됐다**
  (`read_pinned_box`·`fit_pinned_box`, 서식 둘을 다 읽는다). 거는 것은 아래 새 회부.

**정정 하나.**
★**`building_anchors.json` 은 배포에 없다.** T116 카드 §0-ⓒ 가 그것을 `char_meta.json` ·
`nature_anchors.json` 과 나란히 놓았는데, 실측해 보니 `building_render.py:522` 가 그 파일을
`scripts/building_renders/`(= `.gitignore`) 에 쓴다. 배포된 자산군 메타는 넷이다
(char · nature · props · crops · mountains 다섯 중 char 만 서식이 다르고, 건물은 없다).
⇒ **건물에 못박기를 걸려면 그 메타를 먼저 배포로 올려야 한다.** 그게 선행 작업이다.

**새로 올린 둘:**

* ★**[영역 ART · 세션8] 자연물·소품·작물·산에 먹선·셀·못박기를 실제로 건다.**
  함수는 `render_common` 에 있고, 지금 부르는 곳은 캐릭터 셋뿐이다
  (`char_render`·`ink_repost`·`char_mpfb_screen`). `test-render-common ⑧` 의 `WIRED` 목록이
  그 사실을 붙들고 있으니, 걸 때 **그 줄을 지우지 말고 목록을 늘려라**.
  ⚠거는 순간 그 자산군의 배포 PNG 가 **전부 바뀐다**(먹선은 화소를 덮는다). 잠금표
  (`icons.lock.json` 등)와 재굽기 비용을 카드가 미리 계산해야 한다. 건물은 위 정정대로 메타부터.
* ★★**[영역 ART · 재민 판정] EXR 은 배포 PNG 와 짝이 맞아야 한다 — 지금 세 클립이 안 맞는다.**
  T116 §2 실측: 되굽기가 배포와 바이트가 같은 것은 **swing·aim 28장뿐**이고 idle·walk·run 42장은
  다르다(다른 화소 1.0~1.9% · 알파 차 ≤2 · 평균 RGB 차 0.8/255 — **잡음 한 판 차이**).
  원인은 굽기도 후처리도 아니다: **`char_meta.json` 이 앵커를 소수 셋째 자리로 반올림**해 두고
  (T96), T107 의 상자 못박기가 **그 반올림된 값**을 다시 읽는다. 그래서 T107 이후의 모든 굽기는
  T96 판과 카메라가 **0.0015 SS px** 어긋난다. 그만큼만 옮겨도 사이클스가 잡음을 새로 굴린다
  (실측: 0.0012 SS px 옮기니 EXR 화소 4.81% 가 갈렸다). 먹선의 알파 문턱(200/255)이 그 ±2 를
  ±200 으로 키운다. **굽기는 결정적이다**(오늘 두 번 구운 EXR 바이트 동일) — 판이 다를 뿐이다.
  ⇒ 세 길 중 하나를 골라야 한다(전부 배포를 건드리므로 재민 판정):
    ⓐ **아무것도 안 한다** — 세기를 바꿀 때 다섯 클립을 한꺼번에 되굽으면 자체 정합은 맞는다.
      다만 "지금 배포와 바이트가 같다"는 못 쓴다(그 자리가 28장에서 안 는다).
    ⓑ **idle·walk·run 42장을 오늘 EXR 로 갈아 끼운다** — 그림은 잡음 한 판 차이라 눈에 안 보이고,
      그 뒤로는 70/70 이 맞는다. 배포 42장 + 잠금표가 바뀐다.
    ⓒ **메타에 앵커를 전정밀도로도 남긴다**(`anchorYRaw`) — 근본이지만 메타가 바뀌고,
      T96 판을 되살리려면 어차피 다시 구워야 한다(그 값은 이미 잃었다).
  ★교훈 한 줄: **EXR 은 배포 PNG 를 낸 그 판이 같이 남겨야 한다.** 나중에 따로 구운 EXR 은
  "세기를 바꾸는 데"는 쓰지만 "지금 바이트를 되살리는 데"는 못 쓴다.
* **[영역 ART · 아주 작은 것] `--exronly` 가 후처리를 헛돈다** — EXR 은 후처리 **전** 시트라
  `post_all` 결과가 버려지는데도 실행된다(저장만 건너뛴다).
  ⚠**값을 재 보니 카드로 낼 일이 아니다**: 세 클립 후처리는 **23초**(EXR 42장 읽기·PNG 42장
  쓰기 포함)로, 굽기 4,433초의 **0.5%** 다. `if EXR_ONLY: continue` 한 줄이 맞긴 하지만
  누가 그 파일을 다음에 만질 때 곁들이면 된다.

## 0-나무종. [T129 나무 종·열매·그루터기 2026-09-05 밤] 남은 것

* ★★**[영역 서버 · 큰 것] 그림은 다 있는데 서버가 아직 종도 단계도 모른다.** T129 가
  성목 15 · 가을·열매 4 · 묘목 8 · 그루터기 1 을 구웠고 `tree_species.json` 이 어느 그림이 어느
  종인지 말한다. 남은 것은 **읽는 쪽**이다:
  - **종**: `chunk.js` 가 나무를 놓을 때 종을 정하고 실어 주어야 한다(지금은 `tree01~12` 를 고를 뿐).
  - **단계**: T122 가 낼 셋(성목 → 그루터기 → 묘목)을 밭의 `farmStage` 처럼 **사영**해서 실어야 한다.
    ⚠T120 회부와 **같은 자리**다 — 밭도 그림 여섯인데 사영이 넷이라 멈춰 있다. 두 건을 한 카드로
    묶는 것이 맞아 보인다(둘 다 "그림은 늘었는데 정본이 안 늘었다").
  - **철**: 가을 판은 계절이 부른다. 서버가 계절을 아는 자리(`seasonOfDay`)는 이미 있다.
* ★★**[영역 서버] 열매 넷이 아직 품목이 아니다 — 그래서 아이콘을 안 구웠다.**
  `test-icons ③` 이 **아이콘 키 = 서버 품목 키**(`weights.kgOf` 가 null 이 아닐 것)를 요구한다.
  밤·개암·오디·머루는 `specialty.RESOURCES` 에도 `CORE_KG` 에도 없다 ⇒ 지금 아이콘을 배포하면
  **품목 없는 그림**이 된다(하네스가 먼저 빨개진다). 카드 §1 이 "자산만 · 클라 접점 0"이라
  세션이 서버 품목을 새로 만들지 않았다.
  ⇒ 품목이 서면 아이콘은 **모델부터 다시**가 아니라 `props_render` 문법으로 넷을 굽는 작은 일이다
  (밤은 `_fruit_cluster('burr')`, 머루는 `_grape_bunch` 가 이미 모양을 갖고 있다).
* **[영역 ART] 둥근 산 `mt_R*` 40 · 주봉 3**(T120) · **자연물·건물 먹선·못박기 적용(T116 뒤)** ·
  **종별 34세트** · **침상 생활층** · **상추/아욱** — 그대로.
* ★**[영역 ART · 관측] 배포된 나무 12장은 지금 코드로 다시 구우면 바이트가 다르다.**
  얼린 조건에서 재도 평균 |Δ| 0.45/255 · 다른 화소 29.3%(눈으로는 같다 — 가장자리 잡음).
  같은 코드 두 판은 **바이트까지 같으므로**(결정론 실측) 배포본이 **다른 코드 상태**에서 왔다는 뜻이다.
  T120 이 캐릭터 시트에서 잰 것과 **같은 종류**다(그쪽은 0.006~0.010/255 · 0.2~1.8%).
  ⇒ 두 건이 쌓였다: "재굽기 카드가 지나간 뒤에도 **스테이징이 정본**인 자산이 남아 있다."
  판단이 필요하다 — 한 번 싹 다시 굽고 잠글지, 아니면 그대로 두고 잠금표만 믿을지.
  ⚠T129 는 나무 12장을 **한 장도 안 건드렸다**(전수 sha1 대조 · 바뀐 것 0).
