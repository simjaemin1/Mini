# 인계 — T 거래·거래소·장마당

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.

## 다른 영역에 있는 관련 절

* → 참조: `인계/L-로트부패.md` — 3-부. ★★2026-08-31 부패·보존 배치 — **부패 곡선 + 말리기·훈제·절임** (병행 트랙 4)
## T69. ★★2026-09-03 — **시세는 내가 낼 물건 기준 · 받을 양으로 묻고 내는 양은 올림** (거래소 v2)

> 전문 `보고/T69_2026-09-03.md`. 하네스 `scripts/test-trade.js` ⑧⑨ · `scripts/e2e-trade.js` ⑦.
> 캐논 `설계/설계_품목·동사·거래소_캐논.md` §3(재민 확정 09-03).

**없던 것은 둘이었다.** 서버는 이미 임의 give/take 를 받고(`quote`·`exchange`), 받는 쪽 내림도
스프레드·조각 적분도 종전부터 서 있었다. 빠진 건 ⓐ 시세 표시 기준이 `CFG.NUMERAIRE='food'` 로
**고정**이었던 것 ⓑ 견적이 **내는 양 기준뿐**이라 "돌 3개 받으려면 나무 몇 개?"를 **물을 수 없었고**,
그래서 캐논 ②의 "내는 쪽 올림"이 **설 자리가 없었다**는 것.

### ⓐ 기준 품목 — 요청마다 온다

```
  board(ledger, econV2, vil, vid, inventory, numeraire)
    const nres = (numeraire && +prices[numeraire] > 0) ? String(numeraire) : CFG.NUMERAIRE;
    const pn   = +prices[nres] || 0;                       // ← 달라지는 건 **분모 하나**뿐
    numeraireKo: Events.koRes(nres)                        // ← 한글 이름은 정본 하나(KO_NUM 은 폴백)
```
* 낼 물건을 고르면 클라가 `village_trade` 에 `numeraire: trGive` 를 실어 **표를 다시 받는다**.
  안 골랐거나 그 마을에 시세가 없는 품목이면 **종전 곡식**으로 돌아간다(폴백은 살아 있다).
* **클라 재계산 0** — 패널은 서버가 준 `num`·`numeraireKo` 를 찍기만 한다(`test-trade ⑧` 소스 검사).
* 기준 행 자신은 정확히 **1**이다 — "나무를 팔면 시세가 나무 몇 개"의 정의.
* 교환 뒤 되보내는 표도 **같은 기준**이라야 한다(`zone.js tryVillageTradeExec`).

### ⓑ 받을 양으로 묻기 — 올림은 **한 자리**다

```
  quote(…, giveQty, { want, perItem })  →  giveNeeded(개수 · 올림) · giveNeededUnits(단위 · 소수) · takeAtNeeded
  exchange({ …, want })                 →  그 giveNeeded 로 실행(받는 쪽은 종전 내림 · 넘침은 거래소 몫 · 캐논 ③)
```
* **새 가격 줄 0.** `quote` 가 상한을 다 쓰는 `maxGive` 를 내던 **그 이분 탐색**을, 목표만
  `cap` → `want` 로 바꿔 한 번 더 돈다(`_giveForTake` · `planSliced` 재호출뿐).
  상계는 어림(`want/ratio×4`)이 아니라 **`cap` 에 닿는 give(`hiCap`)** 를 쓴다 —
  `want ≤ cap` 이면 거기서 반드시 닿으므로 탐색이 헛돌지 않는다.
* **★★올림은 `_climbToWant` 하나다.** 연속해를 `ceil` 하면 안 된다 — 이분 탐색의 상계는 참값보다
  (hi−lo)만큼 크게 끝나므로 참값이 마침 정수면 한 칸 더 올라가고, 그러면 올림이 **최소가 아니게** 된다.
  ⇒ **한 칸 아래에서 출발해 받을 양에 닿는 첫 정수까지 올라간다.** 그 첫 정수가 정의상 최소다.
* ⚠**그 걸음을 견적과 실행에 따로 적으면 그게 사본이다.** 1차 실장이 그랬고 `test-trade ⑨ⓓ` 가 잡았다 —
  견적을 내림으로 망가뜨렸는데 **실행 쪽 사본이 조용히 고쳐 줘서** 돌연변이가 초록이었다.
  지금은 둘이 같은 함수를 부른다: 견적은 **개당 단위 어림**(`perItem`)으로, 실행은 **고른 그 개체들의
  실제 무게**로 — 같은 걸음, 다른 자.

| 낼 물건 | 개당 단위 | 필요한 단위 | 내야 할 개수 |
|---|---|---|---|
| 개수 물건(1개 = 1단위) | 1 | 8.888589 | **9** (= ceil) |
| 개체 무게 섞임(1개 = 2.22단위) | 2.22 | 8.888589 | **5** (= ceil(8.888589/2.22)) |

필요한 **단위**는 환산과 무관하게 같다 — 달라지는 건 그걸 몇 개로 세느냐뿐이다.
캐논 ②의 올림은 "낱개로만 존재하는 물건"의 규약이라 **개수 자리**에 걸리고, 단위 자리
(`giveNeededUnits`)는 소수 그대로 실어 보낸다 — kg 물건(로트)이 들어올 자리다.

### 화면 (`public/client/50-i-panel.js`)

낼 것 고르기(시세가 그 기준으로 바뀐다) → 받을 것 고르기 → **받을 개수** → "내야 할 양 N".
종전 "낼 개수"도 남는다(kg 물건은 그게 맞다). **둘 중 마지막에 만진 것이 견적의 축**(`trAxis`) —
두 입력이 동시에 축이면 화면이 어느 쪽 수를 말하는지 사람이 알 수 없다.

### 접점 (인자 전달만 · 두 줄 밖 무접촉)

```
  server/trade.js       board(+numeraire) · quote(+opts.want) · exchange(+want) · _climbToWant/_giveForTake 신설
  server/villages.js    villageTradeBoard(+numeraire) · villageTradeQuote(+opts) · villageTradeExec(+want)
  server/zone.js        village_trade / _quote / _exec 세 핸들러 + 두 try 함수 서명 — 전달만
  public/client/*.js    20-r2(trWant·trAxis 선언) · 50-i-panel(거래 패널 절)
```
★`CFG.SPREAD`·`SLICES`·`STOCK_FRAC` 무변 · `exchange` 원자 순서(②재고 먼저) 무변 · econ 무접촉.

### 회부 (구현 금지)

`food` 라는 뭉뚱그린 재화 자체(→ ECON 2-c) · 원격 시세 조회(캐논 §3.2 — 안 만든다) ·
시세 이력 그래프 · 흥정.

## T100. ★★★2026-09-05 — **NPC 농사의 식량이 밭에서 나온다** (ECON 수술 2-b · 승인 대기)

> 전문 `보고/T100_2026-09-05.md`. 하네스 `scripts/test-econ-fieldyield.js`(끔 29/0 · 켬 33/0) ·
> 계측기 `scripts/t100-fieldbase.js`. 가지 `batch/field-yield-0905`.

**무엇이 틀려 있었나** — econ 의 농부 산출은 **밭과 무관**했다: `JOBS.farmer.landBoost = v.land.fertility`
→ 1인 하루 `1.5 × 지력`. 생활층이 밭을 아무리 갈아도(`farmland`/`dryfield` 행) 곳간은 그대로였다.

**모델 — 함수 하나 · 하한 하나 · 브리지 하나**
```
  밭 1칸 1회 = yield × GROUP_KG[group] × kcal ÷ DAY_KCAL   ÷ growDays 로 하루 흐름으로 **편다**
             = **0.0806 food 단위/칸·일**(곡물 9종 평균 — crops.js·kcal.js 에서 유도 · 새 수 0)
  마을 하루 = max(밭 칸, 농부 × 3) × 0.0806 × 지력        ← 3 = 생활층 `LIFE_CLEAR_PDAY`(텃밭 하한)
  무는 자리 셋: JOBS.farmer.landBoost · _capFlow(부양력 prodK) · 선언
  브리지     : villages.js `_lifeLiveFarmTile` 이 `vil.econ._fieldCells = vil._farmSet.size` **한 줄**
               (`_paddyShare`·`_clearedFrac` 계열 — **회계 아님** · 생활층은 여전히 storage 무접촉)
  개간 정본  : `_lifeHeadlessDay` ① 절을 `_lifeClearDay` 로 **순수 추출** → 랩은 `__labProbe._clearProbe`
               로 **그 함수 자체**를 부른다(사본 0)
```

★★**이 카드가 실제로 고친 것은 계측기였다.** 1판 §0 은 어긋남 **×11.66** 을 재고 지시의 정지 규칙에
걸려 멈췄다. 그런데 그 값은 세계가 아니라 **랩**의 값이었다 — econ 랩은 `ENABLE_VILLAGES=0` 이라
**개간이 안 돈다**. 밭 칸이 시딩값(`want = INITIAL_POP(8) × landNeedPer(fert, 8)`)에 800일 내내
얼어붙은 채 인구만 16배로 자란 것이다. 역상관(비옥할수록 밭 적음)도 같은 데서 나온다 —
`landNeedPer = 8 × clamp(0.55/fert, 0.6, 2.5)` 는 **보즈럽 조방화 캐논**이라 비옥도에 반비례한다.
개간을 정본 함수로 랩 안에서 돌리자 같은 자로 잰 어긋남이 **×11.66 → ×0.83** 이 됐다.
⇒ **앵커는 처음부터 맞았다. 틀린 건 밭을 안 자라게 둔 계측기다.**

**세계가 어떻게 달라지나(3시드 800일 · 개간 관측 켬)**
```
  시드      T86            T100           소멸
  1020    6,459  →      6,200  (−4.0%)    0/51 → 0/51
     7    7,125  →      5,521 (−22.5%)    0/51 → 0/51
    42    6,848  →      5,818 (−15.0%)    0/51 → 0/51
  귀속: **굶은 게 아니다**(`hunger>0` 0/51 · 두 팔 다 · 곳간 식량등가 Σ +50%).
        움직인 것은 **주거 게이트**(`gated` 10/51 → 15/51) — T86 §5-b 의 그 사슬이다.
        ★그리고 T86 이 −67% 로 부순 **농촌1 이 81 → 236(+191%)** 으로 되살아난다.
  농부 Σ 1,544 → 995(−36%) — 농사가 노동이 아니라 **면적**에 묶이자 일손이 다른 직업으로 갔다.
```
**되돌림** `T100_FIELD_YIELD=0`(= 미설정) → T86 여덟 수 **세 시드 전부 한 자리도 안 틀린다**.

⚠**농사는 식량의 약 46% 만 댄다** — 개간 목표는 `landNeedPer` 4.8~20칸/인인데 자급에 필요한 값은
`1 ÷ 0.0806 = 12.4칸/인` 이다. 나머지는 어획·수렵·채집·교역이 댄다(설계 그대로).
⚠**밭 밑변은 노동을 안 본다** — 6마을이 `_lifeTasksPerFarmerDay` 201칸/농부·일을 넘는다(최대 ×3.4).
`q` 배선과 한 묶음으로 `인계/회부.md` `0-밭산출` 에 올렸다.

## T86. ★★★2026-09-05 — **식단은 사다리가 아니라 예산이다** (ECON 수술 2-d · PM 승인)

> 전문 `보고/T86_2026-09-05.md`. 하네스 `scripts/test-econ-diet.js`(27/0) · 귀속 계측기 `scripts/t86-attrib.js`.

**무엇이 틀려 있었나** — `consumeFood` 는 **고정 사다리**라 위 칸이 넉넉하면 그것만 먹고 **가격을 안 봤다.**
어촌은 곳간에 곡식이 있어도 생선만 먹었고(생선 86% · 식품군 2군), 지도 51곳 중 캐논이 말하는
"6군 중 4군 = 균형식"에 닿은 마을이 **한 곳도 없었다**(4군 이상 0/51). 다양성이 설계가 아니라 **재고 부족의 부산물**이었다.

**모델 — 한 함수 안에서 예산 → 선택 → 재분배 → 사다리**
```
  ① 배정   x_g = need × (w_g / p_g) / Σ_h (w_h / p_h)          ← 가격 가중 지출 몫(Cobb-Douglas)
           p_g = 그 군 최저 **열량 1단위당 유효가격** = 그림자가격 ÷ 열량환산
           재고 없는 군은 후보에 안 들어와 **분모에서 빠지고 자동 재정규화**된다
  ② 선택   군 안에서 그 유효가격이 싼 품목부터
  ③ 재분배 모자란 군의 남은 예산을 살아 있는 군에 **같은 가격 가중**으로 — 한 바퀴
  ④ 하위호환 그래도 남으면 **종전 사다리**. 곳간에 한 품목뿐이면 종전과 비트 동일
  가격은 v2 가 마을에 덮어 둔 `_priceCache` 를 읽는다 — `server/trade.js` 의 "같은 날 안에선 같은 값"
  규약 그대로다(NPC 교역 틱과 **같은 표** · 사본 0). 캐시가 없으면 조용히 사다리로 내려간다.
```
★★**등분이 아니라 가격 가중인 것이 이 절의 핵심이다.** 초안은 `need × w_g` 였고 그건 재민이 경계한
*"무조건 골고루"* 그 자체였다 — 어촌3 이 곡물을 사 오다 **207 → 34** 로 무너졌다(지도 인구 −20.3%).
가격 가중으로 고치니 어촌3 **207 → 187** · 식품군 **2 → 5** · 지도 인구 −6.8~+4.9% · 소멸 0/51.

**w_g — 새 수 하나.** `specialty` 의 `contributes.subsistence` 를 군별 평균 내어 정규화한 표가 후보(i)였고,
채택은 **후보(ii) 균등 1/6** 이다. 이유는 하나다 — 시드 1020 만 보면 (i)이 1등(+0.7%)인데
**시드 42 에서 소멸 2/51** 을 냈다. 세 시드 모두 0/51 을 지키는 후보가 (ii) 뿐이었다.
⇒ **1시드 스크린은 채택 근거가 될 수 없다**는 것의 실물 사례다.

**뺀 것 둘 — 둘 다 실측이 시켰다**
```
  부패 항 (1−DECAY_RATES)  (마을×군) **18쌍 전수에서 정렬 순서를 한 쌍도 안 바꿨다**(이동 최대 0.8%) ⇒ 식에서 뺐다
  보존식                    예산 밖. 재고를 채우고 재면 보존식이 **열량당 더 싸서**(말린과실 0.283 < 과일 0.499)
                            예산에 넣으면 T17 ② 가 막아 둔 신선식 시장 붕괴가 그대로 일어난다
```
**행복은 종전 계단 유지**(재민 판정 ⓑ) — 달성 효용 비는 다음 카드다. 근거표는 보고 §2-b.
**되돌림** `T86_DIET=0` → T73 여덟 수 비트 재현.

⚠**이 카드가 연 사슬 하나**: 곳간을 먹어 치우니 내다 팔 잉여가 줄어 **목재 대금이 0** 이 되고, 집을 못 지어
성장이 멈춘 마을이 있다(농촌1 246 → 81 · `hunger 0` · `gated: true`). **수출 잉여 판정이 식단 예산을 모른다** —
이 영역의 다음 물음이고 `인계/회부.md` 0-식 에 올렸다.

## T73. ★★★2026-09-03 — **곳간의 생곡은 식량이다** (ECON 수술 2-a · PM 승인)

> 전문 `보고/T73_2026-09-03.md`. 하네스 `scripts/test-econ-rawgrain.js`(25/0 · 돌연변이 검사 포함).

**무엇이 어긋나 있었나** — 세계의 세 곳이 생곡을 두고 서로 다른 말을 했다:
```
  server/specialty.js   wheat/rice/barley → contributes: { subsistence: 1 · 1 · 0.9 }   "식량이다"
  sim/economy-sim.js    FOOD_GLUT_SAT = { food, cooked_food, wheat, rice, barley }      "식량이다"
  sim/economy-sim.js    totalFoodEquivalent · consumeFood 사다리                        "식량이 아니다"
```
그리고 **생곡 → `food` 변환은 econ 어디에도 없다**(전수 확인). 그래서 800일 51마을에 **생곡 114,188** 이
쌓인 채 아무도 안 먹었다. T60 §0-ⓔ' 의 광산1 은 **밀 21 · 쌀 12 를 두고 굶어 죽었다.**

**계수 — 발명이 아니라 두 정본의 나눗셈이고, 그 나눗셈이 스스로를 검산한다**
```
  food 1단위 = 0.70kg(weights.js) × 3,500kcal/kg(kcal.js · T59) = **2,450 = DAY_KCAL**
    ⇒ 식단 사다리의 단위가 **사람 하루**다(DAILY_FOOD_CONSUMPTION = 1.0 이 그 증거).
  생곡 1단위 = 도정 수율만큼의 food.  품목별 유도: 밀 0.750 · 쌀 0.771
    ⇒ 채택 RAW_GRAIN_FOOD_FACTOR = **0.72**(쌀 수율 · 둘보다 작은 보수값 · 새 수는 하나)
  ⚠**잠정이다**(재민 판정 ③): 품목별은 **ECON 2-c**(곳간 구성 = 구체 작물)에서 간다.
    막는 칸이 하나 — **보리 도정 수율에 고증 앵커가 없다.** 보리는 생곡의 25~28% 라 작지 않다.
```

**사다리에서의 자리 — `food` 뒤 · 채집물 앞**
생곡은 **같은 곡물**이라 도정 한 번이면 `food` 다(채집물 0.25~0.5 보다 훨씬 가깝다).
그런데 `food` 보다는 뒤여야 한다 — 앞에 두면 찧는 수고를 먼저 하는 게 이득이 되어 이미 찧어 둔 `food` 가
곳간에서 늙는다(T17 이 보존식을 **맨 뒤**에 둔 것과 같은 결의 거울).
⚠`_cons` 는 **안 부른다** — 사다리 전체에 걸린 flow-EMA 제외 규약 그대로다(식단은 가용성 기반 대체 소비다).

★★**읽는 법 — 생곡을 퍼먹게 만든 카드가 아니다.** 생곡 재고는 거의 안 줄었다
(1020: 114,188 → 114,986). 사다리 3.5단은 `food`·어육이 **다 떨어져야** 열리고 51마을 지도에선 드물다.
바뀐 것은 **"곳간에 있다"가 K(수용력)로 세어진다**는 것 — 식량등가 Σ 383,949 → **489,330**.
그 사다리가 실제로 열리는 자리가 **광산1 같은 마을**이고, 그래서 그 마을이 산다(소멸 1/51 → **0/51**).

**되돌림** `T73_RAWGRAIN=0` → T60 기준선 여덟 수 **비트 동일 재현**(인용이 아니라 재현).

## T60. ★★2026-09-03 — **캐러밴 시계는 다섯 자리가 한 규약이다** (ECON 수술 1.5 · PM 승인)

> 전문 `보고/T60_2026-09-03.md`. 하네스 `scripts/test-msy.js` ⑤(다섯 자리 전부 소스 계약).

**무엇이 틀려 있었나** — 캐러밴이 늦어지면 `arriveDay`·`returnArriveDay` 는 밀었는데
**`travelDays` 는 안 밀었다**. `travelDays` 는 `tradeLog` 가 적는 "이 짐이 며칠 걸렸나" 라서,
그 값만 늦어짐을 모른 채 남았다. T17 이 econ 쪽 두 자리를 고쳤고, T60 이 **존 서버 세 자리**를 마저 고쳤다.

```
  존 서버(server/villages.js)  — 헬퍼 하나로 모았다
    function _clockPush(c, days, phase) {
      if (phase === 'outbound') { c.arriveDay += days; c.returnArriveDay += days;
                                  if (Number.isFinite(c.travelDays)) c.travelDays += days; }
      else                      { c.returnArriveDay += days; }   // 귀로 지연은 가는 구간을 안 건드린다
    }
    ⓐ 로컬 우회 재경로   _clockPush(c, pushed, body.phase)
    ⓑ 도착 임박 가드     _clockPush(c, push,   body.phase)
    ⓒ 고립 화물보존 귀환  c.departDay = world.day; c.travelDays = days;   ← 거리를 덮었으니 시계도 그 구간의 것
  econ(sim/economy-sim-v2.js · T17 ④)
    ⓓ 재routing          c.departDay = day; c.travelDays = extraDays;
    ⓔ 빈손 귀환          c.departDay = day; c.travelDays = travelDaysForDistance(c.distance);
```

⚠**`travelDaysForDistance` 는 아직 export 가 아니다** — 3사본 규약(specialty + economy-sim* → 번들 → 랩 2종)이
export 한 줄에도 걸린다. `server/rumor.js` 는 "한 줄 거울 + 교차 계약 검사"로 버틴다(회부 유지).

★**기준선 무영향**: `travelDays` 를 읽는 곳은 `tradeLog` 와 하네스뿐이라 궤적을 안 바꾼다 —
3시드 800일 기계 비교 **다른 값 0**(`인계/공통.md` 기준선 절).

## T17. ★★★2026-09-03 — **납품 표 한 줄이 세 문을 연다** (ECON 수술 1 · PM 승인)

> 전문은 `보고/T17_2026-09-02.md`. 기준선 정본은 `인계/공통.md` 기준선 절(PM 승인 2026-09-03).
> 지금까지 모든 배치가 "econ 무수정"으로 지켜 온 정본을 **이 카드만** 열었다.

### 정본 사슬 — 이 그림을 외워라

```
  PV_DEPOSIT_MAP  (server/villages.js · 표 하나)
    → events.buildDeliverable(depositMap) → { items, toEcon, fromEcon }
        → 게시판 의뢰 대상(fromEcon) · 보상 지급 역방향(toEcon)
        → trade.tradableIn  = deliverable.fromEcon
          trade.tradableOut = deliverable.toEcon  (게시판 보상으로 잠긴 품목 제외)
  ⇒ 표에 **한 줄**을 넣으면 게시판·곳간·거래소가 **한꺼번에** 그 품목을 다룬다. 사본 0.
  ⇒ 그래서 그 한 줄이 **게시 건수 기준선을 움직인다**(곡물 품목화 전례 · T17 실측 +594).
```

### T17 이 표에 넣은 것 / 안 넣은 것

| | |
|---|---|
| 넣었다 | `dried_fish` · `dried_fruit` · `smoked_meat` · `pickled_veg` · `salt` (플레이어 id = econ id) |
| **안 넣었다** | **`tool`** — 플레이어 도구는 셀 수 있는 인벤 품목이 아니라 **인스턴스**(`player.toolItems = [{id,type,d,max}]`). 표에 넣으면 `deliverToVillage` 가 `player.inventory.tool` 을 찾다가 **유령 품목**을 만든다. **재민 판정 A: 안 넣는다.** 넣으려면 플레이어 층에 인스턴스↔갯수 다리가 먼저 필요하다 |

★도구는 그래도 **가격 앵커를 얻었다** — 마모를 `_cons` 로 기록하니 `_consEMA.tool > 0` 이 되어
v2 가격의 `flowT` 가 선다. 사건 장부에도 처음으로 나온다(부족 82~97건 · 그 전엔 **0건**).
⚠단 **의뢰는 안 된다**(표에 없으므로). 실측: ① 축만 껐다 켜도 인구·무기Q·확장셀·게시가 **완전히 같다**.

### 소금길이 섰다 — 지역차가 처음으로 생긴 축

```
  해안 마을 3곳(바다까지 ≤960px — 회부_자염 §C 표의 첫 칸 · 독립 재측정이 회부와 일치)
    어부 부산물 0.05(기존 광부 소금 비율 그대로) → 소금 산출
  전 마을      인구 비례 소비 SALT_DAILY_PC = 0.01/인·일 (재고 내에서만 · 부족해도 페널티 없음)
  ⇒ 내륙÷해안 가격  1.38 → **3.92** (시드 1020) · 3.64 → 5.37 · 2.38 → 5.61
  ⇒ 캐러밴 소금 유통 0 → 27 · 2 → 34 · 0 → 22 건
```
⚠**갈래 ①(해안선 확장 없음)이 확정**이다 — 소금은 원정이고, 시작 광장에서 바다까지 도보 16.1분이다.

### 캐러밴 시계가 다시 맞는다

재routing·빈손귀환에서 `c.distance` 만 갈아치우고 `c.travelDays` 를 두던 자리 둘에서
**`departDay`·`travelDays` 를 같이 갱신**한다 ⇒ `arriveDay − departDay === travelDays ===
travelDaysForDistance(distance)` 회복. `test-events ⑲` 의 **꺾인 캐러밴 제외 필터를 없앴다**(전수 대조).
⚠`departDay` 를 옮기는 쪽을 골랐다 — `server/central.js:530` 이 `arriveDay − departDay` 로 캐러밴
위치를 보간하기 때문이다(누적 경과일을 분모로 쓰면 재routing 캐러밴이 화면에서 뒤처진다).

★**소문은 이 값을 안 읽는다** — `server/rumor.js` 가 거리에서 독립 재계산한다(`travelDaysOf`).
지시서의 *"T7 도달일이 이 시계를 쓴다"* 는 사실과 달랐다. 도달일은 오염된 적이 없다.

### ⚠남은 것 · 회부
* `travelDays` 를 미는 자리가 **존 서버에 셋 더** 있다(로컬 우회·도착 임박 가드·고립 귀환) — econ 밖이라
  3사본 규약 없이 고칠 수 있는데 이 카드는 econ 만 열었다.
* `travelDaysForDistance` 가 **export 안 된다** — 그래서 `rumor.js` 가 "한 줄 거울 + 교차 계약 검사"를 택했다.
* **사건 밀도 1.92~1.96일/건**이 캐논(2~3일) 하한 아래다. 옛도 이미 2.10(상류 T50 몫), T17 은 −0.18일.
  밀도 문턱은 `EV_*` 손잡이 소관 — 다음 카드가 판정할 자리.

---

## 3-u. ★★2026-08-27 배치 — **마을 거래소: 플레이어 물물교환**

> 재민 원문: *"마을마다 거래소가 있어서 거기서 모든 경제학이 계산되고 npc끼리 교역하잖아 —
> 거기서 플레이어도 물물교환할 수 있어야 해."*
> 제1 규약: **가격 사본을 만들면 그 순간 실패다.** 가격 반응은 **공짜다** —
> 재고가 움직이면 시세는 저절로 따라온다. "가격 갱신" 코드를 짜고 있다면 사본을 만들고 있는 것이다.

### ★§0 인벤토리 — 지시서 전제와 다른 게 둘이었다

1. **곡식(`food`)에는 플레이어 아이템이 없다.** 곳간 대응표(`PV_DEPOSIT_MAP`)가 정본인데
   `food` 를 가리키는 아이템이 한 개도 없다. ⇒ 지시서의 "물고기→**곡물**"은 **문자 그대로는 실행 불가**다.
   대신 두 뜻을 다 지켰다: **표시는 곡식 환산**(`PRICE_NUMERAIRE=food`)이고 **내는 건 물고기**다.
   받을 품목은 그 마을 시세표에서 **살아 있는 것**을 하네스가 골라 쓰고 무엇을 골랐는지 로그에 남긴다.
   (회부 B-2 — 곡식을 들 수 있게 할 것인가.)
2. **물리 상한 부품이 `events.js` 안에 인라인**돼 있었다(B-1 배치). 두 벌을 만들지 않으려고
   **`events.payableQty(stock, frac)` 로 추출**하고 게시판 보상도 그 함수를 쓰게 바꿨다 —
   한쪽만 고쳐질 길이 구조적으로 없다(`test-trade ①` 이 양쪽 소스를 다 확인한다).

### 무엇이 생겼나

| 층 | 파일 | 요지 |
|---|---|---|
| 정본 | **`server/trade.js`**(신규 ~270줄) | 교환비 · 스프레드 · **조각 적분** · 금지/잠금 · 원자적 실행 |
| 정본 부품 | `server/events.js` | `payableQty`(B-1 과 공용) · `pricesFresh`(지금 재고 시세) |
| 배선 | `server/villages.js` | `villageTradeBoard/Quote/Exec` — 셋 다 **첫 줄이 260px 게이트** |
| 배선 | `server/zone.js` | `village_trade` · `village_trade_quote` · `village_trade_exec` |
| 화면 | `public/client.js`·`index.html`·`style.css` | §8 프레임 **둘째 칸**(Shift+T) · 세 클릭 문법 |

### ① 교환비 = 정본 그림자가격의 비율 (사본 0)

* 값은 `econV2.computeShadowPrices(v)` **한 함수**에서만 온다. `trade.js` 에는
  가격을 계산하는 줄이 **하나도 없고**, `computeShadowPrices` 를 직접 부르지도 않는다(접근자만).
* 마을이 살 땐 `p×(1−s)`, 팔 땐 `p×(1+s)`. **`TRADE_SPREAD` s = 0.10** 채택.
  근거 ⓐ 왕복 무한 차익 봉쇄 ⓑ "교역 세금 = 물물" 캐논의 플레이어 판(마진은 **물자로** 마을에 남는다).
* **물리 상한**: 한 거래가 그 품목 재고의 `TRADE_STOCK_FRAC`(=`EV_REW_STOCK_FRAC` 0.25)을 못 넘는다.
  넘치면 **거절이 아니라 절삭**이다(살 수 있는 만큼은 산다).

### ② ★★조각 적분(`planSliced`) — 하네스가 잡은 진짜 결함

1차 실장은 **거래 전 시세로 전량**을 계산했다. `test-trade ②`(스프레드 0 대조군)가 잡았다:
**나무 14 → 돌 45 → 나무 18.** 없던 나무 4개가 생겼다.
원인: 내가 팔면 그게 싸지고 사면 그게 비싸지는데, 되팔 때 **두 다리가 둘 다 유리**해진다 —
내 거래가 만든 가격 변화를 내가 그대로 주워 먹는다(한 번에 재고의 25%를 움직이니 폭이 s=10%보다 크다).
⇒ **곡선을 따라 적분**한다. 거래를 12조각으로 나눠 조각마다 **그때의 시세**로 값을 매긴다.
지금: s=0 에서 **정확히 본전**(19→46→19), s=0.10 에서 왕복 **−30%**.
※ 견적의 `maxGive` 는 곡선이라 나눗셈으로 못 내서 **이분 탐색 24회**로 낸다.

### ③ ★★가격 반응이 화면에 보이는가 — `e2e-trade ⑤` 가 잡은 것

**하루 캐시를 쓰고 있었다.** `board/quote` 가 `Events.pricesOf`(=`v._priceCache`, NPC 교역 틱이
**하루 한 번** 덮는 값)를 봐서, 같은 날 안에서는 재고가 어떻게 움직여도 같은 값을 돌려줬다.
별도 프로브 실측(캐시 warm → 날 얼림 → 대량 매도):

```
  나무  재고 19.95 → 26.95 (+35%)   표시 1.208 → 1.208   견적가 2.734022 → 2.734022
  돌    재고 150.26 → 116.26 (−23%) 표시 0.161 → 0.161   견적가 0.363397 → 0.363397
  해동 후 하루:                      표시 1.208 → 1.098   ← 그제서야 움직인다
```

결과가 둘이었다 — ⓐ **이 배치의 존재 이유가 화면에서 사라진다**(첫 30분 ② "내가 판 물건에 값이
움직인다") ⓑ **표시된 비율과 실제 수령량이 갈린다**(수령량은 `planSliced` 가 지금 재고로 낸다).
⇒ 거래소가 보는 시세는 **지금 재고의 함수**다. 하루 캐시는 NPC 교역 틱의 **성능 장치**일 뿐
정본의 정의가 아니다. `Events.pricesFresh` 로 고쳤다.
⚠**econ 변경이 아니다** — NPC 교역은 종전대로 제 하루 캐시를 읽는다(그 경로는 한 줄도 안 건드렸다).
플레이어가 움직인 재고는 다음 날 캐시가 다시 계산될 때 NPC 쪽에도 그대로 반영된다.

같이 잡힌 것: **표시를 `toFixed(3)` 으로 하고 있었다.** 한 마을 안 시세가 47.79 부터 0.0047 까지
걸치는데 소수점 고정이면 싼 쪽 네 품목이 화면에서 전부 "0.005"로 **같아 보인다**.
⇒ **유효숫자 4자리**로 바꿨다. 값이 같아 보이는 표는 정보가 아니다.

### ④ ★계측기가 자기 눈을 가린 사례 **세 번** — 전부 하네스 결함이었다

⑤는 **네 번 만에** 통과했다. 첫 실패만 제품 결함(위 ③)이고 **나머지 셋은 계측기**였다. 셋 다 남긴다:

* **ⓑ 가격 바닥을 골랐다.** `computeShadowPrices` 는 희소도를 `PRICE_ADJ_MIN..maxAdj` 로 **클램프**한다 —
  과잉 재고 품목은 바닥에 붙어 **재고가 늘어도 값이 안 움직인다**(글럿 바닥의 존재 이유 그 자체).
  1차 하네스는 "마을이 팔 수 있는 양이 가장 많은" 품목을 골랐는데 그건 곧 **가장 과잉인 품목**이라
  바닥일 확률이 가장 높은 선택이었다. **자기가 못 움직일 걸 골라 놓고 "안 움직인다"고 보고했다.**
* **ⓒ 두 판정을 한 짝에 묶었다.** "판 것 ↓ **그리고** 산 것 ↑"을 한 짝에서 동시에 요구했더니,
  러너 실행에서 `가죽→고기` 의 가죽이 **0.03226 → 0.0164 로 반토막 났는데도** 고기가 바닥이라 전체 실패였다.
  ⇒ 판정을 쪼개고 **각자 후보를 순회**하게 했다.
* **ⓓ 거래가 안 일어난 걸 "값이 안 움직였다"로 읽었다.** 받을 것 후보 6종이 전부
  `재고 10.41 → 10.41` 이었는데, 그건 **교환이 거절당한 것**이었다(비싼 물건을 내려는데 마을이
  값을 못 치르면 거절된다). 게다가 **실패엔 서버가 시세표를 다시 안 보내니 화면도 그대로**라
  "아무것도 안 변했다"와 구별이 안 된다. ⇒ `push()` 가 **성사 건수를 재고 변화로** 세고(알림 링버퍼는
  40칸이라 못 믿는다), 0 이면 서버 거절 문구를 찍은 뒤 다음 후보로 넘어간다.
  짝을 고를 때 **마을이 값을 치를 수 있는지**(`재고상한 × 시세 > 한 개 값 × 1.3`)를 먼저 본다.

지금 하네스는 후보를 순회하며 **실측으로** 고르고, 어떤 짝으로 성공했는지·건너뛴 이유·거절 문구를
전부 찍는다. 독립 2회 실행이 **서로 다른 마을 상태에서 각각 40/0**:
`나무 1.568→1.199`(팔았다) · `1.199→1.395`(샀다) / `약초 1.064→0.7309` · `0.7309→0.8148`.

### ⑤ 정보 비대칭 · 게시판 우위 · 금지 품목

* **원격 시세 조회 API 를 안 만들었다.** `board/quote/exec` **셋 다** 첫 줄이 `_villageNear`(260px,
  브리핑과 같은 게이트)다 — 이웃 마을 시세는 **걸어가서 보는 것**이고 그 비대칭이 콘텐츠다(§3.2).
  `test-trade ⑦` 이 3/3 을 구조로, `e2e-trade ①` 이 **클라 우회 시도**로 확인한다
  (vid 를 알고 직접 물어도 거절 · 읽기만 막고 쓰기를 여는 실수도 따로 확인).
* **게시판이 늘 낫다**: ×1.200(프리미엄 0.2) > ×0.818(스프레드 0.1). 둘 다 실제로 걸려 있음도 확인.
  (게시 시점 고정이라는 단서는 회부 B-7.)
* **잠긴 품목**: 열린 의뢰가 **보상으로 약속한 품목**은 마을이 안 판다 — 안 그러면 플레이어가 낸 뒤에
  "갚을 게 없다"를 듣게 된다(게스트보상 배치의 족보). 내부 필드(`_` 로 시작)도 재화가 아니다.
* **원자적**: ①견적 ②**재고를 먼저 잡는다** ③내 물건 차감 ④곳간 입고(정본 `playerVillageDeposit`)
  ⑤실패하면 되돌린다. 동시 경쟁은 ②가 관문 — 둘이 같은 마지막 재고를 못 가져간다(실측 확인).

### ⑥ 화면 — §8 프레임의 **둘째 칸**

패널을 새로 만들지 않았다. 3-v 가 세운 프레임에 **탭 하나**를 등록했다(그 3단계가 문서대로 돌아가는지의 실증):
`#sidebar` 아이콘 한 줄 + 제목표 한글 + `renderSide` 분기. **Shift+T.**
문법은 세 클릭 — 📤 낼 것 → 📥 받을 것 → **바꾼다**. 수치는 전부 서버가 준 것을 그대로 그린다.
열 때 **서버에 다시 물어본다**(클라가 시세를 캐시하면 그게 곧 낡은 시세다).

### 검증

```
  test-trade   38/0   (구조 판정: 왕복비 곱 = ((1−s)/(1+s))² · 소스에 computeShadowPrices 없음 · 상한 공용 함수)
  e2e-trade    40/0   (실클라: 반경 밖 거절 2겹 → 접근 → 세 클릭 → 인벤 반영 → **화면에서 값이 움직임**)
  러너 26종     실패 2 : e2e-trade(당시 구판 하네스 · 위 ⓒ) · **e2e-mtfoot 8/9(이 배치 무관 · 3-v 에 기록된 그것)**
```

⚠`e2e-trade` 는 **마을을 켜는 두 번째 실클라 하네스**다(`e2e-events` 와 같이 `VILLAGE_MAX=2`).
시간 모드: **데울 땐 흐르고(0.5초/일) 상호작용 땐 얼린다**(`__e2e_day_freeze`) —
⑤의 판정이 "**내 거래가** 시세를 움직였는가"라 날이 흐르면 원인이 섞인다.

**3시드 800일 기준선 — 한 자리도 안 움직였다**(`payableQty` 추출이 게시판 보상을 안 건드렸다):

```
1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,561   (기록값과 동일)
   7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,487   (기록값과 동일)
  42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,535   (기록값과 동일)
```

### 대리 지표 — `scripts/trade-metrics.js` (신규 · **계측기지 하네스가 아니다 · 러너에 넣지 마라**)

**① 곡식 환산 시세는 마을마다 크게 다르다**(111 게임일 데운 뒤 4마을):

```
   품목      농촌1     어촌2     임업2     광산4     최대/최소
   나무     1.059    647.5    1.002    95.27      ×646
   가죽     0.015    1.238   0.4185    7.686      ×512
    돌      75.07    5.755    262.1   0.8317      ×315
   광석   0.01264   0.0811   0.4501  0.07331      ×35.6
```

⇒ **걸어가서 사고팔 이유가 실제로 크다**(§3.2 정보 비대칭 = 콘텐츠).
⚠갓 시딩한 마을은 재고가 서로 똑같아 **8종이 ×1.0** 으로 나온다 — 그건 "안 다르다"가 아니라
**아직 살아 보지 않았다**는 뜻이다(1차 측정이 그렇게 나왔다). 데운 뒤 재라.

**② 왕복 손실 vs 스프레드** (광산4 · 고기↔생선 40개 왕복):

```
     s     측정 손실   스프레드만   가격충격 몫
  0.00      32.5%      0.0%      32.5%p
  0.05      32.5%     18.1%      14.4%p
  0.10 ★    35.0%     33.1%       1.9%p
  0.20      57.5%     55.6%       1.9%p
```

⇒ 단조 증가하고, 채택값 s=0.10 에서는 손실이 **거의 스프레드 그대로**다(가격충격 1.9%p).
⇒ **s=0 에서도 32.5% 손실** — 곡선 적분이 무한 차익을 봉쇄한다는 것의 실측.
⚠1차 측정은 거래 크기를 재고에 견주지 않아 `열매 1,761개 ↔ 가죽 11개`(재고의 10배)가 나왔고
손실이 81→73→75→78→77% 로 **널뛰었다** — 스프레드가 아니라 **내가 만든 가격 붕괴**를 재고 있었다.
계측기는 이제 **결론을 데이터에서 낸다**(단조성을 스스로 판정해 문구를 바꾼다).

**③ 물리 상한은 거의 항상 문다**: 마을×품목 34칸 중 **24칸(71%)이 "한 개도 못 내준다"**.
같은 상한을 쓰는 게시판 쪽 800일 실측이 그걸 더 크게 보여 준다 —
**못 갚아서 못 게시한 의뢰 44,875~54,953건** 대 실제 게시 1,487~1,561건.
⇒ 작은 마을 거래소가 대부분 비어 보이는 건 결함이 아니라 B-1 캐논이다(회부 B-5).


