# 인계 — Z 존 서버·계정·운영

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.
## T56. ★★★2026-09-02 — **쓰러짐 2차: 소리와 두 동사** (`server/rescue.js` 신설)

> T43 이 스스로 회부한 두 구멍 — *"쓰러진 걸 남이 모른다"* 와 *"구조 동사가 하나뿐"* — 을 닫았다.
> 몸 쪽 절은 `인계/B-신체서버.md` 3-자. 회부는 `회부/회부_쓰러짐_다음층.md`.
> **클라 무접촉**(T55 가 `41-h-*`·`30-n-net` 을 만지는 중) · **새 기능은 새 파일**.

### 접점 — zone 은 **네 줄**이다
```
  require('./rescue')                          ← 모듈
  Rescue.init({ ... })                         ← 이미 있는 것만 넘긴다(사본 금지 · T11 과 같은 규약)
  Rescue.handleChat(player, text)  1줄         ← 채팅 갈래(`Membership.handleChat` 바로 아래)
  Rescue.onDown(p, Date.now())     1줄         ← `damagePlayer` 의 쓰러짐 블록 끝
  + doEat 에 대상 인자(위 3-자) · onboarding 에 `dirWord` export 한 줄
```
★`rescue.js` 는 **판정을 하나도 안 짓는다.** 마을 반경 `SimVillages.shelterAt` · 물/바다
`isWaterTileLocal`/`isSeaTileLocal` · 먹기 `doEat` · 방위말 `Onboarding.dirWord` · 이름표
`ITEM_LABEL_SERVER` · 먹을 것 목록 `FOOD_EFFECTS` — **전부 남의 정본을 부른다.**
⚠이름표만 **함수로** 받는다 — `ITEM_LABEL_SERVER` 는 작물·특산을 흡수한 뒤 **`Rescue.init` 보다
늦게** 완성되므로, 값으로 넘기면 빈 표를 붙든다(부팅 순서에 물린 자리다).

### ① 외침 — **소리가 없으면 창 3분은 근거가 없다**
```
  쓰러진 순간 + 30초마다, 반경 9,600px 안의 **깨어 있는 사람**에게 한 줄:
     🗣️ ○○이(가) 쓰러졌다 — 해 지는 쪽 150걸음
  · 야생만. **마을 안에서는 안 외친다**(부를 사람이 이미 있고, 거기선 죽지도 않는다 — §12).
  · 거리는 **걸음**(1걸음 = 1셀 = 1m = 32px · 실축화 캐논 — 새 환산 0), 방위는 **촌장과 같은 어휘**.
  · 주사위 0 — (지금, 쓰러진 자리, 내 자리)의 순수 함수다. 같은 상황이면 **글자까지 같다**.
```
★**값의 근거는 실측이다**(`scripts/downed-metrics.js`). T43 이 3분의 근거로 적은
*"800px 을 달려오는 데 ~40초"* 는 **산수가 틀렸다** — 800px 은 25m 고 걸어도 12.5초다. 다시 깔았다:

| | 유도 |
|---|---|
| 달려오기 | 스태미나 정본에서 — 첫 질주 21.6초(3,450px) 뒤 **질주·회복 순환 평균 83.6px/s** |
| 구조 예산 | 창 180초 − 붙들기 5초 = **175초** ⇒ 직선 **16,278px(509m)** 까지 온다 |
| 외침 반경 **9,600px** | **소리가 정한다**(조용한 들의 비명 ≈ 300m). 도달로 **검산**: 가장자리 95초 ⇒ **여유 80초(46%)** = 지형 우회분 |
| 외침 주기 **30초** | 반경으로 걸어 들어온 사람의 무지 시간 ≤ 30초 ≪ 여유 80초. 창 동안 6번 — 도배가 아니다 |

★시계는 `rescue.js` 가 **스스로 든다**(쓰러진 사람이 0 이면 시계도 없다 · `unref`).
zone 의 30Hz 틱에 30초에 한 번 도는 일을 얹지 않는다.

### ② 구조 동사 둘 — 채팅 명령(클라 코드 **0줄** · T11 선례)
```
  /먹이기 <음식>   옆(80px)에 쓰러진 사람에게 **내 인벤의** 음식을. 창이 지났으면 거절.
                   품목은 키도 한글 이름표도 받는다. 인자가 없으면 가진 것을 알려 준다.
  /물              들고 온 민물이 먼저 · 없으면 물가에서 **손으로 떠**(+35 = 정본 한 모금).
```
★★**`/물` 은 갈래가 둘이다** — 이 카드가 도는 동안 **T54 가 민물 휴대를 열었다**(T52 회부 A 가 닫혔다):
```
  ⓐ 들고 온 물이 먼저   인벤에 `fresh_water` 가 있으면 **`doEat` 한 문으로** 먹인다(물가 아니어도 된다).
                        회복량도 빈 병 반납도 표가 한다 — 여기서 숫자를 안 짠다.
  ⓑ 손이 비었으면      물가에서 **손으로 떠** 먹인다(+35). 그러자면 **업어서 강가로 옮기게 된다**
                        — §12 의 "불가로 옮기기"가 그 자리다.
```
★★**빈 병은 준 사람 손에 남는다**(`doEat` 의 `returns` 는 주는 쪽). 물을 먹여 주고 병까지 넘어가면
그건 **주기지 먹이기가 아니다** — "물건은 주는 쪽, 몸은 받는 쪽"이 이 함수의 규약이다.
★민물 품목 id 는 `rescue.js` 가 `tidal.FRESH` 를 **직접** 묻는다. zone 을 거치지 않는 이유:
**zone 이 갯벌 정본을 부르는 자리는 셋으로 못 박혀 있다**(`test-tidal` 이 T54 의 예산을 지킨다) —
초안이 `Rescue.init` 에 한 줄을 얹었다가 그 하네스가 넷째 줄을 잡아냈다.
★짠물은 못 먹인다(T4 의 판정을 다시 부른다) — *"물이 없다"* 가 아니라 **"짠물이다"** 라고 말한다.
★자격은 T43 과 같다 — **소속을 안 본다**(열쇠가 없다). 게이트는 거리와 창뿐이다.

### ③ 새 규칙 0 — 먹이면 안 눕고, 안 먹이면 도로 눕는다
T43 이 세운 *"구조는 몸을 안 씻는다"* 의 반대편이다. 이 카드는 **규칙을 한 줄도 안 더했다**:
```
  먹이고 일으킴  → extremeHpRate 0  → HP 20 → 49 (자연 회복이 이긴다)
  안 먹이고 일으킴 → 0.06944 HP/게임분 → 14초에 1HP · 남은 시간 5분  (T44 의 같은 식이 한다)
```
`test-downed ⑬-나` 가 **두 갈래를 나란히** 잰다.

### 검증
```
  test-downed      53 → **80/0**(외침 결정론·돌연변이 2종·먹이기 원장·짠물 거부·두 갈래)
  e2e-downed       22 → **27/0**(실클라 둘 — 외침이 B 화면에 뜨고 · `/먹이기` 로 먹이고 · 안 눕는다)
  downed-metrics   신규 계측기(러너 밖) — 도달·외침·짐 회수 원정·더위 표
  run-regress      **79종 · 73 통과 / 6 실패**(6건 전부 베이스 A/B 로 T56 무관 귀속)
  econ             3시드 800일 · econ-regress 5시드 — **베이스와 모든 칸 비트 동일**(내 델타 0)
```
★e2e 가 한 번 헛돌았다(하네스 잘못 · 제품 무결): **화면의 완충값은 텔레포트 직후 낡아 있을 수 있다**
(서버가 완충을 1초 메모하고 날씨는 주기적으로만 실어 보낸다). 그래서 마을 한복판을 "완충 0 인 야생"으로
집어 들어 ⓑ 가 통째로 헛돌았다(죽는 대신 **마을 사람이 옮겼다**). ⇒ 두 번째 자물쇠를 걸었다 —
**시딩된 50 마을 중심에서 2,600px 밖**(반경 정본의 최댓값이 2,152px). 그 수는 화면이 아니라 **DB**에서 온다.

---

## T43. ★★★2026-09-02 — **쓰러짐·구조 v1** (HP 0 이 무엇으로 끝나는가)

> §12 [재민 확정 2026-09-02] + `설계/죽음_설계_판정안.md` §5 카드 초안.
> 몸 쪽 절은 `인계/B-신체서버.md` 3-아. 회부는 `회부_쓰러짐_다음층.md`.
> ★종전의 "부활"은 **순간이동 + 무손실 + 풀피**였다. 그게 이 카드로 전부 없어졌다.

### 사슬 — **문이 하나다**
```
  HP 0 ──▶ 쓰러짐(원인 표지 0)
            │
            ├─ 창(3분) 안에 누가 업어 N초 붙들면 ──▶ 일어난다 (HP 20% · 짐 그대로 · 몸은 안 씻긴다)
            │
            └─ 창 소진 · 또는 스스로 포기
                 ├─ 마을 반경 안(`SimVillages.shelterAt > 0`) ─▶ **안 죽는다.** 쉼터로 이송 · 짐 보존
                 └─ 야생 ────────────────────────────────▶ 사망: 짐 전부 그 자리 낙하 · 후유증 3게임일
                       둘 다 **게임 시간이 흐른 뒤**(`DOWN_WAKE_GAMEMIN` 120 = 2분) 깨어난다
```
★`resolveDowned` 하나가 이 갈림을 다 판정한다(창 소진·포기가 **같은 문**으로 들어온다).
★깨어남·구조·이송이 전부 `_wakeUp` **하나**를 지난다 — 세 곳에 흩으면 규약이 갈린다.

### 손잡이 — 전부 env · **채택값에 근거가 붙어 있다**
종전 `RESCUE_WINDOW_MS = 10000` 은 아무 데도 근거가 없었다(죽음 설계 §5-4 가 지적한 자리).

| env | 기본 | 근거 |
|---|---|---|
| `DOWN_RESCUE_WINDOW_MS` | 180000 (3분) | §12. 구조는 옆에 서 있던 사람이 아니라 **소리를 듣고 달려오는 사람**의 일이다(야생 800px ≈ 40초). 10초는 앞엣것만 된다 |
| `DOWN_RESCUE_RANGE_PX` | 80 | 종전 값 유지 |
| `DOWN_RESCUE_HOLD_MS` | 5000 | §12 *"옆에서 N초 붙들기"* |
| `DOWN_RESCUE_HP_FRAC` | 0.2 | §12 *"HP 소폭"*. 종전 0.5(절반)는 소폭이 아니다 |
| `DOWN_CARRY_PERSON_KG` | 60 | 고증표 성인 몸무게. `carry` 소프트 과적 곡선을 **그대로** 탄다 |
| `DOWN_WAKE_GAMEMIN` | 120 | 게임 2시간. **1 게임분 = 1 실초**(시간 구조 불변 캐논 · T44 가 유도해 뒀다) |

### 규약 넷 — 다음 사람이 여기서 갈리지 않게

**① 업기와 붙들기는 한 동작이다.** R = 업기 시작 = 붙들기 시작. 그대로 서 있어도, 걸어서 마을로
옮겨도 **같은 시간이 흐른다**. 같은 키가 내려놓기다(상태 토글 — **새 동사 0**).
`_carrying`/`_carriedBy` 한 쌍이 그 상태의 정본이고, `p._carryingKg` 를 `carry.totalKg` 가 **한 줄로**
더한다 ⇒ 과적 곡선·상한을 한 글자도 안 건드렸다(T12 규약과 같은 결).
⚠업고 있던 사람이 **사라지면**(접속 종료·존 이동) `tickDowned` 가 등을 비운다 — 안 그러면 그 구조자는
60kg 을 평생 진다.

**② 자격의 열쇠를 없앴다.** 서버 `tribeId` / 클라 `tribeName` 로 갈리던 것을(죽음 설계 §0-ⓒ)
맞춘 게 아니라 **양쪽 다 소속을 안 보게** 했다. §12 는 *"다른 플레이어"* 라고만 한다.
⇒ 클라 접점은 `31-m-move.js` 의 `findNearestDownedGuildmate` **두 줄 삭제**가 전부다.

**③ 짐꾸러미는 정본 낙하 경로로, 그리고 안 사라진다.**
`_deathDrop` 은 `_spawnGroundItems` 를 그대로 쓴다 ⇒ **개체 kg 원장도 로트 나이도 그대로 실린다**
(로트는 바닥에서 **계속 부패한다** — §12). `g.keep = 1` 표를 달아 10분 청소가 건너뛴다(디스폰 금지).
⚠**입은 것(equipment)은 안 떨어뜨린다** — 바닥템에 장비 인스턴스를 실을 자리가 없다(회부 B-2).
⚠**지식은 한 자도 안 잃는다**(`craftSkill`·`craftLog`·`fishStats`·`oreLedger` — §6 · 제약 C6).

**④ 깨어날 자리는 **절대 빈 배열이 아니다**.**
`listRespawnOptions` 의 사다리 `사유지 → 길드 → 귀향점 → **가장 가까운 마을 도착 지점**` 에서
4번을 처음으로 코드에 넣었다(주석은 2026-07 부터 약속하고 있었다).
★좌표를 지어내지 않는다 — `SimVillages.clientVillages()` + `Onboarding.arrivalOf(vid)`(온보딩 v2 가
이미 구운 나루터·길목)를 **부른다**. ⚠그 표가 아직 안 데워졌으면(부팅 직후) 마을 **중심**으로 떨어지고
중심은 집·물일 수 있다 ⇒ `nearestVillageWake` 가 나선으로 **설 수 있는 칸**을 찾는다(하네스가 아니라
**제품**이 고친 자리다 — 하네스가 피해 다니면 라이브에서 물 위에 깨어난다).

### "고르는 부활"은 없어졌다 — **부활 패널의 뜻이 바뀌었다**
`tryRespawnChoice` 는 이제 부활이 아니라 **포기**다: *"구조를 기다리지 않고 여기서 깨어나겠다."*
대가는 창이 소진된 것과 **완전히 같다**(마을이면 이송, 야생이면 사망·짐 낙하·후유증).
⇒ 값을 치르는 자리가 하나다. 화면(`#downPanel`)은 그대로 두었다(제약 C7 — 새 패널 0).

### 로그아웃 ≠ 부활 — 그리고 **화면도 안다**
`_acceptConnection` 의 복원이 `parseBody(.., { vitals: true })` 로 바뀌었다(종전 `false`).
⚠**부수 효과**: downed 뿐 아니라 **모든 재접속의 공짜 풀피가 사라진다**(회부 B-3 · `test-handoff-body ④`
를 뒤집어 못 박았다). 되돌리려면 `vitals` 한 글자다.
★그리고 몸만 이어지면 반쪽이다 — `player_downed` 는 **피해 순간에만** 나가서 다시 들어온 사람은
"못 움직이는데 화면은 멀쩡한" 상태가 됐다(배치 5 가 배운 층이 여기서 또 났다).
⇒ **새 메시지를 안 만들고** welcome 뒤에 같은 메시지를 한 번 더 보낸다. 창(`downedAt`)은 안 만진다 —
로그아웃으로 시간을 되돌릴 수 없다는 것이 §12 의 요점이다.

### 검증
```
  scripts/test-downed.js   53/0  (신규 · @regress · 서버 정본)
  scripts/e2e-downed.js    22/0  (신규 · @regress · 실클라 둘 — 업기·사망·짐 회수 원정·재접속)
  scripts/run-regress.sh   72종 — test-handoff-body ④ 를 **뒤집었다**(정책이 바뀌었으므로). 그 외 무변
  3시드 800일 econ 기준선  비트 동일(플레이어 전용 층)
```

---
## T42. ★2026-09-01 — 교역로를 **다시 파지 않는다** (게임일 경계의 남은 바닥)

T41 뒤 조각 최대는 **캐러밴 한 대의 콜드 A\***(1,265~1,896ms)였다. 계산을 싸게 만들려면
`sim/path-core.js`(랩·서버 공용 정본 · 3사본 토폴로지)를 째야 하는데 그건 금지다.
⇒ **일어나지 않게** 한다.

### 규약
| | |
|---|---|
| **영속** | 콜드로 판 길을 존 DB `trade_routes` 에 남긴다. 부팅 때 `_routePrime()` 이 물려받는다 |
| **세계 서명** | `hanbando-terrain.json` + `zone-config.js` 의 (크기:mtime). 다르면 표를 **통째로 버린다** |
| **무효화** | `invalidateTradeDistances` 가 메모리 캐시와 **DB 표를 같이** 비운다 — 한 자리에서 |
| **선계산** | 부팅 뒤, **① 사람이 없고 ② 날아가는 central 쓰기·처리 중인 접속이 없고(`ioBusy`) ③ 그것들이 끝난 지 30초 지났고 ④ 직전 걸음에서 250ms 쉬었을 때** 한 걸음(`_routeWarmStep`). 사람이 들어오면 그 자리에서 멈춘다 |
| 데울 쌍 | 마을마다 가까운 `VILLAGE_ROUTE_WARM_NEAR`(20)곳 — 전쌍 1,225 를 다 데울 이유가 없다(실측 579쌍) |

★**같은 답인가**의 근거는 `sim/path-core.js` 머리 주석이 스스로 적어 뒀다 —
*"왕복 대칭 … 갔던 경로를 저장할 필요 없음: 같은 두 점·같은 세계면 재계산이 같은 복도를
결정론으로 재현"*. 그리고 **보증은 밟아 봐야 검사가 된다** — `/routedbg?audit=N` 이 캐시에 든
경로를 실제로 다시 계산해 비교한다(T41 단조성 감사와 같은 규약). 불일치 0.

### ★★선계산 1차는 라이브 결함이었다 — **사람이 없다 ≠ 할 일이 없다**
1차 게이트는 *"사람이 있으면 멈춘다"* 하나였고, 그 조건 아래 **30Hz 마다 쉼 없이** 한 걸음씩 돌았다.
실측: 걸음당 평균 ~50ms 인데 **한 걸음 최대 2,393ms** ⇒ 70초 동안 루프가 사실상 **100% 막혔다**.

그러면 무슨 일이 나는가 — 러너가 잡았다. `e2e-rumor ⑦`(복귀 브리핑)이 *"부재 0일"*.
사람이 나가며 `savePlayer` 가 central 로 보낸 쓰기가 **소켓 밖으로 나가지도 못한 채** 루프 순번을
기다렸고, 그 사람이 다시 들어올 때까지 안 날았다. `e2e-village`(7건)·`test-tick-slicer ⑥⑧` 도 같은 원인.
`origin/main` 35/0 ↔ 이 배치 4실패 ↔ `VILLAGE_ROUTE_WARM=0` 35/0 으로 **A/B 귀속**했다.

⇒ 한가함의 정의를 고쳤다(위 표). 걸음의 시각은 **끝난 뒤**부터 잰다 — 2.4초짜리 걸음 뒤에도 진짜로 쉬게.

★★**시계만으로는 모자랐다.** 유예 10초로 고친 뒤 러너에서 또 깨졌다 — 부재 창이 13초라 유예의
**꼬리**에 데우기가 끼어들었고, 둘째 얼굴이 더 나빴다: **입장 중인 손님은 `players` 에 안 보인다**
(인증·로드·스폰 단계). "사람이 없다"가 두 번 거짓말한 것이다.
⇒ 시계 말고 **일 자체**를 센다. `zone.js` 가 둘을 세어 `SimVillages.init` 에 술어로 넘긴다:
```
  savePlayer 가 돌려주는 Promise           → 날아가는 central 쓰기  (fire-and-forget ≠ 끝났다)
  wss.on('connection') ~ stage='ready'     → 처리 중인 접속        (등록 전엔 아무도 없어 보인다)
  ⇒ ioBusy() 참이면 손을 뗀다 · ioQuietMs() 가 유예를 넘어야 다시 시작한다
  ⚠관전자 경로도 'ready' 대신 그 자리에서 센 것을 놓는다 — 안 놓으면 배경 작업이 영영 안 돈다
```
~~**⚠고친 것은 빈도다. 한 걸음의 길이(2.4초)는 못 줄인다** — `path-core` 수술(재개 가능 A\*)이고 회부다.~~
**★[T85 2026-09-03] 그 회부를 닫았다 — 아래 `T42-c`.**

### 실측 (`scripts/test-route-persist.js` 29/0)
```
  1판(첫 부팅)     콜드 26 · 최대 1,280ms · DB 26행
  2판(재기동)      **물려받은 26쌍** · 그 중 다시 판 것 **0**
  무효화           메모리 46 → 0 · **DB 46 → 0**(둘 다)
  서명 바뀜        물려받은 **0쌍**
  선계산           579쌍 **완주**(116초) · 그 뒤 3일에 새로 판 길 **0쌍**
  ★조각 최대       caravan 242~482ms → **life 272ms** (가장 큰 조각이 더는 캐러밴이 아니다)
  ★★유예          큐를 세운 지 11초(유예 15초) 시점에 데운 쌍 **0/579**
  ★★간격          데우는 동안 `/health` 응답 간격 최대 **2,687ms** ≤ 한 걸음 2,618ms + 1,500
                   (막히는 건 **걸음 하나 동안뿐** · 걸음이 끝나면 곧바로 대답한다)
  ★★배선          `ioBusy`·`ioQuietMs` 가 실제로 배선돼 있다(빠지면 유예가 시계 하나로 되돌아간다)
```

### 손잡이 밖 관측 하나 — `tickPerf()` 가 **창 전체**의 조각 최대를 같이 낸다 [T42-b · 계측 전용]
`last` 는 **마지막 하루**의 값이다. 루프 지연 히스토그램은 **창 전체**를 담으므로 둘을 나란히 놓으면
자를 두 개 쓰는 셈이고, 실제로 `test-tick-slicer ⑥a` 가 그렇게 틀리게 떨어졌다
(루프 1,337ms vs 마지막 하루 조각 419ms — 그런데 **창 전체 최대 조각은 1,315ms(caravan)** 였고
루프 최대는 정확히 그 조각 하나였다). ⇒ `maxChunk`·`maxChunkAt`·`frameMax` 를 창 전체로도 낸다.

### 손잡이
`VILLAGE_ROUTE_WARM`(1 · **0 = 선계산 끔 = 하네스 대조군**) · `VILLAGE_ROUTE_WARM_NEAR`(20) ·
★`VILLAGE_ROUTE_WARM_GAP_MS`(**250** · 걸음 사이 쉬는 시간 · 0 = 1차의 그 동작) ·
★`VILLAGE_ROUTE_WARM_IDLE_MS`(**30000** · 사람이 나가고 · 쓰기가 착지하고 · 접속 처리가 끝난 뒤 기다리는 시간)
관측창 `GET /routedbg[?audit=N][?invalidate=1]`(E2E_GIVE 게이트) — `invalidate` 는 **진짜 훅**을 부른다(사본 금지).
`/routedbg` 가 `warmGapMs`·`warmIdleMs`·`warmOn`·`ioBusy`·`ioQuietMs` 를 **그대로** 말한다(하네스가 사본을 안 만들게).

### ⚠남은 것 · 안 한 것
* **목표(조각 최대 ≤32ms) 미달.** 새 바닥은 `terr` 181ms / `1마을 life` 233~272ms — 둘 다 **마을 단위**라
  이미 조각이고, 슬라이서는 조각보다 잘게 못 자른다. 다음은 그 **안**을 째는 배치다.
* ★**사람이 붙어 있는 판에서는 이 절의 이득이 안 난다.** 선계산은 무인일 때만 돌고 영속은 **재기동 뒤**에 듣는다.
  `e2e-rtt`(사람이 50초 내내 붙어 있는 판)의 조각 최대는 여전히 `caravan` 369~768ms 다.
  ⇒ **첫 부팅 첫 며칠**은 그대로 무겁다. 그건 재개 가능 A\* 의 몫(회부).
* 결산·곳간 증설 조각화(②)는 **이미 돼 있었다** — T1 이 `life` 를 마을별 조각으로 등재했다.
  더 쪼개도 `1마을 life` 233ms 중 `headless` 232ms 가 남아 이득이 없다(실측).
* 스폰·저장큐(③)는 **몰리지 않는다** — 아래 참조.

### ★N.9 의 내역 두 줄은 **로그 오독**이다(§0 실측)
```
  "NPC 1,560 스폰"  → 그 줄(`스폰 NPC ${npcCount}`)은 그 틱의 스폰 수가 아니라
                      **현재 스폰돼 있는 NPC 총수**다. 하루 경계 스폰은 `POP_SYNC_PER_DAY=2`×마을 = ≤100.
  "저장큐 50행"     → 큐에 **세운** 길이다. 그 뒤 1마을/틱씩 배수된다(T1 이전부터 그랬다).
```
그리고 N.9 는 **T1 이전 빌드**에서 잰 값이다(로그 형식이 옛것 — `조각 N개` 가 없다).
남은 실제 내역은 캐러밴 A* ×16 이고, 이 절이 그걸 없앴다.

---

## T47. ★★2026-09-01 — **몸의 정본은 한 곳, 직렬화는 한 함수** (존 핸드오프 소실 수리)

재민의 부하 실측이 잡은 라이브 결함(`KNOWN_ISSUES.md` N.6): **존을 넘으면 도구가 영구히 사라진다.**
28회 왕복 전부 재현. 원인은 필드 하나가 아니라 **복원 코드가 두 벌**이었다는 것이다.

### 규약 — 새 필드를 몸에 더할 자리는 **한 곳**이다
```
  serializeBody(p)          몸 → 객체.  저장·핸드오프가 **둘 다** 이걸 쓴다.
  parseBody(raw, {vitals})  객체 → 몸.  일반 접속·핸드오프 도착이 **둘 다** 이걸 쓴다.
                            옛 형식(`{axe:3}`) 변환도 이 안에 있다.
```
⚠`savePlayer` 나 도착 경로에 **직접 필드를 쓰지 마라.** 그렇게 하면 T47 이 고친 그 결함이 되돌아온다.
필드를 하나 더하면 저장·핸드오프·재접속이 **동시에** 따라간다 — 그게 이 규약의 값이다.

| 갈래 | vitals | 왜 |
|---|---|---|
| `savePlayer` → `tools_json` | (저장은 다 한다) | 블롭이라 스키마 변경 0 |
| 일반 접속 복원 | **false** | 로그아웃 후 재접속의 **풀피는 정책**이다 — 죽음 설계(T8) 소관. 이 배치가 안 바꿨다 |
| 핸드오프 도착 | **true** | 존을 넘는 건 **한 접속의 연속**이다. 같은 존 안에서는 `_takeover` 가 이미 HP 를 잇고 있었다 |

### 핸드오프 흐름 · 출처 규칙
```
  출발: await savePlayer(...)        ← ★기다린다(종전 fire-and-forget). 행이 먼저 최신이 되게.
        payload.body = serializeBody(p)   ← 같은 스냅샷을 페이로드에도(안전망)
  도착: pending.body 가 있으면 그것.  없으면 central 행 `tools_json`.
```
★**시각 비교를 안 한다.** 행과 페이로드가 `serializeBody` **한 번의 같은 스냅샷**이라 중재할 경합이
없기 때문이다. 행 폴백이 필요한 경우는 **몸을 안 싣는 옛 존에서 넘어올 때**(롤링 배포 창) 하나뿐이고,
그때는 떠나기 직전 저장한 행이 유일한 진실이다. ⇒ 그 갈래는 픽스처로 **실제로 밟아 검사한다**
(`E2E_HANDOFF_NO_BODY=1` · `E2E_GIVE` 게이트).

### 몸의 정본이 있는 곳 (실측 §0-ⓒ)
**central `players` 행 하나뿐이다.** 존 로컬 DB 에는 플레이어 몸이 없다(건물·자원·마을만).
그래서 "도착이 행을 안 읽는다"가 곧 소실이었다.

### 전/후 (`scripts/test-handoff-body.js` 18/0 · 2존 실서버)
```
  수리 전  도구 axe/100+pickaxe/100 → 도착 **(없음)** → 재접속 **(없음)**  = 영구 소실
           kg원장 fish[2·0.4·1.1]  → 도착 **(빈)**
  수리 후  내구 부분(도구·장비·슬롯·숙련·제작이력·원석장부·어획·kg원장·로트) **비트 동일**
           신체 cold 0.7994 → 0.7939 (이어짐) · HP 43 → 52.85 (이어짐 + 도착 후 회복분)
           재접속 여전히 풀피 = **정책 무변**
           몸 없는 페이로드(픽스처)에서도 central 행 폴백이 몸을 살린다
```

### 곁가지 — N.8 수리 (`ZONE_TEST_INV` 가 central 살아 있으면 무력화되던 것)
그 값은 `inventory` 의 **초기값**으로만 얹혀 있었고 계정 행 복원이 변수를 통째로 덮었다.
이제 복원 **뒤에** 없는 키만 채운다(idempotent · env 미설정이면 0회 = 운영 무변).

### 관측창
`GET /bodydbg?name=` — `serializeBody` **그 자체**를 내준다(읽기 전용 · `E2E_GIVE=1` 게이트).
welcome 은 몸의 일부만 싣는다. 왕복 전후로 13가지가 다 같은가를 재려면 정본을 봐야 한다(사본 금지).
`__e2e_body` 에 `hp` 를 더했다 — HP 가 이어지는지 **재려면** 깎아 놓을 수 있어야 한다.

---

## T41. ★2026-09-01 — 조각의 **바닥**: 집터를 매일 헛되이 훑고 있었다

T1 이 하루를 조각으로 나눈 뒤 남은 바닥은 **조각 하나의 크기**였다. 그 셋을 팠고, 첫째를 고쳤다.

### 실측이 말한 것 (마을 50곳 · econ day 515)
```
  `_lifeAddHouseSite`  12일간 호출 238 · **성공 0** · 빈손 238 — 일틱의 56%
    거부 사유 248,191건 중 **92.3%가 `기존 집과 너무 가까움(<18)`**
    ⇒ 그 영토엔 자리가 **구조적으로 없다.** 내일 훑어도 같은 답이다.
  캐러밴 A*   캐시는 **이미 있었다**(`state.routeCache`) — 남은 건 콜드 1,265~1,695ms
  광맥 스캔   `vil._oreCells` 로 **마을당 1회** — day3 이후 증가 0(부팅 워밍업)
```

### 규약 — "못 찾은 자리는 기억한다"
**언제 다시 찾아도 되는가 = 후보가 늘어날 수 있을 때만.** `reject` 가 보는 상태 여덟 중 여섯은
줄기만 한다(= 더 거부된다). 늘릴 수 있는 사건은 셋뿐이고, 그것이 곧 표지가 서는 자리다:

| 사건 | 부르는 것 | 왜 |
|---|---|---|
| `_terrGrow` 가 셀을 더했을 때 | `lifeSiteDirty(vil)` | 새 셀 = 새 후보. **거부 캐시는 유지**(옛 거부는 안 뒤집힌다) |
| `_psite`(의뢰 집터) 소멸 | `lifeSiteReset(vil)` | 반경 18 이 다시 열린다 — 마을 집과 달리 `_houseCells` 에 안 들어간다 |
| 지형 차단 완화(벽·다리) | `lifeSiteResetAll()` (`invalidateTradeDistances` 안) | 옛 거부가 뒤집힐 수 있다 |
| `_potSet` 에서 **삭제만** 하는 두 곳(의뢰 터·집터 선점) | `lifeSiteReset(vil)` | strict `farmAt` 이 false 로 뒤집힌다 |

★**새 상태를 만들 때 이 표를 갱신해라.** 표지를 놓치면 집이 안 지어지는 **조용한 결함**이 된다 —
그래서 안전망이 있다: `LIFE_SITE_RESCAN_DAYS`(7)마다 표지를 무시하고 전수로 돈다.
**마을마다 다른 날**에 돈다(`(day + dbId) % N`) — 처음엔 전부 같은 날이라 7일마다 20곳이
한꺼번에 재스캔해 그날이 7.8초로 튀었다.

거부 캐시는 `vil._siteRejS`(strict) / `_siteRejL`(loose) 두 개다 — `farmAt` 이 달라 판정이 다르다.

### 실측 (같은 DB 스냅샷 · 9일 · `test-site-memo` 14/0)
```
                  집터 호출   스캔한 셀     life:site 중앙   1마을:site 중앙
  종전(MEMO=0)      177      1,227,510      1,209ms          205ms
  채택(기억 ON)      54        376,710          **6ms**        **2ms**
  단조성 감사       건너뛴 셀 **35,819회 재판정 · 위반 0**
```

### 남은 바닥 (회부 `회부_T41_조각바닥_다음층.md` §C~§E)
```
  캐러밴 콜드 A*      1,265~1,695ms  ← ★새 바닥. 재개 가능 A* 는 `sim/path-core.js`(랩 공유 정본)
                                       수술 + 캐러밴 스폰 지연(행동 변경) ⇒ **회부**
  집터 첫날 캐시 빌드   ~1,050ms      부팅 1회(그 뒤 2ms)
  광맥 콜드 스캔         755ms        부팅 1회 · 마을당 1회 ⇒ **회부**(영속 캐시가 답)
  생활층 헤드리스 결산    226ms/마을   ★**매일** — ①을 넣은 뒤 이게 다음 후보다
  곳간 증설               70ms/마을   ★매일
```
⇒ 목표(조각 최대 ≤ 예산 16ms × 2)를 **못 채웠다.** `e2e-rtt` 의 `≤×2` 는 pass/fail 밖에 그대로 뒀다.

### 손잡이
`LIFE_SITE_MEMO`(1 · **0 = 종전 동작 = 되돌리는 스위치이자 대조군**) ·
`LIFE_SITE_RESCAN_DAYS`(7) · `LIFE_SITE_AUDIT`(0 · 하네스 전용 · **느리다**) ·
`LIFE_SITE_NODIRTY`(0 · 안전망 픽스처 전용)

### 하네스
`scripts/test-site-memo.js` **14/0** — 팔 넷(종전·채택·감사·픽스처).
★**비용은 감사 없는 팔로, 정확성은 감사 팔로** 잰다: 감사는 건너뛴 셀마다 `reject` 를 다시 부르므로
그 팔로 비용을 재면 "1/5 로 못 줄었다"는 **가짜 실패**가 난다(첫 판에서 실제로 그랬다).

---

## T1. ★2026-09-01 — **일틱 조각내기**(RTT 스파이크) · 실측이 전제를 뒤집었다

재민 확정: 게임일 경계의 마을 시뮬 일틱이 이벤트 루프를 통째로 막아 RTT 가 튄다(실기 40~800ms).
**econ 함수는 한 줄도 안 바꾸고**, 호출을 여러 프레임에 나눈다.

### ★★전제 정정 둘 — 다음 세션이 먼저 읽어야 할 대목
1. **일틱 구동부는 `server/zone.js` 가 아니다.** `zone.js:8117` 은 한 줄 래퍼일 뿐이고,
   실제 하루 블록은 **`server/villages.js` `onGameTick()`** 안에 있다. 지시서의 파일 범위가 어긋나 있었다.
2. **범인은 econ 이 아니었다.** 단계별 실측(마을 50곳 · econ day 515 · 100일 중앙값):
   ```
   일틱 총 2,018ms
     life (생활층)  1,620ms  80%   ← ★  그 중 `_lifeAddHouseSite` 하나가 1,127ms(56%)
     terr             177ms
     econ(tickWorldV2) 99ms   5%   ← 지시서가 범인으로 지목했던 그것
     caravan           17ms (p95 989ms · 최대 5,046ms — 콜드 A*)
   ```
   ⇒ **econ 을 건드릴 값이 없었다.** 지시 §2-③이 열어 둔 좁은 예외(함수 추출)조차 안 썼다.
   (`sim/` 무수정 · 번들 8/8 · 3시드 비트 동일.)

### 무엇이 생겼나
| | |
|---|---|
| 슬라이서 | 하루 경계에서 그날 할 일을 **조각 목록**으로 세우고(`_openDayJobs`), 프레임마다 예산만큼 꺼내 돈다(`_drainTickJobs`). 조각 경계는 **단계 경계 또는 마을 경계**뿐 |
| 원자 조각 | econ(`tickWorldV2`+전쟁) · 캐러밴 **쓸기** · 방송 — 마을 간 결합이라 쪼개지 않는다 |
| 캐러밴 | `syncCaravanBodies` 를 `_caravanSyncOne`(한 대) + `_caravanSyncSweep`(쓸기) 로 갈랐다. **겉함수는 그대로** 둘을 순서대로 부른다(기존 호출부 무변) |
| "장부 마감 중" 큐 | 마감 창(실측 2.4~3.0초) 안에 들어온 **마을 요청만** 모았다 마감 직후 같은 순서로 흘린다. 목록은 `zone.js` `VILLAGE_BOOK_MSG`. **버리지 않는다** · 낚시·이동·전투·제작은 안 걸린다 |
| 실체 정지 | 마감 중에는 `tickCaravanBodies`·`tickWarBodies` 를 **멈춘다**(아래 ★★). 저장 배수·곳간/장마당 방송은 계속 |
| 벽시계 동결 | `_dayNow()` — 마감 중이면 **경계의 순간**. 생활층 날 파생과 zone.js 광맥 재생 적분이 이걸 읽는다 |
| 손잡이 | `VILLAGE_TICK_SLICE_MS`(기본 16 · **0 = 양보 끈 뽑기 = 종전 동작 = 두 하네스의 대조군**) |
| `/perf` | `sliceMs` · `loop{p50,p95,p99,max}`(`monitorEventLoopDelay` · `?reset=1` 영점) · `econTick{last{frames,frameMax,chunks,maxChunk,maxChunkAt,order,queued},stages}` |
| 마크 이름 | 종전 `econ_day` 는 "한 프레임"이자 "하루"였다. 갈라져서 **`econ_frame`(한 프레임 몫)** 을 새로 뒀다. `econ_day` 는 이제 **마감 시점의 하루 총합**(+`wall`) — `scripts/rtt-metrics.js` 가 그대로 읽는다 |

### ★★쪼갠 게 세계를 갈랐다 — 실측으로 잡아 막았다(다음 세션이 꼭 읽을 것)
하네스 ⑧이 **두 번 거짓말을 했고 두 번 다 고쳤다**. 상세는 회부 §A-4. 요약:
1. **가짜 발산**(49/50) — 저장이 1마을/틱이라 팔마다 **다른 날**의 행을 비교하고 있었다.
   ⇒ 같은 마을 **같은 날**끼리만 대조 + 끄기 전 저장 큐 배수.
2. **진짜 발산**(21/50) — 마감 2~3초 동안 **캐러밴·전투 실체가 계속 걸어서** 뒤 단계가 '더 간 몸'을 봤다.
   ⇒ **마감 중 실체 30Hz 정지**(`onGameTick` 첫머리). 하루 경계는 세계에게 한 순간이어야 한다.
   같은 이유로 하루 안 벽시계 파생도 얼렸다: `_dayNow()`(생활층 날) · `SimVillages.dayNow()`(광맥 재생 적분).
   ⇒ 그 뒤 **A/B 다름 0**.
★A/A 대조군이 없었으면 ①을 진짜로 보고했을 것이고, ②는 A/A 가 50/50 이라서 비로소 보였다.

⚠**그래도 완전히 같지는 않다**(회부 §A-5 — 재민 판정거리):
```
  A/A(같은 일정 두 판)     인구 차 0 · 식량 차 0.00~0.05%     ← 잡음 바닥
  A/B(끈 뽑음 ↔ 조각내기)  인구 차 0~0.10% · 식량 차 0.62~0.76%  ← 6일 누적(≈0.1%/일)
```
원인은 **NPC 가 매 프레임 일하고 그 손이 곳간을 만진다**(`npcLifeTick`)는 것이다. 하루의 단계를
2~3초에 흘리면 그 사이 NPC 노동이 단계 사이로 끼어든다. 없애려면 마감 동안 **생활층까지** 멈춰야
하는데 그건 행동 변경이라 이 배치가 못 한다. ⇒ `test-tick-slicer ⑧` 은 "0"이 아니라 **크기**로
판정한다(인구 ≤0.5% · 식량 ≤2%).

### 실측 (같은 DB 스냅샷 짝 비교)
```
  대조군(끈 뽑음)  총 2,977ms · 조각 203 · **프레임 1** · 한 프레임 최대 **2,977ms**
  조각내기(16ms)   총 3,023ms · 조각 196 · **프레임 30** · 한 프레임 최대 **489ms**
  이벤트 루프 최대 막힘  13,631ms → **1,334ms**   ★판정은 p99 가 아니라 **최댓값**으로 한다
    (두 팔은 막힘 **횟수**가 다르다 — 대조군은 하루 한 번 크게, 조각내기는 서른 번 작게.
     실제로 대조군 p99 가 24ms 로 나온 판이 있다. 같은 판의 최대는 12,994ms 였다.)
  RTT 마감 창 p95  9,899ms → **728ms** (×13.6) · 하루 총 일감 ×1.02(줄지 않았다)
  econ 상태        같은 마을·같은 날 50곳 대조 — **A/B 다름 0**
  단계 순서        econ→pop→terr→ore→wx→bcast→caravan→events→save→life→dist  (양쪽 동일)
```
⚠**마감이 하루보다 길면 econ 날이 밀린다**(마감 중엔 새 날을 안 연다). 실기는 하루 24분이라
3초 마감은 0.2% 지만, `VILLAGE_DAY_MS` 로 하루를 짧게 세우는 하네스는 이 사실을 알고 써야 한다.

### ⚠**목표 미달** — 다음 세션이 이어받을 것
재민 확정 목표는 **≤×2** 였고, 실기 환산 **×20.5** 에서 멈췄다. 바닥은 **조각 하나**다:
마을 한 곳의 생활층 227ms(그 중 `_lifeAddHouseSite` 185ms) · 캐러밴 한 대의 콜드 A* 436ms.
**슬라이서는 조각보다 잘게 못 자른다** — 그 안의 수술은 `회부_T1_일틱쪼개기_다음층.md` §A.
★그리고 일틱은 **세계가 늙을수록 자란다**(day 100 1,285ms → day 515 2,018ms). 급한 대목이다.

### 하네스
* `scripts/test-tick-slicer.js` **21/0** — 구조(조각 수·프레임 수·**순서 불변**·마감 완주·루프 p99).
  **①이 상황 선행 assert** 다: 대조군이 정말 한 프레임에 모는지 먼저 확인한다(자명 통과 방지선).
* `scripts/e2e-rtt.js` **5/0** — 진짜 WS 짝 비교(100ms 마다 500표본). ①상황 선행(대조군 ×5 이상)
  ②창 안 p95·최악 왕복 ≤ 대조군 절반 ③루프 **최대 막힘** ≤ 대조군 1/3 — **이것만 실패로 센다**.
  ⚠②의 문턱이 1/2 인 이유: 대조군 막힘은 하루 한 번이라 판마다 2,419~12,928ms 로 흔들린다. 목표선 ≤×2 는 **수치와 미달 사실을 찍되 pass/fail 에 안 넣었다** —
  그 판단의 근거와 되돌리는 법은 회부 §B-1(재민 확인 대기).
* 둘 다 `/tmp/slicer-seed-*.db` 스냅샷을 복사해 쓴다(첫 실행이 만든다 · 세계가 같아야 ms 비교가 성립).

---

## 3-e. ★2026-08-03f 배치 13 — **게스트 영속 신원**: 소유가 접속을 넘어 산다

재민 확정: **"네 추천대로 하자"** — 배치 12 회부 2 종결(`보고_게스트_영속신원.md`).

**문제**: 소유 판정이 전부 playerId 대조인데(사유지 `ownerPid`·건물 `ownerId`·노/숯가마/회관
`data.owner`·마을 `founder`) 게스트 playerId 는 접속마다 새로 발급되는 1회용이었다. ⇒ 잠깐
끊겼다 붙으면 **제가 세운 마을의 주인이 아니게 됐다.**

**해결**: 불투명 토큰 하나. **계정 체계 아님**(비밀번호·이메일 없음), **`anon_` 접두사 유지**
(코드 전역 28곳이 그걸로 "등록 계정 아님"을 판정 — 그 정책은 무변). 바뀐 건 **접미사가 접속을
넘어 같아진다**는 것뿐.

| | |
|---|---|
| 저장 | central `players.guest_token`(신규 칼럼 + 부분 유니크 인덱스). 기존 마이그레이션 관용 그대로. **새 테이블 0** |
| 발급·대조 | `POST /guest {token?}` → `{player_id, token, isNew}` |
| 전달 | 클라 `localStorage['durango_guest_token']` → ws 쿼리 `guest_token` → welcome 으로 회신 |
| 부수 수리 | `welcome.playerId` 신설 — 클라가 제 영속 신원을 알게 됐다(종전엔 사유지 목록이 `myUsername` 대조라 게스트는 제 사유지도 남의 것으로 보였다) |
| **보안** | 토큰 = 암호학적 난수 32B · **로그·알림·채팅·화면 어디에도 안 찍는다**(하네스가 서버 로그 전문을 값으로 검사) · 비밀번호 생긴 행은 게스트 토큰으로 못 연다 |
| 폴백 | central 불가 시 종전 1회용 신원으로 떨어진다 — 입장을 막지 않는다. 배포 순서 제약 0 |

**검증**: `test-guest-identity` 39/0(프로토콜 층 실서버) · `e2e-guest-reconnect` 26/0(실클라 —
브라우저 컨텍스트를 통째로 닫고 localStorage 만 들고 재입장) · `e2e-metallurgy` 20/0 ·
`test-furnace` 85/0 · `test-village-found` 47/0 · wiring 실패 0 ·
v2 CLI 2,616·무기Q 651·소멸 0/30 · 실지도 3시드 800일 **`b12tr` = `b13` 바이트 완전 동일**.
★이 배치는 `sim/` 을 **한 파일도 안 건드렸다**(econ 무접촉이 정상).

### ★재민 결정 대기 — 라이브 반영
이 변경은 무언가를 고아로 만들지 **않는다**. 라이브의 `anon_*` 소유 자산은 그 세션이 끊긴
순간 **이미 고아**였다(1회용 id). 결정할 건 "이미 있는 고아를 어떻게 하나" 하나다 —
(가)그냥 배포(고아 사유지가 영영 못 쓰는 땅으로 남는다) / **(나)라이브 DB 리셋과 함께(추천)** /
(다)고아 일소 스크립트 신설. 상세·근거는 보고서 §0. **실행은 재민.**

### 남은 것(구현 안 함)
게스트 인벤·좌표는 여전히 안 남는다(`savePlayer` 가 `anon_*` 를 건너뛴다 — 정책 변경이라 별도 판단) ·
게스트→등록 계정 playerId 승계(지시상 범위 밖) · 토큰 회수·만료 없음(복구 코드 = 계정 체계) ·
오래 안 온 게스트 행 prune 없음.

## 3-f. ★2026-08-03g 배치 14 — 게스트의 **몸과 이름**: 승계 + 인벤·좌표 영속

재민 확정 **"그래 그러자."** 계정 시스템 확장은 **플랫폼 결정까지 보류**
(스팀=티켓 어댑터 / 웹=OAuth 어댑터 — 어느 쪽이든 **playerId 발급 어댑터 하나**로 붙는 현 구조 유지).
플랫폼과 무관하게 공통으로 필요한 2건만 했다. 상세: `보고_게스트의_몸과_이름.md`.

### ① 승계 — 등록해도 playerId 가 안 바뀐다
종전엔 게스트가 계정을 만들면 `/auth` 가 **새 행**(`player_id = username`)을 만들어
사유지·건물·노·마을 `founder` 를 **한꺼번에 잃었다** — 배치 13 의 보람이 등록하는 순간 사라지는 역설.
⇒ `POST /promote`: 게스트 토큰으로 그 행을 찾아 **이름·비밀번호만 얹는다**(행 교체 금지).
`guest_token` 은 동시에 NULL — 배치 13 의 "비밀번호 생긴 행은 못 연다" 게이트와 **벨트와 멜빵**.
클라도 `welcome.promoted` 를 보고 죽은 토큰을 지운다.

★**거절 사유를 둘로 가른 것이 안전핀**이다: `username_taken`(남의 계정 — 막는다) vs
`not_promotable`(토큰 무효·이미 계정·**내 기존 계정** — 막지 말고 평소 로그인으로 흘린다).
후자가 없으면 옛 토큰이 남은 브라우저에서 **제 계정에 못 들어가는** 사고가 난다.

★딸려 온 것: 이제 **`player_id ≠ name` 인 계정**이 처음 생긴다 ⇒ `/auth` 로그인 조회와
`/check_username` 중복 검사를 정본 술어 `findAccount`(player_id **또는** name, 비밀번호 있는 행)로
바꾸고, 계정 이름에 **부분 유니크 인덱스**를 걸었다(조회 + DB 제약 둘 다).
그리고 **이름이 바뀌는 첫 사례**이므로 배치 13 의 "소유 판정에 이름 쓰는 곳 0곳" 전수를 재실행 — 여전히 0곳.

### ② 몸의 영속 — 신원만 남고 몸이 초기화되던 반쪽
`savePlayer` 의 `anon_` 가드가 정책이었는데, 배치 13 뒤로 그 접두사는 **영속 게스트**와
**1회용 폴백** 둘을 뭉뚱그린다. ⇒ 판정을 `canPersist(player)` **한 함수**로 모으고
`player.persistent` 를 본다. ★**호출부 사본 17곳을 걷어냈다** — 가드 한 줄만 풀었다면
대부분의 경로에서 게스트 저장이 **조용히 안 켜졌을 것**이다.
복원은 새 경로 0: `/guest` 가 행 전체를 주고 zone 은 **등록 계정과 같은 복원 블록**을 탄다
(접속 핸들러를 `신원 해석(분기) → 공통 복원(한 곳)` 으로 재구성). 핸드오프에도 `persistent` 를 실어
**존을 넘어도** 저장이 이어진다.

**검증**: `test-guest-identity` **62/0**(⑤승계·⑥몸 신설) · `e2e-guest-reconnect` **40/0**
(실클라 — 몸 기준선 → 컨텍스트 종료 → 인벤·좌표 그대로 → 승계 → 비밀번호 로그인 → 전부 내 것 →
구 토큰은 다른 사람) · `test-furnace` 85/0 · `test-village-found` 47/0 · wiring 0 ·
v2 CLI·3시드 800일 비트 동일(econ 무접촉).

### 남은 것
**토큰 회수·만료** — 플랫폼 결정 뒤 과제(복구 코드를 만들면 그게 곧 계정 체계다).
오래 안 온 게스트 행 prune 없음. 라이브 런북 §6 에 승계·영속 스모크 1줄 추가 — **실행은 재민.**

## 3-x. ★★2026-08-26 배치 — **정리 소배치: terrain 사망 원인 · 주기 저장 · e2e 시간 손잡이**

> 새 기능 없음. **회귀를 전부 녹색으로 만들고 저장 구멍을 닫는 것**이 전부다.
> 라이브 기본 동작은 한 줄도 안 바뀐다 — 이번에 생긴 손잡이는 전부 테스트에서만 켜진다.

### ① e2e-terrain 29/3 → **33/0** — 죽인 것은 늑대였다

앞 배치는 "하네스의 미확인 전제"까지만 밝혔다. 이번엔 **원인을 이름으로** 잡았다.
`damagePlayer` 의 사람 갈래에 사망 출처 로그를 넣고 재현했더니:

```
☠️ 여행자(p401) 사망 — by mob:wolf @ (…) hp0 배고픔 98 목마름 97
```

배고픔 98·목마름 97 이므로 아사도 낙하도 아니다. **늑대다.**
`ENABLE_WILDLIFE=0` 은 `wildlife.js` 만 껐고 **레거시 `mobs`(늑대·자칼 무리)는 안 껐다**.

**수리 — 플래그를 새로 만들지 않았다.** 두 개가 되면 다음 세션이 하나만 끄는 사고를 그대로
재생산한다. `ENABLE_WILDLIFE=0` 하나의 뜻을 **적대 개체 전부 OFF** 로 넓혔다.

**적용 범위(blast radius) — 게이트 4곳**:

| 자리 | 무엇이 막히나 |
|---|---|
| 시딩 블록 `while (HOSTILES_ON && aggressive…)` | 공격 개체 무리 **최초 스폰** |
| 리스폰 `if (HOSTILES_ON \|\| !ANIMALS[t].aggressive)` | 죽은 뒤 **재생성** |
| 사냥 리스폰(두 번째 자리) | 같은 규약 |
| 몹→플레이어 공격 `damagePlayer(t, …, 'mob:'+m.type)` | **피해 0** |

평화 개체(사슴·양)는 그대로다 — 적대가 아니다.
**이 값을 끄는 곳은 전부 하네스**다(운영 사용처 0): `e2e-terrain` · `test-tame` · `test-mining` ·
`test-furnace` · `pace-metallurgy` · `era-rehearsal` · (이번에 추가) `test-save-periodic`.
전부 "야생 없는 결정론"을 원해서 끄던 것이라 의미가 넓어지면 **더 조용해질 뿐** 깨지지 않는다.

**결과**: `e2e-terrain` **33/0**. 물 화소 4.7% → **11.2%** — 앞 배치가 "물 렌더 결함"으로 의심했던
수치는 **사망 모달이 화면을 덮어 어두워진 것**이었다. 생존 전제 assert 를 같이 넣어 두어서,
다음에 또 죽으면 화소 3건이 아니라 **"플레이어가 죽었다"** 한 줄이 먼저 말한다.

### ② 주기 저장 — 걷기만 한 진행이 크래시를 넘는다

`savePlayer` 는 접속·종료·핸드오프·행동 때만 돌았다. **걷기만 한 몇 분은 어디에도 없었다.**
(B-6 수리는 "두 세션이 겹치는" 갈래만 막았고 크래시 갈래는 그대로였다 — 3-y ①의 남은 것.)

`SAVE_INTERVAL_MS` **기본 30000**. 설계는 넷 다 의도적이다:
**틱당 최대 한 명**(스파이크 금지) · **사람마다 다음 저장 시각을 흩어 둠**(같은 순간에 안 몰리게) ·
**움직인 사람만**(`SAVE_MOVE_EPS=2px` — 인벤·행동은 원래 그 자리에서 저장한다) ·
**밀려난 세션 제외**(`_supersededBy` — 낡은 좌표가 나중에 도착해 덮어쓰는 것 차단, B-6 과 한 규약).

**비용 실측** — `scripts/cost-save.js`(zone 을 같은 프로세스에 띄워 `__testBind()._saveStats()` 정본을 읽는다):

```
  사람 8명 · 70초 · SAVE_INTERVAL_MS 6000(포화시킴)
  저장 84건 · 누적 22ms → **1회 0.262ms**
  60ms 표본 1155개 중 한 표본 최다 저장 **1건** ← 틱당 1건 상한이 실제로 지켜진다
  서버가 저장에 쓴 시간 ≈ 0.03%
```
★인원이 늘어도 이 값은 안 는다 — **틱당 1건이 구조적 상한**이라 인원이 늘면 한 사람당 주기가
길어질 뿐이다. 상한을 다 쓰는 최악에도 틱(50ms)의 **0.52%**.

**하네스 신규** `scripts/test-save-periodic.js` **20/0** — 자명 통과를 A/B 로 막았다:
ⓐ 간격을 999초로 두면 걷기만으로는 central 좌표가 **0px 움직인다**(픽스처 결백) ·
ⓑ 4초로 두면 같은 걷기가 **510px 따라온다** · ⓒ **소켓을 안 닫고 SIGKILL** 뒤 재기동 —
**9px 안**으로 복구 · ⓓ 승계 뒤의 주기 저장은 **새 몸**의 좌표를 쓴다.

### ③ e2e 시간 손잡이 — **데울 땐 흐르고, 상호작용 땐 얼린다**

`e2e-events` 가 계속 깨진 뿌리는 하나였다: **경제가 검사를 앞지른다**(0.5초/게임일이라 상호작용
몇십 초 사이에 60일이 지나 의뢰가 제 발로 철회되고 품목이 바뀐다).

여기서도 **새 env 플래그를 만들지 않았다** — 이미 있는 테스트 픽스처 게이트(`E2E_GIVE`) 위의
런타임 메시지로 냈다. `__e2e_day_freeze` → `villages.__e2eDayFreeze(on)`:
`state.dayFreeze` 가 서 있으면 하루 경계를 통째로 건너뛰고, **해동할 때 `lastGameDay` 를 지금으로
다시 앵커**한다(안 그러면 얼려 둔 날이 한꺼번에 쏟아진다).

쓰는 하네스: **`e2e-events` 하나**. 파일 머리에 시간 모드를 한 줄로 적어 뒀다
("데울 땐 흐르고, 상호작용 땐 얼린다"). 시간 자체가 검사 대상인 하네스는 **그대로 흐른다**.
앞 배치의 하루 0.5→1.2초 완화는 **되돌렸다**(얼리기가 그 자리를 대신하므로 데우기는 빠른 게 낫다).

### ④ 이 배치가 잡은 **하네스 결함 3건** (제품 결함 아님 — 그래서 더 위험했다)

**ⓐ `e2e-events` — 픽스처가 자기가 재려는 것을 망가뜨렸다.**
단독 실행은 31/0 인데 **다른 하네스 뒤에서만** 3~5건이 깨졌다. 알림이 원인을 그대로 말하고 있었다:
`📋 마을이 지금 갚을 것이 없다`. 게시판에 `돌 2 → 나무 9` 가 걸린 상태에서
`__e2eForceShortage` 가 **wood 를 55.6 → 1.83 으로 깎았다** — 마을이 약속한 보상을 픽스처가
없애 버린 것이다(게임일을 얼려 둔 구간이라 `revalidate` 의 자정도 안 돈다).
제품은 **옳게** 굴렀고(B-1 의 정당한 거절 + 원자적 되돌림) 하네스만 빨개졌다.
· 수리 1(서버 픽스처): **열린 의뢰가 약속한 보상 품목은 후보에서 뺀다.**
· 수리 2(서버 픽스처): 재고를 `문턱×0.1`(사실상 0)이 아니라 **`문턱×0.9`** 로 내린다 —
  바닥까지 비우면 그 품목 시세가 폭발해 ⓐ·ⓑ 가 둘 다 못 갚고 **의뢰가 아예 안 걸린다**
  (실제로 `wood 0→1.55` 인 판에서 30회를 걸어도 게시판이 비었다). 부족의 **정의**는 그대로 지킨다.
· 수리 3(하네스): `ensureBoard` 가 **먼저 보고** 비었을 때만 픽스처를 부른다.
· 그리고 회귀를 **이름으로** 잡는 assert 한 줄을 추가했다 —
  `★마을이 약속한 보상을 갚을 수 있다`. 이게 없으면 아래 세 개(인벤 차감·보상 수령·잔여 감소)가
  같이 무너지면서 "왜" 를 말하지 않는다. → `e2e-events` **32/0**.

**ⓑ `e2e-nature` — 사슴 한 마리가 결정론 판정을 깼다.**
같은 코드로 한 판은 73/0, 다음 판은 72/1. **두 프레임을 눈으로 대조**했더니
두 번째 프레임에 **사슴이 걸어 들어와 있었다**(1007화소). 무풍 잡음 바닥 판정도 같은 이유로
202 > 153 이 났다. 이 판정들의 뜻은 *자연물 자리 해시가 순수 함수인가*이지 **짐승이 멈춰 있는가**가 아니다.
· 수리: 클라가 **자기 변환으로** 생물이 그려진 화면 자리를 내주고(`window.__entBoxes()`,
  `__terrain19.entBoxes` 로 켜는 읽기 전용 훅 · 기본 꺼짐 · 라이브 비용 0),
  하네스는 **그 자리만 가리고** 견준다. 하네스가 화면 변환을 베끼지 않는다(사본 계측기 금지).
· ★**반례 장치**를 같이 넣었다 — 사슴이 지나가 주기를 기다리지 않고, **생물 자리에 가짜 얼룩**을
  찍어 "안 가리면 3600px 잡히고, 가리면 0px" 을 매 판 증명한다. 가리기가 죽으면 그 줄이 먼저 깨진다.
· 가린 넓이(4.2%)를 **같이 보고**하고 25% 를 넘으면 실패시킨다 — 다 가려 놓고 0 이라 말하는 자명 통과 차단.
→ `e2e-nature` **76/0**(73 → 76, 늘어난 3건이 위 반례·전제·가림넓이).

**ⓒ 러너가 실패한 줄의 이름을 안 보여 줬다.** `72/1` 만 보이고 **무엇이** 실패했는지는
로그를 다시 돌려야 알 수 있었다(이번에 두 번 그랬다). 이제 `✗` 줄을 **먼저, 따로** 찍는다.

### 회귀 결과 (순차 단독 · `scripts/run-regress.sh` 경유 · `--selftest` 선통과)

`test-events` 71/0 · `test-guest-rejoin` 26/0 · **`test-save-periodic` 20/0(신규)** ·
`test-guest-identity` 62/0 · `e2e-events` **32/0** · `e2e-guest-reconnect` 40/0 ·
`e2e-mountain` 29/0 · `e2e-mtcorridor` 15/0 · `e2e-mtocc` 17/17 · **`e2e-terrain` 33/0** ·
`e2e-nature` **76/0** · `e2e-rooms` 55/0 · `e2e-cutaway` 15/0 · `e2e-metallurgy` 20/0 · `e2e-village` 32/0.

⚠**미해결 1건 — `e2e-village` 가 세 판 중 한 판에서 31/1**. 단독 재실행 32/0,
앞 하네스(`e2e-metallurgy`)를 앞세운 같은 순서 재현도 32/0 으로 **두 번 다 재현 실패**했다.
그 판 로그에 `central save 실패 (e2echon): socket hang up` 이 있었다(하네스 종료 순서상 central 이
먼저 죽는 자리). **원인 미상으로 남긴다 — 추정을 원인으로 쓰지 않는다.**
다음에 나면 러너가 이제 **✗ 줄을 찍으므로** 어느 판정인지 바로 나온다. 그 줄부터 보라.

### 3시드 800일 기준선 — **한 자리도 안 움직였다**

```
  시드 1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 2.21일/건 · 게시 1,561   (기록값과 동일)
  시드    7 : 인구 5,993 · 소멸 0/51 · 무기Q 755 · 2.22일/건 · 게시 1,487   (기록값과 동일)
  시드   42 : 인구 6,072 · 소멸 0/51 · 무기Q 784 · 2.22일/건 · 게시 1,535   (기록값과 동일)
```

★단 하나 달라진 수치는 **장부 하루 경계 비용**이다: 기록 2.5 ms/일 → 이번 4.2~4.7 ms/일.
**코드가 아니라 기계다.** 같은 순간에 같은 판(200일·시드 1020)을 **HEAD(`18da87c`) 워크트리**와
작업 트리에서 나란히 돌렸더니 **4.079 vs 4.438 ms/일**이고 인구·사건수·밀도·분포·게시 건수는
**전부 비트 동일**이었다. 즉 이 컨테이너가 그때보다 느리다.
⇒ 인계의 ms 수치는 **기계 의존**이다. 비교할 때는 **같은 순간의 A/B** 로만 비교해라.

---

## 3-멎. ★★2026-08-31 밤 — **존 멎음 원인 규명 + 타일 지형 메모** (기본 꺼짐)

> 전말은 **`보고_존_멎음_원인과_수리.md`**. 여기엔 다음 세션이 바로 쓸 것만 적는다.

### 결론

멎음 = **게임일 틱**이다. 야생·도적을 켜고 재도 **멎음 37회 : 게임일 틱 37회 · 42.0초 : 41.8초(99.5% 일치)**.
그 안의 범인은 `_lifeDaily → _lifeAddHouseSite`(신축 터 탐색)가 부르는 **캐시 없는 지형 통행 판정**.
게임일 1일당 지형 질의 **32,900~88,700회 · 756~2,471ms**, 1회 ≈ **50µs**.
교역로 A\* 는 **10.1%** 뿐이었다(후보 ②를 먼저 의심했는데 아니었다).

### 무엇을 넣었나 — 수리가 **둘**이다(겹치지 않고 포갠다)

**ⓐ 타일 지형 메모** — 같은 셀을 **다시** 묻는 비용을 없앤다. `TERRAIN_TILE_CACHE=1` (기본 꺼짐)

* `server/terrain-tilecache.js` (신규) — 타일당 4비트 `Uint8Array`, 8.5MB. 머리말에 근거·등가성 전부.
* `server/zone.js` — `isWaterTileLocal` / `isRockTileLocal` 만 캐시를 문다.
* `scripts/test-terraincache.js` (신규, **21/0**).

**ⓑ 선분 격자 색인** — **처음** 묻는 비용을 없앤다. `TERRAIN_SEG_INDEX=1` (기본 꺼짐)

* ⓐ 만 켰더니 **부팅 25초와 틱 최대 5.9초가 남았다.** 둘의 공통점은 **전부 최초 질의** —
  캐시가 원리상 못 돕는 자리다. 프로파일이 그 자리를 짚었다:
  `SimVillages.init` 24.6초 중 **`computeAndInjectDistMatrix` 24.2초**, 그 밑은 전부 지형 술어.
* `server/terrain-segindex.js` (신규) — 선분을 512px 격자에 적재. 확장 폭 = 구간 halfWidth 의 **상한**
  `max(w1,w2)/2` ⇒ 후보 목록이 '참일 수 있는 모든 선분'을 포함한다(등가성의 근거).
* `server/terrain.js` — `_isPointInRiver` 의 bbox 통과 **뒤** 주사를 후보만 보도록. 거리식은 **한 곳뿐**.
* `scripts/test-segindex.js` (신규, **14/0**) — 자식 프로세스 둘(켬/끔)이 진짜 제품 경로로 답을 내고
  부모가 **비트 단위**로 맞춘다. 표본 51,474점, 강·산맥 **경계에 몰아서**, 셀 중심 아닌 점이 과반.

`scripts/run-regress.sh` — 둘 다 목록에 추가.

### ★건드리면 안 되는 것

**`terrain.js` 안쪽(`isWaterCellLocal`/`isRockCellLocal`)에 캐시를 걸지 마라.**
`chunk.js:447` 이 `x±D` 오프셋 점을, `fishing.js` 가 임의 점을 묻는다 — 거기서 타일 양자화하면 **답이 바뀐다**.
캐시가 붙을 수 있는 이유는 오직 `zone.js` 의 두 술어가 **이미 (tx,ty) 로 양자화한 뒤 중심 한 점만 묻기** 때문이다.
하네스 ④가 이 조건을 글자 단위로 지킨다(두 가지의 terrain 호출 인자식이 같은지 비교).

### 효과 (같은 DB 스냅샷 · 야생·도적 켬 · 플래그만 다름)

| | 기동~서버 열림 | 게임일 틱 중앙 | 틱 최대 | 정상 멎음(부팅 제외) |
|---|---|---|---|---|
| 둘 다 끔(현행) | 52.2초 | 879ms | 6,534ms | 23회 · 25.8초 |
| ⓑ 색인만 | 13.1초 | 204ms | 1,088ms | 1회 · 1.1초 |
| **둘 다** | **12.1초** | **160ms** | **935ms** | **1회 · 0.9초** |

★**부팅(4.3배)과 틱 최대값(꼬리)을 죽인 건 ⓑ 다.** ⓐ 는 그 위에서 중앙값을 한 번 더 깎는다.
ⓐ 만 켰을 때: 멎음 37→4회 · 42.0→6.9초 · 틱 중앙 966→171ms · 벽시계 멎음 비율 45.1%→0.9%.

### ★등가성 사슬 — 셋 다 닫았다

```
변경 전  ==  변경 후(꺼짐)     51,474점 불일치 0   ← git show HEAD 판을 그 자리에서 돌려 대조
변경 후(꺼짐) == 변경 후(켬)   51,474점 비트 동일  ← test-segindex ①
타일 메모 켬 == 원본           10,500회 전부 일치  ← test-terraincache ①
```
★**켬/끔 비교만으로는 부족하다.** 반복문 모양을 바꿨으니 **끔이 원본과 같은지**를 따로 재야 사슬이 닫힌다.

### 전체 회귀 기준선 — **46 통과 / 1 실패** (기본값 = 캐시 꺼짐, 순차 단독)

```
통과 46종: test-events · test-body · test-fishing · test-guest-rejoin · test-save-periodic ·
  test-guest-identity · test-trade · test-weight · test-emptystart · test-craft · test-ledger ·
  test-preserve · test-crops · test-calendar · ★test-terraincache(21/0 신규) · test-move · test-charsheet ·
  e2e-events · e2e-trade · e2e-weight · e2e-emptystart · e2e-forage-village · e2e-craft · e2e-preserve ·
  e2e-inv · e2e-conn · e2e-ui · e2e-cold(30/0) · e2e-fishing · e2e-guest-reconnect · e2e-mountain ·
  e2e-mtcorridor · e2e-mtocc · e2e-terrain · e2e-nature · e2e-rooms · e2e-cutaway · e2e-metallurgy ·
  e2e-village · e2e-mtcut · e2e-mtfuzz · e2e-tilestate · e2e-waterperf(17/0) · e2e-move(37/0) ·
  e2e-charsprite(34/0) · e2e-zoom(28/0)
실패 1종: e2e-mtfoot 8/9 — ⑤ 기존 이월 건(회부_기슭_평탄판정.md 에 원인 규명)
```

### 전체 회귀 2차 — **둘 다 켬**(`TERRAIN_SEG_INDEX=1 TERRAIN_TILE_CACHE=1`) · 45 통과 / 3 실패

세 실패를 하나씩 끝까지 봤다. **제품 회귀는 0건이다.**

1. **`test-terraincache` ③** — ★**내 하네스가 스스로 흔들렸다.** 「2회차가 1회차보다 20배 싸다」의
   20배는 **밑에 깔린 술어가 얼마나 비싼가**에 딸린 수다. 같은 날 들어온 ⓑ 색인이 원본을 9.7배
   싸게 만들자 134배 → **13배**가 되어 이 판정만 깨졌다. 캐시는 멀쩡했다.
   ⇒ **문턱을 낮추는 대신 재는 법을 고쳤다**: 벤치는 **3회 중 최소**(부하 스파이크는 위로만 튄다),
   대조군도 **양쪽 다** 최소로(한쪽만 최소면 대조 비가 1 이 아니라 3~5 로 부풀어 제 문턱을 밀어 올린다).
   결과 대조 비가 0.98~1.21 로 제자리를 찾았고 이득은 70~640배. **켬/끔 양쪽에서 21/0 · 4회 확인.**
2. **`e2e-mtfoot` ⑤** — 기존 이월 건. ★**수치가 0.34 / 0.45 로 꺼짐 판과 한 자리도 안 다르다.**
   플래그가 지형 답을 안 바꾼다는 **독립적인 end-to-end 확증**이다(장면이 비트 동일해야 나오는 수다).
3. **`e2e-charsprite` ⑤** — 러너 끝에서만 「남이 0명」. **단독 재실행은 플래그 켠 채로 34/0.**
   ⇒ 회귀 아님. 다만 원인이 **증상 자리**에서 터지고 있었다: 위쪽 텔레포트 수렴 반복문이 20회 뒤
   **말없이** 빠져나올 수 있어서, B 가 엉뚱한 데 있어도 "A 가 남을 못 본다"로 빨개진다.
   ⇒ 족보 57 대로 **남을 보기 전에 남이 왔는지 먼저 세는 판정**을 넣었다(35/0 · 도착 거리 0px).
   다음에 같은 일이 나면 실패 줄이 **원인을 바로 말한다.**

★**직전 배치에서 "내 탓인가" 로 남겨 뒀던 둘이 이번에 깨끗이 통과했다.**
`e2e-cold` **30/0** · `e2e-waterperf` **17/0**(전판의 8.0배 vs 문턱 8 은 2코어 컨테이너 부하에서
난 경계값이었고, 단독 순차로 돌리니 1.16배 대조군까지 정상). ⇒ **회귀 아님이 확정.**

### 남은 것 (재민 판단 대기)

1. **실운영에 켤까?** (`TERRAIN_SEG_INDEX=1 TERRAIN_TILE_CACHE=1` · 기본값 변경 포함 — 회부 A)
2. **남은 부팅 12초** — 52.2초에서 여기까지 왔다. 더 줄이려면 **비동기 분할**이라 구조 변경이다. **미수리**(회부 B).
3. **실운영 33초 간격 확정** — 헬스체크가 `interval=30s timeout=3s` 라 실패 두 건은 **연속된 두 검사**다.
   한 번의 긴 멎음으로 읽는 게 자연스럽지만 **안 쟀으니 단정 안 했다.** 보고서 §4-나에 측정 스크립트가 있다.

### ★족보 (76): **비싼 술어는 캐시가 아니라 호출 횟수로 죽는다 — 시간과 횟수를 짝으로 세라.**

프로파일이 `_pointToSegmentDist` 를 가리켰을 때 **그 함수를 빠르게 만드는 길**로 갈 뻔했다.
실제 답은 함수가 아니라 **반복**이었다(같은 셀 재질문 27~71%). 자기 시간 상위만 보면
"무엇이 느린가"는 알아도 **"왜 그렇게 자주 불리는가"** 는 안 보인다.
⇒ 프로파일러(시간)와 계수기(횟수)를 **같이** 걸어라. 이번엔 계수기가 답을 줬다.

### ★족보 (77): **재질문을 없앴는데도 안 빨라지는 자리가 있으면, 거기가 '처음'이다.**

ⓐ 메모를 켜고도 부팅 25초·틱 최대 5.9초가 남았다. 두 자리의 공통점은 **전부 최초 질의**였다.
캐시는 최초 질의를 **원리상** 못 돕는다 — 남은 게 있으면 그건 "더 캐시하라"가 아니라
**1회 비용 자체를 줄이라**는 신호다(⇒ ⓑ 선분 색인, 부팅 4.3배).

### ★족보 (78): **플래그가 모듈 적재 때 읽히면 한 프로세스로 A/B 를 못 한다 — 자식을 둘 띄워라.**

`test-segindex` 는 자식 프로세스 둘이 **진짜 제품 경로**로 답을 내게 하고 부모가 비트로 맞춘다.
같은 프로세스에서 색인을 흉내 내면 **내 사본을 검사**하게 된다(사본 금지).
그리고 **켬 vs 끔만으로는 부족하다** — 반복문 모양을 바꿨다면 **끔이 원본과 같은지**를
따로 재야 사슬이 닫힌다(`git show HEAD:` 판을 그 자리에서 돌려 대조).

### 하네스·나 자신이 두 번 먼저 틀렸다

* **`$!` 를 node 의 PID 로 믿었다.** `nohup node … &` 를 툴 셸에서 돌리면 `$!` 는 **래퍼 셸**이다.
  엉뚱한 PID 에 `kill -USR2` 를 쏴서 프로파일 저장이 조용히 실패했고, 진짜 존은 살아남아
  다음 판이 `EADDRINUSE` 로 죽었다. ⇒ **PID 는 `pgrep -af` 로 확인하고 쓴다**(족보 57 의 재판).
* **`pkill -f "zone-count.js"` 가 자기 래퍼 셸을 죽였다.** 명령줄에 그 문자열이 들어 있기 때문.
  `run-regress.sh` 가 이미 쓰는 대괄호 관용구(`"zone-coun[t].js"`)가 정답이다.

---

## 3-s. ⚠2026-08-31 실기 관측 — **라이브 존이 몇 초씩 멎는다** (★같은 날 밤 원인 규명·수리 → 3-멎 참조)

> 배포 확인 중에 `durango-zone-hanbando` 가 계속 `unhealthy` 로 떠서 파고든 것이다.
> **고친 게 아니라 관측만 했다.** 다음 세션이 짐작으로 손대지 말라고 근거를 남긴다.
>
> ★**[2026-08-31 밤 추록]** 아래 후보 셋 중 **①이 맞았다.** 다만 ① 안에서 지목한 자리가
> 예상과 달랐다 — 마을 시뮬 틱의 **생활층 신축 터 탐색**이 **캐시 없는 지형 통행 판정**을
> 게임일마다 3만~9만 번 부르고 있었다. ②교역로 A\* 는 10%뿐, ③부패/작물은 상위 40위 밖.
> 전말·계수·수리·남은 물음은 **`보고_존_멎음_원인과_수리.md`**. 아래 절은 관측 당시 기록 그대로 둔다.

### 무엇을 봤나

`docker inspect --format '{{json .State.Health}}'` 의 실제 기록:

```
  15:58:37  0.11s ✓     (정상 응답은 0.1~0.2s)
  15:59:07  0.11s ✓
  15:59:37  0.19s ✓
  16:00:08  Health check exceeded timeout (3s) ✗
  16:00:41  Health check exceeded timeout (3s) ✗
```

부팅 로그에도 같은 종류가 있다: `교역로 A*: 광산1↔광산6 603정점 **11610ms**`,
`광산1↔어촌1 291정점 4099ms`. **동기 계산이 이벤트 루프를 수 초간 막는다.**

당시 존 부하: `players 1569`(주민 NPC) · `mobs 449` · `buildings 3` · 존 1개 단독.

### 무엇이 **아닌지**부터 (오진 방지)

* **헬스체크 설정 문제가 아니다.** `wget` 있고 `${PORT}` 도 런타임에 제대로 풀린다 —
  성공 기록이 0.11s 로 섞여 있는 게 그 증거다. `Dockerfile.zone` 은 `b8621a8` 이후 무변경.
* **줌·캐릭터 배치 것이 아니다.** 둘 다 클라 전용이고 서버를 안 건드렸다.
  그리고 `CHAR_SPRITE=on` 을 넣기 **전** 배포에서도 이미 `unhealthy` 였다.
* ⚠**단정하지 않은 것**: 오늘 `server/zone.js` 에 부패 곡선·작물 34종이 들어갔다.
  틱당 일이 늘었을 **가능성은 있다.** 재 보기 전엔 모른다 — 그게 이 절의 요점이다.

### 원인 후보 셋 (프로파일 먼저 · 짐작 수리 금지)

1. 마을 시뮬 틱(51마을 · 주민 1569) 2. 교역로 A\* 재계산 3. 부패/작물 신규 틱

### 다음 세션이 먼저 할 일 — 크기부터 잰다

```bash
for i in $(seq 1 120); do s=$(date +%s%N); curl -sf -m10 -o /dev/null http://localhost:3020/health; \
  e=$(date +%s%N); echo $(( (e-s)/1000000 )); sleep 0.5; done \
 | sort -n | awk '{a[NR]=$1} END{print "n="NR" 중앙값="a[int(NR/2)]"ms p90="a[int(NR*0.9)]"ms 최대="a[NR]"ms"}'
```
그 다음 어느 층이 막는지 — `--cpu-prof` 나 틱별 벽시계 계측기. **범인을 고른 뒤에 고쳐라.**

★영향: `unless-stopped` 는 unhealthy 로 재시작하지 않으니 접속은 된다. 다만 멎는 동안
서버가 틱을 안 돌리므로 **플레이어에겐 고무줄·RTT 급등으로 보인다.** 재민 실기 체감이 실제 크기다.

---


## Z-소속. ★[T11 2026-09-02] `zone.js` 접점 — 마을 소속·곳간 인출

정본은 `server/membership.js` 이고, 규약은 `인계/S-사회스킬.md` **S-소속** 절에 있다(여기 복제하지 않는다).
`zone.js` 가 하는 일은 **접점 등록**뿐이다 — 판정은 한 줄도 여기 없다.

| 자리 | 하는 일 |
|---|---|
| `require('./membership')` | 모듈 하나 |
| `Membership.init({ SimVillages, ZONE_ID, send, players, gameDay, afterWithdraw })` | `Onboarding.init` 바로 뒤. **이미 있는 것만 넘긴다** — 기여 계량기는 안 넘긴다(membership 이 온보딩 정본을 직접 읽는다) |
| `serializeBody` / `parseBody` | `member` 필드 한 줄씩 (+ 옛 도구 형식 오독 방지 제외 목록에 `'member'`) |
| 접속 복원 3자리 | `_loadMember` 버퍼 · 핸드오프 · 로그인 · `_takeover.member`(살아 있던 몸이 진실 — **보강**이지 덮어쓰기 아님) |
| `tryVillageDeliver` | `Onboarding.onDeliver` **다음** 한 줄 `Membership.onDeliver` — 기여가 오른 뒤라야 문턱을 정확히 본다 |
| `tryVillageBrief` | `player._memberNearVid = vid` (근접 브리핑이 곧 "여기 있다"는 신호 — 채팅 `/인출` 이 이걸 본다) + `Membership.orderBrief(player, r)` (소속 마을 사건 앞줄 · **가시성은 안 바꾼다**) |
| `tryVillageTrade` | `r.member = _memberLine(...)` — 거래소 payload 에 한 줄 얹는다(새 창구 0) |
| `chat` 분기 첫 줄 | `if (Membership.handleChat(player, text)) return;` — 명령은 말이 아니므로 **방송하지 않는다** |
| `tryVillageWithdraw` / `_afterWithdraw` | 인출 뒤 `Lots.reconcile` → `Carry.reconcile` → `savePlayer` → `sendInventory` → 알림 → 거래소 창 갱신 |
| `VILLAGE_BOOK_MSG` | `'village_withdraw'` 추가 — 인출도 마을 장부라 일틱 조각 창에서 **미뤄졌다 흘러야** 한다 |

⚠**새 존 로컬 테이블을 안 만들었다.** 소속은 몸에 실린다 — 존 로컬 표에 두면 존을 넘는 순간
없던 일이 된다(T47 이 도구로 겪은 그 결함의 소속판). `zone-local-db.js` 는 **무접촉**이다.

⚠`_afterWithdraw` 는 `_memberNearVid` 가 있을 때만 거래소 창을 다시 보낸다 — 창이 안 열려 있으면
보낼 데가 없다. 화면이 낡은 남은 몫을 들고 있으면 그게 거짓말이라, 거래소 실행과 **같은 규약**이다.

## Z-사유지. ★[T45 2026-09-02] `zone.js` 접점 — 사유지 v2

정본은 `server/claims.js` 이고 규약은 `인계/S-사회스킬.md` **S-사유지** 절에 있다(복제하지 않는다).

| 자리 | 하는 일 |
|---|---|
| `require('./claims')` | 모듈 하나 |
| `Claims.init({ claims, buildings, players, db, central, broadcast, ZONE_ID, BUILDING_SIZE })` · `Claims.start()` | `Membership.init` 바로 뒤. ⚠**`gameDay` 를 일부러 안 넘긴다**(부재 시계는 실시간) |
| **부팅 로드부**(`db.getClaims()` 순회) | ★**여기가 §0 의 결함이었다** — `kind`·`guildTribeId`·`state`·`heldBy`·`stateAt` 을 채운다 |
| `tryClaim` | ① 승계(`pref`/`free` 이면 이어받는다) ② 인접(4방) — 둘 다 겹침 검사 **앞**에 · `insertClaim` 에 `kind`·`guild_tribe_id` 를 실제로 넘긴다 |
| `tryUnclaim` | 연결성 — `Claims.unclaimSplits` 하나. `db.deleteClaim` 로 바꿨다(raw SQL 제거) |
| `listRespawnOptions` | ★`c.ownerId` → `c.ownerPid` · `free` 는 제외 |
| 접속(`players.set` 직후) | `Claims.onPlayerActive(player)` — 맡겨 둔 땅 반환 |
| `tryChestTake` | `Claims.chestLocked` 한 줄 — **보관 중 무주** 사유지의 상자만 잠근다 |
| `GET /claimdbg` | 읽기 전용 관측창. `?scan=1` 이면 부재 배치를 지금 한 번 돈다 |
| `__testBind()` | `Claims`·`db`·`tryClaim`·`tryUnclaim`·`countMyClaims`·`listRespawnOptions`·`CLAIM_*` 를 정본 그대로 |

⚠**`insertClaim` 이 종류를 안 쓰던 것이 이 카드가 고친 근본**이다. 새 필드를 클레임에 더할 자리는
**`zone-local-db.js` 의 `insertClaim`/`updateClaim*` 과 부팅 로드부 둘**이다 — 한쪽만 고치면
"메모리에는 있는데 재시작하면 사라지는" 필드가 또 생긴다.

⚠**부재 배치는 존 틱에 안 얹혀 있다**(30분마다 · `CLAIM_SCAN_MS`). central 에 **부재 중인 소유주 수만큼**
`GET /player/:id` 를 묻는다 — 접속 중인 사람은 묻지 않고, central 이 안 뜨면 **그 판을 통째로 건너뛴다**
(모르면 안 건드린다 — 안전한 쪽).

## Z-이방인. ★[T19 2026-09-02] `zone.js` 접점 — 유저 마을 시작지 등록

규약은 `인계/S-사회스킬.md` **S-이방인** 절에 있다(복제하지 않는다).

| 자리 | 하는 일 |
|---|---|
| `require('./newcomers')` | 모듈 하나 |
| `Newcomers.init({...})` · `start()` | `Claims.init` 뒤. ⚠`holdDays: Claims.CFG.HOLD_DAYS` — **새 수를 만들지 않는다** |
| `SimVillages.init` 의 `onVillageAdded` | 마을이 하나 늘면 `Onboarding.noteVillage(dbId)` — 그 곳만 굽는다 |
| `Onboarding.init` 의 `newcomers(vid)` | 시작 화면이 유저 마을을 올릴지 — **판정은 `newcomers.js` 하나**가 하고 온보딩은 부르는 법만 안다 |
| `tryVillageInventory` | `inv.welcome = _welcomeLine(vid)` · `inv.hallId` — 기존 payload 두 줄(새 창구 0) |
| `tryVillageWelcome` | 스위치. 권한·거리는 재고 열람과 **같은 술어** · 자격은 **켤 때 안 막는다** |
| chat 분기 | `Newcomers.handleChat` — `/이방인` |
| `VILLAGE_BOOK_MSG` | `'village_welcome'` 추가 |
| `GET /welcomedbg` | 읽기 전용 관측창(`?scan=1` = 지금 한 번 스캔) |
| `__testBind()` | `Newcomers`·`tryVillageWelcome`·`tryVillageInventory`·`_welcomeLine` |

★★**§0 이 찾은 근본**: `foundPlayerVillage` 가 `state.clientPayload` 에 **자기를 안 넣었다**.
그 표는 시딩·복원 때 **한 번** 만들어지고 하루 틱은 기존 항목만 갱신한다 ⇒ 방금 선 마을은
`clientVillages()` 가 몰랐고, **재시작해야 세계에 나타났다.** 그래서 시작 화면·도착 지점·마을 이름·
쉼터 폴백(T43)·**클라의 촌장 근접 브리핑**이 전부 그 마을을 못 봤다.
⇒ **마을을 추가하는 자리는 `foundPlayerVillage` 하나다.** 새 필드를 마을 페이로드에 더할 땐
**시딩(Stage 4A)과 그 자리 둘 다** 고쳐라 — 한쪽만 고치면 "재시작해야 보이는 필드"가 또 생긴다.

## Z-쉼터. ★[T62 2026-09-03] 마을 안 이송 좌표가 **진짜 쉼터**가 됐다

`resolveDowned` 의 문장 *"마을 사람들이 당신을 쉼터로 옮겼다"* 는 T43 이래 **거짓이었다** —
옮겨 준 자리는 `nearestVillageWake` 가 낸 **도착 지점**이었다(쉼터가 세계에 없었으니 그럴 수밖에 없었다).
T62 가 시설을 만들었으므로 이제 참이다.

**사다리는 `nearestVillageWake` 한 곳에 있다: 쉼터 → 도착 지점 → 마을 중심.**
좌표를 지어내지 않고 **있는 것 중에서** 고르고, 물·막힘이면 종전 나선 탐색이 그대로 돈다.
반환값에 `kind`(`shelter`/`arrive`/`center`)를 실어 **어느 칸이 답했는지** 하네스가 셀 수 있게 했다.

| 접점 | 무엇 |
|---|---|
| `nearestVillageWake` | 앵커를 `SimVillages.shelterOf` 로. `listRespawnOptions` 4번 폴백도 같이 바뀐다(같은 함수를 쓴다) |
| `SHELTER_SPEC` + `tryShelterStart/Advance` | 노·숯가마·회관이 쓰는 `_siteStart/_siteAdvance` 계약 그대로 — **새 동사 0** |
| `_siteStart` 의 `spec.claim` | ★허가를 묻는 문을 spec 이 갈아 끼울 수 있게 한 줄. 기본은 종전 그대로 `_claimFootprint` |
| `Onboarding.init` / `Newcomers.init` | `shelterOf` / `hasShelter` 주입 — **판정은 정본 하나** |
| `_shelterBackfill` | 부팅 뒤 주기 백필(NPC만 · 멱등). `SHELTER_BACKFILL=0` 이면 안 돈다 |
| `GET /shelterdbg` | 전수 실측(자리·영토 안 여부·이송 좌표). `?scan=1` = 백필 1회 |
| `BUILDING_HEIGHT` | `shelter_site: 4` · `shelter: 40`(움집과 같은 높이) |

⚠**`spec.claim` 은 쉼터만 쓴다.** 다른 spec 은 한 글자도 안 바뀌었다(기본 분기가 종전 동작).
⚠쉼터 완공 훅은 **터 표지를 지운다** — 실체(6×4)는 `addShelter` 가 정본 경로로 이미 세운다.
터를 남기면 같은 자리에 표지와 집채가 겹친다.

## Z-시계. ★★[T20 회부 · 재민 지시 2026-09-03] **시계 둘 금지** — `gameDayNow` 하나로

`Membership.init`·`Onboarding.init` 이 `zoneGameDay`(**벽시계 파생**)를 받고 있었다.
그런데 화면의 날짜·달력·온도는 전부 `gameDayNow`(**econ 게임일**)를 쓴다 — 두 시계는
따라잡기가 없어 **영구히 어긋난다**(`gameDayNow` 주석이 이미 그 이유를 적어 뒀다).
⇒ 둘 다 `gameDayNow` 로 바꿨다(**2줄**). `__e2e_day_freeze` 는 두 시계를 다 얼리므로 하네스도 그대로다.
⚠전환 순간 저장된 `s.day`·`wdDay` 가 새 시계와 어긋나 **하루 정산·인출 몫이 한 번 초기화**될 수 있다.
초기화는 안전한 쪽이라 그대로 둔다(값을 지어내지 않는다).

## Z-소켓. ★[T80 ⓪-2 · 재민 확정 2026-09-03] 일틱이 루프를 잡는 동안 **소켓을 닫지 않는다**

`server/zone.js` http 서버(`const wss` 바로 위) **두 줄**:
`server.keepAliveTimeout = 30000`(`ZONE_KEEPALIVE_MS` 로 되돌림) · `server.headersTimeout = +10000`.

node 기본 `keepAliveTimeout` 은 **5초**다. 일틱이 그보다 오래 루프를 잡으면 node 가 **클라이언트가
막 쓴** keep-alive 소켓을 닫고, 그 요청은 답을 못 받아 undici 기본 300초를 기다리다
`HeadersTimeoutError` 로 터진다(T60 에서 하네스 셋이 이걸로 죽었다 — **플레이어 폴링도 같은 창**).

**값은 실측에서 끌었다**(`scripts/test-tick-slicer.js` ⓑ · 12게임일 창):
조각내기 켠 상태 **최대 조각 1,790ms**(caravan) · 끈 상태(대조군)는 하루 전부가 한 프레임 **4,675ms**.
⇒ 30초 = 각각 **×16 · ×6**. 여러 조각이 연달아 겹치는 최악과 `VILLAGE_TICK_SLICE_MS=0` 까지 덮는다.
⚠**안전망일 뿐이다.** 진짜 수리는 T1 의 조각내기고, 남은 바닥(caravan 조각)은 회부 §4-A 다.
⚠`headersTimeout` 은 `keepAliveTimeout` 보다 **커야** 뜻이 있다(node 규약) — 손으로 둘을 따로 만지지 마라.

## T42-c. ★★[T85 2026-09-03 재민 확정] **재개 가능 A\*** — 한 걸음을 조각으로 판다

T42 회부 3 의 종결. **PM 판정 ⓐ 재개 가능 A\* 채택 · 워커 스레드 기각**
(경로는 랩과 서버가 **같은 코드로 같은 답**을 내야 하는 자리다 — 워커로 옮기면 정본이 프로세스
경계를 넘으며 사본이 되고, 결정론이 스레드 스케줄에 얹힌다).

### 문법 — 문이 둘, 계산은 하나

```
sim/path-core.js
  _searchBegin(sx,sy,gx,gy,o) → S        상태를 만든다(힙·g·came·pops 가 전부 S 안)
  _searchStep(S, budget)      → {done,path}   budget<=0 = 완주. 양보는 open.pop() **앞**에서만
  _search(...)  = begin → step(0)        ← **동기 래퍼**. 종전 호출자는 이 문만 안다(서명 무변)
  routePath / routePathBegin             ← 프리셋은 `_routeOpts` **하나**, 문이 둘
server/villages.js
  _routeBegin/_routeFinish               노드열↔px 변환도 한 곳
  computeRoutePts(...)                   ← 동기 문(서명 무변) · 랩·전쟁·귀환·감사가 전부 이것
  getRouteResumable(a,b,budgetMs)        ← 재개형 문 · { done, pts }
```

★**답은 비트 동일**이고 근거는 구조다: 양보는 반복 사이에서만 일어나고(반복 하나는 통째로 돌거나
아예 안 돈다), 양보가 만지는 것은 없다(전부 `S` 에 남는다) ⇒ 예산이 1이든 무한이든 **꺼내는 순서가 같다**.
실측: 정본 시드 세계 **586쌍 전부 비트 동일**(예산 **1노드** · `test-route-persist ⑧`).

### ⚠⚠ 슬롯은 **하나**다 — 그리고 새 탐색이 세워 둔 것을 **버린다**

교역로 격자의 scratch 는 **하나**다: `sc.gen` 은 탐색 시작 때 한 번 오르고 `gGet` 은 `stamp === gen`
으로 읽는다 ⇒ 두 탐색이 번갈아 쓰면 **서로의 g 를 지운다**. `sc.came` 는 스탬프도 없다.
(`sim/_path-core-test ④b` 가 같은 scratch 로 번갈아 돌려 **실제로 깨지는 것**을 보인다.)

```
  let _pathJob = null;                 슬롯 하나
  _routeBegin(): if (_pathJob) { _probe.pathDrop++; _pathJob = null; }
                 ↑ **어떤 문으로든** 새 탐색이 시작되면 버린다.
                   동기 문(전쟁 행군·귀환 폴백·`/routedbg` 감사)이 30Hz 로 끼어들 수 있다.
                   이 줄이 없으면 재개형이 **조용히 틀린 길**을 낸다.
  invalidateTradeDistances(): _pathJob = null;
                 ↑ 세계가 바뀌면 **파던 것도** 버린다 — 안 그러면 바뀌기 전 세계로 판 길이
                   무효화 **뒤에** 캐시에 들어앉는다(`test-route-persist ④` 가 잡았다).
```
⚠**새로 재개형을 쓰는 자리를 만들 사람에게**: 슬롯을 늘리지 마라. 늘리려면 scratch 를 그만큼
더 만들어야 하고(격자 하나가 547×1016 × 3배열), 그건 이 카드가 산 값보다 비싸다.

### 예산 — 새 손잡이 0

`VILLAGE_TICK_SLICE_MS`(16ms) **그대로**. `0`(끈 상태)이면 재개형도 한 번에 완주한다(종전 동작).
알갱이(시계를 보는 단위)는 **32노드 고정 · 키우지 않는다** — 두 번 틀리고 얻은 값이다:
1,024 고정 → 한 조각 **193ms** · "잰 속도로 키우기" → **519ms**(따뜻한 구간에서 재고 차가운 구간에서 씀) ·
32 고정 → **31ms**. 시계 값은 예산의 0.1%(15µs)다.

### 캐러밴은 **하루를 안 넘긴다**

길을 못 받으면 `_caravanSyncOne` 이 `'retry'` 를 내고 `_drainTickJobs` 가 그 조각을 **큐 맨 앞에**
도로 넣는다 ⇒ 같은 하루 안의 다음 조각에 이어 간다.
`departDay`/`arriveDay` 는 econ 소유라 안 건드리고, `arriveAt = econDayToMs(arriveDay)` 는 절대 시각이라
몇 조각 늦게 떠도 **도착이 안 밀린다**(Stage 4B 자기 보정) ⇒ `travelDays` 규약(T60 ③) 무변.
맨 앞에 넣으므로 단계 순서도 그대로다(`test-tick-slicer ④` ✓).

### 실측 (`scripts/test-route-persist.js` **40/0** · `scripts/test-path-core.js` 3/0)

```
  ★586쌍 재개형 vs 동기   불일치 **0** (예산 1노드 · 한 쌍 최대 236,269조각)
  ★재개형 한 조각 최대     1,265~1,695ms → **31ms** (예산 16 + 알갱이 26)
  ★조각 최대(tick-slicer)  베이스 1,813·1,927ms(**caravan**) → **298ms(life)·940ms(ore)**
  ★한 프레임 최대          1,814ms → **309ms** · ③ 절대 1/3 베이스 ✗ → **✓** (효과비 11.87)
  ★e2e-rtt 창 최대 조각    2,928ms(caravan) → **970ms(ore)**
  ★3시드 800일 A/B         출력 통째 diff **0**
```
⚠루프 최대 막힘(⑥a·⑥ · `e2e-rtt ③`)은 **베이스에서도 같은 자릿수로 빨갛다**(16~17초).
가장 큰 조각의 20배라 제품이 만든 시간이 아니라 **2코어 상자가 멎은 시간**이다(회부 §A).

### 러너 밖에 있던 검사기 하나를 물렸다

`sim/_path-core-test.js`(경로 정본 계약)는 **아무도 안 돌리고 있었다** — 러너의 자동 발견이
`scripts/*` 만 훑는데 그 파일은 `sim/` 에 있어 표(`// @regress`)를 붙일 자리가 없었다.
⇒ `scripts/test-path-core.js` 가 그것을 자식으로 돌리고 결과만 옮긴다(검사 사본 0 · 34/34).
