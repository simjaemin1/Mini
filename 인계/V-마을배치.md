# 인계 — V 마을·배치·건립

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.

## T427-현. ★★2026-09-26 — **현장은 집에서 닿는 곳만**(손잡이 `T427_SITE_REACH` 기본 끔 · 승인 대기 · 보고/T427_2026-09-26.md)

> 생활 층이 날마다 주는 직업 현장(`npc._workSite`)을 **배정 순간에 한 번** "집에서 닿나" 묻는다. 술어는 존이 deps 로 주는 `npcCanReach`
> (= `computeNpcPath` 의 그 술어 · 그 반경 64 · 새 탐색 함수 0 · 새 수 0). 안 닿으면 종전 순서의 다음 후보 · 전부 안 닿으면 **집**(`npcHomeX/Y` · 없으면 회관 칸 중심).

* **자리 다섯**(`villages.js` · `_lifeJobSites` 바로 위 도우미 넷 `_t427On`·`_t427HomeOf`·`_t427Reach`·`_t427Site`):
  ① 벌목·채광·채집 ② 어부 ③ 사냥꾼 배정 — 세 줄이 한 식(`_t427On() ? _t427Site(…) : 종전 sites[h % n]`)
  ④ 어부가 드리울 자리(앵커 128px 안 물가 · 켜면 집에서 닿는 것만) ⑤ `huntHunters` 하루 옮기기(같은 선호 — 빈 풍부 셀 → 아무 풍부 셀 · 풍부도 내림 — 로 닿는 첫 셀 · 전부 안 닿으면 현장 = 집 · **장부 `_huntWk` 는 종전 셀**).
* **기억** — 마을·날마다 (집 칸 > 후보 칸) 답을 저장해 같은 날 같은 쌍을 두 번 묻지 않는다(`test-move-soa ⑮-f`). 존 밖(헤드리스·3시드 랩)엔 술어가 없어 **켜도 끈 것**(⑮-g) — 헤드리스 값 무변.
* 켬이 바꾼 배정은 **11 %**(현장 683 중 다음 후보 61 · 집 14 · 사냥꾼 하루 옮기기 12~42 중 5~27) — 나머지는 비트 그대로. 움직임 값은 `인계/M-이동입력.md` T427 절.
* ⚠`T325` 나무꾼 현장(`_t325Site` · `T325_WOOD_ACT` 기본 끔)은 안 덮었다 — 켤 때 같은 한 줄(회부).

### 집이 현장이 된 마을(발견 · 회부 재민 "반경 밖 사냥터 마을 값")

| 마을 | 직업 | 집이 현장인 사람(판 A·C·D 같음) | 까닭 |
|---|---|---:|---|
| 광산3 | 사냥꾼 | **10** | 그날 사냥터 후보가 **하나**(짐승 한 마리 자리)이고 그 칸이 물·바위 |
| 어촌1 · 어촌12 | 사냥꾼 | 2 · 2 | 같은 꼴 |
| 어촌3 · 어촌9 | 사냥꾼(하루 옮기기) | 1~3 | 풍부 셀이 전부 집에서 못 닿음 — 장부는 종전 셀 |

* 뿌리는 **사냥터 후보 = 그날 서 있는 짐승 자리**(`_lifeJobSites` hunt · 초식 짐승 실위치 ≤24)다 — 짐승이 적은 마을은 후보가 하나뿐이고, 그 짐승이 바위 위에 서 있으면 현장이 없다(야생 층 · 회부).
* 종전에는 이 사람들이 막힌 칸을 현장으로 받아 날마다 거기로 갔다(`null` → beeline → 막힌 목표 여행). 켜면 집에 머문다 — **몸은 집 · 장부는 사냥터**를 어떻게 할지는 값의 결정(재민).

### ★사냥 밴드 20 ms 예산 — 깬 사람 수가 CPU 에 묶인다(발견 · T406 재판정의 남는 차)

* `_huntBandBuild`(`T146_BUILD_MS` 기본 20 ms/마을/하루)는 **벽시계 예산**이다 — 칸마다 `ta.isRock`·`ta.isBlocked` 를 묻다가 20 ms 가 차면 커서를 남기고 내일 잇는다.
  `_lifeGameDay` 는 밴드가 안 선 마을에선 `huntHunters` 를 부르지 않는다 ⇒ 그 마을 사냥꾼은 하루 사냥터를 못 받고 `취침` 라벨 그대로 · 밴드가 선 마을 사냥꾼은 `수색`(깸).
* ⇒ **술어 빠르기(T406)와 CPU 경합이 "어느 마을이 오늘 밴드를 다 짓나"를 바꾼다** — 같은 씨·같은 게임 시각에서도. 시계 손잡이로는 못 묶는다.

| 판(같은 벽시계 두 팔) | 두 팔 | 임업1 밴드 | 임업1 깬 사람 | 깬 사람 합(차) |
|---|---|---|---:|---:|
| B · E(시계 고정 · 경합 없음) | `TERRAIN_SEG_INDEX` 켬 ↔ 끔 | (탐침 전) | 23 ↔ 4 | 1,155 ↔ 1,119(+36) · 1,151 ↔ 1,122(+29) |
| F(제품 세계 · 두 팔 다 T406 켬 · CELL_CAP 만 다름) | 끔 ↔ CELL_CAP 켬 | **다 지음 ↔ 못 지음** | 23 ↔ 4 | 1,153 ↔ 1,139(+14) |
| G(시계 고정 · **CPU 경합을 줌**) | 켬 ↔ 끔 | 못 지음 ↔ 못 지음 | 같음 | 1,122 ↔ 1,119(**+3**) |

* 판 G 의 차 +3 은 밴드 목록의 차와 그대로 맞는다 — 켠 팔만 지은 임업5·농촌2·농촌22 각 +2 · 끈 팔만 지은 농촌4 −3. 몹 마릿수(449 · 종별)·술어 답은 두 팔 같다.
* 술어가 같은 두 팔도 갈린다 — 두 존이 한 기계(2코어)에서 CPU 를 나눠 쓰니 20 ms 안에 도는 칸 수가 흔들린다(판 F · ① 게이트 판 A2: 밴드를 다 지은 마을 끈 팔 15곳 ↔ 켠 팔 20곳 · 깬 사람 차 +15 중 +11 이 그 마을들).
* 회부(PM · 재민): 예산을 벽시계 대신 **칸 수**로 세면 결정론이 된다 — T146 이 이미 "이 수는 값 판정거리다(재민)"라 적은 그 자리(이 카드 무접촉).

## T400-집. ★★2026-09-26 — **집도 행위다 1층**(손잡이 `T400_BUILD_ACT` 기본 끔 · 승인 대기 · 보고/T400_2026-09-26.md)

> 크루(`LIFE_CREW` 2 · 명부 앞 두 사람 · 직업 무관 · `builder` 없음)가 곳간에서 통나무를 **손에 들고**(`actFromGranary` = `actToGranary` 의 역 · `npc.inventory`)
> 집터로 걸어가 **놓는 순간** 단계가 오른다. 자재는 `server/hut-stages.js` 표 그 수(econ 재화만: ② 통나무 18 · ③ 4 · 합 22 = 8·8·6 **3짐**).

* **어디서 도나** — `villages.js _t400BuildDay(vil)` 하나. `_lifeDaily` 의 관측자 문(`anyViewerNear`) **앞**에서 **모든 마을**이 부른다.
  ⇒ 관측 마을·빈 마을이 같은 장부 같은 걸음(T347 "빈 마을 하루" 문법을 전 마을에 · 관측자 0 ↔ 1 비트 동일 — `test-build-act ④`).
  관측 마을의 몸은 같은 길을 **보여 주기만** 한다(`_t400BodyStep` · 곳간 사다리 ↔ 집터 · 라벨 인출·운반·건축 · 회계·단계 0).
* **하루** — 한 사람 하루 왕복 상한 = `_t341TripsPerDay`(나무꾼 식 · 걸음이 정한다) · 한 짐 = `carry.CAP_KG ÷ weights.kgOf`(통나무 8) ·
  짐은 **이 집에 남은 몫까지** 꽉 채워 든다 · 자재가 차고 시공(한 사람 하루 `LIFE_STAGE_PDAY`)이 남으면 **그 순간** `_lifeAdvanceSite`.
  곳간이 비면 단계가 안 오른다(기다린다). 손은 날을 넘겨 들지 않는다(나무꾼 절이 곳간에 다시 넣는 일 0).
* **켬이면 꺼지는 것** — `_lifeHeadlessDay` 집 절(호출 0) · 몸의 마을 집터 시공 진척 · 반일 징발. 플레이어 의뢰 집터(`psite`)는 종전 길 그대로(회부).
* **econ** — 켠 마을은 `vil.econ._t400Live = true` → 집 증축의 목재 제약·추상 차감을 끈다(차감은 크루가 진다 · `buildActOn`). 단가는 표에서 유도(통나무 22÷6 · 석재 0).
* **계측** — `/lifedbg` 마을 칸 `t400`(누계: 걸음초·낮초·왕복·나른 통나무·전진 단계·크루 직업 · 끔이면 null). `t361-build-days.js` 가 팔 둘(`act`·`acteye`)을 더 돈다(`T361_ACT=0` 이면 종전 네 팔).
* ⚠**압축 날 계측기** — `T361_DAY_MS=4000`(낮 2.8초)에선 곳간↔집터 왕복이 낮에 안 들어가 켬 팔이 **집 0채**다(자의 잘못 · 실제 낮 1,008초엔 중앙 750px 왕복 23초). 켬 팔은 날 ≥ 약 63초(`T212_DAY_MS=90000`)로 잰다 — 이 카드의 ③ 표가 그 판이다.
* **실측(보고/T400 §0)** — 끔 3시드 800일 `t17-metrics` JSON 전체 동일 3/3(8,318 · 7,935 · 7,853 = 넷째 판) · 실서버 30일(날 90 s) 집 끔 261 ↔ 켬 257 ↔ 켬+관측자 257 ·
  한 채당 걸음 74.7초 = 실제 낮의 **7.4%**(T361 설계 중앙 7%) · 크루·일 = 전 인·일의 1.5%(어부 인·일의 3.5%) · 관측자 게이트 결정론 3/3 · 실서버 관측 6마을 6/6.
* 회부: #61·#62 뒤집기(한 글자) · 켜기(재민 · #69 와 같이) · 의뢰 집(`psite`)이 같은 표인지 · 석재 실물 수요 · 압축 날 계측기 규약(PM).

## T376-몸. ★★2026-09-23 — **몸 수 상한 40 을 풀면 예산 안이다**(낮 p50 23.9ms = 72% · drop 0 · lag 0)

> **`NPC_CAP_PER_VILLAGE`(환경변수 `VILLAGE_NPC_CAP` · `villages.js:92`)는 몸 수의 유일한 자다.**
> 세 판 모두 **몸 수 = Σ min(마을 econ 인구, 상한)** 이 **정확히** 맞는다(T346 등식이 상한을 바꿔도 그대로).
>
> 실지도 51마을 · 200일 · 부하는 **실제 날 길이 1,440,000ms 에 다시 띄워서**(T356 자 · 걸음 켬):
>
> | 상한 | econ | 몸 | 몸 없는 이 | 닿은 마을 | 닿은 날 | 집 | 침상 | 낮 p50 | 예산 | 낮 p95 | 밤 p50 | drop·lag |
> |---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
> | **40**(지금) | 2,606 | **1,640**(62.9%) | **966** | 17/50 | **41일** | 512 | 3,072 | 18.14ms | 54% | 27.05 | 11.14 | 0·0% |
> | 80 | 2,517 | 2,166(86.1%) | 351 | 6/50 | 70일 | 503 | 3,018 | 24.28ms | 73% | 31.45 | — | 0·0% |
> | **무제한** | 2,515 | **2,515(100%)** | **0** | 0/50 | — | 503 | 3,018 | **23.92ms** | **72%** | 31.31 | 15.55 | **0·0%** |
>
> ⇒ **캐논(관측자 없어도 실걸음 · 모든 NPC 는 실체)이 오늘 자로 이미 된다.** 단서 둘:
> **p95 31.3(94%)** 로 여유가 6% 뿐이고, **값이 선형이 아니다** — 40→80 은 +6.1ms 인데 80→무제한은 **−0.4ms** 다
> (80 팔은 몸 2,166 인데 비취침 1,773 · 몸당 걸음/틱 0.0667 로 세 팔 중 가장 낮다 ⇒ 두 팔의 차는 **머릿수가 아니라
> 세계 상태**다). 말할 수 있는 것은 띠: **몸 1,640~2,515 에서 낮 p50 18~24ms(예산 54~73%) · drop·lag 0**.
> 한계는 두 끝점 차분으로 **6.60 µs/사람** · 상수항 **7.32ms** ⇒ 10,000명 **73.3ms(예산 2.20배)** ·
> **예산이 버티는 몸 ≈ 3,942명**(지금 세계 2,515 는 그 64%). T356 의 갈래 합 8.22µs/사람과 같은 자리수다.
>
> **★풀어서 사는 것은 "사람이 더 생긴다" 가 아니라 "이미 있는 사람이 몸을 얻는다" 다** — 몸 +875 · 몸 없는 이 966→**0**,
> 그런데 econ 인구는 2,606→2,515(**−3.5%**) · 집 512→503 · 침상 3,072→3,018 로 **안 늘고 살짝 준다**.
> T361 이 낸 자리와 같다: 집 수를 정하는 것은 짓는 속도가 아니라 **수요**(`houseSiteWant`)이고, econ 인구가
> 안 늘면 수요도 안 는다. ⚠램프: `POP_SYNC_PER_DAY = 2`(마을당 ±2/일)라 **41~70일**에 걸쳐 차오른다(즉시 아님 · 곡선 실측).
>
> **★시공 자 — 1.25배는 노동량이 아니라 "하루" 라는 단위의 불일치다.**
> 랩 `L_BUILDSEC = 4,600 인·초/층`(리터럴) ↔ 서버 4단계 × (`dayLengthMs` ÷ `LIFE_STAGE_PDAY`) = **5,760**(파생).
> 계보는 **랩 → 서버**다(`villages.js:4397` 주석이 스스로 "랩 4,600 **근사**" 라 적었다).
> 문턱은 **벽시계 하루 1,440초/단계**(`:6797`)인데 크루는 **낮 1,008초**만 쌓는다(밤엔 취침 · prog 는 유지 · `:6759`).
> ⇒ 뜻대로("1인 **일하는** 하루") 읽으면 4 × 1,008 = **4,032 인·초 = 랩의 0.88배(차 12%)**.
> 지금 코드(벽시계)로 읽으면 5,760 = **1.25배(차 25%)**. **낮으로 맞추면 붙는다.**
> 그리고 지금 문턱은 한 단계에 **1.43 낮**이 든다 — T361 의 관찰 **2.86일/채**가 바로 그 결과다(⌈4÷2⌉ × 1.43).
> 고를 때 **`dayMs` 를 `dayMs × dayPhaseRatio` 로 읽을지**를 같이 못 박아야 한다(PM · 행위 카드).
>
> 계측기 `scripts/t376-body-cap.js`(러너 밖 · 제품 0줄 · 손잡이는 계측 팔의 환경변수뿐) — `a`(200일 곡선 셋) ·
> `b`(부하 셋 · ⓐ 의 DB 를 템플릿으로) · `table`. ⚠**압축 판의 틱은 부하 표에 못 쓴다**(하루가 360배 빠르면
> 하루 경계 일이 360배 자주 온다) — 그래서 국면을 둘로 갈랐다.
> ⚠자가수리 셋: 표본·관측자를 `spawnSync` 와 같은 프로세스에 두면 **루프가 멈춰 아무것도 안 적힌다** ·
> `/lifedbg` 는 **안 문**(T225)이라 비밀머리 없이는 404 → 관측자가 안 서고 존이 `!hasHuman && !hasObserver`
> 에서 **본문 없이 조기 반환**해 **p50 이 0** 으로 나온다(`zone.js:11468`).

## T361-집. ★★2026-09-23 — **집도 행위다(설계만 · 구현은 T356 뒤)**: 걸음은 병목이 아니고 **수요가 병목**이다

> **한 채의 자재 정본은 `zone.js:8691 HUT_STAGES` 다** — 발자국(`_vbFootprint` 6×4)이 아니다.
> 4단계 = `기둥 6 · 서까래 8 · 풀 6 · 이엉 8`(57.8 kg) · 선납분 = `PSITE_COST` 그 셋(`:8742`).
> 레시피(`:1220~1222`)로 되돌리면 **통나무 22 · 풀 38**(77.4 kg) · **석재 0**.
> ⚠**econ 축과 어긋난다**: `HOUSE_WOOD 1.5 · HOUSE_STONE 2.5` × 정원 6 ⇒ **목재 9 · 석재 15**.
> 목재 **2.44배** · 석재는 서버 공정에 **아예 없다**. 어느 쪽이 정본인지 = **재민 칸**(보고 §4-①).
>
> **걸음은 병목이 아니다.** 곳간↔집 중앙 **750 px**(최소 385 · 최대 1,358 · 마을 38곳 실측) ·
> 짐 `CAP_KG 25` ⇒ 자재 **3짐** · 하루 왕복 `_t341TripsPerDay` 중앙 **43회**(낮 1,008초 ÷ 왕복시간) ⇒
> **운반은 낮의 7.0%**(최대 12.6%) · 운반일 **1일**. 한 채 **4일**(운반 1 + 시공 3).
>
> **병목은 시공이고 그 자는 이미 있다 — 두 정본이 같은 수를 말한다:**
> 서버 관찰 `⌈4단계 ÷ LIFE_CREW 2⌉ ÷ dayPhaseRatio 0.7` = **2.86일/채** ↔ 랩 상한 `L_BUILDCAP 0.35` = **2.9일/채**.
> 인·초는 서버 **5,760** ↔ 랩 `L_BUILDSEC` **4,600**(1.25배 — `villages.js:4397` 주석의 "근사" 가 그 관계다).
> 서버 빨리감기는 `min(LIFE_CREW 2, pop) × LIFE_STAGE_PDAY 1` = **2일/채**.
>
> **★T311 §5 정정 — 서버 관측 마을엔 크루가 온다.** 관측자 6명을 마을 앵커에 세운 30일 팔에서
> **붙은 크루 2명(= `LIFE_CREW` 포화)** · 비관측 마을 0. 전진 단계도 관측 2 ↔ 비관측 2.
> ⇒ T311 의 *"크루 누적초 양쪽 0"* 은 **랩 실측**이고, `L_BUILDRATE × 0.25` **바닥은 서버에 없다**.
> 서버에서 0 인 것은 비관측 마을뿐이고 그건 정상이다(`anyViewerNear` 가 거짓 → `_lifeHeadlessDay` 가 푼다 — `villages.js:6675`).
>
> **★★그래서 옮겨도 집 수는 안 변한다.** 30일 실측 세 팔이 **같은 수**다 —
> 끔 **+119채** · 걸음켬(`T312_FISH_ACT=1`) **+119채** · 관측자 6명 **+118채**(관측 마을만 보면 31 = 31).
> 까닭: 마을당 30일에 3.1채 ⇒ **한 채를 원하는 간격 9.6일** 인데 짓는 데는 2~2.86일이다.
> ⇒ 지금 집 수를 정하는 것은 **짓는 속도가 아니라 수요**(`houseSiteWant`)다. 행위로 옮기면
> **값은 그대로 · 비용만 낮의 7% · 바뀌는 것은 누가 짓느냐**다(그게 #51 ⓐ 가 원한 것이다).
> ⚠`NPC_CAP_PER_VILLAGE = 40`(#54 · T346 표)을 풀면 인구가 늘어 수요 간격이 좁아지고 **그때는 짓는 속도가 병목**이다.
>
> **짓는 직업이 없다.** `JOB_RES`(`:4986`)는 벌목·채광·채집 셋이고 `builder` 가 없다. 지금 짓는 자는
> **직업 무관 여유 크루 ≤ `LIFE_CREW` 2명**(`:6816` — 농부만 개간에 묶인다). 그대로 두면 행위 규칙에서
> **어부가 집을 짓다 물고기를 못 잡는다** ⇒ 재민 칸.
>
> 계측기 `scripts/t361-build-days.js`(러너 밖 · 제품 무접촉) — 네 팔(1일·30일끔·30일걸음·30일관측자).
> ★관측자 팔은 **있는 문**만 쓴다: `ws://…/?observer=1` + `viewport_update`(`e2e-onboarding.js:174`) ·
> 좌표는 `/lifedbg` 의 `ccx/ccy` · `anyViewerNear` 가 `observers` 를 본다(`zone.js:992`).
> ⚠**관측자는 딴 프로세스여야 한다** — `spawnSync` 가 이벤트 루프를 멈춘다(첫 판에서 한 명도 안 섰다).
> 손잡이 이름 **`T361_BUILD_ACT`** 는 다음 카드의 것이다(이 카드는 제품 0줄).

## T219-방. ★★2026-09-12 — **집터 방아쇠는 `village-layout.houseSiteWant` 하나다**(손잡이 기본 끔)

> 규칙은 `villages.js` 가 안 갖는다. `server/village-layout.js` 의 `houseSiteWant(pop, housing, builtFloors, sites)`
> 하나가 정본이고 — 랩(마을실험실·전쟁실험실)의 방아쇠 세 줄과 **같은 규칙**이다 — 생활층은 그걸 부른다
> (`villages.js:5364` 근처). 목표 = `ceil(max(pop, housing) / HOUSE_CAP_PER_FLOOR)` · 원하는가 = **부족분**
> (`완공층 + 공사중 < 목표`). `max` 는 랩이 값비싸게 배운 것이다 — 인구만 보면 *"침대가 차야 인구↑,
> 인구가 늘어야 집터↑"* 데드락이 선다.
>
> **손잡이 `T219_HOUSE_TRIGGER` — 기본 켬(T298 09-19 · 끄는 문은 명시 `=0`).** 끄면 종전 서버 고유 비율(`인구 > 침상 × 0.92` — `0.92` 는 랩에 없다).
> 켜는 조건은 **기하 완화가 먼저**다: T219 실측(실서버 200일 · 50마을 · 끔/켬)에서 켬은 실체 집을 **더 못 지었다**
> (236채 → 230채 · 집터 탐색 931→817회 · 성공 136→130). 끝판 상태로 두 식을 다시 물어보면
> **랩식만 발화한 마을 0 · 종전식만 발화한 마을 12** — 이 세계에서 랩 규칙은 종전보다 **더 엄격**하다.
> 벽은 방아쇠가 아니라 **기하**다: 빈손 795/931 village-day · 거부 사유의 **78%가 집 간격 18** ·
> 영토는 200일에 50마을 합쳐 **63셀**만 자란다(T212 판 93셀). 표는 `보고/T219_2026-09-13.md`.
>
> ⇒ **집터를 여럿 열 필요가 없다**(움집 하나는 2일에 선다 — 슬롯 하나로 200일에 100채가 가능한데 실제는 2.8채).
> 랩의 동시 상한 `sites < max(2, round(pop/25))` 은 서버 슬롯 하나(`!vil._site`)가 **이미 더 엄격**하므로
> 그 수를 서버 쪽에 옮겨 적지 않았다(사본 0).

## T326-간. ★★2026-09-19 — **집 간격 기본은 `15`**(통로 항 버림 · PM 결정 #52 · 손잡이 기본 **켬**)

> **정본 둘 — `server/village-layout.js`(이름에 `LIFE_` 가 붙는 이유는 아래 ⚠):**
> `LIFE_HOUSE_GAP = LOT_R + (LOT_R + FARM_GAP)` = 6.5 + 8.5 = **15** ← **기본**
>   (= 한 집의 **농지 완충 원**이 다른 집의 **부지 원**을 침범하지 않는 최소 중심거리 · `HALL_CLEAR` 와 같은 문법)
> `LIFE_HOUSE_GAP_AISLE = 2 × (LOT_R + FARM_GAP) + AISLE` = **18** ← **되돌림**(`T315_HOUSE_GAP=0`)
> 닫힌식·셀 집합 두 경로가 둘 다 같은 답을 낸다(`lab-wiring-check` [J]ⓐⓑ · 15 는 최대 hypot 14.422 · 18 은 17.889).
> ⚠**통로 항만 빼면 17 이다.** PM 이 고른 수는 **15**(T315 가 실측을 붙인 그 값) — **17 은 아무도 안 쟀다**.
>
> ⚠⚠**`HOUSE_GAP` 이라는 이름은 이미 임자가 있다** — `village-layout.js:172` `generate()` 안의
> `HOUSE_GAP = opts.houseGap ?? (shore ? 18 : 22)` 는 **시딩 배치**의 간격이고 생활층 간격과 **다른 수**다.
> 생활층 쪽에 `LIFE_` 를 붙인 이유가 이것이다(T326 작업 중 이름이 겹쳐 시딩 기하가 조용히 15 로 바뀌는 것을
> 한 번 밟고 되돌렸다). **시딩 18/22 의 출처는 주석 한 줄("골목 여유")뿐 — 유도 안 됐다.** 회부.
>
> **랩도 리터럴을 버렸다** — `L_HGAP = VillageLayout.LIFE_HOUSE_GAP`(랩 둘 · 인라인 사본에 유도 셋을 손 동기).
> 랩 기본 = 서버 기본이다. 검사기 `lab-wiring-check` [I](랩 리터럴 0 · 서버 `HG` 분기 꼴 · 인라인 식 동일) · [J].
>
> **실서버 200일 50마을(`보고/T326_2026-09-19.md`)**: 집 384 → **438~443채** · 침상/인구 78 → **106~107%** ·
> 침상≥인구 **50/50 마을** · 빈손 1,176 → **185**(−84%) · 성공 284 → **339** · **소멸 0** · 인구 대역 안.
> 거부 사유 점유율은 **안** 내려간다(66% → 77.5%) — 간격은 마지막까지 남는 사유다. 내려간 건 빈손 자체다.

## T315-간(옛 · T326 이 뒤집었다). ★★2026-09-19 — **집 간격 `18` 은 유도값이다**(리터럴이 아니다 · 손잡이 기본 끔)

> `18` 은 누가 고른 수가 아니라 **이 모듈의 세 항이 내는 최소 중심거리**다 —
> `village-layout.HOUSE_GAP_DERIVED = 2 × (LOT_R 6.5 + FARM_GAP 2) + AISLE 1 = 18`.
> 뜻: 두 집이 서로의 **농지 완충 원판**(`houseFarmBlock` = `inDisc(LOT_R + FARM_GAP)`)을 침범하지 않고
> 사이에 **통로 한 칸**이 남는다. 집채 발자국(`_vbFootprint('house')` = 6×4 = 24셀)은 부지 원판 안에 **전부 든다**(밖 0셀).
> **닫힌식과 독립인 두 번째 경로**로도 같다: 짝수 격자에서 `LOT_GUARD`(216셀) 원판 둘이 겹치거나 8방 한 칸
> 팽창으로 닿는 최대 `hypot` 17.889 ⇒ 통과 조건 `hypot ≥ HG` 의 최소 정수 = **18**.
> ⚠닫힌식은 연속 원 기준 **하한**이라 상수에 따라 셀 집합보다 1 작을 수 있다(`FARM_GAP` 3·5). **정본 6.5·2 에서는 정확히 같다.**
>
> **어느 항까지 요구하나 → 무슨 수가 되나**(전부 정본에서 유도 · 새 수 0):
> 부지 원판 둘만 비침범 **12** · +통로 **15** · 완충 원 ↔ 부지 원 비침범(`HALL_CLEAR` 문법) **15**(=`HOUSE_GAP_LOT`) ·
> 완충 원 둘 비침범 **17** · **+통로 한 칸 = 18**(=`HOUSE_GAP_DERIVED` · 지금 값).
>
> **손잡이 `T315_HOUSE_GAP` — 기본 끔**(끔 = 종전 리터럴 18). `1` = 유도식 ⇒ **18 이라 끔과 같은 세계**(그 동일함이
> "리터럴이 유도값이었다"의 증명이다). `2` = **진단 팔**(`HOUSE_GAP_LOT` 15 · 제품 아님 · 값 결정은 재민).
> 검사기: `lab-wiring-check` [J](닫힌식·셀 집합·켬=끔·`FARM_GAP` 변조) · [I](랩 `L_HGAP` = 서버 끔값 = 유도값 **셋이 같다**) ·
> `test-psite-server` ⑫ⓐ.
>
> **★그래서 66% 는 유도로 안 풀린다.** 실서버 200일 50마을 실측(`보고/T315_2026-09-19.md`):
> 간격을 **15** 로 좁히면(진단 팔 · 살리기 끔) 집 384 → **458채** · 침상/인구 78% → **101%** ·
> 빈손 1,176 → **127**(−89%) · 성공 284 → **359** · **인구 대가 0**(2,724 — T254 대역 안) · 소멸 0.
> 즉 **`_mapBeds` 를 살릴 조건은 간격 하나가 만든다.** 버릴 항을 고르는 일은 값 결정이라 **재민 몫**이다.

## T315-침 → ★T326. **`_mapBeds` 살리기는 서버 한 줄**(손잡이 `T315_MAPBEDS` — ★T326 부터 **기본 켬**)

> 읽는 자리는 **처음부터 있었다** — `sim/economy-sim.js:3140` `_hcap = min(housing, _mapBeds)`. 서버엔 **쓰는 이가
> 없어** 늘 `undefined` 로 접혀 `_hcap = housing`(곧 **목재**)이 상한이었다(T207·T212·T298 ⓓ).
> 살리는 자리는 `_lifeDaily` 의 `cap` 을 세는 줄 옆 **한 줄**이다:
> `if (T315_MAPBEDS) vil.econ._mapBeds = _mapBedsOf(vil);`
> `_mapBedsOf(vil) = _houseCells.length × village-layout.HOUSE_CAP_PER_FLOOR` — **정본 함수 하나**(제품과 하네스가
> 같이 부른다 · `__probe.mapBedsOf`). 랩 `_bf * L_FLOORCAP` 과 **같은 상수**다(사본 0 · 새 수 0).
> 영속 코드 0 — `vil.econ` 의 plain number 는 `serializeEcon` 이 그대로 나른다(실측 50/50 마을).
> **하루 지연은 설계**다: 일틱 순서가 ① econ → ⑩ 생활층이라 오늘 완공한 침상은 **내일** 출생이 읽는다.
>
> **★[T326] 기본 켬이 됐다**(PM #52 · 미설정 = 켬 · 끄는 문 `=0`). 간격 15 위에서 켠 판(2반복):
> 인구 2,462~2,510(대역 안) · 집 438~443 · 침상/인구 **106~107%** · 침상≥인구 **50/50** · **소멸 0** ·
> `_mapBeds` **50/50 마을 전부 기록**(정확 47~49 · 나머지는 저장 큐 편차 한 채 이내 — `econ_state` 는 1마을/틱).
> 되돌림 `=0` 둘은 종전 판과 열마다 같다(침상/인구 82% · 적힌 마을 0 · 정지 9 · 소멸 0).
>
> **남은 즉시 정지 2~3곳은 땅 때문이 아니다.** `scripts/t326-eochon2.js`(끝난 판 DB + 정본 지형 어댑터):
> 광산5(영토 6,840셀 · **물 0 · 바위 0**) · 어촌2(9,900셀 중 **물 14셀**) · 임업6(14,100셀 · **물 0 · 바위 0**) —
> 셋 다 하드 필터를 지나는 앵커가 543~2,136곳이고, **지금 서 있는 집을 그대로 두고 다음 한 채를 놓을 자리가
> 167·176·875곳** 남아 있으며 `houseSiteWant` 도 **더 원한다**. 그런데도 집이 안 선다.
> ⇒ **"물에 낀 마을" 이라는 설명은 틀렸다.** 막힌 자리는 방아쇠 **뒤** — `_lifeAddHouseSite`(2패스 ·
> 거부 메모 `_siteRejS`/`_siteRejL` · `LIFE_SITE_MEMO`)나 크루/순서다. 세 팔에서 어촌2 의 수가
> 72/72/82.8 로 **완전히 같다** — 잡음이 아니라 결정적 정지다. **다음 카드의 자리.**
> ⇒ 계측기 `scripts/t315-gate.js` · `scripts/t326-eochon2.js`(둘 다 러너 밖 · 세계를 안 돌린다).

## 3-사. [T146 사냥 이식 2026-09-06 · **`인계/W-야생.md` 로 옮겼다**]

> 마을 생활층 하루 틱의 사냥 절(개체군 `vil._gameRich` · 회복 · 차감 · `land.game` 갱신 · 밴드 구축)은
> **`인계/W-야생.md`** 가 주 영역이다(2026-09-07 · T146 2판에서 신설). 여기 두면 두 곳에 같은 말이 산다.
> `villages.js` 를 만지는 세션은 그 파일의 **함정 다섯**을 먼저 읽어라 — 표본 격자 · 이동 문턱 ·
> 분산 사냥 · 구축 예산 · px↔셀 왕복.

## T60-회. ★★2026-09-03 — **농지 0 · 숲 0 자리에도 마을이 선다** (재민 판정: V 회부)

> 나온 자리 `보고/T60_2026-09-03.md` §0-ⓔ'. 잰 자 `scripts/t60-extinct.js`(계측기 · 러너에 안 든다).
> 재민 판정 2026-09-03: **"농지 0 자리 마을"은 V 회부**(같이 나온 "생곡을 식량으로 안 센다"는 ECON 수술 2).

**실측 한 줄** — 시드 3(정본 시드 아님 · 1·2·3 계열)에서 51곳 중 **한 곳이 소멸**했다:

| 마을 | 소멸일 | 최대 인구 | 땅 | 죽기 전날 |
|---|---|---|---|---|
| **광산1**(#0) | Day **428** | 21명 (Day 38) | 해안 X · **농지 0.00 · 숲 0.00** · 물 0.08 · 광 2.47 · 돌 2.50 | 인구 1 · 식량equiv 1.2 · 흐름EMA **−1.591** · dP 분해가 **hunger −0.477** 하나 |

```
  ① 자리가 순수 광산이다 — 제 힘으로 먹을 것을 못 낸다(광·돌만 2.5).
  ② 365일 정착 보호막(SHIELD_DAYS)이 음수 압력 62.5 를 삼키며 21명까지 키웠다.
     보호막이 걷힌 뒤 **63일** 만에 0 — 누적 아사 20.
  ③ 교역으로도 못 메웠다(식량흐름 EMA −1.59).
  ⇒ **econ 축(T17 도구·보존식·소금)은 이 마을의 dP 분해에 한 항도 안 나온다.** 자리의 문제다.
```

⚠**판단거리(구현 금지 — 이 절은 회부다)**
```
  ⓐ 시딩 자리 품질 게이트   `pickSeedVillages` 가 농지·숲이 둘 다 0 인 자리를 거른다
                           ⇒ 지도의 "광산 마을"이 통째로 사라질 수 있다(고증·지도 의도와 충돌)
  ⓑ 보호막을 자리에 맞춘다   척박지는 보호막을 짧게/없이 — 그러면 **일찍** 죽는다(같은 결말, 빠른 진단)
  ⓒ 그대로 둔다             "척박지엔 마을이 안 남는다"는 이미 캐논이다(POP_MIN=0 주석)
                           ⇒ 그렇다면 소멸 0/51 은 **정본 시드에서만** 참인 표라고 적어야 한다
```
★셋 다 **기준선 표의 뜻**(소멸 0/51)을 건드린다 — 고르기 전에 `인계/공통.md` 기준선 절과 같이 읽어라.

## 3-온. ★2026-09-01 — **도착 지점 산출 규약** (온보딩 v2 §9.2)

> 구현 정본 `server/onboarding.js` `_computeArrivals()`. 이 절은 **산출 규약**만 적는다.
> 로비·입장 흐름은 `인계/N-네트워크.md` 3-온, 첫 의뢰·기여는 `인계/E-사건장부.md` 3-온.

캐논: **캐릭터는 발생하지 않고 도착한다.** 임의 좌표·구역 내 무작위는 기각(§9.2).
마을마다 도착 지점은 **하나**이고, 같은 지도면 언제나 같은 자리다(결정론 · 시드 없음).

```
  ⓐ 방향을 정한다 — 새 데이터 0, 전부 이미 있는 정본에서 유도
       물가 마을 : VillageLayout.nearestBank(강·호수에 접한 뭍 한 칸) 방향  = 나루터(dock)
       내륙 마을 : 가장 가까운 **이웃 마을** 방향                          = 길목(road)
       (둘 다 없으면 배산임수 축 axisAt().toWater = 들(field), 그것도 없으면 남(0,1))
  ⓑ 그 방향의 **영토 경계 셀** = 어귀. 원천 셋: welcome `b`(라이브) · `terr`(레이아웃) · 원 근사
  ⓒ 경계 바깥으로 ONB_ARRIVE_OUT(2셀) — 마을 **앞**에 선다
  ⓓ 막혔으면 나선 탐색(≤8셀) · 그래도 없으면 중심에서 바깥→안쪽으로 훑는다
  ⓔ 물가 마을이면 마지막에 다시 nearestBank(6셀)로 물가에 붙인다
  ⓕ **마을 중심까지 4연결 BFS 로 실제 도달 가능**해야 채택한다(정본 술어 isBlocked 하나만 본다)
```

★★**전수 검사가 잡은 것 하나** — 방향만 보고 어귀를 고르면 **물가 취락에서 어귀가 중심에 붙는다**.
배산임수 `shore` 배치는 물 쪽으로 납작해서 물 방향 경계 셀이 중심에서 **1~2셀**이다. 그러면
"도착해서 마을로 걸어 들어간다"(§9.4 0~3분)가 통째로 사라진다 — 이미 안에 있다.
⇒ **거리를 먼저 요구하고 그 안에서 방향을 고른다**: `ONB_GATE_MIN_CELLS`(10) 와
경계 최대 반경 × `ONB_GATE_MIN_FRAC`(0.45) 중 큰 쪽. 수리 전 3곳(어촌2·11·16)이 1~2셀이었고
수리 후 **최소 17셀**이다.

★**산출은 부팅 때, `server.listen` 전에 한다** — 50마을 **37초**(2코어 실측)라 접속 뒤에 돌리면
서버가 그만큼 멎거나(게으른 산출) 틱을 굶긴다(배경 산출). 상세는 `인계/N-네트워크.md` 3-온 ④-b.
비용을 줄이려면 도달성 BFS 범위(`ONB_ARRIVE_SEARCH`·어귀 거리 기준)나 `nearestBank` 반경(30)을
조이면 된다 — 정확도와 맞바꾼다.

**실측 — 실지도 51마을 전수** (`node scripts/test-onboarding.js`, 7분):
```
  51/51 산출 · 물·바위 위 0 · 도달 불가 0 · 존 밖 0 · 8셀 미만 0
  중심까지 거리(셀)  최소 17 · 중앙 38 · 최대 57
  종류               나루터 26 · 길목 25            (전부 나루터도, 전부 길목도 아니다)
                     ★라이브 50마을에서는 나루터 27 · 길목 23 — 지도가 같으니 비율도 같다
  나루터 26곳 전부 물에서 2셀 이내             ← 이름만 나루터가 아니다
  결정론 51/51 (두 번 산출해 같은 좌표)
```

### 마을 성격 — **입지계수**(어촌/산촌/농촌)

시작 화면 아이콘은 econ 직업 분포에서 자동으로 나온다. **절대 수로 고르면 51곳이 전부 농촌이 된다**
(어느 마을이든 농부가 제일 많다 — 실측으로 그랬다). 그래서 `이 마을의 몫 ÷ 세계 평균 몫`(LQ)이
가장 큰 갈래를 쓴다. 임의 문턱 0.
```
  어촌 = fisher · 산촌 = miner+mason+lumberjack+hunter · 농촌 = farmer+forager
  실지도 51마을 분포: 농촌 24 · 어촌 16 · 산촌 11
```

### 빈터 구역

`Onboarding.vacantZoneOf(vid)` = **도착 지점을 중심으로 반경 `ONB_VACANT_R`(10셀 · 320px)**.
누적 기여 N회를 채운 사람만, **자기가 도착한 마을**의 그 원 안에서 개인 사유지의 길드 영토 요건이
면제된다(`tryClaim` 접점 한 줄). 구역 밖·다른 마을은 종전 그대로 거절된다.

## 다른 영역에 있는 관련 절

* → 참조: `인계/C-채집자연물.md` — 3-작. ★★★2026-08-31 작물 층 — **카탈로그 34종이 게임에 들어왔다**
## T48. ★★★2026-09-02 — **집 의뢰 한 번이 존 서버를 죽이고 있었다** (`test-psite-server` 진짜 빨강)

> 접촉 `server/villages.js`(lazy 이름 제거 · 리네임뿐) · `server/zone.js`(건축 비용 지불/환불 대칭) ·
> `scripts/test-psite-server.js`(⑤ 판정 교정 + ⑨ 실행 검증 신설 + 러너 등록).
> **econ diff 0줄** · **클라 diff 0줄** · 3시드 기준선 **불변**.

### 카드가 겨눈 것과 §0 이 찾은 것이 달랐다

T9 전수표는 러너 밖 하네스 31개 중 유일한 진짜 빨강으로 이걸 지목하며
*"차감이 자리 확정 전에 일어난다"* 고 적었다 — **제품 결함 후보**로.

**둘 다 아니었고, 둘 다 맞았다.**

| | 실측 |
|---|---|
| 하네스가 말한 것 | **틀렸다.** 차감은 자리 확정 **뒤**가 맞다. 소스 순서는 08-30 이래 한 번도 안 틀렸다. |
| 왜 빨갰나 | **하네스가 낡았다.** `f1659af9`(원장 승격)가 `player.inventory[it] -= amt` 를 `consumeItem(...)` 으로 바꿨는데 하네스는 **그 문자열**을 찾고 있었다. `indexOf` 가 −1 을 냈고, `-1 > iPlace` 가 거짓이라 **"순서가 틀렸다"** 는 문구로 빨개졌다. |
| 그런데 §0-ⓒ(실서버 재현)가 | **진짜를 찾았다.** 같은 함수가 **실행하면 존 서버를 죽인다.** |

### ★★진짜 결함 — `lifeRequestPlayerSite` 만 lazy 가드가 없었다

`villages.js` 의 생활층은 `village-layout` 을 **lazy require** 한다(`시뮬 off면 sim 모듈 무로드` 계약).
읽기 전에 `_lifeVL()` 를 불러야 하고, **NPC 쪽 진입점 셋은 전부 부르고 있었다**:
`_lifeHeadlessDay` · `_lifeDaily` · `npcLifeTick`. 그중 하나엔 이런 주석까지 달려 있다 —
*"lazy 로드 방어(단독 호출 경로 — `5e8d5f5` 'is not defined' 클래스 재발 금지)"*.

**플레이어 쪽 진입점 `lifeRequestPlayerSite` 는 나중에 생겼고(`12a2229a`), 그 가드를 못 물려받았다.**

```
ws.on('message')            ← try/catch 없음
 └ handlePlayerInput
    └ tryRequestVillageHouse   ← 재료 검사 통과(재료가 있는 사람만 여기 온다)
       └ lifeRequestPlayerSite
          └ VillageLayout.HALL_CLEAR   ← null
             → TypeError → process.on('uncaughtException') → process.exit(1)
```

**실측(자식 프로세스 · 실소켓 · 실메시지):**

```
→ 지급·텔레포트·집 의뢰를 한 묶음으로 @ 14032 78032
되돌아온 안내: 🌀 텔레포트 → (14032,78032)          ← 의뢰 응답이 없다
존 프로세스: ★죽었다 (code=1)
  uncaughtException: Cannot read properties of null (reading 'HALL_CLEAR')
    at Object.lifeRequestPlayerSite (server/villages.js:3540:60)
```

**창의 길이도 쟀다: 부팅 뒤 3분 넘게 계속 열려 있다**(180초 폴링 내내 THROW).
`npcLifeTick` 은 **활성 NPC**만 돈다 ⇒ 아무도 근처에 없는 마을은 lazy 가 안 채워진다.
`_lifeDaily` 는 게임일 경계(24분)라 그전까지는 열려 있다.
★그리고 **재료 검사가 방패였다** — 재료 없는 사람은 그 앞에서 거절돼 못 닿는다.
집을 지으려고 기둥·서까래·이엉을 다 모아 온 사람만 존을 내린다.

### 수리 — 가드를 하나 더 다는 대신 **잊을 수 있는 이름을 없앴다**

```js
let _vlMod = null;                                    // 이름이 바뀌었다
const _lifeVL = () => _vlMod || (_vlMod = require('./village-layout'));
```
생활층의 `VillageLayout.X` **18곳을 전부 `_lifeVL().X`** 로 바꿨다.
⇒ 이제 **맨 이름이 없다.** 새 진입점을 추가하며 가드를 잊는 일이 **구조적으로 불가능**하다.
(`5e8d5f5` 의 `is not defined` 도, 이번 `null` 도 같은 족이다. 한 번은 가드로, 이번엔 이름으로 닫았다.)

⚠**`villages.js` 는 econ 이다.** 변경은 **리네임뿐**이고 그 증명은 3시드 기준선이다 —
`6,280 / 5,993 / 6,072 · 소멸 0/51` **불변**.

### 곁다리 — 건축 비용의 **지불 5줄 / 환불 3줄** (`zone.js`)

같은 족을 전수 수색하다 찾았다. `_tryBuildAt` 은 `plank·wood·stone·seed·fiber` 다섯을 깎는데,
계단 실패 롤백은 **`plank·wood·stone` 셋만** 되돌린다. 오늘 안 터지는 이유는
**계단 비용이 판자뿐**이기 때문이다 — 즉 *다른 곳의 우연*에 기댄 안전이다.
⇒ `_buildCostPay(player, cost, ±1)` 하나가 **부호만 바꿔 같은 표를 두 번 걷는다.**

### 다음 세션이 지킬 것

* **lazy 모듈은 접근자로만 읽어라.** `villages.js` 생활층에 `village-layout` 의 맨 이름은 이제 **없다** —
  다시 만들지 마라(`test-psite-server ⑨` 가 소스를 훑어 지킨다).
* **비용은 지불과 환불이 같은 표를 걷게 해라**(`_buildCostPay`). 한쪽만 늘리지 마라.
* **`handlePlayerInput` 은 문자열을 받는다**(`JSON.parse(raw.toString())`). 하네스가 객체를 넘기면
  파싱이 조용히 실패해 **아무 일도 안 일어난다** — 개발 중 실제로 밟았다(족보 (57)).
* ⚠**플레이어 메시지에서 던지면 존이 죽는다**(`uncaughtException → process.exit(1)`).
  그 방침 자체는 재민 확정("여기서 삼키면 반쯤 죽은 서버가 남는다 — 더 나쁘다")이라 **안 건드렸다** —
  대신 **던질 수 있는 자리를 없애는 쪽**으로 고쳤다. 회부는 `회부_지게_다음층` 이 아니라 아래 §회부.

### 회부 (구현 안 함)

* **플레이어 입력 예외의 폭발 반경** — 지금은 어떤 메시지 핸들러의 어떤 `TypeError` 든 존 전체를 내린다.
  "반쯤 죽은 서버가 더 나쁘다"는 방침과 "한 사람의 클릭이 모두를 내린다"는 사실이 부딪친다.
  중간 지대(입력 핸들러만 잡고 **크게 로그 남기고** 그 플레이어에게만 알린다)는 **재민 판정**이다.
* **다른 진입점의 lazy 감사** — `PathCore`(읽는 자리에서 가드 · 안전) · `Events`(`state.ledger` 게이트로 간접 가드 ·
  안전)까지 확인했다. `village-layout` 만 이 족이었다. 새 lazy 를 만들면 **접근자만** 두어라.

---

## T14. ★★2026-09-01 — **임수(臨水) 항목 신설 + 마을 샘 5곳** (좌표 무이동)

배산임수 감사가 **임수를 한 번도 재지 않고 있었다.** 51/51 은 그 항목이 없어서 나온 수였다.

### ★왜 안 재고 있었나 — 판정이 떨어질 줄을 몰랐다

감사 ④ 식수는 반경 안의 `water_pool` **개체**를 센다. 그런데 그 개체는 마을 둠벙만이 아니다:
`chunk.pickResourceType` 이 **모든 biome 에서 굴림 상위 1~5%** 로 야생 물웅덩이를 깐다
(plains 0.99↑ · forest 0.98↑ · mountain 0.98↑ …). 그래서 지도 어디에 마을이 있든 960px 안에
웅덩이 하나쯤은 걸린다 — **강에서 10.8km 떨어진 광산1 이 "식수 합격"** 이었다.
`인계/공통.md` §2 가 금지한 **자명 통과**다.

★그리고 **처방 스크립트가 같은 셈을 쓰고 있어서** `plan-village-forage.js` 도 그 다섯 마을을
"물 있음"으로 읽고 통째로 건너뛰었다. **감사와 처방이 같이 눈이 멀어 있었다.**

### 무엇이 생겼나

| | 내용 |
|---|---|
| **판정 정본** | **`scripts/imsu-core.js`**(신규) — 감사·처방·하네스 **셋이 같은 함수**를 부른다. 임수 = ⓐ 지형 민물(`terrain.isWaterCellLocal` = 강·호수) 또는 ⓑ **마을 샘**(`groves` 의 물 군락). **야생 웅덩이는 증거가 아니다.** 바다도 아니다(술어는 T3 자염과 같은 식 — 해안선 띠 ∧ ¬민물) |
| **감사** | `audit-village-forage.js` 에 **⑤ 임수** 항목 추가. ④ 식수는 **그대로 둔다**(뜻이 다르다 — 마실 것이 있는가 vs 마을이 물가에 섰는가) |
| **처방** | `plan-village-forage.js` 의 부족 판정이 식수 → **임수**로. 샘 5개 추가 |
| **하네스** | **`scripts/test-imsu.js`**(신규 · `// @regress`) 10/0 |

### 실측 — 전/후

```
  임수 판정(도보 15초 = 960px)      전 46/51  →  후 51/51
  물까지 도보 시간 분포             전 ≤5초 19 · 5~10초 5 · 10~15초 22 · >15초 5
                                    후 ≤5초 19 · 5~10초 5 · 10~15초 27 · >15초 0
  민물에 **닿지 못하는** 마을        전 3곳(광산1·임업4·농촌10 — 4,000px 안에 강·호수 0)  →  후 0곳
  60초(3,840px) 안에 민물이 없는 곳   7곳 = 광산1 · 임업4 · 농촌10 · 광산3 · 임업1 · 농촌9 · 농촌15
                                    (`회부_배산임수_다음층.md` B-2 가 센 그 7곳 — 이 배치가 재확인)
```

★**7곳과 5곳의 관계**: 60초 위반 7곳 중 **4곳**(광산3·임업1·농촌9·농촌15)은 2026-08-29 배치가
이미 둠벙을 놔 줬다. 남은 **3곳**(광산1·임업4·농촌10)이 야생 웅덩이 때문에 처방에서 빠졌고,
거기에 **농촌12·농촌13**(민물 2,272 / 2,112px · 마을 샘 없음)이 더해져 **다섯**이다.
⇒ 샘 5개로 **두 집합이 한꺼번에 닫힌다**.

### 처방 표 — 사유와 조치

| 마을 | 민물 | 야생 웅덩이 | 바다 | 사유 | 조치 |
|---|---|---|---|---|---|
| 광산1 | **>4,000px** | 232px | 없음 | 강·호수가 아예 안 닿는다. 야생 웅덩이가 감사·처방을 둘 다 가렸다 | 샘 @840px |
| 임업4 | **>4,000px** | 696px | 없음 | 동상 | 샘 @840px |
| 농촌10 | **>4,000px** | 821px | 없음 | 동상 | 샘 @840px |
| 농촌12 | 2,272px | 558px | **416px** | ★**바다는 416px 인데 민물이 2.3km** — 여기 사람은 짠물을 마신다(`회부.md` 0-염-3) | 샘 @840px |
| 농촌13 | 2,112px | 929px | 없음 | 민물이 도보 33초 | 샘 @840px |

★**마을 좌표는 한 점도 안 옮겼다.** 원칙은 "마을을 물로"가 아니라 **"물을 마을로"** 이고,
좌표 이동은 econ 시딩·교역로·세이브를 전부 건드린다(T36 · ECON 웨이브 회부).
증거: 3시드 800일 **6,280 / 5,993 / 6,072 · 소멸 0/51** — 한 자리도 안 움직였다.

### ★★고치면서 드러난 결함 둘 (둘 다 이 배치가 닫았다)

**ⓐ `plan-village-forage.js` 는 멱등이 아니었다.** 머리말이 *"다시 돌리면 같은 결과가 나온다"* 고
적어 두었는데, `measure()` 가 **자기가 지난번에 심은 군락까지 세기 때문에**(청크 생성기가 groves 를
실체화한다) 두 번째 실행은 "부족한 마을 0곳"을 보고한다. **실측: 손대기 전 상태에서 그냥 돌리니 군락 0개**
— `--apply` 했으면 **48개가 통째로 사라졌다.**
⇒ 측정을 **자연 상태**(groves 를 걷어 낸 세계)에서 하게 고쳤다(`withoutGroves`). 이제 두 번 돌려도 53개다.
⇒ 그리고 **줄어들면 멈추는 가드**를 넣었다(`--allow-shrink` 없이는 종료).

**ⓑ 군락 배열은 순서가 계약이다.** `chunk.js` 가 개체의 `seedKey` 를 **배열 인덱스**로 만든다
(`gv<gi>_<i>`) 그리고 `harvested_seeds` 가 그 문자열로 "이건 이미 캤다"를 기억한다.
⇒ 군락 하나를 중간에 끼우면 **뒤의 모든 seedKey 가 밀린다** — 캤던 덤불이 되살아나고 안 캔 덤불이 사라진다.
**실측: 그냥 다시 쓰면 옛 군락 48개 전부 인덱스가 밀렸고 그중 28개가 채집 대상**(덤불·자갈밭 = 84 seedKey).
⇒ `keepOrder()` — 옛 배열을 앞에 그대로 두고 **새것만 뒤에 붙인다** + **접두 검사**(어긋나면 종료).
   적용 결과: `옛 48개는 인덱스 그대로 · 새 5개는 뒤에 붙임 — seedKey 불변`(검증됨).

### ★샘은 민물이다 — 바다 가드 (대조군으로 물린 것을 확인)

`fits('water_pool')` 은 `ctx.isWater`(강·호수)만 보고 **해안선 띠는 안 봤다**. 그래서 바닷가 마을의
링 자리가 바다 위로 잡힐 수 있었다 — "민물 샘"이라 적고 짠물을 심는 것이다.
⇒ 중심과 반경 12방위를 `IMSU.isSea` 로 검사한다.
★**대조군**: 가드를 끄면 **어촌6 의 자리가 바다 한복판**(중심·반경 둘 다 바다)으로 잡힌다 —
가드가 실제로 무언가를 막는다는 증거다(어촌6 은 민물이 128px 라 샘이 필요 없어 실제로는 안 심는다).
심은 5곳은 가드 유무와 무관하게 같은 자리다 — **예방 장치**이고, 그렇게 보고한다.

### 어귀 링 데이터 형식 — **불변 확인**

온보딩 v2 가 어귀 링을 읽는다는 지시에 따라 확인했다: `village-layout.js` 의
`ditchRing()` 반환 형식(`{cells, gates, a, b, ao, bo, frame, …}`)과 `DITCH_GATE_HALF` 를
**한 글자도 안 고쳤다.** `groves` 배열의 항목 형식(`{name, vil, kind, center, r, n}`)도 그대로다.
★단 온보딩에 넘기는 관측 하나: **환호(어귀)는 시범 마을에만 있다**(`ditch: 0 = 시범 마을 아님`) —
"어귀 링에서 빈터 구역 산출"을 글자 그대로 쓰면 대부분 마을에서 구역이 안 나온다.
대안은 `territoryBoundary` 의 경계 링이다(`회부.md` 로 넘겼다).

---

## T41. ★2026-09-01 — 집터 자동 배치: **후보를 바꾸는 사건 전수** (성능 배치의 부산물)

`_lifeAddHouseSite` 가 매일 영토를 전수로 훑던 것을 "빈손을 기억"하게 바꾸면서, **무엇이 집터
후보를 바꾸는가**를 전수로 확정해야 했다. 그 목록이 이 절이다 — 새 상태를 만들면 여기도 갱신해라.

`_lifeSiteFilters().reject` 가 보는 상태는 여덟이고, **여섯은 줄기만 한다**(= 후보가 더 거부된다):

| 상태 | 쓰는 곳 | 방향 | 후보에 미치는 영향 |
|---|---|---|---|
| `_farmSet` | `_lifeLiveFarmTile`(개간) | ↑ | 더 거부 — 다시 찾을 이유 없음 |
| `_houseCells` | `_lifeCompleteHouse`(마을 집 완공) | ↑ | 더 거부 |
| `_granList` | `_lifeGranAdd`(곳간 증설) | ↑ | 더 거부 |
| `_ditch` | 환호 적재 | ↑ | 더 거부 |
| `_potSet` | 개간 시 `_farmSet` 으로 이동 | ↓ | strict 는 **합집합**이라 불변 · loose 는 증가 |
| `_site` | 착공/완공 | — | 사라질 땐 `_houseCells` 로 옮겨 앉아 제약이 남는다 |
| **`_terrSet`** | **`_terrGrow`(영토 확장)** | **↑** | ★**새 셀 = 새 후보** |
| **`_psite`** | **`lifeRequestPlayerSite` / 의뢰 집 완공** | **↕** | ★소멸하면 반경 18 이 **다시 열린다**(의뢰 집은 `_houseCells` 에 안 들어간다) |
| **지형 차단** | 벽·다리 설치/철거(`invalidateTradeDistances`) | ↕ | ★철거 = 차단 완화 |
| **`_potSet` 삭제만** | 의뢰 터 선점 · 집터 선점 | ↓(합집합도 ↓) | ★strict `farmAt` 이 false 로 뒤집힌다 |

★굵은 넷이 "다시 찾아라" 신호다. 앞 셋은 `lifeSiteReset`(거부 캐시까지 파기),
`_terrGrow` 만 `lifeSiteDirty`(캐시 유지 — 새 셀만 더 검사하면 된다).
규약·안전망·손잡이는 `인계/Z-존서버.md` `## T41.` 절, 근거와 실측은 `회부_T41_조각바닥_다음층.md`.

★부수 실측 — **집터를 못 찾는 마을이 50곳 중 20곳**이고, 12일간 성공이 **0** 이었다.
거부 사유의 92.3%가 `기존 집과 너무 가까움(<18)`. 즉 인구가 수용력의 0.92 를 넘어도 집이 안 는다.
그게 의도인지는 **마을 설계 판단**이라 성능 배치가 손대지 않았다(회부 §F-2).

---

## 3-c. ★2026-08-03d 배치 11 — 마을 건립 **기반 완료 · 건립 자체는 회부**

### 완료 (NPC 경제 비트 동일 실증 — `f11base` = `tbuy` 전 컬럼 일치)

| | 내용 |
|---|---|
| **출처 필드** | `createVillage(opts.founder)` — 기본 `null`(NPC 시딩) · 문자열(플레이어 창설) |
| **필멸 배선** | `if (!v.founder && _shieldT < SHIELD_DAYS)` — **플레이어 마을은 기근 보호막 미적용** |
| **소멸 지표 분리** | `소멸(NPC)` / `소멸(P)` 두 열 — `ab-summary`·`econ-regress`·랩 덤프 |
| **거리행렬 증분화** | 전쌍 15,271ms → **증분 612ms**(25배) · 결과 **완전 일치**(하네스 9/0) |
| **철 채광 주체** | 코드 무변으로 성립 실증(하네스 11/0) — 캐논 무변 + 수요 자동 형성 |
| **국고 회부 종결** | 재민 세금 캐논으로 `_cash` = **미상환 세곡 채권 장부** 재해석 |

★**필멸은 플레이어 마을에만 적용된다.** NPC 시딩 마을의 소멸 0 기준선은 그대로다 —
`founder == null` 이라 보호막 조건이 항상 참이고, 800일 3시드가 **전 컬럼 비트 동일**로 그걸 실증했다.

### ★이월 — `회부_마을건립_초기인구.md` (건립 자체가 여기서 막혔다)

배치 지시가 미리 못박은 조건에 정확히 걸렸다: *"NPC 정착 여부·인구 유입 규칙이 설계안에 없다면
그건 새 설계 판단이다 — 구현하지 말고 회부."*

`tickVillage` 첫 줄이 `if (v.npcs.length === 0) return` 이라 **인구 0 마을은 존재하지만 살아 있지 않다**
(생산·소비·교역 0, 그런데 소멸 판정에는 걸려 태어나자마자 죽은 것으로 집계된다).
그렇다고 시딩처럼 8명을 그냥 주면 설계안 §1이 지적한 "클릭 한 번에 마을이 아니라 **선물**"이 된다.

⇒ **"사람이 어디서 오는가"가 새 세계 규칙**이다. 이주는 폐지됐고 순간이동은 캐논상 금지라
그냥 만들 수 없다. 선택지 4종·추천((가) 플레이어가 시간을 들여 데려온다 — 모촌·자촌 분화 고증)과
검증 계획은 회부서에 있다.
★**막힌 것은 계약이 아니다** — 결정만 나면 건립은 터 잡기 선례(노·숯가마)를 그대로 쓴다.
**길드 재고 UI 도 함께 이월**한다(대상 마을이 0곳이라 검증할 것이 없다).

## 3-d. ★2026-08-03e 배치 12 — 마을 건립 **완성**: 식량이 사람을 부른다

재민 확정 **(마)**: *"플레이어가 농사지어서 식량 확보하면 그냥 늘어나는 거 아냐?"*
⇒ `회부_마을건립_초기인구.md` **종결**. 회부의 (가)~(라)는 전부 *새 기구를 만드는 안*이었고,
답은 **있는 기구(`tickRecovery`)를 인구 0 플레이어 마을까지 넓히는 것**이었다. 새 상수 0.

| | 내용 |
|---|---|
| **건립 계약** | `village_site` → 3단계(터 다지기·환호·굴립주) → 완공 = 마을 등록. 노·숯가마 계약(`_siteStart`/`_siteAdvance`) 그대로 + **완공 훅 한 줄**(`spec.onDone`) |
| **인구 0 창설** | `createVillage(initialPop: 0)` — 초기 부존이 전부 `initN` 배라 **자동 0**(선물 없음). ★`opts.initialPop \|\| 8` 이 0 을 8 로 바꾸던 결함 수리(기존 호출부 전부 1↑ → 비트 동일) |
| **인구 유입** | `tickRecovery` 게이트 2줄: `npcs.length===0 && !founder` 컷 유지 · 문턱 `max(1,N)×15` |
| **식량 정본 통일** | 회복 게이트가 `storage.food`(곡식)만 보던 것을 **`totalFoodEquivalent`** 로 — 실클라에서 플레이어 산출은 베리(`fruit`)라 곡식이 0 이었다. 3시드 800일 **바이트 완전 동일** |
| **빈 터 ≠ 소멸** | `_everPop` — ≥1 도달 뒤 0 이 된 마을만 `소멸(P)`. 지표 4곳(ab-summary·regress·랩·v2 CLI) 정합 |
| **곳간에 넣기** | `village_deposit` — 플레이어 인벤↔econ 재화 대응표 **한 곳**(`PV_DEPOSIT_MAP`). 환산율 1:1 **잠정·손잡이** |
| **재고 UI** | 회관 클릭 = 재고. **`_cash` 미표시**(`_` 필드 통째 제외) · 화면 재계산 0 · 권한은 `_furnaceCanUse` 그대로 |
| **건립 시 정지 제거** | 건립이 서버를 **10.5초** 멈추던 것(코스 셀 14.3만칸 지형 판정)을 기존 `distDirty` 지연 경로 + 증분으로. 코스 격자 메모는 캐러밴 A* 격자와 **같은 훅**으로 캐시(41,088ms → 1,584ms) |

**검증**: `test-village-found` 42/0 · `test-distmatrix` 9/0 · `test-frontier-iron` 11/0 ·
`test-furnace` 85/0 · wiring 실패 0 · v2 CLI 2,616·무기Q 651·소멸 0/30 ·
실지도 3시드 800일 **`f11base` = `b12base` = `b12fe`**(3,549·소멸 0·무기Q 594) ·
실클라 E2E `e2e-village.js`. 상세: `보고_마을건립과_식량유입.md`.

**실클라 E2E `e2e-village.js`: ~~29 통과 / 1 실패~~ → ★**32 통과 / 0 실패**(2026-08-03e 재민 검증 세션 수리).**

### ~~다음 배치 1순위 — 곳간음수~~ ★규명·수리·종결 (2026-08-03e 재민 검증 세션)
E2E 의 그 1 실패는 **econ `tickDecay` 의 무클램프 과잉재고 가속**이었다 — 접근자 계측(probe)으로
쓰기 지점을 스택 귀속하니 `economy-sim-v2.js tickDecay` 한 곳: rate = base×(1+excess×5) 가
**1을 넘을 수 있어** `s×(1−rate)` 가 음수가 됐다(실측 fruit 202.43 → −103.68, rate 1.512).
"econ 결백" 판정은 **면죄 하네스가 빈 마을에 재고를 안 넣어서** 생긴 오판이었다(s≤0 은 부패
루프를 건너뛴다 — "자명한 상황을 골라 조용히 통과"의 재발. ②-d 는 교역만 검사했다).
수리 2건: ① rate 클램프 0.95(부패는 있는 것보다 많이 못 썩는다 — NPC 세계 비트 동일 실증)
② **창설 대기(founder && 인구 0) 마을은 excess 가속 면제**(기본 부패만 — 첫 주민 대기 비축은
글럿이 아니라 바로 그 용도다. 재민 확정 (마)의 성립 조건). 하네스 ②-e 가 두 갈래 다 지킨다
(면제 중 보존 · 인구 1 서면 가속 복귀 · 어느 쪽도 음수 불가). test-village-found 47/0.
계측기는 `scripts/probe-negstorage.js` 로 남긴다(곳간 쓰기 스택 귀속 — 재발 시 재사용).

### ★회부 2 (구현 안 함)
**게스트는 재접속하면 제 건물의 주인이 아니게 된다** — `anon_*` playerId 가 접속마다 새로
발급되는데 소유 판정이 playerId 대조다. 노·숯가마·사유지에 **이미** 해당하는 성질이고
배치 12 가 만든 것이 아니다. 인증 경로 전체를 건드리므로 별도 판단.

## 3-g. ★2026-08-04a 배치 15 v2 — **50마을 세계 처방전** + 라이브 마감

재민 확정 **"최종적으로는 50개가 있어야 하거든."** 재민 질문 **"마을 위치를 바꿔야 해? 아니면
비옥도를 수정해야 하나?"** → 상세는 `보고_50마을_처방전.md`.

**답: 둘 다 거의 아니다.** 후보 51곳을 전부 심어도 3시드 800일에 **생존 47 · 좀비 2 · 소멸 2**.
손이 필요한 자리는 4곳(광산6·농촌21·농촌11·농촌12)뿐이고 위치를 옮길 자리는 0곳
(51곳을 다 심어도 **영토 겹침 0쌍** — 최단 쌍 3,217px vs 겹침 문턱 2,121px).
32곳이 빠진 진짜 이유는 땅이 아니라 **선별 게이트**다: `MIN_SPACING_PX=12000`(영토 반경의 11.3배)과
`FOOD_FLOOR=2.0`. 상한 `VILLAGE_MAX=20` 은 **병목이 아니다**(20→51 로 올려도 19곳 그대로).

★**하한 2.0 은 생존을 예측하지 못한다** — 하한 미달 14곳 중 문제는 3곳(21%)뿐이고
광산1(식량 0.59·인구 34)·광산3(0.81·48)·임업4(1.73·81)는 멀쩡히 산다. 반대로 하한을 통과한
농촌12(2.52)가 좀비다. 51곳 세계는 인구 +38~88%, 주조 마을 3/19→8/51, 구리 재고 1→217(시드 42).

★**비옥도 레이어를 칠하면 시딩이 바뀐다는 건 틀렸다.** 선별 시점엔 영토가 없어
`extractLandParamsApprox` 가 비옥도 **필드**를 안 읽고 프록시(`0.4 + 물×0.9 − 바위×1.5`)를 쓴다.
선별을 움직이는 것은 **물 거리와 바위 밀도**뿐이다(칠한 비옥도는 심긴 뒤 생산에는 반영된다).

**용량 판정(2코어)**: 50곳 세계 부팅 329초 · 시딩 303초 · 거리행렬 50마을 1,225쌍 26초 ·
실물화 10,078행 · welcome 영토 102KB · RSS 207MB · tick_avg 5.2ms → **라이브 VPS 감당 가능**.
어촌6 은 주변 24셀에 뭍이 없어 시딩 스킵 — 그래서 실제 상한이 **50곳**이다.

**라이브 마감 4건**: ⑴ 레거시 33곳 끄기(`zone-config.hanbando.legacyVillages:false` — NPC 342→144,
claims 33→0, 후보 51→선별 19 공급·디듀프 무변, `scripts/cleanup-legacy.js` 신설) ·
⑵ 첫 부팅 `bld=5` 는 **결함이 아니었다**(옛 스폰이 마을에서 212셀 밖 = 빈 들판. 재시작해도 동일 — 실측) ·
⑶ 아이콘 4종(silver·gold·nickel·jade_raw, bpy 모듈 렌더가 커밋본과 **바이트 동일**) ·
⑷ central 상시 CPU 의 정체 = 프로토타입 econ 월드 2개(v1 20마을·canadia v2 7마을 `검은숲·연어강·늑대골`).
canadia 판은 소비처가 전부 `primaryZoneId==='canadia'` 뒤라 실효 0 → **지연 시작**(3.22% vs 9.02%).

### 새 손잡이(전부 미설정이 채택값)
`SEED_ALL=1`(랩 전수 시딩) · `ENABLE_CANADIA_ECON`(1 즉시 / 0 정지 / 미설정 지연) · `ENABLE_PROTO_ECON=0`

### 남은 것(재민 결정 — 보고서 §7)
(가) 시딩 게이트 완화 수치 · (나) 광산6 을 지형으로 살릴지 교역으로 둘지 · (다) 어촌6 좌표(유일한 이동 필요) ·
(라) **주기적 tick_max 수백 ms 히치 — 원인 미확정**(레거시 ON/OFF·18곳/50곳 전부에서 남. 전용 배치 필요) ·
(마) 한반도 Q키 시세 패널이 프로토타입 20마을을 보여주는 문제 · (바) 스폰 이동(50곳 전환 시점에 결정 — 실측은 끝)

## 3-h. ★2026-08-04b 배치 16 — **50마을 세계 전환** (재민 "해봐")

상세: `보고_50마을_세계_전환.md` · 라이브 절차: `라이브반영_런북_v3_50마을.md`(옛 이름 라이브반영_런북_2026-08-03.md).

**세계가 18곳 → 50곳이 됐다.** `zone-config.hanbando.seedAllVillages:true` 로 품질 게이트
(식량 하한 2.0 · 간격 12,000px · 상한)를 우회한다. **터 미달 스킵은 그대로 산다** —
어촌6 은 중심 주변 24셀에 뭍이 없어 물리적으로 못 세운다. 그래서 후보 51 → **시딩 50**.
우선순위는 **명시한 `VILLAGE_MAX` env > 존 설정 > 기본** — 하네스가 `VILLAGE_MAX=1` 로 빨리 도는
길을 남겼다. ⚠라이브 컨테이너에 그 env 가 남아 있으면 전환이 조용히 안 된다(런북 §6 에 대처).
모듈 상수 `VILLAGE_MAX` 는 안 건드렸다 — 그건 `PV_MAX`(플레이어 마을 상한)의 기본값이기도 하다.

**땅 보강 6곳** — `scripts/migrate-terrain-sw4.js`(멱등·백업·드라이런·`--revert`).
비옥도는 레이어가 아니라 지형 유도값이라(`fert = 0.12 + 0.62·exp(-dw/80) + 0.18·min(1,dr/60) + 0.08·임상`)
만질 수 있는 건 **물·바위·숲**뿐이다. 못 5개(반경 12셀·중심에서 56셀 = 영토 밖) + 숲 1개가 전부고,
`rivers·ridges·passes·ores·villages·valleys` 와 다른 존 10개는 **바이트 동일**이다.
대상은 서쪽 열 6곳 — 광산6·농촌11·농촌12·농촌21(배치 15 의 칼날 4곳) + **농촌6·농촌7**
(라이브 시드 1020 재측정에서 드러난 둘. 새 지물에서 2,184~2,912셀 밖이고 `kDbg.slot` 이
before/after 완전 동일이라 **내 보강이 망가뜨린 게 아니라 원래 물이 먼 자리**임을 실측으로 갈랐다).

**★새 기준선**: 50곳 800일 3시드 **소멸 0 · 좀비 0** · 인구 6,280(1020)/6,072(42)/5,283(7) ·
1,600일 시드 1020 **소멸 0 · 좀비 0 · 총인구 9,765 · 최소 인구 72**. v2 CLI 는 불변(2,616·651·0/30).

**스폰**: 벌말 광장 → **농촌22 광장**(중심성 1위 1,281셀 · 3시드 최소 인구 101 · 겹침 0 실측).

**랩 정합**: `econ-lab-real` 이 존 시딩 정책을 받도록 고치고 `lab-wiring-check` 에 검사를 넣었다
— 안 그러면 프로덕션 50곳 / 랩 19곳이 된다. 리팩터 비트 안전은 배치 15 덤프와 바이트 동일로 확인.

### 이번에도 계측기가 먼저 틀렸다(둘)
1. 비교 스크립트에 `최근 300일 최고 인구 ≤ 12` 라는 **자작 좀비 잣대**를 써서 "비대상 3곳이 새로
   좀비가 됐다"는 **없는 결함**을 보고했다. 정본은 `scripts/ab-summary.js` 의 **최종 인구 < 10**.
2. `test-furnace` 의 `findClearSpot` 이 파생 자리(`spot+(6,6)`)를 안 봐서, 새 못이 그 자리를 덮자
   "노·숯가마 완공 실패" 2건을 찍었다. 쓰는 범위를 다 검사하도록 고쳐 85/0.
   실클라 E2E 3종은 대기 예산 240초 → 900초(50곳 시딩으로 존 첫 부팅 ~7.6분).

### 남은 것(재민 결정 — 보고서 §6)
(가) ★**랩 51 vs 프로덕션 50** — 어촌6. zone.js 의 해안선 water strip 을 terrain.js 가 모른다.
고치면 실지도 랩 기준선이 전부 이동한다(b13/b14/b15 비교 불가) · (나) 시딩 게이트 코드는 살려 둠
(다른 존은 종전대로) · (다) 첫 부팅 1.5~2시간 — 시딩을 백그라운드/사전 굽기로 바꿀지 ·
(라) 주기적 tick_max 수백 ms 히치(배치 15 §5, 원인 미확정) · (마) Q키 시세 패널이 프로토타입 20마을


## V-쉼터. ★[T62 2026-09-03] 마을 건물 종류에 `shelter` 가 하나 늘었다

`village_buildings.type = 'shelter'` — **마을당 최대 하나**. 부팅 로드가 `vil._shelter` 로 복구하고,
`materializeVillageStructures` 가 **움집과 같은 6×4 실체**(`hut` 태그)로 세운다 ⇒ 클라 렌더 접점 0.
NPC 마을 51곳은 부팅 뒤 **백필**(`ensureShelter` · 멱등 · 도착 지점 곁), 유저 마을은 **창설자가 짓는다**.

★**6×4 실체를 세우는 코드가 이제 한 곳이다** — `_liveHut6x4` 를 `_lifeCompleteHouse` 에서 뽑아냈다.
마을 움집 완공과 쉼터가 **같은 함수**를 부른다(벽 규약이 갈릴 자리를 없앴다).
★자리 판정은 집터 필터 정본(`_lifeSiteFilters().reject`)을 그대로 부른다 — 후보 **순서만** 다르다
(집은 중심에서 가까운 순, 쉼터는 **도착 지점에서 가까운 순** = 어귀).
⚠쉼터는 `_houseCells`·`housesPx` 에 **안 넣는다**(NPC 침대 명부 = econ 입력). 대신 `reject` 에 가드 한 줄.
⚠`shelterAt`(미기후)과 이름이 닮았지만 **다른 것**이다 — 섞지 마라.


## V-경작칸. ★★[T100 4판 2026-09-07] **인당 기준 경작칸의 정본은 `LAND_NEED` 하나다** (8 → 12)

`server/village-layout.js` 안의

```js
  const LAND_NEED = 12;   // 인당 기준 경작칸(비옥 0.55 기준 — landNeedPer 가 비옥도로 조정)
```

이 **정본**이고 `API` 로 내보낸다. 밖(=`server/villages.js` · 계측기 · 하네스 · 랩)은 여기서 읽는다.

* 시딩(균형해)·개간 목표가 **같은 상수**를 본다 — `want = round(pop × landNeedPer(fert, LAND_NEED))`.
  `landNeedPer` 는 보즈럽 조방화(비옥할수록 인당 칸 **적게**)라 마을마다 4.8~30칸/인으로 갈린다.
* `server/villages.js` 에 있던 `const L_LANDNEED = 8` **사본은 지웠다** — `_landNeed() = _lifeVL().LAND_NEED`.
  정본이 12 로 가도 거기만 8 로 남던 자리였다. 하네스·`lab-wiring-check` 가 재발을 문다.
* 랩 2종(`lab/마을실험실.html`·`lab/전쟁실험실.html`)은 **손 사본**이다(village-layout 을 넣어 주는
  인라인 스크립트가 없다). `L_LAND_BASE` 와 인라인 layout 폴백 둘 다 `lab-wiring-check [T100]` 이 대조한다.
* 전쟁실험실엔 배수 토글(`setLandNeed`/`setLandMul` · 8 / 10.4 / 12칸 버튼)이 있다 — `gen()` 재호출 ·
  새 수 0 · 정본에서 배수만. `const` 로는 콘솔에서 못 덮으므로 `let` 이다(T138 §0-ⓐ).

### 왜 12 인가

T138(54판 · 8 / 10.4 / 12칸)이 모양을 먼저 보증했다: **영토 밖 0 · 집 붙음 0**(광산3 인구150 의 1% 뿐 —
표에 적고 넘어간다) · 둘레/칸이 L 커질수록 **내려간다**(리본이 굵어지지 갈라지지 않는다) ·
영토 점유 최악 59%. 그 위에서 재민이 ×1.5 를 골랐다. 나머지 반은 **칸당 산출**이 맡는다
(→ `인계/C-채집자연물.md` §3-곳간).
