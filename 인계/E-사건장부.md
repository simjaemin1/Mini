# 인계 — E 사건 장부·게시판

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.
## 9-밀도2. ★★★2026-09-05 [T94] — **재정박 2: 이번엔 문턱이 범인이었다** (완료 · 푸시 `<HASH>`)

> T63 은 *"문턱은 범인이 아니다"* 로 끝났다(늘어난 것의 ⅔가 재고였고 가격 문턱으로는 재고를 못 깎는다).
> **T73(생곡을 식량으로 계상)이 그 판정을 뒤집었다.** 이번에 늘어난 것은 가격이고, 손잡이도 가격이다.
> `EV_PRICE_UP/DOWN` **0.70 → 0.90** · `HYST` 무변(1.60) · econ `sim/` diff **0줄**.

### ⓐ T73 세계의 밀도 — 경보가 울렸다 (3시드 800일 · 실지도 51마을)

| 시드 | ㉮ 전체 | ㉯ 값 유형 | 캐논 2~3일 |
|---|---|---|---|
| 1020 | 1.82 | **1.90** | 하한 밑 |
| 7 | 1.92 | 2.01 | 가까스로 안 |
| 42 | 1.85 | **1.94** | 하한 밑 |

T63 이 심은 `ev-density ⓔ` **경보 한 줄**이 세 시드 전부에서 울렸다(㉯ 둘 · ㉮ 셋).
⇒ 처방 C(값 유형 읽기 유지 · 코드 0)만으로는 **더는 구간 안이 아니다**.

### ⓑ 왜 늘었나 — **되돌림 스위치가 답을 준다**

`T73_RAWGRAIN=0` 으로 옛 세계를 재현해 나란히 놓았다(시드 1020 · 같은 계측기).
되돌림 판은 T63 표를 **한 자도 안 틀리고** 재현한다(3383·3350·6724·6422·1016 · 합 21303 · ㉮1.92 ㉯2.01).

| 유형 | 되돌림(T73 전) | T73 | Δ |
|---|---|---|---|
| STOCK_SHORTAGE | 3,383 | 3,560 | +177 |
| STOCK_GLUT | 3,350 | 3,555 | +205 |
| PRICE_SPIKE | 6,724 | 7,033 | **+309** |
| PRICE_DROP | 6,422 | 6,927 | **+505** |
| 일 유형 | 1,016 | 986 | −30 |
| **합계** | **21,303** | **22,469** | **+1,166** |

**가격이 Δ의 70%**(+814). T63 때(재고 ⅔)와 정반대다.

★그 +814 의 정체는 **쌀·밀 두 품목**이다 — 품목 **종수는 안 늘었다**(급등 40→39종 · 급락 41→42종):

| | 급등 Δ | 급락 Δ |
|---|---|---|
| rice | **+297** | **+243** |
| wheat | **+227** | **+232** |
| (반대로 준 것) | hemp −142 · stone −106 · barley −53 | stone −91 · crab −47 · salmon −44 |

⇒ *"품목이 늘어서"가 아니라 **곳간의 쌀·밀이 식량으로 세어지자 그 둘의 값이 더 자주 뛴다**."*
T73 §3 이 예고한 그 자리다(생곡이 K 에 들어가 인구·확장이 늘었고, 곡물가가 그만큼 흔들린다).

### ⓒ 스윕과 채택 — 같은 틱 스트림 위에 장부 여덟(카오스 잡음 0)

㉯ 값 유형 밀도. `HYST` 는 **1.60 에 고정**했다 — 그건 재고 래치도 쓰는 공유 손잡이다(T63 ㊷).

| 문턱 | 1020 | 7 | 42 | 급등 | 급락 | 재고 | 일 | 게시 |
|---|---|---|---|---|---|---|---|---|
| ±70 (T63 채택) | 1.90 | 2.01 | 1.94 | 7,033 | 6,927 | 7,115 | 986 | 2,451 |
| ±80 | 1.98 | 2.11 | 2.04 | 6,764 | 6,318 | 〃 | 〃 | 〃 |
| ±85 | 2.03 | 2.16 | 2.09 | 6,628 | 5,939 | 〃 | 〃 | 〃 |
| **±90 ★채택** | **2.09** | **2.22** | **2.16** | 6,502 | 5,513 | 〃 | 〃 | 〃 |
| ±95 | 2.17 | 2.30 | 2.24 | 6,373 | 4,899 | 〃 | 〃 | 〃 |

(시드 1020 의 건수 열. 재고·일·게시 열은 **±55 이상 전 구간에서 한 자도 안 움직인다**.)

* **±85 는 여유가 0.03** — T63 이 이미 "얇다"고 경고한 폭이라 안 잡았다.
* **±95 는 두 읽기(㉮·㉯) 모두 구간 안**이라 매력이 있지만 **급락이 −29%** 다. 너무 비싸다.
* **±90 채택** — 세 시드 전부 ㉯ 구간 안(최소 여유 0.09) · 비용은 급등 −7.5% · 급락 −20.4% ·
  **재고 0 · 일 0 · 게시 0**. ㉮ 는 1.99/2.11/2.05 로 시드 하나가 0.01 모자라다(정직하게 적는다 —
  채택 읽기는 처방 C 대로 ㉯다).

### ⚠★남은 여행 거리 — **이 손잡이로는 T86 을 못 넘는다**

`dnOn = 1 − PRICE_DOWN` 이라 `DOWN ≥ 1.0` 이면 **급락이 구조적으로 0** 이다(T63 이 잰 절벽).
지금 0.90 이니 남은 거리는 **0.10**. T86(식단 예산)이 밀도를 1.44~1.46 까지 내리면 그 거리로는 모자란다.
⇒ **회부**: 다음 판은 손잡이가 아니라 *읽기(캐논의 뜻)* 나 *세계* 를 봐야 한다.

### 되돌림 — 이제 **둘**이다

```
EV_PRICE_UP=0.70 EV_PRICE_DOWN=0.70                      → T63/T73 판을 비트 동일 재현
EV_PRICE_UP=0.40 EV_PRICE_DOWN=0.40 EV_HYST=1.35         → T50 전 판을 비트 동일 재현
```

### ★관측자 증명 — 문턱을 옮겨도 econ 은 한 자도 안 움직인다

3시드 800일 여덟 수 중 **일곱**(인구·소멸·무기Q·확장셀·도구Q·보존식·생곡)과 **게시 의뢰**가 불변이고,
움직이는 것은 **일/건 열 하나**다. 구조적 증명이 더 강하다 — `ev-density` 는 **한 세계에 장부 여덟 개**를
달고 여덟 문턱을 동시에 재는데, 그 여덟 판의 econ 상태는 *같은 객체* 다(생곡 재고 포함).

### ★★후 판 [T86 착지 뒤 · 같은 카드 · 2026-09-05] — **손잡이가 끝났다**

T86(식단 예산)이 main 에 왔다(`7fe60271` · 코드 `7d08b677`). 카드 ③ 대로 **한 판 더** 쟀다.

| 시드 | ㉮ 전체 ±70 | ㉮ 전체 ±90 | ㉯ 값 ±70 | **㉯ 값 ±90(채택)** |
|---|---|---|---|---|
| 1020 | 1.52 | 1.70 | 1.58 | **1.78** |
| 7 | 1.52 | 1.70 | 1.57 | **1.77** |
| 42 | 1.49 | 1.65 | 1.54 | **1.72** |

(±70 의 ㉮ 열 1.52/1.52/1.49 는 PM 이 `인계/공통.md` 에 올린 T86 기준선과 **한 자도 안 다르다** —
계측기가 같은 세계를 보고 있다는 교차 확인이다.)

**채택은 방향이 맞다**(세 시드 전부 ±70 보다 느슨해졌다). **그런데 구간에 못 닿는다.**
시드 1020 스크린으로 손잡이 끝까지 밀어 봤다:

| 문턱 | ㉮ | ㉯ | 급등 | 급락 | 재고 |
|---|---|---|---|---|---|
| ±70 (T63) | 1.52 | 1.58 | 8,085 | 9,004 | 8,286 |
| **±90 ★채택** | **1.70** | **1.78** | 7,207 | 7,065 | 8,286 |
| ±95 | 1.77 | 1.85 | 7,041 | 6,367 | 8,286 |
| ±99 | 1.95 | **2.05** | 6,878 | **4,354** | 8,286 |
| ±90 · H2.00 | 1.71 | 1.79 | 7,207 | 7,065 | 8,103 |
| ±99 · H2.00 | 1.97 | 2.07 | 6,878 | 4,354 | 8,103 |

★**구간에 닿는 유일한 칸은 ±99 다.** 그건 `dnOn = 1 − 0.99 = 0.01` — 값이 **자기평균의 1%** 로
떨어져야 급락이라는 뜻이고, 거기서 **급락이 −52%**(9,004 → 4,354) 사라진다. 그리고 그 옆이 절벽이다
(`DOWN ≥ 1.0` 이면 급락 **구조적 0**). `HYST` 는 여기서 아무 도움이 안 된다 — **가격을 안 건드리기**
때문이다(㊶h2). ⇒ **채택은 ±90 에 둔다**(가장 싸게 가장 멀리 간 칸). 더 밀지 않는다.

> ★★**회부 — 다음 판은 손잡이가 아니다.** T86 세계를 캐논 구간(2~3일)으로 되돌리려면
> 값 유형 사건을 **−25% 쯤** 줄여야 하는데, 가격 문턱의 남은 여행 거리 전부를 써도 −13% 다.
> 남은 길은 셋뿐이다: ⓐ 캐논의 **읽기**를 다시 정한다(값 유형에서 가격을 또 가른다 — T50 이 일/값을
> 가른 그 문법) ⓑ **세계**를 본다(쌀·밀 값이 왜 이렇게 자주 뛰는지는 econ 몫) ⓒ 캐논 수를 옮긴다.
> **셋 다 재민 판정이다.** 이 카드는 손잡이가 어디서 끝나는지를 수로 적어 두는 데까지다.

### ★관측자 증명 (후 판 · T86 기준선 위)

문턱을 ±70 → ±90 으로 옮기고 3시드 800일을 다시 쟀다. **여덟 수 중 일곱과 게시 의뢰가 불변**:

| 시드 | 인구 | 소멸 | 무기Q | 확장셀 | 게시 | 도구Q | 보존식 | 마을당 일/건 |
|---|---|---|---|---|---|---|---|---|
| 1020 | 6,459 | 0/51 | 939 | 6,365 | 2,563 | 10,803.3 | 740.7 | 1.52 → **1.70** |
| 7 | 7,125 | 0/51 | 1,049 | 5,946 | 2,710 | 12,009.0 | 606.3 | 1.52 → **1.70** |
| 42 | 6,848 | 0/51 | 1,003 | 6,003 | 2,760 | 11,405.3 | 870.3 | 1.49 → **1.65** |

일곱 열 전부 `인계/공통.md` 의 T86 기준선과 **한 자도 안 다르다**. 움직인 것은 **일/건 한 열**이다.

### 하네스 — `test-events` T94 절 ㊵d~㊶h

* **㊵d·㊵e** 되돌림 ②(±70 → T63 판 비트 동일) + 자명 통과 금지.
* **㊶d** 채택(±90)이 T63 문턱보다 값 유형 밀도를 **8% 이상 느슨하게** 한다(실측 +10.0/+10.4/+11.3%).
* **㊶e** 그런데 **재고·일 유형은 한 건도 안 움직인다**(HYST 고정).
* **㊶f·㊶g** 51마을 3시드 실측 표가 캐논 구간 판정을 지고, 그 표의 채택 칸이 **코드 기본값과 같은지**를
  검사한다 — 표만 고치고 코드를 안 고치면 빨강이다.
* **㊶h** 돌연변이(가격 문턱 ±40 되돌리기 → 값 유형 ×1.41).
* **㊶h2·㊶h3 ★지시서 가정 정정** — 지시서는 돌연변이로 `HYST=1.0` 을 지목했지만 **HYST 는 가격을
  한 건도 안 건드린다**(가격 래치 해제는 `upOff = 1 + PRICE_UP×0.6` 이라 HYST 가 안 들어간다).
  HYST 가 쓰이는 자리는 **재고 래치와 `POP_DOWN`** 둘뿐이다(실측: 급등 370→370 · 부족 247→255).

### ★CI 빨강 귀속 — `test-events ㉜a` 는 **econ 결함이 아니었다** (T94 후 판)

CI(regress-unit)가 T86 세계에서 `㉜a 풍년·흉년이 저절로 난다` 를 빨갛게 잡았다(풍년 0 · 흉년 3 · 192/193).
`_yearShock` 이 T86 뒤 한쪽으로 쏠렸는지 **10시드로 쟀다**(각 5마을 600일 = 뽑기 100번):

| | 풍년 | 흉년 | 풍년 0 인 시드 |
|---|---|---|---|
| T86 켬 | 14 | 9 | 2/10 |
| 되돌림 `T86_DIET=0` | 15 | 12 | 1/10 |

**쏠리지 않았다.** 진짜 원인은 산수다 — `tickYearShock` 은 `day % 365 === 180` 에만 돌아서
5마을 600일이면 뽑기가 **딱 10번**이고 풍년 확률이 0.15 다 ⇒ **P(풍년 0) = 0.85¹⁰ = 19.7%**.
이 절은 처음부터 **5분의 1 확률로 빨간 검사**였고, T86 이 한 일은 econ 공유 `srand()` **스트림 위치를
옮긴 것**뿐이다. 그 자리에서 시드 1020 의 열 번이 하필 풍년을 한 번도 안 뽑았다.

⇒ **세션1(econ) 로 돌려보낼 것이 없다 — 표만 남긴다.** 고칠 곳은 내 영역의 하네스였다:
뜻은 그대로 두고 **표본을 넓혔다**(주 세계가 한쪽을 0 으로 내면 시드 63·64 를 얹어 합산 —
뽑기 30번이면 P(풍년 0) = 0.8%). 이제 이 절이 빨가면 그건 진짜 결함이다.

### 계측기 — 사본 하나를 걷어냈다

`scripts/t17-metrics.js` 가 `cfg: { PRICE_UP: 0.70, … }` 로 **채택 문턱의 사본**을 들고 있었다.
T94 가 채택을 옮기자 그 계측기만 옛 문턱으로 재면서 "기준선 정본"의 일/건 열을 조용히 옛 값(1.82)으로
찍었다. ⇒ 사본을 빼고 정본 기본값을 따르게 했다(되돌리려면 env 한 줄). ⚠남의 영역 계측기다 — 자인한다.

## 8-알림. ★2026-09-03 [T78] — **알림의 이모지는 경계 하나에서 걷는다 · 종류는 `kind`** (완료 · 푸시 `fbf38110`)

T71 ⓑ 판정("알림엔 유형 필드가 없다 — 이모지가 종류를 나르는 유일한 표시다")의 답.
**호출부 322곳을 고치지 않는다.** 새 파일 `server/notice.js` 와 **접점 한 줄**이 전부다:

```js
// server/zone.js  send()  — 접점 전부. 되돌리는 것도 이 한 줄이다.
const str = JSON.stringify(obj && obj.type === 'notice' ? Notice.normalize(obj) : obj);
```

* **`kind` 8종** — `village` `gather` `fishing` `craft` `board` `rescue` `combat` `dev` (+ `info`).
  실측 접두 **39종**을 "무엇에 대한 알림인가"로 접은 것이다(표는 `server/notice.js` 머리).
  모르는 이모지는 `info` — 표에 없다고 알림이 막히면 그게 더 나쁘다.
* **글자만 뺀다**: 이모지가 없던 줄은 **앞 공백까지 그대로**다(모양을 바꾸면 그건 뜻을 바꾼 것이다).
  알림이 아닌 메시지는 **통과만** 시키고, 호출부가 이미 준 `kind` 는 존중한다(영역별 원문 청소의 길).
* **경계는 말을 삼키지 않는다** — `normalize` 전체가 try/catch. 여기서 예외가 나면 `send()` 가 죽는다.
* **회귀**: `scripts/test-notice.js`(신규 · 18건 — 표·글자·합자 찌꺼기·무접촉·돌연변이·**AST 배선 검사**) +
  `e2e-events` 가 **웹소켓 프레임**으로 실화면 경로를 잰다(클라 무접촉으로 서버 것만 가려 보는 유일한 창).
* ⚠**클라는 아직 `kind` 를 안 읽는다** — 선 아이콘은 T66/T68. 오늘 화면 변화는 **이모지가 사라진 것 하나**다.

## 7-문장. ★2026-09-03 [T71] — **서버 문장에 이모지 0 · 유형은 필드가 나른다** (완료 · 푸시 `31137e00`)

E 영역이 사람에게 보내는 문장에 박힌 이모지는 **하나뿐이었다**(`winter.js` 게시판 머리줄 `🧊`) — 뺐다.
`events.js` 는 **원래 0**이다(주석에만 ⚠ 22 · ↔ 3 — 주석은 문장이 아니다).

* **회귀**: `test-events ㊸` — `acorn` 으로 **문자열 리터럴만** 보고 `\p{Extended_Pictographic}` 로 묻는다.
  정규식으로 파일을 훑으면 주석의 ⚠ 까지 세어 **없는 결함**을 보고한다(㊸b 가 그걸 대조로 못 박는다).
  돌연변이(문장 하나에 이모지 되살리기)도 같이 건다.
* **뜻은 안 줄었다**: 머리줄은 게시판에 하나뿐이고 "올겨울"이라는 말과 막대가 뜻을 나른다.
  ⚠**알림(`notice`)은 사정이 다르다** — 유형 필드가 없어서 이모지가 종류를 나르는 **유일한 표시**다.
  거기서 이모지를 빼려면 `kind` 를 먼저 실어야 한다(회부).
* 연표는 T50 이 `type` 을 실어 둔 덕에 이 문제가 없다 — **아이콘은 클라가 그 필드로 그린다**(T66).

## 6-밀도. ★★★2026-09-03 [T63] — **사건 밀도 재정박: 문턱은 범인이 아니었다** (완료 · 푸시 `b06be9c0`)

> 배경: 캐논 *"마을당 2~3일에 1건"*. 채택값 C 는 2.21 이었는데 T50 뒤 2.10, **T17 뒤 1.92~1.96**(하한 밑).
> **재민 PM 판정(지시서에 미리 박힘)**: *밀도를 맞추려고 문턱을 흔들기 전에 늘어난 사건이 무엇인지 먼저 갈라라.*
> ⇒ 갈랐더니 **문턱을 흔들 이유가 없었다.** `server/events.js` **무접촉**(문턱 한 글자도 안 바꿨다).

### 세 판 분해 — 같은 계측기 · 정본 시드 1020·7·42 · 800일

워크트리 셋(`2db68bad` T50 뒤 · `906c059c` T17 뒤 · `3ad06f2d` 지금)에 **오늘의 계측기**를 넣어 돌렸다.

| 유형 | 축 | T50 | T17 | 지금 | Δ(T50→T17) | Δ(T17→지금) |
|---|---|---|---|---|---|---|
| STOCK_SHORTAGE | 값 | 2,752 | 3,383 | 3,383 | **+631** | **0** |
| STOCK_GLUT | 값 | 2,736 | 3,350 | 3,350 | **+614** | **0** |
| PRICE_SPIKE | 값 | 6,344 | 6,724 | 6,724 | +380 | 0 |
| PRICE_DROP | 값 | 6,194 | 6,422 | 6,422 | +228 | 0 |
| 일 유형 전부 | 일 | 1,021 | 1,016 | 1,016 | **−5** | 0 |
| **합계** | | **19,455** | **21,303** | **21,303** | **+1,848** | **0** |

(시드 1020. 7·42 도 같은 모양 — 재고 둘이 Δ의 56~67%, 가격 둘이 33~44%, **일 유형 ±0**)

★**Δ(T17→지금)이 모든 유형·모든 시드에서 0** — T20 은 랩 밀도를 한 건도 안 움직인다(참여 0 인 해엔 사건 없음).

### 늘어난 재고는 **무엇**인가 — 품목까지 보면 답이 나온다

| | STOCK_SHORTAGE | STOCK_GLUT |
|---|---|---|
| ★T50 판엔 **아예 없던 품목** | 6종 **579건 = Δ의 92%** | 6종 **649건 = Δ의 106%** |
| 그 여섯 | fish · meat · tool · vegetable · fruit · salt | 〃 |
| 기존 품목 합 | 2,750 → 2,804 (**+54**) | 2,733 → 2,701 (**−32**) |

⇒ 같은 품목이 더 자주 뜬 게 **아니다**. T17(도구 흐름·소금 소비·보존식/어획)로 **곳간이 처음 움직이기 시작한
품목**이 여섯 늘었을 뿐이다. **세계에 일이 늘어난 것이지 잡음이 아니다.**

**떨림 검사도 통과** — 같은 마을·유형·품목 재발 간격(전수 5,323쌍) 중앙 **54일**, 정착 구간만 **39일**,
**2일 이하 재발 1.0~1.6%**. 800일에 고르게 집은 표본 20건은 전부 서로 다른 마을·품목의 첫 발생이었다.

### ★캐논의 두 읽기 — **수를 둘 다 남긴다**(뜻은 재민이 정한다)

| 읽기 | T50 | T17 | 지금 | 캐논 2~3일 |
|---|---|---|---|---|
| ㉮ **전체** 밀도 | 2.10 / 2.11 / 2.11 | 1.92 / 1.96 / 1.95 | 같음 | **하한 밑** |
| ㉯ **값 유형** 밀도 | **2.21 / 2.22 / 2.22** | **2.01 / 2.06 / 2.04** | 같음 | **안에 있다** |
| 일 유형만 | 40.0 / 41.9 / 42.4일 | 40.2 / 41.5 / 42.2일 | 같음 | — |

★㉯가 **채택 시점의 수(2.21/2.22/2.22)를 정확히 복원**한다. 2.21 → 2.10 의 첫 하락은
**T50 이 일 유형 일곱을 더한 것**이지 문턱이 헐거워진 게 아니었다. 일 유형은 마을당 **40일에 한 번**이라
값의 밀도를 흔들 수 없다 — 두 축을 한 눈금으로 세면 T50 이 갈라 놓은 것을 도로 붙이게 된다.

### 처방 A/B/C — **채택은 재민 · 이 커밋은 어느 것도 실행하지 않았다**

| 처방 | 무엇 | 실측된 비용 | 판정 |
|---|---|---|---|
| **A** 가격 문턱 재스윕 | ±85·H1.6 이면 ㉮ 2.05/2.10/2.09 | **급락 −16% · 급등 −6%** 가 사라진다. 부족·글럿은 **한 건도 안 준다**(3,383·3,350 고정) | 늘어난 것의 ⅔가 재고인데 **가격만 깎는다** |
| 〃 한가운데(2.5)로 | ±100 이상 | ★**`PRICE_DROP` 이 0 이 된다**(`mag < 1−DOWN` — DOWN≥1.0 이면 영영 안 걸린다). 2.1~2.9 는 **유형 하나를 죽여야 건너는 절벽** | 불가 |
| **B** 늘어난 유형의 자기 문턱 | 재고 유형에 문턱 신설 | 늘어난 건 "유형"이 아니라 **여섯 품목**(Δ의 92~106%). 그 소식은 ⓑ 가 **진실**로 판정했다. 재고 문턱 자리는 `SHORT_DAYS`인데 그건 **게시판 의뢰의 정의** | 근거 안 섬 · **새 손잡이 0** |
| **C** 캐논을 값 유형 밀도로 읽기 | 문턱 무변경 | **2.01 / 2.06 / 2.04 — 구간 안.** 코드 변경 0 | ★**기본 · 채택 대기** |

⚠**C 의 여유는 얇다** — 하한까지 0.01~0.06일. econ 이 품목을 더 늘리면 곧 넘는다.
⇒ 계측기 `ev-density ⓔ` 에 **경보 한 줄**을 심었다(구간 밖이면 스스로 ★경고 + 이 보고서를 가리킨다).

### 계측기 — 이 배치가 남긴 도구

| 스크립트 | 무엇 | 러너 |
|---|---|---|
| `scripts/ev-density.js` | ⓔ 유형 전수 + 두 읽기 + **캐논 경보** · ⓖ 품목별 · ⓕ 표본 문장·재발 간격 · `EV_SWEEP_EXTRA` 로 후보 추가 | 안 든다(계측기) |
| `scripts/lab-modules.js` | **랩 적재 추적**(`require` 후킹 · 2일 기동) — 상류(PM)와 같은 시각에 같은 것을 만들었고 **내 것을 버리고 상류 것을 썼다**(사본 금지) | 안 든다 |

```
EV_SWEEP_EXTRA="0.85:1.6,1.00:1.8"   후보를 같은 틱 스트림에 더 얹는다(기본 동작 무변경)
EV_ITEM_TYPES / EV_ITEM_N            품목별 표
EV_SAMPLE_TYPES / EV_SAMPLE_N        표본 문장(전 구간 같은 보폭) + 재발 간격
```

★**랩 적재 목록은 자란다** — `인계/공통.md` §2 의 "15개"는 지금 **16개**다(T20 이 `villages.js` 에
`require('./winter')` 를 더한 순간 늘었다). **인용하지 말고 `lab-modules.js` 로 매번 재라.**

### 하네스 — `test-events` T63 절 ㊴~㊷

* **㊴** 같은 틱 스트림 위 두 장부 **비트 동일**(A/B 에 카오스 잡음 0 인 근거).
* **㊵** 되돌림 — `EV_PRICE_UP=0.40 EV_PRICE_DOWN=0.40 EV_HYST=1.35` 가 채택 전 판을 비트 동일 재현.
  ⚠**자식 프로세스로 잰다**: `CFG` 는 모듈 적재 때 env 를 읽어서, 같은 프로세스에서 `process.env` 를
  바꿔선 아무것도 못 잰다(1차 판이 그렇게 빨갰다).
* **㊶** ㉮·㉯ 두 읽기가 같은 수에서 갈라진다.
* **㊷** ★손잡이가 **무엇을 깎는가** — 문턱 사다리에서 가격 유형은 단조로 줄고 **재고·일 유형은 불변**.
  ⓒ 의 결론이 이 성질 위에 서 있어서 회귀로 걸었다. (1차 판은 절대 밀도로 짰다가 **자명 통과**했다.)

## 5-겨울. ★★★2026-09-02 [T20] — **겨울나기 공동 프로젝트** (완료 · 푸시 `3ad06f2d`)

> 설계 정본 `설계/겨울나기_공동프로젝트_설계안.md` · 재민 판정 W-1ⓐ~W-7ⓐ.
> 새 파일 하나(`server/winter.js`)와 **접점 몇 줄**이다. econ `sim/` diff **0줄**.
> ★한 문장으로: *"가을에 촌장이 올겨울 목표를 말하고, 곡식을 내면 게시판 윗줄이 움직이고,
> 겨울 첫날에 판정이 나고, 넉넉했으면 그 해 곳간 몫이 는다."*

### §0 전제 확인 — 설계안은 T11·T50 **이전** 문서다. 다섯 자리가 뒤집혔다

| 전제 | 설계안이 적은 것 | **실물** | 처치 |
|---|---|---|---|
| ⓐ 양의 저장 자리 | `village_contrib` 열 둘 | 그 표가 **없다**(실물은 `onboarding` 단일 카운터) | 새 표 `village_winter` |
| ⓑ 시계 | (무언) | `Membership.init`·`Onboarding.init` 이 **`zoneGameDay`** 를 받고 있다 | winter 는 `econDay` 를 받는다 · 저건 **회부**(고치지 않았다) |
| ⓒ 공표 자리 | `SEASON_CHANGE` 에 실어 | 브리핑엔 유효 · **연표엔 무효**(T50 ㉝ — `meta` 열이 없다) | 브리핑용 `meta.winter` + 판정은 **자기 유형 둘** |
| ⓕ 목표 품목 | `_consEMA[res] × D` | ★**식량은 `_consEMA` 에 아예 없다** (econ 은 식단을 flow-EMA 에 폴드하지 않는다 — `economy-sim.js:456`) | `SUBSISTENCE_PER_NPC` × 인구 × D |
| ⓖ 게시판 | (패널 가정) | 패널이 아니라 **9초 토스트** · 그리는 건 `rows` 뿐 | 서버가 `head` 한 줄을 더 보내고 클라가 그 줄을 앞에 붙인다 |

★ⓕ 가 이 배치에서 제일 비쌌다. 식량이 `_consEMA` 에 없다는 건 곧 **`d.short` 가 식량에 대해
영영 안 걸린다**는 뜻이고, 그래서 **게시판엔 곡식 의뢰가 한 번도 안 뜬다**(실측 0/4 · 0/6).
`village_deliver` 는 "열린 의뢰"를 전제하므로 — 그대로 뒀다면 **곡식을 낼 방법 자체가 없었다.**
⇒ 겨울 몫은 **의뢰가 없어도 받는 갈래**를 하나 뒀다(보상 0 · `done:false` ⇒ 기여 **횟수**는 안 오른다).
`test-winter ⑦` 과 `e2e-winter ⑤` 가 그 전제("게시판에 곡식 의뢰가 없다")를 **먼저 세우고** 검사한다.

★ⓔ **헤드리스 랩엔 플레이어가 없다** ⇒ 세 갈래 판정:

| 그 해 참여 | 결과 | 왜 |
|---|---|---|
| 목표 달성 | `WINTER_KEPT` + 그 해 인출 한도 `+WINTER_BONUS` | W-6ⓐ |
| 1단위 이상 냈으나 미달 | `WINTER_SHORT`(보상 없음 · **NPC 를 굶기지 않는다** W-4ⓐ) | 손을 보탠 사람이 있었다는 건 사실이다 |
| **참여 0** | **사건 없음** | 랩 = 전 마을 참여 0 ⇒ 기준선이 **자동으로** 비트 동일 |

### 다섯 규약 (`server/winter.js` 머리에 그대로 있다)

```
① 세계를 바꾸지 않는다 — 곳간에 넣는 건 예전부터 `playerVillageDeposit` 이다. 이 파일은 세고 말할 뿐.
② 계량기는 하나다 — 기여 **횟수**(`onboarding.contrib`)를 읽지도 쓰지도 않는다. 여기서 세는 건 **양**.
   (양을 횟수에 실으면 부자가 소속을 산다 — 설계안 W-7)
③ 상수를 적지 않는다 — 365·270·95·158.4 가 이 파일에 없다. 달력은 `Events.calendarOf`,
   1인 하루치는 econ `SUBSISTENCE_PER_NPC`. `test-winter ①` 이 **소스 정규식**으로 검사한다.
④ 시계는 하나다 — `econDay`. `zoneGameDay`(벽시계 파생)는 서버가 꺼진 동안 영구히 벌어진다.
⑤ 달성 여부를 저장하지 않는다 — 그건 사건이고 연표가 갖고 있다. `bonusOf` 는 **연표에 물어본다**
   (T50 이 "처음 들어온 물건"을 연표로 되돌린 것과 같은 자리 — 파생 상태를 두 번 적으면 갈린다).
```

### 유형 표 v3 — 열다섯 (v2 열셋 + 둘)

| 유형 | 축 | mag | 원천 | 랩 800일 |
|---|---|---|---|---|
| `WINTER_KEPT` | **일** | 모인 양 ÷ 목표 N | `village_winter` 적립 | **0** (플레이어 없음) |
| `WINTER_SHORT` | **일** | 〃 | 〃 | **0** (〃) |

★둘 다 `EV_DEED_TYPES`(일 유형 = 연표 문턱 면제 · 브리핑 최우선)에는 들어가고
**`EV_DEED_FOREIGN` 에는 안 들어간다** — 옆 마을의 겨울나기는 내 연표에 적히지 않는다.
(그 마을 사람들의 일이지 세상의 일이 아니다. `e2e-chronicle ⑥` 이 그 배제를 이미 잰다.)

### 손잡이 — **새 것은 `WINTER_D` 하나**(나머지는 표시·되돌리기)

| 이름 | 기본 | 뜻 |
|---|---|---|
| `WINTER_D` | **5** | 목표 = 마을 하루치 × 이 일수 (W-1ⓐ) ★유일한 새 손잡이 · **재민 확정: 기본 5 유지, 실기 뒤 env 로 조정** |
| `WINTER_BONUS` | 2 | 달성한 해의 일일 인출 한도 가산(다음 겨울 첫날 만료) |
| `WINTER_NAMES` | 3 | 브리핑에 이름 적는 기여자 수(**연표엔 안 적는다** — ㉝ 계약) |
| `WINTER_BAR` | 10 | 머리줄 막대 칸 수(표시 전용) |
| `EV_WINTER_OFF` | 0 | **1 이면 T50 동작 정확히 재현**(공표·적립·판정·보상·머리줄 전부 no-op) |

### 접점 — 새 파일 하나 + 몇 줄

| 파일 | 무엇 |
|---|---|
| **`server/winter.js`** (신규) | 공표·적립·판정·머리줄·보너스 전부 |
| `server/events.js` | 유형 둘 + `LINES` 둘 + `SEASON_CHANGE` 가을 갈래 + `extra.winter` 수신(⑩ · `builds` 와 같은 모양) |
| `server/villages.js` | `require` · `Winter.init` · `scanDay` 에 `winter:` · `villageBoard` 에 `head:` · **`villageDeliver` 겨울 갈래** |
| `server/zone.js` | `require` · `Winter.onDeliver` 1줄(`Membership.onDeliver` 옆) · 납품 알림 `r.winter` 갈래 |
| `server/membership.js` | `limitOf` 에 `+ Winter.bonusOf(vid)` — **논리 1줄** + `vid` 를 태우는 기계적 5줄 · **[ⓑ] 한도의 밑변 = 식량 등가** |
| `server/villages.js` (ⓑ) | `playerVillageWithdrawStockFoodEq` · `_countsAsFoodEq` 둘 (econ 정본 호출 · 새 규칙 0) |
| `server/zone.js` (ⓑ) | `_memberLine` 의 곳간 몫도 같은 밑변 |
| `server/zone-local-db.js` | 표 `village_winter` 하나 + 접근자 둘 |
| `public/client/30-n-net.js` | 게시판 토스트가 `bd.head` 를 **맨 윗줄**에 붙인다(3줄 편집 · 새 패널 0) |

★**지시 예산을 넘긴 자리는 둘**이고 둘 다 위 §0 때문이다:
`villages.js` 는 "1줄씩"이 아니라 **납품 갈래 한 덩이**가 필요했고(ⓕ),
`membership.js` 는 "1줄 접점"인데 `vid` 가 `limitOf` 까지 안 내려와 **기계적 5줄**이 더 붙었다(논리는 1줄).

### 저장 — 표 하나

```sql
village_winter(zone, vid, year, player_id, qty, res)   PK(zone,vid,year,player_id)
  player_id = ''  → 그 해 **머리 행**: qty = 공표한 목표 N · res = 품목  (목표를 얼린다)
  player_id ≠ ''  → 사람 행: 그 해 그 마을에 낸 **양**
```
달성 여부·보너스는 **여기 없다**(규약 ⑤ — 연표가 정본).

### 하네스

* **신규** `scripts/test-winter.js` **59/0** — ①소스 규약(상수·시계) ②800일 계절 스윕(공표·판정 각 해 1회)
  ③**양 vs 횟수 분리**(A: 50 을 한 번 → 횟수1·양50 / B: 6 을 다섯 번 → 횟수5·양30 — **순서가 반대**)
  ④해 넘김 ⑤미달 대조군·econ 비트 동일·참여0 무사건 ⑥돌연변이(판정 두 번을 잡는가) ⑦의뢰 없이도 받는다 ⑧**한도의 밑변 = econ 식량 등가**(재민 ⓑ).
* **신규** `scripts/e2e-winter.js` **27/0** — 실클라: 공표 말풍선 → 토스트 머리줄 → 곡식 납품 →
  막대 진행 → 겨울 첫날 판정 → 연표 한 줄 → **인출 한도 1 → 3**.
* **계측기** `scripts/winter-metrics.js`(`@regress` 없음 — 하네스가 아니라 분포를 낸다).

### 대리 지표 — 실지도 51마을 800일(시드 1020)

| 지표 | 값 |
|---|---|
| 품목(ⓑ) | **`food`** — 곳간이 받고 사람이 먹는 것 중 최대(1인 하루 1). T17 뒤 후보에 보존식이 들었지만(dried_fish .04 …) food 1 이 압도적이라 **고르는 값은 안 바뀐다** |
| 목표량 N(ⓐ · D=5) | 최소 **85** · 중앙 **550** · 최대 **1,750** (D=1 이면 중앙 110) |
| 사람 손 환산(ⓓ) | 중앙 마을 550 ⇒ 한 번에 60씩이면 **10번**(혼자서). 여럿이 나누면 그만큼 준다 |
| 예상 건수(ⓒ) | 마을당 **한 해 최대 1건** ⇒ 800일 상한 102건 = 전체 사건(21,303)의 **0.48%**. 랩 실측 **0건**(참여 0) |

★**`WINTER_D` 기본 5 는 재민 실기 판정 자리다** — 중앙 마을 혼자 11번은 무거울 수 있다.
`WINTER_D=3` 이면 중앙 366(7번), `=1` 이면 122(3번). 값만 바꾸면 되고 코드는 안 바뀐다.

### ★T17 과 만나는 자리 둘 (리베이스 뒤 실측)

* **묵은 곡식은 마을이 안 받는다** — T17 `_spoiledGuardRes` 는 가장 오래된 로트가 상했으면 납품 **전체**를
  거절한다. **겨울 몫도 같다**(같은 입구를 쓰므로). 옳은 규칙이라 손대지 않았다.
* ★★**겨울 곳간엔 곡식이 없다 — 보존식으로 갈무리돼 있을 뿐이다.**
  ⇒ **[재민 확정 2026-09-03 ⓑ]** 인출 한도의 **밑변**을 곡식 한 칸이 아니라 **econ 식량 등가**로 바꿨다:

  | 자리 | 무엇 |
  |---|---|
  | `villages.playerVillageWithdrawStockFoodEq(vil)` | econ 정본 `totalFoodEquivalent` 그대로(**사본 0** · 보존식 포함) |
  | `villages._countsAsFoodEq(vil, res)` | 목록을 옮겨 적지 않고 **econ 에게 물어본다**(그 재화를 0 으로 두고 등가가 줄면 식량) |
  | `membership.withdraw` | 한도의 밑변 = `max(그 품목 재고, 식량 등가)` — **한도만** |
  | `zone._memberLine` | 패널의 "곳간 몫"도 같은 밑변(화면이 거짓말하지 않게) |

  ⚠**한도가 재고를 만들지 않는다** — 꺼내지는 양은 여전히 `playerVillageWithdraw` 가 정한다.
  ★*"겨울엔 곳간이 상자보다 못하다"는 캐논이 아니다*(T54 가 말리기를 겨울 비축으로 만든 이유가 그것) —
  그 문장은 회부에서 **내렸다**.

### 기준선 — **한 자리도 안 움직였다**(`ev-density ⓐ`)

(리베이스로 T17 이 들어와 기준선 정본이 갱신됐다 — 심판은 `인계/공통.md` 의 **새 표**다.)

| 시드 | 베이스(`12c66c1f`) | **내 트리** | diff |
|---|---|---|---|
| 1020 | 6,304 · 0/51 · 965 · 2,241 | **같음** | **0** |
| 7 | 5,740 · 0/51 · 885 · 2,151 | **같음** | **0** |
| 42 | 5,649 · 0/51 · 774 · 2,066 | **같음** | **0** |

★같은 기계에서 **베이스 워크트리를 직접 돌려** 옆에 놓은 A/B 다(인용이 아니다).

★리베이스 **전** 옛 정본(6,280/5,993/6,072 · 821/755/784 · 1,647/1,583/1,679)에서도 같은 검사를 했고
역시 diff 0 이었다 — **두 기준선 모두에서** 겨울은 랩을 안 건드린다.

이건 우연이 아니라 **ⓔ 의 귀결**이다: 랩엔 플레이어가 없다 → 참여 0 → 공표도 판정도 사건을 안 낸다.
"세계를 안 건드린다"가 설계에 박혀 있으면 기준선은 **저절로** 선다.

## 4-유형2. ★★★2026-09-02 [T50] — **사건 유형 2차: 세계의 "일"** (완료 · 푸시 `2db68bad`)

> 설계 근거: T18 회부 A-1 — *"연대기가 **값이 크게 움직인 해**로만 읽힌다."*
> 원인은 유형이 여섯이고 **전부 값의 이탈**이었다는 것. 이 배치는 새 시스템을 만들지 않는다 —
> **이미 일어나고 있는 일**에 장부가 눈을 뜬다. econ `sim/` diff 0줄 · 클라 `public/` diff 0줄.

### §0 실측 — 원천이 실재하는 후보만 등재했다 (`scripts/ev-sources.js`)

지시서의 일곱 후보를 실지도 51마을 800일 3시드에 걸어 **원천**을 쟀다(사건이 아니라 원천을).

| 후보 | 원천 | 결과 |
|---|---|---|
| 풍작/흉작 | `economy-sim-v2.js:1246 tickYearShock` → `v._yearShock` | ★**실재**. 가을 첫날 마을마다 15%/12% · 800일 23~27건 |
| 습격(가뭄·폭풍 등 날씨) | `tickWeather` → `v._weather` | ★**실재**. 일 1.5% · 537~573건 |
| 소멸 위기 | `v.npcs.length` 자체 | ★**실재**. 인구 축은 재고 축과 다르다 · 23~24건 |
| 캐러밴 습격 | `bandits.js installHooks` → econ `_raidScrum` → `tradeStats.caravansRaided` | ★**실재하되 실서버에서만**(랩엔 도적 호스트가 없다) |
| 캐러밴 실종 | 같은 사슬의 `tradersKilled` | ★**실재**(별도 유형으로 갈랐다) |
| 완공 | `villages.js _lifeCompleteHouse` · `_lifeCompleteGranary` | ★**실재하되 실서버에서만**(생활층) |
| 첫 물건 | `world.tradeLog` 도착 기록 | ★**실재**. 800일 398건 |
| 마을 소멸(확정) | — | ✗ **관측자가 없다**(인구 0 마을은 `scanDay` 가 건너뛴다) → 회부 |
| 봉쇄 | `world.events` 큐 | ✗ 이 존에서 아무도 안 채운다 → 회부 |
| 동사·부상 사망 | — | ✗ T43·T44 착지 전 · econ 인구는 죽는 이유를 모른다 → 회부 |

★**전제 뒤집힘 셋**
* **ⓐ 풍흉은 이미 있었다.** 지시서는 "수확량이 기대 대비 ±얼마면 풍작인가"를 물었지만,
  econ 은 **자기가 풍년·흉년을 선포하고 있었다**(`_yearShock`, 비옥도 ×1.3 / ×0.7, 가을~겨울 90일).
  ⇒ 장부가 다시 판정하지 않는다. **그 상태의 에지**를 읽을 뿐이고 mag 은 econ 이 실제로 곱하는 배수다.
* **ⓑ `bandit-raid.js`·`predator-bandit.js` 는 배선돼 있지 않다**(zone.js 가 require 하지 않는다 — 2파 유예).
  실체 습격은 아직 없다. 그런데 **추상 약탈은 이미 돈다** — `bandits.js` 가 econ 에 훅을 꽂고
  econ v2 가 갱과 per-entity 전투를 벌여 `tradeStats.caravansRaided/tradersKilled` 를 올린다.
  ⇒ 장부는 그 누계의 **증분**을 본다. 실체 층이 나중에 와도 이 배선은 안 바뀐다.
* **ⓒ ★sev 축은 일 유형에 눈금이 없다.** 흉년은 비옥도 0.7배 = `sev 0.36`, 가뭄 0.65 = `0.43`,
  인구 붕괴 최대 `1.99` — **일 유형은 값 문턱(2.2)을 한 번도 못 넘는다**(`test-events ㉞b` 가 잰다).
  즉 유형만 더하고 정렬을 그대로 뒀으면 **이 배치는 아무것도 안 한 것과 같았다.**

★**리베이스에서 좋은 쪽으로 뒤집힌 것 하나** — T7 때 호출자가 0 이던 `villageNews` 자리에
  **온보딩 v2 가 착지했다**: `server/onboarding.js:545` 가 로비 `/startinfo` 에서
  `ledger.recent(v.id, 1)` → `briefLine` 으로 마을마다 한 줄을 만든다.
  `recent()` 는 `visibleEvents` 의 껍데기라 **T50 의 무게 축을 그대로 탄다** —
  ⇒ 로비의 그 한 줄이 이제 값이 아니라 **일**을 말한다(클라·온보딩 무접촉).

### 정본과 규약 — 축이 둘이다

```
  ① 사건의 무게 = `heavier(a, b)` **하나**. 일이 먼저 서고, 그 안에서 sev 로 견준다.
     브리핑(`visibleEvents`)·게시판·연표가 전부 이 비교자를 쓴다 — 정렬 사본을 만들면
     그날 촌장과 연표가 다른 말을 한다.
  ② "일"의 정의는 발명이 아니라 **실측된 드묾**이다: 800일 51마을에서 값 18,434건 대 일 1,021건(5.2%).
     **드문 것은 걸러낼 필요가 없고, 흔한 것은 걸러야 한다.** 그래서 일 유형은 연표 sev 문턱을 면제받는다.
     ⚠`EV_DEED_TYPES` 에 흔한 유형을 넣으면 그 순간 연표가 그것으로 덮인다.
  ③ 이웃 마을에서 **여기까지 회자되는 일**은 따로 정한다(`EV_DEED_FOREIGN`) — 날씨·완공·첫 물건은 빠진다.
     날씨는 국지적이고 일주일이면 끝나며, 남의 집 짓기와 남의 곳간은 여기 연표에 적을 일이 아니다.
  ④ ★★**연표 문장은 `vid·day·type·item·mag` 만으로 만들어져야 한다.**
     `village_chronicle` 표는 `meta` 를 안 담기 때문이다(T18 스키마). 날씨 종류를 `meta.kind` 에
     뒀더니 재기동 뒤 연표가 전부 "날씨가 심상찮네"로 뭉개졌다 — `test-events ㉘`(결정론)이 잡았다.
     ⇒ 종류는 **`item` 에** 담는다(날씨 이름 · 완공 종류). `㉝` 이 전 유형에 대해 이 계약을 검사한다.
  ⑤ 장부는 **판정하지 않는다.** 일곱 중 여섯은 econ 이 세운 상태의 에지고, 장부가 문턱을 가진 것은
     **인구 축 하나**뿐이다 — 그것도 새 문법이 아니라 **가격 이탈과 같은 문법**(자기평균 대비 이탈).
     ⚠`bandits.js` 의 해체 판정(순감 5일·식량 3일치·잔존 55%)을 **베끼지 않았다**.
       그건 도적단 결성의 조건이고 이건 촌장이 하는 말이다 — 갈라져도 서로 무해해야 한다.
  ⑥ **"처음"은 마을의 이력이다.** 곳간만 봐선 모른다(먹어 없앤 물건도 처음이 아니다).
     기준 = 현재 곳간 ∪ **연표에 남은 지난 `FIRST_GOODS`** — 일 유형은 전부 연표에 남으므로
     `loadChronicle` 이 재기동 뒤 그 이력을 되돌린다. **새 영속 0**(도달일 미저장과 같은 규약).
  ⑦ 교역 도착은 **마을 루프보다 먼저** 본다. 도착한 물건은 이미 곳간에 들어와 있으므로,
     곳간을 "가진 적 있음"으로 적립하기 전에 물어야 "처음 들어왔다"가 성립한다(순서가 계약이다).
```

### 유형 표 v2 — 열셋

| 유형 | 축 | mag = 관측 ÷ 기준 | 원천 | 랩 800일 |
|---|---|---|---|---|
| `STOCK_SHORTAGE` | 값 | 재고 ÷ (소비EMA×5일) | econ 곳간 | 2,752 |
| `STOCK_GLUT` | 값 | 재고 ÷ (소비EMA×45일) | econ 곳간 | 2,736 |
| `PRICE_SPIKE` | 값 | 오늘가 ÷ 30일 자기평균 | econ 시세 | 6,344 |
| `PRICE_DROP` | 값 | 〃 | econ 시세 | 6,194 |
| `CARAVAN_LATE` | 값 | 지연일수 ÷ `LATE_DAYS` | 실체 캐러밴 `delayedDays` | 0 (실서버 전용) |
| `SEASON_CHANGE` | 값 | 1 | 달력 | 408 |
| `HARVEST_BOON` | **일** | 비옥도 배수(1.30) | econ `_yearShock` | 13 |
| `HARVEST_BLIGHT` | **일** | 비옥도 배수(0.70) | econ `_yearShock` | 13 |
| `WEATHER` | **일** | 배수 중 1 에서 가장 먼 것 | econ `_weather` | 573 |
| `POP_COLLAPSE` | **일** | 오늘 인구 ÷ 인구 자기평균 | `v.npcs.length` | 24 |
| `CARAVAN_RAIDED` | **일** | 그날 털린 수 ÷ `RAID_N` | `tradeStats.caravansRaided` | 0 (실서버 전용) |
| `TRADER_KILLED` | **일** | 그날 안 돌아온 수 ÷ `RAID_N` | `tradeStats.tradersKilled` | 0 (실서버 전용) |
| `BUILT` | **일** | 1 | 생활층 완공(`extra.builds`) | 0 (실서버 전용) |
| `FIRST_GOODS` | **일** | 1 | `world.tradeLog` 도착 | 398 |

### 손잡이

| 이름 | 기본 | 뜻 |
|---|---|---|
| `EV_POP_WIN` | 90 | 인구 자기평균 창(한 계절) |
| `EV_POP_DOWN` | 0.30 | 자기평균 대비 −30% → 붕괴 위기 ★채택 |
| `EV_POP_MIN` | 5 | 이 인원 미만은 판정 제외 |
| `EV_RAID_N` | 1 | 약탈·사망 mag 기준(= `LATE_DAYS` 의 자리) |
| `EV_FIRST_MIN` | 1 | 이만큼 들어와야 "처음 들어왔다" |
| `EV_DEED_TYPES` | 일 일곱 | 무게 축에서 먼저 서고 연표 sev 문턱을 면제받는 유형 |
| `EV_DEED_FOREIGN` | 풍년·흉년·붕괴·약탈·사망 | 이웃에서 **여기까지 회자되는** 일 |
| `EV_DEEDS_OFF` | 0 | **되돌릴 줄 하나** — 1 이면 T50 이전 동작이 정확히 재현된다(T7 `RUMOR_OFF` 와 같은 자리) |

★인구 축 채택 근거(`ev-sources ⓒ`): 창을 30→180 으로 여섯 배 늘려도 건수가 **15~31 안에** 머문다.
  이 신호는 드물고 뚜렷해서 손잡이가 사실상 무관하다 — 그래서 한 계절(90)·−30% 를 골랐다.

### 배선 — 접점은 다섯 줄

* **`server/events.js`** — 유형 정의·검출기(`deeds`·`firstGoods`)·문장(`LINES`)·무게(`heavier`·`isDeed`).
* **`server/villages.js`** — 완공 접점 **2줄**(`noteVillageBuilt`) + `scanDay` 에 `builds` 넘기기 1줄
  + 연표 페이로드에 `type·deed·sev` 1줄(게시판 소식이 이미 `type` 을 싣는 것과 같은 이유).
* **`server/zone.js`** — 테스트 전용 픽스처 메시지 `__e2e_village_deed` 1분기(E2E_GIVE 게이트).
* **클라 · econ · DB 스키마 — 한 줄도 안 고쳤다.** 소문(T7)·연표(T18)·게시판·브리핑이 새 유형을
  **자동으로** 받는다(유형 화이트리스트가 `server/events.js` 밖에 한 군데도 없다 — §0 에서 확인).

### 하네스

* `scripts/test-events.js` **170/0**(T18 뒤 136 → +34). T50 절은 ㉜~㊳:
  ㉜ **아무것도 심지 않고 600일** — 풍흉·날씨·첫 물건이 저절로 난다 + 랩에 없는 원천은 0건 + econ 비트 동일
     + **A/B 손잡이**(`EV_DEEDS_OFF=1` 이면 1차 여섯 유형의 건수가 켜나 끄나 같다 · ㉜f~h) ·
  ㉝ ★**모든 유형의 문장이 영속 필드만으로 재현된다**(㉘이 잡은 함정을 유형 전체에 못박음) ·
  ㉞ **무게** — 일 최대 sev 0.43 < 값 문턱 2.2 · 비교자가 일을 먼저 놓는다 · 축을 섞지 않는다 ·
  ㉟ 인구 축(에지·히스테리시스·POP_MIN) · ㊱ 약탈/사망이 **갈라진다** · ㊲ 완공 · ㊳ 첫 물건(재기동 기억).
* `scripts/e2e-rumor.js` **43/0**(35 → +8) — ⑨: A 에 **가뭄을 부르면**(사건이 아니라 날씨를 심는다)
  9일 뒤 B 게시판에 그 문장 그대로 나타난다. 소문 층은 한 줄도 안 고쳤다는 실증.
* `scripts/e2e-chronicle.js` **47/0**(39 → +8) — ⑪: 가뭄이 연표에 저절로 적히고, 그 줄의 `sev` 가
  값 문턱에 한참 못 미치는데도 실린다(문턱 면제가 화면까지 온다는 실증).

### ⚠하네스 낡은 가정 다섯 — 사건이 늘자 드러났다(전부 "제품은 옳고 검사가 성급했다")

사건 유형이 늘면 촌장이 할 말이 늘고 하루 경계가 미세하게 달라진다. 그러자 **시간을 고정 sleep 으로
가정하던 검사들**이 드러났다. 다음 세션이 같은 함정을 다시 밟지 않게 이름을 적어 둔다:

1. `e2e-rumor ④a` — **알림 마지막 넉 줄**에서 정지 문구를 찾았다(= 알림이 몇 줄 밀렸는지를 재는 검사).
   ⇒ **효과를 잰다**(얼린 뒤 실시간이 흘러도 날이 안 간다).
2. `e2e-rumor askBoard` — 고정 500ms 뒤 "마지막으로 받은 것"을 읽었다. 답이 늦으면 **직전 게시판**을
   오늘 브리핑과 견줘 "없는 사본"을 보고한다. ⇒ 칸을 비우고 **새 응답을 폴링**(T18 `askChron` 과 같은 처방).
3. `e2e-chronicle enter()` — 좌표를 받았다고 마을을 아는 게 아니다. ⇒ **근접 틱(`__evDbg`)을 기다린다.**
4. `e2e-chronicle ④` — ★**설계가 드러낸 것**: 이웃 연표에 안 오는 유형(`EV_DEED_FOREIGN` 밖)을
   골라 놓고 "며칠 뒤 온다"를 기다렸다. ⇒ **넘어오는 줄(값 유형)을 고른다.**
5. `e2e-rumor ⑥` — 고정 900ms 뒤 `__evLastBrief` 를 읽었다. 답이 늦으면 `null` 이라 ⑥은
   **빈 배열로 자명 통과**하고 ⑥a 만 빨개진다. ⇒ `askBrief` 폴링 헬퍼(위 2번과 같은 처방).
6. `e2e-village`(다른 영역) — 두 번째 플레이어에게 재고 요청을 **한 번만** 보내고 5초를 기다렸다.
   완공 사건이 하루 경계를 조금 밀자 그 한 번이 연결 앉기 전에 나가 사라졌다.
   ⇒ 폴링마다 다시 묻는다(읽기라 여러 번 물어도 무해). **이분 검사로 방아쇠를 특정했다** — 보고 참조.
* `scripts/ev-sources.js` **신규 계측기**(러너 밖) — §0 표 전부와 인구 축 스윕을 재현한다.
* `scripts/ev-density.js` — ⓑ 에 일/값 분리, ⓔ 스윕을 값 유형으로 한정, ⓕ 에 **일 대 값 비율** 추가.

---

---

## 3-온. ★★2026-09-01 배치 — **온보딩 v2**: 첫 의뢰 · 누적 기여 카운터

> 설계 정본 §9.4. 구현 정본 `server/onboarding.js`. **`events.js` 는 한 줄도 안 고쳤다**(아래 ③ 참조).

### ① 첫 의뢰 = **게시판 생성기가 실제로 낸 의뢰**에서 고른다

새 품목 축을 만들지 않았다. `Onboarding.pickFirstQuest(vid)` 는 `ledger.board(vid)` 의 행 중에서
고르고, 없으면 **null 이다**(없는 의뢰를 지어내지 않는다 — 그러면 촌장이 "모닥불 곁에서 쉬게"라고 한다).

```
  우선순위 ① 보상이 **먹을 것**인 의뢰 (§9.4 "납품 → 밥"을 econ 안에서 성립시키는 유일한 길)
           ② 잔여 비율이 큰 것(가장 급한 일)
```

★★**왜 밥을 따로 주지 않았나** — B-1 캐논("보상은 마을 실재고에서만 · 마을은 없는 걸 못 준다")
때문이다. 촌장 화덕에서 밥 한 그릇을 **만들어** 주면 그건 새 수도꼭지고, 이 층이 지켜 온 규약이 깨진다.
대신 **먹을 것으로 갚는 의뢰를 첫 의뢰로 고른다**. 실지도 51마을 실측(econ 150일):

```
  게시판이 선 마을 49/51 · 그중 **49/49 가 먹을 것으로 갚는다**(곡식·고기·열매 계열).
  ⇒ "납품 → 밥"은 100% 성립했다. 지어내지 않고도 된다.
  첫 의뢰 품목 분포(51마을): 약초 19 · 나무 18 · 돌 6 · 가죽 5 · 광석 1   ★소금 0 (T3 실측과 일치)
```

### ② 누적 기여 = **카운터 하나** (T11 이 그대로 재사용한다)

`onboarding.contrib` — **의뢰를 다 채운 횟수**(`deliverToVillage` 의 `done`). 새 축을 만들지 마라.

```
  zone.js 접점 한 줄:  tryVillageDeliver 성사 직후  Onboarding.onDeliver(player, r, vid)
    · contrib++ (done 일 때만)          ← T11 마을 소속·곳간 인출이 읽을 그 값
    · onboarding_fx  곳간 방향 이펙트 한 번(§9.4 반응 ②)
    · 1회째: 훅 대사 "며칠 일손을 보태면 마을 끝 빈터 하나 내어줌세" (목표 ③)
    · N회째(ONB_LOT_AFTER, 기본 3): 빈터 권리 + "어귀 빈터를 쓰게"
  ⚠아이템을 새로 주지 않는다. 밥은 **그 의뢰의 보상**이다.
```

### ③ ★"첫 의뢰 표지 필드"는 **안 넣었다** — 넣을 수가 없었다 (회부)

지시서 §5 는 `events.js` 에 표지 필드 1개를 허용했다. 실측해 보니 **그 필드가 화면에 못 닿는다**:
`events.js` `board()` 는 `{...r}` 로 전부 펼쳐 주지만, 그 사이에 있는
`villages.js` `villageBoard()` 가 **필드를 명시적으로 골라** 다시 담는다
(`item·remain·qty·rewItem·rewQty·line·give·take`). 새 필드는 거기서 조용히 떨어진다.

⇒ 필드를 넣으면 **아무도 안 읽는 죽은 필드**가 되므로 넣지 않았다. 대신 첫 의뢰는
**`onboarding_quest` 메시지**로 클라에 직접 간다(위 N-네트워크 ③). 게시판 행에 「★첫 의뢰」 배지를
붙이려면 `villageBoard` 의 매핑에 한 줄이 필요하다 — **그건 T 거래/E 영역 세션의 몫**이다(회부 0-온 ①).
## 3-연표. ★★★2026-09-01 [T18] — **연대기(마을 연표)** (완료 · 푸시)

> 설계 근거: `설계_게임성_사건레이어_TODO.md` §3·§10 — *"성장 저장소 = 도시·지식", "부재 = 뉴스".*
> 사건 장부는 **지금**을 보여 주지만 세계에는 역사가 쌓인다.
> T7 의 도달표 **위에** 선다(사본 0) — 연표는 **플레이어가 들은 순서**로 읽는다.

### §0 실측 — 전제 넷 중 셋이 달랐다

* **ⓐ 사건은 90일이면 잘린다.** `village_events` 는 `EV_KEEP_DAYS`(90) 로 prune 되고 링버퍼도 같다.
  ⇒ 연표는 그 표를 **볼 수 없다.** 그래서 **`village_chronicle`(신규 · prune 없음)** 을 만들었다.
* **ⓑ "게시판 패널"은 없었다.** `Shift+G` 는 패널이 아니라 **HUD 알림 한 줄**이다(`showNotice`).
  ⇒ "게시판 옆 탭"을 붙일 자리가 없어 **§8.2 사이드 패널**로 붙였다(아이콘 📜 · 제목표 · `renderSide`).
* **ⓒ 달력 정본은 `events.calendarOf(day)` 하나.** 연·계절·계절일·한 해 길이를 전부 유도한다
  (상수 0개 — econ `seasonOf` 만 본다). ⇒ 연표는 이걸로만 묶고 **클라는 받아 그리기만 한다.**
* **ⓓ ★사건 등급은 없다. 그리고 "큰 사건" 유형 자체가 없다.**
  장부의 6종은 `STOCK_SHORTAGE·STOCK_GLUT·PRICE_SPIKE·PRICE_DROP·CARAVAN_LATE·SEASON_CHANGE` 뿐이고,
  지시서가 예로 든 **캐러밴 습격·풍작·소멸 위기는 사건 유형으로 존재하지 않는다**(회부).
  있는 축은 `sev = |ln(관측÷기준)|` 하나 ⇒ **유형 화이트리스트 + 그 축의 문턱**으로 갈랐다.

### 정본과 규약

```
  ① 연표에 남는 것 = `ledger.isChronicle(ev)` = 유형 화이트리스트 ∩ sev ≥ EV_CHRON_SEV.
     ★채택 2.2 = **평소의 9배(또는 1/9)로 벗어난 일**(e^2.2 ≈ 9.0).
     `SEASON_CHANGE` 는 목록에 없다 — 계절은 사건이 아니라 **연표의 축**이다.
  ② 이웃 마을 소식은 **더 높은 문턱**(EV_CHRON_FOREIGN_SEV 3.5 ≈ 33배)을 넘어야 실린다.
     안 그러면 마을 하나의 연표에 다른 50곳의 후보가 계절당 700건씩 들어와 **세계의 극단값 목록**이 된다.
  ③ 계절 칸 상한은 **우리 마을 5 / 이웃 2 를 따로** 센다. 잘린 수는 `more` 로 **반드시 보고한다**.
  ④ 저장은 **사건 하나당 한 행**이다(마을×사건이 아니라). 누가 언제 들었는지는 **도달표가**
     결정론적으로 되돌린다 — 파생값은 저장하지 않는다(`spoil.js` 신선도와 같은 규약).
  ⑤ 캐시 키 = (마을, 연도). **지난 해는 영원히 유효하다** — 새로 도달하는 사건의 도달일은
     언제나 오늘이라 **올해 칸에만** 들어가기 때문이다. 무효는 ⓐ오늘이 바뀔 때(올해만)
     ⓑ도달표 세대가 바뀔 때(마을이 늘면 전부) 둘뿐.
  ⑥ 260px 게이트를 그대로 쓴다 — 마을의 역사는 **그 마을에 가서** 읽는다(§3.2 캐논).
```

### 손잡이

| 이름 | 기본 | 뜻 |
|---|---|---|
| `EV_CHRON_SEV` | 2.2 | 연표에 남을 문턱(우리 마을) — 9배 벗어난 일 |
| `EV_CHRON_FOREIGN_SEV` | 3.5 | 이웃 마을 소식 문턱 — 33배 벗어난 일 |
| `EV_CHRON_PER_SEASON` | 5 | 한 계절에 실을 **우리 마을** 줄 수 |
| `EV_CHRON_FOREIGN` | 2 | 한 계절에 실을 **이웃 소식** 줄 수 |
| `EV_CHRON_TYPES` | 부족·글럿·급등·급락·지연 | 유형 화이트리스트(계절 전환 제외) |

### 배선

* **`server/events.js`** — `chronicle(vid, {year, today})` · `chronicleYears(vid)` · `loadChronicle(rows)` ·
  `isChronicle(ev)` · `chronOf(vid)`. `onChronicle` 훅으로 호스트가 영속화한다.
* **`server/zone-local-db.js`** — `village_chronicle`(추가 전용 · **prune 없음** · `meta` 안 담는다).
* **`server/villages.js`** — `villageChronicle(vid, px, py, year)`(260px 게이트 · 마을 이름 붙이기 ·
  `Events.calendarOf` 를 그대로 실어 보낸다). 부팅 때 `loadChronicle` 로 전량 복구.
* **`server/zone.js`** — `village_chronicle` 메시지 ↔ `tryVillageChronicle`.
* **클라** — 신규 `public/client/65-s-chronicle.js`(패널 본체) + `index.html` 아이콘 1줄·등록 1줄 +
  `50-i-panel.js` 제목표 1줄·`renderSide` 1줄·열 때 묻기 1줄·단축키 1줄 + `30-n-net.js` 메시지 분기 1줄
  + `99-main.js` **`&& !e.shiftKey` 한 줄**(아래 단축키 절).
  ★**클라는 계산하지 않는다** — 연·계절 표기·문장·잘린 수·연도 목록이 전부 서버에서 온다.

### ⚠단축키 — `Shift+J`. 그런데 그 자리를 열려고 **한 줄을 고쳐야 했다**

`99-main.js` 의 키 체인은 글자를 `k === 'j'` 로만 검사한다(shift 를 안 가린다) ⇒ **`Shift+J` 가
그 분기까지 내려와 모닥불을 짓고 있었다.** 다른 bare-letter 도 전부 같다.
§8.2 는 사이드바 버튼마다 **단축키 병기**를 요구하고 `e2e-ui ①` 이 그걸 검사한다 —
즉 아이콘만 두는 선택지가 없다(1차 판이 그래서 `e2e-ui` 를 빨갛게 만들었다).
⇒ `99-main.js` 의 모닥불 분기에 **`&& !e.shiftKey` 한 줄**을 더해 `Shift+J` 를 연대기에 내줬다.
⚠**나머지 글자의 같은 결함은 그대로다**(`Shift+L` 은 여전히 울타리를 짓는다) — 회부(M/H 영역).

### 하네스

* `scripts/test-events.js` **136/0**(T7 뒤 112 → +24). T18 절은 ㉖~㉛:
  ㉖ **잘린 뒤에도 남는다**(링 93일치 vs 연표 547일치 · 선행 assert 넷) ·
  ㉗ 달력 정본으로 묶는다(계절 넷 · 시작일 · 길이 합 = 한 해) ·
  ㉘ **결정론 + 저장본만으로 재현**(도달일을 저장하지 않는다는 증명) ·
  ㉙ 모든 줄이 도달일 기준(지연 = 도달표 그대로 · 어제 기준이 더 길지 않다) ·
  ㉚ 우리/이웃 상한을 **따로** 지킨다 + `more` 정합 · ㉛ **지난 해 재빌드 0** · 하루 지나면 올해만 · 무효화.
* `scripts/e2e-chronicle.js` **신규 · 실클라 39/0** — 사이드바를 **실제로 눌러** 열고(§8.2 3단계),
  A 에서 사건 → 얼리고 B 로 → "아직 없다" → 녹여서 9일 뒤 나타남 → 화면에 "…일에 들었다" →
  260px 게이트 → 재접속 뒤에도 그대로 → `village_chronicle` 표에 행이 쌓임.
  ⚠이 검사는 **문턱과 상한을 낮춰** 돈다(배선을 재는 검사라 픽스처가 그 코드를 밟아야 한다).
  ★1차 판은 상한을 기본값으로 뒀다가 ⑥이 150회 폴링 끝에 실패했고 ⑤는 **같은 이유로 자명 통과**였다.
  ★★그리고 ⑦d 가 "가끔" 빨개졌다 — 원인은 부하가 아니라 **하네스가 응답을 안 기다린 것**이었다
  (`askChron` 이 고정 600ms 뒤 "마지막으로 받은 것"을 읽어, 답이 늦으면 **직전 응답**을 읽었다).
  ⇒ 이 하네스의 조회 헬퍼는 **칸을 비우고 새 응답을 폴링한다.** 같은 함정이 `e2e-rumor` 의
  복귀 브리핑에도 있었다(거긴 덮어쓰기라 판정을 **HUD 알림 기록**으로 옮겼다).
* `scripts/ev-density.js` ⓔ(문턱 스윕) · ⓕ(연표 행 수·조회 ms) — 계측 절 둘을 더했다.

---

## 3-소문. ★★★2026-09-01 [T7] — **소문 물리 전파 + 복귀 브리핑** (완료 · 푸시)

> 설계 근거: `설계_게임성_사건레이어_TODO.md` §3 — *"사건은 순간 전파되지 않는다."*
> 이건 [[durango-consistency-principle]](순간이동·소멸·추상화 금지)의 **정보판**이다.
> 클라 무접촉(`public/` diff 0줄) · econ 무수정(`sim/` diff 0줄).

### 정본은 어디인가

* **`server/rumor.js` (신규) = 도달 시각표의 정본 하나.** 여기 있는 것: 캐러밴 시계 거울 ·
  마을 그래프 Dijkstra · 행 캐시 · 무효화. **다른 곳에서 도달 일수를 계산하지 마라.**
* **`server/events.js` = 가시성 술어의 정본.** `visibleEvents(vid, {n, today, sinceDay})` **하나**가
  촌장 브리핑·게시판 소식·시작 화면 근황이 사건을 보는 **유일한 문**이다.
  `recent()` 는 이제 그 껍데기이고, `returnBrief()` 도 같은 문을 쓴다.
* **`server/villages.js` = 지리 접점 3줄.** 장부는 지형을 모른다 —
  `geo: { vids(), dist(a,b) }` 만 주입한다. 거리는 **정본 함수 `econ.villageDist`** 에 묻는다
  (지형 BFS 거리행렬 `world._distMatrix` 를 그 함수가 읽는다 · 좌표를 다시 재지 않는다).

### 규약 — 다음 세션이 반드시 지킬 것

```
  ① 가시성 = today ≥ 사건일 + 도달일수.  이 술어를 통하지 않고 ring 을 직접 읽지 마라.
  ② 도달 일수 = **econ 캐러밴 시계**.  max(1, round(거리 / 500)).  시계를 둘 만들지 마라.
     ⚠거리 단위는 **econ 좌표(셀 × 2.5)** 다. px(셀 × 32)를 넣으면 12.8배 틀린다.
       ★이 하네스 1차가 정확히 그렇게 틀렸다(“최소 64일” — 실제 5일).
  ③ 도달표는 **사건이 날 때 출발 마을당 한 번** 계산하고 캐시한다. 조회·틱 비용 0.
     마을이 늘거나 거리행렬이 다시 서면 `ledger.rumorInvalidate()` — villages.js 가 이미 부른다.
  ④ 도달 전 사건은 **없는 것과 같다.**  "소문이 퍼지는 중" 같은 메타 표시를 만들지 마라(디에게틱).
  ⑤ 직접 목격 = `delayTo(v, v) === 0`.  규칙을 따로 두지 않았다 — 술어 하나에서 나온다.
     "예전에 다른 마을에서 본 것"까지 기억하는 **플레이어별 목격 기록은 회부**다.
```

### 손잡이 (전부 env · 기본값이 채택값)

| 이름 | 기본 | 뜻 |
|---|---|---|
| `RUMOR_SPEED` | 500 | 거리 → 일수. **econ `NPC_SPEED` 의 거울**(동기 계약 · `test-events ⑲`) |
| `RUMOR_MIN_DAYS` | 1 | 이웃이라도 하루는 걸린다 |
| `RUMOR_OFF` | 0 | **1 이면 T7 이전 동작**(자기 마을만). A/B 재현용 단일 손잡이 |
| `EV_RETURN_N` | 3 | 복귀 브리핑 사건 줄 수 ⚠클라 말풍선이 `lines.slice(0,3)` 이라 3 이 상한이다 |
| `EV_RETURN_MIN_DAYS` | 1 | 부재가 이 일수 미만이면 평소 브리핑(잔소리 금지) |
| `EV_BOARD_NEWS_N` | 8 | 게시판 응답에 싣는 소식 건수 |

### 배선 — 무엇이 어디로

* `villageBrief(vid, px, py, {sinceDay})` — `sinceDay` 가 있으면 **복귀 브리핑**을 낸다.
  응답에 `returned` · `absentDays` · `heard` 가 붙는다(추가 필드 · 클라는 무시해도 그린다).
* `villageBoard(...)` — `rows`(의뢰 = **상태**, 전파 대상 아님) **+ `news`**(사건 = 전파 대상).
  `news[i] = { line, type, item, day, heard, from }` — `heard − day` 가 곧 소문이 걸어온 일수다.
* `villageNews(vid, n)` — **시작 화면 근황.** 260px 게이트가 없다(시세·재고가 아니라 촌장 말 한 줄이고,
  정보 비대칭은 도달표가 지킨다). **온보딩 v2 는 이 함수를 써라 — 술어를 다시 짜지 마라.**
  ⚠지금은 **호출부가 0곳**이다(`zone.js` 에 메시지 핸들러를 안 만들었다) — 라이브에 새 창구가 생기지 않았다.
* `zone.js` — `player.lastSeenDay`(게임일)를 `tools_json` blob 에 얹는다(**스키마 무변경**).
  `tryVillageBrief` 가 **접속당 한 번** 그 값을 소비한다(`player._returnBriefDone`).
  ★**`_takeover`(몸 승계) 때 `lastSeenDay` 도 같이 넘긴다** — 안 넘기면 클라가 입장 직후 한 번
  재접속할 때 기준일이 **오늘로 덮인 저장본**에서 다시 읽혀 부재가 통째로 사라진다.
  (`e2e-rumor` 가 실측으로 잡았다: 26일 비웠는데 촌장이 "5일 만이군".)

### ⚠온보딩 v2 와의 접점 — **합칠 것**

이 배치의 베이스(`66ec6f99`)에는 온보딩 v2 가 **아직 없었다**(`events.js` 에 "첫 의뢰 표지" 필드 0).
그래서 복귀 브리핑을 **기존 브리핑 진입점**(`zone.js tryVillageBrief`)에 얹었다.
온보딩 v2 의 "촌장이 먼저 말을 건다" 훅이 착지하면 **그 훅과 이 진입점을 하나로 합쳐라** —
촌장이 말을 거는 자리가 둘이면 "누가 먼저 말하나"를 관리하게 된다.
무엇을 말할지는 **서버(장부)가 정한다**는 계약은 그대로 두면 된다.

### 하네스

* `scripts/test-events.js` **112/0**(종전 71 → +41). T7 절은 ⑲~㉕:
  ⑲ **econ 캐러밴 시계 동기 계약**(자기 검사가 아니라 econ 이 실제로 띄운 캐러밴과 대조) ·
  ⑳ 결정론·즉시·대칭·거리 단조·**다단 전파 = 최단 경로**(일렬 배치에서 직행 4일 vs 징검다리 3일) ·
  ㉑ 도달 전 **어느 문으로도** 안 보임 + 도달일에 정확히 보임 · ㉒ 문 하나(recent = 술어의 껍데기) ·
  ㉓ 복귀 브리핑(창·상한·무발동·중복 0) · ㉔ **조회 600회에 그래프 걷기 0회** · ㉕ 랩 경로 = T7 이전.
* `scripts/e2e-rumor.js` **신규 · 실클라 35/0**. A 에서 사건 → **얼리고** B 로 건너가 "아직 없다" →
  **녹여서** 며칠 뒤 게시판에 나타남 → 브리핑과 게시판이 같은 답 → 로그아웃/재접속 복귀 브리핑.
* `scripts/rumor-metrics.js` **계측기**(러너에 넣지 마라 · `@regress` 없음) — 실지도 51마을
  도달 지연 분포·다단 전파 승률·도달표 비용. 실서버를 안 띄우고 `__labProbe._distProbe` 로 잰다.
* ★**하네스가 두 번 틀렸다**(제품이 아니라): ⓐ px 를 econ 좌표에 넣어 "최소 64일" 헛 하한 ·
  ⓑ 사건 키에 출처 마을을 안 넣어 `CARAVAN_LATE|null|57` 이 두 마을에서 충돌 ·
  ⓒ 클라가 **게임일마다** 브리핑을 다시 청하는데(하루 0.5초) 그걸 셈에 안 넣어 브리핑과 게시판이
  서로 다른 날의 답이 됐다. 셋 다 `[[durango-harness-can-be-wrong]]` 계보다.
* ★★**그러나 러너 연속 실행의 빨간불은 부하 의존이 아니라 진짜 경합이었다**(족보 ㊾ 를 곧이곧대로
  믿었으면 놓쳤다). 클라가 입장 직후 소켓을 갈아 끼울 때 **첫 접속의 저장이 기준일을 오늘로 덮는다** —
  한가하면 승계로 메모리 값이 넘어가지만 부하가 높으면 옛 몸이 이미 지워져 승계가 안 걸린다.
  ⇒ `zone.js _lastSeenDayToSave()`: **아직 전하지 못한 부재는 저장본을 앞당기지 않는다.**
  로그아웃 때는 close 핸들러가 그날을 먼저 찍으므로 "마을에 안 들르고 로그아웃"도 옳게 센다.
  가드는 `e2e-rumor ⑦d2`(보고 부재 ≈ 실제 부재).

### ⚠기준선 불변의 **구조적** 근거

랩(`ev-density.js`·`econ-lab-real.js`)은 지형이 없어 `geo` 를 주입하지 않는다 ⇒ 도달표가 **아예 안 선다**
⇒ 남의 마을 사건은 영영 안 보이고, 자기 마을 것만 종전대로 보인다(`test-events ㉕` 가 그걸 검사한다).
의뢰(`rows`)는 **상태**라 애초에 전파 대상이 아니다 ⇒ 게시 건수 기준선도 안 움직인다.

---

## 3-z. ★★2026-08-25 배치 — **사건 레이어 토대 3종** (완료 · 3커밋, 산 아크 종결 `762939b` 위)

> 설계 정본: 레포 `설계_게임성_사건레이어_TODO.md` (§3.1~§3.3 · §10.1). 회부: `회부_사건레이어_다음층.md`.
> 착수 순서 §11 의 ②③ 을 한 배치로 묶었다.

### 무엇이 생겼나

* **`server/events.js` — 사건 장부**(신규, 단일 원천). 하루 경계 1회 판정, 사건 6종:
  `STOCK_SHORTAGE` · `STOCK_GLUT` · `PRICE_SPIKE` · `PRICE_DROP` · `CARAVAN_LATE` · `SEASON_CHANGE`.
  * **에지 트리거 + 히스테리시스** — "오늘도 소금이 없다"는 뉴스가 아니다. 거짓→참 전이 날 1건.
  * `mag` 은 전 타입 공통 **관측값÷기준값**. 심각도 정렬은 `|ln(mag)|` 하나로 전 타입 비교 가능.
  * 영속: `village_events`(마을당 90일 링버퍼) · `village_requests`(`(zone,vid,item)` UNIQUE).
* **촌장 브리핑** — 마을 중심 260px 안에 들어가면 **접근만으로** 촌장이 말을 건다(하루 한 번).
  마을 앵커 위 **말풍선**(기존 `drawSpeechBubble` 재사용) + HUD 한 줄(`#notice`). **새 패널 0.**
  자기 마을 것만 준다(전 서버 브로드캐스트 0).
* **게시판 납품 의뢰** — `Shift+G` 열람 · `Shift+N` 납품(품목은 **서버가** 고른다).
  보상은 **물물**(시세 등가 +20%). 납품은 서버 권위·원자적(잔여 초과분 거절).

### ★★이 배치의 제1 규약 — **장부는 관측자다**

econ 을 건드리지 않는다. 세 겹으로 지켰고 **실측으로 증명**했다:
같은 틱 스트림에 장부를 **0개 / 3개 / 5개** 달고 800일을 돌려 최종값을 비교 —
**인구 6280 · 소멸 0/51 · 무기Q 821 · 거래 5000 으로 전부 동일**(시드 1020).
`test-events` ⑧ 이 그걸 회귀 가드로 건다(scanDay 5회 후 econ 상태 비트 동일).

### 문턱 채택값 — 실지도 51마을 3시드(1020·7·42) 800일

장부가 관측자라 **같은 세계에 후보를 동시에 얹어** 쟀다(카오스 잡음 0 인 A/B — `scripts/ev-density.js`):

| 후보 | 마을당 몇 일에 1건 (1020 / 7 / 42) | 판정 |
|---|---|---|
| A `±40% · H1.35` | 1.94 / 1.92 / 1.92 | 목표 밖(너무 잦다) |
| B `±55% · H1.60` | 2.08 / 2.08 / 2.07 | 목표 하단 |
| **C `±70% · H1.60`** | **2.21 / 2.22 / 2.22** | **채택** (재민 목표 "2~3일에 1건" 한가운데) |

되돌리기: `EV_PRICE_UP=0.40 EV_PRICE_DOWN=0.40 EV_HYST=1.35`.
★`SHORT_DAYS`(부족 정의 = 게시판 의뢰의 정의)는 **밀도 튜닝에 쓰지 않았다** — 밀도 맞추자고
흔들면 "부족"의 뜻이 바뀐다. 밀도는 가격 문턱과 히스테리시스로만 맞췄다.

**하루 경계 비용 2.5 ms/일**(51마을) = econ 틱 **346 ms/일**의 **0.7%**.
지시의 "<1ms/일" 은 마을 19곳 가정이었다 — 19곳 환산은 0.9 ms/일이라 그 기준도 든다.

**타입 분포**(시드 1020, 채택값): 부족 2752 · 글럿 2736 · 급등 6344 · 급락 6194 · 계절 408 · 지연 0.
**편중**: 마을당 90일 보유 최다 126 · 중앙 39 · 최소 3 → 회부 B-3.

### ★기준선 — 인계의 "19마을" 은 낡았다

지시서는 "소멸 0/19 유지"를 요구했지만 **현 레포의 실지도는 51곳**이다
(존 설정 `seedAllVillages` — 배치 16 의 50곳 전환). 실측 기준선(장부 부착 전후 동일):

```
  실지도 51마을 800일 · 시드 1020 : 인구 6,280 · 소멸 0/51 · 무기Q 821 · 거래 5,000
  실지도 51마을 800일 · 시드    7 : 인구 5,993 · 소멸 0/51 · 무기Q 755
  실지도 51마을 800일 · 시드   42 : 인구 6,072 · 소멸 0/51 · 무기Q 784
```
장부는 이 값을 **한 자리도 바꾸지 않는다**(0개 부착 실행과 비트 동일).

### 하네스

* **신규** `scripts/test-events.js` **55/0** — 지시 ①~⑦ + 자체 ⑧~⑬.
  전제 assert 를 먼저 걸고(고갈 **전** 재고>문턱 / **후** <문턱), **반대 방향도** 건다
  (③c: 소비EMA>0 이면 글럿이 실제로 난다 — ③이 "아무것도 안 난다"로 자명 통과하지 않게).
* **신규** `scripts/e2e-events.js` **29/0** (실클라 Chromium) — 접근→말풍선→게시판→납품→보상 왕복.
  이 검사만 `ENABLE_VILLAGES` 를 켠다(`VILLAGE_MAX=2 · VILLAGE_DAY_MS=500` 로 부팅 단축).
* **신규** `scripts/ev-density.js` — 밀도 실측·문턱 A/B. `EV_BASE=1` 이면 장부 0개 기준선.
* `scripts/econ-lab-real.js` 에 `EV_SCAN=1` 훅(랩 복제 아님 — 같은 모듈을 그 루프에 건다).
* 회귀 전원 통과: mountain **29/0** · mtcorridor **15/0** · mtocc **13/13** · nature **73/0** ·
  rooms **55/0** · cutaway **15/0** · metallurgy **20/0** · village **32/0** · guest **40/0**.
* ★**산 아크 종결(`762939b`) 위로 리베이스한 뒤 교차 재검증**(둘 다 `public/client.js` 를 건드렸다):
  test-events **55/0** · e2e-events **29/0**(내 쪽 클라 경로) · e2e-mtocc **17/17** · e2e-mountain **29/0**
  (산 아크 쪽 클라 경로). 텍스트 병합이 깨끗했다는 것과 **동작이 멀쩡하다는 것은 다른 말**이라 실제로 돌려 봤다.

### ⚠다음 세션이 먼저 알아야 할 것 2건 (내 배치와 무관)

1. **`e2e-terrain` 이 이 컨테이너에서 29/3 이다 — 그러나 코드 회귀가 아니다. 환경이다.**
   **서로 다른 코드 위에서 세 판**을 돌렸는데 셋 다 29/3 이고 실패 항목·수치까지
   **한 자리도 안 틀리고 동일**했다(4.7% · 0.811 · 0.05%):
   ① 내 배치 얹은 판 ② 깨끗한 `origin/main`(8efa007) 별도 worktree
   ③ **산 아크 종결 tip(762939b) + 내 배치** — 즉 병행 세션이 **32/0 을 보고한 바로 그 코드**.
   ⇒ 코드 갈래로는 설명이 안 된다. 실패 3건이 전부 **SwiftShader 화소 비율** 판정이고
     이 컨테이너 playwright 는 **1.56.0**(캐논 핀 **1.56.1**)이다. 베이크 누락도 아니다
     (`public/assets/terrain` 커밋돼 있음).
   ⇒ **다음 세션이 할 일**: playwright 를 **1.56.1 로 맞추고 재측정**. 그때 32/0 이면 환경으로 종결,
     그래도 29/3 이면 그때가 진짜 규명 시작이다. **지금 "회귀"라고 적으면 그게 추정이다.**
2. **`pngjs` 가 `package.json` 에 없었다.** 새로 클론해 `npm install` 하면
   mountain·terrain·nature·mtocc·rooms·cutaway **6종이 조용히 크래시**한다(MODULE_NOT_FOUND).
   이번에 의존성에 추가했다. — 하네스가 "실패"가 아니라 **크래시**로 죽으면 러너가 못 알아본다.
   (실제로 내 1차 러너가 `tail` 의 종료코드를 읽어 **전부 통과로 오독**했다. 러너도 하네스다.)

### 남은 회부 — `회부_사건레이어_다음층.md`

A(지시가 명시): 소문 물리 전파 · 마을 연대기 · 복귀 브리핑 · 플레이어 의뢰 게시 · 도적/포식자 · NPC 잡담.
B(실측으로 새로 나옴): **B-1 보상 수량**(`나무 1 → 가죽 55~64` 실측 — 손잡이 `EV_REQ_REW_CAP` 는 넣었고 기본 OFF) ·
B-2 시딩 중 접속하면 마을 목록이 빈다 · B-3 사건 밀도 편중 · B-4 CARAVAN_LATE 는 실서버 전용 ·
B-5 검출기 상태 비영속 · **B-6 게스트 입장 직후 재접속이 워프·초기 상태를 씻는다**(원시 WS 프로브로
서버·클라 각각 결백 확인 + pid 변경까지 잡았다 — 온보딩 §9 에 직결) · B-7 하네스 고정 포트 충돌.

---

