# durango-mini 인계 배치 5 (2026-08-02d) — 구조 부채 2건 + 마감 5건

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md`(부트스트랩·절대 규칙·검증
> 원칙)를 읽고 그대로 따른 뒤**, 아래를 순서대로 전부 진행. 질문 금지, 실측 없는 보고 금지,
> 커밋 즉시 푸시. 기준: `c2e4d51` 이후 — 시작 시 git log 로 그 뒤 커밋 먼저 읽기.
>
> 배치 4 는 재민 검증 통과(소멸 0/19·좀비 0 재현 확인·음성 대조 확인). 이 배치는 그때 등재한
> 구조 부채 2건(⑧⑨)과 회부 없이 진행 가능한 마감 작업이다. **재민 결정 대기 항목(하단)은
> 절대 선제 구현하지 마라.**

## 불변 규칙 (요약 — 전문은 다음세션_인계.md)

- 순간이동 계열(강제소개 포함) 금지 · 이주(tickMigration) 폐지 유지.
- "npc는 자잘광산은 평생 못 봐" — minor 플래그 승격 금지 (캐논).
- 라이브(fly) 배포·DB 는 재민만. force push 금지. PAT 값 채팅·레포·메모리 기록 금지.
- 수요는 절대 시대로 자르지 않는다. NPC 로직은 랩 verbatim 이식만.
- A/B 는 3시드 최소. 손잡이는 기본 OFF 로 넣고 A/B 후 채택. 채택 기준선:
  실지도 3시드 800일 **소멸 0/19 · 좀비 0 · 인구 ~3,415** / v2 CLI 5시드 **소멸 0/30 · 인구 2,079**.
  **이 두 기준선을 깨는 변경은 어떤 것도 채택 불가.**

## 작업

### ① day365 기아사망 보호막 — 구조 교체 (미해결 ⑧, 최우선)
`day < 365` 달력 클램프가 dP<0 을 통째로 삼킨다. 지금은 원인(석재→도구 사슬)을 고쳐 절벽이
안 생기지만, **"보호막이 걷히는 날 누적 압력이 한꺼번에 터지는" 형태 자체가 시한폭탄**이다
(배치 4 음성 대조에서 실측: 해제 직후 40일에 54→5).
- 먼저 궤적부터: 음성 대조(`STONE_NET=0 PRODK_CAP=0`)로 절벽을 재현해 dP 가 어디서 눌리고
  어디서 터지는지 계측 덤프로 남겨라(손잡이 만들기 전에 궤적 — 배치 4 교훈).
- 교체안 후보(측정으로 고를 것, 임의 채택 금지):
  (a) 달력이 아니라 **마을 나이** 기준(신생촌 보호라는 원래 의도에 부합하는지 코드 고고학 먼저),
  (b) 삼키지 말고 **감쇠**(보호기간엔 dP<0 을 ×k 로 줄여 압력이 새어나가게 — 절벽 대신 경사),
  (c) 보호를 원인 조건(도구·식량 흐름)에 연동.
- 손잡이 기본 OFF. 채택 기준: 기준선 2종 무파손 **그리고** 음성 대조에서 day365 절벽이
  경사로 바뀌는 것(사망이 분산되는 궤적을 보고서에 그림/표로).

### ② 석재 sink 규명 (미해결 ⑨)
광산2 가 20일에 석재 1,280 을 잃는 구간 — `STONE_MAINT_PC`(0.02/인/일)로는 27밖에 설명 안 된다.
증상이 사라졌다고 원인이 사라진 게 아니다.
- econ-lab-real 에 **석재 원장(ledger)** 계측: 생산 · 석공 소비 · 건축/유지 · 수출 · 감가 ·
  기타(잔차)를 일 단위로 분해. 잔차가 0 이어야 정상이다.
- 유력 용의자(예단 말고 원장으로 확인): 교역 대량 수출(정상일 수 있음) · kg/단위 불일치 ·
  이중 차감 · CHUNK 반올림. **정상 수출이면 문서화하고 종결**, 버그면 3시드 A/B 로 수정.

### ③ `era.canTame` 배선
`breeding:true` 13종이 시대와 무관하게 먹이 3번에 길들여진다 — `era.canTame` 호출부 **0곳**.
era.js "송국리기엔 기마 없음" 의도가 코드로 안 지켜지고 있다.
- `tryFeed` 에 게이트 한 줄(말 계열만 막히고 개·돼지 등 기존 가축은 그대로여야 한다 —
  UNLOCK 테이블 기준, 하드코딩 금지).
- `test-tame.js` 신설: 현 시대 가축 OK · 말 차단 · `ERA_FLIP` 리허설로 열면 허용.
- 막힐 때 클라 메시지도 한 줄("아직 길들일 수 없는 짐승이다" 류 — 시대 노출 금지, 몰입 유지).

### ④ 노·숯가마 조업 마감
- **노 조업 시간**: 지금 클릭 1회 = 즉시 제련. 움집·노 건설과 같은 진척도 계약(자원 투입 →
  시간 경과)으로 맞추는 게 자연스러운지 `설계_플레이어_NPC_집건설_해석안.md` 진척 공식으로 검토.
  붙이면 test-furnace 에 진척 케이스 추가. **계약이 안 맞으면 이월하고 회부**(반쪽 구현 금지).
- **숯가마 배치 조업**: 1회 3통나무 클릭 반복이 지루하다 — "가득 채우기" 1회 액션.
- 클라 반영 시 실화면 검증(⑥과 묶어서).

### ⑤ `iron` 라벨 정합
`specialty.RESOURCES.iron.ko = '철광석'` 인데 econ 의 `iron` 은 **제련된 금속**이다.
NPC 시장 UI 가 금속을 "철광석"이라 부른다. 참조 전수 조사 → 표시만 바뀌는 것 확인 후 수정
(광석 개념은 `iron_ore` 정광이 이미 따로 있다). grep 만 믿지 말고 시장 UI 실렌더 확인.

### ⑥ 플레이어 야금 사슬 실클라 E2E
서버 함수 E2E(56/56)는 됐지만 **실화면 검증이 없다.** 로컬 풀스택 + Playwright(실클라)로
채광 → 선광(종류 판정 문구) → 숯가마 건설·조업 → 노 건설(개인 사유지) → 제련 → 단조까지
사람이 하듯 통과. 건축 메뉴 · 2×2 고스트 · 클릭 판정 · 인벤 표시 · 타인 접근 차단까지 확인.
스크린샷을 보고서에 남겨라. (Chromium 은 `/opt/pw-browsers/chromium`, playwright install 금지.)

### ⑦ 마무리
전 하네스 + wiring-check + v2 회귀(기준선 2,079 · 0/30) + 실지도 3시드(0/19 · 좀비 0 유지)
+ 랩 2종 재인라인 `--check` + 디바이스 mtime 가드 동기 + 종합 보고 커밋·푸시.
채택/기각/이월을 손잡이별로 표로. 남는 부채는 다음세션_인계.md 미해결 목록 갱신.

## 재민 결정 대기 — 이 배치에서 손대지 마라

1. 광산촌 시딩 특화 슬롯 (회부_구리부존과_원석적체 ①)
2. 구리 파생 투입수요 (같은 문서 ② — 채택 시 청동 희소성 설계가 바뀜)
3. 철검 위세축 실서버 반영 범위 (⑥ 설계 완료 상태)
4. 말 사역 소비처 (⑦)
5. 라이브(fly) DB 리셋 — 실행 자체가 재민 전용
6. 무기 총량 −3~8%(ALLOY_OPP 부작용) 용인 여부 — 현재 기본 ON 유지 중, 되돌리기는 `ALLOY_OPP=0`
