# durango-mini 인계 배치 20 (2026-08-06a) — 산 실장 + 타일 상태계 [재민 확정 문법]

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` 를 읽고** 아래를 순서대로
> 전부 진행. 질문 금지(설계 취향 갈림만 시안 2~3안 첨부 후 네 추천으로 진행), 실측 없는 보고 금지.
> 기준: `ff22245`(산·타일 시안 커밋 6개 포함) 이후 — git log 로 확인. 없으면 재민에게
> `bash ~/Mini/푸시_번들.command ~/Mini/미푸시_산.bundle` 실행을 요청하고 기다려라.
>
> 푸시: `git push` 시도 → 403(git proxy)이면 우회 금지, `git bundle create 미푸시_배치20.bundle
> origin/main..HEAD` → 맥 `~/Mini/` 전달 → 재민이 푸시_번들.command 실행. **커밋 즉시 번들 백업**
> — 이 프로젝트 컨테이너는 롤백 사고가 잦다(하루 2회 실측. 로컬만 믿지 마라).
> 배치 19 C 이월분(물가 술 자연화·나무 재렌더)은 **이 배치가 아니다** — 손대지 마라.

## A. 산 실장 — '장벽 세그먼트' 문법 (시안 왕복 12회로 확정)

정본 도구: `scripts/bake-mountain.py`(스프라이트 45종 베이크) · `scripts/mock-mountain.js`(배치 알고리즘 —
이식 기준). Blender 설치: `apt-get update && apt-get install -y blender`(4.0.2 실측).

1. **단위 = 8방위 벽 세그먼트.** 늘임 축이 진행 방향의 **수직**(단면이 넓은 벽 슬라이스 — 진행 방향으로
   늘이면 '가는 벽'이 된다, 실측). 돌산 `mt_G{0..7}v{0..2}` 24종 + 숲산 `mt_F{0..7}v{0..1}` 16종 +
   캡 `mt_S1/S2`(파괴 절단면·독립 노두) + 매듭 `mt_M1/M2/L1`(급커브 >24°). 앵커 JSON 규약은
   building_render 와 동일(ox/oy/ppu — 클라 `PPU_SCR/an.ppu*sc` 로 축척).
2. **배치 = ridges 폴리라인 보행 + 셀 실측.** 경로를 호길이로 걸으며(간격 = 세그먼트 실길이×0.55),
   배치점마다 **진행 수직으로 실제 바위 셀을 훑어 이어진 개수를 센다** — 그 수가 세그먼트 폭(sc).
   재민 확정: *"산으로 되어 있는 셀들만 산으로 보여야 — 셀이 여러 개 모이면 큰 산."* 고개(셀 0)는
   산 없음(사슬 절단), 파괴된 셀도 자동 반영. 높이 지터 ±14%(발치 고정, 세로 배율)·수직 지터 폭×5%.
3. **산맥별 유형**: ridge 이름→유형 표(예: 한울대간=돌산 G, 그 외=숲산 F). 재민 확정 전 기본값:
   한울대간·눈메만 돌산, 나머지 숲산 — 시안 첨부로 확인받아라. 숲산 톤이 아직 연하다(심화 숙제).
4. **파괴 = 정사각 셀 집합.** 원형 금지(재민: "한 셀이 정사각형인 거 잊었어?"). 부서진 셀은 지질
   플래그 `wasMountain` 을 얻고 §B 의 산터 램프로 그려진다. 잘린 사슬 양끝은 캡(벽보다 낮게 —
   주저앉는 전이). ★밴드 폭보다 좁은 파괴는 벽을 못 끊는다(남은 셀 위로 산이 이어짐 — 정상 동작).
5. **실장 경로**: 클라 렌더러에 세그먼트 배치(부팅 시 존 ridges + 셀 그리드로 결정론 계산 —
   서버 전송 불필요, 파괴 이벤트만 재계산 트리거) + z-정렬(renderables — 앵커 wy+wx). 자산은
   `public/assets/mountains/` 로 커밋. 성능: 배치 결과 캐시(정적) — 파괴 셀 브로드캐스트 시 국소 재계산.
6. ⚠"산을 부수는" 게임 메커니즘 자체(바위 셀 제거 이벤트)가 현재 서버에 있는지 **실측부터** 해라
   (설계_산부수기_광맥접근.md 참조 — 없으면 렌더는 이벤트 규격만 정의하고 메커니즘은 회부).

## B. 타일 상태계 — '상태 벡터 → 레이어 합성' (재민 확정)

> 재민: *"비옥도에 따라 모든 타일이 디자인이 바뀌어야… 번영도·경작·길·채굴에 따라서도."*

**축 5개** (합성 순서대로. 시안 정본: `scripts/mock-fertility-gradient.js` · `mock-tile-axes.js` ·
`mock-fertility-strip.js`):

| # | 축 | 상태값 | 신설? |
|---|---|---|---|
| 1 | 기반 | 바이옴 × 지질(일반토 / 산터암반 `wasMountain`) | 지질 플래그만 신설(파괴 시 세팅, DB 영속) |
| 2 | 채굴=번영도 | **기존 `mined_cells.prosperity`** (0..1000, lazy 회복 — zone-local-db.js) | 없음 — 읽기만. 채광 정본을 렌더가 절대 쓰지 마라 |
| 3 | 비옥도(식생) | 동적 토양치 0..1000 | **신설** — 초기값은 정적 지형 파생, 파기↓·리젠↑, 산 파괴 셀은 **800 에서 시작**(재민 확정) |
| 4 | 경작 | 기존 farmland 상태 | 확장만(이랑 무늬 강도) |
| 5 | 답압(길) | 기존 §16 길 마모값 | 없음 — 읽기만 |

**연속 수학 (재민: "경계마다 딱딱 나누지 말고 연속적으로")** — 전 축 공통:
- 텍스처 층 알파 = `smoothstep(문턱a, 문턱b, 값 + 셀노이즈×진폭)` — 균일 페이드가 아니라 **뙈기로
  번지는** 전이. 답압은 중심 가중(길 가운데부터 다져짐).
- 소품(자갈·이끼·풀포기·들꽃) = **자리별 고유 문턱**(해시) + 문턱 근처 ±40~90 구간에서 알파·크기
  연속 증가 — 정수 개수의 계단 제거. 어떤 값에서도 점프 없음.
- **산터 램프는 별도 축**(재민: "돌만 놓으면 그게 산터냐"): 기본 재질이 **암반**. 비옥도는
  이끼(450~)→틈새 풀(800~)만 늘리고, **풀밭이 되지 않는다**(상한).
- 바이옴×지질 테이블 구조로 — 단 **한반도 라이브는 forest 바이옴 두 축(일반/산터)만 실장**,
  나머지 8종(plains·savanna·jungle·desert·taiga·tundra·mountain·archipelago)은 테이블 자리만.
- 렌더: 지면은 청크 오프스크린 베이크(배치 19 A 의 경로) — 상태값 바뀐 청크만 재베이크.
  갱신 빈도 낮음(리젠은 게임일 단위, 답압·채굴은 이벤트). `_tileAcc` 전후 실측 보고.

**서버**: 동적 토양치 테이블 신설(셀 키 → soil, last_t — mined_cells 의 lazy 패턴 그대로 베껴라),
파기/폭파/리젠 갱신 + 브로드캐스트(청크 단위 묶음). ★`sim/fertility.js`(econ 마을 선별 정적 정본)은
**한 바이트도 건드리지 마라** — 기준선 비트 동일이 깨진다.

## C. 검증 · 보고

- 신설 `scripts/e2e-mountain.js`: ⓐ세그먼트가 실제 바위 셀 위에만(셀 없는 고개 상자에 산 픽셀 0)
  ⓑ폭 정합(넓은 밴드 상자 vs 좁은 밴드 상자의 산 픽셀 폭 비율) ⓒ파괴 후 재계산(셀 제거 →
  그 상자 산 픽셀 소멸 + 캡 등장) ⓓ결정론.
- 신설 `scripts/e2e-tilestate.js`: ⓐ토양치 조작 → 해당 청크만 재베이크(픽셀 변화가 그 상자에만)
  ⓑ연속성(토양치 500 vs 520 화면차가 문턱 점프 없이 작음) ⓒ답압·경작 축 각 1판정 ⓓ산터 셀은
  토양치 1000 에서도 풀밭이 아님(초록 비율 상한 검증).
- 기존 회귀: E2E 6종(metallurgy 20·village 32·guest 40·cutaway 15·rooms 55·terrain) +
  `econ-regress.js` 비트 동일 + `econ-lab-real.js 800`(6,280·0/51) + wiring-check 0.
  ★cutaway·rooms 대조군 상자 오염 주의(판정 완화 금지, 상자 이동+주석).
- 보고 `보고_배치20_산과_타일상태.md`(머리에 재민용 `bash scripts/redeploy-hanbando.sh --central`
  한 블록 — public/ 변경 배치) + 실화면 `증거_배치20/`(산맥·고개·파괴 전후·램프 4축) +
  `다음세션_인계.md` 갱신 + 커밋·번들.

## 함정 기록 (전부 이 프로젝트에서 실제로 밟았다)

- Blender ColorRamp 는 위치 1.0 에 기본 '흰색' 요소를 갖고 태어나고, elements.new() 뒤엔 인덱스가
  위치순 재정렬이라 [1] 참조가 빗나간다 — **색으로 찾아** 교체(봉두 255 클리핑 진범, 2회 재발).
- 컨테이너 롤백 잦음 — 커밋 즉시 번들 백업, 번들 파일명은 세션별 분리(다른 세션이 미푸시.bundle 을
  덮어 커밋이 소실된 사고 실존).
- canvas 텍스처: transform(합성) ≠ setTransform(치환) · 클립 삼각형 0.8px 팽창 · UV 는 연속으로.
- GIF 디버그는 단일 팔레트(프레임별 적응 팔레트는 정지 픽셀도 깜빡임).
- 세그먼트 방위당 1종이면 아코디언 벽 — 변주 3종+높이 지터가 최소.

## 회부 (구현 금지)

- 산 부수기 게임 메커니즘(§A-6 실측 결과 없을 때) · NPC 가 파괴 지형을 경로에 반영하는 문제 ·
  다른 바이옴 8종의 램프 아트 · 번영도(마을 econ) 별도 시각 축 신설 여부 · 관개수로(배치 19 메모 참조).
