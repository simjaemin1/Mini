# durango-mini 인계 배치 15 v2 (2026-08-04a) — 마을 50개 세계 [재민 확정] + 라이브 마감

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` 를 읽고** 아래를 순서대로
> 전부 진행. 질문 금지, 실측 없는 보고 금지, 커밋 즉시 푸시. 기준: `684bff7` 이후 — git log 먼저.
>
> ★v1 에서 목표가 바뀌었다. 재민 확정(2026-08-04): **"최종적으로는 50개가 있어야 하거든"** —
> 레거시 33곳을 그냥 끄는 게 아니라, 후보 51곳 **전부를 완전체 마을로** 가져간다.
> 부유 시딩의 선별(식량 바닥 2.0)이 32곳을 걸러 18곳만 심은 상태다 — "망한" 게 아니라
> **심지 않은** 것. 이제 그 32곳을 살릴 방법을 실측으로 정한다.
> 재민의 질문이 이 배치의 산출물이다: **"마을 위치를 바꿔야 해? 아니면 비옥도를 수정해야 하나?"**
> → 마을별 처방전으로 답한다.

## 불변 규칙 (요약)

- 배포·라이브 실행은 재민만(`bash scripts/redeploy-hanbando.sh`). redeploy-all 금지(한반도 단독).
- 맵 편집은 재민 영역 — 세션은 **처방전(어디를 얼마나)까지만**. hanbando-terrain.json 의
  마을 좌표·지형을 임의 수정하지 마라(측정용 임시 오버라이드는 랩 안에서만).
- 소멸 0 원칙: NPC 시딩 마을 소멸 0 은 **재민이 (나)완화를 고르기 전까지 유지**가 기본값.
- 실화면 검증 도구: `scripts/probe-roof.js` + `/tmp/zone-wrap.js`(mainSquare 래퍼) 선례.
- CPU 2코어 — 51곳 실지도 랩은 한 판이 훨씬 무겁다. 순차·시간 계획 먼저.

## 작업

### ① 전수 시딩 실측 — 51곳 전부 심으면 누가 죽나
- 랩 손잡이(예: `SEED_ALL=1` — pickSeedVillages 의 바닥·상한 우회, 후보 51 전부. 손잡이는
  랩/env 전용, 프로덕션 기본값 무변)로 실지도 3시드 800일. 가능하면 1,600일 1시드 추가(장기).
- 산출: **마을별 결과표** — 인구 궤적 · 소멸/좀비 여부 · 죽었으면 사인(궤적 방법론: 배치 4~6
  선례 — 식량? 도구? 교역 고립?).
- ⚠51곳은 18곳보다 판이 무겁다 — 먼저 1시드로 소요 시간을 재고 계획을 조정해라.

### ② 마을별 처방전 — 재민의 에디터 작업 지시서
죽은/좀비 자리마다 원인을 땅값으로 분해해 **처방**을 적는다:
- `그대로 OK` — 새 엔진으로 버팀(수정 불필요)
- `비옥도 +x` / `물 접근` / `사냥감·나무` — 에디터에서 그 일대 지형 레이어 보강(구체 수치:
  현재 F/W/G/S 와 바닥 통과에 필요한 값의 차)
- `이동 필요` — 물·바위에 박혀 터 미달인 극단 케이스만(예상 소수)
- 처방의 근거는 `extractLandParamsApprox`(정본 함수)로 잰 실측값 — 계측기 사본 금지 원칙.
- 형식: 표 1장(51행) + 지도 위 표시(가능하면 map-editor 오버레이용 json — 디바이스 편집 규약
  안에서 map-editor.html 만 가드 편집 가능함을 기억).

### ③ 서버 용량 실측 — 51곳을 2코어가 감당하나
- 로컬 51곳 세계로 존 부팅: 시딩 총시간(라이브 외삽) · tick_avg/max · 메모리.
- 라이브 VPS(1.9GB/2코어) 기준 감당 판정. 무리면 무엇이 병목인지(실물화? econ? NPC 수?)와
  완화 수단(레거시 NPC 제거로 이미 ~200명 여유 등)을 적어라.

### ④ 라이브 마감 3건 (v1 유지분)
1. **레거시 실체화 끄기** — 51곳 완전체로 대체되므로 그대로 진행(존 플래그 게이트 · 시딩 후보
   공급과 디듀프 경로는 무변 · 라이브 DB 고아 한옥 청소 스크립트 `cleanup-legacy.js`
   드라이런 기본·플레이어 소유물 무접촉).
2. **첫 시딩 부팅 건물 미탑재 수리** — 재시작 없이 첫 부팅부터 실물화가 보이게(라이브 실측
   bld=5 결함). 검증: 새 DB 부팅 1회 → probe-roof 실화면.
3. **아이콘 4종**(silver·gold·nickel·jade_raw) + **central 옛 시뮬 정체 규명**(상시 CPU 50% ·
   "검은숲·연어강" Day 900+ 로그 — 코드로 확인, 실효 없으면 끄는 손잡이).

### ⑤ 마무리
전 하네스 + E2E 3종 + wiring-check + v2 회귀(비트 동일 — 이 배치는 프로덕션 시딩 기본값을
안 바꾼다. 50곳 전환은 재민이 처방전 반영 후 리셋할 때 일어난다) + 종합 보고 커밋·푸시.
보고 구성: ①의 생존표 → ②의 처방전(재민 에디터 작업 목록) → ③ 용량 판정 → ④ 라이브 반영
절차(redeploy-hanbando ± cleanup-legacy). **처방전이 이 배치의 최종 산출물이다.**

## 그 다음 (이 배치 범위 밖 — 순서만 적어 둔다)
재민이 에디터에서 처방 반영 → 지형 json 갱신 → `VILLAGE_MAX=50` 전환 + 라이브 리셋(런북
동형) → 50곳 세계 시작. 스폰 이동은 그 시점에 50곳 배치를 보고 정한다(v1 의 ② 이월).
