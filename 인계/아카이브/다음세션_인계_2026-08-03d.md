# durango-mini 인계 배치 11 (2026-08-03d) — 플레이어 마을 건립 1단계 [재민 "다 해"]

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md`(부트스트랩·절대 규칙·검증
> 원칙)를 읽고, `설계_플레이어_마을건립.md` 를 정독한 뒤** 아래를 순서대로 전부 진행.
> 질문 금지, 실측 없는 보고 금지, 커밋 즉시 푸시. 기준: `07f2e39` 이후 — git log 먼저.

## 재민 확정 (2026-08-03 대화 원문 — 이 배치의 전제)

1. **"플레이어 마을은 망해도 돼"** — 필멸 확정(설계안 §2 (가)).
2. **"마을(길드) 관리자가 식량 등의 마을 재고 현황을 파악할 수 있도록 ui 할 거야"** — 재고 UI 확정.
3. 세금 캐논: **"교역 세금은 물물교환할 때 드는 물건이고, 방문한 쪽이 더 낸다."**
   실물 차원은 이미 그렇게 구현돼 있고(방문자가 매출 −TAU·매수 +TAU 부담, 차액 실물이 도착
   마을 곳간에 남음), `_cash` 는 화폐가 아니라 **국고의 미상환 세곡 채권 장부**다. 이 번역으로
   국고 소비처 회부는 **종결**한다.
4. 철 채광 주체: **플레이어 노동만**(설계안 §4 추천안) — 캐논("npc는 자잘광산은 평생 못 봐") 무변.
5. **"다 해"** — 위 전부 이번 배치에.

## 불변 규칙 (요약 — 전문은 다음세션_인계.md)

- **NPC 시딩 마을의 소멸 0 기준선은 그대로 지킨다** — 필멸은 플레이어 마을에만 적용된다.
  기준선(03c): 800일 3,549·0/19·무기Q 594 / 1,600일 4,502·0/19 / 2,400일 4,916·0/19 / v2 CLI 2,616·0/30.
- 순간이동 금지 · 이주 폐지 · minor 승격 금지 · 수요 시대 절단 금지 · 시대 SCHEDULE 재민만.
- 배포·fly SSH·라이브 DB 재민만 · force push 금지 · PAT 기록 금지 · 계측기 정본 함수 호출.
- UI 를 건드리는 배치다 — **실클라 E2E 로 끝내라**(검증 원칙 02d).
- 설계 판단이 새로 필요해지면 회부. 반쪽 구현보다 이월.

## 작업

### ① 마을 건립 1단계 — 계약과 지표부터 (설계안 §1~§3 구현)
설계안의 추천 선택지를 재민이 확정했다. 순서 고정 — **기반을 먼저, 건립은 그 위에**:
1. **소멸 지표 분리** — `ab-summary`·`econ-regress`·랩 덤프에 `소멸(NPC)` 과 `소멸(플레이어)` 를
   분리. 플레이어 마을이 0곳인 지금은 두 번째 열이 0/0 — 기존 수치와 비트 동일해야 한다.
2. **교역 BFS 증분화** — 마을 1곳 추가 시 전쌍 재계산(25.5초) 대신 신규↔기존 N 회만.
   `setDistMatrix` 주입 계약 유지. 하네스: 증분 결과 = 전쌍 재계산 결과 **완전 일치**(3시드).
   이건 건립 이전에도 무해한 성능 수리다 — 단독 커밋으로.
3. **건립 계약** — 터 잡기 다단계 선례(움집·노) 그대로: 길드 사유지에 `village_site` →
   자재 투입 → 진척 → 완공 시 마을 등록(`_bornDay` = 그날, **founder/길드 기록**).
   설계안 §1 의 시딩 가정 8종 중 플레이어 건립이 깨는 것들(식량 바닥 2.0 없음 · 땅맞춤 부존
   없음 — 맨땅에서 시작)을 의도로 명문화. **NPC 정착 여부·인구 유입 규칙이 설계안에 없다면
   그건 새 설계 판단이다 — 구현하지 말고 회부**(1단계는 "마을이 존재하고 저장되고 죽을 수 있다"까지).
4. **필멸 배선** — 플레이어 마을은 `SHIELD_AGE`/`SHIELD_SOFT` **미적용**(founder 플래그 분기),
   소멸 시 정리 절차(건물·사유지 처리)는 기존 마을 소멸 코드 재사용. 소멸이 실제로 일어나는
   E2E 케이스 1개(식량 0 방치 → 소멸 → 지표에 `소멸(플레이어) 1` 집계).
5. **철 채광 주체 = 플레이어 노동** — 코드 변경이 필요 없다는 것을 **검증으로** 보여라:
   자잘 철 광맥 옆 플레이어 마을 시나리오에서 NPC 광부가 광맥을 안 보는 것(캐논 유지)과
   플레이어 채광→곳간 투입→econ 수요 형성이 되는 것. 캐논 주석에 재민 확정 인용 추가.

### ② 길드 관리자 재고 현황 UI
- 길드 관리자(마을 founder/관리 권한)에게 마을 곳간 재화 현황 표시 — 식량(합산+품목)·목재·석재·
  도구·금속 등. **`_cash` 는 표시하지 않는다**(재민 캐논 3 — 장부이지 재화가 아니다).
- 정본 데이터 재사용(zonePublicMeta 또는 기존 storage 직렬화) — UI 용 사본 계산 금지.
- NPC 마을에는 노출하지 않는다(길드 소유 마을만). 권한 게이트 E2E 포함(비관리자는 못 본다).
- 실클라 E2E: 건립 → 재고 UI 열람 → 표시값 = 서버 storage 실값 일치 assert.

### ③ 국고 회부 종결
`회부_국고_2차_소비처.md` 에 종결 머리말: 재민 캐논 3 의 번역(실물은 이미 물물·방문자 부담으로
정합, `_cash` 는 미상환 세곡 채권 장부, 무해 — UI 비표시로 종결). TREASURY_BUY 는 "국고가
채권으로 곳간에서 실물을 찾아가는 상환 행위"로 재해석해 주석 갱신(코드 무변).

### ④ 마무리
전 하네스 + 실클라 E2E(신규 케이스 포함) + wiring-check + v2 회귀 + 실지도 3시드 800일
(플레이어 마을 0곳 상태 = 기존 기준선과 **비트 동일** 확인 — 이번 배치는 NPC 경제에 손대지
않는다) + 랩 2종 `--check` + 디바이스 동기 + 종합 보고 커밋·푸시. 인계 문서 갱신.
