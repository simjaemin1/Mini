# durango-mini 인계 배치 17 (2026-08-04c) — 실내 컷어웨이·밤 어부·방황 [재민 실플레이 관측 3건]

> 새 세션 첫 메시지로 붙여넣어라. **먼저 `다음세션_인계.md` 를 읽고** 순서대로 전부 진행.
> 질문 금지, 실측 없는 보고 금지, 커밋 즉시 푸시. 기준: `f74e421` 이후 — git log 먼저.
>
> 배경: 50마을 리셋 후 재민 실플레이 관측. 전부 **실화면(스크린샷/녹화)으로 재현 → 규명 →
> 수리 → 실화면으로 증명**이 계약이다(probe-roof/probe-spawn 선례 — mainSquare 래퍼 스폰).

## 재민 원문 (이 배치의 수리 목표)

1. "건물 안으로 들어가도 내부가 안 보이네.. 저번에 동쪽 벽과 남쪽 벽이 근처로 가면
   투명해지도록 한 걸 바꾼다 했는데, 제대로 작동이 안 되네.. **좀보이드처럼 해야 하는데**"
2. "npc가 밤인데도 밖에서 낚시하고, 낚시터에서 미세하게 자꾸 방황하는데, 왜 그럴까?"

## 작업

### ① 실내 컷어웨이 수리 — 좀보이드 스타일 (최우선)
현행 계약: 움집 `data.hut`(벽·바닥 억제+지붕 합성, 안이면 컷어웨이) · 큰집 `data.bld`(입실 시
남·동벽 페이드). **재민 실화면에선 안 먹는다.**
- 먼저 재현: probe 로 움집·큰집·곳간 각각 **안에 서 있는** 실화면 → 어느 건물·어느 요소(지붕/
  남벽/동벽/실내 바닥·가구·실내 NPC)가 안 보이는지 표로.
- 유력 용의선 (실측으로 가려라): 판정이 `data` 태그 기반인데 **클라가 받는 건물 페이로드에
  data 가 실리는가**(buildings_spawn·welcome 경로 — 직렬화에서 태그가 떨어지면 페이드 게이트가
  영영 false다. 배치 5 "display:none" 결함과 같은 결: 계약은 맞는데 배선이 끊김).
- 수리 기준 = **좀보이드 감각**: 안에 들어가면 가리는 벽·지붕이 사라지고 실내(바닥·사람·물건)가
  또렷이 보인다. 밖에서는 온전한 건물. 경계(문간)에서 깜빡이지 않는다.
- e2e 확장: 입실 전/후 스크린샷 픽셀 diff(지붕 영역이 실제로 바뀌는지 assert — 자명 통과 금지).

### ② 밤 어부 + 낚시터 미세 방황 — 규명 후 수리
- 관측 계측: 밤에 밖에 있는 NPC 표본 N명의 `npcHomeX`·`simLonOff`·`behavior`·마을 로컬
  태양시(fv)·좌표 궤적을 덤프. 후보: (a) 집/태양시 미배정("집 없음" 스폰 계열) (b) §19 경도
  로컬 태양시 vs 화면 조명의 차이(그럼 버그가 아니라 설계 — 문서화) (c) 귀가 경로 막힘.
- 방황(제자리 달달거림): 궤적 덤프로 (i) stuck→unstuck 진동인지 (ii) 밤 게이트가 매 틱 목표를
  덮는 왕복인지 (iii) 의도된 일터 배회인지 판별. 진동이면 수리(예: 목표 도달 불가 시 재시도
  쿨다운), 설계면 폭·빈도 조정 여부만 회부.
- 수리 후 실화면 녹화(gif) 또는 연속 스크린샷으로 전/후 비교.

### ③ central 프로토 시뮬 마무리
라이브 central 이 여전히 상시 CPU ~100%다. 배치 15가 canadia 판은 지연 시작으로 처리했고
나머지 하나가 남았다 — **실효 여부를 코드로 판정**하고(로비/게임에 읽히는 값이 있는가),
실효 0 이면 기본 지연/OFF 전환(손잡이 `ENABLE_PROTO_ECON` 기존 것 활용), 있으면 문서화.
라이브 반영 시 `--central` 필요 여부를 보고에 명시.

### ④ 마무리
전 하네스 + E2E(확장분 포함) + wiring-check + v2 회귀(비트 동일) + 실지도 1시드 800일
(50곳 소멸 0 확인 — 3시드는 배치 16 이 이미 쟀다) + 종합 보고 커밋·푸시.
보고 머리에 재민용 라이브 반영 한 줄(redeploy-hanbando ± --central).
