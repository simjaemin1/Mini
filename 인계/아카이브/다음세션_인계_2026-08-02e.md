# durango-mini 인계 배치 6 (2026-08-02e) — 재민 일괄 지시: "최대한 전부 해결하고 개선되는 방향으로"

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md`(부트스트랩·절대 규칙·검증
> 원칙)를 읽고 그대로 따른 뒤**, 아래를 순서대로 전부 진행. 질문 금지, 실측 없는 보고 금지,
> 커밋 즉시 푸시. 기준: `1751cf8` 이후 — 시작 시 git log 로 그 뒤 커밋 먼저 읽기.
>
> 재민 원문(2026-08-02): **"최대한 전부 해결하고 개선되는 방향으로 해봐, 작업량 많아도 돼.
> DB는 리셋해도 된다고 했어"** — 그간 회부로 쌓인 결정 대기 전부를 이 지시로 푼다.
> 각 건은 "개선되는 방향"이 채택 조건이다: **기준선(아래)을 깨면 그 손잡이는 채택 불가**, 이월하고
> 원인을 보고서에 남긴다. 반쪽 구현보다 이월.

## 불변 규칙 (요약 — 전문은 다음세션_인계.md)

- 순간이동 계열 금지 · 이주 폐지 유지 · "npc는 자잘광산은 평생 못 봐"(minor 승격 금지).
- 수요는 절대 시대로 자르지 않는다. NPC 로직은 랩 verbatim 이식만.
- **배포는 재민만 · SSH 시도 금지 · force push 금지** · PAT 값 기록 금지.
- A/B 3시드 최소, 손잡이 기본 OFF 로 넣고 실측 후 채택. 병렬 실행 금지(2코어 — 랩 2개까지).
- 채택 기준선: 실지도 3시드 800일 **소멸 0/19 · 좀비 0 · 인구 ~3,424** /
  v2 CLI 5시드 **소멸 0/30 · 인구 2,120**. 어떤 변경도 이 두 줄을 깨고 채택될 수 없다.

## 작업 (순서 고정 — 앞 항목의 채택 여부가 뒤 항목의 기준선을 바꾼다)

### ① 석재 이중차감 수리 — 채택 방향 (회부_석재_이중차감.md 의 답)
v1 `STONE_MAINT_PC`(0.02)와 v2 `SUBSISTENCE_PER_NPC.stone`(0.04)이 같은 주거 유지를 각자 적는
이중 기재는 **정확성 결함이니 고친다.** 단, "산골 마을이 석재 수출로 식량을 사는" 소득 축이
꺾이면 안 된다:
- 수리 손잡이(이미 있음) ON 으로 3시드 A/B. 판정 지표에 **석재 수출 마을의 1인당 부**(국고+곳간
  식량환산)를 추가해라 — 총인구만 보면 소득 축 붕괴를 놓친다.
- 소멸 0 유지 + 산골 1인당 부가 유지되면 그대로 채택. 소득 축이 꺾이면 **총 유지비를 한 곳에
  합산 계상**(0.02+0.04 를 옳은 한 줄로)하는 보정 손잡이를 별도로 만들어 다시 A/B —
  물리는 한 번만 적되 총량은 보존하는 안이 우선 후보다.

### ② 무기 총량 하락 추세 — 원인 분리 + 품질보정 판정 (미해결 ⑪의 답)
배치 4 −8% · 배치 5 −12%. 갑옷·대장장이는 늘었다.
- 먼저 분리: `ALLOY_OPP=0` 단독 / `SHIELD_*=0` 단독 / 시드 확장(5→8시드)으로 어느 손잡이
  기여인지, 잡음인지 격리.
- ★판정 기준은 **품질보정 총량(Σ 수량×등급)**이다 — 합금 등급이 0.79→1.21 로 올랐으니
  "적지만 좋은 무기"면 개선이 맞다. 품질보정 총량이 배치 3 기준 대비 유지·상승이면 문서화하고
  종결, 하락이면 원인 손잡이를 튜닝(수요 탄력·주조 문턱)해 회복시켜라.

### ③ 구리 축 3건 패키지 (회부_구리부존과_원석적체 의 답 — 전부 채택 방향)
각각 **개별** 손잡이·개별 A/B(패키지로 뭉쳐 재면 어느 게 binding 인지 못 가른다):
1. **광산촌 시딩 특화 슬롯** — pickSeedVillages 에 광산 후보 슬롯 예약. 시딩 다양성이 목적이므로
   식량 바닥 2.0 하드 조건은 그대로(광산이라도 굶는 터는 안 된다 — 배치 3 교훈).
2. **구리 파생 투입수요** — 채택되면 청동 희소성이 바뀌므로 v2 회귀 전 열 비교 + 합금 등급
   평균이 1.0 밑으로 내려가면 기각.
3. **원석 교역 금지(추천안 나)** — 원석은 산지에서 선광·제련 후 금속으로만 유통.
   `_oreMixEff`(수입 원석 조성) 경로와의 정합을 먼저 확인 — 배치 3 채택분을 죽이면 안 된다.

### ④ 철검 위세축 실서버 반영 (설계 완료분)
설계 문서대로 실서버(존) 노출. 캐논 확인: NPC 는 철을 못 만드니(era) 위세 수요는
**플레이어가 만든 철검이 흘러들어올 때만** 발동해야 한다 — 수요를 시대로 자르지 말고,
공급이 시대에 막혀 있는 구조(배치 1 원칙)를 그대로 쓴다. era-rehearsal 로 열림/닫힘 양쪽 검증.

### ⑤ 노 조업 시간 — 플레이어 진척 계약 신설 (회부_노_조업시간_계약.md 의 답)
클릭 즉시 제련 → **장입-대기-출탕** 계약으로:
- 장입(광석+숯 투입) → 진척도가 **시간(틱)** 으로 차오름 → 완료 시 클릭해 출탕.
  NPC 크루 공식을 억지로 재사용하지 말고 플레이어 축 전용 상수(예: 1회 장입 = 실시간 수 분,
  가마 온도 티어가 높을수록 단축)를 새로 정의하되 era.js 물리와 모순되지 않게.
- 대기 중 이탈해도 진척 보존(터 소유권 계약 그대로). 숯가마도 같은 계약으로 통일.
- test-furnace 에 진척 케이스 추가 + **실클라 E2E(e2e-metallurgy) 확장** — 장입→대기→출탕을
  실화면으로. UI 에 진척 표시(움집 게이지 재사용).

### ⑥ 말 사역 소비처 (회부 ⑦의 답 — 시대 뒤에서 미리 완성)
말 시대가 열리는 날 소비처가 없으면 "길들이기만 하는 짐승"이 된다. 지금 코드로 준비:
- 사역 축 후보 실측 후 택1~2: 캐러밴 속도/적재 배수(교역 EV 에 자연 편입 — netExportValue 의
  운반비 항이 이미 있으니 그 항을 낮추는 방향이 정합적) · 기병(전투 축).
- era-rehearsal 로 "열기 전 0 영향(비트 동일) · 연 뒤 작동"을 둘 다 실측. 열기 전 세계에
  1비트라도 영향이 있으면 기각(시대 열기 전후 흥망 분리 — 배치 1 원칙).

### ⑦ 야금 3D 아이콘 8종 배포 (미해결 ⑫)
ore_chunk·iron_ore·charcoal·meteoric_iron·iron·copper·tin·lead. 기존 아이콘 파이프라인
(assets-src) 그대로. 실클라 E2E 에 "인벤 아이콘이 빈 사각형이 아니다" 검사 한 줄 추가.

### ⑧ DB 리셋 (재민: "DB는 리셋해도 된다고 했어")
- 로컬·디바이스: ①~⑦ 채택 확정 **후** 절차 문서대로 재실행(백업 → 이동/제거 → 부팅 재시딩
  스모크). 디바이스는 `_to_delete/` 이동 방식.
- **라이브(fly)**: 재민이 리셋을 허락했지만 **"배포는 재민만·SSH 시도 금지" 규칙은 그대로다.**
  fly ssh/console 을 여는 순간 규칙 위반이다 — 시도하지 마라. 대신 `문서: DB리셋_절차.md` 의
  라이브 절을 **복사-붙여넣기 가능한 명령 시퀀스**(백업 포함, 마스킹 불필요한 형태)로 갱신해
  재민이 1분 안에 실행할 수 있게 만들고, 보고서 머리에 그 블록을 그대로 실어라.

### ⑨ 마무리
전 하네스(11종+신설) + 실클라 E2E + wiring-check + v2 회귀 + 실지도 3시드 + 랩 2종 재인라인
`--check` + 디바이스 mtime 가드 동기 + 종합 보고 커밋·푸시.
손잡이별 채택/기각/이월 표 필수. 다음세션_인계.md 미해결 목록·기준선 갱신.
새 기준선 수치(인구·소멸·품질보정 무기 총량)를 명시해 다음 배치가 이어받게 하라.

## 참고 — 이번에 풀린 결정들의 원문 근거
- 석재 이중차감·노 조업 시간·무기 추세·구리 축 3건·철검 위세축·말 사역·아이콘·DB:
  재민 "최대한 전부 해결하고 개선되는 방향으로 해봐, 작업량 많아도 돼" (2026-08-02).
- DB 리셋: 재민 "db 초기화해도 돼"(02c) + "DB는 리셋해도 된다고 했어"(02e 재확인).
  단 라이브 실행 채널(fly ssh)은 별도 금지 규칙이 살아 있으므로 위 ⑧ 방식으로.
- 사람 화물 캐러밴: 보류 유지(02c — 소멸 0 달성으로 불필요).
