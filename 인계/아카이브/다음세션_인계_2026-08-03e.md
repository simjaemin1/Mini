# durango-mini 인계 배치 12 (2026-08-03e) — 건립 완성: 식량이 사람을 부른다 + 재고 UI

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` 와
> `설계_플레이어_마을건립.md` · `회부_마을건립_초기인구.md` 를 읽고** 아래를 순서대로 전부 진행.
> 질문 금지, 실측 없는 보고 금지, 커밋 즉시 푸시. 기준: `3785b3c` 이후 — git log 먼저.

## 재민 확정 (2026-08-03 대화)

**초기 인구 = 식량 유입** — "플레이어가 농사지어서 식량 확보하면 그냥 늘어나는 거 아냐?"
회부의 (가)~(라)가 아니라 **(마)** 다: 새 기구를 만들지 않고 **기존 `tickRecovery`(식량 ≥ N×15
자연회복)를 인구 0 플레이어 마을까지 확장**한다. 플레이어가 곳간에 식량을 쌓으면 첫 주민이
찾아오고, 그 뒤는 기존 로지스틱+회복이 그대로 성장을 맡는다. NPC 마을 회복도 원래 "어디선가
온다"는 추상이므로 캐논(이주 폐지·순간이동 금지) 충돌 없음 — 이 논거를 회부 문서 종결부에 적어라.

## 불변 규칙 (요약)

- **NPC 마을 비트 동일** — `founder == null` 경로는 한 비트도 안 바뀐다(배치 11 선례:
  분기 조건이 NPC 에서 항상 기존 값이 되게 짜라). 기준선: 800일 3,549·소멸(NPC) 0/19·무기Q 594
  / 1,600일 4,502 / 2,400일 4,916 / v2 CLI 2,616·0/30. 전부 유지.
- 순간이동 금지 · 이주 폐지 · minor 승격 금지 · 시대 SCHEDULE 재민만 · 계측기 정본 함수.
- UI 배치다 — **실클라 E2E 로 끝내라.** 반쪽 구현보다 이월.

## 작업

### ① 건립 계약 완성 (배치 11 이월분)
터 잡기 선례(움집·노) 그대로: 길드 사유지에 `village_site` → 자재 투입 → 진척 → 완공 시
`createVillage({founder})` 등록(`_bornDay`=그날, 인구 0). 자재·단계는 고증(송국리 환호 취락)
기준으로 정하되 상수는 손잡이로. 완공 즉시는 "빈 터"다 — 사람은 ②가 부른다.

### ② 식량 유입 배선 (재민 확정 핵심)
- `tickVillage` 의 `npcs.length === 0` 조기반환을 **플레이어 마을(founder≠null)에 한해** 우회 —
  인구 0 이어도 곳간·재고·유입 판정은 돌게(생산·소비·교역은 인구 0 이면 자연히 0이라 무해 확인).
- 유입 규칙: **기존 tickRecovery 문턱 재사용** — 인구 0 은 `식량 ≥ 15`(=1인×15) 넘으면 첫 주민 1,
  이후는 기존 규칙 그대로(식량 ≥ N×15 → 회복). **새 상수 금지** — 있는 문턱을 그대로 쓴다.
  쓸 수 없는 구조적 이유가 나오면 이월하고 회부.
- 검증: (a) 식량 0 빈 마을 = 영원히 0 · 소멸 판정 유예(사람이 산 적 없는 터는 "죽은" 게 아니다 —
  소멸(P) 집계는 **인구 ≥1 을 찍은 뒤 0 이 된 마을**만) (b) 식량 15+ → 첫 주민 → 성장 개시
  (c) 식량 끊으면 굶고 → 필멸(배치 11 배선) 작동 → 소멸(P) 1 집계. 세 궤적 전부 하네스로.
- NPC 마을 3시드 800일 **비트 동일** 필수.

### ③ 길드 관리자 재고 현황 UI (배치 11 이월분)
곳간 재화 표시(식량 합산+품목·목재·석재·도구·금속), **`_cash` 비표시**, 길드 관리 권한 게이트
(비관리자 차단 E2E). 정본 직렬화 재사용 — UI 용 사본 계산 금지.
실클라 E2E: 건립 → 식량 투입 → 첫 주민 도착 → 재고 UI 열람 → 표시값 = 서버 실값 assert.

### ④ 마무리
전 하네스 + 실클라 E2E + wiring-check + v2 회귀 + 실지도 3시드 800일(비트 동일) + 랩 2종
`--check` + 디바이스 동기 + 종합 보고 커밋·푸시. 회부_마을건립_초기인구.md 를 (마)로 종결,
인계 문서 갱신.
