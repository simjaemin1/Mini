# 인계 — 공통 — 규약·캐논·검증 원칙 (★PM 만 편집)

> ★이 파일은 **영역 소유 세션만** 갱신한다. 다른 영역에 쓸 말이 생기면 `인계/회부.md` 에 한 줄.
> 원문은 `_아카이브_2026-08_다음세션_인계.md` 에 그대로 동결돼 있다(족보 · 삭제 금지).
> 이사할 때 **문장을 한 글자도 안 고쳤다** — 낡아 보이는 줄엔 `[낡음? 확인 필요]` 표만 붙였다.
# durango-mini 다음 세션 인계 (2026-08-03)

아래 전문을 새 세션 첫 메시지로 붙여넣으면 된다.

---

너는 durango-mini(한국 청동기 송국리 문화기 오픈월드 경제 시뮬) 작업을 이어받는다.
아래 규칙·원칙·상태를 전부 읽고, [작업 배치]를 위에서부터 순서대로 진행해라. 질문하지 말고
네가 판단해서 진행하되, [회부만 할 것]은 절대 구현하지 말고 문서만 써라. 튜닝은 자유.
**정확성이 속도보다 우선.** 다 되면 결과를 정리해서 보고해라.

## 0. 부트스트랩

* 레포: `https://github.com/simjaemin1/Mini.git` → `/root/minirepo` 로 클론, `npm install`.
* GitHub PAT: 디바이스 파일 `~/Mini/durango-mini/.git/claude-pat.txt` (93바이트).
  `device_stage_files` 로 스테이징 → 셸 변수로만 읽고 즉시 `unset`. PAT 를 채팅·레포·메모리에
  절대 남기지 마라. push 출력은 반드시 `sed "s/${PAT}/***/g; s/github_pat_[A-Za-z0-9_]*/***/g"` 로 거른다.
* 시작하자마자 `git log --oneline -5`. 기준 커밋: **`f0a1c2d` 급(2026-08-02 배치 완료 시점)** —
  실제 HEAD 를 확인하고 그 뒤 커밋들을 먼저 읽어 상태 인식을 갱신해라.

### ★★푸시 정본 [재민 확정 2026-08-31] — **세션이 main 까지 민다**

```
  ① 컨테이너는 여전히 못 민다(프록시가 쓰기를 403 으로 끊는다 · 읽기는 된다).
     그래서 **맥 셸에서 민다** — `mcp__remote-devices__device_bash` 로 `~/mnt/Mini/durango-mini` 에서.
     PAT 는 맥에 있고 맥에서만 읽는다 ⇒ 토큰이 컨테이너를 거치지 않는다.
  ② 밀기 직전에 **반드시 최신 origin/main 을 확인**하고 그 위로 리베이스한다.
     클론 시점 base 로 굳히지 마라 — 병행 트랙이 4개라 그 사이 늘 전진해 있다.
  ③ **fast-forward 가 아니면 멈추고 보고한다. force push 0회.** (되돌리는 길이 revert 뿐인 문이다.)
  ④ 번들은 계속 `~/Mini/` 에 남긴다(증거·복구용). 들어간 뒤엔 `_bundles_pushed/` 로 치운다.
  ⑤ 재민에게 남는 건 배포 한 줄뿐이다:
       cd ~/Mini/durango-mini && bash scripts/redeploy-hanbando.sh --central
```
⚠**`~/Mini/푸시_번들.command` 의 fast-forward 가드는 허수일 수 있다** — 맥 레포가 번들의 부모를
이미 갖고 있으면 `git bundle verify` 가 "requires" 줄을 안 내고, 스크립트가 "자립 번들"로 오판해
BASE 를 `origin/main` **자신**으로 잡는다 ⇒ 3단계가 자기 자신과 비교하는 검사가 된다.
2026-08-31 추위 2차에서 실제로 그래서 **이미 들어간 커밋을 다시 밀다** non-fast-forward 로 죽었다.
(디바이스 파일이라 고치지 않았다 — 세션이 직접 미는 경로에는 ②③ 가드가 들어 있다.)
* 먼저 읽을 것: 이 파일 + `보고_야금과시대축_배치.md` + 회부 4종
  (`회부_구리부존과_원석적체.md` · `회부_시딩변경과_DB리셋.md` · `회부_마을소멸_시딩.md` ·
   `회부_플레이어_제련_노모델.md`) + 설계안 2종(`설계_배합과교역_한단위_통합.md` · `설계_철검_위세축.md`)
   + 조사 1종(`조사_말_사역_배선.md`).

## 1. 절대 규칙 (사용자 확정 — 위반 금지)

0. ★★[재민 확정 2026-09-03] **26개 존 서버는 실제 그 지역에 둔다 — 핑이 곧 거리다.** 먼 존은 못 할 정도여도 괜찮다(지역별 자연 분류 · 전쟁 팽창 제동). 브라우저 IP 접속은 임시 — 스팀/전용 런처로 간다. 정본 `설계/설계_플랫폼_지역서버_캐논.md`. "서버는 플레이어 위치를 따른다"는 **기각**.
1. **배포는 사용자만 한다.** SSH 시도 금지. fly 배포 명령 금지.
   ★★[재민 확정 2026-08-31] **`git push` 는 배포가 아니다.** 배포는 `scripts/redeploy-hanbando.sh`
   라는 별도 수동 스크립트이고, main 에 밀어도 아무것도 자동으로 안 나간다.
   ⇒ **main 푸시까지 세션이 한다**(아래 §0 푸시 정본). 재민에게 남기는 건 **배포 한 줄**뿐이다.
   ⚠"푸시는 재민이"는 규칙이 아니라 **컨테이너 403 우회로**였다. 배치마다 "재민 실행 대기 — 푸시"로
     반복되며 정책처럼 굳었을 뿐이다. 제약이 사라졌으니 관성을 물려받지 마라.
   ★일반 규약: **재민에게 할 일을 넘기기 전에 "이게 정말 사람이어야 하나"를 한 번 물어라.
     물려받은 할 일은 근거를 다시 읽어라.**
2. **force push 금지.** ~~DB 리셋 금지~~ → ★**2026-08-02c 재민 확정 "db 초기화해도 돼" 로 해제**:
   **로컬·디바이스 DB 는 `DB리셋_절차.md` 대로 백업 후 리셋 가능**하다(이번 배치에 실행함).
   **라이브(fly)의 `world-hanbando.db` 리셋은 여전히 재민만** — 플레이어 건축물·사유지가 전부 사라진다.
   디바이스는 `rm` 이 안 되므로 `_to_delete/` 로 **이동**한다.
3. NPC 로직은 **랩 verbatim 이식만**. econ 계수 임의 보정 금지
   (A/B 재현용 env 손잡이는 허용 — 기본값이 채택값이어야 하고 `lab-wiring-check [A2]` 가 검사한다).
4. 디바이스(`~/Mini`)의 레포 파일 동기 금지 — **랩 2종(전쟁실험실·마을실험실.html)과
   map-editor.html 만** mtime 가드로 편집한다. 그 외 디바이스 파일은 읽기 전용.
5. 스케줄 작업은 claude-code-remote MCP 만(로컬 cron 금지).
6. 이주(`tickMigration`)는 폐지됐다(2026-07). 켜지 마라. 강제소개(`tickForceEvacuation`)도 회부 없이 켜지 마라.
7. **설계 판단은 구현 전에 회부. 반쪽 구현보다 이월.**
8. ★★★**극단 HP 감소 (2026-09-01 재민 확정 · T44 · 캐논 변경)** — ~~아사 폐지~~ **폐기됐다.**
   허기·갈증·극한 추위는 **극단에 닿기 전엔 디버프만**(디버프 표 무변경), **극단에 닿으면 HP 가
   아주 천천히 깎인다.** 여러 축이 동시에 극단이면 감소율은 **기여의 합**(연속·가산).
   정본은 `server/body.js extremeHpRate` **합산기 하나**이고, 적용은 `zone.js` 가 `damagePlayer`
   정본 경로로 한다(HP 0 → 쓰러짐 사슬이 따라온다). 극단 문턱은 **3단계 경계에서 유도**한다.
   ⚠**"극단에서 HP 불감소" assert 를 새로 심지 마라** — T44 가 네 곳을 뒤집었다.
   ⚠마을이라고 면제하지 않는다(§12 의 "마을 안 불사"는 **쓰러진 뒤** 옮겨 준다는 뜻이다).
   되돌리기: `BODY_EXTREME_HP_{HUNGER,THIRST,COLD}` 셋을 0 으로. 상세는 `인계/B-신체서버.md` 3-사.
   ★[PM 승인 2026-09-01 — 이 줄만] 지시서는 "캐논 목록의 아사 폐지 줄을 고쳐라"였는데
     **§1 에 그런 줄이 없었다**(캐논은 영역 파일에만 있었다). 그래서 고치는 대신 **여기에 세웠다.**

## 2. 검증 원칙

* **본 게임이 이미 가진 것을 랩에서 다시 만들지 마라.** 실지도 랩(`scripts/econ-lab-real.js`)은
  `villages.__labProbe`·`createWorldV2`·`tickWorldV2` 를 그대로 부른다.
* 모든 회귀·측정 전에 `node scripts/lab-wiring-check.js`. `scripts/econ-regress.js` 가 이를 선행 실행한다.
* 엔진(specialty/economy-sim/-v2/era) 수정 후: `node sim/build-econ-bundle.js`(`--check` 8/8) →
  `node sim/inline-engine.js <랩들>` → `node scripts/lab-smoke.js` → 디바이스 동기.
  번들·랩 인라인 블록을 **손으로 기우지 마라**(자동 생성물).
* 하네스 전원 PASS 유지:
  `test-alloy` · `test-valuechain` · `test-mining`(⑨ 다광종 E2E 포함) · `test-armor-cap` ·
  `test-picker-tables` · **`test-furnace`**(노·숯가마 실서버 E2E) · **`test-assay`**(감정 몬테카를로) ·
  `test-building-anchor`.
* A/B 는 최소 3시드(`LAB_SEED=n`). 좀비 마을 칼날 평형 때문에 단일 시드는 판정 불가.

### ★★[재민 확정 2026-09-03] 검증 비용 규약 — 기준선·러너는 **만진 파일**이 정한다

배경: 카드 하나에 "통합 러너 전수(~3시간) + 3시드 800일 베이스 A/B(72분) + 리베이스마다 재실행"이 붙어
코딩 30분에 검증 4시간이 곱해졌다. T52·T54·T19·T20 넷이 전부 **"구조적으로 닿을 수 없다"를 먼저 증명**하고
A/B 는 그것을 확인만 했다 — 그러면 확인은 싼 칼로 족하다.

* **① 3시드 800일 베이스 A/B 는 econ 랩이 무는 파일을 만진 카드만 한다.**
  랩이 `require` 하는 저장소 모듈은 **15개**뿐이다(`sim/**` · `villages.js` · `specialty.js` · `events.js` · `trade.js` · … —
  T52 §7 적재 추적 목록이 정본). 그 밖의 파일(`zone.js`·`forage.js`·`spoil.js`·`tidal.js`·`weights.js`·`carry.js`·`lots.js`·
  `body.js`·`rescue.js`·`newcomers.js`·`public/**` …)만 만진 카드는 **랩 모듈 적재 추적**(`Module.prototype.require` 후킹 ·
  2일치 기동 · 몇 분)으로 "내 diff 의 파일 ∩ 랩 적재 목록 = ∅" 을 보고에 적는 것으로 기준선 불변을 갈음한다.
  교집합이 하나라도 있으면 종전대로 **같은 판 베이스 A/B · 정본 시드 1020·7·42 · 출력 통째 diff**.
* **② 통합 러너 전수는 푸시 직전 1회.** 리베이스 뒤에는 **겹친 파일의 하네스 + 자기 카드 하네스**만 재실행하고
  그 목록을 보고에 적는다. 러너가 빨개지면 단독 재실행·귀속(족보 ㊾·이분)은 그대로.
* ③ econ 을 여는 카드(ECON 단독 카드 · `villages.js` 생활층 절 · `events.js` 문턱)는 이 규약의 예외 — 종전 전수.
* ④ 되돌림 스위치·돌연변이 검사·정본 시드 규약은 그대로다. 줄이는 것은 **반복**이지 **증거**가 아니다.
* **커밋 즉시 푸시.** 컨테이너가 재활용되면 미푸시 커밋이 날아간다.
* `pkill` 함정: `pkill -f "econ-lab-rea[l]"` 처럼 브래킷을 쓰고 같은 명령줄에 대상 문자열을 넣지 마라.
* ★**하네스가 틀리면 조용히 통과하는 게 아니라 없는 결함을 보고한다.** 이번 세션에
  test-assay 가 두 번(FP 누설 기준·낱말 기저), test-mining 이 한 번(셀/픽셀 단위) 그렇게 틀렸다.
  **기준을 먼저 검증해라.**
* ★**그리고 반대로도 틀린다 — 자명한 상황을 골라 조용히 통과한다**(2026-08-02c).
  `test-valuechain` ⑥ 1차 작성이 순수출가가 **음수**인 상황을 골라 `max(0,·)` 에 뭉개진 `0 = 0`
  으로 통과했다. **검사 상황이 실제로 그 코드를 밟는지**(여기선 값의 양수성)를 assert 로 걸어라.
* ★**인계 문서의 진단도 실측으로 검증해라**(2026-08-02c). 배치 4 인계가 적어 준
  "좀비 덫 3중 잠금"은 틀렸다 — 좀비 마을은 굶지 않았고(국고 현금 15,000·캐러밴 61회),
  지목된 게이트를 이미 통과하고 있었다. **손잡이를 만들기 전에 궤적부터 재라.**
* ★★**계약도 실행도 멀쩡한데 화면에서 도달 못 하는 층이 하나 더 있다**(2026-08-02d).
  서버 E2E 56/0 이 통과하는 동안 **플레이어는 노를 한 번도 지을 수 없었다** — 진입 버튼이
  `display:none` 컨테이너 안에만 있었다(키 바인딩 0곳). `scripts/e2e-metallurgy.js` 가 그 층이다.
  **UI 를 건드리는 배치는 실클라 E2E 로 끝내라.**
* ★★**계측기도 '사본 금지' 대상이다**(2026-08-02f — 내가 낸 오진의 원인).
  배치 7 이 "지도에 철 0개"라는 **틀린 회부**를 냈다. 원인은 계측 스크립트가
  `hanbando-terrain.json` 을 **직접 파싱**하면서 다광종 dict(`o.minerals`)만 훑은 것이다.
  정본 판독기 `terrain.oreMineralAt` 는 dict 가 없으면 `o.mineral` **문자열로 폴백**하는데
  그 갈래를 안 밟아 **단일광종 광맥 362개(철 자잘 184개 전부)를 통째로 놓쳤다.**
  엔진에는 "사본 금지, 정본 함수를 불러라"를 그렇게 지켜 놓고 **계측기에서 어겼다.**
  ⇒ 지형·부존·가격을 읽는 계측기는 **JSON 을 직접 열지 말고 정본 함수를 불러라.**
  ⇒ 그리고 같은 실험에서 구리·납 광맥만 캐 놓고 "철은 한 톨도 안 나온다"고 **일반화**했다 —
     바로 아래 줄의 "전수로 검사해라"를 정확히 반대로 어긴 것이다. **일반화 전에 그 종을 직접 캐라.**
* ★**전수 조사가 필요한 게이트는 전수로 검사해라**(2026-08-02d). "말이 막히나"가 아니라
  **"말만 막히나"** 가 기준이다 — 화이트리스트로 짰으면 13종 중 11종이 조용히 막혔을 것이다.
* ★★**"무언가가 변하는가"를 재는 하네스는 — 변할 수 있는 대상을 골랐는지 먼저 확인해라**(2026-08-27 거래소).
  `e2e-trade ⑤`가 "내가 판 물건 값이 떨어지는가"에서 **두 번 헛짚었다**:
  ⓐ 처음엔 "마을이 팔 수 있는 양이 가장 많은" 품목을 골랐는데, 그건 곧 **가장 과잉인 품목**이라
     그림자가격이 `PRICE_ADJ_MIN` **바닥에 붙어** 재고가 늘어도 값이 안 움직이는 쪽이었다.
     **자기가 못 움직일 걸 골라 놓고 "안 움직인다"고 보고한 것**이다.
  ⓑ 그리고 "판 것 ↓ **그리고** 산 것 ↑"을 **한 짝에서 동시에** 요구했더니, 바닥 품목 하나가
     두 판정을 다 죽였다(실측: 가죽이 0.03226→0.0164 로 반토막 났는데 상대가 바닥이라 전체 실패).
     ⇒ **판정은 쪼개고, 각자 후보를 순회하게 해라.**
* ★★**하네스는 자기 행동이 실제로 일어났는지 먼저 확인해라**(2026-08-27 거래소).
  같은 하네스가 세 번째로 헛짚은 이유: 받을 것 후보 6종 전부 `재고 10.41 → 10.41` 이었는데
  그건 "값이 안 움직였다"가 아니라 **교환 자체가 거절당한 것**이었다(비싼 물건을 내려는데 마을이
  값을 못 치르면 거절된다). 게다가 **실패엔 서버가 시세표를 다시 안 보내서 화면도 그대로**라,
  "아무것도 안 변했다"와 픽셀 하나 다르지 않게 보인다.
  ⇒ 전후 비교 전에 **행동의 성사 여부를 독립적으로 세라.** 그리고 그 카운트를 **알림 로그로 세지 마라** —
    클라 알림은 40칸 링버퍼라 앞을 버린다(성사한 거래가 조용히 0건으로 읽힌다). 재고처럼 **상태로** 세라.
* ★★**하네스는 "몇 초 걸었다"가 아니라 "얼마나 갔다"로 적어라**(2026-08-27 무게).
  `test-guest-rejoin` 이 "미저장 드리프트 > 200px" 를 만들려고 **4초 고정**으로 걸었는데,
  무게 모델이 들어오며 검사 플레이어가 216% 과적(×0.58)이 되어 250px → **145px** 로 줄었고 전제가 깨졌다.
  **제품은 옳았고 하네스가 자기 걸음 속도를 상수로 박아 둔 것**이었다.
  ⇒ 목표를 **거리로** 적고 도달할 때까지 걸어라. 이동 배율을 건드리는 배치마다 안 깨진다.
* ★**"표에 없는 값"은 지어내지 말고 유도하고, 유도한 목록을 회부에 적어라**(2026-08-27 무게).
  고증표가 안 덮는 플레이어 전용 아이템 7종의 kg 을 표의 앵커에서 유도하고 근거를 한 줄씩 남긴 뒤
  `회부_무게_다음층.md` B-3 에 그 목록을 그대로 옮겼다. 조용히 정한 수는 나중에 아무도 못 고친다.
* ★★**지시서가 지목한 범인은 확인 대상이다 — 프로파일이 먼저, 수술은 그 다음**(2026-09-01 T1).
  카드 제목이 "econ 일틱 쪼개기"였고 §0 실측은 정반대였다: **econ 99ms(5%) · 생활층 1,620ms(80%)**,
  그 중 `_lifeAddHouseSite` 하나가 1,127ms(56%). 지시서대로 econ 을 쪼갰다면 **하루의 5%를 손보고
  일이 끝났다고 보고**했을 것이다. ⇒ 성능 배치의 첫 삽은 언제나 **단계별 프로파일**이다.
  대상을 지목하는 문장은 가설이지 사실이 아니다.
* ★★**A/B 가 갈리면 A/A 를 먼저 — 잡음 바닥 없이는 진짜와 가짜를 못 가른다**(2026-09-01 T1).
  같은 하네스가 "쪼갰더니 세계가 갈렸다"를 두 번 말했다. ①은 **가짜**(저장이 1마을/틱이라 팔마다
  *다른 날*의 행을 비교하고 있었다) ②는 **진짜**(마감 중 캐러밴·전투 실체가 계속 걸어 뒤 단계가
  '더 간 몸'을 봤다). 가른 것은 **같은 설정 두 판(A/A)** 이다 — ①에서도 A/A 가 갈렸고,
  ②에서는 A/A 가 50/50 동일이라 A/B 만 갈린 것이 곧 신호였다.
  ⇒ 두 판을 비교하는 검사에는 **반드시 A/A 대조군**을 같이 세워라. 그리고 비교 대상이
    같은 시점·같은 조건인지부터 확인해라(저장 배수·캐시 워밍업이 흔한 함정이다).
* ★★**"사람이 없다"는 "할 일이 없다"가 아니다 — 무인 프레임은 한가한 프레임이 아니다**(2026-09-01 T42).
  교역로 선계산을 *"사람이 있으면 멈춘다"* 한 조건으로만 게이트하고 30Hz 마다 A\* 를 한 번씩 돌렸다.
  걸음당 평균 50ms 인데 **최대 2,393ms** 라, 무인 구간 70초 동안 루프가 사실상 **100% 막혔다**.
  그 사이 방금 나간 사람의 `savePlayer` 쓰기가 **소켓 밖으로 나가지도 못했고**, 복귀 브리핑이
  "부재 0일"이 됐다(`e2e-rumor ⑦` 이 잡았다 · 내 하네스는 사람 없는 판만 재고 있어서 못 잡았다).
  ★★그리고 **시계로는 다 못 막는다.** 유예 10초를 붙였더니 러너에서 또 깨졌다 — 부재 창이 13초라
    유예의 **꼬리**에 끼어들었고, 더 나쁜 둘째 얼굴이 있었다: **입장 중인 손님은 `players` 에 안 보인다**
    (인증·로드·스폰 동안). "사람이 없다"가 두 번 거짓말한 것이다.
  ⇒ 배경 작업의 한가함은 **시계가 아니라 일 자체**로 정의해라:
    ① 사람이 없고 ② **날아가는 쓰기·처리 중인 접속이 0**(fire-and-forget 은 '끝났다'가 아니다 —
    Promise 를 세라) ③ 그것들이 끝난 지도 충분히 지났고 ④ 직전 걸음에서 쉬었다.
    걸음의 시각은 **끝난 뒤**부터 재라(긴 걸음 뒤에도 진짜로 쉬게).
  ⚠유예·간격이 고치는 것은 **빈도**다. **한 걸음의 길이**는 그 일을 중간에 놓을 수 있어야 줄어든다.
* ★★★**(83) 표에 한 줄 넣는 것과, 그 표를 읽는 함수가 무엇을 하는지는 다른 명제다**(2026-09-01 자염 · PM 승인).
  물병을 `COOK_RECIPES` 에 넣었더니 `doCook` 이 `produces` 를 **안 보고** 언제나 요리 인스턴스를 냈다 —
  '물병'이라는 이름의 **먹는 요리**가 나왔다. 표의 모양이 맞다고 그 표가 내 뜻대로 쓰인다는 보장은 없다.
  ⇒ **표에 넣기 전에 그 표를 읽는 함수를 읽어라.** 서버 하네스는 통과했고 **실클라 하네스가 잡았다** —
    "계약이 맞는가"와 "화면까지 도달하는가"는 여전히 다른 층이다.
* ★★**(84) "없다"는 두 가지다 — 품목이 없는 것과 조달이 없는 것**(2026-09-01 자염 · PM 승인).
  부패 배치가 *"소금이 이 세계에 없다"* 고 적었는데, 품목은 **실재했다**(`RESOURCES.salt` · baseValue 5).
  없던 건 **광종 POOL 과 대응표**였다. 그리고 "장부의 단골 부족 품목"이라던 전제도 틀렸다 —
  소비가 0 이라 **부족이 성립조차 안 했다**(800일 51마을 부족 사건 0건).
  ⇒ **"없다"를 적을 땐 어느 층에서 없는지까지 적어라.** 층을 안 적은 부재는 다음 배치를 헛짚게 한다.
* ★★**(85) 설계 수는 다른 정본 수와 만나야 뜻이 정해진다**(2026-09-01 자염 · PM 승인).
  지시서의 염도 3% 는 그 자체로는 멀쩡한 수였다. 그런데 소금 한 단위 무게(1.00kg · econ 정본)와
  적재 상한(25kg · 무게 정본)과 만나자 **바닷물 34되**가 되어 상한을 넘고 34번 같은 입력이 됐다.
  ⇒ **새 수를 정하면 그 수가 만나는 기존 정본 수를 먼저 곱해 보라.** 혼자서는 안 틀린 수가 만나서 틀린다.
* ★★**(86) 감속을 붙이기 전에 그 자리가 시간을 지우고 있지 않은지 먼저 재라**(2026-09-01 T5 · PM 승인).
  "저장 시설 감속"을 붙이러 갔더니 저장이 **부패 시계를 통째로 지우고** 있었다(넣었다 빼면 1.00).
  더할 것을 재기 전에 **지금 그 자리가 무엇을 하고 있는지**를 먼저 재라.
* ★★**(87) 커질 수 있는 값은 열거하지 마라 — 표에도 상한을 걸어라**(2026-09-01 T5 · PM 승인).
  누적표를 `while (끝 < d) push()` 로 짰다가 게임일 35.8억을 물은 하네스에서 배열을 35억 칸
  늘리려다 죽었다. `| 0` 족보(77)와 **같은 족**이고, 이번엔 열거가 그 자리였다.
* ★★**(88) 카드가 "무엇을 고쳐라"라고 적어도, 그게 이미 고쳐져 있는지 먼저 재라**(2026-09-01 T12 · PM 승인).
  T12 는 "인벤 패널 상한을 서버 값으로"에 클라 한 줄을 배정했는데 **이미 서버 값이었다.**
  그 예산은 버리는 게 아니라 **진짜로 화면이 거짓말하는 자리**(곁재료)로 옮겨야 한다.
* ★★**(89) 안 읽히는 사본은 "지금은 무해"가 아니라 "아직 안 터진 것"이다**(2026-09-01 T12 · PM 승인).
  `welcome.carryCfg.capKg` 는 아무도 안 읽어서 무해했다 — **상한이 상수인 동안만.**
  그 배치가 그 전제를 깼다. 사본을 발견하면 **그게 무해해 보이는 이유가 무엇에 기대고 있는지** 적어라.
* ★**(90) 손잡이의 값은 그것이 곱해질 다른 정본 수를 만나 봐야 뜻이 정해진다**(2026-09-01 T12 · PM 승인).
  (85)의 곱셈판: 염도 3% 는 적재 상한을 만나 34되가 됐고, 지게 가산 20 은 `MAT_GRADE.wood`(0.7)를
  만나 14가 됐다. ⇒ **찍지 말고 유도해라**((86)) — env 는 "결과"를 잡고 스케일은 나눗셈으로 풀어라.
* ★**"한 번"으로 재지 마라 — 날씨 편차가 있는 축은 여러 해 표본의 도달률·중앙값으로 적어라**
  (2026-09-01 T4 · PM 승인). `cold-matrix` 초안과 그 배치의 ⑯㉣ 초안이 **같은 자리**에서 틀렸다.
* ★**계측기가 세는 대상에 "아무 데나 있는 것"이 섞이면 그 판정은 못 떨어진다**(2026-09-01 T14 · PM 승인).
  야생 물웅덩이(모든 biome 굴림 상위 1~5%)가 그것이었고, 감사와 처방이 **같이** 눈이 멀었다.
  ⇒ 판정을 세울 때 **"이게 ✗ 를 낼 수 있는 자리가 지도에 있는가"** 를 하네스가 먼저 증명하게 하라
  (`test-imsu ①` 이 그 형태다).
* 좌표 단위: 광맥 `center`·`radius` 와 `terrain` 의 ore 함수는 **픽셀**. 플레이어 격자는 셀(=px/32).
* CPU 는 **2코어**다. 800일 랩 한 판이 ~4분. 6판을 동시에 던지면 각 15분씩 걸린다 — 순차가 낫다.

## 3. 현재 상태

* **노(爐)·숯가마**: 완결. 사유지 셀 단위 판정(`_claimFootprint`) · 3단계(괴련로는 별도 자재) ·
  숯가마 2단계(통나무 3→숯 4) · 3D 스프라이트 6종 · `test-furnace.js` 56/0.
  클라 메뉴는 `zonePublicMeta().era.furnaces` 로 **알려진 노만** 노출.
* **운철**: 지표 청크 자원(결정론 해시 · 대륙 42개) · 제련 불필요 · 등급 0.904(합금 모델 파생).
* **시대 축**: `era.js` 그대로. `scripts/era-rehearsal.js` 로 열기 전 리허설 가능.
  ★실측 — **시대를 열어도 NPC 경제는 0.0% 변화**(NPC 주요 광맥에 철 0).
* **econ**: 땅맞춤 초기 부존 채택(소멸 −55%·인구 +46%). `LANDFIT=0` 으로 채택 전 재현.
* ★★**소멸 0 달성(2026-08-02c)** — 실지도 3시드 800일 **소멸 0/19 · 좀비 0 · 인구 3,415**
  (배치 시작 1,610 · 소멸 1.33 · 좀비 8.33). 채택 손잡이 **STONE_NET**(재료 결손이면 채집꾼을
  기근보다 먼저) + **PRODK_CAP**(prodK 를 용량 기준으로 = fuelK 동형). **순간이동 미사용.**
  기각: TOOLBOOT(비트 동일=무효) · SWITCH2(무효). 상세 `보고_소멸0과_한단위통합.md`.
* ★**배합↔교역 한 단위 통합 채택** — `ALLOY_OPP` 기본 ON. `opp(r)=max(현지가, 최고 순수출가)`,
  순수출가는 `tickTradeV2` 발주 EV 의 항 그대로(v2 가 `world.netExportFn` 주입 — 사본 금지).
  합금 등급 평균 **0.79 → 1.21**(표준 청동 1.0 을 넘겼다). 무기 총량 −3~8%. 되돌리기 `ALLOY_OPP=0`.
* **v2 CLI 5시드 회귀 기준선(갱신)**: 인구 **2,079** · 무기 1,560 · 갑옷 132 · 소멸 **0/30**.
  (배치 전 2,001 / 1,834 / 198 / 0-30. `STONE_NET=0 PRODK_CAP=0 ALLOY_OPP=0` 으로 정확 재현 가능)
* **DB**: 로컬·디바이스 리셋 **완료**(2026-08-02c). 로컬은 재시딩 실증(선별 19 → 18곳 시딩,
  땅맞춤 부존 실발효 확인). 디바이스 원본은 `_to_delete/db-reset-20260802-0413/`, 백업은 `.bak-…` 로 원위치.
  **라이브(fly) 리셋은 여전히 재민만.**
* ★**배치 5(2026-08-02d) 채택**: **SHIELD_AGE**(보호막을 마을 나이 기준으로 — 랩 비트 동일, 라이브 구멍 수리) +
  **SHIELD_SOFT**(보호막이 삼키지 않고 ×0.25 감쇠 — 절벽→경사). 실지도 3시드 **인구 3,424 · 소멸 0/19 · 좀비 0**.
  `SHIELD_AGE=0`/`SHIELD_SOFT=0` 으로 채택 전 재현.
* ★**era.canTame 배선 완료**(말만 차단 — UNLOCK 표 기준, 하드코딩 0) · `test-tame.js` 11/0.
* ★**실클라 E2E 신설**: `node scripts/e2e-metallurgy.js` (18/0, 진짜 Chromium). 실행하면 central 3010 ·
  zone 3020 을 임시 DB로 띄운다. **이 하네스가 "플레이어가 노를 지을 방법이 없다"를 잡았다** — 회귀 가드다.
* **v2 CLI 5시드 회귀 기준선(갱신)**: 인구 **2,120** · 무기 1,369 · **품질보정 무기 422** · 갑옷 135 · 소멸 **0/30**.
* ★★★**기준선 — 이 절이 정본이다 [PM 승인 2026-09-03 · T17 ECON 수술 1]**

  **실지도 51마을 3시드 800일 — 이 표가 이후 모든 카드의 심판이다:**
  ```
    시드       인구     소멸    무기Q   확장셀    게시 의뢰   마을당 일/건
    1020      6,304    0/51     965    3,771      2,241        1.92
       7      5,740    0/51     885    3,838      2,151        1.96
      42      5,649    0/51     774    3,066      2,066        1.95
    v2 CLI 5시드 800일 : 인구 2,616 · 소멸 0/30 · 무기Q 651 · 확장셀 13,473   ← **안 움직였다**
  ```
  ★**신규 열 셋**(T17 이 세계에 없던 것을 만들었으므로 기준선도 그만큼 넓어진다):
  ```
    도구Q(Σ 재고×_toolQ)  10,868 / 10,152.8 / 9,546.8      · 도구 부족 사건 82 / 97 / 85
    보존식 재고            387 / 235.5 / 230.3             · 캐러밴 유통 34 / 6 / 27 건
    소금 지역차(내륙÷해안)  3.92 / 5.37 / 5.61              · 캐러밴 유통 27 / 34 / 22 건
  ```
  ★★**옛 기준선과 되돌리는 스위치 — 나란히 남긴다**(이 표가 어디서 왔는지를 잃지 않게):
  ```
    옛(T17 이전)  1020: 인구 6,280 · 무기Q 821 · 확장셀 5,197 · 게시 1,647 · 2.10일/건
                     7: 인구 5,993 · 무기Q 755 · 확장셀 3,681 · 게시 1,583 · 2.11일/건
                    42: 인구 6,072 · 무기Q 784 · 확장셀 4,220 · 게시 1,679 · 2.11일/건
    되돌리는 스위치 셋 :  T17_TOOL=0  T17_PRESERVE=0  T17_SALT=0
      ⇒ 셋을 끄면 위 옛 값이 **한 자리도 안 틀리고 재현된다**(인용이 아니라 재현 — `보고/T17_2026-09-02.md` §3).
      ⇒ 잰 자: `node scripts/t17-metrics.js 800 <시드>` (계측기 · 러너에 안 든다)
  ```
  ⚠**읽는 법 셋**
  1. **소멸 0/51 이 이 표의 심판이다.** 인구가 시드마다 ±로 갈리는 것(1020 +0.4% · 7 −4.2% · 42 −7.0%)은
     확장셀 감소와 붙어 있고, 그 귀속은 **후속 실측**으로 넘겼다(재민 판정 C — 수용).
  2. **사건 밀도 1.92~1.96일/건은 캐논(2~3일) 하한 아래다.** 옛도 이미 2.10 이었고(상류 T50 이 유형을
     늘렸다) T17 의 몫은 −0.18일이다. 밀도 문턱은 `EV_*` 손잡이 소관 — 다음 카드가 판정할 자리.
  3. **1,600일·2,400일 줄은 폐기가 아니라 미측정이다.** 옛 19마을 기준의 값이라 새 51마을 표와 축이 다르다.
     장기를 다시 잴 땐 이 표 위에서 새로 세운다.

  ★옛 19마을 시절 세 줄(참고 · 축이 다르다 — 위 표와 섞지 마라):
  ```
    실지도 3시드   800일 : 인구 3,549 · 소멸 0/19 · 좀비 0 · 무기Q 594 · 산골부(재화) 379.0 · 확장셀 3,756 · 현금/인 39.9
    실지도 3시드 1,600일 : 인구 4,502 · 소멸 0/19 · 좀비 0 · 무기Q 1,150 · 산골부(재화) 517.3 · 확장셀 16,684 · 현금기울기 53.1
    실지도 3시드 2,400일 : 인구 4,916 · 소멸 0/19 · 좀비 0 · 무기Q 1,409 · 확장셀 29,167 · 현금/인 127.5
  ```
  ★**2,400일 줄 추가(2026-08-03c 배치 10).** 인구는 아직 완전 평탄은 아니지만 **감속 수렴**한다
  (구간 증가 93.7 → 18.9 → 6.6 → 6.1 → **2.9%**). 소멸 0 · 좀비 0 은 **2,400일 전 구간 유지**.
  ★★**현금 기울기가 되돌아온다** — 구간별 26.5 → 46.8 → **60.6** → 51.5 → **60.0**.
  배치 8 이전(`TREASURY_BUY=0`)이 59.8 이었으니 **장기에서는 사실상 같다.**
  배치 8 의 −11% 는 800~1,600일 구간의 일시적 이득이었다. 셀 비용이 `size^1.3` 으로 커져
  곳간 잉여로 못 채우게 되고, 원자적 규약 때문에 매수가 통째로 실패로 돌아서기 때문이다.
  ⇒ **확장은 국고 현금의 충분한 소비처가 아니다**(`회부_국고_2차_소비처.md` §4 — 내 의견이 바뀌었다).
  ★**2026-08-03a `TREASURY_BUY` 채택으로 갱신된 값이다.** 채택 전 세 줄(3,424 / 3,991 / 2,120)은
  `TREASURY_BUY=0` 으로 **정확히 재현**된다. 800일 무기Q·산골부는 채택 전보다 **낮다** —
  확장 투자 국면의 일시적 비용이고 1,600일에 3/3 시드 역전한다(보고_국고현금_배선.md §2·§3).
  ★**1,600일이 종점이 아닐 수 있다** — 기준선은 1,200일에 평탄해졌는데 채택본은 1,200→1,600 이
  아직 +6.2% 다(확장이 K 를 밀어올린다). 다음에 장기를 잴 땐 2,400일도 후보다.
  ★★**"산골 1인당 부 18.1" 은 폐기된 값이다** — 배치 6 의 그 지표는 `FE(v.storage)` 가
  `totalFoodEquivalent(v)` 에 곳간을 마을인 척 넘겨 **항상 0을 던지고 catch 에 먹혀**,
  결과적으로 `_cash`(엔진에서 **쓰기 전용**인 누적 교역세) 하나만 세고 있었다.
  지금은 `BASE_VALUE` 환산 **재화**만 세고 현금은 별도 열이다(`산골현금`). 두 열을 섞지 마라.
  ★**1,600일까지 소멸 0 이 유지된다**(400/800/1200/1600 = 1,833/3,427/3,980/3,984 — 평탄화이지 붕괴 아님).
  장기 러너: `AB_DAYS=1600 scripts/ab-extinct.sh <태그>` → `node scripts/ab-longcurve.js <태그> 400`.
  ★**무기는 수량이 아니라 품질보정 총량 Σ(수량×`_weapQ`)로 판정한다**(배치 6 ②). 수량만 보면
  합금 등급 개선(0.79→1.21)을 퇴보로 오독한다. `ab-summary.js`·`econ-regress.js` 가 그 열을 찍는다.
* ★**배치 6 채택**: 말 사역(`haulMul` — 닫힌 시대 ×1 = 비트 동일 · 열리면 운반비 ×0.62) ·
  노·숯가마 **조업 진척 계약**(장입→시간→출탕, 시간의 근거는 노 온도) · 야금 아이콘 8종 · 석재 **기재** 수정.
* **미해결(회부 대기)**: ①광산촌 시딩 배제 ②구리 부트스트랩 닫힌 고리 ③~~수입 원석 제련 불가~~(해결)
  ④라이브 DB 리셋 ⑤~~배합↔교역 통합~~(채택) ⑥철검 위세축(설계 완료) ⑦말 사역 소비처
  ⑧~~day365 보호막~~ **(해결 — SHIELD_AGE+SHIELD_SOFT 채택)**
  ⑨~~석재 sink 미규명~~ **(규명 완료 — 정상 교역이었다.** 다만 원장이 **이중 차감**을 잡았고 그건 회부 중:
  `회부_석재_이중차감.md`)
  ⑩~~노 조업 시간 계약~~ **(해결 — 진척 계약 신설, 시간의 근거는 노 온도)**
  ⑪~~무기 총량 하락 추세~~ **(종결 — 품질보정 총량으로 재니 배치 3 대비 +63%)**
  ⑫~~야금 아이콘 8종~~ **(배포 완료)**
  ⑬~~구리 축 3건 미착수~~ **(2026-08-02f 전건 실측 완료 — 셋 다 기각)**:
  ①광맥 슬롯 `VEINSLOT=2` 인구 −18.7%(구리조차 순감) ②구리 파생 투입수요 `COPPER_DERIV` — §아래
  ③원석 교역 금지 `ORE_TRADE_BAN=1` 청동무기 −71%인데 원석 적체는 −4%뿐.
  손잡이 3종 전부 코드에 남아 있고 기본 OFF 다(재설계 시 재측정용).
  ⑬-b ~~부얼타운 재민 대기~~ **★재민 확정(2026-08-02f): 보존·OFF 유지. 켜지 마라.**
  판정 근거를 정확히 적는다 — "값이 비싸다"가 아니라 **"목표를 못 이룬다"**다:
  광산6 은 3/3 시드 **생존하지만 55명으로 연명하고 주조를 못 한다**. 늘어난 주조 +20% 는
  **농촌2**에서 났다(목적이 아니라 부작용). 즉 무기Q −30% 를 치르고 사는 것이
  "광산 도시"가 아니라 "남의 마을 주조 한 곳"이다. 코드·손잡이는 남긴다.
  ★**광산 도시를 정말 원하면 필요한 건 시딩 슬롯이 아니다.** 부존 실측:
  구리 광맥 126(면적 56,029) vs 주석 15(12,815) = **4.4:1** — 표준 청동 Cu88Sn12 가 요구하는
  7.3:1 보다 **구리가 오히려 부족**하다(econ 재고도 주석 318 > 구리 167). 주석은 남는데
  `TIN_EXPORT_KEEP_PC=1.2`/`SURPLUS_PC=0.3` 이 **의도적으로 묶어 둔** 것이다
  ("무산지 마을은 만성 주석 기근" = 청동 희소성 장치). COPPER_DERIV 가 등급을 깎은 것도 이 때문 —
  구리만 퍼뜨리고 주석은 묶여 있으니 무산지 마을이 얇은 배합으로 무른 검을 만든다.
  ⇒ **구리 축과 주석 유통은 짝이라 따로 재면 둘 다 기각으로 나온다.** 다시 잰다면
  `COPPER_DERIV` × `TIN_EXPORT_KEEP_PC` **2×2**(3시드 4판 ≈ 48분)여야 하고, 그건
  "청동 = 소수 엘리트 표지"를 직접 푸는 것이라 **그 자체가 세계관 판단**이다.
  ⑬-c **구리 파생 투입수요는 게이트가 옳아야 산다** — 순동 등급 0.466 < 마제석검 0.5 라
  **첫 구리 한 덩이는 정말 살 값어치가 없다**. 구리는 합칠 것(주석)이 있을 때만 값을 한다.
  현재 형태는 "가진 금속으로 짤 최선 배합"으로 판정한다(`_meltSearch` 공용 헬퍼).
  ⑯~~노 조업 시간 상수 미측정~~ **(해결 — `pace-metallurgy.js` 실측. 180/240초 그대로 채택.**
  **두 대기 모두 채광 한 덩이(60초)보다 길어 '다녀올 일'이 되고 합계 6.5분 ≤ 15분)**
  ⑰ ~~국고 현금이 쓰기 전용~~ **★★완전 종결(2026-08-03d — 재민 세금 캐논)**:
  *"교역 세금은 물물교환할 때 드는 물건이고, 방문한 쪽이 더 낸다."*
  ⇒ 실물 차원은 이미 그렇게 구현돼 있고(방문자 매출 −TAU·매수 +TAU, 차액이 도착 마을 곳간에 남음),
  `_cash` 는 화폐가 아니라 **미상환 세곡 채권 장부**다. 장부는 쌓이는 게 정상이라
  배치 10 의 "기울기가 60 으로 되돌아온다"도 결함이 아니다. **소비처를 만들 필요가 없다** —
  `회부_국고_2차_소비처.md` 의 선택지 (가)~(라) 전부 무효. `TREASURY_BUY` 는
  "국고가 채권으로 곳간에서 실물을 찾아가는 **상환**"으로 재해석(코드 무변).
  ★UI 에는 표시하지 않는다 — 배치 6 이 이 값을 "부"로 오독한 전례가 있다.
  ㉑ ★**마을 건립 초기 인구** — `회부_마을건립_초기인구.md`. **재민 결정 대기(1순위).**
  이것만 정해지면 건립 계약은 터 잡기 선례로 바로 간다.
  (아래는 배치 8 시점 기록)
  ~~⑰ 국고 현금 — `TREASURY_BUY` 채택(기본 ON)~~:
  확장 정지 줄에서 현금으로 곳간 잉여를 사서 채운다. 새 재화·직업·효과 0.
  1,600일 인구 +12.8% · 무기Q +10.5%(3/3) · 소멸 0 유지 · 현금 기울기 59.8 → 53.1(꺾였다).
  규약 4개: 그림자가격 정본 사용 · 현금 실감소 · **가계 유보(RESERVE_PC) 초과 잉여만** · 원자적(외상 금지).
  ⚠남은 것: 현금은 **여전히 순증**한다(기울기가 −11% 로 꺾였을 뿐 0 이 아니다).
  ★**회부 완료(2026-08-03b)**: `회부_국고_2차_소비처.md`. 내 의견은 **지금은 (라) 유지** —
  확장이 아직 자라는 중이라 최종 잔여량이 확정 안 됐고(1,600일이 종점이 아니다),
  공공 건축은 **시대 축과 얽혀** 지금 정하면 시대와 함께 또 바뀐다.
  **2,400일에서 확장이 멈춘 뒤의 기울기**를 보고 정하는 것이 순서다.
  (아래는 채택 전 기록)
  ~~★**국고 현금(`_cash`)이 쓰기 전용이다**~~ — 교역세로만 늘고 줄어드는 경로가 코드에 없다
  (1,600일에 1인당 55.5 → 104.0, 시간에 대략 선형). 행동 무영향이라 버그는 아니다.
  ★★**재민 확정(2026-08-02f): "확장에 현금 배선" — 1순위.** 이건 화폐 설계가 **아니다**:
  **재화 국고는 이미 공공 사업 재원이다.** `economy-sim.js:1003-1006` 이 영토 확장(마을 셀 개척)에
  `treasury.food/wood/stone` 을 쓰고, 모자라면 그날 `return` 으로 멈춘다. `_cash` 만 그 회로 밖에 있다.
  ⇒ **확장이 재화 부족으로 멈추는 그 줄에서, 현금으로 그 재화를 사게 한다.**
    새 재화·새 직업·새 효과 **0** — 교역 기구를 그대로 재사용한다.
  · 손잡이 `TREASURY_BUY` **기본 OFF** + 실지도 3시드 A/B
  · 판정 열 **2개 추가**: **확장 셀 수** · **현금 잔고 기울기**(1,600일에 선형이던 게 꺾이는가)
  · 기준선 2줄(인구·소멸·좀비·무기Q·산골부)은 그대로 지켜야 채택. 확장이 빨라져 인구가 오르면
    그건 개선이지만 **기준선 갱신**이므로 1,600일 재측정까지 같이 해야 한다.
  · ⚠과확장 주의 — 슬롯K 가 늘면 식량 압박이 생길 수 있다. `ab-summary` 에 확장 셀 수를 꼭 찍어라.
  · 이게 실패하면 그때 **세금 폐지(TAU 를 iceberg 손실로 흡수하고 `_cash` 제거)**를 검토한다.
    순서가 이래야 하는 이유: 폐지는 되돌리기 어렵고 배선은 손잡이로 되돌아간다.
  ⑲ ★★**시대 SCHEDULE — 재민 확정 "지금 안 열어"(2026-08-03). 결정 대기 아님.**
  `early_iron` 줄은 주석 처리된 채로 둔다. **SCHEDULE 을 건드리지 마라.** 말 분리 조정도 보류.
  그래서 배치 6 말 사역 · 배치 7 노 사슬 · 철검 위세축은 **의도적으로 잠들어 있다**(결함 아님).
  ~~(구 문구) 시대 SCHEDULE 이 아직 안 열려 있다~~ — `server/era.js` 의 `SCHEDULE` 에서
  `early_iron` 줄이 **주석 처리**돼 있다. 그래서 배치 6 **말 사역**(운반비 ×0.62), 배치 7 **노 사슬**
  (철기 도가니로 2장입 5분), **철검 위세축**이 전부 **잠들어 있다** — 만든 것 중 상당 부분이
  아직 한 번도 세계에서 작동한 적이 없다. 인계 §5 "회부만 할 것"이라 세션은 못 연다. **재민 결정.**
  ⑳ **2,400일 장기** — 배치 8 채택본이 1,600일에 아직 자라는 중이다(1,200→1,600 +6.2%,
  기준선은 +0.1%). 확장이 멈추는 `s*` 와 그 뒤 현금 기울기가 ⑰-b(국고 2차 소비처)의 입력이다.
  ⑱ ~~노에 넣을 것이 이 세계에 없다~~ — **오진으로 철회**(2026-08-02f 재민 검증 세션).
  전수 스크립트가 다광종 dict(`o.minerals`)만 훑어 **단일광종 광맥 362개(철 자잘 184개 전부)를
  놓쳤다**. 정정 후 실채광 실증: 철 자잘 광맥에서 채광→선광→정광→노 출탕까지 주입 0 으로 통과
  (pace-metallurgy 12/0). 노의 입력은 처음부터 세계에 있었다 — 배치 1 설계 그대로.
  `회부_노의_입력이_지도에_없다.md` 의 철회 머리말 참조. ★교훈: **전수 조사는 정본 판독기와
  같은 fallback 을 밟아야 한다**(oreMineralAt 은 dict 없으면 `o.mineral` 로 떨어진다).
  ⑭ **`ALLOY_OPP` 의 무기 대가** — 품질보정 −6%(실지도)/−18%(CLI) vs 합금 등급 +53%. 되돌리기 `ALLOY_OPP=0`. 재민 판단 대기.
  ⑮ **석재 손잡이 2종**(`STONE_MAINT`·`STONE_MAINT_UNIFY`) 기본 OFF 보존 — 석재 시장 재설계 시 재측정.
  (⑯은 위에서 해결로 표시했다)

## 6. 완료 기준

항목마다: 실측 수치(전/후) 또는 하네스 PASS → 커밋(사용자 지시 인용 포함) → **즉시 푸시**.
마지막에 `보고_*.md` 종합 보고 작성·푸시하고, 랩 2종 재인라인·스모크·디바이스 동기까지 확인.

---

---

## ★★[0번 분할 배치 2026-09-01] 여러 세션이 같은 파일을 덜 만지는 규약

이 배치의 목적은 기능이 아니라 **충돌 지점 제거**였다. 셋 중 둘을 마쳤다.

### ⓐ 인계 문서 — 영역별로 갈랐다 (완료)
`다음세션_인계.md`(4,926줄)에 모든 세션이 절을 덧붙이던 구조를 끝냈다.
**세션은 자기 카드의 영역 파일만 갱신한다.** 다른 영역에 쓸 말은 `회부.md` 에 한 줄.
`README.md`·`공통.md` 는 PM 만 — 세션은 보고서에 "공통 갱신 제안"으로 쓴다.
원문은 `_아카이브_2026-08_다음세션_인계.md` 에 **동결**(족보 · 삭제 금지).

### ⓑ 러너 — 표 기반 자동 발견으로 바꿨다 (완료)
```
  새 하네스를 추가하는 법:  그 파일 머리에  // @regress  한 줄.
  ★`scripts/run-regress.sh` 는 **건드리지 마라.** 러너가 스스로 찾는다.
  순서: test-* 먼저(가볍다) → e2e-*(무겁다 · 포트 3010/3020 공유). 각 급 안은 이름순.
        러너는 한 번에 하나씩 돌리므로 같은 급 안의 순서는 결과에 영향이 없다.
        ⚠**이름순은 의존 순서가 아니다.** 앞 하네스가 만든 것에 기대면 신선한 러너에서 영원히
          못 통과한다(`test-site-memo` 가 그랬다 — T49 가 씨앗을 `scripts/slicer-seed.js` 로 독립시켰다).
          픽스처가 필요하면 **자기가 만든다**. 순서를 규약으로 삼지 마라.
  전환 시 집합 보존을 assert 했다: 종전 명시 49개 == 자동 발견 49개(빠짐 0 · 초과 0).
  ★[T49 2026-09-02 · PM 승인] 러너 밖 27개를 등재했다 ⇒ **자동 발견 92종**.
    러너 밖에 남은 것은 4개뿐이다: `test-lab-*` 셋(레포 밖 `~/Mini/전쟁실험실.html` 을 연다 —
    컨테이너·CI 에선 영원히 못 돈다) · `test-psite-server`(제품 결함으로 빨감 · 세션3 T48 소유).
  ★[T49] 러너가 **선행 점검**을 한다: `package.json` 의 dependencies 가 실제로 풀리는지 먼저 본다.
    안 풀리면 하네스를 한 개도 안 돌리고 사유를 말하며 죽는다(`npm install`). 스킵하지 않는다 —
    스킵은 "안 쟀는데 초록"이라 더 나쁘다.
  ★[T49] 러너의 "결과줄" 탐지기를 넓혔다. 종전 문법(`통과`/`PASS <숫자>`)은 이 러너가 길러 온
    하네스의 말투만 알았고, 러너 밖에서 자란 15개가 `결과: PASS` 로 끝나 **rc=0 인데 실패로** 잡혔다.
    지금은 **검사 표시(✓/✔/✗/✘)의 존재**를 1차 근거로 본다("조용한 크래시 금지"는 그대로다).
```

### ⓒ `public/client.js` 분할 — **완료** (T0-b · 재민 확정 2026-09-01 ⓐ)

T0-a 가 "파일이 평평하지 않고 IIFE 하나"임을 밝혀 중단·회부했고, 재민이 결론 ⓐ
**"껍데기 제거 후 분할"** 을 승인해 T0-b 가 마쳤다. 커밋 셋: `95c873c`(껍데기) ·
`02a2675`(분할) · `85d5f60`(감사기). 분할 전 정본 `f789547d` → 껍데기 제거본 `95c873c`.

```
  public/client.js  (15,943줄)  →  public/client/*.js  14조각
    00-const 920 · 10-r1-terrain 2043 · 11-r1-mountain 1925 · 20-r2-visibility 839 ·
    30-n-net 1560 · 31-m-move 573 · 32-m-render 3508 · 40-r2-sprites 354 ·
    41-h-char 981 · 50-i-panel 1045 · 51-s-side 985 · 60-t-market 208 ·
    80-bigmap 700(IIFE 껍데기째) · 99-main 78(이사한 최상위 실행문 26)
```

**새 코드를 쓰는 법**
```
  · 새 기능은 **새 파일** + `index.html` 의 @area client 블록에 등록 1줄.
  · 최상위 실행문(즉시 실행되는 문장)은 **99-main.js 에만.** 나머지 조각엔 선언만.
    (조각은 등록 순으로 실행된다 — 앞 조각이 뒤 조각의 선언을 즉시 참조하면 TDZ 로 죽는다)
  · 원문 순서 보존 > 영역 순도. **재배열 금지** — 순서를 바꾸면 결합 검사가 무너진다.
  · 클라 소스를 문자열로 읽는 하네스는 `scripts/client-src.js` 의 `readClientSrc()` 를 쓴다(사본 금지).
```

**검사 둘 — 둘 다 자기가 실패할 줄 아는 걸 보였다**
```
  bash scripts/split-verify.sh            결합 diff — 조각을 등록 순으로 붙이면 분할 전 정본과 바이트 동일
    ★[T49 2026-09-02 · PM 승인] **이건 분할 커밋 전용 일회용 증명이지 상시 게이트가 아니다.**
      계약이 "분할 **전** 정본과 동일"이라 클라를 한 글자라도 고치면 영원히 differ 다.
      지금은 스크립트가 그걸 스스로 말하고 **중립 종료(0)** 한다 — 조각이 안 바뀐 채 differ 면
      그때만 빨갛게 죽는다. 볼 것은 '같은가'가 아니라 **'의도한 줄만 바뀌었는가'** 다.
  bash scripts/split-verify.sh --selftest ★한 글자 오염이 differ 를 내는지
  node scripts/test-client-globals.js     이름 충돌(중복 선언 · 브라우저 전역 · 새 최상위 실행문)
                                          ★픽스처로 셋 다 일부러 깨뜨려 잡히는지 확인 + 깨끗한 대조
  node scripts/dump-browser-globals.js    브라우저 전역 목록 갱신(추측 금지 — 실제 Chromium 에서 뜬다)
```

**★T0-b 가 남긴 실측 — 다음 사람이 다시 재지 않게**
```
  · IIFE 는 둘이 아니라 셋이었다. 소형 preloadCropSprites(L32–L40)가 앞에 하나 더 있다.
  · IIFE-1 ∩ IIFE-2 이름 겹침 = 2개(`canvas`, `ctx`).
    ⇒ IIFE-2 껍데기 유지는 예방이 아니라 **필수**였다. 합쳤으면 `let` 중복 = SyntaxError.
  · IIFE-1 깊이1 `var` = 0 ⇒ window 로 올라간 건 `function` 301개뿐. let/const 521개는 전역 렉시컬.
  · 껍데기 제거로 이름 충돌 0 (다른 classic 스크립트 60 · 실제 Chromium 전역 964 · 자기 중복).
  · `conns` — IIFE-2 가 선언 없이 쓰는데 IIFE-1 이 선언한다. 껍데기 제거 **전엔 그 줄이
    ReferenceError** 였고(디버그 경로) 제거 후 정상 해석된다. 재민 허용(기록만).
  · `needsRedraw` — IIFE-1 에서 32회 **전부 대입·읽기 0**, 읽는 곳은 IIFE-2 의 자기 `let`.
    두 클로저가 이름만 같고 다른 변수다. **회부 유지 — 고치지 마라.**
```

**★검증 방법 — "같은 빌드 두 번" 대조군 (이 배치가 확립했다)**
```
  클라를 만진 뒤 "화면이 같은가"를 픽셀로 재면, 살아 있는 세계(NPC·게임일·연출) 때문에
  **아무것도 안 바꿔도** 화소가 다르다. 그래서 diff 를 절대값으로 읽으면 없는 회귀를 본다.
  ⇒ 반드시 **같은 빌드를 두 번 돌린 쌍**을 같이 재서 잡음 바닥을 만들고, 변경 전후를 그 아래에서 읽어라.
     실측: 껍데기 제거 전후 223화소(0.032%) vs 같은 빌드 두 번 290화소(0.041%) — 변경 쪽이 **더 적었다**.
           분할 전후 622화소(0.089%) vs 같은 존·같은 빌드 두 번 527화소(0.075%) — 같은 자릿수.
  콘솔도 같다: "welcome 20ms↔25ms"·GPU 컨텍스트 id 는 같은 빌드 쌍에서도 다르다. 에러 **개수**를 봐라.
```

**다음 층(회부)**: ES 모듈 전환(T31) · IIFE-2 껍데기 제거 · 영역 순도 재배열 · 들여쓰기 정리 ·
`zone.js` 분리(T15) · `needsRedraw` 허공 대입.
(~~`32-m-render` 2차 분할~~ — **T51 이 마쳤다** · ~~`41-h-char` 2차 분할~~ — **T53 이 마쳤다**, 아래 ⓓ·ⓔ.)

### ⓓ `32-m-render.js` 2차 분할 — **완료** (T51 · 재민 확정 2026-09-02 · 행동 변경 0)

3,516줄 한 덩이를 **함수 경계에서만** 5조각으로 갈랐다. 원문 순서 그대로다 — 재배열 0.

```
  public/client/32-m-render.js (3,516줄) → 5조각 (등록 순 = 원문 순)
    32-m-render      226줄  fn 4   건축 배치 판정·고스트·진행바        (원문 2–225)
    33-m-conn        123줄  fn 4   재연결 백오프·연결 상태 12개·서버 보정·orphan (226–346)
    34-m-renderloop 2245줄  fn 2   ★render(2,195줄) · 인접 존 화살표   (347–2589)
    35-x-war         126줄  fn 10  전쟁 관전·지휘·지시자·HUD           (2590–2713)
    36-r2-building   805줄  fn 3   그리기 헬퍼·계단 셀·건물 아이소      (2714–3516)
  합: fn 23(원문 23) · 최상위 선언 이름 35(원문 35) · 최상위 실행문 0(원문 0)
```

★**`32-m-render.js` 라는 이름이 이제 건축 오버레이를 담는다.** 원문 머리가 배치·고스트였고
**순서 보존이 이름보다 위**라서 그렇다. 이름을 바꾸면 `split-verify.sh --selftest` 가 박아 둔
경로(`public/client/32-m-render.js`)가 깨져 하네스를 고쳐야 한다 — 이번 배치는 "자르기+마커+등록뿐"
이라 안 건드렸다. **개명은 회부**(하네스 1줄과 함께 옮길 것).

★**2차 분할은 `split-verify.sh` 로 못 잰다** — 그건 등록된 조각을 **전부** 이어 붙여 1차 분할 전
`client.js` 와 비교하므로, T18·온보딩 v2·T13 이 조각에 접촉 줄을 더한 지금은 **이미 `differ`** 다
(T18 이 그 주석을 달아 뒀다). 재는 대상이 다르다 ⇒ 새 검사기:
```
  bash scripts/split-verify2.sh 32-m-render 32-m-render,33-m-conn,34-m-renderloop,35-x-war,36-r2-building
      → identical   (부모 조각 하나 vs 그 자리를 이어받은 조각들 · 바이트 비교)
  bash scripts/split-verify2.sh --selftest …   ★한 글자 오염이 differ 를 내는지 먼저 보인다
```
★그리고 `split-verify.sh` 의 판정은 **T51 전후가 같다**(differ · 같은 5곳). 조각을 갈라도 결합본은
그대로이기 때문이다 — 그 자체가 "행동 변경 0"의 부수 증거다.
⚠관측: `split-verify.sh` 는 `differ` 일 때 EPIPE 스택을 토한다(`cmp` 가 먼저 닫는다). 판정은 맞다.

⚠**새 조각에 `// @@split-added` 를 달면 안 된다.** 그 표는 "분할 뒤 새로 만든 파일"의 것이고,
이 다섯은 **잘라낸 조각**이라 결합에 들어가야 맞다.

### ⓔ `41-h-char.js` 2차 분할 — **완료** (T53 · 재민 확정 2026-09-02 · 행동 변경 0)

986줄을 **함수 경계에서만** 5조각으로 갈랐다. 원문 순서 그대로 — 재배열 0.
★T51 과 달리 **이름을 내용에 맞췄다**(아래 ⓑ 참조) — `41-h-char.js` 는 없어졌다.

```
  public/client/41-h-char.js (986줄) → 5조각 (등록 순 = 원문 순)
    41-h-bubble    137줄  fn 3   마을 근접 브리핑 말풍선·하네스 진단 훅 17  (원문 2–136)
    42-r2-char     289줄  fn 7   캐릭터 스프라이트 상태기계·drawPlayerIso   (137–423)
    43-i-icon       96줄  fn 5   아이템 아이콘·이름표 표·작물 페이로드      (424–517)
    44-h-hud       372줄  fn 7   컨텍스트 메뉴·핫키바·updateHud·미니맵·채팅  (518–887)
    45-t-market    101줄  fn 8   거래소 UI·상자·제작 토글·장비 미리보기      (888–986)
  합: fn 30(원문 30) · 최상위 선언 이름 49(원문 49) · 최상위 실행문 18(원문 18)
```

**★영역이 다섯으로 갈렸다** — 이게 이 배치의 본론이다. 종전엔 H·R2·I·T 네 영역의 세션이
**같은 파일**을 만졌다(T13 이 캐릭터 레이어, 온보딩 v2 가 `updateHud`, T38 이 `ITEM_LABEL`).
이제 각자 자기 조각을 만진다.

★**이 파일은 최상위 실행문이 18개였다**(32-m-render 는 0이었다 — 그래서 T51 이 쉬웠다).
`split-audit` 으로 **뒤 참조 0건**을 먼저 세웠다: 실행문이 자기보다 뒤에 선언된 이름을 하나도
안 부른다 ⇒ 순서만 지키면 조각 경계에서 함수 선언 호이스팅이 끊겨도 안전하다.
(유일하게 즉시 식별자를 읽는 실행문 `setInterval(updateHud, 100)` 는 `updateHud` 와 **같은 조각**(④)에 있다.)

⚠**`scripts/test-client-globals.js` 의 기준선 표를 옮겨 적었다** — 그 표가 파일 이름 단위라
분할하면 반드시 갱신해야 한다(그 표의 주석이 이미 "조각을 재구성하면 같이 갱신하라"고 요구한다).
`41-h-char 18` → `41-h-bubble 17` + `44-h-hud 1`. **총계 115는 그대로**이고, 두 번째 assert 가
그 총계를 본다. 표를 고쳐 감사기가 눈이 멀지 않았는지 **일부러 확인했다**: 조각에 실행문 한 줄을
더하면 `43-i-icon 0→1 · 총계 116` 으로 잡는다(원복 확인).

**검사**(T51 이 만든 것을 그대로 재사용했다 — 고칠 데가 없었다):
```
  bash scripts/split-verify2.sh --selftest 41-h-char \
       41-h-bubble,42-r2-char,43-i-icon,44-h-hud,45-t-market      → identical
```

---

## ★족보 (79)·(80) — 검사기에 대하여 [PM 승인 2026-09-01 · T0-b 실측]

> 번호는 `Z-존서버.md` 의 (78) 에서 이어진다. 둘 다 **검사기가 조용히 거짓말한 자리**에서 나왔다.

### ★족보 (79): **해시를 박은 검사기는 리베이스 한 번에 조용히 거짓말쟁이가 된다 — 검사기는 정본을 스스로 찾는다.**

`split-verify.sh` 가 비교 기준 커밋을 `BASE=95c873c` 로 **박아** 두고 있었다.
로컬에선 통과했다 — 리베이스로 그 커밋이 밀려났는데도 **객체가 dangling 으로 남아 있어서** 였다.
다른 사람이 클론하면 그 해시는 없다. 검사기는 `differ` 가 아니라 **에러로 죽거나, 더 나쁘게는
아무것도 비교하지 않고 통과**한다.

⇒ 기준을 **관계로** 말하게 고쳤다 — "`public/client.js` 를 지운 커밋의 부모":
```bash
DEL="$(git log --diff-filter=D -1 --format=%H -- public/client.js 2>/dev/null)"
[ -n "$DEL" ] && BASE="${DEL}^"
```
같은 결의 것들: 목록을 손으로 든 러너(→ `// @regress` 자동 발견) · 베이스 커밋을 박은 성능 하네스 ·
"정본은 저 파일"이라고 주석에만 적어 둔 사본. **검사기가 아는 것은 규칙이어야 하고 값이면 안 된다.**

### ★족보 (80): **살아 있는 세계의 픽셀·ms 를 절대값으로 읽으면 없는 회귀를 본다 — 같은 빌드 두 번의 잡음 바닥을 먼저 잰다.**

NPC·게임일·연출이 도는 세계에서는 **아무것도 안 바꿔도** 화면이 다르다. 그래서
"변경 전후 223화소가 다르다"는 문장은 그 자체로 아무 뜻이 없다. 실측:
```
  껍데기 제거 전후  223화소(0.032%)   vs  같은 빌드 두 번  290화소(0.041%)   ← 변경 쪽이 **더 적다**
  분할 전후        622화소(0.089%)   vs  같은 빌드 두 번  527화소(0.075%)   ← 같은 자릿수
```
잡음 바닥을 안 쟀으면 둘 다 "회귀"로 보고했을 것이다.
ms 도 같다(`welcome 20ms↔25ms` 는 같은 빌드 쌍에서도 흔들린다). 콘솔은 문자열이 아니라 **에러 개수**를 봐라.

⇒ 규약: **살아 있는 세계를 재는 판정은 반드시 대조 쌍을 같이 낸다.**
`A vs B` 가 아니라 `(A vs B) / (A vs A')`. 절대 문턱은 참고 출력으로만 남긴다.
