# durango-mini 인계 배치 14 (2026-08-03g) — 게스트의 몸과 이름: 승계 + 인벤·좌표 영속

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` 와
> `보고_게스트_영속신원.md` 를 읽고** 아래를 순서대로 전부 진행. 질문 금지, 실측 없는 보고 금지,
> 커밋 즉시 푸시. 기준: `c085241` 이후 — git log 먼저.
>
> 재민 확정(2026-08-03): 계정 시스템 확장은 **플랫폼 결정 전까지 보류**(스팀=티켓 어댑터 /
> 웹=OAuth 어댑터 — 어느 쪽이든 playerId 발급 어댑터 하나로 붙는 현 구조 유지). 대신 어느
> 플랫폼이든 공통으로 필요한 2건을 지금 한다: **"그래 그러자."**

## 불변 규칙 (요약)

- econ 무접촉 배치다 — **전 기준선 비트 동일**이어야 정상(배치 13 동형).
  기준선: 800일 3,549·소멸(NPC) 0/19·무기Q 594 / v2 CLI 2,616·0/30.
- 배포·fly SSH·라이브 DB 재민만 · force push 금지 · PAT·토큰 값 로그/채팅/레포 기록 금지.
- 새 계정 개념(이메일·OAuth·비번 정책 변경) 만들지 마라 — 그건 플랫폼 결정 뒤다.
- UI·접속 경로를 건드린다 — **실클라 E2E 로 끝내라**(재접속·승계 시나리오 필수).

## 작업

### ① 게스트 → 등록 계정 승계 — playerId 가 유지된 채 비밀번호만 얹는다
지금은 게스트가 등록하면 **새 playerId** 로 태어나 소유(사유지·건물·마을 founder)를 전부 잃는다
— 영속 신원(배치 13)을 만든 보람이 등록하는 순간 사라지는 역설이다.
- 경로: 유효한 게스트 토큰 제시 + 새 username/password → **기존 anon_* 행에** name·password 를
  얹는다(행 교체 금지 — playerId 불변이 곧 소유 불변이다). username 중복 검사는 기존
  `/check_username` 그대로.
- 승계 완료 시 `guest_token` 을 **무효화**(NULL) — 배치 13 의 "비밀번호 생긴 행은 게스트 토큰으로
  못 연다" 게이트와 한 몸이다(토큰이 남아 있으면 그 게이트만 믿는 셈 — 벨트와 멜빵).
- 표시 이름: anon 의 '여행자' → 등록 username 으로. **소유 판정에 이름을 쓰는 곳이 0곳**임은
  배치 13이 전수로 검증했지만, 승계가 이름을 바꾸는 첫 사례이므로 그 전수를 재실행해라.
- E2E: 게스트로 마을 건립 + 노 건설 → 승계(등록) → 재접속(이제 비밀번호 로그인) → 마을 재고
  열람·노 조업·사유지 유지. 그리고 **구 게스트 토큰으로는 더 못 들어온다** assert.

### ② 게스트 인벤·좌표 영속 — 신원은 남는데 몸이 초기화되는 반쪽 해소
`savePlayer` 의 게스트 가드 한 줄이 정책이었고, 재민이 이번에 풀었다.
- 게스트도 등록 계정과 같은 경로로 인벤·좌표·도구·숙련을 저장/복원한다. **저장 스키마·경로는
  등록 계정 것을 그대로 재사용**(게스트 전용 테이블 금지 — 이제 게스트는 "비밀번호 없는 계정"일
  뿐이다).
- 용량 주의: 1회용 시절의 anon 행은 저장 대상이 아니었다 — 영속 게스트만 저장한다(토큰 있는
  행). central 불가 폴백(1회용)은 종전대로 저장 안 함.
- E2E(배치 13 e2e-guest-reconnect 확장): 아이템 채집 + 이동 → 완전 재접속 → **인벤·좌표가
  그대로**다. 종전 동작(빈 몸 리스폰)과의 차이를 assert 로.

### ③ 문서·마무리
- 보고_게스트_영속신원.md 의 "남은 것" 2건 해소 반영 · 다음세션_인계.md 갱신
  (남는 것: 토큰 회수·만료 — 플랫폼 결정 뒤 과제로 명기).
- 전 하네스(guest-identity 확장 포함) + e2e-guest-reconnect + e2e-village + e2e-metallurgy +
  wiring-check + v2 회귀 + 실지도 3시드 800일(**전부 비트 동일**) + 종합 보고 커밋·푸시.
- 라이브 반영: 런북(라이브반영_런북_2026-08-03.md)이 아직 실행 전이면 이 배치 내용이 같은
  배포에 자연히 포함된다 — 런북에 승계·영속 스모크 1줄만 추가해라. 실행은 재민.
