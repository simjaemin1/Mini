# 라이브 반영 런북 — 게스트 영속 신원 + DB 리셋 (2026-08-03, 재민 확정 "리셋과 함께 배포")

> ★★**v2 정정(재민 지적 "서버에 배포된 것도 맞아?" → 확인 결과)**: 첫 판이 **fly 명령**으로
> 쓰여 있었는데 라이브는 fly 가 아니라 **Vultr 도커**다(`DEPLOY.md`·`scripts/redeploy-all.sh`·
> 배포 기록 전부 Vultr. fly.toml 은 초기 계획의 잔재). `DB리셋_절차.md` §5-0 의 fly 블록도
> 같은 오류다 — 그 파일 머리에 경고를 달았다. **이 v2 가 정본이다.**
>
> 현재 라이브 HEAD 는 **e238d05(2026-08-01)** — 배치 4~14(소멸 0·마을 건립·게스트 신원 등)는
> 전부 미반영이다. 이 런북 한 번이면 다 올라간다.
>
> 실행은 전부 재민. 위에서 아래로 붙여넣으면 된다. **순서가 안전장치다:
> 세고 → 끄고 → 백업하고 → 지우고 → 새 코드로 켠다.**

## 라이브 좌표 (정본)

| | 값 |
|---|---|
| 호스트 | `ssh root@141.164.35.114` (Vultr) |
| 레포 | `/opt/Mini` = 레포 루트(서브폴더 없음 — 실측 정정) |
| central | 컨테이너 `durango-central` · :3010 · 데이터 `/srv/durango/central/` |
| hanbando 존 | 컨테이너 `durango-zone-hanbando` · :3020 · DB `/srv/durango/hanbando/world-hanbando.db` |
| 재배포 정본 | `bash /opt/Mini/scripts/redeploy-all.sh` (central 은 rm+run — restart 는 새 이미지 미적용) |

## 1. 무엇을 잃는지 먼저 센다

```sh
ssh root@141.164.35.114
cd /srv/durango/hanbando
sqlite3 world-hanbando.db "SELECT type, COUNT(*) FROM buildings GROUP BY type ORDER BY 2 DESC;"
sqlite3 world-hanbando.db "SELECT kind, COUNT(*) FROM claims GROUP BY kind;"
sqlite3 world-hanbando.db "SELECT COUNT(*) villages, SUM(population) pop FROM villages;"
```

## 2. 존을 끈다 — 켠 채로 지우면 메모리 상태가 다시 써 버린다

```sh
docker stop durango-zone-hanbando
```

## 3. 백업 — 반드시 -wal 까지 (WAL 모드라 최근 쓰기가 거기 있다)

```sh
cd /srv/durango/hanbando
TS=$(date +%Y%m%d-%H%M)
cp world-hanbando.db     world-hanbando.db.bak-$TS
cp world-hanbando.db-wal world-hanbando.db-wal.bak-$TS 2>/dev/null
cp world-hanbando.db-shm world-hanbando.db-shm.bak-$TS 2>/dev/null
ls -la | tail -8
```

## 4. 지운다 (이게 리셋이다 — 부팅 시 스키마 자동 생성 + 새 시딩)

```sh
rm -f world-hanbando.db world-hanbando.db-wal world-hanbando.db-shm
```

> ⚠central 데이터(`/srv/durango/central/`)는 **지우지 않는다** — 등록 계정·길드가 거기 있고
> 살아남는 게 맞다. 옛 anon_* 행은 토큰이 없어 아무도 못 여는 행일 뿐이다. 새 코드가 부팅하며
> `guest_token` 컬럼을 자동 추가한다(로그: `[central/db] guest_token 컬럼 추가됨`).

## 5. 새 코드로 켠다 (배포 스크립트가 빌드 + central rm/run + 존 재생성까지)

```sh
cd /opt/Mini
git pull                                   # da0c1de 이후인지 확인: git log --oneline -3
bash scripts/redeploy-all.sh
```

## 6. 확인 (hanbando 부팅 4~5분 — 마을 실체화)

```sh
docker logs durango-central 2>&1 | grep -E "guest_token|central"| tail -5
docker logs -f durango-zone-hanbando 2>&1 | grep -E "마을 시딩 시작|시딩:|zone server up"
```

기대: central 에 `guest_token 컬럼 추가됨`(첫 부팅 1회) · 존에
`🏘️ 마을 시딩 시작 — 후보 51 → 선별 19` → 마을별 `시딩:` ~18줄 → `zone server up`.

## 7. 실플레이 스모크 (브라우저 1~2분)

1. 접속(:3010 로비) → 이름·비밀번호 없이 입장(게스트) → 아무 데나 사유지 1칸.
2. 탭을 완전히 닫고 다시 접속 → **같은 사유지가 내 것으로 보이면** 영속 신원 작동.
3. (선택) F12 콘솔 `localStorage.getItem('durango_guest_token')` — 64자리 hex 면 정상.
   그 값은 아무 데도 붙여넣지 말 것(= 내 계정 열쇠).
4. (배치 12 확인) 길드 사유지에 회관 터 잡기 메뉴가 보이는가 — 마을 건립 진입점.
5. ★[배치 14 추가] **승계·몸 스모크** — 같은 창에서 아무거나 줍고 몇 걸음 걸은 뒤 탭을 닫았다
   다시 접속하면 **인벤·좌표가 그대로**여야 한다. 이어서 이름·비밀번호를 넣고 입장하면
   **같은 사유지가 그대로 내 것**이어야 하고(승계 — playerId 불변), 그 뒤 콘솔의
   `durango_guest_token` 은 **null** 이어야 한다(승계와 동시에 토큰이 죽는다).

## 문제가 생기면

- 되돌리기(월드만): `docker stop durango-zone-hanbando` → §3 백업 3종을 원래 이름으로 복사 →
  `docker start durango-zone-hanbando`. 코드 롤백은 `/opt/Mini` 에서
  `git checkout e238d05 && bash scripts/redeploy-all.sh`.
- central 이 안 뜨거나 `/guest` 5xx: 게스트는 자동으로 종전 1회용 id 폴백으로 입장된다
  (게임은 계속 돌아간다 — 영속만 잠시 꺼진 상태).
