# 라이브 반영 런북 — 게스트 영속 신원 + DB 리셋 (2026-08-03, 재민 확정 "리셋과 함께 배포")

> 실행은 전부 재민. 위에서 아래로 붙여넣으면 된다. **순서가 안전장치다: 세고 → 끄고 → 백업하고
> → 지우고 → 새 코드로 켠다.** 앱 이름만 실제 값으로: `<CENTRAL_APP>`(예: durango-central) ·
> `<ZONE_APP>`(예: durango-korea — fly.korea.toml 의 app).
>
> 왜 이 순서인가: 리셋과 배포를 한 번에 하면 ① 기존 anon 고아 자산(이미 복원 불가 — 1회용 id
> 였다)이 깨끗이 정리되고 ② 새 세계는 첫날부터 영속 신원·땅맞춤 시딩·소멸 0 튜닝 위에서 시작한다.

## 0. 로컬에서 최신 확인 (선택)

```sh
git pull && git log --oneline -3     # 3a12e2d(게스트 영속 신원) 이후인지 확인
```

## 1. 무엇을 잃는지 먼저 센다

```sh
fly ssh console -a <ZONE_APP> -C "sqlite3 /data/world-hanbando.db 'SELECT type, COUNT(*) FROM buildings GROUP BY type ORDER BY 2 DESC;'"
fly ssh console -a <ZONE_APP> -C "sqlite3 /data/world-hanbando.db 'SELECT kind, COUNT(*) FROM claims GROUP BY kind;'"
```

## 2. 끈다 (두 앱 다 — central 이 살아 있으면 존 헬스 폴링이 로그를 어지럽힌다)

```sh
fly scale count 0 -a <ZONE_APP> -y
fly scale count 0 -a <CENTRAL_APP> -y
```

## 3. 백업 — 반드시 -wal 까지 (WAL 모드라 최근 쓰기가 거기 있다)

```sh
fly scale count 1 -a <ZONE_APP> -y   # 볼륨 접근용 임시 기동 (부팅 로그는 무시)
fly ssh console -a <ZONE_APP> -C "sh -c 'cd /data && TS=$(date +%Y%m%d-%H%M) && cp world-hanbando.db world-hanbando.db.bak-$TS 2>/dev/null; cp world-hanbando.db-wal world-hanbando.db-wal.bak-$TS 2>/dev/null; ls -la /data | tail -12'"
fly ssh console -a <ZONE_APP> -C "sh -c 'rm -f /data/world-hanbando.db /data/world-hanbando.db-wal /data/world-hanbando.db-shm && ls -la /data'"
fly scale count 0 -a <ZONE_APP> -y
```

> ⚠central 의 `central.db`(계정·길드)는 **지우지 않는다** — 등록 계정은 살아남는 게 맞다.
> 옛 anon_* 행이 남지만 토큰이 없어 아무도 못 여는 행일 뿐이고, 새 코드가 부팅하며
> `guest_token` 컬럼을 자동 추가한다(마이그레이션 로그: `[central/db] guest_token 컬럼 추가됨`).

## 4. 새 코드로 켠다 (배포 = 곧 기동)

```sh
fly deploy --config fly.central.toml --dockerfile Dockerfile.central -a <CENTRAL_APP>
fly deploy --config fly.korea.toml   --dockerfile Dockerfile.zone    -a <ZONE_APP>
fly scale count 1 -a <CENTRAL_APP> -y
fly scale count 1 -a <ZONE_APP> -y
```

## 5. 확인 (부팅 4~5분 — 마을 실체화)

```sh
fly logs -a <CENTRAL_APP> | grep -E "guest_token|central up"
fly logs -a <ZONE_APP> | grep -E "마을 시딩 시작|시딩:|zone server up"
```

기대: central 에 `guest_token 컬럼 추가됨`(첫 부팅 1회) · zone 에
`🏘️ 마을 시딩 시작 — 후보 51 → 선별 19` → 마을별 `시딩:` ~18줄 → `zone server up`.

## 6. 실플레이 스모크 (브라우저 1분)

1. 시크릿 아님 일반 창으로 접속 → 이름·비밀번호 없이 입장(게스트) → 아무 데나 사유지 1칸.
2. 탭을 완전히 닫고 다시 접속 → **같은 사유지가 내 것으로 보이면** 영속 신원 작동.
3. (선택) F12 콘솔에 `localStorage.getItem('durango_guest_token')` — 64자리 hex 가 있으면 정상.
   그 값은 아무 데도 붙여넣지 말 것(= 내 계정 열쇠).

## 문제가 생기면

- 되돌리기: `fly ssh console` 로 `/data` 의 `.bak-<TS>` 3종을 원래 이름으로 복사 후 재기동.
  코드 롤백은 `fly releases -a <앱>` → `fly deploy --image <직전 이미지>`.
- central 이 안 뜨거나 `/guest` 5xx: 게스트는 자동으로 종전 1회용 id 폴백으로 입장된다
  (게임은 계속 돌아간다 — 영속만 잠시 꺼진 상태).
