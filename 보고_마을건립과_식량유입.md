# 보고 — 마을 건립 완성 · **식량이 사람을 부른다** · 길드 재고 UI

> 2026-08-03e 배치 12. 기준 `3785b3c`. 재민 확정 **(마)**:
> *"플레이어가 농사지어서 식량 확보하면 그냥 늘어나는 거 아냐?"*

---

## 0. 한 줄

플레이어가 **회관을 세우면 마을이 서고(인구 0)**, **곳간에 식량을 넣으면 사람이 깃든다.**
새 기구는 하나도 만들지 않았다 — 터 잡기는 노·숯가마 계약, 인구 유입은 `tickRecovery`,
권한은 `_furnaceCanUse`, 거리행렬은 기존 `distDirty` 지연 경로를 그대로 썼다.
**NPC 마을은 실지도 3시드 800일 비트 동일**(인구 3,549 · 소멸 0 · 무기Q 594).

---

## 1. ① 건립 계약 — 회관을 세우면 마을이 선다

### 무엇을 새로 만들지 않았나
`_siteStart` / `_siteAdvance`(노·숯가마 공용 2×2 사유지 다단계 건축)를 **그대로** 쓴다.
더한 것은 **완공 훅 한 줄**(`spec.onDone`)뿐이다 — 노·숯가마는 훅이 없으므로 동작 무변.

| 단계 | 자재 | 고증 |
|---|---|---|
| ① 터 다지기·지경 놓기 | 돌 30 · 곡괭이 | 구릉 사면 정지·평탄면 조성 |
| ② 환호(둘레도랑)·목책 | 돌 40 · 통나무 20 · 곡괭이 | **송국리형 취락의 표지 유구** — 파낸 흙으로 토루, 그 위 목책 |
| ③ 회관 굴립주 | 통나무 60 | 대형 굴립주 건물 = 마을의 중심 |

눈금은 손잡이다(`VILLAGE_FOUND_COST`, 기본 1.0). 노(괴련로 돌 24·통나무 10)의 여러 배 —
마을을 세우는 일이 노 하나 놓는 일과 같은 무게일 수 없다.

### 완공 = 마을 등록 (`villages.js :: foundPlayerVillage`)
정본 함수를 **같은 순서로** 부른다(사본 금지): `extractLandParamsApprox` → `createVillage` →
DB(`insertVillage` + hall + 영토 셀, 1트랜잭션) → `world.villages`/`state.villages` 등록.

NPC 시딩과 **의도적으로 다른 세 가지** — 전부 설계안 §1 이 지적한 지점이다:

1. **인구 0** (`initialPop: 0`). 초기 부존이 전부 `initN` 배라 **자동으로 0**이다 — 선물이 없다.
   ★여기서 실제 결함을 하나 고쳤다: `createVillage` 의 `opts.initialPop || 8` 이
   **`0` 을 조용히 8 로 바꿔** 놓고 있었다. 기존 호출부는 전부 1 이상이라 **비트 동일**.
2. **영토는 플레이어가 다진 터만**(반경 `PVILLAGE_TERR_R`=4 → 49셀). 2,850셀을 클릭 한 번에
   주지 않는다. 이후는 기존 `_terrGrow` 가 `land.size×25` 를 향해 하루 몇 셀씩 넓힌다.
3. **큰집(8×8)을 짓지 않는다** — 플레이어가 세운 회관이 그 마을의 회관이다.

### 자리 규칙 — "아무데나"를 문자 그대로
`pickSeedVillages` 의 잣대(FOOD_FLOOR 2.0 · MIN_SPACING_PX 12,000)는 **적용하지 않는다.**
소멸 0 은 *NPC 가 자리를 못 고르기 때문에* 세운 원칙이고, 플레이어는 고른다.
막는 것은 **남의 땅 위**뿐이다(기존 마을 영토 반경 + `PVILLAGE_GAP` 10셀). 이건 품질 기준이
아니라 좌표 충돌 방지다 — 영토가 겹치면 두 마을이 같은 셀을 개간·경작한다.
★자리 판정은 **착공 전에** 한다(`dryRun`). 3단계를 다 짓고 나서 거절하면 재료를 삼키는 것이다.
그리고 착공 판정과 완공 판정이 **같은 함수**라 어긋날 수 없다.

---

## 2. ② 식량 유입 — 재민 확정 (마) 그대로

### 고친 곳: `tickRecovery` 게이트 **두 줄**

```js
if (v.npcs.length === 0 && !v.founder) continue;             // NPC 마을 인구 0 = 소멸(종전 그대로)
if (recoveryFoodHave(v) < recoveryFoodThreshold(v)) continue; // = totalFoodEquivalent  vs  max(1,N)×15
```

* **새 상수 0.** 문턱 15 는 있던 값이고, N 하한 1 은 "1인분"이라는 같은 문턱의 자연 하한이다.
  N ≥ 1 이면 `max(1,N)×15 === N×15` — 하네스가 N=1..40 전수로 확인했다.
* **회부 (가)~(라) 무효.** 인원 실어나르기·인구 0 성장 신설·플레이어가 첫 주민·그냥 8명 —
  전부 **새 기구를 만드는 안**이었다. (마)는 있는 기구를 넓힌다.

### 캐논 충돌 없음 — 이 기구는 **원래부터 추상**이다
`tickRecovery` 의 함수 헤더가 직접 말한다: *"작은 마을 자연 회복 (이주 OFF의 보완책)"*.
NPC 마을 19곳이 800일 내내 그 추상으로 회복해 왔고 그게 **소멸 0 의 토대**다.
**이주 폐지**(`tickMigration`)·**강제 소개**(`tickForceEvacuation`)는 **여전히 OFF** —
둘은 *기존 마을의 인구를 실제로 빼 오기* 때문에 껐던 것이고, 이 경로는 **어느 마을의 인구도
줄이지 않는다.** 순간이동도 없다(지도를 가로지르는 이동이 일어나지 않는다).

### ★실클라가 잡은 것 — **"곳간의 식량"은 곡식만이 아니다**
종전 게이트는 `v.storage.food`(곡식) 하나만 봤다. 그런데 이 엔진에서 "이 마을에 식량이
얼마나 있나"의 정본은 `totalFoodEquivalent`(곡식+생선+고기+요리+채집물 환산)이고,
**부양력 K·생산 포만·기근 판정이 전부 그걸 쓴다.** 회복 게이트만 곡식을 보고 있었다 —
정본이 둘인 상태였다.

실증: 실클라에서 플레이어가 농사·채집으로 얻는 것은 **베리(→`fruit`)**라 곡식이 한 톨도
안 생긴다. 그 상태로는 **곳간을 가득 채워도 사람이 영영 안 온다** — 재민 확정 (마)가
화면에서 성립하지 않는다. ⇒ 정본 함수 하나로 통일했다. 새 규칙이 아니라 *같은 질문에
같은 자를 쓰는 것*이다. NPC 경로 영향은 §5 에서 실측했다.

### ★실클라가 잡은 것 둘째 — **아무도 안 사는 터에 캐러밴이 찾아와 없는 물건을 사 갔다**
E2E 대기 로그에 이런 줄이 찍혔다: `[대기] day 76 · 식량 **−69.3**/15 · 인구 0`.
**식량 환산이 음수**다. 아무리 부어도 회복 문턱을 영영 못 넘는 이유가 이것이었다.

원인: `tickTradeV2` 의 **대칭이 빠져 있었다.** 발주 *주체*에는 `npcs.length < 2` 가드가 있는데
발주 *대상*(목적지)에는 인구 조건이 **없었다.** 그래서 인구 0 인 새 마을이 거리상 최우선
목적지로 뽑히고, 캐러밴이 찾아가 그 마을 곳간에서 없는 물건을 실어 갔다.

⇒ 한 줄: `if (b.v.npcs.length === 0) continue;` — **아무도 안 사는 터는 교역 상대가 아니다.**
NPC 세계 영향 0(소멸 0 이라 인구 0 마을이 애초에 없다 — 3시드 800일 **바이트 완전 동일**로 실측).
하네스가 이걸 지킨다: 부자 마을 둘 사이에 빈 터를 두고 200일 돌려 **캐러밴 147회**가 오가는데
빈 터 곳간은 **변동 0**(음수 항목 0개).

### 빈 터는 **소멸이 아니다** (`_everPop`)
인구 0 으로 태어나므로 `pop === 0` 만 보면 태어나자마자 소멸로 찍힌다 — 지표가 틀리면
결론이 틀린다(배치 6·7·8 이 반복해 만난 실패 유형). `_everPop`(한 번이라도 ≥1 이었나)을
두고 **≥1 에 도달한 뒤 0 이 된 마을만** `소멸(P)` 로 센다. NPC 마을은 언제나 1 이라 판정 무변.
`ab-summary`·`econ-regress`·랩 덤프·v2 CLI 덤프 넷을 같은 잣대로 맞췄다.

### 세 궤적 실측 (`test-village-found.js` 42/0)

| 궤적 | 결과 |
|---|---|
| (a) 곳간 0 · 400일(회복 창 7회 통과) | 인구 0 유지 · `everPop=0` — **소멸로 세지 않는다** |
| (b) 식량 15 | 첫 주민 1명(farmer) → 식량을 대면 회복 상한 5명까지 성장 |
| (c) 식량 끊김 | 굶어 사망 → 인구 0 · `everPop=1` — **소멸(P) 1** |
| NPC 대조 | 곳간에 식량 100,000 이어도 **인구 0 NPC 마을은 안 살아난다**(소멸은 소멸) |

---

## 3. ③ 길드 관리자 재고 UI

회관을 클릭하면 재고 패널이 열린다. 규약 셋:

1. **`_cash` 를 안 보여준다.** 그건 화폐가 아니라 **미상환 세곡 채권 장부**다(재민 세금 캐논 —
   `회부_국고_2차_소비처.md` 종결). 서버가 `_` 로 시작하는 필드를 통째로 빼므로 **새 나갈 경로가 없다.**
   배치 6 의 '산골 1인당 부' 지표가 실은 이 값이어서 A/B 결론이 통째로 틀렸던 전례가 있다 —
   화면에 재화와 나란히 놓으면 같은 오독이 **플레이어에게서** 난다.
2. **화면에서 다시 계산하지 않는다.** 식량 환산·자립일수·다음 주민 문턱은 전부 엔진 정본
   함수의 결과를 그대로 그린다(`totalFoodEquivalent` · `recoveryFoodThreshold/Have`).
3. **권한은 새로 만들지 않는다.** 노·숯가마·길드 곳간이 쓰는 `_furnaceCanUse` 그대로 —
   길드 땅에 세운 마을이면 길드원, 개인 땅이면 창설자 본인. 실클라에서 **두 번째 플레이어가
   실제로 차단**되는 것을 확인했다(응답 자체가 안 가고, 거부는 한국어 계약 메시지).

### 곳간에 넣기 (`village_deposit`)
"식량 투입"의 통로. 플레이어 인벤 품목과 econ 재화는 이름 체계가 다르므로(플레이어 `berry`,
econ `fruit`) 그 대응표를 **한 곳에만** 둔다(`PV_DEPOSIT_MAP`). 목록에 없는 물건은 받지 않는다
(곡괭이를 곳간에 넣을 수는 없다). 넣는 즉시 화면이 갱신된다.
⚠**환산율은 잠정 1:1 이고 손잡이다**(`VILLAGE_DEPOSIT_RATE`). 균형 실측은 이 배치의 범위가
아니다 — 지금 필요한 것은 "플레이어의 노동이 마을 곳간에 도달한다"는 **경로의 존재**다.

---

## 4. 실측이 설계를 고친 것 둘 (건립 시 서버 정지)

### (가) 건립이 서버를 **10.5초 멈췄다**
실클라 E2E 로그: 마을 하나를 세우는 순간 교역 거리행렬 재계산이 **10,499ms** 걸렸다.
증분이라 BFS 는 **1쌍**인데, 비용의 전부는 코스 셀 **143,627칸 지형 판정**이었다.
그 사이 클라 워치독이 재접속했고 — 게스트는 재접속 때마다 새 `anon_*` 를 받으므로 —
**방금 제가 세운 회관의 주인이 아니게 됐다.** 마을을 세울 때마다 10초씩 멎는 건 기능이 아니라 사고다.

⇒ **이미 있는 지연 경로에 얹었다.** `state.distDirty` 는 게임일 경계에서 소비되는 기존 계약이다
(`onGameTick` 의 "무효화 훅 소비"). 거기에 증분 시작점(`_distIncrFrom`)만 실어 보낸다 —
**새 스케줄러 0.** 그때까지 그 마을의 교역 거리는 유클리드 폴백인데, 갓 선 **빈 마을**은
팔 것도 살 것도 없으니 한 게임일의 근사는 무해하다(반대로 10초 정지는 유해하다).
⚠벽·울타리 변화가 끼면 옛 쌍도 썩으므로 `invalidateTradeDistances` 가 증분을 취소하고 전쌍으로 돌린다.

### (나) 코스 격자 메모를 **호출 간에** 캐시
종전 주석은 *"재계산 시엔 새로 — 어댑터가 미래에 건물을 반영해도 안전"*이었다. 그 안전은
매번 지형을 다시 재는 값으로 사고 있었다. **캐러밴 A* 격자와 똑같이** 캐시하고 같은 훅
(`invalidateTradeDistances`)으로 비운다 — 새 기구가 아니라 같은 계약을 한 줄 더 쓴 것이다.

실측(`test-distmatrix.js` 9/0, 결과는 전쌍과 **완전 일치**):

| | 종전 | 캐시 후 |
|---|---|---|
| 전쌍 8마을(첫 계산) | 41,088ms | 41,088ms(격자를 처음 짓는 값은 그대로) |
| 그 뒤 7마을 전쌍 | ~41,000ms | **1,060ms** |
| 1곳 증분 | ~41,000ms | **1,584ms** |

---

## 5. 검증

| 검사 | 결과 |
|---|---|
| `test-village-found.js`(신규) | **42 / 0** |
| `test-distmatrix.js` | **9 / 0** (증분 = 전쌍, 불일치 0칸) |
| `test-frontier-iron.js` | **11 / 0** |
| `test-furnace.js` | **85 / 0** |
| `lab-wiring-check.js` | **실패 0** (경고 1 = 랩 HTML 미존재, 디바이스 파일) |
| v2 CLI 회귀(30마을) | 인구 **2,616** · 무기Q **651** · 소멸 **0/30** · 소멸(P) **0/0** — 배치 11 과 동일 |
| 실지도 3시드 800일 | **§5.1** |
| 실클라 E2E `e2e-village.js`(신규) | **§5.2** |

### 5.1 NPC 경로 — 실지도 3시드 800일

| 태그 | 인구 | 소멸(NPC) | 소멸(P) | 좀비 | 도구 | 확장셀 | 무기Q |
|---|---|---|---|---|---|---|---|
| `f11base`(배치 11) | 3,549 | 0 | 0/0 | 0 | 5,325 | 3,756 | 594 |
| `b12base`(건립·유입 배선 후) | **3,549** | **0** | **0/0** | **0** | **5,325** | **3,756** | **594** |

덤프 전문 비교: **신설 계측 필드(`everPop`) 외에는 바이트 단위로 동일**(3/3 시드).

### 5.1b 식량 환산 통일(§2 "곡식만이 아니다") 뒤 재측정

| 태그 | 인구 | 소멸(NPC) | 소멸(P) | 좀비 | 도구 | 확장셀 | 무기Q |
|---|---|---|---|---|---|---|---|
| `b12base`(곡식만) | 3,549 | 0 | 0/0 | 0 | 5,325 | 3,756 | 594 |
| `b12fe`(식량 환산 정본) | **3,549** | **0** | **0/0** | **0** | **5,325** | **3,756** | **594** |
| `b12tr`(+빈 터 교역 제외) | **3,549** | **0** | **0/0** | **0** | **5,325** | **3,756** | **594** |

덤프 전문 비교: **3/3 시드 바이트 단위 완전 동일**(필드 제외 없이 그대로) — 두 수리 모두.
⇒ 정본 통일이 NPC 세계를 **한 비트도** 바꾸지 않는다. 회복 게이트가 걸리는 마을
(인구 < 5)이 800일 세계에 없기 때문이다 — 소멸 0·좀비 0 이 그 자체로 그 이유다.
그래서 이 통일은 *플레이어 마을에서만 실제로 작동하는* 정합 수리다.

### 5.2 실클라 E2E — 사람이 하듯 (**29 통과 / 1 실패**)

진짜 Chromium 이 로비에서 로그인 → 걸어가 임시 사유지 2×2 → 회관 3단계 시공 → 완공 →
재접속 → 재고 열람 → 곳간에 베리 투입 → 첫 주민 → 두 번째 플레이어 차단까지 밟는다.

★하네스를 만들며 **세 번 틀렸고**, 세 번 다 실측이 고쳤다(전부 주석으로 남김):
1. `/zones` 의 `zones` 는 **객체 맵**인데 배열로 읽어 로비 게이트가 영영 false 였다.
2. 이동 펄스를 20ms 로 줬더니 **한 스텝도 안 걷는다**(클라 입력은 고정 스텝 ≤33ms).
   미세 조정이 제자리라 사유지가 2×2 가 안 됐다 — 지형은 사방이 열려 있었는데도.
3. 사유지는 **서버 좌표**로 잡히고 클라 예측은 최대 한 칸 어긋난다. ⇒ 겨냥을 포기하고
   **서버가 방송한 사유지 목록으로 수렴**시킨다(모자란 칸으로 걸어가 다시 잡고, 슬롯이 차면 해제).

**통과한 것(29)** — 건축 패널에 회관 버튼 렌더 · 걸어서 2×2 사유지 4/4 · 3단계 시공 · **완공** ·
"마을이 섰다 · 인구 0" 알림 · **재접속 후 창설자가 자기 마을 재고를 다시 봄**(소유가 세션을 넘음) ·
인구 0 = 빈 터(선물 없음) · 다음 주민 문턱 15(엔진 정본 값) · **`_cash` 가 값 차원에서 없음** ·
화면에도 현금 항목 없음 · "빈 터" 문구 · **곳간 투입이 econ 곳간에 도달**(과일 300) ·
식량 환산 120(엔진 정본) · **화면 표시값 = 서버 실값**(화면 "300" = 서버 300) ·
**두 번째 플레이어 차단**(응답 자체가 안 가고 거부는 한국어 계약 메시지) · 알림에 undefined/NaN 없음.

**실패한 것(1) — 실서버에서 첫 주민이 오지 않았다.** 원인은 §5.4 다.

### 5.4 ★미해결 — 실서버에서 **인구 0 마을 곳간이 음수로 내려간다**

E2E 대기 로그(실측, 500ms/게임일):

```
[대기] day  80 · 식량 -69.3/15 · 인구 0
[대기] day 141 · 식량  12.6/15 · 인구 0
[대기] day 202 · 식량  83.2/15 · 인구 0
[대기] day 265 · 식량  12.5/15 · 인구 0
[대기] day 328 · 식량 -45.6/15 · 인구 0
[대기] day 390 · 식량  12.5/15 · 인구 0
[대기] day 452 · 식량 -46.0/15 · 인구 0
```

투입(베리 200 ≈ 식량 환산 80)과 거의 같은 크기로 **주기적으로 빠진다.** 그리고 **음수**가 된다 —
곳간에서 빠질 수 없는 양이 빠진다는 뜻이다. 그래서 회복 창(day%50·day≥100)마다 문턱 아래에 걸려
첫 주민이 안 온다.

**어디까지 좁혔나(실측):**
* **econ 단독(랩)에서는 재현되지 않는다.** 하네스 ②-d 가 부자 마을 둘 사이에 빈 터를 두고
  200일을 돌린다 — 캐러밴 **147회**가 오가는데 빈 터 곳간은 **변동 0 · 음수 0**이다.
* 교역은 이번에 배제했고(§②-d), **전쟁 로그는 이 판에서 한 줄도 안 찍혔다**(⚔️ 0건).
* ⇒ 원인은 **서버 층**(`villages.js` 생활층 · 곳간 물리화 `guild-treasury` 등) 어딘가다.
  ★**어느 것인지는 아직 안 쟀다** — 추측으로 적지 않는다.

**회부**: 인구 0 마을의 곳간이 실서버에서 음수로 내려가는 경로를 계측해 봉합한다.
그때까지 **실서버에서 첫 주민이 오는 것은 미검증**이다(엔진 층에서는 세 궤적 전부 실측됨 — §2).

### 5.3 함께 드러난 것 — **게스트는 재접속하면 제 건물의 주인이 아니게 된다**
실측: 게스트(`anon_*`)는 접속이 끊겼다 붙을 때마다 **새 playerId** 를 받는다. 소유 판정이
playerId 대조라서, 재접속한 게스트는 **방금 제가 지은 회관·노·숯가마·사유지의 주인이 아니다.**
★이건 배치 12 가 만든 것이 아니라 **게스트 세션의 성질**이고 노·숯가마·사유지에 **이미** 해당한다.
이번 배치에서 고치지 않는다(인증 경로 전체를 건드린다) — **회부**한다.

---

## 6. 손잡이

| 이름 | 기본 | 뜻 |
|---|---|---|
| `VILLAGE_FOUND_COST` | 1.0 | 회관 3단계 자재 배수 |
| `PVILLAGE_TERR_R` | 4 | 창설 직후 영토 반경(셀) |
| `PVILLAGE_GAP` | 10 | 기존 마을 영토 밖 여유(셀) |
| `PVILLAGE_MAX` | `VILLAGE_MAX`(20) | 플레이어 마을 상한 |
| `VILLAGE_DEPOSIT_RATE` | 1.0 | 곳간 투입 환산율(잠정) |
| `E2E_GIVE` | **0(OFF)** | 테스트 전용 재료 지급 분기 — 켜지 않으면 **분기 자체가 없다** |

---

## 7. 남은 것

* **회부 1**: 인구 0 마을 곳간이 실서버에서 음수로 내려간다(§5.4) — **이번 배치의 유일한 미해결**.
  econ 층은 결백함이 실측됐으니 다음은 서버 층 계측이다.
* **회부 2**: 게스트 재접속 시 소유 상실(§5.3) — 인증 경로 전체를 건드리므로 별도 판단.
* **잠정**: 곳간 투입 환산율 1:1 — 균형 실측은 별도 배치.
* **미착수**: 플레이어 마을의 교역 참여 균형(갓 선 마을이 NPC 교역망에 어떻게 섞이는지)은
  아직 장기 실측을 안 했다. 지금은 **빈 마을이라 팔 것이 없어** 영향이 관측되지 않는다.
