# 라이브 반영 런북 v3 — **50마을 세계 전환 + 리셋** (2026-08-04, 재민 확정 "해봐")

> ★★**v3 — 이 판이 정본이다.** v2(게스트 영속 신원 + 리셋)의 절차는 그대로 살아 있고, 여기에
> **마을 50곳 전환**이 얹혔다. 리셋을 한 번 더 해야 한다: 시딩 정책이 바뀌었으므로
> 기존 world DB 의 18곳은 그대로 남고 새 32곳이 안 생긴다(시딩은 villages 테이블이 빌 때만 돈다).
>
> ★**시간 경고 — 첫 부팅이 오래 걸린다.** 라이브 18곳 시딩이 45분이었다. 로컬 실측 배율은
> **2.1배**(18곳 143초 → 50곳 303초)이니 **1.5~2시간**을 잡아라. 그동안 존은 안 뜬다(접속 불가).
> 밤에 돌리는 것을 권한다. 진행은 `docker logs -f durango-zone-hanbando | grep 시딩:` 로 센다(50줄).
>
> ★**cleanup-legacy 는 이번엔 필요 없다.** 리셋이 world DB 를 통째로 지우므로 레거시 한옥·농지
> 고아 행도 같이 사라진다. (리셋 없이 코드만 올릴 때만 쓰는 도구다.)
>
> ★**v2 정정 유지**: 라이브는 fly 가 아니라 **Vultr 도커**다. `DB리셋_절차.md` §5-0 의 fly 블록은 오류.
>
> 실행은 전부 재민. 위에서 아래로 붙여넣으면 된다. **순서가 안전장치다:
> 세고 → 끄고 → 백업하고 → 지우고 → 새 코드로 켠다.**

## 라이브 좌표 (정본)

| | 값 |
|---|---|
| 호스트 | `ssh root@141.164.35.114` (Vultr) |
| 레포 | `/opt/Mini` = 레포 루트(서브폴더 없음 — 실측 정정) |
| central | 컨테이너 `durango-central` · :3010 · 데이터 `/srv/durango/central/` |
| hanbando 존 | 컨테이너 `durango-zone-hanbando` · :3020 · DB `/srv/durango/hanbando/world-hanbando.db` |
| 재배포 정본 | ★**`bash /opt/Mini/scripts/redeploy-hanbando.sh`** (central 코드가 바뀐 배포면 `--central`). **redeploy-all 금지** — 재민 확정(2026-08-03) "한반도만 배포하는 건데 왜 자꾸 다 배포하냐". 아래 §5 도 이 명령으로 읽어라 |

## 1. 무엇을 잃는지 먼저 센다

```sh
ssh root@141.164.35.114
cd /srv/durango/hanbando
sqlite3 world-hanbando.db "SELECT type, COUNT(*) FROM buildings GROUP BY type ORDER BY 2 DESC;"
sqlite3 world-hanbando.db "SELECT kind, COUNT(*) FROM claims GROUP BY kind;"
sqlite3 world-hanbando.db "SELECT COUNT(*) villages, SUM(population) pop FROM villages;"
```

## 2. 존을 끈다 — 켠 채로 지우면 메모리 상태가 다시 써 버린다

```sh
docker stop durango-zone-hanbando
```

## 3. 백업 — 반드시 -wal 까지 (WAL 모드라 최근 쓰기가 거기 있다)

```sh
cd /srv/durango/hanbando
TS=$(date +%Y%m%d-%H%M)
cp world-hanbando.db     world-hanbando.db.bak-$TS
cp world-hanbando.db-wal world-hanbando.db-wal.bak-$TS 2>/dev/null
cp world-hanbando.db-shm world-hanbando.db-shm.bak-$TS 2>/dev/null
ls -la | tail -8
```

## 4. 지운다 (이게 리셋이다 — 부팅 시 스키마 자동 생성 + 새 시딩)

```sh
rm -f world-hanbando.db world-hanbando.db-wal world-hanbando.db-shm
```

> ⚠central 데이터(`/srv/durango/central/`)는 **지우지 않는다** — 등록 계정·길드가 거기 있고
> 살아남는 게 맞다. 옛 anon_* 행은 토큰이 없어 아무도 못 여는 행일 뿐이다. 새 코드가 부팅하며
> `guest_token` 컬럼을 자동 추가한다(로그: `[central/db] guest_token 컬럼 추가됨`).

## 5. 새 코드로 켠다 (배포 스크립트가 빌드 + central rm/run + 존 재생성까지)

```sh
cd /opt/Mini
git pull                                   # da0c1de 이후인지 확인: git log --oneline -3
bash scripts/redeploy-hanbando.sh --central   # ★배치 15 는 central.js 도 바뀌었다 — --central 필수
```

## 6. 확인 (★hanbando 첫 부팅 **1.5~2시간** — 50곳 시딩. 그동안 접속 불가)

```sh
docker logs durango-central 2>&1 | grep -E "guest_token|canadia-econ" | tail -5
# 진행 세기 — 50 이 되면 곧 뜬다
watch -n 60 'docker logs durango-zone-hanbando 2>&1 | grep -c "시딩:"'
docker logs -f durango-zone-hanbando 2>&1 | grep -E "레거시|마을 시딩|시딩 완료|zone server up"
```

기대 로그(★v3 — 숫자가 이렇게 나와야 한다):

```
🏘️ 레거시 마을 실체화 OFF (zone-config legacyVillages:false)
🏘️ 마을 시딩 시작 — 후보 51 → 선별 51 (전수(seedAllVillages))
🏘️ [어촌6] 중심 (65086,126466) 주변 24셀 내 뭍 없음 — 스킵      ← 정상. 물 위엔 못 세운다
🏘️ [<마을>] 시딩: ...                                          ← 50줄
🏘️ 마을 시딩 완료 — 후보 51 → 선별 51 → **시딩 50** (터 미달 스킵 1곳)
🏘️ Stage4A 실물화: 큰집 50·움집 100채(6×4)·곳간 → buildings 10078행
🏘️ 교역 BFS 거리행렬: 50마을 1225쌍 ...
🌏 zone server up on :3020
```

central 은 `guest_token 컬럼 추가됨`(첫 부팅 1회) · `[canadia-econ] ⏸ 대기`.

> **"선별 51 (전수(seedAllVillages))" 가 안 뜨고 "상한 20" / "선별 19" 가 뜨면 전환이 안 된 것이다.**
> 원인은 둘뿐이다:
> 1. 코드가 옛것 — `git pull` 이 배치 16 커밋을 못 받았거나 이미지가 옛것.
>    `docker exec durango-zone-hanbando node -e "console.log(require('/app/server/zone-config').ZONES.hanbando.seedAllVillages)"` → `true` 여야 한다.
> 2. ★**컨테이너에 `VILLAGE_MAX` env 가 남아 있다** — 명시한 env 상한이 존 설정을 이기도록 되어 있다
>    (하네스가 1곳만 심어 빨리 돌 수 있게 한 규칙). `docker exec durango-zone-hanbando env | grep VILLAGE_MAX`
>    가 비어 있어야 한다. 남아 있으면 컨테이너를 그 env 없이 다시 만들어야 한다
>    (`redeploy-hanbando.sh` 는 기존 env 를 그대로 옮긴다 — `/tmp/durango-zone-hanbando.env` 에서 그 줄을 지우고 재실행).

## 7. 실플레이 스모크 (브라우저 1~2분)

1. 접속(:3010 로비) → 이름·비밀번호 없이 입장(게스트) → 아무 데나 사유지 1칸.
2. 탭을 완전히 닫고 다시 접속 → **같은 사유지가 내 것으로 보이면** 영속 신원 작동.
3. (선택) F12 콘솔 `localStorage.getItem('durango_guest_token')` — 64자리 hex 면 정상.
   그 값은 아무 데도 붙여넣지 말 것(= 내 계정 열쇠).
4. (배치 12 확인) 길드 사유지에 회관 터 잡기 메뉴가 보이는가 — 마을 건립 진입점.
5. ★[배치 14 추가] **승계·몸 스모크** — 같은 창에서 아무거나 줍고 몇 걸음 걸은 뒤 탭을 닫았다
   다시 접속하면 **인벤·좌표가 그대로**여야 한다. 이어서 이름·비밀번호를 넣고 입장하면
   **같은 사유지가 그대로 내 것**이어야 하고(승계 — playerId 불변), 그 뒤 콘솔의
   `durango_guest_token` 은 **null** 이어야 한다(승계와 동시에 토큰이 죽는다).

6. ★[배치 15 추가] **레거시 마을 꺼짐 확인** — 부팅 로그에 이 두 줄이 떠야 한다:
   `🏘️ 레거시 마을 실체화 OFF (zone-config legacyVillages:false)` ·
   `🏘️ 레거시 스폰 스킵 — 한옥 0채·농지 0칸·NPC 0명·길드영토 0`.
   그리고 `🏘️ 마을 시딩 시작 — 후보 51 → 선별 19` 는 **그대로** 떠야 한다(후보 공급이 안 잘렸다는 증거).
   central 로그엔 `[canadia-econ] ⏸ 대기` — 옛 프로토타입 시뮬이 지연 시작으로 바뀌었다.
   레거시 한옥·농지 DB 행은 부팅 wipe 가 지운다(재배포만으로 사라진다). 재부팅 없이 즉시 치우거나
   남은 양을 숫자로 보고 싶으면:
   `docker exec durango-zone-hanbando node scripts/cleanup-legacy.js --db /data/world-hanbando.db`(드라이런)
   → 숫자를 보고 납득했으면 `--apply`(백업 자동 선행 · 플레이어 소유물을 물면 스스로 중단).
7. ★[배치 15 추가] **아이콘 4종** — 인벤에 은·금·니켈·옥 원석이 📦 대신 그림으로 뜨는지.
8. ★★[배치 16 추가] **50마을 세계 스모크** —
   · 접속하면 **첫 화면이 농촌22 광장**이다(옛 벌말 광장 아님). 지붕 있는 큰집이 북쪽,
     움집 2채가 남쪽에 보여야 한다. 빈 들판이면 스폰 이동이 안 반영된 것이다.
   · `sqlite3 /srv/durango/hanbando/world-hanbando.db "SELECT COUNT(*) FROM villages;"` → **50**.
   · M(월드 지도)로 마을 분포를 훑어 서쪽 열에도 마을이 들어찼는지 본다.
   · ★**cleanup-legacy 는 돌리지 않는다** — 리셋이 이미 다 지웠다.

## 문제가 생기면

- 되돌리기(월드만): `docker stop durango-zone-hanbando` → §3 백업 3종을 원래 이름으로 복사 →
  `docker start durango-zone-hanbando`. 코드 롤백은 `/opt/Mini` 에서
  `git checkout e238d05 && bash scripts/redeploy-all.sh`.
- central 이 안 뜨거나 `/guest` 5xx: 게스트는 자동으로 종전 1회용 id 폴백으로 입장된다
  (게임은 계속 돌아간다 — 영속만 잠시 꺼진 상태).
