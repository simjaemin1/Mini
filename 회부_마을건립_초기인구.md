# 회부 — 플레이어가 세운 마을에 **사람은 어디서 오는가**

> 2026-08-03d 배치 11 ①-3. **구현 안 함 · 회부.**
> 배치 지시가 이 경우를 미리 못박아 뒀다: *"NPC 정착 여부·인구 유입 규칙이 설계안에 없다면
> 그건 새 설계 판단이다 — 구현하지 말고 회부(1단계는 '마을이 존재하고 저장되고 죽을 수 있다'까지)."*
> 착수해 보니 **정확히 그 조건에 걸렸다.**

---

## 1. 왜 막혔나 — 인구가 0이면 마을이 아니라 빈 껍데기다

`tickVillage` 의 첫 줄이 이렇다:

```js
function tickVillage(v, day) {
  if (v.npcs.length === 0) return;      // ← 인구 0 이면 econ 이 아무것도 안 한다
```

즉 **인구 0으로 등록한 마을은 존재하지만 살아 있지 않다.** 생산도 소비도 교역도 없고,
소멸 판정(`pop === 0`)에는 걸리므로 **태어나자마자 죽은 것으로 집계**된다.
"마을이 존재하고 저장되고 죽을 수 있다"의 **'존재'가 성립하지 않는다.**

그런데 NPC 시딩 경로는 `INITIAL_POP = 8` 을 **그냥 준다**. 플레이어 건립에 그걸 그대로 쓰면
설계안 §1이 이미 지적한 문제가 된다 — *"8명·2,850셀을 클릭 한 번에 주는 건 마을이 아니라 선물이다."*

⇒ **초기 인구를 어디서 가져올지가 안 정해져 있다.** 이건 값의 문제가 아니라 **세계 규칙**이다:
이 세계에서 사람은 태어나거나(기존 마을의 인구 성장) 죽을 뿐, **이주는 폐지됐다**(`tickMigration`, 2026-07).
"순간이동 금지"도 캐논이다. 그러니 "어디선가 사람이 온다"를 그냥 만들 수 없다.

## 2. 이미 확정된 것들과의 충돌 지점

| 캐논/규칙 | 마을 건립과의 충돌 |
|---|---|
| **이주 폐지**(`tickMigration`, 2026-07) | "기존 마을에서 사람이 옮겨 온다"를 그대로 쓰면 폐지한 기구를 되살리는 것이다 |
| **순간이동 금지**(강제소개 포함) | 사람이 지도를 가로질러 즉시 나타나면 안 된다 |
| **소멸 0**(NPC 마을) | 기존 마을에서 사람을 빼 오면 그 마을의 인구가 준다 — 기준선을 건드린다 |
| **플레이어 마을 필멸**(재민 확정) | 그래서 "빼 온 사람이 프런티어에서 죽는" 경로가 생긴다 — 그게 의도인가? |

## 3. 선택지

- [ ] **(가) 플레이어가 데려온다 — 인원을 실어 나른다**
      기존 마을에서 NPC 를 **모집**해 플레이어가 데리고 이동한다(캐러밴처럼 **시간이 걸린다**).
      ★순간이동 아님 · 이주 기구 부활 아님(플레이어 행위다) · 원 마을 인구는 실제로 준다.
      ⚠원 마을 인구가 줄면 **NPC 소멸 0 기준선에 영향**이 갈 수 있다 — 모집 상한이 필요하다.
      고증: 송국리기 취락 분화(모촌에서 갈라져 나가는 자촌)가 정확히 이 그림이다.
- [ ] **(나) 0명으로 시작하고 자연 성장** — 마을은 등록되지만 인구는 스스로 늘어야 한다.
      ⚠지금 엔진에서는 **불가능**하다(`npcs.length === 0` 이면 tick 자체를 안 탄다).
      "인구 0 마을도 성장할 수 있다"를 새로 만들어야 하고, 그건 θ-로지스틱의 전제를 바꾼다.
- [ ] **(다) 플레이어 자신이 첫 주민** — 플레이어 캐릭터가 그 마을에 속하고, 인구는 1부터.
      상태 증가가 가장 작다. 다만 플레이어가 로그아웃하면 그 마을은 인구 0 이 된다 —
      "접속 중에만 사는 마을"이라는 새 개념이 생긴다.
- [ ] **(라) 그냥 준다** — 시딩처럼 8명. 가장 싸고, **선물이라는 문제만 남는다.**
      플레이어가 클릭 한 번으로 인구 8을 만들 수 있으면 마을 수 제한(`VILLAGE_MAX`)이
      곧 세계 인구 제한이 되고, 그건 econ 기준선을 통째로 흔든다.

**내 의견: (가).** 이유 —
① 이 세계에 이미 있는 기구(캐러밴 이동·시간 소요)로 표현되고 **새 개념이 0**이다.
② "아무데나 세운다"의 대가가 **사람을 옮기는 수고**로 구체화된다 — 자리 선택에 무게가 생긴다.
③ 고증이 정면으로 지지한다(모촌·자촌 분화).
④ 필멸과 짝이 맞는다 — 데려간 사람이 죽는 것이 플레이어의 책임이 된다.

⚠**(가)를 고르면 반드시 정해야 하는 것 둘**:
1. **모집 상한** — 원 마을에서 몇 명까지 빼도 되는가. NPC 소멸 0 기준선을 지키려면 상한이 필요하다.
   (검증 계획: 상한별로 3시드 800일 A/B — NPC 마을 소멸 0 이 유지되는 최대 상한을 찾는다)
2. **이동 중 상태** — 옮기는 동안 그 NPC 는 어느 마을 소속인가. 캐러밴 화물처럼 다루면
   기존 `world.caravans` 구조를 재사용할 수 있다(설계안 §3.1 의 "선례가 이미 있다"와 같은 결).

## 4. 그래서 이번 배치가 어디까지 했나

**기반은 전부 놓았다** — 건립 자체만 이월한다:

| | 상태 |
|---|---|
| 마을 출처 필드(`founder`) | **완료** — `createVillage(opts.founder)`, 기본 `null`(NPC) |
| 필멸 배선 | **완료** — `!v.founder && _shieldT < SHIELD_DAYS` (플레이어 마을은 보호막 미적용) |
| 소멸 지표 분리 | **완료** — `소멸(NPC)` / `소멸(P)` 두 열(`ab-summary`·`econ-regress`·랩 덤프) |
| 거리행렬 증분화 | **완료** — 전쌍 15,271ms → 증분 612ms, 결과 완전 일치(하네스 9/0) |
| 철 채광 주체 | **완료(코드 무변)** — 캐논 유지 + 수요 형성 실증(하네스 11/0) |
| **건립 계약**(`village_site` → 마을 등록) | **이월** — 이 문서의 결정이 먼저다 |
| 길드 재고 UI | **이월** — 대상 마을(길드 소유)이 0곳이라 검증할 것이 없다 |

⇒ 위 결정이 나면 건립 계약은 **터 잡기 선례(노·숯가마)를 그대로** 쓸 수 있다.
막힌 것은 계약이 아니라 **"사람이 어디서 오는가"** 하나뿐이다.
