# 회부 — 바람 노출 다음 층 (T4 · 2026-09-01)

> 재민 확정 ④(바람 노출)·⑤(삼베옷 하향) + T3 동봉(바닷물)을 실장하면서 **드러났지만 안 고친 것**들.
> 구현 금지 — 재민 판단이 있어야 여는 문이다. 실장 내용은 `인계/B-신체서버.md` 3-바.

---

## A. 이 배치가 만든 판단거리

### A-1 ★★골 칸이 평지 칸과 **정확히 같다** — 음(−)의 노출을 넣을 것인가

노출을 **더하기만** 하게 설계했다(평지 X=0 이 앵커). 그래야 추위 2차가 재 둔
"한겨울 야생 밤 옷 티어 계단"이 평지에서 한 자리도 안 움직이고, 이 배치의 효과가
**산기슭에서만** 났다고 말할 수 있다. 대가는 이것이다:

```
  한겨울 자정 24년 표본   야생 평지 13/24 (6.2분)   ·   골 13/24 (6.2분)   ← 완전히 같다
```

현실에서 계곡 바닥은 평지보다 바람이 덜 든다(겨울 복사냉각으로 **더** 추운 날도 있다 —
분지 역전층). 그래서 "골이 평지보다 따뜻하다"가 자명한 개선인지도 확실치 않다.

* **여는 방법**: `X` 를 [−1, 1] 로 열고 `t *= (1 + kX·X)` 를 그대로 두면 된다(식 변경 0).
* **대가**: 평지 기준선이 그대로여도 **골 칸이 바뀌므로** 옷 티어 매트릭스를 다시 찍어야 하고,
  `cold-matrix`·`test-body ⑮㉣` 의 채택값이 갱신 대상이 된다.
* **내 의견**: 지금은 열지 않는 쪽. 골짜기의 이득은 **불을 피울 수 있다**(바람이 없다)로
  주는 것이 더 게임적이고, 그건 아래 A-4 와 한 묶음이다.

### A-2 ★★가장 노출된 능선에서는 **갖옷도 3단계를 못 막는다** — 의도인가

```
  능선(X=1) 한겨울 자정 24년   맨몸 1.9분 · 조잡베옷 2.0 · 장인베옷 2.1 · 가죽옷 2.2 · **갖옷 2.5분 · 24/24**
  목표점 = 0.9278 × (1 + 0.6×1) = 1.485   (참고: 고도 2.3km 상당)
```

옷은 여전히 **늦춘다**(맨몸 대비 +32%). 3단계는 죽음이 아니라 느려짐이고(아사 폐지 캐논),
X=1 은 걸을 수 있는 셀의 **0.2%** 다(마을 둘레 표본 4,289셀 중). 그래서 "가장 사나운
한 뼘 땅에서는 옷으로 못 버티고 불을 피우거나 내려와야 한다"는 **읽을 수 있는 규칙**이다.

다만 추위 2차가 정확히 이 모양의 문제("천장에 붙어 옷이 무의미해진다")를 고친 배치라
같은 냄새가 나는지 재민이 봐야 한다. **되돌리기는 손잡이 하나다**: `BODY_COLD_WIND_K`.

```
  kX 0.6(채택) → 능선 갖옷 24/24 · 2.5분
  kX 0.3 이면 목표점 1.21 — 갖옷이 버틸 가능성이 생긴다(측정 안 했다. 열라면 A/B 를 낸다)
```

### A-3 통풍(gap wind) — 바람과 **나란한** 골짜기는 오히려 빨라진다

지금 모델은 골짜기를 **막힘**으로만 읽는다. 실제로는 바람과 나란한 협곡은 유속이 붙는다
(벤투리). 한반도 계곡 24개 중 주향이 북서–남동인 것들이 여기 해당한다
(실측: `너덜계곡` 은 주향이 북북서라 양쪽 레이가 **골을 따라 뚫린다** — bNW=bSE=0).
⇒ 재료는 이미 있다(레이 두 개 + 가로 방향 막힘). **판단은 "골짜기가 안전지대인가 아닌가"**다.

### A-4 바람이 **불·낚시·소리**에 주는 영향

지시서가 회부로 못 박은 것. 특히 **모닥불**이 후보다 — 지금 불은 어디서 피우나 같은 값(0.05)이다.
"능선에선 불이 안 붙는다 / 금방 꺼진다"가 붙으면 A-1·A-2 가 **같이** 풀린다
(골짜기의 이득 = 불을 피울 수 있음 · 능선의 벌 = 못 피움). 새 축 없이 `COLD_FIRE_TARGET` 에
노출을 태우면 된다. **한 묶음으로 결정하는 게 낫다.**

### A-5 바다 취송거리(fetch) — 바다가 상류면 더 춥다

지금 레이는 **바위만** 막힘으로 센다. 물은 트인 것으로 세므로 해안은 이미 노출이 높게 나오지만,
"바다를 건너온 바람이 더 차다"는 항은 없다. 겨울 북서풍이 서해를 건너오는 것은 한반도 기후의
핵심이라 고증 가치가 있다. 재료는 `isWaterTileLocal` 하나면 된다.

---

## B. 다른 영역에 넘기는 것 (`인계/회부.md` 에 한 줄씩 올렸다)

### B-1 HUD 바람 힌트 — **클라** (T38 잔무 묶음)
서버는 이미 `weather` 페이로드에 `wind`(계절 세기 −1..1)와 `exp`(노출 0..1)를 **숫자로** 보낸다.
클라는 이 배치에서 아무것도 안 그렸다(무접촉 카드). 배지 툴팁에 한 줄 더하면 끝이고,
**클라가 지형·계절 산수를 하면 안 된다**는 규약은 달력·온도와 같다.

### B-2 걸을 수 있는 고지 — **산 아크**
`passes` 가 한반도에 **0개**이고 바위 셀은 통행 불가라, 고도 감률(`elevKmAt`)이 여전히 죽어 있다.
이번 배치는 그걸 **우회**한 것이지 고친 게 아니다. 고개가 생기면 `zone.js elevKmAt` **하나만**
고치면 감률이 살아난다(모델은 `test-body ⑮㉢`이 매번 확인하고 있다).

### B-3 짠물을 끓여 민물을 얻기 — **C 영역(채집·자염)**
바닷물을 못 마시게 만들었으니 해안 생활의 물 조달이 판단거리가 된다. 자염 도구(`vessel`·솥)가
이미 있으므로 "끓여서 민물" 갈래는 새 도구 없이 열린다. 신체가 아니라 채집·제작 판단이다.

### B-4 옷 종류 확장 — T27
식물 섬유 천장(26)을 두면서 **겹쳐 입기**가 자연스러운 다음 수가 됐다(고증: 삼베를 여러 겹).
지금 슬롯은 `clothes` 하나다.

---

## C. 이 배치와 무관하게 **드러난 것**

### C-1 ⚠`test-client-globals.js` 가 **새 클론에서 못 돈다** — `acorn` 이 의존성에 없다
```
  ##### test-client-globals.js #####
  Error: Cannot find module 'acorn'   →  러너가 "결과줄 0개 = 실패"로 센다(옳은 동작)
```
T0-b(클라 분할 감사기)가 만든 하네스인데 `package.json` 에 `acorn` 이 없다.
`npm install acorn --no-save` 로 넣으면 **12/0 통과**한다(이 세션이 확인했다) — 코드는 멀쩡하고
**의존성 선언만 빠졌다**. 러너 문서가 경고한 pngjs 사고와 **정확히 같은 부류**다.
⇒ 소유는 T0-b(클라 분할)라 고치지 않았다. `package.json` 에 한 줄이면 끝난다.
