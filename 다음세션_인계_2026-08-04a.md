# durango-mini 인계 배치 15 (2026-08-04a) — 레거시 마을 끄기 [재민 "끄자"] + 라이브 마감 4건

> 새 세션 첫 메시지로 붙여넣어라. **먼저 레포의 `다음세션_인계.md` 를 읽고** 아래를 순서대로
> 전부 진행. 질문 금지, 실측 없는 보고 금지, 커밋 즉시 푸시. 기준: `3cca6d4` 이후 — git log 먼저.
>
> 배경: 라이브 리셋·배포(배치 4~14) 후 재민 실플레이에서 나온 관측들이다. "집 한 채에 지붕
> 날아감"의 정체는 **레거시 하드코딩 마을 33곳**(옛 한옥 빌더 — 1채·무지붕·큰집 없음)이었고,
> 재민 확정: **"끄자."** 완전체는 시딩 마을 18곳뿐인 세계로 간다.

## 불변 규칙 (요약)

- 배포·라이브 실행은 재민만(`bash scripts/redeploy-hanbando.sh` — central 코드 변경 시 `--central`).
  **redeploy-all 금지** — 한반도 단독 운영(재민 확정).
- NPC 시딩 마을 18곳은 한 비트도 안 바뀌어야 한다. econ 기준선(2,616·0/30 등) 무변.
- UI·월드 렌더를 건드린다 — **실화면 검증으로 끝내라**(probe-roof.js 선례: mainSquare 덮어쓰기
  래퍼로 원하는 좌표에 스폰해 스크린샷. `/tmp/zone-wrap.js` 방식 재사용).

## 작업

### ① 레거시 마을 33곳 끄기 (재민 확정)
`zone.js` 레거시 스폰 블록(한옥 1채·NPC 6명·농지·길드영토 — `npcVillageHouses` 루프)을 존
설정으로 끈다. **주의 — 자르면 안 되는 뿌리 둘**:
- `hanbando-terrain.json` 의 villages 51개는 **시딩 후보 목록**이기도 하다(후보 51 → 선별 19).
  후보 공급은 그대로 살아 있어야 한다 — 끄는 것은 "선별 안 된 마을의 레거시 실체화"뿐이다.
- 시딩 마을 18곳의 이름·길드는 레거시 목록에서 온 것 — 디듀프 경로(`isLegacyVillageClaimed`)는
  무변이어야 한다.
- 구현: 존 설정 플래그(예: `legacyVillages: false` — hanbando 만)로 게이트. 기본값은 현행 유지
  (다른 존 회귀 0). 레거시 NPC ~200명·영토 브로드캐스트가 사라지는 만큼 서버가 가벼워진다 —
  before/after tick_avg 를 재서 보고에 적어라.
- **라이브 DB 청소**: 레거시 한옥이 buildings 테이블에 영속돼 있으면(owner `npc_house_*` /
  `village_*` 계열 확인) 끈 뒤 고아 행이 남는다 — 멱등 청소 스크립트(`scripts/cleanup-legacy.js`,
  대상 owner 패턴 전수 확인 후, 백업 선행·드라이런 기본)를 만들어 재민이 라이브에서 돌리게 하라.
  플레이어 건축물(anon_*·등록 계정 소유)은 절대 건드리지 마라.

### ② 스폰 이동 — ①의 귀결
현 스폰(벌말 광장 35000,65008)은 레거시 지대라 ① 후엔 빈 들판이 된다. **가장 가까운 시딩
마을 곁으로** 옮겨라(농촌22 셀 964,1864 부근이 유력 — 실화면으로 마을이 보이는 지점을 골라
스크린샷으로 근거를 남겨라). 신규 플레이어의 첫 화면에 지붕 있는 진짜 마을이 보여야 한다.
스폰 겹침(마을 건물·사유지 위 스폰 금지)도 확인.

### ③ 첫 시딩 부팅 직후 건물 미탑재 수리
라이브 실측: 리셋 후 첫 부팅에서 Stage4A 실물화가 DB 에 4,794행을 쓰지만 **메모리 건물 맵에는
안 올라가**(bld=5) 재시작 한 번이 더 필요했다. 실물화가 쓴 행을 라이브 맵/활성청크 경로에도
올리도록 수리(재시작 없이 첫 부팅부터 보이게). 검증: 새 DB 로컬 부팅 1회 → 재시작 없이
probe-roof 실화면에 큰집·움집 렌더.

### ④ 아이콘 4종 + central 옛 시뮬 정체
- silver·gold·nickel·jade_raw.png 404 — icon_render.py 파이프라인 그대로 4종 추가(고증색).
- central 이 상시 CPU ~50%를 먹고 `🌈풍요/💀캐러밴 약탈 Day 900+`(검은숲·연어강·늑대골 — 존
  마을이 아님) 로그를 찍는다 — **정체를 파악해라**(옛 프로토타입 시뮬로 추정). 게임에 실효가
  없으면 끄는 손잡이(기본 OFF 전환은 실효 0 확인 후)·있으면 문서화. 추측 금지, 코드로 확인.

### ⑤ 마무리
전 하네스 + 실클라 E2E 3종 + wiring-check + v2 회귀(비트 동일) + 실화면(스폰·시딩 마을·구
레거시 자리 3장) + 종합 보고 커밋·푸시. 보고 머리에 재민용 라이브 반영 절차(redeploy-hanbando
± cleanup-legacy 드라이런→실행) 한 블록.
