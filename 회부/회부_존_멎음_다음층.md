# 회부 — 존 멎음: 켤까 · 부팅 25초 · 남은 실측 [2026-08-31 밤]

근거·계수·A/B 전부는 **`보고_존_멎음_원인과_수리.md`**. 여기엔 **내가 혼자 정하면 안 되는 것**만 적는다.

---

## A. 두 플래그를 실운영에 켤까 · 기본값을 바꿀까

수리가 **둘**이다. 서로 다른 비용을 없애고 겹치지 않는다.

| | 없애는 것 | env | 비용 |
|---|---|---|---|
| **ⓐ 타일 지형 메모** | 같은 셀을 **다시** 묻는 비용 | `TERRAIN_TILE_CACHE=1` | 메모리 8.5MB |
| **ⓑ 선분 격자 색인** | **처음** 묻는 비용(강·산맥 path 선형 주사) | `TERRAIN_SEG_INDEX=1` | 수십 KB |

**효과**(같은 DB 스냅샷 · 야생·도적 켬 · 플래그만 다름):

| | 기동~서버 열림 | 게임일 틱 중앙 | 틱 최대 | 정상 멎음 |
|---|---|---|---|---|
| 둘 다 끔(현행) | 52.2초 | 879ms | 6,534ms | 23회 · 25.8초 |
| ⓑ 만 | 13.1초 | 204ms | 1,088ms | 1회 · 1.1초 |
| **둘 다** | **12.1초** | **160ms** | **935ms** | **1회 · 0.9초** |

**등가성 증명 사슬 (셋 다 닫혔다)**

```
변경 전  ==  변경 후(꺼짐)     51,474점 불일치 0   (git show HEAD 판을 그 자리에서 돌려 대조)
변경 후(꺼짐) == 변경 후(켬)   51,474점 비트 동일  (test-segindex ① · 자식 프로세스 둘)
타일 메모 켬 == 원본           10,500회 전부 일치  (test-terraincache ①)
전체 회귀(둘 다 켬)            아래 §E
```
표본은 **강·산맥 경계에 몰아서** 만들었고(halfWidth 의 0~2배 거리), **셀 중심이 아닌 점이 과반**이다
(`chunk.js:447` x±D · `fishing.js` 의 호출 모양).

**선택지**

1. **env 로만 켠다** — `docker-compose` 의 zone 서비스에 두 줄 추가. 기본값은 꺼짐 유지.
   되돌리기가 가장 싸다(env 지우면 끝).
2. **기본값을 켜짐으로 바꾼다** — 조건을 `!== '0'` 으로. 새 존도 자동으로 혜택.
3. **ⓑ 만 켠다** — 부팅과 꼬리(틱 최대)를 잡는 건 ⓑ 다. 메모리 8.5MB 도 안 쓴다.
   ⓐ 는 그 위에서 중앙값을 204→160ms 로 한 번 더 깎는 몫이다.

내 의견: **1번으로 둘 다.** 다만 이건 내 의견일 뿐이고, 손잡이 기본값은 이 프로젝트에서 재민 확정 사항이다.

## B. 남은 부팅 12초를 다음 배치에서 다룰까

프로파일이 자리를 정확히 짚었다 — `SimVillages.init` 24.6초 중
**`computeAndInjectDistMatrix` 가 24.2초**(마을 전쌍 BFS 거리행렬).
그 밑이 전부 지형 술어였고 전부 **최초 질의**라, ⓑ 색인이 **52.2초 → 12.1초**로 줄였다.

* **남은 12초**는 시딩·영토 실물화·거리행렬의 순수 계산이다.
* **왜 이번에 더 안 갔나**: 더 줄이려면 **비동기 분할**(기동 중에도 이벤트 루프를 놓아 주기)이
  필요하고, 그건 구조 변경이다 — 밤중에 혼자 정할 크기가 아니다.
* **영향**: 접속에는 무해. 재시작 시간만 늘어난다(배포마다 12초 + α).
* **물음**: 다음 배치 일감으로 잡을까, 아니면 "12초면 감수" 로 둘까.

## B-2. 클라 거울(`public/terrain.js`)에도 같은 선형 주사가 있다 — 옮길까

`public/terrain.js` 에도 같은 `_isPointInRiver` 가 있다(클라 거울). **서버만 고쳤다.**

* **왜 안 옮겼나**: ⓑ 는 답을 안 바꾸는 **순수 가속기**라 거울이 갈릴 여지가 없고(데이터 규약이 아니라
  탐색 순서만 다르다), 보고된 문제는 **서버 멎음**이지 클라 렌더가 아니며, env 로 켜지는 물건이라
  클라엔 켤 자리도 없다.
* **다만**: 클라도 같은 선형 주사를 한다 ⇒ **거기도 느릴 수 있다.**
* **물음**: 클라 프레임 문제가 실제로 있나? 있으면 **클라 프로파일부터** 재고 나서 옮기는 게 순서다.
  짐작으로 옮겨 심지 않았다.

---

## C. 실운영 33초 간격 — 한 번만 재 주면 닫힌다

`Dockerfile.zone` 은 `--interval=30s --timeout=3s --retries=3`.
실패한 검사가 3초를 쓰므로 **다음 검사는 33초 뒤**다 —
관측된 `16:00:08` · `16:00:41` 은 **연속된 두 검사**이지, 33초 주기의 멎음원이 아니다.

다만 실운영 게임일은 **24분**이라 게임일 틱만으로는 24분에 한 번만 멎어야 한다.
`unhealthy`(연속 3회 실패)가 붙으려면 한 번의 멎음이 **60초 이상**이어야 한다.
로컬 최악이 8.3초(마을 18·NPC 607)였고 실운영은 **마을 51·NPC 1,569** 이니 가능성은 충분하지만
**재지 않았으므로 단정하지 않았다.**

⇒ 보고서 §4-나 스크립트를 30분만 돌려 `STALL` 줄의 **간격**을 보면 된다.
**24분 간격이면** 게임일 틱이 맞고 이번 수리로 끝. **훨씬 짧으면** 원인이 하나 더 있다.

---

## D. 내가 이미 지킨 것 (확인용)

* 제품 코드 변경은 `zone.js` **12줄 추가**, `terrain.js` **17줄 추가 1줄 변경**, 신규 모듈 2개.
  게임 손잡이 튜닝 0건. econ·NPC 로직 무수정. 자산 파일 무변경.
* 기본값 무변경 — env 를 안 주면 **답이 원본과 같다는 걸 51,474점으로 대조해 확인**했다.
* 재민 눈이 판단할 것(소체 비례·아웃라인·1.5배 화소·애니·NPC 적용)은 **일절 손대지 않았다.**
* 푸시는 새 정본(§0)대로 세션이 맥 셸에서 했다 — **force 0회**, ff 아닐 때 멈추고 리베이스했다.

---

## E. 전체 회귀

* **기본값(둘 다 꺼짐)**: 46 통과 / 1 실패 — 실패는 `e2e-mtfoot` 8/9 기존 이월 건.
* **둘 다 켬**: `다음세션_인계.md` 3-멎 절에 결과 기록.
