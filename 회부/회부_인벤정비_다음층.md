# 회부 — 인벤 정비 다음 층 (2026-08-30 · 원장 승격·통일 목록 뒤)

> 이번 배치가 심은 것: `kgLedger` 개체 원장 승격(id·지목 제거) · 소비 정본 `consumeItem` ·
> 불변식 debug assert(쓸개 **이전**에 검사) · 통일 목록 컴포넌트(좀보이드 문법) ·
> 비네트 원인 축(아이콘·색조) · 유령 클라 감지(틱 미수신 → 예측 정지·표시·재연결).
> 아래는 재민이 **구현 금지**로 못 박은 것 + 만들며 **실측으로** 드러난 판단거리다.

---

## A. 재민이 명시적으로 회부한 것 (구현 금지 — §6)

### ⓐ 전면 인스턴스화 — **불필요로 판정한다**
지시서 §6: *"ⓐ 전면 인스턴스화(불필요 판정 — ⓑ로 충분한지 이 배치가 증명)"*.
**증명됐다.** 근거 셋:

1. **UI 가 요구한 것은 전부 ⓑ로 섰다.** 접힘·펼침·하위 줄 개별 드롭·부모 전량 드롭이
   `e2e-inv` 57/0 으로 화면에서 도달한다. `inventory` 스칼라 맵은 한 줄도 안 바꿨다.
2. **파급이 실제로 0 이었다.** ⓐ였다면 저장 blob·거래·요리·제작·NPC 경로가 전부 배열을 읽어야 했다.
   ⓑ는 소비 경로 10곳을 `consumeItem` 하나로 모으는 것으로 끝났고,
   3시드 800일 기준선이 **비트 동일**하게 남았다(econ 무접촉의 증거).
3. **페이로드도 안 터졌다.** 200마리 낚시 후 인벤 메시지 **5.4KB**(항목당 27.7B).
   전면 인스턴스화였다면 모든 품목이 항목 배열이 된다 — 잔가지 200개도 200줄이다. 뜻 없는 비용이다.

⇒ **닫는다.** 다시 열 이유가 생긴다면 그건 "벌크에도 개체 정체가 필요해졌을 때"인데,
그건 개체형 판정이 바뀌었다는 뜻이라 ⓐ가 아니라 **`noteInstance` 호출처를 늘리는 문제**다.

### 도구 슬롯 가산 구조 통일 — 재민 실기 체감 대기(기록지 ⑦)
`crude_axe` 의 `gatherWoodMult 2` 와 장비의 `+round(efficiency × 0.03)` 은 **다른 축에서 더해진다**.
실기 ⑦이 "조잡 ×2 → 정품 ×3" 을 어떻게 느끼는지가 나와야 통일 방향이 정해진다. 손 안 댔다.

### 게임 손잡이 튜닝 전부 — 기록지 대기
낚시 창·신체 감쇠·스프레드·용량(`CARRY_CAP_KG=25`)·제작 시간(3분). **한 값도 안 건드렸다.**

### 건축물 제작의 시설 게이트 · 부패 곡선 · P2P 거래 · 도구 econ 편입
전부 이월. 다만 이번 배치가 **자리를 더 파 뒀다**:
· 부패 곡선 — 로트 하위 줄이 이제 **화면에서 나이를 말한다**(`3개 6일 전`).
  곡선은 조회 함수 하나로 얹힌다(`Lots.view` 가 이미 `ageDays` 를 낸다). 남은 판단거리는 그대로:
  상하면 **버려지는가 / 값이 떨어지는가 / 먹으면 탈나는가** — 셋은 서로 다른 게임이다.
· P2P 거래 — 개체 지목이 서고(`drop_item {ids}`) 화면에도 도달했으므로,
  *"2kg 물고기를 지목해서 건넨다"* 가 이제 **UI 문법까지 준비된 상태**다.

---

## B. 이번 배치가 만들며 드러난 판단거리 (재민 확인 필요)

### B-1 ★상자는 개체 정체를 못 담는다 — 넣었다 빼면 표준 kg
`b.data` 는 품목→개수 **스칼라 맵**이고, 이 배치는 그걸 안 바꿨다(건물 blob·길드 곳간 회계가 걸린다).
지금 동작: 2.0kg 물고기를 상자에 넣으면 원장에서 FIFO 로 빠지고, 꺼내면 **표준 0.9kg** 로 돌아온다.
· 무게 총합은 항상 맞는다(불변식 유지 — `consumeItem` 이 인벤·원장을 같이 깎는다).
· 틀리는 건 **개체 정체**뿐이다.
★판단거리 셋 — ⓐ 상자에도 원장을 단다(건물 blob 확장 · 길드 곳간 회계와 정합 필요)
ⓑ 그대로 둔다("상자에 넣으면 뭉뚱그려진다"를 규칙으로 삼는다 — 청동기 곳간의 실제 모습이기도 하다)
ⓒ 개체형 품목은 **상자 반입을 막는다**(선명하지만 불편하다).
※지금은 화면이 **거짓말하지 않는다**: 상자 줄에는 ▶ 가 없다(하위 줄이 없는 게 사실이다).

### B-2 ★드래그 수정키(Shift=10 · Ctrl=99)가 사라졌다
재민 확정이 *"원래 한 줄을 드래그하면 모든 아이템이 드롭되고"* 라서, 부모 드래그가 **전량**이 됐다.
수정키로 10/99 를 얹던 옛 문법과 양립하지 않아 **뺐다**. 부분 이동은 지금 두 길이다:
· 버튼(↓/↑) = **1개** · 우클릭 메뉴 = 1개 / 10개 / **전부**.
★판단거리: 이걸로 충분한가, 아니면 **수량 입력창**(좀보이드의 "얼마나?" 창)이 필요한가.
실기에서 200마리 물고기를 옮겨 봐야 답이 나온다.

### B-3 ★도구 줄은 아직 통일 목록 밖이다
`toolItems`(도끼·톱·망치)는 **이미 개체**라 원장이 필요 없다. 다만 착용/단축키 버튼과
내구도 줄이 있어 `ul-row` 와 칸 구성이 다르다 — 그래서 이번 통일에 **안 넣었다**.
★판단거리: 도구도 같은 줄 문법으로 접고(`도끼 ×2`) 펼치면 개체별 내구도가 보이게 할 것인가.
지금은 도구가 3개만 돼도 목록이 길어진다. ※`__ulShape` 판정은 `data-toolid` 줄을 제외하고 본다.

### B-4 ★비네트 색조는 **첫 축**만 쓴다
3단계가 둘 이상이면 아이콘은 (§7 상한만큼) 여럿 뜨는데 화면 색조는 **가장 심한 축 하나**를 쓴다.
섞으면 진흙색이 되고, 나누면(사분면 색조) 아날로그 채널이 "최소만"이라는 §8.3 을 어긴다.
★판단거리: 지금(첫 축)이 맞는가, 아니면 다축이면 **중립 적색**으로 돌아가는 게 정직한가.

### B-5 ★유령 클라 문턱 5초 — `checkOrphan`(2초)과의 역할 분담
실측으로 확인한 것: `checkOrphan`(내 pid 2초 미수신)은 **정상적으로 자가 치유**한다.
그게 못 잡는 건 **재연결해도 틱이 안 오는** 상태다(실기의 그것 — `보정 0회`가 10초 넘게).
그래서 새 감시는 `lastTickAt` 을 본다(`checkOrphan` 이 리셋하지 않는 값).
문턱을 2초로 당기면 순간 끊김마다 딱지가 번쩍여 **오히려 화면이 거짓말**을 한다.
★판단거리: 5초가 맞는가. `GHOST_STALL_MS` 로 실기에서 바로 돌려 볼 수 있다.

### B-6 ★원장 페이로드 — 지금은 안 터진다(압축 회부 **불필요**)
지시서 §5 대리 지표 실측:

```
  200마리 낚시 후 인벤 메시지 = 5,541 B  (항목당 27.7 B · 상한 32KB 대비 17%)
```
낚시 30분(≈200마리)이 §B-2(무게 배치) 기준 최악 시나리오다. **압축 회부는 열지 않는다.**
다시 볼 지점: 항목이 1,000개를 넘거나(=27KB) 원장 품목이 여럿 겹칠 때.
※인벤 메시지는 **자기 자신에게만** 나간다(브로드캐스트 아님) — 인원수로 안 곱해진다.

---

## C. 이 배치가 지운 것 / 바꾼 것 (다음 세션이 헷갈리지 않게)

· `handleDrop(payload, target, amount)` **삭제** → `ulSend(d, target, amount|null)` 로 대체.
  짐 모양이 `{kind, item, cid}` → `{kind, item, cid?, n, ids?, lotDay?, giIds?}` 로 넓어졌다.
· `dragAmountFromEvent` **삭제**(B-2 참조).
· `rowsHtml` / 바닥 전용 `giRows` **삭제** → `ulRenderRows` 하나.
· `send(ws, {type:'inventory', …})` 직접 호출 **전부 삭제**(46곳) → `sendInventory(player, where)`.
· `player.inventory[x] -= n` 직접 차감 **전부 삭제**(10곳) → `consumeItem(player, x, n)`.
· 줍기가 **무조건** 원장을 만들던 것 삭제 — 이제 `gi.led` 가 있거나 kg 가 표준과 다를 때만.
  (옛 동작은 잔가지를 버렸다 주우면 벌크에 원장이 생겨 3층 캐논을 어겼다.)
