# 회부 — 지게 다음 층 (T12 · 2026-09-01)

> 이 배치가 **구현하지 않고 문서로만 남긴** 것들. 카드가 "회부만 할 것"으로 지정한 넷과,
> 작업 중에 드러나 재민 판정이 필요해진 것들.
> ★규약: 여기 적힌 것은 **가설이다.** 다음 세션은 §0 실측으로 다시 확인하고 시작해라
> (빈손 배치의 "정품 자작 경로가 없다"가 **있었다**는 전례).

---

## A. ★★재민 판정 — 가산이 숙련에 비례한다 (채택했다 · 되돌리려면 여기)

**지금**: 지게의 적재 가산이 **품질에 비례**한다 — Lv0 +8 · Lv10 **+20**(상한 25 → **45**).

**왜 그렇게 했나** — 카드는 "env 시작 20 = 25→45"라고만 적었고 고정인지 비례인지는 안 적었다.

1. **비례로 하지 않으면 화면이 거짓말한다.** 장비는 전부 `attrs`(방한 62 · 공격 100 …)를 갖고
   그 수가 화면에 뜬다. 지게만 고정 가산으로 두면 화면의 "적재 N" 과 실제 상한이 **다른 수**가 된다
   (족보 (83) 의 사촌 — 표에 넣는 것과 그 표를 읽는 쪽은 다른 명제다).
2. **레포 캐논**: *"도구는 문이 아니라 능률"*(빈손 배치). 지게도 도구다. 고정 가산은 "문"이다.
3. **재미 캐논**: 판단이 있어야 한다. 비례면 "지금 엮을까, 숙련 올리고 엮을까"가 판단이 된다.

**그리고 카드의 수(25→45)를 깨지 않았다** — `attrScale` 을 **유도**했기 때문이다:
`attrScale = CARRY_CARRIER_KG(20) / MAT_GRADE.wood(0.7) = 28.571` ⇒ **만렙 나무 지게가 정확히 +20**.
(20 을 그냥 박았으면 만렙이 +14 였다. 족보 (85): 설계 수는 정본 수와 만나야 뜻이 정해진다.)

**되돌리려면**: `player-items.js` 의 `carrier` 를 `attrs: {}` 로 두고 `carry.carrierBonus` 가
env 를 직접 읽게 하면 고정이 된다. 다만 **그러면 화면의 "적재" 칸이 사라지거나 거짓이 된다** —
그건 이 레포가 두 번 물린 자리다. 고정을 원하면 **화면 쪽도 같이 정해 달라**.

**손잡이**: `CARRY_CARRIER_KG`(기본 20 · 만렙 나무 지게의 가산). 이 하나만 움직이면 전 숙련이 같이 움직인다.

---

## B. ★★재민 판정 — 수레·동물 운반 (카드가 회부로 지정)

지게는 **사람 등** 층이다. 그 위에 두 층이 더 있다:

| 층 | 고증 | 구조적 문제 |
|---|---|---|
| **수레(달구지)** | 청동기 후기엔 아직 흔치 않다. 바퀴는 있었으나 사람이 끄는 썰매·발구가 더 현실적. | **길이 필요하다** — 지금 이속은 지형과 무관하다(`roads.js` 는 NPC 캐러밴 몫). 수레가 산길에서 지게와 같으면 그건 진행바다. |
| **소·말** | 소는 있다(`canTame` 계약). 말은 시대상 이르다. | **동물이 플레이어를 따라오는 구조가 없다.** 그리고 짐승은 먹여야 한다 ⇒ 새 축. |

⇒ 둘 다 **이 카드의 한 줄로 못 얹는다.** 특히 수레는 **길 층**이 먼저다. 새 아크 후보로 올린다.

**중간 착지 후보**(한 카드에 들어갈 만한 것): **지게 두 벌**? 아니다 — 슬롯이 하나다.
차라리 **`발구`(썰매)** — 겨울에만 쓰는 운반구로, 눈·언 땅에서만 가산이 붙는다면
계절이 판단을 만든다(온도 정본이 이미 있다 · T5). 이건 한 줄이 아니라 한 카드다.

---

## C. ★★T22 로 — 도구 슬롯 가산 구조 통일 (카드가 회부로 지정)

**§0 실측: "도구 슬롯 가산 구조"는 없었다.** 지금 세 가지가 **따로** 논다:

1. `toolItems` + `equipped` + `hotkey1` — **손에 든 도구**(도끼·곡괭이). 내구 `d/max`. 효율 배율.
2. `equipment` + `equipSlots` — **입는 장비**(옷·갑옷·무기·도구·**지게**). 내구 `dura/durMax`. 품질 `q`.
3. `player.dishes` — 요리 인스턴스.

⇒ **도구가 두 층에 있다**: `RECIPES.axe`(맨손·`toolItems`)와 `EQUIPMENT_RECIPES.tool`(작업대·`equipment`).
빈손 배치가 "조잡한 석기는 맨손" 계약을 세우며 갈라 둔 것인데, 지금은 **정품 도끼와 정품 '도구'가 다른 물건**이다.

이 배치는 그 통일을 **안 했다** — 지게를 ②에 얹었을 뿐이다. 통일하면 건드리는 자리:
`doCraft`/`doCraftEquipment`/`doEquip`/`doEquipItem`/HUD 핫키/인벤 목록/저장 스키마. **한 카드 몫이다.**

★그리고 통일할 때 **이 배치가 만든 `extra` 문법을 표준으로 올려라** — 재료가 둘인 물건은 지게가 처음이 아니다
(조잡한 석기가 이미 `{pebble, twig, fiber}` 다). 두 표가 같은 문법을 쓰면 통일이 반은 끝난다.

---

## D. ★재민 확인 — 지게 착장 시각 (카드가 회부로 지정)

지금 **지게를 져도 캐릭터 그림이 안 바뀐다.** 인벤·HUD 에만 뜬다.
`41-h-char.js` 의 `drawPlayerIso` 가 손에 든 도구는 그리는데(`getEquippedInstance`) 등짐은 없다.

* 클라 접촉이고 **T0-b 로 파일이 갈린 직후**라 이 배치는 손대지 않았다.
* 붙일 자리: `drawPlayerIso` 의 `opts` 에 `back: 'carrier'` 한 비트 + 등 뒤 A자 실루엣.
* ⚠**스프라이트가 없다** — 건조대·작업대·소금가마와 같은 처지(벡터 발판 도형).
  ⇒ 백로그의 **"시설 스프라이트"** 항목에 지게를 같이 얹는다.

---

## E. ★재민 확인 — 유도값 다섯 (조용히 정한 수는 못 고친다)

| 값 | 채택 | 근거 |
|---|---|---|
| 지게 무게 | **3.00kg** | 재료 합 6.6kg(통나무 2 + 풀 2)의 절반. `pillar`(9→6)·`crude_axe`(2.2→1.4)와 같은 "깎아 낸 몫". 실물 A자 지게가 3~5kg. |
| 재료 | **통나무 2 + 풀 2** | 가지 둘을 A자로 세우고 세장을 지른 뒤 밀삐(짚 끈)로 동여맨다. `fiber` 는 조잡한 석기가 이미 쓰는 그 끈이다. |
| 내구 base | **100** | 옷(120)보다 짧다 — 나무 틀보다 **밀삐가 먼저 삭는다**. |
| 마모 | **2분당 1** | 자염 원정(왕복 32분)을 7~9회 견딘다. 옷과 같은 30초당 1이면 원정 1.8회 = 소모품. |
| 제작 시간 | **3분** | `CRAFT_TOOL_MS` 재사용(새 손잡이 0). 작업대의 다른 물건과 같은 리듬. |

---

## F. ★★드러난 것 — 상한이 커지면 **다른 상한이 병목이 된다**

지게 Lv10 이면 45kg 을 진다. 그런데:

* **상자**(`CHEST_ALLOWED_ITEMS`)는 품목 화이트리스트지 무게 상한이 없다 — 무한이다.
* **거래소**는 물리 상한(재고×0.25)이라 무게와 무관하다.
* **곳간·움집**도 무게 상한이 없다.

⇒ **무게 상한을 가진 건 사람뿐이다.** 그래서 지게가 곧바로 체감된다(좋은 일이다).
다만 다음 층에서 **집에 쌓아 둘 수 있는 양**에 상한을 주려 한다면, 그 순간 지게의 뜻이 또 바뀐다.
"얼마나 나르나"와 "얼마나 쌓나"는 다른 축이고 **지금은 후자가 없다**는 걸 적어 둔다.

---

## G. ★한 줄짜리 잔무 — 다른 세션 몫

* **`_facilityRecipes` 의 `missing` 을 한글로 바꿨다**(이 배치에서 처리 · 종전엔 `bronze 0/4`·`fur 0/3` 이 영문으로 떴다).
  ★남은 것: **재료 버튼 라벨**은 아직 영문이다(`wood 28%` · `meteoric_iron 36%`). 같은 족이고 클라 한 줄이다.
* **`carrier` 아이콘 없음** — 장비는 `EQUIP_ICONS[type] || '🎒'` 폴백이라 **🎒 로 뜬다**(우연히 맞는 그림이다).
  404 도 안 난다(`ITEM_ICONS` 를 안 탄다). 고유 아이콘을 주려면 `EQUIP_ICONS` 에 한 줄.

---

## H. 이 배치가 **안 건드린 것** (다음 세션이 착각하지 않게)

* **`MOVE_CURVE`·`STAGE_AT`·바닥·피로** — 한 수도 안 건드렸다. 상한만 옮겼다.
* **econ** — diff 0줄. 지게는 econ 재화가 아니고 NPC 는 이 층을 안 쓴다.
  ⚠**NPC 캐러밴 무게 예산**(회부 M항)은 여전히 열려 있다 — 캐러밴은 아직 무게를 안 진다.
* **`toolItems` 층** — 손에 든 도구 계통은 한 줄도 안 고쳤다(C 항 참조).
* **인벤 3층 캐논** — 지게는 새 층이 아니라 장비 인스턴스다.
