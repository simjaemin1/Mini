# 회부 — 일틱 조각의 바닥, 그 다음 (2026-09-01 · T41)

> T1 이 하루를 조각으로 나눴고, 바닥은 **조각 하나의 크기**로 남았다. 이 배치는 그 셋을 팠다.
> ①은 고쳤고(일틱의 56% 소멸), ②③은 **원인 귀속까지 하고 손대지 않았다** — 아래가 그 이유다.
>
> ★목표(조각 최대 ≤ 예산 16ms × 2)에는 **못 닿았다.** `e2e-rtt` 의 `≤×2` 는 pass/fail 에
> **복귀시키지 않았다**(T1 회부 B-1 의 조건 미충족). 남은 바닥의 수치는 §D 에 있다.

---

## A. §0 실측 — 셋의 원인 귀속

`/perf` 에 계측기를 달아 마을 50곳 · econ day 515 세계를 10~12일 돌린 값이다.

### A-1 `_lifeAddHouseSite` — **하루 20곳이 훑고 성공 0**
```
  12일 누적: 호출 238 · 성공 **0** · 빈손 238 · 마을 20곳 · 합 20,902ms(마을 한 곳 최대 1,085ms)
  거부 사유 248,191건:
    기존 집과 너무 가까움(<18)   228,972   **92.3%**
    마을 영토 밖                  17,149     6.9%
    개간 농지 위                   1,795     0.7%
    물가 완충 침범(침수)             275     0.1%
```
⇒ 집 간격 제약(HG 18) 때문에 그 영토 안엔 자리가 **구조적으로 없다.** 내일 훑어도 같은 답이다.

### A-2 캐러밴 A* — **캐시는 이미 있었다**
지시서 ②의 앞 절반("마을 쌍 경로 캐시 — lazy · 지형 변경에서 무효")은 **이미 구현돼 있다**:
`getRoute` 가 `state.routeCache`(Map, 무방향 쌍 키, `null` 도 캐시)로 받고,
`invalidateTradeDistances`(벽·울타리 설치/철거)가 통째로 비운다.
```
  10일 누적: 콜드 51회 · 적중 53회 · 무효화 0회 · 합 7,996ms
             한 번 최대 **1,265~1,695ms** · 평균 157ms
```
⇒ 남은 것은 **콜드 계산 자체**다(§C).

### A-3 `_oreMineDaily` 꼬리 — **마을당 1회, 부팅 워밍업**
```
  콜드 스캔 50회 = 마을 수. day3 이후 **한 번도 안 늘었다**(캐시 `vil._oreCells` 가 이미 있다).
  합 4,316~4,545ms · 한 번 최대 755ms
  스캔 범위: 반경 ORE_LABOR_R=150 셀 원판 ≈ 70,686칸 × (isOre·isWater·isRock·oreProbAt)
```
⇒ "최대 4,260ms" 는 **반복 비용이 아니라 부팅 1회**다. 상한을 박을 자리가 아니다(§D).

### A-4 결정론
셋 다 같은 입력에 같은 답을 낸다 — 캐시가 정확해야 하는 근거다.
집터는 `_terrSet`·`_potSet`·`_farmSet`·`_houseCells`·`_granList`·`_ditch`·`_site`/`_psite`·지형 차단의
함수이고 난수를 안 쓴다. 경로는 격자 A*(`sim/path-core.js` 정본)라 같은 격자·같은 출발/도착이면 같다.
광맥 셀 목록은 지형 술어의 함수다.

---

## B. ①이 한 것 — "못 찾은 자리는 기억한다"

**언제 다시 찾아도 되는가 = 후보가 늘어날 수 있을 때만.**
`reject` 가 보는 상태 여덟 중 여섯은 **줄기만** 한다(= 더 거부된다):
`_farmSet`↑ · `_houseCells`↑ · `_granList`↑ · `_ditch`↑ · `_potSet`↓(strict 는 합집합이라 불변) ·
`_site`(사라질 땐 `_houseCells` 로 옮겨 앉아 제약이 남는다).
후보를 **늘릴 수 있는** 사건은 셋뿐이다:
1. `_terrGrow` 가 실제로 셀을 더했을 때 — 새 셀 = 새 후보 → **표지**(거부 캐시는 유지)
2. `_psite`(의뢰 집터) 소멸 — 그 반경 18 이 다시 열린다(마을 집과 달리 `_houseCells` 에 안 들어간다) → **리셋**
3. 지형 차단 완화(벽·다리 철거) — `invalidateTradeDistances` 가 이미 그 훅이다 → **리셋**
   (+ `_potSet` 에서 **삭제만** 하는 두 지점 — 의뢰 터 선점·집터 선점 — 도 리셋. strict `farmAt` 이 뒤집힌다.)

여기에 둘을 더 얹었다:
* **거부 캐시**(`_siteRejS`/`_siteRejL`) — 한 번 거부된 셀은 리셋 전까지 다시 `reject` 를 안 부른다.
  strict/loose 는 `farmAt` 이 달라 판정이 다르므로 집합을 나눴다.
* **안전망은 마을마다 다른 날에** — `(day + dbId) % N`. 처음엔 전부 같은 날 빈손이 되어
  7일마다 20곳이 **한꺼번에** 재스캔했고 그날이 7.8초로 튀었다(실측). 흩으니 하루 마을수/N 곳만 돈다.

```
  실측 (같은 DB 스냅샷 · 9일 · 마을 50곳)
                     집터 호출   스캔한 셀    life:site 중앙   1마을:site 중앙
    종전(MEMO=0)        177      1,227,510      1,209ms          205ms
    채택(기억 ON)        54        376,710          **6ms**        **2ms**
  ⇒ 집터 단계가 **99.5% 사라졌다.** 일틱의 56% 였다.
```

**같은 답인가** — `LIFE_SITE_AUDIT=1` 이 캐시로 건너뛴 셀을 **실제로 다시 판정**한다:
**35,819회 재판정 · 위반 0.** 단조성 논증이 런타임으로 증명됐다.
(선택된 집터 로그 비교는 이 세계에서 성공이 0 이라 **자명 통과**다 — 그래서 감사가 본검사다.)

---

## C. ②는 왜 손대지 않았나 — 재개 가능 A* 는 **회부**

콜드 한 번이 최대 1,695ms 다. 지시서가 연 길("open set 을 들고 양보")은 두 가지를 요구한다:
1. **`sim/path-core.js` 의 `routePath` 를 재개 가능하게** 고쳐야 한다. 이 파일은 랩 `tradePath` 와
   **같은 정본**이다(모듈 주석). 공유 캐논을 성능 목적으로 상태기계로 바꾸는 건 이 배치의 크기를 넘는다.
2. **캐러밴 실체 생성이 여러 프레임에 걸쳐 지연된다.** `spawnCaravanBody` 는 지금 경로를 **동기로**
   받아 즉시 `setBodyPts` 한다. 재개 가능해지면 그 사이 캐러밴이 "아직 몸이 없는" 상태로 존재한다 —
   **행동 변경**이고, 이 배치의 절대 규칙에 걸린다.

★더 싼 길 둘(둘 다 새 층이라 판단이 필요하다):
* ⓐ **경로를 DB 에 영속** — 재기동마다 1,225쌍을 다시 덥히지 않아도 된다. 무효화 규약이 이미 있으므로
  저장·복원만 붙이면 되고 결과는 정의상 동일(같은 A* 결과를 꺼내 쓸 뿐). **새 저장 층**이 생긴다.
* ⓑ **부팅 직후 유휴 프레임에 선계산** — 조각을 옮길 뿐 총량은 그대로. 관측자 없는 시간에 미리 덥힌다.

★그리고 별건으로 하나 더: `invalidateTradeDistances` 는 캐시를 **통째로** 비운다.
플레이어가 울타리 하나 세우면 1,225쌍이 전부 식는다. 정확한 선택적 무효화는 **차단 방향에서만** 가능하고
(개통은 어떤 쌍이든 짧아질 수 있다 — 모듈 주석이 이미 그렇게 적어 뒀다), 실측에서는 무효화가
0회였다(플레이어 없는 판). 실기 빈도를 재기 전에는 손대지 않는 게 맞다.

---

## D. ③은 왜 손대지 않았나 — 부팅 워밍업이고 **반복이 없다**

`_oreCellsOf` 는 이미 `vil._oreCells` 로 마을당 1회만 돈다. 실측이 그걸 확인했다(day3 이후 증가 0).
꼬리 4,260ms 는 **부팅 후 첫 하루에 50곳이 한 번씩** 도는 합이고, 슬라이서가 마을 경계로 나눠
한 조각 최대는 755ms 다. 지시서의 경고("원인 서술 없이 상한을 박지 마라")대로 상한을 안 뒀다.
줄이려면 §C-ⓐ 와 같은 부류(영속 캐시)이거나 스캔 자체를 바꿔야 하는데, 후자는 **다른 셀 집합**을
낳을 수 있어 "같은 답을 더 싸게"가 아니다.

---

## E. 남은 바닥 — 목표(조각 ≤32ms)까지 **아직 멀다**

```
  ①을 넣은 뒤 조각 최대 후보(실측)
    캐러밴 콜드 A*      최대 1,265~1,695ms   ← ★새 바닥 (§C)
    집터 첫날 캐시 빌드  최대   ~1,050ms      부팅 1회 (그 뒤 2ms)
    광맥 콜드 스캔       최대     755ms       부팅 1회 (§D)
    생활층 헤드리스 결산 중앙     226ms       ★매일 — 다음 후보
    곳간 증설(_lifeGranAdd) 중앙  70ms/마을   ★매일
```
⇒ **목표 복귀 조건(조각 최대 ≤32ms)을 못 채웠다.** `e2e-rtt` 의 `≤×2` 는 pass/fail 밖에 그대로 둔다.
   복귀시키려면 §C 를 먼저 끝내야 하고, 그다음 바닥은 **매일 도는** 헤드리스 결산(226ms/마을)이다.

---

## F. 판단거리 (재민 확인 필요)

### F-1 안전망 주기 7일이 맞는가
표지를 놓쳐도 7일 안에 회복한다 — 비용은 1/7. 짧게 하면 안전하고 비싸다.
실측: 표지를 **통째로 끈** 픽스처(`LIFE_SITE_NODIRTY=1`, N=3)에서도 하루 7.9곳이 돌아 회복했다.
★집이 늦게 서는 최악은 7일이다. 하루가 24분이니 실시간 2.8시간이다.

### F-2 집터가 **구조적으로 없는** 마을 20곳 — 그게 의도인가
성공률이 12일 내내 0 이었다. 원인은 집 간격 `HG=18` 이고, 그 마을들은 영토를 넓혀도
간격 때문에 못 짓는다. 지금은 **인구가 수용력의 0.92 를 넘어도 집이 안 늘어난다.**
* ⓐ 그대로 — "빽빽한 마을은 더 못 짓는다"는 말이 된다(현행)
* ⓑ `HG` 를 영토 밀도에 따라 낮춘다 — 마을이 촘촘해진다(고증 판단)
* ⓒ 수용력 초과를 다른 방식으로 푼다(분촌 — 큰 설계)
★이 배치는 **성능만** 만졌다. 위 셋은 마을 설계 판단이라 손대지 않았다.

### F-3 손잡이 넷 (env · 기본값 = 채택값)
| 손잡이 | 기본 | 뜻 |
|---|---|---|
| `LIFE_SITE_MEMO` | **1** | `0` = 종전 동작(매일 전수) — **되돌리는 스위치이자 하네스 대조군** |
| `LIFE_SITE_RESCAN_DAYS` | **7** | 표지를 무시하고 전수로 도는 안전망 주기(마을마다 다른 날) |
| `LIFE_SITE_AUDIT` | 0 | `1` = 건너뛴 셀을 다시 판정해 단조성을 런타임 증명(**느리다** — 하네스 전용) |
| `LIFE_SITE_NODIRTY` | 0 | `1` = 표지를 일부러 안 세운다(안전망 픽스처 — 하네스 전용) |
