# 회부 — 사건 유형 2차(T50) 다음 층으로

작성: 2026-09-02 · 세션6 · 카드 T50(영역 E) 완료 시점
만든 것의 규약: `인계/E-사건장부.md` **4-유형2** 절 · 누적 회부: `인계/회부.md` **0-유형2** 절

---

## 0. 이 문서가 있는 이유

T50 은 "사건 유형을 늘려라"가 아니라 **"이미 일어나고 있는 일에 장부가 눈을 뜨게 하라"** 는 카드였다.
그래서 §0 의 절반은 **원천이 없는 후보를 골라내는 일**이었고, 골라낸 목록이 곧
**"세계에 아직 없는 일"의 목록** = 설계 백로그다. 그 목록이 이 문서의 본문이다.

원칙: **각본 금지.** 습격 사건을 넣으려고 습격을 만들지 않는다.
원천이 없으면 사건도 없고, 없다는 사실을 **적는 것**이 이 카드의 산출물이다.

---

## 1. 원천이 없어 못 만든 것 — 설계 백로그

### 1-1. 마을 소멸(확정) — **관측자가 없다**

붕괴 **위기**는 `POP_COLLAPSE` 로 잡힌다(자기평균 대비 −30%). 그런데 "그 마을이 끝내 비었다"는 못 낸다:
`scanDay` 가 `if (!v.npcs || v.npcs.length === 0) return;` 로 **사람 없는 마을을 건너뛰기** 때문이다.
그 줄은 실수가 아니라 규약이다 — **장부는 마을의 관측이고, 사람이 없으면 관측자가 없다.**

⇒ 소멸을 사건으로 내려면 **"마을 밖의 눈"** 이라는 새 개념이 필요하다. 후보 셋:
* ⓐ 이웃 마을이 관측자가 된다(캐러밴이 가 보니 비어 있더라 — 도달표와 잘 맞는다)
* ⓑ 소멸을 **호스트가 알려 준다**(`extra.builds` 와 같은 자리 — `bandits.js` 가 이미 해체를 판정한다)
* ⓒ 장부가 "어제 있던 마을이 오늘 없다"를 스스로 본다(마을 목록의 에지)
ⓑ 가 제일 싸다(`bandits.js daily()` 의 `formGang(… '해체 …')` 한 줄 옆). **다만 도적 층 소유라 안 건드렸다.**

### 1-2. 봉쇄(blockade) — **큐를 아무도 안 채운다**

`sim/economy-sim.js processEvents` 는 `world.events` 에서 `{type:'blockade', target, duration}` 을 읽어
마을을 `isolated` 로 만든다. 완성된 기능이다. 그런데 **이 존에서 그 큐에 넣는 코드가 없다**(랩 800일 0건).
전쟁 층(X)이 그 큐를 쓰기 시작하면 장부 쪽 배선은 **`v.isolated` 에지 한 줄**이다(`deeds()` 안에).
지금은 원천이 없어 안 만들었다.

### 1-3. 동사·부상 사망 — **econ 은 죽는 이유를 모른다**

econ 의 사망은 `npcs.splice` 하나뿐이고 원인이 붙지 않는다(아사·토벌 전사·도적 이탈이 전부 같은 모양).
T43(쓰러짐)·T44(극단 HP)가 착지해야 "왜 죽었는가"가 생긴다. 그 뒤에 별도 카드로.

### 1-4. 캐러밴 실종 — **후보 탈락(발명 금지)**

지시서가 "CARAVAN_LATE 와 구분되는 실제 사건이 있나(없으면 후보 탈락)"이라고 물었다. 답:
* `_abandoned`(빈손 귀환)는 **경제 판단**이다 — 값이 안 맞아 돌아온 것이지 실종이 아니다. 사건 아님.
* 진짜 실종은 `tradeStats.tradersKilled`(도적에게 죽어 화물 전손)이고, 그건 **`TRADER_KILLED` 로 등재했다.**
⇒ 별도의 "실종" 유형은 만들지 않았다.

---

## 2. 다른 영역에 넘기는 것

### 2-1. [E — 설계 판정] 의뢰 유형 신설

새 일곱 중 **의뢰로 번역되는 것이 하나도 없다.** 지금 의뢰는 "물건을 가져오면 물건으로 갚는다" 하나뿐이라:
* 약탈 → "순찰 동행"(사람이 같이 걷는 의뢰)
* 완공 → "일손"(노동을 내는 의뢰)
* 흉년 → "종자 나눔"(다른 마을에서 실어 오는 의뢰 — 지금 것과 같은 모양이나 **출발지가 다르다**)
셋 다 **새 의뢰 유형**이라 카드가 따로 필요하다(지시서 §4 회부 그대로).

★그런데 **흉년·붕괴는 이미 이어져 있다**: 소출이 줄면 식량 재고가 마르고, 그 부족 래치가
기존 게시판 의뢰를 스스로 건다. 새 배선 없이 사슬이 닿아 있다는 것은 이번에 확인했다 —
즉 "흉년이 들면 게시판에 곡식 의뢰가 는다"는 **지금 이미 참이다.**

### 2-2. [T 거래 / econ] `world.tradeLog` 5000행 상한

`FIRST_GOODS` 는 **오늘 날짜 행만** 뒤에서 훑는다(하루 수십 행 — 무해). 하루 5000행을 넘기는
세계가 오면 조용히 몇 건을 놓친다. 상한(`tickWorldV2` 끝)은 econ 소유라 안 건드렸다.

### 2-3. [E / R2] 연표 화면이 "일"을 아직 구분해 보여 주지 않는다

서버는 이제 연표 줄마다 `type`·`deed`·`sev` 를 실어 보낸다(클라 무접촉 조건이라 화면은 안 고쳤다).
지금은 전부 🕰️ 한 아이콘이다. 🌵가뭄 · ☠️흉년 · 🏘️완공처럼 **유형별 표시**를 붙이면
연표가 훨씬 잘 읽힐 것이다 — 클라 소유 영역 몫.

### 2-4. [E] 소문 왜곡(T34) — 여전히 유예

새 유형도 **도달표를 그대로 탄다**(유형별로 나누지 않았다 — `e2e-rumor ⑨` 가 그 실증).
왜곡을 넣는 순간 "도달은 거리의 함수"가 깨지므로 재민 판정 뒤에.
★다만 이번에 후보가 하나 늘었다: **"일" 유형은 왜곡되기 쉽다**(흉년이 기근으로, 약탈이 전쟁으로).
값 유형(소금값 9배)은 숫자라 덜 변한다. 왜곡을 넣는다면 **일 유형부터**가 자연스럽다.

### 2-5. [E — 판정] `EV_DEED_TYPES` 는 손잡이가 아니라 **설계**다

일 유형이 연표 sev 문턱을 면제받는 근거는 **실측된 드묾**(값 18,434 대 일 1,021 = 5.2%)이다.
이 목록에 흔한 유형을 넣으면 그 순간 연표가 그것으로 덮인다.
⇒ 유형을 새로 더할 때는 **`ev-sources ⓐ` 로 빈도를 먼저 재고** 이 목록에 넣을지 정해야 한다.
그 절차를 규약으로 굳힐지가 판정 대기.

---

## 3. 이번에 안 건드린 것(조건상)

* **econ `sim/` — diff 0줄.** 풍흉·날씨·약탈 누계를 **읽기만** 했다.
  (T7 이 잡은 `c.travelDays` 자기모순도 그대로다 — `인계/회부.md` 0-소문 2번.)
* **클라 `public/` — diff 0줄.** 연표·게시판·브리핑이 새 유형을 자동으로 받는다는 것이
  이번 배치의 검증 항목이었고(§0), `e2e-rumor ⑨`·`e2e-chronicle ⑪` 가 실화면에서 확인했다.
* **DB 스키마 — 변경 0.** `village_chronicle` 은 `meta` 를 안 담는데, 그 제약에 맞춰
  **문장이 `vid·day·type·item·mag` 만 쓰도록** 유형을 설계했다(㉝ 이 그 계약을 검사한다).
