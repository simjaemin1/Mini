# 회부 — 마을 거래소 다음 층 (2026-08-27 · 물물교환 시제품 뒤)

> 이번 배치가 심은 것: 그림자가격 비율 = 교환비 · 스프레드 10% · 물리 상한(B-1 **같은 함수**) ·
> 조각 적분(`planSliced`) · 금지·잠금 품목 · 원자적 실행 · 곡식 환산 표시 · 260px 게이트 ·
> §8 프레임의 둘째 칸(Shift+T).
> 아래는 재민이 **구현 금지**로 못 박은 것 + 만들며 **실측으로 드러난** 판단거리다.

---

## A. 재민이 명시적으로 회부한 것 (구현 금지)

### A-1 곳간 인출
지금 플레이어는 곳간에 **넣기만** 한다(`playerVillageDeposit`). 인출이 생기면 "내 창고"가 되고,
그 순간 거래소의 스프레드가 무의미해진다(넣고 빼면 공짜 보관). 인출은 **소속·권리** 문제라 A-2 와 한 묶음이다.

### A-2 마을 소속 / 멤버십
"이 마을 사람인가"가 정해지면 값·상한·인출이 전부 그 위에 걸린다. 지금은 **누구나 같은 값**이다.
★지금 구조에서 붙일 자리: `trade.js CFG.SPREAD` 를 상수에서 **함수(vid, player)**로 바꾸면 되고,
그 외의 코드는 손댈 필요가 없다(비율은 전부 스프레드 한 곳을 지난다).

### A-3 거래소 **실체 건물**
지금 거래소는 **마을 중심 260px** 이라는 보이지 않는 원이다(브리핑과 같은 게이트).
건물이 서면 게이트가 "그 건물 앞"으로 좁아지고, 마을 배치·동선이 콘텐츠가 된다.
⚠**고증 긴장**은 아래 B-1 에 따로 적었다 — 건물을 세우는 순간 그 판단이 필요하다.

### A-4 흥정 · 평판 할인
스프레드를 사람마다 다르게 만드는 층. A-2 와 같은 접점(위 참조).

### A-5 원거리 위탁 · 캐러밴 동승
지금은 **걸어가야 보인다**(§3.2 정보 비대칭 = 콘텐츠). 위탁·동승은 그 비대칭을 **돈 주고 사는** 층이라
정보 설계와 함께 봐야 한다. ★원격 조회 API 를 만들지 않는다는 규약은 그때도 유지된다 —
위탁은 "멀리서 보는 것"이 아니라 "짐을 맡기는 것"이어야 한다.

### A-6 시세 기록 / 차트 UI
지금은 **지금 값 한 줄**뿐이다. 기록이 생기면 "쌀 때 사서 비쌀 때 판다"가 성립하고,
그건 §8.4 이력서 패널·낚시 `fishStats` 와 같은 "내 기록" 층이다. 데이터는 아직 안 쌓고 있다.

### A-7 MSY 상한 A/B · 낚시·신체 손잡이 튜닝
재민 실기 대기. (`회부_MSY상한_사장.md` · `회부_낚시_다음층.md` · `회부_신체UI_다음층.md`)

---

## B. 만들며 드러난 판단거리 (재민 확인 필요)

### B-1 ★장마당 고증 — "거래소"라는 말이 청동기에 맞는가
이 배치는 재민 원문 *"마을마다 거래소가 있어서…"*를 그대로 실장했다. 다만 고증 긴장을 적어 둔다:

* 송국리기 한반도에 **상설 시장(장마당)의 고고학적 증거는 없다.** 교환은 위세재 증여·재분배·
  원거리 원료 이동(옥·석기 원석)으로 나타나고, 그건 **중심지(회관·수장)를 통한 재분배**에 가깝다.
* 지금 실장이 실제로 하고 있는 일도 **그 재분배**다 — 플레이어는 마을 **곳간**에 물건을 넣고
  곳간에서 물건을 받는다(`playerVillageDeposit` 정본 하나). 즉 **기능은 고증에 맞고 이름만 근대적**이다.
* ★판단거리: ⓐ 이름을 바꿀 것인가(예: **곳간 · 회관 · 나눔터**), ⓑ A-3 에서 세울 "건물"이
  독립 시장 건물인가 **회관/창고**인가. ⓑ 를 정하면 ⓐ 는 따라온다.
* 참고: 화면에는 이미 **"마을 몫 10%"**로 적어 뒀다 — 상인의 마진이 아니라 **공동체 몫**의 어법이다.
  (스프레드의 근거 ⓑ "교역 세금 = 물물" 캐논과 같은 결.)

### B-2 ★곡식(`food`)이 **교환 품목이 아니다** — 기준 품목인데 손에 못 쥔다
실측: 곳간 대응표(`PV_DEPOSIT_MAP`)에 `food` 를 가리키는 **플레이어 아이템이 없다.**
그래서 지시서의 "물고기→**곡물**"은 문자 그대로는 실행 불가고(`e2e-trade` 머리 주석에 적어 뒀다),
곡식은 **표시 기준(numeraire)으로만** 산다. 화면에는 "시세는 곡식 환산"이라 떠 있는데
정작 **곡식은 표에 없다.**
★판단거리: 청동기 사실상의 화폐를 플레이어가 **들 수 있게** 할 것인가(예: `grain` 아이템 — 자루/되).
들 수 있게 되면 ⓐ 곡식이 진짜 교환 매개가 되고 ⓑ §7 배고픔 축과 직결되며(생곡식 → 조리)
ⓒ 게시판 보상 품목으로도 나갈 수 있다. 지금은 그 셋이 다 막혀 있다.
⚠이건 **econ 변경이 아니다**(econ 은 이미 `food` 를 안다). 아이템·제작·조리 표 쪽 일이다.

### B-3 ★가격 **바닥에 붙은 품목**은 아무리 팔아도 안 움직인다
`computeShadowPrices` 는 희소도를 `PRICE_ADJ_MIN..maxAdj` 로 클램프한다. 과잉 재고 품목은
바닥에 붙어 **재고가 더 늘어도 값이 그대로**다(글럿 바닥의 존재 이유 그 자체다).
실측(농촌1): 생선 0.004734 · 고기 0.008104 — 12마리를 넣어도 **소수점까지 그대로**.
같은 마을에서 약초는 1.02 → 0.3853 으로 크게 움직였다.
★판단거리: 플레이어 경험으로는 *"내가 아무리 팔아도 값이 안 변한다"*가 된다. 셋 중 하나다 —
ⓐ 그대로 둔다(정직하다: 남아도는 걸 더 갖다 줘도 마을은 안 반가워한다),
ⓑ 화면이 **그 사실을 말한다**("이 마을엔 이미 남아돈다 — 값이 더 안 내려간다"),
ⓒ 바닥 근처에서 마을이 **아예 안 사준다**(그러면 물고기 처분처가 사라진다 — 위험).
※ 지금은 ⓐ. 회부 이유: ⓑ 는 문구 하나지만 **econ 내부 상수(PRICE_ADJ_MIN)를 화면이 알아야** 하고,
그건 사본 규약과 닿는다(정본이 "바닥이다"를 알려 주는 접근자가 필요하다).

### B-4 ★하루 캐시 vs 지금 시세 — **비대칭이 남아 있다**
이 배치에서 고친 것: 거래소의 표시·견적은 **지금 재고**로 낸다(`Events.pricesFresh`).
고치기 전에는 하루 캐시(`pricesOf`)를 봐서, 재고가 ±30% 움직여도 화면이 소수점까지 안 변했다
(`e2e-trade ⑤` 가 실측으로 잡았다 — 이 배치의 존재 이유가 화면에서 사라지고 있었다).
**NPC 교역은 그대로 하루 캐시를 읽는다**(그 경로는 한 줄도 안 건드렸다).
⇒ 하루 안에서 **플레이어가 아는 값과 NPC 가 쓰는 값이 다르다.**
★판단거리: 이게 콘텐츠인가 결함인가. 콘텐츠로 보는 근거 — 실제 시장도 정보가 시차를 두고 퍼진다.
결함으로 보는 근거 — 플레이어가 재고를 크게 흔든 뒤 그날 NPC 교역은 **옛 값으로** 거래한다.
※ 지금 상태로 **차익이 나지는 않는다**(`planSliced` 곡선 적분이 왕복 이득을 막고, `test-trade ②`
대조군 s=0 이 그걸 실증한다). 그래서 회부로 남긴다 — 급하지 않다.

### B-5 물리 상한 때문에 **작은 마을에선 거래소가 사실상 안 열린다**
"마을이 팔 수 있는 양" = `floor(재고 × 0.25)`. 재고가 4 미만이면 **0** 이다.
실측 화면(농촌1): 10품목 중 마을이 내줄 수 있는 게 있는 건 4품목뿐이었다(익힌음식 0 · 광석 0 · 철 0 …).
★판단거리: 이건 B-1 의 "마을은 없는 걸 못 준다" 캐논이 그대로 나타난 것이라 **옳다**. 다만
플레이어에게는 "거래소가 대부분 비어 있다"로 보인다. ⓐ 그대로, ⓑ 0 인 줄을 흐리게/접어서
표를 짧게, ⓒ `TRADE_STOCK_FRAC` 을 거래소만 따로(0.25 → 0.4). ⓒ 는 **B-1 과 같은 함수를 쓴다**는
이번 규약을 깨지 않는다(함수는 그대로, 계수만 다른 인자) — 그래도 재민 확정 사항이다.

### B-6 ★T/Y 플레이어끼리 교환(`trade_offer`)이 **경제를 통째로 우회한다**
발견: `t`/`y` 키가 가장 가까운 플레이어와 **나무 1 ↔ 돌 1** 을 맞바꾼다. 상대 **동의 절차가 없고**,
값도 없다. 같은 마을 거래소가 매기는 값은 나무 1.75 · 돌 0.1677 로 **10배 차이**다.
자원이 생기지는 않지만(플레이어 사이 이동), ⓐ 상대 인벤을 동의 없이 가져가고
ⓑ 이 배치가 세운 "값은 마을이 매긴다"를 정면으로 부정한다.
★판단거리: ⓐ 제거 ⓑ 동의 절차 + 거래소 시세 참조로 재작성 ⓒ 그대로 둔다(프로토타입).
※ **이번 배치가 만든 것이 아니라 이전부터 있던 것**이라 손대지 않았다.

### B-7 게시판 ↔ 거래소 부등식은 **게시 시점 기준**이다
`test-trade ⑥`: 게시판 ×1.200(프리미엄 0.2) > 거래소 ×0.818(스프레드 0.1) — 배수로는 항상 성립한다.
다만 게시판 보상은 **게시된 날의 값으로 고정**되고 거래소는 지금 값이다. 그래서 시세가 크게 움직인 뒤엔
개별 사례에서 역전이 날 수 있다. ★판단거리: 의뢰 보상을 **납품 시점 시세로 재평가**할 것인가
(그러면 "약속"이 아니게 된다) 아니면 지금처럼 **약속을 지킬** 것인가. 지금은 후자다(권장).

---

## C. 이번에 **일부러 안 한 것** (규약)

* **원격 시세 조회 API 없음** — `villageTradeBoard/Quote/Exec` 셋 다 `_villageNear` 게이트가 첫 줄이다
  (`test-trade ⑦` 이 3/3 을 구조로 확인한다). 우회로를 새로 만들지 마라.
* **가격 갱신 코드 없음** — 재고가 움직이면 시세는 저절로 따라온다. `trade.js` 에 "가격 갱신"류 코드가
  생기는 순간 그건 사본을 만들고 있다는 신호다(`test-trade ①` 이 소스로 막는다).
* **물리 상한 두 벌 금지** — `events.payableQty` **한 함수**를 게시판 보상과 거래소가 같이 쓴다.
* **econ 무수정** — `sim/economy-sim*.js` 는 한 줄도 안 건드렸다(3사본 재인라인 불필요).
