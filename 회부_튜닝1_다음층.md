# 회부 — 튜닝 1차 다음 층 (2026-08-30 · 신체 3층·달력·인벤 마무리 뒤)

> 이번 배치가 심은 것: 스태미나 신설 · 허기·갈증 회복 배율 재배선 · 상태 의존 감쇠 ·
> 추위 평형 수렴 · 달력(econ 정본 유도) · 인벤 마무리 4건 · 안개 위 논밭 수리 · RTT 상관 계측.
> 아래는 재민이 **구현 금지**로 못 박은 것 + 만들며 **실측으로** 드러난 판단거리다.

---

## A. 재민이 명시적으로 회부한 것 (구현 금지)

### 죽음 설계 — 아사 재론은 그때
이번 배치가 **아사 폐지 캐논을 하네스로 못 박았다**(`test-body ⑫`: 극단 10분 방치 HP 불변 +
`body.js` 에 HP 감소 줄 0). 결핍의 벌은 **회복 정지**까지다.
죽음을 설계하는 배치가 열리면 그때 아사·탈수사·동사를 **한 묶음으로** 다룬다 —
지금 하나만 되살리면 나머지와 균형이 안 맞는다.

### MOVE_MODEL 기본 전환 · 도구 슬롯 가산 구조 — 기록지 ⑦⑧ 대기
둘 다 재민 실기 체감이 나와야 방향이 정해진다. **한 값도 안 건드렸다**
(`MOVE_MODEL` 기본은 `legacy` 그대로).

### 낚시·거래소 스프레드·용량·제작 시간 손잡이 — 기록지 대기
그대로.

### 부패·보존 · P2P 거래
이월. 자리는 계속 넓어지고 있다(로트가 화면에서 나이를 말하고, 개체가 주소를 갖는다).

---

## B. 실측으로 드러난 판단거리 (재민 확인 필요)

### B-1 ★★계절 주기 — **첫 겨울이 4.5일이다**(목표는 2~3주)
```
  첫 겨울 = econ day 270 · 하루 24분 → 실시간 108시간 = 4.5일 = **0.64주차**
  한 해 365일 → 실시간 6.1일
  재민 확정 목표: "첫 겨울 = 실시간 2~3주차"
```
★어긋난다. 맞추는 길 셋 — 어느 쪽이든 **econ 에 파급**이 있어 재민 판정이 필요하다:
· ⓐ **하루를 늘린다**(24분 → 80~120분). 계절 구조는 그대로. 대신 하루의 리듬(밤낮·허기·피로)이
  전부 늘어진다 — 신체 감쇠 채택값(허기 2일=48분)도 같이 늘어난다. **가장 큰 파급.**
· ⓑ **한 해를 늘린다**(365일 → 1,200일 급). 하루 감각은 그대로고 계절만 길어진다.
  econ 의 계절 배율(`SEASON_MULT`)이 걸리는 기간이 3배가 되므로 **작물·교역 균형이 바뀐다**
  ⇒ 3시드 800일 A/B 필수(800일이 이제 0.67년이 아니라 2.2년이 된다 — 비교 자체가 달라진다).
· ⓒ **목표를 고친다**. "첫 겨울 4.5일"이 실제로 나쁜지 실기로 확인하고, 괜찮으면 목표를 내린다.
★내 관찰: 지금 구조에서 **가장 싼 건 ⓑ**이고, **가장 정직한 건 ⓒ**다(실기 전에 수를 흔들면
그 뒤 모든 균형이 추정 위에 선다). 어느 쪽이든 `seasonOf` 한 곳만 고치면 달력은 저절로 따라온다.

### B-2 ★★econ 틱 분산 — 상관이 **수치로** 확인됐다
```
  150초 · 하루 6초 · 51마을 (scripts/rtt-metrics.js)
    econ_day 48회 · 중앙 684ms · 최대 8,410ms
    [econ_day] ±600ms 창 안 RTT 평균 1,195ms vs 바깥 110ms → ×10.9
    [tick]     ±600ms 창 안 868ms vs 바깥 2.5ms          → ×350
```
econ 하루 틱이 이벤트 루프를 붙잡는 동안 RTT 가 그대로 밀린다.
라이브 영향 = **게임일마다 한 번**(24분에 1회) 수백 ms~1초 급 정지.
★판단거리 셋:
· ⓐ **쪼갠다** — `tickWorldV2` 를 마을 단위로 나눠 여러 틱에 흩는다.
  ⚠교역이 마을 간 **원자적**이라 쪼개면 정합이 깨진다(`villages.js` 주석이 이미 경고한다).
  쪼개려면 "교역 단계만 통째로, 나머지는 분산" 같은 **단계별 분할**이어야 한다.
· ⓑ **워커로 뺀다** — econ 을 별도 스레드/프로세스로. 정합은 지키되 메인 루프가 안 멈춘다.
  대가: 상태 전송 비용과 복잡도. 이 레포의 "정본 하나" 규약과 충돌할 수 있다.
· ⓒ **그대로 둔다** — 24분에 한 번 1초 멈추는 게 실제로 거슬리는지 실기 판정.
  (지금은 화면이 그 순간을 **말하지 않는다** — 접속 진단 배치의 배너는 5초 문턱이라 안 뜬다.)
※계측은 이미 상시다: `/perf` 가 최근 400건을 낸다. 라이브에서도 바로 볼 수 있다.

### B-3 ★두 시계 — `zoneGameDay()` vs `state.world.day`
```
  ⓐ zoneGameDay()    벽시계 파생 · 로트/원장 나이가 쓴다
  ⓑ state.world.day  econ 틱 카운터 · 계절·작물·사건·달력이 쓴다(따라잡기 없음)
  ⇒ 서버가 꺼져 있던 동안 ⓐ만 뛴다 = **영구히 벌어진다.**
```
이번 배치는 **화면에 나가는 날짜를 ⓑ 로 통일**했다(마을 재고창 + HUD 달력이 같은 값).
그런데 **로트 나이(`Lots`)와 개체 취득일은 여전히 ⓐ 다** — 즉 "3일 전에 잡은 물고기"의 3일과
달력의 3일이 서버 재시작을 사이에 두면 다르다.
★판단거리: ⓐ를 없애고 전부 ⓑ 로 갈 것인가. 그러려면 마을 시뮬이 꺼진 존에서 ⓑ 가 없다는
문제를 풀어야 한다(지금은 그 경우만 ⓐ 로 떨어진다). 부패 곡선이 들어오기 **전에** 정하는 게 좋다 —
들어온 뒤엔 저장된 나이가 전부 어느 시계인지에 매인다.

### B-4 ★상자 안 개체는 **주소가 없다**
`b.data._led[item] = [{kg,d?}]` 로 개체를 담지만 **id 를 안 싣는다** — FIFO 로만 나온다.
그래서 상자 줄은 펼쳐서 **보여 주기만** 하고 개별 인출은 안 된다(그 하위 줄엔 드래그 짐이 없다).
★판단거리: 상자에도 id 를 실을 것인가. 실으면 "상자에서 2kg 물고기만 꺼내기"가 되지만
건물 blob 이 커지고(물고기 200마리면 상자 하나에 200항목) 길드 곳간 회계와 얽힌다.

### B-5 ★스태미나 채택값은 **첫 판**이다
22초 질주 / 30초 회복 / 적재 가중 1.2 — 전부 책상에서 고른 값이다.
`test-body ⑨` 가 값과 실측이 맞는지만 지킨다(값이 옳은지는 안 지킨다 — 그건 실기다).
★실기 확인 항목: 22초가 "한 구간 달리기"로 맞는가 · 짐 가득일 때 10초는 너무 짧은가 ·
25% 재개 문턱이 답답한가.

### B-6 ★도구는 상자에 못 넣는다
상자가 수량 맵이라 개체를 못 담아서다(도구는 `toolItems` 인스턴스). 지금은 화면이 그렇게 말한다.
★판단거리: 상자에 도구 칸을 따로 둘 것인가(`b.data._tools = [{type,d,max}]`) — B-4 와 같은 결이다.

### B-7 ★비네트·무들에 스태미나가 없다
스태미나는 HUD 막대와 상태 패널엔 있지만 **무들 축이 아니다**(3단계 아이콘·비네트 색조 없음).
바닥나면 "😩 숨참" 뱃지만 뜬다.
★판단거리: 스태미나를 §7 축으로 승격할 것인가. 승격하면 §7 동시 표시 상한(3)을 나눠 쓰게 된다.

---

## C. 이 배치가 바꾼 것 (다음 세션이 헷갈리지 않게)

· `Body.CURVES` 에서 **hunger·thirst 가 빠졌다**. 대신 `Body.RECOVER` 가 생겼다.
  축 목록도 둘로 갈렸다: `EFFECT_AXES`(cold/fatigue/injury) · `RECOVER_AXES`(hunger/thirst).
· `Body.tick(p, dt, ctx)` 의 `ctx` 에 **`carryRatio`** 가 추가됐다(스태미나 소모 가중).
  `ctx.sprint` 는 이제 **허기를 안 깎는다**(스태미나만 깎는다).
· 새 정본 함수: `Body.canSprint` · `Body.recoverMult` · `Body.coldTarget` · `Body.decayRate` · `Body.stamina`.
  **달리기 관문·회복 배율·추위 목표점을 다른 데서 다시 계산하지 마라.**
· `SPRINT_MIN_GAUGE` → `SPRINT_MIN_GAUGE__DEAD_SEE_body_canSprint`(사장).
· `Events.calendarOf/yearDaysOf/seasonStartOf` 신설 — **`seasonOf` 만** 부른다(상수 0개).
· `SimVillages.econDay()` 신설 — **`null` 을 낼 수 있다**(day 0 과 구별하려고).
· `invPayload()` — 나가는 인벤에서 수량 0 을 턴다. 살아 있는 맵은 그대로.
· `toolRowsHtml`(도구 전용 표) **삭제** → `ulRowsTools()` 가 통일 목록에 낸다.
· `drop_item` 에 **`toolId`** 인자 추가. 바닥템에 **`gi.tool`** 필드 추가.
· 상자 blob 에 **`data._led`** 추가(밑줄 접두사 = 기존 소비자가 이미 거른다).
· 새 손잡이: `_t19.simvilCellGateOff`(영토 셀 게이트 대조군) · `CONN_*`(직전 배치) ·
  서버 `/perf` 엔드포인트 · `perfMark()`.
· 새 읽기 훅: `window.__calendar()` · `window.__simvilProbe()` · `__getGauges()` 에 스태미나 4필드.
