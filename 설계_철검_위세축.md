# 설계안(회부) — "세계 최초의 철검"에 값을 매기는 축

> 2026-08-02 배치 C14. **구현 금지 · 설계 판단 회부**.
> 배경: h0 교정으로 **연철검 등급 0.75 < 청동검 1.09**. 성능만 보면 철검은 열등품이다.
> 그런데 플레이어가 청동기 세계에서 처음 뽑아낸 철검이 "청동검보다 못한 물건"으로
> 취급되면, 그 고생이 게임 안에서 아무 의미를 못 갖는다.

---

## 1. 지금 수치 (실측 · `alloyGrade(mix,'weapon')`)

| 재료 | 등급 | 비고 |
|---|---|---|
| 청동 Cu83/Sn17 | **1.09** | 표준 |
| 흑요석 날 | 0.95 | (손표) |
| **운철 Fe93/Ni7** | **0.904** | 철 앵커 × 니켈 프리미엄 1.064 (2026-08-02 신설) |
| 마제석검 | 0.6 | (손표) |
| **연철(순철)** | **0.75** | 모델값. 손표 iron 은 0.85 |
| 순동 | 0.466 | 석기보다 못하다 |

**이건 고증이 맞다.** 초기 철기는 청동기를 성능으로 이기지 못했다. 철이 이긴 이유는
품질이 아니라 **광맥이 흔해서**다(주석은 희소하고 편재했다). 그래서 수치를 손보는 건 틀렸다.

## 2. 그러면 무엇으로 값을 매기나 — **위세(prestige)는 성능축이 아니다**

econ 에는 이미 위세 개념이 있다: `RESOURCES[r].contributes.prestige`(금 1.5 · 은 0.8),
그리고 `ORNAMENTAL` 재화의 `LUX_TARGET_PC` 보유 수요. 위세재는 **쓰지 않아도 보유가 효용**이다.

⇒ 철검의 값은 **공격력이 아니라 희소 프리미엄**으로 표현하는 게 축이 맞다.
같은 물건이 두 축에 동시에 서면 된다: 무기로서는 0.75, 위세재로서는 세계에 몇 자루뿐이다.

### 후보 축 셋

### (가) 세계 최초성 — **개체에 붙는 낙인**
아이템 인스턴스에 `firstOfKind: true` 를 찍는다(세계에서 그 종류가 처음 만들어진 N 자루).
표시에 "세계 최초의 철검" 이 붙고, 마을이 살 때 가격 배수를 받는다.

- 장점: 서사가 정확히 재민이 말한 것이다. 한 번뿐이라 인플레가 없다.
- 단점: **세계 전역 상태**가 필요하다(central 이 "이 종류가 이미 나왔나"를 알아야 한다).
  존 간 정합·재부팅 영속이 필요 — 새 인프라다.

### (나) 희소 프리미엄 — **세계 재고의 함수**(상태 없음)
```
  prestige(r) = clamp( log(REF / (worldStock(r) + 1)) , 0, MAX )
```
`_worldStockOf` 가 이미 있다(computeShadowPrices 가 위세재 목표 상한에 쓴다). 그걸 재활용해서
"세계에 거의 없는 금속으로 만든 물건"에 값을 얹는다. 철이 흔해지면 프리미엄이 **저절로 꺼진다.**

- 장점: 새 상태 0. 시대가 열려 철이 퍼지면 프리미엄이 자연 소멸 — "최초"의 값이 스스로 만료된다.
  era.js 의 철학("표가 아니라 물리가 답한다")과 같은 결.
- 단점: "최초"가 아니라 "희소"라 서사가 조금 약하다. 그리고 세계 재고를 매일 재는 비용이 든다
  (지금은 위세재에만 쓰는 함수를 전 금속으로 확장하는 것).
- ⚠**유령 수요 위험**: 재민이 2026-08-01 에 "모든 기본 비축 목표 없애라"로 걷어낸 게 정확히
  이런 종류의 바닥 수요다. 프리미엄은 **소비 흐름이 있는 재화에만** 얹어야 한다(안 쓰는 철에
  프리미엄이 붙으면 다시 "아무도 안 쓰는 철을 비싸게 사오는" 그 버그다).

### (다) 아무것도 안 한다 — 성능이 전부다
철검은 연철이면 청동보다 못하고, 그게 사실이다. 플레이어의 보상은 "**시대가 열리기 전에
혼자 해냈다**"는 것 자체이고, 실질 이득은 **청동이 없는 곳에서도 금속 무기를 쥔다**는 것이다
(주석은 지도에 4곳뿐 — 철 광맥은 플레이어 자잘에 184개).

- 장점: 구현 0. 고증 100%.
- 단점: 값이 서사에만 남고 경제에 안 잡힌다.

---

## 3. 내 의견

**(나) + (가)의 표시만.** 즉 값은 세계 재고 기반 희소 프리미엄으로 계산하고(상태 없음),
"세계 최초"는 **표시 문구**로만 준다(그건 로그를 뒤져 사후 붙여도 되는 장식이라 인프라가 필요 없다).
단 (나)는 **소비 흐름 게이트**와 반드시 세트로 — 안 그러면 걷어낸 유령 수요가 이름만 바꿔 돌아온다.

## 4. 결정이 필요한 것

- [ ] (가)/(나)/(다) 중 무엇
- [ ] (나)라면 프리미엄 상한과 REF 를 얼마로 — "철검 한 자루 = 청동검 몇 자루" 가 감각의 기준이다
- [ ] 프리미엄을 **무기 가격**에 얹을지 **금속 가격**에 얹을지
      (금속에 얹으면 대장장이의 재료비가 올라 도리어 안 만들게 된다 — 무기 쪽이 맞아 보인다)
