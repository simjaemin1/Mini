# durango-mini 다음 세션 인계 프롬프트 (2026-08-02)

> 아래 전문을 새 세션 첫 메시지로 붙여넣으면 된다.

---

너는 durango-mini(한국 청동기 송국리 문화기 오픈월드 경제 시뮬) 작업을 이어받는다.
아래 규칙·원칙·상태를 전부 읽고, [작업 배치]를 위에서부터 순서대로 **전부** 진행해라.
질문하지 말고 네가 판단해서 진행하되, [회부만 할 것] 항목은 절대 구현하지 말고 문서만 써라.
튜닝은 자유. 정확성이 속도보다 우선. 다 되면 결과를 정리해서 보고해라.

## 0. 부트스트랩

- 레포: `https://github.com/simjaemin1/Mini.git` → `/root/minirepo` 로 클론, `npm install`.
- GitHub PAT: 디바이스 파일 `~/Mini/durango-mini/.git/claude-pat.txt` (93바이트).
  `device_stage_files` 로 스테이징 → 셸 변수로만 읽고 즉시 `unset`.
  **PAT 값을 채팅·레포·메모리에 절대 남기지 마라.** push 출력은 반드시
  `sed "s/${PAT}/***/g; s/github_pat_[A-Za-z0-9_]*/***/g"` 로 거른다.
- 시작하자마자 `git log --oneline -5` 로 HEAD 확인. 기준 커밋: `1744e0e`
  (철검 정상화 + 부유 시딩 + 플레이어 노 건설). 그보다 뒤면 그대로, 앞이면 fetch+ff.
- 인계 문서: 이 파일 + `회부_마을소멸_시딩.md` + `회부_플레이어_제련_노모델.md` +
  `보고_밤샘배치2_전사사슬과_소멸사인.md` 를 먼저 읽어라.

## 1. 절대 규칙 (사용자 확정 — 위반 금지)

1. **배포는 사용자만 한다. SSH 시도 금지. fly 배포 명령 금지.**
2. **force push 금지. DB 리셋 금지**(`world-hanbando.db` 포함 — 시딩 변경이 라이브에 안 먹는 이유가 이것이어도, 리셋은 회부만).
3. NPC 로직은 랩 verbatim 이식만. econ 계수 임의 보정 금지.
4. 디바이스(`~/Mini`)의 레포 파일 동기 금지 — **랩 2종(전쟁실험실·마을실험실.html)과 map-editor.html 만** mtime 가드로 편집한다. 그 외 디바이스 파일은 읽기 전용.
5. 스케줄 작업은 claude-code-remote MCP 만(로컬 cron 금지).
6. 이주(tickMigration)는 폐지됐다(사용자 결정 2026-07). 켜지 마라. 강제소개(tickForceEvacuation)도 회부 없이 켜지 마라.
7. 설계 판단은 구현 전에 회부. 반쪽 구현보다 이월.

## 2. 검증 원칙 (이번 세션에 계측기가 7번 틀리며 확립 — 요약)

- **본 게임이 이미 가진 것을 랩에서 다시 만들지 마라.** 실지도 랩(`scripts/econ-lab-real.js`)은
  `villages.__labProbe`(부존 추출·시딩 선별)·`createWorldV2`·`tickWorldV2` 를 그대로 부른다.
- 모든 회귀·측정 전에 `node scripts/lab-wiring-check.js` — 배선·번들·인라인 드리프트 검사.
  `scripts/econ-regress.js`(v2 CLI 5시드)가 이를 선행 실행한다.
- 엔진(specialty/economy-sim/-v2/era) 수정 후: `node sim/build-econ-bundle.js`(--check 8/8) →
  `node sim/inline-engine.js <랩들>` → `node scripts/lab-smoke.js`(헤드리스 크로미움) → 디바이스 동기.
  번들·랩 인라인 블록을 **손으로 기우지 마라**(자동 생성물).
- 하네스: `test-alloy.js`(합금 11종 앵커) · `test-valuechain.js` · `test-mining.js`(pk≡공식·철0·은 다광종) ·
  `test-armor-cap.js` · `test-picker-tables.js`. 전부 PASS 유지.
- **A/B 는 최소 3시드**(`LAB_SEED=n`) — 좀비 마을 칼날 평형 때문에 단일 시드는 판정 불가.
- **커밋 즉시 푸시.** 이번 세션에 컨테이너가 2번 재활용돼 미푸시 커밋을 날렸다/복구했다.
- pkill 함정: `pkill -f "econ-lab-rea[l]"` 처럼 브래킷을 쓰고, **같은 명령줄에 대상 문자열을 넣지 마라**(자기 자신이 죽는다 — 두 번 당했다).
- 브라우저 번들엔 `process` shim 이 있다(build-econ-bundle) — 엔진에 `process.env` 손잡이를 새로 넣어도 안전하지만, 랩 스모크로 확인해라.
- pk 재산출 시 해시 좌표는 **floor** (`orePeakFor(광종, 0.30, hash2(floor(cx/32), floor(cy/32), 500))`).

## 3. 현재 상태 (1744e0e)

- **시대 축**: `server/era.js` — 자물쇠가 아니라 다이얼. 노 온도(FURNACE+연료+풀무) vs 환원온도·융점·끓는점 물리로 판정. 청동기 철 수율 3.4% → 시대 오픈 후 67.8%. SCHEDULE 은 비어 있음(영원히 청동기 — 여는 건 사용자 몫). NPC 게이트는 `npcKnows` 하나.
- **지도**: 주요 광맥 철 0(플레이어 전용 자잘에 184), 다광종(연은·부산 귀금속·일렉트럼), 은 단독 폐지. 시딩은 땅 품질(식량 하한 2.0 × 도구 접근 0.5~1.5배) — 3시드 소멸 11.7→6.0, 인구 696→1,118.
- **econ**: 무조건 비축 바닥 제거(수요=flowT·subs·CAP_TARGET), 시대 게이트 완결(제련·정원·가격·파생수요), 끊김①(제련이 주조 구축)·③(전사 0)·④(갑옷 0) 해소, 철검 일반 재료 게이트. v2 5시드: 인구 2,078·갑옷 183·소멸 0/30.
- **플레이어**: 감정 10단계+종류 오인(바보의 금), 선광 철→정광, 노(爐) 건설(움집 동형 3단계, 사유지 필수)+조업(정광+숯→era 물리), 숯 제작법. 3금속 주조 슬라이더는 기존대로.
- **랩**: 두 랩 최신 엔진 인라인·디바이스 동기 완료. `LAB_DUMP`(마을별+궤적)·`LAB_SEED`·`ERA_FLIP_DAY` 계측 손잡이.

## 4. 작업 배치 — 순서대로 전부

### A. 노(爐)·철 콘텐츠 마감
1. **노 E2E 테스트 하네스** `scripts/test-furnace.js`: 서버 함수 직접 호출로 사유지 생성→터→3단계→조업까지 왕복 검증(권한 거부 케이스 포함: 타인 개인 노, 타길드 노, 사유지 밖 건설). zone.js 부팅이 무거우면 함수 단위로 __테스트 훅 노출(기존 __p3Bind 선례).
2. **노 스프라이트**: 임시 도형 렌더를 움집처럼 에셋으로. `assets-src` + `scripts/icon_render.py`/`building_render.py` 파이프라인 확인 후 도가니로(2×2, 단계 1~3+완공+불) 렌더 → `public/assets` → 클라 `_bldSpr` 계약으로 교체. 파이프가 막히면 도형 렌더 유지하고 회부.
3. **숯가마 설치물**: 제작법(통나무3→숯2)을 유지하되, 숯가마(설치물, 노와 같은 건설 계약)를 지으면 변환비가 좋아지게(예: 통나무3→숯4) — 고증(가마>모닥불 수율). 사유지 규칙 동일.
4. **운철(隕鐵) 지표 스폰**: 자잘 광맥급 초희귀 발견물. `METEORIC={iron:.93,nickel:.07}` 은 era.js 에 있다. 지표 오브젝트로 스폰(좌표 결정론 해시 — 자잘 광맥 생성 선례), 채집 시 `meteoric_iron` 아이템 → **제련 불필요**, 노 없이도 단조 가능(era.canForge 에 이미 등재). 스폰 밀도는 대륙에 수십 개 수준.
5. **시대별 노 티어**: FURNACE_STAGES 를 kind 파라미터화 — 괴련로(bloomery)는 `hasTech('bloomery')`(=early_iron 오픈) 후에만 건설 가능. 지금은 도가니로만 노출.
6. **다광종 선광 E2E**: 연은 광맥에서 캐→선광하면 납+은이 실제 비율(85:15±)로 나오는지 몬테카를로 하네스(test-mining 에 추가).

### B. 경제 관찰·해소
7. **주조 확산 규명**: 왜 1~2/20 마을만 주조하나. labX 덤프 tradeLog 로 구리·주석의 마을 간 흐름을 추적해 "구리가 대장장이 있는 마을로 안 가는" 지점을 특정. 처방이 계수면 3시드 A/B 로 적용, 구조면 회부.
8. **원석 적체(~490) 해소**: 광부 정원 vs 제련 캡(SMELT_LABOR_CAP_PC 0.05) 균형 실측. 광부 파생수요가 원석 재고를 보게 할지 판단 — 이건 econ 구조라 신중히, A/B 필수.
9. **끊김② 정식 재평가**: 석공→대장장이 순서 고정이 아직 병목인지 3시드 측정. 병목이면 회부(한계가치 경쟁은 구조 변경).
10. **좀비 마을 완화 실험**: 회부 문서의 (나) 초기 부존을 땅에 맞추기(돌 없는 마을에 돌·도구 초기 지급)를 3시드 A/B. 효과 있으면 적용, 없으면 기각 기록. (다) 강제소개는 **회부 유지 — 구현 금지.**
11. **배합↔교역 통합 설계안**: "이 주석을 무기에 쓸까 팔까"가 한 곳에서 비교되지 않는 문제. `_alloyMelt` 이윤식과 교역 EV 를 같은 단위로 놓는 설계안 작성 → 회부(구현 금지).

### C. 시대·야생 콘텐츠 준비
12. **시대 전환 리허설 스크립트** `scripts/era-rehearsal.js`: WORLD_ERA / setEra / SCHEDULE 각각의 경로로 열었을 때 실서버 부팅 스모크 + 실지도 800일 플립 자동 비교(이미 있는 ERA_FLIP_DAY 재사용). 사용자가 "열까?" 할 때 바로 돌릴 수 있게.
13. **말(horse) 사역 배선 조사**: wildlife.js 에 말 종이 있는지, tame 소비처가 어디 붙을지 조사 보고서만(구현은 era.canTame('horse') 가 early_iron 이라 아직 잠김).
14. **시대 전 철검 위세축 설계안**: h0 교정으로 연철검 등급 0.75(청동 1.0 미만). "세계 최초의 철검"의 가치를 표현할 아이템 위세 축(희소 프리미엄) 설계안 → 회부.

### D. 계측·문서 위생
15. **감정 하네스** `scripts/test-assay.js`: 레벨 0~10 × 광종별 문구 분포·오인율 몬테카를로 — MINE_TYPE_ACC 표와 실측 일치 검증, FP 누설 재발 방지(맥석 문구에 광맥 진명 등장 0 확인).
16. **lab-wiring-check 확장**: 번들에 era.js 포함 여부, process shim 존재, 랩 시딩 인자(ta) 전달을 검사 항목에 추가.
17. **CHECKLIST.md 갱신**: 이번 이틀치(시대 축·다광종·감정·노·시딩·전사 사슬)를 캐논 형식으로 추가. DESIGN_DOC.md 에 era.js 절 추가.
18. **`다음세션_인계.md` 를 이 파일로 교체**(옛것은 낡았다).

### E. 본 게임 접점 (배포 없이 준비만)
19. **실서버 부팅 스모크**: zone.js 로컬 부팅(포트 충돌 주의)으로 노 핸들러·시딩·다광종이 부팅 경로에서 예외 없는지. **주의: 기존 world-hanbando.db 가 있으면 시딩은 다시 안 돈다(복원 경로)** — 새 시딩은 새 DB 에서만 발효. 라이브 적용엔 DB 리셋이 필요하다는 사실을 회부 문서에 명확히 남겨라(리셋 자체는 금지).
20. **플레이어 아이템 정합**: iron_ore·charcoal 이 인벤 UI·무게·거래(플레이어↔마을 판매)에 자연스럽게 들어가는지 점검, 빠진 라벨/무게는 채워라(`물품_무게_고증표_kg.md` 참조).

## 5. 회부만 할 것 (구현 금지)

- 강제소개/이주 계열 전부 · 배합↔교역 통합 구현 · 아이템 위세축 구현 · DB 리셋/존 id 변경 ·
  NPC 철기 산업 콘텐츠(새 광맥·마을) · 산 부수기 시스템 · 자잘 광맥 미니맵 기록 · 시대 SCHEDULE 실제 설정

## 6. 완료 기준

각 항목마다: 실측 수치(전/후) 또는 하네스 PASS → 커밋(사용자 지시 인용 포함) → **즉시 푸시**.
마지막에 `보고_*.md` 로 종합 보고(변경·수치·회부 목록) 작성·푸시하고, 랩 2종 재인라인·스모크·디바이스 동기까지 확인.

---
(이 파일은 1744e0e 직후 작성됐다. 세션 시작 시 HEAD 가 더 앞서 있으면 git log 로 그 뒤 커밋들을 먼저 읽고 상태 인식을 갱신해라.)
