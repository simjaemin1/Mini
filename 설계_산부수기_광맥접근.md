# 설계안 — 산 부수기(암반 굴착)와 광맥 접근

**상태: 재민 결정 대기.** 구현 안 함(반쪽 구현보다 이월).

---

## 0. 왜 지금 이게 막혀 있나

재민 지시(11차): *"광맥이 산 근처에 퍼져 있는 건 좋은데, 좀 더 **산 안쪽**으로 들어갔으면 좋겠어.
**산 타일을 부숴야만 채굴할 수 있도록** 말이야."*

지시대로 배치를 옮겼다. 실측:

| | 중심이 바위 안 | 기슭(20셀 이내) | 산과 무관(100셀 밖) |
|---|---|---|---|
| 대·중·소 61개 | **45개 (74%)** | 50개 (82%) | 0개 |
| 자잘 | (아래 재측정치 참조) | | |

그런데 `zone.js:3584` 가 이렇게 되어 있다:

```js
if (isWaterTileLocal(px, py) || isTerrainBlockedLocal(px, py)) return false;   // 물·바위 위에선 못 판다
```

`isTerrainBlockedLocal` → `isRockTileLocal` → 바위면 true. **즉 지금 큰 광맥의 74%는 채굴 자체가 불가능하다.**
배치는 지시대로 옮겼는데 그 자리를 열어 줄 "산 부수기"가 없어서 생긴 반쪽 상태다.
이 문서가 결정되기 전까지는 **큰 광맥은 가장자리(바위 밖으로 삐져나온 부분)에서만** 캘 수 있다.

---

## 1. 지금 바위가 어떻게 생겼는지 (구현 사실)

바위는 **저장된 타일이 아니다**. 좌표를 넣으면 노이즈로 계산되는 술어다:

```
isRockTileLocal(x,y) → _terrain.isRockCellLocal(ZONE_ID, ...)   // 결정론 계산, 저장 없음
```

이 술어를 **공유하는 것들**(`zone.js:331` 주석에 명시):
플레이어 이동 · NPC 이동 · A* 경로 · wildlife · 전쟁 콜라이더 · 마을 배치 · 농지 배치 · 건축 가능 판정.

⇒ "바위를 부순다"는 건 **술어에 예외 집합을 얹는 일**이고, 그 예외는 위 전부에 동시에 보여야 한다.
이게 이 기능의 진짜 비용이다(채굴 로직 자체는 사소하다).

---

## 2. 결정이 필요한 것 — 7가지

### ① 무엇으로 부수나
- (가) 곡괭이 그대로. 채광의 연장선.
- (나) 별도 도구(정·쐐기·망치). 대장장이 수요를 만든다.
- (다) 불질(火法, fire-setting) — 고증적으로는 이게 정답이다. 청동기 광산은 곡괭이가 아니라
  **장작을 쌓아 달군 뒤 물로 급랭해 암반을 갈랐다**(Great Orme·Mitterberg 모두 확인됨).
  나무를 소모하고 시간이 오래 걸리는 대신 곡괭이를 안 축낸다.

> 의견: (다)가 고증·게임성 둘 다 낫다. 나무 → 산 부수기 → 광맥이라는 **자원 사슬**이 생기고,
> "혼자 곡괭이 들고 산에 들어가면 안 된다"가 자연스럽게 나온다(2인 1조 규약과도 맞는다).
> 다만 (가)보다 구현이 크다(연료 아이템·점화·대기 시간).

### ② 몇 타 / 얼마나 걸리나
채굴이 1초/타 · 덩이당 60타다. 바위 한 칸은 그보다 **확실히 비싸야** 한다
(안 그러면 산을 통째로 뚫는 지름길이 된다 — 이동 경로가 무너진다).

- 후보 A: 300타(5분 실시간 = 5게임시간). 덩이 5개 값.
- 후보 B: 600타(10분). 덩이 10개 값. 2인이면 5분.
- 불질이면 타수가 아니라 **장작 N단 + 대기 T분**.

> ★핵심 제약: 산맥을 **관통 통로**로 쓰는 게 이득이 되면 안 된다.
> 가장 긴 산맥 폭이 몇 셀인지 재서, "우회가 항상 싸다"가 성립하는 하한을 잡아야 한다. 측정 가능.

### ③ 부순 자리는 되돌아오나
- (가) 영구. 지도가 플레이어 손에 영구히 변한다.
- (나) 리젠. 광맥 재고처럼 시간이 지나면 막힌다.

> 의견: **영구**. 되돌아오면 굴착이 노동이 아니라 유지보수가 되고, 광부 여럿이 판 굴을
> 공유하는 서사가 죽는다. 대신 ②를 비싸게 잡아 남발을 막는다.
> (지도 무결성 우려는 ⑤의 화이트리스트로 처리한다.)

### ④ 부수면 뭐가 나오나
- 돌(stone) 몇 kg? 아니면 아무것도 안 나오나?
- 광맥 안의 바위를 부수면 **원석 덩이**가 나오나(= 굴착이 곧 채굴)?

> 의견: 돌만 나오고 원석은 안 나온다. 굴착과 채굴을 분리해야 "뚫고 → 캔다"의 두 단계가 산다.
> 돌은 나와야 한다(건축 재료 수요 — 지금 돌 획득처가 빈약하다).

### ⑤ 어디를 부술 수 있나
전부 열면 산맥이 스위스 치즈가 된다. 제한 후보:

- (가) **광맥 안(또는 가장자리 N셀)만**. 산은 광맥이 있는 곳에서만 뚫린다.
- (나) 아무 데나.

> 의견: **(가) 광맥 도달 범위 안 + 바깥 여유 8셀**. 그러면
> · 산맥 관통은 애초에 불가능(②를 걱정할 필요가 줄어든다)
> · "광맥을 찾았다 = 산에 굴을 팔 권리가 생겼다"가 발견의 보상이 된다
> · 자잘 광맥은 지금 바위 안 비율이 낮으니 대부분 영향 없다

### ⑥ NPC 는 부술 수 있나
- NPC 광부가 산을 뚫게 하면 A\* 캐시·마을 노동권 계산이 전부 동적이 된다.
- 안 뚫게 하면, **큰 광맥 74%가 바위 안**이라 NPC 광부가 갈 자리가 사라진다 ← 이게 지금 문제다.

> 의견: **NPC 는 안 뚫는다.** 대신 `villages.js` 의 광맥 인식(`isMajorOreAt`)이
> 지금은 중심을 보는데, **"바위가 아닌 광맥 셀"이 있는지**로 바꿔야 한다.
> 큰 광맥은 반경이 32~130셀이라 가장자리에 노출 셀이 반드시 있다 — NPC는 그 노출부만 캔다.
> 플레이어는 굴을 파고 안쪽(p가 높은 중심부)으로 들어간다. **이게 플레이어의 우위다.**
> ⇒ 재민의 "산 안쪽" 지시가 그대로 게임 구조가 된다.
> ※ 다만 지금 `villageProduction` 과 `_oreCellsOf` 가 노출 여부를 안 본다 — 이건 ⑥ 결정과 함께 고쳐야 한다.

**이 구조가 실제로 성립하는지 실측했다** (대·중·소 61개, 광맥 안 셀을 표본해 바위 여부와 p를 잼):

```
노출 셀이 0인 광맥 : 0개 / 61개          ← NPC 가 완전히 막히는 광맥은 없다
광맥별 노출 비율   : 최소 9% · 중앙 27% · 최대 100%   (분위 9/15/27/47/100)
평균 p  광맥 전체 0.0517  vs  노출부만 0.0373        ← 노출부는 **28% 가난하다**
```

⇒ 세 가지가 동시에 확인된다.
> ① NPC 광부는 어느 광맥에서도 굶지 않는다(최소 9%는 열려 있다).
> ② 굴을 파면 품위가 **1.39배**가 된다 — 플레이어가 산을 뚫을 이유가 수치로 존재한다.
> ③ 그 격차가 p 기울기(안쪽일수록 크다)에서 **자동으로** 나온다 — 별도 보정 상수가 필요 없다.

### ⑦ 저장·동기
부순 셀을 어디에 두나:
- `zone-local-db.js` 에 `broken_rock(cell_key)` 테이블 (mined_cells 와 같은 규약)
- 접속 시 welcome 페이로드로 뿌린다 (다리·환호와 같은 규약 — 이미 있는 길이다)
- 클라이언트 타일 렌더 + `isRockTileLocal` 오버라이드
- **A\* 경로 캐시 무효화**가 필요하다(부순 순간 새 길이 열리므로)

> 규모: 다리(bridges)와 완전히 같은 구조다. 그 코드를 그대로 본뜨면 된다.
> 셀 수는 광맥 안으로 한정하면(⑤-가) 존당 수천 셀 상한이라 부담 없다.

---

## 3. 내가 권하는 조합 (재민 확인용 한 줄 요약)

> **불질(장작 소모)로 · 광맥 범위 안에서만 · 영구히 · 돌만 나오게 · 플레이어만 · 다리와 같은 저장 규약으로.**
> NPC 는 광맥의 **노출부**만 캐고, 안쪽 고품위는 굴을 판 플레이어의 몫.

이 조합이면 재민이 말한 세 가지가 전부 성립한다:
"산 안쪽으로" · "산 타일을 부숴야만" · "자잘광맥은 오로지 플레이어만".

---

## 3-b. 곁다리 회부 — 자잘 광맥의 **발견 기록**

자잘 광맥 2600개가 **미니맵에 전부 그려지고 있었다**. 그러면 "걸어 다니다 만난다"는 게 없어져
재민이 말한 *"훨씬 많아야 해.. 그래야 탐험하는 재미가 있지"* 가 성립하지 않는다.
⇒ 일단 **미니맵에서 자잘을 뺐다**(`public/client.js`, `o.minor` 건너뜀). 큰 광맥 61개만 나온다.
   (부수 효과로 프레임당 arc 2600개도 사라졌다.)

여기서 갈리는 게 하나 있다 — **한 번 찾은 자잘 광맥을 지도에 남길 것인가?**

- (가) 안 남긴다. 매번 기억으로 찾아간다. 지도가 깨끗하다.
- (나) **판 자잘은 표시로 남는다**(플레이어별 발견 기록). 탐험이 자산이 된다.
  · 저장: `players` 에 발견 셀 목록 · welcome 때 내려보냄 · 미니맵에 점으로
  · 규모: 다 캐 봐야 플레이어당 수십~수백 개라 부담 없다
- (다) (나) + **거래 가능**. 광맥 위치가 정보 상품이 된다.

> 의견: **(나)**. 발견에 흔적이 남아야 탐험이 노동이 아니라 축적이 된다.
> (다)는 매력적이지만 지금 거래 구조에 얹을 자리가 없다 — 나중 일로 미루는 게 낫다.

※ 개발용 셀맵(`../cell-map.png`)에는 자잘을 **다른 색으로** 남겨 뒀다(분포 감사용).
   이건 게임 안에서 보이는 지도가 아니다.

## 4. 결정 전까지의 상태

- 대·중·소 광맥은 **가장자리 노출부에서만** 채굴된다(가장자리는 열려 있다 — 완전 봉쇄는 아니다).
- NPC 광부 정원은 노출부 여부를 안 보므로 **바위 안 광맥에도 정원이 잡힐 수 있다**(⑥에서 같이 고친다).
- 자잘 광맥은 대부분 바위 밖이라 영향이 작다.
