# 회부 — 마을 소멸의 진짜 원인은 교역이 아니라 시딩이다

> 2026-08-01 밤샘 배치. 소멸 6/20 의 사인을 궤적 실측으로 규명했고, 교역 쪽 처방은
> 시도했다가 3시드 A/B 로 기각·철회했다. 남은 처방이 전부 설계 판단이라 회부한다.

## 사인 (10일 표본 궤적, 실측)

죽는 마을의 공통 궤적 — 농촌22(비옥 1.12 · 물 1.47, **좋은 땅**)가 대표:

```
d0   8명 · 식량환산 371     ← 태어날 때부터 기근선(N×30) 아래
d40  22명 · 식량 699        ← 빠른 성장, 식량 정점
d100 22명 · 식량 1049 → 이후 단조 감소
d220 22명 · 식량 0          ← 도구가 다 닳았다: 돌 0 · 목재 0 · 석공 0 → 생산 ×0.25
d240~d360 22명 · 식량 0     ← 하루 벌어 하루 먹는 좀비 상태
d380 8명                    ← 기근 사망 개시 → 소멸
```

**메커니즘**: 이 마을들의 땅은 비옥도만 좋고 **돌·나무가 바닥값**(stone 0.25 · wood 0.45)이다.
도구가 닳으면 돌이 없어 석공도 자급(막석기)도 못 하고, 맨손 생산(×0.25)으로 추락한다.
비옥한 땅인데 굶는 이유다. 교역이 돌을 배달해 주기도 하지만(d208 에 70 단위 도착 실측)
그때는 이미 식량이 바닥이라 복구 전에 무너진다.

덤으로 관찰된 것: 죽어가는 마을은 모든 재고가 0이라 모든 가격이 폭등하고, 캐러밴들의
최적 목적지가 된다 — 이웃들이 모피(단가 1,170)를 팔고 마지막 보리를 실어 간다.
이걸 막는 게이트를 넣어봤지만 3시드 A/B 가 기각했다(economy-sim-v2.js 주석 참조) —
좀비 마을은 칼날 평형이라 어떤 단일 개입도 시드 분산에 묻힌다.

## 선택지 (전부 설계 판단)

| | 내용 | 비고 |
|---|---|---|
| **가** | **시딩 선별에 땅 품질을 넣는다** — `pickSeedVillages` 는 지금 **타입 prior 만** 본다(plain 1.0 > riverside 0.9 > …). 식량환산 기대치(비옥×슬롯)와 도구 유지 가능성(돌·나무)을 점수에 넣어, 유지 불가능한 자리는 아예 안 심는다 | 내 추천. 소멸의 뿌리를 자름. VILLAGE_MAX 20 은 그대로 — 더 좋은 20곳을 고를 뿐 |
| **나** | **초기 부존을 땅에 맞춘다** — 지금은 전 마을 일괄 food 300일치·도구 약간. 돌 없는 마을엔 돌·도구를 더 주고 시작 | 완화일 뿐 — 도구는 또 닳는다 |
| **다** | **강제소개를 켠다**(`tickForceEvacuation` — 인구≤4 & 척박 & 수입미달이면 큰 마을로 합류) | 코드가 이미 있고 OFF. ⚠이주 폐지(재민 결정 2026-07)와 충돌하는지 판단 필요 — 이건 상시 이주가 아니라 소멸 직전 1회 구제다 |
| **라** | **그대로 둔다** — 소멸은 나쁜 자리에 대한 세계의 정직한 판정이고, 폐허는 콘텐츠다 | 유효한 세계관. 단 게임 800일 = 실시간 13일이라 서버 2주에 마을 절반이 사라진다 |

## 참고 실측

- 소멸/좀비는 **시드 민감**: 같은 지도에서 시드만 바꿔도 소멸 6~15, 인구 242~1,051.
- 살아남는 마을은 명확히 다르다: 어촌2(사냥 2.25)·임업2(돌 1.45)처럼 **식량축이나 돌축이
  둘 중 하나라도 실하면** 산다. 죽는 마을은 비옥도 하나만 좋다.
- `LAB_SEED=n` 으로 랩에서 시드 A/B 가 된다(이번에 만든 계측 손잡이).

---

## ★2026-08-02c 종결 — **소멸 0 달성.** 답은 (가)~(라) 어디에도 없었다

재민 확정: "일단 마을이 절대 소멸하지 않게 튜닝 잘 해야 해."

### 결과 (실지도 800일 · 시드 1020/7/42 · 19마을)

| | 인구 | 소멸/19 | 좀비(<10명) | 도구 |
|---|---|---|---|---|
| 이 문서 작성 시점(2026-08-01) | 1,610 | 1.33 | 8.33 | 2,603 |
| **채택 후** | **3,466** | **0.00 (3/3 시드)** | **0** | 4,796 |

**이주도 강제소개도 순간이동도 쓰지 않았다**(선택지 다 = 미채택 · `tickForceEvacuation` 여전히 OFF).
시딩도 안 건드렸다(선택지 가 = 이미 2026-08-02 에 반영됨, 그것만으론 부족했다).

### 이 문서의 진단은 **옳았다** — 다만 처방이 시딩이 아니라 픽커에 있었다

위 §사인이 적은 그대로다: `도구가 다 닳았다: 돌 0 · 목재 0 · 석공 0 → 생산 ×0.25`.
2026-08-02c 궤적 계측이 그 사슬을 끝까지 따라갔다 — 세 마을(land.stone 0.25/0.70/1.87, **전부 다른 땅**)이
같은 순서로 죽었다:

```
석재 189 → 0  →  석공이 도구를 못 만든다(재료 게이트 stone≥0.2)  →  도구 → 0
  →  전 직업 생산 ×0.25  →  prodK 붕괴(임업3: 61 → 19)
  →  day365 기아사망 보호막 해제  →  40일 만에 54명 → 5명
  →  prodK 가 실현흐름 기준이라 K 가 N 을 따라 내려감(양의 되먹임)  →  바닥까지
  →  N=1 에서 ratio=N/max(1,K)=1 → 로지스틱 항이 정확히 0  →  1↔2 진동 영구 고착
```

그 800일 내내 픽커가 부른 직업은 hunter/farmer/fisher/warrior 였고 **단 한 번도 forager 가 아니었다.**

### 왜 회복 경로가 닫혀 있었나 (이 문서가 몰랐던 것)

`pickDeficitJob_rational` 의 도구자본 우선 블록은 주석으로 이렇게 가정하고 있었다 —
*"재료 없으면 아래 식량직(forage가 돌도 가져옴)으로 자연 회복. 죽음의 나선 방지."*
**그 가정이 거짓이다.** 기근 게이트의 `foodOpts` 는 채집을 `forageLandMean × 0.25` 로 매기는데,
바위 없는 땅(`land.stone` = livelihood.js `FLOOR.stone` = 정확히 0.25)이면 그 값이 0.14 라
농부(`fertility × 1.5` ≈ 1.4)에게 **언제나** 진다. 채집꾼은 영원히 안 뽑힌다.
게다가 별도로 있던 석재 안전망은 `land.stone > 0.25` 였다 — **바닥값 마을이 엡실론 하나로 제외**돼 있었다.

### 처방 (둘 다 채택 · 각각 3시드 개별 A/B)

1. **STONE_NET** — 도구가 치명적으로 부족한데 **재료조차 없으면** 채집꾼을 *기근보다 먼저* 부른다.
   경계도 `>0.25` → `>=0.25`. 단독으로 **소멸 1.33 → 0** · 좀비 8.33 → 0.67 · 인구 +94%.
2. **PRODK_CAP** — `prodK` 를 용량 기준으로(`fuelK` 와 동형). 리비히 min 세 항 중 이것만 실현흐름이라
   K 가 N 을 따라 내려갔다. **단독으론 해롭다**(소멸 3.67 — 못 먹일 인구까지 자란다).
   STONE_NET 뒤에 켜야 좀비 0.67 → **0**. 순서가 전부였다.

기각: TOOLBOOT(석공 안전망 `N>=6` 해제 — 3시드 **비트 동일**, 재료가 binding 이었다) ·
SWITCH2(2명 마을 직업전환 허용 — 무효, 함께 켜면 되레 좀비 +0.67).

### 선택지 재판정

- **(가) 시딩 선별에 땅 품질** — 이미 반영(2026-08-02 `1744e0e`). 필요조건이었지만 충분하지 않았다.
- **(나) 초기 부존 땅맞춤** — 이미 반영(LANDFIT). 위 궤적의 `석재 189` 가 그것이다. **닳으면 끝**이라
  이 문서가 "완화일 뿐"이라 한 판정이 정확했다. 흐름(채집)을 열어야 했다.
- **(다) 강제소개** — **불필요**. 재민 확정("소멸 0 이 달성되면 필요 없다") → `설계_강제소개_이주폐지_정합.md` 보류.
- **(라) 그대로 둔다** — 재민이 기각("절대 소멸하지 않게").
