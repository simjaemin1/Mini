# 생활층 인계 훅 — econ 신기능 본체/생활층 어댑터 명세 (2026-07-13, item 4)

> 목적: 2026-07-13 econ 3종(모시 ramie · 가죽제품 leather goods · 의복 품질 _clothQ)과 기온 모델이 노출하는
> **훅**을 본체(서버)·생활층(관찰 렌더)이 어떻게 소비하는지 자족적으로 명세한다. **econ 무접촉** — 아래 전부
> 훅 미설치=무해(econ은 폴백값으로 동작, A/B 바이트 불변). 커밋: 모시 78a5947 · 가죽제품 b3fead9 · _clothQ 4e38ae4.
> 캐논: `설계_실축화_1셀1m.md` §6·§19 · `durango-mini/CHECKLIST.md`(검증 관례) · `본체이식_계획.md`(웨이브).

---

## 1. 기온 주입 (temperatureAt — 밤낮·고도)

**econ 노출(이미 존재)**: `economy-sim-v2.js:189 temperatureAt(day, hourFrac, elevKm)`
- `hourFrac`: 0=자정(최저)·0.5=정오(최고)의 일교차 사인. **null이면 일교차 0**(econ 일틱은 일평균·야간최저만 소비).
- `elevKm`: 고도 감률 −6.5℃/km. **v.land.elev**(v2:1023 주입 훅, 미설치=0=해수면)에서 옴.
- `v._coldStress`(v2:1026) = clamp(0,1, (coldRef − 야간최저) / 15) → 겨울 한랭 페널티·의복/연료/가죽제품/_clothQ 방한의 구동원.

**생활층 훅 (본체 몫)**:
1. **고도**: 지형층이 마을 중심 셀 고도(m)를 `v.land.elev`(km)로 주입 → 고지대 마을이 정직하게 더 춥다(coldStress↑). 실축 캐논(§1 1셀=1m·존 고도 1.6km)상 위도 아닌 **고도가 한랭 지배**.
2. **시간 곡선(밤낮)**: 생활층 시계(경도 로컬 `_lonOff`, §19)가 `hourFrac`을 만들어 렌더 기온·밤 틴트·NPC 일과에 주입. econ 일 경계는 전역 유지(블록 계약) — hourFrac은 **표시·행동 전용**.
   - 소비처: 밤 오버레이 그라디언트(터미네이터), 야간 귀가 게이트(동쪽 마을 먼저 잠), 겨울 밤 화톳불 연출.

## 2. 신기능 시각화 (관찰 렌더 — econ 필드 읽기 전용)

| 기능 | econ 필드(읽기) | 생활층 렌더 제안 |
|---|---|---|
| **모시(ramie)** | `v.storage.ramie` · 성숙 마을(pop≥40)만 방직 | 모시밭 = 삼밭과 구분되는 밭 텍스처(성숙 마을 근교). 재봉사 작업장에 모시 두루마리 소품 |
| **재봉(tailor)** | `v.counts.tailor` · `v.storage.clothes` | 재봉 작업장(움집형 부속)에 재봉사 NPC 배치 — 베틀·재단 소품. simJob='tailor' 이모지 |
| **가죽제품(leather goods)** | `v._leatherGoods` · `v._lgCov`(0~1 커버리지) | 번영 마커: lgCov↑ 마을은 문간 가죽신·깔개·주머니 소품(비교역 comfort=마을 생활수준 시각). lgCov=0(빈촌·개척기)은 없음 |
| **의복 품질(_clothQ)** | `v._clothQ`(0.3~1.0) | 주민 스프라이트 의복 티어(고품질 마을=고운 겨울옷·모피 안감). _weapQ 무기 광택과 동형 |
| **무두질(tanning)** | `v._tanned`(누적 계측) | 잉여 hide 마을에 무두질대(가죽 널어 말리기) 소품 — 사냥 경제 마을의 생활감 |

**주의**: 위 전부 **읽기 전용**(econ 산출을 렌더만) — 관찰층이 econ에 쓰지 않음(§15 정본 방향: 마을실험실=하네스 기준선, 관찰 물리 회귀 무영향).

## 3. 플레이어 인스턴스 층 — 신기능 연계 (CHECKLIST 166 설계 확장)

CHECKLIST 166의 '장비만 인스턴스·재료 스택·구매 실체화[EMA 샘플]/판매 용해[EMA 넛지]' 방향에 2026-07-13 3종을 접속:

1. **제작 품질 = _clothQ 동형**: 플레이어 의복 제작 품질 = 플레이어 tailoring 숙련(_qSkill 0.4~1.0) × 재료 믹스 등급(**CLOTH_Q_MAT 단일 진실 공유**: 모피1.0>모시0.9>유피0.85>생가죽0.65>삼베0.6). 명장 플레이어 = 고품질 방한 의복.
2. **재료 소스**: 모시(성숙 마을 교역)·가죽/유피(무두질)·모피(사냥)가 플레이어 제작 재료 스택. 가죽제품(신발·주머니)은 플레이어 장비 슬롯(발·소지품) 후보.
3. **경계 계약(구매/판매)**: 플레이어가 마을에서 의복 구매 = 마을 `_clothQ` ±소분산 샘플로 인스턴스 실체화. 판매 = 품질이 마을 `_clothQ` EMA를 넛지(명장 플레이어 옷이 마을 스톡 품질 견인 — 남획이 gameRich에 기록되는 것과 동형).
4. **방어는 갑옷 몫**(역할 분리 유지): 의복=방한·품질, 갑옷=방어(_weapQ 축). 인계 방향 "방어 비권장" 준수.

소재: 본체 서버(인벤토리 확장·equipment·zone.js:938 스칼라 인벤토리 인스턴스화) — **econ 엔진 무접촉**.

---

## 4. 미설치=무해 계약 (검증 관례)
- 위 훅 전부: v.land.elev 미주입→0(해수면), hourFrac null→일교차 0, _leatherGoods/_clothQ/_lgCov 미읽음→렌더만 생략.
- econ 궤적은 훅 유무와 **바이트 무관**(temperatureAt는 econ이 elevKm=v.land.elev만 이미 소비 — 이건 훅 아닌 무조건 경로지만 미주입 시 0). 신기능 필드는 전부 plain number(serializeEcon 안전).
- A/B 시 본체 통합 전/후 마을실험실 회귀 동일해야 함(관찰 렌더는 회귀 무영향 — §15).

---

## ★곳간② 2단계 — 랩 선구현 완료(2026-07-28 야간) · 서버 이식 계획

랩(`~/Mini/전쟁실험실.html`)에 NPC의 **물리적 저장·인출**을 넣었다. 레포 밖 파일이라 커밋 대상이
아니므로, 서버 이식 시 참조할 위치와 규약을 여기 남긴다.

### 랩에 들어간 것 (약 60줄, `★곳간②` 주석으로 전부 표시)

| # | 위치 | 내용 |
|---|---|---|
| 1 | `_granAdd` 정의 직전 | 상수 `G_CARRY=45 · G_STOREW=6 · G_DRAWW=5 · G_DRAW=30` + 헬퍼 3종 |
| 2 | `doTask` 수확 분기 | 수확량을 `_y`로 뽑아 `s.food += _y`(원문 유지) **+ `a.carry += _y`**(짐 적재) |
| 3 | 농부 `work` 분기 | `a.carry >= G_CARRY` 면 `_granGo(life,a,false)` 로 곳간 운반 우선 |
| 4 | 퇴근 전이 | 짐 있으면 곳간 먼저 → 없으면 건축 → 그것도 없으면 `_granGo(...,true)` 인출 후 귀가 |
| 5 | 상태머신 | `toGran`(사다리로 이동) · `gran`(체류=저장/인출 행위) 2상태 추가 |
| 6 | `toHome` 도착 | `a.carry = 0` — 들고 온 것은 집에서 소비 |

헬퍼:
- `_granLadder(g) → {cx:g.cx, cy:g.cy+2}` — **고상곳간은 문이 없다(밀폐)**. 5×3 렉트 `[cy-1..cy+1]`
  바로 남쪽 인접 칸이 사다리 자리이고, **그 칸에 머무는 체류 자체가 저장/인출 행위**다.
- `_granPick(s,a,forDraw)` — 완공(`done`)된 곳간 중 저장은 여유(`stock < G_CAP`), 인출은 재고(`stock > 0`)
  있는 곳의 최근접.
- `_granGo(s,a,forDraw)` — 사다리 칸까지 `moveTo`(경로탐색) 후 `state='toGran'`. **자리·경로가 없으면
  `false`를 반환해 호출측이 기존 흐름을 그대로 타게 한다**(회귀 0).

### ★이중 계상 금지 규약 (이식 시 반드시 유지)

**회계는 econ 소유다.** 수확 시점에 `s.food`가 이미 계상되므로 물리 층은 **다시 더하거나 빼지 않는다.**
이 층이 더하는 것은 오직 '움직임'이고, `g.stock`은 그 움직임의 결과를 보여주는 **물리 장부**일 뿐이다.
(작물 수확이 "식량은 econ 소유, 수확=상태 삭제+연출만"인 것과 동일 원칙.)

### 검증 (랩 원문 헬퍼를 잘라내 node에서 직접 실행 — `/root/work/gran_harness.js` 패턴)

- 상수 로드 · 사다리 자리 `(cx, cy+2)` · 가득 찬 곳간 건너뛰기 · 미완공 제외 · 재고 0 건너뛰기 ·
  곳간 없음/재고 없음 → `false`(기존 흐름 보존) · 장부 보존(120 저장 → 30 인출 = 90 + 30) ·
  `moveTo` 2회 호출(순간이동 없이 경로 접근) — **11항목 전부 PASS**
- 랩 HTML `<script>` 4블록 문법 검사: 편집 전/후 모두 OK

### 서버 이식 계획 (다음 세션)

| 랩 | 서버 |
|---|---|
| 상수 3종 + 헬퍼 3종 | `server/villages.js` — `_lifeGranAdd` 근처에 그대로 |
| `doTask` 수확 분기 | `_cellTask`/수확 처리부 — **서버는 식량을 안 만지므로 `a.carry`만** 추가 |
| 농부 `work` 분기 | `_lifeJobWork`의 farmer 분기 — 짐 상한 시 곳간 목표 |
| 퇴근 전이 | `npcLifeTick` 스케줄 게이트(반일 오후·해질녘) 앞 |
| `toGran`/`gran` | 서버엔 상태머신이 없다 → `npc._lifeTask = {k:'gran', ...}` 로 표현 |
| 라벨 | `운반`·`저장`·`인출` — **셋 다 랩에 이미 있는 라벨**(신규 발명 아님) |

★서버 이식 전 확인: 랩을 화면에서 몇 게임일 돌려 수확→운반→저장→인출→귀가 사이클이 눈으로 보이는지
(컨테이너에서는 브라우저 실행이 불가해 헤드리스 로직 검증까지만 했다).
