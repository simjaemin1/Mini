# 산 부수기 · 광맥 접근 — **기존 설계서와의 차이 정리**

> ⚠️ 이 문서는 처음에 "결정 필요 7가지"로 썼다가 갈아엎었다.
> `~/Mini/설계_광맥과_산부수기.md` 에 **재민이 이미 써 둔 설계**가 있었고(내가 늦게 읽었다),
> 거기서 대부분이 이미 정해져 있다. 아래는 **그 설계 기준으로** 지금 뭐가 어긋나 있는지만 적는다.

---

## 0. 재민 설계서가 이미 정한 것 (그대로 따른다)

| 항목 | 정해진 것 |
|---|---|
| 무엇으로 부수나 | **길드 전용 특수장비**. 산 타일 = 극도로 견고, 길드 단위 노력으로만 |
| 부수면 뭐가 나오나 | **광맥 안의 산 타일 = 부수기가 곧 채굴**(번영도 차감). 광맥 없는 산 = 돌 + 통로만 |
| 어디를 | 제한 없음(광맥 밖은 통로만 나오니 자연히 억제된다) |
| 배치 | 광맥을 산맥 **가장자리에 걸치게**(straddle) — 바깥 셀은 서서 캠, 안쪽 셀은 부숴야 접근 |
| 안쪽의 보상 | 안쪽 셀에 **번영도↑ 또는 귀광물 풀** |
| 구현 순서 | ①길드 전용 특수장비 → ②산 타일 부수기 → ③광맥 straddle 재배치 + 부수기↔광석 연동 |

⇒ **①이 없으므로 ②·③은 아직 손대지 않는다.** 내가 제안했던
"불질로 부수고 · 굴착과 채굴을 분리한다"는 **철회한다** — 재민 설계와 어긋난다.

---

## 1. 지금 어긋나 있는 것 — 배치가 straddle 보다 **더 깊이** 들어갔다

11차 지시 *"좀 더 산 안쪽으로 들어갔으면 좋겠어"* 대로 옮겼는데, 설계서의 straddle
(*"클러스터 절반은 rock 아래, 절반은 평지"*)보다 **한참 안쪽**이다. 실측(대·중·소 61개):

```
중심이 바위 안        45개 / 61 (74%)     ← 설계서 기준이면 ~50% 근처여야
광맥별 노출(평지) 비율  최소 9% · 중앙 27% · 최대 100%
                       (= 중앙값 기준 73%가 바위 아래)
노출 셀이 0인 광맥      0개 / 61            ← 완전히 막힌 광맥은 없다
평균 p  광맥 전체 0.0517  vs  노출부만 0.0373   ← 노출부가 28% 가난하다
```

두 갈래다. **재민 판단 필요:**

- (가) **이대로 둔다.** 11차 지시(더 안쪽)를 설계서(straddle 절반)보다 우선한다.
  → 산 부수기 전까지 큰 광맥은 노출부 27%만 캘 수 있다. NPC 광부는 안 굶는다(최소 9%).
- (나) **straddle 로 되돌린다.** 노출 비율 중앙값을 ~50%로 맞춘다.
  → `plan-ore-clusters.js` 의 대·중·소 점수 `(0.10 + rf×4.0) × lf^0.25` 에서
    rock 편중을 낮추고 lf 하한을 올리면 된다. 재배치·재감사 3분.

> 의견: **(가)**. 세 번째 줄이 그 이유다 — 굴을 파면 품위가 **1.39배**가 되고,
> 그 격차가 p 기울기(안쪽일수록 큼)에서 **자동으로** 나온다. 별도 보정 상수가 필요 없다.
> 설계서의 *"안쪽 셀에 번영도↑ 또는 귀광물 풀 적용"* 이 이미 구조적으로 충족돼 있는 셈이다.
> 다만 27%는 얕은 편이라, 산 부수기가 들어오기 전 기간이 길어지면 답답할 수 있다.

## 2. 지금 상태 (산 부수기가 없는 동안)

- 큰 광맥은 **노출부에서만** 캐진다(완전 봉쇄는 없다 — 최소 9% 열림).
- ⚠️ NPC 광부 정원(`villages.js` `_oreCellsOf`)은 **노출 여부를 안 본다**.
  바위 아래 셀까지 세서 정원을 잡으므로 **광부가 실제보다 많이 배정된다**.
  · 지금 당장 치명적이진 않다(광부가 노출부에서 계속 캐고 재고는 셀별이라 고갈이 분산된다).
  · 산 부수기(②)를 넣을 때 같이 고치는 게 맞다 — 그때 "부술 수 있는 셀"의 정의가 정해지므로.
- 자잘 광맥은 **바위 위에 아예 안 놓인다**(배치기가 rock 셀을 후보에서 뺀다).
  산 부수기 없이도 전부 캘 수 있다 — 플레이어 전용 발견 요소라 그게 맞다.

## 3. 곁다리 회부 — 자잘 광맥의 **발견 기록**

자잘 광맥 2600개가 **미니맵에 전부 그려지고 있었다**. 그러면 "걸어 다니다 만난다"가 없어져
*"훨씬 많아야 해.. 그래야 탐험하는 재미가 있지"* 가 성립하지 않는다.
⇒ 일단 **미니맵에서 자잘을 뺐다**(`public/client.js`, `o.minor` 건너뜀). 큰 광맥 61개만 나온다.
   (부수 효과로 프레임당 arc 2600개도 사라졌다.)

갈리는 것 하나 — **한 번 찾은 자잘 광맥을 지도에 남길 것인가?**

- (가) 안 남긴다. 매번 기억으로 찾아간다.
- (나) **판 자잘은 표시로 남는다**(플레이어별 발견 기록).
  · 저장: `players` 에 발견 셀 목록 · welcome 때 내려보냄 · 미니맵에 점으로
  · 규모: 플레이어당 수십~수백 개라 부담 없다
- (다) (나) + **거래 가능**. 광맥 위치가 정보 상품이 된다.

> 의견: **(나)**. 발견에 흔적이 남아야 탐험이 노동이 아니라 축적이 된다.
> (다)는 매력적이지만 지금 거래 구조에 얹을 자리가 없다 — 나중 일.

※ 개발용 셀맵(`~/Mini/cell-map.png`)에는 자잘을 **다른 색**으로 남겼다(분포 감사용).
   게임 안에서 보이는 지도가 아니다.

## 4. 설계서 쪽에서 낡은 것 (참고 — 지금 코드와 다름)

`설계_광맥과_산부수기.md` 의 "✅ 구현 완료" 절은 **11차 재설계 이전** 내용이다:
- `miningParams` 의 cost / refillMs / dropChance / 티어 → **폐지됨**(1초/타 · 60타 = 재고 1 · 덩이 1)
- "리필시간 대폭 증가 예정" → 리젠이 닫힌 해로 교체됨(`dR/dt = 0.02 − 1e-5·R`, 완전회복 190 게임년)
- 광맥 41개 반경 700 → 지금 **2661개**(대·중·소 61 + 자잘 2600), 반경 4~130셀
- 확률 드롭 → **p 연속장**(`oreProbAt`, 셀마다 다르고 셀들끼리 연속)

straddle·산 부수기 절(📋 계획)은 **그대로 유효**하다.
