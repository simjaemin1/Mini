# 설계 — 화면 규칙 (방향 B · "먹선") [재민 확정 2026-09-03 · T66 의 정본]

> 목업 정본: 클로드 디자인 캔버스 "듀랑고 미니 화면 디자인"(아티팩트 f1ca6af8). 그림 사본: `~/Mini/산그림/디자인B/`(HUD·짐·게시판·로그인·화면규칙 다섯 장).
> 이 문서는 그 캔버스의 **수치**를 옮겨 적은 것이다. 충돌하면 캔버스가 이기고, PM 이 이 문서를 고친다.

## §1 재민 확정 (문장 그대로)
1. 방향 **B "먹선"** — 검정 판에 흰 선, 장식 0. (A 가죽·청동, C 목판·한지 는 기각 · 캔버스 2페이지 보관)
2. **이모티콘 남용 금지** — UI 에 이모지 0.
3. **로그인의 캐릭터 색 선택은 없앤다.**
4. ★**모든 아이템은 그림이 하나다** — 인벤 · 조합법 창 · 바닥에 떨어졌을 때 · 거래소 · 창고 목록 어디서나 **그 3D 모델을 구운 같은 렌더**가 보인다.
5. ★**가구·시설도 같다** — 해체해서 인벤에 넣을 수 있으므로, 세계에 서 있을 때와 인벤에 있을 때가 같은 그림이어야 한다.
   ⇒ **물건 하나 = 모델 하나 = 렌더 둘**(세계용 아이소 스프라이트 · 아이콘 96px). 아이콘 없는 품목은 **이모지가 아니라 점선 빈 칸**("렌더 없음")으로 두고 굽는다.

## §2 토큰 (`style.css` 의 CSS 변수 정본 — 이름은 T66 이 정하되 값은 이것)
| 이름 | 값 | 쓰임 |
|---|---|---|
| 바탕 | `#0b0b0b` | 로비 배경 · 표 안쪽 `#0f0f0f` |
| 판 | `rgba(11,11,11,.92)` | 모든 패널 — 게임이 8% 비친다 |
| 판 머리 | `#171717` | 패널 제목 줄 |
| 테 | `#333333` · 밝은 테 `#4a4a4a` | 1px · 버튼 테는 밝은 쪽 |
| 글 | `#e6e6e6` · 제목 `#f2f2f2` · 흐림 `#8a8a8a` · 더 흐림 `#5a5a5a` | 본문 13 · 보조 11~12 · 제목 14 굵게 |
| 강조 | `#e6b450`(밝음 `#f2c96a`) | **하나뿐** — 선택·활성·허기 막대·"낼 수 있음" 딱지 |
| 체력 | `#c9453a` | 막대·무들·아이콘 |
| 갈증 | `#4a90c2` | 〃 (추위 무들도 이 색) |
| 숨(스태미나) | `#7fa650` | 〃 |
| 주요 버튼 | 배경 `#e6e6e6` 글 `#0b0b0b` 굵게 | "나루터로 간다" 하나뿐 |

* 글꼴 **하나**: `'Noto Sans KR','Apple SD Gothic Neo',system-ui,sans-serif`(웹폰트 로드는 하지 않는다 — 시스템에 있으면 쓴다). 숫자·시각·무게·핑은 고정폭 `'IBM Plex Mono','SF Mono',Menlo,monospace` + `font-variant-numeric: tabular-nums`.
* 모서리 **0** · 그림자 **0** · 테 1px · 간격 눈금 4·8·12·16 · 단축키 표기는 `.key`(고정폭 10px · 테 1px · 패딩 1×5).
* 막대: 높이 10 · 배경 `#0f0f0f` · 테 1px · 채움은 축 색 · 숫자는 오른쪽 고정폭.
* 무들: 판 + 축 색 테 + 아이콘 + **한 단어** + 3단계 점(5px 정사각 · 켜진 단계만 축 색).

## §3 아이콘
* UI 틀(레일 10 · 수치 4 · 분류 · 지도 · 상태 줄)은 **선 아이콘 한 세트**: 24 그리드 · 선 1.6 · 둥근 끝 · `stroke: currentColor` · 인라인 SVG(파일 하나 · 이름으로 찾는다). 캔버스 "화면 규칙" 판의 34개가 초안 — 세션이 같은 문법으로 채운다.
* **아이템·가구는 선 아이콘이 아니다**(§1-4·5). `/assets/icons/<key>.png` 렌더 하나가 정본이고 바닥 낙하물도 같은 PNG 를 그린다. 없으면 점선 빈 칸.

## §4 자리 (HUD 재배치 0)
좌상 상태 줄(존 · 연/계절/일 · 시각 · 날씨 · 인원 · 핑) · 좌측 레일(아이콘 10 · 활성 항목만 오른쪽에 이름+키 꼬리표) · 좌하 수치 4막대 + 적대감/PvP 한 줄 · 그 아래 무들 칩 · 맨 아래 채팅 한 줄 · 하단 가운데 핫바 3 + 짐 무게 판 · 우상 지도 판. 개발용 좌표·속도 줄은 **토글(기본 숨김)**. 촌장 한 줄은 상단(레일 오른쪽)에 — 뜨고 사라지는 규약은 현행.

## §5 화면별
* **짐**: 머리(제목 · 키 · "무거우면 느려진다" 한 줄 · 무게 막대 · 수치) · 두 열(내 짐 | 상자/바닥) · 열 머리(아이콘 · 이름 · 종수 · kg) · 표 머리(이름 · 무게 · 상태 · 분류) · 행 = 렌더 아이콘 26px · 이름 ×n · kg 고정폭 · 상태(신선/내구/보관일) · 분류(흐림) · 화살표 · 오른쪽 상자 탭 세로(아이콘 + 개수).
* **게시판**: 머리 · 겨울나기 띠(제목 · 진행 수/목표 · 막대 · 기여자) · 의뢰 행(렌더 아이콘 · 품목 ×n · "낼 수 있음" 딱지 · 보상 · 남은 날) · 바닥 소식 한 줄 + `⇧N 납품`.
* **로그인**: 가운데 판 두 열 — 왼쪽 이름 · 비밀번호 · **나루터로 간다** · 게스트 안내 한 줄 + "새로 시작" · 오른쪽 "어디서 시작하나" 마을 카드 셋(이름 · 유형 · 근황 한 줄 · 혼잡 · 이방인 배지) · 아래 "조작법 보기"(접힘). **색 선택 없음.**

## §6 T66 카드 범위 (클라 단독 · 행동 변경 0)
`style.css` 토큰 재작성 · 패널 프레임 함수 하나로 전 패널 통과 · 이모지 → 선 아이콘 세트 · 아이템 이모지 폴백 → 점선 칸 · 로그인 색 선택 삭제(서버 필드는 기본값 유지) · 개발용 줄 토글 · 낙하물 = 같은 PNG 접점. **아이콘·가구 렌더 굽기는 ART 카드**(별건).
