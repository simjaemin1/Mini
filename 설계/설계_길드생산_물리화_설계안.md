# 길드 생산 물리화 — 설계 문서 (구현 전 판단용)

작성: 10차 야간(2026-07-29). **구현하지 않았다** — 규칙 §1-9(설계 판단은 구현 전에 올린다)에 따라
단위 충돌·타이밍·이중 계상 방어까지만 정리한다. 코드 변경 0.

---

## 0. 무엇을 물리화하자는 것인가

지금 **마을 생산은 실체가 없다**. 60초 틱이 직업 집계로 산출량을 만들어 `central.tribeTreasury(tribeId, prod)`로
길드 금고(회계)에 바로 얹는다(zone.js 5202~5217). 어디에도 물건이 놓이지 않는다.
"물리화" = 그 산출을 **길드 곳간(`buildings.type='guild_granary'`)에 실물로 쌓는 것**.

9차에 깐 장부 계약(`server/guild-treasury.js`)이 전제다:

> 곳간 `data` = 물리 실체 · `tribes.treasury_json` = 길드 총자산 · **물리 ⊆ 회계** · 같은 물건을 두 번 세지 않는다.

---

## 1. 실재하는 세 장부 — 단위가 서로 다르다 (핵심 장애물)

| 장부 | 위치 | 단위 | 성격 |
|---|---|---|---|
| ① 길드 곳간 | `buildings.data` (guild_granary) | **아이템명 → 개수** (`{wood:10, stone:3}`) | 물리. 플레이어가 넣고 뺀다(`tryChestPut/Take`) |
| ② 길드 금고 | `tribes.treasury_json` (central) | **자원명 → 수량** | 회계. 마을 생산·곳간 델타가 합류 |
| ③ 마을 곳간 | `vil._granStock` (Map "cx,cy"→수) | **수확한 칸 수** | 물리. econ 무접촉이 명시 계약 |

★**단위 충돌의 정체**: ③은 "무엇이 몇 개"가 아니라 **"몇 칸을 수확했나"**다. 서버 `CROPS`에는 랩의 `_y`
(칸당 산출)에 해당하는 값이 아예 **없다**(villages.js 2434~2439 주석이 그 이유를 명시: 서버는 식량 수치를
만들지 않는다 — 수확 = 상태 삭제 + 연출). 그래서 ③ → ①의 환산은 **지금 데이터로는 정의 자체가 불가능**하다.
환산표를 새로 만드는 순간 그것은 econ이 소유하던 산출량 결정권을 지형 층이 가져오는 것이 되고, 규칙 §1
"econ 엔진 수정 금지"의 정신에 정면으로 걸린다.

⇒ **결론 1: ③(마을 곳간)을 물리화 경로로 쓰면 안 된다.** 물리화 대상은 ②→① 방향뿐이다.

---

## 2. 이중 계상 위험 — 정확히 어디서 터지는가

현행 동기(`syncGranary`)는 곳간 내용물의 변화량(현재 − `_tr` 스냅샷)을 **회계에 더한다**.
즉 "곳간에 뭔가 늘었다 = 플레이어가 넣었다 = 총자산이 늘어야 한다"를 가정한다.

생산분을 그대로 곳간 `data`에 써 넣으면:

1. 60초 생산 틱이 `tribeTreasury(+wood 5)` → 회계 +5
2. 같은 틱이 곳간 `data.wood += 5` → 물리 +5
3. 다음 `reconcileGuildGranaries`가 델타 +5를 발견 → `tribeTreasury(+wood 5)` → **회계 +5 또 붙음**

→ **생산 1회당 회계 2배**. 곳간에 쌓일수록 오차가 누적된다(9차에 막은 것과 정확히 같은 계열의 결함).

### 방어 규약 — `_tr`을 같이 밀어야 한다

물리화 입고는 "회계엔 **이미** 반영된 물건을 물리 자리에 놓는 것"이다. 따라서 입고와 동시에
보고 스냅샷 `_tr`도 같은 만큼 올려서 **델타를 0으로 유지**해야 한다.

```
// server/guild-treasury.js 에 추가될 유일한 신규 함수(설계안 — 미구현)
function depositAccounted(b, delta) {   // 회계에 이미 반영된 물건을 물리 곳간에 놓는다
  for (const [k, v] of Object.entries(delta)) {
    b.data[k]  = (b.data[k]  || 0) + v;
    b.data._tr = b.data._tr || {};
    b.data._tr[k] = (b.data._tr[k] || 0) + v;   // ★같은 만큼 스냅샷도 밀어 델타 0 유지 = 재보고 금지
  }
}
```

이 한 줄 규약이 없으면 물리화는 반드시 장부를 깨뜨린다. 하네스 항목도 같이 정해 둔다:

- ⑨ 생산 물리화 입고 후 `pendingDelta(b.data)`가 **빈 객체**여야 한다.
- ⑩ 입고 N회 반복 후 `granaryItems ≤ treasury` 불변식이 유지되어야 한다.
- ⑪ 입고 직후 재부팅(reconcileAll) 시 회계가 **한 톨도 안 늘어야** 한다(9차 부팅 자가치유와 같은 성질).

---

## 3. 타이밍 — 두 틱이 이미 같은 주기다

`reconcileGuildGranaries('주기')`와 생산 틱은 **같은 60초 setInterval 안**에 있다(zone.js 5202~5203).
순서는 reconcile → 생산. 물리화 입고를 생산 뒤에 두면 다음 60초 reconcile까지 60초 창이 열린다.
`depositAccounted`를 쓰면 그 창에서도 델타가 0이라 안전하다. **신규 타이머는 필요 없다**(§1 신규 채널 금지와 정합).

단, 그 60초 창에서 **플레이어가 물건을 꺼내 가면** 델타가 음수로 잡혀 회계가 정확히 줄어든다 — 이건 옳은 동작이다
(물리에서 빠졌으니 총자산도 준다).

---

## 4. 물리화가 새로 만드는 문제 4가지 (전부 판단 사안)

### (가) 운반 주체가 없다
마을 생산은 마을 NPC가, 길드 곳간은 플레이어가 지은 건물이다. **둘을 잇는 사람이 없다.**
durango-consistency-principle(순간이동·추상화 금지)을 지키려면 NPC가 곳간까지 **걸어가야** 한다.
곳간②(`_granGo`/`_granLadder`/사다리 체류) 파이프라인이 이미 있으므로 대상 포인터만 길드 곳간으로 넓히면
되지만, 마을 곳간과 길드 곳간은 좌표계·소유가 다르다(마을 격자 vs `buildings` 행). **가장 큰 미구현 조각.**

### (나) 용량 초과분의 행선지
곳간 1동 물리 수용은 마을 곳간 기준 `G_STOCK_CAP=60`. 길드 곳간엔 현재 상한이 없다.
상한을 두면 넘친 생산은 어디로 가나 — 회계에만 남기면 "물리 ⊆ 회계"는 지켜지지만 물리화는 반쪽이 된다.
상한을 안 두면 곳간 하나에 무한히 쌓여 곳간 증설 압력(마을 곳간의 성장 패턴)이 사라진다.

### (다) 약탈 표면이 생긴다
회계 자산은 훔칠 수 없다. 물리화하는 순간 `tryChestTake`의 길드원 게이트만 통과하면 가져갈 수 있고,
건물 파괴 시 유실 규칙도 정해야 한다. **게임 설계 판단**(방어 가치가 생기는 대신 손실 위험이 생긴다).

### (라) 자원명 사상
생산 `prod`의 키(econ 자원명)와 곳간 아이템명이 1:1인지 검증되지 않았다. 어긋나면 회계엔 있는데
물리로는 놓을 수 없는 자원이 생긴다(그건 그냥 회계로 남기면 되지만 **명시적 화이트리스트**가 필요하다).

---

## 5. 설계안 3가지

### 안 1 — **일일 배당 물리화**(권장 검토안, 위험 중)
- 60초마다가 아니라 **게임일 1회**, 그날 생산 누계 중 화이트리스트 자원만 길드 곳간에 `depositAccounted`로 입고.
- 운반: 마을 NPC 1명이 곳간②의 실보행 파이프라인으로 길드 곳간 사다리까지 간다(도착해야 입고 확정).
- 넘치면 회계에만 남긴다(불변식 유지, 로그로 명시).
- 장점: 눈에 보이는 물건이 생기고 이중 계상은 `depositAccounted` 한 규약으로 막힌다.
- 단점: (가) 운반 경로 구현이 실질 작업량의 대부분. 길드 곳간이 마을에서 멀면 배달이 영영 안 온다.

### 안 2 — **전면 물리화**(비권장)
- 모든 생산이 곳간을 거쳐야 회계에 반영. treasury는 곳간의 그림자.
- 단점: 곳간이 없거나 부서지면 생산이 통째로 사라진다 → econ 성장 곡선이 지형 층 사고에 종속. 규칙 §1 위반 소지.

### 안 3 — **표시만 물리화**(위험 0)
- 장부는 그대로 두고, 길드 곳간 앞 **짐더미 렌더**만 최근 생산량에 비례해 키운다(8차 `gran_pile1~3` 재사용).
- 장점: 이중 계상 위험 0, 코드 최소. 단점: 실체가 없다(집을 수 없다).

---

## 6. 재민 판단 필요

1. **어느 안인가** — 안 1(일일 배당) / 안 2(전면) / 안 3(표시만).
2. 안 1이면 **운반 규약**: NPC가 실제로 걸어야 하나, 아니면 "마을 = 길드 영역이면 즉시 입고"를 허용하나
   (후자는 순간이동 금지 원칙과 충돌 — 예외를 인정할지의 문제).
3. **용량 상한**을 길드 곳간에도 둘 것인가(두면 곳간 증설이 게임 목표가 되고, 안 두면 창고 1동으로 끝난다).
4. **약탈·파괴 시 유실**을 인정할 것인가(물리화의 진짜 대가).
5. 화이트리스트 자원 목록 — 무엇까지 물건으로 놓을 것인가(식량·목재·석재만? 가죽·약재까지?).

---

## 부록 — 확인한 코드 위치(전부 실재 확인)

- 생산 틱 → 회계: `server/zone.js` 5202~5217 (`villageProduction` → `central.tribeTreasury`)
- 곳간 동기·재동기: `server/zone.js` 4323~4334(opts) · 4373 · 4448 · 5183~5200
- 장부 계약·정확히 한 번: `server/guild-treasury.js` 전문
- 마을 물리 곳간(칸 수 단위)·단위 부재 사유: `server/villages.js` 2434~2443
- 물리 재고 방송(연출): `server/villages.js` 2486~2510, `public/client.js` 3494~
