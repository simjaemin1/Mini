# 설계 — 생산은 행위다 (캐논 초안 · PM 2026-09-18 · 재민 확정 칸 `[재민: __ ]`)

> 재민 09-18: **"npc 가 이동하고 일을 할 때 창고에 들어와야지. 매일 한 번씩 일괄로 처리하는 게 아니라, npc 가 낚시하는 순간 물고기가 들어가고, 나무를 베는 순간 통나무가 생기고, 광질하는 순간 광석이 쌓이는 거라고."**
> 이 문서는 그 말을 캐논으로 세우고, 지금 코드가 어디서 어긋나는지와 옮기는 순서를 적는다. 전쟁 캐논(`설계_전쟁_플레이어.md`)과 같은 꼴 — 칸이 채워지면 그 줄이 캐논이다.

## 0. 지금 코드 — 무엇이 추상인가 (2026-09-18 · main `50039540`)

| 층 | 지금 | 자리 |
|---|---|---|
| **econ 하루 틱** | `tickVillage(v, day)` 가 npc 마다 `addProduce(품목, base × land × skill …)` — **행위 없이 수식으로** 곳간에 넣는다. 어부·나무꾼·광부·사냥꾼·채집꾼 전부. | `sim/economy-sim.js:2111~` · `JOB_OUT` |
| **생활층 · 관측자 있는 마을** | 실걸음 크루(농부가 밭 셀에 가서 수확 · 사냥꾼이 짐승을 쏜다) — 그러나 **곳간 회계는 하루 틱이 한다.** 사냥은 명시적으로 *"보이는 사냥은 장부의 연출"*(T158) · 수확만 `harvestToGranary` 로 행위가 곳간을 움직인다(T179/T190). | `server/villages.js` 생활층 · `:5425` T146 · `:4388` T190 |
| **생활층 · 관측자 없는 마을** | **헤드리스 결산** — "랩 빨리감기 · 하루치 물리 결과 일괄 적산"(`_lifeHeadlessDay`). 몸이 없다. ★[T316] `T312_FISH_ACT` 를 켜면 **몸이 생긴다**(개간·신축·작물은 여전히 여기서 일괄) — 비용·굶주림은 보고/T316 §3. | `villages.js:5726` |
| **랩 관찰 모드 — 행위가 장부다(재민 09-18 지적 · PM 정정)** | 농부는 셀에서 수확하는 순간 `s.food += _y`(즉시) · 채집은 숲에서 딸 수 있는 만큼(T135 `forageTakeFn`) · 나무꾼은 벨 때마다 `_wcut`(`:11584`) · 사냥꾼은 잡은 수 `_hkill` — econ 이 **다음 날** 읽는다(`huntIncomeFn`·`woodIncomeFn` real · `:9741` "밭·채집은 이미 실물인데 사냥만 남아 있었다"). **추상은 둘뿐**: ① `L_HUNTINCOME`·`L_WOODINCOME` **기본값 'abstract'**(PM 의 "손잡이 기본 끔" 규약이 실물 모드를 기본에서 밀어냈다) ② **빨리감기**(doBulk · 800일 자) — 몸이 없다. | `lab/전쟁실험실.html:11469` `lifeDayAll` · `:9680·9740` · `:11584` |
| **서버 이식** | 랩의 **추상 기본값** 쪽을 가져왔고 거기에 "보이는 사냥은 연출"(T158)을 얹어 몸을 장식으로 만들었다. 실물 훅(`huntIncomeFn`·`woodIncomeFn`·`forageTakeFn`·`stoneBudgetFn`)은 서버가 **안 준다** → 폴백 = 수식. | `economy-sim.js:2330~` · `villages.js:5425` |
| **어부 — ★[T312 2026-09-19] 옮겼다(손잡이 끔)** | `T312_FISH_ACT=1` 이면 어부는 물가 셀로 걸어가 **셀 예산**에서 종 하나를 낚아 `inventory[종]` 에 들고, 짐이 차거나 낮이 끝나면 `fishToGranary` 로 곳간에 넣는다. 그 마을에서 `addProduce('fish')` 는 **호출 0**(T100 4판 문법). 죽으면 **플레이어 낙하 정본**이 그대로 떨어뜨린다(어부 전용 경로 0). ★**[T316 2026-09-19] 관측자 없는 마을도 걷는다** — 존의 **문 둘**(결정 `:11059` · 이동 `:11255`)을 같은 손잡이로 연다(둘째 손잡이 0 · `_lifeHeadlessDay` 의 어부 칸 삭제). ⚠**그런데 켤 수 없다** — 51마을 1,476명 전원이 걸으면 존 틱 p50 **2.466 → 374.643ms(152배)** · lag **37.3%** · drop 675(게임일당 CPU ≈ 22.5분 — T297 외삽 10~32분 띠 안). 게다가 결정 문의 15ms 예산이 **앞에서부터 자르므로**(이어 돌기 없음) 실제로 일하는 주민은 **17.5%** 뿐이고, 하루 입고는 물가에 선 마을 안에서도 수식의 **26~38%** 다(세계 합 4.8%). **예산 판정이 먼저다**(T316 §8 회부 1). ⚠끄면 **넷째 판 비트 동일**(3시드 전수 실측). | `economy-sim.js` `fishToGranary`·`fishActOn`·`fishBudgetPerCell` · `villages.js:5976~` · `server/freshfish.js` · 보고/T312_2026-09-19.md |
| **자원 실체** | 나무·바위·광맥은 **청크가 켜질 때** 생기는 시드 자원(관측자 의존) — T284 회부 ①. 물고기는 개체가 없다(`land.fish` 수). | `zone.js` 청크 · `qtResources` |

한 줄로: 랩 관찰 모드는 이미 **몸이 하는 일이 장부**였고(정산만 하루 늦었다), 추상화한 것은 **이식과 PM 규약**(기본값 abstract · T146 "kill→곳간은 다음 판" · T158 "연출")이다. 그러니 할 일은 새로 짓기가 아니라 **랩의 실물 모드를 서버의 유일한 모드로 옮기기** — 추상 폴백 없이 · 정산은 다음 날이 아니라 행위/귀환에(ⓐ) · 관측자 없는 마을도 몸으로(ⓑ).

## 1. 재민 확정 원칙 (2026-09-18)

1. **생산은 행위다.** 물고기는 어부가 낚는 순간, 통나무는 나무꾼이 베는 순간, 광석은 광부가 캐는 순간 생긴다. 하루 일괄 `addProduce` 는 **없어진다**(사람의 일에서).
2. **NPC 는 실제로 이동해서 일한다.** 현장(강가·숲·광맥)에 가지 않으면 아무것도 안 생긴다.
3. (전쟁 캐논 2 와 같이) **세계는 항상 실체다** — 관측자가 없어도 마을은 돈다. **[재민 확정 09-18: 관측자 없어도 실걸음]**
4. **[재민 확정 09-18] 반입 시점** — 낚는 순간 **손에**, 귀환하면 **곳간에**. 도중에 공격받아 죽으면 아이템은 **그 자리에 떨어진다**(무게 모델·낙하 규약 그대로). 비용이 너무 커지면 **낚는 순간 곳간**이 1순위 대안 — 판정은 세계 위 틱 p50(T284 자).
5. **랩의 두 모드(정정)** — 관찰 모드는 농부(즉시)·채집(T135)·나무꾼(`_wcut`)·사냥꾼(`_hkill`)이 행위가 장부다 · **어부·광부는 랩에서도 수식**(`:2881` `land.water × 1.2`) · 빨리감기(800일 자)는 몸이 없다.

## 2. 결정 칸

| # | 물음 | 후보 | 재민 |
|---|---|---|---|
| ⓐ | **반입 시점** | ② 손에 → 귀환 시 곳간 · 죽으면 그 자리에 낙하 · 비용 크면 ① 즉시 곳간이 1순위 대안 | **② (재민 09-18)** |
| ⓑ | **관측자 없는 마을** | ① 항상 실걸음 | **① (재민 09-18)** |
| ⓒ | **빨리감기(800일 자)의 지위** | 기대값 모델로 남겨 **자로만** 쓴다 · 새 자 하나: **관찰 모드 하루 합 ↔ 빨리감기 수식 차이(3시드 · 직업별)** — 폭 안이면 800일 기준선이 산다 · 밖이면 그 직업 수식이 틀린 것(발견) | **PM (재민 "잘 조정해봐")** |
| ⓓ | **옮긴 첫날** | ① 행위 파라미터를 기존 수식에서 유도해 **첫날 하루 합 = 수식**(세계 무변) · 그 뒤로는 실체가 내는 만큼(강이 멀면 덜 잡는다) · 새 수 0(전부 유도 · 유도 목록은 회부에) | **① (PM · 재민 "잘 조정해봐")** |
| ⓔ | **하루가 남는 것** | 부패·가격·계절·작물 성장·인구·직업 전환 = 시간의 일 · 하루 틱에 남는다 | **PM (재민 위임 09-18)** |
| ⓕ | **자원의 유한성** | ① 강가 셀 예산(하루 재생) · **종별** — 민물고기 10종(`설계_민물고기.md`) · 재생량은 지금 수식에서 유도(값 안 박음) | **재민 09-18: 민물 10종 · 재생량은 PM 유도** |
| ⓖ | **순서** | 어부 → 나무꾼 → 광부 → 채집 → 사냥 → 농부 | **PM (재민 위임 09-18)** |
| ⓗ | **관측자 의존 자원** | 관측자 무관 자원 색인 선행 → **T301(세션5)** | **PM (재민 위임 09-18)** |
| ⓘ | **NPC 인벤토리** | 지금은 `npc._carry`(품목 없는 짐 칸 수 · 상한 3)뿐 · 플레이어만 `inventory`+무게 모델(`carry.js`). **새 체계 0** — NPC 도 플레이어와 같은 꼴(`inventory` 품목별 수 · `carry.js` 같은 무게 · 같은 낙하 함수 · 사본 0) · `_carry` 는 어부 카드에서 흡수 · 포로·전사자 낙하·군량이 같은 칸을 쓴다 | **PM (재민 09-19 "npc 각자 인벤토리" 확인)** |

## 3. 위험 — 숨기지 않는다

* **부하.** T1 실측(공통 §2): 하루 처리 중 econ 99ms(5%) · **생활층 1,620ms(80%)** — 관측 마을 몇 개일 때 그렇다. ⓑ① 이면 51마을 2,677명이 매 틱 걷는다. 자는 T284 와 같다: **세계 전체 부하 위에서** 존 틱 p50/p95 · 틱 빚. 상한이 나오면 그 수가 곧 설계값(마을당 실걸음 인원 · 나머지는 어떻게 — 이것도 재민 칸).
* **결정성.** 행위 생산은 걸음·경로·충돌에 매인다 — 같은 씨로 같은 결과여야 자가 선다(T284 ⓓ 문법 그대로).
* **기준선.** ⓓ② 면 셋째·넷째 판(6,889/7,148/6,259 · 8,318/7,935/7,853)이 자로서 죽는다 — 다시 잰다.
* **두 벌 금지.** 행위 층이 곳간을 움직이면 그 품목의 `addProduce` 는 **지워야** 한다(공존 = 이중 생산). T135 가 그 문법이다(`repl` 걷어내기).

## 4. 순서 (카드)

1. **T297 (세션3 · 측정 · 코드 0) — 완료 09-19(main): 관찰 헤드리스 결정론 · 밭은 등가(소수 다섯 자리) · 고기·채집·생선은 30일이면 다른 세계 → 등가는 하루 · 어부 8.3마리 = 왕복 한 번 ○ · 광부 750g ✗(#43 재민) · 브라우저 외삽 하루 11.7분(서버 수는 T312)** — 랩 실물 모드(`L_HUNTINCOME=real`·`L_WOODINCOME=real`·T135·T100 4판)가 관찰 모드에서 하루에 내는 양 vs 빨리감기 수식 — **둘의 차이를 3시드로**(등가 자의 바닥) · 직업별로 지금 수식이 하루에 내는 양을 **행위 단위로 역산**: 어부 1인 하루 = 몇 번 × 몇 마리 · 강가까지 거리 · 걸음 시간 → 하루에 가능한 왕복 수 → **가능한가**(고증: 청동기 어부 하루 조업). 안 되면 수식이 틀린 것이고 그게 첫 발견이다. 관측자 없는 마을이 실걸음일 때의 부하 추정(마을 51 × 실걸음 인원)도 표로.
2. **관측자 무관 자원 색인**(ⓗ) — 나무·바위·광맥을 시드 함수로 어디서든 질의(청크 무관).
3. **어부 — T312(세션3 · 09-19)** — NPC 인벤토리(ⓘ) 같이 · 강가 현장 · 던짐/입질/걸림(소리 층이 이미 셋을 가졌다 T272) · 반입(ⓐ) · `addProduce('fish')` 제거 · 등가 자(ⓓ).
4. 나무꾼 → 광부 → 채집 → 사냥 → 농부.
