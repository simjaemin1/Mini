# 포식자 도적 배선 계획 (predator-bandit.js)

> 도적을 "기하 근접 매복(econ 추상)" → **"배회·판단·억지·요격하는 포식종(공간 실체)"** 으로.
> 코어 모듈 `server/predator-bandit.js` 완성·단위검증(5/5). 이 문서는 랩/서버 배선 + fast econ shadow 정합의 청사진.
> ★샌드박스(44s·fast 모드)로는 공간 검증 불가 — **슬로우 랩 시각/서버 존 스모크는 로컬**.

## 1. 모델 요약 (무주사위·결정론)

| 요소 | 기제 | 창발 결과 |
|---|---|---|
| **배회** | semi-camp 앵커(소굴↔교역로 사이) 주위 어슬렁 | 약탈 출발점이 점 아닌 구름 |
| **굶주림 탐색** | 굶으면 배회 반경 확장(`SEMI_R×(1+hunger×FORAGE_MULT)`), 포식 시 리셋 | **거리→약탈 빈도 연속 falloff**(계단 제거) |
| **지각·판단** | 캐러밴 지각 → 필요 규모 `needN=호위력×margin/평균atk` 산정 | 자잘=소집 적음(자주) · 대군=억지(회피) |
| **rally** | 표적 최근접 갱원 needN명 소집 | 흩어져 있어도 규모 맞춰 파병 |
| **리드요격** | 표적 진행방향 앞점 조준(늑대 요격 재사용) | 꼬리물기 아닌 가로채기 |
| **break-off** | 파티 생존 < 초기×`BREAKOFF_FRAC` → 도주 | 자살돌격 회피(억지의 전투 중판) |

단위검증(`sim/_predator-test.js`, 5/5): 결정론·억지(대군→회피)·단조(약한 호위→전손)·리드요격·**절벽 완화(빈도 램프: 20셀 55%→45셀 34%→50셀+ 0%, 포식 사거리까지 부드럽게)**.

## 2. host 인터페이스 (호스트가 제공)

```
host.getCaravans() → [ { cid, x, y(px), alive, escorts:[def], trader:def|null,
                         vx?,vy?,ang?,   // 진행 속도/방향(없으면 위치델타 추정)
                         arrived?,        // 목적지 도착 → 표적 해제(거리-빈도 창발의 핵심)
                         onDefeat(), onRepel(), onAvoided?() } ]
  def = { hp, atk, alive, lastAtk? }      // 호위/상인 몸체
host.damage(def, dmg, src)  · host.rng()  · host.terrainBlocked(px,py)  · host.removeRaider/spawnCorpse/broadcast (선택)
gang = { id, camp:{px,py}, n, tier:'light'|'veteran', routeHint?:{px,py} }   // routeHint=노리는 교역로상 한 점
```

## 3. 랩(슬로우 모드) 배선 — 마을·전쟁실험실

**전제:** 캐러밴이 **공간 이동 엔티티**여야 함(x,y 매 프레임 갱신 + 도착 시 arrived). 현재 랩은 캐러밴을 econ 추상(departDay/arriveDay)으로 처리 → **슬로우 모드용 캐러밴 몸체(호위 def 배열 포함)를 이동 표시층에 스폰**해야 함. 답압길(roadStamp)이 이미 슬로우 전용이니 그 캐러밴 몸체를 재사용 가능.

1. `predator-bandit.js` 코어 로직을 랩에 인라인(전투실험실 battle-core 인라인 패턴, 또는 슬로우 전용 `<script>`).
2. `bdtPairSync()`가 갱마다 `routeHint`=경로상 최근접점 주입(이미 `_bdtPairs`가 경로 인접 매핑을 가짐 → 그 경로의 한 점).
3. 슬로우 틱에서 `predator.tick(dtMs,nowMs)` 호출(updateBandits `if(slow)` 옆). 캐러밴 몸체 x,y는 이동층이 갱신.
4. 결과 콜백 → econ 훅(`onBanditLoot`) 재사용: onDefeat=화물 부분손실(완화 모델), onRepel/onAvoided=무손실.
5. ★양쪽 랩 수동 동기(life 코드 중복 — 메모리 [[durango-bandit-raid-phase2]] 랩 아키텍처 주의).

## 4. 서버(zone.js) 배선

- `bandit-raid.js` → `predator-bandit.js`로 교체(동일 host 계약 + `arrived`/`onAvoided`/`routeHint` 확장).
- zone이 실제 이동 캐러밴 엔티티(호위 몸체=경비 NPC) + `damagePlayer`를 host.damage로 래핑 제공(이미 bandit-raid.js가 하던 것).
- `ENABLE_BANDIT_RAID` 게이트 유지(점진 롤아웃).

## 5. ★fast econ shadow 정합 (중요)

공간 모델이 정본이 되면, **fast econ(economy-sim-v2 `banditGang`/`_raidScrum`)은 그 집계 약탈률의 근사 shadow로 유지**한다(빨리감기·@1500 회귀는 공간이 없어 공간 모델을 못 돌림).
- 정합 방법: 공간 모델을 여러 소굴거리·호위규모로 배치서 돌려 **거리별 약탈률·격퇴율 곡선**을 뽑고(위 ⑤ 스타일), 그 곡선을 fast econ의 `expectedLossRatio`/`banditGang` 확률에 피팅.
- 즉 fast는 "공간이 이 배치서 평균 이만큼 약탈한다"를 재현하는 축약. 두 층이 같은 통계를 내도록 주기적 대조.
- 이미 있는 **위험 인지 발주**(econ-v2:566)는 그대로 — 공간이든 shadow든 상인은 `banditGang`/`banditRouteRisk`로 EV 판단.

## 6. 튜너블 노브 (predator-bandit.js 상단)

`DETECT_PX`(지각), `SEMI_R`(배회), `FORAGE_MULT`(굶주림 최대 사거리 — **falloff 폭 결정**), `HUNGER_RATE`(굶는 속도 — 빈도), `COMMIT_MARGIN`(억지 문턱), `BREAKOFF_FRAC`(도주 문턱), `ROUTE_BIAS`(경로 편향), `LEAD_MAX`(요격 리드).

## 7. 검증(로컬)

1. `node sim/_predator-test.js` — 코어 단위검증(5/5, 샌드박스 가능).
2. 슬로우 랩 시각: 도적이 소굴서 나와 캐러밴 요격·대군 회피가 눈에 보이는지.
3. 존 스모크: `ENABLE_BANDIT_RAID` 켜고 실서버 캐러밴 습격 관찰.
4. shadow 정합: 공간 거리별 약탈률 곡선 ↔ fast econ 약탈률 대조(§5).
