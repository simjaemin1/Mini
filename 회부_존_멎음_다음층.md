# 회부 — 존 멎음: 켤까 · 부팅 25초 · 남은 실측 [2026-08-31 밤]

근거·계수·A/B 전부는 **`보고_존_멎음_원인과_수리.md`**. 여기엔 **내가 혼자 정하면 안 되는 것**만 적는다.

---

## A. `TERRAIN_TILE_CACHE` 를 실운영에 켤까 · 기본값을 바꿀까

**무엇**: `zone.js` 의 `isWaterTileLocal`/`isRockTileLocal` 결과를 타일당 4비트로 기억한다.
지형 원천은 기동 1회 적재 후 불변이라 무효화 규약이 없다.

| | |
|---|---|
| 효과 | 게임일 틱 중앙값 **966ms → 171ms** · 멎음 **37회 → 4회** · 벽시계 멎음 비율 **45.1% → 0.9%** |
| 비용 | 메모리 **8.5MB** (존 힙 8GB 대비) |
| 위험 | 등가성은 하네스 21/0 으로 증명(재질문 섞은 10,500회 전부 일치 · 참·거짓 양쪽 확인) |
| 되돌리기 | env 하나 지우면 끝. 끄면 배열조차 안 만들고 종전 코드 경로 그대로 |

**선택지**

1. **env 로만 켠다** — `docker-compose` 의 zone 서비스에 `TERRAIN_TILE_CACHE=1` 추가.
   기본값은 꺼짐 유지. (되돌리기가 가장 싸다)
2. **기본값을 켜짐으로 바꾼다** — `zone.js` 의 조건을 `!== '0'` 으로. 새 존도 자동으로 혜택.
3. **아직 켜지 마라** — 실기 측정(§C) 먼저.

내 의견: **1번**. 등가성은 증명됐지만 실기에서 한 번 보고 2번으로 가는 게 순서 같다.
다만 이건 내 의견일 뿐이고, 이 프로젝트의 관례상 손잡이 기본값은 재민 확정 사항이다.

---

## B. 부팅 25초를 다음 배치에서 다룰까

`SimVillages.init` (villages.js:1965) 가 **24.6초를 동기로** 쓴다(프로파일 실측).
캐시를 켜도 그대로다 — 부팅은 전부 미적중이라 당연하다.

* **영향**: 접속에는 무해. **재시작 시간만** 늘어난다. 배포마다 25초 + α 동안 존이 죽어 있다.
* **왜 이번에 안 고쳤나**: 원인이 확정되지 않았고(시딩·거리행렬·영토 실물화가 섞여 있다),
  고치려면 **비동기 분할**이라는 구조 변경이 필요하다 — 밤중에 혼자 결정할 크기가 아니다.
* **물음**: 다음 배치의 일감으로 잡을까, 아니면 "재시작이 느린 건 감수" 로 둘까.

---

## C. 실운영 33초 간격 — 한 번만 재 주면 닫힌다

`Dockerfile.zone` 은 `--interval=30s --timeout=3s --retries=3`.
실패한 검사가 3초를 쓰므로 **다음 검사는 33초 뒤**다 —
관측된 `16:00:08` · `16:00:41` 은 **연속된 두 검사**이지, 33초 주기의 멎음원이 아니다.

다만 실운영 게임일은 **24분**이라 게임일 틱만으로는 24분에 한 번만 멎어야 한다.
`unhealthy`(연속 3회 실패)가 붙으려면 한 번의 멎음이 **60초 이상**이어야 한다.
로컬 최악이 8.3초(마을 18·NPC 607)였고 실운영은 **마을 51·NPC 1,569** 이니 가능성은 충분하지만
**재지 않았으므로 단정하지 않았다.**

⇒ 보고서 §4-나 스크립트를 30분만 돌려 `STALL` 줄의 **간격**을 보면 된다.
**24분 간격이면** 게임일 틱이 맞고 이번 수리로 끝. **훨씬 짧으면** 원인이 하나 더 있다.

---

## D. 내가 이미 지킨 것 (확인용)

* 제품 코드 변경은 `zone.js` **12줄 추가 1줄 변경** + 신규 모듈 1개. 게임 손잡이 튜닝 0건.
* 기본값 무변경 — env 를 안 주면 **바이트 단위로 종전 경로**다(하네스 ④가 이걸 검사).
* econ·NPC 로직 무수정. 자산 파일 무변경.
* 재민 눈이 판단할 것(소체 비례·아웃라인·1.5배 화소·애니·NPC 적용)은 **일절 손대지 않았다.**
