# durango-mini 인계 배치 16 (2026-08-04b) — 50마을 세계 전환 [재민 "해봐"]

> 새 세션 첫 메시지로 붙여넣어라. **먼저 `다음세션_인계.md` 와 `보고_50마을_처방전.md` ·
> `처방_50마을_지도오버레이.json` 을 읽고** 순서대로 전부 진행. 질문 금지, 실측 없는 보고 금지,
> 커밋 즉시 푸시. 기준: `3d7a3d5` 이후 — git log 먼저.
>
> 재민 확정: 마을은 최종 50개("해봐"). 배치 15 실측이 전제다 — 후보 51 전부 심어도 생존하고
> (실상한 50 — 어촌6 자연 스킵), 병목은 땅이 아니라 선별 게이트였다. 남서 구석 4곳
> (광산6·농촌11·농촌12·농촌21)만 칼날 위이고, **라이브 시드(1020)에선 농촌21이 800일 소멸**로
> 측정됐다. 비옥도 보강은 선별엔 안 먹지만 **시딩 후 생존(econ 땅값 = 영토 실측)엔 먹는다.**

## 불변 규칙 (요약)

- 배포·라이브 실행 재민만(`bash scripts/redeploy-hanbando.sh`, central 변경 시 `--central`). redeploy-all 금지.
- NPC 시딩 마을 소멸 0 원칙 유지 — 이 배치의 채택 기준이다.
- 지형 데이터 수정은 **이번에 한해 재민 승인**("해봐") — 다광종 마이그레이션 선례
  (`migrate-veins-polymetal.js`)대로: 전용 스크립트 · 멱등 · 백업 · 변경분 최소·국소 ·
  before/after 전수 diff 보고. **4곳 주변 외 셀은 1비트도 안 바뀌어야 한다.**
- 실화면 검증: probe-spawn/probe-roof 선례. v2 CLI 회귀 비트 동일 유지.

## 작업

### ① 프로덕션 50곳 전환 스위치
진단용 `SEED_ALL` env 가 아니라 **정식 존 설정**으로: `zone-config.hanbando.seedAll: true`(이름은
정본 판단) + `VILLAGE_MAX` 51. `pickSeedVillages` 가 존 설정을 받아 하한·간격·상한을 우회하되,
**터 미달(어촌6류) 스킵은 유지**(그건 품질 기준이 아니라 물리다). 다른 존 기본값 무변.
검증: 새 DB 로컬 부팅 → `후보 51 → 시딩 50` 로그 + 마을 수 50 저장 확인.

### ② 남서 4곳 땅 보강 — 시드 1020 이 판정한다
`scripts/migrate-terrain-sw4.js`(신설): 처방 json 의 need 값 기준으로 광산6·농촌11·농촌12·농촌21
주변의 비옥도/물 레이어를 **필요 최소만** 올린다(hanbando-terrain.json — 소스가 .src 라면 그쪽,
파이프라인 확인 먼저). 멱등·백업·드라이런 기본.
- 채택 기준: **시드 1020(라이브 시드) 800일 소멸 0/50 · 좀비 0** + 시드 42·7 도 소멸 0 +
  나머지 46곳 결과가 보강 전과 유의 차 없음(전수 비교 표).
- 1,600일 1시드(1020)로 장기도 확인.
- 안 되면 보강량을 한 단계 올려 재측정(최대 2회) — 그래도 안 되면 그 마을만 회부(억지 금지).

### ③ 스폰 이동
50곳 세계 기준으로 신규 플레이어 첫 화면에 **지붕 있는 마을**이 보이는 스폰을 고른다
(마을 광장 곁 · 건물·사유지 위 스폰 금지 · 물가 금지). probe-spawn 실화면 스크린샷이 근거.
`zone-config.hanbando.mainSquare` 갱신.

### ④ 마무리 + 라이브 리셋 런북 v3
- 전 하네스 + E2E 3종 + wiring-check + v2 CLI 회귀(비트 동일) + 50곳 세계 3시드 800일
  (**소멸 0/50 이 새 기준선**) + 실화면(스폰·보강 4곳 중 1곳·일반 마을).
- `라이브반영_런북`을 v3 로 갱신: 50곳 리셋 절차(기대 로그 "후보 51 → 시딩 50" ·
  라이브 시딩 소요 경고 — 18곳에 45분이었으니 50곳은 **~2시간**, 그동안 접속 대기) +
  구 레거시 잔재는 리셋이 지우므로 cleanup-legacy 불필요 명기.
- 종합 보고: 보강 diff 전수 · 시드 1020 생존표 · 새 기준선. 실행은 재민.
