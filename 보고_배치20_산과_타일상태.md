# 보고 — 배치 20 B: 물가 렉 수리 · 타일 상태계 · 검은 지그재그 띠 판별

> **재민이 먼저 할 것 — 라이브 반영** (public/ 이 바뀌었으므로 `--central` 필수)
>
> ```bash
> bash ~/Mini/푸시_번들.command ~/Mini/미푸시_배치20B.bundle   # 푸시 403 → 번들로 전달
> bash scripts/redeploy-hanbando.sh --central
> ```

---

## 0. 한 줄 요약

재민 제보 **"물 근처로 가니까 엄청나게 렉걸린다"** 의 진범을 분해 실측으로 찾아 고쳤고
(걷는 중 **388ms → 25ms**), 배치 20 §B 타일 상태계를 실장·검증했으며(**26/0**),
스크린샷의 **검은 지그재그 띠**는 배치 19/20 결함이 아니라 **기존 안개 시스템**임을 측정으로 확정했다.

---

## 1. 물가 렉 — 진범은 셰이더가 아니라 CPU 였다

### 1-1. 어디에 시간이 갔나 (분해 실측)

흐름/수심 텍스처는 128×128셀 창이고, 카메라가 **16셀(512px) 움직일 때마다 통째로 다시 굽는다**.
한 장 굽는 435ms 를 뜯어 보니:

| 항목 | 시간 | 비중 |
|---|---|---|
| **물 판정** `Terrain.isWaterCellLocal` × 16,384셀 | **399ms** | **95%** |
| 흐름 방향 `_flowAtCell` (강 구간 최근접) | 33ms | 8% |
| 나머지(거리장 BFS·텍스처 조립) | 3ms | 1% |

셀당 ~75µs. 강·호수 전체를 셀마다 훑기 때문이다.
⇒ 512px 걸을 때마다 **메인스레드가 0.4~1.4초 정지**한다. 그게 재민이 느낀 그 렉이다.

### 1-2. 왜 못 고치는 함수인가

`isWaterCellLocal` 은 **콜라이더 정본**이다. 이동·스폰·경로·자원·econ 기준선이 전부 여기에 매달려 있다.
표준 지시대로 **한 바이트도 안 건드렸다.** 렌더 쪽에서만 세 겹으로 풀었다.

### 1-3. 수리 ①  강 구간 공간 격자 색인

`_flowAtCell` 이 셀마다 **강 구간 4,186개 전체**를 훑던 것을 1,024px 격자 색인으로 바꿨다.

- 등록은 구간의 **AABB 가 겹치는 칸 전부**. 이게 동치의 증명이다 — 최근접점 q 는 반드시
  구간 AABB 안에 있고, `|q−p| ≤ 1400`(그 밖은 흐름 가중치 0)이면 `bucket(q)` 는 탐색 범위 안이다.
- **동률은 구간 번호가 작은 쪽으로 깬다** — 폴리라인의 이웃 두 구간은 공유 꼭짓점에서 거리가
  정확히 같고 방향이 다르다. 전수 순회(번호순)와 답을 맞추려면 필요하다.

**동치 검증 7,000 표본 · 불일치 0 · 최대차 0.** 답이 한 비트도 안 바뀐다.
(성기게 표본해 등록한 1안은 5,000 표본 중 33점이 갈렸다 — 대각으로 스치는 칸을 빠뜨렸다.
 동률 깨기를 안 넣은 2안은 4점이 갈렸다 — 전부 꼭짓점 최근접 점이었다. 둘 다 실측으로 잡았다.)

### 1-4. 수리 ②  직전 창과 겹치는 칸은 다시 안 묻는다

창은 16셀씩만 움직이므로 직전 창과 **76% 가 겹친다**. 겹치는 칸은 물 판정을 베껴 온다
(같은 셀의 답은 정적이라 베껴도 정본과 어긋날 수 없다).

### 1-5. 수리 ③  남은 칸은 프레임당 시간 예산 · 가운데부터

- 예산은 **개수가 아니라 시간**(9ms). 캐시 적중은 거의 0µs 라 개수로 끊으면 싼 칸까지 막힌다.
- **창 가운데부터 바깥으로** 채운다. 위에서부터 채우면 예산이 화면 밖 여백에 다 쓰이고
  정작 눈앞의 물이 몇 프레임 빈다.
- **물어보는 반경은 40셀.** 1400×900 화면의 월드 AABB 반경은 25셀이고 원점 양자화가 8셀이라
  33셀이면 덮는다. 바깥 링까지 채우려 들면 미결이 안 끝나 매 프레임 다시 굽는다
  (**92프레임 미수렴**으로 실측해 잡았다).

### 1-6. 실측 결과 (`scripts/e2e-waterperf.js` — 15/0)

⚠헤드리스는 WebGL 을 SwiftShader(소프트웨어)로 돌아 실기보다 느리다 — 아래는 **상한**이다.

| | 수리 전(손잡이 `slowFlow`) | 수리 후 |
|---|---|---|
| 걷는 중 한 장(창 16셀 이동) | 388 ms | **25 ms** |
| 완전히 새 땅(겹침 0) | 1,355 ms | **12 ms** |
| 그린 물의 양 | 21.46 % | 21.45 % |
| 같은 자리 평균 \|Δ\| | — | 0.62 / 255 |

마지막 두 줄이 핵심이다 — 예산제는 **늦게 그릴 뿐 덜 그리지 않는다**.

### 1-7. ⚠아직 열려 있는 것 — 프래그먼트 셰이더 비용

`e2e-terrain` 성능 항목에서 물 레이어는 여전히 **141.5 ms/f** 로 찍힌다. 이건 CPU 가 아니라
**셰이더가 픽셀을 칠하는 비용**이고, 헤드리스 SwiftShader 수치라 실 GPU 값이 아니다
(초원에서는 0.02ms — scissor 는 제대로 듣고 있다).
재민 기기에서 렉이 **덜해졌지만 남아 있다면** 그건 이쪽이다. 다음 배치 후보:
셰이더 해상도 절반 렌더 후 업스케일 · 잔물결 옥타브 축소 · 프리즘 벡터화 여부.

---

## 2. 하네스가 이걸 왜 못 잡았나 (하네스 오류 7건째)

`e2e-terrain` 은 촬영을 **고정 지점**에서 했다. 흐름 텍스처는 카메라가 512px 움직여야
다시 굽는데, 안 걸으니 평생 한 번만 구웠다. **판정이 아니라 대본이 틀렸다.**

⇒ 신설 `scripts/e2e-waterperf.js` 는 **걷는다**. 그리고 걷기가 느려 A/B 두 바퀴를 못 도는 문제는
`__wfProbe` 로 **정본 `_buildFlowTex` 를 그대로 불러** 창을 옮겨 가며 잰다(하네스가 계산을 다시
쓰지 않는다). 대리 측정이 실사용과 같은 동작이라는 근거는 ⓒ 항목에서 따로 증명한다 —
실제로 걸으면 흐름 창 키가 바뀌고 다시 굽는다.

---

## 3. 배치 20 §B — 타일 상태계 (5축 레이어 합성)

> 재민: *"비옥도에 따라 모든 타일이 디자인이 바뀌어야… 번영도·경작·길·채굴에 따라서도"*
> *"경계마다 딱딱 나누지 말고 연속적으로"* · *"돌만 놓으면 그게 산터냐"*

### 3-1. 구조

| # | 축 | 원천 | 신설 여부 |
|---|---|---|---|
| 1 | 기반 = 바이옴 × 지질 | `geo` 비트(wasMountain) | **신설**(DB 영속) |
| 2 | 채굴 = 번영도 | `minedCells` **거울**(읽기 전용) | 채널만 신설 |
| 3 | 비옥도 | 동적 토양치 0..1000 | **신설** |
| 4 | 경작 | 기존 `farmland` 건물 | 확장만 |
| 5 | 답압(길) | 기존 §16 `roads` | 없음(읽기만) |

- **`public/soil-base.js` — 기준 토양치 공용 1부.** 서버가 `require('../public/soil-base.js')` 로
  **이 파일 자체**를 읽는다. 사본이 두 벌이면 서버의 "만땅 회복 → 행 삭제" 판정과 클라 그림이
  어긋난다(행은 지워졌는데 화면은 파인 채로 남는다).
- **지형 데이터 무접촉을 모듈 경계로 강제.** `server/soil.js` 는 terrain 을 직접 읽지 않고,
  zone.js 가 **정본 술어로 만든** `kindAt(cx,cy) → 'water'|'rock'|'land'` 를 주입받는다.
- **희소 저장**: 기준선에서 벗어난 셀만 행을 갖는다(`mined_cells`·`roads` 의 lazy 패턴 그대로).
  리젠은 **접근 시에만** 적분한다 — 일괄 패스 없음.
- **`sim/fertility.js` 무접촉.** 그건 econ 마을 선별 정적 정본이고 이건 렌더 상태다. 남남이다.
- 방송은 게임일 1회 `tile_state` 변경분. 토양치는 **Q=16 눈금으로 양자화**해 눈금이 바뀔 때만
  보낸다 — 안 그러면 리젠 12/일 때문에 매일 전 셀이 변경분이 된다.
- 클라는 **바뀐 셀이 걸친 타일만** 버린다. 전체 clear 는 화면 전체 재베이크라 히치가 된다.

### 3-2. 연속 수학 (시안 정본 `mock-fertility-gradient.js` · `mock-tile-axes.js` 문법 그대로)

- 텍스처 층 알파 = `smoothstep(문턱a, 문턱b, 값 + 셀노이즈×진폭)` — 균일 페이드가 아니라 **뙈기로 번진다**.
- 소품(자갈·이끼·풀포기)은 **자리별 고유 문턱**(셀 해시) + 문턱 근처에서 알파·크기 연속 증가.
  정수 개수의 계단이 없다. `Math.random()` 미사용 — 프레임마다 안 흔들린다.
- **산터 램프는 별도 축**: 기본 재질이 **암반**. 비옥도는 이끼(400~)·틈새 풀(780~)만 늘리고
  **풀밭이 되지 않는다**(상한).

### 3-3. 실측 (`scripts/e2e-tilestate.js` — 26/0)

| 판정 | 값 | 반례(없으면 자명 통과) |
|---|---|---|
| 토양치 60 주입 | \|Δ\| **27.2** · 초록 92.4% → 0.0% | 대조 상자 \|Δ\| **0.00** |
| 맨흙이 드러난다 | 휘도 76 → 100 | (이 팔레트에선 마른 흙이 풀보다 **밝다**) |
| 연속성 | 500↔520 \|Δ\| **1.33** | 500↔900 \|Δ\| **25.33** |
| **산터는 풀밭이 안 된다** | 토양치 1000 초록 **18.4%** | 같은 값 일반 지질 **93.6%** |
| 산터도 죽은 축은 아니다 | 200 → 1000 에서 0.0% → **18.4%** | |
| 답압 등급 2 | \|Δ\| **39.4** · 초록 93.1% → 0.0% | 대조 \|Δ\| **0.00** |
| 결정론 | 두 프레임 \|Δ\| **0.000** | |
| 손잡이 | `stateOff` \|Δ\| **15.4** | |
| 경작 축 원천 | NPC 마을 농지 **42셀** 실재 | |

**주입은 서버 방송과 같은 입구**(`_tsIngest`/`__roadFeed`)로만 한다 — 우회로를 시험하면
시험이 거짓말을 한다. 투영도 하네스가 다시 쓰지 않는다(`__cellScreen` 이 정본 `w2i` 를 부른다).

### 3-4. 이 하네스가 스스로 밟은 함정 3개 (전부 **계측기**를 고쳐 해결 — 판정은 안 건드렸다)

1. 시험 상자가 **마을 지붕·마당 타일** 위였다 → 모든 측정이 \|Δ\| 0.5 로 죽었다.
   ⇒ 마을 밖으로 걸어 나가 **한 화면에서 후보 12개의 속살 초록**을 재 제일 초원인 둘을 고른다.
2. 상자가 뙈기의 **바깥 사각형**이라 네 귀퉁이에 주변 초원이 섞였다(산터 초록 39%의 정체).
   ⇒ 마름모 안에 들어가는 속살 사각(±70,±34)만 잰다 → 18.4%.
3. 카메라가 보간이라 걷기 직후 미끄러져 **대조 상자까지 8.88 변했다**(국소성 판정이 거짓 실패).
   ⇒ 두 프레임이 완전히 같아질 때까지 기다린 뒤 상자를 다시 잡는다.

※ '진한 풀' 술어(`g-b>30`)도 시도했다가 버렸다 — 이 팔레트의 풀은 카키라 판별력이
3.3% vs 3.1% 로 사실상 0 이었다. **지표를 실측해 보고 버린 것**이지 판정을 느슨하게 한 게 아니다.

### 3-5. ⚠아직 원천이 없는 축 — 산 부수기

배치 20 §A-6 이 지시한 실측 결과: **서버에 바위 셀 제거(산 부수기) 메커니즘이 없다**
(`wasMountain`·`rockRemoved`·`destroyRock` 전부 0건). 그래서 `Soil.markMountainRuin(cx,cy)` 는
**그 이벤트의 규격**으로만 있다 — 메커니즘이 생기면 여기로 들어오면 된다.
산터 램프 자체는 위 실측대로 완성돼 있어서, 메커니즘이 붙는 날 렌더는 손댈 게 없다.

---

## 4. 검은 지그재그 띠 — 판별 끝 (기존 시스템)

재민이 아직 라이브로 `__terrain19.legacy=true` 를 못 봤다고 해서, **증거 스크린샷을 직접 쟀다**.

1. **띠의 색이 정확히 `rgb(0,0,0)`** 이다(화면의 23.6%). 렌더 배경은 `#0a0d10`=(10,13,16),
   UI 패널은 (15,19,23) 로 **전부 다른 값**이다. `rgb(0,0,0)` 을 칠하는 곳은 안개 마스크의
   `fillStyle='rgba(0,0,0,1.0)'` 하나뿐이다.
2. **가장자리 기울기가 행당 정확히 ±2px** 이다(경계 표본 517행: +2 가 292행, −2 가 138행,
   중앙값 \|Δx\|=2). 셀 다이아 변은 x 로 32px 갈 때 y 로 16px 가므로 기울기가 정확히 2 다.
   ⇒ **셀 단위로 양자화된 아이소 다이아 계단** = `seenChunks` 경계.
3. **안개 코드가 배치 19 직전 커밋(`0caa0af`)과 diff 0줄**이다(시야 레이캐스트·seen 마킹·
   마스크 합성 323줄 전부). 배치 19/20 은 이 경로를 한 글자도 안 건드렸다.

⇒ **결론: 기존 안개 시스템이다.** 바위가 시야를 막고(11차 확정 — "산은 완벽한 콜라이더다,
건너편이 절대 안 보인다") seen 집합이 셀 단위라, 능선 옆에 정확히 저 계단이 생긴다.

**다만 재민이 보기 싫다고 느낀 건 사실이고, 그건 정당하다.** 배치 20 A 가 산을 '장벽'으로
세우면서 그 옆의 새까만 띠가 "안 가 본 땅"이 아니라 "렌더 구멍"처럼 읽히게 됐다. 회부 안 둘:

- **ⓐ seen 경계를 부드럽게** — seen 다이아를 그린 마스크에 블러 한 번(계단이 사라진다. 비용 낮음).
- **ⓑ 미탐색을 완전 검정 대신 옅은 안개색으로** — 지형 실루엣이 어렴풋이 비쳐 "구멍"이 아니게 된다.

둘 다 안개 정본을 건드리므로 **재민 확정 후** 손대는 게 맞다(취향 갈림이다).

---

## 5. 회귀 · 검증 전수

| 검사 | 결과 |
|---|---|
| `e2e-terrain` (배치 19 지면·물) | **27 / 0** |
| `e2e-waterperf` (신설 — 물가 렉) | **15 / 0** |
| `e2e-tilestate` (신설 — 타일 상태계) | **26 / 0** |
| `e2e-rooms` | **55 / 0** |
| `e2e-cutaway` | **15 / 0** |
| `e2e-metallurgy` | **20 / 0** |
| `e2e-village` | **32 / 0** |
| `e2e-guest-reconnect` | **40 / 0** (아래 ※) |
| `lab-wiring-check` | 실패 **0** · 경고 1(랩 HTML 은 디바이스 파일이라 컨테이너에 없음 — 기존과 동일) |
| `econ-regress` (v2 CLI 5시드 800일) | **인구 2,616 · 무기Q 651 · 확장셀 13,473** — 기준선과 **비트 동일** |
| `econ-lab-real 800` (실지도 51마을) | **인구 6,280 · 소멸 0/51** — 기준선 그대로 |

※ `e2e-guest-reconnect` 는 배치 실행에서 한 번 **37/3** 이 났다. 세 실패가 전부 한 뿌리였다 —
로그에 `게스트 접속 [1회용 — central 불가]` 가 찍혔고, 그 순간 central 이 죽어 1회용 신원으로
떨어지는 바람에 "몸에 물건이 실렸다" 전제가 `undefined` 가 됐고 뒤따르는 두 판정이 같이 넘어졌다.
**단독 재실행 40/0.** 코드 회귀가 아니라 순차 러너의 포트 정리와 다음 스위트 기동이 겹친
환경 플레이크로 본다(러너가 스위트 사이에 포트를 죽이는데, 게스트 하네스는 제 central 을 따로 띄운다).
⇒ 러너 개선은 §7 로 넘긴다. 지금은 **단독 실행 결과를 정본으로 적는다.**

⚠E2E 는 포트 3010/3020 을 공유해 **동시 실행이 금지**다(겹치면 EADDRINUSE 로 거짓 실패).
`scripts/run-regressions.sh` 가 순차로만 돌린다.

---

## 6. 이 배치가 만진 파일

**신설**
- `public/soil-base.js` — 기준 토양치 공용 1부(서버·클라 같은 파일)
- `server/soil.js` — 동적 토양치·지질 희소맵 + `tile_state` 방송
- `scripts/e2e-waterperf.js` · `scripts/e2e-tilestate.js` — 신설 하네스
- `scripts/probe-wfcost.js` — 흐름 텍스처 비용 분해 진단기
- `scripts/run-regressions.sh` · `scripts/dev-boot.sh` — 순차 실행·기동 헬퍼

**수정**
- `public/client.js` — 물가 렉 수리 3겹 + 타일 상태 5축 합성 + 계측/손잡이
- `server/zone.js` — `Soil.init`(minedCells 뒤 — TDZ 함정) · welcome `soil` · 게임일 틱 · `_oreSave` 거울
- `server/zone-local-db.js` — `soil_cells` 테이블(추가 전용)
- `public/index.html` — `soil-base.js` 로드

**무접촉 확인**: `hanbando-terrain.json` · `server/terrain.js` · `sim/fertility.js` ·
콜라이더 · 자원 스폰 · econ.

---

## 7. 다음 배치로 넘기는 것

1. **`scripts/e2e-mountain.js`** (배치 20 §C) — 배치 20 A 의 장벽 세그먼트 전용 하네스.
   세그먼트가 실제 바위 셀 위에만 / 폭 정합 / 파괴 후 재계산 / 결정론.
2. **물 프래그먼트 셰이더 비용** (§1-7) — 실 GPU 측정이 먼저 필요하다.
3. **검은 지그재그 띠 처리** (§4) — 재민 확정 대기(ⓐ 블러 vs ⓑ 옅은 안개색).
4. **배치 21** — 물가 술 자연화 · 자연물 12종 재렌더 · 들꽃 산포(배치 19 C 이월분).
5. **산 부수기 메커니즘** (§3-5) — 규격은 서 있고 게임 쪽 결정이 남았다.
6. **숲산 톤이 아직 연하다** (배치 20 A 잔여).
7. **`run-regressions.sh` 의 스위트 간 격리** (§5 ※) — 게스트 하네스가 제 central 을 띄우는데
   러너가 포트를 죽이는 시점이 겹친다. 스위트마다 포트를 어긋나게 주거나, 죽인 뒤 소켓이
   완전히 풀릴 때까지 기다리는 쪽으로.
