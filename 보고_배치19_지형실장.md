# 보고 — 배치 19: 지형 실장 1 (지면 질감 · 물)

재민 확정 문법(시안 왕복 13회)을 **실게임에 이식**했다. A(지면)·B(물)는 완료, **C(자연물 재렌더)는 이월**.

---

## 0. 재민이 할 일 — 라이브 반영

```bash
bash /opt/Mini/scripts/redeploy-hanbando.sh --central
```

**`--central` 필요하다.** `server/` 는 한 줄도 안 바뀌었지만 **`public/` 이 바뀌었고 클라는
central 이 서빙한다**(판정 기준은 redeploy-hanbando.sh 머리말). 바뀐 것: `public/client.js` +
새 폴더 `public/assets/terrain/` 3장(풀·마른땅·진흙 질감). DB 스키마 0 · 마이그레이션 0 · 서버 로직 0.

**켜 보고 바로 되돌리려면** 재배포 없이 브라우저 콘솔에서:
```js
__terrain19.legacy = true     // 배치 19 이전 그림으로 즉시 복귀(그 탭에서만)
__terrain19.waterOff = true   // 물 셰이더만 끔
```

> ⚠**WebGL 이 없는 브라우저에서는 물이 종전 단색으로 떨어진다**(기능 저하 허용 — 지시대로).
> 그 경우 물밑도 진흙 대신 종전 단색이다(덮을 것이 없으므로). 이 갈림은 **타일을 굽기 전에** 확정된다.

---

## 1. 무엇을 했나

| | 종전 | 지금 |
|---|---|---|
| **지면** | 셀 다이아몬드 **단색** 하나(프레임당 ~9,000장) | 게임 카메라 각도로 구운 **씨멜리스 풀 질감** + 저주파 마스크로 뚫은 맨땅 뙈기 · iso 512×256 타일로 **한 번 구워 blit** |
| **물** | `#2a5a8a` 단색 + 틴트 | **WebGL 흐름맵 셰이더** — 하류 방향 파동 2파 + 잔물결 2옥타브 + 태양 반사 + 포말 |
| **물가** | 없음 | **수면 −5px** + **블록 프리즘 단면**(남·동 변, 벽 셰이드 문법) + 풀 넘김 립 + 물 접촉선 |
| **얕은물** | 불투명 | **투명**(wa = 0.42+0.58×수심) — 물밑은 **진흙 재질**(풀이 비치면 "반투명 풀" 사건) |
| **바다·호수** | 강과 동일 | 강에서 멀면 흐름 감쇠 → **해안 거리장 기울기**가 파도 방향 |

**지형 데이터 무접촉**: `hanbando-terrain.json` · `server/terrain.js` 판정 · 콜라이더 · 자원 · econ —
전부 무변. `server/` 도 `sim/` 도 **한 파일도 안 건드렸다**(§5-c 로 실증).
바위 셀은 **종전 색·종전 문법 그대로** 옮겨만 놨다 — 산은 배치 20(검증 세션) 영역이다.

---

## 2. 확정 문법 → 구현 대응

| 확정 문법 | 어디에 |
|---|---|
| ① 수면 = 지면 −5px · 단면은 픽셀 아님 | 셰이더가 `iy − uDrop` 의 역변환으로 수면점을 잡고, 내 자리가 뭍이면 **그리지 않는다**(면이 덮을 자리) |
| ② 얕은물 투명 · 물밑 진흙 | 지면 베이크가 물 셀에 **mud_angled** 를 깔고, 셰이더는 `wa` 알파로 그 위에 얹는다 |
| ③ 블록 프리즘 단면(벡터) | `_drawPrisms` — 물에 접한 뭍 셀의 **남·동** 변만(북·서는 뭍이 가린다). 남면 `#4a3a26`(그늘)·동면 `#61492f`(볕) = 벽 셰이드 문법 |
| ④ 흐름맵 셰이더 | rivers path 최근접 구간 방향. 700~1400px 에서 감쇠 → 하구·호수 무방향 |
| ⑤ 포말 = 시간 고정·뭍이 북서일 때만·7px | 셰이더 `vn(wl/4, 128)` 는 **시간항 없음**. N·W 이웃이 뭍일 때만, `ec/7` 로 폭 제한 |
| ⑥ 물가 술 | **이월**(§7) |
| 물가 = 각진 블록(곡선 금지) | 마스크는 **NEAREST** 텍스처, 흐름·수심만 LINEAR — 한 장으로 보내면 보간이 셀 경계를 뭉갠다 |

**씨멜리스가 성립하는 수학** (`bake-terrain-tex.py` 머리말이 원문):
`w2i` 에서 월드 (P,0)→iso(32P,16P) · (0,P)→iso(−32P,16P). 두 벡터의 **합/차** = (0,32P)·(64P,0)
⇒ **iso 공간 64P×32P 직사각형이 주기**다. 월드 산포를 주기 P 로 감아 그 직사각형을 렌더하면
canvas `repeat` 로 이음새가 없다. P=8 → 512×256px, **셀 하나가 정확히 64×32px = 화면 1:1**.
실측 이음새(반대편 변끼리의 차 vs 내부 인접 열의 차): 풀 **9.9 vs 11.0** · 마른땅 **11.1 vs 11.9** ·
진흙 **0.5 vs 0.6** — 전부 내부 변동 **이하**다.

**iso 타일로 자른 이유**: 월드 셀 청크의 iso 바운딩 박스는 마름모라 절반이 낭비고(메모리 2배),
iso 격자로 자르면 타일 원점이 항상 주기의 배수라 **패턴 오프셋이 0** 이다(setTransform 없이 무이음).

---

## 3. ★실화면이 잡은 결함 — 전부 **부동소수 정밀도**였다

증상이 셋 다 똑같았다: **"물이 흐르지도 반짝이지도 않는 뿌연 판"**. 원인은 전혀 달랐다.

1. **`worldNow()/1000` 을 셰이더에 그대로 넘겼다** = 1.7e9 초. `ADV*uT` 가 1.1e11 이 되면
   GLSL highp float(유효숫자 7자리)에서 **파동·노이즈 인자가 통째로 뭉개진다**.
   ⇒ 기준시각을 빼서 0 부터 센다.
2. **값 노이즈 해시 인자가 월드 좌표**(수만 px)라 `fract(sin(dot(p,·)))` 가 뭉개졌다.
   ⇒ 흐름맵 원점을 뺀 **국소 좌표** + **주기 노이즈**. 값 노이즈는 평행이동 불변이 **아니라서**
   원점이 512px 씩 옮겨갈 때 무늬가 튀는데, 격자를 512 의 약수 주기(스케일 8·3.2·32·4 →
   주기 64·160·16·128)로 감으면 그 이동이 **무변**이 된다.
   ★사인 위상은 크기가 커도 매끄러우므로 전체 좌표 그대로 둔다 — 여기서 국소 좌표를 쓰면
   원점이 바뀔 때 **위상이 튄다**(방향이 임의라 2π 배수가 안 된다).
3. **베이크 직사각형의 네 귀퉁이가 맨 흙이었다** — 월드 정사각형은 iso 에서 마름모로 투영된다.
   직사각형은 격자의 두 칸이고 모서리는 격자벡터 u 만큼 옮기면 마름모 안이다
   ⇒ `_wrap_corners()` 가 `(x+W/2, y+H/2)` 를 감아 읽어 채운다(합성이 아니라 **같은 장면의 다른 표본**).
   덧붙여 **Blender 절차 노이즈는 주기가 P 와 안 맞아** 바닥 판에 쓰면 타일 경계에 선을 만든다
   ⇒ 바닥은 평평한 단색, 질감은 감아 놓은 개체(풀·잔돌)가 낸다.

---

## 4. 손잡이 `window.__terrain19`

```js
__terrain19.legacy   = true     // 배치 19 전부 off (before/after 의 'before')
__terrain19.waterOff = true     // 물 셰이더만 off (지면만 보기)
__terrain19.prismOff = true     // 블록 단면만 off (단면 판정의 대조군)
__terrain19.freezeT  = 100      // 셰이더 시각 고정 (결정론 검사·시안 촬영)
```
기본값이 채택값이다(제품 UI 없음). 이게 없으면 같은 프레임·같은 시계의 진짜 before/after 를
얻을 수 없고 — `git stash` 로 만든 "before" 는 다른 세계·다른 시각이라 픽셀도 성능도 비교가 안 된다.

---

## 5. 검증

### 5-a. 신설 `scripts/e2e-terrain.js` — 실클라 픽셀 E2E

판정마다 **"없으면 실패하는 반례"**를 같이 쟀다.

| 판정 | 실측 | 반례 |
|---|---|---|
| ① **물이 하류로 흐른다** | 0.12초 뒤 물 무늬 최적 이동 **(−7, +4)** = 8.1px · 흐름축 투영 **+7.99** | 같은 시각 두 프레임 **(0,0)** · 같은 두 시각의 **뭍 (0,0)** |
| ② **얕은물 투명** | 물밑↔수면 밝기 상관 얕은 띠 **r=0.875** (n=30,486) | 깊은 데 **r=0.518** — 덜 따라간다 |
| ③ **블록 단면** | (아래 ★) | |
| ④ **지면이 질감이다** | 채택 '같은 색' 비율 dx4 **6.9%** | legacy dx4 **86.5%** → dx48 85.0%(거리와 무관하게 한 색) |
| ⑤ **결정론** | 시각 고정 두 프레임 절대차 **0.000** (물 n=89,482 · 지면 n=607,999) | |
| 계약층 | 텍스처 3종 로드 · WebGL on · rivers 구간 **4,186** · 강가 프리즘 **45** | 초원 프리즘 **0** |

★**판정 ③은 하네스가 두 번 헛짚었고, 그 과정 자체가 결과다**:
1차는 '단면 색(갈색) 픽셀'을 셌다가 **초원의 맨땅 뙈기**를 단면으로 오인했다(대조군 0.57%).
2차로 "물에서 12px 안"으로 한정했더니 **0.01%** — 알고 보니 단면 5px 안에 밑면·물접촉선·풀넘김이
겹쳐 **순수 단면색이 1px 도 안 남는다**(시안 코드의 비율을 그대로 옮겼으니 구현이 맞고 계측이 틀렸다).
⇒ 색으로 세는 것을 포기하고 **`prismOff` 손잡이로 같은 프레임에서 켜고 끈 차이**를 재도록 다시 짰다.

### 5-b. 기존 실클라 E2E 5종 회귀

> ⚠**cutaway·rooms 의 대조군 상자가 "빈 땅"이다** — 지면이 단색에서 질감으로 바뀌었으므로
> 대조군의 기저 노이즈가 올라간다. 판정 완화는 하지 않았다.

### 5-c. econ 무접촉

`sim/` 도 `server/` 도 한 파일도 안 건드렸다 — 무접촉이 정상이고, 그래도 잰다.

### 5-d. 성능

| | legacy | 채택 | 물 끔 |
|---|---|---|---|
| 강가 | 지면 3.34ms/f | 지면 **0.08** + 물 145.75 | 지면 0.01 |
| 초원 | 지면 2.53ms/f | 지면 **0.08** + 물 127.78 | 지면 0.02 |

**지면은 40배 싸졌다** — 프레임마다 셀 다이아몬드 ~9,000장을 칠하던 것이 타일 ~15장 blit 이 됐다
(굽는 비용은 그 타일을 처음 볼 때 한 번). 기대한 그대로다.

**물은 headless 에서 프레임당 130~145ms** 다. ⚠이 수치는 **SwiftShader(소프트웨어 래스터)** 값이라
실사용(GPU)과 두세 자릿수가 다르다 — **하한으로만 읽어라.** 다만 이 측정이 진짜 낭비를 하나 잡았다:
1패스는 화면 전체에 셰이더를 돌려서 **물이 한 방울도 없는 초원에서도 137ms** 를 냈다.
⇒ 흐름맵을 굽는 김에 물 셀 바운딩 박스를 같이 뽑아 그 상자를 화면에 투영해 **scissor** 하고,
화면에 물이 없으면 **draw 자체를 건너뛴다**(초원 물 비용 137 → 0.05ms/f).
배치 지시의 "물 영역만 vs 전체 오버레이 — 성능 실측으로 택1"에 대한 답이 이것이다: **물 영역만**.

---

## 6. 파일

**추가**: `public/assets/terrain/{grass,dry,mud}_angled.png` · `scripts/e2e-terrain.js` ·
`설계_물길파기_관개수로.md`
**수정**: `public/client.js`(지면 타일 베이크 · 물 셰이더 · 프리즘 · 손잡이) ·
`scripts/bake-terrain-tex.py`(각도 베이크 3종 + `_wrap_corners` + CLI 게이트) · `.gitignore`
**무변경**: `server/**` · `sim/**` · `server/hanbando-terrain.json` · 미니맵 경로

---

## 7. ★이월 — C(자연물 재렌더)와 그 이유

**C 는 손도 안 댔다.** 밤샘 배치 지시가 "단계 경계에서 끊고 이월하라"고 못박았고, A·B 가
**확정 문법의 본체**라 거기에 시간을 다 썼다. 다음 배치가 그대로 이어받으면 된다:

1. **물가 술(fringe) 자연 군락화** — 재민 미해결 지적 그대로다: 현 시안은 "변 전체 균일 밀식 =
   일부러 심은 느낌". 숙제는 밀도 저주파 변주(빽빽/빈 구간) · 높이·색 변주 · 물가 안쪽 1~2셀
   감쇠 산포 · 갈대/부들 섞기 · **균일 간격 금지**.
   ★이식 자리는 이미 열려 있다: 물 → 프리즘 다음이 술 자리고(물 뒤에 그려야 절단선을 가린다),
   지면 데코를 **안개 마스크 앞**에 그려야 한다는 것도 이미 확인된 사실이다.
2. **나무 12종·수풀 전면 재렌더**(재민 확정 "전면 통일") — Kenney 로우폴리 → Blender 정본 씬
   한국 수종(소나무·참나무·밤나무). 1셀=1m 원칙(나무 ~3m=96px). 기존 로더 경로 유지 = 파일 교체.
3. **들꽃·풀숲 결정론 산포** — claims·farmland·roads 회피(판정은 이미 클라에 온다. 새로 만들지 마라).

**E(선택)** 도 미착수: GIF 급 동영상 검증 · 다리 밑 물 정합 · 미니맵 물색 정합.

## 8. 회부 (구현 안 함)

- **물길 파기(관개수로)** → `설계_물길파기_관개수로.md` 신설. 요지: 지형 데이터가 아니라
  **동적 레이어**(다리·환호와 같은 층) · 수위·흐름은 **취수구 거리장에서 파생**(저장 금지 —
  파생값을 또 저장하면 두 진실) · 배치 19 B 의 셰이더에 **마스크 OR 만** 하면 되므로 렌더 비용 0 ·
  그리핑 방지 4규칙 · ★§4(논 생산 결합)와 §5(그리핑)는 **재민 결정**이 있어야 착수 가능.
- **다른 존 25곳은 눈으로 확인하지 않았다.** 지면 질감은 존 `groundColor` 를 알파 0.18 로 얹어
  존 정체성을 남겼지만, 사막(`#d4b97a`)·설원(`#8a9a9a`)에서 초록 풀 질감이 맞는지는 미검증이다.
  한반도 단독 운영이라 이번엔 안 건드렸다 — 존별 질감이 필요하면 별도 판단.
- **산(山)** — 배치 20(검증 세션) 영역. `isRockCellLocal` 렌더 경로는 종전 색·문법 그대로 옮겨만 놨다.
  ⚠다만 **같은 타일 루프를 구조적으로 재편했으므로 병합 충돌은 확정적이다**. 바위 분기는
  한 곳(`_bakeGroundTile` 안 주석 박힌 블록)에 모아 뒀으니 그 블록만 가져가면 된다.
