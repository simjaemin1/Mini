# 배치 보고 — 회부 5건 승인분 (2026-08-02b)

> 재민: "다 하자" — 회부 5건 승인. "질문 말고 판단해서 진행, 실측 없이 보고 금지, 커밋 즉시 푸시."
> 커밋: `94402e8`(①) → `b6a5401`(②③④⑤ 코드) → (이 보고). 전부 푸시 완료.

---

## 0. 결론 한 줄

**5건 중 3건 채택 · 1건 실측 기각 · 1건 설계 완료.**
그리고 이번 배치의 핵심 발견은 이것이다 — **주조가 2/19 였던 문제를 푼 것은 승인받은 광산촌 예외(②)가
아니라, 같이 승인된 원석 조성 전파(③)였다.** ②는 오히려 세계를 나쁘게 만들어 실측으로 기각했다.

| 항목 | 결과 | 근거 |
|---|---|---|
| ① DB 마이그레이션 | **완료** | 멱등 실측(1회차 보충 · 2·3회차 0) |
| ② 광산촌 예외(부얼타운) | **★기각** | 3라운드 3시드 — 인구 −25% · 소멸 +75% · 광산6 3/3 시드 소멸 |
| ③ 수입 원석 조성 | **★채택** | 소멸 −50% · 주조 +71% · 원석 적체 −57% · 인구 무변화 |
| ④ 철검 위세축 | **완료** | 세계 재고 0.3→90.9 · 300→5.0 (18배 감쇠, 자동) |
| ⑤ 설계안 3종 | **완료** | 구현 금지 유지 |

---

## 1. ① DB 마이그레이션 — 리셋 없이 부존만 소급

`scripts/migrate-db-endowment.js`

- **미리보기 기본**, `--apply` 로 적용(자동 백업 — `-wal`·`-shm` 까지).
- **멱등 실증**: 같은 DB 에 3회 실행 → 1회차 보충(stone 13·wood 23·ore 1), 2·3회차 **0**.
- 목표치를 스크립트에 베끼지 않는다. **`createVillage` 를 실제로 불러** 읽는다 —
  엔진이 공식을 바꾸면 스크립트가 따라간다.
- **창설 인구(8명) 기준**으로 잰다. 지금 300명 마을에 300명분 초기 재고를 부으면
  마이그레이션이 아니라 치트다.

### ★1차 구현을 스스로 뒤집은 것

처음엔 `land` 를 현재 지도에서 **재추출해 덮어썼다.** 실측해 보니 **18곳 전부 fertility 가 달라졌다.**
마을이 변한 게 아니라 **잣대가 다른 것**이었다 — 시딩 때는 실제 영토(`layout.territory`)를 훑고,
여기서 쓸 수 있는 건 영토 없는 **근사 폴백**뿐이다. 그대로 뒀으면 **좋은 데이터를 나쁜 것으로 바꾸는**
회귀가 됐다. ⇒ 기본은 **빠진 키만 채운다**(옛 DB 의 없는 `oreMix` 등). 전면 재추출은 `--reland` 로 분리.

### `DB리셋_절차.md`

잃는 것을 테이블 단위로(★플레이어 건축물·사유지는 zone DB, **계정·인벤은 central DB 라 생존**) ·
얻는 것 · **리셋 vs 마이그레이션 비교표** · 절차(세어 보기 → 정지 → 백업[**-wal 필수**] → 삭제 →
재기동 → 확인) · 롤백 · 대안 3안. **실행은 재민만.**

★스크립트가 스스로 밝히는 한계: **자리 선별(부유 시딩)은 소급 불가**다. 이미 서 있는 마을을
지울 수 없기 때문이다. 소멸 −77% 중 **부존 몫만** 소급되고 **선별 몫은 리셋에서만** 발효한다.

---

## 2. ② 광산촌 예외 — **★실측 기각** (3라운드)

재민 승인은 조건부였다: *"재민 원칙 '존재하는 마을은 전부 부유'가 우선이다 — 살아남는지
3시드 800일 실측이 채택 조건."* 그 조건이 안 채워졌다.

### 라운드별 실측 (실지도 800일 · 3시드 평균)

| 라운드 | 내용 | 인구 | 소멸 | **주조** | 무기 | 갑옷 |
|---|---|---|---|---|---|---|
| 기준 | 예외 없음 | **1,630** | **2.67** | 2.33 | 1,119 | 84 |
| 1차 | 예외만(광산6·광산1 시딩) | 1,556 | 4.33 | **2.00** | 1,064 | 86 |
| 2차 | +식량 부존 +농사 탈출구(광범위) | **1,126** | 4.67 | 6.00 | — | — |
| 3차 | +정밀 타깃(부얼타운만) | 1,230 | 4.67 | 5.67 | 759 | 32 |

- **1차**: 목표였던 주조가 **오히려 줄었다**(2.33 → 2.00). 인구 −4.5% · 소멸 +62%.
- **2차**: 더 나빠졌다. 원인을 찾았다 — "식량 부양력 하한 미달"로 잡으니 **너무 많이 잡혔다.**
  선별 20곳 중 절반이 부양력 1.3~2.0 구간이라, 거기 식량을 얹고 농사 탈출구를 열자 세계가 무너졌다.
- **3차**: `isBoomtown` = 부양력 <1.2 **AND** 광맥점수 ≥1.0 (둘 다)로 좁혔다. 광산6·광산1 만 잡힌다.
  그래도 인구 1,230 · 소멸 4.67.

### ★부얼타운 자신이 죽는다

| 시드 | 광산6(구리 90%) | 광산1(납85/은15) |
|---|---|---|
| 1020 | **소멸** | **소멸** |
| 7 | **소멸** | pop 7 · 광부 3 |
| 42 | **소멸** | pop 2 · 광부 0 |

**광산6 은 3/3 시드 전부 소멸했다.** 식량을 45일치 더 지고 오고, 농사 탈출구까지 열어 주고,
광맥이 세계 최고(구리 90% · 부존 1.13)인데도 그렇다.

⇒ **기각.** `BOOMTOWN`/`BOOMFIT`/`BOOMGATE` 손잡이는 남기되 **전부 기본 OFF**.
   지도에 철이 들어가거나(설계_NPC철기산업_지도) 교역이 두터워지면 다시 잴 값이 있다.

> 이 결과를 재민 승인에 반하는 것으로 보지 않는다. 승인은 "부얼타운을 허용하자"였고 조건은
> "부유해야 한다"였다. 조건이 안 채워졌으니 승인의 취지대로 물린 것이다.
> 그리고 **승인의 원래 목적(주조 확산)은 ③이 달성했다.**

---

## 3. ③ 수입 원석 조성 — **★채택** (이번 배치의 성과)

### 무엇이 문제였나

econ 의 `ore` 는 **조성 없는 스칼라**였고 조성은 땅(`land.oreMix`)에만 있었다.
그래서 남이 실어다 준 원석은 "무엇이 든지 모르는 돌"이라 **영원히 못 녹였다**
(어촌2 281 · 임업3 404 가 묶여 썩고 있었다). 물리적으로 틀렸다 — 광석은 실어 와도 그 광석이다.

### 무엇을 했나

- 마을마다 `_oreMixEff` = 곳간 원석의 **무게 가중 평균 조성**
- 광부가 캐면 자기 땅 조성으로, **캐러밴이 부리면 출발 마을의 유효 조성**으로 폴드
  (들어올 때만 — 제련·부패는 비례 소모라 조성 불변)
- 읽는 쪽은 전부 `oreMixOf(v)` **하나**(사본 금지). v2 가격 상한도 v1 함수를 부른다
- ★**광부 한계가치만은 `land.oreMix`** — 수입 금맥이 내 철광산의 채굴 유인을 올리면 그건 오류다
- `serializeEcon` 자동 영속 · `ORE_MIX_EFF=0` 으로 채택 전 재현

### 실측 (단독 A/B · 실지도 800일 3시드)

| | 인구 | 소멸 | **주조** | **원석 적체** | 무기 | 갑옷 |
|---|---|---|---|---|---|---|
| 기준 | 1,630 | 2.67 | 2.33 | 1,557 | 1,119 | 84 |
| **③ 채택** | 1,610 | **1.33** | **4.00** | **668** | 1,170 | 88 |
| 변화 | −1.2%(잡음) | **−50%** | **+71%** | **−57%** | +5% | +5% |

**목표 둘(적체 해소·주조 확산)을 다 달성하고 부작용이 없다.**

### 덤으로 드러난 것 — 원료가 아니라 **제품**이 움직인다

| 최근 400건×3시드 | 구리 | 주석 | 원석 | **무기** |
|---|---|---|---|---|
| 기준 | 1회/100 | 6회/397 | 13회/693 | 16회/839 |
| ③ 채택 | 0회 | 6회/76 | **6회/171** | **21회/1,027** |

받은 마을이 녹일 수 있게 되자 **원석이 산지에서 바로 소화된다.** 그래서 원료 교역이 줄고
완성품 교역이 늘었다 — 청동기 교역의 실체(잉곳·완제품)와 정확히 같은 방향이다.

### 하네스 `scripts/test-oremix.js` (6절 · PASS)

폴드 산술 · 우선순위와 손잡이 · **★수입 원석이 실제로 녹는가** · 조성 불변 · 직렬화 · 가격 상한.

> ★또 한 번 **하네스가 없는 결함을 보고**했다: "조성 없는 원석은 녹지도 않는다"고 단언했는데
> 틀렸다. 막혀 있던 자리는 제련 **행위**가 아니라 **채용 게이트**(`smeltTarget = 0` → 대장장이
> 미채용)다. `_trySmelt` 를 손으로 부르면 하천 사철 폴백이 돈다 — 별개 경로다.
> (지난 배치의 교훈 "기준을 먼저 검증해라"가 이번에도 걸렸다.)

---

## 4. ④ 철검 위세축 — 완료

### 설계 — 성능축은 손대지 않는다

연철검 0.75 < 청동검 1.09 는 **고증이 맞다.** 대신 **보유축**을 연다:
청동기 사람에게 철검은 처음 보는 물건이고, 처음 보는 물건의 값은 성능이 아니라 위세가 매긴다.

- `iron_relic`(철제 위세품) — `category: gem` · `contributes: { prestige: 1.2 }` · `ORNAMENTAL` 등재
- **주괴·정광엔 프리미엄 0.** 완성품만이다(재민 확정). 하네스가 `iron`/`iron_ore`/`meteoric_iron`
  이 ORNAMENTAL 이 **아님**을 검사한다
- ★**NPC 는 생산하지 않는다** — `economy-sim.js` 전체에 `iron_relic` 언급 **0건**(하네스 검증).
  세상에 들어오는 통로는 **플레이어 판매 하나**뿐
- 유령 수요가 아닌 근거: 위세재는 "쓰지 않는 재화"가 아니라 **보유 자체가 효용**이다
  (2026-08-01 "모든 기본 비축 목표 없애라"가 ORNAMENTAL 을 남긴 그 근거)

### ★희소 감쇠 — 별도 장치를 만들지 않았다

v2 의 `_worldStockOf` 유효수요 상한이 이미 세계 재고에 비례해 목표를 깎는다. 실측:

| 세계 재고 | 0.3 | 30 | 300 |
|---|---|---|---|
| 그 마을이 매기는 값 | **90.9** | 20.0 | **5.0** |

**세계 최초의 철검은 90.9, 300번째는 5.0 — 18배 감쇠가 자동으로 일어난다.**
그러면서도 `LUX_ADJ_MAX` 상한 덕에 폭주하지 않는다(마을이 식량을 쏟아붓지 않는다).

### econ 접점 — 딱 하나

`SimVillages.lifeSellIronRelic()` 하나. 쓰기는 **두 줄**(위세품 +1 · 대금 차감).
생산·직업·인구를 건드리지 않는다(하네스 검증). 값은 **v2 그림자가격을 그대로** 쓴다(사본 금지).
대금은 **마을이 실제로 가진 것**에서만 나오고, 1인 20일치 식량은 남긴다(곳간을 비워 굶기지 않는다).

재질 판정: 철·운철 완성품만. 주조품은 **철 비중 과반**이어야 철기다(Fe70/Cu30 ○ · Fe40/Cu60 ✗).
클라: 판매 칸에 「용해」와 「위세품 판매」가 나뉘고, 철기에만 후자가 뜬다.

하네스 `scripts/test-relic.js` 6절 PASS.

---

## 5. ⑤ 설계안 3종 (구현 금지 유지)

### `설계_강제소개_이주폐지_정합.md`

**승인은 났지만 지금 코드를 그냥 켜면 이주 폐지 결정을 되돌리는 것**이다 —
`tickForceEvacuation` 은 `npcs.pop()/push()` 로 **순간이동**한다.
⇒ **"폐촌 행렬"** 설계: 화물이 사람인 캐러밴. 걸어가고, 며칠 걸리고, **약탈당한다.**
econ 접점 6곳 목록 · 손잡이 `FORCE_EVAC=0` 기본 · 열린 질문 4개.
★내 권고: 소멸은 이미 −50~55% 줄었다. 이 기계의 값은 **소멸을 막는 것이 아니라 소멸에
서사를 주는 것** — 그 값을 인정하면 만들고, 아니면 (라)안이 여전히 유효하다.

### `설계_배합과교역_한단위_통합.md` (갱신 완료)

③ 채택 뒤 다시 쟀다. **결론은 그대로 — 오히려 더 확실해졌다.** 금속 원료 교역이 오히려 줄었다
(원석이 산지에서 소화되므로). ⇒ **이월 유지**, 다만 이월 조건을 못박았다:
주조 마을 5곳 초과 & 원료 교역 20회 초과 / 플레이어 철 판매 유의미 / 철 산지 승격 — **셋 중 하나**.
(가)안의 구현 형태(`opp(k) = max(p_local, 최고 순수출가)`)까지 적어 뒀다 — 남은 건 시점 판단뿐.

### `설계_NPC철기산업_지도.md`

★실측: 주요 광맥 63개의 광종은 납 17 · 구리 17 · 금 9 · 흑요석 8 · 옥 6 · 주석 6 — **철 0개.**
그래서 시대를 열어도 NPC 경제가 0.0% 변한다(= **지금 시대를 언제 열어도 안전하다**).
★**자잘 철광맥 65개가 이미 마을 노동권(150셀) 안에 있다** — 좌표를 새로 그릴 필요가 없고
`minor` 플래그만 떼면 된다. 후보 표(광맥·반경·품위·최근접 마을·거리) 첨부.
**추천: 광맥638(pk 0.729 · r288) 하나만 승격** — 최근접이 광산6(구리 90%)이라
"구리와 철을 함께 캐는 광산촌"이 된다. 하나만 여는 이유는 **희소성이 설계의 골자**이기 때문.
⚠승격은 **DB 리셋과 묶인다**(마을 `land.oreMix` 가 이미 굳어 있다).

---

## 6. 최종 상태와 검증

**채택된 기본값**: `LANDFIT=1` · `SMELT_CAP=0.05` · `PEACE_W=0.03` · **`ORE_MIX_EFF` ON** ·
`BOOMTOWN=0` · `BOOMFIT=0` · `BOOMGATE` OFF (전부 `lab-wiring-check [A2]` 가 검사)

| 검증 | 결과 |
|---|---|
| 실지도 800일 3시드(최종 구성) | 인구 **1,610** · 소멸 **1.33/19** · 주조 **4.00** · 원석 668 |
| v2 CLI 5시드 회귀 | 인구 2,001 · 갑옷 198 · **소멸 0/30** — 채택 전과 **완전 동일** |
| 하네스 10종 | alloy · valuechain · mining · armor-cap · picker-tables · furnace · assay · building-anchor · **oremix** · **relic** 전부 PASS |
| 번들 | `--check` 재생성 일치 · era 축·process shim·손잡이 포함 |
| 배선 검사 | **실패 0 · 경고 0**(랩 인라인 포함) |
| 랩 2종 | 재인라인 + 헤드리스 스모크 **콘솔 에러 0** + 디바이스 동기 완료 |

> v2 CLI 회귀가 **한 자리도 안 변한 것**이 ③의 설계가 옳다는 방증이다 —
> CLI 마을엔 `oreMix` 가 없어 폴드가 애초에 안 돈다(변할 이유가 없다).

## 7. 재민 결정 대기

| # | 문서 | 결정할 것 |
|---|---|---|
| 1 | `DB리셋_절차.md` | 리셋 / 마이그레이션만 / 마을만 재시딩(새 코드) / 새 존 |
| 2 | `설계_NPC철기산업_지도.md` | 광맥638 승격 여부 — **철기가 청동을 대체해도 되는가**가 본질 |
| 3 | `설계_강제소개_이주폐지_정합.md` | 폐촌 행렬을 만들 값이 있는가(소멸 방지 아님 · 서사) |
| 4 | `설계_배합과교역_한단위_통합.md` | 이월 유지(권고) |
| 5 | ② 부얼타운 | 기각 수용 / 다른 조건으로 재시도 지시 |
