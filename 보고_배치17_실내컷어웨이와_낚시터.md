# 배치 17 (2026-08-04c) — 실내 컷어웨이 · 밤 어부 · 낚시터 방황

> **재민에게 — 라이브 반영 한 줄**
> ```bash
> bash scripts/redeploy-hanbando.sh --central
> ```
> `--central` 이 **필요하다**(`server/central.js` 를 고쳤다 — 프로토 시뮬 v1 지연 시작).
> DB 리셋 불필요 · 마이그레이션 없음 · 지형 파일 무변경. 되돌리기는 커밋 revert 하나면 된다.

기준: `724c9db` 이후. 재민 실플레이 관측 2건(①②) + 이월 1건(③) + 마무리(④).

---

## ① 실내 컷어웨이 — "건물 안에 들어가도 내부가 안 보인다"

### 재민 원문
> "건물 안으로 들어가도 내부가 안 보이네.. 저번에 동쪽 벽과 남쪽 벽이 근처로 가면 투명해지도록
> 한 걸 바꾼다 했는데, 제대로 작동이 안 되네.. 좀보이드처럼 해야 하는데"

### 원인 — 좌표계가 어긋나 있었다(추측 아님 · 4곳 전수)

컷어웨이 판정은 **"내가 이 건물 사각형 안에 있나"** 다. 그런데 건물 사각형
(`data.hut` · `bld` · `gran`)은 **존 로컬 셀**인데, 판정에 쓰던 내 위치
`myAbsPredicted` 는 **월드 절대 픽셀**이었다. 한반도 존의 원점은 (409984, 49984)라
내 셀 번호가 실제보다 **12,812셀** 크게 나온다 — 어떤 사각형에도 들어갈 수 없다.
그래서 "안에 들어가도" 판정이 **한 번도 참이 되지 않았다.**

같은 착오가 `public/client.js` 네 곳에 있었고 전부 고쳤다:

| 자리 | 종전 | 수리 |
|---|---|---|
| 큰집 지붕 제거 판정 `_mbx/_mby` | 절대 px → 셀 | **존 로컬** px → 셀 |
| 움집 지붕 제거 판정 `_mcx/_mcy` | 절대 px → 셀 | 같음 |
| 실내 NPC 가림 `_ncx` | 절대 좌표로 셀 계산(NPC `pos` 는 로컬) | `pos` 그대로 로컬 |
| 벽 눕히기(동·남) | 위 셀 값을 그대로 사용 | 렌더러블에 존 원점(`off/offY`)을 실어 매 건물마다 로컬 셀 재계산 |

마지막 줄이 핵심이다. 존이 여럿이면 건물마다 원점이 다르므로 **건물이 들고 있는 원점**으로
그때그때 환산해야 한다(한 번 계산해 돌려 쓰면 다른 존에서 또 어긋난다).

### 증거 — 실화면 + E2E 2층

* 실화면(`증거_배치17/컷어웨이_*.png`): 큰집·움집 **안**에 서면 지붕이 사라지고 동·남 벽이 눕고
  안쪽 침상·화덕이 보인다. 같은 건물을 **밖**에서 보면 온전하다.
* `scripts/e2e-cutaway.js` **15 통과 / 0 실패** (신설 하네스, 진짜 Chromium)
  * 계약층: `window.__cutawayDbg` 로 "내 로컬 셀이 그 사각형 안"이 실제로 참인지
  * 화면층: 지붕 상자의 **이엉 픽셀 비율** — 밖 11.6% → 안 1.5%
  * ★자명 통과 금지: "밖 화면의 지붕 상자에 이엉이 **실제로 있다**(11.6% > 6%)"를 먼저 assert 한다.
    (1차 작성은 상자 평균색 24.5 vs 23.3 을 비교했다 — 통과해도 아무 뜻이 없는 검사여서 버렸다.)

---

## ② 밤 어부 · 낚시터 방황

### 재민 원문
> "npc가 밤인데도 밖에서 낚시하고, 낚시터에서 미세하게 자꾸 방황하는데, 왜 그럴까?"

두 증상은 **원인이 다르다.** 방황은 규명·수리했고, 밤 어부는 **재현되지 않았다.**

### ②-가. 낚시터 방황 — 원인 셋이 겹쳐 있었다 (수리 완료)

| # | 원인 | 자리 | 수리 |
|---|---|---|---|
| 1 | **매 결정 틱마다 목표를 ±20px 로 새로 뽑았다.** 20px 는 한 셀(32px)도 안 되는 거리라 도착 전에 목표가 또 바뀐다 — 영원히 제자리 달달거림 | `villages.js` 어부 분기 | 자리를 정하면 **9~17초 머문다**(체류는 시간만 센다) |
| 2 | **그 ±20px 가 물에 빠질 수 있었다.** 어장 앵커는 '4방에 물이 있는 물가 셀'이라 셀 반폭만 벗어나도 옆이 물칸 → 도달 불가 → unstuck 이 ±80px 로 튕겨 냄 | 같은 곳 | 자리를 **물가 셀 목록(bank)** 에서만 고른다(영토 셀 = 정의상 뭍) |
| 3 | ★**도착해서 서 있는 것을 stuck 으로 셌다.** 1.5초마다 참이 되고 3연속(≈4.5초)이면 `unstuckNpc` 가 `npc.x + cos(랜덤)×80` 으로 튕겨 낸다 — **가만히 서는 모든 NPC** 에 걸리는 엔진 층 결함 | `zone.js detectStuck` | 목표에 도착(경로 소진 + 12px 이내)했으면 stuck 아님 |

3번은 어부만의 문제가 아니다. 취침·대기 중이라 서 있어야 할 NPC가 전부 4.5초마다
무작위로 80px 튀고 있었다.

**계측 — `scripts/probe-nightlife.js` (3초 간격 · 어촌1 활성 청크 · 서버 `/lifedbg` 실상태)**

| 단계 | 표본 | 이동거리 중앙 | 목표가 바뀐 비율 |
|---|---|---|---|
| 수리 전 | 391쌍 | 12.8px | **100%** |
| 1차(체류 + 도착하면 재추첨) | 453쌍 | 13.0px | 99% ← 무효 |
| 2차(bank 셀에서만) | 96쌍 | 37.4px | 48% |
| 3차(체류 = 시간만) | 344쌍 | 15.5px | 37% |
| **4차(detectStuck 수리)** | 255쌍 | **0.0px** | **22%** |

마지막 22% 는 결함이 아니라 **설계값**이다 — 체류 9~17초(중앙 13초)를 3초 간격으로 재면
3/13 ≈ 23% 가 이론값이고, 실측 22% 가 거기 붙었다. 낚시는 드리우고 기다리다 이따금 자리를 옮기는 일이다.

3차에서 남은 37% 의 정체도 수치로 갈랐다: 목표 좌표가 셀중심(=어장 후보)이 **아닌** 표본
29개가 전부 자기 위치에서 반경 ≤80.6px — `unstuckNpc` 의 무작위 튕김이었다
(목표가 바뀐 126쌍 중 29쌍 = 23%). 그래서 3번을 찾았다.

**실화면 전/후 — `증거_배치17/낚시터_방황_전후.gif`** (왼쪽 빨강 = 수리 전, 오른쪽 초록 = 수리 후)

같은 DB · 같은 자리(어촌1 낚시터) · 같은 낮(12시~13시대) · 1초 간격 20프레임.
전은 `git worktree` 로 뽑은 **수리 전 트리(`724c9db`)의 서버**를 그대로 띄워 찍었다.
계측기를 같게 하려고 클라(central 이 서빙하는 `public/`)는 양쪽 다 현재 트리를 썼다 —
방황은 서버 층 결함이고 클라는 관측기일 뿐이다.

| | 낚시 중 어부: 프레임 간 이동 중앙 | 움직인 프레임 | 전체 NPC(대조군) |
|---|---|---|---|
| 수리 전 | 15.0px | **90%** | 49.9px · 96% |
| 수리 후 | 1.2px | **47%** | 49.2px · 73% |

★대조군이 중요하다: 일반 NPC의 보행(≈50px/s)은 **그대로**다. 수리가 "다들 얼려서" 통과한 게 아니다.

### ②-나. 밤 어부 — 재현되지 않았다 (수리 없음 · 회부)

**실측:** 하루를 180초로 줄여 밤을 여러 번 지나며 `/lifedbg` 를 훑었다.

* 전 마을(50곳) · 휴면 청크 포함: **밤에 밖 = 0 / 14,112 표본**
* 관측 마을(활성 청크): 0/232 · 재측정 회차 1/288 — 그 1건은 `출근` 라벨에
  **로컬 태양시 fv = 0.024**(= 그 마을의 새벽)였다. 밤 게이트가 샌 게 아니라 그 마을은 이미 아침이었다.
* 실화면: 게임 23시 18분에 어촌1 낚시터를 찍었다 — **사람이 한 명도 없다**
  (`증거_배치17/밤_낚시터_무인_23시18분.png`).

**가설 넷을 코드로 전부 배제했다:**

1. ~~"화면 조명은 경도를 무시하고 NPC 만 로컬 태양시라 갈린다"~~ — **내 전제가 틀렸다.**
   `client.js worldPhase()` 는 `_lonView`(카메라 위치 기준 0~0.045)를 **더한다**(1079·3535행).
   마을 앞에 선 플레이어의 `_lonView` 와 그 마을 `_lonOff` 는 같은 0.045 를 같은 비율로 쓰므로
   차이가 **셀 반폭 = 0.015초**다. 화면과 마을은 갈라질 수 없다.
   (다만 §19 자체는 살아 있다 — 새벽에 마을이 화면보다 먼저 깨는 폭은 마을별 1~63초/일이고,
   그 시간엔 **플레이어 화면도 같이 밝다.**)
2. ~~"휴면 청크의 NPC 가 낚시터에 얼어붙어 보인다"~~ — `AOI_RADIUS 800` < `CHUNK_ACTIVE_RADIUS 1200`.
   **눈에 보이는 NPC 는 전부 활성 청크 안**이라 반드시 스텝을 밟는다. 구조적으로 불가능.
3. ~~"집 없는 NPC 가 밤 게이트를 건너뛴다"~~ — `villages.js` 취침 게이트는 집 유무를 안 본다
   (`fv >= dayR` 면 무조건 귀가). 개인 오프셋 `_dOff`(0~3%)는 **기상만 늦춘다** — 절대 당기지 않는다.
4. ~~"액션 라벨이 낡아 [낚시] 가 남는다"~~ — 라벨은 뷰어별 최초가시 + 변경 후 1.2초(≈25틱 중복) 전송.
   25틱을 연속으로 잃어야 낡는다.

**⇒ 회부.** 못 재현했으니 손대지 않았다("없는 결함을 고치지 마라"). 다시 보이면 이 셋만 알려 주면 된다:
**마을 이름 · 화면 시계(HH:MM) · 그 NPC 라벨.** `/lifedbg` 가 이제 `lon`·`fv`·`beh`·`tgt`·`dHome`·`dBed`·`on`·`inN`
을 실어 주므로 그 조합이면 바로 갈린다.
가장 그럴듯한 남은 설명은 **이번에 고친 방황 그 자체**다 — 해질녘 귀가 중 80px 씩 튀는 NPC 는
"밤인데 아직 밖에 있다"로 보인다. 그렇다면 이번 수리로 같이 사라진다.

---

## ③ central 프로토 시뮬 마무리 — 유휴 CPU 0

배치 15 에서 캐나디아 v2 만 지연 시작으로 바꿨는데 라이브 central 이 여전히 상시 100% 였다.
원인 둘: ⓐ `--central` 없이 재배포하면 그 변경이 라이브에 안 올라간다 ⓑ **남은 v1 이 계속 돌고 있었다.**

v1(`economyWorld` · 20마을)은 **실효가 있다** — 캐나디아 아닌 존에서 클라 시세 패널(Q키)이
`/economy/prices` 를 부른다. 그래서 끄지 않고 캐나디아와 같은 **첫 요청에 시작**으로 바꿨다.
소비처 전수(3곳: `/economy/villages` · `/economy/prices` · `/economy/prices/<마을>`)에 훅을 달았다.

**실기동 검증(측정 — 로그 아님):**

```
부팅 직후   [economy] ⏸ 대기 — /economy/{prices,villages} 첫 요청에 시작
            [canadia-econ] ⏸ 대기 — /economy/canadia/* 첫 요청에 시작
유휴 60초    CPU 0.4% → 0.2% → 0.1%      (요청 0건)
첫 요청      /economy/prices → day 0 (정지 세계) 반환 + "[economy] ▶ 시뮬 시작 (요청 /economy/prices)"
5초 뒤       day 3 → day 8               (1초 = 1일로 흐른다 ✓)
기동 후 CPU  0.8% → 1.1%                 (v1 만)
캐나디아     끝까지 미기동 ✓             (요청이 없었으니)
```

손잡이(전부 미설정이 채택값):
`ENABLE_PROTO_ECON=1` 부팅 즉시 · `=0` 완전 정지 · 미설정 = 지연 시작.
`ENABLE_CANADIA_ECON` 도 동형.

대가는 하나뿐이다: **처음 Q를 누른 사람이 day 0 시세를 본다.** 존재하지 않는 20개 마을의
프로토타입 세계라는 설계 자체가 이미 회부 항목이다(`보고_50마을_처방전.md` §7-마).

---

## ④ 검증 — 전수

| 대상 | 결과 |
|---|---|
| `lab-wiring-check` | 실패 0 · 경고 0 |
| `test-alloy` · `test-valuechain` · `test-armor-cap` · `test-picker-tables` · `test-building-anchor` | 전부 PASS |
| `test-assay` · `test-mining` | PASS |
| `test-tame` | 11 / 0 |
| `test-furnace` (노·숯가마 실서버 E2E) | **85 / 0** |
| `e2e-cutaway` (신설) | **15 / 0** |
| `e2e-metallurgy` (실클라) | 20 / 0 |
| `e2e-village` (실클라) | 32 / 0 |
| `e2e-guest-reconnect` (실클라) | **40 / 0** (1차 38/2 — 원인은 아래 회부) |
| `test-villager-stuck` (★이번 수리와 같은 층) · `test-village-found` 47/0 · `test-distmatrix` 9/0 · `test-frontier-iron` 11/0 · `test-terr-backfill` · `test-granary-add/haul` · `test-guild-treasury` · `test-reanchor` · `test-relic` · `test-rock-los` · `test-ditch` · `test-bridge-path/sprite` · `test-oremix` · `test-headless-parity` | 전부 PASS |
| v2 CLI 5시드 800일 | 인구 **2,616** · 무기Q **651** · 갑옷 149 · 소멸 **0/30** · 확장셀 **13,473** — 배치 16 기준선과 **비트 동일** |
| 실지도 시드 1020(라이브 시드) 800일 | 인구 **6,280** · **소멸 0/51 · 좀비 0**(정본 `ab-summary` 기준 최종 인구 <10) · 최소 인구 15(광산7) · 거래 5,000건 · 캐러밴 92 — 배치 16 기준선(6,280)과 **비트 동일** |

> 랩이 51곳인 것은 알려진 선재 항목이다(어촌6 — `zone.js` 가 만드는 해안선 water strip 을
> `terrain.js` 가 모른다. 프로덕션은 50곳). 배치 16 §6-(가) 회부 그대로.

**돌아가지 않은 것 3개(선재 · 이번 배치와 무관):** `test-lab-market` · `test-lab-mining` · `test-lab-psite`
는 레포 밖(디바이스)에 사는 **브라우저 랩 HTML** 을 연다. 이 컨테이너의 사본은 8/3 자라 최신이 아니고,
`test-lab-market` 이 랩 쪽 임계값 1건을 찍는다. 이번 배치는 `sim/` 과 `*.html` 을 **한 줄도 안 고쳤고**
(`git status -- sim/ '*.html'` 공집합) `lab-wiring-check` 가 레포 인라인 엔진 = 번들을 확인했다.

### `e2e-guest-reconnect` 1차 실패의 정체 — 회귀 아님, 그러나 **라이브 위험**

1차 실행에서 2건이 깨졌다(`베리 undefined`). 로그를 보면 그 직전에
`게스트 접속 [1회용 — central 불가]: 여행자25 (anon_0jmm9ekk)` 가 찍혀 있다.
즉 **검사 중 WS 가 끊겼고, 재접속 순간 central 호출이 실패해 클라가 1회용 신원으로 들어갔다.**
그 몸에는 당연히 베리가 없다. 2차 실행은 그 폴백 0건에 40/0. 내 변경과 무관하다.

★그런데 이건 라이브에서도 일어날 수 있는 일이다. 배치 13·14 가 게스트 신원을 영속으로 만들었는데,
**central 이 잠깐 삐끗하면 토큰을 들고 있어도 딴 사람이 되어 들어온다**(`zone.js` 2530행 폴백).
그 세션 동안 제 사유지·마을·노의 주인이 아니게 된다. → **회부**(§5).

---

## ⑤ 바뀐 파일

| 파일 | 무엇 |
|---|---|
| `public/client.js` | 컷어웨이 좌표계 4곳 · 진단 훅 `__cutawayDbg` · `__getNpcs` |
| `server/zone.js` | `detectStuck` — 도착해 선 NPC를 stuck 으로 세지 않는다 |
| `server/villages.js` | 어부 분기(체류·bank 셀) · `lifeDebug` 진단 필드 8개 |
| `server/central.js` | 프로토 시뮬 v1 지연 시작 |
| `scripts/e2e-cutaway.js` | 신설 — 계약층 + 화면층 2단 |
| `scripts/probe-nightlife.js` | 신설 — 밤·방황 계측(하루 단축 래퍼) |
| `scripts/probe-fishing-shots.js` | 신설 — 실화면 전/후 비교(트리 지정 가능) |
| `증거_배치17/` | 실화면 증거 5점(gif 1 · png 4) |

---

## ⑥ 회부 — 재민 결정만 필요한 것 (구현 안 함)

1. **게스트 1회용 폴백** — central 이 잠깐 죽으면 토큰을 든 게스트가 딴 사람이 되어 들어온다.
   선택지: ⓐ 현행 유지(입장 우선) ⓑ 토큰을 제시했는데 central 이 불가면 **입장을 막고 재시도**
   ⓒ 존이 마지막으로 본 토큰→playerId 를 캐시해 폴백. ⓑ·ⓒ 는 "central 장애 = 입장 불가"를
   어디까지 허용하느냐의 문제라 내가 정할 일이 아니다.
2. **프로토 시세 패널** — 한반도 플레이어가 존재하지 않는 20개 마을의 시세를 본다.
   지연 시작으로 CPU 는 해결됐지만 **무엇을 보여 줄 것인가**는 그대로 남아 있다.
   (배치 15 회부 재게시 — `보고_50마을_처방전.md` §7-마)
