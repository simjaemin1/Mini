# 밤샘 배치 2 보고 — 전사·갑옷 사슬 복구, 소멸 사인 규명, 계측 위생

> 재민: "계속해. 끊지 말고 쭉. 난 자러 가야 해" (2026-08-01)
> 커밋: `cafb276` (푸시 완료). 전 하네스 PASS · v2 5시드 회귀 소멸 0/30.

## 한 것

### ① 끊김③④ 해소 — 전사·갑옷 사슬
- 실측: 캐러밴 1,572회 발송, 약탈 0 → `tradersKilled>3` 안전망으로는 전사가 구조적으로 0.
- rational picker 에 `warriorTarget`(네가 확정한 평시 상비 3%) 채용줄 추가 — legacy 동형 게이트.
- 결과(실지도 800일): 전사 0→32 · 갑옷장이 0→5 · **갑옷 0→89.3** · 무기 696→851.
  v2 5시드 회귀에서도 갑옷 183(전 시드 양수).
- 청동다움 195→68 감소는 버그 아님: 병기고 청동 상한(45%)이 차서 멈춘 것 — 수출로 재고가
  빠지면 재개되는 경계 평형(청동=소수 엘리트 표지, 설계 그대로).

### ② 마을 소멸 사인 규명 → 회부
- `tickWorldV2` 는 history 를 안 쌓아서(v1 전용) 랩에 10일 표본 계측을 달아 궤적을 떴다.
- **사인: 도구 아사.** 죽는 마을들(비옥 1.1 "좋은 땅" 포함)은 돌·나무가 바닥이라 도구가 닳으면
  재보급 불가 → 생산 ×0.25 → 태어날 때부터 기근선 아래 → 좀비 → 소멸.
- 기근 마을 식량 반출 금지 게이트를 시도 → **3시드 A/B 가 기각** → 철회(사유 코드에 명문화).
  좀비 마을은 칼날 평형이라 단일 개입이 시드 분산(소멸 6~15, 인구 242~1,051)에 묻힌다.
- 진짜 처방은 상류: `pickSeedVillages` 가 **타입 prior 만 보고 땅 품질을 안 본다**.
  → `회부_마을소멸_시딩.md` (가: 시딩에 땅 품질 / 나: 초기 부존 조정 / 다: 강제소개 — 이주
  폐지 결정과의 관계 판단 필요 / 라: 소멸을 세계관으로 수용)

### ③ 계측 위생
- v1 CLI 를 legacy 로 복귀("v1+rational" 키메라 해소 — 프로덕션 대변은 v2 CLI·실지도 랩).
- ★번들 process shim: 전사 줄이 들어가자 `warriorTarget` 의 `process.env.PEACE_W` 가
  브라우저 랩에서 처음 호출되며 ReferenceError 즉사 — CLI 하네스가 못 잡는 브라우저 지뢰.
  번들 프렐류드에서 구조적으로 차단. 두 랩 재인라인·스모크 통과·디바이스 동기 완료.
- `LAB_SEED=n` 시드 A/B 손잡이 · 랩 궤적 표본(10일) 계측 추가.
- `회부_플레이어_제련_노모델.md` — 노(爐) UX 는 설계 판단이라 이월 사유 명문화.

## 회부 대기 (네 결정)
1. **시딩 품질** (마을 소멸의 뿌리) — 회부_마을소멸_시딩.md
2. **플레이어 노 UX** — 회부_플레이어_제련_노모델.md
3. **철검 자격 문턱** max(20, N×1.5) — 시대를 열어도 철검 0(무풍). 철기 보급을 원할 때 손봐야 함
4. (기존) 끊김②의 잔재 관찰 — 주조 1/20 마을 · 원석 적체는 ①③ 해소 후 완화 중, 다음 측정에서 재평가
